---
phase: 87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix
plan: 03
type: execute
wave: 2
depends_on: ["87-01", "87-02"]
files_modified:
  - packages/fichas/src/spike/strategies.ts
  - packages/fichas/src/spike/retrieval-cli.ts
  - .planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md
  - .planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-SCORING.md
autonomous: false
requirements: [RETR-05]

must_haves:
  truths:
    - "El harness spike puede medir la RPC REAL buscar_proyectos_hibrido (no el SQL ad-hoc) sobre el golden set congelado de 86"
    - "La corrida LIVE de la RPC real está medida vs el baseline 86 (RRF 43.8/68.8/53.6, semántico 34.4/53.1/40.3)"
    - "La decisión de flip del default está registrada: si domina → default ON en commit documentado; si no → OFF + hallazgo"
    - "El golden live test permanente apunta al camino real (RPC-real) como regresión de CI"
  artifacts:
    - path: "packages/fichas/src/spike/strategies.ts"
      provides: "runRpcHibrida (SELECT sobre la RPC real vía runSql, vector parametrizado)"
      contains: "export async function runRpcHibrida"
    - path: "packages/fichas/src/spike/retrieval-cli.ts"
      provides: "4ª estrategia rpc-real sobre el mismo GOLDEN_SET"
      contains: "runRpcHibrida"
    - path: ".planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-SCORING.md"
      provides: "resultados LIVE de la RPC real + decisión de flip"
      contains: "DECISIÓN"
  key_links:
    - from: "packages/fichas/src/spike/retrieval-cli.ts"
      to: "buscar_proyectos_hibrido"
      via: "runRpcHibrida sobre GOLDEN_SET congelado"
      pattern: "runRpcHibrida"
    - from: "packages/fichas/src/spike/strategies.ts"
      to: "public.buscar_proyectos_hibrido"
      via: "SELECT ... from buscar_proyectos_hibrido(:q, :query_embedding::vector, :match_count::int)"
      pattern: "from buscar_proyectos_hibrido"
---

<objective>
Extender el harness del spike 86 con una estrategia `runRpcHibrida` que mide la RPC REAL `buscar_proyectos_hibrido` (aplicada en Plan 01) sobre el golden set CONGELADO — no el SQL ad-hoc del spike. Correr el gate de dominancia LIVE (checkpoint humano con creds `SUPABASE_DB_URL` + `GEMINI_API_KEY`), comparar contra el baseline 86, y registrar la decisión de flip: si la RPC real domina (≥ baseline semántico en NL/similares Y arregla literal/boletín), flipear `BUSQUEDA_HIBRIDA_ENABLED` default a ON en un commit documentado; si no domina, dejar OFF y registrar el hallazgo. El golden live test queda como test de regresión permanente apuntando al camino real.

Purpose: Este es el GATE de la fase. La dominancia se demostró en el spike sobre SQL ad-hoc sin unaccent (subestimado); aquí se re-verifica sobre la RPC gobernada de PROD con unaccent instalado. Sin dominancia demostrada sobre la RPC real, NO se flipea el endpoint /buscar (CONTEXT línea 40, 86-SCORING §e).
Output: harness extendido + 87-SCORING.md con la corrida LIVE y la decisión + (si domina) default ON documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-RESEARCH.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-PATTERNS.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md

<interfaces>
<!-- Harness verificado file:line en 87-RESEARCH/PATTERNS — el executor NO explora. -->

Nueva estrategia (espejo de runSemanticOnly, strategies.ts:201-218):
  const RPC_HIBRIDA_QUERY = `
  select boletin, rank
  from buscar_proyectos_hibrido(:q, :query_embedding::vector, :match_count::int)
  order by rank
  `.trim();

  export async function runRpcHibrida(
    query: string, vector: number[], opts: { runSql: SqlRunner; limit?: number },
  ): Promise<string[]> {
    const { runSql, limit = 20 } = opts;
    const rows = await runSql(RPC_HIBRIDA_QUERY, {
      q: query,
      query_embedding: `[${vector.join(",")}]`,   // serialización idéntica a runSemanticOnly:208 (V5, nunca interpolado)
      match_count: String(limit),
    });
    return rows.map((r) => r[0]!).filter(Boolean);
  }

CLI (retrieval-cli.ts:206-221 evalúa 3 estrategias vía evaluarRetrieval(GOLDEN_SET, ...)):
  agregar 4ª columna medida con runRpcHibrida sobre el MISMO GOLDEN_SET congelado, reusando getCachedEmbeddings (:25,197) y runSql intactos:
  const metricasRpc = await evaluarRetrieval(GOLDEN_SET, (caso) => {
    const vector = vectorMap.get(caso.query)!;
    return runRpcHibrida(caso.query, vector, { runSql, limit: opts.limit });
  });
  import runRpcHibrida junto a runFtsOnly, runSemanticOnly, runRrf (línea 24). Agregar la columna al reporte markdown.

Baseline 86 a dominar (86-SCORING.md):
  RRF ad-hoc: hit@1=43.8% hit@5=68.8% MRR=53.6% (unaccent=false, subestimado).
  Semántico-solo (match_proyectos): hit@1=34.4% hit@5=53.1% MRR=40.3%.
  Criterio de victoria: RPC-real ≥ semántico en NL/similares (parafrasis-nl 80% hit@5, similares 80% hit@5) Y boletín 100% hit@1 (4/4) Y literal-título mejora.

Golden set congelado: packages/fichas/src/spike/ (GOLDEN_SET 32 casos, d7bb3d3 con expected verificados LIVE). NO modificar el golden salvo decisión explícita registrada en 86-SCORING (campo nota).

Live test env-gated: vitest.live.config.ts (golden live test). Esta fase le agrega el modo RPC-real como camino de regresión permanente.

Flag a flipear (si domina): BUSQUEDA_HIBRIDA_ENABLED default. El flag vive en app/lib/busqueda-hibrida-gate.ts (Plan 02). El flip del "default" se documenta (env de PROD/CI o el comentario del gate); el flag sigue leyéndose de env — "default ON" significa configurar el entorno productivo con BUSQUEDA_HIBRIDA_ENABLED=true y documentarlo, NO cambiar la lógica fail-closed del gate.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → RPC PROD | El CLI ejecuta un SELECT read-only sobre la RPC; el vector va parametrizado, nunca interpolado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-09 | Tampering | vector/query hacia psql | mitigate | `runSql` parametriza `:q`, `:query_embedding`, `:match_count` (espejo runSemanticOnly:208); nunca string-building del vector |
| T-87-10 | Information Disclosure | flip prematuro del default | mitigate | el flip solo ocurre si la RPC real DOMINA sobre el golden (checkpoint); si no domina, default OFF + hallazgo registrado (CONTEXT línea 40) |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | NO se instala ningún paquete (extensión de harness TS existente; ver 87-RESEARCH Package Legitimacy Audit "No aplica") |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extender el harness con runRpcHibrida + 4ª columna rpc-real en el CLI</name>
  <files>packages/fichas/src/spike/strategies.ts, packages/fichas/src/spike/retrieval-cli.ts</files>
  <read_first>
    - packages/fichas/src/spike/strategies.ts:45-285 (runFtsOnly/runSemanticOnly/runRrf; SEMANTIC_QUERY :107-112; runSemanticOnly :201-218; serialización de vector :208)
    - packages/fichas/src/spike/retrieval-cli.ts (evaluarRetrieval, GOLDEN_SET, getCachedEmbeddings :25/:197, runSql, reporte markdown :206-221)
    - packages/fichas/vitest.config.ts + vitest.live.config.ts (golden live test env-gated)
    - 87-PATTERNS.md secciones strategies.ts / retrieval-cli.ts
  </read_first>
  <action>
    En `strategies.ts`: agregar `RPC_HIBRIDA_QUERY` (SELECT sobre `buscar_proyectos_hibrido(:q, :query_embedding::vector, :match_count::int) order by rank`) y `runRpcHibrida(query, vector, {runSql, limit})` espejando `runSemanticOnly` (serialización de vector idéntica a :208, `[${vector.join(",")}]`, parametrizado NUNCA interpolado). Retorna `rows.map(r => r[0]).filter(Boolean)`.
    En `retrieval-cli.ts`: importar `runRpcHibrida` en la línea de imports de estrategias (junto a runFtsOnly/runSemanticOnly/runRrf); agregar `metricasRpc = await evaluarRetrieval(GOLDEN_SET, caso => runRpcHibrida(caso.query, vectorMap.get(caso.query)!, {runSql, limit: opts.limit}))` reusando `getCachedEmbeddings` intacto; agregar la columna `rpc-real` al reporte markdown junto a fts/sem/rrf.
    Extender el golden live test (vitest.live.config.ts) para que su aserción de regresión mida el camino RPC-real (no solo el RRF ad-hoc) — el test permanente ahora protege la RPC real de PROD. Mantenerlo env-gated (SUPABASE_DB_URL + GEMINI_API_KEY); en CI sin creds, degrada a skip honesto como el patrón actual.
    NO modificar el GOLDEN_SET congelado. NO tocar runFtsOnly/runSemanticOnly/runRrf existentes.
  </action>
  <verify>
    <automated>cd packages/fichas && pnpm test spike && node -e "const s=require('fs').readFileSync('src/spike/strategies.ts','utf8'); process.exit(s.includes('export async function runRpcHibrida') && s.includes('from buscar_proyectos_hibrido')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `strategies.ts` exporta `runRpcHibrida` con el SELECT sobre `buscar_proyectos_hibrido` y el vector parametrizado (no interpolado).
    - `retrieval-cli.ts` importa `runRpcHibrida` y agrega la columna `rpc-real` al reporte sobre el mismo GOLDEN_SET.
    - Suite offline de `packages/fichas/spike` verde (los tests que no requieren DB pasan; el live test degrada a skip sin creds).
    - GOLDEN_SET congelado sin cambios; runFtsOnly/runSemanticOnly/runRrf intactos.
  </acceptance_criteria>
  <done>El harness mide la RPC REAL sobre el golden congelado; el golden live test permanente apunta al camino real; nada del spike original se rompió.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Corrida LIVE del gate de dominancia + decisión de flip del default</name>
  <what-built>
    El harness extendido (Task 1) puede medir la RPC real `buscar_proyectos_hibrido` (aplicada a PROD en Plan 01) sobre el golden set congelado de 86, y compararla contra el baseline (RRF ad-hoc 43.8/68.8/53.6, semántico 34.4/53.1/40.3). El rewire de `buscar.ts` (Plan 02) enruta por el flag `BUSQUEDA_HIBRIDA_ENABLED` (default OFF).
  </what-built>
  <how-to-verify>
    1. Correr el CLI de scoring LIVE con creds de `.env` (`SUPABASE_DB_URL` + `GEMINI_API_KEY`), embeddings desde cache:
       `cd packages/fichas && pnpm tsx src/spike/retrieval-cli.ts --report ...` (usar los flags reales del CLI: rrf-k=50, limit=50, la 4ª columna rpc-real).
    2. Leer la tabla: la columna `rpc-real` debe DOMINAR — criterio de victoria (86-SCORING §e / CONTEXT línea 40):
       (a) boletín 100% hit@1 (4/4) por short-circuit SQL;
       (b) titulo-literal y acentos-topónimos MEJORAN vs baseline (ahora con unaccent instalado);
       (c) parafrasis-nl y similares NO regresionan (≥ semántico-solo: 80% hit@5 cada uno).
    3. Escribir `87-SCORING.md` con la tabla LIVE completa (por estrategia × categoría, espejo 86-SCORING) y la sección DECISIÓN.
    4. DECIDIR el flip del default:
       - Si DOMINA → configurar el entorno productivo/CI con `BUSQUEDA_HIBRIDA_ENABLED=true` (documentar en 87-SCORING + STATE + `.env.example` si aplica, SIN valores secretos) y anotar el commit documentado; actualizar 86-SCORING §e con "gate re-verificado sobre RPC real: domina".
       - Si NO DOMINA → dejar el default OFF, registrar el hallazgo (qué categoría regresionó, hipótesis) en 87-SCORING, y NO flipear (el rewire queda latente tras el flag OFF hasta re-trabajo). Esto NO bloquea el cierre de fase: el hallazgo registrado ES el entregable del gate.
    Resume con la palabra clave y, si domina, el hecho de que el default quedó ON documentado.
  </how-to-verify>
  <resume-signal>Escribe "domina" (con el default flipeado a ON documentado) o "no-domina" (con el hallazgo registrado en 87-SCORING); o describe el fallo de la corrida.</resume-signal>
</task>

</tasks>

<verification>
- El harness mide la RPC real sobre el golden congelado; el live test protege el camino real.
- 87-SCORING.md registra la corrida LIVE (tabla por estrategia×categoría) y la DECISIÓN de flip.
- Si domina: default `BUSQUEDA_HIBRIDA_ENABLED=true` documentado (entorno/CI, sin secretos), 86-SCORING §e actualizado. Si no: hallazgo registrado, default OFF.
</verification>

<success_criteria>
- `runRpcHibrida` + 4ª columna `rpc-real` en el CLI, sobre el GOLDEN_SET congelado.
- Golden live test permanente apunta al camino RPC-real (regresión de CI).
- RETR-05: golden no regresiona NL/similares y la decisión de flip (RPC vieja tras flag hasta que la nueva domine) queda registrada en 87-SCORING.md.
</success_criteria>

<output>
Create `.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-03-SUMMARY.md` when done
</output>
