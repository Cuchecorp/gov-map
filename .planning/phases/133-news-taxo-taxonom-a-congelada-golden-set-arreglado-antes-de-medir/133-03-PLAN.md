---
phase: 133-news-taxo
plan: 03
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - packages/news/src/eval/canonicalizar-cli.ts
  - packages/news/src/eval/thresholds.ts
  - packages/news/src/eval/taxonomia.json
  - packages/news/src/eval/thresholds.json
  - packages/news/src/eval/CONGELADO.md
  - packages/news/src/eval/congelado.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "taxonomia.json y thresholds.json existen, son canónicos, y su sha256 está registrado en CONGELADO.md"
    - "Regenerar taxonomia.json desde taxonomia.ts produce bytes IDÉNTICOS al archivo en disco"
    - "Un drift de un byte en cualquiera de los dos JSON pone el test rojo"
    - "Un clon limpio del repo produce los MISMOS dos sha256 que el working tree"
    - "La suite de @obs/news corre en CI (deja de ser CI-dark)"
  artifacts:
    - path: "packages/news/src/eval/taxonomia.json"
      provides: "Proyección canónica de TAXONOMIA — el artefacto hasheado"
    - path: "packages/news/src/eval/thresholds.json"
      provides: "T1..T9 de D-133-D2 como ARRAY ordenado, con n mínimo, su condición, regla de intervalos y refutación"
      contains: "T9"
    - path: "packages/news/src/eval/CONGELADO.md"
      provides: "Log de congelación con hash_anterior → hash_nuevo, fecha, razón, firma"
    - path: "packages/news/src/eval/congelado.test.ts"
      provides: "Hashes vivos + sincronía byte a byte + última entrada de CONGELADO.md"
  key_links:
    - from: "packages/news/src/eval/congelado.test.ts"
      to: "packages/news/src/eval/taxonomia.json"
      via: "readFileSync bytes + sha256"
      pattern: "readFileSync\\(.*taxonomia\\.json"
    - from: ".github/workflows/ci.yml"
      to: "@obs/news"
      via: "step de vitest run"
      pattern: "filter @obs/news"
---

<objective>
La congelación (D-133-E2): generar las dos proyecciones canónicas, hashearlas, registrarlas en
`CONGELADO.md`, y construir el `congelado.test.ts` que hace estructuralmente imposible el drift.
Más el step de CI que impide que todo esto nazca CI-dark.

Purpose: es la vara. Un hash cuya estabilidad se **asume** es peor que no tener hash: enseña al
equipo a ignorar la CI roja.
Output: `taxonomia.json`, `thresholds.json`, `CONGELADO.md`, `congelado.test.ts`, step en `ci.yml`.

**Solo existen DOS de los tres JSON en 133-a.** `golden-set.json` es 133-b. Todo criterio dice
"dos", nunca "tres" — un `sha256sum` de un archivo inexistente produce lista vacía y el `diff` de
dos listas vacías pasaría (cero-vacuo, P-4).

**PROHIBIDO tocar `packages/news/src/eval/index.ts`** — propiedad exclusiva del plan 133-01, y el
plan 133-04 corre en esta misma wave con `use_worktrees: true`. Importar por **ruta relativa**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md

<interfaces>
Del plan 133-01 (ya en disco):
  packages/news/src/eval/taxonomia.ts   → TAXONOMIA (readonly ClaseTaxonomia[]), ETIQUETAS
  packages/news/src/eval/canonicalizar-json.ts
     → canonicalizar(valor: unknown): string;  // termina en "\n"
     → sha256(raw: string): string;            // hex 64 chars
  packages/news/src/eval/index.ts  → NO TOCAR (dueño: plan 133-01)
Molde a copiar (sin depender del paquete):
  packages/llm-bench/src/guards/freeze.ts:34 (hashCasos), :46 (assertFrozen, "FREEZE ROTO:")
  packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts:52-70 → los 4 tests
Molde del step de CI — rutas VERIFICADAS con `sed -n`:
  .github/workflows/ci.yml:59 = "- name: Test @obs/llm (…)"    / :60 = "run: pnpm --filter @obs/llm exec vitest run"
  .github/workflows/ci.yml:64 = comentario de @obs/cruces        / :65 = "run: pnpm --filter @obs/cruces exec vitest run"
  (la ronda anterior citaba :59 y :64 como las líneas del `run:`; son :60 y :65)
</interfaces>
</context>

<medicion>
## Idiom de medición — OBLIGATORIO

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada**. Verificado.

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**El criterio fuerte de este plan es el DELTA propio**, no un `-gt 206` absoluto: tras la wave 1 la
base ya estará por encima de 206 y ese umbral podría salir verde sin que la tarea haga nada. El plan
captura su **propia** `N_ANTES` al inicio de la Task 1 y exige el delta de `<delta_de_tests>`.
El `-gt 206` se conserva solo como piso secundario de sanidad.

**No** se usa aritmética contra el conteo de cierre de 133-04: corre en esta misma wave y también
sube el conteo, así que dependería del orden de merge.

`grep -c` devuelve 0 con rc=1 ⇒ capturar siempre con `n=$(grep -c ... || true)`.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 1 | **0** | — (genera artefactos, no tests) | — |
| Task 2 | **8** | (a) sha256 vivo de `taxonomia.json` == `HASH_TAXONOMIA` · (b) sha256 vivo de `thresholds.json` == `HASH_THRESHOLDS` · (c) sincronía byte a byte `canonicalizar(TAXONOMIA)` vs disco · (d) sincronía byte a byte `canonicalizar(THRESHOLDS)` vs disco · (e) `meta:` el hash cambia si se muta un byte · (f) última entrada de `CONGELADO.md` contiene los dos hashes · (g) bytes limpios (cero `\r`, cero BOM, newline final) · (h) control positivo apareado: el mismo assert sobre un fixture con `\r` inyectado falla | (a)/(b) ← mutación de byte · (c) ← **mutación de reordenamiento de claves** · (f) ← mutación del hash en el markdown |
| Task 3 | **0** | — (CI + control positivo por script) | — |
| **Total** | **8** | | |

`it.each` PROHIBIDO. Criterio: `test $((N_DESPUES - N_ANTES)) -eq 8`.
</delta_de_tests>

<tasks>

<task type="auto">
  <name>Task 1: thresholds.ts + generar y COMMITEAR las dos proyecciones canónicas, en el orden LOCKED</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-D2 completo (la tabla T1..T9 con umbral, n mínimo y efecto;
      la regla de intervalos en sus 4 puntos; la hipótesis y la refutación pre-registradas, incluida
      la **refutación parcial** de T9). **Atención a T3:** su n mínimo no es un número suelto, es
      *"≥3 clases con n≥8"* — una condición compuesta.
    - `133-READJUDICACION.md` §D-133-E2 (canonicalización; **arrays NO se reordenan**)
    - `133-RESEARCH.md` §F — la secuencia de 4 pasos y el gotcha verificado: **`--renormalize` NO
      añade archivos untracked**; el orden importa
    - `133-PREMORTEM-PLANES.md` §M1 — `git add --renormalize -- packages/news/src/eval` sale rc=128
      si el directorio no existe. Aquí ya existe (lo creó 133-01), así que aquí **sí** corre.
    - `133-RESEARCH.md` §A (TS6307 ⇒ **jamás importar `.json` desde un `.ts` de producción**)
  </read_first>
  <files>packages/news/src/eval/thresholds.ts, packages/news/src/eval/canonicalizar-cli.ts, packages/news/src/eval/taxonomia.json, packages/news/src/eval/thresholds.json</files>
  <action>
    **1. `packages/news/src/eval/thresholds.ts` — forma FIJADA (no queda a discreción).**

    `THRESHOLDS` es un objeto con **exactamente cuatro** claves de primer nivel:

    - `umbrales`: **un ARRAY**, no un objeto. Nueve entradas, en el **orden LOCKED de D-133-D2**:
      `T1, T2, T3, T4, T5, T9, T6, T7, T8`. Array por dos razones acopladas: (a) la canonicalización
      ordena claves ascendentemente y con un objeto el orden LOCKED se perdería (`T5` < `T6` < `T9`);
      (b) los arrays no se reordenan, así que el array **preserva** la secuencia firmada.

      Cada entrada tiene **seis** campos:
      `{ id, metrica, umbral (number | null para T8), n_minimo (number | null),
         n_minimo_condicion (string | null), efecto ("veto" | "informativo"), nota }`.

      **`n_minimo_condicion` existe por T3 y es obligatorio.** El n mínimo de T3 en D-133-D2 es
      *"≥3 clases con n≥8"*: un `number | null` solo no puede representarlo, y sin este campo **la
      condición de una decisión firmada se perdería en el JSON congelado** — que es exactamente el
      modo de fallo que la fase existe para evitar. Para T3: `n_minimo: 8` (el n por clase) y
      `n_minimo_condicion: "al menos 3 clases con n >= 8"`. Para T4, T5 y T9: `n_minimo: 25` y
      `n_minimo_condicion: null`. Para T1, T2, T6, T7, T8: ambos `null`.

      En T4, T5 y T9 la `nota` contiene la cláusula fail-closed: *si n < 25 ⇒ `no-medido` y la clase
      no enruta*. En T3, la `nota` repite en prosa la condición compuesta.

    - `regla_de_intervalos`: los 4 puntos de D-133-D2 en prosa (toda cifra con `n` e IC95; vetos
      sobre la estimación puntual; IC95 que cruza el umbral ⇒ `dentro-del-ruido` con ambos números;
      desempate por **solapamiento de IC95**, no por 6 pp).
    - `hipotesis_preregistrada`: verbatim de D-133-D2.
    - `refutacion`: con su `refutacion_parcial` (T9 falla o `no-medido` ⇒ el enrutamiento a fichas
      de persona NO entra a producción).

    Congelación profunda (`Object.freeze` sobre el objeto y sobre cada entrada del array; **no
    `as const`**, que es solo compile-time).
    **T9 debe estar presente. Su ausencia es el fallo silencioso más caro de este plan.**

    **2. `packages/news/src/eval/canonicalizar-cli.ts`** — importa `TAXONOMIA` y `THRESHOLDS` por
    **ruta relativa** (`./taxonomia`, `./thresholds`; **prohibido tocar `./index.ts`**), los pasa por
    `canonicalizar()` y **escribe** los dos JSON con `writeFileSync(path, str, "utf8")` (Node nunca
    traduce newlines ni escribe BOM). Imprime por stdout los dos sha256.

    **3. Generar, añadir y COMMITEAR — en este orden exacto** (invertirlo mueve el hash solo):
    a. Verificar que `.gitattributes` ya está commiteado:
       `test $(git log --oneline -- .gitattributes | wc -l) -ge 1`. Si no, **PARAR**: el plan 133-01
       no cerró.
    b. Correr el CLI para generar los dos JSON.
    c. `git add packages/news/src/eval/taxonomia.json packages/news/src/eval/thresholds.json`
       (`add` normal: `--renormalize` **NO** añade untracked).
    d. `git add --renormalize -- packages/news/src/eval` (red idempotente; el directorio ya existe).
    e. `git ls-files --eol -- packages/news/src/eval` → `i/lf w/lf attr/text eol=lf` en ambos JSON.
    f. **`git commit`** de los dos JSON junto con `thresholds.ts` y `canonicalizar-cli.ts`.
       Este commit es **requisito** de la Task 3: el control positivo clona **el commit**, no el
       working tree. No dejarlo implícito.
  </action>
  <acceptance_criteria>
    - **Línea base propia del plan:** antes de generar nada,
      `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-03-antes.log`;
      `N_ANTES=$(... | grep -oE '[0-9]+')`. Citado literal (esta tarea añade **0** tests, así que
      tras ella `N` debe ser idéntico a `N_ANTES`: `test $((N_T1 - N_ANTES)) -eq 0`).
    - `n=$(git ls-files --eol -- packages/news/src/eval | grep -c 'attr/text eol=lf' || true); test "$n" -eq 2`
      — el **2** es el anti-cero-vacuo. Y cada línea contiene `i/lf`.
    - **Forma y orden del array de umbrales, más la condición de T3:**
      `node -e 'const o=JSON.parse(require("fs").readFileSync("packages/news/src/eval/thresholds.json","utf8")); if(!Array.isArray(o.umbrales)) throw new Error("umbrales debe ser array"); if(o.umbrales.length!==9) throw new Error("len="+o.umbrales.length); const ids=o.umbrales.map(u=>u.id).join(","); if(ids!=="T1,T2,T3,T4,T5,T9,T6,T7,T8") throw new Error("orden LOCKED perdido: "+ids); const t3=o.umbrales.find(u=>u.id==="T3"); if(t3.n_minimo!==8) throw new Error("T3.n_minimo="+t3.n_minimo); if(t3.n_minimo_condicion!=="al menos 3 clases con n >= 8") throw new Error("T3 condicion compuesta alterada: "+JSON.stringify(t3.n_minimo_condicion)); for(const id of ["T4","T5","T9"]){const u=o.umbrales.find(x=>x.id===id); if(u.n_minimo!==25) throw new Error(id+".n_minimo="+u.n_minimo);} console.log("OK "+ids)'`
      sale rc=0. Caza a la vez: ausencia de T9, conteo distinto de 9, pérdida del orden por
      canonicalización, y **pérdida de la condición compuesta de T3**.
    - Bytes limpios en ambos JSON:
      `node -e 'for(const f of ["taxonomia","thresholds"]){const s=require("fs").readFileSync("packages/news/src/eval/"+f+".json","utf8"); if(s.includes("\r")||s.charCodeAt(0)===0xfeff||!s.endsWith("\n")) throw new Error(f)}'` sale rc=0.
    - `pnpm --filter @obs/news exec tsc -b --force` sale rc=0 (no se introdujo TS6307).
    - `n=$(grep -rn 'taxonomia.json\|thresholds.json' packages/news/src/eval/*.ts | grep -c 'import ' || true); test "$n" -eq 0`.
    - `git diff --name-only <SHA literal del inicio de la tarea>` **no** lista
      `packages/news/src/eval/index.ts`.
    - Los dos JSON están **commiteados**: `git log -1 --name-only` los lista.
  </acceptance_criteria>
  <verify>
    <automated>n=$(git ls-files --eol -- packages/news/src/eval | grep -c 'attr/text eol=lf' || true); echo "json_lf=$n"; test "$n" -eq 2</automated>
  </verify>
  <done>Los dos JSON canónicos existen, commiteados, con LF en el índice; `umbrales` es un array de 9 en el orden LOCKED con T9 y con la condición compuesta de T3 preservada; `tsc -b` verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: congelado.test.ts — hashes, sincronía byte a byte, y CONGELADO.md dentro del test</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-E2 enmiendas 2 y 3 (`CONGELADO.md` entra al test; el criterio
      asserta el conteo impreso; la firma-en-markdown es limitación declarada, no garantía)
    - `packages/llm-bench/src/guards/freeze.ts` completo (`hashCasos` `:34`, `assertFrozen` `:46`,
      mensaje `FREEZE ROTO:`)
    - `packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts:52-70` (los 4 tests, incluido
      `meta: assertFrozen LANZA si el hash derivó`)
    - `133-RESEARCH.md` §L: *"comparar JSON parseado en vez de bytes es **el falso verde más probable
      de toda la fase**"* — `toBe` sobre strings, jamás `toEqual` sobre objetos parseados
    - `133-RESEARCH.md` §C (asserts defensivos: cero `\r`, cero BOM, newline final, con control
      positivo apareado sobre un fixture en memoria)
  </read_first>
  <files>packages/news/src/eval/CONGELADO.md, packages/news/src/eval/congelado.test.ts</files>
  <behavior>
    Los 8 `it` del `<delta_de_tests>`, en concreto:
    - El sha256 vivo de cada JSON coincide con su constante en el test.
    - **Sincronía:** `canonicalizar(TAXONOMIA)` es **byte a byte idéntico** a
      `readFileSync("taxonomia.json","utf8")` — con `toBe`, jamás `toEqual` sobre objetos. Ídem
      `THRESHOLDS`.
    - `meta:` mutar un byte del string leído produce un hash distinto.
    - La **última entrada** de `CONGELADO.md` contiene los dos hashes vigentes como substring.
    - Bytes de ambos JSON: cero `\r`, `charCodeAt(0) !== 0xFEFF`, terminan en `\n`.
    - Control positivo apareado: el mismo assert sobre un fixture en memoria **con** un `\r`
      inyectado debe fallar. Una sola variable: la presencia del `\r`.
  </behavior>
  <action>
    Crear `packages/news/src/eval/CONGELADO.md`: encabezado con el régimen (cambio legítimo = **un
    commit con las tres cosas**: artefacto + hash en el test + entrada nueva aquí con
    `hash_anterior → hash_nuevo`, fecha, razón, firma; cualquier otra combinación es drift), la
    **limitación declarada** (*la firma es un string en un markdown; el control real es el commit en
    git — se dice así en vez de fingir garantía*), y la primera entrada: fecha, razón
    (`congelación inicial 133-a bajo D-133-A2/D-133-D2, firmada por el operador 2026-08-06`), firma,
    y los dos hashes con `hash_anterior: (ninguno)` → `hash_nuevo: <sha256>`.
    Registrar que `golden-set.json` **no existe todavía** y se congela en 133-b con su segunda
    firma — la ausencia declarada, no un hueco.

    Crear `packages/news/src/eval/congelado.test.ts` copiando el patrón de
    `disjuncion.test.ts:52-70` (citar la ruta origen en el JSDoc; **no** depender de
    `@obs/llm-bench`). Importar `TAXONOMIA`/`THRESHOLDS`/`canonicalizar`/`sha256` por **ruta
    relativa** (prohibido tocar `./index.ts`). Los JSON se leen con `readFileSync(path, "utf8")` —
    bytes, no objeto parseado.

    El test de la última entrada de `CONGELADO.md`: leer el markdown, tomar la **última** entrada
    (por el separador que defina el formato) y assertar que contiene ambos hashes como substring.
    NO un `includes` global sobre todo el archivo: con un hash viejo en una entrada anterior,
    pasaría con un log obsoleto.

    Las constantes de hash se escriben con el valor **impreso por el CLI** de la Task 1, literal.
  </action>
  <acceptance_criteria>
    - **DELTA FIJO:** `test $((N_T2 - N_T1)) -eq 8`, con `N_T1` el valor medido al cierre de la
      Task 1. Ambos citados. Piso secundario: `test "$N_T2" -gt 206`.
    - **Mutación del hash** (nunca bajo `set -e`): añadir un espacio al final de `taxonomia.json`,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run congelado > /tmp/mut-h.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0`, y el log nombra el test del hash. Revertir con `git checkout --`.
    - **Mutación de la SINCRONÍA — el mejor criterio de la fase — se corre sobre LOS DOS JSON.**
      Un canonicalizador que tratara distinto a `THRESHOLDS` que a `TAXONOMIA` no se cazaría si la
      mutación solo tocara uno; y `thresholds.json` es el más expuesto, porque su `umbrales` es un
      array cuyo orden es semántico. Ejecutar el procedimiento **dos veces**, una por archivo:
      primero sobre `taxonomia.json` (debe caer el `it` (c)), luego sobre `thresholds.json` (debe
      caer el `it` (d)), citando en el SUMMARY los dos `rc` y los dos nombres de test que cayeron.
      Procedimiento, en ambos casos: reordenar dos claves dentro de una entrada **del archivo en
      cuestión** (`taxonomia.json` en la primera pasada, `thresholds.json` en la segunda), **sin
      cambiar la semántica**. El test de sincronía correspondiente
      debe **fallar por bytes** aunque el JSON parseado sea equivalente. Mismo idiom `if/rc`;
      `test $rc -ne 0`. Revertir.
      Si ese test **pasa** con las claves reordenadas, la comparación es semántica y no de bytes:
      **PARAR y escalar**.
    - **Mutación de `CONGELADO.md`**: reemplazar un carácter de uno de los hashes de la última
      entrada → el test correspondiente falla; revertir.
    - La sincronía usa `toBe` sobre strings: verificar que en los `it` de sincronía no aparece
      `toEqual` (`n=$(grep -c 'toEqual' packages/news/src/eval/congelado.test.ts || true)` más
      revisión manual del bloque).
    - `git diff --name-only <SHA literal>` **no** lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-03.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-03.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>Los dos hashes congelados y registrados, la sincronía compara BYTES (probado por la mutación de reordenamiento de claves), `CONGELADO.md` está dentro del test, y el delta es exactamente 8.</done>
</task>

<task type="auto">
  <name>Task 3: Step de CI para @obs/news + control positivo del hash en clon limpio</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` — el 🔴 HALLAZGO (los 206 tests de la 132 nunca corrieron en
      CI) y §D-133-K2 (se añade el step de `@obs/news`; **la deuda de raíz se DECLARA y NO se arregla**)
    - **`.github/workflows/ci.yml` — líneas verificadas con `sed -n`:** `:59` es el `- name:` de
      `@obs/llm` y `:60` su `run:`; `:64` es un comentario y `:65` el `run:` de `@obs/cruces`.
      El molde a copiar es el par `- name:` / `run:`, y el step nuevo va **después de `:65`**.
    - `133-READJUDICACION.md` §D-133-E2 enmienda 1 (control positivo: clon limpio → sha256 coinciden)
    - `133-RESEARCH.md` §F "El control positivo del hash — cómo hacerlo en Windows/git-bash":
      la receta y sus **cuatro precauciones** (P-4 cero-vacuo; `set -e`; el clon es del **commit**;
      el par difiere en UNA variable)
    - `133-PREMORTEM-PLANES.md` P21 — verificado que
      `git clone -q --no-hardlinks "file://<ruta con espacio bajo OneDrive>"` sale rc=0
    - `133-RESEARCH.md` §K P-12 (comillas obligatorias en toda ruta: `OneDrive - pjud.cl`)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    **1. Step de CI.** Añadir a `.github/workflows/ci.yml`, **después del `run:` de `@obs/cruces`
    (`:65`)** y con el mismo molde `- name:` / `run:`: nombre
    `Test @obs/news (taxonomía congelada + guards de superficie)`, comando
    `pnpm --filter @obs/news exec vitest run`, y un comentario que declare por escrito en el YAML la
    **deuda de raíz**: *CI corre 3 (ahora 4) de ~17 workspaces; los demás paquetes siguen CI-dark.
    Deuda declarada en 133-a, NO arreglada aquí: convertir `ci.yml` a `pnpm test` podría destapar
    rojos de otros paquetes.* NO tocar ningún otro step. NO convertir `ci.yml` a `pnpm test`.

    **2. Control positivo del hash** (D-133-E2.1). Los dos JSON deben estar **commiteados** (Task 1
    paso f) — el clon es del commit, no del working tree. Receta de `133-RESEARCH.md` §F adaptada a
    **DOS** archivos (`taxonomia`, `thresholds`; `golden-set` es 133-b y NO se incluye):
    - `sha256sum` de los dos JSON en el working tree → `/tmp/hashes-wt.txt`
    - `T=$(mktemp -d)`; `git clone -q --no-hardlinks "file://<ruta absoluta del repo>" "$T/clon"`
      (comillas obligatorias); recalcular → `/tmp/hashes-clon.txt`
    - **anti-cero-vacuo ANTES del diff:** `test $(wc -l < /tmp/hashes-clon.txt) -eq 2` **y**
      `test $(wc -l < /tmp/hashes-wt.txt) -eq 2`. Sin esto, dos listas vacías "coinciden".
    - `diff` de las columnas de hash; limpiar el temporal.
    Si el `diff` no coincide, **PARAR y escalar** — el hash no es estable y la congelación es falsa.

    **3. Control positivo apareado del propio control** (una sola variable: el contenido del
    archivo): mutar un byte de `taxonomia.json` **en el working tree, sin commitear**, recalcular
    solo los hashes del working tree, y comprobar que el `diff` contra el clon **falla**
    (`if diff … > /tmp/d.log 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`). Revertir con
    `git checkout --`.
  </action>
  <acceptance_criteria>
    - Esta tarea añade **0** tests: `test $((N_T3 - N_T2)) -eq 0`, y el delta total del plan es
      `test $((N_T3 - N_ANTES)) -eq 8`. Todos los números citados.
    - `n=$(grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml || true); test "$n" -eq 1`.
    - `n=$(grep -c 'deuda' .github/workflows/ci.yml || true); test "$n" -ge 1`.
    - Steps preexistentes intactos: `grep -c 'filter @obs/llm exec vitest run'` = 1 y
      `grep -c 'filter @obs/cruces exec vitest run'` = 1.
    - `git diff --name-only <SHA literal del inicio de esta tarea>` lista `.github/workflows/ci.yml`
      y nada más (SHA literal, jamás `HEAD~1`).
    - Control positivo del hash: `wc -l < /tmp/hashes-clon.txt` = **2**, `wc -l < /tmp/hashes-wt.txt`
      = **2**, y el `diff` de las columnas de hash sale rc=0. Ambos hashes citados literalmente y
      coincidentes con los del `congelado.test.ts` y con la última entrada de `CONGELADO.md`.
    - Control positivo apareado: con un byte mutado en el working tree, el `diff` sale rc≠0
      (idiom `if/rc`, jamás bajo `set -e`).
  </acceptance_criteria>
  <verify>
    <automated>n=$(grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml || true); echo "step=$n"; test "$n" -eq 1</automated>
  </verify>
  <done>`@obs/news` corre en CI, la deuda de raíz declarada en el YAML y en el reporte, y el hash probado estable en un clon limpio con su control positivo apareado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxonomia.ts` → `taxonomia.json` | Si la proyección deriva del fuente, la vara medida deja de ser la vara firmada |
| working tree → índice de git → clon | Una normalización de newlines mueve el hash sin que nadie edite nada |
| repo → CI | Un guard que nadie ejecuta es un falso verde permanente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-07 | Tampering | Drift silencioso del artefacto congelado | mitigate | sha256 en `congelado.test.ts` + `CONGELADO.md` dentro del test + mutación de un byte |
| T-133-08 | Tampering | Sincronía comparada sobre JSON parseado (falso verde nº1 de la fase) | mitigate | `toBe` sobre strings + mutación que **reordena claves sin cambiar semántica** |
| T-133-09 | Repudiation | Guards CI-dark en `packages/news` | mitigate | Step `pnpm --filter @obs/news exec vitest run` en `ci.yml` |
| T-133-10 | Tampering | Control positivo del hash que compara listas vacías (P-4) | mitigate | `wc -l` = 2 en **ambos** ficheros antes del `diff` + control apareado |
| T-133-21 | Tampering | Orden LOCKED de umbrales perdido por la canonicalización | mitigate | `umbrales` es ARRAY; criterio que asserta la secuencia `T1,T2,T3,T4,T5,T9,T6,T7,T8` |
| T-133-22 | Tampering | Conflicto de merge sobre `eval/index.ts` con el plan 04 (misma wave, worktrees ON) | mitigate | Barrel exclusivo del plan 01; rutas relativas + `git diff --name-only` por tarea |
| T-133-28 | Tampering | La condición compuesta de T3 (*≥3 clases con n≥8*) se pierde al no caber en `n_minimo: number\|null` | mitigate | Campo `n_minimo_condicion` obligatorio + criterio `node -e` que asserta su presencia y su contenido |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES`, `N_T1`, `N_T2`, `N_T3` citados; deltas 0 / 8 / 0 y total 8.
2. Las tres mutaciones (byte del JSON, reordenamiento de claves, hash de `CONGELADO.md`) → rc≠0.
3. El `node -e` de umbrales: 9 entradas, orden LOCKED, T3 con `n_minimo=8` y su condición compuesta,
   T4/T5/T9 con `n_minimo=25`.
4. `wc -l` = 2 en los dos ficheros de hashes; `diff` rc=0; con un byte mutado, rc≠0.
5. `grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml` = 1.
6. `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts` en ninguna tarea.
</verification>

<success_criteria>
- `taxonomia.json` y `thresholds.json` congelados y **commiteados**, con LF en el índice y sus
  sha256 en `CONGELADO.md`.
- `thresholds.json` tiene `umbrales` como array de **9** en el orden LOCKED, con **T9** y con la
  condición compuesta de **T3** preservada en `n_minimo_condicion`.
- La sincronía `.ts` → `.json` compara BYTES, demostrado por la mutación de reordenamiento de claves.
- El hash es estable en un clon limpio (control ejecutado, no asumido).
- `@obs/news` corre en CI; la deuda de raíz declarada, no arreglada.
- `eval/index.ts` NO fue tocado.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-03-SUMMARY.md` when done.
Cita: los **dos** sha256 literales, los cuatro conteos `N`, el resultado del control positivo en
clon limpio, y la declaración de la deuda de raíz de `ci.yml`.
</output>
