---
phase: 133-news-taxo
plan: 03
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - packages/news/src/eval/canonicalizar-cli.ts
  - packages/news/src/eval/thresholds.ts
  - packages/news/src/eval/taxonomia.json
  - packages/news/src/eval/thresholds.json
  - packages/news/src/eval/CONGELADO.md
  - packages/news/src/eval/congelado.test.ts
  - packages/news/src/eval/index.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "taxonomia.json y thresholds.json existen, son canónicos, y su sha256 está registrado en CONGELADO.md"
    - "Regenerar taxonomia.json desde taxonomia.ts produce bytes IDÉNTICOS al archivo en disco"
    - "Un drift de un byte en cualquiera de los dos JSON pone el test rojo"
    - "Un clon limpio del repo produce los MISMOS dos sha256 que el working tree"
    - "La suite de @obs/news corre en CI (deja de ser CI-dark)"
  artifacts:
    - path: "packages/news/src/eval/taxonomia.json"
      provides: "Proyección canónica de TAXONOMIA — el artefacto hasheado"
    - path: "packages/news/src/eval/thresholds.json"
      provides: "T1..T9 de D-133-D2, con n mínimo, regla de intervalos y refutación pre-registrada"
      contains: "T9"
    - path: "packages/news/src/eval/CONGELADO.md"
      provides: "Log de congelación con hash_anterior → hash_nuevo, fecha, razón, firma"
    - path: "packages/news/src/eval/congelado.test.ts"
      provides: "Hashes vivos + sincronía byte a byte + última entrada de CONGELADO.md"
  key_links:
    - from: "packages/news/src/eval/congelado.test.ts"
      to: "packages/news/src/eval/taxonomia.json"
      via: "readFileSync bytes + sha256"
      pattern: "readFileSync\\(.*taxonomia\\.json"
    - from: ".github/workflows/ci.yml"
      to: "@obs/news"
      via: "step de vitest run"
      pattern: "filter @obs/news"
---

<objective>
La congelación (D-133-E2): generar las dos proyecciones canónicas, hashearlas, registrarlas en
`CONGELADO.md`, y construir el `congelado.test.ts` que hace estructuralmente imposible el drift.
Más el step de CI que impide que todo esto nazca CI-dark.

Purpose: es la vara. Un hash cuya estabilidad se **asume** es peor que no tener hash: enseña al
equipo a ignorar la CI roja.
Output: `taxonomia.json`, `thresholds.json`, `CONGELADO.md`, `congelado.test.ts`, step en `ci.yml`.

**Solo existen DOS de los tres JSON en 133-a.** `golden-set.json` es 133-b. Todo criterio dice
"dos", nunca "tres" — un `sha256sum` de un archivo inexistente produce una lista vacía y el `diff`
de dos listas vacías pasaría (cero-vacuo clásico, P-4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md

<interfaces>
Del plan 133-01 (ya en disco):
  packages/news/src/eval/taxonomia.ts   → export const TAXONOMIA: readonly ClaseTaxonomia[]
  packages/news/src/eval/canonicalizar-json.ts
     → export function canonicalizar(valor: unknown): string;  // termina en "\n"
     → export function sha256(raw: string): string;            // hex 64 chars
Molde a copiar (NO depender del paquete):
  packages/llm-bench/src/guards/freeze.ts:17-52   → FreezeMarker, hashCasos, assertFrozen
  packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts:52-70 → los 4 tests
Molde del step de CI:
  .github/workflows/ci.yml:59  → "run: pnpm --filter @obs/llm exec vitest run"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: thresholds.ts + generar las dos proyecciones canónicas por CLI, en el orden LOCKED</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-D2 completo (la tabla T1..T9 con umbral, n mínimo y efecto;
      la regla de intervalos en sus 4 puntos; la hipótesis y la refutación pre-registradas,
      incluida la **refutación parcial** de T9)
    - `133-READJUDICACION.md` §D-133-E2 (canonicalización y qué se hashea)
    - `133-RESEARCH.md` §F "Gotcha descubierto: `--renormalize` NO añade archivos untracked" — la
      secuencia de 4 pasos, y que **el orden importa**
    - `133-RESEARCH.md` §A (TS6307: `packages/news/tsconfig.json` incluye solo `src/**/*.ts` ⇒
      **jamás importar `.json` desde un `.ts` de producción**; leer por `readFileSync`)
    - `.gitattributes` creado en 133-01
  </read_first>
  <files>packages/news/src/eval/thresholds.ts, packages/news/src/eval/canonicalizar-cli.ts, packages/news/src/eval/taxonomia.json, packages/news/src/eval/thresholds.json, packages/news/src/eval/index.ts</files>
  <action>
    1. Crear `packages/news/src/eval/thresholds.ts` — el objeto `THRESHOLDS` como fuente ejecutable
       de D-133-D2, con **nueve** umbrales `T1, T2, T3, T4, T5, T9, T6, T7, T8`. Para cada uno:
       `id`, `metrica`, `umbral` (número o `null` para T8), `n_minimo` (número o `null`),
       `efecto` (`"veto"` | `"informativo"`), y `nota` con la cláusula de fail-closed donde aplique
       (T4/T5/T9: *si n < 25 ⇒ `no-medido` y la clase no enruta*).
       Además, en el mismo objeto: `regla_de_intervalos` (los 4 puntos de D-133-D2 en prosa:
       toda cifra con `n` e IC95; vetos sobre la estimación puntual; IC95 que cruza el umbral ⇒
       `dentro-del-ruido` con ambos números; desempate por **solapamiento de IC95**, no por 6 pp),
       `hipotesis_preregistrada` (verbatim de D-133-D2) y `refutacion` con su `refutacion_parcial`
       (T9 falla o `no-medido` ⇒ el enrutamiento a fichas de persona NO entra a producción).
       Congelación profunda, igual que `taxonomia.ts`.
       **T9 debe estar presente. Su ausencia es el fallo silencioso más caro de este plan.**

    2. Crear `packages/news/src/eval/canonicalizar-cli.ts` — script que importa `TAXONOMIA` y
       `THRESHOLDS`, los pasa por `canonicalizar()` y **escribe** `taxonomia.json` y
       `thresholds.json` con `writeFileSync(path, str, "utf8")` (Node nunca traduce newlines ni
       escribe BOM: el string ya lleva LF y newline final). Imprime por stdout los dos sha256.
       Invocación documentada en el JSDoc; añadirlo a `packages/news/src/eval/index.ts` solo si se
       expone como función — el CLI en sí no necesita export.

    3. Generar y añadir a git **en este orden exacto** (invertirlo mueve el hash solo):
       a. Verificar que `.gitattributes` ya está commiteado (`git log --oneline -- .gitattributes`
          devuelve al menos una línea). Si no lo está, **PARAR** — el plan 133-01 no cerró.
       b. Correr el CLI para generar los dos JSON.
       c. `git add packages/news/src/eval/taxonomia.json packages/news/src/eval/thresholds.json`
          (`add` normal: `--renormalize` NO añade untracked, verificado empíricamente).
       d. `git add --renormalize -- packages/news/src/eval` (red idempotente).
       e. `git ls-files --eol -- packages/news/src/eval` → debe imprimir `i/lf w/lf attr/text eol=lf`
          para ambos JSON.
  </action>
  <acceptance_criteria>
    - `git ls-files --eol -- packages/news/src/eval` imprime **exactamente 2** líneas de `.json`
      (`grep -c '\.json$'` = 2), y **cada una** contiene `i/lf` y `attr/text eol=lf`.
      El conteo de 2 es el anti-cero-vacuo: sin él, cero archivos también "cumplirían".
    - `grep -c '"T9"' packages/news/src/eval/thresholds.json` ≥ 1 y
      `node -e 'const t=require("fs").readFileSync("packages/news/src/eval/thresholds.json","utf8");const o=JSON.parse(t);console.log(Object.keys(o.umbrales||o).length)'`
      imprime **9**.
    - Los dos JSON no contienen `\r` ni BOM y terminan en newline:
      `node -e 'for(const f of ["taxonomia","thresholds"]){const s=require("fs").readFileSync("packages/news/src/eval/"+f+".json","utf8");if(s.includes("\r")||s.charCodeAt(0)===0xfeff||!s.endsWith("\n"))throw new Error(f)}'` sale rc=0.
    - `pnpm --filter @obs/news exec tsc -b --force` sale rc=0 (prueba que no se introdujo TS6307:
      ningún `.ts` de producción importa un `.json`).
    - `grep -rn 'taxonomia.json\|thresholds.json' packages/news/src/eval/*.ts | grep -c 'import ' ` = 0.
  </acceptance_criteria>
  <verify>
    <automated>git ls-files --eol -- packages/news/src/eval | grep -c 'attr/text eol=lf'</automated>
  </verify>
  <done>Los dos JSON canónicos existen, con LF en el índice, T9 presente, 9 umbrales, y `tsc -b` verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: congelado.test.ts — hashes, sincronía byte a byte, y CONGELADO.md dentro del test</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-E2 enmiendas 2 y 3 (`CONGELADO.md` entra al test; el criterio
      asserta el conteo impreso, no el exit code; la firma-en-markdown es limitación declarada, no garantía)
    - `packages/llm-bench/src/guards/freeze.ts` completo (el molde: `hashCasos`, `assertFrozen` con
      mensaje `FREEZE ROTO:`)
    - `packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts:52-70` (los 4 tests, incluido
      `meta: assertFrozen LANZA si el hash derivó`)
    - `133-RESEARCH.md` §L: *"comparar JSON parseado en vez de bytes es **el falso verde más probable
      de toda la fase**"* — usar `toBe` sobre strings, jamás `toEqual` sobre objetos parseados
    - `133-RESEARCH.md` §C (asserts defensivos: cero `\r`, cero BOM, newline final, con su control
      positivo apareado sobre un fixture en memoria)
  </read_first>
  <files>packages/news/src/eval/CONGELADO.md, packages/news/src/eval/congelado.test.ts</files>
  <behavior>
    - El sha256 vivo de `taxonomia.json` coincide con la constante `HASH_TAXONOMIA` del test.
    - El sha256 vivo de `thresholds.json` coincide con `HASH_THRESHOLDS`.
    - **Sincronía:** `canonicalizar(TAXONOMIA)` devuelve un string **byte a byte idéntico** a
      `readFileSync("taxonomia.json","utf8")` — comparado con `toBe`, jamás `toEqual` sobre objetos.
    - Ídem para `canonicalizar(THRESHOLDS)` vs `thresholds.json`.
    - `meta:` mutar un byte del string leído produce un hash distinto (el guard muerde).
    - La **última entrada** de `CONGELADO.md` contiene los dos hashes vigentes como substring.
    - Los bytes de ambos JSON: cero `\r`, `charCodeAt(0) !== 0xFEFF`, terminan en `\n`.
    - Control positivo apareado de los asserts de bytes: el mismo assert sobre un fixture en memoria
      **con** un `\r` inyectado debe fallar. Una sola variable: la presencia del `\r`.
  </behavior>
  <action>
    Crear `packages/news/src/eval/CONGELADO.md`: encabezado que explica el régimen (cambio legítimo
    = **un commit con las tres cosas**: artefacto + hash en el test + entrada nueva aquí con
    `hash_anterior → hash_nuevo`, fecha, razón, firma; cualquier otra combinación es drift), la
    **limitación declarada** (*la firma es un string en un markdown; el control real es el commit en
    git — se dice así en vez de fingir garantía*), y la primera entrada de congelación con: fecha,
    razón (`congelación inicial 133-a bajo D-133-A2/D-133-D2, firmada por el operador 2026-08-06`),
    firma, y los dos hashes con `hash_anterior: (ninguno)` → `hash_nuevo: <sha256>`.
    Registrar además que `golden-set.json` **no existe todavía** y se congela en 133-b con su segunda
    firma — para que la ausencia sea declarada, no un hueco.

    Crear `packages/news/src/eval/congelado.test.ts` copiando el patrón de
    `packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts:52-70` (citar la ruta origen en
    el JSDoc; **no** añadir dependencia a `@obs/llm-bench`, que arrastraría medio grafo). Los JSON se
    leen con `readFileSync(path, "utf8")` — bytes, no objeto parseado: es lo que el hash necesita y
    lo que evita TS6307.

    El test de la última entrada de `CONGELADO.md`: leer el markdown, tomar la **última** entrada
    (por el separador que defina el formato del archivo) y assertar que contiene ambos hashes como
    substring. NO parsear el markdown con regex frágil sobre todo el archivo: si contuviera un hash
    viejo en una entrada anterior, un `includes` global pasaría con un log obsoleto.

    Todas las constantes de hash del test se escriben con el valor **impreso por el CLI** de la
    Task 1, copiado literal. No inventar ni truncar.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-03.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-03.log` imprime un número mayor que el conteo
      registrado al cierre del plan 133-01 (citar ambos en el SUMMARY). Nunca el exit code solo.
    - **Prueba de mutación del hash** (no bajo `set -e`): añadir un espacio al final de
      `taxonomia.json`, correr
      `if pnpm --filter @obs/news exec vitest run congelado > /tmp/mut-h.log 2>&1; then rc=0; else rc=$?; fi`,
      comprobar `test $rc -ne 0`, y que `/tmp/mut-h.log` nombra el test del hash. Revertir con
      `git checkout -- packages/news/src/eval/taxonomia.json`.
    - **Prueba de mutación de la SINCRONÍA** (la que caza el falso verde más probable de la fase):
      reordenar dos claves dentro de una entrada de `taxonomia.json` **sin cambiar la semántica**
      (mismo objeto, distinto orden de claves). El test de sincronía debe **fallar** por bytes aunque
      el JSON parseado sea equivalente. Mismo idiom `if/rc`; `test $rc -ne 0`. Revertir.
      Si este test **pasa** con las claves reordenadas, la comparación es semántica y no de bytes:
      **PARAR y escalar**.
    - **Prueba de mutación de `CONGELADO.md`**: reemplazar un carácter de uno de los hashes en la
      última entrada → el test correspondiente falla; revertir.
    - `grep -c 'toEqual' packages/news/src/eval/congelado.test.ts` sobre la comparación de sincronía
      es 0 (verificar manualmente que la sincronía usa `toBe` sobre strings).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-03.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-03.log</automated>
  </verify>
  <done>Los dos hashes están congelados y registrados, la sincronía compara bytes (probado por la mutación de reordenamiento de claves), y `CONGELADO.md` está dentro del test.</done>
</task>

<task type="auto">
  <name>Task 3: Step de CI para @obs/news + control positivo del hash en clon limpio</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` — el 🔴 HALLAZGO (los 206 tests de la 132 nunca corrieron en
      CI) y §D-133-K2 (se añade el step de `@obs/news`; **la deuda de raíz se DECLARA y NO se arregla**)
    - `.github/workflows/ci.yml:55-64` (el molde literal de los steps de `@obs/llm` y `@obs/cruces`)
    - `133-READJUDICACION.md` §D-133-E2 enmienda 1 (control positivo: clon limpio → los sha256 coinciden)
    - `133-RESEARCH.md` §F "El control positivo del hash — cómo hacerlo en Windows/git-bash":
      la receta completa y sus **cuatro precauciones** (P-4 cero-vacuo; `set -e`; el clon es del
      commit, no del working tree; el par debe diferir en UNA variable)
    - `133-RESEARCH.md` §K P-12 (el repo vive en `.../OneDrive - pjud.cl/...`: comillas en toda ruta)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. Añadir a `.github/workflows/ci.yml`, después del step de `@obs/cruces` (`:64`) y con su mismo
       molde, un step:
       nombre `Test @obs/news (taxonomía congelada + guards de superficie)`,
       comando `pnpm --filter @obs/news exec vitest run`,
       y un comentario que declare, por escrito en el YAML, la **deuda de raíz**: *CI corre 3 (ahora
       4) de ~17 workspaces; los demás paquetes siguen CI-dark. Deuda declarada en 133-a, NO
       arreglada aquí: convertir `ci.yml` a `pnpm test` podría destapar rojos de otros paquetes.*
       NO tocar ningún otro step. NO convertir `ci.yml` a `pnpm test`.

    2. **Control positivo del hash** (D-133-E2.1). Los dos JSON deben estar **commiteados** antes de
       correr esto — el clon es del commit, no del working tree; ese es exactamente el punto.
       Ejecutar la receta de `133-RESEARCH.md` §F adaptada a **DOS** archivos (`taxonomia`,
       `thresholds`; `golden-set` es 133-b y NO se incluye):
       - calcular `sha256sum` de los dos JSON en el working tree → `/tmp/hashes-wt.txt`
       - `git clone -q --no-hardlinks "file://<ruta-absoluta-del-repo>" "$T/clon"` (comillas
         obligatorias: la ruta tiene un espacio) y recalcular → `/tmp/hashes-clon.txt`
       - `diff` de las columnas de hash
       - **anti-cero-vacuo:** `test $(wc -l < /tmp/hashes-clon.txt) -eq 2` ANTES del `diff`.
         Sin esto, dos listas vacías "coinciden".
       - limpiar el temporal.
       Si el `diff` **no** coincide, el ejecutor **PARA y escala** — el hash no es estable y toda la
       congelación es falsa.

    3. **Control positivo apareado del propio control** (una sola variable: el contenido del archivo):
       mutar un byte de `taxonomia.json` **en el working tree, sin commitear**, re-correr solo el
       cálculo del working tree, y comprobar que ahora el `diff` contra el clon **falla**
       (`if diff ... > /tmp/d.log 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`). Revertir con
       `git checkout --`. Esto prueba que el control muerde y no es un `diff` de listas vacías.
  </action>
  <acceptance_criteria>
    - `grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml` devuelve 1.
    - `grep -c 'deuda' .github/workflows/ci.yml` ≥ 1 (la deuda de raíz declarada por escrito en el YAML).
    - Los steps preexistentes siguen intactos:
      `grep -c 'filter @obs/llm exec vitest run' .github/workflows/ci.yml` = 1 y
      `grep -c 'filter @obs/cruces exec vitest run' .github/workflows/ci.yml` = 1.
    - `git diff --name-only <SHA literal del inicio de esta tarea>` lista `.github/workflows/ci.yml`
      y nada más (SHA literal, jamás `HEAD~1` — un `git diff` sin base fija siempre pasa con commits
      atómicos).
    - Control positivo del hash: `wc -l < /tmp/hashes-clon.txt` = **2** y el `diff` de las columnas
      de hash sale rc=0. Ambos hashes se citan literalmente en el SUMMARY y coinciden con los del
      `congelado.test.ts` y con la última entrada de `CONGELADO.md`.
    - Control positivo apareado: con un byte mutado en el working tree, el `diff` sale rc≠0
      (capturado con el idiom `if/rc`, jamás bajo `set -e`).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml</automated>
  </verify>
  <done>`@obs/news` corre en CI, la deuda de raíz está declarada en el YAML y en el reporte, y el hash está probado estable en un clon limpio con su control positivo apareado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxonomia.ts` → `taxonomia.json` | Si la proyección deriva del fuente, la vara medida deja de ser la vara firmada |
| working tree → índice de git → clon | Una normalización de newlines mueve el hash sin que nadie edite nada |
| repo → CI | Un guard que nadie ejecuta es un falso verde permanente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-07 | Tampering | Drift silencioso del artefacto congelado | mitigate | sha256 en `congelado.test.ts` + `CONGELADO.md` dentro del test + mutación de un byte que debe poner rojo |
| T-133-08 | Tampering | Sincronía comparada sobre JSON parseado (falso verde nº1 de la fase) | mitigate | `toBe` sobre strings + mutación que **reordena claves sin cambiar semántica** y debe fallar |
| T-133-09 | Repudiation | Guards CI-dark en `packages/news` | mitigate | Step `pnpm --filter @obs/news exec vitest run` en `ci.yml`, con criterio sobre el grep del YAML |
| T-133-10 | Tampering | Control positivo del hash que compara listas vacías (P-4) | mitigate | `test $(wc -l) -eq 2` antes del `diff` + control apareado con un byte mutado |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `pnpm --filter @obs/news exec vitest run` → `Tests N passed`, N citado y mayor que el del plan 01.
2. Las tres mutaciones (byte del JSON, reordenamiento de claves, hash de `CONGELADO.md`) → rc≠0.
3. `wc -l < /tmp/hashes-clon.txt` = 2 y `diff` de hashes rc=0; con un byte mutado, rc≠0.
4. `grep -c 'filter @obs/news exec vitest run' .github/workflows/ci.yml` = 1.
</verification>

<success_criteria>
- `taxonomia.json` y `thresholds.json` congelados, con LF en el índice y sus sha256 en `CONGELADO.md`.
- `thresholds.json` tiene los **nueve** umbrales, con **T9** presente y su cláusula de fail-closed.
- La sincronía `.ts` → `.json` compara BYTES, demostrado por la mutación de reordenamiento de claves.
- El hash es estable en un clon limpio (control positivo ejecutado, no asumido).
- `@obs/news` corre en CI; la deuda de raíz queda declarada por escrito, no arreglada.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-03-SUMMARY.md` when done.
El SUMMARY cita: los **dos** sha256 literales, el conteo impreso de tests antes/después, el resultado
del control positivo en clon limpio, y la declaración de la deuda de raíz de `ci.yml`.
</output>
