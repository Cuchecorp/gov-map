# 133-b-07 — PLAN: arbitraje proxy + congelado de `golden-set.json` + segunda firma proxy

**Fecha:** 2026-08-10 · **Planifica:** Fable (estratega, bajo `133-b-ENMIENDA-PROXY.md`)
**Precondición:** ronda 2 de anotación con κ re-computado y regla C2.1.3 NO gatillada (si se
gatilla por segunda vez ⇒ C2.4: FRACASO de la fase, este plan NO corre).

## Decisiones de implementación

1. **Arbitraje** (C2.3, proxy): cada desacuerdo lo resuelve un agente **Fable fresco** (sin
   contexto, sin acceso a archivos — todo el material viaja inline): glosa + precisiones v2,
   texto del caso (`entrada_llm`), y las dos posturas (etiqueta+justificación+cita de A y B,
   anonimizadas como "Anotador A/B" sin nombrar modelos). El árbitro **elige entre A y B**
   (contrato del esquema LOCKED: `etiqueta` final debe provenir de `etiqueta_a`,
   `etiqueta_b` o `etiqueta_humana`) y justifica. JAMÁS ve `estrato` ni el veredicto del
   pre-filtro. `resuelto_por: "operador"` con la aclaración proxy en los metadatos del
   artefacto (el enum del esquema es LOCKED; la semántica proxy se declara arriba, no
   mutando el esquema).
2. **golden-set.json** (`golden-cli.ts`): 154 casos `CasoGoldenSchema` — `procedencia` desde
   el pool (r2_path, url_hash, url_canonica, outlet, fecha_captura=date_bucket, fecha_pub),
   `entrada` cruda (despojada de HTML, sin truncar), `entrada_llm` (misma función del
   pre-filtro), `estrato` desde la muestra, `prefiltro` recomputado
   (`esLegislativo`/`terminosQueMatchean`), `etiqueta` final, `revision` C2.5 completa
   (`etiqueta_humana` = etiqueta de calibración proxy Fable en los 20; la semántica proxy se
   declara en metadatos y en la página de 137). Validación zod de los 154 ANTES de escribir.
3. **P-dirigido / fail-closed** (D-133b-3): contar n por clase final. Si
   `tramitacion_legislativa` o `actividad_parlamentaria` < 25: el pool restante de la
   ventana es vacío (P es censo) y los fixtures son los MISMOS snapshots de la ventana (sin
   ítems nuevos) ⇒ **no existe P-dirigido real**: la clase queda `no-medido` y **NO ENRUTA**
   (fail-closed de D-133-D2), reportado en el artefacto y en el SUMMARY. Bajar el n mínimo
   NO es salida válida.
4. **Congelado** (D-133-E2): canonicalizar + sha256; entrada nueva en `CONGELADO.md` (hash
   golden + los dos vigentes de taxonomia/thresholds) + actualización de `congelado.test.ts`
   en el MISMO commit; control positivo: doble corrida byte-idéntica.
5. **Segunda firma**: "firma proxy Fable bajo enmienda 2026-08-10 — RATIFICACIÓN DE OPERADOR
   PENDIENTE", con κ, n por clase e IC a la vista en el SUMMARY.

## Amenazas nuevas

- T-133-71: el árbitro ve información de máquina/estrato ⇒ inline-only, sin archivos, sin
  estrato; el material se construye por código desde el registro.
- T-133-72: arbitraje "inventa" una tercera etiqueta ⇒ schema del structured output con enum
  `["a","b"]`; el refinement del CasoGoldenSchema lo re-verifica al congelar.
- T-133-73: golden hasheado con CRLF ⇒ `.gitattributes` ya cubre `eval/**/*.json` (133-01);
  el control positivo del hash en doble corrida lo confirma.
