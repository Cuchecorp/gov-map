---
phase: 133-news-taxo
plan: 05
type: execute
wave: 3
depends_on: ["133-01", "133-02", "133-03"]
files_modified:
  - packages/news/src/eval/taxonomia-guard.test.ts
  - packages/news/src/eval/taxonomia-superficie-guard.test.ts
  - package.json
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Ningún string de taxonomia.ts contiene un término prohibido del guard anti-insinuación"
    - "Ningún literal de etiqueta aparece en app/ con matching CASE-SENSITIVE sobre el snake_case exacto"
    - "G1, G2 y congelado.test.ts corren desde `pnpm guards` de la raíz, por nombre y jamás por glob"
    - "Los tres guards muerden: inyectar la violación pone el test rojo"
  artifacts:
    - path: "packages/news/src/eval/taxonomia-guard.test.ts"
      provides: "G1 — términos prohibidos sobre cada string de la taxonomía"
    - path: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      provides: "G2 — ningún literal de etiqueta en superficie de app/ (D-133-G)"
  key_links:
    - from: "packages/news/src/eval/taxonomia-guard.test.ts"
      to: "app/lib/terminos-insinuacion.ts"
      via: "readFileSync por DISCO (bytes, no import)"
      pattern: "terminos-insinuacion"
    - from: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      to: "app/"
      via: "findWorkspaceRoot desde ../run-news-cli + walk completo"
      pattern: "findWorkspaceRoot"
    - from: "package.json"
      to: "los tres guards de @obs/news"
      via: "script `guards`, por nombre"
      pattern: "filter @obs/news exec vitest run"
---

<objective>
G1 y G2 (D-133-A2.4, D-133-J2, D-133-K1), y el enganche de los tres guards nuevos al script
`guards` de la raíz (D-133-K3).

Purpose: con D-133-G la etiqueta no se renderiza jamás — pero *"el cumplimiento va por guard, no
por promesa"*. Y un guard de régimen fuera del entrypoint reproducible de guards es incoherente con
la propia declaración de ese script.
Output: `taxonomia-guard.test.ts`, `taxonomia-superficie-guard.test.ts`, script `guards` de 17 → 20.

**PROHIBIDO tocar `packages/news/src/eval/index.ts`** (dueño: plan 133-01). Los guards importan por
ruta relativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-02-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-03-SUMMARY.md

<interfaces>
Del plan 133-01:
  packages/news/src/eval/taxonomia.ts → TAXONOMIA (readonly ClaseTaxonomia[]), ETIQUETAS
  packages/news/src/eval/index.ts     → NO TOCAR
Del plan 133-02, en `app/`, se lee por DISCO (jamás por import):
  app/lib/terminos-insinuacion.ts → TERMINOS_PROHIBIDOS, TERMINOS_LINK_EXT, TERMINOS_COBERTURA
Del plan 133-03:
  packages/news/src/eval/congelado.test.ts
Herramienta de rutas — importar del MÓDULO DIRECTO, no del barrel:
  import { findWorkspaceRoot } from "../run-news-cli";
  // definida en packages/news/src/run-news-cli.ts:190; lanza si no halla pnpm-workspace.yaml.
  // El barrel packages/news/src/index.ts:46 también la re-exporta, pero importarlo arrastra
  // writer-supabase/carga-run (y con ellos @supabase/supabase-js, fast-xml-parser) a un guard
  // que solo quiere una función de rutas.
Patrones a copiar verbatim con cita de origen (rutas:línea VERIFICADAS hoy):
  app/lib/anti-insinuacion-guard.test.ts:837 (const WORD) y :839-844 (buildTermRegex)
  app/lib/anti-insinuacion-guard.test.ts:888-894 (detectarTerminos, con la resta en :890)
                                          :896-905 (detectarInsinuaciones, con la resta en :901)
  app/lib/anti-insinuacion-guard.test.ts:1076-1080 (`expect(verificados).toEqual(...)`)
  app/lib/lockdown-guard.test.ts:102-135 (walkSourceFiles + SKIP_DIRS), :735 (piso >10)
  app/lib/anti-insinuacion-guard.test.ts:920-932 (sanity de archivo concreto)
  app/lib/anti-insinuacion-guard.test.ts:80-95 (stripTsComments, con el cuidado del `://`, WR-05)
</interfaces>
</context>

<medicion>
## Idiom de medición — OBLIGATORIO

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada**. Verificado.

```bash
NO_COLOR=1 <comando> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**Este plan captura su PROPIA línea base** al inicio de la Task 1 (`N_ANTES`), sobre el worktree ya
integrado de la wave 2. **No** se compara contra el conteo de cierre de 133-03: 133-04 corre en esa
misma wave y también sube el conteo, así que esa aritmética depende del orden de merge y no es
verificable.

`grep -c` devuelve 0 con rc=1 ⇒ capturar siempre con `n=$(grep -c ... || true)`.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan

| Tarea | `it(...)` nuevos | Cuáles |
|---|---|---|
| Task 1 (G1) | **4** | (a) `piso: se extrajeron >= 100 términos prohibidos del disco` · (b) `piso: se extrajeron >= 12 negaciones del disco` · (c) `cero términos prohibidos en los strings de la taxonomía` (con `verificados` anti-cero-vacuo) · (d) `sanity: terminos-insinuacion.ts se leyó y tiene contenido` |
| Task 2 (G2) | **4** | (a) `el walk de app/ devuelve > 100 archivos` · (b) `sanity: un archivo concreto de app/ se leyó` · (c) `cero literales de etiqueta en app/` (con `escaneados` anti-cero-vacuo) · (d) `control positivo apareado: el mismo fixture con y sin el literal` |
| **Total** | **8** | |

`it.each` PROHIBIDO. Criterio: `test $((N_DESPUES - N_ANTES)) -eq 8`.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G1 — términos prohibidos sobre CADA string de taxonomia.ts</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G1 (mutación obligatoria: inyectar un término prohibido en una
      glosa y demostrar que el test cae)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2 — **J2.3** (lectura por DISCO con piso duro) y
      **J2.4** (aplicar la misma **sustracción** de negaciones antes del match y assertar que **no
      removió nada**; si una glosa contuviera una negación LOCKED verbatim, eso **debe fallar**)
    - **Rutas:línea verificadas hoy** (el addendum trae drift):
      `app/lib/anti-insinuacion-guard.test.ts:837` (`const WORD`), `:839-844` (`buildTermRegex`,
      **compila con el flag `"i"`** — dato decisivo para G2, ver Task 2),
      `:888-894` (`detectarTerminos`, la resta de negaciones en `:890`),
      `:896-905` (`detectarInsinuaciones`, la resta en `:901`),
      `:776-825` (`NEGACIONES_LOCKED`), `:824` (`...IDIOMS_APROBADOS`), `:59` (su import desde
      `@/lib/idioms-panel`), `app/lib/idioms-panel.ts:32-48` (`IDIOMS_APROBADOS`)
    - `133-PREMORTEM-PLANES.md` §P14/P15 — verificado independientemente: la extracción de los tres
      arrays da **104** términos únicos y `buildTermRegex` verbatim sobre las 14 glosas de A2.2 más
      las 6 etiquetas da **0 hits**. G1 nace verde. Extraer solo `TERMINOS_PROHIBIDOS` sin resolver
      sus dos spreads quedaría **< 100**: el margen del piso es de 4.
    - `app/lib/anti-insinuacion-guard.test.ts:1017-1086` (molde: fixture EN MEMORIA por el detector
      real, con JSDoc de alcance honesto) y `:1076-1080` (el `verificados` explícito)
    - `133-RESEARCH.md` §K P-9 (`_` está dentro de `WORD` ⇒ `no_legislativa` no dispara `legislativa`)
  </read_first>
  <files>packages/news/src/eval/taxonomia-guard.test.ts</files>
  <behavior>
    - El guard extrae `TERMINOS_PROHIBIDOS` + `TERMINOS_LINK_EXT` + `TERMINOS_COBERTURA` leyendo
      `app/lib/terminos-insinuacion.ts` **por disco**, con piso duro `>= 100` (medición real: 104).
    - Extrae también las negaciones disponibles por disco, con piso duro `>= 12`.
    - **Cada** string de `TAXONOMIA` (`etiqueta`, `definicion`, `marca_decisoria`, `frontera`,
      `enruta_a`) pasa por el detector y **cero** términos prohibidos aparecen.
    - Anti-cero-vacuo: un contador `verificados` acumula cada string efectivamente pasado por el
      detector y el test asserta que iguala 6 clases × 5 campos = **30**. Un `continue` colado debe
      poner el test rojo.
    - La sustracción de negaciones se aplica antes del match **y se asserta que no removió nada**.
    - Sanity: la lectura de `app/lib/terminos-insinuacion.ts` devuelve contenido de longitud > 100.
      Si la raíz está mal resuelta, `readFileSync` **lanza** en vez de pasar verde.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-guard.test.ts`. Localizar `app/` con
    `findWorkspaceRoot(import.meta.dirname)` importado de **`../run-news-cli`** (módulo directo, no
    el barrel: el barrel arrastra `@supabase/supabase-js` y `fast-xml-parser` a un guard).
    **Prohibido** `resolve(dirname, "../../../../app")`.

    **Extracción por disco, con comentarios strippeados.** Leer `app/lib/terminos-insinuacion.ts`
    con `readFileSync` y **pasarlo primero por un `stripTsComments`** (copiado verbatim de
    `app/lib/anti-insinuacion-guard.test.ts:80-95`, con su cuidado del `://` en URLs, WR-05) antes
    de extraer los literales por regex. Razón concreta: el plan 133-02 obliga a poner un JSDoc en ese
    módulo, y ese JSDoc contiene comillas — sin strippear, el regex capturaría fragmentos de
    comentario como si fueran términos. Alternativa aceptable: acotar el regex al **cuerpo de cada
    array** (entre `const X: string[] = [` y el `];` de cierre). Elegir una y escribirla en el JSDoc.

    Piso duro inmediatamente después de extraer: `>= 100` términos. El JSDoc declara la medición
    real (104) y que el margen es de 4, así que una poda futura del vocabulario romperá el piso —
    lo cual es el comportamiento deseado.

    **Negaciones (D-133-J2.4), con su cero-vacuo cerrado.** `NEGACIONES_LOCKED` se queda en el
    `.test.ts` y **su composición hace que una extracción ingenua sea un cero vacuo**: verificado
    hoy, el array tiene solo **2 literales inline**; las otras entradas son 4 constantes
    `LEYENDA_*` importadas de componentes `.tsx` más el spread `...IDIOMS_APROBADOS`
    (`app/lib/idioms-panel.ts:32-48`, **10 entradas**). Con 0 ó 2 negaciones extraídas, "la
    sustracción no removió nada" pasaría trivialmente.
    Por eso G1:
    - lee por disco **los dos** archivos: `anti-insinuacion-guard.test.ts` (para los 2 literales
      inline de `NEGACIONES_LOCKED`) **y** `app/lib/idioms-panel.ts` (para `IDIOMS_APROBADOS`);
    - asserta **piso duro `>= 12`** sobre el total de negaciones extraídas (2 + 10 medidos hoy);
    - **declara en el JSDoc lo que NO cubre**: las 4 constantes `LEYENDA_ANTI_INSINUACION_MONEY`,
      `LEYENDA_CROSS_LINK`, `LEYENDA_MENCIONES_LOBBY` y `LEYENDA_SIMILITUD_VOTO` viven en `.tsx` y
      quedan fuera de la extracción por disco. Se dice, no se disimula — es el idiom de alcance
      honesto de `anti-insinuacion-guard.test.ts:1017-1032`;
    - aplica la sustracción antes del match y asserta que **no removió nada** de las glosas.

    Copiar `buildTermRegex`/`WORD` **verbatim** de `:837-844` con cita de origen (es el precedente
    del repo). No inventar un `\b` de JS: trata los acentuados como no-palabra.

    Copiar el idiom `verificados` de `:1076-1080` con el mensaje *"Algún string de la taxonomía no
    llegó al detector (¿un `continue`/filtro colado en el bucle?)"*.
  </action>
  <acceptance_criteria>
    - **Línea base propia:** al inicio,
      `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05-antes.log`;
      `N_ANTES=$(... | grep -oE '[0-9]+')`. Citado literal.
    - Tras la Task 1: `test $((N_T1 - N_ANTES)) -eq 4`. Ambos citados.
    - `grep -c 'from "../run-news-cli"' packages/news/src/eval/taxonomia-guard.test.ts` = 1 y
      `n=$(grep -c '\.\./\.\./\.\./\.\.' packages/news/src/eval/taxonomia-guard.test.ts || true); test "$n" -eq 0`.
    - `n=$(grep -c 'from "\.\./index"\|from "\./index"' packages/news/src/eval/taxonomia-guard.test.ts || true); test "$n" -eq 0`
      (no se importa ningún barrel).
    - Los dos pisos pasan y **sus conteos reales se citan literalmente** en el SUMMARY (referencias
      medidas: 104 términos, 12 negaciones). Si alguno sale por debajo del piso: **PARAR y escalar**,
      jamás bajar el piso.
    - **Mutación 1 — término prohibido en una glosa** (nunca bajo `set -e`): inyectar un término
      prohibido literal en la `definicion` de una clase de `taxonomia.ts`,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run taxonomia-guard > /tmp/mut5a.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0`, y el log nombra la clase y el término. Revertir con `git checkout --` **y
      re-correr `congelado.test.ts`** para confirmar que el hash volvió a su valor congelado.
    - **Mutación 2 — anti-cero-vacuo:** añadir un `continue` que salte una clase ⇒ rc≠0 con el
      mensaje de `verificados`. Revertir.
    - **Mutación 3 — piso de términos:** apuntar la lectura a una ruta inexistente ⇒ rc≠0 (el guard
      lanza, no pasa con cero términos). Revertir.
    - **Mutación 4 — piso de negaciones (simétrica a la 3):** hacer que la extracción de
      `idioms-panel.ts` devuelva `[]` ⇒ el piso `>= 12` cae, rc≠0. Revertir. Sin esta mutación, la
      aserción "la sustracción no removió nada" seguiría siendo un cero vacuo.
    - `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>G1 verde con las CUATRO mutaciones demostradas, ambos pisos con su conteo real citado, y el alcance de la extracción de negaciones declarado por escrito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G2 — literales de etiqueta en app/, matching CASE-SENSITIVE</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G2 y la decisión de operador **D-133-G**
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K1 (dónde vive; walk completo **sin allowlist**; piso
      `> 100`; sanity de archivo concreto; **G2 NO strippea comentarios** y se escribe en el JSDoc
      como decisión; los `.test.ts` excluidos)
    - `app/lib/lockdown-guard.test.ts:102-135` (`walkSourceFiles` + `SKIP_DIRS`) y `:735`
      (el piso `>10`, **demasiado laxo** para G2)
    - `app/lib/anti-insinuacion-guard.test.ts:920-932` (sanity de archivo concreto) y **`:844`**
      (`buildTermRegex` compila con el flag **`"i"`** — case-INsensitive)
    - **`app/app/buscar/page.tsx:215`** — el caso conocido que decide el diseño:
      `// Ambiguo o ninguno → null (fail-honest).`
    - `133-RESEARCH.md` §E modos de fallo 1-4 (cero-vacuo por `try{readdirSync}catch{return out}`;
      la sobre-cobertura es virtud; G2 se cazaría a sí mismo si escaneara `.test.ts`)
    - `133-PREMORTEM-PLANES.md` P12/P13 (el walk da **158** archivos; **0 hits** case-sensitive)
  </read_first>
  <files>packages/news/src/eval/taxonomia-superficie-guard.test.ts</files>
  <behavior>
    - El walk de `app/` devuelve **> 100** archivos `.ts`/`.tsx` no-test (medición: 158). Un `[]`
      por raíz mal resuelta pone el test rojo, no verde.
    - Sanity: leer un archivo conocido de `app/` devuelve longitud > 100.
    - Ninguno de esos archivos contiene ninguno de los 6 literales de `ETIQUETAS`, **buscados de
      forma CASE-SENSITIVE**.
    - Anti-cero-vacuo del bucle: un contador `escaneados` iguala la longitud de la lista del walk.
    - **Control positivo apareado, UNA sola variable:** el MISMO string de fixture en memoria, una
      versión **con** `tramitacion_legislativa` insertado (el detector lo caza) y otra **sin** él
      (no lo caza). Idéntico en todo lo demás.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-superficie-guard.test.ts`. Importar `ETIQUETAS` de
    `./taxonomia` (ruta relativa; **prohibido tocar `./index.ts`** y prohibido re-escribir las
    etiquetas). Localizar `app/` con `join(findWorkspaceRoot(import.meta.dirname), "app")`,
    importando `findWorkspaceRoot` de **`../run-news-cli`**, no del barrel.

    Enumeración: copiar `walkSourceFiles` + `SKIP_DIRS` de `app/lib/lockdown-guard.test.ts:102-135`
    con cita de origen. **Walk completo de `app/`, SIN allowlist** — la sobre-cobertura es virtud:
    nunca produce un falso verde, solo falsos rojos ruidosos. Un allowlist reproduciría el modo de
    fallo que G3 cerró en el plan 133-02.

    Piso anti-cero-vacuo `expect(archivos.length).toBeGreaterThan(100)` (medición: 158) — **no** el
    `>10` laxo del existente. Más el sanity de archivo concreto conocido.

    **G2 NO strippea comentarios.** Más simple y estrictamente más estricto. Escribirlo en el JSDoc
    para que sea decisión, no descuido (D-133-K1).

    **MATCHING CASE-SENSITIVE — decisión de diseño, con su razón escrita en el JSDoc.**
    `buildTermRegex` del guard de `app/` compila con el flag `"i"` (`anti-insinuacion-guard.test.ts:844`).
    G2 **no** puede heredar ese flag: combinado con "no strippear comentarios", `"i"` haría que
    `app/app/buscar/page.tsx:215` (`// Ambiguo o ninguno → null (fail-honest).`) matcheara el
    literal `ambiguo` con frontera limpia, y **G2 nacería rojo sobre un comentario en castellano que
    no tiene nada que ver con la taxonomía**. Verificado en el repo: **case-sensitive → 0 hits en
    los 6 literales; case-insensitive → 1 hit, ese comentario.**
    Lo que D-133-G prohíbe es que **el literal de etiqueta** (snake_case, tal cual sale de
    `ETIQUETAS`) llegue a una superficie renderizada; una palabra castellana capitalizada en un
    comentario no es ese literal. Así que G2 busca el **literal snake_case exacto**, sin el flag
    `"i"`, y lo declara en el JSDoc citando `app/app/buscar/page.tsx:215` como el caso que motiva la
    decisión.
    **Si aun así apareciera un hit: PARAR y escalar. JAMÁS editar `app/` para poner el guard verde.**
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 4` y `test $((N_T2 - N_ANTES)) -eq 8` (delta total del plan).
      Todos los números citados.
    - `grep -c 'toBeGreaterThan(100)' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1.
    - `grep -ci 'no strippea' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1
      (la decisión escrita, no implícita).
    - **La regex de G2 NO lleva el flag `i`:**
      `n=$(grep -cE 'new RegExp\([^)]*"[a-z]*i[a-z]*"\)|/[gimsuy]*i[gimsuy]*$' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -eq 0`,
      **más** un `it` que lo asserta desde dentro: la función de matching de G2, aplicada al string
      `"// Ambiguo o ninguno"`, devuelve **0 hits**, y aplicada a `"const x = \"ambiguo\";"`
      devuelve **1**. Ese par difiere en UNA variable (la capitalización) y es el criterio real —
      el grep sobre el fuente es solo el respaldo.
    - `grep -c 'from "./taxonomia"' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1
      (las etiquetas se importan, no se re-tipean). El literal `tramitacion_legislativa` aparece en
      el archivo **exactamente 2 veces** y solo dentro del control positivo apareado:
      `n=$(grep -c 'tramitacion_legislativa' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -eq 2`.
    - `n=$(grep -c 'from "\.\./index"\|from "\./index"' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -eq 0`.
    - **Mutación sobre disco real (el guard muerde):** crear temporalmente
      `app/lib/__tmp-g2-mutacion.ts` con el literal `tramitacion_legislativa` dentro de un string,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run taxonomia-superficie > /tmp/mut6.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0` **y** `grep -c '__tmp-g2-mutacion' /tmp/mut6.log` ≥ 1. **Borrar el archivo
      temporal** y re-correr para confirmar verde. Sin este paso, G2 podría no estar escaneando nada.
    - **Mutación del piso:** apuntar la raíz a un directorio inexistente ⇒ rc≠0 (no un `[]` verde).
      Revertir.
    - `git status --short` al terminar no lista ningún archivo en `app/` (el temporal fue borrado).
    - `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>G2 escanea `app/` completo con piso > 100, matchea CASE-SENSITIVE sobre el literal snake_case (con el par capitalización probado), no encuentra ningún hit, y su mutación sobre disco real nombra el archivo infractor.</done>
</task>

<task type="auto">
  <name>Task 3: Los tres guards al script `guards` de la raíz, por NOMBRE (17 → 20)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K3 (por nombre, **jamás por glob**; molde de
      `@obs/dinero`/`@obs/llm`; 17 → 20)
    - `package.json:12` (el JSDoc `//guards`: *"entrypoint reproducible de los 17 guards de régimen"*
      y *"D-13: jamás glob — `vitest run src/*guard*.test.ts` sale 0 sin correr nada"*)
    - `package.json:14` (el script `guards` actual)
    - `app/lib/create-view-guard.test.ts:444-451` (test de "nombres fantasma")
    - `133-PREMORTEM-PLANES.md` §BLOCKER 6 — **`grep -c '\*guard\*' package.json` vale 1 hoy y
      seguirá valiendo 1**, porque el literal vive dentro del JSDoc `//guards` que este mismo plan
      manda **conservar**. Un criterio que exija 0 obliga a borrar el aviso D-13 o a mentir.
    - `133-PREMORTEM-PLANES.md` §P16 — con `NO_COLOR=1`, `pnpm guards` imprime hoy **3** conteos
      (`347`, `34`, `7`). La meta de **4** es correcta.
  </read_first>
  <files>package.json</files>
  <action>
    Añadir al final del script `guards` de la raíz (`package.json:14`), con el molde de los bloques
    de `@obs/dinero` y `@obs/llm`:
    `&& pnpm --filter @obs/news exec vitest run src/eval/taxonomia-guard.test.ts src/eval/taxonomia-superficie-guard.test.ts src/eval/congelado.test.ts`

    Los tres nombres **explícitos**. Jamás un glob (D-13: `vitest run src/*guard*.test.ts` sale 0 sin
    correr nada).

    Actualizar el JSDoc `//guards` (`package.json:12`): de *"17 guards de régimen = 11 (app) + 3
    (@obs/dinero) + 3 (@obs/llm)"* a *"20 guards de régimen = 11 (app) + 3 (@obs/dinero) + 3
    (@obs/llm) + 3 (@obs/news, Phase 133-a)"*. **Conservar íntegro el aviso D-13 sobre el glob** —
    es una lección LOCKED documentada, y el criterio de abajo está escrito para no obligar a borrarla.

    Verificar antes de commitear que los tres archivos **existen en disco** con esos nombres exactos:
    un nombre fantasma, con `passWithNoTests: true`, sale 0 en silencio.

    NO tocar `app/package.json:9`: G3 se implementó modificando `anti-insinuacion-guard.test.ts`
    (plan 133-02), no creando un archivo nuevo.
  </action>
  <acceptance_criteria>
    - `NO_COLOR=1 pnpm guards 2>&1 | tee /tmp/133-guards.log` sale rc=0 **y**
      `n=$(grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log || true); test "$n" -eq 4`
      (una línea por cada `pnpm --filter` encadenado: app, dinero, llm, news). Hoy, sin el cambio,
      da **3**: citar ambos números. El exit code solo no basta.
    - El bloque de `@obs/news` del log reporta **más de 0** tests: extraer su conteo (el cuarto) y
      verificar > 0. Un `0 passed` sería el nombre fantasma exacto.
    - `n=$(grep -c 'filter @obs/news exec vitest run' package.json || true); test "$n" -eq 1`.
    - **Cero globs EN EL SCRIPT (no en el archivo).** El criterio apunta al script, porque el
      literal `*guard*` vive legítimamente en el JSDoc `//guards` que el plan manda conservar:
      `node -e 'const s=require("./package.json").scripts.guards; if(/\*guard\*/.test(s)) { console.error("GLOB EN EL SCRIPT: "+s); process.exit(1);} console.log("sin glob")'`
      sale rc=0. **Y el aviso D-13 sigue en el archivo:**
      `n=$(grep -c '\*guard\*' package.json || true); test "$n" -eq 1` — se exige que **siga
      valiendo 1**, no 0: borrarlo sería perder la lección.
    - `n=$(grep -c '20 guards de régimen' package.json || true); test "$n" -eq 1` (JSDoc actualizado).
    - **Mutación (nombre fantasma), por CONTEO y no por rc:** renombrar temporalmente
      `packages/news/src/eval/congelado.test.ts` a `congelado.test.ts.bak`, correr
      `if NO_COLOR=1 pnpm guards > /tmp/mut7.log 2>&1; then rc=0; else rc=$?; fi`. Con
      `passWithNoTests: true` esto **puede salir 0** — por eso el criterio es el conteo: el cuarto
      bloque de `/tmp/mut7.log` debe reportar **menos** tests que en `/tmp/133-guards.log`, citando
      ambos números. Restaurar el nombre y re-correr para confirmar el conteo original.
      Un criterio basado en rc aquí sería literalmente inalcanzable.
    - `git diff --name-only <SHA literal del inicio de la tarea>` lista `package.json` y nada más.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm guards 2>&1 | tee /tmp/133-guards.log; n=$(grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log || true); echo "bloques=$n"; test "$n" -eq 4</automated>
  </verify>
  <done>`pnpm guards` corre los 20 guards de régimen por nombre con cuatro conteos impresos; el aviso D-13 sigue en el archivo; la prueba del nombre fantasma demostrada por diferencia de conteo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxonomia.ts` → copy renderizado | Una glosa insinuante entraría al producto por el carril de news, esquivando el linter de `app/` |
| `packages/news` → `app/` | G2 debe leer `app/` sin invertir la dirección de dependencia del monorepo |
| script `guards` → guards en disco | Un nombre que no existe sale 0 en silencio (`passWithNoTests`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-15 | Information Disclosure | Copy insinuante en las glosas de la taxonomía | mitigate | G1 sobre cada string, `verificados` = 30, mutación que inyecta un término prohibido |
| T-133-16 | Information Disclosure | Un literal de etiqueta renderizado en `app/` viola D-133-G | mitigate | G2 walk completo sin allowlist, piso > 100, mutación sobre disco real |
| T-133-17 | Repudiation | Guard con nombre fantasma sale 0 sin correr nada | mitigate | Criterio por **conteo impreso** (4 bloques) + mutación por renombrado que compara conteos |
| T-133-18 | Tampering | G2 en `app/` importando `@obs/news` rompería el desacoplamiento | mitigate | G2 vive en `packages/news`, lee `app/` por bytes, `findWorkspaceRoot` desde el módulo directo |
| T-133-25 | Repudiation | Sustracción de negaciones sobre 0-2 entradas ⇒ "no removió nada" trivialmente cierto | mitigate | Piso `>= 12` leyendo también `idioms-panel.ts`, alcance no cubierto declarado, y mutación simétrica del piso |
| T-133-26 | Repudiation | G2 case-insensitive ⇒ falso rojo sobre un comentario castellano; ⇒ tentación de editar `app/` | mitigate | Matching CASE-SENSITIVE sobre el literal snake_case, con el par capitalización como criterio y `app/app/buscar/page.tsx:215` citado en el JSDoc; prohibición explícita de editar `app/` |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES`, `N_T1`, `N_T2` citados; deltas exactos 4 / 4 y total 8.
2. `NO_COLOR=1 pnpm guards` → rc=0 y exactamente **4** líneas `Tests N passed` (hoy 3, ambos citados).
3. Las seis mutaciones (término en glosa, `continue`, piso de términos, piso de negaciones,
   archivo temporal en `app/`, nombre fantasma) con el idiom `if/rc` — jamás bajo `set -e`.
   La del nombre fantasma se juzga por **conteo**, no por rc.
4. El par de capitalización de G2: `"// Ambiguo o ninguno"` → 0 hits; `"const x = \"ambiguo\";"` → 1.
5. `git status --short` limpio de temporales (especialmente en `app/`).
6. `congelado.test.ts` verde tras revertir la mutación de `taxonomia.ts`: el hash volvió a su valor.
7. `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts` en ninguna tarea.
</verification>

<success_criteria>
- G1 verde: cero términos prohibidos en los 30 strings de la taxonomía, con ambos pisos y sus
  conteos reales citados, y el alcance de la extracción de negaciones declarado.
- G2 verde: cero literales de etiqueta en `app/`, CASE-SENSITIVE, con walk > 100 y mutación que muerde.
- `pnpm guards` corre 20 guards por nombre con cuatro conteos impresos.
- El JSDoc de `package.json` dice 20, y el aviso D-13 sobre el glob **sigue ahí**.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-05-SUMMARY.md` when done.
Cita: los cuatro conteos de `pnpm guards` (y el 3 de antes), los conteos reales de términos y
negaciones extraídos, y los pares de números de cada mutación por conteo.
</output>
