---
phase: 133-news-taxo
plan: 05
type: execute
wave: 3
depends_on: ["133-01", "133-02", "133-03"]
files_modified:
  - packages/news/src/eval/taxonomia-guard.test.ts
  - packages/news/src/eval/taxonomia-superficie-guard.test.ts
  - package.json
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Ningún string de taxonomia.ts contiene un término prohibido del guard anti-insinuación"
    - "Ningún literal de etiqueta aparece en app/ con matching CASE-SENSITIVE sobre el snake_case exacto"
    - "G1, G2 y congelado.test.ts corren desde `pnpm guards` de la raíz, por nombre y jamás por glob"
    - "Los tres guards muerden: inyectar la violación pone el test rojo"
  artifacts:
    - path: "packages/news/src/eval/taxonomia-guard.test.ts"
      provides: "G1 — términos prohibidos sobre cada string de la taxonomía"
    - path: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      provides: "G2 — ningún literal de etiqueta en superficie de app/ (D-133-G)"
  key_links:
    - from: "packages/news/src/eval/taxonomia-guard.test.ts"
      to: "app/lib/terminos-insinuacion.ts"
      via: "readFileSync por DISCO (bytes, no import)"
      pattern: "terminos-insinuacion"
    - from: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      to: "app/"
      via: "findWorkspaceRoot desde ../run-news-cli + walk completo"
      pattern: "findWorkspaceRoot"
    - from: "package.json"
      to: "los tres guards de @obs/news"
      via: "script `guards`, por nombre"
      pattern: "filter @obs/news exec vitest run"
---

<objective>
G1 y G2 (D-133-A2.4, D-133-J2, D-133-K1), y el enganche de los tres guards nuevos al script
`guards` de la raíz (D-133-K3).

Purpose: con D-133-G la etiqueta no se renderiza jamás — pero *"el cumplimiento va por guard, no
por promesa"*. Y un guard de régimen fuera del entrypoint reproducible de guards es incoherente con
la propia declaración de ese script.
Output: `taxonomia-guard.test.ts`, `taxonomia-superficie-guard.test.ts`, script `guards` de 17 → 20.

**PROHIBIDO tocar `packages/news/src/eval/index.ts`** (dueño: plan 133-01). Importar por ruta relativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-02-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-03-SUMMARY.md

<interfaces>
Del plan 133-01:
  packages/news/src/eval/taxonomia.ts → TAXONOMIA (readonly ClaseTaxonomia[]), ETIQUETAS
  packages/news/src/eval/index.ts     → NO TOCAR
Del plan 133-02, en `app/`, se lee por DISCO (jamás por import):
  app/lib/terminos-insinuacion.ts → TERMINOS_PROHIBIDOS, TERMINOS_LINK_EXT, TERMINOS_COBERTURA
Del plan 133-03:
  packages/news/src/eval/congelado.test.ts
Herramienta de rutas — importar del MÓDULO DIRECTO, no del barrel:
  import { findWorkspaceRoot } from "../run-news-cli";
  // definida en packages/news/src/run-news-cli.ts:190; lanza si no halla pnpm-workspace.yaml.
  // El barrel packages/news/src/index.ts:46 también la re-exporta, pero importarlo arrastra
  // writer-supabase/carga-run (y con ellos @supabase/supabase-js, fast-xml-parser) a un guard.
Patrones a copiar verbatim con cita de origen — rutas:línea VERIFICADAS con `sed -n`:
  app/lib/anti-insinuacion-guard.test.ts:80-95   → stripTsComments (con el cuidado del `://`, WR-05)
  app/lib/anti-insinuacion-guard.test.ts:837     → const WORD
  app/lib/anti-insinuacion-guard.test.ts:839-845 → buildTermRegex; el flag "i" está en la **:843**
  app/lib/anti-insinuacion-guard.test.ts:888-894 → detectarTerminos (resta de negaciones en :890)
  app/lib/anti-insinuacion-guard.test.ts:896-905 → detectarInsinuaciones (resta en :901)
  app/lib/anti-insinuacion-guard.test.ts:1076-1080 → `expect(verificados).toEqual(...)`
  app/lib/anti-insinuacion-guard.test.ts:920-932 → sanity de archivo concreto
  app/lib/lockdown-guard.test.ts:102-135         → walkSourceFiles + SKIP_DIRS; piso `>10` en :735
</interfaces>
</context>

<medicion>
## Idiom de medición — OBLIGATORIO

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada**. Verificado.

```bash
NO_COLOR=1 <comando> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**Este plan captura su PROPIA línea base** (`N_ANTES`) al inicio de la Task 1, sobre el worktree ya
integrado de la wave 2, y el criterio fuerte es el **delta**. Un `-gt 206` absoluto en la wave 3
sería casi vacuo (la base ya estará ~230): se conserva solo como piso secundario.

`grep -c` cuenta **líneas**, no ocurrencias, y devuelve 0 con rc=1 ⇒ capturar con
`n=$(grep -c ... || true)`, y usar `grep -o … | wc -l` cuando lo que importa son ocurrencias.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan — reconciliado con las mutaciones

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 1 (G1) | **5** | (a) `piso: >= 90 términos prohibidos extraídos del disco` · (b) `piso: NEGACIONES_LOCKED aporta exactamente 2 literales inline` · (c) `piso: IDIOMS_APROBADOS aporta >= 10` · (d) `cero términos prohibidos en los strings de la taxonomía` (con `verificados === 30`) · (e) `sanity: terminos-insinuacion.ts se leyó y tiene contenido` | (a)←ruta inexistente · (b)/(c)←extracción vaciada · (d)←término inyectado en una glosa y `continue` colado |
| Task 2 (G2) | **5** | (a) `el walk de app/ devuelve > 100 archivos` · (b) `sanity: un archivo concreto de app/ se leyó` · (c) `cero literales de etiqueta en app/` (con `escaneados` anti-cero-vacuo) · (d) `control positivo apareado: el mismo fixture con y sin el literal` · (e) **`el matching es CASE-SENSITIVE: "// Ambiguo o ninguno" → 0 hits, "// ambiguo o ninguno" → 1`** | (c)←archivo temporal en `app/` · (a)←raíz inexistente · (e) es el criterio de B-3, ejecutado por la función real |
| **Total** | **10** | | |

**(b) y (c) de la Task 1 están separados a propósito:** un piso sumado `>= 12` no distingue "2+10"
de "12 basura de JSDoc". **(e) de la Task 2 es el criterio real de la decisión case-sensitive**;
el grep sobre el fuente es solo respaldo, y sin este `it` contado el delta lo expulsaría.

`it.each` PROHIBIDO. Criterio: `test $((N_DESPUES - N_ANTES)) -eq 10`.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G1 — términos prohibidos sobre CADA string de taxonomia.ts</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G1 (mutación obligatoria: inyectar un término prohibido en una
      glosa y demostrar que el test cae)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2 — **J2.3** (lectura por DISCO con piso duro) y
      **J2.4** (aplicar la misma **sustracción** de negaciones antes del match y assertar que **no
      removió nada**)
    - **Rutas:línea verificadas con `grep -n`/`sed -n`:**
      `app/lib/anti-insinuacion-guard.test.ts:80-95` (`stripTsComments`),
      `:837` (`const WORD`), `:839-845` (`buildTermRegex`; **el flag `"i"` está en la `:843`**),
      `:888-894` (`detectarTerminos`, resta en `:890`), `:896-905` (`detectarInsinuaciones`, resta
      en `:901`), `:776-825` (`NEGACIONES_LOCKED`), `:824` (`...IDIOMS_APROBADOS`), `:59` (su
      import desde `@/lib/idioms-panel`), `app/lib/idioms-panel.ts:32-48` (`IDIOMS_APROBADOS`)
    - `app/lib/anti-insinuacion-guard.test.ts:1017-1086` (molde: fixture EN MEMORIA por el detector
      real, con JSDoc de alcance honesto) y `:1076-1080` (el `verificados` explícito)
    - `133-RESEARCH.md` §K P-9 (`_` está dentro de `WORD` ⇒ `no_legislativa` no dispara `legislativa`)
  </read_first>
  <files>packages/news/src/eval/taxonomia-guard.test.ts</files>
  <behavior>
    - Extracción por disco de `TERMINOS_PROHIBIDOS` + `TERMINOS_LINK_EXT` + `TERMINOS_COBERTURA`
      desde `app/lib/terminos-insinuacion.ts`, **con comentarios strippeados**, piso duro `>= 90`.
    - Extracción de negaciones, **también con comentarios strippeados**, con **dos** pisos separados:
      literales inline de `NEGACIONES_LOCKED` **=== 2** y `IDIOMS_APROBADOS` **>= 10**.
    - **Cada** string de `TAXONOMIA` (`etiqueta`, `definicion`, `marca_decisoria`, `frontera`,
      `enruta_a`) pasa por el detector: **cero** términos prohibidos.
    - Anti-cero-vacuo: `verificados === 30` (6 clases × 5 campos). Un `continue` colado lo pone rojo.
    - La sustracción de negaciones se aplica antes del match **y se asserta que no removió nada**.
    - Sanity: la lectura de `app/lib/terminos-insinuacion.ts` devuelve longitud > 100; si la raíz
      está mal resuelta, `readFileSync` **lanza**.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-guard.test.ts`. Localizar `app/` con
    `findWorkspaceRoot(import.meta.dirname)` importado de **`../run-news-cli`** (módulo directo, no
    el barrel). **Prohibido** `resolve(dirname, "../../../../app")`.

    **Extracción por disco — `stripTsComments` OBLIGATORIO sobre TODOS los archivos leídos, y
    acotado al cuerpo de cada array.** Copiar `stripTsComments` verbatim de
    `app/lib/anti-insinuacion-guard.test.ts:80-95` (con su cuidado del `://` en URLs, WR-05).
    Aplicarlo **antes** de extraer literales, y extraer solo entre `const X…= [` y su `]` de cierre
    (balanceo de corchetes). Escribir el método elegido en el JSDoc.

    Motivo medido, no teórico: sin strip, la extracción sobre `anti-insinuacion-guard.test.ts` captura
    los términos citados dentro del JSDoc. Contraste verificado en el working tree:

    | Extracción | sin strip | con strip |
    |---|---|---|
    | `TERMINOS_LINK_EXT` | 8 | 8 |
    | `TERMINOS_COBERTURA` | 6 | 6 |
    | `TERMINOS_PROHIBIDOS` inline | 137 | **78** |
    | total único | 104 | **92** |
    | `NEGACIONES_LOCKED` inline | **20** | **2** |

    **Piso de términos: `>= 90`** (medido 92 con el método correcto; margen 2). El "104" salía de la
    medición sin strip y está **invalidado**: cuando un fix cambia cómo se mide algo, toda cifra
    derivada de la medición anterior deja de valer. Si el conteo real sale < 90: **PARAR y escalar**,
    jamás bajar el piso.

    **Negaciones (D-133-J2.4) — el cero-vacuo cerrado con DOS pisos separados.** `NEGACIONES_LOCKED`
    se queda en el `.test.ts`, y su composición hace que una extracción ingenua sea basura: sin strip
    da **20** literales (JSDoc citando `"influencia"`, `"señal"`, `"afinidad"`,
    `"No implica afinidad, coordinación ni causalidad."`…); con strip da **2**, que son los reales.
    Un piso sumado `>= 12` no distingue "2 + 10" de "12 de basura". Por eso G1:
    - lee por disco **los dos** archivos con strip: `anti-insinuacion-guard.test.ts` (cuerpo de
      `NEGACIONES_LOCKED`) **y** `app/lib/idioms-panel.ts` (cuerpo de `IDIOMS_APROBADOS`);
    - asserta **`inline === 2`** (igualdad exacta, no `>=`: es un conjunto cerrado y su crecimiento
      debe ser una decisión visible) y **`idioms >= 10`** (medido 10);
    - **declara en el JSDoc lo que NO cubre**: las **cinco** constantes que viven en `.tsx` y quedan
      fuera de la extracción por disco — `LEYENDA_ANTI_INSINUACION_MONEY`, `LEYENDA_CROSS_LINK`,
      `LEYENDA_MENCIONES_LOBBY`, **`EMPTY_MENCIONES_LOBBY`** y `LEYENDA_SIMILITUD_VOTO`
      (verificadas con `grep -n`: `:782`, `:787`, `:793`, `:797`, `:804` del cuerpo `776-825`). Son **cinco**,
      no cuatro: una declaración de alcance honesto que omite una entrada es peor que no declararlo;
    - aplica la sustracción antes del match y asserta que **no removió nada** de las glosas.

    Copiar `buildTermRegex`/`WORD` **verbatim** de `:837-845` con cita de origen. No inventar un `\b`
    de JS: trata los acentuados como no-palabra.

    Copiar el idiom `verificados` de `:1076-1080` con el mensaje *"Algún string de la taxonomía no
    llegó al detector (¿un `continue`/filtro colado en el bucle?)"*.
  </action>
  <acceptance_criteria>
    - **Línea base propia:** al inicio,
      `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05-antes.log`;
      `N_ANTES=$(... | grep -oE '[0-9]+')`. Citado literal.
    - `test $((N_T1 - N_ANTES)) -eq 5`. Ambos citados. Piso secundario: `test "$N_T1" -gt 206`.
    - `grep -c 'from "../run-news-cli"' packages/news/src/eval/taxonomia-guard.test.ts` = 1 y
      `n=$(grep -c '\.\./\.\./\.\./\.\.' packages/news/src/eval/taxonomia-guard.test.ts || true); test "$n" -eq 0`.
    - `n=$(grep -cE 'from "\.\./index"|from "\./index"' packages/news/src/eval/taxonomia-guard.test.ts || true); test "$n" -eq 0`.
    - `grep -c 'stripTsComments' packages/news/src/eval/taxonomia-guard.test.ts` ≥ 1, y el JSDoc
      declara que se aplica **a los dos** archivos leídos.
    - Los **tres** pisos pasan y sus conteos reales se citan literalmente en el SUMMARY (referencias
      medidas con strip: **92** términos, **2** negaciones inline, **10** idioms). Si alguno se
      desvía: **PARAR y escalar**, jamás ajustar el piso.
    - El JSDoc lista las **cinco** constantes `.tsx` no cubiertas, con `EMPTY_MENCIONES_LOBBY`
      incluida: `grep -c 'EMPTY_MENCIONES_LOBBY' packages/news/src/eval/taxonomia-guard.test.ts` ≥ 1.
    - **Mutación 1 — término prohibido en una glosa** (nunca bajo `set -e`): inyectar un término
      prohibido literal en la `definicion` de una clase de `taxonomia.ts`,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run taxonomia-guard > /tmp/mut5a.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0`, y el log nombra la clase y el término. Revertir con `git checkout --` **y
      re-correr `congelado.test.ts`** para confirmar que el hash volvió a su valor congelado.
    - **Mutación 2 — anti-cero-vacuo:** añadir un `continue` que salte una clase ⇒ rc≠0 con el
      mensaje de `verificados`. Revertir.
    - **Mutación 3 — piso de términos:** apuntar la lectura de `terminos-insinuacion.ts` a una ruta
      inexistente ⇒ rc≠0 (el guard lanza, no pasa con cero términos). Revertir.
    - **Mutación 4 — piso de negaciones inline:** hacer que la extracción del cuerpo de
      `NEGACIONES_LOCKED` devuelva `[]` ⇒ el assert `inline === 2` cae, rc≠0. Revertir.
    - **Mutación 5 — piso de idioms:** hacer que la extracción de `idioms-panel.ts` devuelva `[]` ⇒
      el assert `idioms >= 10` cae, rc≠0. Revertir.
      *Las mutaciones 4 y 5 son separadas porque los pisos son separados: con un piso sumado, una
      sola de las dos podría seguir pasando gracias a la otra.*
    - **Mutación 6 — el strip:** desactivar `stripTsComments` en la extracción de
      `NEGACIONES_LOCKED` y comprobar que el assert `inline === 2` **cae** (pasaría a 20). Revertir.
      Es la prueba de que el strip está haciendo trabajo real y no es decorativo.
    - `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>G1 verde con las SEIS mutaciones demostradas, los TRES pisos con su conteo real citado, el strip aplicado a los dos archivos, y las cinco constantes `.tsx` no cubiertas declaradas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G2 — literales de etiqueta en app/, matching CASE-SENSITIVE</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G2 y la decisión de operador **D-133-G**
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K1 (dónde vive; walk completo **sin allowlist**; piso
      `> 100`; sanity de archivo concreto; **G2 NO strippea comentarios**, decisión escrita en el
      JSDoc; los `.test.ts` excluidos)
    - `app/lib/lockdown-guard.test.ts:102-135` (`walkSourceFiles` + `SKIP_DIRS`) y `:735`
      (el piso `>10`, **demasiado laxo** para G2)
    - `app/lib/anti-insinuacion-guard.test.ts:920-932` (sanity de archivo concreto) y **`:843`**
      (`buildTermRegex` compila con el flag **`"i"`** — case-INsensitive)
    - **`app/app/buscar/page.tsx:215`** — el caso conocido que decide el diseño:
      `// Ambiguo o ninguno → null (fail-honest).`
    - `133-RESEARCH.md` §E modos de fallo 1-4 (cero-vacuo por `try{readdirSync}catch{return out}`;
      la sobre-cobertura es virtud; G2 se cazaría a sí mismo si escaneara `.test.ts`)
    - `133-PREMORTEM-PLANES.md` P12/P13 (el walk da **158** archivos; **0 hits** case-sensitive)
  </read_first>
  <files>packages/news/src/eval/taxonomia-superficie-guard.test.ts</files>
  <behavior>
    - El walk de `app/` devuelve **> 100** archivos `.ts`/`.tsx` no-test (medición: 158). Un `[]` por
      raíz mal resuelta pone el test rojo, no verde.
    - Sanity: leer un archivo conocido de `app/` devuelve longitud > 100.
    - Ninguno de esos archivos contiene ninguno de los 6 literales de `ETIQUETAS`, buscados de forma
      **CASE-SENSITIVE**.
    - Anti-cero-vacuo: un contador `escaneados` iguala la longitud de la lista del walk.
    - Control positivo apareado (existencia del literal): el MISMO string, con y sin
      `tramitacion_legislativa` insertado.
    - **Control de capitalización, UNA sola variable:** la función de matching de G2 aplicada a
      `"// Ambiguo o ninguno"` devuelve **0** hits, y aplicada a `"// ambiguo o ninguno"` devuelve
      **1**. Los dos strings son idénticos salvo una letra.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-superficie-guard.test.ts`. Importar `ETIQUETAS` de
    `./taxonomia` (ruta relativa; **prohibido tocar `./index.ts`**). Localizar `app/` con
    `join(findWorkspaceRoot(import.meta.dirname), "app")`, importando `findWorkspaceRoot` de
    **`../run-news-cli`**.

    Enumeración: copiar `walkSourceFiles` + `SKIP_DIRS` de `app/lib/lockdown-guard.test.ts:102-135`
    con cita de origen. **Walk completo de `app/`, SIN allowlist** — la sobre-cobertura es virtud.

    Piso anti-cero-vacuo `expect(archivos.length).toBeGreaterThan(100)` (medición: 158), más el
    sanity de archivo concreto conocido.

    **G2 NO strippea comentarios.** Más simple y estrictamente más estricto. Escribirlo en el JSDoc.

    **MATCHING CASE-SENSITIVE — decisión de diseño, con su razón en el JSDoc.**
    `buildTermRegex` del guard de `app/` compila con el flag `"i"`
    (`anti-insinuacion-guard.test.ts:843`). G2 **no** hereda ese flag: combinado con "no strippear
    comentarios", `"i"` haría que `app/app/buscar/page.tsx:215`
    (`// Ambiguo o ninguno → null (fail-honest).`) matcheara el literal `ambiguo` con frontera
    limpia, y **G2 nacería rojo sobre un comentario en castellano ajeno a la taxonomía**.
    Verificado en el repo: **case-sensitive → 0 hits en los 6 literales; case-insensitive → 1 hit,
    ese comentario.**
    Lo que D-133-G prohíbe es que **el literal de etiqueta** (snake_case, tal cual sale de
    `ETIQUETAS`) llegue a una superficie renderizada; una palabra castellana capitalizada en un
    comentario no es ese literal. G2 busca el **literal snake_case exacto**, sin `"i"`, y lo declara
    en el JSDoc citando `app/app/buscar/page.tsx:215` como el caso que motiva la decisión.
    **Si aun así apareciera un hit: PARAR y escalar. JAMÁS editar `app/` para poner el guard verde.**
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 5` y `test $((N_T2 - N_ANTES)) -eq 10` (delta total del plan).
      Todos los números citados.
    - `grep -c 'toBeGreaterThan(100)' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1.
    - `n=$(grep -ci 'no strippea' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -ge 1`.
    - **El criterio REAL de case-sensitividad es el `it` (e), ejecutado por la función de matching de
      G2**, no un grep: `"// Ambiguo o ninguno"` → 0 hits y `"// ambiguo o ninguno"` → 1 hit. Los dos
      strings difieren en **una sola letra** (la `A`/`a`); todo lo demás —longitud, prefijo `//`,
      palabras siguientes— es idéntico.
    - Respaldo por fuente (no sustituye al `it`): la regex de G2 no lleva el flag `i` —
      `n=$(grep -cE 'new RegExp\([^)]*,\s*"[gimsuy]*i[gimsuy]*"\)' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -eq 0`.
    - `grep -c 'from "./taxonomia"' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1
      (las etiquetas se importan, no se re-tipean).
    - **Ocurrencias del literal, contadas como ocurrencias y no como líneas** (`grep -c` cuenta
      líneas y daría un falso rojo si dos aparecen en la misma):
      `n=$(grep -o 'tramitacion_legislativa' packages/news/src/eval/taxonomia-superficie-guard.test.ts | wc -l); test "$n" -eq 2`.
      Las **dos** ocurrencias esperadas son, y solo son: la del fixture **con** el literal del
      control positivo apareado (d), y la del fixture del `it` (e). Cualquier otra aparición
      significa que las etiquetas se están re-tipeando en vez de importarse de `ETIQUETAS`.
    - `n=$(grep -cE 'from "\.\./index"|from "\./index"' packages/news/src/eval/taxonomia-superficie-guard.test.ts || true); test "$n" -eq 0`.
    - **Mutación sobre disco real (el guard muerde):** crear temporalmente
      `app/lib/__tmp-g2-mutacion.ts` con el literal `tramitacion_legislativa` dentro de un string,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run taxonomia-superficie > /tmp/mut6.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0` **y** `grep -c '__tmp-g2-mutacion' /tmp/mut6.log` ≥ 1. **Borrar el archivo
      temporal** y re-correr para confirmar verde. Sin este paso, G2 podría no estar escaneando nada.
    - **Mutación del piso:** apuntar la raíz a un directorio inexistente ⇒ rc≠0 (no un `[]` verde).
      Revertir.
    - `git status --short` al terminar no lista ningún archivo en `app/` (el temporal fue borrado).
    - `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>G2 escanea `app/` completo con piso > 100, matchea CASE-SENSITIVE (probado por el par que difiere en una letra), no encuentra ningún hit, y su mutación sobre disco real nombra el archivo infractor.</done>
</task>

<task type="auto">
  <name>Task 3: Los tres guards al script `guards` de la raíz, por NOMBRE (17 → 20)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K3 (por nombre, **jamás por glob**; molde de
      `@obs/dinero`/`@obs/llm`; 17 → 20)
    - **`package.json:13`** — el JSDoc `//guards` (*"entrypoint reproducible de los 17 guards de
      régimen"* y *"D-13: jamás glob — `vitest run src/*guard*.test.ts` sale 0 sin correr nada"*).
      **Verificado con `sed -n '12,13p'`: `:12` es `"lint"`, `:13` es `"//guards"`, `:14` es
      `"guards"`.** (La ronda anterior citaba `:12`.)
    - `app/lib/create-view-guard.test.ts:444-451` (test de "nombres fantasma")
    - `133-PREMORTEM-PLANES.md` §BLOCKER 6 — **`grep -c '\*guard\*' package.json` vale 1 hoy y
      seguirá valiendo 1**: el literal vive dentro del JSDoc que este plan manda **conservar**.
    - `133-PREMORTEM-PLANES.md` §P16 — con `NO_COLOR=1`, `pnpm guards` imprime hoy **3** conteos
      (`347`, `34`, `7`). La meta de **4** es correcta.
  </read_first>
  <files>package.json</files>
  <action>
    Añadir al final del script `guards` de la raíz (`package.json:14`), con el molde de los bloques
    de `@obs/dinero` y `@obs/llm`:
    `&& pnpm --filter @obs/news exec vitest run src/eval/taxonomia-guard.test.ts src/eval/taxonomia-superficie-guard.test.ts src/eval/congelado.test.ts`

    Los tres nombres **explícitos**. Jamás un glob (D-13).

    Actualizar el JSDoc `//guards` (**`package.json:13`**): de *"17 guards de régimen = 11 (app) + 3
    (@obs/dinero) + 3 (@obs/llm)"* a *"20 guards de régimen = 11 (app) + 3 (@obs/dinero) + 3
    (@obs/llm) + 3 (@obs/news, Phase 133-a)"*. **Conservar íntegro el aviso D-13 sobre el glob** —
    es una lección LOCKED documentada, y el criterio de abajo está escrito para no obligar a borrarla.

    Verificar antes de commitear que los tres archivos **existen en disco** con esos nombres exactos.

    NO tocar `app/package.json:11`: G3 se implementó modificando `anti-insinuacion-guard.test.ts`.
  </action>
  <acceptance_criteria>
    - Esta tarea añade **0** tests al paquete: el delta total del plan sigue siendo
      `test $((N_T3 - N_ANTES)) -eq 10`.
    - `NO_COLOR=1 pnpm guards 2>&1 | tee /tmp/133-guards.log` sale rc=0 **y**
      `n=$(grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log || true); test "$n" -eq 4`
      (una línea por cada `pnpm --filter` encadenado: app, dinero, llm, news). Hoy, sin el cambio,
      da **3**: citar ambos números.
    - El bloque de `@obs/news` reporta **más de 0** tests: extraer el cuarto conteo y verificar > 0.
      Un `0 passed` sería el nombre fantasma exacto.
    - `n=$(grep -c 'filter @obs/news exec vitest run' package.json || true); test "$n" -eq 1`.
    - **Cero globs EN EL SCRIPT (no en el archivo):**
      `node -e 'const s=require("./package.json").scripts.guards; if(/\*guard\*/.test(s)) { console.error("GLOB EN EL SCRIPT: "+s); process.exit(1);} console.log("sin glob")'`
      sale rc=0. **Y el aviso D-13 sigue en el archivo:**
      `n=$(grep -c '\*guard\*' package.json || true); test "$n" -eq 1` — se exige que **siga
      valiendo 1**, no 0: borrarlo sería perder la lección.
    - `n=$(grep -c '20 guards de régimen' package.json || true); test "$n" -eq 1`.
    - **Mutación (nombre fantasma), por CONTEO y no por rc:** renombrar temporalmente
      `packages/news/src/eval/congelado.test.ts` a `congelado.test.ts.bak`,
      `if NO_COLOR=1 pnpm guards > /tmp/mut7.log 2>&1; then rc=0; else rc=$?; fi`. Con
      `passWithNoTests: true` esto **puede salir 0** — por eso el criterio es el conteo: el cuarto
      bloque de `/tmp/mut7.log` debe reportar **menos** tests que en `/tmp/133-guards.log`, citando
      ambos números. Restaurar y re-correr para confirmar el conteo original.
      Un criterio basado en rc sería literalmente inalcanzable.
    - `git diff --name-only <SHA literal del inicio de la tarea>` lista `package.json` y nada más.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm guards 2>&1 | tee /tmp/133-guards.log; n=$(grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log || true); echo "bloques=$n"; test "$n" -eq 4</automated>
  </verify>
  <done>`pnpm guards` corre los 20 guards de régimen por nombre con cuatro conteos impresos; el aviso D-13 sigue en el archivo; la prueba del nombre fantasma demostrada por diferencia de conteo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxonomia.ts` → copy renderizado | Una glosa insinuante entraría al producto por el carril de news, esquivando el linter de `app/` |
| `packages/news` → `app/` | G2 debe leer `app/` sin invertir la dirección de dependencia del monorepo |
| script `guards` → guards en disco | Un nombre que no existe sale 0 en silencio (`passWithNoTests`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-15 | Information Disclosure | Copy insinuante en las glosas de la taxonomía | mitigate | G1 sobre cada string, `verificados === 30`, mutación con término inyectado |
| T-133-16 | Information Disclosure | Un literal de etiqueta renderizado en `app/` viola D-133-G | mitigate | G2 walk completo sin allowlist, piso > 100, mutación sobre disco real |
| T-133-17 | Repudiation | Guard con nombre fantasma sale 0 sin correr nada | mitigate | Criterio por **conteo impreso** (4 bloques) + mutación por renombrado |
| T-133-18 | Tampering | G2 en `app/` importando `@obs/news` rompería el desacoplamiento | mitigate | G2 en `packages/news`, lee `app/` por bytes, `findWorkspaceRoot` del módulo directo |
| T-133-25 | Repudiation | Sustracción de negaciones sobre literales de JSDoc ⇒ "no removió nada" trivialmente cierto | mitigate | `stripTsComments` sobre **los dos** archivos + **dos pisos separados** (`inline === 2`, `idioms >= 10`) + tres mutaciones (4, 5 y 6) que los ejercitan por separado |
| T-133-26 | Repudiation | G2 case-insensitive ⇒ falso rojo sobre un comentario castellano ⇒ tentación de editar `app/` | mitigate | Matching CASE-SENSITIVE, con el par que **difiere en una sola letra** como criterio ejecutado por la función real, y prohibición explícita de editar `app/` |
| T-133-30 | Repudiation | Un piso calibrado sin el método que el plan manda usar | mitigate | Los tres pisos (92 / 2 / 10) medidos **con `stripTsComments`**; la tabla de contraste sin-strip/con-strip queda en el plan |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES`, `N_T1`, `N_T2` citados; deltas exactos 5 / 5 y total 10.
2. `NO_COLOR=1 pnpm guards` → rc=0 y exactamente **4** líneas `Tests N passed` (hoy 3, ambos citados).
3. Las ocho mutaciones (término en glosa, `continue`, piso de términos, piso de negaciones inline,
   piso de idioms, desactivar el strip, archivo temporal en `app/`, nombre fantasma) con el idiom
   `if/rc` — jamás bajo `set -e`. La del nombre fantasma se juzga por **conteo**, no por rc.
4. El par de capitalización de G2, ejecutado por la función real: `"// Ambiguo o ninguno"` → 0;
   `"// ambiguo o ninguno"` → 1. Difieren en una letra.
5. `git status --short` limpio de temporales (especialmente en `app/`).
6. `congelado.test.ts` verde tras revertir la mutación de `taxonomia.ts`.
7. `git diff --name-only <SHA literal>` no lista `packages/news/src/eval/index.ts`.
</verification>

<success_criteria>
- G1 verde: cero términos prohibidos en los 30 strings de la taxonomía; **tres** pisos (92 / 2 / 10)
  con su conteo real citado; strip aplicado a los dos archivos; las cinco constantes `.tsx` no
  cubiertas declaradas por nombre.
- G2 verde: cero literales de etiqueta en `app/`, CASE-SENSITIVE probado por el par de una letra,
  walk > 100, y mutación sobre disco real que muerde.
- `pnpm guards` corre 20 guards por nombre con cuatro conteos impresos.
- El JSDoc de `package.json:13` dice 20, y el aviso D-13 sobre el glob **sigue ahí**.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-05-SUMMARY.md` when done.
Cita: los cuatro conteos de `pnpm guards` (y el 3 de antes), los **tres** conteos de extracción
(términos, negaciones inline, idioms), y los pares de números de cada mutación por conteo.
</output>
