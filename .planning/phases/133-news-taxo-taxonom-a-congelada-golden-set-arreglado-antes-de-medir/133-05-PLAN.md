---
phase: 133-news-taxo
plan: 05
type: execute
wave: 3
depends_on: ["133-01", "133-02", "133-03"]
files_modified:
  - packages/news/src/eval/taxonomia-guard.test.ts
  - packages/news/src/eval/taxonomia-superficie-guard.test.ts
  - package.json
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Ningún string de taxonomia.ts contiene un término prohibido del guard anti-insinuación"
    - "Ningún literal de etiqueta de la taxonomía aparece en ningún archivo fuente de app/"
    - "G1, G2 y congelado.test.ts corren desde `pnpm guards` de la raíz, por nombre y jamás por glob"
    - "Los tres guards muerden: inyectar la violación pone el test rojo"
  artifacts:
    - path: "packages/news/src/eval/taxonomia-guard.test.ts"
      provides: "G1 — términos prohibidos sobre cada string de la taxonomía"
    - path: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      provides: "G2 — ningún literal de etiqueta en superficie de app/ (D-133-G)"
  key_links:
    - from: "packages/news/src/eval/taxonomia-guard.test.ts"
      to: "app/lib/terminos-insinuacion.ts"
      via: "readFileSync por DISCO (bytes, no import — no invierte la dirección del monorepo)"
      pattern: "terminos-insinuacion"
    - from: "packages/news/src/eval/taxonomia-superficie-guard.test.ts"
      to: "app/"
      via: "findWorkspaceRoot + walk completo"
      pattern: "findWorkspaceRoot"
    - from: "package.json"
      to: "los tres guards de @obs/news"
      via: "script `guards`, por nombre"
      pattern: "filter @obs/news exec vitest run"
---

<objective>
G1 y G2 (D-133-A2.4, D-133-J2, D-133-K1), y el enganche de los tres guards nuevos al script
`guards` de la raíz (D-133-K3).

Purpose: con D-133-G la etiqueta no se renderiza jamás — pero *"el cumplimiento va por guard, no
por promesa"*. Y un guard de régimen fuera del entrypoint reproducible de guards es incoherente con
la propia declaración de ese script.
Output: `taxonomia-guard.test.ts`, `taxonomia-superficie-guard.test.ts`, script `guards` de 17 → 20.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-02-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-03-SUMMARY.md

<interfaces>
Disponible del plan 133-01:
  packages/news/src/eval/taxonomia.ts → TAXONOMIA (readonly ClaseTaxonomia[]), ETIQUETAS
Disponible del plan 133-02 (en `app/`, se lee por DISCO, jamás por import):
  app/lib/terminos-insinuacion.ts → TERMINOS_PROHIBIDOS, TERMINOS_LINK_EXT, TERMINOS_COBERTURA
Disponible del plan 133-03:
  packages/news/src/eval/congelado.test.ts
Herramienta del propio paquete (exportada por el barrel `packages/news/src/index.ts:46`):
  findWorkspaceRoot(start: string): string   // sube hasta pnpm-workspace.yaml; LANZA si no lo halla
Patrones a copiar verbatim con cita de origen:
  app/lib/anti-insinuacion-guard.test.ts:828-841  → buildTermRegex + WORD (frontera con acentos)
  app/lib/anti-insinuacion-guard.test.ts:865-871  → la sustracción de negaciones antes de matchear
  app/lib/anti-insinuacion-guard.test.ts:1076-1080 → `expect(verificados).toEqual(...)` anti-cero-vacuo
  app/lib/lockdown-guard.test.ts:102-135          → walkSourceFiles + SKIP_DIRS
  app/lib/anti-insinuacion-guard.test.ts:920-932  → sanity de archivo concreto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G1 — términos prohibidos sobre CADA string de taxonomia.ts</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G1 (prueba de mutación obligatoria: inyectar un término
      prohibido en una glosa y **demostrar que el test cae**)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2 completo — en especial **J2.3** (lectura por
      DISCO, con piso duro sobre el conteo extraído) y **J2.4** (la interpretación de
      `NEGACIONES_LOCKED`: se aplica la misma **sustracción** antes del match y se asserta que la
      sustracción **no removió nada**; si una glosa contuviera una negación LOCKED verbatim, eso
      **debe fallar**)
    - `app/lib/anti-insinuacion-guard.test.ts:828-841` (`buildTermRegex`/`WORD` — **NO** es
      accent-insensitive; las glosas de la taxonomía llevan tildes) y `:857-874` (`detectarInsinuaciones`)
    - `app/lib/anti-insinuacion-guard.test.ts:1017-1086` (el molde: fixture EN MEMORIA que pasa por
      el detector real, con su JSDoc de alcance honesto) y `:1076-1080` (el `verificados` explícito)
    - `133-RESEARCH.md` §K P-9 (`_` está dentro de `WORD` ⇒ `no_legislativa` no dispararía un
      término `legislativa`)
  </read_first>
  <files>packages/news/src/eval/taxonomia-guard.test.ts</files>
  <behavior>
    - El guard extrae `TERMINOS_PROHIBIDOS` (+ `TERMINOS_LINK_EXT`, `TERMINOS_COBERTURA`) leyendo
      `app/lib/terminos-insinuacion.ts` **por disco**, y asserta un **piso duro** de
      `>= 100` términos extraídos. Un fallo de extracción debe ser ruidoso, no un cero vacuo.
    - **Cada** string de `TAXONOMIA` (`etiqueta`, `definicion`, `marca_decisoria`, `frontera`,
      `enruta_a`) pasa por el detector y **cero** términos prohibidos aparecen.
    - Anti-cero-vacuo: un contador `verificados` acumula cada string efectivamente pasado por el
      detector, y el test asserta que su conteo iguala al número total de strings de `TAXONOMIA`
      (6 clases × 5 campos). Un `continue` colado en el bucle debe poner el test rojo.
    - Sustracción de negaciones aplicada antes del match, **y se asserta que no removió nada**
      (D-133-J2.4).
    - Sanity de archivo concreto: la lectura de `app/lib/terminos-insinuacion.ts` devuelve contenido
      de longitud > 100. Si la raíz está mal resuelta, `readFileSync` **lanza** en vez de pasar verde.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-guard.test.ts`. Localiza `app/` con
    `findWorkspaceRoot(import.meta.dirname)` (lanza si no encuentra `pnpm-workspace.yaml` —
    fail-loud, no skip). **Prohibido** `resolve(dirname, "../../../../app")`: se rompe en silencio al
    mover el archivo.

    Lee `app/lib/terminos-insinuacion.ts` con `readFileSync` y extrae los literales de los tres
    arrays por regex. El JSDoc debe declarar por qué es lectura de **bytes y no un import**:
    *no se crea arista en el grafo de módulos y la dirección `app`→`packages` no se invierte*
    (D-133-J2.3). Piso duro `>= 100` inmediatamente después de extraer.

    Copiar `buildTermRegex`/`WORD` **verbatim** de `app/lib/anti-insinuacion-guard.test.ts:828-841`
    con cita de origen en el JSDoc (es el precedente del repo: `anti-insinuacion-guard.test.ts:1-2`
    se declara *"Espejo EXACTO de lockdown-guard.test.ts"*). No inventar un `\b` de JS: trata los
    acentuados como no-palabra y produce falsos positivos y negativos en español.

    Copiar el idiom `verificados` de `:1076-1080` con su mensaje
    *"Algún string de la taxonomía no llegó al detector (¿un `continue`/filtro colado en el bucle?)"*.

    **Nota sobre `NEGACIONES_LOCKED` (D-133-J2.4):** no se mueve de su archivo. G1 aplica la misma
    sustracción semántica antes del match — usando los literales de negación que pueda leer por
    disco del guard — y asserta que **la sustracción no removió nada** de las glosas. Si removiera
    algo, es un hallazgo que debe fallar. Escribir esa interpretación en el JSDoc, no dejarla
    implícita.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05a.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05a.log` imprime un número mayor que el
      registrado al cierre del plan 133-03 (ambos citados en el SUMMARY). Nunca el exit code solo.
    - `grep -c 'findWorkspaceRoot' packages/news/src/eval/taxonomia-guard.test.ts` ≥ 1 y
      `grep -c '\.\./\.\./\.\./\.\.' packages/news/src/eval/taxonomia-guard.test.ts` = 0.
    - **Prueba de mutación** (no bajo `set -e`): inyectar un término prohibido literal en la glosa
      `definicion` de una clase de `taxonomia.ts`, correr
      `if pnpm --filter @obs/news exec vitest run taxonomia-guard > /tmp/mut5.log 2>&1; then rc=0; else rc=$?; fi`,
      comprobar `test $rc -ne 0` **y** que el log nombra la clase y el término. Revertir con
      `git checkout -- packages/news/src/eval/taxonomia.ts` y **re-correr `congelado.test.ts`** para
      confirmar que el hash volvió a su valor congelado.
    - **Prueba de mutación del anti-cero-vacuo:** añadir un `continue` en el bucle que salte una
      clase, comprobar rc≠0 con el mensaje de `verificados`. Revertir.
    - **Prueba de mutación del piso duro:** apuntar la lectura a una ruta inexistente y comprobar
      rc≠0 (el guard lanza, no pasa con cero términos). Revertir.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05a.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05a.log</automated>
  </verify>
  <done>G1 verde con las tres pruebas de mutación demostradas, leyendo los términos por disco con piso duro y sin invertir la dirección del monorepo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G2 — ningún literal de etiqueta en superficie de app/, con walk completo</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G2 y la decisión de operador **D-133-G** (la etiqueta es
      interna, jamás se muestra)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K1 completo (dónde vive, cómo enumera, walk completo
      **sin allowlist**, piso `> 100`, sanity de archivo concreto, **G2 NO strippea comentarios** y
      eso se escribe en el JSDoc como decisión, los `.test.ts` quedan excluidos)
    - `app/lib/lockdown-guard.test.ts:102-135` (`walkSourceFiles` + `SKIP_DIRS`) y `:733-736`
      (el piso `>10`, **demasiado laxo** para G2)
    - `app/lib/anti-insinuacion-guard.test.ts:920-932` (sanity de archivo concreto)
    - `133-RESEARCH.md` §E modos de fallo 1-4 (cero-vacuo por `try{readdirSync}catch{return out}`;
      la sobre-cobertura es virtud; G2 se cazaría a sí mismo si escaneara `.test.ts`)
  </read_first>
  <files>packages/news/src/eval/taxonomia-superficie-guard.test.ts</files>
  <behavior>
    - El walk de `app/` devuelve **> 100** archivos `.ts`/`.tsx` no-test. Un `[]` por raíz mal
      resuelta debe poner el test rojo, no verde.
    - Sanity de archivo concreto: leer un archivo conocido de `app/` devuelve longitud > 100.
    - Ninguno de esos archivos contiene ninguno de los 6 literales de `ETIQUETAS`.
    - Anti-cero-vacuo del bucle: un contador `escaneados` iguala la longitud de la lista del walk.
    - **Control positivo apareado, UNA sola variable:** el MISMO string de fixture en memoria, una
      versión **con** el literal `tramitacion_legislativa` insertado (el detector lo caza) y otra
      **sin** él (el detector no lo caza). Idéntico en todo lo demás: longitud, formato, ruta simulada.
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia-superficie-guard.test.ts`. Importa `ETIQUETAS` de
    `./taxonomia` (misma carpeta: single-source respetado, D-133-A2 — **prohibido re-escribir las
    etiquetas**). Localiza `app/` con `join(findWorkspaceRoot(import.meta.dirname), "app")`.

    Enumeración: copiar el patrón `walkSourceFiles` + `SKIP_DIRS` de
    `app/lib/lockdown-guard.test.ts:102-135`, con cita de origen. **Walk completo de `app/`, SIN
    allowlist** — la sobre-cobertura es virtud: nunca produce un falso verde, solo falsos rojos
    ruidosos que se resuelven registrando la excepción. Un allowlist reproduciría exactamente el
    modo de fallo que G3 acaba de cerrar en el plan 133-02.

    Piso anti-cero-vacuo: `expect(archivos.length).toBeGreaterThan(100)` — **no** el `>10` laxo del
    existente. Más el sanity de archivo concreto conocido de `app/` (idiom de
    `anti-insinuacion-guard.test.ts:920-932`).

    **G2 NO strippea comentarios.** Es más simple y estrictamente más estricto que reusar
    `stripTsComments`. Escribirlo en el JSDoc **para que sea decisión, no descuido** (D-133-K1).

    Los `.test.ts`/`.tsx` quedan excluidos por el filtro del walk — necesario, o G2 se cazaría a sí
    mismo. Declararlo también en el JSDoc.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05b.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05b.log` imprime un número mayor que el de la
      Task 1 (ambos citados).
    - `grep -c 'toBeGreaterThan(100)' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1
      (el piso realista, no el `>10`).
    - `grep -c 'no strippea\|NO strippea' packages/news/src/eval/taxonomia-superficie-guard.test.ts` ≥ 1
      (la decisión escrita, no implícita).
    - `grep -c '"tramitacion_legislativa"' packages/news/src/eval/taxonomia-superficie-guard.test.ts`
      solo aparece dentro del fixture del control positivo, no como lista de etiquetas re-tipeada:
      verificar que `ETIQUETAS` se importa (`grep -c 'from "./taxonomia"'` ≥ 1).
    - **Prueba de mutación (el guard muerde sobre disco real):** crear temporalmente
      `app/lib/__tmp-g2-mutacion.ts` con el literal `tramitacion_legislativa` dentro de un string,
      correr `if pnpm --filter @obs/news exec vitest run taxonomia-superficie > /tmp/mut6.log 2>&1; then rc=0; else rc=$?; fi`,
      comprobar `test $rc -ne 0` **y** que el log nombra `__tmp-g2-mutacion.ts`. **Borrar el archivo
      temporal** y re-correr para confirmar verde. Sin este paso G2 podría no estar escaneando nada.
    - **Prueba de mutación del piso:** apuntar la raíz a un directorio inexistente y comprobar rc≠0
      (no un `[]` verde). Revertir.
    - `git status --short` al terminar la tarea no lista ningún archivo en `app/` (el temporal de
      mutación fue borrado).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-05b.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-05b.log</automated>
  </verify>
  <done>G2 escanea `app/` completo con piso > 100, no encuentra ningún literal de etiqueta, y su mutación sobre disco real demuestra que muerde y nombra el archivo infractor.</done>
</task>

<task type="auto">
  <name>Task 3: Los tres guards al script `guards` de la raíz, por NOMBRE (17 → 20)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K3 (por nombre, **jamás por glob**; molde de
      `@obs/dinero`/`@obs/llm`; 17 → 20; y el corolario de que G3 modifica el guard existente en vez
      de crear archivo nuevo — ya resuelto en el plan 133-02)
    - `package.json:12` (el JSDoc `//guards`: *"entrypoint reproducible de los 17 guards de régimen"*
      y *"D-13: jamás glob — `vitest run src/*guard*.test.ts` sale 0 sin correr nada"*)
    - `package.json:14` (el script `guards` actual, con los moldes de `@obs/dinero` y `@obs/llm`)
    - `app/lib/create-view-guard.test.ts:444-451` (el test de "nombres fantasma" — un nombre en el
      script que no existe en disco es tan grave como un guard en disco fuera del script)
  </read_first>
  <files>package.json</files>
  <action>
    Añadir al final del script `guards` de la raíz (`package.json:14`), con el molde exacto de los
    bloques de `@obs/dinero` y `@obs/llm`:
    `&& pnpm --filter @obs/news exec vitest run src/eval/taxonomia-guard.test.ts src/eval/taxonomia-superficie-guard.test.ts src/eval/congelado.test.ts`

    Los tres nombres van **explícitos**. Jamás un glob: el propio JSDoc lo prohíbe (D-13) porque
    `vitest run src/*guard*.test.ts` sale 0 sin correr nada.

    Actualizar el JSDoc `//guards` (`package.json:12`): de *"17 guards de régimen = 11 (app) + 3
    (@obs/dinero) + 3 (@obs/llm)"* a *"20 guards de régimen = 11 (app) + 3 (@obs/dinero) + 3
    (@obs/llm) + 3 (@obs/news, Phase 133-a)"*. Dejar el número desactualizado sería la clase de
    drift documental que esta fase existe para prevenir.

    Verificar antes de commitear que los tres archivos **existen en disco** con esos nombres exactos
    — un nombre fantasma en el script, con `passWithNoTests: true`, sale 0 en silencio y es
    precisamente el falso verde que `create-view-guard.test.ts:444-451` persigue.

    NO tocar `app/package.json:9`: G3 se implementó modificando `anti-insinuacion-guard.test.ts`
    (plan 133-02), no creando un archivo nuevo, así que no hay nada que añadir allí.
  </action>
  <acceptance_criteria>
    - `pnpm guards 2>&1 | tee /tmp/133-guards.log` sale rc=0 **y** el log contiene **cuatro** líneas
      `Tests N passed` (una por cada `pnpm --filter` encadenado: app, dinero, llm, news):
      `grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log` = **4**. El exit code solo no basta.
    - El bloque de `@obs/news` del log reporta **más de 0 tests**:
      extraer su conteo y verificar > 0. Un `0 passed` sería el nombre fantasma exacto.
    - `grep -c 'filter @obs/news exec vitest run' package.json` = 1.
    - `grep -c '\*guard\*' package.json` = 0 (cero globs, D-13).
    - `grep -c '20 guards de régimen' package.json` = 1 (el JSDoc actualizado).
    - **Prueba de mutación (nombre fantasma):** renombrar temporalmente
      `packages/news/src/eval/congelado.test.ts` a `congelado.test.ts.bak`, correr
      `if pnpm guards > /tmp/mut7.log 2>&1; then rc=0; else rc=$?; fi`. Con `passWithNoTests: true`
      esto **puede salir 0** — por eso el criterio no es el rc sino el **conteo**: comprobar que el
      bloque de `@obs/news` en `/tmp/mut7.log` reporta **menos** tests que en `/tmp/133-guards.log`,
      citando ambos números. Restaurar el nombre y re-correr para confirmar el conteo original.
      Este criterio es la única defensa contra el nombre fantasma; un criterio basado en rc sería
      literalmente inalcanzable.
    - `git diff --name-only <SHA literal del inicio de la tarea>` lista `package.json` y nada más.
  </acceptance_criteria>
  <verify>
    <automated>pnpm guards 2>&1 | tee /tmp/133-guards.log; grep -cE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-guards.log</automated>
  </verify>
  <done>`pnpm guards` corre los 20 guards de régimen por nombre, con cuatro conteos impresos y la prueba del nombre fantasma demostrada por diferencia de conteo, no por exit code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxonomia.ts` → copy renderizado | Una glosa insinuante entraría al producto por el carril de news, esquivando el linter de `app/` |
| `packages/news` → `app/` | G2 debe leer `app/` sin invertir la dirección de dependencia del monorepo |
| script `guards` → guards en disco | Un nombre en el script que no existe sale 0 en silencio (`passWithNoTests`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-15 | Information Disclosure | Copy insinuante en las glosas de la taxonomía | mitigate | G1 sobre cada string, con `verificados` anti-cero-vacuo y mutación que inyecta un término prohibido |
| T-133-16 | Information Disclosure | Un literal de etiqueta renderizado en `app/` viola D-133-G | mitigate | G2 con walk completo sin allowlist, piso > 100, sanity de archivo concreto, y mutación sobre disco real que nombra el archivo infractor |
| T-133-17 | Repudiation | Guard con nombre fantasma en el script `guards` sale 0 sin correr nada | mitigate | Criterio por **conteo impreso**, no por rc; mutación por renombrado que compara conteos |
| T-133-18 | Tampering | G2 en `app/` importando `@obs/news` rompería el desacoplamiento documentado en 3 lugares | mitigate | G2 vive en `packages/news` y lee `app/` por **bytes**; `findWorkspaceRoot` fail-loud en vez de ruta relativa frágil |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `pnpm --filter @obs/news exec vitest run` → conteo final citado, mayor que el del plan 133-03.
2. `pnpm guards` → rc=0 **y** exactamente 4 líneas `Tests N passed`, con el bloque de `@obs/news` > 0.
3. Las cinco pruebas de mutación (término prohibido en glosa, `continue` en el bucle de G1, piso de
   G1, archivo temporal con el literal en `app/`, nombre fantasma en el script) demostradas con el
   idiom `if CMD > log 2>&1; then rc=0; else rc=$?; fi` — jamás bajo `set -e`.
4. `git status --short` limpio de artefactos temporales de mutación (especialmente en `app/`).
5. `pnpm --filter @obs/news exec vitest run congelado` verde tras revertir la mutación de
   `taxonomia.ts`: el hash congelado volvió a su valor.
</verification>

<success_criteria>
- G1 verde: cero términos prohibidos en los 30 strings de la taxonomía, con extracción por disco y piso duro.
- G2 verde: cero literales de etiqueta en `app/`, con walk completo de > 100 archivos y mutación que muerde.
- `pnpm guards` corre 20 guards de régimen por nombre, con cuatro conteos impresos.
- El JSDoc de `package.json` dice 20, no 17.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-05-SUMMARY.md` when done.
El SUMMARY cita los conteos de `pnpm guards` (los cuatro) y los pares de números de cada prueba de
mutación por conteo.
</output>
