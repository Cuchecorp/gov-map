---
phase: 133-news-taxo
plan: b-03
type: execute
wave: 3
depends_on: ["133-b-01", "133-b-02"]
files_modified:
  - packages/news/src/eval/cobertura.ts
  - packages/news/src/eval/cobertura.test.ts
  - packages/news/src/eval/cobertura-cli.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "La cobertura de `prefiltro.terminos` sobre el censo P es 100,00 % y está CONFIRMADA de forma reproducible"
    - "Ese 100 % no es vacuo: el control de mutación del truncado lo hace CAER a 87,84 / 54,05 / 40,54 / 0,00 %"
    - "El gate del 95 % es fail-closed: bajo el umbral se LANZA, jamás se reporta verde"
    - "El límite de truncado NO sube: la confirmación llega antes de etiquetar un solo caso"
    - "El control se logra con un truncador inyectable en la MEDICIÓN — el pre-filtro no se toca ni se ablanda"
  artifacts:
    - path: "packages/news/src/eval/cobertura.ts"
      provides: "Medición de cobertura con truncador inyectable + gate fail-closed del 95 %"
      exports: ["UMBRAL_COBERTURA", "medirCobertura", "verificarCobertura"]
    - path: "packages/news/src/eval/cobertura-cli.ts"
      provides: "Confirmación reproducible sobre el censo P del pool congelado, con el gradiente completo"
      exports: ["confirmarCobertura", "main"]
  key_links:
    - from: "packages/news/src/eval/cobertura.ts"
      to: "packages/news/src/eval/entrada-llm.ts"
      via: "coberturaTerminos / construirEntradaLlm (nunca reimplementadas)"
      pattern: "coberturaTerminos"
    - from: "packages/news/src/eval/cobertura.ts"
      to: "packages/news/src/prefiltro-lexico.ts"
      via: "terminosQueMatchean + truncarDescripcion (el truncador por defecto ES el real)"
      pattern: "truncarDescripcion"
---

<objective>
**D-133-F2.2 + D-133b-3 paso 1.** El chequeo de cobertura de `prefiltro.terminos` debe correr
**antes de etiquetar un solo caso**: si diera < 95 %, el límite de truncado tendría que subir
**antes**; hacerlo después mueve el hash de `golden-set.json` y **obliga a re-etiquetar todo el
golden**. Era el riesgo más caro de la fase.

**Ya se midió: 100,00 % sobre 74/74** (`133-b-PREMORTEM.md` §P-02). Por lo tanto **este plan es
CONFIRMACIÓN automatizada y reproducible, NO remediación.** El límite de truncado se queda donde
está y no hay cambio de contrato antes de etiquetar.

Purpose: un 100 % que no puede bajar es un falso verde. Este plan deja el 100 % **atornillado a un
control de mutación**: reducir el truncado hace que la métrica **caiga** con un gradiente monótono
medido (200→87,84 %, 80→54,05 %, 0→40,54 %, entrada vacía→0,00 %), y el gate del 95 % **lanza**
cuando eso ocurre. Así nadie puede "optimizar" el truncado en 134/135 sin invalidar el golden.
Output: `cobertura.ts`, sus tests y `cobertura-cli.ts`.

**Este plan NO etiqueta ni un solo caso.**

**PROHIBIDO tocar `packages/news/src/prefiltro-lexico.ts`, `eval/entrada-llm.ts`,
`eval/caso-golden.ts`, `eval/index.ts`, `eval/pool-r2.ts`, `eval/muestreo.ts` y sus artefactos JSON.**
El control negativo se consigue **inyectando un truncador en la MEDICIÓN**, jamás mutando el
pre-filtro en el repo. Ablandar el pre-filtro o podar el vocabulario está **LOCKED-prohibido**
(`prefiltro-lexico.ts:6-9`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-ADJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-PREMORTEM.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-01-SUMMARY.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-02-SUMMARY.md
</context>

<orden_de_olas>
## Nota obligatoria sobre el orden de olas (W5) — declararla en el SUMMARY

D-133b-3 fija la secuencia **cobertura (paso 1) → congelar la muestra (paso 2) → etiquetar (paso 3)**.
Este plan corre en la **wave 3**, es decir **después** del congelado de la muestra (133-b-02, wave 2).

**Es una inversión respecto al texto de D-133b-3, y es inocua *solo* porque P-02 ya midió 100,00 %:**
al no haber remediación, el límite de truncado no cambia y la composición congelada en 133-b-02 sigue
siendo válida. **Si este plan hubiera salido < 95 %, la muestra congelada habría quedado inválida** y
habría que re-congelarla antes de etiquetar.

Lo que la secuencia protege de verdad —que **nadie etiquete** antes de que la cobertura pase— **se
mantiene intacto**: el primer plan que etiqueta es el 133-b-05, y el 133-b-04 declara
`gate=PASA` de este plan como precondición dura.

**El SUMMARY debe declarar esta inversión con estas palabras**, no insinuarla: la fase avanza
sabiendo que el orden real fue 02→03, no creyendo que fue 03→02.
</orden_de_olas>

<cifras_de_entrada>
Medidas en `133-b-PREMORTEM.md` §P-02 con las funciones reales de 133-a (`terminosQueMatchean` +
`construirEntradaLlm` + `coberturaTerminos`). **No se re-derivan: se exigen como literales.**

El censo P es de **74** casos, así que cada porcentaje corresponde a un **entero de casos cubiertos**
— y ese entero es el gate, mucho más difícil de falsear que un porcentaje redondeado:

| Truncado de la descripción | Cobertura medida | Casos cubiertos de 74 |
|---|---|---|
| actual (`truncarDescripcion` real) | **100,00 %** | **74** |
| 200 chars | 87,84 % | **65** |
| 80 chars | 54,05 % | **40** |
| 40 chars | 45,95 % | **34** |
| 0 chars (solo titular) | 40,54 % | **30** |
| entrada vacía | 0,00 % | **0** |

```
casos P reconstruidos:        74 / 74
casos con 0 términos:         0   (patología: ninguno)
cobertura actual:             100,00 %   (umbral 95 %)
umbral cruzado a 200 chars:   87,84 % < 95 %  ⇒ el margen lo sostiene el límite actual
```

**Gradiente monótono y fuerte, con los dos extremos correctos: la métrica muerde.**

**Si algún entero difiere del esperado, el pool cambió ⇒ PARAR y escalar.** Jamás ajustar el número
esperado al resultado obtenido: eso es precisamente el falso verde que estos gates existen para
impedir.
</cifras_de_entrada>

<medicion>
## Idiom de medición — OBLIGATORIO

`passWithNoTests: true` está ACTIVO (`packages/news/vitest.config.ts:10`) y los argumentos de
`vitest run` son **filtros de nombre, no rutas** ⇒ **se asserta el conteo impreso, jamás el exit code
solo**. `NO_COLOR=1` obligatorio.

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

`-t` de vitest es **REGEX**: escapar paréntesis y assertar `test "$N" -eq 1`.

### Extractor de campos — OBLIGATORIO (B2). Este plan es donde más muerde.

**`get () { echo "$L" | grep -oE "$1=[0-9]+" | grep -oE '[0-9]+'; }` está PROHIBIDO.** Los nombres de
campo de este plan **contienen dígitos** (`t200`, `t80`, `t0`): el segundo `grep` devuelve **DOS
líneas** (`200` y `65`) y `test` sale **rc=2 siempre**. Reproducido:

```
$ get t200   →  "200\n65"   →  test: integer expression expected, rc=2
```

Eran **tres de los cuatro gates del gradiente** —justo los que hacen que el 100 % muerda— en falso
rojo permanente. El idiom correcto, verificado (`t200=65 t80=40 t0=30 tvacio=0`):

```bash
get () { echo "$L" | sed -nE "s/.*(^| )$1=([0-9]+).*/\2/p"; }
```

### Nombrar el test que cae — OBLIGATORIO (W2)

Toda mutación lleva su comando, no una frase: `grep -qF '(x)' /tmp/mutXX.log`. Sin él, una mutación
que rompe OTRO test cuenta como verde. `-F` obligatorio (los paréntesis son metacaracteres).

`grep -c` sobre **más de un archivo** prefija el nombre (`f1.ts:0`) ⇒ `n` deja de ser entero y
`test "$n" -eq 0` sale **rc=2 siempre**. Reproducido. Con varios archivos, **siempre**
`cat <f1> <f2> | grep -cE ...` (B3).

Piso secundario de sanidad: `-gt 268` (base de 133-a) **más** los deltas de 133-b-01 y 133-b-02.
El criterio fuerte es el **DELTA propio de este plan**.

SHA base: `SHA_BASE=$(git rev-parse HEAD); echo "$SHA_BASE"` al inicio, **citado literal**.
Criterio de limpieza: `git status --short -- . ':(exclude).planning'`.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 1 | **7** | (a) truncador **real** sobre el censo P ⇒ **74/74** · (b) truncado a 200 ⇒ **65/74** · (c) truncado a 80 ⇒ **40/74** · (d) truncado a 0 (solo titular) ⇒ **30/74** · (e) entrada vacía ⇒ **0/74** · (f) lista vacía ⇒ **LANZA** · (g) el gradiente es **monótono no creciente** al reducir el truncado | (b)-(e)←ignorar el truncador inyectado y usar siempre el real ⇒ los cuatro caen · (f)←quitar el `throw` · (g)←invertir un punto del gradiente |
| Task 2 | **3** | (a) `verificarCobertura` **LANZA** bajo 0,95 · (b) `verificarCobertura` **NO lanza** en 0,95 exacto (frontera; control positivo apareado) · (c) con el truncador a 200 sobre el censo P real, `verificarCobertura` **LANZA** — el nexo entre el gradiente y el gate | (a)←degradar el `throw` a `console.warn` · (b)←`>` en vez de `>=` en la frontera · (c)←gate que ignora el truncador |
| **Total** | **10** | | |

`it.each` PROHIBIDO en los tests nuevos. Criterio final: `test $((N_T2 - N_ANTES)) -eq 10`.

**El anti-poda del vocabulario NO se duplica:** ya existe en
`packages/news/src/prefiltro-lexico.test.ts:20-25` (`length` congelado como literal 30 + `isFrozen`)
y `:205-206` (`>= 30`). Añadir otro sería ruido; el ejecutor **verifica que siguen ahí** y lo declara
en el SUMMARY, sin escribir uno nuevo.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cobertura.ts — medición con truncador inyectable y el gradiente como test</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-F2 (línea 521): *"Validación de cobertura **ANTES de etiquetar**:
      si <95 % de los `prefiltro.terminos` está dentro de `entrada_llm`, el límite sube antes de
      etiquetar un solo caso — después obliga a re-etiquetar todo."*
    - `133-b-PREMORTEM.md` §P-02 completa, incluida la tabla del control de mutación y la
      advertencia: *"a 200 chars la cobertura ya cae a 87,84 %, **bajo el umbral**. El margen no es
      cómodo por casualidad — lo sostiene el límite actual. Nadie puede 'optimizar' el truncado en
      134/135 sin invalidar el golden."*
    - `packages/news/src/eval/entrada-llm.ts:29-37` — **el orden importa** (W4):
      `construirEntradaLlm` hace `despojarHtml(descripcion)` (`:34`) y **luego**
      `truncarDescripcion(descripcionDespojada)` (`:35`). El título se despoja (`:33`) y **nunca se
      trunca**. La descripción **no se foldea** (C2.2 exige fragmento literal).
      `:76-89` — `coberturaTerminos` foldea antes de comparar (los términos vienen ya foldeados) y
      **LANZA sobre lista vacía** (`:77-79`, T-133-14).
      `:39-43` — el JSDoc de `contieneTerminoConFrontera` con la prohibición de `String.includes`
      (`:41-42`); `:44-55` — `escaparRegExp` y `FRONTERA`, privados; `:91-92` — el export `_interno`.
    - `packages/news/src/prefiltro-lexico.ts:140-146` — `truncarDescripcion` real (slice a
      `LIMITE_DESCRIPCION + MARGEN_TRUNCADO`, limpieza de cola solo si el slice truncó, y el guard
      WR-03 que devuelve el corte duro si la limpieza vaciaría el resultado).
      `:154-156` — `limiteTruncadoParaTests()` expone el índice de corte **derivado en runtime**;
      las constantes siguen privadas. **Jamás hardcodear 600/615/23/623.**
    - `packages/news/src/prefiltro-lexico.ts:182-185` — `terminosQueMatchean(titulo, descripcion)`
      devuelve los términos que dispararon el match: es la fuente de `prefiltro.terminos`.
    - `packages/news/src/prefiltro-lexico.ts:6-9` — **prohibición LOCKED**: si una tasa no cuadra se
      corrige el matching o se documenta tal cual; **jamás se podan términos**. `:167-168` — la
      prohibición de subcadena (`String.includes`) en el matching.
    - `packages/news/src/prefiltro-lexico.test.ts:20-25,205-206` — el anti-poda **ya existe**;
      no duplicarlo.
  </read_first>
  <files>packages/news/src/eval/cobertura.ts, packages/news/src/eval/cobertura.test.ts</files>
  <behavior>
    - `medirCobertura(casos, opts?)` devuelve
      `{ total, cubiertos, cobertura, sinTerminos, noCubren }` donde `noCubren` lista los `caso_id`
      que fallan (**nunca se silencian**).
    - `opts.truncador?: (s: string) => string` — **por defecto es `truncarDescripcion`, la función
      REAL del pre-filtro**. El parámetro existe únicamente para el control negativo del test; en
      producción nadie lo pasa.
    - **El truncador se aplica sobre la descripción YA DESPOJADA de HTML** (W4), reproduciendo el
      orden exacto de `construirEntradaLlm` (`entrada-llm.ts:34-35`: `despojarHtml` → truncado).
      Truncar **antes** de despojar cortaría dentro de tags y **movería el gradiente congelado**
      (65/40/30 dejarían de ser los números medidos), convirtiendo los literales en ruido.
      El **título nunca se trunca**, ni con el truncador real ni con el inyectado.
    - `casos` vacío ⇒ **LANZA**. Una cobertura de 1,0 sobre cero casos es el cero vacuo exacto que
      T-133-14 y D-133b-1 prohíben.
    - Un caso con `prefiltro.terminos` vacío es una **patología** y se cuenta en `sinTerminos`
      (medido: 0). No se cuenta como cubierto por vacuidad.
    - Cobertura sobre texto foldeado y con **frontera de palabra**. **`String.includes` PROHIBIDO.**
    - Reducir el truncado hace **caer** la cobertura de forma **monótona no creciente**.
  </behavior>
  <action>
    Crear `packages/news/src/eval/cobertura.ts`. Importar por **ruta relativa**:
    `construirEntradaLlm`, `coberturaTerminos` y `_interno` de `./entrada-llm.js`;
    `terminosQueMatchean`, `truncarDescripcion` y `despojarHtml` de `../prefiltro-lexico.js`.
    **Prohibido tocar `./index.ts`. Prohibido replicar `LIMITE_DESCRIPCION` o escribir 600/615/623.**

    El truncador inyectable se aplica **dentro de la construcción de `entrada_llm` para la
    medición**, no mutando `construirEntradaLlm`. Orden obligatorio, idéntico al de producción
    (W4): `titulo = despojarHtml(titulo)` (sin truncar) y
    `descripcion = opts.truncador(despojarHtml(descripcionCruda))`. Con el truncador por defecto,
    el resultado debe ser **byte-idéntico** al de `construirEntradaLlm` — y el `it` (a) lo comprueba
    comparando ambos sobre al menos un caso real del censo, no solo el conteo agregado.

    Documentar en JSDoc, con estas palabras: *"El parámetro `truncador` existe para el CONTROL
    NEGATIVO de D-133-F2.2 — que el 100 % pueda caer. Su default es la función real del pre-filtro y
    se aplica en el MISMO punto del pipeline (después de `despojarHtml`, antes de comparar): truncar
    antes de despojar movería el gradiente congelado. Bajo ninguna circunstancia se usa para ablandar
    el pre-filtro: el módulo `prefiltro-lexico.ts` no se toca (`prefiltro-lexico.ts:6-9`)."*

    `prefiltro.terminos` de cada caso se deriva con `terminosQueMatchean(titulo, descripcion)` sobre
    el **texto crudo del pool** — es lo que el cargador evaluó. La cobertura compara esos términos
    contra la `entrada_llm` construida (posiblemente con el truncador inyectado). Reusar
    `coberturaTerminos` para la comparación caso a caso, o su helper `_interno`; **no reimplementar
    la frontera de palabra**.

    Los cuatro puntos del gradiente se escriben como truncadores triviales en el **test**
    (`s => s.slice(0, 200)`, `80`, `0`, y `() => ""` para la entrada vacía). En el caso de la
    entrada vacía el título también se vacía —es "entrada vacía", no "solo titular"—, y así debe
    quedar comentado junto al `it` (e).

    Los tests corren sobre el **censo P de `pool-133b.json`** (`estado='pasa'`, 74 casos), leído
    resolviendo la ruta desde `import.meta.dirname` / `fileURLToPath` — **jamás `process.cwd()`**
    (pnpm corre con cwd = paquete) ni `new URL(import.meta.url)` (se rompe bajo jsdom).
  </action>
  <acceptance_criteria>
    - **Línea base del plan**, antes de tocar nada: `SHA_BASE=$(git rev-parse HEAD); echo "$SHA_BASE"`
      y `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b03-antes.log`;
      `N_ANTES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b03-antes.log | grep -oE '[0-9]+')`.
      Ambos **citados literalmente**.
    - `test $((N_T1 - N_ANTES)) -eq 7`. Ambos citados. Otro valor ⇒ **PARAR y escalar**.
    - `Test Files` no baja respecto de `/tmp/133b03-antes.log`.
    - **Los cinco enteros del gradiente son literales en los asserts: 74, 65, 40, 30, 0 sobre
      `total = 74`.** No se asserta un porcentaje redondeado ni una desigualdad laxa. Si algún
      entero difiere ⇒ **PARAR y escalar**: el pool cambió, o el truncador se está aplicando en el
      punto equivocado del pipeline (W4). **Prohibido ajustar el literal al resultado.**
    - **Paridad con producción:** el `it` (a) comprueba, sobre al menos un caso real del censo, que
      `medirCobertura` con el truncador por defecto produce la **misma** `entrada_llm` que
      `construirEntradaLlm`. Sin esa igualdad, el 74/74 mediría otro pipeline.
    - **Anti-cero-vacuo del propio censo:** el test asserta `total === 74` **y** que el censo leído
      tiene los 5 outlets (piso de conteo realista **más** sanity de composición). Un censo de 0 o
      de 1 outlet haría que "74/74" no significara nada.
    - `n=$(grep -c '\.includes(' packages/news/src/eval/cobertura.ts || true); test "$n" -eq 0`.
    - **Ninguna constante del pre-filtro replicada (B3 — `grep -c` sobre 2 archivos NO da entero):**
      ```bash
      n=$(cat packages/news/src/eval/cobertura.ts packages/news/src/eval/cobertura.test.ts | grep -cE 'LIMITE_DESCRIPCION|\b600\b|\b615\b|\b623\b' || true)
      echo "magicos=$n"; test "$n" -eq 0
      ```
      **Control anti-cero-vacuo del propio grep:** `echo 'x = 615' | grep -cE 'LIMITE_DESCRIPCION|\b600\b|\b615\b|\b623\b'` debe dar **1**. Citar ambos conteos.
    - `n=$(grep -c 'process.cwd()' packages/news/src/eval/cobertura.test.ts || true); test "$n" -eq 0`.
    - **Mutación A — el truncador inyectado se ignora:** hacer que `medirCobertura` use siempre
      `truncarDescripcion` sin mirar `opts.truncador` (nunca bajo `set -e`):
      ```bash
      if NO_COLOR=1 pnpm --filter @obs/news exec vitest run cobertura > /tmp/mutb03a.log 2>&1; then rc=0; else rc=$?; fi
      test $rc -ne 0 || { echo "MUTACION NO MORDIO rc=$rc"; exit 1; }
      for it in '(b)' '(c)' '(d)' '(e)'; do grep -qF "$it" /tmp/mutb03a.log || { echo "FALTA $it"; exit 1; }; done
      ```
      Revertir. **Los CUATRO `it` del gradiente deben aparecer**: si solo cae uno, el truncador está
      llegando a un punto parcial del pipeline.
      *Esta es la mutación que demuestra que el 100 % **puede** bajar. Sin ella, el 100 % es vacuo.*
      **Escalada escrita:** si no pone rojo, el truncador no está llegando al camino que construye
      `entrada_llm`. El ejecutor **NO** declara cumplido ni relaja el criterio: imprime la longitud
      de la descripción efectivamente medida en un caso concreto, corrige el punto de inyección,
      re-corre. Dos intentos verde ⇒ **PARAR y escalar**.
    - **Mutación B — cero vacuo:** quitar el `throw` de lista vacía ⇒
      `test $rc -ne 0 && grep -qF '(f)' /tmp/mutb03b.log`. Revertir.
    - **Mutación C — gradiente invertido:** hacer que el truncado a 80 devuelva la descripción
      completa ⇒ `test $rc -ne 0 && grep -qF '(g)' /tmp/mutb03c.log`. Revertir.
    - **Verificación de que el anti-poda sigue vivo (no se duplica, se comprueba):**
      `grep -n 'VOCABULARIO_LEGISLATIVO' packages/news/src/prefiltro-lexico.test.ts` muestra los `it`
      de `:20-25` y `:205-206`. Citar las dos líneas en el SUMMARY.
    - `git diff --name-only $SHA_BASE` (SHA literal) no lista `prefiltro-lexico.ts`,
      `prefiltro-lexico.test.ts`, `entrada-llm.ts`, `caso-golden.ts`, `eval/index.ts`, `pool-r2.ts`,
      `muestreo.ts`, `pool-133b.json` ni `muestra-133b.json`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b03a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b03a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 268</automated>
  </verify>
  <done>La cobertura del censo P mide 74/74 con el truncador real aplicado en el MISMO punto del pipeline que producción, y los cuatro puntos del gradiente (65 / 40 / 30 / 0) están congelados como literales con la mutación que los pone rojos a los cuatro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: el gate fail-closed del 95 % + CLI de confirmación reproducible</name>
  <read_first>
    - `133-b-ADJUDICACION.md` §D-133b-3, paso 1: *"Chequeo de cobertura de `prefiltro.terminos`
      (D-133-F2.2) sobre el censo P. **Antes de etiquetar un solo caso.** Si < 95 %, el límite sube
      **aquí**, no después."* Y §D-133b-7: el hash del `golden-set.json` se emite **una sola vez, al
      final** — *"por eso el chequeo de cobertura va en el plan 03 y no más tarde"*.
    - `<orden_de_olas>` de este plan: la inversión 02→03 y por qué es inocua **solo** con 100 %.
    - `133-b-PREMORTEM.md` §P-02: el resultado ya está medido en **100,00 %** ⇒ *"El plan 133-b-03
      pasa a ser **confirmación, no remediación**"*. El límite **no sube**.
    - `packages/news/src/eval/canonicalizar-cli.ts:35-42` — el idiom de entry-point del directorio.
    - `packages/news/src/eval/muestra-133b.json` (133-b-02) — la muestra congelada; el CLI confirma
      que el censo P del pool y el estrato P de la muestra son **el mismo conjunto de 74** (P es
      censo), y lo asserta explícitamente en vez de suponerlo.
  </read_first>
  <files>packages/news/src/eval/cobertura.ts, packages/news/src/eval/cobertura.test.ts, packages/news/src/eval/cobertura-cli.ts</files>
  <behavior>
    - `UMBRAL_COBERTURA = 0.95`, constante exportada y documentada como **pre-registrada**
      (D-133-F2.2). Bajarla después de ver una cifra **no es salida válida** — la misma prohibición
      que D-133-D2 aplica a los umbrales.
    - `verificarCobertura(casos, opts?)`:
      - cobertura **≥ 0,95** ⇒ devuelve el resultado de `medirCobertura`.
      - cobertura **< 0,95** ⇒ **LANZA** con la cifra y el listado de `noCubren`. Jamás
        `console.warn` + continuar: bajo el umbral, el límite de truncado **debe subir antes de
        etiquetar**, y esa decisión no puede tomarse en silencio.
      - En **0,95 exacto** no lanza (control positivo apareado del `it` (a): sin él, un gate
        siempre-rojo pasaría igual de "verde").
    - El CLI imprime el gradiente completo y **falla** si el punto actual no cumple el gate.
  </behavior>
  <action>
    Añadir `UMBRAL_COBERTURA` y `verificarCobertura` a `cobertura.ts`.

    Crear `packages/news/src/eval/cobertura-cli.ts` siguiendo el idiom de
    `canonicalizar-cli.ts:35-42`. Lee `pool-133b.json` (cero red, cero DB), filtra `estado='pasa'`,
    y corre `verificarCobertura` con el truncador **real**, más los cuatro puntos del gradiente con
    truncadores inyectados **solo para el reporte**.

    Antes de medir, el CLI **asserta la identidad del conjunto**: los `caso_id` con `estado='pasa'`
    del pool y los del estrato `P` de `muestra-133b.json` son **el mismo conjunto de 74**. Si
    difieren, el censo dejó de ser censo ⇒ **lanzar y escalar**.

    Imprimir una línea parseable:
    ```
    cobertura: total=<n> cubiertos=<n> sinTerminos=<n> pct=<0.0000> t200=<n> t80=<n> t0=<n> tvacio=<n> umbral=0.95 gate=<PASA|FALLA>
    ```
    **Los campos `t200`/`t80`/`t0` contienen dígitos en el NOMBRE** ⇒ leerlos **solo** con el
    extractor `sed` de `<medicion>`. Con el idiom de doble `grep` salían rc=2 siempre (B2).

    JSDoc de cabecera con la frase, literal: *"Este chequeo es CONFIRMACIÓN, no remediación: la
    cobertura ya se midió en 100,00 % (133-b-PREMORTEM §P-02). Corre ANTES de etiquetar porque
    subir el límite de truncado después mueve el hash del `golden-set.json` y obliga a re-etiquetar
    todo el golden."*

    **No se escribe ningún artefacto JSON nuevo.** La confirmación vive en el SUMMARY y en los
    tests; añadir otro archivo congelado solo daría una segunda fuente de verdad que se puede
    desincronizar.
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 3` y `test $((N_T2 - N_ANTES)) -eq 10` (delta total del plan).
      Todos los números citados.
    - **Corrida real, con los conteos citados literalmente y el extractor `sed` (B2):**
      ```bash
      pnpm --filter @obs/news exec tsx src/eval/cobertura-cli.ts 2>&1 | tee /tmp/133b03-cob.log
      L=$(grep -oE 'cobertura: .*' /tmp/133b03-cob.log); echo "$L"
      get () { echo "$L" | sed -nE "s/.*(^| )$1=([0-9]+).*/\2/p"; }
      test "$(get total)"       -eq 74
      test "$(get cubiertos)"   -eq 74
      test "$(get sinTerminos)" -eq 0
      test "$(get t200)"        -eq 65
      test "$(get t80)"         -eq 40
      test "$(get t0)"          -eq 30
      test "$(get tvacio)"      -eq 0
      echo "$L" | grep -q 'gate=PASA'
      ```
      **Los cuatro puntos del gradiente son parte del gate del CLI, no decoración:** si `t200`
      saliera 74, la métrica no mordería y el 100 % sería vacuo.
      **Control del propio extractor** (que no vuelva a colarse el bug): `test "$(get t200)" -eq 65`
      debe dar rc=0; si diera rc=2, alguien reintrodujo el doble `grep`.
    - **`t200 < 0,95 × 74 = 70,3` ⇒ `t200 ≤ 70`.** El CLI lo asserta y lo declara: a 200 chars la
      cobertura **cruza el umbral hacia abajo**, de modo que "optimizar" el truncado en 134/135
      invalidaría el golden. Escribirlo con esas palabras en el SUMMARY.
    - **Identidad del censo:** el CLI confirma que los 74 `caso_id` `estado='pasa'` del pool y el
      estrato `P` de `muestra-133b.json` son el mismo conjunto (diferencia simétrica vacía). Citarlo.
    - **Mutación A — gate degradado:** sustituir el `throw` de `verificarCobertura` por
      `console.warn` (nunca bajo `set -e`):
      ```bash
      if NO_COLOR=1 pnpm --filter @obs/news exec vitest run cobertura > /tmp/mutb03d.log 2>&1; then rc=0; else rc=$?; fi
      test $rc -ne 0 && grep -qF '(a)' /tmp/mutb03d.log
      ```
      Revertir.
    - **Mutación B — frontera del umbral:** cambiar `>= UMBRAL_COBERTURA` por `> UMBRAL_COBERTURA` ⇒
      `test $rc -ne 0 && grep -qF '(b)' /tmp/mutb03e.log`. Revertir.
      *Sin el `it` (b), un gate que lanzara SIEMPRE pasaría el `it` (a) y nadie lo notaría.*
    - **Mutación C — el gate ignora el truncador:** hacer que `verificarCobertura` descarte
      `opts.truncador` ⇒ `test $rc -ne 0 && grep -qF '(c)' /tmp/mutb03f.log`. Revertir.
      **Escalada escrita:** si no pone rojo, el `it` (c) no está corriendo sobre el censo P real
      (probablemente cargó un fixture sintético). El ejecutor **NO** declara cumplido: imprime el
      `total` que vio el test, lo apunta al censo P de `pool-133b.json`, re-corre. Dos intentos
      verde ⇒ **PARAR y escalar**.
    - `n=$(grep -cE 'UMBRAL_COBERTURA\s*=\s*0\.9[0-4]' packages/news/src/eval/cobertura.ts || true); test "$n" -eq 0`
      y `grep -c 'UMBRAL_COBERTURA = 0.95' packages/news/src/eval/cobertura.ts` = 1
      (el umbral pre-registrado no se relajó).
    - No existe todavía ningún golden:
      `if ls packages/news/src/eval/golden-set.json > /dev/null 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`.
    - `git status --short -- . ':(exclude).planning'` limpio tras el commit.
    - `git diff --name-only $SHA_BASE` (SHA literal) lista **exactamente** los 3 archivos de
      `files_modified` y nada más. En particular **no** lista `prefiltro-lexico.ts`: el límite de
      truncado **no sube en este plan** (P-02 lo dejó en confirmación).
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b03b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b03b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 268</automated>
  </verify>
  <done>El gate del 95 % es fail-closed y tiene su control positivo apareado en la frontera; el CLI confirma 74/74 con el gradiente 65/40/30/0 como parte del gate —leído con el extractor `sed`, no con el doble `grep`—, y `prefiltro-lexico.ts` queda intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-filtro (600+margen) → `entrada_llm` del clasificador | Si un término citado en `prefiltro.terminos` queda fuera del input, el golden penaliza al modelo por texto amputado |
| Umbral pre-registrado (95 %) → resultado observado | Relajar el umbral después de ver la cifra es el falso verde canónico de la fase |
| Cobertura confirmada → hash del `golden-set.json` (plan 07) | Subir el límite de truncado después de congelar obliga a re-etiquetar todo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-45 | Repudiation | Cobertura del 100 % que no puede bajar (falso verde estructural) | mitigate | Truncador inyectable + los 4 puntos del gradiente como literales enteros (65/40/30/0) + mutación que exige que caigan **los cuatro** `it` |
| T-133-46 | Repudiation | Cobertura del 100 % sobre cero casos o sobre un censo degenerado | mitigate | `medirCobertura([])` lanza; asserts `total === 74` y presencia de los 5 outlets |
| T-133-47 | Tampering | Gate degradado a `console.warn` ⇒ se etiqueta bajo el umbral y el golden queda inservible | mitigate | `verificarCobertura` lanza; mutación del `throw` + control positivo apareado en 0,95 exacto |
| T-133-48 | Tampering | Relajar `UMBRAL_COBERTURA` para que el resultado pase | mitigate | Constante pre-registrada con grep que rechaza cualquier valor 0,90–0,94; prohibición heredada de D-133-D2 |
| T-133-49 | Tampering | Ablandar el pre-filtro o podar el vocabulario para mover la cobertura | mitigate | LOCKED (`prefiltro-lexico.ts:6-9`); el control vive en la MEDICIÓN; `git diff` no lista `prefiltro-lexico.ts`; anti-poda preexistente verificado |
| T-133-50 | Tampering | "Optimizar" el truncado en 134/135 sin saber que invalida el golden | mitigate | Gate `t200 ≤ 70` (cruza el umbral) declarado con esas palabras en el SUMMARY |
| T-133-51 | Repudiation | Cobertura calculada con `String.includes` ⇒ falsos positivos de cobertura | mitigate | Reutiliza la frontera de palabra de `entrada-llm.ts`; grep `.includes(` = 0 |
| T-133-63 | Repudiation | Truncar **antes** de despojar HTML ⇒ el gradiente congelado deja de corresponder y los literales 65/40/30 se vuelven ruido | mitigate | Orden fijado en `<behavior>` y JSDoc (`despojarHtml` → truncado, `entrada-llm.ts:34-35`); `it` (a) exige paridad byte a byte con `construirEntradaLlm` |
| T-133-64 | Repudiation | Gate del gradiente en falso rojo permanente por el extractor de campos con doble `grep` | mitigate | Extractor `sed` obligatorio + control explícito de que `get t200` devuelve un entero (rc=0) |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones: solo se importan módulos ya existentes del propio paquete. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `SHA_BASE`, `N_ANTES`, `N_T1`, `N_T2` citados; deltas exactos 7 / 3 y total 10.
2. La línea `cobertura: ...` íntegra, leída con el extractor `sed`, con los 7 gates numéricos,
   `gate=PASA` y `t200 ≤ 70`.
3. Los cuatro puntos del gradiente como **enteros literales** (65 / 40 / 30 / 0), no porcentajes, y
   la paridad byte a byte del `it` (a) con `construirEntradaLlm`.
4. La identidad del censo (pool `pasa` ≡ estrato `P` de la muestra) confirmada.
5. Las **seis** mutaciones —T1: truncador ignorado (debe tumbar los cuatro `it` del gradiente), cero
   vacuo, gradiente invertido; T2: gate degradado, frontera del umbral, gate sin truncador— con
   rc≠0 bajo el idiom `if/rc` **y su `grep -qF '(x)'`**, jamás bajo `set -e`. Dos llevan su escalada
   escrita.
6. `prefiltro-lexico.ts` **intacto**: el límite de truncado NO sube. Los dos `it` anti-poda
   preexistentes (`:20-25`, `:205-206`) verificados y citados, **no duplicados**.
7. El grep de números mágicos con su **control positivo** (`echo 'x = 615' | grep -cE ...` = 1).
8. **La inversión de olas 02→03 declarada en el SUMMARY** con las palabras de `<orden_de_olas>`.
9. `git diff --name-only <SHA literal>` lista exactamente los 3 archivos declarados.
10. **Ningún guard nuevo en `app/lib/`** ⇒ el script `guards` de `app/package.json:11` no se toca.
</verification>

<success_criteria>
- La cobertura del censo P está **confirmada en 74/74 = 100,00 %**, de forma reproducible y sin red.
- Ese 100 % **muerde**: el gradiente 65 / 40 / 30 / 0 está congelado como literal y su mutación
  apareada pone rojos **los cuatro** `it`.
- El truncador se aplica en el **mismo punto del pipeline** que producción (después de
  `despojarHtml`), con paridad probada contra `construirEntradaLlm`.
- El gate del 95 % es **fail-closed** y tiene su control positivo apareado en la frontera exacta.
- El límite de truncado **no sube** y `prefiltro-lexico.ts` queda intacto: el chequeo corrió antes
  de etiquetar, tal como D-133b-3 ordena.
- Queda escrito que a 200 chars la cobertura cruza el umbral ⇒ nadie puede optimizar el truncado en
  134/135 sin invalidar el golden.
- **La inversión de olas 02→03 queda declarada**, no insinuada.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-03-SUMMARY.md` when done.
Declara: `SHA_BASE`, los cuatro conteos de tests, la línea `cobertura: ...` completa, los cuatro
enteros del gradiente, la confirmación de identidad del censo, **la inversión de olas 02→03 con las
palabras de `<orden_de_olas>`**, y la frase explícita de que **este plan es confirmación y no
remediación: el límite de truncado no subió y nada hay que re-etiquetar**.
</output>
