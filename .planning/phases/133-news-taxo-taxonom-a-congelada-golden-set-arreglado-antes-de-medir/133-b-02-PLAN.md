---
phase: 133-news-taxo
plan: b-02
type: execute
wave: 2
depends_on: ["133-b-01"]
files_modified:
  - packages/news/src/eval/muestreo.ts
  - packages/news/src/eval/muestreo.test.ts
  - packages/news/src/eval/muestreo-cli.ts
  - packages/news/src/eval/muestra-133b.json
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "La semilla `133-b-golden-2026` vive en el artefacto, no en la cabeza de quien corrió el script"
    - "El muestreo es reproducible: dos corridas dan la misma lista; otra semilla da una lista distinta"
    - "El orden total se aplica EN CÓDIGO sobre `url_hash`, jamás en un `ORDER BY` de Postgres"
    - "La regla de matching de la sonda es prefijo con frontera IZQUIERDA, congelada por test: `subsecretari` matchea `subsecretaria`"
    - "`N-sonda` se toma primero y `N-alea` se sortea sobre el resto: los estratos son disjuntos"
    - "El estrato P es un censo: los 74 entran, ninguno se sortea"
    - "Los casos sin descripción no se excluyen ni se concentran; `tasa_sin_descripcion` queda registrada"
  artifacts:
    - path: "packages/news/src/eval/muestreo.ts"
      provides: "PRNG determinista sembrado + orden total + estratificación disjunta"
      exports: ["SEMILLA", "prngDeSemilla", "ordenarPorHash", "contieneTokenPrefijo", "elegiblesSonda", "muestrear", "hashComposicion", "TOKENS_INSTITUCIONALES"]
    - path: "packages/news/src/eval/muestra-133b.json"
      provides: "La muestra congelada (74 P + 30 N-sonda + 50 N-alea) con su hash de composición"
  key_links:
    - from: "packages/news/src/eval/muestreo.ts"
      to: "packages/news/src/prefiltro-lexico.ts"
      via: "fold + RÉPLICA del patrón de frontera (no `contieneTerminoConFrontera`: exige frontera derecha y anularía `subsecretari`)"
      pattern: "fold"
    - from: "packages/news/src/eval/muestreo-cli.ts"
      to: "packages/news/src/eval/pool-133b.json"
      via: "lectura del pool congelado por 133-b-01 (cero red)"
      pattern: "pool-133b.json"
---

<objective>
**D-133b-2 + D-133-B2.1.** D-133-B2 exige "aleatorio con **semilla fija documentada** sobre
`url_hash` ordenado" pero no fija el valor ni el mecanismo. Este plan lo fija y lo prueba.

Purpose: una semilla que vive solo en la cabeza del que corrió el script no es reproducible, y un
muestreador roto que devuelve siempre los 50 primeros **pasa como determinista** si nadie escribe el
control negativo. El orden total sobre `url_hash` se aplica **en código** sobre strings hex —
independiente del orden de inserción y de la colación de Postgres.
Output: `muestreo.ts`, `muestreo-cli.ts` y `muestra-133b.json` congelado con su hash de composición.

**Este plan NO etiqueta ni un solo caso.** No corre anotadores, no toca `caso-golden.ts`, no emite el
hash del `golden-set.json`.

**PROHIBIDO ablandar el pre-filtro para conseguir muestra** — prohibición LOCKED en
`prefiltro-lexico.ts:6-9`. Los estratos salen de la población que hay; si no alcanzan, se escala.

**PROHIBIDO tocar** `packages/news/src/eval/index.ts`, `caso-golden.ts`, `entrada-llm.ts`,
`prefiltro-lexico.ts`, `taxonomia.ts` y `pool-r2.ts` / `pool-133b.json` (propiedad de 133-b-01).
Importar por ruta relativa.

**Orden de operaciones LOCKED (D-133b-3):** el chequeo de cobertura (plan 03) corre **antes** de
etiquetar; el hash del `golden-set.json` se emite **una sola vez, al final** (plan 07). Un cambio de
semilla o de composición **después** del congelado obliga a re-etiquetar todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-ADJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-PREMORTEM.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-01-SUMMARY.md
</context>

<cifras_de_entrada>
Congeladas por `133-b-PREMORTEM.md` §P-03 (**corregido el 2026-08-07 tras el blocker B5, y
corregido de nuevo el 2026-08-07 tras el hallazgo del coordinador sobre el paréntesis
descuidado "445"**) y §D-133b-1. **No se re-miden: se exigen.**

```
población total:      579   (74 pasa · 505 descarta)
censo P:              74    latercera 50 · lacuarta 11 · exante 6 · biobiochile 6 · cooperativa 1
N-sonda elegibles:    60    (descartes con token institucional)   → se toman 30   (holgura 2,0×)
N-alea disponibles:   475   (505 − 30, tras excluir SOLO los ya sorteados como sonda,
                       NUNCA el pool elegible completo de 60)      → se toman 50   (holgura 9,5×)
muestra resultante:   74 + 30 + 50 = 154   (piso duro 100 ✅, holgura 54)
sin descripción:      63/579 = 10,9 %
```

**⚠️ CORREGIDO (hallazgo del coordinador, post-cierre):** la versión anterior de esta cifra
decía `N-alea disponibles: 445 (505 − 60, tras excluir la sonda)`, excluyendo el POOL
ELEGIBLE COMPLETO (60) en vez de solo los 30 casos efectivamente sorteados como sonda. Esto
contradecía la propia adjudicación — D-133b-2 dice literalmente *"`N-alea` se sortea sobre la
población **menos los ya tomados**"*, y "los ya tomados" son los 30 sorteados, no los 60
elegibles. **No era cosmético:** medido sobre el artefacto generado bajo el criterio
incorrecto, **0 de los 50 casos de `N-alea` llevaban token institucional** — el estrato que
existe para ser una muestra ALEATORIA de descartes quedaba sistemáticamente vaciado de
justo los casos con mayor probabilidad de ser falso negativo del pre-filtro, produciendo una
tasa de falso negativo optimista por construcción, sin que ningún test fallara. La cifra
correcta es **505 − 30 = 475**.

## La regla de matching de la sonda, y por qué el número es 60

`133-b-PREMORTEM.md` §P-03 midió los tres candidatos sobre los 505 descartes:

| Regla de matching | Elegibles | Veredicto |
|---|---|---|
| `String.includes` pelado | 68 | ❌ **PROHIBIDA por régimen** (`prefiltro-lexico.ts:167`, `eval/entrada-llm.ts:41-42`) |
| **prefijo con frontera IZQUIERDA** (`(^\|[^a-z0-9])<token>`) | **60** | ✅ **LA REGLA** |
| frontera completa (`contieneTerminoConFrontera`) | 57 | ❌ anula un token entero |

**Lo decisivo es el desglose por token:** con frontera completa, **`subsecretari` matchea CERO**
(con prefijo: **4**). D-133-B2 escribió ese token **truncado a propósito**, como stem, para cubrir
`subsecretario`/`subsecretaria`/`subsecretaría`. Con frontera derecha el token **desaparece en
silencio** del estrato y el sondeo de falso negativo pierde una de sus ocho sondas sin que nada
falle. Por eso la regla es **prefijo con frontera izquierda**, y **`contieneTerminoConFrontera` NO
sirve para este estrato** (sí para `coberturaTerminos`, donde los términos son palabras completas).

**Regla B2.3 ya evaluada, NO se re-evalúa:** el Δ de P del día 2 fue **+33 ≥ 15** ⇒ `N-alea` se
queda en **50**, no sube a 70, y la ventana **no se extiende** (`133-b-ADJUDICACION.md:33-35`).

Si `elegiblesSonda` no da **60**, la regla de matching cambió respecto a lo medido ⇒ **PARAR y
escalar**. Jamás ajustar los tokens ni la regla hasta que el número cuadre.
</cifras_de_entrada>

<medicion>
## Idiom de medición — OBLIGATORIO

`passWithNoTests: true` está ACTIVO (`packages/news/vitest.config.ts:10`) y los argumentos de
`vitest run` son **filtros de nombre, no rutas** ⇒ **se asserta el conteo impreso, jamás el exit
code solo**. `NO_COLOR=1` obligatorio (los ANSI rompen el grep del conteo).

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

`-t` de vitest es **REGEX**: escapar paréntesis y assertar `test "$N" -eq 1` (seleccionó
exactamente 1 test). `(1b)` sin escapar selecciona 0 tests con rc=0.

### Extractor de campos — OBLIGATORIO (B2)

**`grep -oE "$1=[0-9]+" | grep -oE '[0-9]+'` está PROHIBIDO**: si el nombre del campo contiene
dígitos, el segundo `grep` devuelve dos líneas y `test` sale rc=2. Reproducido. Usar:

```bash
get () { echo "$L" | sed -nE "s/.*(^| )$1=([0-9]+).*/\2/p"; }
```

### Nombrar el test que cae — OBLIGATORIO (W2)

Toda mutación lleva su comando, no una frase: `grep -qF '(x)' /tmp/mutXX.log`. Sin él, una mutación
que rompe OTRO test cuenta como verde. `-F` obligatorio (los paréntesis son metacaracteres).

`grep -c` sobre **más de un archivo** prefija el nombre ⇒ `n` deja de ser entero. Con varios
archivos, **siempre** `cat <f1> <f2> | grep -cE ...` (B3).

Piso secundario de sanidad: `-gt 268` (base congelada en 133-a) **más** el delta de 133-b-01.
El criterio fuerte es el **DELTA propio de este plan**, medido sobre su propio worktree.

SHA base: `SHA_BASE=$(git rev-parse HEAD); echo "$SHA_BASE"` al inicio, **citado literal** y usado
como literal en cada `git diff --name-only <sha>`.
Criterio de limpieza: `git status --short -- . ':(exclude).planning'`.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 1 | **3** | (a) `prngDeSemilla("133-b-golden-2026")` da la misma secuencia en dos instancias · (b) **dos semillas distintas dan secuencias distintas** · (c) `ordenarPorHash` es orden total: un array y su reverso ordenan igual | (a)←`Math.random()` · (b)←ignorar la semilla · (c)←comparador que devuelve 0 siempre |
| Task 2 | **9** | (a) `contieneTokenPrefijo` casa por **prefijo de palabra**: `subsecretaria` cuenta para `subsecretari` · (b) **control negativo de substring**: `administrador` NO cuenta para `ministro` · (c) solo entran casos con `estado` de descarte: un `pasa` con token institucional NO es sonda · (d) determinismo: dos corridas, misma lista · (e) **control negativo**: otra semilla ⇒ `N-alea` distinto · (f) `N-sonda` ∩ `N-alea` = ∅ y la sonda se toma **primero** · (g) `muestrear` **LANZA** si un estrato tiene menos elegibles que el n pedido · (h) P es censo: entran los 74, ninguno se sortea · (i) **la regla queda CONGELADA sobre el pool real**: `elegiblesSonda(descartes).length === 60` **y** el token `subsecretari` aporta exactamente **4** | (a)←exigir frontera derecha · (b)←`String.includes` · (f)←sortear `N-alea` sobre los 505 completos · (g)←rellenar con lo que haya · (h)←muestrear P · (i)←sustituir por `contieneTerminoConFrontera` ⇒ 57 y `subsecretari`→0 |
| Task 3 | **3** | (a) `hashComposicion` **cambia** si cambia un `caso_id` · (b) `hashComposicion` **NO cambia** al reordenar la entrada · (c) el muestreo **no filtra por descripción**: sobre una población sintética con 50 % sin bajada, la muestra contiene ≥1 sin descripción | (a)←hash de un literal fijo · (b)←serializar sin canonicalizar · (c)←`.filter(c => c.descripcion !== "")` |
| **Total** | **15** | | |

**ADENDA post-cierre (hallazgo del coordinador, 2026-08-07):** se añade un `it` (j) obligatorio
a Task 2, fuera del delta original de 15: *"`N-alea` contiene AL MENOS UN caso con token
institucional entre los 30 elegibles-no-sorteados"*. Mutación apareada: volver a excluir el
pool elegible completo (60) en vez de solo los 30 sorteados debe hacer caer este `it` a 0. El
delta total pasa de **15 a 16** tras la adenda — citar ambos (`15` histórico y `16` tras el
fix) en el SUMMARY re-escrito.

`it.each` PROHIBIDO en los tests nuevos. Criterio final: `test $((N_T3 - N_ANTES)) -eq 16`
(tras la adenda).

**(b) de la Task 1 y (e) de la Task 2 existen porque sin ellas el determinismo es indemostrable:**
un muestreador que devuelve siempre los 50 primeros pasa (a) y (d) sin problema. El control negativo
es lo único que distingue "determinista" de "constante".

**(i) de la Task 2 es el congelado de la regla que exigió el blocker B5:** sin él, alguien podría
"arreglar" el matching a frontera completa —una corrección que *parece* más estricta y más
correcta— y el token `subsecretari` desaparecería del estrato sin que ningún test cayera.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRNG determinista sembrado por cadena + orden total explícito sobre url_hash</name>
  <read_first>
    - `133-b-ADJUDICACION.md` §D-133b-2 completa: la semilla es la **cadena literal
      `133-b-golden-2026`**, escrita en el artefacto y volcada al `golden-set.json`; la población se
      ordena **ascendentemente por `url_hash`**, orden total, estable, **aplicado en el código sobre
      strings hex, no en el `ORDER BY`**; PRNG determinista sembrado con la cadena, **no
      `Math.random()`**.
    - `packages/news/src/eval/canonicalizar-json.ts:61,70` — `canonicalizar()` y `sha256()`. `sha256`
      es síncrono sobre string: sirve para derivar el estado inicial del PRNG desde la cadena.
    - `packages/news/src/eval/pool-r2.ts` (133-b-01) — `PoolCaso`, y su comparador explícito de
      `url_hash`. Este módulo **no lo re-implementa**: reutiliza o replica la misma regla, y el test
      (c) prueba que el orden es total.
    - Gotcha rector v6.1/v12.0: `localeCompare` depende de locale/ICU y `sort()` sin comparador
      ordena por UTF-16 de la representación string — sobre hex coinciden, pero el comparador se
      escribe **explícito** para que no dependa de esa coincidencia.
  </read_first>
  <files>packages/news/src/eval/muestreo.ts, packages/news/src/eval/muestreo.test.ts</files>
  <behavior>
    - `SEMILLA = "133-b-golden-2026"` exportada como constante literal del módulo, y volcada al
      artefacto. No es un parámetro con default oculto: es un valor auditable.
    - `prngDeSemilla(semilla: string): () => number` devuelve un generador determinista en `[0,1)`.
      Dos instancias creadas con la misma cadena producen **la misma secuencia**; con cadenas
      distintas, secuencias **distintas**.
    - `ordenarPorHash(casos)` devuelve una copia ordenada ascendentemente por `url_hash` con
      comparador explícito. Ordenar un array y su reverso produce **el mismo resultado** (orden
      total, sin empates dependientes del orden de entrada).
    - `Math.random()` **no aparece** en el módulo.
  </behavior>
  <action>
    Crear `packages/news/src/eval/muestreo.ts`.

    PRNG: derivar un estado de 32 bits del `sha256(semilla)` (tomando los primeros 8 hex con
    `parseInt(..., 16)`) y avanzar con un generador entero puro y auto-contenido (mulberry32 o
    xorshift32 — **escrito en el módulo, cero dependencia nueva**). JSDoc: no es criptografía, es
    reproducibilidad; el requisito es determinismo + sensibilidad a la semilla, no uniformidad
    perfecta.

    Barajado: Fisher-Yates **completo** consumiendo el PRNG, sobre la lista **ya ordenada por
    `url_hash`**; luego `slice(0, n)`. Prohibido `sort(() => prng() - 0.5)`: no es un barajado
    uniforme y su resultado depende del algoritmo de `sort` del runtime.

    `ordenarPorHash`: `[...casos].sort((a, b) => a.url_hash < b.url_hash ? -1 : a.url_hash > b.url_hash ? 1 : 0)`.
    **`localeCompare` PROHIBIDO.** JSDoc: el orden se aplica **aquí**, jamás con un `ORDER BY` de
    Postgres — la colación de la DB no es parte del contrato de reproducibilidad del golden.

    JSDoc de cabecera: la semilla es reproducible **porque está escrita**, y el control negativo con
    otra semilla es lo que hace demostrable el determinismo.
  </action>
  <acceptance_criteria>
    - **Línea base del plan**, antes de tocar nada: `SHA_BASE=$(git rev-parse HEAD); echo "$SHA_BASE"`
      y `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b02-antes.log`;
      `N_ANTES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b02-antes.log | grep -oE '[0-9]+')`.
      Ambos **citados literalmente**.
    - `test $((N_T1 - N_ANTES)) -eq 3`. Ambos citados. Otro valor ⇒ **PARAR y escalar**.
    - `Test Files` no baja respecto de `/tmp/133b02-antes.log`.
    - `n=$(grep -c 'Math.random' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`.
    - `n=$(grep -c 'localeCompare' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`.
    - `n=$(grep -c 'sort(() =>' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`
      (el anti-patrón del barajado por comparador aleatorio).
    - `grep -c '133-b-golden-2026' packages/news/src/eval/muestreo.ts` ≥ 1 (la semilla está
      **escrita**, no pasada por argv).
    - **Mutación A — el PRNG deja de estar sembrado:** sustituir el generador por `Math.random`
      (nunca bajo `set -e`):
      ```bash
      if NO_COLOR=1 pnpm --filter @obs/news exec vitest run muestreo > /tmp/mutb02a.log 2>&1; then rc=0; else rc=$?; fi
      test $rc -ne 0 && grep -qF '(a)' /tmp/mutb02a.log
      ```
      Revertir.
    - **Mutación B — la semilla se ignora:** hacer que `prngDeSemilla` descarte su argumento y use
      siempre el mismo estado ⇒ `test $rc -ne 0 && grep -qF '(b)' /tmp/mutb02b.log`. Revertir.
      *Esta es la mutación que separa "determinista" de "constante". Si (b) no existiera, ningún test
      caería.*
    - **Mutación C — el orden deja de ser total:** comparador que devuelve `0` siempre ⇒
      `test $rc -ne 0 && grep -qF '(c)' /tmp/mutb02c.log`. Revertir.
      **Escalada escrita:** si no pone rojo, el fixture de (c) está ya ordenado o tiene un solo
      elemento y el reverso coincide trivialmente. El ejecutor **NO** ajusta el criterio: imprime las
      dos listas, usa un fixture de ≥5 hashes desordenados y distintos, re-corre. Dos intentos verde
      ⇒ **PARAR y escalar**.
    - `git diff --name-only $SHA_BASE` (SHA literal) no lista `eval/index.ts`, `caso-golden.ts`,
      `entrada-llm.ts`, `prefiltro-lexico.ts`, `pool-r2.ts` ni `pool-133b.json`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b02a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b02a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 268</automated>
  </verify>
  <done>PRNG sembrado por la cadena literal, sensible a la semilla (probado con control negativo), y orden total explícito sobre `url_hash` sin `localeCompare` ni `Math.random`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: estratos disjuntos — N-sonda primero (regla de prefijo CONGELADA), N-alea sobre el resto, P censo</name>
  <read_first>
    - `133-READJUDICACION.md:144-149` (tabla B2.1) — los cuatro estratos y **los 8 tokens
      institucionales**: `ministro`, `gobierno`, `La Moneda`, `Contraloría`, `presidente`,
      `subsecretari`, `oficialismo`, `oposición`. Nótese que **`subsecretari` es un stem truncado a
      propósito, no una palabra**. `P` es **censo: todos, sin muestreo**.
    - `133-b-PREMORTEM.md` §P-03 **completa, incluida la tabla por token** (corregida el 2026-08-07
      tras el blocker B5): `String.includes`→68 (**regla prohibida**), **prefijo con frontera
      izquierda→60 (LA REGLA)**, frontera completa→57. Y el desglose: **`subsecretari` con frontera
      completa matchea 0; con prefijo, 4.**
    - `133-b-ADJUDICACION.md` §D-133b-2, cláusula de **exclusión**: `N-sonda` se selecciona
      **primero** (es el estrato con criterio sustantivo); `N-alea` se sortea sobre la población
      **menos** los ya tomados. Un caso en dos estratos contaría dos veces en el denominador.
    - `packages/news/src/prefiltro-lexico.ts:167-168` — la prohibición de subcadena escrita en el
      JSDoc de `esLegislativo`: *"nunca por subcadena — `String.includes` está PROHIBIDO en este
      matching, ver PATRONES"*. `:52-57` — `FRONTERA = "[^a-z0-9]"` y `escaparRegExp`, ambos
      **privados del módulo**. `:65-70` — `PATRONES` con
      `` new RegExp(`(^|${FRONTERA})${escaparRegExp(termino)}(${FRONTERA}|$)`) ``: **ese es el patrón
      a replicar, quitándole la frontera derecha.**
    - `packages/news/src/eval/entrada-llm.ts:39-43` — el JSDoc de `contieneTerminoConFrontera`, que
      repite la prohibición de `String.includes` (`:41-42`). `:44-55` — `escaparRegExp` y `FRONTERA`
      son **privados del módulo**; `:91-92` — el export `_interno` expone **solo**
      `contieneTerminoConFrontera`. ⚠️ **Esa función NO sirve para este estrato**: exige frontera
      derecha y anularía `subsecretari` (P-03). Tampoco puede reutilizarse su `escaparRegExp`: no
      está exportado.
    - `packages/news/src/prefiltro-lexico.ts:93-100` — `fold` (NFD → strip diacríticos → lower →
      colapsar espacios → trim), **sí exportado**. Los tokens llevan tilde (`Contraloría`,
      `oposición`) y mayúscula (`La Moneda`) ⇒ hay que foldearlos, como
      `VOCABULARIO_LEGISLATIVO` ya viene foldeado (`prefiltro-lexico.ts:12-14`).
    - `packages/news/src/eval/pool-r2.ts` (133-b-01) — `PoolCaso` con `estado`, `causa`, `titulo`,
      `descripcion`.
    - `prefiltro-lexico.ts:6-9` — **prohibición LOCKED**: jamás se poda ni se ablanda el vocabulario
      para ajustar una métrica. Vale igual para los tokens de la sonda: si `elegiblesSonda` ≠ 60, se
      escala, **no se retocan los tokens ni la regla**.
  </read_first>
  <files>packages/news/src/eval/muestreo.ts, packages/news/src/eval/muestreo.test.ts</files>
  <behavior>
    - `TOKENS_INSTITUCIONALES` son los **8** de B2.1, guardados **ya foldeados**, congelados con
      `Object.freeze`.
    - `contieneTokenPrefijo(textoFoldeado, tokenFoldeado)` casa por **prefijo de palabra**:
      `` (^|[^a-z0-9])<token> `` — **frontera izquierda obligatoria, sin frontera derecha**. Es lo
      que hace que `subsecretari` case `subsecretaria`/`subsecretario`. La frontera izquierda es la
      que impide que `administrador` cuente para `ministro`.
    - `elegiblesSonda(casos)` filtra **solo** casos con `estado` de descarte que contienen al menos
      un token institucional en `fold(titulo + " " + descripcion)`. Un caso `pasa` nunca es sonda.
      Sobre los 505 descartes reales devuelve **60**, y el token `subsecretari` aporta **4**.
    - `muestrear({ pool, semilla, nSonda: 30, nAlea: 50 })` devuelve
      `{ P, sonda, alea, stats }` con:
      - `P` = **todos** los casos `estado='pasa'`, ordenados por `url_hash`. Censo: no se sortea.
      - `sonda` = 30 sorteados de los elegibles, **primero**.
      - `alea` = 50 sorteados de `descartes − sonda`.
      - `sonda ∩ alea = ∅` y `P ∩ (sonda ∪ alea) = ∅` por construcción.
    - `muestrear` **LANZA** si `elegibles.length < n` en cualquier estrato — jamás rellena en
      silencio ni baja el n.
    - El muestreo **no filtra por descripción**: los casos sin bajada entran como cualquier otro.
  </behavior>
  <action>
    Ampliar `packages/news/src/eval/muestreo.ts`.

    Importar `fold` de `../prefiltro-lexico.js` **por ruta relativa** (prohibido tocar `./index.ts`).

    **`contieneTokenPrefijo` es una RÉPLICA declarada del patrón de `PATRONES`
    (`prefiltro-lexico.ts:65-70`), no una reutilización** (W1): ni `escaparRegExp` ni `FRONTERA`
    están exportados en `prefiltro-lexico.ts` (`:52-57`) ni en `entrada-llm.ts` (`:44-55`), y
    `_interno.contieneTerminoConFrontera` (`entrada-llm.ts:91-92`) **exige frontera derecha**, que
    anularía `subsecretari`. Se escribe en `muestreo.ts` un `escaparRegExp` propio y la misma clase
    `FRONTERA = "[^a-z0-9]"`, con el patrón **sin frontera derecha**. El JSDoc debe decir, con estas
    palabras: *"RÉPLICA deliberada del patrón de `prefiltro-lexico.ts:65-70` con la frontera derecha
    ELIMINADA. No es reutilización porque `escaparRegExp`/`FRONTERA` son privados en ambos módulos, y
    `contieneTerminoConFrontera` exige frontera derecha, que haría desaparecer el token stem
    `subsecretari` (0 matches con frontera completa, 4 con prefijo — P-03). `String.includes` sigue
    PROHIBIDO (`prefiltro-lexico.ts:167-168`): replicar el patrón jamás significa relajarlo."*

    Los tokens se escriben ya foldeados (`la moneda`, `contraloria`, `oposicion`) **y** se pasan por
    `fold()` en el módulo, para que la lista sea correcta aunque alguien la edite con tilde. JSDoc:
    los 8 tokens son los de D-133-B2.1, **congelados**; ampliarlos o podarlos exige nuevo plan
    (misma disciplina que `VOCABULARIO_LEGISLATIVO`, `prefiltro-lexico.ts:12-14`).

    `muestrear` en el orden LOCKED: (1) partir el pool en `pasa` / `descarta` por `estado` de la DB;
    (2) `P` = censo de `pasa`; (3) `elegiblesSonda(descartes)` → ordenar → barajar con el PRNG →
    tomar 30 (`sonda`); (4) `resto = descartes − sonda` → ordenar → barajar con el PRNG → tomar 50.

    **SIN AMBIGÜEDAD (hallazgo del coordinador, post-cierre):** la exclusión del paso 4 es de
    los **30 casos efectivamente sorteados como `sonda`**, NUNCA del pool completo de 60
    elegibles. Los otros 30 elegibles-no-sorteados **permanecen disponibles** para `N-alea`.
    Esto es intencional y necesario: `N-alea` es la muestra ALEATORIA de descartes que sirve
    para estimar la tasa de falso negativo del pre-filtro fuera del criterio de sonda, y
    excluir el pool elegible completo la vaciaría sistemáticamente de los casos con MAYOR
    probabilidad de ser ese falso negativo — precisamente lo que el estrato existe para
    detectar. `resto.length` debe dar **475** (505 − 30), no 445 (505 − 60). Si alguien
    "corrige" esto de vuelta a excluir los 60 elegibles completos, el `it` nuevo de esta task
    (N-alea contiene ≥1 caso con token institucional) debe caer — es la mutación apareada.

    **El PRNG se re-siembra por estrato** con `` `${semilla}:sonda` `` y `` `${semilla}:alea` `` para
    que cambiar `nSonda` no desplace toda la secuencia de `alea`; documentarlo en JSDoc.

    `stats` registra: `poblacion`, `nP`, `nDescartes`, `elegiblesSonda`, `restoTrasSonda`,
    `porToken` (el desglose de cuántos descartes aporta cada uno de los 8 tokens — es lo que hace
    auditable el 60 y lo que congela el `it` (i)), `tasa_sin_descripcion` (global y por estrato) y el
    reparto de `P` por outlet.
    **`tasa_sin_descripcion` es obligatoria** (P-04): los 63 casos sin bajada son población honesta y
    el golden debe poder leerse con y sin ese subconjunto.
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 9`, con `N_T1` medido al cierre de la Task 1. Ambos citados.
    - `n=$(grep -c '\.includes(' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`
      y `n=$(grep -c 'indexOf(' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`.
    - **La regla es prefijo, no frontera completa** — verificable en el fuente. El patrón busca la
      cola `}|$)` que cierra la frontera DERECHA; su ausencia es lo que prueba que la regla es de
      prefijo:
      ```bash
      n=$(grep -cF '}|$)' packages/news/src/eval/muestreo.ts || true)
      echo "frontera_derecha_en_muestreo=$n"; test "$n" -eq 0
      # CONTROL POSITIVO del propio grep (sin él, un patrón que no casa nada pasa siempre):
      c=$(grep -cF '}|$)' packages/news/src/eval/entrada-llm.ts || true)
      echo "frontera_derecha_en_entrada_llm=$c"; test "$c" -ge 1
      ```
      `entrada-llm.ts` **sí** usa frontera completa (`:52-53`) ⇒ el control debe dar ≥1. Citar los
      dos conteos. Y además
      `n=$(grep -c 'contieneTerminoConFrontera' packages/news/src/eval/muestreo.ts || true); test "$n" -eq 0`
      (no se importa la función que anularía `subsecretari`).
    - `grep -c 'subsecretari' packages/news/src/eval/muestreo.ts` ≥ 1 y los 8 tokens presentes:
      `n=$(grep -cE 'ministro|gobierno|la moneda|contraloria|presidente|subsecretari|oficialismo|oposicion' packages/news/src/eval/muestreo.ts || true); test "$n" -ge 8`.
    - **Mutación A — frontera derecha:** añadir `` (${FRONTERA}|$) `` al patrón de
      `contieneTokenPrefijo` ⇒
      ```bash
      if NO_COLOR=1 pnpm --filter @obs/news exec vitest run muestreo > /tmp/mutb02d.log 2>&1; then rc=0; else rc=$?; fi
      test $rc -ne 0 && grep -qF '(a)' /tmp/mutb02d.log && grep -qF '(i)' /tmp/mutb02d.log
      ```
      **Deben caer los DOS**: (a) por `subsecretaria`, e (i) porque `elegiblesSonda` baja de 60 a 57
      y `subsecretari` cae a 0. Revertir.
    - **Mutación B — `String.includes`:** sustituir el matching por `String.includes` ⇒
      `test $rc -ne 0 && grep -qF '(b)' /tmp/mutb02e.log`. Revertir.
      *El `it` (b) existe precisamente para que esta mutación sea alcanzable: sin él, ningún test cae
      (con `includes` los elegibles suben a 68, así que (i) también caería — mejor aún: dos redes).*
    - **Mutación C — estratos solapados:** sortear `N-alea` sobre los 505 descartes completos (sin
      excluir la sonda) ⇒ `test $rc -ne 0 && grep -qF '(f)' /tmp/mutb02f.log`. Revertir.
      **Escalada escrita:** si no pone rojo, la intersección salió vacía por azar de la semilla. El
      ejecutor **NO** lo declara cumplido: usa en el test una población sintética pequeña donde
      **todos** los descartes sean elegibles de sonda (intersección forzada), re-corre. Dos intentos
      verde ⇒ **PARAR y escalar**.
    - **Mutación D — relleno silencioso:** hacer que `muestrear` tome `min(n, elegibles.length)` en
      vez de lanzar ⇒ `test $rc -ne 0 && grep -qF '(g)' /tmp/mutb02g.log`. Revertir.
    - **Mutación E — P sorteado:** hacer que `P` se sortee a 50 en vez de ser censo ⇒
      `test $rc -ne 0 && grep -qF '(h)' /tmp/mutb02h.log`. Revertir.
    - **El `it` (i) corre sobre el pool REAL**, no sobre un fixture sintético: lee
      `pool-133b.json`, filtra `estado` de descarte (**piso: 505**, anti-cero-vacuo) y asserta
      `elegiblesSonda(...).length === 60` **y** `stats.porToken.subsecretari === 4`.
    - `git diff --name-only $SHA_BASE` (SHA literal) no lista `entrada-llm.ts`,
      `prefiltro-lexico.ts`, `pool-r2.ts`, `pool-133b.json` ni `eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b02b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b02b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 268</automated>
  </verify>
  <done>Estratos disjuntos por construcción, sonda primero con la regla de prefijo CONGELADA por test sobre el pool real (60 elegibles, `subsecretari`=4), P como censo, y fallo duro si un estrato no alcanza; las CINCO mutaciones rojas nombrando su `it`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: congelar la muestra + hash de composición (NO es el hash del golden-set)</name>
  <read_first>
    - `133-b-ADJUDICACION.md` §D-133b-7: *"El hash de `golden-set.json` se emite al final y **una
      sola vez**"* (plan 07). **Este plan NO lo emite.** El hash de este plan es el de la
      **composición de la muestra**, y así debe nombrarse en el artefacto y en el SUMMARY —
      confundirlos es exactamente lo que haría que un cambio posterior pasara desapercibido.
    - `133-b-ADJUDICACION.md` §D-133b-3, paso 2: *"Congelar la muestra (P censo + N-sonda + N-alea) y
      su hash de composición"* — **antes** de etiquetar.
    - `packages/news/src/eval/canonicalizar-json.ts:61,70` — `canonicalizar()` produce serialización
      estable (claves ordenadas); `sha256(raw)` sobre esa cadena. Es el mismo mecanismo que ya
      congela `taxonomia.json` y `thresholds.json` (`canonicalizar-cli.ts:23-32`).
    - `packages/news/src/eval/canonicalizar-cli.ts:35-42` — el idiom de entry-point del directorio y
      `writeFileSync(path, str, "utf8")` (Node no traduce newlines ni escribe BOM al construir el
      string en memoria).
    - `133-b-PREMORTEM.md` §P-04 punto 4: el muestreo de `N-alea` **no debe excluir ni concentrar**
      los casos sin descripción.
  </read_first>
  <files>packages/news/src/eval/muestreo.ts, packages/news/src/eval/muestreo.test.ts, packages/news/src/eval/muestreo-cli.ts, packages/news/src/eval/muestra-133b.json</files>
  <behavior>
    - `hashComposicion(muestra)` = `sha256(canonicalizar(<lista ordenada de caso_id por estrato>))`.
      **Cambia** si cambia cualquier `caso_id` o su estrato; **no cambia** si solo cambia el orden de
      la entrada (la canonicalización lo absorbe).
    - `muestreo-cli.ts` lee `pool-133b.json` (cero red, cero DB), corre `muestrear` con `SEMILLA`, y
      escribe `muestra-133b.json` con: `semilla`, `ventana`, `regla_matching_sonda`, `n` por estrato,
      `caso_id` + `url_hash` + `outlet` + `estrato` de cada caso, `porToken`,
      `tasa_sin_descripcion` (global y por estrato), el reparto de P por outlet, y
      `hash_composicion`.
    - Re-correr el CLI deja `muestra-133b.json` **byte-idéntico**.
    - **El artefacto NO contiene etiquetas** — ni de máquina ni de humano. Todavía no existen.
  </behavior>
  <action>
    Crear `packages/news/src/eval/muestreo-cli.ts` siguiendo el idiom de
    `canonicalizar-cli.ts:35-42`. Leer `pool-133b.json` con `readFileSync` resolviendo la ruta desde
    `import.meta.url` (**`import.meta.dirname` / `fileURLToPath`, jamás `process.cwd()`**: pnpm corre
    el script con cwd = paquete, y `new URL(import.meta.url)` se rompe bajo jsdom — gotchas
    conocidos). Escribir `muestra-133b.json` con `canonicalizar()` + `writeFileSync(..., "utf8")`.

    El campo `regla_matching_sonda` se emite con el valor literal
    `"prefijo-frontera-izquierda"` y una glosa de una línea: la regla queda **escrita en el
    artefacto**, no solo en el código, porque es lo que distingue 60 de 57 y de 68 (P-03).

    El campo del hash se llama **`hash_composicion`** y el JSDoc del módulo declara, con estas
    palabras: *"Este NO es el hash del `golden-set.json`. Ese se emite una sola vez, al final, en el
    plan 133-b-07. Este hash congela **qué casos** componen la muestra, antes de que exista una sola
    etiqueta."*

    Imprimir una línea parseable (**nombres de campo sin dígitos**, para el extractor `sed`):
    ```
    muestra: P=<n> sonda=<n> alea=<n> total=<n> elegiblesSonda=<n> restoTrasSonda=<n> sinDescripcion=<n> tokenSubsecretari=<n> hash=<sha256>
    ```

    Commitear `muestra-133b.json`: es lo que hace auditable, sin red, qué casos entraron —
    incluyendo por el plan 04, que selecciona los 20 de calibración **de esta misma muestra
    congelada**.
  </action>
  <acceptance_criteria>
    - `test $((N_T3 - N_T2)) -eq 3` y `test $((N_T3 - N_ANTES)) -eq 15` (delta total del plan).
      Todos los números citados.
    - **Corrida real, con los conteos citados literalmente:**
      ```bash
      pnpm --filter @obs/news exec tsx src/eval/muestreo-cli.ts 2>&1 | tee /tmp/133b02-muestra.log
      L=$(grep -oE 'muestra: .*' /tmp/133b02-muestra.log); echo "$L"
      get () { echo "$L" | sed -nE "s/.*(^| )$1=([0-9]+).*/\2/p"; }
      test "$(get P)"                 -eq 74
      test "$(get sonda)"             -eq 30
      test "$(get alea)"              -eq 50
      test "$(get total)"             -eq 154
      test "$(get elegiblesSonda)"    -eq 60
      test "$(get restoTrasSonda)"    -eq 475
      test "$(get tokenSubsecretari)" -eq 4
      # Control nuevo, obligatorio (hallazgo del coordinador): N-alea debe llevar al menos un
      # caso con token institucional. Con la exclusión correcta (solo los 30 sorteados como
      # sonda), quedan 30 elegibles-no-sorteados entre los 475 disponibles de N-alea — en una
      # muestra de 50 sobre 475 es esperable >=1. Si sale 0, PARAR y escalar (sería un
      # hallazgo real, no algo que ajustar).
      test "$(get aleaConToken)"      -ge 1
      ```
      `elegiblesSonda=60`, `restoTrasSonda=475` y `tokenSubsecretari=4` son las cifras de
      `133-b-PREMORTEM.md` §P-03 **bajo la regla de prefijo con frontera izquierda**.
      **Cualquier otro valor ⇒ PARAR y escalar**: la regla de matching cambió respecto a lo medido.
      Jamás retocar los tokens ni la regla hasta que el número cuadre (`prefiltro-lexico.ts:6-9`).
      *(60 y 475 no son los 68/437 de la primera versión del premortem: aquella cifra salió de
      `String.includes` pelado, la regla que el régimen prohíbe.)*
    - **Piso duro de composición:** `total ≥ 100` (D-133-B2.2) — verde con holgura de 54, y así se
      declara. Además `test "$(get sinDescripcion)" -gt 0` (anti-cero-vacuo: los sin bajada **están**
      en la muestra, no fueron filtrados).
    - **Sanity de un caso concreto**, no solo conteos: tomar el primer `caso_id` de `sonda` del
      artefacto, localizarlo en `pool-133b.json` y verificar a mano que (i) su `estado` es de
      descarte y (ii) su titular/bajada contiene efectivamente uno de los 8 tokens
      institucionales. **Citar el caso y el token en el SUMMARY.**
    - **Reproducibilidad byte a byte:** correr el CLI dos veces y comparar `sha256sum` de
      `muestra-133b.json` — idénticos. Citar el `hash_composicion`, **etiquetado explícitamente como
      hash de composición y NO como el hash del `golden-set.json`**.
    - **Doble corrida cruzada (el control que separa determinista de constante):** correr el
      muestreo con la semilla `133-b-golden-2026` y con `133-b-golden-2026-X`; la lista de `alea`
      debe ser **distinta** y el `hash_composicion` **distinto**. Citar ambos hashes. Revertir sin
      dejar rastro: el artefacto committeado es el de la semilla oficial.
    - **Mutación A — hash insensible:** hacer que `hashComposicion` hashee un literal fijo ⇒
      ```bash
      if NO_COLOR=1 pnpm --filter @obs/news exec vitest run muestreo > /tmp/mutb02i.log 2>&1; then rc=0; else rc=$?; fi
      test $rc -ne 0 && grep -qF '(a)' /tmp/mutb02i.log
      ```
      Revertir.
    - **Mutación B — sin canonicalizar:** serializar con `JSON.stringify` directo del array sin
      ordenar ⇒ `test $rc -ne 0 && grep -qF '(b)' /tmp/mutb02j.log`. Revertir.
    - **Mutación C — filtro por descripción:** insertar `.filter(c => c.descripcion !== "")` antes
      del sorteo ⇒ `test $rc -ne 0 && grep -qF '(c)' /tmp/mutb02k.log`. Revertir.
      **Escalada escrita:** si no pone rojo, la población sintética del test no tiene casos sin
      descripción o el filtro cayó fuera del camino ejercitado. El ejecutor **NO** declara cumplido:
      imprime cuántos casos sin descripción entraron al sorteo, corrige la población sintética al
      50 %, re-corre. Dos intentos verde ⇒ **PARAR y escalar**.
    - `n=$(grep -c 'process.cwd()' packages/news/src/eval/muestreo-cli.ts || true); test "$n" -eq 0`.
    - La regla queda escrita en el artefacto:
      `grep -c 'prefijo-frontera-izquierda' packages/news/src/eval/muestra-133b.json` ≥ 1.
    - Cero etiquetas en el artefacto:
      `n=$(grep -cE '"etiqueta|tramitacion_legislativa|actividad_parlamentaria|ambiguo' packages/news/src/eval/muestra-133b.json || true); test "$n" -eq 0`.
    - No existe todavía ningún golden:
      `if ls packages/news/src/eval/golden-set.json > /dev/null 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`.
    - `git status --short -- . ':(exclude).planning'` limpio tras el commit.
    - `git diff --name-only $SHA_BASE` (SHA literal) lista **exactamente** los 4 archivos de
      `files_modified` y nada más.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133b02c.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133b02c.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 268</automated>
  </verify>
  <done>Muestra congelada de 154 casos (74 + 30 + 50) con semilla escrita, regla de matching escrita en el artefacto, estratos disjuntos, `tasa_sin_descripcion` registrada y `hash_composicion` byte-estable — declarado explícitamente como NO siendo el hash del `golden-set.json`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Población de la ventana → muestra congelada | Aquí se decide, sin supervisión, qué casos entran al denominador de toda métrica de 135 |
| Semilla y PRNG → reproducibilidad del golden | Un muestreo irreproducible convierte cualquier cifra posterior en no auditable |
| Regla de matching de la sonda → cobertura del sondeo | Una regla no escrita hace que el número no se pueda reproducir; una regla "más estricta" borra un token entero |
| Muestra congelada → hash del golden (plan 07) | Confundir el hash de composición con el del golden ocultaría un cambio de composición |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-37 | Repudiation | Muestreador constante (siempre los primeros n) que pasa como determinista | mitigate | Control negativo con otra semilla en dos niveles (`it` (b) de T1, `it` (e) de T2) + doble corrida cruzada con hashes distintos |
| T-133-38 | Repudiation | Semilla no escrita (vive en la cabeza del operador o en argv) ⇒ muestra irreproducible | mitigate | `SEMILLA` constante literal del módulo, volcada al artefacto; grep ≥1 |
| T-133-39 | Tampering | Orden dependiente de la colación de Postgres o del locale (`ORDER BY`, `localeCompare`) | mitigate | Orden total en código con comparador explícito; grep `localeCompare` = 0; `.order()` solo para paginar |
| T-133-40 | Tampering | Estratos solapados ⇒ un caso cuenta dos veces en el denominador | mitigate | Sonda primero, alea sobre el resto; `it` (f) + mutación con población forzada |
| T-133-41 | Tampering | Ablandar el pre-filtro o retocar los tokens para "conseguir muestra" | mitigate | Prohibición LOCKED (`prefiltro-lexico.ts:6-9`); gates literales `elegiblesSonda=60` / `restoTrasSonda=475` con escalada, jamás ajuste |
| T-133-42 | Repudiation | Relleno silencioso cuando un estrato no alcanza el n | mitigate | `muestrear` lanza; mutación `min(n, elegibles)` con `grep -qF '(g)'` |
| T-133-43 | Information Disclosure | Sesgo por excluir los 63 casos sin descripción (10,9 %) | mitigate | `it` (c) de no-filtrado + mutación con `.filter`; `tasa_sin_descripcion` global y por estrato |
| T-133-44 | Repudiation | Presentar el `hash_composicion` como el hash del `golden-set.json` | mitigate | Nombre distinto en el artefacto, JSDoc explícito y advertencia obligatoria en el SUMMARY |
| T-133-61 | Tampering | "Arreglar" el matching a frontera completa ⇒ `subsecretari` matchea 0 y una sonda desaparece en silencio | mitigate | `it` (i) congela `elegiblesSonda === 60` **y** `porToken.subsecretari === 4` sobre el pool real; mutación de frontera derecha hace caer (a) **e** (i); greps de fuente que rechazan el patrón de frontera completa |
| T-133-62 | Repudiation | Medir la sonda con `String.includes` (regla prohibida) y publicar el número como reproducible | mitigate | `grep -c '.includes('` = 0; mutación B; la regla se escribe en el artefacto (`regla_matching_sonda`) |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones: PRNG y barajado escritos en el módulo; `zod` ya es dependencia directa. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `SHA_BASE`, `N_ANTES`, `N_T1`, `N_T2`, `N_T3` citados; deltas exactos 3 / 9 / 3 y total 15.
2. La línea `muestra: ...` íntegra, con los 7 gates numéricos (74 / 30 / 50 / 154 / 60 / 475 / 4),
   `sinDescripcion > 0` y `aleaConToken >= 1`.
3. El `it` (i) corriendo sobre el pool real con su piso de 505 descartes.
4. La doble corrida cruzada con dos semillas: dos `hash_composicion` **distintos**, ambos citados.
5. La reproducibilidad byte a byte del artefacto con la semilla oficial (dos `sha256sum` iguales).
6. El sanity de un caso concreto de `sonda`: caso citado + token institucional citado.
7. Las **doce** mutaciones —T1: `Math.random`, semilla ignorada, comparador nulo (3); T2: frontera
   derecha, `String.includes`, estratos solapados, relleno silencioso, P sorteado, **exclusión de
   N-alea vuelta a los 60 elegibles completos** (6); T3: hash fijo, sin canonicalizar, filtro por
   descripción (3)— con rc≠0 bajo el idiom `if/rc` **y su `grep -qF '(x)'`**, jamás bajo `set -e`.
   Tres llevan su escalada escrita.
7b. El `it` nuevo (hallazgo del coordinador): `N-alea` contiene **al menos un** caso con token
   institucional entre los 30 elegibles-no-sorteados. Si sale 0 casos, es un hallazgo real que
   exige PARAR y escalar — no se ajusta el criterio.
8. `git diff --name-only <SHA literal>` lista exactamente los 4 archivos declarados.
9. **Ningún guard nuevo en `app/lib/`** ⇒ el script `guards` de `app/package.json:11` no se toca.
</verification>

<success_criteria>
- Semilla `133-b-golden-2026` **escrita** en el módulo y en el artefacto.
- Determinismo **demostrado con su control negativo**: misma semilla ⇒ misma lista; otra semilla ⇒
  lista y hash distintos.
- Orden total sobre `url_hash` aplicado en código, sin `localeCompare` y sin `ORDER BY`.
- **La regla de matching de la sonda es prefijo con frontera izquierda, escrita en el artefacto y
  congelada por test sobre el pool real** (60 elegibles, `subsecretari` = 4).
- Estratos disjuntos: sonda (30 de 60) primero, alea (50 de 475, con >=1 caso con token
  institucional entre los 30 elegibles-no-sorteados) sobre el resto, P censo de 74.
- Muestra de **154 casos** congelada (piso 100 con holgura 54), con `tasa_sin_descripcion`
  registrada y `hash_composicion` byte-estable, explícitamente distinto del hash del golden.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-b-02-SUMMARY.md` when done.
Declara: `SHA_BASE`, los cinco conteos de tests, la línea `muestra: ...` completa, los dos
`hash_composicion` de la doble corrida cruzada, el caso de sonda verificado a mano con su token, la
regla de matching (`prefijo-frontera-izquierda`) con las tres cifras de P-03 (68 prohibida / **60
LA REGLA** / 57 anula `subsecretari`), y la advertencia literal de que **`hash_composicion` NO es el
hash del `golden-set.json`** — ese se emite una sola vez, en el plan 133-b-07.
</output>
