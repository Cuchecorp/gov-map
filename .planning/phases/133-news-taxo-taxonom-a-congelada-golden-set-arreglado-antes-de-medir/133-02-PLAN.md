---
phase: 133-news-taxo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/terminos-insinuacion.ts
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Un archivo del allowlist de superficies que no existe hace FALLAR el guard, ya no lo salta"
    - "Los DOS skips silenciosos (:947 y :977) están cerrados, no uno"
    - "TERMINOS_PROHIBIDOS vive en un módulo sin JSX que el guard importa, legible por disco desde packages/news"
    - "La suite de app/ sube exactamente 4 tests: N_DESPUES - N_ANTES == 4, ni uno más ni uno menos"
  artifacts:
    - path: "app/lib/terminos-insinuacion.ts"
      provides: "TERMINOS_PROHIBIDOS + TERMINOS_LINK_EXT + TERMINOS_COBERTURA, sin JSX ni imports de componentes"
      exports: ["TERMINOS_PROHIBIDOS", "TERMINOS_LINK_EXT", "TERMINOS_COBERTURA"]
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/lib/terminos-insinuacion.ts"
      via: "import (no re-tipeo)"
      pattern: "from \"./terminos-insinuacion\""
---

<objective>
G3 (D-133-A2.4 + D-133-J3): cerrar los **DOS** skips silenciosos del guard anti-insinuación, y
extraer `TERMINOS_PROHIBIDOS` a un módulo importable (vía B de D-133-J2) para que G1 pueda leerlo
por disco desde `packages/news` sin invertir la dirección de dependencia del monorepo.

Purpose: un allowlist con una ruta muerta es un guard ciego que sale verde — el falso verde de
manual. Y sin la extracción, G1 solo tendría la vía C (replicar la lista), prohibida por D-133-J2.6.
Output: `app/lib/terminos-insinuacion.ts` + un `app/lib/anti-insinuacion-guard.test.ts` que falla
duro ante una superficie inexistente.

**Este plan toca el archivo más sensible del repo.** Ambos cambios van en el MISMO commit
(D-133-J2.5). El criterio rector es **diff-cero de comportamiento con delta de tests FIJO**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
</context>

<medicion>
## Idiom de medición — OBLIGATORIO en todo este plan

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada** (el `22`
del reset ANSI de vitest es dígito y rompe el patrón). Verificado. Idiom correcto:

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**Línea base medida de `app/`: 1799 tests / 121 archivos** (`NO_COLOR=1 pnpm --filter ./app test`,
~98 s). Ese número es referencia; la Task 1 lo re-mide. **El criterio es el delta**, no un `-gt`
absoluto: un umbral absoluto sobre una base de ~1800 sería casi vacuo.

`grep -c` devuelve `0` **con rc=1**. Encadenado con `&&` un "cero correcto" se lee como fallo.
Capturar siempre con `n=$(grep -c ... || true)` y comparar el valor.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan — el criterio de diff-cero

Un criterio del tipo `N_ANTES + (número de tests nuevos)` sin fijar ese número **no puede fallar**.
Aquí está fijado **y reconciliado con las mutaciones de cada tarea**: una mutación que exige que
"caiga un test" necesita que ese test exista y esté contado.

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 2 | **1** | `piso: TERMINOS_PROHIBIDOS conserva >= 90 términos tras la extracción` | borrar 5 entradas ⇒ cae |
| Task 3 | **3** | (a) `no-hueco: toda superficie declarada existe en disco` · (b) `control positivo: leerSuperficie con ruta real devuelve contenido` · (c) `control positivo apareado: leerSuperficie con la MISMA ruta + ".noexiste" LANZA` | ruta inventada ⇒ cae (a); mutación por sitio |
| **Total** | **4** | | |

**`it.each` PROHIBIDO en los tests nuevos**: genera un número variable (y con array vacío, cero —
el cero-vacuo que `anti-insinuacion-guard.test.ts:1588-1594` ya documenta). Cuatro `it` planos.

Criterio final: `test $((N_DESPUES - N_ANTES)) -eq 4`.
</delta_de_tests>

<tasks>

<task type="auto">
  <name>Task 1: Capturar la línea base de app/ ANTES de tocar nada</name>
  <read_first>
    - `133-PREMORTEM-PLANES.md` §BLOCKER 1 (el grep sin `NO_COLOR=1` nunca matchea; con él,
      `app/` da `Tests 1799 passed (1799)` / `Test Files 121 passed (121)`)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2.5 (criterio de diff-cero)
    - `app/package.json:11` — el script `guards` y sus 11 archivos por nombre.
      **Verificado con `sed -n '9,11p'`: `:9` es `"lint"`, `:10` es `"test"`, `:11` es `"guards"`.**
  </read_first>
  <files>(ninguno — solo medición)</files>
  <action>
    Antes de editar una sola línea de `app/`, ejecutar y **registrar literalmente** en el SUMMARY:

    1. `git rev-parse HEAD` → guardar el SHA como **base fija literal** (un `git diff --name-only`
       sin base fija siempre pasa con commits atómicos). Anotar el valor, no "HEAD".
    2. `NO_COLOR=1 pnpm --filter ./app test 2>&1 | tee /tmp/133-app-antes.log`
       `N_ANTES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-antes.log | grep -oE '[0-9]+')`
       Registrar también `Test Files X passed`.
    3. `NO_COLOR=1 pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tee /tmp/133-ai-antes.log`
       `G_ANTES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-ai-antes.log | grep -oE '[0-9]+')`

    Si `N_ANTES` sale vacío, **primero comprobar que se usó `NO_COLOR=1`** — es la causa conocida y
    ya reproducida. Solo si con `NO_COLOR=1` sigue vacío, PARAR y escalar.
  </action>
  <acceptance_criteria>
    - `N_ANTES` es un entero > 0 y está citado literal en el SUMMARY (referencia esperada: 1799).
    - `G_ANTES` es un entero > 0 y está citado literal.
    - El SHA base está anotado como valor literal de 7+ chars, no como `HEAD`.
    - `/tmp/133-app-antes.log` contiene la línea `Test Files` con su conteo, también citado.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter ./app test 2>&1 | tee /tmp/133-app-antes.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-antes.log | grep -oE '[0-9]+'); echo "N_ANTES=$N"; test "$N" -gt 0</automated>
  </verify>
  <done>`N_ANTES`, `G_ANTES` y el SHA base registrados literalmente. Sin ellos el plan no puede continuar.</done>
</task>

<task type="auto">
  <name>Task 2: Extraer TERMINOS_PROHIBIDOS a app/lib/terminos-insinuacion.ts (vía B, D-133-J2)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2 completo (vía B acotada; `NEGACIONES_LOCKED` se
      QUEDA donde está; vía C PROHIBIDA)
    - **Rutas:línea verificadas una por una con `grep -n` / `sed -n`** (el addendum trae drift):
      `app/lib/anti-insinuacion-guard.test.ts:569` (`const TERMINOS_LINK_EXT`),
      `:605` (`const TERMINOS_COBERTURA`),
      `:623-757` (`const TERMINOS_PROHIBIDOS`, con `...TERMINOS_LINK_EXT` y `...TERMINOS_COBERTURA`
      dentro; el `];` de cierre en `:757`),
      `:776-825` (`const NEGACIONES_LOCKED` — **`:776`, no `:764`**),
      `:59` (`import { IDIOMS_APROBADOS } from "@/lib/idioms-panel"` — **`:59`, no `:33-40`**)
    - `app/lib/anti-insinuacion-guard.test.ts:759-768` (JSDoc de `IDIOMS_APROBADOS` — el argumento
      canónico del repo: *"este archivo IMPORTA el array, no lo re-tipea"*)
    - `app/tsconfig.json` (alias `@/*`; `app/` no depende de ningún `@obs/*` — régimen documentado
      en `app/lib/week-utils.ts:3`, `app/lib/types.ts:4`, `app/lib/name-match-rut-guard.test.ts:79`)
  </read_first>
  <files>app/lib/terminos-insinuacion.ts, app/lib/anti-insinuacion-guard.test.ts</files>
  <action>
    Crear `app/lib/terminos-insinuacion.ts` conteniendo **exactamente** los tres arrays movidos
    desde el guard, con sus strings byte por byte: `TERMINOS_LINK_EXT` (hoy `:569`),
    `TERMINOS_COBERTURA` (hoy `:605`) y `TERMINOS_PROHIBIDOS` (hoy `:623-757`, incluidos sus dos
    spreads). El módulo:
    - NO contiene JSX, NO importa componentes `.tsx` ni `@/components/*`, NO importa ningún `@obs/*`.
    - Lleva un JSDoc que explica por qué existe: *"extraído del guard para que
      `packages/news/src/eval/taxonomia-guard.test.ts` (G1) pueda leer los términos por DISCO —
      lectura de bytes, no import: la dirección `app`→`packages` del grafo de módulos no se
      invierte. D-133-J2."*
    - Es un módulo `.ts` de producción (no `.test.ts`) ⇒ lo typechequea `tsc --noEmit` de `app/`.

    En `app/lib/anti-insinuacion-guard.test.ts`: **borrar** las tres declaraciones y sustituirlas
    por `import { TERMINOS_PROHIBIDOS, TERMINOS_LINK_EXT, TERMINOS_COBERTURA } from "./terminos-insinuacion";`.

    **`NEGACIONES_LOCKED` NO se mueve** (D-133-J2.2): importa constantes de componentes `.tsx`.

    **El piso anti-cero-vacuo va como UN `it` dentro de `anti-insinuacion-guard.test.ts`**, no como
    un `node -e` (que no puede importar un `.ts`) ni como archivo nuevo. Es el `it` nº1 del
    `<delta_de_tests>`:
    `expect(TERMINOS_PROHIBIDOS.length).toBeGreaterThanOrEqual(90)` con el mensaje
    *"la extracción a terminos-insinuacion.ts perdió términos"*.

    **El piso es 90, y la cifra tiene método.** Medición re-hecha con **el mismo método que G1 usará**
    (`stripTsComments` + extracción acotada al cuerpo del array, ver plan 133-05):
    `TERMINOS_LINK_EXT = 8`, `TERMINOS_COBERTURA = 6`, literales inline de `TERMINOS_PROHIBIDOS` = **78**
    ⇒ **92 únicos**. Margen real: **2**.
    El "104" que circulaba antes salía de una extracción **sin** strippear comentarios, que
    capturaba términos citados dentro de JSDoc; al mandar el strip, esa cifra quedó **invalidada**.
    *Cuando un fix cambia cómo se mide algo, toda cifra derivada de la medición anterior deja de valer.*
    Sigue siendo cierto que extraer solo `TERMINOS_PROHIBIDOS` sin resolver sus dos spreads da
    **78**, por debajo del piso — que es exactamente lo que el piso debe cazar.
    Si el conteo real sale < 90: **PARAR y escalar**, jamás bajar el piso.

    Movimiento puro: no cambiar el contenido de ningún término, no añadir, no quitar. Cualquier
    necesidad de editar un término es hallazgo: PARAR y escalar.
  </action>
  <acceptance_criteria>
    - `grep -c 'TERMINOS_PROHIBIDOS' app/lib/terminos-insinuacion.ts` ≥ 1 y
      `n=$(grep -c 'const TERMINOS_PROHIBIDOS' app/lib/anti-insinuacion-guard.test.ts || true); test "$n" -eq 0`
      (no quedó copia). Nótese el `|| true`: `grep -c` devuelve 0 con rc=1.
    - `grep -c 'from "./terminos-insinuacion"' app/lib/anti-insinuacion-guard.test.ts` = 1.
    - `n=$(grep -cE '\.tsx"|@/components' app/lib/terminos-insinuacion.ts || true); test "$n" -eq 0`.
    - `n=$(grep -c 'NEGACIONES_LOCKED' app/lib/terminos-insinuacion.ts || true); test "$n" -eq 0`.
    - El `it` del piso existe con el umbral **90** y pasa; el conteo real de `TERMINOS_PROHIBIDOS`
      se **cita literal** en el SUMMARY (referencia medida con strip: **92**).
    - **Mutación del piso** (nunca bajo `set -e`): borrar 5 entradas de `TERMINOS_PROHIBIDOS` en
      `terminos-insinuacion.ts` (92 → 87 < 90),
      `if NO_COLOR=1 pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts > /tmp/mut2b.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0`. Revertir con `git checkout --`. (El margen es 2, así que borrar 3 bastaría;
      se borran 5 para que la mutación sea inequívoca.)
    - `pnpm --filter ./app exec tsc --noEmit` sale rc=0.
    - Conteo del guard solo con el idiom de `<medicion>`: `G_MID` = exactamente `G_ANTES + 1`.
      Ambos citados.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tee /tmp/133-ai-mid.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-ai-mid.log | grep -oE '[0-9]+'); echo "G_MID=$N"; test "$N" -gt 0</automated>
  </verify>
  <done>Los tres arrays viven en `app/lib/terminos-insinuacion.ts`, el guard los importa, `NEGACIONES_LOCKED` no se movió, el piso de **90** pasa con el conteo real citado, y `G_MID == G_ANTES + 1`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: G3 — los DOS skips silenciosos a fallo duro, con criterio que ve LOS DOS</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G3 (*"un guard ciego que sale verde es el falso verde de manual"*)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J3 (**son DOS**; el reporte debe decir dos, no uno;
      el conteo de faltantes se RE-verifica en ejecución)
    - **Ubicaciones verificadas hoy con `grep -n`:** los dos `continue` dentro de
      `try{readFileSync}catch{}` están en `app/lib/anti-insinuacion-guard.test.ts:947` (test `(1)`,
      bloque `:942-948`) y **`:977`** (test `(1b)` WR-03, bloque `:974-978`). El archivo tiene **10**
      ocurrencias de `continue`, de las cuales **8 son texto de JSDoc**
      (`:298, :312, :350, :385, :409, :498, :1076, :1126`) — el criterio debe distinguirlas.
    - `133-PREMORTEM-PLANES.md` §BLOCKER 5, reproducido en la ronda 2 — **`grep -n -A2 'readFileSync'
      | grep -c 'continue'` vale `1` HOY sin ningún cambio**: en el bloque `:942-948` median 3
      líneas entre `readFileSync` y `continue`. Con `-A2` el criterio **solo ve el segundo skip**;
      cerrar la mitad lo haría marcar 0. Con `-A4` y `-A6` vale `2`.
    - `app/lib/anti-insinuacion-guard.test.ts:864-881` (`TODAS_LAS_SUPERFICIES`) y `:68` (`APP_ROOT`)
    - `app/lib/create-view-guard.test.ts:404-452` (guard-of-the-guards)
    - `app/lib/anti-insinuacion-guard.test.ts:920-932` (idiom de sanity de archivo concreto)
    - `133-RESEARCH.md` §D y `133-PREMORTEM-PLANES.md` P4 (verificado dos veces:
      **63 rutas, 0 faltantes** ⇒ G3 no pone la suite roja; el plan NO presupuesta reparación)
  </read_first>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <behavior>
    - `leerSuperficie(rel)` con una ruta real del allowlist devuelve contenido de longitud > 100.
    - `leerSuperficie(rel + ".noexiste")` **lanza** con un mensaje que contiene la ruta y la frase
      `guard ciego`. Control positivo apareado: la MISMA ruta, una sola variable (la existencia).
    - Test de no-hueco: **toda** ruta de `TODAS_LAS_SUPERFICIES` existe, con piso duro
      `expect(TODAS_LAS_SUPERFICIES.length).toBeGreaterThanOrEqual(63)`.
    - Ni el test `(1)` ni el test `(1b)` contienen ya `catch { continue }` sobre `readFileSync`.
  </behavior>
  <action>
    Añadir un helper `leerSuperficie(rel: string): string` que hace
    `readFileSync(join(APP_ROOT, rel), "utf-8")` y, ante error, **lanza** con mensaje accionable:
    `"Superficie declarada inexistente: <rel>. Un allowlist con una ruta muerta es un guard ciego que sale verde. Si el archivo se movió, corrige la ruta; si se borró, quita la entrada."`

    Sustituir por ese helper los **dos** bloques `try { … readFileSync … } catch { continue }`:
    el de `:942-948` (test `(1)`) **y** el de `:974-978` (test `(1b)` WR-03).

    Añadir los tres `it` del `<delta_de_tests>` — **planos, sin `it.each`**:
    (a) no-hueco con el piso `>= 63`; (b) control positivo con `TODAS_LAS_SUPERFICIES[0]`;
    (c) control positivo apareado con `TODAS_LAS_SUPERFICIES[0] + ".noexiste"` que debe lanzar.

    **RE-VERIFICAR el conteo de faltantes en ejecución.** Research y premortem midieron 63/0 en dos
    sesiones distintas, pero un archivo movido lo cambia. Si el conteo de rutas faltantes **no es
    0**, el ejecutor **PARA y escala** — no repara el allowlist por su cuenta: una ruta muerta puede
    significar que una superficie real dejó de escanearse.

    **NO crear un archivo nuevo `app/lib/*guard*.test.ts`**: `create-view-guard.test.ts:435-442`
    exige que todo guard en disco esté listado en `app/package.json:11` en el mismo commit. G3
    modifica el guard existente (D-133-K3, corolario) — así no se abre ese frente.
  </action>
  <acceptance_criteria>
    - **Criterio de los DOS skips, con ventana suficiente y captura tolerante a rc:**
      `n=$(grep -n -A6 'readFileSync' app/lib/anti-insinuacion-guard.test.ts | grep -c 'continue' || true); test "$n" -eq 0`.
      **`-A6`, no `-A2`** (medido: `-A2`→1, `-A4`→2, `-A6`→2).
    - **Control de que el criterio distingue las 8 menciones de `continue` en JSDoc:** antes de tocar
      nada ese mismo comando debe dar **2**; después, **0**. Ambos números citados.
    - **Mutación POR SITIO (una variable cada vez):** revertir **solo** el skip del test `(1)`
      (dejando el otro cerrado) y comprobar que el criterio da `1` y que la mutación de ruta
      inventada **no muerde en ese sitio**. **Cómo aislarlo — sin esto la observación es imposible:**
      el `it` (a) de no-hueco cae igual y teñiría de rojo la suite en ambos casos, así que la
      mutación por sitio se corre con **filtro por nombre**, que es lo que los args de `vitest`
      realmente son:
      `NO_COLOR=1 pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts -t "ningún término prohibido aparece en el texto renderizado"`
      aísla el test `(1)`, y **`-t "1b\) WR-03"`** aísla el `(1b)` (nombres verificados con `grep -n`:
      el `it` de `(1)` está en `:934` y el de `(1b)` en `:963`, ambos bajo el `describe` de `:917`).
      ⚠️ **El paréntesis va ESCAPADO: `-t` es un REGEX, no un literal.** Medido: `-t "(1b) WR-03"`
      trata `(1b)` como grupo de captura, busca `1b WR-03`, no matchea, y sale
      `Tests 57 skipped` con **rc=0** — o sea, las dos direcciones de la mutación saldrían verdes y
      el ejecutor concluiría "cerrado y verificado" sin haber corrido una sola aserción. Es el falso
      verde de manual sobre el `must_haves.truth` de este plan.
      **Por eso cada corrida con `-t` asserta además el conteo:**
      `N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/aislado.log | grep -oE '[0-9]+'); test "$N" -eq 1`.
      Un filtro que selecciona 0 tests es un criterio inservible, no un test verde.
      Con el filtro puesto **y el conteo assertado**, el sitio revertido sale **verde** (skip
      silencioso vivo) y el sitio cerrado sale **rojo**: esa es la observación que prueba el cierre
      por separado.
      Repetir revirtiendo **solo** el del test `(1b)`.
      Restaurar ambos. Sin esto, "se cerraron los dos" no está verificado.
    - `grep -c 'guard ciego' app/lib/anti-insinuacion-guard.test.ts` ≥ 1.
    - **Mutación de G3** (no bajo `set -e`): añadir temporalmente a un array `SUPERFICIES_*` la ruta
      `components/no-existe-jamas.tsx`,
      `if NO_COLOR=1 pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts > /tmp/mut3.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0` **y** `grep -c 'no-existe-jamas' /tmp/mut3.log` ≥ 1. Revertir.
      Antes del cambio esa misma mutación salía **verde**.
    - **DELTA FIJO:** `NO_COLOR=1 pnpm --filter ./app test 2>&1 | tee /tmp/133-app-despues.log`;
      `N_DESPUES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-despues.log | grep -oE '[0-9]+')`;
      **`test $((N_DESPUES - N_ANTES)) -eq 4`**. Ambos citados. Cualquier otro valor ⇒ PARAR y escalar.
      ⚠️ **Punto de fallo más probable de este plan:** un delta exacto sobre una base de ~1799 en el
      archivo más sensible del repo, después de sacarle ~190 líneas. Un `it` colateral escrito sin
      querer, o un test preexistente que se pierde con la extracción, lo pone rojo. Diagnóstico a
      imprimir antes de escalar, **en este orden**:
      (1) el **conteo por archivo** de ambos logs — es ejecutable con los logs que ya existen, porque
      el reporter por defecto sí imprime la línea por archivo (`✓ lib/x.test.ts (57 tests | ...)`);
      (2) solo si eso no basta, **re-correr con `--reporter=verbose`** para obtener los nombres de
      test y recién ahí hacer el `diff`. ⚠️ El reporter por defecto **NO imprime los nombres de los
      tests que pasan**, así que un `diff` sobre los logs normales compara líneas que no existen y no
      diagnostica nada. No ajustar el número esperado: el 4 está fijado por la tabla de
      `<delta_de_tests>`.
    - `Test Files` de `/tmp/133-app-despues.log` es **exactamente el mismo** que en
      `/tmp/133-app-antes.log` (`terminos-insinuacion.ts` no es archivo de test). Citar ambos.
    - `pnpm --filter ./app guards` sale rc=0 y su conteo se registra.
    - `git diff --name-only <SHA literal de la Task 1>` lista exactamente dos archivos:
      `app/lib/terminos-insinuacion.ts` y `app/lib/anti-insinuacion-guard.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter ./app test 2>&1 | tee /tmp/133-app-despues.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-despues.log | grep -oE '[0-9]+'); echo "N_DESPUES=$N"; test "$N" -gt 0</automated>
  </verify>
  <done>Los DOS skips cerrados y **verificados uno por uno** con mutación por sitio; el criterio usa `-A6` y pasa de 2 a 0; delta de tests exactamente 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allowlist declarado → filesystem real | Una ruta del allowlist que no existe hace que el guard escanee menos de lo que declara |
| guard existente → módulo extraído | Mover un array de términos puede perder entradas en silencio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | Tampering / Repudiation | `anti-insinuacion-guard.test.ts:947`, `:977` — skip silencioso | mitigate | `leerSuperficie` que lanza + no-hueco con piso 63 + **mutación por sitio** |
| T-133-05 | Tampering | Extracción de `TERMINOS_PROHIBIDOS` pierde entradas | mitigate | `it` de piso `>= 90` (medición con strip: 92) + su mutación + delta FIJO de 4 |
| T-133-06 | Tampering | Crear un `app/lib/*guard*.test.ts` nuevo pone rojo `create-view-guard.test.ts:435-442` | mitigate | G3 modifica el guard existente; cero archivos de guard nuevos |
| T-133-20 | Repudiation | Criterio de "DOS skips" con ventana `-A2` que solo ve uno | mitigate | Ventana `-A6`, transición 2→0 citada, y mutación por sitio |
| T-133-27 | Repudiation | Piso calibrado con un método de medición distinto del que el plan manda usar | mitigate | Piso 90 medido **con `stripTsComments`**, el mismo método que G1 usa; el "104" sin strip queda declarado inválido |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES` y `N_DESPUES` citados; `N_DESPUES - N_ANTES == 4` exacto; `Test Files` sin cambio.
2. `grep -n -A6 'readFileSync' … | grep -c 'continue'` pasa de **2** a **0**, ambos citados.
3. Mutación por sitio: revertir uno solo de los dos skips hace que el criterio dé `1`.
4. Mutación de ruta inventada → rc≠0 con la ruta nombrada en el log.
5. Mutación del piso (borrar 5 términos) → rc≠0.
6. `git diff --name-only <SHA literal>` → exactamente 2 archivos.
</verification>

<success_criteria>
- Los DOS skips cerrados y verificados **por separado**; el reporte dice **dos**.
- Un allowlist con ruta muerta pone la suite roja, demostrado por mutación.
- `TERMINOS_PROHIBIDOS` importable desde un módulo sin JSX, con piso **90** y conteo real citado.
- Delta de tests de `app/` exactamente **+4**, sin `it.each`.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-02-SUMMARY.md` when done.
Declara: **se cerraron DOS skips silenciosos**, con sus dos ubicaciones y la evidencia de la mutación
por sitio; el conteo de rutas faltantes re-verificado; y el conteo real de `TERMINOS_PROHIBIDOS`.
</output>
