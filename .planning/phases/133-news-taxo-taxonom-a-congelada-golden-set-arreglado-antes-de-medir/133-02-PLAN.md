---
phase: 133-news-taxo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/terminos-insinuacion.ts
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Un archivo del allowlist de superficies que no existe hace FALLAR el guard, ya no lo salta"
    - "Los dos skips silenciosos (:943-948 y :974-978) están cerrados, no uno"
    - "TERMINOS_PROHIBIDOS vive en un módulo sin JSX que el guard importa, legible por disco desde packages/news"
    - "La suite de app/ imprime exactamente el mismo conteo de tests antes y después (diff-cero)"
  artifacts:
    - path: "app/lib/terminos-insinuacion.ts"
      provides: "TERMINOS_PROHIBIDOS + TERMINOS_LINK_EXT + TERMINOS_COBERTURA, sin JSX ni imports de componentes"
      exports: ["TERMINOS_PROHIBIDOS", "TERMINOS_LINK_EXT", "TERMINOS_COBERTURA"]
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/lib/terminos-insinuacion.ts"
      via: "import (no re-tipeo)"
      pattern: "from \"./terminos-insinuacion\""
---

<objective>
G3 (D-133-A2.4 + D-133-J3): cerrar los **DOS** skips silenciosos del guard anti-insinuación, y
extraer `TERMINOS_PROHIBIDOS` a un módulo importable (vía B de D-133-J2) para que G1 pueda leerlo
por disco desde `packages/news` sin invertir la dirección de dependencia del monorepo.

Purpose: un allowlist con una ruta muerta es un guard ciego que sale verde — el falso verde de
manual. Y sin la extracción, G1 solo tendría la vía C (replicar la lista), prohibida por D-133-J2.6.
Output: `app/lib/terminos-insinuacion.ts` + un `app/lib/anti-insinuacion-guard.test.ts` que falla
duro ante una superficie inexistente.

**Este plan toca el archivo más sensible del repo.** Ambos cambios van en el MISMO commit
(D-133-J2.5: una sola pasada). El criterio rector es **diff-cero de comportamiento**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar la línea base de app/ ANTES de tocar nada</name>
  <read_first>
    - `133-RESEARCH.md` §K P-1 (`passWithNoTests: true` en `app/vitest.config.ts:29`; los args de
      `vitest run` son filtros de nombre, no rutas)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2.5 (criterio de diff-cero: el conteo impreso de
      `pnpm --filter ./app test` idéntico antes y después)
    - `app/package.json:9` (el script `guards` y sus 11 archivos por nombre)
  </read_first>
  <files>(ninguno — solo medición)</files>
  <action>
    Antes de editar una sola línea de `app/`, ejecutar y **registrar literalmente** en el SUMMARY:

    1. `git rev-parse HEAD` → guardar el SHA como **base fija** (un `git diff --name-only` sin base
       fija siempre pasa con commits atómicos — gotcha LOCKED).
    2. `pnpm --filter ./app test 2>&1 | tee /tmp/133-app-antes.log`
       y extraer con `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-antes.log` el número
       `N_ANTES` (y también `Test Files X passed`).
    3. `pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tee /tmp/133-ai-antes.log`
       y extraer `G_ANTES` (conteo del guard solo).

    Estos tres valores son la vara de este plan. Si `N_ANTES` no se puede extraer (el grep no
    encuentra la línea), **PARAR y escalar**: sin línea base no hay diff-cero demostrable.
  </action>
  <acceptance_criteria>
    - `/tmp/133-app-antes.log` existe y `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-antes.log`
      devuelve exactamente una línea con un número > 0.
    - `/tmp/133-ai-antes.log` existe y su conteo extraído `G_ANTES` es > 0.
    - El SHA base está anotado en el SUMMARY (valor literal, no "HEAD").
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app test 2>&1 | tee /tmp/133-app-antes.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-antes.log</automated>
  </verify>
  <done>`N_ANTES`, `G_ANTES` y el SHA base registrados literalmente. Sin ellos el plan no puede continuar.</done>
</task>

<task type="auto">
  <name>Task 2: Extraer TERMINOS_PROHIBIDOS a app/lib/terminos-insinuacion.ts (vía B, D-133-J2)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J2 completo (vía B acotada; `NEGACIONES_LOCKED` se
      QUEDA donde está; vía C PROHIBIDA)
    - `app/lib/anti-insinuacion-guard.test.ts:569` (`TERMINOS_LINK_EXT`), `:605`
      (`TERMINOS_COBERTURA`), `:623-757` (`TERMINOS_PROHIBIDOS`, ~110 entradas + 2 spreads)
    - `app/lib/anti-insinuacion-guard.test.ts:759-768` (JSDoc de `IDIOMS_APROBADOS` — el argumento
      canónico del repo: *"este archivo IMPORTA el array, no lo re-tipea, así un stem mal escrito
      rompe el import en vez de divergir en silencio entre dos copias"*)
    - `app/lib/anti-insinuacion-guard.test.ts:764-826` (`NEGACIONES_LOCKED` — importa 5 constantes
      de componentes `.tsx` en `:33-40`; **NO se mueve**)
    - `app/tsconfig.json` (alias `@/*`; `app/` no depende de ningún `@obs/*` — régimen documentado)
  </read_first>
  <files>app/lib/terminos-insinuacion.ts, app/lib/anti-insinuacion-guard.test.ts</files>
  <action>
    Crear `app/lib/terminos-insinuacion.ts` conteniendo **exactamente** los tres arrays movidos
    desde el guard, byte por byte en su contenido de strings: `TERMINOS_LINK_EXT` (hoy `:569`),
    `TERMINOS_COBERTURA` (hoy `:605`) y `TERMINOS_PROHIBIDOS` (hoy `:623-757`, incluidos sus dos
    spreads de las dos sub-listas). El módulo:
    - NO contiene JSX.
    - NO importa ningún componente `.tsx` ni nada de `@/components/*`.
    - NO importa ningún `@obs/*` (mantener el desacoplamiento documentado de `app/`).
    - Lleva un JSDoc que explica por qué existe: *"extraído del guard para que
      `packages/news/src/eval/taxonomia-guard.test.ts` (G1) pueda leer los términos por DISCO —
      lectura de bytes, no import: la dirección `app`→`packages` del grafo de módulos no se
      invierte. D-133-J2."*
    - Es un módulo `.ts` de producción (no `.test.ts`), y por tanto lo typechequea `tsc --noEmit`
      de `app/` en CI.

    En `app/lib/anti-insinuacion-guard.test.ts`: **borrar** las tres declaraciones y sustituirlas
    por un `import { TERMINOS_PROHIBIDOS, TERMINOS_LINK_EXT, TERMINOS_COBERTURA } from "./terminos-insinuacion";`.
    NO re-tipear ni duplicar: si algo queda duplicado, es la deuda de ICS en miniatura.

    **`NEGACIONES_LOCKED` NO se mueve** (D-133-J2.2): importa constantes de componentes `.tsx` y
    moverlo arrastraría jsdom al carril de news.

    NO cambiar el contenido de ningún término, ni añadir, ni quitar entradas. Este es un movimiento
    puro. Cualquier necesidad de editar un término es hallazgo: PARAR y escalar.
  </action>
  <acceptance_criteria>
    - `grep -c 'TERMINOS_PROHIBIDOS' app/lib/terminos-insinuacion.ts` devuelve ≥ 1 y
      `grep -c 'const TERMINOS_PROHIBIDOS' app/lib/anti-insinuacion-guard.test.ts` devuelve **0**
      (no quedó copia).
    - `grep -c 'from "./terminos-insinuacion"' app/lib/anti-insinuacion-guard.test.ts` devuelve 1.
    - `grep -cE '\.tsx"|@/components' app/lib/terminos-insinuacion.ts` devuelve 0 (sin JSX ni componentes).
    - `grep -c 'NEGACIONES_LOCKED' app/lib/terminos-insinuacion.ts` devuelve 0 (se quedó donde estaba).
    - **Piso anti-cero-vacuo del contenido:** un `node -e` que importe el módulo compilado o un test
      nuevo asserta `TERMINOS_PROHIBIDOS.length >= 100` (el research contó ~110 entradas + 2 spreads).
      Un movimiento que pierda entradas debe ser ruidoso, no silencioso.
    - `pnpm --filter ./app exec tsc --noEmit` sale rc=0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tee /tmp/133-ai-mid.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-ai-mid.log</automated>
  </verify>
  <done>Los tres arrays viven en `app/lib/terminos-insinuacion.ts`, el guard los importa, `NEGACIONES_LOCKED` no se movió, y el conteo del guard es idéntico a `G_ANTES`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: G3 — los DOS skips silenciosos pasan a fallo duro, con no-hueco y mutación</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.4 G3 (*"un guard ciego que sale verde es el falso verde de manual"*)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J3 (**son DOS**: `:943-948` y `:974-978`; el reporte
      debe decir dos, no uno; y el conteo de faltantes debe RE-verificarse en ejecución)
    - `app/lib/anti-insinuacion-guard.test.ts:943-948` (skip #1, test (1)) y `:974-978`
      (skip #2, test (1b) WR-03) — el mismo `try { readFileSync } catch { continue }`
    - `app/lib/anti-insinuacion-guard.test.ts:864-881` (`TODAS_LAS_SUPERFICIES`, unión de los 14 arrays)
    - `app/lib/anti-insinuacion-guard.test.ts:68` (`APP_ROOT` anclado a `import.meta.dirname`, WR-06)
    - `app/lib/create-view-guard.test.ts:404-452` (guard-of-the-guards: piso anti-cero-vacuo,
      "todo guard en disco está en el script", "el script no tiene nombres fantasma")
    - `app/lib/anti-insinuacion-guard.test.ts:920-932` (idiom de sanity de archivo concreto)
    - `133-RESEARCH.md` §D (verificado hoy: **63 rutas declaradas, 0 faltantes** ⇒ activar G3 NO
      pone la suite roja; el plan NO presupuesta reparación de allowlist)
  </read_first>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <behavior>
    - `leerSuperficie(rel)` con una ruta real del allowlist devuelve contenido de longitud > 100.
    - `leerSuperficie(rel + ".noexiste")` **lanza** con un mensaje que contiene la ruta y la frase
      `guard ciego`. (Control positivo apareado: la MISMA ruta con una sola variable modificada —
      la existencia del archivo.)
    - Existe un test de no-hueco: **toda** ruta de `TODAS_LAS_SUPERFICIES` existe en disco, con piso
      duro `expect(TODAS_LAS_SUPERFICIES.length).toBeGreaterThanOrEqual(63)`.
    - Ni el test (1) ni el test (1b) contienen ya `catch { continue }` sobre `readFileSync`.
  </behavior>
  <action>
    Añadir en `app/lib/anti-insinuacion-guard.test.ts` un helper
    `leerSuperficie(rel: string): string` que hace `readFileSync(join(APP_ROOT, rel), "utf-8")` y,
    ante error, **lanza** con mensaje accionable literal:
    `"Superficie declarada inexistente: <rel>. Un allowlist con una ruta muerta es un guard ciego que sale verde. Si el archivo se movió, corrige la ruta; si se borró, quita la entrada."`

    Sustituir por ese helper los **dos** bloques `try { raw = readFileSync(full,"utf-8") } catch { continue }`:
    el de `:943-948` (test (1)) **y** el de `:974-978` (test (1b) WR-03). Cerrar solo uno y
    reportarlo como cerrado sería el falso verde de esta fase (D-133-J3).

    Añadir un test nuevo de **no-hueco** en el mismo archivo: recorre `TODAS_LAS_SUPERFICIES`,
    asserta que cada ruta existe, y lleva el piso duro `toBeGreaterThanOrEqual(63)` contra el
    cero-vacuo (`it.each` sobre un array vacío generaría cero tests y saldría verde).

    Añadir el **control positivo apareado** como test: dos llamadas a `leerSuperficie`, una con una
    ruta real tomada de `TODAS_LAS_SUPERFICIES[0]` (no lanza, y el contenido tiene longitud > 100) y
    otra con `TODAS_LAS_SUPERFICIES[0] + ".noexiste"` (lanza). Una única variable entre ambas: la
    existencia del archivo.

    **RE-VERIFICAR el conteo de faltantes en ejecución.** El research midió 63/0 el 2026-08-06, pero
    un archivo movido desde entonces lo cambia. Si al correr el test de no-hueco el conteo de rutas
    faltantes **no es 0**, el ejecutor **PARA y escala** — no repara el allowlist por su cuenta, no
    estaba presupuestado y una ruta muerta puede significar que una superficie real dejó de
    escanearse.

    **NO crear un archivo nuevo `app/lib/*guard*.test.ts`**: `create-view-guard.test.ts:435-442`
    exige que todo guard en disco esté listado en `app/package.json:9` en el mismo commit. G3 se
    implementa **modificando** el guard existente (D-133-K3, corolario) — así no hay nada que añadir
    a ese script y no se abre un frente nuevo.
  </action>
  <acceptance_criteria>
    - `grep -c 'catch' app/lib/anti-insinuacion-guard.test.ts` no incluye ningún `catch` que preceda
      a un `continue` sobre `readFileSync`: verificar con
      `grep -n -A2 'readFileSync' app/lib/anti-insinuacion-guard.test.ts | grep -c 'continue'` = **0**.
    - `grep -c 'guard ciego' app/lib/anti-insinuacion-guard.test.ts` ≥ 1 (el mensaje accionable existe).
    - **Diff-cero + los tests nuevos**, medido con `N_ANTES` de la Task 1:
      `pnpm --filter ./app test 2>&1 | tee /tmp/133-app-despues.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-despues.log` → `N_DESPUES`.
      El criterio captura **ambos números**: `N_DESPUES` debe ser exactamente
      `N_ANTES + (número de tests nuevos añadidos en esta tarea)`, y **cero tests deben haberse
      perdido** (`Test Files X passed` idéntico o +0). Si `N_DESPUES < N_ANTES + nuevos`, el
      ejecutor **PARA y escala**: significa que la extracción de la Task 2 rompió comportamiento.
      Ambos números se citan literalmente en el SUMMARY.
    - **Prueba de mutación de G3** (no bajo `set -e`): añadir temporalmente a uno de los arrays
      `SUPERFICIES_*` una ruta inventada (`components/no-existe-jamas.tsx`), correr
      `if pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts > /tmp/mut3.log 2>&1; then rc=0; else rc=$?; fi`
      y comprobar `test $rc -ne 0` **y** `grep -c 'no-existe-jamas' /tmp/mut3.log` ≥ 1. Revertir.
      Este es el único criterio que prueba que el skip está realmente cerrado — antes del cambio,
      esa misma mutación salía **verde**.
    - `pnpm --filter ./app guards` sale rc=0 y su conteo impreso se registra.
    - `git diff --name-only <SHA_BASE_DE_LA_TASK_1>` lista exactamente dos archivos:
      `app/lib/terminos-insinuacion.ts` y `app/lib/anti-insinuacion-guard.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app test 2>&1 | tee /tmp/133-app-despues.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-app-despues.log</automated>
  </verify>
  <done>Los DOS skips cerrados, el test de no-hueco con piso 63 verde, el control positivo apareado y la prueba de mutación demostrados, y `N_ANTES`/`N_DESPUES` citados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allowlist declarado → filesystem real | Una ruta del allowlist que no existe hace que el guard escanee menos de lo que declara |
| guard existente → módulo extraído | Mover un array de términos puede perder entradas en silencio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | Tampering / Repudiation | `anti-insinuacion-guard.test.ts:943`, `:974` — skip silencioso | mitigate | `leerSuperficie` que lanza + test de no-hueco con piso 63 + mutación con ruta inventada que debe poner rojo |
| T-133-05 | Tampering | Extracción de `TERMINOS_PROHIBIDOS` pierde entradas | mitigate | Piso duro `length >= 100` + diff-cero del conteo de la suite de `app/` con `N_ANTES` capturado antes de tocar nada |
| T-133-06 | Tampering | Crear un `app/lib/*guard*.test.ts` nuevo pone rojo `create-view-guard.test.ts:435-442` | mitigate | G3 modifica el guard existente; no se crea archivo de guard nuevo en `app/lib/` |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones. Cualquier `pnpm add` en este plan es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES` y `N_DESPUES` de `pnpm --filter ./app test` citados literalmente, con la cuenta de los
   tests nuevos que explica la diferencia. Ambos números en el SUMMARY.
2. Mutación de G3 (ruta inventada en un `SUPERFICIES_*`) → rc≠0 y el nombre de la ruta en el log.
3. `grep -n -A2 'readFileSync' app/lib/anti-insinuacion-guard.test.ts | grep -c 'continue'` = 0.
4. `git diff --name-only <SHA literal>` → exactamente 2 archivos.
</verification>

<success_criteria>
- Los DOS skips silenciosos cerrados (no uno), con el reporte diciendo **dos**.
- Un allowlist con una ruta muerta pone la suite roja, demostrado por mutación.
- `TERMINOS_PROHIBIDOS` importable desde un módulo sin JSX, sin ninguna copia residual.
- Diff-cero de comportamiento de `app/` demostrado con ambos conteos impresos.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-02-SUMMARY.md` when done.
El SUMMARY debe declarar explícitamente: **se cerraron DOS skips silenciosos**, con las dos
ubicaciones, y el conteo de rutas faltantes re-verificado en ejecución.
</output>
