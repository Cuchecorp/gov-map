---
phase: 133-news-taxo
plan: 04
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - packages/news/src/prefiltro-lexico.ts
  - packages/news/src/prefiltro-lexico.test.ts
  - packages/news/src/eval/entrada-llm.ts
  - packages/news/src/eval/entrada-llm.test.ts
  - packages/news/src/eval/caso-golden.ts
  - packages/news/src/eval/caso-golden.test.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Existe UNA sola función de truncado; LIMITE_DESCRIPCION y MARGEN_TRUNCADO siguen privados y sin duplicar"
    - "El pre-filtro se comporta igual que antes: los tests preexistentes no bajan y el delta es fijo"
    - "entrada_llm conserva tildes, mayúsculas, y titulo/descripcion separados"
    - "El esquema del caso golden existe y valida, sin ningún caso etiquetado"
    - "La divergencia de cortes foldeado/sin-foldear está MEDIDA, con su causa determinada y su efecto sobre cobertura evaluado"
  artifacts:
    - path: "packages/news/src/eval/caso-golden.ts"
      provides: "Esquema zod del caso golden (D-133-F2), SIN casos"
      exports: ["CasoGoldenSchema", "type CasoGolden"]
    - path: "packages/news/src/eval/entrada-llm.ts"
      provides: "construirEntradaLlm + coberturaTerminos"
      exports: ["construirEntradaLlm", "coberturaTerminos"]
  key_links:
    - from: "packages/news/src/eval/entrada-llm.ts"
      to: "packages/news/src/prefiltro-lexico.ts"
      via: "import de truncarDescripcion (no constante replicada)"
      pattern: "truncarDescripcion"
---

<objective>
D-133-F2 + D-133-J1: una sola función de truncado compartida entre el pre-filtro y `entrada_llm`,
el esquema del caso golden (sin ningún caso), y el chequeo de cobertura que debe correrse **antes**
de etiquetar.

Purpose: `entrada_llm` truncada a 300 mientras el pre-filtro decide sobre 600 hacía que
`prefiltro.terminos` pudiera citar un término **ausente** del input del clasificador — el golden
penalizaría al modelo por información amputada. Y una constante copiada es la deuda de ICS en
miniatura.
Output: `truncarDescripcion` exportada, `entrada-llm.ts`, `caso-golden.ts` y sus tests.

**133-a NO etiqueta ni un solo caso.** Aquí solo se construyen el esquema y las funciones.

**PROHIBIDO tocar `packages/news/src/eval/index.ts`** — propiedad exclusiva del plan 133-01, y el
plan 133-03 corre en esta misma wave con `use_worktrees: true`. Importar por ruta relativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
</context>

<medicion>
## Idiom de medición — OBLIGATORIO

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada**. Verificado.

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**Umbral absoluto de este plan: `test "$N" -gt 206`** (línea base del paquete). No se usa
aritmética relativa contra el conteo de 133-01 ni de 133-03: **133-03 corre en esta misma wave y
también sube el conteo**, así que cualquier diferencia entre planes de la misma wave depende del
orden de merge y no es verificable.

El **delta interno de este plan sí es fijo** — ver `<delta_de_tests>`. Se mide con `N_ANTES` /
`N_DESPUES` capturados **dentro del propio plan**, sobre el mismo worktree.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan

| Tarea | `it(...)` nuevos | Cuáles |
|---|---|---|
| Task 1 | **3** | (a) `truncarDescripcion deja intacto un texto más corto que el límite` · (b) `truncarDescripcion corta en frontera de palabra` sobre una descripción **claramente mayor** que `LIMITE_DESCRIPCION + MARGEN_TRUNCADO` · (c) `truncarDescripcion respeta el margen derivado del vocabulario` |
| Task 2 | **4** | (a) `construirEntradaLlm conserva tildes y mayúsculas` · (b) `construirEntradaLlm no trunca el título` · (c) `comparativo de índices de corte sobre los 5 fixtures` · (d) `coberturaTerminos sobre lista vacía LANZA` |
| Task 3 | **5** | (a) caso bien formado valida · (b) falta un obligatorio ⇒ falla · (c) `estrato` inválido ⇒ falla · (d) etiqueta fuera de `ETIQUETAS` ⇒ falla · (e) campo extra ⇒ falla (`.strict()`) |
| **Total** | **12** | |

**`it.each` PROHIBIDO en los tests nuevos de este plan** (genera un número variable, y con array
vacío genera cero — cero-vacuo). Criterio final: `test $((N_DESPUES - N_ANTES)) -eq 12`.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer truncarDescripcion() + escribir el test que HOY falta para el corte en frontera</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1 (los 4 puntos)
    - `133-RESEARCH.md` §B (tabla de exports; secuencia despojo→fold→truncado→concatenación;
      `MARGEN_TRUNCADO` es **runtime-derivado** — jamás hardcodear 615 ni 23)
    - `packages/news/src/prefiltro-lexico.ts` — verificado hoy: `LIMITE_DESCRIPCION = 600` (`:50`),
      `MARGEN_TRUNCADO` (`:107`, vale **23** hoy: `"tribunal constitucional"`, 30 términos),
      `construirTexto` (`:109`), el truncado en `:120-121`, `String.includes` PROHIBIDO (`:127-128`),
      prohibición LOCKED de podar el vocabulario (`:6-9`)
    - `packages/news/src/prefiltro-lexico.test.ts:187-194` — **LEER CON ATENCIÓN.**
      `133-PREMORTEM-PLANES.md` §BLOCKER 4 lo reprodujo numéricamente: su descripción mide
      `623 = 600 + 23` **exactos**, así que el `slice` no corta nada y el `.replace(/\S*$/,"")` solo
      borra `"hoy"`. **Quitar el `.replace` deja ese test VERDE.** Ese test cubre el *margen*, no el
      corte en frontera. **El `.replace` no tiene cobertura hoy** — por eso esta tarea la escribe.
    - `133-RESEARCH.md` §A (el barrel raíz `packages/news/src/index.ts` es aditivo-seguro; aquí no
      se toca ninguno de los dos barrels)
  </read_first>
  <files>packages/news/src/prefiltro-lexico.ts, packages/news/src/prefiltro-lexico.test.ts</files>
  <behavior>
    - `truncarDescripcion(s)` con `s` más corto que el límite lo devuelve intacto.
    - Con `s` **claramente más largo** que `LIMITE_DESCRIPCION + MARGEN_TRUNCADO` (p. ej. 3× el
      límite), corta en **frontera de palabra**: el resultado no termina en una palabra partida.
      Aserción concreta: el resultado, dividido por espacios, tiene como última palabra una palabra
      **completa** presente en la entrada. Este es el test que hoy no existe.
    - El margen se **deriva** del vocabulario: el test lo calcula igual que el módulo o prueba una
      propiedad, **jamás** hardcodea 615/23.
    - Los tests preexistentes de `prefiltro-lexico.test.ts` siguen verdes (extracción pura).
  </behavior>
  <action>
    En `packages/news/src/prefiltro-lexico.ts`, extraer la lógica de truncado de `:120-121` a
    `export function truncarDescripcion(texto: string): string` — `slice` a
    `LIMITE_DESCRIPCION + MARGEN_TRUNCADO` y `.replace(/\S*$/, "")`. **`LIMITE_DESCRIPCION` y
    `MARGEN_TRUNCADO` siguen privados del módulo y sin duplicar** — eso es lo que D-133-F2.1 protege.

    Reescribir `construirTexto` (`:118-121`) para que **llame** a `truncarDescripcion`. No cambiar
    nada más: ni el orden despojo→fold, ni la concatenación `` `${t} ${d}` ``, ni que el título no
    se trunque. Refactor de extracción puro.

    **Escribir los 3 `it` del `<delta_de_tests>`**, incluido el del corte en frontera con una
    descripción **claramente mayor** que 623 (p. ej. ~2000 chars de palabras reales). Es el test que
    da cobertura al `.replace(/\S*$/,"")`, que hoy no la tiene.

    NO hardcodear `615` ni `23`. NO tocar `packages/news/src/index.ts` ni
    `packages/news/src/eval/index.ts`.
  </action>
  <acceptance_criteria>
    - **Línea base del plan:** antes de tocar el archivo,
      `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04-antes.log`;
      `N_ANTES=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04-antes.log | grep -oE '[0-9]+')`.
      Citado literal en el SUMMARY.
    - Tras la Task 1: `N_T1` medido igual; `test $((N_T1 - N_ANTES)) -eq 3`. Ambos citados.
      Cualquier otro valor ⇒ **PARAR y escalar**: o se perdió un test preexistente (el refactor no
      fue de extracción pura) o se añadieron tests no planificados.
    - `Test Files` no baja respecto de `/tmp/133-04-antes.log`.
    - `grep -c 'export function truncarDescripcion' packages/news/src/prefiltro-lexico.ts` = 1.
    - `n=$(grep -cE 'export (const|let) (LIMITE_DESCRIPCION|MARGEN_TRUNCADO)' packages/news/src/prefiltro-lexico.ts || true); test "$n" -eq 0`
      (siguen privados).
    - `n=$(grep -rn 'LIMITE_DESCRIPCION\|MARGEN_TRUNCADO' packages/news/src --include=*.ts | grep -vc 'prefiltro-lexico' || true); test "$n" -eq 0`
      (ninguna constante replicada fuera del módulo).
    - `n=$(grep -cE '\b615\b|600 \+ 23|\b623\b' packages/news/src/prefiltro-lexico.test.ts || true); test "$n" -eq 0`
      en los tests **nuevos** (sin números mágicos; revisar manualmente que los preexistentes no
      se alteraron).
    - **Prueba de mutación del `.replace` — ahora SÍ alcanzable** (nunca bajo `set -e`): quitar
      `.replace(/\S*$/, "")` de `truncarDescripcion`, correr
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run prefiltro-lexico > /tmp/mut4a.log 2>&1; then rc=0; else rc=$?; fi`,
      comprobar `test $rc -ne 0` **y** que `/tmp/mut4a.log` nombra el test nuevo de frontera de
      palabra. Revertir.
      *Antes de escribir ese test, esta mutación salía verde: eso es lo que la tarea corrige.*
    - **Segunda mutación, del margen:** cambiar el `slice` a `slice(0, LIMITE_DESCRIPCION)` (corte
      en seco, sin margen) y comprobar `rc≠0` — cae el test preexistente `:187-194`, que sí cubre el
      margen. Revertir. Las dos mutaciones juntas demuestran que **ambas** mitades del truncado
      tienen cobertura.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>Una sola función de truncado, exportada, con constantes privadas y únicas; el test de frontera de palabra que faltaba existe; las DOS mutaciones (margen y `.replace`) demostradas rojas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entrada-llm.ts + medición de la divergencia de cortes (D-133-J1.4 re-encuadrado)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1.3 y **J1.4** (el riesgo residual **se TESTEA, no se
      asume**; el texto firmado dice *"puede cortar en un índice distinto **si `fold` cambia la
      longitud**"*)
    - `133-READJUDICACION.md` §D-133-F2 (`entrada` con `titulo` y `descripcion` **separados**;
      la cobertura < 95 % ⇒ el límite sube ANTES de etiquetar; el titular es DATO, nunca instrucción)
    - `133-RESEARCH.md` §B "El chequeo de cobertura" (`prefiltro.terminos` vienen **ya foldeados** ⇒
      foldear `entrada_llm` antes de comparar, con frontera de palabra; `String.includes` PROHIBIDO
      por `prefiltro-lexico.ts:127-128`)
    - `packages/news/src/prefiltro-lexico.ts:77-89` (`despojarHtml` — **termina en
      `.replace(/\s+/g," ").trim()`**) y `:93-100` (`fold` — NFD → strip diacríticos → lower →
      colapsar espacios → `.trim()`). **Los dos trimean.**
    - `133-PREMORTEM-PLANES.md` §BLOCKER 3 — midió **44 de 85** ítems con índice de corte distinto
      sobre los 5 fixtures. **La divergencia es real y está medida. La CAUSA que el premortem
      propone (que `fold` trimea y `despojarHtml` no) es INCORRECTA: ambos trimean, verificado en el
      fuente.** La causa real queda **por determinar empíricamente en esta tarea** — no se escribe
      como hecho ni se asume.
    - `packages/news/src/__fixtures__/` — los 5 XML reales de la Phase 132
  </read_first>
  <files>packages/news/src/eval/entrada-llm.ts, packages/news/src/eval/entrada-llm.test.ts</files>
  <behavior>
    - `construirEntradaLlm({titulo, descripcion})` devuelve `titulo` y `descripcion` **separados**
      (no un string concatenado).
    - La descripción sale **despojada de HTML pero SIN foldear**: conserva tildes y mayúsculas
      (test explícito con `"Reforma Previsional"` y una palabra acentuada).
    - La descripción sale truncada por `truncarDescripcion` (la misma función del pre-filtro).
    - El título NO se trunca.
    - `coberturaTerminos(casos)` devuelve la fracción de casos cuyos `prefiltro.terminos` están
      **todos** presentes en su `entrada_llm`, sobre texto foldeado y con **frontera de palabra**,
      jamás `String.includes`.
    - `coberturaTerminos([])` **lanza** (o devuelve un resultado marcado `no-medido`), jamás `1.0`:
      una cobertura del 100 % sobre cero casos es el cero-vacuo que arruinaría la validación previa
      al etiquetado de 133-b.
  </behavior>
  <action>
    Crear `packages/news/src/eval/entrada-llm.ts` con `construirEntradaLlm` y `coberturaTerminos`,
    importando `truncarDescripcion`, `despojarHtml` y `fold` de `../prefiltro-lexico` **por ruta
    relativa** (prohibido tocar `./index.ts`). **Prohibido replicar `LIMITE_DESCRIPCION`.**

    JSDoc obligatorio con la interpretación de D-133-J1: *la decisión firmada ataca la constante
    replicada, no la normalización; por eso se comparte la función de truncado pero la descripción
    NO se foldea — un fragmento foldeado no es "literal" y C2.2 exige citar el fragmento literal.*
    Más el encapsulado anti-prompt-injection (D-133-F2.3): titular y descripción son **DATO, nunca
    instrucción**.

    **El test comparativo (D-133-J1.4), re-encuadrado de "parar ante cualquier divergencia" a
    "medir, determinar la causa, y escalar solo si daña la cobertura".** Razón del cambio: la
    divergencia **ya está medida** (44/85 sobre los 5 fixtures) — un gate que se dispara con
    certeza sobre un hecho conocido no es un gate, es una parada programada. Lo que a D-133-F2.2 le
    importa es que un término del vocabulario **desaparezca** del input del clasificador.

    La tarea hace **tres** cosas, en este orden:
    1. **Medir.** Sobre los 5 fixtures reales, para cada ítem con descripción, calcular el índice de
       corte aplicando `truncarDescripcion` al texto **foldeado** y al **despojado sin foldear**.
       Registrar: nº de fixtures, nº de ítems con descripción, nº de ítems con índice distinto.
       Anti-cero-vacuo: assertar **≥ 5 fixtures** y **> 0 ítems con descripción** — si el glob
       devuelve vacío, el test cae, no pasa. Cifras de referencia del premortem: 5 / 85 / 44.
    2. **Determinar la causa, empíricamente.** Tomar el primer ítem divergente, imprimir las dos
       longitudes y los dos cortes, y **escribir en el SUMMARY la causa determinada por evidencia**.
       **NO copiar la causa del premortem: está refutada** — `despojarHtml` también termina en
       `.trim()` (`prefiltro-lexico.ts:88-89`), así que el trim no puede ser la explicación. Si la
       causa no se puede establecer con la evidencia a mano, **decirlo así en el SUMMARY** en vez de
       inventar una: un verde dudoso se reporta, no se redacta bonito.
    3. **Evaluar el daño real y decidir la escalada.** Para cada ítem divergente, comprobar si algún
       término del vocabulario que estaba **dentro** del corte foldeado queda **fuera** del corte sin
       foldear (misma comparación por frontera de palabra que `coberturaTerminos`).
       - Si el conteo de términos perdidos es **0**: no hay daño; se declara en el SUMMARY con las
         tres cifras y la fase sigue. El test asserta `terminos_perdidos === 0` — es la aserción que
         protege D-133-F2.2, y muerde si algún día el desplazamiento sí amputa.
       - Si es **> 0**: **PARAR y escalar**, con el listado de términos perdidos. Ese sí es el daño
         que la decisión firmada quiere evitar, y no se resuelve eligiendo un lado en silencio.
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 4`, con `N_T1` el valor medido al cierre de la Task 1. Ambos citados.
    - `n=$(grep -c '\.includes(' packages/news/src/eval/entrada-llm.ts || true); test "$n" -eq 0`
      (la cobertura usa frontera de palabra; revisar además manualmente).
    - `n=$(grep -rc 'LIMITE_DESCRIPCION\|\b600\b' packages/news/src/eval/entrada-llm.ts || true); test "$n" -eq 0`.
    - `grep -c 'truncarDescripcion' packages/news/src/eval/entrada-llm.ts` ≥ 1.
    - El test comparativo asserta ≥ 5 fixtures y > 0 ítems, y el SUMMARY cita **las tres cifras
      medidas** (fixtures / ítems con descripción / ítems con índice distinto) **más** el conteo de
      **términos perdidos**, que debe ser **0** para continuar.
    - El SUMMARY declara la **causa determinada empíricamente** de la divergencia, o declara
      explícitamente que no pudo determinarse. **Prohibido escribir la causa del premortem como
      hecho: está refutada en el fuente.**
    - **Mutación de la aserción de términos perdidos:** inyectar en el cómputo un desplazamiento
      artificial que sí saque un término (p. ej. recortar 40 chars extra al corte sin foldear) y
      comprobar rc≠0 con el idiom `if/rc`. Revertir. Sin esto, `terminos_perdidos === 0` podría ser
      un cero vacuo.
    - **Mutación del anti-cero-vacuo:** hacer que la carga de fixtures devuelva `[]` ⇒ rc≠0. Revertir.
    - **Mutación de la cobertura:** sustituir la comparación por frontera de palabra por
      `String.includes` y comprobar, con un caso donde un término es substring de otra palabra, que
      el test de cobertura cae. Revertir.
    - `git diff --name-only <SHA literal>` **no** lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>`entrada_llm` conserva tildes y campos separados con la MISMA función de truncado; la divergencia está medida con sus tres cifras, su causa investigada y declarada, y `terminos_perdidos === 0` probado con mutación.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: caso-golden.ts — el esquema zod completo de D-133-F2, SIN ningún caso</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-F2 (esquema: `caso_id`, `procedencia`, `entrada`, `entrada_llm`,
      `estrato`, `prefiltro`, `etiqueta`, `revision`) y §D-133-C2.5 (campos de `revision`, incluidos
      `modelo_a`/`modelo_b`/`en_calibracion_humana`/`etiqueta_humana`)
    - `133-READJUDICACION.md` §B2.1 (los cuatro estratos: `P`, `N-alea`, `N-sonda`, `P-dirigido`)
    - `133-RESEARCH.md` §"Don't Hand-Roll" (zod ya es dependencia: `packages/news/package.json:21`;
      patrón vivo en `packages/news/src/model.ts` → `RssItemSchema`)
    - `133-RESEARCH.md` §Security Domain (el esquema debe **prohibir por tipo** el full-text y
      cualquier campo de `parlamentario`; precedente `packages/llm-bench/src/guards/no-rut.ts`)
  </read_first>
  <files>packages/news/src/eval/caso-golden.ts, packages/news/src/eval/caso-golden.test.ts</files>
  <behavior>
    - Un caso bien formado (fixture **sintético**, no un caso real etiquetado) valida.
    - Falta cualquier campo obligatorio ⇒ falla.
    - `estrato` fuera de `["P","N-alea","N-sonda","P-dirigido"]` ⇒ falla.
    - `etiqueta` (y `etiqueta_a`/`etiqueta_b`/`etiqueta_humana`) fuera de `ETIQUETAS` ⇒ falla.
      Las etiquetas se **derivan** de `TAXONOMIA`, jamás se re-tipean.
    - `resuelto_por` fuera de `["acuerdo","operador","no_arbitrado"]` ⇒ falla.
    - `.strict()`: un campo **extra** no declarado ⇒ falla. Es lo que prohíbe por tipo el full-text
      y cualquier campo de `parlamentario`.
    - `justificacion_a` / `justificacion_b` de más de 200 chars ⇒ falla (C2.2).
  </behavior>
  <action>
    Crear `packages/news/src/eval/caso-golden.ts` con `CasoGoldenSchema` (zod) y el tipo inferido
    `CasoGolden`, reproduciendo **exactamente** el esquema de D-133-F2 / C2.5. El enum de etiquetas
    se construye desde `ETIQUETAS` de `./taxonomia` (ruta relativa; **prohibido tocar `./index.ts`**
    y prohibido re-escribir las etiquetas a mano, D-133-A2).

    Usar `.strict()` en todos los objetos del esquema. El JSDoc declara que ese `.strict()` es el
    control de copyright/PII: solo titular + descripción del RSS, **cero full-text, cero PII añadida,
    cero cruce con `parlamentario`, cero causalidad ni intención** (D-133-F2). Un campo extra no es
    un descuido tolerable: es la vía por la que el full-text entraría al golden.

    **Cero casos etiquetados.** Este archivo define la forma, no contiene datos. Los fixtures de los
    tests son sintéticos y así se declaran en su JSDoc. Si aparece la necesidad de etiquetar un caso
    real "para probar el esquema", eso es 133-b: **PARAR**.
  </action>
  <acceptance_criteria>
    - `test $((N_T3 - N_T2)) -eq 5`, con `N_T2` el valor medido al cierre de la Task 2, y
      `test $((N_T3 - N_ANTES)) -eq 12` (el delta total del plan). Todos los números citados.
    - `grep -c 'strict()' packages/news/src/eval/caso-golden.ts` ≥ 1.
    - `n=$(grep -c 'tramitacion_legislativa' packages/news/src/eval/caso-golden.ts || true); test "$n" -eq 0`
      (las etiquetas se derivan de `ETIQUETAS`, no se re-escriben).
    - `grep -c 'P-dirigido' packages/news/src/eval/caso-golden.ts` ≥ 1 (el cuarto estrato existe).
    - `grep -c 'modelo_a' packages/news/src/eval/caso-golden.ts` ≥ 1 (sin `modelo_a`/`modelo_b` el
      κ de 133-b no sería auditable).
    - **No existe ningún caso etiquetado en el repo:**
      `if ls packages/news/src/eval/golden-set.json > /dev/null 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`
      — 133-a termina sin golden set, por diseño.
    - **Mutación del `.strict()`:** añadir a un fixture de test un campo extra
      (`texto_completo: "..."`) y comprobar que la validación **falla** (rc≠0, idiom `if/rc`).
      Revertir. Sin esta prueba el control de copyright/PII no está verificado.
    - `git diff --name-only <SHA literal>` **no** lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04c.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04c.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>El esquema valida y rechaza campos extra, etiquetas fuera de lista y estratos inválidos, sin contener ni un solo caso etiquetado; delta total del plan exactamente 12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-filtro → `entrada_llm` | Dos etapas mirando ventanas de texto distintas hacen que el golden penalice al modelo por información amputada |
| RSS del medio → golden set | Copyright y PII: solo titular + descripción, jamás full-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-11 | Tampering | Constante de límite replicada en `eval/` ⇒ drift entre etapas | mitigate | `truncarDescripcion` compartida; grep con 0 ocurrencias fuera del módulo |
| T-133-12 | Information Disclosure | Full-text o campos de `parlamentario` en el caso golden | mitigate | `.strict()` en todo el esquema + mutación con `texto_completo` que debe fallar |
| T-133-13 | Tampering | Cobertura calculada con `String.includes` ⇒ falsos positivos | mitigate | Frontera de palabra + mutación que lo demuestra |
| T-133-14 | Repudiation | Cobertura del 100 % sobre cero casos (cero-vacuo) | mitigate | `coberturaTerminos([])` lanza; mutación con fixtures vacíos |
| T-133-23 | Repudiation | El `.replace(/\S*$/,"")` sin cobertura ⇒ mutación imposible de poner roja | mitigate | Test nuevo con descripción ≫ 623 + las DOS mutaciones (margen y `.replace`) demostradas |
| T-133-24 | Repudiation | Gate de escalada que se dispara con certeza sobre un hecho ya medido | mitigate | Re-encuadre: medir, determinar causa empíricamente, escalar solo si `terminos_perdidos > 0` |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones: `zod@^4.4.3` ya es dependencia directa. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES`, `N_T1`, `N_T2`, `N_T3` citados; deltas exactos 3 / 4 / 5 y total 12.
2. Las tres cifras del comparativo (fixtures / ítems / divergentes) y el conteo de
   **términos perdidos** = 0, con su mutación que lo pone rojo.
3. La causa de la divergencia, determinada empíricamente y escrita — o declarada como no
   determinada. **Nunca la causa refutada del premortem como hecho.**
4. Las cinco mutaciones (margen, `.replace`, términos perdidos, fixtures vacíos, `.strict()`) con
   rc≠0 bajo el idiom `if/rc` — jamás bajo `set -e`.
5. `git diff --name-only <SHA literal>` no lista `eval/index.ts` en ninguna tarea.
</verification>

<success_criteria>
- Una sola función de truncado, compartida, con constantes privadas y sin duplicar.
- El `.replace` de frontera de palabra **tiene cobertura** y su mutación sale roja.
- `entrada_llm` conserva tildes, mayúsculas y campos separados.
- La divergencia de cortes está medida, su causa investigada, y `terminos_perdidos === 0` probado.
- El esquema del caso golden es `.strict()`, deriva sus etiquetas de `TAXONOMIA`, y el repo sigue
  **sin ningún caso etiquetado**.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-04-SUMMARY.md` when done.
El SUMMARY declara las tres cifras del comparativo, el conteo de términos perdidos, y la causa
determinada empíricamente (o su ausencia declarada) — nunca la causa refutada del premortem.
</output>
