---
phase: 133-news-taxo
plan: 04
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - packages/news/src/prefiltro-lexico.ts
  - packages/news/src/prefiltro-lexico.test.ts
  - packages/news/src/eval/entrada-llm.ts
  - packages/news/src/eval/entrada-llm.test.ts
  - packages/news/src/eval/caso-golden.ts
  - packages/news/src/eval/caso-golden.test.ts
  - packages/news/src/eval/index.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Existe UNA sola función de truncado; LIMITE_DESCRIPCION y MARGEN_TRUNCADO siguen privados y sin duplicar"
    - "El pre-filtro se comporta exactamente igual que antes (diff-cero demostrado por conteo impreso)"
    - "entrada_llm conserva tildes, mayúsculas, y titulo/descripcion separados"
    - "El esquema del caso golden existe y valida, sin ningún caso etiquetado"
    - "El chequeo de cobertura de prefiltro.terminos existe y usa frontera de palabra, no String.includes"
  artifacts:
    - path: "packages/news/src/eval/caso-golden.ts"
      provides: "Esquema zod del caso golden (D-133-F2), SIN casos"
      exports: ["CasoGoldenSchema", "type CasoGolden"]
    - path: "packages/news/src/eval/entrada-llm.ts"
      provides: "construirEntradaLlm + cobertura de prefiltro.terminos"
      exports: ["construirEntradaLlm", "coberturaTerminos"]
  key_links:
    - from: "packages/news/src/eval/entrada-llm.ts"
      to: "packages/news/src/prefiltro-lexico.ts"
      via: "import de truncarDescripcion (no constante replicada)"
      pattern: "truncarDescripcion"
---

<objective>
D-133-F2 + D-133-J1: una sola función de truncado compartida entre el pre-filtro y `entrada_llm`,
el esquema del caso golden (sin ningún caso), y el chequeo de cobertura que debe correrse **antes**
de etiquetar.

Purpose: `entrada_llm` truncada a 300 mientras el pre-filtro decide sobre 600 hacía que
`prefiltro.terminos` pudiera citar un término **ausente** del input del clasificador — el golden
penalizaría al modelo por información amputada. Y una constante copiada es la deuda de ICS en
miniatura.
Output: `truncarDescripcion` exportada, `entrada-llm.ts`, `caso-golden.ts` y sus tests.

**133-a NO etiqueta ni un solo caso.** Aquí solo se construyen el esquema y la función; correrlos
sobre casos P es 133-b.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer truncarDescripcion() con diff-cero de comportamiento demostrable</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1 completo (los 4 puntos: extraer como export,
      `construirTexto` la llama, `entrada_llm` la usa sin foldear, y el riesgo residual que se TESTEA)
    - `133-RESEARCH.md` §B completo (tabla de exports; la secuencia exacta despojo→fold→truncado→
      concatenación; `MARGEN_TRUNCADO` es **runtime-derivado** — jamás hardcodear 615 ni 23)
    - `packages/news/src/prefiltro-lexico.ts` íntegro (146 líneas), en particular `:50`
      (`LIMITE_DESCRIPCION = 600`), `:107` (`MARGEN_TRUNCADO`), `:109-123` (`construirTexto`),
      `:127-128` (`String.includes` PROHIBIDO), `:6-9` (prohibición LOCKED de podar el vocabulario)
    - `packages/news/src/prefiltro-lexico.test.ts:188-192` (test de frontera de palabra que ejercita
      el corte a 600)
    - `133-RESEARCH.md` §A (el barrel `packages/news/src/index.ts` es aditivo-seguro:
      `run-news-cli.test.ts:581-604` usa lista blanca + `toBeGreaterThanOrEqual(8)`)
  </read_first>
  <files>packages/news/src/prefiltro-lexico.ts, packages/news/src/prefiltro-lexico.test.ts</files>
  <behavior>
    - `truncarDescripcion(s)` sobre un string más corto que el límite lo devuelve intacto.
    - Sobre un string más largo, corta en **frontera de palabra** (no deja una palabra partida).
    - `construirTexto` (privada) sigue produciendo exactamente el mismo string que antes para los
      casos de la suite existente — el título nunca se trunca, la descripción sí.
    - El test existente de frontera de palabra (`prefiltro-lexico.test.ts:188-192`) sigue verde.
  </behavior>
  <action>
    En `packages/news/src/prefiltro-lexico.ts`, extraer la lógica de truncado de `:119-121` a una
    función pura **exportada**:
    `export function truncarDescripcion(texto: string): string` — hace el `slice` a
    `LIMITE_DESCRIPCION + MARGEN_TRUNCADO` y el `.replace(/\S*$/, "")` de corte en frontera de
    palabra. **`LIMITE_DESCRIPCION` y `MARGEN_TRUNCADO` siguen privados del módulo y sin duplicar
    en ningún otro archivo** — eso es exactamente lo que D-133-F2.1 protege.

    Reescribir `construirTexto` (`:118-121`) para que **llame** a `truncarDescripcion`. No cambiar
    ninguna otra cosa: ni el orden despojo→fold, ni la concatenación `` `${t} ${d}` ``, ni el hecho
    de que el título no se trunca. Este es un refactor de extracción puro.

    NO hardcodear `615` ni `23` en ningún test: `MARGEN_TRUNCADO` se deriva en runtime del término
    más largo de `VOCABULARIO_LEGISLATIVO` y cambiará si el vocabulario se amplía (lo cual está
    permitido; podarlo está PROHIBIDO por `:6-9`). Los tests deben derivar el límite igual que el
    módulo, o probar propiedades (frontera de palabra, longitud acotada) en vez de números mágicos.

    NO tocar el barrel `packages/news/src/index.ts` en este plan (ningún plan de 133-a lo toca; así
    no hay conflicto de archivo entre waves). `truncarDescripcion` se importa por ruta relativa
    desde `eval/entrada-llm.ts`.
  </action>
  <acceptance_criteria>
    - **Diff-cero, con los dos números capturados.** Antes de tocar el archivo, ejecutar
      `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04-antes.log` y extraer
      `N_ANTES` con `grep -oE 'Tests[^0-9]+[0-9]+ passed'`. Después de la extracción, ejecutar lo
      mismo a `/tmp/133-04-despues.log` → `N_DESPUES`. El criterio exige que **ambos números estén
      citados** y que `N_DESPUES == N_ANTES + (tests nuevos de truncarDescripcion)`, con **cero**
      tests perdidos (`Test Files X passed` sin bajar). Si algún test preexistente cae, el refactor
      NO fue de extracción pura: **PARAR y escalar**.
    - `grep -c 'export function truncarDescripcion' packages/news/src/prefiltro-lexico.ts` = 1.
    - `grep -c 'export const LIMITE_DESCRIPCION\|export const MARGEN_TRUNCADO' packages/news/src/prefiltro-lexico.ts` = **0**
      (siguen privados).
    - `grep -rn 'LIMITE_DESCRIPCION\|MARGEN_TRUNCADO' packages/news/src --include=*.ts | grep -v 'prefiltro-lexico' | wc -l` = **0**
      (ninguna constante replicada fuera del módulo).
    - `grep -cE '\b615\b|\b600 \+ 15\b' packages/news/src/prefiltro-lexico.test.ts` = 0 (sin números mágicos).
    - Prueba de mutación: cambiar `.replace(/\S*$/, "")` por nada (corte en seco), correr
      `if pnpm --filter @obs/news exec vitest run prefiltro-lexico > /tmp/mut4.log 2>&1; then rc=0; else rc=$?; fi`,
      comprobar `test $rc -ne 0` y que el log nombra el test de frontera de palabra. Revertir.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04-despues.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04-despues.log</automated>
  </verify>
  <done>Existe una sola función de truncado, exportada; las constantes siguen privadas y únicas; `N_ANTES` y `N_DESPUES` citados y el diff-cero demostrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entrada-llm.ts + el test comparativo de índices de corte sobre los 5 fixtures</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1.3 y **J1.4** (el riesgo residual que se TESTEA;
      *"si difieren en algún fixture, el ejecutor PARA y escala — no elige un lado en silencio"*)
    - `133-READJUDICACION.md` §D-133-F2 (esquema: `entrada` con `titulo` y `descripcion`
      **separados**; `entrada_llm`; el chequeo de cobertura < 95 % ⇒ el límite sube ANTES de
      etiquetar; el encapsulado anti-prompt-injection: el titular es DATO, nunca instrucción)
    - `133-RESEARCH.md` §B "El chequeo de cobertura" (`prefiltro.terminos` vienen **ya foldeados**
      ⇒ hay que foldear `entrada_llm` antes de comparar, y usar frontera de palabra —
      `String.includes` está PROHIBIDO por `prefiltro-lexico.ts:127-128`)
    - `packages/news/src/prefiltro-lexico.ts:77` (`despojarHtml`), `:93-100` (`fold`), `:142`
      (`terminosQueMatchean`) — los tres exportados
    - `packages/news/src/__fixtures__/` — los 5 XML reales capturados en la Phase 132
  </read_first>
  <files>packages/news/src/eval/entrada-llm.ts, packages/news/src/eval/entrada-llm.test.ts, packages/news/src/eval/index.ts</files>
  <behavior>
    - `construirEntradaLlm({titulo, descripcion})` devuelve un objeto con `titulo` y `descripcion`
      **separados** (no un string concatenado).
    - La descripción sale **despojada de HTML pero SIN foldear**: conserva tildes y mayúsculas.
      Test explícito: una descripción con `"Reforma Previsional"` sale con la R y la P mayúsculas y
      con las tildes intactas.
    - La descripción sale truncada por `truncarDescripcion` (la misma función del pre-filtro).
    - El título NO se trunca (igual que en el pre-filtro).
    - `coberturaTerminos(casos)` devuelve la fracción de casos cuyos `prefiltro.terminos` están
      **todos** presentes en su `entrada_llm`, comparando **sobre texto foldeado y con frontera de
      palabra**, jamás con `String.includes`.
    - `coberturaTerminos([])` sobre lista vacía **lanza** (o devuelve un resultado marcado
      `no-medido`), jamás `1.0` — una cobertura del 100 % sobre cero casos es el cero-vacuo exacto
      que arruinaría la validación previa al etiquetado de 133-b.
  </behavior>
  <action>
    Crear `packages/news/src/eval/entrada-llm.ts` con `construirEntradaLlm` y `coberturaTerminos`,
    importando `truncarDescripcion`, `despojarHtml` y `fold` de `../prefiltro-lexico` por ruta
    relativa. **Prohibido replicar `LIMITE_DESCRIPCION`** — es la deuda de ICS en miniatura.

    El JSDoc debe declarar explícitamente la interpretación de D-133-J1: *la decisión firmada ataca
    la constante replicada, no la normalización; por eso se comparte la función de truncado pero la
    descripción NO se foldea — un fragmento foldeado no es "literal" y C2.2 exige citar el fragmento
    literal del titular o la bajada.*

    Añadir el encapsulado anti-prompt-injection (D-133-F2.3): el JSDoc y la forma del objeto dejan
    claro que titular y descripción son **DATO, nunca instrucción**.

    **Test comparativo de índices de corte (D-133-J1.4) — el punto de escalada de este plan.**
    Escribir un test que, sobre los **5 fixtures reales** de `packages/news/src/__fixtures__/*.xml`,
    para cada ítem con descripción: calcula el índice de corte aplicando `truncarDescripcion` al
    texto **foldeado** y al texto **despojado pero sin foldear**, y compara ambos índices.
    Anti-cero-vacuo obligatorio: el test asserta que procesó **≥ 5 fixtures** y **> 0 ítems con
    descripción** — si el glob de fixtures devuelve vacío, el test debe caer, no pasar.

    **Si los índices difieren en CUALQUIER fixture, el ejecutor PARA y escala.** No elige un lado,
    no ajusta el test, no "documenta la diferencia y sigue". El resultado (coincidencia o
    divergencia) se declara literalmente en el SUMMARY en ambos casos.

    Añadir los exports a `packages/news/src/eval/index.ts`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04b.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04b.log` imprime un número mayor que
      `N_DESPUES` de la Task 1 (ambos citados en el SUMMARY).
    - `grep -c 'String.includes\|\.includes(' packages/news/src/eval/entrada-llm.ts` = **0** en el
      camino del chequeo de cobertura (verificar manualmente que usa frontera de palabra).
    - `grep -rn 'LIMITE_DESCRIPCION\|600' packages/news/src/eval/entrada-llm.ts | wc -l` = 0.
    - `grep -c 'truncarDescripcion' packages/news/src/eval/entrada-llm.ts` ≥ 1.
    - El test comparativo existe, imprime o asserta el conteo de fixtures procesados
      (**≥ 5**) y de ítems con descripción (**> 0**), y su resultado se cita literalmente en el
      SUMMARY: `índices de corte idénticos en los 5 fixtures` **o** el detalle de la divergencia
      con la escalada abierta.
    - Prueba de mutación de la cobertura: sustituir la comparación por frontera de palabra por un
      `String.includes` y comprobar con un fixture donde un término es substring de otra palabra
      que el test de cobertura cae (`if ... ; then rc=0; else rc=$?; fi; test $rc -ne 0`). Revertir.
    - Prueba de mutación del anti-cero-vacuo: hacer que la carga de fixtures devuelva `[]` y
      comprobar rc≠0. Revertir.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04b.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04b.log</automated>
  </verify>
  <done>`entrada_llm` conserva tildes y campos separados usando la MISMA función de truncado; el test comparativo sobre los 5 fixtures corrió y su resultado está declarado; la cobertura usa frontera de palabra y no puede dar 100 % sobre cero casos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: caso-golden.ts — el esquema zod completo de D-133-F2, SIN ningún caso</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-F2 (el esquema completo: `caso_id`, `procedencia`, `entrada`,
      `entrada_llm`, `estrato`, `prefiltro`, `etiqueta`, `revision`) y §D-133-C2.5 (los campos de
      `revision`, incluidos `modelo_a`/`modelo_b`/`en_calibracion_humana`/`etiqueta_humana`)
    - `133-READJUDICACION.md` §B2.1 (los cuatro estratos: `P`, `N-alea`, `N-sonda`, `P-dirigido`)
    - `133-RESEARCH.md` §"Don't Hand-Roll" (zod ya es dependencia: `packages/news/package.json:21`;
      patrón vivo en `packages/news/src/model.ts` → `RssItemSchema`)
    - `133-RESEARCH.md` §Security Domain (el esquema debe **prohibir por tipo** el full-text y
      cualquier campo de `parlamentario`; precedente `packages/llm-bench/src/guards/no-rut.ts`)
    - `packages/news/src/model.ts` (el estilo zod del paquete)
  </read_first>
  <files>packages/news/src/eval/caso-golden.ts, packages/news/src/eval/caso-golden.test.ts, packages/news/src/eval/index.ts</files>
  <behavior>
    - Un caso bien formado (fixture sintético, **no** un caso real etiquetado) valida.
    - Falta cualquier campo obligatorio del esquema ⇒ la validación falla.
    - `estrato` fuera de `["P","N-alea","N-sonda","P-dirigido"]` ⇒ falla.
    - `etiqueta` (y `etiqueta_a`/`etiqueta_b`/`etiqueta_humana`) fuera de `ETIQUETAS` de
      `taxonomia.ts` ⇒ falla. Las etiquetas se **derivan** de `TAXONOMIA`, jamás se re-tipean.
    - `resuelto_por` fuera de `["acuerdo","operador","no_arbitrado"]` ⇒ falla.
    - `.strict()`: un campo **extra** no declarado ⇒ falla. Esto es lo que prohíbe por tipo el
      full-text y cualquier campo de `parlamentario` colado por conveniencia.
    - `justificacion_a` / `justificacion_b` de más de 200 chars ⇒ falla (C2.2).
  </behavior>
  <action>
    Crear `packages/news/src/eval/caso-golden.ts` con `CasoGoldenSchema` (zod) y el tipo inferido
    `CasoGolden`, reproduciendo **exactamente** el esquema de D-133-F2 / C2.5. El enum de etiquetas
    se construye desde `ETIQUETAS` de `./taxonomia` — **prohibido re-escribir las etiquetas a mano
    en ningún otro lugar** (D-133-A2).

    Usar `.strict()` en todos los objetos del esquema. El JSDoc debe declarar que ese `.strict()`
    es el control de copyright/PII: solo titular + descripción del RSS, **cero full-text, cero PII
    añadida, cero cruce con `parlamentario`, cero causalidad ni intención** (D-133-F2). Un campo
    extra no es un descuido tolerable: es la vía por la que el full-text entraría al golden.

    **Cero casos etiquetados.** Este archivo define la forma, no contiene datos. Los fixtures de los
    tests son sintéticos y así se declaran en su JSDoc. Si el ejecutor siente la necesidad de
    etiquetar un caso real "para probar el esquema", eso es 133-b: **PARAR**. El RSS no da histórico
    y hoy solo existe 1 día en R2.

    Añadir los exports a `packages/news/src/eval/index.ts`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04c.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04c.log` imprime un número mayor que el de la
      Task 2 (ambos citados).
    - `grep -c 'strict()' packages/news/src/eval/caso-golden.ts` ≥ 1.
    - `grep -c 'tramitacion_legislativa' packages/news/src/eval/caso-golden.ts` = **0**
      (las etiquetas se derivan de `ETIQUETAS`, no se re-escriben).
    - `grep -cE 'P-dirigido' packages/news/src/eval/caso-golden.ts` ≥ 1 (el cuarto estrato existe).
    - `grep -rc 'modelo_a' packages/news/src/eval/caso-golden.ts` ≥ 1 (sin `modelo_a`/`modelo_b` el
      κ de 133-b no sería auditable).
    - **No existe ningún caso etiquetado en el repo**: `ls packages/news/src/eval/golden-set.json`
      falla (`if ls ... > /dev/null 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`) — 133-a
      termina sin golden set, por diseño.
    - Prueba de mutación del `.strict()`: añadir a un fixture de test un campo extra
      (`texto_completo: "..."`) y comprobar que la validación **falla** (rc≠0 con el idiom `if/rc`).
      Revertir. Sin esta prueba, el control de copyright/PII no está verificado.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04c.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04c.log</automated>
  </verify>
  <done>El esquema del caso golden valida y rechaza campos extra, etiquetas fuera de lista y estratos inválidos, sin contener ni un solo caso etiquetado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-filtro → `entrada_llm` | Dos etapas mirando ventanas de texto distintas hacen que el golden penalice al modelo por información amputada |
| RSS del medio → golden set | Copyright y PII: solo titular + descripción, jamás full-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-11 | Tampering | Constante de límite replicada en `eval/` ⇒ drift silencioso entre etapas | mitigate | `truncarDescripcion` exportada y compartida; grep que exige 0 ocurrencias de `LIMITE_DESCRIPCION` fuera del módulo |
| T-133-12 | Information Disclosure | Full-text o campos de `parlamentario` colados en el caso golden | mitigate | `.strict()` en todo el esquema zod + prueba de mutación con un campo `texto_completo` que debe fallar |
| T-133-13 | Tampering | Cobertura de términos calculada con `String.includes` ⇒ falsos positivos de cobertura | mitigate | Frontera de palabra (régimen `prefiltro-lexico.ts:127-128`) + mutación que lo demuestra |
| T-133-14 | Repudiation | Cobertura del 100 % sobre cero casos (cero-vacuo) valida un etiquetado que nunca se midió | mitigate | `coberturaTerminos([])` lanza; test de mutación con fixtures vacíos |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones: `zod@^4.4.3` ya es dependencia directa (`packages/news/package.json:21`). Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES` / `N_DESPUES` del refactor de `prefiltro-lexico.ts` citados; cero tests perdidos.
2. Resultado literal del test comparativo de índices de corte sobre los 5 fixtures, con el conteo
   de fixtures procesados. Divergencia ⇒ escalada, jamás una elección silenciosa.
3. Las cuatro pruebas de mutación (frontera de palabra en `construirTexto`, `String.includes` en la
   cobertura, fixtures vacíos, `.strict()` del esquema) → rc≠0, con el idiom `if/rc`.
4. `grep` de constantes replicadas fuera de `prefiltro-lexico.ts` = 0.
</verification>

<success_criteria>
- Una sola función de truncado, compartida, con las constantes privadas y sin duplicar.
- `entrada_llm` conserva tildes, mayúsculas y campos separados — y usa esa misma función.
- El test comparativo de índices corrió sobre los 5 fixtures y su resultado está declarado.
- El esquema del caso golden existe, es `.strict()`, deriva sus etiquetas de `TAXONOMIA`, y el repo
  sigue **sin ningún caso etiquetado**.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-04-SUMMARY.md` when done.
El SUMMARY declara literalmente el resultado del test comparativo de índices de corte (D-133-J1.4).
</output>
