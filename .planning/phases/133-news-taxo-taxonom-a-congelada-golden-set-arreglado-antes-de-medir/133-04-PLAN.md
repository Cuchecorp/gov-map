---
phase: 133-news-taxo
plan: 04
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - packages/news/src/prefiltro-lexico.ts
  - packages/news/src/prefiltro-lexico.test.ts
  - packages/news/src/eval/entrada-llm.ts
  - packages/news/src/eval/entrada-llm.test.ts
  - packages/news/src/eval/caso-golden.ts
  - packages/news/src/eval/caso-golden.test.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "Existe UNA sola función de truncado; LIMITE_DESCRIPCION y MARGEN_TRUNCADO siguen privados y sin duplicar"
    - "El pre-filtro se comporta igual que antes: los tests preexistentes no bajan y el delta es fijo"
    - "entrada_llm conserva tildes, mayúsculas, y titulo/descripcion separados"
    - "El esquema del caso golden valida y rechaza los SIETE comportamientos declarados, sin ningún caso etiquetado"
    - "La divergencia de cortes foldeado/sin-foldear está MEDIDA, con su causa determinada y su efecto sobre cobertura evaluado"
  artifacts:
    - path: "packages/news/src/eval/caso-golden.ts"
      provides: "Esquema zod del caso golden (D-133-F2), SIN casos"
      exports: ["CasoGoldenSchema", "type CasoGolden"]
    - path: "packages/news/src/eval/entrada-llm.ts"
      provides: "construirEntradaLlm + coberturaTerminos"
      exports: ["construirEntradaLlm", "coberturaTerminos"]
  key_links:
    - from: "packages/news/src/eval/entrada-llm.ts"
      to: "packages/news/src/prefiltro-lexico.ts"
      via: "import de truncarDescripcion (no constante replicada)"
      pattern: "truncarDescripcion"
---

<objective>
D-133-F2 + D-133-J1: una sola función de truncado compartida entre el pre-filtro y `entrada_llm`,
el esquema del caso golden (sin ningún caso), y el chequeo de cobertura que debe correrse **antes**
de etiquetar.

Purpose: `entrada_llm` truncada a 300 mientras el pre-filtro decide sobre 600 hacía que
`prefiltro.terminos` pudiera citar un término **ausente** del input del clasificador — el golden
penalizaría al modelo por información amputada. Y una constante copiada es la deuda de ICS en
miniatura.
Output: `truncarDescripcion` exportada, `entrada-llm.ts`, `caso-golden.ts` y sus tests.

**133-a NO etiqueta ni un solo caso.**

**PROHIBIDO tocar `packages/news/src/eval/index.ts`** — propiedad exclusiva del plan 133-01, y el
plan 133-03 corre en esta misma wave con `use_worktrees: true`. Importar por ruta relativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-PREMORTEM-PLANES.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md
</context>

<medicion>
## Idiom de medición — OBLIGATORIO

`grep -oE 'Tests[^0-9]+[0-9]+ passed'` **sin `NO_COLOR=1` sale rc=1 y no devuelve nada**. Verificado.

```bash
NO_COLOR=1 <comando de vitest> 2>&1 | tee /tmp/LOG.log
N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/LOG.log | grep -oE '[0-9]+')
```

**El criterio fuerte es el DELTA propio del plan**, no un `-gt 206` absoluto: tras la wave 1 la base
ya estará por encima de 206 y ese umbral podría salir verde sin que la tarea haga nada. Se conserva
`-gt 206` solo como piso secundario de sanidad.

**No** se usa aritmética contra 133-03: corre en esta misma wave y también sube el conteo, así que
dependería del orden de merge. `N_ANTES` se captura **dentro de este plan**, sobre su propio worktree.
</medicion>

<delta_de_tests>
## Delta FIJO de tests de este plan — reconciliado con las mutaciones

Cada mutación que exige "que caiga un test" necesita que ese test **exista y esté contado**. La
tabla está cruzada contra las `<action>` **y** contra las mutaciones de la misma tarea:

| Tarea | `it(...)` nuevos | Cuáles | Mutación que lo ejercita |
|---|---|---|---|
| Task 1 | **3** | (a) `truncarDescripcion deja intacto un texto más corto que el límite` · (b) `truncarDescripcion corta en frontera de palabra` (fixture de tamaño FIJO, ver acción) · (c) `truncarDescripcion respeta el margen derivado del vocabulario` | quitar `.replace(/\S*$/,"")` ⇒ cae (b) · `slice(0, LIMITE_DESCRIPCION)` ⇒ cae el preexistente `:187-194` |
| Task 2 | **5** | (a) `construirEntradaLlm conserva tildes y mayúsculas` · (b) `construirEntradaLlm no trunca el título` · (c) `comparativo de índices de corte sobre los 5 fixtures` + `terminos_perdidos === 0` · (d) `coberturaTerminos sobre lista vacía LANZA` · (e) **`coberturaTerminos no cuenta un término que es substring de otra palabra`** | (e) ← sustituir frontera de palabra por `String.includes` · (c) ← desplazamiento artificial **y** ← fixtures vacíos (el assert `≥5 fixtures` vive en (c), no en (d)) · (d) ← invocar `coberturaTerminos([])` sin que lance |
| Task 3 | **7** | (a) caso bien formado valida · (b) falta un obligatorio ⇒ falla · (c) `estrato` inválido ⇒ falla · (d) etiqueta fuera de `ETIQUETAS` ⇒ falla · (e) campo extra ⇒ falla (`.strict()`) · (f) **`resuelto_por` fuera del enum ⇒ falla** · (g) **`justificacion_a`/`_b` > 200 chars ⇒ falla** | (e) ← campo `texto_completo` |
| **Total** | **15** | | |

**(e) de la Task 2 existe porque sin él la mutación de `String.includes` no puede poner rojo nada:**
el único otro test de cobertura es el de lista vacía, que lanza igual con o sin frontera de palabra.
**(f) y (g) de la Task 3 existen porque cubren dos campos LOCKED** (`resuelto_por` de D-133-C2.5 y
el tope de 200 chars de D-133-C2.2): un delta que los dejara fuera obligaría al ejecutor a omitir
comportamientos firmados para cuadrar el número.

`it.each` PROHIBIDO en los tests nuevos. Criterio final: `test $((N_T3 - N_ANTES)) -eq 15`.
</delta_de_tests>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer truncarDescripcion() + escribir el test que HOY falta para el corte en frontera</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1 (los 4 puntos)
    - `133-RESEARCH.md` §B (tabla de exports; secuencia despojo→fold→truncado→concatenación;
      `MARGEN_TRUNCADO` es **runtime-derivado** — jamás hardcodear 615 ni 23)
    - `packages/news/src/prefiltro-lexico.ts` — verificado: `LIMITE_DESCRIPCION = 600` (`:50`),
      `MARGEN_TRUNCADO` (`:107`, vale **23** hoy: `"tribunal constitucional"`, 30 términos),
      `construirTexto` (`:109`), el truncado en `:120-121`, `String.includes` PROHIBIDO (`:127-128`),
      prohibición LOCKED de podar el vocabulario (`:6-9`)
    - `packages/news/src/prefiltro-lexico.test.ts:187-194` — **LEER CON ATENCIÓN.**
      Reproducido numéricamente: su descripción mide `623 = 600 + 23` **exactos**, así que el `slice`
      no corta nada y el `.replace(/\S*$/,"")` solo borra `"hoy"`. **Quitar el `.replace` deja ese
      test VERDE.** Cubre el *margen*, no el corte en frontera. **El `.replace` no tiene cobertura
      hoy** — por eso esta tarea la escribe.
  </read_first>
  <files>packages/news/src/prefiltro-lexico.ts, packages/news/src/prefiltro-lexico.test.ts</files>
  <behavior>
    - `truncarDescripcion(s)` con `s` más corto que el límite lo devuelve intacto.
    - Con el fixture de corte (ver acción), el resultado **no termina en una palabra partida**: su
      última palabra aparece completa en la entrada.
    - El margen se **deriva** del vocabulario: el test lo calcula igual que el módulo o prueba una
      propiedad; **jamás** hardcodea 615/23/623.
    - Los tests preexistentes de `prefiltro-lexico.test.ts` siguen verdes (extracción pura).
  </behavior>
  <action>
    En `packages/news/src/prefiltro-lexico.ts`, extraer la lógica de truncado de `:120-121` a
    `export function truncarDescripcion(texto: string): string` — `slice` a
    `LIMITE_DESCRIPCION + MARGEN_TRUNCADO` y `.replace(/\S*$/, "")`. **`LIMITE_DESCRIPCION` y
    `MARGEN_TRUNCADO` siguen privados del módulo y sin duplicar.**

    Reescribir `construirTexto` (`:118-121`) para que **llame** a `truncarDescripcion`. Nada más
    cambia. Refactor de extracción puro.

    **El fixture del test de corte en frontera tiene tamaño y forma FIJOS, no "aproximadamente".**
    Un fixture de tamaño libre puede caer justo en un espacio, y entonces quitar el `.replace`
    dejaría el test verde y **la mutación no mordería** — el mismo defecto que este test viene a
    corregir. Construcción obligatoria, derivada en runtime y sin números mágicos:
    - `limite = LIMITE_DESCRIPCION + MARGEN_TRUNCADO` (el test lo obtiene igual que el módulo, o el
      módulo exporta un helper de solo lectura para tests; **no** se escribe el número).
    - Prefijo: palabras cortas separadas por espacio hasta longitud `limite - 5`.
    - A continuación, **una palabra larga de 20 caracteres sin espacios** (p. ej. `"a".repeat(20)`
      o una palabra real larga). Esa palabra **empieza antes** del índice de corte y **termina
      después** ⇒ el corte cae **inequívocamente a mitad de palabra**.
    - Sufijo: más palabras, para que la entrada exceda holgadamente el límite.
    El test asserta que el resultado **no contiene** un fragmento parcial de esa palabra larga, y que
    su última palabra está completa en la entrada. Con el `.replace`, pasa; sin él, cae.

    NO hardcodear `615`, `23` ni `623`. NO tocar `packages/news/src/index.ts` ni
    `packages/news/src/eval/index.ts`.
  </action>
  <acceptance_criteria>
    - **Línea base del plan:** antes de tocar nada,
      `NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04-antes.log`;
      `N_ANTES=$(... | grep -oE '[0-9]+')`. Citado literal.
    - `test $((N_T1 - N_ANTES)) -eq 3`. Ambos citados. Cualquier otro valor ⇒ **PARAR y escalar**:
      o se perdió un test preexistente (el refactor no fue de extracción pura) o se añadieron tests
      no planificados.
    - `Test Files` no baja respecto de `/tmp/133-04-antes.log`.
    - `grep -c 'export function truncarDescripcion' packages/news/src/prefiltro-lexico.ts` = 1.
    - `n=$(grep -cE 'export (const|let) (LIMITE_DESCRIPCION|MARGEN_TRUNCADO)' packages/news/src/prefiltro-lexico.ts || true); test "$n" -eq 0`.
    - `n=$(grep -rn 'LIMITE_DESCRIPCION\|MARGEN_TRUNCADO' packages/news/src --include=*.ts | grep -vc 'prefiltro-lexico' || true); test "$n" -eq 0`
      (ninguna constante replicada fuera del módulo).
    - `n=$(grep -cE '\b615\b|600 \+ 23|\b623\b' packages/news/src/prefiltro-lexico.test.ts || true); test "$n" -eq 0`.
    - **Mutación A — `.replace`, ahora SÍ alcanzable** (nunca bajo `set -e`): quitar
      `.replace(/\S*$/, "")` de `truncarDescripcion`,
      `if NO_COLOR=1 pnpm --filter @obs/news exec vitest run prefiltro-lexico > /tmp/mut4a.log 2>&1; then rc=0; else rc=$?; fi`,
      `test $rc -ne 0` **y** el log nombra el test nuevo de frontera de palabra. Revertir.
      *Antes de escribir ese test con su fixture de forma fija, esta mutación salía verde.*
    - **Mutación B — el margen:** cambiar el `slice` a `slice(0, LIMITE_DESCRIPCION)` ⇒ rc≠0 (cae el
      preexistente `:187-194`, que sí cubre el margen). Revertir. Las dos juntas demuestran que
      **ambas** mitades del truncado tienen cobertura.
    - **Escalada escrita:** si la Mutación A **no** pone rojo, el fixture no está cruzando el índice
      de corte. El ejecutor **NO** declara el criterio cumplido ni ajusta el criterio: imprime el
      índice de corte real y los límites del fixture, corrige el fixture para que la palabra larga
      cruce el corte, y re-corre. Si tras dos intentos sigue verde, **PARAR y escalar**.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04a.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04a.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>Una sola función de truncado, exportada, con constantes privadas y únicas; el test de frontera con fixture de forma fija existe; las DOS mutaciones demostradas rojas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entrada-llm.ts + medición de la divergencia de cortes (D-133-J1.4 re-encuadrado)</name>
  <read_first>
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-J1.3 y **J1.4** (el riesgo residual **se TESTEA, no se
      asume**; el texto firmado dice *"puede cortar en un índice distinto **si `fold` cambia la
      longitud**"*)
    - `133-READJUDICACION.md` §D-133-F2 (`entrada` con `titulo` y `descripcion` **separados**; la
      cobertura < 95 % ⇒ el límite sube ANTES de etiquetar; el titular es DATO, nunca instrucción)
    - `133-RESEARCH.md` §B "El chequeo de cobertura" (`prefiltro.terminos` vienen **ya foldeados** ⇒
      foldear `entrada_llm` antes de comparar, con frontera de palabra; `String.includes` PROHIBIDO
      por `prefiltro-lexico.ts:127-128`)
    - `packages/news/src/prefiltro-lexico.ts:77-89` (`despojarHtml` — **termina en
      `.replace(/\s+/g," ").trim()`**) y `:93-100` (`fold` — NFD → strip diacríticos → lower →
      colapsar espacios → `.trim()`). **Los dos trimean.**
    - `133-PREMORTEM-PLANES.md` §BLOCKER 3 — midió **44 de 85** ítems con índice de corte distinto
      sobre los 5 fixtures. **La divergencia es real y está medida. La CAUSA que el premortem
      propone (que `fold` trimea y `despojarHtml` no) es INCORRECTA: ambos trimean, verificado en el
      fuente.** La causa real queda **por determinar empíricamente en esta tarea**.
    - `packages/news/src/__fixtures__/` — los 5 XML reales de la Phase 132
  </read_first>
  <files>packages/news/src/eval/entrada-llm.ts, packages/news/src/eval/entrada-llm.test.ts</files>
  <behavior>
    - `construirEntradaLlm({titulo, descripcion})` devuelve `titulo` y `descripcion` **separados**.
    - La descripción sale **despojada de HTML pero SIN foldear**: conserva tildes y mayúsculas.
    - La descripción sale truncada por `truncarDescripcion`. El título NO se trunca.
    - `coberturaTerminos(casos)` devuelve la fracción de casos cuyos `prefiltro.terminos` están
      **todos** presentes en su `entrada_llm`, sobre texto foldeado y con **frontera de palabra**.
    - **`coberturaTerminos` NO cuenta un término que aparece solo como substring de otra palabra**
      (p. ej. el término `sala` no se da por presente en `"antesala"`). Es el `it` que hace
      alcanzable la mutación de `String.includes`.
    - `coberturaTerminos([])` **lanza**, jamás devuelve `1.0`.
  </behavior>
  <action>
    Crear `packages/news/src/eval/entrada-llm.ts` con `construirEntradaLlm` y `coberturaTerminos`,
    importando `truncarDescripcion`, `despojarHtml` y `fold` de `../prefiltro-lexico` **por ruta
    relativa** (prohibido tocar `./index.ts`). **Prohibido replicar `LIMITE_DESCRIPCION`.**

    JSDoc obligatorio con la interpretación de D-133-J1: *la decisión firmada ataca la constante
    replicada, no la normalización; por eso se comparte la función de truncado pero la descripción
    NO se foldea — un fragmento foldeado no es "literal" y C2.2 exige citar el fragmento literal.*
    Más el encapsulado anti-prompt-injection (D-133-F2.3): titular y descripción son **DATO, nunca
    instrucción**.

    **El test comparativo (D-133-J1.4), re-encuadrado de "parar ante cualquier divergencia" a
    "medir, determinar la causa, y escalar solo si daña la cobertura".** La divergencia **ya está
    medida** (44/85): un gate que se dispara con certeza sobre un hecho conocido no es un gate, es
    una parada programada. Lo que a D-133-F2.2 le importa es que un término del vocabulario
    **desaparezca** del input del clasificador.

    Tres pasos, en orden:
    1. **Medir.** Sobre los 5 fixtures reales, para cada ítem con descripción, calcular el índice de
       corte con `truncarDescripcion` sobre el texto **foldeado** y sobre el **despojado sin
       foldear**. Registrar: nº de fixtures, nº de ítems con descripción, nº de ítems con índice
       distinto. Anti-cero-vacuo: assertar **≥ 5 fixtures** y **> 0 ítems con descripción**.
       Cifras de referencia: 5 / 85 / 44.
    2. **Determinar la causa, empíricamente.** Tomar el primer ítem divergente, imprimir las dos
       longitudes y los dos cortes, y **escribir en el SUMMARY la causa determinada por evidencia**.
       **NO copiar la causa del premortem: está refutada** — `despojarHtml` también termina en
       `.trim()` (`prefiltro-lexico.ts:88-89`). Si la causa no se puede establecer con la evidencia
       a mano, **decirlo así en el SUMMARY**: un verde dudoso se reporta, no se redacta bonito.
       ⚠️ **Consecuencia explícita, para que nadie la descubra después:** si la causa se declara
       *no determinada*, el gate efectivo de esta tarea pasa a ser **únicamente**
       `terminos_perdidos === 0`. Eso es aceptable —es lo que D-133-F2.2 protege— pero debe quedar
       escrito en el SUMMARY con esas palabras, no insinuado: la fase avanza sabiendo que la
       divergencia 44/85 quedó sin explicación, no creyendo que quedó explicada.
    3. **Evaluar el daño real y decidir la escalada.** Para cada ítem divergente, comprobar si algún
       término del vocabulario que estaba **dentro** del corte foldeado queda **fuera** del corte sin
       foldear (misma comparación por frontera de palabra que `coberturaTerminos`).
       - `terminos_perdidos === 0` ⇒ no hay daño; se declara en el SUMMARY con las tres cifras y la
         fase sigue. El test asserta ese `=== 0`: es la aserción que protege D-133-F2.2 y muerde si
         algún día el desplazamiento sí amputa.
       - `> 0` ⇒ **PARAR y escalar** con el listado de términos perdidos.
  </action>
  <acceptance_criteria>
    - `test $((N_T2 - N_T1)) -eq 5`, con `N_T1` medido al cierre de la Task 1. Ambos citados.
    - `n=$(grep -c '\.includes(' packages/news/src/eval/entrada-llm.ts || true); test "$n" -eq 0`
      (la cobertura usa frontera de palabra; revisar además manualmente).
    - `n=$(grep -rc 'LIMITE_DESCRIPCION\|\b600\b' packages/news/src/eval/entrada-llm.ts || true); test "$n" -eq 0`.
    - `grep -c 'truncarDescripcion' packages/news/src/eval/entrada-llm.ts` ≥ 1.
    - El test comparativo asserta ≥ 5 fixtures y > 0 ítems, y el SUMMARY cita **las tres cifras
      medidas** más el conteo de **términos perdidos**, que debe ser **0** para continuar.
    - El SUMMARY declara la **causa determinada empíricamente**, o declara explícitamente que no
      pudo determinarse. **Prohibido escribir la causa del premortem como hecho: está refutada.**
    - **Mutación 1 — `String.includes` en la cobertura:** sustituir la comparación por frontera de
      palabra por `String.includes`; el `it` (e) —el del término que es substring de otra palabra—
      debe **caer** (`if … ; then rc=0; else rc=$?; fi; test $rc -ne 0`). Revertir.
      *Este `it` existe precisamente para que esta mutación sea alcanzable: sin él, ningún test cae.*
    - **Mutación 2 — términos perdidos:** inyectar un desplazamiento artificial que sí saque un
      término (p. ej. recortar 40 chars extra al corte sin foldear) ⇒ rc≠0. Revertir.
      **Escalada escrita:** si esta mutación **no** pone rojo, el `terminos_perdidos === 0` es un
      cero vacuo (probablemente el conjunto de ítems divergentes llegó vacío al cómputo). El ejecutor
      **NO** lo declara cumplido: imprime el conteo de ítems divergentes que entraron al cómputo,
      aumenta el recorte hasta que alguno pierda un término, y si con un recorte de 200 chars sigue
      verde, **PARAR y escalar**.
    - **Mutación 3 — anti-cero-vacuo:** hacer que la carga de fixtures devuelva `[]` ⇒ rc≠0. Revertir.
    - `git diff --name-only <SHA literal>` **no** lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04b.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04b.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>`entrada_llm` conserva tildes y campos separados con la MISMA función de truncado; la divergencia medida con sus tres cifras, su causa investigada, y `terminos_perdidos === 0` probado con mutación que sí muerde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: caso-golden.ts — el esquema zod completo de D-133-F2, SIN ningún caso</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-F2 (esquema: `caso_id`, `procedencia`, `entrada`, `entrada_llm`,
      `estrato`, `prefiltro`, `etiqueta`, `revision`) y §D-133-C2.5 (campos de `revision`, incluidos
      `resuelto_por` ∈ {`acuerdo`,`operador`,`no_arbitrado`}, `modelo_a`, `modelo_b`,
      `en_calibracion_humana`, `etiqueta_humana`)
    - `133-READJUDICACION.md` §D-133-C2.2 (`justificacion` **≤ 200 chars** que cita el fragmento
      literal) y §B2.1 (los cuatro estratos: `P`, `N-alea`, `N-sonda`, `P-dirigido`)
    - `133-RESEARCH.md` §"Don't Hand-Roll" (zod ya es dependencia: `packages/news/package.json:21`;
      patrón vivo en `packages/news/src/model.ts` → `RssItemSchema`)
    - `133-RESEARCH.md` §Security Domain (el esquema debe **prohibir por tipo** el full-text y
      cualquier campo de `parlamentario`; precedente `packages/llm-bench/src/guards/no-rut.ts`)
  </read_first>
  <files>packages/news/src/eval/caso-golden.ts, packages/news/src/eval/caso-golden.test.ts</files>
  <behavior>
    Los **siete** comportamientos, uno por `it` (ver `<delta_de_tests>`):
    - Un caso bien formado (fixture **sintético**) valida.
    - Falta cualquier campo obligatorio ⇒ falla.
    - `estrato` fuera de `["P","N-alea","N-sonda","P-dirigido"]` ⇒ falla.
    - `etiqueta` (y `etiqueta_a`/`etiqueta_b`/`etiqueta_humana`) fuera de `ETIQUETAS` ⇒ falla.
    - `.strict()`: un campo **extra** no declarado ⇒ falla.
    - **`resuelto_por` fuera de `["acuerdo","operador","no_arbitrado"]` ⇒ falla** (D-133-C2.5).
    - **`justificacion_a` o `justificacion_b` de más de 200 chars ⇒ falla** (D-133-C2.2).
  </behavior>
  <action>
    Crear `packages/news/src/eval/caso-golden.ts` con `CasoGoldenSchema` (zod) y el tipo inferido
    `CasoGolden`, reproduciendo **exactamente** el esquema de D-133-F2 / C2.5, con `.max(200)` en
    ambas justificaciones. El enum de etiquetas se construye desde `ETIQUETAS` de `./taxonomia`
    (ruta relativa; **prohibido tocar `./index.ts`** y prohibido re-escribir las etiquetas).

    Usar `.strict()` en todos los objetos del esquema. El JSDoc declara que ese `.strict()` es el
    control de copyright/PII: solo titular + descripción del RSS, **cero full-text, cero PII añadida,
    cero cruce con `parlamentario`, cero causalidad ni intención** (D-133-F2). Un campo extra no es
    un descuido tolerable: es la vía por la que el full-text entraría al golden.

    **Los siete `it` del `<delta_de_tests>` son obligatorios.** `resuelto_por` y el tope de 200 chars
    son campos LOCKED: dejarlos sin test para cuadrar un número sería exactamente la reducción de
    alcance que esta fase prohíbe.

    **Cero casos etiquetados.** Este archivo define la forma, no contiene datos. Los fixtures de los
    tests son sintéticos y así se declaran en su JSDoc. Si aparece la necesidad de etiquetar un caso
    real "para probar el esquema", eso es 133-b: **PARAR**.
  </action>
  <acceptance_criteria>
    - `test $((N_T3 - N_T2)) -eq 7` y `test $((N_T3 - N_ANTES)) -eq 15` (delta total del plan).
      Todos los números citados.
    - `grep -c 'strict()' packages/news/src/eval/caso-golden.ts` ≥ 1.
    - `n=$(grep -c 'tramitacion_legislativa' packages/news/src/eval/caso-golden.ts || true); test "$n" -eq 0`
      (las etiquetas se derivan de `ETIQUETAS`).
    - `grep -c 'P-dirigido' packages/news/src/eval/caso-golden.ts` ≥ 1.
    - `grep -c 'modelo_a' packages/news/src/eval/caso-golden.ts` ≥ 1.
    - `grep -c 'no_arbitrado' packages/news/src/eval/caso-golden.ts` ≥ 1 y
      `grep -cE '\.max\(200\)|200' packages/news/src/eval/caso-golden.ts` ≥ 1 (los dos campos LOCKED
      están representados en el esquema, no solo en el test).
    - **No existe ningún caso etiquetado en el repo:**
      `if ls packages/news/src/eval/golden-set.json > /dev/null 2>&1; then rc=0; else rc=$?; fi; test $rc -ne 0`.
    - **Mutación del `.strict()`:** añadir a un fixture de test un campo extra
      (`texto_completo: "..."`) y comprobar que la validación **falla** (rc≠0, idiom `if/rc`).
      Revertir.
    - `git diff --name-only <SHA literal>` **no** lista `packages/news/src/eval/index.ts`.
  </acceptance_criteria>
  <verify>
    <automated>NO_COLOR=1 pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-04c.log; N=$(grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-04c.log | grep -oE '[0-9]+'); echo "N=$N"; test "$N" -gt 206</automated>
  </verify>
  <done>El esquema valida y rechaza los siete casos declarados —incluidos `resuelto_por` y el tope de 200 chars—, sin contener ni un solo caso etiquetado; delta total del plan exactamente 15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-filtro → `entrada_llm` | Dos etapas mirando ventanas de texto distintas hacen que el golden penalice al modelo por información amputada |
| RSS del medio → golden set | Copyright y PII: solo titular + descripción, jamás full-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-11 | Tampering | Constante de límite replicada en `eval/` ⇒ drift entre etapas | mitigate | `truncarDescripcion` compartida; grep con 0 ocurrencias fuera del módulo |
| T-133-12 | Information Disclosure | Full-text o campos de `parlamentario` en el caso golden | mitigate | `.strict()` + mutación con `texto_completo` |
| T-133-13 | Tampering | Cobertura calculada con `String.includes` ⇒ falsos positivos de cobertura | mitigate | Frontera de palabra + **un `it` dedicado al término-substring** que hace alcanzable la mutación |
| T-133-14 | Repudiation | Cobertura del 100 % sobre cero casos (cero-vacuo) | mitigate | `coberturaTerminos([])` lanza; mutación con fixtures vacíos |
| T-133-23 | Repudiation | El `.replace(/\S*$/,"")` sin cobertura ⇒ mutación imposible de poner roja | mitigate | Test nuevo con **fixture de forma fija** (palabra de 20 chars cruzando el corte) + las DOS mutaciones |
| T-133-24 | Repudiation | Gate de escalada que se dispara con certeza sobre un hecho ya medido | mitigate | Re-encuadre: medir, determinar causa empíricamente, escalar solo si `terminos_perdidos > 0` |
| T-133-29 | Repudiation | Un delta fijo que obliga a omitir comportamientos LOCKED para cuadrar el número | mitigate | La tabla de delta se cruza contra los `<behavior>` **y** contra las mutaciones; `resuelto_por` y el tope de 200 chars tienen su `it` |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones: `zod@^4.4.3` ya es dependencia directa. Cualquier `pnpm add` es violación: PARAR y escalar. |
</threat_model>

<verification>
1. `N_ANTES`, `N_T1`, `N_T2`, `N_T3` citados; deltas exactos 3 / 5 / 7 y total 15.
2. Las tres cifras del comparativo (fixtures / ítems / divergentes) y `terminos_perdidos` = 0, con
   su mutación que lo pone rojo **y** su escalada escrita si no lo pone.
3. La causa de la divergencia, determinada empíricamente y escrita — o declarada como no
   determinada. **Nunca la causa refutada del premortem como hecho.**
4. Las seis mutaciones (margen, `.replace`, `String.includes`, términos perdidos, fixtures vacíos,
   `.strict()`) con rc≠0 bajo el idiom `if/rc` — jamás bajo `set -e`. Dos de ellas llevan su
   escalada escrita para el caso en que no muerdan.
5. `git diff --name-only <SHA literal>` no lista `eval/index.ts` en ninguna tarea.
</verification>

<success_criteria>
- Una sola función de truncado, compartida, con constantes privadas y sin duplicar.
- El `.replace` de frontera de palabra **tiene cobertura** con fixture de forma fija, y su mutación
  sale roja.
- `entrada_llm` conserva tildes, mayúsculas y campos separados.
- La divergencia de cortes está medida, su causa investigada, y `terminos_perdidos === 0` probado.
- El esquema del caso golden cubre los **siete** comportamientos, incluidos los dos campos LOCKED, y
  el repo sigue **sin ningún caso etiquetado**.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-04-SUMMARY.md` when done.
Declara las tres cifras del comparativo, el conteo de términos perdidos, y la causa determinada
empíricamente (o su ausencia declarada) — nunca la causa refutada del premortem.
</output>
