---
phase: 133-news-taxo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - packages/news/src/eval/taxonomia.ts
  - packages/news/src/eval/canonicalizar-json.ts
  - packages/news/src/eval/index.ts
  - packages/news/src/eval/canonicalizar-json.test.ts
  - packages/news/src/eval/taxonomia.test.ts
autonomous: true
requirements: [NEWS-03]

must_haves:
  truths:
    - "El repo tiene .gitattributes con el patrón LOCKED antes de que exista ningún JSON congelado"
    - "La taxonomía D-133-A2 existe como módulo ejecutable con la precedencia en el orden del array"
    - "Canonicalizar el mismo objeto dos veces produce los mismos bytes; invertir un array produce hash distinto"
  artifacts:
    - path: "packages/news/src/eval/taxonomia.ts"
      provides: "SSoT de las 5 clases + ambiguo (D-133-A2)"
      exports: ["TAXONOMIA", "ETIQUETAS", "type Etiqueta"]
    - path: "packages/news/src/eval/canonicalizar-json.ts"
      provides: "canonicalización recursiva + sha256 (D-133-E2)"
      exports: ["canonicalizar", "sha256"]
    - path: ".gitattributes"
      provides: "normalización LF del directorio eval"
      contains: "packages/news/src/eval/**/*.json text eol=lf"
  key_links:
    - from: "packages/news/src/eval/canonicalizar-json.ts"
      to: "node:crypto"
      via: "createHash('sha256')"
      pattern: "createHash\\(\"sha256\"\\)"
---

<objective>
Fundación de 133-a: el `.gitattributes` LOCKED (primera tarea, D-133-E2.1), la taxonomía firmada
como módulo ejecutable, y el canonicalizador determinista que producirá las proyecciones JSON.

Purpose: sin estas tres piezas nada se puede congelar, y crear el `.gitattributes` DESPUÉS de
commitear un JSON mueve el hash solo (gotcha CRLF de v12.0).
Output: `.gitattributes`, `packages/news/src/eval/{taxonomia.ts,canonicalizar-json.ts,index.ts}` + sus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-READJUDICACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-ADDENDUM-IMPLEMENTACION.md
@.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-RESEARCH.md
@CLAUDE.md

<interfaces>
Superficie que este plan crea y que los planes 02, 04 y 05 consumen. NO cambiarla sin avisar.

packages/news/src/eval/taxonomia.ts
  export type Etiqueta = "tramitacion_legislativa" | "actividad_parlamentaria" | "ley_vigente"
                       | "politica_no_legislativa" | "no_legislativa" | "ambiguo";
  export interface ClaseTaxonomia {
    readonly etiqueta: Etiqueta;
    readonly definicion: string;
    readonly marca_decisoria: string;
    readonly frontera: string;
    readonly enruta_a: "proyecto" | "persona" | "ninguna";
  }
  export const TAXONOMIA: readonly ClaseTaxonomia[];   // precedencia = ORDEN DEL ARRAY
  export const ETIQUETAS: readonly Etiqueta[];         // derivado de TAXONOMIA, mismo orden

packages/news/src/eval/canonicalizar-json.ts
  export function canonicalizar(valor: unknown): string;   // string canónico, terminado en "\n"
  export function sha256(raw: string): string;             // hex de 64 chars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitattributes primero, con el patrón LOCKED y su verificación de índice</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-E2 enmienda 1 (el patrón exacto y por qué es la primera tarea)
    - `133-ADDENDUM-IMPLEMENTACION.md` §D-133-K4 (alcance: solo `*.json`, jamás `.md`)
    - `133-RESEARCH.md` §F (estado de partida verificado: `.gitattributes` no existe,
      `core.autocrlf=false`, cero `.json` trackeado con CRLF; y el gotcha
      "`--renormalize` NO añade untracked")
  </read_first>
  <files>.gitattributes</files>
  <action>
    Crear `.gitattributes` en la raíz del repo con **una sola línea** y nada más:
    `packages/news/src/eval/**/*.json text eol=lf`
    seguida de newline final. El archivo se escribe con LF.

    NO ampliar el patrón a `.md`, a `**/*.json` global, ni a ninguna otra ruta (D-133-K4: ampliar
    sin razón es alcance gratuito). NO ejecutar `git add --renormalize` sin pathspec.

    Ejecutar en este orden exacto:
    1. `git add .gitattributes`
    2. `git add --renormalize -- packages/news/src/eval` (red idempotente; hoy es no-op porque el
       directorio aún no existe — se repite en el plan 02 cuando los JSON existan).
    Commitear el `.gitattributes` SOLO (commit atómico, antes de que exista ningún JSON del eval).

    El research verificó empíricamente que `--renormalize` no toca archivos ajenos con
    `core.autocrlf=false`. Si `git status --short` muestra cualquier archivo modificado fuera de
    `.gitattributes`, PARAR y escalar — significa que la configuración de la máquina cambió.
  </action>
  <acceptance_criteria>
    - `cat .gitattributes` imprime exactamente `packages/news/src/eval/**/*.json text eol=lf`
      (una línea; verificar con `wc -l .gitattributes` = 1).
    - `git check-attr text eol -- packages/news/src/eval/x.json` imprime
      `packages/news/src/eval/x.json: text: set` y `...: eol: lf` (funciona con ruta hipotética).
    - Control negativo apareado, UNA sola variable (la ruta):
      `git check-attr text -- packages/news/src/otro.json` imprime `text: unspecified`.
      Ambos comandos sobre un `.json` inexistente ⇒ solo varía el directorio.
    - `git status --short` tras el commit no lista ningún archivo modificado.
    - `git log -1 --name-only` lista `.gitattributes` y **nada más**.
  </acceptance_criteria>
  <verify>
    <automated>git check-attr text eol -- packages/news/src/eval/x.json | grep -c 'eol: lf' | grep -q '^1$' && git check-attr text -- packages/news/src/otro.json | grep -q 'text: unspecified'</automated>
  </verify>
  <done>`.gitattributes` existe, commiteado solo, con el patrón LOCKED aplicándose a `eval/*.json` y a nada más.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: taxonomia.ts — SSoT de D-133-A2, precedencia en el orden del array</name>
  <read_first>
    - `133-READJUDICACION.md` §A2.2 (la tabla de 5 clases + `ambiguo`: etiqueta, definición
      operativa, marca decisoria, frontera) y §A2.2 tabla de enrutamiento
    - `133-READJUDICACION.md` §A2.1 (regla de decidibilidad textual) y §A2.3 (qué cuelga de una persona)
    - `133-RESEARCH.md` §L (estructura recomendada de `eval/`; `Object.freeze` es shallow;
      precedente `packages/news/src/prefiltro-lexico.ts:16,:65`)
    - `133-RESEARCH.md` §A (TS18003 NO aplica a un directorio nuevo dentro de un paquete composite
      ya poblado; `exclude: ["src/**/*.test.ts"]` ⇒ los `.test.ts` no se typechequean)
    - `packages/news/src/prefiltro-lexico.ts` (estilo del repo: `Object.freeze`, JSDoc con la razón)
  </read_first>
  <files>packages/news/src/eval/taxonomia.ts, packages/news/src/eval/index.ts, packages/news/src/eval/taxonomia.test.ts</files>
  <behavior>
    - `TAXONOMIA` tiene exactamente 6 entradas.
    - `TAXONOMIA.map(c => c.etiqueta)` es, en este orden exacto:
      `["tramitacion_legislativa","actividad_parlamentaria","ley_vigente","politica_no_legislativa","no_legislativa","ambiguo"]`.
    - `agenda_ejecutivo` NO aparece como etiqueta en ninguna entrada.
    - `enruta_a` vale `"proyecto"` para `tramitacion_legislativa` y `ley_vigente`; `"persona"` para
      `actividad_parlamentaria`; `"ninguna"` para `politica_no_legislativa`, `no_legislativa` y `ambiguo`.
    - Ninguna entrada tiene `definicion`, `marca_decisoria` o `frontera` vacíos.
    - Mutar `TAXONOMIA[0]` en runtime lanza en strict mode (congelación profunda efectiva).
    - `ley_vigente.frontera` NO contiene la subcadena `modificación en trámite` (cláusula eliminada
      por D-133-A2; su reaparición es drift de la decisión firmada).
  </behavior>
  <action>
    Crear `packages/news/src/eval/taxonomia.ts` como **única fuente de verdad** de la taxonomía
    firmada en D-133-A2. Estructura: un array `TAXONOMIA` de objetos `ClaseTaxonomia` con las
    cinco columnas de la tabla A2.2 (`etiqueta`, `definicion`, `marca_decisoria`, `frontera`) más
    `enruta_a` derivado de la tabla de enrutamiento de A2.2. Firma exacta en `<interfaces>` del
    `<context>`.

    Reglas de construcción no negociables:
    - **La precedencia vive en el ORDEN DEL ARRAY**, no en un campo `prioridad` numérico. El JSDoc
      del módulo debe decirlo con esas palabras y advertir que reordenar el array ES cambiar la
      taxonomía (y por tanto el hash del plan 02).
    - `ambiguo` va **último** en el array y su JSDoc declara que es escape, no nivel de precedencia.
    - Congelación **profunda**: `Object.freeze` es shallow ⇒ congelar también cada objeto del array
      (o usar `as const` con `readonly`). Un test lo prueba.
    - Los textos de `definicion`, `marca_decisoria` y `frontera` se copian de la tabla A2.2 con su
      sentido íntegro. `ley_vigente` se define por marca textual y **sin** la cláusula
      "si hay una modificación en trámite" (eliminada por D-133-A2).
    - La taxonomía **no nombra sujetos** (D-133-H): ningún nombre propio de persona, partido,
      ministerio ni número de boletín en ningún string.
    - `ETIQUETAS` se **deriva** de `TAXONOMIA` (`TAXONOMIA.map(...)`), jamás se re-tipea a mano —
      re-escribir las etiquetas en otro lugar está prohibido por D-133-A2.

    Crear también `packages/news/src/eval/index.ts` que re-exporte `taxonomia.ts` y (cuando exista)
    `canonicalizar-json.ts`. NO tocar `packages/news/src/index.ts` (el barrel raíz del paquete) en
    este plan — ningún otro plan de 133-a lo toca, y así no hay conflicto de archivo entre waves.

    NO importar ningún `.json` desde un `.ts` de producción (TS6307 — `packages/news/tsconfig.json`
    incluye solo `src/**/*.ts`). Todo JSON se leerá con `readFileSync` en los planes siguientes.

    Escribir `taxonomia.test.ts` con los casos del bloque `<behavior>`, incluido el de mutación
    (`expect(() => { (TAXONOMIA[0] as any).etiqueta = "x"; }).toThrow()`).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec tsc -b --force` sale rc=0 (baseline verificada hoy: rc=0).
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-01-news.log` y luego
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-01-news.log` imprime un número
      **estrictamente mayor que 206** (baseline del paquete medida en el research). Jamás se acepta
      el exit code solo: `passWithNoTests: true` está activo y los args de `vitest run` son filtros
      de nombre, no rutas.
    - `grep -c 'agenda_ejecutivo' packages/news/src/eval/taxonomia.ts` devuelve 0.
    - `grep -c 'prioridad' packages/news/src/eval/taxonomia.ts` devuelve 0 (la precedencia es el orden).
    - Prueba de mutación del test de orden: cambiar de sitio dos entradas de `TAXONOMIA`, correr
      `if pnpm --filter @obs/news exec vitest run taxonomia > /tmp/mut.log 2>&1; then rc=0; else rc=$?; fi`
      y comprobar `test $rc -ne 0`; revertir. (Nunca bajo `set -e`.)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-01-news.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-01-news.log</automated>
  </verify>
  <done>`TAXONOMIA` existe con las 6 clases en el orden de precedencia firmado, congelada en profundidad, con sus tests verdes y la prueba de mutación demostrada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: canonicalizar-json.ts — claves ordenadas recursivas, arrays intactos, sha256</name>
  <read_first>
    - `133-READJUDICACION.md` §D-133-E2 (canonicalización: claves ordenadas ascendentemente por
      code unit UTF-16, **recursiva**; **arrays NO se reordenan**; 2 espacios; LF; UTF-8 sin BOM;
      newline final; hash = sha256 sobre esos bytes)
    - `packages/llm-bench/src/guards/freeze.ts:29-52` (`hashCasos`, `assertFrozen` — el molde;
      JSDoc "NUNCA hash artesanal")
    - `packages/identity/src/backup.ts:64-80` (`withSortedKeys` — ejemplo de estilo, **shallow y
      además ordena filas**: NO reusar tal cual, aquí ordenar arrays está PROHIBIDO)
    - `133-RESEARCH.md` §C (Node nunca traduce newlines ni escribe BOM; `Object.keys().sort()` ya
      ordena por code unit UTF-16 — jamás `localeCompare`)
  </read_first>
  <files>packages/news/src/eval/canonicalizar-json.ts, packages/news/src/eval/canonicalizar-json.test.ts, packages/news/src/eval/index.ts</files>
  <behavior>
    - Dos objetos con las **mismas claves insertadas en orden distinto** producen el mismo string.
    - La recursión funciona: la reordenación de claves ocurre también en objetos anidados dentro de
      arrays y dentro de otros objetos (test con ≥3 niveles).
    - **Un array con sus elementos invertidos produce un string DISTINTO y un sha256 DISTINTO** —
      esta es la prueba de que "arrays no se reordenan" está implementado y no solo escrito.
    - El string resultante: usa indentación de 2 espacios, no contiene `\r`, no empieza por `U+FEFF`,
      y termina en `\n`.
    - `sha256` de un string conocido devuelve 64 chars hex y coincide con el valor de referencia
      calculado por `node -e` en el mismo test (control cruzado, no un literal pegado a mano).
    - Canonicalizar el mismo valor dos veces devuelve bytes idénticos (idempotencia).
  </behavior>
  <action>
    Crear `packages/news/src/eval/canonicalizar-json.ts` con dos exports puros:
    `canonicalizar(valor: unknown): string` y `sha256(raw: string): string`.

    `canonicalizar`: recorrido recursivo que, para cada objeto plano, re-emite sus claves con
    `Object.keys(o).sort()` (sin comparador — orden por code unit UTF-16, exactamente lo que pide
    D-133-E2; **jamás `localeCompare`**, que depende del locale y rompería la reproducibilidad entre
    máquinas). Para cada array, **preserva el orden tal cual** y solo canonicaliza sus elementos.
    Serializa con `JSON.stringify(ordenado, null, 2)` y concatena `"\n"` final. El JSDoc debe decir
    explícitamente: *"los arrays NO se reordenan porque en `taxonomia.ts` la precedencia vive en el
    orden del array"*.

    `sha256`: `createHash("sha256").update(raw).digest("hex")` de `node:crypto`. Nunca un hash
    artesanal (JSDoc de `packages/llm-bench/src/guards/freeze.ts:29-32`). El JSDoc cita esa ruta
    como origen del patrón — copiar el patrón, no depender de `@obs/llm-bench` (arrastraría medio
    grafo de dependencias).

    Cero dependencias nuevas: `node:crypto` y nada más. Si aparece la tentación de
    `json-stable-stringify` o `canonicalize`, rechazarla — el requisito son ~25 líneas y una
    librería externa introduce riesgo de supply-chain sobre el artefacto que define la vara de todo
    el carril news.

    Añadir ambos exports a `packages/news/src/eval/index.ts`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-01b.log` y
      `grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-01b.log` imprime un número mayor que el
      obtenido al final de la Task 2 (registrar ambos números en el SUMMARY).
    - `grep -c 'localeCompare' packages/news/src/eval/canonicalizar-json.ts` devuelve 0.
    - Test de arrays presente y mordiendo: existe un `it` cuyo nombre contiene `arrays no se reordenan`
      y cuya aserción es `expect(sha256(canonicalizar(a))).not.toBe(sha256(canonicalizar(aInvertido)))`.
    - Prueba de mutación: introducir `.sort()` sobre los arrays dentro de `canonicalizar`, correr
      `if pnpm --filter @obs/news exec vitest run canonicalizar > /tmp/mut2.log 2>&1; then rc=0; else rc=$?; fi`
      y comprobar `test $rc -ne 0`; revertir el cambio. El log debe nombrar el test de arrays.
    - `grep -E '"(json-stable-stringify|canonicalize)"' packages/news/package.json` no devuelve
      ninguna línea (cero dependencias nuevas).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run 2>&1 | tee /tmp/133-01b.log; grep -oE 'Tests[^0-9]+[0-9]+ passed' /tmp/133-01b.log</automated>
  </verify>
  <done>El canonicalizador produce bytes deterministas con claves ordenadas recursivamente y arrays intactos, con la prueba de mutación que demuestra que el test de arrays muerde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → artefacto congelado | El `.gitattributes` y el canonicalizador determinan si el hash de la vara es estable o se mueve solo |
| desarrollador → `taxonomia.ts` | Un reordenamiento del array cambia la semántica (precedencia) sin cambiar ninguna línea de texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | `.gitattributes` creado tarde ⇒ índice con CRLF ⇒ hash móvil | mitigate | `.gitattributes` commiteado SOLO y ANTES de cualquier JSON (D-133-E2.1); `git check-attr` como criterio |
| T-133-02 | Tampering | Reordenar `TAXONOMIA` cambia la precedencia en silencio | mitigate | Test que asserta la secuencia exacta de las 6 etiquetas + prueba de mutación que reordena y demuestra el rojo |
| T-133-03 | Tampering | Un canonicalizador que ordena arrays borra la precedencia del JSON | mitigate | Test `arrays no se reordenan` con hashes distintos + mutación introduciendo `.sort()` |
| T-133-SC | Tampering | npm/pip/cargo installs | mitigate | **Cero instalaciones en este plan.** El research verificó que 133-a no requiere ninguna dependencia nueva (`zod`, `node:crypto`, `vitest` ya presentes). Cualquier `pnpm add` en este plan es una violación: PARAR y escalar. |
</threat_model>

<verification>
1. `pnpm --filter @obs/news exec tsc -b --force` → rc=0.
2. `pnpm --filter @obs/news exec vitest run` → `Tests N passed` con N > 206, número citado literal.
3. `git check-attr text eol -- packages/news/src/eval/x.json` → `text: set`, `eol: lf`.
4. Las dos pruebas de mutación (orden de `TAXONOMIA`, `.sort()` en arrays) demostradas con rc≠0,
   usando el idiom `if CMD > log 2>&1; then rc=0; else rc=$?; fi` — jamás bajo `set -e`.
</verification>

<success_criteria>
- `.gitattributes` existe, contiene solo el patrón LOCKED, y está commiteado antes de todo JSON.
- `TAXONOMIA` tiene 6 clases en el orden de precedencia firmado, congelada en profundidad, sin `agenda_ejecutivo`.
- `canonicalizar` es determinista, recursivo en claves, y NO reordena arrays (probado por hash).
- La suite de `@obs/news` sube por encima de 206 tests con el conteo impreso citado.
</success_criteria>

<output>
Create `.planning/phases/133-news-taxo-taxonom-a-congelada-golden-set-arreglado-antes-de-medir/133-01-SUMMARY.md` when done.
Registrar en el SUMMARY: el conteo de tests antes y después de cada tarea, y el resultado literal de `git check-attr`.
</output>
