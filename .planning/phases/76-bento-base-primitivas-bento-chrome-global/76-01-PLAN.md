---
phase: 76-bento-base-primitivas-bento-chrome-global
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/globals.css
  - app/app/globals.test.ts
  - app/components/bento/bento-grid.tsx
  - app/components/bento/bento-grid.test.tsx
  - app/components/bento/bento-tile.tsx
  - app/components/bento/bento-tile.test.tsx
autonomous: true
requirements: [BENTO-01]

must_haves:
  truths:
    - "globals.css define --radius-tile: 16px y --radius-control: 11px; --radius (0.5rem) shadcn INTACTO"
    - "globals.css tiene una regla scroll-margin-top global en headings ancla (≥ altura header sticky)"
    - "BentoGrid es un grid 6-col (md), gap 14px, grid-auto-rows minmax(0,auto), colapsa a 1 col ≤md con orden DOM = orden visual"
    - "BentoTile tiene variant default (bg-card + border-border + rounded-[--radius-tile]) y accent (bg-accent-product), prop span 2/4/6 → md:col-span-N"
    - "Ningún hex hardcodeado en components/bento/ — todo color por token existente"
    - "Las primitivas NO se montan en ninguna página en esta fase (piezas inertes hasta 77-78)"
  artifacts:
    - path: "app/app/globals.css"
      provides: "tokens --radius-tile/--radius-control + scroll-margin-top global"
      contains: "--radius-tile: 16px"
    - path: "app/components/bento/bento-grid.tsx"
      provides: "BentoGrid primitive (Server Component)"
      exports: ["BentoGrid"]
    - path: "app/components/bento/bento-tile.tsx"
      provides: "BentoTile primitive (cva variants)"
      exports: ["BentoTile", "bentoTileVariants"]
    - path: "app/app/globals.test.ts"
      provides: "source-scan de tokens de radio + scroll-mt"
    - path: "app/components/bento/bento-grid.test.tsx"
      provides: "RTL: clases grid + orden DOM"
    - path: "app/components/bento/bento-tile.test.tsx"
      provides: "RTL: variant/span classes + focus-visible/min-h + cero-hex soft"
  key_links:
    - from: "app/components/bento/bento-tile.tsx"
      to: "app/lib/utils.ts (cn) + class-variance-authority"
      via: "cva + cn merge"
      pattern: "cva\\(|cn\\("
    - from: "app/components/bento/bento-tile.tsx"
      to: "--radius-tile token en globals.css"
      via: "rounded-[--radius-tile]"
      pattern: "rounded-\\[--radius-tile\\]"
---

<objective>
Crear las primitivas bento (`BentoGrid`, `BentoTile`) y los tokens de radio (`--radius-tile: 16px`, `--radius-control: 11px`) más la regla `scroll-margin-top` global que mitiga el sticky header. Todo por tokens existentes — CERO hex nuevo.

Purpose: dejar existentes las piezas con las que 77-78 arman el bento, sin montarlas aún en ninguna página. La única fase donde estas primitivas se crean; 77-78 las consumen.
Output: 2 componentes nuevos + 2 tokens de radio + 1 regla scroll-mt + 3 archivos de test (Wave 0 gaps SC1/SC2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-RESEARCH.md
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-UI-SPEC.md

<interfaces>
<!-- Patrón cva verbatim del repo — components/bento/bento-tile.tsx lo espeja. -->
<!-- Fuente: app/components/ui/button.tsx (VERIFIED). -->

From app/components/ui/button.tsx:
```typescript
import * as React from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const buttonVariants = cva(
  "inline-flex items-center justify-center ... focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 ...",
  {
    variants: {
      variant: { default: "bg-primary text-primary-foreground hover:bg-primary/90", /* … */ },
      size:    { default: "h-10 px-4 py-2", /* … */ },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : "button";
    return <Comp className={cn(buttonVariants({ variant, size, className }))} ref={ref} {...props} />;
  }
);
Button.displayName = "Button";
export { Button, buttonVariants };
```

From app/lib/utils.ts:
```typescript
export function cn(...inputs): string; // = twMerge(clsx(inputs))
```

Tokens de color ya registrados en globals.css :root (VERIFIED — el mockup ES estos tokens):
- --background: 40 33% 97% (#F9F6F0) → bg-background
- --card: 40 30% 99% (#FDFBF7) → bg-card
- --border: 40 16% 86% (#E3DDD3) → border-border
- --accent-product: 183 38% 26% (#2A5859) → bg-accent-product / text-accent-product
- --muted-foreground: 222 14% 42% → text-muted-foreground
- --ring: 183 38% 26% → focus-visible:ring-ring
- --radius: 0.5rem (shadcn — NO tocar)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tokens de radio + scroll-mt global en globals.css + source-scan test</name>
  <files>app/app/globals.css, app/app/globals.test.ts</files>
  <read_first>
    - app/app/globals.css (líneas 9-31 :root; líneas 75-87 @layer base — dónde insertar)
    - app/components/global-header.test.ts (idiom source-scan verbatim: process.cwd, stripComments)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md (§globals.css, §globals.test.ts)
  </read_first>
  <behavior>
    - Test: SRC de globals.css contiene "--radius-tile: 16px"
    - Test: SRC contiene "--radius-control: 11px"
    - Test: SRC contiene "--radius: 0.5rem" (INTACTO — D4, no se cambió)
    - Test: SRC contiene "scroll-margin-top" (regla global de anchors)
    - Test usa process.cwd() + path.join, NUNCA new URL(import.meta.url) (lección 45-01, OneDrive/jsdom)
    - Nota: stripComments borra bloques /* */ — asertar sobre las LÍNEAS DE TOKENS que sobreviven, no sobre el texto del comentario del mapa
  </behavior>
  <action>
    En `app/app/globals.css`, dentro del bloque `:root` (después de la línea 30 `--radius: 0.5rem;`), añadir DOS tokens nuevos: `--radius-tile: 16px;` y `--radius-control: 11px;`. NO tocar `--radius: 0.5rem` (D4 — LOCKED; cambiarlo regresiona todos los botones/inputs/badges de todas las rutas). Preceder los tokens con un comentario que documente el mapa mockup→token existente (per D4: `#F9F6F0`≈`--background`, `#FDFBF7`≈`--card`, `#E3DDD3`≈`--border`, `#2A5859`=`--accent-product`, `#5C6373`≈`--muted-foreground`) — así la regla "cero hex nuevo" es auto-evidente en el sitio de definición.

    Dentro del bloque existente `@layer base` (líneas 75-87, junto a las reglas `*` y `body`), añadir la regla global de scroll: `:where(h1, h2, h3)[id] { scroll-margin-top: 5rem; }` (80px ≥ altura del header sticky ~56px + aire; mitiga riesgo #2 — el sticky ocluye anchors en fichas largas). NO tocar las fichas (fuera de scope 76); su `scroll-mt-6` local es override que quedaría corto — se documenta el ajuste fino para Phase 79/81 en el SUMMARY.

    Crear `app/app/globals.test.ts` copiando el idiom source-scan de `global-header.test.ts` (`APP_ROOT = process.cwd()`; `GLOBALS_CSS = path.join(APP_ROOT, "app", "globals.css")`; función `stripComments` verbatim; `const SRC = stripComments(readFileSync(GLOBALS_CSS, "utf8"))`). Asertar las 4 líneas de token/regla del bloque `behavior`. Los tokens de radio son theme-agnostic (sin par dark — D4).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run globals</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test -- --run globals` verde
    - globals.css contiene `--radius-tile: 16px`, `--radius-control: 11px`, `--radius: 0.5rem` (intacto), `scroll-margin-top`
    - globals.test.ts usa `process.cwd()` (grep: NO aparece `import.meta.url`)
    - Comentario del mapa mockup→token presente en :root
  </acceptance_criteria>
  <done>Tokens de radio + scroll-mt global presentes y verificados por source-scan; --radius shadcn intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BentoGrid (grid 6-col colapsable) + test RTL</name>
  <files>app/components/bento/bento-grid.tsx, app/components/bento/bento-grid.test.tsx</files>
  <read_first>
    - app/components/global-header.tsx (analog: Server Component hand-rolled, wrapper div + {children})
    - app/lib/utils.ts (cn)
    - app/components/header-nav.test.tsx (idiom RTL: render + screen + toHaveClass)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md (§bento-grid.tsx, Pattern 2)
  </read_first>
  <behavior>
    - Test: wrapper tiene clase "md:grid-cols-6"
    - Test: wrapper tiene clase "gap-[14px]"
    - Test: wrapper tiene clase "grid-cols-1" (colapso móvil por defecto)
    - Test: children se preservan en orden DOM (render 3 hijos identificables → mismo orden en el DOM)
    - Assert CLASES/props/orden — NUNCA getComputedStyle (jsdom no computa grid; riesgo #5)
  </behavior>
  <action>
    Crear `app/components/bento/bento-grid.tsx` como Server Component (SIN `"use client"`), espejando el idiom hand-rolled de `global-header.tsx`: un único `<div>` wrapper + `{children}`. Props: `{ children, className? }` (tipar con `React.HTMLAttributes<HTMLDivElement>` o interfaz mínima). Clases base vía `cn(...)`: `grid grid-cols-1 gap-[14px] md:grid-cols-6 [grid-auto-rows:minmax(0,auto)]` mergeadas con `className` del consumidor. `gap-[14px]` es arbitrary off-step INTENCIONAL del mockup — NO redondear a `gap-4`/`gap-3` (UI-SPEC §Spacing, banned). `[grid-auto-rows:minmax(0,auto)]` arbitrary intencional. Colapso ≤md: `grid-cols-1` default + `md:grid-cols-6` → los hijos fluyen en orden DOM = orden visual, sin reorden CSS. Export nombrado `BentoGrid`.

    Crear `app/components/bento/bento-grid.test.tsx` con RTL (`render`, `screen`, `cleanup` de `@testing-library/react`; `afterEach(cleanup)`), espejando `header-nav.test.tsx`. Asertar las clases del bloque `behavior` con `toHaveClass` sobre el wrapper, y el orden DOM de 3 hijos. NO usar `getComputedStyle`.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run components/bento</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test -- --run components/bento` verde para bento-grid
    - bento-grid.tsx NO contiene `"use client"` (Server Component)
    - Wrapper tiene `md:grid-cols-6`, `gap-[14px]`, `grid-cols-1`
    - Cero hex `#` en el source de bento-grid.tsx
    - `pnpm --filter ./app exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>BentoGrid renderiza wrapper grid colapsable con orden DOM preservado; test RTL verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BentoTile (cva variants default/accent + span) + test RTL</name>
  <files>app/components/bento/bento-tile.tsx, app/components/bento/bento-tile.test.tsx</files>
  <read_first>
    - app/components/ui/button.tsx (analog EXACTO: cva + cn + forwardRef + Slot/asChild + displayName + doble export)
    - app/lib/utils.ts (cn)
    - app/components/header-nav.test.tsx (idiom RTL)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md (§bento-tile.tsx, Pattern 1)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-UI-SPEC.md (§Component Inventory, §Color — accent AA)
  </read_first>
  <behavior>
    - Test: variant="default" → clases bg-card, border-border, rounded-[--radius-tile]
    - Test: variant="accent" → clase bg-accent-product
    - Test: span={4} → clase md:col-span-4 (idem span 2 → md:col-span-2, span 6 → md:col-span-6)
    - Test: defaultVariants → sin props, aplica default + span 2
    - Test: tile interactivo (asChild envolviendo un <a>) emite focus-visible ring + min-h-11 (touch target ≥44px)
    - Test (soft, candado formal es Phase 80): source de bento-tile.tsx NO contiene literal hex `#`
    - Assert CLASES — nunca geometría
  </behavior>
  <action>
    Crear `app/components/bento/bento-tile.tsx` espejando VERBATIM la forma de `button.tsx`: imports (`React`, `Slot` de `@radix-ui/react-slot`, `cva`/`VariantProps` de `class-variance-authority`, `cn` de `@/lib/utils`); `cva(base, { variants, defaultVariants })`; `forwardRef`; `asChild`/`Slot`; `cn()` merge; `displayName`; export nombrado de `{ BentoTile, bentoTileVariants }`.

    Base cva: `rounded-[--radius-tile] transition-colors` (consume el token del Task 1). Si la tile es interactiva mantener del base de button `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` y `min-h-11` (el test SC2 los asevera). Variants:
    - `variant.default = "bg-card border border-border"` + `hover:border-accent-product` (borde hover interactivo, UI-SPEC §Color).
    - `variant.accent = "bg-accent-product text-primary-foreground"` (texto claro AA sobre petróleo; derivar del token, NO hardcodear el rgba del mockup — la verificación de contraste real es Phase 80).
    - `span: { 2: "md:col-span-2", 4: "md:col-span-4", 6: "md:col-span-6" }` — el span aplica desde `md`; ≤md ocupan la única columna del grid colapsado (full-width sin CSS extra).
    - `defaultVariants: { variant: "default", span: 2 }`.

    Elemento base `<div>` (NO `<button>`): usar `React.HTMLAttributes<HTMLDivElement>` + `forwardRef<HTMLDivElement>`. Polimorfismo `asChild` con `Slot` (patrón repo — deja que la tile envuelva un `<Link>` full-card en 77-78 sin duplicar focus-visible/min-h). CERO hex hardcodeado — todo color por token (`bg-card`, `border-border`, `bg-accent-product`; regla D-decision, candado formal Phase 80).

    NO montar `BentoTile` ni `BentoGrid` en ninguna página en esta fase (piezas inertes hasta 77-78).

    Crear `app/components/bento/bento-tile.test.tsx` con RTL (espejo `header-nav.test.tsx`). Asertar cada punto del bloque `behavior` con `toHaveClass`. Para el tile interactivo, renderizar con `asChild` envolviendo un `<a href>` y verificar focus-visible + min-h-11. Soft-check cero-hex: leer el source con `readFileSync(process.cwd()/components/bento/bento-tile.tsx)` y asertar que NO contiene `#` en contexto de color (regex hex), o un check simple `expect(SRC).not.toMatch(/#[0-9a-fA-F]{3,8}/)`.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run components/bento</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test -- --run components/bento` verde (grid + tile)
    - bento-tile.tsx exporta `BentoTile` y `bentoTileVariants`
    - bento-tile.tsx usa `cva` + `cn` (grep: NO `.join(" ")` para variantes)
    - default→bg-card/border-border/rounded-[--radius-tile]; accent→bg-accent-product; span 4→md:col-span-4
    - bento-tile.tsx NO contiene literal hex `#`
    - `pnpm --filter ./app exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>BentoTile con variants default/accent + span vía cva, polimórfico asChild, cero-hex; test RTL verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno nuevo) | Fase 100% presentación (CSS tokens + componentes React Server). No introduce flujos de datos, ni fetch cliente, ni lectura de env cruda. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | `--radius` shadcn (D4 LOCKED) | mitigate | `globals.test.ts` asevera `--radius: 0.5rem` intacto; el plan prohíbe explícitamente tocarlo. |
| T-76-02 | Information Disclosure | tokens/componentes bento | accept | Sin PII, sin datos, sin superficie de red. Presentación pura; ningún dato nuevo desde cliente. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | CERO instalaciones esta fase (cva/clsx/tailwind-merge/vitest/RTL ya en package.json, en uso productivo). Sin superficie de slopsquatting (RESEARCH §Package Legitimacy Audit: no aplica). |

Nota: el gate `netPublicEnabled` (nav Red) NO se toca en este plan (vive en 76-02); se preserva fail-closed. Sin nuevos flujos de datos ni fetch cliente.
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run globals components/bento` verde (SC1, SC2).
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- Suite app completa verde al merge de wave (`pnpm --filter ./app test -- --run`, target 820+).
- anti-insinuación (`anti-insinuacion-guard.test.ts`) y guard tipográfico intactos (esta fase no introduce copy nuevo).
</verification>

<success_criteria>
- `--radius-tile: 16px` + `--radius-control: 11px` definidos; `--radius: 0.5rem` intacto; comentario mapa mockup→token presente.
- `scroll-margin-top` global en headings ancla.
- `BentoGrid` (6-col gap 14px, colapso 1-col ≤md, orden DOM) y `BentoTile` (variants default/accent, span 2/4/6, cva) existen con tests de estructura.
- Cero hex hardcodeado en `components/bento/`.
- Primitivas NO montadas en ninguna página (inertes).
</success_criteria>

<output>
Create `.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-01-SUMMARY.md` when done.
</output>
