---
phase: 45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - pnpm-lock.yaml
  - app/app/globals.css
  - app/components/carril-accordion.tsx
  - app/components/carril-accordion.test.tsx
autonomous: true
requirements: [LEG-01, LEG-03]
user_setup: []

must_haves:
  truths:
    - "El <h2> del header del acordeón es visible esté el cuerpo abierto o cerrado (preserva h1→h2→h3)"
    - "El cuerpo (children) está presente en el DOM incluso cuando el carril está colapsado (forceMount)"
    - "El trigger toggla aria-expanded / data-state al hacer click (teclado/ARIA vía Radix)"
    - "El conteo 3-estado aparece en el header junto al título"
    - "carril-accordion.tsx NO importa ninguna sección ni @/lib/supabase (no-leak SSR)"
  artifacts:
    - path: "app/components/carril-accordion.tsx"
      provides: "Thin client island (Radix Accordion.Root single+collapsible, 1 item, h2 en header, forceMount)"
      contains: "\"use client\""
      min_lines: 25
    - path: "app/components/carril-accordion.test.tsx"
      provides: "RTL: header visible, conteo, toggle data-state, forceMount, no-leak grep"
      min_lines: 30
    - path: "app/package.json"
      provides: "Dependencia @radix-ui/react-accordion@1.2.14"
      contains: "@radix-ui/react-accordion"
  key_links:
    - from: "app/components/carril-accordion.tsx"
      to: "@radix-ui/react-accordion"
      via: "import * as AccordionPrimitive"
      pattern: "from \"@radix-ui/react-accordion\""
---

<objective>
Instalar la única dependencia nueva de la fase (`@radix-ui/react-accordion@1.2.14`) y crear el wrapper cliente `CarrilAccordion` que envuelve un Server Component de sección pasado como `children`, con el `<h2>` siempre visible en el header y el cuerpo colapsable.

Purpose: Es el componente reutilizable de LEG-01. El único landmine real de la fase es que este wrapper NO debe importar ninguna sección (las importa la página) — de lo contrario una sección server se arrastra al bundle cliente y se filtra el `service_role` (Camino A). Este plan aísla y testea ese contrato.
Output: `@radix-ui/react-accordion` en `package.json`/`pnpm-lock.yaml`, keyframes de acordeón en `globals.css` (respetando `prefers-reduced-motion`), `carril-accordion.tsx` + su test RTL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-CONTEXT.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-RESEARCH.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-PATTERNS.md
@.planning/phases/44-legibilidad-auditoria-plan/UI-SPEC.md

<interfaces>
<!-- Convención del repo para wrappers Radix "use client" + cn — copiar este shape. -->
From app/components/ui/tooltip.tsx (líneas 1-6):
  "use client";
  import * as React from "react";
  import * as TooltipPrimitive from "@radix-ui/react-tooltip";
  import { cn } from "@/lib/utils";

Firma objetivo de CarrilAccordion (RESEARCH Pattern 1 / PATTERNS líneas 42-49):
  CarrilAccordion({ titulo: string; conteo: React.ReactNode; defaultOpen: boolean; children: React.ReactNode })
  — `conteo` es un nodo 3-estado YA formateado ("9" | "—" | "sin registros"); el wrapper NO lo computa.
  — `children` es <Suspense><XSection/></Suspense>; el wrapper lo renderiza en un slot, NUNCA lo importa.

API Radix Accordion (CITED radix-ui.com/primitives/docs/components/accordion):
  Accordion.Root type="single" collapsible defaultValue → Accordion.Item value → Accordion.Header asChild → Accordion.Trigger; Accordion.Content forceMount; CSS var --radix-accordion-content-height
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @radix-ui/react-accordion + keyframes de acordeón</name>
  <read_first>
    - app/package.json (confirmar familia Radix ya instalada: @radix-ui/react-{separator,slot,tooltip})
    - app/tailwind.config.ts (theme.extend SIN keyframes/animation hoy; tailwindcss-animate NO está en plugins)
    - app/app/globals.css (hoja global importada por app/app/layout.tsx; aquí van los keyframes)
    - 45-RESEARCH.md §"CSS de animación" (líneas 352-361) + §Common Pitfalls Pitfall 5 (prefers-reduced-motion)
  </read_first>
  <action>
    Desde `app/`, ejecutar `pnpm add @radix-ui/react-accordion@1.2.14` (pnpm, NO npm — ver memoria; versión EXACTA verificada en registro + slopcheck [OK], peer react ^19.0). Confirmar que aparece en `app/package.json` dependencies y en `pnpm-lock.yaml`.
    Agregar a `app/app/globals.css` los keyframes `accordion-down`/`accordion-up` (usando `var(--radix-accordion-content-height)` de 0→altura y altura→0) y aplicarlos a `[data-state=open]`/`[data-state=closed]` del contenido del acordeón, TODO envuelto en `@media (prefers-reduced-motion: no-preference)` para que con reduce-motion no corra animación. La animación es NO load-bearing: el colapso real lo hace `data-[state=closed]:hidden` en el componente (Task 2), así que el carril funciona aunque la animación no aplique. NO agregar tailwindcss-animate al config (no está en plugins; mantener el cambio contenido en globals.css).
  </action>
  <verify>
    <automated>cd app && pnpm ls @radix-ui/react-accordion</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm ls @radix-ui/react-accordion` reporta 1.2.14.
    - `app/package.json` contiene `"@radix-ui/react-accordion"` en dependencies.
    - `app/app/globals.css` contiene los keyframes accordion-down/up dentro de un bloque `@media (prefers-reduced-motion: no-preference)`.
    - `cd app && pnpm typecheck` sigue limpio (sin consumidor todavía).
  </acceptance_criteria>
  <done>La dep está instalada en la versión exacta y los keyframes existen gated por prefers-reduced-motion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear CarrilAccordion (wrapper cliente) + test RTL</name>
  <read_first>
    - app/components/ui/tooltip.tsx (líneas 1-6: shape "use client" + import * as XPrimitive + cn — convención canónica)
    - app/components/ui/separator.tsx (segundo ejemplo del mismo shape)
    - app/lib/utils.ts (confirmar export `cn`)
    - app/components/lobby-de-parlamentario.test.tsx (líneas 1-42: boilerplate RTL + fixture factory — patrón canónico de tests)
    - app/app/parlamentario/[id]/page.tsx (líneas 54-59: el <h2 className="text-xl font-semibold mb-4"> que hoy vive en cada section y migra al header del acordeón)
    - 45-RESEARCH.md §Pattern 1 (líneas 160-206) + §Common Pitfalls Pitfall 1 y 3 (no-leak + forceMount)
    - 45-PATTERNS.md §carril-accordion.tsx (líneas 26-69)
    - app/lib/lockdown-guard.test.ts (líneas 122-173: por qué importar @/lib/supabase aquí sería una fuga)
  </read_first>
  <behavior>
    - Test 1: render con `defaultOpen={true}` → el `<h2>` con `titulo` está en el documento (getByRole("heading", { level: 2 })); el `children` (un marcador de prueba, p.ej. <div data-testid="cuerpo">) está en el DOM.
    - Test 2: render con `defaultOpen={false}` → el `<h2>` SIGUE en el documento (header siempre visible) Y el `children` SIGUE en el DOM (forceMount) — asertar presencia del nodo, no visibilidad; el trigger tiene `aria-expanded="false"` / `data-state="closed"`.
    - Test 3: click en el trigger (userEvent) → `aria-expanded`/`data-state` cambian a open (asertar sobre el atributo, robusto en jsdom; NO sobre mount/unmount).
    - Test 4: el `conteo` pasado como prop aparece dentro del header.
    - Test 5 (no-leak, grep): leer el texto fuente de `carril-accordion.tsx` con readFileSync y asertar que NO contiene `Section`, `createServerSupabase`, ni `@/lib/supabase` (espejo del estilo source-scan de lockdown-guard.test.ts).
  </behavior>
  <action>
    Crear `app/components/carril-accordion.tsx` con `"use client"` en línea 1, `import * as AccordionPrimitive from "@radix-ui/react-accordion"` y `cn` de `@/lib/utils` (shape de tooltip.tsx). Exportar `function CarrilAccordion({ titulo, conteo, defaultOpen, children })` con la firma del bloque `<interfaces>`. Estructura: `AccordionPrimitive.Root type="single" collapsible defaultValue={defaultOpen ? "c" : undefined}` → `AccordionPrimitive.Item value="c"` → `AccordionPrimitive.Header asChild` envolviendo el `<h2 className="text-xl font-semibold">` (mismo nivel/peso que hoy en page.tsx) que contiene `AccordionPrimitive.Trigger` (clases `group flex w-full items-center justify-between gap-4 min-h-11 text-left`, el `min-h-11` ≈ touch-target 44px) con `<span>{titulo}</span>` y un span de metadata (`text-muted-foreground font-normal text-sm font-mono`) que muestra `{conteo}` + un glifo chevron Unicode inline (p.ej. `▾`) que rota con `group-data-[state=open]:rotate-180` (DESIGN-SYSTEM §5.2 prefiere glifos Unicode; NO agregar icon dep). El cuerpo: `AccordionPrimitive.Content forceMount className="overflow-hidden data-[state=closed]:hidden pt-4"` con `{children}`. PROHIBIDO importar cualquier `*Section`, `createServerSupabase` o `@/lib/supabase` aquí (Pitfall 1 — el wrapper SOLO recibe children).
    Crear `app/components/carril-accordion.test.tsx` cubriendo los 5 comportamientos de `<behavior>`, copiando el boilerplate de `lobby-de-parlamentario.test.tsx` (líneas 1-7) y usando `@testing-library/user-event` para el click. La animación (prefers-reduced-motion) queda como verificación manual operador (no observable en jsdom).
  </action>
  <verify>
    <automated>cd app && pnpm test components/carril-accordion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test components/carril-accordion.test.tsx` verde (5 comportamientos).
    - `carril-accordion.tsx` línea 1 es `"use client";`.
    - grep negativo: `carril-accordion.tsx` no contiene `Section`, `createServerSupabase`, `@/lib/supabase` (asertado por el test 5).
    - El `<h2>` está presente con `defaultOpen` true Y false (header siempre visible).
    - El `children` está en el DOM con `defaultOpen={false}` (forceMount).
    - `cd app && pnpm typecheck` limpio.
  </acceptance_criteria>
  <done>CarrilAccordion existe como isla cliente que preserva el `<h2>`, conserva el contenido SSR colapsado (forceMount) y nunca importa una sección; su test RTL pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (RSC) → Client island | El toggle del acordeón hidrata en el navegador; los datos de sección se renderizan en el server y se pasan como RSC payload (children). El cliente NO recibe keys ni datos crudos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | carril-accordion.tsx (isla cliente) | mitigate | El wrapper recibe `children: React.ReactNode` y NUNCA importa una sección ni `@/lib/supabase`; si se importara, el Server Component entraría al module graph cliente y se filtraría el `service_role` (Camino A). Asertado por grep-test (behavior 5). |
| T-45-02 | Tampering | dependencia @radix-ui/react-accordion | accept | Versión exacta 1.2.14 verificada en registro + slopcheck [OK], misma org @radix-ui ya instalada, peer react ^19.0, sin postinstall. Audit en 45-RESEARCH §Package Legitimacy. |
</threat_model>

<verification>
- `cd app && pnpm test components/carril-accordion.test.tsx` verde.
- `cd app && pnpm typecheck` limpio.
- `cd app && pnpm ls @radix-ui/react-accordion` → 1.2.14.
</verification>

<success_criteria>
- `@radix-ui/react-accordion@1.2.14` instalado (pnpm), keyframes gated por prefers-reduced-motion en globals.css.
- `CarrilAccordion` existe: `<h2>` siempre visible, cuerpo colapsable con forceMount, conteo 3-estado en header, NUNCA importa una sección ni @/lib/supabase.
- Test RTL verde; tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-01-SUMMARY.md` when done
</output>
