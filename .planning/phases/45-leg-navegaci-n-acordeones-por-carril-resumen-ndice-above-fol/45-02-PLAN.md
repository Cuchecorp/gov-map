---
phase: 45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/parlamentario-resumen-conteos.ts
  - app/lib/parlamentario-resumen-conteos.test.ts
  - app/components/parlamentario-resumen.tsx
  - app/components/parlamentario-resumen.test.tsx
autonomous: true
requirements: [LEG-02, LEG-03]
user_setup: []

must_haves:
  truths:
    - "contarCarriles(id) deriva el conteo/estado de cada carril SOLO vía RPCs allowlisted + .from('*_ingesta_estado') guard-safe"
    - "ParlamentarioResumen emite un chip por carril PRESENTE, replicando los gates de la página (cruces/money)"
    - "Cada chip tiene un ancla href=\"#<carril>\" al carril correspondiente"
    - "Los 3 estados honestos (dato / vacío-honesto / no-ingerido) se renderizan distinto; MONEY OFF → chip honest-state, NUNCA un número"
    - "El chip nunca renderiza densidad falsa para un carril vacío"
  artifacts:
    - path: "app/lib/parlamentario-resumen-conteos.ts"
      provides: "contarCarriles(id) server-only con React.cache(); tipo CarrilEstado 3-estado"
      contains: "import \"server-only\""
      min_lines: 40
    - path: "app/components/parlamentario-resumen.tsx"
      provides: "ResumenView (pura, RTL) + ParlamentarioResumen (server, arma chips con gates)"
      min_lines: 40
    - path: "app/components/parlamentario-resumen.test.tsx"
      provides: "RTL sobre la vista pura ResumenView con fixtures (chips, anclas, 3-estado, money-off honest, negative-density)"
      min_lines: 40
  key_links:
    - from: "app/components/parlamentario-resumen.tsx"
      to: "contarCarriles"
      via: "import desde @/lib/parlamentario-resumen-conteos"
      pattern: "contarCarriles"
    - from: "app/lib/parlamentario-resumen-conteos.ts"
      to: "votos_de_parlamentario / lobby_de_parlamentario / declaraciones_de_parlamentario"
      via: "sb.rpc(...) — todos en PUBLIC_RPC_ALLOWLIST"
      pattern: "sb\\.rpc\\(\"(votos|lobby|declaraciones)_de_parlamentario\""
---

<objective>
Crear el módulo server-only de conteos `contarCarriles(id)` y el resumen+índice above-fold (`ParlamentarioResumen` server + `ResumenView` pura). El resumen emite un chip por carril presente con conteo/estado honesto (3-estado) y ancla de salto.

Purpose: Es LEG-02. El segundo landmine de la fase vive aquí: el conteo debe usar SOLO RPCs ya en `PUBLIC_RPC_ALLOWLIST` (más `.from('*_ingesta_estado')`, NO-PII) y NUNCA inventar densidad ni leer `parlamentario`. Replicar los gates de la página garantiza que el resumen liste solo carriles efectivamente presentes en el HTML (V4 Access Control).
Output: `parlamentario-resumen-conteos.ts` (+ test puro), `parlamentario-resumen.tsx` (+ test RTL de la vista pura).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-CONTEXT.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-RESEARCH.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-PATTERNS.md
@.planning/phases/44-legibilidad-auditoria-plan/UI-SPEC.md

<interfaces>
<!-- Tipo 3-estado del conteo (RESEARCH líneas 330-334). El chip NUNCA muestra un número fabricado. -->
CarrilEstado =
  | { tipo: "dato"; n: number }     // chip muestra n
  | { tipo: "vacio" }               // ingestado, 0 → "sin registros"
  | { tipo: "no_ingerido" }         // no ingestado → "—"
  | { tipo: "pendiente" };          // MONEY OFF honest-state

contarCarriles = cache(async (id: string) => { votos, lobby, patrimonio, cruces?, dinero? })  // server-only

RPCs disponibles (firmas exactas, ya en PUBLIC_RPC_ALLOWLIST):
  sb.rpc("votos_de_parlamentario", { p_id, p_limit, p_offset })   // votos-por-parlamentario.tsx:646-649
  sb.rpc("lobby_de_parlamentario", { p_id })                       // lobby-de-parlamentario.tsx:303-306
  sb.rpc("declaraciones_de_parlamentario", { p_id })               // patrimonio-de-parlamentario.tsx:670
  sb.rpc("cruces_de_parlamentario", { p_id })                      // SOLO si crucesPublicEnabled ON
  sb.from("lobby_ingesta_estado").select("parlamentario_id").eq(...).maybeSingle()      // NO-PII, guard-safe
  sb.from("probidad_ingesta_estado").select("parlamentario_id").eq(...).maybeSingle()   // NO-PII, guard-safe

Gates server-only (mismos que page.tsx):
  crucesPublicEnabled(process.env) — @/lib/cruces-gate   (hoy ON)
  moneyPublicEnabled(process.env)  — @/lib/money-gate    (hoy OFF)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear módulo server-only contarCarriles(id) + test puro 3-estado</name>
  <read_first>
    - app/lib/cruces-gate.ts (línea 1: `import "server-only"` — chokepoint a replicar)
    - app/lib/money-gate.ts (línea 1: mismo header server-only)
    - app/lib/supabase.ts (createServerSupabase — cliente service_role server-only, Camino A)
    - app/components/lobby-de-parlamentario.tsx (líneas 295-328: shape RPC + .from('lobby_ingesta_estado') + regla noIngestado = estadoData===null && total===0; error #34 throw líneas 308-312/323-327)
    - app/components/patrimonio-de-parlamentario.tsx (líneas 670, 700-709: declaraciones_de_parlamentario + probidad_ingesta_estado + misma regla)
    - app/components/votos-por-parlamentario.tsx (líneas 646-655: firma votos_de_parlamentario + throw #34)
    - app/lib/lockdown-guard.test.ts (líneas 125-136 PII_TABLES; 157-173 PUBLIC_RPC_ALLOWLIST — *_ingesta_estado y proyecto NO están en PII_TABLES)
    - 45-RESEARCH.md §Code Examples (líneas 323-350) + §Common Pitfalls Pitfall 2
    - 45-PATTERNS.md §parlamentario-resumen-conteos.ts (líneas 125-171)
  </read_first>
  <behavior>
    - Test 1 (mapeo puro, sin Supabase): extraer una función pura de mapeo conteo→CarrilEstado (p.ej. `derivarEstado({ total, ingestado })`) y asertar: total>0 → { tipo:"dato", n:total }; total===0 && ingestado → { tipo:"vacio" }; total===0 && !ingestado → { tipo:"no_ingerido" }.
    - Test 2: la regla noIngestado replica EXACTO la de las secciones: `estadoData === null && total === 0` (no inventar variantes).
    - Test 3: un error real de RPC/.from se traduce en throw (no en estado "vacio"). (Asertable sobre la función de mapeo o documentado; la verificación dura del throw vive en la integración.)
  </behavior>
  <action>
    Crear `app/lib/parlamentario-resumen-conteos.ts` con `import "server-only"` en línea 1 (espejo de cruces-gate.ts:1), `import { cache } from "react"`, `import { createServerSupabase } from "@/lib/supabase"`, los gates `crucesPublicEnabled`/`moneyPublicEnabled`. Exportar el tipo `CarrilEstado` (4 variantes del bloque `<interfaces>`) y una función PURA `derivarEstado({ total, ingestado }): CarrilEstado` (testeable sin runtime). Exportar `export const contarCarriles = cache(async (id: string) => {...})` que: construye `createServerSupabase()`; llama `votos_de_parlamentario` (p_limit alto p.ej. 1000, p_offset 0), `lobby_de_parlamentario`, `declaraciones_de_parlamentario`; lee `lobby_ingesta_estado` y `probidad_ingesta_estado` vía `.from(...).select("parlamentario_id").eq("parlamentario_id", id).maybeSingle()`; deriva el 3-estado por carril con `derivarEstado` usando `.length`/agrupación (votos por votaciones, lobby por audiencias agrupadas, patrimonio por versiones — el label "años" es discreción de presentación, usar el conteo que ya computa la sección para fidelidad). Para `cruces`: SOLO llamar `cruces_de_parlamentario` si `crucesPublicEnabled(process.env)` es true (no invocar el RPC con gate OFF). NO computar `dinero` cuando `moneyPublicEnabled(process.env)` es false (ese carril es honest-state "pendiente", lo arma el resumen). Cada error de RPC/.from → `throw new Error(...)` (patrón #34, NUNCA degradar a vacío). PROHIBIDO: `.from('parlamentario')`, cualquier tabla en PII_TABLES, cualquier RPC fuera del allowlist, crear un RPC nuevo (eso es F46+).
    Crear `app/lib/parlamentario-resumen-conteos.test.ts` (test puro sobre `derivarEstado`, sin Supabase) cubriendo `<behavior>`.
  </action>
  <verify>
    <automated>cd app && pnpm test lib/parlamentario-resumen-conteos.test.ts lib/lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test lib/parlamentario-resumen-conteos.test.ts` verde.
    - `cd app && pnpm test lib/lockdown-guard.test.ts` verde (Block B escanea el módulo nuevo: solo RPCs allowlisted, sin `.from('<pii>')`).
    - `parlamentario-resumen-conteos.ts` línea 1 es `import "server-only";`.
    - grep: el módulo contiene `cache(` y NO contiene `.from('parlamentario')` ni RPC fuera del allowlist.
    - `cruces_de_parlamentario` solo se invoca dentro de un guard `crucesPublicEnabled`.
    - `cd app && pnpm typecheck` limpio.
  </acceptance_criteria>
  <done>contarCarriles deriva el 3-estado honesto de cada carril vía RPCs allowlisted + ingesta-estado guard-safe, con throw en error real; el mapeo puro está testeado y el guard de lockdown sigue verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear ParlamentarioResumen (server) + ResumenView (pura) + test RTL</name>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx (líneas 50-64 interface LobbyViewData; 98 LobbyView pura; 110-135 los 3 estados honestos; 288-349 LobbySection async — el split pure-view/server-fetch a replicar)
    - app/components/lobby-de-parlamentario.test.tsx (líneas 1-42 boilerplate + fixture factory; 78-81 querByRole link + getAttribute href; 113-116 negative-assert de densidad)
    - app/app/parlamentario/[id]/page.tsx (líneas 103, 121, 143, 170-179: cómo gatean cruces/money la <section> entera y el honest-state #financiamiento-pendiente — el resumen replica EXACTO estos gates)
    - app/lib/parlamentario-resumen-conteos.ts (Task 1: contarCarriles + CarrilEstado)
    - app/lib/utils.ts (cn)
    - 45-RESEARCH.md §Pattern 3 (líneas 224-257)
    - 45-PATTERNS.md §parlamentario-resumen.tsx (líneas 72-122)
  </read_first>
  <behavior>
    - Test 1: ResumenView con chips de los 5 carriles presentes → 5 enlaces (queryAllByRole("link")); cada uno con `href` = "#votos"/"#lobby"/"#patrimonio"/"#cruces"/"#financiamiento-pendiente".
    - Test 2: un chip con estado { tipo:"dato", n:9 } muestra "9"; { tipo:"vacio" } muestra copy de vacío-honesto (p.ej. "sin registros"/"0", NO "—"); { tipo:"no_ingerido" } muestra "—"; los tres render textuales son DISTINTOS.
    - Test 3 (money-off honest): el chip de financiamiento con { tipo:"pendiente" } muestra copy honest-state y NUNCA un número.
    - Test 4 (negative-density): para un carril vacío, el chip NO contiene copy que se lea como densidad fabricada (espejo de lobby-de-parlamentario.test.tsx:113-116; queryByText de términos prohibidos = no presente).
  </behavior>
  <action>
    Crear `app/components/parlamentario-resumen.tsx` (Server Component — SIN "use client") con el split pure-view/server-fetch del análogo `lobby-de-parlamentario.tsx`:
    (a) `export interface ResumenChip { href: string; label: string; estado: CarrilEstado }` y `export function ResumenView({ chips }: { chips: ResumenChip[] })` PURA: un `<nav aria-label="Índice de secciones" className="mt-6 flex flex-wrap gap-2">` que mapea cada chip a un `<a href={ch.href} className="inline-flex items-center gap-2 min-h-11 ...">` con `<span>{label}</span>` + un sub-componente `ChipConteo` que mapea `CarrilEstado` a render textual distinto por estado (dato→n; vacio→"sin registros"; no_ingerido→"—"; pendiente→copy honest-state breve). El `min-h-11` mantiene el touch-target. Estilado dentro del DESIGN-SYSTEM (crema/petróleo, escala 8-pt, sin foto/partido) — discreción.
    (b) `export async function ParlamentarioResumen({ id }: { id: string })` que llama `await contarCarriles(id)` y arma `chips: ResumenChip[]` en el ORDEN LOCKED: Votaciones(#votos), Reuniones de lobby(#lobby), Declaraciones de patrimonio(#patrimonio), luego `...(crucesPublicEnabled(process.env) ? [chip #cruces] : [])`, luego `moneyPublicEnabled(process.env) ? [chip #dinero] : [chip #financiamiento-pendiente con estado {tipo:"pendiente"}]`. Los gates DEBEN coincidir byte-a-byte con page.tsx (V4 Access Control: el resumen lista solo carriles presentes en el HTML). Devuelve `<ResumenView chips={chips} />`.
    Crear `app/components/parlamentario-resumen.test.tsx` que importa SOLO la vista pura `ResumenView` (nunca el async `ParlamentarioResumen`) con un fixture factory `makeChips(overrides)`, cubriendo `<behavior>` (copiar boilerplate de lobby-de-parlamentario.test.tsx:1-7).
  </action>
  <verify>
    <automated>cd app && pnpm test components/parlamentario-resumen.test.tsx lib/lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test components/parlamentario-resumen.test.tsx` verde (4 comportamientos).
    - `ResumenView` es pura (sin "use client", sin Supabase) y se testea con fixtures.
    - Cada chip tiene `href="#<carril>"`; los 3 estados + pendiente renderizan textos distintos; pendiente y vacío NUNCA muestran un número.
    - Los gates `crucesPublicEnabled`/`moneyPublicEnabled` en `ParlamentarioResumen` coinciden con los de page.tsx.
    - `cd app && pnpm test lib/lockdown-guard.test.ts` verde.
    - `cd app && pnpm typecheck` limpio.
  </acceptance_criteria>
  <done>El resumen above-fold emite un chip ancla por carril presente con conteo/estado honesto 3-estado, replicando los gates de la página; la vista pura está testeada con fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Árbol público (RSC) → Supabase (service_role, Camino A) | El módulo de conteos lee con `service_role` que BYPASSA RLS; la protección de PII recae en el guard CI + el uso exclusivo de RPCs PII-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Elevation / Information Disclosure | parlamentario-resumen-conteos.ts | mitigate | SOLO RPCs en PUBLIC_RPC_ALLOWLIST (votos/lobby/declaraciones/cruces_de_parlamentario) + `.from('*_ingesta_estado')` (NO en PII_TABLES). Prohibido `.from('parlamentario')` y RPC nuevo. Asertado por lockdown-guard.test.ts Block B sobre el módulo. |
| T-45-04 | Information Disclosure | ChipConteo / ResumenView | mitigate | El conteo es un entero/estado; NUNCA proyecta columnas PII (rut/partido/donante). La vista pura recibe solo `CarrilEstado`. |
| T-45-05 | Tampering (gate bypass) | ParlamentarioResumen | mitigate | El resumen consulta `crucesPublicEnabled`/`moneyPublicEnabled(process.env)` igual que page.tsx; MONEY OFF → chip honest-state "pendiente", nunca un número ni un carril ausente. |
| T-45-06 | Information Disclosure | parlamentario-resumen-conteos.ts | mitigate | `import "server-only"` línea 1 garantiza que el módulo nunca llega al bundle cliente (chokepoint, espejo gates). |
</threat_model>

<verification>
- `cd app && pnpm test lib/parlamentario-resumen-conteos.test.ts components/parlamentario-resumen.test.tsx lib/lockdown-guard.test.ts` verde.
- `cd app && pnpm typecheck` limpio.
- grep: ningún `.from('parlamentario')` ni RPC fuera del allowlist en los archivos nuevos.
</verification>

<success_criteria>
- `contarCarriles(id)` server-only (React.cache) deriva 3-estado honesto vía RPCs allowlisted + ingesta-estado; throw #34 en error real.
- `ParlamentarioResumen` emite un chip ancla por carril presente con gates replicados; `ResumenView` pura testeada.
- Guard de lockdown verde sobre los módulos nuevos; tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-02-SUMMARY.md` when done
</output>
