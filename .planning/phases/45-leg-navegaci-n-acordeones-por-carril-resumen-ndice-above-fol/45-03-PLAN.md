---
phase: 45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol
plan: 03
type: execute
wave: 2
depends_on: ["45-01", "45-02"]
files_modified:
  - app/app/parlamentario/[id]/page.tsx
  - app/app/parlamentario/[id]/page-estructura.test.ts
autonomous: true
requirements: [LEG-01, LEG-03]
user_setup: []

must_haves:
  truths:
    - "El resumen ParlamentarioResumen se renderiza entre ParlamentarioHeader y el primer carril"
    - "Cada carril es su propia <section className=\"...mt-12...\"> hermana, con su <h2> migrado al header del CarrilAccordion"
    - "Hay exactamente UN Accordion (CarrilAccordion) por dominio; jamás dos dominios en una unidad"
    - "La frontera mt-12 sigue en cada <section> de page.tsx (no en el wrapper) y nunca se colapsa, ni con un carril vacío"
    - "Los gates crucesPublicEnabled/moneyPublicEnabled siguen envolviendo la <section> ENTERA (heading incluido); OFF = nodo ausente del HTML"
    - "La página importa las secciones y las pasa como children; el wrapper no las importa (SSR intacto)"
    - "Suite app/ completa verde + tsc -b limpio + guard de lockdown verde"
  artifacts:
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "Shell re-layouteado: resumen above-fold + cada carril envuelto en CarrilAccordion, mt-12 y gates intactos"
      contains: "CarrilAccordion"
    - path: "app/app/parlamentario/[id]/page-estructura.test.ts"
      provides: "Test estructural source-scan: mt-12 por carril, un acordeón por dominio, gates intactos, resumen presente"
      min_lines: 30
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "CarrilAccordion"
      via: "import desde @/components/carril-accordion + envoltura por <section>"
      pattern: "CarrilAccordion"
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "ParlamentarioResumen / contarCarriles"
      via: "import + render above-fold; defaultOpen derivado del 3-estado"
      pattern: "ParlamentarioResumen"
---

<objective>
Re-layoutear `app/app/parlamentario/[id]/page.tsx`: insertar `ParlamentarioResumen` above-fold (tras la cabecera, antes del primer carril) y envolver cada `<section>` de dominio con `CarrilAccordion`, pasando la sección como `children`. Preservar la frontera `mt-12` y los gates MONEY/CRUCES exactamente como hoy. Cerrar con la suite completa verde.

Purpose: Es la integración de LEG-01 (estructural) y LEG-03 (comportamiento-preservante). Aquí se consuma la navegación: el lector ve el índice arriba y abre/cierra cada carril. Los invariantes anti-insinuación (`mt-12`, un acordeón por dominio) y de seguridad (gates, SSR, guard) NO pueden regresar.
Output: `page.tsx` modificado + un test estructural source-scan + suite verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-CONTEXT.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-RESEARCH.md
@.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-PATTERNS.md
@.planning/phases/44-legibilidad-auditoria-plan/UI-SPEC.md
@app/components/carril-accordion.tsx
@app/components/parlamentario-resumen.tsx
@app/lib/parlamentario-resumen-conteos.ts

<interfaces>
<!-- Provistos por 45-01 y 45-02 (Wave 1) -->
CarrilAccordion({ titulo: string; conteo: React.ReactNode; defaultOpen: boolean; children: React.ReactNode })  // @/components/carril-accordion
ParlamentarioResumen({ id: string })  // server component — @/components/parlamentario-resumen
contarCarriles(id): Promise<{ votos, lobby, patrimonio, cruces?, dinero? }>  // @/lib/parlamentario-resumen-conteos (React.cache → barato llamar 2×)

Orden de carriles LOCKED (NO reordenar): #votos → #lobby → #patrimonio → #cruces (gated, hoy ON) → MONEY gated (#dinero/#financiamiento, hoy OFF) o #financiamiento-pendiente (honest-state).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-layout de page.tsx — resumen above-fold + cada carril en CarrilAccordion</name>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (archivo completo: el shell actual con 6 <section className="mt-12"> + el honest-state, los gates líneas 103/121/143/170, los Suspense+Skeletons que se conservan)
    - app/components/carril-accordion.tsx (45-01: firma + forceMount)
    - app/components/parlamentario-resumen.tsx (45-02: ParlamentarioResumen)
    - app/lib/parlamentario-resumen-conteos.ts (45-02: contarCarriles + CarrilEstado para derivar conteo del header y defaultOpen)
    - 45-RESEARCH.md §Pattern 2 (líneas 208-222: la página pasa las secciones como children — CITED Next 16 doc) + §Common Pitfalls Pitfall 4 (no colapsar mt-12)
    - 45-PATTERNS.md §page.tsx (líneas 198-225: el wrap + invariantes a NO regresar)
    - .planning/phases/44-legibilidad-auditoria-plan/UI-SPEC.md §1.2 (reglas del acordeón HARD) + §6 (checklist de invariantes)
  </read_first>
  <action>
    Modificar `app/app/parlamentario/[id]/page.tsx`:
    (1) Importar `CarrilAccordion` de `@/components/carril-accordion`, `ParlamentarioResumen` de `@/components/parlamentario-resumen`, y `contarCarriles` de `@/lib/parlamentario-resumen-conteos`.
    (2) En el cuerpo de `ParlamentarioPage`, tras validar el `id`, llamar `const conteos = await contarCarriles(id)` UNA vez (React.cache deduplica la llamada que también hace el resumen). Derivar el `conteo` (nodo 3-estado formateado) y el `defaultOpen` por carril a partir de `conteos`.
    (3) DESPUÉS del `<Suspense><HeaderSection/></Suspense>` y ANTES del primer carril, renderizar `<ParlamentarioResumen id={id} />` (above-fold, UI-SPEC §1.1).
    (4) Para cada carril (#votos, #lobby, #patrimonio, y dentro de sus gates #cruces, #dinero, #financiamiento): mantener la `<section id="..." className="mt-12">` EXACTAMENTE como hoy (el `mt-12` NO se mueve al wrapper — Pitfall 4), MOVER el `<h2>` de la section al prop `titulo=` del `CarrilAccordion`, y envolver el `<Suspense fallback={<XSkeleton/>}><XSection .../></Suspense>` existente como `children` del `CarrilAccordion`. El `CarrilAccordion` recibe `titulo`, `conteo` (3-estado de ese carril), `defaultOpen` (heurística), y el Suspense+sección como children. Los Skeletons existentes (page.tsx:242-336) se reutilizan sin cambios.
    (5) Heurística de `defaultOpen` (discreción de Claude, conservadora): colapsar carriles vacíos/ralos, abrir los sustantivos. Regla simple y determinista, p.ej.: abrir un carril si su `CarrilEstado.tipo === "dato"` y `n` supera un umbral bajo (o abrir solo el primer carril con datos sustantivos + patrimonio); colapsar `vacio`/`no_ingerido`/`pendiente`. Documentar la regla en un comentario.
    (6) NO TOCAR: la validación `PARLAMENTARIO_ID_RE`, `HeaderSection`, `FinanciamientoSectionConPeriodo`, los gates `crucesPublicEnabled(process.env)`/`moneyPublicEnabled(process.env)` (siguen envolviendo la `<section>` ENTERA, heading incluido — OFF = nodo ausente), el bloque honest-state `#financiamiento-pendiente` (líneas 170-179 — su `<h2>` también migra al header de un CarrilAccordion con conteo "pendiente", pero el gate `!moneyPublicEnabled` y el texto legal LOCKED se conservan). NO modificar el contenido interno de ninguna `*Section`.
    NO importar ninguna `*Section` dentro de `carril-accordion.tsx` (eso ya está prohibido y testeado en 45-01); la página sigue siendo quien importa las secciones.
  </action>
  <verify>
    <automated>cd app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm typecheck` limpio.
    - `page.tsx` renderiza `<ParlamentarioResumen id={id} />` entre `HeaderSection` y la primera `<section>` de carril.
    - Cada `<section>` de carril conserva `className="mt-12"` (y su `id`); el `<h2>` ya no está dentro de la `<section>` sino pasado como `titulo` al `CarrilAccordion`.
    - Hay un `CarrilAccordion` por carril de dominio; ninguna `<section>` agrupa dos dominios.
    - Los gates `crucesPublicEnabled`/`moneyPublicEnabled` siguen envolviendo cada `<section>` gated entera (sin cambios de lógica).
    - `contarCarriles(id)` se llama una vez en la página; `defaultOpen` deriva de su 3-estado.
  </acceptance_criteria>
  <done>La ficha es navegable: índice above-fold + un acordeón por carril con header siempre visible, preservando mt-12, gates, SSR y el contenido de las secciones.</done>
</task>

<task type="auto">
  <name>Task 2: Test estructural (mt-12, un acordeón por dominio, gates) + cierre de suite</name>
  <read_first>
    - app/lib/lockdown-guard.test.ts (líneas 31-120: estilo source-scan con readFileSync sobre archivos del repo — patrón a copiar para un test estructural sobre page.tsx)
    - app/app/parlamentario/[id]/page.tsx (ya modificado en Task 1)
    - 45-RESEARCH.md §Validation Architecture (líneas 422-444: Wave 0 gaps — test estructural de frontera mt-12 + un acordeón por dominio)
    - 45-VALIDATION.md §Per-Task Verification Map (LEG-01 estructural)
  </read_first>
  <behavior>
    - Test 1 (mt-12): leer el texto fuente de `page.tsx` con readFileSync; asertar que cada carril de dominio (#votos, #lobby, #patrimonio, #cruces, #dinero/#financiamiento, #financiamiento-pendiente) aparece dentro de una `<section ... className` que incluye `mt-12`. Contar las ocurrencias de `mt-12` ≥ número de carriles (excluyendo prosa/comentarios — strip de comentarios al estilo lockdown-guard).
    - Test 2 (un acordeón por dominio): asertar que NO existe un único `Accordion.Root`/contenedor que agrupe dos dominios; el número de `CarrilAccordion` usados ≈ número de carriles renderizados, y cada uso está dentro de su propia `<section>` (heurística source-scan: cada `<CarrilAccordion` está precedido por una apertura de `<section ... mt-12` distinta).
    - Test 3 (gates intactos): asertar que `crucesPublicEnabled(process.env)` y `moneyPublicEnabled(process.env)` siguen presentes envolviendo sus secciones (los strings de gate no se eliminaron).
    - Test 4 (resumen presente): asertar que `<ParlamentarioResumen` aparece en el fuente antes de la primera `<section ... mt-12`.
    - Test 5 (no-leak reforzado): asertar que `carril-accordion.tsx` no contiene `Section`/`@/lib/supabase` (puede reusar/duplicar el assert de 45-01 para defensa en profundidad).
  </behavior>
  <action>
    Crear `app/app/parlamentario/[id]/page-estructura.test.ts` (test de node/vitest, NO RTL — el page es async server component; se asierta sobre el SOURCE como hace lockdown-guard.test.ts). Reusar el helper `stripTsComments`/`stripSqlComments`-style para no contar prosa. Cubrir los 5 comportamientos de `<behavior>`. Usar `grep`-hygiene: contar tokens sobre el fuente con comentarios eliminados (no `grep -c` crudo).
    Tras crear el test, correr la suite completa de `app/` + typecheck + guard, y dejar todo verde antes del SUMMARY.
  </action>
  <verify>
    <automated>cd app && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test` (suite completa de app/) verde, incluyendo `page-estructura.test.ts`, `carril-accordion.test.tsx`, `parlamentario-resumen.test.tsx` y `lockdown-guard.test.ts`.
    - `cd app && pnpm typecheck` limpio (tsc).
    - El test estructural falla si: se quita un `mt-12` de un carril, se agrupan dos dominios en un acordeón, se elimina un gate, o falta el resumen above-fold.
    - El guard de lockdown sigue verde sobre todo el árbol (incluidos los 3 módulos nuevos).
  </acceptance_criteria>
  <done>Un test estructural source-scan protege los invariantes LOCKED (mt-12, un acordeón por dominio, gates, resumen); la suite completa de app/ + tsc + guard quedan verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Página server → islas cliente (CarrilAccordion) | La página importa las secciones server y las pasa como children; el RSC payload se renderiza en el server. El cliente solo hidrata el toggle. |
| Gate server-only → DOM | `crucesPublicEnabled`/`moneyPublicEnabled` deciden si la `<section>` entera entra al HTML; OFF = nodo ausente (no oculto-con-CSS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07 | Information Disclosure | page.tsx (wrap de secciones) | mitigate | La página pasa las secciones como `children` (CITED Next 16 doc); el wrapper no las importa → no entran al module graph cliente, el `service_role` nunca se filtra. Reforzado por el no-leak test (behavior 5) + lockdown-guard Block B. |
| T-45-08 | Tampering (gate bypass) | gates cruces/money en page.tsx | mitigate | Los gates se conservan envolviendo la `<section>` ENTERA; el test estructural falla si un gate se elimina. OFF = nodo ausente del HTML. |
| T-45-09 | Repudiation / Integrity | frontera mt-12 anti-insinuación | mitigate | El `mt-12` permanece en cada `<section>` hermana (no en el wrapper); un acordeón por dominio. Test estructural source-scan lo garantiza (Pitfall 4 / DESIGN-SYSTEM §3/§8). |
| T-45-SC | Tampering | npm install (heredado de 45-01) | mitigate | La única dep nueva (@radix-ui/react-accordion) ya fue auditada [OK] en 45-01; este plan no instala paquetes. |
</threat_model>

<verification>
- `cd app && pnpm test && pnpm typecheck` verde (suite completa + estructural + guard + tsc).
- Inspección: `page.tsx` conserva `mt-12` en cada `<section>`, gates intactos, resumen above-fold, contenido de secciones sin tocar.

## Checkpoint de operador (NO autónomo — fuera del plan)
El build OpenNext + deploy a Cloudflare es paso de OPERADOR, no una tarea de este plan:
- Build OpenNext en **Docker Linux** (Windows rompe el worker → 500); deploy `wrangler` local.
- Validar visualmente la animación del acordeón con `prefers-reduced-motion` activo (no observable en jsdom).
El agente NO ejecuta el build de producción ni el deploy. El plan se considera completo con la suite `app/` verde + tsc limpio + guard verde.
</verification>

<success_criteria>
- Ficha navegable: índice above-fold + acordeón por carril (header con `<h2>` siempre visible, cuerpo colapsable, forceMount).
- `mt-12` intacta entre carriles; un acordeón por dominio; gates MONEY/CRUCES sin cambios; SSR intacto (solo el toggle es cliente); contenido de secciones sin tocar.
- Suite `app/` completa verde + `tsc -b` limpio + guard de lockdown verde.
- Build/deploy Cloudflare documentado como checkpoint operador (no tarea autónoma).
</success_criteria>

<output>
Create `.planning/phases/45-leg-navegaci-n-acordeones-por-carril-resumen-ndice-above-fol/45-03-SUMMARY.md` when done
</output>
