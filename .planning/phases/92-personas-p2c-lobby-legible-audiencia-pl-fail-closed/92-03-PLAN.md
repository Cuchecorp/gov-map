---
phase: 92-personas-p2c-lobby-legible-audiencia-pl-fail-closed
plan: 03
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - app/components/lobby-menciones-de-boletin.tsx
  - app/components/lobby-menciones-de-boletin.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [LOB-02, LOB-03]
must_haves:
  truths:
    - "La ficha del proyecto tiene una sección nueva 'Audiencias de lobby que mencionan este boletín', su propia <section id='lobby-menciones' class='mt-12'>, SEPARADA y rotulada distinto de #lobby-tramitacion (0048, temporal — no se toca)"
    - "La sección lista fecha, parlamentario ENLAZADO a /parlamentario/{id}, contraparte cruda, materia completa, enlace fuente; orden fecha DESC; conteo honesto (total_n)"
    - "La sección lleva la leyenda anti-causal LOCKED (mención en registro ≠ influencia/causa) y su empty state honesto"
    - "El linter anti-insinuación cubre las superficies NUEVAS (sección + chip + lobby-de-parlamentario) con las negaciones LOCKED registradas ANTES en NEGACIONES_LOCKED, y muerde (mutation self-check)"
  artifacts:
    - path: "app/components/lobby-menciones-de-boletin.tsx"
      provides: "Sección ficha proyecto de menciones explícitas + leyenda + degrade honesto"
      contains: "lobby_menciones_de_boletin"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "<section id='lobby-menciones'> montada + entrada de rail 'Menciones en lobby'"
      contains: "lobby-menciones"
  key_links:
    - from: "app/components/lobby-menciones-de-boletin.tsx"
      to: "public.lobby_menciones_de_boletin"
      via: "sb.rpc con degrade honesto PGRST202→null"
      pattern: "lobby_menciones_de_boletin"
    - from: "app/components/lobby-menciones-de-boletin.tsx"
      to: "/parlamentario/{id}"
      via: "Link petróleo al parlamentario público"
      pattern: "/parlamentario/"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "NEGACIONES_LOCKED"
      via: "leyenda LOCKED registrada antes de añadir superficies al scan"
      pattern: "NEGACIONES_LOCKED"
---

<objective>
Montar en la ficha del proyecto la sección NUEVA "Audiencias de lobby que mencionan este
boletín" — carril hermano SEPARADO del cruce temporal 0048 — que consume
`lobby_menciones_de_boletin(p_boletin)`, con leyenda anti-causal LOCKED, parlamentario
enlazado (navegación bidireccional PL→audiencia→parlamentario), conteo honesto y empty
honesto; y EXTENDER el linter anti-insinuación a TODAS las superficies nuevas de la fase.

Purpose: LOB-02 (sección de mención explícita, separada de la temporal) y LOB-03
(navegación bidireccional). La lección BLOCKER de 91 es LOCKED: las leyendas que NIEGAN
términos prohibidos se registran en NEGACIONES_LOCKED ANTES de añadir la superficie al
scan, o el linter da falso-positivo y bloquea.

Output: `lobby-menciones-de-boletin.tsx` (+ test), sección montada en page.tsx + entrada
de rail, linter extendido con negaciones LOCKED + mutation self-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-CONTEXT.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-UI-SPEC.md

<interfaces>
<!-- RPC del Plan 01 (dependencia dura). -->
From supabase/migrations/0062_lobby_menciones_de_boletin.sql:
```sql
-- lobby_menciones_de_boletin(p_boletin text) returns table (
--   identificador text, fecha timestamptz, materia text, parlamentario_id text,
--   nombre text, contraparte(s) crudas sin RUT, enlace_detalle text,
--   origen text, fecha_captura timestamptz, total_n bigint) -- orden fecha DESC, LIMIT bounded
```

<!-- Componente ESPEJO de layout/leyenda/degrade (carril temporal 0048). El NUEVO es
     ANÁLOGO pero con parlamentario ENLAZADO y heading/leyenda DISTINTOS. -->
From app/components/lobby-en-tramitacion.tsx:
```typescript
export interface LobbyEnTramitacionRow { parlamentario_nombre; camara; materia; fecha_reunion; semana_iso; comision; enlace_detalle; audiencia_id? }
export function LobbyEnTramitacionView({ rows }): JSX;  // PURA — heading + caveat + filas
export async function LobbyEnTramitacionSection({ boletin }): JSX; // degrade honesto 3 caminos:
//   error.code === "PGRST202" → return null  (RPC ausente pre-apply, sin heading huérfano)
//   otro error → throw (#34)
//   data (incl. 0 filas) → View con empty honesto
// FilaAudiencia: nombre TEXTO PLANO (0048 no enlaza). En la NUEVA sección el nombre SÍ enlaza.
// Ver fuente oficial ↗ vía safeExternalHref(enlace_detalle), min-h-11, text-accent-product
```

<!-- Montaje EXISTENTE de secciones hermanas en la página + el rail. -->
From app/app/proyecto/[boletin]/page.tsx:
```typescript
// <section id="lobby-tramitacion" className="mt-12"> ... </section>  (0048, NO tocar)
// <section id="cruces" className="mt-12"> gated ... </section>
// Insertar <section id="lobby-menciones" className="mt-12"> DESPUÉS de #lobby-tramitacion y ANTES de #cruces.
// ProyectoRail.navEntries: array de RailEntry {id,label,count?,marker?} — añadir {id:"lobby-menciones", label:"Menciones en lobby"} tras "lobby-tramitacion".
```

<!-- Linter EXISTENTE. Patrón LOCKED: registrar la leyenda que NIEGA en NEGACIONES_LOCKED
     ANTES de añadir la superficie al array de scan. -->
From app/lib/anti-insinuacion-guard.test.ts:
```typescript
const SUPERFICIES_PERSONAS: string[] = [ ... ];  // añadir un SUPERFICIES_LOBBY nuevo (o extender) con las rutas nuevas
const NEGACIONES_LOCKED: string[] = [ ..., LEYENDA_CROSS_LINK ];  // añadir la leyenda LOCKED de menciones (VERBATIM, single-source)
// TERMINOS_PROHIBIDOS ya incluye "influencia" — la leyenda de menciones la NIEGA → DEBE restarse
// el bucle escanea [...VOTO, ...MONEY, ...HOME, ...BUSQUEDA, ...PERSONAS] → añadir el array nuevo
```
</interfaces>

@app/components/lobby-en-tramitacion.tsx
@app/lib/anti-insinuacion-guard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sección "Audiencias que mencionan este boletín" (RPC + degrade honesto + leyenda LOCKED) + tests</name>
  <files>app/components/lobby-menciones-de-boletin.tsx, app/components/lobby-menciones-de-boletin.test.tsx</files>
  <action>Crear `LobbyMencionesView` (pura, props) + `LobbyMencionesSection` (server component)
    espejo estructural de `lobby-en-tramitacion.tsx`, con las diferencias LOCKED del UI-SPEC:
    - RPC `lobby_menciones_de_boletin` con el MISMO degrade honesto de 3 caminos:
      `error.code === "PGRST202"` → return null (pre-apply, sin heading huérfano; el mt-12 del
      wrapper preserva la frontera); otro error → throw (#34); data (incl. 0 filas) → View.
    - Heading LOCKED verbatim (h2, text-xl font-semibold): `Audiencias de lobby que mencionan
      este boletín`.
    - Leyenda anti-causal LOCKED verbatim (1×, exportada como constante single-source para el
      linter): `La materia de estas audiencias menciona el número de este boletín en el registro
      público de la Ley del Lobby (Ley 20.730). La mención es un dato del registro; no implica
      influencia en la tramitación ni relación causal con el proyecto.`
    - Conteo honesto neutro: `{N} audiencia registrada menciona este boletín.` /
      `{N} audiencias registradas mencionan este boletín.` (N en font-mono); si la RPC bounded
      truncó (LIMIT alcanzado, filas < total_n) usar la variante total_n:
      `Se muestran las {mostradas} audiencias más recientes de {total} que mencionan este boletín.`
    - Fila (patrón `border-t first:border-t-0 py-3`, min-w-0): `{fecha mono}` ·
      `<Link petróleo>{formatNombre(nombre)}</Link>` → `/parlamentario/{parlamentario_id}`
      (ENLAZADO — DEPARTURE del 0048; hay evidencia dura → navegación justificada); `Contraparte:
      {nombre crudo}` (verbatim, NUNCA enlazada, sin RUT — si la RPC la emite); `Asunto:
      {materia completa}` en bloque `whitespace-pre-line leading-relaxed` (sin clamp); `Ver fuente
      oficial ↗` vía `safeExternalHref(enlace_detalle)`, ml-auto, min-h-11, text-accent-product —
      omitido si no hay enlace (nunca fabricado). Orden fecha DESC.
    - Empty state LOCKED verbatim (0 menciones, NUNCA "sin lobby"/"limpio"): `Ninguna audiencia de
      lobby registrada menciona el número de este boletín en su materia, según las fuentes
      consultadas. Esto no describe la actividad de lobby en torno al proyecto; solo cuenta las
      materias que citan explícitamente este número de boletín.` (heading + leyenda se mantienen).
    Tests RTL (View pura con fixtures): heading + leyenda visibles; parlamentario enlazado a
    /parlamentario/id; materia completa whitespace-pre-line; conteo honesto singular/plural +
    variante truncada; empty honesto verbatim; contraparte texto plano sin enlace.</action>
  <verify>
    <automated>pnpm --filter ./app test -- lobby-menciones-de-boletin</automated>
  </verify>
  <done>LobbyMencionesSection/View existen, degrade honesto 3 caminos, heading/leyenda/empty LOCKED verbatim, parlamentario enlazado, materia legible, conteo honesto total_n; tests RTL verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Montar la sección en page.tsx + entrada de rail + extender el linter con negaciones LOCKED</name>
  <files>app/app/proyecto/[boletin]/page.tsx, app/lib/anti-insinuacion-guard.test.ts</files>
  <action>(1) MONTAJE: en `app/app/proyecto/[boletin]/page.tsx` insertar la
    `<section id="lobby-menciones" className="mt-12">` con `<Suspense>` + `<LobbyMencionesSection
    boletin={boletin} />`, DESPUÉS de `#lobby-tramitacion` (ambas de lobby, contiguas pero
    distintas) y ANTES de `#cruces`. El h2 y la leyenda viven DENTRO del componente (degrade
    honesto: mt-12 del wrapper preserva la frontera aunque el contenido sea null pre-apply). No
    tocar `#lobby-tramitacion` (0048). Añadir a `ProyectoRail.navEntries` la entrada
    `{ id: "lobby-menciones", label: "Menciones en lobby" }` inmediatamente tras
    `{ id: "lobby-tramitacion", label: "Lobby del período" }`. Actualizar el conteo esperado de
    navEntries si algún test/assert lo fija (comentario ":643 DEBE igualar navEntries").
    (2) LINTER (lección BLOCKER 91, ORDEN LOCKED): PRIMERO importar la leyenda LOCKED de menciones
    (single-source exportada en el Task 1) y añadirla a `NEGACIONES_LOCKED` (contiene "influencia"
    en contexto que la NIEGA → sin restarla el scan se auto-cazaría). LUEGO añadir un array
    `SUPERFICIES_LOBBY` con las rutas nuevas (`components/lobby-menciones-de-boletin.tsx`,
    `components/mencion-boletin-chip.tsx`, `components/lobby-de-parlamentario.tsx`) e incluirlo en
    el bucle de scan `[...VOTO, ...MONEY, ...HOME, ...BUSQUEDA, ...PERSONAS, ...LOBBY]`. Añadir una
    prueba de mutation self-check para el carril LOBBY (término de causalidad/afinidad inyectado en
    un fixture EN MEMORIA es cazado → el guard muerde sobre lo nuevo) y una prueba no-falso-positivo
    (la leyenda LOCKED verbatim → sin offender). Verificar que la leyenda de menciones NO contiene
    ningún término prohibido SIN negar; si lo tuviera, es un bug del copy, no del linter.</action>
  <verify>
    <automated>pnpm --filter ./app test -- anti-insinuacion-guard</automated>
  </verify>
  <done>La sección monta como carril hermano separado + entrada de rail "Menciones en lobby"; el linter escanea las 3 superficies nuevas con la leyenda LOCKED restada en NEGACIONES_LOCKED; mutation self-check LOBBY muerde; sin falsos positivos; suite app verde.</done>
</task>

</tasks>

<verification>
- La sección nueva es HERMANA e independiente (mt-12, id propio, heading/leyenda distintos) — nunca se fusiona con 0048.
- Parlamentario ENLAZADO (navegación bidireccional, LOB-03); contraparte texto plano sin RUT.
- Degrade honesto: RPC ausente pre-apply → null sin heading huérfano; error real → throw; 0 filas → empty honesto verbatim.
- Linter cubre las 3 superficies nuevas con negaciones LOCKED registradas ANTES (lección 91); muerde por mutation self-check.
</verification>

<success_criteria>
- LOB-02: sección de mención explícita en ficha proyecto, separada y rotulada distinto de la temporal.
- LOB-03: navegación PL→audiencias→parlamentario vía links específicos.
- Linter verde con mutation check sobre las superficies nuevas.
- Suite app verde.
</success_criteria>

<output>
Create `.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-03-SUMMARY.md` when done.
</output>
