---
phase: 92-personas-p2c-lobby-legible-audiencia-pl-fail-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/boletin-en-materia.ts
  - app/lib/boletin-en-materia.test.ts
  - supabase/migrations/0062_lobby_menciones_de_boletin.sql
  - supabase/tests/0062_lobby_menciones_de_boletin.test.sql
autonomous: true
requirements: [LOB-02]
must_haves:
  truths:
    - "Un extractor puro encuentra TODOS los boletines mencionados dentro de un texto libre (materia), con las MISMAS reglas de formato del detector de query (14309-04, 14309, 14.309-04) y la misma exclusión de separador decimal"
    - "La RPC lobby_menciones_de_boletin(p_boletin) devuelve solo audiencias confirmadas con parlamentario_id cuya materia menciona ese boletín, PII-safe y bounded"
    - "El regex SQL de boletín en 0062 es demostrablemente equivalente al extractor TS (guard de equivalencia)"
  artifacts:
    - path: "app/lib/boletin-en-materia.ts"
      provides: "Extractor puro de boletines EMBEBIDOS en texto libre + validación de formato"
      min_lines: 25
    - path: "supabase/migrations/0062_lobby_menciones_de_boletin.sql"
      provides: "RPC lobby_menciones_de_boletin + doble-revoke + LIMIT bounded + total_n"
      contains: "lobby_menciones_de_boletin"
    - path: "supabase/tests/0062_lobby_menciones_de_boletin.test.sql"
      provides: "pgTAP deny/bounded/columnas/security-definer"
      contains: "has_function"
  key_links:
    - from: "app/lib/boletin-en-materia.ts"
      to: "app/lib/boletin-detector.ts"
      via: "mismas reglas de formato (portada del mismo detector)"
      pattern: "\\\\d\\{3,6\\}"
    - from: "supabase/migrations/0062_lobby_menciones_de_boletin.sql"
      to: "public.lobby_audiencia"
      via: "materia ~ regex boletín + join a proyecto para existencia"
      pattern: "lobby_audiencia"
---

<objective>
Construir el CANAL DE DATOS fail-closed de la mención audiencia→PL: un extractor puro
de boletines EMBEBIDOS en la materia (texto libre) que espeja el detector de query
(mismos formatos, misma exclusión de decimal), y la migración 0062 con la RPC
`lobby_menciones_de_boletin(p_boletin)` (para la ficha proyecto) más el guard de
equivalencia TS↔SQL. Foundation de los Planes 02 (chips en ficha parlamentario) y 03
(sección en ficha proyecto).

Purpose: LOB-02 exige que el enlace sea SOLO por mención EXPLÍCITA de número de
boletín (fail-closed doble: patrón estricto + existencia en `proyecto`), JAMÁS
keywords/tema. La lógica de extracción y validación es el núcleo determinista de toda
la fase — se define y prueba primero, antes de montar cualquier superficie.

Output: `boletin-en-materia.ts` (+ test), migración 0062 (+ pgTAP), allowlist actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-CONTEXT.md

<interfaces>
<!-- Detector de query EXISTENTE (portada del formato de boletín). El extractor NUEVO
     reusa EXACTAMENTE estas reglas de formato pero opera EMBEBIDO en texto libre. -->
From app/lib/boletin-detector.ts:
```typescript
// Detecta si la QUERY COMPLETA es un boletín (toda la string). Formatos:
//   "14309-04" → {base:"14309", sufijo:"04"} | "14309" → {base, sufijo:null}
//   "14.309-04" (punteado, separador de miles válido) → {base:"14309", sufijo:"04"}
//   "12.34" / "3.14" (decimal) → null
export function detectarBoletin(q: string): { base: string; sufijo: string | null } | null;
// núcleo: hasDotThousands = /^\d{1,3}(\.\d{3})*(-\d{1,2})?$/  ; stripped = quita puntos
//         match final: /^\d{3,6}(-\d{1,2})?$/
```

From packages/fichas/src/spike/boletin.test.ts (patrón WR-03 de guard de equivalencia):
```typescript
// Guard de equivalencia: re-implementación inline + assert de igualdad sobre fixtures.
// El extractor NUEVO añade su propio guard TS↔SQL (formatos idénticos).
```

<!-- RPC de referencia MÁS reciente (patrón doble-revoke + total_n + LIMIT bounded). -->
From supabase/migrations/0061_cross_links_conteo_honesto_orden.sql:
```sql
create or replace function public.co_comisionados_de_parlamentario(p_id text)
returns table (id text, nombre text, camara text, comision_nombre text, total_n bigint)
language sql stable security definer set search_path = '' as $$
  select ..., count(*) over () as total_n from ( ... ) d order by d.nombre limit 20;
$$;
revoke all on function public.co_comisionados_de_parlamentario(text) from public;
revoke all on function public.co_comisionados_de_parlamentario(text) from anon, authenticated;
```

<!-- Short-circuit de boletín en SQL (0055) — cómo se detecta un boletín en Postgres. -->
From supabase/migrations/0055_busqueda_hibrida.sql:
```sql
where q ~ '^\d{3,6}(-\d{1,2})?$'
  and (p.boletin = q or p.boletin_num = split_part(q, '-', 1))
```

<!-- Tabla fuente (0021): materia es texto libre; solo confirmadas con parlamentario_id. -->
From supabase/migrations/0021_lobby.sql:
```sql
create table lobby_audiencia (
  identificador text primary key,
  parlamentario_id text references parlamentario(id) on delete set null,
  estado_vinculo text,   -- 'confirmado' | 'no_confirmado' | null
  materia text,          -- resumen de materia (raw), TEXTO LIBRE
  enlace_detalle text, origen text, fecha_captura timestamptz not null default now()
);
-- proyecto(boletin text, boletin_num text) — boletin_num = base sin sufijo (0008)
```
</interfaces>

@app/lib/boletin-detector.ts
@packages/fichas/src/spike/boletin.test.ts
@supabase/migrations/0061_cross_links_conteo_honesto_orden.sql
@supabase/tests/0061_cross_links_conteo_honesto_orden.test.sql
@app/lib/lockdown-guard.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extractor puro de boletines embebidos en materia + guard de equivalencia TS</name>
  <files>app/lib/boletin-en-materia.ts, app/lib/boletin-en-materia.test.ts</files>
  <behavior>
    - "boletín 14309-04 sobre pesca" → ["14309-04"] (base+sufijo canonicalizado con guion)
    - "proyectos 14.309-04 y 12345" → ["14309-04", "12345"] (punteado normalizado + sin sufijo)
    - "monto de $12.345 (3.14%)" → [] (separador decimal NO es boletín; regla LOCKED)
    - "boletines 14309-04, 14309-04 y 14309-04" → ["14309-04"] (dedupe por boletín distinto)
    - "reunión sobre medio ambiente" → [] (sin número → sin mención)
    - null / "" → [] (materia vacía)
    - "año 2024 ley 20730" → [] o solo lo que matchea 3-6 dígitos con límite de palabra; documentar el caso de 4-5 dígitos sueltos (p.ej. "20730") — decidir en plan si un número pelado sin contexto de boletín cuenta; LOCKED: espejar EXACTAMENTE detectarBoletin (que SÍ acepta "14309" pelado) para no divergir del contrato. La red de seguridad de existencia (Task RPC / Plan 02) descarta los que no existen en proyecto.
  </behavior>
  <action>Crear `extraerBoletines(materia: string | null): string[]` — extrae TODOS los
    boletines EMBEBIDOS en texto libre. A diferencia de `detectarBoletin` (que exige que la
    query COMPLETA sea un boletín), este escanea el texto con un regex de límite de palabra
    tolerante a acentos (espejo del `WORD` de anti-insinuacion-guard.test.ts para el borde) y
    aplica las MISMAS reglas de formato: base de 3-6 dígitos, sufijo opcional `-\d{1,2}`,
    separador de miles válido `\d{1,3}(\.\d{3})*` colapsado, exclusión de punto decimal. Emite
    cada boletín en forma canónica `base` o `base-sufijo` (guion, sin puntos), deduplicado,
    orden ascendente por número. Función pura, sin imports de runtime. Documentar en el JSDoc
    que la validación de EXISTENCIA (fail-closed #2) NO vive aquí — es responsabilidad del
    consumidor (RPC / batched query a proyecto). Añadir el guard de equivalencia TS: una
    re-implementación inline en el test que asegura que ambas copias producen salida idéntica
    sobre el fixture (patrón WR-03 de boletin.test.ts), para cazar drift.</action>
  <verify>
    <automated>pnpm --filter ./app test -- boletin-en-materia</automated>
  </verify>
  <done>extraerBoletines cubre los 7 casos del behavior + el guard de equivalencia pasa; suite app verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migración 0062 — RPC lobby_menciones_de_boletin (fail-closed doble, PII-safe, bounded) + pgTAP</name>
  <files>supabase/migrations/0062_lobby_menciones_de_boletin.sql, supabase/tests/0062_lobby_menciones_de_boletin.test.sql, app/lib/lockdown-guard.test.ts</files>
  <action>Crear la migración 0062 (siguiente libre; última aplicada = 0061) con la RPC
    `lobby_menciones_de_boletin(p_boletin text)` — audiencias cuya materia menciona el boletín,
    para la ficha proyecto. `language sql stable security definer set search_path = ''`, nombres
    schema-qualified, `p_boletin` parametrizado. Fail-closed doble: (1) el regex SQL de mención
    de boletín debe espejar el extractor TS del Task 1 — usar `public.lobby_audiencia.materia ~`
    con un patrón de borde de palabra que matchee `{base}` o `{base}-{sufijo}` de p_boletin
    (construir la comparación desde p_boletin, ambos formatos punteado/plano vía normalización a
    dígitos), documentando la equivalencia con el TS en un comentario (guard de equivalencia — el
    pgTAP prueba filas concretas mencionadas vs no-mencionadas); (2) `join public.proyecto` por
    boletín para que solo se emitan menciones a proyectos EXISTENTES. SOLO
    `estado_vinculo = 'confirmado'` con `parlamentario_id is not null` (fail-closed identidad,
    espejo 0048). Returns table: identificador, fecha, materia, parlamentario_id, nombre público
    (coalesce concat_ws como en 0061, sin rut/email), contraparte(s) crudas si el patrón existente
    lo hace sin RUT (espejo lobby_de_parlamentario — sin contraparte_id/RUT), enlace_detalle,
    fecha_captura/origen (provenance), `total_n = count(*) over ()` (conteo honesto, 0061), orden
    fecha DESC, `limit` bounded. Doble-revoke VERBATIM (`revoke all ... from public;` +
    `from anon, authenticated;`), CERO grant. Cabecera de migración con el comando
    `psql --single-transaction` (NUNCA db push) y nota de aplicar UNA vez.
    pgTAP (espejo 0061.test.sql): has_function firma text, is security definer, anon SIN execute,
    emite total_n, NO filtra rut/email; + al menos un test de comportamiento del fail-closed doble
    (materia que menciona un boletín inexistente NO aparece; materia sin boletín NO aparece). Añadir
    `lobby_menciones_de_boletin` a `PUBLIC_RPC_ALLOWLIST` en lockdown-guard.test.ts.</action>
  <verify>
    <automated>pnpm --filter ./app test -- lockdown-guard</automated>
  </verify>
  <done>0062 tiene la RPC con doble-revoke + total_n + fail-closed doble + LIMIT; pgTAP escrito (deny/bounded/columnas/comportamiento); allowlist actualizado; suite app verde. Apply a PROD + pgTAP contra schema aplicado = Task del Plan 04 (checkpoint operador via psql).</done>
</task>

</tasks>

<verification>
- `extraerBoletines` es puro, cubre los formatos LOCKED y excluye decimales; guard de equivalencia TS verde.
- 0062 es aditiva sobre funciones (sin DROP de tabla, sin backfill) → dentro de la autoridad del agente para escribir; el APPLY a PROD se difiere al Plan 04.
- El regex SQL espeja el extractor TS; la equivalencia queda documentada + probada por pgTAP de comportamiento.
- allowlist incluye la RPC nueva; ninguna RPC servida queda fuera.
</verification>

<success_criteria>
- LOB-02 (canal de datos): extractor determinista + RPC fail-closed doble existen y están probados.
- CERO regex de keywords/tema; solo el patrón de número de boletín cuenta.
- Suite app verde; migración lista para apply (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-01-SUMMARY.md` when done.
</output>
