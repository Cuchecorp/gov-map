---
phase: 15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dinero/package.json
  - packages/dinero/src/model-servel.ts
  - packages/dinero/src/parse-servel.ts
  - packages/dinero/src/reconciliar-aporte.ts
  - packages/dinero/src/reconciliar-completitud.ts
  - packages/dinero/src/connector-servel.ts
  - packages/dinero/src/storage-supabase.ts
  - packages/dinero/src/writer-servel.ts
  - packages/dinero/src/writer-supabase-servel.ts
  - packages/dinero/src/ingest-run-servel.ts
  - packages/dinero/src/ingest-cli-servel.ts
  - packages/dinero/src/index.ts
  - packages/dinero/src/parse-servel.test.ts
  - packages/dinero/src/reconciliar-aporte.test.ts
  - packages/dinero/src/reconciliar-completitud.test.ts
  - packages/dinero/src/storage-supabase.test.ts
  - packages/dinero/src/writer-servel.test.ts
  - packages/dinero/test-fixtures/servel-sample.xlsx
  - .env.example
autonomous: false
requirements: [MONEY-03, MONEY-04]
must_haves:
  truths:
    - "El conector SERVEL pide el .xlsx en el ORDEN LOCKED: assertAllowedUrl (host EXACTO repodocgastoelectoral.blob.core.windows.net, https forzado) entonces robots.isAllowed entonces rateLimiter.wait entonces fetcher.get"
    - "El parser valida por TEXTO de header (no por posicion); una columna renombrada/reordenada/faltante THROW (drift estructural)"
    - "El drift SERVEL es BLOQUEANTE a nivel de RUN: cualquier mismatch de completitud (Content-MD5/byte-length/conteo) pone TODA la corrida en cuarentena y emite CERO filas — nunca un parcial silencioso"
    - "El crudo .xlsx se sube a Supabase Storage (bucket crudo-servel) con clave versionada servel/<eleccion>/<fecha>/<hash>.xlsx, idempotente"
    - "El enlace candidato->parlamentario usa correrPipeline (@obs/adjudication) por NOMBRE del candidato; SOLO determinista o confirmado-humano puebla parlamentario_id (IDENT-12); ambiguo -> cola de revision humana via identidad_audit, no_confirmado/null hasta confirmar (fail-closed)"
    - "Data-routing gate LOAD-BEARING: SOLO el nombre del candidato (funcionario publico) llega al pipeline/LLM; PII/nombre/RUT del donante NUNCA toca el pipeline ni el LLM (assertNoRutInLlmInput/assertSensitivityAllowed intactos en correrPipeline)"
    - "candidato_nombre_verbatim se guarda SIEMPRE; el donante queda verbatim en su sub-maestra deny-by-default, jamas llave de enlace"
    - "El writer es idempotente; sin SUPABASE_SERVICE_KEY el CLI degrada a dry-run (InMemory), nunca fabrica"
  artifacts:
    - path: "packages/dinero/src/parse-servel.ts"
      provides: "xlsx -> Aporte[] VERBATIM + gate de header-text que THROW en drift"
      contains: "EXPECTED_HEADERS"
    - path: "packages/dinero/src/reconciliar-completitud.ts"
      provides: "reconciliacion de completitud run-level -> senal de cuarentena"
      contains: "Content-MD5"
    - path: "packages/dinero/src/reconciliar-aporte.ts"
      provides: "enlace por NOMBRE del candidato via correrPipeline a EnlaceConfirmado o null; SOLO determinista/confirmado puebla; nombre del donante NUNCA al pipeline"
      contains: "correrPipeline"
    - path: "packages/dinero/src/storage-supabase.ts"
      provides: "subida idempotente del crudo a Supabase Storage con clave versionada"
      contains: ".storage.from("
    - path: "packages/dinero/src/connector-servel.ts"
      provides: "ServelConnector (fetch .xlsx, host exacto, captura ETag/Content-MD5/byte-length)"
      contains: "assertAllowedUrl"
  key_links:
    - from: "packages/dinero/src/connector-servel.ts"
      to: "@obs/ingest assertAllowedUrl extraHosts"
      via: "host EXACTO scoped al conector (NO DEFAULT_ALLOWED_SUFFIXES) + assercion https"
      pattern: "extraHosts"
    - from: "packages/dinero/src/reconciliar-aporte.ts"
      to: "@obs/adjudication correrPipeline (cruce del candidato por NOMBRE)"
      via: "correrPipeline(mencion, maestra, provider, writer); SOLO determinista/confirmado -> confirmar(); donante NUNCA al pipeline"
      pattern: "correrPipeline"
    - from: "packages/dinero/src/ingest-run-servel.ts"
      to: "reconciliar-completitud (quarantine boundary = el RUN)"
      via: "mismatch -> cuarentena de toda la corrida, 0 filas"
      pattern: "reconciliarCompletitud|cuarentena"
---

<objective>
Crear el conector SERVEL dentro de `@obs/dinero` espejando los archivos ChileCompra de Phase 14, con TRES divergencias deliberadas: (1) parser `xlsx` verbatim con gate de header-text que THROW en drift; (2) drift BLOQUEANTE a nivel de RUN con reconciliacion de completitud (cuarentena, nunca parcial silencioso); (3) crudo a Supabase Storage (R2 esta 401). El enlace candidato->parlamentario reusa el PIPELINE DE IDENTIDAD confirmado/auditado (`correrPipeline` de `@obs/adjudication`) cruzando por el NOMBRE del candidato (funcionario publico) — espejo de `packages/lobby/src/reconciliar-sujeto.ts`, NO de `reconciliar-contrato.ts`. SOLO `determinista` o `confirmado`-humano puebla `parlamentario_id`; lo ambiguo va a la cola de revision humana (`identidad_audit`) y queda `no_confirmado`/null hasta confirmar (fail-closed). El nombre del candidato es lo UNICO que llega al pipeline/LLM; la PII del donante NUNCA. `candidato_nombre_verbatim` se guarda siempre.

Purpose: El motor de ingesta artesanal y fragil. La postura por defecto ante incertidumbre es CUARENTENA, no emision: una fila silenciosa es peor que ninguna fila. RE-RESOLUCION A1 (operador 2026-06-19, principio de finalidad del dato): SERVEL no trae RUT — vincular a un funcionario publico a su fiscalizacion usa la identidad confirmada/auditada, NO se bloquea por la ausencia de RUT. El homonimo se ADJUDICA (no string-match ingenuo, no fabricacion). El data-routing gate (solo el funcionario al LLM, jamas el donante) es LOAD-BEARING.

Output: `packages/dinero/src/*-servel.ts` + tests + nuevo dep `exceljs@4.4.0` (tras checkpoint) + dep `@obs/adjudication` en package.json + host exacto SERVEL en el allowlist + fixture xlsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-RESEARCH.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-PATTERNS.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-CONTEXT.md

# Analogos ChileCompra COMPLETOS a espejar archivo-por-archivo (leer cada uno antes de su gemelo servel):
@packages/dinero/src/connector-chilecompra.ts
@packages/dinero/src/parse-chilecompra.ts
@packages/dinero/src/model.ts
@packages/dinero/src/writer.ts
@packages/dinero/src/writer-supabase.ts
@packages/dinero/src/ingest-run.ts
@packages/dinero/src/ingest-cli.ts
@packages/dinero/src/index.ts
@packages/dinero/package.json

# ANALOGO CLAVE DEL ENLACE (RE-RESUELTO A1): el enlace del candidato por NOMBRE via pipeline.
# Espejar reconciliar-sujeto.ts (NO reconciliar-contrato.ts) — leer ANTES de escribir reconciliar-aporte.ts:
@packages/lobby/src/reconciliar-sujeto.ts
@packages/lobby/src/reconciliar-sujeto.test.ts
@packages/lobby/src/ingest-run.ts
@packages/lobby/src/ingest-cli.ts
@packages/lobby/package.json

# El pipeline de identidad confirmado/auditado que el enlace consume:
@packages/adjudication/src/pipeline.ts
@packages/adjudication/src/index.ts

# Simbolos de identidad reusados TAL CUAL (NO reimplementar modulo-11):
@packages/identity/src/deterministic.ts
@packages/identity/src/enlace-confirmado.ts

# normalizarNombre del candidato (el SUJETO publico) -> mencion del pipeline:
@packages/core/src/index.ts

# Allowlist SSRF (el host SERVEL va como extraHost EXACTO, NO como sufijo):
@packages/ingest/src/allowlist.ts

<interfaces>
<!-- Simbolos exactos a reusar; NO explorar el codebase, usar estos directamente. -->

ENLACE DEL CANDIDATO POR NOMBRE (RE-RESUELTO A1) — espeja reconciliar-sujeto.ts:

De @obs/adjudication (packages/adjudication/src/pipeline.ts + index.ts):
  export async function correrPipeline(mencion: MencionForanea, maestra: Parlamentario[], provider, writer): Promise<ResultadoPipeline>;
  export type ResultadoPipeline =
    | { tipo: "determinista"; parlamentarioId: string }   // RUT exacto O nombre unico en camara+periodo -> CONFIRMA
    | { tipo: "no_confirmado"; razon: string }             // 0 candidatos tras blocking -> null
    | { tipo: "probable"; parlamentarioId: string }        // LLM auto-acepto -> NUNCA confirmado (A4) -> null
    | { tipo: "revision"; razones: string[] };             // a cola humana via enqueueRevision -> null
  export interface PipelineWriter { upsertVinculo; appendAudit; enqueueRevision; }  // el pipeline escribe identidad_audit
  export type { MencionForanea };   // { nombreOriginal, nombreNormalizado, tokens, camara, periodo, region }
  export { MockMiniMaxProvider };   // provider de test (sin red)
  export { RevisionWriter };        // writer real del pipeline (cola/vinculo/audit) para LIVE
  // GUARDA IDENT-12 (espeja reconciliar-sujeto.ts:196-209): SOLO res.tipo === "determinista" mintea
  //   confirmar(res.parlamentarioId, "determinista") + estado_vinculo "confirmado". probable/revision/
  //   no_confirmado -> enlace null + estado_vinculo "no_confirmado" + candidato_nombre_verbatim crudo.
  // El pipeline DENTRO ya corre assertNoRutInLlmInput + assertSensitivityAllowed sobre el prompt EXACTO
  //   ANTES de complete (pipeline.ts:145-153): el RUT NUNCA cruza al LLM. NO replicar ese gate aqui;
  //   reusarlo via correrPipeline. La mencion que se le pasa NO transporta RUT por diseno.

De @obs/core (packages/core/src/index.ts):
  export function normalizarNombre(input: { libre: string }): { nombre_normalizado: string; tokens: string[] };
  export type Parlamentario;   // la maestra (RUT interno puede estar vacio — IDENT-10 — el cruce por NOMBRE no lo necesita)

De @obs/identity (packages/identity/src/enlace-confirmado.ts):
  export interface EnlaceConfirmado { readonly parlamentarioId: string; readonly metodo: "determinista"|"humano"; }
  export function confirmar(parlamentarioId: string, metodo?): EnlaceConfirmado;  // UNICA factory; string crudo NO compila
  // FK plano que escribe el writer: parlamentario_id: enlace?.parlamentarioId ?? null

DATA-ROUTING GATE (LOAD-BEARING) — quien va al pipeline/LLM:
  - SOLO el nombre del candidato (NOMBRE CANDIDATO-PARTIDO POLITICO) -> normalizarNombre -> MencionForanea -> correrPipeline.
  - El donante (NOMBRE APORTANTE / tipo / eventual RUT) JAMAS entra a correrPipeline ni al LLM. Queda verbatim en la
    sub-maestra `donante` (deny-by-default, Plan 01). El donante NUNCA es llave de enlace.

De @obs/ingest:
  Fetcher, HostRateLimiter, RobotsGuard, assertAllowedUrl, type AllowlistOptions
  // ORDEN LOCKED: assertAllowedUrl(url, allowlist) entonces robots.isAllowed(url) entonces rateLimiter.wait(host) entonces fetcher.get(...)
  // assertAllowedUrl(url, { extraHosts: [SERVEL_HOST] }) admite el host EXACTO. CAVEAT (allowlist.ts:116-123):
  //   extraHosts permite TAMBIEN http; SERVEL es https-only -> agregar una assercion u.protocol === "https:" para ese host.
  // NUNCA agregar el host a DEFAULT_ALLOWED_SUFFIXES (ampliaria SSRF a todo tenant Azure).

SERVEL source (research-confirmado live 2026-06-19):
  Host EXACTO: "repodocgastoelectoral.blob.core.windows.net"  (Azure Blob; GET anonimo; .xlsx por eleccion)
  La URL por eleccion la pasa el OPERADOR via --url (no hardcodear el patron de path).
  HTTP HEAD trae: ETag, Content-MD5 (base64), Last-Modified, Content-Length (byte-length).
  Hoja unica (row 4 = headers), 11 columnas, ~16k filas. Headers VERBATIM (UPPERCASE):
    "TIPO DE APORTE","NOMBRE APORTANTE","TIPO APORTANTE","NOMBRE CANDIDATO-PARTIDO POLITICO",
    "TIPO DONATARIO","ELECCION","TERRITORIO ELECTORAL","PACTO","PARTIDO","FECHA DE TRANSFERENCIA","MONTO"
  Mapeo: NOMBRE APORTANTE->donante_nombre; TIPO APORTANTE->tipo_persona; NOMBRE CANDIDATO-PARTIDO POLITICO->candidato_nombre_verbatim;
         ELECCION + TERRITORIO ELECTORAL + anio(titulo) -> eleccion (verbatim concat, p.ej. "DIPUTADO - DISTRITO 23 - 2025");
         FECHA DE TRANSFERENCIA->fecha_aporte; MONTO->monto (string verbatim, NUNCA numeric); TIPO DE APORTE->tipo_aporte.
  La fuente NO trae RUT (ni donante ni candidato), solo NOMBRES -> el enlace del candidato es por NOMBRE via pipeline
  (homonimo adjudicado, NO string-match), SOLO determinista/confirmado puebla; el resto queda null (fail-closed honesto).

Aporte (mapea a 0024_servel.sql del Plan 01):
  fuente_id (id sintetico = hash de la fila + eleccion), fecha_corte, eleccion (NON-NULL),
  donante_nombre, tipo_persona, monto (string verbatim), fecha_aporte (date|null), tipo_aporte,
  candidato_nombre_verbatim, territorio, pacto, partido, estado_vinculo ("confirmado"|"no_confirmado"|null),
  parlamentario_id (= enlace?.parlamentarioId ?? null), origen ("servel"), fecha_captura, enlace, licencia ("terminos por verificar").
Donante (sub-maestra): donante_id (hash nombre+tipo), rut_donante (null hoy), nombre, tipo_persona, provenance.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human" status="done" resolution="vetted by orchestrator (MIT, exceljs/exceljs, 10M wk dl)">
  <what-built>El plan requiere un parser de xlsx bajo Node/tsx. RESEARCH (Package Legitimacy Audit) recomienda `exceljs@4.4.0` y marca tanto `exceljs` como la alternativa `@e965/xlsx@0.20.3` como [ASSUMED] porque slopcheck 0.6.1 esta ROTO en Windows (shell-out a npm -> WinError 2). El paquete bare `xlsx@0.18.5` esta REMOVIDO (CVE-2023-30533 + abandonado en el registry). NINGUN paquete se ha instalado todavia. (`@obs/adjudication` es un paquete del workspace ya existente — NO requiere legitimacy gate.)</what-built>
  <how-to-verify>
Verificar la legitimidad del paquete xlsx ANTES de instalarlo (host Windows -> usar el Bash tool / git-bash, NO PowerShell). Slopcheck no corre en Windows; verificacion manual:

  1. Abrir https://www.npmjs.com/package/exceljs y confirmar: repo `github.com/exceljs/exceljs`, 160+ versiones, modificado 2024-12, maintainers `guyonroche`/`siemienik`, descargas altas.
  2. (Opcional alternativa mas liviana) revisar https://www.npmjs.com/package/@e965/xlsx y confirmar que es el mirror legitimo de SheetJS (0.20.3).
  3. Elegir UNO. La recomendacion por defecto es `exceljs@4.4.0` (mantenido sin ambiguedad). El bare `xlsx` NO se usa jamas (CVE + abandonado).
  4. Aprobar la instalacion: el executor correra `pnpm --filter @obs/dinero add exceljs@4.4.0` (o el `@e965/xlsx` elegido) y registrara la eleccion en el SUMMARY.

Este checkpoint NUNCA es auto-aprobable (legitimacy gate; ignora auto_advance). Si el operador no aprueba, el plan se detiene aqui.
  </how-to-verify>
  <resume-signal>Escribir "aprobado: exceljs@4.4.0" (o "aprobado: @e965/xlsx@0.20.3") o describir el bloqueo de legitimidad.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: package add xlsx + @obs/adjudication dep + model-servel + parse-servel + reconciliar-aporte (enlace por NOMBRE via pipeline) + reconciliar-completitud (nucleo puro)</name>
  <files>packages/dinero/package.json, packages/dinero/src/model-servel.ts, packages/dinero/src/parse-servel.ts, packages/dinero/src/reconciliar-aporte.ts, packages/dinero/src/reconciliar-completitud.ts, packages/dinero/src/parse-servel.test.ts, packages/dinero/src/reconciliar-aporte.test.ts, packages/dinero/src/reconciliar-completitud.test.ts, packages/dinero/test-fixtures/servel-sample.xlsx</files>
  <behavior>
    - parse header OK: una hoja con los 11 headers exactos -> Aporte[] con campos VERBATIM (monto como string crudo, eleccion compuesta verbatim, sin computo).
    - parse drift de header: un header renombrado/faltante/reordenado -> THROW "drift estructural SERVEL" (cuarentena aguas arriba), NUNCA 0 filas silenciosas. Test: quitar/renombrar "MONTO".
    - parse eleccion load-bearing: una fila sin ELECCION construible -> es drift -> THROW (eleccion es NON-NULL en 0024).
    - reconciliar-aporte determinista: un candidato cuyo NOMBRE resuelve `determinista` en la maestra (nombre unico en camara+periodo) via correrPipeline -> enlace = confirmar(id,"determinista"), estado_vinculo "confirmado", parlamentario_id poblado, id en el set confirmados. (MockMiniMaxProvider; el determinista corta antes del LLM, 0 llamadas.)
    - reconciliar-aporte ambiguo/probable/revision/no_confirmado: el candidato no resuelve determinista -> enlace null, estado_vinculo "no_confirmado", parlamentario_id null, candidato_nombre_verbatim preservado. NUNCA un parlamentario_id poblado en estos casos (IDENT-12). El caso de revision deja el pipeline encolar via writer (espia).
    - reconciliar-aporte sin provider real: un candidato homonimo que llegaria al LLM degrada ESE aporte a no_confirmado (fail-closed, NUNCA aborta la corrida ni fabrica enlace); un determinista resuelve igual. (Espeja reconciliar-sujeto.ts:181-193.)
    - reconciliar-aporte DATA-ROUTING (LOAD-BEARING): el nombre/tipo/eventual RUT del DONANTE NUNCA se pasa a correrPipeline. Test: un writer-espia del pipeline registra que SOLO el nombre del candidato fue la mencion; el donante jamas aparece en ninguna escritura de identidad ni en el prompt.
    - reconciliar-completitud Content-MD5 mismatch: ctrl.contentMd5 != bytesMd5 -> { ok:false } (descarga parcial/alterada).
    - reconciliar-completitud byte-length mismatch: Content-Length declarado != bytes recibidos -> { ok:false }.
    - reconciliar-completitud conteo declarado (best-effort): si hay TOTAL numerico parseable y != parsed.length -> { ok:false }; si no hay TOTAL, Content-MD5 + byte-length bastan -> { ok:true }.
  </behavior>
  <action>
PRIMERO: instalar el dep aprobado en el checkpoint anterior: `pnpm --filter @obs/dinero add exceljs@4.4.0` (o el `@e965/xlsx` elegido). Registrar la eleccion exacta en el SUMMARY. NO instalar bare `xlsx`. AGREGAR ADEMAS la dependencia de workspace `@obs/adjudication` a `packages/dinero/package.json` (`"@obs/adjudication": "workspace:*"`, junto a `@obs/identity`/`@obs/ingest`) — el enlace por NOMBRE la requiere, igual que `packages/lobby/package.json` la declara. Correr `pnpm install` para enlazar el workspace.

`model-servel.ts` (espejo de `model.ts`; NO modificar `model.ts` de ChileCompra): tipos `Aporte` y `Donante` + Zod `AporteSheetSchema` (forma de la fila parseada, antes de mapear). Campos literales como `z.string().nullable()`, sin sumas. `Aporte` lleva provenance inline + `eleccion` NON-NULL + `candidato_nombre_verbatim` + `monto` verbatim string + `estado_vinculo` ('confirmado'|'no_confirmado'|null) + `parlamentario_id` plano (string|null). `Donante` keyed por `donante_id` (hash nombre+tipo), `rut_donante` nullable (null hoy). Constantes `ORIGEN_SERVEL = "servel"`, `LICENCIA_SERVEL = "terminos por verificar"` (NO 'CC BY 4.0', NO 'mencion de la fuente'). Helper `fuenteIdDe(fila, eleccion)` (hash estable sha256 hex via crypto de plataforma).

`parse-servel.ts` (espejo de `parse-chilecompra.ts`, pero xlsx en vez de JSON): funcion PURA sin red. Recibe los bytes del .xlsx, los lee con el parser elegido. GATE DE HEADER-TEXT (Pattern 2 de RESEARCH): leer la fila de headers (row 4), normalizar `String(h).trim().toUpperCase()`, comparar contra `EXPECTED_HEADERS` (los 11 literales); si falta/renombra/reordena cualquiera -> `throw new Error("drift estructural SERVEL: faltan/cambiaron columnas [...]")`. NUNCA mapear por indice posicional. Mapear a `Aporte[]` VERBATIM: `monto` como string crudo (jamas numeric), `eleccion` compuesta verbatim (ELECCION + TERRITORIO ELECTORAL + anio del titulo), `candidato_nombre_verbatim` desde "NOMBRE CANDIDATO-PARTIDO POLITICO", `donante_nombre` desde "NOMBRE APORTANTE" (puede ser "-" para aporte menor anonimo). Una fila sin `eleccion` construible es drift -> THROW. Provenance inline por fila. Sort deterministico para idempotencia de tests.

`reconciliar-aporte.ts` (espejo de `packages/lobby/src/reconciliar-sujeto.ts` — NO de `reconciliar-contrato.ts`): el candidato (funcionario publico) se cruza por NOMBRE via `correrPipeline`. Mantener la FORMA de salida (filas con `enlace: EnlaceConfirmado | null` + `candidato_nombre_verbatim` crudo + `estado_vinculo` + set `parlamentariosConfirmados`). Por cada aporte:
  1. Tomar `candidato_nombre_verbatim` (el SUJETO publico). Si esta vacio -> enlace null, estado null (no hay a quien cruzar), candidato_nombre_verbatim crudo preservado.
  2. `const { nombre_normalizado, tokens } = normalizarNombre({ libre: candidatoNombre });` armar `MencionForanea { nombreOriginal: candidatoNombre, nombreNormalizado: nombre_normalizado, tokens, camara, periodo, region: null }` (espeja reconciliar-sujeto.ts:167-178). El blocking filtra por periodo (parametrizable via opts, default como reconciliar-sujeto).
  3. `res = await correrPipeline(mencion, maestra, provider, writer)` — dentro de un try/catch que, SIN provider real inyectado (proveedorAusente), degrada ESE aporte a no_confirmado (fail-closed, no aborta la corrida); con provider real, el error del LLM propaga (espeja reconciliar-sujeto.ts:179-193).
  4. GUARDA LOCKED IDENT-12 (espeja reconciliar-sujeto.ts:196-209): `switch (res.tipo)` -> `case "determinista": enlace = confirmar(res.parlamentarioId, "determinista"); estado_vinculo = "confirmado"; confirmados.add(res.parlamentarioId);` -> `probable | revision | no_confirmado | default: enlace = null; estado_vinculo = "no_confirmado";`. NUNCA poblar parlamentario_id fuera de determinista; lo ambiguo lo encola el pipeline en `identidad_audit` y queda en cola humana.
provider/writer INYECTABLES con defaults seguros (PROVIDER_AUSENTE que lanza si lo invocan + NOOP_WRITER), copiando los patrones `PROVIDER_AUSENTE`/`NOOP_WRITER`/`proveedorAusente` de reconciliar-sujeto.ts:43-67,154. Tipar el provider como `Parameters<typeof correrPipeline>[2]` (evita edge directo a @obs/llm, igual que reconciliar-sujeto.ts:37).
DATA-ROUTING GATE LOAD-BEARING: SOLO `candidatoNombre` arma la mencion que va a `correrPipeline`. El `donante_nombre`, `tipo_persona` del donante y cualquier RUT de donante JAMAS se pasan a `correrPipeline` ni se incluyen en la mencion. (El gate `assertNoRutInLlmInput`/`assertSensitivityAllowed` vive DENTRO de correrPipeline; aqui basta con no rutear el donante hacia el pipeline.) PROHIBIDO: pasar cualquier campo del donante a correrPipeline; reimplementar el pipeline o el LLM; string-match ingenuo del nombre del candidato (el pipeline adjudica el homonimo).

`reconciliar-completitud.ts` (NUEVO, sin analogo directo): funcion PURA `reconciliarCompletitud(parsed: Aporte[], ctrl: ControlTotal, bytes: { md5: string; length: number })` -> `{ ok: true } | { ok: false; motivo: string }`. Control primario SIEMPRE disponible: Content-MD5 (base64 del HEAD) == md5 de los bytes recibidos, Y Content-Length declarado == bytes.length. Control secundario best-effort: si `ctrl.declaredRowCount` es parseable de un TOTAL numerico, comparar con `parsed.length`. Cualquier mismatch -> `{ ok:false, motivo }`. Este es el corazon del drift BLOQUEANTE: el orquestador (Task 3) usa el resultado para poner TODA la corrida en cuarentena.

`test-fixtures/servel-sample.xlsx`: un .xlsx pequeno (recortar el archivo real a ~5 filas con los 11 headers exactos) commiteado como asset de test. Generarlo programaticamente con el parser elegido (escribir un .xlsx de 5 filas) o recortar el real; debe tener los headers VERBATIM en row 4.

Tests: cubrir TODO el bloque `<behavior>` con el fixture xlsx y fixtures sinteticos (sin red, sin DB). Para reconciliar-aporte usar `MockMiniMaxProvider` (@obs/adjudication) + un `PipelineWriter` espia in-memory (copiar el patron `SpyWriter` de reconciliar-sujeto.test.ts:22-38). El test de data-routing asierta que el SpyWriter NUNCA recibe el nombre del donante (la mencion escrita es la del candidato).

Archivos SIN BOM ni unicode invisible (solo UTF-8 plano; referir codepoints por nombre, jamas pegar un BOM literal).
  </action>
  <verify>
    <automated>cd packages/dinero && pnpm test 2>&1 | tail -25</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test` verde para parse-servel, reconciliar-aporte, reconciliar-completitud. `parse-servel.ts` contiene `EXPECTED_HEADERS` y THROW en drift. `reconciliar-aporte.ts` contiene `correrPipeline` y `confirmar(` y `normalizarNombre`, importa de `@obs/adjudication`, SOLO `res.tipo === "determinista"` puebla el FK, y NO pasa ningun campo del donante a `correrPipeline`. `reconciliar-completitud.ts` chequea Content-MD5 + byte-length. `model-servel.ts` usa licencia "terminos por verificar". `package.json` declara `@obs/adjudication`. El dep xlsx instalado es `exceljs` (o `@e965/xlsx`), nunca bare `xlsx`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: connector-servel + storage-supabase + writer-servel + writer-supabase-servel + index + allowlist-verify + .env.example</name>
  <files>packages/dinero/src/connector-servel.ts, packages/dinero/src/storage-supabase.ts, packages/dinero/src/writer-servel.ts, packages/dinero/src/writer-supabase-servel.ts, packages/dinero/src/index.ts, packages/dinero/src/storage-supabase.test.ts, packages/dinero/src/writer-servel.test.ts, packages/ingest/src/allowlist.ts, .env.example</files>
  <behavior>
    - storage clave versionada: subirCrudo(eleccion, fechaCorte, bytes) construye la clave `servel/<eleccion>/<fecha>/<sha256hex>.xlsx`.
    - storage idempotente: re-subir el mismo contenido (mismo hash en la clave) no es error; un 409/duplicate se traga, cualquier otro error THROW. Test con cliente fake.
    - writer idempotente: upsert del mismo lote dos veces -> mismos conteos; clave de version (fuenteId, fechaCorte).
    - writer marcarIngestado: tras una corrida, aportes_ingesta_estado registra los parlamentarios con ingestado_hasta.
    - writer donante sub-maestra: upsert keyed por donante_id last-write-wins.
    - cli sin SUPABASE_SERVICE_KEY o --dry-run: usa InMemory writer, no toca red de escritura, nunca fabrica.
  </behavior>
  <action>
`connector-servel.ts` (espejo de `connector-chilecompra.ts`): inyectar deps `{ fetcher, rateLimiter, robots, allowlist? }`. Constante `SERVEL_HOST = "repodocgastoelectoral.blob.core.windows.net"`. Metodo `descargar(url)` en el ORDEN LOCKED EXACTO: `assertAllowedUrl(url, { extraHosts: [SERVEL_HOST] })` entonces ASSERCION https explicita para SERVEL_HOST (allowlist.ts:116-123 permite http en extraHosts; SERVEL es https-only -> `if (new URL(url).protocol !== "https:") throw ...`) entonces `await robots.isAllowed(url)` entonces `await rateLimiter.wait(host)` (delay 2-3s) entonces `fetcher.get({ url, headers: HEADERS_SERVEL })` pidiendo los BYTES del .xlsx (Accept del content-type spreadsheet). Capturar de la respuesta: `etag`, `contentMd5` (header Content-MD5, base64), `lastModified`, `byteLength` (Content-Length) — anclas de drift/idempotencia. Clase `ServelBloqueadaError(url, status)` (espejo de `ChileCompraBloqueadaError`): mapear 403/503/429 a degradacion honesta. NUNCA `BaseConnector.run` (su cache diaria saltaria re-corridas). NUNCA interpolar secretos ni la URL cruda en mensajes de error (sanitizar como ChileCompra).

`storage-supabase.ts` (NUEVO, GREENFIELD — grep `.storage.from(` en el repo = 0 hits): helper que construye el cliente service-key (espejo de `writer-supabase.ts:createClient(url, serviceKey, { auth: { persistSession:false, autoRefreshToken:false }})`) y expone `subirCrudo(bucket, eleccion, fechaCorte, bytes)`. Clave versionada `servel/${eleccionSlug}/${fechaCorte}/${sha256Hex}.xlsx` (sha256 hex de los bytes via crypto de plataforma). `client.storage.from(bucket).upload(key, bytes, { contentType: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", upsert: false })`. Un error cuyo mensaje matchea `/exists|duplicate|409/i` ES idempotencia esperada -> tragar (no error); cualquier otro -> `throw new Error(\`storage SERVEL: ${error.message}\`)`. El bucket `crudo-servel` lo crea el OPERADOR (Task 3); el helper NO lo crea en runtime. Service key SOLO de env, jamas interpolada en errores. Aceptar un `client` pre-construido para tests (cliente fake).

`writer-servel.ts` (espejo de `writer.ts`): interface `ServelWriter` con `upsertAportes(...)` (versionado por (fuenteId, fechaCorte), idempotente), `upsertDonantes(...)` (sub-maestra keyed por donante_id, last-write-wins) y `marcarIngestado(parlamentarioIds, hasta)`. `InMemoryServelWriter` (fake para dry-run y tests).

`writer-supabase-servel.ts` (espejo de `writer-supabase.ts`): `SupabaseServelWriter` con el mismo patron de cliente service-key. Upsert idempotente por chunks: `aporte` con `onConflict: "fuente_id,fecha_corte"`; `donante` con `onConflict: "donante_id"`; `aportes_ingesta_estado` con `onConflict: "parlamentario_id"`. FK plano `parlamentario_id: f.enlace?.parlamentarioId ?? null` (poblado SOLO cuando el enlace es confirmado/determinista). En error `throw new Error(\`upsert aporte fallo: ${error.message}\`)`. Reusar el helper de storage (composicion) para subir el crudo.

`index.ts` (MODIFY): agregar barrel re-exports de los simbolos publicos servel. NO re-exportar el simbolo branded `ENLACE_CONFIRMADO`.

`packages/ingest/src/allowlist.ts` VERIFICAR: confirmar que NO se agrega `repodocgastoelectoral.blob.core.windows.net` (ni `core.windows.net`, ni `blob.core.windows.net`) a `DEFAULT_ALLOWED_SUFFIXES` — eso ampliaria SSRF a todo tenant Azure (Pitfall 3 de RESEARCH). El host SERVEL se pasa SOLO como `extraHosts` desde el conector. Reportar en el SUMMARY: "allowlist.ts sin cambios; host SERVEL via extraHosts scoped al conector". Si por error alguien agrego el sufijo, REVERTIRLO.

`.env.example` agregar `SERVEL_CRUDO_BUCKET=crudo-servel` (opcional, defaultable en codigo) con comentario: bucket de Supabase Storage para el crudo; el operador lo crea (Task 3). NO agregar credencial SERVEL (la fuente es publica, GET anonimo). Mantener `MONEY_PUBLIC_ENABLED=false` ya presente. NOTA: el enlace por NOMBRE usa el provider LLM del pipeline (`@obs/adjudication`) — las credenciales del provider ya estan en `.env` desde Phase 4/11; NO duplicarlas aqui.

Tests (`storage-supabase.test.ts`, `writer-servel.test.ts`): cubrir el bloque `<behavior>` con clientes fake / InMemory.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd packages/dinero && pnpm test 2>&1 | tail -25</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test` verde (incluye storage-supabase.test.ts y writer-servel.test.ts). `connector-servel.ts` contiene `assertAllowedUrl`, `extraHosts`, `SERVEL_HOST`, la assercion https y `ServelBloqueadaError`. `storage-supabase.ts` contiene `.storage.from(` y la clave `servel/`. `packages/ingest/src/allowlist.ts` NO contiene `windows.net` en `DEFAULT_ALLOWED_SUFFIXES`. `.env.example` contiene `SERVEL_CRUDO_BUCKET` y NO agrega credencial SERVEL. `index.ts` no re-exporta `ENLACE_CONFIRMADO`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ingest-run-servel (drift BLOQUEANTE run-level + cruce por NOMBRE) + ingest-cli-servel</name>
  <files>packages/dinero/src/ingest-run-servel.ts, packages/dinero/src/ingest-cli-servel.ts, packages/dinero/src/index.ts</files>
  <behavior>
    - corrida OK: header OK + completitud OK -> parse -> reconciliar (cruce del candidato por NOMBRE) -> subirCrudo -> upsert aportes/donantes -> marcarIngestado.
    - drift de header (parse THROW): la corrida ENTERA va a cuarentena, 0 filas emitidas, degradacion{cuarentena:true}, marcador NO cuenta como "enlazado". NUNCA emite un subconjunto.
    - completitud mismatch (Content-MD5/byte-length/conteo): la corrida ENTERA va a cuarentena, 0 filas, degradacion{cuarentena:true}. NUNCA un parcial silencioso.
    - fuente bloqueada (ServelBloqueadaError): degradacion honesta, 0 filas para esa eleccion, continua sin abortar el proceso.
    - enlace honesto: un candidato determinista puebla parlamentario_id; uno ambiguo queda null + en cola de revision humana (via el writer del pipeline), nunca un parlamentario_id fabricado.
    - dry-run / sin service key: InMemory writer, nunca fabrica.
  </behavior>
  <action>
`ingest-run-servel.ts` (espejo de `ingest-run.ts` de dinero para la maquinaria de cuarentena/degradacion, + el patron de wiring de reconciliacion de `packages/lobby/src/ingest-run.ts` para inyectar provider/writer/periodo al cruce por NOMBRE). El quarantine boundary es el RUN (un archivo de eleccion), NO la fila. Reusar `DegradacionDinero { fuente, motivo, cuarentena? }` + `cuarentenados[]` — NO reinventarla. Aceptar en las opts un `reconciliar?` ({ provider?, writer?, periodo? }) que se pasa TAL CUAL a `reconciliarAporte` (espeja `runIngestLobby` opts.reconciliar). Flujo por corrida (una eleccion):
  1. `connector.descargar(url)` -> bytes + { contentMd5, byteLength, etag }. ServelBloqueadaError -> push degradacion, continue.
  2. `parse-servel(bytes)`. Si THROW (drift de header) -> CUARENTENA DE TODA LA CORRIDA: push `degradacion{cuarentena:true, motivo:"drift estructural"}`, emitir CERO filas, NO upsert, marcar y continuar. (Divergencia clave vs ChileCompra que tolera por-tarea: aqui es WHOLE-RUN.)
  3. `reconciliarCompletitud(parsed, ctrl, { md5: md5(bytes), length: bytes.length })`. Si `!ok` -> CUARENTENA DE TODA LA CORRIDA (igual que el paso 2): 0 filas, degradacion{cuarentena:true, motivo}. NUNCA emitir un parcial.
  4. Solo si header OK Y completitud OK: `subirCrudo(...)` (Supabase Storage; reemplaza el no-op "R2 BLOQUEADO" de ingest-run.ts:13), luego `await reconciliarAporte(parsed, maestra, opts.reconciliar ?? {})` (ASYNC ahora — el cruce por NOMBRE usa correrPipeline), `writer.upsertDonantes(...)`, `writer.upsertAportes(...)`. Acumular `parlamentariosConfirmados` para el marcador.
  5. Al final: `writer.marcarIngestado([...marcados], hasta)`.
GUARDA EXPLICITA: prohibido cualquier code path que haga `upsertAportes` de un subconjunto tras capturar un error de parse/completitud (Pitfall 1 warning sign). El test debe asertar 0 upserts en el caso drift.

`ingest-cli-servel.ts` (espejo de `ingest-cli.ts` de dinero + el wiring de provider/writer del pipeline de `packages/lobby/src/ingest-cli.ts`): parse de flags antes de cualquier red o DB. Flag `--eleccion <slug>` + `--url <blob-url>` (la URL la pasa el operador; A2: no hardcodear el patron) + `--dry-run`. Service key + url de Supabase SOLO de env (nunca argv). `--dry-run` o falta de service key -> `InMemoryServelWriter` (degrada a dry-run, NUNCA fabrica). Construir colaboradores REALES de @obs/ingest (`new Fetcher()`, `new HostRateLimiter()`, `new RobotsGuard({ allowlist: {} })`) + `ServelConnector`. Para el cruce por NOMBRE, construir/inyectar el `reconciliar` opts: en LIVE el provider LLM real + `RevisionWriter` (@obs/adjudication) — MISMO patron que lobby usa para su sujeto pasivo; el provider/credenciales del LLM ya existen desde Phase 4/11. En dry-run o sin provider, el cruce degrada a no_confirmado (PROVIDER_AUSENTE), nunca fabrica. La maestra se inyecta (tests) o se carga por el caller; vacia en dry-run. Guarda `isMain` para el entry-point.

`index.ts` (MODIFY): re-exportar `ingestRunServel` / CLI segun el patron de barrel.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd packages/dinero && pnpm test 2>&1 | tail -25</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test` verde (incluye el test de cuarentena run-level). `ingest-run-servel.ts`: en drift de header o mismatch de completitud emite CERO filas para toda la corrida (test asierta 0 upserts) y marca `cuarentena:true`; NUNCA un parcial; el cruce del candidato es async (correrPipeline) y solo determinista puebla parlamentario_id. `ingest-cli-servel.ts` degrada a dry-run sin service key y wirea provider+RevisionWriter del pipeline en LIVE (degrada a no_confirmado sin provider). `subirCrudo` se invoca solo cuando header+completitud OK.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>El conector SERVEL esta completo y la suite unit pasa (parse, reconciliar por NOMBRE via pipeline con MockMiniMaxProvider + writer espia, completitud, storage con cliente fake, writer InMemory, cuarentena run-level; sin red, sin DB real, sin LLM real). Faltan: (a) crear el bucket de Supabase Storage `crudo-servel`; (b) una corrida LIVE acotada con una URL real de eleccion (operador), que ademas ejercita el pipeline de identidad real (provider LLM + RevisionWriter contra `identidad_audit`). La forma del .xlsx esta derivada del live probe de research, no de una corrida en este pipeline.</what-built>
  <how-to-verify>
Pasos de operador (host Windows -> Bash tool / git-bash, NO PowerShell):

  1. CREAR el bucket privado `crudo-servel` en Supabase Storage (dashboard Storage -> New bucket, privado; o `insert into storage.buckets(id,name,public) values('crudo-servel','crudo-servel',false)`). El conector usa la service key (bypassa Storage RLS).
  2. Exportar `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` y las credenciales del provider LLM (ya en `.env` desde Phase 4/11) en el entorno (nunca argv).
  3. Corrida LIVE acotada de una eleccion, respetando delay 2-3s:
     `pnpm --filter @obs/dinero exec tsx src/ingest-cli-servel.ts --eleccion presidencial-parlamentaria-2025-definitivas --url "https://repodocgastoelectoral.blob.core.windows.net/public/Presidencial_Parlamentaria_2025/Definitivas/Reporte_Aportes_DEFINITIVAS_2025.xlsx"`
  4. Confirmar: (a) el crudo .xlsx aparece en `crudo-servel` bajo `servel/<eleccion>/<fecha>/<hash>.xlsx`; (b) el gate de header NO disparo cuarentena (los 11 headers coinciden); (c) la reconciliacion de completitud paso (Content-MD5/byte-length OK); (d) el cruce del candidato por NOMBRE via el pipeline poblo parlamentario_id SOLO en deterministas (nombre unico en camara+periodo); los homonimos/ambiguos quedaron null + encolados en `identidad_audit` para revision humana (fail-closed — correcto, NO bug); (e) DATA-ROUTING: ningun nombre/RUT de DONANTE viajo al LLM (solo el candidato) — verificar en los logs/audit que las menciones adjudicadas son nombres de candidatos, jamas de donantes; (f) re-correr produce los mismos conteos (idempotente) y no duplica el crudo.

Si el header drifteo (eleccion nueva con columnas cambiadas), la corrida debe quedar en CUARENTENA con 0 filas — ese es el comportamiento correcto, reportarlo. Sin service key el CLI degrada a dry-run; sin provider LLM el cruce degrada a no_confirmado; ambos son correctos. NUNCA fabricar datos ni un enlace.
  </how-to-verify>
  <resume-signal>Escribir "bucket creado, LIVE OK: crudo subido + header/completitud verde + enlaces solo-determinista poblados (ambiguos en cola humana) + donante NUNCA al LLM + idempotente" o describir la cuarentena/delta.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/dinero test` verde (parse drift, completitud mismatch, reconciliar por NOMBRE solo-determinista, data-routing donante-jamas-al-pipeline, storage idempotente, writer, cuarentena run-level).
- `parse-servel.ts` gatea por header-text y THROW en drift; `reconciliar-aporte.ts` cruza el candidato por NOMBRE via correrPipeline, SOLO determinista puebla, donante nunca al pipeline.
- `ingest-run-servel.ts` cuarentena BLOQUEANTE run-level: 0 filas en cualquier mismatch.
- `connector-servel.ts` host EXACTO via extraHosts + https forzado; allowlist.ts SIN sufijo windows.net.
- `package.json` declara `@obs/adjudication`.
- Crudo a Supabase Storage con clave versionada idempotente.
- Bucket creado + corrida LIVE confirmada (operador): enlaces solo-determinista, ambiguos en cola humana, donante jamas al LLM.
</verification>

<success_criteria>
- Criterio 1 (MONEY-03): el conector SERVEL de `@obs/dinero` ingiere aportes verbatim y crea la sub-maestra `donante` en este bloque.
- Criterio 2 (MONEY-03): drift BLOQUEANTE; corrida parcial -> cuarentena con reconciliacion de completitud, nunca filas silenciosas; el crudo va a object storage (Supabase Storage; R2 es deuda de operador, 401).
- Criterio 4 (MONEY-04): enlace del candidato por NOMBRE via el pipeline de identidad confirmado/auditado (homonimo adjudicado, NO string-match); SOLO determinista/confirmado puebla parlamentario_id (IDENT-12), lo ambiguo a cola humana -> null (estado honesto), nunca un enlace fabricado, nunca una fila silenciosa. PII del donante jamas toca el pipeline/LLM.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @obs/dinero -> repodocgastoelectoral.blob.core.windows.net | URL provista por el operador; cruza a un blob host de terceros (no gov) |
| bytes del .xlsx -> parser | input no confiable (zip); validado por gate de header-text antes de mapear |
| nombre del candidato -> pipeline/LLM | SOLO el funcionario publico cruza este borde; el RUT nunca (gate del pipeline); el donante NUNCA |
| operador -> CLI | service key + credenciales LLM entran SOLO por env, nunca argv; la URL por eleccion via --url |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | SSRF / EoP | URL fabricada hacia metadata/loopback o todo tenant Azure | mitigate | `assertAllowedUrl(url,{extraHosts:[SERVEL_HOST]})` host EXACTO + https forzado; NUNCA `core.windows.net` en DEFAULT_ALLOWED_SUFFIXES |
| T-15-07 | Repudiation / Tampering | dataset parcial silencioso leido como completo | mitigate | reconciliacion de completitud (Content-MD5 + byte-length + TOTAL best-effort) -> cuarentena WHOLE-RUN, 0 filas |
| T-15-08 | Tampering | drift de columnas leido como "sin aportes" o misatribuido por posicion | mitigate | gate de header-TEXT (no posicional); THROW en rename/reorder -> cuarentena run-level |
| T-15-09 | Tampering | misatribucion de aporte por homonimia del candidato | mitigate | enlace por NOMBRE via `correrPipeline` (adjudicacion fail-closed); SOLO `determinista` puebla el FK (IDENT-12); probable/revision/no_confirmado -> null + cola humana, NUNCA un FK fabricado |
| T-15-09b | Information Disclosure | PII del donante ruteada al LLM/pipeline | mitigate | data-routing gate LOAD-BEARING: SOLO el nombre del candidato arma la mencion; donante (nombre/tipo/RUT) jamas entra a correrPipeline; el gate `assertNoRutInLlmInput`/`assertSensitivityAllowed` del pipeline reverifica el prompt |
| T-15-10 | Denial of Service | xlsx malicioso / zip-bomb | mitigate | parser mantenido (`exceljs`/`@e965`, NO bare xlsx CVE-2023-30533); host fijo allowlisted; cap de tamano de descarga |
| T-15-11 | Information Disclosure | service key / credenciales LLM en logs/bundle | mitigate | secretos SOLO de env, server-only, jamas interpolados en errores (espejo writer-supabase) |
| T-15-SC | Tampering | npm install (`exceljs`/`@e965/xlsx`) [ASSUMED] | mitigate | checkpoint:human-verify bloqueante (slopcheck Win-roto) ANTES del install; bare `xlsx` REMOVIDO (CVE); no auto-aprobable. `@obs/adjudication` es paquete del workspace (no install externo) |
</threat_model>

<output>
Crear `.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-02-SUMMARY.md` al terminar.
</output>
