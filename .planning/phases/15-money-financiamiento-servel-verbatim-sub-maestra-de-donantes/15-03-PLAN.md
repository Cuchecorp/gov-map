---
phase: 15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - app/components/financiamiento-de-parlamentario.tsx
  - app/app/parlamentario/[id]/page.tsx
  - app/lib/types.ts
  - app/components/financiamiento-de-parlamentario.test.tsx
autonomous: true
requirements: [MONEY-03, MONEY-04]
must_haves:
  truths:
    - "Con moneyPublicEnabled() === false (DEFAULT) la seccion #financiamiento entera (heading incluido) esta ausente del HTML"
    - "El heading exacto es 'Aportes de campana registrados en SERVEL'; nunca un posesivo"
    - "Los tres estados honestos son textualmente distintos: no-ingestado / verificado-sin-aportes (con fecha de corte) / enlazado; un vacio nunca se lee como limpio (sin verde, sin check)"
    - "Los aportes se agrupan por eleccion (periodo DESC) y cada fila lleva su propio Eleccion: ; un aporte de candidatura anterior lleva caveat amber y nunca se atribuye al mandato actual sin su periodo"
    - "El donante es su propio sujeto (Aporta: {nombre}) con linea separada y muted que asocia al candidato por NOMBRE confirmado (no por RUT, la fuente no trae RUT); el RUT del donante NUNCA se renderiza"
    - "sourceLabel() devuelve 'SERVEL' para origen servel; un error real de RPC se lanza (route boundary), nunca degrada a 'sin aportes'"
  artifacts:
    - path: "app/components/financiamiento-de-parlamentario.tsx"
      provides: "FinanciamientoView (pure) + FinanciamientoSection (Server Component gated)"
      contains: "moneyPublicEnabled"
    - path: "app/lib/types.ts"
      provides: "rama SERVEL en sourceLabel + AporteRpcRow"
      contains: "servel"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "FinanciamientoSection"
      via: "<section id='financiamiento' className='mt-12'> SIBLING despues de #dinero + Suspense + FinanciamientoSkeleton, todo dentro de moneyPublicEnabled() &&"
      pattern: "id=\"financiamiento\""
    - from: "app/components/financiamiento-de-parlamentario.tsx"
      to: "aportes_de_parlamentario RPC"
      via: "sb.rpc despues del gate moneyPublicEnabled"
      pattern: "aportes_de_parlamentario"
---

<objective>
Crear la seccion de ficha "Aportes de campana registrados en SERVEL" (`app/components/financiamiento-de-parlamentario.tsx`), montarla en `/parlamentario/[id]/page.tsx` como `<section id="financiamiento" className="mt-12">` SIBLING inmediatamente despues de `#dinero` (contratos), y agregar la rama SERVEL a `sourceLabel()`. Toda la seccion vive detras de `moneyPublicEnabled()` (default OFF). Espejo de `contratos-de-parlamentario.tsx`, siguiendo `15-UI-SPEC.md` al pie de la letra.

Purpose: La cara ciudadana de la fase. Reglas load-bearing: (1) la seccion entera, heading incluido, esta ausente cuando el gate esta OFF (doble candado con la RLS deny-by-default del Plan 01); (2) tres estados honestos textualmente distintos, un vacio nunca "limpio"; (3) agrupacion por periodo electoral + caveat de candidatura anterior (un aporte previo jamas se atribuye al mandato actual sin su fecha/periodo); (4) el donante es su propio sujeto y su RUT jamas se renderiza (Ley 21.719); (5) atribucion SERVEL = "terminos por verificar", NO CC BY 4.0; (6) HONESTIDAD DEL ENLACE (RE-RESUELTO A1): la fuente SERVEL NO trae RUT — el candidato se enlaza por NOMBRE confirmado/auditado por el pipeline de identidad, asi que la linea de asociacion debe decir la verdad ("por nombre confirmado"), NUNCA "por RUT".

Output: el componente, el mount en page.tsx, la rama de `sourceLabel`, y tests RTL del gate + los 3 estados + el tratamiento de periodo + el donante como sujeto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-UI-SPEC.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-PATTERNS.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-CONTEXT.md

# Analogo de ficha a espejar archivo-por-archivo (leer antes de escribir el componente):
@app/components/contratos-de-parlamentario.tsx
@app/components/contratos-de-parlamentario.test.tsx
@app/app/parlamentario/[id]/page.tsx
@app/lib/types.ts
@app/lib/money-gate.ts

<interfaces>
<!-- Contratos exactos que el componente consume. Definidos por Plan 01 (RPC/marcador) y Plan 02 (origen). -->

Gate (app/lib/money-gate.ts, server-only):
  export function moneyPublicEnabled(env = process.env): boolean;  // strict literal "true"; ausencia = OFF
  // Llamar SIEMPRE via la funcion; NUNCA leer MONEY_PUBLIC_ENABLED crudo. Chokepoint WR-02.

RPC aportes_de_parlamentario(p_id) (Plan 01) returns rows con la forma AporteRpcRow (agregar a types.ts):
  interface AporteRpcRow {
    eleccion: string;                      // NON-NULL, load-bearing (periodo verbatim)
    donante_nombre: string | null;         // -> "Donante no publicado"
    tipo_persona: string | null;           // "natural"|"juridica"
    monto: string | null;                  // verbatim string, NUNCA numeric -> "No publicado" si null
    fecha_aporte: string | null;           // -> "Fecha no publicada"
    tipo_aporte: string | null;
    candidato_nombre_verbatim: string | null;
    origen: string; fecha_captura: string; fecha_corte: string; enlace: string; licencia: string;
  }
  // El RPC ordena por eleccion DESC, fecha_aporte DESC nulls last. NUNCA incluye rut de donante.
  // El parlamentario_id (interno) NO viaja en el RPC; el RPC ya filtra por el enlace confirmado (Plan 01).

Marcador aportes_ingesta_estado (Plan 01), leido via .maybeSingle():
  { parlamentario_id: string; ingestado_hasta: string | null; fecha_captura: string } | null
  - null (fila ausente)  ->  "no_ingestado" (DEFAULT)
  - presente + 0 filas de aporte           ->  "verificado_sin_aportes" (con fecha de corte)
  - presente + >=1 fila                     ->  "enlazado" (grupos por eleccion, paginado)

sourceLabel (app/lib/types.ts): agregar antes del fallthrough:
  if (o.includes("servel")) return "SERVEL";
  // El origen escrito por Plan 02 es "servel".

Estado derivado server-side:
  type FinanciamientoEstado = "no_ingestado" | "verificado_sin_aportes" | "enlazado";

HONESTIDAD DEL ENLACE (RE-RESUELTO A1): el candidato se enlazo por NOMBRE confirmado/auditado por el
pipeline de identidad (Plan 02), NO por RUT (SERVEL no trae RUT). Toda copy que describa la asociacion
candidato<-parlamentario debe decir "por nombre confirmado", NUNCA "por RUT". Esto corrige las menciones
"por RUT" del UI-SPEC (la base del match cambio): mantener la ESTRUCTURA del UI-SPEC pero la PALABRA es
"nombre confirmado".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sourceLabel SERVEL + AporteRpcRow en types.ts; componente financiamiento-de-parlamentario.tsx</name>
  <files>app/lib/types.ts, app/components/financiamiento-de-parlamentario.tsx</files>
  <action>
ANTES de escribir codigo Next.js: leer la guia relevante en `app/node_modules/next/dist/docs/` (AGENTS.md de app: este Next no es el de tu training; conventions pueden diferir). Server Components por defecto, sin `"use client"`.

`app/lib/types.ts` (MODIFY): en `sourceLabel()`, agregar ANTES del fallthrough: `if (o.includes("servel")) return "SERVEL";` (mirror de la rama `chilecompra/mercado`). Agregar la interface `AporteRpcRow` junto a `ContratoRpcRow`, con la forma del bloque `<interfaces>`. NUNCA incluir el RUT del donante en `AporteRpcRow`.

`app/components/financiamiento-de-parlamentario.tsx` (espejo de `contratos-de-parlamentario.tsx`): dos exports, SIN `"use client"`:
  - `FinanciamientoView` (puro, props-only, RTL-testable con fixtures): renderiza el intro (2 lineas muted `text-sm`) + UNO de los tres estados honestos. `PAGE_SIZE = 20`, `buildHref` que preserva el query, ancla `#financiamiento`, `?financiamientoPage=N`. Copy de UI-SPEC Copywriting Contract (los acentos del copy en espanol son UTF-8 normales, NO BOM/comillas tipograficas), CON LA CORRECCION DE HONESTIDAD DEL ENLACE:
      * Intro linea 1 (frame): NO usar "asociados por RUT exacto" (el UI-SPEC quedo desactualizado: SERVEL no trae RUT). Usar el framing honesto: el aporte se muestra tal como lo publica la fuente y se ASOCIA AL CANDIDATO POR SU NOMBRE, identidad confirmada contra la maestra; la asociacion no implica juicio sobre el aporte ni sobre quien aporta. NO afirmar "por RUT". NO afirmar causalidad ni afinidad.
      * Intro linea 2 (atribucion): "Fuente: SERVEL ... terminos de uso por verificar" verbatim de UI-SPEC (esta linea no menciona RUT, queda igual). NO reusar `AtribucionCcBy` (eso es patrimonio); render plano muted.
      * Estado `no_ingestado` (marcador ausente): copy "Aun no hemos ingerido los aportes ... los datos de SERVEL se estan incorporando." NUNCA "sin aportes"/verde/check.
      * Estado `verificado_sin_aportes` (marcador presente, 0 aportes): copy "Consultamos SERVEL por este candidato (corte al {fecha}) y no se registran aportes asociados a ese candidato a esa fecha." con la fecha de corte, distinto del anterior. Usar "por este candidato" (NO "por el RUT de este candidato" — la fuente no tiene RUT). NUNCA verde/check.
      * Estado `enlazado` (>=1 aporte): conteo neutral ("{n} aporte registrado"/"{n} aportes registrados") + lista AGRUPADA por eleccion. NO suma de montos, NO ranking, NO %, NO "total aportado".
  - Lista enlazado AGRUPADA por `eleccion` (periodo DESC; dentro de grupo fecha DESC — el RPC ya viene ordenado). Header de grupo `Eleccion {periodo}` (mono). CAVEAT amber a nivel de grupo (una vez por grupo de periodo anterior): "Aporte de una candidatura anterior ({periodo}). No corresponde al mandato actual." — amber SOLO frescura/caveat, jamas severidad. La deteccion de "periodo anterior" se hace contra el periodo del mandato actual (si no es derivable trivialmente, renderizar el caveat de forma conservadora segun el periodo verbatim; documentar la heuristica en comentario).
  - Por fila `AporteFila` (mirror `ContratoFila`): el SUJETO es el DONANTE (`text-base`): `Aporta: {donante_nombre | "Donante no publicado"}` + tag muted `(persona natural)`/`(persona juridica)`. Linea SEPARADA y muted que asocia al candidato POR NOMBRE CONFIRMADO: `Asociado por nombre confirmado al candidato.` — NUNCA "por RUT" (la fuente no trae RUT; el enlace fue por nombre adjudicado, RE-RESUELTO A1), NUNCA posesivo, NUNCA fusionar donante+candidato en atribucion personal. El RUT del donante NUNCA se renderiza. `<dl>` NOUN-label/value verbatim (sin computo): `Eleccion:` (LOAD-BEARING, siempre presente, defense-in-depth por fila) / `Fecha del aporte:` (mono, "Fecha no publicada" si null) / `Monto:` (mono verbatim, "No publicado" si null, JAMAS fabricar/cero) / `Tipo de aporte:` (si SERVEL lo publica). Linea muted `Consultado por nombre del candidato, corte al {fecha_corte}.` (NO "por RUT"). `ProvenanceBadge` por fila con `ml-auto`, `sourceName={sourceLabel(origen)}` (-> "SERVEL"), `sourceUrl={enlace}`. El donante NO linkea a ninguna pagina (no dead link a la agregacion Phase 16); `IdentityMarker` opcional si la identidad del donante es ambigua. Paginacion `?financiamientoPage=N` con `<nav>` "Anteriores"/"Siguientes" + "Pagina N de M" mono, `min-h-[44px]`, ancla `#financiamiento`.
  - `FinanciamientoSection` (Server Component): PRIMERO `if (!moneyPublicEnabled(process.env)) return null;` ANTES de cualquier lectura de Supabase (doble candado con RLS). Luego `sb.rpc("aportes_de_parlamentario", { p_id: id })`; THROW en `rpcError` (un error real NO es estado vacio); leer `aportes_ingesta_estado` via `.maybeSingle()`; derivar `FinanciamientoEstado` server-side (marcador ausente -> "no_ingestado"; presente + 0 filas -> "verificado_sin_aportes"; >=1 -> "enlazado"); agrupar por eleccion + slice de paginacion server-driven; pasar todo a `FinanciamientoView`.

Colores (LOCKED): NO rojo/verde, NO "verificado = green check". Los tres estados se distinguen por TEXTO. Amber SOLO frescura (ProvenanceBadge) + el caveat "periodo anterior". Accent SOLO en links de paginacion/fuente. Empty -> texto muted, jamas un check/verde "limpio".

Forbidden vocabulary (LOCKED, anti-"maquina de sospechas"): sin lenguaje causal ("a cambio de", "financio su campana para", "antes de votar"), sin afinidad ("cercano a", "su financista", "vinculado a"), sin adjetivos de juicio ("millonario", "cuantioso", "sospechoso"), sin agregados computados ("total aportado", "% de su campana"), sin insinuar que un aporte revela la politica del parlamentario.

Archivos SIN BOM ni unicode invisible (el copy en espanol usa acentos UTF-8 normales, NO marcas de orden de bytes ni comillas tipograficas en codigo).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>`tsc --noEmit` limpio en app. `financiamiento-de-parlamentario.tsx` exporta `FinanciamientoView` y `FinanciamientoSection`, contiene `moneyPublicEnabled`, el heading/copys exactos de UI-SPEC con la correccion de honestidad del enlace, la agrupacion por eleccion, el caveat amber de periodo anterior, el donante como sujeto + linea separada "Asociado por nombre confirmado al candidato.", y NO renderiza RUT de donante. NO contiene la cadena "por RUT" en la copy de asociacion del candidato. `types.ts` `sourceLabel` retorna "SERVEL" para servel; `AporteRpcRow` sin RUT de donante. NO usa `AtribucionCcBy`.</done>
</task>

<task type="auto">
  <name>Task 2: mount en page.tsx (#financiamiento SIBLING de #dinero, gate-off remueve el heading) + tests RTL</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/components/financiamiento-de-parlamentario.test.tsx</files>
  <behavior>
    - gate OFF (moneyPublicEnabled false): la seccion #financiamiento ENTERA, incluyendo el <h2>, esta ausente del HTML (queryByText del heading === null).
    - gate ON: el heading exacto "Aportes de campana registrados en SERVEL" aparece.
    - estado no_ingestado: marcador ausente -> copy de no-ingestado; NUNCA "sin aportes"/verde/check.
    - estado verificado_sin_aportes: marcador presente + 0 aportes -> copy de corte con fecha, distinto del anterior.
    - estado enlazado: >=1 aporte -> conteo neutral + grupos por eleccion; cada fila con su Eleccion: ; donante como sujeto (Aporta:) + linea separada "Asociado por nombre confirmado al candidato.", sin posesivo, sin "por RUT"; RUT de donante ausente del render.
    - periodo anterior: un grupo de candidatura anterior muestra el caveat amber "Aporte de una candidatura anterior ... No corresponde al mandato actual."
  </behavior>
  <action>
ANTES de editar page.tsx: leer la guia de `app/node_modules/next/dist/docs/` si hay duda de conventions.

`app/app/parlamentario/[id]/page.tsx` (MODIFY): agregar un NUEVO `<section id="financiamiento" className="mt-12">` SIBLING inmediatamente DESPUES del bloque `#dinero` (contratos), espejando ese bloque. GATE WIRING (instruccion unica, no ambigua): envolver el `<section id="financiamiento">` COMPLETO — incluyendo `<h2 className="text-xl font-semibold mb-4">Aportes de campana registrados en SERVEL</h2>`, el `<Suspense fallback={<FinanciamientoSkeleton />}>` y `<FinanciamientoSection id={id} searchParams={sp} />` — dentro de `{moneyPublicEnabled(process.env) && ( ... )}`, EXACTAMENTE como el bloque `#dinero` de Phase 14. Con OFF (default) el nodo `<section>` entero, heading incluido, esta AUSENTE del HTML. NO depender de que `FinanciamientoSection` retorne null para ocultar el heading (eso filtraria el `<h2>`). El `mt-12` es la frontera de carril NO NEGOCIABLE (anti-insinuacion). Importar `FinanciamientoSection` desde el componente (junto a `ContratosSection`); `moneyPublicEnabled` ya esta importado. Definir `FinanciamientoSkeleton` junto a los otros skeletons, shape-matched a `FinanciamientoView` (intro `Skeleton h-4 w-3/4` + atribucion `h-3 w-1/2` + 3 filas `h-12 w-full rounded-lg`), espejo de `ContratosSkeleton`. Heading NO posesivo (mirror de la regla contratos).

`app/components/financiamiento-de-parlamentario.test.tsx`: tests RTL sobre `FinanciamientoView` (puro, con fixtures) cubriendo el bloque `<behavior>`: los 3 estados con copy distinto, el heading exacto, el donante como sujeto sin posesivo + linea separada "Asociado por nombre confirmado al candidato." (asertar exactamente esa frase y asertar que NO aparece "por RUT" en la asociacion del candidato), la ausencia del RUT de donante en el render, la agrupacion por eleccion, el caveat amber de periodo anterior. GATE TEST: como el gate vive en page.tsx (Server Component), modelar la suppression con un helper/render que reproduzca el wrapper `moneyPublicEnabled(env) && <section>...`; con el flag a false aserta `screen.queryByText("Aportes de campana registrados en SERVEL")` === null (HEADING ausente, no solo el contenido); con el flag a "true" el heading aparece. Mirror del patron de `contratos-de-parlamentario.test.tsx`.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd app && pnpm test financiamiento 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm --filter app test` verde para `financiamiento-de-parlamentario.test.tsx`. page.tsx tiene `<section id="financiamiento" className="mt-12">` SIBLING despues de `#dinero`, con el heading exacto, Suspense + `FinanciamientoSkeleton`, todo dentro de `moneyPublicEnabled(process.env) &&` (OFF -> seccion + heading ausentes; el test asierta queryByText del heading === null cuando OFF). Los 3 estados son textualmente distintos; el caveat de periodo anterior y el donante-sujeto-sin-posesivo (con "por nombre confirmado", sin "por RUT") estan cubiertos.</done>
</task>

</tasks>

<verification>
- `pnpm --filter app test` verde (gate OFF remueve heading, 3 estados, agrupacion por eleccion, caveat periodo anterior, donante como sujeto, RUT donante ausente, asociacion "por nombre confirmado" no "por RUT").
- `tsc --noEmit` limpio.
- Heading exacto "Aportes de campana registrados en SERVEL"; sin posesivos; sin "limpio"/check/verde.
- `sourceLabel` retorna "SERVEL"; atribucion "terminos por verificar" (NO CC BY 4.0).
- mt-12 carril separator presente; `#financiamiento` SIBLING de `#dinero`.
</verification>

<success_criteria>
- Criterio 3 (MONEY-03): la ficha muestra el financiamiento verbatim con fuente/fecha/enlace por fila, restringido por periodo electoral (grupo por eleccion + caveat de candidatura anterior); un aporte previo nunca se atribuye al mandato actual sin fecharlo.
- Criterio 4 (MONEY-04): tres estados honestos textualmente distintos (no-ingestado != verificado-sin-aportes != enlazado); un vacio nunca se lee como "limpio". La asociacion candidato<-parlamentario se describe honestamente como "por nombre confirmado" (enlace adjudicado/auditado del Plan 02), nunca "por RUT" (la fuente no trae RUT).
- Gate de exposicion: con moneyPublicEnabled() OFF (default), la seccion entera (heading incluido) ausente del HTML (doble candado con RLS deny-by-default del Plan 01).
- Anti-insinuacion: el donante es su propio sujeto, su RUT jamas se renderiza (Ley 21.719); sin lenguaje causal/afinidad/juicio/agregados.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (anon) -> Next.js RSC | la seccion publica solo se renderiza si moneyPublicEnabled() y los datos vienen via RPC |
| RSC -> Supabase | lectura via security-definer RPC; nunca lectura directa a la tabla deny-by-default donante |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | exposicion prematura MONEY sin sign-off legal | mitigate | doble candado: moneyPublicEnabled() OFF (presentacion, seccion+heading ausentes) + RLS deny-by-default + RPC (datos, Plan 01); ambos fail-closed |
| T-15-13 | Information Disclosure | RUT/identidad del donante renderizado (Ley 21.719) | mitigate | `AporteRpcRow` sin RUT de donante; la View nunca renderiza RUT; donante = nombre verbatim + tipo, sin link |
| T-15-14 | Repudiation | un vacio leido como veredicto "limpio" | mitigate | tres estados honestos textualmente distintos; copy prohibe "sin aportes check"/"limpio"; test RTL lo codifica |
| T-15-15 | Tampering | aporte de candidatura previa atribuido al mandato actual | mitigate | agrupacion por eleccion + `Eleccion:` por fila (defense in depth) + caveat amber de periodo anterior; el RPC ordena por eleccion DESC |
| T-15-16 | Tampering | atribucion personal de un aporte (donante fusionado al candidato) | mitigate | donante = sujeto propio (`Aporta:`); enlace en linea muted separada "Asociado por nombre confirmado al candidato."; sin posesivo; test RTL |
| T-15-16b | Repudiation | la ficha afirma una base de enlace falsa ("por RUT" cuando SERVEL no trae RUT) | mitigate | copy honesta "por nombre confirmado" (el enlace fue adjudicado/auditado por el pipeline, Plan 02); test asierta ausencia de "por RUT" en la asociacion del candidato |
| T-15-17 | Information Disclosure | leer MONEY_PUBLIC_ENABLED crudo (bypass chokepoint) | mitigate | SIEMPRE via moneyPublicEnabled() server-only; nunca el env crudo |
| T-15-18 | Repudiation | error real de RPC degradado a "sin aportes" | mitigate | THROW en rpcError a route boundary; un error nunca es estado vacio |
</threat_model>

<output>
Crear `.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-03-SUMMARY.md` al terminar.
</output>
