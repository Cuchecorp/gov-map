---
phase: 15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0024_servel.sql
  - supabase/tests/0025_servel.test.sql
autonomous: false
requirements: [MONEY-03, MONEY-04]
must_haves:
  truths:
    - "La tabla aporte existe, es public-read y versionada por (fuente_id, fecha_corte), con eleccion NOT NULL y candidato_nombre_verbatim"
    - "La sub-maestra donante niega por defecto a anon: RLS enabled, 0 policies, revoke all from anon, authenticated"
    - "El marcador aportes_ingesta_estado existe y es public-read (distingue no-ingestado de verificado-sin-aportes)"
    - "El RPC aportes_de_parlamentario(p_id) es security-definer, revocado de public, grant a anon, ordena por eleccion DESC luego fecha_aporte DESC, y NUNCA proyecta RUT de donante ni PII"
    - "El pgTAP codifica el candado deny-by-default + revoke sobre donante y la firma + orden del RPC contra el schema APLICADO"
  artifacts:
    - path: "supabase/migrations/0024_servel.sql"
      provides: "aporte (public-read, versionado, eleccion NOT NULL, candidato_nombre_verbatim) + donante (deny-by-default+revoke) + aportes_ingesta_estado (public-read) + RPC aportes_de_parlamentario"
      contains: "create table aporte"
    - path: "supabase/tests/0025_servel.test.sql"
      provides: "pgTAP: existencia 3 tablas, RLS, deny-by-default donante (triple-assert), RPC security-definer + orden + sin RUT donante"
      contains: "select plan("
  key_links:
    - from: "supabase/migrations/0024_servel.sql"
      to: "parlamentario(id)"
      via: "FK parlamentario_id references parlamentario(id) on delete set null"
      pattern: "references parlamentario\\(id\\)"
    - from: "aportes_de_parlamentario"
      to: "aporte"
      via: "where a.parlamentario_id = p_id; order by eleccion desc, fecha_aporte desc nulls last"
      pattern: "where a\\.parlamentario_id = p_id"
---

<objective>
Crear la migracion SQL del bloque MONEY-Financiamiento (`0024_servel.sql`) y su pgTAP (`0025_servel.test.sql`): la tabla `aporte` (public-read, versionada, con periodo electoral NOT NULL y `candidato_nombre_verbatim`), la sub-maestra `donante` (deny-by-default + revoke, leccion Phase 11), el marcador `aportes_ingesta_estado` (public-read), y el RPC `aportes_de_parlamentario(p_id)` security-definer agrupable por eleccion. Espejo archivo-por-archivo de `0023_dinero.sql`.

Purpose: Es el piso de datos de toda la fase. La sub-maestra `donante` NACE deny-by-default (criterio 1: "construida en este bloque" + Ley 21.719: un aporte revela afiliacion politica = dato sensible) para que `anon` jamas lea PII de donantes; el RPC es el UNICO canal publico y NUNCA proyecta el RUT del donante. El gate `moneyPublicEnabled()` (Plan 03) es el segundo candado.

Output: `supabase/migrations/0024_servel.sql`, `supabase/tests/0025_servel.test.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-RESEARCH.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-PATTERNS.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-CONTEXT.md
@.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-UI-SPEC.md

# Analogos VERBATIM a copiar seccion-por-seccion (leer antes de escribir):
@supabase/migrations/0023_dinero.sql
@supabase/tests/0024_dinero.test.sql
@supabase/tests/0023_money_gate.test.sql

<interfaces>
<!-- Contratos que el RPC/tablas deben respetar para que Plan 02 (writer) y Plan 03 (ficha) acoplen. -->

Forma del RPC publico (Plan 03 la consume via sb.rpc("aportes_de_parlamentario", { p_id })):
  aportes_de_parlamentario(p_id text) returns table (
    -- SOLO campos que la fuente publica + fecha_corte; SIN parlamentario_id interno;
    -- NUNCA el RUT del donante ni columnas PII (Ley 21.719). eleccion es NON-NULL (load-bearing).
    eleccion text,                 -- periodo electoral verbatim, NON-NULL
    donante_nombre text,           -- nullable -> la UI muestra "Donante no publicado"
    tipo_persona text,             -- nullable ("natural"|"juridica")
    monto text,                    -- nullable, verbatim string, NUNCA numeric
    fecha_aporte date,             -- nullable
    tipo_aporte text,              -- nullable
    candidato_nombre_verbatim text,-- nombre del candidato como lo da SERVEL (puente nombre->RUT futuro)
    origen text, fecha_captura timestamptz, fecha_corte date, enlace text, licencia text
  )
  -- order by eleccion desc, fecha_aporte desc nulls last (UI-SPEC: grupo por eleccion DESC, fecha DESC)

Marcador de 3 estados (Plan 03 lo lee via .maybeSingle()):
  aportes_ingesta_estado(parlamentario_id text PK, ingestado_hasta date, fecha_captura timestamptz)
  - FILA AUSENTE  => "no-ingestado"
  - FILA PRESENTE + 0 filas en aporte => "verificado-sin-aportes" (con fecha de corte)
  - FILA PRESENTE + >=1 fila => "enlazado"

Clave de version de aporte (Plan 02 writer upserta por onConflict sobre esta PK):
  primary key (fuente_id, fecha_corte)   -- fuente_id = id sintetico estable del aporte (hash de la fila + eleccion); NUNCA keyear por parlamentario.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracion 0024_servel.sql (aporte + donante + marcador + RPC)</name>
  <files>supabase/migrations/0024_servel.sql</files>
  <action>
Crear `supabase/migrations/0024_servel.sql` copiando los cuatro arquetipos de seccion de `0023_dinero.sql`. NO inventar arquitectura: cada bloque tiene un hermano VERBATIM en 0023.

(1) Tabla public-read `aporte` (espejo de `contrato`, 0023):
  - PK de version compuesta `primary key (fuente_id, fecha_corte)` — `fuente_id` = id sintetico estable del aporte (el writer del Plan 02 lo deriva de la fila + eleccion; SERVEL no publica un id). `fecha_corte` = fecha de corte de la consulta. NUNCA keyear por `parlamentario_id`.
  - `parlamentario_id text references parlamentario(id) on delete set null` (nullable) + `estado_vinculo text` ('confirmado' | 'no_confirmado' | 'cuarentena' | null).
  - `eleccion text NOT NULL` — periodo electoral verbatim (LOAD-BEARING; UI-SPEC marca este campo como always-present; una fila sin eleccion es drift -> cuarentena aguas arriba, jamas llega aca).
  - `candidato_nombre_verbatim text` — nombre del candidato como lo da SERVEL (la fuente NO trae RUT de candidato; este campo se guarda para un puente nombre->RUT en una fase futura; HOY el enlace por RUT confirma cero filas — fail-closed honesto, NO es bug, NO agregar enlace por nombre).
  - Columnas literales VERBATIM (sin computo): `donante_nombre text`, `tipo_persona text` ('natural'|'juridica'), `monto text` (literal string, NO numeric — verbatim), `fecha_aporte date`, `tipo_aporte text`, `territorio text`, `pacto text`, `partido text` (campos verbatim de SERVEL, nullable).
  - Provenance inline NOT NULL (0023): `origen text not null` (valor "servel"), `fecha_captura timestamptz not null default now()`, `enlace text not null`. CRITICO: `licencia text not null default 'terminos por verificar'` — NUNCA `'mencion de la fuente'` (eso es ChileCompra) ni `'CC BY 4.0'` (eso es probidad). SERVEL = "terminos por verificar" (research-confirmado).
  - RLS public-read EXPLICITO (0023): `enable row level security` + `create policy aporte_public_read ... for select to anon using (true)` + `grant select on aporte to anon`. Sin policies de insert/update/delete (el writer usa service key).
  - Indice `create index aporte_parlamentario_idx on aporte (parlamentario_id)`.

(2) Sub-maestra deny-by-default `donante` (espejo de `contratista` 0023; PII: un aporte puede revelar afiliacion politica = dato sensible Ley 21.719):
  - Clave: SERVEL NO publica RUT de donante, asi que la PK NO es el RUT. Usar `donante_id text primary key` (id sintetico estable derivado del nombre normalizado + tipo, lo construye el writer). `rut_donante text` NULLABLE (NULL hoy; la columna queda lista por si una exportacion futura lo trae). `nombre text`, `tipo_persona text` ('natural'|'juridica'), provenance inline (`origen`, `fecha_captura`, `enlace`, `licencia text not null default 'terminos por verificar'`).
  - DENY-BY-DEFAULT VERBATIM: `alter table donante enable row level security;` con CERO `create policy`. NINGUN `grant select ... to anon`. `revoke all on donante from anon, authenticated;` (cierra el hueco de default-privileges de Supabase, leccion Phase 11). El writer (service_role) conserva privilegios y bypassa RLS.

(3) Marcador `aportes_ingesta_estado` (espejo de `contratos_ingesta_estado` 0023) — sin PII, public-read:
  - `parlamentario_id text primary key references parlamentario(id) on delete cascade`, `ingestado_hasta date`, `fecha_captura timestamptz not null default now()`.
  - `enable row level security` + `create policy aportes_ingesta_estado_public_read ... for select to anon using (true)` + `grant select ... to anon`. Permite distinguir "no-ingestado" (fila ausente) de "verificado-sin-aportes" (fila presente).

(4) RPC `aportes_de_parlamentario(p_id text)` security-definer (espejo de `contratos_de_parlamentario` 0023):
  - `language sql stable security definer set search_path = ''`.
  - Selecciona SOLO los campos source-published + `fecha_corte` + `candidato_nombre_verbatim`: `eleccion, donante_nombre, tipo_persona, monto, fecha_aporte, tipo_aporte, candidato_nombre_verbatim, origen, fecha_captura, fecha_corte, enlace, licencia` desde `public.aporte` `where a.parlamentario_id = p_id`. ORDEN (divergencia vs 0023): `order by a.eleccion desc, a.fecha_aporte desc nulls last` (UI-SPEC: grupo por eleccion DESC, dentro de grupo fecha DESC; reusar el patron `desc nulls last` de 0023 para la fecha nullable).
  - PROHIBIDO proyectar `rut_donante`, `donante_id`, `parlamentario_id` interno ni cualquier columna de `donante` (Ley 21.719: el RUT del donante jamas sale al publico).
  - `revoke execute on function public.aportes_de_parlamentario(text) from public;` + `grant execute on function public.aportes_de_parlamentario(text) to anon;` sobre la firma EXACTA.

Encabezar el archivo con un comentario de cabecera (estilo 0023) explicando: gate de exposicion pendiente (Plan 03 lo aplica con `moneyPublicEnabled`), licencia = terminos por verificar (NO CC BY, NO mencion de la fuente), el RPC nunca proyecta RUT de donante, apply remoto = checkpoint de operador. NO crear pg_cron job en esta migracion (mismo patron que 0021/0022/0023: cron = checkpoint de operador).

ARCHIVO SIN BOM y sin unicode invisible: escribir solo UTF-8 plano; NO pegar marcas de orden de bytes ni comillas tipograficas. El nombre de licencia es ASCII "terminos por verificar" (sin acento en "terminos" si el resto del codebase evita acentos en literales SQL — replicar exactamente el estilo de 0023; el copy en espanol acentuado va en la ficha, no en el SQL).
  </action>
  <verify>
    <automated>grep -c "create table aporte" "supabase/migrations/0024_servel.sql"</automated>
  </verify>
  <done>El archivo existe; contiene `create table aporte`, `create table donante`, `create table aportes_ingesta_estado`, `create function public.aportes_de_parlamentario`, `revoke all on donante from anon, authenticated`, `eleccion text not null` (o equivalente), `candidato_nombre_verbatim`, `order by` con `eleccion desc` y `fecha_aporte desc nulls last`, y `default 'terminos por verificar'`. NO contiene `'CC BY 4.0'`, NO contiene `'mencion de la fuente'`, NO contiene `cron.schedule`. El RPC NO selecciona `rut_donante` ni `donante_id`.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0025_servel.test.sql (deny-by-default donante + RPC orden + sin RUT donante)</name>
  <files>supabase/tests/0025_servel.test.sql</files>
  <action>
Crear `supabase/tests/0025_servel.test.sql` copiando `0024_dinero.test.sql`. Usar el numero de test `0025` (los tests `0023_money_gate.test.sql` y `0024_dinero.test.sql` YA existen — evitar colision; migraciones y tests comparten namespace numerico, el siguiente test LIBRE es 0025). `begin; select plan(N);` con N = numero exacto de asserts, y `select * from finish; rollback;` al final.

Asserts minimos (mapean a los criterios de exito de la fase):
  - Existencia: `has_table('aporte')`, `has_table('donante')`, `has_table('aportes_ingesta_estado')`.
  - RLS habilitada en las tres tablas (helper equivalente al usado por 0024_dinero.test).
  - `aporte` PK = exactamente la clave de version `(fuente_id, fecha_corte)` (espejo de 0024_dinero.test).
  - `aporte.eleccion` es NOT NULL (`col_not_null('aporte','eleccion')` o equivalente) — el campo load-bearing.
  - `aporte` tiene la columna `candidato_nombre_verbatim` (`has_column`).
  - `aporte` public-read: existe policy de select para anon + `anon` TIENE grant SELECT.
  - `donante` DENY-BY-DEFAULT (el candado de la fase, leccion Phase 11): RLS enabled + CERO policies sobre `donante` + `anon` NO tiene grant SELECT sobre `donante`. Tres asserts distintos.
  - `aportes_ingesta_estado` public-read: policy select para anon + grant SELECT a anon.
  - RPC: `has_function('aportes_de_parlamentario')`, es SECURITY DEFINER, `anon` TIENE EXECUTE, y `public` NO tiene EXECUTE (revocado).

NOTA sobre el orden y el no-PII del RPC: si el harness pgTAP del repo permite asertar el cuerpo/columnas de la funcion, agregar un assert de que la definicion del RPC contiene `order by` con `eleccion` y NO contiene `rut_donante` (via `pg_get_functiondef` + `like`/`unlike`). Si el harness no lo soporta facilmente, dejar ese chequeo como parte del checkpoint de operador (Task 3) y NO inflar el plan(N).

ARCHIVO SIN BOM ni unicode invisible (ver Task 1).

NOTA: el apply remoto y la corrida de este pgTAP contra el schema APLICADO es el checkpoint de operador (Task 3) — CI no prueba que el DDL corrio (falso positivo, leccion 0023).
  </action>
  <verify>
    <automated>grep -E "select plan\(|has_table\('donante'\)|aportes_de_parlamentario" "supabase/tests/0025_servel.test.sql" | grep -v '^#'</automated>
  </verify>
  <done>El archivo existe con `select plan(N)`, asserts de existencia de las 3 tablas, el assert NOT NULL de `aporte.eleccion`, el assert `has_column` de `candidato_nombre_verbatim`, los 3 asserts deny-by-default de `donante`, y los asserts del RPC (security definer + execute a anon + revocado de public). El numero de plan coincide con el conteo de asserts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>La migracion `0024_servel.sql` y el pgTAP `0025_servel.test.sql` estan escritos y validados estructuralmente (grep gates verdes). El DDL aun NO se ha aplicado al Postgres remoto — CI no aplica DDL (leccion 0023: build verde no prueba que el schema corrio).</what-built>
  <how-to-verify>
Aplicar la migracion al remoto y correr el pgTAP contra el schema APLICADO (host Windows -> usar el Bash tool / git-bash, NO PowerShell; el `.env` tiene BOM -> pasar `--db-url` explicito, ver MEMORY env-credentials-reality.md):

  1. `supabase db push --db-url "$SUPABASE_DB_URL"`  (esquiva el BOM del .env)
  2. `supabase test db --db-url "$SUPABASE_DB_URL"`  (corre 0025_servel.test.sql contra el schema aplicado)

Confirmar que el pgTAP pasa 100% (todos los asserts verdes), en particular los 3 asserts deny-by-default de `donante` (RLS enabled + 0 policies + 0 grant SELECT a anon) y el NOT NULL de `aporte.eleccion`. Inspeccionar manualmente que el cuerpo del RPC NO proyecta `rut_donante`/`donante_id` y ordena por `eleccion desc, fecha_aporte desc nulls last`. Si algun assert falla, NO es falso positivo de CI — es un hueco real en el DDL; reportar el assert rojo.
  </how-to-verify>
  <resume-signal>Escribir "aplicado, pgTAP N/N verde, RPC sin RUT donante + orden eleccion/fecha confirmado" o describir los asserts en rojo.</resume-signal>
</task>

</tasks>

<verification>
- `0024_servel.sql` y `0025_servel.test.sql` existen, sin BOM.
- `donante` deny-by-default + revoke (3 asserts pgTAP verdes contra schema aplicado).
- `aporte.eleccion` NOT NULL; `candidato_nombre_verbatim` presente.
- RPC `aportes_de_parlamentario` security-definer, revocado de public, grant a anon, orden eleccion DESC / fecha_aporte DESC, sin RUT de donante.
- `licencia` default = 'terminos por verificar' (NO 'CC BY 4.0', NO 'mencion de la fuente').
- Apply remoto + pgTAP verde confirmado por el operador.
</verification>

<success_criteria>
- Criterio 1 (MONEY-03): la sub-maestra `donante` nace en este bloque, deny-by-default + revoke (verificado por pgTAP contra schema aplicado).
- Criterio 4 (MONEY-04): el RPC devuelve SOLO `where parlamentario_id = p_id` (jamas un no-confirmado bajo un parlamentario) con estados honestos via el marcador de 3 estados.
- Criterio 3 (restriccion por periodo): `aporte.eleccion` NOT NULL + orden del RPC por eleccion DESC habilitan el group-by-eleccion de la ficha; un aporte previo jamas se atribuye al mandato actual sin su periodo.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (navegador) -> Postgres | Lectura publica; debe pasar SOLO por policies public-read + RPC, jamas directo a `donante` |
| writer (service_role) -> Postgres | Escritura; bypassa RLS por diseno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | tabla `donante` (un aporte revela afiliacion politica = dato sensible Ley 21.719) | mitigate | `revoke all on donante from anon, authenticated` VERBATIM + 0 policies; pgTAP codifica los 3 asserts deny-by-default |
| T-15-02 | Information Disclosure | RUT/identidad del donante emitido por el RPC | mitigate | RPC NO selecciona `rut_donante`/`donante_id`/columnas de `donante`; solo nombre verbatim + tipo_persona |
| T-15-03 | Elevation of Privilege | RPC security-definer ejecutable por cualquiera | mitigate | `revoke execute ... from public` + `grant execute ... to anon` sobre la firma exacta; RPC filtra SOLO `parlamentario_id = p_id` confirmados |
| T-15-04 | Tampering | falso positivo de CI (DDL no aplicado) | mitigate | apply remoto + pgTAP contra schema APLICADO = checkpoint de operador (no CI) |
| T-15-05 | Tampering | un aporte de candidatura previa atribuido al mandato actual sin fecharlo | mitigate | `eleccion` NOT NULL + RPC ordena por eleccion DESC; la ficha (Plan 03) agrupa por eleccion y etiqueta cada fila |
</threat_model>

<output>
Crear `.planning/phases/15-money-financiamiento-servel-verbatim-sub-maestra-de-donantes/15-01-SUMMARY.md` al terminar.
</output>
