---
phase: 21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fichas/src/writer-supabase.ts
  - packages/fichas/src/writer-supabase.test.ts
autonomous: false
requirements: [SC3, SC4]
tags: [fichas, pipeline, backfill, senado, idea-matriz]

must_haves:
  truths:
    - "leerPendientes entrega el link_mensaje_mocion REAL del XML del Senado, no null hardcodeado"
    - "Tras el backfill LIVE, proyecto_ficha.idea_matriz > 0 (hoy 0/74)"
    - "Un boletín sin link/sin texto degrada honesto a idea_matriz=null — NUNCA se fabrica idea matriz"
    - "El re-fetch del Senado reusa la política @obs/ingest (allowlist + robots + rate-limit 2-3s), no fetch a mano"
  artifacts:
    - path: "packages/fichas/src/writer-supabase.ts"
      provides: "leerPendientes cablea el link real (re-fetch Senado, Opción A sin DDL)"
      contains: "linkMensajeMocion"
    - path: "packages/fichas/src/writer-supabase.test.ts"
      provides: "test: leerPendientes entrega el link real (no null) + degradación honesta"
      contains: "leerPendientes"
  key_links:
    - from: "packages/fichas/src/writer-supabase.ts (leerPendientes)"
      to: "SenadoConnector.fetchTramitacion + parseSenadoTramitacion"
      via: "re-fetch del XML por boletín base + extracción del sidecar"
      pattern: "fetchTramitacion|linkMensajeMocion"
    - from: "proyecto_ficha.idea_matriz"
      to: "pipeline.extraer (DeepSeek)"
      via: "obtenerTextoFuente(link) → extracción literal"
      pattern: "idea_matriz"
---

<objective>
Cerrar la causa raíz de `idea_matriz = 0/74`: el writer de fichas hardcodea `link_mensaje_mocion: null` (`writer-supabase.ts:143`), forzando al pipeline a degradar SIEMPRE a "título+materia". El link al texto íntegro YA viene en el XML del Senado (`<descripcion><link_mensaje_mocion>`, host `senado.cl` ya allowlisted). Cablear el link real (Opción A: re-fetch del XML por boletín, SIN DDL) en `leerPendientes`, extender el test, hacer un smoke test de 1-2 boletines, y correr el backfill LIVE (`@obs/fichas --reembed`) para poblar `idea_matriz`. Verificar con psql `count(idea_matriz) > 0`.

Purpose: SC3 — ideas matrices visibles en la ficha de proyecto. SC4 — degradación honesta intacta (sin texto → honest-state, NUNCA idea matriz fabricada). El render ya está implementado (`proyecto/[boletin]/page.tsx` IdeaMatrizSection) — solo se "enciende" al poblar la DB.
Output: `writer-supabase.ts` con el link real cableado, test extendido, y `idea_matriz > 0` en la nube.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-RESEARCH.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-PATTERNS.md
@packages/fichas/src/writer-supabase.ts
@packages/fichas/src/pipeline-cli.ts
@packages/tramitacion/src/connector-senado.ts

<interfaces>
<!-- BUG raíz: writer-supabase.ts:139-147, línea 143 `link_mensaje_mocion: null`. leerPendientes(boletines?) → PipelinePendienteRow[]. -->
<!-- PipelinePendienteRow: { boletin, titulo, materia: string|null, link_mensaje_mocion: string|null, estado, origen?, fecha_captura? } -->

<!-- Conector a reusar (NO hand-roll): packages/tramitacion/src/connector-senado.ts -->
new SenadoConnector({ fetcher, rateLimiter, robots, allowlist? })
  .fetchTramitacion(boletinBase: string): Promise<string>   // XML; boletín BASE sin sufijo de comisión (Pitfall 1)
<!-- @obs/ingest: Fetcher, HostRateLimiter, RobotsGuard (ensamblados ya en pipeline-cli.ts:158-161). senado.cl ya allowlisted (allowlist.ts:19). -->

<!-- Parser del link (sidecar): packages/tramitacion/src/parse-senado-tramitacion.ts:57-83 -->
parseSenadoTramitacion(xml, enlace?): { proyecto, eventos, linkMensajeMocion: string|null }
<!-- Strip del sufijo de comisión al boletín BASE antes del fetch: boletin.replace(/-\d+$/, "") (parse-senado-tramitacion.ts:79). -->

<!-- Backfill LIVE (pipeline-cli.ts): tsx packages/fichas/src/pipeline-cli.ts --reembed --service-key "$SUPABASE_SECRET_KEY" -->
<!-- decidirDryRun (pipeline-cli.ts:123): sin --service-key (y sin --dry-run) → DRY-RUN SILENCIOSO (no escribe). El operador DEBE pasar la key. Verificar log "LIVE ... dbLoaded=true". -->

<!-- Test análogo: packages/fichas/src/writer-supabase.test.ts (fakeClient captura from().upsert(); para leerPendientes necesita un fake con .select().in()/.eq() encadenable que devuelva {data,error}). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cablear el link real en leerPendientes (Opción A: re-fetch Senado, sin DDL) + extender test</name>
  <read_first>
    - packages/fichas/src/writer-supabase.ts (leerPendientes:123-148, línea 143 = bug; PipelinePendienteRow:152-160)
    - packages/tramitacion/src/connector-senado.ts (fetchTramitacion:45)
    - packages/tramitacion/src/parse-senado-tramitacion.ts:57-83 (linkMensajeMocion sidecar; strip de sufijo :79)
    - packages/fichas/src/pipeline-cli.ts:158-161 (ensamblado de Fetcher/HostRateLimiter/RobotsGuard)
    - packages/fichas/src/writer-supabase.test.ts (fakeClient — base del test)
    - packages/ingest/src/allowlist.ts:19 (senado.cl ya allowlisted)
  </read_first>
  <behavior>
    - Test: para un boletín pendiente cuyo XML Senado trae `<link_mensaje_mocion>`, `leerPendientes` entrega ese link REAL en `link_mensaje_mocion` (no null).
    - Test: para un boletín cuyo XML NO trae el link (o el fetch falla), `link_mensaje_mocion` queda `null` (degradación honesta — NUNCA se inventa).
    - Test: el boletín se consulta al Senado en su forma BASE (sufijo `-NN` de comisión stripeado).
    - Test: el re-fetch usa el SenadoConnector inyectado (no fetch global), confirmando reuso de la política @obs/ingest.
  </behavior>
  <action>
    Modificar `leerPendientes` en `packages/fichas/src/writer-supabase.ts` para que, por cada boletín pendiente, re-fetchee el XML del Senado y extraiga el link real, eliminando el hardcode `link_mensaje_mocion: null` (línea 143). Implementación Opción A (RECOMENDADA, sin DDL — per 21-PATTERNS.md Pattern 3 + RESEARCH): inyectar un colaborador resolvedor del link (p.ej. un parámetro/dep `resolverLink?(boletinBase): Promise<string|null>` o un `SenadoConnector` opcional en las opciones del writer) de modo que el test pueda mockearlo y el CLI lo ensamble con el `SenadoConnector` real (reusando los `Fetcher`/`HostRateLimiter`/`RobotsGuard` que `pipeline-cli.ts:158-161` ya construye). Por cada fila: stripear el sufijo de comisión (`boletin.replace(/-\d+$/, "")`), llamar `fetchTramitacion(base)`, `parseSenadoTramitacion(xml).linkMensajeMocion`, y poner ese valor (o `null` si falla/ausente) en `link_mensaje_mocion`. NO hacer fetch a mano (RESEARCH "Don't Hand-Roll"). Mantener la degradación honesta: cualquier fallo de fetch/parse/ausencia → `null`, NUNCA un valor fabricado. Respetar el rate-limit 2-3s (ya en el connector). Mantener la firma `PipelinePendienteRow` y el comportamiento existente para el resto de campos.
    Si la inyección del connector dentro del writer rompe la separación de capas (el writer no debería conocer @obs/tramitacion), preferir resolver el link en `pipeline-cli.ts` (cargarPendientes) DESPUÉS de `leerPendientes`, enriqueciendo cada `PipelinePendiente` con el link real antes de pasar a `correrPipeline`. Elegir la ubicación de menor blast-radius; documentar la decisión en el SUMMARY. En cualquier caso, el campo `link_mensaje_mocion` debe llegar al pipeline con el valor real.
    Extender `packages/fichas/src/writer-supabase.test.ts` con los casos de `<behavior>` (mock del resolvedor/connector; fakeClient para el select).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas test -t "leerPendientes" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `writer-supabase.ts` (o `pipeline-cli.ts`) ya NO entrega `link_mensaje_mocion: null` incondicional; resuelve el link real vía `fetchTramitacion` + `parseSenadoTramitacion`.
    - El boletín se consulta en forma BASE (sufijo stripeado).
    - Un fetch/parse fallido o link ausente → `null` (degradación honesta, sin fabricar).
    - El re-fetch reusa la política @obs/ingest (SenadoConnector inyectado), no `fetch` global.
    - `pnpm --filter @obs/fichas test` pasa (incl. los casos nuevos de link real y degradación).
  </acceptance_criteria>
  <done>El pipeline recibe el link_mensaje_mocion real por boletín; la degradación honesta a null se mantiene; tests verdes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Pre-flight de keys + smoke test 1-2 boletines + backfill LIVE + gate psql</name>
  <files>(corrida LIVE: escribe a proyecto_ficha en la nube; no edita archivos del repo)</files>
  <action>Confirmar keys, hacer smoke test de 1-2 boletines, correr el backfill LIVE de @obs/fichas, y verificar el gate psql. Paso de operador (corrida LOCAL con service key real). Ver how-to-verify para la secuencia exacta.</action>
  <what-built>
    `leerPendientes`/CLI ahora alimenta el link real al pipeline. El backfill que repuebla `idea_matriz` es una corrida LIVE LOCAL del operador (CLAUDE.md: backfill masivo = LOCAL, no GH Actions). Requiere keys reales (`DEEPSEEK_API_KEY`, `SUPABASE_SECRET_KEY`, `GEMINI_API_KEY`) y escribe a la nube `bctyygbmqcvizyplktuw`. El formato del link Senado (HTML/PDF) no está probado sobre los 74 reales (A2, MEDIUM) → smoke test ANTES del backfill completo.
  </what-built>
  <how-to-verify>
    1. Pre-flight: regenerar `~/obs_env.sh` desde `.env` (BOM/CRLF-safe); cargarlo. Confirmar presentes: `DEEPSEEK_API_KEY`, `SUPABASE_SECRET_KEY`, `GEMINI_API_KEY`, `SUPABASE_URL`, `SUPABASE_DB_URL`. (Sin `SUPABASE_SECRET_KEY` la corrida degrada a DRY-RUN silencioso — Pitfall 2.)
    2. Smoke test (1-2 boletines): `tsx packages/fichas/src/pipeline-cli.ts --boletines <b1>,<b2> --reembed --service-key "$SUPABASE_SECRET_KEY"`. Confirmar en el log: el link real se resolvió, `obtenerTextoFuente` bajó texto legible (no basura/PDF binario), y la extracción produjo `idea_matriz` no-null. Si el link devuelve PDF binario → reportar (A2 fallback: descartar esos o añadir extracción; NO fabricar idea matriz).
    3. Backfill completo: `tsx packages/fichas/src/pipeline-cli.ts --reembed --service-key "$SUPABASE_SECRET_KEY"`. Confirmar el log final `LIVE: ... dbLoaded=true` (NO "DRY-RUN"). Idempotente/reanudable.
    4. Gate psql (data must-have): `psql "$SUPABASE_DB_URL" -c "select count(*) filter (where idea_matriz is not null) as con_idea, count(*) as total from proyecto_ficha;"` → `con_idea > 0`. Reportar la cobertura real (cuántos de 74 traen idea matriz; el resto degrada honesto, aceptable).
  </how-to-verify>
  <verify>Operador: el log final del CLI dice `LIVE: ... dbLoaded=true`; `psql "$SUPABASE_DB_URL" -c "select count(*) filter (where idea_matriz is not null) from proyecto_ficha;"` devuelve > 0.</verify>
  <done>Backfill LIVE corrido (dbLoaded=true); `count(idea_matriz) > 0` en proyecto_ficha; boletines sin texto quedan null (honest-state), no fabricados.</done>
  <acceptance_criteria>
    - Keys confirmadas en el entorno ANTES del backfill (no DRY-RUN accidental).
    - Smoke test de 1-2 boletines produce idea_matriz no-null con texto legible (o reporta PDF/fallo honesto).
    - Backfill completo corrió LIVE (`dbLoaded=true`, no DRY-RUN).
    - `psql count(idea_matriz) > 0` en `proyecto_ficha` (gate explícito SC3).
    - Cobertura real reportada; boletines sin texto quedan `idea_matriz=null` (honest-state), no fabricados.
  </acceptance_criteria>
  <resume-signal>Type "backfilled" con el conteo `con_idea/total`; o describe el fallo (p.ej. link es PDF, key ausente, dbLoaded=false).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → senado.cl | El link_mensaje_mocion se usa para un fetch externo (SSRF surface) |
| pipeline → DeepSeek (LLM) | El texto del proyecto (público) cruza al LLM |
| CLI → Supabase nube | El writer usa la service key (bypassa RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering/SSRF | re-fetch del link_mensaje_mocion | mitigate | `SenadoConnector` reusa `assertAllowedUrl` (@obs/ingest allowlist gubernamental + bloqueo IP privada/metadata) ANTES de cualquier fetch; un link fuera del allowlist degrada, no se busca |
| T-21-03-02 | Information Disclosure | RUT/PII al LLM | accept | El pipeline procesa TEXTO DE PROYECTO (público), no PII de parlamentarios; sin cruce con la maestra. El data-routing gate (assertNoRutInLlmInput) ya existe en @obs/llm. Riesgo bajo |
| T-21-03-03 | Information Disclosure | fuga de service key en logs | mitigate | El writer nunca interpola la key en errores (solo error.message de PostgREST — writer-supabase.ts:87, verificado) |
| T-21-03-04 | Spoofing (dato fabricado) | idea_matriz inventada | mitigate | Degradación honesta obligatoria: sin link/sin texto → idea_matriz=null + honest-state en la ficha; el test asierta que un fetch fallido produce null, no un valor inventado |
| T-21-03-SC | Tampering | npm/pip/cargo installs | mitigate | Cero paquetes nuevos (RESEARCH). Si el operador necesitara un extractor de PDF (caso A2), pasa antes por la Package Legitimacy Gate + checkpoint:human-verify |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas test` verde (link real + degradación honesta).
- [BLOCKING] smoke test + backfill LIVE + `psql count(idea_matriz) > 0` (operador).
- Cobertura real reportada (boletines sin texto = honest null, no fabricados).
</verification>

<success_criteria>
- `leerPendientes`/CLI entrega el link real; el pipeline puebla `idea_matriz`.
- `proyecto_ficha.idea_matriz > 0` en la nube (gate psql).
- Degradación honesta intacta (sin texto → null, nunca fabricado).
</success_criteria>

<output>
Create `.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-03-SUMMARY.md` when done
</output>
