---
phase: 21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0026_parlamentarios_publico_listado.sql
  - supabase/migrations/0027_parlamentarios_publico_listado.test.sql
  - app/app/parlamentarios/page.tsx
  - app/app/parlamentarios/page.test.tsx
  - app/components/parlamentario-directory-row.tsx
  - app/lib/types.ts
autonomous: false
requirements: [SC2, SC4]
tags: [supabase, rls, rpc, directory, nextjs, legal-03]

must_haves:
  truths:
    - "El ciudadano descubre parlamentarios desde la UI sin conocer el id (D####/S####)"
    - "El directorio /parlamentarios lista los 186, filtrable por cámara y por nombre"
    - "Cada item enlaza a /parlamentario/[id]"
    - "El RPC de listado NUNCA emite partido/rut/email a anon (LEGAL-03)"
    - "Un filtro sin resultados muestra honest-empty, no vacío silencioso; un fallo de DB muestra error, no 'sin resultados'"
  artifacts:
    - path: "supabase/migrations/0026_parlamentarios_publico_listado.sql"
      provides: "RPC público parlamentarios_publico() security definer, espejo de 0020"
      contains: "security definer"
    - path: "supabase/migrations/0027_parlamentarios_publico_listado.test.sql"
      provides: "pgTAP: anon no lee partido/rut/email; RPC emite 7 columnas seguras"
      contains: "parlamentarios_publico"
    - path: "app/app/parlamentarios/page.tsx"
      provides: "Ruta directorio RSC con searchParams + Suspense + honest-empty"
      min_lines: 40
    - path: "app/components/parlamentario-directory-row.tsx"
      provides: "Fila de directorio: nombre + cámara + cargo, link a ficha; sin foto/partido"
      min_lines: 15
  key_links:
    - from: "app/app/parlamentarios/page.tsx"
      to: "parlamentarios_publico (RPC)"
      via: "sb.rpc en un Server Component"
      pattern: "rpc\\(\"parlamentarios_publico\""
    - from: "app/components/parlamentario-directory-row.tsx"
      to: "/parlamentario/[id]"
      via: "Link href"
      pattern: "/parlamentario/"
---

<objective>
Entregar la rebanada vertical del directorio de parlamentarios (DB→API→UI): un RPC público `parlamentarios_publico()` espejo de `0020_parlamentario_publico.sql` (sin parámetro, sin PII), su pgTAP de seguridad, y la ruta `/parlamentarios` con búsqueda/filtro por cámara y nombre que enlaza a cada ficha. Cierra la brecha de descubrimiento que el deploy de Phase 20 expuso (hoy solo `/parlamentario/[id]` por id directo).

Purpose: SC2 — el ciudadano DESCUBRE parlamentarios sin conocer el id. El RPC respeta LEGAL-03 deny-by-default (NUNCA partido/rut/email). SC4 — honest-states correctos en el directorio.
Output: migración 0026 + test 0027 + `/parlamentarios/page.tsx` + `parlamentario-directory-row.tsx` + tipo `ParlamentarioListadoRow` en `types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-RESEARCH.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-PATTERNS.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@supabase/migrations/0020_parlamentario_publico.sql

<interfaces>
<!-- RPC análogo EXACTO a espejar: supabase/migrations/0020_parlamentario_publico.sql -->
<!-- Cambios vs 0020: (1) DROP el parámetro `p_id text` y el `where p.id = p_id`; (2) ADD `order by` neutral; (3) DROP las columnas de provenance (origen/fecha_captura/enlace) — el listado no las necesita. -->
RPC objetivo (per 21-PATTERNS.md Pattern 2):
  create or replace function parlamentarios_publico()
  returns table (id text, nombre text, camara text, region text, distrito text, circunscripcion text, periodo text)
  language sql stable security definer set search_path = '' as $$ ... order by p.apellido_paterno nulls last, p.nombre_normalizado; $$;
  grant execute on function parlamentarios_publico() to anon;
PROHIBIDO: ninguna create policy / grant select sobre `parlamentario`; partido/rut/email NUNCA emitidos.

<!-- Columnas de parlamentario (0005_parlamentario.sql): camara NOT NULL; region/distrito/circunscripcion/periodo NULLABLE. partido/rut/email = PII deny-by-default. -->

<!-- Tipo a definir en app/lib/types.ts (espejo de ParlamentarioPublicoRow:109-120 MENOS provenance): -->
export interface ParlamentarioListadoRow {
  id: string; nombre: string; camara: "diputados" | "senado";
  region: string | null; distrito: string | null; circunscripcion: string | null; periodo: string | null;
}

<!-- Análogo de página RSC: app/app/buscar/page.tsx (searchParams Promise + cap MAX_QUERY_CHARS + Suspense + skeleton + honest-empty). -->
<!-- Análogo de fetch RPC error≠empty: app/app/parlamentario/[id]/page.tsx:177-195 (if(error) throw; if(!data) notFound). -->
<!-- Cliente: createServerSupabase() de @/lib/supabase (anon, server-only). -->
<!-- Validación input (V5): MAX_QUERY_CHARS (app/lib/buscar.ts:56) para cap de q; camara ∈ {diputados,senado}. -->

<!-- Análogo de fila: app/components/parlamentario-header.tsx:30-37 (composición de cargoPartes distrito/circunscripcion/region). CamaraChip de @/components/camara-chip. -->
<!-- Copy honest-empty directorio (DESIGN-SYSTEM §7.2): filtro→cero "Sin parlamentarios para este filtro." ; error "No pudimos cargar el directorio…" -->
<!-- Layout: max-w-5xl mx-auto px-4 md:px-8 py-8 md:py-16 ; h1 "Parlamentarios" text-3xl font-semibold leading-tight. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear la migración 0026 (RPC listado) + pgTAP 0027 (gate LEGAL-03)</name>
  <read_first>
    - supabase/migrations/0020_parlamentario_publico.sql (espejo EXACTO — copiar header + cuerpo, ajustar)
    - supabase/migrations/0005_parlamentario.sql (columnas: camara NOT NULL; region/distrito/circunscripcion/periodo nullable; partido/rut/email PII)
    - .planning/phases/21-.../21-PATTERNS.md (Pattern 2 + Shared "Public RPC over deny-by-default maestra"; Pitfall 5 nullable)
    - cualquier *.test.sql pgTAP existente (p.ej. el de 0019/0020 si existe) para el formato de assertion
  </read_first>
  <action>
    Crear `supabase/migrations/0026_parlamentarios_publico_listado.sql` como espejo EXACTO de `0020`: copiar el header de seguridad (el comentario "PROHIBIDO… NINGUNA create policy… NINGÚN grant select que exponga partido/rut/email"), definir `parlamentarios_publico()` SIN parámetro, `language sql stable security definer set search_path = ''`, emitiendo SOLO las 7 columnas `id, nombre, camara, region, distrito, circunscripcion, periodo` (el `nombre` con el mismo `coalesce(nullif(trim(concat_ws(...))), nombre_normalizado)` de 0020), `from public.parlamentario p`, `order by p.apellido_paterno nulls last, p.nombre_normalizado` (orden NEUTRAL, §10.5 — nunca un "ranking"). NO emitir `origen/fecha_captura/enlace` (el listado no los usa). Terminar con `grant execute on function parlamentarios_publico() to anon;`. NO añadir ninguna `create policy` ni `grant select` sobre `parlamentario`.
    Crear `supabase/migrations/0027_parlamentarios_publico_listado.test.sql` (pgTAP): assertions que (a) `parlamentarios_publico()` existe y `anon` tiene execute; (b) la forma de fila tiene EXACTAMENTE las 7 columnas seguras (ninguna llamada `partido`/`rut`/`email`); (c) `anon` NO puede `select partido/rut/email from parlamentario` directamente (deny-by-default sigue en pie). Usar `set role anon` / `has_function_privilege` y `plan(N)`/`finish()` al estilo de los tests pgTAP del repo.
    NOTA: la numeración 0027 para el test es por convención del repo (0025 es el último test SERVEL); si el repo usa otra convención de naming para tests pgTAP (sufijo `.test.sql` sobre el mismo número), seguir la convención observada en los archivos `*.test.sql` existentes.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0026_parlamentarios_publico_listado.sql','utf8'); const body=s.split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n'); if(!/security definer/.test(body)) throw new Error('falta security definer'); if(!/grant execute on function parlamentarios_publico\(\) to anon/.test(body)) throw new Error('falta grant execute a anon'); if(/\b(partido|rut|email)\b/.test(body)) throw new Error('PII en el cuerpo del RPC (LEGAL-03)'); if(/create policy|grant select/i.test(body)) throw new Error('policy/grant select prohibido'); console.log('OK 0026 RPC seguro')"</automated>
  </verify>
  <acceptance_criteria>
    - `0026_*.sql` contiene `security definer` y `grant execute on function parlamentarios_publico() to anon`.
    - El cuerpo (sin comentarios) NO contiene `partido`/`rut`/`email` ni `create policy`/`grant select`.
    - El RPC emite exactamente 7 columnas: id, nombre, camara, region, distrito, circunscripcion, periodo.
    - `0027_*.test.sql` existe con assertions pgTAP de no-exposición de PII.
  </acceptance_criteria>
  <done>La migración del RPC de listado y su pgTAP existen; el cuerpo del RPC es PII-safe por inspección.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definir ParlamentarioListadoRow + componente de fila + ruta /parlamentarios (RSC)</name>
  <read_first>
    - app/app/buscar/page.tsx (análogo de RSC: searchParams Promise, cap, Suspense, skeleton, honest-empty)
    - app/app/parlamentario/[id]/page.tsx:177-195 (patrón error≠empty: if(error) throw)
    - app/components/parlamentario-header.tsx:30-37 (composición de cargo)
    - app/lib/types.ts:109-120 (ParlamentarioPublicoRow — base del tipo nuevo)
    - app/lib/buscar.ts:56 (MAX_QUERY_CHARS), :38 (PARLAMENTARIO_ID_RE)
    - app/node_modules/next/dist/docs/01-app/ (Next 16 searchParams Promise; AGENTS.md)
  </read_first>
  <behavior>
    - Test: render con 186 filas mock del RPC → muestra los 186 (o la página), cada uno con link `/parlamentario/<id>`.
    - Test: filtro `camara=senado` → solo filas con `camara === "senado"`.
    - Test: filtro `q` por nombre → solo filas cuyo nombre incluye `q` (case-insensitive).
    - Test: RPC devuelve `[]` genuino tras filtro → honest-empty "Sin parlamentarios para este filtro." (NO "error").
    - Test: RPC devuelve `error` → la página propaga error (throw), NO "sin resultados".
    - Test: ninguna fila renderiza partido/rut/email/foto (el tipo no los tiene).
  </behavior>
  <action>
    En `app/lib/types.ts`, añadir `ParlamentarioListadoRow` (las 7 columnas seguras, espejo de `ParlamentarioPublicoRow` menos `origen/fecha_captura/enlace`).
    Crear `app/components/parlamentario-directory-row.tsx`: componente presentacional que recibe una `ParlamentarioListadoRow`, renderiza nombre + `<CamaraChip camara={r.camara} />` + la cadena de cargo (distrito/circunscripción/región, reusando el patrón de `parlamentario-header.tsx:30-37`), todo envuelto en un `<Link href={`/parlamentario/${r.id}`}>`. SIN foto, SIN partido (LEGAL-03). Consume `ParlamentarioListadoRow` (NO `ParlamentarioPublicoRow`).
    Crear `app/app/parlamentarios/page.tsx` (RSC): `searchParams: Promise<{ camara?: string|string[]; q?: string|string[] }>`; `const sp = await searchParams`; normalizar `camara` (solo `"diputados"|"senado"`, descartar otro) y `q` (string, `.trim().slice(0, MAX_QUERY_CHARS)`). Shell `max-w-5xl mx-auto px-4 md:px-8 py-8 md:py-16`, `<h1>Parlamentarios</h1>`, `<Suspense key={`${camara}::${q}`} fallback={<DirectorySkeleton/>}>`. Componente async interno `DirectoryList` que: `createServerSupabase()`, `sb.rpc("parlamentarios_publico")`, `if(error) throw new Error(...)` (#34 error≠empty), `rows = (data ?? [])`, filtra server-side por `camara` (NOT NULL, seguro) y por `q` sobre `nombre` (case-insensitive). NO filtrar por region/distrito como eje primario (Pitfall 5: nullable → pierde senadores; región/distrito pueden ser filtros opcionales que toleran null). Si `rows.length === 0` → honest-empty "Sin parlamentarios para este filtro." Mapear a `<ParlamentarioDirectoryRow/>`. Incluir un control de filtro (form GET con `camara` + `q`, progressive-enhancement como `search-box.tsx`) o reusar SearchBox-like; mantenerlo SSR-first. Skeleton `DirectorySkeleton` shape-matched (filas), copiar el patrón de skeleton de `buscar/page.tsx`.
    Crear `app/app/parlamentarios/page.test.tsx` (RTL/Vitest) cubriendo los casos de `<behavior>` (mockear `sb.rpc`).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -20 && pnpm --filter app test -t "parlamentarios" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/types.ts` exporta `ParlamentarioListadoRow` con exactamente las 7 columnas seguras (sin partido/rut/email).
    - `parlamentario-directory-row.tsx` renderiza un `<Link href="/parlamentario/...">` con nombre + CamaraChip + cargo; NO referencia partido/rut/email/foto.
    - `parlamentarios/page.tsx` usa `await searchParams`, llama `sb.rpc("parlamentarios_publico")`, hace `if(error) throw`, filtra por camara/nombre server-side, y muestra honest-empty distinto del error.
    - `pnpm --filter app test -t "parlamentarios"` pasa (render 186, filtro cámara, filtro nombre, honest-empty, error≠empty).
    - `tsc --noEmit` verde en `app/`.
  </acceptance_criteria>
  <done>La ruta /parlamentarios lista, filtra y enlaza a las fichas con honest-states; el tipo y la fila no exponen PII; tests verdes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar 0026 al Supabase remoto + correr pgTAP 0027</name>
  <files>supabase/migrations/0026_parlamentarios_publico_listado.sql, supabase/migrations/0027_parlamentarios_publico_listado.test.sql</files>
  <action>Aplicar la migración 0026 al Supabase remoto y correr el pgTAP 0027 (paso de operador, DDL remoto). Ver how-to-verify para la secuencia exacta. Build/tsc/RTL NO prueban que el RPC existe en la nube; esta aplicacion es obligatoria antes de la verificacion e2e del plan 21-04.</action>
  <what-built>
    Migración `0026_parlamentarios_publico_listado.sql` (RPC público) + pgTAP `0027_*.test.sql`. El build/tsc/RTL pasan SIN la aplicación remota porque el tipo del RPC viene de la config generada, no de la DB viva — la página fallaría en runtime contra la nube si el RPC no existe. La aplicación es obligatoria y es un paso de operador (DDL remoto + BOM/CRLF en .env rompen el CLI sin `--db-url`).
  </what-built>
  <how-to-verify>
    1. Pre-flight: regenerar `~/obs_env.sh` desde `.env` (BOM/CRLF-safe, parser python de Phase 20); cargarlo al shell. Confirmar `SUPABASE_DB_URL` presente.
    2. Aplicar el DDL al remoto: `supabase db push --db-url "$SUPABASE_DB_URL"` (o aplicar `0026_*.sql` directo con psql `/c/Users/Carlo/miniconda3/Library/bin/psql "$SUPABASE_DB_URL" -f supabase/migrations/0026_parlamentarios_publico_listado.sql`). `SUPABASE_DB_URL` bypassa RLS.
    3. Correr el pgTAP: `supabase test db --db-url "$SUPABASE_DB_URL"` y confirmar que `0027_*` pasa (anon no lee partido/rut/email; RPC emite 7 columnas).
    4. Probar el RPC como anon: `psql "$SUPABASE_DB_URL" -c "select * from parlamentarios_publico() limit 3;"` devuelve filas con SOLO id/nombre/camara/region/distrito/circunscripcion/periodo. Auditar que la respuesta NO trae ninguna columna `partido`/`rut`/`email`.
  </how-to-verify>
  <verify>Operador: `supabase test db --db-url "$SUPABASE_DB_URL"` muestra 0027_* verde; `psql "$SUPABASE_DB_URL" -c "select * from parlamentarios_publico() limit 3;"` devuelve 7 columnas sin PII.</verify>
  <done>0026 aplicada al remoto; pgTAP 0027 verde; el RPC emite solo las 7 columnas seguras a anon.</done>
  <acceptance_criteria>
    - `parlamentarios_publico()` existe en el remoto y es ejecutable por anon.
    - pgTAP `0027_*` pasa (incl. la assertion de no-exposición de PII).
    - El payload del RPC tiene exactamente 7 columnas, ninguna de PII.
  </acceptance_criteria>
  <resume-signal>Type "applied" cuando 0026 esté en el remoto y pgTAP 0027 pase; o describe el fallo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /parlamentarios | `camara`/`q` de searchParams = input no confiable |
| RSC anon → RPC | `parlamentarios_publico()` es el ÚNICO canal público a la maestra deny-by-default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | RPC parlamentarios_publico() | mitigate | El RPC emite SOLO 7 columnas de cabecera; NUNCA partido/rut/email. pgTAP 0027 asierta que anon no lee la PII directa y que la firma del RPC no la incluye (LEGAL-03) |
| T-21-02-02 | Tampering | searchParams camara/q | mitigate | `camara` whitelisted a {diputados,senado}; `q` capeado a MAX_QUERY_CHARS; `.rpc()` parametriza (q jamás se interpola en SQL); el id del link reusa PARLAMENTARIO_ID_RE en la ficha destino |
| T-21-02-03 | Information Disclosure | bypass de RLS por policy/grant | mitigate | PROHIBIDO crear policy/grant select sobre `parlamentario`; gate grep en Task 1 + pgTAP en Task 3 confirman deny-by-default intacto |
| T-21-02-04 | Information Disclosure | falso "sin datos" por error de DB | mitigate | `if(error) throw` (#34): un fallo de DB es banner de error, nunca "Sin parlamentarios" — el ciudadano no lee un vacío engañoso |
| T-21-02-SC | Tampering | npm/pip/cargo installs | mitigate | Cero paquetes nuevos (RESEARCH). No aplica install gate |
</threat_model>

<verification>
- `tsc --noEmit` verde en `app/`.
- `pnpm --filter app test -t "parlamentarios"` verde.
- grep gate del cuerpo del RPC (sin PII, sin policy/grant).
- [BLOCKING] 0026 aplicado al remoto + pgTAP 0027 verde (operador).
</verification>

<success_criteria>
- `/parlamentarios` navegable, filtrable por cámara/nombre, enlazando a fichas.
- RPC de listado PII-safe (LEGAL-03) verificado por grep + pgTAP.
- Honest-states correctos (filtro-cero ≠ error).
</success_criteria>

<output>
Create `.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-02-SUMMARY.md` when done
</output>
