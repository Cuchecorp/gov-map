---
phase: 21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/globals.css
  - app/app/layout.tsx
  - app/components/global-header.tsx
autonomous: true
requirements: [SC1, SC4]
tags: [frontend, design-tokens, nextjs]

must_haves:
  truths:
    - "El sitio renderiza fondo crema hsl(40 33% 97%) + acento petróleo, no el blanco clínico v1.0"
    - "Un header global persistente aparece en TODAS las rutas con enlaces a Buscar, Parlamentarios, Agenda, Sobre/Metodología"
    - "El ciudadano puede navegar al directorio /parlamentarios y a /buscar desde cualquier página"
    - "civic-tokens.css queda INTACTO (los colores de cámara/senado no cambian)"
  artifacts:
    - path: "app/app/globals.css"
      provides: "Tokens crema/petróleo extendidos sobre el baseline Slate"
      contains: "--accent-product"
    - path: "app/components/global-header.tsx"
      provides: "Header global server-first con nav"
      min_lines: 20
    - path: "app/app/layout.tsx"
      provides: "Montaje del GlobalHeader en el body"
      contains: "GlobalHeader"
  key_links:
    - from: "app/app/layout.tsx"
      to: "app/components/global-header.tsx"
      via: "import + render dentro de <body>"
      pattern: "GlobalHeader"
    - from: "app/app/globals.css"
      to: "@layer base body"
      via: "hsl(var(--background)) ya consume el token nuevo"
      pattern: "--background: 40 33% 97%"
---

<objective>
Cablear el diseño CERRADO de Phase 19 (DESIGN-SYSTEM.md + 19-UI-SPEC.md) sobre el frontend ya desplegado: extender los tokens de `globals.css` a la paleta crema/petróleo 60/30/10 y montar un `GlobalHeader` persistente en `layout.tsx` que da descubrimiento de navegación (Buscar, Parlamentarios, Agenda, Sobre/Metodología). El diseño está CERRADO — se implementa VERBATIM, no se re-abre ninguna decisión.

Purpose: Eleva el sitio EN VIVO del frontend v1.0 plano (Slate blanco, sin nav global) al producto Phase 19 (SC1). Habilita además el descubrimiento del directorio (SC2) y de la búsqueda desde cualquier ruta.
Output: `globals.css` con tokens nuevos, `global-header.tsx` nuevo, `layout.tsx` montando el header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-RESEARCH.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-PATTERNS.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md

<interfaces>
<!-- Estado actual de globals.css :root (app/app/globals.css:5-26) — Slate baseline. EXTENDER, no reemplazar. -->
Tokens shadcn que NO se tocan: --primary, --primary-foreground, --secondary, --secondary-foreground, --accent, --accent-foreground, --popover*, --input, --radius, --destructive (retune opcional).
Línea 51: `@import "./styles/civic-tokens.css";` — INTACTA. --camara/--senado son identidad de dato, nunca marca.
El bloque `@layer base body` (globals.css:53-65) ya lee `hsl(var(--background))` / `hsl(var(--foreground))` → hereda los tokens nuevos sin cambios.

<!-- Valores LOCKED de los tokens (DESIGN-SYSTEM §1.1 + mockup/landing.html:40-48). Formato HSL space-separated SIN hsl(). -->
:root  → --background: 40 33% 97% · --card: 40 30% 99% · --muted: 40 20% 93% · --muted-foreground: 222 14% 42% · --border: 40 16% 86% · --foreground: 222 47% 11% · --accent-product: 183 38% 26% · --ring: 183 38% 26%
.dark  → --background: 222 28% 7% · --card: 222 24% 12% · --muted: 222 20% 16% · --accent-product: 183 34% 46%

<!-- layout.tsx actual (app/app/layout.tsx). NO tocar generateMetadata (toggle noindex Phase 20, LOCKED). -->
RootLayout devuelve `<html lang="es" className={geist vars}><body>{children}</body></html>`. Montar `<GlobalHeader />` dentro de `<body>` ANTES de `{children}`.

<!-- Análogo de componente server-header: app/components/parlamentario-header.tsx (function, sin "use client", devuelve <header> con clases Tailwind). -->
<!-- Análogo de island "use client" (SOLO si se necesita menú móvil): app/components/search-box.tsx (useState + form GET progressive-enhancement). -->
<!-- GlobalHeader spec: 19-UI-SPEC.md §11.0. wordmark "Observatorio del Congreso 360" → / ; nav derecha: Buscar (/buscar) · Parlamentarios (/parlamentarios) · Agenda (/agenda) · Sobre/Metodología (/sobre). Altura ~56px; px-4 md:px-8; borde inferior --border; fondo crema. Touch targets ≥44px. Item activo subrayado en petróleo. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender los tokens de diseño en globals.css (crema + petróleo)</name>
  <read_first>
    - app/app/globals.css (estado actual — el archivo entero, 72 líneas)
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §1 (Color, valores LOCKED)
    - .planning/phases/21-.../21-PATTERNS.md (Pattern 1: token wiring HSL crudo; Pitfall 4: no romper Slate)
  </read_first>
  <action>
    En `app/app/globals.css`, dentro del bloque `:root` existente, SOBRESCRIBIR solo `--background` (a `40 33% 97%`), `--card` (`40 30% 99%`), `--muted` (`40 20% 93%`), `--muted-foreground` (`222 14% 42%`), `--border` (`40 16% 86%`), `--foreground` (`222 47% 11%`), retune `--ring` (`183 38% 26%`), y AÑADIR la variable nueva `--accent-product: 183 38% 26%`. En el bloque `.dark`, sobrescribir `--background` (`222 28% 7%`), `--card` (`222 24% 12%`), `--muted` (`222 20% 16%`), y añadir `--accent-product: 183 34% 46%`. Formato HSL space-separated SIN `hsl(...)` (per Pattern 1 — mezclar formatos rompe Tailwind v4). NO tocar `--primary`/`--secondary`/`--accent`/`--popover*`/`--input`/`--radius` (shadcn los consume). NO tocar la línea 51 `@import "./styles/civic-tokens.css";`. NO tocar el bloque `@layer base` (ya lee los tokens). El destructive retune (`0 72% 42%` light / `0 62% 45%` dark) es opcional y de bajo riesgo — incluirlo per DESIGN-SYSTEM §1.1 ya que el producto es read-only y el token está sin uso.
    Mapear `--accent-product` como utilidad Tailwind addressable si `tailwind.config.ts` lo requiere para el header (per 19-UI-SPEC §4.4); si las clases `bg-background`/`text-foreground`/`ring` ya cubren el uso del header, no añadir mapping nuevo.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/app/globals.css','utf8'); const cv=require('fs').readFileSync('app/app/styles/civic-tokens.css','utf8'); if(!c.includes('--accent-product')) throw new Error('falta --accent-product'); if(!c.includes('40 33% 97%')) throw new Error('falta crema --background'); if(!c.includes('@import \"./styles/civic-tokens.css\"')) throw new Error('import civic-tokens roto'); if(!c.includes('--primary:')) throw new Error('--primary borrado'); console.log('OK globals tokens')"</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contiene `--accent-product` y `--background: 40 33% 97%`.
    - `globals.css` conserva `--primary:`, `--secondary:`, `--radius:` (no se borraron).
    - La línea `@import "./styles/civic-tokens.css";` sigue presente.
    - `git diff app/app/styles/civic-tokens.css` es VACÍO (archivo intacto).
  </acceptance_criteria>
  <done>Los tokens crema/petróleo están en :root y .dark; el baseline Slate y civic-tokens quedan intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Crear GlobalHeader server-first y montarlo en layout.tsx</name>
  <read_first>
    - app/components/parlamentario-header.tsx (análogo de server header — shape, clases, sin "use client")
    - app/components/search-box.tsx (análogo del island "use client" — SOLO si se añade menú móvil)
    - app/app/layout.tsx (estado actual — montaje del header; NO tocar generateMetadata)
    - .planning/phases/19-.../19-UI-SPEC.md §11.0 (spec del GlobalHeader, VERBATIM)
    - app/node_modules/next/dist/docs/01-app/ (Next 16 — leer ANTES de tocar APIs; AGENTS.md "This is NOT the Next.js you know")
  </read_first>
  <action>
    Crear `app/components/global-header.tsx` como Server Component (función exportada `GlobalHeader`, SIN `"use client"`), devolviendo un `<header>` con: fondo crema (`bg-background`), borde inferior `border-b border-border`, padding horizontal `px-4 md:px-8`, altura ~56px. A la izquierda, el wordmark "Observatorio del Congreso 360" como `<Link href="/">` en Geist Sans semibold. A la derecha (desktop), `<Link>`s a `/buscar` ("Buscar"), `/parlamentarios` ("Parlamentarios"), `/agenda` ("Agenda"), `/sobre` ("Sobre / Metodología"), cada uno con touch target ≥44px (`min-h-11 py-` o equivalente) y el item activo subrayado en petróleo (`--accent-product`). Usar `Link` de `next/link`. Consumir tokens vía clases Tailwind backed por CSS vars (NO hardcodear hex). Para móvil, colapsar la nav: si se requiere un toggle de menú, aislar SOLO ese toggle en un island hijo `"use client"` (espejo de `search-box.tsx` con `useState`), manteniendo el `<header>` server-only — misma división que el repo ya usa (página server + SearchBox island). Si una solución sin JS basta (links siempre visibles / colapso CSS), preferirla y no introducir island. NO auth/login, NO theme toggle obligatorio.
    En `app/app/layout.tsx`: añadir `import { GlobalHeader } from "@/components/global-header";` y renderizar `<GlobalHeader />` dentro de `<body>` ANTES de `{children}`. NO modificar `generateMetadata` (toggle noindex Phase 20, LOCKED). NO cambiar el `<html lang="es" className={...}>` shell.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/global-header.tsx` existe, exporta `GlobalHeader`, devuelve un `<header>` con enlaces a `/buscar`, `/parlamentarios`, `/agenda`, `/sobre`.
    - `global-header.tsx` NO contiene `"use client"` en su primera línea (es server component; un island hijo separado es aceptable solo para el toggle móvil).
    - `layout.tsx` importa y renderiza `<GlobalHeader />` dentro de `<body>`.
    - `layout.tsx` conserva `generateMetadata` sin cambios (`git diff` muestra solo el import + el render del header).
    - `tsc --noEmit` pasa en `app/` sin errores nuevos.
  </acceptance_criteria>
  <done>El header global aparece en todas las rutas con nav a Buscar/Parlamentarios/Agenda/Sobre; el shell del layout y la metadata noindex quedan intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → cliente | El CSS y el header viajan al navegador; no deben filtrar lógica server-only ni secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | global-header.tsx | mitigate | Server component sin acceso a env/keys; solo `<Link>`s estáticos. Si se añade island móvil, este NO recibe props sensibles (solo estado de UI local) |
| T-21-01-02 | Tampering | globals.css token override | accept | Tokens de presentación; romper el baseline Slate es un bug de build (tsc/visual), no una vía de ataque. Mitigado por la verificación grep que asierta que --primary persiste |
| T-21-01-SC | Tampering | npm/pip/cargo installs | mitigate | Cero paquetes nuevos en este plan (RESEARCH: "Sin paquetes nuevos"). No aplica install gate |
</threat_model>

<verification>
- `tsc --noEmit` verde en `app/`.
- `git diff app/app/styles/civic-tokens.css` vacío.
- Verificación visual real ocurre en el plan 21-04 (browseros e2e en producción vs mockup).
</verification>

<success_criteria>
- `globals.css` extiende los tokens crema/petróleo sin romper Slate ni civic-tokens.
- `GlobalHeader` montado en `layout.tsx`, navegable a las 4 superficies.
- Sin regresiones de tipos en `app/`.
</success_criteria>

<output>
Create `.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-01-SUMMARY.md` when done
</output>
