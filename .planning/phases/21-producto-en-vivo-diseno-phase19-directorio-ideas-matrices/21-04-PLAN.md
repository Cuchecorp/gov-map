---
phase: 21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-02", "21-03"]
files_modified: []
autonomous: false
requirements: [SC5, SC4]
tags: [deploy, cloudflare, docker, browseros, e2e]

must_haves:
  truths:
    - "El bundle se construye en Linux (Docker), NO en Windows (Windows 500ea en runtime)"
    - "El sitio en producción muestra el diseño Phase 19 (crema + petróleo), el directorio navegable y las ideas matrices visibles"
    - "noindex sigue activo; MONEY/NET siguen off; sin foto/partido; trazabilidad por dato"
    - "Las secciones lobby/patrimonio sin enlazar muestran el honest-state, nunca vacío silencioso"
  artifacts:
    - path: "app/.open-next"
      provides: "Bundle OpenNext construido en Linux, copiado al host"
      contains: ""
  key_links:
    - from: "docker obsbuild (Linux build)"
      to: "Worker observatorio-congreso (wrangler deploy)"
      via: "docker cp app/.open-next → npx wrangler deploy"
      pattern: "wrangler deploy"
    - from: "producción (workers.dev)"
      to: "diseño + directorio + idea matriz + noindex"
      via: "verificación e2e browseros"
      pattern: "noindex"
---

<objective>
Redeployar el sitio con los tres cambios de los planes 21-01/02/03 (diseño Phase 19, directorio, ideas matrices) construyendo el bundle SOLO en Linux (Docker `docker-cf-build.sh` — el build de Windows 500ea en runtime por `Dynamic require of middleware-manifest.json`), desplegando con `wrangler deploy`, y verificando end-to-end en producción con browseros: diseño nuevo, directorio navegable, ideas matrices visibles, honest-states correctos, `noindex` activo, MONEY/NET off, sin foto/partido.

Purpose: SC5 — redeploy verificado en producción. SC4 — honest-states correctos verificados e2e (lobby/patrimonio 0/0 enlazados → honest-state, no vacío). Es la compuerta de integración que depende de las tres rebanadas anteriores.
Output: bundle Linux desplegado al Worker `observatorio-congreso`; verificación e2e registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-CONTEXT.md
@.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-RESEARCH.md
@docker-cf-build.sh

<interfaces>
<!-- Build/deploy LOCKED (21-CONTEXT.md <code_context> + 21-RESEARCH.md Pattern 5). SOLO Linux. -->
1. docker start -a obsbuild        # contenedor named, sin --rm; reusa node_modules (rápido); corre `pnpm --filter app run cf-build`
2. Remove-Item -Recurse -Force app/.open-next   # PowerShell: borrar el .open-next del host ANTES del cp
3. MSYS_NO_PATHCONV=1 docker cp obsbuild:/build/app/.open-next "<host>\app\.open-next"
4. cd app && npx wrangler deploy   # OAuth ya autenticado (sanchez.rossi@gmail.com); o `pnpm --filter app run deploy` (CON "run")
<!-- NO `pnpm deploy` (colisiona con builtin). NO desplegar el build de Windows. -->

<!-- Worker: observatorio-congreso ; account CF 10fb709d866bb5b06dd2a5d13c8dd472 ; URL https://observatorio-congreso.thevalis.workers.dev -->
<!-- Secrets ya seteados (NO re-setear): SUPABASE_URL, SUPABASE_ANON_KEY, GEMINI_API_KEY. Toggles: PUBLIC_INDEXABLE (noindex, NO encender), MONEY_PUBLIC_ENABLED (off, NO encender). -->

<!-- browseros: server live 127.0.0.1:9200. Driver: .planning/phases/19-.../refs/bros.py (tabs/nav/read/shot). Los tools nativos mcp__browseros__* están desfasados vs el server → USAR bros.py. -->
<!-- Referencia "antes": .planning/phases/20-.../shots/ ; mockup: .planning/phases/19-.../mockup/landing.html -->

<!-- honest-states (DESIGN-SYSTEM §7): lobby/patrimonio 0/0 enlazados → "Esta fuente aún no ha sido consultada…" / "No hay {X} registradas… en las fuentes consultadas." (distintos), nunca vacío silencioso. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Build en Linux (Docker) + deploy a Cloudflare Workers</name>
  <files>app/.open-next (bundle generado por Docker, copiado al host)</files>
  <action>Construir el bundle OpenNext en Linux (contenedor Docker `obsbuild`) y desplegarlo al Worker `observatorio-congreso` con wrangler. Paso de operador. El build de Windows esta PROHIBIDO (500ea en runtime). Ver how-to-verify para la secuencia exacta.</action>
  <read_first>
    - docker-cf-build.sh (contrato del build Linux)
    - .planning/phases/21-.../21-CONTEXT.md <code_context> (gotcha build/deploy LOCKED)
    - .planning/phases/21-.../21-RESEARCH.md Pattern 5 (secuencia exacta) + Pitfall 3 (Windows 500ea)
  </read_first>
  <what-built>
    Los planes 21-01 (tokens + header), 21-02 (directorio + RPC) y 21-03 (ideas matrices en la nube) están en el código/DB. Falta empaquetar el frontend y publicarlo. El build DEBE correr en Linux: el bundle de Windows produce `Dynamic require of "/.next/server/middleware-manifest.json" is not supported` en runtime (500). El deploy usa wrangler con OAuth ya autenticado.
  </what-built>
  <how-to-verify>
    1. Pre-flight: confirmar que 21-01/02/03 están mergeados; `~/obs_env.sh` regenerado (BOM/CRLF-safe); contenedor `obsbuild` disponible (`docker ps -a | grep obsbuild`).
    2. Build Linux: `docker start -a obsbuild` (reusa node_modules; corre `pnpm --filter app run cf-build` → `/build/app/.open-next`). Confirmar que terminó sin error.
    3. Limpiar host: en PowerShell `Remove-Item -Recurse -Force app/.open-next` (borrar el stale del host ANTES del cp).
    4. Copiar el bundle Linux: `MSYS_NO_PATHCONV=1 docker cp obsbuild:/build/app/.open-next "<host>\app\.open-next"`.
    5. Deploy: `cd app && npx wrangler deploy` (o `pnpm --filter app run deploy`, CON "run" — NO `pnpm deploy`). Confirmar que wrangler reporta deploy exitoso al Worker `observatorio-congreso`.
    6. NO setear secrets nuevos (ya están). NO encender PUBLIC_INDEXABLE ni MONEY_PUBLIC_ENABLED.
  </how-to-verify>
  <verify>Operador: `wrangler deploy` reporta exito al Worker `observatorio-congreso`; el bundle desplegado proviene de `app/.open-next` copiado desde el contenedor Linux.</verify>
  <done>Bundle Linux desplegado al Worker; sin secrets nuevos ni toggles encendidos.</done>
  <acceptance_criteria>
    - El build corrió en el contenedor Linux `obsbuild` (no en Windows).
    - `app/.open-next` del host fue borrado y reemplazado por el bundle Linux vía `docker cp`.
    - `wrangler deploy` reportó éxito al Worker `observatorio-congreso`.
    - No se setearon secrets nuevos ni se encendieron toggles.
  </acceptance_criteria>
  <resume-signal>Type "deployed" cuando wrangler reporte el deploy exitoso; o describe el fallo.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Verificación e2e en producción con browseros</name>
  <files>(verificacion e2e: no edita archivos)</files>
  <action>Verificar end-to-end el sitio redesplegado con browseros (driver bros.py): diseno Phase 19, directorio navegable, ideas matrices, honest-states, noindex activo, MONEY/NET off, sin foto/partido. Ver how-to-verify para los pasos exactos.</action>
  <read_first>
    - .planning/phases/19-.../refs/bros.py (driver browseros)
    - .planning/phases/19-.../mockup/landing.html (referencia de diseño)
    - .planning/phases/19-.../DESIGN-SYSTEM.md §7 (honest-states esperados)
  </read_first>
  <what-built>
    El sitio redesplegado en `https://observatorio-congreso.thevalis.workers.dev`. Verificación visual y funcional e2e con browseros (driver `bros.py`, server 127.0.0.1:9200 — los tools nativos mcp__browseros__* están desfasados).
  </what-built>
  <how-to-verify>
    Con `python .planning/phases/19-.../refs/bros.py` (nav + read + shot), en producción:
    1. **Diseño (SC1):** abrir `/`; confirmar fondo crema (no blanco clínico) + acento petróleo; comparar con `mockup/landing.html`. Confirmar el `GlobalHeader` presente con nav a Buscar/Parlamentarios/Agenda/Sobre.
    2. **Directorio (SC2):** abrir `/parlamentarios`; confirmar que lista parlamentarios (≈186), filtrar por cámara (Senado/Cámara), buscar por nombre, y hacer click en uno → su ficha `/parlamentario/<id>` carga. Confirmar SIN foto, SIN partido.
    3. **Ideas matrices (SC3):** abrir una ficha de proyecto `/proyecto/<boletin>` que el backfill pobló; confirmar que `idea_matriz` (y cuerpos legales si aplica) renderiza CON su provenance/fuente. En un boletín sin texto, confirmar honest-state (no idea matriz fabricada).
    4. **Honest-states (SC4):** en una ficha de parlamentario, confirmar que las secciones lobby/patrimonio (0/0 enlazadas) muestran el honest-state de DESIGN-SYSTEM §7 ("Esta fuente aún no ha sido consultada…" / "No hay … registradas… en las fuentes consultadas."), distintos entre sí y del error, NUNCA un vacío silencioso.
    5. **Principios rectores (SC5):** confirmar `noindex` activo (inspeccionar `<meta name="robots">` o headers → index:false); MONEY/NET off (sin secciones #dinero/#financiamiento; `/contraparte/<id>` 404); sin foto/partido en ninguna superficie; ProvenanceBadge por dato.
  </how-to-verify>
  <verify>Operador: browseros (bros.py) sobre la URL de produccion confirma los 5 criterios (diseno, directorio, idea matriz, honest-states, noindex+MONEY/NET off+sin foto/partido), con screenshots.</verify>
  <done>Produccion muestra el diseno Phase 19, directorio navegable, ideas matrices y honest-states correctos; noindex activo; MONEY/NET off; sin foto/partido; provenance por dato.</done>
  <acceptance_criteria>
    - `/` muestra crema + petróleo + GlobalHeader navegable (paridad razonable vs mockup).
    - `/parlamentarios` lista ≈186, filtra por cámara/nombre, enlaza a fichas; sin foto/partido.
    - Una ficha de proyecto del backfill muestra `idea_matriz` con fuente; boletines sin texto muestran honest-state.
    - lobby/patrimonio sin enlace muestran el honest-state correcto (distinto de vacío y de error).
    - `noindex` activo; MONEY/NET off (`/contraparte/<id>` 404); sin foto/partido; provenance por dato.
  </acceptance_criteria>
  <resume-signal>Type "verified" si todos los criterios e2e pasan en producción; o describe las discrepancias (con screenshots de bros.py).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → Worker (producción) | El bundle desplegado sirve datos públicos a internet |
| Worker → público | noindex + MONEY/NET gates protegen la exposición prematura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information Disclosure | indexación prematura | mitigate | `PUBLIC_INDEXABLE` NO se enciende (noindex sigue fail-closed hasta sign-off legal Ley 21.719); verificado e2e en `<meta robots>` |
| T-21-04-02 | Information Disclosure | exposición de MONEY/NET | mitigate | `MONEY_PUBLIC_ENABLED` off (default); `/contraparte/[id]` 404 page-level; secciones #dinero/#financiamiento ausentes del HTML — verificado e2e |
| T-21-04-03 | Information Disclosure | foto/partido en la UI | mitigate | El RPC de listado y la cabecera no emiten partido; sin foto en ninguna superficie — verificado e2e (LEGAL-03) |
| T-21-04-04 | Denial of Service | bundle Windows 500ea | mitigate | Build SOLO en Linux (Docker); `app/.open-next` del host borrado y reemplazado por el bundle Linux antes del deploy (Pitfall 3) |
| T-21-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero paquetes nuevos; el build reusa node_modules cacheados del contenedor `obsbuild` |
</threat_model>

<verification>
- [BLOCKING] build Linux + `wrangler deploy` exitoso (operador).
- [BLOCKING] e2e browseros en producción: diseño, directorio, idea matriz, honest-states, noindex, MONEY/NET off, sin foto/partido (operador).
</verification>

<success_criteria>
- Bundle Linux desplegado al Worker `observatorio-congreso`.
- Producción muestra el diseño Phase 19, directorio navegable e ideas matrices.
- noindex activo; MONEY/NET off; sin foto/partido; honest-states correctos; trazabilidad por dato.
</success_criteria>

<output>
Create `.planning/phases/21-producto-en-vivo-diseno-phase19-directorio-ideas-matrices/21-04-SUMMARY.md` when done
</output>
