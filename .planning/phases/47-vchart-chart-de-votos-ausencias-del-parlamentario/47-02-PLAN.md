---
phase: 47-vchart-chart-de-votos-ausencias-del-parlamentario
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - Temp/votos-chart-47-evidencia.png
autonomous: true
requirements: [VIZ-02]

must_haves:
  truths:
    - "El gate completo (test/typecheck/lockdown-guard/banned-vocab) está verde ANTES de desplegar"
    - "PROD redeployado con el chart 'Cuándo votó' en vivo en la ficha de parlamentario"
    - "El redeploy ARRASTRA los fixes pendientes de F38 (RailSkeleton anti-CLS, pluralización '1 parlamentario', fecha NaN-safe)"
    - "Smoke confirma /parlamentario/[id] 200 con la sección Votaciones y el heading 'Cuándo votó'"
    - "Evidencia visual capturada del chart en un parlamentario con votos confirmados"
  artifacts:
    - path: "Temp/votos-chart-47-evidencia.png"
      provides: "Captura del detalle de Votaciones con el stacked bar por trimestre en vivo"
  key_links:
    - from: "docker-cf-build.sh (.open-next)"
      to: "wrangler deploy"
      via: "docker cp al host + wrangler (OAuth, SIN MSYS_NO_PATHCONV)"
      pattern: "wrangler deploy"
---

<objective>
Cerrar la fase 47: gate completo verde, redeploy del frontend a PROD con el patrón
caliente (docker-cf-build.sh → docker cp → wrangler), smoke que confirma el chart
"Cuándo votó" en vivo, y evidencia visual. El redeploy ARRASTRA los fixes ya
commiteados de F38 (RailSkeleton anti-CLS, pluralización "1 parlamentario", fecha
NaN-safe) que aún no están en PROD.

Purpose: poner VIZ-02 en vivo para la demo y saldar el arrastre de F38 en un solo
deploy.
Output: PROD redeployado + `Temp/votos-chart-47-evidencia.png` + veredicto de smoke.

NOTA: esta fase NO tiene DDL ni checkpoint operador propio (el chart deriva de datos
ya fetcheados). El checkpoint operador de la 38 (apply RPC 0049) sigue pendiente
APARTE y NO es parte de esta fase. Deploy autorizado por operador (2026-07-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-01-SUMMARY.md

# Patrón de deploy caliente + evidencia same-origin (léelo — comandos exactos):
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-03-SUMMARY.md

<interfaces>
<!-- Contexto de deploy ya validado (38-03-SUMMARY) -->
- URL PROD: https://observatorio-congreso.thevalis.workers.dev
- Deploy caliente: `docker-cf-build.sh` (OpenNext build en Docker Linux — el worker de
  Windows está roto) → `docker cp` de `.open-next` al host → `npx wrangler deploy`
  desde `app/` (OAuth host vivo; `wrangler whoami` OK). NUNCA correr wrangler bajo
  MSYS_NO_PATHCONV. Contenedor de build: patrón `docker rm/run obs-cf-build`.
- Fixes F38 YA commiteados (arrastre): 6fcd678 (IN-01 pluralización + IN-02 fecha
  NaN-safe/fechaCortaSegura), e28c051 (IN-03 RailSkeleton anti-CLS). Están en el
  árbol pero no necesariamente en la versión PROD actual → este redeploy los publica.
- Evidencia: helper same-origin `Temp/reshot2.mjs` (iframe in-process, fullPage sin
  clipping OOPIF). Parlamentario con votos confirmados: la sección "Votaciones" debe
  mostrar la barra "Cómo votó" poblada (dataset = 186 parlamentarios con voto).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate completo verde ANTES de desplegar (no deploy sobre rojo)</name>
  <files>(verificación — sin cambios de código)</files>
  <read_first>
    - .planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-03-SUMMARY.md (Task 1 — gate verde antes del deploy)
  </read_first>
  <action>
    Corre el gate completo desde `app/`: `npx vitest run` (debe incluir lockdown-guard y los tests de banned-vocab por-componente — todos dentro de la suite) y `npx tsc --noEmit`. Baseline 690 más los tests nuevos de 47-01: la suite NUNCA debe quedar por debajo de 690. Si algo está rojo, NO se despliega — se reporta y se detiene. Confirma explícitamente en el reporte: nº de tests pasados y `tsc` exit 0.
  </action>
  <verify>
    <automated>cd app && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Suite verde (≥690, nunca menos) + tsc exit 0; gate confirmado antes de tocar deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy caliente a PROD (arrastra fixes F38) + smoke curl</name>
  <files>(deploy — sin cambios de código)</files>
  <read_first>
    - .planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-03-SUMMARY.md:48-51 (comandos exactos del deploy caliente + smoke)
  </read_first>
  <action>
    Ejecuta el deploy caliente (patrón 38-03): build OpenNext en Docker Linux vía `docker-cf-build.sh` (contenedor `obs-cf-build` — `docker rm` previo si existe, luego `docker run`), `docker cp` de `.open-next` al host, y `npx wrangler deploy` desde `app/` (verifica `wrangler whoami` primero; NUNCA bajo MSYS_NO_PATHCONV). Confirma la versión desplegada con `wrangler deployments status` (al 100%).
    Smoke con curl sobre PROD (`https://observatorio-congreso.thevalis.workers.dev`): `/`, `/parlamentarios`, y `/parlamentario/[id]` de un parlamentario CON votos confirmados (identifícalo desde el directorio o reusa uno conocido con la sección Votaciones poblada) → todos 200. En la ficha del parlamentario con votos, confirma que el heading "Cuándo votó" está presente en el HTML servido (el detalle es forceMount SSR aunque arranque colapsado). Confirma cero `internal server error`/`application error`.
    Arrastre F38: verifica en el HTML servido que los fixes están en vivo — pluralización "1 parlamentario" (no "1 parlamentarios") donde aplique y ausencia de "Invalid Date" en cualquier fecha (fecha NaN-safe). RailSkeleton es anti-CLS (no siempre visible en HTML estático) — basta con confirmar 200 sin regresión de layout.
  </action>
  <verify>
    <automated>cd app && npx wrangler deployments status</automated>
  </verify>
  <done>Versión nueva al 100%; smoke 4 rutas 200; heading "Cuándo votó" presente en la ficha con votos; sin 500; fixes F38 en vivo (sin "Invalid Date", pluralización correcta).</done>
</task>

<task type="auto">
  <name>Task 3: Evidencia visual del chart en vivo + verificación anti-insinuación</name>
  <files>Temp/votos-chart-47-evidencia.png</files>
  <read_first>
    - .planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-03-SUMMARY.md:53-56 (Temp/reshot2.mjs — helper de screenshot same-origin)
    - 47-UI-SPEC.md "Copywriting Contract" (strings a confirmar en la captura)
  </read_first>
  <action>
    Con el mismo parlamentario-con-votos del smoke, captura evidencia del detalle de Votaciones EXPANDIDO mostrando el stacked bar "Cuándo votó" por trimestre (reusa el helper same-origin `Temp/reshot2.mjs` de 38-03, ajustando la ruta al `/parlamentario/[id]` y, si aplica, el searchParam que abre el disclosure). Guarda `Temp/votos-chart-47-evidencia.png`. Verifica visualmente: barras apiladas discretas por trimestre (AAAA · Tn en el eje X, Mono), leyenda con labels NOUN (A favor / En contra / Abstención / Pareo / Ausente), colores semánticos (verde/rojo/ámbar/slate — CERO petróleo), heading "Cuándo votó" + caption "…No representa una tendencia.". Confirma anti-insinuación: cero superlativo/ranking/causal en la superficie; empty honesto si el parlamentario no tuviera fechas agrupables.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');process.exit(fs.existsSync('Temp/votos-chart-47-evidencia.png')?0:1)"</automated>
  </verify>
  <done>Captura del chart en vivo guardada; inspección visual confirma stacked-por-trimestre, leyenda NOUN, colores semánticos (no petróleo), copy factual; cero insinuación.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Cloudflare | deploy vía wrangler (OAuth host); build en Docker Linux, cp al host |
| PROD → ciudadano | superficie pública de solo-lectura; el chart no introduce PII nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Denial of service (regresión) | deploy sobre rojo | mitigate | Task 1 exige gate verde ANTES del deploy; no se despliega sobre suite/tsc rojos |
| T-47-05 | Information disclosure | ficha pública | accept | CERO RPC/DDL/dato nuevo — el chart deriva de `votos_de_parlamentario` ya público (PII-safe, confirmadas); no expone superficie nueva |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | CERO dependencia nueva en toda la fase (Recharts ya instalado) |
</threat_model>

<verification>
- `cd app && npx vitest run && npx tsc --noEmit` → verde (≥690) + exit 0.
- `wrangler deployments status` → versión nueva al 100%.
- curl smoke: `/`, `/parlamentarios`, `/parlamentario/[id-con-votos]` → 200; heading "Cuándo votó" en el HTML; sin 500; fixes F38 en vivo.
- `Temp/votos-chart-47-evidencia.png` existe y muestra el chart en vivo.
</verification>

<success_criteria>
- Chart "Cuándo votó" en vivo en PROD; gate verde antes del deploy; fixes F38 arrastrados.
- Evidencia visual capturada; anti-insinuación verificada en la superficie desplegada.
- Fase 47 lista para checkpoint de fase estándar.
</success_criteria>

<output>
Create `.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-02-SUMMARY.md` when done
</output>
