---
phase: 47-vchart-chart-de-votos-ausencias-del-parlamentario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/votos-chart.tsx
  - app/components/votos-chart.test.tsx
autonomous: true
requirements: [VIZ-02]

must_haves:
  truths:
    - "El detalle de Votaciones muestra un chart apilado de votos por trimestre encima de 'Cómo votó'"
    - "Cada barra = un trimestre (AAAA · Tn) con conteos por sentido (si/no/abstención/pareo/ausente)"
    - "Filas sin fecha parseable se excluyen; si eso vacía la serie, se muestra copy honesto (nunca barra en cero)"
    - "El chart es SIEMPRE stacked bar discreto — jamás línea/área (no insinúa tendencia)"
    - "Cero lenguaje de ranking/superlativo/causal en el copy nuevo (negative-match verde)"
  artifacts:
    - path: "app/components/votos-chart.tsx"
      provides: "Isla cliente Recharts (stacked BarChart) que recibe VotoPeriodo[] plano"
      contains: "use client"
      min_lines: 60
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "Agregador puro agruparVotosPorTrimestre + tipo VotoPeriodo + sub-bloque 'Cuándo votó' en VotosView"
      contains: "agruparVotosPorTrimestre"
    - path: "app/components/votos-chart.test.tsx"
      provides: "RTL del chart (mock recharts) + source-scan no-leak"
    - path: "app/components/votos-por-parlamentario.test.tsx"
      provides: "RTL del agregador (bucketing/exclusión/single-bucket) + copy/empty-state"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx (VotosView)"
      to: "app/components/votos-chart.tsx (VotosChart)"
      via: "prop periodos: VotoPeriodo[]"
      pattern: "<VotosChart"
    - from: "app/components/votos-por-parlamentario.tsx (derivarVotosViewData)"
      to: "agruparVotosPorTrimestre"
      via: "cómputo server-side sobre todasConMateria"
      pattern: "agruparVotosPorTrimestre\\("
---

<objective>
Dar a la ficha de parlamentario su chart de evolución temporal de votos: un stacked
BarChart discreto por trimestre montado al TOPE del detalle colapsado de Votaciones
(capa-2), sobre las filas que `VotosSection` YA fetchea (`votos_de_parlamentario`).
CERO fetch nuevo, CERO RPC, CERO DDL, CERO dependencia nueva (Recharts 3.9.0 ya
instalado). Patrón técnico = F46 `patrimonio-chart.tsx` verbatim (isla cliente +
agregador puro server-side).

Purpose: VIZ-02 — la demo del centro de estudios gana con una visual de evolución
clara; el patrón F46 (stacked-NO-line, conteo neutro, degrade honesto) traslada la
gramática anti-insinuación LOCKED al dominio de votos.
Output: `votos-chart.tsx` (isla nueva) + `agruparVotosPorTrimestre`/`VotoPeriodo` en
`votos-por-parlamentario.tsx` + sub-bloque "Cuándo votó" en `VotosView` + RTL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-CONTEXT.md
@.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-UI-SPEC.md

# Análogo técnico DIRECTO (léelo entero antes de escribir la isla):
@app/components/patrimonio-chart.tsx
@app/components/patrimonio-chart.test.tsx

# Archivo a extender + su capa-1 (fuente única de colores/orden de sentidos):
@app/components/capa1/votos-capa1.tsx

<interfaces>
<!-- Contratos que el ejecutor necesita — extraídos del codebase. Úsalos directo. -->

Tipo de fila YA fetcheada (app/lib/types.ts) que el agregador recibe (vía
VotoFichaConMateria = VotoFichaRow & { materia: string | null }):
```typescript
export interface VotoFichaRow {
  votacion_id: string;
  boletin: string;
  fecha: string;              // ISO — puede ser vacío/no-ISO en filas degradadas
  seleccion: Seleccion;       // "si" | "no" | "abstencion" | "pareo" | "ausente"
  etapa: string | null;
  camara: "diputados" | "senado";
  origen: string;
  fecha_captura: string;
  enlace: string | null;
  // …sustancia (titulo/idea_matriz/resultado/total_*) omitida aquí
}
export type Seleccion = "si" | "no" | "abstencion" | "pareo" | "ausente";
```

Orden LOCKED de sentidos + mapa de color (single-source con VotosCapa1 SEGMENTO,
votos-capa1.tsx:21-27). Recharts necesita fills explícitos (como patrimonio-chart):

| Sentido | clase capa-1 | fill Recharts | label NOUN |
|---------|--------------|---------------|------------|
| si         | bg-green-500 | hsl(142 71% 45%) | A favor |
| no         | bg-red-500   | hsl(0 84% 60%)   | En contra |
| abstencion | bg-amber-400 | hsl(43 96% 56%)  | Abstención |
| pareo      | bg-slate-400 | hsl(215 20% 65%) | Pareo |
| ausente    | bg-slate-300 | hsl(213 27% 84%) | Ausente |

El import type-only cruza la frontera cliente sin arrastrar server (como
patrimonio-chart.tsx:13 `import type { SeriePunto } from "./patrimonio-de-parlamentario"`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregador puro VotoPeriodo[] por trimestre + wiring en derivarVotosViewData</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx:196-235 (resumenDeArco — ya parsea `new Date(e.fecha)` y guarda min/max; misma técnica de fecha)
    - app/components/votos-por-parlamentario.tsx:113-153 (VotosViewData — dónde añadir `periodos`)
    - app/components/votos-por-parlamentario.tsx:759-839 (derivarVotosViewData — dónde computar `periodos`)
    - app/lib/format.ts:121-129 (fechaCortaSegura — patrón guard ISO slice(0,10)+regex antes de new Date)
  </read_first>
  <behavior>
    - Test: `agruparVotosPorTrimestre([])` → `[]`.
    - Test: filas de 2024-02, 2024-05, 2024-08, 2024-11 → 4 periodos "2024 · T1".."2024 · T4" con el conteo del sentido correspondiente.
    - Test: dos filas del MISMO trimestre (2024-04 y 2024-06, ambas "si") → UN periodo "2024 · T2" con si=2.
    - Test: fila con fecha "" o null o "no-iso" se EXCLUYE (no lanza, no crea barra); si TODAS carecen de fecha → `[]`.
    - Test: un solo trimestre con datos → array de length 1 (no se rellenan trimestres vacíos entre medio).
    - Test: sin huecos fabricados — periodos sólo para trimestres con ≥1 voto; orden ascendente por año y trimestre.
    - Test: la salida es JSON plano (solo number|string por campo) — cruza la frontera al cliente.
  </behavior>
  <action>
    Exporta el tipo `VotoPeriodo` = `{ periodo: string; si: number; no: number; abstencion: number; pareo: number; ausente: number }` (todos number salvo `periodo`). `periodo` = etiqueta "AAAA · Tn" (p.ej. "2024 · T2"), trimestre = `Math.floor(mes0/3)+1` con `mes0` el mes 0-indexado.
    Escribe `export function agruparVotosPorTrimestre(votos: VotoFichaConMateria[]): VotoPeriodo[]`: PURA, sin fetch. Para cada fila: guarda la fecha con el patrón de `fechaCortaSegura` (slice(0,10) + regex `^\d{4}-\d{2}-\d{2}$`) — si no matchea, EXCLUYE la fila (omisión honesta, jamás una barra en cero). Deriva año+trimestre, acumula el conteo por `seleccion` en un Map keyed por `periodo`. Devuelve SOLO los periodos con ≥1 voto, ordenados ascendente (parsea año y n de trimestre para orden numérico estable, no lexical). Agrega sobre TODO el conjunto confirmado (global, independiente de la faceta de tema) — el chart "Cuándo votó" es el arco completo del registro.
    Añade `periodos: VotoPeriodo[]` a la interfaz `VotosViewData`. En `derivarVotosViewData`, computa `const periodos = agruparVotosPorTrimestre(todasConMateria)` (sobre `todasConMateria`, NO sobre la página) e inclúyelo en el objeto devuelto.
    Docstring: explica por qué stacked-por-trimestre y no serie continua (anti-insinuación: una línea insinuaría trayectoria), espejo del docstring F46.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/votos-por-parlamentario.test.tsx</automated>
  </verify>
  <done>agruparVotosPorTrimestre exportada + tests verdes; VotosViewData tiene `periodos`; derivarVotosViewData lo computa sobre todasConMateria; tsc limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isla cliente votos-chart.tsx (stacked BarChart discreto) + RTL no-leak</name>
  <files>app/components/votos-chart.tsx, app/components/votos-chart.test.tsx</files>
  <read_first>
    - app/components/patrimonio-chart.tsx:1-100 (isla análoga — cópiala en estructura: "use client", SERIES array, ResponsiveContainer/BarChart/Bar stackId, role=img+aria-label)
    - app/components/patrimonio-chart.test.tsx:22-55 (mock de recharts en jsdom) y :317-342 (source-scan no-leak — asertar patrón)
    - Bloque <interfaces> de este plan (mapa de fills/labels/orden)
  </read_first>
  <behavior>
    - Test (source-scan, espejo patrimonio-chart.test.tsx:319-341): el archivo empieza con `"use client"`, importa `from "recharts"`, importa `VotoPeriodo` SOLO como type (`import type`), NUNCA `createServerSupabase` ni `@/lib/supabase`; NUNCA `LineChart|AreaChart|<Line|<Area`; NUNCA `runtime = "edge"`; NUNCA `--accent-product`/petróleo en fills.
    - Test: `dataKey="periodo"` presente en el XAxis; `stackId="votos"` en las barras.
    - Test (RTL con recharts mockeado): con ≥1 VotoPeriodo monta `rc-barchart`; el contenedor tiene `role="img"` y `aria-label="Número de votos por trimestre y sentido del voto"`.
  </behavior>
  <action>
    Crea `app/components/votos-chart.tsx` con `"use client"` en la línea 1, espejando `patrimonio-chart.tsx`. `import type { VotoPeriodo } from "./votos-por-parlamentario"` (type-only, erased). Define `SERIES` en el orden LOCKED si→no→abstencion→pareo→ausente con `dataKey` (keyof VotoPeriodo), `label` NOUN y `fill` exacto de la tabla del bloque <interfaces>. Componente `export function VotosChart({ periodos }: { periodos: VotoPeriodo[] })`: contenedor `div className="h-72 w-full"` con `role="img"` + `aria-label="Número de votos por trimestre y sentido del voto"`; dentro `ResponsiveContainer width="100%" height="100%"` → `BarChart data={periodos}` con `XAxis dataKey="periodo"`, `YAxis allowDecimals={false}`, `Tooltip`, `Legend`, y una `<Bar>` por SERIE con `stackId="votos"`, `name={s.label}`, `fill={s.fill}`. Petróleo PROHIBIDO — los colores CODIFICAN el dato. Sin `LineChart`/`AreaChart`. Docstring espejo del F46 (honestidad: stacked discreto, jamás línea; fills = colores semánticos single-source con VotosCapa1).
    En el test, mockea recharts como en patrimonio-chart.test.tsx (ResizeObserver + passthrough con data-testid). Añade el source-scan con `readFileSync(\`${import.meta.dirname}/votos-chart.tsx\`, "utf8")`.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/votos-chart.test.tsx</automated>
  </verify>
  <done>votos-chart.tsx existe (use client, recharts, sin fuga supabase, sin Line/Area); source-scan + RTL verdes; tsc limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Montar sub-bloque "Cuándo votó" al tope del detalle + empty-state + copy factual</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx:445-557 (VotosView — early returns noIngestado/totalVotos===0 y el div `space-y-10`; el chart va como PRIMER hijo, ANTES del bloque "Cómo votó" en :508)
    - app/components/votos-por-parlamentario.tsx:510 (patrón del `<h3 className="text-sm font-semibold">` existente)
    - 47-UI-SPEC.md "Copywriting Contract" (strings EXACTOS de heading/caption/empty)
  </read_first>
  <behavior>
    - Test: con `periodos` no vacío, VotosView renderiza el heading EXACTO "Cuándo votó", la isla `rc-barchart` (recharts mockeado) y la caption EXACTA "Cada barra agrupa las votaciones de un trimestre por sentido del voto. No representa una tendencia."
    - Test: `totalVotos>0` pero `periodos=[]` (votos sin fecha parseable) → muestra el empty EXACTO "Las fechas de estas votaciones aún no permiten agruparlas por período." y NO monta `rc-barchart` (jamás barra en cero).
    - Test: `noIngestado=true` y `totalVotos===0` → los early returns existentes ganan; el chart NUNCA se renderiza.
    - Test (banned-vocab / negative-match): el textContent del render con chart NO matchea `/el más|la más|el que más|ranking|score|índice de|tendencia(?! )|porque|a cambio de|gracias a|influenc|presi[oó]n/i` — salvo la palabra "tendencia" que SOLO aparece en la caption que la NIEGA ("No representa una tendencia."). Aserta además que la caption está presente.
    - Test: el sub-bloque "Cuándo votó" aparece ANTES que "Cómo votó" en el orden del DOM.
  </behavior>
  <action>
    En `VotosView`, mockea recharts en el test (mismo doble que Task 2). Importa `VotosChart` (isla cliente) al tope del archivo. Añade el sub-bloque como PRIMER hijo del `<div className="space-y-10">` (línea ~507), ANTES del bloque "Cómo votó": un `<div>` con `<h3 className="text-sm font-semibold">Cuándo votó</h3>`; si `data.periodos.length > 0` → `<VotosChart periodos={data.periodos} />` seguido de la caption `<p className="text-sm text-muted-foreground mt-4">Cada barra agrupa las votaciones de un trimestre por sentido del voto. No representa una tendencia.</p>`; si `data.periodos.length === 0` → SOLO el empty `<p className="text-sm text-muted-foreground mt-2">Las fechas de estas votaciones aún no permiten agruparlas por período.</p>` (nunca la isla). El sub-bloque vive DENTRO del detalle (VotosView ya es el subtree del DetalleColapsable) y no cruza la frontera `mt-12` del carril (no la toca). CERO superlativo/ranking/causal (§9.1). Extiende el test con los casos de <behavior>, reusando el helper `makeViewData`/fixtures existentes del archivo y añadiendo `periodos` a los fixtures.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/votos-por-parlamentario.test.tsx components/votos-chart.test.tsx</automated>
  </verify>
  <done>El sub-bloque "Cuándo votó" se monta encima de "Cómo votó" con chart/caption o empty-state honesto; banned-vocab verde; suite completa verde; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client island | `VotosChart` recibe solo `VotoPeriodo[]` serializado; jamás importa el cliente Supabase server-only (Camino A service_role no puede filtrarse al navegador) |
| datos → copy ciudadano | conteos neutros por trimestre; el copy no puede insinuar tendencia/ranking/causalidad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information disclosure | votos-chart.tsx | mitigate | source-scan test asierta `"use client"` + `import type` + ausencia de `createServerSupabase`/`@/lib/supabase` (espejo patrimonio-chart.test.tsx) — la isla nunca arrastra el cliente server al bundle cliente |
| T-47-02 | Tampering (insinuación) | copy "Cuándo votó" + agregador | mitigate | negative-match test (sin superlativo/ranking/causal); stacked BarChart discreto — source-scan prohíbe `LineChart/AreaChart/<Line/<Area` (no fabrica trayectoria) |
| T-47-03 | Tampering (dato falso) | agruparVotosPorTrimestre | mitigate | filas sin fecha parseable EXCLUIDAS (guard ISO), serie vacía → empty-state honesto; jamás barra en cero fabricada (test lo asierta) |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | CERO dependencia nueva — Recharts 3.9.0 ya instalado (F46); no hay tarea de install en esta fase |
</threat_model>

<verification>
- `cd app && npx vitest run` → suite completa verde (baseline 690 + tests nuevos; nunca menos).
- `cd app && npx tsc --noEmit` → exit 0.
- El detalle de Votaciones muestra "Cuándo votó" con stacked bar por trimestre encima de "Cómo votó"; parlamentario con votos sin fecha → empty honesto; cero barra en cero.
</verification>

<success_criteria>
- VIZ-02: chart de evolución temporal de votos montado al tope del detalle, sobre datos ya fetcheados, sin RPC/DDL/deps nuevas.
- Anti-insinuación LOCKED: stacked-NO-line, colores semánticos (no petróleo), degrade honesto, negative-match verde.
- Suite verde + tsc limpio + lockdown-guard + banned-vocab (todos dentro de `vitest run`).
</success_criteria>

<output>
Create `.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-01-SUMMARY.md` when done
</output>
