---
phase: 49-acomp-comparativo-de-ausencias-vs-c-mara
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0050_tasa_ausencia_comparada.sql
  - supabase/tests/0050_tasa_ausencia_comparada.test.sql
  - app/lib/types.ts
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "El shape real de la ausencia en PROD está verificado por psql read-only y los números de D1012 (n_ausencias, m_votaciones, mediana de su cámara, k) quedan documentados en el SUMMARY como evidencia"
    - "Existe la migración 0050 con la RPC tasa_ausencia_comparada(text), security definer, PII-safe, doble revoke, CERO grant — espejo idiom 0049"
    - "El pgTAP 0050 pinea el contrato de columnas (orden posicional), security definer, search_path, anon-no-execute, no-PII, y casos de datos con fixture rollback"
    - "El tipo AusenciaContextoRow existe en types.ts y tasa_ausencia_comparada está en PUBLIC_RPC_ALLOWLIST"
  artifacts:
    - path: "supabase/migrations/0050_tasa_ausencia_comparada.sql"
      provides: "RPC tasa_ausencia_comparada (WRITTEN, NOT applied)"
      contains: "create or replace function public.tasa_ausencia_comparada"
    - path: "supabase/tests/0050_tasa_ausencia_comparada.test.sql"
      provides: "pgTAP del contrato + casos de datos (rollback)"
      contains: "has_function"
    - path: "app/lib/types.ts"
      provides: "AusenciaContextoRow (shape plano PII-safe consumido por el server component)"
      contains: "AusenciaContextoRow"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "allowlist entry para la RPC nueva"
      contains: "tasa_ausencia_comparada"
  key_links:
    - from: "supabase/migrations/0050_tasa_ausencia_comparada.sql"
      to: "revoke all ... from anon, authenticated"
      via: "doble revoke (public + anon/authenticated), CERO grant"
      pattern: "revoke all on function public.tasa_ausencia_comparada"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "PUBLIC_RPC_ALLOWLIST"
      via: "entry string exacto de la RPC"
      pattern: "tasa_ausencia_comparada"
---

<objective>
Escribir (NO aplicar) la migración `0050_tasa_ausencia_comparada.sql` y su pgTAP,
espejando VERBATIM el idiom de `0049_cruces_de_proyecto.sql`, más el tipo TS y la
entrada de allowlist que el frontend (Plan 02) necesitará. La RPC entrega el contexto
factual de asistencia: tasa de ausencia propia del parlamentario y la mediana de su
cámara sobre el universo ingestado — PII-safe, deny-by-default, security definer.

ANTES de escribir el SQL, verificar contra PROD (psql READ-ONLY) el shape real de los
datos y los números reales de D1012 y su cámara, para que el pgTAP y la evidencia se
apoyen en hechos, no supuestos.

Purpose: VIZ-03 — un 0,7% de ausencia no dice nada sin la mediana de la cámara.
Output: 0050_*.sql (escrita, no aplicada) + 0050_*.test.sql + AusenciaContextoRow + allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-CONTEXT.md
@.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-UI-SPEC.md

<!-- Idiom EXACTO a espejar (migración + pgTAP). NO reinventar — copiar la estructura. -->
@supabase/migrations/0049_cruces_de_proyecto.sql
@supabase/tests/0049_cruces_de_proyecto.test.sql

<interfaces>
<!-- Analog del tipo de fila RPC (types.ts:367-386). El nuevo tipo lo espeja en forma plana. -->
De app/lib/types.ts (CruceProyectoRow — analog de estilo/comentario):
- interface con campos escalares planos (number|string), JSDoc que aclara PII-safe.

De app/lib/lockdown-guard.test.ts:165-183 (PUBLIC_RPC_ALLOWLIST):
- Set<string> ordenado alfabéticamente; el arbol público sólo puede invocar RPCs listadas aquí.

Decisión LOCKED del orchestrator — la RPC emite EXACTAMENTE estas columnas:
  n_ausencias (int), m_votaciones (int), tasa_propia (numeric),
  mediana_camara (numeric), k_parlamentarios (int), camara (text).
El % se calcula con 1 decimal es-CL EN EL FRONTEND (Plan 02, formatter existente).
tasa_propia y mediana_camara se emiten como RATIO en [0,1] (paridad con capa1:
`pctFormatter.format(ratio * 100)`); el redondeo del % es discreción del Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar el shape real y los números de D1012 contra PROD (psql READ-ONLY)</name>
  <files>(sin escritura de código — investigación; los hallazgos van al SUMMARY)</files>
  <read_first>
    - CLAUDE.md §"Ingesta y Cron" y memoria env-credentials-reality: psql read-only para research contra PROD (db-url en .env, PGCLIENTENCODING=UTF8 en Windows). SOLO SELECT — CERO DDL/DML.
    - 0049_cruces_de_proyecto.sql:78-88 (cómo se lee voto/votacion: seleccion, estado_vinculo='confirmado', parlamentario_id not null — patrón IDENT-12).
    - app/lib/types.ts:135 (camara: "diputados" | "senado").
  </read_first>
  <action>
    Conectar por psql READ-ONLY a PROD usando la db-url del .env (PGCLIENTENCODING=UTF8).
    Ejecutar SOLO SELECTs (jamás DDL/DML) para verificar y DOCUMENTAR en el SUMMARY:
    (1) Que la ausencia se registra como `voto.seleccion = 'ausente'`:
        `select seleccion, count(*) from public.voto group by 1 order by 2 desc;`
    (2) La cámara del parlamentario vive en `public.parlamentario.camara` y sus valores:
        `select camara, count(*) from public.parlamentario group by 1;`
    (3) Números reales de D1012 (parlamentario del smoke): total de votos confirmados
        (M = filas voto con parlamentario_id='D1012' y estado_vinculo='confirmado') y
        ausencias (N = esas mismas con seleccion='ausente'); calcular tasa_propia = N/M.
    (4) La mediana de su cámara: sobre parlamentarios de la MISMA cámara de D1012 con ≥1
        voto confirmado, la ratio por parlamentario (ausencias/votaciones) y su
        `percentile_cont(0.5) within group (order by ratio)`, más K = conteo de esos
        parlamentarios. Verificar que M(D1012) > 0 (si fuese 0, documentarlo — la RPC
        devolverá 0 filas y la UI omite el bloque).
    Decidir y documentar, con evidencia PROD, si el universo se restringe a
    `estado_vinculo='confirmado'` + `parlamentario_id is not null` (default recomendado,
    espejo IDENT-12 de 0049) — esto pinea los asserts de datos del pgTAP y los números de
    evidencia. Si la conexión psql no está disponible, DETENERSE y reportar el blocker
    (no inventar números).
  </action>
  <verify>
    <automated>MISSING — investigación read-only; la evidencia es la sección "Números PROD verificados" del SUMMARY con N, M, tasa_propia, mediana_camara y K de D1012</automated>
  </verify>
  <done>El SUMMARY documenta: seleccion='ausente' confirmado como fuente de ausencia, camara desde parlamentario.camara, y los números reales de D1012 (N, M, tasa_propia, mediana_camara, K) usados como evidencia del pgTAP.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir 0050_tasa_ausencia_comparada.sql + su pgTAP (espejo VERBATIM de 0049)</name>
  <files>supabase/migrations/0050_tasa_ausencia_comparada.sql, supabase/tests/0050_tasa_ausencia_comparada.test.sql</files>
  <read_first>
    - supabase/migrations/0049_cruces_de_proyecto.sql:55-115 (drop defensivo → create security definer set search_path='' → doble revoke, CERO grant — ESTRUCTURA a espejar).
    - supabase/tests/0049_cruces_de_proyecto.test.sql:10-60 (asserts de contrato: has_function, proargnames en orden, prosecdef, proconfig search_path, anon-no-execute, no-PII regex partido|rut|email) + :62-154 (fixture sembrado en la tx + rollback; casos positivo/negativo).
  </read_first>
  <action>
    Crear `supabase/migrations/0050_tasa_ausencia_comparada.sql`:
    - Encabezado de comentarios explicando: por qué security definer (LEE internamente
      public.parlamentario y public.voto, ambas deny-by-default; emite SOLO derivado
      público), qué es la RPC (contexto factual de asistencia, VIZ-03) y el idiom ACL
      Camino A (espejo 0049:38-46).
    - `drop function if exists public.tasa_ausencia_comparada(text);` (idiom 42P13).
    - `create or replace function public.tasa_ausencia_comparada(p_parlamentario_id text)
      returns table (n_ausencias int, m_votaciones int, tasa_propia numeric,
      mediana_camara numeric, k_parlamentarios int, camara text)
      language sql stable security definer set search_path = '' as $$ ... $$;`
      EXACTAMENTE en ese orden de columnas (el pgTAP y el cliente mapean por posición).
    - Lógica: resolver la cámara del sujeto desde public.parlamentario; el universo de
      votos por parlamentario cuenta filas de public.voto restringidas a
      estado_vinculo='confirmado' y parlamentario_id not null (según lo VERIFICADO en
      Task 1). Propios: N = ausencias del sujeto, M = sus votaciones totales,
      tasa_propia = N::numeric/M (GUARD división por cero: si M=0 → 0 filas, empty
      honesto — NUNCA NaN/Inf). Mediana: sobre parlamentarios de la MISMA cámara con
      M≥1, ratio por parlamentario (ausencias/votaciones), `percentile_cont(0.5) within
      group (order by ratio)` = mediana_camara (RATIO [0,1]); K = conteo de esos
      parlamentarios. `camara` = la del sujeto. JAMÁS emitir partido/rut/email.
    - `revoke all on function public.tasa_ausencia_comparada(text) from public;`
      `revoke all on function public.tasa_ausencia_comparada(text) from anon, authenticated;`
      CERO grant (Camino A: el sitio lee con service_role; re-emitir un grant rompería
      el lockdown-guard Block A y el pgTAP post-apply de 0044).
    Crear `supabase/tests/0050_tasa_ausencia_comparada.test.sql` espejando 0049:
    - Encabezado idéntico en espíritu (corre por `psql -tA -f` contra schema aplicado,
      begin;…;rollback;, NO en el glob de vitest; idiom proargnames vía
      `array_to_string(proargnames, ',')`).
    - Asserts de contrato: (1) has_function con array['text']; (2) proargnames en el
      orden pineado 'p_parlamentario_id,n_ausencias,m_votaciones,tasa_propia,mediana_camara,k_parlamentarios,camara';
      (3) prosecdef true; (4) proconfig like '%search_path=%'; (5) anon NO tiene execute;
      (6) no-PII: proargnames !~* '\y(partido|rut|email)\y'.
    - Fixture sembrado en la tx (rollback): ≥3 parlamentarios en la misma cámara (partido
      NOT null para probar que el returns table NO lo filtra) con votos sembrados de
      ratios conocidos → asserts de DATOS: tasa_propia del sujeto correcta, mediana_camara
      de la cohorte correcta (con números elegidos para una mediana determinista), K
      correcto, y el caso M=0 (parlamentario sin votos → 0 filas, empty honesto).
    - `select * from finish(); rollback;`
    NUNCA aplicar el DDL (es checkpoint operador, Plan 03). Sólo escribir + commitear.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0050_tasa_ausencia_comparada.sql','utf8'); const t=require('fs').readFileSync('supabase/tests/0050_tasa_ausencia_comparada.test.sql','utf8'); const okMig=/create or replace function public\.tasa_ausencia_comparada/.test(s) && /security definer set search_path = ''/.test(s) && /revoke all on function public\.tasa_ausencia_comparada\(text\) from anon, authenticated/.test(s) && !/\bgrant\b/i.test(s.split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n')); const okTest=/has_function/.test(t) && /p_parlamentario_id,n_ausencias,m_votaciones,tasa_propia,mediana_camara,k_parlamentarios,camara/.test(t) && /prosecdef/.test(t) && /partido\|rut\|email/.test(t) && /rollback/.test(t); if(!okMig||!okTest){console.error('mig',okMig,'test',okTest);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>0050_*.sql existe con la RPC security definer + doble revoke + CERO grant; 0050_*.test.sql pinea contrato (orden de columnas), security definer, search_path, anon-no-execute, no-PII y casos de datos con fixture rollback. Ninguna se aplica.</done>
</task>

<task type="auto">
  <name>Task 3: AusenciaContextoRow en types.ts + allowlist en lockdown-guard.test.ts</name>
  <files>app/lib/types.ts, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/lib/types.ts:367-386 (CruceProyectoRow — estilo de interface + JSDoc PII-safe a espejar).
    - app/lib/types.ts:135 (camara: "diputados" | "senado").
    - app/lib/lockdown-guard.test.ts:165-183 (PUBLIC_RPC_ALLOWLIST — Set alfabético; insertar la entrada en orden).
  </read_first>
  <action>
    En `app/lib/types.ts` añadir `export interface AusenciaContextoRow` con la forma plana
    EXACTA que emite la RPC: `n_ausencias: number; m_votaciones: number; tasa_propia:
    number; mediana_camara: number | null; k_parlamentarios: number; camara: "diputados" |
    "senado";`. JSDoc que aclare: tasa_propia/mediana_camara son RATIO [0,1] (el % se
    formatea 1-decimal es-CL en el componente), y que la fila NUNCA trae partido/rut/email
    (deny-by-default, LEGAL-03). mediana_camara nullable → el Plan 02 omite esa línea si es
    null (regla de omisión honesta, sin fabricar número).
    En `app/lib/lockdown-guard.test.ts` añadir `"tasa_ausencia_comparada"` a
    PUBLIC_RPC_ALLOWLIST en su posición alfabética (entre `subgrafo_red` y
    `votos_de_parlamentario`). Sin esto, el guard B FALLARÍA cuando el Plan 02 invoque la
    RPC desde el árbol público (service_role).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx vitest run lib/lockdown-guard.test.ts</automated>
  </verify>
  <done>tsc limpio; lockdown-guard verde con tasa_ausencia_comparada en el allowlist; AusenciaContextoRow exportado con mediana_camara nullable y camara tipada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| árbol público server (service_role) → Postgres RPC | el sitio ejecuta la RPC con service_role (bypassa ACL); la deny-by-default de la DB ya no protege esta ruta → la protección PII es el returns table + el guard CI |
| anon REST → Postgres | anon está a CERO grants (Camino A/0044); un grant re-abriría superficie REST no autenticada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Information Disclosure | RPC tasa_ausencia_comparada returns table | mitigate | El returns table SÓLO emite conteos/ratios/camara; JAMÁS partido/rut/email. pgTAP no-PII (proargnames !~* partido\|rut\|email). parlamentario/voto se leen INTERNAMENTE vía security definer |
| T-49-02 | Elevation of Privilege | EXECUTE de la RPC | mitigate | Doble revoke (public + anon/authenticated), CERO grant; pgTAP anon-no-execute; lockdown-guard Block A bloquea todo grant a anon/public en migraciones >0044 |
| T-49-03 | Tampering | p_parlamentario_id (input) | mitigate | RPC parametrizada language sql (sin SQL dinámico), set search_path='' (nombres schema-qualified); el input es igualdad contra ids conocidos |
| T-49-04 | Information Disclosure (contenido) | tasa/mediana como veredicto | mitigate | La RPC emite ratios neutros; el anti-veredicto (sin adjetivo, sin color, caveat, negative-match) vive en la UI (Plan 02) y su banned-vocab |
| T-49-SC | Tampering | dependencias npm/deno | accept | CERO dependencia nueva en esta fase (sin instalación de paquetes) |
</threat_model>

<verification>
- `node` guard de Task 2 verde (RPC + doble revoke + CERO grant; pgTAP con contrato + no-PII + rollback).
- `npx tsc --noEmit` limpio; `npx vitest run lib/lockdown-guard.test.ts` verde con la entrada nueva.
- SUMMARY documenta los números PROD de D1012 (N, M, tasa_propia, mediana_camara, K).
- NINGUNA migración aplicada (apply = checkpoint operador, Plan 03).
</verification>

<success_criteria>
0050_*.sql escrita (no aplicada) espejando 0049; 0050_*.test.sql con contrato + datos;
AusenciaContextoRow en types.ts; tasa_ausencia_comparada en el allowlist; tsc + lockdown-guard verdes.
</success_criteria>

<output>
Create `.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-01-SUMMARY.md` when done
(incluir la sección "Números PROD verificados" con N, M, tasa_propia, mediana_camara, K de D1012).
</output>
