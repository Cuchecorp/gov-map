---
phase: 49-acomp-comparativo-de-ausencias-vs-c-mara
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - Temp/ausencias-contexto-49-evidencia.png
autonomous: false
requirements: [VIZ-03]
must_haves:
  truths:
    - "El gate completo está verde (pnpm test ≥712, tsc limpio, lockdown-guard, banned-vocab extendido) ANTES del deploy"
    - "PROD está redeployado con el patrón caliente y el smoke confirma degrade honesto pre-apply: el comparativo AUSENTE y capa-1 intacta, sin 500"
    - "El operador aplica 0049 Y 0050, corre ambos pgTAP contra el schema aplicado, y da veredicto visual de cruces-de-proyecto Y del comparativo"
  artifacts:
    - path: "Temp/ausencias-contexto-49-evidencia.png"
      provides: "evidencia visual del sub-bloque en vivo (post-apply o pre-apply degrade)"
  key_links:
    - from: "gate verde"
      to: "redeploy caliente"
      via: "no se despliega sobre rojo (712 floor)"
      pattern: "wrangler deploy"
    - from: "checkpoint operador"
      to: "apply 0049 + 0050 + pgTAP + veredicto visual"
      via: "checkpoint blocking consolidado"
      pattern: "psql .* -f supabase/tests/0050"
---

<objective>
Cerrar la fase: gate completo verde, redeploy caliente a PROD, smoke que verifica el
degrade honesto pre-apply (comparativo ausente, capa-1 intacta), y un ÚNICO checkpoint
operador CONSOLIDADO que aplica 0049 Y 0050, corre ambos pgTAP contra el schema aplicado
y da veredicto visual de ambas superficies (cruces-de-proyecto y comparativo de ausencias).

Purpose: VIZ-03 en vivo, con la única prueba válida del DDL (pgTAP post-apply) en manos del operador.
Output: PROD redeployado + evidencia visual + checkpoint de apply consolidado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-UI-SPEC.md
@.planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate completo verde → redeploy caliente → smoke degrade honesto pre-apply</name>
  <files>Temp/ausencias-contexto-49-evidencia.png</files>
  <read_first>
    - .planning/phases/47-vchart-chart-de-votos-ausencias-del-parlamentario/47-02-SUMMARY.md (patrón EXACTO: gate verde antes del deploy, docker-cf-build.sh → docker cp → wrangler deploy desde host, MSYS_NO_PATHCONV UNSET para wrangler; smoke por curl; helper de screenshot same-origin que EXPANDE el disclosure Radix antes del shot — Temp/reshot-votos.mjs).
    - docker-cf-build.sh (script de build OpenNext Linux).
  </read_first>
  <action>
    1) GATE completo desde `app/` (NO desplegar sobre rojo):
       - `npx vitest run` → verde, conteo ≥ 712 (baseline LOCKED, nunca menos; se esperan
         +N por los RTL de 49-02). Incluye lockdown-guard (con tasa_ausencia_comparada en
         allowlist) y el banned-vocab extendido de ausencias-contexto.
       - `npx tsc --noEmit` → exit 0.
       Si algo está rojo, DETENERSE y reportar — no continuar al deploy.
    2) REDEPLOY caliente (patrón 47-02/38-03): build OpenNext en Docker Linux vía
       `docker-cf-build.sh` → `docker cp` de `.open-next` al host → `npx wrangler deploy`
       desde `app/` (OAuth host; `wrangler whoami` OK; MSYS_NO_PATHCONV UNSET para
       wrangler). Confirmar la versión con `wrangler deployments status`.
    3) SMOKE por curl contra PROD (https://observatorio-congreso.thevalis.workers.dev):
       - `/`, `/parlamentarios`, `/parlamentario/D1012`, `/proyecto/14309-04`, `/agenda`
         → todos 200, sin "internal server error"/"application error".
       - DEGRADE HONESTO PRE-APPLY (la RPC 0050 aún NO está aplicada): en
         `/parlamentario/D1012` el heading "Ausencias en contexto" y el caveat "Sobre las
         votaciones ingestadas" deben estar AUSENTES (grep count 0) mientras "Cómo votó"
         sigue presente (count 1) — el comparativo se omite sin romper la página, capa-1
         intacta. (Nota: el sub-bloque vive en el detalle colapsado; su ausencia se
         confirma en el HTML servido con forceMount/SSR igual que el chart F47.)
    4) EVIDENCIA visual: con el helper same-origin (derivar de Temp/reshot-votos.mjs)
       capturar el estado del detalle de Votaciones expandido y copiar el PNG a
       `Temp/ausencias-contexto-49-evidencia.png`. Pre-apply el sub-bloque estará ausente
       (evidencia del degrade honesto); el veredicto post-apply lo da el operador (Task 2).
    NO aplicar DDL — el deploy PUEDE preceder al apply (LOCKED).
  </action>
  <verify>
    <automated>cd app && npx vitest run 2>&1 | tail -5 && npx tsc --noEmit && node -e "require('fs').existsSync('../Temp/ausencias-contexto-49-evidencia.png')||process.exit(1)"</automated>
  </verify>
  <done>Suite ≥712 verde + tsc limpio ANTES del deploy; PROD redeployado (versión confirmada); smoke 5 rutas 200; degrade honesto verificado (comparativo ausente pre-apply, "Cómo votó" presente, sin 500); evidencia PNG existe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint operador CONSOLIDADO — apply 0049 + 0050 + ambos pgTAP + veredicto visual</name>
  <action>Tarea de OPERADOR (no autónoma): el agente NO aplica DDL. El operador ejecuta los pasos de <how-to-verify> — aplicar 0049+0050 por psql, correr ambos pgTAP contra el schema aplicado, dar veredicto visual de ambas superficies.</action>
  <what-built>
    RPC 0050 tasa_ausencia_comparada + su pgTAP (escritas, no aplicadas), el sub-bloque
    "Ausencias en contexto" desplegado en PROD (degrade honesto pre-apply verificado), y
    la RPC 0049 cruces_de_proyecto + su pgTAP pendientes de apply de F38. Este es el
    ÚNICO checkpoint consolidado: se aplican AMBAS migraciones y se verifican AMBAS superficies.
  </what-built>
  <how-to-verify>
    1) APLICAR ambas migraciones a PROD por psql (NUNCA `supabase db push` — drift de
       schema_migrations; PGCLIENTENCODING=UTF8 en Windows; --single-transaction):
       - `psql "<DB_URL>" --single-transaction -f supabase/migrations/0049_cruces_de_proyecto.sql`
       - `psql "<DB_URL>" --single-transaction -f supabase/migrations/0050_tasa_ausencia_comparada.sql`
    2) Correr AMBOS pgTAP contra el schema APLICADO (única prueba válida del DDL —
       tsc/pnpm test NO prueban que Postgres ejecutó el DDL):
       - `psql "<DB_URL>" -tA -f supabase/tests/0049_cruces_de_proyecto.test.sql` → todos `ok`
       - `psql "<DB_URL>" -tA -f supabase/tests/0050_tasa_ausencia_comparada.test.sql` → todos `ok`
    3) VEREDICTO VISUAL en PROD (https://observatorio-congreso.thevalis.workers.dev):
       - Comparativo de ausencias: abrir `/parlamentario/D1012`, expandir el detalle de
         Votaciones → tras "Cómo votó" aparece "Ausencias en contexto" con "Ausente en N
         de M votaciones (X%)", "Mediana de su cámara: Y% (K parlamentarios)" y el caveat
         de cobertura. Cero adjetivo/color-veredicto; los números coinciden con los de
         D1012 documentados en 49-01-SUMMARY.
       - Cruces de proyecto (F38): abrir una ficha de proyecto con cruce (p.ej. 14309-04)
         → la superficie de cruces se muestra con su caveat anti-causal.
    4) Confirmar que anon sigue denegado (Camino A intacto) — lo cubren los pgTAP
       (anon-no-execute) verdes.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" (con la versión desplegada + resultado de ambos pgTAP) o describe los problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host operador → Postgres PROD | el apply del DDL sólo por psql --single-transaction (nunca db push) |
| deploy → PROD | el degrade honesto debe sostener el deploy-antes-de-apply sin romper capa-1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-09 | Tampering | apply del DDL | mitigate | Checkpoint operador (bloqueante); apply por psql --single-transaction; pgTAP post-apply (0049+0050) como única prueba válida del schema aplicado |
| T-49-10 | Denial of Service | deploy sobre rojo | mitigate | Gate completo (712 floor + tsc + lockdown-guard + banned-vocab) verde ANTES del deploy; no se despliega sobre rojo |
| T-49-11 | Elevation of Privilege | anon post-apply | mitigate | pgTAP anon-no-execute de ambas RPC; lockdown-guard Block A; Camino A intacto (service_role lee, anon a cero grants) |
| T-49-SC | Tampering | dependencias | accept | CERO dependencia nueva en la fase (sin instalación de paquetes) |
</threat_model>

<verification>
- Gate: `npx vitest run` ≥712 verde + `npx tsc --noEmit` exit 0 ANTES del deploy.
- Deploy: `wrangler deployments status` con la versión al 100%; smoke 5 rutas 200.
- Degrade pre-apply: heading "Ausencias en contexto" count 0, "Cómo votó" count 1, sin 500.
- Checkpoint: ambos pgTAP verdes contra el schema aplicado; veredicto visual de ambas superficies.
</verification>

<success_criteria>
Gate verde → PROD redeployado con degrade honesto verificado pre-apply → operador aplica
0049+0050, ambos pgTAP verdes, veredicto visual de cruces-de-proyecto y del comparativo.
</success_criteria>

<output>
Create `.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-03-SUMMARY.md` when done
</output>
