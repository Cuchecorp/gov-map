---
phase: 49-acomp-comparativo-de-ausencias-vs-c-mara
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - app/components/ausencias-contexto.tsx
  - app/components/ausencias-contexto.test.tsx
  - app/components/votos-por-parlamentario.tsx
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "El detalle de Votaciones muestra, INMEDIATAMENTE tras 'Cómo votó', un sub-bloque factual: tasa de ausencia propia + mediana de la cámara + caveat de cobertura"
    - "Con RPC ausente (PGRST202, pre-apply) el sub-bloque se OMITE por completo; capa-1 y el resto del detalle quedan intactos"
    - "Un error real (≠ PGRST202) LANZA (#34) — nunca se degrada a empty (sin falsa exoneración)"
    - "El render no contiene adjetivos ni el negative-match extendido (top/más ausente/peor/mejor asistencia/récord) ni el causal heredado"
  artifacts:
    - path: "app/components/ausencias-contexto.tsx"
      provides: "sub-bloque puro de presentación (recibe shape plano o null)"
      exports: ["AusenciasContexto"]
    - path: "app/components/ausencias-contexto.test.tsx"
      provides: "RTL: render con fixture, degrade null, negative-match extendido"
      min_lines: 40
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "fetch tasa_ausencia_comparada (degrade PGRST202 / throw #34) + montaje tras 'Cómo votó'"
      contains: "tasa_ausencia_comparada"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "tasa_ausencia_comparada"
      via: "sb.rpc en VotosSection, degrade PGRST202→null / #34 throw"
      pattern: "rpc\\(\\s*[\"']tasa_ausencia_comparada"
    - from: "app/components/votos-por-parlamentario.tsx (VotosView)"
      to: "AusenciasContexto"
      via: "montaje tras el div 'Cómo votó', antes de 'Por tema'"
      pattern: "AusenciasContexto"
---

<objective>
Renderizar el comparativo de ausencias como un sub-bloque de texto factual DENTRO del
detalle de Votaciones (capa-2), inmediatamente después de "Cómo votó" y antes de "Por
tema" (placement LOCKED en 49-UI-SPEC). El server hace UN fetch a la RPC
`tasa_ausencia_comparada`, degrada honesto pre-apply (PGRST202 → null → sub-bloque
omitido) y lanza en error real (#34). El componente es presentación pura, testeable con
fixtures RTL, con el copy contract LOCKED y el negative-match extendido.

Purpose: VIZ-03 — la mediana de la cámara es el contexto honesto de la tasa de ausencia.
Output: ausencias-contexto.tsx (sub-bloque puro) + fetch/degrade/montaje + RTL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-UI-SPEC.md

<interfaces>
<!-- Contrato del Plan 01 (ya en el árbol). El componente lo consume, no lo redefine. -->
De app/lib/types.ts (AusenciaContextoRow — creado en 49-01):
  n_ausencias: number; m_votaciones: number; tasa_propia: number;
  mediana_camara: number | null; k_parlamentarios: number; camara: "diputados" | "senado";
  // tasa_propia / mediana_camara son RATIO [0,1] → formatear con pctFormatter.format(ratio*100).

De app/components/capa1/votos-capa1.tsx:55-59 (pctFormatter — reusar la convención es-CL 1 decimal):
  new Intl.NumberFormat("es-CL", { minimumFractionDigits: 1, maximumFractionDigits: 1 })

De app/components/votos-por-parlamentario.tsx:
  - VotosView (511-573): el detalle es `div.space-y-10`; el div "Cómo votó" está en 596-645.
  - VotosSection (936-1035): server component; patrón de fetch RPC + throw #34 (973-977).
  - ResumenLinea (357-399): patrón de línea factual pura con Mono para cifras.

Copy contract LOCKED (49-UI-SPEC §Copywriting), Mono en {…}:
  - "Ausente en {N} de {M} votaciones ({X}%)."
  - "Mediana de su cámara: {Y}% ({K} parlamentarios)."
  - caveat 1×: "Sobre las votaciones ingestadas por este observatorio, no la historia completa."
  - heading opcional: "Ausencias en contexto" (h3 text-sm font-semibold, neutro).
</interfaces>
</context>

<context>
@app/components/cruces-de-proyecto.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sub-bloque AusenciasContexto (puro) + fetch/degrade/montaje en Votaciones</name>
  <files>app/components/ausencias-contexto.tsx, app/components/votos-por-parlamentario.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx:596-645 (div "Cómo votó" — el sub-bloque va JUSTO DESPUÉS, dentro del mismo `div.space-y-10`, antes de "Por tema" en 647-679).
    - app/components/votos-por-parlamentario.tsx:936-1035 (VotosSection: dónde añadir el fetch; 968-977 patrón throw #34).
    - app/components/cruces-de-proyecto.tsx:240-268 (degrade de 3 caminos: PGRST202→null, error real→throw, data→render).
    - app/components/capa1/votos-capa1.tsx:55-73 (pctFormatter + omisión honesta cuando el % no es derivable).
    - app/components/votos-por-parlamentario.tsx:357-399 (ResumenLinea — estilo de línea factual con Mono).
  </read_first>
  <behavior>
    - Test 1: `AusenciasContexto({ data: null })` → renderiza NADA (container vacío).
    - Test 2: con shape {n_ausencias:1, m_votaciones:141, tasa_propia:1/141, mediana_camara:0.032, k_parlamentarios:155, camara:"diputados"} → contiene "Ausente en 1 de 141 votaciones (0,7%)." y "Mediana de su cámara: 3,2% (155 parlamentarios)." y el caveat de cobertura 1×.
    - Test 3: mediana_camara=null → OMITE la línea de mediana (nunca fabrica número); la línea de tasa propia y el caveat siguen.
    - Test 4: singular/plural de "parlamentario(s)" y cifras N/M/X%/Y%/K en font-mono tabular-nums; sin font-bold.
  </behavior>
  <action>
    Crear `app/components/ausencias-contexto.tsx`: componente PURO
    `export function AusenciasContexto({ data }: { data: AusenciaContextoRow | null })`.
    - Si `data` es null → `return null` (omisión honesta; degrade ya resuelto en el server).
    - Formatear con un pctFormatter local es-CL 1-decimal (misma convención que capa1):
      X% = `pctFormatter.format(data.tasa_propia * 100)`, Y% = `pctFormatter.format(data.mediana_camara * 100)`.
    - Heading opcional `<h3 class="text-sm font-semibold">Ausencias en contexto</h3>`
      (neutro, NO superlativo, sin término prohibido), coherente con los h3 hermanos.
    - Línea propia: "Ausente en {N} de {M} votaciones ({X}%)." con N, M, X% en
      `font-mono tabular-nums` (text-sm text-muted-foreground, mt-2, sin bold).
    - Línea mediana (SOLO si mediana_camara != null): "Mediana de su cámara: {Y}%
      ({K} parlamentarios)." con Y% y K en Mono; pluralizar "parlamentario(s)" por K.
    - Caveat 1×: "Sobre las votaciones ingestadas por este observatorio, no la historia
      completa." (text-sm text-muted-foreground). CERO color en las cifras (un % rojo =
      veredicto implícito, PROHIBIDO). CERO chart/barra — sólo texto + Mono.
    - Fabrication guard: si N o M faltan (no derivable), omitir la línea propia en vez de
      inventar; nunca dividir por M=0 (el server ya no envía shape en ese caso, pero
      proteger igual).
    En `app/components/votos-por-parlamentario.tsx`:
    - En VotosSection añadir `const { data: acData, error: acError } = await
      sb.rpc("tasa_ausencia_comparada", { p_parlamentario_id: id });`. Degrade espejo
      de cruces-de-proyecto: `if (acError?.code === "PGRST202") { /* pre-apply → null */ }`
      → ausenciaContexto = null; `else if (acError) throw new Error(
      \`tasa_ausencia_comparada falló para ${id}: ${acError.message}\`);` (#34, sin
      blanket-catch); si hay data → `(acData as AusenciaContextoRow[] | null)?.[0] ?? null`.
    - Pasar el shape a la vista: añadir `ausenciaContexto?: AusenciaContextoRow | null` a
      `VotosViewData` y setearlo sobre el objeto de `derivarVotosViewData` antes de
      renderizar (`<VotosView id={id} data={{ ...data, ausenciaContexto }} />`), o pasar
      como prop directa a VotosView — mantener VotosView puro (recibe el shape ya resuelto).
    - En VotosView, montar `<AusenciasContexto data={data.ausenciaContexto ?? null} />`
      INMEDIATAMENTE después del div "Cómo votó" (tras la línea 645) y ANTES del bloque
      "Por tema" (647). Capa-1 (VotosCapa1) queda byte-idéntica; el chart "Cuándo votó" y
      el resto del orden intactos.
    El sub-bloque NUNCA importa el cliente Supabase (frontera cliente/server LOCKED F45).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx vitest run components/ausencias-contexto.test.tsx</automated>
  </verify>
  <done>AusenciasContexto renderiza las 3 líneas del copy contract (o nada si data null / omite mediana si null); VotosSection hace el fetch con degrade PGRST202→null y throw #34; el sub-bloque se monta tras "Cómo votó"; capa-1 intacta; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: RTL — degrade honesto + negative-match extendido (anti-insinuación)</name>
  <files>app/components/ausencias-contexto.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.test.tsx:711-730 (GATE §6: render completo not.toMatch del regex banned-vocab — patrón a extender).
    - app/components/capa1/cruces-capa1.test.tsx (o cruces-de-proyecto.test.tsx:8-12,172-194) — negative-match inline sobre el DOM + PATRON_RUT.
    - 49-UI-SPEC §Anti-Insinuación Invariants: negative-match extendido NEW LOCKED (top/más ausente/peor/mejor asistencia/récord) + causal heredado.
  </read_first>
  <action>
    Escribir `app/components/ausencias-contexto.test.tsx` (RTL, sin runtime Supabase):
    - Caso null: `render(<AusenciasContexto data={null} />)` → `container.textContent` vacío
      (degrade honesto — el sub-bloque no aparece pre-apply).
    - Caso completo (fixture con números que produzcan "0,7%" y "3,2%"): assert de las 3
      líneas del copy contract verbatim (tasa propia, mediana con K, caveat 1×).
    - Caso mediana null: la línea de mediana AUSENTE; tasa propia + caveat presentes.
    - NEGATIVE-MATCH extendido (LOCKED): sobre el render completo,
      `expect(texto).not.toMatch(/top|más ausente|mas ausente|peor|mejor asistencia|récord|record/i)`
      Y el causal heredado `/afinidad|influencia|conexión|presión|a cambio de|gracias a|porque|score|ranking|índice de|tendencia/i`.
    - Sin RUT en la superficie: `expect(texto).not.toMatch(PATRON_RUT)`.
    - a11y/formato: cifras en `font-mono tabular-nums`, sin `font-bold`/700 (assert de clase).
    Los fixtures son AusenciaContextoRow planos, jamás datos PROD.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/ausencias-contexto.test.tsx</automated>
  </verify>
  <done>RTL verde: degrade null, 3 líneas del copy contract, omisión de mediana null, negative-match extendido + causal + RUT limpios, cifras Mono sin bold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VotosSection (server, service_role) → RPC tasa_ausencia_comparada | fetch server-only; el degrade decide si el hecho aparece |
| render → ciudadano | el % y la mediana no deben leerse como veredicto (anti-insinuación) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Information Disclosure (falsa exoneración) | degrade del fetch | mitigate | SÓLO `error.code === "PGRST202"` → null (nodo ausente); CUALQUIER otro error → throw (#34). Nunca blanket-catch que oculte un error real como "sin ausencias" |
| T-49-06 | Information Disclosure (veredicto implícito) | render del % | mitigate | Sin adjetivo, CERO color en cifras, caveat de cobertura 1×, negative-match extendido (top/más ausente/peor/mejor asistencia/récord) en el test — banned-vocab verde |
| T-49-07 | Tampering (fabricación) | cifras faltantes | mitigate | mediana_camara/N/M null → se OMITE esa línea, nunca se inventa un número (regla de omisión honesta F47) |
| T-49-08 | Spoofing/Information Disclosure | frontera cliente/server | mitigate | El sub-bloque es presentación pura; NUNCA importa el cliente Supabase (F45 LOCKED); capa-1 byte-idéntica |
</threat_model>

<verification>
- `npx tsc --noEmit` limpio; `npx vitest run components/ausencias-contexto.test.tsx` verde.
- Grep: `rpc("tasa_ausencia_comparada"` presente en votos-por-parlamentario.tsx con degrade PGRST202 y throw #34.
- El montaje de `<AusenciasContexto>` está tras el div "Cómo votó" y antes de "Por tema"; capa-1 sin cambios.
</verification>

<success_criteria>
Sub-bloque factual de 3 líneas montado tras "Cómo votó"; degrade honesto PGRST202→null
+ throw #34; negative-match extendido verde; capa-1 intacta; tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/49-acomp-comparativo-de-ausencias-vs-c-mara/49-02-SUMMARY.md` when done
</output>
