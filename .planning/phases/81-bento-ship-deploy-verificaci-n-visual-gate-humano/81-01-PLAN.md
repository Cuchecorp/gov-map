---
phase: 81
plan: "01"
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BENTO-07]
must_haves:
  - "Deploy Cloudflare verde con Version ID registrado; sitio responde 200"
  - "HTTP checks: home contiene kicker OBSERVATORIO DEL CONGRESO + h1 LOCKED + footer CC BY; /parlamentarios 200; /red 200"
  - "master pusheado a origin (Cuchecorp/gov-map)"
---

# Plan 81-01: Deploy bento a Cloudflare (runbook Docker)

**Objetivo:** el bento v8 (fases 76-80) EN VIVO en https://observatorio-congreso.thevalis.workers.dev.

<threat_model>
Deploy-only; cero código nuevo. T-81-01: deploy de estado roto — mitigado por suite 917 verde + tsc pre-deploy y HTTP checks post-deploy. T-81-02: secrets en logs — wrangler OAuth local, no imprimir tokens.
</threat_model>

## Task 1 — Pre-flight

<read_first>
- docs/deploy-cloudflare.md (Opción B)
- docker-cf-build.sh
- .planning/milestones/v6.0-phases/61-comp-comprensi-n-de-visualizaciones-loop-browseros/61-02-SUMMARY.md (gotchas)
</read_first>

<action>
1. Confirmar working tree limpio y suite verde: `git status --short` vacío (o solo .planning), `pnpm --filter ./app test -- --run` verde, `pnpm --filter ./app exec tsc --noEmit` limpio.
2. Push master a origin (Cuchecorp/gov-map): `git push origin master`.
3. Verificar Docker Desktop corriendo: `docker info` (si no corre, reportar BLOCKER al orquestador).
</action>

<acceptance_criteria>
- `git push origin master` exitoso.
- Suite verde confirmada en el log.
</acceptance_criteria>

## Task 2 — Build OpenNext en Docker + deploy wrangler

<read_first>
- docker-cf-build.sh
- app/wrangler.jsonc
</read_first>

<action>
Seguir el runbook EXACTO (61-02 + docs/deploy-cloudflare.md Opción B):
1. Pre-copiar fuente a C:/Temp/obs-build vía PowerShell (robocopy, excluir node_modules, .next, .open-next, .git, .planning, .claude).
2. Build en Docker `node:22-slim` (PowerShell, NO git-bash): montar C:/Temp/obs-build, `docker run -w /app`, dentro: `corepack enable && pnpm config set dangerouslyAllowAllBuilds true && pnpm install --frozen-lockfile && cd app && pnpm exec opennextjs-cloudflare build` (o el comando exacto de docker-cf-build.sh — el script manda).
3. `docker cp` del `.open-next` resultante a `app/.open-next` con ruta Windows EXPLÍCITA (no $(pwd)).
4. Deploy desde app/: `node "C:/Users/Carlo/AppData/Roaming/npm/node_modules/wrangler/bin/wrangler.js" deploy --config wrangler.jsonc` (MSYS_NO_PATHCONV=1 si git-bash).
5. Registrar Version ID del output de wrangler.
</action>

<acceptance_criteria>
- wrangler deploy exitoso con Version ID capturado.
- `curl -s https://observatorio-congreso.thevalis.workers.dev/` → 200 y contiene "OBSERVATORIO DEL CONGRESO" y "Qué pasó con cada proyecto de ley".
- `/parlamentarios` 200; `/red` 200; `/sobre` 200.
</acceptance_criteria>

## Task 3 — SUMMARY

<action>
Escribir 81-01-SUMMARY.md con: Version ID, método, HTTP checks tabla, deviations. Commit.
</action>

<acceptance_criteria>
- SUMMARY committeado.
</acceptance_criteria>

## Verification

- Deploy verde + HTTP checks PASS + push hecho. La verificación BrowserOS y el gate humano son del ORQUESTADOR (81-02, fuera de este plan).
