---
phase: 36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default
plan: 03
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - packages/cruces/src/writer-supabase.ts
  - packages/cruces/src/clasificar-fichas-cli.ts
  - packages/cruces/src/clasificar-lobby-cli.ts
  - packages/cruces/src/golden/golden-set.ts
  - packages/cruces/src/golden/golden-set.test.ts
  - packages/cruces/src/golden/casos.json
autonomous: true
requirements: [CRUCE-02]
user_setup: []

must_haves:
  truths:
    - "El writer hace UPDATE de sector_id vía service-role y NUNCA llama al LLM (etapa derivada separada, D-13)"
    - "Los dos CLIs (fichas DeepSeek / lobby MiniMax) espejan la SHAPE de pipeline-cli.ts (parseArgs, --dry-run, degrade-to-dry-run sin service key)"
    - "El golden score es single-label top-1: correcto=match exacto; null=no-cubierto (no error); sector distinto=error"
    - "El gate del golden exige ≥7/10 con sector_id no-null Y cero misclasificaciones entre los no-null"
  artifacts:
    - path: "packages/cruces/src/writer-supabase.ts"
      provides: "actualizarSectorFicha / actualizarSectorContraparte (service-role UPDATE, sin LLM)"
      exports: ["actualizarSectorFicha", "actualizarSectorContraparte"]
    - path: "packages/cruces/src/clasificar-lobby-cli.ts"
      provides: "CLI batch contrapartes (MiniMax + RUT-gate)"
      contains: "decidirDryRun"
    - path: "packages/cruces/src/golden/golden-set.ts"
      provides: "Golden harness single-label top-1 + abstención + GOLDEN_SET_GATE"
      exports: ["evaluarGolden", "GOLDEN_SET_GATE"]
    - path: "packages/cruces/src/golden/casos.json"
      provides: "~40 casos golden (LLM-propone + humano-valida, D-06)"
      contains: "sector_codigo"
  key_links:
    - from: "packages/cruces/src/clasificar-lobby-cli.ts"
      to: "clasificarContraparte + writer-supabase"
      via: "selecciona filas, clasifica, UPDATE sector_id"
      pattern: "actualizarSectorContraparte"
    - from: "packages/cruces/src/golden/golden-set.test.ts"
      to: "golden-set.ts + casos.json"
      via: "evaluarGolden contra el mock-provider"
      pattern: "GOLDEN_SET_GATE"
---

<objective>
Construir los dos CLIs batch de clasificación (`clasificar-fichas-cli` DeepSeek / `clasificar-lobby-cli` MiniMax), el writer service-role que actualiza `sector_id` (jamás llama al LLM), y el golden propio (~40 casos, scoring single-label top-1 + abstención) con su gate ≥7/10. Espeja la SHAPE de `pipeline-cli.ts` y la estructura del harness de `golden-set.ts` de fichas, REEMPLAZANDO el scoring de substring por top-1/abstención.

Purpose: Cerrar el pipeline derivado de etiquetado de sector como etapa batch separada del writer y verificarlo con un golden propio (CRUCE-02 gate `--dry-run` ≥7/10). El golden es el contrato de calidad del clasificador antes de la corrida LIVE de Plan 04.
Output: CLIs + writer + golden gate verdes en CI (mock-provider, sin red). La corrida LIVE real vive en Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-CONTEXT.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-RESEARCH.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-PATTERNS.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-02-SUMMARY.md
@packages/fichas/src/pipeline-cli.ts
@packages/fichas/src/writer-supabase.ts
@packages/fichas/src/golden/golden-set.ts

<interfaces>
<!-- De Plan 02 (36-02-SUMMARY.md) -->
clasificarFicha(input, provider): Promise<ClasificacionSector>        // DeepSeek (bulk/public)
clasificarContraparte(input, provider): Promise<ClasificacionSector>  // MiniMax (critical/personal), RUT-gate first
ClasificacionSector = { sector_codigo: <enum SECTOR_CODIGOS> | null }
mock-provider keyed por case id (CI sin red)

<!-- pipeline-cli.ts SHAPE a espejar (NO el contenido de extracción) -->
parseArgs(argv) con fail-fast (:68-118); --service-key empty-value guard (:100-110)
decidirDryRun({serviceKey, dryRun}) (:124-126): sin service key -> dry-run con warning (NUNCA silent no-write)
env-driven config (:149-152): SUPABASE_URL/SUPABASE_SECRET_KEY/DEEPSEEK_API_KEY/MINIMAX_API_KEY
isMain entry-point guard (:244-247)

<!-- writer-supabase.ts (fichas) SHAPE -->
createClient(url, serviceKey, { auth:{ persistSession:false, autoRefreshToken:false } }) (:91-93)
service key NUNCA en mensajes de error (:104-105); chunk/dedupePorClave (:64-81)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI sin service key → escritura | Degradar a dry-run explícito; nunca escribir silenciosamente ni filtrar la service key |
| contraparte CLI → LLM | El CLI de lobby hereda el gate de RUT de clasificarContraparte (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Information Disclosure | writer error messages | mitigate | service key NUNCA interpolada en errores; solo `error.message` de PostgREST (mirror fichas :104-105) |
| T-36-10 | Tampering | silent no-write | mitigate | `decidirDryRun`: sin service key → dry-run con warning explícito (mirror pipeline-cli :124-126) |
| T-36-11 | Information Disclosure | inline LLM en writer | mitigate | writer SOLO hace UPDATE; jamás llama clasificar* (two-stage, D-13); verificado por grep |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: writer-supabase.ts (service-role UPDATE de sector_id, sin LLM)</name>
  <files>packages/cruces/src/writer-supabase.ts</files>
  <read_first>
    - packages/fichas/src/writer-supabase.ts:64-81 (chunk/dedupePorClave), :91-93 (createClient sin sesión), :104-105 (service key nunca en error), :125-131 (.update().eq() shape)
    - packages/cruces/src/index.ts (de Plan 02, para exports)
    - 36-PATTERNS.md §"writer-supabase.ts"
  </read_first>
  <action>Crear writer-supabase.ts espejando packages/fichas/src/writer-supabase.ts: `createClient(url, serviceKey, { auth:{ persistSession:false, autoRefreshToken:false } })`; service key JAMÁS interpolada en mensajes de error (solo `error.message`); `chunk`/dedupe para UPDATE en lotes. Métodos `actualizarSectorFicha(boletin, sector_id)` → `.update({ sector_id }).eq('boletin', boletin)` sobre proyecto_ficha; `actualizarSectorContraparte(identificador, nombre, sector_id)` → UPDATE sobre lobby_contraparte por su clave. Este writer NUNCA importa ni llama clasificar*/provider/LLM (etapa derivada separada, D-13). Re-exportar desde index.ts.</action>
  <verify>
    <automated>! grep -qiE "clasificar|complete\(|provider" packages/cruces/src/writer-supabase.ts && grep -q "actualizarSectorContraparte" packages/cruces/src/writer-supabase.ts && pnpm --filter @obs/cruces typecheck</automated>
  </verify>
  <acceptance_criteria>writer-supabase.ts contiene `actualizarSectorFicha` y `actualizarSectorContraparte`, NO contiene ninguna referencia a `clasificar`/`complete(`/`provider` (grep limpio = two-stage respetado), y la service key no aparece en strings de error. `pnpm --filter @obs/cruces typecheck` pasa.</acceptance_criteria>
  <done>Writer escrito; grep confirma cero LLM; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: golden-set.ts (single-label top-1 + abstención) + casos.json + gate ≥7/10</name>
  <files>packages/cruces/src/golden/golden-set.ts, packages/cruces/src/golden/golden-set.test.ts, packages/cruces/src/golden/casos.json</files>
  <read_first>
    - packages/fichas/src/golden/golden-set.ts:459-523 (evaluarGolden driver), :476-515 (scoring a REEMPLAZAR), :13-15 (nota LIVE)
    - packages/cruces/src/mock-provider.ts + clasificar.ts (de Plan 02)
    - 36-CONTEXT.md D-06 (~40 casos LLM-propone+humano-valida), D-07 (single-label top-1), D-08 (abstención=no-cubierto, no error)
    - 36-RESEARCH.md §"Pitfall 3" (abstención NO es error)
  </read_first>
  <behavior>
    - 10 casos, todos con sector correcto → 10/10 cobertura, 0 errores → gate PASA
    - 10 casos, todos abstención (null) → 0/10 cobertura (falla el bar ≥7) pero 0 errores
    - Un caso con sector distinto al golden → cuenta como error → gate FALLA aunque cobertura sea alta
  </behavior>
  <action>casos.json: ~40 casos `{ id, tipo:'ficha'|'contraparte', input:{...}, sector_codigo: <codigo>|null }` (D-06: el set corregido es el golden; usar códigos de SECTOR_CODIGOS). golden-set.ts: espejar la ESTRUCTURA de fichas (`CasoGolden[]`, `MetricasGolden`, `evaluarGolden(set, ejecutar)`, `GOLDEN_SET_GATE`) pero REEMPLAZAR el scoring (D-07/D-08): por caso — `actual === expected` (match exacto) → correcto/cubierto; `actual === null` → no-cubierto (baja cobertura, NUNCA error); `actual !== null && actual !== expected` → misclasificación (error). Métricas: `cobertura = #correctos / #total`, `errores = #misclasificaciones`. `GOLDEN_SET_GATE`: cobertura sobre los 10 de muestra ≥ 0.7 (≥7/10 no-null correctos) AND errores === 0. NO copiar los thresholds precision≥0.95/recall≥0.80 ni el F1 por substring. golden-set.test.ts: corre `evaluarGolden` con el mock-provider y asierta el gate; incluir los 3 casos de behavior arriba como tests unitarios del scoring (cobertura/error contados correctamente). Nota LIVE: bloque gated por env que cambia el mock por DeepSeek/MiniMax (espejo de golden-set.ts:13-15) — NO corre en CI.</action>
  <verify>
    <automated>pnpm --filter @obs/cruces test golden</automated>
  </verify>
  <acceptance_criteria>golden-set.ts exporta `evaluarGolden` y `GOLDEN_SET_GATE`; el scoring trata abstención (null) como no-cubierto (NO error) y sector-distinto como error. casos.json tiene ~40 casos. `pnpm --filter @obs/cruces test golden` pasa, incluyendo los 3 casos de behavior: 10-correctos→pasa, 10-abstención→0 cobertura/0 errores, 1-sector-distinto→error que falla el gate.</acceptance_criteria>
  <done>Golden harness con scoring top-1/abstención + gate ≥7/10; tests verdes (mock).</done>
</task>

<task type="auto">
  <name>Task 3: clasificar-fichas-cli.ts (DeepSeek) + clasificar-lobby-cli.ts (MiniMax)</name>
  <files>packages/cruces/src/clasificar-fichas-cli.ts, packages/cruces/src/clasificar-lobby-cli.ts</files>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts:68-118 (parseArgs fail-fast), :100-110 (--service-key empty-value guard), :124-126 (decidirDryRun), :149-152 (env-driven config), :244-247 (isMain)
    - packages/cruces/src/clasificar.ts + writer-supabase.ts (de Plan 02 + Task 1)
    - 36-PATTERNS.md §"clasificar-fichas-cli.ts + clasificar-lobby-cli.ts" (qué NO reusar de pipeline-cli)
    - 36-RESEARCH.md §"Validation Architecture" (gate dry-run ≥7/10)
  </read_first>
  <action>clasificar-fichas-cli.ts: espejar la SHAPE de pipeline-cli.ts — `parseArgs(argv)` con fail-fast (incl. `--service-key` empty-value guard), `decidirDryRun({serviceKey, dryRun})` (sin service key → dry-run con warning, NUNCA silent), env-driven config (SUPABASE_URL/SUPABASE_SECRET_KEY/DEEPSEEK_API_KEY), `isMain` guard. Flujo: seleccionar filas de proyecto_ficha (idea_matriz cuando exista, si no titulo; `--limite` para acotar), `clasificarFicha` por fila (DeepSeek), y en modo no-dry `actualizarSectorFicha`. En `--dry-run` sobre 10 proyectos: medir cobertura vs el golden y reportar ≥7/10 (CRUCE-02 gate). NO reusar correrPipeline/obtenerTextoFuente/SenadoConnector/R2/--reembed/--boletines. clasificar-lobby-cli.ts: idéntica SHAPE pero `clasificarContraparte` (MiniMax, hereda el RUT-gate de Plan 02) sobre lobby_contraparte, `MINIMAX_API_KEY`, y en no-dry `actualizarSectorContraparte`. Ambos: cero vocabulario causal en logs.</action>
  <verify>
    <automated>grep -q "decidirDryRun" packages/cruces/src/clasificar-fichas-cli.ts && grep -q "decidirDryRun" packages/cruces/src/clasificar-lobby-cli.ts && ! grep -qiE "correrPipeline|obtenerTextoFuente|SenadoConnector" packages/cruces/src/clasificar-fichas-cli.ts && pnpm --filter @obs/cruces typecheck</automated>
  </verify>
  <acceptance_criteria>Ambos CLIs contienen `decidirDryRun` (degrade-to-dry-run) y `parseArgs` con `--service-key` guard; clasificar-fichas-cli NO reusa `correrPipeline`/`obtenerTextoFuente`/`SenadoConnector` (grep limpio); clasificar-lobby-cli usa `clasificarContraparte` (MiniMax). `pnpm --filter @obs/cruces typecheck` pasa. La corrida LIVE real (poblar sector_id) es Plan 04.</acceptance_criteria>
  <done>Dos CLIs batch escritos con dry-run/degrade; typecheck verde; sin contenido de extracción literal.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/cruces test` y `typecheck` verdes (mock-provider, sin red).
- Writer sin LLM (grep); CLIs con degrade-to-dry-run; golden gate ≥7/10 con scoring top-1/abstención.
- La corrida LIVE que realmente puebla sector_id = Plan 04.
</verification>

<success_criteria>
- CLIs + writer + golden escritos y verdes en CI.
- CRUCE-02 (pipeline separado, dry-run ≥7/10 vs golden propio, abstención correcta) cubierto end-to-end a nivel de código.
</success_criteria>

<output>
Create `.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-03-SUMMARY.md` when done. Documentar los comandos de los CLIs (flags, env) y el gate del golden (consumidos por la corrida LIVE de Plan 04).
</output>
