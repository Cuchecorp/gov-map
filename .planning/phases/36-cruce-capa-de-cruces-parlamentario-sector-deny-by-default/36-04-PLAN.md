---
phase: 36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default
plan: 04
type: execute
wave: 3
depends_on: ["36-01", "36-03"]
files_modified: []
autonomous: false
requirements: [CRUCE-01, CRUCE-02, CRUCE-03]
user_setup: []

must_haves:
  truths:
    - "0038/0039/0040 aplicadas al Supabase remoto PROD vía psql --db-url (NUNCA db push)"
    - "Las tres filas 0038/0039/0040 quedan registradas en schema_migrations"
    - "pgTAP 0038/0039/0040 verdes contra el MISMO DB donde se aplicaron las migraciones (sin regresión en suites previas)"
    - "anon NO puede leer cruce_senal ni ejecutar cruces_de_parlamentario (probe deny-by-default)"
    - "Tras clasificar lobby LIVE + materializar, cruce_senal tiene ≥1 fila para ≥5 parlamentarios distintos"
  artifacts: []
  key_links:
    - from: "clasificar-lobby-cli (LIVE)"
      to: "lobby_contraparte.sector_id"
      via: "UPDATE service-role poblando sector_id antes de materializar"
      pattern: "sector_id"
    - from: "cruces.materializar_cruces()"
      to: "cruce_senal"
      via: "full rebuild leyendo lobby_audiencia confirmadas + sector_id"
      pattern: "materializar_cruces"
---

<objective>
Aplicar las tres migraciones de cruces al Supabase remoto PROD por `psql --db-url` (la ÚNICA ruta válida), correr las pgTAP contra el DB aplicado, y ejecutar la corrida LIVE que satisface CRUCE-03: clasificar contrapartes de lobby (poblar `lobby_contraparte.sector_id`) → `materializar_cruces()` → confirmar ≥1 fila de señal para ≥5 parlamentarios distintos.

Purpose: Sin aplicar las migraciones al DB real y correr la corrida LIVE, la fase queda en falso-positivo (tipos/lint verdes pero el DB no tiene los objetos ni la señal). MEMORY: pgTAP-vs-PROD encuentra bugs que el mock no — la corrida PROD es obligatoria.
Output: Migraciones aplicadas + pgTAP verde + cruce_senal poblada (≥5 parlamentarios). Deny-by-default confirmado. CERO grant a anon (Phase 39 lo enciende, no este plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-01-SUMMARY.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-03-SUMMARY.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-RESEARCH.md
@supabase/migrations/0038_sector.sql
@supabase/migrations/0039_cruce_senal.sql
@supabase/migrations/0040_cruces_rpc.sql

<interfaces>
<!-- Patrón de apply (MEMORY + STATE.md blockers 35-01 Task 4): -->
psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/00XX_*.sql   # en orden 0038->0039->0040
<!-- Esquivar BOM U+FEFF al extraer SUPABASE_DB_URL (helper Phases 9-12/35). NUNCA `supabase db push`. -->
<!-- Registrar fila en schema_migrations por cada migración aplicada. -->
<!-- pgTAP: -->
psql "$SUPABASE_DB_URL" -f supabase/tests/00XX_*.test.sql   # 0038/0039/0040 sin fallos + sin regresión en 0021/0030/0034/0035/0036/0037
<!-- CLIs (de 36-03-SUMMARY): clasificar-lobby-cli con --service-key real puebla lobby_contraparte.sector_id (MiniMax). -->
<!-- Materializar: select cruces.materializar_cruces(); -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → DB remoto PROD | Solo `psql --db-url`; jamás `db push` (drift schema_migrations) |
| anon → cruce_senal / RPC | Probe post-apply DEBE devolver permission denied / no execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-12 | Tampering | apply path | mitigate | `psql --db-url --single-transaction -v ON_ERROR_STOP=1` en orden + fila schema_migrations; NUNCA db push |
| T-36-13 | Information Disclosure | anon read post-apply | mitigate | Probe con anon key: `select 1 from cruce_senal` → 42501; RPC sin execute (verificado pgTAP + probe) |
| T-36-14 | Elevation of Privilege | grant prematuro | accept | El plan NO emite grant a anon; el encendido es exclusivo del sign-off humano Phase 39 |
| T-36-SC | Tampering | npm/pip installs | accept | Esta fase NO instala paquetes externos (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING] Aplicar 0038/0039/0040 al remoto + pgTAP verde</name>
  <what-built>Las tres migraciones (Plan 01) y los CLIs/golden (Plan 03) están escritos y verdes en CI. Falta aplicarlas al Supabase remoto PROD — la única ruta de verificación real (los tipos/lint pasan sin aplicar, dejando un falso-positivo donde el DB carece de los objetos).</what-built>
  <read_first>
    - .planning/STATE.md §Blockers (35-01 Task 4: patrón exacto de apply por psql --db-url + esquivar BOM + schema_migrations + pgTAP)
    - supabase/migrations/0038_sector.sql, 0039_cruce_senal.sql, 0040_cruces_rpc.sql (Plan 01)
    - supabase/tests/0038_sector.test.sql, 0039_cruce_senal.test.sql, 0040_cruces_rpc.test.sql
  </read_first>
  <how-to-verify>
    1. Extraer `SUPABASE_DB_URL` de `.env` esquivando el BOM U+FEFF (helper Phases 9-12/35).
    2. Aplicar EN ORDEN: `psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0038_sector.sql`, luego 0039, luego 0040. (Windows: usar heredoc/stdin si hay deltas multibyte — MEMORY.) NUNCA `supabase db push`.
    3. Registrar las 3 filas en `schema_migrations` (0038/0039/0040).
    4. pgTAP: `psql "$SUPABASE_DB_URL" -f supabase/tests/0038_sector.test.sql` (y 0039, 0040) → todos sin fallos; correr también 0021/0030/0034/0035/0036/0037 para confirmar CERO regresión.
    5. Probe deny-by-default con anon key: `select 1 from cruce_senal limit 1` → debe dar permission denied (42501); confirmar que anon NO tiene execute sobre `cruces_de_parlamentario`.
    6. Confirmar el cron: `select count(*) from cron.job where jobname='cruces-materializar'` = 1.
  </how-to-verify>
  <action>Aplicar 0038/0039/0040 al Supabase remoto PROD por `psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f <mig>` EN ORDEN (0038 -> 0039 -> 0040), NUNCA `supabase db push` (drift schema_migrations; última en PROD = 0037). Esquivar el BOM U+FEFF al extraer SUPABASE_DB_URL (helper Phases 9-12/35). Registrar las 3 filas en schema_migrations. Correr las pgTAP 0038/0039/0040 + las previas (0021/0030/0034/0035/0036/0037) sin regresión. Probe deny-by-default con anon key sobre cruce_senal (esperar 42501) y confirmar anon sin execute en el RPC. Confirmar el cron `cruces-materializar` registrado.</action>
  <verify>
    <automated>psql "$SUPABASE_DB_URL" -f supabase/tests/0039_cruce_senal.test.sql (debe terminar sin fallos pgTAP); psql "$SUPABASE_DB_URL" -f supabase/tests/0040_cruces_rpc.test.sql (sin fallos)</automated>
  </verify>
  <acceptance_criteria>0038/0039/0040 aplicadas vía psql --db-url; 3 filas en schema_migrations; pgTAP 0038/0039/0040 verdes contra el DB aplicado + suites previas sin regresión; probe anon sobre cruce_senal = 42501; anon sin execute en el RPC; cron `cruces-materializar` registrado (count=1).</acceptance_criteria>
  <resume-signal>"pgTAP verde" (con conteo de asserts por suite y confirmación del probe anon + cron)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [LIVE] Clasificar contrapartes de lobby + materializar → CRUCE-03 (≥5 parlamentarios)</name>
  <what-built>El clasificador de lobby (Plan 03, MiniMax + RUT-gate) y el materializador (Plan 01) están aplicados. CRUCE-03 es un paso LIVE de operador: el gate ≥5-parlamentarios necesita datos de lobby reales clasificados (no un seed pgTAP).</what-built>
  <read_first>
    - .planning/phases/36-.../36-03-SUMMARY.md (comando exacto del clasificar-lobby-cli: flags, --service-key, env MINIMAX_API_KEY)
    - 36-RESEARCH.md §"Pitfall 2" (ordenar: clasificar sector_id ANTES de materializar) y §"Pitfall 1" (señal lobby-pura)
  </read_first>
  <how-to-verify>
    1. Correr `clasificar-lobby-cli` LIVE con `--service-key` real (MINIMAX_API_KEY presente) para poblar `lobby_contraparte.sector_id` sobre las contrapartes de audiencias confirmadas. (Sin service key degrada a dry-run — no cuenta.)
    2. Confirmar cobertura: `select count(*) from lobby_contraparte where sector_id is not null` > 0.
    3. Materializar: `psql "$SUPABASE_DB_URL" -c "select cruces.materializar_cruces();"`.
    4. Verificar CRUCE-03: `select count(distinct parlamentario_id) from cruce_senal` ≥ 5 (y ≥1 fila por esos parlamentarios). Loguear `select sector_id, count(*) from cruce_senal group by 1;` (un grafo vacío/engañoso se caza aquí, no en producción — MEMORY/lección 18-01).
    5. Confirmar que la evidencia es lobby-pura: cada item de `evidencia->'items'` referencia audiencias (tipo 'reunion'), CERO items de aporte; cada item lleva `enlace_fuente` (FND-08).
  </how-to-verify>
  <action>Correr `clasificar-lobby-cli` LIVE con `--service-key` real (MINIMAX_API_KEY presente) para poblar `lobby_contraparte.sector_id` sobre las contrapartes de audiencias confirmadas (sin service key degrada a dry-run, no cuenta). Confirmar cobertura `sector_id is not null` > 0. Materializar con `select cruces.materializar_cruces();`. Verificar CRUCE-03: `count(distinct parlamentario_id)` en cruce_senal >= 5; loguear el desglose por sector. Confirmar evidencia lobby-pura (tipo reunion, cero aporte, enlace_fuente por item). El RPC sigue SIN grant a anon (Phase 39 lo enciende, no este plan).</action>
  <verify>
    <automated>psql "$SUPABASE_DB_URL" -t -c "select count(distinct parlamentario_id) from cruce_senal;" (debe ser >= 5)</automated>
  </verify>
  <acceptance_criteria>`lobby_contraparte.sector_id` poblado (>0 filas) por la corrida LIVE; `materializar_cruces()` ejecutado; `cruce_senal` tiene ≥1 fila para ≥5 parlamentarios distintos (CRUCE-03); evidencia lobby-pura con `enlace_fuente` por item; señales de voto/aporte ausentes (OFF). RPC sigue SIN grant a anon (deny-by-default hasta Phase 39).</acceptance_criteria>
  <resume-signal>"cruces materializados" (con el conteo de parlamentarios distintos y el desglose por sector)</resume-signal>
</task>

</tasks>

<verification>
- Migraciones aplicadas por psql --db-url; pgTAP verde contra el DB aplicado; sin regresión.
- Probe anon: cruce_senal 42501 + RPC sin execute.
- CRUCE-03: ≥5 parlamentarios con señal lobby-pura tras la corrida LIVE + materialize.
</verification>

<success_criteria>
- 0038/0039/0040 en PROD + schema_migrations + pgTAP verde.
- cruce_senal poblada (≥5 parlamentarios) con evidencia lobby-pura trazable.
- CRUCE-01/02/03 satisfechos end-to-end; deny-by-default intacto (cero grant a anon).
</success_criteria>

<output>
Create `.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-04-SUMMARY.md` when done. Registrar conteos de asserts pgTAP por suite, cobertura de sector_id, y parlamentarios distintos en cruce_senal.
</output>
