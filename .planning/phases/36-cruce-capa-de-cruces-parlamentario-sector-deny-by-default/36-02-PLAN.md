---
phase: 36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cruces/package.json
  - packages/cruces/tsconfig.json
  - packages/cruces/vitest.config.ts
  - packages/cruces/src/index.ts
  - packages/cruces/src/sector.ts
  - packages/cruces/src/model.ts
  - packages/cruces/src/prompt.ts
  - packages/cruces/src/prompt-lobby.ts
  - packages/cruces/src/clasificar.ts
  - packages/cruces/src/clasificar.test.ts
  - packages/cruces/src/mock-provider.ts
autonomous: true
requirements: [CRUCE-02]
user_setup: []

must_haves:
  truths:
    - "Existe el paquete workspace @obs/cruces (descubierto por packages/* glob)"
    - "sector.ts exporta SECTOR_CODIGOS (tupla const) y SECTOR_CATALOGO (codigo+etiqueta), fuente única de la taxonomía"
    - "ClasificacionSectorSchema (zod) valida sector_codigo contra el enum cerrado, nullable (abstención first-class)"
    - "clasificarContraparte llama assertNoRutInLlmInput PRIMERO, luego assertSensitivityAllowed, antes de complete()"
    - "Un RUT en el prompt de contraparte hace fallar el test (gate test-enforced)"
    - "clasificarFicha rutea a DeepSeek (bulk/public); clasificarContraparte a MiniMax (critical/personal)"
  artifacts:
    - path: "packages/cruces/package.json"
      provides: "Paquete @obs/cruces"
      contains: "@obs/cruces"
    - path: "packages/cruces/src/sector.ts"
      provides: "SECTOR_CODIGOS const + catálogo (fuente única de la taxonomía)"
      exports: ["SECTOR_CODIGOS", "SECTOR_CATALOGO"]
    - path: "packages/cruces/src/model.ts"
      provides: "ClasificacionSectorSchema zod (enum cerrado + nullable)"
      exports: ["ClasificacionSectorSchema", "ClasificacionSector"]
    - path: "packages/cruces/src/clasificar.ts"
      provides: "clasificarFicha (DeepSeek) + clasificarContraparte (MiniMax, RUT-gate first)"
      exports: ["clasificarFicha", "clasificarContraparte"]
    - path: "packages/cruces/src/clasificar.test.ts"
      provides: "RUT-gate test + routing test"
      contains: "assertNoRutInLlmInput"
  key_links:
    - from: "packages/cruces/src/clasificar.ts"
      to: "@obs/llm assertNoRutInLlmInput / assertSensitivityAllowed"
      via: "import gate antes de provider.complete"
      pattern: "assertNoRutInLlmInput"
    - from: "packages/cruces/src/model.ts"
      to: "packages/cruces/src/sector.ts"
      via: "z.enum(SECTOR_CODIGOS)"
      pattern: "SECTOR_CODIGOS"
---

<objective>
Crear el paquete workspace `@obs/cruces` con la taxonomía de sectores como fuente única (sector.ts), el schema zod cerrado con abstención (model.ts), los prompts split (proyecto público / contraparte sensible) y el servicio clasificador `clasificar.ts` cuyo orden de gates de PII (assertNoRutInLlmInput PRIMERO) es load-bearing. Incluye los tests Wave 0 del gate de RUT y del routing.

Purpose: Materializar el pipeline de clasificación de sector SEPARADO de la extracción literal (SEM-02), con los gates de PII reusados verbatim de @obs/llm. Es el cimiento de los CLIs y el golden de Plan 03.
Output: Paquete @obs/cruces con núcleo de clasificación tipado + tests de seguridad (RUT-gate, routing). Sin CLIs ni golden-gate todavía (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-CONTEXT.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-RESEARCH.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-PATTERNS.md
@packages/llm/src/data-routing.ts
@packages/adjudication/src/pipeline-entidad.ts
@packages/fichas/src/model.ts
@packages/fichas/package.json

<interfaces>
<!-- Gates a reusar (NO re-implementar) — packages/llm/src/data-routing.ts -->
assertNoRutInLlmInput(prompt: string): void        // fail-closed, error NUNCA filtra el RUT; >=10 tests
assertSensitivityAllowed({ sensitivity: Sensitivity }, provider): void   // Sensitivity = "public" | "personal" (binario)
<!-- Proveedores (packages/llm) — router byCriticality: critical->minimax, bulk->deepseek -->
provider.complete(req, ZodSchema)   // aplica el gate zod + repair loop internamente
<!-- Orden de gates (pipeline-entidad.ts:24-25): assertNoRutInLlmInput PRIMERO, luego assertSensitivityAllowed -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto de contraparte → LLM | Nombre de contraparte (Ley 21.719) jamás debe llevar un RUT al prompt; sensibilidad jamás "public" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Information Disclosure | clasificarContraparte prompt | mitigate | `assertNoRutInLlmInput(prompt)` PRIMERO (fail-closed); test falla si un RUT se cuela |
| T-36-07 | Information Disclosure | mislabel sensitivity | mitigate | contraparte usa `sensitivity:"personal"` NUNCA `"public"` (FND-06); test asierta el label |
| T-36-08 | Tampering | LLM output fuera de taxonomía | mitigate | `provider.complete(req, ClasificacionSectorSchema)` zod gate cerrado contra SECTOR_CODIGOS |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold del paquete @obs/cruces + sector.ts (taxonomía) + model.ts (zod)</name>
  <files>packages/cruces/package.json, packages/cruces/tsconfig.json, packages/cruces/vitest.config.ts, packages/cruces/src/index.ts, packages/cruces/src/sector.ts, packages/cruces/src/model.ts</files>
  <read_first>
    - packages/fichas/package.json (espejar verbatim: name, type:module, main/types, scripts test/typecheck, deps)
    - packages/fichas/src/model.ts:39-43 (rol de FichaSchema: contrato zod validado por provider.complete) + :7-9 (doc-comment de honest-degradation)
    - 36-PATTERNS.md §"packages/cruces/package.json" y §"model.ts" (qué deps mantener/dropear)
    - SUMMARY de 36-01 (lista de códigos+etiquetas confirmada por el operador en Task 1 de Plan 01)
    - pnpm-workspace.yaml (confirma glob packages/* — no requiere edición)
  </read_first>
  <behavior>
    - SECTOR_CODIGOS es una tupla `as const` cuyos valores = exactamente los códigos confirmados en SUMMARY 36-01 (cero 'otros')
    - ClasificacionSectorSchema acepta `{ sector_codigo: '<un-codigo-valido>' }` y `{ sector_codigo: null }`
    - ClasificacionSectorSchema RECHAZA `{ sector_codigo: 'codigo_inexistente' }`
  </behavior>
  <action>Crear packages/cruces espejando packages/fichas: package.json con name `@obs/cruces`, `type:module`, main/types apuntando a src/index.ts, scripts `test: vitest run` + `typecheck: tsc -b`, deps `@obs/core`/`@obs/llm`/`@supabase/supabase-js`/`zod`, devDeps `tsx`/`vitest`/`@types/node` (DROP `@obs/ingest`/`@obs/tramitacion`/`unpdf`). tsconfig.json + vitest.config.ts espejando los de fichas. sector.ts: exportar `SECTOR_CATALOGO` como array const de objetos `{ codigo, etiqueta }` con los valores CONFIRMADOS en SUMMARY 36-01, y `SECTOR_CODIGOS` como tupla `as const` de los códigos (para z.enum) — fuente ÚNICA de la taxonomía (espeja byte-a-byte el seed de 0038; cero 'otros' — D-05). model.ts: `ClasificacionSectorSchema = z.object({ sector_codigo: z.enum(SECTOR_CODIGOS).nullable() })` + tipo inferido `ClasificacionSector`; doc-comment explicando que null = no-confiado (abstención first-class, D-05/D-08), estilo model.ts:7-9 de fichas. index.ts re-exporta sector/model. Confirmar que `pnpm install` descubre el paquete.</action>
  <verify>
    <automated>grep -q '"@obs/cruces"' packages/cruces/package.json && grep -q "SECTOR_CODIGOS" packages/cruces/src/sector.ts && grep -q "z.enum(SECTOR_CODIGOS).nullable()" packages/cruces/src/model.ts && pnpm --filter @obs/cruces typecheck</automated>
  </verify>
  <acceptance_criteria>packages/cruces/package.json contiene `"@obs/cruces"`; sector.ts exporta SECTOR_CODIGOS (tupla = códigos de SUMMARY 36-01, cero 'otros'); model.ts usa `z.enum(SECTOR_CODIGOS).nullable()`; `pnpm --filter @obs/cruces typecheck` pasa.</acceptance_criteria>
  <done>Paquete descubierto por el workspace, taxonomía+schema escritos, typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prompts split + clasificar.ts (gate de PII first) + clasificar.test.ts</name>
  <files>packages/cruces/src/prompt.ts, packages/cruces/src/prompt-lobby.ts, packages/cruces/src/clasificar.ts, packages/cruces/src/mock-provider.ts, packages/cruces/src/clasificar.test.ts</files>
  <read_first>
    - packages/adjudication/src/pipeline-entidad.ts:14-16,24-25 (orden de gates Stage-2: assertNoRutInLlmInput PRIMERO, luego assertSensitivityAllowed, luego provider.complete)
    - packages/llm/src/data-routing.ts:52-72 (firmas de los gates; Sensitivity binario en :67)
    - packages/fichas/src/prompt.ts (estilo del system prompt + construirPrompt) — espejar SHAPE, no el contenido literal de extracción
    - packages/adjudication/src/prompt-entidad.ts (estilo del prompt sensible de contraparte)
    - packages/cruces/src/model.ts + sector.ts (de Task 1)
    - 36-CONTEXT.md D-12 (routing split) y D-13 (zod gate, RUT nunca al LLM)
  </read_first>
  <behavior>
    - clasificarContraparte con un prompt que contenga un RUT lanza ANTES de llamar al provider (cero llamadas LLM cuando hay RUT)
    - clasificarFicha usa `sensitivity:"public"` + `criticality:"bulk"`; clasificarContraparte usa `sensitivity:"personal"` + `criticality:"critical"`
    - El mock-provider keyed por case id devuelve un ClasificacionSector fijo (CI sin red)
    - Una salida del LLM fuera de SECTOR_CODIGOS es rechazada por el zod gate (provider.complete)
  </behavior>
  <action>prompt.ts: `SYSTEM_CLASIFICACION_FICHA` + `construirPromptFicha(idea_matriz?, titulo?, materia?)` que pide elegir UN sector de SECTOR_CATALOGO o null (abstención) — texto factual, sin vocabulario causal. prompt-lobby.ts: `SYSTEM_CLASIFICACION_CONTRAPARTE` + `construirPromptContraparte(nombre, materia?)` para el nombre crudo de contraparte. clasificar.ts: `clasificarFicha(input, provider)` (público: arma prompt, `provider.complete(req con criticality:"bulk", sensitivity:"public"}, ClasificacionSectorSchema)` → DeepSeek por router); `clasificarContraparte(input, provider)` (sensible: arma prompt, luego `assertNoRutInLlmInput(system + "\n" + user)` PRIMERO, luego `assertSensitivityAllowed({sensitivity:"personal"}, provider)`, luego `provider.complete(req con criticality:"critical", sensitivity:"personal"}, ClasificacionSectorSchema)` → MiniMax). Importar los gates de `@obs/llm` (NO re-implementar). mock-provider.ts: provider keyed por case id que retorna un ClasificacionSector fijo, espejando el mock de @obs/fichas. clasificar.test.ts: (a) RUT-gate — `clasificarContraparte` con un nombre que incluye un RUT válido lanza y el provider mock NO se invoca (spy con 0 llamadas); (b) routing/sensitivity — asertar que clasificarFicha pasa `sensitivity:"public"` y clasificarContraparte `sensitivity:"personal"` (inspeccionar el req entregado al mock); (c) abstención — el mock retorna `{sector_codigo:null}` y clasificar lo propaga sin error.</action>
  <verify>
    <automated>pnpm --filter @obs/cruces test clasificar</automated>
  </verify>
  <acceptance_criteria>clasificar.ts importa `assertNoRutInLlmInput` y `assertSensitivityAllowed` de `@obs/llm` y los invoca en ese orden ANTES de `provider.complete` en la ruta de contraparte. `pnpm --filter @obs/cruces test clasificar` pasa: el test de RUT confirma cero llamadas LLM cuando hay RUT; el test de routing confirma `sensitivity:"public"` (ficha) vs `sensitivity:"personal"` (contraparte). Cero vocabulario causal en los system prompts.</acceptance_criteria>
  <done>Clasificador split tipado con gates en orden; tests de seguridad verdes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/cruces typecheck` y `pnpm --filter @obs/cruces test` verdes.
- SECTOR_CODIGOS = códigos confirmados por el operador (SUMMARY 36-01), sin 'otros'.
- Gate de RUT test-enforced; sensitivity correcta por ruta.
</verification>

<success_criteria>
- Paquete @obs/cruces escrito y descubierto por el workspace.
- CRUCE-02 (pipeline separado, sensibilidad correcta, RUT nunca al LLM) cubierto a nivel de núcleo + tests.
</success_criteria>

<output>
Create `.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-02-SUMMARY.md` when done. Documentar la firma de clasificarFicha/clasificarContraparte y la forma del mock-provider (consumidos por los CLIs y el golden de Plan 03).
</output>
