---
phase: 36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0038_sector.sql
  - supabase/migrations/0039_cruce_senal.sql
  - supabase/migrations/0040_cruces_rpc.sql
  - supabase/tests/0038_sector.test.sql
  - supabase/tests/0039_cruce_senal.test.sql
  - supabase/tests/0040_cruces_rpc.test.sql
autonomous: false
requirements: [CRUCE-01, CRUCE-03]
user_setup: []

must_haves:
  truths:
    - "El catálogo sector es public-read (anon SELECT devuelve filas)"
    - "proyecto_ficha, lobby_contraparte y donante tienen columna sector_id (FK a sector, nullable)"
    - "cruce_senal es deny-by-default (RLS on + cero policies + revoke all from anon, authenticated)"
    - "materializar_cruces() es security definer set search_path='' y hace full rebuild transaccional"
    - "El cuerpo de materializar_cruces() no referencia partido ni rut"
    - "cruces_de_parlamentario NO tiene grant execute a anon"
    - "cruces_de_parlamentario nunca proyecta rut/partido/email/donante_id"
  artifacts:
    - path: "supabase/migrations/0038_sector.sql"
      provides: "Catálogo sector public-read + columnas sector_id en 3 tablas"
      contains: "create table sector"
    - path: "supabase/migrations/0039_cruce_senal.sql"
      provides: "Tabla cruce_senal deny-by-default + materializar_cruces() + cron"
      contains: "revoke all on cruce_senal from anon, authenticated"
    - path: "supabase/migrations/0040_cruces_rpc.sql"
      provides: "RPC cruces_de_parlamentario sin grant a anon"
      contains: "revoke execute on function"
    - path: "supabase/tests/0039_cruce_senal.test.sql"
      provides: "pgTAP deny-by-default + body no-PII + seed ≥5 parlamentarios"
      contains: "materializar_cruces"
    - path: "supabase/tests/0040_cruces_rpc.test.sql"
      provides: "pgTAP no-anon-execute + projection exclusion"
      contains: "has_function_privilege"
  key_links:
    - from: "supabase/migrations/0039_cruce_senal.sql"
      to: "lobby_audiencia + lobby_contraparte.sector_id"
      via: "join on identificador en materializar_cruces()"
      pattern: "join public.lobby_contraparte"
    - from: "supabase/migrations/0040_cruces_rpc.sql"
      to: "cruce_senal"
      via: "named-column projection (no select *)"
      pattern: "cruce_senal"
---

<objective>
Escribir las tres migraciones DDL de la capa de cruces (0038 sector / 0039 cruce_senal+materializador / 0040 RPC) y sus tres suites pgTAP, espejando verbatim los análogos aplicados (0030_net.sql, 0021_lobby.sql, 0034_entidad_tercero.sql). Todo deny-by-default, cero exposición pública.

Purpose: Establecer el modelo de datos derivado de cruces (catálogo de sectores, señal materializada, canal de lectura) como cimiento sobre el cual el clasificador (Plan 02/03) etiqueta sector_id y la corrida LIVE (Plan 04) materializa la señal MVP.
Output: 6 archivos SQL (3 migraciones + 3 pgTAP) NO aplicados aún (la aplicación al remoto vive en Plan 04, BLOCKING).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-CONTEXT.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-RESEARCH.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-PATTERNS.md
@.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-VALIDATION.md
@supabase/migrations/0030_net.sql
@supabase/migrations/0021_lobby.sql
@supabase/migrations/0034_entidad_tercero.sql
@supabase/tests/0030_net.test.sql

<interfaces>
<!-- Columnas reales de las tablas fuente que materializar_cruces() lee (verificadas en 0021_lobby.sql). -->
lobby_audiencia: identificador text PK, parlamentario_id text FK->parlamentario(id), mencion_sujeto text,
                 estado_vinculo text ('confirmado'|'no_confirmado'|null), fecha timestamptz, fecha_raw text,
                 enlace_detalle text, origen text NOT NULL, fecha_captura timestamptz, enlace text NOT NULL
lobby_contraparte: id, identificador text FK->lobby_audiencia(identificador), nombre text NOT NULL (nombre crudo),
                   rol text, representado_text text, origen text, enlace text  (sector_id se AÑADE en 0038)
parlamentario: id text PK  (NUNCA proyectar rut/partido/email)

<!-- Análogo del materializador y cron (0030_net.sql:105-178) y del RPC (0030_net.sql:186-254). -->
grafo.materializar_aristas(): security definer set search_path='' ; join lobby_audiencia⨝lobby_contraparte on identificador ;
  where estado_vinculo='confirmado' and parlamentario_id is not null ; on-conflict-do-nothing (← DEPARTURE: 0039 usa delete-all)
cron block 0030_net.sql:149-178: guard de versión pg_cron + cron.schedule('net-materializar-aristas','17 3 * * *', ...) + assertion post-migración cron.job
RPC subgrafo_red (0030_net.sql:186-254): security definer set search_path='' ; jsonb_build_object con columnas nombradas (NO select *) ; revoke execute from public (KEEP) ; grant execute to anon (← DROP en 0040)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated → Postgres (RLS/grants) | Cliente no-confiable nunca debe leer cruce_senal ni ejecutar el RPC antes del sign-off legal (Phase 39) |
| cron (service-definer) → cruce_senal | El materializador corre como owner; debe leer solo tablas de lobby, jamás partido/rut |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | cruce_senal table | mitigate | RLS on + cero policies + `revoke all on cruce_senal from anon, authenticated`; pgTAP `throws_ok 42501` para anon |
| T-36-02 | Information Disclosure | cruces_de_parlamentario RPC | mitigate | DROP `grant execute to anon` (KEEP revoke); pgTAP asserts `not has_function_privilege('anon', ..., 'execute')` |
| T-36-03 | Information Disclosure | RPC over-projection (select *) | mitigate | jsonb_build_object con columnas nombradas; pgTAP body regex `!~* '\y(partido|rut|email|donante_id)\y'` |
| T-36-04 | Information Disclosure | materializar_cruces body | mitigate | Cuerpo lee solo lobby_audiencia+lobby_contraparte; pgTAP body regex `!~* '\y(partido|rut)\y'` |
| T-36-05 | Elevation of Privilege | premature public exposure | mitigate | Migraciones NO aplicadas aquí (Plan 04); RPC sin grant; gate de presentación diferido a Phase 37/39 |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirmar catálogo de sectores (D-03) y token de señal MVP (Open Question 1)</name>
  <decision>Dos decisiones de operador que LOCKEAN el seed y el CHECK antes de escribir las migraciones: (a) la lista concreta de ~13 macro-sectores; (b) el token tipo_senal de la señal MVP.</decision>
  <context>
  D-03 exige confirmación del operador de la lista de sectores ANTES de fijar el seed/CHECK (un código vivo NUNCA se renombra/borra — D-04; el golden de Plan 02 se construye contra esta taxonomía). RESEARCH Open Question 1 deja abierto el token de la señal MVP: el acceptance (CRUCE-03) solo es satisfacible con datos de lobby ya ingeridos (donante/SERVEL gated a Phase 40), así que la señal es lobby-pura.
  </context>
  <read_first>
    - .planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-RESEARCH.md §"Code Examples" (seed propuesto ~13 sectores, [ASSUMED]) y §"Open Question 1"
    - .planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-CONTEXT.md D-01..D-05 (taxonomía) y D-09 (evidencia lobby-pura)
    - .planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-PATTERNS.md §"Sector catalog seed (PROPOSAL)"
  </read_first>
  <options>
    <option id="catalog-as-proposed">
      <name>Sectores: aceptar los 13 propuestos por RESEARCH</name>
      <pros>Anclados en comisiones permanentes del Congreso; legibles para ciudadano/prensa; sin catch-all "Otros" (D-05)</pros>
      <cons>El operador puede querer ajustar etiquetas/códigos antes de lockear</cons>
    </option>
    <option id="catalog-adjusted">
      <name>Sectores: lista ajustada por el operador</name>
      <pros>Taxonomía afinada a la realidad del Congreso según criterio del operador</pros>
      <cons>Requiere que el operador entregue la lista exacta (códigos estables + etiquetas)</cons>
    </option>
    <option id="signal-lobby-sector">
      <name>Token señal MVP: 'lobby_sector' (recomendado por RESEARCH)</name>
      <pros>Semántica honesta lobby-pura ("N reuniones con gestores del sector X"); reserva 'lobby_sector_aporte' para la fusión lobby+aporte de Phase 40</pros>
      <cons>Difiere del token literal del acceptance CRUCE-03</cons>
    </option>
    <option id="signal-lobby-sector-aporte">
      <name>Token señal MVP: 'lobby_sector_aporte' (token literal del acceptance, evidencia lobby-only)</name>
      <pros>Coincide con el texto literal de CRUCE-03</pros>
      <cons>El nombre insinúa aporte que no existe hasta Phase 40 (evidencia lleva cero items de aporte)</cons>
    </option>
  </options>
  <action>[ASSUMED] Presentar al operador el seed de ~13 sectores (de RESEARCH §Code Examples) y el token de señal MVP. Registrar la lista de códigos confirmada (códigos = clave estable, jamás renombrar — D-04) y el token confirmado. El token confirmado se usa LITERAL en el CHECK `tipo_senal in ('TOKEN')` de 0039 y en el INSERT del materializador; los códigos confirmados se usan en el INSERT del seed de 0038 Y como fuente única de SECTOR_CODIGOS en Plan 02. NO escribir 0038/0039 hasta resolver este checkpoint.</action>
  <verify>
    <automated>MISSING — checkpoint de operador; la verificación es que las Tasks 2-4 usen los valores confirmados (greps en sus verify)</automated>
  </verify>
  <acceptance_criteria>El operador entregó (a) la lista exacta de códigos+etiquetas de sector y (b) el token tipo_senal. Ambos quedan registrados en el SUMMARY del plan. Las migraciones posteriores referencian estos valores verbatim.</acceptance_criteria>
  <resume-signal>Confirmar: lista de sectores (códigos+etiquetas) + token de señal (lobby_sector | lobby_sector_aporte)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migración 0038 — catálogo sector public-read + columnas sector_id</name>
  <files>supabase/migrations/0038_sector.sql, supabase/tests/0038_sector.test.sql</files>
  <read_first>
    - supabase/migrations/0021_lobby.sql:52-56 (patrón public-read: enable RLS + create policy for select to anon using(true) + grant select to anon)
    - supabase/migrations/0030_net.sql:1-20 (estilo de header: qué espeja, apply por psql --db-url NUNCA db push, pgTAP única prueba)
    - supabase/migrations/0036_entidad_fk.sql (estilo de ALTER aditivo + FK)
    - supabase/tests/0030_net.test.sql (estructura pgTAP: begin; select plan(N); ...; finish(); rollback;)
    - SUMMARY de Task 1 (lista de códigos+etiquetas confirmada)
  </read_first>
  <action>Crear 0038_sector.sql con header al estilo 0030 (qué espeja + apply path psql --db-url + pgTAP única prueba). Half A: `create table sector (codigo text primary key, etiqueta text not null)` (codigo estable per D-04, nunca renombrar), `insert into sector (codigo, etiqueta) values ...` con los códigos+etiquetas CONFIRMADOS en Task 1 (sin catch-all 'otros' — D-05); luego public-read espejando 0021_lobby.sql:52-56 (`alter table sector enable row level security`; `create policy sector_public_read on sector for select to anon using (true)`; `grant select on sector to anon`). Half B: tres ALTER aditivos `add column sector_id text references sector(codigo)` sobre proyecto_ficha, lobby_contraparte, donante (NULL = honest no-match, D-05). Crear 0038_sector.test.sql espejando 0030_net.test.sql: `has_table('sector')`, conteo de filas de sector > 0 leído como anon (confirma public-read), `has_column` para sector_id en las 3 tablas, FK de sector_id apunta a sector(codigo). Cero referencia a 'otros'.</action>
  <verify>
    <automated>grep -q "for select to anon using (true)" supabase/migrations/0038_sector.sql && [ "$(grep -c 'add column sector_id text references sector(codigo)' supabase/migrations/0038_sector.sql)" = 3 ]</automated>
  </verify>
  <acceptance_criteria>0038_sector.sql contiene `create table sector`, una política public-read + `grant select on sector to anon`, y exactamente 3 `add column sector_id text references sector(codigo)`. El seed contiene SOLO los códigos confirmados en Task 1 (cero 'otros'). 0038_sector.test.sql afirma sector public-read + las 3 columnas sector_id existen. NO se aplica al remoto aquí (Plan 04).</acceptance_criteria>
  <done>0038 + su pgTAP escritos; greps verdes; cero catch-all 'otros'.</done>
</task>

<task type="auto">
  <name>Task 3: Migración 0039 — cruce_senal deny-by-default + materializar_cruces() + cron</name>
  <files>supabase/migrations/0039_cruce_senal.sql, supabase/tests/0039_cruce_senal.test.sql</files>
  <read_first>
    - supabase/migrations/0030_net.sql:66-90 (tabla arista: provenance inline NOT NULL, CHECK tipo allow-list, deny-by-default + revoke all)
    - supabase/migrations/0030_net.sql:105-178 (materializar_aristas security definer + join lobby on identificador + cron block VERBATIM + assertion post-migración)
    - supabase/tests/0030_net.test.sql:25-52 (seed→materialize→assert), :67-72 (zero policies), :100-105 (body regex comment-stripped), :131-140 (throws_ok anon 42501)
    - SUMMARY de Task 1 (token tipo_senal confirmado)
    - 36-CONTEXT.md D-09 (forma de evidencia jsonb), D-10 (nombre crudo), D-11 (full rebuild)
  </read_first>
  <action>Crear 0039_cruce_senal.sql. Tabla `cruce_senal` (forma D-09, NO espejo de arista): columnas `id bigint generated always as identity primary key`, `parlamentario_id text not null references parlamentario(id) on delete cascade`, `sector_id text not null references sector(codigo)`, `tipo_senal text not null check (tipo_senal in ('TOKEN'))` (TOKEN = el confirmado en Task 1), `conteo int not null`, `evidencia jsonb not null`, provenance inline `dataset text not null`, `origen text not null`, `fecha_captura timestamptz not null default now()`, `enlace text not null`, `unique (parlamentario_id, sector_id, tipo_senal)`. Deny-by-default espejando 0030_net.sql:88-90: `alter table cruce_senal enable row level security; revoke all on cruce_senal from anon, authenticated;` + índices en parlamentario_id y sector_id. Materializador con DEPARTURE D-11 (full rebuild en vez de on-conflict): `create schema if not exists cruces;` + `create or replace function cruces.materializar_cruces() returns void language plpgsql security definer set search_path = '' as $$ begin delete from public.cruce_senal; insert into public.cruce_senal (...) select a.parlamentario_id, c.sector_id, 'TOKEN', count(*), jsonb_build_object('conteo', count(*), 'items', jsonb_agg(jsonb_build_object('tipo','reunion','fecha',a.fecha,'contraparte_nombre_crudo',c.nombre,'audiencia_id',a.identificador,'enlace_fuente',a.enlace) order by a.fecha desc)), 'lobby', min(a.origen), now(), min(a.enlace) from public.lobby_audiencia a join public.lobby_contraparte c on c.identificador = a.identificador where a.estado_vinculo = 'confirmado' and a.parlamentario_id is not null and c.sector_id is not null group by a.parlamentario_id, c.sector_id; end; $$;`. El cuerpo NUNCA referencia partido ni rut (las tablas fuente no los traen). Cron block VERBATIM de 0030_net.sql:149-178 cambiando jobname→'cruces-materializar' y expr→'23 3 * * *' (offset evita colisión con '17 3' de net) + assertion post-migración sobre `cron.job where jobname='cruces-materializar'`. Crear 0039_cruce_senal.test.sql: seed parlamentario + lobby_audiencia (estado_vinculo='confirmado') + lobby_contraparte con sector_id directo para ≥5 parlamentarios distintos → `select cruces.materializar_cruces();` → assert `count(distinct parlamentario_id) >= 5` en cruce_senal; zero policies en cruce_senal; `throws_ok($$ select 1 from cruce_senal $$, '42501')` ejecutado como anon (revoke-all); `prosecdef = true` para materializar_cruces; body regex comment-stripped sobre nspname='cruces' proname='materializar_cruces' `!~* '\y(partido|rut)\y'` (mirror 0030_net.test.sql:100-105).</action>
  <verify>
    <automated>grep -q "revoke all on cruce_senal from anon, authenticated" supabase/migrations/0039_cruce_senal.sql && grep -q "delete from public.cruce_senal" supabase/migrations/0039_cruce_senal.sql && grep -q "cruces-materializar" supabase/migrations/0039_cruce_senal.sql && ! grep -niE "(^|[^a-z])(partido|rut)([^a-z]|$)" supabase/migrations/0039_cruce_senal.sql | grep -v '^[0-9]*:--'</automated>
  </verify>
  <acceptance_criteria>0039_cruce_senal.sql contiene `revoke all on cruce_senal from anon, authenticated`, `security definer set search_path = ''`, `delete from public.cruce_senal` (full rebuild D-11), el cron `cruces-materializar` con `'23 3 * * *'`, y CERO referencia a `partido`/`rut` fuera de comentarios. 0039_cruce_senal.test.sql afirma deny-by-default (throws_ok 42501 anon), prosecdef=true, body regex no-PII, y la ruta seeded ≥5 parlamentarios distintos. NO se aplica al remoto aquí (Plan 04).</acceptance_criteria>
  <done>0039 + su pgTAP escritos; greps verdes; full rebuild + cron offset 23 3; body sin partido/rut.</done>
</task>

<task type="auto">
  <name>Task 4: Migración 0040 — RPC cruces_de_parlamentario sin grant a anon</name>
  <files>supabase/migrations/0040_cruces_rpc.sql, supabase/tests/0040_cruces_rpc.test.sql</files>
  <read_first>
    - supabase/migrations/0030_net.sql:186-254 (subgrafo_red: security definer set search_path='', jsonb_build_object columnas nombradas, revoke execute from public [KEEP], grant execute to anon [DROP])
    - supabase/migrations/0021_lobby.sql:100-125 (lobby_de_parlamentario: disciplina de proyección named-column, returns table)
    - supabase/tests/0030_net.test.sql:92-105 (function-privilege assertion [INVERT] + body regex no-PII)
    - 36-PATTERNS.md §"0040_cruces_rpc.sql" (la ONE departure: keep revoke, drop grant)
  </read_first>
  <action>Crear 0040_cruces_rpc.sql. `create or replace function public.cruces_de_parlamentario(p_id text) returns table(...)` con `security definer set search_path = ''`, leyendo `public.cruce_senal` filtrada por `parlamentario_id = p_id`, proyectando SOLO columnas seguras: sector_id (codigo), etiqueta del sector (join a public.sector por codigo, dato público), tipo_senal, conteo, evidencia (jsonb con items[].enlace_fuente). NUNCA `select *`, NUNCA join a parlamentario/donante que filtre rut/partido/email/donante_id — usar jsonb_build_object/columnas nombradas (mirror subgrafo_red 0030_net.sql:224-247). La ONE departure: `revoke execute on function public.cruces_de_parlamentario(text) from public;` (KEEP) pero NO emitir `grant execute ... to anon` (deny-by-default hasta firma Phase 39). Crear 0040_cruces_rpc.test.sql: `has_function`, `prosecdef=true`, body regex comment-stripped sobre proname='cruces_de_parlamentario' `!~* '\y(partido|rut|email|donante_id)\y'`, e INVERTIR la assertion de privilegio de 0030_net.test.sql:92-94 → `ok(not has_function_privilege('anon', 'public.cruces_de_parlamentario(text)', 'execute'), 'anon NO execute (deny hasta Phase 39)')`.</action>
  <verify>
    <automated>grep -q "revoke execute on function public.cruces_de_parlamentario" supabase/migrations/0040_cruces_rpc.sql && ! grep -qE "grant execute on function public.cruces_de_parlamentario.* to anon" supabase/migrations/0040_cruces_rpc.sql && grep -q "not has_function_privilege('anon'" supabase/tests/0040_cruces_rpc.test.sql</automated>
  </verify>
  <acceptance_criteria>0040_cruces_rpc.sql contiene `revoke execute on function public.cruces_de_parlamentario` y NO contiene ningún `grant execute ... to anon`. La función es `security definer set search_path = ''` con proyección named-column. 0040_cruces_rpc.test.sql afirma `not has_function_privilege('anon', ...)` y el body regex no-PII (`partido|rut|email|donante_id`). NO se aplica al remoto aquí (Plan 04).</acceptance_criteria>
  <done>0040 + su pgTAP escritos; greps verdes; RPC sin grant a anon, proyección PII-safe.</done>
</task>

</tasks>

<verification>
- Tres migraciones (0038/0039/0040) y tres pgTAP escritos; numeración correcta (PROD actual = 0037).
- Token de señal MVP y lista de sectores = los confirmados por el operador en Task 1.
- cruce_senal deny-by-default; materializar_cruces full-rebuild sin partido/rut; RPC sin grant a anon.
- Aplicación al remoto + pgTAP verde = Plan 04 (BLOCKING). Este plan NO aplica DDL.
</verification>

<success_criteria>
- 6 archivos SQL escritos y commiteados (no aplicados).
- Greps de verify de Tasks 2-4 verdes.
- CRUCE-01 (estructura) y CRUCE-03 (forma de señal + RPC PII-safe) cubiertos a nivel de DDL/pgTAP.
</success_criteria>

<output>
Create `.planning/phases/36-cruce-capa-de-cruces-parlamentario-sector-deny-by-default/36-01-SUMMARY.md` when done. Registrar la lista de sectores y el token de señal confirmados (consumidos por Plan 02 y Plan 04).
</output>
