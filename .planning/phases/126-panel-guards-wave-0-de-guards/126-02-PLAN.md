---
phase: 126-panel-guards-wave-0-de-guards
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - app/lib/create-view-guard.test.ts
  - app/package.json
autonomous: true
requirements: [DEBT-02, PANEL-08]
must_haves:
  truths:
    - "Existe un guard que FALLA ante un `create view` en `public` sin `security_invoker`, demostrado con fixture inline (control positivo apareado)"
    - "El mismo fixture con `security_invoker = true` pasa verde (control negativo)"
    - "El escaneo real de `supabase/migrations/*.sql` reporta cero violaciones Y afirma haber leído ≥ 70 archivos (cero fuerte, no vacuo)"
    - "`pnpm --filter ./app guards` corre 11 archivos guard POR NOMBRE EXPLÍCITO y reporta `Test Files 11 passed (11)`"
    - "La suite completa de `app/` corre verde con ≥ 1.590 tests (no menos que el baseline)"
  artifacts:
    - path: "app/lib/create-view-guard.test.ts"
      provides: "Detector puro detectarViewsSinInvoker + escaneo real + mutation self-check"
      contains: "security_invoker"
    - path: "app/package.json"
      provides: "script `guards` con los 11 nombres explícitos"
      contains: "\"guards\""
  key_links:
    - from: "app/lib/create-view-guard.test.ts"
      to: "supabase/migrations/*.sql"
      via: "path.resolve(import.meta.dirname, '..', '..', 'supabase', 'migrations')"
      pattern: "MIGRATIONS_DIR"
    - from: "app/package.json script `guards`"
      to: "lib/create-view-guard.test.ts"
      via: "nombre explícito en la lista de vitest run"
      pattern: "create-view-guard"
---

<objective>
Crear el guard B-03 (`create view` en `public` sin `security_invoker`) con control positivo
apareado, y el runner de guards por nombre explícito que mata el glob-trap.

Purpose: DEBT-02 (criterio 3) y criterio 4 del ROADMAP §Phase 126. La aserción debe existir
ANTES de la primera vista del milestone (0080 la crea en Phase 127).
Output: `app/lib/create-view-guard.test.ts` (archivo nuevo) + script `guards` en
`app/package.json`. Cero dependencias nuevas, cero cambios de `vitest.config.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-panel-guards-wave-0-de-guards/126-CONTEXT.md
@.planning/phases/126-panel-guards-wave-0-de-guards/126-RESEARCH.md
@app/lib/vsim-antiflip-guard.test.ts
@app/lib/lockdown-guard.test.ts
</context>

<constraints_locked>
- PROHIBIDO crear cualquier archivo `.sql` dentro de `supabase/migrations/` (D-03): contaminaría el ledger y la numeración (0080 está reservada para Phase 127). Los fixtures son STRINGS inline en memoria.
- PROHIBIDO tocar `lockdown-guard.test.ts` (D-01): archivo dedicado nuevo.
- PROHIBIDO copiar `APP_ROOT = process.cwd()` de lockdown L43 (Pitfall 6, bug v8.1). Usar `import.meta.dirname` (patrón WR-06 de `anti-insinuacion-guard.test.ts` L64-67).
- PROHIBIDO glob en el script `guards` (Pitfall 2): `passWithNoTests: true` está en `app/vitest.config.ts` ⇒ un patrón que no matchea sale 0 SIN correr nada. Nombres explícitos siempre.
- Los guards NO se importan entre sí: `stripSqlComments` y helpers se REPRODUCEN module-local (precedente documentado en `vsim-antiflip-guard.test.ts`).
- Cero dependencias nuevas: `pnpm-lock.yaml` NO se toca; el único cambio en `app/package.json` es el script `guards` (nada en `dependencies`/`devDependencies`).
</constraints_locked>

<tasks>

<task type="auto">
  <name>Task 1: Guard B-03 — detector puro + escaneo real + control positivo apareado</name>
  <files>app/lib/create-view-guard.test.ts</files>
  <read_first>
    - `app/lib/vsim-antiflip-guard.test.ts` L1-40 (header/doc del molde) y los describes `(1)`..`(4)` (L240, L260, L271, L337) — la forma a copiar
    - `app/lib/lockdown-guard.test.ts` L33-70 (APP_ROOT/REPO_ROOT/MIGRATIONS_DIR/migrationNumber/stripSqlComments) y L322-375 (Bloque A: patrón readdirSync→readFileSync→regex→offenders→expect) y L368 (assert de conjunto no vacío)
    - `app/lib/anti-insinuacion-guard.test.ts` L64-67 (WR-06 `import.meta.dirname`)
    - `126-CONTEXT.md` D-01, D-02, D-03, D-04 + §Claude's Discretion
    - `126-RESEARCH.md` §Guard B-03 — evidencia y patrón (L261-290), §Pitfall 3, §Pitfall 6
  </read_first>
  <action>
Crear `app/lib/create-view-guard.test.ts`. Header JSDoc que declare: propósito (DEBT-02 /
B-03), que el cero de hoy es VACUO (0 views en 77 migraciones, verificado 2026-07-30) y que
el control positivo apareado es lo que lo vuelve cero fuerte, y la amenaza mitigada (una
view en `public` sin `security_invoker` corre con privilegios del OWNER = bypass de RLS del
caller).

Rutas (module-local, NUNCA `process.cwd()`):
`const APP_ROOT = path.resolve(import.meta.dirname, "..")`,
`const REPO_ROOT = path.resolve(APP_ROOT, "..")`,
`const MIGRATIONS_DIR = path.join(REPO_ROOT, "supabase", "migrations")`.

Helper module-local `stripSqlComments(sql: string): string` que elimine comentarios `--`
INCLUSO a mitad de línea y bloques `/* … */` (la versión de lockdown L64-70 solo elimina
líneas que EMPIEZAN por `--` — no la importes, es insuficiente).

Detector PURO exportable-a-nivel-de-módulo `detectarViewsSinInvoker(sql: string): string[]`
que devuelve la lista de nombres de view en violación:
  1. `stripSqlComments`
  2. split por `;` en sentencias
  3. por sentencia, matchear `/^\s*create\s+(or\s+replace\s+)?(materialized\s+)?view\s+(if\s+not\s+exists\s+)?(public\.)?["\w]+/i`
  4. schema: considerar SOLO no-calificado o `public.`-calificado (ignorar otros schemas)
  5. si es `materialized view` → violación SIEMPRE (las matviews no soportan `security_invoker`; allowlist inicial VACÍA, declarada como const vacía con comentario de que sumar una exige decisión explícita)
  6. si es view normal → verde SOLO si la MISMA sentencia matchea `/with\s*\(\s*[^)]*security_invoker\s*=\s*(true|on)/i`

Tests, en describes numerados al estilo del molde:
  `(1) escaneo real`: `readdirSync(MIGRATIONS_DIR).filter(f => f.endsWith(".sql"))`,
  primero `expect(archivos.length, "MIGRATIONS_DIR mal resuelto — el cero sería vacuo por
  ruta rota").toBeGreaterThanOrEqual(70)` (hoy 77; precedente L368/L623), luego por archivo
  `readFileSync` → `detectarViewsSinInvoker` → acumular `offenders` como
  `` `${filename} → ${viewName}` `` → `expect(offenders, "<mensaje accionable: añade `with
  (security_invoker = true)` a la view, o —si es matview— justifícala en el allowlist
  vacío>").toHaveLength(0)`.
  `(2) control positivo apareado` (D-03): fixtures STRING inline —
    - `create view public.v_x as select 1;` → detector reporta `1` violación
    - `create or replace view v_y as select 1;` (no calificado) → reporta
    - `create materialized view public.mv_z as select 1;` → reporta (matview siempre)
    - `create view public.v_x with (security_invoker = true) as select 1;` → `[]`
    - variante `security_invoker = on` → `[]`
    - `create view otro_schema.v_w as select 1;` → `[]` (fuera de `public`)
    - una view sin invoker COMENTADA (`-- create view public.v_c …` y dentro de `/* … */`) → `[]` (prueba del strip)
  `(3) mutation self-check`: afirmar explícitamente, con mensaje "el guard sería un no-op
  verde", que el detector aplicado al fixture sin `security_invoker` devuelve length > 0 —
  es decir que el mismo código que da 0 sobre el árbol real SÍ muerde.
  </action>
  <verify>
    <automated>cd app && npx vitest run lib/create-view-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx vitest run lib/create-view-guard.test.ts` sale 0 y reporta `Test Files 1 passed (1)` con ≥ 8 tests.
    - El archivo contiene `import.meta.dirname` y NO contiene `process.cwd()`.
    - `git status --porcelain supabase/migrations` está VACÍO (ningún fixture .sql creado; ledger intacto, 0080 sigue libre).
    - `ls supabase/migrations/*.sql | wc -l` devuelve 77 (igual que el baseline).
    - Control negativo del assert anti-vacuo (ejecutar y REVERTIR): apuntar temporalmente `MIGRATIONS_DIR` a un directorio inexistente/vacío → la corrida FALLA en el assert de `>= 70`; revertir → verde. Constancia en el SUMMARY.
    - El detector es una función pura sin I/O (no llama `readFileSync` dentro de sí misma).
  </acceptance_criteria>
  <done>El guard B-03 existe, el escaneo real de las 77 migraciones da cero FUERTE, y el control positivo apareado demuestra que la aserción mordería ante la primera view sin `security_invoker`.</done>
</task>

<task type="auto">
  <name>Task 2: Script `guards` por nombre explícito + verificación de fase</name>
  <files>app/package.json</files>
  <read_first>
    - `app/package.json` (bloque `scripts`: hoy `dev build start lint test typecheck preview deploy upload cf-build cf-typegen` — NO existe `guards`)
    - `app/vitest.config.ts` (`passWithNoTests: true` — razón mecánica del glob-trap)
    - `126-CONTEXT.md` D-13, D-14
    - `126-RESEARCH.md` §Inventario de guards de régimen (L222-242) y §Open Questions (L478-481)
  </read_first>
  <action>
Añadir a `app/package.json` el script `guards` con `vitest run` seguido de los ONCE nombres
EXPLÍCITOS (jamás un glob), en este orden:
`lib/anti-insinuacion-guard.test.ts`, `lib/lockdown-guard.test.ts`,
`lib/bento-guards.test.ts`, `lib/vsim-antiflip-guard.test.ts`,
`lib/money-antiflip-guard.test.ts`, `lib/notif-antiflip-guard.test.ts`,
`lib/env-example-guard.test.ts`, `lib/name-match-rut-guard.test.ts`,
`lib/bento-coherencia-guard.test.ts`, `components/co-votacion-red-guard.test.ts`,
`lib/create-view-guard.test.ts`.
No tocar `dependencies` ni `devDependencies`; no tocar `pnpm-lock.yaml`.

D-14 — resolver el "14+" del criterio 4 documentando el CONTEO REAL medido, jamás
asumiendo 14: **11 archivos guard en `app/` tras esta fase** (10 preexistentes + el nuevo)
y **17 en el monorepo** contando los 6 de `packages/`
(`packages/dinero/src/name-match-rut-guard.behavior.test.ts`,
`packages/dinero/src/reconciler-frozen-guard.test.ts`,
`packages/dinero/src/servel-frozen-guard.test.ts`,
`packages/llm/src/integ-scope-guard.test.ts`, `packages/llm/src/provider-guard.test.ts`,
`packages/llm/src/tiered-scope-guard.test.ts`), que CI ya corre en su propio workspace.
Escribir esa constancia en el SUMMARY (no en package.json).

Ejecutar la verificación de fase completa y registrar los números exactos.
  </action>
  <verify>
    <automated>cd app && pnpm guards && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app guards` sale 0 y reporta LITERALMENTE `Test Files  11 passed (11)`. Si reporta `(0)` o `no test files found`, es el glob-trap → FALLA.
    - El valor del script `guards` en `app/package.json` NO contiene ningún carácter `*`.
    - Los 11 nombres del script existen en disco (comprobar uno a uno con `ls`); ninguno de los 10 archivos guard listados por `ls app/lib/*guard*.test.ts app/components/*guard*.test.ts` queda fuera del script.
    - `pnpm --filter ./app test` sale 0 y reporta `Test Files 108 passed (108)` con conteo de tests ≥ 1590 (baseline medido 2026-07-30: 107 archivos / 1.590 tests). Un conteo MENOR = archivos que dejaron de correr → FALLA.
    - `git diff app/package.json` muestra únicamente la línea del script `guards` (cero cambios en dependencias).
    - El SUMMARY documenta el conteo real (11 en `app/`, 17 en el monorepo) y la discrepancia explícita con el "14+" del criterio 4.
  </acceptance_criteria>
  <done>Existe un runner de guards por nombre explícito inmune al glob-trap, la suite completa está verde en o por encima del baseline, y el conteo real de guards está documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller anon/authenticated → view en `public` | Una view sin `security_invoker` ejecuta con privilegios del OWNER, bypassando la RLS del caller |
| migración en el ledger → PROD | Lo que entra a `supabase/migrations/` acaba aplicado; el guard estático es el único control pre-merge |
| guard → CI | Un guard que no corre (glob-trap) es un control inexistente que reporta verde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-05 | Elevation of Privilege | `create view` en `public` sin `security_invoker` (`supabase/migrations/*.sql`) | mitigate | Guard estático B-03: detector `detectarViewsSinInvoker` sobre las 77 migraciones, `offenders` → `toHaveLength(0)` con mensaje accionable. Matviews = violación siempre (allowlist vacía) |
| T-126-06 | Repudiation (cero vacuo) | escaneo de `MIGRATIONS_DIR` | mitigate | `expect(archivos.length).toBeGreaterThanOrEqual(70)` antes de afirmar cero + control negativo apareado con MIGRATIONS_DIR roto |
| T-126-07 | Repudiation (guard no-op) | detector B-03 | mitigate | Control positivo apareado con fixtures STRING inline: sin `security_invoker` → reporta; con `= true`/`= on` → `[]` |
| T-126-08 | Tampering (contaminación del ledger) | `supabase/migrations/` | mitigate | Fixtures SOLO como strings en memoria (D-03); acceptance criteria exige `git status --porcelain supabase/migrations` vacío y 77 archivos .sql |
| T-126-09 | Repudiation (control que no corre) | runner `pnpm guards` | mitigate | Nombres explícitos, cero `*`; assert de `Test Files 11 passed (11)` (`passWithNoTests: true` haría salir 0 con un glob vacío) |
</threat_model>

<verification>
- `cd app && npx vitest run lib/create-view-guard.test.ts` → VERDE, ≥ 8 tests.
- `pnpm --filter ./app guards` → `Test Files 11 passed (11)`.
- `pnpm --filter ./app test` → VERDE, 108 archivos, ≥ 1.590 tests.
- `git status --porcelain supabase/migrations` → vacío; `ls supabase/migrations/*.sql | wc -l` → 77.
- Control negativo del assert anti-vacuo ejecutado y revertido, documentado en el SUMMARY.
</verification>

<success_criteria>
- Criterio 3 del ROADMAP: la aserción `create view` sin `security_invoker` existe y muerde, con control positivo apareado; el cero deja de ser vacuo ANTES de la primera vista del milestone.
- Criterio 4 del ROADMAP: suite `app/` completa + guards de régimen verdes por nombre explícito, jamás por glob, con el conteo real documentado.
- Cero migraciones creadas o modificadas; cero dependencias nuevas.
</success_criteria>

<output>
Create `.planning/phases/126-panel-guards-wave-0-de-guards/126-02-SUMMARY.md` when done.
Documentar: números exactos de `pnpm guards` y `pnpm test`, resultado del control negativo del assert anti-vacuo,
y el conteo real de guards (11 en `app/`, 17 en el monorepo) con la discrepancia frente al "14+" del criterio 4.
</output>
