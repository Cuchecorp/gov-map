---
phase: 126-panel-guards-wave-0-de-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [PANEL-08]
must_haves:
  truths:
    - "Los 7 archivos previstos del rediseño del panel están declarados en SUPERFICIES_PANEL antes de que exista su copy, y el guard sigue verde (loader tolera faltantes)"
    - "Un archivo real `app/components/panel-*.tsx` no declarado hace FALLAR el guard (anti-drift), y `panel-actualidad.test.tsx` NO lo hace fallar"
    - "Inyectar `señal`, `exprés` o `los más` en un fixture de superficie panel produce detección (el carril PANEL muerde)"
    - "Los 4 idioms aprobados están registrados en NEGACIONES_LOCKED y exportados como IDIOMS_APROBADOS"
    - "La extensión de NEGACIONES_LOCKED no abre huecos: ningún stem contiene término prohibido, y un término prohibido adyacente a un idiom sigue siendo reportado"
  artifacts:
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "SUPERFICIES_PANEL extendida, anti-drift, IDIOMS_APROBADOS, NEGACIONES_LOCKED extendida, mutation self-checks"
      contains: "IDIOMS_APROBADOS"
  key_links:
    - from: "SUPERFICIES_PANEL"
      to: "TODAS_LAS_SUPERFICIES (L829-846)"
      via: "spread ya existente — no se toca"
      pattern: "SUPERFICIES_PANEL"
    - from: "NEGACIONES_LOCKED"
      to: "detectarInsinuaciones (L861-876)"
      via: "resta de negaciones antes del match"
      pattern: "NEGACIONES_LOCKED"
---

<objective>
Extender el guard anti-insinuación existente para que el régimen del carril PANEL
muerda ANTES de que exista una sola línea de copy del rediseño (Phase 128).

Purpose: PANEL-08 criterios 1 y 2 del ROADMAP §Phase 126. Wave-0 LOCKED: vocabulario y
superficies antes del copy.
Output: `app/lib/anti-insinuacion-guard.test.ts` extendido con datos (2 arrays + 1 export)
y asserts (anti-drift, mutación del trío, no-hueco de idioms). CERO detector nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-panel-guards-wave-0-de-guards/126-CONTEXT.md
@.planning/phases/126-panel-guards-wave-0-de-guards/126-RESEARCH.md
@app/lib/anti-insinuacion-guard.test.ts
</context>

<constraints_locked>
- Fase SOLO de guards: cero copy, cero componentes, cero migraciones. El único archivo que queda MODIFICADO al final de este plan es `app/lib/anti-insinuacion-guard.test.ts`. EXCEPCIÓN AUTORIZADA (control positivo apareado de Task 1): el probe TRANSITORIO `app/components/panel-tile-zzz-probe.tsx` se crea, se observa el FAIL, y se BORRA en la misma task — el árbol final no lo contiene.
- DEDUPE (Pitfall 4): `components/panel-actualidad.tsx` YA está en SUPERFICIES_PANEL (L309) — NO re-declarar. `señal` (L725), `exprés` (L701), `los más` (L708) YA están en TERMINOS_PROHIBIDOS — NO re-agregar.
- `buildTermRegex` NO es accent-insensitive (Pitfall 5): tipear tildes exactas en fixtures y stems (`exprés`, `señal`, `según`, `Cámara`).
- NO tocar `TODAS_LAS_SUPERFICIES` (L829-846): PANEL ya está en la union (L839).
- NO usar `process.cwd()`: el archivo ya ancla en `import.meta.dirname` (L64-67, patrón WR-06). Reutilizar `APP_ROOT`.
- El guard LEE archivos con `readFileSync`; jamás importa componentes.
</constraints_locked>

<tasks>

<task type="auto">
  <name>Task 1: Alta preventiva de SUPERFICIES_PANEL + assert anti-drift</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - `app/lib/anti-insinuacion-guard.test.ts` L60-95 (APP_ROOT + stripTsComments), L287-311 (JSDoc + SUPERFICIES_PANEL), L829-846 (TODAS_LAS_SUPERFICIES), L882-930 (describe `(1)` y el bucle tolerante L905-917)
    - `126-CONTEXT.md` D-05, D-06, D-07, D-08
    - `126-RESEARCH.md` §Baseline anti-drift D-07 (L305-311) y §Common Pitfalls Pitfall 1
  </read_first>
  <action>
Extender el array `SUPERFICIES_PANEL` (L308-311) sumando las 7 rutas de D-06, en este
orden y con estas cadenas exactas: `components/panel-tile-sala.tsx`,
`components/panel-tile-comisiones.tsx`, `components/panel-tile-urgencias.tsx`,
`components/panel-tile-movimiento.tsx`, `components/panel-tile-votaciones.tsx`,
`components/panel-tile-ingresos.tsx`, `components/panel-item-proyecto.tsx`. Conservar
`components/panel-actualidad.tsx` sin duplicar. Reemplazar el comentario placeholder de
L310 por un comentario que cite `v13.0 (126, PANEL-08)`, el prefijo CONGELADO
`components/panel-*.tsx` de D-05 (regla para Phase 128), y la razón de que el alta sea
preventiva (el loader tolera faltantes vía try/catch continue, L905-917). Anotar además
la regla D-08: el helper de links internos de 128 vive en `lib/` y NO entra a este array
salvo que emita labels visibles.

Añadir un `it` NUEVO dentro del `describe (1)` de escaneo real, con id `(1f)` y nombre que
incluya `PANEL-08 anti-drift`. El assert: `readdirSync(path.join(APP_ROOT, "components"))`
filtrado por `/^panel-.+\.tsx$/` Y excluyendo `/\.test\.tsx?$/` (Pitfall 1 —
`panel-actualidad.test.tsx` existe HOY y sin la exclusión el guard nace rojo), mapeado a
`components/${f}`; los que no estén en `new Set(SUPERFICIES_PANEL)` van a `huerfanos`;
`expect(huerfanos, "<mensaje accionable: declarar el archivo en SUPERFICIES_PANEL en el
mismo commit que lo crea>").toHaveLength(0)`. Añadir en el mismo `it` un assert
anti-cero-vacuo (precedente L883-897 / L368): el listado filtrado de archivos REALES debe
tener `length >= 1` (hoy: `panel-actualidad.tsx`), para que un `APP_ROOT` mal resuelto no
dé verde escaneando cero.
  </action>
  <verify>
    <automated>cd app && npx vitest run lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/anti-insinuacion-guard.test.ts` sale 0 y reporta `Test Files 1 passed (1)` con conteo de tests ESTRICTAMENTE MAYOR a 42 (baseline RESEARCH L228).
    - `SUPERFICIES_PANEL` contiene exactamente 8 entradas: `components/panel-actualidad.tsx` + las 7 de D-06, sin duplicados.
    - El nuevo `it` referencia `PANEL-08 anti-drift` en su nombre y el filtro excluye `.test.tsx` (verificable leyendo el archivo: la regex de exclusión `\.test\.tsx?$` está presente).
    - Control positivo manual (ejecutar y REVERTIR): crear `app/components/panel-tile-zzz-probe.tsx` con contenido `export function P(){return null;}` → la corrida FALLA con `huerfanos` conteniendo `components/panel-tile-zzz-probe.tsx`; borrar el archivo → vuelve a verde. Dejar constancia del resultado en el SUMMARY. El árbol final NO contiene el probe (`ls app/components/panel*` devuelve solo `panel-actualidad.tsx` y `panel-actualidad.test.tsx`).
    - `git status --porcelain` no muestra ningún archivo modificado fuera de `app/lib/anti-insinuacion-guard.test.ts`.
  </acceptance_criteria>
  <done>SUPERFICIES_PANEL cubre los 7 archivos previstos del rediseño y todo `panel-*.tsx` real no declarado hace fallar el guard, sin falso positivo sobre el archivo de test existente.</done>
</task>

<task type="auto">
  <name>Task 2: IDIOMS_APROBADOS + NEGACIONES_LOCKED + self-checks de mutación y no-hueco</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - `app/lib/anti-insinuacion-guard.test.ts` L601-735 (TERMINOS_PROHIBIDOS, carril PANEL L678-708), L743-790 (NEGACIONES_LOCKED), L802-813 (WORD / buildTermRegex), L853-876 (detectarTerminos / detectarInsinuaciones), L1110-1455 (describe `(2)` mutación; it PANEL L1273-1299), L1456-fin (describe `(3)` sin falsos positivos)
    - `126-CONTEXT.md` D-09, D-10, D-11, D-12
    - `126-RESEARCH.md` §Verificación de los idioms D-09 vs TERMINOS_PROHIBIDOS (L292-303) y §Code Examples (L371-383)
  </read_first>
  <action>
(a) Declarar un export `IDIOMS_APROBADOS` (array de string, single-source para que Phase 128
los importe verbatim) con los 4 stems FIJOS EXACTOS de D-09/D-11, sin partes variables
(fechas/grados): `Citado el`, `vigente desde`, `En tabla de sala de la Cámara del`,
`según fuente al`. Ubicarlo inmediatamente antes de `NEGACIONES_LOCKED` (~L743). Comentario
obligatorio: `idiom aprobado v13.0 — no niega término prohibido; registrado por mandato
PANEL-08`.

(b) Extender `NEGACIONES_LOCKED` (L743-790) haciendo spread de `IDIOMS_APROBADOS` al final
del array (tras el literal de L789), con el mismo comentario de mandato. NO re-tipear los
stems: una sola fuente.

(c) Añadir dentro del `describe (2)` un `it` HERMANO del de L1273, nombrado con id
`PANEL (126)` y mencionando el trío del criterio 1 (D-12). Fixture STRING inline
representativo de superficie panel (JSX en template literal) que contenga `exprés`,
`señal` y `los más` con tildes exactas; assert
`expect(detectarInsinuaciones(fixture), "<mensaje: sin esto el carril PANEL sería un
no-op>").toEqual(expect.arrayContaining(["exprés", "señal", "los más"]))`.

(d) Añadir el self-check de no-hueco D-10, dos asserts:
  (i) un `it` que recorra `IDIOMS_APROBADOS` y afirme que NINGÚN stem contiene término de
  `TERMINOS_PROHIBIDOS` — implementado POR CÓDIGO usando `detectarTerminos(stem,
  TERMINOS_PROHIBIDOS)` (no grep, no lista hardcodeada), `expect(...).toHaveLength(0)` por
  stem, con mensaje que exija decisión explícita si un idiom futuro lo contuviera.
  (ii) un `it` de mutación: fixture STRING que contenga un idiom verbatim (p.ej. `En tabla
  de sala de la Cámara del 12 de agosto`) CON un término prohibido inyectado adyacente
  (p.ej. `señal`); assert de que `detectarInsinuaciones` SIGUE reportando ese término — la
  resta del stem no enmascara. Ubicar ambos en el `describe (2)` o `(3)` según encaje;
  mensaje de expect accionable.

Reutilizar `detectarInsinuaciones` / `detectarTerminos` / `buildTermRegex` existentes: NO
escribir detector nuevo.
  </action>
  <verify>
    <automated>cd app && npx vitest run lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/anti-insinuacion-guard.test.ts` sale 0, `Test Files 1 passed (1)`, conteo de tests ≥ 46 (42 baseline + ≥1 de Task 1 + ≥3 de Task 2).
    - El archivo contiene `export const IDIOMS_APROBADOS` con exactamente 4 elementos, y `NEGACIONES_LOCKED` los incorpora por spread (`...IDIOMS_APROBADOS`) — verificable leyendo el archivo; los stems NO aparecen re-tipeados como literales sueltos dentro de `NEGACIONES_LOCKED`.
    - Los 4 stems son byte-exactos con tilde: `Citado el`, `vigente desde`, `En tabla de sala de la Cámara del`, `según fuente al`.
    - Control positivo apareado del no-hueco (ejecutar y REVERTIR): añadir temporalmente `señal en tabla` a `IDIOMS_APROBADOS` → el `it` de D-10(i) FALLA; revertir → verde. Constancia en el SUMMARY.
    - Control apareado de D-10(ii) (ejecutar y REVERTIR): eliminar temporalmente el término prohibido inyectado del fixture → el `it` FALLA (el `arrayContaining` ya no lo encuentra); revertir → verde. Constancia en el SUMMARY.
    - Ningún término se agregó a `TERMINOS_PROHIBIDOS` (DEDUPE Pitfall 4), verificable POR COMANDO: medir ANTES de editar `awk '/^const TERMINOS_PROHIBIDOS/,/^\];/' app/lib/anti-insinuacion-guard.test.ts | grep -c '^\s*"'` y repetir DESPUÉS — ambos conteos IGUALES (documentar ambos números en el SUMMARY).
    - `git status --porcelain` no muestra archivos modificados fuera de `app/lib/anti-insinuacion-guard.test.ts`.
  </acceptance_criteria>
  <done>Los 4 idioms están registrados en NEGACIONES_LOCKED vía single-source exportada, el trío del criterio 1 demuestra que muerde, y la extensión probadamente no abre huecos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → copy publicado | Texto que llega a ciudadanía/prensa; insinuar intención o causalidad es el daño reputacional/legal que el linter previene |
| guard → CI | El guard es el control; si es un no-op verde, el control es ficticio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Repudiation (control ficticio) | carril PANEL de `anti-insinuacion-guard.test.ts` | mitigate | Mutation self-check D-12: `señal`/`exprés`/`los más` inyectados en fixture inline DEBEN ser reportados por `detectarInsinuaciones` |
| T-126-02 | Tampering (drift de superficie) | `SUPERFICIES_PANEL` vs `app/components/panel-*.tsx` | mitigate | Assert anti-drift D-07 con readdirSync; excluye `*.test.tsx?` para no nacer rojo |
| T-126-03 | Tampering (resta amplia enmascara prohibido) | `NEGACIONES_LOCKED` extendida | mitigate | D-10(i) ningún stem contiene término prohibido (por código); D-10(ii) término adyacente a idiom sigue reportado |
| T-126-04 | Repudiation (cero vacuo por ruta mal resuelta) | `APP_ROOT` / readdirSync del anti-drift | mitigate | Assert de conjunto no vacío (≥1 archivo real) + `import.meta.dirname` heredado (L64-67), nunca `process.cwd()` |
</threat_model>

<verification>
- `cd app && npx vitest run lib/anti-insinuacion-guard.test.ts` → VERDE, `Test Files 1 passed (1)`, tests ≥ 46.
- Los dos controles positivos apareados (probe `panel-tile-zzz-probe.tsx`; stem contaminado en IDIOMS_APROBADOS) ejecutados y REVERTIDOS, con resultado documentado en el SUMMARY.
- `git status --porcelain` limpio salvo `app/lib/anti-insinuacion-guard.test.ts`.
</verification>

<success_criteria>
- Criterio 1 del ROADMAP: 7 archivos previstos dados de alta antes del copy + carril PANEL verde + mutation self-check con el trío `señal`/`exprés`/`los más` produciendo FAIL.
- Criterio 2 del ROADMAP: `NEGACIONES_LOCKED` extendido con los 4 idioms + self-check que prueba que no abre huecos.
- Cero cambios fuera de `app/lib/anti-insinuacion-guard.test.ts`.
</success_criteria>

<output>
Create `.planning/phases/126-panel-guards-wave-0-de-guards/126-01-SUMMARY.md` when done.
Documentar: conteo de tests antes/después, resultado verbatim de los dos controles positivos apareados,
y la regla D-05 (prefijo `components/panel-*.tsx` CONGELADO) + D-08 como nota heredada para el planner de Phase 128.
</output>
