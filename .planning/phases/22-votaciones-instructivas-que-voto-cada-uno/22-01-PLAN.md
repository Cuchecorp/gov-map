---
phase: 22-votaciones-instructivas-que-voto-cada-uno
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0028_votos_instructivos.sql
  - supabase/tests/0029_votos_instructivos.test.sql
  - app/lib/types.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "El RPC votos_de_parlamentario devuelve, por voto, el titulo del proyecto, el extracto de idea matriz, y resultado/total_si/total_no/total_abstencion/quorum/etapa de la votacion."
    - "El RPC sigue devolviendo SOLO filas confirmadas (estado_vinculo='confirmado'); no expone partido/rut/email."
    - "El tipo VotoFichaRow en app/lib/types.ts incluye los campos nuevos (titulo, idea_matriz, resultado, total_si, total_no, total_abstencion, total_pareo, quorum), todos nullable salvo los ya existentes."
  artifacts:
    - path: "supabase/migrations/0028_votos_instructivos.sql"
      provides: "RPC votos_de_parlamentario extendido (additivo, sin PII)"
      contains: "create or replace function votos_de_parlamentario"
    - path: "supabase/tests/0029_votos_instructivos.test.sql"
      provides: "pgTAP que afirma firma del RPC + columnas nuevas + cero PII a anon"
      contains: "plan("
    - path: "app/lib/types.ts"
      provides: "VotoFichaRow extendido con titulo/idea/desenlace"
      contains: "interface VotoFichaRow"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "votos_de_parlamentario RPC"
      via: "sb.rpc devuelve las filas tipadas como VotoFichaRow"
      pattern: "votos_de_parlamentario"
---

<objective>
Bloque A del runbook (22-CONTEXT.md §specifics): extender la capa de datos para que un voto traiga su SUSTANCIA y su DESENLACE sin N+1 joins en el server component. Se extiende el RPC `votos_de_parlamentario` (decisión LOCKED de este plan: RPC, no join-en-server-component) para que devuelva, por fila confirmada, el `titulo` del proyecto + el `idea_matriz` (de `proyecto_ficha`) + la fila de `votacion` (resultado/total_si/total_no/total_abstencion/total_pareo/quorum/etapa). Se extiende el tipo `VotoFichaRow` en `app/lib/types.ts` para reflejarlo.

Purpose: SC1 (sustancia por voto) y SC2 (desenlace por votación) necesitan estos campos por fila; SC4 (agrupar por proyecto) necesita titulo+idea+resultado para construir el arco. Hacerlo en el RPC mantiene `VotosView` pura y evita los tres `.in()` separados que el runbook marca como N+1.
Output: migración 0028 additiva (sin PII, mismo gate LEGAL-03 que 0019/0020/0026), pgTAP 0029, tipo extendido. La APLICACIÓN al remoto es un checkpoint de OPERADOR (Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md

<interfaces>
<!-- Contrato existente que este plan EXTIENDE. Extraído del codebase; el executor lo usa directo. -->

RPC actual (supabase/migrations/0019_voto_asistencia_y_ficha.sql) — votos_de_parlamentario(p_id text, p_limit int, p_offset int)
returns table (votacion_id text, boletin text, fecha timestamptz, seleccion text, etapa text, camara text, origen text, fecha_captura timestamptz, enlace text)
  — security INVOKER (language sql stable); join voto v → votacion vo on vo.id = v.votacion_id;
    where v.parlamentario_id = p_id and v.estado_vinculo = 'confirmado'; order by vo.fecha desc; grant execute to anon.

Tabla votacion (app/lib/types.ts VotacionRow) — columnas relevantes: resultado text|null, total_si int, total_no int, total_abstencion int, total_pareo int, quorum text|null, etapa text|null. Public-read (RLS 0008).
Tabla proyecto — boletin text, titulo text, materia text|null. Public-read.
Tabla proyecto_ficha (app/lib/types.ts ProyectoFichaRow) — boletin text (PK 1:1), idea_matriz text|null. Public-read (0011).

Tipo actual VotoFichaRow (app/lib/types.ts:149) — { votacion_id, boletin, fecha, seleccion: Seleccion, etapa: string|null, camara, origen, fecha_captura, enlace: string|null }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el RPC votos_de_parlamentario con titulo + idea_matriz + desenlace de la votacion</name>
  <read_first>
    - supabase/migrations/0019_voto_asistencia_y_ficha.sql (RPC actual + por qué votos es INVOKER y rebeldías DEFINER — NO tocar rebeldías)
    - supabase/migrations/0020_parlamentario_publico.sql (patrón de RPC público additivo, firma exacta, grant a anon)
    - app/lib/types.ts (VotacionRow líneas 49-67, ProyectoFichaRow 80-88, VotoFichaRow 149-159)
    - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md §decisions punto "Datos: SIN nueva ingesta" y §code_context "RPCs existentes"
  </read_first>
  <action>
    Crear `supabase/migrations/0028_votos_instructivos.sql` con UN `create or replace function votos_de_parlamentario(p_id text, p_limit int default 20, p_offset int default 0)` que CONSERVE la firma de parámetros y AMPLÍE el `returns table` con las columnas nuevas: `titulo text`, `idea_matriz text`, `resultado text`, `total_si int`, `total_no int`, `total_abstencion int`, `total_pareo int`, `quorum text`. Mantener TODAS las columnas existentes (votacion_id, boletin, fecha, seleccion, etapa, camara, origen, fecha_captura, enlace) en el mismo orden seguido de las nuevas. Mantener `language sql stable` (security INVOKER — toca solo tablas público-read: voto, votacion, proyecto, proyecto_ficha). Conservar el WHERE `v.parlamentario_id = p_id and v.estado_vinculo = 'confirmado'` y el `order by vo.fecha desc nulls last limit p_limit offset p_offset`. Añadir LEFT JOIN a `proyecto` (por `vo.boletin = proyecto.boletin`) para `titulo` y LEFT JOIN a `proyecto_ficha` (por boletin) para `idea_matriz` — LEFT para que un proyecto sin idea matriz (17/74) devuelva NULL, nunca filtre la fila (honest-state aguas arriba, NUNCA fabricar). Tomar resultado/total_si/total_no/total_abstencion/total_pareo/quorum de `vo` (la votación ya joinada). Como ES `create or replace` con returns table modificado, PRECEDER con `drop function if exists votos_de_parlamentario(text, int, int);` (cambiar el returns table requiere drop). Re-emitir `grant execute on function votos_de_parlamentario(text, int, int) to anon;`. PROHIBIDO: cualquier `create policy`/`grant select` sobre `parlamentario`; NO tocar `rebeldias_de_parlamentario`; NO emitir partido/rut/email. Encabezar el archivo con un comentario que explique que es additivo, sin PII, mismo gate LEGAL-03, y que la aplicación es checkpoint de operador.
  </action>
  <verify>
    <automated>grep -v '^--' "supabase/migrations/0028_votos_instructivos.sql" | grep -c "idea_matriz" | grep -qv '^0$' && grep -v '^--' "supabase/migrations/0028_votos_instructivos.sql" | grep -q "grant execute on function votos_de_parlamentario(text, int, int) to anon" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo contiene `drop function if exists votos_de_parlamentario(text, int, int);` antes del `create or replace`.
    - El `returns table` incluye las 8 columnas nuevas (titulo, idea_matriz, resultado, total_si, total_no, total_abstencion, total_pareo, quorum) además de las 9 existentes.
    - LEFT JOIN a proyecto y a proyecto_ficha (no INNER) — un proyecto sin idea matriz no descarta la fila.
    - El WHERE conserva `estado_vinculo = 'confirmado'`; sin lectura de `parlamentario.partido`.
    - `grep -v '^--' supabase/migrations/0028_votos_instructivos.sql | grep -c "create policy"` == 0 (cero policies sobre parlamentario).
    - El RPC sigue siendo INVOKER (no `security definer`).
  </acceptance_criteria>
  <done>0028 escrito; firma de parámetros intacta; returns table extendido; LEFT JOINs; grant a anon re-emitido; cero PII.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0029 + extender el tipo VotoFichaRow</name>
  <read_first>
    - supabase/migrations/0020_parlamentario_publico.sql y cualquier *.test.sql hermano (patrón pgTAP: has_function, columns, anon no-PII) — buscar bajo supabase/tests/ o supabase/migrations/
    - app/lib/types.ts (VotoFichaRow líneas 149-159 — el tipo a extender, y su comentario sobre por qué no trae mencion_nombre)
    - supabase/migrations/0028_votos_instructivos.sql (la firma que el pgTAP debe afirmar)
  </read_first>
  <action>
    (a) Crear `supabase/tests/0029_votos_instructivos.test.sql` (pgTAP). Espejar el estilo de los pgTAP previos (0021/0027). Afirmar: la función `votos_de_parlamentario(text, int, int)` existe; el conjunto de columnas devueltas incluye `titulo`, `idea_matriz`, `resultado`, `total_si`, `total_no`, `quorum`, `etapa` (usar `has_function`/`function_returns` o un `select` que ejecute el RPC con un p_id de prueba y verifique la presencia de las columnas vía `has_column` sobre el resultado, según el patrón usado en 0027); que `anon` PUEDE ejecutar el RPC (privilegio execute presente); y una aserción de NO-PII: que `anon` NO tiene `select` directo sobre `parlamentario` (re-afirma el piso deny-by-default, como 0023). Cerrar con `select * from finish();` y `plan(N)` con el conteo correcto.
    (b) Extender `interface VotoFichaRow` en `app/lib/types.ts` añadiendo: `titulo: string | null;`, `idea_matriz: string | null;`, `resultado: string | null;`, `total_si: number | null;`, `total_no: number | null;`, `total_abstencion: number | null;`, `total_pareo: number | null;`, `quorum: string | null;`. Actualizar el comentario JSDoc del tipo para notar que ahora trae sustancia (titulo/idea) + desenlace (resultado/totales/quorum) de la votación, que `idea_matriz`/`titulo` pueden ser null (honest-state, NUNCA fabricado) y que sigue siendo SOLO filas confirmadas.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "types.ts" | grep -qv "" && echo "no-type-errors-in-types" || echo "check tsc output"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/0029_votos_instructivos.test.sql` existe, abre con `begin;` + `select plan(N);` y cierra con `select * from finish();` + `rollback;` siguiendo el patrón de 0027.
    - El pgTAP afirma presencia de las columnas nuevas (al menos titulo, idea_matriz, resultado) en el resultado del RPC.
    - El pgTAP afirma execute a anon y NO select directo a anon sobre `parlamentario`.
    - `interface VotoFichaRow` en app/lib/types.ts contiene los 8 campos nuevos, todos opcionales/null donde corresponde.
    - `cd app && npx tsc --noEmit` no introduce errores nuevos en lib/types.ts (los consumidores aún no usan los campos nuevos — son additivos).
  </acceptance_criteria>
  <done>0029 escrito; VotoFichaRow extendido; tsc verde sobre types.ts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Aplicar 0028 al Supabase remoto + correr pgTAP (operador)</name>
  <action>Checkpoint de operador: aplicar la migración 0028 al Supabase remoto con supabase db push --db-url y correr el pgTAP 0029, porque build/typecheck no prueban que Postgres ejecutó el DDL (los tipos vienen del config generado, no de la DB viva). Ver how-to-verify para los comandos exactos.</action>
  <what-built>Migración additiva 0028 (RPC votos_de_parlamentario extendido) + pgTAP 0029. La APLICACIÓN al Supabase remoto NO la hace el agente: build/typecheck NO prueban que Postgres ejecutó la migración (los tipos vienen del config generado, no de la DB viva — riesgo de falso positivo, ver schema_push_requirement de 22-CONTEXT).</what-built>
  <how-to-verify>
    1. `source ~/obs_env.sh` (carga $SUPABASE_DB_URL esquivando BOM/CRLF).
    2. Aplicar al remoto: `supabase db push --db-url "$SUPABASE_DB_URL"` (aplica 0028).
    3. Correr pgTAP: `supabase test db --db-url "$SUPABASE_DB_URL"` — confirmar 0029 verde + que 0019/0020/0026/0027 siguen verdes (no regresión).
    4. Probe psql: `psql "$SUPABASE_DB_URL" -c "select titulo, idea_matriz, resultado, total_si, total_no, quorum, etapa from votos_de_parlamentario('D1054', 50, 0) limit 3;"` — confirmar que las columnas nuevas vienen pobladas para los boletines con votación (14309-04 / 18296-05) y que idea_matriz no es null donde el proyecto la tiene.
  </how-to-verify>
  <resume-signal>Escribe "0028 aplicada, pgTAP verde" o pega el error.</resume-signal>
</task>

</tasks>

<verification>
- 0028 es additivo: la firma de parámetros (text,int,int) no cambia; solo crece el returns table.
- Cero PII nueva: ninguna policy/grant sobre parlamentario; rebeldias_de_parlamentario intacto.
- El tipo VotoFichaRow refleja exactamente las columnas del RPC.
- Operador confirma aplicación remota + pgTAP antes de que Wave 2 consuma los campos.
</verification>

<success_criteria>
- RPC extendido devuelve titulo + idea_matriz + desenlace por fila confirmada (SC1/SC2/SC4 habilitados a nivel de datos).
- pgTAP 0029 afirma firma + columnas + cero PII a anon.
- VotoFichaRow tipado para los consumidores de Wave 2.
- Migración aplicada al remoto + pgTAP verde (checkpoint operador).
</success_criteria>

<output>
Create `.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-01-SUMMARY.md` when done
</output>
