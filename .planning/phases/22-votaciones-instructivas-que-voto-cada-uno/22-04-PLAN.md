---
phase: 22-votaciones-instructivas-que-voto-cada-uno
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/shots/
autonomous: false
requirements: []
must_haves:
  truths:
    - "El sitio en producción (https://observatorio-congreso.thevalis.workers.dev) sirve la build Linux con la sección de votaciones instructiva (sustancia + desenlace + asistencia corregida + agrupación por proyecto + honest-states)."
    - "La verificación e2e (browseros) confirma, sobre una ficha real (D1054), que un voto dice qué se votó + el desenlace y que 'Asistencia' está corregida."
    - "noindex, MONEY/NET gated (OFF), sin foto, sin partido permanecen intactos en producción; trazabilidad por dato (ProvenanceBadge) conservada."
  artifacts:
    - path: ".planning/phases/22-votaciones-instructivas-que-voto-cada-uno/shots/"
      provides: "Screenshots e2e 'después' de la verificación en producción"
  key_links:
    - from: "build Linux (Docker obsbuild)"
      to: "Cloudflare Workers (wrangler deploy)"
      via: "docker cp .open-next → cd app && npx wrangler deploy"
      pattern: "wrangler deploy"
---

<objective>
Bloque D del runbook: desplegar a producción y verificar e2e. La build es SOLO Linux (Windows produce bundle roto, 500ea) vía el contenedor `obsbuild` ya existente; el deploy es `wrangler deploy` directo sobre el bundle (NO `pnpm deploy`). La verificación e2e usa el driver browseros `bros.py` (los tools nativos están desfasados) sobre la ficha real D1054 y la ficha de un proyecto con votación. Confirma que las votaciones ahora son instructivas y que los invariantes (noindex, MONEY/NET OFF, sin foto/partido, trazabilidad) siguen intactos. Screenshots "después" a `shots/`.

Purpose: SC6 (redeploy + verificación e2e en producción). Cierra la fase con el resultado visible en vivo.
Output: sitio actualizado en producción; verificación e2e; screenshots en shots/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md

<interfaces>
<!-- Procedimientos LOCKED (de Phase 21), extraídos de 22-CONTEXT §code_context. -->

Build/Deploy SOLO Linux (LOCKED):
  1. `source ~/obs_env.sh` (carga env esquivando BOM/CRLF; OAuth wrangler sanchez.rossi@gmail.com ya autenticado).
  2. `docker start -a obsbuild` (contenedor YA existe, reusa node_modules → corre el build Next/OpenNext en Linux).
  3. Borrar el host `app/.open-next` (la build de Windows está rota; partir del bundle Linux limpio).
  4. `MSYS_NO_PATHCONV=1 docker cp obsbuild:/build/app/.open-next "<host>/app/"` (copia el bundle Linux al host).
  5. `cd app && npx wrangler deploy` (NO `pnpm deploy` — colisiona con builtin).
Sitio: https://observatorio-congreso.thevalis.workers.dev

browseros e2e (LOCKED): server live en 127.0.0.1:9200. Driver: `.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/refs/bros.py` con comandos `tabs` / `nav <page> <url>` / `shot <page> <out.jpg>` / `read <page>`. Los tools nativos mcp__browseros__* están desfasados — usar bros.py. Screenshots "antes": .planning/phases/21-.../shots/parlamentario_D1054_top.jpg.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Build Linux (Docker obsbuild) + wrangler deploy a produccion (operador)</name>
  <action>Checkpoint de operador: correr el build OpenNext en el contenedor Linux obsbuild, copiar el bundle .open-next al host y desplegar con wrangler deploy. El agente no corre Docker/wrangler (requiere el contenedor del operador, ~/obs_env.sh y la sesion OAuth). El build de Windows produce bundle roto (500ea) -> SOLO Linux. Ver how-to-verify para los comandos exactos.</action>
  <what-built>Build Linux + deploy a Cloudflare Workers. El agente NO corre Docker/wrangler (requiere el contenedor obsbuild del operador, su entorno ~/obs_env.sh y la sesión OAuth de wrangler). El build de Windows produce un bundle roto (500ea por middleware-manifest.json) → SOLO Linux.</what-built>
  <how-to-verify>
    1. `source ~/obs_env.sh`.
    2. `docker start -a obsbuild` (espera a que el build OpenNext termine sin error).
    3. Borrar el host `app/.open-next`.
    4. `MSYS_NO_PATHCONV=1 docker cp obsbuild:/build/app/.open-next "<host>/app/"`.
    5. `cd app && npx wrangler deploy` — confirmar deploy OK (URL del worker impresa, sin error de bundle).
    6. Smoke: abrir https://observatorio-congreso.thevalis.workers.dev/parlamentario/D1054 y confirmar 200.
  </how-to-verify>
  <resume-signal>Escribe "deploy OK" o pega el error del build/wrangler.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacion e2e (browseros) en produccion + screenshots</name>
  <action>Checkpoint de verificacion: con el driver bros.py, navegar la ficha real D1054 y la ficha de un proyecto con votacion en produccion, confirmar la instructividad (sustancia+desenlace+asistencia corregida+arco+honest-states) y los invariantes (noindex, MONEY/NET OFF, sin foto/partido, trazabilidad), y guardar screenshots en shots/. Ver how-to-verify para los pasos exactos.</action>
  <what-built>La sección de votaciones instructiva en producción: cada voto con título + idea + desenlace; "Asistencia" corregida (sentido vs asistencia real); agrupación por proyecto; línea explicativa; honest-state MONEY; espejo en la ficha del proyecto. Verificación e2e vía bros.py + screenshots a shots/.</what-built>
  <how-to-verify>
    Usar `.planning/phases/19-.../refs/bros.py` (server 127.0.0.1:9200):
    1. `nav <page> https://observatorio-congreso.thevalis.workers.dev/parlamentario/D1054` → `read <page>` y confirmar:
       - Un voto muestra el TÍTULO del proyecto + extracto de idea matriz (no solo "Boletín N°XXXX"); honest-state "no disponible aún" donde falte.
       - El desenlace por votación: "Votó {sentido} · el proyecto fue {resultado} {si–no}".
       - El bloque de sentido NO se llama "Asistencia" (es "Cómo votó"/"Sentido de sus votos") y hay una métrica de asistencia real separada.
       - Las votaciones están AGRUPADAS por proyecto (etapas bajo un mismo título).
       - La línea explicativa "A favor / En contra se refiere a aprobar o rechazar el proyecto en esa etapa…".
       - El honest-state "Financiamiento y contratos del Estado — Pendiente de revisión legal (Ley 21.719)".
       - SIN foto, SIN chip de partido; ProvenanceBadge en cada dato.
    2. `nav <page> https://observatorio-congreso.thevalis.workers.dev/proyecto/18296-05` → `read <page>` y confirmar el espejo: resultado+conteo por votación + conexión con la idea matriz del proyecto.
    3. Confirmar invariantes en producción: `noindex` en el `<head>` (generateMetadata), MONEY/NET ausentes del HTML (gated OFF), cero términos prohibidos visibles.
    4. `shot <page> .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/shots/parlamentario_D1054_despues.jpg` y un shot de la ficha del proyecto.
  </how-to-verify>
  <resume-signal>Escribe "e2e OK" + confirma los screenshots en shots/, o describe lo que falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host (Windows) → bundle de producción | El build de Windows está roto; solo el bundle Linux (obsbuild) puede desplegarse. |
| build → producción pública | El deploy expone el sitio; los invariantes (noindex, MONEY/NET OFF) deben sobrevivir el deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering | bundle de deploy | mitigate | Build SOLO Linux (obsbuild) + borrar host .open-next antes de docker cp; nunca desplegar bundle de Windows. |
| T-22-07 | Information disclosure | invariantes en producción | mitigate | Verificación e2e (browseros) confirma noindex + MONEY/NET ausentes del HTML + sin foto/partido tras el deploy. |
</threat_model>

<verification>
- Deploy desde bundle Linux (no Windows); wrangler deploy directo.
- e2e (bros.py) confirma instructividad + invariantes sobre fichas reales.
- Screenshots "después" en shots/.
</verification>

<success_criteria>
- SC6 (redeploy + e2e): sitio en producción con votaciones instructivas; noindex/MONEY/NET intactos; sin foto/partido; trazabilidad por dato.
- Screenshots de evidencia en shots/.
</success_criteria>

<output>
Create `.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-04-SUMMARY.md` when done
</output>
