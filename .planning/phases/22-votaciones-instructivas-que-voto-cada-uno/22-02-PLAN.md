---
phase: 22-votaciones-instructivas-que-voto-cada-uno
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/components/votos-por-parlamentario.tsx
  - app/components/voto-ficha-row.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/lib/format.ts
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cada voto muestra el titulo del proyecto + un extracto de la idea matriz; cuando idea_matriz es null muestra un honest-state ('De qué trata: no disponible aún'), nunca fabrica."
    - "Cada votacion muestra resultado + conteo (total_si-total_no, abstencion/quorum/etapa) y enmarca el voto de la persona contra el desenlace ('Votó En contra · el proyecto fue Rechazado 58-81'), sin juicio."
    - "El bloque mal etiquetado 'Asistencia' se separa: asistencia real (presente vs ausente) como métrica propia, y el desglose de sentido se renombra a 'Cómo votó' / 'Sentido de sus votos'."
    - "Las votaciones se agrupan por proyecto (titulo + idea + desenlace del proyecto); bajo cada proyecto se listan las etapas votadas — trayectoria, no lista plana."
    - "Una línea neutra explica qué significa 'a favor/en contra' (aprobar/rechazar el proyecto en esa etapa), sin lenguaje causal; cuando hay pocas votaciones lo dice honestamente."
    - "La ficha tiene un bloque honest-state de 'Financiamiento y contratos del Estado' ('Pendiente de revisión legal — Ley 21.719') cuando MONEY está OFF, en vez de silencio."
    - "Cero términos prohibidos (DESIGN-SYSTEM §6 banned-vocab) en código ni copy."
  artifacts:
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "VotosView instructiva (pura) + VotosSection extendida + agrupación por proyecto"
      min_lines: 200
    - path: "app/components/voto-ficha-row.tsx"
      provides: "VotoFichaRow con titulo/idea/desenlace"
    - path: "app/components/votos-por-parlamentario.test.tsx"
      provides: "Tests RTL con fixtures de los nuevos comportamientos + negative-match anti-insinuación"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "votos_de_parlamentario RPC"
      via: "VotosSection lee titulo/idea_matriz/resultado/totales del RPC extendido"
      pattern: "votos_de_parlamentario"
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "honest-state MONEY"
      via: "bloque 'Financiamiento y contratos del Estado' visible cuando moneyPublicEnabled es false"
      pattern: "Pendiente de revisión legal"
---

<objective>
Bloque B del runbook: reescribir la presentación de votaciones de la ficha del parlamentario para que sea INSTRUCTIVA, consumiendo el RPC extendido (Plan 22-01). Cinco cambios sobre `VotosView` (pura) + `VotoFichaRow`: (a) corregir "Asistencia" (separar asistencia real del sentido del voto); (b) cada voto con titulo + extracto de idea + desenlace; (c) agrupar por proyecto (el arco); (d) línea explicativa neutra de "a favor/en contra"; (e) bloque honest-state de Financiamiento/contratos (MONEY pendiente legal) en la ficha. La vista sigue PURA → tests RTL con fixtures. Respetar DESIGN-SYSTEM §6/§8 (cero score/juicio/causal).

Purpose: SC1, SC2, SC3, SC4, SC5 — el corazón de la fase. Un ciudadano entiende, sin clic, de qué trataba el proyecto, cómo votó la persona, y qué pasó.
Output: VotosView reescrita, VotoFichaRow extendida, tests RTL nuevos verdes, honest-state MONEY en page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md

<interfaces>
<!-- Contratos que el executor usa directo, extraídos del codebase. -->

VotoFichaRow (app/lib/types.ts, EXTENDIDO por 22-01): { votacion_id, boletin, fecha, seleccion: Seleccion, etapa: string|null, camara, origen, fecha_captura, enlace: string|null, titulo: string|null, idea_matriz: string|null, resultado: string|null, total_si: number|null, total_no: number|null, total_abstencion: number|null, total_pareo: number|null, quorum: string|null }.
Seleccion = "si" | "no" | "abstencion" | "pareo" | "ausente". OPCION_LABEL ya mapea si→"A favor", no→"En contra", abstencion→"Abstención", pareo→"Pareo", ausente→"Ausente".

VotosViewData (app/components/votos-por-parlamentario.tsx) — forma pura testeada por RTL. Hoy: { votos: VotoFichaConMateria[], totalVotos, conteos: Record<Seleccion,number>, rebeldias: RebeldiaRow[], materiaActiva, materias, page, totalPages, noIngestado }.
VotoFichaConMateria = VotoFichaRow & { materia: string|null }.
RebeldiaRow: { votacion_id, boletin, fecha, seleccion_propia: Seleccion, mayoria_bancada: Seleccion }.

fechaCorta(d: Date): string — "14 may 2026". (app/lib/format.ts)
ProvenanceBadge, IdentityMarker, Badge — ya importados en voto-ficha-row.tsx.
moneyPublicEnabled(process.env): boolean — server-only gate (app/lib/money-gate.ts). OFF por default; envuelve hoy #dinero/#financiamiento en page.tsx.

Test runner: `cd app && npx vitest run` (package.json "test": "vitest run"). Fixtures: makeVoto/makeMencion/makeViewData en votos-por-parlamentario.test.tsx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VotoFichaRow instructiva (titulo + idea + desenlace) + helper de truncado</name>
  <read_first>
    - app/components/voto-ficha-row.tsx (componente actual: OpcionChip + Boletín enlazado + ProvenanceBadge; VotoFichaMencionRow para estado (b))
    - app/lib/format.ts (fechaCorta; aquí se añade el helper de truncado puro)
    - app/lib/types.ts (VotoFichaRow extendido por 22-01)
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md §9 (copy de votos)
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §6 (banned-vocab) y §8 (invariantes)
  </read_first>
  <behavior>
    - Test: un voto con titulo + idea_matriz renderiza el titulo del proyecto y un extracto truncado de la idea (no solo "Boletín N°XXXX").
    - Test: un voto con idea_matriz=null renderiza el honest-state "De qué trata: no disponible aún" y NUNCA un texto fabricado.
    - Test: un voto con resultado="Rechazado", total_si=58, total_no=81, seleccion="no" renderiza el framing "Votó En contra · el proyecto fue Rechazado" con el conteo "58–81" (mono), como hecho factual sin adjetivo.
    - Test: un voto sin resultado (null) omite la cláusula de desenlace pero conserva titulo/idea (degrada honesto).
    - Test (negative-match): el output renderizado no contiene términos prohibidos de DESIGN-SYSTEM §6 ni adjetivos de juicio sobre el voto.
  </behavior>
  <action>
    En `app/lib/format.ts` añadir un helper PURO `extractoIdea(idea: string, max = 160): string` que corta a ~1-2 líneas en límite de palabra y agrega "…" — NUNCA reescribe ni resume, solo trunca literal. Opcionalmente `conteoVotacion(si: number, no: number): string` que devuelve "58–81" (guion largo, para render en mono). NO fabricar abstención/quorum si vienen null.
    En `app/components/voto-ficha-row.tsx` extender la fila confirmada `VotoFichaRow`: mantener OpcionChip + enlace al boletín + ProvenanceBadge, y AÑADIR, en el bloque del voto: el `titulo` del proyecto (Body, prominente; si null caer al boletín como hoy), un extracto de `idea_matriz` vía `extractoIdea` con prefijo neutro "De qué trata:" (o el honest-state "De qué trata: no disponible aún" cuando null), y una línea de DESENLACE que enmarque el voto contra el resultado usando OPCION_LABEL[seleccion] + verbo neutro + `resultado` + el conteo `total_si–total_no` en mono (p.ej. "Votó En contra · el proyecto fue Rechazado 58–81"). El framing es un HECHO de la votación, no una valoración (DESIGN-SYSTEM §8). Cuando `resultado` es null, omitir solo la cláusula de desenlace. PROHIBIDO: adjetivo/juicio sobre el voto, lenguaje causal, score; el nombre interno "rebeldías" NUNCA aparece. Cero fabricación de titulo/idea. Aplicar el mismo tratamiento de sustancia/desenlace a `VotoFichaMencionRow` (estado b) conservando su IdentityMarker y sin enlazar al parlamentario.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/voto-ficha-row 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `extractoIdea` trunca literal en límite de palabra y agrega "…", nunca reescribe.
    - VotoFichaRow renderiza titulo (o fallback boletín) + extracto de idea o honest-state "no disponible aún" + (si resultado≠null) línea de desenlace con conteo mono.
    - `grep -niE "porque|a cambio de|afinidad|puntaje|score|conflicto de inter|enriquecimiento|sospechoso" app/components/voto-ficha-row.tsx` filtrando comentarios da 0 matches en copy.
    - El heading/identificador "rebeldías" no aparece en UI.
    - Tests RTL de voto-ficha-row verdes.
  </acceptance_criteria>
  <done>VotoFichaRow muestra sustancia + desenlace; honest-state para idea null; helper de truncado puro; tests verdes; cero términos prohibidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VotosView — corregir "Asistencia", agrupar por proyecto, línea explicativa, cobertura honesta</name>
  <read_first>
    - app/components/votos-por-parlamentario.tsx (VotosView pura: bloque "Asistencia" líneas 145-183, lista de votaciones 219-279; VotosSection 327-442 que arma los datos y hace el join manual de materia)
    - app/components/voto-ficha-row.tsx (la fila ya instructiva de Task 1)
    - app/components/votos-por-parlamentario.test.tsx (fixtures makeVoto/makeViewData a extender)
    - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md §decisions puntos 3 (Asistencia), 4 (agrupar por proyecto/arco), 5 (significado + cobertura honesta)
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §6 (voz), §7 (honest-states), §8 (invariantes)
  </read_first>
  <behavior>
    - Test: con conteos {si:5,no:4,ausente:0}, el desglose de SENTIDO se renderiza bajo un heading honesto ("Cómo votó" o "Sentido de sus votos"), NUNCA bajo "Asistencia".
    - Test: la ASISTENCIA real (presente vs ausente) se computa de seleccion==='ausente' vs el resto y se muestra como métrica propia; si no hay ausentes, dice honesto "Emitió N votos registrados" sin inventar asistencia.
    - Test: con dos votos del mismo boletín (etapas "Primer trámite" y "Tercer trámite"), la vista los AGRUPA bajo un solo encabezado de proyecto (titulo + idea + desenlace) listando ambas etapas — no dos filas planas.
    - Test: la vista renderiza la línea neutra "A favor / En contra se refiere a aprobar o rechazar el proyecto en esa etapa de su tramitación." (sin causalidad).
    - Test: con pocas votaciones (2 boletines), la vista muestra una nota honesta de cobertura y NO aparenta exhaustividad.
    - Test (negative-match): el render completo de VotosView no contiene términos de DESIGN-SYSTEM §6 banned-vocab.
  </behavior>
  <action>
    Reescribir `VotosView` en `app/components/votos-por-parlamentario.tsx` (mantenerla PURA: mismas props `id` + `data: VotosViewData`; extender `VotosViewData` si hace falta para llevar la asistencia derivada o un flag de cobertura, manteniendo el contrato testeable):
    (a) ASISTENCIA: renombrar el `<h3>Asistencia</h3>` a un heading honesto de SENTIDO ("Cómo votó" o "Sentido de sus votos"); ese bloque sigue mostrando el desglose si/no/abstencion/pareo + "Emitió N votos". AÑADIR una métrica separada de ASISTENCIA REAL derivada de `conteos.ausente` vs el total (presente = total − ausente): si hay ausentes, "Presente en X de N votaciones · Ausente en K"; si ausente==0, "Emitió N votos registrados" honesto sin afirmar asistencia perfecta.
    (b) AGRUPAR POR PROYECTO (el arco): en vez de la lista plana de `votos`, agrupar por `boletin`; por grupo una cabecera de proyecto (titulo + extracto de idea + desenlace del proyecto cuando el resultado sea consistente) y debajo las etapas votadas (cada una vía la VotoFichaRow de Task 1 con su opción + desenlace). Conservar la paginación de forma coherente (paginar por proyecto o por votación; el executor decide y lo documenta). Mantener la faceta "Por tema" y "Votó distinto a su bancada" sin score/juicio.
    (c) LÍNEA EXPLICATIVA: antes de la lista agrupada, un `<p>` neutro con el copy LOCKED (§decisions punto 5): "A favor / En contra se refiere a aprobar o rechazar el proyecto en esa etapa de su tramitación." Sin causalidad.
    (d) COBERTURA HONESTA: cuando el número de proyectos/votaciones es bajo, una nota muted honesta ("Se registran votaciones de N proyectos en las fuentes consultadas; la cobertura se está ampliando."). NUNCA aparentar exhaustividad. NO re-ingestar (alcance separado).
    Actualizar `VotosSection` (server) para leer del RPC extendido: titulo/idea_matriz/resultado/totales ya vienen por fila — eliminar el join manual `proyecto.materia` por boletín SOLO si el RPC también trae materia; si materia no está en el RPC extendido, conservar ese único join para la faceta "Por tema". Cero idea/materia fabricada.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/votos-por-parlamentario 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - El heading del desglose de sentido NO es "Asistencia" (es "Cómo votó" o "Sentido de sus votos"); existe una métrica de asistencia real separada derivada de `ausente`.
    - Votos del mismo boletín se agrupan bajo una cabecera de proyecto con titulo+idea+etapas; el test del arco pasa.
    - La línea explicativa de "a favor/en contra" está presente con el copy LOCKED.
    - La nota de cobertura honesta aparece cuando hay pocos proyectos.
    - VotosView sigue siendo función pura (recibe data por props; sin fetch/Supabase en la vista).
    - Negative-match de banned-vocab sobre votos-por-parlamentario.tsx == 0 en copy.
    - Suite RTL completa de votos-por-parlamentario verde.
  </acceptance_criteria>
  <done>Asistencia corregida; agrupación por proyecto (arco); línea explicativa; cobertura honesta; VotosSection consume el RPC extendido; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Honest-state MONEY 'Financiamiento y contratos del Estado' en la ficha</name>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (carriles apilados; secciones #dinero/#financiamiento envueltas en moneyPublicEnabled — líneas 88-135; la frontera mt-12 anti-insinuación)
    - app/lib/money-gate.ts (moneyPublicEnabled, server-only, OFF por default)
    - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md §decisions punto 6 (Funds honest-state)
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §7 (honest-states) y §8.6 (MONEY gate)
  </read_first>
  <action>
    En `app/app/parlamentario/[id]/page.tsx` añadir un carril hermano `<section className="mt-12">` con `<h2>Financiamiento y contratos del Estado</h2>` y un cuerpo honest-state muted con el copy LOCKED: "Pendiente de revisión legal (Ley 21.719) antes de publicarse." Este bloque se muestra CUANDO `moneyPublicEnabled(process.env)` es false (el default). CLAVE anti-insinuación: el honest-state NO renderiza NINGÚN dato de dinero (no toca Supabase, no compone con votos) — es solo un texto que explica por qué la sección aún no se muestra. Hacer mutuamente excluyentes el honest-state y las secciones reales: cuando MONEY está ON (futuro), siguen las `<section id="dinero">/<section id="financiamiento">` reales y el honest-state se omite; cuando está OFF, se muestra solo el honest-state. Mantener `mt-12` como frontera de carril; el honest-state es su propia `<section>`, NUNCA anidado en #votos. PROHIBIDO: que el honest-state mencione un monto, un contrato, un donante, o componga con un voto.
  </action>
  <verify>
    <automated>cd app && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Pendiente de revisión legal" "app/app/parlamentario/[id]/page.tsx"` encuentra el copy LOCKED.
    - El honest-state es una `<section className="mt-12">` hermana (no anidada en #votos), con `<h2>Financiamiento y contratos del Estado</h2>`.
    - El honest-state se renderiza cuando moneyPublicEnabled es false y se omite cuando es true (mutuamente excluyente con #dinero/#financiamiento).
    - El bloque no toca Supabase ni renderiza monto/contrato/donante.
    - `cd app && npx vitest run` — suite completa verde; `npx tsc --noEmit` sin errores nuevos en page.tsx.
  </acceptance_criteria>
  <done>Honest-state MONEY presente en la ficha cuando OFF; carril propio mt-12; cero dato de dinero; suite y tsc verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (frontend) → RPC votos_de_parlamentario | El frontend lee como rol anon; el RPC nunca debe emitir partido/rut/email. |
| Datos de fuente → copy renderizado | El titulo/idea/resultado de la fuente se muestran verbatim; el riesgo es editorializar al enmarcarlos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information disclosure | VotosSection lee RPC | mitigate | El RPC (22-01) es INVOKER sobre tablas público-read; cero PII; este plan no añade lectura de partido. |
| T-22-02 | Tampering (anti-insinuación) | VotoFichaRow / VotosView copy | mitigate | Negative-match grep de DESIGN-SYSTEM §6 banned-vocab como criterio de aceptación; framing factual del desenlace, cero juicio/causal. |
| T-22-03 | Information disclosure | honest-state MONEY | mitigate | El honest-state no toca Supabase ni emite dato de dinero; MONEY sigue gated (§8.6); se muestra solo texto legal. |
</threat_model>

<verification>
- VotoFichaRow y VotosView son puras y se testean con fixtures RTL (sin runtime).
- Cero términos prohibidos (negative-match) en código y copy.
- Honest-state MONEY no filtra dato de dinero ni compone con votos.
- `cd app && npx vitest run` verde; `npx tsc --noEmit` sin errores nuevos.
</verification>

<success_criteria>
- SC1 sustancia por voto; SC2 desenlace por votación; SC3 asistencia corregida; SC4 agrupar por proyecto; SC5 línea explicativa + cobertura honesta + honest-state funds.
- Vista pura + tests RTL verdes.
- Anti-insinuación verificada por negative-match.
</success_criteria>

<output>
Create `.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-02-SUMMARY.md` when done
</output>
