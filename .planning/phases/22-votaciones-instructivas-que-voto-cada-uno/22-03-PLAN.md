---
phase: 22-votaciones-instructivas-que-voto-cada-uno
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - app/components/votacion-card.tsx
  - app/components/votacion-card.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "La sección de votaciones de la ficha del proyecto muestra, por votación, resultado + conteo (total_si-total_no, abstención, quorum, etapa) enmarcado como hecho factual."
    - "El espejo conecta la votación con la idea matriz del propio proyecto (que ya vive en esa página) — el ciudadano ve qué se votó y qué pasó sin salir de la ficha del proyecto."
    - "Cero términos prohibidos (DESIGN-SYSTEM §6 banned-vocab) en código ni copy."
  artifacts:
    - path: "app/components/votacion-card.tsx"
      provides: "VotacionCard con desenlace explícito (resultado + conteo enmarcado)"
    - path: "app/components/votacion-card.test.tsx"
      provides: "Tests RTL del desenlace + negative-match anti-insinuación"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "idea matriz del proyecto"
      via: "la sección de votaciones referencia/contextualiza con proyecto_ficha.idea_matriz ya leída en la página"
      pattern: "idea_matriz|IdeaMatriz"
---

<objective>
Bloque C del runbook: el ESPEJO en la ficha del proyecto. La sección de votaciones de `/proyecto/[boletin]` ya muestra `VotacionBar` + totales + ResultadoBadge, pero de forma poco instructiva. Este plan enmarca el desenlace explícitamente (resultado + conteo como frase factual) y conecta la votación con la idea matriz del propio proyecto (que ya vive en esa página, sección `#idea-matriz`), para que el ciudadano que mira un proyecto entienda qué se votó y qué pasó sin salir de la ficha.

Purpose: SC6 (espejo en la ficha del proyecto). Simetría con la ficha del parlamentario (Plan 22-02): el mismo hecho —resultado+conteo— se presenta instructivo en ambas superficies, sin juicio ni causalidad.
Output: VotacionCard con desenlace enmarcado, tests RTL, conexión a la idea matriz en la página del proyecto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md

<interfaces>
<!-- Contratos que el executor usa directo, extraídos del codebase. -->

VotacionCard (app/components/votacion-card.tsx) — props { votacion: VotacionRow }. Hoy renderiza CamaraChip + fecha + etapa + quorum + VotacionBar + "Sí: X · No: Y · Abst.: Z · Pareo: W" + EtapaBadge(resultado) + ProvenanceBadge + (VotoDetalle si hay voto[]).
VotacionRow (app/lib/types.ts:49): { id, boletin, fecha, etapa: string|null, tipo, quorum: string|null, resultado: string|null, total_si, total_no, total_abstencion, total_pareo, camara, origen, fecha_captura, enlace, voto?: VotoRow[] }.

conteoVotacion(si, no): string — helper "58–81" creado en app/lib/format.ts por Plan 22-02 (REUSAR, no duplicar).
fechaCorta(d: Date): string — app/lib/format.ts.

Página del proyecto (app/app/proyecto/[boletin]/page.tsx): VotacionesSection (líneas 154-180) lee `votacion` con embed voto(*) y mapea VotacionCard; IdeaMatrizSection (95-112) lee proyecto_ficha.idea_matriz vía leerFicha (React.cache). Secciones #votaciones y #idea-matriz son hermanas mt-12.

Test runner: `cd app && npx vitest run`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VotacionCard — enmarcar el desenlace (resultado + conteo factual)</name>
  <read_first>
    - app/components/votacion-card.tsx (componente actual completo)
    - app/lib/format.ts (conteoVotacion + fechaCorta — REUSAR conteoVotacion de 22-02)
    - app/lib/types.ts (VotacionRow líneas 49-67)
    - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md §decisions punto 7 (espejo) y §decisions Anti-insinuación
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §6 (voz), §8 (invariantes)
  </read_first>
  <behavior>
    - Test: una votación con resultado="Rechazado", total_si=58, total_no=81 renderiza una frase de desenlace factual ("El proyecto fue Rechazado · 58–81") con el conteo en mono.
    - Test: una votación con resultado=null omite la frase de desenlace pero conserva la barra y los totales (degrada honesto).
    - Test: abstención/quorum/etapa siguen mostrándose cuando existen; cuando null, no se fabrican.
    - Test (negative-match): el render no contiene términos de DESIGN-SYSTEM §6 banned-vocab ni adjetivos de juicio sobre la votación.
  </behavior>
  <action>
    En `app/components/votacion-card.tsx` añadir una línea/bloque de DESENLACE explícito por encima o junto al `EtapaBadge(resultado)` actual: una frase factual "El proyecto fue {resultado} · {conteoVotacion(total_si, total_no)}" usando el helper `conteoVotacion` de `app/lib/format.ts` (importarlo; NO duplicar la lógica). Mantener el desglose existente "Sí: X · No: Y · Abst.: Z · Pareo: W", la VotacionBar, el quorum, la etapa y el ProvenanceBadge. Cuando `resultado` es null, omitir SOLO la frase de desenlace (la barra y totales se conservan). El framing es un hecho de la votación, no una valoración (DESIGN-SYSTEM §8). PROHIBIDO: adjetivo/juicio, lenguaje causal, score. No tocar la guarda de identidad de VotoDetalle/VotoRow.
  </action>
  <verify>
    <automated>cd app && npx vitest run components/votacion-card 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - VotacionCard renderiza "El proyecto fue {resultado} · {si}–{no}" (conteo en mono) cuando resultado≠null, vía conteoVotacion importado de format.ts.
    - resultado=null omite solo la frase de desenlace; barra y totales intactos.
    - `grep -niE "porque|a cambio de|afinidad|puntaje|score|conflicto de inter|sospechoso" app/components/votacion-card.tsx` filtrando comentarios da 0 matches en copy.
    - Tests RTL de votacion-card verdes.
  </acceptance_criteria>
  <done>VotacionCard enmarca el desenlace con resultado+conteo factual; reusa conteoVotacion; tests verdes; cero términos prohibidos.</done>
</task>

<task type="auto">
  <name>Task 2: Conectar la votación con la idea matriz del proyecto en la página</name>
  <read_first>
    - app/app/proyecto/[boletin]/page.tsx (VotacionesSection líneas 154-180; IdeaMatrizSection 95-112; leerFicha React.cache 83-93; orden de secciones #votaciones / #idea-matriz)
    - app/components/votacion-card.tsx (la card ya instructiva de Task 1)
    - .planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-CONTEXT.md §decisions punto 7
    - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md §8 (carriles hermanos mt-12 — NO componer)
  </read_first>
  <action>
    En `app/app/proyecto/[boletin]/page.tsx`, conectar la sección de votaciones con la idea matriz del proyecto SIN romper la frontera de carril (las secciones siguen hermanas mt-12, no se anidan). Opción preferente: leer `proyecto_ficha.idea_matriz` vía la `leerFicha` cacheada que ya existe (React.cache → sin query extra) y mostrar, al inicio de la sección de votaciones, una línea de contexto neutra que recuerde de qué trata el proyecto (titulo/idea ya visibles en la página) — p.ej. un sub-encabezado o una línea muted "Qué se votó: {extracto de idea}" usando `extractoIdea` de format.ts; o, si se prefiere no duplicar la idea, un enlace/ancla interno "Ver de qué trata (idea matriz)" hacia `#idea-matriz`. El objetivo (SC6) es que el ciudadano en la ficha del proyecto entienda qué se votó + qué pasó sin salir; el executor elige la conexión más limpia y la documenta. NO componer dinero/lobby con la votación; NO fabricar idea cuando idea_matriz es null (honest-state "no disponible aún" o simplemente omitir el contexto, sin texto inventado).
  </action>
  <verify>
    <automated>cd app && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - La sección de votaciones del proyecto conecta con la idea matriz (línea de contexto desde proyecto_ficha o ancla a #idea-matriz), reusando leerFicha (sin query nueva) o extractoIdea.
    - Cuando idea_matriz es null, no se fabrica texto (honest-state o ausencia de la línea de contexto).
    - Las secciones #votaciones y #idea-matriz siguen hermanas mt-12 (no anidadas; no componen con lobby/dinero).
    - `cd app && npx vitest run` — suite completa verde; `npx tsc --noEmit` sin errores nuevos en la página.
  </acceptance_criteria>
  <done>Espejo del proyecto conecta votación↔idea matriz; carriles intactos; cero fabricación; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datos de fuente → copy renderizado | resultado/totales/idea de la fuente mostrados verbatim; riesgo de editorializar el framing del desenlace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering (anti-insinuación) | VotacionCard / página proyecto | mitigate | Negative-match grep de DESIGN-SYSTEM §6 banned-vocab; framing factual del desenlace; carriles hermanos mt-12 nunca compuestos con dinero/lobby. |
| T-22-05 | Information disclosure | VotacionesSection lee votacion/proyecto_ficha | accept | Ambas son público-read (RLS 0008/0011); cero PII; misma postura que la página ya enviada. |
</threat_model>

<verification>
- VotacionCard pura; tests RTL con fixtures.
- Conexión votación↔idea sin query nueva (React.cache) y sin fabricar idea.
- Cero términos prohibidos (negative-match).
- Carriles hermanos mt-12 intactos.
</verification>

<success_criteria>
- SC6 (parte espejo): la ficha del proyecto muestra resultado+conteo enmarcado y conecta con su idea matriz.
- Simetría con la ficha del parlamentario; cero juicio/causal.
- Suite y tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/22-votaciones-instructivas-que-voto-cada-uno/22-03-SUMMARY.md` when done
</output>
