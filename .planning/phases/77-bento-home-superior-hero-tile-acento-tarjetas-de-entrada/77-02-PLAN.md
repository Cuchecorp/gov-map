---
phase: 77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - app/app/page.tsx
  - app/app/page.test.tsx
autonomous: true
requirements: [BENTO-02]

must_haves:
  truths:
    - "The home upper half renders as a BentoGrid: hero span-4 + accent tile span-2 (¿Cómo leer esto?) + 3 entry tiles span-2 (Buscar/Parlamentarios/Agenda)"
    - "Every hero string is byte-identical to the current LOCKED copy; the only net-new hero string is the kicker OBSERVATORIO DEL CONGRESO"
    - "The accent tile links to /sobre with the /sobre 'El principio' formula body (NOT the mockup correlaciones string) and CTA Ver metodología →"
    - "force-dynamic is still exported and ActualidadModule stays mounted below the grid (migrates in Phase 78)"
    - "The grid sits inside a max-w-[1120px] mx-auto px-4 md:px-8 container added in page.tsx (no such wrapper exists in layout.tsx)"
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Bento-composed home: BentoGrid + 5 BentoTiles (asChild) + retained ActualidadModule + force-dynamic"
      contains: "BentoGrid"
    - path: "app/app/page.test.tsx"
      provides: "Migrated tests: hero LOCKED strings byte-identical + kicker + accent tile + 3 grid links + spans + retained module + force-dynamic; nav-based Contract-2 replaced"
      contains: "OBSERVATORIO DEL CONGRESO"
  key_links:
    - from: "app/app/page.tsx hero + accent + entry tiles"
      to: "BentoGrid / BentoTile (Phase 76 primitives, accent variant fixed in 77-01)"
      via: "BentoTile asChild wrapping single <section>/<Link>"
      pattern: "BentoTile"
    - from: "app/app/page.tsx accent tile"
      to: "/sobre"
      via: "Link href with Ver metodología → CTA"
      pattern: "href=\"/sobre\""
    - from: "app/app/page.tsx"
      to: "ActualidadModule + force-dynamic"
      via: "retained mount below </BentoGrid> + export const dynamic"
      pattern: "ActualidadModule"
---

<objective>
Rewrite `app/app/page.tsx` into the upper half of the mockup bento (filas 1-2): mount the Phase 76 `BentoGrid`/`BentoTile` primitives (accent variant fixed in 77-01) as a hero tile (span-4), an accent tile "¿Cómo leer esto?" (span-2), and 3 entry cards (span-2). Keep every LOCKED hero string byte-identical, add the mono kicker, preserve `force-dynamic` + the `ActualidadModule` mount, and migrate `page.test.tsx` from the old nav-based Contract-2 to grid/link assertions.

Purpose: This is the BENTO-02 deliverable — the home superior becomes the bento. Consumes 77-01's contracts.
Output: page.tsx rewrite + page.test.tsx migration. Zero new packages, zero data changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-RESEARCH.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-PATTERNS.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-UI-SPEC.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-01-SUMMARY.md

<critical_constraints>
- **Copy LOCKED byte-identical:** h1 `Qué pasó con cada proyecto de ley y cada parlamentario.` + `<em>` cursiva `Con la fuente a la vista.`, subtitle, trust line `Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad.`, 4 pills (`EXAMPLE_CHIPS`), 3 entry-card titles/bodies/hrefs (`ENTRY_CARDS`) — all preserved verbatim. The ONLY net-new hero string is the kicker `OBSERVATORIO DEL CONGRESO`.
- **CERO mockup strings in production:** the mockup placeholder h1 (`Busca cualquier proyecto de ley por tema o número…`) does NOT enter. The accent-tile body adopts the /sobre "El principio" formula; the mockup's "Las correlaciones no son indicativas de irregularidades…" is BANNED (anti-insinuación invariant 2).
- **Accent CTA route (LOCKED):** `href="/sobre"`, label `Ver metodología →`. Do NOT "fix" to /metodologia (RESEARCH Open Question 2).
- **Tailwind 4 (CR-01):** `[var(--token)]` always; never `[--token]`. No `#` hex in the composition.
- **`→` glyph (lesson F53):** always `<span aria-hidden="true" className="pl-1">→</span>`, never a whitespace text node.
- **Slot single-child (RESEARCH gotcha):** `<BentoTile asChild>` wraps EXACTLY ONE `<section>`/`<Link>`; all tile content nests inside that one element.
- **Container (RESEARCH Pitfall 1):** layout.tsx has NO page `max-w-[1120px]` wrapper — add `<div className="max-w-[1120px] mx-auto px-4 md:px-8">` around `<BentoGrid>` in page.tsx. Use `px-4 md:px-8`, NOT the UI-SPEC's `px-6`.
- **force-dynamic + ActualidadModule (RESEARCH Pitfall 5):** keep `export const dynamic = "force-dynamic";` and `<ActualidadModule />` mounted BELOW `</BentoGrid>`.
- **BrandIcon on inverted surface:** pass `color="currentColor"` (default hex #2A5859 would render petróleo-on-petróleo = invisible). Do NOT edit brand-icon.tsx.
- **Discretion (fixed here):** inline the accent tile + entry-card map in page.tsx (no new sub-component files — matches the current self-contained style; SearchBox stays the only extracted island). Hero padding `p-8`, accent/entry padding `p-6`.
</critical_constraints>

<interfaces>
BentoTile asChild API (app/components/bento/bento-tile.tsx — verified; accent variant fixed in 77-01):
```
<BentoTile variant="default" span={4} asChild><section className="p-8 ...">…</section></BentoTile>
<BentoTile variant="accent" span={2} asChild><Link href="/sobre" className="p-6 ...">…</Link></BentoTile>
```
BentoGrid (Phase 76, consume unchanged): grid-cols-1 md:grid-cols-6, gap-[14px]. Import both from `@/components/bento/bento-grid` + `@/components/bento/bento-tile` (verify exact export paths in the bento dir).

LOCKED constants to preserve verbatim (current page.tsx:31-64): EXAMPLE_CHIPS (4 pills) + ENTRY_CARDS (3 cards).

Accent-tile body copy (adopt /sobre "El principio" formula, linter-clean — app/app/sobre/page.tsx:32-38 is the source of the formula):
```
Cada dato lleva su fuente, su fecha y el enlace al documento oficial. Cuando un dato no está disponible, se dice de forma explícita; nunca se inventa.
```

Kicker (only net-new string):
```
<p className="font-mono text-[11px] font-medium uppercase tracking-[0.08em] text-muted-foreground">OBSERVATORIO DEL CONGRESO</p>
```

Container idiom (mirror layout.tsx footer): `max-w-[1120px] mx-auto px-4 md:px-8`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Migrate page.test.tsx — Contract-2 nav→grid; add bento assertions; LOCKED strings byte-identical</name>
  <files>app/app/page.test.tsx</files>
  <read_first>
    - app/app/page.test.tsx lines 1-52 (mocks: next/navigation, actualidad-module→null, next/link→<a>) and lines 142-207 (Contract-2 nav-based tests that BREAK)
    - 77-UI-SPEC.md §Testing Contract + 77-PATTERNS.md §Test Migration Notes
  </read_first>
  <action>
    Rewrite `app/app/page.test.tsx` (RTL, jsdom) keeping the existing mocks (next/navigation pushMock, actualidad-module→null, next/link→<a>) reusable. Because the entry cards become BentoTile links inside a grid (no `<nav aria-label="Secciones del sitio">`), the Contract-2 block (lines 150-207) MUST be replaced:
    - **Hero (keep byte-identical):** h1 line 1 + `<em>` cursiva `Con la fuente a la vista.` (tag EM, `italic`, `text-accent-product`); CTA button `Buscar proyectos` (bg-accent-product); 4 pills verbatim (boletín `14309-04` font-mono); trust line verbatim; pill-click navigates to /buscar?q=… (existing cases 55-139 — preserve).
    - **Kicker (NEW):** assert text `OBSERVATORIO DEL CONGRESO` present.
    - **Accent tile (NEW):** a link with `href="/sobre"`; heading `¿Cómo leer esto?`; body text = the /sobre formula (assert the `nunca se inventa` phrase present); CTA `Ver metodología →` present; NEGATIVE-match: the mockup string `correlaciones` / `irregularidades` NOT present (keep the BANNED_VOCAB regex, extend to include those).
    - **Entry tiles (MIGRATE):** exactly 3 links with hrefs set `{/buscar, /parlamentarios, /agenda}` and verbatim titles + the 3 value strings; the `→` glyph present as `aria-hidden` with `pl-1` (not a whitespace text node). Query by link role across the document (no nav scope).
    - **Retained (NEW):** with ActualidadModule mocked to null, assert the composition renders without it throwing; assert `dynamic` is exported as `force-dynamic` by importing the module namespace (`import * as HomeModule from "./page"; expect(HomeModule.dynamic).toBe("force-dynamic")`).
    - Keep the "no fabricated stats" negative-match. Drop the `<nav>`-scoped queries and the "no heading" assertion (the grid no longer forbids headings — the accent tile has an h2).
    NOTE: this test will RED against the current page.tsx (kicker/accent tile absent) — that is expected; it goes GREEN after Task 2.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run page 2>&1 | Select-String -Pattern "OBSERVATORIO|accent|/sobre" -SimpleMatch</automated>
  </verify>
  <done>page.test.tsx migrated: hero LOCKED strings byte-identical; kicker + accent tile (/sobre, ¿Cómo leer esto?, Ver metodología →, no correlaciones) + 3 grid links + force-dynamic export asserted; nav-based Contract-2 removed. Test compiles; RED on current page.tsx (goes GREEN in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx to the bento composition (hero span-4 + accent span-2 + 3 entry span-2)</name>
  <files>app/app/page.tsx</files>
  <read_first>
    - app/app/page.tsx (full — LOCKED constants lines 31-64, force-dynamic 6-11, hero 66-103, entry cards 111-132, ActualidadModule 139)
    - app/components/bento/bento-grid.tsx + app/components/bento/bento-tile.tsx (exact export names + span/variant/asChild API)
    - app/components/brand-icon.tsx (BrandIcon props: size, color, className)
    - app/app/layout.tsx line ~50 (footer container idiom to mirror)
  </read_first>
  <action>
    Rewrite `app/app/page.tsx` as a Server Component. Preserve VERBATIM: the `force-dynamic` export + its comment; the `EXAMPLE_CHIPS` and `ENTRY_CARDS` constants; the `SearchBox`/`ActualidadModule` imports. Add imports for `BentoGrid`, `BentoTile`, `BrandIcon`, and keep `next/link`.
    Structure inside `<main className="flex-1">`:
    1. New container `<div className="max-w-[1120px] mx-auto px-4 md:px-8 ...">` (add a top padding matching current home rhythm, e.g. `pt-10 md:pt-14` — discretion within UI-SPEC bounds) wrapping `<BentoGrid>`.
    2. **Hero tile:** `<BentoTile variant="default" span={4} asChild><section className="p-8 flex flex-col justify-center">` containing IN DOM ORDER: kicker (`font-mono text-[11px] font-medium uppercase tracking-[0.08em] text-muted-foreground` → `OBSERVATORIO DEL CONGRESO`) → h1 (`text-4xl font-semibold leading-tight md:text-5xl`) with the LOCKED line + `<em className="mt-2 block italic text-accent-product">Con la fuente a la vista.</em>` → the LOCKED subtitle `<p>` → `<SearchBox autoFocus variant="hero" exampleChips={EXAMPLE_CHIPS} />` → trust line `<p className="mt-8 text-sm text-muted-foreground">` LOCKED. Remove the old inline "¿Cómo leer esto?" onboarding link (its /sobre destination migrates to the accent tile CTA).
    3. **Accent tile:** `<BentoTile variant="accent" span={2} asChild><Link href="/sobre" className="p-6 flex flex-col justify-between">` containing: `<BrandIcon color="currentColor" size={30} />` → a div with `<h2 className="text-xl font-semibold text-accent-product-foreground">¿Cómo leer esto?</h2>` + `<p className="mt-2 text-sm text-accent-product-foreground">` with the /sobre formula body → `<span className="mt-6 text-sm font-semibold text-accent-product-foreground">Ver metodología <span aria-hidden="true" className="pl-1">→</span></span>`. Exactly ONE child under asChild (the Link).
    4. **Entry tiles:** map `ENTRY_CARDS` → each `<BentoTile variant="default" span={2} asChild><Link href={card.href} className="p-6 flex flex-col ...">`. Top row (`flex items-center justify-between`): inline single-diamond SVG marker (petróleo via `className="text-accent-product"` + `stroke/fill="currentColor"`, `aria-hidden`, distinct from the two-diamond BrandIcon) left + `<span aria-hidden="true" className="pl-1 text-muted-foreground">→</span>` right. Then `<span className="text-lg font-semibold ...">{card.title}</span>` + `<p className="mt-1 text-sm text-muted-foreground">{card.value}</p>`. Exactly ONE Link child per tile.
    5. Close `</BentoGrid>` and the container, then mount `<ActualidadModule />` BELOW (still inside `<main>`).
    Constraints: no `#` hex (diamond uses currentColor); no bare `[--token]`; `→` never a text node; DOM order = collapse order (hero → accent → entry1 → entry2 → entry3 → ActualidadModule).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run page</automated>
  </verify>
  <done>page.tsx renders BentoGrid with 5 BentoTiles (spans 4/2/2/2/2) inside max-w-[1120px] container; kicker + LOCKED hero + accent tile (/sobre, formula body, Ver metodología →, BrandIcon currentColor) + 3 entry tiles (diamond + → glyph); force-dynamic + ActualidadModule preserved; page.test.tsx GREEN; no hex/bare-var/text-node arrow.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + tsc + anti-insinuación gate</name>
  <files>(no source changes — verification + fixups only)</files>
  <read_first>
    - 77-VALIDATION.md §Sampling Rate (phase gate: full suite + anti-insinuación green)
  </read_first>
  <action>
    Run the full app suite + type gate + anti-insinuación suite. Fix any regressions introduced by the rewrite (e.g. a stale import, a leftover assertion in another test that referenced the old home nav). Do NOT alter LOCKED strings or introduce mockup copy while fixing. Confirm the build treats `/` as dynamic (force-dynamic preserved) — if any test asserts the old nav landmark elsewhere, migrate it consistently.
    Record final counts in the SUMMARY (app suite total vs ~846 base, tsc clean, anti-insinuación green).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run; pnpm --filter ./app exec tsc --noEmit; pnpm --filter ./app test -- --run anti-insinuacion</automated>
  </verify>
  <done>Full app suite green (~846 base + adjustments); tsc --noEmit clean; anti-insinuación suite green; no mockup strings and no LOCKED-string drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only rewrite. No new data flow, auth, or input handling. The SearchBox query→/buscar path is unchanged; /buscar already trims + caps the query server-side and keeps the embed key server-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | Repudiation / defamation (project risk #1) | Accent-tile body copy | mitigate | Adopt the linter-clean /sobre "El principio" formula; BAN the mockup "correlaciones… irregularidades" string. Enforced by the page.test.tsx negative-match (Task 1) + anti-insinuación suite (Task 3). Home is not yet in the linter's scanned surfaces (Phase 80) → self-imposed contract, tested here. |
| T-77-04 | Tampering / info-disclosure | Reflected query param via SearchBox | accept | Not introduced this phase; React auto-escapes; no dangerouslySetInnerHTML added; nav path unchanged. |
| T-77-05 | Denial of service (stale/frozen home) | force-dynamic drop during rewrite | mitigate | force-dynamic export preserved and asserted in page.test.tsx (Task 1); build must show `/` as ƒ (dynamic), not ○ (static). |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (presentation-only; RESEARCH §Package Legitimacy Audit: Not applicable). No slopcheck surface. |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` full suite green.
- `pnpm --filter ./app exec tsc --noEmit` clean.
- `pnpm --filter ./app test -- --run anti-insinuacion` green.
- Manual grep: no `#` hex and no bare `[--token]` in page.tsx; every LOCKED string byte-identical; no mockup copy present.
- Layout/collapse/AA-contrast are the Phase 79/81 BrowserOS visual gate (jsdom cannot see layout) — out of scope here.
</verification>

<success_criteria>
1. Home renders BentoGrid: hero span-4 (kicker + LOCKED h1/cursiva/subtitle/trust + restyled hero SearchBox + LOCKED pills) inside max-w-[1120px] container.
2. Accent tile span-2 → /sobre, ¿Cómo leer esto?, /sobre-formula body (no mockup string), Ver metodología →, BrandIcon currentColor, hover + foreground token from 77-01.
3. 3 entry tiles span-2 with LOCKED copy/hrefs, diamond marker + → glyph.
4. force-dynamic conserved; SearchBox único island; ActualidadModule retained below grid.
5. page.test.tsx migrated + green; full suite + tsc + anti-insinuación green.
</success_criteria>

<output>
Create `.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-02-SUMMARY.md` when done.
</output>
