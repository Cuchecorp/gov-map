---
phase: 77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/globals.css
  - app/tailwind.config.ts
  - app/app/globals.test.ts
  - app/components/bento/bento-tile.tsx
  - app/components/bento/bento-tile.test.tsx
  - app/components/search-box.tsx
  - app/components/search-box.test.tsx
autonomous: true
requirements: [BENTO-02]

must_haves:
  truths:
    - "The accent BentoTile variant renders legible foreground text (text-accent-product-foreground) and darkens on hover, resolving 76-UI-REVIEW warnings #1 and #2"
    - "The accent tile fill stays petróleo (183 38% 26%) in both light and dark (does not lighten to the dark mid-teal), so white-equivalent text keeps ≥7:1 contrast"
    - "The hero SearchBox input and button are 52px tall with rounded-[var(--radius-control)]; the /buscar (default) bar is untouched (still h-12)"
    - "The utility text-accent-product-foreground actually generates (registered in tailwind.config.ts colors, not only in @theme inline)"
  artifacts:
    - path: "app/app/globals.css"
      provides: "--accent-product-foreground triple in :root AND .dark (dark-stable) + --bento-accent-fill triple in :root AND .dark (dark-stable petróleo)"
      contains: "--accent-product-foreground: 183 30% 96%"
    - path: "app/tailwind.config.ts"
      provides: "accent-product-foreground + bento-accent-fill entries in colors map (hsl(var(...)) idiom mirroring accent-product line 46)"
      contains: "accent-product-foreground"
    - path: "app/components/bento/bento-tile.tsx"
      provides: "accent cva variant with dark-stable fill + foreground token + hover"
      contains: "text-accent-product-foreground"
    - path: "app/components/search-box.tsx"
      provides: "isHero-branch-only restyle: input h-[52px] + rounded-[var(--radius-control)]; button h-[52px] + same radius"
      contains: "rounded-[var(--radius-control)]"
  key_links:
    - from: "app/components/bento/bento-tile.tsx accent variant"
      to: "--accent-product-foreground / --bento-accent-fill tokens"
      via: "Tailwind utilities generated from tailwind.config.ts colors map"
      pattern: "text-accent-product-foreground"
    - from: "app/components/search-box.tsx isHero branch"
      to: "--radius-control token"
      via: "rounded-[var(--radius-control)] arbitrary util (NEVER [--radius-control])"
      pattern: "rounded-\\[var\\(--radius-control\\)\\]"
---

<objective>
Land the foundation edits that Plan 02 (page.tsx bento rewrite) consumes: register the accent foreground + dark-stable accent-fill tokens (BOTH globals.css and tailwind.config.ts), fix the `accent` BentoTile variant (foreground token + hover + dark-stable fill — resolves 76-UI-REVIEW warnings #1/#2/#3-fill), and restyle ONLY the hero branch of SearchBox (52px + `--radius-control`) while keeping `/buscar` isolated.

Purpose: These are the contracts the home rewrite depends on. Interface-first ordering — tokens and variants exist before the page mounts them.
Output: 4 source edits + 3 test extensions. Zero new packages. 100% presentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-RESEARCH.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-PATTERNS.md
@.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-UI-SPEC.md

<critical_constraints>
- **Tailwind 4 (CR-01):** arbitrary-var syntax is ALWAYS `rounded-[var(--radius-control)]`, NEVER `rounded-[--radius-control]` (bare compiles to invalid CSS). The bento-tile source-scan tests already enforce this.
- **Zero new hex:** no `#` literal enters `bento-tile.tsx` or `search-box.tsx`. Every color by registered token/utility.
- **Token registration idiom (RESEARCH Pitfall 2 — highest risk):** the new tokens are BARE HSL triples in globals.css `:root`+`.dark`, consumed via `hsl(var(...))` in `tailwind.config.ts` `colors` — MIRROR the existing `"accent-product": "hsl(var(--accent-product))"` line (tailwind.config.ts:46). Do NOT use the `@theme inline` block (that idiom is only for civic tokens whose source is already a full `hsl()`; double-`hsl()` = invalid color, gotcha 54-04).
- **SearchBox isolation (RESEARCH Pitfall 3):** touch ONLY the truthy (`isHero`) side of each ternary. `/buscar` uses `variant="default"` and its `h-12` must survive. The hero button must KEEP `bg-accent-product`, `font-semibold`, `text-background`, `hover:bg-accent-product/90`, label `Buscar proyectos` (search-box.test.tsx:40-47 asserts all four).
- **jsdom gotcha (F50/45-01):** source-scan tests use `process.cwd()` + `readFileSync`, NEVER `new URL(import.meta.url)`.
</critical_constraints>

<interfaces>
Current accent variant (app/components/bento/bento-tile.tsx:28-29 — verified HEAD):
```
default: "bg-card border border-border hover:border-accent-product",
accent: "bg-accent-product text-primary-foreground",
```

Existing token registration idiom (app/tailwind.config.ts:46):
```
"accent-product": "hsl(var(--accent-product))",
```

Existing globals.css token triples (:root line 24, .dark line 53):
```
:root { --accent-product: 183 38% 26%; ... --radius-control: 11px; }
.dark { --accent-product: 183 34% 46%; }   /* lightens → white drops to 3.30:1 FAIL */
```

Current SearchBox hero input (search-box.tsx:115 — shared string, must be split):
```
className="h-12 pl-9 text-base"
```
Current SearchBox hero button (search-box.tsx:119-128 — already an isHero ternary):
```
isHero ? "h-12 whitespace-nowrap bg-accent-product px-6 font-semibold text-background hover:bg-accent-product/90" : "h-12 bg-accent-product text-background hover:bg-accent-product/90"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register accent-foreground + dark-stable accent-fill tokens (globals.css + tailwind.config.ts)</name>
  <files>app/app/globals.css, app/tailwind.config.ts, app/app/globals.test.ts</files>
  <read_first>
    - app/app/globals.css lines 17-59 (:root and .dark token blocks — exact insertion points)
    - app/app/globals.css lines 70-78 (@theme inline block — DO NOT add these tokens here)
    - app/tailwind.config.ts lines 17-55 (colors map; mirror line 46)
    - app/app/globals.test.ts (existing source-scan idiom: process.cwd(), toContain, stripComments)
  </read_first>
  <behavior>
    - Test (globals.test.ts, extended): SRC toContain "--accent-product-foreground: 183 30% 96%" — present in the file (survives stripComments as a token line).
    - Test: SRC toContain "--bento-accent-fill: 183 38% 26%".
    - Test: assert the token appears in BOTH :root and .dark (e.g. count occurrences of "--accent-product-foreground:" ≥ 2, OR two separate scoped assertions). Same for --bento-accent-fill.
    - Existing 4 tests in globals.test.ts stay green.
  </behavior>
  <action>
    In `app/app/globals.css`, add two BARE HSL triples to BOTH `:root` (after line 35, alongside the radius tokens) AND `.dark` (after line 58) with IDENTICAL values in both scopes (dark-stable):
    - `--accent-product-foreground: 183 30% 96%;` (≈ #EAF2F1, full alpha, ≥7:1 on petróleo — per UI-SPEC contrast table).
    - `--bento-accent-fill: 183 38% 26%;` (petróleo, pinned dark-stable so the accent tile fill does NOT lighten to the .dark mid-teal `183 34% 46%` where white falls to 3.30:1 FAIL).
    Add a short comment on each explaining the dark-stability rationale (per UI-SPEC §Accent Foreground Fix / Pattern 3 option a).
    In `app/tailwind.config.ts` `colors` map (after line 46, mirroring the `accent-product` line EXACTLY), add:
    - `"accent-product-foreground": "hsl(var(--accent-product-foreground))",`
    - `"bento-accent-fill": "hsl(var(--bento-accent-fill))",`
    Do NOT touch the `@theme inline` block in globals.css — these tokens are bare triples wrapped by `hsl(var(...))` in the config (Pattern 2; @theme inline is for civic full-`hsl()` tokens only).
    Extend `app/app/globals.test.ts` with the assertions in <behavior> using the existing `SRC`/`toContain` idiom (no import.meta.url).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run globals</automated>
  </verify>
  <done>--accent-product-foreground (183 30% 96%) and --bento-accent-fill (183 38% 26%) present in :root AND .dark of globals.css with identical values; both registered in tailwind.config.ts colors via hsl(var(...)); globals.test.ts green (old 4 + new assertions); @theme inline untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix accent BentoTile variant — foreground token + hover + dark-stable fill</name>
  <files>app/components/bento/bento-tile.tsx, app/components/bento/bento-tile.test.tsx</files>
  <read_first>
    - app/components/bento/bento-tile.tsx lines 23-42 (bentoTileVariants; accent = line 29)
    - app/components/bento/bento-tile.test.tsx (existing class-presence + zero-hex + no-bare-var source-scan idiom at ~lines 33-88)
  </read_first>
  <behavior>
    - Test: accent variant className contains "bg-bento-accent-fill" (dark-stable fill, NOT bg-accent-product which lightens in dark).
    - Test: accent variant className contains "text-accent-product-foreground".
    - Test: accent variant className contains "hover:bg-bento-accent-fill/90" (derived opacity hover — resolves warning #2; use the same fill token so hover stays dark-stable).
    - Test: accent variant className does NOT contain "text-primary-foreground" (the old inverting foreground is gone).
    - Existing source-scan guards stay green: no `#` hex in the file; no bare `[--token]` (only `[var(--token)]`); default variant + min-h-11 + rounded-[var(--radius-tile)] + focus-visible ring UNCHANGED.
  </behavior>
  <action>
    In `app/components/bento/bento-tile.tsx`, change ONLY the `accent` string (line 29) from
    `accent: "bg-accent-product text-primary-foreground",`
    to
    `accent: "bg-bento-accent-fill text-accent-product-foreground hover:bg-bento-accent-fill/90",`
    Rationale (put a one-line comment): dark-stable fill (`--bento-accent-fill` fixed at petróleo in both themes) keeps white-equivalent text ≥7:1 (the `.dark` `--accent-product` mid-teal drops to 3.30:1 FAIL); `text-accent-product-foreground` replaces the wrongly-inverting `text-primary-foreground` (warning #1); `hover:bg-bento-accent-fill/90` gives the link-tile a hover affordance (warning #2, derived opacity, no new hex).
    Do NOT touch the `default` variant, the base string (rounded/focus/min-h), the span map, or the component body. Do NOT introduce any `#` hex or bare `[--token]`.
    Extend `bento-tile.test.tsx` with the class-presence assertions in <behavior> (render `<BentoTile variant="accent">` and assert `.className.toContain(...)`; NEVER getComputedStyle). Keep the zero-hex / no-bare-var source-scan guards passing.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run components/bento</automated>
  </verify>
  <done>accent variant = "bg-bento-accent-fill text-accent-product-foreground hover:bg-bento-accent-fill/90"; text-primary-foreground removed; default variant + base + spans unchanged; no hex, no bare-var; bento-tile.test.tsx green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restyle SearchBox hero branch (52px + radius-control), isolate /buscar</name>
  <files>app/components/search-box.tsx, app/components/search-box.test.tsx</files>
  <read_first>
    - app/components/search-box.tsx lines 104-128 (Input className line 115 + Button ternary lines 119-128)
    - app/components/search-box.test.tsx lines 25-48 (hero button assertions that must survive; default-branch assertions that must NOT change)
  </read_first>
  <behavior>
    - Test (hero): render `<SearchBox variant="hero" />`; the input (role searchbox) className contains "h-[52px]" and "rounded-[var(--radius-control)]".
    - Test (hero): the submit button className contains "h-[52px]" and "rounded-[var(--radius-control)]" AND still contains "bg-accent-product", "font-semibold", "text-background", "hover:bg-accent-product/90"; label still "Buscar proyectos".
    - Test (default isolation): render `<SearchBox />`; the input className contains "h-12" and does NOT contain "h-[52px]" or "rounded-[var(--radius-control)]"; the button still "h-12" + label "Buscar" + no font-semibold (existing search-box.test.tsx cases 25-38 stay green byte-identical).
    - Empty-submit guard preserved: submitting empty does not call router.push.
  </behavior>
  <action>
    In `app/components/search-box.tsx`:
    - Split the shared Input className (line 115 `"h-12 pl-9 text-base"`) into an `isHero` ternary so hero gets `"h-[52px] rounded-[var(--radius-control)] pl-9 text-base"` and default keeps `"h-12 pl-9 text-base"`. Use `[var(--radius-control)]`, NEVER `[--radius-control]`.
    - In the Button ternary (lines 121-125), change the hero (truthy) string's `h-12` → `h-[52px]` and ADD `rounded-[var(--radius-control)]`. KEEP `whitespace-nowrap bg-accent-product px-6 font-semibold text-background hover:bg-accent-product/90` and label `Buscar proyectos`. Leave the default (falsy) string EXACTLY `"h-12 bg-accent-product text-background hover:bg-accent-product/90"`.
    - Do NOT touch pills (lines 138-144 — already `min-h-11 rounded-full hover:border-accent-product/50`, UI-SPEC says unchanged), the form GET fallback, the empty-submit guard, the `"use client"` directive, or the placeholder strings.
    Extend `search-box.test.tsx` with the hero input `h-[52px]` + `rounded-[var(--radius-control)]` assertions and the default-input isolation assertion. Keep the 3 existing cases (25-48) passing.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run search-box</automated>
  </verify>
  <done>Hero input+button are h-[52px] with rounded-[var(--radius-control)]; default /buscar input stays h-12 with no radius-control; hero button keeps petróleo+semibold+label; pills/form/guard/island untouched; search-box.test.tsx green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits CSS tokens, a cva variant string, and a client-island className branch. No new data flow, no auth, no input handling. The SearchBox query→/buscar path is unchanged (query already trimmed + capped server-side in /buscar). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information Disclosure | SearchBox island | accept | No new client fetch/secret; SearchBox still never calls Gemini/pgvector (server-only in /buscar). Restyle touches className only. |
| T-77-02 | Tampering | globals.css / tailwind.config token registration | mitigate | Wrong idiom (double-hsl or @theme inline) yields a silent no-op utility, not a security issue — caught by the class-presence + source-scan tests (Task 1/2 behavior). |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (presentation-only; RESEARCH §Package Legitimacy Audit: Not applicable). No slopcheck surface. |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run globals search-box components/bento` all green.
- `pnpm --filter ./app exec tsc --noEmit` clean.
- Grep guard: no `[--` bare arbitrary-var and no `#` hex in the edited bento-tile.tsx / search-box.tsx accent+hero strings.
</verification>

<success_criteria>
- accent-product-foreground + bento-accent-fill registered in BOTH globals.css (:root + .dark) AND tailwind.config.ts colors.
- accent BentoTile variant uses dark-stable fill + foreground token + hover; text-primary-foreground gone.
- SearchBox hero input+button 52px + radius-control; /buscar default branch byte-identical.
- Targeted suites (globals, search-box, components/bento) green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/77-bento-home-superior-hero-tile-acento-tarjetas-de-entrada/77-01-SUMMARY.md` when done.
</output>
