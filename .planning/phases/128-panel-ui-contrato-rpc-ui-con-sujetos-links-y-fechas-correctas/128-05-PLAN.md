---
phase: 128-panel-ui
plan: 05
type: execute
wave: 2
depends_on: ["128-01"]
files_modified:
  - app/components/panel-tile-votaciones.tsx
  - app/components/panel-tile-votaciones.test.tsx
  - app/components/actualidad-module.tsx
autonomous: true
requirements: [PANEL-02, PANEL-04, PANEL-05]

must_haves:
  truths:
    - "Dos votaciones del mismo boletín producen DOS líneas, jamás una agregada"
    - "Una votación de Senado con resultado NULL dice 'resultado no informado por la fuente' y jamás 'Aprobado'/'Rechazado'"
    - "La grafía de cámara se normaliza a 'Cámara de Diputados' / 'Senado' (la tabla emite minúsculas)"
    - "La lectura de votaciones es determinista (order fecha desc, id desc) y bounded"
  artifacts:
    - path: "app/components/panel-tile-votaciones.tsx"
      provides: "Tile 5 — Votaciones recientes (L4), wrapper async + vista pura"
      exports: ["PanelTileVotaciones", "PanelTileVotacionesView"]
  key_links:
    - from: "app/components/panel-tile-votaciones.tsx"
      to: "public.votacion"
      via: ".from('votacion') lectura directa NO-PII"
      pattern: "from\\(\"votacion\"\\)"
    - from: "app/components/panel-tile-votaciones.tsx"
      to: "app/lib/links-internos.ts"
      via: "hrefProyecto(b,'votaciones')"
      pattern: "hrefProyecto"
---

<objective>
Tile 5 — Votaciones recientes (L4), VISIBLE por O-2 (VSIM ON + sign-off legal 2026-07-30). Es el
único tile que NO lee la RPC 0066: lee `public.votacion` directamente (tabla NO-PII, precedente
vivo `actualidad-module.tsx:244`), cero RPC nueva, cero allowlist.

Purpose: es la respuesta a la pregunta de mayor demanda periodística, y el carril de copy más
minado del sitio — se construye aislado para que su verificación sea nítida.
Output: `panel-tile-votaciones.tsx` (wrapper async + vista pura) + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-CONTEXT.md

<read_first>
- `128-RESEARCH.md` §"Dónde vive L4" — columnas reales de `votacion`, nulls por cámara, filas de ejemplo
- `.planning/spikes/v13.0-editorial-portada.md` §3.2 TILE 5 — la FORMA del copy y las 4 reglas duras
- `app/components/actualidad-module.tsx:239-285` — patrón async/View + `leerTitulos` (:517)
- `app/lib/voto-presentacion.ts` — `VOTO_PRESENTACION`, `LEYENDA_ANTI_INSINUACION`
- `app/lib/format.ts` — `fechaCorta`, `conteoVotacion`
- `app/lib/lockdown-guard.test.ts:150-165` — `PII_TABLES` (confirma que `votacion` NO es PII)
</read_first>

<interfaces>
Ya creados por 128-01 — importar:
```ts
hrefProyecto(boletin, "votaciones"): string          // lib/links-internos.ts
<PanelItemProyecto boletin titulo enCorpus ancla detalle />   // components/panel-item-proyecto.tsx
```

Columnas REALES de `public.votacion` (⚠ no existen `si`/`no`/`abstencion`):
`id (text, NOT NULL)` · `boletin (text, NOT NULL)` · `fecha (timestamptz, null)` · `etapa` · `tipo` ·
`quorum` · `resultado (null en el 100 % de Senado)` · `total_si` · `total_no` · `total_abstencion` ·
`total_pareo` (integer NOT NULL) · `camara (text, "diputados"|"senado" EN MINÚSCULA)` · `origen` ·
`fecha_captura` · `enlace`.

Firma del tile:
```ts
export interface VotacionPanelItem {
  id: string; boletin: string; titulo: string | null;
  fecha: Date; camara: string;             // grafía YA normalizada por el wrapper
  resultado: string | null;                // null = no informado por la fuente
  si: number; no: number; abstencion: number;
}
export function PanelTileVotacionesView(p: { items: VotacionPanelItem[]; fechaFuente: Date | null }): JSX.Element;
export async function PanelTileVotaciones(p?: { maxItems?: number }): Promise<JSX.Element>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exportar leerTitulos para reuso del lookup NO-PII</name>
  <files>app/components/actualidad-module.tsx</files>
  <action>
Añadir `export` a la función `leerTitulos` (`actualidad-module.tsx:517`) para que el tile L4 la
reuse en vez de escribir un segundo lookup de títulos. Cambio de una palabra: NO tocar su cuerpo,
su firma, su manejo de errores (#34: throw, jamás `?? []`) ni ninguno de sus dos call-sites
existentes. Añadir una línea de JSDoc indicando que es reusada por `panel-tile-votaciones.tsx`
(lookup NO-PII sobre `proyecto`; jamás toca `parlamentario`).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/actualidad-module.test.tsx lib/lockdown-guard.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>`leerTitulos` está exportada; los tests de actualidad-module y el lockdown-guard siguen verdes con conteo > 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PanelTileVotacionesView — el copy del carril más minado</name>
  <files>app/components/panel-tile-votaciones.tsx, app/components/panel-tile-votaciones.test.tsx</files>
  <behavior>
    - Título del tile: "Votaciones recientes"
    - Dos items con el MISMO boletín (mismo día, distinta `id`) → DOS `<li>` renderizados; el DOM no agrega ni suma sus votos
    - Item con `resultado:"Aprobado"` (diputados) → detalle `Votación en la Cámara de Diputados el {d}: Aprobado — 80 a favor, 48 en contra, 2 abstenciones`
    - Item con `resultado:null` (senado) → detalle con los números + `resultado no informado por la fuente`; el `<li>` NO contiene "Aprobado" ni "Rechazado" (cero fuerte con control positivo: los números SÍ aparecen)
    - `camara:"diputados"` normalizado ANTES de llegar a la vista → el DOM muestra `Cámara de Diputados`; el DOM NO contiene la cadena `diputados` en minúscula suelta
    - Cada item: `<a href="/proyecto/{b}#votaciones">`
    - `titulo:null` → boletín solo, sin fabricar título
    - `items:[]` → vacío HONESTO con causa ("sin votaciones fechadas en las fuentes consultadas al {d}"), jamás un "0" mudo ni el tile en blanco
    - Footer: `Fuente: Votaciones · según fuente al {fechaCorta(fechaFuente)}`; cero "datos al"; `fecha_captura` nunca en el DOM
    - Cero vocabulario prohibido del carril voto: el DOM no contiene "votan juntos", "votan igual", "aliados", "tasa de coincidencia", "disciplina", "señal" (salvo dentro de la leyenda registrada, que los NIEGA)
  </behavior>
  <action>
Crear `app/components/panel-tile-votaciones.tsx` con la vista pura `PanelTileVotacionesView`.
RSC puro, nombre de archivo congelado, clases arbitrarias solo de la whitelist sancionada.

Copy: el vocabulario de voto sale de `lib/voto-presentacion.ts`; renderizar `LEYENDA_ANTI_INSINUACION`
VERBATIM al pie del tile (ya está registrada en `NEGACIONES_LOCKED` — importarla, jamás re-tipearla;
si se re-tipea, el guard se auto-caza sobre la superficie, Pitfall 1).

Reglas duras (D-04, no negociables):
- UNA LÍNEA POR VOTACIÓN. Jamás agregar por boletín (`18384-08` tiene 6 el mismo día en Senado).
- `resultado` null ⇒ literal EXACTO `resultado no informado por la fuente`. Jamás inferir el
  resultado de los números (`8 a favor, 26 en contra` NO autoriza a decir "Rechazado").
- Conteos verbatim de `total_si`/`total_no`/`total_abstencion` — cero cálculo de tasas, porcentajes,
  totales agregados o comparaciones entre votaciones.
- Cero ordenamiento por resultado o por magnitud del voto: el orden es cronológico descendente.

Fechas (D-05): `votacion.fecha` es timestamptz real ⇒ `fechaCorta` (NO `fechaCivilCorta`, que es
para las date-only del jsonb). Footer con `según fuente al` (idiom registrado).

Test primero, con los 5 fixtures reales del research (§"Filas reales del 2026-07-22"), incluidas
las dos de Senado con `resultado` NULL y las dos de `18384-08`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-votaciones.test.tsx lib/anti-insinuacion-guard.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; el carril de voto del linter verde sobre la superficie nueva.</done>
</task>

<task type="auto">
  <name>Task 3: PanelTileVotaciones — wrapper async, lectura determinista y bounded</name>
  <files>app/components/panel-tile-votaciones.tsx, app/components/panel-tile-votaciones.test.tsx</files>
  <action>
Añadir el wrapper `export async function PanelTileVotaciones({ maxItems = 4 })` en el mismo archivo,
siguiendo el patrón async/View de `actualidad-module.tsx:239`:

```
sb.from("votacion")
  .select("id, boletin, fecha, resultado, total_si, total_no, total_abstencion, camara")
  .order("fecha", { ascending: false })
  .order("id",    { ascending: false })      // desempate OBLIGATORIO — `order by fecha desc` solo
  .limit(maxItems)                            // no es determinista (gotcha B-01)
```

- `error` real ⇒ `throw` con mensaje trazable (#34). JAMÁS `?? []` que fabrique "sin votaciones".
  `[]` es SOLO el path legítimo de 0 filas → vacío honesto con causa.
- Filas con `fecha` null se descartan (no se fabrica un hecho fechado), igual que `VotadoEstaSemana`.
- Normalización de grafía: `votacion.camara` viene `"diputados"`/`"senado"` en minúscula (esa tabla
  NO pasó por el fix del materializador). Escribir una función local pequeña
  `grafiaCamaraCiudadana(raw: string): string` que devuelva `Cámara de Diputados` / `Senado` y, ante
  un literal desconocido, devuelva el literal verbatim (fallback honesto, jamás inventar cámara).
  Exportarla y testearla con los 3 casos.
- Títulos: `leerTitulos(sb, boletines)` (exportada en el Task 1). Título ausente → `null`.
- `fechaFuente` = la `fecha` máxima de las filas leídas (frescura de la fuente, D-05). JAMÁS
  `fecha_captura` — esa columna no se selecciona siquiera.
- Sobre el gate VSIM (assumption A1 del research, decisión explícita del planner): el tile NO se
  envuelve en `vsimPublicEnabled()`. `vsim-gate` gatea la SIMILITUD de votación (`/comparar`), no el
  hecho de votación, que ya es público sin gate en `/proyecto#votaciones`; O-2 lo ratifica VISIBLE.
  Documentar esta decisión en el docblock del componente. Ningún flag se toca.

PostgREST cap 1k no aplica (se piden 4), pero no subir el `limit` por encima de una decena sin
re-medir.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-votaciones.test.tsx lib/lockdown-guard.test.ts lib/vsim-antiflip-guard.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>El wrapper compila y ordena por `fecha desc, id desc`; `grafiaCamaraCiudadana` probada con `diputados`/`senado`/desconocido; lockdown y antiflip verdes; cero flags tocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public.votacion` → portada pública | el cliente público es `service_role` y bypassa RLS; la protección real es `lockdown-guard` |
| hechos de voto → lectura ciudadana | el carril con mayor riesgo de insinuación de intención del sitio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-15 | Repudiation | resultado NULL de Senado | mitigate | literal fijo `resultado no informado por la fuente`; test de cero "Aprobado"/"Rechazado" con control positivo |
| T-128-16 | Repudiation | agregación por boletín | mitigate | una línea por votación; test con las 2 filas de `18384-08` |
| T-128-17 | Information Disclosure | acceso a tabla PII | mitigate | solo `.from("votacion")` y `leerTitulos` sobre `proyecto`, ambas NO-PII; `lockdown-guard` en CI |
| T-128-18 | Tampering | orden no determinista de la lectura | mitigate | desempate `.order("id")`; documentado como gotcha B-01 |
| T-128-19 | Elevation of Privilege | flip de flag por agente | accept | ningún flag se toca; `vsim-antiflip-guard` vivo lo verifica en CI |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas |
</threat_model>

<verification>
```bash
cd app && pnpm vitest run components/panel-tile-votaciones.test.tsx
cd app && pnpm guards        # lockdown + antiflip + anti-insinuación + bento
```
Verificar el CONTEO de tests ejecutados.
</verification>

<success_criteria>
- Dos votaciones del mismo boletín → dos líneas.
- `resultado:null` → "resultado no informado por la fuente", sin "Aprobado"/"Rechazado" en ese `<li>`.
- Grafía normalizada a `Cámara de Diputados` / `Senado` con fallback verbatim.
- Lectura bounded y determinista (`fecha desc, id desc`, limit 4); cero RPC nueva, cero allowlist, cero flags tocados.
- `pnpm guards` verde.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-05-SUMMARY.md` when done
</output>
