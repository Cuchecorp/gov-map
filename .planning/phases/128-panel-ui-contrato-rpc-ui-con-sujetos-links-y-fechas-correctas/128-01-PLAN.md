---
phase: 128-panel-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/links-internos.ts
  - app/lib/links-internos.test.ts
  - app/components/panel-item-proyecto.tsx
  - app/components/panel-item-proyecto.test.tsx
autonomous: true
requirements: [PANEL-02, PANEL-05]

must_haves:
  truths:
    - "El idiom de fecha de urgencia usado por el copy está registrado en IDIOMS_APROBADOS antes de que exista copy que lo use"
    - "Un boletín del corpus produce un href /proyecto/{b}#estado con el ancla real"
    - "Un link de agenda con semana produce /agenda?semana=YYYY-Www#ancla (query ANTES del fragmento)"
    - "Un ítem en_corpus:false no emite ningún href interno; emite enlace externo con rel=noopener noreferrer"
  artifacts:
    - path: "app/lib/links-internos.ts"
      provides: "hrefProyecto / hrefAgenda / semanaIsoDeFecha"
      exports: ["hrefProyecto", "hrefAgenda", "semanaIsoDeFecha"]
    - path: "app/components/panel-item-proyecto.tsx"
      provides: "ítem nombrado reusable con guard en_corpus"
      exports: ["PanelItemProyecto"]
  key_links:
    - from: "app/components/panel-item-proyecto.tsx"
      to: "app/lib/links-internos.ts"
      via: "import hrefProyecto"
      pattern: "hrefProyecto"
    - from: "app/lib/links-internos.ts"
      to: "app/lib/week-utils.ts"
      via: "isoWeekOf + semanaIsoKey"
      pattern: "semanaIsoKey"
---

<objective>
Wave 0 de régimen + los dos cimientos que todos los tiles consumen: el alta del idiom de fecha
de urgencia en el single-source del guard 126, el helper central de links internos (`lib/`, sin
labels visibles) y el ítem nombrado reusable `panel-item-proyecto.tsx` con el guard `en_corpus`.

Purpose: sin el alta de idiom, el primer copy de urgencia rompe el guard (Pitfall P6 del research);
sin el helper, cada tile re-inventa el orden `?query#hash` (P1, degradación muda).
Output: `lib/links-internos.ts` + `components/panel-item-proyecto.tsx` + tests + `IDIOMS_APROBADOS`
actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md
@app/AGENTS.md

<read_first>
- `app/lib/anti-insinuacion-guard.test.ts` — `IDIOMS_APROBADOS` (~:766), `SUPERFICIES_PANEL` (~:323),
  `NEGACIONES_LOCKED` (~:779), anti-drift `(1f)` (~:1157)
- `app/lib/week-utils.ts` — `isoWeekOf`, `semanaIsoKey`, `parseISOWeek`
- `app/app/agenda/page.tsx` — anclas `resultados` / `citaciones` / `tabla-sala`; `?semana=`
- `app/app/proyecto/[boletin]/page.tsx` — anclas `estado` / `timeline` / `votaciones`
- `app/components/capa1/tramitacion-stepper.tsx:137` — único precedente con orden `?query#hash` correcto
- `app/lib/format.ts` — `extractoIdea`
</read_first>

<interfaces>
<!-- Contratos que las plans 03/04/05 consumen. Los executors de esas plans NO exploran el codebase. -->

app/lib/links-internos.ts (NUEVO — este plan lo crea):
```ts
export type AnclaProyecto = "estado" | "timeline" | "votaciones";
export type AnclaAgenda = "resultados" | "citaciones" | "tabla-sala";

export function hrefProyecto(boletin: string, ancla: AnclaProyecto): string;
export function hrefAgenda(ancla: AnclaAgenda, semanaIso?: string | null): string;
export function semanaIsoDeFecha(fechaIso: string | null | undefined): string | null;
```

app/components/panel-item-proyecto.tsx (NUEVO — este plan lo crea):
```ts
export interface PanelItemProyectoProps {
  boletin: string | null;
  titulo: string | null;
  enCorpus: boolean;
  ancla?: AnclaProyecto;          // default "estado"
  detalle?: React.ReactNode;      // línea de hecho fechado con verbo (la aporta cada tile)
  textoAlterno?: string | null;   // usado cuando no hay boletín/corpus (p.ej. `materia`)
  enlaceFuente?: string | null;   // enlace externo de la fuente para en_corpus:false
}
export function PanelItemProyecto(props: PanelItemProyectoProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alta del idiom de urgencia en el single-source del guard 126</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <action>
**FIX B-4 del checker (single-source REAL, no re-tipeo):** crear `app/lib/idioms-panel.ts` que
exporte `IDIOMS_APROBADOS` (los 4 stems existentes + los nuevos), e INVERTIR la dirección: el guard
`anti-insinuacion-guard.test.ts` pasa a IMPORTAR `IDIOMS_APROBADOS` desde `@/lib/idioms-panel`
(borrando su const local; el spread en NEGACIONES_LOCKED queda igual) — misma dirección prod→test
que las LEYENDA_* existentes. Así los tiles de wave 2 importan GENUINAMENTE el single-source y un
stem mal tipeado rompe el import, no pasa en silencio.
**FIX W-3 (inventario COMPLETO — el alta precede al copy y esta es la única ranura wave-1):** los
stems nuevos son TODOS los que el copy de los 6 tiles usará: `"fechada el"` (per O-4: el molde ratificado
por el operador es "Urgencia {grado} fechada el {d} · Citado el {d}"). Conservar los 4 stems
existentes VERBATIM y en su orden; el nuevo entra al final. Actualizar el JSDoc del array para
decir que son 5 stems y que `fechada el` es el alta de la Phase 128 por mandato O-4 (el molde
`vigente desde` sigue registrado y sigue siendo legal donde aplique).

NO añadir entradas a `NEGACIONES_LOCKED` en esta tarea: ningún copy de este plan niega un término
prohibido. Regla para los planes siguientes (documentarla como comentario junto a
`SUPERFICIES_PANEL`): si un tile renderiza una leyenda que CONTIENE un término prohibido para
NEGARLO, esa leyenda se registra verbatim en `NEGACIONES_LOCKED` en el MISMO commit que la
superficie, ANTES de que entre al scan (Pitfall 3, lección BLOCKER 91). El tile L4 reusará
`LEYENDA_ANTI_INSINUACION` verbatim, que YA está registrada — no se re-tipea.

Self-check obligatorio (mismo patrón que 126): añadir un `it` que verifique que cada stem de
`IDIOMS_APROBADOS` NO contiene ningún término de `TERMINOS_PROHIBIDOS` (un idiom aprobado que
contuviera un término prohibido sería una puerta trasera al carril). Debe pasar con los 5 stems.

Añadir además la nota de deuda declarada en el JSDoc de `SUPERFICIES_PANEL`: `lib/links-internos.ts`
NO emite labels visibles (solo hrefs) ⇒ NO entra al array; si alguna vez emite texto renderizado,
debe darse de alta.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tee /tmp/g128-01.txt; grep -o "passed" /tmp/g128-01.txt | wc -l</automated>
  </verify>
  <done>`app/lib/idioms-panel.ts` existe y exporta `IDIOMS_APROBADOS` con el inventario COMPLETO
(4 previos + `"fechada el"`, `"fechado el"`, `"fechadas en"`, `"En tabla de sala del"` — W-3: las
variantes de género/número/Senado que los planes 03/04/05 emiten); el guard lo IMPORTA (su const
local borrada); el self-check idiom×prohibidos EXISTENTE (it.each :1576 + anti-cero-vacuo :1594 —
W-2: NO duplicarlo, verificar que cubre los stems nuevos y que D-10(i) sigue siendo piso, no igual)
pasa sobre el array importado; guard completo verde con conteo de tests > 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/links-internos.ts — helper central con el orden ?query#hash correcto</name>
  <files>app/lib/links-internos.ts, app/lib/links-internos.test.ts</files>
  <behavior>
    - `hrefProyecto("16569-25","estado")` === `"/proyecto/16569-25#estado"`
    - `hrefProyecto` acepta solo los 3 anclas incondicionales (`estado`|`timeline`|`votaciones`) por tipo
    - el boletín se pasa por `encodeURIComponent` (un boletín con caracteres raros no rompe la URL)
    - `hrefAgenda("tabla-sala","2026-W32")` === `"/agenda?semana=2026-W32#tabla-sala"` — el `?` va ANTES del `#`
    - test que MUERDE el orden invertido: `expect(href).not.toContain("#tabla-sala?")`
    - `hrefAgenda("citaciones", null)` === `"/agenda#citaciones"` (sin `?semana=` vacío)
    - `hrefAgenda` con semana malformada (`"basura"`) → omite el query, devuelve `/agenda#ancla` (nunca emite un semana= inválido)
    - `semanaIsoDeFecha("2026-08-04")` === `"2026-W32"` (date-only medianoche UTC; SIN conversión de zona)
    - `semanaIsoDeFecha(null)` === `null`; `semanaIsoDeFecha("no-fecha")` === `null`
  </behavior>
  <action>
Crear `app/lib/links-internos.ts` como módulo PURO (cero I/O, cero JSX, cero strings visibles al
usuario — solo hrefs; ver la nota de D-08 en el guard). Implementar las 3 funciones de la sección
`<interfaces>`.

`semanaIsoDeFecha` NO hace aritmética propia: usa `isoWeekOf` + `semanaIsoKey` de `lib/week-utils.ts`
sobre `new Date(fechaIso)`. La fecha del jsonb es date-only (`"2026-08-04"`) y `new Date` la parsea
como medianoche UTC, que es lo que `isoWeekOf` espera — JAMÁS convertir a zona de Chile (gotcha
rector v9.0/v12.0). Validar `Number.isNaN(d.getTime())` → `null`.

`hrefAgenda` valida la semana con `parseISOWeek` antes de incluirla; si no parsea, se omite el query.

Escribir PRIMERO `links-internos.test.ts` con los casos de `<behavior>` (deben fallar), luego el
módulo. Documentar en el docblock el pitfall P1 con el precedente `tramitacion-stepper.tsx:137`.

Deuda declarada (comentario en el docblock, NO trabajo de esta fase): los 14 call-sites existentes
de `/proyecto/…` no se migran en 128 — el alcance es el panel.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/links-internos.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan (conteo de tests ejecutados > 0); el test del orden invertido existe y muerde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: panel-item-proyecto.tsx — ítem nombrado con guard en_corpus</name>
  <files>app/components/panel-item-proyecto.tsx, app/components/panel-item-proyecto.test.tsx</files>
  <behavior>
    - `enCorpus:true` + boletín → un `<a href="/proyecto/{b}#estado">` que contiene el boletín y el título
    - `ancla:"timeline"` → el href termina en `#timeline`
    - `enCorpus:false` → CERO elementos con `href` que empiece por `/proyecto` (control apareado: SÍ existe un `<a href>` externo)
    - `enCorpus:false` con `enlaceFuente` → el `<a>` externo lleva `target="_blank"` y `rel` conteniendo `noopener` y `noreferrer`
    - `enCorpus:false` sin boletín → renderiza `textoAlterno` truncado con `extractoIdea` (un `materia` de 400+ caracteres no se vuelca entero)
    - `titulo:null` + boletín → renderiza el boletín solo, sin fabricar título ni mostrar "null"/"undefined"
    - `detalle` se renderiza tal cual (es la línea de hecho fechado que aporta cada tile)
  </behavior>
  <action>
Crear `app/components/panel-item-proyecto.tsx` — RSC puro (JAMÁS `"use client"`), componente
síncrono de presentación con los props de `<interfaces>`. Usa `hrefProyecto` de
`lib/links-internos.ts` (Task 2) y `extractoIdea` de `lib/format.ts` (max ~120) para `textoAlterno`.

Nombre de archivo CONGELADO: el anti-drift `(1f)` del guard es recursivo sobre `app/components/` y
muerde cualquier `panel-*.tsx` fuera de los 8 declarados. Usar exactamente `panel-item-proyecto.tsx`.

Estilos: reusar SOLO las clases arbitrarias YA sancionadas por `bento-guards`
(`text-[15px]`, `text-[13px]`, `text-[11px]`, `px-[9px]`, `gap-[14px]`, `w-[3px]`, `rounded-[2px]`).
Un `text-[12px]` nuevo hace fallar el guard. Variables Tailwind v4 siempre `bg-[var(--x)]`, jamás
`bg-[--x]`.

Interpolación de `titulo`/`textoAlterno` por JSX (escapado por defecto); JAMÁS
`dangerouslySetInnerHTML` — el texto viene de fuente externa.

Escribir el test primero con los casos de `<behavior>`; el assert de "cero href interno" se hace
sobre el container (`container.querySelectorAll('a[href^="/proyecto"]').length === 0`) con el
control positivo apareado en el mismo test.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-item-proyecto.test.tsx lib/anti-insinuacion-guard.test.ts lib/bento-guards.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; anti-drift y bento-guards siguen verdes con el archivo nuevo en el árbol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente gubernamental → jsonb → DOM | `titulo`/`materia`/`enlace` son texto ajeno interpolado en la portada |
| panel → sitio externo (`target="_blank"`) | la pestaña abierta obtiene `window.opener` si falta `rel` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering/XSS | panel-item-proyecto.tsx | mitigate | interpolación JSX escapada; prohibición explícita de `dangerouslySetInnerHTML` en el docblock y verificada por lectura |
| T-128-02 | Tampering | enlaces externos de fuente | mitigate | `rel="noopener noreferrer"` + `target="_blank"` asertado por test |
| T-128-03 | Repudiation | href fabricado a ficha inexistente (404) | mitigate | guard `en_corpus` en el punto de link; test de cero `a[href^="/proyecto"]` con control positivo |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas en este plan (solo módulos del repo) — si el executor necesitara una, se detiene y escala |
</threat_model>

<verification>
```bash
cd app && pnpm vitest run lib/links-internos.test.ts components/panel-item-proyecto.test.tsx
cd app && pnpm guards        # anti-drift (1f) + bento + lockdown verdes
```
Verificar el CONTEO de tests ejecutados en cada corrida (un glob que no matchea sale 0 sin correr
nada — gotcha v12.0).
</verification>

<success_criteria>
- `IDIOMS_APROBADOS` tiene 5 stems, con `"fechada el"`, y un self-check que prueba que ningún idiom contiene un término prohibido.
- `hrefAgenda` emite `?semana=` ANTES de `#ancla`, con test que muerde el orden invertido.
- `PanelItemProyecto` con `en_corpus:false` emite CERO hrefs internos y sí un enlace externo con `rel="noopener noreferrer"`.
- `pnpm guards` verde con los archivos nuevos en el árbol.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-01-SUMMARY.md` when done
</output>
