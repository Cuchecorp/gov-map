---
phase: 128-panel-ui
plan: 03
type: execute
wave: 2
depends_on: ["128-01", "128-02"]
files_modified:
  - app/components/panel-tile-sala.tsx
  - app/components/panel-tile-sala.test.tsx
  - app/components/panel-tile-comisiones.tsx
  - app/components/panel-tile-comisiones.test.tsx
autonomous: true
requirements: [PANEL-02, PANEL-03, PANEL-05, PANEL-07]

must_haves:
  truths:
    - "El tile de sala nombra boletín + título de cada punto de la tabla, con el día en verbo"
    - "La fila sintética de Cámara (numero/tipo/hora null) no fabrica 'Sesión N.º a las HH:MM'"
    - "El tile de comisiones declara cobertura con denominador: '23 citaciones del Senado · 0 de la Cámara en las fuentes consultadas'"
    - "El chip L5 yuxtapone dos hechos fechados ('Urgencia {grado} fechada el {d} · Citado el {d}'), jamás una relación"
    - "Cada tile muestra 4 ítems y 'y N más →' respaldado por el total del jsonb"
  artifacts:
    - path: "app/components/panel-tile-sala.tsx"
      provides: "Tile 1 — En tabla de sala esta semana (L2)"
      exports: ["PanelTileSala"]
    - path: "app/components/panel-tile-comisiones.tsx"
      provides: "Tile 2 — Comisiones citadas (L1+L5+L6+L7)"
      exports: ["PanelTileComisiones"]
  key_links:
    - from: "app/components/panel-tile-sala.tsx"
      to: "app/lib/links-internos.ts"
      via: "hrefAgenda('tabla-sala', semanaIso)"
      pattern: "hrefAgenda"
    - from: "app/components/panel-tile-comisiones.tsx"
      to: "app/components/panel-item-proyecto.tsx"
      via: "render de cada punto citado"
      pattern: "PanelItemProyecto"
---

<objective>
Los dos tiles de agenda de la propuesta editorial ratificada: Tile 1 (sala-semana, PRIMERO por O-5)
y Tile 2 (comisiones citadas con chips L5, ítem de comisión mixta L6 y cobertura declarada L7).

Purpose: son la mayor pérdida de valor del panel actual — el dato con boletín, título y urgencia
ya existe en el jsonb y hoy se rinde como un contador mudo.
Output: `panel-tile-sala.tsx` + `panel-tile-comisiones.tsx` como vistas puras testeables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-CONTEXT.md

<read_first>
- `.planning/spikes/v13.0-editorial-portada.md` §3.2 TILE 1 y TILE 2 — el copy de ejemplo es la FORMA;
  los sujetos salen del jsonb vivo
- `128-RESEARCH.md` §"Contrato REAL del jsonb" (fixtures) y §"Cobertura verificada"
- `app/components/panel-actualidad.tsx` — patrón vista pura + `claseCamara` (barra cívica 3px, regla A)
- `app/lib/dia-calendario.ts` (`fechaCivilCorta`) y `app/lib/week-utils.ts` (`formatWeekLabel`)
- `app/components/bento/bento-tile.tsx` — `BentoTile variant span asChild`
</read_first>

<interfaces>
Ya creados por 128-01 y 128-02 — importar, no re-implementar:

```ts
// lib/links-internos.ts
hrefProyecto(boletin, "estado"|"timeline"|"votaciones"): string
hrefAgenda("resultados"|"citaciones"|"tabla-sala", semanaIso?: string|null): string
semanaIsoDeFecha(fechaIso: string|null): string|null

// lib/panel-evidencia.ts
parseEvidenciaSala(raw): Evidencia<ItemSesionSala>
parseEvidenciaCitaciones(raw): Evidencia<ItemCitacion>
etiquetaFuente(f): string|null
gradoUrgencia(literal: string|null): string|null
urgenciaVigentePorBoletin(items: ItemProyecto[]): Map<string,{grado:string;fecha:string}>

// components/panel-item-proyecto.tsx
<PanelItemProyecto boletin titulo enCorpus ancla detalle textoAlterno enlaceFuente />
```

Firma de los dos tiles nuevos (vistas puras — el wrapper async vive en 128-06):
```ts
export interface PanelTileSalaProps {
  filas: { cobertura_camara: string | null; evidencia: unknown }[];
  urgencias: Map<string, { grado: string; fecha: string }>;  // cruce L5 (vacío = sin chips)
  maxItems?: number;                                          // default 4 (O-7)
}
export function PanelTileSala(props: PanelTileSalaProps): JSX.Element;

export interface PanelTileComisionesProps {
  filas: { cobertura_camara: string | null; conteo: number; evidencia: unknown }[];
  urgencias: Map<string, { grado: string; fecha: string }>;
  maxItems?: number;
}
export function PanelTileComisiones(props: PanelTileComisionesProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: panel-tile-sala.tsx — Tile 1, tabla de sala nombrada (L2)</name>
  <files>app/components/panel-tile-sala.tsx, app/components/panel-tile-sala.test.tsx</files>
  <behavior>
    - Título del tile: "En tabla de sala esta semana"
    - Fila de Senado (`numero:"47"`, `tipo:"Ordinaria"`, `hora_inicio:"16:00"`) → encabezado de sesión con esos datos + el día en `fechaCivilCorta`
    - Fila de Cámara (`numero/tipo/hora_inicio` NULL) → CERO ocurrencias de `Sesión N` y CERO hora fabricada; el encabezado dice el día + "tabla semanal" (control positivo apareado: el día SÍ aparece)
    - Cada punto de `tabla[]` con `en_corpus:true` → `<a href="/proyecto/{b}#estado">` con boletín y título
    - Punto con `en_corpus:false` → cero href interno + enlace externo (delegado a `PanelItemProyecto`)
    - Orden de los puntos = `posicion` de la fuente (ASC), nunca reordenado por otro criterio
    - `tabla_total:25` con `maxItems:4` → se ven 4 ítems y un link "y 21 más →" cuyo href es `/agenda?semana=YYYY-Www#tabla-sala` (query ANTES del hash)
    - `tabla_total <= maxItems` → NO se renderiza el "y N más"
    - `semana_iso` ausente en `agenda_sala` → se deriva de `items[].fecha` con `semanaIsoDeFecha`; el href nunca queda sin `?semana=` cuando hay fecha válida
    - Chip L5: boletín presente en el Map de urgencias → texto `Urgencia {grado} fechada el {d}`; boletín ausente → sin chip (jamás "sin urgencia")
    - `quorum` NO se renderiza como "quórum" ni como urgencia (P4): el test asserta que la cadena "quórum" no aparece en el DOM del tile
    - Footer: `Fuente: Agenda del Congreso · según fuente al {consultado_al}` y CERO ocurrencias de "datos al"
    - `etiquetaFuente` null → el footer omite la fuente pero conserva "según fuente al {d}" (nunca inventa una fuente)
  </behavior>
  <action>
Crear `app/components/panel-tile-sala.tsx` — RSC puro, vista pura síncrona (JAMÁS `"use client"`),
un `BentoTile` por tile. Nombre de archivo CONGELADO (anti-drift `(1f)` recursivo).

Parsear cada fila con `parseEvidenciaSala` (128-02). Una fila por cámara: PROD emite 1 de Cámara y
2 de Senado. Renderizar un bloque de sesión por fila, en el ORDEN DE LLEGADA de las filas (jamás
reordenar por conteo cross-cámara — T-52-13, regla B). Barra cívica 3px: reusar la lógica de
`claseCamara` importada desde `@/lib/panel-camara` (B-3: movida y exportada por 128-02 Task 0 en wave 1; añadirla a las props/uso del tile según el diseño
NO — copiarla habría que evitarlo: en su lugar, este plan la deja donde está y el tile recibe la
clase derivada del `cobertura_camara` con la MISMA regla A: sin cámara → sin barra).

Fechas (D-05, tres carriles):
- CUERPO: el hecho con verbo — `En tabla de sala del {fechaCivilCorta(fecha)}` para el encabezado
  (idiom registrado: `En tabla de sala de la Cámara del`; usar la variante registrada cuando la
  cobertura es Cámara). Las fechas del jsonb son date-only ⇒ SIEMPRE `fechaCivilCorta`, JAMÁS
  `new Date(...).toLocaleDateString()` ni conversión de zona.
- FOOTER: `Fuente: {etiquetaFuente} · según fuente al {fechaCivilCorta(consultado_al)}`. El literal
  `"datos al"` está PROHIBIDO. `fecha_captura` jamás se muestra.
- Los stems `"según fuente al"`, `"En tabla de sala de la Cámara del"` y `"fechada el"` se IMPORTAN
  de `IDIOMS_APROBADOS` desde `@/lib/idioms-panel` (B-4: single-source de producción creado por 128-01; el guard importa del mismo archivo) — PROHIBIDO re-tipear stems (o, si importar desde un
  archivo `.test.ts` en producción no es viable, se escriben verbatim IDÉNTICOS a los del array
  registrado — el guard los verifica. NO inventar variantes.

Degradación honesta de Cámara (D-06/P10): con `numero`/`tipo`/`hora_inicio` NULL, el encabezado
dice solo el día + "tabla semanal". Prohibido fabricar "Sesión N.º X a las HH:MM".

Chips L5 (O-4): yuxtaposición de DOS hechos fechados, jamás una relación. Molde exacto:
`Urgencia {grado} fechada el {d}`. Prohibido cualquier conector que sugiera causa ("por", "debido a",
"gracias a"). El grado sale de `gradoUrgencia`; el `(dd.mm.aaaa)` del literal NUNCA se muestra.

Estilos: SOLO clases arbitrarias ya sancionadas (`text-[15px]`, `text-[13px]`, `text-[11px]`,
`px-[9px]`, `gap-[14px]`, `w-[3px]`, `rounded-[2px]`); Tailwind v4 `bg-[var(--x)]`.

Test primero, con fixtures copiados verbatim de `128-RESEARCH.md` (incluida la fila sintética de
Cámara). El assert de "no fabrica sesión" usa el container completo y va apareado con un control
positivo (el día SÍ aparece) — un cero fuerte sin control positivo no prueba nada.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-sala.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; el href del "y N más" tiene `?semana=` antes de `#tabla-sala`; cero "datos al" y cero "Sesión N" en la rama Cámara.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: panel-tile-comisiones.tsx — Tile 2, citaciones + chips L5 + cobertura L7</name>
  <files>app/components/panel-tile-comisiones.tsx, app/components/panel-tile-comisiones.test.tsx</files>
  <behavior>
    - Título del tile: "Comisiones citadas esta semana"
    - Cada punto con boletín y `en_corpus:true` → `<a href="/proyecto/{b}#estado">` con boletín + título
    - Detalle del punto: `Citado el {fechaCivilCorta(fecha)} · Comisión {comision} · {horario}`; `horario` null → se omite el segmento, sin fabricar hora
    - Punto con `boletin:null` (L6 comisión mixta / punto sin boletín) → texto plano desde `materia` truncado + enlace externo de la citación; CERO href interno
    - Chip L5 cuando el boletín está en el Map: `Urgencia {grado} fechada el {d}` — dos hechos fechados yuxtapuestos, cero conector causal
    - Cobertura L7 con denominador: con filas `Senado conteo:23` y sin fila de Cámara → renderiza `23 citaciones del Senado · 0 de la Cámara en las fuentes consultadas`. FIX W-5: el universo de dos cámaras se declara como constante nombrada (p.ej. `CAMARAS_CORPUS = ["Cámara de Diputados","Senado"] as const`) con comentario que la ata a la definición del corpus — un tercer origen NO desaparece en silencio.
    - El cero de Cámara NUNCA aparece mudo: siempre con el denominador "en las fuentes consultadas"
    - `puntos_total:31` con `maxItems:4` → 4 ítems + `y 27 más →` con href `/agenda?semana=2026-W32#citaciones`
    - `semana_iso` viene del ítem de `agenda_citacion` (presente en el contrato); si falta, se deriva de `fecha`
    - Footer: `Fuente: Agenda del Congreso · según fuente al {consultado_al}`; cero "datos al"; cero `fecha_captura`
    - Cero vocabulario de ranking: el DOM no contiene "top", "los más", "la más activa"
  </behavior>
  <action>
Crear `app/components/panel-tile-comisiones.tsx` con la firma de `<interfaces>`. Parsear con
`parseEvidenciaCitaciones`. El ítem del tile es el PUNTO (el sujeto), no la citación: aplanar
`items[].puntos[]` conservando el contexto de la citación (comisión, horario, fecha, enlace) para
el detalle. El presupuesto de 4 (O-7) se aplica sobre los puntos aplanados; el "y N más" se respalda
con la suma de `puntos_total` de las citaciones (NO con la longitud del array renderizado).

Cobertura L7 (D-06/PANEL-07): se calcula del conjunto de filas recibidas — la cámara con fila aporta
su `conteo`; la cámara SIN fila aporta `0` explícito con el denominador "en las fuentes consultadas".
Es un hecho de cobertura, no un juicio: prohibido cualquier adjetivo sobre el hueco (el carril
COBERTURA del linter muerde).

Chips L5 con el mismo molde y las mismas prohibiciones que el Tile 1.

`materia` largo → `extractoIdea(materia, 120)` vía `PanelItemProyecto.textoAlterno`.

Mismas reglas de fechas, idioms, estilos y RSC puro que el Task 1.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-comisiones.test.tsx lib/anti-insinuacion-guard.test.ts lib/bento-guards.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; la línea de cobertura aparece con ambos números y el denominador; guards verdes con los dos tiles nuevos en el árbol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jsonb de agenda → copy de portada | `materia`/`comision`/`titulo` son texto ajeno |
| yuxtaposición de dos hechos (L5) | el riesgo dominante de la fase: insinuar relación causal urgencia↔citación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-07 | Repudiation | chip L5 | mitigate | molde fijo `Urgencia {grado} fechada el {d} · Citado el {d}`; cero conectores causales; carril PANEL del linter como gate en CI |
| T-128-08 | Repudiation | encabezado de sesión de Cámara | mitigate | scalars NULL → solo el día + "tabla semanal"; test de cero "Sesión N" con control positivo |
| T-128-09 | Information Disclosure | cobertura parcial leída como completa | mitigate | denominador obligatorio "en las fuentes consultadas"; cero mudo prohibido |
| T-128-10 | Tampering/XSS | interpolación de `materia` | mitigate | JSX escapado; prohibido `dangerouslySetInnerHTML` |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas |
</threat_model>

<verification>
```bash
cd app && pnpm vitest run components/panel-tile-sala.test.tsx components/panel-tile-comisiones.test.tsx
cd app && pnpm guards
```
Verificar el CONTEO de tests ejecutados (glob que no matchea sale 0 sin correr nada).
</verification>

<success_criteria>
- Los dos tiles nombran sujetos (boletín + título) y enlazan vía el helper central.
- La rama Cámara no fabrica número, tipo ni hora.
- La cobertura se declara con denominador y el cero nunca es mudo.
- Cero "datos al" en el DOM de ambos tiles; el footer usa "según fuente al".
- `pnpm guards` verde.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-03-SUMMARY.md` when done
</output>
