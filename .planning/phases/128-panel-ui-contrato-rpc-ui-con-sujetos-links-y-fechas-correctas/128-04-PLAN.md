---
phase: 128-panel-ui
plan: 04
type: execute
wave: 2
depends_on: ["128-01", "128-02"]
files_modified:
  - app/components/panel-tile-urgencias.tsx
  - app/components/panel-tile-urgencias.test.tsx
  - app/components/panel-tile-movimiento.tsx
  - app/components/panel-tile-movimiento.test.tsx
  - app/components/panel-tile-ingresos.tsx
  - app/components/panel-tile-ingresos.test.tsx
autonomous: true
requirements: [PANEL-02, PANEL-03, PANEL-05, PANEL-07]

must_haves:
  truths:
    - "El tile de urgencias cuenta BOLETINES distintos por grado (5/42/24); el '95' de eventos no aparece"
    - "El tile de movimiento lista trámites nombrados y no inventa la descripción del trámite (velocity no la trae)"
    - "El tile de ingresos dice '{N} eventos de {M} proyecto(s)' nombrando el boletín, jamás '2 movimientos'"
    - "La señal suprimida rinde su causa VERBATIM como cuerpo, sin '0' mudo"
  artifacts:
    - path: "app/components/panel-tile-urgencias.tsx"
      provides: "Tile 3 — Urgencias del Ejecutivo por grado (L3)"
      exports: ["PanelTileUrgencias"]
    - path: "app/components/panel-tile-movimiento.tsx"
      provides: "Tile 4 — Movimiento reciente (velocity nombrada)"
      exports: ["PanelTileMovimiento"]
    - path: "app/components/panel-tile-ingresos.tsx"
      provides: "Tile 6 — Ingresos, archivos y retiros (fusión)"
      exports: ["PanelTileIngresos"]
  key_links:
    - from: "app/components/panel-tile-urgencias.tsx"
      to: "app/lib/panel-evidencia.ts"
      via: "gradoUrgencia + parseEvidenciaProyectos"
      pattern: "gradoUrgencia"
    - from: "app/components/panel-tile-movimiento.tsx"
      to: "app/lib/links-internos.ts"
      via: "hrefProyecto(b,'timeline')"
      pattern: "hrefProyecto"
---

<objective>
Los tres tiles de tramitación de la propuesta editorial: Tile 3 (urgencias por grado, contando
boletines — el "95" muere), Tile 4 (movimiento reciente nombrado) y Tile 6 (ingresos + archivos
fusionados, "{N} eventos de {M} proyecto(s)").

Purpose: los contadores sin sujeto son el defecto raíz medido del panel; estos tres tiles los
reemplazan por hechos nombrados y fechados.
Output: 3 componentes `panel-tile-*.tsx` como vistas puras testeables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-CONTEXT.md

<read_first>
- `.planning/spikes/v13.0-editorial-portada.md` §3.2 TILES 3, 4 y 6 — la FORMA del copy
- `128-RESEARCH.md` §"Contrato REAL del jsonb" (fixtures de `velocity`/`urgencias`/`archivados`),
  §"Paridad y flags verificados" (5/42/24 boletines vs 14/57/24 eventos), §Pitfall P2
- `app/components/panel-actualidad.tsx:182-201` — regla C (supresión = causa verbatim como cuerpo)
- `app/lib/dia-calendario.ts` (`fechaCivilCorta`)
</read_first>

<interfaces>
Ya creados por 128-01 / 128-02 — importar, no re-implementar:
```ts
// lib/panel-evidencia.ts
parseEvidenciaProyectos(raw): Evidencia<ItemProyecto>   // items: {fecha,enlace,titulo,boletin,en_corpus,descripcion,enlace_evento}
etiquetaFuente(f): string|null
gradoUrgencia(descripcion: string|null): string|null
// lib/links-internos.ts
hrefProyecto(boletin, "estado"|"timeline"|"votaciones"): string
// components/panel-item-proyecto.tsx
<PanelItemProyecto boletin titulo enCorpus ancla detalle textoAlterno enlaceFuente />
```

Firmas de los tres tiles (vistas puras; el wrapper async vive en 128-06):
```ts
export interface FilaPanel {
  cobertura_camara: string | null;
  conteo: number;
  fecha_max: string | null;
  supresion_causa: string | null;
  evidencia: unknown;
}
export function PanelTileUrgencias(p: { filas: FilaPanel[]; maxItems?: number }): JSX.Element;
export function PanelTileMovimiento(p: { filas: FilaPanel[]; maxItems?: number }): JSX.Element;
export function PanelTileIngresos(p: { ingresos: FilaPanel[]; archivados: FilaPanel[]; maxItems?: number }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: panel-tile-urgencias.tsx — Tile 3, por grado y por boletines distintos</name>
  <files>app/components/panel-tile-urgencias.tsx, app/components/panel-tile-urgencias.test.tsx</files>
  <behavior>
    - Título del tile: "Urgencias del Ejecutivo, por grado"
    - Encabezado derivado de los items: con un fixture de 95 eventos que cubren 5/42/24 boletines distintos → renderiza `5 proyectos con Discusión inmediata · 42 con Suma · 24 con Simple`
    - El literal `95` NO aparece en el DOM del tile (cero fuerte) — control positivo apareado: `42` SÍ aparece
    - Un mismo boletín con 3 urgencias cuenta UNA vez en el encabezado y aparece UNA vez en la lista (la más reciente por fecha)
    - Ítems: `<a href="/proyecto/{b}#estado">` con boletín + título, detalle `Urgencia {grado} fechada el {fechaCivilCorta(fecha)}`
    - `descripcion` con literal desconocido → se muestra el literal verbatim, jamás se omite ni se tipifica mal
    - Orden de los grados = orden fijo declarado (Discusión inmediata, Suma, Simple), documentado como orden institucional del grado, NO como ranking por volumen
    - NO se renderiza link agregado de tile (O-6): cero `<a>` cuyo texto sea "ver todas"/"y N más" a nivel de tile
    - Footer: `Fuente: Tramitación · según fuente al {d}`; cero "datos al"
  </behavior>
  <action>
Crear `app/components/panel-tile-urgencias.tsx`. Parsear con `parseEvidenciaProyectos`; agrupar por
`gradoUrgencia(descripcion)` contando BOLETINES DISTINTOS (`Set<boletin>`), NUNCA eventos: contar
eventos es exactamente el defecto que O-3/D-01 mandan matar ("95 urgencias").

El orden de los grados es FIJO y declarado en un array constante con comentario explicando que es
el orden institucional del grado (de mayor a menor premura formal), no un ranking de volumen — el
carril anti-ranking del linter y la regla B lo exigen.

Ítems listados: uno por boletín (el evento más reciente de ese boletín), hasta `maxItems` (default 4,
O-7). Como O-6 elimina el link agregado del tile, cuando hay más boletines que `maxItems` NO se
emite un "y N más →" con destino; se puede declarar el remanente como texto sin link o simplemente
omitirse — decidir por la opción que NO fabrique un destino inexistente.

Fechas: date-only del jsonb ⇒ `fechaCivilCorta`. Idioms verbatim del registro (`fechada el`,
`según fuente al`). RSC puro, nombre de archivo congelado, clases arbitrarias solo de la whitelist
sancionada, `bg-[var(--x)]`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-urgencias.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; el conteo por boletines distintos está probado con un fixture de eventos repetidos; el "95" no aparece.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: panel-tile-movimiento.tsx — Tile 4, velocity nombrada (sin inventar la descripción)</name>
  <files>app/components/panel-tile-movimiento.tsx, app/components/panel-tile-movimiento.test.tsx</files>
  <behavior>
    - Título del tile: "Movimiento reciente"
    - Cada ítem: `<a href="/proyecto/{b}#timeline">` con boletín + título, detalle `Trámite del {fechaCivilCorta(fecha)} · {cámara}`
    - La cámara sale de `cobertura_camara` de la FILA (grafía ya única en PROD: `Cámara de Diputados` / `Senado`); `cobertura_camara` null → se omite el segmento de cámara y la barra cívica (regla A)
    - El DOM NO contiene ninguna descripción de trámite fabricada: con un fixture de `velocity` (sin clave `descripcion`) el texto renderizado del ítem se compone solo de boletín, título, fecha y cámara (P2)
    - `titulo` null → se muestra el boletín solo
    - `total:2` con `maxItems:4` → no se renderiza "y N más"
    - Filas de dos cámaras → ambas se listan, en el orden de llegada de las filas; JAMÁS reordenadas por conteo cross-cámara (T-52-13)
    - Footer: `Fuente: Tramitación · según fuente al {d}`; cero "datos al"
  </behavior>
  <action>
Crear `app/components/panel-tile-movimiento.tsx`. Parsear con `parseEvidenciaProyectos`. Fusionar
los ítems de las filas de ambas cámaras conservando de qué fila viene cada ítem (para la etiqueta
de cámara y la barra cívica), preservando el orden de llegada de las filas; dentro de cada fila, el
orden que trae el jsonb.

PROHIBIDO fabricar el texto del trámite: `velocity` no trae `descripcion` (P2 del research) y el
copy de ejemplo del spike ("Informe de Comisión Mixta…") NO es renderizable con el dato actual. El
detalle es `Trámite del {fecha} · {cámara}` y nada más.

Fechas, idioms, RSC puro, naming congelado, whitelist de clases: mismas reglas que el Task 1.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-movimiento.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; el test que prueba la ausencia de descripción fabricada existe y va apareado con un control positivo (título y fecha SÍ aparecen).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: panel-tile-ingresos.tsx — Tile 6, ingresos + archivos fusionados</name>
  <files>app/components/panel-tile-ingresos.tsx, app/components/panel-tile-ingresos.test.tsx</files>
  <behavior>
    - Título del tile: "Ingresos, archivos y retiros"
    - Fila `nuevos_ingresos` con `supresion_causa` → la causa VERBATIM es el cuerpo, seguida de `— en las fuentes consultadas al {d}`; CERO "0" mudo y cero "sin movimiento" fabricado
    - Fila suprimida con `evidencia:{}` → parsea sin throw y no lista items
    - `archivados` con 2 eventos del MISMO boletín → renderiza `2 eventos de 1 proyecto` (singular correcto) y nombra el boletín; el DOM NO contiene "2 movimientos"
    - 3 eventos de 2 boletines → `3 eventos de 2 proyectos` (plural correcto)
    - Cada proyecto: `<a href="/proyecto/{b}#timeline">` con boletín + título, detalle `Archivo o retiro fechado el {d}` (usa `descripcion` verbatim cuando existe)
    - Ambas subsecciones conviven en un solo tile con encabezados internos distinguibles
    - Footer: `Fuente: Tramitación · según fuente al {d}`; cero "datos al"; `cobertura_camara` de ingresos (`"2022-2026 (piso de corpus)"`) se rinde como etiqueta de VENTANA, jamás como cámara (herencia 0065, D-02) — el test asserta que no se renderiza como chip de cámara ni produce barra cívica
  </behavior>
  <action>
Crear `app/components/panel-tile-ingresos.tsx` fusionando las señales `nuevos_ingresos` y
`archivados` en un solo `BentoTile` (D-01, Tile 6).

Regla C CONSERVADA ÍNTEGRA: `supresion_causa != null` ⇒ la causa es el cuerpo, VERBATIM, con el
sufijo `— en las fuentes consultadas al {rótulo}`. Nunca lista vacía, nunca "0" mudo, nunca
"sin movimiento". Reusar el copy exacto que hoy vive en `panel-actualidad.tsx:182-201`.

Conteo del defecto D-07: agrupar los items de `archivados` por boletín y emitir
`{N} eventos de {M} proyecto{s}` con concordancia de número correcta. El literal "movimientos" está
prohibido en este tile (sugiere M proyectos donde hay uno).

`cobertura_camara` de `nuevos_ingresos` es `"2022-2026 (piso de corpus)"`: es una etiqueta de
ventana, NO una cámara — no alimenta `claseCamara` ni el chip de cámara (la regla A ya devuelve null
para ese literal; el test lo fija como invariante).

Fechas, idioms, RSC puro, naming congelado, whitelist de clases: mismas reglas que los tasks previos.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-tile-ingresos.test.tsx lib/anti-insinuacion-guard.test.ts lib/bento-guards.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; "2 eventos de 1 proyecto" probado; regla C intacta; guards verdes con los 3 tiles nuevos en el árbol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eventos de tramitación → conteo mostrado | la unidad del conteo debe ser la unidad del hecho (proyecto vs evento) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-11 | Repudiation | conteo de urgencias | mitigate | `Set<boletin>` como unidad; test de cero "95" con control positivo apareado |
| T-128-12 | Repudiation | descripción de trámite en velocity | mitigate | prohibición explícita de fabricar texto; test de composición del ítem |
| T-128-13 | Repudiation | "2 movimientos" sugiriendo 2 proyectos | mitigate | molde `{N} eventos de {M} proyecto(s)` con concordancia probada |
| T-128-14 | Information Disclosure | vacío honesto convertido en "0" mudo | mitigate | regla C conservada verbatim; test de causa verbatim |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas |
</threat_model>

<verification>
```bash
cd app && pnpm vitest run components/panel-tile-urgencias.test.tsx components/panel-tile-movimiento.test.tsx components/panel-tile-ingresos.test.tsx
cd app && pnpm guards
```
Verificar el CONTEO de tests ejecutados en cada corrida.
</verification>

<success_criteria>
- Urgencias cuenta boletines distintos (5/42/24) y el "95" no aparece.
- Movimiento no fabrica descripción de trámite.
- Ingresos dice "{N} eventos de {M} proyecto(s)" y conserva la regla C intacta.
- Cero "datos al" en el DOM de los tres tiles.
- `pnpm guards` verde.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-04-SUMMARY.md` when done
</output>
