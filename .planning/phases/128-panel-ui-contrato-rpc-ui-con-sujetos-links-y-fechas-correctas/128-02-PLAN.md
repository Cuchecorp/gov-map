---
phase: 128-panel-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/panel-evidencia.ts
  - app/lib/panel-evidencia.test.ts
autonomous: true
requirements: [PANEL-03, PANEL-05, PANEL-07]

must_haves:
  truths:
    - "El jsonb de cada señal se lee con narrowing por clave, jamás con `as` — una clave ausente no se renderiza como undefined"
    - "La evidencia de una señal suprimida (`{}`) se parsea sin throw y sin fabricar items"
    - "El grado de urgencia se deriva del literal `descripcion` con fallback honesto al literal"
    - "El cruce L5 devuelve la urgencia MÁS RECIENTE por boletín, jamás la de mayor grado"
  artifacts:
    - path: "app/lib/panel-evidencia.ts"
      provides: "contrato TS del jsonb por señal + derivaciones puras"
      exports: ["parseEvidenciaProyectos", "parseEvidenciaCitaciones", "parseEvidenciaSala", "etiquetaFuente", "gradoUrgencia", "urgenciaVigentePorBoletin"]
      min_lines: 120
  key_links:
    - from: "app/lib/panel-evidencia.ts"
      to: "supabase/migrations/0081_actualidad_evidencia_fix.sql"
      via: "claves del contrato jsonb"
      pattern: "puntos_total|tabla_total|descripcion"
---

<objective>
Tipar el jsonb `actualidad_senal.evidencia` como contrato explícito por señal (el contrato NO es
homogéneo entre señales) y encapsular las tres derivaciones puras que los tiles necesitan:
etiqueta de fuente desde el dato, grado de urgencia desde el literal, y el cruce L5
urgencia↔boletín con la regla "más reciente por fecha".

Purpose: sin este módulo, cada tile hace `as ItemX[]` y una clave ausente se convierte en
`undefined` renderizado (anti-pattern #1 del research).
Output: `app/lib/panel-evidencia.ts` + tests con fixtures del contrato REAL de PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md

<read_first>
- `128-RESEARCH.md` §"Contrato REAL del jsonb en PROD" — las claves exactas por señal, verificadas
  con `jsonb_pretty` contra PROD. Es la ÚNICA fuente del shape; no re-consultar la DB.
- `supabase/migrations/0081_actualidad_evidencia_fix.sql` — `grado`→`descripcion`, `puntos_total`,
  `tabla_total`, ítems `boletin:null`
- `app/lib/format.ts` — helpers de formato existentes (no duplicar)
</read_first>

<interfaces>
<!-- CONTRATO QUE LAS PLANS 03/04/06 CONSUMEN. Este plan lo define; los tiles lo importan. -->

app/lib/panel-evidencia.ts (NUEVO):
```ts
export interface FuenteEvidencia { origen: string | null; dataset: string | null }

/** velocity | urgencias | archivados — `descripcion` NO existe en velocity (P2) */
export interface ItemProyecto {
  fecha: string | null;        // date-only "2026-07-28"
  enlace: string | null;
  titulo: string | null;
  boletin: string | null;
  en_corpus: boolean;
  descripcion: string | null;  // urgencias: el GRADO verbatim; velocity: siempre null
  enlace_evento: string | null;
}

export interface PuntoCitacion {
  boletin: string | null; titulo: string | null; enlace: string | null;
  materia: string | null; posicion: number | null; en_corpus: boolean;
}
export interface ItemCitacion {
  fecha: string | null; enlace: string | null; comision: string | null;
  horario: string | null; semana_iso: string | null;   // presente SOLO en agenda_citacion
  puntos_total: number | null; puntos: PuntoCitacion[];
}

export interface ItemTablaSala {
  boletin: string | null; titulo: string | null; enlace: string | null;
  materia: string | null; posicion: number | null;
  quorum: string | null;        // POLISÉMICO por cámara (P4) — el tile decide
  en_corpus: boolean; parte_sesion: string | null;
}
export interface ItemSesionSala {
  fecha: string | null; tipo: string | null; numero: string | null;
  hora_inicio: string | null; enlace: string | null;
  tabla_total: number | null; tabla: ItemTablaSala[];
}

export interface Evidencia<T> {
  items: T[]; total: number | null; consultado_al: string | null; fuente: FuenteEvidencia;
}

export function parseEvidenciaProyectos(raw: unknown): Evidencia<ItemProyecto>;
export function parseEvidenciaCitaciones(raw: unknown): Evidencia<ItemCitacion>;
export function parseEvidenciaSala(raw: unknown): Evidencia<ItemSesionSala>;

/** Etiqueta ciudadana de la fuente DESDE EL DATO (D-02) — nunca un mapa por tipo_senal. */
export function etiquetaFuente(f: FuenteEvidencia): string | null;

/** `"Suma"` | `"Simple"` | `"Discusión inmediata"` derivado del literal; fallback = literal. */
export function gradoUrgencia(descripcion: string | null): string | null;

/** Cruce L5: por boletín, la urgencia MÁS RECIENTE por `fecha` (P7). */
export function urgenciaVigentePorBoletin(
  items: ItemProyecto[],
): Map<string, { grado: string; fecha: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (FIX B-3 checker): mover claseCamara a lib exportada</name>
  <files>app/lib/panel-camara.ts, app/components/panel-actualidad.tsx</files>
  <read_first>
    - `app/components/panel-actualidad.tsx` L146-160 (la función privada claseCamara — regla A, barra cívica; L153: "2022-2026 (piso de corpus)" → null)
  </read_first>
  <action>
Mover `claseCamara` VERBATIM (cero cambio de lógica) a `app/lib/panel-camara.ts` como export
nombrado, con su comentario de regla A intacto; `panel-actualidad.tsx` la importa desde
`@/lib/panel-camara`. Razón (B-3): los planes 03 y 04 la necesitan EN PARALELO en wave 2 y era
privada — sin dueño en wave 1 cada worktree inventaría su copia o crearía un ciclo de import
tiles↔panel-actualidad.
  </action>
  <verify><automated>cd app && grep -c "export function claseCamara" lib/panel-camara.ts && grep -c 'from "@/lib/panel-camara"' components/panel-actualidad.tsx && pnpm vitest run components/panel-actualidad.test.tsx</automated></verify>
  <acceptance_criteria>
    - `lib/panel-camara.ts` exporta `claseCamara` verbatim; `panel-actualidad.tsx` la importa y NO conserva copia local (grep de `function claseCamara` en el componente == 0).
    - `claseCamara("2022-2026 (piso de corpus)")` sigue devolviendo null (test unit nuevo de 3 casos: Cámara, Senado, piso-de-corpus).
    - Suite del panel verde.
  </acceptance_criteria>
  <done>claseCamara vive exportada en lib con dueño de wave 1; 03 y 04 la importan sin colisión.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 1: Contrato TS + parse defensivo del jsonb por señal</name>
  <files>app/lib/panel-evidencia.ts, app/lib/panel-evidencia.test.ts</files>
  <behavior>
    - `parseEvidenciaProyectos({})` (señal suprimida) → `{items:[],total:null,consultado_al:null,fuente:{origen:null,dataset:null}}` sin throw
    - `parseEvidenciaProyectos(null)` / `(undefined)` → mismo objeto vacío, sin throw
    - item de `velocity` (sin clave `descripcion`) → `descripcion === null` (NUNCA `undefined`)
    - item con `en_corpus` ausente → `en_corpus === false` (fail-closed: sin bandera no se enlaza)
    - `total` no numérico o ausente → `null`
    - `parseEvidenciaCitaciones` con `puntos` ausente → `puntos: []` y `puntos_total: null`
    - punto con `boletin:null` + `materia` largo → se conserva íntegro (el truncado es del render)
    - `parseEvidenciaSala` sobre la fila sintética de Cámara (`tipo/numero/hora_inicio` null, `tabla` ausente) → los tres scalars `null`, `tabla: []`, `tabla_total: 25`
    - item de `tabla` con `quorum:"SUMA (04.08.2026)"` → `quorum` verbatim (el parse no interpreta)
    - un item que no es objeto (string, number, null dentro del array) se DESCARTA, no rompe el parse
  </behavior>
  <action>
Crear `app/lib/panel-evidencia.ts` con los tipos e implementaciones de `<interfaces>` (parte de
parse). Prohibido `as` sobre el jsonb: usar type-guards por clave (`typeof x === "string" ? x : null`)
o `zod` — `zod` ya está en el stack; si se usa, los schemas deben ser `.catch()`/`.nullish()` de
forma que un shape parcial degrade a null en vez de lanzar (una señal mal formada NO debe tumbar
la portada).

Fuente ÚNICA del shape: `128-RESEARCH.md` §"Contrato REAL del jsonb en PROD". Los fixtures del test
se copian de ahí VERBATIM (las 4 muestras JSON: velocity, urgencias/archivados, agenda_citacion,
agenda_sala + la fila sintética de Cámara).

Documentar en el docblock las 3 asimetrías que el planner ya pagó: `velocity` sin `descripcion`
(P2), `agenda_sala` sin `semana_iso` (P3), `quorum` polisémico por cámara (P4).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/panel-evidencia.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; cero ocurrencias de ` as ` sobre el jsonb en el módulo (verificable por lectura: `grep -v '^\s*//' lib/panel-evidencia.ts | grep -c ' as Item'` == 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivaciones puras — etiquetaFuente, gradoUrgencia, cruce L5</name>
  <files>app/lib/panel-evidencia.ts, app/lib/panel-evidencia.test.ts</files>
  <behavior>
    - `etiquetaFuente({dataset:"tramitacion",origen:"plataforma-tramitacion"})` → `"Tramitación"`
    - `etiquetaFuente({dataset:"agenda",...})` → `"Agenda del Congreso"`
    - `etiquetaFuente({dataset:null,origen:null})` → `null` (el caller omite el footer de fuente; JAMÁS inventa "Fuentes del Congreso")
    - `etiquetaFuente` con un dataset desconocido (`"noticias"`) → devuelve el dataset verbatim, no un genérico
    - `gradoUrgencia("Discusión inmediata")` → `"Discusión inmediata"`; `gradoUrgencia("SUMA (04.08.2026)")` → `"Suma"` (solo el grado; el paréntesis NUNCA se afirma — R7)
    - `gradoUrgencia("literal desconocido")` → `"literal desconocido"` (fallback honesto, no null)
    - `gradoUrgencia(null)` → `null`
    - cruce: boletín con 4 urgencias en fechas distintas → devuelve la de `fecha` máxima
    - cruce: `18389-04` con `Simple 2026-07-06` y `Suma 2026-07-08` → devuelve `Suma / 2026-07-08` (por fecha, NO por grado)
    - cruce: dos urgencias con la MISMA fecha → desempate determinista y documentado (por orden de aparición), jamás aleatorio
    - cruce: items sin boletín o sin fecha se ignoran (no entran al Map)
  </behavior>
  <action>
Implementar en el mismo módulo `etiquetaFuente`, `gradoUrgencia` y `urgenciaVigentePorBoletin`.

`etiquetaFuente`: la etiqueta sale del DATO (D-02, el mapa hardcodeado por `tipo_senal` MUERE). La
tabla de conversión permitida es ÚNICAMENTE de legibilidad (restauración de tilde + nombre
ciudadano de los 2 datasets que PROD emite: `tramitacion`→`Tramitación`, `agenda`→`Agenda del
Congreso`); cualquier dataset fuera de esa tabla se devuelve verbatim. Documentar que esto NO es
un mapa semántico por tipo de señal.

`gradoUrgencia`: normaliza el literal de `descripcion` (o del `quorum` de Cámara) a uno de los 3
grados conocidos por comparación case-insensitive sobre el prefijo alfabético, ignorando cualquier
paréntesis adjunto. El `(dd.mm.aaaa)` tiene semántica NO verificada ⇒ se DESCARTA, jamás se
renderiza como fecha (R7). Fallback = literal completo tal como vino.

`urgenciaVigentePorBoletin`: recorre `ItemProyecto[]` de la señal `urgencias` y construye el Map
tomando la de `fecha` máxima por boletín. Ordenar por GRADO sería un ranking implícito y además
fabricaría un hecho (P7) — prohibido explícitamente en el docblock.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/panel-evidencia.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; el docblock del cruce declara la regla "más reciente por fecha, jamás por grado".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jsonb de PROD → tipos TS | dato semi-estructurado no validado, escrito por un proc SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04 | Tampering | parse del jsonb | mitigate | narrowing por clave / zod con degradación a null; prohibición de `as`; test de item no-objeto descartado |
| T-128-05 | Denial of Service | portada completa | mitigate | un shape parcial degrada a `[]`/`null`, jamás lanza — la portada no cae por una señal mal formada |
| T-128-06 | Repudiation | `gradoUrgencia` inventando un grado | mitigate | fallback al literal verbatim; `(dd.mm.aaaa)` descartado, nunca afirmado |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas (`zod` ya está en el stack) |
</threat_model>

<verification>
```bash
cd app && pnpm vitest run lib/panel-evidencia.test.ts   # verificar CONTEO de tests > 0
cd app && pnpm exec tsc -b --pretty false               # el contrato compila
```
</verification>

<success_criteria>
- Los 3 parsers toleran `{}`, `null` y shapes parciales sin throw.
- `descripcion` ausente (velocity) es `null`, nunca `undefined`.
- El cruce L5 devuelve la urgencia más reciente por fecha, con test del caso `18389-04`.
- Cero `as` sobre el jsonb en el módulo.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-02-SUMMARY.md` when done
</output>
