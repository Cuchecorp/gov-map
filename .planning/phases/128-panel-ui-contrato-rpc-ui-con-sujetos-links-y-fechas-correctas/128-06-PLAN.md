---
phase: 128-panel-ui
plan: 06
type: execute
wave: 3
depends_on: ["128-01", "128-02", "128-03", "128-04", "128-05"]
files_modified:
  - app/components/panel-actualidad.tsx
  - app/components/panel-actualidad.test.tsx
  - .gitignore
autonomous: true
requirements: [PANEL-02, PANEL-03, PANEL-05, PANEL-07]

must_haves:
  truths:
    - "La portada renderiza los 6 tiles editoriales en el orden D-01, con sala primero (O-5)"
    - "`agrupacion_materia` se filtra EXPLÍCITAMENTE: cero '(sin materia)' y cero heading 'Por materia' en el DOM"
    - "`\"datos al\"` tiene 0 ocurrencias en el HTML del panel, con `según fuente al` >= 1 como control positivo"
    - "`fecha_captura` jamás aparece en el DOM"
    - "El cruce L5 se calcula en render (server) y alimenta los chips de los tiles 1 y 2"
  artifacts:
    - path: "app/components/panel-actualidad.tsx"
      provides: "orquestador RSC del panel rediseñado"
      exports: ["PanelActualidad", "rotuloFecha", "SenalRow"]
    - path: "app/.artifacts/panel-render.html"
      provides: "volcado DOM del panel para los greps de cierre (D-10)"
  key_links:
    - from: "app/components/panel-actualidad.tsx"
      to: "app/components/panel-tile-sala.tsx"
      via: "render del tile 1"
      pattern: "PanelTileSala"
    - from: "app/components/panel-actualidad.tsx"
      to: "app/lib/panel-evidencia.ts"
      via: "urgenciaVigentePorBoletin (cruce L5 en render)"
      pattern: "urgenciaVigentePorBoletin"
---

<objective>
Cierre de la fase: reescribir `panel-actualidad.tsx` como orquestador de los 6 tiles editoriales,
matar lo que el research sentenció (`TITULO`, `FRAMING`, `fuenteLabel`, `TileSenal`, la única
ocurrencia funcional de `"datos al"`, el tile materia), calcular el cruce L5 en render, y producir
la evidencia de cierre (suite + greps DOM sobre el render local).

Purpose: hasta este plan los tiles existen pero la portada sigue mostrando el panel viejo.
Output: panel rediseñado + suite de tests actualizada + volcado DOM con los 5 greps en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-RESEARCH.md
@.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-CONTEXT.md

<read_first>
- `128-RESEARCH.md` §"Qué se conserva vs qué muere" (tabla línea por línea) y §"Tests existentes"
  (tabla describe por describe con el veredicto de cada uno)
- `app/components/panel-actualidad.tsx` (315 líneas) y `app/components/panel-actualidad.test.tsx` (237)
- Los 5 SUMMARYs de 128-01..128-05 (contratos ya construidos)
- `app/app/page.tsx:5,15,138` — monta `<PanelActualidad/>` en `<BentoGrid>` con Suspense y
  `dynamic="force-dynamic"`. **NO TOCAR ese archivo.**
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir panel-actualidad.tsx como orquestador de los 6 tiles</name>
  <files>app/components/panel-actualidad.tsx</files>
  <action>
Aplicar la tabla §"Qué se conserva vs qué muere" del research, literalmente:

CONSERVA: imports base · `interface SenalRow` (9 columnas; `evidencia` ahora SÍ se lee) ·
`TIPOS_AGENDA` · `fechaValida` · `rotuloFecha` con su docblock F-14/WR-05 íntegro (exportada, 5 tests
vivos) · `claseCamara` (regla A) · la mecánica de `PanelActualidad()`: `sb.rpc("actualidad_senales_panel",
{p_tipo:null})`, throw-on-error (#34, jamás `?? []`), agrupación en `Map porTipo`.

MUERE: `TITULO` · `FRAMING` · `fuenteLabel` (la fuente sale de `evidencia.fuente`, D-02) ·
`TileSenal` completo · `SPAN_TIPO` viejo · y con `TileSenal` muere la línea 245
(`{rotulo && <> · datos al {rotulo}</>}`), ÚNICA ocurrencia funcional de `"datos al"` en todo `app/`.
Verificar tras el cambio: `grep -rn --include=*.tsx --include=*.ts "datos al" app/ | grep -v '^\s*//'`
no debe devolver ninguna ocurrencia en código renderizado.

REESCRIBE el docblock de reglas A-F: la regla F (materia verbatim) MUERE con O-3; añadir las reglas
nuevas — links vía helper central con guard `en_corpus`; fechas de tres carriles (hecho con verbo en
el cuerpo / footer solo `según fuente al` / `fecha_captura` jamás visible); presupuesto O-7 de 4
ítems + "y N más" respaldado por total. Conservar A, B, C, D, E.

ORDEN D-01 (O-5: sala PRIMERO):
1 `PanelTileSala` (filas `agenda_sala`) → 2 `PanelTileComisiones` (filas `agenda_citacion`) →
3 `PanelTileUrgencias` (`urgencias`) → 4 `PanelTileMovimiento` (`velocity`) →
5 `PanelTileVotaciones` (async, sin filas) → 6 `PanelTileIngresos` (`nuevos_ingresos` + `archivados`).
Definir los `span` de cada tile en un mapa nuevo. El tile materia NO se monta.

FILTRO EXPLÍCITO (O-3 / P8): `agrupacion_materia` sigue emitiendo 10 filas en la DB. El ruteo NO
puede ser "lo que llegue": filtrar por una lista blanca explícita de los tipos que el panel renderiza,
con un comentario que diga que la señal sigue viva en la DB y que el tile murió sin tombstone.

CRUCE L5 EN RENDER (server): parsear la evidencia de `urgencias` con `parseEvidenciaProyectos` y
construir el Map con `urgenciaVigentePorBoletin` (128-02); pasarlo como prop `urgencias` a los tiles
1 y 2. Es una derivación en render, no una señal nueva ni una RPC nueva.

El tile 5 es `async`: montarlo dentro de su propio `<Suspense>` si el patrón del archivo lo requiere,
o simplemente `await`-earlo dentro de `PanelActualidad` (que ya es async). NO introducir `"use client"`
en ningún punto (D-08). NO tocar `app/app/page.tsx`.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc -b --pretty false && pnpm vitest run lib/anti-insinuacion-guard.test.ts lib/lockdown-guard.test.ts lib/bento-guards.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>El panel compila; `TITULO`/`FRAMING`/`fuenteLabel`/`TileSenal` ya no existen; `rotuloFecha` y `claseCamara` intactos; cero `"datos al"` en código renderizado; cero `"use client"` en el árbol del panel; guards verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescribir la suite de panel-actualidad y actualizar el fixture stale</name>
  <files>app/components/panel-actualidad.test.tsx</files>
  <behavior>
    - `makeSenal()` actualizado: `cobertura_camara` por defecto `"Cámara de Diputados"` (la grafía `"C.Diputados"` es STALE desde 127) y `evidencia` con un payload realista (no `{}`)
    - Filas con `tipo_senal:"agrupacion_materia"` presentes en la entrada → el DOM NO contiene `(sin materia)` ni un heading `Por materia`; control positivo apareado: los tiles de las otras señales SÍ se renderizan
    - El texto del panel completo NO contiene `"datos al"`; control positivo: SÍ contiene `"según fuente al"` al menos una vez
    - El DOM no contiene la cadena `fecha_captura` ni ninguna fecha de captura; control positivo: la fecha del hecho SÍ aparece
    - El orden del DOM es sala → comisiones → urgencias → movimiento → votaciones → ingresos (O-5/D-01), asertado por el orden de aparición de los headings
    - Los chips L5 aparecen en los tiles 1 y 2 cuando el boletín tiene urgencia vigente (el cruce se hizo en el orquestador)
    - Señal SUPRIMIDA (`nuevos_ingresos`) → causa verbatim + `en las fuentes consultadas al`, cero `"0"` mudo (test CONSERVADO)
    - WR-01: el chip nunca filtra los tokens internos de ventana (`30d`/`futuras`) — test CONSERVADO
    - Ausencia de vocabulario de ranking (`top`, `los más`, `la cámara más activa`) — test CONSERVADO y ampliado
    - Los 5 tests F-14 de `rotuloFecha` se CONSERVAN íntegros y siguen pasando
  </behavior>
  <action>
Reescribir `panel-actualidad.test.tsx` aplicando la tabla §"Tests existentes" del research, describe
por describe: los 3 describes marcados MUERE se eliminan o INVIERTEN (el de `datos al` invierte a
`not.toContain("datos al")` + `toContain("según fuente al")`); WR-02 se REEMPLAZA por los títulos
editoriales nuevos; los marcados CONSERVA se mantienen palabra por palabra.

El fixture `makeSenal()` debe reflejar el contrato REAL: grafía viva y `evidencia` con el shape de
`128-RESEARCH.md`. Un fixture stale hace pasar tests sobre una grafía que PROD ya no emite (P5).

Los tests que necesitan render del panel completo prueban la composición de VISTAS PURAS (los tiles
reciben filas como props) — cero DB, cero mocks de red. El wrapper async de votaciones no se ejerce
aquí (su cobertura vive en 128-05); si el orquestador lo llama, el test compone la vista con un
stub de items.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-actualidad.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>Todos los casos de `<behavior>` pasan; los 5 tests F-14 siguen presentes y verdes; el fixture ya no contiene `"C.Diputados"`.</done>
</task>

<task type="auto">
  <name>Task 3: Evidencia de cierre — volcado DOM + los 5 greps normalizados</name>
  <files>app/components/panel-actualidad.test.tsx, .gitignore</files>
  <action>
Añadir en `panel-actualidad.test.tsx` un test de cierre que renderice el panel completo con los
fixtures realistas y ESCRIBA el HTML resultante a `app/.artifacts/panel-render.html`
(`fs.mkdirSync` recursivo + `writeFileSync`). Ese archivo es el instrumento de la evidencia D-10
(correctitud por render local; el "queda bien" visual es de 129).

Añadir `app/.artifacts/` a `.gitignore` (artefacto de test, no se versiona).

El mismo test asserta en-proceso los 5 controles (para que CI los muerda aunque nadie corra el
shell), y el bloque `<verify>` los repite sobre el archivo con la instrumentación correcta:
`grep -o … | wc -l` (jamás `grep -c`, que topa en 1 sobre HTML de una línea), sin combinar `-i` con
`-F`, sin encadenar `pipefail` + `grep -q`. Cada cero fuerte va apareado con un control positivo.

⚠ React intercala `<!-- -->` entre expresiones adyacentes: los literales a grepear deben ser
fragmentos que no crucen una interpolación (`datos al`, `según fuente al`, `(sin materia)`,
`fecha_captura`, `href="/proyecto/`).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-actualidad.test.tsx >/dev/null 2>&1; H=.artifacts/panel-render.html; cd app && echo "datos_al=$(grep -o 'datos al' $H | wc -l) segun=$(grep -o 'según fuente al' $H | wc -l) materia=$(grep -o '(sin materia)' $H | wc -l) captura=$(grep -o 'fecha_captura' $H | wc -l) links=$(grep -o 'href="/proyecto/' $H | wc -l)"</automated>
  </verify>
  <done>El volcado existe y los 5 greps dan: `datos al`=0, `según fuente al`>=1, `(sin materia)`=0, `fecha_captura`=0, `href="/proyecto/`>=1. `.artifacts/` ignorado por git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| señales de la DB → ruteo de tiles | una señal no prevista (p.ej. `agrupacion_materia`) reaparecería en la portada si el ruteo fuera permisivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-20 | Repudiation | ruteo "lo que llegue" | mitigate | lista blanca explícita de tipos renderizados; test con filas `agrupacion_materia` en la entrada y cero `(sin materia)` en el DOM |
| T-128-21 | Repudiation | `"datos al"` sobre un hecho futuro | mitigate | el molde muere con `TileSenal`; grep normalizado sobre el volcado + assert en-proceso |
| T-128-22 | Information Disclosure | `fecha_captura` presentada como el hecho | mitigate | la columna no se selecciona ni se renderiza; grep de cero con control positivo |
| T-128-23 | Denial of Service | error de lectura convertido en "sin señales" | mitigate | throw-on-error (#34) conservado; `[]` reservado al path legítimo de 0 filas |
| T-128-SC | Tampering | npm installs | mitigate | cero dependencias nuevas |
</threat_model>

<verification>
```bash
cd app && pnpm test            # suite completa — baseline v12.0 = 1590; meta de fase >= 1630
cd app && pnpm guards          # anti-insinuación (carril PANEL), lockdown, bento, antiflip
cd app && pnpm exec tsc -b --pretty false
# 5 greps DOM sobre app/.artifacts/panel-render.html (ver Task 3)
```
Verificar SIEMPRE el conteo de tests ejecutados, no solo el exit code.
</verification>

<success_criteria>
- Los 6 tiles se montan en el orden D-01 con sala primero; el tile materia no está en el DOM.
- `agrupacion_materia` filtrada explícitamente con test que lo prueba.
- `"datos al"` = 0 y `"según fuente al"` >= 1 en el volcado DOM; `fecha_captura` = 0; `href="/proyecto/` >= 1.
- Los 5 tests F-14 conservados; fixture actualizado a la grafía viva.
- Suite >= 1630 y `pnpm guards` verdes; cero `"use client"`, cero RPC nueva, cero flags tocados.
</success_criteria>

<output>
Create `.planning/phases/128-panel-ui-contrato-rpc-ui-con-sujetos-links-y-fechas-correctas/128-06-SUMMARY.md` when done
</output>
