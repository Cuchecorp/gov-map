---
phase: 72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - .planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md
autonomous: false
requirements: [MONEY-03]

must_haves:
  truths:
    - "Existe un runbook operador-LOCAL que documenta cómo aplicar 0052 al remoto PROD (psql --db-url --single-transaction, PGCLIENTENCODING=UTF8, esquivar BOM del .env, NUNCA supabase db push) y cómo correr el pgTAP contra el schema aplicado"
    - "El runbook verifica el nombre real del constraint (cruce_senal_tipo_senal_check) contra pg_constraint ANTES del drop, por si un forward-fix lo renombró"
    - "El runbook deja explícito que la señal rinde 0 filas HOY (RUT-01 0% + backfill ChileCompra pendiente) = vacío honesto correcto, y que la señal real se puebla cuando el operador aplique RUT-01 + backfill ChileCompra"
    - "El runbook deja explícito que MONEY_PUBLIC_ENABLED permanece OFF (el flip es el sign-off legal de Phase 73, acto humano); esta fase NO presenta la señal"
    - "El checkpoint de operador es blocking-human: el agente NO aplica el DDL a PROD en esta corrida"
  artifacts:
    - path: ".planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md"
      provides: "Procedimiento operador-LOCAL de aplicación de 0052 + verificación pgTAP + estado de la señal (vacío honesto) + confirmación MONEY OFF"
      min_lines: 30
  key_links:
    - from: "72-APPLY-RUNBOOK.md"
      to: "supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql"
      via: "psql --db-url --single-transaction"
      pattern: "0052"
---

<objective>
Producir el runbook operador-LOCAL que documenta la aplicación de la migración `0052` al remoto PROD y su verificación pgTAP, y registrar el checkpoint blocking-human. El agente NO aplica el DDL a PROD en esta corrida (patrón repetido en 0023/0038/0039/0049: build/typecheck son falso positivo; la única prueba válida es el pgTAP contra el schema aplicado, que corre el operador).

Purpose: cerrar MONEY-03 en su parte offline-verificable (migración + pgTAP escritos y validados en el Plan 01) dejando la aplicación controlada como deuda de operador explícita, con el gate MONEY OFF hasta el sign-off legal de Phase 73.

Output: `72-APPLY-RUNBOOK.md` + un checkpoint de operador registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-RESEARCH.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-VALIDATION.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-01-PLAN.md
@supabase/migrations/0039_cruce_senal.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el runbook operador-LOCAL de aplicación de 0052 + verificación pgTAP</name>
  <files>.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md</files>
  <read_first>
    - supabase/migrations/0039_cruce_senal.sql (encabezado con el patrón de aplicación de operador: psql --db-url, BOM, db push prohibido, checkpoint)
    - supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql (la migración a aplicar, escrita en Plan 01)
    - supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql (el pgTAP a correr contra el schema aplicado)
    - .planning/STATE.md (líneas 90/109/110: RUT-01 0%, backfill ChileCompra pendiente, MONEY OFF; el patrón de "deuda operador-LOCAL" de 66-02/67-02/70-03/71-03)
  </read_first>
  <action>
    Crear `72-APPLY-RUNBOOK.md` espejando el tono y estructura de los runbooks operador previos (66-BACKFILL-RUNBOOK / 70-BACKFILL-CHILECOMPRA-RUNBOOK). Secciones:

    (1) Precondición — verificar el nombre real del constraint contra el schema aplicado ANTES del drop: `select conname from pg_constraint where conrelid='public.cruce_senal'::regclass and contype='c';` — si no es exactamente `cruce_senal_tipo_senal_check` (por un forward-fix que lo renombró), ajustar el `drop constraint` de 0052 al nombre real antes de aplicar (Pitfall A1 de RESEARCH).

    (2) Aplicación — el comando exacto: `psql "<SUPABASE_DB_URL>" --single-transaction -f supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql` con `PGCLIENTENCODING=UTF8` (Windows) y `SUPABASE_DB_URL` extraído del `.env` esquivando el BOM UTF-8 (pasar `--db-url` explícito). NUNCA `supabase db push` (drift de `schema_migrations`). Documentar que este DDL es idempotente en su parte de función (`create or replace`) pero el `drop constraint`/`add constraint` NO es re-ejecutable dos veces sin el guard del nombre (si ya se aplicó, el segundo drop falla) — aplicar UNA vez.

    (3) Verificación — correr el pgTAP contra el schema aplicado: `PGCLIENTENCODING=UTF8 psql "<SUPABASE_DB_URL>" -tA -f supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql` y esperar todas las líneas `ok` (0 `not ok`). Documentar que build/typecheck NO prueban el DDL (falso positivo de CI conocido).

    (4) Estado de la señal HOY — dejar EXPLÍCITO que tras aplicar, `select count(*) from cruce_senal where tipo_senal='lobby_sector_aporte';` = 0 y eso es CORRECTO (vacío honesto): RUT-01 está a 0% de cobertura + el backfill ChileCompra es deuda de operador pendiente (STATE.md). La señal real se poblará cuando el operador aplique RUT-01 (checkpoint Phase 69) + el backfill ChileCompra por RUT (runbook 70-03) y luego corra `select cruces.materializar_cruces();` (o espere al cron `cruces-materializar '23 3 * * *'`, que ya la invoca).

    (5) Gate MONEY — dejar EXPLÍCITO que `MONEY_PUBLIC_ENABLED` permanece OFF: la señal se materializa OFF-line pero NO se presenta hasta el sign-off legal 21.719 de Phase 73 (acto humano exclusivo). Esta fase NO toca el flag, NO añade grants/policies, NO monta superficie. El RPC `cruces_de_parlamentario` (0040, genérico por tipo_senal) hereda el token nuevo automáticamente pero sigue sin grant a anon.

    (6) Rollback — documentar el rollback del DDL si el pgTAP falla: revertir el constraint a `check (tipo_senal in ('lobby_sector'))` y re-emitir `cruces.materializar_cruces()` desde 0039 (la función previa).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md','utf8'); const a=[['min-lines',t.split('\n').length>=30],['psql --db-url',/psql\s+\"?\$?\{?SUPABASE_DB_URL|psql\s+\"<SUPABASE_DB_URL/i.test(t)||/--single-transaction/i.test(t)],['no db push',/no?\s*.*supabase\s+db\s+push|NUNCA\s+.*db\s+push/i.test(t)],['pgTAP',/\.test\.sql|pgtap|-tA -f/i.test(t)],['verifica constraint',/pg_constraint|conname/i.test(t)],['vacío honesto 0',/0\s+filas|count\(\*\)\s*=\s*0|vac[ií]o honesto/i.test(t)],['MONEY OFF',/MONEY_PUBLIC_ENABLED/i.test(t)&&/OFF/i.test(t)],['Phase 73 flip',/Phase\s*73|sign-?off|21\.719/i.test(t)],['0052',/0052/.test(t)]]; let ok=true; for(const[n,p]of a){console.log((p?'PASS':'FAIL')+': '+n); if(!p)ok=false;} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `72-APPLY-RUNBOOK.md` existe con >=30 líneas
    - Documenta la verificación del nombre del constraint contra `pg_constraint` antes del drop
    - Documenta la aplicación por `psql --db-url --single-transaction`, `PGCLIENTENCODING=UTF8`, BOM esquivado, y prohíbe `supabase db push`
    - Documenta la corrida del pgTAP 0052 contra el schema aplicado (esperar 0 `not ok`)
    - Deja explícito que la señal rinde 0 filas HOY (vacío honesto) y cuándo se poblará (RUT-01 + backfill ChileCompra)
    - Deja explícito que `MONEY_PUBLIC_ENABLED` permanece OFF y que el flip es Phase 73 (sign-off 21.719)
    - Incluye rollback del DDL
  </acceptance_criteria>
  <done>El runbook operador-LOCAL existe, cubre precondición/aplicación/verificación/estado-de-señal/gate-MONEY/rollback; el verify automatizado pasa.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>La migración aditiva `0052` (CHECK ampliado + rama `lobby_sector_aporte` en `cruces.materializar_cruces()`) + su pgTAP + el runbook de aplicación (`72-APPLY-RUNBOOK.md`). Todo escrito y validado estructuralmente offline. El agente NO aplicó el DDL a PROD (patrón operador-LOCAL, mismo que 0023/0038/0039/0049).</what-built>
  <how-to-verify>
    1. Revisar `72-APPLY-RUNBOOK.md` y seguir la precondición: `psql "<SUPABASE_DB_URL>" -tAc "select conname from pg_constraint where conrelid='public.cruce_senal'::regclass and contype='c';"` — confirmar que el constraint del token es `cruce_senal_tipo_senal_check` (ajustar el `drop` de 0052 si difiere).
    2. Aplicar 0052: `PGCLIENTENCODING=UTF8 psql "<SUPABASE_DB_URL>" --single-transaction -f supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql` (NUNCA `supabase db push`).
    3. Correr el pgTAP contra el schema aplicado: `PGCLIENTENCODING=UTF8 psql "<SUPABASE_DB_URL>" -tA -f supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql` — esperar todas las líneas `ok`, 0 `not ok`.
    4. Confirmar vacío honesto: `psql "<SUPABASE_DB_URL>" -tAc "select count(*) from cruce_senal where tipo_senal='lobby_sector_aporte';"` = 0 HOY (correcto; se poblará con RUT-01 + backfill ChileCompra).
    5. Confirmar MONEY OFF: `MONEY_PUBLIC_ENABLED` sigue sin `"true"` en el entorno de producción.
  </how-to-verify>
  <resume-signal>Escribir "aplicado" (con el resultado del pgTAP) o describir el fallo (nombre de constraint distinto, error de aplicación, o pgTAP rojo).</resume-signal>
</task>

</tasks>

<verification>
- Task 1 verify: el runbook cubre precondición/aplicación/pgTAP/estado-señal/gate-MONEY/rollback.
- Task 2 (checkpoint): aplicación a PROD + pgTAP verde = acto de operador (blocking-human), fuera de esta corrida.
</verification>

<success_criteria>
1. Existe el runbook operador-LOCAL de aplicación de 0052 + verificación pgTAP (SC#2 en su parte de aplicación controlada).
2. La aplicación a PROD queda como checkpoint blocking-human (el agente no toca PROD).
3. El runbook deja explícito el vacío honesto de la señal (SC#3) y el gate MONEY OFF hasta Phase 73.
</success_criteria>

<output>
Create `.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-02-SUMMARY.md` when done
</output>
