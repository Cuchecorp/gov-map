---
phase: 78-bento-home-actualidad-votado-urgencias-frescura-como-tiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/actualidad-module.tsx
  - app/components/actualidad-module.test.tsx
  - app/app/page.tsx
  - app/app/page.test.tsx
autonomous: true
requirements: [BENTO-03]

must_haves:
  truths:
    - "El ciudadano ve 'Votado esta semana' como tile span-4 con barra civic por cámara (azul Cámara / burdeos Senado) y tally mono en-dash, o el empty honesto si no hay votaciones"
    - "El ciudadano ve 'Urgencias vigentes' como tile span-2 con chip pill del tipo (suma/simple) en fondo petróleo suave"
    - "El ciudadano ve el strip 'Última actualización de datos' span-6 con dot petróleo + fuente + fecha mono, y el strip desaparece si no hay datos"
    - "El módulo lineal viejo (ActualidadModule / Panel) ya no se renderiza; los 3 bloques viven dentro del BentoGrid de la home en orden votado→urgencias→frescura"
    - "La barra de cámara se omite honestamente cuando la votación no trae cámara (nunca se adivina)"
  artifacts:
    - path: "app/components/actualidad-module.tsx"
      provides: "3 *View restilados a BentoTile + fetchers verbatim + camara proyectada; Panel y ActualidadModule wrapper borrados"
      contains: "bg-[var(--camara)]"
    - path: "app/app/page.tsx"
      provides: "3 fetchers montados en BentoGrid bajo las entry tiles; <ActualidadModule/> retirado; force-dynamic conservado"
      contains: "VotadoEstaSemana"
    - path: "app/components/actualidad-module.test.tsx"
      provides: "tests migrados a la forma tile + asserts de barra cámara/chip/strip + GATE §9.1 conservado"
      contains: "makeVotado"
    - path: "app/app/page.test.tsx"
      provides: "assert de retiro del módulo lineal + montaje de tiles + force-dynamic"
      contains: "force-dynamic"
  key_links:
    - from: "app/components/actualidad-module.tsx VotadoEstaSemana()"
      to: "sb.from('votacion').select"
      via: "columna camara añadida al select existente"
      pattern: "select\\([^)]*camara"
    - from: "app/app/page.tsx"
      to: "BentoGrid"
      via: "3 <Suspense> con fetchers como hijos del grid"
      pattern: "<Suspense"
---

<objective>
Migrar los 3 bloques de `ActualidadModule` (Votado, Urgencias, Frescura) desde el módulo lineal viejo a tiles del `BentoGrid` de la home: "Votado esta semana" span-4, "Urgencias vigentes" span-2, strip "Última actualización de datos" span-6. Es presentación 100%: los fetchers async, `urgenciaVigente()`, `leerTitulos()`, la disciplina `#34` (throw-on-read-error), los 3 empty-states honestos y el allowlist NO-PII `FUENTES_FRESCURA` se conservan verbatim. El ÚNICO toque de datos sancionado: añadir la columna `camara` (no-PII, ya existe en `votacion`) al `.select()` existente de Votado para dibujar la barra civic honesta.

Purpose: Completa la mitad inferior del bento de la home (BENTO-03). Deja la portada bento entera montada y deployable.
Output: `actualidad-module.tsx` restilado (Panel + wrapper borrados), `page.tsx` con los 3 fetchers en el grid, tests migrados verdes, tsc limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-bento-home-actualidad-votado-urgencias-frescura-como-tiles/78-RESEARCH.md
@.planning/phases/78-bento-home-actualidad-votado-urgencias-frescura-como-tiles/78-UI-SPEC.md

<interfaces>
<!-- Contratos exactos leídos del código esta sesión. El ejecutor NO necesita explorar. -->

BentoTile (app/components/bento/bento-tile.tsx) — Server Component, cva + asChild/Slot:
```tsx
// variant="default" → "bg-card border border-border hover:border-accent-product rounded-[var(--radius-tile)] ... min-h-11"
// span 2|4|6 → md:col-span-{2|4|6}; base full-width (colapso ≤md gratis)
// asChild → Slot fusiona clases sobre el hijo (envolver un <section>)
<BentoTile variant="default" span={4} asChild><section className="p-6">…</section></BentoTile>
```

VotacionRow.camara (app/lib/types.ts:61): `camara: "diputados" | "senado"` — columna REAL, no-PII.

Helpers verbatim (NO re-implementar — el source-scan test falla si se redeclaran):
- app/lib/format.ts: `fechaCorta(date)` → "14 may 2026"; `conteoVotacion(si, no)` → "58–81" (en-dash U+2013)
- app/lib/utils.ts: `safeExternalHref(enlace)` → http/https o null
- app/components/estado-actual-block.tsx: `urgenciaVigente(eventos)` → { tipo, desde } | null

Tokens (YA definidos, NO crear):
- `--camara: hsl(213 94% 38%)` y `--senado: hsl(355 65% 38%)` = strings hsl() COMPLETOS en civic-tokens.css:10,16 → consumir con `bg-[var(--camara)]` / `bg-[var(--senado)]` (NUNCA `hsl(var(--camara))` = doble-hsl inválido; NUNCA `bg-camara` = util inexistente).
- `--accent-product-soft` YA genera la util `bg-accent-product-soft` (globals.css:87) → usar directa.
- `--radius-tile: 16px` (globals.css:34) horneado en BentoTile.

Estado actual de page.tsx (Phase 77): BentoGrid con hero span-4 + accent tile span-2 + `<nav className="contents">` con 3 entry tiles span-2. `<ActualidadModule />` montado en línea 175 BAJO el grid, dentro de `<main>`. `export const dynamic = "force-dynamic"` en línea 14.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Migrar tests de actualidad + page a la forma tile con asserts nuevos</name>
  <files>app/components/actualidad-module.test.tsx, app/app/page.test.tsx</files>
  <read_first>
    - app/components/actualidad-module.test.tsx (fixtures makeVotado/makeUrgencia/makeFrescura, GATE §9.1, source-scan invariantes)
    - app/app/page.test.tsx (mock ActualidadModule→null, Contract 3 force-dynamic)
    - 78-UI-SPEC.md §Testing Contract (líneas 197-211) y §Component Inventory (contratos por tile)
  </read_first>
  <behavior>
    En actualidad-module.test.tsx:
    - `makeVotado` gana campo `camara: "diputados"` por default; añadir overrides en los casos.
    - Votado con camara "diputados" → existe un marcador con clase `bg-[var(--camara)]` (querySelector `[class*="var(--camara)"]`) + meta muestra `· Cámara`; con "senado" → `bg-[var(--senado)]` + `· Senado`; con `camara: null` → NO existe marcador de barra y meta = `Votación del {fecha}` sin suffix. La barra lleva `aria-hidden`.
    - Votado sigue afirmando: heading "Votado esta semana", título/boletín-mono fallback, `El proyecto fue aprobado` + tally mono `58–81`, `resultado:null` omite la frase pero conserva `Votación del`, `Ver fuente oficial ↗` con href `https://camara.cl/votacion/1` + `target="_blank"` + `rel="noopener noreferrer"`, empty exacto `Sin votaciones registradas esta semana en las fuentes consultadas.`
    - Urgencias: chip renderiza `{it.tipo}` verbatim (fixture "Suma") con clases `bg-accent-product-soft` + `font-mono` + `text-[11px]`; `desde {fecha}` en font-mono; link `Ver proyecto →` href `/proyecto/16284-07`; empty `No hay urgencias vigentes registradas esta semana.` (NOTA: la vieja assertion de prosa `— urgencia Suma vigente desde el` se REEMPLAZA por la forma chip.)
    - Frescura: label `Última actualización de datos`; cada item = dot + fuente + fecha mono; con 2 items → 2 `span.font-mono`; con 0 items → `UltimaActualizacionView` NO renderiza contenido (el `describe` de "0 datos" se ajusta a la decisión OMITIR: assert que el container queda vacío / no aparece el label). Conservar el assert de que `FUENTES_FRESCURA` son labels no-PII.
    - GATE §9.1 negative-match (banned-vocab) se CONSERVA y sigue verde sobre los 3 bloques poblados con la nueva forma chip/barra.
    - Source-scan invariantes (`use client` ausente, ≥4 throws, import urgenciaVigente, cero `.from` PII, `tabla: "votacion"`/`"proyecto_ficha"`) se CONSERVAN.
    En page.test.tsx:
    - Contract 3 se AMPLÍA: `<ActualidadModule/>` lineal (section `aria-label="Actualidad"` con `max-w-5xl`) YA NO se renderiza. Ajustar el mock: mockear los 3 fetchers (`VotadoEstaSemana`/`UrgenciasVigentes`/`UltimaActualizacion`) a `() => null` en vez del wrapper retirado. Assert: no existe `[aria-label="Actualidad"]`, no hay `max-w-5xl`; `force-dynamic` sigue exportado; los Contract 1/2 del hero/tiles siguen verdes.
  </behavior>
  <action>Ampliar los dos archivos de test para describir el comportamiento tile ANTES de tocar el código de producción. Añadir `camara: "diputados"` al retorno de `makeVotado`. Añadir los 3 casos de barra cámara (diputados/senado/null) apuntando a la clase `var(--camara)`/`var(--senado)` vía `container.querySelector('[class*="var(--camara)"]')` (jsdom NO resuelve CSS pero SÍ ve las clases en el atributo). Reemplazar la assertion de prosa de urgencia por la assertion del chip (`bg-accent-product-soft`, `font-mono`, `text-[11px]`, texto `Suma`). Ajustar el caso "0 datos" de frescura a la decisión OMITIR del strip (ver Open Question 2 del research → decisión: omitir tile cuando 0 items). En page.test.tsx, cambiar el `vi.mock("@/components/actualidad-module", …)` para exportar los 3 fetchers como `() => null` y añadir el assert de retiro del wrapper lineal. Estos tests deben FALLAR ahora (RED) porque el código aún no cambió — es lo esperado en Wave 0.</action>
  <verify>
    <automated>cd app && pnpm exec vitest run components/actualidad-module.test.tsx app/page.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>Los tests compilan (tsc de test OK) y FALLAN en los asserts nuevos de barra/chip/strip/retiro (RED esperado); los asserts heredados que aún aplican (headings, tally mono, empty states de votado/urgencias) pasan o fallan solo por la forma nueva, no por errores de sintaxis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Restilar los 3 *View a BentoTile + añadir camara + borrar Panel/wrapper</name>
  <files>app/components/actualidad-module.tsx</files>
  <read_first>
    - app/components/actualidad-module.tsx (fetchers, *View, Panel, ActualidadModule wrapper, query Votado línea 156-161)
    - 78-RESEARCH.md §Pattern 1/2/3 + §Code Examples + §Common Pitfalls (doble-hsl, "diputados" vs "camara", omit Ver todo, retirar Panel padding)
    - 78-UI-SPEC.md §Component Inventory (contrato por tile) + §Typography (tamaños: heading text-lg, item title text-[15px], meta text-xs mono)
  </read_first>
  <behavior>
    - `VotadoItem` gana `camara?: "diputados" | "senado" | null`. La query de `VotadoEstaSemana()` añade SOLO `camara` al `.select("boletin, resultado, total_si, total_no, fecha, enlace, camara")` existente (misma tabla `votacion` no-PII, mismo `.gte/.order/.limit`, mismo throw #34). El `Pick<VotacionRow, …>` y el `.map()` propagan `camara: v.camara ?? null`.
    - `VotadoEstaSemanaView` se envuelve en `<BentoTile variant="default" span={4} asChild><section className="p-6">…`. Heading `text-lg font-semibold`. Cada `<li>`: `flex gap-[14px] items-start border-t border-border pt-4 first:border-t-0 first:pt-0`. Barra: `it.camara && <span aria-hidden="true" className={\`w-[3px] self-stretch rounded-[2px] ${it.camara === "diputados" ? "bg-[var(--camara)]" : "bg-[var(--senado)]"}\`} />`. Columna de contenido: título (text-[15px] font-semibold leading-snug, boletín-mono fallback), frase desenlace + `conteoVotacion` mono (omitida si resultado null), meta `font-mono text-xs text-muted-foreground`: `{fechaCorta(it.fecha)}{it.camara ? ` · ${camaraLabel(it.camara)}` : ""}` con label `Votación del` conservado cuando NO hay cámara (`camara ? "{fecha} · {label}" : "Votación del {fecha}"`), link `Ver fuente oficial ↗` (safeExternalHref) / `Ver proyecto →` fallback verbatim. Helper local `camaraLabel = (c) => c === "diputados" ? "Cámara" : "Senado"`. "Ver todo →" OMITIDO (no existe ruta honesta — research A1/Pitfall 3).
    - `UrgenciasVigentesView` → `<BentoTile variant="default" span={2} asChild><section className="p-6">…`. Heading `text-lg font-semibold`. Cada `<li>` (`border-t border-border pt-3 first:border-t-0 first:pt-0`): chip `<span className="inline-flex items-center px-[9px] py-0.5 font-mono text-[11px] font-medium text-accent-product bg-accent-product-soft rounded-full">{it.tipo}</span>` → título (text-sm font-semibold leading-snug, boletín-mono fallback) → `desde {fechaCorta(it.desde)}` (font-mono text-xs text-muted-foreground) → `Ver proyecto →` link verbatim. La prosa vieja `{titulo} — urgencia {tipo} vigente desde el {fecha}.` se REEMPLAZA por chip+título+desde.
    - `UltimaActualizacionView` → strip: si `items.length === 0` retorna `null` (OMITIR el tile — decisión fijada). Con items: `<BentoTile variant="default" span={6} asChild><section className="py-[18px] px-6 flex items-center flex-wrap gap-y-2.5 gap-x-[22px]">` con label `Última actualización de datos` (text-[13px] font-semibold text-foreground mr-1.5) + por item un `inline-flex items-center gap-2`: dot `<span className="w-1.5 h-1.5 rounded-full bg-accent-product" aria-hidden="true" />` + `{it.fuente}` (text-[13px] text-muted-foreground) + `{fechaCorta(it.fecha)}` (font-mono text-[13px] text-foreground).
    - `Panel` helper BORRADO. `ActualidadModule` wrapper + `BloqueSkeleton` BORRADOS (el `<Suspense>` con skeleton se re-monta en page.tsx — ver Task 2; conservar/mover `BloqueSkeleton` a page.tsx O usar un skeleton honesto inline). Los fetchers async (`VotadoEstaSemana`/`UrgenciasVigentes`/`UltimaActualizacion`) + `leerTitulos` + `FUENTES_FRESCURA` + `inicioSemanaIso` + `fechaValida` + los 3 throws #34 se CONSERVAN verbatim (salvo el select de Votado). Sigue siendo RSC (sin "use client"). CERO hex; Tailwind `[var(--token)]` siempre.
  </behavior>
  <action>Editar `actualidad-module.tsx` in-place. Añadir `camara` a la interface `VotadoItem`, al `.select()`, al `Pick<>` y al `.map()` de `VotadoEstaSemana`. Restilar los 3 `*View` según §behavior: quitar cada `<Panel>` y envolver en `<BentoTile … asChild>` con `<section>` que absorba el padding (`p-6` / `py-[18px] px-6`). Definir `camaraLabel` local. Omitir "Ver todo →". Hacer que `UltimaActualizacionView` retorne `null` cuando `items.length === 0`. Borrar el helper `Panel`, el wrapper `ActualidadModule` y `BloqueSkeleton` (este último migra a page.tsx en Task 2). Conservar los fetchers, helpers, throws #34, `FUENTES_FRESCURA`, `inicioSemanaIso`, `fechaValida` intactos. Verificar CERO `#` hex, cero `hsl(var(--camara))`, cero `bg-camara`, cero `[--token]` bare.</action>
  <verify>
    <automated>cd app && pnpm exec vitest run components/actualidad-module.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Los tests de actualidad-module (migrados en Task 0) pasan VERDE: barra cámara por token en los 3 casos, chip urgencia, strip frescura (omitido en 0), empty states verbatim, GATE §9.1 y source-scan verdes. `grep -nE '#[0-9a-fA-F]{3,6}|hsl\(var\(|bg-camara|bg-senado[^-]|\[--' app/components/actualidad-module.tsx` no arroja violaciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Montar los 3 fetchers en el BentoGrid de page.tsx + retirar ActualidadModule</name>
  <files>app/app/page.tsx</files>
  <read_first>
    - app/app/page.tsx (BentoGrid con hero/accent/entry tiles, `<ActualidadModule />` línea 175, force-dynamic línea 14)
    - app/components/actualidad-module.tsx (exports de fetchers tras Task 1)
    - 78-UI-SPEC.md §Component Inventory "Collapse order" (orden DOM votado→urgencias→frescura DENTRO del mismo BentoGrid)
  </read_first>
  <behavior>
    - Los 3 fetchers (`VotadoEstaSemana`, `UrgenciasVigentes`, `UltimaActualizacion`) se importan de `@/components/actualidad-module` y se montan como hijos del `<BentoGrid>` existente, DESPUÉS del `<nav>` de entry tiles, cada uno bajo su propio `<Suspense fallback={<BloqueSkeleton …/>}>`, en orden votado → urgencias → frescura. El skeleton fallback debe declarar el span correcto (ser un BentoTile o llevar la clase `md:col-span-N`) para no romper el grid mientras carga.
    - El import y el montaje de `<ActualidadModule />` se ELIMINAN. El `<section aria-label="Actualidad">` lineal desaparece del árbol.
    - `export const dynamic = "force-dynamic"` se CONSERVA (frescura/votado leen datos vivos por request). Hero + accent tile + entry tiles del Phase 77 quedan INTACTOS.
    - `BloqueSkeleton` (movido desde actualidad-module en Task 1) se define en page.tsx o inline, honesto (aria-hidden, sin afirmar dato), con el span del tile que reemplaza.
  </behavior>
  <action>Editar `page.tsx`: reemplazar `import { ActualidadModule } from "@/components/actualidad-module"` por `import { VotadoEstaSemana, UrgenciasVigentes, UltimaActualizacion } from "@/components/actualidad-module"` y añadir `import { Suspense } from "react"`. Dentro del `<BentoGrid>`, tras el `</nav>` de entry tiles, añadir los 3 `<Suspense fallback={<BloqueSkeleton span={N} />}><Fetcher /></Suspense>` en orden votado(4)→urgencias(2)→frescura(6). Eliminar el `<ActualidadModule />` y su bloque de comentario del final del `<main>`. Definir `BloqueSkeleton` (movido desde actualidad-module) como un `<BentoTile variant="default" span={span} aria-hidden>` con placeholders `bg-muted`. Conservar `force-dynamic` y todo el hero/accent/entry. Verificar cero hex nuevo.</action>
  <verify>
    <automated>cd app && pnpm exec vitest run app/page.test.tsx components/actualidad-module.test.tsx 2>&1 | tail -20 && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>`page.test.tsx` verde: retiro del wrapper lineal (`aria-label="Actualidad"`/`max-w-5xl` ausentes), 3 fetchers montados en el grid, force-dynamic conservado, hero/tiles Phase 77 intactos. `tsc --noEmit` limpio. `<ActualidadModule/>` ya no se importa ni se renderiza.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente XML externa → render (`votacion.enlace`) | El `enlace` de la votación viene de una fuente gubernamental externa; cruza al DOM como `href`. |
| Supabase (service_role) → RSC | Lecturas server-side sobre tablas; `camara` es la única columna nueva proyectada. |
| mockup DesignCanvas → repo | Datos de ejemplo del mockup (títulos reales + tallies inventados) NO deben entrar como copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering / Elevation | `href` derivado de `votacion.enlace` (`javascript:`/`data:`) | mitigate | `safeExternalHref(it.enlace)` conservado verbatim + `rel="noopener noreferrer" target="_blank"`; fallback a `/proyecto/{boletin}` interno. |
| T-78-02 | Information Disclosure | `.from()` a tabla PII vía frescura o el nuevo select | mitigate | `FUENTES_FRESCURA` (allowlist no-PII) intacto; `camara` es enum de cámara (no PII) sobre `votacion` ya leída. El source-scan test `Bloque 3 lee … SOLO tablas NO-PII` (cero `.from` PII) muerde. |
| T-78-03 | Repudiation (integridad de fuente) | Datos fabricados del mockup presentados como reales | mitigate | Copy verbatim del módulo existente + empty-states honestos + throw #34; GATE §9.1 negative-match verde; barra/label omitidos honestamente si `camara` null (nunca se adivina cámara). |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | Este fase NO instala paquetes (cero deps nuevas — research §Standard Stack). Sin superficie de supply-chain; audit de legitimidad N/A. |
</threat_model>

<verification>
- `cd app && pnpm exec vitest run` → suite completa verde (base ~863 + ajustes de migración; el número exacto se confirma al correr — research A3).
- `cd app && pnpm exec vitest run anti-insinuacion` → verde (invariante 2).
- `cd app && pnpm exec tsc --noEmit` → limpio.
- `grep -rnE '#[0-9a-fA-F]{3,6}|hsl\(var\(|\[--[a-z]' app/components/actualidad-module.tsx app/app/page.tsx` → cero violaciones (cero hex, cero doble-hsl, cero `[--token]` bare).
- Barra cámara: `bg-[var(--camara)]`/`bg-[var(--senado)]` presentes; `bg-camara`/`bg-senado` ausentes.
</verification>

<success_criteria>
1. "Votado esta semana" tile span-4: barra 3px `bg-[var(--camara)]`/`bg-[var(--senado)]` por cámara (omitida si null) + label `· Cámara`/`· Senado`, tally mono en-dash verbatim (`conteoVotacion`), `Ver fuente oficial ↗` safeExternalHref-guarded, SIN "Ver todo →" (ruta inexistente), empty `Sin votaciones registradas esta semana en las fuentes consultadas.` verbatim.
2. "Urgencias vigentes" tile span-2: chip pill `bg-accent-product-soft` + font-mono del tipo verbatim, `desde {fecha}` mono, `urgenciaVigente()` intacto, empty verbatim.
3. Strip "Última actualización de datos" span-6: dot petróleo token + fuente + fecha mono, flex-wrap, OMITIDO cuando 0 items, solo fuentes NO-PII.
4. `ActualidadModule` wrapper + `Panel` retirados; 3 fetchers montados en el `BentoGrid` en orden votado→urgencias→frescura; `<ActualidadModule/>` no importado ni renderizado; `force-dynamic` conservado.
5. Único toque de datos = `camara` en el `.select()` de Votado; cero query/RPC nueva. Suite verde + tsc limpio + anti-insinuación verde + cero hex.
</success_criteria>

<output>
Create `.planning/phases/78-bento-home-actualidad-votado-urgencias-frescura-como-tiles/78-01-SUMMARY.md` when done.
</output>
