---
phase: 106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro
plan: 03
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - packages/llm-bench/src/tasks/juez/casos.json
  - packages/llm-bench/src/tasks/juez/casos.freeze.json
  - packages/llm-bench/src/tasks/juez/prompt_exemplars.json
  - packages/llm-bench/src/tasks/juez/scorer.ts
  - packages/llm-bench/src/tasks/juez/scorer.test.ts
  - packages/llm-bench/src/tasks/juez/disjuncion.test.ts
  - packages/llm-bench/src/tasks/extraccion/casos.json
  - packages/llm-bench/src/tasks/extraccion/casos.freeze.json
  - packages/llm-bench/src/tasks/extraccion/prompt_exemplars.json
  - packages/llm-bench/src/tasks/extraccion/scorer.ts
  - packages/llm-bench/src/tasks/extraccion/scorer.test.ts
  - packages/llm-bench/src/tasks/extraccion/disjuncion.test.ts
autonomous: true
requirements: [BENCH-02]
must_haves:
  truths:
    - "El golden de juez es pares (respuesta, etiqueta_humana) que rinden accuracy CONDICIONAL (cuando el juez dice OK, ¿cuán seguido la respuesta es correcta?)"
    - "El tipo CasoJuez lleva HOOKS de sesgo (productor del answer, ambos órdenes para posición, longitud para verbosidad) — medidos en 107, definidos y congelados en 106"
    - "Existe un split de calibración held-out disjunto del split de scoring, y un slot en el reporte para la curva de confiabilidad — el fit isotónica/Platt es 107"
    - "El golden de extracción separa schema-parse-rate de field-value accuracy (NUNCA colapsadas) + fidelidad literal por substring + negación es-CL"
    - "Cada tarea tiene su caso adversario aislado (juez que sella-de-goma / extracción que fabrica texto) probando que el gate PUEDE fallar, + guard freeze/no-RUT/disjunción que muerde"
  artifacts:
    - path: "packages/llm-bench/src/tasks/juez/casos.json"
      provides: "pares (answer, human_label) con hooks de sesgo, split scoring/calibración, es-CL, congelado"
      contains: "human_label"
    - path: "packages/llm-bench/src/tasks/extraccion/casos.json"
      provides: "golden de extracción literal es-CL con casos de negación, congelado"
      contains: "idea_matriz"
    - path: "packages/llm-bench/src/tasks/juez/scorer.ts"
      provides: "accuracy condicional (precisión del OK + recall de rechazo) + slots de sesgo + split calibración"
      min_lines: 50
    - path: "packages/llm-bench/src/tasks/extraccion/scorer.ts"
      provides: "schema-parse-rate SEPARADO de value-accuracy + precision/recall substring (fichas generalizado)"
      min_lines: 50
  key_links:
    - from: "packages/llm-bench/src/tasks/juez/scorer.ts"
      to: "conditional accuracy (Pitfall 1/2)"
      via: "precisión-del-OK + recall-de-rechazo"
      pattern: "condicional|rechazo|rubber|sella"
    - from: "packages/llm-bench/src/tasks/extraccion/scorer.ts"
      to: "packages/fichas/src/golden/golden-set.ts"
      via: "parse-rate SEPARATE from value-accuracy + substring fidelity"
      pattern: "parse|substring|fp|fabricad"
---

<objective>
Build the two remaining golden tasks — juez/validación and paridad-extracción — completing the four
BENCH-02 sets. The judge set is modeled as `(answer, human_label)` pairs yielding CONDITIONAL accuracy
(precision-of-OK + recall-of-rejection) with bias-metric HOOKS and a held-out calibration split baked in
(measured/fitted in 107, defined & frozen in 106). The extraction set generalizes the fichas golden:
schema-parse-rate kept SEPARATE from field-value accuracy, literal substring fidelity, and load-bearing
es-CL negation cases. Same freeze/no-RUT/disjunction guard trio and per-task adversarial meta-test.

Purpose: The judge and extraction tasks have distinct failure modes (rubber-stamping; fabricating text)
the two single-label tasks (Plan 02) do not cover. This plan makes their scorers honest per RESEARCH
§ Judge-Set Methodology and § Quality per task.
Output: Two frozen golden sets + scorers + guards, all green in CI (mock, no network).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-RESEARCH.md
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Precedents to generalize + Plan 01 report contract. -->

From packages/fichas/src/golden/golden-set.ts — the parse-vs-value + literal-fidelity + adversarial precedent:
```typescript
export function normalizarLiteral(s: string): string; // NFD + strip diacritics + collapse ws + lowercase
export interface MetricasGolden { tp; fp; fn; precision; recall; detalle; }
// idea_matriz: afirmada ∧ substring → tp; afirmada ∧ NO-substring (paráfrasis/alucinación) → fp; expected non-null ∧ null → fn
// cuerpos_legales: F1 by normalized key set (norma::articulos)
// IDS_CASOS_ADVERSARIOS + GOLDEN_SET_ADVERSARIO — isolated fabrication cases prove the gate can fail
// gate: precision >= 0.95, recall >= 0.80
```

From packages/llm-bench/src/report.ts (Plan 01):
```typescript
export type TaskId = "routing" | "clasificacion" | "juez" | "extraccion";
// this plan fills the juez + extraccion quality shapes in calidad_por_tarea
```

From packages/llm-bench/src/guards/{freeze,no-rut}.ts (Plan 02):
```typescript
export function hashCasos(raw: string): string;             // sha256
export function assertFrozen(raw: string, marker): void;
export function contieneRut(raw: string): boolean;          // NO-PII scan
```

Judge-set shape (RESEARCH § Judge-Set Methodology — DEFINE in 106, measure Phi-vs-human in 107):
```typescript
// CasoJuez = { id, answer, human_label: boolean, producer: string /*self-preference hook*/,
//              answerLen: number /*verbosity hook*/, orderings?: [A,B]|[B,A] /*position hook*/,
//              split: "scoring" | "calibracion" /*held-out disjoint from scoring*/, estrato }
// conditional accuracy: precision-of-OK = P(answer correct | judge says OK); recall-of-rejection = P(judge rejects | answer wrong)
```
</interfaces>

Source corpus (NO invented text, NO PII):
@packages/fichas/src/golden/golden-set.ts   <!-- extraction precedent + realistic es-CL legal text to seed extracción -->
@packages/fichas/src/golden/golden-set.test.ts   <!-- CI-mock / LIVE-gated split + gate contract to mirror -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extracción golden set — parse-rate SEPARATE from value-accuracy + es-CL negation + guard</name>
  <files>packages/llm-bench/src/tasks/extraccion/casos.json, packages/llm-bench/src/tasks/extraccion/casos.freeze.json, packages/llm-bench/src/tasks/extraccion/prompt_exemplars.json, packages/llm-bench/src/tasks/extraccion/scorer.ts, packages/llm-bench/src/tasks/extraccion/scorer.test.ts, packages/llm-bench/src/tasks/extraccion/disjuncion.test.ts, packages/llm-bench/src/index.ts</files>
  <read_first>
    - packages/fichas/src/golden/golden-set.ts (generalize evaluarGolden: idea_matriz substring fidelity + cuerpos F1 + normalizarLiteral + IDS_CASOS_ADVERSARIOS)
    - packages/fichas/src/golden/golden-set.test.ts (behavior contract + adversarial "gate can fail" meta-test + CI-mock/LIVE split)
    - 106-RESEARCH.md § Quality per task (paridad-extracción row: schema-parse-rate SEPARATE from field-value accuracy — Cleanlab; the failure mode is fabricating text)
    - 106-RESEARCH.md § Stratification axes (negation presence → dropped-negation inverts legal meaning; archaic register; scanned-PDF)
    - packages/llm-bench/src/guards/{freeze,no-rut}.ts + packages/llm-bench/src/metrics.ts (Plan 01: reuse structured-output/zod outcome for parse-rate)
  </read_first>
  <behavior>
    - evaluarExtraccion reports schema_parse_rate (did the payload parse+validate at all?) SEPARATELY from value-accuracy (precision/recall of literal substring fidelity). A 100%-parse / wrong-value model must show high parse-rate AND low value-precision — never one blended number.
    - idea_matriz value scoring is by literal substring on normalizarLiteral(text): afirmada∧substring→tp; afirmada∧NON-substring→fp (fabrication); expected-non-null∧null→fn; expected-null∧afirmada→fp (dishonest degradation).
    - At least 2 cases carry a load-bearing es-CL negation ("deróganse", "no será aplicable") where dropping the negation inverts meaning; the scorer catches an answer that drops it as fp.
    - Isolated adversarial case (fabricated idea/norma) makes the value-precision gate fail — meta-test proves the gate is alive.
  </behavior>
  <action>
    Create `packages/llm-bench/src/tasks/extraccion/casos.json` — a golden set (~40 cases) of literal
    extraction over REAL/realistic es-CL legal text (seed style from fichas golden), each case with `id`,
    `textoFuente`, `proyecto`, `expected` (`idea_matriz_substring: string|null`, `cuerpos: CuerpoLegal[]`),
    `estrato`, `muestra` flag, and ≥2 negation-bearing cases + ≥2 scanned-PDF-register cases. NO RUT/PII.
    Create `packages/llm-bench/src/tasks/extraccion/prompt_exemplars.json` disjoint from `casos.json`.
    Create `packages/llm-bench/src/tasks/extraccion/scorer.ts` generalizing fichas: `normalizarLiteral`,
    `evaluarExtraccion(set, ejecutar)` returning `{ schema_parse_rate, value: { tp, fp, fn, precision, recall }, detalle }`
    with parse-rate SEPARATE from value metrics (use Plan 01 `clasificarOutcome`/`agregarFallos` for the parse
    side); export GOLDEN_SET, GOLDEN_SET_GATE, IDS_CASOS_ADVERSARIOS, GOLDEN_SET_ADVERSARIO, `gatePasa`
    (precision ≥ threshold AND recall ≥ threshold — mirror fichas 0.95/0.80 or document a chosen bar).
    Create `packages/llm-bench/src/tasks/extraccion/casos.freeze.json` (sha256 marker).
    Create `packages/llm-bench/src/tasks/extraccion/scorer.test.ts` (parse-vs-value SEPARATION asserted +
    negation-drop → fp + adversarial meta-test) and `disjuncion.test.ts` (∩=∅, no-RUT, frozen-hash).
    Extend the barrel `src/index.ts`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test extraccion 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `evaluarExtraccion` returns `schema_parse_rate` and `value.{precision,recall}` as SEPARATE fields; a test constructs a 100%-parse/wrong-value case and proves parse-rate stays high while value-precision drops.
    - ≥2 negation-bearing cases exist; a test proves a dropped-negation answer scores as fp.
    - Isolated adversarial (fabricated) case makes the value-precision gate fail (meta-test green).
    - `disjuncion.test.ts` bites on ∩≠∅, RUT presence, or frozen-hash drift.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test extraccion` green; parse-rate separated from value, negation caught, gate can fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Juez golden set — (answer, human_label) pairs, conditional accuracy, bias hooks, calibration split</name>
  <files>packages/llm-bench/src/tasks/juez/casos.json, packages/llm-bench/src/tasks/juez/casos.freeze.json, packages/llm-bench/src/tasks/juez/prompt_exemplars.json, packages/llm-bench/src/tasks/juez/scorer.ts, packages/llm-bench/src/tasks/juez/scorer.test.ts, packages/llm-bench/src/tasks/juez/disjuncion.test.ts, packages/llm-bench/src/index.ts</files>
  <read_first>
    - 106-RESEARCH.md § Judge-Set Methodology (pairs; conditional accuracy = precision-of-OK + recall-of-rejection; bias hooks: self-preference/position/verbosity; calibration split held-out disjoint from scoring; judge NEVER approves in-design — that is 107/TIER)
    - 106-RESEARCH.md § Quality per task (juez row) + § State of the Art (judge vs HUMAN labels, not vs responder)
    - packages/cruces/src/golden/golden-set.ts (abstención/label discipline) + packages/fichas/src/golden/golden-set.ts (adversarial meta-test pattern)
    - packages/llm-bench/src/guards/{freeze,no-rut}.ts (guard trio)
  </read_first>
  <behavior>
    - Each case = (answer, human_label: boolean). Scorer computes conditional accuracy: precisión-del-OK = correct-answers-among-judge-OK / judge-OK; recall-de-rechazo = judge-rejects-among-wrong-answers / wrong-answers.
    - A rubber-stamp judge (always OK) scores near-0 recall-de-rechazo → exposed by the scorer (behavior test).
    - CasoJuez carries bias HOOKS (producer, answerLen, optional orderings) and a `split: "scoring"|"calibracion"`; the scoring metric uses ONLY the scoring split; the calibration split is reserved (fit is 107).
    - The Reporte has a slot for a reliability curve (bins of predicted-confidence vs observed-accuracy) left empty in 106.
    - Isolated adversarial case (a wrong answer the judge should reject) proves the rejection-recall metric can drop → gate/meta-test alive.
  </behavior>
  <action>
    Create `packages/llm-bench/src/tasks/juez/casos.json` — a golden set (~40 cases) of `(answer, human_label)`
    pairs over NON-PII task outputs (proposed sector classifications, proposed idea_matriz extractions —
    reuse the public corpus, NEVER adjudication/golden-1263). Each case: `id`, `answer`, `human_label: boolean`,
    `producer` (which model family produced the answer — self-preference hook), `answerLen` (verbosity hook),
    optional `orderings` (position hook for any pairwise case), `split: "scoring" | "calibracion"` (calibración
    disjoint from scoring), `estrato`, `muestra`. Include wrong-answer cases (human_label:false) so
    rejection-recall is measurable. NO RUT/PII.
    Create `packages/llm-bench/src/tasks/juez/prompt_exemplars.json` disjoint from `casos.json`.
    Create `packages/llm-bench/src/tasks/juez/scorer.ts`: zod-validate at load; export GOLDEN_SET,
    `GOLDEN_SET_SCORING` (split==="scoring"), `GOLDEN_SET_CALIBRACION` (split==="calibracion", disjoint),
    IDS_CASOS_ADVERSARIOS + GOLDEN_SET_ADVERSARIO, and `evaluarJuez(set, juzgar)` returning
    `{ precision_ok, recall_rechazo, n, detalle, hooks: { porProductor, porLongitud } }` (hooks aggregated but
    NOT interpreted in 106 — 107 measures bias). Provide `gatePasa` if a bar is defined, else document that
    106 only reports (measurement is 107). The judge is ESCALATE-ONLY by design note (never approves) — but
    that composition is 108/TIER; 106 only measures verdict-vs-human agreement.
    Create `packages/llm-bench/src/tasks/juez/casos.freeze.json` (sha256 marker).
    Create `packages/llm-bench/src/tasks/juez/scorer.test.ts` (rubber-stamp → near-0 rejection-recall;
    scoring split excludes calibración; hooks populated; adversarial meta-test) and `disjuncion.test.ts`
    (∩=∅ exemplar/eval, scoring∩calibración=∅, no-RUT, frozen-hash). Extend the barrel.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test juez 2>&1 | tail -20 && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Cases are `(answer, human_label)` pairs; a rubber-stamp judge (always OK) yields near-0 recall-de-rechazo (behavior test proves it).
    - `CasoJuez` carries `producer`, `answerLen`, optional `orderings`, and `split`; the scoring metric uses only the scoring split; `GOLDEN_SET_CALIBRACION` is disjoint (test asserts ∩=∅).
    - `evaluarJuez` returns `precision_ok` and `recall_rechazo` as separate numbers + populated bias `hooks` (not interpreted in 106).
    - Adversarial meta-test proves rejection-recall can drop; no-RUT + frozen-hash guards bite.
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test juez` green; judge set frozen as pairs with bias hooks + calibration split; conditional accuracy measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → harness | juez/extracción `casos.json` are untrusted-JSON validated by zod at load |
| golden → LLM (LIVE, 107) | a case carrying a RUT would cross to a provider |
| adjudication corpus | OFF-LIMITS — no juez case may source golden-1263 (LOCKED) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-07 | Information Disclosure | judge/extraction case carries RUT/PII | mitigate | `no-rut.ts` scan per casos.json; corpus is public bill data; golden-1263 untouched |
| T-106-08 | Tampering | golden edited after freeze | mitigate | `casos.freeze.json` sha256 asserted in disjuncion.test.ts |
| T-106-09 | Spoofing | judge measured vs responder instead of humans (false parity) | mitigate | judge golden is `(answer, human_label)` pairs — verdict compared to HUMAN label, never to the responder |
| T-106-10 | Repudiation | calibration threshold hand-picked (false confidence) | accept | 106 leaves the held-out calibration split + report slot; isotonic/Platt fit is 107 (LOCKED) |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new external package |
</threat_model>

<verification>
- `pnpm --filter @obs/llm-bench test juez` and `... test extraccion` green (mock, no network).
- Extracción: schema-parse-rate SEPARATE from value-accuracy (asserted); negation caught; gate can fail.
- Juez: conditional accuracy (precision-of-OK + recall-of-rejection) + bias hooks + disjoint calibration split; rubber-stamp exposed.
- `tsc -b` exit 0; root `pnpm test` not regressed at wave merge.
</verification>

<success_criteria>
- Four golden sets now exist and are frozen (routing+clasificación from Plan 02; juez+extracción here).
- Judge set is `(answer, human_label)` pairs with bias hooks + held-out calibration split (fit deferred to 107).
- Extraction keeps schema-parse-rate separate from field-value accuracy + literal fidelity + es-CL negation.
- Anti-leakage + no-RUT + freeze guards bite for both tasks; no 107 scope introduced.
</success_criteria>

<output>
Create `.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-03-SUMMARY.md` when done
</output>
