---
phase: 106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - packages/llm-bench/src/tasks/routing/casos.json
  - packages/llm-bench/src/tasks/routing/casos.freeze.json
  - packages/llm-bench/src/tasks/routing/prompt_exemplars.json
  - packages/llm-bench/src/tasks/routing/scorer.ts
  - packages/llm-bench/src/tasks/routing/scorer.test.ts
  - packages/llm-bench/src/tasks/routing/disjuncion.test.ts
  - packages/llm-bench/src/tasks/clasificacion/casos.json
  - packages/llm-bench/src/tasks/clasificacion/casos.freeze.json
  - packages/llm-bench/src/tasks/clasificacion/prompt_exemplars.json
  - packages/llm-bench/src/tasks/clasificacion/scorer.ts
  - packages/llm-bench/src/tasks/clasificacion/scorer.test.ts
  - packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts
  - packages/llm-bench/src/guards/freeze.ts
  - packages/llm-bench/src/guards/no-rut.ts
autonomous: true
requirements: [BENCH-02]
must_haves:
  truths:
    - "Existen golden sets es-CL congelados para routing y clasificación, estratificados del corpus real, sin PII/RUT"
    - "El scorer de cada tarea es single-label top-1 + abstención first-class (abstención NUNCA cuenta como error — precedente cruces)"
    - "Cada tarea tiene un caso adversario aislado que prueba que su gate PUEDE fallar (meta-test, no teatro)"
    - "Un guard CI muerde: exemplar-pool ∩ eval-pool = ∅, ningún caso lleva RUT, y el sha256 del casos.json coincide con su marcador de freeze"
    - "Los pools de ejemplo (prompt_exemplars) y de eval (casos.json) son disjuntos por construcción"
  artifacts:
    - path: "packages/llm-bench/src/tasks/routing/casos.json"
      provides: "golden set es-CL de routing (¿qué tarea/tier es este input?), estratificado, congelado"
      contains: "id"
    - path: "packages/llm-bench/src/tasks/clasificacion/casos.json"
      provides: "golden set es-CL de clasificación de sector (semilla cruces, sin re-exponer al prompt)"
      contains: "sector_codigo"
    - path: "packages/llm-bench/src/tasks/routing/scorer.ts"
      provides: "single-label top-1 + abstención + gatePasa + set adversario aislado"
      min_lines: 40
    - path: "packages/llm-bench/src/guards/freeze.ts"
      provides: "sha256 freeze marker helper (node:crypto)"
      min_lines: 10
    - path: "packages/llm-bench/src/guards/no-rut.ts"
      provides: "RUT regex scanner over golden JSON (NO-PII by construction)"
      min_lines: 8
  key_links:
    - from: "packages/llm-bench/src/tasks/routing/disjuncion.test.ts"
      to: "casos.freeze.json"
      via: "sha256 hash assertion"
      pattern: "createHash|sha256|freeze"
    - from: "packages/llm-bench/src/tasks/routing/scorer.ts"
      to: "packages/cruces/src/golden/golden-set.ts"
      via: "generalized single-label top-1 + abstención scoring"
      pattern: "abstenc|no-cubierto|misclasif"
---

<objective>
Build the two SINGLE-LABEL golden tasks — routing and clasificación — as frozen es-CL `casos.json`
sets stratified from the real corpus, each with a top-1 + first-class-abstention scorer generalized
from `packages/cruces/src/golden/golden-set.ts`, plus the CI guard-que-muerde (disjunction ∩=∅,
no-RUT scan, frozen-hash) that RESEARCH § Golden-Set Construction mandates.

Purpose: These are two of the four golden sets BENCH-02 requires. Their failure mode is *assigning
the wrong label* (not fabricating text), so they reuse the cruces scoring verbatim — abstention
lowers coverage but is NEVER an error. Freezing before any 107 integration is LOCKED.
Output: Two frozen golden sets + scorers + the shared freeze/no-RUT guards + per-task disjunction
tests, all green in CI (mock, no network).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-RESEARCH.md
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Precedent to generalize (read the source; do not re-invent scoring). -->

From packages/cruces/src/golden/golden-set.ts — the single-label top-1 + abstención precedent:
```typescript
export type ResultadoCaso = "correcto" | "no-cubierto" | "misclasificacion";
export interface MetricasGolden { total; correctos; noCubiertos; misclasificaciones; cobertura; errores; detalle; }
// actual===expected → correcto; actual===null → no-cubierto (NUNCA error, D-08); actual!==null≠expected → misclasificacion
// gatePasa = cobertura >= COBERTURA_MIN && errores === 0
// casos.json validated with z.array(CasoGoldenSchema).parse(casosRaw) at load
// GOLDEN_SET_GATE = casos with muestra:true
```

From packages/llm-bench/src/report.ts (Plan 01) — where these scorers plug their quality shape:
```typescript
export type TaskId = "routing" | "clasificacion" | "juez" | "extraccion";
// MetricasModelo.calidad_por_tarea: Partial<Record<TaskId, unknown>> — this plan's scorer fills routing + clasificacion
```

Freeze-guard shape (RESEARCH § Freezing + Code Examples):
```typescript
// casos.freeze.json = { hash: string (sha256 of casos.json bytes), fecha, n_casos, estratos }
// createHash("sha256").update(readFileSync(CASOS_PATH,"utf8")).digest("hex") === FREEZE.hash
```
</interfaces>

Source corpus for stratified sampling (NO invented text, NO PII):
@packages/cruces/src/golden/casos.json   <!-- reuse sector gold labels as SEEDS for clasificación (do NOT re-expose as prompt exemplars) -->
@packages/cruces/src/golden/golden-set.test.ts   <!-- CI-mock / LIVE-gated split + behavior contract to mirror -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared freeze + no-RUT guards</name>
  <files>packages/llm-bench/src/guards/freeze.ts, packages/llm-bench/src/guards/no-rut.ts, packages/llm-bench/src/index.ts</files>
  <read_first>
    - 106-RESEARCH.md § Freezing mechanism + § Code Examples (sha256 marker + guard-que-muerde)
    - packages/llm/src/data-routing.ts is NOT imported here — the golden must be RUT-free by construction; this guard is a static regex scan over the JSON bytes
    - packages/cruces/src/golden/golden-set.ts (parse-at-load zod compuerta pattern the sets reuse)
  </read_first>
  <action>
    Create `packages/llm-bench/src/guards/freeze.ts` exporting `hashCasos(raw: string): string`
    (`createHash("sha256").update(raw).digest("hex")` from `node:crypto`) and a `FreezeMarker` type
    `{ hash: string; fecha: string; n_casos: number; estratos: string[] }`. Provide `assertFrozen(rawBytes: string, marker: FreezeMarker): void`
    (throws if hash mismatch) so any post-freeze edit fails CI until the marker is deliberately re-cut.
    Create `packages/llm-bench/src/guards/no-rut.ts` exporting `RUT_RE` (Chilean RUT pattern, e.g.
    `\d{1,3}(?:\.\d{3})*-[\dkK]`) and `contieneRut(raw: string): boolean`. This is the NO-PII-by-construction
    scanner run over each `casos.json`'s raw bytes.
    Both are pure, dependency-free, no network. Export both from the barrel `src/index.ts`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `hashCasos` uses `node:crypto` `createHash("sha256")` (grep confirms), no hand-rolled hashing.
    - `assertFrozen` throws on hash mismatch (a later disjunction test exercises this).
    - `contieneRut` returns true for a sample RUT string `"12.345.678-9"` and false for RUT-free legal text (unit-checked in Task 2/3 disjunction tests).
  </acceptance_criteria>
  <done>Guards compile; freeze + no-RUT helpers exported and importable by both task disjunction tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Routing golden set + top-1/abstención scorer + disjunction guard</name>
  <files>packages/llm-bench/src/tasks/routing/casos.json, packages/llm-bench/src/tasks/routing/casos.freeze.json, packages/llm-bench/src/tasks/routing/prompt_exemplars.json, packages/llm-bench/src/tasks/routing/scorer.ts, packages/llm-bench/src/tasks/routing/scorer.test.ts, packages/llm-bench/src/tasks/routing/disjuncion.test.ts</files>
  <read_first>
    - packages/cruces/src/golden/golden-set.ts (generalize evaluarGolden/gatePasa/MetricasGolden verbatim — top-1 + abstención)
    - packages/cruces/src/golden/golden-set.test.ts (the 3-case behavior contract: all-correct→pass, all-abstain→0 coverage/0 errors, one-wrong→fail; adapt for routing)
    - 106-RESEARCH.md § Golden-Set Construction (stratification axes: doc format, legal register, length, chamber, negation) + § Judge/routing scoring row
    - 106-RESEARCH.md § Anti-leakage (disjoint prompt_exemplars vs casos.json + biting guard) + § Size (~40, prioritize stratum coverage)
    - packages/llm-bench/src/guards/{freeze,no-rut}.ts (from Task 1)
  </read_first>
  <behavior>
    - evaluarRouting scores single-label top-1: actual===expected → correcto; actual===null (abstención) → no-cubierto (NUNCA error); actual!==null≠expected → misclasificación.
    - gatePasa(m) = cobertura >= UMBRAL && errores === 0 (abstención lowers coverage but never fails via errors).
    - An isolated adversarial case (GOLDEN_SET_ADVERSARIO) with a deliberately wrong label proves gatePasa CAN return false — the "gate can fail" meta-test.
    - disjuncion.test.ts: exemplarIds ∩ evalIds = ∅; contieneRut(casos raw) === false; hashCasos(casos raw) === freeze.hash.
  </behavior>
  <action>
    Create `packages/llm-bench/src/tasks/routing/casos.json` — a golden set (~40 cases, prioritize
    stratum coverage) sourced from REAL tramitación/citación metadata already in-repo. The routing label
    is which task/tier an input belongs to (e.g. `clasificacion` | `extraccion` | `null`-for-abstención).
    Stratify across the RESEARCH axes: XML-clean vs scanned-PDF-derived text, archaic vs modern register,
    short vs long, Cámara/Senado/BCN source, and cases WITH a load-bearing negation. Every case has `id`,
    `input`, `label` (or `null`), `estrato`, and a `muestra: true` flag on the curated gate subset. Include
    ≥1 abstención-esperada case. NO case carries a RUT or personal identifier.
    Create `packages/llm-bench/src/tasks/routing/prompt_exemplars.json` — few-shot exemplars DISJOINT from
    `casos.json` (no shared id/entity), used by the model-under-test in 107; never overlaps the eval pool.
    Create `packages/llm-bench/src/tasks/routing/scorer.ts` generalizing cruces: zod-validate `casos.json`
    at load; export `GOLDEN_SET`, `GOLDEN_SET_GATE` (muestra:true), `IDS_CASOS_ADVERSARIOS` + `GOLDEN_SET_ADVERSARIO`,
    `evaluarRouting(set, ejecutar)` → top-1 + abstención metrics, and `gatePasa(m)`.
    Create `packages/llm-bench/src/tasks/routing/casos.freeze.json` = `{ hash, fecha, n_casos, estratos }`
    with `hash` = the sha256 of the committed `casos.json` bytes.
    Create `packages/llm-bench/src/tasks/routing/scorer.test.ts` mirroring the cruces behavior contract
    (all-correct→pass, all-abstain→0 coverage but 0 errors and gate FALSE by coverage, one-wrong→error→gate FALSE)
    using a mock executor, plus the isolated adversarial meta-test proving the gate can fail.
    Create `packages/llm-bench/src/tasks/routing/disjuncion.test.ts` asserting the three guard invariants.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test routing 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `casos.json` has ≥35 cases spanning ≥4 estratos and ≥1 abstención-esperada; a test asserts stratum coverage.
    - `evaluarRouting` treats `null` (abstención) as `no-cubierto`, never as `misclasificacion` — a behavior test proves 0 errors when the model always abstains.
    - The isolated adversarial case makes `gatePasa` return false (meta-test green) — the gate is not theater.
    - `disjuncion.test.ts` fails loud if any id appears in both pools, if any RUT is present, or if the frozen hash drifts.
    - `casos.freeze.json.hash` equals `hashCasos(readFileSync(casos.json))` at commit time.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test routing` green; routing set frozen, scored top-1+abstención, guard bites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clasificación golden set (sector seeds, no leakage) + scorer + disjunction guard</name>
  <files>packages/llm-bench/src/tasks/clasificacion/casos.json, packages/llm-bench/src/tasks/clasificacion/casos.freeze.json, packages/llm-bench/src/tasks/clasificacion/prompt_exemplars.json, packages/llm-bench/src/tasks/clasificacion/scorer.ts, packages/llm-bench/src/tasks/clasificacion/scorer.test.ts, packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts, packages/llm-bench/src/index.ts</files>
  <read_first>
    - packages/cruces/src/golden/casos.json (existing sector gold labels — reuse as SEEDS, do NOT re-expose in prompt_exemplars)
    - packages/cruces/src/golden/golden-set.ts (reuse the sector single-label scoring verbatim; SECTOR taxonomy is the label space)
    - 106-RESEARCH.md § Sourcing (sector classifications from cruces have existing gold; anti-leakage seed rule)
    - 106-RESEARCH.md § Anti-leakage + § Freezing (same guard trio as routing)
    - packages/llm-bench/src/guards/{freeze,no-rut}.ts + packages/llm-bench/src/tasks/routing/scorer.ts (mirror structure)
  </read_first>
  <behavior>
    - evaluarClasificacion scores single-label top-1 + abstención identical to cruces sector (correcto/no-cubierto/misclasificación).
    - The clasificación casos.json seeds from cruces sector labels but its ids are DISTINCT from any prompt_exemplars id (anti-leakage) — a seed reused as label is fine; a seed reused as a prompt exemplar is forbidden.
    - Same adversarial meta-test (one wrong sector → gate false) + same freeze/no-RUT/disjunction guard trio.
  </behavior>
  <action>
    Create `packages/llm-bench/src/tasks/clasificacion/casos.json` — a golden set (~40 cases) of sector
    classification stratified across the RESEARCH axes, seeded from the existing cruces sector gold labels
    (idea_matriz / contraparte nombre → sector_codigo, or `null` for abstención). Distinct ids from any
    exemplar. NO RUT/PII. Include the muestra:true gate subset + ≥1 abstención-esperada.
    Create `packages/llm-bench/src/tasks/clasificacion/prompt_exemplars.json` — few-shot exemplars DISJOINT
    from `casos.json`; if a cruces gold case is reused as an exemplar it must NOT also appear as an eval case
    (guard enforces ∩=∅).
    Create `packages/llm-bench/src/tasks/clasificacion/scorer.ts` reusing the routing scorer's top-1+abstención
    shape against the SECTOR label space (import `SECTOR_CODIGOS` from `@obs/cruces` OR inline the enum if
    adding the cruces dep is undesirable — prefer the enum inline to avoid a runtime dep on a domain package;
    document the choice). Export GOLDEN_SET, GOLDEN_SET_GATE, adversarios, `evaluarClasificacion`, `gatePasa`.
    Create `packages/llm-bench/src/tasks/clasificacion/casos.freeze.json` = the sha256 marker.
    Create `packages/llm-bench/src/tasks/clasificacion/scorer.test.ts` (behavior contract + adversarial meta-test)
    and `packages/llm-bench/src/tasks/clasificacion/disjuncion.test.ts` (∩=∅, no-RUT, frozen-hash).
    If a runtime dep on `@obs/cruces` is added for `SECTOR_CODIGOS`, add it to package.json + tsconfig references;
    otherwise inline the enum and note it. Extend the barrel `src/index.ts`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test clasificacion 2>&1 | tail -20 && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `casos.json` seeds from cruces sector gold but its ids are disjoint from `prompt_exemplars.json` (disjunction test proves ∩=∅).
    - `evaluarClasificacion` scores top-1 + abstención exactly like cruces (abstención never an error).
    - Adversarial meta-test proves `gatePasa` can fail; no-RUT + frozen-hash guards bite.
    - If `@obs/cruces` runtime dep is added it appears in both package.json and tsconfig references (NOT paths); if inlined, the enum is documented as a deliberate copy.
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test clasificacion` green; sector set frozen, seeded without leakage, guard bites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → harness | `casos.json` / `prompt_exemplars.json` are untrusted-JSON validated by zod at load |
| golden → LLM (LIVE, 107) | a golden case that carried a RUT would cross to a provider at LIVE run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | Information Disclosure | RUT/PII leaks into a golden case → crosses to LLM at LIVE | mitigate | `no-rut.ts` static regex guard over every `casos.json`; corpus sourced from public bill data only; adjudication corpus off-limits |
| T-106-05 | Tampering | golden edited after freeze (silent gate drift) | mitigate | `casos.freeze.json` sha256 marker asserted in each disjuncion.test.ts; re-cut is a reviewed act |
| T-106-06 | Repudiation | self-referential benchmark (golden cases used as exemplars) | mitigate | disjoint `prompt_exemplars.json` vs `casos.json` + biting ∩=∅ guard per task |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | no new external package; only a possible internal `@obs/cruces` workspace dep (vetted) or inlined enum |
</threat_model>

<verification>
- `pnpm --filter @obs/llm-bench test routing` and `... test clasificacion` green (mock, no network).
- Each disjunction test bites: ∩=∅, no-RUT, frozen-hash all asserted.
- Each task has a passing adversarial meta-test proving its gate can fail.
- `tsc -b` exit 0; root `pnpm test` not regressed at wave merge.
</verification>

<success_criteria>
- Two frozen es-CL golden sets (routing, clasificación) exist, stratified from the real corpus, RUT-free.
- Both use single-label top-1 + first-class-abstention scoring (cruces precedent generalized).
- Anti-leakage + no-RUT + freeze guards are CI tests that bite.
- No 107 scope (no adapters, no real endpoint, no verdict) is introduced.
</success_criteria>

<output>
Create `.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-02-SUMMARY.md` when done
</output>
