---
phase: 90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - packages/bio/src/parse-diputados.ts
  - packages/bio/src/parse-diputados.test.ts
  - packages/bio/src/parse-bcn-senadores.ts
  - packages/bio/src/parse-bcn-senadores.test.ts
  - packages/bio/src/parse-senado-ficha.ts
  - packages/bio/src/parse-comisiones.ts
  - packages/bio/src/parse-comisiones.test.ts
  - packages/bio/src/writer.ts
  - packages/bio/src/writer.test.ts
  - packages/bio/src/writer-supabase.ts
  - packages/bio/src/run-bio.ts
  - packages/bio/src/run-bio.test.ts
  - packages/bio/src/index.ts
  - packages/bio/src/__fixtures__/diputados-periodo-actual.xml
  - packages/bio/src/__fixtures__/bcn-militancy.json
autonomous: true
requirements: [BIO-01, BIO-05]
user_setup: []

must_haves:
  truths:
    - "El parser de diputados dropea PII (RUT/FechaNacimiento/Sexo) por construcción: un fixture con PII sintético produce un modelo que no la contiene"
    - "El parser mapea Militancia→{partido,desde,hasta,esActual} y elige 'actual' por FechaTermino abierta/más futura (nunca la primera del XML)"
    - "El match de identidad es fail-closed: DIPID exacto para diputados; parlid/nombre único para senadores; sin match único → skip + reporte, JAMÁS fabrica FK"
    - "El orquestador dos-etapas escribe crudo a R2 primero y --from-r2 reconstruye sin fetch"
    - "El writer upsert es idempotente (2× run = conteos idénticos) y actualiza parlamentario.partido desde la militancia actual"
    - "El parser de comisiones enlaza membresía SOLO por identidad confirmada; sin fuente de integrantes → catálogo sin membresía (nunca inventa)"
  artifacts:
    - path: "packages/bio/src/parse-diputados.ts"
      provides: "Parser XML retornarDiputadosPeriodoActual con allowlist + militancia vigente"
      min_lines: 40
    - path: "packages/bio/src/run-bio.ts"
      provides: "Orquestador dos-etapas fetch→R2→parse→matchDeterminista→writer, --from-r2 replay"
      min_lines: 40
    - path: "packages/bio/src/writer-supabase.ts"
      provides: "SupabaseBioWriter upsert idempotente por clave natural + update parlamentario.partido"
      min_lines: 30
  key_links:
    - from: "packages/bio/src/parse-diputados.ts"
      to: "allowlist minimización 21.719"
      via: "el parser ignora <FechaNacimiento>/<RUT>/<Sexo>, no los mapea al modelo"
      pattern: "Militancia|Partido"
    - from: "packages/bio/src/run-bio.ts"
      to: "identidad fail-closed"
      via: "match por id_diputado_camara === DIPID; === 1 confirma, si no skip"
      pattern: "id_diputado_camara"
    - from: "packages/bio/src/run-bio.ts"
      to: "R2Store.putImmutable"
      via: "Etapa 1 crudo antes de parsear; --from-r2 sin red"
      pattern: "putImmutable|from-?[Rr]2"
---

<objective>
Implementar el NÚCLEO net-new de `@obs/bio`: parsers con allowlist (diputados XML + BCN SPARQL senadores con mini-spike de vocabulario + comisiones Open Q1 con curl-spike), el writer idempotente, y el orquestador dos-etapas fail-closed. Todo OFFLINE contra fixtures — la corrida LIVE y el apply a PROD son 90-03.

Purpose: Este es el código que efectivamente ingiere la bio con minimización 21.719 e identidad fail-closed. El allowlist EN EL PARSER es la defensa por construcción; el DIPID exacto es la defensa contra atribución falsa.
Output: parsers + writer + run-bio con suite verde contra fixtures; fixtures crudos recortados (con PII sintético) commiteados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-RESEARCH.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-PATTERNS.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-01-SUMMARY.md

<interfaces>
<!-- Contratos definidos en 90-01 (packages/bio/src/model.ts) que este plan consume. -->
<!-- Endpoint diputados CORREGIDO por research: retornarDiputadosPeriodoActual (opendata.camara.cl), NO getDiputados. -->

De packages/bio/src/model.ts (creado en 90-01):
  BioParlamentario, Militancia { parlamentarioId, partido, partidoAlias, desde, hasta, esActual, origen, fechaCaptura, enlace }, Comision, ComisionMembresia

De @obs/identity (packages/identity/src/deterministic.ts):
  matchDeterminista(mention: { nombreNormalizado; camara; periodo; rut?; claveEstricta? }, maestra: MaestraRow[]) → Resolution { estado: "confirmado"|"no_confirmado", metodo, id?, razon? }  // dos args posicionales (deterministic.ts:92); confirma SOLO si === 1 candidato
  confirmar(id): EnlaceConfirmado  // única factory del branded type

De @obs/ingest (packages/ingest/src/index.ts):
  Fetcher, HostRateLimiter, RobotsGuard, R2Store (putImmutable(prefijo, recurso, date, sha, ext, bytes) → { r2Path, existed }), sha256Hex

De @obs/core:
  Parlamentario (campos: id, id_diputado_camara, parlid_senado, nombre, partido, camara, periodo)

Analog EXACTO del parser de diputados (MISMO endpoint, MISMO XML): packages/identity/src/parse-camara.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse-diputados.ts — XML allowlist + militancia vigente (test que MUERDE)</name>
  <files>packages/bio/src/parse-diputados.ts, packages/bio/src/parse-diputados.test.ts, packages/bio/src/__fixtures__/diputados-periodo-actual.xml, packages/bio/src/index.ts</files>
  <read_first>
    - packages/identity/src/parse-camara.ts (analog EXACTO: mismo endpoint retornarDiputadosPeriodoActual, mismo XML DiputadosPeriodoColeccion>DiputadoPeriodo>Diputado, misma lógica partidoVigente líneas ~110-132, helpers str()/parseFecha()/fechaTerminoOf/fechaInicioOf, FechaInvalidaError fail-loud)
    - packages/bio/src/model.ts (los tipos Militancia/BioParlamentario ya definidos en 90-01)
    - packages/lobby/src/parse-camara-lobby.test.ts (role-match para la forma del test + fixture en __fixtures__)
    - .planning/phases/90-.../90-RESEARCH.md (VERDICT 1: XML de ejemplo con <FechaNacimiento>/<RUT>/<Sexo> PII presentes; Pitfall 3: "actual" = FechaTermino abierta/más futura, NO la primera; A1: ambigüedad → fail-loud)
    - .planning/phases/90-.../90-PATTERNS.md (sección parse-diputados.ts con los excerpts VERBATIM de parse-camara.ts)
  </read_first>
  <behavior>
    - Test 1 (que MUERDE): fixture XML con `<RUT>12345678-9</RUT>` + `<FechaNacimiento>1975-04-24T00:00:00</FechaNacimiento>` + `<Sexo Valor="0">Femenino</Sexo>` sintéticos → `expect(JSON.stringify(parsed)).not.toContain("12345678")` y `not.toContain("FechaNacimiento")` y `not.toContain("Femenino")`
    - Test 2: mapea cada `<Militancia>` → { partido, partidoAlias (de <Partido><Alias>), desde (FechaInicio), hasta (FechaTermino o null si vigente), esActual }
    - Test 3: "actual" = la militancia con FechaTermino abierta/más futura (fixture con 2 militancias, la primera del XML es la histórica) → esActual true en la correcta, false en la otra
    - Test 4: DIPID emitido = `<Id>` del `<Diputado>` (clave de match); fecha malformada → lanza FechaInvalidaError (no Invalid Date silencioso)
  </behavior>
  <action>
    Crear el fixture __fixtures__/diputados-periodo-actual.xml: recorte de 2-3 `<DiputadoPeriodo>` del XML real (research VERDICT 1 tiene el shape), incluyendo DELIBERADAMENTE campos PII sintéticos (`<RUT>12345678-9</RUT>`, `<FechaNacimiento>`, `<Sexo>`) y ≥2 `<Militancia>` por diputado (una histórica cerrada, una vigente abierta). Escribir parse-diputados.ts copiando la FORMA de parse-camara.ts: XMLParser `{ ignoreAttributes: false, parseTagValue: false }`, helpers str/parseFecha/fechaInicioOf/fechaTerminoOf, asArray para Militancias.Militancia[]. ALLOWLIST por construcción: leer SOLO `<Id>`, `<Nombre>/<ApellidoPaterno>/<ApellidoMaterno>` (para nombre normalizado del match) y `<Militancias>` — NUNCA leer/mapear `<FechaNacimiento>/<RUT>/<RUTDV>/<Sexo>`. Militancia vigente: copiar el bloque partidoVigente (candidatas por rango [FechaInicio, FechaTermino-nil=vigente], elegir la de FechaInicio MÁS RECIENTE; WR-04 el orden del XML no garantiza recencia); marcar esActual solo en esa. profesion/region/distrito quedan null (research Open Q2/Q3 — no fabricar). Emitir `{ dipid, nombreNormalizado, militancias: Militancia[] }` por diputado. Fail-loud (FechaInvalidaError) ante fecha malformada. Re-export desde index.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test parse-diputados</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test parse-diputados` pasa los 4 comportamientos
    - El fixture contiene `12345678` y `FechaNacimiento`, pero el modelo parseado NO (test que muerde verde)
    - parse-diputados.ts NO referencia los tags `FechaNacimiento`/`RUT`/`Sexo` como campos leídos hacia el modelo (grep filtrado sin comentarios)
    - "actual" se elige por FechaTermino abierta/más reciente, verificado por el fixture de 2 militancias
  </acceptance_criteria>
  <done>El parser de diputados dropea PII por construcción, mapea militancia con "actual" correcta y falla-loud ante fecha inválida.</done>
</task>

<task type="auto">
  <name>Task 2: parsers senadores (BCN SPARQL + fallback senado.cl) + comisiones (Open Q1) con SPIKE-first y degradación honesta</name>
  <files>packages/bio/src/parse-bcn-senadores.ts, packages/bio/src/parse-bcn-senadores.test.ts, packages/bio/src/parse-senado-ficha.ts, packages/bio/src/parse-comisiones.ts, packages/bio/src/parse-comisiones.test.ts, packages/bio/src/__fixtures__/bcn-militancy.json, packages/bio/src/index.ts</files>
  <read_first>
    - packages/identity/src/parse-camara.ts (forma del parser + str()/parseFecha() reusables)
    - packages/lobby/src/parse-camara-lobby.ts (cheerio: `cheerio.load(html)` + normWs, líneas ~24 y ~150-219 — plantilla para parse-senado-ficha.ts y parse-comisiones.ts si HTML)
    - .planning/phases/90-.../90-RESEARCH.md (VERDICT 2: BCN SPARQL 200 OK, ontología Militancy; caveats A2 vocabulario NO es hasParty/startDate → mini-spike ?pred ?obj; A3 join por NOMBRE determinista, BCN no expone parlid_senado. VERDICT 3 + Open Question 1: WSComisiones opendata en mantención 302; candidatos citaciones_semana.aspx / senado.cl comisiones; si ninguna trae integrantes → catálogo sin membresía. Pitfall 5: www.camara.cl WAF → curl-first)
    - .planning/phases/90-.../90-PATTERNS.md (secciones parse-bcn-senadores.ts, parse-senado-ficha.ts, parse-comisiones.ts + Code Example SPARQL por fetch con URLSearchParams)
    - packages/bio/src/model.ts (Militancia, Comision, ComisionMembresia)
  </read_first>
  <action>
    (A) SPIKE-first de comisiones (Open Question 1) — al INICIO del task, probar curl-first con rate-limit 2-3s + UA identificatorio, en este orden: (1) `https://www.camara.cl/legislacion/comisiones/citaciones_semana.aspx?prmSemana=` (HTML, WAF→curl); (2) páginas de comisiones de `senado.cl` / PHP `?mo=comisiones&ac=listado`. CRITERIO DE DECISIÓN EXPLÍCITO: si ALGUNA fuente trae comisiones CON integrantes identificables (id o nombre) → parse-comisiones.ts extrae Comision + ComisionMembresia. Si NINGUNA trae integrantes → DEGRADACIÓN HONESTA: parse-comisiones.ts extrae SOLO el catálogo (nombre/camara/tipo), `comision_membresia` queda vacía con estado honesto documentado en el summary; NUNCA inventar membresía. Documentar el veredicto (endpoint elegido / degradación) en el SUMMARY.
    (B) parse-bcn-senadores.ts: cliente SPARQL = `fetch` + `JSON.parse`, CERO librería RDF, query construida con `URLSearchParams`, `Accept: application/sparql-results+json`, UA `"ObservatorioCongreso360/1.0 (contacto: sanchez.rossi@gmail.com)"`. Mini-spike de vocabulario (research A2): documentar en el summary los predicados reales de `Militancy` (party/fechas) descubiertos con `?pred ?obj`. Mapear `results.bindings[]` → Militancia con la MISMA allowlist (sin PII). Join a la maestra por NOMBRE determinista (BCN no expone parlid_senado, research A3). Los tests corren contra el fixture __fixtures__/bcn-militancy.json (shape de sparql-results), NO contra red.
    (C) parse-senado-ficha.ts (fallback LOCKED research VERDICT 2 caveat): cheerio sobre la ficha senado.cl, cruzando por `parlid_senado` que la maestra YA tiene. Usar si el spike BCN de vocabulario/join no cierra — degradación honesta declarada.
    (D) parse-comisiones.test.ts: si hay integrantes, aserción de que la membresía NO se emite sin nombre/id (fail-closed); si degradado, aserción de que el catálogo se emite sin membresía. Re-exportar desde index.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test parse-bcn-senadores parse-comisiones</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test parse-bcn-senadores parse-comisiones` pasa contra fixtures (sin red)
    - parse-bcn-senadores.ts construye la query SPARQL con `URLSearchParams` (no interpolación cruda) y usa `Accept: application/sparql-results+json`
    - parse-comisiones.ts NO emite ComisionMembresia sin nombre/id de integrante (fail-closed); en modo degradado emite solo Comision
    - El SUMMARY documenta: veredicto del spike de comisiones (endpoint elegido O degradación a catálogo) + predicados BCN Militancy descubiertos (o fallback senado.cl activado)
    - Ningún parser mapea campos PII (grep filtrado)
  </acceptance_criteria>
  <done>Senadores (BCN o fallback ficha) y comisiones (con membresía o catálogo degradado honesto) parseados fail-closed, con veredicto de los spikes documentado.</done>
</task>

<task type="auto">
  <name>Task 3: writer idempotente + run-bio orquestador dos-etapas fail-closed</name>
  <files>packages/bio/src/writer.ts, packages/bio/src/writer.test.ts, packages/bio/src/writer-supabase.ts, packages/bio/src/run-bio.ts, packages/bio/src/run-bio.test.ts, packages/bio/src/index.ts</files>
  <read_first>
    - packages/lobby/src/writer.ts (interface + InMemory writer, Map por clave natural → idempotencia verificable)
    - packages/lobby/src/writer-supabase.ts (createClient con persistSession false, chunk 500, .upsert onConflict ignoreDuplicates false, dedupePorClave antes del lote, throw solo con error.message — NUNCA interpolar service key)
    - packages/lobby/src/run-camara-lobby.ts (estructura RunOpts/RunResult + dos-etapas R2 líneas ~82-110)
    - packages/votos/src/run-camara-votos.ts (modo --from-r2 replay, envelope-fake-connector líneas ~181-242)
    - packages/identity/src/deterministic.ts (matchDeterminista fail-closed) + enlace-confirmado.ts (confirmar())
    - .planning/phases/90-.../90-PATTERNS.md (secciones writer.ts, writer-supabase.ts con onConflict sugeridos, run-bio.ts con RunBioOpts/RunBioResult, Match determinista fail-closed)
    - .planning/phases/90-.../90-RESEARCH.md (Pitfall 4: NO name-match de diputados — hay DIPID exacto; A1 partido stale → fail-loud no actualizar)
  </read_first>
  <action>
    writer.ts: interface `BioWriter` con métodos por entidad (upsertBio, upsertMilitancias, upsertComisiones, upsertMembresias, actualizarPartidoParlamentario) + `InMemoryBioWriter` (Map por clave natural → idempotencia: 2× upsert = 1 entrada). writer-supabase.ts: `SupabaseBioWriter` copiando lobby/writer-supabase: createClient(url, serviceKey, { auth: { persistSession:false, autoRefreshToken:false } }); chunk 500; `.upsert(lote, { onConflict, ignoreDuplicates:false })` con onConflict: parlamentario_bio→`parlamentario_id`, parlamentario_militancia→`parlamentario_id,partido_alias,desde`, comision→`nombre,camara`, comision_membresia→`comision_id,parlamentario_id`; dedupePorClave antes de cada lote; `if (error) throw new Error(...falló: ${error.message})` — NUNCA interpolar service key. actualizarPartidoParlamentario: UPDATE parlamentario SET partido, fecha_captura desde la militancia ACTUAL (research A1: si ambiguo/sin actual clara → NO actualizar, fail-loud en log). run-bio.ts: `RunBioOpts { conector, writer, maestra, r2Store?, fechaCaptura?, fromR2?, log? }` + `RunBioResult { r2Path, actualizados, sinMatch, comisiones, membresias }`. Flujo: fetch (o replay --from-r2 vía envelope-fake) → Etapa 1 R2 putImmutable (short-circuit `existed` "sin novedades") → parse (allowlist) → MATCH FAIL-CLOSED: diputados por `maestra.filter(p => p.id_diputado_camara === dipid)` confirma SOLO si length===1 (NUNCA name-match diputados, Pitfall 4); senadores por parlid_senado o matchDeterminista por nombre único; sin match único → skip + push a sinMatch (nombres SOLO en log local, NO persistidos) → writer.upsert*. FK tipado con confirmar()/EnlaceConfirmado donde aplique. run-bio.test.ts: (1) --from-r2 reconstruye sin fetch (conector-fake que falla si se llama a red); (2) putImmutable short-circuit; (3) DIPID sin match → skip, no fabrica FK; (4) 2× run con InMemory = conteos idénticos (idempotencia); (5) actualiza parlamentario.partido desde la actual.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test writer run-bio</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test writer run-bio` pasa los comportamientos
    - run-bio.ts hace el match de diputados por `id_diputado_camara === dipid` con confirmación SOLO si length===1 (grep confirma), y NO usa matchDeterminista/normalizarNombre para diputados
    - 2× run con InMemoryBioWriter produce conteos idénticos (idempotencia demostrada por test)
    - --from-r2 reconstruye con un conector-fake que arroja si se invoca la red (prueba de replay sin fetch)
    - writer-supabase.ts NUNCA interpola la service key en mensajes de error (solo error.message)
    - DIPID sin match → skip + sinMatch, cero FK fabricado
  </acceptance_criteria>
  <done>El writer es idempotente, el orquestador es dos-etapas y fail-closed; --from-r2 replay sin red; parlamentario.partido se refresca desde la actual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente gubernamental → parser | XML/SPARQL/HTML con PII cruda y datos no confiables |
| parser → matchDeterminista | nombres/DIPID que podrían atribuir voto/bio a la persona equivocada |
| writer → Supabase (service_role) | service key nunca en logs; bypassa RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-PII | Information Disclosure | parse-diputados.ts / parse-bcn / parse-comisiones | mitigate | Allowlist EN EL PARSER: el modelo tipado no declara PII; test que muerde con fixture PII sintético |
| T-90-SPOOF | Spoofing (falsa atribución) | run-bio.ts match | mitigate | DIPID exacto (===1) diputados; parlid/nombre único fail-closed senadores; sin match → skip, jamás fabrica FK; membresía solo por identidad confirmada |
| T-90-STALE | Tampering (partido stale creíble) | actualizarPartidoParlamentario | mitigate | "actual" = FechaTermino abierta/más futura; fecha_captura+fuente; ambigüedad → fail-loud, no actualizar (A1) |
| T-90-KEY | Information Disclosure | writer-supabase.ts | mitigate | Solo error.message de PostgREST; nunca interpola service key (patrón LOCKED) |
| T-90-INJ | Tampering | parse-bcn-senadores.ts | mitigate | Query SPARQL con URLSearchParams (no inyección); parser XML sin entidades externas |
</threat_model>

<verification>
- `pnpm --filter @obs/bio test` verde (parsers + writer + run-bio contra fixtures)
- `pnpm test` (raíz) verde tras el wave
- `tsc -b` compila
- SUMMARY documenta veredictos de los dos spikes (BCN vocabulario + comisiones Open Q1)
</verification>

<success_criteria>
- Allowlist por construcción probado por test que muerde (diputados + senadores + comisiones)
- Identidad fail-closed: DIPID exacto diputados, parlid/nombre senadores, sin match → skip
- Dos-etapas: R2 crudo primero, --from-r2 replay sin red
- Writer idempotente + refresco de parlamentario.partido desde la actual
- Comisiones: membresía por identidad confirmada o catálogo degradado honesto (nunca inventa)
</success_criteria>

<output>
Create `.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-02-SUMMARY.md` when done
</output>
