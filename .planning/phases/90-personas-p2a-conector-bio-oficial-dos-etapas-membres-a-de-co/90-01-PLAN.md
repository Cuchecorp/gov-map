---
phase: 90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/bio/package.json
  - packages/bio/tsconfig.json
  - packages/bio/vitest.config.ts
  - packages/bio/src/index.ts
  - packages/bio/src/model.ts
  - packages/bio/src/model.test.ts
  - supabase/migrations/0059_bio_comisiones.sql
  - supabase/tests/0059_bio_comisiones.test.sql
  - tsconfig.json
autonomous: true
requirements: [BIO-01, BIO-05]
user_setup: []

must_haves:
  truths:
    - "El paquete @obs/bio existe, resuelve en el workspace pnpm y su suite corre (no CI-DARK)"
    - "El modelo tipado de bio NO declara ningún campo PII (fechaNacimiento/rut/sexo) — imposible persistirlos por construcción"
    - "La migración 0059 crea parlamentario_bio + parlamentario_militancia + comision + comision_membresia con provenance inline, RLS deny-by-default y cero grant a anon"
    - "El pgTAP 0059 verifica RLS habilitada, cero policies y cero grant anon en las 4 tablas nuevas"
  artifacts:
    - path: "packages/bio/vitest.config.ts"
      provides: "Config vitest para que la suite del paquete corra (gotcha Phase 43: sin él 0 tests corren)"
      contains: "defineConfig"
    - path: "packages/bio/src/model.ts"
      provides: "Contratos tipados con allowlist: BioParlamentario/Militancia/Comision/ComisionMembresia, sin campos PII"
      min_lines: 30
    - path: "supabase/migrations/0059_bio_comisiones.sql"
      provides: "DDL aditivo de las 4 tablas nuevas deny-by-default"
      contains: "enable row level security"
    - path: "supabase/tests/0059_bio_comisiones.test.sql"
      provides: "pgTAP de deny-by-default + provenance NOT NULL"
      contains: "pg_policies"
  key_links:
    - from: "packages/bio/src/model.ts"
      to: "allowlist minimización 21.719"
      via: "el modelo NO declara fechaNacimiento/rut/sexo"
      pattern: "interface (BioParlamentario|Militancia)"
    - from: "supabase/migrations/0059_bio_comisiones.sql"
      to: "lockdown-guard Block A (>0044)"
      via: "revoke all from anon, authenticated + cero grant to anon"
      pattern: "revoke all on .* from anon"
---

<objective>
Levantar el andamiaje del paquete `@obs/bio` (espejo estructural de `@obs/lobby`), definir los CONTRATOS tipados con allowlist de minimización (el modelo NO declara PII → imposible persistirla), y escribir la migración aditiva 0059 + su pgTAP con las 4 tablas nuevas deny-by-default. Todo OFFLINE (sin red, sin apply a PROD — eso es 90-03).

Purpose: Sin el paquete, el modelo y las tablas, los parsers/writer de 90-02 no tienen dónde escribir ni contra qué tipar. Este plan es Wave 0 + los contratos (interface-first) que los downstream implementan.
Output: `packages/bio/` esqueleto compilable con suite verde, `model.ts` con allowlist, migración 0059 + pgTAP escritos (no aplicados).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-RESEARCH.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-PATTERNS.md

<interfaces>
<!-- Contratos que los downstream (90-02 parsers/writer, 90-03 CLI) consumen. -->
<!-- El modelo tipado ES el allowlist: campos PII NO se declaran. -->

Modelo a definir en packages/bio/src/model.ts (allowlist — SIN fechaNacimiento/rut/sexo):
```typescript
interface BioParlamentario { parlamentarioId: string; profesion: string | null; origen: string; fechaCaptura: string; enlace: string; }
interface Militancia { parlamentarioId: string; partido: string; partidoAlias: string; desde: string; hasta: string | null; esActual: boolean; origen: string; fechaCaptura: string; enlace: string; }
interface Comision { nombre: string; camara: "diputados" | "senadores"; tipo: string; origen: string; fechaCaptura: string; enlace: string; }
interface ComisionMembresia { comisionId: string; parlamentarioId: string; cargo: string | null; origen: string; fechaCaptura: string; enlace: string; }
```

Clave natural (onConflict) que la migración 0059 y el writer de 90-02 deben respetar:
- parlamentario_bio → parlamentario_id
- parlamentario_militancia → parlamentario_id, partido_alias, desde
- comision → nombre, camara
- comision_membresia → comision_id, parlamentario_id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/bio (config + barrel) espejo de @obs/lobby</name>
  <files>packages/bio/package.json, packages/bio/tsconfig.json, packages/bio/vitest.config.ts, packages/bio/src/index.ts, tsconfig.json</files>
  <read_first>
    - packages/lobby/package.json (plantilla exacta de package.json)
    - packages/lobby/tsconfig.json (usa "references", NO "paths" — gotcha Phase 43 rompe tsc -b)
    - packages/lobby/vitest.config.ts (9 líneas; sin él 0 tests corren — dinero fue CI-DARK por esto)
    - packages/lobby/src/index.ts (patrón barrel)
    - tsconfig.json (raíz del monorepo — añadir referencia a packages/bio)
    - pnpm-workspace.yaml (ya globbea packages/* — NO editar; solo re-pnpm install)
    - .planning/phases/90-.../90-PATTERNS.md (sección "packages/bio/package.json" con el JSON exacto de deps)
  </read_first>
  <action>
    Crear packages/bio como espejo de packages/lobby. package.json: name `@obs/bio`, private, type module, main/types `src/index.ts`, scripts test=`vitest run` y typecheck=`tsc -b`. Deps: `@obs/core`, `@obs/identity`, `@obs/ingest` (workspace:*), `@supabase/supabase-js` ^2.108.2, `cheerio` 1.2.0, `fast-xml-parser` ^5, `zod` ^4.4.3. devDeps: `@types/node`, `tsx`, `vitest` ^3. NO añadir `@obs/adjudication` (bio no usa LLM). tsconfig.json del paquete: `composite: true`, `rootDir: src`, `outDir: dist`, `references` a ../core ../ingest ../identity (NUNCA `paths`), exclude `**/*.test.ts`. vitest.config.ts: copiar verbatim de lobby (environment node, include `src/**/*.test.ts`, passWithNoTests true). index.ts: barrel vacío por ahora (re-export de model). Añadir `{ "path": "./packages/bio" }` al array `references` del tsconfig.json RAÍZ del monorepo. Correr `pnpm install` para materializar el symlink workspace.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test` resuelve el paquete y sale 0 (passWithNoTests permite suite vacía en este task)
    - packages/bio/tsconfig.json contiene `"references"` y NO contiene la clave `"paths"`
    - packages/bio/vitest.config.ts contiene `include: ["src/**/*.test.ts"]`
    - tsconfig.json raíz contiene una entrada de references apuntando a `./packages/bio`
    - package.json declara `fast-xml-parser` y NO declara `@obs/adjudication`
  </acceptance_criteria>
  <done>El paquete @obs/bio existe, resuelve en el workspace, su suite corre (no CI-DARK) y tsc lo referencia.</done>
</task>

<task type="auto">
  <name>Task 2: model.ts — contratos con allowlist (el modelo NO declara PII)</name>
  <files>packages/bio/src/model.ts, packages/bio/src/model.test.ts, packages/bio/src/index.ts</files>
  <read_first>
    - packages/lobby/src/model.ts (bloque ProvenanceInline `origen`/`fecha_captura`/`enlace` como z.string() + patrón interface+zod-schema por entidad)
    - .planning/phases/90-.../90-PATTERNS.md (sección model.ts — PATRÓN CLAVE ALLOWLIST research Pattern 2)
    - .planning/phases/90-.../90-RESEARCH.md (VERDICT 1: campos PII presentes en el XML = FechaNacimiento/RUT/RUTDV/Sexo que el modelo NO debe declarar)
  </read_first>
  <action>
    Definir en model.ts los tipos del bloque <interfaces> de este plan: `BioParlamentario`, `Militancia`, `Comision`, `ComisionMembresia`, cada uno con provenance inline (`origen`/`fechaCaptura`/`enlace` como z.string()) copiando el patrón ProvenanceInline de lobby/model.ts. REGLA RECTORA (allowlist por construcción, research Pattern 2): NINGÚN tipo declara `fechaNacimiento`, `rut`, `rutDv`, `sexo`, ni campos de terceros/familiares — esos jamás existen en el objeto que verá el writer. Adjuntar zod schema por entidad (validación de contrato de salida). model.test.ts: aserción estructural de que los schemas parsean un objeto válido y RECHAZAN uno con campo PII extra (`.strict()` en el zod → un objeto con `rut` falla el parse). Re-exportar los tipos desde index.ts.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test model</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test model` pasa (schemas parsean válidos, rechazan PII extra)
    - model.ts NO contiene los identificadores `fechaNacimiento`, `rut`, `sexo` en ninguna interface ni schema (grep filtrado: `grep -v '^\s*//' packages/bio/src/model.ts | grep -ciE 'fechaNacimiento|\brut\b|\bsexo\b'` == 0)
    - los 4 tipos declaran `origen`, `fechaCaptura`, `enlace`
    - el zod schema usa `.strict()` de modo que un campo extra (p.ej. rut) hace fallar el parse
  </acceptance_criteria>
  <done>El modelo tipado de bio existe con allowlist por construcción; un objeto con PII no compila ni valida.</done>
</task>

<task type="auto">
  <name>Task 3: Migración 0059 + pgTAP (offline, no aplicada) — 4 tablas deny-by-default</name>
  <files>supabase/migrations/0059_bio_comisiones.sql, supabase/tests/0059_bio_comisiones.test.sql</files>
  <read_first>
    - supabase/migrations/0021_lobby.sql (plantilla EXACTA deny-by-default: provenance inline NOT NULL, unique() clave natural, enable RLS SIN policies, revoke all from anon/authenticated; NO clonar el RPC público ni grant to anon — eso es Phase 91)
    - supabase/migrations/0005_parlamentario.sql (columnas existentes: partido, region, distrito, circunscripcion, id_diputado_camara, parlid_senado; FK target parlamentario(id))
    - supabase/tests/0021_lobby.test.sql (plantilla pgTAP: has_table, RLS habilitada, count pg_policies=0, role_table_grants anon SELECT=0, col_not_null)
    - .planning/phases/90-.../90-PATTERNS.md (secciones 0059 migration + pgTAP con los excerpts VERBATIM del deny-by-default)
    - .planning/phases/90-.../90-RESEARCH.md (Pitfall 6: aplicar por psql --single-transaction, NUNCA db push; esta migración es la 0059 — última aplicada = 0058)
  </read_first>
  <action>
    Escribir 0059_bio_comisiones.sql con las 4 tablas siguiendo lobby_contraparte (0021) VERBATIM en su forma deny-by-default. (1) `parlamentario_bio`: parlamentario_id FK a parlamentario(id) on delete cascade (unique → 1:1), profesion text NULL, region/distrito/circunscripcion text NULL (poblables después; research Open Q2/Q3 → NULL honesto hoy), provenance inline (origen/fecha_captura/enlace NOT NULL). (2) `parlamentario_militancia`: parlamentario_id FK, partido text, partido_alias text, desde date, hasta date NULL, es_actual boolean, provenance NOT NULL, `unique(parlamentario_id, partido_alias, desde)`. (3) `comision`: id (generated), nombre text, camara text check in (diputados,senadores), tipo text, provenance NOT NULL, `unique(nombre, camara)`. (4) `comision_membresia`: comision_id FK on delete cascade, parlamentario_id FK on delete cascade, cargo text NULL, provenance NOT NULL, `unique(comision_id, parlamentario_id)`. En TODAS: `enable row level security` SIN policies + `revoke all on <tabla> from anon, authenticated`. CERO `grant … to anon` (lockdown-guard Block A prohíbe grants a anon en migraciones >0044). Header comentado: "última migración APLICADA = 0058; esta es la 0059; aplicar por PGCLIENTENCODING=UTF8 psql --single-transaction, NUNCA db push". NO tocar el DDL de `parlamentario` (partido se ACTUALIZA por el writer en 90-02, no requiere DDL). Escribir 0059_bio_comisiones.test.sql (pgTAP) espejando 0021_lobby.test.sql: por cada una de las 4 tablas → has_table, RLS habilitada (relrowsecurity true), pg_policies count=0, role_table_grants anon SELECT=0; col_not_null de origen/fecha_captura/enlace. NO aplicar nada a PROD en este task (eso es 90-03).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && node -e "const s=require('fs').readFileSync('supabase/migrations/0059_bio_comisiones.sql','utf8'); const noc=s.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n'); if(/grant\s+.*\s+to\s+anon/i.test(noc)){console.error('FAIL: grant to anon presente');process.exit(1)} for(const t of ['parlamentario_bio','parlamentario_militancia','comision','comision_membresia']){if(!new RegExp('create table[\\s\\S]*'+t).test(noc)){console.error('FAIL: falta tabla '+t);process.exit(1)} if(!new RegExp('revoke all on (?:public\\.)?'+t+'\\s+from anon').test(noc)){console.error('FAIL: falta revoke '+t);process.exit(1)}} console.log('OK 4 tablas deny-by-default, cero grant anon')"</automated>
  </verify>
  <acceptance_criteria>
    - El check node sale 0: las 4 tablas existen, cada una con `revoke all on <tabla> from anon`, y CERO `grant … to anon` (fuera de comentarios)
    - 0059_...sql contiene 4× `enable row level security` y 0× `create policy`
    - 0059_...test.sql contiene por cada tabla un assert de `pg_policies` count 0 y de `role_table_grants` anon SELECT 0
    - El archivo es `0059_` (no reusa 0058 ni salta a 0060)
    - El DDL NO altera la tabla `parlamentario` (partido se refresca por writer, no por DDL)
  </acceptance_criteria>
  <done>La migración 0059 y su pgTAP existen offline, deny-by-default, listos para aplicar en 90-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente gubernamental → parser | XML/HTML no confiable con PII cruda (se maneja en 90-02); aquí solo el CONTRATO que la excluye |
| migración → Supabase (default privileges) | service_role bypassa RLS; anon/authenticated NO deben tener grant en tablas nuevas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-PII | Information Disclosure | model.ts (contrato) | mitigate | El modelo tipado NO declara fechaNacimiento/rut/sexo — allowlist por construcción; test con `.strict()` que muerde |
| T-90-EOP | Elevation of Privilege | migración 0059 | mitigate | RLS deny-by-default + `revoke all from anon, authenticated`; cero `grant … to anon` (lockdown-guard Block A >0044); check automatizado en el verify |
| T-90-SC | Tampering | npm installs | mitigate | CERO paquetes nuevos — todas las deps ya en el monorepo desde v2.0 (RESEARCH Package Legitimacy Audit: sin superficie nueva) |
</threat_model>

<verification>
- `pnpm --filter @obs/bio test` verde (model + suite vacía OK por passWithNoTests)
- `tsc -b` del monorepo compila con la nueva referencia a packages/bio
- El check automatizado de la migración 0059 pasa (4 tablas, cero grant anon)
- `pnpm test` (raíz) sigue verde (nada roto: paquete nuevo aislado)
</verification>

<success_criteria>
- Paquete @obs/bio scaffolded, resuelve en workspace, suite corre
- model.ts con allowlist por construcción (sin PII), test que muerde con `.strict()`
- Migración 0059 + pgTAP escritos, deny-by-default, cero grant anon, NO aplicados aún
</success_criteria>

<output>
Create `.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-01-SUMMARY.md` when done
</output>
