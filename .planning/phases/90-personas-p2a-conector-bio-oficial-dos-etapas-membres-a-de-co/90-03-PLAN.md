---
phase: 90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co
plan: 03
type: execute
wave: 3
depends_on: ["90-01", "90-02"]
files_modified:
  - packages/bio/src/run-bio-cli.ts
  - packages/bio/src/run-bio-cli.test.ts
  - packages/bio/src/index.ts
  - .planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-BIO-LEDGER.md
autonomous: true
requirements: [BIO-01, BIO-05]
user_setup: []

must_haves:
  truths:
    - "El CLI run-bio-cli ensambla los colaboradores reales (Fetcher/HostRateLimiter/RobotsGuard/R2Store) y soporta --dry-run, --from-r2, --xml-file, loadEnv BOM-safe"
    - "La migración 0059 está APLICADA a PROD por psql --single-transaction y el pgTAP 0059 pasa contra el schema aplicado (RLS on, cero policies, cero grant anon en las 4 tablas)"
    - "La bio de diputados está POBLADA en PROD tras la corrida LIVE acotada (rate-limit 2-3s, curl-first ante WAF): parlamentario_bio/militancia poblados, parlamentario.partido refrescado"
    - "La cobertura (N diputados actualizados / M sin match) y el estado de comisiones (con membresía o catálogo degradado) quedan MEDIDOS y DECLARADOS en el ledger"
    - "El camino de SENADORES queda INTENTADO y su veredicto DECLARADO en el ledger (N confirmados/M por estado, o fallback ficha senado.cl activado, o degradado CON razón) — la degradación se declara, jamás se defaultea en silencio"
  artifacts:
    - path: "packages/bio/src/run-bio-cli.ts"
      provides: "Entry-point operador/agente dos-etapas, espejo run-camara-lobby-cli"
      min_lines: 40
    - path: ".planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-BIO-LEDGER.md"
      provides: "Registro del apply 0059 + pgTAP + cobertura LIVE + veredicto comisiones"
      contains: "0059"
  key_links:
    - from: "packages/bio/src/run-bio-cli.ts"
      to: "@obs/ingest colaboradores reales"
      via: "ensambla Fetcher+HostRateLimiter+R2Store desde .env R2_*"
      pattern: "R2Store|Fetcher"
    - from: "migracion 0059 aplicada"
      to: "pgTAP contra schema aplicado"
      via: "psql --single-transaction -f + psql -tA -f test"
      pattern: "0059"
---

<objective>
Cerrar la fase: escribir el CLI `run-bio-cli` (entry-point operador/agente), APLICAR la migracion aditiva 0059 a PROD (dentro de autoridad del agente, precedente pasada-1 0055-0058), correr el pgTAP contra el schema aplicado, ejecutar la corrida LIVE acotada de bio (1 request de diputados cubre la camara; rate-limit 2-3s; curl-first ante WAF), y DECLARAR la cobertura + el veredicto de comisiones en el ledger.

Purpose: Sin apply + corrida LIVE, la bio no esta poblada y el gate de 91 (columna/modelo + bio de diputados) no cierra. Este plan entrega el DATO real en PROD.
Output: run-bio-cli con test; 0059 aplicada + pgTAP verde; parlamentario_bio/militancia poblados; ledger con cobertura y veredicto de comisiones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-RESEARCH.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-PATTERNS.md
@.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-02-SUMMARY.md

<interfaces>
<!-- De 90-02: run-bio.ts (RunBioOpts/RunBioResult) que el CLI ensambla. -->
<!-- Endpoint LIVE (research VERDICT 1): GET https://opendata.camara.cl/camaradiputados/WServices/WSDiputado.asmx/retornarDiputadosPeriodoActual (200, 155 dip, sin WAF sobre curl) -->

De packages/bio/src/run-bio.ts (90-02):
  runBio(opts: RunBioOpts): Promise<RunBioResult>   // dos-etapas; fromR2 replay

Helpers a copiar VERBATIM de packages/lobby/src/run-camara-lobby-cli.ts:
  flagValue(name) (lineas ~31-34)
  loadEnv() BOM-safe con precedencia process.env (~41-63) — keys ya: SUPABASE_API_URL/SUPABASE_SECRET_KEY/R2_*
  findWorkspaceRoot() (~70-82)
  cargarMaestra() desde supabase/seeds/parlamentario.seed.json (~84-88)

Convencion LOCKED apply a PROD (NUNCA supabase db push):
  PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0059_bio_comisiones.sql
  PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0059_bio_comisiones.test.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run-bio-cli.ts — entry-point dos-etapas (espejo run-camara-lobby-cli)</name>
  <files>packages/bio/src/run-bio-cli.ts, packages/bio/src/run-bio-cli.test.ts, packages/bio/src/index.ts</files>
  <read_first>
    - packages/lobby/src/run-camara-lobby-cli.ts (analog EXACTO — copiar casi verbatim: flagValue, loadEnv BOM-safe, findWorkspaceRoot, cargarMaestra, ensamblado condicional conector real vs stub-de-archivo lineas ~98-107)
    - packages/votos/src/run-camara-votos.ts (ensamblado de colaboradores + modo --from-r2)
    - packages/bio/src/run-bio.ts (creado en 90-02 — el CLI lo invoca)
    - .planning/phases/90-.../90-RESEARCH.md (VERDICT 1 endpoint LIVE; Pitfall 5: www.camara.cl WAF pero opendata.camara.cl NO -> fetch directo OK para diputados, --xml-file solo para comisiones)
    - .planning/phases/90-.../90-PATTERNS.md (seccion run-bio-cli.ts con flags y helpers a copiar)
  </read_first>
  <action>
    Escribir run-bio-cli.ts copiando run-camara-lobby-cli.ts: helpers flagValue/loadEnv(BOM-safe, precedencia process.env)/findWorkspaceRoot/cargarMaestra verbatim. Flags: `--dry-run` (InMemoryBioWriter + sin R2), `--from-r2 <path>` (replay sin fetch), `--xml-file <ruta>` (bypass WAF para comisiones; diputados via opendata.camara.cl NO necesita — fetch directo OK), `--fuente diputados|senadores|comisiones` para acotar la corrida. Ensamblado condicional: si NO --dry-run, construir Fetcher+HostRateLimiter+RobotsGuard+R2Store reales desde loadEnv (R2_* + SUPABASE_API_URL/SUPABASE_SECRET_KEY) y SupabaseBioWriter; si --dry-run, InMemory + sin R2. Endpoint diputados: `https://opendata.camara.cl/camaradiputados/WServices/WSDiputado.asmx/retornarDiputadosPeriodoActual`. Invocar runBio y loguear el RunBioResult (N actualizados / M sin match; nombres sin-match SOLO en log local, no persistidos). run-bio-cli.test.ts: asercion de que flagValue/loadEnv se comportan (--dry-run no toca red ni R2; parsea flags). Re-export desde index.ts si aplica.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/bio test run-bio-cli</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/bio test run-bio-cli` pasa y `tsc -b` compila
    - run-bio-cli.ts declara los flags --dry-run, --from-r2, --xml-file y usa el endpoint retornarDiputadosPeriodoActual (opendata.camara.cl)
    - --dry-run ensambla InMemoryBioWriter y NO construye R2Store (verificado por test)
    - loadEnv es BOM-safe con precedencia process.env (copiado verbatim del analog)
  </acceptance_criteria>
  <done>El CLI existe, compila, ensambla colaboradores reales o dry-run, y usa el endpoint corregido.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar 0059 a PROD + pgTAP contra schema aplicado (autoridad agente, precedente 0055-0058)</name>
  <files>.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-BIO-LEDGER.md</files>
  <read_first>
    - supabase/migrations/0059_bio_comisiones.sql (creada en 90-01 — la que se aplica)
    - supabase/tests/0059_bio_comisiones.test.sql (pgTAP creado en 90-01)
    - .planning/phases/72-*/72-APPLY-RUNBOOK.md (runbook espejo de apply por psql --single-transaction, BOM esquivado — si existe)
    - .planning/phases/90-.../90-RESEARCH.md (Pitfall 6: build/typecheck NO prueban que Postgres ejecuto el DDL — pgTAP contra schema APLICADO obligatorio; migracion es ADITIVA = dentro de autoridad del agente)
    - CLAUDE.md (Conventions: migraciones a PROD por PGCLIENTENCODING=UTF8 psql --single-transaction -f, NUNCA supabase db push)
  </read_first>
  <action>
    Aplicar la migracion ADITIVA 0059 a PROD (dentro de autoridad del agente por precedente pasada-1 0055-0058: DDL aditivo, tablas nuevas, RLS deny-by-default, cero grant anon — sin riesgo destructivo). Comando LOCKED: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0059_bio_comisiones.sql` (NUNCA supabase db push; SUPABASE_DB_URL de .env BOM-safe). Aplicar UNA sola vez. Luego correr el pgTAP contra el schema APLICADO: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0059_bio_comisiones.test.sql` y confirmar todos los asserts `ok` / cero `not ok` (RLS on, cero policies, cero grant anon en las 4 tablas, provenance NOT NULL). Si el apply falla porque las tablas ya existen (corrida previa), diagnosticar idempotencia y NO re-aplicar destructivamente — verificar el estado con el pgTAP y registrar. Escribir 90-BIO-LEDGER.md con: comando exacto de apply, resultado, salida del pgTAP (N ok / 0 not ok), y la nota de que 0059 NO se re-aplica.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && node -e "require('child_process').execSync('psql \"'+process.env.SUPABASE_DB_URL+'\" -tA -c \"select count(*) from information_schema.tables where table_name in (\\'parlamentario_bio\\',\\'parlamentario_militancia\\',\\'comision\\',\\'comision_membresia\\')\"',{stdio:'inherit',env:{...process.env,PGCLIENTENCODING:'UTF8'}})"</automated>
  </verify>
  <acceptance_criteria>
    - Las 4 tablas (parlamentario_bio, parlamentario_militancia, comision, comision_membresia) existen en PROD (count = 4)
    - El pgTAP 0059 pasa contra el schema aplicado: cero `not ok` (RLS on, cero policies, cero grant anon, provenance NOT NULL)
    - El ledger 90-BIO-LEDGER.md registra el comando de apply, la salida del pgTAP y la nota de no-re-aplicar
    - Se uso `psql --single-transaction -f`, NUNCA `supabase db push`
  </acceptance_criteria>
  <done>La migracion 0059 esta aplicada a PROD y verificada por pgTAP contra el schema vivo; el ledger lo documenta.</done>
</task>

<task type="auto">
  <name>Task 3: Corrida LIVE acotada de bio (dry-run primero) + cobertura declarada en ledger</name>
  <files>.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-BIO-LEDGER.md</files>
  <read_first>
    - packages/bio/src/run-bio-cli.ts (creado en Task 1 — el entry-point que se ejecuta)
    - .planning/phases/90-.../90-RESEARCH.md (VERDICT 1: 1 request cubre 155 diputados; VERDICT 2: BCN senadores; VERDICT 3 + Open Q1: veredicto de comisiones resuelto en 90-02 SUMMARY)
    - .planning/phases/90-.../90-02-SUMMARY.md (veredicto de los spikes BCN + comisiones de 90-02 — determina que fuentes corre este task)
    - CLAUDE.md (Conventions: rate-limit 2-3s/host, UA identificatorio, dos-etapas fuente->R2->Supabase, curl-first ante WAF)
  </read_first>
  <action>
    Ejecutar la corrida LIVE acotada de bio (la ejecuta el AGENTE — bio = pocas requests, no backfill masivo; precedente pasada-1). PASO 1 (seco): `pnpm --filter @obs/bio exec tsx src/run-bio-cli.ts --fuente diputados --dry-run` — verificar que parsea el XML, cuenta match/sin-match, y NO escribe. PASO 2 (LIVE diputados): correr sin --dry-run con rate-limit 2-3s (1 request a opendata.camara.cl retornarDiputadosPeriodoActual, sin WAF sobre fetch/curl; si el WAF de Node bloqueara, curl-first + --xml-file). Etapa 1 crudo a R2 (`bio/diputados-periodo-actual/<fecha>/<sha>.xml`), Etapa 2 write a PROD: parlamentario_bio + parlamentario_militancia poblados, parlamentario.partido refrescado desde la militancia ACTUAL. Reportar N actualizados / M sin match. PASO 3 (senadores): correr `--fuente senadores` segun el veredicto del spike BCN de 90-02 (BCN SPARQL o fallback ficha senado.cl); cobertura por nombre determinista fail-closed. PASO 4 (comisiones): correr `--fuente comisiones` segun el veredicto de Open Q1 de 90-02 — si hay fuente de integrantes, poblar comision+membresia; si degradado, poblar SOLO el catalogo comision (membresia vacia, estado honesto). Actualizar 90-BIO-LEDGER.md con: cobertura diputados (N/M), cobertura senadores (N/M por estado), estado de comisiones (con membresia o catalogo degradado + N comisiones), r2Path de los crudos, y la confirmacion de que NINGUN FK fue fabricado (sin-match -> skip). Verificar los conteos en PROD por psql. NUNCA persistir nombres de sin-match fuera de la maestra.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && node -e "const o=require('child_process').execSync('psql \"'+process.env.SUPABASE_DB_URL+'\" -tA -c \"select count(*) from parlamentario_militancia\"',{env:{...process.env,PGCLIENTENCODING:'UTF8'}}).toString().trim(); if(parseInt(o,10)>0){console.log('OK militancia poblada: '+o)}else{console.error('FAIL: militancia vacia');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `parlamentario_militancia` tiene filas > 0 en PROD (diputados poblados)
    - `parlamentario_bio` poblada y `parlamentario.partido` refrescado con fecha_captura fresca desde la militancia ACTUAL
    - El ledger declara la cobertura: N diputados actualizados / M sin match; N/M senadores por estado; estado de comisiones (con membresia o catalogo degradado + N comisiones); r2Path de los crudos
    - CERO FK fabricado: todo sin-match quedo como skip (verificable en el reporte del run); nombres de sin-match NO persistidos fuera de la maestra
    - El dry-run corrio ANTES del LIVE (no escribe, solo cuenta)
  </acceptance_criteria>
  <done>La bio de diputados esta poblada en PROD (militancia + bio + partido fresco), senadores y comisiones segun veredicto de spikes, cobertura declarada en el ledger, cero FK fabricado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente gubernamental -> CLI (LIVE) | red externa + WAF (opendata sin WAF; www.camara.cl con WAF -> curl-first) |
| CLI -> Supabase PROD (service_role) | write a produccion; service key nunca en logs |
| migracion -> DB viva | DDL aditivo a PROD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-SPOOF | Spoofing (falsa atribucion) | corrida LIVE (write) | mitigate | Match fail-closed (DIPID exacto / parlid-nombre); sin-match -> skip, cero FK fabricado; verificado en el reporte del run |
| T-90-EOP | Elevation of Privilege | apply 0059 a PROD | mitigate | DDL aditivo deny-by-default; pgTAP contra schema aplicado confirma cero grant anon; psql --single-transaction (rollback atomico si falla) |
| T-90-KEY | Information Disclosure | CLI/writer LIVE | mitigate | loadEnv BOM-safe; service key solo en cliente Supabase, nunca en logs; error.message de PostgREST no la contiene |
| T-90-WAF | DoS (auto-infligido / bloqueo fuente) | corrida LIVE | mitigate | rate-limit 2-3s/host + UA identificatorio (HostRateLimiter LOCKED); curl-first ante WAF; 1 request cubre la camara |
| T-90-SC | Tampering | npm installs | accept | CERO paquetes nuevos en esta fase (RESEARCH audit); sin superficie de slopsquatting nueva |
</threat_model>

<verification>
- `pnpm --filter @obs/bio test` verde + `tsc -b` compila
- `pnpm test` (raiz) verde
- Migracion 0059 aplicada a PROD; pgTAP 0059 cero `not ok` contra schema vivo
- `parlamentario_militancia` con filas > 0 en PROD; `parlamentario.partido` refrescado
- Ledger 90-BIO-LEDGER.md con apply + pgTAP + cobertura + veredicto comisiones
</verification>

<success_criteria>
- CLI run-bio-cli funcional (dry-run + LIVE + from-r2 + xml-file)
- 0059 aplicada a PROD y verificada por pgTAP
- Bio de diputados poblada en PROD (militancia + partido fresco); senadores/comisiones segun veredicto de spikes
- Cobertura N/M declarada; cero FK fabricado; PII solo en R2
- Gate de 91 desbloqueado: modelo + columna de bio + membresia (o catalogo) + bio de diputados poblada
</success_criteria>

<output>
Create `.planning/phases/90-personas-p2a-conector-bio-oficial-dos-etapas-membres-a-de-co/90-03-SUMMARY.md` when done
</output>
