---
phase: 96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat
plan: 03
type: execute
wave: 2
depends_on: ["96-01", "96-02"]
files_modified:
  - app/next.config.ts
  - .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-OPERATOR-HANDOFF.md
  - .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-CSP-DEPLOY-EVIDENCIA.md
autonomous: true
requirements: [SEC-02, SEC-04]
tags: [security, csp, deploy, cloudflare, handoff, browseros]

must_haves:
  truths:
    - "El CSP pasa de Report-Only a ENFORCED en next.config.ts (object-src 'none' + connect-src añadidos, frame-ancestors/base-uri conservados) sin romper la hidratación de Next en el deploy real"
    - "El deploy Cloudflare (runbook LOCKED: Docker node:22-slim + wrangler OAuth) va a PROD y arrastra el bump de Next del Plan 01 + los fixes latentes de 94 (dedup counts ficha + a11y)"
    - "La verificación es EMPÍRICA sobre el deploy real: BrowserOS console con 0 errores CSP + islands de filtros vivos, curl -sI muestra 'content-security-policy:' (no -report-only), errores genéricos confirmados (cero texto Postgres al cliente)"
    - "96-OPERATOR-HANDOFF.md consolida la deuda viva de operador: B26 (cita runbook 75, NO rota), pgvector-gap (del Plan 02), rotaciones=cero de gitleaks (del Plan 01), sign-offs F13/F17 y gates v7.0"
  artifacts:
    - path: "app/next.config.ts"
      provides: "CSP enforced (era Report-Only)"
      contains: "Content-Security-Policy"
    - path: ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-OPERATOR-HANDOFF.md"
      provides: "Handoff consolidado de deuda de operador (zero credential values)"
      contains: "B26"
    - path: ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-CSP-DEPLOY-EVIDENCIA.md"
      provides: "Evidencia empírica del deploy (BrowserOS + curl -sI + errores genéricos + fixes 94)"
      contains: "content-security-policy"
  key_links:
    - from: "app/next.config.ts"
      to: "Cloudflare Worker (deploy)"
      via: "headers() emitido por OpenNext en todas las rutas"
      pattern: "Content-Security-Policy"
    - from: "96-OPERATOR-HANDOFF.md"
      to: ".planning/phases/75-*/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md"
      via: "cita del runbook B26 (no duplica)"
      pattern: "75-DB-PASSWORD-ROTATION"
---

<objective>
Flipear el CSP de Report-Only a ENFORCED (SEC-02, el ítem riesgoso), deployar a Cloudflare por el runbook LOCKED (arrastrando el bump de Next del Plan 01 + los fixes latentes de 94), verificar EMPÍRICAMENTE con BrowserOS + curl, y consolidar el handoff de operador (SEC-04: B26 + pgvector-gap + gitleaks + sign-offs).

Purpose: CSP Report-Only protege CERO (Pitfall 12) — enforce pragmático > perfecto. La hidratación de Next con inline scripts sin nonce solo se valida sobre el Worker real; una CSP que rompe la app es peor que ninguna. Este es el ÚLTIMO deploy del milestone: debe llevar el Next parcheado y los fixes latentes de 94 que viajan en master sin bundlear. El handoff cierra la deuda de operador viva que el cierre de milestone referencia.
Output: `app/next.config.ts` (CSP enforced), `96-CSP-DEPLOY-EVIDENCIA.md` (BrowserOS + curl + errores genéricos + fixes 94), `96-OPERATOR-HANDOFF.md` (B26 + pgvector + rotaciones + sign-offs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-RESEARCH.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-PATTERNS.md
@app/next.config.ts

<interfaces>
<!-- CSP actual (Report-Only) y target (enforced). Cambio mínimo: 1 key + 2 líneas al value array. -->

next.config.ts HOY (línea 33): `key: "Content-Security-Policy-Report-Only"` con value:
  default-src 'self' / img-src 'self' data: / style-src 'self' 'unsafe-inline' / script-src 'self' 'unsafe-inline' / frame-ancestors 'none' / base-uri 'self' / form-action 'self'

TARGET (reemplazar SOLO la entrada CSP; los otros 5 headers intactos):
```typescript
{
  key: "Content-Security-Policy",              // enforced (era Report-Only)
  value: [
    "default-src 'self'",
    "img-src 'self' data:",
    "style-src 'self' 'unsafe-inline'",
    "script-src 'self' 'unsafe-inline'",       // Next hidrata inline sin nonce en OpenNext estático
    "connect-src 'self'",                       // navegador NO habla con Supabase directo (todo server-side); validar en deploy
    "object-src 'none'",                        // NET-NEW vs la política actual
    "frame-ancestors 'none'",
    "base-uri 'self'",
    "form-action 'self'",
  ].join("; "),
}
```

Deploy runbook LOCKED (61-02-SUMMARY): robocopy a C:/Temp/obs-build (excl node_modules/.next/.open-next/.git) → docker build node:22-slim (NUNCA alpine) → docker cp .open-next → wrangler OAuth GLOBAL (`node .../AppData/Roaming/npm/node_modules/wrangler/bin/wrangler.js deploy` desde app/, NO el wrangler pnpm-local ni el impostor Python de miniconda).

Sitio PROD: https://observatorio-congreso.thevalis.workers.dev (deploy actual 369f9cbe).
BrowserOS MCP: http://127.0.0.1:9200/mcp vía scripts/bros-cli.mjs (gotchas: sleep 8-10s entre screenshots, evaluate_script usa expression).
B26 runbook: .planning/phases/75-*/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md (cita, NO duplica; cierre "Esta nota no contiene ningún valor de secret.").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSP Report-Only → ENFORCED en next.config.ts (SEC-02)</name>
  <files>app/next.config.ts</files>
  <read_first>
    - app/next.config.ts (self-analog: los 5 headers a conservar + la entrada CSP a cambiar)
    - 96-PATTERNS.md §"app/next.config.ts" (diff mínimo: 1 key + 2 líneas) y 96-RESEARCH.md §"CSP enforced propuesta" + §"Pitfall 4" (mantener 'unsafe-inline' en script-src; nonce no disponible)
  </read_first>
  <action>
    Reemplazar SOLO la entrada CSP en `securityHeaders`: cambiar `key: "Content-Security-Policy-Report-Only"` → `key: "Content-Security-Policy"`. Añadir al value array `"connect-src 'self'"` y `"object-src 'none'"` (per <interfaces>). Conservar `script-src 'self' 'unsafe-inline'` (Next hidrata con inline sin nonce en OpenNext estático — quitarlo rompe hidratación, Pitfall 4), `frame-ancestors 'none'`, `base-uri 'self'`, `form-action 'self'`. NO tocar los otros 5 headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy) ni el shape de nextConfig ni el import de OpenNext. Actualizar el comentario del bloque CSP: era "solo Report-Only para no bloquear hidratación / pendiente report-uri" → ahora "enforced pragmático (unsafe-inline en script-src porque el worker estático no soporta nonce per-request; object-src none + frame-ancestors none como defensa de injection)".
    Correr `cd app && npx tsc --noEmit` para confirmar que el config compila. La validación EMPÍRICA (que no rompe hidratación) es la Task 2 sobre el deploy real.
  </action>
  <verify>
    <automated>grep -c '"Content-Security-Policy"' app/next.config.ts && grep -c "object-src 'none'" app/next.config.ts && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/next.config.ts` tiene `key: "Content-Security-Policy"` (SIN `-Report-Only`)
    - El value array incluye `connect-src 'self'` y `object-src 'none'`, conserva `script-src 'self' 'unsafe-inline'`, `frame-ancestors 'none'`, `base-uri 'self'`
    - Los otros 5 headers de seguridad quedan byte-idénticos
    - `npx tsc --noEmit` en app/ sale con 0 errores
  </acceptance_criteria>
  <done>CSP enforced en el config; la hidratación real la valida el deploy de la Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Cloudflare + verificación empírica BrowserOS/curl (SEC-02)</name>
  <files>.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-CSP-DEPLOY-EVIDENCIA.md</files>
  <read_first>
    - 96-PATTERNS.md §"Deploy runbook" (robocopy → docker node:22-slim → docker cp → wrangler OAuth global; los 3 gotchas)
    - 96-RESEARCH.md §"Pitfall 2" (Next prod strippea error.message al cliente; solo digest) + §"Pitfall 4" (CSP no rompe hidratación) + §"Pitfall 3" (bump Next no rompe OpenNext — el deploy es el gate)
    - MEMORY / STATE §Blockers: fixes latentes 94 (WR-01/02 dedup counts ficha + a11y de 94-UI-REVIEW) que viajan con este deploy
  </read_first>
  <action>
    Deployar por el runbook LOCKED (61-02): robocopy el árbol a C:/Temp/obs-build (excl node_modules/.next/.open-next/.git — el bump de Next del Plan 01 ya está en package.json/lock), docker build node:22-slim (NUNCA alpine), docker cp del `.open-next`, deploy con el wrangler GLOBAL OAuth (`node .../AppData/Roaming/npm/.../wrangler.js deploy` desde app/). Esto arrastra el Next parcheado + los fixes latentes de 94. Si el build Docker falla por el bump de Next (Pitfall 3): documentar el error, evaluar pin exacto, y NO deployar un bundle roto — reportar como BLOCKING.
    Verificación EMPÍRICA sobre el deploy real (NO fingir capturas — si BrowserOS MCP http://127.0.0.1:9200/mcp está caído, marcarlo en la evidencia y degradar a curl/DOM estático; el gate humano queda en el handoff, regla de la corrida):
    (a) `curl -sI https://observatorio-congreso.thevalis.workers.dev` → confirmar header `content-security-policy:` presente y SIN `-report-only`; confirmar los otros 5 headers.
    (b) BrowserOS (bros-cli.mjs, sleep 8-10s entre screenshots): abrir la home y una ficha con filtros → console SIN errores CSP; los islands de filtros (buscar-filtros/agenda-filtros) responden a interacción (hidratación viva). Si el console reporta violación de `connect-src` hacia supabase.co: significa que hay un fetch cliente→Supabase inesperado → añadir el origen Supabase a connect-src y re-deployar (degradación honesta, nunca quitar frame-ancestors/object-src); documentar el trade-off.
    (c) Errores genéricos: provocar un error (ruta inválida o RPC caída) en el deploy y confirmar por DOM/console que el cliente recibe copy genérico + digest opaco, NUNCA `relation "x" does not exist` ni texto PostgREST (Pitfall 2 — Next prod strippea el message).
    (d) Fixes 94: confirmar por DOM que los fixes latentes quedaron bundleados (dedup counts en la ficha + a11y min-h-11/focus-visible en los links nuevos de 94).
    Escribir `96-CSP-DEPLOY-EVIDENCIA.md`: versión de deploy, salida de `curl -sI` (headers, sin secretos), evidencia BrowserOS (o nota de MCP caído + curl fallback), errores genéricos confirmados, fixes 94 confirmados.
  </action>
  <verify>
    <automated>curl -sI https://observatorio-congreso.thevalis.workers.dev | grep -i "content-security-policy" | grep -vi "report-only"</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI` del sitio PROD muestra `content-security-policy:` SIN `-report-only` (grep encuentra la línea enforced)
    - `96-CSP-DEPLOY-EVIDENCIA.md` documenta: nueva versión de deploy, headers de curl, evidencia BrowserOS (console 0 errores CSP + islands vivos) o nota explícita de MCP-caído+fallback
    - Errores genéricos confirmados: el cliente NO recibe texto Postgres/PostgREST (solo digest) — documentado con la observación DOM
    - Fixes latentes 94 (dedup counts ficha + a11y) confirmados bundleados por DOM
    - Ningún valor de secreto en la evidencia (curl -sI no imprime cookies/keys)
  </acceptance_criteria>
  <done>CSP enforced LIVE sin romper hidratación (validado empíricamente); último deploy del milestone con Next parcheado + fixes 94.</done>
</task>

<task type="auto">
  <name>Task 3: 96-OPERATOR-HANDOFF.md consolidado (SEC-04)</name>
  <files>.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-OPERATOR-HANDOFF.md</files>
  <read_first>
    - .planning/phases/75-*/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md (analog + runbook B26 a CITAR, no duplicar; cierre "Esta nota no contiene ningún valor de secret.")
    - 96-PATTERNS.md §"96-OPERATOR-HANDOFF.md" (estructura zero-credential-values + contenido a consolidar)
    - 96-01-SUMMARY.md (gitleaks rotaciones=cero + FP triage) y 96-02-SUMMARY.md (pgvector gap) — los outputs de los planes wave-1
    - STATE.md §Blockers/Concerns (deuda viva: B26, sign-offs F13/F17, gates v7.0)
  </read_first>
  <action>
    Crear `96-OPERATOR-HANDOFF.md` (analog 75, zero-credential-values). Consolidar la deuda de operador viva que el cierre de milestone referencia:
    - **B26** (rotación DB password): CITAR el runbook `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` (NO duplicar sus pasos) + confirmar que el checkbox sigue abierto. Radio de impacto: solo `SUPABASE_DB_URL`; CI/sitio usan `SUPABASE_SECRET_KEY` por REST (no afectados). SEC-04: el agente NO rota.
    - **pgvector CVE-2026-3172**: del 96-AUDIT-DB-VIVA.md — extversion 0.8.0, default_version topa en 0.8.0, `alter extension` no alcanza ≥0.8.2 → platform Postgres upgrade en el dashboard cuando Supabase publique ≥0.8.2; exposición práctica matizada (0 funciones anon-executable).
    - **gitleaks**: del 96-01 — rotaciones requeridas = CERO (4 FP triados); si el Plan 01 halló un secreto NUEVO real, súmalo aquí a la tabla de rotación (sin valor).
    - **Cross-refs de deuda de milestone** (citar de STATE/MEMORY, no re-derivar): sign-offs F13 (MONEY 21.719) / F17 (NET), gates de operador v7.0 (`.planning/HANDOFF-v7.0-operator-gates.md`).
    Estructura: "Hallazgo rector" arriba, tabla "qué rompe / qué NO", pasos de OPERADOR numerados (acto humano), sección "Estado" con checkboxes. Cierre LOCKED: "Esta nota no contiene ningún valor de secret." JAMÁS imprimir valores.
  </action>
  <verify>
    <automated>test -f ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-OPERATOR-HANDOFF.md" && grep -c "75-DB-PASSWORD-ROTATION" ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-OPERATOR-HANDOFF.md"</automated>
  </verify>
  <acceptance_criteria>
    - `96-OPERATOR-HANDOFF.md` existe y CITA el runbook `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` (no lo duplica)
    - Consolida B26 (checkbox abierto, agente no rota), pgvector-gap (0.8.0 → handoff), gitleaks (rotaciones=cero o secreto nuevo sin valor), y cross-refs F13/F17 + gates v7.0
    - Tiene tabla "qué rompe / qué NO" y pasos de operador numerados
    - Cierre "Esta nota no contiene ningún valor de secret." y grep por `sb_secret_`/`eyJ`/`postgresql://.*:.*@` con password real = 0
  </acceptance_criteria>
  <done>Deuda de operador consolidada en un doc que el cierre de milestone referencia; SEC-04 documentado (agente no rota).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador hostil → sitio | Inyección de scripts/clickjacking; CSP es la defensa de injection en el cliente |
| cliente → error surface | Un error puede filtrar texto Postgres/schema si el boundary lo renderiza |
| operador → credenciales/plataforma | Rotación B26 + upgrade pgvector son actos humanos exclusivos (el agente documenta, no ejecuta) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-10 | Tampering (injection) | CSP Report-Only (protege cero) | mitigate | flip a enforced: `object-src 'none'` + `frame-ancestors 'none'` + `base-uri 'self'`; validado empíricamente en el deploy (0 errores CSP, hidratación viva) |
| T-96-11 | Information Disclosure | error surface (texto Postgres al cliente) | mitigate | Next prod strippea `error.message` (solo digest); verificado empíricamente provocando un error en el deploy real (Pitfall 2) |
| T-96-12 | Spoofing / clickjacking | frame embedding | mitigate | `frame-ancestors 'none'` (conservado) + X-Frame-Options DENY |
| T-96-13 | Elevation of Privilege | DB password B26 (arrastrado, repo público) | transfer | handoff de operador (runbook 75 citado); agente NO rota; radio de impacto solo SUPABASE_DB_URL |
| T-96-14 | (por clasificar) | pgvector 0.8.0 platform | transfer | handoff de operador (platform upgrade cuando ≥0.8.2 esté disponible); exposición baja (0 funciones anon-executable) |
</threat_model>

<verification>
- `curl -sI` PROD → `content-security-policy:` sin `-report-only`
- BrowserOS: 0 errores CSP en console + islands de filtros vivos (o nota de MCP caído + curl fallback)
- Errores genéricos: cliente recibe digest, no texto Postgres (verificado en deploy)
- Fixes 94 bundleados confirmados por DOM
- `96-OPERATOR-HANDOFF.md` cita runbook 75, consolida B26+pgvector+gitleaks+sign-offs, cero valores
- `npx tsc --noEmit` en app/ verde
</verification>

<success_criteria>
SEC-02 (cierre): CSP Report-Only → ENFORCED live sin romper hidratación (validado empíricamente), errores genéricos confirmados, headers verificados. SEC-04: rotación B26 documentada como handoff de operador (agente no rota). El deploy arrastra el Next parcheado (Plan 01) + fixes latentes 94. El handoff consolida la deuda viva que el cierre de milestone referencia.
</success_criteria>

<output>
Create `.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-03-SUMMARY.md` when done
</output>
