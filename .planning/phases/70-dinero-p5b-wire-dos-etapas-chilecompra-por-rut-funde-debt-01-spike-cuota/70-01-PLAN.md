---
phase: 70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dinero/src/ingest-run.ts
  - packages/dinero/src/ingest-run.test.ts
  - packages/dinero/src/ingest-cli.ts
  - packages/dinero/src/run-dinero-masivo-cli.ts
  - packages/dinero/src/run-dinero-masivo-cli.test.ts
  - packages/dinero/src/connector-chilecompra.test.ts
  - packages/dinero/package.json
autonomous: true
requirements: [MONEY-01, DEBT-01]
must_haves:
  truths:
    - "ChileCompra ingesta escribe PRIMERO crudo content-addressed a R2 y LUEGO R2→Supabase, re-ejecutables por separado"
    - "Un fallo del put R2 (no-412) GATEA el upsert — nunca hay derivado sin crudo reconstruible"
    - "--from-r2 replay puebla contratos con conector fake que LANZA si se toca (0 fetch a la fuente)"
    - "El MERCADOPUBLICO_TICKET nunca aparece en claro en logs/errores/envelope (redactarTicket en el nuevo path)"
    - "monto se preserva VERBATIM string (nunca re-parseado a número que altere el dato de la fuente)"
  artifacts:
    - path: "packages/dinero/src/ingest-run.ts"
      provides: "runIngestDinero con r2Store/snapshotWriter/fromR2 (Etapa 1 R2 put-gatea-upsert)"
      contains: "putImmutable"
    - path: "packages/dinero/src/ingest-run.test.ts"
      provides: "Tests fake-R2 A/B/C/D (put-antes-upsert, from-r2 0-fetch, put-falla-gatea, 412-idempotente)"
      contains: "FakeR2Store"
    - path: "packages/dinero/src/run-dinero-masivo-cli.ts"
      provides: "CLI operador que construye R2Store real de .env + acepta --from-r2"
      contains: "R2Store"
  key_links:
    - from: "packages/dinero/src/ingest-run.ts"
      to: "opts.r2Store.putImmutable"
      via: "Etapa 1 R2 antes del upsert Supabase"
      pattern: "putImmutable\\(\\s*\"dinero\""
    - from: "packages/dinero/src/run-dinero-masivo-cli.ts"
      to: "runIngestDinero"
      via: "threading de r2Store/fromR2"
      pattern: "fromR2"
---

<objective>
Añadir la Etapa 1 R2 (crudo fuente→R2 content-addressed) a `runIngestDinero`, que hoy fetchea directo del conector a Supabase SIN R2, espejando exactamente el molde de dos-etapas que Phase 66 ("RUTA A") aplicó a votos y que tramitación tiene en producción. Añadir un modo `--from-r2` replay y un CLI de operador que construye el `R2Store` real de `.env`.

Purpose: Funde DEBT-01 (dos-etapas + `--from-r2`) para ChileCompra y cumple la porción de MONEY-01 que dice "ingesta fuente→R2→Supabase re-ejecutable". El wire es PURO plumbing: threadea símbolos que ya existen y están probados.
Output: `runIngestDinero` con `r2Store`/`snapshotWriter`/`fromR2`; tests fake-R2 (put-gatea-upsert, replay 0-fetch, 412-idempotente); CLI operador `run-dinero-masivo-cli.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-RESEARCH.md
@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-VALIDATION.md
@.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-01-SUMMARY.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código. Usar directamente — sin exploración. -->

From packages/dinero/src/ingest-run.ts (el archivo a MODIFICAR):
```typescript
export interface TareaRut { rut: string; dias: string[]; }
export interface RunIngestDineroOpts {
  conector: ChileCompraConnector;
  writer: DineroWriter;
  ticket: string;
  maestra: Parlamentario[];
  tareas: TareaRut[];
  reconciliar?: ReconciliarContratoOpts;
  ingestadoHasta?: string;
  log?: (msg: string) => void;
  // ← AÑADIR: r2Store?, snapshotWriter?, fromR2?
}
export async function runIngestDinero(opts: RunIngestDineroOpts): Promise<RunIngestDineroResult>;
// Estructura interna del loop por-RUT (líneas 94-223): paso 1 buscarProveedor → codigoEmpresa/nombreProveedor;
// paso 2 por-día ordenesDeCompra → contratosTarea[]; luego reconciliarContrato → upsertContratistas/upsertContratos.
// El bloque Etapa 1 R2 va DESPUÉS del paso 2 (contratosTarea listo) y ANTES del bloque reconcilia+upsert (línea 189).
```

From packages/dinero/src/connector-chilecompra.ts (el conector — método names EXACTOS para el fake):
```typescript
async buscarProveedor(rut: string, ticket: string): Promise<unknown>;   // → { CodigoEmpresa, NombreEmpresa }
async ordenesDeCompra(codigoProveedor: string, ddmmaaaa: string, ticket: string): Promise<unknown>; // → { Cantidad, Listado }
```

From @obs/ingest (r2-store.ts):
```typescript
export async function sha256Hex(body: Uint8Array): Promise<string>;
export class R2Store {
  constructor(cfg: { accessKeyId; secretAccessKey; endpoint; bucket }, opts?);
  putImmutable(source, resource, dateBucket, sha, ext, bytes): Promise<{ r2Path: string; existed: boolean }>;
  getObject(r2Path: string): Promise<Uint8Array>;
}
export type SnapshotWriter; // .write({source, resource, cacheKey, r2Path, contentHash, fingerprint, dateBucket, provenance})
```

From packages/dinero/src/query.ts:
```typescript
export function redactarTicket(s: string): string; // reemplaza ticket=<SECRET> por ticket=*** (idempotente, null-safe)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests fake-R2 del wire dos-etapas en ingest-run.test.ts</name>
  <files>packages/dinero/src/ingest-run.test.ts</files>
  <read_first>
    - packages/dinero/src/ingest-run.ts (el runner completo — el bloque Etapa 1 R2 se insertará entre el paso 2 y el reconcilia+upsert, línea ~188)
    - packages/tramitacion/src/ingest-run.ts:293-357 (el MOLDE Etapa 1 R2 put-gatea-upsert — incluye el gate CR-01 fail-closed de Phase 66: put fallido → errores.push + continue, NO upsert)
    - packages/dinero/src/run-camara-votos.test.ts NO existe en dinero; usar en su lugar el patrón descrito en 66-01-SUMMARY (FakeR2Store putImmutable/getObject + OrderTrackingWriter con contador monotónico compartido — Test A/B/C/D)
    - packages/dinero/src/connector-chilecompra.ts:11-12,133-138 (nombres EXACTOS buscarProveedor/ordenesDeCompra para el conector fake)
    - packages/dinero/src/query.ts (redactarTicket)
    - packages/dinero/src/writer.ts (InMemoryDineroWriter / DineroWriter — upsertContratos/upsertContratistas/marcarIngestado)
  </read_first>
  <behavior>
    - Test A (put-antes-upsert): con `r2Store` (FakeR2Store) configurado y un `OrderTrackingWriter` que comparten un contador monotónico, la primera `putImmutable` (Etapa 1) captura un orden ESTRICTAMENTE menor que la primera `upsertContratos` (Etapa 2) — por orden de captura, no por presencia. DEBT-01.
    - Test B (--from-r2 replay 0-fetch): con `fromR2` apuntando a un r2Path, `runIngestDinero` lee el envelope de `getObject`, reconstruye los contratos, y el `conector` inyectado es un fake cuyos `buscarProveedor`/`ordenesDeCompra` LANZAN si se invocan → prueba 0 fetch a la fuente. Guard: `fromR2` sin `r2Store` → lanza error de args.
    - Test C (put falla gatea upsert): un FakeR2Store cuyo `putImmutable` LANZA (no-412) → el RUT se registra en `errores` (mensaje redactado) y NO se llama `upsertContratos` para ese RUT. Etapa-1-primero LOCKED.
    - Test D (412 idempotente): FakeR2Store cuyo `putImmutable` devuelve `existed=true` → se hace skip de la Etapa 2 (0 upsert para ese RUT).
    - Test E (monto VERBATIM): con un listado fake que trae un monto string particular, tras el replay el contrato persistido conserva el string byte-idéntico (no numérico). Reusa el parseo real; no toca el modelo.
  </behavior>
  <action>Escribir/extender `ingest-run.test.ts` con un `FakeR2Store` (métodos `putImmutable(source,resource,dateBucket,sha,ext,bytes)` y `getObject(r2Path)`) y un `OrderTrackingWriter` que comparte un contador monotónico con el store para probar el ORDEN de captura (Test A). El conector fake para el camino normal sirve `buscarProveedor`→`{CodigoEmpresa,NombreEmpresa}` y `ordenesDeCompra`→`{Cantidad,Listado}`; para `--from-r2` el conector fake LANZA en ambos métodos (Test B). El envelope de dinero es POR-RUT: `{ rut, buscarProveedor: <json paso1>, ordenes: { [dia]: <json paso2> } }` (Pitfall 2 — NO copiar la forma por-boletín de tramitación). Test C usa un store cuyo `putImmutable` rechaza; Test D uno que devuelve `existed:true`. Estos tests deben FALLAR ahora (RED) porque `RunIngestDineroOpts` aún no tiene `r2Store`/`fromR2`. Commit `test(70-01)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test ingest-run 2>&1 | grep -qi "fail\|FAIL\|✗\|r2Store" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `ingest-run.test.ts` contiene `FakeR2Store` con `putImmutable` y `getObject`.
    - Test A comprueba orden de captura (put < upsert) vía contador monotónico compartido, no por presencia.
    - Test B: conector fake que LANZA en `buscarProveedor`/`ordenesDeCompra` bajo `--from-r2` (0 fetch); guard `fromR2` sin `r2Store` lanza.
    - Test C: `putImmutable` que lanza → RUT en `errores`, `upsertContratos` NO llamado para ese RUT.
    - Test D: `existed:true` → skip Etapa 2.
    - Test E: monto string byte-idéntico tras replay.
    - Los tests fallan ahora (RED) — la suite reporta fallo referenciando `r2Store`/`fromR2` inexistentes.
  </acceptance_criteria>
  <done>El archivo de test define los 5 casos con fakes offline; corre y falla (RED) porque el runner aún no acepta `r2Store`/`fromR2`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Etapa 1 R2 + --from-r2 en runIngestDinero</name>
  <files>packages/dinero/src/ingest-run.ts</files>
  <read_first>
    - packages/dinero/src/ingest-run.ts (líneas 47-61 RunIngestDineroOpts; 147-223 el bloque paso-2 + reconcilia+upsert donde se inserta la Etapa 1)
    - packages/tramitacion/src/ingest-run.ts:293-357 (molde VERBATIM del bloque Etapa 1 R2: sha256Hex → putImmutable → catch gatea (errores.push + continue) → existed 412 skip → snapshotWriter best-effort)
    - packages/tramitacion/src/ingest-cli.ts:219-266 (molde del bloque --from-r2: getObject → JSON.parse → conector fake → runIngest)
    - packages/dinero/src/query.ts (redactarTicket — TODO log/errores.push del nuevo path pasa por él)
    - @obs/ingest export: `import { sha256Hex, type R2Store, type SnapshotWriter } from "@obs/ingest"` (ver tramitacion/ingest-run.ts:21)
  </read_first>
  <action>Añadir a `RunIngestDineroOpts`: `r2Store?: R2Store`, `snapshotWriter?: SnapshotWriter`, `fromR2?: string`. Importar `sha256Hex`, `type R2Store`, `type SnapshotWriter` de `@obs/ingest`. (1) Camino NORMAL: tras acumular `contratosTarea` (paso 2, línea ~186) y ANTES del bloque reconcilia+upsert (línea 189), si `opts.r2Store` está configurado, construir el envelope POR-RUT `{ rut: tarea.rut, buscarProveedor: <json crudo paso1>, ordenes: { [dia]: <json crudo paso2> } }` (guardar la RESPUESTA JSON cruda, NUNCA la URL con `&ticket=`), `sha256Hex` de los bytes, `putImmutable("dinero", tarea.rut, today, sha, "json", bytes)`. Si `putImmutable` LANZA → `errores.push({fuente, clave, mensaje: redactarTicket(String(err))})` + `continue` (gatea la Etapa 2 — Etapa-1-primero LOCKED). Si `existed` → log skip + `continue`. Tras el put exitoso, `snapshotWriter.write(...)` best-effort (try/catch no-fatal) con `source:"chilecompra"`. Para capturar el JSON crudo del paso 1/2, guardar las respuestas `json` que hoy ya se obtienen en el loop (buscarProveedor devuelve `json`; cada `ordenesDeCompra` devuelve `json` por día) en variables del envelope conforme se recorre. (2) Camino `--from-r2`: al inicio de `runIngestDinero`, si `opts.fromR2` está seteado, exigir `opts.r2Store` (lanzar `Error`/`DineroCliArgsError` si falta), `getObject(opts.fromR2)`, `JSON.parse` el envelope, construir un `conectorFake` cuyo `buscarProveedor` devuelve `envelope.buscarProveedor` y `ordenesDeCompra(_c, dia)` devuelve `envelope.ordenes[dia] ?? {Cantidad:0,Listado:[]}`, y re-entrar al flujo de reconcilia+upsert reusando el `writer` YA resuelto (W-1 de Phase 66: NO re-derivar el writer). NO tocar `reconciliar-contrato.ts`, `model.ts`, ni `0023`. Commit `feat(70-01)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test ingest-run && pnpm --filter @obs/dinero typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `RunIngestDineroOpts` tiene `r2Store?`, `snapshotWriter?`, `fromR2?`.
    - Camino normal: `putImmutable("dinero", ...)` ocurre ANTES del `upsertContratos` (Test A verde).
    - `putImmutable` que lanza → `errores.push` + `continue`, sin upsert (Test C verde).
    - `existed:true` → skip Etapa 2 (Test D verde).
    - `--from-r2`: conector fake sirve el envelope; el conector REAL nunca se toca (Test B verde, 0 fetch); guard lanza si `fromR2` sin `r2Store`.
    - Envelope guarda solo respuestas JSON crudas (NO URLs con `&ticket=`); todo `errores.push`/log del nuevo path pasa por `redactarTicket`.
    - `git diff --exit-code -- packages/dinero/src/reconciliar-contrato.ts packages/dinero/src/model.ts supabase/migrations/0023_dinero.sql` → exit 0 (VACÍO).
    - `pnpm --filter @obs/dinero test` verde (incluye la suite de 100 tests de Phase 69 sin regresión); `tsc -b` verde.
  </acceptance_criteria>
  <done>`runIngestDinero` ejecuta Etapa 1 R2 put-gatea-upsert en el camino normal y replay 0-fetch en `--from-r2`; los 5 tests verdes; reconciliador/modelo/migración con git diff vacío.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI operador run-dinero-masivo-cli + CR-01 al nuevo path</name>
  <files>packages/dinero/src/run-dinero-masivo-cli.ts, packages/dinero/src/run-dinero-masivo-cli.test.ts, packages/dinero/src/connector-chilecompra.test.ts, packages/dinero/package.json</files>
  <read_first>
    - packages/dinero/src/ingest-cli.ts (el CLI acotado de demo — construcción de writer/conector/ticket de env; NO se toca, se crea uno masivo separado — precedente Phase 66 run-votos-masivo-cli separado del ingest-cli)
    - packages/votos/src/run-votos-masivo-cli.ts:78-97 (molde de construcción de `R2Store` de `.env R2_*` + guard `--from-r2 requiere R2` + log destino LOCAL/REMOTO)
    - packages/dinero/src/query.ts (redactarTicket)
    - packages/dinero/src/connector-chilecompra.test.ts (molde del test CR-01 existente — extender: el ticket NUNCA en el envelope ni en logs del nuevo wire)
    - packages/dinero/package.json (scripts; añadir @supabase/supabase-js@^2.108.2 como dep directa SOLO si tsc -b no resuelve SupabaseClient — igual que Phase 66; pnpm install --offline, 0 descargas)
  </read_first>
  <action>Crear `run-dinero-masivo-cli.ts` (espejo de `run-votos-masivo-cli.ts`): construye un `R2Store` real de `.env R2_*` (`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_ENDPOINT_URL`/`R2_BUCKET`), toma `MERCADOPUBLICO_TICKET` de env (SOLO env, nunca argv), acepta `--from-r2 <r2Path>`, `--rut`/`--dia` repetibles (o `--ruts-file`), `--dry-run`; threadea `r2Store`/`fromR2` a `runIngestDinero`; loguea destino LOCAL/REMOTO; imprime conteos y pasa TODO `errores`/`degradaciones` por `redactarTicket`. Guard: `--from-r2` sin R2 → lanza. NO consume cuota LIVE en esta corrida (el crawl LIVE es operador-LOCAL, Wave 2). Añadir `run-dinero-masivo-cli.test.ts`: (a) construcción de R2Store cuando `.env R2_*` presente; (b) threading de `--from-r2`; (c) log destino LOCAL vs REMOTO; (d) el `MERCADOPUBLICO_TICKET` nunca aparece en la salida capturada (inyectar un ticket fake y asertar ausencia de plaintext + presencia de `ticket=***` si surfacea una URL). Extender `connector-chilecompra.test.ts` con un caso CR-01 que ejercita el nuevo path del envelope/wire: el ticket nunca en el envelope guardado ni en un mensaje de error del wire. Commit `feat(70-01)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test && pnpm --filter @obs/dinero typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `run-dinero-masivo-cli.ts` construye `R2Store` de `.env R2_*` y lo threadea (no solo grep del flag — construcción + threading verificados por test).
    - `--from-r2` reenviado a `runIngestDinero`; guard lanza si falta R2.
    - Log distingue destino LOCAL/REMOTO.
    - `run-dinero-masivo-cli.test.ts`: el `MERCADOPUBLICO_TICKET` inyectado NUNCA aparece en claro en la salida capturada; toda URL/mensaje con ticket surfacea como `ticket=***`.
    - `connector-chilecompra.test.ts` extendido: CR-01 aplicado al nuevo path (envelope no contiene el ticket).
    - `pnpm --filter @obs/dinero test` verde; `tsc -b` verde.
    - Si se añadió `@supabase/supabase-js@^2.108.2`: `pnpm install --offline` (0 descargas), documentado en el SUMMARY (no es paquete externo nuevo — ya en el monorepo).
  </acceptance_criteria>
  <done>El CLI de operador arma el R2Store real, threadea `--from-r2`, redacta el ticket en toda salida; el test del wire CR-01 verifica que el ticket nunca surfacea; suite dinero verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector→api.mercadopublico.cl | fetch server-only con ticket secreto en querystring (no en esta corrida — replay/fake) |
| runner→R2 | crudo content-addressed vía SigV4 (aws4fetch) |
| runner→Supabase | upsert versionado service_role (gated deny-by-default 0023) |
| logs/errores/envelope→consola | superficie donde el ticket podría fugarse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | MERCADOPUBLICO_TICKET en logs/errores/envelope del nuevo wire | mitigate | `redactarTicket` en TODO log/errores.push del nuevo path; el envelope guarda respuestas JSON crudas, NUNCA la URL con `&ticket=`; test dedicado (Task 3) asegura 0 plaintext |
| T-70-02 | Tampering/Integridad | Derivado sin crudo reconstruible (put R2 falla, upsert procede) | mitigate | Etapa-1-primero LOCKED: `putImmutable` que lanza → `errores.push` + `continue`, sin upsert (Test C); espejo tramitación:293-357 |
| T-70-03 | Tampering | Falso enlace parlamentario por nombre (defamación) | mitigate | `reconciliar-contrato.ts` INTACTO (git diff vacío); jurídica RUT-only fail-closed; congelado por Phase 69 (guard estático + companion) |
| T-70-04 | Tampering | Monto alterado por parseo numérico | mitigate | `monto` VERBATIM string (LOCKED); Test E prueba byte-identidad; `model.ts` no tocado |
| T-70-05 | Elevation of Privilege | Flip accidental del gate MONEY | accept (fuera de scope de este plan) | Este plan no toca `MONEY_PUBLIC_ENABLED` ni superficies públicas; el gate + anti-flip se validan en Plan 70-02 |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones de registro (RESEARCH §Package Legitimacy Audit: no aplica). La única dep potencial `@supabase/supabase-js@^2.108.2` ya vive en el monorepo → `pnpm install --offline`, 0 descargas; sin checkpoint de legitimidad (no es paquete externo nuevo) |
</threat_model>

<verification>
- `pnpm --filter @obs/dinero test` — suite dinero verde (100 de Phase 69 + los nuevos del wire).
- `pnpm --filter @obs/dinero typecheck` (`tsc -b`) — verde.
- `git diff --exit-code -- packages/dinero/src/reconciliar-contrato.ts packages/dinero/src/model.ts supabase/migrations/0023_dinero.sql` — exit 0 (VACÍO).
- Grep de aceptación: `putImmutable("dinero"` en ingest-run.ts; `fromR2` con getObject + conector fake; `R2Store` construido de `.env` en run-dinero-masivo-cli.ts.
</verification>

<success_criteria>
- ChileCompra ingesta escribe crudo→R2 ANTES de R2→Supabase; put fallido gatea el upsert; `--from-r2` replay puebla sin tocar la fuente (0 fetch).
- El `MERCADOPUBLICO_TICKET` (nombre REAL) nunca surfacea en claro; `redactarTicket` cubre el nuevo path.
- `monto` VERBATIM string preservado.
- Reconciliador/modelo/migración con git diff VACÍO.
- Suite dinero + typecheck verdes.
</success_criteria>

<output>
Create `.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-01-SUMMARY.md` when done
</output>
