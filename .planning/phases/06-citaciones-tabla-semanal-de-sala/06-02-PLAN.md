---
phase: 06-citaciones-tabla-semanal-de-sala
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - packages/agenda/src/semana-iso.ts
  - packages/agenda/src/semana-iso.test.ts
  - packages/agenda/src/parse-camara-citaciones.ts
  - packages/agenda/src/parse-camara-citaciones.test.ts
  - packages/agenda/src/parse-senado-citaciones.ts
  - packages/agenda/src/parse-senado-citaciones.test.ts
  - packages/agenda/src/parse-senado-tabla.ts
  - packages/agenda/src/parse-senado-tabla.test.ts
  - packages/agenda/src/index.ts
autonomous: true
requirements: [TRAM-07, TRAM-08]

must_haves:
  truths:
    - "El parser de Cámara extrae Citacion[] del HTML real (comisión/horario/sala/materia/estado/invitados/boletines) con cheerio"
    - "El parser de citaciones del Senado extrae Citacion[] del JSON real con CitacionPunto.boletin para el cruce"
    - "El parser de tabla del Senado extrae SesionSala + SesionTablaItem[] del JSON real"
    - "El helper de semana ISO-8601 enumera semanas correctamente en bordes de año (52 vs 53 semanas)"
    - "El slice.e2e.test.ts pasa a VERDE para los 3 parsers"
  artifacts:
    - path: "packages/agenda/src/parse-camara-citaciones.ts"
      provides: "cheerio: <article.citaciones> → Citacion[]"
      min_lines: 40
    - path: "packages/agenda/src/parse-senado-citaciones.ts"
      provides: "JSON data[].CITACIONES[] → Citacion[]"
      min_lines: 25
    - path: "packages/agenda/src/parse-senado-tabla.ts"
      provides: "JSON data[].TABLA[] → SesionSala"
      min_lines: 25
    - path: "packages/agenda/src/semana-iso.ts"
      provides: "Helper ISO-8601 (semana del jueves, rango)"
      min_lines: 20
  key_links:
    - from: "packages/agenda/src/parse-senado-citaciones.ts"
      to: "proyecto.boletin (Fase 5)"
      via: "CitacionPunto.boletin = NUMERO_BOLETIN"
      pattern: "NUMERO_BOLETIN"
    - from: "packages/agenda/src/parse-camara-citaciones.ts"
      to: "cheerio article.citaciones"
      via: "selector"
      pattern: "article\\.citaciones|article.*citaciones"
---

<objective>
Implementa los 3 parsers de la fase contra fixtures reales (cheerio sobre el HTML de Cámara, JSON de las dos APIs del Senado) más el helper de enumeración de semanas ISO-8601 — el único músculo nuevo de verdad de esta fase. Cada parser valida con zod por fila (drift) y emite el modelo común de 06-01. Vuelve a VERDE la diana `slice.e2e.test.ts`.

Purpose: Los parsers son el núcleo de TRAM-07 (citaciones ambas cámaras) y TRAM-08 (tabla del Senado). El helper ISO es el motor de la "cobertura completa" de Cámara por enumeración de semanas.
Output: 3 parsers + helper ISO, todos con tests sobre fixtures reales; slice E2E verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-RESEARCH.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-01-SUMMARY.md

<interfaces>
<!-- El modelo común y los fixtures vienen de 06-01. NO re-explorar. -->

Tipos consumidos (de packages/agenda/src/model.ts, creado en 06-01):
  Citacion { id, camara, comision, fecha, horario, sala, materia, estado, semana_iso, invitados[], puntos[], provenance }
  CitacionInvitado { nombre, calidad }
  CitacionPunto { boletin, id_proyecto, materia, tipo_tramite }
  SesionSala { id, camara, fecha, numero, hora_inicio, tipo, items[], provenance }
  SesionTablaItem { posicion, parte_sesion, materia, boletin, id_proyecto, alias, quorum }
  + zod schemas: CitacionSchema, SesionSalaSchema, etc.

Fixtures (de 06-01, en packages/agenda/test/fixtures/):
  camara-citaciones-semana.html  (prmSemana=2026-25, ~235KB)
  senado-commissions-citations.json
  senado-weekly-table.json

Estructura HTML Cámara (verbatim del RESEARCH, validado LIVE):
  <article class="grid-12 citaciones"> (uno por día)
    <p class="fecha">LUNES, 15 DE JUNIO DE 2026</p>
    <table class="tabla"><thead>Comisión|Horario|Sala|Citación|Invitados</thead>
      <tbody><tr>
        <td>Economía <p style="color:red">Suspendida</p></td>  ← estado en <p color:red>
        <td>10:00 a 12:00</td>
        <td><p>Sala Ramón Pérez Opazo<br/>tercer nivel (Presencial)</p></td>
        <td colspan="2"><table><tr>     ← OJO: última col suele venir como tabla anidada
          <td class="w40">...materia con boletín N°18296-05...</td>
          <td class="w30">...invitados...</td>
        </tr></table></td>
      </tr></tbody></table></article>
  Notas: (1) la col Citación+Invitados puede venir como <td colspan=2> con tabla anidada
  (.w40 materia / .w30 invitados) O como 2 <td> hermanos → manejar ambas formas.
  (2) estado vive en <p style*="color:red"> dentro de la celda de comisión.
  (3) un <article.citaciones> por día → iterar todos.

JSON Senado commissions_citations (verbatim del RESEARCH, validado LIVE):
  { data: [ { FECHA:"18/06/2026", CITACIONES:[
      { ID_CITACION, ID_COMISION, COMINOMBRE, LUGAR, FECHA, HORARIO, MATERIA, SIN_EFECTO,
        PUNTOS_PROPUESTOS:[{ NUMERO_BOLETIN:"16569-25", ID_PROYECTO, MATERIA, TIPO_TRAMITE }] } ] } ] }
  Ventana forward-only (no histórico por esta API).

JSON Senado weekly_table (verbatim del RESEARCH, validado LIVE):
  { data: [ { ID_SESION, NUMERO_SESION, FECHA, HORA_INICIO, HORA_TERMINO, TIPO_SESION,
      TABLA:[{ POSICION, PARTE_SESION, MATERIA, BOLETIN:"2734-14", ID_PROYECTO, ALIAS, QUORUM, LINK_PROYECTO }] } ] }
  PARTE_SESION ∈ {"ORDEN DEL DÍA","TIEMPO DE VOTACIONES"}.

Cruce de boletín: NUMERO_BOLETIN/BOLETIN formato `NNNNN-NN` casa con proyecto.boletin de Fase 5.
Algunos puntos NO traen NUMERO_BOLETIN → boletin nullable, link opcional.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Helper de enumeración de semanas ISO-8601</name>
  <files>packages/agenda/src/semana-iso.ts, packages/agenda/src/semana-iso.test.ts, packages/agenda/src/index.ts</files>
  <read_first>.planning/phases/06-citaciones-tabla-semanal-de-sala/06-RESEARCH.md</read_first>
  <behavior>
    - `isoWeekOf(date)` devuelve `{ year, week }` ISO-8601 (anclado al jueves): 2026-01-01 → semana correcta (puede pertenecer a 2025-W53/2026-W01 según el día), 2020-12-31 → la semana ISO correcta.
    - `semanaIsoKey(year, week)` formatea `YYYY-Www` con padding (`2026-W05`, `2026-W25`).
    - `enumerarSemanas(desde, hasta)` (cada uno `{year,week}`) lista todas las semanas ISO entre ambos inclusive, cruzando el borde de año sin saltar ni duplicar; respeta años de 53 semanas (p.ej. 2020, 2026 — comprobar el último jueves).
    - `prmSemanaParam(year, week)` devuelve `"{year}-{week}"` (formato del parámetro de Cámara, validado: `2026-25`).
  </behavior>
  <action>Implementar el helper ISO-8601 con la convención del jueves (la semana ISO 1 contiene el primer jueves del año / el 4 de enero). NO usar aritmética naïf de fechas (Pitfall 4). Exportar `isoWeekOf`, `semanaIsoKey`, `enumerarSemanas`, `prmSemanaParam` desde el barrel. Los tests cubren bordes de año (dic/ene), años de 52 y 53 semanas, y que `enumerarSemanas` produzca secuencias contiguas sin huecos.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test semana-iso</automated>
  </verify>
  <done>Tests de bordes ISO verdes; `prmSemanaParam(2026,25)` === "2026-25".</done>
</task>

<task type="tdd">
  <name>Task 2: Parser de citaciones de Cámara (cheerio sobre HTML real)</name>
  <files>packages/agenda/src/parse-camara-citaciones.ts, packages/agenda/src/parse-camara-citaciones.test.ts, packages/agenda/src/index.ts</files>
  <read_first>packages/agenda/test/fixtures/camara-citaciones-semana.html, packages/agenda/src/model.ts</read_first>
  <behavior>
    - `parseCamaraCitaciones(html, semanaIso)` sobre el fixture real devuelve ≥1 `Citacion` con `camara="camara"`, `comision`, `horario`, `sala`, `materia` no vacíos.
    - Extrae el `estado` ("Suspendida"/"Sin efecto") del `<p style*="color:red">` cuando está presente; null si no.
    - Maneja AMBAS formas de la última columna (tabla anidada `<td colspan=2>` con `.w40`/`.w30`, y 2 `<td>` hermanos).
    - Extrae `invitados[]` (nombre crudo; `calidad` null si no se discierne) e `puntos[]` con `boletin` cuando la materia contiene `N°NNNNN-NN`.
    - Cada `Citacion` valida contra `CitacionSchema`; una fila que no valida se descarta y se registra (drift), NO se fabrica.
  </behavior>
  <action>Implementar el parser con `cheerio.load(html)`: iterar `article.citaciones`, leer el día de `p.fecha` (parsear "LUNES, 15 DE JUNIO DE 2026" → Date es-CL), recorrer `table.tabla > tbody > tr`, mapear las columnas fijas (Comisión|Horario|Sala|Citación|Invitados). Para la celda de comisión, separar el nombre del `<p color:red>` de estado. Para la última columna, detectar la tabla anidada (`td[colspan] table` con `.w40`/`.w30`) o las 2 celdas hermanas. Extraer boletines de la materia con regex `\bN[°º]?\s?(\d{3,5}-\d{1,2})\b` → `CitacionPunto`. Generar `id` sintético estable (p.ej. hash de `semana_iso|comision|horario|fecha`). Adjuntar provenance (origen "camara", enlace = la URL de `citaciones_semana.aspx?prmSemana=...`, fecha_captura). Validar cada fila con zod; descartar+log las inválidas. NO inventar invitados ni cargos.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test parse-camara-citaciones</automated>
  </verify>
  <done>El parser devuelve Citacion[] reales del fixture (comisión/horario/sala/materia/estado/invitados/puntos), maneja la tabla anidada, valida con zod; tests verdes.</done>
</task>

<task type="tdd">
  <name>Task 3: Parsers JSON del Senado (citaciones + tabla de sala) + slice E2E verde</name>
  <files>packages/agenda/src/parse-senado-citaciones.ts, packages/agenda/src/parse-senado-citaciones.test.ts, packages/agenda/src/parse-senado-tabla.ts, packages/agenda/src/parse-senado-tabla.test.ts, packages/agenda/src/index.ts</files>
  <read_first>packages/agenda/test/fixtures/senado-commissions-citations.json, packages/agenda/test/fixtures/senado-weekly-table.json, packages/agenda/src/slice.e2e.test.ts, packages/agenda/src/model.ts</read_first>
  <behavior>
    - `parseSenadoCitaciones(json)` sobre el fixture real devuelve `Citacion[]` con `camara="senado"`, `comision`=COMINOMBRE, `materia`, y al menos una con `puntos[].boletin` poblado desde `PUNTOS_PROPUESTOS[].NUMERO_BOLETIN`.
    - `parseSenadoTabla(json)` sobre el fixture real devuelve ≥1 `SesionSala` con `items[]` (posicion int, parte_sesion, materia, boletin nullable desde BOLETIN).
    - Ambos validan con zod por fila; el campo `FECHA` "DD/MM/YYYY" se parsea a Date correctamente.
    - El `slice.e2e.test.ts` de 06-01 pasa a VERDE (los 3 parsers existen y cumplen el contrato).
  </behavior>
  <action>Implementar `parseSenadoCitaciones`: recorrer `data[].CITACIONES[]`, mapear a `Citacion` (id = ID_CITACION, comision = COMINOMBRE, sala = LUGAR, horario = HORARIO, materia = MATERIA, estado = SIN_EFECTO ? "Sin efecto" : null), `puntos` desde `PUNTOS_PROPUESTOS[]` (boletin = NUMERO_BOLETIN nullable, id_proyecto = ID_PROYECTO, tipo_tramite = TIPO_TRAMITE). Invitados [] vacío (esta API no los expone). Provenance origen "senado", enlace = la página oficial de citaciones del Senado. Implementar `parseSenadoTabla`: recorrer `data[]`, cada sesión → `SesionSala` (id = ID_SESION, numero = NUMERO_SESION, fecha = FECHA, hora_inicio = HORA_INICIO, tipo = TIPO_SESION), `items` desde `TABLA[]` (posicion = POSICION, parte_sesion = PARTE_SESION, materia = MATERIA, boletin = BOLETIN nullable, id_proyecto = ID_PROYECTO, alias = ALIAS, quorum = QUORUM). Validar cada fila con zod; descartar+log inválidas. Parsear "DD/MM/YYYY" con helper explícito (no `new Date(str)` ambiguo). Confirmar que `slice.e2e.test.ts` queda verde.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test parse-senado-citaciones parse-senado-tabla slice.e2e</automated>
  </verify>
  <done>Ambos parsers del Senado devuelven el modelo común desde los fixtures reales con boletín cruzable; `slice.e2e.test.ts` VERDE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente→parser | HTML/JSON crudo de origen externo (potencialmente malformado o con shape cambiado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | drift de esquema de la fuente | mitigate | zod por fila en los 3 parsers; una fila que no valida se descarta+registra, NUNCA se fabrica; fixtures reales como contrato |
| T-06-05 | DoS | HTML/JSON gigante (parser) | mitigate | cheerio no ejecuta scripts ni resuelve entidades externas; parseo de tamaño acotado (fixtures ~235KB); sin recursión sobre input no acotado |
| T-06-02 | Tampering | identidad de invitados | mitigate | invitados como texto crudo (nombre+calidad), sin `parlamentario_id`, sin afirmar identidad/cargo verificado |
</threat_model>

<verification>
- `pnpm --filter @obs/agenda test` verde para semana-iso, parse-camara-citaciones, parse-senado-citaciones, parse-senado-tabla.
- `slice.e2e.test.ts` VERDE (los 3 parsers cumplen el contrato).
- `pnpm -w typecheck` exit 0.
</verification>

<success_criteria>
- Helper ISO con bordes de año cubiertos.
- 3 parsers contra fixtures reales emitiendo el modelo común con zod por fila.
- Boletín cruzable poblado (Cámara desde materia, Senado desde NUMERO_BOLETIN/BOLETIN).
- slice E2E verde.
</success_criteria>

<output>
Create `.planning/phases/06-citaciones-tabla-semanal-de-sala/06-02-SUMMARY.md` when done
</output>
