---
phase: 04-adjudicacion-identidad-compuerta-humana-golden-set
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/adjudication/package.json
  - packages/adjudication/tsconfig.json
  - packages/adjudication/vitest.config.ts
  - packages/adjudication/src/index.ts
  - packages/adjudication/src/tipos.ts
  - packages/adjudication/src/candidatos.ts
  - packages/adjudication/src/candidatos.test.ts
  - packages/adjudication/src/prompt.ts
  - packages/adjudication/src/prompt.test.ts
  - packages/adjudication/src/compuerta.ts
  - packages/adjudication/src/compuerta.test.ts
  - packages/adjudication/src/mock-provider.ts
  - packages/adjudication/src/slice.e2e.test.ts
  - packages/llm/src/types.ts
  - packages/llm/src/providers/minimax.ts
  - tsconfig.json
  - tsconfig.base.json
autonomous: true
requirements: [ID-03, ID-04, ID-06]

must_haves:
  truths:
    - "Para una mención dudosa con ≥1 candidato, el pipeline produce una decisión de compuerta (auto-aceptar→probable o revisión) sin tocar la red"
    - "La compuerta enruta a revisión cuando confidence < 0.90, conflicts no vacío, decision≠match, chosen_id no es candidato, o inconsistencia cámara/periodo"
    - "confidence === 0.90 PASA la regla de umbral (borde estricto < 0.90)"
    - "Auto-aceptar produce estado 'probable' — el LLM NUNCA produce 'confirmado'"
    - "generarCandidatos filtra la maestra por apellido+cámara+periodo+región y es fail-open (región blanda)"
    - "MiniMaxProvider acepta una temperatura baja opcional sin romper callers existentes"
  artifacts:
    - path: "packages/adjudication/src/compuerta.ts"
      provides: "aplicarCompuerta() pura fail-closed + UMBRAL=0.90"
      contains: "aplicarCompuerta"
    - path: "packages/adjudication/src/candidatos.ts"
      provides: "generarCandidatos() blocking fail-open"
      contains: "generarCandidatos"
    - path: "packages/adjudication/src/prompt.ts"
      provides: "AdjudicacionSchema (zod) + construirPromptAdjudicacion()"
      contains: "AdjudicacionSchema"
    - path: "packages/adjudication/src/slice.e2e.test.ts"
      provides: "e2e del slice vertical con MiniMax mockeado"
      contains: "correrPipeline"
  key_links:
    - from: "packages/adjudication/src/compuerta.ts"
      to: "AdjudicacionSchema (decision/confidence/conflicts/chosen_id)"
      via: "consume el objeto validado"
      pattern: "confidence\\s*<\\s*0\\.90|UMBRAL"
    - from: "packages/llm/src/providers/minimax.ts"
      to: "chat.completions.create temperature"
      via: "pasa req.temperature al SDK"
      pattern: "temperature"
---

<objective>
Levanta el paquete `@obs/adjudication` y construye el CORAZÓN puro del subsistema de identidad asistida: la generación de candidatos por blocking (ID-03), el schema zod + prompt del adjudicador (ID-03), y la compuerta fail-closed con umbral 0.90 asimétrico (ID-04) que auto-acepta como máximo a `probable` (ID-06 / A4). Cierra con un test e2e del slice vertical (mención dudosa → candidatos → LLM mockeado → compuerta → probable/revisión) usando un mock determinista de `MiniMaxProvider` — sin red. También extiende `@obs/llm` con un campo `temperature?` opcional retrocompatible (A1) para fijar temperatura baja en la adjudicación.

Purpose: Aísla el único escritor de `parlamentario_id` reconciliado detrás de funciones puras testeables. El riesgo existencial #1 (un match equivocado = afirmación falsa creíble) se sella en `aplicarCompuerta`, ~15 líneas que el golden set (04-03) ejercita a fondo.
Output: Paquete `@obs/adjudication` dado de alta, lógica pura testeada, slice e2e verde con mock, `temperature?` opcional en `@obs/llm`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-CONTEXT.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-RESEARCH.md

# Patrón de scaffold de paquete (espejar EXACTAMENTE)
@packages/identity/package.json
@packages/identity/tsconfig.json
@packages/identity/vitest.config.ts

<interfaces>
<!-- Contratos que el executor consume directamente — sin explorar el codebase. -->

De @obs/core (packages/core/src/parlamentario.ts):
```typescript
export type Camara = "diputados" | "senado";
export type EstadoIdentidad = "confirmado" | "probable" | "no_confirmado";
export interface Parlamentario {
  id: string;                       // "P00001"
  nombre_normalizado: string;       // clave (tokens, sin acentos, ñ→n)
  nombres: string; apellido_paterno: string; apellido_materno: string;
  camara: Camara; periodo: string; region: string | null;
  distrito: string | null; circunscripcion: string | null;
  partido: string | null; rut: string | null;
  parlid_senado: string | null; id_diputado_camara: string | null;
  estado: EstadoIdentidad; email: string | null;
  origen: string; fecha_captura: string; enlace: string;
}
```

De @obs/core (packages/core/src/nombre.ts) — re-exportado por @obs/identity:
```typescript
// Produce { nombre_normalizado, tokens, alias_capturados } a partir de campos
// estructurados o cadena libre. tokens[0] = primer token de apellido paterno.
export function normalizarNombre(...): { nombre_normalizado: string; tokens: string[]; alias_capturados: string[] };
```

De @obs/llm (packages/llm/src/types.ts):
```typescript
export type Criticality = "critical" | "bulk";
export type Sensitivity = "public" | "personal";
export interface CompletionRequest {
  system?: string; user: string;
  criticality: Criticality; sensitivity: Sensitivity;
  maxRepairAttempts?: number;
  // ← este plan AÑADE: temperature?: number;
}
export interface LLMProvider {
  readonly id: string;
  readonly trainsOnInputs: boolean;
  complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T>;
}
```

De @obs/llm (packages/llm/src/data-routing.ts):
```typescript
export function assertNoRutInLlmInput(text: string): void;  // lanza RutInLlmInputError, mensaje SIN el RUT
export function assertSensitivityAllowed(req: { sensitivity: Sensitivity }, provider: LLMProvider): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @obs/adjudication + temperature? opcional en @obs/llm</name>
  <files>packages/adjudication/package.json, packages/adjudication/tsconfig.json, packages/adjudication/vitest.config.ts, packages/adjudication/src/index.ts, packages/adjudication/src/tipos.ts, packages/llm/src/types.ts, packages/llm/src/providers/minimax.ts, tsconfig.json, tsconfig.base.json</files>
  <action>
Da de alta el paquete `@obs/adjudication` espejando `packages/identity` (mismo `type: module`, `main`/`types`/`exports` apuntando a `src/index.ts`, script `test: "vitest run"` y `typecheck: "tsc -b"`). Dependencias: `@obs/core`, `@obs/identity`, `@obs/llm`, `@supabase/supabase-js` (^2.108.2), `zod` (^4.4.3); devDeps `@types/node`, `tsx` (^4.22.4), `vitest` (^3.0.0). El `tsconfig.json` extiende `../../tsconfig.base.json`, `composite: true`, `references` a `../core`, `../identity`, `../llm`. El `vitest.config.ts` es clon exacto del de identity (environment node, include `src/**/*.test.ts`, passWithNoTests). Registra el paquete en el `tsconfig.json` raíz (`references`) y el path mapping en `tsconfig.base.json` (espeja cómo aparece `@obs/identity`). Corre `pnpm install` para enlazar el workspace.

Crea `packages/adjudication/src/tipos.ts` con la interfaz `MencionForanea` (sin `rut`: nombreOriginal, nombreNormalizado, tokens[], camara, periodo, region|null) y `type DecisionCompuerta = { ruta: "auto-aceptar"; chosenId: string } | { ruta: "revision"; razones: string[] }`. `index.ts` re-exporta `tipos` (los módulos puros se agregan en tasks siguientes — barrel placeholder que re-exporta lo que exista).

Extiende `@obs/llm` (A1): añade `temperature?: number` a `CompletionRequest` en `packages/llm/src/types.ts` (campo OPCIONAL, doc: "temperatura del modelo; default del adapter; usar baja/0 para adjudicación determinista"). En `packages/llm/src/providers/minimax.ts`, pasa `temperature: req.temperature` al objeto de `this.client.chat.completions.create({...})` SOLO cuando `req.temperature !== undefined` (spread condicional para no alterar el comportamiento por defecto). NO cambies `validate.ts`, `router.ts` ni el barrel `index.ts`. Verifica que la suite de @obs/llm sigue verde (los 27 tests existentes no deben requerir temperature).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm -w typecheck && pnpm --filter @obs/llm test --run</automated>
  </verify>
  <done>`@obs/adjudication` resuelve en el workspace (`pnpm -w typecheck` exit 0); `CompletionRequest.temperature?` existe; los 27 tests de @obs/llm siguen verdes sin cambios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generarCandidatos (blocking fail-open) + AdjudicacionSchema + prompt</name>
  <files>packages/adjudication/src/candidatos.ts, packages/adjudication/src/candidatos.test.ts, packages/adjudication/src/prompt.ts, packages/adjudication/src/prompt.test.ts</files>
  <behavior>
generarCandidatos(mencion, maestra):
  - Test: incluye un candidato cuyo apellido paterno (token) coincide y comparte cámara+periodo
  - Test: excluye candidatos de OTRA cámara aunque el apellido coincida
  - Test: excluye candidatos de OTRO periodo
  - Test (fail-open región): si la mención NO trae región (region=null), NO filtra por región — incluye candidatos de cualquier región
  - Test (fail-open región): si mención y candidato traen región distinta, lo excluye; si coinciden, lo incluye
  - Test: "Walker P., Matías" (apellido "walker", senado) recupera al candidato Walker de la maestra aunque el nombre normalizado del candidato sea "walker prieto matias" (token de apellido compartido)
AdjudicacionSchema (zod):
  - Test: parsea { decision:"match", chosen_id:"P00123", confidence:0.95, evidence:[], conflicts:[] } OK
  - Test: rechaza confidence fuera de [0,1]
  - Test: rechaza chosen_id con formato distinto de /^P\d{5}$/ (cuando no es null)
  - Test: rechaza decision="match" con chosen_id=null (refine cruzado)
  - Test: acepta decision="uncertain" con chosen_id=null
construirPromptAdjudicacion(mencion, candidatos):
  - Test: el string resultante contiene el nombreOriginal y los nombres/cámara/periodo/región de los candidatos
  - Test: el prompt NO contiene ningún RUT (assertNoRutInLlmInput(prompt) no lanza para una mención sin RUT)
  - Test: el prompt está en español y enumera los candidatos por su id (P00xxx) para que el modelo elija de la lista
  </behavior>
  <action>
`candidatos.ts`: `generarCandidatos(m: MencionForanea, maestra: Parlamentario[]): Parlamentario[]` filtra en memoria — descarta si `p.camara !== m.camara`, si `p.periodo !== m.periodo`, y SOLO si ambos lados traen región y difieren (`m.region != null && p.region != null && p.region !== m.region`). El criterio de apellido: el token de apellido paterno de la mención (`m.tokens[0]`) debe estar entre los tokens de `p.nombre_normalizado.split(" ")`. Fail-OPEN por diseño (Pitfall 2): preferir sobre-incluir; perder el candidato real sería un falso negativo silencioso. NO uses `normalizarNombre` aquí sobre la maestra (ya viene normalizada); úsalo solo si necesitas derivar tokens de un campo libre de la mención.

`prompt.ts`: exporta `AdjudicacionSchema = z.object({ decision: z.enum(["match","no_match","uncertain"]), chosen_id: z.string().regex(/^P\d{5}$/).nullable(), confidence: z.number().min(0).max(1), evidence: z.array(z.string().max(500)).max(10), conflicts: z.array(z.string().max(500)).max(10) }).refine(o => o.decision !== "match" || o.chosen_id != null, { message: "decision=match requiere chosen_id no nulo" })` y `type Adjudicacion = z.infer<...>`. `construirPromptAdjudicacion(mencion, candidatos)` arma un prompt en español restrictivo (ver Pattern 2 del RESEARCH): instruye elegir SOLO un chosen_id de la lista provista o null, responder "uncertain" ante cualquier ambigüedad, listar inconsistencias en conflicts, citar en evidence SOLO coincidencias de nombre/cámara/periodo/región, y NUNCA inferir intención/parentesco ni usar conocimiento externo (riesgo existencial #2). Enumera cada candidato con su `id` + nombres + cámara + periodo + región. Importa `assertNoRutInLlmInput` de `@obs/llm` en el test para probar que el prompt final está limpio. NO inlinees el cuerpo del prompt como bloque de código en esta acción: el executor lo redacta en español siguiendo el SYSTEM del RESEARCH Pattern 2.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test --run candidatos prompt</automated>
  </verify>
  <done>generarCandidatos pasa los tests de blocking incluyendo el caso fail-open de región y "Walker P., Matías"; AdjudicacionSchema rechaza confidence fuera de rango, chosen_id mal formado y match-con-null; el prompt contiene candidatos y pasa assertNoRutInLlmInput.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: aplicarCompuerta fail-closed (borde 0.90) + mock provider + slice e2e</name>
  <files>packages/adjudication/src/compuerta.ts, packages/adjudication/src/compuerta.test.ts, packages/adjudication/src/mock-provider.ts, packages/adjudication/src/slice.e2e.test.ts, packages/adjudication/src/index.ts</files>
  <behavior>
aplicarCompuerta(llm, mencion, candidatos) — tabla de decisión exhaustiva:
  - Test (BORDE, MANDATORIO): confidence === 0.90 con decision=match, sin conflicts, chosen_id candidato válido, cámara/periodo consistentes → ruta="auto-aceptar"
  - Test (BORDE, MANDATORIO): confidence === 0.8999 (todo lo demás OK) → ruta="revision" con razón de umbral
  - Test: decision="no_match" → revision (aunque confidence alta)
  - Test: decision="uncertain" → revision
  - Test: conflicts=["..."] no vacío → revision (aunque confidence=1.0)
  - Test: chosen_id no está entre los candidatos → revision
  - Test: chosen_id=null → revision
  - Test: chosen del candidato con cámara distinta a la mención → revision (inconsistencia de cámara)
  - Test: chosen del candidato con periodo distinto → revision (inconsistencia de periodo)
  - Test: TODAS las reglas pasan → auto-aceptar con chosenId correcto
  - Test (acumulación): múltiples fallas producen múltiples razones (lista, no corto-circuito)
slice e2e (correrPipeline con mock determinista):
  - Test (happy path): mención dudosa con 1 candidato + mock que devuelve match/0.97/sin-conflicts → resultado ruta="auto-aceptar", estado resultante "probable" (NUNCA "confirmado")
  - Test: mención con mock que devuelve confidence 0.5 → ruta="revision"
  - Test (fail-closed PII): si la mención trae un RUT que se colaría al prompt → el pipeline lanza antes de llamar al provider y el mock registra 0 llamadas
  </behavior>
  <action>
`compuerta.ts`: `const UMBRAL = 0.90;` y `aplicarCompuerta(llm: Adjudicacion, mencion: MencionForanea, candidatos: Parlamentario[]): DecisionCompuerta`. Acumula `razones: string[]`: push si `llm.decision !== "match"`, si `llm.confidence < UMBRAL` (estricto `<`, NO `<=` — el borde 0.90 pasa), si `llm.conflicts.length > 0`, si `chosen_id == null` o no se encuentra en `candidatos`; y si el candidato elegido difiere en cámara o periodo de la mención. FAIL-CLOSED: cualquier razón → `{ ruta: "revision", razones }`; lista vacía → `{ ruta: "auto-aceptar", chosenId: llm.chosen_id! }`. Documenta en cabecera que el operador invertido (`>` en vez de `<`) es el bug existencial #1 (Pitfall 1) — por eso el test del borde exacto es obligatorio.

`mock-provider.ts`: un `LLMProvider` mockeado determinista que NO toca red: recibe un mapa `mención→Adjudicacion` (o una `Adjudicacion` fija) y un contador de llamadas; su `complete(req, schema)` corre `schema.parse(respuestaFijada)` (para validar que la respuesta del mock cumple `AdjudicacionSchema`) y devuelve el objeto. Reusa el patrón de mock de `@obs/llm` (no hace red). Expón `id="minimax-mock"`, `trainsOnInputs=false` para que `assertSensitivityAllowed` lo acepte.

`slice.e2e.test.ts`: define `correrPipeline(mencion, candidatos, provider)` INLINE en el test (la orquestación real definitiva vive en 04-03; aquí basta una composición local para el slice): si no hay candidatos → resultado no_confirmado; si hay, construye el prompt, corre `assertNoRutInLlmInput(prompt)`, llama `provider.complete(req, AdjudicacionSchema)` con `criticality:"critical"`, `sensitivity:"personal"`, `temperature: 0`, aplica `aplicarCompuerta`, y mapea `auto-aceptar → estado "probable"` (NUNCA "confirmado" — A4). El test de RUT verifica 0 llamadas al mock. Actualiza `index.ts` para re-exportar candidatos, prompt, compuerta, tipos.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test --run compuerta slice</automated>
  </verify>
  <done>El test del borde 0.90 (=== pasa, 0.8999 enruta) está presente y verde; cada regla de la compuerta tiene su test; el slice e2e auto-acepta a "probable" (nunca "confirmado") con el mock; el caso de RUT lanza con 0 llamadas al provider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dominio → @obs/llm (red MiniMax) | El prompt cruza a un tier externo; RUT/PII no deben cruzar |
| salida LLM → compuerta | La salida del modelo es untrusted hasta validarse por zod + reglas duras |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | aplicarCompuerta (umbral) | mitigate | Borde 0.90 con test `=== 0.90 → auto-aceptar` y `0.8999 → revisión`; operador estricto `<`; fail-closed (cualquier razón → revisión). El test del borde es MANDATORIO. |
| T-04-02 | Information Disclosure | construirPromptAdjudicacion | mitigate | `assertNoRutInLlmInput(promptFinal)` corre sobre el string exacto antes de `complete`; test de RUT verifica 0 llamadas al provider (fail-closed). Mención sin `rut` por diseño. |
| T-04-03 | Tampering | salida LLM (chosen_id fabricado) | mitigate | `AdjudicacionSchema` (enum/rango/regex P\d{5}) + compuerta verifica `chosen_id ∈ candidatos`; refine rechaza match-con-null. |
| T-04-04 | Elevation of Privilege | estado auto-aceptado | mitigate | Auto-aceptar mapea SOLO a `probable` (A4); el LLM nunca produce `confirmado`. Verificado en el slice e2e. |
| T-04-SC | Tampering | npm/pnpm installs | accept | Cero paquetes externos nuevos (todo `workspace:*` o ya auditado en Fases 1-2; ver Package Legitimacy Audit del RESEARCH). |
</threat_model>

<verification>
- `pnpm -w typecheck` exit 0 (paquete nuevo integrado).
- `pnpm --filter @obs/adjudication test --run` verde (candidatos + prompt + compuerta + slice).
- `pnpm --filter @obs/llm test --run` sigue verde (temperature retrocompatible).
- Borde 0.90 cubierto por test explícito; caso "Walker P., Matías" recupera candidato.
</verification>

<success_criteria>
- `@obs/adjudication` dado de alta y resolviendo en el workspace.
- `generarCandidatos` fail-open testeado (región blanda, cámara/periodo duros).
- `AdjudicacionSchema` valida y rechaza salidas adversarias.
- `aplicarCompuerta` fail-closed con el borde 0.90 exacto testeado (T-04-01).
- Slice e2e: dudoso → mock LLM → compuerta → `probable` (nunca `confirmado`); RUT → 0 llamadas.
- `temperature?` opcional en `@obs/llm` sin romper callers.
</success_criteria>

<output>
Create `.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-01-SUMMARY.md` when done
</output>
