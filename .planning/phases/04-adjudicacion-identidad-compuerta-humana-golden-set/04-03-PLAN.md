---
phase: 04-adjudicacion-identidad-compuerta-humana-golden-set
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - packages/adjudication/src/pipeline.ts
  - packages/adjudication/src/pipeline.test.ts
  - packages/adjudication/src/writer-revision.ts
  - packages/adjudication/src/writer-revision.test.ts
  - packages/adjudication/src/revisor-cli.ts
  - packages/adjudication/src/revisor-cli.test.ts
  - packages/adjudication/src/golden/golden-set.ts
  - packages/adjudication/src/golden/golden-set.test.ts
  - packages/adjudication/src/index.ts
  - packages/adjudication/package.json
autonomous: true
requirements: [ID-03, ID-04, ID-05, ID-06, ID-07, ID-08]

must_haves:
  truths:
    - "El pipeline encadena etapas 0-3: determinista (reuse) → blocking → LLM (mock) → compuerta → cola o probable, y escribe la fila de audit correspondiente"
    - "Un revisor puede confirmar/rechazar/corregir un caso de la cola vía CLI, escribiendo estado + fila de audit con revisor_id + timestamp"
    - "confirm/correct del revisor promueve el vínculo a 'confirmado'; el pipeline LLM nunca escribe 'confirmado'"
    - "El golden set corre como regresión con LLM mockeado: precisión < umbral hace FALLAR el test (bloquea deploy)"
    - "El golden set incluye 'Walker P., Matías' + homónimos + nombres de casada + abreviaturas + grafía"
    - "Existe un modo LIVE gated por env que mide precisión real de MiniMax-M3 y se salta por defecto"
  artifacts:
    - path: "packages/adjudication/src/pipeline.ts"
      provides: "correrPipeline() orquesta etapas 0-3 + escribe audit"
      contains: "correrPipeline"
    - path: "packages/adjudication/src/revisor-cli.ts"
      provides: "CLI list/show/confirm/reject/correct con revisor_id"
      contains: "revisor"
    - path: "packages/adjudication/src/golden/golden-set.ts"
      provides: "casos etiquetados incl. Walker P., Matías"
      contains: "Walker"
    - path: "packages/adjudication/src/golden/golden-set.test.ts"
      provides: "regresión que bloquea deploy + LIVE gated"
      contains: "toBeGreaterThanOrEqual"
  key_links:
    - from: "packages/adjudication/src/golden/golden-set.test.ts"
      to: "precision >= PRECISION_MIN"
      via: "expect que falla = bloquea CI"
      pattern: "PRECISION_MIN|toBeGreaterThanOrEqual"
    - from: "packages/adjudication/src/revisor-cli.ts"
      to: "identidad_audit (metodo='humano', revisor_id, created_at)"
      via: "INSERT de audit al resolver"
      pattern: "humano|revisor_id"
    - from: "packages/adjudication/src/pipeline.ts"
      to: "aplicarCompuerta + writer-revision"
      via: "revisión → cola; auto-aceptar → probable + audit"
      pattern: "aplicarCompuerta"
---

<objective>
Cierra el slice vertical de Fase 4 cableando las piezas puras de 04-01 contra la persistencia de 04-02: el `pipeline` que orquesta las cuatro etapas (determinista reuse → blocking → LLM → compuerta) y escribe audit; los `writers` de cola/vínculo/audit; el `revisor-cli` (ID-05) que confirma/rechaza/corrige con `revisor_id`+timestamp y promueve a `confirmado`; y el `golden set` (ID-07) como test de regresión que BLOQUEA el deploy si la precisión baja del umbral (incluye "Walker P., Matías") más un modo LIVE gated para medir MiniMax-M3 real.

Purpose: Completa "dado un registro ambiguo, el sistema genera candidatos, adjudica, aplica la compuerta, deja en probable/cola, o un revisor lo confirma — auditado inmutablemente; y el golden set bloquea el deploy si la precisión cae". Este es el sello del riesgo existencial #1.
Output: Pipeline + writers + CLI + golden set; suite `@obs/adjudication` verde (mock, sin red); LIVE gated apagado por defecto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-CONTEXT.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-RESEARCH.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-01-SUMMARY.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-02-SUMMARY.md

# Patrón de CLI (tsx + service role + args) y writer Supabase mockeable a espejar
@packages/identity/src/seed-cli.ts
@packages/identity/src/writer-supabase.ts

<interfaces>
<!-- Producido por 04-01 (consumir directo, sin re-explorar): -->
De @obs/adjudication (04-01):
```typescript
export interface MencionForanea { nombreOriginal: string; nombreNormalizado: string; tokens: string[]; camara: "diputados"|"senado"; periodo: string; region: string | null; }
export type DecisionCompuerta = { ruta: "auto-aceptar"; chosenId: string } | { ruta: "revision"; razones: string[] };
export function generarCandidatos(m: MencionForanea, maestra: Parlamentario[]): Parlamentario[];
export const AdjudicacionSchema; export type Adjudicacion = { decision:"match"|"no_match"|"uncertain"; chosen_id: string|null; confidence: number; evidence: string[]; conflicts: string[] };
export function construirPromptAdjudicacion(m: MencionForanea, candidatos: Parlamentario[]): string;
export function aplicarCompuerta(llm: Adjudicacion, m: MencionForanea, candidatos: Parlamentario[]): DecisionCompuerta;
// mock-provider.ts: LLMProvider determinista (id="minimax-mock", trainsOnInputs=false)
```

De @obs/llm:
```typescript
export function assertNoRutInLlmInput(text: string): void;
export function assertSensitivityAllowed(req, provider): void;
// CompletionRequest.temperature?: number (añadido en 04-01)
```

De @obs/identity:
```typescript
export function matchDeterminista(mention, maestra): { estado: EstadoIdentidad; ... }; // Etapa 0 reuse
```

Esquema DDL de 04-02 (writers escriben contra estas tablas):
  vinculo_identidad(id, mencion_nombre, mencion_normalizada, camara, periodo, parlamentario_id, estado, metodo, origen, fecha_captura, enlace)
  revision_identidad(id, vinculo_id, mencion_nombre, mencion_normalizada, camara, periodo, region, candidatos jsonb, salida_modelo jsonb, modelo_version, estado, revisor_id, motivo, created_at, resolved_at)
  identidad_audit(id, vinculo_id, metodo, decision, confidence, modelo_version, revisor_id, evidence jsonb, conflicts jsonb, created_at)  -- APPEND-ONLY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline (etapas 0-3) + writer-revision (cola/vínculo/audit, Supabase mockeable)</name>
  <files>packages/adjudication/src/pipeline.ts, packages/adjudication/src/pipeline.test.ts, packages/adjudication/src/writer-revision.ts, packages/adjudication/src/writer-revision.test.ts</files>
  <behavior>
correrPipeline(mencion, maestra, provider, writer):
  - Test: Etapa 0 — si matchDeterminista devuelve 'confirmado', NO llama al LLM (0 llamadas al mock); escribe vínculo confirmado metodo='determinista' + audit
  - Test: Etapa 1 — mención no_confirmado con 0 candidatos tras blocking → no_confirmado directo, NO llama al LLM, audit con metodo='llm'? no: registra que no hubo candidatos (sin llamada)
  - Test: Etapa 2/3 happy — 1 candidato, mock devuelve match/0.97 → auto-aceptar → vínculo 'probable' metodo='llm' + audit con confidence/modelo_version; NUNCA 'confirmado'
  - Test: Etapa 3 revisión — mock devuelve confidence 0.5 → escribe revision_identidad (estado pendiente) con candidatos+salida_modelo + audit
  - Test (fail-closed PII): mención con RUT que se colaría al prompt → lanza antes de complete; 0 llamadas al provider; nada se escribe como confirmado
writer-revision (cliente Supabase mockeado, espeja writer-supabase.test.ts):
  - Test: enqueueRevision inserta en revision_identidad con candidatos/salida_modelo serializados a jsonb
  - Test: upsertVinculo inserta/actualiza vinculo_identidad con estado+metodo
  - Test: appendAudit inserta una fila en identidad_audit (nunca update); error de DB se propaga con mensaje claro
  </behavior>
  <action>
`writer-revision.ts`: clase `RevisionWriter` espejando `SupabaseMaestraWriter` (constructor con `{ url, serviceKey, client? }`, `createClient` con `persistSession:false`; cliente inyectable para tests). Métodos: `enqueueRevision(caso)` → `insert` en `revision_identidad` (candidatos y salida_modelo van como jsonb); `upsertVinculo(v)` → `upsert` en `vinculo_identidad` por la PK o clave natural parcial; `appendAudit(row)` → `insert` (SOLO insert; nunca update — la inmutabilidad la enforcea la DB de 04-02). Cada método propaga errores con un mensaje que NO expone secretos ni RUT.

`pipeline.ts`: `correrPipeline(mencion, maestra, provider, writer)` orquesta:
  - Etapa 0: llama `matchDeterminista` (reuse de @obs/identity) con la mención sobre la maestra. Si `confirmado` → `writer.upsertVinculo({estado:'confirmado', metodo:'determinista', parlamentario_id})` + `writer.appendAudit({metodo:'determinista', decision:'confirmado'})` y retorna; NO toca el LLM.
  - Etapa 1: `generarCandidatos(mencion, maestra)`. Si 0 candidatos → vínculo no_confirmado + audit, retorna (sin LLM).
  - Etapa 2: `construirPromptAdjudicacion`, luego `assertNoRutInLlmInput(prompt)` (sobre el string EXACTO; Pitfall 3), `assertSensitivityAllowed({sensitivity:'personal'}, provider)`, `provider.complete({system, user:prompt, criticality:'critical', sensitivity:'personal', temperature:0}, AdjudicacionSchema)`.
  - Etapa 3: `aplicarCompuerta(salida, mencion, candidatos)`. `auto-aceptar` → `upsertVinculo({estado:'probable', metodo:'llm', parlamentario_id:chosenId})` + audit (confidence, modelo_version). `revision` → `enqueueRevision({candidatos, salida_modelo, modelo_version, estado:'pendiente'})` + audit. NUNCA escribe `confirmado` (A4).
  Retorna un resultado discriminado ({tipo:'determinista'|'no_confirmado'|'probable'|'revision', ...}) para que el caller/golden lo evalúe. Usa el mock-provider de 04-01 en los tests (sin red). Actualiza `index.ts` para re-exportar pipeline + writer.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test --run pipeline writer-revision</automated>
  </verify>
  <done>El pipeline cubre etapas 0-3 con mock (determinista no llama LLM; auto-aceptar→probable; revisión→cola; RUT→0 llamadas); writers escriben cola/vínculo/audit contra cliente mockeado; appendAudit solo inserta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: revisor-cli (list/show/confirm/reject/correct con revisor_id + audit humano)</name>
  <files>packages/adjudication/src/revisor-cli.ts, packages/adjudication/src/revisor-cli.test.ts, packages/adjudication/package.json</files>
  <behavior>
  - Test: `list` devuelve los casos con estado 'pendiente' de revision_identidad
  - Test: `show <id>` devuelve el caso con su registro foráneo + candidatos + salida del modelo (evidence/conflicts)
  - Test: `confirm <id> --revisor ana` → revision_identidad.estado='confirmado', revisor_id='ana', resolved_at set; promueve vinculo_identidad a estado='confirmado'; appendAudit(metodo='humano', decision='confirmado', revisor_id='ana')
  - Test: `reject <id> --revisor ana --motivo "homónimo"` → estado='rechazado', revisor_id+motivo; audit metodo='humano' decision='rechazado'
  - Test: `correct <id> --revisor ana --chosen-id P00077` → estado='corregido', el vínculo apunta a P00077 estado='confirmado'; audit metodo='humano' decision='corregido' con el nuevo id
  - Test (validación de input): id no numérico o revisor vacío → error claro, ninguna escritura
  - Test: confirmar un id inexistente o ya resuelto → error claro, sin escritura
  </behavior>
  <action>
`revisor-cli.ts` espeja el patrón de `seed-cli.ts` (entry-point detectado por regex sobre `process.argv[1]`, parse manual de args, service role local desde env `SUPABASE_LOCAL_URL`/`SUPABASE_LOCAL_SERVICE_KEY`, funciones exportadas testeables con un writer/cliente inyectable). Subcomandos:
  - `list` → consulta `revision_identidad` where estado='pendiente'; imprime id + mencion_nombre + candidatos resumidos.
  - `show <id>` → imprime el caso completo (registro foráneo + candidatos + salida_modelo evidence/conflicts).
  - `confirm <id> --revisor <quien>` → UPDATE revision_identidad (estado='confirmado', revisor_id, resolved_at=now()); upsertVinculo(estado='confirmado', metodo='humano'); appendAudit(metodo='humano', decision='confirmado', revisor_id, created_at). La promoción a `confirmado` es EXCLUSIVA del humano/determinista (A4).
  - `reject <id> --revisor <quien> --motivo <texto>` → estado='rechazado' + motivo; audit decision='rechazado'.
  - `correct <id> --revisor <quien> --chosen-id <Pxxxxx>` → valida formato /^P\d{5}$/; estado='corregido'; el vínculo apunta al nuevo id con estado='confirmado'; audit decision='corregido'.
Valida `id` numérico y `revisor` no vacío ANTES de cualquier escritura (V5; un input inválido no debe tocar la DB). Reusa `RevisionWriter` de Task 1 para appendAudit/upsertVinculo y añade los métodos de cola que falten (listarPendientes, obtenerCaso, resolver). Los tests usan un cliente Supabase mockeado (como writer-supabase.test.ts), sin red. Añade un script `revisor` a `package.json` (`tsx src/revisor-cli.ts`).
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test --run revisor-cli</automated>
  </verify>
  <done>list/show/confirm/reject/correct funcionan con cliente mockeado; confirm/correct promueven el vínculo a 'confirmado' y escriben audit metodo='humano' con revisor_id+timestamp; inputs inválidos no escriben nada.</done>
</task>

<task type="auto">
  <name>Task 3: Golden set + regresión que bloquea deploy (mock CI) + LIVE gated</name>
  <files>packages/adjudication/src/golden/golden-set.ts, packages/adjudication/src/golden/golden-set.test.ts, packages/adjudication/src/index.ts</files>
  <action>
`golden-set.ts`: exporta `GOLDEN_SET: CasoGolden[]` y la interfaz `CasoGolden { id; mencion: MencionForanea; maestraRelevante: Parlamentario[]; llmEsperado: Adjudicacion; expected: {tipo:"match"; chosenId} | {tipo:"no_match"} | {tipo:"revision"} }` (ver Code Examples del RESEARCH). Incluye ≥20 casos cubriendo OBLIGATORIAMENTE las 5 categorías:
  - "Walker P., Matías" (abreviatura de apellido materno, Senado) — caso canónico, debe matchear al Walker correcto.
  - Homónimos (dos candidatos con mismo apellido paterno en la misma cámara+periodo → expected revision).
  - Nombres de casada (apellido distinto del catálogo → blocking laxo debe recuperar el candidato; expected match o revision según ambigüedad).
  - Abreviaturas/iniciales (nombre con inicial).
  - Cambios de grafía (acentos/ñ, tildes faltantes) — normalizarNombre debe converger.
También un caso `no_match` (mención que NO está en la maestra → debe quedar no_confirmado, NO auto-aceptarse) y un caso de región distinta entre fuentes (verifica el blocking fail-open de 04-01).

`golden-set.test.ts`: implementa `evaluarGolden(set, provider)` según el Code Example del RESEARCH (cuenta tp/fp/fn: un auto-aceptar de un id equivocado = fp; precision = tp/(tp+fp), recall = tp/(tp+fn)). Dos bloques:
  - MODO CI (default): usa el mock-provider de 04-01 alimentado con `llmEsperado` por caso (mapea mención→Adjudicacion). `const PRECISION_MIN = 0.95; const RECALL_MIN = 0.80;` `expect(precision).toBeGreaterThanOrEqual(PRECISION_MIN)` — un fallo aquí BLOQUEA el deploy. `expect(recall).toBeGreaterThanOrEqual(RECALL_MIN)`. Corre `correrPipeline` con un writer no-op/in-memory (sin DB).
  - MODO LIVE (gated): `const LIVE = process.env.IDENTITY_GOLDEN_LIVE === "1";` `(LIVE ? describe : describe.skip)(...)` con `it.skipIf(!process.env.MINIMAX_API_KEY)` que instancia el `MiniMaxProvider` REAL (de @obs/llm) y mide precisión real de MiniMax-M3 contra el mismo umbral. Skip por defecto (CI no quema cuota — Pitfall 5). Espeja el patrón de `packages/llm/src/smoke.test.ts`.
Documenta el flag `IDENTITY_GOLDEN_LIVE=1` y que el gate de CI corre SIEMPRE mockeado (no depende de red ni de la key). Actualiza `index.ts` para re-exportar el golden set y `evaluarGolden`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test --run golden-set</automated>
  </verify>
  <done>El golden set tiene ≥20 casos con las 5 categorías + "Walker P., Matías" + un no_match + un caso de región distinta; el test de regresión mockeado verifica precision≥0.95 (falla = bloquea deploy); el bloque LIVE existe gated por IDENTITY_GOLDEN_LIVE y se salta por defecto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input (id/revisor/chosen-id) → DB | Input del revisor cruza a escrituras de estado |
| pipeline → red MiniMax (LIVE) | RUT/PII no deben cruzar; gate antes de complete |
| CI gate → red | El gate de deploy NO debe depender de red/cuota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering (afirmación falsa) | golden set como gate | mitigate | precision≥0.95 mockeado bloquea CI; fp (auto-aceptar id equivocado) pesa máximo; incluye los casos difíciles canónicos. |
| T-04-09 | Information Disclosure | pipeline → LLM | mitigate | `assertNoRutInLlmInput(promptFinal)` + `assertSensitivityAllowed`; test de RUT con 0 llamadas (heredado de 04-01). |
| T-04-10 | Elevation of Privilege | revisor-cli escribe 'confirmado' | mitigate | Solo confirm/correct (humano) promueve a `confirmado`; valida revisor_id no vacío + id numérico antes de escribir; audit metodo='humano'. |
| T-04-11 | Repudiation | resolución sin trazabilidad | mitigate | Cada resolución del CLI escribe `identidad_audit` (append-only de 04-02) con revisor_id + created_at. |
| T-04-12 | DoS/Quota | golden set LIVE en CI | mitigate | CI corre SIEMPRE mockeado; LIVE gated por `IDENTITY_GOLDEN_LIVE`, skip por defecto (Pitfall 5). |
| T-04-SC | Tampering | npm/pnpm installs | accept | Sin paquetes nuevos; ver Package Legitimacy Audit del RESEARCH. |
</threat_model>

<verification>
- `pnpm --filter @obs/adjudication test --run` verde completo (pipeline + writer + cli + golden), sin red.
- `pnpm -w test --run && supabase test db` verde (suite + pgTAP de 04-02).
- Golden set: precisión ≥0.95 mockeado (gate de deploy); LIVE skip sin `IDENTITY_GOLDEN_LIVE`.
- "Walker P., Matías" presente en el golden set y resuelto correctamente.
</verification>

<success_criteria>
- Pipeline orquesta etapas 0-3 y escribe audit por decisión (ID-03/04/08).
- Reviewer CLI confirma/rechaza/corrige con revisor_id+timestamp y promueve a confirmado (ID-05/06).
- LLM auto-acepta solo a `probable`; `confirmado` solo humano/determinista (A4/ID-06).
- Golden set bloquea el deploy bajo umbral, incluye "Walker P., Matías", LIVE gated (ID-07).
</success_criteria>

<output>
Create `.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-03-SUMMARY.md` when done
</output>
