---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0034_entidad_tercero.sql
  - supabase/migrations/0035_vinculo_entidad.sql
  - supabase/migrations/0036_entidad_fk.sql
  - supabase/tests/0034_entidad_tercero.test.sql
  - supabase/tests/0035_vinculo_entidad.test.sql
  - supabase/tests/0036_entidad_fk.test.sql
autonomous: false
requirements: [ENT-01, ENT-03, ENT-04]
must_haves:
  truths:
    - "Existe la maestra entidad_tercero con discriminador tipo_entidad ('natural'|'juridica') y estado default 'no_confirmado'"
    - "anon y authenticated NO pueden leer entidad_tercero, entidad_tercero_alias, vinculo_entidad ni revision_entidad (deny-by-default real)"
    - "Un vinculo_entidad ya 'confirmado' no puede degradarse ni reapuntar su entidad_tercero_id (RAISE)"
    - "El FK lobby_contraparte.contraparte_id -> entidad_tercero(id) existe; contratista.entidad_id existe; identidad_audit.tipo_entidad existe"
    - "El RPC resolver_entidad es transaccional y NO es ejecutable por anon/authenticated/public"
    - "vinculo_entidad tiene un indice unico TOTAL sobre su clave natural (tipo_entidad, mencion_normalizada)"
  artifacts:
    - path: "supabase/migrations/0034_entidad_tercero.sql"
      provides: "Maestra entidad_tercero + entidad_tercero_alias + entidad_id_seq + trigger anti-regresion + RLS deny-by-default"
      contains: "create table entidad_tercero"
    - path: "supabase/migrations/0035_vinculo_entidad.sql"
      provides: "vinculo_entidad + revision_entidad + guardas anti-demotion (RAISE) + indice unico TOTAL"
      contains: "create table vinculo_entidad"
    - path: "supabase/migrations/0036_entidad_fk.sql"
      provides: "FK lobby_contraparte.contraparte_id + contratista.entidad_id + identidad_audit.tipo_entidad + RPC resolver_entidad"
      contains: "create or replace function public.resolver_entidad"
    - path: "supabase/tests/0034_entidad_tercero.test.sql"
      provides: "pgTAP: tablas/columnas/CHECK/RLS deny + sequence + trigger coercion"
    - path: "supabase/tests/0035_vinculo_entidad.test.sql"
      provides: "pgTAP: vinculo/revision deny-by-default + guardas RAISE + indice unico total"
    - path: "supabase/tests/0036_entidad_fk.test.sql"
      provides: "pgTAP: FK + columnas nuevas + resolver_entidad anon-no-execute"
  key_links:
    - from: "lobby_contraparte.contraparte_id"
      to: "entidad_tercero.id"
      via: "foreign key constraint (0036)"
      pattern: "references entidad_tercero"
    - from: "contratista.entidad_id"
      to: "entidad_tercero.id"
      via: "foreign key column (0036)"
      pattern: "references entidad_tercero"
---

<objective>
Crear las TRES migraciones DDL + sus TRES pgTAP que levantan la maestra de terceros `entidad_tercero` y su subsistema de identidad, ESPEJANDO el subsistema de `parlamentario` (0005/0006/0007/0012/0015) con las tres piezas de logica nueva (Δ1 tipo_entidad, Δ2 defensa-en-DB juridica-solo-RUT, Δ3 cablear los FK que hoy quedan NULL).

Purpose: ENT-01 (maestra + cola + guardas + RLS deny-by-default, pgTAP verde) es el cimiento de DB de toda la fase; ademas entrega el lado-DB de ENT-03 (FK lobby_contraparte/contratista) y ENT-04 (RPC transaccional resolver_entidad). Las migraciones son la UNICA forma de probar el DDL (pgTAP); build/typecheck dan falso positivo.
Output: 3 .sql en supabase/migrations/ + 3 .test.sql en supabase/tests/, verificados localmente. El apply al remoto PROD es checkpoint de operador (no lo hace el agente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-RESEARCH.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-PATTERNS.md

<analogs>
<!-- Molde DDL: copiar estructura 1:1, renombrando parlamentario->entidad_tercero, identidad->entidad. -->
- supabase/migrations/0005_parlamentario.sql (L17-64) — maestra + alias + RLS — molde de 0034
- supabase/migrations/0012_parlamentario_estado_guarda.sql (L23-41) — trigger anti-regresion COERCION silenciosa (NO RAISE) — para 0034
- supabase/migrations/0006_revision_identidad.sql (L22-65, L85-101) — vinculo + cola + audit inmutable — molde de 0035
- supabase/migrations/0007_identidad_guardas.sql (L34-141) — guardas anti-demotion RAISE + force RLS + revoke service_role — para 0035
- supabase/migrations/0015_resolver_identidad_rpc.sql (L20-108) — RPC transaccional + grants firma-exacta — molde de 0036
- supabase/migrations/0021_lobby.sql (L64-98) — lobby_contraparte.contraparte_id nullable sin FK + revoke all — target de 0036
- supabase/migrations/0023_dinero.sql (L103-127) — contratista PK rut_proveedor sin entidad_id + revoke all — target de 0036
- supabase/tests/0005_parlamentario.test.sql, 0006_revision_identidad.test.sql, 0015_resolver_identidad.test.sql — moldes de los 3 .test.sql
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracion 0034 entidad_tercero (maestra + alias + sequence + trigger COERCION + RLS deny) + pgTAP</name>
  <read_first>
    - supabase/migrations/0005_parlamentario.sql (L17-64) — estructura maestra + alias a espejar
    - supabase/migrations/0012_parlamentario_estado_guarda.sql (L23-41) — trigger COERCION silenciosa (new.estado := old.estado, NO RAISE)
    - supabase/migrations/0021_lobby.sql (L90-98) — patron revoke all from anon,authenticated (la leccion Phase 11 que 0005 no tiene)
    - supabase/tests/0005_parlamentario.test.sql — molde del pgTAP
  </read_first>
  <action>
    Crear supabase/migrations/0034_entidad_tercero.sql espejando 0005:
    - `create sequence entidad_id_seq;` (LOCKED: id estable por sequence, no logica TS). Columna `id text primary key` con default `'E' || lpad(nextval('entidad_id_seq')::text, 5, '0')`.
    - `nombre_normalizado text not null` (clave de blocking; el fold lo hace normalizarNombre en TS aguas arriba).
    - Δ1 COLUMNA NUEVA: `tipo_entidad text not null check (tipo_entidad in ('natural','juridica'))` — discriminador de toda la fase, no existe en 0005.
    - `rut text` NULLABLE (las contrapartes de lobby no traen RUT; los proveedores si) — mismo patron que parlamentario.rut.
    - `estado text not null default 'no_confirmado' check (estado in ('confirmado','probable','no_confirmado'))` VERBATIM de 0005.
    - Provenance inline NOT NULL: `origen text not null`, `fecha_captura timestamptz not null default now()`, `enlace text not null` VERBATIM.
    - Clave natural: indice unico PARCIAL `create unique index ... on entidad_tercero(rut) where rut is not null;` (un RUT identifica univocamente).
    - Tabla `entidad_tercero_alias` VERBATIM de 0005 L52-58: `id bigint generated always as identity primary key`, `entidad_tercero_id text not null references entidad_tercero(id) on delete cascade`, `alias text not null`, `origen text`, `unique (entidad_tercero_id, alias)`.
    - RLS deny-by-default en AMBAS tablas: `enable row level security` SIN policies + `revoke all on <t> from anon, authenticated;` (copiar de 0021/0023, NO la omision de 0005 — la maestra es PII interna).
    - Δ1 trigger `entidad_tercero_estado_no_regresa()` espejo de 0012: BEFORE UPDATE que hace `new.estado := old.estado` SILENCIOSO (NO RAISE) cuando un 'confirmado' intentaria degradarse — el upsert masivo del backfill no debe abortar. `set search_path = ''`, todo calificado `public.`.
    Crear supabase/tests/0034_entidad_tercero.test.sql espejando 0005_parlamentario.test.sql: afirmar columnas (incl. tipo_entidad + CHECK), default estado='no_confirmado', existencia de entidad_id_seq, RLS habilitado, anon/authenticated SIN privilegio (has_table_privilege false), trigger de coercion presente, alias FK on delete cascade.
    NO confundir la semantica: el trigger de la MAESTRA coerciona silencioso (este task); el del VINCULO hace RAISE (Task 2). Pitfall 4.
  </action>
  <verify>
    <automated>grep -c "create table entidad_tercero" supabase/migrations/0034_entidad_tercero.sql; grep -c "tipo_entidad in ('natural','juridica')" supabase/migrations/0034_entidad_tercero.sql; grep -c "revoke all on entidad_tercero from anon, authenticated" supabase/migrations/0034_entidad_tercero.sql; grep -v '^--' supabase/migrations/0034_entidad_tercero.sql | grep -c "create sequence entidad_id_seq"</automated>
  </verify>
  <acceptance_criteria>
    - 0034.sql crea entidad_tercero (con tipo_entidad CHECK, estado default no_confirmado, rut nullable, provenance NOT NULL) + entidad_tercero_alias + entidad_id_seq + trigger COERCION + RLS deny + revoke anon/authenticated en ambas tablas.
    - 0034.test.sql afirma columnas, CHECK, default, RLS-on, anon-sin-SELECT, sequence, trigger de coercion.
  </acceptance_criteria>
  <done>Ambos archivos creados; greps verdes; el .test.sql espeja la forma de 0005_parlamentario.test.sql.</done>
</task>

<task type="auto">
  <name>Task 2: Migracion 0035 vinculo_entidad + revision_entidad + guardas RAISE + indice unico TOTAL + pgTAP</name>
  <read_first>
    - supabase/migrations/0006_revision_identidad.sql (L22-65) — vinculo_identidad + revision_identidad a espejar
    - supabase/migrations/0007_identidad_guardas.sql (L34-117) — guardas anti-demotion RAISE + force row level security
    - supabase/migrations/0014_*.sql (si existe) — la conversion de indice parcial -> TOTAL para onConflict de PostgREST (Pitfall 6)
    - supabase/migrations/0024_servel.sql (L104-113) — precedente de CHECK "regla verdadera en el esquema" (para Δ2)
    - supabase/tests/0006_revision_identidad.test.sql — molde del pgTAP
  </read_first>
  <action>
    Crear supabase/migrations/0035_vinculo_entidad.sql espejando 0006+0007:
    - Tabla `vinculo_entidad` espejo de vinculo_identidad (0006 L22-39): `id bigint generated always as identity primary key`, `mencion_nombre text not null`, `mencion_normalizada text not null`, Δ1 `tipo_entidad text not null check (tipo_entidad in ('natural','juridica'))` (reemplaza camara/periodo como clave de blocking), `entidad_tercero_id text references entidad_tercero(id)` NULLABLE, `estado text not null default 'no_confirmado' check (...)` VERBATIM, `metodo text not null check (metodo in ('determinista','llm','humano'))` VERBATIM, provenance inline NOT NULL.
    - Δ Pitfall 6 — CLAVE NATURAL RESUELTA (Open Question 1): `create unique index vinculo_entidad_clave_natural on vinculo_entidad(tipo_entidad, mencion_normalizada);` indice unico TOTAL (NO parcial `where ... is not null`), porque PostgREST .upsert(onConflict) no puede targetear un parcial. Esta clave `(tipo_entidad, mencion_normalizada)` debe coincidir byte-a-byte con el onConflict del writer (Plan 03) y el on conflict del RPC (Task 3). De esto depende ENT-05 "2da corrida = 0 nuevos".
    - Tabla `revision_entidad` espejo de revision_identidad (0006 L48-65): `id`, `vinculo_id bigint references vinculo_entidad(id)` nullable, `mencion_nombre`, `mencion_normalizada`, `tipo_entidad text not null check (...)`, `candidatos jsonb not null default '[]'::jsonb` (SIN rut — minimizacion aguas arriba), `salida_modelo jsonb`, `modelo_version text`, `estado text not null default 'pendiente' check (estado in ('pendiente','confirmado','rechazado','corregido'))`, `revisor_id text`, `motivo text`, `created_at timestamptz not null default now()`, `resolved_at timestamptz`.
    - NO crear entidad_audit: identidad_audit se reusa (la columna tipo_entidad se anade en 0036). NO recrear las guardas de inmutabilidad del audit (ya existen y ya cubren las filas de terceros).
    - Δ Guardas anti-demotion del VINCULO espejo de 0007 L34-113, semantica RAISE (hecho publico individual, OPUESTA a la coercion de la maestra): `vinculo_entidad_guarda()` BEFORE UPDATE + `vinculo_entidad_guarda_insert()` BEFORE INSERT que (1) impiden degradar/reapuntar una fila ya 'confirmado' (RAISE), (2) exigen para promover A 'confirmado' que `metodo in ('humano','determinista')` y `entidad_tercero_id` not null. `set search_path = ''`.
    - Δ2 DEFENSA-EN-DB (Open Question 2, RESUELTO: incluir): en la misma guarda, impedir confirmar `tipo_entidad='juridica'` por `metodo <> 'determinista'` (RAISE) — coherente con aporte_parlamentario_solo_confirmado (0024). El matcher TS ya lo enforce (Plan 02); esta es la defensa en profundidad LOCKED.
    - `alter table vinculo_entidad force row level security;` + RLS deny-by-default + revoke all from anon,authenticated en vinculo_entidad y revision_entidad.
    Crear supabase/tests/0035_vinculo_entidad.test.sql espejando 0006/0007 pgTAP: deny-by-default en ambas tablas (anon sin SELECT), existencia del indice unico TOTAL `vinculo_entidad_clave_natural` sobre (tipo_entidad, mencion_normalizada) y que NO sea parcial, guarda RAISE al degradar un confirmado (lives_ok/throws_ok), guarda RAISE al promover juridica por metodo<>determinista (Δ2), default estado/pendiente.
  </action>
  <verify>
    <automated>grep -c "create table vinculo_entidad" supabase/migrations/0035_vinculo_entidad.sql; grep -c "create table revision_entidad" supabase/migrations/0035_vinculo_entidad.sql; grep -c "vinculo_entidad_clave_natural" supabase/migrations/0035_vinculo_entidad.sql; grep -c "force row level security" supabase/migrations/0035_vinculo_entidad.sql; grep -v '^--' supabase/migrations/0035_vinculo_entidad.sql | grep -ic "raise"</automated>
  </verify>
  <acceptance_criteria>
    - 0035.sql crea vinculo_entidad (con indice unico TOTAL sobre (tipo_entidad, mencion_normalizada)) + revision_entidad + guardas RAISE (degradacion + promocion + Δ2 juridica) + force RLS + revoke anon/authenticated en ambas.
    - 0035.test.sql afirma deny-by-default, indice unico total no-parcial, guardas RAISE (degradacion y juridica), defaults.
  </acceptance_criteria>
  <done>Ambos archivos creados; greps verdes; el indice de la clave natural es TOTAL (sin `where`); la guarda Δ2 esta presente.</done>
</task>

<task type="auto">
  <name>Task 3: Migracion 0036 FK + columnas + RPC resolver_entidad transaccional + pgTAP</name>
  <read_first>
    - supabase/migrations/0015_resolver_identidad_rpc.sql (L20-108) — RPC molde + grants firma-exacta (Pitfall 5)
    - supabase/migrations/0021_lobby.sql (L64-82) — lobby_contraparte.contraparte_id text nullable sin FK
    - supabase/migrations/0023_dinero.sql (L103-114) — contratista PK rut_proveedor, sin entidad_id
    - supabase/tests/0015_resolver_identidad.test.sql — molde del pgTAP (anon-no-execute)
  </read_first>
  <action>
    Crear supabase/migrations/0036_entidad_fk.sql:
    - Δ3 FK: `alter table lobby_contraparte add constraint lobby_contraparte_contraparte_id_fkey foreign key (contraparte_id) references entidad_tercero(id);` (hoy contraparte_id es text nullable SIN FK).
    - Δ3 columna nueva: `alter table contratista add column entidad_id text references entidad_tercero(id);` (contratista NO la tiene; PK es rut_proveedor).
    - Δ1 columna nueva en el audit reusado: `alter table identidad_audit add column tipo_entidad text;` (NO crear entidad_audit; A3).
    - NOTA: `donante.entidad_id` se DIFIERE a Phase 36 (A2) — NO anadirlo aqui.
    - RPC `resolver_entidad` espejo casi total de resolver_identidad (0015), con un 10mo parametro `p_tipo_entidad text`:
      Firma: `resolver_entidad(p_caso_id bigint, p_estado text, p_revisor text, p_motivo text, p_resolved_at timestamptz, p_promover boolean, p_vinculo jsonb, p_decision text, p_modelo_version text, p_tipo_entidad text) returns bigint language plpgsql set search_path = ''`.
      Cuerpo (espejo 0015 L39-80): (1) UPDATE revision_entidad guardado contra estado='pendiente', 0 filas -> `raise exception ... using errcode='no_data_found'` (rollback total); (2) si p_promover, UPSERT vinculo_entidad `on conflict (tipo_entidad, mencion_normalizada)` (DEBE coincidir con el indice de Task 2); (3) INSERT identidad_audit con metodo='humano' + `tipo_entidad = p_tipo_entidad`.
    - Δ Pitfall 5 — grants firma EXACTA con los 10 tipos: `revoke execute on function public.resolver_entidad(bigint,text,text,text,timestamptz,boolean,jsonb,text,text,text) from public, anon, authenticated;` + `grant execute on function public.resolver_entidad(bigint,text,text,text,timestamptz,boolean,jsonb,text,text,text) to service_role;`. Olvidar un tipo deja la funcion grantable a public.
    Crear supabase/tests/0036_entidad_fk.test.sql espejando 0015 pgTAP: FK lobby_contraparte.contraparte_id presente (has_fk/col_is_fk), columna contratista.entidad_id presente y FK, columna identidad_audit.tipo_entidad presente, funcion resolver_entidad existe con la firma de 10 params, y anon/authenticated/public NO pueden ejecutarla (has_function_privilege false). Si es practico, un caso transaccional: UPDATE sobre revision_entidad inexistente lanza (rollback).
  </action>
  <verify>
    <automated>grep -c "references entidad_tercero(id)" supabase/migrations/0036_entidad_fk.sql; grep -c "add column tipo_entidad text" supabase/migrations/0036_entidad_fk.sql; grep -c "function public.resolver_entidad(bigint,text,text,text,timestamptz,boolean,jsonb,text,text,text)" supabase/migrations/0036_entidad_fk.sql; grep -c "on conflict (tipo_entidad, mencion_normalizada)" supabase/migrations/0036_entidad_fk.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0036.sql anade el FK lobby_contraparte.contraparte_id, la columna contratista.entidad_id, la columna identidad_audit.tipo_entidad y el RPC resolver_entidad (10 params, on conflict coincidente, grants firma-exacta revoke anon/grant service_role).
    - 0036.test.sql afirma los 3 cambios de schema + firma del RPC + anon-no-execute.
  </acceptance_criteria>
  <done>Ambos archivos creados; greps verdes; el on conflict del RPC coincide byte-a-byte con el indice de Task 2; los grants listan los 10 tipos.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: CHECKPOINT OPERADOR — aplicar 0034/0035/0036 al remoto PROD + pgTAP verde</name>
  <action>
    Checkpoint de operador (needs-human, autonomous:false). El agente NO aplica DDL al remoto. El operador aplica las 3 migraciones por psql --db-url (--single-transaction, NUNCA `supabase db push`), registra las filas en schema_migrations y corre los 3 pgTAP contra PROD. Detalle exacto en what-built / how-to-verify / resume-signal.
  </action>
  <what-built>
    Tres migraciones (0034/0035/0036) + tres pgTAP listas y verificadas localmente por grep. NO aplicadas al remoto PROD por el agente (la ultima migracion en PROD es 0033).
  </what-built>
  <how-to-verify>
    LOCKED: DDL SOLO por psql --single-transaction + fila en schema_migrations. NUNCA `supabase db push`. Como operador:
    1. Extraer SUPABASE_DB_URL (esquivar BOM U+FEFF si aplica, helper de Phases 9-12).
    2. Aplicar en orden, cada una en su propia transaccion atomica:
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0034_entidad_tercero.sql
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0035_vinculo_entidad.sql
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0036_entidad_fk.sql
    3. Registrar las 3 filas en schema_migrations (0034/0035/0036) — patron de los applies previos.
    4. Correr pgTAP contra el schema aplicado:
       psql "$SUPABASE_DB_URL" -f supabase/tests/0034_entidad_tercero.test.sql
       psql "$SUPABASE_DB_URL" -f supabase/tests/0035_vinculo_entidad.test.sql
       psql "$SUPABASE_DB_URL" -f supabase/tests/0036_entidad_fk.test.sql
       Confirmar 0/0 fallos en los tres + sin regresion en 0018/0021/0022/0023/0024 si se corren.
    5. Probe deny-by-default: con la anon key, `select * from entidad_tercero limit 1` debe dar permission denied.
  </how-to-verify>
  <verify>
    <human-check>El operador confirma: 3 migraciones aplicadas, 3 filas en schema_migrations, pgTAP 0034/0035/0036 sin fallos, probe anon permission-denied.</human-check>
  </verify>
  <done>Las 3 migraciones estan en PROD; pgTAP verde; probe anon permission-denied confirmado.</done>
  <resume-signal>Escribir "pgTAP verde" cuando las 3 migraciones esten aplicadas + pgTAP 0034/0035/0036 sin fallos + probe anon permission-denied confirmado. Si algo falla, describir el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated (PostgREST) -> tablas de terceros | Datos de terceros (Ley 21.719); nunca public-read |
| service_role -> vinculo/audit | El service_role bypassa RLS; solo el trigger/grant lo detiene |
| revisor humano -> RPC resolver_entidad | Unico canal de promocion a confirmado de un dudoso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Info Disclosure | entidad_tercero / alias / vinculo_entidad / revision_entidad | mitigate | RLS-on + cero policies + `revoke all from anon,authenticated`; pgTAP afirma anon-sin-SELECT (deny-by-default real) |
| T-35-02 | Tampering | vinculo_entidad (degradar/reapuntar un confirmado) | mitigate | Trigger `vinculo_entidad_guarda` RAISE (control vinculante, no la RLS); pgTAP throws_ok |
| T-35-03 | Spoofing/Tampering | confirmar juridica por nombre en DB | mitigate | Δ2 guarda en DB: RAISE si tipo_entidad='juridica' y metodo<>'determinista'; defensa en profundidad del matcher TS |
| T-35-04 | Elevation of Privilege | resolver_entidad ejecutable por anon | mitigate | revoke execute from public/anon/authenticated con firma de 10 tipos exacta; grant solo service_role; pgTAP anon-no-execute |
| T-35-05 | Repudiation | borrado del audit log de terceros | accept (heredado) | identidad_audit reusado: trigger BEFORE UPDATE/DELETE/TRUNCATE + revoke service_role YA existen (0006/0007); no se recrean |
| T-35-SC | Tampering | psql/pgTAP installs | accept | sin instalacion de paquetes nueva en este plan (solo .sql) |
</threat_model>

<verification>
- Los 3 .sql y 3 .test.sql existen en supabase/migrations/ y supabase/tests/.
- greps de cada task verdes (estructura, Δ1/Δ2/Δ3, indice total, firma RPC).
- La clave natural `(tipo_entidad, mencion_normalizada)` aparece IDENTICA en: el indice unico TOTAL de 0035, el on conflict del RPC en 0036.
- Checkpoint operador: pgTAP 0034/0035/0036 verde contra PROD + probe anon permission-denied.
</verification>

<success_criteria>
- ENT-01: maestra + alias + sequence + trigger anti-demotion + vinculo_entidad + revision_entidad + RLS deny-by-default, pgTAP verde (tras checkpoint).
- ENT-03 (lado DB): FK lobby_contraparte.contraparte_id + columna contratista.entidad_id.
- ENT-04 (lado DB): RPC transaccional resolver_entidad, no ejecutable por anon.
- Δ1/Δ2/Δ3 presentes y verificados; clave natural unica TOTAL resuelta (Open Question 1); defensa-en-DB juridica resuelta (Open Question 2).
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-01-SUMMARY.md` when done
</output>
