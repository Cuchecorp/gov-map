---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 03
type: execute
wave: 2
depends_on: [35-02]
files_modified:
  - packages/adjudication/src/prompt-entidad.ts
  - packages/adjudication/src/prompt-entidad.test.ts
  - packages/adjudication/src/pipeline-entidad.ts
  - packages/adjudication/src/pipeline-entidad.test.ts
  - packages/adjudication/src/writer-revision-entidad.ts
  - packages/adjudication/src/writer-revision-entidad.test.ts
  - packages/adjudication/src/revisor-entidad-cli.ts
  - packages/adjudication/src/revisor-entidad-cli.test.ts
  - packages/adjudication/src/index.ts
autonomous: true
requirements: [ENT-02, ENT-04]
must_haves:
  truths:
    - "El pipeline aplica assertNoRutInLlmInput sobre el prompt EXACTO (system+user) antes de cualquier llamada al LLM; si un RUT se cuela, lanza con 0 llamadas"
    - "Una entidad tipo_entidad='juridica' NUNCA alcanza construirPromptEntidad ni el LLM (Δ2: salta la Etapa 2 completa -> no_confirmado directo)"
    - "El LLM auto-aceptado mapea SOLO a 'probable' metodo='llm', NUNCA a 'confirmado' (A4)"
    - "Los matches dudosos van a la cola revision_entidad estado='pendiente'; la promocion humana corre via RPC resolver_entidad"
    - "La compuerta UMBRAL=0.9 es comparacion ESTRICTA < (0.90 PASA; 0.8999 -> revision)"
  artifacts:
    - path: "packages/adjudication/src/pipeline-entidad.ts"
      provides: "Orquestador 0->3 con Δ2 (juridica salta LLM) + gate RUT"
      exports: ["correrPipelineEntidad"]
    - path: "packages/adjudication/src/prompt-entidad.ts"
      provides: "AdjudicacionEntidadSchema (chosen_id /^E\\d{5}$/) + SYSTEM para terceros + construirPromptEntidad sin RUT"
    - path: "packages/adjudication/src/writer-revision-entidad.ts"
      provides: "upsertVinculoEntidad + resolverEntidad (RPC resolver_entidad con p_tipo_entidad)"
    - path: "packages/adjudication/src/revisor-entidad-cli.ts"
      provides: "CLI revisor list/show/confirm/reject/correct sobre revision_entidad"
  key_links:
    - from: "pipeline-entidad.ts"
      to: "@obs/llm assertNoRutInLlmInput"
      via: "gate sobre system+user antes de complete()"
      pattern: "assertNoRutInLlmInput"
    - from: "pipeline-entidad.ts"
      to: "@obs/identity matchDeterministaEntidad"
      via: "Etapa 0 determinista"
      pattern: "matchDeterministaEntidad"
    - from: "writer-revision-entidad.ts"
      to: "resolver_entidad RPC"
      via: ".rpc con p_tipo_entidad"
      pattern: "resolver_entidad"
---

<objective>
Construir la capa de adjudicacion de terceros en `@obs/adjudication`: el pipeline `correrPipelineEntidad` (etapas 0->3, ESPEJO de correrPipeline) con la pieza nueva Δ2 (una juridica salta la Etapa 2 LLM por completo), el prompt+schema de terceros, el writer de la cola/RPC y el CLI revisor humano. Reusa `compuerta.ts` (UMBRAL 0.9 estricto) y `assertNoRutInLlmInput` tal cual.

Purpose: ENT-02 (gate RUT al prompt + jurica-nunca-LLM) y ENT-04 (dudosos -> cola revision_entidad; promocion solo humana via RPC). Depende de Plan 02 (matchDeterministaEntidad + EnlaceEntidadConfirmado). Probado con vitest; NO depende del apply de migraciones (el writer/RPC se prueban mockeando el cliente Supabase, como writer-revision.test.ts).
Output: 8 archivos nuevos en packages/adjudication/src/ + export en index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-RESEARCH.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-PATTERNS.md

<analogs>
- packages/adjudication/src/pipeline.ts (L88-219) — etapas 0 determinista / 1 blocking / 2 LLM+gate / 3 compuerta — molde de pipeline-entidad
- packages/adjudication/src/prompt.ts (L26-91) — AdjudicacionSchema (chosen_id /^P\d{5}$/ L31) + SYSTEM_ADJUDICACION (L48-58) + construirPromptAdjudicacion (L66-91)
- packages/adjudication/src/compuerta.ts (L24-40) — UMBRAL 0.9 ESTRICTO < — REUSAR tal cual (no crear compuerta-entidad)
- packages/adjudication/src/writer-revision.ts (L155-214) — upsertVinculo onConflict (L160-170) + resolverIdentidad RPC (L182-210)
- packages/adjudication/src/revisor-cli.ts (L1-60) — subcomandos list/show/confirm/reject/correct
- packages/adjudication/src/pipeline.test.ts, writer-revision.test.ts, revisor-cli.test.ts, prompt.test.ts — moldes de tests (mock-provider.ts para el LLM)
- packages/llm/src/data-routing.ts (L46-96) — assertNoRutInLlmInput (L52) + assertSensitivityAllowed (L63) REUSAR
</analogs>

<interfaces>
<!-- Del Plan 02 (Wave 1), ya disponibles: -->
matchDeterministaEntidad(mention, maestra): ResolutionEntidad   // @obs/identity
confirmarEntidad(entidadTerceroId, metodo?): EnlaceEntidadConfirmado   // @obs/identity
<!-- Del Plan 01 (DB), targeteado por el RPC del writer: -->
resolver_entidad(p_caso_id, p_estado, p_revisor, p_motivo, p_resolved_at, p_promover, p_vinculo, p_decision, p_modelo_version, p_tipo_entidad)
// vinculo_entidad onConflict natural key = (tipo_entidad, mencion_normalizada)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prompt-entidad (schema /^E\d{5}$/ + SYSTEM terceros + construirPromptEntidad sin RUT)</name>
  <read_first>
    - packages/adjudication/src/prompt.ts (L26-91) — AdjudicacionSchema, chosen_id regex L31, SYSTEM_ADJUDICACION L48-58, construirPromptAdjudicacion L66-91
    - packages/adjudication/src/prompt.test.ts — molde
  </read_first>
  <behavior>
    - Test 1: AdjudicacionEntidadSchema acepta chosen_id 'E00042' y rechaza 'P00042' (regex /^E\d{5}$/).
    - Test 2: el schema conserva decision/confidence/evidence/conflicts identicos al analog (para que aplicarCompuerta sirva sin cambios).
    - Test 3: construirPromptEntidad sobre una mencion con candidatos NO contiene ningun RUT en su salida (sin RUT por construccion).
  </behavior>
  <action>
    Crear packages/adjudication/src/prompt-entidad.ts espejando prompt.ts:
    - Δ1 AdjudicacionEntidadSchema: copiar AdjudicacionSchema cambiando SOLO el regex de chosen_id de `/^P\d{5}$/` a `/^E\d{5}$/` (formato id de entidad, A1). Mantener decision/confidence/evidence/conflicts identicos -> aplicarCompuerta (compuerta.ts) sirve sin cambios.
    - Reescribir SYSTEM_ADJUDICACION_ENTIDAD para terceros (donantes/proveedores/gestores de lobby personas-naturales-homonimas, NO parlamentarios). Mantener las reglas anti-causalidad/anti-invencion.
    - construirPromptEntidad espeja construirPromptAdjudicacion — SIN RUT en el user (por construccion; el gate lo enforce pero el prompt no debe contenerlo).
    Crear prompt-entidad.test.ts con los casos del <behavior>.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test prompt-entidad</automated>
  </verify>
  <acceptance_criteria>
    - chosen_id valida /^E\d{5}$/ y rechaza el formato P.
    - construirPromptEntidad no emite RUT; el resto del schema es identico al analog.
  </acceptance_criteria>
  <done>prompt-entidad.ts existe; tests verdes; schema compatible con aplicarCompuerta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline-entidad (etapas 0-3 + Δ2 juridica salta LLM + gate RUT)</name>
  <read_first>
    - packages/adjudication/src/pipeline.ts (L88-219) — etapas 0 (L94-119) / 1 (L121-143) / 2 (L145-164) / 3 (L166-218)
    - packages/adjudication/src/compuerta.ts (L24-40) — UMBRAL 0.9 ESTRICTO; aplicarCompuerta acumula razones
    - packages/llm/src/data-routing.ts (L46-72) — assertNoRutInLlmInput, assertSensitivityAllowed
    - packages/adjudication/src/mock-provider.ts + pipeline.test.ts — moldes de test (provider mock)
  </read_first>
  <behavior>
    - Test 1 (Etapa 0): matchDeterministaEntidad confirmado -> upsertVinculoEntidad metodo='determinista' + audit; 0 llamadas LLM.
    - Test 2 (Δ2 CRITICO): mencion juridica -> no_confirmado directo + audit; el provider mock recibe 0 llamadas (NUNCA construye prompt ni llama complete).
    - Test 3 (Etapa 1): 0 candidatos (natural) -> no_confirmado + audit; 0 llamadas LLM.
    - Test 4 (gate RUT, ENT-02): si la mencion natural trae un RUT que se colaria al prompt, assertNoRutInLlmInput lanza RutInLlmInputError ANTES de complete() -> 0 llamadas LLM.
    - Test 5 (Etapa 3 auto-aceptar): confidence 0.90 -> probable metodo='llm' (NUNCA confirmado).
    - Test 6 (Etapa 3 borde): confidence 0.8999 -> revision -> enqueueRevision pendiente + audit vinculo_id:null.
  </behavior>
  <action>
    Crear packages/adjudication/src/pipeline-entidad.ts -> `correrPipelineEntidad(mencion, maestra, provider, writer)` espejando correrPipeline:
    - Etapa 0 DETERMINISTA: matchDeterministaEntidad confirmado -> upsertVinculoEntidad confirmado metodo='determinista' + appendAudit, RETORNA. NO toca LLM.
    - Δ2 NUEVA (antes de Etapa 1/2): si `mencion.tipoEntidad === 'juridica'` -> no_confirmado directo + audit, RETORNA. Una juridica NUNCA alcanza construirPromptEntidad ni el LLM.
    - Etapa 1 BLOCKING: 0 candidatos -> no_confirmado + audit (metodo='determinista', NO 'llm'), RETORNA.
    - Etapa 2 LLM (solo natural homonima): `const userPrompt = construirPromptEntidad(mencion, candidatos); assertNoRutInLlmInput(`${SYSTEM_ADJUDICACION_ENTIDAD}\n${userPrompt}`); assertSensitivityAllowed({ sensitivity: 'personal' }, provider); provider.complete({ system, user: userPrompt, criticality:'critical', sensitivity:'personal', temperature:0 }, AdjudicacionEntidadSchema)`.
    - Etapa 3 COMPUERTA: aplicarCompuerta (REUSAR compuerta.ts). auto-aceptar -> probable metodo='llm' (NUNCA confirmado, A4); revision -> enqueueRevision pendiente + audit vinculo_id:null.
    Crear pipeline-entidad.test.ts con los 6 casos del <behavior> (mock-provider con espia de llamadas).
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test pipeline-entidad</automated>
  </verify>
  <acceptance_criteria>
    - El test juridica afirma 0 llamadas al provider (Δ2).
    - El test del gate RUT afirma RutInLlmInputError con 0 llamadas (ENT-02).
    - auto-aceptar -> probable (nunca confirmado); borde 0.90/0.8999 correcto.
  </acceptance_criteria>
  <done>pipeline-entidad.ts existe; 6 tests verdes; juridica y gate-RUT con 0 llamadas LLM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: writer-revision-entidad (cola + RPC resolver_entidad) + revisor-entidad-cli</name>
  <read_first>
    - packages/adjudication/src/writer-revision.ts (L155-214) — upsertVinculo onConflict (L160-170) + resolverIdentidad .rpc (L182-210)
    - packages/adjudication/src/revisor-cli.ts (L1-60) — subcomandos list/show/confirm/reject/correct
    - packages/adjudication/src/writer-revision.test.ts, revisor-cli.test.ts — moldes (mock del cliente Supabase)
  </read_first>
  <behavior>
    - Test 1: upsertVinculoEntidad usa onConflict 'tipo_entidad,mencion_normalizada' (coincide con el indice unico TOTAL de 0035 — Pitfall 6).
    - Test 2: resolverEntidad llama .rpc('resolver_entidad', {...}) con los 10 params incluyendo p_tipo_entidad.
    - Test 3: el CLI 'confirm' promueve via resolverEntidad con p_promover=true (la promocion humana mintea confirmarEntidad(..., 'humano')).
    - Test 4: el CLI 'list' muestra solo revision_entidad estado='pendiente'.
  </behavior>
  <action>
    Crear packages/adjudication/src/writer-revision-entidad.ts espejando writer-revision.ts:
    - upsertVinculoEntidad: `.upsert([v], { onConflict: "tipo_entidad,mencion_normalizada" })` — la clave debe coincidir BYTE-A-BYTE con el indice unico TOTAL de 0035 y el on conflict del RPC en 0036 (Pitfall 6).
    - resolverEntidad espeja resolverIdentidad: `.rpc("resolver_entidad", { p_caso_id, p_estado, p_revisor, p_motivo, p_resolved_at, p_promover, p_vinculo, p_decision, p_modelo_version, p_tipo_entidad })` — anadir p_tipo_entidad.
    - enqueueRevision sobre revision_entidad estado='pendiente' (consumido por el pipeline Etapa 3).
    Crear packages/adjudication/src/revisor-entidad-cli.ts espejando revisor-cli.ts: subcomandos list/show/confirm/reject/correct sobre revision_entidad via RevisionEntidadWriter.resolverEntidad(). La promocion humana mintea confirmarEntidad(..., 'humano'). NINGUN dudoso se auto-confirma (gate humano LOCKED).
    Crear ambos .test.ts con los casos del <behavior> (mock del cliente Supabase). Exportar correrPipelineEntidad y RevisionEntidadWriter desde index.ts.
  </action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test writer-revision-entidad revisor-entidad-cli; pnpm --filter @obs/adjudication build</automated>
  </verify>
  <acceptance_criteria>
    - onConflict = 'tipo_entidad,mencion_normalizada' (coincide con 0035/0036).
    - resolverEntidad pasa p_tipo_entidad; el CLI 'confirm' es el unico camino de promocion (humano).
    - @obs/adjudication compila.
  </acceptance_criteria>
  <done>writer-revision-entidad.ts + revisor-entidad-cli.ts existen; tests verdes; build verde; onConflict consistente con la migracion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mencion/candidatos -> prompt LLM | RUT nunca debe cruzar; gate fail-closed |
| dudoso -> cola/confirmado | Solo el revisor humano via RPC promueve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Info Disclosure | RUT filtrado a un prompt LLM | mitigate | assertNoRutInLlmInput(system+user) antes de complete(); 0 llamadas si detecta; test #4 lo asierta (ENT-02) |
| T-35-11 | Spoofing | juridica adjudicada por LLM | mitigate | Δ2: juridica salta la Etapa 2 completa; test afirma 0 llamadas al provider |
| T-35-12 | Tampering | dudoso auto-promovido a confirmado | mitigate | auto-aceptar -> 'probable' (A4); confirmado solo via RPC resolver_entidad (humano); UMBRAL 0.9 estricto |
| T-35-13 | Tampering | huerfanos por resolucion no atomica | mitigate | resolverEntidad usa el RPC transaccional resolver_entidad (UPDATE+UPSERT+audit atomicos) |
| T-35-SC | Tampering | dependencias npm nuevas | accept | sin paquetes nuevos (reusa @obs/llm, @obs/identity, mock-provider existentes) |
</threat_model>

<verification>
- `pnpm --filter @obs/adjudication test` verde (prompt-entidad, pipeline-entidad 6 casos, writer-revision-entidad, revisor-entidad-cli).
- `pnpm --filter @obs/adjudication build` verde.
- onConflict 'tipo_entidad,mencion_normalizada' identico en writer y en la migracion 0035/0036.
</verification>

<success_criteria>
- ENT-02: gate RUT sobre el prompt EXACTO (0 llamadas si se cuela) + juridica nunca LLM (Δ2).
- ENT-04: dudosos -> revision_entidad pendiente; promocion exclusiva humana via RPC resolver_entidad; UMBRAL 0.9 estricto reusado.
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-03-SUMMARY.md` when done
</output>
