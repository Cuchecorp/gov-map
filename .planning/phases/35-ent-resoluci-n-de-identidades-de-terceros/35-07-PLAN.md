---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/0035_vinculo_entidad.test.sql
autonomous: false
requirements: [ENT-01]
gap_closure: true

must_haves:
  truths:
    - "0035_vinculo_entidad.test.sql declares plan(18) and runs exactly 18 assertions (planned == ran)"
    - "All 18 assertions pass (0 not ok) against PROD"
    - "ENT-01 0035 pgTAP suite is green — the plan-count defect from Issue 2 is resolved"
    - "With 0035 pgTAP green (18/18) AND 0036/0037 green on PROD, ENT-01 ('maestra+vinculo+revision aplicadas por psql con pgTAP verde') is fully satisfied — 35-07 is the closing plan for ENT-01"
  artifacts:
    - path: "supabase/tests/0035_vinculo_entidad.test.sql"
      provides: "Complete pgTAP suite for 0035 schema"
      contains: "select plan(18)"
  key_links:
    - from: "supabase/tests/0035_vinculo_entidad.test.sql"
      to: "PROD Supabase DB"
      via: "psql rollback-only pgTAP run"
      pattern: "select plan\\(18\\)"
---

<objective>
Fix the plan-count defect in the 0035 pgTAP suite (Issue 2 from 35-05-SUMMARY.md).

The file declares `select plan(18)` but only contains 16 assertions. pgTAP reports
"Looks like you planned 18 tests but ran 16" even though all 16 pass. This keeps
the 0035 suite from being green and blocks ENT-01 closure.

Fix: add the 2 missing assertions grounded in the real 0035 migration schema (both
must pass against PROD). Keep `plan(18)` — increase coverage, do not lower the count.

Purpose: Close ENT-01 ("maestra+vinculo+revision aplicadas por psql con pgTAP verde").
This is the terminal plan for ENT-01: once 0035 pgTAP runs 18/18 AND 0036/0037 are
confirmed green on PROD (via 35-06), ENT-01 is fully satisfied.
Output: Edited test file committed; operator confirms pgTAP runs 18/18 pass on PROD.

Execution note: 35-06 and 35-07 are independent plans (no shared artifacts or
schema dependencies). Recommend running them in the same operator PROD session:
apply 0037 (35-06) first, then run the 0035/0036/0037 pgTAP suites together.
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 2 missing assertions to 0035 pgTAP suite</name>
  <files>supabase/tests/0035_vinculo_entidad.test.sql</files>

  <read_first>
  Read the full test file at supabase/tests/0035_vinculo_entidad.test.sql.
  Read supabase/migrations/0035_vinculo_entidad.sql to confirm the real schema.

  The 16 existing asserts cover (in order):
  1.  RLS enabled on vinculo_entidad + revision_entidad (relrowsecurity = true, count = 2)
  2.  No policies on either table (deny-by-default)
  3.  anon NO SELECT on vinculo_entidad
  4.  anon NO SELECT on revision_entidad
  5.  vinculo_entidad force row level security (relforcerowsecurity = true)
  6.  has_index vinculo_entidad_clave_natural
  7.  indpred is null (TOTAL not partial)
  8.  throws_ok demotion confirmado->probable (UPDATE)
  9.  throws_ok reescribir entidad_tercero_id (UPDATE)
  10. lives_ok columnas no-criticas (UPDATE)
  11. throws_ok INSERT confirmado por metodo=llm
  12. throws_ok INSERT confirmado sin entidad_tercero_id
  13. throws_ok INSERT juridica confirmada por metodo=humano
  14. lives_ok INSERT juridica confirmada por metodo=determinista
  15. lives_ok UPDATE probable->confirmado (humano, con entidad)
  16. is() revision_entidad.estado default = 'pendiente'

  The 2 missing asserts to add (both grounded in 0035 migration, both pass against PROD):

  Assert 17 — revision_entidad does NOT have force row level security.
  Rationale: The migration applies `alter table vinculo_entidad force row level security`
  (line 177) but NOT to revision_entidad — the asymmetry is intentional (revision_entidad
  is the human review queue, vinculo_entidad is the immutable public-fact table). The
  existing suite documents force-RLS on vinculo_entidad but is silent on revision_entidad.
  This assert makes the asymmetry explicit and verifiable.
  SQL: `select is((select relforcerowsecurity from pg_class where relname = 'revision_entidad'), false, 'revision_entidad NAO tiene force row level security (asimetria intencional con vinculo_entidad)');`

  Assert 18 — anon has no INSERT grant on vinculo_entidad.
  Rationale: The migration does `revoke all on vinculo_entidad from anon, authenticated`
  (line 182). The existing suite verifies anon has no SELECT grant on vinculo_entidad
  (assert 3) but does not verify INSERT is also revoked. INSERT denial on the
  "producto final" table is the highest-value unchecked deny-by-default guarantee.
  SQL: `select is((select count(*)::int from information_schema.role_table_grants where table_name = 'vinculo_entidad' and grantee = 'anon' and privilege_type = 'INSERT'), 0, 'anon NO tiene grant INSERT sobre vinculo_entidad');`
  </read_first>

  <action>
  Edit supabase/tests/0035_vinculo_entidad.test.sql to add the 2 new assertions
  described in read_first. Place them in the logical sections they belong to:

  - Assert 17 (revision_entidad force-RLS asymmetry): insert immediately after the
    existing assert 5 `'vinculo_entidad tiene force row level security'`, still within
    the `-- ── (a) deny-by-default ──` section. This extends the force-RLS coverage
    symmetrically while grouping it with the existing force-RLS check.

  - Assert 18 (anon no INSERT on vinculo_entidad): insert after the existing
    `'anon NO tiene grant SELECT sobre revision_entidad'` assert (current assert 4),
    before the force-RLS asserts. The section header is already `-- ── (a) deny-by-default ──`.

  The final section (a) block after edits will read (in order):
    - RLS enabled (count 2)
    - no policies (count 0)
    - anon NO SELECT vinculo_entidad
    - anon NO SELECT revision_entidad
    - anon NO INSERT vinculo_entidad   ← NEW assert 18
    - vinculo_entidad force-RLS = true
    - revision_entidad force-RLS = false ← NEW assert 17

  Do NOT change `select plan(18)` — it stays as-is. Do NOT modify any other assertion,
  fixture data, or the begin/rollback wrapper.

  After editing, verify the file has exactly 18 `select is\|select has_index\|select is_empty\|select throws_ok\|select lives_ok` calls (not counting `select plan` and `select * from finish()`).

  Commit message (per phase commit convention):
  `test(35-07): add 2 missing asserts to 0035 pgTAP suite (plan-count defect Issue 2)`
  </action>

  <verify>
  <automated>
  powershell -Command "(Select-String -Path 'supabase/tests/0035_vinculo_entidad.test.sql' -Pattern 'select (is|has_index|is_empty|throws_ok|lives_ok)').Count"
  </automated>
  Expected output: 18

  Secondary check — plan declaration unchanged:
  <automated>
  powershell -Command "Select-String -Path 'supabase/tests/0035_vinculo_entidad.test.sql' -Pattern 'select plan\(18\)'"
  </automated>
  Expected: one match on `select plan(18)`.
  </verify>

  <done>
  The file has exactly 18 assertion calls. `select plan(18)` is unchanged.
  The 2 new asserts are grounded in the real 0035 migration schema (force-RLS asymmetry; anon INSERT deny)
  and will pass against PROD (no invented schema properties).
  Committed to git with the message above.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs 0035 pgTAP against PROD — confirm 18/18 green (ENT-01 closure)</name>

  <what-built>
  Task 1 added 2 assertions to the 0035 pgTAP file, bringing the total to 18
  to match the declared `plan(18)`. The file is edited and committed.

  This checkpoint closes ENT-01. With 0035 pgTAP green (18/18) here AND
  0036/0037 confirmed green via 35-06, ENT-01 ("maestra+vinculo+revision
  aplicadas por psql con pgTAP verde") is fully satisfied.

  Recommended: run this in the same PROD session as 35-06. Apply 0037 migration
  (35-06) first, then pipe all three test suites (0035, 0036, 0037) to psql.
  </what-built>

  <read_first>
  This is a rollback-only pgTAP run — the test wraps everything in begin/rollback.
  No schema changes are applied. The PROD DB is read-only from the test's perspective.

  Required env var: DATABASE_URL (or --db-url flag) pointing to PROD Supabase DB.
  The pgTAP extension must already be installed (it was confirmed present in Phase 35-05).

  Run command (PowerShell, heredoc via stdin — avoids multibyte ARGV issues per GOTCHA in 35-05):
  ```
  $env:PGCLIENTENCODING = "UTF8"
  Get-Content supabase/tests/0035_vinculo_entidad.test.sql | psql $env:DATABASE_URL
  ```
  Or equivalently with --db-url if that alias is configured.
  </read_first>

  <how-to-verify>
  1. Set PGCLIENTENCODING=UTF8 (avoids encoding issues with tildes/special chars).
  2. Pipe the test file to psql against PROD:
     `Get-Content supabase/tests/0035_vinculo_entidad.test.sql | psql $env:DATABASE_URL`
  3. Inspect pgTAP output:
     - PASS: output ends with `# Looks like you passed all 18 tests.` (or equivalent TAP summary)
     - PASS: zero lines matching `not ok`
     - PASS: `1..18` in the TAP header/footer
     - FAIL if you see `# Looks like you planned 18 tests but ran 16` (or any other count mismatch)
     - FAIL if any line starts with `not ok`
  </how-to-verify>

  <acceptance_criteria>
  - pgTAP reports `1..18` (18 tests ran)
  - pgTAP reports 0 failures (no `not ok` lines)
  - No plan-count mismatch warning
  - 0036 and 0037 pgTAP suites also green on PROD (confirmed in 35-06 or this session)
  - ENT-01 ("maestra+vinculo+revision aplicadas por psql con pgTAP verde") fully closed — 35-07 is the closing plan
  </acceptance_criteria>

  <resume-signal>
  Type "approved — 18/18 green" to close ENT-01 and complete Phase 35 gap-closure Issue 2.
  If any test fails, paste the failing `not ok` line(s) and describe the output.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → PROD DB | pgTAP assertions run as superuser in a rollback-only transaction; no writes persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 | Tampering | 0035_vinculo_entidad.test.sql | accept | Test-only file; rollback wrapper ensures no PROD state change; reviewed by operator before run |
| T-35-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; test-only file edit |
</threat_model>

<verification>
Overall phase checks for this gap-closure plan:

1. File assertion count == 18:
   `(Select-String -Path 'supabase/tests/0035_vinculo_entidad.test.sql' -Pattern 'select (is|has_index|is_empty|throws_ok|lives_ok)').Count` → 18

2. plan(18) declaration intact:
   `Select-String -Path 'supabase/tests/0035_vinculo_entidad.test.sql' -Pattern 'select plan\(18\)'` → 1 match

3. PROD pgTAP run: operator confirms 18/18 green (Task 2 checkpoint).

4. ENT-01 closure: 0035 18/18 green here + 0036/0037 green (35-06) = ENT-01 fully satisfied.
</verification>

<success_criteria>
- `0035_vinculo_entidad.test.sql` has exactly 18 assertion calls and `select plan(18)` unchanged
- Both new asserts are grounded in the real 0035 migration (force-RLS asymmetry; anon INSERT deny)
- pgTAP run against PROD returns 18 tests run, 0 failures, no plan-count mismatch
- ENT-01 ("maestra+vinculo+revision aplicadas por psql con pgTAP verde") fully closed — 35-07 is the closing plan for ENT-01, which requires both 0035 18/18 green (this plan) AND 0036/0037 green on PROD (35-06)
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-07-SUMMARY.md` when done.
</output>
