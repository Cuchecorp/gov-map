---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/identity/src/enlace-entidad-confirmado.ts
  - packages/identity/src/enlace-entidad-confirmado.test-d.ts
  - packages/identity/src/deterministic-entidad.ts
  - packages/identity/src/deterministic-entidad.test.ts
  - packages/identity/src/writer-entidad-supabase.ts
  - packages/identity/src/writer-entidad-supabase.test.ts
  - packages/identity/src/seeder-entidad.ts
  - packages/identity/src/seeder-entidad.test.ts
  - packages/identity/src/backup-entidad.ts
  - packages/identity/src/backup-entidad.test.ts
  - packages/identity/src/backfill-entidad-cli.ts
  - packages/identity/src/index.ts
autonomous: true
requirements: [ENT-02, ENT-05]
must_haves:
  truths:
    - "matchDeterministaEntidad confirma por RUT-unico o nombre-unico-por-tipo; toda ambiguedad -> no_confirmado (fail-closed)"
    - "Una entidad tipo_entidad='juridica' SOLO puede confirmar por RUT exacto; nombre-sin-RUT de juridica -> siempre no_confirmado (Δ2, nunca habilita LLM)"
    - "Existe EnlaceEntidadConfirmado branded con simbolo privado propio (no exportado); un string crudo asignado al FK es error de compilacion"
    - "El backfill LOCAL es idempotente/reanudable: una 2da corrida produce 0 entidades/vinculos nuevos"
    - "La maestra entidad_tercero se exporta a JSON deterministico byte-a-byte para custodia en git"
  artifacts:
    - path: "packages/identity/src/deterministic-entidad.ts"
      provides: "matchDeterministaEntidad puro fail-closed con rama juridica-solo-RUT (Δ1/Δ2)"
      exports: ["matchDeterministaEntidad"]
    - path: "packages/identity/src/enlace-entidad-confirmado.ts"
      provides: "EnlaceEntidadConfirmado branded + confirmarEntidad factory"
      exports: ["EnlaceEntidadConfirmado", "confirmarEntidad"]
    - path: "packages/identity/src/seeder-entidad.ts"
      provides: "Upsert idempotente por clave natural (ENT-05)"
    - path: "packages/identity/src/backfill-entidad-cli.ts"
      provides: "CLI LOCAL idempotente/reanudable"
    - path: "packages/identity/src/backup-entidad.ts"
      provides: "Export JSON determinista a supabase/seeds/entidad_tercero.seed.json"
  key_links:
    - from: "deterministic-entidad.ts"
      to: "@obs/identity normRut/isRutValido"
      via: "import (reusar, no reimplementar el DV)"
      pattern: "normRut|isRutValido"
    - from: "enlace-entidad-confirmado.ts"
      to: "unique symbol privado"
      via: "branded type no exportado (grep-gate)"
      pattern: "declare const ENLACE_ENTIDAD_CONFIRMADO"
---

<objective>
Construir el nucleo TS PURO del matcher de terceros en `@obs/identity`: el matcher determinista `matchDeterministaEntidad` (Δ1 tipo_entidad + Δ2 juridica-solo-RUT), el branded type propio `EnlaceEntidadConfirmado`, el writer Supabase, el seeder idempotente, el export JSON de custodia y el CLI de backfill LOCAL. Todo es ESPEJO MECANICO del subsistema de `parlamentario` excepto las dos piezas de logica nueva (Δ1/Δ2) y los dos artefactos branded/custodia que NO son reusables (simbolo privado + tipo Parlamentario-especifico).

Purpose: ENT-02 (matcher fail-closed con la regla juridica-solo-RUT, ≥10 tests) y ENT-05 (backfill idempotente + custodia JSON). Es la pieza que consumen el pipeline LLM (Plan 03) y los reconciliadores (Plan 04), por eso va en Wave 1 — entrega los CONTRATOS (matcher + branded type) que esos planes implementan contra. Probado con vitest (`pnpm --filter @obs/identity test`); NO depende del apply de migraciones.
Output: 11 archivos nuevos en packages/identity/src/ + export en el barrel index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-RESEARCH.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-PATTERNS.md

<analogs>
- packages/identity/src/deterministic.ts (L52-139) — matcher PURO fail-closed; normRut (L52-54) e isRutValido (L63-78) REUSABLES tal cual desde el barrel
- packages/identity/src/enlace-confirmado.ts (L27-71) — PATRON del branded type (NO el tipo: parlamentarioId + simbolo privado ENLACE_CONFIRMADO no exportado)
- packages/identity/src/enlace-confirmado.test-d.ts — molde del test de tipos + grep-gate
- packages/identity/src/writer-supabase.ts (L52-144) — upsert/promote (molde de writer-entidad-supabase)
- packages/identity/src/seeder.ts (L1-66) — idempotencia por clave natural (molde de seeder-entidad)
- packages/identity/src/backup.ts (L20-113) — PATRON del export JSON (NO el tipo Parlamentario[]; nuevo SEED_PATH)
- packages/identity/src/index.ts — barrel; donde exportar los simbolos publicos nuevos (NO el unique symbol)
</analogs>

<interfaces>
<!-- Contratos que el ejecutor debe materializar. Plan 03 y Plan 04 implementan contra estos. -->
matchDeterministaEntidad(
  mention: { rut?: string; nombreNormalizado: string; tipoEntidad: "natural" | "juridica" },
  maestra: EntidadTerceroRow[],
): ResolutionEntidad
//   ResolutionEntidad = { estado:'confirmado'; metodo:'rut'|'nombre'; id: string } | { estado:'no_confirmado'; razon: 'homonimo'|'sin-candidato'|'juridica-sin-rut' }

interface EnlaceEntidadConfirmado {
  readonly entidadTerceroId: string;
  readonly metodo: "determinista" | "humano";
  readonly [ENLACE_ENTIDAD_CONFIRMADO]: true;   // unique symbol PRIVADO, no exportado
}
confirmarEntidad(entidadTerceroId: string, metodo?: "determinista" | "humano"): EnlaceEntidadConfirmado
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EnlaceEntidadConfirmado branded + matchDeterministaEntidad (Δ1/Δ2)</name>
  <read_first>
    - packages/identity/src/enlace-confirmado.ts (L17-71) — unique symbol privado + interface + factory confirmar()
    - packages/identity/src/enlace-confirmado.test-d.ts — molde del test-d + el grep-gate que rechaza casts fuera de tests
    - packages/identity/src/deterministic.ts (L44-139) — tipo Resolution (L44-46) + rama RUT (L98-105) + rama nombre (L108-116) + fail-closed (L135-138); normRut/isRutValido reusables
    - packages/identity/src/deterministic.test.ts — molde de los casos
  </read_first>
  <behavior>
    matchDeterministaEntidad:
    - Test 1: RUT exacto unico en la maestra -> { confirmado, metodo:'rut', id }.
    - Test 2: RUT presente en 2+ filas -> no_confirmado (no inventa).
    - Test 3: RUT presente en 0 filas, natural -> cae a rama nombre.
    - Test 4: natural, nombre-unico-POR-TIPO -> { confirmado, metodo:'nombre' }.
    - Test 5: natural, nombre presente en 2+ del mismo tipo (homonimo) -> no_confirmado razon:'homonimo'.
    - Test 6: natural, sin candidato -> no_confirmado razon:'sin-candidato'.
    - Test 7 (Δ2): juridica con RUT unico -> confirmado metodo:'rut'.
    - Test 8 (Δ2 CRITICO): juridica SIN rut, nombre-unico -> no_confirmado razon:'juridica-sin-rut' (NUNCA confirma por nombre, NUNCA habilita LLM aguas arriba).
    - Test 9 (Δ2): juridica con RUT presente en 2+ -> no_confirmado (no cae a nombre).
    - Test 10: el "nombre unico" de un natural NO colisiona con una juridica del mismo nombre (unicidad calculada POR tipo_entidad).
    enlace-entidad-confirmado: confirmarEntidad mintea el branded; un string crudo asignado a EnlaceEntidadConfirmado es error de compilacion (test-d).
  </behavior>
  <action>
    Crear packages/identity/src/enlace-entidad-confirmado.ts espejando enlace-confirmado.ts: `declare const ENLACE_ENTIDAD_CONFIRMADO: unique symbol;` (NUEVO simbolo privado, NO exportado), interface `EnlaceEntidadConfirmado { readonly entidadTerceroId: string; readonly metodo: "determinista"|"humano"; readonly [ENLACE_ENTIDAD_CONFIRMADO]: true }`, factory `confirmarEntidad(entidadTerceroId, metodo='determinista')`. Mantener el grep-gate: el simbolo nunca se exporta; el cast branded solo dentro de la factory. Crear enlace-entidad-confirmado.test-d.ts espejando el molde.
    Crear packages/identity/src/deterministic-entidad.ts:
    - REUSAR normRut/isRutValido importados de ./deterministic (o del barrel) — Δ NO reimplementar el DV modulo-11 (Don't Hand-Roll, el archivo lo prohibe).
    - Tipo ResolutionEntidad discriminado segun <interfaces>.
    - Δ2 rama juridica PRIMERO: si `mention.tipoEntidad === 'juridica'` -> intentar SOLO la rama RUT (normRut + filtrar maestra, length===1 -> confirmado metodo:'rut'). Cualquier otro caso (sin RUT, RUT no unico) -> no_confirmado razon:'juridica-sin-rut' DIRECTO, sin tocar la rama nombre.
    - Rama natural: RUT-unico O nombre-unico-POR-TIPO (filtrar por tipoEntidad + nombreNormalizado, length===1) confirman; homonimo (2+) -> 'homonimo', sin candidato (0) -> 'sin-candidato'. Omitir el desempate materno/estricto de 0005 (N/A para terceros).
    Crear deterministic-entidad.test.ts con los ≥10 casos del <behavior>.
    Exportar matchDeterministaEntidad, EnlaceEntidadConfirmado, confirmarEntidad desde index.ts (NUNCA el unique symbol).
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test deterministic-entidad enlace-entidad-confirmado</automated>
  </verify>
  <acceptance_criteria>
    - ≥10 tests en deterministic-entidad.test.ts verdes, incluyendo juridica-sin-rut -> no_confirmado y unicidad-por-tipo.
    - test-d verde: string crudo al FK no compila; el unique symbol no esta exportado (grep-gate).
    - normRut/isRutValido se importan, NO se reimplementan.
  </acceptance_criteria>
  <done>matchDeterministaEntidad y EnlaceEntidadConfirmado existen y exportados (sin exportar el simbolo); suite del filtro verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: writer-entidad-supabase + seeder-entidad idempotente</name>
  <read_first>
    - packages/identity/src/writer-supabase.ts (L52-144) — upsert/promote a espejar
    - packages/identity/src/writer-supabase.test.ts — molde (espia el cliente Supabase)
    - packages/identity/src/seeder.ts (L1-66) — idempotencia por clave natural
    - packages/identity/src/seeder.test.ts — molde del test de doble corrida
  </read_first>
  <behavior>
    - Test 1: seeder upsert de una entidad nueva -> 1 insert.
    - Test 2 (ENT-05): segunda corrida del seeder con la misma maestra -> 0 entidades nuevas (idempotente por clave natural; el upsert no duplica).
    - Test 3: el writer nunca auto-confirma — una fila nueva nace estado='no_confirmado' (la promocion es exclusiva de determinista/humano).
  </behavior>
  <action>
    Crear packages/identity/src/writer-entidad-supabase.ts espejando writer-supabase.ts: upsert de entidad_tercero por clave natural (rut donde no nulo; o la clave que el seeder use), promote a confirmado solo desde determinista/humano. Reusar el tipo EntidadTerceroRow. El writer NUNCA fija estado='confirmado' por su cuenta.
    Crear packages/identity/src/seeder-entidad.ts espejando seeder.ts: idempotencia por clave natural -> ENT-05 "2da corrida = 0 nuevos". Crear ambos .test.ts con los casos del <behavior> (espiar el cliente Supabase como en los moldes).
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test writer-entidad-supabase seeder-entidad</automated>
  </verify>
  <acceptance_criteria>
    - El test de doble corrida del seeder afirma 0 entidades nuevas en la 2da pasada (ENT-05).
    - El writer no auto-confirma (fila nueva = no_confirmado).
  </acceptance_criteria>
  <done>writer-entidad-supabase.ts y seeder-entidad.ts existen + tests verdes (incl. idempotencia).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: backup-entidad (custodia JSON determinista) + backfill-entidad-cli LOCAL</name>
  <read_first>
    - packages/identity/src/backup.ts (L20-113) — PATRON export JSON (SEED_PATH, ordenarFilas, withSortedKeys, writer inyectable, r2Enabled default false)
    - packages/identity/src/backup.test.ts — molde del test de determinismo byte-a-byte
    - packages/identity/src/seed-cli.ts y packages/lobby/src/ingest-cli.ts — forma de CLI; loadEnv con fallback a process.env (ver run-agenda-prod-cli.ts)
  </read_first>
  <behavior>
    - Test 1: exportMaestraEntidad sobre una lista produce JSON ordenado por id, claves alfabeticas, determinista (dos corridas = bytes identicos).
    - Test 2: filas en orden distinto producen el MISMO JSON (orden estable).
  </behavior>
  <action>
    Crear packages/identity/src/backup-entidad.ts espejando el PATRON de backup.ts (NO el tipo Parlamentario): `SEED_PATH = "supabase/seeds/entidad_tercero.seed.json"`, ordenar por `id`, claves alfabeticas, determinista byte-a-byte (diff estable en git), writer inyectable, R2 gated por r2Enabled default false. Tipo EntidadTercero. Crear backup-entidad.test.ts con los casos del <behavior>.
    Crear packages/identity/src/backfill-entidad-cli.ts: CLI LOCAL (operador, NO CI), idempotente/reanudable. loadEnv con try/catch + fallback a process.env (patron run-agenda-prod-cli.ts); reusar SUPABASE_URL + service key existentes. Encadena: lee menciones de fuente -> matchDeterministaEntidad -> seeder-entidad (idempotente) -> export custodia via backup-entidad. NO requiere LIVE en este plan (el CLI se ejercita por el operador; aqui basta que compile y su nucleo idempotente este testeado en Task 2).
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test backup-entidad; pnpm --filter @obs/identity build</automated>
  </verify>
  <acceptance_criteria>
    - exportMaestraEntidad es determinista byte-a-byte (test verde); SEED_PATH apunta a entidad_tercero.seed.json.
    - backfill-entidad-cli.ts compila, usa fallback process.env, encadena matcher+seeder+custodia.
  </acceptance_criteria>
  <done>backup-entidad.ts (determinista) + backfill-entidad-cli.ts existen; tests verdes; @obs/identity compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mention de fuente -> matcher | Input no confiable; el matcher es fail-closed |
| FK string crudo -> tipo del reconciliador | Solo EnlaceEntidadConfirmado puede poblar el FK (compile-time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Spoofing/Tampering | confirmar juridica por nombre | mitigate | matchDeterministaEntidad: juridica solo RUT exacto; nombre-sin-RUT -> no_confirmado:'juridica-sin-rut'; test #8 lo asierta |
| T-35-07 | Tampering | string crudo publicado como match en el FK | mitigate | EnlaceEntidadConfirmado branded, simbolo privado no exportado, grep-gate; test-d falla si un string crudo compila |
| T-35-08 | Tampering | DV de RUT mal validado | mitigate | reusar isRutValido/normRut (modulo-11 probado), NO reimplementar |
| T-35-09 | Repudiation/dup | backfill duplica filas en 2da corrida | mitigate | idempotencia por clave natural; test de doble corrida = 0 nuevos (ENT-05) |
| T-35-SC | Tampering | dependencias npm nuevas | accept | sin paquetes nuevos (todo dentro de @obs/identity con deps existentes) |
</threat_model>

<verification>
- `pnpm --filter @obs/identity test` verde (deterministic-entidad ≥10, enlace-entidad-confirmado test-d, seeder doble-corrida, backup determinista).
- `pnpm --filter @obs/identity build` verde.
- grep-gate: el unique symbol ENLACE_ENTIDAD_CONFIRMADO no aparece exportado en index.ts.
</verification>

<success_criteria>
- ENT-02: matchDeterministaEntidad fail-closed con juridica-solo-RUT (≥10 tests, incl. el caso critico jurica-sin-rut).
- ENT-05: seeder idempotente (2da corrida = 0 nuevos) + export JSON deterministico de custodia.
- EnlaceEntidadConfirmado branded propio listo para que Plan 03/04 tipen el FK.
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-02-SUMMARY.md` when done
</output>
