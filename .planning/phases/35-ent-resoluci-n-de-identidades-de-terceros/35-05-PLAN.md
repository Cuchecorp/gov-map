---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 05
type: execute
wave: 1
depends_on: ["35-01"]
files_modified:
  - supabase/migrations/0034_entidad_tercero.sql
  - supabase/tests/0034_entidad_tercero.test.sql
  - packages/identity/src/writer-entidad-supabase.ts
autonomous: false
gap_closure: true
requirements: [ENT-01, ENT-05]
must_haves:
  truths:
    - "entidad_tercero tiene un indice unico TOTAL (no parcial) entidad_tercero_clave_natural sobre (tipo_entidad, nombre_normalizado) que el ON CONFLICT del writer si puede targetear (cierra CR-01)"
    - "Un upsert real ON CONFLICT (tipo_entidad, nombre_normalizado) contra entidad_tercero ya NO lanza SQLSTATE 42P10; una 2da corrida del backfill produce 0 entidades nuevas (ENT-05 ejercitable contra el schema real)"
    - "El docstring de writer-entidad-supabase.ts ya NO afirma falsamente que un indice unico TOTAL respalda la clave natural en 0034/0035; describe el indice que realmente existe en 0034"
    - "El operador confirmo la semantica de deduplicacion (clave natural (tipo_entidad, nombre_normalizado) vs el indice parcial rut) ANTES de aplicar la DDL"
    - "Las migraciones 0034 (con el fix CR-01)/0035/0036 estan aplicadas a PROD por psql --single-transaction, con sus filas en schema_migrations y los 3 pgTAP verdes (0 fallos) + probe anon permission-denied (ENT-01)"
  artifacts:
    - path: "supabase/migrations/0034_entidad_tercero.sql"
      provides: "Indice unico TOTAL entidad_tercero_clave_natural sobre (tipo_entidad, nombre_normalizado), ademas del indice parcial rut existente"
      contains: "entidad_tercero_clave_natural"
    - path: "supabase/tests/0034_entidad_tercero.test.sql"
      provides: "pgTAP: el indice clave_natural existe, es NO parcial, y un doble-insert sobre la misma (tipo,nombre) lanza 23505"
      contains: "entidad_tercero_clave_natural"
    - path: "packages/identity/src/writer-entidad-supabase.ts"
      provides: "Docstring corregido sin la afirmacion falsa del indice TOTAL en 0034/0035"
  key_links:
    - from: "writer-entidad-supabase.ts onConflict 'tipo_entidad,nombre_normalizado'"
      to: "entidad_tercero_clave_natural (indice unico TOTAL en 0034)"
      via: "PostgREST .upsert(onConflict) targetea el indice unico total"
      pattern: "entidad_tercero_clave_natural"
---

<objective>
Cerrar los DOS gaps de Phase 35 que bloquean ENT-01 y ENT-05:

1. **CR-01 (BLOCKER):** `writer-entidad-supabase.ts` hace upsert `ON CONFLICT (tipo_entidad, nombre_normalizado)` sobre `entidad_tercero`, pero 0034 solo crea un indice unico PARCIAL sobre `(rut) WHERE rut IS NOT NULL`. No existe indice TOTAL sobre `(tipo_entidad, nombre_normalizado)` → un upsert real lanza SQLSTATE 42P10 y la idempotencia "2da corrida = 0 nuevos" (ENT-05) nunca es ejercitable contra el schema real (los tests pasan solo porque el cliente Supabase esta mockeado). Se agrega el indice TOTAL (espejo del patron ya correcto de 0035 `vinculo_entidad_clave_natural`), se extiende el pgTAP de 0034, y se corrige el docstring que afirma falsamente que ese indice ya existe.

2. **ENT-01 (apply pendiente):** Las migraciones 0034 (FIJADA por el fix de arriba)/0035/0036 + 3 pgTAP estan escritas pero NO aplicadas a PROD (la ultima en PROD es 0033). ENT-01 exige "aplicadas por psql --db-url con pgTAP verde". Checkpoint de operador bloqueante (`autonomous: false`): el agente NUNCA aplica DDL a PROD. PREREQUISITO: el fix de 0034 DEBE estar en el archivo antes de aplicar, o el seeder/backfill lanza post-apply.

Purpose: ENT-05 idempotente real (no contra mock) + ENT-01 maestra aplicada con pgTAP verde — los dos unicos requisitos BLOQUEADOS de la fase. ENT-02/03/04 ya estan SATISFIED y NO se re-planifican.
Output: 0034.sql + 0034.test.sql con el indice TOTAL; docstring corregido; checkpoint de decision de semantica de dedup; runbook de apply de operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-VERIFICATION.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-01-SUMMARY.md

<interfaces>
<!-- Contratos clave que el ejecutor necesita. Extraidos del codebase. NO explorar. -->

El writer hace upsert con esta clave natural (writer-entidad-supabase.ts L52, L69):
  onConflict default = "tipo_entidad,nombre_normalizado"
  .upsert(rows, { onConflict: this.onConflict, ignoreDuplicates: false })

El patron CORRECTO ya existe en 0035 (vinculo_entidad) — espejarlo en 0034:
  create unique index vinculo_entidad_clave_natural
    on vinculo_entidad (tipo_entidad, mencion_normalizada);   -- TOTAL, sin WHERE

0034 hoy SOLO tiene el indice PARCIAL (entidad_tercero.sql L48-50):
  create unique index entidad_tercero_rut_key
    on entidad_tercero (rut)
    where rut is not null;                                    -- PARCIAL

Docstring drift a corregir (writer-entidad-supabase.ts L14-16): afirma
  "la clave natural (tipo_entidad, nombre_normalizado) esta respaldada por un
   indice unico TOTAL en 0034/0035" — ese indice NO existe en 0034 (es la raiz de CR-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar indice unico TOTAL entidad_tercero_clave_natural a 0034 + pgTAP (CR-01) + corregir docstring del writer</name>
  <files>supabase/migrations/0034_entidad_tercero.sql, supabase/tests/0034_entidad_tercero.test.sql, packages/identity/src/writer-entidad-supabase.ts</files>
  <read_first>
    - supabase/migrations/0034_entidad_tercero.sql (L47-50) — el indice PARCIAL rut existente; el indice TOTAL nuevo va junto a el
    - supabase/migrations/0035_vinculo_entidad.sql (L46-50) — patron FUENTE-DE-VERDAD del indice unico TOTAL (vinculo_entidad_clave_natural, sin WHERE) a espejar
    - supabase/tests/0034_entidad_tercero.test.sql (L8 plan(22), L51-60 el bloque del rut parcial) — pgTAP a extender; el plan(N) debe subir
    - packages/identity/src/writer-entidad-supabase.ts (L14-16, L29-30, L52, L69) — onConflict + docstring drift
  </read_first>
  <action>
    **A. 0034_entidad_tercero.sql — agregar el indice TOTAL (NO tocar el indice parcial rut, NO tocar nada mas):**
    Justo despues del indice parcial `entidad_tercero_rut_key` (L48-50), agregar el indice unico TOTAL que el ON CONFLICT del writer requiere, espejando el patron de 0035 `vinculo_entidad_clave_natural`:
    `create unique index entidad_tercero_clave_natural on entidad_tercero (tipo_entidad, nombre_normalizado);`
    — TOTAL, sin clausula WHERE. Razon (comentar inline, breve): PostgREST `.upsert(onConflict)` NO puede targetear un indice parcial; el writer hace ON CONFLICT (tipo_entidad, nombre_normalizado); sin este indice TOTAL un upsert real lanza SQLSTATE 42P10. Es la misma decision LOCKED que en 0035 (clave natural con indice TOTAL, no parcial).
    Actualizar el comentario de cabecera de 0034 (L18-19) que hoy describe SOLO la clave parcial rut: agregar que la clave natural de identidad por nombre es `(tipo_entidad, nombre_normalizado)` con indice TOTAL, coexistente con el parcial rut.

    **B. 0034_entidad_tercero.test.sql — extender el pgTAP:**
    - Subir `plan(22)` al numero correcto de asserts tras agregar los nuevos.
    - `has_index('public', 'entidad_tercero', 'entidad_tercero_clave_natural', ...)` — el indice existe.
    - Afirmar que el indice es NO parcial. Usar `pg_index.indpred is null` para `entidad_tercero_clave_natural` (un indice parcial tiene `indpred` NOT NULL; el total lo tiene NULL). NO afirmar esto sobre `entidad_tercero_rut_key` — ese SI es parcial y DEBE seguir siendo parcial. Construir el assert con `is( (select indpred is null from pg_index join pg_class on ... where relname='entidad_tercero_clave_natural'), true, '...es indice TOTAL no parcial' )`.
    - Doble-insert que colisiona en la clave natural y lanza 23505: insertar una fila con un `(tipo_entidad, nombre_normalizado)` dado (rut NULL para que el parcial rut NO intervenga), luego `throws_ok` con `'23505'` al insertar otra fila con el MISMO `(tipo_entidad, nombre_normalizado)` (otro rut NULL o distinto). Esto prueba que el indice TOTAL realmente dispara, independiente del parcial rut.
    - Mantener intactos los asserts existentes del indice parcial rut (L51-60).

    **C. writer-entidad-supabase.ts — corregir el docstring drift (L14-16):**
    Reescribir el bloque de Idempotencia: la clave natural `(tipo_entidad, nombre_normalizado)` esta respaldada por el indice unico TOTAL `entidad_tercero_clave_natural` creado en 0034 (no "0034/0035"; el de 0035 es de OTRA tabla, vinculo_entidad). Que el texto describa el indice que REALMENTE existe tras el fix A. NO cambiar codigo ejecutable (onConflict L52 ya es correcto); solo el comentario.
  </action>
  <verify>
    <automated>grep -c "entidad_tercero_clave_natural" supabase/migrations/0034_entidad_tercero.sql; grep -v '^--' supabase/migrations/0034_entidad_tercero.sql | grep -c "create unique index entidad_tercero_clave_natural on entidad_tercero (tipo_entidad, nombre_normalizado)"; grep -c "entidad_tercero_clave_natural" supabase/tests/0034_entidad_tercero.test.sql; grep -c "23505" supabase/tests/0034_entidad_tercero.test.sql; grep -c "indice unico TOTAL" packages/identity/src/writer-entidad-supabase.ts; pnpm --filter @obs/identity test writer-entidad</automated>
  </verify>
  <acceptance_criteria>
    - 0034.sql contiene exactamente la sentencia `create unique index entidad_tercero_clave_natural on entidad_tercero (tipo_entidad, nombre_normalizado);` SIN clausula WHERE (verificable: la linea no contiene `where`).
    - El indice parcial `entidad_tercero_rut_key on entidad_tercero (rut) where rut is not null` sigue presente e intacto.
    - 0034.test.sql: `has_index(...,'entidad_tercero_clave_natural',...)` presente; assert de `indpred is null` para `entidad_tercero_clave_natural`; `throws_ok(...,'23505',...)` sobre doble-insert de la misma `(tipo_entidad, nombre_normalizado)`; el `plan(N)` coincide con el numero real de asserts.
    - writer-entidad-supabase.ts L14-16: el docstring ya NO dice "0034/0035"; nombra `entidad_tercero_clave_natural` en 0034; el onConflict ejecutable no cambio.
  </acceptance_criteria>
  <done>El indice TOTAL existe en 0034, el pgTAP afirma existencia + no-parcial + 23505, el docstring describe el indice real, y `pnpm --filter @obs/identity test writer-entidad` sigue verde.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CHECKPOINT DECISION OPERADOR — confirmar semantica de deduplicacion antes de aplicar la DDL</name>
  <files>supabase/migrations/0034_entidad_tercero.sql</files>
  <action>Checkpoint de decision (autonomous:false). El agente NO aplica DDL. El operador confirma la semantica de unicidad de entidad_tercero (clave natural por nombre vs incluir rut) ANTES del apply de Task 3. Detalle en decision/context/options.</action>
  <read_first>
    - supabase/migrations/0034_entidad_tercero.sql (indice parcial rut + indice TOTAL clave_natural tras Task 1)
    - packages/identity/src/seeder-entidad.ts (L19-20) — el seeder ya documenta la clave natural como "rut (donde no nulo) o (tipo_entidad, nombre_normalizado)"
  </read_first>
  <decision>
    El indice TOTAL `entidad_tercero_clave_natural` sobre `(tipo_entidad, nombre_normalizado)` cambia la semantica de unicidad de `entidad_tercero`: dos filas con el MISMO `(tipo_entidad, nombre_normalizado)` pero RUTs DISTINTOS ahora COLISIONAN (el upsert las funde en una sola fila por nombre normalizado), mientras que el indice parcial rut existente trata RUTs distintos como entidades distintas. Hay que confirmar que esta fusion-por-nombre es la deduplicacion deseada para el backfill ANTES de aplicar la DDL a PROD (cambiar la clave despues de tener datos es caro).
  </decision>
  <context>
    - El writer hace ON CONFLICT (tipo_entidad, nombre_normalizado): el codigo YA asume esta clave; el fix de Task 1 solo crea el indice que la respalda. La semantica "una entidad por (tipo, nombre normalizado)" es lo que el writer ya implementa.
    - Las contrapartes de lobby NO traen RUT (rut NULL) → para ellas SOLO la clave de nombre puede deduplicar; sin el indice TOTAL no hay deduplicacion alguna de contrapartes sin RUT.
    - Riesgo de la clave solo-nombre: dos proveedores juridicos homonimos con RUTs distintos se fundirian en una fila. Mitigante: el matcher (Plan 02, ENT-02) confirma juridicas SOLO por RUT exacto y deja el resto en no_confirmado; la fila fundida queda no_confirmado, no se afirma identidad. Aun asi el operador debe confirmar que prefiere esto a una clave compuesta que incluya rut.
  </context>
  <options>
    <option id="opt-nombre-solo">
      <name>A — Clave natural = (tipo_entidad, nombre_normalizado), indice TOTAL (lo que implementa Task 1)</name>
      <pros>Coincide byte-a-byte con el onConflict que el writer YA usa; deduplica contrapartes de lobby sin RUT; espejo exacto del patron de 0035; cero cambio de codigo ejecutable.</pros>
      <cons>Funde homonimos con RUTs distintos en una fila (mitigado: quedan no_confirmado por el matcher fail-closed).</cons>
    </option>
    <option id="opt-incluye-rut">
      <name>B — Clave natural incluye rut (p.ej. (tipo_entidad, nombre_normalizado, rut) con coalesce o un esquema distinto)</name>
      <pros>No funde homonimos con RUTs distintos.</pros>
      <cons>Requiere CAMBIAR el onConflict del writer (codigo ejecutable + sus tests), rut es NULLABLE (NULLs no colisionan en un indice unico estandar → no deduplica contrapartes de lobby sin RUT, que es el caso mayoritario); rompe el espejo con 0035; mayor alcance fuera de gap-closure.</cons>
    </option>
  </options>
  <verify>
    <human-check>El operador selecciona opt-nombre-solo (default recomendado, coherente con el writer existente y el espejo 0035) o pide opt-incluye-rut (lo cual reabre el writer y excede el gap-closure → se re-planifica).</human-check>
  </verify>
  <acceptance_criteria>
    - El operador confirma explicitamente la semantica de unicidad antes del apply.
    - Si elige opt-nombre-solo: se procede al apply (Task 3) sin cambios adicionales.
    - Si elige opt-incluye-rut: NO se aplica; se detiene y se devuelve al planner para re-planificar el cambio del writer (fuera de alcance de este plan).
  </acceptance_criteria>
  <done>Decision de semantica registrada; si es opt-nombre-solo, habilitado el apply de Task 3.</done>
  <resume-signal>Escribir "clave por nombre confirmada" para aplicar tal cual (opt-nombre-solo), o "incluir rut" para detener y re-planificar.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: CHECKPOINT OPERADOR — aplicar 0034 (FIJADA)/0035/0036 a PROD por psql --single-transaction + pgTAP verde (ENT-01)</name>
  <files>supabase/migrations/0034_entidad_tercero.sql, supabase/migrations/0035_vinculo_entidad.sql, supabase/migrations/0036_entidad_fk.sql, supabase/tests/0034_entidad_tercero.test.sql, supabase/tests/0035_vinculo_entidad.test.sql, supabase/tests/0036_entidad_fk.test.sql</files>
  <read_first>
    - supabase/migrations/0034_entidad_tercero.sql (CON el indice TOTAL de Task 1 — PREREQUISITO; aplicar la version SIN fix lanza 42P10 en el backfill post-apply)
    - supabase/migrations/0035_vinculo_entidad.sql, supabase/migrations/0036_entidad_fk.sql
    - supabase/tests/0034_entidad_tercero.test.sql (extendido en Task 1), 0035_vinculo_entidad.test.sql, 0036_entidad_fk.test.sql
    - .planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-01-PLAN.md (Task 4) — molde del runbook de apply previo
  </read_first>
  <action>
    Checkpoint de operador (needs-human, autonomous:false). El agente NUNCA aplica DDL al remoto. PREREQUISITO DURO: Task 1 (fix CR-01 en 0034) y Task 2 (decision opt-nombre-solo) DEBEN estar resueltos antes; aplicar una 0034 sin el indice TOTAL deja el backfill lanzando 42P10. Detalle exacto en how-to-verify.
  </action>
  <what-built>
    0034 FIJADA (con el indice unico TOTAL entidad_tercero_clave_natural que cierra CR-01) + 0035 + 0036 + sus 3 pgTAP, verificadas localmente. NO aplicadas a PROD por el agente (la ultima migracion en PROD es 0033). Supersede el Task 4 de 35-01 (aquel apuntaba a una 0034 SIN el fix).
  </what-built>
  <how-to-verify>
    LOCKED (CLAUDE.md / convencion de la fase): DDL SOLO por psql --single-transaction + fila en schema_migrations. NUNCA `supabase db push`. Como operador:
    1. Extraer SUPABASE_DB_URL del .env (esquivar BOM U+FEFF si aplica — helper de Phases 9-12).
    2. Aplicar EN ORDEN, cada una en su propia transaccion atomica (0034 PRIMERO, ya con el fix CR-01):
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0034_entidad_tercero.sql
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0035_vinculo_entidad.sql
       psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0036_entidad_fk.sql
    3. Registrar las 3 filas en schema_migrations (0034/0035/0036) — patron de los applies previos.
    4. Correr los 3 pgTAP contra el schema aplicado:
       psql "$SUPABASE_DB_URL" -f supabase/tests/0034_entidad_tercero.test.sql
       psql "$SUPABASE_DB_URL" -f supabase/tests/0035_vinculo_entidad.test.sql
       psql "$SUPABASE_DB_URL" -f supabase/tests/0036_entidad_fk.test.sql
       Confirmar 0/0 fallos en los tres. El pgTAP de 0034 (extendido en Task 1) DEBE pasar el assert del indice TOTAL no-parcial y el doble-insert 23505 — esto prueba en PROD que CR-01 esta cerrado.
    5. Probe deny-by-default: con la anon key, `select * from entidad_tercero limit 1` debe dar permission denied.
    6. (Opcional, evidencia ENT-05 contra schema real) tras el apply, una 2da corrida del backfill LOCAL debe reportar 0 entidades nuevas sin lanzar 42P10.
  </how-to-verify>
  <verify>
    <human-check>El operador confirma: 3 migraciones aplicadas (0034 con el indice TOTAL), 3 filas en schema_migrations, pgTAP 0034/0035/0036 con 0 fallos (incl. el assert clave_natural no-parcial + 23505), probe anon permission-denied.</human-check>
  </verify>
  <acceptance_criteria>
    - 0034 (con entidad_tercero_clave_natural)/0035/0036 aplicadas a PROD; 3 filas nuevas en schema_migrations.
    - pgTAP 0034/0035/0036 con 0 fallos contra PROD; el de 0034 incluye el assert del indice TOTAL no-parcial y el throws 23505.
    - Probe anon SELECT sobre entidad_tercero → permission denied.
  </acceptance_criteria>
  <done>Las 3 migraciones (0034 fijada) en PROD; pgTAP verde con los asserts de CR-01; probe anon permission-denied confirmado.</done>
  <resume-signal>Escribir "pgTAP verde" cuando 0034(fijada)/0035/0036 esten aplicadas + pgTAP sin fallos (incl. clave_natural no-parcial + 23505) + probe anon permission-denied. Si algo falla, describir el error y SQLSTATE.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer (service_role) → entidad_tercero | El upsert masivo del backfill; sin el indice TOTAL correcto, falla cerrado (42P10) en vez de silenciosamente |
| anon/authenticated (PostgREST) → entidad_tercero | PII interna de terceros (Ley 21.719); nunca public-read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Tampering | entidad_tercero upsert sin indice ON CONFLICT (CR-01) | mitigate | Indice unico TOTAL entidad_tercero_clave_natural sobre (tipo_entidad, nombre_normalizado); pgTAP afirma existencia + no-parcial + throws_ok('23505') sobre doble-insert |
| T-35-07 | Tampering | Aplicar DDL con un metodo no atomico o no auditado | mitigate | psql --single-transaction -v ON_ERROR_STOP=1; NUNCA supabase db push; fila en schema_migrations; checkpoint humano bloqueante |
| T-35-08 | Info Disclosure | entidad_tercero legible por anon tras apply | mitigate | Probe anon SELECT → permission denied como parte del checkpoint (RLS deny + revoke heredados de 0034) |
| T-35-SC | Tampering | psql/pgTAP installs | accept | sin instalacion de paquetes nueva en este plan (solo .sql + un docstring TS) |
</threat_model>

<verification>
- 0034.sql contiene el indice TOTAL entidad_tercero_clave_natural (sin WHERE) ademas del parcial rut.
- 0034.test.sql afirma: indice existe, indpred is null (no parcial), throws_ok 23505 sobre doble-insert de la misma (tipo_entidad, nombre_normalizado).
- writer-entidad-supabase.ts L14-16 ya no afirma el indice TOTAL "en 0034/0035" inexistente.
- Checkpoint decision: semantica de dedup confirmada por el operador.
- Checkpoint apply: 0034(fijada)/0035/0036 en PROD, pgTAP verde (incl. asserts CR-01), probe anon permission-denied.
</verification>

<success_criteria>
- ENT-05: la idempotencia "2da corrida = 0 nuevos" es ejercitable contra el schema real (el upsert ON CONFLICT ya no lanza 42P10) — CR-01 cerrado.
- ENT-01: maestra + vinculo + revision aplicadas por psql --db-url con pgTAP verde (incl. el assert del indice TOTAL de 0034) — apply de operador completado.
- Docstring drift de writer-entidad-supabase.ts corregido (sin afirmacion falsa del indice).
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-05-SUMMARY.md` when done
</output>
