---
phase: 35-ent-resoluci-n-de-identidades-de-terceros
plan: 04
type: execute
wave: 2
depends_on: [35-02, 35-03]
files_modified:
  - packages/lobby/src/reconciliar-sujeto.ts
  - packages/lobby/src/reconciliar-sujeto.test.ts
  - packages/dinero/src/reconciliar-contrato.ts
  - packages/dinero/src/reconciliar-contrato.test.ts
  - app/app/admin/revisar-entidades/page.tsx
  - app/app/admin/revisar-entidades/page.test.tsx
autonomous: true
requirements: [ENT-03, ENT-04]
must_haves:
  truths:
    - "reconciliar-sujeto.ts puebla lobby_contraparte.contraparte_id con un match confirmado (antes SIEMPRE null); el sujeto pasivo parlamentario NO cambia"
    - "reconciliar-contrato.ts resuelve el proveedor contra entidad_tercero y puebla contratista.entidad_id (juridica por RUT exacto; natural por RUT o nombre)"
    - "El RUT crudo del proveedor viaja por canal de auditoria interno, NUNCA al LLM ni al jsonb de revision_*"
    - "La pagina admin revisar-entidades esta protegida server-side, lista revision_entidad 'pendiente' via service-role y resuelve via RPC resolver_entidad; ningun dudoso se auto-confirma"
  artifacts:
    - path: "packages/lobby/src/reconciliar-sujeto.ts"
      provides: "contraparteId poblado con EnlaceEntidadConfirmado al confirmar (Δ3)"
    - path: "packages/dinero/src/reconciliar-contrato.ts"
      provides: "contratista.entidad_id poblado resolviendo contra entidad_tercero (Δ3)"
    - path: "app/app/admin/revisar-entidades/page.tsx"
      provides: "Cola de revision protegida server-side (ENT-04)"
  key_links:
    - from: "reconciliar-sujeto.ts"
      to: "matchDeterministaEntidad / correrPipelineEntidad"
      via: "resolver cada contraparte"
      pattern: "matchDeterministaEntidad|correrPipelineEntidad"
    - from: "reconciliar-contrato.ts"
      to: "contratista.entidad_id"
      via: "resolver proveedor contra entidad_tercero"
      pattern: "entidad_id"
    - from: "app/app/admin/revisar-entidades/page.tsx"
      to: "resolver_entidad RPC"
      via: "promocion humana protegida"
      pattern: "resolver_entidad"
---

<objective>
Cablear los reconciliadores que HOY dejan el FK NULL por diseno (Δ3) y montar la cola de revision humana en la UI admin. `reconciliar-sujeto.ts` (lobby) pasa de `contraparteId: null` hardcodeado a poblar el FK con un match confirmado; `reconciliar-contrato.ts` (dinero) puebla la columna nueva `contratista.entidad_id`. La pagina `revisar-entidades` es el equivalente web protegido del revisor-cli.

Purpose: ENT-03 (los reconciliadores escriben el FK resuelto, antes siempre null) y ENT-04 (UI admin protegida; promocion solo humana). Depende de Plan 02 (matcher + branded) y Plan 03 (pipeline + RPC writer). Probado con vitest (lobby/dinero) + RTL (la pagina); usa la columna/FK de Plan 01 pero los tests mockean el cliente Supabase, asi que NO bloquea contra el apply remoto.
Output: 2 reconciliadores modificados + 1 pagina admin nueva (+ sus tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-RESEARCH.md
@.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-PATTERNS.md

<analogs>
- packages/lobby/src/reconciliar-sujeto.ts (L70-76 interface ContraparteParaEscribir; L133-142 contraparteId:null hardcodeado; L189 normalizarNombre; L218-224 sujeto pasivo con EnlaceConfirmado) — SELF, sitio a modificar
- packages/dinero/src/reconciliar-contrato.ts (L234-451 proveedor; L417-444 disciplina data-routing) — SELF, sitio a modificar
- packages/adjudication/src/revisor-cli.ts — la logica list/show/confirm/reject/correct que la pagina espeja
- app/app/parlamentario/[id]/page.tsx, app/app/red/page.tsx — forma RSC (Server Component, lectura via RPC/service)
- app/app/contraparte/[id]/page.tsx — molde de test RTL de pagina server (mock sb.rpc)
</analogs>

<interfaces>
<!-- Del Plan 02 (Wave 1): -->
matchDeterministaEntidad(mention, maestra): ResolutionEntidad   // @obs/identity
confirmarEntidad(entidadTerceroId, metodo?): EnlaceEntidadConfirmado   // @obs/identity
<!-- Del Plan 03 (Wave 2): -->
correrPipelineEntidad(mencion, maestra, provider, writer)   // @obs/adjudication
RevisionEntidadWriter.resolverEntidad({ casoId, estado, revisor, motivo, resolvedAt, promover, vinculo, decision, modeloVersion, tipoEntidad })
<!-- Del Plan 01 (DB): contratista.entidad_id, lobby_contraparte.contraparte_id FK -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconciliar-sujeto.ts — poblar lobby_contraparte.contraparte_id (Δ3)</name>
  <read_first>
    - packages/lobby/src/reconciliar-sujeto.ts (L70-76, L133-142, L189, L218-224) — interface ContraparteParaEscribir, el null hardcodeado, normalizarNombre, el patron del sujeto pasivo con EnlaceConfirmado
    - packages/lobby/src/reconciliar-sujeto.test.ts — molde; anadir casos FK
    - packages/identity/src/deterministic-entidad.ts (de Plan 02) — matchDeterministaEntidad
  </read_first>
  <behavior>
    - Test 1: una contraparte con tipo natural y nombre-unico-por-tipo en la maestra de terceros -> contraparteId poblado (EnlaceEntidadConfirmado) con el id correcto.
    - Test 2: una contraparte juridica SIN RUT -> contraparteId queda null (Δ2: juridica sin RUT nunca confirma); la fila se escribe igual con el nombre crudo.
    - Test 3: una contraparte ambigua/homonima -> contraparteId null (fail-closed); NO se inventa enlace.
    - Test 4 (no-regresion): el sujeto pasivo (parlamentario) sigue reconciliando exactamente como antes (EnlaceConfirmado intacto).
  </behavior>
  <action>
    Modificar packages/lobby/src/reconciliar-sujeto.ts: en contrapartesDe() (L133-142) reemplazar el `contraparteId: null` hardcodeado por: correr matchDeterministaEntidad (o correrPipelineEntidad para naturales homonimas, a discrecion — el determinista basta para el FK, el pipeline LLM solo si se inyecta provider) sobre cada contraparte usando su tipo_entidad y nombre normalizado (normalizarNombre, L189) contra la maestra de terceros; poblar contraparteId con confirmarEntidad(id) SOLO cuando confirme; null en cualquier otro caso (degradacion honesta — LeyLobby no trae RUT, la mayoria de gestores juridicos quedan null por Δ2). Idealmente re-tipar el campo a EnlaceEntidadConfirmado | null (hoy es string|null crudo, L75). El sujeto pasivo parlamentario (L218-224) NO cambia. Anadir los casos FK al .test.ts.
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test reconciliar-sujeto</automated>
  </verify>
  <acceptance_criteria>
    - contraparteId se puebla en el caso confirmado y queda null en juridica-sin-RUT / homonimo.
    - El sujeto pasivo parlamentario no cambia (test de no-regresion verde).
  </acceptance_criteria>
  <done>reconciliar-sujeto.ts pobla contraparte_id al confirmar; tests verdes incl. los casos FK nuevos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconciliar-contrato.ts — poblar contratista.entidad_id (Δ3)</name>
  <read_first>
    - packages/dinero/src/reconciliar-contrato.ts (L234-451 proveedor; L417-444 data-routing: RUT crudo por canal de auditoria, nunca al LLM ni al jsonb de revision_*)
    - packages/dinero/src/reconciliar-contrato.test.ts — molde; anadir casos entidad_id
  </read_first>
  <behavior>
    - Test 1: proveedor juridico con RUT exacto en la maestra de terceros -> contratista.entidad_id poblado (RUT exacto, Δ2 juridica).
    - Test 2: proveedor juridico sin match RUT -> entidad_id null (nunca por nombre).
    - Test 3: proveedor natural con RUT o nombre-unico-por-tipo -> entidad_id poblado.
    - Test 4 (data-routing): el RUT crudo del proveedor NUNCA aparece en un payload destinado al LLM ni en el jsonb de revision_* (assertNoRutInLlmInput intacto; canal de auditoria preservado).
    - Test 5 (no-regresion): la reconciliacion del proveedor contra parlamentario (fiscalizacion, L268-391) sigue igual.
  </behavior>
  <action>
    Modificar packages/dinero/src/reconciliar-contrato.ts: tras (o junto a) la reconciliacion existente del proveedor contra parlamentario (que NO cambia), resolver el proveedor contra entidad_tercero y poblar la columna nueva contratista.entidad_id (de 0036): juridica -> matchDeterministaEntidad por RUT exacto; natural -> RUT o nombre. Usar confirmarEntidad para el FK. Preservar la disciplina data-routing existente (L417-444): el RUT crudo viaja por canal de auditoria interno, NUNCA al LLM ni al jsonb de revision_*. Anadir los casos entidad_id al .test.ts.
  </action>
  <verify>
    <automated>pnpm --filter @obs/dinero test reconciliar-contrato</automated>
  </verify>
  <acceptance_criteria>
    - entidad_id se puebla para juridica-por-RUT-exacto y natural-por-RUT/nombre; null sin match.
    - El RUT crudo nunca cruza al LLM/jsonb (test de data-routing verde).
    - La reconciliacion contra parlamentario no cambia (no-regresion verde).
  </acceptance_criteria>
  <done>reconciliar-contrato.ts pobla contratista.entidad_id; tests verdes incl. data-routing y no-regresion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: app/app/admin/revisar-entidades — cola de revision protegida server-side (ENT-04)</name>
  <read_first>
    - packages/adjudication/src/revisor-cli.ts (L1-60) — contrato list/show/confirm/reject/correct
    - app/app/red/page.tsx — forma de Server Component protegido por gate como primera sentencia
    - app/app/contraparte/[id]/page.tsx — molde de test RTL de pagina server (mock sb.rpc)
  </read_first>
  <behavior>
    - Test 1: la pagina lista solo revision_entidad estado='pendiente'.
    - Test 2: sin autorizacion server-side -> la pagina no monta la cola (notFound o equivalente protegido); NO es superficie publica.
    - Test 3: la accion 'confirmar' invoca el RPC resolver_entidad con p_promover=true (promocion humana); ningun dudoso se auto-confirma.
  </behavior>
  <action>
    Crear app/app/admin/revisar-entidades/page.tsx como Server Component PROTEGIDO (no superficie publica; sin UI-SPEC formal, discrecion Claude):
    - PRIMERA sentencia: chequeo de autorizacion server-side (gate; si no autorizado -> notFound(), espejo del patron de /red). La maestra de terceros es PII interna (deny-by-default) — esta cola jamas debe ser publica.
    - Listar revision_entidad estado='pendiente' via service-role (la tabla es deny-by-default a anon).
    - Acciones list/show/confirm/reject/correct que resuelven via RPC resolver_entidad (Server Action o ruta interna), espejando los subcomandos del revisor-cli. La promocion humana mintea confirmarEntidad(..., 'humano'). NINGUN dudoso se auto-confirma (gate humano LOCKED).
    Crear page.test.tsx (RTL, mock del cliente Supabase/sb.rpc, espejo de contraparte/[id]) con los casos del <behavior>.
  </action>
  <verify>
    <automated>pnpm --filter app test revisar-entidades</automated>
  </verify>
  <acceptance_criteria>
    - La pagina lista solo 'pendiente', esta protegida server-side, y 'confirmar' va por resolver_entidad (humano).
    - No expone la maestra de terceros publicamente.
  </acceptance_criteria>
  <done>page.tsx + page.test.tsx existen; RTL verde; cola protegida; promocion solo humana.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciliador -> FK confirmado | Solo un match confirmado puebla el FK (branded type) |
| proveedor RUT crudo -> LLM/jsonb | El RUT nunca cruza; canal de auditoria interno |
| usuario web -> cola revision_entidad | Pagina protegida server-side; nunca publica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14 | Spoofing/Tampering | FK poblado con match equivocado | mitigate | Solo EnlaceEntidadConfirmado puebla el FK; juridica solo por RUT exacto (Δ2); null en homonimo (fail-closed) |
| T-35-15 | Info Disclosure | RUT del proveedor al LLM/jsonb | mitigate | preservar data-routing existente (L417-444); RUT por canal de auditoria; test lo asierta |
| T-35-16 | Info Disclosure | cola de revision (PII terceros) publica | mitigate | pagina protegida server-side, gate como primera sentencia (espejo /red); service-role solo server |
| T-35-17 | Elevation of Privilege | dudoso auto-confirmado en la UI | mitigate | promocion solo via RPC resolver_entidad (humano); ningun auto-confirm |
| T-35-SC | Tampering | dependencias npm nuevas | accept | sin paquetes nuevos (reusa @obs/identity, @obs/adjudication, app stack existente) |
</threat_model>

<verification>
- `pnpm --filter @obs/lobby test reconciliar-sujeto`, `pnpm --filter @obs/dinero test reconciliar-contrato`, `pnpm --filter app test revisar-entidades` verdes.
- contraparte_id y contratista.entidad_id se pueblan al confirmar; null en juridica-sin-RUT/homonimo.
- El RUT crudo nunca cruza al LLM/jsonb (test de data-routing).
- La cola admin esta protegida server-side y promueve solo por humano.
</verification>

<success_criteria>
- ENT-03: reconciliar-sujeto.ts puebla lobby_contraparte.contraparte_id; reconciliar-contrato.ts puebla contratista.entidad_id (Δ3); el sujeto pasivo y la fiscalizacion contra parlamentario intactos.
- ENT-04: UI admin revisar-entidades protegida; dudosos solo se promueven por humano via RPC resolver_entidad.
</success_criteria>

<output>
Create `.planning/phases/35-ent-resoluci-n-de-identidades-de-terceros/35-04-SUMMARY.md` when done
</output>
