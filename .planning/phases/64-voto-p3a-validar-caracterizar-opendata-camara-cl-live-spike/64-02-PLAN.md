---
phase: 64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - packages/votos/src/spike-votacion-detalle.live.test.ts
  - packages/votos/src/spike-votacion-detalle.live.test.ts.md
autonomous: true
requirements: [VOTO-05]

user_setup:
  - service: cloudflare-r2
    why: "STAGE-1 del ingesta dos-etapas: persistir el crudo LIVE content-addressed"
    env_vars:
      - name: R2_ACCESS_KEY_ID
        source: ".env (token R2 Read&Write ya emitido — v3 memory 2026-06-20)"
      - name: R2_SECRET_ACCESS_KEY
        source: ".env"
      - name: R2_ENDPOINT_URL
        source: ".env (https://<accountid>.r2.cloudflarestorage.com)"
      - name: R2_BUCKET
        source: ".env"

must_haves:
  truths:
    - "Una corrida LIVE gated (VOTOS_LIVE=1) fetchea getVotacion_Detalle vía CamaraConnector respetando el rate-limit 2-3s LOCKED (no override)"
    - "El crudo LIVE se persiste PRIMERO a R2 vía R2Store.putImmutable (content-addressed, If-None-Match:*, 412=idempotente) — STAGE 1 antes de parsear"
    - "El probe escanea suficientes votaciones para OBSERVAR un <Pareos> no vacío y (si existe) una votación con TotalDispensados>0, y registra lo observado"
    - "La suma voto-a-voto == totales del header en la respuesta LIVE cruda (no solo en el fixture); un mismatch se registra ruidoso"
    - "El fallback a getVotaciones_Boletin queda documentado: si el detalle falla a escala, se registra la decisión de fallback + re-plan del bloque VOTO (SC#4)"
  artifacts:
    - path: "packages/votos/src/spike-votacion-detalle.live.test.ts"
      provides: "probe LIVE gated que persiste crudo a R2, hunt Pareo/Dispensado, cross-check LIVE"
      contains: "VOTOS_LIVE"
    - path: "packages/votos/src/spike-votacion-detalle.live.test.ts.md"
      provides: "hallazgos del probe: códigos observados, Pareo/Dispensado, r2Path del crudo, decisión de fallback (SC#4)"
      contains: "fallback"
  key_links:
    - from: "spike-votacion-detalle.live.test.ts"
      to: "R2Store.putImmutable"
      via: "STAGE 1 persist del crudo content-addressed"
      pattern: "putImmutable"
    - from: "spike-votacion-detalle.live.test.ts"
      to: "CamaraConnector.fetchVotacionDetalle"
      via: "fetch LIVE con política LOCKED (rate-limit 2-3s)"
      pattern: "fetchVotacionDetalle"
---

<objective>
Cerrar la validación LIVE del SPIKE: probar contra la fuente VIVA que la semántica fijada offline (Plan 01) cuadra con el endpoint real, persistir el crudo LIVE a R2 como fixture autoritativo (STAGE 1 del ingesta dos-etapas, LOCKED), y resolver por observación las dos preguntas abiertas — Pareo (bloque `<Pareos>`) y Dispensado (`TotalDispensados` vs "No Vota") — sin fabricar semántica.

Este plan es un `*.live.test.ts` gated por `VOTOS_LIVE=1` (SKIP por defecto → CI no quema el WAF), que MIRRORrea el patrón existente `run-camara-votos.live.test.ts`. Reusa VERBATIM: `CamaraConnector.fetchVotacionDetalle` (política LOCKED: SSRF allowlist → robots → rate-limit 2-3s → fetcher), `R2Store.putImmutable` + `sha256Hex` (content-addressed, idempotente), `buildCamaraConnector()`. NO se crea conector ni se escribe a Supabase (eso es Phase 66).

Purpose: SC#1 (crudo LIVE en R2), la mitad LIVE de SC#2 (Pareo/Dispensado confirmados contra la fuente), la validación LIVE de SC#3 (cross-check sobre respuesta real), y SC#4 (fallback honesto documentado).
Output: el test gated + un `.md` con los hallazgos (códigos observados, r2Path del crudo, veredicto de escala, decisión de fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike/64-RESEARCH.md
@.planning/phases/64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike/64-01-SUMMARY.md
@packages/votos/src/run-camara-votos.live.test.ts

<interfaces>
<!-- Contratos existentes que el ejecutor usa directamente — NO explorar el codebase. -->

CamaraConnector (packages/tramitacion/src/connector-camara.ts):
  async fetchVotacionDetalle(votacionId: string): Promise<string>   // getVotacion_Detalle?prmVotacionId=<id>, política LOCKED
  async fetchVotacionesBoletin(boletinBase: string): Promise<string> // getVotaciones_Boletin?prmBoletin=<b> (fallback SC#4)

buildCamaraConnector() (packages/votos/src/run-camara-votos.ts):
  export function buildCamaraConnector(): CamaraConnector  // ensambla Fetcher/HostRateLimiter(2-3s)/RobotsGuard con allowlist {} (cubre camara.cl). NO editar allowlist.

R2Store (@obs/ingest, packages/ingest/src/r2-store.ts):
  new R2Store({ accessKeyId, secretAccessKey, endpoint, bucket })   // config desde process.env.R2_*
  async putImmutable(source, resource, date, sha, ext, body: Uint8Array): Promise<{ r2Path: string; existed: boolean }>
     // key = `${source}/${resource}/${date}/${sha}.${ext}`; If-None-Match:* → 412=existed=true=idempotente OK
  sha256Hex(body: Uint8Array): Promise<string>   // hex SHA-256 (Web Crypto)

parseCamaraVotoDetalle(xml): CamaraVotoDetalle[]  // corregido en Plan 01 (pareo desde <Pareos>, abstención code 2)

Env R2 (patrón loadEnv en packages/votos/src/run-votos-masivo-cli.ts): R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_ENDPOINT_URL, R2_BUCKET.
Gate LIVE: `const LIVE = process.env.VOTOS_LIVE === "1"; (LIVE ? describe : describe.skip)(...)`.
vitest excluye `**/*.live.test.ts` del glob por defecto (packages/votos/vitest.config.ts), testTimeout 120_000.

IDs LIVE confirmados (RESEARCH, 2026-07-13, HTTP 200): prmVotacionId ∈ {89178,89179,89180,88813,89000}; boletines {14309,18296}.
Header del detalle: TotalAfirmativos/TotalNegativos/TotalAbstenciones/TotalDispensados; roster <Votos><Voto>; pareos <Pareos><Pareo>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe LIVE gated — persist crudo a R2 + hunt Pareo/Dispensado + cross-check LIVE</name>
  <files>packages/votos/src/spike-votacion-detalle.live.test.ts</files>
  <read_first>
    - packages/votos/src/run-camara-votos.live.test.ts (patrón gated VOTOS_LIVE, loadEnv, delay 2-3s LOCKED)
    - packages/votos/src/run-votos-masivo-cli.ts (loadEnv BOM-safe; buildCamaraConnector; R2_* env)
    - packages/ingest/src/r2-store.ts (R2Store.putImmutable, sha256Hex — firma exacta)
    - packages/tramitacion/src/connector-camara.ts (fetchVotacionDetalle / fetchVotacionesBoletin)
    - .planning/phases/.../64-RESEARCH.md (Code Examples: URL LIVE, persist R2; Open Questions 1-3; Pattern 1)
    - .planning/phases/.../64-01-SUMMARY.md (semántica ya fijada: pareo desde <Pareos>, code 2)
  </read_first>
  <action>
    Crea `packages/votos/src/spike-votacion-detalle.live.test.ts`, gated por `VOTOS_LIVE=1` (`describe.skip` si no; MIRRORrea `run-camara-votos.live.test.ts`). Dentro del describe LIVE:
    (1) Carga `.env` con el `loadEnv` BOM-safe existente y construye `R2Store` desde `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_ENDPOINT_URL`/`R2_BUCKET`. Si faltan → falla ruidoso con mensaje claro (no persist silencioso).
    (2) Con `buildCamaraConnector()`, itera los prmVotacionId confirmados LIVE (89178, 89179, 89180, 88813, 89000) SERIALMENTE — el `HostRateLimiter` interno impone 2-3s; NO lo override, NO paralelices. Para cada uno: `fetchVotacionDetalle(id)` → crudo XML.
    (3) STAGE 1 (dos-etapas LOCKED): por cada crudo, `body = new TextEncoder().encode(xml)`, `sha = await sha256Hex(body)`, `date = new Date().toISOString().slice(0,10)`, y `r2.putImmutable("camara-opendata", "getVotacion_Detalle", date, sha, "xml", body)`. Assert que `r2Path` es no-vacío (412 existed=true es éxito idempotente, también válido). Loguea cada `r2Path`.
    (4) STAGE 2: `parseCamaraVotoDetalle(xml)` sobre el crudo LIVE; cross-check por bucket semántico contra los totales del header del MISMO XML (reusa el helper de cross-check de Plan 01). Assert Σ(si)==TotalAfirmativos, etc.; un mismatch hace expect-fail RUIDOSO.
    (5) HUNT: acumula sobre todas las votaciones observadas — (a) si alguna trae `<Pareos>` no vacío, registra los DIPID pareados y confirma que el parser (Plan 01) los emite como "pareo"; (b) si alguna trae `TotalDispensados > 0`, inspecciona las filas de esos diputados para determinar si "Dispensado" es una `<Opcion>` distinta o se pliega a "No Vota" (código 4) — registra el hallazgo. Si en la ventana NO aparece un Pareo o un Dispensado>0, NO falles el test: registra "no observado en la muestra" (fail-closed documentado, nunca fabricado).
    Usa `console.log("[spike-detalle] …")` para todo hallazgo (r2Path, códigos, Pareo/Dispensado). NO escribas a Supabase. NO inlinees implementaciones en el plan.
  </action>
  <verify>
    <automated>VOTOS_LIVE=1 pnpm --filter @obs/votos exec vitest run src/spike-votacion-detalle.live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - El archivo es `describe.skip` sin `VOTOS_LIVE=1` (verificable: `pnpm --filter @obs/votos test` NO lo colecta — está excluido del glob y skippeado).
    - Con `VOTOS_LIVE=1`: al menos una `putImmutable` retorna un `r2Path` no-vacío (crudo LIVE persistido content-addressed) — SC#1.
    - El cross-check LIVE (Σ roster == header) pasa para las votaciones probadas; un mismatch haría expect-fail (no silencioso) — SC#3 LIVE.
    - El test recorre las votaciones SERIALMENTE respetando el `HostRateLimiter` (no hay `Promise.all` sobre los fetch; el delay 2-3s no se override).
    - Los hallazgos de Pareo y Dispensado (observado / no-observado) quedan en el output del test.
  </acceptance_criteria>
  <done>Probe LIVE gated que persiste el crudo a R2 (SC#1), valida el cross-check contra la fuente viva (SC#3), y caza Pareo/Dispensado registrando lo observado sin fabricar códigos.</done>
</task>

<task type="auto">
  <name>Task 2: Documento de hallazgos del SPIKE + decisión de fallback (SC#4)</name>
  <files>packages/votos/src/spike-votacion-detalle.live.test.ts.md</files>
  <read_first>
    - .planning/phases/.../64-RESEARCH.md (Success criterion #4; getVotaciones_Boletin como fallback; Open Questions)
    - .planning/phases/.../64-01-SUMMARY.md
    - packages/votos/src/spike-votacion-detalle.live.test.ts (Task 1 — para citar r2Path/códigos observados)
  </read_first>
  <action>
    Crea `packages/votos/src/spike-votacion-detalle.live.test.ts.md` (documento junto al test) que registre el veredicto del SPIKE: (a) estado del endpoint a escala (UP / degradado) según lo observado en Task 1 y en RESEARCH (probed 200, 5 votaciones, 2-3s); (b) mapeo confirmado LIVE `0→no, 1→si, 2→abstencion, 4→ausente` + pareo-desde-`<Pareos>` + el hallazgo de Dispensado (observado / no-observado, bucket); (c) el/los `r2Path` del crudo persistido; (d) **decisión de fallback SC#4**: dado que `getVotaciones_Boletin` está UP y probado, el detalle voto-a-voto es la vía primaria para Phase 66; SI en el backfill de Phase 66 el detalle degradara a escala, el fallback honesto es agregados `getVotaciones_Boletin` (se pierde el voto individual, se conservan los `Total*`) + un re-plan del bloque VOTO — registra esto explícitamente como contingencia, no como plan actual. Cierra con las preguntas abiertas que queden (ej. si Pareo/Dispensado no se observaron en la ventana, anótalo para una re-probe en Phase 66). NO uses lenguaje causal/insinuante; es un hallazgo técnico factual.
  </action>
  <verify>
    <automated>test -f packages/votos/src/spike-votacion-detalle.live.test.ts.md && grep -qi "fallback" packages/votos/src/spike-votacion-detalle.live.test.ts.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El `.md` existe y contiene: el veredicto de escala, el mapeo confirmado LIVE (incl. pareo/dispensado), el/los r2Path del crudo, y la decisión de fallback a `getVotaciones_Boletin` (SC#4) marcada como contingencia con re-plan.
    - Las preguntas abiertas no resueltas en la ventana LIVE quedan registradas para Phase 66 (nunca fabricadas).
    - `grep -qi "fallback"` y `grep -qi "getVotaciones_Boletin"` devuelven match.
  </acceptance_criteria>
  <done>El fallback honesto (SC#4) y los hallazgos LIVE quedan documentados junto al probe; el bloque VOTO (Phase 66) tiene un veredicto binario de arranque.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador/CI → WS gubernamental (opendata.camara.cl) | request saliente al WS público; ráfagas → WAF bloquea |
| WS → parser/atribución de voto | integridad del voto: un código mal mapeado es afirmación falsa (difamación) |
| crudo LIVE → R2 (SigV4) | credenciales R2 firman el PUT; no deben filtrarse en logs/errores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | DoS (hacia el WS gob) / Denial | probe LIVE contra `opendata.camara.cl` | mitigate | Rate-limit 2-3s LOCKED vía `HostRateLimiter` (no override); fetch serial (sin `Promise.all`); UA identificatorio (`IDENTIFIED_UA`/`Fetcher`); gated por `VOTOS_LIVE=1` (CI no lo corre). |
| T-64-05 | Info disclosure | credenciales R2 en logs/errores | mitigate | `R2Store` ya omite auth headers en el mensaje de error (T-01-06); el test loguea solo `r2Path`/status, nunca la config R2. |
| T-64-06 | Spoofing/SSRF | URL de fetch hacia host interno | mitigate | `CamaraConnector.fetch` ejecuta `assertAllowedUrl` (allowlist gob) ANTES del fetch; host desde `new URL()`; no se edita el allowlist. |
| T-64-07 | Tampering (Integrity) | mapeo de voto en la respuesta LIVE | mitigate | Cross-check LIVE Σ roster == header ruidoso; Pareo/Dispensado se registran por observación, nunca se fabrica un código. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Este SPIKE instala CERO paquetes nuevos (RESEARCH: Package Legitimacy Audit N/A); todas las deps ya vendorizadas en el lockfile. Sin checkpoint de instalación necesario. |

*V6 Cryptography: la firma SigV4 del PUT a R2 usa `aws4fetch` en `R2Store` — NUNCA hand-roll (T-01-05); sha256 vía Web Crypto.*
</threat_model>

<verification>
- Sin `VOTOS_LIVE=1`: `pnpm --filter @obs/votos test` pasa y NO colecta el `*.live.test.ts` (excluido del glob).
- Con `VOTOS_LIVE=1` (corrida deliberada del operador/ejecutor): el probe fetchea serial, persiste ≥1 crudo a R2 (r2Path logueado), y el cross-check LIVE pasa.
- El `.md` de hallazgos existe con la decisión de fallback (SC#4).
- `git grep -n "putImmutable" packages/votos/src/spike-votacion-detalle.live.test.ts` → ≥1 match (STAGE 1 presente).
- `git grep -n "Promise.all" packages/votos/src/spike-votacion-detalle.live.test.ts` → 0 matches sobre los fetch (serial, rate-limit respetado).
</verification>

<success_criteria>
- SC#1: existe ≥1 respuesta LIVE cruda de `getVotacion_Detalle` en R2, content-addressed (dos namespaces caracterizados: el detalle tempuri + el boletín agregado disponible como fallback).
- SC#2 (mitad LIVE): Pareo (bloque `<Pareos>`) y Dispensado (`TotalDispensados`) resueltos por observación contra la fuente — o registrados como no-observados en la ventana (fail-closed, nunca fabricados).
- SC#3 (LIVE): el cross-check de totales cuadra sobre la respuesta viva; mismatch ruidoso.
- SC#4: fallback honesto a `getVotaciones_Boletin` documentado + re-plan del bloque VOTO como contingencia.
</success_criteria>

<output>
Create `.planning/phases/64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike/64-02-SUMMARY.md` when done
</output>
