---
phase: 64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/src/parse-camara-votacion.ts
  - packages/tramitacion/src/parse-camara-votacion.test.ts
  - packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml
autonomous: true
requirements: [VOTO-05]

must_haves:
  truths:
    - "opcionDeVoto mapea codigo 2 → abstencion por CÓDIGO (live-confirmado 2026-07-13), no solo por #text"
    - "El pareo se deriva del bloque <Pareos> (Diputado1/Diputado2 por DIPID), NUNCA de un Opcion Codigo=3 en el roster (que no existe live)"
    - "Un diputado pareado, que en <Votos> figura como No Vota (codigo 4), se emite como opcion 'pareo', no 'ausente'"
    - "Un test suma el roster por Seleccion y assert Σ == TotalAfirmativos/TotalNegativos/TotalAbstenciones; un fixture corrupto hace THROW ruidoso"
    - "El código de opción sigue fail-closed: una opción ilegible devuelve null y se omite — nunca se fabrica un sí/no"
  artifacts:
    - path: "packages/tramitacion/src/parse-camara-votacion.ts"
      provides: "opcionDeVoto con codigo 2→abstencion + parseCamaraVotoDetalle que resuelve pareo desde <Pareos>"
      contains: "Pareos"
    - path: "packages/tramitacion/src/parse-camara-votacion.test.ts"
      provides: "cross-check de totales ruidoso + assert de pareo desde <Pareos> + assert code-2 abstención"
      contains: "TotalAfirmativos"
    - path: "packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml"
      provides: "fixture LIVE-derivado autoritativo con <Pareos> y (idealmente) una abstención code-2"
      contains: "Pareos"
  key_links:
    - from: "parseCamaraVotoDetalle"
      to: "bloque <Pareos>"
      via: "set de DIPID pareados que sobreescribe la opción del roster"
      pattern: "Pareos"
    - from: "parse-camara-votacion.test.ts"
      to: "TotalAfirmativos/TotalNegativos/TotalAbstenciones"
      via: "suma del roster por Seleccion asertada contra los totales del header"
      pattern: "reduce|filter.*opcion"
---

<objective>
Fijar por test la semántica del voto de `getVotacion_Detalle` (ns tempuri) contra el fixture LIVE-derivado autoritativo, corrigiendo el mapeo con lo confirmado LIVE el 2026-07-13 — SIN tocar la fuente (trabajo 100% offline, feedback instantáneo).

Dos correcciones duras que salen de leer el fixture LIVE real (`camara-votacion-detalle-real.xml`):
1. **Abstención por código**: `codigo === "2"` es Abstención (confirmado LIVE, 5 votaciones, cross-check exacto). Hoy `opcionDeVoto` solo lo caza por `#text`; se añade la rama por código.
2. **Pareo NO es un código de roster**: el fixture LIVE prueba que el pareo vive en un bloque top-level `<Pareos><Pareo><Diputado1><DIPID>…<Diputado2><DIPID>`, y que esos diputados figuran en `<Votos>` como `No Vota` (codigo 4). El supuesto A1b ("Pareo = Codigo 3") es FALSO en la forma real: no existe tal código en el roster. El parser debe derivar `pareo` cruzando los DIPID del bloque `<Pareos>`, no esperando un código 3 inexistente. Hoy un diputado pareado se emite como `ausente` (bug de atribución: pareo ≠ ausente, VOTO-04).

Purpose: de-riesgar TODO el bloque VOTO (Phases 65-68) convirtiendo el supuesto de mapeo en hecho verificado por test determinista. Un voto mal mapeado es difamatorio (riesgo #1 del milestone).
Output: `opcionDeVoto`/`parseCamaraVotoDetalle` con abstención-por-código y pareo-desde-`<Pareos>`; un test de cross-check de totales que falla RUIDOSO; fixture confirmado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike/64-RESEARCH.md

<interfaces>
<!-- Contratos existentes que el ejecutor usa directamente — NO explorar el codebase. -->

Seleccion (packages/tramitacion/src/model.ts):
  type Seleccion = "si" | "no" | "abstencion" | "pareo" | "ausente";

CamaraVotoDetalle (packages/tramitacion/src/parse-camara-votacion.ts):
  interface CamaraVotoDetalle { diputadoId: string; opcion: Seleccion; nombreCrudo: string; }

opcionDeVoto(voto): Seleccion | null  — privada; mapeo bajo test. Estado actual:
  - codigo "1" | /a favor|afirmativ/ → "si"
  - codigo "0" | /en contra|negativ/ → "no"
  - /abstenci/ (SOLO texto) → "abstencion"     ← AÑADIR codigo === "2"
  - /pareo/  (SOLO texto)   → "pareo"           ← el roster REAL nunca trae este texto/código
  - codigo "4" | /no vota|dispensad|inasist|ausen/ → "ausente"
  - else → null (fail-closed)

parseCamaraVotoDetalle(detalleXml): CamaraVotoDetalle[]  — itera <Votacion>/<Votos>/<Voto>, cruza por DIPID.
  Hoy NO lee el bloque hermano <Pareos>.

Forma LIVE real del XML (fixture camara-votacion-detalle-real.xml, votación 88813, boletín 14309-04):
  <Votacion xmlns="http://tempuri.org/">
    <TotalAfirmativos>58</TotalAfirmativos> <TotalNegativos>81</TotalNegativos>
    <TotalAbstenciones>0</TotalAbstenciones> <TotalDispensados>0</TotalDispensados>
    <Votos><Voto><Diputado><DIPID>803</DIPID>…</Diputado><Opcion Codigo="4">No Vota</Opcion></Voto>…</Votos>
    <Pareos><Pareo><Diputado1><DIPID>1240</DIPID>…</Diputado1><Diputado2><DIPID>1082</DIPID>…</Diputado2></Pareo>…</Pareos>
  </Votacion>
  → DIPID 1240, 1082, 1259, 1142, 1039, 1131, 1015, 1217, 1107, 1219 están en <Pareos> Y figuran como "No Vota" en <Votos>.

XMLParser (fast-xml-parser) ya instanciado en el módulo: { ignoreAttributes:false, parseTagValue:false }.
Atributos → prefijo "@_" (ej. Opcion["@_Codigo"]); texto → "#text". Helpers `txt`/`asArray` ya existen en el módulo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir abstención-por-código (codigo 2) a opcionDeVoto + test</name>
  <files>packages/tramitacion/src/parse-camara-votacion.ts, packages/tramitacion/src/parse-camara-votacion.test.ts</files>
  <read_first>
    - packages/tramitacion/src/parse-camara-votacion.ts (opcionDeVoto, líneas ~258-286)
    - packages/tramitacion/src/parse-camara-votacion.test.ts (describe "ns tempuri REAL")
    - packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml (forma real, header TotalAbstenciones)
    - .planning/phases/.../64-RESEARCH.md (Pattern 2, Assumption A1a RESUELTO)
  </read_first>
  <behavior>
    - opcionDeVoto con `Opcion Codigo="2"` (cualquier `#text`, incluso vacío) → "abstencion" por CÓDIGO, sin depender del texto.
    - La rama de texto `/abstenci/` se conserva como fallback (no se elimina — otra fuente/legislatura podría traer solo texto).
    - Regresión: codigo "1"→si, "0"→no, "4"→ausente siguen intactos; opción ilegible → null.
  </behavior>
  <action>
    En `opcionDeVoto` (rama shape real `<Opcion Codigo>`), añade `codigo === "2"` a la condición de abstención — ANTES o dentro de la línea `if (/abstenci/i.test(texto)) return "abstencion";` — dejando el fallback de texto. Refleja en el comentario que el código 2 pasó de A1 sintético a CONFIRMADO LIVE 2026-07-13 (cross-check de totales, 5 votaciones). NO cambies las ramas si/no/ausente ni el `return null` final fail-closed. Añade en el describe "ns tempuri REAL" un test que asserte que, para un `<Opcion Codigo="2">` (con o sin texto), la opción es "abstencion", y otro que reafirme code-1→si / code-0→no / code-4→ausente contra el fixture real. No inlinees código en el plan; escribe el test contra `camara-votacion-detalle-real.xml` con el helper `leer` ya existente.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test -- parse-camara-votacion</automated>
  </verify>
  <acceptance_criteria>
    - `opcionDeVoto` retorna "abstencion" cuando `codigo === "2"` independientemente del `#text` (asertado en test).
    - El test suite de `parse-camara-votacion.test.ts` pasa en verde (incluye los describes existentes v1/real/roster sin regresión).
    - `git diff` muestra que las ramas "si"/"no"/"ausente" y el `return null` final NO cambiaron.
  </acceptance_criteria>
  <done>codigo 2 → abstencion fijado por código y verificado por test contra el fixture LIVE-derivado; sin regresión de las otras opciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivar pareo del bloque Pareos (no de código 3) + fixture con Pareos</name>
  <files>packages/tramitacion/src/parse-camara-votacion.ts, packages/tramitacion/src/parse-camara-votacion.test.ts, packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml</files>
  <read_first>
    - packages/tramitacion/src/parse-camara-votacion.ts (parseCamaraVotoDetalle líneas ~207-246; helpers txt/asArray)
    - packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml (bloque Pareos al final: 5 Pareo, DIPID 1240/1082/1259/1142/1039/1131/1015/1217/1107/1219)
    - packages/tramitacion/src/parse-camara-votacion.test.ts (describe "ns tempuri REAL" y "roster")
    - .planning/phases/.../64-RESEARCH.md (Open Question 1: Pareo; Assumption A1b; Pitfall 2)
  </read_first>
  <behavior>
    - parseCamaraVotoDetalle, sobre el fixture real, emite "pareo" para cada DIPID presente en Pareos (Diputado1 y Diputado2), AUNQUE su fila en Votos diga "No Vota" (codigo 4).
    - Un DIPID que figura como "No Vota" y NO está en Pareos sigue siendo "ausente".
    - Sin bloque Pareos (o vacío) el comportamiento no cambia: nadie se marca pareo espurio.
    - El total de "pareo" emitido == número de DIPID distintos en Pareos (Diputado1 ∪ Diputado2). En el fixture real = 10.
  </behavior>
  <action>
    En `parseCamaraVotoDetalle`, ANTES del loop de `<Votos>`, recolecta el set de DIPID pareados leyendo `v.Pareos.Pareo` (usa `asArray`), y de cada `<Pareo>` extrae `Diputado1.DIPID` y `Diputado2.DIPID` con `txt`. Luego, al construir cada fila del roster, si `diputadoId` está en el set de pareados, fuerza `opcion = "pareo"` (sobreescribe el "ausente"/código-4 que `opcionDeVoto` habría dado). NO fabriques filas: solo re-etiquetas filas que YA existen en el roster. Mantén el fail-closed de `opcionDeVoto` para el resto. Actualiza el docstring de `parseCamaraVotoDetalle` y de `opcionDeVoto` para registrar el hallazgo LIVE: el pareo es un bloque hermano `<Pareos>`, NO un `Opcion Codigo=3` (A1b resuelto: código 3 no observado live; el pareo se resuelve por cruce de DIPID). Verifica que `camara-votacion-detalle-real.xml` ya contiene el bloque `<Pareos>` (existe en el fixture actual); si por alguna razón no estuviera, NO lo inventes — anótalo y usa el roster fixture. Añade tests en el describe "ns tempuri REAL": (a) los 10 DIPID de `<Pareos>` se emiten como "pareo"; (b) un DIPID "No Vota" fuera de `<Pareos>` (ej. 1009 Alessandri) se emite "ausente"; (c) todas las opciones ∈ {si,no,abstencion,pareo,ausente}.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test -- parse-camara-votacion</automated>
  </verify>
  <acceptance_criteria>
    - Sobre `camara-votacion-detalle-real.xml`, `parseCamaraVotoDetalle(...).filter(v=>v.opcion==="pareo").length === 10` (asertado en test).
    - DIPID 1240 y 1082 (Pareo 1 del fixture) → opcion "pareo"; DIPID 1009 (No Vota, no pareado) → "ausente".
    - `git diff` de `parse-camara-votacion.ts` muestra que se lee el bloque `Pareos` y NO se añade ninguna rama `codigo === "3"` (A1b NO se promueve a verificado).
    - Suite de `parse-camara-votacion.test.ts` en verde sin regresión de los describes v1/roster.
  </acceptance_criteria>
  <done>El pareo se deriva del bloque `<Pareos>` por cruce de DIPID; un diputado pareado ya no se mis-atribuye como "ausente"; código 3 nunca se fabrica.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-check ruidoso Σ roster == totales del header (gate SC#3)</name>
  <files>packages/tramitacion/src/parse-camara-votacion.test.ts, packages/tramitacion/src/parse-camara-votacion.ts</files>
  <read_first>
    - packages/tramitacion/src/parse-camara-votacion.ts (parseCamaraVotoDetalle; intParse/intOf; header TotalAfirmativos/Negativos/Abstenciones/Dispensados)
    - packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml (header: 58/81/0/0)
    - packages/tramitacion/src/parse-camara-votacion.test.ts
    - .planning/phases/.../64-RESEARCH.md (Pitfall 3: header labels ≠ row labels; SC#3)
  </read_first>
  <behavior>
    - Un test parsea el fixture real, suma el roster por Seleccion y assert: Σ(si)==TotalAfirmativos(58), Σ(no)==TotalNegativos(81), Σ(abstencion)==TotalAbstenciones(0). El cross-check es por BUCKET SEMÁNTICO, nunca por string de label.
    - Un fixture DELIBERADAMENTE corrupto (un voto cambiado de "no" a "si", desbalanceando la suma) hace que el assert FALLE ruidoso (throw/expect fail) — nunca pasa en silencio ni retorna 0.
    - El pareo se cuenta aparte (Σ(pareo) == nº de pareados); "ausente" ∪ "pareo" cubre los no-nominales — se documenta que TotalDispensados corresponde a un bucket a confirmar LIVE (Plan 02), no se assert-a ciegamente contra "ausente".
  </behavior>
  <action>
    Añade un describe "cross-check de totales (SC#3)" en `parse-camara-votacion.test.ts`. Escribe una función de test-helper local que, dado el XML de detalle, extraiga los totales del header (TotalAfirmativos/TotalNegativos/TotalAbstenciones) y las sumas del roster por `opcion`, y asserte igualdad por bucket semántico (si↔Afirmativos, no↔Negativos, abstencion↔Abstenciones). Añade un caso negativo: toma el XML real, muta en memoria UN `<Opcion Codigo="0">En Contra</Opcion>` a `Codigo="1">Afirmativo`, parsea, y `expect(() => crossCheck(...)).toThrow()` (o `expect(sumaSi).not.toBe(58)` de forma explícita y ruidosa). NO cambies el parser para esto salvo que necesites exponer un helper de cross-check exportable; si lo exportas, hazlo como función pura documentada (no un `if` silencioso que retorna 0). Documenta en el test que TotalDispensados vs "ausente" queda pendiente de confirmación LIVE en Plan 02 (Open Question 2) — no lo assert-es ciegamente hoy.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test -- parse-camara-votacion</automated>
  </verify>
  <acceptance_criteria>
    - Test positivo: Σ(si)==58, Σ(no)==81, Σ(abstencion)==0 sobre el fixture real (asertado).
    - Test negativo: el fixture mutado hace THROW / expect-fail explícito (mismatch nunca silencioso) — verificable en el test.
    - El cross-check empareja por bucket semántico (si↔Afirmativos…), NO por comparación de strings de label del header.
    - Suite `@obs/tramitacion` en verde.
  </acceptance_criteria>
  <done>El cross-check de totales existe, cuadra sobre el fixture LIVE, y un mismatch falla ruidoso (SC#3 cubierto por test determinista).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente WS (opendata.camara.cl) → parser | XML no confiable cruza aquí; este plan NO toca la red (offline), pero fija la semántica que el Plan 02 ejercerá LIVE |
| roster crudo → atribución de voto por DIPID | un código/pareo mal mapeado es una afirmación factual falsa (difamación) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering (Integrity) | `opcionDeVoto` / `parseCamaraVotoDetalle` mapeo de voto | mitigate | Mapeo code-first fail-closed; opción ilegible → null (nunca fabrica sí/no). Pareo derivado de `<Pareos>` por DIPID, no de un código inventado. Tests fijan cada bucket contra fixture LIVE. |
| T-64-02 | Tampering (Integrity) | suma roster vs totales header | mitigate | Cross-check por bucket semántico con caso negativo que hace THROW ruidoso (SC#3); `intParse` distingue ausente-0 de ilegible-null → nunca fabrica un total. |
| T-64-03 | Repudiation | atribución de pareo a la persona equivocada | mitigate | Pareo se re-etiqueta SOLO sobre filas ya presentes en el roster, cruzando DIPID exacto; nunca crea una fila nueva ni adivina. |

*V5 Input Validation: el parser valida vía `VotacionSchema` (zod) aguas arriba; este plan no abre red → SSRF/crypto no aplican en Plan 01 (viven en Plan 02).*
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test` en verde (todos los describes: v1, tempuri real, roster, cross-check).
- `git diff packages/tramitacion/src/parse-camara-votacion.ts` NO contiene ninguna rama `codigo === "3"` (A1b no promovido).
- `git grep -n "codigo === \"2\"" packages/tramitacion/src/parse-camara-votacion.ts` devuelve ≥1 match (abstención por código añadida).
- `git grep -n "Pareos" packages/tramitacion/src/parse-camara-votacion.ts` devuelve ≥1 match (bloque leído).
</verification>

<success_criteria>
- SC#2 (parcial, offline): el mapeo `Opcion Codigo → Selección` está fijado por test contra el fixture LIVE-derivado, incluyendo abstención (codigo 2, confirmado) y pareo (derivado de `<Pareos>`, no de un código 3 sintético). Dispensado queda para confirmación LIVE en Plan 02.
- SC#3: la suma voto-a-voto cuadra contra los totales del header y un mismatch falla RUIDOSO por test determinista.
- Ninguna semántica fabricada: código 3 nunca se promueve a verificado; opción ilegible → null.
</success_criteria>

<output>
Create `.planning/phases/64-voto-p3a-validar-caracterizar-opendata-camara-cl-live-spike/64-01-SUMMARY.md` when done
</output>
