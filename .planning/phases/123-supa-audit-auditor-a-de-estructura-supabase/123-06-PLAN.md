---
phase: 123-supa-audit
plan: 06
type: execute
wave: 4
depends_on: ["123-02", "123-03", "123-04", "123-05"]
files_modified:
  - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-REVIEWER-VEREDICTO.md
  - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md
autonomous: true
requirements: [SUPA-01]

must_haves:
  truths:
    - "El subagente supabase-reviewer emite un veredicto APROBADO/BLOQUEADO sobre los seis ejes y ese veredicto está citado como el gate de la fase"
    - "Existe UN artefacto consolidado con método, seis ejes, tabla de offenders y secciones de 0-offenders-demostrado"
    - "Cada offender tiene objeto, riesgo, fix propuesto, la query que lo detectó y su destino"
    - "Los dos ítems de estructura heredados de la Phase 122 quedan anclados como candidatos a migración aditiva de la Phase 124"
  artifacts:
    - path: ".planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-REVIEWER-VEREDICTO.md"
      provides: "Veredicto bloqueante/no-bloqueante del subagente supabase-reviewer"
      contains: "VEREDICTO"
    - path: ".planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md"
      provides: "Artefacto consolidado de la Phase 123 (deliverable de SUPA-01)"
      contains: "## Tabla de offenders"
  key_links:
    - from: "123-SUPA-AUDIT.md"
      to: "123-SUPA-REVIEWER-VEREDICTO.md"
      via: "el consolidado cita el veredicto del reviewer como gate"
      pattern: "supabase-reviewer"
    - from: "123-SUPA-AUDIT.md"
      to: ".planning/phases/124-*/"
      via: "cada offender lleva destino 124-aditivo / supabase-architect+checkpoint / guard / deuda-operador"
      pattern: "124-aditivo"
---

<objective>
Delegar el gate al subagente `supabase-reviewer` y consolidar los fragmentos en el deliverable único de
SUPA-01.

Purpose: la decisión LOCKED del CONTEXT es que **el veredicto del `supabase-reviewer` ES el gate**, no
una opinión consultiva. La consolidación existe para que la Phase 124 reciba una lista de offenders
ejecutable con destino asignado, y para que los dos ítems de estructura que dejó la Phase 122 (cap
`p_limit: 1000` en la RPC de votos; tile *Por materia* sin denominador de cobertura) no se pierdan: el
ROADMAP de 124 **no los nombra**.

Output: `123-SUPA-REVIEWER-VEREDICTO.md` + `123-SUPA-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-00-METODO.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-01-SCHEMA-RLS-GRANTS.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-02-RPC-SECDEF.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-03-EXPOSICION-GUARDS.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md
</context>

<regimen_locked>
Cero DDL, cero DML, cero migración, cero deploy, cero flag. El `supabase-reviewer` es **read-only por
diseño**: no aplica ni arregla nada. Cero PII y cero valores de credencial en ambos artefactos.
</regimen_locked>

<tasks>

<task type="auto">
  <name>Task 1: Delegar el gate al subagente supabase-reviewer</name>
  <files>.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-REVIEWER-VEREDICTO.md</files>
  <read_first>
    - $HOME/.claude/agents/supabase-reviewer.md (procedimiento, clasificación BLOQUEANTE/DEUDA, entregable)
    - $HOME/.claude/skills/supabase-ops/references/02-security-rls.md y 08-tech-debt-review.md
    - .supabase-ops.yaml
    - los cuatro fragmentos 00/01/02/03 y el 04 de guards
  </read_first>
  <action>
Invocar la tool `Task` con `subagent_type: supabase-reviewer`. El prompt de delegación debe incluir,
explícitamente:
1. El **manifiesto** `.supabase-ops.yaml` (ruta) y el régimen read-only de la fase.
2. Las **rutas** de los cinco fragmentos (`123-SUPA-AUDIT-00-METODO.md` … `-04-GUARDS.md`) como
   evidencia ya recolectada contra la DB viva — el reviewer **no** debe re-consultar PROD ni escribir.
3. El encargo: **veredicto APROBADO / BLOQUEADO** sobre los seis ejes (schema, RLS, grants, RPCs
   bounded + `PUBLIC_RPC_ALLOWLIST`, `SECURITY DEFINER`/`search_path`, buckets/keys/secrets), con cada
   hallazgo mapeado a su número de catálogo **Splinter (0001-0029)** y su clasificación
   **BLOQUEANTE** vs **DEUDA**.
4. La restricción: **NO aplica ni arregla nada**; propone el fix. Todo hallazgo que exija DROP, cambio
   de tipo o backfill se marca para `supabase-architect` + checkpoint de operador.
5. El recordatorio de que los **Database Advisors / Splinter** contra el remoto no son invocables desde
   esta sesión: si el reviewer los considera necesarios, lo declara como
   **acción de operador pendiente**, no como hallazgo fabricado.

Escribir la respuesta íntegra del subagente en `123-SUPA-REVIEWER-VEREDICTO.md`, con frontmatter
(`fase: 123`, `subagente: supabase-reviewer`, `fecha`, `veredicto: APROBADO|BLOQUEADO`) y el cuerpo
verbatim. **No editorializar ni resumir el veredicto**: si el reviewer BLOQUEA, se registra BLOQUEADO y
la fase lo trata como gate cerrado hacia la Phase 124 (124 no arranca hasta que los bloqueantes tengan
fix planificado).

Si el subagente `supabase-reviewer` no está disponible en el entorno, **NO se fabrica un veredicto**:
se registra `veredicto: NO-EMITIDO` con la evidencia del intento y se escala como blocker al
orquestador — el gate no se auto-otorga.
  </action>
  <verify>
    <automated>f=.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-REVIEWER-VEREDICTO.md; test -f "$f" &amp;&amp; grep -qE 'veredicto: (APROBADO|BLOQUEADO|NO-EMITIDO)' "$f" &amp;&amp; grep -q 'supabase-reviewer' "$f" &amp;&amp; ! grep -qiE 'postgres://|sb_secret|eyJ' "$f" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `123-SUPA-REVIEWER-VEREDICTO.md` existe con frontmatter que incluye `veredicto:` con uno de los tres valores exactos `APROBADO` / `BLOQUEADO` / `NO-EMITIDO`.
    - El cuerpo es la respuesta verbatim del subagente, no un resumen del padre.
    - Cada hallazgo del reviewer lleva su clasificación `BLOQUEANTE` o `DEUDA` y, cuando aplique, su número Splinter.
    - Cero valores de credencial y cero PII en el archivo.
    - Si `NO-EMITIDO`, el archivo transcribe el error/evidencia del intento y el SUMMARY escala el blocker.
  </acceptance_criteria>
  <done>El gate de la fase está emitido por el subagente designado y registrado sin editorializar.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidar 123-SUPA-AUDIT.md con la tabla de offenders y los 0-offenders demostrados</name>
  <files>.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md</files>
  <read_first>
    - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md (molde de consolidación de fragmentos)
    - .planning/phases/113-*/113-INVENTARIO.md (molde de artefacto rector con método declarado)
    - los cinco fragmentos de la fase + `123-SUPA-REVIEWER-VEREDICTO.md`
  </read_first>
  <action>
Escribir `123-SUPA-AUDIT.md` — el deliverable de SUPA-01. Estructura obligatoria:

1. **Frontmatter**: `fase: 123`, `requisito: SUPA-01`, `ancla_temporal`, `veredicto_gate`
   (copiado del archivo del reviewer), `fragmentos: [00,01,02,03,04]`.
2. **`## Método y régimen declarados`** — copiado de §0.0/§0.0.1 del fragmento 00 (régimen read-only,
   filtro `pg_depend deptype='e'`, cero PII, cero DDL/DML, fixes = Phase 124) + §0.4 el gotcha de
   `schema_migrations` demostrado.
3. **Una sección por eje** (1..6), cada una con **la consulta verbatim** (`Q-NN`) y su tabla de
   resultados. Las queries se **transcriben**, no se referencian: el consolidado tiene que ser
   auto-contenido y re-ejecutable sin abrir los fragmentos.
4. **`## Tabla de offenders`** — plantilla de §0.2, columnas exactas y en este orden:
   `| # | objeto (tipo · nombre) | eje | riesgo | fix propuesto | query que lo detectó (Q-NN) | destino |`
   con `destino` del vocabulario cerrado: `124-aditivo` · `supabase-architect+checkpoint` · `guard` ·
   `deuda-operador`.
5. **`## 0 offenders demostrado`** — una subsección por eje que salió limpio, cada una con **la query
   que lo prueba** y su salida `(0 filas)`. Regla dura escrita: un eje `conforme` sin su bloque ```sql
   es inválido; si alguno llega así desde su fragmento, este plan lo **rechaza** y no consolida hasta
   que la query esté.
6. **`## Veredicto del gate`** — el veredicto del `supabase-reviewer` **citado** (bloque de cita con el
   veredicto y la lista de bloqueantes), enlazando a `123-SUPA-REVIEWER-VEREDICTO.md`.
7. **`## Identidad aritmética de la consolidación`** — espejo de la Phase 122: `N_offenders (tabla §4)
   == suma de offenders declarados en los fragmentos 01+02+03+04`, con los sumandos escritos. Cero
   filas huérfanas, cero filas perdidas.

Cero PII, cero credenciales, cero `postgres://`.
  </action>
  <verify>
    <automated>f=.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md; grep -q '## Tabla de offenders' "$f" &amp;&amp; grep -q '## 0 offenders demostrado' "$f" &amp;&amp; grep -q '## Veredicto del gate' "$f" &amp;&amp; grep -q 'schema_migrations' "$f" &amp;&amp; grep -c '```sql' "$f" | awk '$1>=10{exit 0} {exit 1}' &amp;&amp; ! grep -qiE 'postgres://|sb_secret|eyJ' "$f" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `123-SUPA-AUDIT.md` contiene los encabezados literales `## Método y régimen declarados`, `## Tabla de offenders`, `## 0 offenders demostrado`, `## Veredicto del gate`, `## Identidad aritmética de la consolidación`.
    - Contiene una sección por cada uno de los seis ejes, y **al menos 10 bloques ```sql transcritos verbatim** (el verify lo cuenta).
    - La tabla de offenders usa las 7 columnas en el orden dado y todo `destino` pertenece al vocabulario cerrado de 4 valores.
    - Toda subsección de `## 0 offenders demostrado` tiene su bloque ```sql y su salida `(0 filas)`.
    - `## Veredicto del gate` cita el veredicto verbatim y enlaza el archivo del reviewer.
    - La identidad aritmética está escrita con sus sumandos y cuadra.
    - Cero `postgres://`/`sb_secret`/`eyJ` y cero PII.
  </acceptance_criteria>
  <done>Deliverable de SUPA-01 completo, auto-contenido y re-ejecutable.</done>
</task>

<task type="auto">
  <name>Task 3: Anclar el backlog de estructura para la Phase 124</name>
  <files>.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md</files>
  <read_first>
    - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md (las filas que 122 declaró y NO corrigió)
    - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-05-SUMMARY.md (decisión: "sin migración 0073 — su diseño pertenece a la Phase 124")
    - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-02-RPC-SECDEF.md (Eje 4a: la nota sobre `p_limit` con default alto)
  </read_first>
  <action>
Añadir a `123-SUPA-AUDIT.md` la sección **`## Backlog de estructura heredado (entrada a la Phase 124)`**.
El ROADMAP de la Phase 124 **no nombra** estos ítems; anclarlos aquí es lo que evita que se pierdan.

Registrar como mínimo estas **dos** filas, cada una con la evidencia que ya existe (no re-medir salvo
que la query esté a mano):

| # | ítem | evidencia | por qué exige SQL | forma del fix (aditiva) | destino |
|---|------|-----------|-------------------|-------------------------|---------|
| B-01 | Cap `p_limit: 1000` en la RPC de votos | `D1165` tiene **3.752** votos reales; el deploy muestra **1.000**. La RPC ordena `by fecha desc` ⇒ además **distorsiona la composición** del desglose (no es solo truncar) | el total honesto no es derivable del set truncado | **RPC de conteo dedicada** (aguja completa: secdef PII-safe, `search_path`, `statement_timeout`, `LIMIT`, doble-revoke, entrada en `PUBLIC_RPC_ALLOWLIST`) — **aditiva**, no altera la RPC existente | `124-aditivo` |
| B-02 | Tile *Por materia* agrupa **3.100/3.675 (84,4 %)** sin declarar cobertura | fragmento de cruces de la Phase 122 | la RPC hoy **no emite el denominador**; declarar cobertura sin denominador es fabricarla | añadir el denominador a la salida de la RPC (firma **v2 paralela**, precedente 0060: no alterar la firma viva ⇒ evita `42P13` y la re-arma de default privileges) | `124-aditivo` |

Añadir también, si los ejes 4a/5 los produjeron, las filas de offender de estructura que 124 debe
resolver, referenciando su `Q-NN`. **No** diseñar el SQL aquí: la forma del fix se describe en una
línea; la migración es la Phase 124.

Nota LOCKED a escribir al pie de la sección: **ninguna migración `0073` se escribe en esta fase.** El
precedente de 122-05 es explícito: escribir una migración especulativa contra un contrato no adjudicado
deja una migración huérfana.
  </action>
  <verify>
    <automated>f=.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT.md; grep -q '## Backlog de estructura heredado' "$f" &amp;&amp; grep -q 'B-01' "$f" &amp;&amp; grep -q 'B-02' "$f" &amp;&amp; grep -q '3.752' "$f" &amp;&amp; grep -q '84,4' "$f" &amp;&amp; ls supabase/migrations/0073* 2>/dev/null | wc -l | grep -q '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe el encabezado literal `## Backlog de estructura heredado (entrada a la Phase 124)`.
    - La tabla contiene al menos las filas `B-01` (cap `p_limit: 1000`, con las cifras 3.752 y 1.000 y la mención a la distorsión por `order by fecha desc`) y `B-02` (tile *Por materia*, con 3.100/3.675 y 84,4 %).
    - Cada fila declara su `forma del fix (aditiva)` y `destino: 124-aditivo`.
    - Existe la nota LOCKED de que ninguna migración `0073` se escribe en esta fase, y **no existe** ningún archivo `supabase/migrations/0073*`.
    - `git diff --quiet -- supabase app` sale 0.
  </acceptance_criteria>
  <done>La Phase 124 recibe su backlog de estructura anclado con evidencia y con la forma aditiva del fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagente `supabase-reviewer` → decisión de gate | el veredicto autoriza (o no) el avance a la Phase 124 |
| artefacto consolidado → Phase 124 | lo que no quede escrito aquí, no se arregla allá |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-21 | Repudiation | veredicto de gate fabricado por el padre | mitigate | el cuerpo es verbatim del subagente; si no está disponible → `NO-EMITIDO` + escalada, jamás auto-otorgado |
| T-123-22 | Information Disclosure | consolidado con salida cruda de psql | mitigate | cero PII y cero credenciales; verify grep anti-`postgres://`/`sb_secret`/`eyJ` |
| T-123-23 | Tampering | consolidación que "limpia" offenders al resumir | mitigate | identidad aritmética: offenders del consolidado == suma de los fragmentos, con sumandos escritos |
| T-123-24 | Tampering | escribir una migración especulativa `0073` | mitigate | acceptance exige que `supabase/migrations/0073*` NO exista |
</threat_model>

<verification>
- `123-SUPA-REVIEWER-VEREDICTO.md` con veredicto de los tres valores permitidos.
- `123-SUPA-AUDIT.md` con las 6 secciones de eje, tabla de offenders, 0-offenders demostrado, gate citado, identidad aritmética y backlog B-01/B-02.
- `git diff --quiet -- supabase app` sale 0.
</verification>

<success_criteria>
El veredicto del `supabase-reviewer` es el gate registrado de la fase, el deliverable de SUPA-01 está
consolidado y auto-contenido, y la Phase 124 recibe una lista de offenders con destino asignado más el
backlog heredado de la Phase 122.
</success_criteria>

<output>
Create `.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-06-SUMMARY.md` when done
</output>
