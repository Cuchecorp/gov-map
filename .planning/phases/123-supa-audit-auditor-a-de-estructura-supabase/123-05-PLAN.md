---
phase: 123-supa-audit
plan: 05
type: execute
wave: 3
depends_on: ["123-02", "123-03", "123-04"]
files_modified:
  - app/lib/lockdown-guard.test.ts
  - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md
autonomous: true
requirements: [SUPA-01]

must_haves:
  truths:
    - "Los puntos ciegos de guard detectados por los ejes 3 y 4b quedan cerrados con una aserción estática nueva, o se demuestra que no hay ninguno"
    - "Toda extensión de guard trae un mutation self-check que prueba que el detector MUERDE"
    - "La suite de app/ y todos los guards siguen verdes tras la extensión"
    - "Ningún fix de estructura de datos se aplica en esta fase: los offenders siguen ruteados a la Phase 124"
  artifacts:
    - path: ".planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md"
      provides: "Registro del punto ciego (o su ausencia demostrada) y de la extensión aplicada"
      contains: "## Punto ciego"
  key_links:
    - from: "app/lib/lockdown-guard.test.ts"
      to: "supabase/migrations/*.sql"
      via: "la extensión escanea el texto de las migraciones (CI no tiene acceso a la DB)"
      pattern: "MIGRATIONS_DIR"
---

<objective>
Cerrar, con "guard primero", los puntos ciegos que la auditoría descubrió — **antes** de que la Phase
124 toque nada.

Purpose: los planes 123-02 (Eje 3) y 123-03 (Eje 4b) contrastan el guard estático contra la DB viva.
Donde el guard dice "0 offenders" y la DB muestra filas, hay un punto ciego: un fix de 124 podría
reintroducir el mismo defecto y pasar CI verde. La regla del proyecto (Wave-0 de v10.0/v11.0) es
extender el guard primero.

Output: extensión de `app/lib/lockdown-guard.test.ts` (si hay punto ciego) + el registro
`123-SUPA-AUDIT-04-GUARDS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-00-METODO.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-01-SCHEMA-RLS-GRANTS.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-02-RPC-SECDEF.md
@.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-03-EXPOSICION-GUARDS.md
</context>

<restriccion_locked>
Este plan puede modificar **solo** `app/lib/lockdown-guard.test.ts` (guard) y su propio fragmento.
Cero DDL, cero DML, cero migración nueva, cero deploy, cero flag tocado. Si el "fix" del punto ciego
exigiera cambiar la estructura de datos, **no se hace aquí**: se rutea a la Phase 124 o a
`supabase-architect+checkpoint`.
</restriccion_locked>

<tasks>

<task type="auto">
  <name>Task 1: Adjudicar el punto ciego — extensión estática o ausencia demostrada</name>
  <files>.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-01-SCHEMA-RLS-GRANTS.md (Eje 3: tabla `guard estático vs DB viva`, `Q-09`)
    - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-02-RPC-SECDEF.md (Eje 4b: los dos `comm`)
    - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-03-EXPOSICION-GUARDS.md (Eje 6b: la columna "qué NO ve")
    - app/lib/lockdown-guard.test.ts (`anonGrantOffenders` :240, `authenticatedGrantOffenders` :273, `definedRpcNames` :596, describe A2 :609, describe A3 :679)
  </read_first>
  <action>
Crear `123-SUPA-AUDIT-04-GUARDS.md` con la sección **`## Punto ciego`** y adjudicar, con la evidencia
de los tres fragmentos, UNA de estas dos ramas. La adjudicación se escribe **antes** de tocar código.

**Rama A — hay punto ciego.** Extender `app/lib/lockdown-guard.test.ts` con la aserción **estática**
que lo cierra. El guard corre en CI **sin acceso a la DB**, así que la extensión escanea el texto de
`supabase/migrations/*.sql`, nunca consulta Postgres. Candidato prioritario ya identificado en la
planificación (confirmarlo o descartarlo con la evidencia):

> **Direction-C** — hoy Direction-B (`:614`) verifica `PUBLIC_RPC_ALLOWLIST ⊆ definedRpcNames(...)`.
> El sentido inverso **no está cubierto por texto de migración**: una migración puede declarar
> `grant execute on function <f>(...) to anon` sin que `<f>` esté en `PUBLIC_RPC_ALLOWLIST`, y el
> guard pasa verde. La aserción nueva: extraer de las migraciones todo nombre de función con
> `grant execute ... to anon` (sobre el SQL ya pasado por `stripSqlComments`, por sentencia
> `split(";")`, espejando la forma de `anonGrantOffenders` :240) y exigir que ese conjunto sea
> **subconjunto** de `PUBLIC_RPC_ALLOWLIST`.

Reglas de construcción de la extensión, LOCKED:
- Reusar los helpers existentes (`stripSqlComments`, `migrationNumber`, `MIGRATIONS_DIR`); **no**
  duplicar lógica de parsing.
- La función detectora es **pura** y exportable dentro del archivo, para poder ejercitarla en memoria.
- El `describe` nuevo declara su bloque y su fase en el nombre (p. ej.
  `"(A4) Guard — grant execute a anon en migraciones ⊆ PUBLIC_RPC_ALLOWLIST (Direction-C, Phase 123)"`).
- Mensaje de fallo accionable, espejo de `:700`: nombra el offender y dice qué hacer.
- **Cero renombres** de constantes o describes existentes (lección Phase 100 Pitfall 1). La extensión
  es **aditiva**.

**Rama B — no hay punto ciego.** Escribir en el fragmento la **demostración**: por cada contraste
guard-vs-DB de los ejes 3 y 4b, la salida que prueba la coincidencia (`(0 filas)` en ambos `comm` y en
`Q-09`), y la frase explícita de que **el guard no se toca porque no hay evidencia de ceguera**. En
esta rama `app/lib/lockdown-guard.test.ts` **no se modifica** y se retira de `files_modified` en el
SUMMARY.

En ambas ramas, el fragmento incluye una tabla:
| punto ciego candidato | evidencia (fragmento §, Q-NN) | ¿confirmado? | acción |
  </action>
  <verify>
    <automated>f=.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md; grep -q '## Punto ciego' "$f" &amp;&amp; grep -qE 'Rama A|Rama B' "$f" &amp;&amp; grep -q 'Q-' "$f" &amp;&amp; git diff --quiet -- supabase &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `123-SUPA-AUDIT-04-GUARDS.md` existe con el encabezado `## Punto ciego` y declara explícitamente `Rama A` o `Rama B`.
    - La tabla de puntos ciegos candidatos cita al menos un `Q-NN` real de los fragmentos 01/02 por fila; ninguna celda `evidencia` vacía.
    - **Rama A**: `app/lib/lockdown-guard.test.ts` tiene un `describe` NUEVO cuyo nombre contiene `Phase 123`, y `git diff app/lib/lockdown-guard.test.ts` muestra solo líneas AÑADIDAS (cero líneas borradas de describes/constantes preexistentes).
    - **Rama B**: `git diff --quiet -- app/lib/lockdown-guard.test.ts` sale 0 y el fragmento transcribe la salida `(0 filas)` de `Q-09` y de ambos `comm`.
    - `git diff --quiet -- supabase` sale 0 en ambas ramas: cero migraciones tocadas.
  </acceptance_criteria>
  <done>El punto ciego queda adjudicado con evidencia; el guard se extiende solo si la evidencia lo exige.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation self-check + suite y guards verdes</name>
  <files>app/lib/lockdown-guard.test.ts, .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md</files>
  <read_first>
    - app/lib/lockdown-guard.test.ts (los self-checks existentes como molde: `:472` mutation self-check de `authenticatedGrantOffenders`, `:623` Direction-B self-check, `:704` crossLinkReader self-check)
    - .planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-SUPA-AUDIT-04-GUARDS.md (rama adjudicada en Task 1)
  </read_first>
  <action>
**Solo si Task 1 adjudicó Rama A.** Añadir al `describe` nuevo un **mutation self-check** que ejercite
el detector REAL **en memoria** (fixture string, sin escribir archivos ni tocar `supabase/migrations/`),
espejando `:472` y `:623`:
- (a) fixture POSITIVO: SQL con `grant execute on function <nombre_inexistente>(uuid) to anon;` ⇒ el
  detector devuelve ese nombre como offender.
- (b) fixture NEGATIVO: SQL con un `grant execute` a una función SÍ presente en `PUBLIC_RPC_ALLOWLIST`
  ⇒ cero offenders.
- (c) fixture de COMENTARIO: el mismo `grant` dentro de `-- comentario` ⇒ cero offenders (prueba que
  `stripSqlComments` está en el camino y que la prosa de un header no auto-invalida el guard).

Luego, en AMBAS ramas, correr y transcribir en el fragmento (sección `## Suite y guards verdes`):
```bash
pnpm --filter ./app test -- lockdown-guard
pnpm --filter ./app test
pnpm --filter ./app exec tsc --noEmit
```
(usar el comando REAL del proyecto, resolviéndolo desde `package.json` de raíz y de `app/`, y
transcribirlo tal cual se ejecutó).

Registrar: conteo de aserciones de `lockdown-guard` **antes y después** (referencia previa: 22) y el
total de la suite de `app/` (referencia: **1577**). El total debe ser `>= 1577`; si bajó, es regresión
y el task no cierra.

Finalmente añadir al fragmento la sección `## Lo que este plan NO hizo`: cero migraciones, cero DDL/DML,
cero deploy, cero flag; los offenders de estructura siguen ruteados a la Phase 124.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm test 2>&amp;1 | tail -20 &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - **Rama A**: el `describe` nuevo contiene al menos 3 `it(...)` incluido el mutation self-check con los fixtures (a), (b) y (c); todos pasan.
    - La suite de `app/` sale verde con total `>= 1577`, y el número exacto queda transcrito en el fragmento.
    - `tsc --noEmit` sale 0.
    - El fragmento tiene la sección `## Suite y guards verdes` con el comando REAL y su salida (conteos antes/después de `lockdown-guard`).
    - El fragmento tiene la sección `## Lo que este plan NO hizo` enumerando: migraciones, DDL/DML, deploy, flags.
    - `git diff --quiet -- supabase` sale 0.
  </acceptance_criteria>
  <done>La extensión muerde por construcción demostrada, y la suite completa sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migración futura (Phase 124) → régimen de grants | el guard CI es lo único que impide reintroducir un defecto |
| autor de código → árbol público del sitio | Block B del guard es el control de PII bajo `service_role` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-18 | Elevation of Privilege | migración de la Phase 124 que otorga `execute` a `anon` fuera de la allowlist | mitigate | extensión Direction-C: `grant execute ... to anon` en migraciones ⊆ `PUBLIC_RPC_ALLOWLIST` |
| T-123-19 | Repudiation | guard que "pasa verde" sin detectar nada | mitigate | mutation self-check con fixture positivo, negativo y de comentario, ejercitando el detector real en memoria |
| T-123-20 | Tampering | el propio plan corrigiendo estructura de datos | mitigate | `files_modified` acotado a un archivo de guard + su fragmento; acceptance exige `git diff --quiet -- supabase` |
</threat_model>

<verification>
- `pnpm --filter ./app test` verde con total `>= 1577`; `tsc --noEmit` 0.
- `git diff --quiet -- supabase` sale 0.
- `123-SUPA-AUDIT-04-GUARDS.md` declara rama, evidencia y "lo que NO hizo".
</verification>

<success_criteria>
Todo punto ciego descubierto por la auditoría queda cerrado con una aserción estática que muerde, o su
ausencia queda demostrada con la consulta; la suite y los guards siguen verdes.
</success_criteria>

<output>
Create `.planning/phases/123-supa-audit-auditor-a-de-estructura-supabase/123-05-SUMMARY.md` when done
</output>
