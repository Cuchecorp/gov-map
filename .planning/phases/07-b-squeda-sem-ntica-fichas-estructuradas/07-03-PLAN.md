---
phase: 07-b-squeda-sem-ntica-fichas-estructuradas
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/lib/types.ts
  - app/lib/buscar.ts
  - app/components/search-box.tsx
  - app/components/search-result-card.tsx
  - app/components/idea-matriz-block.tsx
  - app/components/cuerpos-legales-list.tsx
  - app/components/proyectos-similares.tsx
  - app/app/page.tsx
  - app/app/buscar/page.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/components/search-result-card.test.tsx
  - app/lib/buscar.test.ts
autonomous: false
requirements: [SEM-04, SEM-05, SEM-06]
user_setup:
  - service: shadcn
    why: "Input primitive para SearchBox (registro oficial)"
    dashboard_config:
      - task: "Añadir el componente Input"
        location: "cd app && pnpm dlx shadcn@latest add input"
must_haves:
  truths:
    - "Un usuario escribe una consulta en lenguaje natural y recibe tarjetas de ficha resumida con trazabilidad (boletín, título, materia, estado, cámara, frescura, enlace)"
    - "Una consulta que coincide con el patrón de boletín redirige directo a /proyecto/[boletin] ANTES de embeber (atajo)"
    - "El embedding de la consulta (RETRIEVAL_QUERY) y el kNN corren server-only; la Gemini key nunca llega al cliente"
    - "La ficha /proyecto/[boletin] muestra idea matriz (cita literal o 'no disponible'), cuerpos legales y proyectos similares (kNN excluyendo el propio)"
    - "Ningún score de relevancia / % match se muestra al usuario (UI-SPEC §5)"
    - "La home reemplaza el scaffold de Next por una landing de búsqueda"
  artifacts:
    - path: "app/lib/buscar.ts"
      provides: "Embed query RETRIEVAL_QUERY + rpc match_proyectos, server-only"
      contains: "server-only"
    - path: "app/app/buscar/page.tsx"
      provides: "Resultados server-side + atajo boletín"
      contains: "match_proyectos"
    - path: "app/components/idea-matriz-block.tsx"
      provides: "Cita literal o estado no disponible"
      contains: "no disponible"
  key_links:
    - from: "app/lib/buscar.ts"
      to: "rpc match_proyectos"
      via: "createServerSupabase().rpc"
      pattern: "match_proyectos"
    - from: "app/app/buscar/page.tsx"
      to: "redirect(/proyecto/...)"
      via: "BOLETIN_RE shortcut antes de embeber"
      pattern: "BOLETIN_RE"
---

<objective>
Construir el **read-path** (rebanada vertical de búsqueda ciudadana): landing de búsqueda en la home, `/buscar` con embedding de consulta server-only + kNN (SEM-04), y las secciones estructuradas de la ficha — idea matriz (cita literal o "no disponible"), cuerpos legales, y "proyectos similares" por kNN excluyendo el propio (SEM-05/06).

Esta rebanada vuelve verde la mitad-read del slice E2E y entrega la diana ciudadana de la fase: un usuario busca en lenguaje natural y recibe fichas estructuradas con trazabilidad. Consume el RPC match_proyectos + tablas de 0011 (aplicadas en LOCAL por la ola 1).

Purpose: Cerrar el frente "proyectos" del milestone 1 con la búsqueda semántica visible al ciudadano, server-only, sin exponer keys ni scores.
Output: lib/buscar + SearchBox + SearchResultCard + secciones de ficha + landing + /buscar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-RESEARCH.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-PATTERNS.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-UI-SPEC.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md

<interfaces>
<!-- Contratos de la ola 1 + analogs del codebase. La UI completa está en 07-UI-SPEC.md. -->

From app/lib/supabase.ts (server-only data access):
```
import "server-only";
export function createServerSupabase(): SupabaseClient;  // anon key, SIN NEXT_PUBLIC_, RLS public-read
```

From app/app/proyecto/[boletin]/page.tsx (line 16) + reuso:
```
const BOLETIN_RE = /^\d{3,6}(-\d{1,2})?$/;   // validador de path injection, reusar para el atajo
// Server Component con <Suspense> por sección + skeletons (lines 37-49, 111-146)
```

From app/components/citacion-card.tsx (analog exacto de SearchResultCard):
```
// Card/CardHeader/CardContent + CamaraChip + ProvenanceBadge; título = <Link href={`/proyecto/${boletin}`}>
// sourceLabel(origen) de @/lib/types; ProvenanceBadge al fondo de CardContent
```

From migración 0011 (RPC, aplicado en LOCAL ola 1):
```
match_proyectos(query_embedding vector(768), match_count int default 20, match_threshold float8 default 0.0, exclude_boletin text default null)
  returns table(boletin text, similarity float8)   -- grant execute a anon
```

From packages/llm (@obs/llm):
```
class GeminiEmbeddingProvider { embed(texts, taskType?: "RETRIEVAL_QUERY"): Promise<{vector:number[]}[]> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/types (row types) + lib/buscar (embed query server-only + rpc match_proyectos)</name>
  <files>app/lib/types.ts, app/lib/buscar.ts, app/lib/buscar.test.ts</files>
  <read_first>
    - app/lib/supabase.ts (import "server-only" + createServerSupabase, anon key sin NEXT_PUBLIC_)
    - app/lib/types.ts lines 10-73 (ProyectoRow snake_case + sourceLabel — añadir ProyectoFichaRow, MatchProyectoRow)
    - app/app/proyecto/[boletin]/page.tsx line 16 (BOLETIN_RE — reusar el mismo validador)
    - 07-RESEARCH.md §"Code Examples" (server-only search helper: trim+cap, BOLETIN_RE redirect antes de embed, rpc match_proyectos)
    - 07-PATTERNS.md §app/lib/buscar.ts
  </read_first>
  <behavior>
    - Test buscar: q vacío/whitespace → devuelve [] sin llamar Gemini ni rpc
    - Test: q que matchea BOLETIN_RE → redirect("/proyecto/{q}") antes de embeber (mock de redirect)
    - Test: q normal → embed RETRIEVAL_QUERY (mock) → rpc match_proyectos con query_embedding (mock supabase) → devuelve filas
    - Test: q se trimea y se capea a ≤300 chars (V5 input validation)
    - Test: la Gemini key se lee de process.env server-only (no NEXT_PUBLIC_)
  </behavior>
  <action>
    En `types.ts` añadir `ProyectoFichaRow` (idea_matriz string|null, cuerpos_legales {norma,articulos[]}[], texto_r2_path string|null, estado string) y `MatchProyectoRow` ({ boletin: string; similarity: number }) en snake_case espejando ProyectoRow. `buscar.ts`: `import "server-only"`; `buscarProyectos(qRaw, { excludeBoletin? })`: trim + slice(0,300); si vacío → []; reusar `BOLETIN_RE` y `if (BOLETIN_RE.test(q)) redirect(\`/proyecto/${q}\`)` ANTES de embeber; instanciar GeminiEmbeddingProvider con `process.env.GEMINI_API_KEY` (server-only, nunca NEXT_PUBLIC_), `embed([q], "RETRIEVAL_QUERY")`; `createServerSupabase().rpc("match_proyectos", { query_embedding, match_count: 20, match_threshold, exclude_boletin })`; devolver data ?? []. supabase-js `.rpc()` parametriza — nunca interpolar q en SQL. NO exponer similarity a la UI más allá del orden.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm vitest run lib/buscar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run lib/buscar.test.ts` verde incl. q-vacío-no-llama, atajo-boletín-redirect, trim+cap
    - `grep -c "server-only" app/lib/buscar.ts` ≥ 1
    - `grep -c "NEXT_PUBLIC_" app/lib/buscar.ts` == 0 (key nunca pública)
    - `grep -c "BOLETIN_RE" app/lib/buscar.ts` ≥ 1 (atajo reusa el validador existente)
  </acceptance_criteria>
  <done>Capa de datos de búsqueda server-only: embed RETRIEVAL_QUERY + rpc kNN + atajo boletín, key server-only (SEM-04).</done>
</task>

<task type="auto">
  <name>Task 2: SearchBox (client island) + SearchResultCard + secciones de ficha (idea matriz / cuerpos / similares)</name>
  <files>app/components/search-box.tsx, app/components/search-result-card.tsx, app/components/search-result-card.test.tsx, app/components/idea-matriz-block.tsx, app/components/cuerpos-legales-list.tsx, app/components/proyectos-similares.tsx</files>
  <read_first>
    - app/components/citacion-card.tsx (entire — estructura exacta a espejar para SearchResultCard: Card/CardHeader/CardContent, CamaraChip, ProvenanceBadge, título Link)
    - app/components/etapa-badge.tsx, app/components/camara-chip.tsx, app/components/provenance-badge.tsx (reuso directo)
    - 07-UI-SPEC.md §3 (SearchBox: form GET progresivo, "use client", h-12, role=search, empty-submit guard), §5 (SearchResultCard props + sin score), §6 (IdeaMatrizBlock blockquote vs "no disponible" bg-muted/40; CuerposLegalesList; ProyectosSimilares exclude self)
    - app/lib/buscar.ts (Task 1 — buscarProyectos consumido por ProyectosSimilares con excludeBoletin)
  </read_first>
  <behavior>
    - Test search-result-card: renderiza boletín, título como Link a /proyecto/{boletin}, EtapaBadge, CamaraChip, ProvenanceBadge; NO renderiza ningún número de score/distancia
    - Test: materia con line-clamp; sin materia → no rompe
    - (component-level) IdeaMatrizBlock con ideaMatriz string → <blockquote>; con null → bloque "no disponible" (bg-muted/40, NO border-destructive)
    - (component-level) CuerposLegalesList con items → <ul>; vacío → párrafo honesto
    - SearchBox: form method=get action=/buscar (funciona sin JS); empty-submit no navega
  </behavior>
  <action>
    `search-box.tsx` ("use client", única isla): `<form role="search" method="get" action="/buscar" className="flex gap-2">` con shadcn `Input` (type=search, name="q", h-12, aria-label) + `Button` "Buscar"; `router.push("/buscar?q=" + encodeURIComponent(q))` para transición ágil; guard de submit vacío (no navega). NO llama Gemini/pgvector. `search-result-card.tsx` (Server Component): copiar la estructura de citacion-card; props `{ boletin, titulo, materia, estado, camaraOrigen, provenance }`; título `<Link href={\`/proyecto/${boletin}\`}>`; EtapaBadge + CamaraChip + ProvenanceBadge; **sin score** (UI-SPEC §5). `idea-matriz-block.tsx`: ideaMatriz string → `<blockquote className="border-l-2 ...">` + ProvenanceBadge; null → bloque "Idea matriz no disponible" (border-border bg-muted/40, copy de UI-SPEC §6.1, nunca "próximamente", nunca resumen LLM). `cuerpos-legales-list.tsx`: `<ul>` de {norma, articulos[]} o párrafo vacío honesto. `proyectos-similares.tsx` (async Server Component): llama `buscarProyectos`-equivalente / rpc match_proyectos con `exclude_boletin = boletin` (self-exclusion), top ~5, renderiza SearchResultCard rows o copy vacío honesto ("Aún no hay proyectos similares para mostrar…", nunca "no existen", nunca lenguaje de afinidad). Ejecutar `pnpm dlx shadcn@latest add input` en app/ antes de SearchBox.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm vitest run components/search-result-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run components/search-result-card.test.tsx` verde (título Link + badges, sin score)
    - `grep -c "border-destructive" app/components/idea-matriz-block.tsx` == 0 (ausencia ≠ error; usa bg-muted/40)
    - `grep -c "no disponible" app/components/idea-matriz-block.tsx` ≥ 1
    - `grep -Ei "similarity|distance|% match|score" app/components/search-result-card.tsx` == 0 (UI-SPEC §5)
    - SearchBox tiene `method="get"` y `action="/buscar"` (progressive enhancement)
  </acceptance_criteria>
  <done>Componentes de búsqueda y secciones de ficha con degradación honesta, sin score, reusando el design system (SEM-05/06).</done>
</task>

<task type="auto">
  <name>Task 3: Landing (reemplaza scaffold) + /buscar (server-side) + secciones en la ficha</name>
  <files>app/app/page.tsx, app/app/buscar/page.tsx, app/app/proyecto/[boletin]/page.tsx</files>
  <read_first>
    - app/app/page.tsx (scaffold actual de Next — reemplazar entero; quitar import de page.module.css)
    - app/app/agenda/page.tsx lines 40-56 (searchParams: Promise + await + validación; shell max-w-3xl)
    - app/app/proyecto/[boletin]/page.tsx (lines 16 BOLETIN_RE; lines 37-49/111-146 patrón <Suspense> por sección + skeleton — añadir secciones DESPUÉS de Votaciones sin tocar header/timeline/votaciones)
    - 07-UI-SPEC.md §2 (landing: hero copy exacto, py-16 md:py-24, no fabricated stats), §4 (/buscar: trim+cap, atajo boletín redirect, result-count copy, paginación simple), §6 (secciones de ficha + Suspense)
  </read_first>
  <action>
    `app/app/page.tsx`: borrar el scaffold (y el import de page.module.css; eliminar page.module.css si queda huérfano). Server Component shell `<main className="max-w-3xl mx-auto px-4 md:px-8 py-16 md:py-24">` con H1 "Observatorio del Congreso 360", purpose line y hint exactos de UI-SPEC §2, embebiendo `<SearchBox autoFocus />`. SIN stats fabricadas (solo count(*) real si se muestra; si no, omitir). `app/app/buscar/page.tsx`: Server Component; `searchParams: Promise<{q?,page?}>`, `await`; trim + cap ≤300; si `BOLETIN_RE.test(q)` → `redirect(\`/proyecto/${q}\`)` antes de embeber; si no, `buscarProyectos(q)`; render `<SearchBox initialQuery={q} />` persistente + result-count line + `<Suspense>` con 4 skeletons → SearchResultCard[]; paginación prev/next simple solo si >20 (Button ghost estilo WeekNav); estados vacío/error de UI-SPEC §8. `app/app/proyecto/[boletin]/page.tsx`: añadir `<section id="idea-matriz">`, `<section id="cuerpos-legales">` y `<section id="similares">` DESPUÉS de Votaciones, cada una en su `<Suspense>`, leyendo proyecto_ficha (idea_matriz/cuerpos) y ProyectosSimilares(boletin); NO modificar header/timeline/votaciones. Esto cierra la mitad-read del slice E2E.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm build && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` (Next 16) compila sin error de tipos/RSC
    - `pnpm vitest run` en app verde
    - `grep -c "page.module.css" app/app/page.tsx` == 0 (scaffold removido)
    - `app/app/buscar/page.tsx` hace redirect a /proyecto/ cuando BOLETIN_RE matchea (antes de embeber)
    - Las 3 nuevas secciones están en proyecto/[boletin] sin alterar las secciones existentes (header/timeline/votaciones siguen presentes)
  </acceptance_criteria>
  <done>Landing de búsqueda + /buscar server-side + ficha estructurada con idea matriz/cuerpos/similares; slice E2E ciudadano completo (SEM-04/05/06).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación visual de la búsqueda ciudadana end-to-end</name>
  <what-built>Landing de búsqueda, /buscar con embedding server-only + kNN, y ficha estructurada (idea matriz literal o "no disponible", cuerpos legales, proyectos similares). Requiere que la ola 2 haya poblado al menos algunas fichas+embeddings en el Supabase LOCAL para ver resultados reales.</what-built>
  <how-to-verify>
    1. `cd app && pnpm dev`; abre http://localhost:3000 — la home muestra la landing de búsqueda (NO el scaffold de Next), H1 "Observatorio del Congreso 360", input grande con autofocus.
    2. Escribe una consulta en lenguaje natural (p.ej. "protección de datos personales") y envía → /buscar muestra tarjetas de ficha resumida con boletín, título (link), materia, EtapaBadge, CamaraChip y ProvenanceBadge. NO debe aparecer ningún % de relevancia ni score.
    3. Escribe un número de boletín (p.ej. 15234-07) → redirige directo a /proyecto/15234-07 (atajo).
    4. En una ficha con texto disponible: la sección "Idea matriz" muestra una cita literal en <blockquote> con su ProvenanceBadge; "Cuerpos legales afectados" lista normas; "Proyectos similares" muestra vecinos kNN (sin el propio boletín).
    5. En una ficha con texto NO disponible: "Idea matriz no disponible" en bloque gris (bg-muted/40, NO rojo), sin inventar resumen.
    6. Confirma en DevTools/Network que ninguna llamada a Gemini ocurre desde el cliente (todo server-side; la key no aparece en el bundle).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /buscar (searchParams.q) | input no confiable del usuario; trim + cap + regex, parametrizado en rpc |
| servidor RSC → Gemini/Supabase | embed + kNN server-only; key nunca cruza al cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering (SQL injection) | buscar.ts / buscar page | mitigate | supabase-js `.rpc()` parametriza; q nunca se interpola en SQL; cap ≤300 + regex en el atajo |
| T-07-10 | Tampering (path injection) | atajo boletín → redirect | mitigate | reusar BOLETIN_RE `/^\d{3,6}(-\d{1,2})?$/` antes de redirect |
| T-07-11 | Information Disclosure | Gemini key al bundle cliente | mitigate | embed server-only (import "server-only"); key NUNCA NEXT_PUBLIC_; SearchBox solo navega, no llama modelos |
| T-07-12 | Information Disclosure | RPC expone columnas no públicas | mitigate | match_proyectos retorna solo (boletin, similarity); tablas sin datos personales; parlamentario intacto |
| T-07-13 | Spoofing | LLM output presentado como hecho | mitigate | idea matriz = cita literal en <blockquote> con provenance; "no disponible" nunca fabrica; sin lenguaje de afinidad/causalidad (guardarraíl #2) |
| T-07-SC | Tampering | shadcn input add | mitigate | registro oficial shadcn (no npm runtime nuevo); audit RESEARCH §Package Legitimacy: Approved |
</threat_model>

<verification>
- `pnpm vitest run` en app verde + `pnpm build` (Next 16) compila
- Checkpoint visual: búsqueda NL → tarjetas, atajo boletín, secciones de ficha, sin score, key no en cliente
- slice E2E de la ola 1 deja de fallar por los imports del read-path
</verification>

<success_criteria>
- Búsqueda en lenguaje natural devuelve fichas con trazabilidad (SEM-04)
- Proyectos similares por kNN excluyendo el propio (SEM-05)
- Ficha unifica boletín/título/iniciativa/autores/cámara/materia/idea matriz/cuerpos/estado/enlace (SEM-06)
- Todo server-only, sin keys en cliente, sin scores en UI
</success_criteria>

<output>
Create `.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-03-SUMMARY.md` when done
</output>
