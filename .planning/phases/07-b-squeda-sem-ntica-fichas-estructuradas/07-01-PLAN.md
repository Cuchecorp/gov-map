---
phase: 07-b-squeda-sem-ntica-fichas-estructuradas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fichas/package.json
  - packages/fichas/tsconfig.json
  - packages/fichas/vitest.config.ts
  - packages/fichas/src/index.ts
  - packages/fichas/src/model.ts
  - packages/fichas/src/model.test.ts
  - packages/fichas/src/prompt.ts
  - packages/fichas/src/extraer.ts
  - packages/fichas/src/extraer.test.ts
  - packages/fichas/src/mock-provider.ts
  - packages/fichas/src/golden/golden-set.ts
  - packages/fichas/src/golden/golden-set.test.ts
  - packages/fichas/src/slice.e2e.test.ts
  - packages/tramitacion/src/parse-senado-tramitacion.ts
  - packages/llm/src/providers/gemini-embeddings.ts
  - supabase/migrations/0011_fichas_embeddings.sql
  - supabase/tests/0011_fichas_embeddings.test.sql
autonomous: false
requirements: [SEM-01, SEM-02, SEM-03]
must_haves:
  truths:
    - "El golden set de extracción corre como gate de CI mockeado y BLOQUEA si la fidelidad literal baja del umbral (precisión ≥ 0.95)"
    - "FichaSchema valida idea_matriz nullable + cuerpos_legales[] y rechaza estructura inválida"
    - "La migración 0011 aplica en el Supabase LOCAL: proyecto_ficha, proyecto_embedding(vector(768)), índice HNSW vector_cosine_ops y RPC match_proyectos existen"
    - "El RPC match_proyectos tiene grant execute a anon y las dos tablas RLS public-read + grant select"
    - "parseSenadoTramitacion extrae link_mensaje_mocion del <descripcion>"
    - "GeminiEmbeddingProvider.embed acepta taskType opcional sin romper callers existentes"
    - "slice.e2e.test.ts está en RED por imports ausentes (diana walking-skeleton)"
  artifacts:
    - path: "packages/fichas/src/model.ts"
      provides: "FichaSchema + CuerpoLegalSchema (contrato de salida LLM)"
      contains: "FichaSchema"
    - path: "packages/fichas/src/golden/golden-set.test.ts"
      provides: "Gate de CI de fidelidad literal (mock + LIVE gated)"
      contains: "PRECISION_MIN"
    - path: "supabase/migrations/0011_fichas_embeddings.sql"
      provides: "Tablas fichas/embedding + HNSW + RPC + RLS"
      contains: "match_proyectos"
  key_links:
    - from: "packages/fichas/src/extraer.ts"
      to: "DeepSeekProvider.complete"
      via: "import @obs/llm + FichaSchema"
      pattern: "complete\\(.*FichaSchema"
    - from: "supabase/migrations/0011_fichas_embeddings.sql"
      to: "anon role"
      via: "grant execute on function match_proyectos"
      pattern: "grant execute on function match_proyectos"
---

<objective>
Establecer la fundación del paquete `@obs/fichas` y la base de datos de la fase, con el **gate de calidad de extracción** (golden set, flag P7 de STATE) como pieza central de validación, y dejar la **rebanada E2E en RED** como diana walking-skeleton que las olas 2 y 3 vuelven verde.

Esta ola entrega los **contratos** (FichaSchema, prompt restrictivo, RPC SQL, extensiones de proveedor/parser) que las dos olas siguientes consumen, más la migración 0011 aplicada al Supabase LOCAL.

Purpose: Sellar el guardarraíl riesgo existencial #2 (extracción literal, nunca interpretativa) antes de comprometer el prompt, y darle a los ejecutores de las olas 2/3 los contratos en mano (no exploración).
Output: Paquete `@obs/fichas` scaffold + FichaSchema + prompt + extraer + golden gate + migración 0011 aplicada en LOCAL + parser/provider extendidos + slice E2E en RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-RESEARCH.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-PATTERNS.md
@.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-VALIDATION.md

<interfaces>
<!-- Contratos extraídos del codebase. El ejecutor usa estos directamente, sin explorar. -->

From packages/llm/src/providers/deepseek.ts (consumir, NO reimplementar):
```
export class DeepSeekProvider implements LLMProvider {
  async complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T>;
}
// CompletionRequest: { system: string; user: string; sensitivity: "publico"|...; temperature?: number }
// El provider ya hace: assertSensitivityAllowed, prefijo system estable (prompt-cache),
// response_format json_object, y parseAndValidate (repair loop). NO hagas safeParse propio.
```

From packages/llm/src/providers/gemini-embeddings.ts (EXTENDER, ver Task 4):
```
export const EMBEDDING_MODEL = "gemini-embedding-001";
export const EMBEDDING_DIMS = 768;
export const EMBEDDING_VERSION = "v1";
export class GeminiEmbeddingProvider implements EmbeddingProvider {
  async embed(texts: string[]): Promise<EmbeddingResult[]>;  // hoy: sin taskType
}
// EmbeddingResult: { vector: number[]; model: string; dims: number; version: string }
```

From packages/adjudication/src/golden/golden-set.test.ts (espejar la estructura del gate):
```
const PRECISION_MIN = 0.95;  const RECALL_MIN = 0.8;
function mockDelGolden(set): LLMProvider  // RECHAZA claves duplicadas (choque revienta el test)
// Bloque CI (default, sin red, mock) + meta-test (fp alcanzable) + bloque LIVE gated por env.
```

From supabase/migrations/0008_tramitacion.sql (espejar RLS public-read):
```
alter table <t> enable row level security;
create policy <t>_public_read on <t> for select to anon using (true);
grant select on <t> to anon;   -- el privilegio Y la policy deben coincidir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @obs/fichas + FichaSchema + prompt restrictivo + slice E2E en RED</name>
  <files>packages/fichas/package.json, packages/fichas/tsconfig.json, packages/fichas/vitest.config.ts, packages/fichas/src/index.ts, packages/fichas/src/model.ts, packages/fichas/src/model.test.ts, packages/fichas/src/prompt.ts, packages/fichas/src/slice.e2e.test.ts</files>
  <read_first>
    - packages/tramitacion/package.json, packages/tramitacion/tsconfig.json, packages/tramitacion/vitest.config.ts (scaffold analog — copiar nombres de scripts, deps workspace @obs/llm/@obs/ingest/@obs/core, vitest ^3)
    - packages/tramitacion/src/index.ts (barrel: bloques export/export type con comentarios de sección)
    - packages/adjudication/src/prompt.ts (AdjudicacionSchema lines 26-42 + SYSTEM_ADJUDICACION lines 44-91 + construirPromptAdjudicacion lines 66-91 — espejo exacto del prompt restrictivo)
    - packages/tramitacion/src/slice.e2e.test.ts (forma de la rebanada E2E en RED por imports ausentes)
    - 07-RESEARCH.md §"Code Examples" (FichaSchema/CuerpoLegalSchema target) y §"Pattern 1" (texto SYSTEM_EXTRACCION)
  </read_first>
  <behavior>
    - Test model.test.ts: FichaSchema.parse acepta { idea_matriz: "cita textual", cuerpos_legales: [{norma:"Ley N° 19.628", articulos:["artículo 4"]}] }
    - Test: FichaSchema.parse acepta idea_matriz: null (degradación honesta es first-class)
    - Test: FichaSchema rechaza idea_matriz > 4000 chars y cuerpos_legales > 100 items
    - Test: cuerpos_legales por defecto [] cuando ausente
    - slice.e2e.test.ts: importa correrPipeline (Task de ola 2) y buscarProyectos-equivalente → RED por imports ausentes
  </behavior>
  <action>
    Crear `packages/fichas/` espejando el scaffold de `packages/tramitacion` (package.json name `@obs/fichas`, type module, scripts test/typecheck, deps workspace `@obs/llm`/`@obs/ingest`/`@obs/core`/`@obs/tramitacion`, devDep vitest). En `model.ts` definir `CuerpoLegalSchema` (norma string.max(200), articulos array(string.max(50)).max(50).default([])) y `FichaSchema` (idea_matriz string.max(4000).nullable(), cuerpos_legales array(CuerpoLegalSchema).max(100).default([])) + tipo `Ficha = z.infer`. En `prompt.ts` exportar `SYSTEM_EXTRACCION` (string Español estable para prompt-cache, reglas literales per SEM-02 / guardarraíl #2: idea_matriz = cita textual o null, NUNCA paráfrasis/resumen/inferencia/conexión de hechos; cuerpos_legales = solo normas citadas textualmente) y `construirPromptExtraccion(textoFuente, proyecto)` (espeja construirPromptAdjudicacion). `index.ts` barrel exporta FichaSchema/Ficha + SYSTEM_EXTRACCION/construirPromptExtraccion. `slice.e2e.test.ts` en RED importando los símbolos de ola 2/3 aún ausentes. Ejecutar `pnpm install` para registrar el workspace package.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/fichas test -t "model" && pnpm --filter @obs/fichas typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/fichas test -t "model"` pasa (FichaSchema contract verde)
    - `grep -v '^//' packages/fichas/src/prompt.ts | grep -c "NUNCA"` ≥ 1 (regla literal presente)
    - slice.e2e.test.ts existe y falla por imports ausentes (RED esperado, NO se cuenta como fallo de la tarea)
    - `@obs/fichas` resuelve en el workspace (pnpm typecheck no reporta "cannot find module @obs/fichas")
  </acceptance_criteria>
  <done>FichaSchema + prompt restrictivo + barrel existen y tipan; slice E2E en RED; package registrado en el workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extraer.ts (DeepSeek + zod gate) + Golden set + Gate de CI BLOQUEANTE (flag P7)</name>
  <files>packages/fichas/src/extraer.ts, packages/fichas/src/extraer.test.ts, packages/fichas/src/mock-provider.ts, packages/fichas/src/golden/golden-set.ts, packages/fichas/src/golden/golden-set.test.ts</files>
  <read_first>
    - packages/llm/src/providers/deepseek.ts lines 59-101 (contrato complete<T>(req, schema); ya hace gate + repair)
    - packages/adjudication/src/golden/golden-set.ts (CasoGolden interface lines 36-55, GOLDEN_SET, GOLDEN_SET_ADVERSARIO, evaluarGolden, ID_CASO_ADVERSARIO)
    - packages/adjudication/src/golden/golden-set.test.ts (entire — estructura de dos bloques: CI mock + meta-test fp + LIVE gated; mockDelGolden RECHAZA claves duplicadas)
    - packages/adjudication/src/mock-provider.ts (MockMiniMaxProvider — patrón de mock keyed-by-input)
    - 07-RESEARCH.md §"Pattern 4" (métrica: F1 cuerpos legales + substring literal idea matriz, caso adversarial) y §"Pitfall 6"
  </read_first>
  <behavior>
    - Test extraer.test.ts (mock provider, sin red): extraer(texto, proyecto) devuelve Ficha validada por FichaSchema; pasa sensitivity "publico"
    - Test: cuando el mock devuelve idea_matriz fabricada que NO es substring del texto, el gate lo marca como fallo de fidelidad
    - Golden gate (mock CI): precision ≥ 0.95 sobre cuerpos legales citados; un cuerpo legal fabricado (no en el texto) = false positive que baja precision
    - Golden gate: idea_matriz extraída debe ser substring literal del texto fuente (normalizando whitespace/acentos) OR null cuando expected null
    - Meta-test: el branch de false-positive es alcanzable (gate no es teatro)
    - ≥1 caso adversarial (texto sin idea matriz explícita) donde un modelo ingenuo alucinaría → expected null
  </behavior>
  <action>
    `extraer.ts`: función `extraer(textoFuente, proyecto, deepseek)` que llama `deepseek.complete({ system: SYSTEM_EXTRACCION, user: construirPromptExtraccion(textoFuente, proyecto), sensitivity: "publico" }, FichaSchema)`. NO hagas safeParse/repair propio (el provider lo hace, per SEM-02). `mock-provider.ts`: un `MockDeepSeekProvider` keyed por texto fuente que devuelve el `llmEsperado` del caso (espeja MockMiniMaxProvider). `golden/golden-set.ts`: interface `CasoGolden` (id, categoria, textoFuente, proyecto, llmEsperado, expected: { idea_matriz_substring: string|null, cuerpos: {norma,articulos}[] }), 15-20 casos anotados a mano sobre texto legal real + ≥1 caso adversarial; `evaluarGolden` que computa precision/recall (F1 cuerpos por norma+articulos; substring literal normalizado para idea_matriz). `golden/golden-set.test.ts`: bloque CI mock (PRECISION_MIN=0.95, RECALL_MIN=0.80, `expect(precision).toBeGreaterThanOrEqual(PRECISION_MIN)` BLOQUEA), meta-test fp-alcanzable, bloque LIVE gated por `process.env.FICHAS_GOLDEN_LIVE === "1"` instanciando DeepSeekProvider real (skip default). Reusar fixtures de texto íntegro real para los casos del golden set.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/fichas test -t "extraer" && pnpm --filter @obs/fichas test -t "golden"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/fichas test -t "golden"` pasa con el gate mockeado (precision ≥ 0.95)
    - El test NO depende de red ni de DEEPSEEK_API_KEY (CI siempre mockeado — mirror Pitfall 5 de identidad)
    - El golden set tiene ≥15 casos y ≥1 caso adversarial (`expected.idea_matriz_substring === null`)
    - El meta-test prueba que un fp baja la precisión (gate no tautológico)
    - `grep -c "FICHAS_GOLDEN_LIVE" packages/fichas/src/golden/golden-set.test.ts` ≥ 1 (bloque LIVE gated existe)
  </acceptance_criteria>
  <done>Extracción vía provider + zod gate; golden set ≥15 casos con adversarial; gate de CI mockeado que bloquea si precisión < 0.95; bloque LIVE gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender parser Senado (link_mensaje_mocion) + Gemini taskType (asimétrico)</name>
  <files>packages/tramitacion/src/parse-senado-tramitacion.ts, packages/llm/src/providers/gemini-embeddings.ts</files>
  <read_first>
    - packages/tramitacion/src/parse-senado-tramitacion.ts (lines 57-99: extracción de <descripcion>; helper txt() lines 26-36 — NO lee link_mensaje_mocion hoy)
    - packages/tramitacion/src/model.ts (ProyectoSchema — para añadir el campo o devolverlo sidecar)
    - packages/tramitacion/src/parse-senado-tramitacion.test.ts + test/fixtures/senado-tramitacion.xml (el fixture YA contiene <link_mensaje_mocion>, verificado)
    - packages/llm/src/providers/gemini-embeddings.ts (lines 67-90: body de batchEmbedContents SIN taskType; guards lines 116-120; l2normalize line 123)
    - 07-RESEARCH.md §"Pattern 3" + Assumption A1 (taskType per-request vs embedContentConfig — flag de smoke LIVE)
  </read_first>
  <behavior>
    - Test parse-senado-tramitacion.test.ts: el resultado expone link_mensaje_mocion extraído del fixture (no null cuando el XML lo trae)
    - Test: link_mensaje_mocion es null cuando el <descripcion> no lo trae (degradación)
    - Test gemini-embeddings: embed(texts) sin taskType sigue funcionando idéntico (callers existentes intactos)
    - Test: embed(texts, "RETRIEVAL_DOCUMENT") incluye taskType en cada request del body; embed(texts, "RETRIEVAL_QUERY") idem
    - Test: el guard de dim-mismatch y l2normalize permanecen sin cambios (verde con fetch mockeado)
  </behavior>
  <action>
    En `parse-senado-tramitacion.ts` añadir extracción de `link_mensaje_mocion` vía `txt(desc.link_mensaje_mocion)` en el bloque de `<descripcion>`; exponerlo en el resultado (añadir a ProyectoSchema en model.ts como campo nullable, o devolverlo como sidecar `{ proyecto, eventos, linkMensajeMocion }` — elegir lo de menor blast-radius para no romper writer/reconciliación existentes; documentar la elección). En `gemini-embeddings.ts` extender la firma a `embed(texts: string[], taskType?: "RETRIEVAL_DOCUMENT" | "RETRIEVAL_QUERY")` y añadir `...(taskType ? { taskType } : {})` a cada item de `requests` (additivo; callers sin taskType siguen igual). Mantener guard de dims (lines 116-120) y l2normalize intactos. NO ejecutar embed masivo aquí; el smoke LIVE del taskType (FICHAS_EMBED_LIVE) lo corre la ola 2 antes del backfill (Assumption A1).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/tramitacion test -t "senado-tramitacion" && pnpm --filter @obs/llm test -t "embed"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/tramitacion test -t "senado-tramitacion"` verde incl. nueva aserción de link_mensaje_mocion
    - `pnpm --filter @obs/llm test` verde (callers existentes de embed() sin regresión)
    - `grep -c "taskType" packages/llm/src/providers/gemini-embeddings.ts` ≥ 1
    - `grep -c "link_mensaje_mocion" packages/tramitacion/src/parse-senado-tramitacion.ts` ≥ 1
  </acceptance_criteria>
  <done>El parser extrae el link primario de texto íntegro (SEM-01); el provider de embeddings soporta embedding asimétrico (SEM-03) sin romper callers.</done>
</task>

<task type="auto">
  <name>Task 4: Migración 0011 (proyecto_ficha + proyecto_embedding + HNSW + RPC match_proyectos + RLS) + pgTAP</name>
  <files>supabase/migrations/0011_fichas_embeddings.sql, supabase/tests/0011_fichas_embeddings.test.sql</files>
  <read_first>
    - supabase/migrations/0008_tramitacion.sql (tail: bloque RLS public-read + grant select — espejo verbatim; FK a proyecto(boletin))
    - supabase/migrations/0010_agenda.sql (RLS public-read pattern lines 93-115)
    - supabase/migrations/0001_extensions.sql (pgvector ya habilitado)
    - 07-RESEARCH.md §"Pattern 2" (HNSW DDL + RPC match_proyectos verbatim) y §"Pattern 5" (grant execute a anon — adición crítica) y §"Pitfall 2/3"
    - 07-PATTERNS.md §migración 0011 (provenance/version columns; ambas tablas 1:1 con proyecto)
  </read_first>
  <action>
    Escribir `0011_fichas_embeddings.sql` (con la herramienta Write, NUNCA heredoc). Tabla `proyecto_ficha` (boletin text PK references proyecto(boletin), idea_matriz text, cuerpos_legales jsonb not null default '[]', texto_r2_path text, estado text not null default 'pendiente' check estado in ('pendiente','embebido'), provenance inline origen/fecha_captura). Tabla `proyecto_embedding` (boletin text PK references proyecto(boletin), embedding vector(768) not null, embedding_model text not null, embedding_dims int not null, embedding_version text not null). Índice HNSW: `create index proyecto_embedding_hnsw on proyecto_embedding using hnsw (embedding vector_cosine_ops) with (m=16, ef_construction=64)`. RPC `match_proyectos(query_embedding vector(768), match_count int default 20, match_threshold float8 default 0.0, exclude_boletin text default null) returns table(boletin text, similarity float8) language sql stable` — `order by e.embedding <=> query_embedding` (distancia cruda ASC, usa HNSW per Pitfall 3), similarity = 1 - distance solo en SELECT/WHERE; self-exclusion `exclude_boletin is null or e.boletin <> exclude_boletin`. RLS: enable row level security + policy public_read for select to anon using(true) + grant select en ambas tablas + **grant execute on function match_proyectos(vector, int, float8, text) to anon** (adición crítica vs 0008/0010, Pitfall 2). `0011_fichas_embeddings.test.sql` (pgTAP): las 2 tablas existen, columna embedding es vector(768), índice HNSW existe, función match_proyectos existe y anon tiene execute, RLS habilitado + policies presentes. NO incluye datos personales (parlamentario intacto).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && grep -c "grant execute on function match_proyectos" supabase/migrations/0011_fichas_embeddings.sql && grep -c "vector_cosine_ops" supabase/migrations/0011_fichas_embeddings.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "grant execute on function match_proyectos" supabase/migrations/0011_fichas_embeddings.sql` == 1 (Pitfall 2 sellado)
    - `grep -c "order by e.embedding <=> query_embedding" supabase/migrations/0011_fichas_embeddings.sql` == 1 (Pitfall 3: orden por distancia cruda)
    - `grep -c "for select to anon using (true)" supabase/migrations/0011_fichas_embeddings.sql` == 2 (ambas tablas)
    - El archivo NO referencia parlamentario.rut ni email (sin datos personales)
    - pgTAP file existe con aserciones de tablas/índice/RPC/RLS
  </acceptance_criteria>
  <done>Migración 0011 escrita con tablas, HNSW, RPC con grant execute a anon, RLS public-read; pgTAP de la migración presente.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 [BLOCKING]: Aplicar migración 0011 al Supabase LOCAL</name>
  <what-built>La migración 0011 está escrita y validada por grep/pgTAP estático. Build/typecheck pasan SIN la migración aplicada (los tipos vienen de config generada, no del esquema vivo) — esto crea un falso-positivo de verificación. Aplicar al LOCAL es obligatorio antes de que la ola 2 escriba fichas/embeddings y la ola 3 invoque el RPC.</what-built>
  <how-to-verify>
    1. Asegúrate de que el Supabase LOCAL (Docker) está arriba (`supabase status` — puertos remapeados 544xx).
    2. Aplica la migración al LOCAL: `supabase db reset` (recrea desde migraciones 0001..0011) o `supabase migration up` contra la instancia local.
    3. Corre el pgTAP de 0011 contra el LOCAL para confirmar tablas/índice/RPC/RLS.
    4. Verifica que el RPC es invocable por anon (smoke: `select * from match_proyectos((select array_fill(0::float4, array[768])::vector), 5, 0.0, null)` no lanza permiso denegado con el rol anon).
    NOTA DE OPERADOR: el push REMOTO de DDL está BLOQUEADO (service key, no PAT de management — probado 2026-06-18). NO intentes `supabase db push` al remoto en esta tarea; el push remoto es un paso de operador separado (necesita DB password / PAT `sbp_`), fuera de autonomía.
  </how-to-verify>
  <resume-signal>Escribe "aplicada" cuando 0011 esté en el LOCAL y el pgTAP pase, o describe el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto legal público → DeepSeek | input no confiable (texto fuente) cruza al LLM; debe ser solo texto público, sin RUT/PII |
| migración → rol anon | el RPC y las tablas se exponen a anon (grant); no deben filtrar columnas no públicas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering/Spoofing | extraer.ts (prompt injection en texto legal → extracción fabricada) | mitigate | Prompt restrictivo literal (SYSTEM_EXTRACCION) + FichaSchema (zod) + gate golden de substring literal; la fidelidad literal es invariante verificada por el gate (Pitfall 6) |
| T-07-02 | Information Disclosure | extraer.ts (dato personal al LLM) | mitigate | sensitivity:"publico" pasa por assertSensitivityAllowed del provider; texto fuente es público (mensaje/moción), sin RUT |
| T-07-03 | Information Disclosure | migración 0011 (RPC anon filtra columnas no públicas) | mitigate | match_proyectos solo retorna (boletin, similarity); tablas nuevas no llevan datos personales; parlamentario intacto deny-by-default |
| T-07-04 | Elevation of Privilege | RPC sin grant execute → o grant de más | mitigate | grant execute solo a anon sobre la firma exacta; función security-invoker (lee solo tablas públicas) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No se instalan paquetes npm nuevos en esta ola (solo workspace + shadcn en ola 3); audit de RESEARCH §Package Legitimacy sin [ASSUMED]/[SUS]/[SLOP] |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas test` verde (model + extraer + golden gate)
- `pnpm --filter @obs/tramitacion test` y `pnpm --filter @obs/llm test` verdes (sin regresión por las extensiones)
- Migración 0011 aplicada en LOCAL (Task 5) con pgTAP verde
- slice.e2e.test.ts permanece en RED (diana de las olas 2/3)
</verification>

<success_criteria>
- El gate de extracción (golden, mock CI) bloquea si la precisión < 0.95 (flag P7 sellado)
- FichaSchema, SYSTEM_EXTRACCION, RPC match_proyectos, taskType y link_mensaje_mocion existen como contratos para las olas 2/3
- 0011 aplicada en el Supabase LOCAL; remote push diferido a operador
</success_criteria>

<output>
Create `.planning/phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md` when done
</output>
