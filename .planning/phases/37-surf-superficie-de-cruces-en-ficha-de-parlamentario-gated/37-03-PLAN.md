---
phase: 37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated
plan: 03
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - app/app/parlamentario/[id]/page.tsx
  - app/app/parlamentario/[id]/page.test.tsx
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "Con gate OFF (default) la <section id=\"cruces\"> está AUSENTE del HTML (nodo ausente, no oculto-con-CSS) y createServerSupabase NO se llama por la sección de cruces"
    - "Con gate ON la sección monta sin lanzar sobre un fixture normal del RPC"
    - "La <section id=\"cruces\" className=\"mt-12\"> es un carril hermano (sibling), NUNCA anidado en otra sección; el mt-12 es la frontera anti-insinuación"
    - "La página importa crucesPublicEnabled de @/lib/cruces-gate (NUNCA lee CRUCES_PUBLIC_ENABLED crudo)"
  artifacts:
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "Carril gated <section id=cruces> + CrucesSkeleton + import del gate"
      contains: "crucesPublicEnabled"
    - path: "app/app/parlamentario/[id]/page.test.tsx"
      provides: "Test de gate a nivel de sección: ausente (OFF) / presente (ON) vía mocks + renderToStaticMarkup"
      contains: "renderToStaticMarkup"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "@/lib/cruces-gate"
      via: "import crucesPublicEnabled + wrap de la <section>"
      pattern: "crucesPublicEnabled\\(process\\.env\\)"
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "CrucesSection"
      via: "Suspense fallback CrucesSkeleton"
      pattern: "CrucesSection"
---

<objective>
Cablear la `<section id="cruces" className="mt-12">` en `app/app/parlamentario/[id]/page.tsx` como carril hermano, envuelta ENTERA (heading incluido) en `crucesPublicEnabled(process.env)` (espejo del patrón MONEY), con Suspense + `CrucesSkeleton`, más el test NUEVO de gate a nivel de sección (ausente OFF / presente ON) vía mocks + `renderToStaticMarkup`.

Purpose: prueba de Candado B — con el gate OFF (default) el nodo entero está AUSENTE del HTML, NO oculto-con-CSS, y la DB no se toca. El flag ships OFF; encenderlo es Phase 39.

Output: edición de `app/app/parlamentario/[id]/page.tsx` + `app/app/parlamentario/[id]/page.test.tsx` (nuevo — no existía test de esta página).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-CONTEXT.md
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-RESEARCH.md
@app/app/parlamentario/[id]/page.tsx
@app/app/red/page.test.tsx

<interfaces>
<!-- Patrón MONEY a espejar (page.tsx lines 97–106): el gate envuelve la <section>
     ENTERA incluido el <h2>; OFF ⇒ nodo ausente, NO se depende de que el componente
     retorne null. -->

```tsx
{moneyPublicEnabled(process.env) && (
  <section id="dinero" className="mt-12">
    <h2 className="text-xl font-semibold mb-4">Contratos del Estado asociados al RUT</h2>
    <Suspense fallback={<ContratosSkeleton />}>
      <ContratosSection id={id} searchParams={sp} />
    </Suspense>
  </section>
)}
```

Disponibles desde 37-01 / 37-02:
- crucesPublicEnabled(env = process.env): boolean — @/lib/cruces-gate (server-only)
- CrucesSection({ id }: { id: string }) — @/components/cruces-de-parlamentario (async Server Component; SIN searchParams)

Scaffold del test (red/page.test.tsx): vi.mock de @/lib/<gate> + @/lib/supabase + import de renderToStaticMarkup de "react-dom/server"; mocks ANTES del import de la página. Diferencia clave: este test aserta AUSENCIA de `id="cruces"` (no `notFound()`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carril gated <section id="cruces"> + CrucesSkeleton en page.tsx</name>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (patrón MONEY gate-wraps-section lines 88–135; orden de carriles; LobbySkeleton line 244 como shape de referencia para CrucesSkeleton; imports de gates line 12)
    - app/lib/cruces-gate.ts (creado en 37-01 — firma de crucesPublicEnabled)
    - app/components/cruces-de-parlamentario.tsx (creado en 37-02 — firma de CrucesSection({ id }))
  </read_first>
  <files>app/app/parlamentario/[id]/page.tsx</files>
  <action>
    En `app/app/parlamentario/[id]/page.tsx`:
    1. Importar `import { crucesPublicEnabled } from "@/lib/cruces-gate";` y `import { CrucesSection } from "@/components/cruces-de-parlamentario";` (NUNCA leer `CRUCES_PUBLIC_ENABLED` crudo — chokepoint WR-02).
    2. Insertar la `<section id="cruces" className="mt-12">` como carril HERMANO (sibling), envuelta ENTERA en `{crucesPublicEnabled(process.env) && ( ... )}` (espejo EXACTO del bloque MONEY: el gate envuelve la `<section>` incluido el `<h2>`; OFF ⇒ nodo ausente, NO se depende de que `CrucesSection` retorne null). Posición (discreción CONTEXT): DESPUÉS de `#patrimonio` y ANTES de las secciones MONEY gated (`#dinero`/`#financiamiento`), para no leerse como un "score" pegado a `#lobby`. NUNCA anidar dentro de otra `<section>`.
    3. `<h2>` con el texto LOCKED EXACTO: "Cruces con sectores" (sin posesivo, sin juicio).
    4. `<Suspense fallback={<CrucesSkeleton />}>` envolviendo `<CrucesSection id={id} />` (SIN `searchParams` — la sección no pagina).
    5. Añadir `function CrucesSkeleton()` shape-matched a `CrucesView` (espejo de `LobbySkeleton`: línea de intro + ~3 filas), con `aria-hidden="true"`.
    NO añadir un bloque honest-state `!crucesPublicEnabled(...)` (no está en alcance — el patrón de "pendiente legal" es solo para MONEY). NO encender el flag. NO tocar `supabase/migrations/`. NO `grant`.
  </action>
  <verify>
    <automated>cd app && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` importa `crucesPublicEnabled` desde `@/lib/cruces-gate` y `CrucesSection` desde `@/components/cruces-de-parlamentario`
    - `page.tsx` NO contiene la subcadena cruda `process.env.CRUCES_PUBLIC_ENABLED` (siempre vía `crucesPublicEnabled(process.env)`)
    - `page.tsx` contiene `{crucesPublicEnabled(process.env) && (` envolviendo una `<section id="cruces" className="mt-12">` que incluye su `<h2>`
    - El `<h2>` de la sección dice exactamente "Cruces con sectores"
    - La `<section id="cruces">` NO está anidada dentro de otra `<section>` (es sibling con `mt-12`)
    - `page.tsx` define `CrucesSkeleton` y la usa como `Suspense fallback`
    - `cd app && npx tsc -b` exits 0
    - CERO archivos bajo `supabase/migrations/`; CERO `grant`
  </acceptance_criteria>
  <done>La sección de cruces es un carril hermano gated; OFF ⇒ el bloque entero (incl. heading) está ausente del JSX renderizado; la página enruta toda exposición vía `crucesPublicEnabled`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test de gate a nivel de sección (ausente OFF / presente ON)</name>
  <read_first>
    - app/app/red/page.test.tsx (scaffold a adaptar: vi.mock de gate + @/lib/supabase + import de renderToStaticMarkup; mocks ANTES del import de la página; beforeEach mockClear/mockReset)
    - app/app/parlamentario/[id]/page.tsx (cómo se invoca la página: params/searchParams son Promises Next 16; valida [id] contra PARLAMENTARIO_ID_RE)
    - app/lib/buscar.ts (PARLAMENTARIO_ID_RE — para elegir un id válido en el fixture, p.ej. "P00001")
  </read_first>
  <files>app/app/parlamentario/[id]/page.test.tsx</files>
  <behavior>
    - Gate OFF (default) → el HTML renderizado de la página NO contiene `id="cruces"` ni "Cruces con sectores" (nodo AUSENTE, no oculto-con-CSS).
    - Gate ON + RPC mock devuelve 1 fila normal → el HTML SÍ contiene "Cruces con sectores" y la `sector_etiqueta` del fixture; la página resuelve truthy sin lanzar (sin error de hidratación).
    - El test NO toca PROD/DB real: `@/lib/supabase` mockeado; el RPC de cruces solo se ejerce en el caso ON con un fixture.
  </behavior>
  <action>
    Crear `app/app/parlamentario/[id]/page.test.tsx` adaptando el scaffold de `app/app/red/page.test.tsx`:
    1. `vi.mock("@/lib/cruces-gate", ...)` con un `crucesEnabledMock` inyectable por test (default false).
    2. `vi.mock("@/lib/supabase", ...)` con un `rpcMock` + `createServerSupabaseMock` para PROBAR que con el gate OFF la sección de cruces no fuerza un RPC de cruces (modelo del red test que aserta `createServerSupabaseMock` no se llamó). Configurar el mock para tolerar los otros RPC de la página (header `parlamentario_publico`, etc.) — devolver fixtures mínimos o un mock que no lance, de modo que el test aísle el comportamiento de la sección de cruces.
    3. Importar `ParlamentarioPage` DESPUÉS de los mocks + `renderToStaticMarkup` de `react-dom/server`.
    4. Caso OFF: `crucesEnabledMock.mockReturnValue(false)`; renderizar la página (params/searchParams como Promises); aserta que el HTML NO contiene `id="cruces"` ni "Cruces con sectores" (la prueba load-bearing de Candado B). Aserta que el RPC `cruces_de_parlamentario` NO se invocó.
    5. Caso ON: `crucesEnabledMock.mockReturnValue(true)`; `rpcMock` devuelve, para `cruces_de_parlamentario`, `{ data: [<1 fila CruceSenalRpcRow con sector_etiqueta y evidencia.items[1]>], error: null }`; aserta que el HTML contiene "Cruces con sectores" y la `sector_etiqueta`, y que la promesa resuelve truthy sin lanzar.
    Usar un `[id]` que pase `PARLAMENTARIO_ID_RE` (p.ej. "P00001"). NO test contra DB real. NO encender el flag fuera del mock. NO tocar `supabase/migrations/`.
  </action>
  <verify>
    <automated>cd app && npx vitest run parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - `page.test.tsx` mockea `@/lib/cruces-gate` y `@/lib/supabase`, e importa `renderToStaticMarkup` de `react-dom/server`
    - Caso OFF: aserta que el HTML renderizado NO contiene `id="cruces"` NI la subcadena "Cruces con sectores"
    - Caso OFF: aserta que el RPC `cruces_de_parlamentario` NO se invocó
    - Caso ON: aserta que el HTML contiene "Cruces con sectores" y la `sector_etiqueta` del fixture, y que la página resuelve sin lanzar
    - `cd app && npx vitest run parlamentario` exits 0
    - `cd app && npx vitest run` (suite completa) exits 0
    - CERO archivos bajo `supabase/migrations/`; el test NO ejecuta contra DB real
  </acceptance_criteria>
  <done>El test prueba por comportamiento que el gate OFF deja el nodo `#cruces` AUSENTE del HTML (Candado B) y que ON lo monta sin lanzar. La suite completa queda verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate (`crucesPublicEnabled`) → DOM público | con OFF la sección sensible NO debe existir en el HTML; con ON expone datos que esperan firma legal (Phase 39) |
| página → RPC `cruces_de_parlamentario` | el RPC no tiene grant a anon (Candado A) → en prod el path ON es inalcanzable hasta Phase 39; el test lo ejerce solo con mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Information Disclosure | sección expuesta prematuramente (oculta-con-CSS en vez de ausente) | mitigate | Gate envuelve la `<section>` ENTERA (espejo MONEY); test de sección-ausente aserta que el HTML NO contiene `id="cruces"` con gate OFF |
| T-37-08 | Information Disclosure | leer `CRUCES_PUBLIC_ENABLED` crudo fuera del chokepoint | mitigate | Import único de `crucesPublicEnabled` de `@/lib/cruces-gate`; acceptance veta `process.env.CRUCES_PUBLIC_ENABLED` crudo en page.tsx |
| T-37-03 | Elevation of Privilege | agente encendiendo el flag / grant del RPC a anon | accept (OUT OF SCOPE) | Phase 39 (firma humana exclusiva). Este plan ships el flag OFF, CERO grant, CERO migración |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (vitest/RTL/react-dom ya presentes) |
</threat_model>

<verification>
- `cd app && npx tsc -b` limpio (page.tsx cableada).
- `cd app && npx vitest run parlamentario` verde (sección ausente OFF + presente ON).
- `cd app && npx vitest run` suite completa verde (gate de fase antes de verify-work).
- Inspección: page.tsx importa `crucesPublicEnabled` del chokepoint; la `<section id="cruces">` es sibling `mt-12` envuelta entera por el gate.
- CERO DDL, CERO grant, CERO flip de flag, CERO `.env` que encienda.
</verification>

<success_criteria>
La ficha del parlamentario monta `<section id="cruces">` como carril hermano gated detrás de `crucesPublicEnabled` (default OFF), con Suspense + skeleton; el test prueba que OFF ⇒ nodo ausente del HTML y ON ⇒ sección presente sin lanzar; la suite completa queda verde y el flag ships OFF.
</success_criteria>

<output>
Create `.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-03-SUMMARY.md` when done
</output>
