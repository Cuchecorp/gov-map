---
phase: 37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/cruces-gate.ts
  - app/lib/cruces-gate.test.ts
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "El flag de presentación crucesPublicEnabled nace OFF (fail-closed): solo el literal \"true\" lo enciende"
    - "El flag es server-only (nunca viaja al bundle del navegador) y la var NO lleva prefijo NEXT_PUBLIC_"
    - "La ausencia de la var ES el default seguro (OFF), no un error que se lance"
  artifacts:
    - path: "app/lib/cruces-gate.ts"
      provides: "crucesPublicEnabled(env): boolean — Candado B del doble candado de exposición de cruces"
      contains: "export function crucesPublicEnabled"
      min_lines: 10
    - path: "app/lib/cruces-gate.test.ts"
      provides: "Tabla de verdad del gate (mirror de money-gate.test.ts)"
      contains: "crucesPublicEnabled"
  key_links:
    - from: "app/lib/cruces-gate.ts"
      to: "process.env.CRUCES_PUBLIC_ENABLED"
      via: "comparación estricta === \"true\""
      pattern: "CRUCES_PUBLIC_ENABLED.*===.*\"true\""
---

<objective>
Construir el gate de presentación `crucesPublicEnabled` (Candado B del doble candado de exposición de cruces), espejo byte-a-byte de `app/lib/money-gate.ts` / `app/lib/net-gate.ts`, con su test de tabla de verdad. Nace OFF (fail-closed). NO se enciende — encenderlo es Phase 39 (firma humana exclusiva).

Purpose: aísla la decisión de exposición de la sección de cruces en un chokepoint server-only único (WR-02), de modo que la página (37-03) nunca lea `CRUCES_PUBLIC_ENABLED` crudo.

Output: `app/lib/cruces-gate.ts` (nuevo) + `app/lib/cruces-gate.test.ts` (nuevo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-CONTEXT.md
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-RESEARCH.md

<interfaces>
<!-- Patrón EXACTO a espejar. El archivo nuevo difiere SOLO en: nombre de función,
     nombre de la var de entorno, y el docstring (referencia a cruces / Phase 39 /
     migraciones 0039-0040 / doble candado A+B). -->

From app/lib/money-gate.ts (estructura verbatim a copiar):
```typescript
import "server-only";

/** ...docstring... */
export function moneyPublicEnabled(
  env: Record<string, string | undefined> = process.env,
): boolean {
  return env.MONEY_PUBLIC_ENABLED === "true";
}
```

From app/lib/money-gate.test.ts (5 casos a espejar verbatim):
```typescript
import { describe, expect, it } from "vitest";
import { moneyPublicEnabled } from "./money-gate";
// 5 it(): {} -> false ; "false" -> false ; "1" -> false ; "TRUE" -> false ; "true" -> true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate crucesPublicEnabled + su tabla de verdad (espejo byte-a-byte de money-gate)</name>
  <read_first>
    - app/lib/money-gate.ts (estructura EXACTA a copiar: import server-only en línea 1, firma, comparación === "true")
    - app/lib/net-gate.ts (segundo espejo del mismo patrón doble-candado)
    - app/lib/money-gate.test.ts (5 casos de la tabla de verdad a espejar verbatim)
  </read_first>
  <files>app/lib/cruces-gate.ts, app/lib/cruces-gate.test.ts</files>
  <behavior>
    - `crucesPublicEnabled({})` (var ausente) → `false` (default fail-closed)
    - `crucesPublicEnabled({ CRUCES_PUBLIC_ENABLED: "false" })` → `false`
    - `crucesPublicEnabled({ CRUCES_PUBLIC_ENABLED: "1" })` → `false` (sin truthiness laxa)
    - `crucesPublicEnabled({ CRUCES_PUBLIC_ENABLED: "TRUE" })` → `false` (case-sensitive: solo literal "true")
    - `crucesPublicEnabled({ CRUCES_PUBLIC_ENABLED: "true" })` → `true` (único valor que enciende)
  </behavior>
  <action>
    Crear `app/lib/cruces-gate.ts` como espejo byte-a-byte de `app/lib/money-gate.ts`. Diferencias EXACTAS y ÚNICAS respecto al espejo:
    1. `import "server-only";` en la línea 1 (idéntico).
    2. Nombre de función: `crucesPublicEnabled` (en vez de `moneyPublicEnabled`).
    3. Firma idéntica: `(env: Record<string, string | undefined> = process.env): boolean`.
    4. Cuerpo: `return env.CRUCES_PUBLIC_ENABLED === "true";` (comparación estricta al literal `"true"`; NUNCA `Boolean(env.X)` ni truthiness laxa — dejaría pasar `"false"`).
    5. Docstring: adaptar el de money-gate a cruces — dejar EXPLÍCITO que (a) es **Candado B** (presentación) del doble candado de exposición de cruces; (b) **Candado A** es el RPC `cruces_de_parlamentario` SIN grant a anon + RLS deny-by-default sobre `cruce_senal` (migraciones 0039/0040, ya en PROD); (c) nace OFF (fail-closed): solo `"true"` enciende, la ausencia ES el default seguro (NO un error que se lance); (d) `import "server-only"` + var sin prefijo `NEXT_PUBLIC_` garantizan que nunca llega al bundle del navegador; (e) `env` inyectable (default `process.env`) para testear sin tocar el global; (f) ENCENDER requiere `signoff: approved` (Phase 39) — un agente NUNCA flipea este flag (CHOKEPOINT WR-02: el consumidor único es la página 37-03 vía `crucesPublicEnabled(process.env)`, NUNCA leyendo `CRUCES_PUBLIC_ENABLED` crudo).
    Crear `app/lib/cruces-gate.test.ts` como espejo de `app/lib/money-gate.test.ts`: 5 casos `it()` cubriendo la tabla de verdad del bloque <behavior>, importando `crucesPublicEnabled` desde `./cruces-gate`.
    NO encender el flag. NO añadir el flag a ningún `.env`. NO tocar `supabase/migrations/`. NO añadir prefijo `NEXT_PUBLIC_`.
  </action>
  <verify>
    <automated>cd app && npx vitest run cruces-gate</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/cruces-gate.ts` contiene `import "server-only";` como primera línea
    - `app/lib/cruces-gate.ts` contiene `export function crucesPublicEnabled`
    - `app/lib/cruces-gate.ts` retorna `env.CRUCES_PUBLIC_ENABLED === "true"` (comparación estricta al literal, NO `Boolean(...)` ni `==`)
    - `app/lib/cruces-gate.ts` NO contiene la subcadena `NEXT_PUBLIC_`
    - `app/lib/cruces-gate.test.ts` aserta los 5 casos: `{}`/`"false"`/`"1"`/`"TRUE"` → false; `"true"` → true
    - `cd app && npx vitest run cruces-gate` exits 0
    - CERO archivos nuevos/editados bajo `supabase/migrations/`
    - CERO `.env` modificado para encender `CRUCES_PUBLIC_ENABLED`
  </acceptance_criteria>
  <done>El gate existe, es server-only, fail-closed, y los 5 casos de la tabla de verdad pasan. El flag permanece OFF (sin consumidor todavía — 37-03 lo cablea).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env (operador/CI) → render server | `CRUCES_PUBLIC_ENABLED` decide si la sección sensible se expone; el gate es el único punto de decisión |
| render server → bundle navegador | el flag NUNCA debe cruzar al cliente (sin `NEXT_PUBLIC_` + `import "server-only"`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information Disclosure | `crucesPublicEnabled` truthiness laxa | mitigate | Comparación estricta `=== "true"`; `"false"`/`"1"`/`"TRUE"`/ausente → false (test de tabla de verdad lo prueba) |
| T-37-02 | Information Disclosure | flag filtrado al bundle del navegador | mitigate | `import "server-only"` línea 1 + var sin prefijo `NEXT_PUBLIC_` (acceptance lo verifica) |
| T-37-03 | Elevation of Privilege | agente encendiendo el flag / grant del RPC | accept (OUT OF SCOPE) | Encender es Phase 39 (firma humana exclusiva). Este plan deja el flag OFF y CERO grant; el docstring lo declara LOCKED |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (vitest/RTL ya presentes); no aplica audit de legitimidad |
</threat_model>

<verification>
- `cd app && npx vitest run cruces-gate` verde (5/5 casos).
- Inspección: `app/lib/cruces-gate.ts` es estructuralmente idéntico a `money-gate.ts` salvo nombre de función, var y docstring.
- CERO DDL, CERO grant, CERO flip de flag, CERO `.env` que encienda.
</verification>

<success_criteria>
El gate `crucesPublicEnabled` existe como espejo fail-closed de `money-gate.ts`, server-only, sin `NEXT_PUBLIC_`, con su tabla de verdad verde, y el flag ships OFF.
</success_criteria>

<output>
Create `.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-01-SUMMARY.md` when done
</output>
