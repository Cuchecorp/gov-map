---
phase: 37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/types.ts
  - app/components/cruces-de-parlamentario.tsx
  - app/components/cruces-de-parlamentario.test.tsx
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "CrucesView renderiza señales factuales (conteo neutro + etiqueta de sector) sin verbo causal, afinidad, score, ranking ni adjetivo de juicio"
    - "Cada item de evidencia trae un ProvenanceBadge con enlace a item.enlace_fuente (FND-08: trazable a la fuente original)"
    - "La contraparte es el nombre CRUDO + IdentityMarker (identidad no verificada), NUNCA enlazada, NUNCA un RUT"
    - "Cero cruces → empty honesto factual; NUNCA se lee como limpio/impecable/transparente"
    - "CrucesSection async lee el RPC cruces_de_parlamentario; un error real de DB/red lanza (no degrada a 'sin cruces')"
  artifacts:
    - path: "app/lib/types.ts"
      provides: "Tipos CruceSenalRpcRow / CruceEvidencia / CruceEvidenciaItem (forma del RPC 0040)"
      contains: "CruceSenalRpcRow"
    - path: "app/components/cruces-de-parlamentario.tsx"
      provides: "CrucesView puro + CrucesSection Server Component"
      contains: "export function CrucesView"
      min_lines: 60
    - path: "app/components/cruces-de-parlamentario.test.tsx"
      provides: "Tests RTL: empty-honesto + provenance + identidad + negative-match anti-insinuación (PROHIBIDO + PATRON_RUT) + conteo neutro"
      contains: "PROHIBIDO"
  key_links:
    - from: "app/components/cruces-de-parlamentario.tsx"
      to: "cruces_de_parlamentario"
      via: "sb.rpc en CrucesSection"
      pattern: "rpc\\(\\s*\"cruces_de_parlamentario\""
    - from: "app/components/cruces-de-parlamentario.tsx"
      to: "ProvenanceBadge"
      via: "sourceUrl = item.enlace_fuente"
      pattern: "enlace_fuente"
---

<objective>
Construir el componente `cruces-de-parlamentario.tsx` (`CrucesView` PURO + `CrucesSection` async Server Component), espejo de `lobby-de-parlamentario.tsx` adaptado a la forma del RPC `cruces_de_parlamentario`, más los tipos nuevos del RPC en `types.ts`, más el test RTL (empty-honesto + provenance + identidad + negative-match anti-insinuación + conteo neutro).

Purpose: materializa la superficie factual de cruces parlamentario↔sector con provenance inline y la frontera anti-insinuación §9.1, lista para que la página (37-03) la monte detrás del gate. La sección NO se enciende; el path del RPC es inalcanzable en prod hasta Phase 39 (gate OFF + sin grant a anon).

Output: edición de `app/lib/types.ts` + `app/components/cruces-de-parlamentario.tsx` (nuevo) + `app/components/cruces-de-parlamentario.test.tsx` (nuevo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-CONTEXT.md
@.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-RESEARCH.md
@app/components/lobby-de-parlamentario.tsx
@app/components/lobby-de-parlamentario.test.tsx

<interfaces>
<!-- Contrato del RPC (0040, ya en PROD — NO modificar, NO planear DDL). 5 columnas
     nombradas; el evidencia.items[] NO trae fecha_captura ni origen (divergencia
     clave vs lobby — ver Pitfall 1 de RESEARCH). -->

RPC cruces_de_parlamentario(p_id text) → filas:
  sector_id text, sector_etiqueta text, tipo_senal text (hoy solo 'lobby_sector'),
  conteo int, evidencia jsonb
  -- order by conteo desc, sector_id asc

evidencia jsonb = { conteo: int, items: [ { tipo, fecha, contraparte_nombre_crudo, audiencia_id, enlace_fuente } ] }
  -- items NO traen fecha_captura ni origen.

Tipos a AÑADIR a app/lib/types.ts (mirror de LobbyAudienciaRpcRow @ types.ts:243):
```typescript
export interface CruceSenalRpcRow {
  sector_id: string;
  sector_etiqueta: string;
  tipo_senal: string;          // hoy solo 'lobby_sector'; degradar honesto a otros
  conteo: number;
  evidencia: CruceEvidencia;
}
export interface CruceEvidencia {
  conteo: number;
  items: CruceEvidenciaItem[];
}
export interface CruceEvidenciaItem {
  tipo: string;                       // 'reunion' hoy
  fecha: string | null;               // ISO date de la audiencia
  contraparte_nombre_crudo: string;   // CRUDO (D-10), nunca normalizado → IdentityMarker
  audiencia_id: string;
  enlace_fuente: string | null;       // → ProvenanceBadge.sourceUrl
}
```

Primitivos reusados (verbatim):
- ProvenanceBadge({ capturedAt: Date|null, sourceName: string, sourceUrl: string|null }) — app/components/provenance-badge.tsx
- IdentityMarker() — aria-label="identidad no verificada" — app/components/identity-marker.tsx
- sourceLabel(origen: string|null): string — types.ts:517 ; sourceLabel("lobby") === "Ley del Lobby"
- fechaCorta(d: Date): string — app/lib/format.ts:17
- createServerSupabase() — app/lib/supabase.ts (server-only anon client)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos del RPC de cruces en types.ts</name>
  <read_first>
    - app/lib/types.ts (líneas 243–303: LobbyAudienciaRpcRow/LobbyContraparteRow/LobbyAudienciaRow a espejar; convención de comentarios PII-safe)
    - .planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-RESEARCH.md (§Data Contract: forma EXACTA del jsonb)
  </read_first>
  <files>app/lib/types.ts</files>
  <action>
    Añadir a `app/lib/types.ts` las interfaces `CruceSenalRpcRow`, `CruceEvidencia` y `CruceEvidenciaItem` con la forma EXACTA del bloque <interfaces> (mirror de `LobbyAudienciaRpcRow`). Comentario PII-safe LOCKED: la fila CRUDA del RPC solo proyecta `sector_id`/`sector_etiqueta` (catálogo público) + `tipo_senal` + `conteo` + `evidencia`; NUNCA `rut`/`donante_id`/`partido`. `contraparte_nombre_crudo` es CRUDO (D-10), nunca normalizado → siempre va con `IdentityMarker`. `enlace_fuente` alimenta `ProvenanceBadge.sourceUrl`. NO tocar tipos existentes salvo añadir las 3 interfaces nuevas.
  </action>
  <verify>
    <automated>cd app && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/types.ts` contiene `export interface CruceSenalRpcRow` con campos `sector_id`, `sector_etiqueta`, `tipo_senal`, `conteo`, `evidencia`
    - `app/lib/types.ts` contiene `export interface CruceEvidenciaItem` con campos `tipo`, `fecha`, `contraparte_nombre_crudo`, `audiencia_id`, `enlace_fuente`
    - `CruceEvidenciaItem` NO contiene los campos `fecha_captura` ni `origen` (no existen en el RPC — Pitfall 1)
    - `cd app && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Los 3 tipos del RPC existen y typecheckean; la forma coincide con el jsonb de 0039/0040.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CrucesView (puro) + CrucesSection (Server Component) + test RTL</name>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx (espejo MÁS cercano: ContraparteCruda lines 72–95, LobbyView estados honestos lines 97–240, LobbySection async RPC lines 284–345, bloque gate de contenido §9.1 lines 19–45)
    - app/components/lobby-de-parlamentario.test.tsx (espejo del test: carril aislado §9.1 lines 44–68, contraparte cruda + identidad + sin-RUT lines 70–101, estados honestos lines 103–134, ProvenanceBadge por fila lines 136–152, GATE §9.1 negative-match con la constante PROHIBIDO lines 154–182)
    - app/components/provenance-badge.tsx (firma de props: capturedAt/sourceName/sourceUrl; tolera nulls)
    - app/components/identity-marker.tsx (aria-label "identidad no verificada")
  </read_first>
  <files>app/components/cruces-de-parlamentario.tsx, app/components/cruces-de-parlamentario.test.tsx</files>
  <behavior>
    - Cero cruces (`cruces: []`) → copy factual tipo "No se registran cruces de sector para este parlamentario con los datos actuales."; `queryByText(/limpio|impecable|sin compromisos|transparente|no se reúne/i)` es null.
    - Por señal (fila del RPC, `tipo_senal === 'lobby_sector'`) → encabezado factual con conteo neutro + etiqueta de sector, p.ej. matchea `/\d+ reuniones con gestores del sector/i`; sin verbo causal/score/afinidad.
    - Por cada item de `evidencia.items[]` → `getAllByText(/fuente oficial ↗/i).length === items.length` (un ProvenanceBadge por item).
    - Contraparte → `getByText(contraparte_nombre_crudo)` presente; `getAllByLabelText("identidad no verificada").length > 0`; ningún `link` contiene el nombre; `textContent` NO matchea `PATRON_RUT = /\b\d{1,2}\.?\d{3}\.?\d{3}-[\dkK]\b/`.
    - Carril aislado → con ≥2 señales, `textContent` NO matchea la constante `PROHIBIDO` (mirror de lobby) ni `/voto|votación|votacion|boletín|boletin|a favor|en contra/i`; ningún `href` matchea `^/proyecto/` ni `^/parlamentario/`.
    - `tipo_senal` desconocido (futuro) → degrada honesto: renderiza conteo neutro + etiqueta SIN verbo de reunión fabricado, sin lanzar.
  </behavior>
  <action>
    Crear `app/components/cruces-de-parlamentario.tsx` como espejo de `lobby-de-parlamentario.tsx`. SIN `"use client"`. Estructura:
    1. `CrucesViewData` = `{ id: string; cruces: CruceSenalRpcRow[] }` (sin paginación — discreción: renderizar todas las señales).
    2. `ContraparteCruda`-equivalente: render del `item.contraparte_nombre_crudo` VERBATIM como texto + `<IdentityMarker/>`, NUNCA un enlace, NUNCA un RUT (espejo de `ContraparteCruda` de lobby, sin `representado`/`tipo` si la evidencia no los trae).
    3. `export function CrucesView({ data })` PURO: línea de intro honesta factual; si `cruces.length === 0` → empty honesto (copy factual, NUNCA "limpio/transparente"); si hay señales → por cada fila un encabezado factual LOCKED con conteo neutro + `sector_etiqueta`. Wording cuando `tipo_senal === 'lobby_sector'`: "{conteo} reuniones con gestores del sector {sector_etiqueta}" (sin verbo causal, sin score, sin afinidad, sin ranking). Para `tipo_senal` distinto: degradar honesto (conteo neutro + etiqueta, sin verbo de reunión fabricado). Por cada `evidencia.items[]`: nombre crudo + `IdentityMarker` + `ProvenanceBadge` con `sourceUrl = item.enlace_fuente`, `capturedAt = item.fecha ? new Date(item.fecha) : null`, `sourceName = sourceLabel("lobby")` (Pitfall 1: NO existe `fecha_captura`/`origen` por item; guardar contra `fecha`/`enlace_fuente` null — Pitfall 2; `ProvenanceBadge` ya null-checkea internamente). NO compartir un `<article>/<Card>/<li>/<tr>` con un voto/reunión/boletín/declaración: cada señal es su propio bloque, conteo neutro como único agregado.
    4. `export async function CrucesSection({ id }: { id: string })`: `const sb = createServerSupabase()`; `const { data, error } = await sb.rpc("cruces_de_parlamentario", { p_id: id })`; `if (error) throw new Error(...)` (#34: error real de DB/red NUNCA se degrada a "sin cruces"); `const cruces = (data as CruceSenalRpcRow[] | null) ?? []`; `return <CrucesView data={{ id, cruces }} />`. SIN `searchParams` (sin paginación).
    Crear `app/components/cruces-de-parlamentario.test.tsx` como espejo de `lobby-de-parlamentario.test.tsx`, testeando SOLO `CrucesView` puro con fixtures. Incluir INLINE la constante `PROHIBIDO` (mirror verbatim de lobby-de-parlamentario.test.tsx line 176–177) y `PATRON_RUT = /\b\d{1,2}\.?\d{3}\.?\d{3}-[\dkK]\b/`. Cubrir todos los casos del bloque <behavior>. NO hay linter de vocabulario compartido — la valla §9.1 se satisface con esta negative-match inline (convención del repo).
    NO tocar `supabase/migrations/`. NO `grant`. NO leer `CRUCES_PUBLIC_ENABLED`. NO encender nada. NO añadir test de live-DB/PROD.
  </action>
  <verify>
    <automated>cd app && npx vitest run cruces-de-parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/cruces-de-parlamentario.tsx` contiene `export function CrucesView` y `export async function CrucesSection`
    - `app/components/cruces-de-parlamentario.tsx` NO contiene `"use client"`
    - `CrucesSection` llama `sb.rpc("cruces_de_parlamentario", { p_id: id })` y hace `if (error) throw` (no degrada a "sin cruces")
    - `ProvenanceBadge` se invoca con `sourceUrl={...enlace_fuente}` y `sourceName={sourceLabel("lobby")}`; NO se referencia `fecha_captura` ni `.origen` de un item
    - La contraparte se renderiza como texto crudo + `IdentityMarker`, NUNCA dentro de un `<a>`/`<Link>`
    - `cruces-de-parlamentario.test.tsx` declara INLINE una constante `PROHIBIDO` y `PATRON_RUT`, y aserta que `textContent` NO matchea ninguna
    - Test aserta: empty-honesto (sin "limpio/transparente"); 1 ProvenanceBadge por item; `getAllByLabelText("identidad no verificada").length > 0`; conteo neutro factual; ningún `href` `^/proyecto/` ni `^/parlamentario/`
    - `cd app && npx vitest run cruces-de-parlamentario` exits 0
    - CERO archivos bajo `supabase/migrations/`; CERO `grant`; el string `CRUCES_PUBLIC_ENABLED` NO aparece en estos archivos
  </acceptance_criteria>
  <done>`CrucesView` puro renderiza señales factuales con provenance + identidad bajo la frontera anti-insinuación §9.1, los estados honestos y el negative-match pasan, y `CrucesSection` lee el RPC con manejo de error #34. La sección no se enciende.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC `cruces_de_parlamentario` → CrucesSection | datos sensibles de cruce (PII-safe por construcción: sin rut/donante_id/partido); el componente NO debe re-introducir PII |
| significado mostrado → ciudadano | el render NUNCA debe insinuar causalidad/afinidad/score (riesgo existencial #1 del producto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering (of meaning) | copy de `CrucesView` insinuando causalidad/afinidad/score | mitigate | Wording factual LOCKED (conteo neutro único agregado) + carril aislado (cada señal su propio bloque, nunca comparte `<article>/<li>/<tr>` con voto/reunión) + test negative-match `PROHIBIDO` + `/voto|boletín|.../` |
| T-37-05 | Information Disclosure | RUT / nombre-confirmado de contraparte filtrado al DOM | mitigate | Contraparte = nombre CRUDO + `IdentityMarker`, NUNCA enlazada; test `PATRON_RUT` aserta ausencia de RUT en el DOM |
| T-37-06 | Information Disclosure | error de DB degradado a "sin cruces" (falsa exoneración) | mitigate | `if (error) throw` en CrucesSection (#34); empty honesto SOLO ante cero filas reales |
| T-37-03 | Elevation of Privilege | agente encendiendo flag / grant RPC | accept (OUT OF SCOPE) | Phase 39. Este plan no lee el flag ni toca grants/migraciones |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (vitest/RTL/react-dom ya presentes) |
</threat_model>

<verification>
- `cd app && npx tsc -b` limpio (tipos nuevos).
- `cd app && npx vitest run cruces-de-parlamentario` verde (empty-honesto + provenance + identidad + PROHIBIDO/PATRON_RUT negative-match + conteo neutro).
- Inspección: `CrucesSection` lee el RPC con `if (error) throw`; ProvenanceBadge usa `enlace_fuente` + `sourceLabel("lobby")`, nunca `fecha_captura`/`origen`.
- CERO DDL, CERO grant, CERO lectura de `CRUCES_PUBLIC_ENABLED`.
</verification>

<success_criteria>
`CrucesView` (puro) + `CrucesSection` (Server Component) existen como espejo de lobby adaptado al RPC de cruces, con tipos nuevos en types.ts y test RTL verde que prueba empty-honesto, provenance por evidencia, identidad cruda sin RUT, conteo neutro y la valla anti-insinuación §9.1.
</success_criteria>

<output>
Create `.planning/phases/37-surf-superficie-de-cruces-en-ficha-de-parlamentario-gated/37-02-SUMMARY.md` when done
</output>
