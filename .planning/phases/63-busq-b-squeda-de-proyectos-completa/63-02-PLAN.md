---
phase: 63-busq-b-squeda-de-proyectos-completa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/src/parse-camara-legislativo.ts
  - packages/tramitacion/src/parse-camara-legislativo.test.ts
  - packages/tramitacion/src/connector-camara.ts
  - packages/tramitacion/src/run-enumerar-historico-cli.ts
autonomous: true
requirements: [BUSQ-02]
nyquist_compliant: true

must_haves:
  truths:
    - "enumerarProyectosXAnno(año) devuelve los NumeroBoletin de mociones+mensajes de ese año, vía WSLegislativo.asmx"
    - "La enumeración reusa la política LOCKED @obs/ingest (allowlist→robots→rate-limit 2-3s→fetch), no un fetch crudo"
    - "El parser valida el shape ProyectoLey[] con zod antes de devolver (contrato de fuente)"
    - "El CLI de enumeración es LOCAL one-shot, NO cableado a ningún cron YAML"
  artifacts:
    - path: "packages/tramitacion/src/parse-camara-legislativo.ts"
      provides: "parse ProyectoLey[] → NumeroBoletin[] (fast-xml-parser + zod)"
      contains: "NumeroBoletin"
    - path: "packages/tramitacion/src/connector-camara.ts"
      provides: "método enumerarProyectosXAnno"
      contains: "enumerarProyectosXAnno"
    - path: "packages/tramitacion/src/run-enumerar-historico-cli.ts"
      provides: "CLI LOCAL que enumera años → lista de boletines"
      contains: "run-enumerar-historico-cli"
    - path: "packages/tramitacion/src/parse-camara-legislativo.test.ts"
      provides: "test de parseo con fixture XML"
  key_links:
    - from: "packages/tramitacion/src/connector-camara.ts"
      to: "parse-camara-legislativo.parseCamaraLegislativo"
      via: "import + llamada tras fetch"
      pattern: "parseCamaraLegislativo"
    - from: "packages/tramitacion/src/connector-camara.ts"
      to: "assertAllowedUrl/robots/rateLimiter"
      via: "this.fetch (política LOCKED)"
      pattern: "this\\.fetch"
---

<objective>
Entregar el CÓDIGO de enumeración histórica (BUSQ-02): un método `enumerarProyectosXAnno` en `CamaraConnector` que consulta `WSLegislativo.asmx retornarMocionesXAnno` + `retornarMensajesXAnno` por año y devuelve los `NumeroBoletin`, más un parser con zod y un CLI LOCAL que enumera años → lista de boletines. NO corre el backfill (eso es P03).

Purpose: El corpus actual = solo el set incremental agenda∪votación∪tabla. Para ampliarlo hace falta ENUMERAR proyectos históricos. La fuente verificada (2026-07-10) es `WSLegislativo.asmx` (el WS de votaciones devuelve [] — anti-patrón conocido). La lista enumerada entra luego por el camino existente `run-tramitacion-prod-cli --boletines`.
Output: `parse-camara-legislativo.ts` (+test), método `enumerarProyectosXAnno`, `run-enumerar-historico-cli.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md

<interfaces>
<!-- Extraído del código; el executor usa esto directamente, sin explorar. -->

From packages/tramitacion/src/connector-camara.ts (líneas 59-124):
- `private async fetch(url): Promise<string>` — orden LOCKED: `assertAllowedUrl(url, allowlist)` → `robots.isAllowed(url)` → `rateLimiter.wait(host)` → `fetcher.get({url})` → `TextDecoder().decode`. REUSAR verbatim; NO hand-roll.
- `descubrirBoletines` / `fetchVotacionesBoletin` — analogs de método (dedup Set + best-effort por op).
- deps: `{ allowlist, robots, rateLimiter, fetcher }`.

From packages/tramitacion/src/parse-camara-votacion.ts (líneas 15-60):
- `XMLParser({ ignoreAttributes: false, parseTagValue: false })`
- helper `txt(v)` (maneja `#text` de fast-xml-parser) y `asArray<T>(v)` (colapsa nodo único a array).
- zod-validate-before-return (VotacionSchema).

WSLegislativo (verificado 2026-07-10):
- BASE: `https://opendata.camara.cl/camaradiputados/WServices/WSLegislativo.asmx`
- ops: `retornarMocionesXAnno?prmAnno={año}` + `retornarMensajesXAnno?prmAnno={año}`
- respuesta: `ProyectoLey[]` con `NumeroBoletin` (string "12345-67"), `Nombre`, `FechaIngreso`, `CamaraOrigen`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar el shape live de WSLegislativo (1 llamada) + fixture</name>
  <files>packages/tramitacion/src/parse-camara-legislativo.test.ts</files>
  <read_first>
    - packages/tramitacion/src/parse-camara-votacion.test.ts (estructura de test con fixture XML)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md (Pitfall 2, A1/A2: confirmar shape con UNA fetch antes de codificar)
    - CLAUDE.md (rate-limit 2-3s, UA identificatorio — obligatorio en la llamada live)
  </read_first>
  <action>Hacer UNA sola llamada live con `curl` (UA identificatorio, respetando rate-limit) a `retornarMocionesXAnno?prmAnno=2024` para capturar el XML real y confirmar el nombre exacto de los tags (`ProyectoLey`, `NumeroBoletin`, colección envolvente). Si devuelve 403/WAF → registrar en el SUMMARY y usar el patrón `createCurlTransport` de `@obs/agenda` como fallback (A2); NO martillar. Guardar un fragmento representativo del XML como fixture inline en `parse-camara-legislativo.test.ts` (o `__fixtures__/`). Escribir los tests en RED: (Test 1) parsea múltiples `ProyectoLey` → array de NumeroBoletin; (Test 2) colapso de nodo único a array; (Test 3) descarta boletines que no matchean `/^\d{3,6}-\d{1,3}$/`; (Test 4) zod rechaza shape inválido. Tests fallan porque el parser aún no existe.</action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion exec vitest run src/parse-camara-legislativo.test.ts</automated>
  </verify>
  <done>Fixture XML real capturado (o WAF registrado con fallback); tests en RED por parser ausente.</done>
  <acceptance_criteria>El archivo test existe con un fixture que contiene `<NumeroBoletin>`; el SUMMARY documenta el resultado de la llamada live (shape confirmado o WAF+fallback).</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parser parse-camara-legislativo.ts (fast-xml-parser + zod)</name>
  <files>packages/tramitacion/src/parse-camara-legislativo.ts</files>
  <read_first>
    - packages/tramitacion/src/parse-camara-votacion.ts (líneas 15-60: XMLParser config, txt, asArray, zod idiom — copiar verbatim)
    - packages/tramitacion/src/parse-camara-legislativo.test.ts (contrato de Task 1)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (bloque parse-camara-legislativo)
  </read_first>
  <action>Crear `parseCamaraLegislativo(xml: string): string[]` reusando el idiom exacto de `parse-camara-votacion.ts`: `XMLParser({ ignoreAttributes: false, parseTagValue: false })`, helpers `txt`/`asArray`. Definir `ProyectoLeySchema` (zod) con `NumeroBoletin` string validado contra `/^\d{3,6}-\d{1,3}$/` y `Nombre` string opcional; validar cada elemento antes de aceptarlo. Coercionar la colección `ProyectoLey` a array (nodo único → array). Devolver `NumeroBoletin[]` deduplicados que pasen el regex; descartar (no lanzar) los inválidos. Mantener parseable `Nombre`/`FechaIngreso`/`CamaraOrigen` si se quiere provenance, pero el retorno mínimo es la lista de boletines.</action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion exec vitest run src/parse-camara-legislativo.test.ts</automated>
  </verify>
  <done>Los tests de Task 1 pasan en GREEN.</done>
  <acceptance_criteria>`grep -n "z.object\|zod" packages/tramitacion/src/parse-camara-legislativo.ts` presente (gate de contrato); vitest del archivo verde.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: enumerarProyectosXAnno en el connector + CLI LOCAL de enumeración</name>
  <files>packages/tramitacion/src/connector-camara.ts, packages/tramitacion/src/run-enumerar-historico-cli.ts</files>
  <read_first>
    - packages/tramitacion/src/connector-camara.ts (líneas 59-124: this.fetch LOCKED, descubrirBoletines como analog de método)
    - packages/tramitacion/src/run-tramitacion-prod-cli.ts (loadEnv BOM-safe, flagValue, BOLETIN_RE, exit codes)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (bloques connector-camara y run-enumerar-historico-cli)
  </read_first>
  <action>Agregar `enumerarProyectosXAnno(anno: number): Promise<string[]>` a `CamaraConnector`: validar `Number.isInteger(anno) && 1990 <= anno <= 2100` (V5, no basura al WS gob); por cada op en `["retornarMocionesXAnno","retornarMensajesXAnno"]` construir la URL `${BASE_LEG}/${op}?prmAnno=${encodeURIComponent(String(anno))}` y llamar `this.fetch(url)` (política LOCKED); pasar el XML a `parseCamaraLegislativo`; unir en un Set y devolver `[...out]`. `BASE_LEG = "https://opendata.camara.cl/camaradiputados/WServices/WSLegislativo.asmx"`. Best-effort por op (un fallo de una op no aborta la otra), registrar el error.

  Crear `run-enumerar-historico-cli.ts` espejando `run-tramitacion-prod-cli.ts`: `loadEnv` BOM-safe con precedencia process.env; flags `--desde YYYY --hasta YYYY [--dry-run]` (validar rango de años); ensamblar el `CamaraConnector` con las deps @obs/ingest reales (allowlist/robots/rateLimiter/fetcher); loop de años → `enumerarProyectosXAnno` → dedup global → filtrar con `BOLETIN_RE` → imprimir la lista (una por línea o CSV) para que el operador la pipe a `run-tramitacion-prod-cli --boletines`. Guard `isMain` con regex del nombre propio. Exit code `process.exit(errores ? 1 : 0)` + `.catch` que loggea `err.message`. Secretos SOLO de env, nunca argv. NO cablear a ningún cron YAML (comentario explícito: histórico = one-shot LOCAL).</action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion exec vitest run</automated>
  </verify>
  <done>El connector expone enumerarProyectosXAnno reusando this.fetch; el CLI enumera años y emite la lista; suite del paquete verde.</done>
  <acceptance_criteria>`grep -n "this.fetch" packages/tramitacion/src/connector-camara.ts` dentro de enumerarProyectosXAnno; `grep -n "run-enumerar-historico-cli" packages/tramitacion/src/run-enumerar-historico-cli.ts` (guard isMain); NO existe referencia a este CLI en `.github/workflows/*.yml`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/connector → WSLegislativo (gov) | fetch saliente a fuente no confiable; entrada = año validado |
| WSLegislativo → parser | XML no confiable; validado con zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04 | Tampering | SSRF via URL construida | mitigate | `assertAllowedUrl` deny-by-default dentro de `this.fetch` (política LOCKED @obs/ingest) |
| T-63-05 | Denial of Service | ráfagas al WS gob | mitigate | `rateLimiter.wait(host)` 2-3s + robots + UA (política LOCKED); best-effort, backoff, jamás martillar |
| T-63-06 | Tampering | año/boletín malformado enviado al WS | mitigate | validación `Number.isInteger` + rango + `encodeURIComponent`; `BOLETIN_RE` filtra la salida |
| T-63-07 | Tampering | XML malicioso/malformado del WS | mitigate | `ProyectoLeySchema` zod valida antes de devolver; inválidos descartados, no lanzan |
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test` verde (incluye parse-camara-legislativo.test.ts).
- `grep -n "WSLegislativo" packages/tramitacion/src/connector-camara.ts` presente.
- Ningún workflow YAML referencia `run-enumerar-historico-cli`.
</verification>

<success_criteria>
- enumerarProyectosXAnno reusa la política LOCKED, devuelve NumeroBoletin de mociones+mensajes.
- parser con zod verde por fixture real.
- CLI LOCAL emite la lista de boletines; no está en ningún cron.
</success_criteria>

<output>
Create `.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-02-SUMMARY.md` when done
</output>
