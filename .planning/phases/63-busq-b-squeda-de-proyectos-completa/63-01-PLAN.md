---
phase: 63-busq-b-squeda-de-proyectos-completa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fichas/src/writer-supabase.ts
  - packages/fichas/src/seed-fichas.test.ts
  - packages/fichas/src/seed-fichas-cli.ts
  - scripts/verify-cobertura.sql
autonomous: true
requirements: [BUSQ-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Existe un paso de seed que crea una fila proyecto_ficha estado='pendiente' para todo proyecto sin ficha"
    - "El seed es idempotente: no re-abre filas 'embebido'/'error' existentes (ON CONFLICT DO NOTHING)"
    - "Sin SUPABASE_SECRET_KEY el CLI degrada a dry-run con aviso, jamás escribe silenciosamente"
    - "Un script SQL reporta la igualdad de counts (proyecto/ficha/embedding) o la diferencia por causa"
  artifacts:
    - path: "packages/fichas/src/writer-supabase.ts"
      provides: "método seedFichasPendientes() idempotente"
      contains: "seedFichasPendientes"
    - path: "packages/fichas/src/seed-fichas-cli.ts"
      provides: "CLI LOCAL del seed, dry-run gateado por service key"
      contains: "seed-fichas-cli"
    - path: "packages/fichas/src/seed-fichas.test.ts"
      provides: "test de idempotencia del seed"
    - path: "scripts/verify-cobertura.sql"
      provides: "conteos de cobertura para verificación/reporte"
      contains: "proyecto_ficha"
  key_links:
    - from: "packages/fichas/src/seed-fichas-cli.ts"
      to: "SupabaseFichasWriter.seedFichasPendientes"
      via: "instanciación en LIVE"
      pattern: "seedFichasPendientes"
    - from: "packages/fichas/src/writer-supabase.ts"
      to: "proyecto_ficha"
      via: "upsert ignoreDuplicates:true"
      pattern: "ignoreDuplicates:\\s*true"
---

<objective>
Cerrar la causa raíz de BUSQ-01: el pipeline de fichas es ciego a los 82 proyectos sin fila en `proyecto_ficha`. Este plan agrega el paso de **seed idempotente** (writer + CLI) que crea una fila `estado='pendiente'` para cada `proyecto` sin ficha, más el SQL de verificación de cobertura. NO corre el backfill (eso es P03) — este plan entrega el CÓDIGO y sus tests.

Purpose: `runIngest` upserta `proyecto` pero nunca `proyecto_ficha`; `leerPendientes` lee `proyecto_ficha`. Sin seed, los 82 son invisibles incluso con `--boletines`. Sin este paso, correr el pipeline reporta "0 procesados" (bug silencioso).
Output: `seedFichasPendientes()` en el writer, `seed-fichas-cli.ts`, `seed-fichas.test.ts`, `scripts/verify-cobertura.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md

<interfaces>
<!-- Extraído del código; el executor usa esto directamente, sin explorar. -->

From packages/fichas/src/writer-supabase.ts (líneas 20-107):
- `FichaRow { boletin, idea_matriz, cuerpos_legales, texto_r2_path, estado, origen, fecha_captura }`
- `FichaEstado = "pendiente" | "procesando" | "embebido" | "error"`
- `upsertFicha(filas)` usa `.upsert(lote, { onConflict: "boletin", ignoreDuplicates: false })` sobre `proyecto_ficha`, con `dedupePorClave` + `chunk` (CHUNK=500). El seed espeja esto pero con `ignoreDuplicates: true`.
- `marcarError` solo propaga `error.message` de PostgREST (nunca la service key) — misma disciplina para el seed.
- Constructor: `createClient(url, serviceKey, { auth: { persistSession: false, autoRefreshToken: false } })`.

From packages/fichas/src/pipeline-cli.ts (líneas 68-126):
- `parseArgs(argv)` + `FichasCliArgsError` (fail-fast por flag desconocido / valor vacío).
- `decidirDryRun({ serviceKey, dryRun })`: dry-run si `dryRun===true` O `serviceKey` vacío.
- Patrón env: `url = process.env.SUPABASE_URL ?? process.env.SUPABASE_API_URL`; `serviceKey = process.env.SUPABASE_SECRET_KEY`.
- Guard `isMain` con regex del nombre propio del archivo (MEMORY gotcha "dos entrypoints CLI").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test del seed idempotente (Wave 0)</name>
  <files>packages/fichas/src/seed-fichas.test.ts</files>
  <read_first>
    - packages/fichas/src/pipeline.test.ts (estructura de test del paquete: mock del SupabaseClient/writer)
    - packages/fichas/src/writer-supabase.ts (líneas 83-131: shape del writer, upsertFicha, dedupePorClave)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (sección writer-supabase seed)
  </read_first>
  <behavior>
    - Test 1: dado `proyecto`={A,B,C} y `proyecto_ficha`={A}, seedFichasPendientes crea filas SOLO para {B,C} con estado='pendiente', origen='fichas-seed'.
    - Test 2: idempotencia — segunda corrida con las mismas filas ya presentes crea 0 (upsert usa ignoreDuplicates:true / DO NOTHING).
    - Test 3: NO re-abre estado terminal — una fila 'embebido' o 'error' preexistente NUNCA se sobreescribe a 'pendiente' (verificar que el lote de upsert no la incluye).
    - Test 4: si no faltan filas (todos los proyectos ya tienen ficha), retorna { creados: 0 } sin llamar upsert.
  </behavior>
  <action>Crear el test espejo de `pipeline.test.ts`: mockear el `SupabaseClient` inyectado (`opts.client`) de modo que `from("proyecto").select` y `from("proyecto_ficha").select` devuelvan conjuntos controlados, y capturar los argumentos de `.upsert(...)`. Verificar el conjunto `faltantes`, el `estado='pendiente'`/`origen='fichas-seed'`, y que la opción de upsert sea `ignoreDuplicates: true`. El test corre RED (el método aún no existe).</action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/seed-fichas.test.ts</automated>
  </verify>
  <done>El test existe y falla en RED porque `seedFichasPendientes` aún no está implementado (o falla por método ausente).</done>
  <acceptance_criteria>`grep -n "ignoreDuplicates" packages/fichas/src/seed-fichas.test.ts` encuentra la aserción; el comando vitest corre y reporta fallo por método ausente (no error de sintaxis).</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar seedFichasPendientes() en el writer</name>
  <files>packages/fichas/src/writer-supabase.ts</files>
  <read_first>
    - packages/fichas/src/writer-supabase.ts (líneas 20-131: FichaRow, upsertFicha, dedupePorClave, chunk, marcarError)
    - packages/fichas/src/seed-fichas.test.ts (contrato definido en Task 1)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (bloque seedFichasPendientes)
  </read_first>
  <action>Agregar el método `seedFichasPendientes(): Promise<{ creados: number }>` a `SupabaseFichasWriter` reusando `dedupePorClave` + `chunk` del propio archivo. Dos pasos: (1) SELECT `boletin` de `proyecto` y de `proyecto_ficha`, computar `faltantes` = proyectos sin ficha vía Set; (2) para cada faltante armar un `FichaRow` con `estado:'pendiente'`, `origen:'fichas-seed'`, `idea_matriz:null`, `cuerpos_legales:[]`, `texto_r2_path:null`, `fecha_captura: new Date().toISOString()`, y upsertear con `.upsert(lote, { onConflict: "boletin", ignoreDuplicates: true })`. Propagar solo `error.message` (nunca la key). Retornar `{ creados: faltantes.length }`. Si `faltantes.length===0`, retornar `{ creados: 0 }` sin tocar la DB. NUNCA re-abrir filas terminales (ignoreDuplicates:true garantiza DO NOTHING).</action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/seed-fichas.test.ts</automated>
  </verify>
  <done>Los 4 tests de Task 1 pasan en GREEN.</done>
  <acceptance_criteria>`grep -n "ignoreDuplicates: true" packages/fichas/src/writer-supabase.ts` presente dentro de seedFichasPendientes; vitest del archivo verde.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CLI de seed (seed-fichas-cli.ts) + SQL de verificación</name>
  <files>packages/fichas/src/seed-fichas-cli.ts, scripts/verify-cobertura.sql</files>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts (líneas 68-126, 243-269: parseArgs, decidirDryRun, isMain guard, patrón env-driven)
    - packages/fichas/src/writer-supabase.ts (constructor + seedFichasPendientes de Task 2)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md (Code Examples: queries de cobertura)
  </read_first>
  <action>Crear `seed-fichas-cli.ts` espejando el esqueleto de `pipeline-cli.ts` pero sin providers/pipeline: solo flags `--dry-run` y `--service-key` (fail-fast en `--service-key` vacío, como el analog). Usar `decidirDryRun`; env `SUPABASE_URL ?? SUPABASE_API_URL` + `SUPABASE_SECRET_KEY`. En dry-run: NO instanciar writer, loggear "sin SUPABASE_SECRET_KEY → DRY-RUN (no toca DB)". En LIVE: `new SupabaseFichasWriter({ url, serviceKey })` → `seedFichasPendientes()` → loggear `creados`. Guard `isMain` con regex `/seed-fichas-cli\.(ts|js|mjs|cjs)$/`. Exit code 0 en éxito, 1 en error con `err.message`.

  Crear `scripts/verify-cobertura.sql` con los conteos de RESEARCH §Code Examples: `count(proyecto)`, `count(proyecto_ficha)`, `count(proyecto_embedding)`, fichas con `idea_matriz` no vacío, `select estado, count(*) from proyecto_ficha group by estado`, y `select embedding_version, count(*) from proyecto_embedding group by 1`. Comentar cada query con su significado (proyecto vs ficha vs embedding vs techo honesto).</action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run && node -e "process.argv[1]='x'" </automated>
  </verify>
  <done>El CLI existe, exporta `parseArgs`/`decidirDryRun` reutilizados o propios; corre en dry-run sin key sin escribir; el SQL existe con los 6 conteos.</done>
  <acceptance_criteria>`grep -n "seed-fichas-cli" packages/fichas/src/seed-fichas-cli.ts` (guard isMain) y `grep -c "count(" scripts/verify-cobertura.sql` >= 4; suite del paquete `@obs/fichas` verde.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI operador → Supabase | service key desde `.env`/process.env, nunca por argv |
| Writer → PostgREST | error.message propagado; jamás la key en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Information Disclosure | service key en logs/errores | mitigate | Solo `error.message` de PostgREST (patrón writer-supabase líneas 104-105); key desde env, nunca argv/log |
| T-63-02 | Tampering | seed re-abre estado terminal ('embebido'/'error') | mitigate | `ignoreDuplicates: true` (ON CONFLICT DO NOTHING); test dedicado (Task 1, Test 3) |
| T-63-03 | Elevation/Tampering | secreto por argv | mitigate | `--service-key` solo como override; fuente primaria = process.env (patrón pipeline-cli) |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas test` verde (incluye seed-fichas.test.ts).
- `grep -n "seedFichasPendientes" packages/fichas/src/writer-supabase.ts` presente.
- `scripts/verify-cobertura.sql` existe con los conteos de cobertura.
</verification>

<success_criteria>
- seedFichasPendientes() implementado, idempotente, testeado (4 tests verdes).
- seed-fichas-cli.ts corre en dry-run sin key; guard isMain correcto.
- verify-cobertura.sql entrega los conteos para la verificación de P03.
</success_criteria>

<output>
Create `.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-01-SUMMARY.md` when done
</output>
