---
phase: 16-money-agregaci-n-contratos-aportes-por-contraparte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0025_agregacion.sql
  - supabase/tests/0026_agregacion.test.sql
autonomous: false
requirements: [MONEY-05]
tags: [money, aggregation, rpc, pii, postgres, security-definer]

must_haves:
  truths:
    - "The RPC agregado_por_contraparte(text) exists, is security-definer, anon has EXECUTE, public does NOT."
    - "The RPC returns ONLY tipo_persona='juridica' contrapartes; persona-natural names never appear."
    - "The contratos facet groups by contrato.rut_proveedor; the aportes facet groups by aporte.donante_nombre — the RPC body never references contratista/donante, never projects donante_id/rut_donante/rut_proveedor as an output name."
    - "Counts + the underlying rows (each with origen/fecha/enlace) are returned per facet."
    - "Sub-maestras contratista and donante remain deny-by-default (RLS on, zero policies, anon no SELECT grant) after 0025."
  artifacts:
    - path: "supabase/migrations/0025_agregacion.sql"
      provides: "ALTER contrato ADD rut_proveedor (reconcile writer) + agregado_por_contraparte RPC + optional contrapartes_listado RPC; no new tables"
      contains: "create function public.agregado_por_contraparte"
    - path: "supabase/tests/0026_agregacion.test.sql"
      provides: "pgTAP: RPC existence/grants + functiondef PII introspection + sub-maestra deny-by-default regression"
      contains: "pg_get_functiondef"
  key_links:
    - from: "supabase/migrations/0025_agregacion.sql"
      to: "public.contrato.rut_proveedor"
      via: "GROUP BY on the rut_proveedor column the writer (writer-supabase.ts:54) already persists"
      pattern: "rut_proveedor"
    - from: "supabase/migrations/0025_agregacion.sql"
      to: "public.aporte.donante_nombre"
      via: "GROUP BY on the public fact-row donante name (no donante_id on the fact)"
      pattern: "donante_nombre"
---

<objective>
Ship `supabase/migrations/0025_agregacion.sql` — a security-definer aggregation RPC `agregado_por_contraparte(p_id text)` (plus an optional `contrapartes_listado()` listing RPC) that aggregates `contrato` and `aporte` by contraparte, **jurídica-only**, and `supabase/tests/0026_agregacion.test.sql` proving the PII invariants. This is the DB half of the Phase 16 vertical slice (MONEY-05): the only public path to contraparte data. No new tables, no connectors, no ingest.

Purpose: A citizen can ask "what State contracts and campaign donations are publicly registered to THIS company" and get counts + traced rows — without ever exposing a private individual's name or a donor RUT (Ley 21.719). The RPC is the PII chokepoint; the sub-maestras stay deny-by-default.

Output: migration `0025_agregacion.sql` + pgTAP `0026_agregacion.test.sql`. Remote apply + pgTAP run = OPERATOR CHECKPOINT (Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-money-agregaci-n-contratos-aportes-por-contraparte/16-CONTEXT.md
@.planning/phases/16-money-agregaci-n-contratos-aportes-por-contraparte/16-RESEARCH.md
@.planning/phases/16-money-agregaci-n-contratos-aportes-por-contraparte/16-PATTERNS.md
@supabase/migrations/0023_dinero.sql
@supabase/migrations/0024_servel.sql
@supabase/tests/0025_servel.test.sql

<interfaces>
<!-- LOAD-BEARING O1 RESOLUTION — verified from codebase, do NOT re-investigate -->

`contrato` fact table (0023_dinero.sql:57-87) — public-read columns the RPC may project:
  fuente_id, fecha_corte, codigo_orden, proveedor_nombre, tipo_persona ('natural'|'juridica'),
  organismo, nombre_orden, monto (text, today NULL), fecha_oc, origen, fecha_captura, enlace, licencia
  PK: (fuente_id, fecha_corte)

  CRITICAL: the committed 0023 migration does NOT define a `rut_proveedor` column on `contrato`,
  but the SHIPPED writer DOES persist it:
    packages/dinero/src/writer-supabase.ts:54 → `rut_proveedor: f.rutProveedor`
  i.e. the contratoRoot() upsert writes contrato.rut_proveedor. The migration and writer are
  out of sync. The honest fix (NOT a new table): 0025 ADDs the column so the RPC can GROUP BY it
  and the already-shipped writer's column lands somewhere real.

`aporte` fact table (0024_servel.sql:68-115) — public-read columns the RPC may project:
  fuente_id, fecha_corte, eleccion (NOT NULL), candidato_nombre_verbatim, donante_nombre,
  tipo_persona, monto (text), fecha_aporte, tipo_aporte, territorio, pacto, partido,
  origen, fecha_captura, enlace, licencia
  PK: (fuente_id, fecha_corte)

  CRITICAL: the `aporte` fact row has NO donante_id column. The writer (writer-supabase-servel.ts:46-68)
  writes donante_nombre + tipo_persona ONLY; donante_id lives solely on the deny-by-default `donante`
  sub-maestra. Therefore the aportes contraparte KEY is `donante_nombre` (the public fact name).

`contratista` sub-maestra (0023:103-127) — DENY-BY-DEFAULT: rut_proveedor PK, nombre (INTERNAL PII),
  tipo_persona, codigo_empresa. RLS on, zero policies, `revoke all ... from anon, authenticated`.
`donante` sub-maestra (0024:132-156) — DENY-BY-DEFAULT: donante_id PK, rut_donante (nullable, always NULL),
  nombre (INTERNAL PII), tipo_persona. RLS on, zero policies, revoke.

Shipped RPC skeletons to mirror EXACTLY:
  `contratos_de_parlamentario(p_id text)` (0023:149-167) — language sql stable security definer
     set search_path = '' ; revoke execute ... from public ; grant execute ... to anon.
  `aportes_de_parlamentario(p_id text)` (0024:180-199) — same clause + `order by ... eleccion desc`.

pgTAP introspection pattern to mirror (0025_servel.test.sql:111-133):
  has_function / prosecdef=true / has_function_privilege(anon, ..., execute)=true /
  not has_function_privilege(public, ..., execute) /
  pg_get_functiondef(...) not ilike '%rut_donante%'  (PII assertion)
  deny-by-default three-assert block (0025_servel.test.sql:89-99): relrowsecurity=true,
  pg_policies count=0, role_table_grants anon SELECT count=0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write pgTAP 0026_agregacion.test.sql (RED — invariants before the RPC exists)</name>
  <files>supabase/tests/0026_agregacion.test.sql</files>
  <behavior>
    The test file asserts (against an APPLIED schema) the MONEY-05 DB invariants. Author it FIRST so it
    fails until 0025 is written + applied (Nyquist: the test is the spec). Assertions:
    - has_function('public','agregado_por_contraparte',ARRAY['text'], '...existe').
    - prosecdef = true ('...es security definer').
    - has_function_privilege('anon','public.agregado_por_contraparte(text)','execute') = true.
    - not has_function_privilege('public','public.agregado_por_contraparte(text)','execute') (revoked).
    - PII functiondef introspection on pg_get_functiondef('public.agregado_por_contraparte(text)'::regprocedure):
        * ilike '%juridica%'         → body filters to persona jurídica.
        * not ilike '%rut_donante%'  → never projects donor RUT (Ley 21.719).
        * not ilike '%donante_id%'   → never projects the synthetic donor key.
        * not ilike '%contratista.nombre%' AND not ilike '%donante.nombre%' → never reads the
          deny-by-default sub-maestra PII name columns.
    - contrato now HAS a rut_proveedor column (reconciliation guard): count from information_schema.columns
      where table_name='contrato' and column_name='rut_proveedor' = 1.
    - Sub-maestra deny-by-default REGRESSION for BOTH contratista AND donante (mirror 0025_servel.test.sql:89-99):
        relrowsecurity=true (1 each), pg_policies count=0 (each), anon SELECT grant count=0 (each).
    Wrap in begin; select plan(N); ... select * from finish(); rollback;  with the exact N you author.
  </behavior>
  <action>Create supabase/tests/0026_agregacion.test.sql mirroring supabase/tests/0025_servel.test.sql structure (begin/plan/finish/rollback). Copy the RPC existence + security-definer + execute-privilege block verbatim, swapping the function name to agregado_por_contraparte. Copy the deny-by-default three-assert block for BOTH contratista and donante (the contratista block lives in 0024_dinero.test.sql; the donante block in 0025_servel.test.sql:89-99 — include both tables). Add the functiondef introspection asserts listed in <behavior>: one positive ilike '%juridica%' and four negative not-ilike guards (rut_donante, donante_id, contratista.nombre, donante.nombre). Add the contrato.rut_proveedor column-exists guard. File MUST be saved WITHOUT a BOM and without invisible unicode. NEVER write the string "CC BY 4.0" anywhere. Set plan(N) to the exact assertion count.</action>
  <verify>
    <automated>grep -c "agregado_por_contraparte" "supabase/tests/0026_agregacion.test.sql" | grep -qv '^0$' && grep -q "not ilike '%rut_donante%'" "supabase/tests/0026_agregacion.test.sql" && grep -q "not ilike '%donante_id%'" "supabase/tests/0026_agregacion.test.sql" && grep -q "ilike '%juridica%'" "supabase/tests/0026_agregacion.test.sql" && ! grep -q "CC BY 4.0" "supabase/tests/0026_agregacion.test.sql" && echo OK</automated>
  </verify>
  <done>0026_agregacion.test.sql exists with all RPC-existence, PII-functiondef, deny-by-default (contratista+donante), and rut_proveedor-column asserts; no "CC BY 4.0"; begin/plan/finish/rollback wrapper present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 0025_agregacion.sql (GREEN — column reconcile + jurídica-only aggregation RPC)</name>
  <files>supabase/migrations/0025_agregacion.sql</files>
  <behavior>
    The migration makes 0026's asserts pass. It contains, in order:
    (a) A doc-block header mirroring 0024_servel.sql:1-59 style: "última migración APLICADA es 0024; esta
        es la 0025"; the "APLICACION = CHECKPOINT DE OPERADOR" + BOM `--db-url` note; the GATE note
        (moneyPublicEnabled default OFF, candado B; this DDL is candado A); the "pgTAP usa el número
        libre 0026" note; the "no pg_cron en esta migración (matview diferida, MVP = RPC runtime)" note;
        and an O1-RESOLUTION note explaining the contrato.rut_proveedor reconciliation.
    (b) `alter table public.contrato add column if not exists rut_proveedor text;` — reconciles the
        committed 0023 schema with the already-shipped writer (writer-supabase.ts:54). NO new table.
        Optional: `create index if not exists contrato_rut_proveedor_idx on public.contrato (rut_proveedor);`
    (c) `create function public.agregado_por_contraparte(p_id text) returns table (...) language sql
        stable security definer set search_path = '' as $$ ... $$;` — a UNION of two facets:
        - CONTRATOS facet: from public.contrato where tipo_persona = 'juridica' and rut_proveedor =
          <id-without-'c:'-prefix>, grouped/projected to emit the company name (contrato.proveedor_nombre),
          the neutral COUNT, and the underlying rows. The function parses the p_id prefix: 'c:' → contratos
          facet keyed by rut_proveedor; 'd:' → aportes facet keyed by donante_nombre.
        - APORTES facet: from public.aporte where tipo_persona = 'juridica' and donante_nombre =
          <id-without-'d:'-prefix>, projecting aporte.donante_nombre, COUNT, rows (eleccion/candidato/
          monto/fecha_aporte/tipo_aporte/origen/fecha_captura/fecha_corte/enlace/licencia).
        The return shape carries a `facet` discriminator ('contrato'|'aporte'), the jurídica `contraparte_nombre`,
        `tipo_persona`, the per-facet `conteo`, and the underlying rows as jsonb (each row object MUST include
        origen, fecha_captura, fecha_corte, enlace, licencia + the facet's verbatim public fields). The body
        NEVER references public.contratista / public.donante, NEVER selects donante_id / rut_donante, NEVER
        projects a persona-natural name (the only names projected are the fact-row jurídica names, gated by
        the tipo_persona='juridica' WHERE). Montos stay VERBATIM text — no SUM, no ::numeric cast.
    (d) `revoke execute on function public.agregado_por_contraparte(text) from public;`
        `grant execute on function public.agregado_por_contraparte(text) to anon;`
    (e) OPTIONAL listing RPC `contrapartes_listado()` returning, jurídica-only, distinct contrapartes with
        their prefixed id ('c:'||rut_proveedor for contratos, 'd:'||donante_nombre for aportes), the
        contraparte name, facet, and a neutral count — same revoke-from-public + grant-to-anon. If you ship
        it, mirror the same security-definer clause; it does NOT change the gate (the page still 404s when OFF).
  </behavior>
  <action>Create supabase/migrations/0025_agregacion.sql. Mirror the doc-block + security-definer clause + revoke/grant pattern from 0023_dinero.sql:149-167 and 0024_servel.sql:180-199 EXACTLY (`language sql stable security definer set search_path = ''` — every table reference schema-qualified as public.contrato / public.aporte because search_path is empty). Add the `alter table public.contrato add column if not exists rut_proveedor text;` reconciliation (O1: the writer at writer-supabase.ts:54 already persists this column; the 0023 migration omitted it). Implement agregado_por_contraparte(p_id text) as a prefix-dispatched UNION ALL of the contratos facet (key rut_proveedor, name proveedor_nombre) and the aportes facet (key donante_nombre, name donante_nombre), each filtered `where tipo_persona = 'juridica'`, aggregating a neutral COUNT and bundling the underlying rows as jsonb_agg with per-row provenance (origen, fecha_captura, fecha_corte, enlace, licencia). Use substring/split to strip the 'c:'/'d:' prefix from p_id. Do NOT JOIN or reference public.contratista or public.donante anywhere. Do NOT cast monto to numeric or SUM it (monto is verbatim text, today NULL for contratos). Add the revoke-from-public + grant-to-anon on the exact `(text)` signature. Optionally add contrapartes_listado() (jurídica-only, prefixed ids, neutral count) with the same security-definer + revoke/grant. Do NOT add any policy or grant to contratista/donante. Do NOT register any cron.schedule. File saved WITHOUT BOM, no invisible unicode, NEVER the string "CC BY 4.0" (ChileCompra = "mención de la fuente"; SERVEL = "términos por verificar").</action>
  <verify>
    <automated>grep -q "add column if not exists rut_proveedor" "supabase/migrations/0025_agregacion.sql" && grep -q "security definer set search_path = ''" "supabase/migrations/0025_agregacion.sql" && grep -q "tipo_persona = 'juridica'" "supabase/migrations/0025_agregacion.sql" && grep -q "revoke execute on function public.agregado_por_contraparte(text) from public" "supabase/migrations/0025_agregacion.sql" && grep -q "grant execute on function public.agregado_por_contraparte(text) to anon" "supabase/migrations/0025_agregacion.sql" && ! grep -qE "public\.(contratista|donante)" "supabase/migrations/0025_agregacion.sql" && ! grep -qiE "sum\(|::numeric" "supabase/migrations/0025_agregacion.sql" && ! grep -q "CC BY 4.0" "supabase/migrations/0025_agregacion.sql" && echo OK</automated>
  </verify>
  <done>0025_agregacion.sql exists: adds contrato.rut_proveedor (if not exists), defines agregado_por_contraparte(text) as a jurídica-only prefix-dispatched aggregation that never references contratista/donante and never SUMs/casts monto, with revoke-from-public + grant-to-anon; no cron; no "CC BY 4.0".</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Migration 0025_agregacion.sql + pgTAP 0026_agregacion.test.sql. These are inert until applied to the remote Postgres (sa-east-1) — CI/typecheck do NOT execute DDL (false-positive, Pitfall 2). The agregado_por_contraparte RPC and the contrato.rut_proveedor column do not exist in production until the operator applies them.</what-built>
  <how-to-verify>
    Run these from git-bash (NOT PowerShell), extracting SUPABASE_DB_URL from .env while skipping the UTF-8 BOM (same path proven for 0018–0024):
    1. Apply the migration to the remote:
       `supabase db push --db-url "$SUPABASE_DB_URL"`
       (or `supabase migration up --db-url "$SUPABASE_DB_URL"`)
    2. Run the pgTAP against the APPLIED schema:
       `supabase test db --db-url "$SUPABASE_DB_URL"`
       Expect ALL asserts in 0026_agregacion.test.sql to PASS — especially:
         - agregado_por_contraparte exists + is security definer + anon EXECUTE + public NOT.
         - functiondef does NOT contain rut_donante / donante_id / contratista.nombre / donante.nombre.
         - functiondef DOES filter to juridica.
         - contratista AND donante still deny-by-default (RLS on, 0 policies, 0 anon SELECT grant).
         - contrato.rut_proveedor column now exists.
    3. Sanity-call (optional): `select * from public.agregado_por_contraparte('c:76123456-7');` returns 0+ rows, no error.
    Confirm there is NO regression: `select has_table_privilege('anon','public.contratista','select');` is false.
  </how-to-verify>
  <resume-signal>Type "applied" once 0026 pgTAP passes against the remote schema, or paste the failing asserts.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (browser/public) → RPC | Untrusted `p_id` crosses into the only public data path; the RPC is the PII chokepoint |
| RPC (security definer) → sub-maestras | Definer privilege could read deny-by-default PII; the body must never project it |
| operator → remote Postgres | DDL applied out-of-band (CI cannot prove it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | agregado_por_contraparte projecting a persona-natural name | mitigate | `where tipo_persona = 'juridica'` on the FACT row; body never references contratista/donante; pgTAP functiondef `not ilike '%contratista.nombre%'`/`%donante.nombre%` |
| T-16-02 | Information Disclosure | donor RUT / synthetic key leak | mitigate | RPC never selects rut_donante/donante_id (the aporte FACT has neither); pgTAP `not ilike '%rut_donante%'`/`%donante_id%` |
| T-16-03 | Elevation of Privilege | default-privilege grant loosening contratista/donante | mitigate | 0025 adds NO policy/grant to sub-maestras; pgTAP deny-by-default regression asserts both tables (RLS on, 0 policies, 0 anon SELECT) |
| T-16-04 | Tampering | SQL injection via p_id | mitigate | Parameterized RPC arg (`p_id text`); `set search_path = ''`; route-level regex guard in Plan 16-02 |
| T-16-05 | Information Disclosure | exposure before legal sign-off | accept | `moneyPublicEnabled()` default OFF gates the route (Plan 16-02); enabling is operator debt F13 — out of this plan's scope |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | Phase installs ZERO packages (RESEARCH §Package Legitimacy Audit: not applicable); no install surface |
</threat_model>

<verification>
- pgTAP `0026_agregacion.test.sql` passes against the APPLIED remote schema (operator checkpoint, Task 3).
- `agregado_por_contraparte(text)` exists, security definer, anon EXECUTE, public revoked.
- functiondef contains 'juridica' and contains NONE of: rut_donante, donante_id, contratista.nombre, donante.nombre.
- `contratista` and `donante` remain deny-by-default after 0025.
- `contrato.rut_proveedor` column exists (writer/migration reconciled).
</verification>

<success_criteria>
- MONEY-05 (DB half): aggregation RPC over contrato/aporte by contraparte, jurídica-only, rows carry source/fecha/enlace — verifiable by pgTAP, not convention.
- No persona-natural name / donor RUT / internal key is reachable through any public channel introduced by 0025.
- Sub-maestras unchanged (still deny-by-default).
- No "CC BY 4.0" string; ChileCompra/SERVEL attribution defaults untouched.
</success_criteria>

<output>
Create `.planning/phases/16-money-agregaci-n-contratos-aportes-por-contraparte/16-01-SUMMARY.md` when done. Record: the final RPC return shape, whether contrapartes_listado() was shipped, the exact addressing scheme (`c:<rut_proveedor>` / `d:<donante_nombre>`), and the O1 reconciliation (contrato.rut_proveedor added) for Plan 16-02 to consume.
</output>
