---
phase: 05-tramitaci-n-core-ficha-timeline-votaciones
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/tramitacion/src/reconciliar-camara.ts
  - packages/tramitacion/src/reconciliar-camara.test.ts
  - packages/tramitacion/src/reconciliar-senado.ts
  - packages/tramitacion/src/reconciliar-senado.test.ts
  - packages/tramitacion/src/index.ts
autonomous: true
requirements: [TRAM-06]
must_haves:
  truths:
    - "El voto-a-voto de la Cámara resuelve parlamentario_id por Diputado/Id contra la maestra de forma determinista"
    - "Un voto de Cámara con Id presente en la maestra queda confirmado/determinista"
    - "El voto-a-voto del Senado se reconcilia por nombre vía correrPipeline"
    - "Un voto del Senado solo lleva parlamentario_id si el resultado es determinista/confirmado; probable/revisión → null + mención cruda"
  artifacts:
    - path: "packages/tramitacion/src/reconciliar-camara.ts"
      provides: "reconciliarVotosCamara: voto-a-voto Cámara → Voto[] con parlamentario_id por Diputado/Id"
      contains: "export function reconciliarVotosCamara"
    - path: "packages/tramitacion/src/reconciliar-senado.ts"
      provides: "reconciliarVotosSenado: voto-por-nombre → correrPipeline → Voto[] (solo confirmado vincula)"
      contains: "export function reconciliarVotosSenado"
  key_links:
    - from: "packages/tramitacion/src/reconciliar-camara.ts"
      to: "parlamentario.id_diputado_camara"
      via: "lookup determinista por Diputado/Id"
      pattern: "id_diputado_camara"
    - from: "packages/tramitacion/src/reconciliar-senado.ts"
      to: "@obs/adjudication correrPipeline + @obs/core normalizarNombre"
      via: "MencionForanea por nombre → pipeline → solo determinista vincula"
      pattern: "correrPipeline|normalizarNombre"
---

<objective>
Reconciliar el voto-a-voto de ambas cámaras contra la maestra, respetando la guarda de identidad LOCKED (riesgo existencial #1). Cámara: cruce DETERMINISTA por `Diputado/Id` contra `parlamentario.id_diputado_camara` (sin LLM, sin riesgo). Senado: cruce por nombre vía `correrPipeline` (Fase 4); solo `determinista`/`confirmado` puebla `parlamentario_id`, todo lo demás deja `null` + conserva la mención cruda para mostrar con marca "identidad no verificada".

Purpose: Cierra el Test 4 del slice E2E y materializa la tabla puente `voto` para AMBAS cámaras (decisión ADDENDUM del usuario: incluir voto individual de Cámara ahora, cruzando por Id). Es donde el "cómo vota" se vincula a una persona sin afirmar identidades dudosas.
Output: Dos funciones de reconciliación que transforman los votos crudos (de la ola 2) en `Voto[]` listos para persistir, con `parlamentario_id`/`metodo`/`estado_vinculo` correctos según la guarda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-RESEARCH.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-CONTEXT.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-02-SUMMARY.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-03-SUMMARY.md

<interfaces>
<!-- Contratos de @obs/core, @obs/adjudication y el modelo de 05-01/05-02. Usar directo. -->

From @obs/core (nombre.ts):
```typescript
export function normalizarNombre(input: { libre?: string; nombres?; apellidoPaterno?; apellidoMaterno? }): {
  nombre_normalizado: string; clave_estricta: string; tokens: string[]; alias_capturados: string[];
};
// El formato de voto Senado "Coloma C., Juan Antonio" entra por { libre }.
```

From @obs/adjudication (index barrel):
```typescript
export interface MencionForanea {
  nombreOriginal: string; nombreNormalizado: string; tokens: string[];
  camara: Camara; periodo: string; region: string | null;
}
export interface PipelineWriter {
  upsertVinculo(v: FilaVinculo): Promise<number | null>;
  appendAudit(row: FilaAudit): Promise<void>;
  enqueueRevision(caso: CasoRevision): Promise<void>;
}
export type ResultadoPipeline =
  | { tipo: "determinista"; parlamentarioId: string }
  | { tipo: "no_confirmado"; razon: string }
  | { tipo: "probable"; parlamentarioId: string }
  | { tipo: "revision"; razones: string[] };
export function correrPipeline(
  mencion: MencionForanea, maestra: Parlamentario[], provider: LLMProvider, writer: PipelineWriter
): Promise<ResultadoPipeline>;
// GUARDA LOCKED: solo tipo==="determinista" debe poblar voto.parlamentario_id.
// "probable" NO vincula (auto-aceptar del LLM mapea a 'probable', nunca a la ficha pública).
```

From @obs/tramitacion model (05-01):
```typescript
export interface Voto { votacion_id; mencion_nombre; parlamentario_id: string|null;
  seleccion: 'si'|'no'|'abstencion'|'pareo'; metodo: 'determinista'|'llm'|'humano'|null;
  estado_vinculo: 'confirmado'|'probable'|'no_confirmado'|null }
```

From @obs/core (parlamentario.ts): `Parlamentario` con `id`, `id_diputado_camara: string|null`, `camara`, `periodo`.

Voto-a-voto crudo de la ola 2:
- Cámara (parseCamaraVotoDetalle): `{ diputadoId: string; opcion: 'si'|'no'; nombreCrudo: string }[]`
- Senado (parseSenadoVotacion): `{ mencionNombre: string; seleccion: 'si'|'no'|'abstencion'|'pareo' }[]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconciliación determinista de Cámara por Diputado/Id</name>
  <files>packages/tramitacion/src/reconciliar-camara.ts, packages/tramitacion/src/reconciliar-camara.test.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - Un voto con diputadoId que existe en maestra.id_diputado_camara → Voto con parlamentario_id = ese id, metodo='determinista', estado_vinculo='confirmado'.
    - Un voto con diputadoId ausente en la maestra → Voto con parlamentario_id=null, estado_vinculo='no_confirmado', conservando nombreCrudo en mencion_nombre.
    - seleccion se preserva (si/no del detalle de Cámara; abstencion si OpcionVoto lo indica).
  </behavior>
  <action>
**reconciliar-camara.ts** — `reconciliarVotosCamara(votosCrudos, votacionId, maestra): Voto[]`:
- Construir un índice `Map<string, Parlamentario>` por `id_diputado_camara` (saltando los null) UNA vez.
- Por cada voto crudo `{ diputadoId, opcion, nombreCrudo }`:
  - `const p = idx.get(String(diputadoId))`.
  - Si `p` existe: `{ votacion_id, mencion_nombre: nombreCrudo, parlamentario_id: p.id, seleccion: opcion, metodo: 'determinista', estado_vinculo: 'confirmado' }`. Este es el cruce por Id, MÁS fuerte que por nombre: sin LLM, sin riesgo de identidad (decisión ADDENDUM). No requiere `correrPipeline`.
  - Si `p` NO existe (Id no está en la maestra vigente, p.ej. diputado de periodo anterior): `parlamentario_id: null`, `metodo: null`, `estado_vinculo: 'no_confirmado'`, conservando `mencion_nombre: nombreCrudo` para display.
- Función pura, sin red ni DB (recibe la maestra ya cargada). Validar cada `Voto` con `VotoSchema`.
- Exportar desde `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run reconciliar-camara</automated>
  </verify>
  <done>reconciliar-camara verde: Id en maestra → confirmado/determinista con el id correcto; Id ausente → null + no_confirmado conservando la mención; seleccion preservada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliación del Senado por nombre vía correrPipeline (guarda de identidad)</name>
  <files>packages/tramitacion/src/reconciliar-senado.ts, packages/tramitacion/src/reconciliar-senado.test.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - Un voto cuyo nombre matchea determinísticamente a un senador único → parlamentario_id poblado, estado_vinculo='confirmado'.
    - Un voto que el pipeline resuelve como 'probable' (auto-aceptar LLM) → parlamentario_id=null, estado_vinculo='probable', mención cruda conservada (NO se vincula a la ficha — guarda LOCKED).
    - Un voto sin candidato / a revisión → parlamentario_id=null, estado_vinculo='no_confirmado'.
    - Cierra el Test 4 del slice E2E.
  </behavior>
  <action>
**reconciliar-senado.ts** — `reconciliarVotosSenado(votosCrudos, votacionId, maestra, provider, writer): Promise<Voto[]>`:
- Por cada voto crudo `{ mencionNombre, seleccion }`:
  - `const { nombre_normalizado, tokens } = normalizarNombre({ libre: mencionNombre.trim() })` (Pitfall 3: trim antes; el campo `libre` está diseñado para "Apellido P., Nombre").
  - Construir `MencionForanea`: `{ nombreOriginal: mencionNombre.trim(), nombreNormalizado: nombre_normalizado, tokens, camara: "senado", periodo: "senado-vigente-2026", region: null }`.
  - `const res = await correrPipeline(mencion, maestra, provider, writer)`.
  - Mapear el resultado a la guarda LOCKED:
    - `res.tipo === "determinista"` → `{ parlamentario_id: res.parlamentarioId, metodo: 'determinista', estado_vinculo: 'confirmado' }`.
    - `res.tipo === "probable"` → `{ parlamentario_id: null, metodo: 'llm', estado_vinculo: 'probable' }` (NUNCA vincula a la ficha pública; el nombre crudo se muestra con marca).
    - `res.tipo === "revision"` → `{ parlamentario_id: null, metodo: null, estado_vinculo: 'no_confirmado' }`.
    - `res.tipo === "no_confirmado"` → `{ parlamentario_id: null, metodo: null, estado_vinculo: 'no_confirmado' }`.
  - Construir `Voto { votacion_id: votacionId, mencion_nombre: mencionNombre.trim(), seleccion, ...mapeo }`. Validar con `VotoSchema`.
- IMPORTANTE: SOLO `determinista` puebla `parlamentario_id` en `voto` (aunque el pipeline escriba un vínculo 'probable' en `vinculo_identidad`, la capa pública NO lo refleja como vínculo). Esto es la guarda LOCKED de CONTEXT.
- `provider` y `writer` son inyectables (mock/espía en tests, MiniMax + RevisionWriter reales en la ola 4). En tests usar `MockMiniMaxProvider` de @obs/adjudication y un `PipelineWriter` espía in-memory (patrón 04-03). El provider LLM NO es obligatorio para los votos que resuelven determinísticamente (correrPipeline corta antes del LLM).
- Exportar desde `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run reconciliar-senado; pnpm -w typecheck</automated>
  </verify>
  <done>reconciliar-senado verde: determinista vincula; probable/revisión/no_confirmado → null + mención cruda; Test 4 del slice E2E verde; `slice.e2e.test.ts` completo verde; typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voto crudo (nombre/Id) → parlamentario_id (afirmación pública de identidad) | Riesgo existencial #1: atribuir un voto a la persona equivocada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering / falsa afirmación creíble | reconciliar-senado (voto por nombre) | mitigate | SOLO `tipo==="determinista"` puebla `voto.parlamentario_id`; `probable` (auto-aceptar LLM) NUNCA vincula a la ficha pública; resto → null + mención cruda. Guarda LOCKED de CONTEXT, sellada por `correrPipeline` (Fase 4). |
| T-05-07 | Tampering | reconciliar-camara (cruce por Id) | accept | El cruce por `Diputado/Id` contra `id_diputado_camara` es determinista y de bajo riesgo (Id es identificador oficial, no nombre); un Id ausente degrada a `no_confirmado` (fail-closed). Sin LLM. |
| T-05-08 | Information Disclosure | RUT al LLM en la reconciliación Senado | mitigate | `MencionForanea` por construcción NO transporta RUT (T-04-02); `correrPipeline` reusa `assertNoRutInLlmInput`. Heredado de Fase 4. |
| T-05-SC | Tampering | npm installs | mitigate | Sin paquetes nuevos. N/A. |
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test --run reconciliar-camara reconciliar-senado` verde.
- `slice.e2e.test.ts` COMPLETO verde (4 tests).
- `pnpm -w typecheck` exit 0.
</verification>

<success_criteria>
- Voto-a-voto de Cámara cruzado por Diputado/Id determinísticamente.
- Voto-a-voto de Senado reconciliado por nombre; guarda LOCKED respetada (solo confirmado vincula).
- Tabla puente `voto` poblable para ambas cámaras (lógica lista; la persistencia la cablea la ola 4).
</success_criteria>

<output>
Create `.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-03-SUMMARY.md` when done
</output>
