---
phase: 05-tramitaci-n-core-ficha-timeline-votaciones
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - packages/tramitacion/src/connector-camara.ts
  - packages/tramitacion/src/connector-camara.test.ts
  - packages/tramitacion/src/connector-senado.ts
  - packages/tramitacion/src/connector-senado.test.ts
  - packages/tramitacion/src/writer.ts
  - packages/tramitacion/src/writer.test.ts
  - packages/tramitacion/src/writer-supabase.ts
  - packages/tramitacion/src/ingest-run.ts
  - packages/tramitacion/src/ingest-cli.ts
  - packages/tramitacion/src/ingest-cli.test.ts
  - packages/tramitacion/src/index.ts
  - packages/tramitacion/package.json
autonomous: false
requirements: [TRAM-01, TRAM-02, TRAM-03, TRAM-09]
must_haves:
  truths:
    - "Los conectores fetchean Cámara/Senado reusando @obs/ingest (allowlist→robots→rateLimiter→fetcher)"
    - "El writer upserta proyecto/votacion/voto/tramitacion_evento idempotentemente por clave natural"
    - "Una corrida LIVE acotada (Leg 58) produce filas reales de proyecto/votacion/voto en Supabase local con provenance"
    - "El timeline materializado en tramitacion_evento permite que la ficha consulte sin fusionar en render"
  artifacts:
    - path: "packages/tramitacion/src/connector-camara.ts"
      provides: "CamaraConnector: descubre boletines (Leg 58) y fetchea votaciones/detalle reusando @obs/ingest"
      contains: "assertAllowedUrl"
    - path: "packages/tramitacion/src/connector-senado.ts"
      provides: "SenadoConnector: fetchea tramitacion.php + votaciones.php por boletín"
      contains: "assertAllowedUrl"
    - path: "packages/tramitacion/src/writer.ts"
      provides: "Writer idempotente inyectable (upsert por clave natural)"
      contains: "export interface TramitacionWriter"
    - path: "packages/tramitacion/src/ingest-run.ts"
      provides: "Orquestación de la corrida acotada (descubrir → fetch → parse → reconciliar → upsert)"
      contains: "export async function runIngest"
  key_links:
    - from: "packages/tramitacion/src/connector-*.ts"
      to: "@obs/ingest (Fetcher/RobotsGuard/HostRateLimiter/assertAllowedUrl)"
      via: "reuso de colaboradores (NO BaseConnector.run)"
      pattern: "Fetcher|rateLimiter\\.wait|assertAllowedUrl"
    - from: "packages/tramitacion/src/ingest-run.ts"
      to: "reconciliarVotosCamara / reconciliarVotosSenado / fusionarTimeline"
      via: "ensambla parsers + reconciliación + materialización del timeline"
      pattern: "reconciliarVotos|fusionarTimeline"
---

<objective>
Cerrar la rebanada con datos REALES: construir los conectores Cámara/Senado que reusan `@obs/ingest` (rate-limit/robots/allowlist/UA — NO `BaseConnector.run`, su caché diaria saltaría re-corridas), el writer idempotente que persiste el modelo común por clave natural, y una **corrida LIVE acotada de la legislatura vigente (Leg 58)** que descubre boletines, fetchea ambas fuentes, parsea, reconcilia el voto-a-voto (Cámara por Id, Senado por pipeline) y materializa proyecto/votacion/voto/tramitacion_evento en el Supabase local con provenance — alimentando la ficha de la ola 4.

Purpose: TRAM-01/02 exigen ingesta real de ambas cámaras; el slice MVP solo está "vivo" cuando un ciudadano abre una ficha con datos reales. Materializa el timeline (decisión de research) para que la ficha consulte sin fusionar en render.
Output: Conectores testeados con mock fetch + fixtures reales, writer idempotente, CLI de ingesta, y una corrida LIVE que deja filas reales en la DB local (checkpoint humano, como el seeder de Fase 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-RESEARCH.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-CONTEXT.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-02-SUMMARY.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-03-SUMMARY.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-03-SUMMARY.md

<interfaces>
<!-- Reuso de @obs/ingest (patrón del seeder Fase 3, NO BaseConnector). -->
From @obs/ingest:
```typescript
class Fetcher { get(spec: { url: string }): Promise<{ body: string; status; headers }> }
class HostRateLimiter { wait(host: string): Promise<void> }   // 2-3s serial por host
class RobotsGuard { isAllowed(url: string): Promise<boolean> } // fail-open
function assertAllowedUrl(url: string): Promise<void>          // SSRF deny-by-default; camara.cl/senado.cl en allowlist
const IDENTIFIED_UA: string;
```
Patrón LOCKED por request (seeder Fase 3):
`await assertAllowedUrl(url); if(!(await robots.isAllowed(url))) return; await rateLimiter.wait(host); const { body } = await fetcher.get({ url });`

<!-- Parsers y reconciliación de las olas 2-3 (consumir) -->
parseCamaraVotacion / parseCamaraVotoDetalle / parseCamaraSesion / parseSenadoTramitacion / parseSenadoVotacion / fusionarTimeline / eventoDesdeVotacion
reconciliarVotosCamara(votosCrudos, votacionId, maestra) : Voto[]
reconciliarVotosSenado(votosCrudos, votacionId, maestra, provider, writer) : Promise<Voto[]>

<!-- Writer real Supabase: espejar packages/identity/src/writer-supabase.ts (SupabaseMaestraWriter) -->
// createClient(url, serviceKey) con SUPABASE_LOCAL_SERVICE_KEY (bypassa RLS); upsert con onConflict en la clave natural.
// Supabase local URL = http://127.0.0.1:54421 (DEFAULT_LOCAL_URL en seed-cli.ts).

<!-- Endpoints LIVE (05-RESEARCH §1-6, todos VERIFIED 2026-06-18, UA Bot-Ciudadano/1.0, delay 2-3s) -->
Descubrir boletines Leg 58: GET retornarVotacionesXAnno?prmAnno=2026 (boletín en <Descripcion> texto libre, regex /Bolet[íi]n N°\s*(\d+-\d+)/) — o iterar sesiones via retornarSesionesXLegislatura?prmLegislaturaID=58.
Cámara votación por boletín: getVotaciones_Boletin?prmBoletin={base}; detalle: retornarVotacionDetalle?prmVotacionID={id}.
Senado: tramitacion.php?boletin={base} (base SIN sufijo, Pitfall 1) + votaciones.php?boletin={base}.

<!-- maestra: cargar de supabase/seeds/parlamentario.seed.json (186 filas reales) o leer de la DB local. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conectores Cámara/Senado (reusan @obs/ingest) + writer idempotente</name>
  <files>packages/tramitacion/src/connector-camara.ts, packages/tramitacion/src/connector-camara.test.ts, packages/tramitacion/src/connector-senado.ts, packages/tramitacion/src/connector-senado.test.ts, packages/tramitacion/src/writer.ts, packages/tramitacion/src/writer.test.ts, packages/tramitacion/src/writer-supabase.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - connector con mock fetch: descubre boletines de Leg 58 (regex sobre Descripcion) y fetchea por boletín reusando assertAllowedUrl→robots→rateLimiter.wait→fetcher.get en ese orden.
    - connector Senado: fetchea tramitacion.php + votaciones.php con el número base (sin sufijo).
    - writer (espía in-memory): upsert idempotente — correr 2x con el mismo input no duplica filas (clave natural: proyecto.boletin, votacion.id, voto(votacion_id,mencion_nombre), tramitacion_evento(boletin,fecha,camara,tipo,descripcion)).
  </behavior>
  <action>
**connector-camara.ts** — `CamaraConnector` con deps inyectables `{ fetcher, robots, rateLimiter }` (como el seeder Fase 3):
- `descubrirBoletines(anno: number): Promise<string[]>`: fetch `retornarVotacionesXAnno?prmAnno={anno}` (vía el patrón LOCKED), extraer boletines de `<Descripcion>` con regex `/Bolet[íi]n N°\s*(\d+-\d+)/` (solo Tipo=Proyecto de Ley los traen; resoluciones/acuerdos no → ignorar). Dedup.
- `fetchVotacionesBoletin(boletinBase: string)`: fetch `getVotaciones_Boletin?prmBoletin={base}` → body XML (para parseCamaraVotacion).
- `fetchVotacionDetalle(votacionId: string)`: fetch `retornarVotacionDetalle?prmVotacionID={id}` → body XML (para el voto-a-voto por Diputado/Id).
- Cada fetch: `assertAllowedUrl(url)` → `robots.isAllowed(url)` → `rateLimiter.wait(host)` → `fetcher.get({url})`. NO `BaseConnector.run` (anti-pattern: caché diaria saltaría re-corridas).

**connector-senado.ts** — `SenadoConnector` análogo:
- `fetchTramitacion(boletinBase)`: `tramitacion.php?boletin={base}` (base sin sufijo, Pitfall 1).
- `fetchVotaciones(boletinBase)`: `votaciones.php?boletin={base}`.
- Mismo orden de colaboradores.

**writer.ts** — `TramitacionWriter` (interfaz inyectable) con `upsertProyecto`, `upsertVotacion`, `upsertVotos`, `upsertEventos`, todos idempotentes por clave natural. Un fake in-memory para tests (Map por clave natural; correr 2x no duplica).

**writer-supabase.ts** — `SupabaseTramitacionWriter implements TramitacionWriter`: espejar `SupabaseMaestraWriter` (packages/identity/src/writer-supabase.ts): `createClient(url, serviceKey)` con `SUPABASE_LOCAL_SERVICE_KEY`; `from(tabla).upsert(filas, { onConflict: '<clave natural>' })`. La service key bypassa RLS. No exponer la key en errores.

Exportar conectores + writer desde `index.ts`. Tests con mock fetch + fixtures reales (sin red), verificando el ORDEN de las llamadas a los colaboradores (rateLimiter.wait antes de fetcher.get).
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run "connector|writer"; pnpm -w typecheck</automated>
  </verify>
  <done>Conectores verdes: reusan @obs/ingest en el orden correcto (mock verifica rateLimiter.wait antes del fetch); writer idempotente (2x no duplica); typecheck exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orquestación de la corrida acotada (ingest-run) + CLI</name>
  <files>packages/tramitacion/src/ingest-run.ts, packages/tramitacion/src/ingest-cli.ts, packages/tramitacion/src/ingest-cli.test.ts, packages/tramitacion/src/index.ts, packages/tramitacion/package.json</files>
  <behavior>
    - runIngest (con conectores/writer/provider mock + fixtures): para una lista acotada de boletines, fetchea ambas fuentes, parsea, reconcilia votos (Cámara por Id, Senado por pipeline), materializa eventos (tramitación + eventoDesdeVotacion) y upserta todo vía el writer. Idempotente.
    - El CLI valida flags (anno, límite de boletines, --dry-run) antes de tocar la red/DB y respeta el rate-limit del framework.
  </behavior>
  <action>
**ingest-run.ts** — `runIngest(opts: { boletines?: string[]; anno?: number; limite?: number; maestra; camara: CamaraConnector; senado: SenadoConnector; writer: TramitacionWriter; provider }): Promise<{ proyectos; votaciones; votos; eventos }>`:
- Si no se pasan `boletines`: `camara.descubrirBoletines(anno ?? 2026)`, recortar a `limite` (alcance acotado — NO el universo completo; WAF + tiempo).
- Por cada boletín (con base = quitar sufijo `-\d+`):
  1. Senado: `fetchTramitacion(base)` → `parseSenadoTramitacion` → `{ proyecto, eventos }`. (El Senado tiene el `descripcion` que alimenta la ficha.)
  2. Cámara: `fetchVotacionesBoletin(base)` → `parseCamaraVotacion`; por cada votación con detalle disponible, `fetchVotacionDetalle(id)` → `parseCamaraVotoDetalle` → `reconciliarVotosCamara(votos, votacionId, maestra)`.
  3. Senado votaciones: `fetchVotaciones(base)` → `parseSenadoVotacion` (puede ser [] — Pitfall 2, no error); por cada votación `reconciliarVotosSenado(votos, votacionId, maestra, provider, writer-revision)`.
  4. Materializar timeline: `eventos` del Senado + `eventoDesdeVotacion(v)` por cada Votacion de ambas cámaras; ordenar con `fusionarTimeline`.
  5. Upsert: `writer.upsertProyecto(proyecto)`, `upsertVotacion(votaciones)`, `upsertVotos(votos)`, `upsertEventos(eventos)`. Provenance inline en cada fila (ya viene de los parsers).
- Tolerar fuentes vacías/ausentes por boletín sin abortar la corrida completa (un boletín en primer trámite Cámara no tiene votos Senado).
- El `provider` del pipeline: las menciones que resuelven determinísticamente NO llaman al LLM; las dudosas van a la cola de revisión (no bloquean la ingesta — Environment Availability de research). En la corrida LIVE, MiniMax es opcional (gated); sin él, lo dudoso queda en cola.

**ingest-cli.ts** — entry-point (tsx) espejando `seed-cli.ts` de Fase 3: flags `--anno`, `--limite`, `--boletines a,b,c`, `--dry-run` (no escribe), `--service-key`/`SUPABASE_LOCAL_SERVICE_KEY`. Validar flags ANTES de fetch/DB. Sin service key → corrida en dry-run con aviso (no carga DB). Añadir script `ingest` (tsx) en package.json.

Tests: `ingest-cli` con args válidos/inválidos (sin red); `runIngest` con conectores fake (devuelven fixtures), writer espía y `MockMiniMaxProvider` → verifica que produce proyecto/votaciones/votos/eventos y que re-correr es idempotente.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run "ingest"; pnpm -w test --run; pnpm -w typecheck</automated>
  </verify>
  <done>runIngest e ingest-cli verdes con conectores fake; idempotencia verificada; suite completa de @obs/tramitacion verde; typecheck exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (gate): Corrida LIVE acotada (Leg 58) → filas reales en Supabase local</name>
  <files>packages/tramitacion/src/ingest-cli.ts</files>
  <what-built>
La orquestación `runIngest` con conectores REALES (Fetcher/RobotsGuard/HostRateLimiter de @obs/ingest) + `SupabaseTramitacionWriter` contra el Supabase local, sobre un conjunto ACOTADO de boletines de la legislatura vigente (Leg 58). Respeta rate-limit 2-3s + UA identificatorio (framework Fase 1). Materializa proyecto/votacion/voto/tramitacion_evento con provenance. Esto es el equivalente de la corrida LIVE del seeder de Fase 3 (03-04): real, acotada, con checkpoint humano. R2/remoto diferidos por credenciales (r2Enabled=false).
  </what-built>
  <how-to-verify>
1. Con Supabase local arriba y `SUPABASE_LOCAL_SERVICE_KEY` en el entorno, correr: `pnpm --filter @obs/tramitacion ingest --anno 2026 --limite 5` (acotado; expandible después). Incluir al menos un boletín que cruce ambas cámaras (p.ej. 14309, con votos en ambas) — pasarlo explícito con `--boletines 14309` si el descubrimiento no lo trae en los primeros 5.
2. Confirmar en la salida: 0 errores 403/429 (rate-limit respetado), N proyectos / M votaciones / K votos / E eventos cargados.
3. Verificar en la DB local (psql o supabase studio): `select count(*) from proyecto; from votacion; from voto; from tramitacion_evento;` > 0; que `voto` de Cámara tenga `parlamentario_id` poblado (determinista por Id) y que `voto` de Senado no confirmado tenga `parlamentario_id null` + `mencion_nombre` crudo; que cada fila tenga provenance (`origen`/`fecha_captura`/`enlace`).
4. Re-correr el mismo comando → conteos estables (idempotente, sin duplicados).
5. (Opcional, encadena con 05-04 Task 4) abrir `/proyecto/{boletin}` y ver la ficha con datos reales.
  </how-to-verify>
  <resume-signal>Escribe "approved" con los conteos reales obtenidos, o describe los errores (403/429, parseo, upsert).</resume-signal>
  <action>Checkpoint BLOQUEANTE de corrida LIVE (ver what-built / how-to-verify / resume-signal). El ejecutor corre `runIngest` con conectores REALES + `SupabaseTramitacionWriter` contra el Supabase local sobre un conjunto ACOTADO de boletines de Leg 58 (`--limite` pequeño + un boletín cross-cámara explícito), respetando el rate-limit 2-3s del framework. Tras la corrida, verifica conteos > 0 en las 4 tablas, provenance por fila, `voto` de Cámara con `parlamentario_id` determinista y Senado no confirmado con null + mención cruda, y re-corre para confirmar idempotencia. NO declarar la fase viva sin el visto bueno del operador con los conteos reales. Si hay 403/429, reducir el alcance y re-validar el rate-limit; si hay errores de parseo/upsert, corregir antes de aprobar.</action>
  <verify><human-check>El operador corrió la ingesta LIVE acotada, reportó conteos reales > 0 en proyecto/votacion/voto/tramitacion_evento con provenance, confirmó 0 errores 403/429, el voto-a-voto de Cámara con parlamentario_id por Id, y la idempotencia al re-correr.</human-check></verify>
  <done>Corrida LIVE acotada aprobada: filas reales con provenance en las 4 tablas del Supabase local; rate-limit respetado; idempotente; el slice MVP está vivo (o el operador reportó errores a corregir).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector → fuente gov | URL de fetch debe estar en la allowlist (SSRF). |
| corrida LIVE → WAF gubernamental | Ráfagas disparan bloqueo 403/429. |
| writer (service key) → Postgres | La service key bypassa RLS; debe usarse solo server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | DoS auto-infligido / WAF | corrida LIVE | mitigate | Reuso de `HostRateLimiter` (2-3s serial por host) + `RobotsGuard` + `IDENTIFIED_UA` de @obs/ingest; alcance ACOTADO (--limite); NO `BaseConnector.run`. El conector NO reimplementa la política. |
| T-05-12 | SSRF | URL de fuente en el conector | mitigate | `assertAllowedUrl` (deny-by-default; camara.cl/senado.cl en allowlist) antes de cada fetch. |
| T-05-03 | DoS / Disclosure | XML malformado en la corrida | mitigate | fast-xml-parser sin entidades externas; zod por fila; respuestas acotadas. |
| T-05-13 | Information Disclosure | service key en logs/errores del writer | mitigate | Espejar SupabaseMaestraWriter: la key nunca se interpola en mensajes de error; solo server-side. |
| T-05-06 | Tampering / falsa identidad | voto persistido | mitigate | Solo determinista puebla `parlamentario_id` (reconciliar-* de ola 3); Senado dudoso → null + mención cruda. |
| T-05-SC | Tampering | npm installs | mitigate | Sin paquetes nuevos en este plan (reusa @obs/* + fast-xml-parser/zod/@supabase). N/A. |
</threat_model>

<verification>
- `pnpm -w test --run` verde (incluye toda la suite de @obs/tramitacion + slice E2E).
- `pnpm -w typecheck` exit 0.
- Gate LIVE aprobado: filas reales de proyecto/votacion/voto/tramitacion_evento en Supabase local con provenance, 0 errores 403/429, idempotente, voto-a-voto de Cámara con parlamentario_id por Id.
</verification>

<success_criteria>
- Conectores Cámara/Senado reusan @obs/ingest (sin reimplementar política).
- Writer idempotente por clave natural; SupabaseTramitacionWriter contra DB local.
- Corrida LIVE acotada (Leg 58) produce datos REALES con provenance — el slice MVP está vivo.
- Timeline materializado en tramitacion_evento.
</success_criteria>

<output>
Create `.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-05-SUMMARY.md` when done
</output>
