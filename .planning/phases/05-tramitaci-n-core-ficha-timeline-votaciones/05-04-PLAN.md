---
phase: 05-tramitaci-n-core-ficha-timeline-votaciones
plan: 04
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - app/package.json
  - app/components.json
  - app/tailwind.config.ts
  - app/postcss.config.mjs
  - app/app/globals.css
  - app/app/styles/civic-tokens.css
  - app/app/layout.tsx
  - app/lib/supabase.ts
  - app/lib/format.ts
  - app/lib/format.test.ts
  - app/components/provenance-badge.tsx
  - app/components/identity-marker.tsx
  - app/components/etapa-badge.tsx
  - app/components/camara-chip.tsx
  - app/components/ficha-header.tsx
  - app/components/timeline-event.tsx
  - app/components/timeline-view.tsx
  - app/components/votacion-card.tsx
  - app/components/votacion-bar.tsx
  - app/components/voto-detalle.tsx
  - app/components/voto-row.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/app/proyecto/[boletin]/not-found.tsx
  - app/vitest.config.ts
autonomous: false
requirements: [TRAM-04, TRAM-05, TRAM-06, TRAM-09]
user_setup:
  - service: supabase-local
    why: "El frontend lee las tablas públicas de tramitación del Supabase local"
    env_vars:
      - name: SUPABASE_URL
        source: "http://127.0.0.1:54421 (Supabase local, ya en uso por Fase 3-4)"
      - name: SUPABASE_ANON_KEY
        source: "supabase status (anon key del proyecto local)"
must_haves:
  truths:
    - "Un ciudadano abre /proyecto/[boletin] y ve la ficha con estado/etapa actual"
    - "Ve el timeline de tramitación fusionado cruzando ambas cámaras por boletín"
    - "Ve los resultados de votación (totales SI/NO/Abstención + resultado)"
    - "Cada dato muestra frescura por fuente + enlace a la fuente oficial (ProvenanceBadge)"
    - "Un voto del Senado sin identidad confirmada muestra el nombre crudo + marca 'identidad no verificada', nunca como link"
  artifacts:
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "Server Component de la ficha: header + timeline + votaciones leyendo Supabase"
      contains: "proyecto/[boletin]"
    - path: "app/components/provenance-badge.tsx"
      provides: "Badge frescura + fuente (amber si >48h) per UI-SPEC §4 (TRAM-09)"
      contains: "ProvenanceBadge"
    - path: "app/components/identity-marker.tsx"
      provides: "Marca 'identidad no verificada' per UI-SPEC §5"
      contains: "IdentityMarker"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "Supabase proyecto/votacion/voto/tramitacion_evento"
      via: "supabase-js server-only con anon key"
      pattern: "from\\(.(proyecto|votacion|tramitacion_evento)"
    - from: "app/components/voto-row.tsx"
      to: "IdentityMarker"
      via: "render condicional: link solo si estado_vinculo='confirmado', si no marca"
      pattern: "estado_vinculo|confirmado|IdentityMarker"
---

<objective>
Construir el frontend Next.js 16 de la ficha de proyecto: inicializar el design system (shadcn + tokens cívicos del UI-SPEC), y los Server Components que leen las tablas públicas de Supabase para renderizar `/proyecto/[boletin]` con header (estado/etapa), timeline fusionado cross-cámara, votaciones (totales + barra CSS + voto-a-voto del Senado), el `ProvenanceBadge` (frescura + fuente, TRAM-09) y el `IdentityMarker` (guarda de identidad en la UI). Tono sobrio, español, sin causalidad.

Purpose: Es el valor ciudadano VISIBLE de la fase (TRAM-04/05/06/09). Cierra el slice MVP: un ciudadano abre la ficha y ve qué pasó, cuándo y según qué fuente. Depende solo del esquema/RLS de 05-01 (lee tablas); la ingesta real que las puebla es la ola 4.
Output: Ficha funcional servida localmente, componentes del UI-SPEC implementados, badge de frescura y marca de identidad operativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-UI-SPEC.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-CONTEXT.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-01-SUMMARY.md

<interfaces>
<!-- Esquema de las tablas que la ficha lee (de 05-01). RLS public-read para anon ya aplicada. -->
proyecto(boletin pk, boletin_num, titulo, iniciativa, camara_origen, autores[], materia, estado, etapa, subetapa, origen, fecha_captura, enlace)
votacion(id pk, boletin fk, fecha, etapa, tipo, quorum, resultado, total_si, total_no, total_abstencion, total_pareo, camara, origen, fecha_captura, enlace)
voto(id, votacion_id fk, mencion_nombre, parlamentario_id nullable, seleccion, metodo, estado_vinculo)
tramitacion_evento(id, boletin fk, fecha, camara, tipo, descripcion, enlace, origen, fecha_captura, enlace)

<!-- Scaffold existente -->
app/app/layout.tsx: usa next/font Geist (geistSans/geistMono variables); ACTUALMENTE `<html lang="en">` → cambiar a `lang="es"` (UI-SPEC §8/12.3).
app/package.json: next 16.2.9, react 19.2.4; sin Tailwind/shadcn aún.
Next 16: `params` es `Promise<{ boletin: string }>` → `const { boletin } = await params`.

<!-- Patrón de lectura supabase-js server-only (UI-SPEC §10, RESEARCH Code Examples) -->
const sb = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_ANON_KEY!); // server-only
const { data } = await sb.from("proyecto").select("*").eq("boletin", boletin).single();
</interfaces>

<!-- El UI-SPEC es el contrato de diseño COMPLETO (tokens, componentes, copy, accesibilidad, estados). Seguirlo al pie. Secciones clave: §0 bootstrap, §1 tokens, §2 inventario, §3 pantallas, §4 ProvenanceBadge, §5 IdentityMarker, §6 estados, §9 copy, §12 notas de ejecución. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (gate): Inicializar shadcn + Tailwind (paquetes nuevos del frontend)</name>
  <files>app/package.json, app/components.json, app/tailwind.config.ts, app/postcss.config.mjs</files>
  <what-built>
El UI-SPEC §0 requiere instalar Tailwind + shadcn en `/app` antes de cualquier componente. Esto AÑADE dependencias npm nuevas al frontend (`tailwindcss`, `@tailwindcss/typography`, `postcss`, `autoprefixer`, y los deps que shadcn copia). El research marca estos como `[ASSUMED]` (no pasaron slopcheck en la sesión Windows). Este gate verifica la legitimidad antes de instalar.

El ejecutor debe, ANTES de pedir aprobación, presentar:
  - tailwindcss → npmjs.com/package/tailwindcss (Tailwind Labs, ~20M/wk)
  - @tailwindcss/typography → npmjs.com/package/@tailwindcss/typography (Tailwind Labs)
  - shadcn CLI (`pnpm dlx shadcn@latest`) — copia componentes al repo (no es dep de runtime clásica)
NO se usan registries de terceros (UI-SPEC §11: solo registro oficial). NO se adoptan visx/recharts (la barra y el timeline son CSS puro per UI-SPEC §2.3).
  </what-built>
  <how-to-verify>
1. Confirmar en npmjs.com que `tailwindcss` y `@tailwindcss/typography` son los paquetes oficiales de Tailwind Labs (no typosquats).
2. Aprobar la instalación. El ejecutor entonces corre (desde `/app`): `pnpm add -D tailwindcss @tailwindcss/typography postcss autoprefixer` y `pnpm dlx shadcn@latest init` (Style: Default, Base color: Slate, CSS vars: yes, RSC: yes, components alias `@/components`, utils `@/lib/utils`) y `pnpm dlx shadcn@latest add badge card separator skeleton tooltip`.
3. Verificar que `app/components.json` y `app/tailwind.config.ts` existen tras el init.
  </how-to-verify>
  <resume-signal>Escribe "approved" para autorizar la instalación de Tailwind/shadcn, o indica objeciones.</resume-signal>
  <action>Checkpoint BLOQUEANTE de legitimidad de paquetes (ver what-built / how-to-verify / resume-signal). Tras verificar en npmjs.com que `tailwindcss` y `@tailwindcss/typography` son oficiales de Tailwind Labs (no typosquats) y recibir aprobación humana explícita, correr desde `/app`: `pnpm add -D tailwindcss @tailwindcss/typography postcss autoprefixer`, `pnpm dlx shadcn@latest init` (Default/Slate/CSS vars/RSC, alias `@/components` + `@/lib/utils`), y `pnpm dlx shadcn@latest add badge card separator skeleton tooltip`. NO adoptar visx/recharts ni registries de terceros (solo registro oficial, UI-SPEC §11). NO instalar sin aprobación explícita (paquetes [ASSUMED], no auto-aprobables).</action>
  <verify><human-check>El operador verificó la legitimidad de los paquetes en npmjs.com y aprobó; `app/components.json` y `app/tailwind.config.ts` existen tras el init.</human-check></verify>
  <done>Tailwind + shadcn instalados con los componentes requeridos (badge/card/separator/skeleton/tooltip); `components.json` y `tailwind.config.ts` presentes; sin paquetes de terceros ni visx/recharts.</done>
</task>

<task type="auto">
  <name>Task 2: Design system bootstrap (tokens cívicos, layout es, cliente supabase, formato)</name>
  <files>app/app/styles/civic-tokens.css, app/app/globals.css, app/tailwind.config.ts, app/app/layout.tsx, app/lib/supabase.ts, app/lib/format.ts, app/lib/format.test.ts, app/vitest.config.ts</files>
  <action>
- **civic-tokens.css** (UI-SPEC §1.2): crear `app/app/styles/civic-tokens.css` con los tokens `--camara*`, `--senado*`, `--provenance-*`, `--identity-warn-*` para `:root` y `.dark` EXACTAMENTE como en UI-SPEC §1.1 "Civic Semantic Tokens". Importarlo en `globals.css` DESPUÉS del bloque de variables de shadcn (no inline).
- **tailwind.config.ts**: extender `fontFamily.sans = ['var(--font-geist-sans)', ...]` y `fontFamily.mono = ['var(--font-geist-mono)', ...]` (UI-SPEC §12.7).
- **layout.tsx**: cambiar `<html lang="en">` → `<html lang="es">` (UI-SPEC §8/§12.3). Conservar las font variables Geist.
- **lib/supabase.ts**: `createServerSupabase()` que devuelve `createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_ANON_KEY!)` — server-only (sin `NEXT_PUBLIC_` para que la key no llegue al cliente; estas lecturas viven en Server Components). Documentar las env vars.
- **lib/format.ts** + test: `relativeTimeEs(capturedAt: Date): string` (Intl.RelativeTimeFormat es-CL: <1h "hace X min", <24h "hace X h", <7d "hace X días", ≥7d fecha absoluta DD MMM YYYY), `fechaCorta(d: Date): string` (`Intl.DateTimeFormat('es-CL',{day:'2-digit',month:'short',year:'numeric'})` → "14 may 2026") y `esStale(capturedAt: Date): boolean` (>48h). Test unitario de los rangos.
- **vitest.config.ts** en `/app` para correr el test de format (env node/jsdom según necesidad).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run format; node -e "const fs=require('fs');if(!fs.existsSync('app/components.json'))throw new Error('shadcn no inicializado')"</automated>
  </verify>
  <done>tokens cívicos importados, `lang="es"`, cliente supabase server-only, helpers de formato testeados; format.test verde.</done>
</task>

<task type="auto">
  <name>Task 3: Componentes de la ficha + página /proyecto/[boletin] (header, timeline, votaciones, provenance, identidad)</name>
  <files>app/components/provenance-badge.tsx, app/components/identity-marker.tsx, app/components/etapa-badge.tsx, app/components/camara-chip.tsx, app/components/ficha-header.tsx, app/components/timeline-event.tsx, app/components/timeline-view.tsx, app/components/votacion-card.tsx, app/components/votacion-bar.tsx, app/components/voto-detalle.tsx, app/components/voto-row.tsx, app/app/proyecto/[boletin]/page.tsx, app/app/proyecto/[boletin]/not-found.tsx</files>
  <action>
Implementar los componentes del UI-SPEC §2.2 + §3 + §4 + §5. Server Components por defecto; SOLO `voto-detalle.tsx` (toggle expand/collapse) y el nav sticky llevan `"use client"` (UI-SPEC §12.4).
- **provenance-badge.tsx** (UI-SPEC §4): props `{ capturedAt: Date; sourceName: 'Cámara'|'Senado'|'BCN'; sourceUrl: string }`. Estructura y clases EXACTAS del spec; usa `relativeTimeEs`; aplica override amber (`text-amber-700 border-amber-400`) si `esStale` (>48h); enlace `target="_blank" rel="noopener noreferrer"` con aria-label; envolver en shadcn `Tooltip` con timestamp ISO + URL cruda. Si provenance null → `sourceName="fuente desconocida"` sin link (UI-SPEC §6.3), nunca ocultar el badge.
- **identity-marker.tsx** (UI-SPEC §5): marca inline "identidad no verificada ⚠" con los tokens `--identity-warn-*`, `title` explicativo, `⚠` con `aria-hidden`, span con `aria-label`. Texto SIEMPRE "identidad no verificada" (nunca "dudoso/posible").
- **etapa-badge.tsx** + **camara-chip.tsx** (UI-SPEC §3.1.1): variantes de estado/etapa y chip Cámara/Senado con los tokens cívicos; usar shadcn `Badge` con className (no nuevas variantes en components.json).
- **ficha-header.tsx** (UI-SPEC §3.1.1): EtapaBadge + CamaraChip + título (text-3xl) + boletín (mono) + iniciativa/materia chips + autores (>3 → "+N más" expandible inline, nombres en texto plano, NO links) + ProvenanceBadge por fuente.
- **timeline-event.tsx** + **timeline-view.tsx** (UI-SPEC §3.2): lista vertical CSS con rail izquierdo (sin SVG), dot coloreado por cámara, cada evento con CamaraChip + fecha (mono, `fechaCorta`) + tipo + descripción + "Ver fuente oficial ↗" (min-h-44px) cuando hay enlace + ProvenanceBadge. Orden ya viene del server (fecha ASC). Empty state UI-SPEC §6.1.
- **votacion-card.tsx** + **votacion-bar.tsx** + **voto-detalle.tsx** + **voto-row.tsx** (UI-SPEC §3.3): card con CamaraChip+fecha+etapa+quórum, VotacionBar (4 segmentos CSS con aria-label por segmento, total 0 → barra muted "Sin datos de votación"), totales en texto (Sí/No/Abst/Pareo), ResultadoBadge, y para Senado VotoDetalle expandible (`use client`, aria-expanded/controls) con VotoRow. **VotoRow (guarda de identidad):** si `estado_vinculo === 'confirmado'` y `parlamentario_id` presente → `<Link href="/parlamentario/[id]">nombre</Link>`; si NO → `<span>{mencion_nombre}</span><IdentityMarker/>`. NUNCA link si no confirmado.
- **page.tsx** (UI-SPEC §3.1 + §12.5): Server Component; `const { boletin } = await params`; validar `boletin` con regex `^\d{3,6}(-\d{1,2})?$` y `notFound()` si no matchea (V5/path injection T-05-09); leer `proyecto` (`.eq("boletin",boletin).single()`), `tramitacion_evento` (`.order("fecha",{ascending:true})`), `votacion` (`.select("*, voto(*)")`). Si `proyecto` no existe → `notFound()`. Envolver cada sección async en `<Suspense>` con su Skeleton (UI-SPEC §6.2). Render: FichaHeader + section#timeline (TimelineView) + section#votaciones (VotacionCard[]).
- **not-found.tsx** (UI-SPEC §6.1 "Ficha not found"): copy en español con enlaces externos a Senado/Cámara.
- Copy/tono per UI-SPEC §9: sobrio, sin causalidad, "fuente desconocida" nunca omitir, "identidad no verificada" nunca hedge.
NO usar Recharts ni visx (barra y timeline CSS puro, UI-SPEC §2.3/§12.6).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>La ficha compila (`pnpm build` sin errores de tipo); componentes del UI-SPEC presentes; VotoRow respeta la guarda (link solo si confirmado); ProvenanceBadge y IdentityMarker implementados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (gate): Render visual de la ficha en el navegador</name>
  <files>app/app/proyecto/[boletin]/page.tsx</files>
  <what-built>
La ficha `/proyecto/[boletin]` renderiza header (estado/etapa), timeline fusionado cross-cámara, votaciones (totales + barra + voto-a-voto del Senado), con ProvenanceBadge (frescura+fuente) e IdentityMarker donde la identidad no está confirmada. Lee del Supabase local vía anon key (RLS public-read de 05-01). NOTA: los datos reales los puebla la ola 4 (LIVE ingest); para este gate, si aún no hay datos, el ejecutor puede insertar una fila semilla mínima de prueba o coordinar con la ola 4.
  </what-built>
  <how-to-verify>
1. `cd app && pnpm dev` (con `SUPABASE_URL`/`SUPABASE_ANON_KEY` del Supabase local en el entorno).
2. Abrir `http://localhost:3000/proyecto/{boletin}` con un boletín presente en la DB (p.ej. el de la corrida de la ola 4, o una semilla).
3. Verificar: (a) header con estado/etapa y boletín en mono; (b) timeline cronológico con chips de cámara y enlaces "Ver fuente oficial ↗"; (c) votaciones con barra de colores + totales en texto + resultado; (d) ProvenanceBadge "Actualizado hace X · fuente — fuente oficial ↗" en header/eventos/votaciones; (e) si hay voto Senado no confirmado, el nombre aparece con "identidad no verificada ⚠" y NO como link; (f) tono sobrio, español, sin causalidad.
4. Abrir un boletín inexistente → página not-found con copy en español.
  </how-to-verify>
  <resume-signal>Escribe "approved" o describe los problemas visuales/de contenido encontrados.</resume-signal>
  <action>Checkpoint BLOQUEANTE de verificación visual (ver what-built / how-to-verify / resume-signal). El ejecutor levanta `pnpm dev` con las env vars del Supabase local y guía al operador por la verificación de la ficha (header/timeline/votaciones/provenance/identidad/not-found). NO marcar la fase como cerrada sin el visto bueno del operador. Si el operador reporta problemas visuales o de contenido, corregirlos antes de continuar.</action>
  <verify><human-check>El operador abrió la ficha en el navegador y confirmó header+timeline+votaciones+ProvenanceBadge+IdentityMarker correctos, tono sobrio en español, y la página not-found para un boletín inexistente.</human-check></verify>
  <done>El operador aprobó el render visual de la ficha (o reportó problemas a corregir); la guarda de identidad y el badge de frescura se ven correctos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `/proyecto/[boletin]` (cliente) → query Supabase | `boletin` es input no confiable del path. |
| anon key (frontend) → Postgres | Lee solo tablas públicas; RLS de 05-01 impide leer la maestra. |
| paquetes npm del frontend (Tailwind/shadcn) | Cadena de suministro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | path injection en `/proyecto/[boletin]` | mitigate | Validar `boletin` con regex `^\d{3,6}(-\d{1,2})?$`; `notFound()` si no matchea; `.eq()` parametrizado de supabase-js (no SQL string). |
| T-05-06 | Tampering / falsa identidad | VotoRow render | mitigate | Link solo si `estado_vinculo='confirmado'`; si no, nombre crudo + IdentityMarker. NUNCA link a una identidad no confirmada (guarda LOCKED). |
| T-05-10 | Information Disclosure | supabase key en el cliente | mitigate | `SUPABASE_ANON_KEY` (no service) y sin prefijo `NEXT_PUBLIC_`; lecturas en Server Components server-only; anon limitado por RLS a tablas públicas. |
| T-05-SC | Tampering | npm installs (tailwindcss, @tailwindcss/typography, shadcn) | mitigate | `[ASSUMED]` → checkpoint:human-verify BLOQUEANTE (Task 1) verifica en npmjs.com antes de instalar; solo registro oficial de shadcn (UI-SPEC §11); no visx/recharts. |
</threat_model>

<verification>
- `cd app && pnpm vitest run format` verde.
- `cd app && pnpm exec tsc --noEmit` y `pnpm build` sin errores.
- Gate visual aprobado: ficha renderiza header+timeline+votaciones+provenance+identidad correctamente.
</verification>

<success_criteria>
- shadcn + tokens cívicos inicializados (gate de paquetes aprobado).
- `/proyecto/[boletin]` renderiza la ficha completa leyendo Supabase local.
- ProvenanceBadge (TRAM-09) e IdentityMarker (guarda) operativos.
- Barra de votación y timeline en CSS puro; tono sobrio español sin causalidad.
</success_criteria>

<output>
Create `.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-04-SUMMARY.md` when done
</output>
