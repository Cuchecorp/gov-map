---
phase: 53-uxnav-auditoria-ux-navegada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md
  - .planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "53-UX-AUDIT.md existe con los 4 journeys navegados en vivo contra PROD, en desktop 1280 y móvil 390"
    - "Cada hallazgo tiene clasificación P0/P1/P2, ubicación exacta (ruta+viewport, y file:line del fix si es P0) y evidencia visual archivada"
    - "Todo P0 propuesto mapea a UN remedio del contrato UI-SPEC (ítem de nav / breadcrumb / línea de continuación / cross-link ya-legal); lo que exceda eso queda como P1"
    - "El 404 de /contraparte con MONEY gate OFF queda registrado como GATED, no como P0"
  artifacts:
    - path: ".planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md"
      provides: "Informe de auditoría con journeys, matriz de cobertura y hallazgos clasificados"
      contains: "## Journey 1"
    - path: ".planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/"
      provides: "Screenshots numerados por journey (jpeg q70), ≥8 archivos (4 journeys × 2 viewports)"
  key_links:
    - from: "53-UX-AUDIT.md hallazgos P0"
      to: "53-UI-SPEC.md contrato (nav / breadcrumb / continuation / cross-link legal)"
      via: "cada P0 nombra su remedio contratado"
---

<objective>
Auditar la UX del sitio EN VIVO (https://observatorio-congreso.thevalis.workers.dev) navegándolo con un navegador real (BrowserOS vía `scripts/bros-cli.mjs`, páginas OCULTAS) en los 4 journeys × 2 viewports (desktop 1280×800, móvil 390×844), y producir `53-UX-AUDIT.md` con hallazgos P0/P1/P2, evidencia visual y ubicación exacta.

Purpose: Este informe es el ÚNICO insumo de los planes de fix (Wave 2). El CONTEXT prohíbe el loop abierto: la auditoría produce el universo COMPLETO de hallazgos ANTES de tocar código.
Output: `53-UX-AUDIT.md` + carpeta `ux-evidence/` con screenshots numerados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-CONTEXT.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-RESEARCH.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UI-SPEC.md
@scripts/bros-cli.mjs

# El protocolo de DOS PISTAS con comandos verbatim, el esqueleto del informe,
# los datos reales de cada journey y las convenciones de nombres de evidencia
# están en 53-RESEARCH.md §BrowserOS Audit Mechanics y §Audit Report Structure.
# El executor los sigue AL PIE DE LA LETRA — fueron medidos en vivo el 2026-07-07.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pista A — navegación funcional directa de los 4 journeys (console + links + snapshot por ruta)</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/</files>
  <action>
Ejecutar la Pista A del protocolo de 53-RESEARCH.md §Protocolo de DOS PISTAS: abrir UNA página oculta con `node scripts/bros-cli.mjs open`, parsear el Page ID con el patrón `grep -oE "Page ID: [0-9]+"` (NUNCA asumir PID=1), y navegar con `navigate_page` a cada ruta de los 4 journeys reutilizando la misma página. Rutas verificadas con datos (53-RESEARCH §Datos reales): J1 `/`; J2 `/buscar` → `/buscar?q=proteccion%20de%20datos` → `/proyecto/14309-04`; J3 `/parlamentarios` → `/parlamentario/D1012` → `/red?seed=D1012`; J4 transversal `/proyecto/14309-04` ↔ `/parlamentario/[id]` ↔ `/agenda` ↔ `/contraparte/[id]` (404 esperado = GATED, NO P0) ↔ `/`. Por cada ruta: `get_console_logs` con `clear:true` (error de consola = hallazgo automático: P0 si rompe función, P1 si es ruido), `links` (inventario de salidas → detectar callejón sin salida y verificar el criterio "≤2 clicks a home/otras secciones"), `snapshot` (affordances interactivas), y opcionalmente `take_enhanced_snapshot` (headings/landmarks → auditar "¿la página anuncia dónde estás?"). Respetar `sleep 4-6` tras cada carga y `sleep 3` mínimo entre requests (ingesta respetuosa, aplica al crawl de PROD propio). Validar la affordance real del SearchBox UNA vez con `fill`+`click` (la búsqueda también es URL-driven; preferir URL para reproducibilidad). Cerrar la página con `close` al terminar. Registrar los console logs y el inventario de links crudos para el informe; NO tocar código de app.
  </action>
  <verify>
    <automated>test -d ".planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence"</automated>
  </verify>
  <done>Los 4 journeys navegados en Pista A; console logs y link-inventory por ruta capturados; baseline de consola conocido (/red = 0 entradas verificado en research); página oculta cerrada.</done>
</task>

<task type="auto">
  <name>Task 2: Pista B — screenshots de evidencia con harness iframe a 390 y 1280 exactos</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/</files>
  <action>
Ejecutar la Pista B del protocolo (53-RESEARCH §Protocolo de DOS PISTAS): NUNCA capturar "móvil" en la página oculta nativa (viewport medido 772×728, no controlable; `window.resizeTo` es no-op) — TODO screenshot de evidencia sale del harness iframe. Abrir una página oculta en `about:blank`, inyectar con `evaluate_script` un `<iframe id="vp">` apuntando a PROD con el ancho EXACTO del viewport (móvil `width:390px;height:2500px`; desktop `width:1280px;height:2000px`), `sleep 6` (SSR + hidratación dentro del iframe), y `save_screenshot` con `fullPage:true, format:"jpeg", quality:70` a ruta ABSOLUTA con forward-slashes bajo `ux-evidence/`. Envolver TODO `save_screenshot` en retry-once (`cmd || (sleep 3 && cmd)`) — el `CDP request timeout` es real y reproducido. Cambiar ruta/viewport re-ejecutando `evaluate_script` con nuevo `f.src` y/o `f.style.width`. Nombrar `j{journey}-{paso}-{slug}-{vp}.jpg` (vp = `390` o `1280`). Cubrir los 4 journeys × 2 viewports (≥8 shots). El gotcha de autofocus cross-origin en la consola del harness es RUIDO del harness, NO un hallazgo (los hallazgos de consola salen SOLO de Pista A). Cerrar todas las páginas al terminar. Fallback si BrowserOS muere: `curl` del HTML servido + suite RTL (la evidencia visual queda como deuda, ROADMAP lo autoriza) — documentarlo en el informe.
  </action>
  <verify>
    <automated>test $(ls ".planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/"*.jpg 2>/dev/null | wc -l) -ge 8</automated>
  </verify>
  <done>≥8 screenshots (4 journeys × desktop+móvil) archivados en ux-evidence/ como jpeg q70; layout móvil real capturado a 390px (nav envuelve a 2 filas, verificable en el shot).</done>
</task>

<task type="auto">
  <name>Task 3: Redactar 53-UX-AUDIT.md con hallazgos clasificados y matriz de cobertura</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md</files>
  <action>
Escribir `53-UX-AUDIT.md` con el esqueleto EXACTO de 53-RESEARCH.md §Audit Report Structure: encabezado (sitio + versión desplegada + mecánica dos pistas + límite conocido de consola cross-origin), resumen ejecutivo (conteo P0/P1/P2 + veredicto por journey), una sección por Journey (tabla paso·ruta·desktop·móvil·console·hallazgos con enlaces a ux-evidence/), la lista de Hallazgos (cada uno: dónde, evidencia, qué pasa/por qué desorienta en 2-3 líneas factuales, criterio [no-sé-dónde-estoy | no-puedo-llegar-a-X | callejón-sin-salida | affordance-rota | error-consola-grave], y para P0 el remedio contratado + file:line del fix), la matriz de cobertura de journeys, y la sección Re-walkthrough VACÍA (la llena la Wave 3). Clasificar aplicando el criterio LOCKED del CONTEXT: P0 = SOLO orientación/navegación/bloqueo; rediseños visuales/jerarquía = P1/P2 → F54; ante la duda P0/P1 → P1 (la fase no debe crecer). Regla de acotamiento por contrato (53-RESEARCH Pattern 2): todo P0 DEBE resolverse con nav-item / breadcrumb / continuation-line / cross-link ya-legal según la matriz del 53-UI-SPEC; si el remedio natural excede eso → degradar a "P1 alto". Registrar como GATED (no P0): `/contraparte/[id]` 404 con MONEY gate OFF, y el cross-link contraparte-en-lobby (NOT SHIPPABLE: RPC sin contraparte_id + doctrina texto-crudo + ruta gated). NO re-proponer cross-links ya shipped (53-RESEARCH §Cross-links ya shipped). NO proponer tocar el carril lobby×tramitación (texto plano LOCKED 52-03) ni la contraparte cruda.
  </action>
  <verify>
    <automated>grep -q "## Journey 1" ".planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md" && grep -q "## Journey 4" ".planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md"</automated>
  </verify>
  <done>53-UX-AUDIT.md existe con los 4 journeys, hallazgos P0/P1/P2 con criterio+evidencia+remedio contratado, matriz de cobertura, /contraparte registrado GATED, y sección Re-walkthrough lista para la Wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → BrowserOS MCP local (127.0.0.1:9200) | Comandos de navegación; MCP local sin auth, no recibe env/keys |
| harness iframe → PROD workers.dev | Framing cross-origin; solo se navega el sitio PÚBLICO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | Screenshots committeados | accept | Solo se navega el sitio público (sin /admin en los journeys); la evidencia es lo que cualquier visitante ve |
| T-53-02 | Information Disclosure | console/evidencia con secrets | mitigate | MCP local sin auth; bros-cli no recibe env; consola de PROD no emite keys (verificado: /red limpia) |
| T-53-03 | Information Disclosure | evidencia pesada infla repo | accept | jpeg q70; fullPage solo donde el hallazgo lo requiera |
</threat_model>

<verification>
- `ls ux-evidence/*.jpg | wc -l` ≥ 8
- `53-UX-AUDIT.md` contiene los 4 journeys y la sección de hallazgos con criterio+remedio por P0
- Cada P0 nombra un remedio del contrato UI-SPEC; `/contraparte` 404 marcado GATED (no P0)
- Ninguna página oculta de BrowserOS queda huérfana (todas cerradas)
</verification>

<success_criteria>
`53-UX-AUDIT.md` existe con ≥4 journeys navegados en vivo (desktop+móvil), screenshots archivados, hallazgos clasificados P0/P1/P2 con evidencia y ubicación exacta, y cada P0 acotado al contrato UI-SPEC (ROADMAP Phase 53 Success Criterion 1).
</success_criteria>

<output>
Create `.planning/phases/53-uxnav-auditoria-ux-navegada/53-01-SUMMARY.md` when done. Incluir el conteo P0/P1/P2 y la lista de superficies P0 con su remedio contratado — es el insumo directo de los planes 02/03/04.
</output>
