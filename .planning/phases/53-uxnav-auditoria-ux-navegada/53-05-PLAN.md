---
phase: 53-uxnav-auditoria-ux-navegada
plan: 05
type: execute
wave: 3
depends_on: [53-01, 53-02, 53-03, 53-04]
files_modified:
  - .planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md
  - .planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "La suite completa (541+) + tsc -b + lockdown-guard verdes ANTES del redeploy"
    - "PROD re-desplegado con los fixes de orientación (docker-cf-build.sh + wrangler deploy)"
    - "El re-walkthrough demuestra cada P0 con screenshot before/after contra el PROD re-desplegado"
    - "53-UX-AUDIT.md cerrado: sección Re-walkthrough con estado por fix; anti-insinuación/gates/mt-12 intactos"
  artifacts:
    - path: ".planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/"
      provides: "Screenshots fix-F{NN}-before.jpg / fix-F{NN}-after.jpg por cada P0"
    - path: ".planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md"
      provides: "Sección Re-walkthrough completa (before/after por P0, versión desplegada)"
      contains: "Re-walkthrough"
  key_links:
    - from: "PROD re-desplegado"
      to: "docker-cf-build.sh + npx wrangler deploy"
      via: "build Linux Docker + docker cp + wrangler deploy (OAuth)"
      pattern: "wrangler deploy"
---

<objective>
Cerrar el ciclo único de la fase: correr el gate de no-regresión (suite + tsc + guard), re-desplegar PROD con el patrón autorizado (docker-cf-build.sh + wrangler local), y hacer UN re-walkthrough de verificación con screenshots before/after por cada P0 contra el PROD re-desplegado, cerrando `53-UX-AUDIT.md`.

Purpose: Success Criteria 2 y 4 de ROADMAP Phase 53 ("TODOS los P0 corregidos con suite verde y redeploy; re-walkthrough demuestra cada fix before/after" + "cero regresión"). El CONTEXT lock: 1 redeploy al final, sin loop abierto.
Output: PROD re-desplegado + evidencia before/after + informe cerrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-CONTEXT.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-RESEARCH.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md
@scripts/bros-cli.mjs

# Patrón de deploy en 53-CONTEXT §code_context y 53-RESEARCH §Supporting:
# docker-cf-build.sh (build Linux node:22 vía Docker Desktop — Start-Process si no corre)
# + docker cp + cd app && npx wrangler deploy (OAuth). Build Windows está ROTO (500ea) — SIEMPRE Docker Linux.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de no-regresión (suite + tsc + lockdown-guard) ANTES del redeploy</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md</files>
  <action>
Correr la suite completa `pnpm --dir app test -- --run` (541+ tests, incluye lockdown-guard que escanea `.from()` PII y `.rpc` no-allowlisted, y el banned-vocab negative-match) + `pnpm --dir app exec tsc -b` desde repo root. Confirmar verde antes de tocar el deploy (phase gate de 53-RESEARCH §Sampling Rate). Verificar además que el git diff de la fase NO toca ninguna frontera de carril `mt-12`, NO flipea ningún flag `*_PUBLIC_ENABLED`, NO añade DDL/migración, y NO añade headers X-Frame-Options/CSP frame-ancestors (Assumption A4 — el harness depende de que PROD siga frameable). Si algo falla, NO desplegar: devolver el fallo para fix. Registrar el conteo de tests y el resultado del guard en el SUMMARY.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run && pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>Suite completa verde, tsc limpio, lockdown-guard verde; diff sin DDL/flag/mt-12/frame-headers; listo para redeploy.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy PROD (docker-cf-build.sh + wrangler deploy) — auth gate dinámico si OAuth expiró</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md</files>
  <action>
Verificar PRIMERO `cd app && npx wrangler whoami`. Si devuelve error de autenticación (OAuth expirado, Assumption A1), NO intentar login autónomo — crear un checkpoint de autenticación dinámico (pedir al operador `npx wrangler login`) y reintentar tras la señal. Si `whoami` responde autenticado: arrancar Docker Desktop si el daemon no corre (`Start-Process` en PowerShell), ejecutar `docker-cf-build.sh` (build Linux node:22 — el build Windows está ROTO, 500ea) → `docker cp` del artefacto → `cd app && npx wrangler deploy`. El deploy fue autorizado por el operador (2026-07-06). CERO DDL, CERO flags, CERO grants en este deploy (solo el bundle de chrome de navegación). Tras el deploy, capturar la nueva versión desplegada (id de wrangler) y anotarla en el encabezado y en la sección Re-walkthrough de `53-UX-AUDIT.md`. Verificación curl mínima post-deploy: `/`, `/buscar`, `/proyecto/14309-04`, `/parlamentario/D1012`, `/red?seed=D1012`, `/agenda` responden 200; `/red` sigue con `force-dynamic` (no 500 con flag ON).
  </action>
  <verify>
    <automated>cd app && npx wrangler whoami 2>&1 | grep -qiE "account|logged in|email"</automated>
  </verify>
  <done>PROD re-desplegado con los fixes; nueva versión anotada en el informe; curl post-deploy 200 en las rutas clave; /red sin 500. (Si OAuth expiró: checkpoint operador resuelto y deploy completado tras el login.)</done>
</task>

<task type="auto">
  <name>Task 3: Re-walkthrough before/after por P0 + cerrar 53-UX-AUDIT.md</name>
  <files>.planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/, .planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md</files>
  <action>
Ejecutar la Pista B del protocolo (harness iframe 390/1280, retry-once en save_screenshot, sleep 4-6 tras carga, ruta absoluta forward-slash) contra el PROD RE-DESPLEGADO para capturar `fix-F{NN}-after.jpg` por cada P0. El `before` sale de la evidencia ya archivada en la pasada de auditoría (Plan 01) — si un P0 no tenía shot dedicado del estado defectuoso, capturarlo desde la versión previa no es posible tras el deploy; en ese caso usar el screenshot de journey de Plan 01 como before y documentarlo. Correr también una Pista A puntual sobre las rutas fixeadas para confirmar consola limpia y que ningún link de continuación/breadcrumb renderiza 404 (un link a 404 sería un P0 nuevo). Llenar la sección "Re-walkthrough (post-redeploy {versión})" de `53-UX-AUDIT.md`: tabla Fix·Ruta·Before·After·Estado (resuelto/pendiente) por cada P0, confirmando los 3 criterios de orientación (≤2 clicks a home/otras, toda página anuncia dónde estás, ningún callejón sin salida) en desktop y móvil. Cerrar todas las páginas ocultas de BrowserOS al terminar. Marcar el informe como cerrado (P0 corregidos N/N).
  </action>
  <verify>
    <automated>test $(ls ".planning/phases/53-uxnav-auditoria-ux-navegada/ux-evidence/"fix-*-after.jpg 2>/dev/null | wc -l) -ge 1 && grep -q "Re-walkthrough" ".planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md"</automated>
  </verify>
  <done>Cada P0 tiene before/after; la sección Re-walkthrough del informe está completa con estado por fix y la versión desplegada; los 3 criterios de orientación verificados desktop+móvil; páginas BrowserOS cerradas; informe cerrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → wrangler/Cloudflare | Deploy con OAuth del operador; auth gate dinámico si expiró |
| harness iframe → PROD re-desplegado | Framing cross-origin; solo sitio público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05-01 | Elevation of Privilege | wrangler deploy | mitigate | Deploy autorizado 2026-07-06; auth gate dinámico (checkpoint operador) si OAuth expiró — el agente NUNCA hace login autónomo |
| T-53-05-02 | Tampering | deploy introduce regresión | mitigate | Gate suite+tsc+guard verde ANTES del deploy; curl post-deploy en rutas clave; diff sin DDL/flag/mt-12 |
| T-53-05-03 | Denial of Service | deploy añade frame-headers | mitigate | Verificación explícita: el deploy NO añade X-Frame-Options/CSP frame-ancestors (Assumption A4) |
</threat_model>

<verification>
- `pnpm --dir app test -- --run` + `tsc -b` verdes ANTES del deploy
- `npx wrangler whoami` autenticado (o checkpoint operador resuelto)
- curl post-deploy 200 en /, /buscar, /proyecto/14309-04, /parlamentario/D1012, /red?seed=D1012, /agenda
- `ux-evidence/fix-*-after.jpg` presentes; sección Re-walkthrough de 53-UX-AUDIT.md completa
- CERO DDL/flag/grant en el deploy; mt-12 y gates existentes intactos
</verification>

<success_criteria>
Todos los P0 corregidos con suite verde y PROD re-desplegado; el re-walkthrough demuestra cada fix before/after y confirma los 3 criterios de orientación desktop+móvil; cero regresión (ROADMAP Phase 53 SC2 + SC4).
</success_criteria>

<output>
Create `.planning/phases/53-uxnav-auditoria-ux-navegada/53-05-SUMMARY.md` when done. Incluir la versión desplegada y el conteo P0 corregidos N/N.
</output>
