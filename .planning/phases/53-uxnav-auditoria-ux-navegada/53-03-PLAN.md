---
phase: 53-uxnav-auditoria-ux-navegada
plan: 03
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - app/components/breadcrumbs.tsx
  - app/components/breadcrumbs.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/app/parlamentario/[id]/page.tsx
  - app/components/parlamentario-header.tsx
  - app/app/contraparte/[id]/page.tsx
  - app/app/parlamentario/[id]/page.test.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "Cada ficha (proyecto/parlamentario/contraparte) muestra 'Inicio / {sección} / {actual}' arriba del h1 — el visitante sabe dónde está"
    - "Los segmentos Inicio y sección son links; el segmento actual es texto plano con aria-current='page'"
    - "Desde una ficha se vuelve a Inicio o a la sección en 1 click (breadcrumb) — soporta el criterio ≤2 clicks"
    - "El breadcrumb es un <nav> arriba del h1, nunca un heading; no re-nivela h1–h3; no mueve el carril mt-12"
  artifacts:
    - path: "app/components/breadcrumbs.tsx"
      provides: "Server Component presentacional puro (props literales, cero JS, cero usePathname)"
      min_lines: 20
    - path: "app/components/breadcrumbs.test.tsx"
      provides: "Test RTL: links/current/aria-current/mono, sin heading"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "app/components/breadcrumbs.tsx"
      via: "render <Breadcrumbs items=[Inicio /, Proyectos /buscar, Boletín {n}]>"
      pattern: "Breadcrumbs"
    - from: "app/components/parlamentario-header.tsx"
      to: "app/components/breadcrumbs.tsx"
      via: "render <Breadcrumbs> con nombre desde el RPC cacheado"
      pattern: "Breadcrumbs"
---

<objective>
Crear un componente `Breadcrumbs` server puro y montarlo en las 3 fichas (proyecto, parlamentario, contraparte) para que toda página de detalle anuncie dónde estás y ofrezca vuelta a Inicio/sección en 1 click. Remedio contratado por 53-UI-SPEC §(b) para el gap "no sé dónde estoy" (en `/proyecto/*` y `/contraparte/*` NINGÚN ítem del nav queda activo — el breadcrumb es el remedio, no un ítem nuevo).

Purpose: Success Criterion 3 de ROADMAP Phase 53 ("toda página muestra dónde estás; volver al home / saltar a otra sección en ≤2 clicks").
Output: `breadcrumbs.tsx` + test + 3 fichas cableadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UI-SPEC.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-RESEARCH.md
@app/components/parlamentario-header.tsx

<interfaces>
<!-- El markup completo (clases, aria) y el contrato de props del Breadcrumbs están en
     53-UI-SPEC §Component Inventory y 53-RESEARCH §Breadcrumb Pattern (código de referencia).
     Puntos de inserción verificados file:line en 53-RESEARCH §Navigation Inventory. -->

Contrato de props (53-UI-SPEC §Component Inventory):
  items: { label: string; href?: string; mono?: boolean }[]   // último sin href = segmento actual
  <nav aria-label="Ruta de navegación"><ol class="flex flex-wrap items-center gap-x-1 text-sm text-muted-foreground mb-4">
  link: inline-flex min-h-11 items-center underline-offset-4 hover:underline hover:text-accent-product focus-visible:ring-2 focus-visible:ring-ring rounded-md
  separador: <span aria-hidden="true">/</span>   |   actual: <span aria-current="page" class="text-foreground"> (mono → font-mono)

Puntos de inserción (primer hijo dentro del container max-w, ANTES del h1, sin full-bleed, sin tocar mt-12):
  - proyecto/[boletin]/page.tsx:50 — antes del <Suspense><FichaSection> (el crumb NO necesita datos; boletín es route param)
  - parlamentario/[id]/page.tsx:116 — el nombre vive en parlamentario-header.tsx (usa parlamentario.nombre del RPC getParlamentarioPublico, React.cache F52 → cero RPC extra)
  - contraparte/[id]/page.tsx:62 — DESPUÉS del notFound() del MONEY gate (invisible en PROD hoy, future-proof)

Crumbs por ficha (53-UI-SPEC §(b)):
  proyecto:    [Inicio→/, Proyectos→/buscar, "Boletín {boletin}" mono]   (no existe /proyectos; /buscar es la superficie de hallazgo)
  parlamentario: [Inicio→/, Parlamentarios→/parlamentarios, {nombre}]     (nombre as-shipped; Title Case es F54, NO esta fase)
  contraparte: [Inicio→/, {nombre}]                                        (crumb 2 OMITIDO — no hay ruta de listado)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear Breadcrumbs (server puro) + test RTL (Wave 0)</name>
  <files>app/components/breadcrumbs.tsx, app/components/breadcrumbs.test.tsx</files>
  <behavior>
    - <nav aria-label="Ruta de navegación"> con <ol>; N ítems renderizan N-1 separadores "/" (aria-hidden)
    - ítem con href → <Link> navegable; ítem sin href → <span aria-current="page"> texto plano (no es link)
    - ítem con mono:true → clase font-mono en el segmento (p.ej. "Boletín 14309-04")
    - el componente NO renderiza ningún h1/h2/h3 (es un <nav>, no un heading)
    - todos los links llevan min-h-11 (touch target 44px)
  </behavior>
  <action>
Crear `app/components/breadcrumbs.tsx` como Server Component presentacional puro (SIN "use client", SIN usePathname — cada página pasa sus crumbs literales) siguiendo el markup/clases/aria del contrato en el bloque <interfaces> y 53-RESEARCH §Breadcrumb Pattern. Separador = glifo Unicode `/` inline (aria-hidden); CERO dependencia nueva (NO instalar el bloque breadcrumb de shadcn ni lucide — Pitfall/Don't Hand-Roll de 53-RESEARCH). Crear el test RTL `breadcrumbs.test.tsx` con las aserciones del bloque <behavior>. Este es el Wave 0 del componente nuevo.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run components/breadcrumbs.test.tsx --run && pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>breadcrumbs.tsx existe (server puro, cero JS), test RTL verde, tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear breadcrumb en fichas proyecto y contraparte</name>
  <files>app/app/proyecto/[boletin]/page.tsx, app/app/contraparte/[id]/page.tsx</files>
  <action>
En `proyecto/[boletin]/page.tsx` (punto :50), renderizar `<Breadcrumbs>` como primer hijo del container `<main>` (max-w, px — nunca full-bleed), ANTES del `<Suspense><FichaSection>`, con crumbs `[{label:"Inicio",href:"/"}, {label:"Proyectos",href:"/buscar"}, {label:\`Boletín ${boletin}\`, mono:true}]` (el boletín es route param, no necesita el fetch). En `contraparte/[id]/page.tsx` (punto :62), renderizar `<Breadcrumbs>` DESPUÉS del `notFound()` del MONEY gate (primera sentencia intacta), con crumbs `[{label:"Inicio",href:"/"}, {label:nombre}]` (crumb 2 omitido — no hay ruta de listado; per 53-UI-SPEC §(b)). Invisible en PROD de esta fase (gate OFF → notFound), future-proof, inofensivo. NO mover ni anidar ningún carril `mt-12`; NO añadir/re-nivelar h1.
  </action>
  <verify>
    <automated>pnpm --dir app exec tsc -b && grep -q "Breadcrumbs" "app/app/proyecto/[boletin]/page.tsx" && grep -q "Breadcrumbs" "app/app/contraparte/[id]/page.tsx"</automated>
  </verify>
  <done>Ambas fichas renderizan Breadcrumbs en el punto correcto; proyecto con boletín mono; contraparte tras el gate; tsc limpio; mt-12 intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Cablear breadcrumb en ficha parlamentario (nombre desde RPC cacheado) + extender page test</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/components/parlamentario-header.tsx, app/app/parlamentario/[id]/page.test.tsx</files>
  <action>
Montar `<Breadcrumbs>` para la ficha parlamentario usando `parlamentario.nombre` (columna pública del RPC `getParlamentarioPublico`, ya cacheado con React.cache F52 → cero RPC extra; 53-RESEARCH Open Question 2). Renderizarlo dentro de `parlamentario-header.tsx` como primer elemento de su salida (el componente ya tiene `parlamentario.nombre`), o en `page.tsx:116` pasando el nombre — decisión: DENTRO de parlamentario-header (tiene el dato, evita duplicar el fetch). Crumbs `[{label:"Inicio",href:"/"}, {label:"Parlamentarios",href:"/parlamentarios"}, {label: parlamentario.nombre}]`. El nombre va as-shipped (Title Case es F54, NO esta fase). El breadcrumb va arriba del `<h1>` del header, dentro del container existente, sin full-bleed, sin re-nivelar el h1. Extender `parlamentario/[id]/page.test.tsx` (existe) con una aserción de que el breadcrumb renderiza los 3 crumbs correctos (Inicio link, Parlamentarios link, nombre como aria-current text). NO tocar partido/rut/foto (LEGAL-03 — el header ya los omite).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/parlamentario --run && pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>La ficha parlamentario muestra breadcrumb con nombre real arriba del h1 sin RPC extra; page.test.tsx extendido y verde; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Breadcrumbs props → render | Props literales de página (no user input); nombre = columna pública shipped |
| contraparte page → gate | Breadcrumb va DESPUÉS del notFound() del MONEY gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Information Disclosure | breadcrumbs.tsx | mitigate | Renderiza SOLO labels de ruta + nombre público shipped (nombre ya visible en el h1); cero PII/partido/foto; lockdown-guard sigue en la suite |
| T-53-03-02 | Access Control | contraparte breadcrumb | mitigate | Crumb DESPUÉS del gate `notFound()` (V4 ASVS): con MONEY OFF la ruta entera es 404, el crumb nunca se sirve — sin filtración de existencia |
| T-53-03-03 | Information Disclosure | crumb de contraparte enlazando ruta gated | accept | Ningún crumb apunta a `/contraparte` desde afuera; el link a superficie gated (404) está prohibido por la matriz de legalidad — no se introduce |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run components/breadcrumbs.test.tsx app/parlamentario` verde
- `pnpm --dir app exec tsc -b` limpio
- `grep Breadcrumbs` presente en las 3 páginas de ficha
- `mt-12` intacto (git diff no toca fronteras de carril); ningún h1 nuevo/re-nivelado
</verification>

<success_criteria>
Las 3 fichas anuncian dónde estás con un breadcrumb ligero server-only y permiten volver a Inicio/sección en 1 click (ROADMAP Phase 53 SC3), sin JS, sin dependencia nueva, sin tocar la capa de datos ni el carril mt-12.
</success_criteria>

<output>
Create `.planning/phases/53-uxnav-auditoria-ux-navegada/53-03-SUMMARY.md` when done.
</output>
