---
phase: 53-uxnav-auditoria-ux-navegada
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - app/components/header-nav.tsx
  - app/components/header-nav.test.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "El header muestra 5 ítems (Buscar · Parlamentarios · Agenda · Red · Sobre); /red deja de ser huérfana del header"
    - "El ítem de la sección actual queda marcado (subrayado petróleo + aria-current) — active-state intacto por prefix-match"
    - "A 390px los 5 ítems + wordmark caben en ≤2 filas (wordmark + 1 fila de nav) — el label 'Sobre' acortado lo permite"
  artifacts:
    - path: "app/components/header-nav.tsx"
      provides: "NAV_ITEMS con 5 ítems incl. /red y label 'Sobre'"
      contains: "href: \"/red\""
    - path: "app/components/header-nav.test.tsx"
      provides: "Test RTL del HeaderNav (5 ítems, hrefs, label 'Sobre', active por prefix)"
  key_links:
    - from: "app/components/header-nav.tsx NAV_ITEMS"
      to: "/red"
      via: "static <Link>"
      pattern: "/red"
---

<objective>
Extender la nav global para que los 5 destinos del sitio (incluida `/red`, LIVE desde 2026-07-02) sean alcanzables desde el header y la sección actual quede visualmente marcada. Remedio contratado por 53-UI-SPEC §(a) para el gap de orientación "no sé a dónde puedo ir" y para la orfandad de `/red`.

Purpose: Success Criterion 3 de ROADMAP Phase 53 ("saltar a las demás secciones en ≤2 clicks"). `/red` hoy solo se alcanza desde el link "Ver relaciones" de una ficha; con el ítem de nav pasa a 1 click desde cualquier página.
Output: `header-nav.tsx` con 5 ítems + test RTL de cobertura (Wave 0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UI-SPEC.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-RESEARCH.md
@app/components/header-nav.tsx

<interfaces>
<!-- Estado shipped de header-nav.tsx (extraído del código). El executor SOLO edita el array + comentario. -->

Isla "use client" (usePathname es Client-only en Next 16). `esActivo(pathname, href)` = prefix-match shipped (línea 34-37) — NO se toca. NAV_ITEMS es `readonly NavItem[]` con `{ href, label }`. El active-underline petróleo (`text-accent-product underline decoration-2 ... underline-offset-8`) y `min-h-11` YA existen — se heredan sin cambio para el ítem Red.

Estado shipped (línea 26-32):
  // LOCKED (UI-SPEC §11.0): Buscar · Parlamentarios · Agenda · Sobre / Metodología.
  const NAV_ITEMS = [ Buscar /buscar, Parlamentarios /parlamentarios, Agenda /agenda, "Sobre / Metodología" /sobre ]  // 4 ítems, SIN Red
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test RTL del HeaderNav (RED)</name>
  <files>app/components/header-nav.test.tsx</files>
  <behavior>
    - Renderiza exactamente 5 ítems de nav con hrefs [/buscar, /parlamentarios, /agenda, /red, /sobre] en ese orden
    - El ítem 4 tiene label "Red" y href "/red"
    - El ítem 5 tiene label exacto "Sobre" (NO "Sobre / Metodología")
    - Con pathname "/red" (mock de usePathname) el ítem Red lleva aria-current="page"; con "/parlamentario/D1012" NINGÚN ítem queda activo (no existe ítem "Proyectos"/parlamentario-ficha — el breadcrumb es el remedio de orientación en fichas, no un ítem nuevo)
    - Con pathname "/parlamentarios/D123" (subárbol) el ítem Parlamentarios queda activo (prefix-match)
  </behavior>
  <action>
Crear `app/components/header-nav.test.tsx` (no existe — Wave 0 gap de 53-RESEARCH §Validation Architecture). Mockear `usePathname` de `next/navigation` (patrón de tests de islas existentes en el repo). Aserciones por rol/href. Este test FALLA contra el código shipped (4 ítems, label largo) — es el RED del ciclo. Ejecutar `pnpm --dir app exec vitest run components/header-nav.test.tsx` y confirmar el fallo esperado antes de la Task 2.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run components/header-nav.test.tsx 2>&1 | grep -qE "fail|FAIL|✗|expected"</automated>
  </verify>
  <done>header-nav.test.tsx existe con las aserciones de 5 ítems/hrefs/label/active y FALLA contra el código shipped (RED confirmado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender NAV_ITEMS (+Red, label "Sobre") — GREEN</name>
  <files>app/components/header-nav.tsx</files>
  <behavior>
    - Los tests de la Task 1 pasan (5 ítems, orden, label "Sobre", active-state)
  </behavior>
  <action>
Editar SOLO el array `NAV_ITEMS` de `header-nav.tsx` per 53-UI-SPEC §(a) y §Component Inventory: orden por journey Buscar · Parlamentarios · Agenda · **Red** · Sobre — añadir `{ href: "/red", label: "Red" }` en posición 4, y acortar el ítem 5 a `{ href: "/sobre", label: "Sobre" }`. El label es el nombre de la ruta, factual; NUNCA "Red de influencia"/"Conexiones" (framing prohibido, banned-vocab). Todo lo demás byte-idéntico: `esActivo`, aria-current, `min-h-11`, underline petróleo activo, `flex-wrap` (colapso sin JS), sin hamburguesa, isla sin props nuevas — CERO JS cliente nuevo. Actualizar el comentario `// LOCKED (UI-SPEC §11.0)` para referenciar el re-open autorizado del 53-UI-SPEC §(a) (Pitfall 5 de 53-RESEARCH: no dejar el comentario apuntando al contrato viejo). Metodología sigue alcanzable desde `/sobre` y el footer — el acortamiento es para que 5 ítems quepan a 390px.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run components/header-nav.test.tsx --run && pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>NAV_ITEMS tiene 5 ítems con /red en posición 4 y label "Sobre"; test verde; tsc limpio; comentario LOCKED actualizado al 53-UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HeaderNav island → cliente | Isla "use client" sin props sensibles; solo `<Link>`s estáticos + estado UI local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Information Disclosure | header-nav.tsx | mitigate | El ítem Red solo expone una ruta ya LIVE (2026-07-02); añadir el link NO enciende nada nuevo, cero DDL/flag/grant. Isla no recibe env/keys |
| T-53-02-02 | Tampering | copy del label | mitigate | Label "Red" = nombre de ruta factual; banned-vocab negative-match de la suite veta "Red de influencia"/"Conexiones" |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run components/header-nav.test.tsx` verde (5 ítems, active-state)
- `pnpm --dir app exec tsc -b` limpio
- Suite completa + lockdown-guard sin regresión (se corre en el gate de Wave 3)
</verification>

<success_criteria>
El header ofrece los 5 destinos del sitio incl. /red en ≤1 click y marca la sección actual (ROADMAP Phase 53 SC3), sin JS nuevo, sin tocar la capa de datos, anti-insinuación intacta.
</success_criteria>

<output>
Create `.planning/phases/53-uxnav-auditoria-ux-navegada/53-02-SUMMARY.md` when done.
</output>
