---
phase: 53-uxnav-auditoria-ux-navegada
plan: 04
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - app/app/buscar/page.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
  - app/app/agenda/page.tsx
  - app/components/red/red-graph.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "Cada empty state que la auditoría marca como callejón sin salida gana UNA línea de continuación con un solo link a una superficie con datos (/buscar, /agenda, /parlamentarios)"
    - "El párrafo honesto shipped queda byte-idéntico arriba; la línea nueva es un <p> hermano (mt-2) con su propio assert"
    - "Los tres estados honestos siguen distintos: 'no ingestado' sigue diciendo que el dato se incorpora; 'sin resultados' sigue diciendo consultado-con-cero; la línea solo añade una ruta, nunca re-enmarca el hecho"
    - "Ningún link de continuación apunta a una superficie gated/vacía ni a una URL externa; secciones que degradan con null NO reciben línea"
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "Línea de continuación en el grafo vacío (isla existente, no crea isla nueva)"
  key_links:
    - from: "empty states flagged por 53-UX-AUDIT.md"
      to: "/buscar | /agenda | /parlamentarios"
      via: "un solo <Link> petróleo con glifo → aria-hidden"
      pattern: "text-accent-product"
---

<objective>
Añadir UNA línea de continuación orientadora a cada empty state honesto que la auditoría (53-UX-AUDIT.md) marque como callejón sin salida (P0 "no puedo llegar a X"), y aplicar cualquier P0 in-contract restante cuyo remedio sea una línea de continuación o un cross-link ya-legal. Remedio contratado por 53-UI-SPEC §(c).

Purpose: Success Criterion 3 de ROADMAP Phase 53 ("ningún callejón sin salida"). Un visitante que aterriza en una ficha con datos vacíos (votos/lobby no ingeridos, búsqueda sin resultados, semana sin citaciones, grafo vacío) recibe una salida a una superficie con datos, sin fabricar virtud.
Output: empty-state components tocados (solo los flagged) + asserts nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UI-SPEC.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-RESEARCH.md
@.planning/phases/53-uxnav-auditoria-ux-navegada/53-UX-AUDIT.md

<interfaces>
<!-- Superficies candidatas con file:line + copy shipped VERBATIM en 53-RESEARCH §Navigation Inventory > Empty states.
     Copy prescrito por superficie en 53-UI-SPEC §(c). Patrón del <p> hermano en 53-RESEARCH §Code Examples. -->

Patrón (53-UI-SPEC §(c)): shipped honest <p> intacto; nuevo <p class="text-sm mt-2"> con UN solo <Link className="text-accent-product underline underline-offset-2"> y el glifo → como <span aria-hidden="true"> DENTRO del texto del link. Target = ruta de nav con datos (/buscar, /agenda, /parlamentarios), NUNCA ancla in-page de sección posible-vacía, NUNCA URL externa.

Copy prescrito por superficie (53-UI-SPEC §(c)):
  /buscar sin resultados → "También puedes revisar [la agenda legislativa de la semana →](/agenda)." (evita el eco del shipped que ya dice "Prueba con otras palabras…"; ver 53-RESEARCH Open Question 1 — cae en Claude's Discretion, dejar constancia en el informe)
  ficha parl. votos sin registros → "Puedes explorar [otros parlamentarios en el directorio →](/parlamentarios)."
  ficha parl. lobby no ingestado / cero confirmadas → "Mientras tanto, puedes [buscar un proyecto de ley por su idea →](/buscar)."
  /agenda sin citaciones → "Puedes [buscar un proyecto de ley por su idea →](/buscar)."
  /red grafo vacío → "Vuelve al [directorio de parlamentarios →](/parlamentarios)."
  home actualidad → NO línea (home no es dead end — NO TOCAR actualidad-module.tsx)

Superficies file:line (53-RESEARCH §Navigation Inventory):
  buscar/page.tsx:80-90 (server) · votos-por-parlamentario.tsx:462-466 (server) · lobby-de-parlamentario.tsx:296-300 y :310-314 (server) ·
  agenda/page.tsx:294-297 (server) · red/red-graph.tsx:158-165 (CLIENTE — isla "use client" existente, añadir el Link ahí NO crea isla nueva)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar líneas de continuación a las superficies flagged (según 53-UX-AUDIT.md)</name>
  <files>app/app/buscar/page.tsx, app/components/votos-por-parlamentario.tsx, app/components/lobby-de-parlamentario.tsx, app/app/agenda/page.tsx, app/components/red/red-graph.tsx</files>
  <action>
Leer la lista de P0 "callejón sin salida" en `53-UX-AUDIT.md` y aplicar la línea de continuación SOLO a las superficies que el informe marca (regla del 53-UI-SPEC: superficie no marcada = copy byte-idéntico). Para cada superficie flagged, tomar el copy EXACTO de la tabla del bloque <interfaces> (53-UI-SPEC §(c)) y añadir un `<p class="text-sm mt-2">` HERMANO debajo del párrafo honesto shipped, con UN solo `<Link>` petróleo y el glifo `→` como `<span aria-hidden="true">` dentro del texto del link. El shipped honest string queda byte-idéntico (Pitfall 4 de 53-RESEARCH: no romper los `getByText` exactos). Guard semántico LOCKED (53-UI-SPEC): la línea NUNCA difumina los tres estados honestos — "no ingestado" sigue diciendo que el dato se incorpora, "sin resultados" sigue diciendo consultado-con-cero; solo añade una ruta. PROHIBIDO fabricar virtud ("limpio/transparente/nada que ocultar") — banned-vocab negative-match aplica a todo string nuevo. Secciones que degradan renderizando `null` (MONEY/cruces gated, RPC-pre-apply) NO reciben línea (el nodo se queda ausente). NO TOCAR `actualidad-module.tsx` (home no es dead end). La línea de `/red` vive en `red-graph.tsx` (isla cliente existente — permitido, no crea isla nueva).
  </action>
  <verify>
    <automated>pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>Cada superficie flagged por el informe tiene su línea de continuación con el copy prescrito; el shipped honest string byte-idéntico arriba; superficies no marcadas intactas; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Asserts RTL de la línea nueva + shipped byte-idéntico</name>
  <files>app/components/votos-por-parlamentario.test.tsx, app/components/lobby-de-parlamentario.test.tsx</files>
  <action>
Para CADA superficie flagged tocada en la Task 1 (sin excepción — plan-checker W3): añadir un assert que verifica (a) el párrafo honesto shipped sigue presente byte-idéntico (getByText exacto existente sigue pasando), y (b) la línea de continuación renderiza exactamente UN link con el href prescrito y el texto prescrito. Si la superficie no tiene test file, CREAR un test mínimo de componente/página (render + los dos asserts) — no basta "documentar la cobertura". NO cambiar los asserts existentes del copy honesto (deben seguir verdes sin edición). Verificar que el banned-vocab negative-match de la suite pasa sobre los strings nuevos.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run components/votos-por-parlamentario.test.tsx components/lobby-de-parlamentario.test.tsx --run && pnpm --dir app exec tsc -b</automated>
  </verify>
  <done>Cada línea nueva tiene su propio assert (1 link, href+texto correctos); los asserts del shipped honest copy siguen verdes sin cambios; banned-vocab negative-match verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| línea de continuación → ruta de nav | Solo apunta a superficies con datos (/buscar, /agenda, /parlamentarios); nunca a superficie gated (404) ni externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Tampering | copy de la línea | mitigate | Guard semántico LOCKED: la línea añade ruta, nunca verdicto; banned-vocab negative-match veta "limpio/transparente/nada que ocultar" y lenguaje causal/afinidad/score |
| T-53-04-02 | DoS UX / Information Disclosure | link a superficie gated | mitigate | Targets restringidos a rutas de nav con datos; matriz de legalidad del 53-UI-SPEC prohíbe link que renderice 404 en PROD |
| T-53-04-03 | Tampering | asserts de estados honestos | mitigate | shipped honest string byte-idéntico (test-asserted); las secciones null-degradadas no reciben línea (nodo ausente) |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run` de los componentes tocados verde (línea nueva + shipped intacto)
- `pnpm --dir app exec tsc -b` limpio
- banned-vocab negative-match de la suite verde sobre los strings nuevos
- `actualidad-module.tsx` sin cambios (git diff vacío); superficies no-flagged con copy byte-idéntico
</verification>

<success_criteria>
Ningún empty state flagged queda como callejón sin salida: cada uno ofrece una salida a una superficie con datos con una sola línea honesta (ROADMAP Phase 53 SC3), sin fabricar virtud, sin difuminar los tres estados honestos, sin tocar la capa de datos.
</success_criteria>

<output>
Create `.planning/phases/53-uxnav-auditoria-ux-navegada/53-04-SUMMARY.md` when done. Listar qué superficies recibieron línea y cuáles el informe dejó sin marcar.
</output>
