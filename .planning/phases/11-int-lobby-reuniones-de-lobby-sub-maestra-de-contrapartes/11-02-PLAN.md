---
phase: 11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lobby/package.json
  - packages/lobby/tsconfig.json
  - packages/lobby/vitest.config.ts
  - packages/lobby/src/model.ts
  - packages/lobby/src/parse-leylobby.ts
  - packages/lobby/src/parse-leylobby.test.ts
  - packages/lobby/src/connector-leylobby.ts
  - packages/lobby/src/reconciliar-sujeto.ts
  - packages/lobby/src/reconciliar-sujeto.test.ts
  - packages/lobby/src/writer.ts
  - packages/lobby/src/writer.test.ts
  - packages/lobby/src/writer-supabase.ts
  - packages/lobby/src/ingest-run.ts
  - packages/lobby/src/ingest-run.test.ts
  - packages/lobby/src/ingest-cli.ts
  - packages/lobby/src/live-leylobby.probe.ts
  - packages/lobby/src/index.ts
  - packages/lobby/test/fixtures/audiencias-congreso.html
autonomous: false
requirements: [INT-01, INT-02]
user_setup: []

must_haves:
  truths:
    - "El parser mapea un fixture HTML real de audiencias de leylobby a LobbyAudiencia[] keyed por el cell Identificador (no el número de URL)"
    - "Cada asistente no-Sujeto-Pasivo se convierte en una fila de contraparte cruda; contraparte_id queda NULL"
    - "El sujeto pasivo cruza vía correrPipeline; SOLO determinista mintea un EnlaceConfirmado y puebla el FK; el resto deja NULL + mención cruda"
    - "El writer es idempotente: dos corridas con el mismo input producen conteos idénticos"
    - "Un drift estructural BLOQUEA la corrida (no emite filas vacías que se lean como 'sin lobby')"
    - "Una corrida LIVE acotada o degrada a fixture; nunca fabrica filas"
  artifacts:
    - path: "packages/lobby/src/parse-leylobby.ts"
      provides: "Parser cheerio del HTML de audiencias → LobbyAudiencia[] keyed por Identificador"
      min_lines: 40
    - path: "packages/lobby/src/reconciliar-sujeto.ts"
      provides: "Cruce del sujeto pasivo vía correrPipeline; mint de EnlaceConfirmado solo en determinista"
      contains: "correrPipeline"
    - path: "packages/lobby/src/writer-supabase.ts"
      provides: "SupabaseLobbyWriter idempotente por clave natural; escribe lobby_audiencia, lobby_contraparte, lobby_ingesta_estado"
      contains: "onConflict"
  key_links:
    - from: "packages/lobby/src/reconciliar-sujeto.ts"
      to: "@obs/identity confirmar"
      via: "import + mint en rama determinista"
      pattern: "confirmar\\("
    - from: "packages/lobby/src/connector-leylobby.ts"
      to: "@obs/ingest assertAllowedUrl + robots + rateLimiter + fetcher"
      via: "orden LOCKED"
      pattern: "assertAllowedUrl"
    - from: "packages/lobby/src/writer-supabase.ts"
      to: "lobby_audiencia / lobby_contraparte / lobby_ingesta_estado"
      via: "upsert onConflict por clave natural"
      pattern: "lobby_contraparte"
---

<objective>
Construir el paquete `@obs/lobby`: conector + parser + reconciliación del sujeto pasivo + writer idempotente + orquestación, espejando `@obs/agenda` casi archivo-por-archivo. Ingesta las audiencias de la Ley del Lobby desde `leylobby.gob.cl` por HTML+cheerio (la ruta bulk CSV está muerta: 503), construye la sub-maestra de contrapartes (texto crudo), cruza el sujeto pasivo contra la maestra vía `correrPipeline` (FK solo-determinista vía `EnlaceConfirmado`), y persiste a las tablas de Plan 01 con drift BLOQUEANTE.

Purpose: Es la pieza de ingesta de INT-01. Toda la política (rate-limit, robots, SSRF, R2, drift) se reusa de `@obs/ingest` en el ORDEN LOCKED; la guarda de identidad se reusa de Phase 9. El único código genuinamente nuevo es el parser cheerio de UNA forma HTML, la reconciliación del sujeto pasivo, y el writer.
Output: el paquete `packages/lobby` + fixture de congreso + tests vitest. La corrida LIVE acotada + escritura a Supabase es checkpoint de operador (autonomous:false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-RESEARCH.md
@.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-VALIDATION.md

<read_first>
- `packages/agenda/src/connector-camara.ts` — el patrón de conector a espejar: orden LOCKED (`assertAllowedUrl → robots.isAllowed → rateLimiter.wait → fetcher.get`), `BROWSER_HEADERS`, NUNCA `BaseConnector.run`, y el manejo de un 403 de WAF como error tipado (`CamaraBloqueadaError`).
- `packages/agenda/src/parse-camara-citaciones.ts` — el patrón de parser cheerio (importa `* as cheerio`, valida cada fila con zod, descarta+registra lo que no valida; nunca fabrica).
- `packages/agenda/src/writer-supabase.ts` — el writer idempotente: `dedupePorClave` + `chunk` + `upsert(filas, { onConflict })`, raíz ANTES que hijos, dedupe-before-batch (Postgres aborta un lote con dos filas de la misma clave de conflicto).
- `packages/agenda/src/writer.ts` — la interfaz `AgendaWriter` + `InMemoryAgendaWriter` (espejar como `LobbyWriter` + `InMemoryLobbyWriter` para los tests).
- `packages/agenda/src/ingest-run.ts` — la orquestación tolerante con DEGRADACIÓN HONESTA (`Degradacion`, errores por clave, nunca aborta toda la corrida, nunca fabrica filas).
- `packages/agenda/src/ingest-cli.ts` — el entry-point que arma los colaboradores REALES de `@obs/ingest` + el writer Supabase, toma la service key SOLO de env (nunca argv), degrada a dry-run sin key.
- `packages/tramitacion/src/reconciliar-senado.ts` — el choke point de reconciliación: `correrPipeline(mencion, maestra, provider, writer)` → `switch(res.tipo)`; SOLO `determinista` llama `confirmar(id, "determinista")` y puebla el FK; `probable`/`revision`/`no_confirmado` → `enlace: null`. Usa `normalizarNombre({ libre })` y `MencionForanea`. Provider/writer inyectables con defaults seguros (NOOP_WRITER, PROVIDER_AUSENTE que lanza si se invoca).
- `packages/ingest/src/robots.ts` — `RobotsGuard.isAllowed`: un robots.txt con `!res.ok` (incluido 403) hace `robotsParser(robotsUrl, "")` → fail-OPEN (permitido). Esto resuelve Open Question 1: el 403-robots de leylobby permite el fetch. Confirmar y documentar; NO cambiar el comportamiento salvo que se necesite un override por-host.
- `.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-RESEARCH.md` §"leylobby Access" (rutas live: `/instituciones/{CODE}/audiencias/{year}` paginado `?page=N`; detail con el cell `Identificador`), §"Field map", §"Connector Reuse" (tabla archivo-por-archivo), §"Open Questions" (1 robots, 2 código de congreso + fixture, 3 marcador de ingesta), §"Common Pitfalls".
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold del paquete + modelo + parser cheerio (con fixture de congreso)</name>
  <files>packages/lobby/package.json, packages/lobby/tsconfig.json, packages/lobby/vitest.config.ts, packages/lobby/src/model.ts, packages/lobby/src/parse-leylobby.ts, packages/lobby/src/parse-leylobby.test.ts, packages/lobby/test/fixtures/audiencias-congreso.html, packages/lobby/src/index.ts</files>
  <behavior>
    - Parsea el fixture HTML de audiencias → `LobbyAudiencia[]`, cada una keyed por el cell `Identificador` (`{INST}AW{N}`), NUNCA el número de URL del listado (Pitfall 1).
    - Cada audiencia tiene: `identificador`, `institucionCodigo`, `fecha` (parseada) + `fechaRaw` (string fuente; si el parseo falla, `fecha` null + `fechaRaw` preservado — nunca fabricar), `materia` (raw), `enlaceDetalle` (raw), y `asistentes: {rol, nombre, representado}[]`.
    - El asistente con `rol === "Sujeto Pasivo"` es el funcionario; los demás son contrapartes.
    - Cada fila se valida con zod (`LobbyAudienciaSchema`); una fila que no valida se descarta y se registra (drift), nunca se fabrica.
    - El parser es column-agnostic respecto al rol del no-sujeto-pasivo (guarda `rol` crudo) — robusto a la taxonomía exacta del congreso (Open Question 2 / Assumption A2).
  </behavior>
  <action>
Scaffold `packages/lobby` espejando `packages/agenda` (mismo `package.json` name pattern `@obs/lobby`, `tsconfig.json`, `vitest.config.ts`). Resolver Open Question 2 ANTES de capturar el fixture:

1. **Resolver el/los código(s) de institución del congreso en leylobby** (Cámara de Diputados, Senado). La lista `/instituciones` es paginada (100+ págs); buscar/paginar `leylobby.gob.cl/instituciones?q=...` para hallar el código. Si no se resuelve un código de congreso, fallback documentado: usar la página de lobby propia de la Cámara (`camara.cl/transparencia/ley_de_lobby.aspx`, ya allowlisted) — pero preferir leylobby. Documentar el/los código(s) hallado(s) en un comentario del conector.

2. **Capturar un fixture HTML real de una página de audiencias de un congreso** (`/instituciones/{CODE_CONGRESO}/audiencias/{year}/{rowId}`) y guardarlo en `packages/lobby/test/fixtures/audiencias-congreso.html`. Esto fija el golden del parser (Wave 0). Respetar el delay 2-3s y el UA identificatorio al capturar. Si leylobby no es alcanzable, capturar de la mejor fuente disponible y marcar el fixture como `human_verification` pendiente en un comentario — nunca inventar HTML.

3. **`model.ts`**: tipos + zod schemas `LobbyAudiencia` (`identificador`, `institucionCodigo`, `parlamentarioId: EnlaceConfirmado-derived | null` en la forma para-escribir, `mencionSujeto`, `fecha`, `fechaRaw`, `materia`, `enlaceDetalle`, provenance) y `LobbyContraparte` (`identificador`, `nombre`, `rol`, `representadoText`, `contraparteId: null`, provenance). Espejar `model.ts` de agenda (forma anidada: asistentes/contrapartes dentro de la audiencia; el writer aplana).

4. **`parse-leylobby.ts`**: parser cheerio que recorre el `<table>` de asistentes de la página de detalle, extrae el cell `Identificador` como clave natural, `Fecha` (+raw), `Materia`, `enlace_detalle`, y las filas `{rol, nombre, representado}`. `ORIGEN = "leylobby-audiencias"`. Validar con zod; descartar+registrar lo no-válido.

5. **`parse-leylobby.test.ts`**: tests vitest contra el fixture — (a) keying por `Identificador` (assertion explícita de que NO usa el número de URL); (b) el sujeto pasivo se identifica por `rol === "Sujeto Pasivo"`; (c) cada no-sujeto-pasivo produce una contraparte; (d) una fila malformada se descarta sin fabricar.
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test -t parse-leylobby</automated>
  </verify>
  <done>El paquete `@obs/lobby` compila; el fixture de congreso existe; `parse-leylobby` mapea el fixture a `LobbyAudiencia[]` keyed por `Identificador`; los tests de keying/sujeto-pasivo/contraparte/descarte pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliación del sujeto pasivo + writer idempotente + interfaz LobbyWriter</name>
  <files>packages/lobby/src/reconciliar-sujeto.ts, packages/lobby/src/reconciliar-sujeto.test.ts, packages/lobby/src/writer.ts, packages/lobby/src/writer.test.ts, packages/lobby/src/writer-supabase.ts</files>
  <behavior>
    - `reconciliarSujeto`: para cada audiencia, el sujeto pasivo cruza vía `correrPipeline`; `determinista` → `confirmar(id, "determinista")` puebla el FK + `estado_vinculo = "confirmado"`; `probable`/`revision`/`no_confirmado` → `enlace: null` + `estado_vinculo: "no_confirmado"` + mención cruda preservada. Cada decisión deja una fila en `identidad_audit` (vía el writer del pipeline).
    - Las contrapartes pasan SIN reconciliación: filas crudas `{nombre, rol, representadoText}`, `contraparteId` SIEMPRE null (Pitfall 4 — un tercero nunca se enlaza a una persona).
    - `SupabaseLobbyWriter`: upsert idempotente — `lobby_audiencia` onConflict `identificador`; `lobby_contraparte` onConflict `(identificador,nombre,rol)`; dedupe-before-batch; raíz antes que hijos. Dos corridas con el mismo input → conteos idénticos.
    - El writer también upserta `lobby_ingesta_estado` (un row por parlamentario tocado en la corrida) para alimentar el marcador "no ingestado" de la ficha.
  </behavior>
  <action>
Espejar `reconciliar-senado.ts` y `writer-supabase.ts`/`writer.ts` de agenda:

1. **`reconciliar-sujeto.ts`**: `reconciliarSujeto(audiencias, maestra, opts)` con `opts: { provider?, writer?, periodo? }` y defaults seguros (NOOP_WRITER, PROVIDER_AUSENTE que lanza si un homónimo lo invoca; los deterministas nunca lo tocan). Por audiencia: `normalizarNombre({ libre: aud.mencionSujeto.trim() })` → `MencionForanea` (camara según la institución, periodo) → `correrPipeline` → `switch(res.tipo)`. SOLO `determinista` llama `confirmar(res.parlamentarioId, "determinista")`. Las contrapartes se mapean crudas (filtrando `rol !== "Sujeto Pasivo"`), `contraparteId: null`. Devuelve la forma para-escribir (audiencia + contrapartes anidadas) + el set de `parlamentarioId` confirmados (para el marcador de ingesta). NO importar `@obs/llm` directo — derivar el tipo del provider de la firma de `correrPipeline` como hace reconciliar-senado.

2. **`writer.ts`**: interfaz `LobbyWriter` (`upsertAudiencias(filas)`, `marcarIngestado(parlamentarioIds, hasta)`) + `InMemoryLobbyWriter` para tests (cuenta filas, permite assertions de idempotencia).

3. **`writer-supabase.ts`**: `SupabaseLobbyWriter implements LobbyWriter`. `createClient` con la SERVICE key (bypassa RLS; server-side). `upsertAudiencias`: aplana — upsert `lobby_audiencia` por `identificador` (dedupe por id), luego `lobby_contraparte` por `(identificador,nombre,rol)` (dedupe por esa clave). El FK persiste plano (`parlamentario_id = enlace?.parlamentarioId ?? null`) — input branded, storage plano (Anti-Pattern A4 de Phase 9). `marcarIngestado`: upsert `lobby_ingesta_estado` por `parlamentario_id`. La service key NUNCA se interpola en mensajes de error.

4. **`reconciliar-sujeto.test.ts`**: (a) determinista → FK poblado + estado confirmado + audit; (b) probable/revision/no_confirmado → FK null + mención cruda; (c) contrapartes siempre crudas con `contraparteId` null; (d) sin provider, un homónimo degrada a no_confirmado (fail-closed) y un determinista resuelve igual.

5. **`writer.test.ts`**: idempotencia — 2× el mismo input al `InMemoryLobbyWriter` → conteos idénticos de audiencia y contraparte; dedupe-before-batch (dos contrapartes de la misma `(identificador,nombre,rol)` colapsan a una).
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test -t "reconciliar-sujeto" && pnpm --filter @obs/lobby test -t writer</automated>
  </verify>
  <done>`reconciliarSujeto` mintea `EnlaceConfirmado` solo en determinista (un string crudo como FK no compila); las contrapartes son crudas con `contraparteId` null; `SupabaseLobbyWriter` es idempotente por clave natural y upserta el marcador de ingesta; los tests pasan.</done>
</task>

<task type="auto">
  <name>Task 3: Orquestación (ingest-run con drift BLOQUEANTE + degradación honesta) + CLI + probe LIVE + barril</name>
  <files>packages/lobby/src/connector-leylobby.ts, packages/lobby/src/ingest-run.ts, packages/lobby/src/ingest-run.test.ts, packages/lobby/src/ingest-cli.ts, packages/lobby/src/live-leylobby.probe.ts, packages/lobby/src/index.ts</files>
  <action>
Espejar `connector-camara.ts` + `ingest-run.ts` + `ingest-cli.ts` + `live-camara.probe.ts` de agenda:

1. **`connector-leylobby.ts`**: `LeylobbyConnector` con deps inyectables (`fetcher`, `rateLimiter`, `robots`, `allowlist`). Método `fetch(url)` en el ORDEN LOCKED (`assertAllowedUrl → robots.isAllowed → rateLimiter.wait → fetcher.get` con `BROWSER_HEADERS`). `urlAudiencias(code, year, page)` → `/instituciones/{code}/audiencias/{year}?page={page}`. Errores tipados: `LeylobbyBloqueadaError` para 403/503 (como `CamaraBloqueadaError`). Comentar el resultado de Open Question 1 (robots 403 → fail-open vía `RobotsGuard`, confirmado en código). NUNCA `BaseConnector.run`. Comentar el/los código(s) de congreso resueltos en Task 1.

2. **`ingest-run.ts`**: orquesta `for each (institucion, año, page)`: fetch (orden LOCKED) → R2 raw (reusar el writer de R2 de @obs/ingest si el connector lo expone, como agenda) → **DriftDetector en modo BLOQUEANTE**: si la forma del parser cambió (drift estructural), CUARENTENA la corrida — emite 0 filas y registra una `Degradacion`, NUNCA filas vacías/garbage que se lean como "sin lobby" (Pitfall 3 / C4; difiere del default no-bloqueante de agenda). → `parseLobbyAudiencias` → `reconciliarSujeto` → `writer.upsertAudiencias` → `writer.marcarIngestado`. Una institución inalcanzable degrada honestamente (registra `Degradacion`, sigue), nunca fabrica. Devuelve un reporte (`audiencias` escritas, `contrapartes`, errores por clave, degradaciones, drift-quarantine flag).

3. **`ingest-cli.ts`**: entry-point que arma los colaboradores REALES + `SupabaseLobbyWriter`, service key SOLO de env, degrada a dry-run sin key. **Corrida LIVE ACOTADA**: limita a (una institución de congreso, año actual, page 1) por defecto — flags para ampliar. Respeta el delay 2-3s. Si leylobby no es alcanzable → degrada a fixture + marca `human_verification`, nunca fabrica.

4. **`live-leylobby.probe.ts`**: probe acotado (gated, mirror de `live-camara.probe.ts`) que hace 1 request real y verifica que el HTML trae el cell `Identificador` y la tabla de asistentes.

5. **`ingest-run.test.ts`**: (a) drift estructural → cuarentena (0 filas + degradación, NUNCA filas); (b) institución inalcanzable → degradación honesta sin abortar; (c) corrida idempotente; (d) nunca fabrica (fuente vacía → 0 filas, no inventa).

6. **`index.ts`**: barril que re-exporta los símbolos públicos.

Registrar el paquete en el workspace (`pnpm install` tras el scaffold).
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test -t ingest-run && pnpm --filter @obs/lobby test && pnpm --filter @obs/lobby typecheck</automated>
  </verify>
  <done>`@obs/lobby` corre la ingesta en el orden LOCKED con drift BLOQUEANTE (cuarentena ante drift estructural), degrada honestamente fuentes inalcanzables, es idempotente y nunca fabrica filas; el CLI acota la corrida LIVE y degrada a fixture sin key/alcance; toda la suite del paquete + typecheck pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>El conector `@obs/lobby` completo (parser, reconciliación, writer, orquestación con drift bloqueante) con suite offline verde. La corrida LIVE real contra leylobby + escritura a Supabase NO se ejecutó autónomamente (sitio gov rate-limited, paginado, no-determinista; requiere el schema 0021 aplicado por Plan 01).</what-built>
  <how-to-verify>
Operador, tras confirmar que 0021 está aplicada (Plan 01 checkpoint):
1. Correr la corrida LIVE ACOTADA del CLI (`ingest-cli`) contra una institución de congreso, año actual, page 1, con el delay 2-3s y la service key en env.
2. Verificar: provenance por fila; clave natural = `Identificador` (no el número de URL); el sujeto pasivo cruzó vía `correrPipeline` (FK solo en determinista, `identidad_audit` con una fila por decisión); las contrapartes quedaron crudas con `contraparte_id` NULL; `lobby_ingesta_estado` marcó al/los parlamentario(s) tocado(s).
3. Re-correr → conteos idénticos (idempotencia).
4. Si leylobby NO es alcanzable en el entorno → degradar a fixture + `human_verification`, NUNCA fabricar audiencias.
  </how-to-verify>
  <resume-signal>Escribe "corrida LIVE acotada OK" (o "degradado a fixture") o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- Suite offline `@obs/lobby` verde + typecheck verde.
- Parser keyed por `Identificador`; reconciliación solo-determinista; writer idempotente; drift bloqueante; nunca fabrica.
- (Operador) corrida LIVE acotada escribe a las tablas de Plan 01 con provenance, FK confirmado y contrapartes crudas; o degrada honesto a fixture.
</verification>

<success_criteria>
- INT-01: `@obs/lobby` ingesta audiencias de leylobby por HTML+cheerio y construye la sub-maestra de contrapartes (filas crudas), reusando `@obs/ingest` en el orden LOCKED, idempotente y sin fabricar.
- INT-02: cada audiencia lleva provenance por fila; el sujeto pasivo se enlaza solo en determinista/confirmado (vía `EnlaceConfirmado` + `identidad_audit`), el resto deja mención cruda.
</success_criteria>

<output>
Crear `.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-02-SUMMARY.md` al terminar.
</output>
