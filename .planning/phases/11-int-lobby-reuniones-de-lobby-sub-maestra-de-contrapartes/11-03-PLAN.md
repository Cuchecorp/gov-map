---
phase: 11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/lib/types.ts
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: [INT-02]
user_setup: []

must_haves:
  truths:
    - "La ficha /parlamentario/[id] muestra una sección de lobby en su propio carril, separada de #votos por mt-12"
    - "Cada reunión muestra la contraparte como TEXTO CRUDO + ProvenanceBadge por fila; sin enlace salvo identidad confirmada"
    - "La sección distingue tres estados honestos: no-ingestado, ingestado-cero-confirmadas, y con-audiencias"
    - "NINGUNA unidad de UI compone una reunión de lobby junto a un voto; cero lenguaje causal/afinidad/score/flag"
  artifacts:
    - path: "app/components/lobby-de-parlamentario.tsx"
      provides: "LobbySection (Server Component) + LobbyView (puro, testeable) que lee lobby_de_parlamentario + lobby_ingesta_estado"
      contains: "lobby_de_parlamentario"
    - path: "app/components/lobby-de-parlamentario.test.tsx"
      provides: "RTL: carril propio, contraparte cruda, 3 estados honestos, gate anti-insinuación"
      min_lines: 40
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "LobbySection"
      via: "<section id=\"lobby\" className=\"mt-12\"> apilada tras #votos"
      pattern: "id=\"lobby\""
    - from: "app/components/lobby-de-parlamentario.tsx"
      to: "public.lobby_de_parlamentario"
      via: "sb.rpc"
      pattern: "lobby_de_parlamentario"
---

<objective>
Apilar una `<section id="lobby">` en el shell existente de `/parlamentario/[id]` (Phase 10), en su propio carril, que renderiza las reuniones de lobby confirmadas del parlamentario leídas vía el RPC `lobby_de_parlamentario` (Plan 01). La contraparte es TEXTO CRUDO con `ProvenanceBadge` por fila; tres estados honestos; cero composición cruzada con votos y cero lenguaje causal/afinidad. Esta es la PRIMERA sección multi-dataset → fija la regla anti-insinuación (carril propio) para todo el frente parlamentario.

Purpose: INT-02 (el ciudadano ve la lista de reuniones con la contraparte trazable a la fuente). El gate de contenido §9.1 (carril aislado, sin causalidad/afinidad/score/flag, sin RUT de tercero) es un release gate de la fase y fija el patrón para Phases 12/14-16.
Output: el componente de sección + tests RTL + la sección apilada en la página. Server Component end-to-end (sin "use client").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-UI-SPEC.md
@.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-RESEARCH.md

<read_first>
- `app/app/parlamentario/[id]/page.tsx` — el shell apilable (Phase 10). La sección de lobby se AÑADE como sibling de `#votos`, con su propio `<h2>` + `<Suspense>` + `LobbySkeleton`, a `mt-12`. NO re-construir el shell ni la cabecera; el comentario en el archivo ya marca dónde apilar `<section id="lobby">`.
- `app/components/votos-por-parlamentario.tsx` — el patrón EXACTO a espejar: `VotosSection` (Server Component que llama `sb.rpc`) + `VotosView` (puro, RTL-testeable por props), paginación server-driven, y los TRES estados honestos (`noIngestado` → "no ingestado"; `totalVotos === 0` → "ingestado, cero"; con datos → lista). Distinción `.maybeSingle()`/error real ≠ "sin filas". El gate de contenido §9.1 ya documentado en su encabezado.
- `app/lib/types.ts` — `VotoFichaRow`, `RebeldiaRow`, `ParlamentarioPublicoRow`, `sourceLabel(origen)`. Añadir aquí `LobbyAudienciaRow` (la forma del payload de la fila de lobby, ver UI-SPEC §10).
- `app/lib/format.ts` — `fechaCorta`, `relativeTimeEs`, `esStale` (reusados vía ProvenanceBadge).
- `app/components/provenance-badge.tsx` — `ProvenanceBadge` (props `capturedAt: Date|null`, `sourceName`, `sourceUrl: string|null`); reuse verbatim, por fila, obligatorio.
- `app/components/identity-marker.tsx` — `IdentityMarker` (sin props; "identidad no verificada"); reuse verbatim para contraparte/sujeto no confirmado.
- `app/lib/supabase.ts` — `createServerSupabase()` (anon key, RLS public-read).
- `.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-UI-SPEC.md` §3 (layout + carril mt-12), §3.2 (anatomía de fila + contraparte cruda), §3.4 (anti-feature: sin viz/score/flag/cross-composition), §6.1 (tres estados honestos), §9 (copy contract), §9.1 (gate de contenido — release gate), §10 (contrato de payload con backend).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipo de payload + LobbyView puro + LobbySection (Server Component) + tests RTL</name>
  <files>app/lib/types.ts, app/components/lobby-de-parlamentario.tsx, app/components/lobby-de-parlamentario.test.tsx</files>
  <behavior>
    - `LobbyView` (puro, por props) renderiza una `<ul>` de audiencias en orden `fecha DESC`, cada `<li>` con: fecha (font-mono `DD MMM YYYY`), contraparte como TEXTO CRUDO (`contraparte_nombre` verbatim), asunto opcional (verbatim si existe), y `ProvenanceBadge` (`sourceName="Ley del Lobby"`/`sourceLabel(origen)`, `capturedAt=fecha_captura`, `sourceUrl=enlace`) por fila, obligatorio.
    - Contraparte: si `contraparte_estado_vinculo !== 'confirmado'` → texto crudo + `<IdentityMarker />`, NUNCA un enlace. Si confirmado → texto plano (la página de sub-maestra no existe aún; nunca un enlace muerto). NUNCA renderiza un RUT de contraparte.
    - Tres estados honestos: (a) `noIngestado` → copy "Aún no hemos ingerido las reuniones de lobby…"; (b) ingestado pero 0 audiencias → copy "No se registran reuniones de lobby confirmadas…"; (c) con audiencias → la lista. Un vacío NUNCA se lee como "no se reúne con nadie".
    - Una línea de intro muted bajo el `<h2>` (UI-SPEC §3.1): "Audiencias registradas bajo la Ley del Lobby (Ley 20.730)…".
    - NINGÚN voto, boletín, proyecto, score, ranking, flag, ni barra de agregado dentro de la sección (§3.4). Un conteo neutro de reuniones es lo único agregado permitido.
  </behavior>
  <action>
1. **`app/lib/types.ts`**: añadir `LobbyAudienciaRow` (UI-SPEC §10): `audiencia_id`/`identificador`, `fecha`, `materia: string|null`, `enlace: string|null`, `origen`, `fecha_captura`, `contraparte_nombre: string`, `contraparte_tipo: string|null` (rol crudo), `contraparte_id: string|null`, `contraparte_estado_vinculo: ...|null`. NO incluir RUT de contraparte (no es parte del payload público — LEGAL-03 deny-by-default). El RPC `lobby_de_parlamentario` ya proyecta solo estos campos.

2. **`app/components/lobby-de-parlamentario.tsx`**: espejar `votos-por-parlamentario.tsx`:
   - `LobbyView({ id, data })` — componente PURO (props), RTL-testeable, sin runtime Supabase/Next. Renderiza la línea de intro + los tres estados + la lista `<ul>/<li>` con `LobbyAudienciaRow` inline (fecha mono, contraparte cruda + IdentityMarker condicional, asunto opcional, ProvenanceBadge `ml-auto`). Paginación server-driven `?lobbyPage=N` (page size 20) idéntica al patrón de §3.2 si hay muchas audiencias. NO `CamaraChip` por fila.
   - `LobbySection({ id, searchParams })` — Server Component: `createServerSupabase()` → `sb.rpc("lobby_de_parlamentario", { p_id: id })` (maybeSingle? no — devuelve table; usar el patrón de array como votos). Distinguir error real de DB/red (throw → UI de error honesta §6.3) de "0 filas" (→ estado honesto). Consultar `lobby_ingesta_estado` (public-read) para `noIngestado` (ausencia de fila para `p_id` = no ingestado todavía) — esto resuelve el gap real de `VotosView.noIngestado` hardcodeado; computar el booleano de verdad. Agrupar filas del RPC por `identificador` (el left join trae una fila por contraparte) → una audiencia con sus contrapartes. Pasar la forma a `LobbyView`.
   - GATE DE CONTENIDO (§9.1) en el encabezado del archivo: documentar la lista dura de términos prohibidos (causalidad: "se reunió para"/"a cambio de"/"antes de votar"; afinidad: "cercano a"/"vinculado a"/"su lobista"; score/ranking/flag/"conflicto"). Nada de eso entra en código ni copy. Carril aislado: la sección NUNCA referencia un voto/boletín.

3. **`app/components/lobby-de-parlamentario.test.tsx`** (RTL, sobre `LobbyView` con fixtures):
   - Carril/estructura: la sección renderiza su propia lista; NINGÚN elemento contiene un voto/boletín/proyecto (assertion negativa: el render no contiene texto de voto ni un link a `/proyecto/`).
   - Contraparte cruda: el nombre de contraparte aparece verbatim; si no confirmada → `IdentityMarker` ("identidad no verificada") presente y SIN enlace; nunca un RUT en el DOM.
   - Tres estados honestos: (a) `noIngestado=true` → copy "no ingerido"; (b) ingestado + 0 → copy "no se registran… confirmadas"; (c) con datos → las filas. Los tres copies son textualmente distintos (un vacío ≠ "limpio").
   - ProvenanceBadge presente en cada fila (obligatorio).
   - **Gate anti-insinuación (content-gate test):** el render NO contiene ninguno de los términos prohibidos de §9.1 (causal/afinidad/score/flag) — assertion sobre el textContent. Este test es el release gate de la fase.
  </action>
  <verify>
    <automated>pnpm --filter app test -t lobby</automated>
  </verify>
  <done>`LobbyView`/`LobbySection` existen; `LobbyView` renderiza contraparte cruda + ProvenanceBadge por fila, IdentityMarker si no-confirmada, sin RUT, sin enlace muerto; los tres estados honestos son distintos; el content-gate test (sin causal/afinidad/score/flag, sin composición con voto) pasa; `LobbySection` computa `noIngestado` real desde `lobby_ingesta_estado`.</done>
</task>

<task type="auto">
  <name>Task 2: Apilar la sección de lobby en /parlamentario/[id] (carril propio, mt-12, Suspense)</name>
  <files>app/app/parlamentario/[id]/page.tsx</files>
  <action>
Editar `app/app/parlamentario/[id]/page.tsx` (Phase 10) para apilar la sección de lobby DESPUÉS de `#votos`, en el lugar marcado por el comentario existente:

- Añadir `<section id="lobby" className="mt-12"><h2 className="text-xl font-semibold mb-4">Reuniones de lobby</h2><Suspense fallback={<LobbySkeleton />}><LobbySection id={id} searchParams={sp} /></Suspense></section>` como sibling de `#votos` (NUNCA anidada en `#votos`).
- Importar `LobbySection` de `@/components/lobby-de-parlamentario`.
- Añadir `LobbySkeleton` (shape-matched: línea de intro + 3 filas de audiencia) espejando `VotosSkeleton`, `aria-hidden`.
- NO tocar la cabecera ni `#votos`. La jerarquía de headings sigue válida (`h1` cabecera, `h2` Votaciones + `h2` Reuniones de lobby como siblings).
- El `mt-12` es la frontera de carril: las dos secciones son siblings con 48px de separación; un voto y una reunión NUNCA en el mismo `<article>`/`<Card>`/`<li>` (§9.1 lane isolation).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/app/parlamentario/[id]/page.tsx','utf8'); const must=['id=\"lobby\"','LobbySection','LobbySkeleton','mt-12']; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('FALTAN:',miss);process.exit(1)} if(!/section id=\"votos\"[\s\S]*section id=\"lobby\"/.test(s)){console.error('lobby debe ser sibling DESPUES de votos, no anidado');process.exit(1)} console.log('page OK')" && pnpm --filter app build</automated>
  </verify>
  <done>`/parlamentario/[id]` renderiza `<section id="lobby">` como sibling de `#votos` a `mt-12`, con su propio `<h2>` + Suspense + `LobbySkeleton`; el build de la app pasa; la cabecera y `#votos` quedan intactos.</done>
</task>

</tasks>

<verification>
- `pnpm --filter app test -t lobby` verde (incluido el content-gate anti-insinuación).
- `pnpm --filter app build` verde.
- La sección es su propio carril (sibling, mt-12), nunca compone un voto con una reunión.
</verification>

<success_criteria>
- INT-02: el ciudadano ve la lista de reuniones de lobby del parlamentario, con la contraparte como texto crudo (enlace solo si confirmada — y aún sin página, texto plano) y `ProvenanceBadge` por fila.
- Tres estados honestos distintos; un vacío nunca se lee como "no se reúne con nadie".
- Gate de contenido §9.1 PASS: carril aislado, sin causalidad/afinidad/score/flag, sin RUT de tercero — fija el patrón anti-insinuación para Phases 12/14-16.
</success_criteria>

<output>
Crear `.planning/phases/11-int-lobby-reuniones-de-lobby-sub-maestra-de-contrapartes/11-03-SUMMARY.md` al terminar.
</output>
