---
phase: 80-bento-guards-responsive-a11y-dark-candados-de-r-gimen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/search-box.tsx
  - app/components/brand-icon.tsx
  - app/components/actualidad-module.tsx
  - app/app/styles/civic-tokens.css
  - app/app/page.test.tsx
autonomous: true
requirements: [BENTO-05]
must_haves:
  truths:
    - "El form de búsqueda (SearchBox) tiene aria-label — landmark search con nombre único"
    - "BrandIcon no tiene default hex: color por defecto = currentColor (ambos callers pasan color explícito)"
    - "La barra cívica 3px es theme-aware en dark (par dark de --camara/--senado) O su degradación en dark queda documentada + fijada por test"
    - "Un test estructural fija el colapso ≤md (grid-cols-1 md:grid-cols-6, spans solo md:), el orden DOM de los tiles, y los landmarks (un solo main, secciones con heading)"
  artifacts:
    - path: "app/components/search-box.tsx"
      provides: "aria-label en el <form role=search>"
      contains: "aria-label"
    - path: "app/components/brand-icon.tsx"
      provides: "default color currentColor (sin hex literal)"
      contains: "currentColor"
    - path: "app/app/page.test.tsx"
      provides: "asserts de colapso/orden DOM/landmarks/form a11y"
      contains: "col-span"
  key_links:
    - from: "app/app/page.test.tsx"
      to: "app/app/page.tsx (Home)"
      via: "render(<Home/>) → getByRole/querySelectorAll asserts de estructura"
      pattern: "render\\(.*Home"
    - from: "app/components/actualidad-module.tsx"
      to: "civic-tokens.css (--camara/--senado)"
      via: "bg-[var(--camara)]/bg-[var(--senado)] en la barra 3px (par dark o degradación documentada)"
      pattern: "var\\(--camara\\)|var\\(--senado\\)"
---

<objective>
Cerrar los 4 gaps reales de a11y/dark/candado que el research identificó como edits mínimos sobre código YA construido (76-78), y fijar con un test estructural el colapso responsive / orden DOM / landmarks de la home que hoy funciona pero NO está protegido contra regresión.

Purpose: BENTO-05 — el bento no se degrada en móvil ni en dark, y es accesible. Esta ola son los FIXES + los TESTS que los fijan; los candados de régimen (BENTO-06) van en el plan 80-02, que depende de que aquí quede resuelto el hex de BrandIcon (el candado cero-hex correría rojo si el default hex sigue presente).
Output: aria-label en SearchBox; BrandIcon → currentColor; decisión de dark de la barra cívica (par dark o degradación honesta documentada + test); page.test.tsx con asserts de colapso/orden/landmarks/form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-bento-guards-responsive-a11y-dark-candados-de-r-gimen/80-RESEARCH.md
@.planning/phases/80-bento-guards-responsive-a11y-dark-candados-de-r-gimen/80-VALIDATION.md

<interfaces>
<!-- Contratos exactos que el ejecutor necesita — extraídos del repo. NO explorar. -->

De app/components/search-box.tsx (el form, hoy SIN aria-label — GAP a11y Pitfall 2):
```tsx
<form
  role="search"
  action="/buscar"
  method="get"
  onSubmit={handleSubmit}
  className={isHero ? "flex flex-col gap-3 sm:flex-row" : "flex gap-2"}
>
```
El <Input> interno YA tiene `aria-label="Buscar proyectos de ley"` (línea 115). El GAP es el <form> sin label.

De app/components/brand-icon.tsx (default hex — GAP candado Pitfall 3):
```tsx
export function BrandIcon({ size = 24, color = "#2A5859", className, "aria-hidden": ariaHidden = true }: BrandIconProps)
```
Ambos callers pasan color explícito (VERIFICADO por grep en research):
- page.tsx:128 → color="currentColor"
- global-header.tsx:39 → color="hsl(var(--accent-product))"
Ningún caller depende del default hex. El JSDoc (líneas 6-8, 13) también menciona "#2A5859" — el candado cero-hex del plan 02 SÓLO escanea componentes bento (brand-icon NO está en su scope), así que el hex del JSDoc no es un problema de scope; pero CONTEXT pide resolver la deuda 77-UI-REVIEW cambiando el default a currentColor de todos modos.

De app/components/actualidad-module.tsx (barra cívica 3px — GAP dark Pitfall 1):
Líneas ~170-174: la barra usa `bg-[var(--camara)]`/`bg-[var(--senado)]` + `w-[3px] rounded-[2px]`. En app/app/styles/civic-tokens.css, `--camara` (213 94% 38%) y `--senado` (355 65% 38%) NO tienen override en `.dark` (solo `--camara-muted`/`--senado-muted` sí). Es el único elemento bento no theme-aware.

Nota jsdom (Anti-Patterns research): getComputedStyle/getBoundingClientRect devuelven 0 en jsdom. El test fija CLASES (toHaveClass("md:col-span-N")), NUNCA píxeles. El píxel real es Phase 81.

Idiom de source-scan del repo (para cualquier assert de fuente): `readFileSync` + `process.cwd()` + `path.join`, NUNCA `new URL(import.meta.url)` (lección 45-01: rompe en jsdom sobre OneDrive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A11y + candado fixes — aria-label en SearchBox, BrandIcon → currentColor, decisión dark de la barra cívica</name>
  <files>app/components/search-box.tsx, app/components/brand-icon.tsx, app/components/actualidad-module.tsx, app/app/styles/civic-tokens.css</files>
  <read_first>
    - app/components/search-box.tsx (el <form role="search">, ~línea 89)
    - app/components/brand-icon.tsx (default color, líneas 13/23; JSDoc 6-8)
    - app/components/actualidad-module.tsx (barra cívica 3px, ~líneas 170-174)
    - app/app/styles/civic-tokens.css (bloque :root y bloque .dark — confirmar que --camara/--senado NO tienen par dark; --camara-muted/--senado-muted sí)
  </read_first>
  <action>
    (1) SearchBox (Pitfall 2, BENTO-05): añadir `aria-label="Buscar proyectos de ley"` al `<form role="search">`. Edit de una línea; NO tocar el `<Input>` (ya tiene su propio aria-label). El label del form da nombre único al landmark search.

    (2) BrandIcon (Pitfall 3, deuda 77-UI-REVIEW): cambiar el default `color = "#2A5859"` a `color = "currentColor"`. Actualizar el JSDoc (líneas 6-8 y 13) para que ya NO afirme "Default: petrol #2A5859" — reemplazar por "Default: currentColor (hereda color del contexto)". Es SEGURO: ambos callers pasan color explícito (page.tsx currentColor, global-header hsl(var(--accent-product))). Eliminar TODOS los literales `#2A5859` del archivo (incluidos los del JSDoc) — deja el archivo sin hex, alineado con la disciplina cero-hex del milestone.

    (3) Barra cívica dark (Pitfall 1, A1, BENTO-05 — decisión a documentar en el SUMMARY): la barra usa `--camara`/`--senado` que NO tienen par `.dark`. Como el gate visual real es Phase 81 (jsdom no computa contraste), tomar la decisión CONSERVADORA y explícita: añadir el par dark en civic-tokens.css. En el bloque `.dark`, definir `--camara` y `--senado` con valores derivados de los tokens dark existentes que garanticen ≥3:1 sobre card dark (WCAG 1.4.11 non-text; la barra es provenance de cámara, no texto). Usar los valores muted-foreground dark ya presentes como base de derivación (mismos hues 213/355, luminancia subida para dark card 222 24% 12%): p.ej. `--camara: 213 90% 62%` y `--senado: 355 70% 62%` (hue idéntico, L elevada para separarse del fondo oscuro). Documentar en un comentario CSS que el par dark existe por la barra cívica 3px de actualidad-module y que el ratio real se verifica en Phase 81. NO tokenizar nuevos nombres (reutilizar --camara/--senado); NO tocar --camara-muted/--senado-muted. Si al añadir el par dark algún OTRO consumidor de --camara/--senado en dark regresiona visualmente, es aceptable (mejora la coherencia dark) — pero verificar por grep que los consumidores de --camara/--senado son la barra + usos que se benefician del par dark, y anotar cualquier hallazgo en el SUMMARY.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run search-box brand-icon actualidad</automated>
  </verify>
  <done>El form de SearchBox tiene aria-label; brand-icon.tsx no contiene ningún `#2A5859` (ni en JSDoc); civic-tokens.css tiene par `.dark` para --camara/--senado con comentario que lo justifica; los tests afectados verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Test estructural de home — colapso ≤md, orden DOM de tiles, landmarks, form a11y</name>
  <files>app/app/page.test.tsx</files>
  <read_first>
    - app/app/page.test.tsx (estructura actual: cómo renderiza <Home/>, BANNED_VOCAB líneas ~161-162, asserts existentes de nav/landmarks)
    - app/app/page.tsx (orden DOM de tiles: hero → cómo-leer → 3 entradas → votado → urgencias → frescura; el único <main> ~línea 85; el <nav aria-label>)
    - app/components/bento/bento-grid.test.tsx (patrón de assert de clases del grid — grid-cols-1 md:grid-cols-6)
  </read_first>
  <action>
    Añadir a page.test.tsx (extender el archivo existente, NO crear uno nuevo) un describe-block "BENTO-05 — colapso/orden/landmarks (estructural, jsdom-safe)" con estos asserts, todos sobre `render(<Home/>)` (fijan CLASES/estructura, NUNCA píxeles — jsdom devuelve 0 para layout):

    (a) COLAPSO: ningún tile del grid tiene `col-span-N` SIN prefijo `md:` (un base col-span rompería el colapso a 1 columna en móvil). Escanear los elementos con clase `col-span` en el DOM renderizado y assertar que cada uno matchea solo `md:col-span-N`. Patrón: `document.querySelectorAll("[class*='col-span']")` → cada `className` NOT match `/(?<!md:)\bcol-span-\d/`. El grid contenedor debe tener `grid-cols-1` y `md:grid-cols-6`.

    (b) ORDEN DOM = orden visual: los tiles aparecen en el orden hero → cómo-leer → 3 entradas (Buscar/Parlamentarios/Agenda) → votado → urgencias → frescura. Verificar por el orden de aparición de landmarks/headings/hrefs estables (p.ej. el orden de los `<h2>`/títulos de sección o de los href de las 3 entradas). Anclar el assert a texto/hrefs LOCKED existentes, no a clases frágiles. Este es el orden DOM que garantiza el colapso correcto (sin reordenar CSS).

    (c) LANDMARK único: `container.querySelectorAll("main")` tiene longitud 1 (layout.tsx NO envuelve children en <main> — el único <main> es el de page.tsx). El `<nav>` tiene aria-label (ya existe — confirmar).

    (d) FORM a11y: `getByRole("search", { name: /buscar/i })` encuentra el form con su aria-label nuevo (Task 1). Si el multiple-search fuera ambiguo, usar el name para desambiguar.

    (e) SECCIONES con heading: las secciones de actualidad (votado/urgencias) tienen su `<h2>` — assertar presencia por texto ("Votado esta semana", "Urgencias vigentes") o por role heading.

    Reusar los mocks/fixtures de datos que page.test.tsx ya usa para renderizar Home (no inventar nuevos). Si Home requiere async data, seguir el patrón async que el archivo ya emplea.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run page.test</automated>
  </verify>
  <done>page.test.tsx tiene el describe-block BENTO-05 con los 5 grupos de asserts (colapso, orden DOM, main único, form aria-label, headings de sección); todos verdes contra la home actual; ningún assert usa getComputedStyle/getBoundingClientRect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Phase 80-01 no cruza input no confiable — son edits de presentación/a11y + tests estructurales sobre la home pública (server-only reads). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | test estructural de home que pasa sin probar nada (no-op verde) | mitigate | Los asserts anclan a estructura/hrefs/landmarks LOCKED reales; una regresión de colapso (base col-span) o de landmark rompería el test. El mutation self-check formal es de los guards (plan 02); este test fija estructura observable. |
| T-80-02 | Elevation of Privilege | gate bypass por superficie nueva | accept | Invariante del milestone: cero superficie nueva; 80-01 solo añade aria-label/currentColor/par-dark/tests. No lee datos gated (NET/MONEY/CRUCES). |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas (research §Standard Stack: "Sin instalación nueva"); no hay install tasks, Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `cd app && pnpm test -- --run` (suite app completa, base 885 + los asserts nuevos de page.test.tsx) verde.
- `cd app && pnpm exec tsc --noEmit` limpio.
- brand-icon.tsx sin ningún literal hex (`#[0-9a-fA-F]{3,8}`).
- civic-tokens.css tiene par `.dark` para --camara/--senado.
</verification>

<success_criteria>
- BENTO-05 (porción fixes+tests): aria-label en form; BrandIcon currentColor; barra cívica con par dark documentado; test estructural de colapso/orden/landmarks verde.
- Suite app verde + tsc limpio; cero cambios en queries/RPCs/datos (presentación pura).
- El default hex de BrandIcon está eliminado → el candado cero-hex del plan 02 podrá correr verde.
</success_criteria>

<output>
Create `.planning/phases/80-bento-guards-responsive-a11y-dark-candados-de-r-gimen/80-01-SUMMARY.md` when done. Documentar explícitamente la decisión de dark de la barra cívica (par dark añadido vs degradación) y cualquier consumidor de --camara/--senado afectado.
</output>
