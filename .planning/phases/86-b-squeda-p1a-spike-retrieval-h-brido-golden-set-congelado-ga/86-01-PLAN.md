---
phase: 86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fichas/src/spike/golden-set.ts
  - packages/fichas/src/spike/boletin.ts
  - packages/fichas/src/spike/rrf.ts
  - packages/fichas/src/spike/score.ts
  - packages/fichas/src/spike/golden-set.test.ts
  - packages/fichas/src/spike/boletin.test.ts
  - packages/fichas/src/spike/rrf.test.ts
  - packages/fichas/src/spike/score.test.ts
autonomous: true
requirements: [RETR-03, RETR-04]

must_haves:
  truths:
    - "El golden set tiene ≥30 queries frozen cubriendo las 6 categorías mandatadas"
    - "El detector de boletín matchea los 3 formatos incluyendo el punteado 14.309-04"
    - "El scorer computa hit@1/hit@5/MRR por categoría y agregado desde una lista rankeada"
    - "El merge RRF fusiona por rank (1/(rrf_k+rank)), nunca por suma ponderada de scores"
  artifacts:
    - path: "packages/fichas/src/spike/golden-set.ts"
      provides: "GOLDEN_SET ≥30 CasoRetrieval frozen + normalizarLiteral + tipo CategoriaRetrieval"
      contains: "GOLDEN_SET"
      min_lines: 60
    - path: "packages/fichas/src/spike/boletin.ts"
      provides: "detectarBoletin: normaliza 3 formatos → {base, sufijo|null} | null"
      contains: "export function detectarBoletin"
    - path: "packages/fichas/src/spike/rrf.ts"
      provides: "rrf(fts, sem, rrfK, wFts, wSem): fusión por rank"
      contains: "export function rrf"
    - path: "packages/fichas/src/spike/score.ts"
      provides: "evaluarRetrieval + MetricasRetrieval (hit1/hit5/mrr por categoría)"
      contains: "export interface MetricasRetrieval"
  key_links:
    - from: "packages/fichas/src/spike/score.ts"
      to: "packages/fichas/src/spike/golden-set.ts"
      via: "import CasoRetrieval, normalizarLiteral"
      pattern: "from \"\\./golden-set"
---

<objective>
Construir el núcleo OFFLINE del spike de retrieval: el golden set congelado (≥30 queries), el detector de boletín de 3 formatos, el merge RRF por rank, y el scorer hit@1/hit@5/MRR. Todo puro (sin red, sin DB) y testeable sin credenciales — es la base contra la que se miden las 3 estrategias en las fases siguientes.

Purpose: El scoring del golden set ES la investigación (RETR-03). Congelar el set y los tipos ANTES de tocar la DB evita el anti-patrón "afinar por sensación" (Pitfall #4): la decisión será una tabla numérica, no una vibra.
Output: `packages/fichas/src/spike/{golden-set,boletin,rrf,score}.ts` + sus tests offline. Cero red, cero DB, cero schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-CONTEXT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-PATTERNS.md

<interfaces>
<!-- Analog EXACTO a espejar: packages/fichas/src/golden/golden-set.ts (mismo paquete, mismo patrón de equipo). -->
<!-- Reusar/copiar de golden/golden-set.ts:  CasoGolden shape, normalizarLiteral (NFD + strip diacritics + lowercase + collapse ws), evaluarGolden (scorer puro con `ejecutar` inyectado + detalle[] por caso). -->

Tipo de caso (adaptar CasoGolden → retrieval):
  export type CategoriaRetrieval =
    | "titulo-literal" | "parafrasis-nl" | "normas" | "boletin" | "acentos-toponimos" | "similares";
  export interface CasoRetrieval { id: string; category: CategoriaRetrieval; query: string; expected: string[]; nota: string; }
  export const GOLDEN_SET: CasoRetrieval[] = [ /* ≥30, frozen */ ];

normalizarLiteral (copiar verbatim del sibling golden/golden-set.ts):
  export function normalizarLiteral(s: string): string {
    return s.normalize("NFD").replace(/\p{Diacritic}/gu, "").toLowerCase().replace(/\s+/g, " ").trim();
  }

Scorer (mirror evaluarGolden — fn pura, runner inyectado, detalle[] load-bearing):
  export interface MetricasRetrieval {
    porCategoria: Record<string, { hit1: number; hit5: number; mrr: number; n: number }>;
    agregado: { hit1: number; hit5: number; mrr: number; n: number };
    detalle: { id: string; category: string; rank: number | null; ok: boolean }[];
  }
  export async function evaluarRetrieval(
    set: CasoRetrieval[],
    ejecutar: (caso: CasoRetrieval) => Promise<string[]>,   // estrategia → boletin[] rankeado
  ): Promise<MetricasRetrieval>;

RRF (rank, NUNCA suma de scores — LOCKED-forbidden):
  export function rrf(fts: string[], sem: string[], rrfK = 50, wFts = 1, wSem = 1): string[];
  // score.set(b, score+w/(rrfK + i + 1)); sort desc; map boletin.

Boletín (extiende BOLETIN_RE de app/lib/buscar.ts:28 que NO cubre punteado):
  // Current app: /^\d{3,6}(-\d{1,2})?$/  — NO matchea "14.309-04".
  export function detectarBoletin(q: string): { base: string; sufijo: string | null } | null;
  // normaliza los 3 formatos: "14309-04" → {base:"14309", sufijo:"04"}; "14309" → {base:"14309", sufijo:null};
  //                            "14.309-04" (strip puntos) → {base:"14309", sufijo:"04"}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detector de boletín (3 formatos) + merge RRF</name>
  <files>packages/fichas/src/spike/boletin.ts, packages/fichas/src/spike/boletin.test.ts, packages/fichas/src/spike/rrf.ts, packages/fichas/src/spike/rrf.test.ts</files>
  <read_first>
    - app/lib/buscar.ts (líneas 24-28: BOLETIN_RE actual `/^\d{3,6}(-\d{1,2})?$/` que NO cubre el punteado — este es el bug que el detector arregla)
    - .planning/phases/86-.../86-PATTERNS.md (sección strategies.ts: RRF snippet ~10 líneas + regla "boletín short-circuit ANTES de RRF"; Pitfall #5 formatos)
    - .planning/phases/86-.../86-RESEARCH.md (Code Examples: función rrf; Pitfall 5 boletín punteado)
  </read_first>
  <behavior>
    - detectarBoletin: Test "14309-04" → {base:"14309", sufijo:"04"}
    - detectarBoletin: Test "14309" → {base:"14309", sufijo:null}
    - detectarBoletin: Test "14.309-04" (con punto) → {base:"14309", sufijo:"04"} (el caso que hoy falla, Pitfall #5)
    - detectarBoletin: Test "14.309" → {base:"14309", sufijo:null}
    - detectarBoletin: Test "medio ambiente" (texto libre) → null
    - detectarBoletin: Test "" → null
    - rrf: Test dos listas → boletín presente en ambas rankea sobre el que sale en una sola
    - rrf: Test rrfK más chico separa más los tops (verificar orden, no suma cruda)
    - rrf: Test pesos wFts=0 → resultado = orden de sem solo
  </behavior>
  <action>
    Crear `boletin.ts` con `export function detectarBoletin(q)`: `.trim()`, strip puntos (`.replace(/\./g, "")`), matchear `^\d{3,6}(-\d{1,2})?$` sobre el resultado sin-puntos, y devolver `{base, sufijo}` separando por `-`. Devolver `null` si no matchea (texto libre) — el consumidor decide short-circuit vs FTS/semántico.
    Crear `rrf.ts` con `export function rrf(fts, sem, rrfK=50, wFts=1, wSem=1): string[]` EXACTAMENTE como el snippet de RESEARCH: acumular en `Map<string,number>` `w/(rrfK + i + 1)` sobre CADA lista, ordenar desc, devolver boletines. PROHIBIDO sumar scores crudos (cosine + ts_rank) — solo rank. Sin dependencias externas.
    Escribir los tests primero (RED) según el bloque behavior, luego implementar (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/boletin.test.ts src/spike/rrf.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `boletin.test.ts` verde: los 6 casos del behavior pasan, incluyendo el punteado `14.309-04` y el null de texto libre
    - `rrf.test.ts` verde: fusión por rank probada; wFts=0 reduce a sem-solo
    - `grep -n "export function detectarBoletin" packages/fichas/src/spike/boletin.ts` devuelve 1 línea
    - `grep -c "score.*+.*similarity\|ts_rank.*+.*cosine" packages/fichas/src/spike/rrf.ts` == 0 (sin suma ponderada de scores)
  </acceptance_criteria>
  <done>detectarBoletin resuelve los 3 formatos (incl. punteado) o null; rrf fusiona por rank sin suma de scores; ambos con tests verdes offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden set frozen ≥30 queries + normalizarLiteral</name>
  <files>packages/fichas/src/spike/golden-set.ts, packages/fichas/src/spike/golden-set.test.ts</files>
  <read_first>
    - packages/fichas/src/golden/golden-set.ts (analog EXACTO: shape CasoGolden líneas ~42-54, normalizarLiteral líneas ~62-71, filtros de subset frozen líneas ~411-432 — copiar el idiom)
    - .planning/phases/86-.../86-CONTEXT.md (sección "Golden set: composición y formato" — las 6 clases mandatadas y los casos literales del bug estrella)
    - .planning/phases/86-.../86-PATTERNS.md (sección golden-set.ts: CategoriaRetrieval, CasoRetrieval, normalizarLiteral verbatim)
  </read_first>
  <behavior>
    - GOLDEN_SET.length >= 30
    - Cada CategoriaRetrieval de las 6 tiene ≥1 caso: titulo-literal, parafrasis-nl, normas, boletin, acentos-toponimos, similares
    - La categoría "boletin" incluye los 3 formatos como casos separados: "14309-04", "14309", "14.309-04"
    - La categoría "acentos-toponimos" incluye "Ñuñoa", "Aysén", "medio ambiente"
    - Todo `expected[]` es no vacío y en formato canónico (dígitos + guion, p.ej. "14309-04")
    - Todos los `id` son únicos
    - normalizarLiteral("Ñuñoa") === normalizarLiteral("nunoa") (accent-insensitive)
  </behavior>
  <action>
    Crear `golden-set.ts` exportando `type CategoriaRetrieval` (union de 6), `interface CasoRetrieval {id, category, query, expected: string[], nota}`, `const GOLDEN_SET: CasoRetrieval[]` con ≥30 casos DERIVADOS del corpus real (los `expected` se validan LIVE en el plan 03; por ahora usar boletines plausibles del dominio — p.ej. `14309-04` es el caso canónico documentado — y marcarlos como hipótesis en `nota`). Copiar `normalizarLiteral` verbatim del sibling golden/. Distribuir: título-literal (los casos EXACTOS que hoy fallan, palabras textuales del título), paráfrasis-nl (el semántico debe seguir ganando), normas (cuerpos legales), boletín (3 formatos, hit@1), acentos-topónimos (Ñuñoa/Aysén/medio ambiente), similares (no regresionar SEM-05). Congelado: comentario de cabecera "CONGELADO al cierre — cambios requieren decisión registrada, no ediciones silenciosas".
    Escribir los tests primero (RED) según behavior (asserts de cobertura de categorías, longitud, unicidad de ids, formato de expected), luego poblar el set (GREEN).
    NOTA: los `expected` boletines definitivos se confirman contra PROD en el plan 03 (Task de derivación live); este plan congela la ESTRUCTURA y las queries; el plan 03 puede ajustar boletines dentro de la disciplina "decisión registrada".
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/golden-set.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `golden-set.test.ts` verde: GOLDEN_SET.length ≥ 30, las 6 categorías cubiertas, 3 formatos de boletín presentes, Ñuñoa/Aysén/medio ambiente presentes, ids únicos
    - `grep -c "category:" packages/fichas/src/spike/golden-set.ts` ≥ 30
    - `grep -n "CONGELADO" packages/fichas/src/spike/golden-set.ts` devuelve ≥1 (disciplina de congelamiento documentada)
    - normalizarLiteral es accent-insensitive (test Ñuñoa==nunoa verde)
  </acceptance_criteria>
  <done>GOLDEN_SET tiene ≥30 casos frozen cubriendo las 6 categorías + los 3 formatos de boletín + los casos de acento; normalizarLiteral copiado; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scorer hit@1/hit@5/MRR por categoría</name>
  <files>packages/fichas/src/spike/score.ts, packages/fichas/src/spike/score.test.ts</files>
  <read_first>
    - packages/fichas/src/golden/golden-set.ts (evaluarGolden líneas ~459-523: fn pura, `ejecutar` inyectado, `detalle[]` por caso — copiar la forma, cambiar precision/recall por hit@1/hit@5/MRR)
    - packages/fichas/src/spike/golden-set.ts (creado en Task 2: CasoRetrieval, normalizarLiteral)
    - .planning/phases/86-.../86-PATTERNS.md (sección score.ts: MetricasRetrieval, evaluarRetrieval, "hit@1 es el win criterion para boletin"; detalle[] load-bearing Pitfall #4)
  </read_first>
  <behavior>
    - evaluarRetrieval con un `ejecutar` que devuelve el expected en pos 0 → hit1=1, hit5=1, mrr=1 para ese caso
    - expected en pos 4 (índice 4, rank 5) → hit1=0, hit5=1, mrr=1/5
    - expected en pos 7 → hit1=0, hit5=0, mrr=0 (fuera del top-5), rank=8 en detalle
    - expected ausente de la lista → rank=null, hit1=0, hit5=0, mrr=0, ok=false
    - Match accent-insensitive: expected "14309-04" hace hit contra "14309-04" en la lista via normalizarLiteral
    - porCategoria agrupa por category y agregado promedia sobre todos los casos
    - detalle[] tiene una entrada por caso con {id, category, rank, ok}
  </behavior>
  <action>
    Crear `score.ts` con `interface MetricasRetrieval` (porCategoria: Record<string,{hit1,hit5,mrr,n}>, agregado, detalle[]) y `async function evaluarRetrieval(set, ejecutar)`. Para cada caso: correr `ejecutar(caso)` → lista rankeada de boletines; encontrar el rank (1-based) del PRIMER expected que matchea (comparar via `normalizarLiteral` para tolerar acentos/formato); hit1 = rank===1, hit5 = rank!==null && rank<=5, mrr = rank ? 1/rank : 0. Agrupar por categoría (promedios) + agregado global. Poblar `detalle[]` con {id, category, rank, ok}. Fn PURA — el runner de estrategia se inyecta, sin red ni DB acá.
    Escribir tests primero (RED) con un `ejecutar` mock que devuelve listas fijas (offline), luego implementar (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/score.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `score.test.ts` verde: los 7 casos del behavior pasan (posición 0/4/7/ausente, accent-insensitive, agrupación, detalle)
    - `grep -n "export interface MetricasRetrieval" packages/fichas/src/spike/score.ts` == 1
    - `grep -n "normalizarLiteral" packages/fichas/src/spike/score.ts` ≥ 1 (matching accent-insensitive)
    - `grep -c "\.rpc\|fetch\|psql\|spawn" packages/fichas/src/spike/score.ts` == 0 (scorer puro, sin I/O)
    - `pnpm --filter @obs/fichas exec tsc -b` sin errores de tipo
  </acceptance_criteria>
  <done>evaluarRetrieval computa hit@1/hit@5/MRR por categoría + agregado + detalle[] desde un runner inyectado; puro; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden query text → scorer/detector | Texto controlado del fixture (no input de usuario en runtime); riesgo bajo, pero el detector es regex anclada sin backtracking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Denial of Service | detectarBoletin regex | mitigate | Regex anclada `^...$` lineal (sin cuantificadores anidados) → sin ReDoS; input capeado por el llamador (MAX_QUERY_CHARS en runtime). |
| T-86-02 | Info Disclosure | golden-set.ts fixture | mitigate | Fixture = solo queries públicas + boletines públicos; PROHIBIDO commitear PII o secretos. Verificado por revisión: sin RUT, sin nombres privados, sin claves. |
| T-86-03 | Tampering | módulo spike (todo) | mitigate | Este plan es 100% offline (sin DB, sin red): cero DDL/DML posible. `grep` de acceptance confirma ausencia de `.rpc/fetch/psql/spawn` en score.ts. |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas test` verde (suite offline del paquete, incluye los 4 nuevos test files)
- `pnpm --filter @obs/fichas exec tsc -b` sin errores
- Ningún archivo de este plan importa `app/lib/buscar.ts` (server-only) ni contiene `.rpc`/`fetch`/`psql`
</verification>

<success_criteria>
- GOLDEN_SET ≥30 casos frozen, 6 categorías cubiertas, 3 formatos de boletín + acentos presentes
- detectarBoletin resuelve los 3 formatos (incl. `14.309-04`) o null
- rrf fusiona por rank (sin suma de scores)
- evaluarRetrieval computa hit@1/hit@5/MRR por categoría + agregado + detalle
- 4 test files offline verdes; tsc limpio
</success_criteria>

<output>
Create `.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-01-SUMMARY.md` when done
</output>
