---
phase: 86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - packages/fichas/src/spike/psql.ts
  - packages/fichas/src/spike/embed-query.ts
  - packages/fichas/src/spike/embed-cache.ts
  - packages/fichas/src/spike/strategies.ts
  - packages/fichas/src/spike/psql.test.ts
  - packages/fichas/src/spike/embed-cache.test.ts
  - packages/fichas/src/spike/strategies.test.ts
autonomous: true
requirements: [RETR-03]

must_haves:
  truths:
    - "El harness lee la DB SOLO por SELECT/RPC: cero DDL/DML posible"
    - "Las queries del golden set se embeben con RETRIEVAL_QUERY/768/L2 (espejo exacto de buscar.ts)"
    - "El cache query→vector persiste en archivo committeable y evita re-pegarle a Gemini"
    - "Las 3 estrategias (FTS-solo, semántico-solo, RRF) devuelven un boletin[] rankeado"
    - "El FTS usa websearch_to_tsquery SIEMPRE y unaccent en AMBOS lados"
  artifacts:
    - path: "packages/fichas/src/spike/psql.ts"
      provides: "runSql read-only wrapper (spawn psql -At, SELECT-only)"
      contains: "export"
    - path: "packages/fichas/src/spike/embed-query.ts"
      provides: "embedQuery (Gemini REST, RETRIEVAL_QUERY, 768, L2) — copia de defaultEmbedder"
      contains: "RETRIEVAL_QUERY"
    - path: "packages/fichas/src/spike/embed-cache.ts"
      provides: "cache query-text → vector JSON (read-first, write-on-miss)"
      contains: "export"
    - path: "packages/fichas/src/spike/strategies.ts"
      provides: "runFtsOnly / runSemanticOnly / runRrf → boletin[]"
      contains: "runRrf"
  key_links:
    - from: "packages/fichas/src/spike/strategies.ts"
      to: "packages/fichas/src/spike/rrf.ts"
      via: "import rrf (creado en 86-01)"
      pattern: "from \"\\./rrf"
    - from: "packages/fichas/src/spike/strategies.ts"
      to: "match_proyectos"
      via: "psql select * from match_proyectos(...) o .rpc()"
      pattern: "match_proyectos"
    - from: "packages/fichas/src/spike/embed-query.ts"
      to: "gemini batchEmbedContents"
      via: "fetch header x-goog-api-key"
      pattern: "x-goog-api-key"
---

<objective>
Construir la capa de acceso a datos del spike (READ-ONLY estricto): wrapper `psql` que solo ejecuta SELECT, el embedder de queries que espeja `buscar.ts::defaultEmbedder` (RETRIEVAL_QUERY/768/L2), un cache de vectores committeable, y las 3 estrategias candidatas (FTS-solo ad-hoc, semántico-solo vía `match_proyectos`, RRF). El FTS corre con expresiones `to_tsvector('spanish', unaccent(...))` ad-hoc — CERO huella de schema.

Purpose: Materializar las 3 ramas medibles de RETR-03 reusando piezas probadas del repo (psql idiom, match_proyectos RPC, defaultEmbedder). La única lógica nueva es el SELECT FTS ad-hoc; el resto es wiring.
Output: `packages/fichas/src/spike/{psql,embed-query,embed-cache,strategies}.ts` + tests offline (los que tocan DB/red se prueban con inyección de dependencias / mocks). El scoring LIVE ocurre en el plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-CONTEXT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-PATTERNS.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-RESEARCH.md

<interfaces>
<!-- Analogs verificados: -->
<!--   psql idiom: scripts/verify-cobertura.sql header + scripts/run-with-env.mjs (spawn, shell:win32) -->
<!--   embedder:   app/lib/buscar.ts::defaultEmbedder (líneas 99-156) — COPIAR, NO importar (server-only + next-coupled) -->
<!--   semántico:  supabase/migrations/0011 match_proyectos(query_embedding vector(768), match_count int, match_threshold float8, exclude_boletin text) -->
<!--   FTS:        supabase/migrations/0032_agenda_search.sql (to_tsvector spanish + websearch_to_tsquery + ts_rank template) -->

Contrato Gemini a copiar VERBATIM de buscar.ts:70-156:
  const EMBEDDING_MODEL = "gemini-embedding-001"; const EMBEDDING_DIMS = 768;
  const GEMINI_API_BASE = "https://generativelanguage.googleapis.com"; const GEMINI_API_VERSION = "v1beta";
  // POST ${BASE}/${VERSION}/models/${MODEL}:batchEmbedContents
  // requests[].taskType = "RETRIEVAL_QUERY"  (OBLIGATORIO — asimetría SEM-03; @obs/llm NO lo emite)
  // requests[].outputDimensionality = 768;  key SOLO via header "x-goog-api-key";  L2-normalize; assert length===768

FTS ad-hoc (weight A=titulo, B=idea_matriz, C=normas-de-jsonb); MISMO unaccent ambos lados; websearch_to_tsquery SIEMPRE:
  with q as (select websearch_to_tsquery('spanish', unaccent($1)) as tsq)
  select p.boletin, ts_rank_cd(
      setweight(to_tsvector('spanish', unaccent(coalesce(p.titulo,''))),'A') ||
      setweight(to_tsvector('spanish', unaccent(coalesce(f.idea_matriz,''))),'B') ||
      setweight(to_tsvector('spanish', unaccent(coalesce(
          (select string_agg(c->>'norma',' ') from jsonb_array_elements(f.cuerpos_legales) c),''))),'C'),
      q.tsq) as rank
  from proyecto p left join proyecto_ficha f on f.boletin = p.boletin, q
  where (...titulo A || idea_matriz B...) @@ q.tsq order by rank desc limit :N;
  -- VERIFICAR el key 'norma' del jsonb cuerpos_legales contra una fila viva antes de finalizar (Assumption A3).
  -- PROBE A1 primero: select unaccent('Ñuñoa'); si falla → degradar a FTS sin unaccent + documentar (evidencia para fase 87).

Semántico (RPC existente, sin cambios):
  select boletin, similarity from match_proyectos($1::vector, :N, 0.59, null);
  // o sb.rpc("match_proyectos", { query_embedding, match_count, match_threshold: 0.59, exclude_boletin }) — cap PostgREST 1k irrelevante (N≤100)

RRF (de 86-01): import { rrf } from "./rrf";  // rrf(fts, sem, rrfK, wFts, wSem): string[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapper psql read-only + probe unaccent</name>
  <files>packages/fichas/src/spike/psql.ts, packages/fichas/src/spike/psql.test.ts</files>
  <read_first>
    - scripts/run-with-env.mjs (spawn con `shell: process.platform === "win32"`; BOM-safe env; línea ~21)
    - scripts/verify-cobertura.sql (header: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -At -f`)
    - .planning/phases/86-.../86-PATTERNS.md (sección psql.ts: idiom + SQL-injection guard V5 + probe A1 unaccent)
    - .planning/phases/86-.../86-RESEARCH.md (Pattern 1 psql read-only; Open Question 1 unaccent)
  </read_first>
  <action>
    Crear `psql.ts` con `export async function runSql(sql: string, params?: string[]): Promise<string[][]>`: spawnea `psql` con `env.SUPABASE_DB_URL` como connection string, `-At` (tuples-only unaligned), `-F` tab, `PGCLIENTENCODING=UTF8`, `shell: process.platform === "win32"`. Pasar el SQL por stdin o `-c`; los params (query text, vector serializado) van vía `psql -v` / bind, NUNCA interpolados en el string SQL (V5 — SQL injection guard). Parsear la salida `-At` split por `\n` y tab → `string[][]`.
    GUARDA READ-ONLY: exportar/usar un chequeo que rechace cualquier SQL cuyo primer token (tras trim, ignorando `with`) no sea `select` — lanzar si contiene `create|alter|insert|update|delete|drop|truncate|grant` como statement. El harness es incapaz de escribir.
    Exportar `export async function probeUnaccent(): Promise<boolean>` que corre `select unaccent('Ñuñoa')` y devuelve false si la extensión no existe (para degradar el FTS en strategies).
    Tests OFFLINE: parser de salida `-At` (dado un string tab/newline → string[][]); guarda read-only (un SQL con `insert`/`drop` lanza; un `select`/`with ... select` pasa). NO se pega a la DB en el test unit (eso es el plan 03 live).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/psql.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `psql.test.ts` verde: parser -At correcto; guarda read-only lanza en insert/update/delete/drop/alter/create y acepta select/with-select
    - `grep -n "process.platform === \"win32\"\|shell:" packages/fichas/src/spike/psql.ts` ≥1 (spawn Windows-safe)
    - `grep -ci "insert\|update\|delete\|drop\|alter\|create\|truncate\|grant" packages/fichas/src/spike/psql.ts` cuenta solo la lista de la GUARDA (no statements ejecutados) — revisión confirma que ningún literal de esos es enviado como statement
    - `grep -n "SUPABASE_DB_URL" packages/fichas/src/spike/psql.ts` ≥1 y NUNCA se imprime el valor en logs
  </acceptance_criteria>
  <done>runSql ejecuta SELECT-only con params bindeados y parsea -At; guarda read-only muerde escrituras; probeUnaccent detecta la extensión; tests offline verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Embedder Gemini RETRIEVAL_QUERY + cache de vectores</name>
  <files>packages/fichas/src/spike/embed-query.ts, packages/fichas/src/spike/embed-cache.ts, packages/fichas/src/spike/embed-cache.test.ts</files>
  <read_first>
    - app/lib/buscar.ts (líneas 70-156: constantes EMBEDDING_MODEL/DIMS/BASE/VERSION + l2normalize + defaultEmbedder REST — COPIAR verbatim, NO importar)
    - scripts/run-with-env.mjs (BOM-safe readFileSync idiom para el fs del cache)
    - .planning/phases/86-.../86-PATTERNS.md (secciones embed-query.ts key hygiene V7/V12 + embed-cache.ts committeable)
  </read_first>
  <action>
    Crear `embed-query.ts`: copiar VERBATIM de `buscar.ts` las constantes `EMBEDDING_MODEL/EMBEDDING_DIMS/GEMINI_API_BASE/GEMINI_API_VERSION`, `l2normalize`, y la lógica REST de `defaultEmbedder`. Exportar `export async function embedQuery(texts: string[]): Promise<number[][]>` que llama `batchEmbedContents` con `taskType: "RETRIEVAL_QUERY"` (OBLIGATORIO), `outputDimensionality: 768`, key SOLO por header `x-goog-api-key` (NUNCA en URL/body/logs/errores — el mensaje de error no incluye la key), y L2-normaliza + assert `length===768`. NO importar `app/lib/buscar.ts` (es server-only + next-coupled).
    Crear `embed-cache.ts`: `export async function getCachedEmbeddings(queries: string[], embed: (t: string[]) => Promise<number[][]>, cachePath: string): Promise<Map<string, number[]>>`. Lee el JSON del cache (BOM-safe, read-first), embebe SOLO las queries en miss, escribe el JSON de vuelta (committeable). El cache guarda SOLO floats (query-text → vector), CERO env values, CERO key. Embedder inyectado para testear offline.
    Test OFFLINE de `embed-cache.ts`: con un `embed` mock (devuelve vectores fijos) y un cachePath en tmp — hit no re-llama al mock; miss llama solo por lo faltante; el JSON escrito no contiene claves ni URLs. No testear `embed-query.ts` con red (eso es el live del plan 03).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/embed-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `embed-cache.test.ts` verde: cache-hit no invoca el embedder; miss invoca solo lo faltante; JSON escrito solo con floats
    - `grep -n "RETRIEVAL_QUERY" packages/fichas/src/spike/embed-query.ts` == 1 (asimetría query-side obligatoria)
    - `grep -n "x-goog-api-key" packages/fichas/src/spike/embed-query.ts` == 1 y `grep -ci "apikey\|api_key\|GEMINI_API_KEY" ...` NO aparece en ningún template de log/error string
    - `grep -n "outputDimensionality" packages/fichas/src/spike/embed-query.ts` ≥1 y assert `=== 768` presente
    - `grep -c "import.*buscar" packages/fichas/src/spike/embed-query.ts` == 0 (copia, no import)
  </acceptance_criteria>
  <done>embedQuery espeja defaultEmbedder (RETRIEVAL_QUERY/768/L2, key por header); embed-cache persiste vectores committeables read-first; test offline del cache verde.</done>
</task>

<task type="auto">
  <name>Task 3: Las 3 estrategias (FTS-solo, semántico-solo, RRF)</name>
  <files>packages/fichas/src/spike/strategies.ts, packages/fichas/src/spike/strategies.test.ts</files>
  <read_first>
    - supabase/migrations/0011_fichas_embeddings.sql (firma match_proyectos + proyecto_ficha.idea_matriz + cuerpos_legales jsonb {norma, articulos[]})
    - supabase/migrations/0032_agenda_search.sql (template FTS spanish + websearch_to_tsquery + ts_rank)
    - supabase/migrations/0008_tramitacion.sql (proyecto: titulo, boletin, boletin_num)
    - packages/fichas/src/spike/rrf.ts (de 86-01: firma rrf)
    - packages/fichas/src/spike/boletin.ts (de 86-01: detectarBoletin para el short-circuit)
    - .planning/phases/86-.../86-PATTERNS.md (sección strategies.ts: FTS ad-hoc weight A/B/C, Pitfall #1 normas en jsonb, short-circuit boletín ANTES de RRF)
  </read_first>
  <action>
    Crear `strategies.ts` exportando tres runners que devuelven `Promise<string[]>` (boletin[] rankeado), cada uno recibiendo `runSql`/`embedCache` inyectados para testear offline:
    - `runFtsOnly(query, {runSql, limit})`: construye el SELECT FTS ad-hoc (weight A=`proyecto.titulo`, B=`proyecto_ficha.idea_matriz` join por `boletin`, C=normas via `string_agg(c->>'norma')` sobre `jsonb_array_elements(f.cuerpos_legales)`), `websearch_to_tsquery('spanish', unaccent($1))` SIEMPRE, MISMO `unaccent()` en tsvector y tsquery, `order by rank desc limit :N`. Query text bindeado (NUNCA interpolado). Si `probeUnaccent()` es false, degrada a sin-unaccent y documenta.
    - `runSemanticOnly(vector, {runSql|rpc, limit, matchThreshold=0.59, excludeBoletin})`: llama `match_proyectos($vector, N, 0.59, excludeBoletin)` (la categoría "similares"/SEM-05 pasa `excludeBoletin`).
    - `runRrf(query, vector, {runSql, rrfK, limit, wFts, wSem})`: detecta boletín con `detectarBoletin(query)` → si es boletín, SHORT-CIRCUIT (devuelve el match exacto contra `proyecto.boletin`/`boletin_num`) ANTES de cualquier fusión; si no, corre FTS + semántico y fusiona con `rrf(...)` importado de 86-01. PROHIBIDO sumar scores; PROHIBIDO meter boletín en el RRF.
    ANTES de finalizar el string_agg: verificar el key `norma` del jsonb `cuerpos_legales` contra una fila viva (Assumption A3) — dejar el SQL parametrizado por si el key difiere.
    Test OFFLINE: inyectar un `runSql` mock que devuelve filas fijas → verificar que runFtsOnly parsea a boletin[]; runRrf con detectarBoletin de un boletín devuelve short-circuit (NO llama a FTS/semántico); runRrf de texto libre llama ambas ramas y fusiona vía rrf. No golpear la DB real (eso es el plan 03).
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run src/spike/strategies.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `strategies.test.ts` verde: runFtsOnly→boletin[]; runRrf(boletín)=short-circuit sin llamar ramas; runRrf(texto)=fusiona por rrf
    - `grep -c "websearch_to_tsquery" packages/fichas/src/spike/strategies.ts` ≥1 y `grep -c "to_tsquery(" ...` (crudo, sin `websearch_`) == 0
    - `grep -c "match_proyectos" packages/fichas/src/spike/strategies.ts` ≥1
    - `grep -n "string_agg\|cuerpos_legales\|jsonb_array_elements" packages/fichas/src/spike/strategies.ts` ≥1 (normas desde jsonb, no `normas_afectadas`)
    - `grep -c "normas_afectadas" packages/fichas/src/spike/strategies.ts` == 0 (columna inexistente — Pitfall #1)
    - `grep -n "from \"./rrf\"\|detectarBoletin" packages/fichas/src/spike/strategies.ts` ≥1 (reusa 86-01)
    - `pnpm --filter @obs/fichas exec tsc -b` limpio
  </acceptance_criteria>
  <done>runFtsOnly/runSemanticOnly/runRrf devuelven boletin[] rankeado; FTS con websearch_to_tsquery + unaccent ambos lados + normas desde jsonb; RRF con short-circuit de boletín fuera de la fusión; tests offline verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → PROD DB (SUPABASE_DB_URL owner) | URL privilegiada de owner; el spike DEBE ser SELECT-only |
| golden query text → SQL/RPC | Texto que se convierte en tsquery/param; nunca interpolado |
| harness → Gemini REST | La API key viaja por header; jamás en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Tampering | psql.ts (owner URL) | mitigate | Guarda read-only: rechaza cualquier statement no-SELECT (insert/update/delete/drop/alter/create/truncate/grant). Test lo prueba. Cero DDL/DML por construcción. |
| T-86-05 | Tampering | strategies.ts (FTS query) | mitigate | `websearch_to_tsquery` SIEMPRE (nunca `to_tsquery` crudo → 500s); query text bindeado por `psql -v`/param, NUNCA interpolado (V5). |
| T-86-06 | Info Disclosure | embed-query.ts (Gemini key) | mitigate | Key SOLO por header `x-goog-api-key`; nunca en URL/body/logs/errores; el mensaje de error omite la key (espejo buscar.ts:135-138). |
| T-86-07 | Info Disclosure | embed-cache.ts (cache committeable) | mitigate | El cache guarda SOLO floats (query→vector); cero env values, cero key. Revisión confirma. |
| T-86-08 | Info Disclosure | SUPABASE_DB_URL en logs | mitigate | El connection string nunca se imprime; solo se pasa a spawn env/argv. |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas test` verde (offline: parser psql, cache, strategies con mocks)
- `pnpm --filter @obs/fichas exec tsc -b` limpio
- Revisión: ningún statement no-SELECT se envía a la DB; ninguna key ni URL de conexión aparece en logs
</verification>

<success_criteria>
- runSql SELECT-only con guarda que muerde escrituras; probeUnaccent operativo
- embedQuery espeja defaultEmbedder (RETRIEVAL_QUERY/768/L2, key por header)
- embed-cache persiste vectores committeables (read-first, write-on-miss)
- 3 estrategias devuelven boletin[]; FTS ad-hoc correcto (websearch_to_tsquery, unaccent ambos lados, normas de jsonb); RRF con short-circuit de boletín
- tests offline verdes; tsc limpio
</success_criteria>

<output>
Create `.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-02-SUMMARY.md` when done
</output>
