---
phase: 100-panel-p1c-landing-panel
plan: 04
type: execute
wave: 4
depends_on: ["100-03"]
files_modified:
  - .planning/phases/100-panel-p1c-landing-panel/100-BENCHMARK.md
  - .planning/phases/100-panel-p1c-landing-panel/100-BROWSEROS-GATE.md
autonomous: false
requirements: [PANEL-03, PANEL-04]
user_setup:
  - service: cloudflare
    why: "Deploy del Worker OpenNext (build Docker node:22-slim + wrangler global) para el gate sobre el deploy real"
    dashboard_config:
      - task: "wrangler login OAuth global (host) ya presente; re-deploy estándar sin secrets nuevos"
        location: "host operador (Docker + wrangler)"
must_haves:
  truths:
    - "Existe 100-BENCHMARK.md: capturas BrowserOS de senado.cl y camara.cl (portada/actualidad/tablas ASP.NET) + crítica de diseño (qué EVITAR / qué SUPERAR) + cómo el panel iteró diseño→crítica→loop"
    - "El panel está deployado al Worker real (workers.dev) vía el runbook OpenNext Docker antes del gate"
    - "Existe 100-BROWSEROS-GATE.md: veredicto de lectura fría sobre el DEPLOY real, 390px, con getComputedStyle verificando los candados (barra cívica hsl resuelta, petróleo presente, tokens no bare-var-rotos)"
    - "El veredicto de comprensión ('qué pasó, cuándo, según qué fuente' desde el panel solo) para periodista/tramitador/ciudadano es 'comprensible' — criterio de éxito, no opcional"
  artifacts:
    - path: ".planning/phases/100-panel-p1c-landing-panel/100-BENCHMARK.md"
      provides: "Benchmark documentado de portales oficiales + crítica de diseño + loop"
      contains: "senado.cl"
    - path: ".planning/phases/100-panel-p1c-landing-panel/100-BROWSEROS-GATE.md"
      provides: "Registro del gate de lectura fría sobre el deploy real + getComputedStyle de candados"
      contains: "getComputedStyle"
  key_links:
    - from: "100-BROWSEROS-GATE.md"
      to: "https://observatorio-congreso.thevalis.workers.dev"
      via: "bros-cli.mjs open/shot/content + evaluate_script getComputedStyle a 390px"
      pattern: "workers.dev"
---

<objective>
Cerrar los dos criterios de éxito empíricos: (PANEL-03) un benchmark BrowserOS documentado de senado.cl/camara.cl con crítica de diseño y el loop diseño→crítica; y (PANEL-04) el gate de lectura fría sobre el DEPLOY real (workers.dev) con veredicto "comprensible" + verificación de candados por `getComputedStyle`. El deploy OpenNext precede al gate.

Purpose: El linter no puede garantizar comprensión humana ni resolver la cascada CSS real (solo cazable en el deploy — gotcha F55/v6.1/v8.0). El benchmark ancla el diseño contra lo que los portales oficiales hacen mal; el gate confirma que el panel es legible y que los candados resuelven en el browser real.
Output: `100-BENCHMARK.md` + `100-BROWSEROS-GATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-panel-p1c-landing-panel/100-UI-SPEC.md
@.planning/phases/100-panel-p1c-landing-panel/100-RESEARCH.md
@.planning/phases/100-panel-p1c-landing-panel/100-03-SUMMARY.md
@.planning/phases/68-.../68-BROWSEROS-GATE.md

<interfaces>
<!-- Recipe BrowserOS + deploy. De 100-RESEARCH §Recipe BrowserOS + CLAUDE.md deploy. -->

scripts/bros-cli.mjs: open <url> → "Page ID: N"; shot <PID> <path>; content <PID> (markdown); snapshot; close <PID>.
Gotchas CDP: sleep 8-10s tras open (SSR+hidratación) ANTES del shot; evaluate_script usa arg `expression`; click usa arg `element`; si CDP timeout → reabrir página.

Deploy (CLAUDE.md / project memory Camino A):
- Build OpenNext en Docker node:22-slim (build Windows rompe worker 500ea).
- robocopy a C:/Temp; wrangler global (AppData/Roaming/npm/wrangler.cmd), OAuth del host montado en /root/.config/.wrangler dentro del contenedor; correr `pnpm run deploy` DENTRO del node:22-slim.
- Re-escribir helper scripts del build dir tras cada robocopy /MIR (no están en el repo).
- Cloudflare tarda ~10-30s en propagar la versión nueva (500 intermitentes durante el lapso).
- PROD: https://observatorio-congreso.thevalis.workers.dev

Gate (68-BROWSEROS-GATE.md molde): pre-flight CDP; viewport 390px; getComputedStyle sobre elementos de candado; rúbrica de comprensión periodista/tramitador/ciudadano; registro de veredicto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark BrowserOS de senado.cl / camara.cl + crítica de diseño (PANEL-03)</name>
  <files>.planning/phases/100-panel-p1c-landing-panel/100-BENCHMARK.md</files>
  <read_first>
    100-UI-SPEC.md §"Benchmark & Cold-Read Gate" (north star: AVOID tablas densas ASP.NET/editorial/mono-cámara/números sin fecha; SURPASS panel bicameral cuantitativo con fuente+fecha por dato).
    100-RESEARCH.md §"Recipe BrowserOS (benchmark + gate)".
    scripts/bros-cli.mjs (header + comandos open/shot/content/close).
    68-BROWSEROS-GATE.md (molde de registro/rúbrica).
  </read_first>
  <action>
    Con el wrapper BrowserOS: abrir https://www.senado.cl/ y https://www.camara.cl/ (portada/actualidad + una tabla ASP.NET densa cada uno); sleep 8-10s antes de cada shot (fallback: reintentar shot tras sleep 3 si CDP timeout); capturar screenshots a C:/Temp/bench-senado*.png / bench-camara*.png; volcar `content` (markdown) para la crítica; cerrar cada página. Escribir `100-BENCHMARK.md`: por portal, qué EVITAR (tablas GridView densas, titulares editoriales/redactados, framing mono-cámara, números sin fecha, muros de filas sin proveniencia) y qué SUPERAR (panel bicameral, cuantitativo, con fuente+fecha por dato — lo único que los portales oficiales NO ofrecen). Documentar la iteración diseño→crítica→loop: comparar el panel deployado (o el layout del UI-SPEC) contra el benchmark y anotar ajustes. Si el MCP BrowserOS está caído, PAUSAR — el orquestador (tiene MCP) ejecuta las capturas y cierra esta parte.
  </action>
  <verify>
    <human-check>100-BENCHMARK.md existe con capturas de ambos portales, la crítica AVOID/SURPASS y la nota del loop diseño→crítica contra el panel.</human-check>
  </verify>
  <acceptance_criteria>
    - Capturas de senado.cl y camara.cl (portada/actualidad + tabla densa) referenciadas en el doc.
    - Crítica de diseño explícita: lista de qué EVITAR y qué SUPERAR.
    - Registro del loop diseño→crítica (cómo el panel se compara/ajusta contra el benchmark).
  </acceptance_criteria>
  <done>
    Benchmark empírico documentado con crítica y loop (PANEL-03).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy real + gate BrowserOS de lectura fría (PANEL-04)</name>
  <files>.planning/phases/100-panel-p1c-landing-panel/100-BROWSEROS-GATE.md</files>
  <what-built>
    El panel de actualidad (Plans 01-03) deployado al Worker real OpenNext (workers.dev) vía el runbook Docker node:22-slim + wrangler global. Es un checkpoint del orquestador/operador: el orquestador tiene el MCP BrowserOS y cierra el gate; el deploy es acto de operador (Docker + wrangler OAuth del host).
  </what-built>
  <action>
    (Checkpoint — orquestador/operador cierra el gate.) Deployar el panel al Worker real vía el runbook OpenNext Docker + wrangler; luego correr el gate de lectura fría con bros-cli sobre workers.dev a 390px; verificar candados por getComputedStyle; aplicar la rúbrica de comprensión; registrar el veredicto en 100-BROWSEROS-GATE.md. Ver <how-to-verify> para los pasos exactos.
  </action>
  <how-to-verify>
    1. Build + deploy: OpenNext en Docker node:22-slim, robocopy a C:/Temp (re-escribir helper scripts del build dir tras /MIR), `pnpm run deploy` DENTRO del contenedor con el OAuth del host montado; esperar ~10-30s a que Cloudflare propague la versión nueva.
    2. Gate de lectura fría sobre el DEPLOY real (NO localhost): con bros-cli.mjs abrir https://observatorio-congreso.thevalis.workers.dev, viewport 390px, sleep 8-10s antes del shot.
    3. Candados por getComputedStyle (evaluate_script, arg `expression`): la barra cívica resuelve a un valor hsl real (no vacío), el petróleo accent está presente, los tokens NO están bare-var-rotos (la cascada solo se resuelve en el deploy real — gotcha v6.1/v8.0). Confirmar cero-hex efectivo y tipografía whitelisteada renderizada. Confirmar CSP intacta (frame-ancestors/object-src/connect-src no ampliados).
    4. Rúbrica de comprensión: ¿puede un periodista / tramitador / ciudadano responder "qué pasó, cuándo, según qué fuente" desde el panel SOLO? Verificar que cada tile lleva conteo + framing factual + cobertura + fuente + fecha; que la supresión muestra su causa (no lista vacía); que NO hay ranking cross-cámara ni vocabulario insinuante.
    5. Registrar todo en `100-BROWSEROS-GATE.md` (molde 68-BROWSEROS-GATE.md): versión deployada, capturas 390px, resultados getComputedStyle, veredicto.
    Si el MCP BrowserOS está caído, el orquestador (tiene MCP) cierra el gate.
  </how-to-verify>
  <verify>
    <human-check>100-BROWSEROS-GATE.md registra la versión deployada, los resultados de getComputedStyle de los candados y el veredicto "comprensible".</human-check>
  </verify>
  <done>
    El panel está deployado al Worker real; el veredicto de lectura fría es "comprensible" con candados verificados por getComputedStyle; CSP intacta.
  </done>
  <resume-signal>Escribir "comprensible" (con versión deployada + resultados getComputedStyle en 100-BROWSEROS-GATE.md), o listar los puntos que fallaron para iterar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy real → lector humano | la cascada CSS y la comprensión solo se validan en el browser sobre el deploy, no en HTML crudo ni localhost |
| panel → periodista/tramitador/ciudadano | ¿el dato es legible y trazable sin insinuación? garantía humana que el linter no da |
| build Windows → Worker | build Windows rompe el worker (500ea) — debe ser Docker Linux |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-12 | Repudiation/Reputación | superficie que pasa tests pero se lee como insinuación | mitigate | Gate BrowserOS de lectura fría (PANEL-04): garantía humana sobre el deploy real |
| T-100-13 | Tampering | falso-positivo de candados sobre HTML crudo (cascada no resuelta) | mitigate | getComputedStyle vía evaluate_script sobre workers.dev a 390px, sleep 8-10s; NUNCA sobre localhost/HTML crudo (Pitfall 6) |
| T-100-14 | Denial of Service | build Windows → worker 500ea | mitigate | Build OpenNext en Docker node:22-slim; esperar propagación Cloudflare ~10-30s |
| T-100-15 | Tampering | CSP: nuevo origen en connect-src | accept | El panel lee de Supabase server-side → NO necesita origen nuevo; jamás quitar frame-ancestors/object-src (CONTEXT LOCKED); verificar CSP intacta en el gate |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | Phase 100 NO instala paquetes; sin superficie supply-chain |
</threat_model>

<verification>
- 100-BENCHMARK.md con capturas + crítica AVOID/SURPASS + loop.
- 100-BROWSEROS-GATE.md con versión deployada, getComputedStyle de candados (barra cívica hsl, petróleo, no bare-var), veredicto "comprensible".
- CSP intacta en el deploy (frame-ancestors/object-src/connect-src no ampliados).
</verification>

<success_criteria>
Benchmark BrowserOS documentado con crítica de diseño y loop (PANEL-03); veredicto de lectura fría sobre el deploy real es "comprensible" con candados verificados por getComputedStyle (PANEL-04).
</success_criteria>

<output>
Create `.planning/phases/100-panel-p1c-landing-panel/100-04-SUMMARY.md` when done
</output>
