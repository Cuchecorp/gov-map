---
phase: 100-panel-p1c-landing-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/bento-guards.test.ts
autonomous: true
requirements: [PANEL-01]
must_haves:
  truths:
    - "El linter anti-insinuación escanea un array SUPERFICIES_PANEL nuevo que incluye components/panel-actualidad.tsx"
    - "Los términos de timing/editorial insinuante ('último momento','a última hora','de madrugada','exprés','revivido','reactivado','zombie','resucitó','colado','la cámara más activa') están en TERMINOS_PROHIBIDOS con tildes exactas"
    - "Un mutation self-check PANEL prueba que el detector MUERDE sobre un término de timing inyectado"
    - "components/panel-actualidad.tsx está en SUPERFICIES_CERO_HEX y SUPERFICIES_TIPOGRAFIA de bento-guards"
    - "Los guards corren red (superficie declarada, archivo aún inexistente o vacío) → green cuando el componente existe; sin regresión de lockdown-guard"
  artifacts:
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "Array SUPERFICIES_PANEL + términos timing + mutation self-check PANEL"
      contains: "SUPERFICIES_PANEL"
    - path: "app/lib/bento-guards.test.ts"
      provides: "panel-actualidad.tsx en cero-hex + tipografía scans"
      contains: "panel-actualidad.tsx"
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "components/panel-actualidad.tsx"
      via: "SUPERFICIES_PANEL en el bucle de escaneo L472"
      pattern: "SUPERFICIES_PANEL"
    - from: "app/lib/bento-guards.test.ts"
      to: "components/panel-actualidad.tsx"
      via: "arrays SUPERFICIES_CERO_HEX + SUPERFICIES_TIPOGRAFIA"
      pattern: "panel-actualidad\\.tsx"
---

<objective>
Extender los TRES guards-como-test para que cubran el componente NUEVO del panel (`components/panel-actualidad.tsx`) y añadir el vocabulario de timing/editorial insinuante a la denylist — ANTES de escribir cualquier copy del panel. Este es el tripwire de honestidad: el copy nuevo pasa por el linter antes de existir.

Purpose: El contrato de honestidad (riesgo existencial #1) se hace cumplir estáticamente en CI. Extender los guards PRIMERO garantiza que ninguna línea de copy del panel se escriba sin pasar por el linter (Pitfall #6 + #3, lección BLOCKER 91).
Output: `anti-insinuacion-guard.test.ts` con `SUPERFICIES_PANEL` + términos timing + mutation self-check; `bento-guards.test.ts` con la superficie panel en cero-hex + tipografía.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-panel-p1c-landing-panel/100-PATTERNS.md
@.planning/phases/100-panel-p1c-landing-panel/100-UI-SPEC.md
@.planning/phases/98-senales-p1a-spike-de-datos/98-SPIKE-FINDINGS.md

<interfaces>
<!-- Contratos existentes en los guards; extraídos del código. NO explorar el árbol. -->

anti-insinuacion-guard.test.ts:
- L100 `const SUPERFICIES_VOTO: string[]` … (8 arrays SUPERFICIES_* con JSDoc, patrón repetido L100-282)
- L145 `const SUPERFICIES_HOME: string[]` YA incluye "app/page.tsx" + "components/actualidad-module.tsx" (NO renombrar — Pitfall 1)
- L293 `const TERMINOS_PROHIBIDOS` (lista de términos; cubre score/ranking/índice/puntaje, NO cubre timing)
- L378 `NEGACIONES_LOCKED` (leyendas que niegan un término prohibido, se restan antes de escanear)
- L414-419 `buildTermRegex` — NO es accent-insensitive (tildes exactas obligatorias)
- L472 bucle de escaneo: `for (const rel of [...SUPERFICIES_VOTO, ...SUPERFICIES_MONEY, ...SUPERFICIES_HOME, ...SUPERFICIES_BUSQUEDA, ...SUPERFICIES_PERSONAS, ...SUPERFICIES_LOBBY, ...SUPERFICIES_AGENDA, ...SUPERFICIES_DEEPLINK])`
- L64 anclaje vía `import.meta.dirname` (NO cambiar a process.cwd() — bug v8.1)
- `detectarInsinuaciones(html: string): string[]` — retorna términos hallados en el render
- mutation self-check: bloques `it(...)` por carril (ej. AGENDA L609-628)

bento-guards.test.ts:
- L69 `const SUPERFICIES_CERO_HEX: string[]` incluye bento-grid/bento-tile/app/page.tsx/actualidad-module.tsx/brand-icon.tsx
- L86 `const SUPERFICIES_TIPOGRAFIA: string[]` incluye bento-grid/bento-tile/app/page.tsx/actualidad-module.tsx
- L249-263 `WHITELIST_ARBITRARIOS` (off-steps permitidos: text-[11px], text-[13px], text-[15px], gap-[14px], gap-x-[22px], px-[9px], py-[18px], w-[3px], rounded-[2px], max-w-[1120px])
- Guard III (bare-var, L467-498) YA escanea todo app/components/** recursivamente (excluye *.test.*) → panel cubierto automáticamente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender anti-insinuacion-guard con SUPERFICIES_PANEL + términos timing + mutation self-check</name>
  <read_first>
    app/lib/anti-insinuacion-guard.test.ts (leer L100-148 para el shape de un array SUPERFICIES_* con su JSDoc; L293-370 TERMINOS_PROHIBIDOS; L378-400 NEGACIONES_LOCKED; L414-419 buildTermRegex; L472 bucle; L609-628 el it() de mutación AGENDA como molde).
    100-PATTERNS.md §"app/lib/anti-insinuacion-guard.test.ts (MODIFIED)" (Steps 1-5, verbatim).
    100-UI-SPEC.md §Copywriting Contract "Banned in panel copy".
  </read_first>
  <action>
    Paso 1 — Añadir un array NUEVO `SUPERFICIES_PANEL: string[]` (con JSDoc espejo de SUPERFICIES_HOME L145) con el único componente nuevo del panel: `"components/panel-actualidad.tsx"`. NO renombrar SUPERFICIES_HOME (ya cubre app/page.tsx + actualidad-module.tsx — Pitfall 1). Comentar que si el panel se divide en sub-tiles, esos archivos se suman aquí.
    Paso 2 — Añadir `...SUPERFICIES_PANEL` al final del array del bucle de escaneo en L472.
    Paso 3 — Añadir a TERMINOS_PROHIBIDOS (tildes EXACTAS, buildTermRegex NO es accent-insensitive): "último momento", "a última hora", "de madrugada", "exprés", "revivido", "reactivado", "zombie", "resucitó", "colado", "la cámara más activa". ANTES de añadir "top"/"los más": verificar con grep si ya están en la lista; si NO están, añadir los idioms anti-ranking exactos ("top", "los más") — son parte del contrato T-52-13 del UI-SPEC. Registrar en el commit qué se añadió por verificación.
    Paso 4 — Añadir un bloque mutation self-check `it("PANEL (100): caza timing insinuante inyectado ...", () => {...})` en el describe de mutación (molde AGENDA L609-628) que inyecte un render con "exprés" y "de madrugada" y asevere que `detectarInsinuaciones` los retorna (`expect(hits).toEqual(expect.arrayContaining(["exprés","de madrugada"]))`).
    Paso 5 — NEGACIONES_LOCKED: NO añadir nada AÚN (el copy del panel no existe; el germen no tiene negación que contenga un término prohibido). Documentar en el SUMMARY que si el copy del panel (Plan 02) introduce una leyenda que NIEGA un término prohibido, esa leyenda debe registrarse verbatim en NEGACIONES_LOCKED ANTES de que la superficie entre al escaneo (Pitfall 2). NO tocar `import.meta.dirname` (L64).
    NO crear panel-actualidad.tsx aquí (es Plan 02); el guard puede quedar tolerante a archivo inexistente (verificar el comportamiento del loader del guard — si lanza por archivo faltante, dejar el array preparado pero comentado con la nota de que se descomenta al crear el archivo, replicando cómo otros arrays manejaron superficies nuevas; preferir que el guard sea green en esta wave usando el patrón del propio archivo).
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run anti-insinuacion-guard</automated>
  </verify>
  <acceptance_criteria>
    - `SUPERFICIES_PANEL` existe y aparece en el array del bucle L472.
    - Los 10+ términos de timing/editorial + anti-ranking están en TERMINOS_PROHIBIDOS con tildes exactas.
    - El nuevo `it()` PANEL de mutación PASA (el detector muerde "exprés"/"de madrugada").
    - `anti-insinuacion-guard` suite verde sin regresión de los carriles existentes.
  </acceptance_criteria>
  <done>
    El guard anti-insinuación cubre la superficie panel y el vocabulario de timing; el mutation self-check prueba que muerde; suite anti-insinuacion verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir panel-actualidad.tsx a los guards cero-hex + tipografía de bento-guards</name>
  <read_first>
    app/lib/bento-guards.test.ts (L69-75 SUPERFICIES_CERO_HEX; L86-91 SUPERFICIES_TIPOGRAFIA; L249-263 WHITELIST_ARBITRARIOS; L467-498 Guard III bare-var).
    100-PATTERNS.md §"app/lib/bento-guards.test.ts (MODIFIED)".
    100-UI-SPEC.md §Régimen Candados (cero-hex, whitelist tipográfica, Tailwind v4 [var(--t)]).
  </read_first>
  <action>
    Añadir `"components/panel-actualidad.tsx"` a AMBOS arrays: SUPERFICIES_CERO_HEX (L69) y SUPERFICIES_TIPOGRAFIA (L86). NO tocar Guard III (bare-var) — ya escanea app/components/** recursivamente. NO añadir off-steps nuevos a WHITELIST_ARBITRARIOS: el panel reusa SOLO los ya-whitelisteados (text-[11px], text-[13px], text-[15px], gap-[14px], gap-x-[22px], px-[9px], py-[18px], w-[3px], rounded-[2px], max-w-[1120px]). Si Plan 02 requiere un off-step genuinamente nuevo, se añadirá allí CON razón documentada (patrón DEBT-05), no aquí. Igual que Task 1: mantener el guard green ante archivo aún inexistente replicando el patrón del propio archivo (comentar/preparar si el loader lanza por archivo faltante).
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run bento-guards lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - "components/panel-actualidad.tsx" aparece en SUPERFICIES_CERO_HEX y SUPERFICIES_TIPOGRAFIA.
    - WHITELIST_ARBITRARIOS sin off-steps nuevos.
    - bento-guards y lockdown-guard verdes (lockdown SIN cambios — actualidad_senales_panel ya en PUBLIC_RPC_ALLOWLIST L166, NO tocar).
  </acceptance_criteria>
  <done>
    La superficie panel está declarada en los dos guards de bento; lockdown-guard intacto y verde (14/14).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy del panel → lector | texto renderizado que podría insinuar intención/causalidad temporal |
| CI/build → régimen de candados | los guards son el contrato que impide copy insinuante o hex/tipografía fuera de whitelist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Repudiation | copy del panel (timing insinuante: "exprés","de madrugada","revivido") | mitigate | Añadir a TERMINOS_PROHIBIDOS con tildes exactas + mutation self-check que prueba que muerde, ANTES del copy (este plan) |
| T-100-02 | Tampering | bento candados (hex/tipografía fuera de whitelist en superficie nueva) | mitigate | Declarar panel-actualidad.tsx en SUPERFICIES_CERO_HEX + SUPERFICIES_TIPOGRAFIA (este plan); Guard III bare-var ya cubre components/** |
| T-100-03 | Elevation of Privilege | regresión de lockdown-guard (RPC fuera de allowlist) | accept | actualidad_senales_panel ya en PUBLIC_RPC_ALLOWLIST L166; este plan NO toca lockdown-guard; verificado verde en Task 2 |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | Phase 100 NO instala paquetes (RESEARCH §Package Legitimacy: no aplica); sin superficie de supply-chain |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run anti-insinuacion-guard bento-guards lockdown-guard` verde.
- git grep "panel-actualidad" en app/lib/*.test.ts retorna las 3 superficies (1 en anti-insinuacion, 2 en bento).
- TERMINOS_PROHIBIDOS contiene los términos de timing con tildes exactas.
</verification>

<success_criteria>
Los guards cubren la superficie panel y el vocabulario de timing ANTES de escribir copy (PANEL-01: linter home extendido a SUPERFICIES_PANEL ANTES del copy). Suite de guards verde; lockdown-guard intacto.
</success_criteria>

<output>
Create `.planning/phases/100-panel-p1c-landing-panel/100-01-SUMMARY.md` when done
</output>
