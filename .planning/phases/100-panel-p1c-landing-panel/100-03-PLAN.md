---
phase: 100-panel-p1c-landing-panel
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified:
  - app/app/page.tsx
  - app/app/page.test.tsx
autonomous: true
requirements: [PANEL-01, PANEL-02]
must_haves:
  truths:
    - "La home / monta <PanelActualidad/> (bajo Suspense con BloqueSkeleton) EN LUGAR del cuerpo producto-céntrico (los 3 tiles germen de actualidad-module.tsx)"
    - "export const dynamic = 'force-dynamic' (L19) se PRESERVA — la home sigue siendo ƒ (dinámica), no ○ (estática)"
    - "El hero (kicker+h1+SearchBox), el tile accent '¿Cómo leer esto?' y los 3 entry-cards conservan su copy LOCKED byte-idéntico; la URL, section[id] y scroll-margin intactos; max-w-[1120px] preservado"
    - "page.test.tsx se actualiza para mockear @/components/panel-actualidad (PanelActualidad: () => null) y aseverar el montaje del panel; Contract 1 (hero) y Contract 2 (accent + entry-cards) quedan byte-idénticos"
    - "La suite completa (app 1252+), tsc y los guards quedan verdes sin regresión"
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Landing con panel de actualidad montado, hero + candados intactos"
      contains: "PanelActualidad"
    - path: "app/app/page.test.tsx"
      provides: "Mock de panel-actualidad + Contract-3 reescrito al montaje del panel; Contract 1/2 sin cambio"
      contains: "@/components/panel-actualidad"
  key_links:
    - from: "app/app/page.tsx"
      to: "app/components/panel-actualidad.tsx"
      via: "import + <Suspense><PanelActualidad/></Suspense>"
      pattern: "PanelActualidad"
    - from: "app/app/page.tsx"
      to: "force-dynamic"
      via: "export const dynamic = 'force-dynamic' (L19, load-bearing)"
      pattern: "force-dynamic"
    - from: "app/app/page.test.tsx"
      to: "app/components/panel-actualidad.tsx"
      via: "vi.mock('@/components/panel-actualidad', ...) espejo del germ mock"
      pattern: "@/components/panel-actualidad"
---

<objective>
Montar `<PanelActualidad/>` en `app/app/page.tsx` reemplazando el CUERPO producto-céntrico (los 3 tiles germen votado/urgencias/frescura de `actualidad-module.tsx` bajo Suspense), conservando el hero LOCKED, los candados de régimen, la URL/anchors y `force-dynamic`. Actualizar en el mismo movimiento el test pre-existente `page.test.tsx` (que mockea los germ tiles y asevera el retiro del wrapper lineal) para que refleje el montaje del panel. Cerrar con la suite completa verde.

Purpose: PANEL-01 (la landing muestra las señales SEN validadas, no el folleto producto-céntrico, reusando BentoGrid/tokens y conservando candados) + PANEL-02 (cero agregación on-read, URL intacta). Es una self-modification quirúrgica: reemplaza el cuerpo, no el hero. El test que hoy hace de contrato de la home DEBE moverse con el código o rompe (mockea los germ tiles y asevera "no ActualidadModule lineal"; al montar el panel esos asserts quedan obsoletos).
Output: `page.tsx` con el panel montado; `page.test.tsx` actualizado (mock del panel + Contract 3 reescrito); suite/tsc/guards verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-panel-p1c-landing-panel/100-PATTERNS.md
@.planning/phases/100-panel-p1c-landing-panel/100-UI-SPEC.md
@.planning/phases/100-panel-p1c-landing-panel/100-02-SUMMARY.md

@app/app/page.tsx
@app/app/page.test.tsx

<interfaces>
<!-- Contrato de la home actual. Extraído del código. -->

app/app/page.tsx:
- L19 `export const dynamic = "force-dynamic";` — LOAD-BEARING (sin él / se prerenderiza estática → stale/500; gotcha F50). NO borrar.
- L35-45 `BloqueSkeleton` (fallback honesto de Suspense; reusar).
- L48-53 `EXAMPLE_CHIPS` — LOCKED.
- L58-81 `ENTRY_CARDS` (los 3 entry tiles /buscar,/parlamentarios,/agenda) — copy LOCKED.
- L87 `max-w-[1120px]` container (whitelisted).
- L90-107 hero tile (kicker + h1 + SearchBox) — copy LOCKED byte-idéntico (v8.1 D1: operador ANULÓ cambios de copy; NO autorizado cambiar).
- L112-132 tile accent "¿Cómo leer esto?" → /sobre — copy LOCKED.
- L136-170 `<nav className="contents">` con los ENTRY_CARDS — copy LOCKED; PUEDE moverse debajo del panel (discreción de layout).
- L172-183 los 3 tiles germen bajo Suspense (VotadoEstaSemana / UrgenciasVigentes / UltimaActualizacion) → ESTO se REEMPLAZA por el panel.
- `:where([id]) { scroll-margin-top: 5rem }` + section[id] anchors — intactos (gotcha v8.0).

PanelActualidad (de Plan 02): `export async function PanelActualidad()` en @/components/panel-actualidad — RSC, se monta bajo Suspense.

app/app/page.test.tsx (test pre-existente — se MODIFICA en este plan):
- L26-29 mock de `next/navigation` (useRouter push) — NO tocar.
- L34-38 `vi.mock("@/components/actualidad-module", ...)` mockea los 3 germ tiles a `() => null` — este mock queda si `actualidad-module.tsx` sigue importado por page.tsx; si el panel deja de importarlo, se retira/ajusta con el import real.
- L41-54 mock de `next/link` — NO tocar.
- Contract 1 (héroe editorial, describe L67-151) — hero/pills/CTA/trust-line/stats: byte-idéntico, NO tocar.
- Contract 2 (accent + 3 entry tiles, describe L160-245) — accent `/sobre`, copy `¿Cómo leer esto?`, `Ver metodología →`, BANNED_VOCAB, 3 entry hrefs + copy verbatim, `<nav aria-label="Secciones del sitio">`, glyph aria-hidden: byte-idéntico, NO tocar.
- BENTO-05 (describe L267-381) — colapso/orden DOM/landmarks/form a11y: se ancla a hrefs LOCKED + BentoGrid + h1; si el panel cambia el orden DOM de los entry-cards (se mueven debajo), (b) y (e) pueden requerir ajuste MÍNIMO del orden esperado — solo si el layout los reubica; los hrefs y su presencia NO cambian.
- Contract 3 (describe L385-403) — HOY asevera: `HomeModule.dynamic === "force-dynamic"`, render sin throw con germ tiles mockeados a null, y AUSENCIA del wrapper lineal ActualidadModule (`[aria-label="Actualidad"]` y `.max-w-5xl` null). ESTO es lo que rompe al montar el panel → se REESCRIBE (ver Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Montar PanelActualidad en page.tsx reemplazando el cuerpo producto-céntrico</name>
  <read_first>
    app/app/page.tsx (COMPLETO — identificar exactamente el bloque L172-183 de los 3 tiles germen bajo Suspense; el hero L90-107; el accent L112-132; los ENTRY_CARDS L58-81 + nav L136-170; force-dynamic L19; BloqueSkeleton L35-45).
    app/components/panel-actualidad.tsx (la firma de PanelActualidad + cómo emite sus tiles — para montar el número correcto de Suspense boundaries).
    100-PATTERNS.md §"app/app/page.tsx (MODIFIED)" (MUST NOT change + Mount pattern).
    100-UI-SPEC.md §Panel Layout (orden de tiles + spans; DOM order = collapse order a 390px).
  </read_first>
  <action>
    Importar `PanelActualidad` de "@/components/panel-actualidad". Reemplazar el bloque de los 3 tiles germen (L172-183: los <Suspense> de VotadoEstaSemana/UrgenciasVigentes/UltimaActualizacion) por el panel, envuelto en `<Suspense fallback={<BloqueSkeleton span={N}/>}>` (reusar BloqueSkeleton). Mantener el orden DOM del UI-SPEC §Panel Layout (hero → accent "¿Cómo leer esto?" → tiles del panel → entry-cards abajo) de modo que a 390px colapse en orden de lectura. Los ENTRY_CARDS pueden moverse DEBAJO del panel (discreción de layout) pero su COPY queda byte-idéntico. NO tocar: force-dynamic (L19), hero (L90-107), accent tile (L112-132), copy de ENTRY_CARDS/EXAMPLE_CHIPS, la URL, section[id]/scroll-margin, max-w-[1120px]. Decidir qué pasa con actualidad-module.tsx: RESOLVED (RESEARCH Open Question #1) — conservar el archivo pero DESMONTADO (dejar de montar sus 3 tiles), NO borrarlo en esta wave para no arrastrar cambios de guard (sigue en SUPERFICIES_HOME). Si tras desmontar ya no se importa `@/components/actualidad-module` en page.tsx, ajustar el import (y el mock del test — Task 2 lo cubre). Usar SOLO spans 2|4|6 en los tiles (BentoTile no soporta otros). Tailwind v4 [var(--t)]; cero-hex.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit && pnpm test -- --run anti-insinuacion-guard bento-guards lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` importa y monta `<PanelActualidad/>` bajo Suspense; los 3 tiles germen ya no se montan.
    - `export const dynamic = "force-dynamic"` intacto en L19 (grep confirma).
    - Copy hero/accent/ENTRY_CARDS/EXAMPLE_CHIPS byte-idéntico (git diff no toca esos rangos salvo reubicación de nodos JSX sin cambiar su texto).
    - anti-insinuacion + bento + lockdown guards verdes; tsc limpio.
  </acceptance_criteria>
  <done>
    La landing monta el panel de actualidad en lugar del cuerpo producto-céntrico; hero y candados intactos; guards y tsc verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar page.test.tsx (mock del panel + Contract 3 reescrito) y correr suite completa + tsc + audit</name>
  <read_first>
    app/app/page.test.tsx (COMPLETO — L34-38 el germ mock como molde; Contract 3 describe L385-403 que rompe; Contract 1 L67-151 y Contract 2 L160-245 que NO cambian; BENTO-05 L267-381 orden DOM).
    app/components/panel-actualidad.tsx (la firma export `PanelActualidad` — para mockearlo con el path correcto).
    100-RESEARCH.md §Validation Architecture §Sampling Rate (phase gate: suite completa + tsc + audit 0).
    100-VALIDATION.md §Sampling Rate (before close).
  </read_first>
  <action>
    Paso A — Mock del panel: añadir un `vi.mock("@/components/panel-actualidad", () => ({ PanelActualidad: () => null }))` ESPEJO del germ mock existente (L34-38), para aislar el hero del runtime Supabase en jsdom (el panel es un RSC async que llama la RPC). Colocarlo junto a los otros mocks, ANTES del `import Home from "./page"`. Si page.tsx dejó de importar `@/components/actualidad-module`, retirar/ajustar el germ mock L34-38 acorde (si se sigue importando aunque desmontado, dejarlo).
    Paso B — Reescribir Contract 3 (describe L385-403) para reflejar el montaje del panel EN LUGAR de los germ tiles retirados:
      - Conservar el assert `expect(HomeModule.dynamic).toBe("force-dynamic")` (protege la build-marker; ver verify de esta tarea).
      - Conservar el assert `expect(() => render(<Home />)).not.toThrow()` (render sin throw con el panel mockeado a null).
      - REEMPLAZAR el assert "NO wrapper lineal ActualidadModule" por asserts del NUEVO estado: (1) el cuerpo producto-céntrico germen ya NO se monta (los headings de los germ tiles no están — ya null por mock, o aseverar ausencia del `[aria-label="Actualidad"]`/`.max-w-5xl` si se quiere conservar la garantía de no-regresión del wrapper lineal, que sigue siendo válida); (2) el panel ESTÁ presente en el árbol montado bajo Suspense (p.ej. aseverar que page.tsx importa/monta PanelActualidad — dado el mock a `() => null`, aseverar el montaje vía la estructura estable: el Suspense boundary del panel existe donde antes estaban los germ tiles). Preferir un assert robusto en jsdom (estructura/DOM, no píxeles).
    Paso C — Contract 1 (hero, L67-151) y Contract 2 (accent + entry-cards, L160-245): NO tocar (byte-idénticos). Si el layout de Task 1 movió los entry-cards debajo del panel, ajustar SOLO el orden esperado en BENTO-05 (b)/(e) si el test lo asevera por posición; los hrefs, copy y presencia NO cambian.
    Paso D — Correr la suite completa del monorepo + tsc + audit. Si hay regresión introducida por el montaje (imports, tipos, snapshots), corregirla dentro de page.tsx / panel-actualidad.tsx / page.test.tsx SIN tocar el copy LOCKED ni los candados. Deuda pre-existente conocida (STATE): `app/lib/buscar.test.ts:193` puede fallar por drift Phase 89 (similarity 0→null); si aparece, documentarlo como deuda pre-existente fuera de alcance en el SUMMARY, NO arreglarlo aquí.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run page && pnpm test -- --run && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `page.test.tsx` mockea `@/components/panel-actualidad` (`PanelActualidad: () => null`) espejo del germ mock.
    - Contract 3 reescrito: `HomeModule.dynamic === "force-dynamic"` PRESERVA la garantía de build dinámico (la home sigue ƒ, no ○); render sin throw; asserts reflejan el panel montado (no los germ tiles retirados).
    - Contract 1 (hero) y Contract 2 (accent + entry-cards) byte-idénticos (git diff no toca sus asserts salvo, a lo sumo, orden DOM en BENTO-05 si los entry-cards se reubicaron).
    - `page.test.tsx` PASA verde tras el swap.
    - Suite app verde (baseline 1252+ v9.0) salvo deuda pre-existente documentada (buscar.test.ts:193); `pnpm exec tsc --noEmit` limpio; `pnpm audit` sin cambios respecto al baseline; ninguna regresión NUEVA atribuible a Phase 100.
  </acceptance_criteria>
  <done>
    page.test.tsx refleja el montaje del panel (mock + Contract 3 reescrito), Contract 1/2 intactos y verde; la garantía force-dynamic sigue protegida por el test; suite/tsc/audit verdes (deuda pre-existente aparte, documentada); el código de Phase 100 listo para deploy + gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → runtime | force-dynamic decide si / se hornea estática (500 en runtime) o se sirve dinámica |
| copy hero LOCKED → operador | cambio de copy hero requiere autorización de operador (no otorgada) |
| SEO/anchors → consumidores externos | URL y section[id] son contrato con enlaces/bookmarks externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-09 | Denial of Service | home horneada estática por force-dynamic omitido → 500/stale | mitigate | Preservar `export const dynamic = "force-dynamic"` L19; la garantía queda protegida por `expect(HomeModule.dynamic).toBe("force-dynamic")` en page.test.tsx (ahora en scope) — falla el test si se borra (Pitfall 5) |
| T-100-10 | Tampering | cambio no autorizado del copy hero LOCKED | mitigate | NO tocar L90-132; git diff acotado a montaje del panel; copy byte-idéntico (v8.1 D1); Contract 1/2 de page.test.tsx sin cambio (asertan el copy) |
| T-100-11 | Repudiation | ruptura de SEO/anchors (URL o section[id] cambiados) | mitigate | URL raíz sin cambio; section[id]/scroll-margin intactos (gotcha v8.0); verificado en git diff + BENTO-05 orden DOM |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | Phase 100 NO instala paquetes; sin superficie supply-chain |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` verde (+ tsc + audit) salvo deuda pre-existente documentada.
- `page.test.tsx` verde: Contract 1/2 sin cambio, Contract 3 reescrito al panel, `HomeModule.dynamic === "force-dynamic"` pasa (garantía de build dinámico — la home sigue ƒ).
- grep `force-dynamic` en page.tsx presente.
- git diff de page.tsx: los rangos hero/accent/ENTRY_CARDS/EXAMPLE_CHIPS sin cambios de texto; URL/section[id] intactos.
</verification>

<success_criteria>
La landing muestra el panel de actualidad en lugar del bento producto-céntrico, reusando BentoGrid/tokens y conservando los candados (PANEL-01); cero agregación on-read, URL intacta (PANEL-02); force-dynamic preservado (protegido por el test); page.test.tsx actualizado y verde con Contract 1/2 byte-idénticos; suite/tsc/guards verdes sin regresión nueva.
</success_criteria>

<output>
Create `.planning/phases/100-panel-p1c-landing-panel/100-03-SUMMARY.md` when done
</output>
</content>
</invoke>
