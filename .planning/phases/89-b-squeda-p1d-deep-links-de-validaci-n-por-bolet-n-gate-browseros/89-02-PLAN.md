---
phase: 89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/validacion-fuente.tsx
  - app/components/validacion-fuente.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - scripts/validar-deeplinks.mjs
autonomous: true
requirements: [TRACE-01, TRACE-02, TRACE-03]
must_haves:
  truths:
    - "La ficha de proyecto muestra una sección 'Valida este dato en la fuente' con deep-link Senado SIEMPRE, Cámara SOLO si prm_id_camara != null, BCN omitido del DOM"
    - "La fecha de captura ('según fuente al {fecha}') es visible junto a los links"
    - "El respaldo R2 muestra fecha + hash abreviado del source_snapshot SIN link de descarga; solo cuando r2_path empieza con 'tramitacion/' (allowlist de prefijo)"
    - "Un script reproducible valida deep-links vivos (HTTP 200 + content-match del boletín) sobre una muestra >=10, curl-first para Cámara"
  artifacts:
    - path: "app/components/validacion-fuente.tsx"
      provides: "Sección de deep-links + fecha + respaldo R2 sin descarga"
      min_lines: 40
    - path: "app/components/validacion-fuente.test.tsx"
      provides: "Tests TRACE-01/03 (fail-honest + allowlist prefijo)"
    - path: "scripts/validar-deeplinks.mjs"
      provides: "Validación empírica curl 200 + content-match (TRACE-02)"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "app/components/validacion-fuente.tsx"
      via: "sección hermana mt-12 + Suspense + entrada rail"
      pattern: "ValidacionFuenteSection"
    - from: "app/components/validacion-fuente.tsx"
      to: "safeExternalHref"
      via: "guard obligatorio en todo href externo"
      pattern: "safeExternalHref"
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "source_snapshot"
      via: "read directo service_role .from() (no RPC)"
      pattern: "from\\(.source_snapshot.\\)"
---

<objective>
UI de trazabilidad accionable (TRACE-01/02/03) en la ficha de proyecto: una sección "Valida este dato en la fuente" que construye deep-links server-side fail-honest — Senado SIEMPRE (boletín en mano), Cámara SOLO si `prm_id_camara != null` (poblado por Plan 01), BCN omitido (sin `idNorma` en datos) — más la fecha de captura visible y el respaldo del snapshot R2 (fecha + hash, SIN descarga por seguridad — la descarga pública queda como deuda 95/96). Y un script reproducible que valida los deep-links vivos por curl (HTTP 200 + content-match), curl-first para Cámara por el WAF.

Purpose: TRACE-01 (deep-link preciso), TRACE-02 (validación empírica automatizable), TRACE-03 (fecha + respaldo). La seguridad gana al feature: el respaldo R2 NUNCA expone el bucket crudo (PII vive en R2); resolución de key server-side por boletín + allowlist de prefijo `tramitacion/*`.
Output: `validacion-fuente.tsx` + test; wiring en la ficha; `scripts/validar-deeplinks.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-RESEARCH.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-PATTERNS.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-UI-SPEC.md

# Análogos (leer para copiar idioms de link externo + no-leak de key R2)
@app/components/provenance-badge.tsx
@app/app/proyecto/[boletin]/page.tsx

<interfaces>
<!-- Contratos extraídos del pattern-map/UI-SPEC. NO re-explorar el árbol. -->

safeExternalHref (app/lib/utils.ts:15-23):
- `safeExternalHref(url: string): string | null` — solo http/https; javascript:/data: → null (#9). OBLIGATORIO en todo href externo.

provenance-badge.tsx (idiom link externo, :37,:58-71):
- `<a href={safeUrl} target="_blank" rel="noopener noreferrer" aria-label="... (abre en nueva pestaña)">Texto ↗</a>`
- glyph `↗` como indicador externo; ProvenanceBadge ya renderiza "Actualizado hace X · {fuente}" con esStale amber.

Deep-link URLs (verificadas live 2026-07-21, RESEARCH §Deep-links):
- Senado (SIEMPRE): `https://tramitacion.senado.cl/appsenado/templates/tramitacion/index.php?boletin_ini=${encodeURIComponent(boletin)}` — boletín COMPLETO con sufijo (sin sufijo → lista, no ficha).
- Cámara (SOLO si prm_id_camara != null): `https://www.camara.cl/legislacion/proyectosdeley/tramitacion.aspx?prmID=${encodeURIComponent(prmId)}&prmBOLETIN=${encodeURIComponent(boletin)}`
- BCN: OMITIDO del DOM (cuerpos_legales = {norma, articulos}, sin idNorma). NO fila, NO placeholder, NO link genérico.

page.tsx (ficha SSR):
- `leerProyecto(boletin)` hace `select("*")` → `prm_id_camara` viaja SIN cambiar la query (columna aditiva Plan 01). `createServerSupabase()` = service_role.
- Secciones hermanas `<section className="mt-12">` (frontier rule LOCKED); Suspense + Skeleton por sección.
- `navEntries` (`:245-274`) alimenta el rail; `RailSkeleton` (`:580-595`) DEBE igualar el count o hay CLS (WR-02).
- Precedente no-leak: `page.tsx:322-328` — `texto_r2_path` es key interna, `sourceUrl: null`. NO renderizar key R2 como href.

source_snapshot (mig 0002, read directo permitido bajo Camino A — NO es PII_TABLE, lockdown-guard block B `:133-144`):
- `sb.from("source_snapshot").select("content_hash, fetched_at, r2_path").eq("source","leyes").eq("resource", boletin).order("date_bucket",{ascending:false}).limit(1).maybeSingle()`
- ALLOWLIST de prefijo: servir SOLO cuando `r2_path` empieza con "tramitacion/". JAMÁS "infoprobidad/"/"servel"/"money"/"rut". Mostrar fecha + `content_hash.slice(0,12)` — NUNCA `r2_path` como href.

UI-SPEC copy LOCKED:
- h2: "Valida este dato en la fuente"
- Senado: "Ver en el Senado ↗" (secundario "Ficha de tramitación oficial")
- Cámara: "Ver en la Cámara ↗" (secundario "Ficha de tramitación oficial")
- fecha: "según fuente al {fecha_captura}" (vía ProvenanceBadge)
- respaldo: "Respaldo del {fetched_at} · hash {content_hash.slice(0,12)}…" + leyenda "Esto decía la fuente ese día."
- Anti-insinuación: Senado vs Cámara por TEXTO (label), NUNCA por color; petróleo --accent-product solo para el link (nunca fill). Sin `score`/`ranking`/`nivel`.
- Touch target ≥44px (min-h-11); target=_blank + rel=noopener noreferrer; aria-label con fuente + "(abre en nueva pestaña)".

Validación empírica (RESEARCH §Validación Empírica, verificado live):
- UA="ObservatorioCongreso360/1.0 (+https://observatorio-congreso.thevalis.workers.dev; contacto: sanchez.rossi@gmail.com)"
- Senado: `curl -sL -A "$UA" --max-time 40 --connect-timeout 15 "<senado-url>"` → HTTP 200 + `grep -F "$boletin"`.
- Cámara: `curl -s -A "$UA" --max-time 25 "<camara-url>"` → HTTP 200 + `grep -F "$boletin"` (curl-first pasa el WAF).
- sleep 3 entre boletines. Muestra ≥10 incl. golden `14309-04` + fixture `16572-06`. prmID sale del backfill (Plan 01), NO adivinado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente validacion-fuente.tsx (deep-links fail-honest + fecha + respaldo R2)</name>
  <files>app/components/validacion-fuente.tsx, app/components/validacion-fuente.test.tsx</files>
  <behavior>
    - Test (TRACE-01): con boletín `14309-04` → renderiza el link Senado con `boletin_ini=14309-04` (boletín completo, encodeURIComponent) SIEMPRE.
    - Test (TRACE-01): con `prm_id_camara="17140"` y boletín `16572-06` → renderiza link Cámara con `prmID=17140&prmBOLETIN=16572-06`.
    - Test (TRACE-01): con `prm_id_camara=null` → NO renderiza fila/link Cámara (fail-honest); sin placeholder ni "no disponible".
    - Test (TRACE-01): BCN NUNCA aparece en el DOM (sin idNorma).
    - Test (TRACE-03): con source_snapshot `r2_path="tramitacion/16572-06/..."` → muestra "Respaldo del {fecha} · hash {12 chars}" + leyenda "Esto decía la fuente ese día"; NUNCA un href con el r2_path.
    - Test (TRACE-03): con `r2_path="infoprobidad/..."` (o cualquier prefijo != "tramitacion/") → respaldo OMITIDO (allowlist de prefijo muerde).
    - Test (guard): todo href pasa por safeExternalHref (un href javascript:/data: no aparece).
  </behavior>
  <action>Crear `app/components/validacion-fuente.tsx` (`ValidacionFuenteSection`) como server component. Recibe los datos ya resueltos (boletin, prm_id_camara, fecha_captura, y el registro de source_snapshot resuelto en page.tsx) — o resuelve el source_snapshot read internamente vía `createServerSupabase()` si es más limpio (Claude's Discretion; si lee source_snapshot dentro del componente, usar el read directo `.from()` del interfaces block, NO una RPC nueva). Construir `senadoUrl` SIEMPRE y `camaraUrl` solo si `prm_id_camara != null` (patrón del interfaces block). BCN: no renderizar nada. Pasar cada URL por `safeExternalHref` antes del render; `target="_blank" rel="noopener noreferrer"`, glyph `↗`, aria-label con fuente + "(abre en nueva pestaña)", touch target `min-h-11`. Reusar `ProvenanceBadge` para "según fuente al {fecha_captura}". Respaldo R2: mostrar `Respaldo del {fetched_at} · hash {content_hash.slice(0,12)}…` + leyenda "Esto decía la fuente ese día." SOLO si `r2_path` empieza con `"tramitacion/"` (allowlist de prefijo — helper puro testeable); JAMÁS renderizar `r2_path` como href (precedente page.tsx:322-328). Aplicar tokens UI-SPEC: petróleo `--accent-product` solo en el link/underline+focus, nunca fill; Senado vs Cámara por label de texto, nunca por color; copy factual sin `score`/`ranking`/`nivel`. Escribir el `.test.tsx` con los 7 casos de behavior (vitest + testing-library, patrón de otros tests de componentes en app/components). Incluir un `ValidacionFuenteSkeleton` shape-matched (Skeleton existente) para el Suspense fallback que consume page.tsx.</action>
  <verify>
    <automated>pnpm --filter ./app test -- --run validacion-fuente</automated>
  </verify>
  <done>Componente renderiza deep-links fail-honest (Senado siempre, Cámara condicional, BCN nunca), fecha vía ProvenanceBadge, respaldo R2 con allowlist de prefijo y sin href de bucket; los 7 tests pasan; anti-insinuación y tokens respetados.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring en la ficha (page.tsx) + script de validación empírica</name>
  <files>app/app/proyecto/[boletin]/page.tsx, scripts/validar-deeplinks.mjs</files>
  <action>MODIFICAR `app/app/proyecto/[boletin]/page.tsx`: montar `<ValidacionFuenteSection>` como sección hermana `<section id="validacion-fuente" className="mt-12">` dentro de `<Suspense fallback={<ValidacionFuenteSkeleton/>}>` (frontier rule LOCKED). Resolver el read de `source_snapshot` server-side por boletín (read directo `.from("source_snapshot")` con service_role del interfaces block — NO RPC nueva, permitido bajo Camino A porque source_snapshot no es PII_TABLE) y pasar los datos al componente (o dejar que el componente los lea; consistente con Task 1). `prm_id_camara` ya viaja por `leerProyecto` (`select("*")`) — no cambiar esa query. Añadir la entrada `{ id: "validacion-fuente", label: "..." }` a `navEntries` (`:245-274`) Y actualizar el count de `RailSkeleton` (`:580-595`) para que iguale el nº de entradas (evita CLS, WR-02). Preservar honest-error #34: un fallo real de DB/red se LANZA, jamás se degrada a empty (no fabricar respaldo vacío a partir de un error). Crear `scripts/validar-deeplinks.mjs` (Node ESM, `#!/usr/bin/env node`, sin deps, patrón de `scripts/bros-cli.mjs`): recibe una muestra de `{boletin, prmId}` (≥10, incl. golden `14309-04` + fixture `16572-06`; leerla de un flag `--muestra <json>` o de un array/archivo; el prmID debe venir del backfill YA poblado — el script puede consultar PROD por los boletines de la muestra o recibirlos como input, NO adivinar). Por cada boletín: construir Senado+Cámara URL igual que el componente, `curl` con el UA identificatorio (Senado `--max-time 40 --connect-timeout 15`; Cámara `--max-time 25`, curl-first por WAF), aseverar HTTP 200 + content-match `grep -F "$boletin"`, `sleep 3` entre boletines (rate-limit LOCKED). Reportar tabla por boletín (fuente, HTTP, match sí/no) y exit 1 si algún assert falla. JAMÁS construir rutas con buildId ni URLs de sesión (TRACE-02).</action>
  <verify>
    <automated>pnpm --filter ./app test -- --run && pnpm --filter ./app exec tsc --noEmit && node --check scripts/validar-deeplinks.mjs</automated>
  </verify>
  <done>La ficha monta la sección `mt-12` con el rail actualizado (count consistente); source_snapshot leído server-side sin RPC; honest-error preservado; `validar-deeplinks.mjs` construye las URLs correctas (sin buildId), valida por curl 200 + content-match con rate-limit y exit code honesto. Suite app verde; tsc limpio; script parsea.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ficha SSR → source_snapshot (service_role) | Read de un manifiesto que apunta a keys R2 (algunas PII) |
| Ficha SSR → navegador (deep-link href) | URL externa construida con datos de DB |
| Componente → hosts gubernamentales (senado.cl/camara.cl) | href a host fijo con boletín/prmID en query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-06 | Information Disclosure | respaldo R2 en la ficha | mitigate | Allowlist de prefijo `tramitacion/*` (helper puro testeado); mostrar solo fecha+hash, NUNCA `r2_path` como href; resolución server-side por boletín (cero input de key del usuario); precedente page.tsx:322-328 |
| T-89-07 | Tampering/SSRF | construcción de deep-link | mitigate | Hosts fijos (tramitacion.senado.cl/www.camara.cl), boletín validado (BOLETIN_RE upstream) + `encodeURIComponent`; prmID de fuente zod-validada; nunca URL de input libre; JAMÁS rutas buildId/sesión |
| T-89-08 | XSS | href de fuente | mitigate | `safeExternalHref` (solo http/https, rechaza javascript:/data:) en TODO href externo |
| T-89-09 | Information Disclosure | read source_snapshot en árbol público | accept | source_snapshot NO es PII_TABLE; read directo `.from()` permitido bajo Camino A (service_role, lockdown-guard block B); sin RPC nueva → sin tocar PUBLIC_RPC_ALLOWLIST |
| T-89-10 | DoS (self, WAF Cámara) | script validar-deeplinks | mitigate | curl-first (verificado 200), rate-limit `sleep 3`, UA identificatorio |
| T-89-SC | Tampering | npm installs | mitigate | N/A — cero instalaciones (reusa Tooltip/Skeleton/ProvenanceBadge existentes; RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` verde (incluye validacion-fuente + lockdown-guard sin morder + no-nueva-RPC en allowlist).
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- `node --check scripts/validar-deeplinks.mjs` OK.
- Runtime (tras backfill del Plan 01): `node scripts/validar-deeplinks.mjs` con la muestra → todos HTTP 200 + content-match (validado en Plan 03 sobre deploy real / o local contra fuente).
</verification>

<success_criteria>
- Sección "Valida este dato en la fuente" en la ficha: Senado SIEMPRE, Cámara condicional, BCN omitido; fecha visible; respaldo R2 (fecha+hash, sin descarga, allowlist prefijo).
- Todo href externo pasa por safeExternalHref; sin key R2 como href; anti-insinuación respetada.
- Script reproducible valida deep-links por curl (200 + content-match, curl-first Cámara), rate-limit 2-3s, sin buildId.
</success_criteria>

<output>
Create `.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-02-SUMMARY.md` when done
</output>
