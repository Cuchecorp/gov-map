---
phase: 94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - app/components/agenda-filtros.tsx
  - app/components/agenda-filtros.test.tsx
  - app/app/agenda/page.tsx
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [CIT-04]

must_haves:
  truths:
    - "El island agenda-filtros filtra EN MEMORIA el slice serializado por el server; NUNCA importa @/lib/supabase ni .rpc/.from"
    - "Filtra por cámara (chips), comisión (facetas + Sin dato), rango de fechas (inputs date acotados a lo cargado), boletín mencionado (boletin-detector)"
    - "Los counts son honestos 'de estas N citaciones cargadas de esta semana' (sobre el slice completo, no el filtrado)"
    - "Facetas con count=0 quedan disabled; 'Esta semana' resetea todos los filtros"
    - "El server pasa filas planas + el dayKey (Chile) ya calculado; el island reagrupa por día en memoria"
    - "El linter anti-insinuación cubre la superficie nueva sin falso-positivo (NEGACIONES_LOCKED si hay copy que niega términos)"
  artifacts:
    - path: "app/components/agenda-filtros.tsx"
      provides: "Island client de filtros de periodista (FichaRail; facetas + counts + reagrupación por día)"
      min_lines: 120
      contains: "use client"
    - path: "app/app/agenda/page.tsx"
      provides: "Serialización del slice plano + dayKey Chile + montaje del island"
      contains: "AgendaFiltros"
  key_links:
    - from: "app/app/agenda/page.tsx"
      to: "app/components/agenda-filtros.tsx"
      via: "montaje del island con slice serializado + dayKey"
      pattern: "AgendaFiltros"
    - from: "app/components/agenda-filtros.tsx"
      to: "app/lib/boletin-detector.ts"
      via: "filtro por boletín mencionado"
      pattern: "detectarBoletin"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "SUPERFICIES_AGENDA"
      via: "scan extendido a la superficie nueva"
      pattern: "agenda-filtros"
---

<objective>
Island de filtros de periodista para /agenda (CIT-04): filtra en memoria el slice de
la semana cargada por cámara / comisión / rango de fechas / boletín mencionado, con
counts honestos "de estas N", y reagrupa por día-Chile. Extiende el linter
anti-insinuación a la superficie nueva.

Purpose: un periodista puede afinar la semana cargada (cámara, comisión, rango,
boletín) sin re-consultar — contrato FichaRail LOCKED (jamás toca Supabase).

Output: agenda-filtros.tsx (island) + serialización del slice en page.tsx + montaje +
linter extendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-CONTEXT.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-UI-SPEC.md
@app/components/parlamentarios-filtro.tsx
@app/app/agenda/page.tsx
@app/lib/boletin-detector.ts

<interfaces>
<!-- Espejo estructural del island de personas — FacetChip + counts-de-estos-N VERBATIM: -->
From app/components/parlamentarios-filtro.tsx:
```typescript
// "use client"; useCallback/useMemo/useState
// FacetChip({ label, count, engaged, onToggle }) — chip toggle:
//   disabled cuando count===0; aria-pressed/aria-disabled;
//   engaged → "border-accent-product bg-accent-product-soft text-accent-product";
//   min-h-11; focus-visible:outline-2 outline-offset-2 outline-accent-product
// PARTIDO_SIN_DATO = "sin_dato" (bucket "Sin dato" al final, ordena por freq desc→alfa)
// leyendaCounts(n) = `Conteos sobre estos ${n} … cargados, no sobre todo el Congreso.`
// EMPTY_HEADING/EMPTY_BODY para el estado sin resultados tras filtro
```

From app/lib/boletin-detector.ts:
```typescript
export function detectarBoletin(q: string): { base: string; sufijo: string|null } | null;
// "14309-04" / "14309" / "14.309-04" → {base,sufijo}; texto libre → null
```

From app/lib/anti-insinuacion-guard.test.ts:
```typescript
// const SUPERFICIES_LOBBY: string[] = [...];  // patrón de array por bloque
// El scan itera [...SUPERFICIES_VOTO, ..., ...SUPERFICIES_LOBBY]
// NEGACIONES_LOCKED: string[] — negaciones restadas ANTES del match (Pitfall 1, lección BLOCKER 91)
// detectarInsinuaciones(raw): strip comentarios → normaliza \s → resta NEGACIONES → matchea TERMINOS_PROHIBIDOS
```

Copy LOCKED del island (§UI-SPEC §Component Contract 2 / §Copywriting):
- leyenda counts: "Conteos sobre estas {N} citaciones cargadas de esta semana, no sobre toda la agenda."
- empty tras filtro: heading "Sin citaciones para este filtro" + body "Ajusta o quita filtros para ver más."
- fieldsets: Cámara / Comisión / Rango de fechas / Boletín; bucket "Sin dato · {n}" para comisión ausente.
</interfaces>

Decisiones LOCKED (§UI-SPEC §2):
- El SERVER pasa las filas planas + el dayKey ya calculado (Chile); el island filtra y reagrupa por día (no duplica tzdb en cliente).
- Rango de fechas = `<input type="date">` desde/hasta, acotados a min/max del slice (NO slider).
- Boletín = input + `detectarBoletin`; filtra citaciones cuyo `citacion_punto.boletin` matchea; el island NO navega (el short-circuit SSR de la barra global se mantiene aparte).
- "Esta semana" = reset de todos los filtros island.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Island agenda-filtros.tsx (facetas cámara/comisión/rango/boletín + counts honestos + reagrupación por día)</name>
  <files>app/components/agenda-filtros.tsx, app/components/agenda-filtros.test.tsx</files>
  <behavior>
    - Dado un slice de filas planas con { id, camara, comision, fecha, dayKey, dayLabel, boletines[], estado, materia, ... }, el island reagrupa por dayKey y renderiza los días con citaciones tras el filtro (los días vacíos tras filtro se ocultan).
    - Faceta cámara: chips "Cámara · {n}" / "Senado · {n}" (counts de estas N); toggle filtra en memoria.
    - Faceta comisión: chips por comisión presente, orden freq desc→alfabético, "Sin dato · {n}" al final; count=0 → disabled.
    - Rango de fechas: inputs date desde/hasta acotados a min/max del slice; vacío = sin filtro; filtra por fecha del día.
    - Boletín: input; detectarBoletin(input) → filtra filas cuyo boletines[] contiene el base (con/sin sufijo); texto libre no-boletín no filtra a cero silenciosamente (o filtra por match textual de boletín declarado — decisión: usar detectarBoletin, si null no aplica filtro de boletín).
    - Counts honestos: cada faceta cuenta sobre el SLICE COMPLETO, no sobre el resultado ya filtrado (espejo parlamentarios-filtro).
    - "Esta semana": resetea cámara/comisión/rango/boletín.
    - Empty tras filtro: heading + body LOCKED (≠ empty de datos).
  </behavior>
  <action>
Crear `agenda-filtros.tsx` (`"use client"`) espejo estructural de
`parlamentarios-filtro.tsx`. CONTRATO FichaRail DURO: JAMÁS importa `@/lib/supabase`
ni usa `.rpc`/`.from` — recibe el slice serializado por props y filtra EN MEMORIA
(React state). Reutilizar `FacetChip` (mismo idiom: `min-h-11`, `aria-pressed`,
`disabled` cuando count=0, `engaged` en petróleo via utilidad registrada — NUNCA
envolver `hsl()` a mano, lección UI-REVIEW 89). Estructura vertical mobile-first
(§UI-SPEC §2 anatomía): leyenda counts → fieldset Cámara → fieldset Comisión →
fieldset Rango de fechas (inputs date `min`/`max` = extremos del slice) → fieldset
Boletín (input + `detectarBoletin`) → "Esta semana" (reset).

El island reagrupa el slice filtrado por `dayKey` (que el server ya calculó en tz
Chile — el island NO recalcula tz) y renderiza cada día con sus citaciones. Días sin
citaciones tras el filtro se ocultan. Counts SIEMPRE sobre el slice completo
(`useMemo` sobre `slice`, no sobre `listaVisible`). Copy LOCKED verbatim (leyenda
counts, empty tras filtro). Accesibilidad §7: `<fieldset><legend>`, `<label>` en
inputs date, `aria-pressed` en chips, focus-visible.

Como el island renderiza las citaciones filtradas, debe reproducir el layout por día
(acordeón/lista). Decisión de implementación: el island renderiza una lista por día
(no necesita `CarrilAccordion` server; puede usar `<details>` o lista simple con
día-header h3 + conteo mono) — mantener sobrio, sin color de estado en la cancelación
(§Color). PROHIBIDO copy de afinidad/relación/causalidad (anti-insinuación).

Tests RTL: filtro cámara reduce el set; comisión con "Sin dato"; rango date acota;
boletín "14309" filtra por base; counts se mantienen sobre el slice completo; "Esta
semana" resetea; empty tras filtro muestra copy LOCKED.
  </action>
  <verify>
    <automated>cd app && pnpm test -- agenda-filtros</automated>
  </verify>
  <done>Island filtra por 4 facetas en memoria; counts honestos sobre slice completo; reagrupa por dayKey Chile; reset "Esta semana"; empty LOCKED; CERO import de supabase (grep); tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Serializar el slice plano + dayKey en page.tsx, montar el island, extender el linter</name>
  <files>app/app/agenda/page.tsx, app/lib/anti-insinuacion-guard.test.ts</files>
  <action>
En `CitacionesSection` de page.tsx: además de (o en vez de) la agrupación server que
ya rinde el SSR de los acordeones, computar un SLICE PLANO serializable para el island
— cada fila con `{ id, camara, comision, fecha (ISO), dayKey (Chile, del Task 94-01),
dayLabel, boletines: string[] (de citacion_punto), estado, materia, horario, sala,
provenance }`. El `dayKey` en tz Chile ya se calcula en 94-01 — REUSARLO (no
recalcular con otra tz). Montar `<AgendaFiltros slice={slice} />` como el vehículo de
filtrado de la vista semanal. Decisión de convivencia (§UI-SPEC §2): el island envuelve
la presentación por día de la semana cargada; el SSR sigue produciendo el primer render
(hidratación progresiva). Mantener el buscador FTS global (`buscarCitaciones`) intacto —
coexiste, no se reemplaza.

Extender el linter `anti-insinuacion-guard.test.ts`: añadir `const SUPERFICIES_AGENDA:
string[] = ["components/agenda-filtros.tsx", "components/agenda-cobertura.tsx",
"app/app/agenda/page.tsx"]` y sumarlo al spread del scan
(`[...SUPERFICIES_LOBBY, ...SUPERFICIES_AGENDA]`). ANTES de añadirlo (lección BLOCKER
91): revisar si el copy nuevo NIEGA algún término prohibido; la leyenda de estado
("…no confirma que la sesión se realizará") y la intro del banner ("…no es un
calendario completo…") usan "confirma"/"completo" que NO están en TERMINOS_PROHIBIDOS
(no requieren negación) — verificar el diff de TERMINOS_PROHIBIDOS; si alguna leyenda
LOCKED contuviera un término prohibido en contexto de negación, registrarla en
NEGACIONES_LOCKED verbatim ANTES de sumar la superficie. Añadir un caso al mutation
self-check si se registra una negación nueva.
  </action>
  <verify>
    <automated>cd app && pnpm test -- anti-insinuacion agenda && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5; echo "tsc-done"</automated>
  </verify>
  <done>Slice plano serializado con dayKey Chile; island montado en la vista semanal; linter escanea SUPERFICIES_AGENDA sin falso-positivo (verde); FTS global intacto; tsc 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → island | slice serializado cruza al cliente; debe ser no-PII (citacion/citacion_punto son públicas 0010) |
| input de boletín (island) | texto del usuario en el input de filtro — se procesa client-side con detectarBoletin (regex acotada), no navega ni consulta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03 | Information Disclosure | slice serializado al cliente | mitigate | solo campos no-PII (comision/fecha/materia/boletin/estado); invitados NO se serializan al island de filtros (gestores de interés = terceros); contrato FichaRail escaneado por lockdown-guard (`.from`/`.rpc` en app/) |
| T-94-04 | Tampering | copy del island/banner | mitigate | linter anti-insinuación extendido a SUPERFICIES_AGENDA con mutation self-check; NEGACIONES_LOCKED registra cualquier negación ANTES del scan |
| T-94-SC | Tampering | npm installs | mitigate | CERO paquete nuevo (reutiliza React/Intl/shadcn) |
</threat_model>

<verification>
- `grep -n "@/lib/supabase\|\.rpc\|\.from(" app/components/agenda-filtros.tsx` → CERO (contrato FichaRail).
- Filtros operan: cámara reduce, comisión con Sin dato, rango date acota, boletín filtra por base.
- Counts "de estas N" sobre slice completo.
- Linter verde con SUPERFICIES_AGENDA incluido; mutation self-check muerde.
- `cd app && pnpm test` verde; tsc 0.
</verification>

<success_criteria>
- CIT-04: filtros periodista (cámara, comisión, rango de fechas, boletín mencionado, "esta semana") operando en memoria con counts honestos.
- Contrato FichaRail intacto (cero Supabase en el island).
- Linter cubre la superficie nueva sin falso-positivo.
</success_criteria>

<output>
Create `.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-02-SUMMARY.md` when done
</output>
