---
phase: 94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/estado-actual-block.tsx
  - app/components/estado-actual-block.test.tsx
autonomous: true
requirements: [CIT-04, CIT-05]

must_haves:
  truths:
    - "La ficha del proyecto muestra las citaciones PASADAS (fecha < hoy-Chile) marcadas '(sesión pasada)' sobrias, además de la vigente/próxima"
    - "La ficha declara 'En tabla de sala de la {Cámara|Senado} del {fecha}' con link a /agenda?semana={Www} cuando el boletín aparece en sesion_tabla_item"
    - "Sujeto 18193-06 (citación 2026-07-21 pasada) renderiza la línea de citación pasada"
    - "Sujeto 13665-07 (en tabla de sala Senado W28/W29) renderiza la línea de tabla de sala"
    - "Las firmas existentes de derivarEstadoActual/EstadoActual siguen compilando (campos nuevos opcionales); los tests 88/89 no regresionan"
    - "Ninguna línea fabrica vigencia; ausencia de pasada/sala → línea omitida (omit-when-not-derivable)"
  artifacts:
    - path: "app/components/estado-actual-block.tsx"
      provides: "citacionesPasadas + enTablaSala derivados + lectura sesion_tabla_item + render de ambas líneas"
      contains: "sesion_tabla_item"
  key_links:
    - from: "app/components/estado-actual-block.tsx"
      to: "sesion_tabla_item"
      via: "lectura server-side batched en el Promise.all existente"
      pattern: "sesion_tabla_item"
    - from: "EstadoActualView"
      to: "/agenda?semana="
      via: "link petróleo a la semana de la tabla de sala"
      pattern: "agenda\\?semana="
---

<objective>
Cerrar los 2 gaps de WIRING de la ficha confirmados con DOM en 93 (reporte operador
2026-07-22): la ficha oculta citaciones PASADAS (gap #1) y no lee `sesion_tabla_item`
(gap #2). CIT-04 (cross-link ficha↔agenda) + CIT-05 (honestidad temporal).

Purpose: para la prensa que revisa un proyecto histórico, la ficha debe declarar que
fue citado (aunque la sesión ya pasó) y que está/estuvo en la tabla de sala — la data
YA está en DB (§6 de la auditoría: es wiring, no cobertura). Se arregla SIN re-ingesta.

Output: estado-actual-block.tsx con citacionesPasadas + enTablaSala derivados y
renderizados, protegiendo los tests 88/89.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-CONTEXT.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-UI-SPEC.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md
@app/components/estado-actual-block.tsx
@app/lib/agenda-types.ts

<interfaces>
<!-- Estado derivado ACTUAL — extender con campos OPCIONALES (backward-compatible): -->
From app/components/estado-actual-block.tsx:
```typescript
export interface EstadoActual {
  etapaLinea?: string;
  ultimoHito?: { descripcion: string; fecha: Date };
  urgenciaVigente?: { tipo: string; desde: Date };
  urgenciaEstado?: {...};
  urgenciaFuente?: {...};
  citacionVigente?: { comision: string; fecha: Date };   // se MANTIENE (fecha >= hoy)
}
export interface CitacionCruda { comision: string|null; fecha: string|null; }
// DIA_CALENDARIO_CHILE (:92-98) — reutilizar para "hoy-Chile"
export function citacionVigente(citaciones: CitacionCruda[], hoy?: Date): {...}|null;
export function derivarEstadoActual(
  proyecto, eventos, citaciones: CitacionCruda[] = [], hoy: Date = new Date()
): EstadoActual;  // ← firma con 2 args opcionales DEBE seguir compilando
// EstadoActualBlock({boletin}) — Promise.all lee proyecto + tramitacion_evento +
//   citacion_punto×citacion (:293-315). NO lee sesion_tabla_item hoy (gap #2).
```

From app/lib/agenda-types.ts:
```typescript
export interface SesionTablaItemRow { sesion_id: string; posicion: number;
  parte_sesion: string; materia: string|null; boletin: string|null; ... }
export interface SesionSalaRow { id: string; camara: "camara"|"senado";
  fecha: string|null; semana_iso?: string; ... }  // embed sesion_sala(camara,fecha,semana_iso)
```

Copy LOCKED (§UI-SPEC §Copywriting / §Component Contract 5):
- citación pasada: "Citado el {fecha} en {comisión}" + marca "(sesión pasada)" en text-muted-foreground
- citación vigente/futura (existente, se mantiene): "Citado en {comisión} el {fecha}."
- tabla de sala: "En tabla de sala de la {Cámara|Senado} del {fecha}" → link a /agenda?semana={Www}
- varias tablas: "En tabla de sala {N} veces" (conteo honesto; enlaza a la más reciente o lista cada semana)
</interfaces>

Sujetos de validación (93-WIRING-EVIDENCIA §0, deterministas):
- 18193-06 → citación pasada 2026-07-21 (Senado "de Economía"); 0 en sesion_tabla_item.
- 13665-07 → en sesion_tabla_item Senado W28 (2026-07-07) y W29 (2026-07-14); 0 citaciones.
- 11929-13 → control positivo citación HOY (vigente sigue funcionando).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivar citacionesPasadas + enTablaSala (puros) sin romper firmas 88/89</name>
  <files>app/components/estado-actual-block.tsx, app/components/estado-actual-block.test.tsx</files>
  <behavior>
    - citacionesPasadas: de las citaciones crudas, las con comisión válida y fecha < hoy-Chile (día calendario), orden DESC (más reciente primero), acotadas (máx ~5). Si 0 → campo ausente (omit).
    - citacionVigente se MANTIENE idéntico (fecha >= hoy-Chile, la más próxima).
    - enTablaSala: de las filas de sesion_tabla_item del boletín (con embed sesion_sala), lista { camara, fecha, semanaIso } orden DESC; si 0 → ausente.
    - derivarEstadoActual con 2 args (firma vieja) sigue compilando y devuelve lo mismo que antes para citacionVigente/etapaLinea/urgencia.
    - Un sujeto tipo 18193-06 (1 citación 2026-07-21, hoy 2026-07-22) → citacionesPasadas tiene 1 entrada, citacionVigente null.
    - Un sujeto tipo 13665-07 (2 filas de sala W28/W29, 0 citaciones) → enTablaSala tiene 2 entradas.
  </behavior>
  <action>
Extender `EstadoActual` con dos campos OPCIONALES (backward-compatible):
`citacionesPasadas?: { comision: string; fecha: Date }[]` y `enTablaSala?: { camara:
"camara"|"senado"; fecha: Date; semanaIso: string }[]`. Añadir una función pura
`citacionesPasadas(citaciones: CitacionCruda[], hoy?: Date)` espejo de `citacionVigente`
pero con predicado `fecha < hoy-Chile`, orden DESC, `.slice(0,5)`. Extender la firma de
`derivarEstadoActual` con un parámetro OPCIONAL adicional para las filas de sala
(p.ej. `tablaSala: TablaSalaCruda[] = []`) — DEBE mantener compatibilidad: la firma de
2 args (`derivarEstadoActual(proyecto, eventos)`) y de 3 args
(`+citaciones`) siguen compilando (los tests 88/89 la llaman así). Derivar
`enTablaSala` de las filas de sala (map camara/fecha/semanaIso, filtrar fecha válida,
orden DESC). Todo omit-when-not-derivable — CERO fabricación, NUNCA vigencia inventada
para una pasada.

Tests: añadir casos para citacionesPasadas (18193-06-like), enTablaSala (13665-07-like),
y REVERIFICAR que los casos existentes de citacionVigente/etapa/urgencia siguen verdes
(no regresión 88/89). Usar `hoy` fijo (2026-07-22) para determinismo tz.
  </action>
  <verify>
    <automated>cd app && pnpm test -- estado-actual-block</automated>
  </verify>
  <done>citacionesPasadas + enTablaSala derivados puros con omit-when-empty; firma vieja de derivarEstadoActual compila; tests nuevos + existentes verdes (cero regresión 88/89).</done>
</task>

<task type="auto">
  <name>Task 2: Leer sesion_tabla_item server-side + renderizar ambas líneas (pasadas + tabla de sala)</name>
  <files>app/components/estado-actual-block.tsx</files>
  <action>
En `EstadoActualBlock` (Server Component): añadir al `Promise.all` existente
(`:293-315`) una lectura de `sesion_tabla_item` filtrada por `boletin`, embebiendo
`sesion_sala(camara, fecha, semana_iso)` (todas no-PII, public-read 0010). Aplanar el
embed a filas `{ camara, fecha, semanaIso }`. Un error real de DB/red se LANZA (#34,
espejo de las otras 3 lecturas) — NUNCA se degrada a línea vacía. Pasar las filas de
sala a `derivarEstadoActual`.

En `EstadoActualView`: renderizar dos bloques nuevos DENTRO del mismo bloque
estado-actual (mismo dominio tramitación — NO compone con lobby/voto):
1. `citacionesPasadas` (si presente): líneas "Citado el {fecha} en {comisión}" con marca
   "(sesión pasada)" en `text-muted-foreground` (sobria, NUNCA alarma/destructive,
   §Color). Fechas en `font-mono`. Máx ~5 visibles (o conteo "ver N más" si se decide);
   omitir el sub-bloque si vacío.
2. `enTablaSala` (si presente): línea "En tabla de sala de la {Cámara|Senado} del
   {fecha}" con link PETRÓLEO (`text-accent-product`, utilidad registrada — regla
   accent #2 del §Color) a `/agenda?semana={semanaIso}`. Si aparece en varias → conteo
   honesto "En tabla de sala {N} veces" enlazando a cada semana (o a la más reciente +
   lista). Omitir si vacío. Fecha en `font-mono`.

Actualizar el guard `if (!etapaLinea && !ultimoHito && !urgenciaEstado &&
!citacionVigente) return null;` para incluir los campos nuevos (el bloque se renderiza
si hay CUALQUIER línea derivable, incl. pasadas o tabla de sala).

CROSS-LINK comisión→membresía (CIT-04): §UI-SPEC lo declara barato-o-diferido. Como una
página propia de comisión NO existe en esta fase (Deferred Ideas: "página propia por
comisión — v9.x/v10"), DIFERIR el cross-link comisión→/comision/{slug} declarándolo en
el SUMMARY (no bloquea; sin enlace inventado). Las comisiones se muestran como texto,
sin link de membresía.
  </action>
  <verify>
    <automated>cd app && pnpm test -- estado-actual-block && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "estado-actual" -SimpleMatch; echo "tsc-done"</automated>
  </verify>
  <done>EstadoActualBlock lee sesion_tabla_item batched; EstadoActualView renderiza citaciones pasadas (marca sobria) + línea de tabla de sala (link petróleo a /agenda?semana=); guard actualizado; cross-link comisión→membresía DIFERIDO declarado; tsc 0; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → RSC | lectura sesion_tabla_item + embed sesion_sala; tablas no-PII public-read 0010 |
| boletin param → query | `boletin` ya validado aguas arriba (ficha /proyecto/[boletin]); `.eq()` parametrizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05 | Repudiation | citaciones pasadas ocultas | mitigate | derivar y mostrar las pasadas → la ficha ya no niega que un proyecto histórico fue citado (integridad del registro para prensa) |
| T-94-06 | Repudiation | fabricación de vigencia | mitigate | omit-when-not-derivable estricto; marca "(sesión pasada)" neutra; NUNCA "Vigente" a partir de ausencia de cancelación |
| T-94-07 | Information Disclosure | lectura sesion_tabla_item | accept | tabla no-PII (materia/boletin/posicion); ya servida públicamente por /agenda |
| T-94-SC | Tampering | npm installs | mitigate | CERO paquete nuevo |
</threat_model>

<verification>
- `grep -n "sesion_tabla_item" app/components/estado-actual-block.tsx` → presente (query + derivación).
- Sujeto 18193-06: la línea "Citado el 21-07-2026 en de Economía (sesión pasada)" deriva (test).
- Sujeto 13665-07: la línea "En tabla de sala del Senado …" con link /agenda?semana=2026-W28|W29 deriva (test).
- Firma vieja de derivarEstadoActual (2/3 args) compila → tests 88/89 verdes.
- `cd app && pnpm test` verde; tsc 0.
</verification>

<success_criteria>
- CIT-04/CIT-05: gap #1 (citaciones pasadas visibles) + gap #2 (tabla de sala en la ficha) FIXED, verificables por los sujetos 18193-06 / 13665-07.
- Cero regresión de los tests compartidos 88/89 de estado-actual-block.
- Cross-link comisión→membresía DIFERIDO declarado (sin enlace inventado).
</success_criteria>

<output>
Create `.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-03-SUMMARY.md` when done
</output>
