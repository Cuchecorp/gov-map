---
phase: 94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/agenda/page.tsx
  - app/components/citacion-card.tsx
  - app/components/agenda-cobertura.tsx
autonomous: true
requirements: [CIT-03, CIT-05]

must_haves:
  truths:
    - "/agenda agrupa las citaciones por DÍA-CALENDARIO de Chile (America/Santiago), nunca por UTC"
    - "Una citación de las 21:00 CL almacenada a medianoche UTC cae en el día-Chile correcto (no en el siguiente)"
    - "El banner de cobertura DECLARADA aparece bajo el <h1>, con celdas derivadas (Cámara comisiones N/S/rango) y estructurales (forward-only)"
    - "El estado de cancelación de la citación (Suspendida/Sin efecto) es visible cuando existe, sobrio, sin color de alarma"
    - "La leyenda 'estado ausente ≠ vigente confirmado' se muestra 1× por página"
  artifacts:
    - path: "app/components/agenda-cobertura.tsx"
      provides: "Banner de cobertura declarada (Server Component; derivadas dinámicas + estructurales fijas)"
      min_lines: 40
    - path: "app/app/agenda/page.tsx"
      provides: "Agrupación por día-Chile (dayKey + diaFmt en tz America/Santiago) + banner montado"
      contains: "America/Santiago"
    - path: "app/components/citacion-card.tsx"
      provides: "Prop estado?: string|null + marca sobria"
      contains: "estado"
  key_links:
    - from: "app/app/agenda/page.tsx"
      to: "app/components/agenda-cobertura.tsx"
      via: "import + montaje bajo <h1>"
      pattern: "AgendaCobertura"
    - from: "app/app/agenda/page.tsx"
      to: "DIA_CALENDARIO_CHILE"
      via: "dayKey por día-calendario-Chile"
      pattern: "America/Santiago"
---

<objective>
Corregir el BUG núcleo de la fase (agrupación por día en UTC → debe ser
America/Santiago), montar el banner de cobertura DECLARADA (insumo §7/§8 de 93), y
hacer visible el estado de cancelación honesto en la card. CIT-03 (por día tz Chile
+ distinción sala/comisiones ya heredada) + CIT-05 (cancelación honesta + cobertura
declarada).

Purpose: /agenda debe agrupar por el día real de Chile (una citación de las 21:00 CL
NO puede caer al día siguiente) y declarar su cobertura parcial sin fingir un
calendario completo. Es el gate cognitivo de la prensa.

Output: page.tsx con dayKey/diaFmt en tz Chile; agenda-cobertura.tsx nuevo;
citacion-card.tsx con marca de estado sobria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-CONTEXT.md
@.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-UI-SPEC.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md
@app/app/agenda/page.tsx
@app/components/citacion-card.tsx

<interfaces>
<!-- Idiom tz Chile YA en el repo — reutilizar VERBATIM (NO re-inventar): -->
From app/components/estado-actual-block.tsx:92-98:
```typescript
const DIA_CALENDARIO_CHILE = new Intl.DateTimeFormat("en-CA", {
  timeZone: "America/Santiago",
  year: "numeric", month: "2-digit", day: "2-digit",
}); // emite "YYYY-MM-DD" DST-safe
```

From app/lib/agenda-types.ts:
```typescript
export interface CitacionRow {
  id: string; camara: "camara"|"senado"; comision: string;
  fecha: string|null; horario: string|null; sala: string|null;
  materia: string|null; estado: string|null; semana_iso: string;
  origen: string; fecha_captura: string; enlace: string;
  citacion_invitado?: ...; citacion_punto?: ...;
}
```

From app/components/citacion-card.tsx (prop actual, extender con estado):
```typescript
export interface CitacionCardProps {
  comision: string; fecha: Date|null; horario: string|null;
  sala: string|null; materia: string|null; camara: "camara"|"senado";
  invitados: CitacionInvitado[]; boletin?: string|null;
  provenance: ProvenanceBadgeProps;
}
```

From app/lib/format.ts: `capitalizarPrimera(s: string): string`
From app/lib/supabase.ts: `createServerSupabase()` (service_role, RSC-only)
</interfaces>

Cifras del banner (93 §7, DERIVAR las de Cámara, DECLARAR las estructurales):
- comisiones×Cámara: N y S y rango DERIVADOS de DB (count, count(distinct semana_iso), min/max fecha). Post-backfill hoy N≈164, S=6, rango W20(mayo)→W28. NO hardcodear.
- comisiones×Senado: forward-only, sin histórico (estructural — texto fijo LOCKED §UI-SPEC).
- sala×Cámara: solo sesión vigente, sin histórico estructurado (estructural).
- sala×Senado: forward-only, sin histórico (estructural).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Banner de cobertura declarada (agenda-cobertura.tsx) + prop estado en CitacionCard</name>
  <files>app/components/agenda-cobertura.tsx, app/components/agenda-cobertura.test.tsx, app/components/citacion-card.tsx</files>
  <behavior>
    - agenda-cobertura: dada la métrica derivada { camaraN, camaraSemanas, camaraMin, camaraMax } renderiza la celda Cámara comisiones con N/S/rango en font-mono (copy LOCKED §Copywriting "Comisiones de la Cámara: {N} citaciones ingeridas en {S} semanas ({rango min→max}); …").
    - Las tres celdas estructurales (comisiones Senado / sala Cámara / sala Senado) usan el texto fijo LOCKED verbatim, sin derivar.
    - Intro LOCKED siempre visible ("Esta agenda muestra lo que se ha ingerido … no es un calendario completo del Congreso.").
    - Leyenda de estado LOCKED presente ("Cuando una citación fue suspendida … no confirma que la sesión se realizará.").
    - CitacionCard con estado="Suspendida" renderiza la marca sobria; con estado=null NO renderiza marca alguna y NUNCA añade "Vigente"/"Confirmada".
  </behavior>
  <action>
Crear `agenda-cobertura.tsx` como Server Component PURO de presentación (recibe la
métrica derivada por props — NO consulta Supabase). Banda `rounded-lg border
border-border bg-muted/40 px-6 py-4`. Heading sobrio "Cobertura de la agenda" (h2).
Intro LOCKED (§UI-SPEC Copywriting, verbatim). `<ul>` con 4 `<li>` (una por celda):
la celda Cámara comisiones interpola {N}/{S}/{rango} en `font-mono` desde props; las
otras tres son texto estructural fijo LOCKED. Al final, la leyenda de estado LOCKED
como `<p>`. Tono neutro — PROHIBIDO `--destructive`, ámbar o rojo (§Color): la
parcialidad es un hecho declarado, no alarma. Rangos y N SIEMPRE font-mono.

Extender `CitacionCardProps` con `estado?: string | null` (opcional,
backward-compatible). Cuando `estado` presente, render marca junto a la fecha/horario
mono como `<span className="text-sm text-muted-foreground">· {estado}</span>`
(idiom `page.tsx:248`). PROHIBIDO badge de alarma / `--destructive` / rojo. Cuando
`estado` ausente: NO render marca, NUNCA "Vigente"/"Confirmada" (§Honestidad LOCKED:
ausencia de estado ≠ vigencia). Tests RTL cubren: banner con métrica → celda mono
correcta; celda estructural verbatim; card estado="Suspendida" → marca visible;
card estado=null → cero marca + cero "Vigente".

CERO fenced code en el copy — todo el texto LOCKED va verbatim del §UI-SPEC.
  </action>
  <verify>
    <automated>cd app && pnpm test -- agenda-cobertura citacion-card</automated>
  </verify>
  <done>agenda-cobertura.tsx renderiza intro+4 celdas+leyenda LOCKED (Cámara derivada, resto estructural); CitacionCard muestra estado sobrio cuando existe y nada cuando ausente; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: dayKey + diaFmt en tz America/Santiago + montaje del banner + estado a la card</name>
  <files>app/app/agenda/page.tsx</files>
  <action>
FIX NÚCLEO (§UI-SPEC §Component Contract 1, BUG A CORREGIR): en `CitacionesSection`
la agrupación por día hoy usa `c.fecha.slice(0,10)` (día UTC almacenado) y
`diaFmt`/rotulación con `timeZone:"UTC"` (`page.tsx:320,326-331,344`). Ambos violan
la regla LOCKED tz Chile. Cambios exactos:
1. Definir `DIA_CALENDARIO_CHILE` (idiom `estado-actual-block.tsx:92-98`,
   `Intl.DateTimeFormat("en-CA",{timeZone:"America/Santiago",…})`) — o importarlo si
   se exporta; si no, replicar la constante local.
2. `dayKey` = `DIA_CALENDARIO_CHILE.format(new Date(c.fecha))` para las citaciones con
   fecha (día-calendario-Chile), `"sin-fecha"` para las sin fecha (idiom se mantiene).
3. `diaFmt` de rotulación pasa a `timeZone:"America/Santiago"` (weekday/day/month,
   es-CL, `capitalizarPrimera`). Formatear desde `new Date(c.fecha)` de la primera fila
   del grupo (NO desde `${dayKey}T00:00:00Z`, que re-introduce UTC). Guardar la Date
   representativa del día junto al Map, o re-derivar del primer item del grupo.
4. Ídem en `ResultadosBusqueda` (`page.tsx:217-222`): su `diaFmt` también usa UTC —
   pasar a `timeZone:"America/Santiago"` por coherencia (no agrupa por día pero rotula
   fechas).
5. Pasar `estado={c.estado}` a cada `<CitacionCard>` (nueva prop del Task 1).
6. Montar `<AgendaCobertura>` bajo el `<h1>` y ANTES del `<WeekNav>` (solo en la vista
   semanal, no en `buscando`). Derivar la métrica Cámara server-side: un `Promise.all`
   / query dedicada `count(*)`, `count(distinct semana_iso)`, `min/max(fecha)` sobre
   `citacion where camara='camara'` (sin cap 1k — usar `count` head o RPC de conteo;
   NUNCA traer todas las filas para contar). Las tres celdas estructurales son texto
   fijo (no requieren query). Si la métrica falla → lanzar (frontera de error honesta
   #34), NUNCA banner fabricado.

NO tocar la distinción sala/comisiones (§UI-SPEC: sala vs comisiones es POR SECCIÓN —
#citaciones vs #tabla-sala — ya heredado). NO alterar `SalaTableServer`.
  </action>
  <verify>
    <automated>cd app && pnpm test -- agenda && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "agenda" -SimpleMatch; echo "tsc-done"</automated>
  </verify>
  <done>Sin ninguna aparición de `timeZone: "UTC"` en la agrupación/rotulación de día de page.tsx (verificable por grep); dayKey deriva del día-calendario-Chile; banner montado bajo h1; estado fluye a cada card; tsc 0 errores; suite agenda verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams → RSC | `?semana` ya validado por `parseISOWeek` (existente); esta fase no añade input nuevo |
| Supabase → RSC | lecturas `citacion`/`sesion_sala` no-PII, public-read 0010; service_role RSC-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Information Disclosure | banner de cobertura (métrica de DB) | accept | solo conteos agregados no-PII (N/semanas/rango de fecha); cero fila individual expuesta más allá de lo ya público en /agenda |
| T-94-02 | Repudiation | agrupación por día tz Chile | mitigate | dayKey por día-calendario-Chile evita atribuir una citación al día equivocado (integridad temporal del dato mostrado) |
| T-94-SC | Tampering | npm/pip/cargo installs | mitigate | CERO paquete nuevo (reutiliza Intl/date idioms + shadcn instalado); slopcheck n/a |
</threat_model>

<verification>
- Grep de sanidad: `timeZone: "UTC"` NO aparece en las funciones de agrupación/rotulación de día de page.tsx (comentarios excluidos con `grep -v '^\s*//'` si aplica).
- Banner cobertura visible bajo h1 con celda Cámara derivada (mono) + 3 estructurales + leyenda estado.
- CitacionCard: estado presente → marca sobria; ausente → nada.
- `cd app && pnpm test` verde (incluye agenda-cobertura + citacion-card + agenda existentes 88/89 NO regresionan).
</verification>

<success_criteria>
- CIT-03: /agenda agrupa por día-calendario-Chile (America/Santiago), nunca UTC.
- CIT-05: estado de cancelación visible honesto + banner de cobertura declarada + leyenda "estado ausente ≠ vigente".
- Suite app verde; tsc 0.
</success_criteria>

<output>
Create `.planning/phases/94-agenda-p2e-agenda-por-d-a-filtros-periodista-gate-browsero/94-01-SUMMARY.md` when done
</output>
