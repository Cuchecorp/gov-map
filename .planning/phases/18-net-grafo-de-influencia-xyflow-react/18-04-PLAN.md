---
phase: 18-net-grafo-de-influencia-xyflow-react
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified: []
autonomous: false
requirements: [NET-01, NET-02]
must_haves:
  truths:
    - "el build de producción no infla el server bundle de las 7 rutas existentes con xyflow"
    - "el redeploy Linux no expone NET: /red da notFound/honest-state porque NET_PUBLIC_ENABLED no está en true"
    - "la suite completa (vitest RTL + pgTAP) está verde y tsc limpio antes del cierre"
    - "ninguna arista es inferida por LLM y el copy no contiene score/path-as-accusation/causal (negative-match grep en cero)"
  artifacts:
    - path: ".planning/phases/18-net-grafo-de-influencia-xyflow-react/18-04-SUMMARY.md"
      provides: "registro de verificación de fase + estado del gate cerrado"
  key_links:
    - from: "deploy"
      to: "NET_PUBLIC_ENABLED"
      via: "flag ausente => /red 404"
      pattern: "NET_PUBLIC_ENABLED"
---

<objective>
Cerrar Phase 18: verificar el gate cerrado de extremo a extremo (build-size check de aislamiento de xyflow, negative-match anti-insinuación de fase, suite completa verde + tsc limpio) y redeployar a Cloudflare desde Linux/Docker SIN exponer NET (la ruta /red 404 porque `NET_PUBLIC_ENABLED` no es `"true"`).

Purpose: NET nace gated-OFF — este plan prueba que el deploy NO expone nada nuevo públicamente y que el doble candado (RLS de Plan 01 + flag de Plan 02) sostiene. Encender NET es deuda de operador F17 (depende de 17-LEGAL-DOSSIER signoff approved), fuera del alcance de esta fase.
Output: verificación de fase registrada + redeploy Linux gated-OFF; 18-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-CONTEXT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-01-PLAN.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-02-PLAN.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificación de fase — suite completa, tsc, aislamiento de bundle, negative-match anti-insinuación</name>
  <files>app/ (sin modificar — solo verificación)</files>
  <read_first>
    - .planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md §Validation Architecture (sampling: per-wave = `npm run test && npm run typecheck` + pgTAP; phase gate = suite verde + tsc + Linux redeploy sin NET) + Pitfall 3 (build-size check: chunk xyflow solo en /red)
    - app/app/red/page.tsx, app/components/red/red-graph.tsx (objetivos del negative-match)
  </read_first>
  <action>
    Correr la suite y verificaciones de cierre (sin modificar código): (1) `cd app && npm run test` (vitest RTL: net-gate, red/page, red-graph) en verde; (2) `cd app && npm run typecheck` limpio; (3) build de producción `cd app && npm run build` y verificar que el chunk de `@xyflow/react` aparece SOLO en el route chunk de `/red` y NO en los bundles de `/`, `/proyecto/[boletin]`, `/parlamentario/[id]`, `/buscar`, `/agenda`, `/parlamentarios`, `/contraparte/[id]` (Pitfall 3 — inspección del output de build / análisis de chunks); (4) negative-match anti-insinuación de FASE: grep insensible a mayúsculas sobre `app/components/red/`, `app/app/red/`, y el cuerpo no-comentario de `supabase/migrations/0030_net.sql` por `puntaje|score|ranking|afinidad|cercan|alinead|por eso|a cambio de|conexión sospechosa|co_votacion|inferid|llm|gpt|gemini` → debe dar CERO (ninguna arista inferida por LLM, cero co_votacion, cero score/causal); (5) confirmar que `NET_PUBLIC_ENABLED` NO está seteado en el entorno de build (ausente => OFF).
    Registrar resultados; si algún check falla, detener y reportar (no parchar a ciegas).
  </action>
  <verify>
    <automated>cd app && npm run test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` (vitest) y `npm run typecheck` en verde.
    - `npm run build` exitoso; el chunk de @xyflow/react NO aparece en ninguno de los 7 server-route bundles existentes (solo en /red).
    - Negative-match grep de fase en CERO: sin `co_votacion`, sin `inferid/llm/gpt/gemini`, sin `score/ranking/afinidad/cercan/causal` en archivos NET.
    - `NET_PUBLIC_ENABLED` ausente en el entorno (gate OFF).
  </acceptance_criteria>
  <done>Suite verde, tsc limpio, xyflow aislado en /red, negative-match anti-insinuación en cero, gate OFF confirmado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [OPERADOR] Redeploy Linux/Docker gated-OFF + verificar /red cerrado en producción</name>
  <what-built>El frontend con la ruta /red gateada y la isla xyflow, listo para redeploy. El build/deploy a Cloudflare requiere Linux/Docker (obsbuild) — en Windows OpenNext da 500s (memoria de proyecto). NET nace gated-OFF: el redeploy NO debe exponer nada. Solo el operador tiene el entorno Linux + credenciales wrangler.</what-built>
  <how-to-verify>
    1. Build en Linux/Docker: `obsbuild` (OpenNext en Linux — NO Windows).
    2. Deploy: `wrangler deploy`. Confirmar que `NET_PUBLIC_ENABLED` NO está seteado en el entorno de Cloudflare (ausente => OFF; NO encender — depende de 17-LEGAL-DOSSIER signoff approved, deuda F17).
    3. Verificar en producción que `/red` (y `/red?seed=D###`) devuelve 404 / honest-state — NET NO expuesto.
    4. Verificar que las 7 rutas existentes (`/`, `/buscar`, `/proyecto/[boletin]`, `/parlamentario/[id]`, `/agenda`, `/parlamentarios`, `/contraparte/[id]`) siguen funcionando sin regresión.
    Esperado: deploy verde; /red 404 en producción; cero exposición de NET; resto del sitio intacto.
  </how-to-verify>
  <resume-signal>Escribe "desplegado" si /red da 404 en producción y el resto del sitio funciona, o pega los errores.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → público | el redeploy no debe exponer NET; gate OFF en el entorno de producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Elevation of Privilege | NET expuesto en deploy | mitigate | NET_PUBLIC_ENABLED ausente en Cloudflare; verificación humana de /red=404 en producción |
| T-18-11 | Information Disclosure | xyflow bloat / leak en server bundles | mitigate | build-size check confirma chunk solo en /red |
</threat_model>

<verification>
- `cd app && npm run test && npm run typecheck` verde; build aísla xyflow; negative-match en cero.
- Tras operador (Task 2): /red 404 en producción, 7 rutas existentes sin regresión, NET_PUBLIC_ENABLED ausente.
</verification>

<success_criteria>
- Suite + tsc verdes; xyflow contenido en /red; ninguna arista LLM-inferida; copy sin score/causal/path-as-accusation; redeploy Linux gated-OFF con /red cerrado en producción.
</success_criteria>

<output>
Create `.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-04-SUMMARY.md` when done
</output>
