---
phase: 18-net-grafo-de-influencia-xyflow-react
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/components/red/red-graph.tsx
  - app/components/red/red-graph.test.tsx
  - app/components/red/nodo-parlamentario.tsx
  - app/components/red/arista-hecho.tsx
  - app/app/globals.css
autonomous: true
requirements: [NET-02]
must_haves:
  truths:
    - "el grafo se renderiza como isla client island @xyflow/react@12, sin inflar el server bundle de las 7 rutas existentes"
    - "el ciudadano filtra por tipo de arista y por ventana temporal"
    - "cada arista es trazable a su fuente (tooltip provenance: origen + fecha + ventana + enlace + licencia)"
    - "el nodo muestra solo nombre + cámara — sin partido, sin foto, sin score/badge de persona"
    - "el copy es sobrio ES, describe el hecho (no afinidad/causal); el mecanismo de propagación CC BY 4.0 existe para cuando lleguen aristas derivadas de InfoProbidad"
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "isla 'use client' xyflow con filtros tipo/tiempo + provenance"
      contains: "use client"
    - path: "app/components/red/nodo-parlamentario.tsx"
      provides: "nodo custom: nombre + cámara, sin partido/foto/score"
  key_links:
    - from: "app/components/red/red-graph.tsx"
      to: "@xyflow/react"
      via: "client-island import + dist/style.css"
      pattern: "@xyflow/react"
    - from: "app/components/red/arista-hecho.tsx"
      to: "provenance tooltip"
      via: "@radix-ui/react-tooltip origen/fecha/enlace/licencia"
      pattern: "licencia"
---

<objective>
Construir la UI de NET (NET-02): la isla `'use client'` `@xyflow/react@12.11.0` bajo `app/components/red/` que renderiza el subgrafo que la RSC `/red` (Plan 02) le pasa como props — con filtros por tipo de arista y por tiempo, nodo custom (nombre + cámara, sin partido/foto/score), arista custom (copy sobrio del hecho + ventana temporal) y tooltip de provenance por arista (origen + fecha + enlace + licencia), incluyendo el mecanismo de propagación de atribución CC BY 4.0 para cuando lleguen aristas derivadas de InfoProbidad.

Purpose: Cerrar NET-02 — el ciudadano explora la red gateada-OFF. La superficie MÁS insinuante del producto: el copy describe el hecho, NUNCA afinidad/cercanía/causalidad; cero score/ranking/path-as-feature. xyflow se mantiene estrictamente client-island para no inflar el bundle de las 7 rutas server existentes.
Output: install de @xyflow/react en app/; red-graph.tsx + nodo/arista custom + tests; extensión de globals.css (NO toca civic-tokens.css).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-CONTEXT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-02-PLAN.md

# Source-of-truth patterns
@app/app/red/page.tsx

<interfaces>
<!-- Props the RSC (Plan 02) passes to the island. Shape from subgrafo_red (Plan 01). -->
RedGraph props: { subgrafo: { nodos: {id,nombre,camara}[], aristas: {tipo,a,b,contexto,desde,hasta,dataset,origen,enlace,licencia}[] } }
  -- nodos NUNCA traen partido/rut. tipo del MVP = 'co_lobby_contraparte'.

Already-installed deps to REUSE (do NOT add): @radix-ui/react-tooltip ^1.2.10 (provenance popover), lucide-react ^1.20.0 (icons).
xyflow CSS import REQUIRED: import "@xyflow/react/dist/style.css"  (else nodes/edges unstyled).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [LEGITIMIDAD] Verificar @xyflow/react@12.11.0 antes de instalar</name>
  <what-built>@xyflow/react es una dep nueva (no instalada). RESEARCH la marca Approved (org oficial xyflow, sucesor de react-flow-renderer, v12.11.0 confirmado por `npm view`), pero slopcheck no pudo emitir JSON en esta versión del CLI → verificación humana belt-and-suspenders antes del install (gate T-18-SC).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/@xyflow/react — confirmar: publicado por la org `xyflow`, repo `github.com/xyflow/xyflow`, última versión 12.11.0, descargas en millones/semana.
    2. Confirmar peerDependencies.react = `>=17` (compatible con React 19.2.4 del repo).
    3. Confirmar que NO hay dependencias node-only (solo react/react-dom como peers) — relevante para el build OpenNext/Workers.
    Esperado: paquete legítimo de la org oficial, sin señales de slop/typosquat.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para autorizar el install, o "rechazado" con motivo.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar @xyflow/react@12.11.0 en app/ (client island)</name>
  <files>app/package.json, app/package-lock.json</files>
  <read_first>
    - .planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md §Standard Stack (install en app/ SOLO; versión 12.11.0; peer react>=17) + Pitfall 3 (xyflow NO debe entrar en bundles server) + Pitfall 7 (build OpenNext/Workers)
    - app/package.json (confirmar next 16.2.9 / react 19.2.4)
  </read_first>
  <action>
    `cd app && npm install @xyflow/react@12.11.0` (en `app/` únicamente — es dep client-island; NO instalar en raíz ni en packages server). Confirmar que `package.json` registra `@xyflow/react: 12.11.0` y que `package-lock.json` se actualizó. NO importar la lib en ningún archivo server/compartido todavía (eso es Task 3, en `'use client'`).
  </action>
  <verify>
    <automated>node -e "const p=require('./app/package.json'); const v=(p.dependencies&&p.dependencies['@xyflow/react'])||(p.devDependencies&&p.devDependencies['@xyflow/react']); if(!v){console.error('@xyflow/react no instalado');process.exit(1)} console.log('@xyflow/react',v)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/package.json` dependencies incluye `@xyflow/react` en versión 12.11.0.
    - `app/package-lock.json` actualizado.
    - La lib NO está importada todavía en ningún archivo (se introduce en Task 3 dentro de `'use client'`).
  </acceptance_criteria>
  <done>@xyflow/react@12.11.0 instalado en app/ sin importarse aún en código.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Isla RedGraph (xyflow) + nodo/arista custom + provenance + filtros + globals.css</name>
  <files>app/components/red/red-graph.tsx, app/components/red/nodo-parlamentario.tsx, app/components/red/arista-hecho.tsx, app/components/red/red-graph.test.tsx, app/app/globals.css</files>
  <read_first>
    - .planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md §xyflow Client-Island Integration Steps (pasos 3-7: 'use client', import dist/style.css, controlled useNodesState/useEdgesState, custom node = nombre+cámara sin partido/foto/score, custom edge = copy del hecho + ventana, Radix tooltip provenance, layout neutral NO force-directed) + §Anti-Patterns (no aggregating weight, no force-directed clustering) + §Edge Taxonomy (copy del hecho co_lobby_contraparte: "ambos recibieron audiencia de la contraparte Y")
    - app/app/red/page.tsx (Plan 02 — la prop `subgrafo` que esta isla recibe)
    - app/app/globals.css (extender; NO tocar civic-tokens.css; tokens crema/petróleo de Phase 19)
    - un componente existente que use @radix-ui/react-tooltip (patrón de provenance popover) y lucide-react (iconos)
  </read_first>
  <behavior>
    - Dado un subgrafo con N nodos, RedGraph renderiza N nodos custom que muestran nombre + cámara y NUNCA partido/rut/foto/score.
    - Dada una arista co_lobby_contraparte, el label describe el hecho ("ambos recibieron audiencia de {contraparte}") con la ventana temporal, sin palabra de afinidad/causal.
    - El tooltip de provenance de una arista expone origen + fecha + ventana (desde/hasta) + enlace + licencia (si la fila la trae; NULL → no se muestra atribución).
    - El filtro por tipo de arista oculta/muestra aristas por `tipo`.
    - El filtro por tiempo (ventana) oculta aristas fuera del rango seleccionado.
    - Si una arista trae licencia 'CC BY 4.0' (futuro InfoProbidad-derived), el tooltip propaga la atribución; con licencia NULL, no se afirma atribución.
  </behavior>
  <action>
    Crear `app/components/red/red-graph.tsx` con `"use client"` en línea 1 e `import "@xyflow/react/dist/style.css"`. Mapear `subgrafo.nodos`/`subgrafo.aristas` a nodes/edges controlados (`useNodesState`/`useEdgesState`). Layout NEUTRAL determinista (p.ej. grid o agrupación por cámara) — NUNCA force-directed/spring (lectura de cercanía = afinidad, anti-insinuación). Controles `Background`/`Controls` de xyflow. Filtros UI: por tipo de arista (en el MVP solo `co_lobby_contraparte`, pero el control es genérico) y por ventana temporal (rango de fechas que filtra aristas por desde/hasta), operando client-side sobre el subgrafo ya recibido (sin round-trips extra).
    `app/components/red/nodo-parlamentario.tsx`: nodo custom que renderiza SOLO `nombre` + `camara` (label sobrio ES). PROHIBIDO partido, foto, badge de score/ranking.
    `app/components/red/arista-hecho.tsx`: arista custom con label = copy del hecho tipado ("ambos recibieron audiencia de {contexto}") + ventana temporal; tooltip `@radix-ui/react-tooltip` con provenance: origen + fecha_captura/ventana + enlace + licencia. La licencia se renderiza SOLO si la fila la trae (mecanismo de propagación CC BY 4.0 listo para aristas InfoProbidad-derived; con NULL no muestra atribución). Iconos vía lucide-react.
    Extender `app/app/globals.css` con los estilos de la isla (tokens crema/petróleo de Phase 19); NO tocar `civic-tokens.css`.
    `app/components/red/red-graph.test.tsx` (RTL): los 6 casos de <behavior> — assert que el nodo NO renderiza partido/foto/score; que el label de arista describe el hecho sin afinidad; que el tooltip expone provenance + maneja licencia NULL; que los filtros tipo/tiempo cambian las aristas visibles.
  </action>
  <verify>
    <automated>cd app && npm run test -- red/red-graph && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `red-graph.tsx` línea 1 es `"use client";` e importa `@xyflow/react` + `@xyflow/react/dist/style.css`.
    - Negative-match anti-insinuación sobre los 3 componentes red/*: CERO ocurrencias de `puntaje|score|ranking|afinidad|cercan|alinead|bloque de votos|por eso|a cambio de|conexión sospechosa|fuerza|force` (grep insensible a mayúsculas) — y CERO `partido`/`rut`/`foto` en el nodo.
    - El test afirma que el nodo muestra nombre+cámara y NO partido/foto/score.
    - El test afirma que el tooltip de arista expone origen+fecha+ventana+enlace y propaga licencia cuando existe (y la omite cuando NULL).
    - El test afirma que el filtro por tipo y por tiempo alteran las aristas visibles.
    - `npm run typecheck` limpio.
    - El import de `@xyflow/react` aparece SOLO en archivos `'use client'` bajo `app/components/red/` (no en server/compartidos).
  </acceptance_criteria>
  <done>Isla RedGraph xyflow renderiza el subgrafo con nodo/arista custom sobrios, filtros tipo/tiempo, provenance + propagación CC BY 4.0; tests verdes y tsc limpio; xyflow contenido en la isla.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client island | xyflow vive solo en 'use client'; no debe filtrarse al bundle server de las 7 rutas |
| arista data → copy | el copy de arista/nodo es la superficie más insinuante; debe describir el hecho, nunca afinidad/causal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Information Disclosure | nodo emite partido/foto/score | mitigate | nodo custom proyecta solo nombre+cámara; grep + test niegan partido/foto/score |
| T-18-09 | Repudiation / framing | arista/camino leído como acusación | mitigate | copy del hecho tipado + ventana; layout neutral (no force-directed); cero score/path-as-feature; negative-match grep |
| T-18-SC | Tampering | npm install @xyflow/react | mitigate | checkpoint blocking-human de legitimidad antes del install (Task 1) |
</threat_model>

<verification>
- `cd app && npm run test -- red/red-graph` y `npm run typecheck` verdes.
- Negative-match grep anti-insinuación sobre app/components/red/* en CERO.
- @xyflow/react importado solo en la isla.
</verification>

<success_criteria>
- Isla xyflow con filtros tipo/tiempo, nodo/arista sobrios, provenance por arista y mecanismo CC BY 4.0; xyflow contenido en client island; tests + tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-03-SUMMARY.md` when done
</output>
