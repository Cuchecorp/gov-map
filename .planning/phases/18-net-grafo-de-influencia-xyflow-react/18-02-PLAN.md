---
phase: 18-net-grafo-de-influencia-xyflow-react
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - app/lib/net-gate.ts
  - app/lib/net-gate.test.ts
  - app/app/red/page.tsx
  - app/app/red/page.test.tsx
  - app/app/red/not-found.tsx
autonomous: true
requirements: [NET-01, NET-02]
must_haves:
  truths:
    - "netPublicEnabled() nace OFF: solo el literal 'true' enciende; ausente/''/'false'/'1'/'TRUE' => false"
    - "la ruta /red da notFound() cuando el gate está OFF, como PRIMERA sentencia, sin filtrar DOM de NET"
    - "cuando el gate está ON, la ruta consume el RPC subgrafo_red por createServerSupabase() y pasa JSON a la isla cliente"
    - "net-gate.ts es server-only: el flag NET_PUBLIC_ENABLED nunca llega al bundle del navegador"
  artifacts:
    - path: "app/lib/net-gate.ts"
      provides: "Candado B (presentación) server-only, fail-closed"
      contains: "netPublicEnabled"
    - path: "app/app/red/page.tsx"
      provides: "RSC gateada que consume subgrafo_red y monta la isla"
      contains: "notFound"
  key_links:
    - from: "app/app/red/page.tsx"
      to: "subgrafo_red"
      via: "createServerSupabase().rpc"
      pattern: "subgrafo_red"
    - from: "app/app/red/page.tsx"
      to: "netPublicEnabled"
      via: "gate as first statement"
      pattern: "netPublicEnabled\\(process.env\\)"
---

<objective>
Construir el Candado B (presentación) de NET: `app/lib/net-gate.ts` (espejo exacto de `money-gate.ts`, server-only, fail-closed) y la ruta `/red` como Server Component que (1) gatea con `notFound()` como PRIMERA sentencia cuando OFF, y (2) cuando ON, consume el RPC `subgrafo_red` vía `createServerSupabase()` y pasa el JSON plano a la isla cliente (Plan 03). Más sus tests vitest/RTL.

Purpose: Cerrar el doble candado (RLS de Plan 01 + flag de presentación) y dar a NET su superficie pública gateada-OFF. Mientras `NET_PUBLIC_ENABLED` no sea `"true"` (deuda operador F17, depende de 17-LEGAL-DOSSIER signoff approved), la ruta 404 sin filtrar nada. Plan 03 reemplaza el placeholder de la isla por el grafo xyflow real.
Output: `net-gate.ts` + test; `app/app/red/page.tsx` + `page.test.tsx` + `not-found.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-CONTEXT.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md
@.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-01-PLAN.md

# Source-of-truth patterns this plan mirrors
@app/lib/money-gate.ts
@app/lib/money-gate.test.ts
@app/app/contraparte/[id]/page.tsx

<interfaces>
<!-- RPC contract from Plan 01 (0030_net.sql). The route consumes this via supabase.rpc. -->
subgrafo_red(p_id text, p_depth int default 1, p_tipos text[] default null, p_desde timestamptz default null, p_hasta timestamptz default null) returns jsonb
  -> { nodos: [{ id, nombre, camara }], aristas: [{ tipo, a, b, contexto, desde, hasta, dataset, origen, enlace, licencia }] }
  -- nodos NUNCA traen partido/rut. tipo del MVP = 'co_lobby_contraparte'.

From app/lib/money-gate.ts (mirror target):
  export function moneyPublicEnabled(env = process.env): boolean { return env.MONEY_PUBLIC_ENABLED === "true"; }
From app/lib/supabase.ts:
  createServerSupabase()  // used by every route to call RPCs server-side
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: net-gate.ts (server-only, fail-closed) + test</name>
  <files>app/lib/net-gate.ts, app/lib/net-gate.test.ts</files>
  <read_first>
    - app/lib/money-gate.ts (espejo EXACTO: `import "server-only"` línea 1; `export function moneyPublicEnabled(env = process.env): boolean { return env.MONEY_PUBLIC_ENABLED === "true"; }`; sin truthiness laxa; sin prefijo NEXT_PUBLIC_)
    - app/lib/money-gate.test.ts (los 5 casos: ausente/'false'/'1'/'TRUE' => false; 'true' => true)
    - .planning/phases/18-net-grafo-de-influencia-xyflow-react/18-RESEARCH.md §Gate Wiring (net-gate.ts)
  </read_first>
  <behavior>
    - netPublicEnabled({}) => false (ausente, default fail-closed)
    - netPublicEnabled({ NET_PUBLIC_ENABLED: "false" }) => false
    - netPublicEnabled({ NET_PUBLIC_ENABLED: "1" }) => false (sin truthiness laxa)
    - netPublicEnabled({ NET_PUBLIC_ENABLED: "TRUE" }) => false (case-sensitive)
    - netPublicEnabled({ NET_PUBLIC_ENABLED: "true" }) => true (único valor que enciende)
  </behavior>
  <action>
    Crear `app/lib/net-gate.ts` como espejo carácter-por-carácter de `money-gate.ts` con el flag `NET_PUBLIC_ENABLED`: `import "server-only"` en línea 1; export `netPublicEnabled(env: Record<string, string | undefined> = process.env): boolean` que retorna `env.NET_PUBLIC_ENABLED === "true"`. Docstring que declare: Candado B (presentación) del doble candado NET; default OFF fail-closed; sin prefijo NEXT_PUBLIC_; encender requiere `17-LEGAL-DOSSIER.md signoff: approved` (deuda operador F17); el chokepoint queda enforzado cuando la ruta /red enruta por `netPublicEnabled(process.env)` y NUNCA lee `NET_PUBLIC_ENABLED` crudo.
    Crear `app/lib/net-gate.test.ts` con los 5 casos de <behavior> (espejo de money-gate.test.ts).
  </action>
  <verify>
    <automated>cd app && npm run test -- net-gate</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/net-gate.ts` línea 1 es `import "server-only";`.
    - La función usa comparación estricta `=== "true"` (no `Boolean(...)`, no truthiness).
    - El flag NO lleva prefijo `NEXT_PUBLIC_`.
    - Los 5 tests pasan (vitest), incluyendo `"TRUE" => false` y `"1" => false`.
  </acceptance_criteria>
  <done>net-gate.ts existe como espejo server-only fail-closed de money-gate.ts; 5 tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ruta /red gateada que consume subgrafo_red + not-found + test</name>
  <files>app/app/red/page.tsx, app/app/red/page.test.tsx, app/app/red/not-found.tsx</files>
  <read_first>
    - app/app/contraparte/[id]/page.tsx (gate a nivel de página: `if (!moneyPublicEnabled(process.env)) notFound();` como PRIMERA sentencia ANTES de `await params` y de cualquier RPC/heading; params es Promise en Next 16; createServerSupabase().rpc; manejo de error del RPC)
    - app/app/contraparte/[id]/not-found.tsx (estructura de not-found.tsx de la ruta)
    - app/app/contraparte/[id]/page.test.tsx (patrón RTL: mock de env OFF → assert que NO se renderiza DOM de la sección; mock ON → assert render)
    - app/lib/net-gate.ts (Task 1), .planning/.../18-RESEARCH.md §xyflow Client-Island Integration Steps paso 2 (la RSC gatea primero, luego rpc, luego pasa props a la isla)
    - app/AGENTS.md (Next.js de este repo NO es el de tu training; revisar node_modules/next/dist/docs antes de escribir convenciones de App Router)
  </read_first>
  <action>
    Crear `app/app/red/page.tsx` como Server Component (NO `'use client'`): `if (!netPublicEnabled(process.env)) notFound();` como PRIMERA sentencia (antes de leer searchParams, antes de cualquier RPC o heading — no filtrar DOM de NET al HTML mientras OFF). Cuando ON: leer la semilla `seed` de `searchParams` (Promise en Next 16) validando contra el patrón de id de parlamentario (`D####`/`S####`, espejo de la validación de id en contraparte antes de tocar DB); `const sb = createServerSupabase(); const { data, error } = await sb.rpc("subgrafo_red", { p_id: seed, p_depth: 1 });` con throw si error; renderizar el placeholder `<RedGraph subgrafo={data} />` importado desde `@/components/red/red-graph` (Plan 03 entrega el componente real — en este plan, si aún no existe, dejar un placeholder client island mínimo o un import que Plan 03 reemplaza; documentar el handoff en comentario). Sin semilla seleccionada: estado honesto sobrio (picker / mensaje ES), NO un whole-graph seedless.
    Crear `app/app/red/not-found.tsx` (espejo de contraparte/[id]/not-found.tsx) con copy ES sobrio.
    Crear `app/app/red/page.test.tsx` (RTL): con `NET_PUBLIC_ENABLED` ausente/OFF, la página llama notFound() y NO renderiza ningún heading/DOM de NET; con ON + seed válido (mock del RPC), renderiza la isla con los datos. Mockear `next/navigation` notFound y `@/lib/supabase`.
  </action>
  <verify>
    <automated>cd app && npm run test -- red/page</automated>
  </verify>
  <acceptance_criteria>
    - `netPublicEnabled(process.env)` es invocado y `notFound()` es la PRIMERA sentencia ejecutable (grep: aparece antes de `rpc(` y antes de cualquier `<h1`/heading en el archivo).
    - La página llama `subgrafo_red` vía `createServerSupabase().rpc("subgrafo_red", ...)` con `p_id` = la semilla, NUNCA seedless.
    - `page.test.tsx` afirma: OFF => notFound() y CERO DOM de NET renderizado; ON => isla renderizada con datos del RPC.
    - Negative-match anti-insinuación en page.tsx + not-found.tsx: CERO palabras de score/ranking/causal/afinidad (`puntaje|score|ranking|afinidad|cercanía|por eso|a cambio de|conexión sospechosa`).
    - El flag se lee SOLO vía `netPublicEnabled(...)`, nunca `process.env.NET_PUBLIC_ENABLED` crudo en la ruta.
  </acceptance_criteria>
  <done>/red gatea con notFound() OFF (sin filtrar DOM), consume subgrafo_red por semilla cuando ON, y monta la isla; not-found.tsx y page.test.tsx verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env flag → render | NET_PUBLIC_ENABLED decide visibilidad; debe ser server-only y fail-closed |
| searchParams seed → RPC | la semilla es input no confiable; validar antes de tocar DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Elevation of Privilege | gate bypass / DOM leak while OFF | mitigate | `notFound()` como primera sentencia; flag estricto `=== "true"`; test afirma default OFF + cero DOM |
| T-18-06 | Information Disclosure | flag en bundle del navegador | mitigate | `import "server-only"` línea 1; sin prefijo NEXT_PUBLIC_ |
| T-18-07 | Input Validation | seed no validada → DB | mitigate | validar patrón id parlamentario antes del rpc (espejo contraparte) |
</threat_model>

<verification>
- `cd app && npm run test -- net-gate red/page` verde.
- Grep confirma `notFound()` como primera sentencia y consumo de `subgrafo_red`.
</verification>

<success_criteria>
- net-gate.ts server-only fail-closed; /red 404 cuando OFF sin filtrar DOM; consume subgrafo_red por semilla cuando ON; tests verdes.
</success_criteria>

<output>
Create `.planning/phases/18-net-grafo-de-influencia-xyflow-react/18-02-SUMMARY.md` when done
</output>
