---
phase: 105-bcn-parser-senadores-en-origen-re-corrida-de-militancias
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - .planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-02-SUMMARY.md
autonomous: true
requirements: [BCN-01, BCN-02]
must_haves:
  truths:
    - "La re-corrida de senadores corre --from-r2 sobre el envelope BCN ya en R2 (Etapa 2 relee del crudo, NO re-scrapea BCN)"
    - "Tras la re-corrida Y la limpieza de filas URI stale, las filas de militancia de senadores en PROD tienen cero URI-como-partido (partido ~* '^https?://' == 0)"
    - "parlamentario.partido de senadores tampoco contiene URIs tras la re-corrida"
    - "Las filas URI stale de parlamentario_militancia (alias-cambió-por-relabel) se BORRAN explícitamente tras el upsert; el cero se verifica DESPUÉS del borrado"
    - "La decisión sobre partidoLegible() queda DOCUMENTADA con evidencia post-re-corrida (cero URI en PROD)"
    - "El cruce por partido (/parlamentarios filtros/facetas) sigue funcionando (sin regresión)"
  artifacts:
    - path: ".planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-02-SUMMARY.md"
      provides: "evidencia de la re-corrida + limpieza de filas URI stale + query de verificación (cero URI) + decisión documentada BCN-02"
      contains: "partidoLegible"
  key_links:
    - from: "run-bio-cli --from-r2 <r2Path>"
      to: "PROD Supabase (parlamentario_militancia + parlamentario.partido)"
      via: "SupabaseBioWriter upsert idempotente desde el crudo R2"
      pattern: "from-r2"
    - from: "DELETE parlamentario_militancia where partido ~* '^https?://'"
      to: "PROD Supabase (reconciliación del defecto URI stale)"
      via: "psql --single-transaction acotado al patrón URI, tras el upsert"
      pattern: "partido ~\\* '\\^https"
---

<objective>
Ejecutar la re-corrida de militancias de senadores a PROD leyendo el crudo desde R2 (dos-etapas,
`--from-r2`, SIN re-molestar a BCN), LIMPIAR las filas de militancia URI-stale que el upsert deja
atrás (el alias cambia con el relabel → el upsert inserta la fila limpia pero NO borra la vieja),
verificar que quedan CERO URI-como-partido en PROD, y DOCUMENTAR la decisión sobre `partidoLegible()`
(cinturón display-only de 104-03) con la evidencia post-re-corrida.

Este plan depende de 105-01 (el parser corregido debe estar en el árbol ANTES del replay: la
Etapa 2 re-parsea el crudo con el código nuevo → produce labels limpios).

Purpose: cerrar el defecto en el DATO de PROD, no solo en el código; dejar constancia auditable
(cero URI) y la decisión de defensa-en-profundidad documentada.
Output: re-corrida ejecutada + filas URI stale reconciliadas + evidencia de verificación + SUMMARY
con la decisión BCN-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-CONTEXT.md
@CLAUDE.md

@packages/bio/src/run-bio.ts
@packages/bio/src/run-bio-cli.ts
@packages/bio/src/writer-supabase.ts
@packages/bio/src/parse-bcn-senadores.ts
@app/lib/format.ts

<interfaces>
<!-- Contratos y comandos que el executor usa directamente. -->

De packages/bio/src/run-bio-cli.ts:
- Replay sin red: `tsx packages/bio/src/run-bio-cli.ts --from-r2 <r2Path>`
  → requiere `R2_*` en .env (lectura del crudo). Sin `--dry-run` y con `SUPABASE_API_URL` +
  `SUPABASE_SECRET_KEY` presentes → escribe a PROD (SupabaseBioWriter, upsert idempotente).
  Con `--dry-run` → InMemoryBioWriter (inspecciona conteos sin tocar PROD).
- `loadEnv` carga `.env` BOM-safe; process.env tiene precedencia.

De packages/bio/src/run-bio.ts:
- El envelope R2 vive en la key `bio/envelope/<fecha>/<sha>.json`; la sección (B) senadores lee
  `envelope.senadoresSparql`, re-parsea con `parseBcnSenadores` (ya corregido por 105-01) y hace
  upsert idempotente de militancias + `actualizarPartidoParlamentario` desde la vigente.
  Nota: `actualizarPartidoParlamentario` hace `.update().eq(id)` → `parlamentario.partido` SÍ se
  sobreescribe en sitio (verificación (b) pasará). Las militancias NO: ver contrato de upsert abajo.

Tablas PROD (de writer-supabase.ts:101-112):
- `parlamentario_militancia` (columna `partido`) — onConflict `parlamentario_id,partido_alias,desde`.
- `parlamentario` (columna `partido`).
Ref PROD: bctyygbmqcvizyplktuw. Caso testigo: S1344 = `parlid_senado` (Matías Walker), NO es la PK
`parlamentario.id`. El join maestra es por `parlid_senado` (`enlazarSenadoresPorParlid`); resolver
las filas afectadas por el PATRÓN URI (`partido ~* '^https?://'`), NUNCA asumiendo que la PK es S1344.

CONTRATO DE UPSERT (verificado en writer-supabase.ts:101-112 + aliasDePartido en
parse-bcn-senadores.ts:75-83): la clave del upsert de militancia es
`parlamentario_id,partido_alias,desde`. `aliasDePartido` parte por espacios: un URI (sin espacios)
NO produce iniciales ≥2 → devuelve el URI ENTERO como alias. Tras 105-01 el parser emite el label
legible → `partido_alias` CAMBIA (p.ej. a `"PDC"`), MISMO `desde`. El writer es upsert-only, SIN
delete/reconcile → el upsert INSERTA la fila limpia y DEJA la fila URI vieja stale. Por eso la
re-corrida NO basta: hay que BORRAR explícitamente las filas URI stale (Task 3), y verificar el
cero DESPUÉS del borrado.

Comando DB (CLAUDE.md): `set -a; source .env; set +a` y luego psql contra la URL de PROD con
`--single-transaction`; JAMÁS imprimir la URL. PostgREST/consultas: cap 1k → usar COUNT, no listar
filas. El DELETE de Task 3 es raw SQL psql directo, ACOTADO a filas con `partido ~* '^https?://'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localizar el/los envelope(s) R2 vigentes para el replay</name>
  <read_first>
    - packages/bio/src/run-bio.ts (formato de key `bio/envelope/<fecha>/<sha>.json`, campo senadoresSparql)
    - packages/bio/src/run-bio-cli.ts (construcción del R2Store desde .env, flag --from-r2)
    - CLAUDE.md (convención dos-etapas: Etapa 2 SIEMPRE lee del crudo R2)
  </read_first>
  <action>
    Localizar la(s) key(s) del envelope BCN en R2 que contienen `senadoresSparql` no-null, para el
    replay. Usar las credenciales `R2_*` de `.env` (ya presentes por milestones previos). Listar el
    prefijo `bio/envelope/` en el bucket R2 (usar el `R2Store`/cliente S3 del paquete `@obs/ingest`,
    o un script tsx puntual que instancie el store como lo hace `run-bio-cli.ts`), identificar la
    key más reciente cuyo envelope tenga `senadoresSparql` poblado. Si hay varias fechas, elegir la
    más reciente que contenga senadores (esa es la que refleja el estado vivo de PROD). JAMÁS
    imprimir la URL/endpoint de R2 ni credenciales en el reporte; reportar SOLO la key relativa
    (`bio/envelope/<fecha>/<sha>.json`).
    Opcional de robustez: correr un `--dry-run --from-r2 <key>` para confirmar que el envelope
    parsea y trae senadores (conteo de militancias > 0) SIN tocar PROD, antes de la corrida real.
    NOTA (WARNING-2 105-01): este es el MISMO crudo que 105-01 Task 1 debió inspeccionar para
    fundamentar el mapa URI→label. Si la lista de URIs de partido del envelope (extraíbles de
    `senadoresSparql`) contiene alguna URI que el mapa de 105-01 NO cubre, registrarlo aquí: esas
    URIs quedarán fail-closed (omitidas) → sus militancias URI-stale entran en el conjunto a borrar
    en Task 3 igual (el borrado por patrón las cubre), pero conviene dejar constancia por si procede
    volver a 105-01 a ampliar el mapa antes de re-emitir.
  </action>
  <verify>
    <automated>tsx packages/bio/src/run-bio-cli.ts --from-r2 <r2Path> --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - La key del envelope BCN con `senadoresSparql` no-null está identificada (formato `bio/envelope/<fecha>/<sha>.json`), reportada SIN exponer URL/credenciales R2.
    - El `--dry-run --from-r2 <key>` corre sin error y reporta `militancias > 0` (envelope válido, senadores presentes).
    - El reporte confirma que el crudo trae la militancia del caso testigo (S1344 / partido-democratas-chile) o registra que ese envelope no lo contiene (dato para la re-corrida).
    - Registrada la comparación URIs-del-crudo vs mapa-105-01 (URIs no cubiertas → fail-closed → filas a borrar en Task 3).
  </acceptance_criteria>
  <done>Key R2 del envelope BCN vigente para el replay identificada y validada por dry-run.</done>
</task>

<task type="auto">
  <name>Task 2: Re-corrida --from-r2 a PROD (upsert idempotente de militancias limpias)</name>
  <read_first>
    - packages/bio/src/run-bio-cli.ts (path --from-r2 LIVE: writer Supabase cuando hay creds y no --dry-run)
    - packages/bio/src/run-bio.ts (sección B senadores: upsert idempotente + actualizarPartidoParlamentario)
    - packages/bio/src/writer-supabase.ts:101-112 (upsert-only, sin delete → el defecto que Task 3 reconcilia)
    - CLAUDE.md (write de datos permitido — NO es flag *_PUBLIC_ENABLED, NO sign-off legal, NO RUT)
    - El SUMMARY de 105-01 (confirmar que el parser corregido está en el árbol)
  </read_first>
  <action>
    Ejecutar la re-corrida REAL contra PROD leyendo el crudo desde R2 (Etapa 2, SIN red a BCN):
    `tsx packages/bio/src/run-bio-cli.ts --from-r2 <r2Path>` (la key de la Task 1), con `.env`
    cargando `R2_*` + `SUPABASE_API_URL` + `SUPABASE_SECRET_KEY` y SIN `--dry-run`. El writer
    Supabase hace upsert idempotente de las militancias (ahora con `partido` legible y
    `partido_alias` legible) e `actualizarPartidoParlamentario` (`.update().eq(id)`) desde la
    militancia vigente. Registrar el conteo reportado (`militancias=`, `partidosActualizados=`,
    `sinMatch=`) — las URIs desconocidas del parser (105-01) aparecen como omisiones declaradas.
    IMPORTANTE: tras este upsert, `parlamentario.partido` ya está limpio en sitio (verificación (b)
    pasará), pero en `parlamentario_militancia` conviven la fila NUEVA limpia y la fila VIEJA URI
    (el alias cambió → clave distinta → el upsert NO la sobrescribió). Esas filas URI stale se
    borran en Task 3; la verificación de cero-URI de militancia se hace DESPUÉS de Task 3.
    Verificación parcial post-upsert (read-only, `set -a; source .env; set +a` y psql; JAMÁS imprimir
    la URL): contar en PROD con COUNT (no listar — cap 1k):
      (b) `select count(*) from parlamentario where partido ~* '^https?://';` → debe ser 0.
    Confirmar el caso testigo por parlid (NO por PK): 
      `select p.partido from parlamentario p where p.parlid_senado = 'S1344';`
      (o el id maestra confirmado que enlaza a S1344) → un nombre legible, NO un URI.
    Registrar ambos resultados como evidencia parcial para el SUMMARY.
    Verificación de NO regresión del cruce por partido: confirmar (dry-run del app o consulta) que
    la faceta/filtro de `/parlamentarios` por partido sigue devolviendo grupos (la clave serializada
    RAW se mantiene por diseño 104-03 — solo cambió el VALOR almacenado). Registrar la evidencia.
  </action>
  <verify>
    <automated>set -a; source .env; set +a; psql "$SUPABASE_DB_URL" -tAc "select count(*) from parlamentario where partido ~* '^https?://';" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - La re-corrida `--from-r2` corrió sin `--dry-run` contra PROD y reportó `militancias > 0` upsert idempotente (sin re-scrapear BCN — Etapa 2 desde R2).
    - `count(*) parlamentario where partido ~* '^https?://'` == 0 (evidencia registrada) — la columna de la tabla persona se limpió en sitio.
    - El caso testigo (resuelto por `parlid_senado = 'S1344'`, NO por PK) muestra un partido legible (no URI).
    - La faceta/filtro por partido de `/parlamentarios` sigue produciendo grupos (sin regresión) — evidencia registrada.
    - Ninguna URL de PROD ni credencial impresa en el reporte.
  </acceptance_criteria>
  <done>Militancias de senadores re-corridas a PROD desde R2 (upsert idempotente, filas limpias insertadas); parlamentario.partido limpio; caso testigo legible; cruce por partido sin regresión. Las filas URI stale de militancia quedan pendientes para Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Reconciliar filas URI-stale en parlamentario_militancia (DELETE acotado) + verificación cero URI</name>
  <read_first>
    - packages/bio/src/writer-supabase.ts:101-112 (upsert-only, onConflict `parlamentario_id,partido_alias,desde` — la razón de la fila stale)
    - packages/bio/src/parse-bcn-senadores.ts:75-83 (aliasDePartido: un URI sin espacios → alias = URI entero → clave distinta al relabel)
    - CLAUDE.md (DB write directo permitido; `--single-transaction`; JAMÁS imprimir la URL; borrado ACOTADO a un valor conocido-malo)
    - La evidencia de conteos de Task 2 (militancias limpias ya insertadas por el upsert)
  </read_first>
  <action>
    Reconciliar el defecto de dato: el upsert idempotente de Task 2 YA insertó la fila de militancia
    LIMPIA (partido legible, alias legible) para cada senador afectado, pero la fila URI VIEJA sigue
    presente porque su `partido_alias` (= el URI entero, por `aliasDePartido`) difiere del alias
    nuevo → clave natural distinta → el upsert no la tocó. Esto NO es fabricación: es la eliminación
    de un VALOR CONOCIDO-MALO (un URI donde debe ir un label de partido), es decir reconciliación de
    un defecto de dato, consistente con la regla fail-closed / "ante la duda, calidad".

    PRE-BORRADO (read-only, `set -a; source .env; set +a`; psql; JAMÁS imprimir la URL): contar y
    registrar cuántas filas URI stale existen ANTES del borrado, resueltas por el PATRÓN URI (robusto,
    captura TODAS las filas afectadas sin depender de ninguna id concreta):
      `select count(*) from parlamentario_militancia where partido ~* '^https?://';`
    Registrar ese N (>0 esperado; es el testigo del defecto).

    BORRADO (raw SQL psql directo, `--single-transaction`, ACOTADO al valor conocido-malo — un URI
    donde va un label; JAMÁS imprimir la URL):
      `delete from parlamentario_militancia where partido ~* '^https?://';`
    El borrado se identifica SOLO por el patrón URI (no por `S1344` ni por PK), de modo que cubre
    todas las filas afectadas cualquiera sea la forma de la id. Las URIs desconocidas que 105-01
    dejó fail-closed (militancia no re-emitida) también quedan cubiertas: su fila URL stale se borra
    igual (no queda un URI en PROD), y su ausencia de militancia limpia queda como omisión declarada
    del parser — NUNCA se fabrica un partido para rellenarla.

    POST-BORRADO — VERIFICACIÓN cero URI (read-only, COUNT, cap 1k):
      (a) `select count(*) from parlamentario_militancia where partido ~* '^https?://';` → debe ser 0.
      (b) `select count(*) from parlamentario where partido ~* '^https?://';` → debe ser 0 (ya de Task 2).
    Confirmar de nuevo el caso testigo por parlid: la militancia vigente del senador enlazado a
    `parlid_senado = 'S1344'` existe con partido legible (la fila limpia que insertó Task 2), y NO
    quedó ninguna fila suya con URI.
    Registrar los tres resultados (N previo borrado, dos ceros, testigo legible) como evidencia para
    el SUMMARY. Si (a) NO es 0 tras el borrado, NO cerrar: reportar las filas que persisten (raro:
    implicaría un valor que empieza con http fuera del patrón, o una fila insertada nueva errónea) y
    NO fabricar partido.
  </action>
  <verify>
    <automated>set -a; source .env; set +a; psql "$SUPABASE_DB_URL" -tAc "select count(*) from parlamentario_militancia where partido ~* '^https?://';" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Registrado el conteo PRE-borrado de filas URI stale (N > 0 esperado — testigo del defecto), resuelto por el patrón `partido ~* '^https?://'` (no por id concreta).
    - DELETE ejecutado con `--single-transaction`, ACOTADO a `partido ~* '^https?://'`, sin imprimir la URL.
    - POST-borrado: `count(*) parlamentario_militancia where partido ~* '^https?://'` == 0 (evidencia registrada).
    - POST-borrado: `count(*) parlamentario where partido ~* '^https?://'` == 0 (evidencia registrada).
    - El caso testigo (por `parlid_senado = 'S1344'`) conserva su militancia vigente con partido legible; sin filas suyas con URI.
    - Ninguna URL de PROD ni credencial impresa; ningún partido fabricado para URIs fail-closed.
  </acceptance_criteria>
  <done>Filas URI-stale de parlamentario_militancia borradas (reconciliación de valor conocido-malo, acotada al patrón URI); cero URI-como-partido verificado en AMBAS tablas DESPUÉS del borrado; caso testigo legible.</done>
</task>

<task type="auto">
  <name>Task 4: Documentar la decisión sobre partidoLegible() (BCN-02) en el SUMMARY</name>
  <read_first>
    - app/lib/format.ts:153 (partidoLegible — el cinturón display-only de 104-03)
    - .planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-CONTEXT.md (decisión LOCKED: conservar como defensa en profundidad)
    - La evidencia de verificación de las Tasks 2 y 3 (cero URI en PROD)
  </read_first>
  <action>
    Documentar en el 105-02-SUMMARY.md la decisión sobre `partidoLegible()` (BCN-02), aplicando la
    postura LOCKED del operador en CONTEXT.md: **CONSERVAR** `app/lib/format.ts:153 partidoLegible()`
    como cinturón display-only de **defensa en profundidad**, SIN eliminar código por defecto.
    Escribir la justificación con la evidencia post-re-corrida de las Tasks 2 y 3: dado que el dato en
    PROD quedó limpio (cero URI en ambas tablas tras el upsert + la reconciliación de filas stale),
    la función YA NO debería activarse en la práctica; se conserva como protección barata e
    idempotente ante regresiones futuras del parser o datos BCN nuevos que introduzcan un URI. Dejar
    constancia de que sus 6 tests (`format.test.ts:198`) permanecen verdes y sus 3 consumidores
    (`partido-chip.tsx`, `militancias-de-parlamentario.tsx`, `parlamentarios-filtro.tsx`) quedan
    intactos. NO se elimina código en este plan (Deferred: retiro definitivo solo si evidencia futura
    lo justifica).
    Incluir en el SUMMARY: (1) la key R2 usada, (2) los conteos de la re-corrida, (3) el conteo
    PRE-borrado de filas URI stale + el DELETE ejecutado, (4) los dos ceros de verificación
    POST-borrado + el partido legible del caso testigo (por parlid), (5) la lista de URIs desconocidas
    omitidas por el parser (si las hubo), (6) la decisión BCN-02 documentada con la evidencia.
    Correr la suite completa para confirmar cero regresión: app (1428), packages, tsc 0, y los 9
    guards v10.0 (según convención del proyecto).
  </action>
  <verify>
    <automated>grep -c "partidoLegible" .planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - El 105-02-SUMMARY.md documenta la decisión BCN-02 (conservar partidoLegible como defensa en profundidad) CON la evidencia post-re-corrida (cero URI en PROD).
    - El SUMMARY incluye: key R2, conteos de re-corrida, conteo PRE-borrado + DELETE de filas URI stale, dos ceros de verificación POST-borrado, partido legible del testigo (por parlid), URIs desconocidas omitidas.
    - `app/lib/format.ts` NO fue modificado (conservación por defecto) — confirmable por `git status`.
    - Suite verde: app 1428 + packages + `tsc` 0 + 9 guards v10.0 (sin regresión de las superficies de militancia).
  </acceptance_criteria>
  <done>Decisión BCN-02 documentada con evidencia; partidoLegible conservado sin cambios; suite verde.</done>
</task>

</tasks>

<verification>
- Re-corrida `--from-r2` ejecutada contra PROD (Etapa 2 desde R2, sin re-scrapear BCN).
- Filas URI-stale de `parlamentario_militancia` reconciliadas por DELETE acotado al patrón URI, tras el upsert.
- `count(*)` de partido con URI == 0 en `parlamentario_militancia` (POST-borrado) y en `parlamentario`.
- Caso testigo (por `parlid_senado = 'S1344'`) con militancia vigente de partido legible.
- Faceta/filtro por partido de `/parlamentarios` sin regresión.
- Decisión BCN-02 documentada en el SUMMARY con evidencia.
- Suite verde (app 1428 + packages + tsc 0 + 9 guards v10.0).
</verification>

<success_criteria>
- BCN-01 (mitad DATOS): tras la re-corrida Y la reconciliación de filas stale, cero URI-como-partido
  en las filas afectadas de PROD (ambas tablas), verificado DESPUÉS del borrado.
- BCN-02: decisión sobre `partidoLegible()` documentada con evidencia post-re-corrida (conservar
  como defensa en profundidad, sin retiro por defecto).
- Sin regresión: el cruce por partido sigue funcionando; la clave serializada RAW intacta (104-03).
</success_criteria>

<output>
Create `.planning/phases/105-bcn-parser-senadores-en-origen-re-corrida-de-militancias/105-02-SUMMARY.md` when done.
</output>
