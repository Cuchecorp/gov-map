---
phase: 120-escalera-on-flip-clasificacion-escalera
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - .env
  - .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md
autonomous: true
requirements: [CRON-03]

must_haves:
  truths:
    - "`CLASIFICACION_ESCALERA=1` queda activo SOLO después del checkpoint de keys (cerrado) y de los gates 2-4 verdes; el agente jamás lee ni transcribe la key (SC#4)"
    - "Una corrida de humo acotada del CLI de clasificación emite el log `provider=tiered:granite→deepseek`, probando que la escalera está realmente encendida"
    - "El rollback inverso está re-probado en vivo: quitar la var devuelve el log `provider=deepseek (default incumbente)`; volver a dejarla en 1 restaura la escalera (SC#3)"
    - "La adjudicación de identidad y la extracción strict-schema siguen intocadas: integ-scope-guard + provider-guard + env-example verdes (SC#5)"
    - "Suite y tsc verdes al cierre; `120-FLIP-RECORD.md` cierra con el estado final de CRON-03"
  artifacts:
    - path: ".planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md"
      provides: "Registro completo gates + flip + humo + rollback inverso + guards, consumido por 121 y el E2E 125"
      contains: "## Gate 5 — Flip"
  key_links:
    - from: ".env"
      to: "packages/cruces/src/clasificar-fichas-cli.ts resolverProvider rama 3"
      via: "env var CLASIFICACION_ESCALERA=1"
      pattern: "^CLASIFICACION_ESCALERA=1"
    - from: "120-FLIP-RECORD.md"
      to: "corrida de humo del CLI"
      via: "log del provider resuelto"
      pattern: "provider=tiered:granite"
---

<objective>
Ejecutar el paso 5 del orden DURO: escribir `CLASIFICACION_ESCALERA=1` en `.env`, probar en vivo con una corrida de humo acotada que la escalera está encendida, re-probar el rollback inverso, dejar los guards de régimen verdes y cerrar `120-FLIP-RECORD.md`.

Purpose: CRON-03 exige la escalera encendida en clasificación CON red de seguridad demostrada, sin tocar adjudicación ni extracción.
Output: `.env` con el flip, registro cerrado, guards + suite + tsc verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-CONTEXT.md
@.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md

<read_first>
- `.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md` § `## Veredicto de gates` — PRECONDICIÓN DURA del plan entero.
- `packages/cruces/src/clasificar-fichas-cli.ts:202-245` (`resolverProvider`, logs exactos de rama 2 y rama 3) y el bloque `isMain` del final del archivo (entry-point `tsx clasificar-fichas-cli.ts --limite N [--dry-run]`, `parseArgs` fail-fast, exit codes 0/1/2).
- `packages/cruces/src/clasificar-fichas-cli.ts:82-135` — flags reales: `--dry-run`, `--limite N`, `--service-key K`, `--desde`, `--solo-confirmadas`. `--dry-run` NO escribe en DB pero SÍ lee para reportar cobertura y SÍ hace llamadas reales al LLM: es exactamente la forma acotada de la corrida de humo.
</read_first>

<precondicion>
Si `120-FLIP-RECORD.md` NO contiene `GATES 2-4 VERDES — EL FLIP PROCEDE`, este plan NO ejecuta ninguna task de flip: anota en el registro `Plan 02 NO EJECUTADO — gates no verdes` y termina como ÉXITO (cierre honesto = resultado VÁLIDO).
</precondicion>

<secretos>
Misma regla dura: exportar con `set -a; source .env; set +a`; jamás imprimir, catear ni transcribir valores. Escribir la línea del flip en `.env` sin volcar el archivo a la consola.
</secretos>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate 5a — flip `CLASIFICACION_ESCALERA=1` + corrida de humo acotada</name>
  <files>.env, .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md (§ Veredicto de gates)
packages/cruces/src/clasificar-fichas-cli.ts:202-245 y el bloque isMain final (flags + logs exactos)
  </read_first>
  <action>
Verificar la precondición por grep del veredicto de gates. Si no procede, aplicar la rama de cierre honesto y terminar.

Flip (dos actos de operador, ambos previos y ambos citados: **autorización del flip verbatim el 2026-07-27** — "Flip autorizado… tras shadow-eval verde y con rollback-by-config" — y **checkpoint de provisión de keys Workers AI cerrado en sesión el 2026-07-28** — "Sí — proceder con gates y flip"; reusar la cita literal que 120-01 dejó escrita en `## Gate 1` del registro, tras gates verdes): APPEND de la línea `CLASIFICACION_ESCALERA=1` a `.env` — append idempotente, sin reescribir el archivo, sin leer ni imprimir las demás líneas. `.env` NO se commitea y `.env.example` NO se toca (ya tiene el placeholder desde 109; el guard env-example queda verde sin cambios).

Corrida de humo ACOTADA — pocas fichas, sin escribir en DB, respetando el rate-limit del provider:

  set -a; source .env; set +a
  pnpm --filter @obs/cruces exec tsx src/clasificar-fichas-cli.ts --limite 3 --dry-run

Nota de ejecución: `pnpm --filter … exec` mueve el cwd a `packages/cruces` (gotcha v8.1/119-01); este CLI lee `process.env` directamente, por eso el `source .env` desde la raíz es lo que lo alimenta — no depende del cwd. Timeout holgado: la corrida hace llamadas LLM reales secuenciales.

Verificar en la salida la línea `cruces-fichas: provider=tiered:granite→deepseek (CLASIFICACION_ESCALERA=1)` y la línea final `cruces-fichas DRY-RUN: procesados=N asignados=… abstenidos=…` con **N > 0**: `procesados=0` significa que no hubo llamadas reales al LLM y el humo NO es concluyente (el provider se resolvió pero nunca se ejerció). Causa típica: la línea `cruces-fichas: SUPABASE_URL ausente → dry-run sin lectura DB` (nombre exacto de la variable: `SUPABASE_URL`) — sin URL no se cargan fichas. Si aparece esa línea o N==0, registrar `humo NO concluyente` con la causa y resolver la lectura (exportar `SUPABASE_URL`/`SUPABASE_API_URL` desde `.env`) antes de declarar PASS. Si en cambio aparece el fallback `…WORKERS_AI_API_TOKEN o CLOUDFLARE_ACCOUNT_ID ausentes → fallback a DeepSeek (Pitfall 2)`, el flip NO está efectivo: revertir la línea de `.env`, registrar el hallazgo y cerrar honesto sin flip.

Escribir `## Gate 5 — Flip + humo` en el registro con: las dos fechas de autorización (flip 2026-07-27 / keys 2026-07-28) citadas, la línea exacta añadida a `.env` (es un nombre y el literal `1`, no un secreto), comando de humo verbatim, hora, la línea del provider observada, el resumen DRY-RUN, y `VEREDICTO: PASS|REVERTIDO`.
  </action>
  <acceptance_criteria>
- `grep -c "^CLASIFICACION_ESCALERA=1" .env` == 1 (exactamente una, sin duplicar)
- La salida de la corrida de humo contiene `provider=tiered:granite` y NO contiene `fallback a DeepSeek (Pitfall 2)`
- El resumen final trae `procesados=N` con N > 0 y la salida NO contiene `SUPABASE_URL ausente → dry-run sin lectura DB` (si N==0, el registro dice `humo NO concluyente` y el veredicto no puede ser PASS)
- Exit code del CLI de humo == 0
- `grep -c "^## Gate 5 — Flip" 120-FLIP-RECORD.md` == 1 y la sección contiene `provider=tiered:granite`
- La sección Gate 5 cita ambas fechas: `2026-07-27` (autorización del flip) y `2026-07-28` (checkpoint de keys cerrado)
- `git diff --exit-code .env.example` == 0 (sin cambios)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const env=fs.readFileSync('.env','utf8').split(/\r?\n/).filter(l=>/^CLASIFICACION_ESCALERA=1$/.test(l)).length;if(env!==1)throw new Error('CLASIFICACION_ESCALERA=1 lineas='+env);const r=fs.readFileSync('.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md','utf8');const g=r.split('## Gate 5')[1]||'';if(!/provider=tiered:granite/.test(g))throw new Error('registro sin log de escalera');const m=g.match(/procesados=(\d+)/);if(!m)throw new Error('registro sin resumen DRY-RUN');if(Number(m[1])===0)throw new Error('humo NO concluyente: procesados=0');if(/SUPABASE_URL ausente/.test(g))throw new Error('humo sin lectura DB');console.log('gate5a ok')"</automated>
  </verify>
  <done>`.env` tiene el flip una sola vez, la corrida de humo probó en vivo que el provider resuelto es la escalera, y el registro lo documenta sin secretos.</done>
</task>

<task type="auto">
  <name>Task 2: Gate 5b — rollback inverso re-probado en vivo y restauración del flip</name>
  <files>.env, .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
packages/cruces/src/clasificar-fichas-cli.ts:210-216 (rama 2: log `cruces-fichas: provider=deepseek (default incumbente)`)
.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md (§ Gate 5)
  </read_first>
  <action>
Probar el rollback SOBRE el sistema ya flipeado (SC#3 exige que apagar devuelva el incumbente byte-idéntico, sin migración ni deploy):

1. Quitar de `.env` la línea `CLASIFICACION_ESCALERA=1` (edición dirigida a esa línea; no reescribir el resto).
2. Correr la misma corrida acotada:
   `set -a; source .env; set +a` seguido de
   `pnpm --filter @obs/cruces exec tsx src/clasificar-fichas-cli.ts --limite 3 --dry-run`
   y verificar el log `cruces-fichas: provider=deepseek (default incumbente)` y la AUSENCIA de `provider=tiered`.
   Cautela: el shell ya tiene la var exportada de la task anterior — abrir la corrida con `unset CLASIFICACION_ESCALERA` antes del `source` para que el entorno refleje el `.env` real, si no la prueba sería un falso negativo.
3. Volver a dejar `CLASIFICACION_ESCALERA=1` en `.env` (estado final de la fase = escalera ENCENDIDA) y confirmar con una tercera corrida acotada que el log vuelve a ser `provider=tiered:granite→deepseek`.

Registrar `## Gate 5b — Rollback inverso re-probado` con los tres comandos, sus horas, la línea de provider observada en cada uno, y la declaración de que el rollback es una sola línea de `.env` — sin migración, sin deploy, sin código.
  </action>
  <acceptance_criteria>
- El registro documenta las tres corridas con, en orden, `provider=tiered:granite`, `provider=deepseek (default incumbente)`, `provider=tiered:granite`
- Estado final: `grep -c "^CLASIFICACION_ESCALERA=1" .env` == 1
- Los tres CLIs terminaron con exit 0
- Gate anti-secreto sobre el registro == 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');if(fs.readFileSync('.env','utf8').split(/\r?\n/).filter(l=>/^CLASIFICACION_ESCALERA=1$/.test(l)).length!==1)throw new Error('estado final del flip incorrecto');const g=(fs.readFileSync('.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md','utf8').split('## Gate 5b')[1]||'');if(!/provider=deepseek \(default incumbente\)/.test(g))throw new Error('falta evidencia del rollback');if(!/provider=tiered:granite/.test(g))throw new Error('falta evidencia de la restauracion');console.log('gate5b ok')"</automated>
  </verify>
  <done>Rollback probado en vivo y escalera restaurada; el registro prueba el ciclo ON→OFF→ON con logs reales.</done>
</task>

<task type="auto">
  <name>Task 3: Guards intocables + suite + tsc + cierre del registro</name>
  <files>.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
packages/llm/src/integ-scope-guard.test.ts, packages/llm/src/provider-guard.test.ts, packages/llm/src/tiered-scope-guard.test.ts — los tres guards viven en `@obs/llm`, NO en `@obs/cruces` (verificado por `ls`). Congelan que la escalera no alcanza adjudicación de identidad ni extracción strict-schema.
app/lib/env-example-guard.test.ts — paquete `app`, ruta relativa al paquete `lib/env-example-guard.test.ts`.
  </read_first>
  <action>
Correr los guards de régimen individualmente (sin env-gates LIVE, offline):

  pnpm --filter @obs/llm exec vitest run src/integ-scope-guard.test.ts src/provider-guard.test.ts src/tiered-scope-guard.test.ts
  pnpm --filter app exec vitest run lib/env-example-guard.test.ts

Luego el cierre general: suite de `@obs/cruces` + `@obs/llm` y `tsc -b` (typecheck de paquetes y raíz). CERO cambio de código en esta fase — si algún guard muerde, es una regresión real y hay que detenerse y reportarla, no relajar el guard.

Cerrar `120-FLIP-RECORD.md` con:
- `## Gate 6 — Guards y suite`: comandos, conteos de tests, exit codes.
- `## Estado final CRON-03`: `ESCALERA ENCENDIDA EN CLASIFICACIÓN` (o `CIERRE HONESTO SIN FLIP` con causa), la constancia de que adjudicación de identidad y extracción strict-schema NO fueron tocadas, y la nota para 121/125: `CLASIFICACION_ESCALERA` NO pertenece a la familia `*_PUBLIC_ENABLED` y su encendido está autorizado con precondiciones cumplidas y documentadas aquí.
- Nota de alcance: clasificación no corre en ningún cron de CI, el flip vive en el `.env` local del operador; esta fase NO creó cron ni GH secret.
  </action>
  <acceptance_criteria>
- Los tres guards de `@obs/llm` + el env-example de `app` verdes (exit 0)
- Suite `@obs/cruces` y `@obs/llm` verdes; `tsc -b` exit 0
- `grep -c "^## Estado final CRON-03" 120-FLIP-RECORD.md` == 1, con exactamente uno de `ESCALERA ENCENDIDA EN CLASIFICACIÓN` / `CIERRE HONESTO SIN FLIP`
- El registro menciona explícitamente que adjudicación y extracción permanecen intocadas
- Gate anti-secreto sobre el registro completo == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/llm exec vitest run src/integ-scope-guard.test.ts src/provider-guard.test.ts src/tiered-scope-guard.test.ts && pnpm --filter app exec vitest run lib/env-example-guard.test.ts</automated>
  </verify>
  <done>Guards, suite y tsc verdes; registro cerrado con el estado final de CRON-03 listo para 121 y 125.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` local → runtime del CLI | el flip y las keys viven aquí; nunca en git ni en logs |
| CLI → Workers AI / DeepSeek | texto público de fichas; sin RUT/PII (telemetría payload-free 109) |
| CLI → Supabase | la corrida de humo es `--dry-run`: lee, no escribe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05 | Elevation of Privilege | flip de régimen | mitigate | el flip está autorizado verbatim por el operador Y gated por los gates 2-4 verificables por grep; sin veredicto verde el plan no ejecuta |
| T-120-06 | Tampering | tareas intocables (adjudicación/extracción) | mitigate | integ-scope-guard + provider-guard + tiered-scope-guard corridos al cierre, sin relajarlos |
| T-120-07 | Information Disclosure | `.env` y registro | mitigate | append dirigido sin volcar el archivo; regex anti-secreto sobre el registro |
| T-120-08 | Denial of Service | rate-limit del provider | mitigate | humo acotado a `--limite 3`, secuencial, sin reintentos en ráfaga |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | CERO paquete nuevo (tsx y vitest ya están en el monorepo) |
</threat_model>

<verification>
- `grep -c "^CLASIFICACION_ESCALERA=1" .env` == 1 al cierre.
- Ciclo ON→OFF→ON evidenciado con logs reales del CLI.
- Guards + suite + tsc verdes; `.env.example` sin diff.
- Ningún valor de secreto en el registro.
</verification>

<success_criteria>
- ROADMAP SC#3 (rollback probado sin migración ni deploy), SC#4 (flip solo tras el checkpoint de keys, agente nunca carga la key) y SC#5 (adjudicación + extracción intocadas) cerrados con evidencia.
- CRON-03 con estado final declarado en `120-FLIP-RECORD.md`.
</success_criteria>

<output>
Create `.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-02-SUMMARY.md` when done
</output>
