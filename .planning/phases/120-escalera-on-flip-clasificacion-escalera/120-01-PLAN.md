---
phase: 120-escalera-on-flip-clasificacion-escalera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md
autonomous: true
requirements: [CRON-03]

must_haves:
  truths:
    - "El drift canary corre LIVE y confirma que el modelo servido es @cf/ibm-granite/granite-4.0-h-micro (el del veredicto full-40); un mismatch invalida el veredicto y aborta el flip (SC#2)"
    - "La shadow-eval Granite vs DeepSeek corre LIVE sobre los 10 casos GOLDEN_SET_GATE y su veredicto queda documentado ANTES de cualquier flip; no-verde ⇒ no hay flip (SC#1)"
    - "El rollback-by-config queda probado ANTES del flip: sin CLASIFICACION_ESCALERA el provider resuelto es DeepSeek incumbente byte-idéntico (SC#3)"
    - "Ningún comando imprime, transcribe ni registra valores de secretos"
    - "El cierre honesto sin flip es un resultado VÁLIDO y queda documentado con la salida del gate que falló"
  artifacts:
    - path: ".planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md"
      provides: "Registro gate-por-gate (comando, salida sin secretos, veredicto, hora) consumido por 121 y 125"
      contains: "## Gate 2 — Drift canary"
  key_links:
    - from: "120-FLIP-RECORD.md"
      to: "packages/cruces/src/drift-canary.test.ts"
      via: "comando verbatim del header del test + su exit code"
      pattern: "CLASIFICACION_DRIFT_CHECK=1"
    - from: "120-FLIP-RECORD.md"
      to: "packages/cruces/src/shadow-eval.test.ts"
      via: "comando verbatim del header del test + su exit code"
      pattern: "CLASIFICACION_SHADOW_LIVE=1"
---

<objective>
Ejecutar, en ORDEN DURO y con abort-semántica, los gates 2–4 que preceden al flip de `CLASIFICACION_ESCALERA=1`: drift canary, shadow-eval LIVE y rollback-by-config probado. Abrir `120-FLIP-RECORD.md` y dejar cada gate con comando, salida relevante (sin secretos), veredicto y hora.

Purpose: el flip solo es legítimo si el modelo servido es el que produjo el veredicto full-40, si Granite acuerda con el incumbente en los 10 casos gate, y si apagar la escalera devuelve el comportamiento DeepSeek byte-idéntico. Sin eso, el cierre honesto SIN flip es el resultado correcto.
Output: `120-FLIP-RECORD.md` con Gates 2, 3 y 4 resueltos y un veredicto explícito de si el Plan 02 (flip) procede o no.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-CONTEXT.md

<read_first>
- `packages/cruces/src/drift-canary.test.ts` (líneas 1-45) — el header documenta el comando LIVE exacto y el modelo pinneado `@cf/ibm-granite/granite-4.0-h-micro` (fecha 2026-07-27). El gate es `CLASIFICACION_DRIFT_CHECK === "1"` + `WORKERS_AI_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`; sin ellos el test hace skip LIMPIO (verde vacío) — un skip NO es un gate aprobado.
- `packages/cruces/src/shadow-eval.test.ts` (líneas 1-60) — bloque OFFLINE (siempre corre, prueba el rollback) + bloque LIVE gate `CLASIFICACION_SHADOW_LIVE=1`, 10 casos `GOLDEN_SET_GATE`, secuencial con delay 2-3s.
- `packages/cruces/src/clasificar-fichas-cli.ts:202-245` — `resolverProvider`: rama 2 (sin `CLASIFICACION_ESCALERA=1` → `DeepSeekProvider`, log `provider=deepseek (default incumbente)`), rama 3 (con `=1` + keys → tiered, log `provider=tiered:granite→deepseek`), fallback Pitfall 2 si las keys están vacías.
</read_first>

<secretos>
REGLA DURA de toda la fase: los comandos LIVE consumen las keys desde `.env` exportándolas al entorno con
`set -a; source .env; set +a`
y NUNCA se imprime, ecoa, cat-ea, grep-ea por valor ni se transcribe ninguna key. El registro documenta NOMBRES de variables, jamás valores. Antes de cerrar, un gate anti-secreto verifica que el registro no contiene valores.
</secretos>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate 2 — drift canary LIVE (aborta todo si hay mismatch)</name>
  <files>.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
packages/cruces/src/drift-canary.test.ts (header 1-45, comando y modelo pinneado)
.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-CONTEXT.md (orden DURO, paso 1 CERRADO)
  </read_first>
  <action>
El paso 1 (checkpoint operador de keys Workers AI) está CERRADO por el operador el 2026-07-28 (respuesta verbatim "Sí — proceder con gates y flip", registrada en 120-CONTEXT.md). NO re-preguntar, NO abrir checkpoint humano.

Crear `120-FLIP-RECORD.md` con encabezado (fase, fecha, autorización verbatim del operador citada desde 120-CONTEXT.md, y la nota de que ningún valor de secreto aparece en el documento) y una sección `## Gate 1 — Checkpoint operador (keys Workers AI)` marcada CERRADO con la cita.

Luego correr el gate 2, exportando `.env` al entorno sin imprimirlo, desde la raíz del repo:

  set -a; source .env; set +a
  CLASIFICACION_DRIFT_CHECK=1 pnpm --filter @obs/cruces exec vitest run src/drift-canary.test.ts

Capturar exit code y la salida. Escribir la sección `## Gate 2 — Drift canary` con: comando verbatim, hora (ISO local), salida relevante RECORTADA (líneas de vitest y del assert de provenance — jamás el token ni el ACCOUNT_ID; si el ACCOUNT_ID aparece interpolado en alguna URL de la salida, redactarlo como `<ACCOUNT_ID>`), y el veredicto.

Semántica de abort (per D-orden-DURO paso 2). ATENCIÓN al falso verde: el test usa `describe`-gate + `it.skipIf`, así que sin las env vars vitest reporta `1 skipped` CON exit code 0 — un exit 0 NO basta:
- vitest reporta `1 passed` (y `0 skipped`) + el assert de provenance confirma modelo servido == `@cf/ibm-granite/granite-4.0-h-micro` → veredicto PASS, continuar.
- Cualquier `skipped > 0` (o `0 passed`) → NO es PASS: es "gate no ejercido"; tratarlo como FAIL de gate y detener.
- Mismatch de modelo, o 401 del token pese a la confirmación del operador, o cualquier exit ≠ 0 → veredicto ABORT: escribir `**VEREDICTO DE FASE: CIERRE HONESTO SIN FLIP**` en el registro con la causa exacta y la salida del gate; anotar que el veredicto full-40 queda invalidado si la causa fue mismatch de modelo (dato que 121 consume); si la causa fue 401, anotar "re-checkpoint de keys documentado, no re-preguntado en sesión". En ese caso NO ejecutar las Tasks 2 y 3 (dejar sus secciones escritas como `NO EJECUTADO — abortado por Gate 2`) y terminar el plan como ÉXITO (cierre honesto es resultado VÁLIDO).
  </action>
  <acceptance_criteria>
- `grep -c "^## Gate 2 — Drift canary" 120-FLIP-RECORD.md` == 1
- La sección Gate 2 contiene la línea de comando literal `CLASIFICACION_DRIFT_CHECK=1 pnpm --filter @obs/cruces exec vitest run src/drift-canary.test.ts`
- La sección Gate 2 contiene exactamente uno de: `VEREDICTO: PASS`, `VEREDICTO: ABORT`
- La sección Gate 2 transcribe el conteo de vitest; PASS exige `1 passed` y `skipped` == 0 (un `1 skipped` con exit 0 es FAIL de gate, no PASS)
- Si PASS: el registro cita el modelo servido == `@cf/ibm-granite/granite-4.0-h-micro`
- Gate anti-secreto: `grep -Ec "WORKERS_AI_API_TOKEN=[^ ]|DEEPSEEK_API_KEY=[^ ]|Bearer [A-Za-z0-9_-]{10,}" 120-FLIP-RECORD.md` == 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md','utf8');const g=t.split('## Gate 2')[1]||'';if(!/CLASIFICACION_DRIFT_CHECK=1 pnpm --filter @obs\/cruces exec vitest run src\/drift-canary.test.ts/.test(g))throw new Error('falta comando verbatim');if(!/VEREDICTO: (PASS|ABORT)/.test(g))throw new Error('falta veredicto');if(/Bearer [A-Za-z0-9_-]{10,}/.test(t))throw new Error('posible secreto en el registro');console.log('gate2 ok')"</automated>
  </verify>
  <done>Gate 2 ejecutado y registrado con veredicto PASS o ABORT; si ABORT, el registro declara el cierre honesto sin flip y las Tasks 2-3 quedan marcadas como no ejecutadas.</done>
</task>

<task type="auto">
  <name>Task 2: Gate 3 — shadow-eval LIVE Granite vs DeepSeek (10 casos gate)</name>
  <files>.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
packages/cruces/src/shadow-eval.test.ts (header 1-60: comando LIVE, 10 casos GOLDEN_SET_GATE, secuencial delay 2-3s)
.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md (Gate 2 debe estar PASS)
  </read_first>
  <action>
Precondición dura: solo ejecutar si el Gate 2 quedó `VEREDICTO: PASS`. Si quedó ABORT, no correr nada.

Correr el gate 3 desde la raíz del repo, sin imprimir secretos:

  set -a; source .env; set +a
  CLASIFICACION_SHADOW_LIVE=1 pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts

Es secuencial con delay 2-3s por caso (rate-limit LOCKED de Workers AI/DeepSeek) — no paralelizar, no reducir el delay, no reintentar en ráfaga. Dar timeout holgado a la invocación.

Escribir `## Gate 3 — Shadow-eval LIVE (Granite vs DeepSeek)` con: comando verbatim, hora, salida relevante recortada (conteo de tests, y el reporte de acuerdo/desacuerdo por caso que el test imprime a console.log — sector_codigo salida-a-salida, sin payloads de secretos), y el veredicto.

Semántica de abort (paso 3 del orden DURO): verde (exit 0 con los tests LIVE efectivamente ejecutados, no skipped) → `VEREDICTO: PASS`, el flip procede. Cualquier otra cosa (fallo de aserción de acuerdo, error de red, skip) → `VEREDICTO: NO-VERDE`; escribir `**VEREDICTO DE FASE: CIERRE HONESTO SIN FLIP**` con la salida exacta del gate, marcar la Task 3 y todo el Plan 02 como no ejecutados, y terminar el plan como ÉXITO.
  </action>
  <acceptance_criteria>
- `grep -c "^## Gate 3 — Shadow-eval LIVE" 120-FLIP-RECORD.md` == 1
- Contiene el comando literal `CLASIFICACION_SHADOW_LIVE=1 pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts`
- Contiene exactamente uno de `VEREDICTO: PASS` / `VEREDICTO: NO-VERDE`
- Si PASS: el registro reporta el acuerdo sobre los 10 casos `GOLDEN_SET_GATE`
- Gate anti-secreto (mismo regex de Task 1) == 0 sobre el registro completo
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md','utf8');const g=t.split('## Gate 3')[1]||'';if(!/Gate 2/.test(t))throw new Error('falta Gate 2');if(!/VEREDICTO: (PASS|NO-VERDE|ABORT)/.test(g)&&!/NO EJECUTADO/.test(g))throw new Error('Gate 3 sin veredicto ni marca de no-ejecutado');console.log('gate3 ok')"</automated>
  </verify>
  <done>Gate 3 registrado con veredicto PASS o NO-VERDE (o marcado NO EJECUTADO si Gate 2 abortó).</done>
</task>

<task type="auto">
  <name>Task 3: Gate 4 — rollback-by-config probado ANTES del flip</name>
  <files>.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md</files>
  <read_first>
packages/cruces/src/shadow-eval.test.ts (bloque OFFLINE, líneas 34-60: sin CLASIFICACION_ESCALERA → DeepSeekProvider; con =1 + keys → TieredProvider; keys vacías → fallback)
packages/cruces/src/clasificar-fichas-cli.ts:202-245 (resolverProvider, ramas 2 y 3)
  </read_first>
  <action>
Precondición: Gates 2 y 3 en PASS. Este gate se corre ANTES del flip por diseño — probar la red de seguridad antes de necesitarla.

Correr, sin ningún env-gate LIVE (offline puro, sin keys):

  pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts src/clasificar-fichas-cli.test.ts

Verificar que el bloque OFFLINE de shadow-eval y el test del CLI aseveran las tres ramas de `resolverProvider`: sin la env var → `DeepSeekProvider` (incumbente byte-idéntico), `=1` + keys → `TieredProvider`, `=1` con key vacía → fallback DeepSeek (Pitfall 2).

Escribir `## Gate 4 — Rollback-by-config probado (pre-flip)` con comando verbatim, hora, conteo de tests pasados, la enumeración de las tres ramas cubiertas y `VEREDICTO: PASS|FAIL`. Declarar explícitamente que el rollback NO requiere migración ni deploy especial (es una línea de `.env`), que es lo que exige SC#3.

Cerrar el registro con una sección `## Veredicto de gates` que diga `GATES 2-4 VERDES — EL FLIP PROCEDE (Plan 02)` o, si algún gate falló, `CIERRE HONESTO SIN FLIP` con la causa. Esa línea es la que el Plan 02 lee como precondición.
  </action>
  <acceptance_criteria>
- `grep -c "^## Gate 4 — Rollback-by-config probado" 120-FLIP-RECORD.md` == 1
- El registro contiene exactamente una línea `## Veredicto de gates` seguida de `GATES 2-4 VERDES — EL FLIP PROCEDE` o `CIERRE HONESTO SIN FLIP`
- El comando offline corre con exit 0 y >0 tests ejecutados
- Gate anti-secreto == 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts src/clasificar-fichas-cli.test.ts && node -e "const t=require('fs').readFileSync('.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md','utf8');const h=(t.match(/^## Veredicto de gates\s*$/gm)||[]).length;if(h!==1)throw new Error('secciones Veredicto de gates='+h);const b=t.split(/^## Veredicto de gates\s*$/m)[1]||'';const ok=/GATES 2-4 VERDES — EL FLIP PROCEDE/.test(b),ko=/CIERRE HONESTO SIN FLIP/.test(b);if(ok===ko)throw new Error('veredicto de gates ausente o ambiguo');console.log('veredicto de gates ok')"</automated>
  </verify>
  <done>Gate 4 registrado; el registro declara un veredicto de gates inequívoco que el Plan 02 consume como precondición.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Workers AI / DeepSeek | salen prompts del golden set (texto público de fichas); vuelven completions |
| `.env` → proceso hijo | las keys cruzan como env vars al vitest; jamás a disco ni a logs |
| proceso → `120-FLIP-RECORD.md` | salida de comandos que podría arrastrar tokens en URLs/headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | 120-FLIP-RECORD.md | mitigate | salida recortada + redacción de `<ACCOUNT_ID>` + gate anti-secreto por regex en acceptance de cada task |
| T-120-02 | Information Disclosure | logs de shell | mitigate | `set -a; source .env; set +a` — nunca `echo`/`cat` de `.env` ni de variables |
| T-120-03 | Spoofing | endpoint Workers AI | mitigate | drift canary compara el `model` servido contra el pinneado; mismatch = ABORT |
| T-120-04 | Tampering | payloads al LLM | accept | solo texto público de fichas del golden set; RUT/PII jamás cruza (telemetría payload-free 109) |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | CERO paquete nuevo en esta fase — toda la maquinaria existe desde 109-03 |
</threat_model>

<verification>
- Los tres gates corridos en orden, cada uno registrado con comando + hora + veredicto.
- Ningún valor de secreto en el registro (regex gate == 0).
- `## Veredicto de gates` presente y unívoco.
- Un cierre honesto sin flip es un resultado VÁLIDO del plan, no un fallo.
</verification>

<success_criteria>
- ROADMAP SC#1 (shadow-eval LIVE antes del flip, documentada), SC#2 (drift canary), SC#3 (rollback probado) quedan evidenciados en `120-FLIP-RECORD.md`.
- El Plan 02 tiene una precondición legible por grep para proceder o no.
</success_criteria>

<output>
Create `.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-01-SUMMARY.md` when done
</output>
