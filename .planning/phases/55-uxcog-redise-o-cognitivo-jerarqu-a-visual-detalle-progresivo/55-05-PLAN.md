---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/agenda/page.tsx
  - app/app/agenda/page.test.tsx
  - app/components/red/red-graph.tsx
  - app/components/red/red-graph.test.tsx
  - app/components/red/nodo-parlamentario.tsx
  - app/app/red/page.tsx
  - app/app/red/page.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "La agenda agrupa por día → comisión con jerarquía tipográfica clara y bloques colapsables por día; los cross-links a boletín quedan intactos"
    - "/red con ?seed= abre centrado en el vecindario inmediato del seed (ego-network), NO fitView global de 136 nodos"
    - "El nodo seed se marca de forma sobria (no-ranking) en el grafo"
    - "/red sin seed conserva el fitView global + nota de uso"
    - "Cero query nueva: agrupación agenda es presentacional; /red usa solo props de @xyflow/react ya disponibles"
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "grafo con framing ego del seed (fitViewOptions.nodes) + marca de seed"
      contains: "fitViewOptions"
    - path: "app/app/agenda/page.tsx"
      provides: "agenda agrupada día→comisión con días colapsables"
      contains: "comision"
  key_links:
    - from: "app/app/red/page.tsx"
      to: "app/components/red/red-graph.tsx"
      via: "pasa seedId (?seed=) → RedGraph"
      pattern: "seedId"
    - from: "app/components/red/red-graph.tsx"
      to: "@xyflow/react"
      via: "fitViewOptions.nodes = ego (seed + vecinos 1-hop)"
      pattern: "fitViewOptions"
---

<objective>
Aplicar el rediseño cognitivo a las dos superficies secundarias: (a) **agenda** (11.606px) — agrupar por día → comisión con jerarquía tipográfica y bloques colapsables por día (reusando `CarrilAccordion` existente y el `grupos` Map ya presente); (b) **/red** — vista inicial legible del ego-network del seed (centrar/zoom al vecindario del nodo vía `fitViewOptions.nodes`, marca sobria del seed), en vez del fitView global de 136 nodos. Sin dependencia de otras piezas del plan; corre en Wave 1.

Purpose: Success Criteria #3 (/red abre legible) y la parte de agenda del rediseño IA.
Output: agenda page re-agrupada + test; red-graph/nodo/page con framing ego + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<interfaces>
Reuso existente:
- `CarrilAccordion` (carril-accordion.tsx, F45): disclosure de cuerpo entero — el header (día) es el trigger. Fits para bloques de día colapsables.
- `agenda/page.tsx`: `CitacionesSection` ya agrupa por día (`grupos` Map keyed `fecha.slice(0,10)`, `<h3>` por día, línea 317-334); modo búsqueda ya agrupa por comisión; `SalaTableServer`.
- `@xyflow/react@12.11.0`: `fitViewOptions.nodes?: (Node|{id})[]` VERIFICADO en `@xyflow/system@0.0.77` (types/general.d.ts:150-161). Layout es grid determinista por cámara (NUNCA física).
- `red/page.tsx`: server, `export const dynamic="force-dynamic"` (load-bearing), gate `netPublicEnabled` PRIMERA sentencia; `?seed=` ya parseado; `subgrafo_red(p_id, p_depth:1)` YA seed-scoped (ego). `NodoParlamentario` (grid por cámara, líneas 95-114).
</interfaces>

<resolved_decisions>
- /red usage-note copy LOCKED: seed → "Centrado en {nombre} y su vecindario inmediato."; sin seed → "Mostrando toda la red. Abre una ficha y usa 'Ver relaciones' para centrar el grafo en un parlamentario."
- Solo props de @xyflow/react ya disponibles (fitView sobre nodos filtrados, `nodes`, `minZoom`); cero física nueva, cero dep nueva.
</resolved_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agenda — agrupar día → comisión con días colapsables</name>
  <files>app/app/agenda/page.tsx, app/app/agenda/page.test.tsx</files>
  <read_first>
    - `app/app/agenda/page.tsx` COMPLETO (secciones id resultados/citaciones/tabla-sala; `CitacionesSection` 276+; `grupos` Map por día 317-334; modo búsqueda agrupa por comisión 209-231)
    - `app/components/carril-accordion.tsx` (disclosure cuerpo-entero, header=trigger — fits para día)
    - `app/app/agenda/page.test.tsx` — forma de test a extender
    - `55-UI-SPEC.md` §Per-Surface "/agenda"; `55-PATTERNS.md` §"agenda/page.tsx (MODIFY)" (Assumption A4: sub-grupo presentacional, sin query)
  </read_first>
  <action>
    Dentro de `CitacionesSection` (modo semana), agregar un sub-agrupamiento por comisión DENTRO de cada día: sobre el `grupos` Map por día ya existente, construir un segundo nivel keyed por comisión (jerarquía tipográfica clara: día `<h3>` → comisión sub-heading → filas). Envolver cada bloque de día en un `CarrilAccordion` (el `<h3>` del día es el trigger; cuerpo colapsable). Mantener `SalaTableServer` y los cross-links a boletín INTACTOS. Es agrupación PRESENTACIONAL sobre datos ya fetched — CERO query nueva (Assumption A4). Preservar `scroll-mt`/anclas existentes; no mover `mt-12`/`mt-8` de las secciones.
    Test `agenda/page.test.tsx`: extender para aseverar (1) agrupación día→comisión (sub-heading de comisión dentro de un día), (2) bloque de día colapsable (trigger presente, cuerpo forceMount en DOM), (3) cross-link a boletín preservado. Mockear la lectura como en el test existente.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run "app/agenda/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `CitacionesSection` agrupa día→comisión (segundo nivel presentacional sobre `grupos`)
    - cada día es un `CarrilAccordion` colapsable (header día = trigger)
    - cross-links a boletín intactos; CERO query nueva (grep: sin RPC/`.from` nuevos)
    - test verde con las 3 aserciones
  </acceptance_criteria>
  <done>Agenda agrupada día→comisión con días colapsables, cross-links intactos; test verde.</done>
</task>

<task type="auto">
  <name>Task 2: /red — framing ego del seed + marca de nodo seed</name>
  <files>app/components/red/red-graph.tsx, app/components/red/red-graph.test.tsx, app/components/red/nodo-parlamentario.tsx, app/app/red/page.tsx, app/app/red/page.test.tsx</files>
  <read_first>
    - `app/components/red/red-graph.tsx` COMPLETO (props 74-77; layout grid por cámara 95-114 — NUNCA física; bloque `<ReactFlow>` 293-309; empty-states 164-186, 277-281; nota móvil 287-290)
    - `app/components/red/nodo-parlamentario.tsx` (para la marca de seed vía `data.esSeed`)
    - `app/app/red/page.tsx` COMPLETO (gate netPublicEnabled primera sentencia; `export const dynamic="force-dynamic"` load-bearing; `?seed=` parseado)
    - `55-RESEARCH.md` §Code Examples "/red ego framing" (líneas 319-333, API VERIFICADA) + §Pattern 5
    - `55-PATTERNS.md` §"red-graph.tsx + red/page.tsx (MODIFY)" (copy usage-note LOCKED)
    - `app/AGENTS.md` (leer `node_modules/next/dist/docs/` antes de tocar searchParams/routing)
  </read_first>
  <action>
    En `red-graph.tsx`: agregar prop `seedId?: string` a `RedGraphProps`. Sobre el `<ReactFlow>` existente, cuando hay `seedId`, framear el ego: `const egoIds = [seedId, ...aristasVisibles.flatMap(a => [a.a, a.b])].filter(Boolean)` y pasar `fitViewOptions={{ padding: 0.2, nodes: egoIds.map(id => ({ id })), minZoom: 0.2 }}` (API `fitViewOptions.nodes` VERIFICADA en la versión instalada). Conservar TODAS las props existentes (nodes, edges, nodeTypes, edgeTypes, proOptions) y el layout grid determinista por cámara (NUNCA física — anti-insinuación LOCKED). Marcar el nodo seed vía `data.esSeed` y en `nodo-parlamentario.tsx` renderizar una emphasis SOBRIA no-ranking (borde/énfasis discreto). Conservar empty-states y nota móvil. Sin seed: mantener el fitView global existente + la nota de uso.
    En `red/page.tsx`: pasar `seedId` (el `?seed=` ya parseado) a `<RedGraph seedId subgrafo />`. NO tocar el gate ni el orden (`netPublicEnabled` primera sentencia + `export const dynamic`). Renderizar la usage-note LOCKED según haya o no seed (copy exacto del UI-SPEC).
    Tests: extender `red-graph.test.tsx` para aseverar que con `seedId` se pasa `fitViewOptions.nodes` con el ego (seed+vecinos) y que el nodo seed lleva `data.esSeed`; extender `red/page.test.tsx` para aseverar el paso de `seedId` y la usage-note correcta (seed vs sin-seed).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run "components/red/red-graph.test.tsx" "app/red/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `RedGraphProps` incluye `seedId?`; con seed se construye `egoIds` y se pasa `fitViewOptions.nodes`
    - layout grid por cámara intacto (grep: sin simulación de física nueva)
    - nodo seed marcado vía `data.esSeed` con emphasis sobria no-ranking
    - `red/page.tsx`: gate + `export const dynamic` intactos; usage-note LOCKED (seed / sin-seed)
    - tests verdes (framing ego + seed marker + usage-note)
  </acceptance_criteria>
  <done>/red abre centrado en el ego-network del seed con marca sobria; sin seed conserva global + nota; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| gate netPublicEnabled → /red HTML | Gate primera sentencia + force-dynamic; OFF ⇒ ruta 404 (sin filtrar DOM). No se toca. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-12 | Elevation/Access Control | red/page.tsx | mitigate | Gate `netPublicEnabled` + `export const dynamic="force-dynamic"` preservados verbatim (STATE gotcha); solo se agrega framing cliente. |
| T-55-13 | Tampering (anti-insinuación) | red-graph seed marker / agenda | mitigate | Marca seed sobria no-ranking; layout grid determinista (no física); conteos/agrupación neutros; banned-vocab negative-match. |
| T-55-14 | Information Disclosure | agenda grouping | mitigate | Agrupación presentacional sobre datos ya fetched; sin query/`.from` nueva. |
| T-55-SC | Tampering | package installs | accept | CERO instalación (@xyflow ya instalado). |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, ≥594)
- `pnpm typecheck` (root)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7)
- banned-vocab negative-match verde
</verification>

<success_criteria>
Agenda agrupada día→comisión con días colapsables (cross-links intactos); /red abre en el ego-network del seed (fitViewOptions.nodes, seed marcado sobrio) y conserva global sin seed; cero query nueva, gate /red intacto. Suite ≥594, tsc limpio, lockdown-guard 7/7.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-05-SUMMARY.md` when done
</output>
