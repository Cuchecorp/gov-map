---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/styles/civic-tokens.css
  - app/app/globals.css
  - app/lib/use-scrollspy.ts
  - app/lib/use-scrollspy.test.ts
  - app/components/detalle-colapsable.tsx
  - app/components/detalle-colapsable.test.tsx
  - app/components/ficha-rail.tsx
  - app/components/ficha-rail.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "El token --accent-product-soft existe (light+dark) y produce la utilidad Tailwind bg-accent-product-soft sin doble-hsl"
    - "Un hook useScrollspy marca la sección actual vía IntersectionObserver (cero deps)"
    - "DetalleColapsable colapsa SOLO el detalle (children server), arranca CERRADO por defecto, mantiene el contenido SSR en el DOM (forceMount)"
    - "FichaRail renderiza un rail sticky con header, una entrada de nav por carril presente, conteo 3-estado honesto, caveat anti-causal 1×, y marca la sección activa"
    - "Ninguna isla nueva importa un *Section ni @/lib/supabase (contrato no-leak F45)"
  artifacts:
    - path: "app/lib/use-scrollspy.ts"
      provides: "hook IntersectionObserver → id activo"
      exports: ["useScrollspy"]
    - path: "app/components/detalle-colapsable.tsx"
      provides: "isla disclosure sobre children server, default cerrado, forceMount"
      exports: ["DetalleColapsable"]
    - path: "app/components/ficha-rail.tsx"
      provides: "isla rail sticky genérica (header slot + navEntries + caveat + scrollspy)"
      exports: ["FichaRail"]
    - path: "app/app/globals.css"
      provides: "registro @theme inline de --color-accent-product-soft"
      contains: "--color-accent-product-soft"
  key_links:
    - from: "app/components/ficha-rail.tsx"
      to: "app/lib/use-scrollspy.ts"
      via: "useScrollspy(ids) → activeId marca la entrada"
      pattern: "useScrollspy"
    - from: "app/components/detalle-colapsable.tsx"
      to: "@radix-ui/react-accordion"
      via: "AccordionPrimitive Root/Item/Content forceMount"
      pattern: "forceMount"
---

<objective>
Construir las PRIMITIVAS COMPARTIDAS del rediseño cognitivo (variante B "Informe con rail"): el token de color `--accent-product-soft`, el hook `useScrollspy`, la isla `DetalleColapsable` (disclosure que colapsa SOLO el detalle) y la isla genérica `FichaRail` (rail sticky + nav + scrollspy + caveat). Estas cuatro piezas son los contratos que consumen los planes de superficie (parlamentario 55-02/55-03, proyecto 55-04).

Purpose: Interface-first — definir los contratos antes de que los planes de wiring los consuman, evitando la exploración del codebase durante la ejecución. Todo tiene analog byte-level en el repo (F45 carril-accordion / parlamentario-resumen).
Output: 2 archivos de config (tokens), 1 hook + test, 2 islas cliente + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<resolved_decisions>
- Rail sticky offset: el GlobalHeader es NO-sticky (56px, scrollea). NO hacer el header sticky. El rail usa `sticky top-6` (24px) y las secciones `scroll-mt-6`. La `top-16` del UI-SPEC queda SUPERSEDED por esta decisión.
- Token gap: agregar `--accent-product-soft` con el MISMO patrón @theme inline de identity-warn (54-04), valor `hsl()` COMPLETO en :root/.dark, referenciado SIN hsl() envolvente. NUNCA doble-hsl.
</resolved_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token --accent-product-soft + hook useScrollspy</name>
  <files>app/app/styles/civic-tokens.css, app/app/globals.css, app/lib/use-scrollspy.ts, app/lib/use-scrollspy.test.ts</files>
  <read_first>
    - `app/app/styles/civic-tokens.css` (líneas 27-29 :root y 40-42 dark: patrón de token hsl() completo identity-warn)
    - `app/app/globals.css` (líneas 59-69: bloque `@theme inline` de identity-warn — el registro a copiar)
    - `55-RESEARCH.md` §Code Examples "Scrollspy hook" (líneas 296-317) y §Pitfall 2/4 (doble-hsl)
    - `55-PATTERNS.md` §"Token registration" y §"use-scrollspy.ts"
  </read_first>
  <action>
    En `civic-tokens.css` definir `--accent-product-soft: hsl(183 30% 93%);` en el bloque `:root` (junto a los tokens existentes) y `--accent-product-soft: hsl(183 30% 18%);` en el bloque `.dark` (valor legible en oscuro). En `globals.css`, DENTRO del bloque `@theme inline` existente (junto a `--color-identity-warn-bg`), agregar `--color-accent-product-soft: var(--accent-product-soft);` — referencia BARE (sin `hsl()` envolvente; doble-hsl = color inválido silenciosamente descartado, gotcha 54-04). Resultado: utilidad plana `bg-accent-product-soft` disponible, sin arbitrary values.
    Crear `app/lib/use-scrollspy.ts` como hook cliente (`"use client"`) que recibe `ids: string[]`, observa cada `document.getElementById(id)` con un único `IntersectionObserver` (`rootMargin: "-20% 0px -70% 0px"`, threshold 0), y devuelve el `id` de la sección que cruza el tercio superior (sort por `boundingClientRect.top`, primer visible). Cleanup con `obs.disconnect()`. Cero dependencias externas. Adoptar la referencia de 55-RESEARCH §Code Examples verbatim.
    Test `use-scrollspy.test.ts`: mockear `IntersectionObserver` global (capturar el callback), renderizar el hook con `renderHook` sobre ids conocidos, disparar el callback con una entrada `isIntersecting` y aseverar que el hook devuelve ese id.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/use-scrollspy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `civic-tokens.css` contiene `--accent-product-soft:` en :root Y en .dark, ambos con `hsl(` completo
    - `globals.css` `@theme inline` contiene la línea literal `--color-accent-product-soft: var(--accent-product-soft);` (sin `hsl(` envolvente)
    - grep confirma CERO `bg-[hsl(var(` nuevo en los archivos tocados
    - `use-scrollspy.ts` empieza con `"use client"`, exporta `useScrollspy`, usa `IntersectionObserver`, no importa ninguna lib externa
    - el test pasa (exit 0) y asevera el id activo devuelto tras un intersect mockeado
  </acceptance_criteria>
  <done>Token registrado (utilidad plana disponible) + hook scrollspy con test verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isla DetalleColapsable (disclosure inverso, default cerrado)</name>
  <files>app/components/detalle-colapsable.tsx, app/components/detalle-colapsable.test.tsx</files>
  <behavior>
    - Default CERRADO: sin interacción, el trigger dice "Ver detalle (N)" y el contenido está `data-[state=closed]:hidden` pero PRESENTE en el DOM (forceMount)
    - Click en el trigger → abre → el label cambia a "Ocultar detalle" y el contenido queda visible (data-state=open)
    - Los children (un server component pasado por la página) se renderizan dentro del Content en ambos estados (traceability + no lazy-fetch)
    - Source-scan: el archivo NO contiene `/Section/`, `/createServerSupabase/`, ni `/@\/lib\/supabase/`
  </behavior>
  <read_first>
    - `app/components/carril-accordion.tsx` (líneas 1-6, 8-19 contrato no-leak, 33-69 estructura Radix + forceMount) — el analog EXACTO
    - `app/components/carril-accordion.test.tsx` (líneas 11-25 fixture, 29-63 open/closed/toggle, 74-85 source-scan no-leak) — la forma del test a copiar
    - `55-RESEARCH.md` §Code Examples "Detail disclosure island" (líneas 271-294)
    - `55-UI-SPEC.md` §Copywriting Contract (fila "Secondary disclosure CTA") y §Progressive-Disclosure
  </read_first>
  <action>
    Crear `detalle-colapsable.tsx` como isla `"use client"` que reusa el contrato de `carril-accordion.tsx` con UNA inversión clave: el `<h2>` y la capa-1 NO viven aquí (los renderiza la página FUERA de la isla); esta isla envuelve SOLO el detalle. Firma: `DetalleColapsable({ n, children, defaultOpen? }: { n: number; children: React.ReactNode; defaultOpen?: boolean })` con `defaultOpen` = `false` por defecto (detalle arranca CERRADO — invierte la heurística F45 `abrePorDefecto`). Estructura Radix: `<AccordionPrimitive.Root type="single" collapsible defaultValue={defaultOpen ? "d" : undefined}>` → `Item value="d"` → `Header asChild` → `Trigger` con `className="group ... min-h-11 ..."` cuyo label es "Ver detalle ({n})" ↔ "Ocultar detalle" guiado por `group-data-[state=open]` (mostrar/ocultar dos spans, patrón carril-accordion) → `Content forceMount className={cn("... data-[state=closed]:hidden pt-4")}`{children}. Copiar el comentario-contrato no-leak (intención de líneas 8-19 del analog). NO importar ningún `*Section` ni `@/lib/supabase`.
    Test `detalle-colapsable.test.tsx`: espejo de `carril-accordion.test.tsx` — render con un child fixture; (1) default cerrado: trigger "Ver detalle (3)" presente, child en el DOM (forceMount) con `data-state=closed`; (2) click → "Ocultar detalle", `data-state=open`; (3) source-scan `readFileSync` del propio archivo → `not.toMatch(/Section/)`, `/createServerSupabase/`, `/@\/lib\/supabase/`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/detalle-colapsable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `detalle-colapsable.tsx` empieza con `"use client"` y exporta `DetalleColapsable`
    - `defaultOpen` por defecto = false (el `defaultValue` de Root es `undefined` sin prop)
    - usa `forceMount` en `AccordionPrimitive.Content` y `data-[state=closed]:hidden`
    - trigger tiene `min-h-11`; label "Ver detalle (" y "Ocultar detalle" presentes en el source
    - test verde: assert default-cerrado + forceMount-en-DOM + toggle + source-scan no-leak (3 patrones)
  </acceptance_criteria>
  <done>Isla disclosure inversa con test verde; contrato no-leak aseverado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Isla FichaRail genérica (sticky + nav + scrollspy + caveat)</name>
  <files>app/components/ficha-rail.tsx, app/components/ficha-rail.test.tsx</files>
  <behavior>
    - Renderiza un `<aside>` sticky (`sticky top-6`) con un slot `header` (ReactNode) arriba
    - Renderiza UNA entrada de nav por cada item de `navEntries` (link `#id`), con su conteo/estado ya serializado (nunca fabrica dígitos)
    - La entrada cuyo id == activeId (de useScrollspy) recibe el estado "actual": `bg-accent-product-soft` + borde izquierdo petróleo 2px + texto petróleo/600
    - La entrada de cruces muestra el marcador diamante `◆`
    - El caveat anti-causal aparece EXACTAMENTE 1× (texto LOCKED del UI-SPEC)
    - En móvil colapsa a barra superior compacta con nav horizontal scrolleable, sin perder conteos
    - Source-scan: NO importa `*Section` ni `@/lib/supabase`
  </behavior>
  <read_first>
    - `app/components/parlamentario-resumen.tsx` (`ChipConteo` líneas 34-52 render 3-estado, `ResumenView` 61-107 anchor-links, `construirChips` 120-162) — el prototipo del rail nav
    - `app/components/parlamentario-resumen.test.tsx` — la forma del test (entradas por carril presente, orden gate-aware)
    - `app/lib/use-scrollspy.ts` (Task 1) — el hook que marca la entrada activa
    - `55-UI-SPEC.md` §Layout Contract (rail), §Color (petróleo reservado: item 2 scrollspy-current, item 3 diamante cruces), §Copywriting (caveat rail LOCKED)
    - `55-PATTERNS.md` §"ficha-rail.tsx" (partes NEW y reuso)
  </read_first>
  <action>
    Crear `ficha-rail.tsx` como isla `"use client"` GENÉRICA (la consumen parlamentario y proyecto). Props: `{ header: React.ReactNode; navEntries: RailEntry[]; caveat: string }` donde `RailEntry = { id: string; label: string; count?: React.ReactNode; marker?: "diamante"; }`. Internamente: `const activeId = useScrollspy(navEntries.map(e => e.id))`. Render: `<aside className="... md:sticky md:top-6 md:self-start">` con `{header}`, luego `<nav>` con un `<a href={"#"+id} className="min-h-11 ...">` por entrada; la entrada activa (`id === activeId`) recibe `bg-accent-product-soft border-l-2 border-[color:var(--accent-product)] text-[color:var(--accent-product)] font-semibold` (usar utilidad de token, NO arbitrary color — `bg-accent-product-soft` de Task 1; el borde/texto petróleo usa el token util existente que ya emplea el repo). El marcador `◆` se antepone al label cuando `marker === "diamante"`. El conteo se renderiza tal cual llega (ya es 3-estado honesto, computado server-side; la isla NUNCA lo deriva). Caveat: `<p className="text-xs ...">{caveat}</p>` una sola vez al pie del rail. Responsive: en `< md` el aside es una barra superior (`overflow-x-auto` nav horizontal) que conserva labels+conteos. `focus-visible:outline-[var(--accent-product)]` en los links, `min-h-11`. NO importar secciones ni supabase.
    Test `ficha-rail.test.tsx`: render con navEntries fixture (incluyendo una entrada `marker:"diamante"` para cruces y conteos en los 3 estados); aseverar (1) una `<a href="#...">` por entrada, (2) el conteo honesto se muestra sin fabricar, (3) `◆` presente en la entrada de cruces, (4) el caveat aparece 1×, (5) source-scan no-leak (`/Section/`, `/@\/lib\/supabase/`). Para scrollspy-active: mockear `useScrollspy` (o IntersectionObserver) para forzar un activeId y aseverar la clase `bg-accent-product-soft` sobre esa entrada.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/ficha-rail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ficha-rail.tsx` empieza con `"use client"`, exporta `FichaRail`, importa `useScrollspy` de `@/lib/use-scrollspy`
    - usa `sticky top-6` (NO top-16) y `bg-accent-product-soft` para el estado activo
    - el caveat se renderiza exactamente 1× (grep del texto LOCKED en el test)
    - CERO arbitrary color value nuevo (`bg-[hsl(var(`) ; grid tracks geométricos permitidos
    - test verde con los 5 asserts + scrollspy-active
    - source-scan no-leak verde (no `/Section/`, no `/@\/lib\/supabase/`)
  </acceptance_criteria>
  <done>Rail genérico con nav gate-aware, scrollspy-active, caveat 1×, responsive; test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server RSC → client island | Las islas nuevas reciben datos server como props/children serializables; nunca importan el cliente Supabase (service_role, Camino A). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Information Disclosure | detalle-colapsable.tsx / ficha-rail.tsx | mitigate | Contrato no-leak F45: islas nunca importan `*Section` ni `@/lib/supabase`; aseverado por source-scan test en ambos archivos. |
| T-55-02 | Tampering | copy nuevo del rail/caveat | mitigate | Caveat = texto LOCKED del UI-SPEC verbatim; banned-vocab negative-match en la suite; React auto-escapa. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | CERO instalación de paquetes en esta fase (LOCKED); slopcheck N/A. |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, baseline 594 — puede crecer, nunca decrecer)
- `pnpm typecheck` (root, usar `references` no `paths` — gotcha 43-DEBT)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7 verde)
- banned-vocab negative-match verde
</verification>

<success_criteria>
Token `--accent-product-soft` registrado (utilidad plana), hook `useScrollspy` + isla `DetalleColapsable` (default cerrado, forceMount) + isla `FichaRail` (nav gate-aware, scrollspy, caveat 1×) construidos con tests verdes y contrato no-leak aseverado. Suite ≥594 verde, tsc limpio, lockdown-guard 7/7.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-01-SUMMARY.md` when done
</output>
