---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 04
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/components/capa1/tramitacion-stepper.tsx
  - app/components/capa1/tramitacion-stepper.test.tsx
  - app/components/estado-actual-block.tsx
  - app/components/estado-actual-block.test.tsx
  - app/components/timeline-view.tsx
  - app/components/timeline-view.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/app/proyecto/[boletin]/page.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "La ficha de proyecto abre con rail sticky (Dónde está / Tramitación / Votaciones / Lobby del período / Idea matriz / Similares)"
    - "La capa-1 de Tramitación es un stepper visual de etapas que ELEVA el '¿Dónde está hoy?' existente, con hitos clave siempre visibles"
    - "Los trámites repetitivos de urgencia se AGRUPAN en 1 línea con conteo ('N trámites de urgencia · ver todos') reusando la lógica de agrupación existente"
    - "La tramitación completa vive en un detalle colapsado; ningún hito se pierde"
    - "El nombre en lobby×tramitación sigue en texto plano no-enlazado (52-03)"
  artifacts:
    - path: "app/components/capa1/tramitacion-stepper.tsx"
      provides: "stepper de etapas capa-1 (hitos clave + urgencia agrupada)"
      exports: ["TramitacionStepper"]
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "página proyecto con rail + stepper + detalle colapsado"
      contains: "FichaRail"
  key_links:
    - from: "app/components/capa1/tramitacion-stepper.tsx"
      to: "app/components/timeline-view.tsx"
      via: "reusa construirItems/paresDeUrgencia/esRetiroUrgencia (agrupación existente)"
      pattern: "construirItems|paresDeUrgencia"
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "app/components/ficha-rail.tsx"
      via: "navEntries de las 6 secciones + DetalleColapsable sobre el timeline"
      pattern: "FichaRail"
---

<objective>
Rediseñar la ficha de proyecto (10.391px) con el mismo patrón "Informe con rail": rail sticky de 6 secciones, capa-1 de Tramitación = stepper visual de etapas que eleva el "¿Dónde está hoy?" existente + hitos CLAVE siempre visibles, trámites de urgencia repetitivos agrupados en 1 línea (reusando la agrupación YA existente en `timeline-view.tsx`), y la tramitación completa colapsada en detalle. Corre en paralelo con 55-02 (archivos disjuntos).

Purpose: Success Criteria #1/#2/#3 para la ficha de proyecto; F38 (cruces en proyecto) heredará este patrón de drill-down.
Output: nuevo stepper capa-1 + test, estado-actual-block/timeline-view extendidos, page.tsx re-compuesta + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<interfaces>
De 55-01: `FichaRail({ header, navEntries, caveat })`, `DetalleColapsable({ n, children, defaultOpen? })`, token `bg-accent-product-soft`.
Reuso existente (NO re-derivar):
- `timeline-view.tsx`: `esEventoUrgencia` (43-48), `esRetiroUrgencia` (56-58), `tipoUrgenciaKey` (65-69), `construirItems` (105-166), `paresDeUrgencia` (172-178), agrupación de corridas de urgencia ≥2 en 1 línea (198-205, 251-267), `fechaValida` (31-35).
- `estado-actual-block.tsx`: `derivarEstadoActual`, `urgenciaVigente` (51-74), `citacionVigente` (100-118), forma `EstadoActual` de campos opcionales (19-29), `fechaValida` (37-42).
- `leerFicha(boletin)` React.cache; secciones existentes con id `#timeline`, `#votaciones`, `#lobby-tramitacion`, `#idea-matriz`, `#similares`; `LobbyEnTramitacionSection` (nombre PLANO 52-03).
</interfaces>

<resolved_decisions>
- Rail `sticky top-6`; secciones `scroll-mt-6` (header NO-sticky).
- Hitos "clave" (mínimo): ingreso, cambios de etapa/comisión, informes de comisión, votaciones con desenlace, urgencia vigente. Agrupar SOLO lo inequívoco (si la heurística es ambigua, no agrupar).
- Copy línea agrupada LOCKED: "{N} trámites de urgencia · ver todos" (conteo neutro).
- Truncación/detalle: conservar el mecanismo server existente (?urgencias=<id>) dentro del disclosure; tramitación completa = detalle colapsado.
</resolved_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stepper de tramitación capa-1 (eleva estado-actual, reusa agrupación urgencia)</name>
  <files>app/components/capa1/tramitacion-stepper.tsx, app/components/capa1/tramitacion-stepper.test.tsx, app/components/estado-actual-block.tsx, app/components/estado-actual-block.test.tsx, app/components/timeline-view.tsx, app/components/timeline-view.test.tsx</files>
  <behavior>
    - TramitacionStepper renderiza un stepper visual de etapas (dónde está hoy elevado) con los hitos CLAVE siempre visibles
    - Las corridas de urgencia repetitivas ≥2 se muestran como 1 línea "{N} trámites de urgencia · ver todos" (neutro, sin verbo causal)
    - Reusa la lógica de agrupación existente de timeline-view (no re-implementa la heurística)
    - Omisión honesta: un hito no derivable se omite; fechas inválidas nunca se renderizan (fechaValida)
  </behavior>
  <read_first>
    - `app/components/timeline-view.tsx` COMPLETO (agrupación urgencia 43-69, 105-178, 198-267; fechaValida 31-35)
    - `app/components/estado-actual-block.tsx` COMPLETO (derivación estado/urgencia/citación, campos opcionales, fechaValida)
    - `app/components/timeline-view.test.tsx`, `app/components/estado-actual-block.test.tsx` — forma de test a extender
    - `55-UI-SPEC.md` §Per-Surface "/proyecto" (stepper + hitos clave + urgencia agrupada) + §Copywriting (línea urgencia LOCKED)
    - `55-PATTERNS.md` §"proyecto page.tsx" (Don't hand-roll grouping — reusar verbatim) + Pattern D/E
  </read_first>
  <action>
    Crear `capa1/tramitacion-stepper.tsx` exportando `TramitacionStepper` — vista PURA que recibe los eventos de tramitación ya fetched + el estado derivado. Render: un stepper visual de etapas (CSS sobre tokens; etapa actual destacada de forma sobria, NO petróleo salvo que sea afordancia de drill-down) elevando el "¿Dónde está hoy?" (reusar los derivadores de `estado-actual-block.tsx`: `urgenciaVigente`, `citacionVigente`, forma `EstadoActual`). Hitos CLAVE siempre visibles: ingreso, cambios de etapa/comisión, informes de comisión, votaciones con desenlace, urgencia vigente. Para las corridas de urgencia repetitivas, REUSAR `construirItems`/`paresDeUrgencia`/`esRetiroUrgencia`/`tipoUrgenciaKey` de `timeline-view.tsx` (extraer a export compartido si hoy son privados — mínima refactorización, sin cambiar comportamiento) y renderizar la línea agrupada "{N} trámites de urgencia · ver todos". Omisión honesta con `fechaValida`.
    Si hace falta exponer helpers de `timeline-view.tsx`, exportarlos SIN alterar su lógica (los tests existentes deben seguir verdes).
    Tests: `tramitacion-stepper.test.tsx` — fixture con etapas + una corrida de urgencia ≥2 → aseverar stepper con hitos clave, línea agrupada "N trámites de urgencia · ver todos" (neutro), omisión de hito no derivable, sin fecha inválida, negative-match sin vocab causal. Extender `timeline-view.test.tsx`/`estado-actual-block.test.tsx` si se tocaron exports (verdes).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/capa1/tramitacion-stepper.test.tsx components/timeline-view.test.tsx components/estado-actual-block.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `TramitacionStepper` es vista pura (no importa supabase); reusa los helpers de agrupación de timeline-view (no reimplementa)
    - línea agrupada usa el copy LOCKED "{N} trámites de urgencia · ver todos"
    - hitos clave presentes; omisión honesta (sin fecha inválida)
    - los tests de timeline-view/estado-actual-block existentes siguen verdes tras cualquier export
    - negative-match sin vocab causal
  </acceptance_criteria>
  <done>Stepper capa-1 que eleva el estado actual y agrupa urgencia reusando la lógica existente; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Recomponer proyecto page.tsx — rail + stepper capa-1 + detalle colapsado</name>
  <files>app/app/proyecto/[boletin]/page.tsx, app/app/proyecto/[boletin]/page.test.tsx</files>
  <read_first>
    - `app/app/proyecto/[boletin]/page.tsx` COMPLETO (composición Breadcrumbs→FichaSection→EstadoActualBlock→#timeline→#votaciones→#lobby-tramitacion→#idea-matriz→#cuerpos-legales→#similares; validación BOLETIN_RE; ?urgencias normalizado)
    - `app/components/capa1/tramitacion-stepper.tsx` (Task 1) + `app/components/ficha-rail.tsx` + `app/components/detalle-colapsable.tsx` (55-01)
    - `app/AGENTS.md` (leer `node_modules/next/dist/docs/` antes de tocar searchParams/routing — HARD CONSTRAINT)
    - `55-UI-SPEC.md` §Per-Surface "/proyecto" + §Layout Contract
  </read_first>
  <action>
    Recomponer el árbol de render SIN tocar lecturas/validación/orden load-bearing (BOLETIN_RE → searchParams → force-dynamic si aplica). Mantener `Breadcrumbs` (F53). Ensanchar a `max-w-5xl` + grid `md:grid-cols-[13rem_1fr] gap-8 items-start`.
    Rail: `<FichaRail header={título+boletín+estado} navEntries={6 secciones} caveat={CAVEAT_RAIL} />` con ids `#estado` (Dónde está) / `#timeline` (Tramitación) / `#votaciones` / `#lobby-tramitacion` (Lobby del período) / `#idea-matriz` / `#similares`. Conteos por sección donde apliquen (votaciones N, lobby N), 3-estado honesto. Caveat 1× en el rail.
    Contenido: cada sección `<section id className="mt-12 scroll-mt-6">` hermana (frontera LOCKED). Tramitación (#timeline): capa-1 = `<TramitacionStepper .../>` SIEMPRE visible (eleva EstadoActualBlock) → `<DetalleColapsable n={nEventos}>{<TimelineView .../>}</DetalleColapsable>` (tramitación completa colapsada, ?urgencias sigue operando dentro). Votaciones/Idea matriz/Similares: capa-1 breve + detalle colapsado donde aporte (votaciones puede quedar visible si es corta; usar criterio de escaneabilidad). Lobby×tramitación: capa-1 conteo+semana + detalle inline; nombre PLANO no-enlazado (52-03).
    Preservar la conexión existente "Qué se votó" (leerFicha cacheada) y los anclas #idea-matriz.
    Actualizar `page.test.tsx`: rail presente con las 6 entradas; stepper de tramitación visible; tramitación completa en detalle colapsado; nombre lobby plano; anti-insinuación (caveat 1×, mt-12 hermanos).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run "app/proyecto/[boletin]/page.test.tsx" && cd .. && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - container `max-w-5xl` + grid; `FichaRail` 1× con 6 navEntries; caveat 1×
    - #timeline: `TramitacionStepper` FUERA del disclosure; `TimelineView` dentro de `DetalleColapsable` (default cerrado)
    - cada sección `mt-12 scroll-mt-6` hermana; nombre lobby×tramitación plano no-enlazado
    - orden BOLETIN_RE→searchParams intacto; Breadcrumbs preservado
    - page.test.tsx verde; `pnpm typecheck` limpio
  </acceptance_criteria>
  <done>Ficha proyecto re-compuesta con rail + stepper + tramitación colapsada; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server page → client islands | Secciones server pasadas como children; islas no importan supabase. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-09 | Information Disclosure | proyecto page rewrite | mitigate | `*Section`/`TimelineView` como children; sin RPC nueva; lockdown-guard Block B verde. |
| T-55-10 | Tampering (anti-insinuación) | stepper + copy urgencia | mitigate | Copy urgencia LOCKED neutro; nombre lobby plano (52-03); caveat 1×; mt-12 hermanos; banned-vocab negative-match. |
| T-55-11 | Tampering | fechas derivadas del stepper | mitigate | `fechaValida` reusado; omisión honesta; nunca renderiza epoch/fecha inválida. |
| T-55-SC | Tampering | package installs | accept | CERO instalación. |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, ≥594)
- `pnpm typecheck` (root)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7)
- banned-vocab negative-match verde
</verification>

<success_criteria>
Ficha de proyecto con rail sticky + stepper de tramitación escaneable + urgencia agrupada + tramitación completa colapsada (cero hito perdido); nombre lobby plano; anti-insinuación intacta. Suite ≥594, tsc limpio, lockdown-guard 7/7.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-04-SUMMARY.md` when done
</output>
