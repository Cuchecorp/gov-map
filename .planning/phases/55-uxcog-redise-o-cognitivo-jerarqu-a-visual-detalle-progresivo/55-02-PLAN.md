---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/lib/parlamentario-resumen-conteos.ts
  - app/lib/parlamentario-resumen-conteos.test.ts
  - app/components/capa1/votos-capa1.tsx
  - app/components/capa1/votos-capa1.test.tsx
  - app/components/capa1/lobby-capa1.tsx
  - app/components/capa1/lobby-capa1.test.tsx
  - app/components/capa1/patrimonio-capa1.tsx
  - app/components/capa1/patrimonio-capa1.test.tsx
  - app/components/capa1/cruces-capa1.tsx
  - app/components/capa1/cruces-capa1.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "contarCarrilesSeguro devuelve, desde las MISMAS filas que YA lee (sin RPC nueva, import 'server-only' intacto, lockdown-guard verde), el desglose de votos (si/no/abstención/pareo/ausente), el ranking de materias de lobby, los sectores de cruces (nReuniones/nVotos) y las declaraciones de patrimonio (año/tipo) + rango de años"
    - "capa-1 votos muestra 5 cifras Mono grandes + barra apilada 'Cómo votó' (colores semánticos, nunca petróleo), siempre visible"
    - "capa-1 lobby muestra barras horizontales top-N por materia (asunto verbatim de la fuente) en color NEUTRO (petróleo reservado) + conteo total"
    - "capa-1 patrimonio muestra una tira de mini-columnas de declaraciones por año (altura = CONTEO de declaraciones del año, NUNCA montos ni ítems fabricados) + resumen 'N declaraciones · rango años'"
    - "capa-1 cruces es petróleo-framed: chips 'sector · N reuniones' (· M votos solo cuando M>0) + botón primario 'Explorar los N cruces'"
    - "cada capa-1 es una vista PURA testeada con fixtures (sin runtime Supabase)"
  artifacts:
    - path: "app/lib/parlamentario-resumen-conteos.ts"
      provides: "ConteoCarriles extendido con votosBreakdown + lobbyTopMaterias + crucesSectores + patrimonioPorDeclaracion + rangoAnios (todo desde filas ya leídas)"
      contains: "votosBreakdown"
    - path: "app/components/capa1/votos-capa1.tsx"
      provides: "capa-1 votos (5 cifras Mono + stacked bar)"
      exports: ["VotosCapa1"]
    - path: "app/components/capa1/lobby-capa1.tsx"
      provides: "capa-1 lobby (barras materia neutras)"
      exports: ["LobbyCapa1"]
    - path: "app/components/capa1/patrimonio-capa1.tsx"
      provides: "capa-1 patrimonio (mini-columnas por año, sin montos)"
      exports: ["PatrimonioCapa1"]
    - path: "app/components/capa1/cruces-capa1.tsx"
      provides: "capa-1 cruces petróleo-framed + CTA"
      exports: ["CrucesCapa1"]
  key_links:
    - from: "app/components/capa1/votos-capa1.tsx"
      to: "app/lib/parlamentario-resumen-conteos.ts"
      via: "consume votosBreakdown (mismas cifras que VotosView 'Cómo votó')"
      pattern: "votosBreakdown"
    - from: "app/components/capa1/cruces-capa1.tsx"
      to: "app/lib/parlamentario-resumen-conteos.ts"
      via: "consume crucesSectores + total (mismas filas que CrucesView)"
      pattern: "crucesSectores"
    - from: "app/components/capa1/patrimonio-capa1.tsx"
      to: "app/lib/parlamentario-resumen-conteos.ts"
      via: "consume patrimonioPorDeclaracion + rangoAnios (mismas filas que PatrimonioSection)"
      pattern: "patrimonioPorDeclaracion"
---

<objective>
Producir la CAPA-1 (resumen preatentivo siempre visible) de la ficha de parlamentario: extender el servicio de conteos para exponer, desde las filas que YA lee, los productores de datos de las cuatro capa-1 (desglose de votos, ranking de materias de lobby, sectores de cruces, declaraciones de patrimonio), y construir cuatro vistas puras capa-1 (votos, lobby, patrimonio, cruces) hechas con CSS sobre tokens (sin chart lib). Estas piezas las cablea el plan 55-03 en la página, fuera del disclosure.

Purpose: La capa-1 es la que hace la ficha escaneable en <5s (Success Criteria #1). Deriva SOLO de filas ya fetched server-side — CERO RPC nueva, CERO lazy-fetch. Cada capa-1 debe tener un PRODUCTOR real en el servicio de conteos: sin productor, la vista no tendría datos que renderizar.
Output: 1 lib extendida + test, 4 componentes capa-1 + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<interfaces>
Consumido de 55-01 (ya construido): `DetalleColapsable`, `FichaRail` (islas), `useScrollspy`, token `bg-accent-product-soft`.
Colores semánticos de datos (NO son el 10% accent — codifican dato): `--color-favor` (verde), `--color-contra` (rojo), `--color-abstencion` (ámbar), `--color-ausente` (gris). Petróleo `--accent-product` reservado SOLO para cruces/drill-down.
Formas de fila REALES que este plan lee (ya fetched por los mismos RPCs allowlisted que el módulo invoca hoy):
- `votos_de_parlamentario(p_id, p_limit:1000)` → filas con `.seleccion` (ya leídas, líneas ~135-159).
- `lobby_de_parlamentario(p_id)` → filas `LobbyAudienciaRpcRow` con `.identificador` Y `.materia` (asunto verbatim de la fuente; hoy el módulo solo lee `.identificador`, línea ~174; `.materia` se rinde como "Asunto" en `lobby-de-parlamentario.tsx` ~445).
- `cruces_de_parlamentario(p_id)` (gated) → filas `CruceSenalRpcRow` con `.sector_id`/`.sector_etiqueta`/`.tipo_senal` (hoy solo `'lobby_sector'`)/`.conteo`; hoy el módulo solo cuenta `.length` (línea ~216).
- `declaraciones_de_parlamentario(p_id)` → filas `DeclaracionRpcRow` con `.fecha_presentacion` (ISO date) y `.tipo` (periódica/rectificación/cese); hoy el módulo solo cuenta `.length` (línea ~194). NO trae conteo de ítems por declaración (los bienes viven en el RPC APARTE `bienes_de_parlamentario`, que este módulo NO invoca ni debe invocar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender parlamentario-resumen-conteos con los productores de las 4 capa-1 (votos + lobby + cruces + patrimonio)</name>
  <files>app/lib/parlamentario-resumen-conteos.ts, app/lib/parlamentario-resumen-conteos.test.ts</files>
  <behavior>
    - contarCarrilesSeguro devuelve `votosBreakdown: {si,no,abstencion,pareo,ausente}` derivado de las filas de voto ya leídas (mismo RPC, mismo p_limit:1000)
    - las cifras del breakdown coinciden byte-a-byte con las que VotosView computa para "Cómo votó" (fuente única)
    - `lobbyTopMaterias: Array<{ materia: string; n: number }>` ordenado desc, derivado del campo `materia` de las filas lobby ya leídas (deduplicando por audiencia `identificador`, omitiendo materia null/vacía)
    - `crucesSectores: Array<{ sector: string; nReuniones: number; nVotos: number }>` ordenado por nReuniones desc, derivado agrupando las filas de cruces ya leídas por sector (nReuniones = suma de `conteo` de las señales lobby; nVotos = suma de `conteo` de señales de voto, hoy 0 porque `tipo_senal` solo toma `'lobby_sector'`)
    - `patrimonioPorDeclaracion: Array<{ anio: number; tipo: string }>` (una entrada por declaración, año de `fecha_presentacion.slice(0,4)`, tipo verbatim) + `rangoAnios: { min: number; max: number } | null`, derivados de las filas de declaraciones ya leídas — SIN montos, SIN conteo de ítems (no se lee el RPC de bienes)
    - un error real de cualquier RPC sigue haciendo throw (#34); nunca degrada a vacío
    - `conteosDesconocidos()` (fallback) incluye los campos nuevos en su forma vacía honesta (arrays vacíos, rangoAnios null)
  </behavior>
  <read_first>
    - `app/lib/parlamentario-resumen-conteos.ts` COMPLETO (líneas 1 `import "server-only"`, 57-76 `ConteoCarriles`, 128-159 lectura votos + asistencia, 165-180 lectura lobby, 182-199 lectura declaraciones, 201-220 lectura cruces gated, 255-263 objeto retornado, 274-285 `conteosDesconocidos`)
    - `app/lib/parlamentario-resumen-conteos.test.ts` — forma de test existente a extender
    - `app/components/votos-por-parlamentario.tsx` (cómputo de `conteos` para "Cómo votó") — la fuente que debe quedar byte-parity
    - `app/components/lobby-de-parlamentario.tsx` (~445: `a.materia` = "Asunto" verbatim; `LobbyAudienciaRpcRow` en `lib/types.ts` confirma el campo `materia`) — el campo real del que sale `lobbyTopMaterias`
    - `app/components/cruces-de-parlamentario.tsx` + `lib/types.ts` `CruceSenalRpcRow` (sector_id/sector_etiqueta/tipo_senal/conteo) — la forma real de las filas de cruces
    - `app/components/patrimonio-de-parlamentario.tsx` + `lib/types.ts` `DeclaracionRpcRow` (fecha_presentacion/tipo, SIN conteo de ítems; los bienes son un RPC aparte) — la forma real de las filas de declaraciones; espejar la guarda ISO de `esHistorica`/`seriePatrimonio` para años no parseables
    - `55-RESEARCH.md` §Code Examples "Capa-1 votos figures" (líneas 335-342), §Assumption A1 (guard verde), §Pattern 3
    - `55-PATTERNS.md` §"parlamentario-resumen-conteos.ts (MODIFY)"
  </read_first>
  <action>
    Extender `ConteoCarriles` con CINCO campos derivados de filas YA leídas (sin segunda lectura, sin RPC nueva, sin tocar el orden gate-aware de cruces/money):
    (1) `votosBreakdown`: recorrer `votosRows` (ya fetched, líneas ~144-145) acumulando `{ si:0, no:0, abstencion:0, pareo:0, ausente:0 }` por `v.seleccion`; agregarlo al objeto retornado. Debe producir las MISMAS cifras que `VotosView` para "Cómo votó" (fuente única → chip/capa-1/sección nunca desincronizan; mantener el comentario del cap p_limit:1000 byte-parity con VotosSection).
    (2) `lobbyTopMaterias`: cambiar el tipo de `lobbyFilas` para leer TAMBIÉN `.materia` (`{ identificador: string; materia: string | null }[]`) — es el MISMO RPC ya invocado, sin fetch nuevo. Deduplicar por `identificador` (el RPC hace left-join → una fila por contraparte, la materia se repite por audiencia), agrupar por el string `materia` verbatim, contar audiencias por materia, y devolver `Array<{ materia, n }>` ordenado por `n` desc (ranking completo o top-8; el top-N final lo decide capa-1). Omisión honesta: materia null/vacía se EXCLUYE del ranking (nunca se fabrica una categoría).
    (3) `crucesSectores`: dentro del bloque `if (crucesPublicEnabled(...))` YA existente, cambiar el tipo de `crucesData` para leer los campos reales (`{ sector_id: string; sector_etiqueta: string; tipo_senal: string; conteo: number }[]`) — MISMO RPC, sin fetch nuevo. Agrupar por sector (`sector_etiqueta`), acumulando `nReuniones` = suma de `conteo` de señales lobby (`tipo_senal === 'lobby_sector'`) y `nVotos` = suma de `conteo` de señales de voto (hoy inexistentes → 0; dejar la rama para cuando el materializador emita señales de voto). Devolver `Array<{ sector, nReuniones, nVotos }>` ordenado por `nReuniones` desc. Con el gate OFF, `crucesSectores` queda `[]` (default seguro, igual que `cruces`). El `total` para la CTA de capa-1 sigue siendo `crucesTotal` (`.length` de las filas — byte-parity con el conteo del rail).
    (4) `patrimonioPorDeclaracion` + `rangoAnios`: cambiar el tipo de `patrData` para leer `{ fecha_presentacion: string; tipo: string }[]` — MISMO RPC, sin fetch nuevo, SIN tocar `bienes_de_parlamentario` (los ítems por declaración viven ahí y NO se leen aquí — regla F46: nunca montos; y sin RPC nueva). Mapear cada fila a `{ anio, tipo }` derivando `anio = Number(fecha_presentacion.slice(0,4))` con la MISMA guarda ISO de `esHistorica`/`seriePatrimonio` (regex `^\d{4}$`; año no parseable se EXCLUYE, nunca se grafica basura). `rangoAnios` = `{ min, max }` de los años presentes, o `null` si no hay ninguno. Devolver ambos en el objeto.
    Actualizar `conteosDesconocidos()` para incluir los cinco campos en su forma vacía honesta: `votosBreakdown` en ceros, `lobbyTopMaterias: []`, `crucesSectores: []`, `patrimonioPorDeclaracion: []`, `rangoAnios: null`.
    Mantener `import "server-only"` (línea 1). NO agregar `.from(PII)`, NO nueva RPC (Assumption A1, guard Block B escanea este módulo). NUNCA leer/exponer montos.
    Test: extender `parlamentario-resumen-conteos.test.ts` mockeando `sb.rpc` por nombre → aseverar que `votosBreakdown` suma por seleccion; `lobbyTopMaterias` deduplica por identificador y rankea desc omitiendo materia null; `crucesSectores` agrupa por sector con nReuniones desc (y nVotos 0 con solo lobby_sector); `patrimonioPorDeclaracion` mapea año/tipo excluyendo fecha no-ISO y `rangoAnios` correcto; un rpc error hace throw (no degrada); `conteosDesconocidos()` trae los campos vacíos.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/parlamentario-resumen-conteos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `parlamentario-resumen-conteos.ts` conserva `import "server-only"` en línea 1
    - el objeto retornado incluye `votosBreakdown`, `lobbyTopMaterias`, `crucesSectores`, `patrimonioPorDeclaracion` y `rangoAnios`; `conteosDesconocidos()` incluye sus formas vacías
    - grep: CERO `.from(` de tabla PII nueva, CERO nombre de RPC fuera del allowlist existente, CERO lectura de `bienes_de_parlamentario`, CERO monto
    - test verde: breakdown suma por seleccion + lobby ranking (dedupe por identificador, desc) + cruces por sector (nReuniones desc) + patrimonio año/tipo + rango + throw en error + fallback vacío
  </acceptance_criteria>
  <done>Servicio de conteos expone los productores REALES de las 4 capa-1 desde las mismas filas; guard verde; sin montos ni RPC nueva.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capa-1 votos + capa-1 lobby (CSS sobre tokens)</name>
  <files>app/components/capa1/votos-capa1.tsx, app/components/capa1/votos-capa1.test.tsx, app/components/capa1/lobby-capa1.tsx, app/components/capa1/lobby-capa1.test.tsx</files>
  <behavior>
    - VotosCapa1: 5 cifras Mono grandes (text-2xl/600): {si} a favor / {no} en contra / {abstencion} abst / {ausente} ausente / {asistencia}% asistencia + barra apilada "Cómo votó" con colores semánticos
    - los colores de voto son SOLO --color-favor/contra/abstencion/ausente; nunca petróleo, nunca en chrome de sección
    - LobbyCapa1: barras horizontales top-N por materia (asunto verbatim) en color NEUTRO (--muted-foreground/border) + conteo total; degradación honesta si hay pocas/ninguna materia distinta
    - ambos son vistas puras (props de fixtures, sin Supabase runtime)
  </behavior>
  <read_first>
    - `app/components/parlamentario-resumen.tsx` (`ResumenView` líneas 54-107: patrón vista-pura)
    - `app/components/votos-por-parlamentario.tsx` (la barra apilada "Cómo votó" existente a reusar/espejar en CSS)
    - `55-UI-SPEC.md` §Typography (Display KPI 24px/600 Mono), §Color (tabla semántica; petróleo reservado), §Per-Surface "/parlamentario" capa-1 votos/lobby
    - `55-PATTERNS.md` §"capa1/*.tsx" (mini-visuals = CSS on tokens) + Pattern B/C/D
  </read_first>
  <action>
    Crear `capa1/votos-capa1.tsx` exportando `VotosCapa1` — vista PURA `{ breakdown, asistencia }` (recibe `votosBreakdown` + `asistencia` computados en Task 1). Render: 5 facts Mono (`text-2xl font-semibold font-mono` con label Sans muted debajo/al lado) usando colores semánticos por fact (favor=verde, contra=rojo, abstención=ámbar, ausente=gris) SOLO en el número/acento, no en el fondo de la card; + una barra apilada "Cómo votó" en CSS (segmentos proporcionales con los 4 colores semánticos). Omisión honesta: si asistencia no derivable, omitir esa cifra (nunca fabricar %). Sin `font-bold`/700.
    Crear `capa1/lobby-capa1.tsx` exportando `LobbyCapa1` — vista PURA `{ topMaterias, total }` (recibe `lobbyTopMaterias` + el total de reuniones). Render: barras horizontales top-N (máx ~5, chunking ≤7) proporcionales sobre `topMaterias` (materia = asunto verbatim), en color NEUTRO (`bg-[color:var(--muted-foreground)]`/border — petróleo PROHIBIDO aquí) con label materia (Sans) + conteo Mono; + "N reuniones" (conteo neutro, sin verbo causal). Degradación honesta: si `topMaterias` está vacío (ninguna materia publicada), omitir las barras y mostrar solo el conteo total. Nombre de contraparte NO aparece en capa-1 (va al detalle, plano no-enlazado 52-03).
    Tests: `votos-capa1.test.tsx` — fixture breakdown → aseverar las 5 cifras presentes (Mono) + omisión de asistencia null + ausencia de clases petróleo. `lobby-capa1.test.tsx` — fixture topMaterias → aseverar N barras top ordenadas + conteo neutro + degradación honesta con topMaterias vacío + CERO vocab causal (negative-match) + CERO petróleo.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/capa1/votos-capa1.test.tsx components/capa1/lobby-capa1.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `VotosCapa1` y `LobbyCapa1` son vistas puras (no importan supabase, no `async` fetch)
    - votos capa-1: 5 cifras en `font-mono` + `text-2xl` + `font-semibold` (nunca 700); colores solo semánticos
    - lobby capa-1: barras en color neutro; CERO uso de `--accent-product`/petróleo (grep); degradación honesta con topMaterias vacío
    - tests verdes: cifras/barras correctas + omisión honesta + negative-match sin vocab causal
  </acceptance_criteria>
  <done>capa-1 votos y lobby puras, escaneables, colores disciplinados; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: capa-1 patrimonio + capa-1 cruces (petróleo-framed)</name>
  <files>app/components/capa1/patrimonio-capa1.tsx, app/components/capa1/patrimonio-capa1.test.tsx, app/components/capa1/cruces-capa1.tsx, app/components/capa1/cruces-capa1.test.tsx</files>
  <behavior>
    - PatrimonioCapa1: tira de mini-columnas de declaraciones por año (altura = CONTEO de declaraciones de ese año, NUNCA montos ni un conteo de ítems fabricado) + "N declaraciones · rango de años"
    - CrucesCapa1: sección con marco petróleo 1.5px + h2 petróleo + chips "sector · N reuniones" (append "· M votos" SOLO cuando M>0) + botón primario "Explorar los N cruces"
    - cruces es el ÚNICO uso de petróleo/color de producto en la página de capa-1
    - patrimonio NUNCA renderiza un monto (regla F46) ni un conteo de ítems por declaración (ese dato NO viene en las filas leídas)
  </behavior>
  <read_first>
    - `app/components/patrimonio-de-parlamentario.tsx` + `app/components/patrimonio-chart.tsx` (F46: conteo de ítems/años, NUNCA montos; nota: el conteo de ítems por versión sale del RPC de bienes, que capa-1 NO recibe)
    - `app/components/cruces-de-parlamentario.tsx` (`CrucesView` líneas 85-195; comentario gate 18-42; caveat cruces LOCKED; `CruceSenalRpcRow`)
    - `55-UI-SPEC.md` §Color (accent reservado item 1 cruces frame + CTA), §Copywriting ("Explorar los {N} cruces", caveat cruces), §Per-Surface capa-1 patrimonio/cruces
    - `55-PATTERNS.md` Pattern E (anti-insinuación) + §"capa1/*.tsx"
  </read_first>
  <action>
    Crear `capa1/patrimonio-capa1.tsx` exportando `PatrimonioCapa1` — vista PURA `{ porDeclaracion: Array<{ anio: number; tipo: string }>, rangoAnios: { min: number; max: number } | null }` (recibe `patrimonioPorDeclaracion` + `rangoAnios` de Task 1). Render: una tira compacta de mini-columnas CSS ordenadas por `anio`. Como las filas de declaraciones NO traen un conteo de ítems por declaración (los bienes son un RPC aparte, deliberadamente no leído — sin RPC nueva; y la regla F46 prohíbe montos), la mini-columna encode el CONTEO DE DECLARACIONES por año: agrupar `porDeclaracion` por `anio` y hacer la altura ∝ nº de declaraciones de ese año (métrica basada en conteo, derivada de las filas ya leídas) — NUNCA un monto, NUNCA un conteo de ítems fabricado. Rotular por año/tipo (periódica/rectificación/cese — sin mezclar; espejo F46). Resumen "N declaraciones · {rangoAnios.min}–{rangoAnios.max}" en Mono. Degradación honesta <2 declaraciones ("datos insuficientes para una tendencia") y rangoAnios null. CERO monto en el DOM.
    Crear `capa1/cruces-capa1.tsx` exportando `CrucesCapa1` — vista PURA `{ sectores: Array<{ sector: string; nReuniones: number; nVotos: number }>, total: number }` (recibe `crucesSectores` + `total` de Task 1). Render: contenedor con marco petróleo 1.5px (`border-[color:var(--accent-product)]` via util de token existente) + `<h2>` en petróleo + chips neutros "sector · N reuniones", agregando "· M votos" SOLO cuando `nVotos > 0` (omisión honesta: no se pinta una dimensión de votos que la fuente no trae hoy; conteos factuales lado a lado, NUNCA compone reunión+voto en una frase causal) + botón PRIMARIO petróleo "Explorar los {total} cruces" (este botón dispara el drill-down — en la página lo conectará 55-03 al DetalleColapsable de cruces). Caveat cruces 1× LOCKED: "La coincidencia temporal no implica relación entre la reunión y el voto." Petróleo SOLO aquí.
    Tests: `patrimonio-capa1.test.tsx` — fixture → aseverar mini-columnas por año (altura = conteo de declaraciones del año) + tipo rotulado + resumen rango + CERO monto (grep de símbolo moneda/dígito-con-$ ausente) + degradación <2. `cruces-capa1.test.tsx` — fixture → aseverar marco/h2 petróleo + chips "N reuniones" (neutro) + "· M votos" presente solo con nVotos>0 y ausente con nVotos=0 + botón "Explorar los N cruces" + caveat 1× + negative-match sin vocab causal + NO composición reunión-voto en una oración.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/capa1/patrimonio-capa1.test.tsx components/capa1/cruces-capa1.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - patrimonio capa-1: NUNCA renderiza montos (test asevera ausencia) ni un conteo de ítems; mini-columnas ∝ conteo de declaraciones por año; degradación <2 declaraciones
    - cruces capa-1: usa petróleo en marco/h2/CTA; chips neutros; "· M votos" solo con M>0; caveat cruces 1×; CTA "Explorar los N cruces"
    - petróleo aparece SOLO en cruces-capa1 (no en patrimonio-capa1)
    - tests verdes incl. negative-match anti-insinuación
  </acceptance_criteria>
  <done>capa-1 patrimonio (sin montos, mini-columnas por año) y cruces (petróleo-framed, destino del drill-down) con tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RPC PII-safe → capa-1 SSR | capa-1 deriva solo de la salida PII-safe de RPCs existentes; nunca renderiza rut/partido/monto. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Information Disclosure | parlamentario-resumen-conteos.ts | mitigate | Sin RPC nueva, sin `.from(PII)`, sin leer `bienes_de_parlamentario`; `import "server-only"` intacto; lockdown-guard Block B escanea el módulo (Assumption A1). |
| T-55-04 | Information Disclosure | patrimonio-capa1.tsx | mitigate | NUNCA renderiza montos ni conteo de ítems (regla F46); solo conteo de declaraciones por año; test asevera ausencia de monto. |
| T-55-05 | Tampering (anti-insinuación) | cruces-capa1 / lobby-capa1 copy | mitigate | Conteos neutros lado a lado; "· M votos" solo con M>0; caveat cruces 1×; banned-vocab negative-match; petróleo reservado a cruces. |
| T-55-SC | Tampering | package installs | accept | CERO instalación en esta fase. |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, ≥594)
- `pnpm typecheck` (root)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7)
- banned-vocab negative-match verde
</verification>

<success_criteria>
Conteos extendido con los productores REALES de las 4 capa-1 (votosBreakdown + lobbyTopMaterias + crucesSectores + patrimonioPorDeclaracion + rangoAnios desde filas ya leídas, guard verde, sin montos ni RPC nueva) + 4 vistas capa-1 puras (votos, lobby, patrimonio sin montos, cruces petróleo-framed) con colores disciplinados y anti-insinuación intacta. Tests verdes; suite ≥594; tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-02-SUMMARY.md` when done
</output>
</output>
