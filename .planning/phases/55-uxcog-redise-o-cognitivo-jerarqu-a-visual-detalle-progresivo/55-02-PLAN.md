---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/lib/parlamentario-resumen-conteos.ts
  - app/lib/parlamentario-resumen-conteos.test.ts
  - app/components/capa1/votos-capa1.tsx
  - app/components/capa1/votos-capa1.test.tsx
  - app/components/capa1/lobby-capa1.tsx
  - app/components/capa1/lobby-capa1.test.tsx
  - app/components/capa1/patrimonio-capa1.tsx
  - app/components/capa1/patrimonio-capa1.test.tsx
  - app/components/capa1/cruces-capa1.tsx
  - app/components/capa1/cruces-capa1.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "contarCarrilesSeguro devuelve el desglose de votos (si/no/abstención/pareo/ausente) derivado de las MISMAS filas ya leídas (votos_de_parlamentario p_limit:1000), sin RPC nueva"
    - "capa-1 votos muestra 5 cifras Mono grandes + barra apilada 'Cómo votó' (colores semánticos, nunca petróleo), siempre visible"
    - "capa-1 lobby muestra barras horizontales top-N por materia en color NEUTRO (petróleo reservado) + conteo"
    - "capa-1 patrimonio muestra tira de mini-columnas por declaración (SOLO conteos, NUNCA montos) + resumen 'N declaraciones · rango años'"
    - "capa-1 cruces es petróleo-framed: chips 'sector · N reuniones · M votos' + botón primario 'Explorar los N cruces'"
    - "cada capa-1 es una vista PURA testeada con fixtures (sin runtime Supabase)"
  artifacts:
    - path: "app/lib/parlamentario-resumen-conteos.ts"
      provides: "ConteoCarriles extendido con votosBreakdown + lobby top-materias"
      contains: "votosBreakdown"
    - path: "app/components/capa1/votos-capa1.tsx"
      provides: "capa-1 votos (5 cifras Mono + stacked bar)"
      exports: ["VotosCapa1"]
    - path: "app/components/capa1/lobby-capa1.tsx"
      provides: "capa-1 lobby (barras materia neutras)"
      exports: ["LobbyCapa1"]
    - path: "app/components/capa1/patrimonio-capa1.tsx"
      provides: "capa-1 patrimonio (mini-columnas, sin montos)"
      exports: ["PatrimonioCapa1"]
    - path: "app/components/capa1/cruces-capa1.tsx"
      provides: "capa-1 cruces petróleo-framed + CTA"
      exports: ["CrucesCapa1"]
  key_links:
    - from: "app/components/capa1/votos-capa1.tsx"
      to: "app/lib/parlamentario-resumen-conteos.ts"
      via: "consume votosBreakdown (mismas cifras que VotosView 'Cómo votó')"
      pattern: "votosBreakdown"
---

<objective>
Producir la CAPA-1 (resumen preatentivo siempre visible) de la ficha de parlamentario: extender el servicio de conteos para exponer el desglose de votos que YA lee, y construir cuatro vistas puras capa-1 (votos, lobby, patrimonio, cruces) hechas con CSS sobre tokens (sin chart lib). Estas piezas las cablea el plan 55-03 en la página, fuera del disclosure.

Purpose: La capa-1 es la que hace la ficha escaneable en <5s (Success Criteria #1). Deriva SOLO de filas ya fetched server-side — CERO RPC nueva, CERO lazy-fetch.
Output: 1 lib extendida + test, 4 componentes capa-1 + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<interfaces>
Consumido de 55-01 (ya construido): `DetalleColapsable`, `FichaRail` (islas), `useScrollspy`, token `bg-accent-product-soft`.
Colores semánticos de datos (NO son el 10% accent — codifican dato): `--color-favor` (verde), `--color-contra` (rojo), `--color-abstencion` (ámbar), `--color-ausente` (gris). Petróleo `--accent-product` reservado SOLO para cruces/drill-down.
`votos_de_parlamentario(p_id, p_limit:1000)` ya se lee en parlamentario-resumen-conteos.ts (líneas ~135-159); cada fila tiene `.seleccion`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender parlamentario-resumen-conteos con votosBreakdown + lobby top-materias</name>
  <files>app/lib/parlamentario-resumen-conteos.ts, app/lib/parlamentario-resumen-conteos.test.ts</files>
  <behavior>
    - contarCarrilesSeguro devuelve `votosBreakdown: {si,no,abstencion,pareo,ausente}` derivado de las filas ya leídas (mismo RPC, mismo p_limit:1000)
    - las cifras del breakdown coinciden byte-a-byte con las que VotosView computa para "Cómo votó" (fuente única)
    - lobby top-materias: un arreglo `[{materia, n}]` ordenado desc, derivado de las filas lobby ya leídas
    - un error real del RPC sigue haciendo throw (#34); nunca degrada a vacío
  </behavior>
  <read_first>
    - `app/lib/parlamentario-resumen-conteos.ts` COMPLETO (líneas 1 `import "server-only"`, 128-134 comentario cap WR-03, 135-159 lectura votos + asistencia, 165-175 lectura lobby, 255-263 objeto ConteoCarriles retornado)
    - `app/lib/parlamentario-resumen-conteos.test.ts` — forma de test existente a extender
    - `app/components/votos-por-parlamentario.tsx` (cómputo de `conteos` para "Cómo votó") — la fuente que debe quedar byte-parity
    - `55-RESEARCH.md` §Code Examples "Capa-1 votos figures" (líneas 335-342), §Assumption A1 (guard verde)
    - `55-PATTERNS.md` §"parlamentario-resumen-conteos.ts (MODIFY)"
  </read_first>
  <action>
    Extender `ConteoCarriles` con dos campos derivados de filas YA leídas (sin segunda lectura, sin RPC nueva):
    (1) `votosBreakdown`: recorrer `votosRows` (ya fetched, líneas ~144-145) acumulando `{ si:0, no:0, abstencion:0, pareo:0, ausente:0 }` por `v.seleccion`; agregarlo al objeto retornado. Debe producir las MISMAS cifras que `VotosView` para "Cómo votó" (fuente única → chip/capa-1/sección nunca desincronizan; mantener el comentario del cap p_limit:1000 byte-parity con VotosSection).
    (2) `lobbyTopMaterias`: derivar de las filas `lobby_de_parlamentario` ya leídas un `Array<{ materia: string; n: number }>` agrupado por materia y ordenado por `n` desc (top-N lo decide capa-1; la lib entrega el ranking completo o top-8). Sin fabricar: materia ausente/null se omite u honesta según la fila.
    Mantener `import "server-only"` (línea 1). NO agregar `.from(PII)`, NO nueva RPC (Assumption A1, guard Block B escanea este módulo).
    Test: extender `parlamentario-resumen-conteos.test.ts` mockeando `sb.rpc` para votos → aseverar que `votosBreakdown` suma correctamente por seleccion; para lobby → aseverar el ranking desc; aseverar que un rpc error hace throw (no degrada).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/parlamentario-resumen-conteos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `parlamentario-resumen-conteos.ts` conserva `import "server-only"` en línea 1
    - el objeto retornado incluye `votosBreakdown` y `lobbyTopMaterias`
    - grep: CERO `.from(` de tabla PII nueva, CERO nombre de RPC fuera del allowlist existente
    - test verde: breakdown suma por seleccion + ranking lobby desc + throw en error
  </acceptance_criteria>
  <done>Servicio de conteos expone breakdown de votos + top-materias lobby desde las mismas filas; guard verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capa-1 votos + capa-1 lobby (CSS sobre tokens)</name>
  <files>app/components/capa1/votos-capa1.tsx, app/components/capa1/votos-capa1.test.tsx, app/components/capa1/lobby-capa1.tsx, app/components/capa1/lobby-capa1.test.tsx</files>
  <behavior>
    - VotosCapa1: 5 cifras Mono grandes (text-2xl/600): {si} a favor / {no} en contra / {abstencion} abst / {ausente} ausente / {asistencia}% asistencia + barra apilada "Cómo votó" con colores semánticos
    - los colores de voto son SOLO --color-favor/contra/abstencion/ausente; nunca petróleo, nunca en chrome de sección
    - LobbyCapa1: barras horizontales top-N por materia en color NEUTRO (--muted-foreground/border) + conteo total
    - ambos son vistas puras (props de fixtures, sin Supabase runtime)
  </behavior>
  <read_first>
    - `app/components/parlamentario-resumen.tsx` (`ResumenView` líneas 54-107: patrón vista-pura)
    - `app/components/votos-por-parlamentario.tsx` (la barra apilada "Cómo votó" existente a reusar/espejar en CSS)
    - `55-UI-SPEC.md` §Typography (Display KPI 24px/600 Mono), §Color (tabla semántica; petróleo reservado), §Per-Surface "/parlamentario" capa-1 votos/lobby
    - `55-PATTERNS.md` §"capa1/*.tsx" (mini-visuals = CSS on tokens) + Pattern B/C/D
  </read_first>
  <action>
    Crear `capa1/votos-capa1.tsx` exportando `VotosCapa1` — vista PURA `{ breakdown, asistencia }` (recibe `votosBreakdown` + `asistencia` computados en Task 1). Render: 5 facts Mono (`text-2xl font-semibold font-mono` con label Sans muted debajo/al lado) usando colores semánticos por fact (favor=verde, contra=rojo, abstención=ámbar, ausente=gris) SOLO en el número/acento, no en el fondo de la card; + una barra apilada "Cómo votó" en CSS (segmentos proporcionales con los 4 colores semánticos). Omisión honesta: si asistencia no derivable, omitir esa cifra (nunca fabricar %). Sin `font-bold`/700.
    Crear `capa1/lobby-capa1.tsx` exportando `LobbyCapa1` — vista PURA `{ topMaterias, total }`. Render: barras horizontales top-N (máx ~5, chunking ≤7) proporcionales, en color NEUTRO (`bg-[color:var(--muted-foreground)]`/border — petróleo PROHIBIDO aquí) con label materia (Sans) + conteo Mono; + "N reuniones" (conteo neutro, sin verbo causal). Nombre de contraparte NO aparece en capa-1 (va al detalle, plano no-enlazado 52-03).
    Tests: `votos-capa1.test.tsx` — fixture breakdown → aseverar las 5 cifras presentes (Mono) + omisión de asistencia null + ausencia de clases petróleo. `lobby-capa1.test.tsx` — fixture topMaterias → aseverar N barras top ordenadas + conteo neutro + CERO vocab causal (negative-match) + CERO petróleo.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/capa1/votos-capa1.test.tsx components/capa1/lobby-capa1.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `VotosCapa1` y `LobbyCapa1` son vistas puras (no importan supabase, no `async` fetch)
    - votos capa-1: 5 cifras en `font-mono` + `text-2xl` + `font-semibold` (nunca 700); colores solo semánticos
    - lobby capa-1: barras en color neutro; CERO uso de `--accent-product`/petróleo (grep)
    - tests verdes: cifras/barras correctas + omisión honesta + negative-match sin vocab causal
  </acceptance_criteria>
  <done>capa-1 votos y lobby puras, escaneables, colores disciplinados; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: capa-1 patrimonio + capa-1 cruces (petróleo-framed)</name>
  <files>app/components/capa1/patrimonio-capa1.tsx, app/components/capa1/patrimonio-capa1.test.tsx, app/components/capa1/cruces-capa1.tsx, app/components/capa1/cruces-capa1.test.tsx</files>
  <behavior>
    - PatrimonioCapa1: tira de mini-columnas por declaración (altura = CONTEO de ítems, NUNCA montos) + "N declaraciones · rango de años"
    - CrucesCapa1: sección con marco petróleo 1.5px + h2 petróleo + chips "sector · N reuniones · M votos" + botón primario "Explorar los N cruces"
    - cruces es el ÚNICO uso de petróleo/color de producto en la página de capa-1
    - patrimonio NUNCA renderiza un monto (regla F46)
  </behavior>
  <read_first>
    - `app/components/patrimonio-de-parlamentario.tsx` + `app/components/patrimonio-chart.tsx` (F46: conteo de ítems, NUNCA montos)
    - `app/components/cruces-de-parlamentario.tsx` (`CrucesView` líneas 85-195; comentario gate 18-42; caveat cruces LOCKED)
    - `55-UI-SPEC.md` §Color (accent reservado item 1 cruces frame + CTA), §Copywriting ("Explorar los {N} cruces", caveat cruces), §Per-Surface capa-1 patrimonio/cruces
    - `55-PATTERNS.md` Pattern E (anti-insinuación) + §"capa1/*.tsx"
  </read_first>
  <action>
    Crear `capa1/patrimonio-capa1.tsx` exportando `PatrimonioCapa1` — vista PURA `{ porDeclaracion: Array<{ anio; tipo; nItems }>, rangoAnios }`. Render: tira compacta de mini-columnas CSS (altura ∝ `nItems`, NUNCA monto) con el tipo de declaración rotulado (periódica/rectificación/cese — no mezclar; espejo F46) + resumen "N declaraciones · {rango}" en Mono. Degradación honesta <2 declaraciones ("datos insuficientes para una tendencia"). CERO monto en el DOM.
    Crear `capa1/cruces-capa1.tsx` exportando `CrucesCapa1` — vista PURA `{ sectores: Array<{ sector; nReuniones; nVotos }>, total }`. Render: contenedor con marco petróleo 1.5px (`border-[color:var(--accent-product)]` via util de token existente) + `<h2>` en petróleo + chips neutros "sector · N reuniones · M votos" (conteos factuales lado a lado, NUNCA compone reunión+voto en una frase causal) + botón PRIMARIO petróleo "Explorar los {total} cruces" (este botón dispara el drill-down — en la página lo conectará 55-03 al DetalleColapsable de cruces). Caveat cruces 1× LOCKED: "La coincidencia temporal no implica relación entre la reunión y el voto." Petróleo SOLO aquí.
    Tests: `patrimonio-capa1.test.tsx` — fixture → aseverar mini-columnas por declaración + tipo rotulado + resumen rango + CERO monto (grep de dígitos con símbolo moneda ausente) + degradación <2. `cruces-capa1.test.tsx` — fixture → aseverar marco/h2 petróleo + chips "N reuniones · M votos" (neutro) + botón "Explorar los N cruces" + caveat 1× + negative-match sin vocab causal + NO composición reunión-voto en una oración.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/capa1/patrimonio-capa1.test.tsx components/capa1/cruces-capa1.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - patrimonio capa-1: NUNCA renderiza montos (test asevera ausencia); mini-columnas ∝ conteo; degradación <2 declaraciones
    - cruces capa-1: usa petróleo en marco/h2/CTA; chips neutros; caveat cruces 1×; CTA "Explorar los N cruces"
    - petróleo aparece SOLO en cruces-capa1 (no en patrimonio-capa1)
    - tests verdes incl. negative-match anti-insinuación
  </acceptance_criteria>
  <done>capa-1 patrimonio (sin montos) y cruces (petróleo-framed, destino del drill-down) con tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RPC PII-safe → capa-1 SSR | capa-1 deriva solo de la salida PII-safe de RPCs existentes; nunca renderiza rut/partido/monto. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Information Disclosure | parlamentario-resumen-conteos.ts | mitigate | Sin RPC nueva, sin `.from(PII)`; `import "server-only"` intacto; lockdown-guard Block B escanea el módulo (Assumption A1). |
| T-55-04 | Information Disclosure | patrimonio-capa1.tsx | mitigate | NUNCA renderiza montos (regla F46); test asevera ausencia de monto. |
| T-55-05 | Tampering (anti-insinuación) | cruces-capa1 / lobby-capa1 copy | mitigate | Conteos neutros lado a lado; caveat cruces 1×; banned-vocab negative-match; petróleo reservado a cruces. |
| T-55-SC | Tampering | package installs | accept | CERO instalación en esta fase. |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, ≥594)
- `pnpm typecheck` (root)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7)
- banned-vocab negative-match verde
</verification>

<success_criteria>
Conteos extendido (votosBreakdown + lobbyTopMaterias desde filas ya leídas, guard verde) + 4 vistas capa-1 puras (votos, lobby, patrimonio sin montos, cruces petróleo-framed) con colores disciplinados y anti-insinuación intacta. Tests verdes; suite ≥594; tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-02-SUMMARY.md` when done
</output>
