---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 03
type: execute
wave: 3
depends_on: ["55-01", "55-02"]
files_modified:
  - app/app/parlamentario/[id]/page.tsx
  - app/app/parlamentario/[id]/page.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "La ficha de parlamentario abre con rail sticky + capa-1 siempre visible; el detalle de cada carril arranca COLAPSADO (default cerrado)"
    - "El primer viewport comunica el resumen (qué es, cifras clave, dónde profundizar) sin scroll"
    - "El estado por defecto baja de ~28.000px a un orden de ~5.000px lógicos SIN perder ningún dato (todo accesible vía DetalleColapsable)"
    - "Cada carril sigue siendo un <section className='mt-12'> hermano (frontera LOCKED); la capa-1 vive FUERA del disclosure, solo el detalle colapsa"
    - "El rail refleja los gates exactamente (crucesPublicEnabled/moneyPublicEnabled): solo carriles presentes en el HTML aparecen en el rail"
    - "Cada capa-1 recibe sus props desde contarCarrilesSeguro (votosBreakdown/lobbyTopMaterias/crucesSectores+total/patrimonioPorDeclaracion+rangoAnios); los *Section existentes se pasan como children (contrato no-leak F45); cero RPC nueva, cero lazy-fetch"
  artifacts:
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "página con rail + grid + capa-1 + detalle colapsado por defecto"
      contains: "FichaRail"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "app/components/ficha-rail.tsx"
      via: "navEntries construidas con construirChips (orden gate-aware)"
      pattern: "FichaRail"
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "app/components/detalle-colapsable.tsx"
      via: "wrap de cada *Section (detalle), capa-1 afuera"
      pattern: "DetalleColapsable"
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "app/components/capa1/cruces-capa1.tsx"
      via: "pasa crucesSectores + total desde contarCarrilesSeguro"
      pattern: "CrucesCapa1"
---

<objective>
Cablear la ficha de parlamentario (deliverable PRIMARIO, 28.048px→~5.000px) en el patrón "Informe con rail": ensanchar a grid `max-w-5xl` con rail sticky (`FichaRail`), renderizar la capa-1 de cada carril SIEMPRE visible fuera del disclosure (alimentada por los productores que 55-02 expuso en `contarCarrilesSeguro`), envolver SOLO el detalle (`*Section`) en `DetalleColapsable` (default cerrado), invertir la heurística `abrePorDefecto`, y actualizar los skeletons anti-CLS. Preserva gates, frontera `mt-12`, force-dynamic y el contrato no-leak.

Purpose: Es la superficie de mayor deuda; aquí se materializa el Success Criteria #1/#2/#3 para parlamentario.
Output: page.tsx re-compuesta + page.test.tsx actualizado (las aserciones viejas de auto-open fallarán con el default invertido).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-PATTERNS.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-RESEARCH.md

<interfaces>
De 55-01: `FichaRail({ header, navEntries, caveat })`, `DetalleColapsable({ n, children, defaultOpen? })` (default CERRADO), token `bg-accent-product-soft`.
De 55-02: `VotosCapa1({ breakdown, asistencia })`, `LobbyCapa1({ topMaterias, total })`, `PatrimonioCapa1({ porDeclaracion, rangoAnios })`, `CrucesCapa1({ sectores, total })` (vistas puras); `contarCarrilesSeguro(id)` extendido con `votosBreakdown` + `lobbyTopMaterias` + `crucesSectores` + `patrimonioPorDeclaracion` + `rangoAnios` (todo derivado de filas ya leídas, sin RPC nueva) — ESTOS son los datos que alimentan las cuatro capa-1.
Reuso existente: `construirChips(c, env)` (orden gate-aware, parlamentario-resumen.tsx 120-162), `conteoLabel`, gates `crucesPublicEnabled(env)`/`moneyPublicEnabled(env)`, `VotosSection`/`LobbySection`/`PatrimonioSection`/`CrucesSection` (children detalle).
</interfaces>

<resolved_decisions>
- Rail `sticky top-6`; secciones `scroll-mt-6` (header NO-sticky, 56px scrollea; UI-SPEC top-16 superseded).
- Truncación: conservar la paginación server-driven existente DENTRO del disclosure donde ya existe (votos ?votosPage/?materia, lobby ?lobbyPage). Renderizar todas las filas en el detalle colapsado (DOM presente, oculto por CSS) para secciones chicas (patrimonio 10, cruces 12). EXCEPCIÓN votaciones (141 filas agrupadas por proyecto): si el detalle vuelca >~30 filas planas, aplicar "mostrar más" client-side sobre datos YA renderizados (sin RPC nueva, sin lazy-fetch). Preferir la paginación server existente si ya cubre el caso; no duplicar paginadores en conflicto.
</resolved_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recomponer page.tsx — grid + rail + capa-1 fuera + detalle colapsado</name>
  <files>app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - `app/app/parlamentario/[id]/page.tsx` COMPLETO (líneas 57-68 conteoLabel, 77-79 abrePorDefecto, 111-113 orden gate/searchParams/force-dynamic, 116 max-w-3xl container, 182-280 CarrilesSection, 195-199 comentario frontera mt-12, 268-280 gate cruces, 282+ gate money, los skeletons ResumenSkeleton/CarrilesSkeleton)
    - `app/components/parlamentario-resumen.tsx` (`construirChips` 120-162 — construye navEntries gate-aware)
    - `app/lib/parlamentario-resumen-conteos.ts` (55-02: `contarCarrilesSeguro` retorna `votosBreakdown`/`lobbyTopMaterias`/`crucesSectores`/`patrimonioPorDeclaracion`/`rangoAnios` + `asistencia`) — de aquí salen los props de cada capa-1
    - `55-PATTERNS.md` §"parlamentario page.tsx (MODIFY)" (los 5 cambios) + Pattern A/E
    - `app/AGENTS.md` (leer `node_modules/next/dist/docs/` antes de tocar searchParams/RSC — HARD CONSTRAINT del repo)
    - `55-UI-SPEC.md` §Layout Contract + §Per-Surface "/parlamentario"
  </read_first>
  <action>
    Recomponer el árbol de render de la página (NO tocar las lecturas de datos, gates ni el orden load-bearing de líneas 111-113: id-validate → gate → searchParams → force-dynamic):
    1. Ensanchar container `max-w-3xl` → `max-w-5xl` (línea 116) y envolver el cuerpo en un grid `md:grid-cols-[13rem_1fr] gap-8 items-start` (grid track = geometría, permitido; NO arbitrary color). Mantener `mx-auto px-4 md:px-8` y el `py-8 md:py-16`.
    2. Columna rail: renderizar `<FichaRail header={...} navEntries={...} caveat={CAVEAT_RAIL} />`. `header` = chip cámara + `<h1>` nombre (formatNombre) + período (Mono muted) + badge frescura compacto (reusar ProvenanceBadge/frescura). `navEntries` = derivadas de `construirChips(conteos, env)` mapeadas a `RailEntry` (id `#votos`/`#lobby`/`#patrimonio`/`#cruces`/`#financiamiento`, label, count 3-estado, marker "diamante" para cruces). CAVEAT_RAIL = texto LOCKED del UI-SPEC. El rail vive UNA vez (satisface la regla caveat 1×/página).
    3. Columna contenido: cada carril sigue siendo su `<section id className="mt-12 scroll-mt-6">` hermano (frontera LOCKED — NUNCA mover mt-12 a un wrapper ni anidar dos dominios). Dentro de cada section: `<h2>` + conteo (conteoLabel, siempre visible) → la capa-1 correspondiente SIEMPRE visible, FUERA del disclosure, alimentada por `contarCarrilesSeguro`: `<VotosCapa1 breakdown={conteos.votosBreakdown} asistencia={conteos.asistencia} />`, `<LobbyCapa1 topMaterias={conteos.lobbyTopMaterias} total={...} />`, `<PatrimonioCapa1 porDeclaracion={conteos.patrimonioPorDeclaracion} rangoAnios={conteos.rangoAnios} />`, `<CrucesCapa1 sectores={conteos.crucesSectores} total={...} />` → luego `<DetalleColapsable n={conteo}>{<VotosSection .../> etc.}</DetalleColapsable>` (el `*Section` server como children — contrato no-leak; detalle CERRADO por defecto). El botón "Explorar los N cruces" de `CrucesCapa1` es la afordancia de drill-down hacia el `DetalleColapsable` de cruces.
    4. Invertir `abrePorDefecto` (líneas 77-79): eliminar/neutralizar la heurística de auto-open por `tipo === "dato"`; el detalle arranca cerrado (`defaultOpen` omitido → false). Conservar `conteoLabel` verbatim para el sufijo del header.
    5. Preservar los branches de gate verbatim: cruces (268-280) envuelto por `crucesPublicEnabled(env)`, money (282+) por `moneyPublicEnabled(env)`; OFF ⇒ nodo ausente del HTML Y entrada ausente del rail (el rail deriva de construirChips que ya es gate-aware). Con el gate de cruces OFF, `crucesSectores` llega `[]` y la sección de cruces no se renderiza (no se pinta una capa-1 vacía). Financiamiento pendiente = sección `opacity-60` con la línea legal LOCKED, presente en rail como "—".
    6. Votaciones: conservar la paginación server existente (?votosPage/?materia) dentro del `DetalleColapsable`; si vuelca >~30 filas planas aplicar "mostrar más" client-side sobre datos ya renderizados (sin RPC nueva) — solo si no entra en conflicto con la paginación server ya presente.
    Usar `scroll-mt-6` en cada `<section>` para que los saltos del rail no queden bajo un gap fantasma.
  </action>
  <verify>
    <automated>cd app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - container es `max-w-5xl` con grid `md:grid-cols-[13rem_1fr]`
    - `FichaRail` renderizado 1× con navEntries de construirChips; caveat 1× (solo en el rail)
    - cada carril es `<section ... className` con `mt-12` y `scroll-mt-6`; capa-1 FUERA del `DetalleColapsable`; `*Section` pasado como children
    - cada capa-1 recibe sus props desde `contarCarrilesSeguro` (VotosCapa1←votosBreakdown/asistencia, LobbyCapa1←lobbyTopMaterias/total, PatrimonioCapa1←patrimonioPorDeclaracion/rangoAnios, CrucesCapa1←crucesSectores/total)
    - `abrePorDefecto` de auto-open eliminado/neutralizado (detalle default cerrado)
    - gates cruces/money preservados verbatim; orden id-validate→gate→searchParams intacto; `export const dynamic` intacto
    - grep: los `*Section` NO se importan en las islas (siguen importados solo por la página server)
    - `pnpm typecheck` limpio
  </acceptance_criteria>
  <done>Ficha parlamentario re-compuesta: rail + capa-1 visible (alimentada por conteos) + detalle colapsado, gates y frontera intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar page.test.tsx + skeletons anti-CLS</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/app/parlamentario/[id]/page.test.tsx</files>
  <read_first>
    - `app/app/parlamentario/[id]/page.test.tsx` COMPLETO (aserciones viejas de accordion/auto-open que ahora fallan)
    - `app/app/parlamentario/[id]/page.tsx` (los skeletons ResumenSkeleton/CarrilesSkeleton — su forma actual, 5 chips/headers de accordion)
    - `55-RESEARCH.md` §Pitfall 6 (CLS por skeletons con forma vieja), §Test Map
    - `55-PATTERNS.md` §Tests (EXTEND page test)
  </read_first>
  <action>
    Actualizar `page.test.tsx`: reemplazar las aserciones de auto-open/accordion por las del nuevo patrón — (1) el rail está presente con una entrada de nav por carril presente + orden gate-aware; (2) la capa-1 (cifras Mono) está siempre visible, NO dentro de una región colapsada; (3) el detalle arranca colapsado ("Ver detalle (N)" presente, contenido en DOM vía forceMount con `data-state=closed`); (4) los gates OFF ⇒ sección Y entrada de rail ausentes. Mantener el mock de `contarCarrilesSeguro`/RPC como en el test existente, extendiéndolo para devolver los campos nuevos (votosBreakdown/lobbyTopMaterias/crucesSectores/patrimonioPorDeclaracion/rangoAnios) que ahora consumen las capa-1.
    Reformar los skeletons `ResumenSkeleton`/`CarrilesSkeleton` (en page.tsx) para que coincidan con la nueva forma de rail+capa-1 (evita CLS — patrón anti-CLS IN-02/IN-03 ya en el codebase): un placeholder de rail (columna angosta) + placeholders de capa-1 (bloques de cifras) en lugar de 5 chips / headers de accordion.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run "app/parlamentario/[id]/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - page.test.tsx verde con las 4 aserciones nuevas (rail presente, capa-1 visible, detalle default-cerrado, gate OFF ⇒ ausente)
    - el mock de conteos devuelve los campos nuevos que consumen las capa-1
    - skeletons reformados a la forma rail+capa-1 (grep: sin los 5 chips viejos / headers de accordion en el skeleton)
    - suite `app/` no decrece respecto al baseline
  </acceptance_criteria>
  <done>Test de página actualizado al patrón nuevo + skeletons anti-CLS reformados; verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server page → client islands | page compone `*Section` server como children de las islas; service_role nunca cruza al browser. |
| gate → HTML | crucesPublicEnabled/moneyPublicEnabled envuelven la sección entera; OFF ⇒ nodo ausente (no oculto por CSS). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-06 | Information Disclosure | page.tsx grid/rail rewrite | mitigate | `*Section` como children (no import en islas); orden gate-first + force-dynamic intacto; lockdown-guard Block B verde. |
| T-55-07 | Elevation/Access Control | gates cruces/money | mitigate | Branches de gate preservados verbatim; rail deriva de construirChips gate-aware ⇒ entrada ausente cuando el gate está OFF; crucesSectores `[]` con gate OFF. |
| T-55-08 | Tampering (anti-insinuación) | copy rail/capa-1 | mitigate | Caveat 1× en rail; conteos neutros; mt-12 hermanos; banned-vocab negative-match; petróleo solo cruces. |
| T-55-SC | Tampering | package installs | accept | CERO instalación. |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa, ≥594)
- `pnpm typecheck` (root)
- `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7)
- banned-vocab negative-match verde
- Inspección de altura por defecto (informal, en el checkpoint de 55-06): orden ~5.000px
</verification>

<success_criteria>
Ficha de parlamentario abre con rail sticky + capa-1 escaneable (alimentada por los productores de conteos) en el primer viewport; detalle colapsado por defecto (~5.000px, cero dato perdido); gates y frontera mt-12 intactos; contrato no-leak verde. Suite ≥594, tsc limpio, lockdown-guard 7/7.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-03-SUMMARY.md` when done
</output>
</output>
