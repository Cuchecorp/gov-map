---
phase: 55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo
plan: 06
type: execute
wave: 4
depends_on: ["55-01", "55-02", "55-03", "55-04", "55-05"]
files_modified:
  - docs/demo/
autonomous: false
requirements: [UX-03]
must_haves:
  truths:
    - "La suite completa (≥594), tsc, lockdown-guard 7/7 y banned-vocab negative-match están verdes ANTES del deploy"
    - "El sitio se despliega con el pipeline autorizado (docker-cf-build.sh + wrangler) y pasa un smoke básico"
    - "El set de demo se recaptura (técnica same-origin iframe, cap 12.800px lógicos) mostrando las superficies rediseñadas"
    - "El operador revisa el set y aprueba/rechaza la dirección visual (checkpoint humano)"
  artifacts:
    - path: "docs/demo/"
      provides: "set de demo recapturado post-rediseño"
      contains: "screenshots de ficha parlamentario/proyecto/agenda/red"
  key_links:
    - from: "gate completo verde"
      to: "deploy + smoke + recaptura"
      via: "pipeline autorizado docker-cf-build.sh + wrangler"
      pattern: "docker-cf-build"
---

<objective>
Cierre de fase: verificar el gate completo, desplegar con el pipeline autorizado, correr un smoke, recapturar el set de demo con la técnica same-origin, y presentarlo al operador como checkpoint de dirección visual (la corrección de F54 es de percepción — el operador valida el resultado). Depende de todos los planes de superficie.

Purpose: Success Criteria #4 (cero regresión) y #5 (set de demo recapturado + checkpoint humano).
Output: deploy en vivo + set de demo recapturado + veredicto del operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-UI-SPEC.md
@.planning/phases/54-uxdemo-pulido-presentacional/54-05-SUMMARY.md

<notes>
- Deploy autorizado 2026-07-06: `docker-cf-build.sh` (build OpenNext en Docker Linux — NUNCA build Windows, rompe worker) → `docker cp` → `wrangler deploy` (OAuth) desde host.
- Recaptura: `scripts/rewalk-shot.mjs` + técnica same-origin iframe (ver 54-05-SUMMARY), cap 12.800px lógicos por captura.
- CERO DDL, CERO flip de flag: los gates (cruces/money/net) quedan como estén; el deploy no enciende nada.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate completo + deploy + smoke</name>
  <files>docs/demo/</files>
  <read_first>
    - `.planning/phases/54-uxdemo-pulido-presentacional/54-05-SUMMARY.md` (pipeline de deploy + técnica de captura + curl smoke)
    - `docker-cf-build.sh` (script de build autorizado)
    - `scripts/rewalk-shot.mjs` (recaptura same-origin)
  </read_first>
  <action>
    (1) Correr el gate completo y confirmar verde ANTES de tocar el deploy: `cd app && pnpm test` (suite ≥594, nunca menos que el baseline), `pnpm typecheck` (root), `cd app && pnpm vitest run lib/lockdown-guard.test.ts` (7/7), y confirmar banned-vocab negative-match verde. Si algo está rojo, DETENERSE y reportar (no desplegar).
    (2) Build + deploy con el pipeline autorizado: `docker-cf-build.sh` (OpenNext en Docker Linux) → `docker cp` → `wrangler deploy` desde el host (OAuth). CERO flip de flag; CERO DDL.
    (3) Smoke por curl: home 200, `/parlamentario/<id>` 200 con rail/capa-1 en el HTML, `/proyecto/<boletin>` 200 con stepper, `/agenda` 200 agrupada, `/red` 200/gate según estado actual. Reportar cualquier 500 (gotcha force-dynamic gate).
  </action>
  <verify>
    <automated>cd app && pnpm test && cd .. && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - suite ≥594 verde, tsc limpio, lockdown-guard 7/7, banned-vocab verde — ANTES del deploy
    - deploy ejecutado con docker-cf-build.sh + wrangler (no build Windows)
    - smoke: superficies rediseñadas responden 200 (o 404 esperado por gate OFF en /red); cero 500
    - CERO flip de flag / CERO DDL en el proceso
  </acceptance_criteria>
  <done>Gate verde, sitio desplegado con el pipeline autorizado, smoke pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Recapturar el set de demo (same-origin, cap 12.800px)</name>
  <files>docs/demo/</files>
  <read_first>
    - `.planning/phases/54-uxdemo-pulido-presentacional/54-05-SUMMARY.md` (técnica same-origin iframe + cap 12.800px lógicos + set de superficies)
    - `scripts/rewalk-shot.mjs`
  </read_first>
  <action>
    Recapturar el set de demo con `rewalk-shot.mjs` (técnica same-origin iframe) contra el deploy en vivo, cap 12.800px lógicos por captura. Cubrir las superficies rediseñadas: ficha de parlamentario (estado por defecto ~5.000px con rail + capa-1), ficha de proyecto (rail + stepper), agenda (agrupada), /red (ego-network del seed) — más las que el set F54 ya incluía para comparación. Guardar en `docs/demo/`.
  </action>
  <verify>
    <automated>ls docs/demo/ | head</automated>
  </verify>
  <acceptance_criteria>
    - set de demo recapturado en `docs/demo/` con las 4 superficies rediseñadas
    - cada captura respeta el cap de 12.800px lógicos
    - las capturas muestran el estado por defecto (detalle colapsado, capa-1 visible)
  </acceptance_criteria>
  <done>Set de demo recapturado post-rediseño en docs/demo/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint de dirección visual (operador)</name>
  <what-built>Rediseño cognitivo (variante B "Informe con rail") aplicado a ficha parlamentario (28.048px→~5.000px), ficha proyecto (rail + stepper), agenda (día→comisión), /red (ego-network del seed). Capa-1 escaneable siempre visible, detalle colapsado por defecto, cruces destacados en petróleo como destino del drill-down. Cero dato perdido, anti-insinuación intacta.</what-built>
  <how-to-verify>
    1. Abrir el set de demo recapturado en `docs/demo/` (o el sitio en vivo).
    2. Confirmar que la ficha de parlamentario comunica su resumen en el primer viewport (qué es, cifras clave, dónde profundizar) SIN scroll, y que las listas completas están colapsadas/truncadas por defecto.
    3. Confirmar que los cruces están visualmente elevados (marco/CTA petróleo) como destino del drill-down.
    4. Confirmar que /red abre legible (centrado en el seed, no 136 nodos).
    5. Evaluar la dirección visual/cognitiva contra la corrección original ("claro, ir aumentando el detalle, viendo cruces; no tanto texto genérico; piensa de modo cognitivo").
  </how-to-verify>
  <resume-signal>Escribir "approved" o describir los ajustes de dirección visual pendientes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deploy → producción | Deploy no enciende flags ni aplica DDL; gates quedan como estén. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-15 | Elevation/Access Control | deploy | mitigate | CERO flip de flag / CERO DDL; el deploy es solo código presentacional; gates preservados. |
| T-55-16 | Information Disclosure | smoke/deploy | mitigate | Gate completo (lockdown-guard 7/7 + no `.from(PII)`) verde ANTES de desplegar; Camino A intacto. |
| T-55-SC | Tampering | package installs | accept | CERO instalación. |
</threat_model>

<verification>
- Gate completo verde ANTES del deploy (suite ≥594, tsc, lockdown-guard 7/7, banned-vocab)
- Smoke curl post-deploy: superficies 200 / gate esperado; cero 500
- Set de demo recapturado presente en docs/demo/
- Veredicto del operador registrado
</verification>

<success_criteria>
Cero regresión (suite/tsc/guard/negative-match verdes), sitio desplegado con el pipeline autorizado, set de demo recapturado, y checkpoint de dirección visual resuelto por el operador. CERO DDL/flip de flag.
</success_criteria>

<output>
Create `.planning/phases/55-uxcog-redise-o-cognitivo-jerarqu-a-visual-detalle-progresivo/55-06-SUMMARY.md` when done
</output>
