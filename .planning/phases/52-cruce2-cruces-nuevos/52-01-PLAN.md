---
phase: 52-cruce2-cruces-nuevos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cruces/src/clasificar-lobby-cli.ts
  - packages/cruces/src/clasificar-fichas-cli.ts
  - packages/cruces/src/clasificar-lobby-cli.test.ts
autonomous: true
requirements: [CRUCE2, SC1]
must_haves:
  truths:
    - "El CLI de lobby carga SOLO contrapartes en audiencias confirmadas y sin sector_id (alto-ROI, incremental)"
    - "Re-correr con --limite mayor NO re-clasifica las mismas filas (sector_id is null las excluye)"
    - "El RUT-gate (assertNoRutInLlmInput PRIMERO) permanece intacto"
  artifacts:
    - path: "packages/cruces/src/clasificar-lobby-cli.ts"
      provides: "modo --solo-confirmadas en cargarContrapartes (join audiencia confirmada + sector_id is null)"
      contains: "solo-confirmadas"
    - path: "packages/cruces/src/clasificar-lobby-cli.test.ts"
      provides: "unit test del filtro (query shape + escape hatch filas)"
  key_links:
    - from: "packages/cruces/src/clasificar-lobby-cli.ts"
      to: "lobby_audiencia (estado_vinculo='confirmado')"
      via: "supabase-js !inner embed filter sobre lobby_contraparte.identificador"
      pattern: "lobby_audiencia.*inner|estado_vinculo"
---

<objective>
SC1 (precondición de código). Corregir el gap BLOQUEANTE del `clasificar-lobby-cli` documentado en RESEARCH Pitfall 1: hoy `cargarContrapartes` hace un `.from("lobby_contraparte").select(...).limit()` plano — sin filtro a audiencias confirmadas, sin `where sector_id is null`, sin avance de lote. Consecuencia medida contra PROD: solo 5.094/17.681 contrapartes distintas aparecen en audiencias confirmadas (las únicas que `materializar_cruces()` cuenta), y re-correr con `--limite` mayor re-lee desde el tope (re-paga las mismas filas MiniMax críticas).

Este plan añade un modo de carga `--solo-confirmadas` que carga únicamente contrapartes que (a) aparecen en un `lobby_audiencia` con `estado_vinculo='confirmado' and parlamentario_id is not null` Y (b) tienen `lobby_contraparte.sector_id is null`. El filtro `sector_id is null` hace la corrida naturalmente incremental y resumible: cada corrida clasifica lo pendiente, la siguiente lo omite.

Purpose: sin esta tarea, la única alternativa para SC1 es `--limite 17681` (~17k llamadas MiniMax, ~37% del cupo free semanal, horas de latencia serial, 71% señal-cero). Con ella, la corrida LIVE de 52-05 cubre las ~5.062 confirmadas-sin-sector en lotes acotados.
Output: CLI extendido + unit test del filtro. NO corre nada LIVE (eso es 52-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md
@.planning/phases/52-cruce2-cruces-nuevos/52-PATTERNS.md

<interfaces>
<!-- Contrato del schema (verificado en 0021_lobby.sql + 0038_sector.sql). El executor lo usa directo. -->

lobby_contraparte (0021:64-82, sector_id añadida por 0038:69):
  - identificador text NOT NULL  → FK a lobby_audiencia(identificador)  (la contraparte se enlaza al HECHO)
  - nombre text NOT NULL, rol text NOT NULL default ''
  - sector_id text NULL           → references sector(codigo); NULL = sin clasificar (D-05)

lobby_audiencia (0021:30-58):
  - identificador text            → clave natural de la audiencia
  - parlamentario_id text NULL    → FK; NULL cuando no hay enlace confirmado
  - estado_vinculo text           → 'confirmado' | 'no_confirmado' | null

Writer (writer-supabase.ts) — firma INTACTA, no se toca:
  actualizarSectorContraparte(identificador, nombre, sector_codigo, rol): Promise<void>

Escape hatch de tests (clasificar-lobby-cli.ts:83): `if (opts.filas !== undefined) return opts.filas;` — MANTENER intacto; los tests inyectan `filas`.
Flag-parse compartido: `parseArgs` vive en clasificar-fichas-cli.ts:70-104 y lobby lo re-exporta vía parseArgsBase (clasificar-lobby-cli.ts:21-23,64-66).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir flag --solo-confirmadas al parser compartido y a LobbyCliOptions</name>
  <read_first>
    - packages/cruces/src/clasificar-fichas-cli.ts:60-104 (parseArgs — el switch fail-fast que ambos CLIs comparten)
    - packages/cruces/src/clasificar-lobby-cli.ts:37-66 (LobbyCliOptions + re-export parseArgs)
  </read_first>
  <behavior>
    - Test: parseArgs(["--solo-confirmadas"]) → opts.soloConfirmadas === true
    - Test: parseArgs([]) → soloConfirmadas ausente/undefined (default off)
    - Test: parseArgs(["--solo-confirmadas","--limite","50"]) → { soloConfirmadas: true, limite: 50 }
    - Test: un flag desconocido sigue lanzando CrucesCliArgsError (no se rompe el fail-fast)
  </behavior>
  <action>
    Añadir un `case "--solo-confirmadas": opts.soloConfirmadas = true; break;` al switch de `parseArgs` en clasificar-fichas-cli.ts (espejo exacto del `case "--dry-run"` booleano). Añadir el campo opcional `soloConfirmadas?: boolean` a la interfaz de opciones de fichas (para que el parser base lo tipe) Y a `LobbyCliOptions` en clasificar-lobby-cli.ts:37-49. El parser es compartido (lobby re-exporta vía parseArgsBase) — un solo `case` cubre ambos CLIs. NO añadir el flag al CLI de fichas por comportamiento: fichas clasifica TODAS (74, decisión LOCKED); el flag solo altera la CARGA de lobby (Task 2). El flag es inerte en fichas.
  </action>
  <verify>
    <automated>pnpm --filter @obs/cruces test -- --run clasificar-lobby-cli</automated>
  </verify>
  <acceptance_criteria>
    - `parseArgs(["--solo-confirmadas"]).soloConfirmadas === true` verificado por test
    - default (sin flag) deja `soloConfirmadas` falsy
    - `CrucesCliArgsError` sigue lanzándose ante flag desconocido (test existente verde)
  </acceptance_criteria>
  <done>El parser reconoce --solo-confirmadas; LobbyCliOptions lo expone; suite cruces verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rama de carga filtrada en cargarContrapartes (confirmadas + sector_id is null)</name>
  <read_first>
    - packages/cruces/src/clasificar-lobby-cli.ts:76-99 (cargarContrapartes — la carga plana a reemplazar/ramificar)
    - packages/cruces/src/clasificar-fichas-cli.ts:123-151 (cargarFichas — estilo de join embed supabase-js a copiar)
    - .planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md (Pitfall 1 + Runtime State Inventory: 5.062 confirmadas-sin-sector)
  </read_first>
  <behavior>
    - Test: con opts.filas inyectadas → cargarContrapartes las devuelve verbatim (escape hatch intacto, precede a cualquier query)
    - Test: con soloConfirmadas=true + un client mock, la query aplica (a) `.is("sector_id", null)` y (b) un embed !inner a lobby_audiencia con estado_vinculo='confirmado' y parlamentario_id no-null; el test asierta la forma de la query construida (spy sobre el builder encadenado)
    - Test: con soloConfirmadas falsy → conserva la carga plana actual (no regresión)
    - Test: el mapeo de filas preserva identificador/nombre/rol (rol omitido cuando null)
  </behavior>
  <action>
    Ramificar `cargarContrapartes`: mantener el escape hatch `if (opts.filas !== undefined) return opts.filas;` y el early-return dry-run-sin-client SIN cambios. Cuando `opts.soloConfirmadas` es true, construir la carga filtrada con el embed !inner de supabase-js sobre `lobby_contraparte`: seleccionar `identificador, nombre, rol` + un embed `lobby_audiencia!inner(estado_vinculo, parlamentario_id)`, aplicar `.is("sector_id", null)`, `.eq("lobby_audiencia.estado_vinculo", "confirmado")`, `.not("lobby_audiencia.parlamentario_id", "is", null)`, y `.limit(limite)`. El `sector_id is null` es load-bearing: hace la corrida incremental (RESEARCH Pitfall 1 — cada corrida excluye lo ya clasificado; re-correr AVANZA en vez de re-pagar). Mapear el resultado a `ContrapartePorClasificar` igual que la rama plana (rol omitido si null). NO cambiar la firma del writer ni el loop de clasificación (líneas 149-168): `clasificarContraparte` sigue corriendo `assertNoRutInLlmInput` PRIMERO (línea 152) — NO envolver en try/catch, un nombre sucio DEBE abortar. La rama plana (sin flag) queda como fallback intacto.
  </action>
  <verify>
    <automated>pnpm --filter @obs/cruces test -- --run clasificar-lobby-cli</automated>
  </verify>
  <acceptance_criteria>
    - Test asierta que soloConfirmadas produce una query con `.is("sector_id", null)` + embed !inner a lobby_audiencia confirmada + parlamentario_id no-null
    - Escape hatch `opts.filas` sigue devolviendo las filas inyectadas sin tocar el client
    - RUT-gate: `clasificarContraparte` no está envuelto en try/catch (grep confirma la ausencia de captura alrededor de la línea 152)
    - Sin flag, la carga plana original se preserva (test de no-regresión verde)
  </acceptance_criteria>
  <done>cargarContrapartes filtra a confirmadas-sin-sector bajo --solo-confirmadas; incremental; RUT-gate intacto; suite cruces verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nombre de contraparte → LLM (MiniMax) | texto de tercero cruza a un proveedor externo; puede contener RUT |
| CLI → Supabase (service_role) | lectura de tabla PII deny-by-default (bypassa RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | nombre de contraparte con RUT enviado al LLM | mitigate | `assertNoRutInLlmInput` corre PRIMERO en `clasificarContraparte` (línea 152); NO envolver en try/catch (un RUT aborta con 0 llamadas). El filtro nuevo no toca esta ruta. |
| T-52-02 | Information Disclosure | MINIMAX_API_KEY / SUPABASE_SECRET_KEY en logs | mitigate | keys leídas de env (línea 108/118), nunca interpoladas en logs de error; sin cambios en este plan. |
| T-52-03 | Tampering | inyección vía nombre de tabla/columna en la query filtrada | accept | columnas/tablas son literales de código, no input; supabase-js parametriza; sin string-concat SQL. |
</threat_model>

<verification>
- `pnpm --filter @obs/cruces test -- --run` verde (incluye el nuevo clasificar-lobby-cli.test.ts).
- `pnpm --dir app exec tsc -b` limpio (o el tsc del workspace cruces): sin errores de tipo por el campo nuevo.
- grep: `assertNoRutInLlmInput` no está envuelto en try/catch.
</verification>

<success_criteria>
- El CLI de lobby soporta `--solo-confirmadas` y carga únicamente contrapartes confirmadas-sin-sector.
- La corrida es incremental (sector_id is null excluye lo ya clasificado).
- El RUT-gate y la firma del writer permanecen intactos.
- Suite de @obs/cruces verde.
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-01-SUMMARY.md` when done
</output>
