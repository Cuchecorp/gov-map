---
phase: 52-cruce2-cruces-nuevos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/lobby-en-tramitacion.tsx
  - app/components/lobby-en-tramitacion.test.tsx
  - app/components/estado-actual-block.tsx
  - app/components/estado-actual-block.test.ts
  - app/app/proyecto/[boletin]/page.tsx
autonomous: true
requirements: [CRUCE2, SC2, SC3]
must_haves:
  truths:
    - "La ficha de proyecto muestra un carril mt-12 'Reuniones de lobby registradas en el mismo período' con caveat anti-causal 1×/sección, fuente por fila"
    - "El carril degrada a null (nodo ausente) cuando la RPC no está aplicada (PGRST202), throw #34 en error real, empty-state honesto con 0 filas"
    - "El bloque '¿Dónde está hoy?' muestra 'Citado en {comisión} el {fecha}' cuando hay citación vigente/futura, y omite la línea si no derivable"
  artifacts:
    - path: "app/components/lobby-en-tramitacion.tsx"
      provides: "LobbyEnTramitacionSection (RSC) + LobbyEnTramitacionView (pura) consumiendo la RPC lobby_en_tramitacion"
      min_lines: 80
    - path: "app/components/estado-actual-block.tsx"
      provides: "citacionVigente derivada + línea SC3"
      contains: "citacionVigente"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "<section id='lobby-tramitacion' className='mt-12'> con Suspense + skeleton"
      contains: "lobby-tramitacion"
  key_links:
    - from: "app/components/lobby-en-tramitacion.tsx"
      to: "rpc lobby_en_tramitacion"
      via: "sb.rpc('lobby_en_tramitacion', { p_boletin: boletin })"
      pattern: "lobby_en_tramitacion"
    - from: "app/components/estado-actual-block.tsx"
      to: "citacion_punto × citacion"
      via: ".from('citacion_punto').select('citacion:citacion(comision, fecha, semana_iso)')"
      pattern: "citacion_punto"
---

<objective>
SC2 + SC3. Dos superficies en la ficha de proyecto (`/proyecto/[boletin]`), ambas yuxtaposición de hechos fechados con fuente, cero causal:

- **SC2 — carril lobby×tramitación:** nuevo `<section id="lobby-tramitacion" className="mt-12">` (hermano de `#votaciones`/`#idea-matriz`) que consume la RPC `lobby_en_tramitacion(p_boletin)` (contrato LOCKED por 52-02) y muestra "Reuniones de lobby registradas en el mismo período" — audiencias en la misma semana ISO en que una comisión vio el boletín. Caveat obligatorio 1×/sección ("coincidencia temporal; no implica relación"), fuente por fila, nombre del parlamentario en texto plano (no enlazado en este carril). **Degrade honesto pre-apply** (la RPC no está en PROD hasta 52-06): PGRST202/function-missing → render null; otro error → throw #34; 0 filas → empty-state honesto.
- **SC3 — línea de citación:** una línea derivable dentro del bloque F51 `EstadoActualBlock` ("¿Dónde está hoy?"): "Citado en {comisión} el {fecha}." cuando hay una citación vigente/futura (`citacion.fecha >= hoy`), leída por `.from("citacion_punto") × citacion` (tablas no-PII). Omite la línea si no derivable (espejo de la omisión honesta F51).

Purpose: convierte cruces ya posibles con datos existentes en superficie ciudadana sin insinuar causalidad. Todo el diseño está LOCKED en 52-UI-SPEC (§SC2/§SC3/§Degrade honesto/§Anti-insinuación).
Output: 1 componente nuevo + estado-actual extendido + page wiring + RTL. El carril degrada honesto hasta que 52-06 aplique 0048.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-UI-SPEC.md
@.planning/phases/52-cruce2-cruces-nuevos/52-PATTERNS.md
@app/components/cruces-de-parlamentario.tsx
@app/components/estado-actual-block.tsx

<interfaces>
<!-- Contrato de la RPC (LOCKED por 52-02, consumir verbatim): -->
lobby_en_tramitacion(p_boletin text) returns rows of:
  { parlamentario_nombre: string, camara: string, materia: string | null,
    fecha_reunion: string, semana_iso: string, comision: string, enlace_detalle: string | null }

<!-- Analogs a copiar: -->
cruces-de-parlamentario.tsx:
  - split PURO (CrucesView) + Server Component (CrucesSection); RTL testea la vista pura; NO "use client".
  - content-gate header 9 puntos (líneas 18-42): copiar verbatim (carril aislado, cero causal, provenance/fila, empty=hecho).
  - CrucesSection (177-192): `const sb = createServerSupabase(); const { data, error } = await sb.rpc(...)`.
  - IMPORTANTE divergencia: CrucesSection hace throw en CUALQUIER error (183). LobbyEnTramitacionSection NO — distingue PGRST202 (→ null) de error real (→ throw). Ver §Degrade honesto.

estado-actual-block.tsx (self-extend):
  - EstadoActual interface (20-27): añadir `citacionVigente?: { comision: string; fecha: Date }`
  - derivarEstadoActual (72-105): PURO, exportado, testeado — patrón omit-when-not-derivable (nunca "—")
  - EstadoActualBlock (156-194): Promise.all de reads no-PII; error real → throw #34 (175-184)
  - EstadoActualView (112-149): render guardado `{citacionVigente && <p>...}`; fecha en font-mono vía fechaCorta

proyecto page (app/app/proyecto/[boletin]/page.tsx):
  - EstadoActualBlock ya montado (61-63); NO re-montar (la línea SC3 va DENTRO del block, no toca esta sección)
  - patrón de carril: `<section id="..." className="mt-12"><h2/><Suspense fallback={<XSkeleton/>}><XSection boletin={boletin}/></Suspense></section>` (65-97)
  - copiar IdeaMatrizSkeleton (335-343) para el skeleton del carril lobby
  - BOLETIN_RE.test(boletin) ya valida el path (37-39)

ProvenanceBadge: sourceUrl = enlace_detalle de la fila. fechaCorta(new Date(fecha)) de @/lib/format.

Copy LOCKED (52-UI-SPEC §SC2): heading "Reuniones de lobby registradas en el mismo período"; caveat "Se muestran por coincidencia de fechas: en la misma semana en que una comisión vio este proyecto. La coincidencia temporal no implica relación entre la reunión y la tramitación del proyecto."; summary "En la misma semana en que la comisión vio este proyecto se registraron {N} reuniones de lobby."; per-row "{nombre} — {materia}" + "Reunión registrada el {fecha} · semana {semanaISO}"; empty "No se registran reuniones de lobby en las semanas en que una comisión vio este proyecto, según las fuentes consultadas."; SC3 line "Citado en {comisión} el {fecha}.".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LobbyEnTramitacionSection + View (carril SC2, degrade honesto)</name>
  <read_first>
    - app/components/cruces-de-parlamentario.tsx (espejo completo: view pura + section + content-gate)
    - app/components/provenance-badge.tsx (props de ProvenanceBadge)
    - .planning/phases/52-cruce2-cruces-nuevos/52-UI-SPEC.md (§SC2 copy + §Degrade honesto + §Anti-insinuación)
  </read_first>
  <behavior>
    - Test (vista pura): con N filas agrupa/renderiza el caveat 1×, el summary con {N} en Mono, una fila por audiencia con nombre + materia + "Reunión registrada el {fecha} · semana {semanaISO}" + link "Ver fuente oficial ↗" a enlace_detalle
    - Test (vista pura): 0 filas → empty-state honesto ("No se registran reuniones... según las fuentes consultadas."), caveat presente, sin filas fabricadas
    - Test (degrade): Section con error.code='PGRST202' → retorna null (render vacío, nodo ausente)
    - Test (degrade): Section con error mensaje ~ /does not exist|schema cache/ → retorna null
    - Test (degrade): Section con error real (otro code) → throw
    - Test (negative-match): el output no contiene vocabulario causal/afinidad (banned-vocab §6): "a cambio de","influyó","gestionó","presionó","afinidad","porque"
  </behavior>
  <action>
    Crear `app/components/lobby-en-tramitacion.tsx` espejo de cruces-de-parlamentario.tsx: header content-gate 9 puntos (copiar y adaptar: carril aislado mt-12, cero causal, provenance por fila, empty=hecho, nombre público plano NO enlazado). Definir `LobbyEnTramitacionRow` (los 7 campos del contrato) y una vista PURA `LobbyEnTramitacionView({ rows })` + Server Component `LobbyEnTramitacionSection({ boletin })`. NO "use client".

    Vista: renderizar el caveat neutro sobre banda `--muted` (`text-sm`) UNA vez arriba; el summary line con `{N}` en Mono; si el mismo boletín aparece en >1 semana ISO, agrupar por semana y repetir el summary por semana ("Semana {semanaISO} · comisión {comisión}: {N} reuniones."). Por fila: `{nombre} — {materia}` (nombre peso h3, materia text-sm) + línea Mono "Reunión registrada el {fechaCorta(fecha_reunion)} · semana {semana_iso}" + ProvenanceBadge/link "Ver fuente oficial ↗" (min-h-11) a `enlace_detalle`. Empty-state honesto (0 filas) con el copy LOCKED. El nombre del parlamentario es texto plano — NO enlazado en este carril (contexto de yuxtaposición, no atribución). Cero composición con voto/tally/declaración.

    Section: `const sb = createServerSupabase(); const { data, error } = await sb.rpc("lobby_en_tramitacion", { p_boletin: boletin });`. **Degrade honesto (LOAD-BEARING, §Degrade honesto):** `if (error?.code === 'PGRST202' || /does not exist|schema cache/i.test(error?.message ?? '')) return null;` (path 1, RPC ausente pre-apply). `if (error) throw new Error(...)` (path 3, #34 — error real ≠ "sin datos"). Con data → `<LobbyEnTramitacionView rows={data ?? []} />` (path 2, incluye empty-state). NO usar blanket-catch.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run lobby-en-tramitacion</automated>
  </verify>
  <acceptance_criteria>
    - LobbyEnTramitacionView renderiza caveat 1×, summary Mono {N}, filas con provenance por fila; 0 filas → empty-state honesto
    - Section retorna null ante PGRST202 y ante mensaje function-missing; throw ante error real; render con data
    - grep negative-match: sin banned-vocab causal en el componente ni en el copy
    - nombre del parlamentario NO envuelto en <a>/Link dentro del carril
    - RTL verde
  </acceptance_criteria>
  <done>Carril lobby×tramitación construido, degrade honesto de 3 caminos, anti-insinuación intacta, RTL verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Línea de citación SC3 en EstadoActualBlock (omit-when-not-derivable)</name>
  <read_first>
    - app/components/estado-actual-block.tsx (self: interface, derivarEstadoActual, EstadoActualView, EstadoActualBlock)
    - app/components/estado-actual-block.test.ts (extender para el caso citación)
    - .planning/phases/52-cruce2-cruces-nuevos/52-UI-SPEC.md (§SC3 copy + omission rule)
  </read_first>
  <behavior>
    - Test (derivación pura): dada una citación con fecha >= hoy → citacionVigente = { comision, fecha }; la línea se renderiza
    - Test: sin citación vigente/futura (todas fecha < hoy, o ninguna) → citacionVigente ausente → línea OMITIDA (nunca "—")
    - Test: varias citaciones futuras → se elige la más próxima (menor fecha >= hoy)
    - Test (view): con citacionVigente presente renderiza "Citado en {comision} el {fecha}." con fecha en font-mono
  </behavior>
  <action>
    Extender `EstadoActual` (líneas 20-27) con `citacionVigente?: { comision: string; fecha: Date }`. Añadir una función pura de derivación (o parámetro a derivarEstadoActual) que reciba las filas de citación crudas y devuelva la citación futura/vigente más cercana (`fecha >= hoy`, mínima), u omita si none — espejo del patrón omit-when-not-derivable de `etapaLinea`/`ultimoHito`. En `EstadoActualBlock` (Promise.all, 159-171) añadir una TERCERA query no-PII (guard OK): `sb.from("citacion_punto").select("citacion:citacion(comision, fecha, semana_iso)").eq("boletin", boletin)`; un error real de lectura → throw #34 (nueva rama espejo de las existentes). Mapear el embed a filas planas y pasar a la derivación. En `EstadoActualView` (112-149) renderizar `{citacionVigente && <p>Citado en {comision} el <span className="font-mono">{fechaCorta(fecha)}</span>.</p>}`. La línea se OMITE por completo si `citacionVigente` es ausente (nunca "—", nunca fabricada).
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run estado-actual-block</automated>
  </verify>
  <acceptance_criteria>
    - `citacionVigente` en la interfaz EstadoActual; derivación pura elige la citación futura más próxima
    - sin citación futura → línea omitida (test verde; sin "—")
    - error real de lectura de citación → throw #34
    - la línea usa el copy LOCKED "Citado en {comisión} el {fecha}." con fecha Mono
    - RTL/unit verde
  </acceptance_criteria>
  <done>Línea de citación derivable dentro de "¿Dónde está hoy?", omit-when-not-derivable, throw #34, tests verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring del carril lobby en la page de proyecto</name>
  <read_first>
    - app/app/proyecto/[boletin]/page.tsx (patrón de sección mt-12 + Suspense + skeleton, 65-97; IdeaMatrizSkeleton 335-343)
    - app/components/lobby-en-tramitacion.tsx (recién creado — el componente a montar)
  </read_first>
  <action>
    En `app/app/proyecto/[boletin]/page.tsx`: importar `LobbyEnTramitacionSection`. Añadir un nuevo `<section id="lobby-tramitacion" className="mt-12">` como hermano de los carriles existentes (colocarlo después de `#votaciones` o tras el timeline, antes de `#similares` — Claude's discretion dentro del orden de carriles; NO anidado en ningún otro carril). El heading h2 lo emite el propio componente (o la section, siguiendo el patrón del repo — elegir consistente con cruces-de-parlamentario montado en la ficha de parlamentario). Envolver en `<Suspense fallback={<LobbyTramitacionSkeleton />}>`; añadir el skeleton copiando `IdeaMatrizSkeleton`. NO tocar la sección de EstadoActualBlock (la línea SC3 ya vive dentro del block por Task 2). NO añadir `force-dynamic` (la page ya es dinámica por segmento `[boletin]`); confirmar que no hay static baking del carril. El caveat/degrade viven en el componente — la page solo monta la sección.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run proyecto && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `<section id="lobby-tramitacion" className="mt-12">` presente, hermano (no anidado) de los demás carriles
    - LobbyEnTramitacionSection montado bajo Suspense con skeleton
    - EstadoActualBlock intacto (sin re-montaje)
    - `tsc -b` limpio; tests de page verdes
  </acceptance_criteria>
  <done>Carril lobby montado en la ficha de proyecto como hermano mt-12; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC pre-apply → UI build | la RPC no existe en PROD hasta 52-06; el HTML horneado debe degradar sin 500 |
| datos de lobby/citación → render ciudadano | yuxtaposición fechada que no debe leerse como causal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | Repudiation (integridad narrativa) | insinuación causal lobby↔tramitación | mitigate | caveat obligatorio 1×/sección; carril mt-12 propio nunca compuesto con voto; nombre no enlazado; negative-match banned-vocab en RTL. |
| T-52-09 | Denial of Service / integridad | RPC ausente pre-apply causa 500 o banda fabricada | mitigate | degrade honesto de 3 caminos: PGRST202/function-missing→null, 0 filas→empty honesto, error real→throw #34. Sin blanket-catch. |
| T-52-10 | Information Disclosure | lectura PII directa desde app/ (citación) | accept | `citacion`/`citacion_punto` son no-PII (0010, public-read) — permitidas por el guard; el carril lobby lee vía RPC allowlisted, nunca `.from()` PII. |
| T-52-11 | Tampering | boletín no confiable en el path | mitigate | BOLETIN_RE.test ya gatea la page; `p_boletin` va como arg parametrizado a la RPC. |
</threat_model>

<verification>
- `pnpm --dir app test -- --run lobby-en-tramitacion estado-actual-block proyecto` verde.
- `pnpm --dir app exec tsc -b` limpio.
- negative-match banned-vocab sobre el copy nuevo (sin causal/afinidad/score).
- Suite app/ completa verde en el merge de wave (baseline 497 + nuevos).
</verification>

<success_criteria>
- Carril lobby×tramitación en la ficha de proyecto con caveat 1×, fuente por fila, degrade honesto pre-apply.
- Línea de citación SC3 en "¿Dónde está hoy?" con omit-when-not-derivable.
- Anti-insinuación intacta (cero composición, cero causal, mt-12 frontier).
- tsc limpio, RTL verde.
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-03-SUMMARY.md` when done
</output>
