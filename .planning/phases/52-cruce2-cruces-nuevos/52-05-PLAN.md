---
phase: 52-cruce2-cruces-nuevos
plan: 05
type: execute
wave: 2
depends_on: [52-01]
files_modified: []
autonomous: true
requirements: [CRUCE2, SC1]
must_haves:
  truths:
    - "El golden gate LIVE (cobertura ≥0.7, errores=0) pasó ANTES de la corrida masiva"
    - "El clasificador corrió LIVE sobre las contrapartes confirmadas-sin-sector + las 74 proyecto_ficha"
    - "cruce_senal re-materializado con cobertura real (>>30 señales), verificado por psql read-only"
    - "Conteos before/after pegados en el SUMMARY"
  artifacts:
    - path: ".planning/phases/52-cruce2-cruces-nuevos/52-05-SUMMARY.md"
      provides: "conteos before/after de cruce_senal + distinct parlamentarios/sectores + muestra de evidencia"
      contains: "cruce_senal"
  key_links:
    - from: "clasificar-lobby-cli --solo-confirmadas"
      to: "cruces.materializar_cruces()"
      via: "sector_id poblado → FULL REBUILD por psql"
      pattern: "materializar_cruces"
---

<objective>
SC1 (corrida LIVE de datos por el agente). Ejecutar localmente el clasificador sectorial ya escrito (con el fix `--solo-confirmadas` de 52-01), re-materializar `cruce_senal` con cobertura real (>>30 señales), y verificar el resultado por psql read-only. Esta es una escritura de DATOS no-DDL — el patrón LOCKED de corridas LIVE previas (lobby 0→5.106, NET, agenda 36-04); la autonomía está LOCKED en CONTEXT.

Secuencia golden (LOCKED, RESEARCH Pattern 1, verbatim de 36-04):
1. **Golden gate LIVE** (`CRUCES_GOLDEN_LIVE=1`, cobertura ≥0.7 sobre muestra 10, errores=0) ANTES de cualquier corrida masiva.
2. **Dry-run acotado** de ambos CLIs para confirmar la selección filtrada.
3. **Corrida LIVE** en lotes acotados (`--solo-confirmadas --limite N`) sobre las ~5.062 contrapartes confirmadas-sin-sector (MiniMax free 45k/sem alcanza) + `clasificar-fichas-cli --limite 74` sobre TODAS las fichas (DeepSeek bulk).
4. **Materializar** `select cruces.materializar_cruces();` por psql.
5. **Verificación** psql read-only (conteos before/after, distinct, muestra de evidencia) pegada en el SUMMARY.

Purpose: hoy `cruce_senal` tiene 30 señales porque solo 34/17.681 contrapartes tienen sector. Esta corrida des-raquitiza el cruce sector que YA está LIVE (0042 + Candado B aplicados). NINGÚN flag se flipea; ninguna DDL.
Output: SUMMARY con conteos before/after. Cero cambios de código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md
@.planning/phases/52-cruce2-cruces-nuevos/52-PATTERNS.md
@.planning/phases/36-cruces/36-04-SUMMARY.md

<interfaces>
<!-- Comandos exactos (RESEARCH Pattern 1 / PATTERNS §Agent-run LIVE). tsx es devDep de @obs/cruces, NO en root → correr por el filtro del workspace. -->

# Golden gate LIVE (antes de la corrida masiva):
CRUCES_GOLDEN_LIVE=1 pnpm --filter @obs/cruces test -- --run golden   # cobertura ≥0.7, errores=0, testTimeout 120s

# Dry-run acotado:
node scripts/run-with-env.mjs pnpm --filter @obs/cruces exec tsx src/clasificar-lobby-cli.ts --solo-confirmadas --limite N --dry-run
node scripts/run-with-env.mjs pnpm --filter @obs/cruces exec tsx src/clasificar-fichas-cli.ts --limite 74 --dry-run

# Corrida LIVE (--service-key real):
node scripts/run-with-env.mjs pnpm --filter @obs/cruces exec tsx src/clasificar-lobby-cli.ts --solo-confirmadas --limite N --service-key "$SUPABASE_SECRET_KEY"
node scripts/run-with-env.mjs pnpm --filter @obs/cruces exec tsx src/clasificar-fichas-cli.ts --limite 74 --service-key "$SUPABASE_SECRET_KEY"

# Materializar (psql directo, NUNCA supabase db push):
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select cruces.materializar_cruces();"

# Verificación read-only:
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from cruce_senal; select count(distinct parlamentario_id) from cruce_senal; select count(distinct sector_id) from cruce_senal;"

Universo (RESEARCH, PROD 2026-07-03):
  contrapartes_total 17681 · con_sector 34 · confirmadas_sin_sector 5062 · cruce_senal 30 · proyecto_ficha 74 (0 con sector)

Env presentes (nombres confirmados en .env): MINIMAX_API_KEY, DEEPSEEK_API_KEY, SUPABASE_SECRET_KEY, SUPABASE_DB_URL, SUPABASE_URL. psql 17.9.
Windows: PowerShell — usar `$env:VAR` para variables; run-with-env.mjs carga .env → process.env (BOM-safe). Para el golden gate LIVE en PowerShell, setear `$env:CRUCES_GOLDEN_LIVE=1` antes de invocar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden gate LIVE + dry-run acotado (gate antes de escribir)</name>
  <read_first>
    - packages/cruces/src/golden/golden-set.ts (COBERTURA_MIN=0.7, gate LIVE por CRUCES_GOLDEN_LIVE==="1")
    - .planning/phases/36-cruces/36-04-SUMMARY.md (la corrida original verbatim)
    - .planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md (Assumptions A4: latencia serial ~5.062 filas)
  </read_first>
  <action>
    (1) Correr el golden gate LIVE con `CRUCES_GOLDEN_LIVE=1` (cobertura ≥0.7 sobre muestra 10, errores=0). Si NO pasa → ABORTAR el plan y reportar (no escribir nada). (2) Correr el dry-run acotado de `clasificar-lobby-cli --solo-confirmadas --limite N --dry-run` y confirmar por el log que la selección son contrapartes confirmadas-sin-sector (procesados > 0, cobertura muestra razonable). (3) Dry-run de `clasificar-fichas-cli --limite 74 --dry-run`. Elegir el tamaño de lote `N` (Claude's discretion — RESEARCH sugiere lotear por sesión si la latencia serial es prohibitiva; respetar el rate-limit del provider). NO escribir DB en esta task.

    Antes de correr, capturar los conteos BEFORE por psql read-only (cruce_senal, distinct parlamentarios/sectores, contrapartes con_sector, proyecto_ficha con_sector) para el SUMMARY.
  </action>
  <verify>
    <automated>echo "golden gate + dry-run — evidencia en logs; conteos BEFORE capturados por psql read-only"</automated>
  </verify>
  <acceptance_criteria>
    - golden gate LIVE PASÓ (cobertura ≥0.7, errores=0) — evidencia en log
    - dry-run de lobby muestra selección confirmadas-sin-sector (procesados > 0)
    - conteos BEFORE capturados (cruce_senal=30, con_sector=34, proyecto_ficha con_sector=0 esperados)
    - CERO escritura a DB en esta task
  </acceptance_criteria>
  <done>Golden gate verde + dry-run confirmado + conteos BEFORE capturados. Sin escrituras.</done>
</task>

<task type="auto">
  <name>Task 2: Corrida LIVE del clasificador + materializar_cruces() + verificación</name>
  <read_first>
    - .planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md (Pattern 1 comandos + Runtime State Inventory)
    - packages/cruces/src/clasificar-lobby-cli.ts (el --solo-confirmadas de 52-01, incremental)
  </read_first>
  <action>
    Correr LIVE (`--service-key "$SUPABASE_SECRET_KEY"`): `clasificar-lobby-cli --solo-confirmadas --limite N` en lotes hasta cubrir las ~5.062 confirmadas-sin-sector (el filtro `sector_id is null` de 52-01 hace cada lote incremental — re-correr AVANZA). Luego `clasificar-fichas-cli --limite 74` (todas las fichas). Ambos por `node scripts/run-with-env.mjs`. Vigilar el cupo MiniMax (45k/sem) y la latencia serial (A4) — lotear por sesión si es prohibitivo. El RUT-gate (`assertNoRutInLlmInput`) aborta filas sucias por diseño; no capturar.

    Al terminar la clasificación: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select cruces.materializar_cruces();"` (FULL REBUILD transaccional server-side; NUNCA supabase db push). Es idempotente.

    Verificación read-only (pegar AFTER en el SUMMARY): `select count(*) from cruce_senal;` (esperado >>30), `select count(distinct parlamentario_id) from cruce_senal;`, `select count(distinct sector_id) from cruce_senal;`, `select count(*) from lobby_contraparte where sector_id is not null;`, `select count(*) from proyecto_ficha where sector_id is not null;`, y una muestra de 3-5 filas de evidencia de `cruce_senal` (sin PII — solo conteo/sector/parlamentario_id/evidencia). Escribir el SUMMARY con la tabla before/after y la muestra.
  </action>
  <verify>
    <automated>echo "corrida LIVE + materializar + verificación — conteos AFTER en psql read-only pegados al SUMMARY (cruce_senal >> 30)"</automated>
  </verify>
  <acceptance_criteria>
    - clasificar-lobby-cli --solo-confirmadas corrió LIVE cubriendo las confirmadas-sin-sector; clasificar-fichas-cli cubrió las 74
    - `select cruces.materializar_cruces()` ejecutado por psql (no db push)
    - `count(*) from cruce_senal` >> 30 verificado por psql read-only
    - conteos before/after + muestra de evidencia (sin PII) pegados en el SUMMARY
    - CERO DDL, CERO flag flipeado, CERO `.from()` a cruce_senal desde app/ (verificación es por psql)
  </acceptance_criteria>
  <done>cruce_senal re-materializado con cobertura real (>>30 señales), verificado por psql, conteos en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nombre de contraparte → MiniMax | texto de tercero a proveedor externo; puede contener RUT |
| CLI/psql → PROD (service_role) | escritura de datos + FULL REBUILD sobre tablas PII deny-by-default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-16 | Information Disclosure | RUT de contraparte enviado al LLM | mitigate | `assertNoRutInLlmInput` PRIMERO (heredado de 52-01/36); una fila con RUT aborta con 0 llamadas. |
| T-52-17 | Information Disclosure | verificación por `.from(cruce_senal)` desde app/ (PII) | mitigate | verificación EXCLUSIVAMENTE por psql read-only; nunca `.from()` desde app/; cruce_senal es PII_TABLE. |
| T-52-18 | Tampering | corrida no idempotente corrompe cruce_senal | accept | `materializar_cruces()` es FULL REBUILD transaccional (idempotente); sector_id UPDATE es idempotente (clave natural). Re-correr converge. |
| T-52-19 | Elevation of Privilege | escritura toca ACL/flags | mitigate | corrida es DATA-only (sector_id + rebuild); CERO DDL, CERO grant, CERO flip de `*_PUBLIC_ENABLED` (doctrina LOCKED). |
</threat_model>

<verification>
- Golden gate LIVE verde antes de escribir.
- `count(*) from cruce_senal` >> 30 (psql read-only).
- distinct parlamentarios/sectores crecen respecto a 24/10.
- SUMMARY contiene la tabla before/after + muestra de evidencia sin PII.
- CERO cambios de código (git status limpio salvo el SUMMARY).
</verification>

<success_criteria>
- Golden gate LIVE pasó; corrida LIVE cubrió confirmadas-sin-sector + 74 fichas.
- cruce_senal re-materializado con >>30 señales, verificado por psql.
- Conteos before/after en el SUMMARY; cero DDL, cero flag flipeado.
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-05-SUMMARY.md` when done
</output>
