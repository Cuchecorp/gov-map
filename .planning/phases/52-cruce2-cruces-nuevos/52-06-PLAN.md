---
phase: 52-cruce2-cruces-nuevos
plan: 06
type: execute
wave: 2
depends_on: [52-02]
files_modified: []
autonomous: false
requirements: [CRUCE2, SC2, SC5]
must_haves:
  truths:
    - "0047 y 0048 aplicadas a PROD por psql --db-url --single-transaction (NUNCA supabase db push)"
    - "Ambos pgTAP (0047 + 0048) verdes contra el schema aplicado"
    - "schema_migrations stampeada para 0047 y 0048"
    - "anon sigue sin EXECUTE sobre lobby_en_tramitacion (Camino A deny verificado)"
  artifacts:
    - path: ".planning/phases/52-cruce2-cruces-nuevos/52-06-SUMMARY.md"
      provides: "resultado del apply + salida de ambos pgTAP + stamping"
      contains: "0048"
  key_links:
    - from: "supabase/migrations/0048_lobby_en_tramitacion.sql"
      to: "PROD schema"
      via: "psql --db-url --single-transaction -v ON_ERROR_STOP=1 -f"
      pattern: "psql"
---

<objective>
SC2/SC5 — checkpoint de operador (DDL = humano, LOCKED). Aplicar a PROD las migraciones 0047 (rebeldías honestas, pendiente de F51) y 0048 (lobby_en_tramitacion, escrita en 52-02) en un solo paso acumulado, correr ambos pgTAP contra el schema aplicado, y stampear `schema_migrations`. Tras el apply, el carril lobby×tramitación (52-03) deja de degradar a null y renderiza en vivo.

**Por qué es checkpoint humano:** el DDL vive en Postgres PROD; `tsc`/`pnpm test` NO prueban que Postgres ejecutó el DDL (falso positivo conocido). La única prueba válida es el pgTAP contra el schema aplicado. La aplicación va por `psql --db-url --single-transaction`, NUNCA `supabase db push` (drift de schema_migrations). Un agente NO ejecuta este apply.

Purpose: cerrar la deuda de apply de 0047 (F51) + 0048 (F52) en un solo paso operador; encender la RPC lobby_en_tramitacion. NINGÚN flag `*_PUBLIC_ENABLED` se toca (la RPC no está detrás de flag; degrada por presencia).
Output: SUMMARY con la salida del apply + ambos pgTAP verdes + stamping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md
@supabase/migrations/0047_rebeldias_honestas.sql
@supabase/migrations/0048_lobby_en_tramitacion.sql

<interfaces>
<!-- Comandos de apply (RESEARCH Pitfall 5 / cabecera 0047). Windows: PGCLIENTENCODING=UTF8. -->
# Aplicar (una transacción por migración; orden 0047 → 0048):
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0047_rebeldias_honestas.sql
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0048_lobby_en_tramitacion.sql

# pgTAP contra el schema aplicado (begin/…/rollback dentro del archivo):
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0047_rebeldias_honestas.test.sql
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0048_lobby_en_tramitacion.test.sql

# Stamp schema_migrations (patrón del repo — versión + name):
#   insert into supabase_migrations.schema_migrations (version, name) values ('0047', ...), ('0048', ...);

# Verificación post-apply (Camino A deny):
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select has_function_privilege('anon','public.lobby_en_tramitacion(text)','execute');"   # → f
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operador aplica 0047 + 0048 + corre pgTAP + stampea schema_migrations</name>
  <what-built>
    52-02 escribió la migración 0048 (RPC `lobby_en_tramitacion`, idiom 0047, PII-safe, doble revoke sin grant) + su pgTAP + la entrada en PUBLIC_RPC_ALLOWLIST. 0047 (rebeldías honestas, F51) sigue pendiente de apply. Ambas están listas para aplicarse a PROD en un solo paso acumulado. El agente NO puede ejecutar el DDL (checkpoint LOCKED).
  </what-built>
  <how-to-verify>
    1. Aplicar en orden por psql (una transacción por migración):
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0047_rebeldias_honestas.sql`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0048_lobby_en_tramitacion.sql`
    2. Correr ambos pgTAP contra el schema aplicado:
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0047_rebeldias_honestas.test.sql`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0048_lobby_en_tramitacion.test.sql`
       → todos los asserts `ok`, cero `not ok`.
    3. Verificar Camino A deny: `psql ... -c "select has_function_privilege('anon','public.lobby_en_tramitacion(text)','execute');"` → `f`.
    4. Stampear `supabase_migrations.schema_migrations` con las versiones 0047 y 0048.
    5. Confirmar que ninguna migración previa (≤0046) regresó (los pgTAP previos siguen verdes si se corren).
    NUNCA `supabase db push` (drift). PGCLIENTENCODING=UTF8 en Windows.
  </how-to-verify>
  <resume-signal>Escribe "aplicadas" con la salida de ambos pgTAP (plan/ok/finish) + el resultado de la verificación anon (f), o describe el error para replanificar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → PROD DDL | aplicación de migración fuera de CI; falso positivo de tsc/vitest |
| anon/REST → RPC nueva | superficie pública tras el apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-20 | Elevation of Privilege | 0048 concede EXECUTE a anon por DEFAULT PRIVILEGES tras el apply | mitigate | doble revoke en 0048; pgTAP assert (5) + verificación post-apply `has_function_privilege('anon',...)='f'` obligatoria antes de resume. |
| T-52-21 | Tampering | `supabase db push` produce drift de schema_migrations | mitigate | apply EXCLUSIVO por `psql --db-url --single-transaction`; stamping manual de versiones. |
| T-52-22 | Repudiation | apply parcial (0047 sí, 0048 no) deja estado inconsistente | mitigate | orden explícito 0047→0048, una transacción por archivo, ON_ERROR_STOP=1; SUMMARY registra ambas versiones stampeadas. |
</threat_model>

<verification>
- Ambos pgTAP verdes contra el schema aplicado (0047 + 0048).
- `has_function_privilege('anon','public.lobby_en_tramitacion(text)','execute')` = f.
- schema_migrations contiene 0047 y 0048.
- Carril lobby×tramitación (52-03) renderiza en vivo post-apply (deja de degradar a null).
</verification>

<success_criteria>
- 0047 + 0048 aplicadas a PROD por psql; pgTAP verdes; schema_migrations stampeada.
- anon sigue sin EXECUTE sobre lobby_en_tramitacion.
- Ningún flag `*_PUBLIC_ENABLED` tocado.
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-06-SUMMARY.md` when done
</output>
