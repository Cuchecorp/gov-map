---
phase: 52-cruce2-cruces-nuevos
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/actualidad-module.tsx
  - app/components/actualidad-module.test.tsx
  - app/app/page.tsx
autonomous: true
requirements: [CRUCE2, SC4]
must_haves:
  truths:
    - "El home muestra un módulo de actualidad de 3 bloques bajo el hero: 'Votado esta semana', 'Urgencias vigentes', 'Última actualización de datos'"
    - "Cada bloque degrada a un empty-state honesto propio e independiente"
    - "El home usa export const dynamic = 'force-dynamic' (gotcha F50)"
    - "Todas las lecturas son .from() sobre tablas NO-PII (votacion/tramitacion_evento/proyecto/citacion)"
  artifacts:
    - path: "app/components/actualidad-module.tsx"
      provides: "ActualidadModule (RSC) + VotadoEstaSemana + UrgenciasVigentes + UltimaActualizacion"
      min_lines: 90
    - path: "app/app/page.tsx"
      provides: "force-dynamic + <ActualidadModule/> bajo el hero (hero LOCKED intacto)"
      contains: "force-dynamic"
  key_links:
    - from: "app/components/actualidad-module.tsx"
      to: "votacion / tramitacion_evento / fecha_captura no-PII"
      via: ".from() directo + conteoVotacion + urgenciaVigente"
      pattern: "conteoVotacion|urgenciaVigente|fecha_captura"
---

<objective>
SC4. Añadir un módulo de actualidad al home (`/`) bajo el hero: 3 bloques compactos server-rendered que convierten la portada de "solo buscador" en razón de retorno diario (diagnóstico §2.5). Cero JS cliente nuevo, cero carrusel, cero RPC nueva.

- **Bloque 1 "Votado esta semana":** últimas votaciones con desenlace factual "El proyecto fue {resultado} {si}–{no}." (tally Mono en-dash vía `conteoVotacion`), fecha Mono, link fuente. `resultado` null → omite la frase, conserva el hecho fechado. Nunca ranking/"quién ganó".
- **Bloque 2 "Urgencias vigentes":** REUSA `urgenciaVigente(eventos)` de estado-actual-block sobre `tramitacion_evento`; "{título} — urgencia {tipo} vigente desde el {fecha}." + boletín Mono + link a la ficha.
- **Bloque 3 "Última actualización de datos":** `max(fecha_captura)` por fuente NO-PII, una línea por fuente. Transparencia de frescura, NO ranking de actividad.

Purpose: SC4 del ROADMAP; el home deja de ser solo un buscador. Diseño LOCKED en 52-UI-SPEC §SC4.
Output: 1 componente (module + 3 sub-bloques) + RTL + home extendido con force-dynamic. Hero (pills/copy) LOCKED intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-UI-SPEC.md
@.planning/phases/52-cruce2-cruces-nuevos/52-PATTERNS.md
@app/app/page.tsx
@app/components/estado-actual-block.tsx

<interfaces>
<!-- Helpers y patrones a reusar (verificados): -->
urgenciaVigente(eventos: TramitacionEventoRow[]): { tipo: string; desde: Date } | null   // estado-actual-block.tsx:43 (exportado)
conteoVotacion(si, no): string   // app/lib/format.ts:96 — tally en-dash Mono, banned-vocab-safe
fechaCorta / relativeTimeEs      // app/lib/format.ts — fechas Mono
createServerSupabase()           // service_role, server-only

force-dynamic idiom (red/page.tsx:37-43): `export const dynamic = "force-dynamic";`  // gotcha F50: ruta estática horneada = 500 con datos vivos

Shapes NO-PII (permitidas por el guard):
  votacion            → resultado, total_si, total_no, boletin, fecha, fecha_captura, origen  (RESEARCH: columnas verificadas en PROD)
  tramitacion_evento  → boletin, descripcion, fecha, fecha_captura, origen  (TramitacionEventoRow)
  proyecto            → boletin, titulo, fecha_captura
  citacion            → comision, fecha, semana_iso, fecha_captura
  lobby_audiencia / proyecto_ficha → fecha_captura  (no-PII, public-read)
  PROHIBIDAS (PII → guard falla): aporte, contrato, declaracion*, donante, cruce_senal, parlamentario

Home actual (app/app/page.tsx): solo hero SearchBox; EXAMPLE_CHIPS + copy LOCKED (líneas 22-66). El módulo se AÑADE bajo el `<section>` del hero, dentro de `<main>`; el hero NO se toca.

Copy LOCKED (52-UI-SPEC §SC4): headings "Votado esta semana"/"Urgencias vigentes"/"Última actualización de datos"; empties "Sin votaciones registradas esta semana en las fuentes consultadas."/"No hay urgencias vigentes registradas esta semana."/"Aún no hay registros de actualización disponibles."; item1 "El proyecto fue {resultado} {si}–{no}." + "Votación del {fecha}"; item2 "{título} — urgencia {tipo} vigente desde el {fecha}."; item3 "{fuente}: actualizada el {fecha}.".
Layout: cada sub-bloque `--card` panel `rounded-lg border p-6`, grid `grid gap-6 md:grid-cols-3` (apilado en móvil).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActualidadModule + 3 sub-bloques (lecturas no-PII, empty-states independientes)</name>
  <read_first>
    - app/components/estado-actual-block.tsx (urgenciaVigente exportada + patrón #34 throw)
    - app/lib/format.ts (conteoVotacion, fechaCorta, relativeTimeEs)
    - app/components/provenance-badge.tsx (props)
    - .planning/phases/52-cruce2-cruces-nuevos/52-UI-SPEC.md (§SC4 copy + layout + §Anti-insinuación)
  </read_first>
  <behavior>
    - Test (vista pura de cada bloque): con datos renderiza los items con desenlace/urgencia/frescura + provenance por item
    - Test: Bloque 1 con `resultado` null → omite "El proyecto fue..." pero conserva "Votación del {fecha}"
    - Test: cada bloque con 0 datos → su empty-state honesto propio (3 strings distintos), independiente de los otros
    - Test (negative-match): sin ranking/score/"los más …"/porcentaje-como-veredicto; tallies neutros Mono
    - Test (#34): un error real de lectura en un bloque → throw (no fabrica bloque vacío)
  </behavior>
  <action>
    Crear `app/components/actualidad-module.tsx`. Separar cada bloque en una vista PURA (testeable con fixtures) + un Server Component que lee. NO "use client". `ActualidadModule` compone los 3 bajo `grid gap-6 md:grid-cols-3`, cada sub-bloque un panel `rounded-lg border p-6` (`--card`). Cada bloque lee vía `createServerSupabase().from(<tabla NO-PII>)` con `.limit()` acotado + `.order("fecha"/"fecha_captura", { ascending: false })`; envolver reads compartidas en `React.cache` si se comparten. Un error real de lectura → `throw new Error(...)` (#34, espejo de VotacionesSection); NUNCA `?? []` que fabrique "sin datos" (salvo en el path de 0 filas legítimo, que es el empty-state honesto).

    - Bloque 1 "Votado esta semana": `.from("votacion").select("boletin, resultado, total_si, total_no, fecha")` top-N por fecha DESC de la semana vigente; por item título del proyecto (join/lookup a `proyecto` por boletin — no-PII) + desenlace "El proyecto fue {resultado} {conteoVotacion(total_si,total_no)}." (omitir la frase si `resultado` null) + "Votación del {fechaCorta(fecha)}" (Mono) + "Ver fuente oficial ↗". Cero slant por parlamentario, cero "quién ganó".
    - Bloque 2 "Urgencias vigentes": leer `tramitacion_evento` (no-PII) acotado; derivar urgencias vigentes por boletín reusando `urgenciaVigente`; por item "{título} — urgencia {tipo} vigente desde el {fechaCorta(desde)}." (Mono) + boletín Mono + link a `/proyecto/{boletin}`.
    - Bloque 3 "Última actualización de datos": `max(fecha_captura)` por fuente NO-PII (una query `.select("fecha_captura").order(...).limit(1)` por tabla de `votacion`/`proyecto`/`tramitacion_evento`/`citacion`/`lobby_audiencia`/`proyecto_ficha`); una línea "{fuente}: actualizada el {fechaCorta(fecha)}." por fuente. NUNCA leer fecha_captura de tablas PII (aporte/contrato/declaracion*/donante/cruce_senal/parlamentario) — el guard falla (Pitfall 4).

    Cada bloque degrada a su empty-state honesto propio (copy LOCKED) independiente; el módulo nunca se oculta entero. Provenance/link por item; un dato sin fuente no se muestra.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run actualidad-module</automated>
  </verify>
  <acceptance_criteria>
    - 3 bloques con vistas puras testeadas; cada uno con su empty-state honesto propio
    - Bloque 1 omite la frase de desenlace si resultado null, conserva el hecho fechado; tally vía conteoVotacion (Mono en-dash)
    - Bloque 2 reusa urgenciaVigente (import, no re-implementa la lógica)
    - Bloque 3 lee fecha_captura SOLO de tablas no-PII (grep: sin `.from("aporte"/"contrato"/"declaracion"/"donante"/"cruce_senal"/"parlamentario")`)
    - negative-match banned-vocab: sin ranking/score/porcentaje-veredicto
    - error real de lectura → throw
    - RTL verde
  </acceptance_criteria>
  <done>Módulo de actualidad de 3 bloques, lecturas no-PII, empty-states independientes, anti-insinuación intacta, RTL verde.</done>
</task>

<task type="auto">
  <name>Task 2: Montar ActualidadModule en el home con force-dynamic (hero LOCKED intacto)</name>
  <read_first>
    - app/app/page.tsx (hero completo; EXAMPLE_CHIPS + copy LOCKED)
    - app/app/red/page.tsx:37-43 (idiom force-dynamic + comentario de rationale)
    - app/components/actualidad-module.tsx (recién creado)
  </read_first>
  <action>
    En `app/app/page.tsx`: añadir `export const dynamic = "force-dynamic";` con un comentario que cite el gotcha F50 (ruta estática horneada con datos vivos = stale/500), espejo del comentario en red/page.tsx. Importar `ActualidadModule` y renderizarlo DENTRO de `<main>`, DEBAJO del `<section>` del hero, cada bloque bajo su propio `<Suspense>` si conviene (o el módulo entero bajo un Suspense con skeleton acotado). NO tocar el hero: `EXAMPLE_CHIPS`, el h1 con la cursiva petróleo, la trust line y el onboarding link son LOCKED (líneas 22-66) — quedan byte-idénticos. El módulo compone SOLO hechos de tramitación entre sí (sin lobby, sin composición con voto de un parlamentario específico).
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run app/page && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `export const dynamic = "force-dynamic"` presente en app/app/page.tsx
    - `<ActualidadModule/>` montado bajo el hero, dentro de `<main>`
    - hero (EXAMPLE_CHIPS + h1 + trust line + onboarding) sin cambios (git diff no toca líneas 22-66 salvo import/montaje)
    - `tsc -b` limpio; tests de home verdes
  </acceptance_criteria>
  <done>Home con módulo de actualidad bajo el hero + force-dynamic; hero LOCKED intacto; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de tramitación/votación → home ciudadano | hechos fechados que no deben leerse como ranking/juicio |
| lectura .from() en app/ → tabla | el guard prohíbe `.from()` sobre tablas PII (service_role bypassa RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-12 | Information Disclosure | fecha_captura leída de tabla PII dispara fuga/guard | mitigate | bloque 3 lee SOLO tablas no-PII (votacion/proyecto/tramitacion_evento/citacion/lobby_audiencia/proyecto_ficha); lockdown-guard verifica; grep de exclusión en acceptance. |
| T-52-13 | Repudiation (integridad narrativa) | el home rankea/juzga (más votado, quién ganó) | mitigate | counts neutros Mono; sin SUM-as-verdict/ranking/porcentaje-score; negative-match banned-vocab en RTL. |
| T-52-14 | Denial of Service | ruta estática horneada con datos vivos = 500 (F50) | mitigate | `export const dynamic = "force-dynamic"`; reads con `.limit()` acotado. |
| T-52-15 | Availability/integridad | un error de lectura fabrica bloque vacío | mitigate | error real → throw #34; empty-state honesto SOLO en 0 filas legítimo. |
</threat_model>

<verification>
- `pnpm --dir app test -- --run actualidad-module app/page` verde.
- `pnpm --dir app exec tsc -b` limpio.
- grep: bloque 3 no lee `.from()` de ninguna tabla PII.
- `pnpm --dir app test -- --run lib/lockdown-guard` verde (guard B no reporta `.from()` PII en app/).
- negative-match banned-vocab sobre el copy del módulo.
</verification>

<success_criteria>
- Módulo de actualidad de 3 bloques bajo el hero; empty-states honestos independientes.
- force-dynamic aplicado; hero LOCKED intacto.
- Lecturas 100% no-PII; anti-insinuación (cero ranking/score); tsc limpio; RTL verde.
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-04-SUMMARY.md` when done
</output>
