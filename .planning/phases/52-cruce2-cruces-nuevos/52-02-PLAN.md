---
phase: 52-cruce2-cruces-nuevos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0048_lobby_en_tramitacion.sql
  - supabase/tests/0048_lobby_en_tramitacion.test.sql
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [CRUCE2, SC2, SC5]
must_haves:
  truths:
    - "Existe una RPC lobby_en_tramitacion(p_boletin text) security definer PII-safe que devuelve audiencias de lobby en la misma semana ISO en que una comisión vio el boletín"
    - "La RPC emite SOLO nombre público + cámara del parlamentario (jamás rut/partido/email)"
    - "anon NO tiene EXECUTE (doble revoke, cero grant — Camino A)"
    - "lobby_en_tramitacion está en PUBLIC_RPC_ALLOWLIST"
  artifacts:
    - path: "supabase/migrations/0048_lobby_en_tramitacion.sql"
      provides: "RPC nueva idiom 0047 (create or replace + doble revoke + cero grant)"
      contains: "lobby_en_tramitacion"
    - path: "supabase/tests/0048_lobby_en_tramitacion.test.sql"
      provides: "pgTAP: firma, security definer, search_path, anon deny, no-PII, coincidencia por semana"
      contains: "has_function"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "lobby_en_tramitacion en PUBLIC_RPC_ALLOWLIST"
      contains: "lobby_en_tramitacion"
  key_links:
    - from: "supabase/migrations/0048_lobby_en_tramitacion.sql"
      to: "citacion.semana_iso"
      via: "to_char((a.fecha at time zone 'America/Santiago'), 'IYYY\"-W\"IW') = c.semana_iso"
      pattern: "semana_iso|IYYY"
---

<objective>
SC2/SC5. Escribir la migración 0048 con la RPC `lobby_en_tramitacion(p_boletin text)` — espejo mecánico del idiom 0047 — que devuelve las audiencias de lobby registradas en la MISMA SEMANA ISO en que una comisión vio el boletín (`citacion.semana_iso` × `citacion_punto.boletin` × `lobby_audiencia.fecha`). Es yuxtaposición pura de hechos fechados, sin flag nuevo, sin `cruce_senal`. Acompañarla del pgTAP (contrato + fixture de coincidencia por semana) y añadirla a `PUBLIC_RPC_ALLOWLIST`.

**Contrato de la RPC (LOCKED para este plan set — 52-03 lo consume verbatim):**
```
lobby_en_tramitacion(p_boletin text) returns table (
  parlamentario_nombre text,   -- p.nombre_normalizado (proyección pública de parlamentario_publico/0020)
  camara               text,
  materia              text,
  fecha_reunion        timestamptz,
  semana_iso           text,
  comision             text,
  enlace_detalle       text
)
```
proargnames posicional (para el pgTAP): `p_boletin,parlamentario_nombre,camara,materia,fecha_reunion,semana_iso,comision,enlace_detalle`

Purpose: es la única vía pública PII-safe a `lobby_contraparte`/`parlamentario` (deny-by-default). Apply a PROD = checkpoint operador (52-06), acumulable con 0047. La UI (52-03) degrada honesta pre-apply.
Output: 0048_*.sql + 0048_*.test.sql + allowlist +1. NO se aplica a PROD (eso es 52-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cruce2-cruces-nuevos/52-RESEARCH.md
@.planning/phases/52-cruce2-cruces-nuevos/52-PATTERNS.md

<interfaces>
<!-- Analogs verbatim. Copiar el idiom, no inventar. -->

Idiom RPC PII-safe (0047_rebeldias_honestas.sql:46-102):
  create or replace function public.<name>(<args>)
  returns table (...) language sql stable security definer set search_path = '' as $$ ... $$;
  revoke all on function public.<name>(<args>) from public;
  revoke all on function public.<name>(<args>) from anon, authenticated;
  -- CERO grant. Función NUEVA → NO drop previo (0047 dropeó solo por cambiar returns table existente).

Proyección pública (0020_parlamentario_publico.sql:29-41):
  returns table (id text, nombre text, camara text, ...)
  select p.nombre_normalizado, p.camara ...   -- nombre público = p.nombre_normalizado (confirma A3)

Schema de join (verificado):
  citacion            → id, comision, fecha, semana_iso  (0010, no-PII, public-read)
  citacion_punto      → citacion_id, boletin (nullable)  (0010)
  lobby_audiencia     → identificador, parlamentario_id, estado_vinculo, fecha, materia, enlace_detalle  (0021:30-58; PII deny-by-default)
  parlamentario       → id, nombre_normalizado, camara   (PII deny-by-default; leer solo interno vía security definer)

pgTAP idiom (0047_rebeldias_honestas.test.sql):
  begin; select plan(N); ... select * from finish(); rollback;
  - array_to_string(proargnames, ',') para el orden posicional (NO pg_get_function_result)
  - not has_function_privilege('anon', 'public.<name>(text)', 'execute')  → Camino A deny
  - returns !~* '\y(partido|rut|email)\y'  → no-PII
  Corre por `psql -tA -f` post-apply (fuera del glob vitest .test.{ts,tsx}).

Allowlist (lockdown-guard.test.ts:165-181): Set alfabético; insertar "lobby_en_tramitacion" tras "lobby_de_parlamentario".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0048 — RPC lobby_en_tramitacion (idiom 0047, PII-safe, semana ISO)</name>
  <read_first>
    - supabase/migrations/0047_rebeldias_honestas.sql (idiom completo: cabecera + create or replace + doble revoke)
    - supabase/migrations/0020_parlamentario_publico.sql:29-41 (proyección pública nombre_normalizado + camara)
    - supabase/migrations/0021_lobby.sql:30-58 (columnas lobby_audiencia)
    - supabase/migrations/0010_agenda.sql (citacion.semana_iso + citacion_punto.boletin — confirmar nombres exactos de columna)
  </read_first>
  <action>
    Crear `supabase/migrations/0048_lobby_en_tramitacion.sql`. Cabecera espejo de 0047 (bloque de comentario): explicar la semántica de yuxtaposición-pura ("en la misma semana ISO en que una comisión vio el boletín"; NO causal), que lee `parlamentario`/`lobby_contraparte` INTERNAMENTE vía security definer pero emite solo derivado público, que el apply es checkpoint operador vía `psql --db-url --single-transaction` (NUNCA `supabase db push`; PGCLIENTENCODING=UTF8 en Windows), y el supuesto de timezone (A1). Documentar que la última migración en disco es 0047 → esta es la 0048.

    Cuerpo: `create or replace function public.lobby_en_tramitacion(p_boletin text)` (función NUEVA → SIN drop previo) `returns table (parlamentario_nombre text, camara text, materia text, fecha_reunion timestamptz, semana_iso text, comision text, enlace_detalle text) language sql stable security definer set search_path = ''`. La query: `from public.citacion c join public.citacion_punto cp on cp.citacion_id = c.id join public.lobby_audiencia a on to_char((a.fecha at time zone 'America/Santiago'), 'IYYY"-W"IW') = c.semana_iso join public.parlamentario p on p.id = a.parlamentario_id where cp.boletin = p_boletin and a.estado_vinculo = 'confirmado' and a.parlamentario_id is not null order by a.fecha desc nulls last`. Emitir `p.nombre_normalizado, p.camara, a.materia, a.fecha, c.semana_iso, c.comision, a.enlace_detalle` en ESE orden posicional (coincide con el returns table). NUNCA emitir rut/partido/email.

    La normalización de timezone (`at time zone 'America/Santiago'`) es load-bearing (Pitfall 2): iguala la convención con que el conector pobló `citacion.semana_iso`. Confirmar contra un par real `(fecha, semana_iso)` de `citacion` no es posible sin PROD; documentar el supuesto A1 en la cabecera y dejar el `at time zone` explícito.

    ACL determinista (copiar VERBATIM de 0047:101-102, load-bearing): `revoke all on function public.lobby_en_tramitacion(text) from public;` + `revoke all on function public.lobby_en_tramitacion(text) from anon, authenticated;`. CERO grant (Camino A: el sitio lee con service_role; anon a cero grants desde 0044). Un grant a anon/public fallaría el guard CI Block A.
  </action>
  <verify>
    <automated>grep -v '^--' "supabase/migrations/0048_lobby_en_tramitacion.sql" | grep -c 'revoke all on function public.lobby_en_tramitacion(text) from' | grep -qx 2 && grep -qv 'grant .* to \(anon\|public\)' "supabase/migrations/0048_lobby_en_tramitacion.sql" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `create or replace function public.lobby_en_tramitacion(p_boletin text)` presente, SIN `drop function` previo
    - `security definer set search_path = ''` presente
    - exactamente DOS `revoke all on function ... from` (public; anon, authenticated); CERO `grant ... to anon/public`
    - returns table en el orden posicional del contrato LOCKED (7 columnas + p_boletin)
    - join de semana usa `to_char((a.fecha at time zone 'America/Santiago'), 'IYYY"-W"IW') = c.semana_iso`
    - el cuerpo NO emite partido/rut/email en la lista SELECT final
  </acceptance_criteria>
  <done>0048_*.sql escrita, idiom 0047, PII-safe, doble revoke sin grant, join de semana ISO con timezone explícito.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0048 (contrato + fixture de coincidencia por semana) + allowlist</name>
  <read_first>
    - supabase/tests/0047_rebeldias_honestas.test.sql (estructura begin/plan/finish/rollback + asserts de contrato + fixture)
    - supabase/migrations/0048_lobby_en_tramitacion.sql (recién escrita — el contrato a asertar)
    - app/lib/lockdown-guard.test.ts:165-181 (PUBLIC_RPC_ALLOWLIST — el Set a extender)
  </read_first>
  <action>
    Crear `supabase/tests/0048_lobby_en_tramitacion.test.sql` espejo de 0047_*.test.sql: `begin; select plan(N); ... select * from finish(); rollback;`. Asserts de contrato (copiar el idiom, adaptar nombres):
    (1) `has_function('public','lobby_en_tramitacion', array['text'], ...)`.
    (2) `is( array_to_string(proargnames,',') , 'p_boletin,parlamentario_nombre,camara,materia,fecha_reunion,semana_iso,comision,enlace_detalle', 'returns table posicional pineado')`.
    (3) `is( prosecdef, true, 'security definer')`.
    (4) `ok( array_to_string(proconfig,',') like '%search_path=%', 'search_path fijado')`.
    (5) `ok( not has_function_privilege('anon','public.lobby_en_tramitacion(text)','execute'), 'anon NO execute — Camino A deny')`.
    (6) `is( array_to_string(proargnames,',') !~* '\y(partido|rut|email)\y', true, 'returns sin PII')`.
    Fixture de DATOS (7): insertar `parlamentario` + `proyecto` + `citacion` (con `semana_iso` fijo, p.ej. '2024-W10' y una `fecha` de comisión) + `citacion_punto` (boletin = el de prueba) + `lobby_audiencia` con `estado_vinculo='confirmado'`, `parlamentario_id` = el insertado, y una `fecha` que caiga en la MISMA semana ISO ('2024-W10' bajo America/Santiago). Asertar que `(select count(*) from public.lobby_en_tramitacion('BTEST-01')) = 1` y que la fila trae el `parlamentario_nombre`/`semana_iso`/`comision` esperados. Añadir un caso negativo: una audiencia en OTRA semana no aparece (count sigue 1). Usar las listas de columnas de insert de 0047_*.test.sql:69-89 donde solapen (parlamentario/proyecto/citacion/votacion).

    Añadir `"lobby_en_tramitacion"` al Set `PUBLIC_RPC_ALLOWLIST` en app/lib/lockdown-guard.test.ts (posición alfabética: tras `"lobby_de_parlamentario"`, antes de `"match_proyectos"`). Este es el ÚNICO cambio al guard: la regla Block-A (sin grant a anon) ya cubre el doble-revoke de 0048 (cero grants → nada que allowlistear en grants).
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run lib/lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - 0048_*.test.sql existe con begin/plan/finish/rollback y los 6 asserts de contrato + fixture de coincidencia por semana (caso positivo count=1 + caso negativo otra-semana)
    - el pgTAP usa `array_to_string(proargnames,',')` (NO pg_get_function_result)
    - `PUBLIC_RPC_ALLOWLIST` contiene `"lobby_en_tramitacion"` en orden alfabético
    - `pnpm --dir app test -- --run lib/lockdown-guard` verde (guard no reporta offenders sobre 0048 — cero grants)
  </acceptance_criteria>
  <done>pgTAP 0048 escrito (contrato + fixture semana), allowlist +1, lockdown-guard verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/REST → RPC public | superficie pública no autenticada; Camino A exige anon a cero grants |
| RPC (security definer) → parlamentario/lobby_contraparte (PII) | lee PII internamente, debe emitir solo derivado público |
| client (path boletín) → RPC arg | input no confiable a `p_boletin` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Elevation of Privilege | 0048 re-abre EXECUTE a anon por DEFAULT PRIVILEGES | mitigate | doble revoke `from public` + `from anon, authenticated`; pgTAP assert (5) `not has_function_privilege('anon',...)`; guard CI Block-A sin excepciones. |
| T-52-05 | Information Disclosure | RPC filtra rut/partido/email del parlamentario | mitigate | returns table emite solo nombre_normalizado + camara; pgTAP assert (6) `!~* '\y(partido\|rut\|email)\y'`; body no selecciona columnas PII. |
| T-52-06 | Tampering | SQL injection vía p_boletin | mitigate | arg parametrizado por supabase-js/RPC; `search_path=''` en la función; BOLETIN_RE valida el path en la page (52-03). |
| T-52-07 | Repudiation (integridad narrativa) | la coincidencia temporal se lee como causal | transfer | la RPC solo devuelve hechos fechados; el caveat anti-causal vive en la UI (52-03); documentado en la cabecera de 0048. |
</threat_model>

<verification>
- `grep`/gate: exactamente 2 revoke, 0 grant a anon/public en 0048.
- `pnpm --dir app test -- --run lib/lockdown-guard` verde.
- pgTAP 0048 NO corre en la suite vitest (es .test.sql, fuera del glob) — se corre por operador en 52-06.
- `pnpm --dir app exec tsc -b` limpio (la edición del guard es TS).
</verification>

<success_criteria>
- 0048 define lobby_en_tramitacion con el contrato LOCKED, idiom 0047, PII-safe, doble revoke sin grant.
- pgTAP cubre firma/security-definer/search_path/anon-deny/no-PII + coincidencia por semana ISO.
- lobby_en_tramitacion en PUBLIC_RPC_ALLOWLIST; lockdown-guard verde.
- Ningún flag `*_PUBLIC_ENABLED` tocado; ninguna migración aplicada (apply = 52-06).
</success_criteria>

<output>
Create `.planning/phases/52-cruce2-cruces-nuevos/52-02-SUMMARY.md` when done
</output>
