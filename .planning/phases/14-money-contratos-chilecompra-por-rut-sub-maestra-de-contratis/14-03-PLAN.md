---
phase: 14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - app/components/contratos-de-parlamentario.tsx
  - app/app/parlamentario/[id]/page.tsx
  - app/lib/types.ts
  - app/components/contratos-de-parlamentario.test.tsx
autonomous: true
requirements: [MONEY-02]
must_haves:
  truths:
    - "Con moneyPublicEnabled() === false (DEFAULT) la seccion #dinero entera esta ausente del HTML (sin heading, sin skeleton, sin nodo DOM)"
    - "El heading exacto es 'Contratos del Estado asociados al RUT'; nunca un posesivo"
    - "Los tres estados honestos son textualmente distintos: 'no consultado todavia' / 'consultado sin contratos' / 'enlazado'"
    - "Un contrato a persona juridica muestra la entidad proveedora como sujeto, separada del enlace al parlamentario; nunca un posesivo"
    - "sourceLabel() devuelve 'ChileCompra' para origen chilecompra/mercado, no 'fuente desconocida'"
    - "Un error real de RPC se lanza (route boundary), nunca se degrada silenciosamente a 'sin contratos'"
  artifacts:
    - path: "app/components/contratos-de-parlamentario.tsx"
      provides: "ContratosView (pure) mas ContratosSection (Server Component gated)"
      contains: "moneyPublicEnabled"
    - path: "app/lib/types.ts"
      provides: "rama ChileCompra en sourceLabel mas ContratoRpcRow"
      contains: "chilecompra"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "ContratosSection"
      via: "<section id='dinero' className='mt-12'> mas Suspense mas ContratosSkeleton"
      pattern: "id=\"dinero\""
    - from: "app/components/contratos-de-parlamentario.tsx"
      to: "contratos_de_parlamentario RPC"
      via: "sb.rpc despues del gate moneyPublicEnabled"
      pattern: "contratos_de_parlamentario"
---

<objective>
Crear la seccion de ficha "Contratos del Estado asociados al RUT" (`app/components/contratos-de-parlamentario.tsx`), montarla en `/parlamentario/[id]/page.tsx` como `<section id="dinero" className="mt-12">` en el slot reservado, y agregar la rama ChileCompra a `sourceLabel()`. Toda la seccion vive detras de `moneyPublicEnabled()` (default OFF). Espejo de `lobby-de-parlamentario.tsx` mas `patrimonio-de-parlamentario.tsx`, siguiendo `14-UI-SPEC.md` al pie de la letra.

Purpose: La cara ciudadana de la fase. Las reglas load-bearing: (1) la seccion entera esta ausente cuando el gate esta OFF (doble candado con la RLS deny-by-default del Plan 01); (2) la redaccion "asociados al RUT" es un contrato de honestidad, no cosmetica; (3) tres estados honestos textualmente distintos; (4) una persona juridica nunca se colapsa en atribucion personal.

Output: el componente, el mount en page.tsx, la rama de `sourceLabel`, y tests RTL del gate mas los 3 estados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-UI-SPEC.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-PATTERNS.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-CONTEXT.md

# Analogos de ficha a espejar (leer antes de escribir el componente):
@app/components/lobby-de-parlamentario.tsx
@app/components/patrimonio-de-parlamentario.tsx
@app/app/parlamentario/[id]/page.tsx
@app/lib/types.ts
@app/lib/money-gate.ts

<interfaces>
<!-- Contratos exactos que el componente consume. Definidos por Plan 01 (RPC/marcador) y Plan 02 (origen). -->

Gate (app/lib/money-gate.ts, server-only):
  export function moneyPublicEnabled(env = process.env): boolean;  // strict literal "true"; ausencia = OFF
  // Llamar SIEMPRE via la funcion; NUNCA leer MONEY_PUBLIC_ENABLED crudo. Chokepoint WR-02.

RPC contratos_de_parlamentario(p_id) (Plan 01) returns rows con la forma ContratoRpcRow (agregar a types.ts):
  interface ContratoRpcRow {
    codigo_orden: string; proveedor_nombre: string; tipo_persona: string; organismo: string;
    monto: string; fecha_oc: string; origen: string; fecha_captura: string;
    fecha_corte: string; enlace: string; licencia: string;
  }

Marcador contratos_ingesta_estado (Plan 01), leido via .maybeSingle():
  { parlamentario_id: string; ingestado_hasta: string | null; fecha_captura: string } | null
  - null (fila ausente) O sin RUT interno  ->  estado "No consultado todavia" (DEFAULT)
  - presente mas 0 filas de contrato       ->  estado "Consultado sin contratos (corte al {fecha})"
  - presente mas >=1 fila                   ->  estado "Enlazado" (lista paginada)

sourceLabel (app/lib/types.ts:343-352): agregar antes del fallthrough:
  if (o.includes("chilecompra") || o.includes("mercado")) return "ChileCompra";
  // El origen escrito por Plan 02 es "chilecompra".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sourceLabel ChileCompra mas ContratoRpcRow en types.ts; componente contratos-de-parlamentario.tsx</name>
  <files>app/lib/types.ts, app/components/contratos-de-parlamentario.tsx</files>
  <action>
ANTES de escribir codigo Next.js: leer la guia relevante en `app/node_modules/next/dist/docs/` (AGENTS.md de app: este Next no es el de tu training; conventions pueden diferir). Server Components por defecto, sin `"use client"`.

`app/lib/types.ts` (MODIFY): en `sourceLabel()` (lineas 343-352), agregar ANTES del fallthrough: `if (o.includes("chilecompra") || o.includes("mercado")) return "ChileCompra";`. Agregar la interface `ContratoRpcRow` junto a `DeclaracionRpcRow` (lineas 254-259) con la forma del bloque `<interfaces>`.

`app/components/contratos-de-parlamentario.tsx` (espejo de `lobby-de-parlamentario.tsx` mas `patrimonio-de-parlamentario.tsx`): dos exports, SIN `"use client"`:
  - `ContratosView` (puro, props-only, RTL-testable con fixtures): renderiza el intro (2 lineas muted `text-sm`) mas UNO de los tres estados honestos. `PAGE_SIZE = 20`, `buildHref` que preserva el query, ancla `#dinero`, `?contratosPage=N`. Copy EXACTO de UI-SPEC Copywriting Contract:
      * Intro linea 1 y linea 2 (atribucion ChileCompra "mencion de la fuente", NO CC BY 4.0) verbatim de UI-SPEC.
      * Estado "No consultado todavia": mensaje muted que dice que aun no se consulto ChileCompra para ese RUT y que ausencia de consulta NO es ausencia de contratos. NUNCA "limpio"/"sin contratos ✓".
      * Estado "Consultado sin contratos": mensaje muted con la `{fecha de corte}`, distinto del anterior.
      * Estado "Enlazado": linea de conteo neutral ("{n} contrato registrado" / "{n} contratos registrados") mas lista paginada.
  - Por fila (estado Enlazado): el SUJETO es la entidad proveedora (`text-base`). Persona juridica (mirror `ContraparteCruda` de lobby): `Proveedor: {nombre}` mas `(persona juridica)` muted parentetico; el enlace al parlamentario es una linea muted SEPARADA "Enlazado por RUT al parlamentario." NUNCA un posesivo, NUNCA fusionar entidad mas parlamentario en una atribucion personal. Campos literales del contrato (organismo, monto literal, fecha) como NOUN-label mas valor verbatim via `<dl>`/`<dt>`/`<dd>` (mirror patrimonio); el UI NO computa nada (sin sumas, sin %). `ProvenanceBadge` por fila empujado a la derecha con `ml-auto`, `sourceName={sourceLabel(origen)}` (-> "ChileCompra"), `sourceUrl={enlace}`. Mostrar `fecha de corte` por fila, distinta de `capturedAt`. Paginacion `?contratosPage=N` con `<nav>` "Anteriores"/"Siguientes" mas "Pagina N de M" en `font-mono`, identica a los hermanos. Orden default fecha DESC.
  - `ContratosSection` (Server Component): PRIMERO `if (!moneyPublicEnabled(process.env)) return null;` ANTES de cualquier lectura de Supabase. Luego `sb.rpc("contratos_de_parlamentario", { p_id: id })`; THROW en `rpcError` (#34: un error real NO es estado vacio); leer `contratos_ingesta_estado` via `.maybeSingle()`; derivar el estado server-side (sin RUT interno o fila ausente -> "no consultado"; fila presente mas 0 filas -> "consultado sin contratos"; mas filas -> "enlazado"); slice de paginacion server-driven; pasar todo a `ContratosView`.

Colores: sin rojo/verde de severidad en montos ni contrapartes; ambar SOLO para frescura via ProvenanceBadge (nunca "sospechoso"). Accent SOLO en links de paginacion/fuente.

Archivos SIN BOM ni unicode invisible (solo UTF-8 plano; el copy en espanol usa caracteres acentuados normales UTF-8, NO marcas de orden de bytes ni comillas tipograficas en codigo).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>`tsc --noEmit` limpio en app. `contratos-de-parlamentario.tsx` exporta `ContratosView` y `ContratosSection`, contiene `moneyPublicEnabled`, el heading/copys exactos de UI-SPEC, y la separacion persona-juridica (sujeto proveedor mas linea de enlace separada, sin posesivo). `types.ts` `sourceLabel` retorna "ChileCompra" para chilecompra/mercado.</done>
</task>

<task type="auto">
  <name>Task 2: mount en page.tsx (seccion #dinero mas ContratosSkeleton) mas tests RTL del gate y los 3 estados</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/components/contratos-de-parlamentario.test.tsx</files>
  <behavior>
    - gate OFF (moneyPublicEnabled false): ContratosSection retorna null; la seccion #dinero no aparece en el HTML renderizado.
    - estado "no consultado todavia": ContratosView con marcador ausente renderiza el copy de no-consultado; NO renderiza "sin contratos ✓" ni nada con tono positivo/limpio.
    - estado "consultado sin contratos": marcador presente mas 0 contratos renderiza el copy de corte con fecha, distinto del anterior.
    - estado "enlazado": >=1 contrato renderiza el conteo neutral mas la fila con sujeto proveedor; persona juridica muestra "(persona juridica)" mas la linea separada "Enlazado por RUT al parlamentario.", sin posesivo.
    - heading exacto "Contratos del Estado asociados al RUT" presente cuando el gate esta ON.
  </behavior>
  <action>
ANTES de editar page.tsx: leer la guia de `app/node_modules/next/dist/docs/` si hay duda de conventions.

`app/app/parlamentario/[id]/page.tsx` (MODIFY): en el slot reservado (lineas 85-90, el comentario `<section id="dinero">`), agregar un NUEVO `<section>` hermano DESPUES de `#patrimonio` (lineas 76-83), espejando los bloques lobby/patrimonio:
```
{moneyPublicEnabled(process.env) && (
  <section id="dinero" className="mt-12">
    <h2 className="text-xl font-semibold mb-4">Contratos del Estado asociados al RUT</h2>
    <Suspense fallback={<ContratosSkeleton />}>
      <ContratosSection id={id} searchParams={sp} />
    </Suspense>
  </section>
)}
```
(Las llaves del fence anterior son ilustrativas del JSX a escribir, no las pegues como bloque de codigo dentro de prosa.) El `mt-12` es la frontera de carril NO NEGOCIABLE (anti-insinuacion, Phase 11). Importar `ContratosSection` desde el componente y `moneyPublicEnabled` desde `app/lib/money-gate`. Definir `ContratosSkeleton` junto a los otros skeletons (lineas 143-170), shape-matched a `ContratosView` (linea de intro `Skeleton h-4 w-3/4` mas linea de atribucion `h-3 w-1/2` mas 3 filas `h-12 w-full rounded-lg`), espejo de `LobbySkeleton`/`PatrimonioSkeleton`. GATE WIRING (INSTRUCCION UNICA, no ambigua): llamar `moneyPublicEnabled(process.env)` en el Server Component de `page.tsx` y envolver el `<section id="dinero">` COMPLETO — incluyendo `<h2>`, `<Suspense>` y `ContratosSkeleton` — en ese check, como muestra el JSX de arriba. Con OFF (default) el nodo `<section>` entero, heading incluido, esta AUSENTE del HTML. NO depender de que `ContratosSection` retorne null para ocultar el heading (eso dejaria el `<h2>` filtrado). Seguir UI-SPEC Exposure Gate al pie de la letra.

`app/components/contratos-de-parlamentario.test.tsx`: tests RTL sobre `ContratosView` (puro, con fixtures) cubriendo el bloque `<behavior>`: los 3 estados con copy distinto, el heading exacto, el tratamiento persona-juridica (sin posesivo). GATE TEST: como el gate vive en page.tsx (Server Component, no trivial de montar en RTL), modelar la suppression con un pequeno helper/render que reproduzca el wrapper `moneyPublicEnabled(env) && <section>...`; con el flag a false aserta `screen.queryByText("Contratos del Estado asociados al RUT")` === null (HEADING ausente, no solo el contenido); con el flag a "true" el heading aparece. Mirror del patron de test de los hermanos si existe; si no, fixtures sinteticos.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd app && pnpm test contratos 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm --filter app test` verde para `contratos-de-parlamentario.test.tsx`. page.tsx tiene `<section id="dinero" className="mt-12">` con el heading exacto, Suspense mas `ContratosSkeleton`, y la presencia de la seccion gobernada por `moneyPublicEnabled` (OFF -> seccion ausente). Los 3 estados son textualmente distintos en los tests.</done>
</task>

</tasks>

<verification>
- `pnpm --filter app test` verde (gate OFF, 3 estados, persona juridica).
- `tsc --noEmit` limpio.
- Heading exacto "Contratos del Estado asociados al RUT"; sin posesivos; sin "limpio"/"✓".
- `sourceLabel` retorna "ChileCompra".
- mt-12 carril separator presente.
</verification>

<success_criteria>
- Criterio 2 (MONEY-02): la ficha muestra los contratos "asociados al RUT" (nunca "del parlamentario"), provenance mas fecha de corte por fila.
- Criterio 3: tres estados honestos textualmente distintos; "consultado sin contratos" != "no consultado todavia".
- Criterio 4: persona juridica = entidad proveedora como sujeto, separada del enlace; nunca atribucion personal.
- Gate de exposicion: con moneyPublicEnabled() OFF (default), la seccion entera ausente del HTML (doble candado con RLS deny-by-default del Plan 01).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (anon) a Next.js RSC | la seccion publica solo se renderiza si moneyPublicEnabled() y los datos vienen via RPC |
| RSC a Supabase | lectura via security-definer RPC; nunca lectura directa a la tabla deny-by-default contratista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Information Disclosure | exposicion prematura MONEY sin sign-off legal | mitigate | doble candado: moneyPublicEnabled() OFF (presentacion, seccion ausente) mas RLS deny-by-default mas RPC (datos, Plan 01); ambos fail-closed |
| T-14-11 | Repudiation | un vacio leido como veredicto "limpio" | mitigate | tres estados honestos textualmente distintos; copy prohibe "sin contratos ✓"/"limpio"; test RTL lo codifica |
| T-14-12 | Tampering | atribucion personal de un contrato a persona juridica | mitigate | sujeto = entidad proveedora; enlace en linea separada; sin posesivo; test RTL del tratamiento persona-juridica |
| T-14-13 | Information Disclosure | leer MONEY_PUBLIC_ENABLED crudo (bypass del chokepoint) | mitigate | SIEMPRE via moneyPublicEnabled() server-only (import "server-only"); nunca el env crudo |
| T-14-14 | Repudiation | error real de RPC degradado a "sin contratos" | mitigate | THROW en rpcError (#34) a route boundary; un error nunca es estado vacio |
</threat_model>

<output>
Crear `.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-03-SUMMARY.md` al terminar.
</output>
