---
phase: 14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
retrofit: true
files_modified:
  - packages/dinero/src/reconciliar-contrato.ts
  - packages/dinero/src/reconciliar-contrato.test.ts
  - packages/dinero/src/harvest-rut.ts
  - packages/dinero/src/harvest-rut.test.ts
  - packages/dinero/src/index.ts
autonomous: false
requirements: [MONEY-01, MONEY-02, IDENT-10]
must_haves:
  truths:
    - "Un proveedor cuyo RUT-exacto matchea el RUT interno de un parlamentario sigue confirmando por la rama determinista (camino LOCKED intacto)"
    - "Un proveedor PERSONA NATURAL sin match RUT-exacto, cuyo NOMBRE resuelve determinista/confirmado-humano contra la maestra, queda enlazado via correrPipeline (finalidad del dato)"
    - "Un proveedor persona natural ambiguo (homonimo/probable/revision) queda enlace null + estadoVinculo no_confirmado + mencion cruda (fail-closed, cola humana)"
    - "Un proveedor PERSONA JURIDICA NUNCA se enlaza por nombre a un parlamentario; persona juridica sigue siendo RUT-exacto-only"
    - "En un match persona-natural CONFIRMADO se emite un candidato de cosecha { parlamentarioId, rutHarvested, provenance } con el rutProveedor DV-revalidado; nunca se fabrica un RUT"
    - "El rutProveedor (ni ningun RUT) JAMAS llega a correrPipeline ni al prompt/LLM: solo el proveedorNombre (persona natural) viaja al pipeline"
    - "El invariante IDENT-12 se preserva: el FK solo se puebla via confirmar() en un resultado determinista (RUT-exacto o nombre unico) o promocion humana"
  artifacts:
    - path: "packages/dinero/src/reconciliar-contrato.ts"
      provides: "reconciliarContrato con rama RUT-exacto (intacta) + fallback persona-natural via correrPipeline + persona-juridica RUT-only + emision de candidatos de cosecha"
      contains: "correrPipeline"
    - path: "packages/dinero/src/harvest-rut.ts"
      provides: "construir candidatos de cosecha FilaRutCruda + runHarvestRut que reusa runBackfillRut/RutBackfillWriter (DV-gate + provenance + updateRut)"
      contains: "runBackfillRut"
    - path: "packages/dinero/src/reconciliar-contrato.test.ts"
      provides: "tests: RUT-exacto intacto, persona-natural confirmada->enlace+cosecha, persona-natural ambigua->null, persona-juridica nunca name-link, data-routing (RUT nunca al pipeline/prompt)"
      contains: "SpyProvider"
  key_links:
    - from: "packages/dinero/src/reconciliar-contrato.ts"
      to: "@obs/adjudication correrPipeline"
      via: "fallback persona-natural: solo proveedorNombre arma la MencionForanea"
      pattern: "correrPipeline"
    - from: "packages/dinero/src/harvest-rut.ts"
      to: "@obs/identity runBackfillRut"
      via: "candidato de cosecha -> FilaRutCruda -> runBackfillRut (DV-gate + provenance)"
      pattern: "runBackfillRut"
    - from: "packages/dinero/src/reconciliar-contrato.ts"
      to: "@obs/identity confirmar"
      via: "EnlaceConfirmado minteado SOLO en determinista (RUT-exacto o nombre unico)"
      pattern: "confirmar\\("
---

<objective>
Retrofit de `packages/dinero/src/reconciliar-contrato.ts` bajo el principio aprobado por el operador "finalidad del dato": enlazar un FUNCIONARIO PUBLICO usa el pipeline de identidad confirmado/auditado; la PII de un tercero/donante queda protegida. Hoy el enlace contrato->parlamentario es RUT-exacto-only; como el RUT interno de la maestra esta sin poblar (IDENT-10), esto da CERO enlaces. El retrofit:

1. MANTIENE la rama RUT-exacto determinista intacta (el match mas fuerte).
2. AGREGA un fallback persona-NATURAL: sin match RUT-exacto Y `tipoPersona === "natural"`, corre `correrPipeline` con el NOMBRE del proveedor (`proveedorNombre`) contra la maestra (espejo de `reconciliar-aporte.ts` / `reconciliar-sujeto.ts`). Solo determinista / humano-confirmado puebla el FK (IDENT-12); ambiguo -> null + cola humana (fail-closed).
3. COSECHA de RUT (el avance de IDENT-10): cuando un proveedor persona-natural queda CONFIRMADO como parlamentario Z, el `rutProveedor` DV-valido de ChileCompra ES el RUT de Z -> se emite un candidato de cosecha `{ parlamentarioId, rutHarvested, provenance }` para escribir al `rut` de la maestra, con provenance ("harvested from ChileCompra confirmed persona-natural match, <fuente/fecha>"). DV modulo-11 (`isRutValido`) re-validado ANTES de cosechar; nunca se fabrica; solo en enlace CONFIRMADO.
4. PERSONA JURIDICA SIN CAMBIOS: una empresa NUNCA se name-linkea a un parlamentario (la empresa no es el parlamentario); persona juridica sigue RUT-exacto-only.
5. DATA-ROUTING GATE: solo el NOMBRE (persona natural) llega al pipeline/LLM; el `rutProveedor` NUNCA llega al LLM/prompt (el gate `assertNoRutInLlmInput` vive DENTRO de `correrPipeline`; aqui basta no rutear el RUT). Test-enforce con SpyWriter/SpyProvider.

Purpose: Desbloquear MONEY enlazando funcionarios publicos por el pipeline auditado y, de paso, avanzar IDENT-10 cosechando el RUT verificado del proveedor en matches persona-natural confirmados, sin fabricar RUT y sin filtrar PII al LLM. La escritura remota a la maestra y la corrida LIVE son checkpoints de operador (persistencia DB gateada).

Output: `reconciliar-contrato.ts` refactorizado (RUT-exacto intacto + fallback persona-natural + persona-juridica intacta + emision de candidatos de cosecha), `harvest-rut.ts` (writer path que reusa `runBackfillRut`), tests, exports del barrel, y el comentario de cabecera actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-CONTEXT.md

# Archivo a refactorizar (leer ANTES de editar):
@packages/dinero/src/reconciliar-contrato.ts
@packages/dinero/src/reconciliar-contrato.test.ts
@packages/dinero/src/model.ts

# ANALOGO INTRA-PAQUETE A ESPEJAR (mismo patron: name-link via pipeline + data-routing gate del PII):
@packages/dinero/src/reconciliar-aporte.ts
@packages/dinero/src/reconciliar-aporte.test.ts

# Analogo de lobby (sujeto pasivo por pipeline):
@packages/lobby/src/reconciliar-sujeto.ts

# Maquinaria de identidad reusada (NO reimplementar):
@packages/identity/src/deterministic.ts
@packages/identity/src/backfill-rut.ts
@packages/identity/src/enlace-confirmado.ts
@packages/adjudication/src/pipeline.ts

<interfaces>
<!-- Contratos exactos que el retrofit consume. Extraidos del codebase; el ejecutor los usa directo, sin explorar. -->

De @obs/identity (packages/identity/src/index.ts):
  export function isRutValido(rut: string): boolean;            // DV modulo-11; NO reimplementar
  export function normRut(rut: string): string;
  export function matchDeterminista(mention: Mention, maestra: MaestraRow[]): Resolution;
  export function confirmar(parlamentarioId: string, metodo?: "determinista" | "humano"): EnlaceConfirmado;
  export type EnlaceConfirmado;                                 // branded; unico constructor = confirmar()
  // Maquinaria de cosecha (reusar, NO duplicar):
  export function runBackfillRut(filas: FilaRutCruda[], writer: RutBackfillWriter): Promise<ResultadoBackfill>;
  export function aceptarRutBackfill(fila: FilaRutCruda): ResultadoAceptacion;  // PURA: DV-gate + provenance
  export interface FilaRutCruda { id: string; rut: string; origen: string; fecha_captura: string; enlace: string; }
  export interface RutBackfillWriter { updateRut(rows: FilaRutEscribir[]): Promise<{ actualizadas: number }>; }
  export interface ResultadoBackfill { escritas: number; rechazadas: { id: string; razon: "dv-invalido" | "provenance-faltante" }[]; }

De @obs/adjudication (packages/adjudication/src/index.ts):
  export function correrPipeline(mencion, maestra, provider, writer): Promise<ResultadoPipeline>;
  export type PipelineWriter { upsertVinculo(v): Promise<number|null>; appendAudit(row): Promise<void>; enqueueRevision(caso): Promise<void>; }
  export type MencionForanea { nombreOriginal: string; nombreNormalizado: string; tokens: string[]; camara; periodo: string; region: string | null; }
  export type ResultadoPipeline =
    | { tipo: "determinista"; parlamentarioId: string }
    | { tipo: "no_confirmado"; razon: string }
    | { tipo: "probable"; parlamentarioId: string }
    | { tipo: "revision"; razones: string[] };
  export { MockMiniMaxProvider };  // para tests
  export type { FilaVinculo, FilaAudit, CasoRevision };  // para el SpyWriter de tests
  // NOTA: assertNoRutInLlmInput corre DENTRO de correrPipeline (etapa 2) sobre el prompt EXACTO. NO se invoca aqui.

De @obs/core:
  export function normalizarNombre({ libre: string }): { nombre_normalizado: string; tokens: string[] };

De ./model (Contrato — ya existe):
  interface Contrato { rutProveedor: string; proveedorNombre: string | null; tipoPersona: "natural" | "juridica"; /* + provenance: origen, fecha_captura, enlace, licencia, fechaCorte... */ }

Defaults seguros del pipeline (copiar de reconciliar-aporte.ts):
  NOOP_WRITER (PipelineWriter no-op), PROVIDER_AUSENTE (LANZA si se invoca; sin provider real un homonimo degrada a no_confirmado, NO aborta la corrida).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor reconciliar-contrato.ts — fallback persona-natural via correrPipeline + emision de candidatos de cosecha; persona-juridica intacta</name>
  <files>packages/dinero/src/reconciliar-contrato.ts, packages/dinero/src/reconciliar-contrato.test.ts</files>
  <behavior>
    - RUT-EXACTO INTACTO: proveedor cuyo RUT-exacto matchea el RUT interno de un parlamentario -> confirmar(id,"determinista"), estadoVinculo "confirmado" (los tests existentes de la rama RUT siguen verdes sin cambios de semantica).
    - RUT invalido (DV malo) -> cuarentena, enlace null, nunca fila confirmada (intacto).
    - FALLBACK PERSONA-NATURAL: rutProveedor DV-valido, SIN match RUT-exacto, tipoPersona "natural", proveedorNombre presente -> correrPipeline con el NOMBRE; res.tipo "determinista" -> enlace confirmar(id,"determinista") + estadoVinculo "confirmado" + se EMITE un candidato de cosecha { parlamentarioId, rutHarvested: rutProveedor, provenance }.
    - PERSONA-NATURAL AMBIGUA: res.tipo "probable" / "revision" / "no_confirmado" -> enlace null, estadoVinculo "no_confirmado", mencion cruda preservada, SIN candidato de cosecha (fail-closed; el caso va a la cola humana via el writer del pipeline).
    - PERSONA-JURIDICA: rutProveedor DV-valido, SIN match RUT-exacto, tipoPersona "juridica" -> NUNCA correrPipeline; enlace null, estadoVinculo "no_confirmado", mencion cruda. Una empresa nunca se name-linkea ni emite cosecha.
    - SIN provider real inyectado: un proveedor persona-natural homonimo degrada a no_confirmado (fail-closed, no aborta la corrida); un determinista resuelve igual (correrPipeline corta antes del LLM).
    - DATA-ROUTING (LOAD-BEARING): el rutProveedor (ni ningun RUT) NUNCA aparece en ninguna escritura del pipeline (vinculos/colas) ni en ningun prompt; SOLO el proveedorNombre arma la MencionForanea.
    - COSECHA solo en CONFIRMADO: el candidato de cosecha re-valida el rutProveedor con isRutValido antes de emitirse; nunca se emite para un enlace null; nunca se fabrica un RUT.
  </behavior>
  <action>
Refactorizar `reconciliarContrato` para que sea ASYNC (mismo cambio de firma que `reconciliarAporte`) y acepte `provider`/`writer`/`periodo`/`camara` inyectables en `ReconciliarContratoOpts`, con defaults seguros copiados VERBATIM de `reconciliar-aporte.ts` (`NOOP_WRITER`, `PROVIDER_AUSENTE`, `proveedorAusente = opts.provider === undefined`). Tipar `LLMProvider` derivandolo de `Parameters<typeof correrPipeline>[2]` (no edge directo a @obs/llm), igual que reconciliar-aporte/reconciliar-sujeto.

Orden del matcher por contrato (preservar la rama RUT-exacto EXACTAMENTE como esta hoy en las lineas 104-126):
  1. `!isRutValido(c.rutProveedor)` -> cuarentena (intacto).
  2. RUT valido -> `matchDeterminista({ rut: normRut(c.rutProveedor), nombreNormalizado: "", camara, periodo }, maestra)`. Si `res.estado === "confirmado" && res.metodo === "rut"` -> `confirmar(res.id,"determinista")`, "confirmado", `confirmados.add(res.id)` (intacto). Este camino NO emite cosecha (el RUT interno YA estaba poblado; no hay nada que cosechar).
  3. NUEVO — sin match RUT-exacto y RUT valido: ramificar por `c.tipoPersona`:
       - "natural" Y `c.proveedorNombre` no vacio -> correr el fallback de nombre via `correrPipeline` (ver abajo).
       - "juridica" (o nombre vacio) -> enlace null, estadoVinculo "no_confirmado" (comportamiento actual del else, INTACTO para juridica). NUNCA correrPipeline para juridica.

Fallback persona-natural (espejo archivo-por-archivo de `reconciliarAporte`, lineas 127-180): construir la MencionForanea SOLO con `c.proveedorNombre` (`normalizarNombre({ libre: proveedorNombre })` -> nombreNormalizado + tokens; `region: null`; `camara`/`periodo` del opts). NINGUN campo de RUT entra en la mencion. Try/catch alrededor de `correrPipeline`: si `proveedorAusente` y lanza, degradar ESE contrato a no_confirmado y continuar (no abortar); con provider real, propagar. Switch sobre `res.tipo`: solo "determinista" mintea `confirmar(res.parlamentarioId,"determinista")`, "confirmado", `confirmados.add(res.parlamentarioId)`, y EMITE un candidato de cosecha. "probable"/"revision"/"no_confirmado"/default -> enlace null, "no_confirmado".

Emision del candidato de cosecha (NUEVO): definir y exportar `export interface CandidatoCosechaRut { parlamentarioId: string; rutHarvested: string; provenance: { origen: string; fecha_captura: string; enlace: string }; }`. SOLO en la rama persona-natural CONFIRMADA, tras re-validar `isRutValido(c.rutProveedor)` (defensivo; ya se valido arriba pero se re-valida ANTES de cosechar — NUNCA fabricar), empujar a un array `cosechas: CandidatoCosechaRut[]`. `rutHarvested` = `normRut(c.rutProveedor)`. `provenance.origen` = una etiqueta que mencione la cosecha (p.ej. `harvest:chilecompra-persona-natural`), `provenance.enlace` = `c.enlace` (URL de la orden), `provenance.fecha_captura` = `c.fecha_captura`. NO escribir nada aqui (funcion sigue sin red/DB en lo que respecta a la maestra; la escritura es Task 2 + checkpoint de operador). El `provenance` debe llevar texto que diga "harvested from ChileCompra confirmed persona-natural match" + la fuente/fecha — incluirlo como el `origen`/`enlace` que ya documentan fuente+fecha, suficiente para satisfacer el NOT NULL de provenance de backfill-rut.

Extender `ResultadoReconciliacionDinero` con `cosechas: CandidatoCosechaRut[]` (ademas de `contratos` / `parlamentariosConfirmados` / `cuarentenados` existentes). El `filaParaEscribir` y `ContratoParaEscribir` NO cambian (la fila para-escribir del contrato es identica; la cosecha es un canal SEPARADO de salida).

Tests (`reconciliar-contrato.test.ts`): MANTENER los 5 describe existentes (RUT invalido, IDENT-10, RUT-exacto unico, 2+ matches, "NUNCA enlaza por nombre" — este ultimo ahora aplica a persona-juridica; ajustar su fixture a `tipoPersona: "juridica"` para que siga siendo valido bajo la nueva semantica, y AGREGAR el caso persona-natural). Como la firma ahora es async, envolver las llamadas con `await`. Importar `MockMiniMaxProvider`, `type PipelineWriter`, `FilaVinculo`, `FilaAudit`, `CasoRevision` de @obs/adjudication. Copiar las clases `SpyWriter` y `SpyProvider` VERBATIM de `reconciliar-aporte.test.ts`. Nuevos describe:
  - "fallback persona-natural confirmada": maestra con un parlamentario de nombre unico en camara+periodo; contrato persona-natural con ese nombre y RUT DV-valido SIN match RUT-exacto -> enlace confirmado + estadoVinculo "confirmado" + `r.cosechas` tiene 1 entrada con parlamentarioId correcto, rutHarvested = normRut del rutProveedor, provenance presente.
  - "persona-natural ambigua (homonimo) -> null + sin cosecha": dos parlamentarios con el mismo nombre_normalizado; con MockMiniMaxProvider "match" -> enlace null, "no_confirmado", `r.cosechas` vacio.
  - "persona-juridica NUNCA name-link": parlamentario con nombre coincidente; contrato persona-JURIDICA con ese nombre, RUT valido sin match RUT-exacto -> enlace null, "no_confirmado", `r.cosechas` vacio, y (clave) correrPipeline NO se invoco (usar SpyProvider y aserta `spyProvider.prompts.length === 0`).
  - "sin provider: persona-natural homonima degrada, determinista resuelve" (espejo del test homonimo de reconciliar-aporte).
  - "DATA-ROUTING: el rutProveedor NUNCA aparece en escrituras del pipeline ni en el prompt": usar un RUT distintivo (p.ej. "76.543.210-K") en un contrato persona-natural que fuerza el camino LLM (homonimos), SpyWriter + SpyProvider; aserta que `JSON.stringify(writer.vinculos)`, `JSON.stringify(writer.colas)` y cada `spyProvider.prompts[i]` NO contienen el rutProveedor (ni con guion/puntos ni normalizado). Sanity: el nombre del proveedor SI aparece en la mencion escrita.

Archivos SIN BOM ni unicode invisible (solo UTF-8 plano; los comentarios en espanol usan acentos UTF-8 normales, NO marcas BOM ni comillas tipograficas dentro del codigo).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/dinero test reconciliar-contrato 2>&1 | tail -25</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test reconciliar-contrato` verde. `reconciliar-contrato.ts` contiene `correrPipeline`, `confirmar(`, `isRutValido`, exporta `CandidatoCosechaRut`, `cosechas` en el resultado. La rama RUT-exacto y la de cuarentena siguen intactas (tests originales verdes). Persona-juridica nunca invoca el pipeline (test con prompts.length === 0). Data-routing: el rutProveedor nunca aparece en vinculos/colas/prompts (test verde).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: harvest-rut.ts — writer path de cosecha que reusa runBackfillRut (DV-gate + provenance), con la escritura remota como checkpoint de operador</name>
  <files>packages/dinero/src/harvest-rut.ts, packages/dinero/src/harvest-rut.test.ts</files>
  <behavior>
    - construirFilasCosecha(cosechas): mapea CandidatoCosechaRut[] -> FilaRutCruda[] ({ id: parlamentarioId, rut: rutHarvested, origen/fecha_captura/enlace de provenance }). PURA.
    - runHarvestRut(cosechas, writer): delega en runBackfillRut(filas, writer) -> { escritas, rechazadas }. Un rutHarvested DV-invalido (no deberia ocurrir, defensivo) o sin provenance es RECHAZADO por aceptarRutBackfill, NUNCA escrito.
    - idempotente: re-correr con la misma lista produce el mismo updateRut (no-op la 2.a vez por id).
    - NUNCA fabrica un RUT: solo las filas DV-validas + provenance llegan al writer.updateRut.
    - Test del writer espia (InMemory): captura las filas escritas; NUNCA toca la DB.
  </behavior>
  <action>
Crear `packages/dinero/src/harvest-rut.ts`. Importar de @obs/identity: `runBackfillRut`, `type FilaRutCruda`, `type RutBackfillWriter`, `type ResultadoBackfill`. Importar `type CandidatoCosechaRut` de `./reconciliar-contrato`.

  - `export function construirFilasCosecha(cosechas: CandidatoCosechaRut[]): FilaRutCruda[]` — PURA: por cada cosecha, `{ id: c.parlamentarioId, rut: c.rutHarvested, origen: c.provenance.origen, fecha_captura: c.provenance.fecha_captura, enlace: c.provenance.enlace }`. La validacion DV + provenance NOT NULL la hace `runBackfillRut` aguas abajo (reuso, NO reimplementacion).
  - `export async function runHarvestRut(cosechas: CandidatoCosechaRut[], writer: RutBackfillWriter): Promise<ResultadoBackfill>` — `return runBackfillRut(construirFilasCosecha(cosechas), writer);`. Comentario de cabecera load-bearing: este path escribe el RUT COSECHADO al `rut` de la maestra (IDENT-10) SOLO para enlaces persona-natural CONFIRMADOS; reusa la maquinaria de backfill-rut (DV-gate isRutValido + provenance NOT NULL + fail-closed a log de revision); NUNCA fabrica un RUT; el `RutBackfillWriter` real (updateRut contra la maestra remota) es un CHECKPOINT DE OPERADOR (persistencia DB gateada — ver MEMORY: write DB remoto es deuda de operador). En este paquete se construye y testea el path; la corrida LIVE con el writer Supabase real la dispara el operador.

Tests (`harvest-rut.test.ts`): writer espia in-memory que implementa `RutBackfillWriter` (captura `updateRut` rows, devuelve `{ actualizadas: rows.length }`).
  - cosecha persona-natural confirmada (RUT DV-valido + provenance) -> 1 fila escrita, id/rut correctos, rechazadas vacio.
  - cosecha con RUT DV-invalido (defensivo) -> 0 escritas, 1 rechazada razon "dv-invalido", el writer NO recibio esa fila.
  - cosecha sin provenance (origen/enlace/fecha vacios) -> rechazada razon "provenance-faltante", nunca escrita.
  - idempotencia: dos corridas con la misma lista -> mismas filas a updateRut (no-op semantica por id).

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/dinero test harvest-rut 2>&1 | tail -25</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test harvest-rut` verde. `harvest-rut.ts` contiene `runBackfillRut` (reuso, no reimplementa el DV-gate), exporta `construirFilasCosecha` y `runHarvestRut`. RUT DV-invalido o sin provenance -> rechazado, nunca escrito. El comentario de cabecera marca la escritura remota como checkpoint de operador.</done>
</task>

<task type="auto">
  <name>Task 3: actualizar el comentario de cabecera LOCKED + exports del barrel; typecheck del paquete</name>
  <files>packages/dinero/src/reconciliar-contrato.ts, packages/dinero/src/index.ts</files>
  <action>
`reconciliar-contrato.ts` (comentario de cabecera, lineas 1-18): reescribir la "REGLA LOCKED (MONEY-02)" para reflejar la finalidad del dato. El comentario actual dice "RUT-exacto, nunca por nombre" de forma absoluta; ahora:
  - Persona NATURAL: el enlace es RUT-exacto determinista O, sin match RUT-exacto, NOMBRE via correrPipeline (solo determinista/humano-confirmado puebla el FK, IDENT-12); en un match persona-natural confirmado se COSECHA el rutProveedor DV-valido al `rut` de la maestra (IDENT-10), nunca fabricando, solo en enlace confirmado.
  - Persona JURIDICA: SIGUE RUT-exacto-only; una empresa NUNCA se name-linkea a un parlamentario (no es el parlamentario).
  - DATA-ROUTING: solo el proveedorNombre (persona natural) llega a correrPipeline/LLM; el rutProveedor JAMAS toca el pipeline/prompt (el gate assertNoRutInLlmInput vive dentro de correrPipeline).
  - Dejar constancia explicita de que esto SUPERSEDE el comentario LOCKED previo de "RUT-exacto, nunca por nombre" SOLO para persona natural, por decision de operador "finalidad del dato"; persona juridica conserva la regla original.

`index.ts` (barrel de @obs/dinero): 
  - Actualizar el comentario del export de `reconciliarContrato` (lineas 39-40) que hoy dice "sin correrPipeline; enlace solo por rama RUL" — ahora menciona el fallback persona-natural via correrPipeline + cosecha de RUT.
  - Exportar el nuevo tipo: `export type { CandidatoCosechaRut } from "./reconciliar-contrato";` junto a los demas tipos de reconciliar-contrato (lineas 41-46).
  - Exportar el harvest path: `export { construirFilasCosecha, runHarvestRut } from "./harvest-rut";`.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/dinero typecheck 2>&1 | tail -20 && pnpm --filter @obs/dinero test 2>&1 | tail -15</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero typecheck` limpio y `pnpm --filter @obs/dinero test` verde (toda la suite del paquete, no solo los dos archivos nuevos). El barrel exporta `CandidatoCosechaRut`, `construirFilasCosecha`, `runHarvestRut`. El comentario de cabecera de reconciliar-contrato.ts documenta finalidad del dato (persona natural por nombre + cosecha; persona juridica RUT-only) y marca el supersede explicito.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>El path de cosecha de RUT esta construido y testeado (logica + writer + provenance + DV-gate), pero la persistencia DB remota a la maestra esta gateada por operador (MEMORY: write DB remoto es deuda de operador; solo via db push --db-url).</what-built>
  <how-to-verify>
    Dos acciones de OPERADOR, ninguna automatizable por Claude:
    1. ESCRITURA REMOTA DEL RUT COSECHADO: ejecutar `runHarvestRut` con el `SupabaseMaestraWriter`/`RutBackfillWriter` real apuntando a la maestra remota (db-url), revisando que cada fila escrita corresponde a un enlace persona-natural CONFIRMADO y que el `rechazadas` log esta vacio (o auditado). Confirmar que el `rut` quedo poblado con provenance "harvested from ChileCompra confirmed persona-natural match".
    2. CORRIDA LIVE: disparar la reconciliacion persona-natural sobre contratos reales de ChileCompra con el MiniMax provider real (no mock), revisando la cola humana (`identidad_audit`) para los ambiguos y confirmando que ningun RUT aparece en el audit/prompt.
    Ambas son fail-closed por diseno: sin esta accion de operador, nada se escribe a la maestra remota ni se corre LIVE; el codigo y los tests quedan listos.
  </how-to-verify>
  <resume-signal>Escribe "operador: cosecha escrita + LIVE corrido" (o describe lo observado en el log de rechazadas / cola humana).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/dinero test` verde (toda la suite, incl. reconciliar-contrato y harvest-rut).
- `pnpm --filter @obs/dinero typecheck` limpio.
- RUT-exacto determinista intacto (tests originales verdes); persona-juridica nunca name-link (prompts.length === 0).
- Persona-natural confirmada -> enlace + candidato de cosecha con rutHarvested DV-valido + provenance.
- Persona-natural ambigua -> null + sin cosecha (fail-closed, cola humana).
- Data-routing: el rutProveedor nunca aparece en vinculos/colas/prompts del pipeline.
- IDENT-12 preservado: el FK solo se puebla via confirmar() en determinista.
- harvest-rut reusa runBackfillRut (DV-gate + provenance NOT NULL); nunca fabrica RUT.
- Escritura remota + LIVE = checkpoint de operador.
</verification>

<success_criteria>
- MONEY-01 / MONEY-02: el enlace contrato->parlamentario funciona para funcionarios persona-natural via el pipeline confirmado/auditado (finalidad del dato), preservando la rama RUT-exacto; persona juridica sin cambios (entidad proveedora como sujeto, nunca atribucion personal).
- IDENT-10: en un match persona-natural confirmado se cosecha el rutProveedor DV-valido al `rut` de la maestra, con provenance, sin fabricar, solo en enlace confirmado; la escritura remota es checkpoint de operador.
- IDENT-12: el FK solo se puebla via confirmar() en un resultado determinista (RUT-exacto o nombre unico) o promocion humana; ambiguo -> null + cola humana.
- Data-routing gate: solo el proveedorNombre (persona natural) llega al LLM; el rutProveedor jamas toca el pipeline/prompt; test-enforced con SpyWriter/SpyProvider.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciliar-contrato -> correrPipeline/LLM | el NOMBRE del proveedor (persona natural) cruza al pipeline de adjudicacion; el rutProveedor (PII/clave dura) NUNCA debe cruzar |
| candidato de cosecha -> maestra remota (rut) | un RUT cosechado de ChileCompra se escribe al `rut` interno (PII, RLS deny-by-default); solo en enlace confirmado + DV-valido + provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-20 | Spoofing | atribucion por homonimia (persona natural enlazada al parlamentario equivocado) | mitigate | IDENT-12: solo correrPipeline determinista (nombre unico) mintea el FK via confirmar(); probable/revision/no_confirmado -> null + cola humana (fail-closed) |
| T-14-21 | Information Disclosure | el rutProveedor (PII) filtrado al LLM/prompt | mitigate | data-routing: solo proveedorNombre arma la MencionForanea; assertNoRutInLlmInput corre dentro de correrPipeline; test SpyWriter/SpyProvider asegura que el RUT no aparece en vinculos/colas/prompts |
| T-14-22 | Tampering | atribucion personal de un contrato a persona JURIDICA | mitigate | persona juridica NUNCA name-linkea (no llama correrPipeline); RUT-exacto-only; test con prompts.length === 0 |
| T-14-23 | Tampering | RUT fabricado/erroneo escrito a la maestra (IDENT-10) | mitigate | cosecha solo en enlace CONFIRMADO; isRutValido re-validado antes de emitir; runBackfillRut re-aplica DV-gate + provenance NOT NULL; rechazadas -> log de revision, nunca writer |
| T-14-24 | Repudiation | escritura remota del RUT cosechado sin auditoria de operador | mitigate | la escritura remota a la maestra + la corrida LIVE son checkpoint:human-action bloqueante; provenance "harvested from ChileCompra confirmed persona-natural match" viaja con cada fila |
</threat_model>

<output>
Crear `.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-04-SUMMARY.md` al terminar.
</output>
