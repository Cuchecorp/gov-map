---
phase: 14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dinero/package.json
  - packages/dinero/src/model.ts
  - packages/dinero/src/query.ts
  - packages/dinero/src/connector-chilecompra.ts
  - packages/dinero/src/parse-chilecompra.ts
  - packages/dinero/src/reconciliar-contrato.ts
  - packages/dinero/src/writer.ts
  - packages/dinero/src/writer-supabase.ts
  - packages/dinero/src/ingest-run.ts
  - packages/dinero/src/ingest-cli.ts
  - packages/dinero/src/live-chilecompra.probe.ts
  - packages/dinero/src/index.ts
  - packages/dinero/src/parse-chilecompra.test.ts
  - packages/dinero/src/reconciliar-contrato.test.ts
  - packages/dinero/src/writer.test.ts
  - packages/ingest/src/allowlist.ts
  - .env.example
autonomous: false
requirements: [MONEY-01, MONEY-02]
must_haves:
  truths:
    - "El conector @obs/dinero pide a ChileCompra en el ORDEN LOCKED: assertAllowedUrl entonces robots.isAllowed entonces rateLimiter.wait entonces fetcher.get"
    - "El flujo de 2 pasos resuelve RUT a CodigoEmpresa (BuscarProveedor) antes de pedir ordenes por CodigoProveedor mas dia"
    - "Un RUT invalido (DV modulo-11) va a cuarentena, nunca produce fila silenciosa"
    - "El enlace contrato a parlamentario se mintea SOLO por RUT-exacto unico via confirmar(); sin RUT interno el enlace es null"
    - "El writer es idempotente: re-correr con el mismo input produce los mismos conteos"
    - "Sin MERCADOPUBLICO_TICKET el CLI degrada a dry-run (InMemory), nunca fabrica"
  artifacts:
    - path: "packages/dinero/src/reconciliar-contrato.ts"
      provides: "enlace RUT-exacto determinista a EnlaceConfirmado o null; sin correrPipeline"
      contains: "confirmar("
    - path: "packages/dinero/src/connector-chilecompra.ts"
      provides: "ContratoConnector REST (BuscarProveedor mas ordenesdecompra) mas ChileCompraBloqueadaError"
      contains: "assertAllowedUrl"
    - path: "packages/dinero/src/model.ts"
      provides: "Contrato mas Contratista mas Zod schemas; licencia = mencion de la fuente"
      contains: "z."
    - path: "packages/dinero/src/writer-supabase.ts"
      provides: "SupabaseDineroWriter idempotente mas marcarIngestado"
      contains: "upsert"
  key_links:
    - from: "packages/dinero/src/reconciliar-contrato.ts"
      to: "@obs/identity (matchDeterminista rama rut mas confirmar)"
      via: "import; acepta SOLO estado=confirmado y metodo=rut"
      pattern: "matchDeterminista|confirmar"
    - from: "packages/dinero/src/connector-chilecompra.ts"
      to: "@obs/ingest assertAllowedUrl"
      via: "validacion SSRF antes de cada fetch"
      pattern: "assertAllowedUrl"
---

<objective>
Crear el paquete `packages/dinero/` (`@obs/dinero`) espejando `@obs/probidad` archivo-por-archivo: conector REST de ChileCompra (flujo de 2 pasos con `MERCADOPUBLICO_TICKET`), reconciliacion por RUT-exacto determinista (reusa `isRutValido`/`normRut`/`matchDeterminista`/`confirmar`), sub-maestra `contratista`, writer idempotente con marcador de 3 estados, y CLI acotado que degrada a dry-run sin ticket.

Purpose: El motor de ingesta. La regla load-bearing es que el enlace contrato-a-parlamentario se fija UNICAMENTE por RUT-exacto contra el RUT interno de la maestra; NUNCA por nombre. Por eso `@obs/dinero` NO depende de `@obs/adjudication` y NO llama `correrPipeline` (arrastraria el fallback name-only del LLM). Mientras el RUT interno este vacio (deuda IDENT-10), todo enlace es `null` y el parlamentario queda "no consultado todavia" honestamente; se construye igual.

Output: `packages/dinero/src/*` mas tests mas rama ChileCompra verificada en el allowlist SSRF mas `MERCADOPUBLICO_TICKET` en `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-RESEARCH.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-PATTERNS.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-CONTEXT.md

# Analogo de conector COMPLETO a espejar archivo-por-archivo (leer cada uno antes de su gemelo dinero):
@packages/probidad/src/connector-infoprobidad.ts
@packages/probidad/src/parse-infoprobidad.ts
@packages/probidad/src/model.ts
@packages/probidad/src/reconciliar-declarante.ts
@packages/probidad/src/writer.ts
@packages/probidad/src/writer-supabase.ts
@packages/probidad/src/ingest-run.ts
@packages/probidad/src/ingest-cli.ts
@packages/probidad/src/index.ts
@packages/probidad/package.json

# Simbolos de identidad reusados TAL CUAL (NO reimplementar modulo-11):
@packages/identity/src/deterministic.ts
@packages/identity/src/enlace-confirmado.ts

# Allowlist SSRF (verificar la rama ChileCompra):
@packages/ingest/src/allowlist.ts

<interfaces>
<!-- Simbolos exactos a reusar; NO explorar el codebase, usar estos directamente. -->

De @obs/identity (packages/identity/src/deterministic.ts):
  export function normRut(rut: string): string;                 // strip puntos/espacios/guion mas casefold k
  export function isRutValido(rut: string): boolean;            // DV modulo-11; false = tratar como sin RUT
  export function matchDeterminista(mention, maestra): Resolution;
  // Resolution.confirmado tiene .metodo: "rut" | "nombre" | "nombre-estricto"
  // ACEPTAR SOLO estado === "confirmado" y metodo === "rut"; CUALQUIER otro caso el enlace es null.
  // La rama RUT (deterministic.ts:98-105) confirma SOLO si exactamente 1 match por RUT normalizado.

De @obs/identity (packages/identity/src/enlace-confirmado.ts):
  export interface EnlaceConfirmado { readonly parlamentarioId: string; readonly metodo: "determinista"|"humano"; }
  export function confirmar(parlamentarioId: string, metodo?): EnlaceConfirmado;  // UNICA factory
  // El simbolo branded ENLACE_CONFIRMADO es module-private: NO re-exportar desde index.ts.
  // Storage del FK: parlamentario_id: enlace?.parlamentarioId ?? null

De @obs/ingest:
  Fetcher, HostRateLimiter, RobotsGuard, assertAllowedUrl, FetchError, type AllowlistOptions
  // ORDEN LOCKED: assertAllowedUrl(url, allowlist) entonces robots.isAllowed(url) entonces rateLimiter.wait(host) entonces fetcher.get(...)
  // mercadopublico.cl YA esta en DEFAULT_ALLOWED_SUFFIXES (allowlist.ts:27) y cubre api.mercadopublico.cl.

ChileCompra REST (flujo de 2 pasos OBLIGATORIO; la API NO filtra ordenes por RUT):
  Paso 1: GET https://api.mercadopublico.cl/servicios/v1/Publico/Empresas/BuscarProveedor?rutempresaproveedor=<RUT>&ticket=<T>
          a { CodigoEmpresa, NombreEmpresa }
  Paso 2: GET https://api.mercadopublico.cl/servicios/v1/publico/ordenesdecompra.json?fecha=<ddmmaaaa>&CodigoProveedor=<codigo>&ticket=<T>
          a { Cantidad, Listado: [...] }   ; fecha = UN SOLO DIA, iterar ventanas de dia.

Contrato (el writer escribe estas columnas; deben mapear a 0023_dinero.sql del Plan 01):
  fuente_id (= codigo unico de la orden), fecha_corte, codigo_orden, proveedor_nombre,
  tipo_persona ("natural"|"juridica"), organismo, monto (string verbatim), fecha_oc (date),
  mencion_proveedor, estado_vinculo, parlamentario_id (= enlace?.parlamentarioId ?? null),
  origen ("chilecompra"), fecha_captura, enlace, licencia ("mencion de la fuente").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: package.json mas model mas query mas parse mas reconciliar (nucleo puro, sin red)</name>
  <files>packages/dinero/package.json, packages/dinero/src/model.ts, packages/dinero/src/query.ts, packages/dinero/src/parse-chilecompra.ts, packages/dinero/src/reconciliar-contrato.ts, packages/dinero/src/parse-chilecompra.test.ts, packages/dinero/src/reconciliar-contrato.test.ts</files>
  <behavior>
    - parse: JSON de ChileCompra valido a Contrato[] con campos VERBATIM (monto como string crudo, sin computo).
    - parse drift: respuesta sin `Listado`/`Cantidad` (forma inesperada) THROW (cuarentena aguas arriba), NUNCA 0 filas silenciosas.
    - tipoPersona: RUT con cuerpo menor a 50000000 a "natural"; mayor o igual a 50000000 a "juridica" (fallback; preferir campo de la fuente si existe). Test ambos lados del umbral.
    - reconciliar RUT invalido (DV malo): isRutValido false a cuarentena, enlace null, sin fila de contrato confirmada.
    - reconciliar sin RUT interno en la maestra (caso IDENT-10): enlace null mas mencion cruda mas estadoVinculo "no_confirmado" o null; NUNCA un parlamentario_id poblado.
    - reconciliar RUT-exacto unico: maestra con 1 parlamentario de ese RUT a confirmar(id,"determinista") a EnlaceConfirmado; estadoVinculo "confirmado".
    - reconciliar RUT con 2 o mas matches: NO confirma (fail-closed) a enlace null.
  </behavior>
  <action>
Crear `packages/dinero/package.json` copiando `packages/probidad/package.json` pero con `"name": "@obs/dinero"` y SIN la dependencia `@obs/adjudication` (no se usa `correrPipeline`; enlace RUT-only). Dependencias workspace: `@obs/core`, `@obs/identity`, `@obs/ingest`, `@supabase/supabase-js`, `zod`. Scripts `test`/`typecheck`/`ingest` identicos a probidad.

`model.ts` (espejo de probidad model.ts:1-60): tipos `Contrato` y `Contratista` mas Zod schemas para la respuesta de ChileCompra (`BuscarProveedorResponse` con `CodigoEmpresa`/`NombreEmpresa`; `OrdenesResponse` con `Cantidad`/`Listado[]`). Campos literales como `z.string().nullable()`, sin sumas. `Contrato` lleva provenance inline (`origen`, `fecha_captura`, `enlace`) mas `licencia` con valor "mencion de la fuente" (NO el `'CC BY 4.0'` de probidad). NUEVO vs probidad: `Contratista` keyed por `rutProveedor` mas `tipoPersona` ("natural"|"juridica"); el sujeto del contrato es la entidad proveedora, distinta de cualquier enlace. Clave de version del contrato = `(fuenteId = codigo de la orden, fechaCorte)`.

`query.ts` (reemplaza `sparql.ts` de probidad; Claude's Discretion): builders de URL puros `urlBuscarProveedor(rut, ticket)` y `urlOrdenesDeCompra(codigoProveedor, ddmmaaaa, ticket)` apuntando a `https://api.mercadopublico.cl/servicios/v1/...` mas un generador de ventanas de fecha `fechasEntre(desde, hasta)` que emite `ddmmaaaa` por dia. NUNCA hardcodear el ticket; recibirlo como parametro.

`parse-chilecompra.ts` (espejo de parse-infoprobidad.ts): parser PURO sin red. Valida la respuesta JSON con los Zod schemas de `model.ts`; una forma inesperada THROW (capturada por `ingest-run` como drift estructural a cuarentena). Mapea a `Contrato[]` VERBATIM (monto como string crudo, sin convertir a numero). Funcion `tipoPersona(rut)`: cuerpo de `normRut(rut).slice(0,-1)` mayor o igual a 50000000 a "juridica", si no "natural"; pero preferir un campo `tipoProveedor` o equivalente de la respuesta si esta presente (marcar el umbral como fallback MEDIUM en comentario).

`reconciliar-contrato.ts` (espejo de reconciliar-declarante.ts:72-178 con el matcher CAMBIADO): mantener la FORMA de salida (filas con `enlace: EnlaceConfirmado | null` mas `mencion*` cruda mas `estadoVinculo` mas set `parlamentariosConfirmados` para `marcarIngestado`). REEMPLAZAR la llamada name-only `correrPipeline` por enlace RUT-exacto: pasar el RUT del proveedor por `isRutValido` (RUT invalido a cuarentena, enlace null, NO escribir); normalizar con `normRut`; llamar `matchDeterminista({ rut: rutProveedor, nombreNormalizado: "", camara, periodo }, maestra)` y aceptar SOLO `estado === "confirmado" && metodo === "rut"` a `confirmar(res.id, "determinista")`; CUALQUIER otro resultado (incluido caer a la rama nombre) a `enlace: null` mas mencion cruda. NO importar `@obs/adjudication` ni `correrPipeline`. NO usar `normalizarNombre` para enlazar.

Tests (`parse-chilecompra.test.ts`, `reconciliar-contrato.test.ts`): cubrir el bloque `<behavior>` con fixtures sinteticos VERBATIM de la forma de respuesta documentada (no requieren red).

Archivos SIN BOM ni unicode invisible (solo UTF-8 plano; referir codepoints por nombre, jamas pegar un BOM literal).
  </action>
  <verify>
    <automated>cd packages/dinero && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test` verde para `parse-chilecompra.test.ts` y `reconciliar-contrato.test.ts`. `reconciliar-contrato.ts` NO contiene `correrPipeline`, `@obs/adjudication` ni `normalizarNombre`. `package.json` NO declara `@obs/adjudication`. `model.ts` usa licencia "mencion de la fuente", nunca 'CC BY 4.0'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: connector mas writer mas writer-supabase mas ingest-run mas cli mas probe mas index mas allowlist-verify mas .env.example</name>
  <files>packages/dinero/src/connector-chilecompra.ts, packages/dinero/src/writer.ts, packages/dinero/src/writer-supabase.ts, packages/dinero/src/ingest-run.ts, packages/dinero/src/ingest-cli.ts, packages/dinero/src/live-chilecompra.probe.ts, packages/dinero/src/index.ts, packages/dinero/src/writer.test.ts, packages/ingest/src/allowlist.ts, .env.example</files>
  <behavior>
    - writer idempotente: upsert del mismo lote dos veces a mismos conteos (sin duplicados); clave de version (fuenteId, fechaCorte).
    - writer marcarIngestado: tras una corrida que toco parlamentarios X, el marcador contratos_ingesta_estado registra esos ids con `ingestado_hasta`.
    - writer contratista sub-maestra: upsert keyed por rutProveedor last-write-wins (mirror dedupePorClave).
    - cli sin ticket o --dry-run: usa InMemoryDineroWriter, no toca red de escritura real, nunca fabrica.
  </behavior>
  <action>
`connector-chilecompra.ts` (espejo de connector-infoprobidad.ts:1-103): inyectar deps `{ fetcher, rateLimiter, robots, allowlist? }`. Metodo `fetch(url)` privado en el ORDEN LOCKED EXACTO: `assertAllowedUrl(url, allowlist)` entonces `await robots.isAllowed(url)` (false a `RobotsDisallowError`) entonces `await rateLimiter.wait(parsed.host)` (delay 2-3s) entonces `fetcher.get({ url, headers: HEADERS_CHILECOMPRA })`. UA identificatorio con sufijo `Bot-Ciudadano/1.0` mas `Accept: application/json`. Clase `ChileCompraBloqueadaError(url, status)` (espejo de `InfoProbidadBloqueadaError`): mapear 403/503/429 (rate-limit o ticket agotado) para degradar honestamente ese RUT sin abortar. Metodos publicos `buscarProveedor(rut, ticket)` (paso 1) y `ordenesDeCompra(codigoProveedor, ddmmaaaa, ticket)` (paso 2) usando los builders de `query.ts`. NUNCA `BaseConnector.run` (su cache diaria saltaria re-corridas). NUNCA interpolar el ticket en mensajes de error.

`writer.ts` (espejo de writer.ts): interface `DineroWriter` con `upsertContratos(...)` (versionado, idempotente), `upsertContratistas(...)` (sub-maestra keyed por RUT, last-write-wins) y `marcarIngestado(parlamentarioIds, hasta)`. `InMemoryDineroWriter` (fake para dry-run y tests).

`writer-supabase.ts` (espejo de writer-supabase.ts): `SupabaseDineroWriter` con `createClient(url, serviceKey, { auth: { persistSession:false, autoRefreshToken:false }})`; service key SOLO de env, nunca interpolada en errores. Upsert idempotente por chunks con `upsert(lote, { onConflict: "fuente_id,fecha_corte", ignoreDuplicates:false })`; en error `throw new Error(\`upsert contrato fallo: ${error.message}\`)`. FK plano: `parlamentario_id: f.enlace?.parlamentarioId ?? null`. `marcarIngestado` upserta `contratos_ingesta_estado` con `onConflict: "parlamentario_id"`. Sub-maestra `contratista` con `onConflict: "rut_proveedor"`.

`ingest-run.ts` (espejo de ingest-run.ts:114-207): loop serial por RUT (`TareaRut { rut }` reemplaza `TareaDeclarante { nombre }`). Por RUT: `ChileCompraBloqueadaError` a push a `degradaciones` y `continue` (NO abort); drift estructural (parse throw) a cuarentena (0 filas, nunca fabricar); fuente vacia o sin proveedor a 0 filas ("consultado sin contratos"). Flujo: `isRutValido` entonces 2 pasos del conector entonces parse entonces `reconciliarContrato(...)` entonces `writer.upsertContratistas(...)` mas `writer.upsertContratos(...)` entonces al final `writer.marcarIngestado([...marcados], hasta)`. R2 BLOQUEADO (probado): omitir snapshot crudo, dejar marca, NO bloquear.

`ingest-cli.ts` (espejo de ingest-cli.ts:62-179): parse de flags antes de cualquier red o DB; flag `--rut` (reemplaza `--nombre`); service key mas `MERCADOPUBLICO_TICKET` SOLO de env (nunca argv). `--dry-run` o falta de ticket o service key a `InMemoryDineroWriter` (degrada a dry-run, NUNCA fabrica). Construir colaboradores REALES de @obs/ingest (`new Fetcher()`, `new HostRateLimiter()`, `new RobotsGuard({ allowlist: {} })`). Guarda `isMain` para el entry-point.

`live-chilecompra.probe.ts` (espejo de live-infoprobidad.probe.ts): probe LIVE manual (NO CI); una request real respetando delay 2-3s mas UA, asertando la forma de la respuesta. Requiere ticket real (operador).

`index.ts` (espejo de index.ts): barrel re-exports. NO re-exportar el simbolo branded `ENLACE_CONFIRMADO` (module-private).

`packages/ingest/src/allowlist.ts` VERIFICAR (reportes de agentes en conflicto): `mercadopublico.cl` YA esta en `DEFAULT_ALLOWED_SUFFIXES` (linea 27) y cubre `api.mercadopublico.cl` como subdominio. CONFIRMAR que sigue presente; SOLO si faltara, agregarlo. Reportar en el SUMMARY si se edito o si ya estaba (esperado: ya estaba, archivo sin cambios).

`.env.example` agregar `MERCADOPUBLICO_TICKET=` con un comentario: secreto de operador (Clave Unica, 10000 req/dia); sin el el CLI de @obs/dinero degrada a dry-run; server-only, nunca al cliente. Mantener `MONEY_PUBLIC_ENABLED=false` ya presente.

`writer.test.ts`: cubrir el bloque `<behavior>` (idempotencia, marcarIngestado, sub-maestra) con `InMemoryDineroWriter`.

Archivos SIN BOM ni unicode invisible.
  </action>
  <verify>
    <automated>cd packages/dinero && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm --filter @obs/dinero test` verde (incluye writer.test.ts). `connector-chilecompra.ts` contiene `assertAllowedUrl` y `ChileCompraBloqueadaError`. `packages/ingest/src/allowlist.ts` contiene `mercadopublico.cl`. `.env.example` contiene `MERCADOPUBLICO_TICKET`. El ticket nunca se interpola en mensajes de error. `index.ts` no re-exporta `ENLACE_CONFIRMADO`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>El conector `@obs/dinero` esta completo y la suite unit pasa (parse, reconciliar, writer con InMemory; sin red). El allowlist SSRF ya cubre `api.mercadopublico.cl`. La forma EXACTA de la respuesta JSON de ChileCompra (A2: campos de BuscarProveedor y ordenesdecompra) esta derivada de docs, NO de una respuesta capturada; el enlace RUT-exacto real requiere el RUT interno de la maestra (deuda IDENT-10).</what-built>
  <how-to-verify>
Corrida LIVE acotada (operador; requiere `MERCADOPUBLICO_TICKET` real, host Windows a usar Bash tool / git-bash NO PowerShell):

  1. Exportar `MERCADOPUBLICO_TICKET` y `SUPABASE_SERVICE_KEY` en el entorno (nunca por argv).
  2. Correr el probe: `pnpm --filter @obs/dinero exec tsx src/live-chilecompra.probe.ts <un-RUT-de-empresa-conocido>` respetando el delay 2-3s.
  3. Confirmar que la forma de la respuesta real coincide con los Zod schemas de `model.ts` (BuscarProveedor a CodigoEmpresa; ordenesdecompra a Cantidad/Listado). Si difiere, ajustar el schema y el parser y reportar el delta.
  4. (Opcional, si hay RUT interno poblado) corrida acotada del CLI por uno o dos RUT con writer real para verificar idempotencia (re-correr a mismos conteos).

Sin ticket, el CLI degrada a dry-run; eso es correcto. NUNCA fabricar datos.
  </how-to-verify>
  <resume-signal>Escribir "probe OK, schema coincide" o describir el delta de forma de la respuesta.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/dinero test` verde (parse, reconciliar, writer).
- `reconciliar-contrato.ts` sin `correrPipeline`/`@obs/adjudication`/`normalizarNombre`; enlace SOLO por rama RUT.
- `connector-chilecompra.ts` en el ORDEN LOCKED; ticket nunca interpolado en errores.
- `mercadopublico.cl` presente en el allowlist; `MERCADOPUBLICO_TICKET` en `.env.example`.
- Probe LIVE confirma la forma de respuesta (operador).
</verification>

<success_criteria>
- Criterio 1 (MONEY-01): `@obs/dinero` ingiere por RUT (flujo 2 pasos), valida DV modulo-11, etiqueta natural vs juridica, crea sub-maestra `contratista`; barrido serial por RUT con delay 2-3s.
- Criterio 3 (MONEY-02): enlace SOLO por RUT-exacto; sin RUT interno a null; RUT invalido a cuarentena, nunca fila silenciosa.
- Criterio 4: el sujeto es la entidad proveedora (sub-maestra), distinta de cualquier enlace al parlamentario.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @obs/dinero a api.mercadopublico.cl | URL construida por el conector; cruza a un servicio externo con ticket secreto |
| respuesta JSON de ChileCompra a parser | input no confiable; validado por Zod antes de mapear |
| operador a CLI | service key y ticket entran SOLO por env, nunca por argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | MERCADOPUBLICO_TICKET o service key en logs o bundle | mitigate | secreto SOLO de env, server-only, jamas interpolado en mensajes de error (espejo writer-supabase) |
| T-14-06 | Tampering | atribucion equivocada por homonimia (enlace por nombre) | mitigate | enlace SOLO rama RUT de matchDeterminista mas confirmar(); sin `correrPipeline`/`normalizarNombre`; RUT invalido a cuarentena |
| T-14-07 | SSRF | URL fabricada hacia metadata o loopback | mitigate | assertAllowedUrl antes de cada fetch (ORDEN LOCKED); mercadopublico.cl allowlisted, todo lo demas deny |
| T-14-08 | Denial of Service | rafaga contra el WAF gubernamental o agotar el ticket (10000/dia) | mitigate | rateLimiter.wait delay 2-3s serial por host; 429 mapeado a ChileCompraBloqueadaError a degradacion honesta |
| T-14-09 | Tampering | drift de la respuesta leido como "sin contratos" | mitigate | Zod gate; forma inesperada THROW a cuarentena, nunca 0 filas silenciosas |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | N/A: cero paquetes externos nuevos; solo deps de workspace ya auditadas (RESEARCH Package Legitimacy Audit) |
</threat_model>

<output>
Crear `.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-02-SUMMARY.md` al terminar.
</output>
