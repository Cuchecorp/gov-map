---
phase: 14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0023_dinero.sql
  - supabase/tests/0024_dinero.test.sql
autonomous: false
requirements: [MONEY-02]
must_haves:
  truths:
    - "La tabla contrato existe y es public-read (la ficha puede leer hechos de contrato)"
    - "La tabla contratista (sub-maestra) niega por defecto a anon: RLS enabled, 0 policies, sin grant SELECT a anon"
    - "El marcador contratos_ingesta_estado existe y es public-read (distingue 'no consultado' de 'consultado sin contratos')"
    - "El RPC contratos_de_parlamentario(p_id) es security-definer, revocado de public, con grant a anon, y devuelve SOLO filas confirmadas (parlamentario_id = p_id)"
    - "El pgTAP codifica el candado deny-by-default + revoke (lección Phase 11) y la firma del RPC"
  artifacts:
    - path: "supabase/migrations/0023_dinero.sql"
      provides: "contrato (public-read) + contratista (deny-by-default+revoke) + contratos_ingesta_estado (public-read) + RPC contratos_de_parlamentario"
      contains: "create table contrato"
    - path: "supabase/tests/0024_dinero.test.sql"
      provides: "pgTAP: tablas existen, RLS, deny-by-default contratista, RPC security-definer"
      contains: "select plan("
  key_links:
    - from: "supabase/migrations/0023_dinero.sql"
      to: "parlamentario(id)"
      via: "FK parlamentario_id references parlamentario(id) on delete set null"
      pattern: "references parlamentario\\(id\\)"
    - from: "contratos_de_parlamentario"
      to: "contrato"
      via: "where c.parlamentario_id = p_id (SOLO confirmados)"
      pattern: "where c\\.parlamentario_id = p_id"
---

<objective>
Crear la migración SQL del bloque MONEY-Contratos (`0023_dinero.sql`) y su pgTAP (`0024_dinero.test.sql`): la tabla `contrato` (public-read, versionada), la sub-maestra `contratista` (deny-by-default + revoke, lección Phase 11), el marcador `contratos_ingesta_estado` (public-read), y el RPC `contratos_de_parlamentario(p_id)` security-definer. Espejo archivo-por-archivo de `0022_probidad.sql` + `0021_lobby.sql`.

Purpose: Es el piso de datos de toda la fase. La sub-maestra NACE deny-by-default (criterio de éxito 4) para que no exista una sola corrida en que `anon` pueda leer PII de contratistas; el RPC es el ÚNICO canal público hacia los datos. El gate `moneyPublicEnabled()` (Plan 03) es el segundo candado.

Output: `supabase/migrations/0023_dinero.sql`, `supabase/tests/0024_dinero.test.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-RESEARCH.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-PATTERNS.md
@.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-CONTEXT.md

# Análogos VERBATIM a copiar (leer estos antes de escribir):
@supabase/migrations/0022_probidad.sql
@supabase/migrations/0021_lobby.sql
@supabase/tests/0022_probidad.test.sql
@supabase/tests/0023_money_gate.test.sql

<interfaces>
<!-- Contratos que el RPC debe respetar para que Plan 03 (ficha) y Plan 02 (writer) acoplen. -->

Forma del RPC público (Plan 03 la consume vía sb.rpc("contratos_de_parlamentario", { p_id })):
  contratos_de_parlamentario(p_id text) returns table (
    -- SOLO campos que la fuente publica + fecha_corte; SIN parlamentario_id interno;
    -- por defecto SIN rut_proveedor crudo (OQ3/A4: PII pendiente de sign-off legal — no emitir).
    codigo_orden text, proveedor_nombre text, tipo_persona text, organismo text,
    monto text, fecha_oc date, origen text, fecha_captura timestamptz,
    fecha_corte date, enlace text, licencia text
  )

Marcador de 3 estados (Plan 03 lo lee vía .maybeSingle()):
  contratos_ingesta_estado(parlamentario_id text PK, ingestado_hasta date, fecha_captura timestamptz)
  - FILA AUSENTE  ⇒ "no consultado todavía"
  - FILA PRESENTE + 0 filas en contrato ⇒ "consultado sin contratos"
  - FILA PRESENTE + ≥1 fila ⇒ "enlazado"

Clave de versión de contrato (Plan 02 writer upserta por onConflict sobre esta PK):
  primary key (fuente_id, fecha_corte)   -- fuente_id = código único de la orden de compra; NUNCA keyear por parlamentario (Pitfall: colapsaría órdenes distintas)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0023_dinero.sql (contrato + contratista + marcador + RPC)</name>
  <files>supabase/migrations/0023_dinero.sql</files>
  <action>
Crear `supabase/migrations/0023_dinero.sql` copiando los cuatro arquetipos de sección de `0022_probidad.sql` + `0021_lobby.sql`. NO inventar arquitectura: cada bloque tiene un hermano VERBATIM.

(1) Tabla public-read `contrato` (espejo de `declaracion`, 0022 líneas 79-107):
  - PK de versión compuesta `primary key (fuente_id, fecha_corte)` — `fuente_id` = código único de la orden de compra ChileCompra; `fecha_corte` = fecha de corte de la consulta por RUT. NUNCA keyear por `parlamentario_id` solo (colapsaría órdenes distintas).
  - `parlamentario_id text references parlamentario(id) on delete set null` (nullable) + `mencion_proveedor text not null` (nombre crudo del proveedor como lo da la fuente) + `estado_vinculo text` ('confirmado' | 'no_confirmado' | null).
  - Columnas literales VERBATIM (sin cómputo): `codigo_orden text`, `proveedor_nombre text`, `tipo_persona text` ('natural' | 'juridica'), `organismo text`, `monto text` (literal string, NO numeric — verbatim de la fuente), `fecha_oc date`.
  - Provenance inline NOT NULL (FND-08, 0022:91-96): `origen text not null` (valor "chilecompra"), `fecha_captura timestamptz not null default now()`, `enlace text not null`. CRÍTICO: `licencia text not null default 'mención de la fuente'` — NUNCA el default `'CC BY 4.0'` de probidad (criterio de éxito: atribución ChileCompra = mención de la fuente).
  - RLS public-read EXPLÍCITO (0022:100-105): `enable row level security` + `create policy contrato_public_read ... for select to anon using (true)` + `grant select on contrato to anon`. Sin policies de insert/update/delete (el writer usa service key).
  - Índice `create index contrato_parlamentario_idx on contrato (parlamentario_id)` (0022:107).

(2) Sub-maestra deny-by-default `contratista` (espejo de `declaracion_familiar` 0022:250-279 / `lobby_contraparte` 0021:64-98), keyed por RUT del proveedor (contiene PII potencial = RUT de persona natural):
  - `rut_proveedor text primary key` (ya DV-validado por el writer; clave de la sub-maestra), `nombre text`, `tipo_persona text` ('natural'|'juridica'), `codigo_empresa text` (CodigoEmpresa de ChileCompra), provenance inline (`origen`, `fecha_captura`, `enlace`, `licencia text not null default 'mención de la fuente'`).
  - DENY-BY-DEFAULT VERBATIM: `alter table contratista enable row level security;` con CERO `create policy`. NINGÚN `grant select ... to anon`. `revoke all on contratista from anon, authenticated;` (cierra el hueco de default-privileges de Supabase, lección LEGAL-03/Phase 11). El writer (service_role) conserva privilegios y bypassa RLS.

(3) Marcador `contratos_ingesta_estado` (espejo de `probidad_ingesta_estado` 0022:361-368) — sin PII, public-read:
  - `parlamentario_id text primary key references parlamentario(id) on delete cascade`, `ingestado_hasta date`, `fecha_captura timestamptz not null default now()`.
  - `enable row level security` + `create policy contratos_ingesta_estado_public_read ... for select to anon using (true)` + `grant select ... to anon`. Permite distinguir "no consultado" (fila ausente) de "consultado sin contratos" (fila presente).

(4) RPC `contratos_de_parlamentario(p_id text)` security-definer (espejo de `declaraciones_de_parlamentario` 0022:287-302):
  - `language sql stable security definer set search_path = ''`.
  - Selecciona SOLO los campos source-published + `fecha_corte`: `codigo_orden, proveedor_nombre, tipo_persona, organismo, monto, fecha_oc, origen, fecha_captura, fecha_corte, enlace, licencia` desde `public.contrato` `where c.parlamentario_id = p_id` `order by c.fecha_oc desc`. SIN `parlamentario_id` interno en el output. SIN `rut_proveedor` crudo por defecto (OQ3/A4: PII pendiente de sign-off legal — no emitir hasta F13).
  - `revoke execute on function public.contratos_de_parlamentario(text) from public;` + `grant execute on function public.contratos_de_parlamentario(text) to anon;` sobre la firma EXACTA.

Encabezar el archivo con un comentario de cabecera (estilo 0022) explicando: gate de exposición pendiente (Plan 03 lo aplica con `moneyPublicEnabled`), licencia = mención de la fuente (NO CC BY), apply remoto = checkpoint de operador.

NO crear pg_cron job en esta migración (Open Question 2: ni 0021 ni 0022 registraron cron; queda como checkpoint de operador, mismo patrón que fases previas).

ARCHIVO SIN BOM y sin unicode invisible: escribir solo ASCII/UTF-8 plano; NO pegar marcas de orden de bytes ni comillas tipográficas. Referir codepoints por nombre si hiciera falta, nunca pegar un BOM literal.
  </action>
  <verify>
    <automated>grep -c "create table contrato" "supabase/migrations/0023_dinero.sql"</automated>
  </verify>
  <done>El archivo existe; contiene `create table contrato`, `create table contratista`, `create table contratos_ingesta_estado`, `create function public.contratos_de_parlamentario`, `revoke all on contratista from anon, authenticated`, y `default 'mención de la fuente'`. NO contiene `'CC BY 4.0'`. NO contiene `cron.schedule`.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0024_dinero.test.sql (deny-by-default + RPC + version key)</name>
  <files>supabase/tests/0024_dinero.test.sql</files>
  <action>
Crear `supabase/tests/0024_dinero.test.sql` copiando `0022_probidad.test.sql` y `0023_money_gate.test.sql`. Usar el número de test `0024` (el `0023_money_gate.test.sql` ya existe — evitar colisión). `begin; select plan(N);` con N = número exacto de asserts, y `select * from finish; rollback;` al final.

Asserts mínimos (mapean a los criterios de éxito de la fase):
  - Existencia: `has_table('contrato')`, `has_table('contratista')`, `has_table('contratos_ingesta_estado')`.
  - RLS habilitada en las tres tablas (`is_rls_enabled` o el helper equivalente usado por 0022.test).
  - `contrato` PK = exactamente la clave de versión `(fuente_id, fecha_corte)` (espejo de 0022.test:46-53).
  - `contrato` public-read: existe policy de select para anon + `anon` TIENE grant SELECT (espejo 0022.test:64-70).
  - `contratista` DENY-BY-DEFAULT (el candado de la fase, lección Phase 11, espejo 0022.test:55-62): RLS enabled + CERO policies sobre `contratista` + `anon` NO tiene grant SELECT sobre `contratista`. Tres asserts distintos.
  - `contratos_ingesta_estado` public-read: policy select para anon + grant SELECT a anon.
  - RPC: `has_function('contratos_de_parlamentario')`, es SECURITY DEFINER, `anon` TIENE EXECUTE, y `public` NO tiene EXECUTE (revocado).

ARCHIVO SIN BOM ni unicode invisible (ver Task 1).

NOTA: el apply remoto y la corrida de este pgTAP contra el schema APLICADO es el checkpoint de operador (Task 3) — CI no prueba que el DDL corrió (falso positivo, lección 0022).
  </action>
  <verify>
    <automated>grep -E "select plan\(|has_table\('contratista'\)|contratos_de_parlamentario" "supabase/tests/0024_dinero.test.sql" | grep -v '^#'</automated>
  </verify>
  <done>El archivo existe con `select plan(N)`, asserts de existencia de las 3 tablas, los 3 asserts deny-by-default de `contratista`, y los asserts del RPC (security definer + execute a anon + revocado de public). El número de plan coincide con el conteo de asserts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>La migración `0023_dinero.sql` y el pgTAP `0024_dinero.test.sql` están escritos y validados estructuralmente (grep gates verdes). El DDL aún NO se ha aplicado al Postgres remoto — CI no aplica DDL (lección 0022: build verde no prueba que el schema corrió).</what-built>
  <how-to-verify>
Aplicar la migración al remoto y correr el pgTAP contra el schema APLICADO (host Windows → usar el Bash tool / git-bash, NO PowerShell; el `.env` tiene BOM → pasar `--db-url` explícito, ver MEMORY env-credentials-reality.md):

  1. `supabase db push --db-url "$SUPABASE_DB_URL"`  (esquiva el BOM del .env)
  2. `supabase test db --db-url "$SUPABASE_DB_URL"`  (corre 0024_dinero.test.sql contra el schema aplicado)

Confirmar que el pgTAP pasa 100% (todos los asserts verdes), en particular los 3 asserts deny-by-default de `contratista` (RLS enabled + 0 policies + 0 grant SELECT a anon). Si algún assert falla, NO es un falso positivo de CI — es un hueco real en el DDL; reportar el assert rojo.
  </how-to-verify>
  <resume-signal>Escribir "aplicado, pgTAP N/N verde" o describir los asserts en rojo.</resume-signal>
</task>

</tasks>

<verification>
- `0023_dinero.sql` y `0024_dinero.test.sql` existen, sin BOM.
- `contratista` deny-by-default + revoke (3 asserts pgTAP verdes contra schema aplicado).
- RPC `contratos_de_parlamentario` security-definer, revocado de public, grant a anon.
- `licencia` default = 'mención de la fuente' (NO 'CC BY 4.0').
- Apply remoto + pgTAP verde confirmado por el operador.
</verification>

<success_criteria>
- Criterio 4 (deny-by-default): `contratista` nace RLS-enabled + revoke, verificado por pgTAP contra schema aplicado.
- Criterio 3 (enlace solo RUT-exacto): el RPC devuelve SOLO `where parlamentario_id = p_id` (jamás un no-confirmado bajo un parlamentario).
- Marcador de 3 estados disponible para la ficha (Plan 03).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (navegador) → Postgres | Lectura pública; debe pasar SOLO por policies public-read + RPC, jamás directo a `contratista` |
| writer (service_role) → Postgres | Escritura; bypassa RLS por diseño |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | tabla `contratista` (RUT PII de persona natural) | mitigate | `revoke all on contratista from anon, authenticated` VERBATIM + 0 policies; pgTAP codifica los 3 asserts deny-by-default |
| T-14-02 | Information Disclosure | RUT crudo del proveedor emitido por el RPC | mitigate | RPC NO selecciona `rut_proveedor` (OQ3/A4); solo nombre + tipo_persona hasta sign-off legal F13 |
| T-14-03 | Elevation of Privilege | RPC security-definer ejecutable por cualquiera | mitigate | `revoke execute ... from public` + `grant execute ... to anon` sobre la firma exacta; RPC filtra SOLO `parlamentario_id = p_id` confirmados |
| T-14-04 | Tampering | falso positivo de CI (DDL no aplicado) | mitigate | apply remoto + pgTAP contra schema APLICADO = checkpoint de operador (no CI) |
</threat_model>

<output>
Crear `.planning/phases/14-money-contratos-chilecompra-por-rut-sub-maestra-de-contratis/14-01-SUMMARY.md` al terminar.
</output>
