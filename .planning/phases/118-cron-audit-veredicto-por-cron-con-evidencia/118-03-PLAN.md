---
phase: 118-cron-audit-veredicto-por-cron-con-evidencia
plan: 03
type: execute
wave: 3
depends_on: ["118-02"]
files_modified:
  - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md
  - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-OPERATOR-CHECKPOINT.md
  - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh
autonomous: true
requirements: [CRON-01]

must_haves:
  truths:
    - "Los gaps están priorizados P0/P1/P2 con id estable, puntero a archivo:línea y pasos concretos — Phase 119 puede tomarlos como backlog sin re-investigar"
    - "Las A1-A5 y las 4 Open Questions del RESEARCH quedan cerradas con su probe, o declaradas parcialmente resueltas con la razón"
    - "Los límites de la auditoría están declarados, no disfrazados de cobertura"
    - "El documento se auto-verifica: un script re-ejecutable comprueba conteos, punteros y ausencia de secretos"
  artifacts:
    - path: ".planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md"
      provides: "§4 gap-list priorizada, §5 assumptions resueltas, §6 Límites, §7 Verificación de cierre"
      contains: "Fix propuesto (Phase 119)"
    - path: ".planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-OPERATOR-CHECKPOINT.md"
      provides: "Petición zero-credential-value de los secrets faltantes, si los hay"
    - path: ".planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh"
      provides: "Gate re-ejecutable de completitud del artefacto"
  key_links:
    - from: "secciones por unidad (§2)"
      to: "gap-list (§4)"
      via: "ids G1..Gn estables referenciados en ambos sentidos"
      pattern: "G[0-9]+"
    - from: "check-crons.sh"
      to: "118-CRON-VERDICTS.md"
      via: "greps de conteo y de anti-secreto"
      pattern: "118-CRON-VERDICTS.md"
---

<objective>
Cerrar la auditoría: gap-list priorizada P0/P1/P2 consumible por Phase 119, resolución de las
assumptions y open questions del RESEARCH, límites declarados, checkpoint de operador para los
secrets faltantes (una sola vez, sin bloquear el cierre), y el gate auto-verificable del documento.

Purpose: SC4 del ROADMAP (gaps priorizados como entrada ejecutable) y la regla de cierre del
CONTEXT (el documento se auto-verifica por grep, conteos == unidades, 0 secretos).
Output: `118-CRON-VERDICTS.md` §4-§7 + `118-OPERATOR-CHECKPOINT.md` + `check-crons.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CONTEXT.md
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-RESEARCH.md
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PATTERNS.md
</context>

<régimen>
READ-ONLY sobre el producto. Se crea un script nuevo bajo el dir de fase (gate del propio
documento), lo cual NO es código de producto. Prohibido tocar `app/`, `packages/`,
`.github/workflows/`, `supabase/migrations/`, `.env`. Todo fix va a Phase 119.
</régimen>

<tasks>

<task type="auto">
  <name>Task 1: Gap-list priorizada P0/P1/P2 + resolución de assumptions + §Límites</name>
  <read_first>
    - .planning/milestones/v6.0-phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md (líneas 430-454: gap-list consolidada con `Archivo:Línea` y fix propuesto; 234-237 y 530-534: resolución de assumptions en dos niveles; 512-524: cómo se cierra la pregunta de billing)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md (líneas 841-856: índice compacto de hallazgos cerrado con conteo; 953-991: §Límites — el tono de "un límite declarado es un resultado válido")
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md (§2: los `G<n>` ya referenciados por unidad deben cuadrar con esta lista)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-RESEARCH.md (§Assumptions Log A1-A5, §Open Questions 1-4)
  </read_first>
  <action>
    Añadir a `118-CRON-VERDICTS.md`:

    `## 4. Gaps priorizados (backlog de Phase 119)`. Primero un índice compacto (forma 116:841-856):
    `| Gn | P0/P1/P2 | unidad de cron | pata que lo detectó | archivo principal |`, cerrado con el
    conteo por prioridad en negrita (p.ej. "**N gaps: X P0, Y P1, Z P2.**"). Luego la tabla de
    detalle ORDENADA POR PRIORIDAD (P0 → P1 → P2), no por id:
    `| # | Gap | Prioridad | Unidad | Archivo:Línea o dato | Fix propuesto (Phase 119) |`.
    Escala LOCKED del CONTEXT: **P0** = roto accionable, **P1** = stale accionable, **P2** = deuda
    de operador. La última columna lleva PASOS CONCRETOS, no un verbo genérico — 119 debe poder
    ejecutarla sin re-investigar. Ids `G1..Gn` estables y referenciados desde las secciones §2
    (verificar la correspondencia en ambos sentidos: todo `G<n>` citado en §2 existe aquí y
    viceversa). Usar el idiom de gap SISTÉMICO de 56 (G23: una fila transversal con varios
    `archivo:línea`) para los dos hallazgos de instrumentación ya conocidos:
    `catalog.ts` apunta a `chilecompra-weekly.yml` y `servel-weekly.yml` inexistentes (P2 de
    instrumentación — distinto de "MONEY gated", que es estado esperado y NO es gap), y las cuatro
    unidades sin señal freshness (`actualidad-refresh`, `digest-daily`, `backup-parlamentario`,
    pg_cron).
    Sección corta `### 4.1 Estados esperados — NO son gaps` enumerando con su cita:
    MONEY/SERVEL sin scheduler (`0023_dinero.sql:46`, `0025_agregacion.sql:46`),
    `lobby-camara-weekly` sin schedule (WAF, YAML:14-17), `digest-daily`/`roster-weekly` gated.

    `## 5. Assumptions y preguntas abiertas — resolución`: tabla
    `| ID | Claim original | Resultado | Evidencia (probe / archivo:línea) |` cubriendo A1-A5 y
    OQ1-OQ4 del RESEARCH. Cuando el dato no alcanza, usar el idiom del precedente:
    "**parcialmente resuelto**, causa exacta irresolvable sin X" — jamás sobre-afirmar.

    `## 6. Límites`: numerados, con el tono de 116:953-991 ("un límite declarado es un resultado
    válido; uno silenciado, no"). Incluir al menos: (a) checkpoint de operador sin responder
    (deuda 110-02) si aplica, (b) unidades sin pata 3 por hueco de catálogo, (c) logs de
    `gh run view` no recuperables (retención GH), (d) R2 verificado sólo por proxy
    `source_snapshot`, sin SigV4 directo, (e) caducidad ~14 días de los veredictos,
    (f) que no se encendió ningún flag ni se corrió ninguna ingesta para cerrar un límite:
    hacerlo habría violado el régimen de la fase.
  </action>
  <acceptance_criteria>
    - Existen `## 4.`, `### 4.1`, `## 5.` y `## 6.` en 118-CRON-VERDICTS.md
    - `grep -cE "^\| G[0-9]+ \|" 118-CRON-VERDICTS.md` ≥ 1 y cada gap tiene prioridad P0|P1|P2
    - `grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+" 118-CRON-VERDICTS.md` ≥ 10 (SC3: punteros reales)
    - Correspondencia bidireccional de ids: todo `G<n>` citado en §2 aparece en la tabla de §4
      (comprobable listando ids con `grep -oE 'G[0-9]+' | sort -u` en ambas regiones)
    - El conteo por prioridad está declarado en texto en negrita y coincide con las filas de la tabla
    - A1..A5 y OQ1..OQ4 aparecen todas en la tabla de §5 (9 filas mínimo)
    - `## 6. Límites` tiene ≥ 5 ítems numerados
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia && grep -q '^## 4\.' 118-CRON-VERDICTS.md && grep -q '^### 4.1' 118-CRON-VERDICTS.md && grep -q '^## 5\.' 118-CRON-VERDICTS.md && grep -q '^## 6\. Límites' 118-CRON-VERDICTS.md && [ "$(grep -cE 'G[0-9]+.*\.(ts|yml|sql):[0-9]+' 118-CRON-VERDICTS.md)" -ge 10 ] && echo OK</automated>
  </verify>
  <done>Gap-list con ids estables, prioridades P0/P1/P2, punteros y pasos concretos; assumptions y open questions cerradas o declaradas parciales; límites numerados y explícitos.</done>
</task>

<task type="auto">
  <name>Task 2: Checkpoint de operador (secrets faltantes) — zero-credential-value, no bloqueante</name>
  <read_first>
    - .planning/milestones/v11.0-phases/110-*/110-02-OPERATOR-CHECKPOINT.md (formato precedente de la petición de secrets al operador)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PROBES-RAW.md (P4: `gh secret list` — nombres presentes vs requeridos por el lado `secrets.*` de cada YAML)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CONTEXT.md (§"Manejo de secrets y checkpoint": pedir UNA vez, seguir con lo no bloqueado, la fase CIERRA igual)
  </read_first>
  <action>
    Si —y sólo si— P4 muestra secrets requeridos ausentes (esperado: `CLOUDFLARE_API_TOKEN`,
    `CLOUDFLARE_ACCOUNT_ID` de la deuda 110-02, y lo que revele la comparación por workflow),
    crear `118-OPERATOR-CHECKPOINT.md` con, por secret faltante: NOMBRE exacto del secret, el
    workflow que lo requiere con `archivo:línea`, dónde cargarlo (GitHub → repo Cuchecorp/gov-map →
    Settings → Secrets and variables → Actions), y el PERMISO MÍNIMO del token. Cero valores, cero
    plantillas con placeholders que inviten a pegar el secreto en el repo, cero instrucciones que
    hagan pasar el valor por el agente. Encabezar con "El agente no carga esta credencial: es un
    acto de operador."
    Si no falta ninguno, crear igualmente el archivo con una sola línea de resultado negativo
    fechada ("Sin secrets requeridos ausentes al <fecha>; evidencia P4") — un resultado negativo
    declarado también es evidencia.

    Registrar cada faltante como gap **P2 (deuda operador)** en §4 de `118-CRON-VERDICTS.md`,
    enlazando a este archivo. La fase NO se bloquea esperando respuesta: se pide una vez y se
    cierra igual (CONTEXT LOCKED).
  </action>
  <acceptance_criteria>
    - `118-OPERATOR-CHECKPOINT.md` existe y tiene fecha
    - `grep -icE "(sk|ghp|gho|eyJ)[-_a-z0-9]{12,}|postgres(ql)?://" 118-OPERATOR-CHECKPOINT.md` → 0
    - Cada secret faltante listado aparece también como gap P2 en §4 de 118-CRON-VERDICTS.md
      (o el archivo declara explícitamente "sin faltantes")
    - Ningún paso del checkpoint pide al operador entregar el valor al agente ni pegarlo en el repo
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia && test -f 118-OPERATOR-CHECKPOINT.md && [ "$(grep -icE '(sk|ghp|gho|eyJ)[-_a-z0-9]{12,}|postgres(ql)?://' 118-OPERATOR-CHECKPOINT.md)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>Petición al operador escrita una sola vez y sin exponer credenciales, o resultado negativo declarado con fecha; faltantes reflejados como P2 en la gap-list.</done>
</task>

<task type="auto">
  <name>Task 3: §7 Verificación de cierre + check-crons.sh (gate re-ejecutable)</name>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/check-fechas.sh (líneas 1-40: cabecera, contrato STRICT=0/1, portabilidad POSIX grep, y la regla de oro del denominador derivado + ancla que se REPORTA, jamás se ajusta)
    - .planning/milestones/v6.0-phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md (líneas 563-581: los greps de auto-verificación y la línea de firma final)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md (completo: el script valida ESTE archivo)
  </read_first>
  <action>
    Crear `check-crons.sh` en el dir de fase, clonando el contrato de `check-fechas.sh`:
    shebang `#!/usr/bin/env bash`, `set -u`, `STRICT=${STRICT:-0}` (0 = reporta sin fallar,
    1 = cualquier falta ⇒ exit 1, modo de cierre de fase), `SCRIPT_DIR`/`REPO_ROOT` derivados,
    helper `falta()`, y POSIX grep sin PCRE. Read-only: no toca DB, no toca red, corre en segundos.
    Denominador DERIVADO, nunca hardcodeado: `ls .github/workflows/*.yml | wc -l` con `ESPERADO=13`
    como ANCLA de sanidad que se REPORTA con el diff si difiere — jamás se ajusta para que pase.

    Comprobaciones:
    - C1 (SC1): `grep -c "Veredicto: "` == (nº de YAML locales) + 2 platform-managed + N de
      `cron.job`; los tres sumandos se leen de las líneas de conteo declaradas en §1 del propio
      documento, y el script reporta el diff si no cuadran.
    - C2 (SC2): nº de bloques `#### Evidencia observada` ≥ nº de secciones `### W-`/`### PG-`, y
      el documento cita al menos un comando `gh ` y un comando `psql ` por región de unidad.
    - C3 (SC3): `grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+"` ≥ 10.
    - C4 (SC4): existe `## 4.`, cada gap tiene P0|P1|P2 y una columna de fix no vacía.
    - C5: `grep -c "#### Cómo re-verificar"` ≥ nº de unidades.
    - C6 (anti-secreto, OBLIGATORIO): `grep -icE "(sk|ghp|gho|eyJ)[-_a-z0-9]{12,}"` → 0 y
      `grep -icE "postgres(ql)?://"` → 0, sobre los tres artefactos (`118-CRON-VERDICTS.md`,
      `118-PROBES-RAW.md`, `118-OPERATOR-CHECKPOINT.md`). Higiene del propio gate: los patrones que
      el script busca aparecen en su código, así que las comprobaciones se hacen SOBRE LOS
      ARTEFACTOS, nunca sobre `check-crons.sh` mismo, y todo grep de conteo sobre el documento
      filtra los comentarios/bloques de código del propio §7 (`grep -v '^#'` donde aplique) para no
      auto-invalidarse.
    - C7: correspondencia bidireccional de ids `G<n>` entre §2 y §4.

    Añadir a `118-CRON-VERDICTS.md` la sección `## 7. Verificación de cierre` con los greps
    embebidos (para que el gate no dependa del script) + `STRICT=1 bash check-crons.sh` como
    comando de cierre, y cerrar con la línea de firma del precedente 56:581: resumen de probes
    usados + "Ningún valor de secret fue impreso en este documento."
  </action>
  <acceptance_criteria>
    - `check-crons.sh` existe, es POSIX-grep only (`grep -P` ausente: `grep -c 'grep -P' check-crons.sh` == 0)
    - `bash check-crons.sh` corre y sale 0 en modo reporte (STRICT=0)
    - `STRICT=1 bash check-crons.sh` sale 0 con el documento terminado
    - `ESPERADO` está presente como ancla y el script imprime el diff si el denominador derivado difiere
    - `## 7. Verificación de cierre` existe en 118-CRON-VERDICTS.md con los greps embebidos y la línea de firma
    - `pnpm --filter ./app test -- --run` sigue verde y sin delta de conteo (fase read-only: la suite no debe moverse respecto de 1560 al cierre de 117)
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia && bash check-crons.sh && STRICT=1 bash check-crons.sh && grep -q '^## 7\. Verificación de cierre' 118-CRON-VERDICTS.md && [ "$(grep -c 'grep -P' check-crons.sh)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>El documento se auto-verifica: conteos == unidades, punteros presentes, gaps priorizados, 0 secretos; `STRICT=1 bash check-crons.sh` verde; firma de cierre puesta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → repo | la petición de secrets podría inducir a pegar valores en git |
| gate script → artefactos | el propio gate contiene los patrones de secreto que busca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-08 | Information Disclosure | `118-OPERATOR-CHECKPOINT.md` | mitigate | sólo NOMBRE del secret + ubicación en el dashboard + permiso mínimo; prohibido pedir el valor al agente o plantillas que inviten a pegarlo en el repo |
| T-118-09 | Information Disclosure | los tres artefactos | mitigate | C6 de `check-crons.sh`: grep anti-secreto y anti-URL de DB sobre los artefactos, en modo STRICT al cierre de fase |
| T-118-10 | Tampering | `check-crons.sh` | mitigate | el gate no se ajusta para pasar: `ESPERADO` es ancla que se REPORTA con diff (regla de oro heredada de `check-fechas.sh`); los greps se aplican a los artefactos, no al propio script |
| T-118-11 | Tampering | código de producto | accept | la fase es read-only y lo prueba el delta de suite (1560) + `git diff --name-only` acotado a `.planning/` |
</threat_model>

<verification>
```bash
cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia
STRICT=1 bash check-crons.sh                       # exit 0
grep -icE "(sk|ghp|gho|eyJ)[-_a-z0-9]{12,}|postgres(ql)?://" 118-CRON-VERDICTS.md 118-PROBES-RAW.md 118-OPERATOR-CHECKPOINT.md   # 0
git diff --name-only | grep -v '^\.planning/'      # vacío
pnpm --filter ./app test -- --run                  # verde, sin delta de conteo
```
</verification>

<success_criteria>
- Gap-list P0/P1/P2 con ids estables, punteros y pasos concretos — Phase 119 la consume tal cual.
- Estados esperados separados de los gaps, con su cita.
- A1-A5 y OQ1-OQ4 resueltas o declaradas parciales con razón.
- Límites numerados y explícitos, incluida la caducidad de ~14 días.
- Checkpoint de operador emitido una vez sin exponer credenciales; la fase cierra igual.
- `STRICT=1 bash check-crons.sh` verde: conteos == unidades, 0 secretos.
</success_criteria>

<output>
Create `.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-03-SUMMARY.md` when done
</output>
