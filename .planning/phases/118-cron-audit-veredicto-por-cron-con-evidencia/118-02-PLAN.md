---
phase: 118-cron-audit-veredicto-por-cron-con-evidencia
plan: 02
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md
autonomous: true
requirements: [CRON-01]

must_haves:
  truths:
    - "Cada unidad de cron tiene su propia sección con evidencia observada, compliance de dos etapas y bloque 'Cómo re-verificar'"
    - "Cada veredicto no-verde apunta a archivo:línea o a un dato con timestamp — nunca a una intuición"
    - "Donde una pata de evidencia no aplica, se declara por qué en vez de omitirse"
  artifacts:
    - path: ".planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md"
      provides: "§2 Secciones por unidad de cron + §3 tablas de estado observado"
      contains: "#### Cómo re-verificar"
  key_links:
    - from: "sección por unidad"
      to: "118-PROBES-RAW.md"
      via: "cita del id de probe y del comando exacto"
      pattern: "Cómo re-verificar"
---

<objective>
Escribir el cuerpo de `118-CRON-VERDICTS.md`: una sección por unidad de cron (GH workflows +
jobs pg_cron + platform-managed) con la anatomía de 56, más las tablas de estado observado
(observabilidad, frescura baseline, `cron.job` × `cron.job_run_details`).

Purpose: SC2 y SC3 del ROADMAP — el veredicto de la tabla maestra sólo es defendible si cada
unidad expone su evidencia, su cadena de ingesta y cómo re-verificarla.
Output: `118-CRON-VERDICTS.md` §2 y §3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CONTEXT.md
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-RESEARCH.md
@.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PATTERNS.md
@./CLAUDE.md
</context>

<régimen>
READ-ONLY. Este plan sólo escribe dentro de `118-CRON-VERDICTS.md`. Prohibido correr probes
nuevas que escriban, invocar CLIs de ingesta, o editar código/workflows/migraciones. Si una
afirmación necesita un dato que no está en `118-PROBES-RAW.md`, se permite una probe `select`
adicional read-only — y se apenda a la bitácora, no se inventa.
</régimen>

<tasks>

<task type="auto">
  <name>Task 1: Sección por unidad para los 13 workflows locales + 2 platform-managed</name>
  <read_first>
    - .planning/milestones/v6.0-phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md (líneas 34-76: la plantilla W-1 completa — cadena de ingesta, DOS ETAPAS compliance, gaps, cómo re-verificar; y 276/366/401: el idiom "decisión declarada, no gap")
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PROBES-RAW.md (TODA la evidencia sale de aquí)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md (§1 tabla maestra escrita en el plan 01: las secciones deben coincidir fila a fila)
    - Por cada workflow auditado: su YAML en .github/workflows/ y el archivo `.ts` del entrypoint que el YAML invoca según el mapa maestro del RESEARCH (p.ej. `packages/tramitacion/src/run-tramitacion-prod-cli.ts`, `packages/agenda/src/run-agenda-prod-cli.ts`, `packages/probidad/src/run-probidad-todos-cli.ts`, `packages/lobby/src/ingest-cli.ts`, `packages/actualidad/src/run-actualidad-prod-cli.ts`, `packages/identity/src/seed-cli.ts`) — leer el entrypoint REAL, jamás el CLI hermano
  </read_first>
  <action>
    Añadir `## 2. Unidades de cron` con una subsección `### W-<n>: <workflow>` por cada uno de los
    13 YAML locales y `### PM-<n>: <Dependabot Updates | CodeQL>` por los 2 platform-managed.
    Cada subsección con estos bloques (anatomía 56:34-76 adaptada):

    - `**YAML:**` ruta · `**Schedule:**` real con `archivo:línea` (o "sin schedule — causa
      declarada en YAML:NN" citando el comentario) · `**Entrypoint invocado:**` `archivo:línea`
      del YAML que lo invoca + ruta del `.ts` (Pitfall 1 / gotcha 57-05: si el paquete tiene CLIs
      duales, nombrar el hermano NO ejecutado para dejar constancia de que se distinguió).
    - `Veredicto: verde | stale | roto | no-cron`.
    - `**Causa raíz del veredicto:**` una frase con puntero a `archivo:línea` o a dato con
      timestamp. Obligatorio para todo veredicto ≠ verde.
    - `#### Evidencia observada` — bloque NUEVO respecto de 56, exigido por la regla de validación
      de 118: por cada pata, el comando exacto y su salida recortada (o cita del id de probe en
      `118-PROBES-RAW.md`). Donde la pata NO aplica —freshness no cubre `actualidad-refresh`,
      `digest-daily`, `backup-parlamentario` ni pg_cron— DECLARARLO ("pata 3 no aplica: la fuente
      no está en `packages/freshness/src/catalog.ts`"), nunca omitirlo.
    - `#### Cadena de ingesta` — tabla `| Etapa | Implementada | Estado | Archivo:Línea |` con
      Etapa-1 fuente→R2, Etapa-2 desde R2, hash-check pre-descarga.
    - `#### DOS ETAPAS compliance` — viñetas Etapa-1 / Etapa-2 / hash-check / rate-limit 2-3s /
      UA identificatorio / robots.txt, cada una cumple|parcial|no-cumple con `archivo:línea`
      (regla LOCKED de CLAUDE.md; 56 la tenía por workflow y se conserva).
    - `#### Gaps de esta unidad` — referencias `G<n>` a la gap-list (que se consolida en el plan 03).
    - `#### Cómo re-verificar` — bloque bash con los comandos exactos; las líneas psql comentadas
      con `#` (idiom 56) para que nadie las ejecute a ciegas.

    Distinguir decisión declarada de gap (Pitfall 3), citando el texto: `lobby-camara-weekly`
    (WAF camara.cl, YAML:14-17, fallback `docs/runbooks/cron-local-fallback.md`), `digest-daily` y
    `roster-weekly` (schedule comentado = estreno gated por diseño), `ci.yml` /
    `deploy-cloudflare.yml` / `backfill.yml` / `fichas-backfill.yml` (no-cron por naturaleza:
    CI / deploy manual / backfill manual — y CLAUDE.md además exige backfill masivo LOCAL).
    Las platform-managed: veredicto `no-cron` / clasificación "platform-managed, no-ingesta",
    causa = "no versionado en `.github/workflows/`", sin patas 2-3.
  </action>
  <acceptance_criteria>
    - `grep -c '^### W-' 118-CRON-VERDICTS.md` == nº de YAML locales (`ls .github/workflows/*.yml | wc -l`, ancla 13; si difiere se REPORTA el diff, no se ajusta)
    - `grep -c '^### PM-' 118-CRON-VERDICTS.md` == 2
    - `grep -c '#### Cómo re-verificar' 118-CRON-VERDICTS.md` ≥ 15
    - `grep -c '#### Evidencia observada' 118-CRON-VERDICTS.md` ≥ 15
    - `grep -c '#### DOS ETAPAS compliance' 118-CRON-VERDICTS.md` ≥ 9 (las unidades de ingesta)
    - `grep -c '\*\*Entrypoint invocado:\*\*'` ≥ 13 y cada uno con patrón `\.(ts|yml):[0-9]+`
    - Toda línea `Veredicto:` distinta de `verde` va seguida (dentro de su sección) de una línea
      `**Causa raíz del veredicto:**` que contiene `.(ts|yml|sql):[0-9]+` o una fecha ISO
    - `grep -icE "(sk|ghp|gho|eyJ|sb_secret_|sb_publishable_)[-_a-z0-9]{12,}|[0-9a-f]{40}|postgres(ql)?://" 118-CRON-VERDICTS.md` → 0
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia && N=$(ls ../../../.github/workflows/*.yml | wc -l) && [ "$(grep -c '^### W-' 118-CRON-VERDICTS.md)" -eq "$N" ] && [ "$(grep -c '^### PM-' 118-CRON-VERDICTS.md)" -eq 2 ] && [ "$(grep -c '#### Cómo re-verificar' 118-CRON-VERDICTS.md)" -ge 15 ] && [ "$(grep -c '#### Evidencia observada' 118-CRON-VERDICTS.md)" -ge 15 ] && [ "$(grep -icE '(sk|ghp|gho|eyJ|sb_secret_|sb_publishable_)[-_a-z0-9]{12,}|[0-9a-f]{40}|postgres(ql)?://' 118-CRON-VERDICTS.md)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>15 secciones (13 W + 2 PM), cada una con entrypoint citado, veredicto, causa apuntada, evidencia por pata (o su no-aplicabilidad declarada), compliance de dos etapas y bloque de re-verificación.</done>
</task>

<task type="auto">
  <name>Task 2: Secciones de los jobs pg_cron vivos + tablas de estado observado</name>
  <read_first>
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PROBES-RAW.md (P6 `cron.job` / `cron.job_run_details`, P7 últimas filas, P8 cursores, P10 `source_snapshot`)
    - .planning/milestones/v6.0-phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md (líneas 477-508: `## Observabilidad — Estado de tablas` y `## Frescura baseline (FECHA)` con el probe SQL literal por fila)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-PATTERNS.md (§"No Analog Found": cómo se construye la fila de un job pg_cron, que no tiene precedente)
    - supabase/migrations/0003_orchestration.sql (líneas ~210-235), 0030_net.sql:162, 0039_cruce_senal.sql:138, 0065_actualidad_senal.sql:326 — la EXPECTATIVA contra la que se contrasta lo vivo
  </read_first>
  <action>
    Añadir a `## 2. Unidades de cron` una subsección `### PG-<n>: <jobname>` por cada fila viva de
    `cron.job` (N según P6, expectativa 5). Anatomía adaptada: `**Job:**` jobname + jobid ·
    `**Schedule real (cron.job):**` valor leído de la DB, NO de la migración · `**Declarado en:**`
    `archivo:línea` de la migración · `**Comando:**` el `command` de `cron.job` ·
    `**active:**` · `Veredicto:` · `**Causa raíz:**`. En vez de pata 1 usar
    `cron.job_run_details` (status + return_message + start_time del último run); en vez de pata 2,
    la tabla/vista que el comando materializa (`grafo` aristas, `cruces`, `actualidad_senal`).
    `#### Cómo re-verificar` con las dos queries comentadas con `#`.
    Declarar explícitamente el DELTA migración↔vivo (Open Question 1): jobs esperados que no están,
    jobs vivos no declarados, y cuál de las dos ramas de `0003_orchestration.sql:214/221` quedó
    activa según el `schedule` real. Un job vivo no declarado en migraciones es hallazgo P1 como
    mínimo; su ausencia también.

    Luego añadir `## 3. Estado observado (ancla <fecha del probe>)` con tres tablas, con la fecha
    del probe EN EL TÍTULO de cada una (idiom 56:489):
    - `### 3.1 Observabilidad — Estado de tablas`: `| Tabla | Definida en | Escrita por (unidad de cron) | Estado actual | Última entrada (probe P<n>) |`.
    - `### 3.2 Frescura baseline`: `| Fuente / Tabla | Umbral (catalog.ts) | Última fecha observada | Veredicto freshness | Probe SQL |` — el umbral se CITA de `packages/freshness/src/catalog.ts`, no se redefine (Don't Hand-Roll). Debajo, viñetas de **Observaciones de frescura** interpretando la tabla, incluida la discrepancia entre lo que reporta `pnpm freshness` y la última fila real donde la haya (el CONTEXT declara esa discrepancia hallazgo en sí mismo).
    - `### 3.3 pg_cron vivo × job_run_details`: `| jobid | jobname | schedule (vivo) | active | último run | status | return_message (recortado) | esperado en migración |`.
    - `### 3.4 Cursores` : `| tabla de estado | columna de avance | valor observado | interpretación (sin novedades honesto vs cursor detenido) |` — es la evidencia que sostiene o refuta cada veredicto `stale` (Pitfall 4).
  </action>
  <acceptance_criteria>
    - `grep -c '^### PG-' 118-CRON-VERDICTS.md` == nº de filas de `cron.job` declarado en §1 y en P6
    - El total `grep -c "Veredicto: "` sigue cuadrando con la suma de las tres poblaciones declarada en §1
    - Existen `### 3.1`, `### 3.2`, `### 3.3` y `### 3.4`, y cada título de §3.x lleva la fecha ancla
    - §3.2 cita el umbral de cada fuente con referencia a `packages/freshness/src/catalog.ts` (no redefine umbrales)
    - El delta migración↔vivo de pg_cron está declarado en texto explícito (jobs esperados vs vivos, con nombres)
    - `grep -icE "(sk|ghp|gho|eyJ|sb_secret_|sb_publishable_)[-_a-z0-9]{12,}|[0-9a-f]{40}|postgres(ql)?://" 118-CRON-VERDICTS.md` → 0
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia && grep -q '^### PG-' 118-CRON-VERDICTS.md && grep -q '^### 3.1' 118-CRON-VERDICTS.md && grep -q '^### 3.3' 118-CRON-VERDICTS.md && grep -q '^### 3.4' 118-CRON-VERDICTS.md && grep -q 'catalog.ts' 118-CRON-VERDICTS.md && [ "$(grep -icE '(sk|ghp|gho|eyJ|sb_secret_|sb_publishable_)[-_a-z0-9]{12,}|[0-9a-f]{40}|postgres(ql)?://' 118-CRON-VERDICTS.md)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>Cada job pg_cron vivo tiene sección con veredicto y delta contra migraciones; §3 expone observabilidad, frescura baseline, pg_cron vivo y cursores con ancla temporal en el título.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bitácora de probes → documento público del repo | la evidencia se re-pega al redactar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Information Disclosure | `118-CRON-VERDICTS.md` §2/§3 | mitigate | `return_message` de `cron.job_run_details` y logs de runs se pegan RECORTADOS; gate anti-secreto y anti-URL en el verify de cada task |
| T-118-06 | Tampering | DB PROD | mitigate | sólo `select` si hace falta una probe complementaria; se apenda a `118-PROBES-RAW.md`, nunca se inventa el dato |
| T-118-07 | Repudiation | veredictos sin puntero | mitigate | acceptance criteria exige `archivo:línea` o timestamp en toda causa de veredicto ≠ verde (SC3) |
</threat_model>

<verification>
```bash
cd .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia
grep -c '^### W-' 118-CRON-VERDICTS.md; grep -c '^### PM-' 118-CRON-VERDICTS.md; grep -c '^### PG-' 118-CRON-VERDICTS.md
grep -c 'Veredicto: ' 118-CRON-VERDICTS.md        # == suma de los tres conteos de §1
grep -icE "(sk|ghp|gho|eyJ|sb_secret_|sb_publishable_)[-_a-z0-9]{12,}|[0-9a-f]{40}|postgres(ql)?://" 118-CRON-VERDICTS.md   # 0
git diff --name-only | grep -v '^\.planning/'      # vacío
```
</verification>

<success_criteria>
- Una sección por unidad de cron: 13 W + 2 PM + N PG, sin huecos.
- Entrypoint invocado citado con `archivo:línea` en cada workflow de ingesta (gotcha 57-05 cerrado).
- Toda causa de veredicto ≠ verde apunta a archivo o a dato con timestamp.
- Patas no aplicables declaradas, no omitidas.
- §3 con las cuatro tablas de estado observado y ancla temporal en el título.
</success_criteria>

<output>
Create `.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-02-SUMMARY.md` when done
</output>
