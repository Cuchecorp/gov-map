---
phase: 54-uxdemo-pulido-presentacional
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - app/components/cruces-de-parlamentario.tsx
  - app/components/cruces-de-parlamentario.test.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/patrimonio-de-parlamentario.tsx
  - app/components/patrimonio-de-parlamentario.test.tsx
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "La sección Cruces integra 1 frase anti-causal al final de su intro existente (no duplica el caveat)"
    - "La nota de método de rebeldías se muestra bajo el h3 (una sola vez, nunca duplicada), string byte-identical"
    - "La sección Patrimonio agrega 1 frase factual sobre múltiples versiones/presentaciones"
    - "/red NO cambia (OMIT — ya tiene línea anti-causal)"
    - "Ningún caveat anti-causal existente se duplica; empty-states byte-identical"
  artifacts:
    - path: "app/components/cruces-de-parlamentario.tsx"
      provides: "Intro de cruces con la frase anti-causal apendida"
      contains: "no afirma intención ni causa"
    - path: "app/components/patrimonio-de-parlamentario.tsx"
      provides: "Intro de patrimonio con la frase de versiones"
      contains: "presentación distinta"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "h3 de rebeldías"
      via: "nota de método reubicada bajo el heading (no duplicada)"
      pattern: "opción mayoritaria de su bancada"
---

<objective>
Contract 3: microcopy "cómo leer esto" — 1 línea factual al pie del heading en las secciones complejas, con la regla integrate-or-omit (NUNCA duplicar un caveat existente).

Purpose: Ayudar al lector (centro de estudios) a interpretar secciones densas sin introducir vocabulario causal ni duplicar caveats. Tono factual, es-CL.
Output: 1 frase integrada en Cruces, la nota de método de rebeldías reubicada bajo su h3 (byte-identical, no ×2), 1 frase agregada en Patrimonio. /red se deja intacto (OMIT).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-uxdemo-pulido-presentacional/54-UI-SPEC.md
@app/components/cruces-de-parlamentario.tsx
@app/components/votos-por-parlamentario.tsx
@app/components/patrimonio-de-parlamentario.tsx

<interfaces>
<!-- Contract 3 del UI-SPEC — strings EXACTOS. No re-redactar. -->
<!-- Este plan corre DESPUÉS de 54-01 (comparte cruces-de-parlamentario.tsx). El wrap formatNombre en cruces:56 YA existe; NO tocarlo. -->

Patrón: <p class="text-sm text-muted-foreground mt-2"> (o apendido a un párrafo intro existente).
Regla LOCKED: NUNCA duplicar un caveat existente. Cada string pasa banned-vocab (regex por-componente: correlaci|af[ií]n|influy|cercano|rebeld|score|porque).

| Sección | Decisión | Contrato exacto |
| Cruces (cruces-de-parlamentario.tsx, intro ~:89-93) | INTEGRATE | Apendir UNA frase al párrafo intro existente (mismo <p>, después de "…apunta al registro original."): "Un cruce solo agrupa reuniones registradas por sector; no afirma intención ni causa." El empty-state debajo queda byte-identical. |
| Rebeldías / "Votó distinto a su bancada" (votos-por-parlamentario.tsx ~:677-734) | INTEGRATE (relocate) | La nota de método shippeada "Se compara el voto del parlamentario con la opción mayoritaria de su bancada en esa misma votación." se MUEVE de debajo de la lista (~:728-731) a directamente bajo el h3 (antes del conteo), string BYTE-IDENTICAL (los getByText RTL siguen pasando). Sin string nuevo. Rama vacía (~:681-683) intacta — sin línea ahí. NUNCA se renderiza dos veces. |
| Patrimonio (patrimonio-de-parlamentario.tsx, intro ~:514-525) | ADD (no hay línea how-to-read) | Apendir UNA frase al primer párrafo intro (después de "…con su fecha de presentación."): "Un mismo parlamentario puede registrar varias versiones; cada una es una presentación distinta ante la fuente." La línea CC BY y el caveat de montos del chart quedan intactos. |
| Red (app/app/red/page.tsx :83-87 y :157-160) | OMIT | Ambas ramas ya tienen la línea anti-causal. NO cambiar copy de /red. |

Nota: las líneas exactas pueden haber corrido tras 54-01 (que tocó cruces). Verificar por contenido, no por número de línea.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar frase en Cruces + agregar frase en Patrimonio</name>
  <files>app/components/cruces-de-parlamentario.tsx, app/components/patrimonio-de-parlamentario.tsx</files>
  <action>En `cruces-de-parlamentario.tsx`: apendir al MISMO `<p>` del intro existente (tras "…apunta al registro original.") la frase EXACTA "Un cruce solo agrupa reuniones registradas por sector; no afirma intención ni causa." NO tocar el wrap `formatNombre` de :56 (ya aplicado por 54-01) ni el empty-state (byte-identical). En `patrimonio-de-parlamentario.tsx`: apendir al primer párrafo intro (tras "…con su fecha de presentación.") la frase EXACTA "Un mismo parlamentario puede registrar varias versiones; cada una es una presentación distinta ante la fuente." Dejar intactas la línea CC BY y el caveat de montos del chart. Localizar los párrafos por su contenido (las líneas pueden haber corrido).</action>
  <verify>
    <automated>cd app && npx vitest run components/cruces-de-parlamentario.test.tsx components/patrimonio-de-parlamentario.test.tsx</automated>
  </verify>
  <done>Cruces y Patrimonio muestran su frase exacta integrada; empty-states/CC BY/montos intactos; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Reubicar la nota de método de rebeldías bajo el h3 (byte-identical, sin duplicar)</name>
  <files>app/components/votos-por-parlamentario.tsx</files>
  <action>MOVER (no copiar) el `<p>` con la nota de método "Se compara el voto del parlamentario con la opción mayoritaria de su bancada en esa misma votación." desde debajo de la lista (~:728-731) a directamente bajo el `h3` de rebeldías, antes del conteo. El string queda BYTE-IDENTICAL (los `getByText` RTL existentes deben seguir pasando). Eliminar el original para que NUNCA se renderice dos veces. La rama vacía (~:681-683) queda intacta — sin línea ahí. No agregar strings nuevos.</action>
  <verify>
    <automated>cd app && npx vitest run components/votos-por-parlamentario.test.tsx</automated>
  </verify>
  <done>La nota de método aparece una sola vez, bajo el h3, byte-identical; rama vacía intacta; tests verdes (getByText pasa, sin doble render).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Strings estáticos en componentes server existentes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Tampering (insinuación causal) | Microcopy | mitigate | Cada string pasa banned-vocab negative-match; regla integrate-or-omit evita duplicar caveats; strings prescritos verbatim |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (hard bound LOCKED) |
</threat_model>

<verification>
- Tests de los 3 componentes verdes.
- Grep: la nota de rebeldías aparece exactamente 1 vez en el DOM renderizado (no ×2).
- /red no fue tocado (OMIT) — confirmar que no hay diff en app/app/red/page.tsx por este plan.
</verification>

<success_criteria>
Microcopy integrada/reubicada/agregada según Contract 3, sin duplicar caveats, sin vocabulario causal; empty-states byte-identical; suite de los 3 componentes verde.
</success_criteria>

<output>
Create `.planning/phases/54-uxdemo-pulido-presentacional/54-02-SUMMARY.md` when done
</output>
