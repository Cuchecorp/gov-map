---
phase: 54-uxdemo-pulido-presentacional
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/page.tsx
  - app/app/page.test.tsx
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "El home muestra 3 tarjetas de entrada (Proyectos de ley, Parlamentarios 360, Agenda de la semana) entre el hero y ActualidadModule"
    - "Hero + las 3 tarjetas caben sin scroll en desktop 1280×800"
    - "Las tarjetas son server-rendered, cero JS; funcionan sin JavaScript"
    - "El contenido del hero (h1, subtítulo, SearchBox, chips, trust line) queda byte-identical (LOCKED F19)"
    - "En móvil las tarjetas apilan a 1 columna, full-width, target ≥44px"
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Bloque <nav aria-label='Secciones del sitio'> con 3 <Link> full-card entre hero y ActualidadModule"
      contains: "Secciones del sitio"
  key_links:
    - from: "app/app/page.tsx"
      to: "/buscar, /parlamentarios, /agenda"
      via: "next/link href estático"
      pattern: "href=\"/(buscar|parlamentarios|agenda)\""
---

<objective>
Contract 2: insertar 3 tarjetas de entrada server-rendered en el home, entre el hero (LOCKED, byte-identical) y `<ActualidadModule />`, visibles sin scroll a 1280×800.

Purpose: El home debe comunicar qué es el sitio y ofrecer ≥3 rutas de entrada guiadas — es la primera impresión para el centro de estudios. Cero JS, dentro del design system.
Output: Bloque `<nav>` con 3 `<Link>` full-card + ajuste del único lever permitido (bottom padding del `<section>` hero) para respetar el fold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-uxdemo-pulido-presentacional/54-UI-SPEC.md
@app/app/page.tsx

<interfaces>
<!-- Contract 2 del UI-SPEC — prescriptivo. NO improvisar clases arbitrarias. -->

Placement: bloque server-rendered ENTRE el <section> del hero y <ActualidadModule />. Cero client JS.
Semántica: <nav aria-label="Secciones del sitio"> con tres <Link> full-card. SIN elemento heading dentro del bloque (ni h2/h3) — se ubica entre el h1 del hero y los h2 de actualidad sin insertar nivel de heading. Los títulos de tarjeta son <span>.

Layout (clases exactas, tokens — NADA de arbitrary [Npx]):
- Container: mx-auto max-w-5xl px-4 md:px-8, con pb-12 md:pb-16 debajo.
- Grid: grid gap-4 sm:grid-cols-3 (apila a 1 columna <640px, 3 desde sm).
- Card: rounded-lg border border-border bg-card p-4 (Panel compacto) + transition-colors hover:border-accent-product/50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring. La tarjeta ENTERA es el link (block), ≥44px trivialmente.
- Contenido: título <span class="text-base font-semibold leading-snug"> con glyph → apendido como <span aria-hidden="true" class="pl-1">→</span> (padding, NUNCA text node de whitespace — lección F53) + línea de valor <p class="mt-1 text-sm text-muted-foreground">.

Copy exacto (titles LOCKED por CONTEXT; value lines prescritas):
| Título (LOCKED) | Href | Línea de valor |
| Proyectos de ley | /buscar | En qué etapa está cada proyecto y cómo se ha votado, con cada fuente enlazada. |
| Parlamentarios 360 | /parlamentarios | Votaciones, lobby y patrimonio de cada parlamentario, según los registros públicos. |
| Agenda de la semana | /agenda | Citaciones de comisiones y tabla de sala, enlazadas a cada proyecto. |

Fold contract (1280×800): hero + 3 tarjetas sin scroll. Hero CONTENT byte-identical (LOCKED F19). Único lever = bottom padding del <section> hero: cambiar py-16 md:py-24 -> pt-16 pb-8 md:pt-24 md:pb-10. Si el fold aún falla, piso pb-8 md:pb-8. NUNCA tocar top padding ni contenido del hero.

Accent (petróleo) NO se usa para: títulos de tarjeta, líneas de valor, el glyph → (hereda color del título). Solo hover border/50 + focus ring.
Banned-vocab: cada string pasa el negative-match (sin "limpio/transparente", sin causal/afinidad/score, sin promesas ni stats fabricadas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar el bloque de 3 tarjetas + ajustar el seam del hero</name>
  <files>app/app/page.tsx</files>
  <action>Insertar un bloque `<nav aria-label="Secciones del sitio">` server-rendered ENTRE el `<section>` del hero y `<ActualidadModule />`, con las clases de layout exactas del bloque interfaces (container mx-auto max-w-5xl px-4 md:px-8 pb-12 md:pb-16; grid gap-4 sm:grid-cols-3; cada card como `<Link>` full-card con las clases de card y hover/focus prescritas). Cada tarjeta: título en `<span class="text-base font-semibold leading-snug">` con el glyph `→` como `<span aria-hidden="true" class="pl-1">→</span>` (padding, nunca whitespace text node), + línea de valor en `<p class="mt-1 text-sm text-muted-foreground">`. Usar el copy exacto (3 filas de la tabla). SIN heading dentro del bloque. Cambiar el bottom padding del `<section>` hero de `py-16 md:py-24` a `pt-16 pb-8 md:pt-24 md:pb-10` (único lever permitido para el fold); el CONTENIDO del hero queda byte-identical. Cero client JS (no "use client", sin handlers).</action>
  <verify>
    <automated>cd app && npx vitest run app/page.test.tsx</automated>
  </verify>
  <done>3 <Link> con hrefs /buscar, /parlamentarios, /agenda + copy exacto; nav aria-label presente; sin heading en el bloque; hero content byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Tests del bloque de tarjetas (nav, hrefs, copy, banned-vocab, sin heading)</name>
  <files>app/app/page.test.tsx</files>
  <action>Extender `app/app/page.test.tsx` para asertar: (1) existe un `<nav>` con aria-label "Secciones del sitio"; (2) hay exactamente 3 links con los hrefs `/buscar`, `/parlamentarios`, `/agenda` y los títulos LOCKED; (3) las 3 líneas de valor exactas aparecen; (4) el bloque NO introduce un heading (h2/h3) nuevo; (5) banned-vocab negative-match sobre el copy nuevo (regex del patrón existente en el repo: sin "limpio/transparente", sin causal/afinidad/score). Reusar el patrón de banned-vocab ya usado en otros tests del repo.</action>
  <verify>
    <automated>cd app && npx vitest run app/page.test.tsx</automated>
  </verify>
  <done>Tests verdes cubriendo nav/aria, 3 hrefs, copy exacto, ausencia de heading nuevo y banned-vocab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Bloque estático server-rendered; sin input de usuario, sin datos externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | Copy de tarjetas | mitigate | Strings estáticos vetados por banned-vocab negative-match (sin virtud/causal); sin datos dinámicos |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (hard bound LOCKED) |
</threat_model>

<verification>
- `cd app && npx vitest run app/page.test.tsx` verde.
- Revisión: el bloque es server-rendered (sin "use client"); el hero content no cambió (solo el bottom padding del section).
- La verificación visual del fold a 1280×800 se cubre en el Plan 05 (screenshot demo-01-home-1280.jpg post-deploy).
</verification>

<success_criteria>
Home con 3 tarjetas de entrada server-rendered, copy exacto, dentro del design system, hero LOCKED intacto salvo el bottom padding del seam; tests verdes.
</success_criteria>

<output>
Create `.planning/phases/54-uxdemo-pulido-presentacional/54-03-SUMMARY.md` when done
</output>
