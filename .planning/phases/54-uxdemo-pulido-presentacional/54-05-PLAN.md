---
phase: 54-uxdemo-pulido-presentacional
plan: 05
type: execute
wave: 3
depends_on: [54-01, 54-02, 54-03, 54-04]
files_modified:
  - docs/demo/demo-01-home-1280.jpg
  - docs/demo/demo-02-buscar-1280.jpg
  - docs/demo/demo-03-proyecto-1280.jpg
  - docs/demo/demo-04-parlamentario-1280.jpg
  - docs/demo/demo-05-agenda-1280.jpg
  - docs/demo/demo-06-red-1280.jpg
autonomous: false
requirements: [UX-02]

must_haves:
  truths:
    - "El sitio en PROD refleja formatNombre, tarjetas home, microcopy, F-04 y los fixes post-deploy de F53 (WR-01 gate-aware, px-2 móvil, pl-1 flecha)"
    - "docs/demo/ contiene ≥6 screenshots full-width (ancho ≥1260px) del sitio FINAL desplegado, nombrados por superficie"
    - "Las superficies clave (/, /buscar, /proyecto/*, /parlamentario/*, /agenda, /red) responden 200"
    - "El NET gate ON está verificado en vivo en PROD; el estado OFF se verifica por test, nunca flipeando PROD"
    - "El QA navegado no registra errores de consola (los woff2 son warning, no error)"
  artifacts:
    - path: "docs/demo/demo-01-home-1280.jpg"
      provides: "Home con hero + 3 tarjetas sobre el fold (doble acceptance de Contract 2)"
    - path: "docs/demo/demo-04-parlamentario-1280.jpg"
      provides: "Ficha parlamentario con nombres formateados en votos/lobby/patrimonio/cruces"
  key_links:
    - from: "scripts/rewalk-shot.mjs"
      to: "PROD (Cloudflare Worker)"
      via: "harness iframe, desktop 1280, width ≥1260"
      pattern: "docs/demo/demo-0"
---

<objective>
Contract 5f + Contract 6: gate de fase completo, 1 redeploy final (que arrastra también los fixes post-deploy de F53), smoke post-deploy, QA navegado y el set de ≥6 screenshots de demo capturados DESPUÉS del deploy.

Purpose: Dejar el sitio en PROD presentable y producir la evidencia visual que se usará en la presentación al centro de estudios. Cero DDL, cero flags, cero deps.
Output: PROD redeployado + smoke 200s + `docs/demo/` con ≥6 capturas full-width.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-uxdemo-pulido-presentacional/54-UI-SPEC.md
@.planning/phases/54-uxdemo-pulido-presentacional/54-CONTEXT.md
@scripts/rewalk-shot.mjs

<interfaces>
<!-- Contracts 5f y 6 del UI-SPEC. Protocolo de deploy del CONTEXT §code_context. -->

Deploy (5f) — patrón autorizado (2026-07-06):
  docker-cf-build.sh (build OpenNext en Docker LINUX, NUNCA Windows — gotcha worker roto 500ea) -> docker cp del build -> cd app && npx wrangler deploy.
  PRECHECK obligatorio: `npx wrangler whoami` primero. Si OAuth murió -> checkpoint de operador para re-login (auth gate dinámico, no pre-planeado). Docker Desktop puede requerir Start-Process para arrancar.
  El deploy arrastra los commits post-deploy de F53 ya en master (WR-01 gate-aware showRed, nav px-2 móvil, pl-1 flechas) además de todo F54.

Smoke post-deploy:
  - NET gate ON verificado EN VIVO en PROD (/red poblado). El estado OFF se verifica por el test existente lib/net-gate.test.ts — NUNCA flipeando PROD.
  - Superficies clave 200: /, /buscar, /proyecto/*, /parlamentario/*, /agenda, /red (curl o bros-cli).
  - QA navegado: sin errores de consola (los 2 warnings woff2 por ruta son level:warning, no error — no cuentan).

Screenshots (6) — con scripts/rewalk-shot.mjs (harness iframe) DESPUÉS del deploy, contra PROD, desktop 1280:
| # | File | Subject | Debe mostrar |
| 1 | docs/demo/demo-01-home-1280.jpg | / | Hero + 3 tarjetas completas sobre el fold |
| 2 | docs/demo/demo-02-buscar-1280.jpg | /buscar?q=protección de datos personales | Resultados semánticos + botón submit petróleo |
| 3 | docs/demo/demo-03-proyecto-1280.jpg | Ficha proyecto (elegir un boletín cuyo carril lobby×tramitación TENGA filas; si 14309-04 no lo tiene, usar otro) | Carril lobby×tramitación RENDERIZADO |
| 4 | docs/demo/demo-04-parlamentario-1280.jpg | Ficha parlamentario (ej. D1012) | votos + lobby + patrimonio + cruces con nombres formateados |
| 5 | docs/demo/demo-05-agenda-1280.jpg | /agenda | Citaciones + tabla de sala con cross-links a boletín |
| 6 | docs/demo/demo-06-red-1280.jpg | /red?seed=D1012 | Grafo poblado + filtros |

Quality gate (lección F53): las capturas 1280 deben ser FULL-WIDTH (ancho de imagen ≥1260px, sin right-crop del viewport padre de 772px del harness — ensanchar la página/viewport del harness ANTES de capturar). JPEG q≈70 está bien. Shots móviles 390 adicionales son bienvenidos pero NO reemplazan los 6 canónicos desktop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de fase — suite completa + tsc + lockdown-guard</name>
  <files>(sin cambios de archivos — solo verificación)</files>
  <action>Correr el gate de fase ANTES del deploy: `pnpm test` desde el repo root (baseline 565/565; debe quedar ≥565 verde, sin regresión — F54 agregó tests, el conteo sube) + `pnpm typecheck` (`tsc -b` limpio desde root) + confirmar lockdown-guard verde (corre dentro de la suite) y banned-vocab negative-match verde. Si algo falla, detener y reportar antes de tocar PROD.</action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <done>Suite completa verde (≥565), tsc -b limpio, lockdown-guard y banned-vocab verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy final + smoke post-deploy</name>
  <files>(deploy operacional — sin cambios de código)</files>
  <action>PRECHECK: `cd app && npx wrangler whoami`. Si falla (OAuth expirado), crear un checkpoint de operador para re-login y detener (auth gate dinámico). Si Docker Desktop no responde, arrancarlo (Start-Process) antes del build. Ejecutar el patrón autorizado: `docker-cf-build.sh` (build OpenNext en Docker Linux — NUNCA build Windows) -> `docker cp` del build al host -> `cd app && npx wrangler deploy`. El deploy incluye todo master (F54 + los commits post-deploy de F53: WR-01 gate-aware, px-2 móvil, pl-1). Smoke: verificar 200 en /, /buscar, /proyecto/*, /parlamentario/*, /agenda, /red (curl o bros-cli); verificar NET gate ON en vivo (/red poblado). NO flipear PROD para el estado OFF (ese se cubre por lib/net-gate.test.ts).</action>
  <verify>
    <automated>cd app && npx wrangler whoami</automated>
  </verify>
  <done>Deploy exitoso; superficies clave 200; NET gate ON en vivo verificado; sin flip de PROD.</done>
</task>

<task type="auto">
  <name>Task 3: Capturar el set de demo + QA navegado</name>
  <files>docs/demo/demo-01-home-1280.jpg, docs/demo/demo-02-buscar-1280.jpg, docs/demo/demo-03-proyecto-1280.jpg, docs/demo/demo-04-parlamentario-1280.jpg, docs/demo/demo-05-agenda-1280.jpg, docs/demo/demo-06-red-1280.jpg</files>
  <action>Con `scripts/rewalk-shot.mjs` (harness iframe), DESPUÉS del deploy y contra PROD, capturar las 6 superficies canónicas de la tabla del bloque interfaces a desktop 1280, nombradas exactamente como en la tabla. Antes de capturar, ensanchar la página/viewport del harness para que el ancho de imagen sea ≥1260px (lección F53: evitar right-crop del viewport padre de 772px). Para demo-03, elegir un boletín cuyo carril lobby×tramitación tenga filas. JPEG q≈70. Durante la navegación, registrar el QA navegado: confirmar que no hay errores de consola (los warnings woff2 no cuentan). Verificar que los 6 archivos existen.</action>
  <verify>
    <automated>ls docs/demo/demo-01-home-1280.jpg docs/demo/demo-02-buscar-1280.jpg docs/demo/demo-03-proyecto-1280.jpg docs/demo/demo-04-parlamentario-1280.jpg docs/demo/demo-05-agenda-1280.jpg docs/demo/demo-06-red-1280.jpg</automated>
  </verify>
  <done>Los 6 JPEG existen en docs/demo/, full-width (≥1260px), del sitio FINAL desplegado; QA sin errores de consola.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Revisión humana del set de demo + sitio PROD</name>
  <action>Presentar al operador el set de ≥6 screenshots en docs/demo/ y el sitio PROD redeployado para verificación visual final antes de cerrar la fase. No hay automatización adicional — esto valida la evidencia de presentación para el centro de estudios.</action>
  <what-built>Sitio redeployado a PROD con todo F54 (formatNombre, tarjetas home, microcopy, F-04 grafo móvil, botón petróleo, marker ámbar restaurado, skeletons anti-CLS) + set de ≥6 screenshots de demo full-width en docs/demo/.</what-built>
  <how-to-verify>
    1. Abrir docs/demo/demo-01-home-1280.jpg: hero + las 3 tarjetas (Proyectos de ley / Parlamentarios 360 / Agenda de la semana) completas, sin corte a la derecha (ancho ≥1260px).
    2. demo-02-buscar-1280.jpg: resultados semánticos y botón "Buscar" en petróleo (no azul default).
    3. demo-03-proyecto-1280.jpg: el carril lobby×tramitación RENDERIZADO con filas (nombres en texto plano, Title Case).
    4. demo-04-parlamentario-1280.jpg: votos + lobby + patrimonio + cruces, con nombres formateados (no minúscula cruda).
    5. demo-05-agenda-1280.jpg y demo-06-red-1280.jpg: agenda con cross-links y grafo poblado con filtros.
    6. Visitar https://<PROD>/red en un móvil (o DevTools 390px): canvas visible + nota "mejor en pantalla ancha" + filtros usables.
    7. Confirmar que el QA navegado no reportó errores de consola (solo warnings woff2, aceptados).
  </how-to-verify>
  <resume-signal>Escribe "approved" si el set de demo y el sitio PROD están presentables, o describe qué corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → PROD (Cloudflare Worker) | Deploy con credenciales wrangler locales (OAuth), fuera del repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-08 | Information Disclosure | Deploy con secrets en cliente | mitigate | Sin env vars nuevas; patrón autorizado (wrangler OAuth local, creds fuera del repo); build OpenNext en Docker Linux |
| T-54-09 | Elevation of Privilege (flag flip accidental) | NET gate en PROD | mitigate | Estado OFF verificado SOLO por lib/net-gate.test.ts; jamás se flipea PROD; smoke solo lee superficies 200 |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (hard bound LOCKED); deploy repite patrón existente |
</threat_model>

<verification>
- `pnpm test && pnpm typecheck` verde antes del deploy.
- `npx wrangler whoami` OK (o checkpoint de operador).
- Superficies clave 200 post-deploy; NET gate ON en vivo.
- Los 6 JPEG existen en docs/demo/ con ancho ≥1260px.
- Checkpoint humano aprueba el set de demo.
</verification>

<success_criteria>
PROD redeployado con todo F54 + fixes F53; smoke 200 en superficies clave; NET gate ON en vivo (OFF por test); `docs/demo/` con ≥6 capturas full-width del sitio final; QA navegado sin errores de consola; suite verde + tsc limpio.
</success_criteria>

<output>
Create `.planning/phases/54-uxdemo-pulido-presentacional/54-05-SUMMARY.md` when done
</output>
