---
phase: 54-uxdemo-pulido-presentacional
plan: 04
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - app/components/red/red-graph.tsx
  - app/components/red/red-graph.test.tsx
  - app/app/globals.css
  - app/components/search-box.tsx
  - app/components/search-box.test.tsx
  - app/components/identity-marker.tsx
  - app/components/identity-marker.test.tsx
  - app/components/lobby-de-parlamentario.tsx
  - app/app/parlamentario/[id]/page.tsx
  - app/app/contraparte/[id]/page.tsx
  - app/components/header-nav.test.tsx
  - app/components/breadcrumbs.test.tsx
  - app/components/global-header.tsx
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "El grafo /red usa canvas de altura adaptativa (h-96 móvil / md:h-120 = 480px desktop) sin inline style ni arbitrary [Npx]"
    - "En móvil (<768px) los filtros del grafo se compactan y aparece 1 nota honesta 'mejor en pantalla ancha' (visible, no bloqueante)"
    - "El botón submit no-hero de /buscar usa el token petróleo (bg-accent-product); la rama hero queda byte-identical"
    - "El marker de identidad recupera su fondo/texto/borde ámbar (el bg-[--identity-warn-bg] shippeado compilaba a CSS inválido)"
    - "Los skeletons de ficha (parlamentario + contraparte) tienen una fila placeholder de breadcrumb → sin CLS"
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "Canvas h-96 md:h-120 + nota md:hidden + knobs xyflow (fitViewOptions/minZoom)"
      contains: "md:h-120"
    - path: "app/app/globals.css"
      provides: "@theme inline con --color-identity-warn-* + media query compacta net-filtros"
      contains: "--color-identity-warn-bg"
  key_links:
    - from: "app/components/identity-marker.tsx"
      to: "app/app/globals.css (@theme identity-warn)"
      via: "utilities planas bg-identity-warn-bg / text-identity-warn-fg / border-identity-warn-border"
      pattern: "identity-warn"
---

<objective>
Contract 4 + Contract 5: fix acotado del grafo /red en móvil (NO rediseño), botón /buscar al token petróleo, corrección de sintaxis del token identity-warn (que RESTAURA el visual ámbar), fila de breadcrumb en skeletons (anti-CLS), y minors de código IN-01/IN-02/IN-04. F-05 (woff2) se documenta y difiere.

Purpose: Cerrar los P1 heredados de F53 dejando el sitio presentable en móvil y desktop, corrigiendo drift de color y un CLS, sin instalar nada ni tocar el layout de fonts.
Output: red-graph adaptativo + nota móvil; search-box petróleo; identity-warn con @theme + utilities planas; skeletons con fila de crumb; 3 minors de código; F-05 documentado en el summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-uxdemo-pulido-presentacional/54-UI-SPEC.md
@.planning/phases/54-uxdemo-pulido-presentacional/54-RESEARCH.md
@app/components/red/red-graph.tsx
@app/components/search-box.tsx
@app/components/identity-marker.tsx

<interfaces>
<!-- Contracts 4 y 5 del UI-SPEC + mecánica verificada del RESEARCH. Este plan corre DESPUÉS de 54-01 -->
<!-- (comparte lobby-de-parlamentario.tsx y contraparte/[id]/page.tsx). NO tocar los wraps formatNombre de 54-01. -->

C4 — F-04 grafo móvil (solo lo que @xyflow/react 12.11.0 YA soporta; cero JS nuevo significativo; el layout de grid determinista por cámara NUNCA pasa a physics):
1. Altura adaptativa: reemplazar el inline style={{ height: 480 }} de .net-lienzo por clases token h-96 md:h-120 (384px móvil / 480px ≥768px). VERIFICADO que compilan con el Tailwind 4.3.1 instalado. Sin inline style, sin arbitrary [Npx].
2. Compactar filtros en móvil: bloque .net-filtros (checkboxes de tipo + ventana de fecha) apila vertical y ajusta en <768px — CSS-only en el bloque net-* existente de globals.css (flex-direction: column, gaps reducidos en la escala 4/8px). NINGÚN filtro se remueve ni oculta; los inputs de fecha conservan font-mono y labels. Ejemplo verificado: @media (max-width: 47.99rem) { .net-filtros { flex-direction: column; align-items: stretch; gap: 0.5rem; padding: 0.5rem 0.75rem; } }
3. Nota honesta (solo móvil): UNA línea sobre el canvas, visible solo <md: <p class="mt-4 text-sm text-muted-foreground md:hidden">El grafo se lee mejor en pantalla ancha; en pantallas angostas puede verse comprimido.</p> — visible, no bloqueante, nunca overlay, nunca oculta el grafo.
4. Knobs xyflow permitidos (discreción): fitViewOptions={{ padding: 0.05 }} y minZoom={0.2} para reducir overlap de labels a 390px (props de primera clase en la 12.11.0 instalada). NO permitido: nuevo algoritmo de layout, clustering, physics, truncar labels que ocultan nombres.
5. Intacto: rama empty-state (copy honesto + línea de continuación), semántica de filtros (set tiposOcultos), tipos edge/node, tooltips de provenance.

C5a — Botón /buscar (search-box.tsx ~:119-126, rama no-hero): cambiar la clase de "h-12" a "h-12 bg-accent-product text-background hover:bg-accent-product/90". Label sigue "Buscar". El weight de la rama no-hero se conserva como está (NO propagar font-semibold del hero). Rama hero BYTE-IDENTICAL.

C5b — identity-warn (corrección de sintaxis que RESTAURA el visual — NO pixel-identical):
  Hallazgo del RESEARCH: la clase shippeada bg-[--identity-warn-bg] compila a `background-color: --identity-warn-bg;` (inválido, sin var()) → el marker HOY se renderiza SIN fondo/texto/borde ámbar. El fix RESTAURA el diseño ámbar.
  - En globals.css agregar un bloque @theme inline registrando los tokens SIN wrapper hsl() (civic-tokens.css ya define valores hsl() COMPLETOS):
      @theme inline { --color-identity-warn-bg: var(--identity-warn-bg); --color-identity-warn-fg: var(--identity-warn-fg); --color-identity-warn-border: var(--identity-warn-border); }
    PELIGRO (Pitfall 2): NO copiar el patrón hsl(var(--accent-product)) — doble hsl = color inválido otra vez.
  - Reemplazar las clases arbitrary-var por utilities planas en identity-marker.tsx (:16-17) y lobby-de-parlamentario.tsx (:262-263): bg-identity-warn-bg text-identity-warn-fg border-identity-warn-border. Los valores HSL en civic-tokens.css quedan intactos (light + dark).
  - Aceptación (REWORDED por RESEARCH): before = marker sin ámbar / after = marker con ámbar. NO exigir pixel-diff == 0.

C5c — Skeletons anti-CLS: agregar UNA fila placeholder de breadcrumb ARRIBA del shimmer del título en ParlamentarioHeaderSkeleton (parlamentario/[id]/page.tsx ~:440-451) y HeaderSkeleton (contraparte/[id]/page.tsx ~:161-169): una `<Skeleton className="h-4 w-40" />` al tope de ambos (usar el mismo componente Skeleton shippeado; animate-pulse si los skeletons existentes lo usan). Elimina el layout shift cuando los Breadcrumbs hacen stream-in.

C5d — Minors de código (sin contrato visual):
  IN-01: app/components/header-nav.test.tsx:2 — quitar el import `within` sin uso.
  IN-02: app/components/breadcrumbs.test.tsx:76-86 — renombrar el título del test a "dos ítems (contraparte: [Inicio, nombre]) dibujan 1 separador y 1 link" (el fixture tiene DOS items; el título actual "un solo ítem/0 separadores" miente).
  IN-04: app/components/global-header.tsx:16-17 — actualizar el docstring del nav a la lista de 5 items (orden del 53-UI-SPEC §a: incluye "Red" y "Sobre" acortado); ya no la lista pre-F53 de 4.

C5e — F-05 woff2 preload warnings: VEREDICTO = documentar y diferir. NO tocar app/app/layout.tsx. El knob (preload:false) existe pero trocaría ruido invisible por riesgo de FOUT (prohibido por el propio 5e). Documentar en el SUMMARY: root cause (heurística de timing de Chrome sobre fonts que SÍ se usan) + knob + por qué se difiere. Los woff2 son level:warning, no error → el criterio "cero errores de consola" del ROADMAP se cumple.

Nota: los números de línea pueden haber corrido tras 54-01. Localizar por contenido.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: F-04 grafo móvil + botón /buscar petróleo</name>
  <files>app/components/red/red-graph.tsx, app/components/red/red-graph.test.tsx, app/app/globals.css, app/components/search-box.tsx, app/components/search-box.test.tsx</files>
  <action>F-04 (C4): en red-graph.tsx reemplazar `style={{ height: 480 }}` de `.net-lienzo` por clases `h-96 md:h-120`; agregar `fitViewOptions={{ padding: 0.05 }}` y `minZoom={0.2}` al `<ReactFlow>`; agregar la nota móvil `<p class="mt-4 text-sm text-muted-foreground md:hidden">` con el string EXACTO sobre el canvas. En globals.css agregar la media query `@media (max-width: 47.99rem)` que compacta `.net-filtros` (column, gaps reducidos) sin ocultar filtros ni quitar font-mono a los inputs de fecha. Dejar intactos empty-state, semántica de filtros, tipos edge/node, tooltips de provenance. C5a: en search-box.tsx cambiar la clase de la rama no-hero de `"h-12"` a `"h-12 bg-accent-product text-background hover:bg-accent-product/90"`; label sigue "Buscar"; rama hero byte-identical. Extender red-graph.test.tsx para asertar clases h-96/md:h-120 en el lienzo, la nota md:hidden, y filtros intactos. Crear search-box.test.tsx (Wave 0) asertando la rama no-hero con clases petróleo y la rama hero byte-identical.</action>
  <verify>
    <automated>cd app && npx vitest run components/red/red-graph.test.tsx components/search-box.test.tsx</automated>
  </verify>
  <done>Canvas adaptativo + nota móvil + knobs xyflow; filtros compactos sin remover ninguno; botón /buscar petróleo con hero intacto; tests verdes (incl. el nuevo search-box.test.tsx).</done>
</task>

<task type="auto">
  <name>Task 2: identity-warn @theme + utilities planas + skeletons anti-CLS</name>
  <files>app/app/globals.css, app/components/identity-marker.tsx, app/components/lobby-de-parlamentario.tsx, app/components/identity-marker.test.tsx, app/app/parlamentario/[id]/page.tsx, app/app/contraparte/[id]/page.tsx</files>
  <action>C5b: en globals.css agregar un bloque `@theme inline` registrando `--color-identity-warn-bg/fg/border` como `var(--identity-warn-*)` SIN wrapper hsl() (Pitfall 2). Reemplazar las clases `bg-[--identity-warn-bg]`-style por utilities planas `bg-identity-warn-bg text-identity-warn-fg border-identity-warn-border` en identity-marker.tsx (:16-17) y en lobby-de-parlamentario.tsx (:262-263 — SOLO estas clases; NO tocar el wrap formatNombre de :236 que aplicó 54-01). Valores HSL en civic-tokens.css intactos. Crear identity-marker.test.tsx (Wave 0) asertando que el marker usa las utilities planas (sin arbitrary-var). C5c: agregar `<Skeleton className="h-4 w-40" />` al tope de ParlamentarioHeaderSkeleton (parlamentario/[id]/page.tsx) y de HeaderSkeleton (contraparte/[id]/page.tsx), arriba del shimmer del título, matcheando la caja real del breadcrumb.</action>
  <verify>
    <automated>cd app && npx vitest run components/identity-marker.test.tsx components/lobby-de-parlamentario.test.tsx</automated>
  </verify>
  <done>@theme registra los tokens sin doble-hsl; marker y lobby usan utilities planas (visual ámbar restaurado); ambos skeletons tienen fila de crumb; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Minors de código IN-01/IN-02/IN-04 + documentar F-05</name>
  <files>app/components/header-nav.test.tsx, app/components/breadcrumbs.test.tsx, app/components/global-header.tsx</files>
  <action>IN-01: en header-nav.test.tsx:2 quitar `within` de la lista de imports (sin uso). IN-02: en breadcrumbs.test.tsx:76-86 renombrar el título del test a "dos ítems (contraparte: [Inicio, nombre]) dibujan 1 separador y 1 link"; los asserts NO cambian. IN-04: en global-header.tsx:16-17 actualizar el docstring del nav a la lista de 5 items del 53-UI-SPEC §a (incluye "Red" y "Sobre" acortado). NO tocar app/app/layout.tsx (F-05 se difiere). Documentar F-05 en el SUMMARY de este plan: root cause + knob preload:false + por qué se difiere (riesgo FOUT, prohibido por 5e; los woff2 son warning no error).</action>
  <verify>
    <automated>cd app && npx vitest run components/header-nav.test.tsx components/breadcrumbs.test.tsx</automated>
  </verify>
  <done>within removido; título del test de breadcrumbs corregido; docstring de global-header actualizado; layout.tsx sin cambios; F-05 documentado en el SUMMARY; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (isla RedGraph/SearchBox) | Único código cliente tocado; sin input nuevo de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06 | Tampering (insinuación) | red-graph F-04 | mitigate | Fix acotado CSS/props; el grafo sigue grid determinista — sin physics, sin proximity-as-relation, sin agregados; filtros y tooltips de provenance intactos |
| T-54-07 | Information Disclosure | identity-warn / IdentityMarker | accept | Solo sintaxis de token; IdentityMarker semántico intacto; valores HSL sin cambio; restaura el aviso "identidad no verificada" |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | CERO paquetes nuevos (hard bound LOCKED); F-04 usa solo lo que xyflow 12.11.0 ya trae |
</threat_model>

<verification>
- Tests de red-graph, search-box (nuevo), identity-marker (nuevo), lobby-de-parlamentario, header-nav, breadcrumbs verdes.
- Grep: no queda `bg-[--identity-warn-bg]` ni arbitrary-var identity-warn en el código.
- Grep: `style={{ height: 480 }}` ya no existe en red-graph.tsx.
- app/app/layout.tsx sin diff (F-05 diferido).
- Verificación visual (grafo 390, marker ámbar, fold, botón petróleo) se cubre en Plan 05 con screenshots post-deploy.
</verification>

<success_criteria>
Grafo /red usable en móvil (canvas adaptativo + filtros compactos + nota honesta); botón /buscar petróleo con hero intacto; visual ámbar del marker restaurado vía @theme + utilities planas; skeletons sin CLS; 3 minors de código resueltos; F-05 documentado y diferido; suite de los archivos tocados verde.
</success_criteria>

<output>
Create `.planning/phases/54-uxdemo-pulido-presentacional/54-04-SUMMARY.md` when done
</output>
