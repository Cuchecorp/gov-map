---
phase: 121-escalera-doc-extension-solo-con-benchmark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md
  - .planning/phases/121-escalera-doc-extension-solo-con-benchmark/check-escalera-doc.sh
  - .planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-VERIFICATION.md
autonomous: true
requirements: [CRON-04]

must_haves:
  truths:
    - "Un lector abre 121-ESCALERA-ESTADO.md y ve, para cada una de las 5 tareas LLM (routing, clasificación, juez, extracción, adjudicación), un estado explícito EXTENDIDA / NO EXTENDIDA / INTOCABLE con la evidencia de benchmark que lo respalda (SC#1)"
    - "Las tareas sin benchmark de paridad quedan NO EXTENDIDAS citando la regla LOCKED 'ante la duda, siempre calidad', con el flip de routing a incumbent-stays en full-40 y el veto es-CL de extracción como evidencia (SC#2)"
    - "La adjudicación de identidad queda INTOCABLE por decisión explícita de diseño (SEED-001 v11.0), declarada como tal — no ausente del documento (SC#3)"
    - "Para cada tarea pendiente el documento dice qué evidencia concreta la extendería (SC#4)"
    - "El estado EXTENDIDA de clasificación queda condicionado a la vigencia del drift canary: si el modelo servido difiere del pinneado, el veredicto se invalida y el estado vuelve a evaluarse"
  artifacts:
    - path: ".planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md"
      provides: "Estado por tarea LLM con evidencia citada, qué-haría-falta, régimen de guards y condición de vigencia"
      contains: "121-ESCALERA-ESTADO"
    - path: ".planning/phases/121-escalera-doc-extension-solo-con-benchmark/check-escalera-doc.sh"
      provides: "Verificación de cierre por grep (5 estados, ≥1 cita por estado, 0 secretos)"
    - path: ".planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-VERIFICATION.md"
      provides: "Salida del check + mapeo SC#1-SC#4"
  key_links:
    - from: "121-ESCALERA-ESTADO.md"
      to: "107-VEREDICTO-LIVE-FULL-2026-07-27.md (be0b1b9)"
      via: "cita verbatim de cifras"
      pattern: "107-VEREDICTO-LIVE-FULL-2026-07-27"
    - from: "121-ESCALERA-ESTADO.md"
      to: "120-FLIP-RECORD.md"
      via: "justificante del estado EXTENDIDA de clasificación"
      pattern: "120-FLIP-RECORD"
    - from: "121-ESCALERA-ESTADO.md"
      to: "packages/cruces/src/drift-canary.test.ts"
      via: "condición de vigencia re-ejecutable"
      pattern: "drift-canary"
---

<objective>
Producir `121-ESCALERA-ESTADO.md`: el registro auditable, tarea LLM por tarea LLM, del estado
extendida / no extendida / intocable de la escalera, con la evidencia de benchmark que lo respalda
y con qué evidencia concreta extendería cada pendiente — de modo que nadie la extienda por intuición.

Purpose: cerrar CRON-04. Sin este documento, la escalera se extiende por memoria o por intuición;
el caso routing (approved en la muestra de 10, revertido en el full-40) demuestra por qué eso es
inaceptable. Regla LOCKED: ante la duda, SIEMPRE calidad.
Output: un documento en el phase dir + un script de verificación por grep + el registro de su salida.

**Fase DOC pura: CERO código de producto.** No se toca `packages/`, `app/`, migraciones, `.env`
ni `.env.example`. Los únicos archivos escritos son los tres de `files_modified`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-CONTEXT.md
@.planning/milestones/v11.0-phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-VEREDICTO-LIVE-FULL-2026-07-27.md
@.planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md

<interfaces>
<!-- Rutas reales verificadas en el repo. El ejecutor NO debe salir a buscarlas. -->

Fuente de evidencia primaria (veredicto full-40, commit be0b1b9, 2026-07-27):
  .planning/milestones/v11.0-phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-VEREDICTO-LIVE-FULL-2026-07-27.md

Justificante del estado EXTENDIDA de clasificación (2026-07-28):
  .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md

Guards de régimen que congelan el alcance de la escalera (7 tests, verdes al cierre de 120):
  packages/llm/src/integ-scope-guard.test.ts
  packages/llm/src/provider-guard.test.ts
  packages/llm/src/tiered-scope-guard.test.ts

Condición de vigencia re-ejecutable (drift canary, env-gated):
  packages/cruces/src/drift-canary.test.ts
  invocación: CLASIFICACION_DRIFT_CHECK=1 pnpm --filter @obs/cruces exec vitest run src/drift-canary.test.ts

Patrón de verificación por grep a espejar (Phase 118):
  .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh
  .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-VERIFICATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer las cifras verbatim del veredicto y escribir §Fuentes + tabla maestra</name>
  <files>.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md</files>
  <read_first>
    - .planning/milestones/v11.0-phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-VEREDICTO-LIVE-FULL-2026-07-27.md (LEER ÍNTEGRO — las cifras se copian de aquí, JAMÁS de memoria)
    - .planning/phases/120-escalera-on-flip-clasificacion-escalera/120-FLIP-RECORD.md (§Gate 2, §Gate 3, §Estado final CRON-03)
    - .planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-CONTEXT.md
  </read_first>
  <action>
    Crear `121-ESCALERA-ESTADO.md` con el encabezado, la sección de fuentes y la tabla maestra.

    REGLA DE FIDELIDAD (rectora de toda la fase): cada cifra que aparezca en el documento debe
    existir literalmente en el archivo de origen. Antes de escribir cualquier número, localizarlo
    en el texto del veredicto ya leído. Si un número no está en el archivo, NO se escribe — se
    declara "no medido en el veredicto full-40". Cero cifras de memoria, cero redondeos nuevos.

    Encabezado: título `# 121 — ESCALERA-ESTADO: extensión solo con benchmark`, fecha 2026-07-28,
    requisito CRON-04, y la regla LOCKED enunciada en una línea propia con el texto exacto
    "ante la duda, siempre calidad".

    Sección `## Fuentes de evidencia` (tabla): por cada fuente, ruta relativa desde la raíz del
    repo + qué aporta + fecha/commit. Las tres fuentes son el veredicto full-40 (commit `be0b1b9`,
    2026-07-27), `120-FLIP-RECORD.md` (2026-07-28) y los tres guards de `packages/llm/src/`.
    Declarar la cobertura del veredicto: golden es-CL FROZEN COMPLETOS (no la muestra de 10),
    corrida `LLM_BENCH_LIVE=1 LLM_BENCH_LIMIT=0`, PASSED, y la duración tal como la reporta el
    archivo.

    Sección `## Tabla maestra` con exactamente 5 filas — una por tarea LLM: routing, clasificación,
    juez, extracción, adjudicación — y 4 columnas: `Tarea | Estado | Evidencia (cita) | Qué haría falta`.

    Los estados son de vocabulario CERRADO y se escriben en MAYÚSCULAS para que el grep los cace:
    `EXTENDIDA`, `NO EXTENDIDA`, `INTOCABLE`. Asignación (decisiones LOCKED del CONTEXT):
    - routing → NO EXTENDIDA
    - clasificación → EXTENDIDA
    - juez → NO EXTENDIDA
    - extracción → NO EXTENDIDA
    - adjudicación → INTOCABLE

    La columna Evidencia de cada fila cita el archivo de origen por nombre (`107-VEREDICTO-LIVE-FULL-2026-07-27.md`
    o `120-FLIP-RECORD.md`) y las cifras copiadas del veredicto: para routing el Δcalidad y las
    coberturas Granite vs DeepSeek; para clasificación el Δcalidad y las coberturas en paridad más
    el registro del flip; para juez el `recall_rechazo` de Phi vs el incumbente y el Δ; para
    extracción los valores de `negacion.accuracy` y de precision/recall de value de ambos modelos.
    Para adjudicación la evidencia NO es una métrica: es la decisión de diseño v11.0 (SEED-001) —
    decirlo así, sin fabricar un número.

    La columna "Qué haría falta" resume en una línea lo que la sección por tarea (Task 2) desarrolla.

    Sección `## Cómo leer los estados`: definir los tres valores. EXTENDIDA = la escalera está
    encendida para esa tarea con benchmark de paridad demostrado. NO EXTENDIDA = el incumbente se
    queda; puede haber evidencia parcial o favorable, pero no basta para promover. INTOCABLE = no
    es candidata a benchmark de extensión en absoluto; sólo cambia por decisión de operador con
    dossier. Dejar explícito que NO EXTENDIDA no significa "sin evaluar": las cuatro tareas no
    extendidas fueron medidas, y tres de ellas perdieron o quedaron cortas con evidencia.

    Prohibido en TODO el documento: nombres de variables con valor, tokens, account IDs, cualquier
    string que parezca un secreto. Se pueden nombrar variables (`CLASIFICACION_ESCALERA`,
    `WORKERS_AI_API_TOKEN`) pero JAMÁS con un valor asociado distinto del literal `1` de la primera.
  </action>
  <acceptance_criteria>
    - El archivo existe y contiene las 5 tareas en la tabla maestra.
    - Cada estado usa el vocabulario cerrado EXTENDIDA / NO EXTENDIDA / INTOCABLE.
    - Cada fila cita al menos un archivo de origen por nombre.
    - Toda cifra presente en el documento aparece también en 107-VEREDICTO-LIVE-FULL-2026-07-27.md.

    NOTA DE FORMA (la verificación de cierre depende de esto): en la tabla maestra el estado va
    en su PROPIA celda, escrito exactamente como el vocabulario cerrado y nada más —
    `| EXTENDIDA |`, `| NO EXTENDIDA |`, `| INTOCABLE |`. Sin emoji, sin adornos, sin paréntesis
    dentro de la celda: los matices van en la columna Evidencia o en la sección por tarea.
    Reparto exacto de las 5 filas: 1 celda `EXTENDIDA` a secas (clasificación), 3 celdas
    `NO EXTENDIDA` (routing, juez, extracción), 1 celda `INTOCABLE` (adjudicación). El grep de
    Task 3 cuenta celdas, no menciones en prosa — si el estado se escribe con adornos, el conteo
    falla y el gate muerde (es lo que se quiere).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md && test -f "$D" && for t in routing clasificaci juez extracci adjudicaci; do grep -qi "$t" "$D" || { echo "FALTA tarea: $t"; exit 1; }; done && EX=$(grep -c '^|.*| *EXTENDIDA *|' "$D"); NE=$(grep -c '^|.*| *NO EXTENDIDA *|' "$D"); IN=$(grep -c '^|.*| *INTOCABLE *|' "$D"); echo "celdas -> EXTENDIDA=$EX NO_EXTENDIDA=$NE INTOCABLE=$IN"; [ "$EX" -eq 1 ] && [ "$NE" -eq 3 ] && [ "$IN" -eq 1 ] && grep -q "107-VEREDICTO-LIVE-FULL-2026-07-27" "$D" && grep -q "be0b1b9" "$D" && grep -q "ante la duda, siempre calidad" "$D" && echo OK-T1</automated>
  </verify>
  <done>121-ESCALERA-ESTADO.md tiene encabezado, §Fuentes, tabla maestra de 5 filas con estados del vocabulario cerrado y citas, y §Cómo leer los estados.</done>
</task>

<task type="auto">
  <name>Task 2: Secciones por tarea, recuadro del flip de routing, guards y condición de vigencia</name>
  <files>.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md</files>
  <read_first>
    - El propio 121-ESCALERA-ESTADO.md escrito en Task 1
    - 107-VEREDICTO-LIVE-FULL-2026-07-27.md §"Contraste con el 10-sample" y §"Qué autoriza esto (BENCH-05)" y §"BENCH-04"
    - 120-FLIP-RECORD.md §Gate 2 (drift canary), §Gate 6 (guards), §"Estado final CRON-03"
  </read_first>
  <action>
    Extender el documento con el cuerpo. Misma REGLA DE FIDELIDAD de Task 1: ninguna cifra nueva
    que no esté en los archivos leídos.

    (a) `## Recuadro — la lección: por qué extensión SOLO con benchmark`. Es la sección de mayor
    valor del documento y va ANTES de las secciones por tarea. Contar el caso routing: la muestra
    de 10 dio approved para el candidato; el full-40 revirtió el resultado a incumbent-stays. Citar
    ambos números tal como los da el veredicto en su tabla de contraste. Enunciar la conclusión
    operativa: una muestra chica es direccional, no decisoria; ninguna integración se flipea sin el
    set completo congelado. Cerrar con la regla LOCKED literal "ante la duda, siempre calidad".
    No dramatizar ni moralizar: es un registro técnico.

    (b) Una sección por tarea, cinco en total, en este orden: clasificación (la única EXTENDIDA
    primero), routing, extracción, juez, adjudicación. Cada sección lleva SIEMPRE estas cuatro
    subpartes rotuladas:
      - **Estado**: el valor del vocabulario cerrado.
      - **Evidencia**: cifras copiadas del veredicto + cita del archivo. Para clasificación además
        el justificante de encendido: `120-FLIP-RECORD.md`, con el drift canary (modelo servido ==
        pinneado), el shadow-eval sobre los casos ficha comparables del `GOLDEN_SET_GATE`, y el
        rollback probado en vivo — con los conteos exactos que el FLIP-RECORD reporta.
      - **Lectura**: qué significa esa evidencia, en prosa breve. Para extracción, decir que el
        veto es-CL se disparó por corto-circuito y qué métrica lo disparó. Y **reconciliar el
        vocabulario de forma explícita**, porque las dos fuentes rotulan distinto y un lector
        futuro lo leería como contradicción: el veredicto full-40 escribe "DeepSeek se queda.
        INTOCABLE" para extracción, mientras que este documento la registra **NO EXTENDIDA**.
        No hay conflicto y hay que decir por qué: en el veredicto "INTOCABLE" significa "el
        incumbente no se mueve"; en ESTE documento `INTOCABLE` está reservado a lo que NO es
        candidato a benchmark en absoluto (sólo adjudicación). Extracción SÍ es candidata — un
        benchmark es-CL que superara el veto podría extenderla — luego le corresponde
        NO EXTENDIDA. Aparte de eso, su strict-schema queda congelado por `integ-scope-guard` y
        `provider-guard`, que es un eje distinto del estado de extensión. Para juez, que el
        instrumento está validado contra humano pero permanece ESCALATE-ONLY: recall alto no es
        benchmark de paridad para promoverlo a decisor. Para adjudicación, que el estado es una
        decisión de diseño explícita (SEED-001, v11.0): el RUT jamás cruza a un LLM ajeno al
        pipeline aprobado y lo crítico/sensible se queda en MiniMax.
      - **Qué evidencia concreta la extendería** (SC#4): condición verificable, no un deseo.
        routing → un full-40 (o mayor) con el candidato ganando en TODAS las métricas separadas y
        sin flip entre muestra y full. extracción → benchmark es-CL con `negacion.accuracy` ≥ el
        incumbente sobre set congelado + strict-schema validado por zod, manteniendo el veto es-CL
        como gate. juez → benchmark de paridad juez-vs-juez con recall ≥ el actual y falsos
        ESCALATE no peores; mientras tanto ESCALATE-ONLY. adjudicación → **N/A por diseño**:
        decirlo explícitamente, ninguna cantidad de benchmark la extiende; sólo una decisión de
        operador con dossier. Que la subparte exista igual en adjudicación es deliberado — el SC#3
        exige que sea INTOCABLE por decisión, no por omisión.

    (c) `## Régimen de guards`. Los tres guards de `packages/llm/src/` (integ-scope-guard,
    provider-guard, tiered-scope-guard) por nombre de archivo, qué congela cada uno, y el comando
    que los corre. Declarar que estaban verdes al cierre de la Phase 120 con la escalera YA
    encendida, con el conteo de tests tal como lo reporta el Gate 6 del FLIP-RECORD. Esta sección
    es el mecanismo que hace del documento algo más que buena voluntad: el alcance de la escalera
    está congelado en tests, no sólo en prosa.

    (d) `## Condición de vigencia — drift canary`. El estado EXTENDIDA de clasificación NO es
    permanente: vale mientras el modelo servido por Workers AI coincida con el pinneado del
    veredicto. Dar el comando de re-ejecución del canary (env-gated) y la regla: si el canary
    reporta mismatch, el veredicto full-40 queda invalidado para esa tarea, el estado EXTENDIDA
    cae y la tarea vuelve a NO EXTENDIDA hasta que un benchmark nuevo la re-apruebe. El rollback
    correspondiente es quitar la línea `CLASIFICACION_ESCALERA=1` de `.env` — sin migración, sin
    deploy, sin cambio de código, tal como lo probó el Gate 5b. Añadir la aclaración del propio
    FLIP-RECORD: `CLASIFICACION_ESCALERA` NO pertenece a la familia `*_PUBLIC_ENABLED`, no es un
    flag de exposición pública, y su estado esperado en el E2E de la Phase 125 es ON.

    (e) `## Alcance y límites declarados` (honestidad, patrón 118/119): la escalera encendida cubre
    únicamente la clasificación de fichas; la clasificación no corre en ningún cron de CI (el flip
    vive en el `.env` local del operador); el veredicto se midió sobre golden sets es-CL congelados
    y no sobre tráfico de producción; cualquier benchmark nuevo para extender tareas es trabajo de
    un milestone futuro, fuera de v12.0.

    Mantener la prohibición de secretos de Task 1 en todo lo añadido.
  </action>
  <acceptance_criteria>
    - Las 5 secciones por tarea existen y cada una tiene las 4 subpartes rotuladas, incluida la de "qué evidencia la extendería" en adjudicación (con N/A por diseño).
    - Existe el recuadro del flip de routing muestra-10 vs full-40.
    - Existe §Régimen de guards nombrando los 3 archivos de guard.
    - Existe §Condición de vigencia citando drift-canary y la regla de invalidación.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-ESCALERA-ESTADO.md && grep -q "integ-scope-guard" "$D" && grep -q "provider-guard" "$D" && grep -q "tiered-scope-guard" "$D" && grep -q "drift-canary" "$D" && grep -q "120-FLIP-RECORD" "$D" && grep -qi "SEED-001" "$D" && grep -qi "es-CL" "$D" && grep -qi "ESCALATE-ONLY" "$D" && [ "$(grep -ci 'qu[eé] evidencia' "$D")" -ge 5 ] && echo OK-T2</automated>
  </verify>
  <done>El documento está completo: recuadro de la lección, 5 secciones por tarea con las 4 subpartes, régimen de guards, condición de vigencia y límites declarados.</done>
</task>

<task type="auto">
  <name>Task 3: Script de verificación por grep + 121-VERIFICATION.md</name>
  <files>.planning/phases/121-escalera-doc-extension-solo-con-benchmark/check-escalera-doc.sh, .planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh (patrón de script a espejar: forma, fail-loud, salida legible)
    - .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-VERIFICATION.md (patrón de registro)
    - El 121-ESCALERA-ESTADO.md terminado
  </read_first>
  <action>
    Escribir `check-escalera-doc.sh` espejando la FORMA de `check-crons.sh` (Phase 118), que el
    `read_first` obliga a leer antes:

    - Shebang `#!/usr/bin/env bash` — NO POSIX sh puro.
    - `set -u` a secas. **NO usar `set -e` ni `pipefail`**: el script debe correr TODAS las
      comprobaciones y reportarlas juntas; con `set -e` la primera falta aborta y el operador ve
      un solo problema por corrida en vez del cuadro completo.
    - Acumulador `FAILS=0`: cada comprobación imprime su línea `PASS`/`FAIL` e incrementa `FAILS`
      cuando falla. Al final, resumen + `exit 1` si `FAILS -gt 0`, `exit 0` si no.
    - Se resuelve a la raíz del repo con `git rev-parse --show-toplevel` (no depende del cwd).
    - POSIX grep/awk únicamente — sin `grep -P` (no disponible en este entorno, gotcha 118).
    - Read-only: no escribe en el repo, no toca DB ni red (salvo los temporales del self-check,
      que viven en el scratchpad y se borran).

    HIGIENE DEL PROPIO GATE (lección literal de `check-crons.sh`): este script contiene en su
    código los mismos patrones de secreto que busca. Por eso las comprobaciones se aplican
    SIEMPRE al ARTEFACTO (`121-ESCALERA-ESTADO.md`), nunca a `check-escalera-doc.sh` mismo, que
    no debe aparecer en ninguna lista de archivos escaneados.

    Comprobaciones obligatorias:
    1. El documento existe.
    2. Las 5 tareas están nombradas (routing, clasificación, juez, extracción, adjudicación).
    3. Cada una de las 5 tareas tiene un estado explícito del vocabulario cerrado. Comprobar la
       presencia de los tres valores EXTENDIDA / NO EXTENDIDA / INTOCABLE y que la tabla maestra
       tiene 5 filas de tarea.
    4. Cada estado tiene ≥1 cita: el documento referencia `107-VEREDICTO-LIVE-FULL-2026-07-27`,
       `be0b1b9` y `120-FLIP-RECORD`, y la sección de adjudicación referencia `SEED-001`.
    5. Existe la subparte "qué evidencia … extendería" para las 4 tareas pendientes + el N/A de
       adjudicación (conteo ≥ 5).
    6. Cero secretos: el documento NO contiene ningún patrón de valor de credencial. Buscar
       asignaciones sospechosas del tipo `TOKEN=<algo>`, `KEY=<algo>`, `SECRET=<algo>`,
       `ACCOUNT_ID=<algo>` con un valor no vacío y distinto de un placeholder redactado, y strings
       largos hex/base64. La ÚNICA asignación con valor permitida es `CLASIFICACION_ESCALERA=1`
       (nombre de flag interno + literal `1`, no es secreto) — excluirla explícitamente antes de
       evaluar el resto.

    GATE DE HIGIENE (LOCKED, lección del proyecto): las comprobaciones por conteo NO deben contar
    líneas de comentario ni el propio texto del script. Cuando una comprobación cuente ocurrencias
    dentro del documento, filtrar antes las líneas que son cita del propio criterio (por ejemplo,
    la sección "Cómo leer los estados" que enumera el vocabulario) para que el conteo mida
    sustancia y no el glosario. Un `grep -c` desnudo con `== 0` como gate está PROHIBIDO: todo
    gate de ausencia debe filtrar primero y declarar qué filtró.

    SELF-CHECK ANTI-SECRETO (obligatorio, DOS fixtures). Al final del script, probar que el
    detector MUERDE — un detector que no muerde es peor que ninguno, porque da falsa confianza:

      - Fixture A: una asignación de credencial con valor falso, del tipo
        `WORKERS_AI_API_TOKEN=<valor-falso-inventado>`. Ejercita la rama "asignación con valor".
      - Fixture B: un string hex de ≥ 32 caracteres suelto (sin `=` delante). Ejercita la rama
        "blob que parece credencial" — es una rama DISTINTA: un token pegado en una salida
        transcrita no siempre viene como asignación.

    AMBOS fixtures deben ser reportados por la función de detección. Si cualquiera de los dos
    pasa inadvertido, el self-check FALLA y el script sale != 0 — aunque el documento real esté
    limpio. Los dos fixtures son claramente falsos e inventados; jamás se copia un valor real de
    `.env` para probar el detector.

    Los temporales se crean en el directorio de scratchpad de la sesión (NUNCA dentro del repo,
    para que un fallo a medias no deje un archivo con pinta de secreto en el árbol de trabajo) y
    se borran con `trap 'rm -f "$TMP_A" "$TMP_B"' EXIT`, de modo que se limpien incluso si el
    script aborta.

    Correr el script y escribir `121-VERIFICATION.md` con: el comando exacto, la fecha/hora, el
    exit code, la salida íntegra (sin secretos), y una tabla que mapea SC#1–SC#4 del ROADMAP §121 a
    la comprobación del script y a la sección del documento que lo satisface. Si alguna comprobación
    falla, corregir el documento y re-correr — se registra la corrida final, no una corrida roja
    maquillada.
  </action>
  <acceptance_criteria>
    - check-escalera-doc.sh existe, corre con exit 0 y su self-check de secretos muerde.
    - 121-VERIFICATION.md registra comando, exit code, salida y el mapeo SC#1–SC#4.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && sh .planning/phases/121-escalera-doc-extension-solo-con-benchmark/check-escalera-doc.sh && V=.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-VERIFICATION.md && test -f "$V" && grep -q "check-escalera-doc.sh" "$V" && grep -qi "exit" "$V" && for s in "SC#1" "SC#2" "SC#3" "SC#4"; do grep -q "$s" "$V" || { echo "FALTA $s"; exit 1; }; done && echo OK-T3</automated>
  </verify>
  <done>El script pasa en verde, el self-check de secretos muerde, y 121-VERIFICATION.md deja registro reproducible del cierre con los 4 SC mapeados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` local del operador → artefacto de planning versionado | El FLIP-RECORD y este documento transcriben salidas de comandos que consumieron `.env`; un descuido filtra un token a git |
| Documento de estado → decisiones futuras de integración | Si el documento afirma una paridad que el benchmark no midió, una fase futura extiende la escalera sobre evidencia falsa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Information Disclosure | 121-ESCALERA-ESTADO.md | mitigate | Prohibición explícita de valores de secreto en las tres tasks + comprobación 6 del `check-escalera-doc.sh` con self-check que prueba que el detector muerde |
| T-121-02 | Tampering | Cifras del veredicto | mitigate | REGLA DE FIDELIDAD: toda cifra se copia del archivo leído; `read_first` obliga a leer el veredicto íntegro antes de escribir; prohibido escribir números de memoria |
| T-121-03 | Repudiation | Estado EXTENDIDA de clasificación | mitigate | Cada estado lleva cita a archivo/commit (`be0b1b9`, `120-FLIP-RECORD.md`); comprobación 4 del script exige la presencia de las citas |
| T-121-04 | Elevation of Privilege | Alcance de la escalera | mitigate | §Régimen de guards documenta los tres guards de `packages/llm/src/` que congelan que la escalera NO alcanza adjudicación ni extracción strict-schema |
| T-121-05 | Denial of Service (de la validez) | Drift del modelo servido | mitigate | §Condición de vigencia: canary re-ejecutable; mismatch ⇒ estado EXTENDIDA cae a NO EXTENDIDA; rollback = quitar una línea de `.env` |
| T-121-SC | Tampering | npm/pip/cargo installs | mitigate | N/A por construcción: esta fase NO instala paquetes ni toca `package.json` / `pnpm-workspace.yaml`. Cero superficie de cadena de suministro |
</threat_model>

<verification>
1. `sh .planning/phases/121-escalera-doc-extension-solo-con-benchmark/check-escalera-doc.sh` → exit 0.
2. `git status --porcelain` muestra SOLO los tres archivos de `files_modified` como nuevos. Cero
   cambios en `packages/`, `app/`, `supabase/`, `.env`, `.env.example`, `package.json`.
3. Muestreo de fidelidad: elegir 3 cifras cualesquiera del documento y confirmar que aparecen
   literalmente en `107-VEREDICTO-LIVE-FULL-2026-07-27.md`.
</verification>

<success_criteria>
- SC#1: las 5 tareas LLM tienen estado explícito con evidencia de benchmark que lo respalda.
- SC#2: las tareas sin benchmark de paridad están NO EXTENDIDAS citando "ante la duda, siempre
  calidad", con el flip de routing a incumbent-stays y el veto es-CL de extracción registrados.
- SC#3: adjudicación marcada INTOCABLE por decisión explícita (SEED-001), con su propia subparte
  "qué la extendería" respondida N/A por diseño — presencia, no omisión.
- SC#4: cada tarea pendiente declara qué evidencia concreta y verificable la extendería.
- El check por grep pasa en verde y su self-check de secretos muerde.
- Cero código de producto tocado.
</success_criteria>

<output>
Create `.planning/phases/121-escalera-doc-extension-solo-con-benchmark/121-01-SUMMARY.md` when done
</output>
