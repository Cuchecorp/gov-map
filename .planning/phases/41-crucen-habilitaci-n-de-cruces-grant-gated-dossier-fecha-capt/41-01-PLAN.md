---
phase: 41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0041_cruces_rpc_fecha_captura.sql
  - supabase/tests/0041_cruces_rpc_fecha_captura.test.sql
  - app/lib/types.ts
  - app/components/cruces-de-parlamentario.tsx
  - app/components/cruces-de-parlamentario.test.tsx
autonomous: false
requirements: [CRUCEN-01]

must_haves:
  truths:
    - "El RPC cruces_de_parlamentario proyecta fecha_captura (frescura real del materializado), sin conceder execute a anon"
    - "El ProvenanceBadge de cada evidencia usa s.fecha_captura (nivel señal), no item.fecha → sin stale-amber falso"
    - "La fecha de la reunión (item.fecha) se muestra como texto factual plano sin verbo causal (§9.1 intacto)"
    - "El comentario LIMITACIÓN CONOCIDA (WR-02) fue eliminado del componente"
    - "Tras el drop+recreate, anon SIGUE sin execute (deny-by-default intacto)"
  artifacts:
    - path: "supabase/migrations/0041_cruces_rpc_fecha_captura.sql"
      provides: "RPC drop+recreate con fecha_captura LAST + doble revoke"
      contains: "revoke execute on function public.cruces_de_parlamentario(text) from anon, authenticated"
    - path: "supabase/tests/0041_cruces_rpc_fecha_captura.test.sql"
      provides: "pgTAP plan(4): columna + orden posicional + anon-deny + no-PII"
      contains: "proargnames"
    - path: "app/lib/types.ts"
      provides: "fecha_captura: string en CruceSenalRpcRow"
      contains: "fecha_captura"
  key_links:
    - from: "app/components/cruces-de-parlamentario.tsx"
      to: "CruceSenalRpcRow.fecha_captura"
      via: "capturedAt={new Date(s.fecha_captura)}"
      pattern: "capturedAt=\\{new Date\\(s\\.fecha_captura\\)\\}"
    - from: "supabase/tests/0041_cruces_rpc_fecha_captura.test.sql"
      to: "public.cruces_de_parlamentario"
      via: "has_function_privilege anon deny assert"
      pattern: "has_function_privilege\\('anon'"
---

<objective>
CRUCEN-01 — Fix WR-02 (frescura honesta) como rebanada vertical: migración 0041 que dropea+recrea `cruces_de_parlamentario` para PROYECTAR `cruce_senal.fecha_captura`, su pgTAP, el tipo, el componente que usa esa fecha como `capturedAt` del `ProvenanceBadge`, y los tests RTL.

Purpose: el badge hoy se alimenta con la fecha de la REUNIÓN (`item.fecha`), antigua → marca stale-amber falso y "Actualizado hace …" sobre una fecha de evento, no de frescura. El fix es proyectar la fecha de materialización del cruce (nivel señal). Deja la superficie lista para firmar/encender SIN conceder nada.

Output: 0041 (escrita; apply = checkpoint operador), 0041 pgTAP, `CruceSenalRpcRow.fecha_captura`, componente y tests actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-RESEARCH.md
@.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-CONTEXT.md
@.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-VALIDATION.md

<interfaces>
<!-- Contratos que el ejecutor necesita; extraídos del repo. NO explorar el codebase. -->

Migración espejo del drop+recreate (returns table modificada): supabase/migrations/0028_votos_instructivos.sql:24,32
Espejo del doble revoke (lección DEFAULT PRIVILEGES): supabase/migrations/0040_cruces_rpc.sql:55-63 — emite
  `revoke execute on function public.cruces_de_parlamentario(text) from public;`
  `revoke execute on function public.cruces_de_parlamentario(text) from anon, authenticated;`
  e INTENCIONALMENTE NO concede a anon.
Idiom pgTAP a copiar (bag_has proargnames + array_to_string ordered): supabase/tests/0029_votos_instructivos.test.sql:32-49

Tipo a modificar (app/lib/types.ts:305-341):
```ts
export interface CruceSenalRpcRow {
  sector_id: string;
  sector_etiqueta: string;
  tipo_senal: string;
  conteo: number;
  evidencia: CruceEvidencia;   // <- añadir fecha_captura: string DESPUÉS de evidencia
}
export interface CruceEvidenciaItem {
  tipo: string;
  fecha: string | null;        // fecha de la reunión → ahora SOLO texto factual plano
  contraparte_nombre_crudo: string;
  audiencia_id: string;
  enlace_fuente: string | null;
}
```

Helper de formato (app/lib/format.ts): `fechaCorta(d: Date): string` (línea 17), `relativeTimeEs`, `esStale` (default `now=new Date()`).
ProvenanceBadge (app/components/provenance-badge.tsx): `capturedAt: Date | null`; `capturedAt===null` → "fuente desconocida" + "Sin fecha de actualización"; stale (>48h) añade clase `text-amber-700` al `<span>` outer con `rounded-md` (líneas 41-45).
Componente actual: app/components/cruces-de-parlamentario.tsx — `cruces.map((s) => …)` (s en scope) → `s.evidencia.items.map((item, idx) => …)` (item en scope); badge actual línea 156-162; comentario WR-02 líneas 139-155.
Fixtures de test: app/components/cruces-de-parlamentario.test.tsx — `makeItem(...)`, `makeSenal(overrides)`, `makeViewData(overrides)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0041 (drop+recreate con fecha_captura) + pgTAP — Wave 0</name>
  <read_first>
    - supabase/migrations/0040_cruces_rpc.sql (el RPC actual, returns table SIN fecha_captura; el patrón de doble revoke a copiar)
    - supabase/migrations/0028_votos_instructivos.sql (espejo del drop+recreate por returns table modificada)
    - supabase/tests/0029_votos_instructivos.test.sql (idiom canónico bag_has proargnames + array_to_string)
    - supabase/migrations/0039_cruce_senal.sql (confirma que cruce_senal tiene la columna fecha_captura)
    - 41-RESEARCH.md §CRUCEN-01 (los drafts SQL exactos — seguir verbatim)
  </read_first>
  <action>
    Crear `supabase/migrations/0041_cruces_rpc_fecha_captura.sql` siguiendo VERBATIM el draft de 41-RESEARCH §CRUCEN-01 (líneas 17-71): cabecera LOUD (última aplicada=0040; apply=checkpoint operador por psql --db-url --single-transaction, NUNCA db push; PGCLIENTENCODING=UTF8 en Windows); `drop function if exists public.cruces_de_parlamentario(text);`; `create or replace function … returns table (sector_id text, sector_etiqueta text, tipo_senal text, conteo int, evidencia jsonb, fecha_captura timestamptz)` con `fecha_captura` AL FINAL (menor churn posicional; el cliente mapea por posición); cuerpo `language sql stable security definer set search_path = ''` que añade `cs.fecha_captura` al select del 0040; join a `sector` por código; order by conteo desc, sector_id asc. Tras el create, re-emitir AMBOS revokes (gate 5): `revoke execute ... from public;` Y `revoke execute ... from anon, authenticated;`. NO emitir ningún `grant ... to anon` (deny-by-default; eso es CRUCEN-02). Proyección PII-safe: sin rut/partido/donante_id.

    Crear `supabase/tests/0041_cruces_rpc_fecha_captura.test.sql` siguiendo VERBATIM el draft 41-RESEARCH (líneas 77-116), `plan(4)`: (1) `bag_has` sobre `unnest(proargnames)` afirma que el set CONTIENE `fecha_captura`; (2) `is(array_to_string(proargnames,','), 'p_id,sector_id,sector_etiqueta,tipo_senal,conteo,evidencia,fecha_captura', …)` orden posicional exacto; (3) `ok(not has_function_privilege('anon','public.cruces_de_parlamentario(text)','execute'), …)` anon SIGUE sin execute tras drop+recreate (cacha la omisión del 2º revoke — gate 5); (4) no-PII: `pg_get_functiondef` con comentarios stripeados `!~* '\y(partido|rut|email|donante_id)\y'`. NO usar `pg_get_function_result` regex (rechazado por validador). `begin; … rollback;`.
  </action>
  <verify>
    <automated>grep -c "from anon, authenticated" supabase/migrations/0041_cruces_rpc_fecha_captura.sql (debe ser >=1) Y grep -c "grant execute" supabase/migrations/0041_cruces_rpc_fecha_captura.sql que excluya comentarios (filtrar con grep -v '^--' | grep -c "grant execute" = 0)</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0041_cruces_rpc_fecha_captura.sql` existe; contiene `drop function if exists public.cruces_de_parlamentario(text);`, `fecha_captura timestamptz` como ÚLTIMA columna del returns table, y AMBAS líneas revoke (from public; from anon, authenticated).
    - Filtrando comentarios (`grep -v '^--'`), NO existe ninguna línea `grant execute ... to anon` en 0041.
    - `supabase/tests/0041_cruces_rpc_fecha_captura.test.sql` existe con `plan(4)`, usa `proargnames` (no `pg_get_function_result`), y tiene el assert `not has_function_privilege('anon', …, 'execute')`.
    - El cuerpo del RPC no menciona partido/rut/email/donante_id.
  </acceptance_criteria>
  <done>0041 + su pgTAP escritos verbatim del research; doble revoke presente; cero grant a anon; idiom proargnames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tipo + componente + tests RTL (capturedAt = s.fecha_captura)</name>
  <read_first>
    - app/lib/types.ts (CruceSenalRpcRow líneas 305-313, CruceEvidenciaItem líneas 330-341 — JSDoc a actualizar)
    - app/components/cruces-de-parlamentario.tsx (el badge línea 156-162; el comentario WR-02 líneas 139-155 a borrar)
    - app/components/provenance-badge.tsx (acople capturedAt===null → "fuente desconocida"; clase text-amber-700)
    - app/components/cruces-de-parlamentario.test.tsx (makeItem/makeSenal/makeViewData)
    - app/lib/format.ts (fechaCorta línea 17)
    - 41-RESEARCH.md §CRUCEN-01 (snippets exactos del componente y los tests)
  </read_first>
  <behavior>
    - Test 1: `fecha_captura` reciente (now-ISO) en la señal → ningún badge tiene la clase `text-amber-700` (no stale falso).
    - Test 2: render con `fecha_captura` reciente → aparece "Actualizado" y NO "Sin fecha de actualización".
    - Test 3: cuando `item.fecha` está seteado → aparece el texto plano "Reunión registrada el {fechaCorta}"; cuando `item.fecha` es null → ese texto está ausente.
  </behavior>
  <action>
    En `app/lib/types.ts`: añadir `fecha_captura: string;` a `CruceSenalRpcRow` DESPUÉS de `evidencia` (espejo del orden de columnas), con el JSDoc de 41-RESEARCH (nivel SEÑAL, proyectada por 0041, ISO del timestamptz → ProvenanceBadge.capturedAt). Actualizar el JSDoc de `CruceEvidenciaItem` (líneas ~324-329) para quitar el texto stale "Pitfall 1: el item NO trae fecha_captura" → frescura ahora viene de `CruceSenalRpcRow.fecha_captura`; `item.fecha` es solo texto factual de la reunión.

    En `app/components/cruces-de-parlamentario.tsx`: (1) `import { fechaCorta } from "@/lib/format";`. (2) Dentro del `<li>`, después de `ContraparteCruda`, añadir texto factual plano de la fecha de reunión §9.1-safe: si `item.fecha` existe, un `<span className="text-xs text-muted-foreground">Reunión registrada el {fechaCorta(new Date(item.fecha))}</span>` (sin verbo causal). (3) BORRAR el bloque de comentario completo "LIMITACIÓN CONOCIDA (WR-02 …)" (líneas ~139-155) y reemplazar el `ProvenanceBadge` para usar `capturedAt={new Date(s.fecha_captura)}` (de la SEÑAL, en scope en `cruces.map((s) => …)`), manteniendo `sourceName={sourceLabel("lobby")}` y `sourceUrl={item.enlace_fuente}`. Añadir el comentario corto (41-RESEARCH líneas 131-135) explicando que capturedAt = fecha de materialización (nivel señal), no item.fecha, y la nota de honestidad R6 (badge = frescura del rebuild del pipeline, cron '23 3 * * *').

    En `app/components/cruces-de-parlamentario.test.tsx`: añadir `fecha_captura: new Date().toISOString()` al default de `makeSenal` (reciente → nunca stale, determinista en la corrida). Añadir el describe block "CrucesView — frescura honesta (CRUCEN-01 / WR-02)" con los 3 asserts del behavior (snippet 41-RESEARCH líneas 165-187): buscar badges por `span[class*="rounded-md"]`, afirmar ninguno con `text-amber-700`; afirmar "Actualizado" presente y "Sin fecha de actualización" ausente; afirmar el texto "Reunión registrada el …" presente cuando item.fecha set y ausente cuando null.
  </action>
  <verify>
    <automated>cd app && npx vitest run cruces-de-parlamentario && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx vitest run cruces-de-parlamentario` verde (incl. los 3 nuevos asserts del describe "frescura honesta").
    - `cd app && npx tsc -b` limpio (el campo `fecha_captura: string` compila a través del componente).
    - `grep -c "LIMITACIÓN CONOCIDA" app/components/cruces-de-parlamentario.tsx` = 0 (comentario WR-02 eliminado).
    - `grep -c "capturedAt={new Date(s.fecha_captura)}" app/components/cruces-de-parlamentario.tsx` = 1.
    - El texto factual de reunión no usa verbo causal (§9.1): exactamente "Reunión registrada el …".
  </acceptance_criteria>
  <done>Tipo extendido, componente usa s.fecha_captura + texto plano de reunión, comentario WR-02 borrado, tests verdes, tsc limpio.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: CHECKPOINT OPERADOR — aplicar 0041 a PROD + pgTAP (autónomo NO aplica)</name>
  <what-built>
    Migración 0041 (drop+recreate de `cruces_de_parlamentario` con `fecha_captura` + doble revoke, SIN grant a anon) y su pgTAP `0041_cruces_rpc_fecha_captura.test.sql`. 0041 es APLICABLE a PROD porque NO concede nada (deny-by-default intacto), pero el apply es checkpoint de operador (gate 4): build/typecheck NO prueban que Postgres ejecutó el DDL — la única prueba válida es pgTAP-vs-PROD. El agente NO aplica salvo autorización explícita del operador en la corrida.
  </what-built>
  <how-to-verify>
    1. Extraer `SUPABASE_DB_URL` esquivando BOM U+FEFF (helper Phases 9-12). En Windows: `PGCLIENTENCODING=UTF8`.
    2. Aplicar: `psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0041_cruces_rpc_fecha_captura.sql` (NUNCA `supabase db push` — la última en PROD es 0040).
    3. Registrar la fila en `schema_migrations` para 0041.
    4. Correr el pgTAP nuevo: `psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0041_cruces_rpc_fecha_captura.test.sql` → 4/4 sin fallos.
    5. Regresión deny-by-default: `psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0040_cruces_rpc.test.sql` → sigue verde (anon NO execute — el doble revoke se re-emitió tras el drop+recreate).
  </how-to-verify>
  <acceptance_criteria>
    - pgTAP 0041 = 4/4 (fecha_captura emitida, orden posicional exacto, anon NO execute, no-PII).
    - pgTAP 0040 sin regresión (anon-deny intacto).
    - Fila 0041 registrada en `schema_migrations`.
    - 0042 NO aplicada (no es parte de este plan).
  </acceptance_criteria>
  <resume-signal>Operador escribe "pgTAP verde" tras aplicar 0041 y correr ambos tests, o "diferido" para dejar 0041 escrita sin aplicar (el código y los tests RTL ya quedan verdes; el apply queda como deuda de operador).</resume-signal>
</task>

</tasks>

<verification>
- `cd app && npx vitest run` full suite verde + `npx tsc -b` limpio (sin regresión en el resto de la suite).
- `grep "from anon, authenticated" supabase/migrations/0041_cruces_rpc_fecha_captura.sql` presente (gate 5).
- `grep -v '^--' supabase/migrations/0041_cruces_rpc_fecha_captura.sql | grep -c "grant execute"` = 0 (deny-by-default).
- Tras apply (operador): pgTAP 0041 4/4 + 0040 sin regresión.
</verification>

<success_criteria>
- 0041 escrita verbatim del research (drop+recreate, fecha_captura LAST, doble revoke, cero grant).
- 0041 pgTAP usa proargnames bag_has + array_to_string (no pg_get_function_result).
- `CruceSenalRpcRow.fecha_captura: string`; componente usa `capturedAt={new Date(s.fecha_captura)}`; comentario WR-02 borrado; fecha de reunión = texto plano §9.1-safe.
- Tests RTL verdes (no stale-amber falso) + tsc limpio.
- Apply de 0041 = checkpoint operador (autonomous: false); el agente NO aplica salvo autorización.
</success_criteria>

<output>
Create `.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-01-SUMMARY.md` when done. Debe dejar EXPLÍCITO el estado de apply de 0041 (aplicada por operador / diferida).
</output>
