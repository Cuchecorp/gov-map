---
phase: 41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0042_cruces_grant_anon.sql
  - supabase/tests/post-apply/0042_cruces_grant_anon.test.sql
autonomous: true
requirements: [CRUCEN-02]

must_haves:
  truths:
    - "0042 contiene el único grant execute to anon que 0040 omite, con precondición fail-loud si 0041 no está aplicada"
    - "0042 NO se aplica a PROD ni se registra en schema_migrations durante la corrida autónoma"
    - "El guard de no-apply-prematuro es el assert #3 existente de 0040_cruces_rpc.test.sql, dejado SIN cambios"
    - "El pgTAP de encendido vive en supabase/tests/post-apply/ (fuera del glob de la suite)"
  artifacts:
    - path: "supabase/migrations/0042_cruces_grant_anon.sql"
      provides: "grant gated escrito (no aplicado) + guard do$$ precondición + cabecera LOUD no-apply"
      contains: "grant execute on function public.cruces_de_parlamentario(text) to anon"
    - path: "supabase/tests/post-apply/0042_cruces_grant_anon.test.sql"
      provides: "prueba post-apply: anon TIENE execute + sigue security definer"
      contains: "has_function_privilege('anon'"
  key_links:
    - from: "supabase/migrations/0042_cruces_grant_anon.sql"
      to: "supabase/migrations/0041_cruces_rpc_fecha_captura.sql"
      via: "precondición do$$ que raise si fecha_captura ausente"
      pattern: "raise exception"
---

<objective>
CRUCEN-02 — Grant gated: escribir (y commitear) la migración 0042 con el único `grant execute on function public.cruces_de_parlamentario(text) to anon` que 0040 omite intencionalmente, MÁS una precondición fail-loud (`do $$`) que aborta si 0041 no está aplicada, MÁS su pgTAP de encendido en `post-apply/`.

Purpose: dejar la válvula de release (Candado A) lista para el día del encendido, SIN aplicarla. El apply de 0042 es checkpoint humano post-sign-off legal (espejo del patrón F17/NET). Un agente NUNCA la aplica ni la registra en schema_migrations.

Output: `0042_cruces_grant_anon.sql` (inerte en el repo), `post-apply/0042_cruces_grant_anon.test.sql`, y lenguaje obligatorio en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-RESEARCH.md
@.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-CONTEXT.md

<interfaces>
<!-- Contratos extraídos del repo. NO explorar el codebase. -->

Espejo del grant a anon: supabase/migrations/0030_net.sql:253-254 (subgrafo_red) y supabase/migrations/0021_*.sql (lobby_de_parlamentario) — firma exacta `(text)`.
0040 (lo que 0042 completa): supabase/migrations/0040_cruces_rpc.sql:62 — el comentario "INTENCIONALMENTE NO HAY grant execute … to anon".
Guard existente (NO tocar): supabase/tests/0040_cruces_rpc.test.sql assert #3 — `anon NO execute`; corre cada suite; es el guard de no-apply-prematuro.
Precondición fail-loud + cuerpo: 41-RESEARCH.md §CRUCEN-02 líneas 203-246 (draft verbatim).
Post-apply test: 41-RESEARCH.md §CRUCEN-02 líneas 261-275 (draft verbatim).
El directorio `supabase/tests/post-apply/` NO existe aún — crearlo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0042 (grant gated, escrita NO aplicada) + guard fail-loud — Wave 0</name>
  <read_first>
    - supabase/migrations/0040_cruces_rpc.sql (la línea que 0042 completa; el patrón de doble revoke)
    - supabase/migrations/0030_net.sql (líneas ~250-254, espejo del grant a anon de subgrafo_red)
    - 41-RESEARCH.md §CRUCEN-02 (draft SQL verbatim, líneas 203-246)
  </read_first>
  <action>
    Crear `supabase/migrations/0042_cruces_grant_anon.sql` siguiendo VERBATIM el draft de 41-RESEARCH §CRUCEN-02 (líneas 204-246): cabecera LOUD con el banner "████ MIGRACIÓN DE RELEASE — NO APLICAR EN CORRIDAS AUTÓNOMAS ████", explicando que es la válvula del Candado A (el único grant que 0040 omite), el ORDEN DE DEPENDENCIA crítico (aplicar DESPUÉS de 0041 — un grant antes de 0041 se pierde porque el drop+recreate de 0041 lo descarta), la regla NO-APLICAR (se commitea pero no se aplica ni se registra en schema_migrations durante Phase 41; apply = checkpoint humano post-sign-off), y el ORDEN DE ENCENDIDO documentado (firmar dossier → aplicar 0042 + fila schema_migrations → post-apply test → flip CRUCES_PUBLIC_ENABLED, los 4 juntos). Luego la precondición fail-loud `do $$ … if not exists (select 1 from pg_proc … 'fecha_captura' = any(p.proargnames)) then raise exception '0042 abortada: 0041 no está aplicada …' end if; end; $$;`. Finalmente la ÚNICA acción: `grant execute on function public.cruces_de_parlamentario(text) to anon;` (firma exacta `(text)`).

    NO crear ninguna fila en schema_migrations. NO aplicar. NO modificar `supabase/tests/0040_cruces_rpc.test.sql` (gate: el assert #3 es el guard; conditionalizarlo fue RECHAZADO por el validador — security regression).
  </action>
  <verify>
    <automated>grep -c "to anon" supabase/migrations/0042_cruces_grant_anon.sql (>=1) Y grep -c "raise exception" supabase/migrations/0042_cruces_grant_anon.sql (>=1) Y git diff --name-only supabase/tests/0040_cruces_rpc.test.sql (vacío — 0040 test sin cambios)</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0042_cruces_grant_anon.sql` existe; contiene exactamente UN `grant execute on function public.cruces_de_parlamentario(text) to anon;`.
    - Contiene el bloque `do $$ … raise exception …` que aborta si `fecha_captura` no está en `proargnames`.
    - La cabecera tiene el banner LOUD "NO APLICAR EN CORRIDAS AUTÓNOMAS".
    - `supabase/tests/0040_cruces_rpc.test.sql` NO fue modificado (git diff vacío para ese archivo).
    - 0042 NO está en `schema_migrations` ni se aplicó a PROD.
  </acceptance_criteria>
  <done>0042 escrita verbatim con guard fail-loud + grant único; 0040 test intacto; 0042 inerte (no aplicada).</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP post-apply (fuera del glob de la suite)</name>
  <read_first>
    - supabase/tests/0040_cruces_rpc.test.sql (el assert #3 que este test INVIERTE)
    - 41-RESEARCH.md §CRUCEN-02 líneas 261-275 (draft post-apply verbatim)
    - app/vitest.config.ts y la convención de la suite pgTAP (post-apply/ debe quedar fuera del glob)
  </read_first>
  <action>
    Crear el directorio `supabase/tests/post-apply/` y dentro `0042_cruces_grant_anon.test.sql` siguiendo VERBATIM el draft 41-RESEARCH (líneas 261-275): `begin; select plan(2);` → (1) `ok(has_function_privilege('anon','public.cruces_de_parlamentario(text)','execute'), 'anon TIENE EXECUTE … Candado A abierto')` (espejo INVERTIDO del 0040 assert #3); (2) `is((select p.prosecdef …), true, 'sigue security definer post-0042')`; `select * from finish(); rollback;`.

    Este test vive en `post-apply/` deliberadamente para que NINGÚN glob de suite lo recoja (asierta el estado OPUESTO al 0040 assert #3 → fallaría pre-encendido). El operador lo corre A MANO el día del encendido, tras aplicar 0042. Documentarlo en una cabecera corta del archivo.
  </action>
  <verify>
    <automated>test -f supabase/tests/post-apply/0042_cruces_grant_anon.test.sql && grep -c "has_function_privilege('anon'" supabase/tests/post-apply/0042_cruces_grant_anon.test.sql (>=1)</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/post-apply/0042_cruces_grant_anon.test.sql` existe con `plan(2)`: anon TIENE execute + sigue security definer.
    - El archivo está bajo `post-apply/`, NO en la raíz `supabase/tests/` (no lo recoge el glob de la suite ni el de vitest).
    - La cabecera declara "POST-APPLY ONLY — corre tras aplicar 0042".
  </acceptance_criteria>
  <done>post-apply/0042 escrito fuera del glob; asierta el estado de encendido (anon execute + security definer).</done>
</task>

</tasks>

<verification>
- `grep -v '^--' supabase/migrations/0042_cruces_grant_anon.sql | grep -c "to anon"` >= 1 (el grant existe en código).
- 0042 NO en `schema_migrations`; SUMMARY lo deja EXPLÍCITO.
- `supabase/tests/0040_cruces_rpc.test.sql` sin cambios (el guard de no-apply-prematuro intacto).
- `supabase/tests/post-apply/0042_cruces_grant_anon.test.sql` existe fuera del glob.
- `cd app && npx vitest run` sin regresión (0042 no toca frontend; sanity de que el repo sigue verde).
</verification>

<success_criteria>
- 0042 escrita verbatim del research: precondición fail-loud + único grant a anon + cabecera LOUD no-apply.
- 0042 NUNCA aplicada a PROD ni registrada en schema_migrations (gate 2).
- 0040 test SIN modificar (la variante condicional fue rechazada — gate 5 / Candado A).
- post-apply test fuera del glob, asierta el estado de encendido.
- SUMMARY incluye el lenguaje obligatorio de 41-RESEARCH §CRUCEN-02 ("0042 fue escrita y commiteada … NO fue aplicada … Apply = checkpoint humano post-sign-off").
</success_criteria>

<output>
Create `.planning/phases/41-crucen-habilitaci-n-de-cruces-grant-gated-dossier-fecha-capt/41-02-SUMMARY.md` when done. Debe dejar EXPLÍCITO que 0042 quedó SIN aplicar (no en schema_migrations).
</output>
