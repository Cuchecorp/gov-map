---
phase: 130-votos-real-b-01-el-numero-falso-muere
plan: 03
type: execute
wave: 3
depends_on: ["130-01", "130-02"]
files_modified:
  - .planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "El HTML realmente renderizado de /parlamentario/D1165 contra la DB de PROD dice 3.752 (o 3752) y jamas 1000"
    - "Un segundo sujeto de la clase afectada (D1170 = 3.773) confirma que no es idiosincratico del testigo"
    - "El numero mostrado coincide byte-a-byte con el recalculo por psql -tA, jamas por REST"
    - "La deuda residual (totalProyectos y chart siguen capados) queda REGISTRADA por escrito para el audit de v13"
  artifacts:
    - path: ".planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md"
      provides: "Evidencia E2E verbatim + registro de deuda residual OQ-1"
      contains: "3752"
  key_links:
    - from: "HTML renderizado de /parlamentario/D1165"
      to: "public.votos_conteo_de_parlamentario('D1165')"
      via: "contarCarriles -> chip / Ver detalle (N)"
      pattern: "3\\.?752"
---

<objective>
Probar contra datos REALES de PROD que el numero visible murio como falsedad: el HTML efectivamente
renderizado de las fichas testigo muestra el conteo real y coincide con el recalculo SQL.

Purpose: el criterio 2 exige que el numero MOSTRADO iguale el recalculo SQL. Un test con mocks
prueba el cableado; solo un render contra la DB viva prueba el dato. Y OQ-1 exige dejar escrita la
deuda residual para que el audit de v13 no la re-descubra como hallazgo nuevo.

Output: evidencia E2E verbatim + deuda residual registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-CONTEXT.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-RESEARCH.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-01-SUMMARY.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-02-SUMMARY.md

<read_first>
- `.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-RESEARCH.md` §Comandos exactos de
  verificacion PROD (V-1, V-2, V-7) y §Medicion PROD C/D (cifras de control)
- `.planning/milestones/v12.0-MILESTONE-AUDIT.md` L39, L43-44 — el hallazgo B-01 original que esta
  fase cierra ("el DOM vivo dice `Ver detalle (1000)`")
</read_first>

<interfaces>
Sujetos de la clase afectada (medidos en PROD 2026-07-30):
  D1165 (Agustin Romero) = 3752  — el testigo del audit v12
  D1170 (Luis Sanchez)   = 3773  — el maximo de la clase
  D1012 (Boris Barrera)  = 3736  — tercero disponible
Clase afectada: 71 de 186 parlamentarios con votos confirmados (todos mostraban 1000).

Preambulo psql LOCKED (nunca ecoar la URL, siempre `| tr -d '\r'`):
  DBURL=$(grep '^SUPABASE_DB_URL=' .env | sed 's/^SUPABASE_DB_URL=//' | tr -d '\r"' | sed 's/^\xef\xbb\xbf//')
  export PGCLIENTENCODING=UTF8
</interfaces>

<alcance>
Esta fase NO despliega a Cloudflare. El build OpenNext exige Docker Linux (en Windows produce un
worker roto 500) y el deploy del milestone v13.0 es un evento posterior, compartido con las demas
fases. La verificacion aqui es contra el render REAL del servidor Next.js local leyendo la DB de
PROD por service_role — mismo codigo, mismos datos, misma RPC; lo unico que falta es el runtime de
Cloudflare, que no participa del calculo del numero. El SUMMARY debe declarar esto explicitamente
para que el audit sepa que la confirmacion sobre el deploy vivo ocurre en el deploy del milestone.
</alcance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render real contra PROD de las fichas testigo y cotejo con psql</name>
  <files>.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md</files>
  <action>
Levantar el servidor Next.js en background (`pnpm --filter ./app dev`, puerto 3000 por defecto) con
el entorno que ya usa el sitio (service_role, Camino A). Esperar a que responda antes de medir.

Para D1165 y D1170:
1. Descargar el HTML de `/parlamentario/{id}` a un archivo en el scratchpad (nunca a `app/`).
2. **Control positivo de que hubo HTML real** ANTES de cualquier assert de contenido: tamano del
   archivo > 10 KB y presencia del titulo del carril "Votaciones". El gotcha v12 documenta que un
   cliente puede salir 0 tras un timeout y dejar salida vacia — un `grep` sobre un archivo vacio da
   0 coincidencias y se leeria como "el 1000 desaparecio", un falso verde perfecto. Sin control
   positivo, la medicion NO vale.
3. Extraer el numero del chip / `Ver detalle (...)`. Cuidado (gotcha v12): React intercala
   `<!-- -->` entre texto y expresion, asi que hay que buscar el numero tolerando esa insercion, y
   el separador de miles es-CL puede formatearlo como `3.752`. Extraer con un patron que acepte
   ambas formas (`3752` y `3.752`) y normalizar quitando los puntos antes de comparar.
4. Recalcular por `psql -tA` (V-1 del research) y comparar los enteros normalizados.
5. Assert negativo apareado: el HTML NO contiene el `Ver detalle (1000)` (ni su forma con el
   separador). Este assert solo es valido acompanado del control positivo del paso 2.

Registrar en el SUMMARY, verbatim: el numero extraido del HTML, el numero de psql, y el resultado
de la comparacion, para ambos sujetos. Y un tercer registro: el desglose "Como voto" que aparece en
el HTML de D1165 cotejado con V-2 (si 1764 / no 1772 / abstencion 171 / pareo 16 / ausente 29) —
esa es la prueba de que la COMPOSICION dejo de estar distorsionada, no solo el total.

Auto-ratificacion autorizada: cerrar sin checkpoint humano, dejando la evidencia verbatim.
Detener el servidor al terminar.
  </action>
  <verify>
    <automated>
DBURL=$(grep '^SUPABASE_DB_URL=' .env | sed 's/^SUPABASE_DB_URL=//' | tr -d '\r"' | sed 's/^\xef\xbb\xbf//')
export PGCLIENTENCODING=UTF8
SP="${TMPDIR:-/tmp}/gsd-130"; mkdir -p "$SP"   # fix W-8: TMPDIR no garantizado en Git Bash

for ID in D1165 D1170; do
  curl -s "http://localhost:3000/parlamentario/$ID" > "$SP/$ID.html"
  # (a) CONTROL POSITIVO — sin esto, cualquier assert negativo es vacuo
  SIZE=$(wc -c < "$SP/$ID.html"); echo "$ID bytes=$SIZE"          # EXIGIDO: > 10000
  grep -c "Votaciones" "$SP/$ID.html"                              # EXIGIDO: >= 1
  # (b) numero mostrado — tolera <!-- --> DENTRO del parentesis y miles es-CL:
  #     capturar todo el contenido del parentesis y quedarse solo con los digitos
  HTMLN=$(tr -d '\n' < "$SP/$ID.html" | grep -o "Ver detalle ([^)]*)" | head -1 | sed 's/[^0-9]//g')
  # (c) recalculo SQL (V-1) EN LA MISMA CORRIDA — el esperado se deriva vivo, jamas horneado
  SQLN=$(psql "$DBURL" -tA -c "select count(*) from voto v join votacion vo on vo.id=v.votacion_id where v.parlamentario_id='$ID' and v.estado_vinculo='confirmado';" | tr -d '\r')
  echo "$ID html=$HTMLN sql=$SQLN"
  [ -n "$HTMLN" ] && [ "$HTMLN" = "$SQLN" ] && [ "$SQLN" -gt 1000 ] && echo "$ID PARIDAD OK" || echo "$ID PARIDAD FAIL"   # EXIGIDO: OK
  # (d) assert negativo apareado (solo valido junto al control (a))
  grep -c "Ver detalle (1000)\|Ver detalle (1.000)" "$SP/$ID.html" # EXIGIDO: 0
done

# (e) COMPOSICION no distorsionada (fix B-2 checker) — anclada al bloque "Como voto" del HTML y
#     comparada por IGUALDAD contra el group by de psql corrido EN ESTE MISMO instante.
#     Jamas digitos sueltos sobre el documento (16/29 matchean cualquier HTML).
#     1) psql: mapa seleccion->n vivo
psql "$DBURL" -tA -c "select v.seleccion||'='||count(*) from voto v join votacion vo on vo.id=v.votacion_id where v.parlamentario_id='D1165' and v.estado_vinculo='confirmado' group by v.seleccion order by 1;" | tr -d '\r' > "$SP/sql-desglose.txt"
cat "$SP/sql-desglose.txt"
#     2) HTML: extraer el fragmento del bloque de desglose (anclado por el heading del carril,
#        p.ej. "Como voto" / los labels si/no/abstencion/pareo/ausente con su numero adyacente,
#        tolerando <!-- --> con [^0-9<]*), parsear los 5 enteros normalizados (sin puntos)
#     3) comparar 1:1 contra el mapa de psql: los 5 pares deben coincidir. Emitir
#        "COMPOSICION OK" / "COMPOSICION FAIL linea-por-linea".                # EXIGIDO: OK
#     El ejecutor implementa la extraccion (node inline o grep -o anclado) y pega en el SUMMARY
#     los 5 pares HTML vs los 5 pares SQL, verbatim.
# (f) fix B-1 checker (ex human-check, ahora asserts): los rotulos de alcance EXISTEN en el HTML:
tr -d '\n' < "$SP/D1165.html" | grep -oc "de los [0-9.]* cargados\|sobre los [0-9.]* mas recientes\|N de M"   # EXIGIDO: >= 1 (rotulo listado/proyectos/chart; usar el texto REAL que Plan 02 implanto — ajustar el patron al copy definitivo y dejarlo registrado)
    </automated>
  </verify>
  <done>
Para D1165 y D1170: control positivo de HTML real superado, numero del HTML == numero de psql,
ambos > 1000, cero `Ver detalle (1000)`. El desglose real de D1165 aparece en el HTML y coincide
con V-2. Evidencia verbatim en el SUMMARY. Servidor detenido.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registrar la deuda residual (OQ-1) y cerrar la fase</name>
  <files>.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md</files>
  <action>
Escribir en el SUMMARY una seccion `## Deuda residual declarada (OQ-1)` — el objetivo es que el
audit de v13 la lea como decision consciente, no la descubra como hallazgo nuevo. Debe contener,
con cifras medidas:
- `totalProyectos` (`votos-por-parlamentario.tsx` ~L954) sigue derivando de las 1.000 filas
  cargadas: para D1165 muestra 191 donde el registro tiene 555. Mitigacion aplicada: rotulo que
  declara el alcance (Plan 02 Task 2). Fix real pendiente: RPC de conteo de proyectos distintos.
- La serie del chart "Cuando voto" (~L959) cubre solo el tramo cargado — para D1165, 2025-05 ->
  2026-07 de un registro que arranca en 2022-03. Mitigacion: rotulo. Fix real pendiente: agregado
  por trimestre en SQL.
- El listado de detalle sigue capado a 1.000 filas (paginacion real >1000 esta DEFERIDA en
  CONTEXT.md, fuera de DEBT-01). Mitigacion: declaracion "N de M".
- Por que NO se amplio el alcance: DEBT-01 nombra "el conteo REAL de votos … con composicion no
  distorsionada"; "composicion" es inequivocamente el desglose por `seleccion`, que si quedo
  corregido. Ampliar habria roto la paralelizacion acotada de la fase.
- Declaracion explicita de que NINGUN numero visible deriva ya de un `length` capado sin rotulo, y
  de que en ningun momento se uso un clamp como fix (`p_limit` quedo en 1000, sin tocar).

Anadir tambien una seccion de alcance de verificacion: la confirmacion sobre el deploy vivo de
Cloudflare ocurre en el deploy del milestone v13.0 (esta fase verifico contra el render real con
datos de PROD, sin el runtime de Workers — ver `<alcance>`).

Cerrar con el mapeo de los 4 criterios de exito de ROADMAP §Phase 130 a su evidencia concreta
(migracion+pgTAP, HTML vs psql, desglose V-2, test centinela), cada uno con el comando que lo
prueba.
  </action>
  <verify>
    <automated>
S=.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md
grep -c "Deuda residual declarada" "$S"        # EXIGIDO: 1
# fix W-3: las cifras del SUMMARY son las MEDIDAS en la corrida (pueden derivar de 3752/191/555
# por backfill legitimo) — se asserta que la seccion tiene cifras, no constantes horneadas:
grep -Ec "totalProyectos|chart|listado" "$S"   # EXIGIDO: >= 3 (los tres items de deuda presentes)
grep -c "OQ-1" "$S"                            # EXIGIDO: >=1
# Control final anti-clamp sobre TODO el diff de la fase (fix W-8: acotado a p_limit con valor,
# excluyendo el 1000 legitimo re-indentado)
git diff $(git merge-base HEAD master)..HEAD -- app/ | grep "^[+-].*p_limit:" | grep -vc "p_limit: 1000"   # EXIGIDO: 0
    </automated>
  </verify>
  <done>
El SUMMARY declara la deuda residual con cifras, la razon de no ampliar alcance, el alcance de la
verificacion y el mapeo criterio->evidencia. El diff completo de la fase no toca ningun `p_limit`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor local de verificacion -> Postgres PROD | lectura con service_role sobre datos reales |
| salida de comandos -> logs/commits | `SUPABASE_DB_URL` jamas debe cruzar esta frontera |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-12 | Information Disclosure | credenciales en logs/SUMMARY | mitigate | `$DBURL` leida a variable y nunca ecoada; el SUMMARY registra cifras, jamas la URL. Los HTML descargados van al scratchpad, nunca al repo. |
| T-130-13 | Repudiation (falso verde) | verificacion que "pasa" sobre salida vacia | mitigate | Control positivo obligatorio (bytes > 10 KB + presencia de "Votaciones") ANTES de cualquier assert negativo; gotcha v12 "control de ausencia exige control positivo apareado". |
| T-130-14 | Tampering | verificar por REST en vez de psql | mitigate | Prohibido REST: PostgREST capa a 1.000 filas y mentiria exactamente en el rango de la clase afectada. Todo recalculo por `psql -tA \| tr -d '\r'`. |
| T-130-15 | Information Disclosure | servidor de verificacion expuesto | accept | `next dev` en localhost, efimero, detenido al terminar la tarea; sin exposicion de red. |
| T-130-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas en esta fase. |
</threat_model>

<verification>
- Ambos sujetos: control positivo de HTML real superado antes de cualquier assert negativo.
- `html == sql` y ambos > 1000 para D1165 (3752) y D1170 (3773).
- Desglose de D1165 presente en el HTML y coincidente con V-2.
- `git diff` de la fase sin una sola linea que toque `p_limit` (criterio 4, anti-clamp).
- `$DBURL` no aparece en ningun archivo ni salida commiteada.
</verification>

<success_criteria>
- El numero mostrado en las fichas testigo iguala el recalculo por psql, jamas por REST.
- `Ver detalle (1000)` no existe en el HTML de ninguna ficha de la clase afectada verificada.
- La composicion mostrada es la real (1764/1772/171/16/29 para D1165).
- La deuda residual de OQ-1 queda registrada por escrito con cifras y mitigacion.
</success_criteria>

<output>
Create `.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-03-SUMMARY.md` when done
</output>
