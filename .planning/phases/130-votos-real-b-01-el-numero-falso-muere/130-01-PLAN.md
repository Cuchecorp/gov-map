---
phase: 130-votos-real-b-01-el-numero-falso-muere
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0082_votos_conteo_de_parlamentario.sql
  - supabase/tests/0082_votos_conteo_de_parlamentario.test.sql
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "La RPC public.votos_conteo_de_parlamentario(text) existe en PROD y devuelve el desglose por seleccion sobre el universo COMPLETO (D1165 suma 3752, no 1000)"
    - "La RPC es secdef con search_path='' y statement_timeout='5s', y anon/authenticated NO pueden ejecutarla"
    - "La suma de la RPC coincide con count(*) del universo del listado para los 186 parlamentarios con votos confirmados (cero divergencias)"
    - "Las cotas de 0077/0078/0079 siguen verdes tras aplicar (cero regresion de regimen)"
  artifacts:
    - path: "supabase/migrations/0082_votos_conteo_de_parlamentario.sql"
      provides: "RPC agregada con aguja completa (molde 0068 L48-85)"
      contains: "create or replace function public.votos_conteo_de_parlamentario"
    - path: "supabase/tests/0082_votos_conteo_de_parlamentario.test.sql"
      provides: "pgTAP contra schema aplicado: secdef, proconfig, doble-revoke, shape, paridad"
      contains: "select plan("
  key_links:
    - from: "supabase/migrations/0082_votos_conteo_de_parlamentario.sql"
      to: "public.voto / public.votacion"
      via: "join schema-qualified bajo search_path=''"
      pattern: "from public\\.voto v"
    - from: "supabase/tests/0082_votos_conteo_de_parlamentario.test.sql"
      to: "public.votos_conteo_de_parlamentario(text)"
      via: "has_function / prosecdef por ::regprocedure"
      pattern: "::regprocedure"
---

<objective>
Crear y APLICAR a PROD la RPC agregada `public.votos_conteo_de_parlamentario(p_id text)` que
devuelve `(seleccion text, n bigint)` sobre el universo COMPLETO de votos confirmados, con la
aguja completa del regimen (molde 0068 verbatim), mas su pgTAP contra el schema aplicado.

Purpose: B-01 es un bug de UNIVERSO, no de calculo. El chip, la capa-1 y `VotosSection` derivan
todo de las 1.000 filas que devuelve `votos_de_parlamentario`, cuyo `order by fecha desc` recorta
al ultimo anio. Esta RPC es la unica fuente numerica honesta. `votos_de_parlamentario` queda
INTACTA (D-03: firma viva, 42P13).

Output: migracion 0082 aplicada a PROD + pgTAP verde + evidencia verbatim de paridad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-CONTEXT.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-RESEARCH.md

<read_first>
- `supabase/migrations/0068_coincidencia_votos_par.sql` L36-85 — **el molde a copiar VERBATIM**
  (drop-first, secdef, search_path='', statement_timeout, limit, doble-revoke)
- `supabase/tests/0068_coincidencia_votos_par.test.sql` — **el molde pgTAP a espejar** (idiom
  `::regprocedure` para `prosecdef`; nunca `proname` a secas)
- `supabase/migrations/0078_cotas_duras_parametro.sql` L178-209 — definicion VIVA de
  `votos_de_parlamentario` (el universo a espejar) + L327-341 (patron de post-check de `proconfig`)
</read_first>

<interfaces>
Universo canonico que la RPC nueva DEBE espejar (verbatim de 0078 L191-205, sin los left join a
`proyecto`/`proyecto_ficha`, que estan medidos como no-fan-out y se OMITEN por robustez):

  from public.voto v
  join public.votacion vo on vo.id = v.votacion_id
  where v.parlamentario_id = p_id and v.estado_vinculo = 'confirmado'
  group by v.seleccion

Shape de retorno LOCKED (cero identidad, cero fila individual — PII-safe por construccion):
  returns table (seleccion text, n bigint)

Dominio cerrado de `seleccion` (orden LOCKED en la UI): si, no, abstencion, pareo, ausente.

Cifras de control medidas en PROD 2026-07-30 (`psql -tA`, jamas REST):
  D1165 total = 3752 · si 1764 · no 1772 · abstencion 171 · pareo 16 · ausente 29
  D1170 total = 3773 · D1012 total = 3736
  voto confirmado total = 283550 · parlamentarios con votos = 186 · con >1000 = 71
</interfaces>

<preambulo_psql>
LOCKED en todo comando contra PROD. **JAMAS ecoar `$DBURL`** en logs, commits ni salidas.
Toda salida pasa por `| tr -d '\r'` (psql en Windows emite CRLF — gotcha v12).

DBURL=$(grep '^SUPABASE_DB_URL=' .env | sed 's/^SUPABASE_DB_URL=//' | tr -d '\r"' | sed 's/^\xef\xbb\xbf//')
export PGCLIENTENCODING=UTF8
</preambulo_psql>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migracion 0082 y su pgTAP (molde 0068 verbatim)</name>
  <files>supabase/migrations/0082_votos_conteo_de_parlamentario.sql, supabase/tests/0082_votos_conteo_de_parlamentario.test.sql</files>
  <action>
PRIMERO confirmar la numeracion (D-07): `ls supabase/migrations | tail -3`. Se espera que existan
`0080_actualidad_evidencia.sql` (Phase 127, LOCKED) y `0081_actualidad_evidencia_fix.sql` (fix
post-review de 127 — 0081 esta TOMADA) y que 0082 este libre. Si 0082 ya existiera
(otra fase aterrizo antes), usar el siguiente numero libre y ajustar TODOS los nombres de archivo
y referencias de este plan en consecuencia; jamas rellenar huecos ni reusar un numero ajeno.

Crear `supabase/migrations/0082_votos_conteo_de_parlamentario.sql` copiando la estructura de
`0068_coincidencia_votos_par.sql` L48-85 elemento por elemento. Header de comentarios que declare:
(a) ADITIVA — no toca `votos_de_parlamentario` (firma viva, 42P13, D-03); (b) ACL Camino A
post-0044 = CERO grant, doble-revoke explicito al final, jamas re-emitir grant; (c) que los
`left join proyecto` / `left join proyecto_ficha` de la RPC vieja se OMITEN porque se midio en PROD
el 2026-07-30 que no cambian la cardinalidad (cero boletines duplicados en ambas tablas) y su
inclusion crearia una dependencia de que esa unicidad se mantenga.

Cuerpo, en este orden exacto:
1. `drop function if exists public.votos_conteo_de_parlamentario(text);` — idiom anti-42P13 (0068 L48).
2. `create or replace function public.votos_conteo_de_parlamentario(p_id text)`
   `returns table (seleccion text, n bigint)`
   `language sql stable security definer`
   `set search_path = ''`
   `set statement_timeout = '5s'`
   Las tres clausulas `set`/`security definer` son LOCKED: un `create or replace` que omita
   `set statement_timeout` lo BORRA en silencio (riesgo Nº1 de 0078 L93-96).
3. Cuerpo con nombres SCHEMA-QUALIFIED obligatorios (`public.voto`, `public.votacion`) — con
   `search_path=''` un `from voto` pelado falla en RUNTIME, no al crear (Pitfall 3). Usar el SQL
   de `<interfaces>` verbatim, con `count(*) as n`, cerrando con `limit 1000;` (piso LOCKED del
   regimen; sobre 5 filas agrupadas es trivialmente holgado).
   `p_id` va SIEMPRE como parametro tipado — cero `format()`, cero concatenacion (T-51-05).
4. Doble-revoke en el MISMO archivo (0068 L84-85), ambas lineas:
   `revoke all on function public.votos_conteo_de_parlamentario(text) from public;`
   `revoke all on function public.votos_conteo_de_parlamentario(text) from anon, authenticated;`
5. Post-check dentro de la propia migracion siguiendo el patron de 0078 L327-341: un bloque
   `do $$ ... raise exception ... $$` que verifique contra `pg_proc` (via `::regprocedure`) que
   `prosecdef` es true y que `proconfig` contiene tanto `search_path=` como `statement_timeout=5s`.
   Asi la transaccion aborta sola si una clausula se perdio (Pitfall 4).

Crear `supabase/tests/0082_votos_conteo_de_parlamentario.test.sql` espejando
`supabase/tests/0068_coincidencia_votos_par.test.sql`: envoltura `begin; select plan(N); ...
select * from finish(); rollback;` con N igual al numero real de asserts escritos. Asserts
obligatorios (minimo 8):
  1. `has_function('public','votos_conteo_de_parlamentario', ARRAY['text'])`
  2. `prosecdef = true`, leido de `pg_proc` scopeado por
     `oid = 'public.votos_conteo_de_parlamentario(text)'::regprocedure` (WR-05: `proname` a secas
     rompe con overloads)
  3. `has_function_privilege('anon','public.votos_conteo_de_parlamentario(text)','execute') = false`
  4. idem para `authenticated` (2º leg del doble-revoke)
  5. `proconfig` contiene `search_path=`
  6. `proconfig` contiene `statement_timeout=5s`
  7. shape del `returns table`: exactamente las columnas `seleccion` y `n` — assert explicito de
     que NO existe ninguna columna de identidad (`rut`, `nombre`, `parlamentario_id`, `partido`)
     en la salida. Este es el gate LEGAL-03/PII.
  8. **Paridad (SC3, control fuerte apareado):** para el testigo `'D1165'`,
     `(select sum(n) from public.votos_conteo_de_parlamentario('D1165'))` es igual a
     `(select count(*) from public.voto v join public.votacion vo on vo.id=v.votacion_id
       where v.parlamentario_id='D1165' and v.estado_vinculo='confirmado')`
     Y ese valor es `> 1000` — el segundo assert es el CONTROL POSITIVO que impide que la paridad
     pase de forma vacua si el testigo quedara sin filas (gotcha v12: "control de ausencia exige
     control positivo apareado").
  9. **Equivalencia con-left-join vs sin-left-join** para el testigo: el `count(*)` del universo
     agregando `left join public.proyecto pr on pr.boletin = vo.boletin` y
     `left join public.proyecto_ficha pf on pf.boletin = vo.boletin` da el MISMO numero que sin
     ellos. Esto es el centinela que exige el research: si maniana `proyecto` admite boletines
     duplicados, listado y conteo divergirian en silencio y este assert se pone rojo.
NO aplicar nada todavia: esta tarea solo escribe archivos.
  </action>
  <verify>
    <automated>
grep -c "security definer" supabase/migrations/0082_votos_conteo_de_parlamentario.sql
# Conteos insensibles a comentarios: se stripean las lineas que empiezan por `--` antes de contar.
S=$(grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -c "set search_path = ''")
T=$(grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -c "set statement_timeout = '5s'")
R=$(grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -o "revoke all on function" | wc -l)
D=$(grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -c "drop function if exists public.votos_conteo_de_parlamentario(text)")
Q=$(grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -o "public\.\(voto\|votacion\)\b" | wc -l)
echo "search_path=$S timeout=$T revokes=$R drop=$D qualified=$Q"
# EXIGIDO: S>=1, T>=1, R==2, D==1, Q>=2
# Control negativo apareado: cero referencias sin calificar (romperia en runtime con search_path='')
grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -E "from voto|join votacion" | wc -l   # EXIGIDO: 0
# Control de no-regresion D-03: la migracion NO menciona la firma viva
grep -c "votos_de_parlamentario" supabase/migrations/0082_votos_conteo_de_parlamentario.sql   # EXIGIDO: 0 fuera de comentarios
grep -v '^\s*--' supabase/migrations/0082_votos_conteo_de_parlamentario.sql | grep -c "votos_de_parlamentario"   # EXIGIDO: 0
    </automated>
  </verify>
  <done>
Ambos archivos existen. La migracion tiene drop-first, `security definer`, `set search_path=''`,
`set statement_timeout='5s'`, `limit 1000`, doble-revoke (2 lineas) y post-check de `proconfig`;
cero referencia sin calificar; cero mencion a `votos_de_parlamentario` en codigo ejecutable. El
pgTAP tiene >=8 asserts incluyendo paridad con control positivo y equivalencia de left-join.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aplicar 0082 a PROD y verificar (pgTAP + V-3..V-6 + no-regresion)</name>
  <files>supabase/migrations/0082_votos_conteo_de_parlamentario.sql, supabase/tests/0082_votos_conteo_de_parlamentario.test.sql</files>
  <action>
Auto-ratificacion autorizada (fase autonoma; precedente 127-03 Task 3): aplicar a PROD sin
checkpoint humano, PERO dejando la evidencia verbatim de cada comando en el SUMMARY.

PRE-APLICACION — verificar el supuesto A3 del research (indice sobre `voto.parlamentario_id`).
Si NO existiera indice, el `group by` seria un seq scan sobre 283.550 filas por render y el
`statement_timeout=5s` lo cortaria: en ese caso DETENERSE y reportar antes de aplicar (no crear
indices por iniciativa propia — es cambio de regimen no adjudicado).

Aplicar con `psql "$DBURL" --single-transaction -f supabase/migrations/0082_votos_conteo_de_parlamentario.sql`.
NUNCA `supabase db push` (drift de `schema_migrations` — precedente 0060/0061/0067/0068).

Post-aplicacion, correr en este orden y capturar la salida verbatim:
- El pgTAP nuevo (debe salir todo `ok`, cero `not ok`).
- **No-regresion del regimen:** los tres pgTAP post-apply existentes
  (`supabase/tests/post-apply/0077_statement_timeout_rpcs_no_acotadas.test.sql` — debe seguir en
  20/20 —, `0078_cotas_duras_parametro.test.sql`, `0079_limit_explicito_rpcs.test.sql`).
- V-3 (paridad del testigo), V-4 (cero divergencias sobre los 186 — cero FUERTE, ver abajo),
  V-5 (ACL `f|f|t`), V-6 (`proconfig`), tal como estan escritos en 130-RESEARCH.md §Comandos
  exactos de verificacion PROD.
- **Control de no-regresion de D-03:** confirmar que `votos_de_parlamentario` sigue viva e
  intacta comparando su `pg_get_functiondef` con lo transcrito en 0078 L184-206 (misma firma,
  mismo `returns table`, mismo `statement_timeout`).

V-4 exige un cero FUERTE, no vacuo: junto al conteo de divergencias hay que emitir el denominador
(`select count(*)` de parlamentarios con >=1 voto confirmado, esperado **186**). Un `0` sin
denominador podria venir de una lista vacia y no probaria nada.

Registrar en el SUMMARY, verbatim: total del testigo (3752), su desglose (1764/1772/171/16/29),
el `f|f|t` del ACL, el `t|{search_path=,statement_timeout=5s}` del proconfig, y el par
divergencias=0 / sujetos=186.
  </action>
  <verify>
    <automated>
DBURL=$(grep '^SUPABASE_DB_URL=' .env | sed 's/^SUPABASE_DB_URL=//' | tr -d '\r"' | sed 's/^\xef\xbb\xbf//')
export PGCLIENTENCODING=UTF8

# 1. pgTAP nuevo: cero fallos y al menos 8 asserts corridos (no un exit-0 vacuo)
OUT=$(psql "$DBURL" -tA -f supabase/tests/0082_votos_conteo_de_parlamentario.test.sql | tr -d '\r')
echo "$OUT" | grep -c "^not ok"    # EXIGIDO: 0
echo "$OUT" | grep -c "^ok "       # EXIGIDO: >=8  (control positivo: el archivo SI corrio)

# 2. No-regresion de las cotas de regimen
psql "$DBURL" -tA -f supabase/tests/post-apply/0077_statement_timeout_rpcs_no_acotadas.test.sql | tr -d '\r' | grep -c "^not ok"   # 0
psql "$DBURL" -tA -f supabase/tests/post-apply/0078_cotas_duras_parametro.test.sql | tr -d '\r' | grep -c "^not ok"                # 0
psql "$DBURL" -tA -f supabase/tests/post-apply/0079_limit_explicito_rpcs.test.sql | tr -d '\r' | grep -c "^not ok"                 # 0

# 3. V-3 paridad del testigo
psql "$DBURL" -tA -c "select sum(n) from public.votos_conteo_de_parlamentario('D1165');" | tr -d '\r'   # EXIGIDO: 3752
psql "$DBURL" -tA -c "select seleccion, n from public.votos_conteo_de_parlamentario('D1165') order by 1;" | tr -d '\r'
# EXIGIDO verbatim: abstencion|171 / ausente|29 / no|1772 / pareo|16 / si|1764

# 4. V-4 cero FUERTE con denominador apareado
psql "$DBURL" -tA -c "
with real as (select v.parlamentario_id pid, count(*) n from voto v join votacion vo on vo.id=v.votacion_id where v.estado_vinculo='confirmado' group by 1)
select (select count(*) from real) as sujetos,
       (select count(*) from real r where r.n <> (select coalesce(sum(n),0) from public.votos_conteo_de_parlamentario(r.pid))) as divergencias;" | tr -d '\r'
# EXIGIDO: 186|0   (sujetos>0 prueba que el cero no es vacuo)

# 5. V-5 ACL + V-6 proconfig
psql "$DBURL" -tA -c "select has_function_privilege('anon','public.votos_conteo_de_parlamentario(text)','execute'), has_function_privilege('authenticated','public.votos_conteo_de_parlamentario(text)','execute'), has_function_privilege('service_role','public.votos_conteo_de_parlamentario(text)','execute');" | tr -d '\r'   # EXIGIDO: f|f|t
psql "$DBURL" -tA -c "select prosecdef, proconfig from pg_proc where oid='public.votos_conteo_de_parlamentario(text)'::regprocedure;" | tr -d '\r'   # EXIGIDO: t|{search_path=,statement_timeout=5s}

# 6. D-03: la firma viva sigue intacta
psql "$DBURL" -tA -c "select count(*) from pg_proc where oid='public.votos_de_parlamentario(text,integer,integer)'::regprocedure;" | tr -d '\r'   # EXIGIDO: 1
    </automated>
  </verify>
  <done>
0082 aplicada a PROD en una sola transaccion. pgTAP nuevo verde con >=8 asserts corridos; los tres
pgTAP post-apply de regimen sin fallos. `sum(n)` del testigo == 3752 con el desglose exacto;
divergencias=0 sobre sujetos=186; ACL `f|f|t`; proconfig `t|{search_path=,statement_timeout=5s}`;
`votos_de_parlamentario` intacta. Evidencia verbatim en el SUMMARY. `$DBURL` jamas ecoada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> Next.js (server) | Solo `p_id` desde la ruta; nunca alcanza la DB sin parametrizar |
| Next.js (service_role) -> Postgres | service_role BYPASSA ACL/RLS: la barrera real es el allowlist del lockdown-guard (Plan 02), no el grant |
| anon/authenticated (REST) -> Postgres | superficie publica; debe quedar CERRADA para esta RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Tampering | `p_id` de `votos_conteo_de_parlamentario` | mitigate | Parametro tipado `text` en funcion SQL; cero `format()`/concatenacion (T-51-05). Assert de ausencia de `format(` en la migracion. |
| T-130-02 | Elevation of Privilege | `security definer` sin `search_path` | mitigate | `set search_path = ''` + TODOS los nombres schema-qualified (`public.voto`, `public.votacion`); post-check de `proconfig` dentro de la propia migracion aborta la transaccion si falta. |
| T-130-03 | Information Disclosure | superficie REST re-abierta por DEFAULT PRIVILEGES sobre funciones nuevas de `public` | mitigate | Doble-revoke (`from public` + `from anon, authenticated`) en el MISMO archivo; verificado por V-5 (`f|f|t`) y por dos asserts pgTAP. |
| T-130-04 | Information Disclosure (PII) | shape del `returns table` | mitigate | Retorno = `(seleccion text, n bigint)`: cero identidad, cero rut/nombre/partido, cero fila individual. Estrictamente MENOS expuesto que `votos_de_parlamentario`. Assert pgTAP explicito de ausencia de columnas de identidad. Gate LEGAL-03 satisfecho por construccion. |
| T-130-05 | Denial of Service | `group by` sobre 283.550 filas de `public.voto` | mitigate | `set statement_timeout='5s'` + `limit 1000`; verificacion pre-apply del indice sobre `voto.parlamentario_id` (A3) con parada si no existe. |
| T-130-06 | Tampering | `create or replace` que borra clausulas `set` en silencio (0078 L93-96) | mitigate | Post-check de `proconfig` en la migracion + re-corrida de los pgTAP post-apply 0077/0078/0079. |
| T-130-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala ningun paquete (research §Standard Stack: "cero dependencias nuevas"). Sin superficie de supply-chain; sin checkpoint requerido. |
</threat_model>

<verification>
- `ls supabase/migrations | tail -3` confirma que 0082 es el numero libre siguiente a 0080 (127).
- pgTAP nuevo verde CON conteo de asserts > 0 (nunca aceptar un exit-0 sin `ok` — gotcha v12).
- Los tres pgTAP post-apply de regimen siguen sin `not ok`.
- V-4 devuelve el par `186|0`: cero fuerte con denominador, no vacuo.
- Toda salida de psql pasa por `| tr -d '\r'`; `$DBURL` jamas aparece en logs ni commits.
</verification>

<success_criteria>
- `public.votos_conteo_de_parlamentario(text)` vive en PROD con la aguja completa: secdef,
  `search_path=''`, `statement_timeout='5s'`, `limit 1000`, doble-revoke.
- `sum(n)` para D1165 == 3752 y el desglose es 1764/1772/171/16/29.
- Paridad con el universo del listado sobre los 186 sujetos: 0 divergencias.
- `votos_de_parlamentario` sin tocar (D-03) y cotas 0077/0078/0079 verdes.
</success_criteria>

<output>
Create `.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-01-SUMMARY.md` when done
</output>
