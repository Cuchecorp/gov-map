---
phase: 130-votos-real-b-01-el-numero-falso-muere
plan: 02
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - app/lib/types.ts
  - app/lib/parlamentario-resumen-conteos.ts
  - app/lib/lockdown-guard.test.ts
  - app/app/parlamentario/[id]/page.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/lib/parlamentario-resumen-conteos.test.ts
  - app/app/parlamentario/[id]/page.test.tsx
  - app/components/votos-por-parlamentario.test.tsx
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "El chip 'Votaciones', el 'Ver detalle (N)' y la capa-1 muestran el conteo de la RPC agregada, no el length de las filas capadas"
    - "La seccion 'Como voto' muestra el desglose global real cuando no hay tema activo, y el del subconjunto filtrado cuando hay ?materia= (WR-01 preservado)"
    - "Existe un test que MUERDE si el cap vuelve a gobernar el numero visible: con conteo=3752 y listado=3 filas, la UI muestra el render real `Ver detalle (3752)` — SIN separador de miles: conteoLabel es String(n), page.tsx L89-99 (Fable M1) — y jamas 3"
    - "Todo numero que sigue derivando de las 1.000 filas capadas lleva rotulo honesto que declara su alcance"
    - "El lockdown-guard pasa: la entrada del allowlist tiene funcion en migraciones (A2) y todo .rpc() esta allowlisted (A5)"
  artifacts:
    - path: "app/lib/parlamentario-resumen-conteos.ts"
      provides: "contarCarriles leyendo votos_conteo_de_parlamentario; agregador filas->VotosBreakdown"
      contains: "votos_conteo_de_parlamentario"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "entrada en PUBLIC_RPC_ALLOWLIST"
      contains: "votos_conteo_de_parlamentario"
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "conteos globales por prop + rotulos honestos sobre lo capado"
      contains: "conteosGlobales"
  key_links:
    - from: "app/lib/parlamentario-resumen-conteos.ts"
      to: "public.votos_conteo_de_parlamentario"
      via: "sb.rpc con literal string (el guard A5 no resuelve indirecciones)"
      pattern: "\\.rpc\\(\\s*\"votos_conteo_de_parlamentario\""
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "app/components/votos-por-parlamentario.tsx"
      via: "prop conteosGlobales desde contarCarrilesSeguro"
      pattern: "conteosGlobales="
---

<objective>
Hacer que TODO numero visible de votaciones salga de la RPC agregada de 0082, en un solo commit
para chip + capa-1 + `Ver detalle (N)` + seccion "Como voto" (D-04, cero desincronizacion), con
rotulo honesto sobre lo que sigue capado y un test que muerde si el cap vuelve.

Purpose: el `.length` de 1.000 filas es el numero falso. El fix estructural es que
`contarCarriles` (unico productor, React.cache) lea el agregado y que `VotosSection` RECIBA los
conteos en vez de recalcularlos.

Output: chip/capa-1/seccion reales, allowlist en sync, suite verde, guard A2+A5 verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-CONTEXT.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-RESEARCH.md
@.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-01-SUMMARY.md

<read_first>
- `app/lib/parlamentario-resumen-conteos.ts` L99-112 (`resumirVotos`) y L262-306 (`contarCarriles`,
  el `sb.rpc` raiz + `.length` + `resumirVotos` + `asistencia`) y L455-465 (fallback seguro)
- `app/components/votos-por-parlamentario.tsx` L130-160 (tipos de la view data), L591-601 (destructuring
  del render), L637-642 y L690-735 ("Como voto"), L809-818 (nota de cobertura por proyectos),
  L891-975 (`derivarVotosViewData` puro), L978-1063 (`VotosSection` server: el fetch del listado)
- `app/app/parlamentario/[id]/page.tsx` L89-99 (`conteoLabel`), L520-530 (indice above-fold),
  L566-603 (carril #votos: `CarrilHeader`, `VotosCapa1`, `DetalleColapsable`, `<VotosSection>`)
- `app/lib/lockdown-guard.test.ts` L195-224 (`PUBLIC_RPC_ALLOWLIST`), L609-632 (assert A2),
  L750-784 (assert A5)
- `app/lib/parlamentario-resumen-conteos.test.ts` — el fake de supabase (L19: los nombres NO
  registrados en `rpcResponses` devuelven `{data:[],error:null}` en silencio) y L308-320
</read_first>

<interfaces>
Shape de la fila que devuelve la RPC de 0082 (una fila por seleccion presente; una seleccion sin
votos simplemente NO viene — el agregador debe partir de ceros y sumar, jamas asumir 5 filas):
  { seleccion: string; n: number }

`VotosBreakdown` (ya existe en `app/lib/parlamentario-resumen-conteos.ts`):
  { si: number; no: number; abstencion: number; pareo: number; ausente: number }

Orden LOCKED de la barra (`SELECCION_ORDEN` / `ORDEN_BARRA`, espejado en
`app/components/capa1/votos-capa1.tsx` L40-46 y en `votos-por-parlamentario.tsx`):
  ['si','no','abstencion','pareo','ausente']

Cifras de control de PROD para fixtures/asserts:
  D1165 -> 3752 total · si 1764 · no 1772 · abstencion 171 · pareo 16 · ausente 29
          (lo que se mostraba antes: 1000 · 469/466/22/16/27; proyectos 191 vs 555 reales)
</interfaces>

<orden_de_trabajo>
LOCKED (Pitfall 2 del research): **migracion -> allowlist -> call-site**. La migracion ya existe
(Plan 01). Dentro de este plan: la entrada del allowlist (Task 1) va ANTES o en el MISMO commit
que el `.rpc()` nuevo, o el assert A5 se pone rojo.

LOCKED (D-04): **Task 1 y Task 2 se commitean JUNTAS, en un unico commit**. Chip y `VotosSection`
no pueden viajar por separado — es el requisito explicito de la decision. No commitear al terminar
Task 1.
</orden_de_trabajo>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contarCarriles lee el agregado (allowlist + tipo + productor unico)</name>
  <files>app/lib/lockdown-guard.test.ts, app/lib/types.ts, app/lib/parlamentario-resumen-conteos.ts</files>
  <behavior>
    - Con la RPC de conteo devolviendo filas que suman 3752, `contarCarriles` reporta
      `votos.n === 3752` y `votosBreakdown` igual al desglose real, AUNQUE la RPC del listado
      devolviera solo 3 filas.
    - Una seleccion ausente de la respuesta cuenta 0 (no rompe, no fabrica).
    - Una seleccion desconocida en la respuesta se IGNORA (nunca fabrica una categoria) — regla
      heredada verbatim de `resumirVotos`.
    - Sin filas (parlamentario sin votos): total 0, `asistencia === null` (jamas un "0 de 0").
    - Error de la RPC de conteo: lanza, para que el fallback honesto de `contarCarrilesSeguro`
      (asistencia null, breakdown en ceros) haga su trabajo.
  </behavior>
  <action>
1. `app/lib/lockdown-guard.test.ts`: anadir `"votos_conteo_de_parlamentario",` al
   `PUBLIC_RPC_ALLOWLIST` (L195-224) en ORDEN ALFABETICO — va inmediatamente ANTES de
   `"votos_de_parlamentario"`. Comentario en el estilo vigente del archivo, indicando que la
   funcion vive en `supabase/migrations/0082_votos_conteo_de_parlamentario.sql` y se consume en
   Phase 130 Plan 02. El nombre debe quedar como literal string tanto aqui como en el call-site:
   el detector A5 no resuelve variables ni templates.

2. `app/lib/types.ts`: junto a la fila de la RPC vieja documentada alrededor de L301, anadir el
   tipo de la fila de la RPC nueva (`seleccion: string; n: number`) con un comentario que
   referencie 0082 y aclare que `count(*)` es `bigint` en Postgres pero llega como `number` por
   supabase-js (valores < 4.000; sin riesgo de precision).

3. `app/lib/parlamentario-resumen-conteos.ts`:
   - Re-propositar `resumirVotos` (L104-112) en el agregador de filas de la RPC (OQ-2, opcion que
     conserva cobertura): misma semantica de "seleccion desconocida se ignora", pero acumulando
     `n` por fila en vez de contar filas. Renombrarla a `agregarConteoVotos` y actualizar el JSDoc
     para que diga que la fuente es el agregado SQL sobre el universo completo. NO dejar la version
     vieja conviviendo: cero codigo muerto que invite a revertir al `.length`.
   - En `contarCarriles`, reemplazar el bloque L266-306 completo:
     * BORRAR el comentario WR-03 (L271-278) — describe una deuda que deja de existir — y
       sustituirlo por una nota que explique que el conteo y el desglose vienen del agregado SQL
       sobre TODAS las filas confirmadas (Phase 130 / migracion 0082), que `votos_de_parlamentario`
       queda como carril EXCLUSIVO del listado paginado (D-03), y que este modulo es el unico
       productor de verdad numerica del carril de votos.
     * Sustituir el `sb.rpc("votos_de_parlamentario", { p_id, p_limit: 1000, p_offset: 0 })` por
       `sb.rpc("votos_conteo_de_parlamentario", { p_id: id })`. Conservar el patron de error
       existente (lanzar con mensaje que nombre la RPC y el id).
     * `votosBreakdown` = `agregarConteoVotos(filas)`. `votosTotal` = suma de las 5 claves del
       breakdown recorriendo el orden LOCKED (no `Object.values`, para que el total y la barra
       usen exactamente el mismo dominio). `derivarEstado` se alimenta de ese total.
     * `asistencia` se deriva del breakdown (`presentes = total - breakdown.ausente`), no de un
       `filter` sobre filas. Conservar la regla honesta: `total === 0` -> `asistencia = null`.
     * `contarCarriles` YA retorna `votosBreakdown` y `asistencia` (L434-435): no cambiar la forma
       de `ConteoCarriles`, solo su procedencia. El fallback de `contarCarrilesSeguro` (L459-462)
       se deja EXACTAMENTE como esta.
   - Verificar que ningun otro consumidor importaba `resumirVotos`; si lo hacia, migrarlo.
NO commitear al terminar esta tarea (ver `<orden_de_trabajo>`).
  </action>
  <verify>
    <automated>
cd app
# Guard A2 (entrada <-> migracion) y A5 (.rpc allowlisted) — por NOMBRE de archivo, jamas por glob
pnpm exec vitest run lib/lockdown-guard.test.ts
# El call-site nuevo existe como literal y el viejo murio en este modulo:
grep -c 'rpc(\s*$\|"votos_conteo_de_parlamentario"' lib/parlamentario-resumen-conteos.ts
grep -o '"votos_conteo_de_parlamentario"' lib/parlamentario-resumen-conteos.ts | wc -l   # EXIGIDO: 1
grep -o '"votos_de_parlamentario"' lib/parlamentario-resumen-conteos.ts | wc -l          # EXIGIDO: 0
# Cero codigo muerto: `resumirVotos` no sobrevive
grep -c "resumirVotos" lib/parlamentario-resumen-conteos.ts                              # EXIGIDO: 0
# El comentario WR-03 obsoleto murio
grep -c "WR-03" lib/parlamentario-resumen-conteos.ts                                     # EXIGIDO: 0
# Allowlist en orden alfabetico: la entrada nueva precede a la vieja
grep -n "votos_conteo_de_parlamentario\|\"votos_de_parlamentario\"," lib/lockdown-guard.test.ts
cd .. && pnpm typecheck
    </automated>
  </verify>
  <done>
`contarCarriles` invoca `votos_conteo_de_parlamentario` (literal) y deriva total, breakdown y
asistencia del agregado. `resumirVotos` y el comentario WR-03 ya no existen. La entrada esta en
`PUBLIC_RPC_ALLOWLIST` en orden alfabetico y `lockdown-guard.test.ts` pasa (A2+A5). `pnpm typecheck`
verde. Sin commit todavia.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VotosSection recibe los conteos por prop + rotulos honestos sobre lo capado</name>
  <fable_blocker_1>
**MAPEO DEL FALLBACK (Fable blocker 1 — el número falso NO puede resucitar por el error-path):**
(a) `page.tsx` pasa `conteosGlobales={null}` cuando `conteos.votos.tipo !== "dato"` — JAMÁS reenviar
el breakdown-en-ceros de `conteosDesconocidos()` como si fuera dato (mostraría "total 0" con
listado lleno, peor que el 1000).
(b) En `derivarVotosViewData`, cuando `materiaActiva === null && conteosGlobales === null` (RPC de
conteo falló pero el listado cargó): el copy de total DEBE declarar alcance sobre las votaciones
CARGADAS (p.ej. "sobre las N votaciones cargadas") o suprimirse — NUNCA "Emitió N votos
registrados." pelado, que en ese camino ES B-01 resucitado (filasCargadas == totalConteos == 1000
⇒ el rótulo "N de M" no se gatea).
  </fable_blocker_1>
  <files>app/app/parlamentario/[id]/page.tsx, app/components/votos-por-parlamentario.tsx</files>
  <behavior>
    - Sin `?materia=`: "Como voto", la barra apilada, la asistencia y el "Emitio N votos" usan los
      conteos GLOBALES recibidos por prop (3752), no `conteoSet.length` (1000).
    - Con `?materia=X` activo: el desglose sigue siendo el del subconjunto filtrado (WR-01
      preservado — el rotulo dice el tema; mostrar el global bajo etiqueta de tema seria una
      falsedad NUEVA, Pitfall 7).
    - Si el listado renderiza menos votaciones de las que existen, la seccion lo DECLARA ("N de M"),
      nunca lo insinua ni lo omite.
    - La nota de cobertura por proyectos declara que su cifra es sobre las votaciones cargadas en
      el detalle, no sobre el registro completo.
    - Sin conteos globales disponibles (fallback): la seccion se comporta como hoy, sin fabricar.
  </behavior>
  <action>
1. `app/app/parlamentario/[id]/page.tsx` L598: pasar a `<VotosSection>` una prop
   `conteosGlobales={conteos.votosBreakdown}` (viene del `contarCarrilesSeguro(id)` ya awaiteado en
   L574 — cero fetch adicional). El `DetalleColapsable n={conteos.votos.n}` y el
   `CarrilHeader conteo={conteoLabel(conteos.votos)}` NO se tocan: ya quedan reales por Task 1.

2. `app/components/votos-por-parlamentario.tsx`:
   - `VotosSection` acepta la prop nueva `conteosGlobales` (tipo `VotosBreakdown | null`) y la
     reenvia a `derivarVotosViewData`. **El `sb.rpc("votos_de_parlamentario", ..., p_limit: 1000)`
     de L1009-1012 SE CONSERVA**: sigue sirviendo el listado paginado, la faceta por materia y el
     chart (D-03; es el carril del detalle, no del numero). Actualizar el comentario L1006-1008 para
     decir que este fetch ya NO produce conteos — los conteos llegan por prop desde `contarCarriles`.
   - `derivarVotosViewData` (L891-975) recibe `conteosGlobales` como parametro nuevo y aplica esta
     regla, escrita explicita en un comentario:
       * si `materiaActiva === null` y `conteosGlobales` existe -> `conteos` = `conteosGlobales` y
         `totalVotos` = suma de las 5 claves en el orden LOCKED;
       * en cualquier otro caso -> el calculo actual sobre `conteoSet` (WR-01 intacto).
     Ademas exponer en la view data `filasCargadas` (= `todasConMateria.length`) para que el render
     pueda declarar el recorte. NO tocar `arcos`/paginacion: siguen operando sobre las filas.
   - Rotulos honestos (D-04 "N de M" + OQ-1), en el render:
     * "Como voto" (L717-729): cuando `materiaActiva === null` y `filasCargadas < totalConteos`,
       anadir una linea que declare que el desglose es sobre el total real y que el listado de
       abajo muestra solo las `filasCargadas` votaciones mas recientes de `totalConteos`. Copy
       sobrio, sin insinuacion, en la voz del repo (mirar las notas honestas vecinas L802-818).
     * Cobertura por proyectos (L812-818): la cifra `totalProyectos` sigue derivando de las filas
       capadas (medido: 191 mostrados vs 555 reales). Reescribir el copy para que declare su
       alcance verbatim — que son los proyectos presentes en las votaciones cargadas en este
       detalle, NO el total del registro. Prohibido dejar una frase que se lea como total.
     * Chart "Cuando voto" (L666-685): la serie cubre solo el tramo cargado (2025-05 -> 2026-07 de
       un registro que arranca en 2022-03 para el testigo). Anadir a la nota existente L671-674 la
       declaracion de que el grafico cubre las votaciones cargadas en este detalle, no el periodo
       completo.
   - **Prohibido** subir `p_limit` a 4000 o a cualquier otro valor: es el clamp-como-fix que el
     criterio 4 veta (Pitfall 1). El diff de esta tarea no debe tocar ningun `p_limit`.
   - **Prohibido** ampliar el alcance a paginar realmente el listado (deferred en CONTEXT) o a
     corregir `totalProyectos`/chart con nuevas RPC (OQ-1: no ampliar alcance; solo rotulo honesto
     + deuda registrada en el SUMMARY).

3. Commit UNICO de Task 1 + Task 2 (D-04): un solo commit que contenga migracion-consumo, allowlist,
   chip y seccion.
  </action>
  <verify>
    <automated>
cd app
# fix W-4 checker: NO correr aqui votos-por-parlamentario.test.tsx — sus casos se adaptan recien
# en Task 3 (la firma de derivarVotosViewData ya cambio ⇒ verde imposible o vacuo). El verify de
# esta task es typecheck + greps; los tests corren en Task 3.
# La prop viaja de la pagina a la seccion:
grep -c "conteosGlobales=" "app/parlamentario/[id]/page.tsx"          # EXIGIDO: 1
grep -o "conteosGlobales" components/votos-por-parlamentario.tsx | wc -l   # EXIGIDO: >=3
# Control anti-clamp (criterio 4): p_limit intacto en 1000, cero 4000
grep -o "p_limit: 1000" components/votos-por-parlamentario.tsx | wc -l    # EXIGIDO: 1
grep -c "p_limit: 4000\|p_limit: 3752" components/votos-por-parlamentario.tsx   # EXIGIDO: 0
# El carril del listado sigue vivo (D-03): la seccion conserva su RPC de detalle
grep -o '"votos_de_parlamentario"' components/votos-por-parlamentario.tsx | wc -l   # EXIGIDO: 1
cd .. && pnpm typecheck 2>/dev/null || pnpm --filter ./app typecheck
pnpm --filter ./app exec vitest run lib/anti-insinuacion-guard.test.ts
    </automated>
  </verify>
  <done>
`VotosSection` recibe `conteosGlobales` por prop y el desglose global sale de ahi cuando no hay tema
activo; con `?materia=` el comportamiento WR-01 es identico al anterior. Los tres numeros que siguen
capados (listado, proyectos, chart) llevan rotulo que declara su alcance. Cero cambio de `p_limit`.
Task 1 + Task 2 en un unico commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: El test que MUERDE si el cap vuelve a gobernar + suite completa</name>
  <files>app/lib/parlamentario-resumen-conteos.test.ts, app/app/parlamentario/[id]/page.test.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <behavior>
    - TEST CENTINELA (D-05/SC4): con la RPC de conteo mockeada devolviendo el desglose de D1165
      (suma 3752) y `votos_de_parlamentario` mockeada devolviendo SOLO 3 filas, el DOM renderizado
      muestra el 3752 y NO muestra el 3. Si alguien revierte al `.length`, el assert positivo cae
      Y el assert negativo cae — muerde por los dos lados.
    - `contarCarriles` con conteo 3752 / listado 3 filas: `votos.n === 3752`.
    - Asistencia: con ausente=29 sobre 3752, presentes = 3723 (no 973 de la version capada).
    - Con `?materia=` activo el desglose sigue siendo el del subconjunto (control de no-regresion
      de WR-01).
  </behavior>
  <action>
1. `app/lib/parlamentario-resumen-conteos.test.ts`: **registrar EXPLICITAMENTE**
   `votos_conteo_de_parlamentario` en el mapa `rpcResponses` del fake de supabase. Critico: el fake
   devuelve `{data:[],error:null}` para nombres no registrados (L19) — un caso sin registrar daria
   un breakdown en ceros SILENCIOSO y el test pasaria en verde siendo vacuo. Adaptar los casos
   existentes de votos (L308-320) al shape agregado y anadir:
   - caso "el total no depende del listado": conteo suma 3752, listado 3 filas -> `votos.n === 3752`;
   - caso "seleccion ausente de la respuesta cuenta 0";
   - caso "seleccion desconocida se ignora" (no fabrica categoria);
   - caso "sin filas -> asistencia null";
   - caso "error de la RPC de conteo -> lanza" y su apareado "contarCarrilesSeguro devuelve el
     fallback honesto (breakdown en ceros, asistencia null)".
   Adaptar/eliminar los tests de `resumirVotos` a `agregarConteoVotos` (conservando la cobertura de
   "desconocida se ignora").

2. `app/app/parlamentario/[id]/page.test.tsx`: PRIMERO (fix W-5 checker) reparar los casos
   PREEXISTENTES: todo caso que aserta sobre el chip / `Ver detalle` (p.ej. ~L430
   `expect(html).toContain("Ver detalle (3)")`) mockea solo `votos_de_parlamentario`; tras el
   cambio el chip sale de `votos_conteo_de_parlamentario` y el fake devuelve `[]` para nombres no
   registrados ⇒ estado `vacio` y el assert cae. Registrar `votos_conteo_de_parlamentario` en TODOS
   esos casos (con el desglose agregado coherente con las filas mockeadas) y ajustar expectativas.
   DESPUES anadir el TEST CENTINELA sobre la superficie SSR.
   Mockear por NOMBRE de RPC (patron ya existente alrededor de L101). Assertear sobre el chip y el
   `Ver detalle (...)`, que vienen de `contarCarriles` FUERA del boundary de `<Suspense>` — mas
   robusto que assertear dentro (Pitfall 6: `Suspense` esconde en `<div hidden id="S:N">`).
   Precaucion de asserts (gotcha v12): React intercala `<!-- -->` entre texto y expresion, asi que
   assertear sobre el numero formateado tal como sale del render (seguir el precedente que YA
   funciona en este archivo: `expect(html).toContain("Ver detalle (3)")` en ~L430) y anadir el
   assert negativo `not.toContain` del numero del listado capado. El assert negativo debe elegirse
   de forma que no pueda coincidir por casualidad con una subcadena del positivo — usar el numero
   con sus delimitadores de render, no el digito suelto.

3. `app/components/votos-por-parlamentario.test.tsx`: casos de `derivarVotosViewData`:
   - sin materia + `conteosGlobales` -> `conteos` y `totalVotos` son los globales;
   - con materia activa -> `conteos` son los del subconjunto filtrado (no-regresion WR-01);
   - `conteosGlobales` null -> comportamiento previo;
   - render: cuando `filasCargadas < totalVotos` aparece el rotulo "N de M"; cuando son iguales, NO
     aparece (control de ausencia apareado con su control positivo).
   - **caso Fable blocker 1 (apareado):** RPC de conteo en ERROR + listado con filas → el DOM NO
     contiene "Emitió 1000 votos registrados." pelado (el copy declara alcance o se suprime), CON
     control positivo de que la sección sí renderizó (heading presente).

4. Correr la suite completa y los guards de regimen. **Jamas por glob**: `vitest run lib/*guard*.test.ts`
   sale 0 sin correr nada (gotcha v12) — usar `pnpm guards` de la raiz, que lista los 17 por nombre.
  </action>
  <verify>
    <automated>
cd app
pnpm exec vitest run lib/parlamentario-resumen-conteos.test.ts
pnpm exec vitest run "app/parlamentario/[id]/page.test.tsx"
pnpm exec vitest run components/votos-por-parlamentario.test.tsx
# Control positivo del fake: la RPC nueva esta registrada explicitamente (si no, verdes vacuos)
grep -c "votos_conteo_de_parlamentario" lib/parlamentario-resumen-conteos.test.ts   # EXIGIDO: >=1
# El centinela existe con sus dos lados (positivo + negativo)
grep -c "not.toContain" "app/parlamentario/[id]/page.test.tsx"                      # EXIGIDO: >=1
cd ..
pnpm typecheck
pnpm guards
pnpm test
    </automated>
  </verify>
  <done>
El test centinela falla si el numero visible vuelve a derivar del listado capado (verificado
invirtiendo el mock durante el desarrollo). `votos_conteo_de_parlamentario` esta registrada
explicitamente en el fake. `pnpm test`, `pnpm typecheck` y `pnpm guards` verdes, con conteo de
tests > 0 en cada corrida (nunca aceptar exit-0 sin tests ejecutados).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> Server Component | `?materia=`, `?votosPage=`, `?votosVer=` y el `id` de ruta son entrada no confiable |
| arbol publico -> Postgres (service_role) | service_role bypassa ACL/RLS: el lockdown-guard A5 es la UNICA barrera efectiva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-07 | Elevation of Privilege | `.rpc()` nuevo en el arbol publico | mitigate | Entrada literal en `PUBLIC_RPC_ALLOWLIST` + assert A5 (L761); nombre como literal string, jamas variable/template (el detector no resuelve indirecciones). |
| T-130-08 | Spoofing / integridad de la barrera | allowlist con entrada sin funcion real | mitigate | Assert A2 (L626) exige que el nombre matchee un `create ... function` en `supabase/migrations/` — satisfecho por 0082 (Plan 01, wave anterior). |
| T-130-09 | Tampering | `?materia=` gobernando el desglose mostrado | mitigate | Comparacion de igualdad contra slugs ya derivados de datos conocidos; cero interpolacion en SQL (T-51-05). WR-01 preservado: con tema activo el desglose es del subconjunto, jamas el global bajo etiqueta de tema. |
| T-130-10 | Information Disclosure | payload del Server Component | mitigate | El agregado son 5 filas; el listado conserva su `p_limit: 1000` — no se sube a 4000 (evita ×3,7 de payload ademas del veto del criterio 4). |
| T-130-11 | Repudiation (integridad del dato mostrado) | numero visible desincronizado entre chip y seccion | mitigate | Un solo productor (`contarCarriles`, React.cache) + prop a `VotosSection`; commit unico (D-04). El test centinela impide la regresion. |
| T-130-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas en esta fase (research §Standard Stack). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `pnpm --filter ./app exec vitest run lib/lockdown-guard.test.ts` verde (A2 y A5).
- Test centinela verde y comprobadamente mordedor (invertir el mock lo pone rojo).
- Cero `p_limit` modificado en el diff completo del plan: `git diff --stat` + `git diff | grep p_limit`.
- `pnpm test` y `pnpm guards` con conteo de tests > 0 (jamas aceptar un exit-0 vacuo).
- Un unico commit cubre allowlist + `contarCarriles` + `page.tsx` + `VotosSection` (D-04).
</verification>

<success_criteria>
- Chip, capa-1 y `Ver detalle (N)` derivan del agregado SQL; con el mock de D1165 muestran 3752.
- "Como voto" muestra 1764/1772/171/16/29 sin tema activo y el subconjunto con tema activo.
- Listado, `totalProyectos` y chart declaran su alcance; ninguno se lee como total del registro.
- El test que muerde existe y esta en la suite.
</success_criteria>

<output>
Create `.planning/phases/130-votos-real-b-01-el-numero-falso-muere/130-02-SUMMARY.md` when done
</output>
