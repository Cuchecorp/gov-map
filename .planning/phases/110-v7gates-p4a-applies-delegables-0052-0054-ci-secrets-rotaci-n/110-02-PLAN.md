---
phase: 110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [V7-07]
user_setup:
  - service: cloudflare
    why: "Deploy workflow (deploy-cloudflare.yml) needs Wrangler credentials to publish the site"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard → My Profile → API Tokens (permission: Edit Cloudflare Workers) → loaded as a GH Actions secret in Cuchecorp/gov-map"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard account id (the endpoint id, 10fb709d...) → loaded as a GH Actions secret in Cuchecorp/gov-map"
    dashboard_config:
      - task: "Verify GitHub Actions billing is active for the repo"
        location: "GitHub → repo/org Settings → Billing"
  - service: supabase
    why: "Rotate the exposed DB password B26 (blast radius = SUPABASE_DB_URL only)"
    dashboard_config:
      - task: "Reset database password + copy new connection string"
        location: "Supabase Dashboard → Settings → Database → Reset database password"
must_haves:
  truths:
    - "CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID exist as GH secrets (names present; values operator-loaded)"
    - "GitHub Actions billing verified active"
    - "DB password B26 rotated: old SUPABASE_DB_URL fails auth, new one returns 1"
    - "CI crons + deployed site stay green after rotation (they use SUPABASE_SECRET_KEY REST — unaffected)"
    - "The agent loads NO secret values and rotates NO credential (operator-only acts)"
  artifacts:
    - path: ".planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-OPERATOR-CHECKPOINT.md"
      provides: "Zero-credential-value operator steps for CI secrets + B26 rotation, with the agent's post-verification checklist"
      contains: "SUPABASE_DB_URL"
  key_links:
    - from: ".github/workflows/deploy-cloudflare.yml"
      to: "secrets.CLOUDFLARE_API_TOKEN / secrets.CLOUDFLARE_ACCOUNT_ID"
      via: "GH Actions secret reference (already correct — DEBT-03)"
      pattern: "secrets\\.CLOUDFLARE_(API_TOKEN|ACCOUNT_ID)"
---

<objective>
Close the OPERATOR half of V7-07: prepare and verify (a) the CI secrets
`CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` for the deploy workflow, and (b) the
rotation of the exposed DB password B26. The operator supplies secret values and rotates
the credential; the agent DOCUMENTS the exact zero-credential-value steps and VERIFIES the
post-state — it NEVER loads a secret value nor rotates anything.

Purpose: v7.0 gate closure requires the deploy pipeline credentialed and the leaked DB
password retired. Both are dashboard acts with no CLI/API path for the agent (rotating live
breaks active psql connections; secret values live only in operator-controlled surfaces).

Output: an operator-checkpoint document with copy-paste-ready steps (no secret values), and
the agent's verification results (GH secret names present, billing active, old DB url fails,
new works, CI/site green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-RESEARCH.md
@.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-CONTEXT.md
@.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md

<interfaces>
<!-- Deploy workflow YAML reference is ALREADY correct (DEBT-03) — only the secret VALUES are missing: -->
.github/workflows/deploy-cloudflare.yml:59  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
.github/workflows/deploy-cloudflare.yml:60  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

<!-- B26 blast radius (verified in note 75): password lives ONLY in SUPABASE_DB_URL. -->
<!-- grep SUPABASE_DB_URL over .github/workflows/ = 0 hits → CI crons + site use SUPABASE_SECRET_KEY REST → UNAFFECTED by rotation. -->
<!-- Post-rotation verify idiom (operator runs; agent verifies): -->
<!--   old url:  PGCLIENTENCODING=UTF8 psql "<old-url>" -c "select 1;"  → MUST fail auth -->
<!--   new url:  DB_URL=$(node -e "require('dotenv').config(); console.log(process.env.SUPABASE_DB_URL)"); PGCLIENTENCODING=UTF8 psql "$DB_URL" -c "select 1;"  → 1 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the operator-checkpoint document (zero credential values)</name>
  <files>.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-OPERATOR-CHECKPOINT.md</files>
  <read_first>
    - .planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md (the canonical B26 steps + blast-radius table — do NOT reinvent)
    - .github/workflows/deploy-cloudflare.yml (confirm the secret references at lines 59-60 are correct)
  </read_first>
  <action>
    Author `110-02-OPERATOR-CHECKPOINT.md` — a single operator-facing checklist referencing
    ONLY secret NAMES, never values. Two sections:

    (A) CI secrets (SC2): operator loads `CLOUDFLARE_API_TOKEN` (token with "Edit Cloudflare
    Workers" permission) + `CLOUDFLARE_ACCOUNT_ID` (endpoint account id) as GH Actions secrets
    in the deployed mirror repo (Cuchecorp/gov-map → Settings → Secrets and variables → Actions),
    and confirms GH Actions billing is active. State that the deploy YAML reference is ALREADY
    correct (DEBT-03) — only the values are missing. Do NOT surface any token value.

    (B) DB password rotation B26 (SC3): reproduce the 6 operator steps from note 75 verbatim in
    intent (rotate in Supabase Dashboard → Settings → Database → Reset database password;
    re-load ONLY SUPABASE_DB_URL in local .env; check Cuchecorp/gov-map for any *_DB_URL secret;
    confirm old url FAILS auth; confirm new url returns 1; confirm CI + site stay green). Include
    the anti-misinterpretation warnings: blast radius = SUPABASE_DB_URL ONLY; do NOT re-load
    SUPABASE_SECRET_KEY / SUPABASE_API_URL / ANON / JWT (independent creds, Pitfall 2); never
    commit the new password (.env gitignored).

    End with a RESUME SIGNAL: the operator writes "cargado y rotado" (with results) or describes
    the blocker. This document is the source the execute-phase checkpoint surfaces to the operator.
  </action>
  <verify>
    <automated>test -f ".planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-OPERATOR-CHECKPOINT.md" && grep -q "SUPABASE_DB_URL" ".planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-OPERATOR-CHECKPOINT.md" && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    Document exists with both sections (CI secrets + B26 rotation); references only secret NAMES
    (no values, no host, no connection string with a real password); includes the blast-radius
    warnings and the resume signal. No secret value appears anywhere in the file.
  </acceptance_criteria>
  <done>Operator-checkpoint doc written, zero credential values, both SC2 + SC3 covered.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator loads CI secrets + rotates DB password B26</name>
  <action>Operator-only checkpoint (no CLI/API path for the agent). Follow 110-02-OPERATOR-CHECKPOINT.md to (A) load CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID as GH secrets and verify GH billing, and (B) rotate the DB password B26. The agent loads no value and rotates nothing.</action>
  <what-built>
    The agent wrote `110-02-OPERATOR-CHECKPOINT.md` with exact, zero-credential-value steps for
    (A) loading `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` as GH secrets + verifying GH
    billing, and (B) rotating the DB password B26. The deploy YAML reference is already correct.
    The agent has NOT loaded any secret value and has NOT rotated any credential (operator-only:
    the agent has no dashboard access, and rotating live breaks active psql connections).
  </what-built>
  <how-to-verify>
    Follow `110-02-OPERATOR-CHECKPOINT.md`:
    1. (SC2) In Cuchecorp/gov-map → Settings → Secrets and variables → Actions, add
       `CLOUDFLARE_API_TOKEN` (Edit Cloudflare Workers token) and `CLOUDFLARE_ACCOUNT_ID`
       (endpoint account id). Verify GH Actions billing is active.
    2. (SC3) In Supabase Dashboard → Settings → Database → Reset database password. Copy the new
       connection string; paste ONLY `SUPABASE_DB_URL` into local .env (never commit). Check
       Cuchecorp/gov-map for any `*_DB_URL` secret and refresh if present.
    3. Confirm the OLD url fails auth: `PGCLIENTENCODING=UTF8 psql "<old-url>" -c "select 1;"` → error.
    4. Confirm the NEW url works:
       `DB_URL=$(node -e "require('dotenv').config(); console.log(process.env.SUPABASE_DB_URL)"); PGCLIENTENCODING=UTF8 psql "$DB_URL" -c "select 1;"` → returns 1.
    5. Confirm CI crons + deployed site remain green (they use SUPABASE_SECRET_KEY REST — unaffected).
  </how-to-verify>
  <resume-signal>Type "cargado y rotado" (with results: GH secrets loaded, old url fails, new url returns 1, CI/site green) — or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Agent post-verification of the operator acts</name>
  <files>(no repo files modified — read-only verification; results recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-OPERATOR-CHECKPOINT.md
  </read_first>
  <action>
    After the operator resumes, VERIFY (never loading or printing secret values):

    (A) CI secrets: if `gh` is authenticated for Cuchecorp/gov-map, run `gh secret list` and
    confirm `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` NAMES appear (values are never
    printed by `gh secret list`). If `gh` is not authed against the mirror, record the operator's
    reported confirmation from the resume signal and mark it operator-attested. Confirm the deploy
    workflow succeeded (a green deploy run is the functional proof the token works).

    (B) B26 rotation: confirm the operator-attested results from the resume signal — old url fails
    auth, new url returns 1, CI/site green. If the agent has the NEW SUPABASE_DB_URL in local .env
    (BOM-safe extract, never print), it MAY run `psql "$SUPABASE_DB_URL" -c "select 1;"` → 1 as an
    independent liveness check. The agent does NOT verify the OLD url itself (it never held the old
    value) — that is operator-attested.

    If any file was touched during this phase, assert tsc 0 + the 9 régimen guards unaffected
    (this phase touches no app/package source, so the suite should be trivially green).

    Record all verification outcomes in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "require('dotenv').config(); process.exit(process.env.SUPABASE_DB_URL ? 0 : 1)" && echo DBURL_PRESENT || echo OPERATOR_ATTESTED</automated>
  </verify>
  <acceptance_criteria>
    GH secret NAMES confirmed present (via gh secret list or operator attestation); deploy run
    green; B26 rotation confirmed (old fails / new works / CI+site green) via agent check or
    operator attestation; no secret value loaded or printed by the agent.
  </acceptance_criteria>
  <done>CI secrets + B26 rotation verified (agent + operator-attested); results in SUMMARY; no secret value handled by the agent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator dashboards → GH secrets / Supabase | secret values enter only via operator-controlled UIs; the agent must never receive or print them |
| local .env → psql liveness check | new SUPABASE_DB_URL may live in .env; agent extracts BOM-safe, never prints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Information Disclosure | secret values (CF token, DB url) in logs | mitigate | agent references only NAMES; `gh secret list` prints names not values; DB liveness check extracts BOM-safe and never echoes the url |
| T-110-06 | Elevation of Privilege | agent rotating/loading credentials | mitigate | rotation + secret loading are operator-only (no dashboard access; live rotation breaks connections) — enforced by checkpoint:human-action gate=blocking-human |
| T-110-07 | Repudiation | claiming rotation without proof | mitigate | verification requires old-url-fails + new-url-works + CI/site green; agent-run or operator-attested and recorded in SUMMARY |
| T-110-08 | Denial of Service | rotation silently breaking CI/site | accept | note 75 proves blast radius = SUPABASE_DB_URL only; CI/site use SUPABASE_SECRET_KEY REST (unaffected); green-check confirms |
</threat_model>

<verification>
- `110-02-OPERATOR-CHECKPOINT.md` exists, zero credential values, covers SC2 + SC3.
- CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID secret NAMES present (gh secret list / attested).
- GH Actions billing active; deploy run green.
- B26: old SUPABASE_DB_URL fails auth; new returns 1; CI crons + site green.
- Agent loaded no secret value and rotated no credential.
</verification>

<success_criteria>
V7-07 operator half closed: CI secrets loaded + billing verified (SC2); DB password B26 rotated
and verified old-fails/new-works/CI-green (SC3); the agent documented + verified without loading
any secret value or rotating any credential (SC4).
</success_criteria>

<output>
Create `.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-02-SUMMARY.md` when done
</output>
