---
phase: 110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [V7-01]
must_haves:
  truths:
    - "0052 applied to PROD: CHECK cruce_senal_tipo_senal_check admits 'lobby_sector_aporte'"
    - "0052 pgTAP runs against the APPLIED PROD schema: 7/7 ok, 0 not ok"
    - "count(cruce_senal where tipo_senal='lobby_sector_aporte') = 0 (honest empty, not a bug)"
    - "0053 leylobby_cursor_estado confirmed already-applied (honest no-op, not re-applied)"
    - "0054 leyes_rotacion_estado confirmed already-applied singleton (honest no-op, not re-applied)"
    - "MONEY_PUBLIC_ENABLED remains OFF (flip is Phase 112, not this phase)"
  artifacts:
    - path: ".planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-01-SUMMARY.md"
      provides: "Apply + verification record with pgTAP result and honest count=0"
      contains: "0052"
  key_links:
    - from: "0052 apply (psql --single-transaction)"
      to: "supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql"
      via: "pgTAP against applied schema"
      pattern: "psql.*0052.*test\\.sql"
---

<objective>
Close the AGENT-DELEGABLE half of V7-01: apply the additive migration 0052 to PROD
(constraint expansion + materializar_cruces redefinition) via `psql --single-transaction`,
verify it with its pgTAP against the applied schema, and confirm the already-applied
migrations 0053/0054 as honest no-ops (verify-only, NEVER re-apply).

Purpose: v7.0 gate closure requires 0052/0053/0054 live in PROD with pgTAP green. The
orchestrator's live read-only check (2026-07-27) proved 0053/0054 are ALREADY applied
and only 0052 is missing — so this plan applies exactly ONE migration and verifies three.

Output: 0052 live in PROD, pgTAP 7/7 ok recorded, 0053/0054 existence asserted, honest
count=0 for lobby_sector_aporte documented as correct-by-construction (not a bug).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-RESEARCH.md
@.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-CONTEXT.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md

<interfaces>
<!-- Live PROD state from the orchestrator's read-only check (2026-07-27) — AUTHORITATIVE. -->
<!-- 0052 NOT applied: constraint still CHECK ((tipo_senal = 'lobby_sector'::text)); lobby_sector_aporte ABSENT. -->
<!-- 0053 leylobby_cursor_estado: exists (true) → NO-OP verify only. -->
<!-- 0054 leyes_rotacion_estado: exists (true) → NO-OP verify only. -->

Migration file (apply target): supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql
  Bloque 1: drop constraint cruce_senal_tipo_senal_check; add constraint ... check (tipo_senal in ('lobby_sector','lobby_sector_aporte'))  [NOT idempotent — apply ONCE]
  Bloque 2: create or replace function cruces.materializar_cruces()  [idempotent; does NOT call the function → non-destructive at apply time]

pgTAP file: supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql  [7 assertions, plan(7), self-seeds fixture + rollback — leaves no PROD residue]

0053 shape: create table leylobby_cursor_estado (institucion_codigo text primary key, anio int not null, pagina int not null, fecha_captura timestamptz)  [RLS enabled, deny-by-default]
0054 shape: create table leyes_rotacion_estado (id int primary key default 1 check (id = 1), offset_rotacion int, ultimo_boletin text, fecha_captura timestamptz)  [singleton, RLS enabled]

DB URL extraction (BOM-safe, NEVER print the value):
  export SUPABASE_DB_URL="$(grep -a '^SUPABASE_DB_URL=' .env | sed 's/^\xEF\xBB\xBF//; s/^SUPABASE_DB_URL=//')"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify live PROD state + apply 0052 (fail-closed pre-check)</name>
  <files>(no repo files modified — PROD DB apply of supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql)</files>
  <read_first>
    - .planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md (§2 apply mechanics, §2.1 constraint precondition)
    - supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql (confirm additive, no grants/policies)
  </read_first>
  <action>
    Extract SUPABASE_DB_URL BOM-safe from .env (see interfaces block); NEVER print the value.

    PRE-CHECK 1 (constraint name, Pitfall A1): query
    `select conname from pg_constraint where conrelid='public.cruce_senal'::regclass and contype='c';`
    Expect exactly `cruce_senal_tipo_senal_check`. If the name differs, STOP and report — do NOT
    apply (the drop would target a nonexistent constraint).

    PRE-CHECK 2 (fail-closed idempotency guard — 0052 is NOT idempotent): re-verify
    `lobby_sector_aporte` is ABSENT from the CHECK def RIGHT BEFORE applying. Query
    `select pg_get_constraintdef(oid) from pg_constraint where conname='cruce_senal_tipo_senal_check' and conrelid='public.cruce_senal'::regclass;`
    Expect the def to NOT contain `lobby_sector_aporte`. If it is already present, STOP with an
    honest no-op declaration — do NOT re-run Bloque 1 (a second drop would fail).

    PRE-CHECK 3 (MONEY stays OFF): confirm `grep -E '^MONEY_PUBLIC_ENABLED=' .env` returns nothing
    (absent = OFF). If it is `=true`, STOP — the flip is Phase 112, not this apply.

    APPLY (exactly once, atomic): run
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql`.
    NEVER `supabase db push` (schema_migrations drift — LOCKED). Applying does NOT call
    materializar_cruces() → non-destructive at apply time.

    POST-APPLY: re-query the constraint def; confirm it now admits both `lobby_sector` and
    `lobby_sector_aporte`.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tAc "select pg_get_constraintdef(oid) from pg_constraint where conname='cruce_senal_tipo_senal_check' and conrelid='public.cruce_senal'::regclass;" | grep -q lobby_sector_aporte && echo APPLIED</automated>
  </verify>
  <acceptance_criteria>
    Constraint name confirmed before drop; lobby_sector_aporte confirmed ABSENT before apply;
    MONEY_PUBLIC_ENABLED absent (OFF); 0052 applied exactly once via psql --single-transaction
    (never db push); post-apply constraint def admits 'lobby_sector_aporte'. SUPABASE_DB_URL
    value never printed to logs/argv.
  </acceptance_criteria>
  <done>0052 live in PROD; CHECK admits lobby_sector_aporte; MONEY still OFF; DB URL never logged.</done>
</task>

<task type="auto">
  <name>Task 2: Run 0052 pgTAP against applied schema + confirm honest count=0</name>
  <files>(no repo files modified — pgTAP run + read-only count against PROD)</files>
  <read_first>
    - supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql (7 assertions, self-seeds + rollback)
    - .planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-APPLY-RUNBOOK.md (§3 pgTAP, §4 honest-empty)
  </read_first>
  <action>
    Run the pgTAP against the SAME applied PROD schema (the test seeds its own fixture and
    rolls back — leaves no residue):
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql`.
    Expect all lines `ok`, 0 `not ok`, plan(7). If any `not ok`, STOP, apply rollback per
    runbook §6, and report the failing assertion (build/typecheck are false positives per
    Pitfall 5 — pgTAP is the ONLY valid proof).

    Then confirm the honest empty state of the new signal (read-only):
    `select count(*) from cruce_senal where tipo_senal='lobby_sector_aporte';`
    Expect `0`. Document this as CORRECT-BY-CONSTRUCTION (empresa→sector edge absent + RUT-01/
    ChileCompra backfill pending, deferred to Phase 111) — NOT a materializer bug. Do NOT run
    materializar_cruces() manually here (the 0039 cron inherits the new branch nightly;
    populating the signal is Phase 111's job).
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql | grep -c '^not ok' | grep -qx 0 && echo PGTAP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    pgTAP reports 7 ok / 0 not ok against the APPLIED PROD schema; count of
    tipo_senal='lobby_sector_aporte' = 0 documented as honest-empty (not a bug); rollback path
    referenced if any assertion fails; materializar_cruces NOT invoked manually.
  </acceptance_criteria>
  <done>0052 pgTAP 7/7 green against applied schema; honest count=0 recorded with rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Verify-only no-op of 0053 + 0054 (already applied — NEVER re-apply)</name>
  <files>(no repo files modified — read-only existence/shape assertions against PROD)</files>
  <read_first>
    - supabase/migrations/0053_leylobby_cursor_estado.sql (table shape)
    - supabase/migrations/0054_leyes_rotacion_estado.sql (singleton table shape)
    - .planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-RESEARCH.md (0053/0054 = NO-OP verify only)
  </read_first>
  <action>
    0053 and 0054 are ALREADY applied in PROD (orchestrator live check). Do NOT re-run these
    migrations — a second `create table` would error. Verify-only (read-only), then declare
    honest no-ops.

    0053: assert `select to_regclass('public.leylobby_cursor_estado');` is non-null AND columns
    match the shape (institucion_codigo pk, anio, pagina, fecha_captura) via
    `select column_name from information_schema.columns where table_name='leylobby_cursor_estado' order by 1;`.

    0054: assert `select to_regclass('public.leyes_rotacion_estado');` is non-null AND the
    singleton guard exists — confirm the `check (id = 1)` constraint via
    `select pg_get_constraintdef(oid) from pg_constraint where conrelid='public.leyes_rotacion_estado'::regclass and contype='c';`
    (expect a def containing `id = 1`). Neither table has a pgTAP file → verification is
    object-existence + shape spot-check (per research §24).

    Record both as honest no-ops (applied during v8.1) in the SUMMARY.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tAc "select (to_regclass('public.leylobby_cursor_estado') is not null) and (to_regclass('public.leyes_rotacion_estado') is not null);" | grep -qx t && echo BOTH_EXIST</automated>
  </verify>
  <acceptance_criteria>
    leylobby_cursor_estado exists with expected columns; leyes_rotacion_estado exists with the
    id=1 singleton CHECK; neither migration re-applied; both declared honest no-ops in the SUMMARY.
  </acceptance_criteria>
  <done>0053 + 0054 confirmed present with correct shape; declared no-op; not re-applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator .env → psql/PROD | SUPABASE_DB_URL carries the DB password; must never appear in logs/argv history |
| DDL apply → cruce_senal | additive CHECK expansion; must not drop data or open a public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information Disclosure | SUPABASE_DB_URL in logs | mitigate | BOM-safe extract to env var; pass `--db-url` implicitly via `$SUPABASE_DB_URL`; never echo the value; read-only queries only print booleans/counts |
| T-110-02 | Tampering | 0052 double-apply (drop constraint) | mitigate | fail-closed pre-check re-verifies `lobby_sector_aporte` ABSENT immediately before apply; STOP as honest no-op if already present |
| T-110-03 | Elevation of Privilege | premature public exposure of MONEY signal | mitigate | pre-check confirms MONEY_PUBLIC_ENABLED absent (OFF); 0052 adds no grants/policies (inherits deny-by-default); flip is Phase 112 |
| T-110-04 | Repudiation | claiming apply without proof | mitigate | pgTAP 7/7 against APPLIED schema is the only valid proof (build/typecheck are false positives, Pitfall 5) |
</threat_model>

<verification>
- 0052 constraint def in PROD admits `lobby_sector_aporte` (post-apply query).
- pgTAP 0052: 7 ok / 0 not ok against applied schema.
- count(cruce_senal where tipo_senal='lobby_sector_aporte') = 0 (honest empty).
- leylobby_cursor_estado + leyes_rotacion_estado both exist with correct shape (no-op verify).
- MONEY_PUBLIC_ENABLED still absent/OFF in .env.
- SUPABASE_DB_URL value never printed.
</verification>

<success_criteria>
V7-01 agent-delegable half closed: 0052 applied to PROD via psql --single-transaction with
pgTAP 7/7 green against the applied schema; 0053/0054 verified as honest no-ops; honest
count=0 for lobby_sector_aporte documented as correct-by-construction; MONEY stays OFF.
</success_criteria>

<output>
Create `.planning/phases/110-v7gates-p4a-applies-delegables-0052-0054-ci-secrets-rotaci-n/110-01-SUMMARY.md` when done
</output>
