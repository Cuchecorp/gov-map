---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/news/package.json
  - packages/news/tsconfig.json
  - packages/news/vitest.config.ts
  - packages/news/src/feeds.ts
  - packages/news/src/feeds.test.ts
  - packages/news/src/allowlist-news.ts
  - packages/news/src/allowlist-news.test.ts
  - packages/news/src/probe-feeds.ts
  - packages/news/src/__fixtures__/
  - tsconfig.json
autonomous: true
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "El paquete @obs/news corre tests reales bajo `pnpm test` de la raíz (no CI-DARK)"
    - "Los 5 feeds y sus hosts están congelados en código con test que cae si cambian"
    - "Los 5 hosts de prensa pasan assertAllowedUrl SOLO vía extraHosts scoped; un host arbitrario no"
    - "El Fetcher REAL de Node obtuvo los 5 feeds (riesgo WAF A4 descartado con evidencia)"
    - "Los 5 XML crudos quedan como fixtures en el repo para todos los tests posteriores"
  artifacts:
    - path: "packages/news/package.json"
      provides: "workspace package @obs/news con script test=vitest run"
      contains: "\"@obs/news\""
    - path: "packages/news/vitest.config.ts"
      provides: "config propio — sin él el paquete es CI-DARK (Pitfall 8)"
    - path: "packages/news/src/feeds.ts"
      provides: "FEEDS + NEWS_HOSTS congelados (5 medios directos, D-132-A)"
      contains: "Object.freeze"
    - path: "packages/news/src/probe-feeds.ts"
      provides: "probe con Fetcher/RobotsGuard/HostRateLimiter reales + captura de fixtures"
    - path: "packages/news/src/__fixtures__"
      provides: "5 XML crudos reales capturados con el Fetcher del framework"
  key_links:
    - from: "tsconfig.json (raíz)"
      to: "packages/news"
      via: "references"
      pattern: "packages/news"
    - from: "packages/news/src/allowlist-news.ts"
      to: "@obs/ingest assertAllowedUrl"
      via: "extraHosts scoped (jamás DEFAULT_ALLOWED_SUFFIXES)"
      pattern: "extraHosts"
---

<objective>
Levantar el paquete `@obs/news` con su infraestructura de test REAL (no CI-DARK), congelar el set
de 5 feeds directos adjudicado por D-132-A, y **descartar el riesgo A4 del research** (el WAF de
un medio podría responder 403 al `fetch` de Node aunque `curl` funcione — precedente camara.cl)
probando los 5 feeds con el `Fetcher` real de `@obs/ingest` ANTES de escribir el conector.

Purpose: si A4 se materializa, cambia el set de fuentes y por tanto SC4 — hay que saberlo en la
primera tarea de red, no después de escribir 14 archivos.
Output: paquete linkeado al workspace, feeds congelados con test, allowlist scoped verificada, y
los 5 XML crudos capturados como fixtures que alimentan todos los tests de los planes 02/04/05/06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@packages/tramitacion/package.json
@packages/tramitacion/tsconfig.json
@packages/tramitacion/vitest.config.ts
@packages/dinero/src/connector-servel.ts
@packages/ingest/src/allowlist.ts
@packages/ingest/src/robots.ts
@packages/ingest/src/fetcher.ts

<interfaces>
De `@obs/ingest` (barrel `packages/ingest/src/index.ts`, ya exportados):

```
export { Fetcher, RetryableError, FetchError } from "./fetcher";
export { RobotsGuard, IDENTIFIED_UA } from "./robots";
export { HostRateLimiter } from "./rate-limiter";
export { assertAllowedUrl, HostNotAllowedError, DEFAULT_ALLOWED_SUFFIXES } from "./allowlist";
export type { AllowlistOptions } from "./allowlist";
export type { RequestSpec } from "./base-connector";
```

`assertAllowedUrl(rawUrl: string, opts: AllowlistOptions = {}): URL` — deny-by-default;
`DEFAULT_ALLOWED_SUFFIXES` es SOLO gubernamental (camara.cl, senado.cl, bcn.cl, leychile.cl,
leylobby.gob.cl, cplt.cl, infoprobidad.cl, mercadopublico.cl). Ningún medio de prensa está ahí.
`AllowlistOptions.extraHosts` admite TAMBIÉN `http:` (allowlist.ts:116-123) ⇒ aserción `https:`
explícita obligatoria (precedente literal `connector-servel.ts:157-160`).
`RobotsGuard` gatea el propio GET de `robots.txt` con la MISMA allowlist (`robots.ts:105-111`):
sin `extraHosts` devuelve HOST_BLOCKED ⇒ `isAllowed=false` silencioso en los 5 feeds.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold del paquete @obs/news linkeado al workspace y NO CI-DARK</name>
  <read_first>
    - packages/tramitacion/package.json (copiar entero; quitar @obs/adjudication y @obs/identity)
    - packages/tramitacion/tsconfig.json (references, NUNCA paths a src — rompe `tsc -b`, gotcha Phase 43)
    - packages/tramitacion/vitest.config.ts (analog literal)
    - tsconfig.json (raíz, líneas 4-18: lista de references)
    - package.json (raíz, script `test`: `pnpm -r --filter "./packages/*" test`)
  </read_first>
  <files>packages/news/package.json, packages/news/tsconfig.json, packages/news/vitest.config.ts, tsconfig.json</files>
  <action>
    Crear `packages/news/package.json` con `name: "@obs/news"`, `private: true`, `type: "module"`,
    `main`/`types` = `src/index.ts`, `exports: { ".": "./src/index.ts" }`, scripts
    `test: "vitest run"`, `typecheck: "tsc -b"`, `ingest: "tsx src/run-news-cli.ts"`.
    Dependencies: `@obs/core`, `@obs/ingest` (ambos `workspace:*`), `@supabase/supabase-js ^2.108.2`,
    `fast-xml-parser ^5.9.2`, `zod ^4.4.3`. DevDependencies: `@types/node ^20.19.43`, `tsx ^4.22.4`,
    `vitest ^3.0.0`. Cero dependencias nuevas al ecosistema (research §Package Legitimacy Audit:
    la fase no instala paquetes externos nuevos; si aparece la necesidad de uno, PARAR y escalar).

    `packages/news/tsconfig.json`: extends `../../tsconfig.base.json`, `rootDir: "src"`,
    `outDir: "dist"`, `composite: true`, `noEmit: false`, `lib: ["ES2022","DOM"]`, `types: ["node"]`,
    `references: [{ "path": "../core" }, { "path": "../ingest" }]`, include `src/**/*.ts`,
    exclude `src/**/*.test.ts`, `test`, `dist`. PROHIBIDO `compilerOptions.paths` apuntando a `src`.

    `packages/news/vitest.config.ts`: analog literal de tramitacion —
    `test: { environment: "node", include: ["src/**/*.test.ts"], passWithNoTests: true }`.

    Añadir `{ "path": "./packages/news" }` a `references` de `tsconfig.json` de la raíz, después de
    `./packages/tramitacion`.

    Correr `pnpm install` para linkear el workspace.

    Cabecera de comentario en cada archivo nuevo declarando qué hace y qué está prohibido
    (convención del repo, PATTERNS §Shared Patterns).
  </action>
  <acceptance_criteria>
    - `pnpm install` termina 0 y existe `packages/news/node_modules/@obs/ingest` (symlink del workspace).
    - `pnpm --filter @obs/news exec node -e "import('@obs/ingest').then(m=>{if(typeof m.assertAllowedUrl!=='function')process.exit(1)})"` sale 0 — prueba que el link resuelve de verdad, no que el archivo existe.
    - `grep -n '"path": "./packages/news"' tsconfig.json` devuelve exactamente 1 línea.
    - `grep -c '"paths"' packages/news/tsconfig.json` == 0 (gotcha Phase 43).
    - `pnpm typecheck` (raíz, `tsc -b`) sale 0 con el nuevo reference incluido.
    - **Anti-vacuo CI-DARK:** crear temporalmente `packages/news/src/__cidark.test.ts` con un único `expect(1).toBe(2)`; correr `pnpm test` desde la RAÍZ y comprobar que **la suite raíz cae con exit ≠ 0** (esto es lo que prueba que el paquete no es CI-DARK; `passWithNoTests: true` haría verde un paquete vacío). Luego borrar el archivo y confirmar que `pnpm test` raíz vuelve a 0. Registrar ambos exit codes en el SUMMARY.
    - `pnpm --filter @obs/news test` imprime una línea `Tests  N passed` con N ≥ 1 al final del plan (verificar el CONTEO, no el exit code — `vitest run <glob>` puede salir 0 sin correr nada).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news test && pnpm typecheck</automated>
  </verify>
  <done>El paquete existe, está en el workspace y en los references de la raíz, y se demostró con un test que falla a propósito que `pnpm test` de la raíz LO RECORRE.</done>
</task>

<task type="auto">
  <name>Task 2: feeds.ts congelado (5 medios, D-132-A) + allowlist scoped con test</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md §D-132-A y §D-01 (set final = 5 medios directos; Google News DESCARTADO)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §Fuentes (URLs exactas verificadas 2026-08-05, HTTP 200)
    - packages/dinero/src/connector-servel.ts:1-27 y :107-120 y :157-160 (SERVEL_HOST congelado, allowlistConServel, aserción https, HEADERS_SERVEL con Object.freeze)
    - packages/ingest/src/allowlist.ts:19-28 y :99-137 (DEFAULT_ALLOWED_SUFFIXES, assertAllowedUrl, extraHosts admite http)
  </read_first>
  <files>packages/news/src/feeds.ts, packages/news/src/feeds.test.ts, packages/news/src/allowlist-news.ts, packages/news/src/allowlist-news.test.ts</files>
  <action>
    `feeds.ts`: exportar `FEEDS` como `Object.freeze` de 5 entradas `{ slug, outlet, url }` con las
    URLs EXACTAS verificadas por el research:
      biobiochile  → https://www.biobiochile.cl/static/feed-rss
      cooperativa  → https://www.cooperativa.cl/noticias/site/tax/port/all/rss_3_158__1.xml
      latercera    → https://www.latercera.com/arc/outboundfeeds/rss/?outputType=xml
      lacuarta     → https://www.lacuarta.com/arc/outboundfeeds/rss/?outputType=xml
      exante       → https://www.ex-ante.cl/feed/
    Exportar `NEWS_HOSTS` (readonly, derivado o congelado) = www.biobiochile.cl, www.cooperativa.cl,
    www.latercera.com, www.lacuarta.com, www.ex-ante.cl.
    Cabecera del archivo: declarar que Google News RSS Search está DESCARTADO por D-132-A
    (`news.google.com/robots.txt` prohíbe `/rss/` para todo UA) y que agregar un feed nuevo exige
    verificar robots.txt + RSS vivo primero.

    `allowlist-news.ts`: exportar `allowlistNews(base?: AllowlistOptions): AllowlistOptions` que
    mergea `base.extraHosts` con `NEWS_HOSTS` (patrón `allowlistConServel`, connector-servel.ts:107-112)
    y `assertFeedUrl(url: string): URL` que llama `assertAllowedUrl(url, allowlistNews())` y además
    **lanza explícitamente si `protocol !== "https:"`** (extraHosts admite http). Cabecera con la
    prohibición literal: jamás tocar `DEFAULT_ALLOWED_SUFFIXES` (ampliaría SSRF a todos los
    conectores gubernamentales).

    `feeds.test.ts`: congela las 5 URLs y los 5 hosts por valor literal + `FEEDS.length === 5` +
    que `Object.isFrozen(FEEDS)`. Test referenciado por NOMBRE de archivo, nunca por glob.

    `allowlist-news.test.ts`: (a) cada uno de los 5 hosts pasa `assertFeedUrl`; (b)
    `assertAllowedUrl(feedUrl)` SIN opts lanza `HostNotAllowedError` (prueba que el default NO los
    cubre — Pitfall 1); (c) `https://evil.example.com/x` lanza con `allowlistNews()`;
    (d) `http://www.latercera.com/...` lanza por la aserción https.
  </action>
  <acceptance_criteria>
    - `packages/news/src/feeds.test.ts` y `allowlist-news.test.ts` pasan; el reporte de vitest muestra ≥ 9 tests corridos en el paquete.
    - **Mutación obligatoria (registrar en SUMMARY):** cambiar una letra en la URL de `latercera` en `feeds.ts` ⇒ `feeds.test.ts` FALLA; revertir. Quitar `NEWS_HOSTS` del merge en `allowlistNews` ⇒ `allowlist-news.test.ts` FALLA; revertir. Quitar la aserción `https:` ⇒ el caso (d) FALLA; revertir.
    - `grep -n "news.google.com" packages/news/src/feeds.ts` aparece SOLO dentro del comentario de cabecera que documenta el descarte, y `grep -c "google" packages/news/src/feeds.ts | ...` — verificar que NO existe ninguna entrada de FEEDS con host google: `node -e "import('./packages/news/src/feeds.ts')"` no aplica; usar el test: assert que ningún `FEEDS[i].url` contiene `google`.
    - `grep -n "DEFAULT_ALLOWED_SUFFIXES" packages/news/src/*.ts` no muestra ninguna asignación/mutación (solo puede aparecer en comentarios).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run src/feeds.test.ts src/allowlist-news.test.ts</automated>
  </verify>
  <done>Los 5 feeds y hosts están congelados con test que cae al mutarlos, y la allowlist de prensa es scoped al package con aserción https y prueba de que el default NO los permite.</done>
</task>

<task type="auto">
  <name>Task 3: [RIESGO A4] Probe con Fetcher REAL de Node + captura de los 5 fixtures</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §Assumptions Log A4 (curl OK ≠ Node fetch OK; precedente WAF camara.cl)
    - packages/ingest/src/fetcher.ts (firma de `get(spec: RequestSpec)`, UA identificatorio, RetryableError)
    - packages/ingest/src/robots.ts:49-111 (RobotsGuard, IDENTIFIED_UA, allowlist gatea el GET de robots.txt)
    - packages/ingest/src/rate-limiter.ts (HostRateLimiter)
    - packages/news/src/allowlist-news.ts (Task 2)
  </read_first>
  <files>packages/news/src/probe-feeds.ts, packages/news/src/__fixtures__/*.xml</files>
  <action>
    Script LOCAL (no test, no CI) `probe-feeds.ts` ejecutable con
    `pnpm --filter @obs/news exec tsx src/probe-feeds.ts`. Para cada feed de `FEEDS`, EN SERIE:
      1. `new RobotsGuard({ allowlist: allowlistNews() })` → `isAllowed(url)`; si false, imprimir
         `[robots-disallow] <slug>` y **PARAR y escalar** (no continuar con ese feed).
      2. `await rateLimiter.wait(new URL(url).host)` con `new HostRateLimiter({ minIntervalMs: 3000 })`
         — 3 s, extremo superior del rango 2-3 s del régimen.
      3. `await fetcher.get({ url, resource: \`rss-\${slug}\`, key: slug, ext: "xml" })` con
         `new Fetcher({ allowlist: allowlistNews() })`.
      4. Escribir el body TAL CUAL a `packages/news/src/__fixtures__/<slug>.xml`.
      5. Imprimir una línea por feed: `<slug> <bytes> <sha256(8)> <nItems>` donde nItems es el
         conteo de ocurrencias de `<item` en el crudo.
    Este script NO escribe a R2 ni a Supabase: es solo probe + captura de fixtures.
    Prohibido reintentar en ráfaga: ante 403/429/5xx, imprimir el status y **PARAR** — un WAF que
    bloquea al Fetcher es una decisión de fuentes (cambia SC4), no algo que el ejecutor improvise.

    **Si algún host devuelve 403/406 al `Fetcher` de Node pero el research lo vio 200 con curl:
    materializó A4. PARAR, no inventar workaround (nada de UA spoof, nada de headers de navegador),
    documentar host+status en el SUMMARY y escalar al operador.**
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec tsx src/probe-feeds.ts` sale 0 e imprime exactamente 5 líneas de resultado, una por slug.
    - Existen 5 archivos en `packages/news/src/__fixtures__/` y `ls -l` muestra tamaño > 10.000 bytes en cada uno (los feeds medidos por research van de 42 KB a 605 KB; un 403 devolvería HTML corto).
    - Cada fixture contiene `<rss` y al menos un `<item` — verificar con: `for f in packages/news/src/__fixtures__/*.xml; do grep -c "<item" "$f"; done` y que ningún conteo sea 0. (No usar `grep -c` sobre HTML de una línea como prueba de otra cosa — gotcha v12.0.)
    - Ningún fixture contiene `<!DOCTYPE html` (Emol/El Mostrador devolvían HTML; si un feed lo devuelve, es un fallo de fuente, no un fixture válido).
    - El SUMMARY registra, por feed: status HTTP, bytes, sha256 y nº de `<item>`; y el veredicto explícito de A4 (`Fetcher de Node OK` / `bloqueado`).
    - El delay observado entre requests consecutivos es ≥ 3 s — el script debe imprimir el timestamp de cada request y el SUMMARY debe citar las 5 marcas de tiempo.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec tsx src/probe-feeds.ts</automated>
  </verify>
  <done>Los 5 feeds respondieron al Fetcher real del framework con delay ≥3 s entre requests, y sus XML crudos quedaron como fixtures del repo. El riesgo A4 tiene veredicto escrito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso → hosts de prensa (internet) | URL de feed sale del proceso hacia un host no gubernamental |
| XML de terceros → nuestro parser | contenido no confiable entra al repo como fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Elevation of Privilege | `allowlist-news.ts` | mitigate | `extraHosts` EXACTOS scoped al package; test que prueba que `DEFAULT_ALLOWED_SUFFIXES` NO cubre los feeds y que un host arbitrario es rechazado |
| T-132-02 | Information Disclosure (SSRF) | `probe-feeds.ts` | mitigate | Todo GET pasa por `Fetcher` con `assertAllowedUrl` + aserción `https:` explícita (extraHosts admite http) |
| T-132-03 | Denial of Service (al tercero) | `probe-feeds.ts` | mitigate | `HostRateLimiter({minIntervalMs:3000})` serial, sin concurrencia, sin reintentos en ráfaga; PARAR ante 429/5xx |
| T-132-SC | Tampering | dependencias npm | mitigate | Cero paquetes externos nuevos (research §Package Legitimacy Audit). Si el plan necesitara uno: PARAR y correr el Package Legitimacy Gate |
</threat_model>

<verification>
- `pnpm typecheck` (raíz) sale 0.
- `pnpm test` (raíz) recorre `@obs/news` — demostrado con el test que falla a propósito.
- Los 3 tests de mutación (URL de feed, merge de extraHosts, aserción https) están registrados en el SUMMARY con el resultado de la corrida mutada.
</verification>

<success_criteria>
- `@obs/news` existe, está linkeado y no es CI-DARK (probado por falla inducida en la suite raíz).
- `FEEDS` tiene exactamente 5 entradas, ninguna con host `google`, congeladas con test.
- Los 5 fixtures XML reales están en el repo, cada uno con `<rss` y ≥1 `<item>`.
- Veredicto escrito del riesgo A4.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-01-SUMMARY.md` when done
</output>
