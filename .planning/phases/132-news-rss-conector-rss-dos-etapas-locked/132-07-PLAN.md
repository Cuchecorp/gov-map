---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 07
type: execute
wave: 5
depends_on: ["132-06"]
files_modified:
  - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md
  - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REPORTE-OPERADOR.md
autonomous: true
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "Los 5 feeds fueron descargados por el conector real con delay observable entre requests"
    - "El crudo de los 5 feeds está en R2 bajo `news/rss-<slug>/<fecha>/<sha256>.xml`"
    - "La re-corrida inmediata sale `[skip]` en los 5 feeds y con cero re-descarga"
    - "`noticia` y `noticia_url_vista` tienen filas en PROD y el conteo de descartes es consultable"
    - "Un replay `--from-r2` de un r2Path real reproduce la carga sin tocar la red"
    - "El operador recibe un reporte de handoff con los números reales y la decisión de alcance D-132-A"
  artifacts:
    - path: ".planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md"
      provides: "Per-Task Verification Map completada con los task IDs reales"
    - path: ".planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REPORTE-OPERADOR.md"
      provides: "Artefacto de handoff: números reales de la corrida + D-132-A para revisión del operador"
  key_links:
    - from: "run-news-cli"
      to: "R2 + Supabase PROD"
      via: "corrida LIVE local (única pasada de red de la fase para estos hosts)"
      pattern: "news/rss-"
---

<objective>
La corrida de verificación de la fase: **una** pasada LIVE contra los 5 feeds, su re-corrida
inmediata que debe salir `[skip]`, el replay `--from-r2` sobre un r2Path real, y los conteos por
`psql -tA`. Es la evidencia de SC1, SC2, SC3 y SC4 sobre el sistema real, no sobre dobles.

Purpose: todo lo anterior está probado con dobles inyectados; esto prueba el ensamblaje contra las
fuentes, R2 y Postgres reales.
Output: evidencia registrada + `132-VALIDATION.md` completada + `132-REPORTE-OPERADOR.md`.

**Régimen:** backfill/corrida = LOCAL (nunca GitHub Actions). Ninguna corrida de red adicional
más allá de la descrita aquí. Un agente JAMÁS flipea flags `*_PUBLIC_ENABLED` (en 132 no hay
ninguno que tocar: el sitio no lee noticias hasta la 137).

**Presupuesto de red de la fase (F-8, contabilizado explícitamente):** `probe-feeds.ts` del plan 01
hace **5 requests** (1 por host, ≥3 s de separación) y **no escribe `source_snapshot`**; este plan
hace **5 requests más** en la corrida real (paso 1). **Total de la fase = 10 requests, 2 por host,
separados por horas.** La re-corrida del paso 2 hace **0 requests** (caché diaria) y el replay del
paso 4 hace **0 requests** (lee de R2). Cualquier request adicional es una desviación que debe
justificarse por escrito en el SUMMARY.

**Por qué este plan es `autonomous: true` (adjudicado en la revisión de premortem, LOCKED):** la
corrida LIVE la ejecuta el agente — es UNA pasada, serial, rate-limited, con robots evaluado ANTES
de cada fetch por `BaseConnector.run()`. El reporte al operador es un **artefacto de handoff**
(`132-REPORTE-OPERADOR.md`), **jamás un gate bloqueante dentro de la fase**: el precedente de la
Phase 129 (`AUSENTE-HANDOFF`) mostró que un gate humano en una sesión sin operador detiene el
milestone. Lo que NO se relaja: ante 403/429/5xx de un medio se **PARA** sin reintentos, sin cambiar
el UA y sin saltarse robots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md
@packages/news/src/run-news-cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa verde + Per-Task Verification Map de 132-VALIDATION.md</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md (tabla a completar; Wave 0 Requirements)
    - Los SUMMARY de 132-01 a 132-06 (nombres reales de tareas y comandos automatizados)
  </read_first>
  <files>.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md</files>
  <action>
    Correr `pnpm test` (raíz), `pnpm typecheck` y `pnpm guards`; los tres deben salir 0.

    **Conteo de tests — cómo medirlo sin falso verde.** `pnpm -r` emite un `Tests  N passed` **por
    paquete**, no un total, y `git stash` no revierte un paquete ya commiteado ⇒ el criterio
    "comparar con la rama base vía stash" **no es operable**. En su lugar: capturar la salida de
    `pnpm test` a un archivo y verificar que aparece un bloque del paquete `@obs/news` con
    `Tests  N passed` y `N ≥ 45` (suma de los mínimos exigidos por los planes: 01 ≥ 9, 03 ≥ 10,
    04 ≥ 34 combinados, 05 y 06 ≥ 9 — ajustar el umbral al mínimo real agregado de los SUMMARY y
    dejarlo escrito). Además, `@obs/news` debe aparecer en la salida: un paquete ausente es
    CI-DARK, y eso ya se probó por falla inducida en 132-01 Task 1.

    Completar la tabla **Per-Task Verification Map** con una fila por tarea de los planes 01-06:
    `Task ID` = `132-0N-T{k}`, `Plan`, `Wave`, `Requirement` (NEWS-01/NEWS-02), `Threat Ref`
    (T-132-xx del `<threat_model>` del plan correspondiente), `Secure Behavior`, `Test Type`,
    `Automated Command` (el literal del `<verify><automated>` de esa tarea), `File Exists` (✅) y
    `Status` (✅ green).
    Marcar las casillas de **Wave 0 Requirements** y de **Validation Sign-Off** que quedaron
    cumplidas. (La contradicción "sin config propio" ya está corregida en `132-VALIDATION.md`: el
    paquete **sí** lleva `packages/news/vitest.config.ts`, analog literal de tramitacion; lo que se
    verifica es que `pnpm test` de la raíz lo RECORRE.)
    Actualizar el frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`.
  </action>
  <acceptance_criteria>
    - `pnpm test`, `pnpm typecheck` y `pnpm guards` salen 0 **cada uno por separado** (capturar el exit code de cada comando en su propia variable; no encadenarlos con `&&` en una sola aserción).
    - La salida capturada de `pnpm test` contiene una línea que menciona `@obs/news` y un `Tests  N passed` con `N ≥ 45`; ambos números citados literalmente en el SUMMARY.
    - `grep -c "TBD (planner)" .planning/phases/132-*/132-VALIDATION.md` == 0.
    - `grep -c "sin config propio" .planning/phases/132-*/132-VALIDATION.md` == 0.
    - La tabla tiene ≥ 13 filas (una por tarea de los planes 01-06) y cada fila tiene un `Automated Command` no vacío que existe **literalmente** en el PLAN correspondiente (verificar con `grep -F "<comando>" <plan>` por cada fila; registrar el conteo de filas verificadas).
    - `grep -c "nyquist_compliant: true" .planning/phases/132-*/132-VALIDATION.md` == 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'V=.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md; pnpm test > /tmp/132-test.log 2>&1; rc_test=$?; pnpm typecheck > /tmp/132-tc.log 2>&1; rc_tc=$?; pnpm guards > /tmp/132-g.log 2>&1; rc_g=$?; echo "test=$rc_test typecheck=$rc_tc guards=$rc_g"; test "$rc_test" -eq 0; test "$rc_tc" -eq 0; test "$rc_g" -eq 0; grep -q "@obs/news" /tmp/132-test.log; test "$(grep -c "TBD (planner)" "$V")" = "0"; test "$(grep -c "sin config propio" "$V")" = "0"; test "$(grep -c "nyquist_compliant: true" "$V")" = "1"'</automated>
  </verify>
  <done>Suite completa verde con los tres exit codes verificados por separado, y la validación de la fase documentada con task IDs reales y comandos verificables.</done>
</task>

<task type="auto">
  <name>Task 2: [CORRIDA LIVE] 5 feeds → R2 → Supabase, re-corrida `[skip]`, replay y conteos</name>
  <read_first>
    - packages/news/src/run-news-cli.ts (flags disponibles y su semántica)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §Pitfall 5 (la re-corrida NO producirá un 412: `<lastBuildDate>` cambia el sha256; el `[skip]` lo da la CACHÉ DIARIA, no el 412 — redactar la evidencia sobre la caché)
    - 132-CONTEXT.md §D-132-A (Google News descartado — debe ir destacado en el reporte al operador)
  </read_first>
  <files>(ninguno — corrida de verificación; la evidencia va al SUMMARY)</files>
  <action>
    Ejecutar LOCALMENTE los 6 pasos de abajo, **en este orden exacto**, pegando la salida COMPLETA
    de cada paso en el SUMMARY.

    **PROHIBIDO un `--dry-run` previo a la corrida real (premortem F-2, LOCKED).** Razón, verificada
    contra el código: `BaseConnector.run()` escribe `source_snapshot` **incondicionalmente**
    (`base-connector.ts:170-185`) y `DailyCache.hasToday` lee exactamente de esa tabla
    (`cache.ts:54-58`). `--dry-run` solo cambia el **writer de la Etapa 2**, no el snapshot store de
    la Etapa 1 ⇒ un dry-run previo envenenaría la caché del día y la corrida real saldría
    `descargados=0 skips=5`, la Etapa 2 no recibiría ningún `SnapshotRef` y `noticia` quedaría
    **vacía**, sin camino de reintento el mismo día. **La corrida real es la primera y única pasada
    de red del día para estos hosts.**

    1. **Corrida real** (Etapa 1 + Etapa 2, escribe a R2 y a PROD):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts`
       Esperado: `descargados=5 skips=0`, los 5 `r2Path` impresos, timestamps con ≥2 s entre
       requests consecutivos, y los conteos `vistos/cargados/descartados` por feed.
       Si la salida trae `skips>0` en esta primera corrida, **PARAR**: significa que ya hubo una
       pasada del día (caché envenenada) — documentarlo y escalar, no maquillarlo.

    2. **Re-corrida INMEDIATA** (evidencia del SC2):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts --etapa1`
       Esperado: **5 líneas `[skip]`**, `descargados=0 skips=5`, y **cero requests HTTP** a los
       medios. Nota honesta que debe quedar escrita: el `[skip]` lo produce la **caché diaria**, NO
       un 412 de R2 — `<lastBuildDate>` cambia en cada corrida, así que el sha256 del crudo sería
       distinto y `putImmutable` nunca devolvería 412. **No redactar la evidencia sobre el 412.**

    3. **Verificar el crudo en R2:** tomar los `r2Path` impresos por el paso 1 y confirmar que cada
       uno matchea `^news/rss-(biobiochile|cooperativa|latercera|lacuarta|exante)/\d{4}-\d{2}-\d{2}/[0-9a-f]{64}\.xml$`,
       uno por cada uno de los 5 slugs.

    4. **Replay SC3 sobre un r2Path REAL** (0 requests a medios; `--dry-run` aquí es seguro porque
       la rama `--from-r2` NO pasa por `BaseConnector.run()` ni escribe `source_snapshot`):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts --from-r2 <r2Path-real> --dry-run`
       Esperado: reproduce los mismos conteos de ese feed sin ningún request a los medios.

    5. **Conteos en PROD** (`psql -tA`, no PostgREST — capa en 1.000 filas):
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from noticia;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select coalesce(causa,'(pasa)') as causa, count(*) from noticia_url_vista group by 1 order by 2 desc;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select outlet, count(*) from noticia group by 1 order by 2 desc;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select source, resource, date_bucket from source_snapshot where source='news' order by resource;"`

    6. **Idempotencia contra PROD:** correr el paso 4 una segunda vez **SIN** `--dry-run` sobre el
       mismo `r2Path` y confirmar que `select count(*) from noticia` **no cambia**.

    **Si un medio devuelve 403/429/5xx: PARAR.** No reintentar en ráfaga, no cambiar el UA, no
    saltarse robots. Documentar host+status y dejarlo escrito en el reporte al operador (Task 3) —
    es una decisión de fuentes que afecta a SC4.
  </action>
  <acceptance_criteria>
    - Paso 1 imprime `descargados=5 skips=0`. **Si imprime `skips>0`, el criterio NO está cumplido** y el plan para (caché del día ya consumida).
    - Paso 2 imprime exactamente 5 líneas `[skip]` y `descargados=0`; la evidencia del SC2 está redactada sobre la **caché diaria**, y el SUMMARY dice explícitamente que el 412 NO ocurre y por qué.
    - Los 5 `r2Path` matchean el regex del paso 3, uno por slug — los 5 slugs presentes.
    - `select count(*) from noticia` > 0 y `select count(*) from source_snapshot where source='news'` == 5.
    - La query de `causa` devuelve al menos una fila con `prefiltro_lexico` y un conteo > 0 — el descarte es **observable** (SC4).
    - La suma `pasa + prefiltro_lexico` de `noticia_url_vista` es igual al total de ítems parseados reportado por el CLI.
    - Paso 4 reproduce los conteos del feed sin requests a medios (verificable porque el CLI no imprime líneas de fetch para ese feed).
    - Paso 6: `count(*) from noticia` idéntico antes y después — idempotencia contra la base real.
    - Los timestamps del paso 1 muestran ≥2 s entre requests consecutivos; se citan las 5 marcas.
    - El SUMMARY contabiliza los requests de la fase: 5 del probe (plan 01) + 5 de esta corrida = **10**, y declara que los pasos 2, 4 y 6 hicieron **0** requests a medios.
    - **Ningún artefacto nuevo contiene el project-ref de Supabase** (B26): `grep -rEil "[a-z]{20}\.supabase\.co" packages/news .planning/phases/132-* supabase/migrations/0084_noticia.sql` no devuelve nada.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; n=$(PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from noticia;"); echo "noticia=$n"; test "$n" -gt 0; s=$(PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from source_snapshot where source='"'"'news'"'"';"); echo "snapshots=$s"; test "$s" -eq 5; d=$(PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from noticia_url_vista where causa='"'"'prefiltro_lexico'"'"';"); echo "descartes=$d"; test "$d" -gt 0'</automated>
  </verify>
  <done>La corrida LIVE, su re-corrida `[skip]`, el replay desde un r2Path real y los conteos en PROD están registrados como evidencia de SC1-SC4, con el presupuesto de red de la fase contabilizado.</done>
</task>

<task type="auto">
  <name>Task 3: 132-REPORTE-OPERADOR.md — handoff con los números reales y D-132-A destacado</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md §D-132-A
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §BLOCKER (tabla de opciones A/B/C y evidencia de robots.txt)
    - .planning/ROADMAP.md § Phase 132 SC4 (ya re-redactado) y .planning/REQUIREMENTS.md NEWS-02
    - El SUMMARY del Task 2 (números reales de la corrida)
  </read_first>
  <files>.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REPORTE-OPERADOR.md</files>
  <action>
    Escribir `132-REPORTE-OPERADOR.md`: **artefacto de handoff, NO un gate**. La fase se cierra sin
    esperar respuesta; el operador lo lee cuando esté y puede pedir un cambio de alcance después.

    Antes de escribirlo, **re-verificar `news.google.com/robots.txt` en la fecha de ejecución** (A6
    del research: "no confiar en el documento"): un único GET a `https://news.google.com/robots.txt`
    (request de infraestructura, no de contenido; contabilizarlo aparte del presupuesto de 10) y
    evaluar `/rss/search` con `robots-parser` bajo `IDENTIFIED_UA`. Reportar el `isAllowed` obtenido
    y si el documento cambió respecto de lo que registró el research.

    Secciones obligatorias del reporte:
      1. **Qué se ingirió**: ítems parseados, cargados y descartados por `prefiltro_lexico`, por
         outlet, con los números literales del Task 2.
      2. **D-132-A destacado**: Google News RSS Search DESCARTADO porque `news.google.com/robots.txt`
         declara `Disallow: /` para `User-agent: *` con una allowlist que **no incluye `/rss/`** —
         verificado con `robots-parser@3.0.1` (la librería exacta de `RobotsGuard`) y con el
         `IDENTIFIED_UA` del framework, más la re-verificación de hoy. Costo real: se pierde el
         fan-out a 40+ outlets vía el tag `<source>`; la cobertura queda en los 5 feeds directos.
         Cualquier evasión (UA spoofeado, saltarse el guard) viola CLAUDE.md §Ingesta y el propio
         SC1 — **no es una opción disponible** salvo que Google reabra `/rss/`.
      3. **Opciones de alcance para el operador** (a decidir CUANDO quiera, fuera de esta fase):
         (a) confirmar D-132-A tal cual; (b) confirmar + encargar a una fase futura la ampliación de
         medios directos (cada candidato exige verificar robots.txt + RSS vivo antes); (c) revisar el
         descarte solo si aporta información nueva sobre `/rss/`.
      4. **Qué NO entrega la fase**: sin clasificación LLM (135), sin taxonomía (133), sin resolver
         (134), sin cron (136), sin lectura pública (137). `noticia` es deny-all: nada del sitio la lee.
      5. **Nota honesta sobre el SC2**: la evidencia es la **caché diaria**, no un 412 de R2, y la
         razón (`<lastBuildDate>` volátil cambia el sha256 en cada corrida).
      6. **Incidencias de fuentes**: cualquier 403/429/5xx observado, con host y status.

    El reporte NO contiene el project-ref de Supabase ni credenciales (B26).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/132-*/132-REPORTE-OPERADOR.md` y el archivo tiene las 6 secciones (verificar por encabezado con `grep -c "^## "` ≥ 6).
    - El reporte cita literalmente el conteo de ítems ingeridos, de descartes por `prefiltro_lexico` y de noticias cargadas por outlet, tomados del Task 2 — los mismos números que el SUMMARY (verificar que coinciden, no re-derivarlos).
    - El reporte incluye el resultado de la RE-verificación de `news.google.com/robots.txt` en la fecha de ejecución, con el `isAllowed` para `/rss/search` bajo `IDENTIFIED_UA` y si el documento cambió.
    - `grep -c "412" .planning/phases/132-*/132-REPORTE-OPERADOR.md` ≥ 1 y el contexto es la nota honesta de que el 412 **NO** ocurre (la nota debe existir, no basta con no mentir por omisión).
    - `grep -Eil "[a-z]{20}\.supabase\.co|eyJ[A-Za-z0-9]" .planning/phases/132-*/132-REPORTE-OPERADOR.md` no devuelve nada (B26).
    - El SUMMARY declara explícitamente que este reporte es **handoff, no gate**, y que la fase cierra sin esperar respuesta del operador.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; R=.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REPORTE-OPERADOR.md; test -f "$R"; test "$(grep -c "^## " "$R")" -ge 6; test "$(grep -c "412" "$R")" -ge 1; test -z "$(grep -Eil "[a-z]{20}\.supabase\.co|eyJ[A-Za-z0-9]" "$R" || true)"'</automated>
  </verify>
  <done>El operador tiene, en un artefacto versionado, los números reales de la corrida y la decisión de alcance D-132-A con su re-verificación — sin que la fase haya quedado bloqueada esperándolo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corrida local → 5 hosts de prensa | tráfico saliente real a terceros |
| CLI (service_role) → PROD | escritura real en la base viva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-19 | Denial of Service (al tercero) | corrida LIVE | mitigate | Una sola pasada, serial, con delay ≥2 s verificado por timestamps; presupuesto de red de la fase contabilizado (10 requests); PARAR ante 403/429/5xx sin reintentos |
| T-132-20 | Spoofing (falso verde) | evidencia del SC2 | mitigate | La evidencia se redacta sobre la caché diaria; se prohíbe expresamente prometer un 412 que no ocurrirá |
| T-132-21 | Information Disclosure | artefactos de la fase | mitigate | Grep de project-ref (B26) sobre `packages/news`, la migración y los artefactos de `.planning/` == 0 |
| T-132-23 | Denial of Service (auto-infligido) | caché diaria envenenada por un `--dry-run` previo | mitigate | El dry-run previo está PROHIBIDO; la corrida real es la primera pasada del día y el criterio exige `skips=0` en ella |
</threat_model>

<verification>
- `pnpm test` + `pnpm typecheck` + `pnpm guards` verdes, con el exit code de cada uno capturado por separado.
- Los 6 pasos de la corrida LIVE con su salida pegada en el SUMMARY.
- `132-VALIDATION.md` completada con `nyquist_compliant: true`.
- `132-REPORTE-OPERADOR.md` escrito como handoff.
</verification>

<success_criteria>
- SC1: delay observable entre requests + el conector pasa por `BaseConnector.run()` (probado estructuralmente en 132-03 y observado aquí).
- SC2: re-corrida con 5 `[skip]` y cero re-descarga; key R2 content-addressed verificada en los 5 slugs.
- SC3: replay `--from-r2` sobre un r2Path real reproduce la carga sin red.
- SC4: 5 medios directos operan y el conteo de descartes del pre-filtro es consultable por `psql -tA`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-07-SUMMARY.md` when done
</output>
