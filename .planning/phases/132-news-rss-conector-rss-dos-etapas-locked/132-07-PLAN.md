---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 07
type: execute
wave: 5
depends_on: ["132-06"]
files_modified:
  - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md
autonomous: false
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "Los 5 feeds fueron descargados por el conector real con delay observable entre requests"
    - "El crudo de los 5 feeds está en R2 bajo `news/rss-<slug>/<fecha>/<sha256>.xml`"
    - "La re-corrida inmediata sale `[skip]` en los 5 feeds y con cero re-descarga"
    - "`noticia` y `noticia_url_vista` tienen filas en PROD y el conteo de descartes es consultable"
    - "Un replay `--from-r2` de un r2Path real reproduce la carga sin tocar la red"
  artifacts:
    - path: ".planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md"
      provides: "Per-Task Verification Map completada con los task IDs reales"
  key_links:
    - from: "run-news-cli"
      to: "R2 + Supabase PROD"
      via: "corrida LIVE local del operador"
      pattern: "news/rss-"
---

<objective>
La corrida de verificación de la fase: **una** pasada LIVE contra los 5 feeds, su re-corrida
inmediata que debe salir `[skip]`, el replay `--from-r2` sobre un r2Path real, y los conteos por
`psql -tA`. Es la evidencia de SC1, SC2, SC3 y SC4 sobre el sistema real, no sobre dobles.

Purpose: todo lo anterior está probado con dobles inyectados; esto prueba el ensamblaje contra las
fuentes, R2 y Postgres reales.
Output: evidencia registrada + `132-VALIDATION.md` completada + reporte al operador.

**Régimen:** backfill/corrida = LOCAL (nunca GitHub Actions). Ninguna corrida de red adicional
más allá de las descritas aquí. Un agente JAMÁS flipea flags `*_PUBLIC_ENABLED` (en 132 no hay
ninguno que tocar: el sitio no lee noticias hasta la 137).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md
@packages/news/src/run-news-cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa verde + Per-Task Verification Map de 132-VALIDATION.md</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md (tabla a completar, líneas 37-46; Wave 0 Requirements, líneas 50-56)
    - Los SUMMARY de 132-01 a 132-06 (nombres reales de tareas y comandos automatizados)
  </read_first>
  <files>.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md</files>
  <action>
    Correr `pnpm test` (raíz), `pnpm typecheck` y `pnpm guards`; los tres deben salir 0 y `pnpm test`
    debe reportar un conteo de tests **mayor** que el de la rama base (registrar ambos números).

    Completar la tabla **Per-Task Verification Map** con una fila por tarea de los planes 01-06:
    `Task ID` = `132-0N-T{k}`, `Plan`, `Wave`, `Requirement` (NEWS-01/NEWS-02), `Threat Ref`
    (T-132-xx del `<threat_model>` del plan correspondiente), `Secure Behavior`, `Test Type`,
    `Automated Command` (el literal del `<verify><automated>` de esa tarea), `File Exists` (✅) y
    `Status` (✅ green).
    Marcar las casillas de **Wave 0 Requirements** y de **Validation Sign-Off** que quedaron
    cumplidas, y corregir la línea que dice "sin config propio" — el paquete **sí** lleva
    `packages/news/vitest.config.ts` (analog literal de tramitacion); lo que se verificó fue que
    `pnpm test` de la raíz lo RECORRE (test que falla a propósito, plan 01 Task 1).
    Actualizar el frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`.
  </action>
  <acceptance_criteria>
    - `pnpm test` (raíz) sale 0 y el conteo total de tests es estrictamente mayor que el de `git stash`-ed base (registrar ambos números en el SUMMARY).
    - `pnpm typecheck` y `pnpm guards` salen 0.
    - `grep -c "TBD (planner)" .planning/phases/132-*/132-VALIDATION.md` == 0.
    - La tabla tiene ≥ 13 filas (una por tarea de los planes 01-06) y cada fila tiene un `Automated Command` no vacío que existe literalmente en el PLAN correspondiente.
    - `grep -c "nyquist_compliant: true" .planning/phases/132-*/132-VALIDATION.md` == 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'pnpm test && pnpm typecheck && pnpm guards && test "$(grep -c "TBD (planner)" .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VALIDATION.md)" = "0"'</automated>
  </verify>
  <done>Suite completa verde y la validación de la fase documentada con task IDs reales y comandos verificables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CORRIDA LIVE] 5 feeds → R2 → Supabase, re-corrida `[skip]`, replay y conteos</name>
  <read_first>
    - packages/news/src/run-news-cli.ts (flags disponibles y su semántica)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §Pitfall 5 (la re-corrida NO producirá un 412: `<lastBuildDate>` cambia el sha256; el `[skip]` lo da la CACHÉ DIARIA, no el 412 — redactar la evidencia sobre la caché)
    - 132-CONTEXT.md §D-132-A (Google News descartado — debe ir destacado en el reporte al operador)
  </read_first>
  <files>(ninguno — corrida de verificación; la evidencia va al SUMMARY)</files>
  <action>Ejecutar LOCALMENTE los 7 pasos de `<how-to-verify>` en orden: dry-run, corrida real, re-corrida `[skip]`, verificación de las keys en R2, replay `--from-r2` sobre un r2Path real, conteos por `psql -tA`, y prueba de idempotencia. Pegar la salida completa de cada paso en el SUMMARY. Ante 403/429/5xx de un medio: PARAR y escalar, sin reintentos ni cambio de UA.</action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from noticia; select coalesce(causa,'(pasa)'), count(*) from noticia_url_vista group by 1;"</automated>
  </verify>
  <what-built>
    Conector Etapa 1 sobre `BaseConnector` (robots + throttle durable + rate-limit + UA + R2
    content-addressed + snapshot), Etapa 2 (parse → canonicalizar → ledger → pre-filtro → `noticia`),
    CLI con `--dry-run`/`--from-r2`/`--feeds`, migración 0084 aplicada a PROD, y una suite completa
    que ejercita todo con dobles inyectados. Falta la única corrida contra las fuentes vivas.
  </what-built>
  <how-to-verify>
    LOCAL, en este orden exacto. Registrar la salida COMPLETA de cada paso en el SUMMARY.

    1. **Dry-run primero** (sin escribir a Supabase, sí escribe crudo a R2):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts --dry-run`
       Esperado: 5 feeds descargados, delay ≥2 s entre requests visible en los timestamps, y los
       conteos de `vistos/cargados/descartados` por feed.

    2. **Corrida real** (escribe a PROD):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts`
       Esperado: `descargados=5 skips=0` (o `skips=5` si el dry-run ya llenó la caché del día — en
       ese caso, el paso 3 se cumple ya aquí y hay que decirlo así, sin maquillarlo).

    3. **Re-corrida INMEDIATA** (evidencia del SC2):
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts --etapa1`
       Esperado: **5 líneas `[skip]`**, `descargados=0 skips=5`, y **cero requests HTTP** a los
       medios. Nota honesta que debe quedar escrita: el `[skip]` lo produce la **caché diaria**, NO
       un 412 de R2 — `<lastBuildDate>` cambia en cada corrida, así que el sha256 del crudo sería
       distinto y `putImmutable` nunca devolvería 412. **No redactar la evidencia sobre el 412.**

    4. **Verificar el crudo en R2:** tomar un `r2Path` impreso por la corrida y confirmar que
       matchea `^news/rss-(biobiochile|cooperativa|latercera|lacuarta|exante)/\d{4}-\d{2}-\d{2}/[0-9a-f]{64}\.xml$`,
       uno por cada uno de los 5 slugs.

    5. **Replay SC3 sobre un r2Path REAL:**
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts --from-r2 <r2Path-real> --dry-run`
       Esperado: reproduce los mismos conteos de ese feed sin ningún request a los medios.

    6. **Conteos en PROD** (`psql -tA`, no PostgREST — capa en 1.000 filas):
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from noticia;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select coalesce(causa,'(pasa)') as causa, count(*) from noticia_url_vista group by 1 order by 2 desc;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select outlet, count(*) from noticia group by 1 order by 2 desc;"`
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select source, resource, date_bucket from source_snapshot where source='news' order by resource;"`

    7. **Idempotencia contra PROD:** correr el paso 5 (replay) una segunda vez SIN `--dry-run`
       sobre el mismo `r2Path` y confirmar que `select count(*) from noticia` **no cambia**.

    **Si un medio devuelve 403/429/5xx: PARAR.** No reintentar en ráfaga, no cambiar el UA, no
    saltarse robots. Documentar host+status y escalar — es una decisión de fuentes (afecta SC4).
  </how-to-verify>
  <acceptance_criteria>
    - Paso 3 imprime exactamente 5 líneas `[skip]` y `descargados=0`; la evidencia del SC2 está redactada sobre la **caché diaria**, y el SUMMARY dice explícitamente que el 412 NO ocurre y por qué.
    - Los 5 `r2Path` matchean el regex del paso 4, uno por slug — los 5 slugs presentes.
    - `select count(*) from noticia` > 0 y `select count(*) from source_snapshot where source='news'` == 5.
    - La query de `causa` devuelve al menos una fila con `prefiltro_lexico` y un conteo > 0 — el descarte es **observable** (SC4).
    - La suma `pasa + prefiltro_lexico` de `noticia_url_vista` es igual al total de ítems parseados reportado por el CLI.
    - Paso 5 reproduce los conteos del feed sin requests a medios (verificable porque el CLI no imprime líneas de fetch para ese feed).
    - Paso 7: `count(*) from noticia` idéntico antes y después — idempotencia contra la base real.
    - Los timestamps del paso 1 muestran ≥2 s entre requests consecutivos al conjunto de hosts; se citan las 5 marcas.
    - **Ningún artefacto nuevo contiene el project-ref de Supabase** (B26): `grep -rEil "[a-z]{20}\.supabase\.co" packages/news .planning/phases/132-* supabase/migrations/0084_noticia.sql` no devuelve nada.
  </acceptance_criteria>
  <resume-signal>Escribir "verificado" pegando la salida de los 7 pasos, o describir el fallo (host+status si fue un medio).</resume-signal>
  <done>La corrida LIVE, su re-corrida `[skip]`, el replay desde un r2Path real y los conteos en PROD están registrados como evidencia de SC1-SC4.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Reporte al operador — D-132-A (Google News descartado) destacado</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md §D-132-A
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md §BLOCKER (tabla de opciones A/B/C y evidencia de robots.txt)
    - .planning/ROADMAP.md § Phase 132 SC4 (ya re-redactado) y .planning/REQUIREMENTS.md NEWS-02
  </read_first>
  <files>(ninguno — reporte al operador en el SUMMARY)</files>
  <action>Redactar el reporte de cierre al operador con los números reales del Task 2 y presentar la decisión de alcance descrita en `<decision>` / `<options>`. Re-verificar `news.google.com/robots.txt` en la fecha de ejecución (A6) e incluir el `isAllowed` de `/rss/search` bajo `IDENTIFIED_UA`.</action>
  <verify>
    <human-check>El operador selecciona una de las tres opciones.</human-check>
  </verify>
  <decision>
    Confirmar (o revertir) el cambio de alcance de la fase: **Google News RSS Search quedó
    DESCARTADO** y el set de fuentes son 5 medios directos.
  </decision>
  <context>
    `news.google.com/robots.txt` declara `Disallow: /` para `User-agent: *` con una allowlist que
    **no incluye `/rss/`** — verificado con fetch real y con `robots-parser@3.0.1`, la librería
    exacta que usa `RobotsGuard`, con el `IDENTIFIED_UA` del framework. Cualquier evasión (UA
    spoofeado, saltarse el guard) viola la regla LOCKED de CLAUDE.md §Ingesta y el propio SC1 de
    esta fase. Fable adjudicó la Opción A+B: descartar Google News y entrar Ex-Ante como 5º medio
    directo (RSS 2.0 vivo, robots allow-all, ambos verificados).

    **Costo real:** se pierde el fan-out a 40+ outlets vía el tag `<source>`. La cobertura de
    outlets legislativos queda limitada a los 5 feeds. Ex-Ante aporta una ventana de solo ~10 ítems.
    Además, el research midió densidad legislativa: La Tercera 12/100, La Cuarta 3/100,
    BioBioChile 1/20, Cooperativa 0/15, Ex-Ante 0/10 ⇒ el pre-filtro descartará ~90-95% de lo
    ingerido (esperado y confirmado por la corrida LIVE del Task 2).

    Re-verificar el robots.txt de `news.google.com` al momento de este reporte (A6 del research:
    "no confiar en el documento") y reportar si cambió.
  </context>
  <options>
    <option id="confirmar">
      <name>Confirmar D-132-A: 5 medios directos, Google News fuera</name>
      <pros>Cero deuda de régimen; SC1 y SC4 simultáneamente verdaderos; ya implementado y verificado</pros>
      <cons>Menor cobertura de outlets; la cola legislativa (El Mostrador, Emol) queda fuera — el research no halló RSS vivo en las rutas probadas</cons>
    </option>
    <option id="ampliar">
      <name>Confirmar + encargar ampliación de medios directos a una fase futura</name>
      <pros>Recupera cobertura por la vía legítima (más feeds verificados), sin tocar robots</pros>
      <cons>Trabajo adicional fuera del roadmap actual; requiere verificar robots.txt + RSS vivo de cada candidato</cons>
    </option>
    <option id="revisar">
      <name>Revisar el descarte (el operador aporta información nueva)</name>
      <pros>—</pros>
      <cons>Cualquier camino que ignore el robots.txt de Google está PROHIBIDO por CLAUDE.md; esta opción solo aplica si Google reabrió `/rss/`</cons>
    </option>
  </options>
  <acceptance_criteria>
    - El reporte cita literalmente el conteo de ítems ingeridos, de descartes por `prefiltro_lexico` y de noticias cargadas por outlet, tomados del Task 2.
    - El reporte incluye el resultado de la RE-verificación de `news.google.com/robots.txt` en la fecha de ejecución (A6), con `isAllowed` para `/rss/search` bajo `IDENTIFIED_UA`.
    - El reporte declara explícitamente qué NO entrega la fase: sin clasificación LLM (135), sin taxonomía (133), sin resolver (134), sin cron (136), sin lectura pública (137).
    - El reporte declara que la evidencia del SC2 es la **caché diaria**, no un 412 de R2, con la razón (`<lastBuildDate>` volátil).
  </acceptance_criteria>
  <resume-signal>Seleccionar: confirmar, ampliar, o revisar.</resume-signal>
  <done>El operador tiene el cambio de alcance y los números reales de la corrida frente a sí, y decidió.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corrida local → 5 hosts de prensa | tráfico saliente real a terceros |
| CLI (service_role) → PROD | escritura real en la base viva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-19 | Denial of Service (al tercero) | corrida LIVE | mitigate | Una sola pasada, serial, con delay ≥2 s verificado por timestamps; PARAR ante 403/429/5xx sin reintentos |
| T-132-20 | Spoofing (falso verde) | evidencia del SC2 | mitigate | La evidencia se redacta sobre la caché diaria; se prohíbe expresamente prometer un 412 que no ocurrirá |
| T-132-21 | Information Disclosure | artefactos de la fase | mitigate | Grep de project-ref (B26) sobre `packages/news`, la migración y los artefactos de `.planning/` == 0 |
</threat_model>

<verification>
- `pnpm test` + `pnpm typecheck` + `pnpm guards` verdes.
- Los 7 pasos de la corrida LIVE con su salida pegada en el SUMMARY.
- `132-VALIDATION.md` completada con `nyquist_compliant: true`.
</verification>

<success_criteria>
- SC1: delay observable entre requests + el conector pasa por `BaseConnector.run()` (probado estructuralmente en 132-03 y observado aquí).
- SC2: re-corrida con 5 `[skip]` y cero re-descarga; key R2 content-addressed verificada en los 5 slugs.
- SC3: replay `--from-r2` sobre un r2Path real reproduce la carga sin red.
- SC4: 5 medios directos operan y el conteo de descartes del pre-filtro es consultable por `psql -tA`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-07-SUMMARY.md` when done
</output>
