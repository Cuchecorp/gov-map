---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 03
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - packages/news/src/connector-news.ts
  - packages/news/src/connector-news.test.ts
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "Cada request a un feed pasa por robots.isAllowed ANTES del fetch, y por rateLimiter.wait ANTES del fetch"
    - "Si robots prohíbe un feed, NO se llama a fetcher.get para ese feed y se loguea el skip"
    - "El host usado para robots/rate-limit se deriva de la URL real, no de spec.host"
    - "Cada feed tiene su propio `resource` ⇒ la caché diaria no apaga a los otros 4"
    - "El crudo va a R2 con extensión .xml en la key `news/rss-<slug>/<fecha>/<sha256>.xml`"
  artifacts:
    - path: "packages/news/src/connector-news.ts"
      provides: "NewsConnector extends BaseConnector<RssRaw> — Etapa 1 completa"
      contains: "extends BaseConnector"
    - path: "packages/news/src/connector-news.test.ts"
      provides: "Tests estructurales del SC1 y del SC2 (parte unitaria)"
  key_links:
    - from: "packages/news/src/connector-news.ts"
      to: "@obs/ingest BaseConnector.run()"
      via: "herencia — el orden LOCKED no es sobreescribible"
      pattern: "extends BaseConnector"
    - from: "packages/news/src/connector-news.ts"
      to: "allowlistNews()"
      via: "extraHosts inyectado en Fetcher Y en RobotsGuard"
      pattern: "allowlistNews"
---

<objective>
Escribir el conector de Etapa 1: `NewsConnector extends BaseConnector<RssRaw>`, implementando solo
los tres hooks (`endpoints`, `validateShape`, `fingerprint`) y **nada más**. Los tests demuestran
estructuralmente el SC1 (robots + rate-limit + UA + cero ráfagas por construcción) y la parte
unitaria del SC2 (key de R2 y caché diaria).

Purpose: los 4 huecos de régimen de Is Chile Safe quedan cerrados POR CONSTRUCCIÓN porque la
política vive en `BaseConnector.run()`, que no es sobreescribible. Cada línea de infraestructura
que se reimplemente aquí es una regresión de régimen (D-08 lo prohíbe).
Output: conector + suite que cae si alguien intenta saltarse el orden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@packages/ingest/src/base-connector.ts
@packages/ingest/src/dummy-connector.ts
@packages/ingest/src/base-connector.test.ts
@packages/dinero/src/connector-servel.ts
@packages/news/src/feeds.ts
@packages/news/src/allowlist-news.ts

<interfaces>
`BaseConnector<Raw>` (packages/ingest/src/base-connector.ts) — hooks abstractos:

```
protected abstract sourceId: string;
protected abstract endpoints(): RequestSpec[];
protected abstract validateShape(body: unknown): Raw;
protected abstract fingerprint(raw: Raw): string | Promise<string>;
constructor(protected readonly deps: ConnectorDeps)
```

`RequestSpec = { url: string; host?: string /* DEPRECATED, WR-01 */; resource: string; key: string; params?; ext?: string /* default "json" */ }`

`ConnectorDeps` (todos inyectables ⇒ tests sin red):
`cache{dailyKey,hasToday}`, `robots{isAllowed}`, `rateLimiter{wait}`, `hostThrottle?{reserve}`,
`fetcher{get}`, `drift{check,alert}`, `r2{putImmutable(source,resource,date,sha,ext,body)}`,
`snapshot{write}`, `log{skip(spec,reason)}`, `now?`.

Orden LOCKED de `run()` (líneas 118-184), NO sobreescribible:
cache.hasToday → (continue) → robots.isAllowed → (log.skip("robots-disallow") + continue) →
hostThrottle.reserve(host) → rateLimiter.wait(host) → fetcher.get → validateShape(decodeJson(body))
→ fingerprint → drift.check/alert → sha256Hex(body) → r2.putImmutable → snapshot.write.
`const host = new URL(spec.url).host` (línea 121) — spec.host se ignora (WR-01).
`decodeJson` devuelve **string** cuando el body no es JSON (líneas 189-198) ⇒ `validateShape`
recibe un `string` XML.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NewsConnector con los 3 hooks y la allowlist scoped en Fetcher Y RobotsGuard</name>
  <read_first>
    - packages/ingest/src/base-connector.ts (completo — contrato y orden LOCKED)
    - packages/ingest/src/dummy-connector.ts:16-42 (forma de los hooks; único `extends BaseConnector` del repo)
    - packages/dinero/src/connector-servel.ts:1-13 y :100-140 (cabecera con prohibiciones + merge de extraHosts)
    - packages/news/src/feeds.ts y allowlist-news.ts (creados en 132-01)
    - 132-RESEARCH.md §Pitfall 1, §Pitfall 3, §Pitfall 5 y §Code Examples (fingerprint estructural)
  </read_first>
  <files>packages/news/src/connector-news.ts</files>
  <action>
    `export type RssRaw = { xml: string }`.
    `export class NewsConnector extends BaseConnector<RssRaw>` con `protected sourceId = "news"`.

    `endpoints()`: `FEEDS.map(f => ({ url: f.url, resource: \`rss-\${f.slug}\`, key: f.slug, ext: "xml" }))`.
    **`resource` ÚNICO por feed** — si los 5 compartieran `resource`, `cache.hasToday` haría que
    solo el primero se descargue y los otros 4 salgan skip para siempre (Pattern 2 del research).
    **`ext: "xml"`** — el default de `RequestSpec.ext` es `"json"` y el SC2 exige la extensión real
    en la key. NO setear `spec.host` (DEPRECATED, WR-01).

    `validateShape(body: unknown): RssRaw`: shape-guard **SUAVE** sobre string —
    `if (typeof body !== "string") throw` ; `if (!/<rss[\s>]|<feed[\s>]/i.test(body)) throw` ;
    `if (!/<item[\s>]/i.test(body)) throw` ; devolver `{ xml: body }`. **NADA de zod aquí** — el zod
    estricto vive en Etapa 2 (D-10). Razón: `decodeJson` entrega un string cuando el body no es JSON.

    `fingerprint(raw)`: **estructural**, jamás sobre los bytes crudos (D-132-C). Calcular el set
    ORDENADO y deduplicado de nombres de tag presentes en el XML
    (`[...new Set([...raw.xml.matchAll(/<([a-zA-Z0-9:_-]+)[\s>/]/g)].map(m => m[1]))].sort().join(",")`)
    y devolver `sha256Hex(new TextEncoder().encode(tags))`. Razón escrita en comentario: los 5 feeds
    emiten `<lastBuildDate>` que cambia en cada corrida; un fingerprint sobre el contenido dispararía
    `drift.alert` en cada corrida (ruido puro) y no detectaría lo que drift existe para detectar
    (cambio de FORMA de la fuente).

    Exportar además `buildNewsDeps(base: Partial<ConnectorDeps> & {...}): ConnectorDeps` o una
    fábrica equivalente que construya `Fetcher` **y** `RobotsGuard` **ambos** con
    `allowlistNews()`. **Pitfall 1:** `RobotsGuard` gatea el propio GET de `robots.txt` con la misma
    allowlist (`robots.ts:105-111`); construirlo con `{ allowlist: {} }` (como hace
    `ingest-cli.ts:272`) daría `HOST_BLOCKED` ⇒ `isAllowed=false` ⇒ los 5 feeds saldrían `[skip]`
    **silenciosamente** con "0 errores". NO copiar ese `{}`.

    Cabecera del archivo declarando: el orden LOCKED que se hereda, que está PROHIBIDO reimplementar
    rate-limit/robots/R2 aquí (D-08), y por qué el fingerprint es estructural (D-132-C).

    Si aparece la necesidad de sobreescribir `run()` o de tocar `@obs/ingest`: **PARAR y escalar**
    (D-132-B ya adjudicó que el framework NO se toca en esta fase).
  </action>
  <acceptance_criteria>
    - `grep -c "extends BaseConnector" packages/news/src/connector-news.ts` == 1.
    - `grep -Ec "async run\(|override run\(" packages/news/src/connector-news.ts` == 0 — el conector NO redefine el Template Method.
    - `grep -Ec "setTimeout|robots-parser|aws4fetch|putImmutable\s*\(" packages/news/src/connector-news.ts` == 0 — cero reimplementación de política (D-08).
    - `grep -c "allowlistNews" packages/news/src/connector-news.ts` ≥ 2 (una para `Fetcher`, otra para `RobotsGuard`) y el test del Task 2 lo prueba por comportamiento, no solo por grep.
    - `grep -c 'ext: "xml"' packages/news/src/connector-news.ts` == 1.
    - `pnpm typecheck` sale 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec tsc -b</automated>
  </verify>
  <done>El conector implementa exactamente 3 hooks, hereda el orden LOCKED sin tocarlo, y su allowlist scoped entra en Fetcher y en RobotsGuard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests estructurales SC1 + parte unitaria del SC2</name>
  <read_first>
    - packages/ingest/src/base-connector.test.ts (donde el comportamiento del orden LOCKED ya está ejercitado — copiar la forma de los dobles inyectados)
    - packages/news/src/connector-news.ts (tal como quedó en Task 1)
    - packages/ingest/src/r2-store.ts:56-100 (firma de putImmutable y forma de la key)
  </read_first>
  <files>packages/news/src/connector-news.test.ts</files>
  <behavior>
    - SC1-a: con 5 endpoints y todo permitido, `rateLimiter.wait` se llama **5 veces**, una por feed, y cada llamada ocurre ANTES del `fetcher.get` correspondiente (registrar el orden en un array de eventos e inspeccionarlo, no solo contar).
    - SC1-b: el host recibido por `rateLimiter.wait` y por `robots.isAllowed` corresponde a `new URL(spec.url).host`; si el test inyecta un `spec.host` distinto vía un endpoint manipulado, el host usado sigue siendo el de la URL (WR-01).
    - SC1-c: si `robots.isAllowed` devuelve false para un feed, `fetcher.get` NO se llama para ese feed y `log.skip` recibe `(spec, "robots-disallow")`.
    - SC1-d: `fetcher.get` nunca se llama para dos feeds sin un `rateLimiter.wait` intercalado (cero ráfagas) — verificar sobre la traza de eventos.
    - SC2-a: `r2.putImmutable` recibe `("news", "rss-<slug>", "<YYYY-MM-DD>", "<sha256 de 64 hex>", "xml", body)` y el `r2Path` resultante matchea `^news/rss-[a-z]+/\d{4}-\d{2}-\d{2}/[0-9a-f]{64}\.xml$`.
    - SC2-b: si `cache.hasToday` devuelve true para todos, `fetcher.get` se llama **0 veces** y `run()` devuelve `[]`.
    - SC2-c: el body que llega a `putImmutable` es el **crudo** del fetch (byte-idéntico), no lo que devuelve `validateShape`.
    - Forma: `validateShape` acepta el fixture real de cada uno de los 5 feeds; rechaza un string `"{}"`, un JSON object, y un HTML sin `<item>`.
    - Fingerprint: dos XML que difieren SOLO en `<lastBuildDate>` producen el MISMO fingerprint; un XML con un tag nuevo produce uno distinto.
  </behavior>
  <action>
    Escribir `connector-news.test.ts` con dobles inyectados por `ConnectorDeps` (cero red, cero DB).
    Los fixtures de `src/__fixtures__/*.xml` (capturados en 132-01) alimentan los casos de forma y
    de fingerprint. Usar `deps.now = () => new Date("2026-08-05T12:00:00Z")` para que el
    `dateBucket` sea determinista.
    Un doble `fetcher` que registre en la traza de eventos, y un `rateLimiter` que registre
    `wait:<host>`; los asserts operan sobre la traza (`["wait:www.biobiochile.cl","get:...", ...]`).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/connector-news.test.ts` reporta `Tests  N passed` con N ≥ 10 (verificar el CONTEO impreso; el exit code solo no basta — `passWithNoTests: true` está activo).
    - **Mutaciones obligatorias, cada una registrada en el SUMMARY con el nombre del test que cayó:**
      1. En `connector-news.ts`, cambiar `ext: "xml"` por `ext: "json"` ⇒ el test de la key de R2 FALLA. Revertir.
      2. Cambiar `resource: \`rss-${f.slug}\`` por un literal fijo `"rss"` ⇒ un test debe FALLAR (los 5 endpoints ya no tienen resource único). Revertir. *(Si no falla, el test no cubre Pattern 2 — hay que escribirlo.)*
      3. Construir `RobotsGuard` con `{ allowlist: {} }` ⇒ un test debe FALLAR demostrando que los feeds quedarían HOST_BLOCKED. Revertir.
      4. Hacer que `fingerprint` devuelva `sha256(raw.xml)` ⇒ el test de `<lastBuildDate>` FALLA. Revertir.
    - Ningún test usa red: `grep -Ec "fetch\(|https?://(www|news)\." packages/news/src/connector-news.test.ts` — las URLs solo pueden aparecer como datos de fixture/endpoint, y no debe existir ningún `global.fetch` real; inyectar un `fetcher` doble que **lance** si se le pide un host no esperado.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run src/connector-news.test.ts</automated>
  </verify>
  <done>Los tests demuestran, por comportamiento observado, que robots y rate-limit preceden a cada fetch, que el host se deriva de la URL, que la caché diaria apaga el fetch y que la key de R2 es la del SC2 — y las 4 mutaciones probaron que no son vacuos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector → hosts de prensa | request saliente a host no gubernamental |
| XML de tercero → `validateShape` | entrada no confiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-07 | Elevation of Privilege | `RobotsGuard` construido sin allowlist | mitigate | `allowlistNews()` inyectado en Fetcher Y RobotsGuard; test que cae con `{ allowlist: {} }` (mutación 3) |
| T-132-08 | Denial of Service (al tercero) | orden de `run()` | mitigate | Herencia de `BaseConnector.run()` sin override; test de traza que prueba `wait` antes de cada `get` |
| T-132-09 | Denial of Service (XXE / entity expansion) | `validateShape` | accept | En Etapa 1 no se parsea XML (shape-guard por regex sobre string); el parseo va con `fast-xml-parser` (no resuelve entidades externas) en Etapa 2 |
</threat_model>

<verification>
- `pnpm --filter @obs/news test` verde con conteo ≥ 10 tests en este archivo.
- Las 4 mutaciones registradas en el SUMMARY con el test que cayó en cada caso.
</verification>

<success_criteria>
- El conector no redefine `run()` ni reimplementa política (greps == 0).
- La traza de eventos prueba orden robots→rate-limit→fetch por cada feed.
- La key de R2 matchea `^news/rss-[a-z]+/\d{4}-\d{2}-\d{2}/[0-9a-f]{64}\.xml$`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-03-SUMMARY.md` when done
</output>
