---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 06
type: execute
wave: 4
depends_on: ["132-03", "132-05"]
files_modified:
  - packages/news/src/run-news-cli.ts
  - packages/news/src/run-news-cli.test.ts
  - packages/news/src/replay.test.ts
  - packages/news/src/index.ts
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "Un replay `--from-r2` reproduce la carga completa con la red PROHIBIDA (fetch que lanza)"
    - "El CLI reporta `[skip]` por feed cuando la caché diaria evitó la descarga"
    - "Sin R2 configurado el CLI FALLA duro; no degrada en silencio"
    - "El CLI corre bajo `pnpm --filter` sin romperse por `process.cwd`"
    - "El framework compartido `@obs/ingest` no fue modificado por esta fase"
  artifacts:
    - path: "packages/news/src/run-news-cli.ts"
      provides: "CLI local Etapa1+Etapa2 con --dry-run, --from-r2, --feeds"
      contains: "--from-r2"
    - path: "packages/news/src/replay.test.ts"
      provides: "Prueba del SC3 con fetch que LANZA"
    - path: "packages/news/src/index.ts"
      provides: "Barrel FINAL del paquete (≥ 8 símbolos). REEMPLAZA el placeholder que dejó 132-01 T1; este plan (wave 4) es su dueño, 132-01 es wave 1 ⇒ no hay colisión de escritura en paralelo"
  key_links:
    - from: "packages/news/src/run-news-cli.ts"
      to: "R2Store.getObject"
      via: "--from-r2, única fuente de datos del replay"
      pattern: "getObject"
---

<objective>
El CLI local que ata las dos etapas y **materializa el SC3**: `--from-r2` reproduce la carga
completa leyendo EXCLUSIVAMENTE del crudo en R2, con la red prohibida. Además deriva el `[skip]`
observable del SC2 sin tocar `@obs/ingest` (D-132-B).

Purpose: sin el CLI no hay corrida de verificación (plan 07) ni evidencia de los SC2/SC3.
Output: `run-news-cli.ts` + suite de replay que cae si la rama `--from-r2` se elimina.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@packages/tramitacion/src/ingest-cli.ts
@packages/tramitacion/src/run-tramitacion-prod-cli.ts
@packages/tramitacion/src/index.ts
@packages/news/src/connector-news.ts
@packages/news/src/carga-run.ts

<interfaces>
`R2Store` (packages/ingest/src/r2-store.ts):
```
constructor(cfg: R2Config /* {accessKeyId, secretAccessKey, endpoint, bucket} */, opts?)
async putImmutable(source, resource, date, sha, ext, body): Promise<{ r2Path: string; existed: boolean }>
async getObject(r2Path: string): Promise<Uint8Array>
```
`BaseConnector.run(ctx: IngestRun): Promise<SnapshotRef[]>` — devuelve una ref por endpoint
efectivamente descargado. `run()` hace `continue` SILENCIOSO en cache-hit (`base-connector.ts:124`,
**no llama a `log.skip`**) ⇒ el `[skip]` se deriva comparando endpoints pedidos vs `refs` (D-132-B).

`buildNewsDeps(opts?)` (packages/news/src/connector-news.ts, plan 03) construye `Fetcher` y
`RobotsGuard` con `allowlistNews()` y admite un `fetchFn` inyectable **solo para test**: el CLI lo
omite siempre ⇒ en producción se usa el fetch global.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run-news-cli.ts — flags, R2 obligatorio, [skip] derivado, barrel index.ts</name>
  <read_first>
    - packages/tramitacion/src/ingest-cli.ts:80-161 (parseArgs con error tipado, findWorkspaceRoot LITERAL — Pitfall 9 `process.cwd` bajo `pnpm --filter exec`)
    - packages/tramitacion/src/ingest-cli.ts:200-266 (construcción tri-estado de R2Store, rama --from-r2)
    - packages/tramitacion/src/ingest-cli.ts:279-346 (selección de writer, log de errores, guard de entry-point y exit codes)
    - packages/tramitacion/src/index.ts (barrel por olas)
    - packages/news/src/connector-news.ts y carga-run.ts
  </read_first>
  <files>packages/news/src/run-news-cli.ts, packages/news/src/index.ts</files>
  <action>
    `export class NewsCliArgsError extends Error` (`this.name` propio) y
    `export function parseArgs(argv): NewsCliOptions` soportando:
      `--dry-run`            → writer in-memory, sin escribir a Supabase
      `--from-r2 <r2Path>`   → replay: ÚNICA fuente de datos = `r2Store.getObject(r2Path)`
      `--feeds <slug,slug>`  → subconjunto de `FEEDS` (para no golpear los 5 innecesariamente)
      `--etapa1|--etapa2`    → correr solo una etapa
    Flag desconocido ⇒ `NewsCliArgsError` y `process.exit(2)` ANTES de tocar red o DB.

    **Semántica de `--dry-run` — documentarla en la cabecera, sin ambigüedad (premortem F-2):**
    `--dry-run` cambia **solo el writer de la Etapa 2**. La Etapa 1 sigue pasando por
    `BaseConnector.run()`, que escribe `source_snapshot` **incondicionalmente**, y `DailyCache`
    lee de ahí ⇒ **un `--dry-run` de Etapa 1 consume la caché diaria del día**. Escribir esa
    advertencia literal en el `--help` y en la cabecera del archivo. La rama `--from-r2` **no** pasa
    por `run()` y por tanto NO consume la caché: es el único modo seguro de repetir.

    Copiar `findWorkspaceRoot` (o importarlo de `@obs/tramitacion` si ya está exportado — decidir
    leyendo el barrel; preferir importar antes que duplicar).

    Construcción de `R2Store` desde env (`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`,
    `R2_ENDPOINT_URL`, `R2_BUCKET`), inyectable tri-estado (`opts.r2Store === null` = forzar sin R2;
    `undefined` = construir desde env). **DIFERENCIA CON EL ANALOG:** tramitación degrada con
    `[WARN] R2 no configurado`. Para news eso **NO es aceptable** — R2 es el corazón de SC2/SC3.
    Sin R2 y sin `--dry-run`, el CLI debe **fallar duro** con mensaje explícito y exit 2.

    Etapa 1: instanciar `NewsConnector` con las deps de `buildNewsDeps()` (**sin** `fetchFn`: en
    producción se usa el fetch global), `HostRateLimiter` (2-3 s), `PgHostThrottle` si hay conexión,
    `DailyCache` + `SupabaseSnapshotStore`, `R2Store`, `DriftDetector`. Llamar `connector.run(ctx)`.
    **`[skip]` observable (D-132-B):** comparar los slugs pedidos con los `resource` presentes en
    `refs` y emitir una línea `[skip] rss-<slug> (cache-hit del día)` por cada faltante, más un
    resumen `descargados=N skips=M`. **Prohibido modificar `@obs/ingest`** para esto.

    Etapa 2: por cada `SnapshotRef` (o por cada `--from-r2`), `r2Store.getObject(ref.r2Path)` →
    `parseRss(new TextDecoder().decode(bytes), outlet)` → `cargar({...})`. **Sin envelope JSON** —
    news guarda el XML crudo tal cual, así que no se copia la indirección de conectores fake del
    analog.

    Writer: `--dry-run` ⇒ `InMemoryNewsWriter`; si no, `SupabaseNewsWriter` con URL y service key
    **desde env** (`.env` BOM-safe, patrón `run-tramitacion-prod-cli.ts:1-45`). Credenciales JAMÁS
    por argv. El project-ref NO se transcribe en el código (B26).

    Guard de entry-point con regex `/run-news-cli\.(ts|js|mjs|cjs)$/`; exit 1 si hay errores, 0 si no.

    `index.ts`: **este archivo YA EXISTE** — 132-01 T1 (wave 1) lo creó como **placeholder mínimo** para que
    el proyecto `composite` tuviera inputs (sin ningún `.ts` en `src/`, `tsc -b` aborta con TS18003 y rompe
    `pnpm typecheck` de toda la wave 1). Aquí se **REEMPLAZA por completo** por el barrel final: borrar el
    export placeholder (`NEWS_PACKAGE`) y su cabecera de scaffolding. No es una colisión de posesión: 132-01
    es wave 1 y este plan es wave 4, nunca corren en paralelo. Barrel del paquete, agrupado por olas del plan, con `export type {}` separado de
    `export {}` (convención `packages/tramitacion/src/index.ts`). Exportar: `FEEDS`, `NEWS_HOSTS`,
    `allowlistNews`, `NewsConnector`, `buildNewsDeps`, `RssRaw`, `parseRss`, `RssItem`,
    `canonicalizarUrl`, `urlHash`, `esLegislativo`, `VOCABULARIO_LEGISLATIVO`, `NewsWriter`,
    `InMemoryNewsWriter`, `SupabaseNewsWriter`, `cargar`, `parseArgs`, `NewsCliArgsError`.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` sale 0 (**exit code capturado por separado**, nunca encadenado con `&&` a otra aserción — si `tsc -b` falla con código 2, un `test $? -eq 2` posterior saldría verde sin haber ejecutado nada; gotcha registrado en el premortem F-5).
    - `pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- --flag-inexistente` sale con código **2** e imprime el nombre del flag. **Cómo medirlo sin falso ROJO (F-5 bis):** un comando que DEBE fallar NO puede invocarse "a pelo" bajo `set -e` — bash aborta el script antes de que se lea `$?` y el criterio se vuelve **inalcanzable** (falso rojo que empuja a improvisar). Capturar SIEMPRE dentro de un `if`, forma que `set -e` no intercepta:
      `if pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- --flag-inexistente > /tmp/132-cli.log 2>&1; then rc=0; else rc=$?; fi; test "$rc" -eq 2; grep -q -- "--flag-inexistente" /tmp/132-cli.log`
      Tampoco puede pasar en vacío: exige el código **2 exacto** (no "≠ 0") y la presencia literal del flag en la salida.
      **Separador `--` obligatorio (warning ronda 2):** sin él, `pnpm` puede **interceptar** el flag desconocido y
      fallar él mismo con SU propio código de salida — el criterio mediría el parser de pnpm, no el `parseArgs` del
      CLI, y el `2` sería coincidencia o falso rojo. El `--` fuerza que todo lo que sigue llegue intacto al script.
    - **Fallo duro sin R2 — se prueba por el TRI-ESTADO, no por el entorno.** El criterio `env -u R2_ACCESS_KEY_ID …` queda **PROHIBIDO como evidencia**: es inalcanzable por construcción, porque esta misma tarea ordena cargar `.env` desde el workspace root (patrón `run-tramitacion-prod-cli.ts:1-45`) y `.env` contiene esas variables ⇒ el `env -u` se revierte dentro del proceso y el CLI arrancaría igual. Intentar "arreglarlo" degradando el fallo duro sería romper el truth "falla duro sin R2".
      **Evidencia válida (Task 2, `run-news-cli.test.ts`):** invocar `main({ r2Store: null, dryRun: false, … })` — tri-estado `null` = forzar sin R2 — debe **fallar duro** (lanzar / resolver con código 2) con un error assertado por tipo y con `message` que contenga `R2`; **apareado con** `main({ r2Store: fake, dryRun: false, … })`, idéntico salvo `r2Store`, que **SÍ** completa. La **mutación obligatoria** (quitar el fallo duro ⇒ cae el negativo y sigue pasando el positivo) es lo que impide que el criterio se cumpla en vacío. **No** debe existir ninguna rama que imprima `[WARN]` y continúe. El `env -u` baja a comprobación **auxiliar opcional**: si se corre, su resultado se anota en el SUMMARY como informativo — nunca como criterio de cierre.
    - `grep -Ec "\[WARN\] R2 no configurado" packages/news/src/run-news-cli.ts` == 0.
    - `grep -c "findWorkspaceRoot" packages/news/src/run-news-cli.ts` ≥ 1 (Pitfall 9).
    - `grep -c "cache diaria\|caché diaria" packages/news/src/run-news-cli.ts` ≥ 1 — la advertencia de que `--dry-run` de Etapa 1 consume la caché del día está escrita en el código (F-2).
    - `grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" packages/news/src/run-news-cli.ts` == 0 (B26 + secretos).
    - **El framework compartido no se toca (D-132-B), medido contra el commit BASE de la fase, no contra el working tree.** `git diff --name-only` a secas **siempre pasa** bajo GSD porque cada tarea commitea de forma atómica y el árbol queda limpio — criterio vacuo. Usar el **SHA literal** del commit de creación de los planes:
      `BASE=90580a2; git diff --name-only "$BASE"..HEAD | grep -c '^packages/ingest/'` == **0**.
      **Por qué SHA literal y no `git rev-list --grep` (warning ronda 2):** `--grep` devuelve el commit **más
      reciente** que cite la cadena, así que cualquier commit futuro que la mencione (un SUMMARY, un fix, este
      mismo mensaje de revisión) **desplaza el BASE hacia adelante** y encoge la ventana del diff hasta volverla
      vacía — el criterio pasaría sin haber mirado nada. `90580a2` (`docs(132): 7 planes en 5 waves para NEWS-RSS`)
      es inmutable; verificado que resuelve a ese commit.
      Registrar en el SUMMARY el SHA de `BASE` y la lista completa de archivos del diff acumulado.
    - **El barrel exporta de verdad** (criterio ejecutable; el `node -e "import('...ts')"` sobre TS puro NO funciona y no cuenta como verificación): un test (`run-news-cli.test.ts`) importa ≥ 8 símbolos desde `../src/index.ts` y assert que ninguno es `undefined`; el conteo de símbolos comprobados se cita en el SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; pnpm --filter @obs/news exec tsc -b; echo "tsc=OK"; if pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- --flag-inexistente > /tmp/132-cli.log 2>&1; then rc=0; else rc=$?; fi; echo "cli_rc=$rc"; test "$rc" -eq 2; grep -q -- "--flag-inexistente" /tmp/132-cli.log; BASE=90580a2; echo "base=$BASE"; git cat-file -e "$BASE^{commit}"; test "$(git diff --name-only "$BASE"..HEAD | grep -c "^packages/ingest/")" = "0"'</automated>
  </verify>
  <done>El CLI parsea flags con error tipado, exige R2, documenta que `--dry-run` consume la caché diaria, deriva el `[skip]` sin tocar el framework, y el barrel exporta la superficie del paquete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [SC3] Test de replay con la red PROHIBIDA + test del [skip] derivado</name>
  <read_first>
    - packages/news/src/run-news-cli.ts (tal como quedó)
    - 132-RESEARCH.md §Pattern 3 y §Pitfall 10 (un "test de replay" que nunca toca `r2Store.getObject` certifica una omisión)
    - packages/news/src/carga-run.ts
  </read_first>
  <files>packages/news/src/replay.test.ts, packages/news/src/run-news-cli.test.ts</files>
  <behavior>
    - **SC3:** con `globalThis.fetch` reemplazado por una función que LANZA `Error("RED PROHIBIDA EN REPLAY")`, y con un `R2Store` doble que sirve los bytes del fixture de La Tercera desde `getObject(r2Path)`, `main({ fromR2: "news/rss-latercera/2026-08-05/<sha>.xml", dryRun: true, r2Store: fake, writer: inMemory })` completa y carga el mismo nº de noticias que la carga directa del mismo fixture.
    - El doble de `R2Store` registra que `getObject` fue llamado **exactamente 1 vez** con el `r2Path` pasado — prueba de que el dato vino de R2 y no de un fixture local en memoria (Pitfall 10).
    - El replay **no** escribe `source_snapshot`: el doble de snapshot store registra 0 escrituras — es lo que hace del replay el único modo repetible sin consumir la caché del día (F-2).
    - `--from-r2` sin R2 configurado lanza `NewsCliArgsError`.
    - **Fallo duro sin R2 (evidencia canónica del truth, sustituye al `env -u`) — con control positivo que
      varía UNA SOLA VARIABLE (blocker ronda 2):**
      · Caso negativo: `main({ r2Store: null, dryRun: false, … })` — tri-estado `null` = forzar sin R2 — falla
        duro con código/estado **2**, y el error assertado **por tipo y por mensaje**: es la clase de error del
        fallo duro (no un `TypeError` ni un `NewsCliArgsError` genérico) y su `message` **contiene `R2`**.
        Assertar solo "lanzó" aceptaría un `undefined is not a function` como si fuera el fallo duro.
      · Caso positivo apareado: `main({ r2Store: fake, dryRun: false, … })` con el **mismo input** completa sin
        lanzar. **Difiere del negativo únicamente en `r2Store`** (`fake` vs `null`); `dryRun` se mantiene en
        `false` en AMBOS.
      · **Por qué (blocker ronda 2):** el par anterior variaba `r2Store` **y** `dryRun` a la vez, así que no
        aislaba la causa — el caso positivo podía estar completando por ser dry-run, no por tener R2, y el
        negativo podía estar fallando por cualquier otra cosa del camino no-dry-run. Un control apareado que
        mueve dos variables no controla nada.
      · **ninguna** salida de ninguno de los dos casos contiene `[WARN]`.
    - **Cero red en `run-news-cli.test.ts` — obligatorio y medido (warning ronda 3):** ambos casos del par apareado (`r2Store: null` y `r2Store: fake`) corren `main()` con `dryRun: false`, es decir por el camino de Etapa 1; si el ejecutor no inyecta un **conector doble**, ese camino construiría las deps reales y **golpearía los N feeds vivos en cada `pnpm test`** — la suite se volvería un scraper contra terceros, violando el régimen de ingesta respetuosa. Por tanto:
      · **AMBOS** casos reciben el mismo **conector doble** inyectado (su `run()` devuelve `refs` de fixture); la única variable que difiere entre negativo y positivo sigue siendo `r2Store`.
      · A nivel de ARCHIVO (top-level o `beforeEach`): `vi.stubGlobal("fetch", () => { throw new Error("red prohibida en tests"); })`, restaurado en `afterEach`. No basta “no llamarlo”: debe explotar si alguien lo llama.
      · El archivo **no construye** un `Fetcher` real — verificado por grep en el `<automated>`, mismo régimen que 132-03 T2.
    - **SC2 (`[skip]` derivado):** con un conector doble cuyo `run()` devuelve `refs` para solo 2 de 5 feeds, el CLI emite 3 líneas `[skip]` con los slugs faltantes y un resumen `descargados=2 skips=3`.
    - Etapa 1 con caché llena: 0 llamadas a `fetcher.get` y 5 líneas `[skip]`.
    - `parseArgs`: `--from-r2` sin argumento lanza; flag desconocido lanza; `--feeds latercera,exante` produce 2 endpoints.
    - Barrel: ≥ 8 símbolos importados de `../src/index.ts` y ninguno `undefined`.
  </behavior>
  <action>
    Escribir `replay.test.ts` y `run-news-cli.test.ts` con todos los colaboradores inyectados.
    El `fetch` que lanza se instala con `vi.stubGlobal("fetch", () => { throw new Error("RED PROHIBIDA EN REPLAY"); })`
    y se restaura en `afterEach`. **No basta con "no llamarlo": debe explotar si alguien lo llama.**
    Comparar el resultado del replay contra el resultado de `cargar()` invocado directamente con los
    ítems del mismo fixture — mismos conteos `vistos/cargados/descartados`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/replay.test.ts src/run-news-cli.test.ts` reporta `Tests  N passed` con N ≥ 10 (verificar el CONTEO impreso, no el exit code — `vitest run <glob>` sale 0 sin correr nada si el glob no matchea; acá se pasan rutas literales, no globs).
    - **Mutación rectora del SC3 (registrar en el SUMMARY):** eliminar la rama `if (opts.fromR2)` de `run-news-cli.ts` ⇒ `replay.test.ts` debe FALLAR. Revertir. Si NO falla, el test está certificando una omisión — reescribirlo.
    - **Mutación del anti-Pitfall-10:** hacer que el replay lea el fixture del disco en vez de `r2Store.getObject` ⇒ el assert de "getObject llamado 1 vez" FALLA. Revertir.
    - **Mutación del `[skip]`:** eliminar la comparación endpoints-vs-refs ⇒ el test de las 3 líneas `[skip]` FALLA. Revertir.
    - **Mutación del fallo duro sin R2 (obligatoria):** reemplazar el fallo duro por un `[WARN]`+continuar ⇒ el test de `r2Store: null` debe FALLAR **y** el control positivo apareado (`r2Store: fake, dryRun: false`) debe seguir **pasando**. Si caen los dos, el test no está midiendo el fallo duro sino cualquier error — reescribirlo. Registrar en el SUMMARY el resultado de AMBOS casos bajo la mutación, no solo el que cae. Revertir.
    - **Mutación del no-snapshot en replay:** hacer que la rama `--from-r2` escriba un `source_snapshot` ⇒ el test de "0 escrituras de snapshot" FALLA. Revertir.
    - El test del `fetch` que lanza incluye un control positivo: un caso que SÍ llama a `fetch` y comprueba que efectivamente explota (prueba de que el stub está instalado; sin él, "no explotó" no significa nada).
    - **Gate de cero-red DENTRO del `<automated>` (warning ronda 3), patrón 132-03 T2 pero invertido:** allá se exige la AUSENCIA del stub, acá su PRESENCIA — porque acá el stub *es* la prohibición de red.
      `grep -c 'vi.stubGlobal("fetch"' packages/news/src/run-news-cli.test.ts` ≥ 1 · `grep -c "red prohibida en tests" …` ≥ 1 · `grep -Ec "new Fetcher\\(" …` == 0.
      Smoke-test (ronda 3, bash real): archivo con el stub y el conector doble → `stub=1 msg=1 realfetcher=0` ⇒ pasa; archivo con `new Fetcher({ allowlist: allowlistNews() })` → `stub=0 msg=0 realfetcher=1` ⇒ los tres gates muerden. No puede salir 0 sin el stub instalado, ni es inalcanzable.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-06-t2.log; T=packages/news/src/run-news-cli.test.ts; pnpm --filter @obs/news exec vitest run src/replay.test.ts src/run-news-cli.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 10; test "$(grep -c "vi.stubGlobal(\"fetch\"" "$T")" -ge 1; test "$(grep -c "red prohibida en tests" "$T")" -ge 1; test "$(grep -Ec "new Fetcher\(" "$T")" = "0"'</automated>
  </verify>
  <done>El replay `--from-r2` está probado con la red literalmente prohibida (stub de `fetch` que lanza, presencia verificada por grep) y con conector doble en AMBOS casos del par apareado, con evidencia de que el dato vino de `getObject` y de que no consume la caché diaria; el `[skip]` derivado tiene test que cae si se quita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv/env → CLI | flags y credenciales de entrada |
| R2 → proceso | bytes de crudo previamente escritos por nosotros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-16 | Information Disclosure | credenciales por argv | mitigate | Service key y R2 SOLO desde env/`.env`; `parseArgs` no acepta ningún flag de credencial; grep de `eyJ` == 0 |
| T-132-17 | Spoofing (falso verde) | degradación silenciosa sin R2 | mitigate | Fallo duro exit 2 en vez de `[WARN]`; test que verifica el exit code medido en su propia variable |
| T-132-18 | Tampering | `--from-r2` con un r2Path arbitrario | accept | El path lo provee el operador local sobre nuestro propio bucket; `getObject` no ejecuta contenido, solo lo parsea con `fast-xml-parser` |
| T-132-26 | Denial of Service (auto-infligido) | `--dry-run` de Etapa 1 consume la caché diaria | mitigate | Advertencia literal en `--help` y en la cabecera; el replay `--from-r2` no pasa por `run()` y tiene test de 0 escrituras de snapshot |
</threat_model>

<verification>
- `pnpm test` (raíz) y `pnpm typecheck` verdes, con el exit code de cada comando capturado por separado.
- `git diff --name-only 90580a2..HEAD` (SHA literal del commit de creación de los planes; **no** `--grep`, que se desplaza) no toca `packages/ingest/`.
- Las 4 mutaciones registradas en el SUMMARY con el test que cayó.
</verification>

<success_criteria>
- El replay reproduce la carga con `fetch` que lanza, `getObject` llamado 1 vez y 0 escrituras de snapshot.
- El CLI emite `[skip]` por feed no descargado y falla duro sin R2.
- Diff acumulado de la fase contra el commit base: cero archivos bajo `packages/ingest/`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-06-SUMMARY.md` when done
</output>
