---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 04
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - packages/news/src/model.ts
  - packages/news/src/parse-rss.ts
  - packages/news/src/parse-rss.test.ts
  - packages/news/src/canonicalizar-url.ts
  - packages/news/src/canonicalizar-url.test.ts
  - packages/news/src/prefiltro-lexico.ts
  - packages/news/src/prefiltro-lexico.test.ts
autonomous: true
requirements: [NEWS-02]

must_haves:
  truths:
    - "Los 5 feeds reales se parsean a ítems con titular, link, fecha y descripción"
    - "La fecha respeta el offset del feed (Cooperativa -0400) y jamás se normaliza a una tz global"
    - "La URL canónica descarta parámetros de tracking y produce un sha256 estable"
    - "El pre-filtro léxico es determinista, recall-first, opera sobre texto despojado y matchea por PALABRA, no por subcadena"
  artifacts:
    - path: "packages/news/src/parse-rss.ts"
      provides: "XML crudo → RssItem[] (puro, sin red)"
      contains: "XMLParser"
    - path: "packages/news/src/canonicalizar-url.ts"
      provides: "canonicalizarUrl + urlHash (sha256 de la canónica) — D-13"
    - path: "packages/news/src/prefiltro-lexico.ts"
      provides: "VOCABULARIO_LEGISLATIVO congelado + esLegislativo() con frontera de palabra — D-05/D-06"
      contains: "Object.freeze"
  key_links:
    - from: "packages/news/src/parse-rss.ts"
      to: "packages/news/src/__fixtures__/*.xml"
      via: "tests sobre los 5 feeds reales"
      pattern: "__fixtures__"
---

<objective>
Escribir la lógica **pura** de la Etapa 2: parseo del RSS, canonicalización de URL (dedup nivel 1-2,
D-13) y pre-filtro léxico legislativo determinista (D-05/D-06). Cero red, cero DB, cero LLM.
Todo verificado contra los 5 XML reales capturados en 132-01.

Purpose: es el corazón del SC4 ("el pre-filtro léxico determinista descarta antes de gastar LLM").
Al ser puro, es 100% testeable sin infraestructura, y es donde vive el riesgo de falsos negativos
(una noticia legislativa descartada se pierde para siempre).
Output: tres módulos puros + suites sobre fixtures reales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-RESEARCH.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@packages/tramitacion/src/parse-senado-tramitacion.ts
@packages/tramitacion/src/model.ts
@packages/tramitacion/src/fecha.ts
@packages/core/src/nombre.ts
@packages/news/src/feeds.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model.ts (zod) + parse-rss.ts contra los 5 fixtures reales</name>
  <read_first>
    - packages/tramitacion/src/parse-senado-tramitacion.ts:1-70 (config del XMLParser, helper `txt`, gotcha de `#text` vs nodo nested, gotcha de listas con `[].concat`)
    - packages/tramitacion/src/model.ts (convención `XSchema` + tipo derivado)
    - packages/tramitacion/src/fecha.ts (helpers puros de fecha con test)
    - packages/news/src/__fixtures__/*.xml (los 5 crudos reales)
    - 132-RESEARCH.md §Campos disponibles por ítem y §Pitfall 6 y §Pitfall 7
  </read_first>
  <files>packages/news/src/model.ts, packages/news/src/parse-rss.ts, packages/news/src/parse-rss.test.ts</files>
  <behavior>
    - Los 5 fixtures parsean sin lanzar y producen el nº de ítems observado por el research: biobiochile 20, cooperativa 15, latercera 100, lacuarta 100, exante 10 (aceptar el conteo real del fixture capturado; congelarlo por fixture).
    - Cada ítem tiene `titulo` no vacío y `link` que empieza con `https://`.
    - `<guid>` NO se usa como identidad: para el fixture de biobiochile, `guid` (`?p=<id>`) difiere del `link`, y el ítem resultante conserva el `link` como fuente de identidad.
    - Un feed con un solo `<item>` (fixture sintético mínimo) devuelve 1 ítem, no revienta (fast-xml-parser colapsa nodo único a objeto ⇒ `[].concat`).
    - `pubDate` de Cooperativa (`... -0400`) produce un instante cuyo `toISOString()` es exactamente 4 horas mayor que el reloj local del feed; el de La Tercera (`+0000`) se conserva tal cual. **NUNCA** normalizar a una tz global.
    - CDATA en `<title>` (Cooperativa) se desenvuelve; entidades escapadas (`&quot;` en BioBioChile) quedan como el carácter real.
    - Un XML sin `<item>` produce `[]` (no lanza) y un XML mal formado lanza un error tipado.
  </behavior>
  <action>
    `model.ts`: `RssItemSchema` (zod) con `titulo: string`, `link: string.url()`,
    `guid: string.nullable()`, `fechaPub: string.nullable()` (ISO con offset preservado),
    `descripcion: string.nullable()`, `outlet: string` (slug del feed); tipo `RssItem` derivado.
    Comentario explícito: `fechaPub` se persiste en columna `timestamptz`; el offset del feed se
    respeta; jamás se normaliza a tz global (gotcha rector v12.0/v9.0 — normalizar fabricaría días).

    `parse-rss.ts`: `export function parseRss(xml: string, outlet: string): RssItem[]`.
    `new XMLParser({ ignoreAttributes: false, parseTagValue: false })` — `parseTagValue: false`
    evita que un `<guid>` numérico se convierta a número.
    Forzar `channel.item` a array con `[].concat(...)`.
    Helper `txt(v)` copiado del analog: soporta string, `{ "#text": ... }`, y devuelve null en vacío.
    Para `<dc:creator>`/`<source>`/`<media:content>`: leer la clave real primero, `#text` solo como
    fallback (fast-xml-parser 5.x parsea `<a><B>x</B></a>` como `{B:"x"}`). **No modificar `txt()`
    genérico para acomodar un caso** — el analog es explícito en eso.
    Validar cada ítem con `RssItemSchema.safeParse`; los ítems inválidos se omiten y se acumulan en
    un `errores[]` devuelto por el caller (degradación honesta), no abortan el parseo del feed.

    **Pureza del módulo:** `parse-rss.ts` no importa nada de red ni llama a `fetch`. Los ejemplos de
    URL en la documentación van en el JSDoc del archivo, no en el cuerpo — así el criterio de pureza
    es un grep ejecutable sobre el código sin comentarios (ver acceptance).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/parse-rss.test.ts` reporta `Tests  N passed` con N ≥ 12 (verificar el conteo impreso).
    - Los conteos de ítems por fixture están congelados como literales en el test y coinciden con `grep -c "<item" <fixture>` (nota: los fixtures son multilinea, así que `grep -c` es válido aquí; documentar el número en el SUMMARY).
    - **Mutaciones obligatorias (registrar cuál test cayó):** (1) quitar el `[].concat` que fuerza array ⇒ el test del feed de un solo ítem FALLA; (2) reemplazar el parseo de `pubDate` por `new Date(s).toISOString().slice(0,10)` o cualquier normalización a una tz fija ⇒ el test de Cooperativa `-0400` FALLA; (3) usar `guid` en vez de `link` como identidad ⇒ el test de biobiochile FALLA. Revertir las tres.
    - **Pureza (criterio ejecutable, sin la cláusula inejecutable "salvo en comentarios"):** despojar comentarios ANTES de contar —
      despojar las **líneas** de comentario y contar sobre el resto:
      `grep -v "^[[:space:]]*//" packages/news/src/parse-rss.ts | grep -v "^[[:space:]]*\*" | grep -v "^[[:space:]]*/\*" | grep -Ec "fetch\(|https?://"` == **0**.
      (Gotcha v12.0: `grep -c` no distingue comentarios; un criterio "== 0 salvo en comentarios" es auto-contradictorio y se declara cumplido a ojo.)
    - **Por qué NO `sed -E 's://.*::'` (warning ronda 2).** Ese sed destruye `https://` ANTES del grep: convierte
      `const u = "https://x.cl"` en `const u = "https:` ⇒ la alternativa `https?://` **nunca puede matchear** y esa
      mitad del criterio queda muerta (pasa siempre, aunque haya una URL cableada en el código). El filtro por
      línea de arriba preserva el resto de la línea. Verificado en bash sobre un archivo con URL en código + URL
      en comentario + `fetch(`: el `sed` contaba **1** (perdía la URL de código), el filtro por línea cuenta **2**.
      **Consecuencia para el ejecutor:** las URL de documentación van en JSDoc de cabecera o en líneas `//` propias,
      **nunca como comentario al final de una línea de código** (esa forma sí contaría y haría fallar el criterio).
    - El criterio de pureza va **dentro del `<automated>`**, no solo en la prosa (warning ronda 2).
    - **El `<automated>` NO puede salir 0 sin correr tests (blocker ronda 2).** Los argumentos de `vitest run` son **filtros de nombre, no rutas**: si el archivo no existe (typo, renombre, no creado aún) vitest imprime `No test files found` y sale **0**. `--passWithNoTests=false` tampoco basta — cubre “cero archivos”, no “cero tests dentro del archivo”. La compuerta real es el **conteo impreso** MÁS el **exit code capturado por separado** (un run con fallos imprime igualmente `Tests N passed` junto a `M failed`). Forma verificada en bash contra los tres casos: verde ⇒ pasa; `No test files found` ⇒ falla; `10 passed | 2 failed` ⇒ falla.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-04-t1.log; pnpm --filter @obs/news exec vitest run src/parse-rss.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 12; test "$(grep -v "^[[:space:]]*//" packages/news/src/parse-rss.ts | grep -v "^[[:space:]]*\*" | grep -v "^[[:space:]]*/\*" | grep -Ec "fetch\(|https?://")" = "0"'</automated>
  </verify>
  <done>Los 5 feeds reales parsean a ítems tipados, con fechas que respetan el offset del feed y con `link` (no `guid`) como identidad.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canonicalizar-url.ts + urlHash (dedup nivel 1-2, D-13)</name>
  <read_first>
    - packages/core/src/nombre.ts:50-63 (forma de una función pura normalizadora + test de congelado)
    - packages/ingest/src/r2-store.ts:12-18 (`sha256Hex`, Web Crypto — no instalar librería de hash)
    - 132-CONTEXT.md §D-13
  </read_first>
  <files>packages/news/src/canonicalizar-url.ts, packages/news/src/canonicalizar-url.test.ts</files>
  <behavior>
    - `canonicalizarUrl("https://x.cl/a?utm_source=rss&utm_medium=feed&id=7")` == `"https://x.cl/a?id=7"` (se van los `utm_*`, se conserva lo demás).
    - Se eliminan también `fbclid`, `gclid`, `mc_cid`, `mc_eid`, `igshid`, `ref`, `source` cuando son parámetros de tracking; el set eliminado está congelado con `Object.freeze` y con test.
    - Se elimina el fragmento (`#...`) y la barra final redundante; el host se pasa a minúsculas; el esquema se conserva.
    - Dos URLs que solo difieren en el orden de sus parámetros de tracking producen la MISMA canónica y por tanto el mismo `urlHash`.
    - Una URL sin query queda idéntica.
    - `urlHash(url)` devuelve 64 hex y es estable entre corridas (mismo input ⇒ mismo output), calculado sobre la canónica, no sobre la original.
    - Una URL inválida lanza un error tipado (no devuelve la cadena tal cual en silencio).
  </behavior>
  <action>
    `export const PARAMS_TRACKING` como `Object.freeze([...])` incluyendo el prefijo `utm_` y la
    lista exacta de nombres. `export function canonicalizarUrl(raw: string): string` usando `URL`
    nativo y `searchParams.delete` — **no instalar ninguna dependencia** (research: cero paquetes
    nuevos en la fase; si parece necesario uno, PARAR y escalar).
    `export async function urlHash(raw: string): Promise<string>` = `sha256Hex` (de `@obs/ingest`)
    sobre los bytes UTF-8 de la canónica. Comentario: este hash es el `url_hash` PK de `noticia` y
    de `noticia_url_vista` (D-13); cambiar la canonicalización invalida el ledger existente ⇒ un
    cambio aquí exige migración de datos, no es refactor libre.
    Ordenar los `searchParams` restantes alfabéticamente para que el orden no altere el hash.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/canonicalizar-url.test.ts` reporta `Tests  N passed` con N ≥ 8.
    - **Mutación obligatoria:** quitar `utm_` del set de tracking ⇒ el primer caso FALLA; quitar el ordenamiento de params ⇒ el test de "mismo hash con distinto orden" FALLA. Revertir ambas y registrar.
    - `urlHash` de una URL fija está congelado como literal de 64 hex en el test (regresión: si alguien cambia la canonicalización, el test cae).
    - `grep -c "Object.freeze" packages/news/src/canonicalizar-url.ts` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-04-t2.log; pnpm --filter @obs/news exec vitest run src/canonicalizar-url.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 8'</automated>
  </verify>
  <done>La URL canónica descarta tracking de forma congelada y produce un `url_hash` estable, literal en el test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: prefiltro-lexico.ts — vocabulario congelado, recall-first, matching por PALABRA sobre texto despojado</name>
  <read_first>
    - packages/core/src/nombre.ts:50-63 (el `fold` canónico del repo: NFD → strip diacríticos → lower)
    - packages/dinero/src/connector-servel.ts:113-120 (congelado con `Object.freeze` + test)
    - 132-CONTEXT.md §D-05, §D-06, §D-07
    - 132-RESEARCH.md §Densidad legislativa y §Pitfall 7 (HTML en `description`)
    - packages/news/src/__fixtures__/latercera.xml (titulares legislativos reales para los casos positivos)
    - packages/news/src/__fixtures__/lacuarta.xml (titulares deportivos/farándula para los casos negativos)
  </read_first>
  <files>packages/news/src/prefiltro-lexico.ts, packages/news/src/prefiltro-lexico.test.ts</files>
  <behavior>
    - Casos POSITIVOS (deben pasar), tomados de titulares reales de los fixtures: "Senadores presentan bancada para impulsar proyecto de indulto general…", "Gobierno logra despachar a ley la megarreforma…", "Congreso despacha la Megarreforma del Gobierno: cómo votaron en el Senado…".
    - Casos con diacríticos y variantes: "Comisión de Hacienda", "comision de hacienda", "COMISIÓN DE HACIENDA", "boletín 12.345", "boletin 12345" — todos pasan (normalización NFD→ascii→lower).
    - **Frontera de palabra (anti-subcadena, premortem F-4).** Casos NEGATIVOS que un `includes()` daría por positivos y que aquí DEBEN descartar: "Receta de ensalada de verano" (contiene `sala`), "Reajuste salarial en el retail" (contiene `sala`), "Nueva leyenda del fútbol chileno" (contiene `ley`), "Bradley Cooper estrena película" (contiene `ley`), "Veterinarios de urgencia animal" (contiene `veto`… y `urgencia` como palabra completa ⇒ este caso PASA por `urgencia`; usar en su lugar "Grupo de veteranos conmemora" para el caso de `veto`), "Comisario detenido" (contiene `comis`, no `comision`).
      Cada uno de estos casos va escrito como aserción individual con el término corto que lo tentaba, comentado en el test.
    - Casos POSITIVOS de frontera que deben seguir pasando: "La sala del Senado aprobó", "la ley de presupuestos", "veto presidencial", "Comisión mixta".
    - Recall-first (D-06): ante ambigüedad, PASA. Un titular que solo menciona "ministro" pasa si `ministro` está en el vocabulario (falso positivo aceptable; lo filtra el clasificador de 135). Un titular sin ninguna palabra del vocabulario descarta.
    - HTML despojado (Pitfall 7): una `description` que contiene `<script>var senado=1</script>` y ningún término legislativo en el texto visible **DESCARTA** — la palabra dentro del script no cuenta.
    - Truncado: la descripción se trunca antes del matching (límite congelado, p. ej. 600 chars) para que un artículo largo no domine.
    - Determinismo: la misma entrada devuelve el mismo resultado; cero llamadas a red o LLM.
  </behavior>
  <action>
    `export const VOCABULARIO_LEGISLATIVO = Object.freeze([...])` con, como mínimo (D-05):
    `proyecto de ley`, `boletin`, `sala`, `comision`, `votacion`, `indicacion`, `veto`, `urgencia`,
    `mocion`, `mensaje`, `tribunal constitucional`, `tramitacion`, `promulgacion`, `senado`,
    `senador`, `senadora`, `camara de diputados`, `diputado`, `diputada`, `congreso`, `ley`,
    `parlamentario`, `parlamentaria`, `legislativo`, `legislativa`, `quorum`, `reforma`,
    `constitucional`, `despacha a ley`, `promulga`. **Los términos se escriben ya foldeados**
    (sin tildes, minúsculas) porque el matching ocurre sobre texto foldeado.

    **Matching con FRONTERA DE PALABRA — obligatorio (premortem F-4).** `String.includes()` está
    **PROHIBIDO** para este matching: con términos cortos matchea subcadenas (`sala` ⊂ *ensalada*,
    *salario*; `ley` ⊂ *leyenda*, *Bradley*; `veto` ⊂ *veteranos*) y convertiría el pre-filtro en un
    pasa-todo. Implementar:
    ```
    const FRONTERA = "[^a-z0-9]";  // el texto ya viene foldeado a ascii+lower
    // término -> RegExp("(^|" + FRONTERA + ")" + escapar(t) + "(" + FRONTERA + "|$)")
    ```
    Compilar las RegExp **una sola vez** a nivel de módulo (`const PATRONES = VOCABULARIO_LEGISLATIVO.map(...)`)
    para que el filtro siga siendo barato sobre cientos de ítems. Los términos multipalabra
    (`proyecto de ley`, `camara de diputados`) usan el mismo patrón: el espacio interno es literal.
    **PROHIBIDO podar el vocabulario para ajustar ninguna métrica** (D-06 recall-first): si una tasa
    no cuadra, se corrige el matching o se documenta la tasa — jamás se quitan términos.

    `export function despojarHtml(s: string): string` — elimina `<script>...</script>`,
    `<style>...</style>` y sus contenidos ANTES de eliminar el resto de tags, luego decodifica las
    entidades básicas (`&amp; &quot; &lt; &gt; &#39; &nbsp;`) y colapsa espacios.
    `export function fold(s: string): string` — copia de la forma de `packages/core/src/nombre.ts`
    (NFD, strip `\p{Diacritic}`, lower, colapsar espacios). No importar `nombre.ts` si su semántica
    de puntuación no calza; si calza, reusar en vez de duplicar (decidir leyendo el archivo).
    `export function esLegislativo(titulo: string, descripcion?: string | null): boolean` —
    construye `texto = fold(despojarHtml(titulo)) + " " + fold(despojarHtml(descripcion ?? "")).slice(0, LIMITE)`
    y devuelve `PATRONES.some(re => re.test(texto))`.
    Exportar además `export function terminosQueMatchean(titulo, descripcion?): string[]` — devuelve
    qué términos dispararon; es lo que hace depurable la tasa y lo que se imprime en el reporte.

    Cabecera: recall-first (D-06) — un falso negativo pierde la noticia para siempre; ante duda,
    ampliar el vocabulario, nunca restringirlo. Todo descarte queda registrado (D-07) — el registro
    lo hace `carga-run.ts` (plan 05), no este módulo.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/prefiltro-lexico.test.ts` reporta `Tests  N passed` con N ≥ 20 (subió desde 14 por los casos de frontera).
    - **Anti-subcadena (el criterio que reemplaza a la banda 2-35%).** El test contiene aserciones de **casos concretos nombrados**, no un rango: los 5 negativos de frontera del `<behavior>` (`ensalada`, `salarial`, `leyenda`, `Bradley`, `veteranos`) devuelven `false`, y los 4 positivos de frontera (`sala del Senado`, `ley de presupuestos`, `veto presidencial`, `Comisión mixta`) devuelven `true`. Los 9 casos son aserciones individuales.
    - **La tasa de paso sobre los fixtures es un REPORTE, no un assert de rango.** El test corre `esLegislativo` sobre TODOS los ítems parseados de los 5 fixtures, imprime `outlet: pasan/total (pct%)` por feed y el agregado, y **el SUMMARY registra los números**. La única aserción sobre la tasa es que sea `> 0%` y `< 100%` (cero fuerte por ambos lados: si pasa 0 el filtro está roto, si pasa todo el filtro no filtra). **Está PROHIBIDO ajustar el vocabulario para mover esta tasa** (D-06).
    - Si la tasa agregada resulta > 35%, **no se poda el vocabulario**: se ejecuta `terminosQueMatchean` sobre una muestra de 20 ítems que pasaron, se pega la salida en el SUMMARY, y se documenta qué término la explica. Si el término dispara por subcadena, es un bug del matching y se arregla el matching. Si dispara por palabra completa, la tasa es correcta y se registra tal cual.
    - **Mutaciones obligatorias (registrar cuál test cayó):** (1) quitar el fold NFD ⇒ el caso "COMISIÓN DE HACIENDA" FALLA; (2) quitar el despojo de `<script>` ⇒ el caso del script con "senado" FALLA (pasaría cuando debe descartar); (3) vaciar `VOCABULARIO_LEGISLATIVO` ⇒ los 3 casos positivos FALLAN; (4) **reemplazar el matching por frontera de palabra por `texto.includes(t)`** ⇒ los 5 casos negativos de frontera FALLAN. Revertir las cuatro.
    - `grep -c "Object.freeze" packages/news/src/prefiltro-lexico.ts` ≥ 1 y el test congela `VOCABULARIO_LEGISLATIVO.length` como literal.
    - `grep -Ec "\.includes\(" packages/news/src/prefiltro-lexico.ts` == **0 sobre el archivo COMPLETO** — umbral duro, sin cláusula de juicio (la redacción anterior, "==0 en el camino de matching, si aparece para otra cosa justificarlo", no era medible y se declaraba cumplida a ojo). Si el módulo necesitara comprobar pertenencia para algo que no es matching, usar `Set.has` o `indexOf` — el archivo entero queda libre de `.includes(`. La garantía de fondo la da la **mutación (4)** de arriba (`texto.includes(t)` ⇒ caen los 5 negativos de frontera); este grep es el gate barato que la acompaña.
    - `grep -Eic "openai|llm|gemini|deepseek|minimax|fetch\(" packages/news/src/prefiltro-lexico.ts` == 0 — en 132 no se llama a ningún LLM.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-04-t3.log; pnpm --filter @obs/news exec vitest run src/prefiltro-lexico.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 20; test "$(grep -Ec "\.includes\(" packages/news/src/prefiltro-lexico.ts)" = "0"'</automated>
  </verify>
  <done>El pre-filtro es determinista, congelado, recall-first, matchea por palabra completa (probado con 9 casos de frontera y una mutación a `includes`), y su tasa de paso sobre los feeds reales está medida y registrada como reporte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XML/HTML de tercero → parser y filtro | contenido no confiable, incluye `<script>`/`<iframe>` embebidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-10 | Denial of Service (XXE / entity expansion) | `parse-rss.ts` | mitigate | `fast-xml-parser@5` no resuelve entidades externas por diseño; PROHIBIDO cambiar a un DOM parser |
| T-132-11 | Tampering (contaminación del matching) | `prefiltro-lexico.ts` | mitigate | `despojarHtml` elimina `<script>`/`<style>` **con su contenido** antes del fold; test que cae si se quita |
| T-132-12 | Information Disclosure (XSS diferido) | `descripcion` persistida | mitigate | Se persiste el texto DESPOJADO; el crudo con HTML queda solo en R2. El render público es de la 137 |
| T-132-24 | Spoofing (falso verde) | matching por subcadena | mitigate | Frontera de palabra obligatoria + 9 casos de frontera nombrados + mutación a `includes` que debe hacer caer 5 tests |
</threat_model>

<verification>
- `pnpm --filter @obs/news test` verde con conteo agregado ≥ 40 tests en los 3 archivos.
- Las 9 mutaciones (3+2+4) registradas en el SUMMARY con el test que cayó en cada caso.
- La tasa de paso del pre-filtro por feed, impresa y registrada como reporte (no como assert de rango).
</verification>

<success_criteria>
- Los 5 fixtures reales parsean; fechas con offset preservado.
- `urlHash` estable y congelado por literal.
- Pre-filtro con vocabulario congelado, matching por frontera de palabra probado con casos concretos, tasa medida y registrada, cero LLM.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-04-SUMMARY.md` when done
</output>
