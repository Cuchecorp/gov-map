---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0084_noticia.sql
  - supabase/tests/0084_noticia.test.sql
autonomous: false
requirements: [NEWS-01]

must_haves:
  truths:
    - "Las tablas `noticia` y `noticia_url_vista` existen en PROD"
    - "Ni `anon` ni `authenticated` pueden leer, insertar ni actualizar ninguna de las dos tablas"
    - "El conteo de descartes del pre-filtro es consultable por `psql -tA` sobre `noticia_url_vista`"
  artifacts:
    - path: "supabase/migrations/0084_noticia.sql"
      provides: "DDL de noticia + noticia_url_vista con RLS deny-all total (D-11, D-12)"
      contains: "enable row level security"
    - path: "supabase/tests/0084_noticia.test.sql"
      provides: "pgTAP de RLS deny-all × 2 tablas × 2 roles"
      contains: "has_table_privilege"
  key_links:
    - from: "supabase/migrations/0084_noticia.sql"
      to: "PROD (SUPABASE_DB_URL)"
      via: "psql --single-transaction -f"
      pattern: "single-transaction"
---

<objective>
Crear y **aplicar a PROD** la migración que da soporte a la Etapa 2: `noticia` (ítems que pasan el
pre-filtro) y `noticia_url_vista` (seen-ledger: idempotencia + conteo de descartes), ambas con RLS
deny-all total y cero superficie pública (D-11, D-12). El sitio NO lee noticias hasta la 137.

Purpose: sin schema aplicado, la Etapa 2 no se puede verificar contra nada real y el pgTAP sería
un test contra un schema imaginario.
Output: `0084_noticia.sql` aplicada + `0084_noticia.test.sql` verde contra el schema APLICADO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@supabase/migrations/0070_notificacion_envio.sql
@supabase/tests/0070_notificacion_envio.test.sql
@supabase/migrations/0080_actualidad_evidencia.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 0084_noticia.sql + pgTAP 0084_noticia.test.sql</name>
  <read_first>
    - supabase/migrations/0070_notificacion_envio.sql (analog EXACTO: cabecera documentada, comando de apply, `enable row level security` SIN policy, línea de `schema_migrations`)
    - supabase/tests/0070_notificacion_envio.test.sql (analog del pgTAP; copiar íntegro cambiando tablas)
    - supabase/migrations/0080_actualidad_evidencia.sql:1-60 (cómo se documentan decisiones D-xx dentro del archivo)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md §D-11, §D-12, §D-13
  </read_first>
  <files>supabase/migrations/0084_noticia.sql, supabase/tests/0084_noticia.test.sql</files>
  <action>
    **Antes de nombrar el archivo:** correr `ls supabase/migrations | tail -3` y confirmar que el
    último es `0083_coautoria_v2.sql` ⇒ el número libre es `0084`. Si hubiera cambiado, usar el
    siguiente libre y renombrar también el test. **JAMÁS editar `0073` ni `0075`** (escritas y no
    aplicadas).

    `noticia` (nombres en español, singular — D-11):
      `url_hash text primary key`            -- sha256(url_canonica), D-13
      `url text not null`                    -- <link> original del ítem
      `url_canonica text not null`           -- strip utm_*/fbclid/gclid
      `titular text not null`
      `outlet text not null`                 -- slug del feed (biobiochile|cooperativa|latercera|lacuarta|exante)
      `fecha_pub timestamptz`                -- **timestamptz**: Cooperativa emite -0400, el resto +0000 (Pitfall 6). NUNCA normalizar a una tz global.
      `descripcion text`                     -- texto despojado de HTML y truncado
      `r2_path text not null`                -- referencia al crudo en R2 (etapa 1)
      `contenido_hash text`                  -- sha256 del crudo del que salió
      `estado text not null default 'pendiente' check (estado in ('pendiente','clasificada','descartada'))`
      `created_at timestamptz not null default now()`
    Índices: `create index on noticia (fecha_pub desc)` y `create index on noticia (outlet)`.

    `noticia_url_vista` (seen-ledger — D-07):
      `url_hash text primary key`
      `url_canonica text not null`
      `outlet text not null`
      `estado text not null check (estado in ('pasa','descarta'))`
      `causa text` (`check (causa is null or causa in ('prefiltro_lexico','duplicado'))`)
      `primera_vista timestamptz not null default now()`
    Índice: `create index on noticia_url_vista (causa)` — el conteo de descartes por corrida sale de acá.

    Ambas: `alter table <t> enable row level security;` **SIN ninguna policy y SIN ningún grant**
    a `anon`/`authenticated` (patrón 0070; `service_role` bypassa RLS). Comentario explícito de que
    esto cae en Block D y Block E del lockdown-guard si alguien agrega un `to authenticated`.

    Cabecera del archivo: qué crea, por qué deny-all (D-12: el sitio no lee noticias hasta la 137),
    el comando exacto de apply, la prohibición de `supabase db push`, y la línea
    `-- insert into schema_migrations (version) values ('0084');`.

    `supabase/tests/0084_noticia.test.sql` (pgTAP, patrón 0070): `plan(N)` con
    — `has_table('public','noticia',...)` y `has_table('public','noticia_url_vista',...)`
    — RLS enabled en ambas (`select is((select count(*)::int from pg_class where relname=... and relrowsecurity=true), 1, ...)`)
    — `has_column` de `noticia.url_hash`, `noticia.fecha_pub`, `noticia_url_vista.causa`
    — `is(has_table_privilege('anon','noticia','select'), false, ...)` y lo mismo para
      insert/update, × 2 tablas × 2 roles (`anon`, `authenticated`) = 12 aserciones de privilegio.
    Ajustar `plan(N)` al número real de aserciones (un `plan()` desalineado hace fallar `finish()`).
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations | tail -1` fue verificado y queda citado en el SUMMARY junto al número elegido.
    - `grep -c "create policy" supabase/migrations/0084_noticia.sql` == 0.
    - `grep -Ec "grant .* to (anon|authenticated)" supabase/migrations/0084_noticia.sql` == 0.
    - `grep -c "enable row level security" supabase/migrations/0084_noticia.sql` == 2.
    - `grep -c "timestamptz" supabase/migrations/0084_noticia.sql` ≥ 3 y **no aparece `timestamp without time zone`** ni `date` para `fecha_pub`.
    - El `plan(N)` del pgTAP coincide con el número de `select` de aserción del archivo (contar con `grep -vc '^--' | ...`; usar `grep -v '^\s*--'` antes de contar — los comentarios inflan el conteo).
    - El archivo NO contiene el project-ref de Supabase ni ninguna credencial: `grep -Ei "supabase\.co|service_role_key|eyJ" supabase/migrations/0084_noticia.sql` == 0 resultados (B26).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -f supabase/migrations/0084_noticia.sql; test -f supabase/tests/0084_noticia.test.sql; test "$(grep -c "enable row level security" supabase/migrations/0084_noticia.sql)" = "2"; test "$(grep -c "create policy" supabase/migrations/0084_noticia.sql)" = "0"'</automated>
  </verify>
  <done>Migración y pgTAP escritos, deny-all total, sin policies ni grants, fechas en timestamptz.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar 0084 a PROD con psql y correr el pgTAP contra el schema APLICADO</name>
  <read_first>
    - supabase/migrations/0084_noticia.sql (el archivo recién escrito, tal como quedó)
    - supabase/migrations/0070_notificacion_envio.sql:20-35 (el comando de apply canónico del régimen)
  </read_first>
  <files>supabase/migrations/0084_noticia.sql, supabase/tests/0084_noticia.test.sql</files>
  <action>Aplicar la migración a PROD con `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0084_noticia.sql` (JAMÁS `supabase db push`), registrar `0084` en `schema_migrations`, y correr el pgTAP con `psql -tA -f supabase/tests/0084_noticia.test.sql` contra el schema APLICADO, más el control positivo con la aserción invertida. Ver los pasos exactos en `<how-to-verify>`.</action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0084_noticia.test.sql</automated>
  </verify>
  <what-built>
    La migración `0084_noticia.sql` y su pgTAP están escritos y verificados estáticamente. Falta
    aplicarlos a PROD — operación irreversible sobre la base viva, por eso es checkpoint.
  </what-built>
  <how-to-verify>
    Ejecutar, en este orden exacto:

    1. Apply (JAMÁS `supabase db push`):
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0084_noticia.sql`
       Debe terminar sin ERROR. Un `WARNING 01006` de un REVOKE ajeno NO es error, pero tampoco es
       prueba de nada (gotcha v12.0: `REVOKE` ajeno no falla, no-opea).

    2. Registrar en el ledger:
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "insert into schema_migrations (version) values ('0084') on conflict do nothing; select version from schema_migrations where version='0084';"`
       Debe imprimir `0084`.

    3. pgTAP contra el schema APLICADO:
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0084_noticia.test.sql`
       Todas las líneas deben empezar con `ok `; ninguna `not ok`. La última línea del plan debe
       coincidir con el `plan(N)`.

    4. Control positivo del pgTAP (anti-vacuo — un pgTAP puede pasar sin probar nada si el
       `plan()` está mal): correr
       `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select has_table_privilege('anon','noticia','select');"`
       y confirmar que devuelve `f`. Y correr una aserción deliberadamente falsa en una copia
       temporal del test (`is(has_table_privilege('anon','noticia','select'), true, 'control')`) y
       verificar que sale `not ok`. Borrar la copia.

    Anotar en el SUMMARY: exit codes, la salida de `select version`, y el conteo `ok`/`not ok`.

    **Si el apply falla: PARAR y escalar. No editar la migración a ciegas ni aplicar por partes.**
  </how-to-verify>
  <acceptance_criteria>
    - `psql ... -f supabase/migrations/0084_noticia.sql` exit 0, sin líneas `ERROR:`.
    - `select version from schema_migrations where version='0084'` devuelve exactamente `0084`.
    - `psql -tA -f supabase/tests/0084_noticia.test.sql` no imprime ninguna línea que empiece con `not ok`, y el número de `ok ` == `plan(N)`.
    - El control positivo (aserción invertida) produjo `not ok` — prueba de que el pgTAP realmente ejercita el privilegio y no es vacuo.
    - `select has_table_privilege('anon','noticia','select')` == `f` y `select has_table_privilege('anon','noticia_url_vista','select')` == `f`.
  </acceptance_criteria>
  <resume-signal>Escribir "aplicada" con la salida de los 4 pasos, o describir el error.</resume-signal>
  <done>0084 aplicada a PROD, registrada en `schema_migrations`, pgTAP verde con control positivo demostrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST/`anon` → tablas nuevas | superficie pública potencial de datos de noticias |
| CLI local (service_role) → PROD | escritura privilegiada que bypassa RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04 | Information Disclosure | `noticia`, `noticia_url_vista` | mitigate | RLS enabled SIN policy y SIN grant a `anon`/`authenticated`; pgTAP con 12 aserciones de privilegio + control positivo |
| T-132-05 | Elevation of Privilege | policy `to authenticated` añadida por descuido | mitigate | Comentario en la migración + lockdown-guard Block D/E; `grep -c "create policy"` == 0 como criterio |
| T-132-06 | Information Disclosure | project-ref/credenciales en el artefacto | mitigate | Grep de `supabase.co`/`eyJ` == 0 en la migración (B26) |
</threat_model>

<verification>
- La migración está aplicada y registrada; el pgTAP corre contra el schema real, no contra un local.
- La aserción invertida demostró que el pgTAP no es vacuo.
</verification>

<success_criteria>
- `noticia` y `noticia_url_vista` existen en PROD con RLS deny-all total.
- `has_table_privilege('anon'|'authenticated', <tabla>, 'select'|'insert'|'update')` == false en los 12 casos.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-02-SUMMARY.md` when done
</output>
