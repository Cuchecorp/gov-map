---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0084_noticia.sql
  - supabase/tests/0084_noticia.test.sql
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "Las tablas `noticia` y `noticia_url_vista` existen en PROD"
    - "Ni `anon` ni `authenticated` pueden leer, insertar ni actualizar ninguna de las dos tablas"
    - "El deny-all está escrito en la migración (REVOKE explícito), no delegado al rol de conexión"
    - "El conteo de descartes del pre-filtro es consultable por `psql -tA` sobre `noticia_url_vista`"
  artifacts:
    - path: "supabase/migrations/0084_noticia.sql"
      provides: "DDL de noticia + noticia_url_vista con RLS deny-all total (D-11, D-12)"
      contains: "enable row level security"
    - path: "supabase/tests/0084_noticia.test.sql"
      provides: "pgTAP de RLS deny-all × 2 tablas × 2 roles"
      contains: "has_table_privilege"
  key_links:
    - from: "supabase/migrations/0084_noticia.sql"
      to: "PROD (SUPABASE_DB_URL)"
      via: "psql --single-transaction -f"
      pattern: "single-transaction"
---

<objective>
Crear y **aplicar a PROD** la migración que da soporte a la Etapa 2: `noticia` (ítems que pasan el
pre-filtro) y `noticia_url_vista` (seen-ledger: idempotencia + conteo de descartes), ambas con RLS
deny-all total y cero superficie pública (D-11, D-12). El sitio NO lee noticias hasta la 137.

Purpose: sin schema aplicado, la Etapa 2 no se puede verificar contra nada real y el pgTAP sería
un test contra un schema imaginario.
Output: `0084_noticia.sql` aplicada + `0084_noticia.test.sql` verde contra el schema APLICADO.

**Por qué este plan es `autonomous: true` (adjudicado en la revisión de premortem, LOCKED):** el DDL
es **aditivo** (crea dos tablas que no existen), **deny-all** y **sin lectores** — nada en el sitio
ni en ningún job las consulta hasta la Phase 137 (D-12). El precedente del milestone son las
migraciones `0080`-`0083`, aplicadas autónomamente en la Pasada 1 por el mismo procedimiento. Lo que
NO se relaja es el **procedimiento**: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL"
--single-transaction -f`, JAMÁS `supabase db push`, y pgTAP post-apply con control positivo. Si el
apply falla: **PARAR y escalar**, no editar la migración a ciegas ni aplicar por partes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@supabase/migrations/0070_notificacion_envio.sql
@supabase/tests/0070_notificacion_envio.test.sql
@supabase/migrations/0080_actualidad_evidencia.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 0084_noticia.sql + pgTAP 0084_noticia.test.sql</name>
  <read_first>
    - supabase/migrations/0070_notificacion_envio.sql (analog EXACTO: cabecera documentada, comando de apply, `enable row level security` SIN policy)
    - supabase/tests/0070_notificacion_envio.test.sql (analog del pgTAP; copiar íntegro cambiando tablas)
    - supabase/migrations/0080_actualidad_evidencia.sql:1-60 (cómo se documentan decisiones D-xx dentro del archivo)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md §D-11, §D-12, §D-13
  </read_first>
  <files>supabase/migrations/0084_noticia.sql, supabase/tests/0084_noticia.test.sql</files>
  <action>
    **Antes de nombrar el archivo:** correr `ls supabase/migrations | tail -3` y confirmar que el
    último es `0083_coautoria_v2.sql` ⇒ el número libre es `0084`. Si hubiera cambiado, usar el
    siguiente libre y renombrar también el test. **JAMÁS editar `0073` ni `0075`** (escritas y no
    aplicadas).

    `noticia` (nombres en español, singular — D-11):
      `url_hash text primary key`            -- sha256(url_canonica), D-13
      `url text not null`                    -- <link> original del ítem
      `url_canonica text not null`           -- strip utm_*/fbclid/gclid
      `titular text not null`
      `outlet text not null`                 -- slug del feed (biobiochile|cooperativa|latercera|lacuarta|exante)
      `fecha_pub timestamptz`                -- **timestamptz**: Cooperativa emite -0400, el resto +0000 (Pitfall 6). NUNCA normalizar a una tz global.
      `descripcion text`                     -- texto despojado de HTML y truncado
      `r2_path text not null`                -- referencia al crudo en R2 (etapa 1)
      `contenido_hash text`                  -- sha256 del crudo del que salió
      `estado text not null default 'pendiente' check (estado in ('pendiente','clasificada','descartada'))`
      `created_at timestamptz not null default now()`
    Índices: `create index on noticia (fecha_pub desc)` y `create index on noticia (outlet)`.

    `noticia_url_vista` (seen-ledger — D-07):
      `url_hash text primary key`
      `url_canonica text not null`
      `outlet text not null`
      `estado text not null check (estado in ('pasa','descarta'))`
      `causa text` (`check (causa is null or causa in ('prefiltro_lexico','duplicado'))`)
      `primera_vista timestamptz not null default now()`
    Índice: `create index on noticia_url_vista (causa)` — el conteo de descartes por corrida sale de acá.

    Ambas: `alter table <t> enable row level security;` **SIN ninguna policy y SIN ningún grant**
    a `anon`/`authenticated` (patrón 0070; `service_role` bypassa RLS). Comentario explícito de que
    esto cae en Block D y Block E del lockdown-guard si alguien agrega un `to authenticated`.

    **REVOKE explícito obligatorio** (premortem F-10 — el deny-all NO puede depender del rol de
    conexión): añadir al final de la migración
    `revoke all on table public.noticia, public.noticia_url_vista from anon, authenticated;`
    Con la conexión actual (rol `postgres`, cuyo `pg_default_acl` no otorga nada a `anon`) este
    REVOKE es un **no-op idempotente**; su razón de ser es que el `pg_default_acl` de
    `supabase_admin` en `public` SÍ otorga `arwdDxtm` a `anon`/`authenticated`, y `0073` (que lo
    cierra globalmente) está escrita y **no aplicada**. Comentario en la migración diciendo
    exactamente esto, más el gotcha v12.0: **un REVOKE ajeno no falla — no-opea con `WARNING 01006`
    — así que este REVOKE no prueba nada por sí solo; la prueba es el pgTAP.**

    Cabecera del archivo: qué crea, por qué deny-all (D-12: el sitio no lee noticias hasta la 137),
    el comando exacto de apply, la prohibición de `supabase db push`, y la línea documental
    `-- ledger: insert into supabase_migrations.schema_migrations (version) values ('0084');`
    (esquema **calificado** — en esta base el ledger vive en `supabase_migrations`, NO en `public`).
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations | tail -1` fue verificado y queda citado en el SUMMARY junto al número elegido.
    - `grep -c "create policy" supabase/migrations/0084_noticia.sql` == 0.
    - `grep -Ec "grant .* to (anon|authenticated)" supabase/migrations/0084_noticia.sql` == 0.
    - `grep -c "enable row level security" supabase/migrations/0084_noticia.sql` == 2.
    - `grep -Ec "revoke all on table .* from anon, authenticated" supabase/migrations/0084_noticia.sql` == 1 (F-10).
    - `grep -c "timestamptz" supabase/migrations/0084_noticia.sql` ≥ 3 y **no aparece `timestamp without time zone`** ni `date` para `fecha_pub`.
    - Toda mención al ledger en el archivo está **calificada por esquema**: `grep -c "schema_migrations" supabase/migrations/0084_noticia.sql` == `grep -c "supabase_migrations.schema_migrations" supabase/migrations/0084_noticia.sql` (ninguna aparición pelada).
    - El `plan(N)` del pgTAP coincide con el número de `select` de aserción del archivo. Contar SIEMPRE excluyendo comentarios: `grep -v '^\s*--' supabase/tests/0084_noticia.test.sql | grep -Ec "select (ok|is|has_|isnt)\("` (gotcha v12.0: `grep -c` cuenta los comentarios y la prosa de la cabecera se auto-invalida).
    - El archivo NO contiene el project-ref de Supabase ni ninguna credencial: `grep -Eic "supabase\.co|service_role_key|eyJ" supabase/migrations/0084_noticia.sql` == 0 (B26).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -f supabase/migrations/0084_noticia.sql; test -f supabase/tests/0084_noticia.test.sql; test "$(grep -c "enable row level security" supabase/migrations/0084_noticia.sql)" = "2"; test "$(grep -c "create policy" supabase/migrations/0084_noticia.sql)" = "0"; test "$(grep -Ec "revoke all on table .* from anon, authenticated" supabase/migrations/0084_noticia.sql)" = "1"; test "$(grep -c "schema_migrations" supabase/migrations/0084_noticia.sql)" = "$(grep -c "supabase_migrations.schema_migrations" supabase/migrations/0084_noticia.sql)"'</automated>
  </verify>
  <done>Migración y pgTAP escritos, deny-all total con REVOKE explícito, sin policies ni grants, fechas en timestamptz, ledger citado con esquema calificado.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar 0084 a PROD con psql, registrar el ledger y correr el pgTAP contra el schema APLICADO</name>
  <read_first>
    - supabase/migrations/0084_noticia.sql (el archivo recién escrito, tal como quedó)
    - supabase/migrations/0070_notificacion_envio.sql:20-35 (el comando de apply canónico del régimen)
  </read_first>
  <files>supabase/migrations/0084_noticia.sql, supabase/tests/0084_noticia.test.sql</files>
  <action>
    Ejecutar los 5 pasos siguientes **en este orden exacto**, registrando en el SUMMARY el exit code
    y la salida literal de cada uno. Un paso que falle detiene el plan: **PARAR y escalar**, no
    editar la migración a ciegas ni aplicar por partes.

    **Paso 1 — Apply** (JAMÁS `supabase db push`):
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0084_noticia.sql`
    Debe terminar sin ninguna línea `ERROR:`. Un `WARNING 01006` de un REVOKE ajeno NO es error,
    pero **tampoco es prueba de nada** (gotcha v12.0: el REVOKE ajeno no falla, no-opea).

    **Paso 2 — Inspeccionar el ledger ANTES de escribirlo.** El ledger de migraciones de esta base
    **no está en `public`**: un `insert into schema_migrations …` pelado falla con
    `ERROR: relation "schema_migrations" does not exist` (verificado contra PROD en el premortem).
    Descubrir el esquema y las columnas obligatorias antes de insertar:
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select table_schema from information_schema.tables where table_name='schema_migrations';"`
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select column_name, is_nullable, column_default from information_schema.columns where table_schema='supabase_migrations' and table_name='schema_migrations' order by ordinal_position;"`
    Esperado: el esquema es `supabase_migrations` (además de las copias internas de `auth` y
    `realtime`, que NO son el ledger). Construir el INSERT del paso 3 con **exactamente** las
    columnas `is_nullable='NO'` que no tengan default.

    **Paso 3 — Registrar `0084` en el ledger, con el esquema CALIFICADO:**
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "insert into supabase_migrations.schema_migrations (<columnas del paso 2>) values ('0084'<, …>) on conflict do nothing; select version from supabase_migrations.schema_migrations where version='0084';"`
    Debe imprimir `0084`.
    **Nota de alcance (LOCKED):** al inspeccionar el ledger se verá que la última entrada previa es
    `0079` y que **`0080`-`0083` están aplicadas y sin registrar** — es drift **preexistente**.
    Registrarlo como observación en el SUMMARY (con la lista de versiones presentes) y **NO
    reconciliarlo**: no insertar `0080`-`0083`, no tocar filas ajenas. Esta fase solo registra `0084`.

    **Paso 4 — pgTAP contra el schema APLICADO:**
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0084_noticia.test.sql`
    Todas las líneas de aserción deben empezar con `ok `; ninguna `not ok`. El conteo de `ok` debe
    coincidir con el `plan(N)`.

    **Paso 5 — Control positivo del pgTAP** (anti-vacuo: un pgTAP puede pasar sin probar nada si el
    `plan()` está mal). Dos comprobaciones:
    (a) `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select has_table_privilege('anon','public.noticia','select'), has_table_privilege('anon','public.noticia_url_vista','select'), has_table_privilege('authenticated','public.noticia','select');"`
        debe devolver `f|f|f`.
    (b) copiar el test a un archivo temporal, invertir UNA aserción
        (`is(has_table_privilege('anon','public.noticia','select'), true, 'control positivo')`),
        correrlo y verificar que **esa línea sale `not ok`**. Borrar la copia. Si la aserción
        invertida NO sale `not ok`, el pgTAP es vacuo y hay que reescribirlo.
  </action>
  <acceptance_criteria>
    - Paso 1: `psql --single-transaction -f supabase/migrations/0084_noticia.sql` exit 0 y `grep -c "^ERROR:"` sobre su salida capturada == 0.
    - Paso 2: la salida de `information_schema.tables` está pegada en el SUMMARY y muestra `supabase_migrations` — el comando del paso 3 se construyó a partir de ella, no de memoria.
    - Paso 3: `select version from supabase_migrations.schema_migrations where version='0084'` devuelve exactamente `0084`. **Ningún comando ejecutado usa `schema_migrations` sin calificar.**
    - El SUMMARY registra la lista de versiones del ledger observada y declara explícitamente que el drift `0080`-`0083` es **preexistente y fuera del alcance de la 132** (no se reconcilió).
    - Paso 4: la salida del pgTAP no contiene ninguna línea que empiece con `not ok`, y el conteo de líneas que empiezan con `ok ` == `plan(N)`. Ambos números citados en el SUMMARY.
    - Paso 5(a): las tres llamadas a `has_table_privilege` devuelven `f`.
    - Paso 5(b): la aserción invertida produjo una línea `not ok` — citada literalmente en el SUMMARY. Sin esta línea, el criterio del paso 4 NO cuenta como cumplido.
    - **CRLF de `psql -tA` — obligatorio en TODA captura (blocker ronda 2; gotcha v12.0 ya pagado).** `psql -tA`
      emite terminadores CRLF: `v=$(psql …)` deja `0084\r`, con lo que `test "$v" = "0084"` **falla siempre**
      (falso rojo que empuja a relajar el criterio), y un `test "$s" -eq "$N"` sobre `5\r` aborta con
      `integer expression expected`. **Toda** captura de `psql` de este plan pasa por `| tr -d "\r"`, y el
      valor capturado se imprime entre corchetes para que un residuo sea visible en el log. Comprobado en bash
      que el fix no vuelve el criterio vacuo: con el valor equivocado (`0083`) sigue fallando.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; out=$(PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0084_noticia.test.sql | tr -d "\r"); echo "$out"; test "$(printf "%s\n" "$out" | grep -c "^not ok")" = "0"; test "$(printf "%s\n" "$out" | grep -c "^ok ")" -ge "12"; v=$(PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select version from supabase_migrations.schema_migrations where version='"'"'0084'"'"';" | tr -d "\r"); echo "ledger=[$v]"; test "$v" = "0084"'</automated>
  </verify>
  <done>0084 aplicada a PROD, registrada en `supabase_migrations.schema_migrations`, pgTAP verde con control positivo demostrado, y el drift preexistente 0080-0083 documentado sin tocarlo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST/`anon` → tablas nuevas | superficie pública potencial de datos de noticias |
| CLI local (service_role) → PROD | escritura privilegiada que bypassa RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04 | Information Disclosure | `noticia`, `noticia_url_vista` | mitigate | RLS enabled SIN policy y SIN grant a `anon`/`authenticated`; pgTAP con 12 aserciones de privilegio + control positivo |
| T-132-05 | Elevation of Privilege | policy `to authenticated` añadida por descuido | mitigate | Comentario en la migración + lockdown-guard Block D/E; `grep -c "create policy"` == 0 como criterio |
| T-132-06 | Information Disclosure | project-ref/credenciales en el artefacto | mitigate | Grep de `supabase.co`/`eyJ` == 0 en la migración (B26) |
| T-132-22 | Elevation of Privilege | `pg_default_acl` de `supabase_admin` otorga a `anon` (0073 no aplicada) | mitigate | `revoke all … from anon, authenticated` explícito en la migración; el pgTAP prueba el estado resultante, el REVOKE no se toma como prueba (F-10) |
</threat_model>

<verification>
- La migración está aplicada y registrada en el ledger CALIFICADO; el pgTAP corre contra el schema real, no contra un local.
- La aserción invertida demostró que el pgTAP no es vacuo.
- El deny-all está escrito en la migración, no delegado al rol de conexión.
</verification>

<success_criteria>
- `noticia` y `noticia_url_vista` existen en PROD con RLS deny-all total.
- `has_table_privilege('anon'|'authenticated', <tabla>, 'select'|'insert'|'update')` == false en los 12 casos.
- `supabase_migrations.schema_migrations` contiene `0084`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-02-SUMMARY.md` when done
</output>
