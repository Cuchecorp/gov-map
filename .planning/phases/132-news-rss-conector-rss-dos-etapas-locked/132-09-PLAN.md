---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - supabase/migrations/0085_noticia_url_vista_pendiente.sql
  - supabase/tests/0085_noticia_url_vista_pendiente.test.sql
  - packages/news/src/writer.ts
  - packages/news/src/writer-supabase.ts
  - packages/news/src/writer-supabase.test.ts
  - packages/news/src/carga-run.ts
  - packages/news/src/carga-run.test.ts
autonomous: true
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "Un fallo transitorio de DB NO deja el ítem marcado con una causa de descarte falsa: la corrida siguiente vuelve a evaluarlo y a cargarlo"
    - "El conteo de descartes por causa es exacto por encima de 1.000 filas (no renace B-01)"
    - "`SupabaseNewsWriter` — el único componente que escribe en PROD — tiene tests que caen si se rompe su onConflict/chunk/dedupe"
  artifacts:
    - path: "supabase/migrations/0085_noticia_url_vista_pendiente.sql"
      provides: "estado 'pendiente' (marcado provisional sin causa final) en noticia_url_vista"
      contains: "pendiente"
    - path: "supabase/tests/0085_noticia_url_vista_pendiente.test.sql"
      provides: "pgTAP contra el schema APLICADO: acepta 'pendiente', rechaza estados inventados, RLS sigue deny-all"
    - path: "packages/news/src/writer-supabase.test.ts"
      provides: "Tests de SupabaseNewsWriter con cliente doble (onConflict, chunking, dedupe, error, conteo >1.000)"
  key_links:
    - from: "packages/news/src/carga-run.ts"
      to: "noticia_url_vista.estado='pendiente'"
      via: "marcado provisional SIN causa final, promovido solo tras éxito"
      pattern: "pendiente"
    - from: "packages/news/src/writer-supabase.ts"
      to: "count exacto de PostgREST"
      via: "select('*', { count: 'exact', head: true })"
      pattern: "head: true"
---

<objective>
Cierra **CR-02** (un fallo transitorio de DB marca el ítem como `descarta/prefiltro_lexico` PARA
SIEMPRE: pérdida definitiva del dato — el falso negativo que D-06 declara inaceptable — y causa
falsa inflando la métrica del SC4), **WR-05** (`contarPorCausa` bajo el cap de 1.000 de PostgREST:
el patrón B-01 que este milestone mató), **WR-15** (el writer que escribe PROD no tiene tests),
**WR-17** (HTML crudo en `descripcion` contra lo que declara la migración) e **IN-04**.

Purpose: el ledger es la idempotencia del pipeline; si miente, la noticia se pierde y nadie se
entera. Output: marcado provisional neutro + promoción a causa final solo tras éxito, conteo real
por causa, y el writer de PROD cubierto por tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@packages/news/src/carga-run.ts
@packages/news/src/writer.ts
@packages/news/src/writer-supabase.ts
@supabase/migrations/0084_noticia.sql
@supabase/tests/0084_noticia.test.sql

<interfaces>
Estado actual (`writer.ts`):
```ts
export interface UrlVistaRow {
  url_hash: string; url_canonica: string; outlet: string;
  estado: "pasa" | "descarta";
  causa: "prefiltro_lexico" | "duplicado" | null;
}
export interface NewsWriter {
  upsertNoticias(filas: NoticiaRow[]): Promise<void>;
  marcarVistas(filas: UrlVistaRow[]): Promise<void>;
  contarPorCausa(): Promise<Record<string, number>>;
  urlsYaVistas(hashes: string[]): Promise<Set<string>>;  // dedup nivel 1 (D-13)
}
```
`0084_noticia.sql:67-68` (constraints INLINE, nombre autogenerado por Postgres):
`estado text not null check (estado in ('pasa','descarta'))`
`causa text check (causa is null or causa in ('prefiltro_lexico','duplicado'))`

`despojarHtml(s: string): string` ya existe y está exportado en `packages/news/src/prefiltro-lexico.ts:77`.

PROD hoy: `noticia`=25, `noticia_url_vista`=245 (220 `prefiltro_lexico` + 25 `pasa`). Ninguna fila
existente queda en un estado inválido con este cambio (solo se AMPLÍA el dominio de `estado`).

Baseline de tests medido al planificar: `carga-run.test.ts` = 10, total `@obs/news` = 123. Los
pisos de abajo están calculados sobre esa línea base.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0085 — estado 'pendiente' en noticia_url_vista + pgTAP contra PROD</name>
  <read_first>
    - supabase/migrations/0084_noticia.sql (cabecera de régimen, comando de apply, ledger calificado `supabase_migrations`)
    - supabase/tests/0084_noticia.test.sql (idiom pgTAP del repo, `has_table_privilege(...)=false`)
    - CLAUDE.md §Ingesta (dos etapas, hash-check) — contexto de por qué el ledger no puede mentir
  </read_first>
  <files>supabase/migrations/0085_noticia_url_vista_pendiente.sql, supabase/tests/0085_noticia_url_vista_pendiente.test.sql</files>
  <action>
    **Descubrir PRIMERO el nombre real de la constraint** (los checks de 0084 son inline ⇒ nombre
    autogenerado; adivinarlo produce un `drop constraint` que falla o, peor, un `if exists` que
    no-opea en silencio y deja el check viejo vivo):
    ```
    PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select conname, pg_get_constraintdef(oid) from pg_constraint where conrelid='public.noticia_url_vista'::regclass and contype='c'" | tr -d '\r'
    ```
    Escribir en la migración el nombre LITERAL descubierto (sin `if exists`, para que un nombre
    equivocado falle ruidosamente dentro de la transacción).

    Migración `0085_noticia_url_vista_pendiente.sql`:
    - `alter table noticia_url_vista drop constraint <nombre_literal>;`
    - `alter table noticia_url_vista add constraint noticia_url_vista_estado_check
       check (estado in ('pasa','descarta','pendiente'));`
    - Cabecera explicando el PORQUÉ (CR-02): `pendiente` es el marcado **provisional** que se
      escribe ANTES de evaluar el pre-filtro. La causa final (`prefiltro_lexico`) solo se escribe
      DESPUÉS de que la decisión se tomó y el destino quedó confirmado. Una fila `pendiente` es
      re-evaluable en la corrida siguiente; una fila con causa final NO.
    - Documentar por qué NO se agrega `'error_carga'` a `causa` (lo sugería el review): con el
      estado `pendiente` + causa `null` el ítem ya es re-evaluable y no hace falta un vocabulario
      de causas de error que después nadie consulta; `causa` sigue significando "por qué se
      descartó", nunca "qué falló".
    - `create index if not exists noticia_url_vista_estado_idx on noticia_url_vista (estado);`
      (la consulta de dedup filtrará por estado).
    - ADITIVA/compatible: ninguna fila existente viola el nuevo check (solo amplía el dominio).
    - Aplicar con `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f ...`
      (JAMÁS `supabase db push`) y registrar el ledger:
      `insert into supabase_migrations.schema_migrations (version) values ('0085');`

    pgTAP `supabase/tests/0085_noticia_url_vista_pendiente.test.sql` (patrón 0084), corrido contra
    el schema APLICADO:
    - insert con `estado='pendiente', causa=null` **SUCCEEDS** (`lives_ok`);
    - insert con `estado='inventado'` **FAILS** (`throws_ok`) — control negativo apareado: sin él,
      un check borrado por completo pasaría el primer assert;
    - `estado='pasa'` y `estado='descarta'` siguen aceptados (no-regresión);
    - RLS sigue habilitada y `anon`/`authenticated` siguen sin select/insert/update sobre ambas
      tablas (copiar los asserts de 0084 — el gap closure no puede aflojar el deny-all);
    - `rollback` al final / fixtures limpios, como 0084.
  </action>
  <acceptance_criteria>
    - La migración quedó APLICADA a PROD: la query de `pg_constraint` muestra el nuevo
      `pg_get_constraintdef` conteniendo `pendiente` (salida pasada por `tr -d '\r'`).
    - pgTAP: `psql -tA -f supabase/tests/0085_...test.sql | tr -d '\r' > /tmp/132-09-pgtap.log` ⇒
      **0** líneas `not ok` **y ≥ 6** líneas `ok ` — el segundo gate es el que impide el cero vacuo
      (un archivo que no corre nada tiene 0 `not ok` y también 0 `ok`).
    - El ledger `supabase_migrations.schema_migrations` contiene `'0085'` (verificado por psql).
    - Ningún archivo `0073*`/`0075*` fue modificado (están escritas y NO aplicadas — JAMÁS se
      editan): `git diff --name-only 7b188f3..HEAD | grep -c "007[35]"` == 0.
    - El SUMMARY no transcribe project-ref ni credenciales (B26).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; P=/tmp/132-09-pgtap.log; test -s "$P"; test "$(grep -c "^not ok" "$P")" = "0"; test "$(grep -c "^ok " "$P")" -ge 6; test "$(git diff --name-only 7b188f3..HEAD | grep -c "007[35]")" = "0"'</automated>
  </verify>
  <done>`noticia_url_vista` acepta el estado provisional `pendiente`, probado contra el schema aplicado, con el deny-all intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: carga-run — marcado provisional neutro + promoción a causa final solo tras éxito</name>
  <read_first>
    - packages/news/src/carga-run.ts (pipeline completo; el ORDEN LOCKED de la cabecera)
    - packages/news/src/carga-run.test.ts:223-248 (el test de degradación honesta que NO mira el ledger — por eso el defecto pasó los gates)
    - packages/news/src/writer.ts (tipos + InMemoryNewsWriter)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md §CR-02
  </read_first>
  <files>packages/news/src/writer.ts, packages/news/src/carga-run.ts, packages/news/src/carga-run.test.ts</files>
  <behavior>
    - **Reproducción del fallo (test rector):** writer cuyo `upsertNoticias` LANZA una vez
      (fallo transitorio de DB) sobre un ítem que SÍ pasa el pre-filtro ⇒ tras la corrida, la fila
      del ledger para ese `url_hash` tiene `estado='pendiente'` y `causa=null`. **Jamás**
      `causa='prefiltro_lexico'`.
    - **Re-evaluación en la corrida siguiente:** con el mismo writer ya sin fallo, una segunda
      `cargar()` sobre el mismo input ⇒ el ítem **NO** se cuenta como `duplicados`, se evalúa de
      nuevo, se carga (`cargados` ≥ 1) y su ledger queda `estado='pasa', causa=null`.
    - **Control positivo apareado:** el mismo ítem SIN fallo inyectado ⇒ `cargados=1` y ledger
      `pasa` en la primera corrida; y una segunda corrida lo cuenta como `duplicados` (la
      idempotencia no se rompió al hacer re-evaluables los `pendiente`).
    - **El descarte legítimo sigue siendo terminal:** un ítem que no pasa el pre-filtro queda
      `estado='descarta', causa='prefiltro_lexico'` y en la corrida siguiente cuenta como
      `duplicados` (no se re-evalúa) — el ORDEN LOCKED de D-07 se preserva: el ledger se escribe
      ANTES de cualquier camino de rechazo, y la causa se promueve en el mismo paso en que la
      decisión se toma.
    - **WR-17:** `noticia.descripcion` se persiste ya despojado de HTML (`despojarHtml`), acorde a
      lo que declara el comentario de 0084. Test: descripción con `<b>` y `<script>alert(1)</script>`
      ⇒ la fila guardada no contiene `<`.
    - **IN-04:** el error de la rama de canonicalización deja de poner la URL cruda en `urlHash`.
  </behavior>
  <action>
    `writer.ts`:
    - `UrlVistaRow.estado` pasa a `"pasa" | "descarta" | "pendiente"`.
    - `NewsWriter.urlsYaVistas` documenta su nuevo contrato: devuelve solo los hashes **resueltos**
      (`estado in ('pasa','descarta')`); los `pendiente` se consideran NO vistos para que sean
      re-evaluables. Ajustar `InMemoryNewsWriter.urlsYaVistas` en consecuencia.
    - `CargarResult["errores"]`: renombrar `urlHash` → `ref` (IN-04) y actualizar los call-sites
      (el log de `carga-run.ts` y los tests). Es un cambio de forma interno; `run-news-cli.ts` solo
      lee `errores.length` — verificarlo antes de tocar.

    `carga-run.ts`:
    - Provisional: `{ url_hash, url_canonica, outlet, estado: "pendiente", causa: null }`.
    - Si NO pasa el pre-filtro ⇒ `marcarVistas([{ ..., estado:"descarta", causa:"prefiltro_lexico" }])`
      inmediatamente (promoción a causa final en el mismo paso de la decisión), `descartados += 1`.
      Si esa promoción falla ⇒ `errores.push(...)` y el ítem queda `pendiente` (re-evaluable).
    - Si pasa ⇒ `upsertNoticias([fila])` y **solo si tuvo éxito** `marcarVistas([{ ..., estado:"pasa",
      causa:null }])`. Cualquier fallo deja la fila en `pendiente`.
    - `descripcion: despojarHtml(item.descripcion ?? "") || null` en la fila de `noticia`
      (importar de `./prefiltro-lexico`).
    - Reescribir la cabecera §ORDEN LOCKED para que describa el nuevo pipeline y **por qué** el
      provisional no puede llevar causa final (con referencia a CR-02): la causa es un veredicto,
      y un veredicto escrito antes de conocer el resultado es una mentira que el dedup convierte
      en pérdida permanente.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/carga-run.test.ts` imprime `Tests  N passed`
      con **N ≥ 15** (baseline medido: 10; el piso exige al menos 5 tests nuevos y no puede
      satisfacerse sin agregarlos). Conteo leído del log, nunca el exit code: `vitest run` sale 0
      sin correr nada si la ruta no matchea.
    - **Mutación RECTORA de CR-02 (registrar en el SUMMARY con AMBOS resultados):** restaurar
      `causa: "prefiltro_lexico"` en el marcado provisional ⇒ el test de "ledger tras fallo
      transitorio" y el de "re-evaluación en la corrida siguiente" deben **FALLAR**, y el control
      positivo (sin fallo inyectado) debe seguir **PASANDO**. Revertir.
    - **Mutación 2:** hacer que `urlsYaVistas` vuelva a incluir los `pendiente` ⇒ el test de
      re-evaluación FALLA y el de "descarte terminal cuenta como duplicado" sigue PASANDO. Revertir.
    - **Mutación 3 (WR-17):** quitar `despojarHtml` ⇒ el test del `<script>` FALLA. Revertir.
    - `grep -c "causa: \"prefiltro_lexico\"" packages/news/src/carga-run.ts` == **1** — la causa
      final aparece UNA sola vez, en la promoción posterior a la decisión (no en el provisional).
      **Prohibido** citar esa cadena literal en los comentarios del archivo (haría el gate
      auto-invalidante); describirla en prosa.
    - `pnpm typecheck` sale 0 (exit code capturado por separado) y `pnpm --filter @obs/news test`
      sale 0 con conteo total **≥ 123** (no-regresión: la suite no puede encoger cerrando un gap).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-09-t2.log; pnpm --filter @obs/news exec vitest run src/carga-run.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 15; test "$(grep -c "causa: \"prefiltro_lexico\"" packages/news/src/carga-run.ts)" = "1"'</automated>
  </verify>
  <done>Un fallo transitorio deja el ítem re-evaluable en `pendiente` y jamás con una causa de descarte falsa; probado con reproducción del fallo, control positivo y tres mutaciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: writer-supabase — contarPorCausa con count real (WR-05) + suite del writer de PROD (WR-15)</name>
  <read_first>
    - packages/news/src/writer-supabase.ts (completo)
    - packages/tramitacion/src/writer-supabase.ts y su test (idiom del cliente doble estructural)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md §WR-05 y §WR-15
  </read_first>
  <files>packages/news/src/writer-supabase.ts, packages/news/src/writer-supabase.test.ts</files>
  <behavior>
    - **WR-05 (B-01 no renace):** `contarPorCausa()` con un cliente doble que **simula el cap de
      PostgREST** (devuelve como máximo 1.000 filas en `data` pero un `count` de 3.752) ⇒ el
      resultado reporta **3.752**, no 1.000. Este es el test que hace caer la implementación
      actual.
    - `contarPorCausa()` no descarga filas: el doble assert que cada llamada usó
      `select("*", { count: "exact", head: true })` — `head: true` significa cero payload.
    - Se cuentan TODAS las causas conocidas más las filas con `causa is null`
      (clave `sin_causa`), con una llamada por causa. La lista de causas se deriva de una constante
      exportada, no se hardcodea en dos lugares.
    - **WR-15:** `upsertNoticias` usa `onConflict: "url_hash"`; 1.200 filas se parten en **3** lotes
      de ≤500; dos filas con el mismo `url_hash` llegan como **una sola**; un `error` de PostgREST
      se convierte en `throw` cuyo mensaje contiene `error.message` y **no** contiene la service key.
    - Los mismos cuatro asserts para `marcarVistas`.
    - `urlsYaVistas` consulta por chunks de 500 y filtra `estado` a los resueltos (contrato nuevo
      de Task 2): con 1.200 hashes hace 3 consultas; una fila `pendiente` devuelta por el doble
      NO aparece en el `Set` resultante.
  </behavior>
  <action>
    Reescribir `contarPorCausa()`: una llamada por causa con
    `.select("*", { count: "exact", head: true }).eq("causa", c)`, más una pasada con
    `.is("causa", null)` cuyo resultado va a la clave `sin_causa`. Manejo de `error` que LANZA
    (nunca devolver un conteo parcial silencioso). Exportar
    `export const CAUSAS_CONOCIDAS = ["prefiltro_lexico","duplicado"] as const;` y atarla al tipo
    `UrlVistaRow["causa"]` (si el tipado limpio no sale, dejar el tipo como está y añadir un test
    que compruebe la correspondencia entre ambos).
    Añadir en `urlsYaVistas` el filtro `.in("estado", ["pasa","descarta"])` (contrato de Task 2) y
    documentar en la cabecera POR QUÉ (los `pendiente` deben re-evaluarse — CR-02).
    Cabecera: dejar escrito el gotcha B-01 con su lección (un `select` de filas sin `range()` topa
    en 1.000 y devuelve un número falso sin aviso; el propio archivo ya lo citaba para
    `urlsYaVistas` y lo olvidaba en el conteo).

    Escribir `writer-supabase.test.ts` con un cliente doble estructural que registre tabla, método,
    argumentos y opciones de cada llamada. Cero red, cero DB.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/writer-supabase.test.ts` imprime
      `Tests  N passed` con **N ≥ 10** (archivo nuevo: hoy no existe ninguno).
    - **Mutación RECTORA de WR-05:** revertir `contarPorCausa` al `select` de filas sin `count` ⇒ el
      test del cap (3.752 vs 1.000) debe **FALLAR**. Revertir. Si no falla, el doble no está
      simulando el cap — arreglar el doble, no el criterio.
    - **Mutación 2:** quitar `onConflict: "url_hash"` de `upsertNoticias` ⇒ su test FALLA. Revertir.
    - **Mutación 3:** subir `CHUNK` a 5.000 ⇒ el test de "1.200 filas → 3 lotes" FALLA. Revertir.
    - `grep -c "head: true" packages/news/src/writer-supabase.ts` ≥ 1.
    - `grep -Ec "\.select\(\"causa\"\)" packages/news/src/writer-supabase.ts` == **0** — el patrón
      capado ya no existe en el archivo (prohibido citarlo literal en comentarios).
    - `pnpm --filter @obs/news test` sale 0 con conteo total **≥ 123**; `pnpm typecheck` sale 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-09-t3.log; pnpm --filter @obs/news exec vitest run src/writer-supabase.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 10; test "$(grep -c "head: true" packages/news/src/writer-supabase.ts)" -ge 1; test "$(grep -Ec "\.select\(\"causa\"\)" packages/news/src/writer-supabase.ts)" = "0"'</automated>
  </verify>
  <done>El conteo por causa es exacto por encima de 1.000 y el writer que escribe PROD tiene tests que caen si se rompe su onConflict, su chunking o su dedupe.</done>
</task>

</tasks>

<deferred_con_razon>
- **WR-16 (batching de `marcarVistas`/`upsertNoticias` por lote):** DIFERIDO. La consecuencia grave
  del round-trip por ítem —divergencia ledger/`noticia` con pérdida de dato— la cierra CR-02 en
  este mismo plan (el ítem queda `pendiente` y se re-evalúa). Lo que queda es rendimiento
  (~270 requests por corrida sobre 245 ítems), no corrección, y el rediseño por lotes obliga a
  re-derivar el ORDEN LOCKED a nivel de lote — cambio estructural que no cabe en el presupuesto de
  contexto de este plan. Reabrir si la fase 136 mide latencia inaceptable en el cron.
- **IN-05 (`canonicalizarUrl` elimina `ref`/`source`):** DIFERIDO por diseño congelado — cambiarlo
  invalida el `url_hash` de las 245 filas ya en el ledger. Sin un caso real observado de colisión,
  moverlo sería fabricar deuda; se documenta el riesgo en la cabecera del archivo cuando alguna
  fase toque ese módulo.
</deferred_con_razon>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed de tercero → `noticia.descripcion` | HTML no confiable que la fase 137 renderizará |
| proceso → PostgREST | escrituras no transaccionales entre sí |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-31 | Repudiation (dato falso en el ledger) | causa final escrita antes de confirmar destino | mitigate | Estado provisional `pendiente` sin causa; promoción solo tras éxito; test de reproducción del fallo con mutación |
| T-132-32 | Spoofing (métrica falsa) | `contarPorCausa` capado en 1.000 | mitigate | `count: "exact", head: true` por causa; test que simula el cap |
| T-132-33 | Tampering / XSS almacenado | HTML crudo de tercero en `descripcion` | mitigate | `despojarHtml` antes de persistir (WR-17) + test con `<script>` |
| T-132-34 | Information Disclosure | service key en errores del writer | mitigate | Solo `error.message`; assert `not.toContain` de la key en los tests |
| T-132-35 | Elevation of Privilege | migración que afloje el deny-all | mitigate | pgTAP 0085 reafirma RLS + ausencia de privilegios para anon/authenticated |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm guards`, `pnpm --filter @obs/news test` (≥ 123) verdes con exit codes por separado.
- pgTAP 0085 contra el schema APLICADO: ≥ 6 `ok`, 0 `not ok`.
- Las 6 mutaciones (3 en Task 2, 3 en Task 3) registradas en el SUMMARY con el test caído Y el control que siguió pasando.
</verification>

<success_criteria>
- Fallo transitorio ⇒ ítem `pendiente` re-evaluable; jamás causa de descarte falsa.
- `contarPorCausa` devuelve el número real por encima de 1.000 filas.
- `SupabaseNewsWriter` cubierto: onConflict, chunking, dedupe, error, filtro de estado.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-09-SUMMARY.md` when done
</output>
