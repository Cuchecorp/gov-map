---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 10
type: execute
wave: 2
depends_on: ["132-08", "132-09"]
gap_closure: true
files_modified:
  - packages/news/src/connector-news.ts
  - packages/news/src/connector-news.test.ts
  - packages/news/src/feeds.ts
  - packages/news/src/feeds.test.ts
  - packages/news/src/model.ts
  - packages/news/src/run-news-cli.ts
  - packages/news/src/run-news-cli.test.ts
  - packages/news/src/index.ts
autonomous: true
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "Entre dos requests consecutivos del conector hay ≥ 2 s aunque sean de hosts distintos (el delay entre feeds del SC1 es real, no accidental)"
    - "`--feeds latercera` hace UN request, no cinco: el flag reduce la red de verdad"
    - "`--dry-run` no golpea a ningún medio ni escribe en R2; `--etapa2` sin trabajo lo dice y sale con código distinto de 0"
    - "`noticia.outlet` guarda el slug que declara el schema, y las filas ya materializadas en PROD quedan corregidas"
  artifacts:
    - path: "packages/news/src/connector-news.ts"
      provides: "gate global cross-host + assertFeedUrl en el camino real + subconjunto de feeds inyectable"
      contains: "assertFeedUrl"
    - path: "packages/news/src/run-news-cli.ts"
      provides: "CLI honesto: --feeds reduce red, --dry-run sin red, --etapa2 explícito, --from-r2 validado, argumentos fail-closed"
      contains: "R2_PATH_RE"
  key_links:
    - from: "packages/news/src/run-news-cli.ts"
      to: "NewsConnector(deps, feedsPedidos)"
      via: "el subconjunto viaja al conector, no se filtra después"
      pattern: "new NewsConnector\\(deps, "
    - from: "packages/news/src/run-news-cli.ts"
      to: "noticia.outlet = feed.slug"
      via: "parseRss recibe el slug, nunca el nombre display"
      pattern: "feed.slug"
---

<objective>
Cierra el régimen de red y la honestidad del CLI: **WR-03** (sin delay cross-host, 3 de 4 gaps de
la corrida LIVE quedaron bajo 2 s — el "delay entre feeds observable" del SC1 se cumplió por
accidente), **WR-07/WR-09** (la única aserción https es decorativa; `--from-r2` acepta cualquier
clave del bucket de crudo), **WR-01/WR-02/WR-06/WR-08/WR-10/WR-11** (flags que no hacen lo que
documentan: `--feeds` golpea igual los 5, `--etapa2` es un no-op con exit 0, `--dry-run` golpea
medios y escribe R2, un typo sin `--` corre LIVE contra PROD), **WR-04** (contrato de `outlet`:
el código guarda display, el schema declara slug, y PROD ya tiene 25 filas con el vocabulario
equivocado) e **IN-03**.

Purpose: un CLI que miente sobre lo que toca es el vector directo de gasto de red no autorizado
contra terceros y de datos con dos vocabularios en la misma columna (que rompe los joins de
134/135/137).
Output: conector con gate global y aserción https viva, CLI fail-closed, y PROD con `outlet` en un
solo vocabulario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-07-SUMMARY.md
@packages/news/src/connector-news.ts
@packages/news/src/run-news-cli.ts
@packages/news/src/feeds.ts
@packages/news/src/allowlist-news.ts
@packages/news/src/probe-feeds.ts

<interfaces>
`ConnectorDeps.rateLimiter: { wait(host: string): Promise<void> }` — `HostRateLimiter` serializa
**por host**; con 5 hosts distintos ninguno paga `minDelayMs` (ese es el defecto).
`assertFeedUrl(url: string): URL` — `packages/news/src/allowlist-news.ts:21`, exige https; hoy solo
lo invoca su propio test.
`FeedDef = { slug, outlet, url }` — `outlet` contiene el **nombre display** ("La Tercera"), que es
justo lo que NO debe llegar a la columna `noticia.outlet` (`model.ts:31` y `0084_noticia.sql:49`
declaran slug).
`slugDesdeR2Path` — `run-news-cli.ts:207`, regex `[a-z]+` (WR-11: un slug con guion o dígito se
pierde en silencio y su feed nunca se carga, con exit 0).
`probe-feeds.ts:37-41` — el patrón `sleep(3000)` explícito entre feeds que ya resolvió este mismo
problema fuera del framework.
PROD hoy: `noticia.outlet` = `La Tercera` (18) y `La Cuarta` (7); `noticia_url_vista.outlet` = los
mismos nombres display sobre 245 filas.

Baseline de tests medido al planificar: `connector-news.test.ts` = 15, `run-news-cli.test.ts` = 11
(+ los que agrega 132-08), `feeds.test.ts` = 10, total `@obs/news` = 123.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NewsConnector — gate global cross-host, assertFeedUrl en el camino real, feeds inyectables, contrato RSS 2.0</name>
  <read_first>
    - packages/news/src/connector-news.ts (tal como quedó tras 132-08)
    - packages/news/src/probe-feeds.ts:30-50 (el sleep explícito cross-host que ya funciona)
    - packages/news/src/allowlist-news.ts (assertFeedUrl)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md §WR-03, §WR-07, §WR-01, §WR-14
  </read_first>
  <files>packages/news/src/connector-news.ts, packages/news/src/connector-news.test.ts, packages/news/src/index.ts</files>
  <behavior>
    - **Gate global (WR-03):** con 5 endpoints de 5 hosts DISTINTOS, entre cada par de requests
      consecutivos el gate espera ≥ `minIntervalMs`. Medición determinista: `buildNewsDeps` acepta
      un `sleepFn` inyectable (test-only, default `setTimeout`); el test assert que `sleepFn` fue
      llamado **4 veces** con un valor ≥ 2000 al correr 5 endpoints, y que el `rateLimiter.wait`
      por host se sigue llamando (el gate ENVUELVE al limiter, no lo reemplaza).
    - **Control positivo apareado:** con `minIntervalMs: 0` el gate no espera (0 llamadas a
      `sleepFn`) y los 5 requests igual ocurren — prueba de que el test mide el gate y no "que algo
      pasó".
    - **assertFeedUrl vivo (WR-07):** con un `FEEDS` sustituto que contenga una URL `http://`,
      `endpoints()` **LANZA**. Control positivo: con los 5 feeds https reales, `endpoints()`
      devuelve 5 specs.
    - **Feeds inyectables (WR-01):** `new NewsConnector(deps, [feedLatercera])` ⇒ `endpoints()`
      devuelve **1** spec y `fetcher.get` se llama **1** vez. Default sin segundo argumento = los 5.
    - **Contrato RSS 2.0 (WR-14):** `validateShape` deja de aceptar `<feed>` de Atom (el parser de
      Etapa 2 solo entiende `rss.channel.item`, así que aceptarlo era un contrato contradictorio).
      Test: un documento Atom válido hace lanzar `validateShape` con un mensaje que dice
      explícitamente "RSS 2.0".
  </behavior>
  <action>
    `buildNewsDeps`: envolver el `rateLimiter` (el construido o el inyectado por `overrides`) en un
    gate global con estado de "último request" compartido entre hosts: espera
    `max(0, minIntervalMs - (now - last))` antes de delegar en `rl.wait(host)`, y actualiza `last`.
    Usar `sleepFn` y `nowFn` inyectables (default reales) para que el test sea determinista y
    rápido. `minIntervalMs: 0` ⇒ gate inerte (modo test).
    Cabecera: escribir POR QUÉ el gate vive acá y no en `@obs/ingest` (D-132-B: el framework no se
    toca; el 2-3 s/host del framework sigue siendo la autoridad por host, este gate agrega el piso
    cross-host que pide el SC1) y por qué replica el patrón de `probe-feeds.ts`.

    `NewsConnector`: `constructor(deps: ConnectorDeps, private readonly feeds: readonly FeedDef[] = FEEDS)`
    y `endpoints()` mapea `this.feeds`, invocando `assertFeedUrl(f.url)` por feed (la aserción https
    pasa a estar en el camino real, no solo en su test).

    `validateShape`: quitar `<feed` del regex y documentar "RSS 2.0 únicamente".
    **Alcance acotado, con razón escrita:** el review también señala que un feed que no valida
    aborta la corrida completa (`BaseConnector.run()` no captura la excepción). Eso NO se arregla
    acá: exigiría modificar `@obs/ingest`, prohibido por D-132-B. Dejar la nota en la cabecera para
    la fase que reabra el framework.

    `index.ts`: exportar lo nuevo que sea superficie pública del paquete.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/connector-news.test.ts` imprime
      `Tests  N passed` con **N ≥ 20** (baseline medido: 15; el piso exige al menos 5 tests nuevos).
      Conteo leído del log, nunca el exit code.
    - **Mutación de WR-03:** quitar el gate global (devolver el `rateLimiter` pelado) ⇒ el test de
      las 4 esperas ≥ 2000 **FALLA** y el control positivo (`minIntervalMs: 0`, 5 requests) sigue
      **PASANDO**. Revertir.
    - **Mutación de WR-07:** quitar `assertFeedUrl` de `endpoints()` ⇒ el test del feed `http://`
      FALLA y el de los 5 feeds https sigue pasando. Revertir.
    - **Mutación de WR-01:** hacer que `endpoints()` ignore `this.feeds` y devuelva `FEEDS` ⇒ el
      test de "1 feed ⇒ 1 fetch" FALLA. Revertir.
    - `grep -c "assertFeedUrl" packages/news/src/connector-news.ts` ≥ 1.
    - `<feed` no sobrevive en el cuerpo ejecutable, medido excluyendo comentarios:
      `grep -v '^\s*\*' packages/news/src/connector-news.ts | grep -v '^\s*//' | grep -Ec "<feed"` == **0**.
      (Filtrar comentarios es obligatorio: la propia nota que documenta la decisión menciona Atom y
      volvería el gate auto-invalidante.)
    - `pnpm typecheck` sale 0; `pnpm --filter @obs/news test` sale 0 con conteo total **≥ 123**.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-10-t1.log; F=packages/news/src/connector-news.ts; pnpm --filter @obs/news exec vitest run src/connector-news.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 20; test "$(grep -c "assertFeedUrl" "$F")" -ge 1; test "$(grep -v "^\s*\*" "$F" | grep -v "^\s*//" | grep -Ec "<feed")" = "0"'</automated>
  </verify>
  <done>El conector espera ≥2 s entre requests aunque cambien de host, valida https en el camino real, pide solo los feeds que le pidieron, y declara un único contrato de formato.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run-news-cli — flags que hacen lo que dicen (feeds, dry-run, etapa2, from-r2, fail-closed)</name>
  <read_first>
    - packages/news/src/run-news-cli.ts (completo, tal como quedó tras 132-08)
    - packages/news/src/run-news-cli.test.ts (no romper el par apareado de early-exit de 132-08)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md §WR-01/02/06/08/09/10/11 e §IN-03
  </read_first>
  <files>packages/news/src/run-news-cli.ts, packages/news/src/run-news-cli.test.ts</files>
  <behavior>
    - **WR-01:** `main({ feeds: ["latercera"] })` construye `new NewsConnector(deps, [latercera])` ⇒
      `fetcher.get` se llama **1** vez (no 5). Control positivo: sin `--feeds`, 5 veces.
    - **WR-06:** `main({ dryRun: true })` **sin** `--from-r2` LANZA `NewsCliArgsError` con un mensaje
      que explica que `--dry-run` no toca red ni R2 y exige `--from-r2`. Con `--from-r2` completa
      y `fetcher.get`/`putImmutable` reciben **0** llamadas. Decisión escrita: el contrato natural
      de `--dry-run` es "no producir efectos"; se elige hacerlo cumplir en vez de renombrarlo.
    - **WR-02:** `--etapa1` y `--etapa2` juntos ⇒ `NewsCliArgsError`. `--etapa2` sin `--from-r2` y
      sin snapshots del día que cargar ⇒ mensaje explícito ("no hay nada que cargar") y **exit ≠ 0**
      (nunca éxito silencioso).
    - **WR-08:** un argumento que no empieza con `--` (typo `-dry-run`, `dry-run`) ⇒
      `NewsCliArgsError` (fail-closed). El separador `--` sigue ignorándose.
    - **WR-09:** `--from-r2` valida contra `^news/rss-(<alternancia derivada de FEEDS>)/\d{4}-\d{2}-\d{2}/[0-9a-f]{64}\.xml$`
      y rechaza `otra-fuente/...` y `news/rss-latercera/2026-08-05/noesunsha.xml`. Además, el
      `contenidoHash` deja de ser `""`: se deriva del sha256 presente en la clave validada.
    - **WR-10:** faltando CUALQUIERA de las 4 variables R2 ⇒ `NewsR2RequeridoError` cuyo mensaje
      lista **las que faltan por nombre** (jamás por valor).
    - **WR-11:** `slugDesdeR2Path` acepta `[a-z0-9-]+`; test con un slug con guion y con dígito.
    - **IN-03:** el resumen final del CLI imprime el conteo por causa (`contarPorCausa`, ya correcto
      tras 132-09) cuando la Etapa 2 escribió en la DB.
    - Cero red en el archivo de test: el stub de `fetch` que lanza sigue instalado a nivel de
      archivo, con su caso de control que comprueba que explota.
  </behavior>
  <action>
    Implementar los cambios anteriores en `run-news-cli.ts`. Derivar `R2_PATH_RE` de `FEEDS` (no
    escribir la alternancia a mano: se desincroniza al agregar un feed) y exportarla para el test.
    `parseArgs`: `default: throw new NewsCliArgsError(...)` para cualquier argumento que no sea `--`.
    Validación cruzada de flags (`soloEtapa1 && soloEtapa2`, `dryRun && !fromR2`) ANTES de tocar
    red/DB. Actualizar la cabecera del archivo: la ADVERTENCIA vigente de que `--dry-run` consume la
    caché diaria queda **obsoleta** (ya no corre Etapa 1) — reescribirla, no dejar dos versiones
    contradictorias conviviendo.
    Pasar `feedsPedidos` al constructor del conector y eliminar el filtrado posterior de `refs`
    (el `[skip]` derivado sigue comparando `feedsPedidos` vs `refs`, ahora sobre el mismo conjunto).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/run-news-cli.test.ts` imprime `Tests  N passed`
      con **N ≥ 22** (baseline 11 + los ~4 de 132-08 + al menos 7 nuevos), y los tests del par
      apareado de early-exit de 132-08 siguen pasando.
    - **Comportamiento medido end-to-end, no solo unitario:** un argumento inválido sale con
      código **2**, capturado en la forma que `set -e` no intercepta y con el separador `--`
      obligatorio (sin él `pnpm` intercepta el flag y el criterio mediría el parser de pnpm):
      `if pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- -dry-run > /tmp/132-10-cli.log 2>&1; then rc=0; else rc=$?; fi; test "$rc" -eq 2`
      y el log contiene literalmente `-dry-run`.
    - **Mutación de WR-06:** quitar la validación `dryRun && !fromR2` ⇒ el test de "dry-run sin
      from-r2 lanza" FALLA y el test de "dry-run con from-r2 completa con 0 fetches" sigue PASANDO.
      Revertir.
    - **Mutación de WR-09:** aceptar cualquier clave ⇒ el test de `otra-fuente/...` FALLA y el de
      la clave válida sigue pasando. Revertir.
    - **Mutación de WR-01:** volver a filtrar `refs` después de pedir los 5 ⇒ el test de "1 feed ⇒
      1 fetch" FALLA. Revertir.
    - `grep -c "R2_PATH_RE" packages/news/src/run-news-cli.ts` ≥ 1.
    - `grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" packages/news/src/run-news-cli.ts` == 0 (B26).
    - `pnpm typecheck` sale 0; `pnpm --filter @obs/news test` sale 0 con conteo total **≥ 123**.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-10-t2.log; F=packages/news/src/run-news-cli.ts; pnpm --filter @obs/news exec vitest run src/run-news-cli.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 22; if pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- -dry-run > /tmp/132-10-cli.log 2>&1; then c=0; else c=$?; fi; echo "cli_rc=$c"; test "$c" -eq 2; grep -q -- "-dry-run" /tmp/132-10-cli.log; test "$(grep -c "R2_PATH_RE" "$F")" -ge 1; test "$(grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" "$F")" = "0"'</automated>
  </verify>
  <done>Cada flag del CLI hace exactamente lo que documenta, falla cerrado ante typos, y `--dry-run` deja de ser una corrida LIVE con otro nombre.</done>
</task>

<task type="auto">
  <name>Task 3: [WR-04] Contrato de `outlet` = slug, en código y en las filas ya materializadas en PROD</name>
  <read_first>
    - packages/news/src/feeds.ts (FeedDef con `slug` y `outlet`-display)
    - packages/news/src/model.ts:31 y supabase/migrations/0084_noticia.sql:49 (ambos declaran SLUG)
    - packages/news/src/run-news-cli.ts:281 y :379 (los dos sitios que pasan el display a `parseRss`)
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-07-SUMMARY.md §Paso 5 (La Tercera=18, La Cuarta=7)
  </read_first>
  <files>packages/news/src/feeds.ts, packages/news/src/feeds.test.ts, packages/news/src/model.ts, packages/news/src/run-news-cli.ts, packages/news/src/run-news-cli.test.ts</files>
  <action>
    **Código — hacer imposible el error, no solo corregirlo:** renombrar el campo `FeedDef.outlet`
    (nombre display) a `FeedDef.display`, de modo que ningún call-site pueda pasar el display
    creyendo que pasa el outlet. Actualizar todos los consumidores (`feeds.test.ts`,
    `probe-feeds.ts` si aplica, el CLI). El CLI pasa `feed.slug` a `parseRss` en AMBOS sitios
    (rama `--from-r2` y bucle de Etapa 2). El fallback `?? "desconocido"` desaparece: tras WR-09 el
    slug siempre sale de una clave validada contra `FEEDS`; si no se resuelve, es un
    `NewsCliArgsError`, no un valor inventado en la columna.
    Test: para los 5 feeds, el valor que llega a `NoticiaRow.outlet` está en
    `FEEDS.map(f => f.slug)` — con un caso negativo que verifica que un display ("La Tercera") NO
    puede llegar a esa columna.

    **PROD — corregir las filas ya materializadas.** UPDATE parametrizado por psql (jamás
    concatenación de strings), con el mapa display→slug explícito y en UNA transacción:
    ```
    PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 <<'SQL'
    with mapa(display, slug) as (values
      ('BioBioChile','biobiochile'),('Cooperativa','cooperativa'),
      ('La Tercera','latercera'),('La Cuarta','lacuarta'),('Ex-Ante','exante'))
    update noticia n set outlet = m.slug from mapa m where n.outlet = m.display;
    with mapa(display, slug) as (values
      ('BioBioChile','biobiochile'),('Cooperativa','cooperativa'),
      ('La Tercera','latercera'),('La Cuarta','lacuarta'),('Ex-Ante','exante'))
    update noticia_url_vista v set outlet = m.slug from mapa m where v.outlet = m.display;
    SQL
    ```
    Antes y después: `select outlet, count(*) from noticia group by 1 order by 1` y lo mismo para
    `noticia_url_vista`, con `-tA | tr -d '\r'`. Registrar ambas salidas en el SUMMARY.

    **Por qué NO se agrega un `check (outlet in (...))` a la tabla** (el review lo ofrecía como
    alternativa): congelaría el set de feeds en el schema y obligaría a una migración cada vez que
    se agregue un medio — el congelamiento correcto vive en `feeds.ts` (`Object.freeze` + test).
    Dejar esta razón escrita en el SUMMARY.
  </action>
  <acceptance_criteria>
    - PROD, después del UPDATE: **cero** filas con vocabulario display.
      `select count(*) from noticia where outlet !~ '^[a-z0-9-]+$'` == `0` y lo mismo para
      `noticia_url_vista` (ambas salidas por `-tA | tr -d '\r'`).
      **Control positivo apareado obligatorio** (sin él, un `0` podría venir de una tabla vacía o de
      un typo en la consulta): `select count(*) from noticia where outlet in ('latercera','lacuarta')`
      == **25** y `select count(*) from noticia_url_vista` == **245** (los totales no cambiaron:
      fue un UPDATE, no un DELETE).
    - La composición por outlet se conserva: `latercera`=18 y `lacuarta`=7 (los mismos números que
      traían `La Tercera` y `La Cuarta` en 132-07).
    - `grep -c "display" packages/news/src/feeds.ts` ≥ 5 (el campo se renombró en los 5 feeds).
    - `grep -c "desconocido" packages/news/src/run-news-cli.ts` == **0** (el fallback inventado murió).
    - `pnpm --filter @obs/news exec vitest run src/feeds.test.ts src/run-news-cli.test.ts` imprime
      `Tests  N passed` con **N ≥ 34** (baseline 10 + los ≥22 de la Task 2 + el caso del contrato
      de outlet y su negativo).
    - **Mutación:** volver a pasar `feed.display` a `parseRss` ⇒ el test del contrato de outlet
      FALLA. Revertir.
    - El SUMMARY no transcribe project-ref ni credenciales (B26).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-10-t3.log; pnpm --filter @obs/news exec vitest run src/feeds.test.ts src/run-news-cli.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 34; test "$(grep -c "desconocido" packages/news/src/run-news-cli.ts)" = "0"; test "$(grep -c "display" packages/news/src/feeds.ts)" -ge 5'</automated>
  </verify>
  <done>`outlet` significa una sola cosa —el slug— en el modelo, en el código y en las filas que ya viven en PROD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv → CLI | flags de un operador que decide qué se golpea y qué se escribe |
| clave R2 arbitraria → parser/DB | `--from-r2` puede apuntar a cualquier objeto del bucket de crudo |
| conector → medios (terceros) | requests salientes sujetos al régimen de ingesta respetuosa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-36 | Denial of Service (a terceros) | ráfaga cross-host sin delay | mitigate | Gate global ≥2 s envolviendo el rateLimiter; test con `sleepFn` inyectable y control positivo |
| T-132-37 | Spoofing (falso verde operacional) | `--dry-run`/`--feeds` que igual golpean 5 medios | mitigate | `--dry-run` exige `--from-r2` y no cablea R2; `--feeds` viaja al constructor del conector; tests con conteo de `fetcher.get` |
| T-132-38 | Tampering | `--from-r2` con clave arbitraria del bucket cargada como si fuera prensa | mitigate | `R2_PATH_RE` derivada de `FEEDS`; `contenidoHash` derivado del sha de la clave, no `""` |
| T-132-39 | Elevation of Privilege (SSRF) | feed `http://` aceptado por `extraHosts` | mitigate | `assertFeedUrl` invocado en `endpoints()` del camino real + test con feed http |
| T-132-40 | Repudiation | dos vocabularios en `noticia.outlet` | mitigate | Rename a `display`, slug al persistir, UPDATE de PROD con conteos antes/después |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm guards`, `pnpm test` (raíz) y `pnpm --filter @obs/news test` (≥ 123) verdes, exit codes por separado.
- `git diff --name-only 7b188f3..HEAD | grep -c '^packages/ingest/'` == 0 (D-132-B).
- Las 7 mutaciones registradas en el SUMMARY con el test caído Y el control que siguió pasando.
- Conteos PROD antes/después del UPDATE de `outlet`, con `tr -d '\r'`.
</verification>

<success_criteria>
- ≥ 2 s entre requests consecutivos aunque cambien de host, probado sin esperar 10 s reales.
- `--feeds latercera` = 1 request; `--dry-run` = 0 requests y 0 escrituras en R2; `--etapa2` vacío = mensaje + exit ≠ 0; typo de flag = exit 2.
- PROD sin ninguna fila con nombre display en `outlet`, con totales intactos (25 / 245).
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-10-SUMMARY.md` when done
</output>
