---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 05
type: execute
wave: 3
depends_on: ["132-02", "132-04"]
files_modified:
  - packages/news/src/writer.ts
  - packages/news/src/writer-supabase.ts
  - packages/news/src/carga-run.ts
  - packages/news/src/carga-run.test.ts
autonomous: true
requirements: [NEWS-01, NEWS-02]

must_haves:
  truths:
    - "Correr la carga dos veces con el mismo crudo NO duplica filas (upsert por url_hash)"
    - "Toda URL vista queda registrada en el ledger ANTES de cualquier camino de rechazo"
    - "El conteo de descartes por causa es consultable"
    - "Un feed con error no aborta la carga de los otros"
  artifacts:
    - path: "packages/news/src/writer.ts"
      provides: "Interfaz NewsWriter + InMemoryNewsWriter para dry-run y tests"
    - path: "packages/news/src/writer-supabase.ts"
      provides: "SupabaseNewsWriter (service_role, upsert idempotente, chunking, dedupe)"
      contains: "onConflict"
    - path: "packages/news/src/carga-run.ts"
      provides: "Etapa 2 orquestada: parse → canonicalizar → MARCAR VISTA → pre-filtro → noticia"
  key_links:
    - from: "packages/news/src/carga-run.ts"
      to: "noticia_url_vista"
      via: "marcar vista ANTES del reject (Pitfall 11)"
      pattern: "marcarVista"
---

<objective>
Escribir la Etapa 2 orquestada: de ítems parseados a filas en `noticia` y `noticia_url_vista`, con
el **orden LOCKED** que exige D-07/Pitfall 11 — la URL se marca vista **antes** de cualquier camino
de rechazo, o los descartados se re-procesan eternamente y el conteo queda inflado.

Purpose: aquí vive la idempotencia (correr 2× no duplica) y el conteo observable de descartes que
pide el SC4.
Output: writers (fake + Supabase) y el orquestador, con tests que corren sin red ni DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-PATTERNS.md
@packages/tramitacion/src/writer.ts
@packages/tramitacion/src/writer-supabase.ts
@packages/tramitacion/src/ingest-run.ts
@packages/news/src/model.ts
@packages/news/src/canonicalizar-url.ts
@packages/news/src/prefiltro-lexico.ts
@supabase/migrations/0084_noticia.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: writer.ts (interfaz + InMemory) y writer-supabase.ts (upsert idempotente)</name>
  <read_first>
    - packages/tramitacion/src/writer.ts:1-40 (cabecera que declara la clave natural + interfaz + fake)
    - packages/tramitacion/src/writer-supabase.ts:1-80 (cliente service_role, upsert con onConflict, CHUNK=500, dedupePorClave, regla de secretos)
    - supabase/migrations/0084_noticia.sql (columnas exactas tal como quedaron aplicadas)
  </read_first>
  <files>packages/news/src/writer.ts, packages/news/src/writer-supabase.ts</files>
  <action>
    `writer.ts`: `export interface NewsWriter` con
      `upsertNoticias(filas: NoticiaRow[]): Promise<void>`
      `marcarVistas(filas: UrlVistaRow[]): Promise<void>`
      `contarPorCausa(): Promise<Record<string, number>>` (para el reporte del CLI; en el fake se
      calcula en memoria, en Supabase por query agrupada)
      `urlsYaVistas(hashes: string[]): Promise<Set<string>>` (dedup nivel 1, D-13)
    Más `export class InMemoryNewsWriter implements NewsWriter` con Maps.
    Cabecera declarando la clave natural, igual que el analog:
      `noticia            → PK url_hash`
      `noticia_url_vista  → PK url_hash`
      "Correr la carga 2× con el mismo input NO duplica filas (upsert, no insert)."

    `writer-supabase.ts`: `SupabaseNewsWriter implements NewsWriter`, cliente creado con
    `createClient(url, serviceKey, { auth: { persistSession: false, autoRefreshToken: false } })`
    e inyectable por `opts.client` para tests. `upsert(filas, { onConflict: "url_hash", ignoreDuplicates: false })`.
    `CHUNK = 500` y `dedupePorClave(filas, f => f.url_hash)` **obligatorios**: La Tercera y La Cuarta
    traen 100 ítems cada uno y un mismo `url_hash` puede repetirse dentro del lote — sin dedupe,
    Postgres aborta con `command cannot affect row a second time`.
    `urlsYaVistas`: consultar `noticia_url_vista` filtrando `in('url_hash', chunk)` **paginando por
    chunks**; nunca traer la tabla entera (PostgREST capa en 1.000 filas — gotcha v6.1).
    Regla de secretos: la service key JAMÁS se interpola en mensajes de error; solo se propaga
    `error.message` de PostgREST. **El project-ref de Supabase no se escribe en ningún archivo**
    (B26): la URL viene de env, nunca hardcodeada.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` sale 0.
    - `grep -Ec "serviceKey|SUPABASE_SECRET|SUPABASE_LOCAL_SERVICE_KEY" packages/news/src/writer-supabase.ts` > 0 solo en la construcción del cliente y en tipos; `grep -n "throw\|Error(" packages/news/src/writer-supabase.ts` no muestra ninguna interpolación de la key en el mensaje.
    - `grep -Eic "supabase\.co|\.supabase\.in|[a-z]{20}\.supabase" packages/news/src/*.ts` == 0 (B26: cero project-ref transcrito).
    - `grep -c "onConflict" packages/news/src/writer-supabase.ts` ≥ 2 (una por tabla).
    - `grep -c "dedupePorClave\|CHUNK" packages/news/src/writer-supabase.ts` ≥ 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec tsc -b</automated>
  </verify>
  <done>Existe la interfaz con fake in-memory y la implementación Supabase con upsert idempotente, chunking, dedupe y cero secretos en artefactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: carga-run.ts — orden LOCKED (marcar vista ANTES del reject) + idempotencia</name>
  <read_first>
    - packages/tramitacion/src/ingest-run.ts (contrato `runIngest({...,writer,log})`, conteos + `errores[]` para degradación honesta)
    - packages/news/src/writer.ts (recién escrito)
    - packages/news/src/prefiltro-lexico.ts y canonicalizar-url.ts (132-04)
    - 132-RESEARCH.md §Pitfall 11 y §Anti-patterns (marcar vista después del reject = bug de ICS)
  </read_first>
  <files>packages/news/src/carga-run.ts, packages/news/src/carga-run.test.ts</files>
  <behavior>
    - **Orden LOCKED verificable:** para un ítem que el pre-filtro descarta, la traza de llamadas al writer muestra `marcarVistas` ANTES de cualquier decisión de descarte, y `upsertNoticias` NO se llama con ese ítem. Assert sobre la traza ordenada, no sobre el estado final.
    - Idempotencia: correr `cargar()` dos veces con el mismo input produce el mismo nº de filas en `noticia` (el fake modela upsert por `url_hash`), y la segunda corrida reporta los ítems como `duplicados`, no como nuevos.
    - Dedup nivel 1: un `url_hash` ya presente en `noticia_url_vista` se cuenta como `duplicado` y NO se re-evalúa por el pre-filtro (`esLegislativo` no se llama para él — verificar con un espía).
    - Dedup nivel 2: dos ítems del mismo lote cuyas URLs difieren solo en `utm_*` colapsan a una sola fila de `noticia`.
    - Conteos: el resultado incluye `vistos`, `nuevos`, `duplicados`, `descartados`, `cargados` y `errores[]`, y `descartados + cargados + duplicados == vistos`.
    - Degradación honesta: un ítem que revienta el writer se acumula en `errores[]` con `{ urlHash, etapa, mensaje }` y NO aborta el resto del lote.
    - Cero red: el módulo no importa `Fetcher` ni hace `fetch`.
  </behavior>
  <action>
    `export interface CargarOpts { items: RssItem[]; r2Path: string; contenidoHash: string; writer: NewsWriter; log: (s: string) => void }`
    `export interface CargarResult { vistos; nuevos; duplicados; descartados; cargados; errores: {urlHash:string;etapa:string;mensaje:string}[] }`
    `export async function cargar(opts: CargarOpts): Promise<CargarResult>`

    Pipeline, en este orden EXACTO (cabecera del archivo debe declararlo como LOCKED por D-07/Pitfall 11):
      1. canonicalizar URL de cada ítem + `urlHash`; colapsar duplicados dentro del lote.
      2. `writer.urlsYaVistas(hashes)` → los ya vistos se cuentan como `duplicados` y salen del flujo
         **sin** re-evaluar el pre-filtro.
      3. **`writer.marcarVistas(...)` para TODOS los ítems nuevos, con `estado`/`causa` provisorios**
         — ANTES de aplicar `esLegislativo`. Si el proceso muere entre marcar y cargar, se re-procesa
         un ítem ya marcado (aceptable); el orden inverso perdería el registro del descarte.
      4. `esLegislativo(titulo, descripcion)`: los que pasan → fila de `noticia` con
         `estado='pendiente'`, `r2_path`, `contenido_hash`, `outlet`, `fecha_pub`; y su entrada del
         ledger queda `estado='pasa', causa=null`. Los que no → ledger `estado='descarta',
         causa='prefiltro_lexico'`. (Se puede hacer con un `marcarVistas` inicial + un segundo
         `marcarVistas` que actualice el resultado, siempre que el PRIMERO ocurra antes del filtro.)
      5. `writer.upsertNoticias(filas)`.
      6. Log final con los 5 conteos y los errores, patrón `ingest-cli.ts:305-311`.

    Todos los colaboradores por parámetro (writer, log) ⇒ tests sin red ni DB.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/carga-run.test.ts` reporta `Tests  N passed` con N ≥ 10.
    - **Mutación rectora (registrar en el SUMMARY):** mover la llamada a `marcarVistas` DESPUÉS de la evaluación del pre-filtro ⇒ el test de orden LOCKED debe FALLAR. Si no falla, el test está mirando el estado final en vez de la traza — reescribirlo. Revertir.
    - **Mutación de idempotencia:** cambiar el upsert del fake por un push ciego ⇒ el test de "correr 2× no duplica" FALLA. Revertir.
    - **Mutación de dedup nivel 2:** quitar `canonicalizarUrl` del paso 1 ⇒ el test de los dos ítems con `utm_*` distintos FALLA. Revertir.
    - El test verifica el invariante aritmético `descartados + cargados + duplicados === vistos` sobre un lote mixto construido desde los fixtures reales.
    - `grep -Ec "fetch\(|Fetcher|createClient" packages/news/src/carga-run.ts` == 0 — el orquestador no toca red ni construye clientes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @obs/news exec vitest run src/carga-run.test.ts</automated>
  </verify>
  <done>La Etapa 2 orquestada marca toda URL vista antes de cualquier rechazo, es idempotente, deduplica en dos niveles y degrada honestamente — probado por traza y por mutación.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI local (service_role) → PROD | escritura privilegiada que bypassa RLS |
| datos de tercero → filas de Postgres | contenido no confiable persistido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-13 | Information Disclosure | `writer-supabase.ts` | mitigate | service key nunca interpolada en errores; solo `error.message` de PostgREST; project-ref jamás transcrito (grep == 0, B26) |
| T-132-14 | Tampering | upsert sin dedupe | mitigate | `dedupePorClave` + `CHUNK=500`; sin eso Postgres aborta el lote (`command cannot affect row a second time`) |
| T-132-15 | Repudiation | ledger de URLs vistas | mitigate | `marcarVistas` ANTES del reject; test de traza con mutación que lo prueba |
</threat_model>

<verification>
- `pnpm --filter @obs/news test` verde; conteo de tests de este plan ≥ 10.
- Las 3 mutaciones registradas con el test que cayó.
</verification>

<success_criteria>
- Correr `cargar()` 2× con el mismo input no duplica filas.
- La traza prueba `marcarVistas` antes del pre-filtro.
- `descartados + cargados + duplicados === vistos`.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-05-SUMMARY.md` when done
</output>
