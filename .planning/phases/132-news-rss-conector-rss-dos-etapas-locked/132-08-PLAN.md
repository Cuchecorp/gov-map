---
phase: 132-news-rss-conector-rss-dos-etapas-locked
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - packages/news/src/snapshot-lookup-supabase.ts
  - packages/news/src/snapshot-lookup-supabase.test.ts
  - packages/news/src/connector-news.ts
  - packages/news/src/connector-news.test.ts
  - packages/news/src/run-news-cli.ts
  - packages/news/src/run-news-cli.test.ts
  - packages/news/src/index.ts
autonomous: true
requirements: [NEWS-01]

must_haves:
  truths:
    - "Una re-corrida de Etapa 1 en el MISMO date-bucket sale temprano: `[skip]` por feed y CERO requests HTTP a los medios"
    - "El `cache.hasToday` que corre en producción consulta `source_snapshot` de verdad — no queda ningún doble no-op en el camino real"
    - "El framework compartido `@obs/ingest` sigue sin ser modificado (D-132-B)"
  artifacts:
    - path: "packages/news/src/snapshot-lookup-supabase.ts"
      provides: "SupabaseSnapshotLookup: implementación de SnapshotLookup contra source_snapshot"
      contains: "source_snapshot"
    - path: "packages/news/src/snapshot-lookup-supabase.test.ts"
      provides: "Test unitario del lookup (hit/miss/error) con cliente doble"
    - path: "packages/news/src/run-news-cli.test.ts"
      provides: "Test de early-exit con DailyCache REAL: fetcher.get 0 veces + N líneas [skip]"
  key_links:
    - from: "packages/news/src/run-news-cli.ts"
      to: "packages/news/src/snapshot-lookup-supabase.ts"
      via: "buildNewsDeps({ cache: new DailyCache(new SupabaseSnapshotLookup(...)) })"
      pattern: "DailyCache"
    - from: "packages/news/src/snapshot-lookup-supabase.ts"
      to: "source_snapshot (Postgres)"
      via: "select id where source/resource/date_bucket"
      pattern: "date_bucket"
---

<objective>
Cierra **GAP-SC2 / CR-01**: el hash-check ANTES de descargar (regla LOCKED de CLAUDE.md §Ingesta
punto 2 y SC2 de la fase) no existe en el camino real porque `buildNewsDeps()` deja
`cache.hasToday` como doble no-op de **producción** y ningún caller lo sobrescribe. Este plan
escribe el `SnapshotLookup` respaldado en `source_snapshot` DENTRO de `packages/news` (cero
cambios a `@obs/ingest`, D-132-B), lo cablea como `DailyCache` real, lo prueba con mutación, y
produce la evidencia empírica del SC2 contra PROD.

Purpose: sin esto la fase 136 (cron diario L-V) re-scrapea los 5 medios cada día — exactamente
el hueco #4 de Is Chile Safe que el goal de la fase declara cerrado.
Output: `SupabaseSnapshotLookup` + wiring + test de early-exit que cae si se revierte + corrida
de evidencia con `[skip]` real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-CONTEXT.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-VERIFICATION.md
@.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-REVIEW.md
@packages/news/src/connector-news.ts
@packages/news/src/run-news-cli.ts
@packages/ingest/src/cache.ts
@packages/ingest/src/snapshot-store-supabase.ts

<interfaces>
De `@obs/ingest` (packages/ingest/src/cache.ts) — YA EXPORTADO, no se toca:
```ts
export interface SnapshotLookup {
  hasSnapshot(source: string, resource: string, dateBucket: string): Promise<boolean>;
}
export function dateBucket(now: Date): string;      // YYYY-MM-DD en UTC
export class DailyCache {
  constructor(lookup: SnapshotLookup);
  dailyKey(source: string, spec: CacheSpec, now: Date): Promise<string>;
  hasToday(source: string, spec: CacheSpec, now: Date): Promise<boolean>; // lookup.hasSnapshot(source, spec.resource, dateBucket(now))
}
```
`BaseConnector.run()` (packages/ingest/src/base-connector.ts:124):
`if (await this.deps.cache.hasToday(this.sourceId, spec, now)) continue;` — `continue` SILENCIOSO
(no llama `log.skip`). El `[skip]` observable se DERIVA en el CLI comparando slugs pedidos vs
`refs` devueltos (D-132-B, ya implementado en `run-news-cli.ts:356-366`) — ese código YA existe y
es correcto; lo que faltaba era que `hasToday` pudiera devolver `true` alguna vez.

`sourceId` del conector news = `"news"`; `resource` por feed = `rss-<slug>`; `date_bucket` en
`source_snapshot` es `YYYY-MM-DD` (unique `(source,resource,date_bucket)`).

Patrón de cliente estructural (no acoplar el package a un tipo concreto): ver
`SupabaseClientLike` en `packages/ingest/src/snapshot-store-supabase.ts:34-51`.

Baseline de tests del paquete (medido al planificar): `connector-news.test.ts` = 15,
`run-news-cli.test.ts` = 11, total `@obs/news` = 123. Los pisos de abajo están calculados sobre
esa línea base: un piso por debajo del baseline sería un gate que pasa sin haber agregado nada.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SupabaseSnapshotLookup — consulta real a source_snapshot (+ test con cliente doble)</name>
  <read_first>
    - packages/ingest/src/cache.ts (interfaz `SnapshotLookup`, `DailyCache`, `dateBucket`)
    - packages/ingest/src/snapshot-store-supabase.ts:34-131 (tipado estructural del cliente + manejo de `error` sin filtrar la service key)
    - packages/news/src/writer-supabase.ts:49-58 (construcción de cliente con service key: `auth: { persistSession:false, autoRefreshToken:false }`)
  </read_first>
  <files>packages/news/src/snapshot-lookup-supabase.ts, packages/news/src/snapshot-lookup-supabase.test.ts</files>
  <behavior>
    - `hasSnapshot("news","rss-latercera","2026-08-05")` ⇒ `true` cuando el doble devuelve una fila.
    - Mismo llamado ⇒ `false` cuando el doble devuelve `data: null`.
    - Si PostgREST devuelve `error` ⇒ LANZA con `error.message` en el mensaje. **Jamás degradar a
      `false`**: un `false` silencioso ante error de DB re-descargaría los 5 medios creyendo que no
      hay caché — el fallo debe ser ruidoso (mismo criterio que el fallo duro sin R2, T-132-17).
    - El doble registra los filtros aplicados: se assertan los TRES `eq` (`source`, `resource`,
      `date_bucket`) con los valores exactos recibidos. Un lookup que filtre solo por `source`
      devolvería `true` para cualquier recurso/día y produciría un `[skip]` mentiroso.
    - La service key NUNCA aparece en el mensaje de error (assert por `not.toContain`).
  </behavior>
  <action>
    Crear `packages/news/src/snapshot-lookup-supabase.ts`:
    `export class SupabaseSnapshotLookup implements SnapshotLookup` (importar el tipo de `@obs/ingest`).
    Constructor con `{ url, serviceKey, client? }` (espejo de `SupabaseNewsWriterOptions`): si se
    pasa `client`, se usa; si no, `createClient(url, serviceKey, { auth: { persistSession:false,
    autoRefreshToken:false } })`. El project-ref JAMÁS se transcribe en el archivo (B26).

    `hasSnapshot(source, resource, dateBucket)`:
    `.from("source_snapshot").select("id").eq("source",source).eq("resource",resource)
     .eq("date_bucket",dateBucket).limit(1).maybeSingle()`.
    `if (error) throw new Error(\`hasSnapshot falló: ${error.message}\`)`; `return data != null`.
    `limit(1)` antes de `maybeSingle()` es deliberado: el unique `(source,resource,date_bucket)`
    garantiza a lo más una fila, pero `maybeSingle()` sin `limit` lanzaría si alguna vez hubiera
    dos — y ante duda queremos `true` (no re-descargar), no una excepción.

    Escribir el test con un cliente doble estructural que registre los `eq` recibidos.
    Cabecera del archivo: explicar por qué esta clase vive en `packages/news` y NO en `@obs/ingest`
    (D-132-B: el framework no se toca en esta fase; `ConnectorDeps.cache` es un punto de inyección).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/snapshot-lookup-supabase.test.ts` imprime
      `Tests  N passed` con **N ≥ 5** (leer el CONTEO impreso, no el exit code: `vitest run` sale 0
      sin correr nada si la ruta no matchea).
    - **Mutación obligatoria (registrar en el SUMMARY):** cambiar el `throw` del branch de `error`
      por `return false` ⇒ el test del error debe FALLAR; los tests hit/miss deben seguir PASANDO
      (si caen los tres, el test no está midiendo el manejo de error). Revertir.
    - **Mutación 2:** quitar el `.eq("date_bucket", ...)` ⇒ el test que assert los tres filtros debe
      FALLAR. Revertir.
    - `grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" packages/news/src/snapshot-lookup-supabase.ts` == 0 (B26).
    - `pnpm typecheck` sale 0 (exit code capturado en su propia variable, nunca encadenado con `&&`).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-08-t1.log; pnpm --filter @obs/news exec vitest run src/snapshot-lookup-supabase.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 5; test "$(grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" packages/news/src/snapshot-lookup-supabase.ts)" = "0"'</automated>
  </verify>
  <done>Existe una implementación real de `SnapshotLookup` contra `source_snapshot`, probada en hit/miss/error y con los tres filtros assertados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cablear DailyCache real en buildNewsDeps/CLI + test de early-exit con mutación</name>
  <read_first>
    - packages/news/src/connector-news.ts:107-145 (el default no-op de `cache` que hay que matar)
    - packages/news/src/run-news-cli.ts:216-368 (resolución de credenciales, `buildNewsDeps`, derivación del `[skip]`)
    - packages/news/src/run-news-cli.test.ts:66-112 (test del `[skip]` derivado que hoy prueba un escenario que producción no puede producir — IN-02)
    - packages/news/src/index.ts (barrel)
  </read_first>
  <files>packages/news/src/connector-news.ts, packages/news/src/connector-news.test.ts, packages/news/src/run-news-cli.ts, packages/news/src/run-news-cli.test.ts, packages/news/src/index.ts</files>
  <behavior>
    - **Early-exit real:** con `buildNewsDeps({ cache: new DailyCache(lookupDoble) })` donde el
      lookup devuelve `true` para los 5 recursos ⇒ `fetcher.get` se llama **0 veces**, `run()`
      devuelve `refs = []`, y el CLI emite **5 líneas `[skip]`** con `descargados=0 skips=5`.
    - **Control positivo apareado (varía UNA sola variable):** el MISMO montaje con el lookup
      devolviendo `false` ⇒ `fetcher.get` se llama 5 veces y `descargados=5 skips=0`. Difiere
      únicamente en el valor que devuelve `hasSnapshot`.
    - **El lookup recibe los argumentos correctos:** se assert que `hasSnapshot` fue invocado con
      `source="news"`, `resource="rss-<slug>"` para los 5 slugs y el `dateBucket` derivado del `now`
      inyectado (`deps.now` fijo, fecha determinista) — no un `dateBucket` calculado a otra hora.
    - **El default de producción ya no es un no-op:** un test verifica que
      `buildNewsDeps({})` **lanza** un error explícito si no recibe ni `cache` ni las credenciales
      para construirlo (ver `<action>`), assertado por tipo y por mensaje. Un default silencioso
      que "no encuentra caché" nunca vuelve a ser aceptable.
    - Cero red en el archivo de test: `vi.stubGlobal("fetch", () => { throw new Error("red prohibida en tests"); })`
      instalado a nivel de archivo y restaurado en `afterEach`, con un caso de control que
      comprueba que el stub efectivamente explota.
  </behavior>
  <action>
    **`connector-news.ts` — matar el doble no-op de producción.** Reemplazar el default actual de
    `cache` (el objeto con `dailyKey`/`hasToday` inertes) por:
    - si `overrides.cache` viene ⇒ usarlo;
    - si no, y `opts` trae `supabase: { url, serviceKey }` ⇒
      `new DailyCache(new SupabaseSnapshotLookup({ url, serviceKey }))`;
    - si no viene ninguno ⇒ **lanzar** `NewsCacheRequeridaError` (clase exportada, `this.name`
      propio) con mensaje que nombre las variables faltantes por NOMBRE (jamás por valor). Razón
      escrita en la cabecera: un `cache` que siempre dice "no hay caché" no es un default
      conservador, es una re-descarga garantizada contra terceros — CR-01 nació exactamente de ahí.
    Extender `BuildNewsDepsOptions` con `supabase?: { url: string; serviceKey: string }`.

    **`run-news-cli.ts`:** pasar `supabase: { url, serviceKey }` a `buildNewsDeps` cuando ambos
    estén resueltos (mismas credenciales ya calculadas en las líneas 222-223); si faltan y la
    Etapa 1 va a correr, fallar duro ANTES de tocar red con un mensaje que liste las variables que
    faltan. NO tocar el bloque de derivación del `[skip]` (ya es correcto).

    `index.ts`: exportar `SupabaseSnapshotLookup` y `NewsCacheRequeridaError`.

    Tests: en `run-news-cli.test.ts` añadir el par apareado (lookup true / lookup false) pasando por
    `buildNewsDeps` de verdad —el test debe medir el WIRING, no un conector doble— con `fetcher`,
    `robots`, `r2`, `snapshot` y `now` inyectados como overrides y el conector construido en el
    test. Registrar las llamadas a `fetcher.get`. Reemplazar/ampliar el test de IN-02 para que el
    escenario provenga del cache real.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @obs/news exec vitest run src/run-news-cli.test.ts src/connector-news.test.ts`
      imprime `Tests  N passed` con **N ≥ 30** (baseline medido: 11 + 15 = 26; el piso exige al
      menos 4 tests nuevos y no puede satisfacerse sin agregarlos).
    - **Mutación RECTORA de CR-01 (registrar en el SUMMARY con el resultado de AMBOS casos):**
      restaurar el `hasToday` inerte como default en `buildNewsDeps` ⇒ el caso `lookup=true`
      (0 fetches, 5 `[skip]`) debe **FALLAR** y el control positivo (`lookup=false`, 5 fetches) debe
      seguir **PASANDO**. Si caen los dos, el test mide "algo se rompió", no el early-exit —
      reescribirlo. Revertir.
    - **Mutación 2:** hacer que `hasSnapshot` reciba `resource` fijo (`"rss-latercera"`) para todos
      ⇒ el assert de los 5 `resource` distintos FALLA. Revertir.
    - `grep -c "hasToday: async () => false" packages/news/src/connector-news.ts` == **0** — el
      doble no-op ya no existe en el archivo de producción. **Prohibido citar esa cadena literal en
      comentarios** (haría el gate auto-invalidante); describirla en prosa.
    - `grep -c "DailyCache" packages/news/src/connector-news.ts` ≥ 1 y
      `grep -c "SupabaseSnapshotLookup" packages/news/src/connector-news.ts` ≥ 1.
    - `grep -c 'vi.stubGlobal("fetch"' packages/news/src/run-news-cli.test.ts` ≥ 1.
    - **`@obs/ingest` intacto (D-132-B), medido contra un SHA literal e inmutable:**
      `BASE=7b188f3; git diff --name-only "$BASE"..HEAD | grep -c '^packages/ingest/'` == **0**.
      SHA literal y no `git rev-list --grep`: `--grep` devuelve el commit más reciente que cite la
      cadena, así que cualquier commit futuro que la mencione desplaza el BASE y vacía la ventana
      del diff. Registrar en el SUMMARY el SHA y la lista completa del diff acumulado.
    - `pnpm typecheck` y `pnpm --filter @obs/news test` salen 0, con el conteo total del paquete
      **≥ 127** (baseline 123 + los 4 tests nuevos mínimos: la suite no puede encoger cerrando un gap).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-08-t2.log; pnpm --filter @obs/news exec vitest run src/run-news-cli.test.ts src/connector-news.test.ts > "$L" 2>&1; rc=$?; cat "$L"; test "$rc" -eq 0; test "$(grep -Eo "Tests +[0-9]+ passed" "$L" | grep -Eo "[0-9]+" | head -1)" -ge 30; test "$(grep -c "hasToday: async () => false" packages/news/src/connector-news.ts)" = "0"; test "$(grep -c "DailyCache" packages/news/src/connector-news.ts)" -ge 1; test "$(grep -c "vi.stubGlobal(\"fetch\"" packages/news/src/run-news-cli.test.ts)" -ge 1; BASE=7b188f3; git cat-file -e "$BASE^{commit}"; test "$(git diff --name-only "$BASE"..HEAD | grep -c "^packages/ingest/")" = "0"'</automated>
  </verify>
  <done>El camino de producción usa un `DailyCache` respaldado en `source_snapshot`; el early-exit tiene test apareado que cae bajo la mutación que restaura el no-op; `@obs/ingest` sin tocar.</done>
</task>

<task type="auto">
  <name>Task 3: [EVIDENCIA SC2] Re-corrida Etapa 1 → [skip] x5 con CERO requests HTTP</name>
  <read_first>
    - .planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-07-SUMMARY.md §Corrida LIVE (los 5 snapshots de 2026-08-05 y sus r2Path)
    - packages/news/src/run-news-cli.ts (tal como quedó tras Task 2)
  </read_first>
  <files>(sin archivos de código — la evidencia va al SUMMARY)</files>
  <action>
    **Presupuesto de red: 0 requests si el date-bucket UTC de hoy sigue siendo `2026-08-05`; máximo
    5 si el día ya cambió.** Primer paso OBLIGATORIO, antes de invocar el CLI:

    1. Determinar el date-bucket actual y si ya hay snapshots de news para él:
       ```
       date -u +%F
       PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select date_bucket, count(*) from source_snapshot where source='news' group by 1 order by 1 desc limit 3" | tr -d '\r'
       ```
       (`tr -d '\r'` obligatorio: `psql` en Windows emite CRLF y las comparaciones fallan mudas.)

    2. **Caso A — el bucket de hoy ya tiene 5 filas (0 requests):** correr UNA sola vez
       `pnpm --filter @obs/news exec tsx src/run-news-cli.ts -- --etapa1` y capturar el log en
       `/tmp/132-08-evidencia.log`. Esperado: **5 líneas `[skip] rss-<slug>`** y
       `descargados=0 skips=5`.

    3. **Caso B — el bucket de hoy NO tiene los 5 (el día cambió; 5 requests):** correr el CLI
       DOS veces con `--etapa1`. La 1ª gasta 5 requests (documentar el gasto explícitamente en el
       SUMMARY, con la razón: el day-roll invalidó la caché del 2026-08-05). La 2ª —la que se
       captura como evidencia— debe salir `descargados=0 skips=5`. Entre ambas, ≥ 5 s.

    4. **Prueba de que la corrida de evidencia NO tocó la red** (no basta el log del CLI): comparar
       el conteo de `source_snapshot` y el `max(fetched_at)` del bucket ANTES y DESPUÉS — ambos
       deben quedar **idénticos**:
       ```
       PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*), max(fetched_at) from source_snapshot where source='news' and date_bucket = to_char(now() at time zone 'utc','YYYY-MM-DD')" | tr -d '\r'
       ```
       `fetched_at` invariante es la evidencia dura: si hubiera habido fetch, `BaseConnector.run()`
       habría llegado a `snapshot.write` y el CLI habría reportado `descargados` > 0.

    5. Registrar en el SUMMARY: caso (A o B), el log literal de la corrida de evidencia, los
       conteos antes/después, y el **presupuesto de red gastado** (0 o 5). **B26: jamás transcribir
       el project-ref ni ninguna credencial** — citar `$SUPABASE_DB_URL` por nombre.

    **NO reintentar** si el resultado no es el esperado: documentar el resultado real y PARAR
    (régimen v13: no se re-corre contra terceros el mismo día para "conseguir" el verde).
  </action>
  <acceptance_criteria>
    - El log de la corrida de evidencia contiene **exactamente 5** líneas que matchean
      `^\[skip\] rss-` y la línea resumen contiene `descargados=0 skips=5`.
    - `count(*)` y `max(fetched_at)` de `source_snapshot` para el bucket de hoy: **idénticos**
      antes y después (comparación literal de las dos salidas de psql, ambas por `tr -d '\r'`).
    - El SUMMARY declara el presupuesto de red gastado (0 o 5) y el caso aplicado (A o B), sin
      project-ref ni credenciales.
    - **Este criterio NO se cumple con el test unitario de Task 2:** la evidencia exigida es la
      corrida real del CLI contra la base real. Un `[skip]` producido por un doble no cuenta.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; L=/tmp/132-08-evidencia.log; test -s "$L"; test "$(grep -c "^\[skip\] rss-" "$L")" = "5"; grep -q "descargados=0 skips=5" "$L"; S=.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-08-SUMMARY.md; test "$(grep -Eic "supabase\.co|eyJ[A-Za-z0-9]" "$S")" = "0"'</automated>
  </verify>
  <done>Existe evidencia empírica —log del CLI + conteos psql invariantes— de que la re-corrida del mismo día no toca la red: SC2 cumplido literalmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → conector | la respuesta de `source_snapshot` decide si se golpea a un tercero |
| env → cliente Supabase | service key que bypassa RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-27 | Denial of Service (a terceros) | `cache.hasToday` que siempre dice "no hay caché" | mitigate | Se elimina el default no-op; sin `cache` ni credenciales, `buildNewsDeps` LANZA en vez de re-descargar |
| T-132-28 | Spoofing (falso verde) | lookup que devuelve `false` ante error de DB | mitigate | El branch de `error` LANZA; test con mutación que lo prueba |
| T-132-29 | Information Disclosure | service key en mensajes de error | mitigate | Solo se propaga `error.message` de PostgREST; grep B26 == 0 en código y SUMMARY |
| T-132-30 | Tampering | `[skip]` mentiroso por lookup mal filtrado | mitigate | Test que assert los tres `eq` (source/resource/date_bucket) + mutación que quita `date_bucket` |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm guards` y `pnpm --filter @obs/news test` (≥ 127) verdes, cada exit code capturado por separado.
- `git diff --name-only 7b188f3..HEAD` no toca `packages/ingest/`.
- Las 4 mutaciones registradas en el SUMMARY, cada una con el test que cayó Y el control que siguió pasando.
</verification>

<success_criteria>
- Ninguna ruta de producción usa un `cache` no-op: `buildNewsDeps` sin `cache` ni credenciales lanza.
- Test apareado de early-exit (0 fetches / 5 fetches) que cae bajo la mutación que restaura el no-op.
- Corrida real: 5 `[skip]`, `descargados=0`, `source_snapshot` invariante, presupuesto de red declarado.
</success_criteria>

<output>
Create `.planning/phases/132-news-rss-conector-rss-dos-etapas-locked/132-08-SUMMARY.md` when done
</output>
