---
phase: 71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01
plan: 03
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - .planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-BACKFILL-SERVEL-RUNBOOK.md
autonomous: false
requirements: [MONEY-02]
must_haves:
  truths:
    - "El operador tiene un runbook end-to-end para obtener + colocar el .xlsx de SERVEL en R2 por elección y correr la Etapa 2 (--from-r2)"
    - "El runbook NOTA la dependencia dura: RUT-01 no aplica a SERVEL (enlace por NOMBRE), pero MONEY_PUBLIC_ENABLED sigue OFF hasta el flip legal (Phase 73)"
    - "El runbook documenta el modo LOCAL (Etapa 1 = acto humano de colocar el .xlsx) y la fecha de corte/elección visible por dato"
  artifacts:
    - path: ".planning/phases/71-.../71-BACKFILL-SERVEL-RUNBOOK.md"
      provides: "Procedimiento operador-LOCAL del backfill SERVEL por elección (obtener + colocar .xlsx en R2 + correr --from-r2)"
      min_lines: 60
  key_links:
    - from: "71-BACKFILL-SERVEL-RUNBOOK.md"
      to: "run-servel-local-cli.ts"
      via: "comandos LOCAL --from-r2 / --r2-path por elección"
      pattern: "run-servel-local-cli"
---

<objective>
Entregar el runbook operador-LOCAL end-to-end del backfill de SERVEL por elección: cómo obtener el `.xlsx` de financiamiento electoral de una elección, colocarlo en R2 content-addressed (Etapa 1 = acto humano, SERVEL no tiene feed estable), y correr la Etapa 2 (`--from-r2`/`--r2-path`) con `run-servel-local-cli.ts` (Plan 71-02). El agente NO obtiene ni coloca el `.xlsx` real (es toil operador-LOCAL por elección) ni corre ninguna corrida que toque la fuente ni flipea el gate MONEY.

Purpose: Cierra la porción operativa de MONEY-02 SC#1 ("el operador deja el .xlsx correcto en R2 y el pipeline re-corre SIN volver a tocar la fuente"). El wire (Plan 01) + el CLI/freshness (Plan 02) están listos; esta es la documentación operativa que el operador ejecuta cuando decide poblar SERVEL por elección.
Output: `71-BACKFILL-SERVEL-RUNBOOK.md` + un checkpoint `human-action` que entrega el runbook al operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-RESEARCH.md
@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-01-SUMMARY.md
@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-02-SUMMARY.md
@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-BACKFILL-CHILECOMPRA-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 71-BACKFILL-SERVEL-RUNBOOK.md</name>
  <files>.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-BACKFILL-SERVEL-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-BACKFILL-CHILECOMPRA-RUNBOOK.md (el runbook hermano a ESPEJAR — estructura, secciones, tono operador)
    - .planning/phases/71-.../71-01-SUMMARY.md (los flags/símbolos reales del wire: putImmutable("servel",...), r2Path, --from-r2, modo LOCAL)
    - .planning/phases/71-.../71-02-SUMMARY.md (el CLI real run-servel-local-cli.ts + sus flags exactos + la señal freshness servel)
    - packages/dinero/src/run-servel-local-cli.ts (flags EXACTOS: --eleccion/--r2-path/--from-r2/--anio/--dry-run)
    - CLAUDE.md sección "Ingesta y Cron (regla arquitectónica — LOCKED)" (dos etapas, content-addressed servel/<eleccion>/<fecha>/<sha>.xlsx, hash-check, respeto al servidor)
  </read_first>
  <action>Escribir `71-BACKFILL-SERVEL-RUNBOOK.md` (espejo de `70-BACKFILL-CHILECOMPRA-RUNBOOK.md`, adaptado a SERVEL LOCAL). Secciones: (1) Contexto — SERVEL es LOCAL por elección, sin cron, sin feed estable; el enlace candidato→parlamentario es por NOMBRE determinista (SERVEL NO trae RUT — a diferencia de ChileCompra; RUT-01 NO es prerrequisito de SERVEL); MONEY_PUBLIC_ENABLED sigue OFF hasta el flip legal de Phase 73 (el operador NO enciende aquí). (2) Etapa 1 (acto humano) — cómo obtener el `.xlsx` de financiamiento electoral de una elección desde SERVEL, y cómo colocarlo en R2 content-addressed bajo `servel/<eleccion>/<fecha>/<sha256(bytes)>.xlsx` (PUT `If-None-Match: *`; 412 = ya existía = éxito idempotente); una elección = un `.xlsx`. (3) Etapa 2 (pipeline) — comando real `run-servel-local-cli.ts --eleccion <slug> --r2-path <r2Path> [--anio YYYY] [--dry-run]` que lee de R2 y NO toca la fuente; qué esperar en la salida (aportes/donantes/parlamentariosMarcados/cuarentena/errores/degradaciones). (4) Fail-soft por elección — si el `.xlsx` de una elección tiene drift de header o falla completitud, ESA elección va a cuarentena (0 filas) sin abortar; re-obtener/re-colocar el `.xlsx` correcto y re-correr. (5) Frescura por dato — cada fila lleva `eleccion` + `fecha_corte` visibles ("Financiamiento de la elección X (corte YYYY-MM-DD)"); tras el backfill, la señal `servel` en `pnpm freshness` sale de "n/d"/stale. (6) Verificación post-corrida — conteos, cobertura, que ningún aporte quedó atribuido por nombre no-determinista (parlamentario_id NULL en no_confirmado). (7) Notas de respeto — rate-limit/robots no aplican al re-run (lee de R2); la obtención del `.xlsx` respeta la fuente (descarga manual puntual, no ráfaga). Mínimo 60 líneas. Commit `docs(71-03)`.</action>
  <verify>
    <automated>test -f ".planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-BACKFILL-SERVEL-RUNBOOK.md" && grep -q "run-servel-local-cli" ".planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-BACKFILL-SERVEL-RUNBOOK.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El runbook existe con ≥60 líneas, las 7 secciones descritas.
    - Documenta la Etapa 1 (acto humano: obtener + colocar `.xlsx` en R2 content-addressed `servel/<eleccion>/<fecha>/<sha>.xlsx`) y la Etapa 2 (`run-servel-local-cli.ts --from-r2`/`--r2-path`) con los flags REALES del CLI.
    - Aclara EXPLÍCITAMENTE que SERVEL NO trae RUT (enlace por NOMBRE determinista) y que RUT-01 NO es prerrequisito de SERVEL (a diferencia de ChileCompra).
    - Nota que `MONEY_PUBLIC_ENABLED` sigue OFF (el operador NO enciende aquí; el flip es Phase 73).
    - Documenta la fecha de corte/elección visible por dato + que la señal `servel` en freshness sale de "n/d" tras el backfill.
    - Documenta fail-soft por elección (cuarentena de una elección sin abortar la corrida).
  </acceptance_criteria>
  <done>El runbook operador-LOCAL de SERVEL está escrito con los comandos reales, la aclaración NOMBRE-no-RUT, el gate OFF, y la frescura por dato.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>El wire de dos etapas R2 de SERVEL (Etapa-1-R2-primero + modo LOCAL `--from-r2`), el CLI operador `run-servel-local-cli.ts`, la señal de frescura `servel`, y el runbook `71-BACKFILL-SERVEL-RUNBOOK.md`. Todo offline-probado (FakeR2Store + fixture `.xlsx` exceljs). El agente NO obtuvo/colocó ningún `.xlsx` real, NO tocó la fuente SERVEL, NO flipeó el gate MONEY.</what-built>
  <how-to-verify>
    Cuando decidas poblar SERVEL (toil operador-LOCAL, POR ELECCIÓN):
    1. Sigue `71-BACKFILL-SERVEL-RUNBOOK.md`.
    2. Obtén el `.xlsx` de financiamiento electoral de una elección desde SERVEL (descarga manual puntual, respetando la fuente).
    3. Colócalo en R2 content-addressed bajo `servel/<eleccion>/<fecha>/<sha256(bytes)>.xlsx` (Etapa 1 = acto humano).
    4. Corre la Etapa 2: `run-servel-local-cli.ts --eleccion <slug> --r2-path <r2Path> [--anio YYYY]` (lee de R2, NO toca la fuente).
    5. Verifica: aportes/donantes poblados, elección cuarentenada = 0 (o re-coloca el `.xlsx` correcto), `eleccion`+`fecha_corte` visibles por dato, la señal `servel` en `pnpm freshness` sale de "n/d".
    RECORDATORIO: `MONEY_PUBLIC_ENABLED` sigue OFF — los aportes existen en la DB pero NO se presentan al público hasta el sign-off legal 21.719 (Phase 73). NO enciendas el flag aquí.
  </how-to-verify>
  <resume-signal>Escribe "runbook recibido" (o "backfill corrido" con los conteos) para cerrar la fase; o lista qué falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador→SERVEL (obtención .xlsx) | descarga manual puntual del .xlsx (acto humano, respeto a la fuente) |
| operador→R2 (Etapa 1) | coloca el crudo content-addressed (acto humano) |
| CLI→R2 (Etapa 2) | lee de R2, NO toca la fuente |
| gate→superficie pública | MONEY_PUBLIC_ENABLED OFF (candado B, Phase 73) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-10 | Elevation of Privilege | El operador enciende MONEY prematuramente en el backfill | mitigate | El runbook NOTA explícitamente que el gate sigue OFF y el flip es Phase 73 (acto humano legal separado); el backfill NO enciende nada |
| T-71-11 | Repudiation | Backfill sin crudo reconstruible | mitigate | La Etapa 1 (crudo en R2 content-addressed) es precondición documentada de la Etapa 2; el wire ya gatea (Plan 71-01) |
| T-71-12 | Tampering | El operador cree que necesita RUT para SERVEL y fuerza un cruce por RUT | mitigate | El runbook aclara EXPLÍCITAMENTE que SERVEL es por NOMBRE determinista, sin RUT; RUT-01 no aplica |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones (solo un archivo de documentación) |
</threat_model>

<verification>
- El archivo `71-BACKFILL-SERVEL-RUNBOOK.md` existe (≥60 líneas) y referencia `run-servel-local-cli` + los flags reales.
- El checkpoint `human-action` entrega el runbook al operador (bloqueante).
- El plan NO ejecuta ninguna corrida LIVE ni toca el gate MONEY.
</verification>

<success_criteria>
- El operador tiene un runbook end-to-end para poblar SERVEL por elección (Etapa 1 humana → Etapa 2 `--from-r2`).
- El runbook aclara NOMBRE-no-RUT, el gate OFF, la frescura por dato, y el fail-soft por elección.
- Checkpoint operador entregado.
</success_criteria>

<output>
Create `.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-03-SUMMARY.md` when done
</output>
</content>
