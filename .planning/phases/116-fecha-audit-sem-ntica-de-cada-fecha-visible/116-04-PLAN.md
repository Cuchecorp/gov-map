---
phase: 116-fecha-audit-sem-ntica-de-cada-fecha-visible
plan: 04
type: execute
wave: 3
depends_on: ["116-01", "116-02", "116-03"]
files_modified:
  - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md
  - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/check-fechas.sh
autonomous: true
requirements: [FECHA-01]

must_haves:
  truths:
    - "Existe un artefacto único 116-FECHAS-AUDIT.md con el veredicto de TODA fecha del inventario 113, sin emisor sin veredicto"
    - "Cada veredicto está cruzado contra el dato real de PROD: un sujeto concreto por superficie, con su query y su valor registrados"
    - "Los hallazgos están numerados F-01, F-02… y cada uno lleva archivo:línea, superficie, qué dice hoy, por qué está mal y fix sugerido — consumibles por Phase 117 sin re-investigar"
    - "Un script reproducible prueba la completitud: todo id E-xxx con fecha en 113 §3.0 tiene fila de veredicto, con el denominador derivado por la regla de celda LOCKED (38 ids esperados) y toda desviación reportada, nunca silenciada"
  artifacts:
    - path: ".planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md"
      provides: "Artefacto rector de la fase: veredicto por emisor×fecha + evidencia PROD + hallazgos numerados"
      contains: "F-01"
      min_lines: 200
    - path: ".planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/check-fechas.sh"
      provides: "Checklist re-ejecutable de completitud (diff ids E-xxx de 113 §3.0 vs filas de 116)"
      contains: "STRICT"
      min_lines: 60
  key_links:
    - from: "check-fechas.sh"
      to: "113-INVENTARIO.md §3.0"
      via: "deriva el denominador de ids con fecha desde el catálogo, no de una lista hardcodeada"
      pattern: "113-INVENTARIO"
    - from: "116-FECHAS-AUDIT.md sección HALLAZGOS"
      to: "Phase 117"
      via: "cada F-xx con archivo:línea + fix sugerido"
      pattern: "F-[0-9]{2}"
---

<objective>
Cerrar la fase: cruzar el veredicto contra el dato real de PROD (un sujeto por superficie),
ensamblar `116-PARCIAL-A.md` + `116-PARCIAL-B.md` + `116-FORMATTERS.md` en el artefacto único
`116-FECHAS-AUDIT.md`, numerar los hallazgos `F-01..F-NN` para que Phase 117 los consuma sin
re-investigar, y probar la completitud con un script reproducible estilo `check-inventario.sh`.

Purpose: los tres planes anteriores producen veredictos DE CÓDIGO. El contrato del ROADMAP exige
además el cruce contra PROD (SC4) y un artefacto único con cobertura demostrada (SC1).
Output: `116-FECHAS-AUDIT.md` + `check-fechas.sh`.

Régimen: SOLO LECTURA — psql read-only, cero DDL, cero DML, cero flag tocado, cero cambio en
`app/` o `packages/`. Todo fix va a Phase 117.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-VALIDATION.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md

<interfaces>
<!-- Sujetos deterministas YA elegidos por 113 §1. Se REUTILIZAN; solo se elige sujeto nuevo si
     113 no fijó uno para esa superficie (decisión LOCKED del 116-CONTEXT). -->

  Sujeto A  D1165      diputado, 6 bloques visibles (votos 3752, lobby 112, patrimonio 6, cruces 11,
                       comisiones 2, militancias 2)              → /parlamentario/D1165
  Sujeto B  S1338      senador (votos 949, patrimonio 9, militancia 1; lobby/cruces/comisiones = 0,
                       estados vacíos honestos)                  → /parlamentario/S1338
                       GOTCHA 105-02: la PK es 'S1338'; existe además parlid_senado = 1338 numérico.
                       Consultar por 'S1338', jamás por 1338.
  Sujeto C  14309-04   boletín bicameral (7 votaciones, 1 embedding, 47 cruces, prm_id_camara=14891)
                                                                 → /proyecto/14309-04
  Sujeto D  17870-05   boletín solo-Senado (256 votaciones, 355 eventos, prm_id_camara IS NULL)
                                                                 → /proyecto/17870-05
  Sujeto E  contraparte NO ELEGIBLE — contrato y aporte tienen 0 filas en PROD y la ruta 404ea
                       entera con MONEY OFF. Se declara la degradación honesta; no se inventa id.
  /comparar 113 §5 usó el par D1165 + D1012 para observar el gate VSIM.

Invocación psql (verbatim, precedente 113 §0.1 — el valor de SUPABASE_DB_URL JAMÁS se imprime ni
se escribe en ningún archivo):

  set -a; source .env; set +a
  PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"

Pitfall 8 (113 §1.4): PROD tiene fechas corruptas en el futuro (p. ej. 2626-05-25). Toda consulta
de verificación filtra `fecha <= current_date`. Si una fecha corrupta ES visible en el sitio, eso
es hallazgo para 117.

Conteo por REST prohibido (cap PostgREST 1.000 filas): todo conteo va por `psql -tA`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cruce contra PROD — un sujeto por superficie</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md</files>
  <read_first>
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md §1 (los 5 sujetos con sus queries verbatim) y §5 (estado de gates)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md (qué columnas/RPC hay que verificar)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md (idem)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md (regla del cruce: DB → formatter → etiqueta)
  </read_first>
  <action>
Crear `116-FECHAS-AUDIT.md` con front-matter (`phase: 116`, `titulo: Auditoría semántica de fechas`,
`requirement: FECHA-01`, `consumido_por: [117, 125]`, `ancla_temporal:` resultado de
`select now()::date`, `gates_observados:` copiados de 113 §5 CON la fecha de esa observación
declarada como heredada) y escribir la sección `## 2. Cruce contra el dato real de PROD`.

Abrir la sección declarando el régimen: psql read-only, `set -a; source .env; set +a`,
`PGCLIENTENCODING=UTF8`, el valor de `SUPABASE_DB_URL` jamás se imprime ni se escribe, cero DDL,
cero DML, todo conteo por `psql -tA`, filtro `fecha <= current_date` (Pitfall 8).

Producir una fila por **superficie** (no por emisor) con estas columnas:
`superficie (ruta) | sujeto usado (id de 113 §1, o 'nuevo — <id> + criterio') | fecha auditada (columna/RPC) | query verbatim | valor observado en PROD | lo que el formatter renderizaría | etiqueta que el sitio afirma | ¿coincide la semántica? (sí/no + causa)`.

Superficies obligatorias y su sujeto:
  - `/parlamentario/[id]` → `D1165` (rico) **y** `S1338` (estados vacíos honestos). Verificar al
    menos: `voto`/`votacion.fecha` vs `votacion.fecha_captura`, `lobby_audiencia.fecha`,
    `declaracion.fecha_presentacion`, `parlamentario_militancia.desde/.hasta`,
    `cruce_senal` (fecha del hecho vs `fecha_captura` del rebuild), `parlamentario.fecha_captura`.
  - `/proyecto/[boletin]` → `14309-04` (bicameral) **y** `17870-05` (solo-Senado). Verificar al
    menos: `tramitacion_evento.fecha`, `votacion.fecha` y `votacion.fecha_captura`,
    `source_snapshot.fecha_captura`, `citacion.fecha` (date-only), `sesion_tabla_item`,
    y la fecha de urgencia que alimenta `estado-actual-block.tsx:429`.
  - `/agenda` → sujeto nuevo determinista: la semana ISO con más citaciones cuyo `fecha <=
    current_date`, elegida por query con desempate estable. Verificar que `citacion.fecha` es
    date-only medianoche UTC (`select fecha, fecha::text, fecha AT TIME ZONE 'UTC' from citacion
    where ... limit 1` o el equivalente que revele el tipo de columna) — si el tipo es `date`,
    declararlo; si es `timestamptz` a medianoche UTC, declararlo también, porque cambia el riesgo tz.
  - `/` (home) → verificar `actualidad_senal` (las señales que el panel muestra), distinguiendo las
    `agenda_*` (date-only) del resto.
  - `/buscar` → verificar qué fecha traen `RPC:match_proyectos` / `RPC:buscar_proyectos_hibrido`
    para un `q` determinista y acotado.
  - `/comparar` → par `D1165` + `D1012`: verificar `coincidencia_votos_par.fecha_captura_max` y
    confirmar por SQL que es un MAX de fechas de captura, no de fechas de votación.
  - `/contraparte/[id]` (gate MONEY OFF) → **degradación honesta declarada**: `select count(*) from
    contrato;` y `select count(*) from aporte;` (esperado 0 y 0). El cruce de los emisores gated se
    hace por SQL directo sobre esas tablas, SIN tocar ningún flag y SIN pedir la ruta al deploy.
    Si el conteo es 0, se declara `sin datos que cruzar — el veredicto de E-013..E-016/E-060 queda
    respaldado solo por código`, y eso ES el resultado honesto.
  - `/cuenta` (gate NOTIF OFF) → `suscripcion.created_at` / `consentimiento.created_at`: verificar
    solo el TIPO y el conteo agregado. **CERO PII**: prohibido seleccionar `email`, `user_id` o
    cualquier valor de fila; solo `count(*)` y metadatos de columna.

Regla de honestidad: si una query devuelve 0 filas, se registra `0 filas` con su causa, jamás se
sustituye por otro sujeto para "que dé bonito". Si una fecha corrupta futura aparece pese al filtro
en algún camino que el sitio SÍ renderiza, se registra como candidato a hallazgo.

Opcionalmente (discreción, con mesura — el Worker es propio, sin WAF), verificar 2-3 superficies
contra el deploy real con `curl -s` + grep del HTML SSR para confirmar que la etiqueta visible
coincide con la registrada por los planes 02/03. El mínimo obligatorio es DB→código; el `curl` es
refuerzo, no requisito. Si se hace, declarar la URL base y la hora de observación.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md; grep -qF "## 2. Cruce contra el dato real de PROD" "$f" && for s in D1165 S1338 14309-04 17870-05; do grep -qF "$s" "$f" || { echo "FALTA sujeto $s"; exit 1; }; done; grep -qF "fecha <= current_date" "$f" && test "$(grep -cE "postgres(ql)?://" "$f")" = "0" && echo OK-cruce'</automated>
  </verify>
  <acceptance_criteria>
    - `116-FECHAS-AUDIT.md` existe con front-matter YAML que incluye `phase: 116`,
      `requirement: FECHA-01` y un `ancla_temporal` con la fecha devuelta por `select now()::date`.
    - La sección `## 2.` tiene una fila por cada superficie listada en el `<action>`; cero
      superficies omitidas, cero celdas vacías.
    - Cada fila trae una `query verbatim` re-ejecutable y un `valor observado`; ninguna fila tiene
      valor "—" salvo con causa declarada (p. ej. `0 filas — contrato vacío en PROD`).
    - `grep -cE 'postgres(ql)?://' 116-FECHAS-AUDIT.md` → 0.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 116-FECHAS-AUDIT.md` → 0 (cero RUT; ojo: los boletines con
      guion como `14309-04` NO matchean ese patrón, que exige 7-8 dígitos).
    - Ningún email de persona natural: `grep -oE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+' | sort -u`
      devuelve como máximo el buzón institucional `contacto@observatoriocongreso.cl`.
    - Toda consulta de fecha del artefacto contiene el filtro `fecha <= current_date` o declara
      por qué no aplica.
    - `git status --porcelain app/ packages/` vacío y `git diff --quiet -- .env .env.example`.
  </acceptance_criteria>
  <done>Cada superficie tiene un sujeto concreto con query verbatim, valor de PROD y comparación DB → formatter → etiqueta; las superficies sin datos declaran su degradación honesta.</done>
</task>

<task type="auto">
  <name>Task 2: Ensamblado del artefacto único + hallazgos numerados F-01..F-NN</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md</files>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md (estado tras Task 1)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md
    - .planning/ROADMAP.md (los 4 Success Criteria de la Phase 116 — son el contrato)
  </read_first>
  <action>
Completar `116-FECHAS-AUDIT.md` con las secciones restantes, en este orden:

`## 0. Método y régimen` — qué se auditó (universo = ids E-xxx de 113 §3.0 con fecha), cómo
(análisis de código + psql read-only), qué NO hace (no corrige nada; los fixes son Phase 117), las
3 reglas LOCKED verbatim (`fecha_captura` jamás es el hecho; "captura" pelado prohibido, idiom
"según fuente al…"; date-only medianoche UTC = día chileno, jamás convertir tz), y los comandos
re-ejecutables.

`## 1. Veredicto por emisor × fecha` — la tabla ÚNICA, fusionando `116-PARCIAL-A.md` (§A.1, §A.2) y
`116-PARCIAL-B.md` (§B.1, §B.2), ordenada por id `E-xxx` ascendente, conservando las 9 columnas.
Prefijar con la sección `### 1.0 Semántica por formatter` importada de `116-FORMATTERS.md` §1 y
`### 1.1 Chokepoint ProvenanceBadge` importada de §2, para que el artefacto sea autocontenido.
Reconciliar cualquier divergencia entre los dos parciales sobre un mismo formatter: si A y B
dieron veredictos distintos para el mismo formatter, se resuelve leyendo el código y se declara la
divergencia y su resolución en `### 1.2 Reconciliación`. Absorber el residual de `## B.4` si existe.

Añadir `### 1.3 Auditoría date-only (gotcha LOCKED v9.0)`: la fusión de la tabla `### A.2.1` de
`116-PARCIAL-A.md` y la `### B.2.1` de `116-PARCIAL-B.md` en una sola tabla, mismo formato
(`columna date-only | emisor(es) | formatter | ¿se convierte de zona? | veredicto`). Toda fila que
los parciales dejaron en `pendiente de confirmar por SQL` se resuelve aquí con la evidencia de PROD
del `## 2.` (tipo real de la columna), o se declara como límite en `## 6.` si tampoco pudo
resolverse.

`## 3. HALLAZGOS` — la sección que Phase 117 consume. Numerar `F-01`, `F-02`, … Cada hallazgo con
subsección propia y estos campos obligatorios, todos poblados:
  - `id`: F-NN
  - `superficie`: ruta(s)
  - `emisor`: id E-xxx
  - `archivo:línea`
  - `qué dice hoy`: el texto visible VERBATIM
  - `por qué está mal`: cuál de las 3 reglas LOCKED viola, citada
  - `fix sugerido`: el copy o el cambio de formatter concreto (p. ej. sustituir el rótulo por
    "según fuente al {fecha}"), SIN implementarlo
  - `severidad`: `miente` (afirma el hecho siendo captura, o convierte tz sobre date-only) o
    `ambigua` (el usuario no puede distinguir)
Ordenar por severidad `miente` primero.

`## 4. Cobertura — sin hallazgos declarados` — la lista de TODOS los ids E-xxx con fecha que NO
generaron hallazgo, cada uno con la línea `E-xxx — sin hallazgos`. Cero excepciones silenciosas: un
emisor que no aparece ni en §3 ni en §4 es un fallo de cobertura.

**El denominador son 38 ids**: los 18 del grupo A + los 19 del grupo B + **`E-040`**
(`provenance-badge.tsx`, el chokepoint), que NO pertenece a ningún grupo porque lo audita el plan
01. `E-040` debe aparecer explícitamente en `## 3.` o en `## 4.` como cualquier otro emisor — su
veredicto sale de `116-FORMATTERS.md` §2.1.

`## 5. Trazabilidad a los Success Criteria del ROADMAP` — una tabla `SC | dónde se satisface |
evidencia`, con las 4 filas: SC1 (veredicto por fecha con origen citado) → §1; SC2 (lista completa
de `fecha_captura`-como-hecho con archivo:línea y superficie) → una sub-lista explícita extraída de
§1 filtrando VEREDICTO=`captura` con `¿miente?`=`sí`; SC3 (date-only verificadas contra el gotcha
tz) → la **unión** de `### A.2.1` (grupo A: militancia.desde/.hasta, declaracion.fecha_presentacion,
votacion.fecha, lobby_audiencia.fecha, fecha_oc/fecha_aporte/fecha_corte) y `### B.2.1` (grupo B:
citacion.fecha, citacion_punto, sesion_tabla_item), fusionadas en una sola tabla `### 1.3 Auditoría
date-only (gotcha LOCKED v9.0)` — ninguna de las dos puede quedar fuera; SC4 (cruce contra PROD, un
sujeto por superficie) → §2.

Declarar en `## 6. Límites` lo que esta fase NO pudo cerrar y por qué (p. ej. emisores gated sin
datos en PROD verificables solo por código; superficies no observadas contra el deploy real, que
Phase 125 re-verifica). Un límite declarado es un resultado válido; un límite silenciado no.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md; for s in "## 0. Método y régimen" "## 1. Veredicto por emisor" "## 3. HALLAZGOS" "## 4. Cobertura" "## 5. Trazabilidad" "## 6. Límites"; do grep -qF "$s" "$f" || { echo "FALTA seccion: $s"; exit 1; }; done; grep -qE "^#+ .*F-01" "$f" || grep -qF "F-01" "$f"; echo "hallazgos: $(grep -coE "F-[0-9]{2}" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `116-FECHAS-AUDIT.md` contiene las secciones `## 0.`, `## 1.` (con `### 1.0`, `### 1.1`,
      `### 1.2`), `## 2.`, `## 3. HALLAZGOS`, `## 4. Cobertura`, `## 5. Trazabilidad`, `## 6. Límites`.
    - Cada hallazgo `F-NN` de `## 3.` tiene los 8 campos poblados; ningún campo con `TBD`, `TODO`
      o vacío (`grep -nE 'TBD|TODO|FIXME' 116-FECHAS-AUDIT.md` → sin match).
    - La unión de los ids citados en `## 3.` y `## 4.` cubre TODOS los ids con fecha del catálogo
      113 §3.0 (lo prueba `check-fechas.sh` en el Task 3).
    - `## 5.` tiene exactamente 4 filas de datos, una por SC del ROADMAP, cada una con una
      referencia de sección concreta; la fila de SC3 apunta a `### 1.3` y esa sección contiene
      filas provenientes de AMBOS parciales (`A.2.1` y `B.2.1`).
    - Existe `### 1.3 Auditoría date-only (gotcha LOCKED v9.0)` con ≥ 8 filas de datos y sin
      ninguna celda `pendiente de confirmar por SQL` sin resolver o sin límite declarado en `## 6.`.
    - La sub-lista de SC2 enumera cada ocurrencia de `fecha_captura`-como-hecho con `archivo:línea`
      y superficie; el conteo de esa sub-lista coincide con el número de filas de `## 1.` cuyo
      VEREDICTO es `captura` y `¿miente?` es `sí`.
    - Cero celdas de tabla vacías en filas de datos.
    - `git status --porcelain app/ packages/` vacío.
  </acceptance_criteria>
  <done>Artefacto único autocontenido, con hallazgos numerados accionables por Phase 117, cobertura declarada emisor por emisor y trazabilidad a los 4 SC del ROADMAP.</done>
</task>

<task type="auto">
  <name>Task 3: check-fechas.sh — completitud reproducible + cierre de compuertas</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/check-fechas.sh</files>
  <read_first>
    - .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh (patrón a espejar: set -u, STRICT, falta(), REPO_ROOT, POSIX grep sin -P)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md §3.0 (la tabla de la que se deriva el denominador)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md (el archivo que el script verifica)
  </read_first>
  <action>
Crear `check-fechas.sh` en el phase dir, espejando el estilo de `check-inventario.sh`: shebang
`#!/usr/bin/env bash`, `set -u`, variable `STRICT=${STRICT:-0}` (STRICT=0 reporta sin fallar;
STRICT=1 falla con exit 1 ante cualquier falta), función `falta()`, resolución de `REPO_ROOT`
desde `$(dirname "$0")`, POSIX grep (**prohibido `grep -P`**, no disponible en este entorno),
read-only (no escribe nada), corrida en segundos.

Checks a implementar:

  1. **Denominador derivado, no hardcodeado — con la regla de celda EXACTA.** Extraer de
     `113-INVENTARIO.md` §3.0 los ids `E-xxx` de las filas cuya columna "fechas que muestra"
     indica que el emisor SÍ muestra fechas.

     **Cuidado (esto es donde la regla ingenua falla):** las celdas "sin fecha" NO son un `—`
     pelado. Llevan backticks y casi siempre un paréntesis explicativo — p. ej. E-006 tiene
     `` `—` (el chrome no muestra fechas) `` y E-039 tiene una forma análoga. Comparar la celda
     por igualdad contra `—` da 60 ids en vez de 38, y entonces los checks 2 y 3 exigirían
     veredicto para 22 emisores que no muestran ninguna fecha ⇒ 22 declaraciones falsas de
     "sin hallazgos". La fase perdería su valor.

     **Regla LOCKED:** una celda cuenta como *sin fecha* si su contenido, **tras eliminar espacios,
     tabs y backticks**, **EMPIEZA por** `—` (guion largo U+2014). Todo lo demás cuenta como
     *con fecha*. Implementación verificada que produce exactamente 38 ids:

         awk -F'|' '/^\| E-/{c=$5; gsub(/[ \t`]/,"",c); if(substr(c,1,3)!="—"){...}}'

     (`substr(c,1,3)` porque en UTF-8 el guion largo ocupa 3 bytes y el awk del entorno es
     byte-oriented; `$5` es el campo "fechas que muestra" contando el campo vacío inicial que
     produce el `|` de apertura.) Extraer el id de la fila con un match de patrón
     (`E-[0-9][0-9][0-9]`), no por recorte posicional del campo.

     **Sanity check anclado:** el denominador esperado es **38**. Si el awk devuelve otro número,
     el script lo REPORTA como falta explícita (`-- check 1 — denominador 41, esperado 38`) con la
     lista de ids que entraron o salieron respecto de 38. **Jamás se silencia ni se ajusta el
     número esperado para que pase**: una desviación significa o bien que el inventario cambió
     (legítimo, se declara) o bien que la regla de celda se rompió (bug, se arregla).
  2. **Cobertura**: cada id del denominador aparece en `116-FECHAS-AUDIT.md`. Reportar los ausentes
     por id.
  3. **Veredicto declarado**: cada id del denominador aparece en la sección `## 3. HALLAZGOS` o en
     la sección `## 4. Cobertura` (cero excepciones silenciosas). Un id presente en el documento
     pero ausente de ambas secciones es una falta.
  4. **Conjunto cerrado de veredictos**: toda celda de veredicto de la tabla de `## 1.` pertenece a
     {`hecho`, `captura`, `ambigua`}. Reportar cualquier valor fuera del conjunto.
  5. **Higiene**: cero credenciales (`grep -cE 'postgres(ql)?://'` → 0), cero RUT
     (`grep -cE '[0-9]{7,8}-[0-9kK]'` → 0), cero `TBD|TODO|FIXME`, cero celdas vacías en filas de
     tabla (`grep -nE '^\|.*\|[[:space:]]*\|'` menos las separadoras `|---`).
  6. **Régimen solo-lectura**: `git status --porcelain app/ packages/` vacío.

Salida con el mismo formato que `check-inventario.sh`: una línea `OK check N — …` o
`-- check N — …` por check, un separador `---`, y `RESULTADO: sin faltas (STRICT=$STRICT)` o
`RESULTADO: N falta(s)`.

Correr el script con `STRICT=1`, y anexar su salida verbatim a `116-FECHAS-AUDIT.md` en una
sección `## 7. Verificación de cierre`, junto con el resultado de las compuertas de higiene y la
comprobación de no-regresión de suite (`git status --porcelain app/ packages/` vacío — esta fase no
toca código, así que NO es necesario correr `pnpm test`; declararlo explícitamente como el motivo).

Si el script encuentra faltas, se corrigen en `116-FECHAS-AUDIT.md` (añadiendo el veredicto o la
declaración `sin hallazgos` faltante) y se re-corre hasta exit 0. Nunca se relaja el script para
que pase.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && STRICT=1 bash .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/check-fechas.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `check-fechas.sh` existe, empieza por `#!/usr/bin/env bash`, contiene `set -u` y
      `STRICT=${STRICT:-0}`, y NO contiene la cadena `grep -P`.
    - El script deriva el denominador leyendo `113-INVENTARIO.md` (contiene la cadena
      `113-INVENTARIO`), no una lista de ids escrita a mano: `grep -c 'E-0[0-9][0-9]' check-fechas.sh`
      es 0 salvo por patrones de regex genéricos (`E-[0-9]`), nunca ids enumerados.
    - `STRICT=1 bash check-fechas.sh` → **exit 0**, con los 6 checks en `OK`.
    - El check 1 imprime el denominador derivado y su comparación contra el esperado **38**; con el
      inventario actual el denominador es exactamente 38 y los ids `E-006` y `E-039` (celda que
      empieza por `—` tras quitar backticks) NO están en él.
    - Prueba negativa de la regla de celda: al relajarla a una comparación por igualdad contra `—`,
      el denominador salta a 60 y el check 1 REPORTA la desviación en vez de continuar. Registrar
      el resultado de esta prueba en `## 7. Verificación de cierre`.
    - `STRICT=0 bash check-fechas.sh` → exit 0 siempre (modo reporte).
    - Mutación de prueba: borrar temporalmente una línea de veredicto de un id en una copia de
      `116-FECHAS-AUDIT.md` hace que el check 2 o el 3 reporte falta (el script MUERDE). Registrar
      el resultado de esta prueba en `## 7. Verificación de cierre` y restaurar la copia.
    - `116-FECHAS-AUDIT.md` contiene la sección `## 7. Verificación de cierre` con la salida
      verbatim del script.
    - `git status --porcelain app/ packages/` vacío y `git diff --quiet -- .env .env.example`.
  </acceptance_criteria>
  <done>La completitud del audit es una propiedad probada por un script re-ejecutable que muerde, no una afirmación; su salida queda anexada al artefacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → PROD Postgres | Se cruza con credenciales de `.env`; solo `SELECT` |
| repo → artefacto público en git | El artefacto no debe filtrar credenciales, RUT, email ni montos |
| repo → deploy Cloudflare (curl opcional) | Worker propio, sin WAF; cero requests a fuentes gubernamentales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-10 | Information Disclosure | `SUPABASE_DB_URL` | mitigate | Invocación verbatim `set -a; source .env; set +a` + `psql "$SUPABASE_DB_URL"`; el valor jamás se ecoa ni se escribe. Compuerta: `grep -cE 'postgres(ql)?://'` sobre el artefacto → 0 |
| T-116-11 | Information Disclosure | queries a `suscripcion`/`consentimiento`/`declaracion`/`contrato` | mitigate | Solo `count(*)` y metadatos de columna; prohibido `select email`, `user_id`, `rut`, monto individual. Compuertas RUT/email en acceptance |
| T-116-12 | Tampering | PROD Postgres | mitigate | Cero DDL y cero DML: solo `SELECT`. Ninguna task ejecuta `insert/update/delete/alter/drop` |
| T-116-13 | Elevation of Privilege | gates MONEY/NOTIF | mitigate | El cruce de emisores gated va por SQL directo sobre las tablas, sin encender flags; `git diff --quiet -- .env .env.example` es criterio de aceptación |
| T-116-14 | Denial of Service | fuentes gubernamentales | mitigate | Cero requests a camara.cl/senado.cl/BCN/leylobby ⇒ el rate-limit 2-3s de CLAUDE.md no aplica. El único `curl` opcional va al Worker propio |
| T-116-15 | Repudiation | veredicto sin evidencia | mitigate | Cada fila lleva query verbatim + valor observado; `check-fechas.sh` prueba la cobertura y muerde ante omisión |
| T-116-SC | Tampering | instalación de paquetes | accept | Plan sin instalaciones npm/pnpm/pip: cero superficie de supply chain |
</threat_model>

<verification>
- `STRICT=1 bash check-fechas.sh` → exit 0, 6/6 checks OK.
- Compuertas de higiene sobre `116-FECHAS-AUDIT.md`: `postgres://` = 0, RUT = 0, `TBD|TODO|FIXME` = 0,
  celdas vacías = 0.
- `git status --porcelain app/ packages/` vacío; `.env` y `.env.example` sin cambios.
- Las 4 filas de `## 5. Trazabilidad` apuntan a secciones existentes del propio artefacto.
</verification>

<success_criteria>
- SC1: cada fecha del inventario 113 tiene veredicto explícito con su columna de origen citada.
- SC2: toda ocurrencia de `fecha_captura` presentada como el hecho está listada con archivo:línea
  y superficie.
- SC3: las date-only del Congreso están verificadas contra el gotcha LOCKED de zona horaria.
- SC4: el veredicto está cruzado contra el dato real de PROD con un sujeto concreto por superficie.
- La completitud es demostrada por un script que muerde, y los hallazgos son consumibles por
  Phase 117 sin re-investigación.
</success_criteria>

<output>
Create `.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-04-SUMMARY.md` when done
</output>
