---
phase: 116-fecha-audit-sem-ntica-de-cada-fecha-visible
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md
autonomous: true
requirements: [FECHA-01]

must_haves:
  truths:
    - "Cada fecha visible del carril proyecto/agenda/home/buscar tiene veredicto hecho/captura/ambigua con su columna o RPC de origen citada"
    - "Toda fecha date-only del Congreso (citacion.fecha y análogas) queda verificada contra el gotcha LOCKED: ninguna conversión de zona horaria pasa sin hallazgo"
    - "La ocurrencia conocida de urgenciaFuente.fechaCaptura en estado-actual-block.tsx:429 queda resuelta con veredicto explícito"
    - "El emisor HUÉRFANO E-008 está auditado con su condición registrada"
  artifacts:
    - path: ".planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md"
      provides: "Veredicto por emisor×fecha del grupo B (20 ids E-xxx)"
      contains: "diaCalendarioCitacion"
      min_lines: 60
  key_links:
    - from: "116-PARCIAL-B.md"
      to: "116-FORMATTERS.md"
      via: "aplica el veredicto de capa por formatter sin re-derivarlo"
      pattern: "116-FORMATTERS"
    - from: "116-PARCIAL-B.md"
      to: "app/lib/dia-calendario.ts"
      via: "veredicto tz de las date-only del Congreso"
      pattern: "date-only"
---

<objective>
Emitir el veredicto **hecho / captura / ambigua** para cada fecha visible del **grupo B** de
emisores del catálogo 113 §3.0: el carril proyecto (`/proyecto/[boletin]`), `/agenda`, la home `/`
y `/buscar`.

Este grupo concentra el riesgo tz: las date-only del Congreso (`citacion.fecha`,
`sesion_tabla_item`) y el único punto conocido donde una fecha de scraping viaja a la ficha de
proyecto (`estado-actual-block.tsx:429`, `urgenciaFuente.fechaCaptura`).

Purpose: la otra mitad del universo de fechas del inventario, particionada para veredicto a
profundidad sin exceder el contexto de un ejecutor.
Output: `116-PARCIAL-B.md` — tabla emisor×fecha con veredicto y evidencia archivo:línea.

Régimen: SOLO LECTURA. NO se modifica código de producto ni copy. Todo fix va a Phase 117.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md

<interfaces>
<!-- GRUPO B — LOCKED. Estos 20 ids salen de 113 §3.0 (filas cuya columna "fechas que muestra"
     NO es "—"). El ejecutor NO re-particiona; el plan 04 detecta cualquier residual. -->

Carril /proyecto/[boletin]:
  E-010  app/components/timeline-view.tsx        Intl es-CL mes-año :22 → tabla.tramitacion_evento.fecha
  E-020  app/components/lobby-menciones-de-boletin.tsx  fechaCorta(fecha) :129 → RPC:lobby_menciones_de_boletin.fecha
  E-027  app/components/validacion-fuente.tsx    toLocaleDateString("es-CL") :226,:239 (timeZone America/Santiago :230,:243) → tabla.proyecto + props del llamante
  E-032  app/components/estado-actual-block.tsx  fechaCorta :397,:413,:429,:445,:460,:475,:497 ; relativeTimeEs :417 ; diaCalendarioCitacion :189,:221,:237,:270 ; Intl timeZone America/Santiago :153 → tabla.tramitacion_evento.fecha, tabla.citacion.fecha, tabla.citacion_punto :541, tabla.sesion_tabla_item :551.  **:429 = urgenciaFuente.fechaCaptura ⇒ fecha de SCRAPING, jamás el hecho**
  E-035  app/components/autor-row.tsx            props :57 → tabla.proyecto_autor + tabla.proyecto.enlace
  E-038  app/components/timeline-event.tsx       fechaCorta(fecha) :32 → tabla.tramitacion_evento.fecha
  E-041  app/components/lobby-en-tramitacion.tsx fechaCorta(fecha) :144 → RPC:lobby_en_tramitacion.fecha
  E-043  app/components/ficha-header.tsx         props :66 → tabla.proyecto
  E-044  app/components/cruces-de-proyecto.tsx   fechaCortaSegura(item.fecha) :168 → RPC:cruces_de_proyecto.fecha ; capturedAt = row.fecha_captura :177   [gate CRUCES = ON]
  E-045  app/components/capa1/tramitacion-stepper.tsx  fechaCorta(d) :99 ; fechaCorta(estado.urgenciaVigente.desde) :194 → tabla.tramitacion_evento.fecha
  E-048  app/app/proyecto/[boletin]/page.tsx     capturedAt = masReciente.fecha_captura :490 (sourceUrl={null}) → tabla.source_snapshot :649 ; tabla.proyecto_ficha :364 ; tabla.proyecto_autor :589
  E-056  app/components/votacion-card.tsx        fechaCorta(fecha) :39 → tabla.votacion.fecha ; capturedAt :96 → tabla.votacion.fecha_captura
  E-058  app/components/idea-matriz-block.tsx    tabla.proyecto_ficha (leída por E-048)

Carril /agenda:
  E-004  app/app/agenda/page.tsx        diaCalendarioCitacion(...) :334,:335,:438 → tabla.citacion.fecha  (DATE-ONLY medianoche UTC — jamás convertir tz)
  E-018  app/components/sala-table-section.tsx  props → tabla.sesion_sala / tabla.sesion_tabla_item
  E-033  app/components/citacion-card.tsx       props → tabla.citacion.fecha  (DATE-ONLY)

Carril / (home) y /buscar:
  E-055  app/components/panel-actualidad.tsx    diaCalendarioCitacion(iso) para agenda_* :104 ; fechaCorta(d) para el resto :107 → RPC:actualidad_senales_panel (contrato en :96-97)
  E-008  app/components/actualidad-module.tsx   fechaCorta(it.fecha) :202,:203,:451 ; fechaCorta(it.desde) :318 ; Intl día-Chile :46,:53,:95 (timeZone America/Santiago) → tabla.votacion.fecha / tabla.tramitacion_evento.fecha   [EMISOR HUÉRFANO, 113 §3.0.1 — superseded por E-055]
  E-028  app/components/search-result-card.tsx  props → RPC:match_proyectos / RPC:buscar_proyectos_hibrido
  E-026 y E-019 NO pertenecen a este grupo (están en el grupo A / plan 02) aunque aparezcan anidados

Estado de gates observado por 113 §5 el 2026-07-27:
  NET=ON  CRUCES=ON  VSIM=ON  MONEY=OFF  NOTIF=OFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Veredicto del carril /proyecto/[boletin] (E-010, E-020, E-027, E-035, E-038, E-041, E-043, E-044, E-045, E-048, E-056, E-058)</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md</files>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md (veredicto de capa por formatter y por call-site de capturedAt — NO re-derivarlo)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md (reglas LOCKED del veredicto tri-estado)
    - app/components/timeline-view.tsx (:15-:35)
    - app/components/timeline-event.tsx (archivo completo)
    - app/components/lobby-menciones-de-boletin.tsx (:120-:145)
    - app/components/lobby-en-tramitacion.tsx (:138-:155)
    - app/components/validacion-fuente.tsx (:215-:250)
    - app/components/autor-row.tsx (archivo completo)
    - app/components/ficha-header.tsx (:60-:90)
    - app/components/cruces-de-proyecto.tsx (:160-:185)
    - app/components/capa1/tramitacion-stepper.tsx (:90-:110, :185-:200)
    - app/app/proyecto/[boletin]/page.tsx (:355-:375, :480-:500, :560-:600, :640-:660)
    - app/components/votacion-card.tsx (:30-:50, :90-:110)
    - app/components/idea-matriz-block.tsx (archivo completo)
  </read_first>
  <action>
Crear `116-PARCIAL-B.md` con front-matter (`phase: 116`, `plan: 03`, `grupo: B`,
`consumido_por: [116-04]`) y la sección `## B.1 Carril /proyecto/[boletin]`.

Usar la MISMA tabla de 9 columnas y las MISMAS reglas de decisión que el grupo A, declaradas
verbatim al abrir la sección para que `116-PARCIAL-B.md` sea legible por sí solo:
`id E-xxx | ruta(s) | archivo:línea | formatter | columna/RPC de origen | VEREDICTO (hecho|captura|ambigua) | etiqueta visible actual (texto verbatim del JSX) | ¿miente o es ambigua? (sí/no + causa) | gate`.

Reglas de decisión (idénticas al grupo A):
  - Fecha vía `capturedAt` del `ProvenanceBadge` → **captura** por REGLA LOCKED (113 §3.1.1); se
    referencia el veredicto de `116-FORMATTERS.md` §2.2, no se recalcula. Aplica a `E-044:177`,
    `E-048:490`, `E-056:96`.
  - Toda otra fecha exige rastreo hasta columna o RPC concreto; sin rastreo ⇒ **ambigua** con causa
    `origen no rastreable estáticamente`. Jamás se adivina el origen.
  - Origen `fecha_captura` (o derivado) presentado sin el idiom "según fuente al…" ⇒ **captura** +
    `¿miente?` = `sí — presentada como el hecho`.
  - "captura" pelado en copy visible ⇒ `sí — término prohibido (v10.0)`.
  - Conversión de zona sobre date-only ⇒ `sí — conversión tz sobre date-only`.

Emitir una fila por (emisor, fecha renderizada) — no una por emisor. `E-045` renderiza dos fechas
de semántica distinta (`fecha` del paso vs `urgenciaVigente.desde`): son dos filas.

Casos con tratamiento explícito obligatorio:
  - **E-027 (`validacion-fuente.tsx:226,:239` con `timeZone: "America/Santiago"` en `:230,:243`)**:
    determinar el tipo del valor que recibe. Si es date-only del Congreso, la conversión a
    `America/Santiago` es hallazgo por regla LOCKED. Si es timestamp real, el veredicto se decide
    por si el render puede cambiar el día visible (cruzar con `116-FORMATTERS.md` §1). Registrar
    además QUÉ representa esa fecha en el bloque de validación de fuente (¿la fecha del hecho, o la
    fecha en que se consultó la fuente?) — esa distinción es el corazón de FECHA-01.
  - **E-048 (`page.tsx:490`)**: `masReciente.fecha_captura` de `source_snapshot` con
    `sourceUrl={null}` — badge sin link, solo fecha de captura. Veredicto **captura**; verificar el
    rótulo que lo acompaña.
  - **E-058 / E-043 / E-035 / E-028**: son consumidores de props. Rastrear hasta el llamante y
    registrar la columna real; si el llamante ya tiene fila propia, referenciarla en vez de
    duplicar el veredicto (pero la fila del consumidor DEBE existir).
  - **E-044** está bajo gate `CRUCES` (ON): superficie viva; su `fecha_captura` es la del FULL
    REBUILD diario de cruces, no la de la fuente original — anotarlo, porque cambia qué significa
    "según fuente al…" para ese bloque.

Cerrar con la línea literal `Sin hallazgos: <lista de ids>` para los emisores de la sección sin
defecto.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md; for id in E-010 E-020 E-027 E-035 E-038 E-041 E-043 E-044 E-045 E-048 E-056 E-058; do grep -qF "$id" "$f" || { echo "FALTA $id"; exit 1; }; done; grep -qF "Sin hallazgos:" "$f" && echo OK-B1'</automated>
  </verify>
  <acceptance_criteria>
    - `116-PARCIAL-B.md` existe con front-matter `phase: 116` / `plan: 03` / `grupo: B`.
    - Los 12 ids `E-010 E-020 E-027 E-035 E-038 E-041 E-043 E-044 E-045 E-048 E-056 E-058` están
      todos en la tabla de `## B.1`.
    - Toda fila tiene VEREDICTO del conjunto cerrado {`hecho`, `captura`, `ambigua`}.
    - Cero celdas vacías en filas de datos (`grep -nE '^\|.*\|[[:space:]]*\|'` menos separadoras → sin match).
    - Cada `archivo:línea` citado resuelve a una línea no vacía.
    - `E-045` tiene al menos 2 filas (dos fechas de semántica distinta).
    - `E-027` tiene registrado explícitamente si su valor es date-only o timestamp real, con la
      cadena `America/Santiago` citada.
    - `git status --porcelain app/ packages/` devuelve vacío.
  </acceptance_criteria>
  <done>Los 12 emisores del carril proyecto tienen veredicto por fecha con origen citado y etiqueta verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Veredicto de /agenda, home y /buscar + auditoría date-only (E-004, E-018, E-033, E-055, E-008, E-028) y el caso E-032</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md</files>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md (estado tras Task 1: reusar tabla y reglas)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md (§1 — veredicto de capa de diaCalendarioCitacion)
    - app/lib/dia-calendario.ts (archivo completo)
    - app/app/agenda/page.tsx (:325-:345, :430-:445)
    - app/components/citacion-card.tsx (archivo completo)
    - app/components/sala-table-section.tsx (archivo completo)
    - app/components/panel-actualidad.tsx (:90-:115, :220-:235)
    - app/components/actualidad-module.tsx (:40-:60, :90-:100, :195-:210, :310-:325, :445-:455)
    - app/components/search-result-card.tsx (archivo completo)
    - app/components/estado-actual-block.tsx (:145-:160, :180-:280, :390-:500, :535-:555)
  </read_first>
  <action>
Añadir a `116-PARCIAL-B.md` la sección `## B.2 /agenda, home, /buscar y estado-actual`, con la
misma tabla de 9 columnas (referenciar las reglas de `## B.1`, no re-declararlas).

Tratamiento específico obligatorio:

  - **E-032 (`estado-actual-block.tsx`) es el emisor más denso del sitio y el caso testigo de la
    fase**. Emitir una fila por CADA una de sus fechas, sin agrupar semánticas distintas:
    `:397`, `:413`, `:429`, `:445`, `:460`, `:475`, `:497` (fechaCorta), `:417` (relativeTimeEs),
    `:189`, `:221`, `:237`, `:270` (diaCalendarioCitacion), `:153` (Intl con
    `timeZone: "America/Santiago"`). La línea `:429` renderiza `urgenciaFuente.fechaCaptura` — es
    fecha de SCRAPING; su veredicto es **captura** y la pregunta a responder es si el rótulo que la
    acompaña la presenta como el hecho (p. ej. "urgencia desde…"). Este es el hallazgo esperado
    número uno de la fase: documentarlo con el texto visible verbatim.
    Las líneas con `diaCalendarioCitacion` reciben `citacion.fecha` (DATE-ONLY): verificar contra
    el veredicto de capa de `116-FORMATTERS.md` §1 que no hay conversión de zona.
    La línea `:153` usa `timeZone: "America/Santiago"`: rastrear qué valor recibe. Si recibe una
    date-only del Congreso ⇒ hallazgo por regla LOCKED v9.0.

  - **E-004, E-033, E-018 (carril /agenda)**: `citacion.fecha` y `sesion_tabla_item` son date-only
    medianoche UTC. Producir una subsección `### B.2.1 Auditoría date-only (gotcha LOCKED v9.0)`
    con una tabla dedicada: `columna date-only | emisor(es) | formatter | ¿se convierte de zona?
    (sí/no + evidencia de línea) | veredicto`. La regla es dura: la parte fecha UTC ES el día
    chileno; cualquier conversión es hallazgo. Enumerar TODAS las date-only que el grupo B toca
    (`citacion.fecha`, `citacion_punto`, `sesion_tabla_item`, y cualquier otra que aparezca en el
    rastreo), no solo las tres citadas.

  - **E-055 (`panel-actualidad.tsx`)**: usa `diaCalendarioCitacion` para las señales `agenda_*` y
    `fechaCorta` para el resto. Verificar que el ruteo entre ambos formatters coincida con el tipo
    real de cada señal según el contrato documentado en `:96-97`: una señal date-only formateada
    con `fechaCorta` (o viceversa) es hallazgo.

  - **E-008 (HUÉRFANO)**: se audita igual; la columna `gate` lleva
    `— (EMISOR HUÉRFANO, superseded por E-055)`. Sus tres `Intl` con
    `timeZone: "America/Santiago"` (`:46,:53,:95`) se auditan como los demás — el veredicto vale si
    alguien lo re-monta.

  - **E-028 (`search-result-card.tsx`)**: consumidor de props desde `RPC:match_proyectos` /
    `RPC:buscar_proyectos_hibrido`. Rastrear qué fecha muestra y si el rótulo la identifica.

Cerrar con `Sin hallazgos: <lista de ids>` y con una sección `## B.3 Cierre del grupo B` que liste
los 20 ids del grupo (los 12 de B.1 + `E-004 E-008 E-018 E-028 E-032 E-033 E-055` = 19; el residual
declarado explícitamente si el conteo real difiere) y afirme que cada uno tiene al menos una fila.
Si al recorrer 113 §3.0 aparece un id con fecha que no está en ninguno de los dos grupos, se
registra en una sección `## B.4 Residual detectado` con su id y archivo, para que el plan 04 lo
absorba — jamás se descarta en silencio.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-B.md; for id in E-004 E-008 E-018 E-028 E-032 E-033 E-055; do grep -qF "$id" "$f" || { echo "FALTA $id"; exit 1; }; done; grep -qF "### B.2.1 Auditoría date-only" "$f" && grep -qF "## B.3 Cierre del grupo B" "$f" && grep -qF ":429" "$f" && echo OK-B2'</automated>
  </verify>
  <acceptance_criteria>
    - Los 19 ids del grupo B (`E-004 E-008 E-010 E-018 E-020 E-027 E-028 E-032 E-033 E-035 E-038
      E-041 E-043 E-044 E-045 E-048 E-055 E-056 E-058`) aparecen todos en `116-PARCIAL-B.md`.
    - `E-032` tiene al menos 10 filas de veredicto (una por línea de fecha citada en el
      `<interfaces>` del `<context>`), y la fila de `:429` tiene VEREDICTO `captura`.
    - Existe la subsección `### B.2.1 Auditoría date-only (gotcha LOCKED v9.0)` con una tabla de
      ≥ 3 filas de datos, cada una con la columna `¿se convierte de zona?` resuelta en `sí` o `no`
      con evidencia de línea.
    - La fila de `E-008` contiene la cadena literal `HUÉRFANO`.
    - Existe `## B.3 Cierre del grupo B`; si hubo residual existe `## B.4 Residual detectado`.
    - Cero celdas vacías en filas de datos; cada `archivo:línea` citado resuelve.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 116-PARCIAL-B.md` → 0 y `grep -cE 'postgres(ql)?://'` → 0.
    - `git status --porcelain app/ packages/` devuelve vacío.
  </acceptance_criteria>
  <done>El grupo B queda cerrado: 19 emisores con veredicto, la auditoría date-only explícita, y el caso testigo `estado-actual-block.tsx:429` resuelto con texto visible verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → artefacto público en git | El artefacto no debe filtrar PII ni credenciales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-07 | Information Disclosure | `116-PARCIAL-B.md` | mitigate | Solo nombres de columna, jamás valores de fila. Compuertas RUT/`postgres://` → 0 matches en acceptance |
| T-116-08 | Tampering | código de producto | mitigate | `git status --porcelain app/ packages/` vacío en cada task |
| T-116-09 | Repudiation | veredicto sin evidencia | mitigate | Cada fila exige `archivo:línea` que resuelve a línea real; un veredicto sin rastreo se registra como `ambigua`, nunca se adivina |
| T-116-SC | Tampering | instalación de paquetes | accept | Plan sin instalaciones: cero superficie de supply chain |
</threat_model>

<verification>
- 19 ids del grupo B presentes, cada uno con ≥1 fila de veredicto del conjunto cerrado.
- Subsección date-only presente y resuelta.
- Cero celdas vacías, cero PII, cero credenciales.
- `git status --porcelain app/ packages/` vacío.
</verification>

<success_criteria>
- Toda fecha visible del carril proyecto/agenda/home/buscar tiene veredicto con origen citado.
- Las date-only del Congreso quedan verificadas contra el gotcha LOCKED con evidencia de línea.
- El emisor huérfano está auditado con su condición declarada; cualquier residual queda registrado
  en vez de descartado.
</success_criteria>

<output>
Create `.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-03-SUMMARY.md` when done
</output>
