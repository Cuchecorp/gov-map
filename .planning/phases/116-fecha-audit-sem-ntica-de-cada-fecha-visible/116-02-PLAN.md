---
phase: 116-fecha-audit-sem-ntica-de-cada-fecha-visible
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md
autonomous: true
requirements: [FECHA-01]

must_haves:
  truths:
    - "Cada fecha visible del carril parlamentario (ficha 360, directorio, comparar, cuenta) tiene veredicto hecho/captura/ambigua con su columna o RPC de origen citada"
    - "Los emisores bajo gate MONEY (E-013..E-016, E-060) están auditados en código, marcados 'no emitido en el deploy auditado', sin tocar ningún flag"
    - "El emisor HUÉRFANO E-003 está auditado con su condición registrada"
    - "Cada emisor sin defecto declara 'sin hallazgos' de forma explícita — cero excepciones silenciosas"
    - "Las columnas date-only del grupo A (militancia.desde/.hasta, declaracion.fecha_presentacion, votacion.fecha, fecha_oc/fecha_aporte/fecha_corte) quedan verificadas contra el gotcha tz LOCKED en una tabla dedicada"
  artifacts:
    - path: ".planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md"
      provides: "Veredicto por emisor×fecha del grupo A (18 ids E-xxx)"
      contains: "E-013"
      min_lines: 60
  key_links:
    - from: "116-PARCIAL-A.md"
      to: "116-FORMATTERS.md"
      via: "aplica el veredicto de capa por formatter sin re-derivarlo"
      pattern: "116-FORMATTERS"
    - from: "116-PARCIAL-A.md"
      to: "113-INVENTARIO.md §3.0"
      via: "usa los ids E-xxx del catálogo como clave primaria de fila"
      pattern: "E-0[0-9]{2}"
---

<objective>
Emitir el veredicto **hecho / captura / ambigua** para cada fecha visible del **grupo A** de
emisores del catálogo 113 §3.0: el carril parlamentario (ficha 360, directorio, `/comparar`,
`/cuenta`) más los emisores bajo gate MONEY.

Purpose: la mitad del universo de fechas del inventario, particionada para que el veredicto se
emita a profundidad (lectura del código, no grep superficial) sin exceder el contexto de un
ejecutor.
Output: `116-PARCIAL-A.md` — tabla emisor×fecha con veredicto y evidencia archivo:línea.

Régimen: SOLO LECTURA. NO se modifica código de producto ni copy. NO se toca ningún flag/gate.
Todo fix va a Phase 117.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md

<interfaces>
<!-- GRUPO A — LOCKED. Estos 18 ids salen de 113 §3.0 (filas cuya columna "fechas que muestra"
     NO empieza por "—"). El ejecutor NO re-particiona; el plan 04 detecta cualquier residual.
     ANCHORS NO AUTORITATIVOS: los números de línea son PISTAS heredadas del inventario 113 y
     pueden estar STALE. El ejecutor RE-LOCALIZA cada uno con `grep -n '<símbolo>' <archivo>`,
     registra la línea OBSERVADA y declara toda diferencia. -->

Carril ficha parlamentario:
  E-001  app/components/votos-por-parlamentario.tsx     fechaCortaSegura(e.fecha) :528 → RPC:votos_de_parlamentario.fecha ; Intl es-CL mes-año :287 (timeZone UTC :290)
  E-002  app/components/lobby-de-parlamentario.tsx      fechaCorta(new Date(a.fecha)) :153,:478 → RPC:lobby_de_parlamentario.fecha
  E-003  app/components/voto-ficha-row.tsx              props :135,:220 (`capturedAt=`) → RPC:votos_de_parlamentario.fecha / .fecha_captura  [EMISOR HUÉRFANO, 113 §3.0.1]
  E-005  app/components/patrimonio-de-parlamentario.tsx fechaCortaSegura(version.fecha_presentacion) :418,:687,:701,:728 → RPC:declaraciones_de_parlamentario.fecha_presentacion ; frescura tabla.probidad_ingesta_estado :989
  E-012  app/components/parlamentario-directory-row.tsx props → RPC:parlamentarios_publico_v2 (fecha vía E-019)
  E-019  app/components/partido-chip.tsx                fechaCorta(...) :65 → props ← RPC:parlamentarios_publico_v2 / parlamentario_publico_v2 (militancia vigente)
  E-026  app/components/voto-row.tsx                    props → tabla.votacion.fecha / RPC:votos_de_parlamentario
  E-053  app/components/cruces-de-parlamentario.tsx     fechaCorta(item.fecha) :178 → RPC:cruces_de_parlamentario.fecha ; capturedAt = s.fecha_captura :196 (comentario :184)   [gate CRUCES = ON]
  E-054  app/components/militancias-de-parlamentario.tsx fechaCorta(desde)/(hasta) :26,:27 → RPC:militancias_de_parlamentario.desde/.hasta (sin hasta ⇒ copy "vigente")
  E-057  app/components/comisiones-de-parlamentario.tsx  → RPC:comisiones_de_parlamentario
  E-059  app/components/parlamentario-header.tsx        capturedAt :116, sourceUrl :118 → RPC:parlamentario_publico_v2

Otras rutas del carril:
  E-051  app/app/comparar/page.tsx   Intl es "en-CA" :55 → RPC:coincidencia_votos_par.fecha_captura_max   [gate VSIM = ON]
  E-052  app/app/cuenta/page.tsx     fechaCorta local Intl "en-CA" :90-91 usado en :282,:310 → tabla.consentimiento.created_at :215 / tabla.suscripcion.created_at :210   [gate NOTIF = OFF]

Bajo gate MONEY = OFF ("no emitido en el deploy auditado"):
  E-013  app/components/financiamiento-de-parlamentario.tsx  fechaCorta(a.fecha_aporte) :176, fechaCorta(a.fecha_corte) :179,:355 → RPC:aportes_de_parlamentario ; frescura tabla.aportes_ingesta_estado :517
  E-014  app/components/contratos-por-contraparte.tsx        fechaCorta(c.fecha_oc) :136, fechaCorta(c.fecha_corte) :139 → RPC:agregado_por_contraparte
  E-015  app/components/contratos-de-parlamentario.tsx       fechaCorta(c.fecha_oc) :135, fechaCorta(c.fecha_corte) :138,:225 → RPC:contratos_de_parlamentario ; frescura tabla.contratos_ingesta_estado :353
  E-016  app/components/aportes-por-contraparte.tsx          fechaCorta(a.fecha_aporte) :149, fechaCorta(a.fecha_corte) :152 → RPC:agregado_por_contraparte
  E-060  app/app/contraparte/[id]/page.tsx                   RPC:agregado_por_contraparte ; su match de ProvenanceBadge es SOLO un COMENTARIO (:19), no un render

Estado de gates observado por 113 §5 el 2026-07-27:
  NET=ON  CRUCES=ON  VSIM=ON  MONEY=OFF  NOTIF=OFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Veredicto del carril ficha parlamentario (E-001, E-002, E-003, E-005, E-012, E-019, E-026, E-053, E-054, E-057, E-059)</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md</files>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md (veredicto de capa por formatter y por call-site de capturedAt — NO re-derivarlo)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md (reglas LOCKED del veredicto tri-estado)
    - app/components/votos-por-parlamentario.tsx (líneas alrededor de :287-:295 y :520-:560)
    - app/components/lobby-de-parlamentario.tsx (:145-:160 y :470-:485)
    - app/components/voto-ficha-row.tsx (:125-:145 y :210-:225)
    - app/components/patrimonio-de-parlamentario.tsx (:410-:425, :680-:735, :980-:995)
    - app/components/partido-chip.tsx (archivo completo — es corto y gobierna la fecha del directorio)
    - app/components/voto-row.tsx (archivo completo)
    - app/components/cruces-de-parlamentario.tsx (:170-:200)
    - app/components/militancias-de-parlamentario.tsx (archivo completo)
    - app/components/comisiones-de-parlamentario.tsx (archivo completo)
    - app/components/parlamentario-header.tsx (:105-:125)
  </read_first>
  <action>
Crear `116-PARCIAL-A.md` con front-matter (`phase: 116`, `plan: 02`, `grupo: A`,
`consumido_por: [116-04]`) y la sección `## A.1 Carril ficha parlamentario`.

Emitir **una fila por (emisor, fecha renderizada)** — no una fila por emisor: si `E-005` renderiza
`fecha_presentacion` en cuatro sitios distintos con la misma semántica, se agrupan en una fila
citando las cuatro líneas; si un emisor renderiza DOS fechas de semántica distinta (p. ej. `E-054`
con `desde` y `hasta`), son DOS filas.

Columnas exactas de la tabla:
`id E-xxx | ruta(s) | archivo:línea | formatter | columna/RPC de origen | VEREDICTO (hecho|captura|ambigua) | etiqueta visible actual (texto verbatim del JSX que rodea la fecha) | ¿miente o es ambigua? (sí/no + causa) | gate`.

Reglas de decisión, aplicadas sin excepción y declaradas al abrir la sección:
  - Toda fecha que llega vía la prop `capturedAt` del `ProvenanceBadge` → **captura** por REGLA
    LOCKED (113 §3.1.1), sin más análisis. El veredicto ya está en `116-FORMATTERS.md` §2.2: se
    referencia, no se recalcula.
  - Toda OTRA fecha exige rastreo explícito hasta su columna o RPC. Si el rastreo no llega a una
    columna concreta, el veredicto es **ambigua** con causa `origen no rastreable estáticamente` —
    jamás se adivina el origen.
  - Una fecha cuyo origen es `fecha_captura` (o derivado: `fecha_captura_max`, `fechaCaptura`,
    `capturedAt`, `ingestado_hasta`) y que se presenta SIN el idiom "según fuente al…" (o
    equivalente aprobado) → veredicto **captura** + columna "¿miente?" = `sí — presentada como el
    hecho`. Esto es hallazgo para Phase 117.
  - Aparición de la palabra "captura" pelada en copy visible → `sí — término prohibido (v10.0)`.
  - Fecha date-only sometida a conversión de zona → `sí — conversión tz sobre date-only`
    (gotcha v9.0 pasada 2).

La columna "etiqueta visible actual" se llena con el texto VERBATIM del JSX que el usuario lee
junto a la fecha (rótulo, prefijo, sufijo). Si el rótulo se compone dinámicamente, se registra la
expresión y sus valores posibles. Si no hay rótulo alguno (la fecha aparece pelada), se registra
`(sin rótulo)` — y eso es en sí un dato para el veredicto de ambigüedad.

Casos con tratamiento explícito obligatorio:
  - **E-003 (HUÉRFANO)**: se audita igual; la columna `gate` lleva `— (EMISOR HUÉRFANO, no
    renderizado en ninguna ruta)`. El veredicto vale si alguien lo re-monta. Ojo: `:135` y `:220`
    reciben `.fecha` y `.fecha_captura` del mismo RPC — verificar cuál llega a cuál render.
  - **E-019 / E-012**: `partido-chip` es quien pone la fecha del directorio; `parlamentario-directory-row`
    solo la propaga. Registrar el par sin duplicar el veredicto: la fila de E-012 referencia la de E-019.
  - **E-054**: la ausencia de `hasta` produce el copy "vigente". Verificar que NO se renderice una
    fecha inventada (p. ej. `new Date()`); si el copy dice "vigente" y no hay fecha, el veredicto
    de esa fila es `hecho` con nota `ausencia honesta`.
  - **E-053** está bajo gate `CRUCES` (ON en el deploy auditado): se audita como superficie viva.

Cerrar la sección con la línea literal `Sin hallazgos: <lista de ids>` para todos los emisores del
grupo cuya columna "¿miente?" sea `no` en TODAS sus filas. La ausencia de hallazgo se DECLARA;
nunca se omite un emisor.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md; for id in E-001 E-002 E-003 E-005 E-012 E-019 E-026 E-053 E-054 E-057 E-059; do grep -qF "$id" "$f" || { echo "FALTA $id"; exit 1; }; done; grep -qF "Sin hallazgos:" "$f" && echo OK-A1'</automated>
  </verify>
  <acceptance_criteria>
    - `116-PARCIAL-A.md` existe con front-matter `phase: 116` / `plan: 02` / `grupo: A`.
    - Los 11 ids `E-001 E-002 E-003 E-005 E-012 E-019 E-026 E-053 E-054 E-057 E-059` aparecen todos
      en la tabla de `## A.1`.
    - Toda fila tiene un VEREDICTO del conjunto cerrado {`hecho`, `captura`, `ambigua`}:
      `grep -oE '\| (hecho|captura|ambigua) \|' <archivo> | wc -l` es igual al número de filas de datos.
    - Cero celdas vacías en filas de datos: `grep -nE '^\|.*\|[[:space:]]*\|'` (excluidas las
      separadoras `|---|`) → sin match.
    - **Anchors verificados por símbolo, no por existencia.** Para CADA par `archivo:línea` citado,
      `sed -n '<linea>p' <archivo> | grep -qF '<símbolo>'` → exit 0. Una línea no vacía pero con
      otro contenido es un anchor STALE y NO cumple.
    - La fila de `E-003` contiene la cadena literal `HUÉRFANO`.
    - La sección cierra con una línea que empieza por `Sin hallazgos:`.
    - `git status --porcelain app/ packages/` devuelve vacío.
  </acceptance_criteria>
  <done>Los 11 emisores del carril ficha parlamentario tienen veredicto por fecha con origen citado, etiqueta visible verbatim y declaración explícita de ausencia de hallazgo donde corresponde.</done>
</task>

<task type="auto">
  <name>Task 2: Veredicto de /comparar, /cuenta y los emisores bajo gate MONEY (E-051, E-052, E-013, E-014, E-015, E-016, E-060)</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md</files>
  <read_first>
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md (estado tras Task 1: reusar la misma tabla y reglas)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md (§2.2 — los call-sites MONEY ya tienen veredicto de badge)
    - app/app/comparar/page.tsx (:45-:70 — el Intl "en-CA" y el consumo de fecha_captura_max)
    - app/app/cuenta/page.tsx (:85-:100, :205-:220, :275-:315)
    - app/components/financiamiento-de-parlamentario.tsx (:170-:185, :340-:360, :505-:525)
    - app/components/contratos-de-parlamentario.tsx (:128-:145, :215-:235, :345-:360)
    - app/components/contratos-por-contraparte.tsx (:128-:145)
    - app/components/aportes-por-contraparte.tsx (:142-:158)
    - app/app/contraparte/[id]/page.tsx (:15-:60 — el comentario :19 y el gate :50-52)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md §5 (estados de gate observados)
  </read_first>
  <action>
Añadir a `116-PARCIAL-A.md` la sección `## A.2 /comparar, /cuenta y gate MONEY`, con la MISMA tabla
de 9 columnas y las MISMAS reglas de decisión de `## A.1` (no re-declararlas: referenciarlas).

Tratamiento específico obligatorio:

  - **E-051 (`/comparar`, gate VSIM=ON)**: la fecha proviene de `RPC:coincidencia_votos_par`
    campo `fecha_captura_max` — un MÁXIMO de fechas de captura del par. Aplicar la regla dura:
    origen `fecha_captura` ⇒ veredicto **captura**. Verificar si el copy que la rodea usa el idiom
    aprobado o si la presenta como "última votación / última coincidencia" (que sería el hecho) —
    ese es exactamente el patrón de mentira que la fase busca. Registrar el rótulo verbatim.
    Verificar además si el `Intl` con locale `en-CA` produce formato ISO `YYYY-MM-DD` y si su
    timeZone es explícita o implícita del runtime (cruzar con `116-FORMATTERS.md` §1).

  - **E-052 (`/cuenta`, gate NOTIF=OFF)**: las fechas son `consentimiento.created_at` y
    `suscripcion.created_at` — timestamps de un acto del PROPIO usuario, no de scraping ni de un
    hecho del Congreso. Emitir veredicto justificado (probablemente `hecho`, siendo el hecho el
    acto del usuario) y anotarlo con la nota de gate `no emitido en el deploy auditado (NOTIF OFF)`.
    CERO PII: no se registra ningún email ni user_id, solo nombres de columna.

  - **E-013, E-014, E-015, E-016, E-060 (gate MONEY=OFF)**: se auditan en CÓDIGO igual que los
    vivos, porque el inventario lo exige explícitamente (113 §0.4.1 LÍMITE 3: "116 sí debe saber
    que existe copy de fecha bajo gate que hoy no se ve"). Cada fila lleva en la columna `gate` la
    cadena literal `MONEY — no emitido en el deploy auditado`. Prestar atención especial a la
    distinción semántica entre `fecha_oc` / `fecha_aporte` (fechas del HECHO: orden de compra,
    aporte) y `fecha_corte` (fecha de CORTE del dataset — no es el hecho ni es exactamente el reloj
    de scraping: es el borde del periodo cubierto por la fuente). Si `fecha_corte` se presenta sin
    distinguirla del hecho, el veredicto es **ambigua** y es hallazgo. Verificar también las fechas
    de frescura (`aportes_ingesta_estado`, `contratos_ingesta_estado`) que sí son reloj de ingesta.

  - **E-060**: registrar que su único match de `ProvenanceBadge` es un COMENTARIO (`:19`), no un
    render, y que la ruta entera 404ea con MONEY OFF (`page.tsx:50-52`). NO se enciende ningún
    flag; NO se prueba la ruta contra el deploy.

Producir además la subsección `### A.2.1 Auditoría date-only (gotcha LOCKED v9.0)` con una tabla
dedicada, del MISMO formato que la `### B.2.1` del plan 03, para que el plan 04 pueda unirlas:
`columna date-only | emisor(es) | formatter | ¿se convierte de zona? (sí/no + evidencia de línea) | veredicto`.

Filas obligatorias de esa tabla (las date-only que toca el grupo A):
  - `parlamentario_militancia.desde` y `.hasta` (E-054)
  - `declaracion.fecha_presentacion` (E-005)
  - `votacion.fecha` (E-001, E-026)
  - `lobby_audiencia.fecha` (E-002)
  - `contrato.fecha_oc`, `aporte.fecha_aporte` y `fecha_corte` (E-013, E-014, E-015, E-016)
Para cada una: determinar por lectura del código si el valor llega como `date` (date-only) o como
timestamp, y si el formatter aplicado le impone una zona. La regla es dura: sobre date-only, la
parte fecha UTC ES el día chileno; cualquier conversión de zona es hallazgo. Si el tipo real de la
columna no puede determinarse estáticamente, se marca `pendiente de confirmar por SQL en el plan
04` — jamás se adivina.

Cerrar con la línea literal `Sin hallazgos: <lista de ids>` para los emisores de esta sección sin
defecto, y con una sección `## A.3 Cierre del grupo A` que liste los 18 ids del grupo y afirme que
cada uno tiene al menos una fila de veredicto.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-PARCIAL-A.md; for id in E-051 E-052 E-013 E-014 E-015 E-016 E-060; do grep -qF "$id" "$f" || { echo "FALTA $id"; exit 1; }; done; grep -qF "no emitido en el deploy auditado" "$f" && grep -qF "## A.3 Cierre del grupo A" "$f" && echo OK-A2'</automated>
  </verify>
  <acceptance_criteria>
    - Los 18 ids del grupo A (`E-001 E-002 E-003 E-005 E-012 E-013 E-014 E-015 E-016 E-019 E-026
      E-051 E-052 E-053 E-054 E-057 E-059 E-060`) aparecen todos en `116-PARCIAL-A.md`.
    - Las 5 filas bajo MONEY contienen la cadena literal `no emitido en el deploy auditado`.
    - `fecha_corte` aparece en el documento con veredicto y justificación explícita distinta de la
      de `fecha_oc`/`fecha_aporte`.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 116-PARCIAL-A.md` → 0 (cero RUT).
    - `grep -cE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+' 116-PARCIAL-A.md` → 0 (cero email; el buzón
      institucional no pertenece a este carril).
    - `grep -cE 'postgres(ql)?://' 116-PARCIAL-A.md` → 0.
    - `git diff --quiet -- .env .env.example` (ningún flag tocado) y
      `git status --porcelain app/ packages/` vacío.
    - La sección `## A.3 Cierre del grupo A` enumera los 18 ids.
    - Existe la subsección `### A.2.1 Auditoría date-only (gotcha LOCKED v9.0)` con una tabla de
      ≥ 5 filas de datos, cada una con la columna `¿se convierte de zona?` resuelta en `sí`, `no` o
      `pendiente de confirmar por SQL en el plan 04`, con evidencia de línea.
    - **Anchors verificados por símbolo, no por existencia.** Para CADA par `archivo:línea` citado,
      `sed -n '<linea>p' <archivo> | grep -qF '<símbolo>'` → exit 0. Los anchors del bloque
      `<interfaces>` son PISTAS: se re-localizan con `grep -n '<símbolo>' <archivo>` y se registra
      la línea OBSERVADA; si difiere, se declara la diferencia.
  </acceptance_criteria>
  <done>El grupo A queda cerrado: 18 emisores, cada uno con al menos una fila de veredicto, los gated marcados, cero PII, cero flags tocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → artefacto público en git | El artefacto no debe filtrar PII ni credenciales |
| gates de producto (MONEY/NOTIF) | Auditar copy gated NO debe convertirse en encenderlo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04 | Information Disclosure | `116-PARCIAL-A.md` (carril patrimonio/financiamiento) | mitigate | Solo nombres de columna, jamás valores. Compuertas en acceptance: RUT/email/postgres:// → 0 matches |
| T-116-05 | Elevation of Privilege | flags `MONEY_PUBLIC_ENABLED` / `NOTIF_PUBLIC_ENABLED` | mitigate | Auditoría estrictamente de código; `git diff --quiet -- .env .env.example` es criterio de aceptación. El agente NO flipea gates (regla LOCKED v12.0) |
| T-116-06 | Tampering | código de producto | mitigate | `git status --porcelain app/ packages/` vacío en cada task |
| T-116-SC | Tampering | instalación de paquetes | accept | Plan sin instalaciones: cero superficie de supply chain |
</threat_model>

<verification>
- 18 ids del grupo A presentes, cada uno con ≥1 fila de veredicto del conjunto cerrado.
- Cero celdas vacías, cero PII, cero credenciales.
- `git status --porcelain app/ packages/` vacío; `.env`/`.env.example` sin cambios.
</verification>

<success_criteria>
- Toda fecha visible del carril parlamentario tiene veredicto con origen citado y etiqueta verbatim.
- Los emisores gated y el huérfano están auditados con su condición declarada, no omitidos.
- Cada emisor sin defecto tiene una declaración explícita `Sin hallazgos`.
</success_criteria>

<output>
Create `.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-02-SUMMARY.md` when done
</output>
