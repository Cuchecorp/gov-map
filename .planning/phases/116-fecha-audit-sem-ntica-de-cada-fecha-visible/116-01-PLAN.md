---
phase: 116-fecha-audit-sem-ntica-de-cada-fecha-visible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md
autonomous: true
requirements: [FECHA-01]

must_haves:
  truths:
    - "Cada formatter de fecha del sitio tiene semántica declarada: qué tipo de valor recibe (timestamp real vs date-only) y si su render puede cambiar el día visible"
    - "El chokepoint ProvenanceBadge queda auditado UNA vez, con la regla LOCKED de que capturedAt es SIEMPRE fecha de captura"
    - "Los N call-sites de capturedAt observados por el grep vivo (N se declara en el artefacto, jamás se hereda de 113) quedan verificados uno por uno: cada uno pasa fecha_captura (correcto) o una fecha del hecho (hallazgo)"
    - "El grupo A y el grupo B de emisores (planes 02 y 03) reciben una tabla de formatters común, sin re-derivarla"
  artifacts:
    - path: ".planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md"
      provides: "Semántica por formatter + auditoría del chokepoint ProvenanceBadge + tabla de call-sites capturedAt"
      contains: "diaCalendarioCitacion"
      min_lines: 80
  key_links:
    - from: "116-FORMATTERS.md"
      to: "app/lib/format.ts + app/lib/dia-calendario.ts + app/components/provenance-badge.tsx"
      via: "cita archivo:línea verbatim por formatter"
      pattern: "app/lib/format\\.ts:[0-9]+"
    - from: "116-FORMATTERS.md"
      to: "113-INVENTARIO.md §3.1"
      via: "reutiliza la tabla de call-sites de sourceUrl/capturedAt como denominador"
      pattern: "§3\\.1"
---

<objective>
Auditar, ANTES de recorrer los emisores, la capa transversal que gobierna toda fecha visible del
sitio: (a) los formatters centrales (`fechaCorta`, `fechaCortaSegura`, `relativeTimeEs`,
`diaCalendarioCitacion`, los `Intl.DateTimeFormat` inline) y (b) el chokepoint DUAL
`ProvenanceBadge` (`capturedAt`).

Purpose: sin esto, los planes 02 y 03 re-derivarían la misma semántica en paralelo y podrían
divergir en el veredicto de un mismo formatter. Esta base compartida hace que el veredicto por
emisor sea una aplicación mecánica, no un juicio repetido.
Output: `116-FORMATTERS.md` — tabla de semántica por formatter + auditoría del badge + veredicto
por cada call-site de `capturedAt`.

Régimen: SOLO LECTURA. Esta fase NO modifica código de producto ni copy. Todo fix va a Phase 117.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md

<interfaces>
<!-- Firmas de la capa de formateo. ANCHORS NO AUTORITATIVOS: los números de línea son una
     PISTA de partida, no la verdad. El ejecutor RE-LOCALIZA cada anchor con
     `grep -n '<símbolo>' <archivo>`, registra la línea OBSERVADA en el artefacto y, si difiere
     de la citada aquí, DECLARA la diferencia en una nota. Un anchor cuya línea no contiene su
     símbolo es un anchor stale: se corrige, nunca se copia. -->

app/lib/format.ts:
  export function fechaCorta(d: Date): string                       // :21
  export function relativeTimeEs(capturedAt: Date, now?: Date): string  // :33
  export function esStale(...)                                      // :65 (umbral 48h → amber)
  export function fechaCortaSegura(...)                             // :121
  export function partidoLegible(raw: string|null|undefined): string|null  // :153 (NO es fecha)

app/lib/dia-calendario.ts:
  diaCalendarioCitacion(...)   // :41 declara "Cero aritmética de zona: no se usa Intl con timeZone America/Santiago."

app/components/provenance-badge.tsx  (anchors re-verificados 2026-07-28; re-localizar igual):
  prop capturedAt: Date | null   // :38
  prop sourceUrl: string | null  // :42
  esStale(capturedAt)            // :66      relativeTimeEs(capturedAt)  // :90
  safeExternalHref(sourceUrl)    // :70
  capturedAt === null → "Sin fecha de actualización" (:92); displaySource → "fuente desconocida" (:67)
  tooltip capturedAt.toISOString()  // :128
  rama de omisión del badge: capturedAt === null && safeUrl === null (:114)

Usos de timeZone explícita (grep verificado 2026-07-28):
  app/lib/week-utils.ts:113,119            timeZone: "UTC"
  app/components/actualidad-module.tsx:47,54,96   timeZone: "America/Santiago"  (emisor HUÉRFANO E-008)
  app/components/estado-actual-block.tsx:153      timeZone: "America/Santiago"
  app/components/validacion-fuente.tsx:230,243    timeZone: "America/Santiago"
  app/components/votos-por-parlamentario.tsx:290  timeZone: "UTC"  (comentario WR-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabla de semántica por formatter</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md</files>
  <read_first>
    - app/lib/format.ts (cuerpo completo de fechaCorta, fechaCortaSegura, relativeTimeEs, esStale)
    - app/lib/dia-calendario.ts (cuerpo completo de diaCalendarioCitacion y su comentario :41)
    - app/lib/week-utils.ts (líneas 100-130: los dos Intl con timeZone "UTC")
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-CONTEXT.md (reglas LOCKED del veredicto)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md §0.2 (comando grep de fechas — es el universo)
  </read_first>
  <action>
Crear `116-FORMATTERS.md` con front-matter (`phase: 116`, `plan: 01`, `tipo: base-compartida`,
`consumido_por: [116-02, 116-03, 116-04]`) y una sección `## 1. Semántica por formatter`.

Enumerar el universo de formatters re-corriendo el grep verbatim de 113 §0.2 sobre
`app/app app/components app/lib --include=*.tsx --include=*.ts` filtrando `.test.`, y registrar el
conteo observado de archivos (113 observó 28 el 2026-07-27; si hoy difiere, se declara la
diferencia, NO se copia el número).

Una fila por formatter con estas columnas exactas:
`formatter | archivo:línea | tipo de entrada (timestamp real / date-only / Date ya construido por el llamante) | timeZone efectiva (explícita, o implícita del runtime) | ¿puede cambiar el día visible? (sí/no + por qué) | veredicto de la capa (seguro / riesgo-tz / ambiguo-por-construcción)`.

**Re-localizar TODO anchor antes de citarlo.** Los números de línea de abajo y del bloque
`<interfaces>` son PISTAS, no verdad: para cada símbolo se corre `grep -n '<símbolo>' <archivo>`,
se registra la línea OBSERVADA en la tabla y, si difiere de la citada en el plan, se declara la
diferencia en una nota al pie de la sección. Jamás se copia un anchor sin re-localizarlo.

Filas obligatorias (mínimo): `fechaCorta` (format.ts:21), `fechaCortaSegura` (format.ts:121),
`relativeTimeEs` (format.ts:33), `esStale` (format.ts:65), `diaCalendarioCitacion`
(dia-calendario.ts), los dos Intl UTC de `week-utils.ts:113,119`, y cada `Intl.DateTimeFormat`
inline listado en el bloque `<interfaces>` del `<context>` (actualidad-module.tsx:47,54,96;
estado-actual-block.tsx:153; validacion-fuente.tsx:230,243; votos-por-parlamentario.tsx:290;
comparar/page.tsx:55 `en-CA`; cuenta/page.tsx:90-91 `en-CA`; timeline-view.tsx:29 `mesAnioFormatter`).

Aplicar las tres reglas LOCKED del veredicto, citándolas verbatim en una sección
`## 0. Reglas LOCKED aplicadas`:
  1. `fecha_captura` es reloj de scraping y JAMÁS representa el hecho (gotcha Phase 98).
  2. "captura" pelado en copy visible está PROHIBIDO (decisión v10.0); el idiom aprobado es
     "según fuente al…".
  3. Las fechas date-only del Congreso (`citacion.fecha` y análogas) son medianoche UTC y la parte
     fecha UTC ES el día chileno: cualquier conversión de zona sobre date-only es hallazgo
     (gotcha mayor v9.0 pasada 2).

Para `diaCalendarioCitacion` declarar explícitamente si su implementación honra la regla 3 (el
comentario de `dia-calendario.ts:41` afirma "Cero aritmética de zona"; verificar que el CUERPO lo
cumpla, no solo el comentario) y registrar el veredicto con evidencia de línea.

Para cada `Intl.DateTimeFormat` con `timeZone: "America/Santiago"` determinar si el valor que
recibe es date-only (⇒ conversión prohibida ⇒ candidato a hallazgo, se anota como
`riesgo-tz — confirmar el origen del valor en el plan 02/03`) o timestamp real (⇒ evaluación caso
a caso: `ambiguo` solo si el render puede cambiar el día visible).

NO proponer fixes en código. Cada riesgo se anota como `candidato a hallazgo` con el texto que el
plan 04 convertirá en `F-xx`.
  </action>
  <verify>
    <automated>test -f .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md && grep -c 'diaCalendarioCitacion\|fechaCortaSegura\|relativeTimeEs\|esStale' .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `116-FORMATTERS.md` existe y tiene front-matter YAML con `phase: 116` y `plan: 01`.
    - La sección `## 0. Reglas LOCKED aplicadas` contiene las cadenas literales `fecha_captura`,
      `según fuente al` y `medianoche UTC`.
    - La sección `## 1. Semántica por formatter` contiene al menos 12 filas de datos (excluida la
      cabecera y la separadora), cada una con las 6 columnas y CERO celdas vacías
      (`grep -nE '^\|.*\|[[:space:]]*\|'` sobre las filas de la tabla → sin match).
    - **Anchors verificados por símbolo, no por existencia.** Para CADA par `archivo:línea`
      citado en el artefacto, la línea debe CONTENER el símbolo que se le atribuye:
      `sed -n '<linea>p' <archivo> | grep -qF '<símbolo>'` → exit 0. Una línea no vacía pero
      con otro contenido es un anchor STALE y NO cumple.
    - Los símbolos `fechaCorta`, `fechaCortaSegura`, `relativeTimeEs`, `esStale`,
      `diaCalendarioCitacion` aparecen todos en la tabla.
    - `git status --porcelain app/ packages/` devuelve vacío (la fase no toca código de producto).
  </acceptance_criteria>
  <done>Tabla de semántica por formatter completa, con veredicto de capa y evidencia archivo:línea verificable para cada fila.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoría del chokepoint ProvenanceBadge y sus call-sites de capturedAt</name>
  <files>.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md</files>
  <read_first>
    - app/components/provenance-badge.tsx (archivo completo — ~130 líneas: props :38,:42; esStale :66; displaySource :67; safeExternalHref :70; relativeTimeEs :90; texto null :92; rama de omisión :114; tooltip :128)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md §3.1 y §3.1.4 (tabla de 17 filas de call-sites de sourceUrl — es el denominador)
    - .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md (estado actual tras Task 1)
  </read_first>
  <action>
Añadir a `116-FORMATTERS.md` la sección `## 2. Chokepoint ProvenanceBadge (E-040)`.

Subsección `### 2.1 El componente`: declarar, con archivo:línea de `provenance-badge.tsx`, el
veredicto del badge como componente: prop `capturedAt` (:38), prop `sourceUrl` (:42), `esStale`
umbral 48h (:66), `displaySource` → "fuente desconocida" (:67), formatter `relativeTimeEs` (:90),
degradación `null` → texto literal "Sin fecha de actualización" (:92), rama de omisión del
badge (:114), tooltip `capturedAt.toISOString()` (:128). Estos anchors se RE-LOCALIZAN con
`grep -n '<símbolo>' app/components/provenance-badge.tsx` y se registra la línea OBSERVADA; si
difiere de la citada aquí, se declara la diferencia. Registrar el texto VISIBLE exacto que el badge emite
alrededor de la fecha (leerlo del JSX, no parafrasearlo) y evaluarlo contra las reglas LOCKED:
¿usa el idiom "según fuente al…" o equivalente aprobado? ¿aparece la palabra "captura" pelada?
Emitir veredicto del componente: **captura** (es el reloj de scraping por construcción) + un
segundo veredicto de COPY: `idiom correcto` / `candidato a hallazgo`.

Subsección `### 2.2 Veredicto por call-site de `capturedAt``: re-correr
`grep -rn "capturedAt=" app/app app/components --include=*.tsx | grep -v "\.test\."` para obtener
el denominador vivo (NO copiar el conteo de 113; declarar el conteo observado y, si difiere de las
15 filas de producción de 113 §3.1.4, declarar la diferencia y su causa).

Una fila por call-site con columnas:
`# | id E-xxx (de 113 §3.0) | archivo:línea | expresión del prop verbatim | columna/RPC de origen | ¿es fecha_captura? (sí/no) | veredicto (captura correcta / HECHO-COMO-CAPTURA / captura-como-hecho / ambigua) | gate | nota`.

La regla de decisión es dura y se declara al abrir la tabla: toda fecha que llega por `capturedAt`
se MARCA como `captura` sin más análisis (REGLA LOCKED, 113 §3.1.1). El hallazgo aparece por dos
vías, ambas obligatorias de chequear:
  (a) el llamante pasa por `capturedAt` una fecha que NO es `fecha_captura` (p. ej. la fecha del
      hecho) → hallazgo `HECHO-COMO-CAPTURA`;
  (b) el llamante presenta `fecha_captura` FUERA del badge, en copy propio, sin el idiom aprobado
      → se anota como pista para los planes 02/03 (no se resuelve aquí).

Marcar explícitamente los call-sites bajo gate (`MONEY` → `no emitido en el deploy auditado`) y el
emisor HUÉRFANO `voto-ficha-row.tsx` (E-003): se auditan igual, con su condición registrada en la
columna `nota`.

Cerrar con `### 2.3 Cierre del denominador`: N call-sites observados = N filas de la tabla; cero
call-site sin veredicto.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && n_grep=$(grep -rn "capturedAt=" app/app app/components --include=*.tsx | grep -v "\.test\." | wc -l); echo "call-sites observados: $n_grep"; grep -q "### 2.3 Cierre del denominador" .planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FORMATTERS.md && echo OK-cierre'</automated>
  </verify>
  <acceptance_criteria>
    - `116-FORMATTERS.md` contiene las secciones `## 2. Chokepoint ProvenanceBadge (E-040)`,
      `### 2.1 El componente`, `### 2.2` y `### 2.3 Cierre del denominador`.
    - La tabla de §2.2 tiene una fila por cada call-site que devuelve
      `grep -rn "capturedAt=" app/app app/components --include=*.tsx | grep -v "\.test\."`;
      el número declarado en §2.3 es idéntico al `wc -l` de ese grep.
    - Toda fila de §2.2 tiene un valor de la columna veredicto dentro del conjunto cerrado
      {`captura correcta`, `HECHO-COMO-CAPTURA`, `captura-como-hecho`, `ambigua`}; cero celdas vacías.
    - Los ids `E-003` (huérfano) y al menos uno de `E-013`/`E-014`/`E-015`/`E-016` (MONEY) aparecen
      con la cadena literal `no emitido en el deploy auditado` o `HUÉRFANO` en su columna `nota`.
    - El texto visible del badge citado en §2.1 aparece verbatim en `provenance-badge.tsx`
      (`grep -F "<texto citado>" app/components/provenance-badge.tsx` → match).
    - **Anchors verificados por símbolo, no por existencia.** Para CADA par `archivo:línea`
      citado en el artefacto, la línea debe CONTENER el símbolo que se le atribuye:
      `sed -n '<linea>p' <archivo> | grep -qF '<símbolo>'` → exit 0. Una línea no vacía pero
      con otro contenido es un anchor STALE y NO cumple.
    - `git status --porcelain app/ packages/` devuelve vacío.
  </acceptance_criteria>
  <done>El chokepoint queda auditado una sola vez y cada call-site de `capturedAt` tiene veredicto explícito con su gate y su condición de huérfano registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → artefacto de planificación | El artefacto es público en git: no debe filtrar credenciales ni PII |
| (sin frontera de red) | Este plan NO golpea PROD ni fuentes gubernamentales: solo lee el repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Information Disclosure | `116-FORMATTERS.md` | mitigate | Cero valores de columnas PII (RUT/email/monto). Solo nombres de columna. Compuerta de cierre en el plan 04: `grep -cE '[0-9]{7,8}-[0-9kK]'` → 0 |
| T-116-02 | Information Disclosure | invocación psql | mitigate | Este plan no invoca psql. La regla (jamás imprimir `SUPABASE_DB_URL`) se hereda al plan 04 |
| T-116-03 | Tampering | código de producto | mitigate | Régimen solo-lectura: `git status --porcelain app/ packages/` vacío es criterio de aceptación de cada task |
| T-116-SC | Tampering | instalación de paquetes | accept | Este plan no instala ningún paquete (npm/pnpm/pip): cero superficie de supply chain |
</threat_model>

<verification>
- `116-FORMATTERS.md` existe con las secciones 0, 1, 2.1, 2.2, 2.3.
- Cada `archivo:línea` citado resuelve a una línea real del repo.
- Cero celdas de tabla vacías en filas de datos.
- `git status --porcelain app/ packages/` → vacío.
</verification>

<success_criteria>
- Todo formatter de fecha del universo del grep de 113 §0.2 tiene semántica y veredicto de capa.
- Todo call-site de `capturedAt` tiene veredicto dentro del conjunto cerrado, con gate y condición
  de huérfano declaradas.
- Los planes 02 y 03 pueden aplicar el veredicto por emisor sin re-derivar la semántica del
  formatter ni la del badge.
</success_criteria>

<output>
Create `.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-01-SUMMARY.md` when done
</output>
