---
phase: 12-int-patrimonio-intereses-declaraciones-con-historial-y-compa
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - app/components/ui/table.tsx
  - app/lib/types.ts
  - app/components/patrimonio-de-parlamentario.tsx
  - app/components/patrimonio-de-parlamentario.test.tsx
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: [INT-04, INT-05]
user_setup: []

must_haves:
  truths:
    - "La ficha /parlamentario/[id] muestra una sección de patrimonio en su propio carril, separada de #lobby por mt-12"
    - "El historial lista versiones por fecha_presentacion DESC; cada versión muestra la fecha PROMINENTE (font-mono) + badge ámbar de frescura; una declaración vieja NUNCA se lee como estado actual"
    - "La comparación dispone N versiones lado-a-lado (shadcn Table) con SOLO valores literales — CERO columna de delta/variación/enriquecimiento/conflicto; un campo ausente lee 'No declarado en esta versión'"
    - "La atribución CC BY 4.0 es visible en el intro Y repetida en el caption de la comparación (vista derivada)"
    - "La sección distingue tres estados honestos: no-ingestado, ingestado-cero-confirmadas, con-versiones; un vacío nunca lee como ausencia de patrimonio"
    - "El content-gate test ejerce TANTO la lista COMO la comparación; ningún campo fuera del contrato §10; cero veredicto/causal/afinidad; ningún RUT/familiar en el DOM"
  artifacts:
    - path: "app/components/patrimonio-de-parlamentario.tsx"
      provides: "PatrimonioSection (Server Component) + PatrimonioView + DeclaracionComparacion (puros, testeables) que leen declaraciones_de_parlamentario + comparar_declaraciones + probidad_ingesta_estado"
      contains: "declaraciones_de_parlamentario"
    - path: "app/components/patrimonio-de-parlamentario.test.tsx"
      provides: "RTL: carril propio, fecha prominente + frescura, comparación sin veredicto, CC BY 4.0 en caption, 3 estados honestos, gate anti-veredicto sobre lista Y comparación"
      min_lines: 60
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "PatrimonioSection"
      via: "<section id=\"patrimonio\" className=\"mt-12\"> apilada tras #lobby"
      pattern: "id=\"patrimonio\""
    - from: "app/components/patrimonio-de-parlamentario.tsx"
      to: "public.comparar_declaraciones"
      via: "sb.rpc"
      pattern: "comparar_declaraciones"
---

<objective>
Apilar una `<section id="patrimonio">` en el shell existente de `/parlamentario/[id]` (el comentario placeholder ya reserva el lugar tras `#lobby`), en su propio carril, que renderiza: (1) el HISTORIAL de versiones de declaraciones confirmadas leídas vía `declaraciones_de_parlamentario` (fecha de presentación PROMINENTE + badge ámbar de frescura; una vieja nunca lee como actual — INT-04), y (2) la COMPARACIÓN lado-a-lado vía `comparar_declaraciones` (shadcn Table, SOLO datos, CERO veredicto/delta — INT-05). CC BY 4.0 visible en el intro Y en el caption de la comparación. Tres estados honestos; cero composición cruzada con votos/lobby; cero lenguaje de veredicto/causal/afinidad. El content-gate §9.1 cubre TANTO la lista COMO la comparación (cierra la brecha `representado` de Phase 11).

Purpose: INT-04 (el ciudadano ve el historial fechado) + INT-05 (compara versiones lado-a-lado sin veredicto). El gate de contenido §9.1 (CERO "enriquecimiento"/"conflicto"/delta, carril aislado, sin RUT/familiar) es el release gate más fuerte de la fase, y cubre explícitamente la vista derivada de comparación.
Output: el componente Server + tests RTL + la sección apilada + el componente shadcn `Table`. Server Component end-to-end (sin "use client"; comparación SSR vía `?comparar=A,B`, sin motor de diff cliente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-UI-SPEC.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-RESEARCH.md

<read_first>
- `app/app/parlamentario/[id]/page.tsx` — el shell apilable (Phase 10/11). El comentario placeholder YA marca `<section id="patrimonio" className="mt-12"> <h2>Patrimonio e intereses</h2> …` tras `#lobby`. NO re-construir el shell ni la cabecera; apilar como sibling de `#lobby` con su propio `<h2>` + `<Suspense>` + `PatrimonioSkeleton`, a `mt-12`.
- `app/components/lobby-de-parlamentario.tsx` — el patrón EXACTO a espejar (Phase 11): `LobbySection` (Server Component que llama `sb.rpc` + consulta el marcador de ingesta para `noIngestado` REAL) + `LobbyView` (puro, RTL-testeable por props), paginación server-driven, TRES estados honestos distintos, distinción `.maybeSingle()`/error real ≠ "sin filas", el gate de contenido §9.1 documentado en el encabezado, contraparte cruda + IdentityMarker. `PatrimonioSection`/`PatrimonioView` lo espejan.
- `app/components/lobby-de-parlamentario.test.tsx` — el patrón de test RTL + content-gate (assertion negativa sobre `textContent`: sin términos prohibidos, sin RUT, sin composición cruzada).
- `app/lib/types.ts` — `LobbyAudienciaRow` (línea 229), `VotoFichaRow`, `sourceLabel(origen)` (línea 248). Añadir aquí `DeclaracionVersionRow` (UI-SPEC §10: `declaracion_id`, `version_id`, `tipo`, `fecha_presentacion`, `parlamentario_estado_vinculo`, `parlamentario_mencion`, `campos: {etiqueta, valor}[]`, `origen`, `fecha_captura`, `enlace`, `licencia: "CC BY 4.0"`, `es_historica`). NUNCA RUT/familiar en el payload.
- `app/lib/format.ts` — `fechaCorta`, `relativeTimeEs`, `esStale` (reusados vía ProvenanceBadge + columnas de comparación).
- `app/components/provenance-badge.tsx` — `ProvenanceBadge` (props `capturedAt: Date|null`, `sourceName`, `sourceUrl: string|null`); reuse verbatim, por versión Y por columna de comparación, obligatorio. El estado ámbar es la señal de frescura.
- `app/components/identity-marker.tsx` — `IdentityMarker` ("identidad no verificada"); reuse verbatim para una mención de declarante no confirmada (excluida por defecto del perfil).
- `app/lib/supabase.ts` — `createServerSupabase()` (anon key, RLS public-read).
- `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-UI-SPEC.md` — §3 (layout + carril mt-12), §3.2 (anatomía de fila de versión + fecha prominente + frescura), §3.3 (disclosure server-driven), §3.4 (guarda de identidad — solo confirmado entra), §3.5 (comparación lado-a-lado — LOCKED, SOLO DATOS, las 7 reglas), §3.6 (anti-features: sin viz/score/flag/cross-composition), §6.1 (estados honestos), §6.4 (caveat de frescura), §9 (copy contract), §9.1 (gate de contenido + el mandato gate-test sobre la comparación), §10 (contrato de payload), §11 (Table es el único componente shadcn nuevo).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir shadcn Table + tipo de payload DeclaracionVersionRow</name>
  <files>app/components/ui/table.tsx, app/lib/types.ts</files>
  <action>
1. **Añadir el componente shadcn `Table`** (`pnpm dlx shadcn@latest add table`) — el único componente oficial nuevo de la fase (UI-SPEC §11), usado EXCLUSIVAMENTE para la grilla de comparación lado-a-lado. Es una tabla de datos sobria, NO un widget de "diff". Verificar que se creó `app/components/ui/table.tsx` con los exports estándar (`Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `TableCaption`). Registro oficial — sin vetting de seguridad de terceros.

2. **`app/lib/types.ts`**: añadir `DeclaracionVersionRow` (UI-SPEC §10) — `declaracion_id: string`, `version_id: string`, `tipo: "patrimonio" | "intereses"` (label literal de la fuente), `fecha_presentacion: string` (ISO; PROMINENTE), `parlamentario_id: string | null`, `parlamentario_estado_vinculo: "confirmado" | "probable" | "revision" | "no_confirmado" | null`, `parlamentario_mencion: string`, `campos: Array<{ etiqueta: string; valor: string }>` (etiqueta = NOUN label muted; valor = literal verbatim, incl. "No declarado en esta versión"), `origen: string`, `fecha_captura: string`, `enlace: string | null`, `licencia: "CC BY 4.0"`, `es_historica: boolean`. NO incluir RUT del parlamentario ni datos de familiares (LEGAL-03 deny-by-default; el RPC no los proyecta). Reusar `sourceLabel` para `origen` → "InfoProbidad".
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('app/components/ui/table.tsx')){console.error('falta table.tsx');process.exit(1)} const t=fs.readFileSync('app/lib/types.ts','utf8'); const must=['DeclaracionVersionRow','fecha_presentacion','campos','es_historica','licencia']; const miss=must.filter(m=>!t.includes(m)); if(miss.length){console.error('FALTAN en types.ts:',miss);process.exit(1)} if(/rut/i.test(t.slice(t.indexOf('DeclaracionVersionRow'), t.indexOf('DeclaracionVersionRow')+800))){console.error('PROHIBIDO: RUT en el payload de patrimonio');process.exit(1)} console.log('Task1 OK')"</automated>
  </verify>
  <done>`app/components/ui/table.tsx` existe (shadcn oficial); `DeclaracionVersionRow` está en `types.ts` con `campos`/`fecha_presentacion`/`es_historica`/`licencia` y SIN RUT/familiar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PatrimonioView (historial) + DeclaracionComparacion + PatrimonioSection (Server Component) + tests RTL con content-gate sobre lista Y comparación</name>
  <files>app/components/patrimonio-de-parlamentario.tsx, app/components/patrimonio-de-parlamentario.test.tsx</files>
  <behavior>
    - `PatrimonioView` (puro, por props) renderiza una línea de intro muted + la atribución CC BY 4.0 visible (texto + link a la licencia), y una `<ul>` de versiones en orden `fecha_presentacion DESC`, cada `<li>` (DeclaracionVersionRow) con: fecha de presentación PROMINENTE (`font-mono`, `text-base`, labeled "Presentada el {fecha}"), badge ámbar de frescura si `es_historica` + el caveat §6.4 ("Esta es una declaración histórica…"), tipo de declaración (label literal, muted), campos como `<dl>` clave/valor (etiqueta NOUN muted + valor literal), y `ProvenanceBadge` por versión (obligatorio). Una declaración vieja NUNCA lee como "actual".
    - `DeclaracionComparacion` (puro, por props): un selector server-driven (`?comparar=A,B`) + una shadcn `<Table>` con `<caption>` (la copy "solo datos, sin cálculo ni interpretación" + la atribución CC BY 4.0 repetida), `<th scope="col">` columnas fechadas (Presentada el {fecha}, mono), `<th scope="row">` etiquetas de campo, y celdas con valores literales. CERO columna de delta/variación/Δ/enriquecimiento/conflicto. Sin color de valencia, sin estilo diff. Un campo ausente en una versión → celda "No declarado en esta versión". `ProvenanceBadge` por columna. Si <2 versiones confirmadas → selector oculto + línea "Se necesita más de una versión para comparar. Hay {N} versión registrada."
    - Guarda de identidad (§3.4): SOLO versiones `confirmado` entran al perfil, al conteo y a la comparación; `probable`/`revision`/`no_confirmado` se excluyen por defecto (si se muestran, en un área "Menciones sin verificar" con mención cruda + `<IdentityMarker />`, nunca comparadas).
    - Tres estados honestos (§6.1): (a) `noIngestado` → "Aún no hemos ingerido las declaraciones…"; (b) ingestado pero 0 confirmadas → "No se registran declaraciones… confirmadas…"; (c) con versiones → el historial. Un vacío NUNCA lee como ausencia de patrimonio/virtud.
    - NINGÚN voto/lobby/proyecto/score/ranking/flag/chart/viz dentro de la sección (§3.6). Un conteo neutro ("3 versiones registradas") es lo único agregado permitido.
  </behavior>
  <action>
Espejar `lobby-de-parlamentario.tsx`:

1. **`PatrimonioView({ id, data, comparar })`** — componente PURO (props), RTL-testeable, sin runtime Supabase/Next. Renderiza intro + CC BY 4.0 + los tres estados + la `<ul>/<li>` de versiones (fecha mono prominente + frescura ámbar + caveat, tipo, `<dl>` de campos, ProvenanceBadge `ml-auto`). Disclosure de campos server-driven (`?ver=<versionId>`, `min-h-[44px]`). Paginación server-driven (`?patrimonioPage=N`, page size 10) si hay muchas versiones. NO `CamaraChip` por fila.

2. **`DeclaracionComparacion({ id, versiones, seleccion })`** — componente PURO: el selector (`?comparar=A,B`, anchors `min-h-[44px]`) + la `<Table>` shadcn (caption con framing "solo datos" + CC BY 4.0; columnas fechadas mono; filas por campo con valores literales; celda ausente = "No declarado en esta versión"). CERO columna computada/veredicto. Sin color de valencia. ProvenanceBadge por columna. Semántica de tabla real: `<th scope="col">`/`<th scope="row">` (a11y §8). SSR end-to-end (sin motor de diff cliente).

3. **`PatrimonioSection({ id, searchParams })`** — Server Component: `createServerSupabase()` → `sb.rpc("declaraciones_de_parlamentario", { p_id: id })` (devuelve table; patrón de array como votos/lobby). Si `?comparar=A,B` presente → `sb.rpc("comparar_declaraciones", { p_id: id, fechas: [A,B] })` para la grilla. Distinguir error real de DB/red (throw → UI de error honesta §6.3) de "0 filas" (→ estado honesto). Consultar `probidad_ingesta_estado` (public-read) para `noIngestado` REAL (ausencia de fila para `p_id` = no ingestado todavía). Agrupar/derivar `es_historica` desde la fecha vs el periodo legislativo actual (frescura). Pasar la forma a `PatrimonioView` + `DeclaracionComparacion`.
   - GATE DE CONTENIDO (§9.1) en el encabezado del archivo: documentar la lista dura de términos PROHIBIDOS (veredicto: "enriqueci"/"conflicto de inter"/"aument"/"disminuy"/"incrementó"/"variaci"/"delta"/"Δ"/"creció"/"pasó de"/"más rico"/"patrimonio total" como suma computada; causal: "para"/"a cambio de"/"antes de votar"; afinidad/connective prose: "en representación de"/"vinculado a"/"asociado con"). Nada de eso entra en código ni copy. Carril aislado: la sección NUNCA referencia un voto/lobby/proyecto. Cada campo renderizado DEBE estar en el contrato §10 (`campos[]`) con etiqueta NOUN — nunca prosa conectiva autorada (la lección `representado` de Phase 11).

4. **`patrimonio-de-parlamentario.test.tsx`** (RTL, sobre `PatrimonioView` + `DeclaracionComparacion` con fixtures):
   - Carril/estructura: la sección renderiza su propia lista; NINGÚN elemento contiene un voto/reunión-de-lobby/proyecto (assertion negativa sobre `textContent` + sin link a `/proyecto/`).
   - Fecha prominente + frescura: la fecha de presentación aparece labeled "Presentada el …"; una versión `es_historica` muestra el badge ámbar + el caveat; la más reciente NO se rotula "actual" (solo su fecha).
   - **Comparación SIN veredicto:** una comparación poblada multi-versión con un valor cambiado Y un campo ausente NO contiene ninguna columna/celda/caption con vocabulario de veredicto (regex de la regla 1: enriqueci/conflicto/aument/disminuy/variaci/delta/Δ/creció/pasó de/más rico/patrimonio total). El campo ausente lee "No declarado en esta versión", NO "—" ni un gap coloreado.
   - **CC BY 4.0 presente** en el intro Y en el caption de la comparación (assertion sobre el texto del caption).
   - **Sin RUT/familiar:** ningún patrón de RUT ni nombre de familiar aparece en el `textContent` de la lista NI de la comparación.
   - Guarda de identidad: una versión no-confirmada se excluye del conteo y de la comparación; si se muestra, con `IdentityMarker` y sin enlace.
   - Tres estados honestos distintos (a/b/c textualmente diferentes; un vacío ≠ "limpio").
   - ProvenanceBadge presente por versión Y por columna de comparación.
   - **Gate-test mandate (§9.1, cierra la brecha Phase 11):** el test ejerce TANTO la lista COMO la comparación; asserta (a) ningún campo fuera del contrato §10, (b) ninguna prosa conectiva verbo/preposición, (c) ningún vocabulario de veredicto/delta en celda/caption/label, (d) ningún RUT/familiar en `textContent`, (e) la atribución CC BY 4.0 presente en el caption de la comparación. Este test es el release gate de la fase.
  </action>
  <verify>
    <automated>pnpm --filter app test -t patrimonio</automated>
  </verify>
  <done>`PatrimonioView`/`DeclaracionComparacion`/`PatrimonioSection` existen; el historial muestra fecha prominente + frescura ámbar + caveat (vieja ≠ actual); la comparación es una `<Table>` sobria con CERO veredicto/delta + CC BY 4.0 en el caption + celda ausente literal; sin RUT/familiar; tres estados honestos distintos; el content-gate test ejerce lista Y comparación y pasa; `PatrimonioSection` computa `noIngestado` real desde `probidad_ingesta_estado`.</done>
</task>

<task type="auto">
  <name>Task 3: Apilar la sección de patrimonio en /parlamentario/[id] (carril propio, mt-12, Suspense)</name>
  <files>app/app/parlamentario/[id]/page.tsx</files>
  <action>
Editar `app/app/parlamentario/[id]/page.tsx` para apilar la sección de patrimonio DESPUÉS de `#lobby`, reemplazando el comentario placeholder existente (`<section id="patrimonio" …> <h2>Patrimonio e intereses</h2> …`):

- Añadir `<section id="patrimonio" className="mt-12"><h2 className="text-xl font-semibold mb-4">Declaraciones de patrimonio e intereses</h2><Suspense fallback={<PatrimonioSkeleton />}><PatrimonioSection id={id} searchParams={sp} /></Suspense></section>` como sibling de `#lobby` (NUNCA anidada en `#lobby` ni en `#votos`).
- Importar `PatrimonioSection` de `@/components/patrimonio-de-parlamentario`.
- Añadir `PatrimonioSkeleton` (shape-matched: línea intro + atribución + 3 filas de versión: barra de fecha prominente + tipo + bloque de campos + provenance) espejando `LobbySkeleton`, `aria-hidden`.
- NO tocar la cabecera, `#votos` ni `#lobby`. La jerarquía de headings sigue válida (`h1` cabecera; `h2` Votaciones + Reuniones de lobby + Declaraciones de patrimonio e intereses como siblings).
- El `mt-12` es la frontera de carril: las secciones son siblings con 48px de separación; una declaración y un voto/reunión NUNCA en el mismo `<article>`/`<Card>`/`<li>`/`<tr>` (§9.1 lane isolation).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/app/parlamentario/[id]/page.tsx','utf8'); const must=['id=\"patrimonio\"','PatrimonioSection','PatrimonioSkeleton','mt-12']; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('FALTAN:',miss);process.exit(1)} if(!/section id=\"lobby\"[\s\S]*section id=\"patrimonio\"/.test(s)){console.error('patrimonio debe ser sibling DESPUES de lobby, no anidado');process.exit(1)} console.log('page OK')" && pnpm --filter app build</automated>
  </verify>
  <done>`/parlamentario/[id]` renderiza `<section id="patrimonio">` como sibling de `#lobby` a `mt-12`, con su propio `<h2>` + Suspense + `PatrimonioSkeleton`; el build de la app pasa; la cabecera, `#votos` y `#lobby` quedan intactos.</done>
</task>

</tasks>

<verification>
- `pnpm --filter app test -t patrimonio` verde (incluido el content-gate sobre lista Y comparación).
- `pnpm --filter app build` verde.
- La sección es su propio carril (sibling, mt-12), nunca compone una declaración con un voto/reunión; la comparación no tiene veredicto/delta; CC BY 4.0 visible en intro Y caption.
</verification>

<success_criteria>
- INT-04: el ciudadano ve el historial de versiones de declaraciones del parlamentario con la fecha de presentación PROMINENTE + frescura ámbar; una declaración vieja nunca lee como estado actual; solo versiones confirmadas entran al perfil.
- INT-05: el ciudadano compara versiones lado-a-lado (shadcn Table) con SOLO datos literales, CERO veredicto de enriquecimiento/conflicto/delta; CC BY 4.0 visible también en la vista derivada; el gate de contenido §9.1 cubre la comparación (cierra la brecha `representado` de Phase 11).
- Tres estados honestos distintos; un vacío nunca lee como ausencia de patrimonio. Sin RUT/familiar en el DOM.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (anon) → RPC public-read | la sección lee solo campos publicados vía RPC; nunca RUT/familiar |
| copy/campos renderizados → ciudadano | superficie de mayor riesgo de insinuación (la comparación derivada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Tampering (product/legal risk) | `DeclaracionComparacion` (vista derivada) | mitigate | CERO columna de delta/veredicto; sin color de valencia ni estilo diff; el content-gate test ejerce la comparación con un valor cambiado + un campo ausente y asserta cero vocabulario de veredicto (regla 1) — release gate |
| T-12-13 | Tampering (integridad/misrepresentación) | declaración vieja como actual | mitigate | fecha de presentación prominente labeled "Presentada el…" + badge ámbar `es_historica` + caveat §6.4; la más reciente se rotula por su fecha, nunca "actual" |
| T-12-14 | Information disclosure | RUT/familiar al cliente | mitigate | el RPC no los proyecta; `DeclaracionVersionRow` no los incluye; el content-gate test asserta su ausencia en el `textContent` |
| T-12-15 | Spoofing | atribución de una declaración no-confirmada | mitigate | guarda de identidad §3.4: solo `confirmado` entra al perfil/conteo/comparación; `IdentityMarker` para menciones sin verificar |
| T-12-16 | Tampering (insinuación) | prosa conectiva fuera de contrato (lección `representado`) | mitigate | cada campo en el contrato §10 con etiqueta NOUN; el gate-test asserta que ningún campo renderiza fuera del contrato ni con verbo/preposición conectiva |
| T-12-SC | Tampering | shadcn `Table` (registro) | accept | registro oficial shadcn; sin terceros (UI-SPEC §11); no aplica vetting |
</threat_model>

<output>
Crear `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-03-SUMMARY.md` al terminar.
</output>
