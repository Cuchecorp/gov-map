---
phase: 12-int-patrimonio-intereses-declaraciones-con-historial-y-compa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/probidad/package.json
  - packages/probidad/tsconfig.json
  - packages/probidad/vitest.config.ts
  - packages/probidad/src/model.ts
  - packages/probidad/src/sparql.ts
  - packages/probidad/src/parse-infoprobidad.ts
  - packages/probidad/src/parse-infoprobidad.test.ts
  - packages/probidad/src/connector-infoprobidad.ts
  - packages/probidad/src/reconciliar-declarante.ts
  - packages/probidad/src/reconciliar-declarante.test.ts
  - packages/probidad/src/writer.ts
  - packages/probidad/src/writer.test.ts
  - packages/probidad/src/writer-supabase.ts
  - packages/probidad/src/ingest-run.ts
  - packages/probidad/src/ingest-run.test.ts
  - packages/probidad/src/ingest-cli.ts
  - packages/probidad/src/live-infoprobidad.probe.ts
  - packages/probidad/src/index.ts
  - packages/probidad/test/fixtures/declaraciones-sparql.json
autonomous: false
requirements: [INT-03, INT-04]
user_setup: []

must_haves:
  truths:
    - "El parser mapea un fixture SPARQL-JSON real de InfoProbidad → Declaracion[] LITERALMENTE (zod, SIN LLM); fecha_presentacion = ip:fechaDeclaracion"
    - "Un declarante con ≥2 fechas distintas produce ≥2 versiones; cada versión lleva licencia CC BY 4.0 por fila"
    - "El declarante cruza name-only vía correrPipeline; SOLO determinista mintea un EnlaceConfirmado y puebla el FK; el resto deja NULL + mención cruda; los familiares pasan deny-by-default sin reconciliación"
    - "El writer es VERSIONADO: dos declaraciones con distinta fecha_presentacion → dos filas; re-run con el mismo input → conteos idénticos (nunca sobreescribe una versión)"
    - "Un drift estructural de la respuesta SPARQL BLOQUEA la corrida (cuarentena; no emite filas vacías que se lean como 'no declara')"
    - "Ningún @obs/llm se importa en la ruta de ingesta (contenido estructurado → sin LLM)"
    - "Una corrida LIVE acotada o degrada a fixture; nunca fabrica filas"
  artifacts:
    - path: "packages/probidad/src/parse-infoprobidad.ts"
      provides: "Parser zod del SPARQL-JSON de InfoProbidad → Declaracion[] versionadas (LITERAL, sin LLM)"
      min_lines: 40
    - path: "packages/probidad/src/reconciliar-declarante.ts"
      provides: "Cruce name-only del declarante vía correrPipeline; mint de EnlaceConfirmado solo en determinista; familiares deny-by-default"
      contains: "correrPipeline"
    - path: "packages/probidad/src/writer-supabase.ts"
      provides: "SupabaseProbidadWriter VERSIONADO por (fuente_id, fecha_presentacion); escribe declaracion, sub-tablas de bienes, declaracion_familiar, probidad_ingesta_estado"
      contains: "fecha_presentacion"
  key_links:
    - from: "packages/probidad/src/reconciliar-declarante.ts"
      to: "@obs/identity confirmar"
      via: "import + mint en rama determinista"
      pattern: "confirmar\\("
    - from: "packages/probidad/src/connector-infoprobidad.ts"
      to: "@obs/ingest assertAllowedUrl + robots + rateLimiter + fetcher"
      via: "orden LOCKED"
      pattern: "assertAllowedUrl"
    - from: "packages/probidad/src/writer-supabase.ts"
      to: "declaracion / declaracion_familiar / probidad_ingesta_estado"
      via: "upsert onConflict por clave de versión"
      pattern: "fecha_presentacion"
---

<objective>
Construir el paquete `@obs/probidad`: conector SPARQL + parser zod + reconciliación del declarante + writer VERSIONADO + orquestación, espejando `@obs/lobby` casi archivo-por-archivo. Ingesta las declaraciones de patrimonio e intereses desde `datos.cplt.cl/sparql` (contenido ESTRUCTURADO → parseo literal con zod, SIN LLM), cruza el declarante name-only contra la maestra vía `correrPipeline` (FK solo-determinista vía `EnlaceConfirmado`; la fuente NO tiene RUT), persiste cada versión a las tablas de Plan 01 (clave incluye `fecha_presentacion` → nunca sobreescribe), con drift BLOQUEANTE.

Purpose: Es la pieza de ingesta de INT-03 + el modelo versionado de INT-04. Toda la política (rate-limit, robots, SSRF, R2, drift) se reusa de `@obs/ingest` en el ORDEN LOCKED; la guarda de identidad se reusa de Phase 9. El único código genuinamente nuevo es los query builders SPARQL + el parser JSON→rows de UNA forma estructurada, la reconciliación del declarante (name-only), y el writer versionado. NO se usa LLM (el contenido es RDF estructurado; un LLM agregaría costo + riesgo de PII a entrenamiento para CERO beneficio).
Output: el paquete `packages/probidad` + fixture SPARQL-JSON + tests vitest. La corrida LIVE acotada + escritura a Supabase es checkpoint de operador (autonomous:false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-RESEARCH.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-VALIDATION.md

<read_first>
- `packages/lobby/src/connector-leylobby.ts` — el patrón de conector a espejar: orden LOCKED (`assertAllowedUrl → robots.isAllowed → rateLimiter.wait → fetcher.get`), header-set identificatorio (`Bot-Ciudadano/1.0`), errores tipados (`LeylobbyBloqueadaError` para 403/503), NUNCA `BaseConnector.run`. El conector de probidad fetchea SPARQL GET (`${BASE}/sparql?query=${encodeURIComponent(q)}`, `Accept: application/sparql-results+json`) en vez de HTML.
- `packages/lobby/src/model.ts` — la forma del modelo a espejar: entidad raíz con hijos ANIDADOS, `ProvenanceInline` zod, schemas zod por entidad. `Declaracion` lleva sus bienes + familiares anidados; el writer aplana.
- `packages/lobby/src/parse-leylobby.ts` — el patrón de parser (valida cada fila con zod, descarta+registra lo que no valida; nunca fabrica). Probidad parsea `results.bindings` del SPARQL-JSON con `JSON.parse` nativo + zod (NO una lib RDF).
- `packages/lobby/src/reconciliar-sujeto.ts` — el choke point de reconciliación a espejar EXACTO: `normalizarNombre({ libre })` → `MencionForanea` → `correrPipeline(mencion, maestra, provider, writer)` → `switch(res.tipo)`; SOLO `determinista` llama `confirmar(id, "determinista")` y puebla el FK; el resto → `enlace: null` + mención cruda. Provider/writer inyectables con defaults seguros (NOOP_WRITER, PROVIDER_AUSENTE que lanza). El tipo del provider se deriva de la firma de `correrPipeline` (NO import directo a `@obs/llm`). Los familiares pasan SIN reconciliación (espejo de las contrapartes: filas crudas, sin enlace).
- `packages/lobby/src/writer-supabase.ts` — el writer idempotente: `createClient` con SERVICE key, `dedupePorClave` + `chunk` + `upsert(filas, { onConflict })`, raíz ANTES que hijos, dedupe-before-batch, storage plano (FK branded → `string|null`). Probidad espeja esto PERO el `onConflict` de `declaracion` incluye `fecha_presentacion` (`'fuente_id,fecha_presentacion'`) → las versiones acumulan; un re-run upserta la MISMA versión, una nueva fecha es una fila nueva. NUNCA sobreescribe ni borra una versión vieja.
- `packages/lobby/src/writer.ts` — la interfaz `LobbyWriter` + `InMemoryLobbyWriter` (espejar como `ProbidadWriter` + `InMemoryProbidadWriter`).
- `packages/lobby/src/ingest-run.ts` — la orquestación con DRIFT BLOQUEANTE (cuarentena ante drift estructural; 0 filas + Degradacion, nunca filas vacías) y degradación honesta (institución/declarante inalcanzable degrada, no aborta, nunca fabrica).
- `packages/lobby/src/ingest-cli.ts` — el entry-point que arma colaboradores REALES de `@obs/ingest` + el writer Supabase, service key SOLO de env, degrada a dry-run/fixture sin key.
- `packages/lobby/src/live-leylobby.probe.ts` — el probe acotado (gated) que hace 1 request real y verifica la forma.
- `packages/ingest/src/allowlist.ts` — confirmar que `cplt.cl` (cubre `datos.cplt.cl`) e `infoprobidad.cl` ya están en `DEFAULT_ALLOWED_SUFFIXES` (research: líneas 25-26). NO requiere edición.
- `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-RESEARCH.md` §"InfoProbidad Access" (endpoint SPARQL, predicados de `Declaracion`/`Persona`, sub-clases de bienes, versioning nativo, NO RUT), §"Recommended Project Structure" (los archivos a crear), §"Code Examples" (query SPARQL por nombre + reconciliación), §"Common Pitfalls 1-6", §"Validation Architecture" (Wave 0: fixture SPARQL-JSON + golden del parser).
- `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-01-PLAN.md` — las columnas exactas de las tablas/sub-tablas que este writer puebla (Plan 01 resuelve OQ2: las props literales de cada sub-clase de bien). Leer la migración resultante de Plan 01 si está disponible.
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold del paquete + modelo + query builders SPARQL + parser zod (con fixture SPARQL-JSON)</name>
  <files>packages/probidad/package.json, packages/probidad/tsconfig.json, packages/probidad/vitest.config.ts, packages/probidad/src/model.ts, packages/probidad/src/sparql.ts, packages/probidad/src/parse-infoprobidad.ts, packages/probidad/src/parse-infoprobidad.test.ts, packages/probidad/test/fixtures/declaraciones-sparql.json, packages/probidad/src/index.ts</files>
  <behavior>
    - Parsea el fixture SPARQL-JSON de InfoProbidad → `Declaracion[]` LITERALMENTE (zod), cada una keyed por `(fuenteId, fechaPresentacion)` donde `fechaPresentacion = ip:fechaDeclaracion`.
    - Un declarante con ≥2 fechas distintas produce ≥2 versiones de `Declaracion` (versioning nativo — INT-04). Las versiones NO se colapsan.
    - Cada `Declaracion` lleva: `fuenteId`, `fechaPresentacion`, `tipo` (label resuelto o URI cruda), `cargo`, `organismo`, `declaranteNombre` (crudo, con `\t`+dobles-espacios que `normalizarNombre` limpiará después), `bienes[]` (sub-entidades literales por sub-clase), `familiares[]` ({relacion, nombre} crudos), y provenance + `licencia: "CC BY 4.0"`.
    - Cada fila se valida con zod (`DeclaracionSchema`); una fila que no valida se descarta y se registra (drift), nunca se fabrica.
    - El parser es LITERAL: ningún campo se computa, normaliza-con-juicio, ni redondea; un monto se guarda tal como la fuente lo da (string/categoría/rango verbatim). NINGÚN import de `@obs/llm`.
  </behavior>
  <action>
Scaffold `packages/probidad` espejando `packages/lobby` (mismo patrón `package.json` name `@obs/probidad`, `tsconfig.json`, `vitest.config.ts`).

1. **Capturar el fixture SPARQL-JSON real** (Wave 0): consultar `https://datos.cplt.cl/sparql` por UN declarante con ≥2 declaraciones fechadas (la query por nombre de research §"Code Examples"), respetando el delay 2-3s + UA identificatorio. Guardar la respuesta cruda en `packages/probidad/test/fixtures/declaraciones-sparql.json`. Esto fija el golden del parser Y prueba el versioning (≥2 fechas). Capturar también una instancia por sub-clase de bien (OQ2 — coordinar con lo resuelto en Plan 01 Task 1). Si el endpoint NO es alcanzable, capturar de la mejor fuente disponible y marcar el fixture `human_verification` pendiente en un comentario — NUNCA inventar JSON.

2. **`model.ts`**: tipos + zod schemas. `Declaracion` (raíz, con `bienes`/`familiares` anidados; `parlamentarioId: EnlaceConfirmado-derived | null` en la forma para-escribir, `mencionDeclarante`, `fuenteId`, `fechaPresentacion`, `tipo`, `cargo`, `organismo`, provenance, `licencia`). Sub-schemas de bien por sub-clase (`BienInmueble`/`BienMueble`/`Actividad`/`Pasivo`/`AccionDerecho`/`Valor`) con las props literales pineadas. `DeclaracionFamiliar` ({relacion, nombre} crudos). `ProvenanceInline` zod (espejo de lobby). NO existe campo RUT en ningún schema.

3. **`sparql.ts`**: query builders puros (`queryDeclaracionesPorNombre(apellidoNormalizado)`, `queryTipoLabels()`, etc.) que construyen el SPARQL desde un nombre normalizado escapado/encodeado (sin input crudo de usuario → sin inyección SPARQL). Más `bindingsToRows(sparqlJson)` puro que mapea `results.bindings` a filas planas con `JSON.parse` nativo (NO una lib RDF).

4. **`parse-infoprobidad.ts`**: `parseDeclaraciones(sparqlJson)` que usa `bindingsToRows` + agrupa por `(fuenteId, fechaPresentacion)` → `Declaracion[]` versionadas, valida con zod, descarta+registra lo no-válido. `ORIGEN = "infoprobidad-sparql"`, `licencia = "CC BY 4.0"`.

5. **`parse-infoprobidad.test.ts`**: tests vitest contra el fixture — (a) keying por `(fuenteId, fechaPresentacion)`; (b) un declarante con ≥2 fechas → ≥2 versiones (versioning); (c) cada versión lleva `licencia: "CC BY 4.0"`; (d) los familiares se extraen anidados; (e) una fila malformada se descarta sin fabricar; (f) NO se importa `@obs/llm` (assertion: el árbol de imports del parser/connector no contiene `@obs/llm`).
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test -t parse-infoprobidad</automated>
  </verify>
  <done>El paquete `@obs/probidad` compila; el fixture SPARQL-JSON existe (con ≥2 fechas); `parse-infoprobidad` mapea el fixture a `Declaracion[]` versionadas keyed por `(fuenteId, fechaPresentacion)`; cada versión lleva CC BY 4.0; los tests de keying/versioning/licencia/familiares/descarte/no-LLM pasan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliación del declarante (name-only) + writer VERSIONADO + interfaz ProbidadWriter</name>
  <files>packages/probidad/src/reconciliar-declarante.ts, packages/probidad/src/reconciliar-declarante.test.ts, packages/probidad/src/writer.ts, packages/probidad/src/writer.test.ts, packages/probidad/src/writer-supabase.ts</files>
  <behavior>
    - `reconciliarDeclarante`: para cada declaración, el declarante cruza name-only vía `correrPipeline` (la fuente NO tiene RUT → cruce por nombre, igual que el sujeto-pasivo de lobby); `determinista` → `confirmar(id, "determinista")` puebla el FK + `estado_vinculo = "confirmado"`; `probable`/`revision`/`no_confirmado` → `enlace: null` + `estado_vinculo: "no_confirmado"` + mención cruda. Cada decisión deja una fila en `identidad_audit`.
    - `normalizarNombre` se aplica al nombre crudo (los nombres traen `\t` + dobles-espacios → DEBE normalizarse antes del cruce).
    - Los familiares pasan SIN reconciliación: filas crudas `{relacion, nombre}`, NUNCA enlazadas a una persona (deny-by-default; espejo de las contrapartes de lobby).
    - `SupabaseProbidadWriter`: upsert VERSIONADO — `declaracion` onConflict `'fuente_id,fecha_presentacion'` (las versiones ACUMULAN); sub-tablas de bienes onConflict por su clave de versión; `declaracion_familiar` insertada a la tabla deny-by-default; dedupe-before-batch; raíz antes que hijos. Dos corridas con el mismo input → conteos idénticos. Dos declaraciones con distinta fecha → dos filas (NUNCA se colapsan, NUNCA se sobreescribe una versión vieja).
    - El writer también upserta `probidad_ingesta_estado` (un row por parlamentario confirmado tocado) para el marcador "no ingestado" de la ficha.
  </behavior>
  <action>
Espejar `reconciliar-sujeto.ts` y `writer-supabase.ts`/`writer.ts` de lobby:

1. **`reconciliar-declarante.ts`**: `reconciliarDeclarante(declaraciones, maestra, opts)` con `opts: { provider?, writer?, periodo? }` y defaults seguros (NOOP_WRITER, PROVIDER_AUSENTE que lanza si un homónimo lo invoca; los deterministas nunca lo tocan; un homónimo sin provider real degrada a `no_confirmado` fail-closed, no aborta la corrida). Por declaración: `normalizarNombre({ libre: decl.declaranteNombre.trim() })` → `MencionForanea` → `correrPipeline` → `switch(res.tipo)`. SOLO `determinista` llama `confirmar(res.parlamentarioId, "determinista")`. Los familiares se mapean crudos (sin enlace). Devuelve la forma para-escribir (declaración + bienes + familiares anidados) + el set de `parlamentarioId` confirmados (para el marcador). NO importar `@obs/llm` directo — derivar el tipo del provider de la firma de `correrPipeline`.

2. **`writer.ts`**: interfaz `ProbidadWriter` (`upsertDeclaraciones(filas)`, `marcarIngestado(parlamentarioIds, hasta)`) + `InMemoryProbidadWriter` para tests (cuenta filas por clave de versión, permite assertions de idempotencia + acumulación de versiones).

3. **`writer-supabase.ts`**: `SupabaseProbidadWriter implements ProbidadWriter`. `createClient` con la SERVICE key (bypassa RLS; server-side). `upsertDeclaraciones`: aplana — upsert `declaracion` por `(fuente_id, fecha_presentacion)` (dedupe por esa clave compuesta), luego las sub-tablas de bienes por su clave de versión, luego `declaracion_familiar` (insert/upsert a la tabla deny-by-default). El FK persiste plano (`parlamentario_id = enlace?.parlamentarioId ?? null` — input branded, storage plano). `marcarIngestado`: upsert `probidad_ingesta_estado` por `parlamentario_id`. La service key NUNCA se interpola en mensajes de error. **CRÍTICO: el `onConflict` de `declaracion` incluye `fecha_presentacion` — NUNCA keyear por el declarante solo (Pitfall 1: colapsa versiones).**

4. **`reconciliar-declarante.test.ts`**: (a) determinista → FK poblado + estado confirmado + audit; (b) probable/revision/no_confirmado → FK null + mención cruda; (c) familiares siempre crudos sin enlace; (d) sin provider, un homónimo degrada a no_confirmado (fail-closed) y un determinista resuelve igual; (e) `normalizarNombre` limpia el `\t`+dobles-espacios antes del cruce.

5. **`writer.test.ts`**: (a) idempotencia — 2× el mismo input al `InMemoryProbidadWriter` → conteos idénticos; (b) **versioning — dos declaraciones del MISMO declarante con distinta `fechaPresentacion` → DOS filas (no se colapsan); re-run de una versión existente NO crea duplicado NI sobreescribe** (la assertion clave de INT-04); (c) dedupe-before-batch.
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test -t "reconciliar-declarante" && pnpm --filter @obs/probidad test -t writer</automated>
  </verify>
  <done>`reconciliarDeclarante` cruza name-only y mintea `EnlaceConfirmado` solo en determinista (un string crudo como FK no compila); los familiares son crudos sin enlace; `SupabaseProbidadWriter` es VERSIONADO por `(fuente_id, fecha_presentacion)` (acumula versiones, nunca sobreescribe) e idempotente, y upserta el marcador de ingesta; los tests pasan, incluido el de versioning.</done>
</task>

<task type="auto">
  <name>Task 3: Conector SPARQL + orquestación (ingest-run con drift BLOQUEANTE) + CLI + probe LIVE + barril</name>
  <files>packages/probidad/src/connector-infoprobidad.ts, packages/probidad/src/ingest-run.ts, packages/probidad/src/ingest-run.test.ts, packages/probidad/src/ingest-cli.ts, packages/probidad/src/live-infoprobidad.probe.ts, packages/probidad/src/index.ts</files>
  <action>
Espejar `connector-leylobby.ts` + `ingest-run.ts` + `ingest-cli.ts` + `live-leylobby.probe.ts` de lobby:

1. **`connector-infoprobidad.ts`**: `InfoProbidadConnector` con deps inyectables (`fetcher`, `rateLimiter`, `robots`, `allowlist`). Método `fetch(url)` en el ORDEN LOCKED (`assertAllowedUrl → robots.isAllowed → rateLimiter.wait → fetcher.get` con header-set identificatorio + `Accept: application/sparql-results+json`). `urlSparql(query)` → `${BASE}/sparql?query=${encodeURIComponent(query)}` (BASE = `https://datos.cplt.cl`). Error tipado `InfoProbidadBloqueadaError` para 403/503/timeout. Comentar: `cplt.cl` ya allowlisted; CSV bulk es backfill-only (timeout en fetch acotado → ruta de GitHub Actions, NO Edge). NUNCA `BaseConnector.run`. NINGÚN import de `@obs/llm`.

2. **`ingest-run.ts`**: orquesta `for each declarante (por lote/nombre)`: build query (sparql.ts) → fetch (orden LOCKED) → R2 raw (reusar el writer R2 de @obs/ingest si el connector lo expone, como lobby) → **DriftDetector en modo BLOQUEANTE**: si la forma del SPARQL-JSON cambió (drift estructural — p.ej. CPLT evoluciona la ontología), CUARENTENA la corrida — emite 0 filas + registra `Degradacion`, NUNCA filas vacías/garbage que se lean como "no declara" (Pitfall 5 / C4). → `parseDeclaraciones` → `reconciliarDeclarante` → `writer.upsertDeclaraciones` → `writer.marcarIngestado`. Un declarante inalcanzable degrada honestamente, nunca fabrica. Devuelve un reporte (`declaraciones` escritas, versiones, bienes, familiares, errores por clave, degradaciones, drift-quarantine flag).

3. **`ingest-cli.ts`**: entry-point que arma colaboradores REALES + `SupabaseProbidadWriter`, service key SOLO de env, degrada a dry-run sin key. **Corrida LIVE ACOTADA**: limita a un puñado de declarantes (parlamentarios vigentes) por nombre por defecto — flags para ampliar. Respeta el delay 2-3s. Si InfoProbidad no es alcanzable → degrada a fixture + marca `human_verification`, nunca fabrica.

4. **`live-infoprobidad.probe.ts`**: probe acotado (gated, mirror de `live-leylobby.probe.ts`) que hace 1 request SPARQL real y verifica que el JSON trae `results.bindings` con `fechaDeclaracion` y la estructura esperada.

5. **`ingest-run.test.ts`**: (a) drift estructural → cuarentena (0 filas + degradación, NUNCA filas); (b) declarante inalcanzable → degradación honesta sin abortar; (c) corrida idempotente; (d) versioning end-to-end (dos fechas → dos versiones persistidas); (e) nunca fabrica (fuente vacía → 0 filas, no inventa).

6. **`index.ts`**: barril que re-exporta los símbolos públicos.

Registrar el paquete en el workspace (`pnpm install` tras el scaffold).
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test -t ingest-run && pnpm --filter @obs/probidad test && pnpm --filter @obs/probidad typecheck</automated>
  </verify>
  <done>`@obs/probidad` corre la ingesta SPARQL en el orden LOCKED con drift BLOQUEANTE (cuarentena ante drift estructural), degrada honestamente declarantes inalcanzables, es idempotente y versionada y nunca fabrica filas; el CLI acota la corrida LIVE y degrada a fixture sin key/alcance; ningún `@obs/llm` en la ruta; toda la suite del paquete + typecheck pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>El conector `@obs/probidad` completo (query builders SPARQL, parser zod, reconciliación name-only, writer versionado, orquestación con drift bloqueante) con suite offline verde y SIN LLM. La corrida LIVE real contra `datos.cplt.cl/sparql` + escritura a Supabase NO se ejecutó autónomamente (endpoint gov rate-limited, no-determinista; requiere el schema 0022 aplicado por Plan 01).</what-built>
  <how-to-verify>
Operador, tras confirmar que 0022 está aplicada (Plan 01 checkpoint):
1. Correr la corrida LIVE ACOTADA del CLI (`ingest-cli`) contra un puñado de parlamentarios vigentes por nombre, con el delay 2-3s y la service key en env.
2. Verificar: provenance + `licencia` CC BY 4.0 por fila; clave de versión = `(fuente_id, fecha_presentacion)`; un declarante con ≥2 fechas produjo ≥2 versiones; el declarante cruzó name-only vía `correrPipeline` (FK solo en determinista, `identidad_audit` con una fila por decisión); los familiares quedaron en `declaracion_familiar` (deny-by-default, sin enlace, sin RUT); `probidad_ingesta_estado` marcó al/los parlamentario(s) confirmado(s).
3. Re-correr → conteos idénticos (idempotencia); una versión vieja NO se sobreescribió.
4. Si InfoProbidad NO es alcanzable → degradar a fixture + `human_verification`, NUNCA fabricar declaraciones.
  </how-to-verify>
  <resume-signal>Escribe "corrida LIVE acotada OK" (o "degradado a fixture") o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- Suite offline `@obs/probidad` verde + typecheck verde.
- Parser literal (zod, sin LLM) keyed por `(fuenteId, fechaPresentacion)`; reconciliación name-only solo-determinista; writer VERSIONADO (acumula, nunca sobreescribe); drift bloqueante; nunca fabrica; CC BY 4.0 por fila.
- (Operador) corrida LIVE acotada escribe versiones a las tablas de Plan 01 con provenance/licencia, FK confirmado y familiares deny-by-default; o degrada honesto a fixture.
</verification>

<success_criteria>
- INT-03: `@obs/probidad` ingesta declaraciones de InfoProbidad por SPARQL+zod (LITERAL, SIN LLM) con `fecha_presentacion` + atribución CC BY 4.0 por fila, reusando `@obs/ingest` en el orden LOCKED, idempotente y sin fabricar.
- INT-04: el writer es versionado por `(fuente_id, fecha_presentacion)` (las versiones acumulan, nunca se sobreescriben); el declarante se enlaza solo en determinista/confirmado (vía `EnlaceConfirmado` + `identidad_audit`), el resto deja mención cruda; los familiares son deny-by-default sin enlace.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector → `datos.cplt.cl/sparql` (fuente gov) | server-only; fetch validado por allowlist (SSRF) |
| query builder → endpoint SPARQL | el nombre normalizado se escapa/encodea (sin inyección SPARQL) |
| writer (service role) → Postgres | bypassa RLS; escribe la versión + familiares deny-by-default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | SSRF / Info-disclosure | `connector-infoprobidad.fetch` | mitigate | `assertAllowedUrl` deny-by-default (`cplt.cl` allowlisted); nunca un host de usuario |
| T-12-07 | Tampering | query SPARQL | mitigate | la query se construye desde un nombre normalizado escapado/encodeado; sin input crudo de usuario en el string |
| T-12-08 | Spoofing/Repudiation | FK declarante (atribución falsa) | mitigate | `EnlaceConfirmado` tipado; FK solo en determinista; `identidad_audit` por decisión; cruce name-only con golden ≥0.95 (extender golden con formas de nombre InfoProbidad) |
| T-12-09 | Tampering (integridad) | scrape SPARQL parcial/vacío en fuente PII | mitigate | drift BLOQUEANTE (cuarentena); parser pineado a fixture golden; nunca fabrica; degradación honesta |
| T-12-10 | Tampering (integridad) | colapso de versiones | mitigate | writer onConflict incluye `fecha_presentacion`; nunca sobreescribe/borra una versión vieja |
| T-12-11 | Info-disclosure | PII de familiares al LLM | mitigate | NO se usa LLM en la fase (contenido estructurado); assertion de que no se importa `@obs/llm`; si un futuro campo libre lo requiriera, ruta obligatoria por `assertPiiDocumentSafeForLlm` |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase 12 NO instala paquetes externos (research §"Package Legitimacy Audit"). No aplica checkpoint de legitimidad |
</threat_model>

<output>
Crear `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-02-SUMMARY.md` al terminar.
</output>
