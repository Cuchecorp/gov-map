---
phase: 12-int-patrimonio-intereses-declaraciones-con-historial-y-compa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0022_probidad.sql
  - supabase/tests/0022_probidad.test.sql
autonomous: false
requirements: [INT-03, INT-04, INT-05]
user_setup: []

must_haves:
  truths:
    - "El conector @obs/probidad tiene un destino de escritura VERSIONADO: declaracion (hecho público) keyed por (fuente_id, fecha_presentacion) → las versiones se ACUMULAN, nunca se sobreescriben"
    - "anon puede leer el hecho de la declaración (declaracion + sub-tablas de bienes) pero NUNCA la tabla declaracion_familiar (terceros) directamente"
    - "La ficha lee solo declaraciones confirmadas vía un RPC security-definer que proyecta exactamente los campos que la fuente publica + la licencia CC BY 4.0, ordenadas por fecha_presentacion DESC"
    - "La comparación lee versiones lado-a-lado vía RPC que devuelve SOLO valores literales — CERO columna de delta/veredicto/enriquecimiento"
    - "La ficha puede distinguir 'no ingestado todavía' de 'ingestado, cero confirmadas' por un marcador por-parlamentario"
  artifacts:
    - path: "supabase/migrations/0022_probidad.sql"
      provides: "Tablas declaracion (public-read VERSIONADA) + sub-tablas de bienes (public-read) + declaracion_familiar (deny-by-default + revoke) + RPCs declaraciones_de_parlamentario / comparar_declaraciones + marcador probidad_ingesta_estado"
      contains: "create table declaracion"
    - path: "supabase/tests/0022_probidad.test.sql"
      provides: "pgTAP: declaracion_familiar anon-denegada (cero policies + revoke), declaracion public-read, version key incluye fecha_presentacion, provenance + licencia NOT NULL, RPCs sin columna de delta"
      contains: "declaracion_familiar"
  key_links:
    - from: "app Server Component (Plan 03)"
      to: "public.declaraciones_de_parlamentario(p_id)"
      via: "sb.rpc"
      pattern: "declaraciones_de_parlamentario"
    - from: "declaracion_familiar.fuente_id,fecha_presentacion"
      to: "declaracion(fuente_id, fecha_presentacion)"
      via: "FK references on delete cascade"
      pattern: "references public.declaracion"
---

<objective>
Crear el destino de datos VERSIONADO de la fase: la migración 0022 con la tabla pública `declaracion` (keyed por `(fuente_id, fecha_presentacion)` — versiones que ACUMULAN, nunca se sobreescriben), las sub-tablas de bienes públicas, la tabla deny-by-default `declaracion_familiar` (terceros PII) con el `revoke` explícito de anon (lección Phase 11), el RPC público `declaraciones_de_parlamentario` (historial), el RPC `comparar_declaraciones` (lado-a-lado, SIN delta), y un marcador de ingesta por-parlamentario; más su prueba pgTAP. Esta es la base sobre la que escribe el conector (Plan 02) y lee la ficha (Plan 03).

Purpose: Sin tablas no hay dónde escribir ni leer. La RLS deny-by-default de `declaracion_familiar` (espejo de 0018/0021 INCLUYENDO el `revoke all from anon, authenticated`) y la clave de versión que incluye `fecha_presentacion` son el piso PII/versionado de INT-03/04/05. La fuente NO tiene RUT (verificado en research) → no se almacena RUT de tercero; minimización Ley 21.719 satisfecha por la fuente misma.
Output: `supabase/migrations/0022_probidad.sql` + `supabase/tests/0022_probidad.test.sql`. La APLICACIÓN del DDL + pgTAP es un checkpoint de operador (build/typecheck NO prueban que Postgres ejecutó el DDL — Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-RESEARCH.md
@.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-VALIDATION.md

<read_first>
- `supabase/migrations/0021_lobby.sql` — el espejo MÁS cercano: `lobby_audiencia` (public-read) + `lobby_contraparte` (deny-by-default + `revoke all from anon, authenticated`) + RPC `lobby_de_parlamentario` (security definer `set search_path = ''`, `revoke execute from public` + `grant execute to anon`) + marcador `lobby_ingesta_estado`. La migración 0022 lo espeja casi tabla-por-tabla, pero VERSIONADA.
- `supabase/migrations/0018_piso_pii.sql` — la convención deny-by-default a copiar VERBATIM para `declaracion_familiar` (RLS enable, CERO policies, provenance NOT NULL inline). Es la plantilla viva de toda PII nueva.
- `supabase/migrations/0010_agenda.sql` — el patrón de tabla public-read (`enable row level security` + `create policy ... for select to anon using (true)` + `grant select to anon`). `declaracion` + sub-tablas de bienes lo espejan.
- `supabase/tests/0021_lobby.test.sql` — la plantilla pgTAP a espejar: existencia + RLS de las tablas, deny-by-default (cero policies + SIN grant SELECT a anon vía `information_schema.role_table_grants`), public-read (policy + grant), provenance NOT NULL, FK, RPC security definer + grant execute anon, `plan(N)`/`finish()`.
- `supabase/tests/0018_piso_pii.test.sql` — la assertion de deny-by-default sobre una tabla PII (espejo para `declaracion_familiar`).
- `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-RESEARCH.md` §"Data Model (migración ≥0022)" (DDL propuesto: `declaracion`, `declaracion_familiar`, keying summary, los RPCs `declaraciones_de_parlamentario`/`comparar_declaraciones`/`probidad_ingesta_estado`), §"Open Questions 1-3" (clave de versión, props de sub-entidades de bienes, labels de tipoDeclaracion), §"Comparison View (INT-05)" (CERO veredicto), §"Common Pitfalls 1-6".
- `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-UI-SPEC.md` §10 (contrato de payload: qué campos devuelve cada RPC; RUT/familiar NUNCA en la proyección).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver Open Questions 1-3 vía SPARQL (clave de versión, props de bienes, labels de tipo) y autorar la migración 0022_probidad.sql</name>
  <files>supabase/migrations/0022_probidad.sql</files>
  <action>
PRIMERO resolver las 3 open questions de research vía SPARQL en vivo contra `https://datos.cplt.cl/sparql` (HTTP 200 confirmado 2026-06-19; respetar el delay 2-3s + UA identificatorio; el endpoint responde GET con `?query=` URL-encoded, `Accept: application/sparql-results+json`). Documentar cada respuesta en un comentario del SQL. Si el endpoint NO es alcanzable en el entorno, usar el fallback documentado de research y marcarlo en un comentario — NUNCA fabricar un valor:

- **OQ1 (clave de versión, per Assumption A1):** una consulta que confirme si `ip:identificadorFuente` es único por versión. Si NO lo es, keyear por la URI del nodo `Declaracion` + `ip:fechaDeclaracion`. Default documentado: keyear por la URI de declaración + `fecha_presentacion` si `identificadorFuente` colisiona.
- **OQ2 (props de sub-entidades de bienes, per Assumption A3):** enumerar los predicados literales de UNA instancia de `BienInmueble`, `BienMueble`, `Actividad`, `Pasivo`, `AccionDerecho`, `Valores` (vía `ip:tieneBien`/`ip:tieneActividad`/`ip:tienePasivo`/`ip:tieneAccionDerecho`). Pinear las columnas de las sub-tablas a esos predicados LITERALES (sin valores computados).
- **OQ3 (labels de tipoDeclaracion, per Assumption A6):** `?tipo rdfs:label ?l` para resolver `tipoDeclaracion_1..5`. Si no hay label, almacenar la URI cruda verbatim — NUNCA fabricar un label.

Luego crear la migración con los objetos siguientes, siguiendo las convenciones de 0010/0018/0021 (per INT-03/04/05 y CONTEXT decisiones LOCKED):

1. **`declaracion`** (HECHO PÚBLICO, public-read, VERSIONADA — espejo de `lobby_audiencia` pero con clave compuesta):
   - **PK compuesta `(fuente_id, fecha_presentacion)`** — `fuente_id text not null` (`ip:identificadorFuente` o la URI de declaración según OQ1), `fecha_presentacion date not null` (`ip:fechaDeclaracion` — LA fecha de presentación; ancla de versión + frescura, INT-04). Comentar: la clave INCLUYE la fecha → las versiones ACUMULAN; un re-run upserta la MISMA versión (idempotente), una nueva `fechaDeclaracion` es una FILA NUEVA. NUNCA keyear por el declarante solo (Pitfall 1: colapsa versiones).
   - `parlamentario_id text references parlamentario(id) on delete set null` — NULLABLE. FK del declarante SOLO si enlace confirmado/determinista (IDENT-12, Phase 9).
   - `mencion_declarante text not null` — nombre crudo del declarante como lo publica la fuente.
   - `estado_vinculo text` — 'confirmado' | 'no_confirmado' | null.
   - `tipo text` (label resuelto en OQ3, o URI cruda), `cargo text` (`ip:poseeCargo`), `organismo text` (`ip:organismoFuente`).
   - Provenance inline NOT NULL (FND-08): `origen text not null` ("infoprobidad-sparql"), `fecha_captura timestamptz not null default now()`, `enlace text not null`.
   - **Atribución CC BY 4.0 que viaja con la fila (PITFALLS D4):** `licencia text not null default 'CC BY 4.0'`.
   - `alter table declaracion enable row level security;` + `create policy declaracion_public_read on declaracion for select to anon using (true);` + `grant select on declaracion to anon;`
   - `create index declaracion_parlamentario_idx on declaracion (parlamentario_id);`

2. **Sub-tablas de bienes** (`declaracion_bien_inmueble`, `declaracion_bien_mueble`, `declaracion_actividad`, `declaracion_pasivo`, `declaracion_accion_derecho`, `declaracion_valor`) — PÚBLICAS (son el contenido publicado de la declaración, CC BY 4.0). Cada una:
   - `id bigint generated always as identity primary key`.
   - `fuente_id text not null` + `fecha_presentacion date not null`, con `foreign key (fuente_id, fecha_presentacion) references declaracion(fuente_id, fecha_presentacion) on delete cascade`.
   - Columnas LITERALES de los predicados pineados en OQ2 (sin valores computados; un monto se guarda tal como la fuente lo da — string/categoría/rango verbatim).
   - Provenance inline NOT NULL + `licencia text not null default 'CC BY 4.0'`.
   - `unique (fuente_id, fecha_presentacion, <clave natural de la fila del bien>)` para upsert idempotente.
   - public-read: `enable row level security` + policy SELECT a anon + grant SELECT a anon.

3. **`declaracion_familiar`** (TERCEROS PII, DENY-BY-DEFAULT — copia VERBATIM la convención de 0018/0021):
   - `id bigint generated always as identity primary key`.
   - `fuente_id text not null` + `fecha_presentacion date not null`, FK a `declaracion(fuente_id, fecha_presentacion) on delete cascade`.
   - `relacion text` (`esConyugeDe`/`esHijoDe`/… raw), `nombre text` (crudo, tercero, uso interno).
   - Provenance inline NOT NULL.
   - **NO existe columna RUT** (la fuente no expone RUT — verificado en research; minimización por la fuente).
   - `alter table declaracion_familiar enable row level security;` — NINGUNA policy, NINGÚN grant a anon, **+ `revoke all on declaracion_familiar from anon, authenticated;`** (la lección Phase 11: el proyecto concede por DEFAULT PRIVILEGES a anon en tablas nuevas de public; la RLS sin policy niega filas pero el privilegio seguía presente — LEGAL-03 exige ambos). Comentar explícitamente la ausencia intencional de policy + el porqué del revoke.

4. **`declaraciones_de_parlamentario(p_id text)`** (RPC público — historial, espejo de `lobby_de_parlamentario`):
   - `language sql stable security definer set search_path = ''`.
   - `returns table (...)` con SOLO los campos que la fuente publica del payload §10 (`fuente_id`/`version_id`, `fecha_presentacion`, `tipo`, `cargo`, `organismo`, `origen`, `fecha_captura`, `enlace`, `licencia`). NUNCA `parlamentario_id` interno crudo más allá del filtro, NUNCA un RUT, NUNCA datos de `declaracion_familiar`.
   - Cuerpo: `select ... from public.declaracion d where d.parlamentario_id = p_id order by d.fecha_presentacion desc;` — SOLO declaraciones confirmadas (FK = p_id), ordenadas DESC (la más reciente primero; una vieja NUNCA se marca "actual", INT-04).
   - `revoke execute on function public.declaraciones_de_parlamentario(text) from public;` + `grant execute ... to anon;`

5. **`comparar_declaraciones(p_id text, fechas date[])`** (RPC público — comparación lado-a-lado, INT-05):
   - `language sql stable security definer set search_path = ''`.
   - `returns table (fecha_presentacion date, etiqueta text, valor text, origen text, fecha_captura timestamptz, enlace text, licencia text)` — devuelve los campos declarados LITERALES (clave/valor) de las versiones pedidas, en filas. **CERO columna de delta/variación/enriquecimiento/conflicto/Δ** (PITFALLS B3/E2; LOCKED). El UI las dispone lado-a-lado; el RPC NO computa nada. Si un campo está ausente en una versión, el UI muestra "No declarado en esta versión" (no el RPC).
   - Cuerpo: une `declaracion` + sub-tablas de bienes para `parlamentario_id = p_id AND fecha_presentacion = any(fechas)`, proyectando (etiqueta_noun, valor_literal). Devuelve la `licencia` por fila (la atribución viaja a la vista derivada).
   - `revoke execute on function public.comparar_declaraciones(text, date[]) from public;` + `grant execute ... to anon;`

6. **`probidad_ingesta_estado`** (marcador de ingesta por-parlamentario — espejo de `lobby_ingesta_estado`): tabla mínima `parlamentario_id text primary key references parlamentario(id) on delete cascade`, `ingestado_hasta date`, `fecha_captura timestamptz not null default now()`. public-read (la ficha la consulta con anon para distinguir "no ingestado" (fila ausente) de "ingestado, cero confirmadas" (fila presente)). NO contiene PII → public-read es correcto.

Encabezar el archivo con un comentario que: (a) cite que la última migración aplicada es 0021; (b) explique que la aplicación + pgTAP son checkpoint de operador (Pitfall 6: BOM en .env → pasar `--db-url` explícito); (c) marque `declaracion_familiar` como deny-by-default + revoke per 0021; (d) registre las respuestas a OQ1/2/3. NO incluir bloques de código de la aplicación; es SQL puro.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0022_probidad.sql','utf8').split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n'); const must=['create table declaracion','fecha_presentacion','primary key (fuente_id, fecha_presentacion)','create policy declaracion_public_read','grant select on declaracion to anon','create table declaracion_familiar','revoke all on declaracion_familiar from anon, authenticated','declaraciones_de_parlamentario','comparar_declaraciones','grant execute on function','probidad_ingesta_estado','licencia']; const miss=must.filter(m=>!s.includes(m)); const banned=['create policy declaracion_familiar','grant select on declaracion_familiar','delta','enriquecimiento','variacion']; const bad=banned.filter(b=>s.toLowerCase().includes(b.toLowerCase())); if(miss.length){console.error('FALTAN:',miss);process.exit(1)} if(bad.length){console.error('PROHIBIDO (familiar deny-by-default; RPC sin delta/veredicto):',bad);process.exit(1)} console.log('0022 OK')"</automated>
  </verify>
  <done>0022_probidad.sql existe; `declaracion` es public-read con PK compuesta `(fuente_id, fecha_presentacion)` (versionada) + `licencia` NOT NULL; las sub-tablas de bienes son public-read y FK-referencian la versión; `declaracion_familiar` tiene RLS habilitada SIN policy/grant a anon + `revoke all from anon, authenticated`; los RPCs `declaraciones_de_parlamentario` (DESC) y `comparar_declaraciones` (sin delta/veredicto) son security-definer + revoke from public + grant a anon; existe `probidad_ingesta_estado`. Sin RUT en ninguna tabla. OQ1/2/3 documentadas.</done>
</task>

<task type="auto">
  <name>Task 2: Autorar la prueba pgTAP 0022_probidad.test.sql</name>
  <files>supabase/tests/0022_probidad.test.sql</files>
  <action>
Crear la prueba pgTAP espejando `supabase/tests/0021_lobby.test.sql` y `0018_piso_pii.test.sql`. Aserciones observables (cada una prueba una invariante de seguridad/identidad/versionado de INT-03/04/05):

- `has_table('declaracion')`, `has_table('declaracion_familiar')`, `has_table('probidad_ingesta_estado')`, y `has_table` para cada sub-tabla de bienes creada.
- RLS habilitada en todas (consulta a `pg_class.relrowsecurity` o `row_security_active`).
- **`declaracion` VERSIONADA:** assertion de que la PK de `declaracion` incluye `fecha_presentacion` (consulta a `pg_constraint`/`information_schema.key_column_usage` confirmando que la PK son las columnas `(fuente_id, fecha_presentacion)`). Esto codifica "las versiones acumulan, nunca se sobreescriben" (Pitfall 1).
- **`declaracion_familiar` deny-by-default:** NO existe ninguna policy (`SELECT count(*) FROM pg_policies WHERE tablename = 'declaracion_familiar'` = 0) Y `anon` NO tiene grant SELECT (`information_schema.role_table_grants`). Espeja la assertion 8 de 0021 que cazó el grant heredado.
- **`declaracion` + sub-tablas de bienes public-read:** existe una policy SELECT para anon Y `anon` tiene grant SELECT en `declaracion` y en al menos una sub-tabla de bienes.
- **Provenance + licencia NOT NULL:** `col_not_null('declaracion','origen')`, `col_not_null('declaracion','enlace')`, `col_not_null('declaracion','licencia')`; idem para `declaracion_familiar` (`origen`, `enlace`).
- **Sin columna RUT:** assertion de que `declaracion` y `declaracion_familiar` NO tienen una columna que contenga 'rut' (consulta a `information_schema.columns`).
- **FK de familiar a la VERSIÓN, no a parlamentario:** assertion de que la única FK saliente de `declaracion_familiar` es a `declaracion(fuente_id, fecha_presentacion)` y NO existe FK a `parlamentario`.
- **RPCs existen y son security definer:** `has_function('declaraciones_de_parlamentario')` + `has_function('comparar_declaraciones')` + assertion de `prosecdef = true` en `pg_proc` para ambas; y que `anon` tiene grant EXECUTE en ambas.
- Usar `plan(N)` con el conteo correcto y `finish()`. NO ejecutar la prueba aquí (requiere schema aplicado — checkpoint de operador en Task 3).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/tests/0022_probidad.test.sql','utf8'); const must=['declaracion','declaracion_familiar','probidad_ingesta_estado','pg_policies','declaraciones_de_parlamentario','comparar_declaraciones','fecha_presentacion','plan(','finish()']; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('FALTAN:',miss);process.exit(1)} console.log('pgTAP 0022 OK')"</automated>
  </verify>
  <done>0022_probidad.test.sql existe con `plan(N)`/`finish()`; aserciones de deny-by-default sobre `declaracion_familiar` (cero policies + sin grant anon), public-read sobre `declaracion` + sub-tablas de bienes, PK de versión incluye `fecha_presentacion`, sin columna RUT, provenance + licencia NOT NULL, FK familiar→versión (no a parlamentario), y ambos RPCs security-definer + grant execute a anon.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>La migración 0022 (declaracion VERSIONADA public-read + sub-tablas de bienes public-read + declaracion_familiar deny-by-default + revoke + RPCs declaraciones_de_parlamentario/comparar_declaraciones + probidad_ingesta_estado) y su prueba pgTAP fueron autoradas. NO se aplicaron — build/typecheck NO prueban que Postgres ejecutó el DDL (Pitfall 6).</what-built>
  <how-to-verify>
Operador aplica la migración al Supabase remoto sa-east-1 (alcanzable; 0018/0019/0020/0021 se aplicaron ahí) y corre la prueba pgTAP contra el schema aplicado:
1. Aplicar 0022 vía el mismo camino que 0021 (extraer `SUPABASE_DB_URL` con node esquivando el BOM de .env; `psql --single-transaction --db-url`).
2. Correr `supabase/tests/0022_probidad.test.sql` contra el schema aplicado (pgTAP).
3. Confirmar: todas las aserciones PASS; en particular `anon` NO puede `select * from declaracion_familiar` (deny-by-default + revoke) y SÍ puede `select * from declaracion` (public-read); `select * from declaraciones_de_parlamentario('<algún id>')` ejecuta como anon y ordena por fecha_presentacion DESC; `comparar_declaraciones('<id>', array['2024-01-01'::date])` ejecuta como anon y NO devuelve ninguna columna de delta/veredicto.
Si el remoto no es alcanzable en el entorno, queda como checkpoint diferido (split 09-03/11-01) — el DDL ya está autorado y versionado.
  </how-to-verify>
  <resume-signal>Escribe "aplicada y pgTAP verde" o describe qué assertion falló.</resume-signal>
</task>

</tasks>

<verification>
- 0022_probidad.sql y 0022_probidad.test.sql existen y pasan los gates de grep.
- `declaracion` es public-read VERSIONADA (PK incluye fecha_presentacion) con licencia NOT NULL; sub-tablas de bienes public-read; `declaracion_familiar` deny-by-default (RLS on, cero policies, revoke anon/authenticated); los RPCs son security-definer + revoke from public + grant a anon; `comparar_declaraciones` sin columna de delta/veredicto.
- (Operador) migración aplicada + pgTAP verde contra el schema aplicado.
</verification>

<success_criteria>
- INT-03: existe el destino de la declaración (`declaracion` + sub-tablas de bienes) con provenance NOT NULL + licencia CC BY 4.0 por fila y la convención RLS correcta.
- INT-04: la clave de versión incluye `fecha_presentacion` (acumula, nunca sobreescribe); el RPC de historial ordena DESC; `probidad_ingesta_estado` habilita los tres estados honestos.
- INT-05: el RPC `comparar_declaraciones` devuelve valores literales lado-a-lado SIN columna de delta/veredicto/enriquecimiento; la licencia viaja a la vista derivada.
- `declaracion_familiar` (terceros) es deny-by-default + revoke; nunca tiene FK a parlamentario; sin columna RUT en ninguna tabla.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (navegador) → Postgres RLS | `anon` lee `declaracion`/sub-tablas/RPCs; NUNCA debe leer `declaracion_familiar` (terceros PII) |
| service role (writer Plan 02) → Postgres | bypassa RLS; única vía de escritura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure | `declaracion_familiar` (terceros PII) | mitigate | deny-by-default (RLS on + cero policies) **+ `revoke all from anon, authenticated`** (lección Phase 11); pgTAP asserta cero policies + sin grant anon; nunca en la proyección de un RPC |
| T-12-02 | Tampering (integridad) | clave de versión de `declaracion` | mitigate | PK compuesta incluye `fecha_presentacion` → versiones acumulan; un re-run no colapsa el historial (Pitfall 1); pgTAP asserta la PK compuesta |
| T-12-03 | Information disclosure | RUT de declarante/familiar | mitigate | la fuente no expone RUT → ninguna columna RUT se crea; pgTAP asserta la ausencia de columna 'rut' |
| T-12-04 | Spoofing/Repudiation | FK `parlamentario_id` (declarante) | mitigate | nullable + poblado solo si confirmado (IDENT-12, Plan 02); RPC filtra `parlamentario_id = p_id` (solo confirmadas) |
| T-12-05 | Tampering (product/legal risk) | `comparar_declaraciones` (vista derivada) | mitigate | el RPC devuelve solo valores literales; CERO columna de delta/veredicto; el gate de grep prohíbe 'delta'/'enriquecimiento'/'variacion' en el SQL |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase 12 NO instala paquetes externos (research §"Package Legitimacy Audit": cero installs; todo locked/workspace/Deno-std). No aplica checkpoint de legitimidad |
</threat_model>

<output>
Crear `.planning/phases/12-int-patrimonio-intereses-declaraciones-con-historial-y-compa/12-01-SUMMARY.md` al terminar.
</output>
