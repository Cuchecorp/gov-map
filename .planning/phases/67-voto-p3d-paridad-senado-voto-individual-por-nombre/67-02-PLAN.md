---
phase: 67-voto-p3d-paridad-senado-voto-individual-por-nombre
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - .planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-BACKFILL-SENADO-RUNBOOK.md
autonomous: false
requirements: [VOTO-01]
tags: [voto, senado, backfill, runbook, operador-LOCAL, votaciones.php, VOTO-01]
locked:
  - "Backfill masivo = LOCAL operador, NO GitHub Actions (minimizar minutos CI)"
  - "Rate-limit 2-3s/host, UA identificatorio, serial, NUNCA ráfagas (WAF gubernamental)"
  - "Dos etapas: fuente→R2 (crudo inmutable) SIEMPRE antes de R2→Supabase"
  - "Senado por NOMBRE → probable/no_confirmado; determinista único = confirmado (D-A1); nunca FK ambiguo"
entry_point: packages/votos/src/run-votos-masivo-cli.ts

must_haves:
  truths:
    - "El operador tiene un runbook LOCAL reproducible para poblar el voto individual del Senado a escala vía votaciones.php sin que el agente toque el WAF ni escriba a PROD"
    - "El runbook impone dos etapas (fuente→R2 crudo ANTES de R2→Supabase), rate-limit 2-3s serial, y `--from-r2` para replay sin re-tocar la fuente"
    - "El runbook incluye un SPIKE gated para fijar los tokens LIVE de <SELECCION> del Senado (A4) y detectar cualquier token que hoy fallaría ruidoso"
    - "El runbook reporta cobertura Senado por estado_vinculo y NO fuerza el invariante DIPID-maestra (Cámara-only)"
  artifacts:
    - path: ".planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-BACKFILL-SENADO-RUNBOOK.md"
      provides: "runbook operador-LOCAL del backfill LIVE Senado (espejo de 66-BACKFILL-RUNBOOK.md)"
      min_lines: 120
  key_links:
    - from: "67-BACKFILL-SENADO-RUNBOOK.md"
      to: "packages/votos/src/run-votos-masivo-cli.ts"
      via: "flags reales del CLI (--boletines-file, --from-r2, --dry-run, --limit)"
      pattern: "run-votos-masivo-cli"
---

<objective>
Producir el runbook operador-LOCAL del backfill LIVE del voto individual del Senado a escala vía `votaciones.php`, espejo de `66-BACKFILL-RUNBOOK.md`. El agente NO corre el backfill LIVE (golpea el WAF gubernamental a escala + escribe a la Supabase REMOTA PROD — ambos operador-LOCAL por regla LOCKED). Este plan produce el DOCUMENTO que el operador ejecuta; el checkpoint humano confirma la corrida.

Purpose: El wire dos-etapas + el fix de `votXmlSenado` (Plan 67-01) hacen el path Senado REPRODUCIBLE offline; el paso que solo el operador puede dar es la corrida LIVE contra `votaciones.php` (rate-limit 2-3s) + write PROD. Mismo patrón que Phase 66 (`66-02` runbook). Incluye el SPIKE gated para fijar los tokens `<SELECCION>` LIVE (A4/Open Q2) — con el fail-loud de 67-01, un token inesperado será VISIBLE en `errores`.

Output: `67-BACKFILL-SENADO-RUNBOOK.md` + un checkpoint que pausa para la corrida del operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-RESEARCH.md
@.planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-01-PLAN.md
@.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md
@packages/votos/src/run-votos-masivo-cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 67-BACKFILL-SENADO-RUNBOOK.md (operador-LOCAL, espejo de 66)</name>
  <files>.planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-BACKFILL-SENADO-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md (la estructura completa a espejar: header QUIÉN/POR QUÉ LOCAL, PRE-CHECKS offline, derivar boletines, particionar reanudable, corrida LIVE con verificación de destino, replay --from-r2, reporte de cobertura, criterios de cierre, rollback, seguridad de credenciales)
    - packages/votos/src/run-votos-masivo-cli.ts (los flags REALES verbatim: `--dry-run`, `--limit N`, `--boletines-file <ruta>`, `--from-r2 <r2Path>`; construcción del R2Store de .env; log de destino LOCAL/REMOTO; impresión de cobertura)
    - .planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-RESEARCH.md (§Runtime State: el backfill Senado INSERTA voto con seq:<n>, votacion gana filas senado:...; A4/Open Q2 tokens SELECCION LIVE; Open Q3 cobertura Senado usa porEstado, NO el invariante DIPID-maestra)
    - .planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-01-PLAN.md (D-A1 determinista único=confirmado; D-A4 mapSeleccion fail-loud; votXmlSenado en el envelope para replay)
  </read_first>
  <action>Escribir `67-BACKFILL-SENADO-RUNBOOK.md` espejando la estructura de `66-BACKFILL-RUNBOOK.md`, adaptado al Senado. Secciones: (0) header QUIÉN CORRE ESTO (el operador, LOCAL; NO el agente, NO GitHub Actions) + POR QUÉ (WAF `tramitacion.senado.cl` a escala + write PROD REMOTO). (1) PRE-CHECKS offline obligatorios: suite Senado verde (`pnpm --filter @obs/votos test run-camara-votos`, `pnpm --filter @obs/tramitacion test parse-senado-votacion`, `typecheck`), migración 0019 aplicada al REMOTO (CHECK `voto.seleccion` incluye `ausente`), `.env` con `R2_*` + `SUPABASE_API_URL`/`SUPABASE_SECRET_KEY` (solo NOMBRES, nunca valores). (2) SPIKE LIVE gated para fijar tokens `<SELECCION>` (A4/Open Q2): correr `--dry-run` contra 1-2 boletines Senado con votaciones conocidas, capturar el crudo `votaciones.php` a R2, INSPECCIONAR los tokens `<SELECCION>` reales; con el fail-loud de 67-01, un token desconocido aparecerá en `errores` con etapa `senado-votaciones` → si aparece, fijarlo con un test de fixture ANTES del backfill masivo (no dejar votos perdidos). (3) DERIVAR boletines Senado: `SELECT boletin FROM proyecto` (paginar `.range()` / `ORDER BY` — cap 1k PostgREST); el Senado se consulta con el boletín BASE sin sufijo (`runIngest` ya hace `baseDe`). (4) PARTICIONAR reanudable (split en lotes + idempotencia por `(votacion_id, fuente_voter_id)` con `seq:<n>`). (5) CORRIDA LIVE lote-por-lote serial (rate-limit 2-3s LOCKED, NUNCA `Promise.all`, verificar destino REMOTO en el log ANTES de escribir — Pitfall 5). (6) REPLAY `--from-r2` sin re-tocar la fuente (ahora reconstruye votos Senado gracias a `votXmlSenado` de 67-01). (7) COBERTURA Senado: reportar `porEstado` (confirmado/probable/no_confirmado); NOTA EXPLÍCITA: NO forzar el invariante DIPID-maestra (Cámara-only, el Senado no tiene DIPID — Open Q3); documentar D-A1 (determinista único → confirmado es legítimo, NO una fabricación). (8) CRITERIOS DE CIERRE + señal de reanudación. (9) ROLLBACK ante WAF 429/5xx + reanudar por `--from-r2` para lo persistido. (10) SEGURIDAD DE CREDENCIALES (solo nombres de env, votos = dato público nombre+DIPID, sin RUT/PII). Usar los flags CLI verbatim; declarar que el agente NO invocó `VOTOS_LIVE` ni tocó el WAF ni escribió a PROD.</action>
  <verify>
    <automated>test -f ".planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-BACKFILL-SENADO-RUNBOOK.md" && grep -c "run-votos-masivo-cli\|from-r2\|rate-limit\|porEstado\|votXmlSenado\|operador" ".planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-BACKFILL-SENADO-RUNBOOK.md"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `67-BACKFILL-SENADO-RUNBOOK.md` existe con ≥120 líneas.
    - Contiene los flags CLI reales verbatim (`--boletines-file`, `--from-r2`, `--dry-run`, `--limit`) — verificable por grep contra `run-votos-masivo-cli.ts`.
    - Incluye el SPIKE gated de tokens `<SELECCION>` (A4) y referencia el fail-loud de 67-01 (un token desconocido aparece en `errores`, no se pierde).
    - Reporta cobertura Senado por `porEstado` y declara EXPLÍCITAMENTE que NO se aplica el invariante DIPID-maestra (Cámara-only, Open Q3).
    - Documenta D-A1 (determinista único → confirmado legítimo) y la disciplina SC#4.
    - Declara operador-LOCAL / NO GitHub Actions / rate-limit 2-3s serial LOCKED, y que el agente NO corrió el LIVE.
  </acceptance_criteria>
  <done>El runbook operador-LOCAL del backfill Senado existe, espeja 66, usa los flags CLI reales, incluye el SPIKE de tokens y la cobertura por estado, y deja el backfill LIVE explícitamente al operador.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (checkpoint): Correr el backfill LIVE Senado (operador-LOCAL) o diferirlo</name>
  <action>CHECKPOINT — el agente NO ejecuta esta task. El operador corre el backfill LIVE del Senado siguiendo 67-BACKFILL-SENADO-RUNBOOK.md (PRE-CHECKS offline, SPIKE de tokens, derivar/particionar boletines, corrida LIVE serial rate-limit 2-3s con verificación de destino REMOTO, replay --from-r2, reporte de cobertura por porEstado). El agente NO invoca VOTOS_LIVE, NO toca el WAF, NO escribe a PROD.</action>
  <what-built>El runbook `67-BACKFILL-SENADO-RUNBOOK.md` (operador-LOCAL) que consume el wire dos-etapas + `votXmlSenado` de 67-01. El agente NO corre el backfill LIVE: golpea el WAF `tramitacion.senado.cl` a escala (rate-limit 2-3s) y escribe a la Supabase REMOTA PROD — ambos operador-LOCAL por regla LOCKED (CLAUDE.md "Ingesta y Cron — LOCKED"). El agente NO invocó `VOTOS_LIVE`, NO tocó el WAF, NO escribió a PROD.</what-built>
  <how-to-verify>
    1. Leer `67-BACKFILL-SENADO-RUNBOOK.md` completo.
    2. Correr los PRE-CHECKS offline (suite Senado verde + typecheck + 0019 en el REMOTO + `.env`).
    3. Correr el SPIKE gated (1-2 boletines Senado, `--dry-run`) y confirmar/fijar los tokens `<SELECCION>` reales; si el fail-loud de 67-01 registra un token desconocido en `errores`, fijarlo con un test ANTES del masivo.
    4. Derivar `boletines-senado.txt`, particionar, y correr el backfill LIVE lote-por-lote (serial, rate-limit 2-3s), verificando el destino REMOTO en el log ANTES de escribir.
    5. Usar `--from-r2` para reanudar cualquier lote que falle en Etapa 2 (ahora reconstruye votos Senado).
    6. Reportar cobertura Senado por `porEstado` (confirmado/probable/no_confirmado). NO aplicar el invariante DIPID-maestra (Cámara-only).
  </how-to-verify>
  <resume-signal>El operador reporta "backfill Senado hecho" con la cobertura por estado (N confirmado / M probable / K no_confirmado) y confirma que los tokens `<SELECCION>` LIVE quedaron fijados (o describe cualquier token nuevo detectado por el fail-loud). Alternativamente, "diferir backfill" para cerrar la fase con el código+runbook listos y la corrida LIVE pendiente.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → WAF `tramitacion.senado.cl` | corrida LIVE a escala; ráfagas → ban del WAF |
| CLI → Supabase REMOTA (PROD) | write con service_role (bypassa RLS); destino equivocado = corrupción de datos |
| `<SELECCION>` LIVE → clasificación de voto | tokens reales no confirmados (A4); un token desconocido no debe perder el voto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04 | DoS (contra la fuente) | corrida LIVE votaciones.php | mitigate | Runbook impone rate-limit 2-3s serial LOCKED, NUNCA `Promise.all`, backfill LOCAL acotado por lotes; `--from-r2` = 0 fetch en replay. Operador-LOCAL, NO GitHub Actions. |
| T-67-05 | Elevation/Tampering | write PROD desde CI o destino equivocado | mitigate | Backfill = operador-LOCAL (`autonomous:false`); el agente NO invoca `VOTOS_LIVE` ni escribe a PROD. El runbook exige verificar `REMOTO (<url PROD>)` en el log ANTES de escribir (Pitfall 5). |
| T-67-06 | Tampering (omisión de voto) | tokens `<SELECCION>` LIVE desconocidos | mitigate | SPIKE gated fija los tokens reales; el fail-loud de 67-01 hace VISIBLE (en `errores`) cualquier token inesperado en vez de perder el voto en silencio. |
| T-67-07 | Info Disclosure | logs / credenciales | mitigate | El runbook referencia SOLO nombres de env (nunca valores); el CLI loguea URL de destino + conteos, jamás la service key ni filas. Votos = dato público (nombre + selección), sin RUT/PII. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | Sin instalaciones (runbook = documento; el CLI ya existe). Sin paquetes externos nuevos. |
</threat_model>

<verification>
- `67-BACKFILL-SENADO-RUNBOOK.md` existe con los flags CLI reales, el SPIKE de tokens, la cobertura por estado y las reglas LOCKED (LOCAL, rate-limit 2-3s, dos etapas).
- El checkpoint pausa la ejecución: el agente NO corre el LIVE (0 fetch al WAF, 0 write PROD).
</verification>

<success_criteria>
- SC#1 (completo tras la corrida del operador): el voto individual del Senado poblado a escala vía `votaciones.php` con dos etapas R2 + `--from-r2`.
- SC#2/SC#4: cobertura reportada por `porEstado`; solo `confirmado` atribuido; determinista único = confirmado (D-A1), ambiguo nunca.
- Backfill LIVE + write PROD ejecutados por el operador (o diferidos explícitamente), NUNCA por el agente.
</success_criteria>

<output>
Create `.planning/phases/67-voto-p3d-paridad-senado-voto-individual-por-nombre/67-02-SUMMARY.md` when done
</output>
