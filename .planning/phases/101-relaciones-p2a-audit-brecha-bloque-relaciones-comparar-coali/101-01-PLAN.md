---
phase: 101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md
autonomous: true
requirements: [REL-01, REL-04, REL-05]
must_haves:
  truths:
    - "Existe un audit N/M por relación reproducible con queries psql verbatim contra PROD"
    - "El hallazgo zona-gap (155 diputados sin distrito) queda registrado con evidencia SQL"
    - "El hallazgo contraparte_id 100% NULL queda registrado; la decisión lobby (DIFERIDA o name-match) queda tomada con N"
    - "El probe empírico de coalición (Servel pactos / comités Senado) tiene veredicto viable/DIFERIDA con evidencia curl/BrowserOS"
    - "El audit emite el N/M de militancia histórica compartida (net-new 696 vs shared-ever 1966) que alimenta el copy de cobertura de Plan 02/03"
  artifacts:
    - path: ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md"
      provides: "Matriz N/M por relación + probe coalición + decisión lobby (gate de diseño)"
      min_lines: 80
  key_links:
    - from: "101-AUDIT-RELACIONES.md"
      to: "PROD via psql -tA"
      via: "queries verbatim reproducibles"
      pattern: "psql .*SUPABASE_DB_URL"
---

<objective>
Producir el documento-gate `101-AUDIT-RELACIONES.md` (espejo del patrón 93-AUDITORIA-CITACIONES): matriz N/M por relación (dato-disponible vs superficie-mostrada), decisión de la relación lobby-misma-contraparte, y el probe empírico de coalición REL-05. Este audit GATEA el diseño de los planes UI: sus conteos N/M alimentan el copy de cobertura declarada de Plan 02 (bloque relaciones) y Plan 03 (/comparar + militancia histórica).

Purpose: Sin este gate el panel de relaciones mentiría por cobertura parcial (el mayor riesgo del proyecto: insinuación por dato ralo). El audit fija la verdad empírica ANTES de escribir cualquier copy.
Output: `.planning/phases/101-.../101-AUDIT-RELACIONES.md` — matriz N/M + probe coalición + decisión lobby, todo con queries/comandos verbatim reproducibles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-RESEARCH.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-CONTEXT.md

<audit_mold>
Espejo del patrón de audit ya validado en v9.0:
`.planning/milestones/v9.0-phases/93-agenda-p2d-auditoria-de-cobertura-de-citaciones/93-AUDITORIA-CITACIONES.md`
— secciones medibles (§1-4), matriz N/M re-medida con psql verbatim CERO deriva, probes curl con hallazgos CONFIRMADO/REFUTADO, correcciones RULE-1/RULE-2 documentadas.
</audit_mold>

<verbatim_queries>
Las queries verbatim (LIVE 2026-07-24 en 101-RESEARCH.md §"Verbatim audit queries") DEBEN re-correrse — son el gate, no los snapshots. Todas corren:
`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"` (nunca PostgREST — cap 1k subestima, Pitfall 4).
Hallazgos esperados a CONFIRMAR o corregir con RULE-1:
- militancia base: 363 filas / 186 distinct parlamentarios / 177 histórico (es_actual=false)
- militancia histórica compartida: any_pairs=1966 | actual_pairs=1270 | net-new=696
- Pitfall 1: DC/Liberal/PPD → 2 partido_alias por display (display ≠ alias)
- ZONA gap (headline): diputados 155 → con_distrito=0/con_circ=0/con_region=0; senado 31 → con_circ=31
- lobby contraparte_id NOT NULL = 0 (de 17681); name-based fallback = 3749 pares / 136 parl
- comisiones: 386 membresías / 34 comisiones; proyecto_autor confirmado = 9937
</verbatim_queries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr las queries N/M verbatim + construir la matriz de brecha (REL-01) y la decisión lobby (REL-04)</name>
  <files>.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md</files>
  <read_first>
    - .planning/phases/101-.../101-RESEARCH.md § "Verbatim audit queries" (queries LIVE) y § Common Pitfalls 1-4
    - .planning/milestones/v9.0-phases/93-agenda-p2d-auditoria-de-cobertura-de-citaciones/93-AUDITORIA-CITACIONES.md (mold del doc: encabezados §, matriz N/M, hallazgos CONFIRMADO/REFUTADO, correcciones RULE-1)
    - CLAUDE.md § Conventions (migraciones/lecturas por psql, PGCLIENTENCODING=UTF8, nunca db push; PostgREST cap 1k)
  </read_first>
  <action>
    Crear `.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md` espejando la estructura de 93-AUDITORIA-CITACIONES.md.

    §1 MATRIZ N/M POR RELACIÓN — re-correr CADA query verbatim de 101-RESEARCH.md § "Verbatim audit queries" con `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"` (SUPABASE_DB_URL de .env). Para cada relación (partido, zona, comisiones, co-autoría, militancia histórica) tabular: dato-disponible (N filas/pares vía psql), superficie-mostrada-hoy (dónde/cómo aparece: RPC 0061 montada en ficha page.tsx:283-294; /red NET OFF). Pegar el número REAL de la corrida (no el snapshot del research) y marcar CONFIRMADO o RULE-1 (corrección) si difiere.

    §2 HALLAZGO ZONA-GAP (headline) — correr la query `select camara, count(*), count(distrito), count(circunscripcion), count(region) from parlamentario group by camara;`. Declarar honestamente: "de_la_misma_zona rinde 0 para los 155 diputados (distrito/region/circunscripcion NULL); el eje zona es SOLO Senado (31)". NO fabricar distrito. Registrar que Cámara-zona sería tarea de INGESTA (fuera de alcance, Future Requirement).

    §3 DECISIÓN LOBBY-MISMA-CONTRAPARTE (REL-04) — correr `select count(*) filter (where contraparte_id is not null) from lobby_contraparte;` (esperado 0 de 17681) + la query name-based fallback (esperado 3749 pares / 136 parl). Inspeccionar el top-15 de contrapartes compartidas por nombre normalizado (query provista en research). VEREDICTO explícito con N: por DEFAULT DIFERIDA (contraparte_id 100% NULL = identidad no resuelta; name-match = provenance débil, riesgo de conflación "cge"/"cge s.a." como una entidad). Documentar AMBAS (DIFERIDA + query name-based) para que el operador pueda decidir; NO shippear name-match sin decisión operador. Registrar la decisión tomada.

    §4 MILITANCIA HISTÓRICA — NET-NEW vs SHARED-EVER (REL-04) — correr la query de pares (any_pairs / actual_pairs / diff). RECOMENDACIÓN LOCKED: net-new-only (696 pares que SOLO comparten militancia histórica, excluyendo los que ya comparten el alias vigente = el bloque "Del mismo partido"). Registrar el N que alimentará el copy de cobertura del 5º bloque y del eje /comparar.

    Cada sección lleva la query verbatim pegada + el resultado real + fecha de corrida. Anti-ranking: los conteos son datos, jamás sort keys.
  </action>
  <verify>
    <automated>test -f ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md" && grep -q "SUPABASE_DB_URL" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md" && grep -qi "zona" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md" && grep -qi "contraparte_id" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `101-AUDIT-RELACIONES.md` existe con §1 matriz N/M, §2 zona-gap, §3 decisión lobby, §4 militancia histórica.
    - Cada N/M lleva la query psql verbatim + el resultado de la corrida real (fecha) — reproducible.
    - El zona-gap está declarado como "eje SOLO Senado" con evidencia (diputados con_distrito=0); NO se fabrica distrito.
    - La decisión lobby-misma-contraparte está tomada explícitamente con N (DIFERIDA por default, name-based query documentada, sin ship sin decisión operador).
    - El N de militancia histórica (net-new recomendado 696) está registrado para alimentar el copy de cobertura downstream.
    - CERO afirmación de afinidad/ranking; los conteos son datos.
  </acceptance_criteria>
  <done>El audit N/M reproducible existe; zona-gap, decisión lobby y N de militancia histórica quedan registrados con queries verbatim y resultados reales.</done>
</task>

<task type="auto">
  <name>Task 2: Probe empírico de coalición REL-05 (curl-first, rate-limit 2-3s, sin write a PROD) + veredicto viable/DIFERIDA</name>
  <files>.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md</files>
  <read_first>
    - .planning/phases/101-.../101-RESEARCH.md § "Sources" tertiary (Servel pactos URL, comités Senado sitio.senado.cl 301) + § Assumptions Log A1/A2 + Environment Availability (curl/BrowserOS)
    - CLAUDE.md § Conventions (ingesta dos-etapas LOCKED, rate-limit 2-3s/host, User-Agent identificatorio, robots.txt, curl-first para WAF; backfill masivo = LOCAL)
    - .planning/phases/101-.../101-CONTEXT.md § Relaciones nuevas + coalición (criterio de viabilidad: fuente oficial estable + machable por nombre determinista)
  </read_first>
  <action>
    Ejecutar un probe empírico de fuentes candidatas de coalición y anexar el resultado como §5 COALICIÓN (REL-05) en `101-AUDIT-RELACIONES.md`.

    Fuentes candidatas (research): (a) Servel pactos parlamentarios 2025 — https://www.servel.cl/2025/08/19/pactos-elecciones-parlamentarias-2025/ + CSV datos abiertos electorales (datos.gob.cl); (b) comités del Senado — sitio.senado.cl/senadores/comites (301 desde www.senado.cl, rechazó fetch plano).

    Protocolo (LOCKED CLAUDE.md): curl-first con User-Agent identificatorio, rate-limit 2-3s ENTRE requests, respetar robots.txt, NUNCA ráfagas. Si curl es bloqueado por WAF/301, usar el MCP BrowserOS (take_snapshot) para leer el DOM. NUNCA escribir a PROD ni a R2 en este probe (solo lectura/evidencia; la ingesta dos-etapas, si viable, es un plan futuro).

    Evaluar el CRITERIO DE VIABILIDAD por cada fuente: (1) fuente oficial estable (URL responde, formato parseable HTML/CSV/XML/__NEXT_DATA__); (2) machable a parlamentarios por nombre DETERMINISTA (los pactos Servel son a nivel de partido → mapear partido→miembro vía la militancia existente; los comités Senado listan senadores por nombre). Registrar la evidencia cruda (status HTTP, primeras filas/estructura, si el nombre es machable).

    VEREDICTO binario por fuente y global:
    - VIABLE → documentar la ruta de ingesta dos-etapas R2 mínima (crudo content-addressed primero `servel/pactos/<fecha>/<sha>` o `senado/comites/<fecha>/<sha>`, parse desde R2 después) — SIN ejecutarla en este plan; queda como recomendación para el operador/plan futuro.
    - NO-VIABLE → DIFERIDA documentada con la evidencia del bloqueo (301/formato inestable/no machable), enlazando al Future Requirement.

    Regla LOCKED: coalición JAMÁS inferida desde votos. El probe solo evalúa fuentes oficiales declaradas.
  </action>
  <verify>
    <automated>grep -qi "coalici" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md" && grep -qiE "viable|DIFERIDA" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md" && grep -qiE "servel|comit" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-AUDIT-RELACIONES.md"</automated>
  </verify>
  <acceptance_criteria>
    - §5 COALICIÓN existe con el probe de AMBAS fuentes candidatas (Servel pactos + comités Senado), cada una con evidencia cruda (status HTTP/DOM).
    - El probe usó curl-first con rate-limit 2-3s (o BrowserOS si bloqueado); CERO write a PROD/R2 documentado.
    - Cada fuente tiene veredicto VIABLE (con ruta dos-etapas R2 documentada, NO ejecutada) o DIFERIDA (con evidencia del bloqueo + enlace a Future Requirement).
    - Coalición NO inferida desde votos (regla LOCKED explícita en el doc).
  </acceptance_criteria>
  <done>El probe de coalición tiene veredicto viable/DIFERIDA por fuente con evidencia empírica; ninguna escritura a PROD/R2 ocurrió.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→PROD (read-only) | queries psql -tA de solo lectura contra la maestra; ninguna escritura |
| agent→fuentes oficiales (Servel/Senado) | fetch de solo lectura con rate-limit; WAF gubernamental |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information disclosure | audit doc con conteos | mitigate | El audit emite SOLO conteos N/M agregados; jamás rut/email; nombres de contraparte solo para la decisión de provenance, no como hecho publicado |
| T-101-02 | Integrity (conflación identidad) | lobby name-match | mitigate | DIFERIDA por default; name-match no se shippea sin decisión operador (contraparte_id NULL = identidad no resuelta) |
| T-101-03 | Tampering (ráfaga WAF) | probe coalición | mitigate | curl-first rate-limit 2-3s, User-Agent identificatorio, robots.txt; NUNCA ráfaga; CERO write a PROD/R2 |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 101 instala CERO paquetes nuevos (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `101-AUDIT-RELACIONES.md` existe con §1-§5 (matriz N/M, zona-gap, lobby, militancia histórica, coalición).
- Cada conteo lleva query/comando verbatim + resultado real reproducible.
- Decisión lobby y veredicto coalición tomados con evidencia.
- CERO write a PROD/R2; CERO paquete instalado.
</verification>

<success_criteria>
- REL-01: matriz N/M reproducible con psql verbatim (dato-disponible vs superficie-mostrada) para las 5 relaciones + zona-gap headline.
- REL-04: decisión lobby-misma-contraparte tomada (DIFERIDA default) + N de militancia histórica registrado.
- REL-05: probe coalición con veredicto viable/DIFERIDA por fuente con evidencia curl/BrowserOS.
- El audit GATEA Plan 02 y Plan 03 (sus N/M alimentan el copy de cobertura).
</success_criteria>

<output>
Create `.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-01-SUMMARY.md` when done, registrando: los N/M reales de la corrida, la decisión lobby, el N de militancia histórica elegido (net-new/shared-ever), y el veredicto coalición — para consumo directo de Plan 02/03.
</output>
