---
phase: 08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/votos/package.json
  - packages/votos/tsconfig.json
  - packages/votos/vitest.config.ts
  - packages/votos/spike/spike.ts
  - packages/votos/spike/spike.test.ts
  - .planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-SUMMARY.md
  - .planning/STATE.md
autonomous: false
requirements: [VOTE-01]

must_haves:
  truths:
    - "Una corrida en vivo contra getVotaciones_Boletin -> getVotacion_Detalle devuelve, para boletines reales, votos por diputado con Diputado/DIPID y Opcion poblados (no null)"
    - "Los conteos nominales del detalle reconcilian contra los totales (count(si)===TotalAfirmativos, count(no)===TotalNegativos)"
    - "El DIPID de cada voto nominal mapea al id_diputado_camara oficial de la maestra v1.0 (enlace determinista, sin LLM)"
    - "El spike documenta cobertura de la muestra y comportamiento de rate respetando el delay 2-3s LOCKED"
    - "Existe una decision binaria registrada: confirmar y construir Phase 10, o replanificar solo el bloque VOTE"
  artifacts:
    - path: "packages/votos/spike/spike.ts"
      provides: "Spike desechable: ensambla colaboradores @obs/ingest en orden LOCKED, tira la muestra, reconcilia, imprime FINDINGS"
      min_lines: 40
    - path: "packages/votos/spike/spike.test.ts"
      provides: "Gate vitest: mitad offline sobre el fixture real (siempre verde) + mitad LIVE-gated (VOTE_SPIKE_LIVE=1) que asserta los 4 criterios"
      min_lines: 40
    - path: ".planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-SUMMARY.md"
      provides: "FINDINGS conciso + decision binaria (confirmar/replanificar)"
      contains: "FINDINGS"
  key_links:
    - from: "packages/votos/spike/spike.ts"
      to: "@obs/tramitacion CamaraConnector"
      via: "import + ensamblaje con Fetcher/HostRateLimiter/RobotsGuard de @obs/ingest"
      pattern: "CamaraConnector"
    - from: "packages/votos/spike/spike.ts"
      to: "reconciliarVotosCamara"
      via: "cruce DIPID -> id_diputado_camara contra la maestra del seed"
      pattern: "reconciliarVotosCamara"
    - from: "packages/votos/spike/spike.test.ts"
      to: "supabase/seeds/parlamentario.seed.json"
      via: "readFileSync de la maestra (155 diputados con id_diputado_camara)"
      pattern: "parlamentario\\.seed\\.json"
---

<objective>
Validar EN VIVO, detras del WAF gubernamental, que `opendata.camara.cl/wscamaradiputados.asmx` (`getVotaciones_Boletin` -> `getVotacion_Detalle`) entrega el voto individual por diputado utilizable para enlace determinista, y registrar una decision binaria confirmar/replanificar para el bloque VOTE.

Es un SPIKE confirm-or-replan, no una construccion. El pipeline ya existe y se verifico LIVE una vez en v1.0 Phase 5 (`connector-camara.ts` + `parse-camara-votacion.ts` + `reconciliar-camara.ts` + fixture `camara-votacion-detalle-real.xml`). El spike RE-USA esos simbolos verbatim contra una muestra Leg-58 fresca y graba el resultado. NO construye el conector `@obs/votos` de produccion, ni el modelo de voto, ni la ficha (eso es Phase 10).

Purpose: VOTE-01 es el GATE de todo el bloque VOTE. Si el voto individual viene null o el DIPID no mapea, se replanifica SOLO el bloque VOTE (no bloquea INT/MONEY). El doc indica MEDIUM-HIGH que el shape existe -> se espera CONFIRMAR.
Output: un spike desechable (test gated + script), un FINDINGS en `08-SUMMARY.md`, y una linea de decision en `STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-CONTEXT.md
@.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-RESEARCH.md

# Codigo v1.0 que el spike RE-USA verbatim (leer antes de implementar):
@packages/tramitacion/src/connector-camara.ts
@packages/tramitacion/src/parse-camara-votacion.ts
@packages/tramitacion/src/reconciliar-camara.ts
@packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml

<interfaces>
<!-- Contratos que el executor consume directamente. Extraidos del codebase. No explorar. -->

Exportado por @obs/tramitacion (packages/tramitacion/src/index.ts):
  class CamaraConnector { constructor(deps: CamaraConnectorDeps); fetchVotacionesBoletin(boletinBase: string): Promise<string>; fetchVotacionDetalle(votacionId: string): Promise<string>; }
  interface CamaraConnectorDeps { fetcher: Fetcher; rateLimiter: HostRateLimiter; robots: RobotsGuard; allowlist?: AllowlistOptions; }
  function parseCamaraVotacion(xml: string, opts?: { detalleXml?: string; enlace?: string }): Votacion[]
  function parseCamaraVotoDetalle(detalleXml: string): CamaraVotoDetalle[]
  interface CamaraVotoDetalle { diputadoId: string; opcion: "si" | "no"; nombreCrudo: string }
  function reconciliarVotosCamara(votosCrudos: CamaraVotoDetalle[], votacionId: string, maestra: Parlamentario[], opts?: { periodo?: string }): Voto[]

Tipo Votacion (campos relevantes): { id: string ("camara:<ID>"); boletin: string; total_si: number; total_no: number; total_abstencion: number; total_pareo: number; camara: "diputados"; ... }
Tipo Voto (campos relevantes): { votacion_id: string; fuente_voter_id: string; parlamentario_id: string | null; seleccion: "si" | "no"; metodo: "determinista" | null; estado_vinculo: "confirmado" | "no_confirmado"; ... }

Exportado por @obs/ingest (packages/ingest/src/index.ts):
  class Fetcher { constructor(opts?: FetcherOptions); }
  class HostRateLimiter { constructor(opts?: RateLimiterOptions); }   // 2-3s serial por host (LOCKED default)
  class RobotsGuard { constructor(opts?: RobotsGuardOptions); }
  function assertAllowedUrl(url: string, allowlist?: AllowlistOptions): URL
  // El allowlist por defecto YA incluye el sufijo "camara.cl" -> opendata.camara.cl pasa sin editar nada.

Maestra: supabase/seeds/parlamentario.seed.json
  Parlamentario[] (de @obs/core), 186 filas; 155 con id_diputado_camara (camara='diputados'). DIPID 815 (Bobadilla) presente.

Realidad LIVE del endpoint (verificada 2026-06-18, encodada en el connector):
  - getVotaciones_Boletin?prmBoletin=<base>   (p.ej. "14309") -> XML lista de Votacion + totales
  - getVotacion_Detalle?prmVotacionId=<id>     (note casing: prmVotacionId) -> XML con Votos>Voto>Diputado/DIPID + Opcion Codigo
  - NO existen retornarVotacionDetalle ni retornarVotacionesXAnno (500 "nombre de metodo no valido").
  - El detalle real es un unico <Votacion> raiz (ns tempuri.org), NO una lista <Votaciones>.

Patron LIVE-gated del repo (verbatim, packages/llm/src/smoke.test.ts / golden-set.test.ts):
  const LIVE = process.env.VOTE_SPIKE_LIVE === "1";
  (LIVE ? describe : describe.skip)("...", () => { ... });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spike desechable + gate vitest (offline siempre verde, LIVE gated)</name>
  <files>packages/votos/package.json, packages/votos/tsconfig.json, packages/votos/vitest.config.ts, packages/votos/spike/spike.ts, packages/votos/spike/spike.test.ts</files>
  <read_first>
    - packages/tramitacion/src/connector-camara.ts (URLs verbatim getVotacionesBoletin/getVotacionDetalle; ensamblaje LOCKED; NO BaseConnector.run)
    - packages/tramitacion/src/parse-camara-votacion.ts (parseCamaraVotacion totales + parseCamaraVotoDetalle: solo si/no nominal, omite No Vota/Abstencion cod 4)
    - packages/tramitacion/src/reconciliar-camara.ts (cruce DIPID->id_diputado_camara, fail-closed, index camara='diputados')
    - packages/tramitacion/test/fixtures/camara-votacion-detalle-real.xml (vote 88813, boletin 14309-04, 58 si / 81 no, ~139 nominales)
    - packages/llm/src/smoke.test.ts (patron LIVE-gated: process.env.X==="1" + (LIVE?describe:describe.skip))
    - packages/tramitacion/package.json y packages/llm/package.json (forma de un package.json de workspace: type module, scripts.test vitest run, deps workspace:*)
    - supabase/seeds/parlamentario.seed.json (forma de la maestra; confirmar DIPID 815 presente)
  </read_first>
  <behavior>
    - Offline (siempre, sin red): parseCamaraVotoDetalle(camara-votacion-detalle-real.xml) devuelve >=1 voto; todo item tiene diputadoId no vacio y opcion in {si,no}; conteos nominales reconcilian (count(si)===58, count(no)===81); reconciliarVotosCamara(detalle, "camara:88813", maestraSeed) marca DIPID 815 como estado_vinculo='confirmado' metodo='determinista'; los no-mapeados quedan parlamentario_id=null 'no_confirmado' (fail-closed, NO crash).
    - LIVE (solo con VOTE_SPIKE_LIVE=1): por cada boletin de la muestra [14309, 18296, + 2-3 votaciones recientes Leg-58 descubiertas via getVotaciones_Boletin], el detalle fresco tiene detalle.length>=1 y detalle.every(d => d.diputadoId && (d.opcion==='si'||d.opcion==='no')); count(si)===total_si y count(no)===total_no de parseCamaraVotacion; ratio confirmados/nominales >= 0.95 con metodo==='determinista' en los matches; sin tormenta de 429/RetryableError.
    - Skip por defecto: sin VOTE_SPIKE_LIVE el bloque LIVE no corre y no toca la red (no quema el WAF en CI).
  </behavior>
  <action>
    Crear el paquete desechable `packages/votos` (manifest minimo: name "@obs/votos", private, type module, scripts.test "vitest run", deps workspace:* a @obs/tramitacion/@obs/ingest/@obs/core + devDeps vitest/tsx/@types/node, espejando packages/tramitacion/package.json; tsconfig.json y vitest.config.ts espejando los de tramitacion con include "spike/**/*.test.ts"). NO crear `packages/votos/src/` de produccion ni modelo de voto ni migracion — es throwaway, lo real es Phase 10.

    En `spike/spike.ts` exportar una funcion `runSpike(opts)` que: ensambla `new CamaraConnector({ fetcher: new Fetcher({allowlist}), rateLimiter: new HostRateLimiter(), robots: new RobotsGuard({allowlist}), allowlist })` con `allowlist = {}` (el default ya cubre el sufijo camara.cl — NO editar el allowlist, eso es Phase 10 per Deferred). Para cada boletin base de la muestra: fetchVotacionesBoletin -> parseCamaraVotacion -> por cada Votacion, derivar wsId con v.id.replace(/^camara:/,"") -> fetchVotacionDetalle(wsId) -> parseCamaraVotoDetalle -> reconciliarVotosCamara(crudos, v.id, maestra). Acumular por votacion: nominales (count si/no), reconciliacion contra total_si/total_no, ratio de confirmados, latencia por request y status. Cargar la maestra con readFileSync("supabase/seeds/parlamentario.seed.json","utf8") (NO tocar la DB — el .env tiene BOM que rompe el supabase CLI per project memory; ademas R2/Supabase remoto bloqueados). Imprimir por consola un bloque FINDINGS legible (shape, campos poblados, mapeo DIPID, cobertura de la muestra, comportamiento de rate) que Task 2 transcribe al SUMMARY. NO usar BaseConnector.run (su cache diaria saltaria la re-corrida LIVE del mismo dia). NO usar fetch a pelo. NO escribir a Supabase ni R2.

    En `spike/spike.test.ts`: mitad OFFLINE (sin gate) que lee el fixture real con readFileSync y asserta el bloque <behavior> offline reusando parseCamaraVotoDetalle + parseCamaraVotacion + reconciliarVotosCamara y la maestra del seed — esto mantiene la suite verde sin red y satisface el gate Nyquist. Mitad LIVE con `const LIVE = process.env.VOTE_SPIKE_LIVE === "1"; (LIVE ? describe : describe.skip)(...)` que invoca runSpike sobre la muestra y asserta los 4 criterios. Para las 2-3 votaciones recientes Leg-58: descubrirlas dentro del bloque LIVE consultando getVotaciones_Boletin de los boletines de la muestra (sin metodo de enumeracion por anno — no existe); si la muestra reciente no se puede ampliar, degradar a los 2 boletines cross-camara conocidos (14309, 18296) y registrar la cobertura reducida (no abortar). Respetar el delay 2-3s del HostRateLimiter (no paralelizar contra el mismo host).
  </action>
  <verify>
    <automated>pnpm --filter @obs/votos test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/votos test` pasa con la mitad offline verde y la mitad LIVE en skip (sin VOTE_SPIKE_LIVE).
    - El test offline prueba sobre `camara-votacion-detalle-real.xml`: detalle.length>=1, todo diputadoId no vacio + opcion in {si,no}, count(si)===58, count(no)===81, y DIPID 815 reconcilia a estado_vinculo='confirmado' metodo='determinista'.
    - `spike/spike.ts` ensambla CamaraConnector con colaboradores de @obs/ingest en el orden LOCKED y NO usa BaseConnector.run ni fetch a pelo; carga la maestra del seed (no DB, no R2).
    - No se crea codigo de produccion en `packages/votos/src/`, ni modelo de voto, ni migracion, ni edicion del allowlist.
  </acceptance_criteria>
  <done>Spike throwaway implementado; suite offline verde; bloque LIVE gated por VOTE_SPIKE_LIVE=1 listo para una corrida deliberada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Corrida LIVE deliberada + registro de FINDINGS y decision binaria</name>
  <files>.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-RESEARCH.md (Binary decision recording; los 4 criterios; pitfalls de reconciliacion)
    - .planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-VALIDATION.md (sign-off del spike)
    - .planning/STATE.md (seccion Accumulated Context -> Decisions y Blockers/Concerns -> donde grabar la decision)
  </read_first>
  <what-built>
    El spike LIVE-gated del Task 1 (suite offline ya verde). Falta la corrida LIVE real contra `opendata.camara.cl` (no-deterministica, rate-limited, no apta para CI por defecto) y la transcripcion del resultado a docs — el deliverable real del spike es la decision binaria registrada.
  </what-built>
  <how-to-verify>
    1. Correr el spike LIVE deliberadamente: `VOTE_SPIKE_LIVE=1 pnpm --filter @obs/votos test` (respeta el delay 2-3s; ~una docena de requests, mantener minimo).
    2. Observar el bloque FINDINGS impreso: shape del XML, campos poblados (Diputado/DIPID + Opcion no null), conteos nominales vs totales, ratio de mapeo DIPID->id_diputado_camara, cobertura de la muestra, latencias/429.
    3. Escribir `08-SUMMARY.md` con un FINDINGS conciso (shape, campos poblados, mapeo, cobertura historica de la muestra, comportamiento de rate) y la DECISION binaria: CONFIRMAR (si los 4 criterios pasan -> construir Phase 10 tal cual) o REPLANIFICAR (si el voto viene null / no mapea -> replanificar SOLO el bloque VOTE, sin bloquear INT/MONEY), citando la evidencia observada (status HTTP, conteos, DIPIDs sin match).
    4. Agregar una linea de Decision a `STATE.md` (Accumulated Context -> Decisions) con el mismo veredicto y, si REPLAN, actualizar el blocker de Phase 8 en Blockers/Concerns.
    Esperado por el research: CONFIRMAR.
  </how-to-verify>
  <verify>
    <human-check>FINDINGS + decision binaria escritos en 08-SUMMARY.md y reflejados en STATE.md (Decisions); la corrida LIVE se ejecuto una vez.</human-check>
  </verify>
  <resume-signal>Escribe "approved" cuando el FINDINGS y la decision binaria esten registrados, o describe que ajustar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike -> opendata.camara.cl | Salida HTTP a un endpoint gubernamental publico detras de WAF; sin auth, sin input de usuario, solo lectura. |
| spike -> seed JSON local | Lectura de la maestra de identidades (uso interno; sin exponer RUT). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/Info-disclosure | URL de fetch (SSRF) | mitigate | `assertAllowedUrl` (allowlist por sufijo + bloqueo RFC1918/loopback/metadata) aplicado antes de cada fetch via Fetcher/CamaraConnector; el spike NO usa fetch a pelo. |
| T-08-02 | DoS (auto-infligido) | ráfagas contra el WAF | mitigate | `HostRateLimiter` 2-3s serial-por-host LOCKED + UA identificado + robots respetado; muestra acotada a ~una docena de requests; sin paralelizar contra el mismo host. |
| T-08-03 | DoS/Tampering | XML malformado / entity expansion | mitigate | `fast-xml-parser` (sin resolucion de entidades externas) + validacion zod (VotacionSchema/VotoSchema) en los parsers reusados. |
| T-08-04 | Info-disclosure | secretos en mensajes de error | accept | El spike no usa secretos en la ruta de fetch gov; Fetcher/clases de error ya excluyen credenciales de los mensajes. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Cero paquetes nuevos instalados (RESEARCH Package Legitimacy Audit: N/A); solo imports de workspace + deps ya locked. Sin superficie de registry. |
</threat_model>

<verification>
- `pnpm --filter @obs/votos test` verde (offline; LIVE en skip).
- Corrida LIVE ejecutada una vez con `VOTE_SPIKE_LIVE=1` y FINDINGS observado.
- `08-SUMMARY.md` contiene FINDINGS + decision binaria; `STATE.md` Decisions refleja el veredicto.
- Sin codigo de produccion `@obs/votos`, sin modelo de voto, sin migracion, sin edicion de allowlist (todo eso es Phase 10).
</verification>

<success_criteria>
1. Corrida LIVE -> `getVotacion_Detalle` devuelve votos por diputado con DIPID + Opcion no null, reconciliables contra los totales nominales (count(si)===total_si, count(no)===total_no).
2. DIPID mapea a `id_diputado_camara` de la maestra v1.0 (enlace determinista sin LLM; ratio >= 0.95 en la muestra Leg-58).
3. Cobertura de la muestra y comportamiento de rate (delay 2-3s LOCKED) documentados en el FINDINGS.
4. Decision binaria registrada (CONFIRMAR / REPLANIFICAR solo VOTE) en `08-SUMMARY.md` + `STATE.md`.
</success_criteria>

<output>
Create `.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-01-SUMMARY.md` when done (ademas del `08-SUMMARY.md` con el FINDINGS + decision binaria, que es el deliverable de la fase).
</output>
