---
phase: 93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md
autonomous: true
requirements: [CIT-01]
must_haves:
  truths:
    - "La matriz N/M de las 4 celdas {sala,comisiones}×{Cámara,Senado} queda declarada con la query psql verbatim que produjo cada N y la fecha de rango"
    - "Cada endpoint candidato tiene veredicto UP/DOWN/PARCIAL con evidencia (HTTP code + muestra recortada) y si trae histórico o solo forward"
    - "Los 3 hallazgos previos quedan CONFIRMADOS o refutados citando la medición (incluye la refutación 'Cámara citaciones tiene histórico')"
    - "Queda anotado el % de citaciones con estado de cancelación/reagendo poblado por cámara"
    - "Queda verificado si agenda-weekly.yml existe/corre y con qué secrets (gap de frescura si falta)"
  artifacts:
    - path: ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md"
      provides: "Reporte de auditoría con secciones Matriz N/M, Veredictos de endpoints, Hallazgos confirmados/refutados, Estado de cancelación, Frescura"
      contains: "Matriz N/M"
  key_links:
    - from: "93-AUDITORIA-CITACIONES.md § Matriz N/M"
      to: "PROD (citacion, sesion_sala, sesion_tabla_item)"
      via: "psql -tA verbatim (SUPABASE_DB_URL)"
      pattern: "select .*from citacion group by camara"
---

<objective>
Componer las secciones MEDIBLES del reporte `93-AUDITORIA-CITACIONES.md`: la matriz N/M por celda (re-medida en vivo, query verbatim incluida), los veredictos por endpoint (re-sondeados con curl), la confirmación/refutación de los 3 hallazgos previos, el % de estado de cancelación poblado, y la verificación de la frescura (cron agenda-weekly).

Purpose: CIT-01 exige medir —no asumir— qué se scrapea hoy vs qué publica cada fuente ANTES de tocar UI. Un calendario parcial mostrado como completo engaña a la prensa. Esta es la base cuantitativa del gate de 94.
Output: `93-AUDITORIA-CITACIONES.md` con las secciones cuantitativas pobladas (la sección de wiring la escribe Plan 02; la DECLARACIÓN de cobertura + backfill los cierra Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-CONTEXT.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md

<interfaces>
<!-- El research (2026-07-22) YA midió estos valores en vivo. Este plan los RE-MIDE
     para asegurar reproducibilidad y los transcribe verbatim al reporte. Si la
     re-medición difiere del research, se usa el valor RE-MEDIDO y se anota la deriva
     (las ventanas forward-only se desplazan a diario). -->

Queries psql verbatim (ejecutar con: PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"):

Matriz principal N por celda:
  select 'citacion_'||camara, count(*), min(fecha)::date, max(fecha)::date from citacion group by camara
  union all
  select 'sesion_sala_'||camara, count(*), min(fecha)::date, max(fecha)::date from sesion_sala group by camara
  order by 1;

Boletines cruzables por celda:
  select c.camara, count(*) puntos, count(cp.boletin) con_boletin from citacion_punto cp join citacion c on c.id=cp.citacion_id group by c.camara;
  select s.camara, count(*) items, count(sti.boletin) con_boletin from sesion_tabla_item sti join sesion_sala s on s.id=sti.sesion_id group by s.camara;

Estado de cancelación poblado:
  select camara, count(*) total, count(estado) con_estado from citacion group by camara;

Semanas ISO distintas:
  select camara, count(distinct semana_iso) from citacion group by camara;

Endpoints (curl-first, UA identificatorio, ≥3s entre hosts). Veredictos medidos en el research (§ Sondeo Empírico):
  Senado comisiones : https://web-back.senado.cl/api/commissions_citations?limit=100  → UP, forward-only
  Senado sala       : https://web-back.senado.cl/api/weekly_table?limit=100            → UP, forward-only
  Cámara comisiones : https://www.camara.cl/legislacion/sesiones_sala/citaciones_semana.aspx?prmSemana=2026-30 (vigente) y ...?prmSemana=2026-20 (mayo, histórico) → UP, histórico confirmado (GET simple pasa el WAF con solo UA)
  Cámara sala PDF   : https://www.camara.cl/verDoc.aspx?prmId=0&prmTipo=TABLASEMANAL (Referer: .../sala_sesiones/tabla.aspx) → UP, solo vigente
  Cámara opendata   : opendata.camara.cl/.../WSComisiones.asmx/getComisiones_Vigentes → DOWN (302 → /mantencion.html)

Frescura (verificar existencia + secrets del cron):
  .github/workflows/agenda-weekly.yml (referido en packages/freshness/src/catalog.ts:259)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-medir la matriz N/M + boletines + estado + semanas ISO (psql verbatim)</name>
  <files>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md</files>
  <read_first>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md (§ Métricas DB — N por Celda), packages/agenda/src/model.ts (columnas citacion/sesion_sala/sesion_tabla_item), supabase/migrations/0010_agenda.sql</read_first>
  <action>
    Crear `93-AUDITORIA-CITACIONES.md` con encabezado (fase, fecha de re-medición, principio "cobertura parcial declarada, nunca completa") y sección "1. Matriz N/M por celda".
    Ejecutar cada query verbatim del bloque <interfaces> con `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"` (SUPABASE_DB_URL viene de .env; cargar BOM-safe si es necesario). NO medir N vía REST del sitio (cap 1k PostgREST subestima — Pitfall 4).
    Transcribir al reporte, por celda {comisiones×Cámara, comisiones×Senado, sala×Cámara, sala×Senado}: N (DB), rango de fecha (min/max), % cruzable a boletín, semanas ISO distintas, y la QUERY VERBATIM que produjo cada número (bloque fenced sql). Declarar M (universo) por celda usando las reglas del research § "Cómo Estimar M": Cámara comisiones = histórico navegable ~20-40/semana; Senado comisiones/sala = ventana forward-only de hoy; Cámara sala = 1 PDF vigente. Marcar cada celda THIN / AL DÍA-en-su-ventana con justificación.
    Si el valor re-medido difiere del research (las ventanas forward-only se desplazan a diario), usar el RE-MEDIDO y anotar la deriva en una nota al pie.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select 'citacion_'||camara, count(*) from citacion group by camara" && grep -q "Matriz N/M" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md" && grep -c "```sql" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md" | grep -qv '^0$'</automated>
  </verify>
  <done>La sección "1. Matriz N/M" existe con las 4 celdas, cada una con N/M, rango de fecha y la query verbatim; el % cruzable a boletín, el % de estado de cancelación y las semanas ISO distintas están tabulados; los números concuerdan con una re-corrida de las queries (o la deriva está anotada).</done>
</task>

<task type="auto">
  <name>Task 2: Veredictos de endpoints (curl re-sondeo) + hallazgos confirmados/refutados + frescura</name>
  <files>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md</files>
  <read_first>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md (§ Sondeo Empírico de Endpoints, § Confirmación/Refutación), packages/agenda/src/connector-camara.ts, packages/agenda/src/connector-senado.ts, packages/freshness/src/catalog.ts:253-260</read_first>
  <action>
    Añadir al reporte "2. Veredictos de endpoints" y "3. Hallazgos confirmados/refutados".
    Re-sondear cada endpoint del bloque <interfaces> con curl (UA `ObservatorioCongreso360/1.0 (contacto: sanchez.rossi@gmail.com)`, ≥3s entre hosts distintos, rate-limit LOCKED — NUNCA ráfagas). Para el PDF de Cámara incluir `Referer: https://www.camara.cl/legislacion/sala_sesiones/tabla.aspx`. Registrar por endpoint: URL, HTTP code, veredicto UP/DOWN/PARCIAL, histórico SÍ/NO, y una muestra RECORTADA de evidencia (primeros bytes / conteo de `article.citaciones` / mensaje de mantención). Total ≤ ~12 requests.
    En "3. Hallazgos": para cada uno de los 3 hallazgos previos ((a) Senado comisiones forward-only; (b) Cámara sala thin PDF→DeepSeek; (c) comisiones unidas/especiales por fuente) escribir veredicto CONFIRMADO/REFUTADO citando la medición concreta (probe # + N de DB). Incluir la refutación NUEVA: "Cámara citaciones tiene histórico" (probe prmSemana=2026-20 devuelve 200 con días de mayo). Para comisiones unidas/especiales anotar presencia en Senado ("Comisión Mixta para Boletín N°...", "Comisión Especial de Zonas Extremas") y en Cámara (aparecen como filas normales en citaciones_semana — confirmar en el HTML sondeado).
    Añadir "4. Frescura": verificar que `.github/workflows/agenda-weekly.yml` existe; leer qué secrets declara (DEEPSEEK/R2/SUPABASE) y su schedule; anotar si es un gap de frescura (cron ausente/sin secrets/no corre). Cruzar con la señal `fuente: "agenda"` de catalog.ts (umbral 7d).
  </action>
  <verify>
    <automated>test -f .github/workflows/agenda-weekly.yml && grep -q "Veredictos de endpoints" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md" && grep -qi "REFUTADO" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md" && grep -qi "forward-only" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-AUDITORIA-CITACIONES.md"</automated>
  </verify>
  <done>Existen las secciones 2/3/4: cada endpoint tiene HTTP code + veredicto + histórico + muestra; los 3 hallazgos previos + la refutación de Cámara-histórico están declarados citando la medición; la frescura del cron agenda-weekly está verificada (existe/corre/secrets) o declarada como gap.</done>
</task>

</tasks>

<verification>
- `93-AUDITORIA-CITACIONES.md` existe con secciones 1-4 pobladas.
- Las queries psql del reporte re-corren y concuerdan (o la deriva está anotada).
- Los veredictos de endpoints son re-ejecutables con el UA declarado.
- Ningún número presentado como "cobertura completa"; cada celda declara THIN o AL DÍA-en-su-ventana.
</verification>

<success_criteria>
- Matriz N/M de 4 celdas con query verbatim, rango y M declarado.
- Veredicto por endpoint con evidencia + histórico sí/no.
- 3 hallazgos confirmados/refutados citando medición + refutación Cámara-histórico.
- % estado de cancelación poblado + frescura del cron verificada.
</success_criteria>

<output>
Crear `.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-01-SUMMARY.md` al terminar.
</output>
