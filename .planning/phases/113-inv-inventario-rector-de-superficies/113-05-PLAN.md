---
phase: 113-inv-inventario-rector-de-superficies
plan: 05
type: execute
wave: 5
depends_on: ["113-04"]
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDACION-OPUS.md
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "El checklist automatizable pasa en modo estricto (STRICT=1)"
    - "Un validador Opus independiente evaluó los 7 criterios y su veredicto está registrado con evidencia"
    - "Todo hallazgo del validador quedó cerrado en el inventario o registrado como límite explícito"
    - "La suite existente sigue verde (no-regresión: esta fase no toca código de producto)"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDACION-OPUS.md"
      provides: "Veredicto por criterio (1-7) con evidencia y hallazgos"
      contains: "Criterio 7"
  key_links:
    - from: "113-VALIDACION-OPUS.md"
      to: "113-INVENTARIO.md"
      via: "veredicto por criterio con cita de sección"
      pattern: "113-INVENTARIO"
---

<objective>
Gate de cierre de la fase: correr el checklist en modo **estricto**, someter `113-INVENTARIO.md` a un **validador Opus independiente** con los 7 criterios del `113-VALIDATION.md`, cerrar los hallazgos y confirmar no-regresión de la suite.

Purpose: el inventario es RECTOR de 114/115/116/122/125 — un hueco aquí se propaga a cinco fases. El CONTEXT LOCKEA la validación por Opus ANTES de cerrar la fase.
Output: `113-VALIDACION-OPUS.md` con veredicto por criterio; `113-INVENTARIO.md` con los hallazgos cerrados y estado final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDATION.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-CONTEXT.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checklist estricto + no-regresión de la suite</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    Correr `STRICT=1 bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh` y cerrar toda falta que reporte (rutas ausentes, not-found ausentes, builders no citados, `` ```sql `` < 5, sección Cobertura ausente).

    Correr las verificaciones de higiene sobre el documento y dejarlas registradas: cero credenciales (`grep -E 'postgres(ql)?://' 113-INVENTARIO.md` sin match), cero RUT (`grep -E '[0-9]{7,8}-[0-9kK]'` sin match), cero celdas vacías (`grep -c '||'` == 0), cero ids `E-NNN` duplicados.

    Correr `pnpm test` como no-regresión (esta fase no toca código de producto; el resultado esperado es idéntico al baseline). Si algo falla, verificar que sea deuda pre-existente documentada y NO causada por esta fase — jamás "arreglarlo" aquí (la fase no corrige).
  </action>
  <verify>
    <automated>STRICT=1 bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</automated>
  </verify>
  <acceptance_criteria>
    - `STRICT=1 bash check-inventario.sh` exit 0 con los 5 checks en OK
    - `grep -E 'postgres(ql)?://' 113-INVENTARIO.md` sin match
    - `grep -E '[0-9]{7,8}-[0-9kK]' 113-INVENTARIO.md` sin match
    - `grep -c '||' 113-INVENTARIO.md` == 0
    - `grep -o 'E-[0-9]\{3\}' 113-INVENTARIO.md | sort | uniq -d` vacío
    - `pnpm test` con el mismo resultado que el baseline (cero fallos nuevos atribuibles a esta fase)
  </acceptance_criteria>
  <done>Checklist estricto verde, higiene de seguridad verificada y suite sin regresión.</done>
</task>

<task type="auto">
  <name>Task 2: Validador Opus — 7 criterios de aceptación</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDACION-OPUS.md, .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDATION.md §Criterios de aceptación del validador Opus (los 7)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md (completo)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md (listas cerradas de RPCs/tablas/builders/gates)
  </read_first>
  <action>
    Lanzar un agente validador con modelo Opus, INDEPENDIENTE de quien escribió el inventario (no reutilizar el contexto de los planes 01-04): recibe `113-INVENTARIO.md`, `113-RESEARCH.md` y `113-VALIDATION.md`, y emite veredicto PASS/FAIL por cada uno de los 7 criterios, con evidencia citada (sección + comando):

    1. **Rutas** — las 15 de `find` están; `/admin/revisar-entidades` EXCLUIDA con razón; `/cuenta` y `/notificaciones/*` marcadas por naturaleza; las 4 `not-found.tsx` apendizadas.
    2. **Links internos** — toda plantilla `href="/..."` del catálogo aparece en al menos una ruta; el chrome inventariado una vez y referenciado.
    3. **Links externos** — los 4 builders (`buildSenadoUrl`, `buildCamaraUrl`, `enlaceHumanoProyecto`, `partidoLegible`) citados con plantilla verbatim; toda familia de URL-desde-columna declarada con su columna; `safeExternalHref` declarado chokepoint; clasificación por fuente (camara/senado/BCN/leylobby/otro) completa.
    4. **Fechas** — toda fecha lleva formatter + origen (`RPC.campo` o `tabla.columna`); las que van por `capturedAt`/`ProvenanceBadge` MARCADAS como `fecha_captura`; los nombres de RPC/tabla pertenecen a las listas cerradas del research.
    5. **Sujetos** — 5 sujetos con query verbatim + resultado inline + URL PROD + `ORDER BY` con desempate; ancla temporal y deploy declarados; PK bio en formato string.
    6. **Cobertura** — Tabla D sin celdas vacías; límite "links externos desde columnas" declarado; cero rutas sin evidencia.
    7. **Régimen** — el documento no corrige nada; `fecha_captura` jamás presentada como el hecho (ni en ejemplos); ninguna URL de conexión impresa; cero PII.

    Escribir `113-VALIDACION-OPUS.md` con el veredicto por criterio, los hallazgos y su disposición. Cerrar en `113-INVENTARIO.md` todo hallazgo que sea una omisión real. Si un hallazgo NO puede cerrarse dentro del alcance (p. ej. requiere DOM real), registrarlo como límite explícito en §0 y remitirlo a la fase que lo cubre (114/115/125) — nunca silenciarlo. Marcar el front-matter del inventario como `estado: validado` solo si los 7 criterios quedan en PASS; si alguno queda FAIL sin cierre posible, marcar `estado: validado con límites` y enumerarlos.
  </action>
  <verify>
    <automated>test -f .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDACION-OPUS.md &amp;&amp; grep -c 'Criterio' .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDACION-OPUS.md</automated>
  </verify>
  <acceptance_criteria>
    - `113-VALIDACION-OPUS.md` contiene los 7 criterios, cada uno con veredicto PASS o FAIL y evidencia citada (sección del inventario + comando)
    - Todo hallazgo tiene disposición: `cerrado en el inventario` o `límite declarado → Phase NNN`
    - El front-matter de `113-INVENTARIO.md` queda en `estado: validado` o `estado: validado con límites` con los límites enumerados
    - `STRICT=1 bash check-inventario.sh` sigue en exit 0 tras los cierres
    - Ningún cierre de hallazgo modificó código de producto: `git status --porcelain app/ packages/` vacío
  </acceptance_criteria>
  <done>Veredicto Opus registrado por criterio, hallazgos cerrados o declarados como límite, inventario marcado como validado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documento final → repo público | último punto donde una credencial o PII podría escaparse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | inventario final | mitigate | Criterio 7 del validador + greps de higiene en Task 1 (cero `postgres://`, cero patrón RUT, cero email fuera del `mailto:` público) |
| T-113-09 | Tampering | código de producto | mitigate | La fase no corrige: `git status --porcelain app/ packages/` debe quedar vacío tras los cierres |
| T-113-04 | Information Disclosure | superficie admin | mitigate | Criterio 1 verifica que `/admin/revisar-entidades` figure EXCLUIDA |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes |
</threat_model>

<verification>
- `STRICT=1 bash check-inventario.sh` exit 0
- `113-VALIDACION-OPUS.md` con 7 criterios evaluados
- `git status --porcelain app/ packages/` vacío
- `pnpm test` sin fallos nuevos
</verification>

<success_criteria>
El inventario pasa el checklist estricto y el gate del validador Opus con los 7 criterios evaluados y evidenciados; los hallazgos están cerrados o declarados como límite con la fase que los cubre; cero cambios en código de producto; LINK-01 satisfecho.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-05-SUMMARY.md` when done
</output>
