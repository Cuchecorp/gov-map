---
phase: 113-inv-inventario-rector-de-superficies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
  - .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "El inventario declara el método de enumeración con comandos verbatim re-ejecutables"
    - "El inventario declara el estado REAL de los 5 feature-gates en el deploy auditado (verificado, no copiado de STATE)"
    - "Los 5 sujetos deterministas están elegidos por SQL con ORDER BY desempatado, con query verbatim + resultado inline + URL PROD"
    - "Existe un script de checklist rutas-vs-filesystem re-ejecutable en < 2 s"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md"
      provides: "Secciones 0 (método/cobertura), 1 (sujetos), 5 (gates) + esqueleto de 2/3/4"
      contains: "## 1. Sujetos deterministas"
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh"
      provides: "Checklist automatizable rutas/tablas/builders/sujetos/cobertura"
  key_links:
    - from: "113-INVENTARIO.md §1"
      to: "PROD Postgres"
      via: "psql read-only con .env"
      pattern: "```sql"
---

<objective>
Fundar `113-INVENTARIO.md` con sus tres secciones de base: **§0 Método y cobertura** (comandos verbatim), **§1 Sujetos deterministas** (5 sujetos elegidos por SQL contra PROD) y **§5 Gates y su estado** (verificado empíricamente, no asumido de STATE — assumption A1 del research). Además crear el script de checklist que las waves siguientes correrán tras cada task.

Purpose: sin sujetos deterministas ni estado real de gates, las secciones de rutas (waves 3-4) documentarían superficies falsas y las fases 114/115/116/122/125 apuntarían a sujetos fantasma.
Output: `113-INVENTARIO.md` con §0, §1, §5 escritas y §2/§3/§4 como headers-placeholder; `check-inventario.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-CONTEXT.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Esqueleto del inventario + script de checklist (Wave 0)</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md, .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</files>
  <read_first>
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md §Architecture Patterns (estructura de 3 capas, Tablas A/B/C/D)
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-VALIDATION.md §Per-Task Verification Map y §Wave 0 Gaps
    - .planning/phases/113-inv-inventario-rector-de-superficies/113-CONTEXT.md §decisions
  </read_first>
  <action>
    Crear `113-INVENTARIO.md` con front-matter (`phase: 113`, `deploy_auditado:`, `fecha_corrida:`, `estado: en construcción`) y los 6 headers de la estructura LOCKED del research: `## 0. Método y cobertura`, `## 1. Sujetos deterministas`, `## 2. Chrome compartido`, `## 3. Catálogo de emisores`, `## 4. Las 15 rutas`, `## 5. Gates y su estado`. Las secciones 2/3/4 quedan con la marca literal `_(pendiente — Plan 02/03/04)_`.

    En §0 escribir: (a) los comandos verbatim del research §Code Examples — `find app/app -name "page.tsx" | sort` (15), `find app/app -name "not-found.tsx" | sort` (4), el loop de conteo de `<Link|href=`, el grep de formatters (`Intl.DateTimeFormat|toLocaleDateString|toLocaleString|fechaCorta|relativeTimeEs|fechaCortaSegura|diaCalendario`), `grep -rln "ProvenanceBadge"`, `grep -rl "sourceUrl=" app/components app/app` (call-sites de link externo del badge), el grep de `.rpc(` y el de `.from("`, y `find app/app -name "route.ts"` — este último devuelve **0 archivos**, lo que cierra por vacío la decisión LOCKED "route handlers no son superficies" (registrar el comando y el 0 como evidencia, no como suposición); (b) la declaración de régimen: el método es ANÁLISIS DE CÓDIGO (verificación DOM = 114/125), la fase NO corrige nada, cero requests a fuentes gubernamentales (no aplica rate-limit 2-3s), solo SELECT read-only a PROD, jamás se imprime `SUPABASE_DB_URL`, cero PII (ni RUT ni email ni monto individual); (c) el LÍMITE declarado del método: los links externos mayoritariamente NO se construyen en TSX sino que vienen de columnas de la DB (`proyecto.enlace`, `url_fuente`, `enlace_fuente`, `link_*`) — enumerarlos solo desde código sería incompleto; (d) el placeholder de la Tabla D (cobertura método×ruta) que llena el Plan 04. Encabezado de Tabla D: `| ruta | links enumerados por | fechas enumeradas por | sujeto usado | ¿exhaustivo o muestra? | evidencia |`.

    Crear `check-inventario.sh` (bash POSIX, sin `grep -P`) con 5 checks que impriman `OK`/`FALTA ...` y salgan 1 si hay faltas: (1) cada ruta de `find app/app -name page.tsx` aparece en el inventario; (2) cada `not-found.tsx` de `find app/app -name not-found.tsx` aparece; (3) los 4 builders `buildSenadoUrl`, `buildCamaraUrl`, `enlaceHumanoProyecto`, `partidoLegible` están citados; (4) `grep -c '```sql'` ≥ 5; (5) existe la cadena `Cobertura`. **Matching robusto (obligatorio):** las rutas contienen `[id]` y `[boletin]`, que en BRE son clases de carácter y dan falsos positivos/negativos — usar SIEMPRE `grep -qF -- "$ruta"` (literal), nunca `grep -q`. La ruta raíz `/` matchea cualquier línea: tratarla como caso especial buscando el literal `### 4.4 /` (su header de sección) en vez del string `/`. Derivar la ruta con `ruta="${archivo#app/app}"; ruta="${ruta%/page.tsx}"; [ -z "$ruta" ] && ruta="/"`.

    Los checks 1-2 deben tolerar que las secciones aún no existan durante la wave 1 — usar una variable `STRICT=${STRICT:-0}` que, en 0, reporte faltas sin fallar, y en 1 falle. Documentar en el header del script que las waves 2-4 lo corren con `STRICT=0` y el Plan 05 con `STRICT=1`.

    **Baseline de no-regresión:** correr `pnpm test` UNA vez y registrar en §0 el conteo exacto observado (tests pasados por workspace app y por packages) bajo el rótulo `Baseline de suite (pre-fase)`, con la fecha. El Plan 05 compara contra ese número real, no contra una cifra recordada.
  </action>
  <verify>
    <automated>bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh; test -f .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -c '^## ' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' 113-INVENTARIO.md` devuelve exactamente 6
    - `grep -q 'no corrige' 113-INVENTARIO.md` y `grep -q 'Cobertura' 113-INVENTARIO.md` pasan
    - `grep -q 'SUPABASE_DB_URL' 113-INVENTARIO.md` NO encuentra ningún valor de conexión (solo el nombre de la variable, nunca su valor)
    - `bash check-inventario.sh` con STRICT=0 termina con exit 0 e imprime al menos 5 líneas de check
    - `bash -n check-inventario.sh` exit 0
    - El script usa `grep -qF --` para las rutas (`grep -n 'grep -qF' check-inventario.sh` con match) y trata `/` como caso especial
    - §0 registra `find app/app -name "route.ts"` → `0` como evidencia
    - §0 contiene el rótulo `Baseline de suite (pre-fase)` con conteos numéricos de `pnpm test` y fecha
  </acceptance_criteria>
  <done>`113-INVENTARIO.md` existe con 6 secciones, §0 completa con comandos verbatim y régimen declarado; `check-inventario.sh` corre en &lt; 2 s.</done>
</task>

<task type="auto">
  <name>Task 2: §1 Sujetos deterministas por SQL contra PROD</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - .planning/milestones/v9.0-phases/93-agenda-p2d-*/93-WIRING-EVIDENCIA.md §0 (molde exacto a replicar)
    - 113-RESEARCH.md §Sujetos deterministas por SQL + Pitfalls 4, 6, 7, 8
    - .env (solo para `source`; JAMÁS imprimir `SUPABASE_DB_URL`)
  </read_first>
  <action>
    Elegir los 5 sujetos con psql read-only contra PROD, invocando siempre así (sin ecoar la variable): `set -a; source .env; set +a` y luego `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "<query>"`.

    Sujetos (per D-CONTEXT §Sujetos dinámicos por SQL): (1) Parlamentario A diputado con máxima riqueza de bloques VISIBLES — votos + lobby + patrimonio + cruces + comisiones + militancias; MONEY OFF ⇒ contratos/aportes NO cuentan como criterio de riqueza. (2) Parlamentario B senador, mismo criterio. (3) Boletín A con votaciones + similares (`proyecto_embedding`) + cruces. (4) Boletín B de zona solo-Senado (`proyecto where prm_id_camara is null`) para ejercitar la rama sin `buildCamaraUrl`. (5) Contraparte con agregados no vacíos (fuente de `agregado_por_contraparte`).

    Reglas duras: todo `ORDER BY` lleva desempate estable (`ORDER BY <metrica> DESC, <pk> ASC LIMIT 1`) — Pitfall 7; todo `max(fecha)`/ventana filtra `fecha <= current_date` (hay filas corruptas `2626-05-25`) — Pitfall 8; nunca contar por REST (cap 1k) — Pitfall 4; el id de parlamentario se registra en formato PK string (`S1344`), nunca el `parlid_senado` numérico — Pitfall 6.

    Escribir `## 1. Sujetos deterministas` replicando LITERALMENTE el formato del molde 93 §0: por sujeto, un bloque ```sql con la query verbatim y el resultado como comentario inline (`-- S1344|…`), seguido de la URL PROD completa a visitar (`https://observatorio-congreso.thevalis.workers.dev/parlamentario/<id>` etc.) y una línea de expectativa declarada (qué bloques debería mostrar). Cerrar §1 con el ancla temporal: `now()::date` de la corrida y el deploy auditado.

    Si `SUPABASE_DB_URL` no estuviera disponible, NO inventar sujetos: escribir `sujetos no elegidos — causa: <X>` y surfacear el bloqueo (degradar honesto, per §Environment Availability).
  </action>
  <verify>
    <automated>test $(grep -c '```sql' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md) -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '```sql'` ≥ 5 y cada bloque contiene la cadena `ORDER BY` con al menos dos claves (métrica + desempate)
    - Cada uno de los 5 sujetos tiene: query verbatim, resultado comentado inline (`--`), URL PROD completa y expectativa declarada
    - El id de senador aparece en formato string (patrón `S<dígitos>`), no como entero suelto
    - Toda query con `max(` incluye también `current_date`
    - `grep -q 'postgres://\|postgresql://\|@aws-\|bctyygbmqcvizyplktuw' 113-INVENTARIO.md` NO devuelve nada (cero credenciales/URL de conexión)
    - Cero RUT, email o monto individual en §1
  </acceptance_criteria>
  <done>§1 con 5 sujetos deterministas reproducibles, ancla temporal y deploy declarados; cero credenciales y cero PII en el documento.</done>
</task>

<task type="auto">
  <name>Task 3: §5 Gates verificados empíricamente en el deploy auditado</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/lib/net-gate.ts, app/lib/cruces-gate.ts, app/lib/vsim-gate.ts, app/lib/money-gate.ts, app/lib/notif-gate.ts
    - app/components/header-nav.tsx (filtro de `/red` cuando NET está OFF)
    - 113-RESEARCH.md §Feature gates que suprimen superficies + Assumptions Log A1 + Open Question 3
  </read_first>
  <action>
    Verificar el estado REAL de los 5 gates contra el deploy `https://observatorio-congreso.thevalis.workers.dev` en vez de copiarlo de STATE (assumption A1). Método de observación sin BrowserOS: `curl -s` de las superficies testigo y grep del HTML — NET: ¿aparece `href="/red"` en el nav de `/`? CRUCES: ¿aparece el bloque de cruces en la ficha de proyecto del sujeto? VSIM: ¿aparece el eje de coincidencia de votos en `/comparar`? MONEY: ¿aparece bloque de financiamiento/contratos/aportes en la ficha de parlamentario? NOTIF: ¿aparece el botón de seguir en las fichas? Registrar el comando y el conteo observado (p. ej. `0 ocurrencias`) como evidencia inline.

    Escribir `## 5. Gates y su estado` con la tabla de columnas fijas: `| gate | env var | chokepoint (archivo:línea) | estado observado | evidencia (comando + resultado) | efecto en links/fechas |`. Los 5 chokepoints son `lib/net-gate.ts`, `lib/cruces-gate.ts`, `lib/vsim-gate.ts`, `lib/money-gate.ts`, `lib/notif-gate.ts`; declarar que los 5 son server-only (sin prefijo `NEXT_PUBLIC_`, el flag jamás llega al bundle). Declarar la REGLA del inventario: cada bloque de las secciones 3 y 4 lleva columna `gate` con valor de la lista cerrada `—|NET|CRUCES|VSIM|MONEY|NOTIF`, y un bloque con gate OFF se inventaría igual pero se marca con la cadena literal `no emitido en el deploy auditado`. Si alguna observación resultara ambigua, registrar `estado observado: indeterminado` con la causa — nunca inventar el estado.

    Registrar en el front-matter del documento el `deploy_auditado` observado y la fecha de la corrida.
  </action>
  <verify>
    <automated>grep -c 'net-gate\|cruces-gate\|vsim-gate\|money-gate\|notif-gate' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - La tabla §5 tiene exactamente 5 filas de datos (una por gate) y ninguna celda vacía
    - Los 5 nombres de env var `NET_PUBLIC_ENABLED`, `CRUCES_PUBLIC_ENABLED`, `VSIM_PUBLIC_ENABLED`, `MONEY_PUBLIC_ENABLED`, `NOTIF_PUBLIC_ENABLED` aparecen
    - Cada fila tiene evidencia con comando + resultado observado (no la palabra "según STATE")
    - La cadena literal `no emitido en el deploy auditado` está definida como convención en §5
    - `grep -q 'NEXT_PUBLIC_' ` sobre §5 solo aparece en la frase que declara su AUSENCIA
  </acceptance_criteria>
  <done>§5 completa con estado observado por evidencia para los 5 gates y la convención de la columna `gate` declarada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entorno local → PROD Postgres | credenciales de `.env` cruzan hacia la DB de producción |
| PROD (datos) → documento `.md` versionado en git | datos reales quedan escritos en un artefacto público del repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | invocación psql (Task 3) | mitigate | `set -a; source .env; set +a` + `psql "$SUPABASE_DB_URL"` sin `echo`; acceptance criteria greppea que ninguna URL de conexión quede en el `.md` |
| T-113-02 | Information Disclosure | contenido del inventario | mitigate | Cero PII: solo ids públicos y boletines; prohibido RUT/email/monto individual; nombres de columna sí, valores de columnas PII no |
| T-113-03 | Tampering | PROD Postgres | mitigate | Solo `SELECT`; cero DDL/DML en todo el plan |
| T-113-04 | Information Disclosure | superficies gated | mitigate | §5 declara estado observado por gate; `/admin/revisar-entidades` se marca EXCLUIDA (Plan 04) — el inventario nunca presenta lo gated como público |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala ningún paquete (research §Package Legitimacy Audit: no aplica) |
</threat_model>

<verification>
- `bash check-inventario.sh` (STRICT=0) exit 0
- `grep -c '^## ' 113-INVENTARIO.md` == 6
- `grep -c '```sql' 113-INVENTARIO.md` ≥ 5
- Cero credenciales: `grep -q 'postgres://\|postgresql://' 113-INVENTARIO.md` sin match
</verification>

<success_criteria>
§0, §1 y §5 completas; método y régimen declarados; 5 sujetos deterministas reproducibles con evidencia; estado de los 5 gates observado y no asumido; script de checklist operativo para las waves siguientes.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-01-SUMMARY.md` when done
</output>
