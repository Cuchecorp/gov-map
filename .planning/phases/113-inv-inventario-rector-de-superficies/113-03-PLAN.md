---
phase: 113-inv-inventario-rector-de-superficies
plan: 03
type: execute
wave: 4
depends_on: ["113-06"]
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "Las 3 rutas dinámicas densas están inventariadas con sujeto concreto de §1 y sus 3 tablas (internos / externos / fechas)"
    - "Toda fecha de estas rutas lleva formatter + origen y las de fecha_captura están MARCADAS"
    - "Los links externos distinguen builder en código vs columna de la DB"
    - "Las not-found.tsx de estas rutas están apendizadas como sub-superficie"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md"
      provides: "§4.1 /parlamentario/[id], §4.2 /proyecto/[boletin], §4.3 /contraparte/[id]"
      contains: "### 4."
  key_links:
    - from: "§4 rutas densas"
      to: "§3 catálogo"
      via: "referencia por id"
      pattern: "E-[0-9]{3}"
---

<objective>
Inventariar las **3 rutas dinámicas más densas** —`/parlamentario/[id]`, `/proyecto/[boletin]`, `/contraparte/[id]`— instanciadas con los sujetos concretos elegidos en §1, cada una con las tablas A (links internos), B (links externos) y C (fechas), más sus `not-found.tsx` como sub-superficie.

Purpose: son las rutas que concentran links externos a fuente y fechas con semántica de captura; son el insumo principal de 115, 116 y 122.
Output: §4.1, §4.2 y §4.3 de `113-INVENTARIO.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4.1 /parlamentario/[id] (ficha 360)</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx y app/app/parlamentario/[id]/not-found.tsx
    - app/components/{parlamentario-header, votos-por-parlamentario, lobby-de-parlamentario, patrimonio-de-parlamentario, cruces-de-parlamentario, comisiones-de-parlamentario, militancias-de-parlamentario, financiamiento-de-parlamentario, contratos-de-parlamentario, partido-chip, ficha-header}.tsx
    - 113-INVENTARIO.md §1 (sujetos A y B), §3 (catálogo), §5 (gates)
  </read_first>
  <action>
    Escribir `### 4.1 /parlamentario/[id]` encabezada por: tipo (pública, dinámica), sujeto usado (los ids string de §1 — diputado A y senador B, nunca el `parlid_senado` numérico) y URL PROD de cada uno.

    Tres tablas de columnas fijas LOCKED:
    - **Tabla A (internos)**: `| # | href (plantilla) | emisor (archivo:línea o → E-NNN) | ancla #id? | condicional/gate | ruta destino |`
    - **Tabla B (externos)**: `| # | fuente (camara/senado/BCN/leylobby/otro) | plantilla o columna de origen | builder o \`columna\` | parámetro | emisor (archivo:línea o → E-NNN) | gate |`
    - **Tabla C (fechas)**: `| # | etiqueta visible | formatter | origen (RPC.campo / tabla.columna) | ¿es fecha_captura? | vía ProvenanceBadge? | gate | emisor |`

    Reglas: el chrome NO se repite — se referencia `→ C-01..C-04`; los componentes se referencian `→ E-NNN` en vez de re-describirse. Bloques con gate `MONEY` (financiamiento/contratos/aportes) se inventarían igual pero con la marca literal `no emitido en el deploy auditado`; lo mismo `NOTIF` para `SeguirButton`. Toda fecha que llegue por prop `capturedAt` se marca `sí` en "¿es fecha_captura?" y `sí` en "vía ProvenanceBadge". Los nombres de origen salen de las listas cerradas de §3 — cero invención.

    **Regla de badge (BLOCKER del checker):** `ProvenanceBadge` emite también un link EXTERNO vía su prop `sourceUrl` (`<a href={safeExternalHref(sourceUrl)}>` — `provenance-badge.tsx:25,37,62`). En cada ruta, TODA instancia del badge aporta UNA fila a la **Tabla B** (link externo, columna "builder o `columna`" = la columna de origen del `sourceUrl` según la tabla de call-sites de §3.1) ADEMÁS de su fila en la Tabla C (fecha). Un badge que aparezca solo en Tabla C es un defecto de inventario.

    Apendizar `#### 4.1.b not-found.tsx` con la tabla A del `not-found.tsx` de la ruta (1 href), declarando que es la misma ruta en estado 404.
  </action>
  <verify>
    <automated>grep -q '### 4.1' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -c 'fecha_captura' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - §4.1 contiene exactamente 3 tablas de datos (A, B, C) con los encabezados verbatim de arriba, más la sub-tabla de not-found
    - Cero celdas vacías en las 3 tablas: `grep -nE '\|[[:space:]]*\|' 113-INVENTARIO.md` sin match (usar `—` cuando no aplica)
    - Toda instancia de `ProvenanceBadge` de la ruta aporta fila en Tabla B (link externo) Y en Tabla C (fecha)
    - Todos los bloques `MONEY`/`NOTIF` llevan la cadena literal `no emitido en el deploy auditado`
    - Toda fila de Tabla C tiene formatter no vacío y origen que matchea una entrada de las listas cerradas
    - Los ids de sujeto usados coinciden byte a byte con los registrados en §1 y el senador está en formato `S<dígitos>`
    - `not-found.tsx` de la ruta aparece con su href
  </acceptance_criteria>
  <done>§4.1 completa con las 3 tablas, sujetos concretos, gates marcados y not-found apendizado.</done>
</task>

<task type="auto">
  <name>Task 2: §4.2 /proyecto/[boletin] (la más densa en links externos)</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/proyecto/[boletin]/page.tsx y app/app/proyecto/[boletin]/not-found.tsx
    - app/components/{validacion-fuente, voto-ficha-row, votacion-card, timeline-view, timeline-event, capa1/tramitacion-stepper, estado-actual-block, idea-matriz-block, autor-row, cruces-de-proyecto, lobby-en-tramitacion, lobby-menciones-de-boletin, citacion-card, sala-table-section}.tsx
    - 113-INVENTARIO.md §1 (boletín A y boletín B solo-Senado), §3.2 (builders), §5
  </read_first>
  <action>
    Escribir `### 4.2 /proyecto/[boletin]` con las mismas 3 tablas de columnas fijas de §4.1, instanciada con AMBOS boletines de §1: boletín A (con votaciones + similares + cruces) y boletín B (zona solo-Senado, `prm_id_camara is null`).

    Precisiones obligatorias de esta ruta:
    - En Tabla B, el link al Senado se registra **post-rewrite** de `enlaceHumanoProyecto` (si `proyecto.enlace` apunta a `tramitacion.senado.cl/wspublico/` el link emitido es el de `buildSenadoUrl`, no el crudo). Registrar la fila con ambos estados: valor de la columna y href finalmente emitido.
    - `buildCamaraUrl` solo emite cuando `prm_id_camara != null` → el boletín B ejercita la rama SIN link a Cámara. Registrar esa diferencia explícitamente por sujeto.
    - En Tabla C, la fecha de frescura de la ficha proviene del `fecha_captura` MÁS RECIENTE del set de eventos — marcarla como `fecha_captura` y NUNCA describirla como fecha del hecho; las fechas de tramitación provienen de `tramitacion_evento.fecha` (el hecho).
    - Bloques `CRUCES` y los de lobby llevan su gate según §5.

    Apendizar `#### 4.2.b not-found.tsx`: emite 3 hrefs, dos de ellos externos (senado.cl y camara.cl) — registrarlos en su propia tabla B, ya que son links externos que 115 debe validar.
  </action>
  <verify>
    <automated>grep -q '### 4.2' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'enlaceHumanoProyecto' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - §4.2 documenta ambos sujetos (boletín A y B) y declara para B la ausencia de `buildCamaraUrl`
    - La fila del link a Senado registra el href post-rewrite y la columna de origen (`proyecto.enlace`)
    - La fecha de frescura está marcada `¿es fecha_captura? sí` y su etiqueta visible no la presenta como el hecho
    - La sub-tabla de `not-found.tsx` lista 3 hrefs, con los 2 externos clasificados por fuente
    - Cero celdas vacías (`grep -nE '\|[[:space:]]*\|'` sin match); orígenes dentro de las listas cerradas
    - Toda instancia de `ProvenanceBadge` aporta fila en Tabla B y en Tabla C
  </acceptance_criteria>
  <done>§4.2 completa con ambos boletines, rewrite documentado y not-found con sus externos.</done>
</task>

<task type="auto">
  <name>Task 3: §4.3 /contraparte/[id]</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/contraparte/[id]/page.tsx y app/app/contraparte/[id]/not-found.tsx
    - app/components/{aportes-por-contraparte, contratos-por-contraparte}.tsx
    - 113-INVENTARIO.md §1 (sujeto contraparte), §5 (gate MONEY)
  </read_first>
  <action>
    Escribir `### 4.3 /contraparte/[id]` con las 3 tablas de columnas fijas, instanciada con el sujeto contraparte de §1 y su URL PROD. Registrar que los bloques de aportes y contratos están bajo gate `MONEY` (OFF) → marca literal `no emitido en el deploy auditado`, pero se inventarían igual porque existen en código (su copy de fecha lo audita 116). Las fechas de esos bloques (`fecha_captura: string` en los tipos de aportes/contratos) se marcan como `fecha_captura`.

    Apendizar `#### 4.3.b not-found.tsx` (1 href).

    Registrar en el documento, además, la nota de PII: los nombres de columna de tablas PII se citan, sus VALORES no; el inventario no contiene RUT, email ni monto individual.
  </action>
  <verify>
    <automated>grep -q '### 4.3' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</automated>
  </verify>
  <acceptance_criteria>
    - §4.3 tiene las 3 tablas + sub-tabla de not-found; `grep -nE '\|[[:space:]]*\|'` sin match
    - Toda instancia de `ProvenanceBadge` aporta fila en Tabla B y en Tabla C
    - Los bloques MONEY llevan la marca literal `no emitido en el deploy auditado`
    - Cero RUT, email o monto individual en toda la sección (`grep -E '[0-9]{7,8}-[0-9kK]'` sin match en el archivo)
    - `bash check-inventario.sh` (STRICT=0) exit 0
  </acceptance_criteria>
  <done>§4.3 completa; las 3 rutas dinámicas densas inventariadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de PROD (sujetos) → documento en git | los sujetos concretos aparecen citados en el inventario |
| superficies gated → documento público | riesgo de presentar lo gated como público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | Information Disclosure | §4.1–4.3 | mitigate | Solo ids públicos (parlamentario, boletín, contraparte); prohibido RUT/email/monto individual; acceptance criteria greppea el patrón de RUT |
| T-113-04 | Information Disclosure | bloques gated MONEY/NOTIF | mitigate | Marca literal `no emitido en el deploy auditado` en cada fila con gate OFF, según §5 |
| T-113-07 | Repudiation | trazabilidad de la evidencia | mitigate | Cada fila lleva `archivo:línea` o referencia `E-NNN`; cero filas sin emisor |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes |
</threat_model>

<verification>
- `bash check-inventario.sh` (STRICT=0) exit 0
- `grep -c '### 4\.' 113-INVENTARIO.md` ≥ 3
- Cero celdas vacías en las tablas de §4.1–4.3: `grep -nE '\|[[:space:]]*\|' 113-INVENTARIO.md` sin match
</verification>

<success_criteria>
Las 3 rutas dinámicas densas inventariadas con sujetos concretos, tablas A/B/C completas, gates marcados, rewrite de `enlaceHumanoProyecto` documentado y `not-found.tsx` apendizados.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-03-SUMMARY.md` when done
</output>
