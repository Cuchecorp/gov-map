---
phase: 113-inv-inventario-rector-de-superficies
plan: 04
type: execute
wave: 5
depends_on: ["113-03"]
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "Las 12 rutas restantes del universo LOCKED están inventariadas (incluida /admin/revisar-entidades como EXCLUIDA con razón)"
    - "/cuenta y /notificaciones/* están marcadas por su naturaleza (auth OTP / token-based)"
    - "Las 4 not-found.tsx del filesystem están apendizadas"
    - "La Tabla D de cobertura método×ruta está completa, sin celdas vacías y sin rutas asumidas"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md"
      provides: "§4.4–4.15 y §0 Tabla D completa"
      contains: "EXCLUIDA"
  key_links:
    - from: "§0 Tabla D"
      to: "§4 (15 rutas)"
      via: "una fila por ruta"
      pattern: "Cobertura"
---

<objective>
Cerrar el inventario: las **12 rutas restantes** del universo LOCKED (`/`, `/agenda`, `/buscar`, `/comparar`, `/parlamentarios`, `/red`, `/metodologia`, `/sobre`, `/cuenta`, `/notificaciones/baja`, `/notificaciones/confirmar`, `/admin/revisar-entidades`), las `not-found.tsx` restantes, y la **Tabla D de cobertura método×ruta** (success criterion 4 del ROADMAP).

Purpose: sin cobertura declarada, el inventario deja de ser el denominador completo y 114/115/116/122/125 no saben qué se enumeró exhaustivo vs por muestra.
Output: §4.4–4.15 y §0 con Tabla D completa; documento listo para el gate del validador Opus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-CONTEXT.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4.4–4.9 rutas públicas restantes</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/page.tsx, app/app/agenda/page.tsx, app/app/buscar/page.tsx, app/app/comparar/page.tsx, app/app/parlamentarios/page.tsx, app/app/red/page.tsx, app/app/red/not-found.tsx
    - app/components/panel-actualidad.tsx, actualidad-module.tsx, week-nav.tsx, citacion-card.tsx, sala-table-section.tsx, search-result-card.tsx, parlamentario-directory-row.tsx, red/red-graph.tsx
    - 113-INVENTARIO.md §1 (sujetos), §3 (catálogo E-NNN), §5 (gates)
  </read_first>
  <action>
    Escribir `### 4.4 /`, `### 4.5 /agenda`, `### 4.6 /buscar`, `### 4.7 /comparar`, `### 4.8 /parlamentarios`, `### 4.9 /red`, usando las MISMAS tablas A/B/C de columnas fijas definidas en §4.1 y referenciando chrome (`→ C-0N`) y emisores (`→ E-NNN`) en vez de re-describirlos.

    Precisiones por ruta:
    - `/`: panel de actualidad; fechas desde `RPC:actualidad_senales_panel`; hay `fecha_captura` en el módulo — marcarla.
    - `/agenda`: searchParams `semana` (`YYYY-Www`); en Tabla C registrar el gotcha LOCKED de que `citacion.fecha` es date-only medianoche UTC y la parte UTC ES el día chileno (helper `diaCalendario`) — el inventario REGISTRA el origen, no convierte; hay `fecha_captura` en citación y en sala.
    - `/buscar`: searchParams `q`, `page`; registrar el comentario LOCKED de que el año mostrado sale de `min(tramitacion_evento.fecha)` y JAMÁS de `fecha_captura`.
    - `/comparar`: ids validados contra `PARLAMENTARIO_ID_RE`; eje VSIM desde `RPC:coincidencia_votos_par` con el campo `fecha_captura_max` del par — marcarlo `fecha_captura`; gate `VSIM` según §5.
    - `/parlamentarios`: searchParams `q` (trim + cap `MAX_QUERY_CHARS`); links a fichas de parlamentario.
    - `/red`: gate `NET` según §5; añadir `#### 4.9.b not-found.tsx` con su href.

    Cero celdas vacías (usar `—`); orígenes exclusivamente de las listas cerradas de §3.

    **Regla de badge (BLOCKER del checker):** `ProvenanceBadge` emite también un link EXTERNO vía su prop `sourceUrl` (`<a href={safeExternalHref(sourceUrl)}>` — `provenance-badge.tsx:25,37,62`). En cada ruta, TODA instancia del badge aporta UNA fila a la **Tabla B** (link externo, columna "builder o `columna`" = la columna de origen del `sourceUrl` según la tabla de call-sites de §3.1) ADEMÁS de su fila en la Tabla C (fecha). Un badge que aparezca solo en Tabla C es un defecto de inventario.
  </action>
  <verify>
    <automated>for r in '### 4.4' '### 4.5' '### 4.6' '### 4.7' '### 4.8' '### 4.9'; do grep -q "$r" .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md || echo "FALTA $r"; done</automated>
  </verify>
  <acceptance_criteria>
    - Las 6 sub-secciones existen, cada una con las 3 tablas A/B/C y sin celdas vacías
    - `/agenda` registra el gotcha date-only UTC y nombra `diaCalendario`
    - `/buscar` registra `min(tramitacion_evento.fecha)` como origen del año y niega `fecha_captura`
    - `/comparar` marca `fecha_captura_max` como `fecha_captura` y lleva gate `VSIM`
    - `/red` lleva gate `NET` y su `not-found.tsx` está apendizado
    - `grep -nE '\|[[:space:]]*\|' 113-INVENTARIO.md` sin match (el guard `grep -c '||'` NO caza `| |`)
    - Toda instancia de `ProvenanceBadge` aporta fila en Tabla B (link externo) Y en Tabla C (fecha)
  </acceptance_criteria>
  <done>Las 6 rutas públicas restantes inventariadas con sus 3 tablas y los gotchas de fecha registrados.</done>
</task>

<task type="auto">
  <name>Task 2: §4.10–4.15 estáticas, auth/token y la EXCLUIDA</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/metodologia/page.tsx, app/app/sobre/page.tsx, app/app/cuenta/page.tsx
    - app/app/notificaciones/baja/page.tsx, app/app/notificaciones/confirmar/page.tsx
    - app/app/admin/revisar-entidades/page.tsx
    - 113-CONTEXT.md §Alcance de rutas (decisión LOCKED sobre /admin, /cuenta y /notificaciones)
  </read_first>
  <action>
    Escribir `### 4.10 /metodologia`, `### 4.11 /sobre`, `### 4.12 /cuenta`, `### 4.13 /notificaciones/baja`, `### 4.14 /notificaciones/confirmar`, `### 4.15 /admin/revisar-entidades`.

    - `/metodologia` y `/sobre`: estáticas; tablas A/B/C; los links del chrome se referencian `→ C-01` y se listan aparte solo los propios de la página (incluido `creativecommons.org` si lo emite la página misma).
    - `/cuenta`: marcar naturaleza **auth (OTP)**; gate `NOTIF` (OFF) → marca literal `no emitido en el deploy auditado` donde corresponda; origen de datos `suscripcion` / `consentimiento`. Prohibido registrar email alguno.
    - `/notificaciones/baja` y `/notificaciones/confirmar`: marcar naturaleza **token-based** y `noindex`; describir el mecanismo (token crudo en el link, hash en DB) sin jamás incluir un token real de ejemplo.
    - `/admin/revisar-entidades`: **NO se inventaría**. Una sola fila con la marca literal `EXCLUIDA` y la razón "gated admin, no pública (decisión LOCKED del CONTEXT)". Cero tablas A/B/C para esta ruta.

    Verificar con `find app/app -name "not-found.tsx"` que las 4 estén apendizadas entre los planes 03 y 04; si alguna quedó fuera, apendizarla aquí.

    **Matching literal obligatorio** al verificar cobertura de rutas/not-found: las rutas contienen `[id]`/`[boletin]`, que en BRE son clases de carácter — usar SIEMPRE `grep -qF -- "$ruta"`, nunca `grep -q`; y tratar la raíz `/` como caso especial (buscar el header literal `### 4.4 /`, porque `/` matchea cualquier línea). Es la misma regla que implementa `check-inventario.sh`: preferir correr el script antes que re-escribir el loop.
  </action>
  <verify>
    <automated>grep -q 'EXCLUIDA' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</automated>
  </verify>
  <acceptance_criteria>
    - Las 6 sub-secciones §4.10–§4.15 existen
    - `/admin/revisar-entidades` aparece con la marca literal `EXCLUIDA` + razón, y sin tablas A/B/C
    - `/cuenta` marcada `auth (OTP)`; `/notificaciones/baja` y `/confirmar` marcadas `token-based` + `noindex`
    - `bash check-inventario.sh` no imprime ninguna línea `FALTA` para rutas ni para `not-found.tsx` (matching con `grep -qF --`)
    - Cero emails y cero tokens de ejemplo: `grep -E '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+' 113-INVENTARIO.md` solo matchea el `mailto:` público del chrome
  </acceptance_criteria>
  <done>Las 15 rutas del universo LOCKED cubiertas (14 inventariadas + 1 EXCLUIDA con razón) y las 4 not-found apendizadas.</done>
</task>

<task type="auto">
  <name>Task 3: Tabla D de cobertura método×ruta</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - 113-INVENTARIO.md completo (§0 a §5)
    - 113-VALIDATION.md §Criterios de aceptación del validador Opus (criterio 6)
    - 113-RESEARCH.md §Architecture Patterns → Tabla D
  </read_first>
  <action>
    Llenar en §0 la **Tabla D** con una fila por cada una de las 15 rutas más una por cada `not-found.tsx`, encabezado verbatim: `| ruta | links enumerados por | fechas enumeradas por | sujeto usado | ¿exhaustivo o muestra? | evidencia |`.

    Reglas de llenado: "links enumerados por" y "fechas enumeradas por" toman valores de un vocabulario cerrado que se declara arriba de la tabla (`código (grep del árbol de componentes)`, `código + psql (columnas de URL)`, `n/a — EXCLUIDA`); "sujeto usado" cita el id exacto de §1 o `—` para rutas sin sujeto; "evidencia" cita el archivo o el comando. Cero celdas vacías, cero rutas sin evidencia.

    Cerrar §0 con los tres LÍMITES declarados: (1) los links externos que provienen de columnas de la DB se enumeran por familia de host vía psql, no por plantilla en TSX — el conteo por host es a la fecha de la corrida y puede crecer; (2) la verificación contra DOM real NO es parte de esta fase (es 114 para internos y 125 para E2E); (3) los bloques con gate OFF se inventarían desde el código y NO están presentes en el deploy auditado.

    Añadir al final del documento una línea de régimen: "Este documento inventaría; no corrige. Los fixes son 114 (links internos), 115 (patrones externos) y 117 (fechas)."
  </action>
  <verify>
    <automated>grep -q 'Cobertura' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</automated>
  </verify>
  <acceptance_criteria>
    - La Tabla D tiene ≥ 19 filas de datos (15 rutas + 4 not-found) y cero celdas vacías: `grep -nE '\|[[:space:]]*\|'` sin match
    - Los 3 límites del método están declarados textualmente en §0
    - La línea de régimen "no corrige" está presente
    - `bash check-inventario.sh` (STRICT=0) exit 0 y los 5 checks reportan OK
  </acceptance_criteria>
  <done>Tabla D completa y límites del método declarados; el inventario queda listo para el gate del validador Opus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superficies auth/token → documento público | riesgo de filtrar tokens o emails de usuarios |
| superficie admin gated → documento público | riesgo de presentar lo gated como público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04 | Information Disclosure | `/admin/revisar-entidades` | mitigate | Marca literal `EXCLUIDA` + razón; cero enumeración de su superficie (ASVS V4) |
| T-113-08 | Information Disclosure | `/notificaciones/*`, `/cuenta` | mitigate | Describir el mecanismo de token (crudo en link, hash en DB) sin incluir jamás un token real; cero emails de usuario |
| T-113-02 | Information Disclosure | Tabla D y §4 | mitigate | Solo ids públicos; cero RUT/email/monto individual |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes |
</threat_model>

<verification>
- `bash check-inventario.sh` (STRICT=0) exit 0
- Las 15 rutas de `find app/app -name page.tsx` aparecen en el documento
- Las 4 `not-found.tsx` aparecen
- Tabla D sin celdas vacías
</verification>

<success_criteria>
Las 15 rutas del universo LOCKED cubiertas, las 4 not-found apendizadas, la Tabla D de cobertura completa con vocabulario cerrado y los tres límites del método declarados.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-04-SUMMARY.md` when done
</output>
