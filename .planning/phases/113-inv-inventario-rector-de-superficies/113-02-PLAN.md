---
phase: 113-inv-inventario-rector-de-superficies
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "El chrome compartido (layout/header-nav/global-header/breadcrumbs) está inventariado UNA vez y es referenciable por id desde cualquier ruta"
    - "Existe un catálogo de emisores con id estable por componente, sus hrefs, sus fechas y su gate"
    - "Los 4 builders de URL externa están citados con plantilla verbatim y `safeExternalHref` declarado como chokepoint"
    - "Las familias de URL almacenadas en columnas de la DB están enumeradas por host con evidencia SQL"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md"
      provides: "Secciones 2 (chrome) y 3 (catálogo de emisores)"
      contains: "## 3. Catálogo de emisores"
  key_links:
    - from: "113-INVENTARIO.md §4 (Plan 03/04)"
      to: "113-INVENTARIO.md §3"
      via: "referencia por id E-NNN"
      pattern: "E-[0-9]{3}"
---

<objective>
Escribir la capa que hace finito el documento: **§2 Chrome compartido** (inventariado una sola vez) y **§3 Catálogo de emisores** (~50 componentes con id estable, hrefs que emiten, fechas que muestran y gate). Incluye la resolución del hallazgo rector: distinguir URL externa CONSTRUIDA en código (4 builders) de URL externa ALMACENADA en columnas de la DB (el resto), esta última enumerada por host con evidencia psql.

Purpose: sin catálogo, `ProvenanceBadge` (25 call-sites) y el chrome (5 links) se repetirían 15 veces y las fases 115/116 re-derivarían la misma semántica en cada ruta.
Output: §2 y §3 completas; §4 pasa a ser una lista de referencias `→ E-NNN` + lo exclusivo de cada ruta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-CONTEXT.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §2 Chrome compartido</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/app/layout.tsx
    - app/components/header-nav.tsx
    - app/components/global-header.tsx
    - app/components/breadcrumbs.tsx
    - 113-RESEARCH.md §Emisores de links → "Chrome compartido"
  </read_first>
  <action>
    Escribir `## 2. Chrome compartido` con un id estable por pieza (`C-01` layout, `C-02` header-nav, `C-03` global-header, `C-04` breadcrumbs) y, por pieza, una tabla `| # | href | tipo (interno/externo/mailto) | emisor (archivo:línea) | condicional/gate |`.

    Contenido verificado a registrar: `app/app/layout.tsx` emite el link CC BY 4.0 a `creativecommons.org`, `/metodologia`, `/sobre` y el `mailto:contacto@observatoriocongreso.cl`; `header-nav.tsx` emite `/buscar`, `/parlamentarios`, `/agenda`, `/red`, `/sobre` — con la condición LOCKED de que `/red` se FILTRA del nav cuando NET está OFF (registrar el gate `NET` en esa fila); `global-header.tsx` emite `/` (logo); `breadcrumbs.tsx` emite links dinámicos y el ÚLTIMO ítem va SIN href (segmento actual) — registrarlo como nota de comportamiento, no como href.

    Re-verificar los números de línea por grep contra el árbol actual antes de escribirlos (no copiar los del research a ciegas). Declarar explícitamente que §2 aplica a las 15 rutas y que §4 la referencia por id en vez de repetirla.
  </action>
  <verify>
    <automated>grep -c '^| C-0\|C-01\|C-02\|C-03\|C-04' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - Los ids `C-01`..`C-04` aparecen en §2
    - Las cadenas `creativecommons.org`, `mailto:`, `/metodologia`, `/sobre`, `/buscar`, `/parlamentarios`, `/agenda`, `/red` aparecen en §2
    - La fila de `/red` lleva `NET` en la columna gate
    - Cada fila de href tiene `archivo:línea` no vacío y la línea citada corresponde al árbol actual (verificable con `grep -n`)
    - `breadcrumbs` tiene registrada la nota "último ítem sin href"
  </acceptance_criteria>
  <done>§2 completa, con ids referenciables y gate de `/red` marcado.</done>
</task>

<task type="auto">
  <name>Task 2: §3 Catálogo de emisores (componentes → hrefs, fechas, gate)</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/components/validacion-fuente.tsx (buildSenadoUrl, buildCamaraUrl, enlaceHumanoProyecto, esR2PathPermitido)
    - app/lib/utils.ts (safeExternalHref), app/lib/format.ts (fechaCorta, fechaCortaSegura, relativeTimeEs, esStale, partidoLegible), app/lib/dia-calendario.ts, app/lib/week-utils.ts
    - app/components/provenance-badge.tsx (prop `capturedAt`)
    - Los componentes de mayor densidad del research: votos-por-parlamentario.tsx, lobby-de-parlamentario.tsx, voto-ficha-row.tsx, patrimonio-de-parlamentario.tsx, week-nav.tsx, actualidad-module.tsx, timeline-view.tsx, red/red-graph.tsx, parlamentario-directory-row.tsx, financiamiento-*, contratos-*, aportes-*
    - 113-RESEARCH.md §Emisores de links, §Constructores de URL externa, §Fechas visibles, §RPCs y tablas por superficie
  </read_first>
  <action>
    Regenerar la enumeración con los comandos declarados en §0 (loop de conteo `<Link|href=`, grep de formatters, `grep -rln ProvenanceBadge`) y escribir `## 3. Catálogo de emisores` con id estable `E-001`, `E-002`, … por componente, ordenado por densidad descendente.

    Por emisor, una fila: `| id | componente (archivo) | hrefs que emite | fechas que muestra (formatter → origen) | gate | rutas donde aparece |`. La columna origen usa EXCLUSIVAMENTE el vocabulario cerrado: `RPC:<nombre>.<campo>` con los nombres de la lista del research (`resolver_entidad`, `parlamentarios_publico_v2`, `parlamentarios_publico`, `parlamentario_publico_v2`, `militancia_historica_compartida`, `militancias_de_parlamentario`, `comisiones_de_parlamentario`, `coautores_de_parlamentario`, `coincidencia_votos_par`, `agregado_por_contraparte`, `subgrafo_red`, `buscar_citaciones`, `match_proyectos`, `cruces_de_parlamentario`, `cruces_de_proyecto`, `lobby_en_tramitacion`, `lobby_menciones_de_boletin`, `actualidad_senales_panel`) o `tabla.<columna>` con las tablas de la lista (`suscripcion`, `proyecto`, `tramitacion_evento`, `citacion`, `votacion`, `sesion_sala`, `consentimiento`, `source_snapshot`, `sesion_tabla_item`, `revision_entidad`, `proyecto_ficha`, `proyecto_embedding`, `proyecto_autor`, `probidad_ingesta_estado`, `lobby_ingesta_estado`, `contratos_ingesta_estado`, `citacion_punto`, `aportes_ingesta_estado`). NO inventar nombres fuera de esas dos listas; si aparece uno nuevo, verificarlo por grep y añadirlo declarando la evidencia.

    Sub-sección `### 3.1 Chokepoints` con: `ProvenanceBadge` (prop `capturedAt`, usa `relativeTimeEs` + `esStale`) y la REGLA — toda fecha que llega vía `capturedAt` se MARCA como `fecha_captura` sin más análisis, toda otra fecha exige rastreo a su columna; listar sus call-sites verificados por grep. Y `safeExternalHref` (`lib/utils.ts`) como guard único de href externo (devuelve `null` salvo `http:`/`https:`), declarado como chokepoint de LINK-03.

    Sub-sección `### 3.2 Constructores de URL externa` con los 4 builders y su plantilla VERBATIM: `buildSenadoUrl` → `https://tramitacion.senado.cl/appsenado/templates/tramitacion/index.php?boletin_ini={boletin}` (boletín COMPLETO con sufijo); `buildCamaraUrl` → `https://www.camara.cl/legislacion/ProyectosDeLey/tramitacion.aspx?prmID={prmId}&prmBOLETIN={boletin}` (solo si `prm_id_camara != null`); `enlaceHumanoProyecto` → rewrite por host+path (si `proyecto.enlace` es host `tramitacion.senado.cl` con path `/wspublico/` → `buildSenadoUrl(boletin)`, si no VERBATIM) — registrar SIEMPRE el link post-rewrite; `partidoLegible` (`lib/format.ts`) → NO construye link, desactiva URIs `datos.bcn.cl/.../partido-politico/{slug}` (invariante "CERO URI en el DOM"), display-only.

    Sub-sección `### 3.3 Familias de URL almacenadas en columnas` — cierre del hallazgo rector. Enumerar por psql read-only los hosts distintos presentes en las columnas de URL (`proyecto.enlace` y las columnas `url_fuente` / `enlace_fuente` / `link_*` que existan realmente; descubrir el set con una query a `information_schema.columns` por patrón de nombre). Patrón de query: `select split_part(<col>,'/',3) as host, count(*) from <tabla> where <col> is not null group by 1 order by 2 desc, 1 asc`. Registrar query verbatim + resultado inline, y clasificar cada host por fuente (`camara` / `senado` / `BCN` / `leylobby` / `otro`). Nunca imprimir la URL de conexión; nunca listar keys de r2_path de dominios PII (`infoprobidad/`, `servel/`, `money/`, `rut/`) — solo `tramitacion/` está allowlistado por `esR2PathPermitido`.
  </action>
  <verify>
    <automated>grep -c 'E-0' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'buildSenadoUrl' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'buildCamaraUrl' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'enlaceHumanoProyecto' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'partidoLegible' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'safeExternalHref' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - El catálogo tiene al menos 40 ids `E-NNN` (el loop de conteo del research da ~50 archivos con href; los que solo muestran fechas también entran)
    - Los 4 builders aparecen con su plantilla verbatim y `safeExternalHref` declarado chokepoint
    - `ProvenanceBadge` y la prop `capturedAt` aparecen con la regla de marcado explícita
    - Todo valor de la columna origen matchea `RPC:[a-z_]*\.` o `[a-z_]*\.[a-z_]*` con nombre dentro de las dos listas cerradas — cero nombres inventados
    - §3.3 contiene al menos un bloque ```sql con query verbatim + resultado inline y clasifica cada host observado en {camara, senado, BCN, leylobby, otro}
    - Cero keys de r2_path de dominios PII; cero credenciales; cero RUT/email
  </acceptance_criteria>
  <done>§3 completa con catálogo referenciable, chokepoints, 4 builders verbatim y familias de URL-desde-columna con evidencia SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD Postgres → documento en git | los hosts y conteos de §3.3 salen de datos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Information Disclosure | queries de §3.3 | mitigate | Solo `split_part(...,'/',3)` (host) + `count(*)`; jamás `select <col>` completo que pueda arrastrar identificadores; invocación sin ecoar `SUPABASE_DB_URL` |
| T-113-06 | Information Disclosure | r2_path de dominios PII | mitigate | Registrar solo el prefijo allowlistado `tramitacion/` per `esR2PathPermitido`; no listar keys de `infoprobidad/`, `servel/`, `money/`, `rut/` |
| T-113-03 | Tampering | PROD Postgres | mitigate | Solo `SELECT` |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes |
</threat_model>

<verification>
- `bash check-inventario.sh` (STRICT=0) exit 0 y el check de builders pasa (los 4 citados)
- El catálogo `E-NNN` es denso (≥ 40 ids) y sin ids duplicados: `grep -o 'E-[0-9]\{3\}' 113-INVENTARIO.md | sort | uniq -d` vacío
</verification>

<success_criteria>
§2 y §3 completas: chrome inventariado una vez, catálogo de emisores con ids estables, chokepoints declarados, 4 builders verbatim y familias de URL-desde-columna cerradas con evidencia SQL.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-02-SUMMARY.md` when done
</output>
