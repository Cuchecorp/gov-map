---
phase: 91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0060_bio_partido_publico.sql
  - supabase/tests/0060_bio_partido_publico.test.sql
  - app/lib/types.ts
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [BIO-02, BIO-03, BIO-04]

must_haves:
  truths:
    - "Una RPC PII-safe emite partido vigente + su fecha_captura + origen para la cabecera de la ficha"
    - "Una RPC PII-safe emite las militancias (partido, desde, hasta, es_actual) de un parlamentario, vigente primero"
    - "Una RPC PII-safe emite las comisiones de un parlamentario (nombre, cámara, tipo, cargo)"
    - "Cuatro RPCs bounded (LIMIT) emiten cross-links: mismo partido, misma zona, misma comisión, co-autoría — orden neutral, nunca ranking"
    - "El listado /parlamentarios recibe partido + partido_fecha_captura + partido_origen sin re-query del island"
    - "Ninguna RPC emite rut, email ni datos de terceros/familiares"
    - "0060 aplicada a PROD y verificada por pgTAP contra el schema aplicado"
  artifacts:
    - path: "supabase/migrations/0060_bio_partido_publico.sql"
      provides: "RPCs de cabecera+militancias+comisiones+cross-links+listado ampliado, doble-revoke CERO grant"
      contains: "create or replace function"
    - path: "supabase/tests/0060_bio_partido_publico.test.sql"
      provides: "pgTAP: firmas existen, security definer, search_path='', cero grant anon, columnas PII-safe"
      contains: "select plan("
    - path: "app/lib/types.ts"
      provides: "Row types de las RPCs nuevas"
      contains: "partido_fecha_captura"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "RPCs nuevas en PUBLIC_RPC_ALLOWLIST"
      contains: "PUBLIC_RPC_ALLOWLIST"
  key_links:
    - from: "supabase/migrations/0060_bio_partido_publico.sql"
      to: "public.parlamentario_militancia / public.comision_membresia / public.proyecto_autor"
      via: "security definer select con set search_path=''"
      pattern: "parlamentario_militancia|comision_membresia|proyecto_autor"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "las RPCs nuevas de 0060"
      via: "PUBLIC_RPC_ALLOWLIST Set"
      pattern: "PUBLIC_RPC_ALLOWLIST"
---

<objective>
Construir el CANAL de datos público de la ficha 360: una migración 0060 con RPCs
security-definer PII-safe que sirven partido DIRECTO (vigente + fecha de fuente),
militancias históricas, comisiones del parlamentario, cuatro cross-links factuales
bounded, y el listado /parlamentarios ampliado con partido. Registrar cada RPC en
el allowlist, verificar por pgTAP, y aplicar a PROD.

Purpose: Los datos ya viven en PROD (Phase 90) en tablas deny-by-default. Sin este
canal la UI (Plans 02/03) no puede leer nada. Esta es la Wave 0 de contratos: define
las firmas y los row-types que los planes de montaje consumen sin explorar el schema.

Output: Migración 0060 aplicada a PROD + pgTAP verde + row-types en types.ts + allowlist actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-CONTEXT.md

<read_first>
- supabase/migrations/0059_bio_comisiones.sql — las 4 tablas fuente (parlamentario_bio, parlamentario_militancia, comision, comision_membresia) con sus columnas exactas y claves naturales
- supabase/migrations/0020_parlamentario_publico.sql — RPC de cabecera ACTUAL (retiene partido por LEGAL-03; esta fase lo REVIERTE por decisión operador 2026-07-21)
- supabase/migrations/0026_parlamentarios_publico_listado.sql — RPC de listado ACTUAL a ampliar
- supabase/migrations/0055_busqueda_hibrida.sql — patrón POST-Camino-A más reciente: drop defensivo + create or replace + set search_path='' + doble-revoke CERO grant + caps de fila (least(match_count,50)); operator(public.<=>) idiom; DO-exception idempotencia
- supabase/migrations/0051_proyecto_autor.sql — tabla proyecto_autor (co-autoría F48): columnas boletin, autor_crudo_norm, parlamentario_id, estado_vinculo; índice por parlamentario_id where not null
- supabase/tests/0059_bio_comisiones.test.sql — patrón pgTAP a espejar (has_table/is/pg_policies/pg_proc), begin/plan(N)
- app/lib/lockdown-guard.test.ts:165-183 — PUBLIC_RPC_ALLOWLIST Set (añadir entradas nuevas, orden alfabético)
- app/lib/types.ts:173-204 — ParlamentarioPublicoRow y ParlamentarioListadoRow (row-types a ampliar/crear)
</read_first>

<interfaces>
Tablas fuente (0059) — columnas exactas:
- parlamentario_militancia(parlamentario_id text, partido text, partido_alias text, desde date, hasta date, es_actual bool, origen text, fecha_captura timestamptz, enlace text)
- comision(id bigint, nombre text, camara text, tipo text, origen, fecha_captura, enlace)
- comision_membresia(comision_id bigint, parlamentario_id text, cargo text, origen, fecha_captura, enlace)
- proyecto_autor(boletin text, autor_crudo_norm text, parlamentario_id text, estado_vinculo text)  -- co-autoría F48
- parlamentario(id text, camara text, region text, distrito text, circunscripcion text, ...)

RPC de listado actual (0026): parlamentarios_publico() returns table(id,nombre,camara,region,distrito,circunscripcion,periodo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0060 — RPCs de cabecera+militancias+comisiones+listado ampliado</name>
  <files>supabase/migrations/0060_bio_partido_publico.sql</files>
  <read_first>
    - supabase/migrations/0055_busqueda_hibrida.sql (patrón doble-revoke CERO grant + drop defensivo + search_path='')
    - supabase/migrations/0020_parlamentario_publico.sql + 0026 (firmas a acompañar/ampliar)
    - supabase/migrations/0059_bio_comisiones.sql (columnas fuente)
  </read_first>
  <action>
    Crear la migración 0060 (última aplicada = 0059) documentando en cabecera de comentario:
    (a) la decisión del OPERADOR 2026-07-21 que REVIERTE la retención de partido de 0020_parlamentario_publico.sql
    (el partido del cargo electo es dato público esencial para accountability, Ley 21.719 minimización plena SOLO
    para terceros/familiares/RUT/email), con referencia a PROJECT.md, para que el guard histórico no parezca violado;
    (b) el orden de apply (después de 0059) y el comando psql --single-transaction (NUNCA db push, BOM en .env → --db-url explícito).

    Crear estas RPCs security-definer `set search_path=''`, cada una con drop defensivo previo (idiom 42P13, 0055:54),
    y doble-revoke CERO grant al final (VERBATIM de 0055:137-138: `revoke all on function <firma> from public;` +
    `from anon, authenticated;` — el sitio ejecuta vía service_role, NUNCA re-emitir grant):

    1. `parlamentario_publico_v2(p_id text)` — NUEVA firma (NO alterar 0020 in-place: cambiar su returns table dispara 42P13
       y re-arma default privileges; una firma nueva paralela es menos invasiva — decisión de discreción tomada aquí).
       Emite las MISMAS columnas que ParlamentarioPublicoRow MÁS: `partido text`, `partido_fecha_captura timestamptz`,
       `partido_origen text`. Deriva el partido vigente de `public.parlamentario_militancia` where `es_actual=true`
       (LEFT JOIN lateral, limit 1, la de `desde` más reciente para tie-break); si no hay militancia vigente → NULLs honestos.
       NUNCA emite rut/email/partido_alias.
    2. `militancias_de_parlamentario(p_id text)` — returns table(partido text, desde date, hasta date, es_actual boolean,
       origen text, fecha_captura timestamptz, enlace text). Orden: es_actual DESC, desde DESC (vigente primero, luego histórico
       cronológico desc). LIMIT bounded (p.ej. 50). NUNCA partido_alias.
    3. `comisiones_de_parlamentario(p_id text)` — returns table(nombre text, camara text, tipo text, cargo text,
       origen text, fecha_captura timestamptz, enlace text). JOIN comision_membresia→comision por comision_id.
       Orden neutral alfabético por nombre. LIMIT bounded (p.ej. 50).
    4. `parlamentarios_publico_v2()` — listado: MISMAS columnas que parlamentarios_publico() (0026) MÁS `partido text`,
       `partido_fecha_captura timestamptz`, `partido_origen text` (mismo LEFT JOIN lateral de militancia vigente). Orden neutral
       (apellido_paterno nulls last, nombre_normalizado) igual que 0026. Sin cap (186 filas fijas).

    Todas: `set search_path=''` con nombres schema-qualified (`public.parlamentario`, etc.). Si usa operador de distancia/vector
    NO aplica aquí. Cero PII. Los cross-links van en Task 2.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/0060_bio_partido_publico.sql | grep -c "grant execute" | grep -qx 0 && echo "CERO grant OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create or replace function" supabase/migrations/0060_bio_partido_publico.sql` ≥ 4
    - `grep -c "security definer set search_path = ''" ...` = número de RPCs (todas hardened)
    - `grep -c "revoke all on function" ...` = 2× número de RPCs (doble-revoke cada una)
    - Ningún `grant … to anon` / `to public` / `grant execute` (guard Block A)
    - El comentario de cabecera menciona "2026-07-21" y "0020"
  </acceptance_criteria>
  <done>Migración 0060 con ≥4 RPCs hardened, doble-revoke CERO grant, cabecera documenta la reversión operador.</done>
</task>

<task type="auto">
  <name>Task 2: RPCs de cross-links bounded + row-types en types.ts + allowlist + pgTAP</name>
  <files>supabase/migrations/0060_bio_partido_publico.sql, supabase/tests/0060_bio_partido_publico.test.sql, app/lib/types.ts, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - supabase/tests/0059_bio_comisiones.test.sql (patrón pgTAP: has_table→has_function, is(pg_policies…), begin/plan)
    - app/lib/types.ts:173-204 (ParlamentarioPublicoRow/ParlamentarioListadoRow a espejar)
    - app/lib/lockdown-guard.test.ts:165-183 (PUBLIC_RPC_ALLOWLIST)
    - supabase/migrations/0051_proyecto_autor.sql (proyecto_autor para co-autoría)
  </read_first>
  <action>
    (a) Añadir a 0060 cuatro RPCs de cross-links bounded, cada una security-definer `set search_path=''` + doble-revoke CERO grant,
    con LIMIT explícito (cap de fila least/const, p.ej. 20) y auto-exclusión del propio parlamentario (`where p2.id <> p_id`):
    - `copartidarios_de_parlamentario(p_id text)` — otros parlamentarios cuya militancia vigente (es_actual) tiene el MISMO
      partido_alias que la del p_id. returns table(id text, nombre text, camara text). Orden neutral alfabético.
    - `de_la_misma_zona(p_id text)` — otros con el mismo distrito (Cámara) o circunscripción (Senado) que p_id. returns
      table(id, nombre, camara). Orden alfabético.
    - `co_comisionados_de_parlamentario(p_id text)` — otros que comparten AL MENOS una comision_membresia con p_id. returns
      table(id, nombre, camara, comision_nombre text). Distinct por parlamentario. Orden alfabético.
    - `coautores_de_parlamentario(p_id text)` — otros parlamentarios con proyecto_autor confirmado (estado_vinculo='confirmado')
      en un boletín donde p_id también es autor confirmado. returns table(id, nombre, camara, n_proyectos int). Orden por
      nombre (NUNCA por n_proyectos: eso sería ranking de afinidad — el conteo es dato honesto, no criterio de orden).
    Todas: nombre legible con el mismo coalesce(concat_ws...) de 0020. Cero PII. LIMIT bounded en TODAS.

    (b) types.ts: crear/ampliar row-types (exportados), espejando el estilo JSDoc del archivo:
    - `ParlamentarioPublicoRow` → añadir `partido: string | null; partido_fecha_captura: string | null; partido_origen: string | null;`
      (v2 emite super-set; el tipo existente se amplía porque page.tsx migrará a la RPC v2).
    - `ParlamentarioListadoRow` → añadir los mismos 3 campos.
    - Nuevos: `MilitanciaRow`, `ComisionRow`, `CrossLinkRow` (id/nombre/camara + campo extra opcional: comision_nombre?, n_proyectos?).
    Documentar en JSDoc: cero rut/email/partido_alias.

    (c) lockdown-guard.test.ts: añadir las 6 RPCs nuevas al Set PUBLIC_RPC_ALLOWLIST en ORDEN ALFABÉTICO
    (co_comisionados_de_parlamentario, coautores_de_parlamentario, comisiones_de_parlamentario, copartidarios_de_parlamentario,
    de_la_misma_zona, militancias_de_parlamentario, parlamentario_publico_v2, parlamentarios_publico_v2).

    (d) supabase/tests/0060_bio_partido_publico.test.sql: pgTAP espejando 0059. plan(N) con: cada RPC existe (has_function
    'public', '<nombre>', ARRAY['text'] o '{}'); cada RPC es security definer (pg_proc.prosecdef); NINGUNA tiene grant execute
    a anon (has_function_privilege('anon','<firma>','execute') = false para todas); las columnas de salida NO incluyen rut/email/partido_alias
    (verificar por pg_get_function_result no contiene 'rut'/'email'). begin/rollback.
  </action>
  <verify>
    <automated>cd app; pnpm test -- lockdown-guard 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test lockdown-guard` verde (allowlist consistente)
    - `pnpm --filter ./app exec tsc -b` exit 0 (row-types válidos)
    - `grep -c "create or replace function" supabase/migrations/0060_bio_partido_publico.sql` ≥ 8 (4 base + 4 cross-links)
    - Cada cross-link RPC tiene un `where` con `<> p_id` (auto-exclusión) y un `limit`
    - pgTAP file tiene `select plan(` y al menos 1 aserto por RPC de "cero grant anon"
  </acceptance_criteria>
  <done>8 RPCs totales hardened + row-types + 6 entradas en allowlist + pgTAP escrito; tsc + lockdown test verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Aplicar 0060 a PROD + correr pgTAP contra schema aplicado</name>
  <files>supabase/migrations/0060_bio_partido_publico.sql, supabase/tests/0060_bio_partido_publico.test.sql</files>
  <read_first>
    - .planning/STATE.md (precedente 0059 apply por el agente; DDL aditivo dentro de autoridad)
    - CLAUDE.md §Conventions (psql --single-transaction NUNCA db push; PGCLIENTENCODING=UTF8; --db-url explícito por BOM)
  </read_first>
  <action>
    0060 es ADITIVO (solo create-or-replace de funciones + revokes; sin DROP de tabla, sin backfill) → dentro de la autoridad
    del agente (precedente 0055-0059, decisión operador en CONTEXT).

    Aplicar UNA vez a PROD:
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0060_bio_partido_publico.sql`
    (leer SUPABASE_DB_URL de .env con precedencia process.env, BOM-safe; NUNCA supabase db push).

    Correr el pgTAP CONTRA EL SCHEMA APLICADO:
    `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0060_bio_partido_publico.test.sql`
    Debe reportar `N ok / 0 not ok`. build/typecheck NO prueban que Postgres ejecutó el DDL (Pitfall 6).

    Verificación funcional mínima (SELECT read-only, cap 1k respetado): invocar cada RPC v2 con un id conocido
    (p.ej. un diputado con partido fresco de la corrida 90-03) y confirmar que `partido` NO es null y las columnas PII
    (rut/email) no aparecen en el result-set. Registrar el resultado (N ok / 0 not ok + partido visible) en el summary.

    Si $SUPABASE_DB_URL no está disponible o el apply falla por credenciales → NO fabricar éxito: documentar como
    checkpoint de operador pendiente en el summary con el comando exacto (espejo de los runbooks de apply previos).
  </action>
  <verify>
    <automated>echo "pgTAP result documented in SUMMARY: expect N ok / 0 not ok against applied schema"</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY registra la salida pgTAP `N ok / 0 not ok` contra el schema aplicado
    - El SUMMARY confirma partido visible (no-null) para un id conocido vía la RPC v2
    - El SUMMARY confirma cero columna rut/email en los result-sets probados
    - Si no se pudo aplicar: documentado como gate de operador con el comando psql exacto
  </acceptance_criteria>
  <done>0060 aplicada a PROD (o gate documentado); pgTAP verde contra schema vivo; partido confirmado visible sin PII.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → RPC | El sitio ejecuta RPCs con service_role (bypassa RLS/ACL, Camino A); la RPC debe auto-limitar qué columnas emite |
| RPC → tablas maestras | Cada RPC security-definer lee tablas deny-by-default; el cuerpo es el ÚNICO filtro contra fuga de PII |
| anon REST → RPC | anon podría intentar invocar la RPC vía REST no autenticado si hubiera grant (prohibido) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information Disclosure | RPCs 0060 | mitigate | returns table SOLO columnas públicas; pgTAP asserta que pg_get_function_result no contiene rut/email/partido_alias |
| T-91-02 | Elevation of Privilege | grant a anon | mitigate | doble-revoke CERO grant VERBATIM (0055); pgTAP asserta has_function_privilege('anon',…)=false; lockdown-guard Block A caza grants en migraciones >0044 |
| T-91-03 | Denial of Service | RPCs cross-link sin LIMIT | mitigate | LIMIT bounded (const/least) en TODAS las RPCs de cross-link y militancias/comisiones |
| T-91-04 | Tampering | inyección SQL vía p_id | mitigate | p_id parametrizado, search_path=''; nunca interpolación de string |
| T-91-EOP | Elevation of Privilege | apply a PROD | mitigate | DDL aditivo (no DROP tabla); --single-transaction rollback atómico; NUNCA db push |
| T-91-SC | Tampering | npm/pip installs | mitigate | CERO paquetes nuevos en este plan (solo SQL + TS types) |
</threat_model>

<verification>
- `pnpm --filter ./app test lockdown-guard` verde (allowlist consistente con las 6 RPCs nuevas)
- `pnpm --filter ./app exec tsc -b` exit 0
- pgTAP 0060 `N ok / 0 not ok` contra el schema APLICADO
- Ningún `grant execute … to anon/public` en 0060 (guard Block A)
- Partido no-null visible vía RPC v2 para un id conocido; cero rut/email en result-sets
</verification>

<success_criteria>
- Migración 0060 con 8 RPCs hardened (4 cabecera/listado/militancias/comisiones + 4 cross-links), doble-revoke CERO grant
- Row-types en types.ts (partido + 3 campos, MilitanciaRow, ComisionRow, CrossLinkRow)
- 6 RPCs nuevas en PUBLIC_RPC_ALLOWLIST (orden alfabético)
- pgTAP escrito y verde contra schema aplicado
- 0060 aplicada a PROD (o gate de operador documentado con comando exacto)
</success_criteria>

<output>
Create `.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-01-SUMMARY.md` when done
</output>
