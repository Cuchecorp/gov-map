---
phase: 91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale
plan: 02
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - app/components/partido-chip.tsx
  - app/components/partido-chip.test.tsx
  - app/components/parlamentario-header.tsx
  - app/components/comisiones-de-parlamentario.tsx
  - app/components/militancias-de-parlamentario.tsx
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: [BIO-02, BIO-03]

must_haves:
  truths:
    - "La ficha muestra el partido vigente como chip NEUTRO con 'según fuente al [fecha]'"
    - "La ficha muestra las comisiones del parlamentario (nombre · tipo · cargo) o leyenda empty honesta"
    - "La ficha muestra las militancias registradas: vigente primero + acordeón de tramos históricos"
    - "El chip de partido se OMITE (null) cuando no hay partido, sin placeholder"
    - "Profesión NO se renderiza (bio 0 filas → campo omitido)"
    - "El header sigue sin RUT/email/foto/terceros"
  artifacts:
    - path: "app/components/partido-chip.tsx"
      provides: "Chip reutilizable neutro con tooltip fuente+fecha; null si sin dato"
      min_lines: 25
    - path: "app/components/comisiones-de-parlamentario.tsx"
      provides: "Bloque de comisiones con DetalleColapsable si >5"
    - path: "app/components/militancias-de-parlamentario.tsx"
      provides: "Sección militancias vigente + acordeón histórico"
    - path: "app/components/parlamentario-header.tsx"
      provides: "Header con PartidoChip en la fila de chips + comisiones"
      contains: "PartidoChip"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "parlamentario_publico_v2 / comisiones_de_parlamentario / militancias_de_parlamentario"
      via: ".rpc() server-only cacheado"
      pattern: "parlamentario_publico_v2|comisiones_de_parlamentario|militancias_de_parlamentario"
    - from: "app/components/parlamentario-header.tsx"
      to: "app/components/partido-chip.tsx"
      via: "import + render en flex flex-wrap gap-2"
      pattern: "PartidoChip"
---

<objective>
Montar el header ampliado de la ficha 360: revertir la omisión LEGAL-03 del partido
(decisión operador 2026-07-21) mostrándolo como PartidoChip neutro "según fuente al
[fecha]", añadir el bloque de comisiones, y una sección de militancias (vigente +
acordeón histórico). Cablear las RPCs v2 de 0060 en page.tsx.

Purpose: BIO-02 (bio oficial: comisiones) + BIO-03 (partido directo en la ficha).
El chip de partido es reutilizable — Plan 03 lo consume en cross-links y directorio.

Output: PartidoChip + comisiones + militancias montados en la ficha, leyendo de las RPCs v2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-CONTEXT.md
@.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-UI-SPEC.md

<read_first>
- app/components/parlamentario-header.tsx — header ACTUAL (fila de chips flex-wrap, cargo, ProvenanceBadge; docstring LEGAL-03 a revertir)
- app/components/camara-chip.tsx — patrón EXACTO a espejar por PartidoChip (Badge variant outline, classify, return null si desconocida, tokens [var(--)])
- app/app/parlamentario/[id]/page.tsx:116-127 — getParlamentarioPublico (React.cache, .rpc parlamentario_publico → migrar a v2); HeaderSection:545-555; CarrilesSection:253 (dónde insertar la <section> de militancias mt-12)
- app/components/detalle-colapsable.tsx — DetalleColapsable (contrato no-leak: children server, defaultOpen=false, triggerLabel) para comisiones >5 y militancias históricas
- app/lib/format.ts:21 fechaCorta, :173 formatNombre; app/lib/types.ts (ParlamentarioPublicoRow ampliado + MilitanciaRow + ComisionRow de Plan 01)
- app/components/ui/tooltip.tsx / provenance-badge.tsx — idiom Tooltip Radix para el subtexto fuente+fecha del chip
- app/lib/types.ts sourceLabel — mapear origen → etiqueta de fuente legible
</read_first>

<interfaces>
Row-types disponibles (de Plan 01, types.ts):
- ParlamentarioPublicoRow: + partido: string|null, partido_fecha_captura: string|null, partido_origen: string|null
- MilitanciaRow: partido, desde, hasta, es_actual, origen, fecha_captura, enlace
- ComisionRow: nombre, camara, tipo, cargo, origen, fecha_captura, enlace

RPCs (allowlisted en Plan 01):
- parlamentario_publico_v2(p_id) → super-set de ParlamentarioPublicoRow con partido
- militancias_de_parlamentario(p_id) → MilitanciaRow[] (vigente primero)
- comisiones_de_parlamentario(p_id) → ComisionRow[] (alfabético)

Copy LOCKED (91-UI-SPEC §Copywriting): chip subtexto "según {fuente} al {fecha}";
militancias heading "Militancias registradas" + leyenda; "Vigente" etiqueta sobria;
empty comisiones "Sin comisiones registradas para este parlamentario en la fuente.";
empty militancia "Sólo se registra la militancia vigente; no hay tramos anteriores en la fuente."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PartidoChip reutilizable (neutro, tooltip fuente+fecha, null si sin dato)</name>
  <files>app/components/partido-chip.tsx, app/components/partido-chip.test.tsx</files>
  <behavior>
    - Con partido no-null: renderiza un Badge variant="outline" NEUTRO (bg-muted border-border text-foreground, JAMÁS color partidista) con el nombre del partido
    - Con partido null/vacío: retorna null (se omite, espejo CamaraChip desconocida) — NO renderiza "Sin partido"
    - El subtexto "según {fuente} al {fecha}" está disponible (Tooltip Radix o aria-label): fecha en font-mono vía fechaCorta
    - aria-label = "Partido: {partido}, según {origen} al {fecha}"
    - El MISMO partido en dos instancias produce el MISMO fondo (color nunca codifica identidad política)
  </behavior>
  <read_first>
    - app/components/camara-chip.tsx (espejo estructural: Badge, classify→null, tokens)
    - app/components/provenance-badge.tsx + app/components/ui/tooltip.tsx (idiom tooltip)
    - app/lib/format.ts:21 (fechaCorta), app/lib/types.ts (sourceLabel)
  </read_first>
  <action>
    Crear PartidoChip({ partido, fechaCaptura, origen }: { partido: string|null; fechaCaptura: string|Date|null; origen: string|null }).
    Si partido es null/"" → return null (VERBATIM del contrato CamaraChip §2 del UI-SPEC).
    Con dato: Badge variant="outline" className neutro (bg-muted border-border text-foreground — NUNCA una paleta por partido;
    prohibido codificar bloque/afinidad por color, principio anti-insinuación UI-SPEC §Color). Envolver el chip en Tooltip Radix
    (idiom ProvenanceBadge) cuyo contenido es `según {sourceLabel(origen)} al {fechaCorta(fecha)}` con la fecha en font-mono.
    aria-label completo en el trigger. min-h no requerido (no interactivo salvo el tooltip trigger). NUNCA la palabra "actual"
    sin la fecha de fuente.
    Escribir partido-chip.test.tsx PRIMERO (RTL): render con dato → chip visible + aria-label con fuente+fecha; render con null → nada;
    dos partidos distintos → mismo className de fondo (neutralidad de color).
  </action>
  <verify>
    <automated>cd app; pnpm test -- partido-chip 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test partido-chip` verde
    - grep confirma `bg-muted` y NINGÚN token de color cívico/partidista en partido-chip.tsx
    - Test cubre: con-dato, null→omite, neutralidad de color, aria-label con fuente+fecha
  </acceptance_criteria>
  <done>PartidoChip neutro, omite si null, expone fuente+fecha; tests RTL verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Bloque comisiones + sección militancias (vigente + acordeón histórico)</name>
  <files>app/components/comisiones-de-parlamentario.tsx, app/components/militancias-de-parlamentario.tsx</files>
  <read_first>
    - app/components/detalle-colapsable.tsx (DetalleColapsable: children server, triggerLabel, defaultOpen=false)
    - app/components/lobby-de-parlamentario.tsx (patrón de sección server con leyenda + empty honesto)
    - app/lib/format.ts (fechaCorta para rangos mono); 91-UI-SPEC §Component Contract 3 (militancias) + Header (comisiones)
  </read_first>
  <action>
    (a) ComisionesDeParlamentario({ comisiones }: { comisiones: ComisionRow[] }) — bloque presentacional (server-friendly).
    Lista `<ul>` compacta: cada `<li>` = `{nombre} · {tipo} · {cargo}` (tipo omitido si '', cargo omitido si null — sin "· ·" colgantes).
    Máx 5 visibles; si >5, envolver el excedente en DetalleColapsable ("Ver las N comisiones"). Vacío (0) → leyenda
    "Sin comisiones registradas para este parlamentario en la fuente." (NUNCA "no participa en comisiones"). Sin foto, sin PII.

    (b) MilitanciasDeParlamentario({ militancias }: { militancias: MilitanciaRow[] }) — su propia `<section className="mt-12">`
    (carril hermano, frontera anti-insinuación). Capa-1 FUERA del disclosure: h2 "Militancias registradas" + leyenda LOCKED
    "Afiliaciones partidarias según registro oficial, con la fecha de cada tramo. La militancia vigente aparece primero." +
    la militancia vigente (es_actual): partido + rango mono `{desde} – vigente` + etiqueta sobria "Vigente" (NO badge de alarma).
    Los tramos históricos (es_actual=false) van DENTRO de DetalleColapsable (cerrado): `<ul>` orden cronológico desc, cada `<li>`
    partido + rango mono `{desde} – {hasta}`; trigger "Ver militancias anteriores (N)". Si SOLO hay vigente → NO renderizar el
    trigger, mostrar leyenda "Sólo se registra la militancia vigente; no hay tramos anteriores en la fuente." Rótulo partido≠comité
    (Senado, UNA vez) si aplica. Fechas SIEMPRE font-mono.

    COPY: cero términos prohibidos (el guard de Plan 03 los escaneará). Sin "trayectoria" editorializada.
  </action>
  <verify>
    <automated>cd app; pnpm exec tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app exec tsc -b` exit 0
    - comisiones-de-parlamentario.tsx: usa DetalleColapsable solo si >5; leyenda empty honesta presente
    - militancias-de-parlamentario.tsx: `<section className="mt-12">`, "Militancias registradas", "Vigente", fechas en font-mono
    - Ningún token de color partidista; ningún campo rut/email/profesión
  </acceptance_criteria>
  <done>Comisiones y militancias como componentes tipados con estados vacíos honestos y acordeón; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 3: Cablear v2 en page.tsx — header con partido/comisiones + sección militancias</name>
  <files>app/components/parlamentario-header.tsx, app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx:116-127 (getParlamentarioPublico cache → migrar a v2), :253-471 (CarrilesSection, dónde va la <section> militancias), :545-555 (HeaderSection)
    - app/components/parlamentario-header.tsx (docstring LEGAL-03 a revertir; fila flex-wrap:69-71)
  </read_first>
  <action>
    (a) page.tsx: migrar `getParlamentarioPublico` de `parlamentario_publico` → `parlamentario_publico_v2` (mismo React.cache,
    mismo .maybeSingle<ParlamentarioPublicoRow>, mismo throw #34 en error). Todas las llamadas cacheadas (Rail/Header/Lobby/Financiamiento)
    heredan el super-set sin re-query. Añadir DOS lecturas server cacheadas nuevas: `comisiones_de_parlamentario(p_id)` (para el header)
    y `militancias_de_parlamentario(p_id)` (para la sección) — cada una su propia React.cache dedup, error → throw (nunca degradar a "sin datos").
    Insertar una `<section id="militancias" className="mt-12">` en CarrilesSection (carril hermano, posición coherente:
    tras el header, antes de los carriles de dominio o donde el UI-SPEC lo ubique) que renderiza MilitanciasDeParlamentario.

    (b) parlamentario-header.tsx: REVERTIR el docstring LEGAL-03 (documentar la decisión operador 2026-07-21 con referencia a
    PROJECT.md/CONTEXT: el partido del cargo electo es público). Añadir <PartidoChip partido={parlamentario.partido}
    fechaCaptura={parlamentario.partido_fecha_captura} origen={parlamentario.partido_origen ?? parlamentario.origen} /> en la
    MISMA fila `flex flex-wrap gap-2` que CamaraChip, DESPUÉS de CamaraChip (orden cámara→partido, UI-SPEC §Component 1).
    Recibir comisiones por prop (el header las renderiza como bloque bajo el cargo, ANTES del ProvenanceBadge, vía
    ComisionesDeParlamentario). Profesión: NO renderizar (bio 0 filas → campo omitido, sin "sin profesión"). Sin foto.
    El subtexto de fecha del chip va en el tooltip (fila densa) o como <p mt-1 text-xs font-mono> bajo la fila (elegir tooltip).
  </action>
  <verify>
    <automated>cd app; pnpm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test` verde (incluye page.test / header tests existentes)
    - `pnpm --filter ./app exec tsc -b` exit 0
    - grep en page.tsx: `parlamentario_publico_v2`, `comisiones_de_parlamentario`, `militancias_de_parlamentario` presentes
    - grep en parlamentario-header.tsx: `PartidoChip` renderizado en la fila de chips
    - Ninguna referencia a rut/email/profesión renderizada; el docstring LEGAL-03 revertido documenta 2026-07-21
  </acceptance_criteria>
  <done>Header muestra partido+comisiones, sección militancias montada, page.tsx lee las 3 RPCs v2; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server RSC → cliente | El service_role vive server-only; los componentes de detalle pasan como children a islands, nunca importan Supabase |
| datos maestros → render | El header debe renderizar SOLO campos del allowlist (partido público, cero rut/email/terceros) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-05 | Information Disclosure | header render | mitigate | PartidoChip/comisiones consumen SOLO row-types PII-safe (sin rut/email); profesión omitida (bio 0 filas) |
| T-91-06 | Repudiation | partido sin fuente | mitigate | chip SIEMPRE con "según {fuente} al {fecha}"; nunca "actual" sin fecha |
| T-91-07 | Information Disclosure | color partidista | accept→mitigate | chip NEUTRO bg-muted idéntico por partido; color nunca codifica identidad política (anti-insinuación) |
| T-91-SC | Tampering | npm installs | mitigate | CERO paquetes nuevos (Radix Accordion/Tooltip ya instalados) |
</threat_model>

<verification>
- `pnpm --filter ./app test` verde (RTL de PartidoChip + tests existentes de page/header)
- `pnpm --filter ./app exec tsc -b` exit 0
- Chip de partido neutro con fuente+fecha; comisiones con empty honesto; militancias vigente+acordeón
- Cero rut/email/profesión/foto en el header
</verification>

<success_criteria>
- PartidoChip reutilizable (neutro, omite si null, tooltip fuente+fecha)
- Bloque comisiones (DetalleColapsable si >5) + empty honesto
- Sección militancias (vigente primero + acordeón histórico) con copy LOCKED
- page.tsx lee parlamentario_publico_v2 + comisiones + militancias; header revierte LEGAL-03
</success_criteria>

<output>
Create `.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-02-SUMMARY.md` when done
</output>
