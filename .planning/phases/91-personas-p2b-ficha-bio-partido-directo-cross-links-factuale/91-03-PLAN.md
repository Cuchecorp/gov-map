---
phase: 91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale
plan: 03
type: execute
wave: 3
depends_on: [91-01, 91-02]
files_modified:
  - app/components/cross-links-parlamentario.tsx
  - app/components/parlamentarios-filtro.tsx
  - app/components/parlamentario-directory-row.tsx
  - app/app/parlamentario/[id]/page.tsx
  - app/app/parlamentarios/page.tsx
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [BIO-03, BIO-04]

must_haves:
  truths:
    - "La ficha muestra 4 bloques cross-link factuales (mismo partido / misma zona / misma comisión / co-autoría), cada uno con leyenda anti-causal"
    - "Cada bloque es su propia <section mt-12> con conteo honesto, máx 8 filas, 'Ver los N' si excede"
    - "El bloque vacío (N=0) se OMITE por completo"
    - "El propio parlamentario nunca aparece en sus bloques"
    - "/parlamentarios tiene faceta de filtro por partido client-side (island) con counts 'de estos N'"
    - "El PartidoChip aparece en las filas del directorio y en cruces con fuente+fecha"
    - "El linter anti-insinuación cubre las superficies nuevas y su mutation self-check muerde"
  artifacts:
    - path: "app/components/cross-links-parlamentario.tsx"
      provides: "4 bloques cross-link con leyenda LOCKED + conteo + 'Ver los N'"
    - path: "app/components/parlamentarios-filtro.tsx"
      provides: "Island client de filtro por partido (contrato FichaRail, cero Supabase)"
      contains: "use client"
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "SUPERFICIES_PERSONAS con las superficies nuevas + mutation self-check"
      contains: "SUPERFICIES"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "copartidarios_de_parlamentario / de_la_misma_zona / co_comisionados_de_parlamentario / coautores_de_parlamentario"
      via: ".rpc() server-only por bloque"
      pattern: "copartidarios_de_parlamentario|coautores_de_parlamentario"
    - from: "app/components/parlamentarios-filtro.tsx"
      to: "el slice serializado por el server"
      via: "filtrado EN MEMORIA, JAMÁS import de @/lib/supabase"
      pattern: "use client"
---

<objective>
Cerrar la ficha 360: 4 bloques de cross-links factuales anti-causales en la ficha,
el filtro por partido client-side en /parlamentarios (cierra FILT-01 para personas)
con PartidoChip en las filas, extender el linter anti-insinuación a las superficies
nuevas, y deployar a Cloudflare con verificación BrowserOS básica.

Purpose: BIO-04 (cross-links factuales, jamás afinidad inferida) + BIO-03 (partido
correlacionado en filtros/directorio/cruces). Es el titular visible de la pasada 2.

Output: Cross-links + filtro partido + linter extendido + deploy PROD verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-CONTEXT.md
@.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-UI-SPEC.md

<read_first>
- app/components/buscar-filtros.tsx — ANALOG EXACTO del island de filtro (FacetChip petróleo, counts "de estos N", contrato no-Supabase, orden freq desc→alfabético, sin_dato bucket); espejar su estructura para el filtro por partido
- app/app/parlamentarios/page.tsx — directorio ACTUAL (form GET SSR cámara+q; DirectoryList lee parlamentarios_publico → migrar a v2; ParlamentarioDirectoryRow)
- app/components/parlamentario-directory-row.tsx — fila del directorio (docstring "SIN partido LEGAL-03" a revertir; añadir PartidoChip)
- app/app/parlamentario/[id]/page.tsx:253-471 CarrilesSection (dónde insertar las 4 <section mt-12> de cross-link); getParlamentarioPublico ya en v2 (Plan 02)
- app/components/partido-chip.tsx — PartidoChip de Plan 02 (reutilizar en filas de cruces y directorio)
- app/lib/anti-insinuacion-guard.test.ts — guard: arrays SUPERFICIES_*, TERMINOS_PROHIBIDOS, detectarInsinuaciones, mutation self-check (Test 2)
- app/components/lobby-de-parlamentario.tsx:14-46 — plantilla de leyenda anti-causal por sección
- app/lib/types.ts — CrossLinkRow, ParlamentarioListadoRow ampliado (Plan 01)
</read_first>

<interfaces>
RPCs cross-link (allowlisted, Plan 01) → CrossLinkRow[]:
- copartidarios_de_parlamentario(p_id) → id, nombre, camara
- de_la_misma_zona(p_id) → id, nombre, camara
- co_comisionados_de_parlamentario(p_id) → id, nombre, camara, comision_nombre
- coautores_de_parlamentario(p_id) → id, nombre, camara, n_proyectos
- parlamentarios_publico_v2() → ParlamentarioListadoRow + partido/partido_fecha_captura/partido_origen

Copy LOCKED (91-UI-SPEC §Copywriting):
- Headings: "Del mismo partido" / "De la misma zona" / "En la misma comisión" / "Han co-firmado proyectos"
- Leyenda anti-causal por bloque (VERBATIM 1×): "Relación DECLARADA por una fuente oficial (militancia, comisión o autoría de proyecto). No implica afinidad, coordinación ni causalidad."
- Conteo: "{N} parlamentarios comparten {X}." · "Ver los {N}" → directorio pre-filtrado
- Filtro: legend "Partido"; leyenda "Conteos sobre estos {N} parlamentarios cargados, no sobre todo el Congreso."; chip "{Partido} · {count}"; sin dato → "Sin dato · {count}"
- Términos PROHIBIDOS nuevos: "aliado", "cercano a", "bloque de", "afín", "coordina con", "alineado" (además de los ya en el guard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 bloques cross-link en la ficha + cableado v2 en page.tsx</name>
  <files>app/components/cross-links-parlamentario.tsx, app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx (leyenda anti-causal por sección)
    - app/components/partido-chip.tsx (chip por fila opcional); app/components/camara-chip.tsx
    - app/app/parlamentario/[id]/page.tsx:253-471 (CarrilesSection); 91-UI-SPEC §Component Contract 4
  </read_first>
  <action>
    Crear un componente genérico de bloque cross-link (server-friendly): CrossLinkBloque({ heading, conteoTexto, filas, verTodosHref, verTodosN }).
    Estructura VERBATIM del UI-SPEC §4: `<section className="mt-12">` → h2 heading → `<p>` leyenda anti-causal LOCKED (1×, VERBATIM) →
    `<p>` conteo honesto "{N} parlamentarios comparten {X}." → `<ul>` máx 8 filas, cada `<li>` = [CamaraChip] <Link petróleo a
    /parlamentario/{id}>{nombre}</Link> [PartidoChip opcional] → `<a>` "Ver los {N}" (petróleo) SOLO si N>8 → navega a directorio
    pre-filtrado (/parlamentarios?partido=X | ?comision= | ?region= según eje). Bloque vacío (filas.length===0) → la `<section>` NO se
    renderiza (return null). Orden EXACTAMENTE el que emite la RPC (alfabético/cámara — NUNCA re-ordenar por n_proyectos: sería ranking).
    El PartidoChip por fila: incluirlo en "misma comisión"/"misma zona"; OMITIRLO en "mismo partido" (redundante).

    En page.tsx: 4 lecturas server cacheadas (React.cache, error→throw #34) — copartidarios/de_la_misma_zona/co_comisionados/coautores.
    Insertar las 4 `<section>` (vía CrossLinkBloque) dentro de CarrilesSection como carriles hermanos mt-12, tras la sección
    militancias de Plan 02. Cada bloque su propia lectura + su propio <Suspense> (streaming independiente, un fallo no tumba la ficha).
    Auto-exclusión ya la garantiza la RPC (where <> p_id). Conteo honesto = filas totales que emite la RPC (no un ranking).
  </action>
  <verify>
    <automated>cd app; pnpm exec tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app exec tsc -b` exit 0
    - cross-links-parlamentario.tsx: la leyenda anti-causal LOCKED aparece 1× por bloque; return null si filas vacías
    - Cada bloque es `<section className="mt-12">`; link a /parlamentario/{id} petróleo; "Ver los N" solo si >8
    - grep en page.tsx: las 4 RPCs cross-link presentes; ninguna re-ordena por n_proyectos
    - Cero término prohibido en el copy (validado por el guard en Task 3)
  </acceptance_criteria>
  <done>4 bloques cross-link factuales con leyenda anti-causal, conteo honesto y "Ver los N"; page.tsx lee las 4 RPCs.</done>
</task>

<task type="auto">
  <name>Task 2: Filtro por partido en /parlamentarios (island) + PartidoChip en filas del directorio</name>
  <files>app/components/parlamentarios-filtro.tsx, app/components/parlamentario-directory-row.tsx, app/app/parlamentarios/page.tsx</files>
  <read_first>
    - app/components/buscar-filtros.tsx (island analog COMPLETO: FacetChip, counts, contrato no-Supabase, sin_dato)
    - app/app/parlamentarios/page.tsx (DirectoryList lee parlamentarios_publico → v2; form GET SSR cámara/q intacto)
    - app/components/parlamentario-directory-row.tsx (fila; revertir docstring "sin partido")
  </read_first>
  <action>
    (a) parlamentarios-filtro.tsx: island "use client" (contrato FichaRail: JAMÁS importa @/lib/supabase ni .rpc/.from).
    Recibe el slice serializado (ParlamentarioListadoRow[] con partido) del server, ya filtrado grueso por el form GET SSR
    (cámara/q). Faceta CLIENT "Partido" (fieldset/legend, FacetChip petróleo — espejo VERBATIM buscar-filtros.tsx): chips
    "{Partido} · {count}", count=0 → disabled/opacity-40, engaged → petróleo, orden freq desc→alfabético. Filas con partido null →
    bucket "Sin dato · {count}" (nunca ocultar; espejo ANIO_SIN_DATO). Counts "de estos N" (leyenda LOCKED). Filtra EN MEMORIA;
    renderiza las filas (ParlamentarioDirectoryRow) del subconjunto. Empty tras filtro → heading "Sin parlamentarios para este partido"
    + body "Ajusta o quita filtros para ver más." (distinto del banner de error, que lo lanza el server). min-h-11 + focus-visible en chips.

    (b) parlamentarios/page.tsx: DirectoryList migra a `parlamentarios_publico_v2` (error→throw). Pasa el slice serializado al island
    parlamentarios-filtro (el form GET SSR cámara/q sigue filtrando grueso server-side; el island afina por partido — ortogonales).

    (c) parlamentario-directory-row.tsx: revertir el docstring LEGAL-03 (decisión operador 2026-07-21). Añadir <PartidoChip> en la
    fila (partido/partido_fecha_captura/partido_origen del row v2). ParlamentarioListadoRow ya trae los campos (Plan 01). Sin foto/rut/email.
  </action>
  <verify>
    <automated>cd app; pnpm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test` verde (incluye parlamentarios/page.test)
    - `pnpm --filter ./app exec tsc -b` exit 0
    - parlamentarios-filtro.tsx tiene "use client" y NO importa @/lib/supabase (guard PII lockdown lo verificará)
    - grep en page.tsx: `parlamentarios_publico_v2`; grep en directory-row: `PartidoChip`
    - Counts honestos "de estos N", bucket "Sin dato", empty-state honesto ≠ error
  </acceptance_criteria>
  <done>Filtro por partido island (cierra FILT-01 personas) + PartidoChip en filas del directorio; suite verde.</done>
</task>

<task type="auto">
  <name>Task 3: Extender linter anti-insinuación a superficies nuevas + deploy Cloudflare + verificación BrowserOS</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (arrays SUPERFICIES_*, TERMINOS_PROHIBIDOS, detectarInsinuaciones, mutation self-check Test 2)
    - CLAUDE.md (deploy runbook Docker node:22-slim; wrangler local); memoria: build OpenNext en Docker Linux (Windows worker roto)
  </read_first>
  <action>
    (a) anti-insinuacion-guard.test.ts: añadir un array `SUPERFICIES_PERSONAS` con las superficies nuevas
    (components/partido-chip.tsx, components/comisiones-de-parlamentario.tsx, components/militancias-de-parlamentario.tsx,
    components/cross-links-parlamentario.tsx, components/parlamentarios-filtro.tsx, components/parlamentario-directory-row.tsx,
    components/parlamentario-header.tsx). Incluirlo en el bucle del guard (Test 1): `[...SUPERFICIES_VOTO, ...MONEY, ...HOME,
    ...BUSQUEDA, ...SUPERFICIES_PERSONAS]`. Añadir a TERMINOS_PROHIBIDOS los términos nuevos con tildes exactas: "aliado",
    "cercano a", "bloque de", "afín", "coordina con", "alineado" (verificar que no dupliquen los existentes; "alineado" ya está).
    Ampliar el mutation self-check (Test 2) con un fixture EN MEMORIA que inyecte un término personas nuevo (p.ej. "cercano a su
    bloque") y asserta que detectarInsinuaciones lo caza → el guard MUERDE sobre lo nuevo. Añadir un caso no-falso-positivo:
    la leyenda anti-causal LOCKED de cross-links CONTIENE 'afinidad' (ya en TERMINOS_PROHIBIDOS:178) → es OBLIGATORIO añadir la leyenda VERBATIM (whitespace-normalizada) a NEGACIONES_LOCKED ANTES de añadir cross-links-parlamentario.tsx al scan (mismo tratamiento que las leyendas VOTO/MONEY en :243-250); el mutation self-check debe inyectar un término FRESCO no-negado (p.ej. 'cercano a su bloque') para probar que el guard muerde. Además: dedupe los términos nuevos contra TERMINOS_PROHIBIDOS existente (alineado YA está — no re-agregar) y verifica con grep que ningún término nuevo ('afín', 'aliado', 'cercano a', 'bloque de', 'coordina con') aparece en copy renderizado de las 7 superficies nuevas antes de activar el scan.

    (b) Correr el guard: `pnpm --filter ./app test anti-insinuacion` → 0 offenders sobre las superficies nuevas. Si alguna superficie
    dispara un término → corregir el COPY (no el guard).

    (c) Deploy Cloudflare al cierre (runbook: build OpenNext en Docker node:22-slim en Linux — NUNCA Windows, worker roto; wrangler
    local OAuth; migraciones ya aplicadas en Plan 01). Registrar la versión de deploy en el summary.

    (d) Verificación BrowserOS BÁSICA de la ficha en PROD: partido visible con fuente+fecha; un bloque cross-link con su leyenda
    anti-causal visible. Si el deploy o la verificación requieren credenciales de operador no disponibles → documentar como gate de
    operador con los pasos exactos (espejo de gates BrowserOS previos), NO fabricar éxito.
  </action>
  <verify>
    <automated>cd app; pnpm test -- anti-insinuacion 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test anti-insinuacion` verde (0 offenders sobre SUPERFICIES_PERSONAS)
    - El mutation self-check falla ante un término personas inyectado (guard muerde sobre lo nuevo)
    - `pnpm --filter ./app test` raíz verde (suite completa)
    - Deploy Cloudflare registrado (versión) o gate de operador documentado
    - Verificación BrowserOS: partido con fuente+fecha + leyenda cross-link, o gate documentado
  </acceptance_criteria>
  <done>Linter extendido y mordiendo sobre lo nuevo; deploy a PROD (o gate documentado); ficha verificada en vivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| island client → red | El filtro por partido es "use client": NUNCA debe tocar Supabase (contrato FichaRail); el guard PII escanea .from/.rpc en app/ |
| copy renderizado → ciudadano | Los cross-links son el vector #1 de insinuación de afinidad; el copy debe ser estrictamente factual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-08 | Information Disclosure | filtro island | mitigate | parlamentarios-filtro.tsx no importa @/lib/supabase; lockdown-guard escanea .from/.rpc en app/; slice ya PII-safe (RPC v2) |
| T-91-09 | Spoofing/Repudiation | cross-link afinidad inferida | mitigate | leyenda anti-causal LOCKED 1×/bloque; orden neutral (nunca por n_proyectos); linter anti-insinuación extendido + mutation self-check |
| T-91-10 | Tampering | linter no-op | mitigate | mutation self-check inyecta término personas nuevo y asserta que el guard lo caza |
| T-91-11 | Denial of Service | RPC cross-link | mitigate | LIMIT bounded en las RPCs (Plan 01); "Ver los N" navega, no expande lista infinita |
| T-91-SC | Tampering | npm installs | mitigate | CERO paquetes nuevos |
</threat_model>

<verification>
- `pnpm --filter ./app test` raíz verde (RTL + guards: lockdown, anti-insinuación, PII, cero-hex)
- `pnpm --filter ./app exec tsc -b` exit 0
- 4 bloques cross-link con leyenda anti-causal; bloque vacío omitido; auto-exclusión
- Filtro partido island sin Supabase; PartidoChip en filas del directorio
- Linter extendido muerde sobre superficies personas; deploy PROD registrado
- Verificación BrowserOS: partido con fuente+fecha + leyenda cross-link visible (o gate documentado)
</verification>

<success_criteria>
- 4 bloques cross-link factuales anti-causales en la ficha (mismo partido/zona/comisión/co-autoría)
- Filtro por partido client-side en /parlamentarios (cierra FILT-01 personas) + counts honestos
- PartidoChip en filas del directorio y en cruces con fuente+fecha
- Linter anti-insinuación extendido + mutation self-check mordiendo sobre lo nuevo
- Deploy Cloudflare + verificación BrowserOS básica de la ficha (o gate de operador documentado)
</success_criteria>

<output>
Create `.planning/phases/91-personas-p2b-ficha-bio-partido-directo-cross-links-factuale/91-03-SUMMARY.md` when done
</output>
