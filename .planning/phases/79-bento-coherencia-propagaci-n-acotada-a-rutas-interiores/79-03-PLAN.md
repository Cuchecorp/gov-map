---
phase: 79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores
plan: 03
type: execute
wave: 2
depends_on: ["79-01", "79-02"]
files_modified:
  - app/app/red/page.test.tsx
  - app/lib/bento-coherencia-guard.test.ts
autonomous: true
requirements: [BENTO-04]
must_haves:
  truths:
    - "/red NO cambió de ancho: su <main> sigue en max-w-3xl (isla .net-* pixel-intocable, invariante 4)"
    - "red-graph.test.tsx (.net-chip 11px) sigue verde"
    - "components/ui/card.tsx sigue sin editar (firewall D3), verificado por un guard source-scan que MUERDE"
    - "La suite app completa está verde (base 870) y tsc limpio tras propagar la coherencia a 8 rutas"
    - "La exclusión de /red queda documentada en el SUMMARY con su racional"
  artifacts:
    - path: "app/lib/bento-coherencia-guard.test.ts"
      provides: "guard source-scan que congela el firewall D3 (card.tsx sin rounded bento) y la exclusión de /red (max-w-3xl, no 1120px), con mutation self-check"
      contains: "card.tsx"
    - path: "app/app/red/page.test.tsx"
      provides: "assert de que /red NO fue ensanchado (max-w-3xl presente, max-w-[1120px] ausente en su <main>)"
      contains: "max-w-3xl"
  key_links:
    - from: "app/lib/bento-coherencia-guard.test.ts"
      to: "app/components/ui/card.tsx + app/app/red/page.tsx"
      via: "source-scan que falla si card.tsx recibe rounded bento o /red recibe max-w-[1120px]"
      pattern: "max-w-3xl"
---

<objective>
Cerrar la fase con los candados de no-regresión de BENTO-04: (1) un guard source-scan (`bento-coherencia-guard.test.ts`) que CONGELA el firewall D3 (card.tsx nunca recibe `rounded-[var(--radius-tile)]`) y la exclusión de /red (su `<main>` sigue en `max-w-3xl`, nunca `max-w-[1120px]`), con mutation self-check que prueba que muerde; (2) extender el test de página de /red para afirmar la no-regresión de ancho; (3) verificar la suite completa verde + tsc + guard tipográfico + `.net-chip` 11px; (4) documentar la exclusión de /red en el SUMMARY con su racional.

Purpose: La coherencia se propagó a 8 rutas (Plans 01+02); esta fase debe garantizar que NO se propagó a /red (invariante 4) ni al primitivo card.tsx (firewall D3), y que nada se rompió. El gate visual real (getComputedStyle en deploy) es Phase 81 — aquí solo los candados a nivel de clase/source.
Output: 1 guard nuevo + 1 test de /red extendido + SUMMARY con la exclusión documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-UI-SPEC.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-CONTEXT.md

<interfaces>
<!-- Verificado 2026-07-15. Depende de que Plans 01+02 estén aplicados. -->

Firewall D3 (verificado): app/components/ui/card.tsx L12 tiene `"rounded-lg border bg-card ..."`. El firewall exige que card.tsx NUNCA contenga `rounded-[var(--radius-tile)]` (el swap va SOLO en call sites). El guard debe fallar si ese string aparece en card.tsx.

Exclusión /red (invariante 4, verificado): app/app/red/page.tsx tiene DOS `<main className="max-w-3xl mx-auto ...">` (L82 rama picker/honest, L163 rama grafo). AMBOS deben seguir en `max-w-3xl` — /red queda EXCLUIDO del contenedor 1120px. El guard debe fallar si `max-w-[1120px]` aparece en red/page.tsx. La isla `.net-*` (incl. `.net-chip` 11px, DEBT-05) es pixel-intocable.

Guards existentes que sirven de IDIOMA (source-scan + mutation self-check):
  app/lib/lockdown-guard.test.ts, app/lib/name-match-rut-guard.test.ts, app/lib/anti-insinuacion-guard.test.ts, app/lib/money-antiflip-guard.test.ts — todos leen archivos como texto con `readFileSync` + `process.cwd()` (NO import.meta.url — lección 45-01: jsdom rompe `new URL(import.meta.url)`), asertan presencia/ausencia de strings, e incluyen un mutation self-check EN MEMORIA (el detector aplicado a una cadena mutada DEBE fallar) para probar que el guard muerde.
  app/app/globals.test.ts — source-scan de globals.css (idioma readFileSync + process.cwd()).

Guard `.net-chip` 11px: app/components/red/red-graph.test.tsx — ya verde, NO se toca; solo se verifica que siga verde.

Test de página /red: app/app/red/page.test.tsx (ya cubre el gate NET-02 por comportamiento). Extender con un assert a nivel de source/markup: /red mantiene `max-w-3xl` y NO `max-w-[1120px]`.

jsdom NO ve px. Todos los asserts de esta fase son a nivel de CLASE/SOURCE. El getComputedStyle real de /red (no-regresión de ancho) es el gate de Phase 81.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard source-scan del firewall D3 + exclusión /red (con mutation self-check)</name>
  <files>app/lib/bento-coherencia-guard.test.ts</files>
  <read_first>
    - app/lib/lockdown-guard.test.ts (idioma readFileSync + process.cwd() + mutation self-check)
    - app/lib/name-match-rut-guard.test.ts (mutation self-check EN MEMORIA por eje)
    - app/components/ui/card.tsx (L12 rounded-lg — no debe recibir bento radius)
    - app/app/red/page.tsx (L82,L163 max-w-3xl — no debe recibir 1120px)
  </read_first>
  <action>
    Crear `app/lib/bento-coherencia-guard.test.ts` espejando el idioma de lockdown-guard.test.ts (readFileSync + `process.cwd()`, NUNCA `import.meta.url`):
    - Detector puro que recibe (contenidoCard, contenidoRed) y devuelve offenders: (a) card.tsx contiene `rounded-[var(--radius-tile)]` → offender FIREWALL (viola D3); (b) red/page.tsx contiene `max-w-[1120px]` → offender EXCLUSION-RED (viola invariante 4); (c) red/page.tsx NO contiene `max-w-3xl` → offender EXCLUSION-RED (perdió su ancho propio).
    - Test verde: aplicar el detector a los archivos REALES → 0 offenders (Plans 01+02 respetaron el firewall y la exclusión).
    - Mutation self-check EN MEMORIA por eje: aplicar el detector a un card.tsx mutado (con el string bento) → DEBE reportar FIREWALL; a un red mutado (con max-w-[1120px] y sin max-w-3xl) → DEBE reportar EXCLUSION-RED. Prueba que el guard muerde.
    Cero `#` hex en el archivo. Preferir `[var(--radius-tile)]` literal en el detector.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run bento-coherencia-guard 2>$null; exit $LASTEXITCODE</automated>
  </verify>
  <done>bento-coherencia-guard.test.ts verde: 0 offenders sobre archivos reales + mutation self-check muerde por ambos ejes (firewall + exclusión /red).</done>
</task>

<task type="auto">
  <name>Task 2: Assert de no-regresión de ancho en el test de página de /red</name>
  <files>app/app/red/page.test.tsx</files>
  <read_first>
    - app/app/red/page.test.tsx (gate NET-02 por comportamiento; cómo mockea notFound/supabase)
    - app/app/red/page.tsx (L82,L163 <main max-w-3xl>)
  </read_first>
  <action>
    Añadir un `it` que afirme a nivel de source/markup que /red NO fue ensanchado: su `<main>` mantiene `max-w-3xl` y NO contiene `max-w-[1120px]` (invariante 4: /red excluido del contenedor 1120px; la isla `.net-*` intocable). Reusar el patrón de mock ya presente en el archivo (notFound sentinel + createServerSupabase mock) o leer el source de red/page.tsx por readFileSync si el assert es estructural. Comentar que la no-regresión de PÍXELES real (getComputedStyle) se valida en Phase 81. Cero hex.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run red 2>$null; exit $LASTEXITCODE</automated>
  </verify>
  <done>page.test.tsx de /red verde con assert de `max-w-3xl` presente y `max-w-[1120px]` ausente; red-graph.test.tsx (.net-chip 11px) sigue verde.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de no-regresión de fase (suite completa + tsc + guards) y SUMMARY con exclusión /red</name>
  <files>.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-03-SUMMARY.md</files>
  <read_first>
    - app/app/globals.test.ts (guard tipográfico de tokens — debe seguir verde)
    - app/components/red/red-graph.test.tsx (.net-chip 11px — debe seguir verde)
  </read_first>
  <action>
    Correr el gate de fase completo y documentar. En el SUMMARY (usando el template) registrar EXPLÍCITAMENTE, además del resultado estándar:
    - La DECISIÓN de exclusión de /red y su RACIONAL (CONTEXT §/red): mover /red a 1120px cambiaría el ancho disponible del grafo (layout B aprobado 2026-07-13) sin beneficio de coherencia comparable; el chrome global (header/footer) ya da la coherencia; la no-regresión de px se confirma con el gate visual getComputedStyle de Phase 81 (que también cierra el gate 75).
    - Que /admin/revisar-entidades NO se tocó (sin valor de coherencia pública).
    - Que las fichas NO recibieron swap de radio (correcto por construcción: únicos rounded-lg = skeletons) y que el scroll-mt-6 local fue reconciliado con el header de 80px.
    - Recordatorio al orquestador: las capturas BrowserOS antes/después POR RUTA (8 rutas, incl. /red no-regresión) son responsabilidad del ORQUESTADOR sobre `pnpm --filter ./app dev`, archivadas en `captures/` — NO son tarea del executor.
    NO editar código de producción en este task.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run 2>$null; if($LASTEXITCODE -ne 0){exit 1}; pnpm --filter ./app exec tsc --noEmit; exit $LASTEXITCODE</automated>
  </verify>
  <done>Suite app verde (base 870 + tests nuevos), tsc limpio, globals.test.ts + red-graph.test.tsx verdes; SUMMARY documenta la exclusión de /red + racional + no-tocado /admin + fichas sin swap de radio + handoff de capturas al orquestador.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno nuevo) | Fase 100% presentación: cero datos, cero RPC, cero red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05 | Tampering | card.tsx firewall D3 | mitigate | bento-coherencia-guard.test.ts falla si card.tsx recibe `rounded-[var(--radius-tile)]`; mutation self-check prueba que muerde. |
| T-79-06 | Tampering | exclusión /red (invariante 4) | mitigate | guard + page.test.tsx fallan si /red recibe `max-w-[1120px]` o pierde `max-w-3xl`; .net-chip 11px verde; px real en Phase 81. |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes; gate de legitimidad no aplica. |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` verde (base 870 + tests nuevos de Plans 01/02/03).
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- globals.test.ts (tokens tipográficos) + red-graph.test.tsx (.net-chip 11px) verdes.
- bento-coherencia-guard.test.ts muerde por ambos ejes (firewall D3 + exclusión /red).
- SUMMARY documenta la exclusión de /red con racional.
</verification>

<success_criteria>
- Guard source-scan verde que congela el firewall D3 y la exclusión de /red, con mutation self-check.
- /red sin cambio de ancho (max-w-3xl; no 1120px); .net-* intocado.
- Suite completa verde + tsc + guards tipográfico/net-chip verdes.
- Exclusión de /red documentada en el SUMMARY; handoff de capturas al orquestador registrado.
</success_criteria>

<output>
Create `.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-03-SUMMARY.md` when done
</output>
