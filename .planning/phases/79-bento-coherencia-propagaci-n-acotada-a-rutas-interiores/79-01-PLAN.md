---
phase: 79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/buscar/page.tsx
  - app/app/parlamentarios/page.tsx
  - app/app/agenda/page.tsx
  - app/app/sobre/page.tsx
  - app/app/metodologia/page.tsx
  - app/components/search-result-card.tsx
  - app/components/parlamentario-directory-row.tsx
  - app/app/buscar/coverage.test.tsx
  - app/app/parlamentarios/page.test.tsx
  - app/app/agenda/page.test.tsx
  - app/components/search-result-card.test.tsx
autonomous: true
requirements: [BENTO-04]
must_haves:
  truths:
    - "Al navegar de la home a /buscar, /parlamentarios, /agenda, /sobre o /metodologia el ancho de contenido es el mismo (1120px) — no se siente como cambiar de sitio"
    - "Las tarjetas de primer nivel de /buscar, /parlamentarios y /agenda tienen la esquina bento (--radius-tile 16px), no la esquina vieja rounded-lg"
    - "Los interiores (skeletons, inputs de filtro, pills, acordeones) quedan byte-idénticos: misma esquina que antes"
    - "components/ui/card.tsx NO se edita (firewall D3); el swap ocurre en el call site vía className"
  artifacts:
    - path: "app/app/buscar/page.tsx"
      provides: "container 1120px + empty/error box radius bento"
      contains: "max-w-[1120px]"
    - path: "app/components/search-result-card.tsx"
      provides: "override rounded-[var(--radius-tile)] sobre el Card de primer nivel"
      contains: "rounded-[var(--radius-tile)]"
    - path: "app/components/parlamentario-directory-row.tsx"
      provides: "root de fila directorio con radius bento"
      contains: "rounded-[var(--radius-tile)]"
    - path: "app/app/agenda/page.tsx"
      provides: "li de resultado + empty boxes con radius bento, container 1120px"
      contains: "max-w-[1120px]"
  key_links:
    - from: "app/app/buscar/page.tsx"
      to: "SearchResultCard"
      via: "call site render — la tarjeta de resultado es de primer nivel"
      pattern: "SearchResultCard"
    - from: "app/app/parlamentarios/page.tsx"
      to: "ParlamentarioDirectoryRow"
      via: "root de la fila swap a --radius-tile"
      pattern: "rounded-\\[var\\(--radius-tile\\)\\]"
---

<objective>
Propagar la coherencia bento ACOTADA a las 5 rutas de primer nivel (/buscar, /parlamentarios, /agenda, /sobre, /metodologia) y a sus 2 componentes de tarjeta de primer nivel (SearchResultCard, ParlamentarioDirectoryRow): (1) ensanchar el `<main>` de cada página a `max-w-[1120px]`, y (2) cambiar SOLO la esquina de las tarjetas de PRIMER NIVEL a `rounded-[var(--radius-tile)]`. Cero re-layout, cero hex nuevo, interiores byte-idénticos.

Purpose: Salir de la home no debe sentirse como cambiar de sitio (BENTO-04). Es un swap quirúrgico de dos clases por ruta, no un rediseño.
Output: 7 archivos de producción con clases swapeadas + tests de ruta extendidos con asserts de container/radius.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-UI-SPEC.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-CONTEXT.md

<interfaces>
<!-- Contratos verificados por lectura directa 2026-07-15 (grep + read). El executor NO debe re-explorar: aquí está el estado exacto de cada call site. -->

Regla cardinal (invariante 5): la ÚNICA clase nueva permitida es `rounded-[var(--radius-tile)]`.
Sintaxis Tailwind 4 SIEMPRE `[var(--radius-tile)]`, NUNCA `[--radius-tile]` (CR-01 Phase 76: `[--token]` compila a CSS inválido). Cero `#` hex en cualquier archivo tocado.

Contenedores actuales (verificado):
  app/app/buscar/page.tsx        L60  `<main className="max-w-3xl mx-auto px-4 md:px-8 py-8 md:py-16">`
  app/app/parlamentarios/page.tsx L43  `<main className="max-w-5xl ...">`
  app/app/agenda/page.tsx        L76  `<main className="max-w-3xl ...">`
  app/app/sobre/page.tsx         L20  `<main className="max-w-3xl ...">`
  app/app/metodologia/page.tsx   L21  `<main className="max-w-3xl ...">`
→ En cada uno: `max-w-3xl`/`max-w-5xl` → `max-w-[1120px]`. Preservar VERBATIM `mx-auto px-4 md:px-8 py-8 md:py-16`.

Radius de primer nivel — SWAP (verificado):
  buscar: SearchResultCard renderiza `<Card>` PELADO (search-result-card.tsx L39, sin className) → el Card es la tarjeta de primer nivel. Añadir `className="rounded-[var(--radius-tile)]"` al `<Card>` en search-result-card.tsx L39 (NO editar card.tsx). card.tsx L12 tiene `rounded-lg` hardcodeado; tailwind-merge hace ganar la última clase.
  buscar page.tsx: empty-state box L105 `rounded-lg` → `rounded-[var(--radius-tile)]`; error boxes L93 y L139 `rounded-lg` → `rounded-[var(--radius-tile)]`.
  parlamentarios: parlamentario-directory-row.tsx L34 root `block rounded-lg border ...` → `rounded-[var(--radius-tile)]`; parlamentarios page.tsx L128 honest-empty `rounded-lg` → `rounded-[var(--radius-tile)]`.
  agenda page.tsx: `<li>` de resultado L238 `rounded-lg border bg-card p-4` → `rounded-[var(--radius-tile)]`; empty boxes L199 y L191 `rounded-lg` → `rounded-[var(--radius-tile)]`.

INTERIORES byte-idénticos (NO tocar):
  buscar: skeleton Cards (L211) y pill `rounded-full` (L214-216) quedan.
  parlamentarios: inputs de filtro L76/L84 `rounded-lg`, botón L93, skeleton rows L162 quedan (son controles/loading, no tarjetas de contenido).
  agenda: form input/button `rounded-md` (L87/L92), filter pills `rounded-full` (L163), skeleton L573, CitacionCard/SalaTableSection/CarrilAccordion internals quedan.
  sobre, metodologia: SON PÁGINAS PROSA (`<section>` + `<Separator>`, sin `rounded-*`). SOLO swap de container. Radius: nada que swapear.

Convención de tests por ruta (verificado):
  parlamentarios/page.test.tsx + buscar/coverage.test.tsx usan `renderToStaticMarkup` → assert sobre el string HTML (`expect(html).toContain("max-w-[1120px]")`).
  agenda/page.test.tsx + search-result-card.test.tsx usan RTL `render` + `screen`/`container` → assert sobre `container.innerHTML` o `.className`.
  jsdom NO ve layout (px). Los asserts son a NIVEL DE CLASE (presencia del string de clase), nunca de píxeles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap container + radius en /buscar, /parlamentarios, /agenda (rutas con tarjetas)</name>
  <files>app/app/buscar/page.tsx, app/components/search-result-card.tsx, app/app/parlamentarios/page.tsx, app/components/parlamentario-directory-row.tsx, app/app/agenda/page.tsx</files>
  <read_first>
    - app/app/buscar/page.tsx (L60 container; L93, L105, L139 boxes rounded-lg)
    - app/components/search-result-card.tsx (L39 `<Card>` pelado)
    - app/app/parlamentarios/page.tsx (L43 container; L128 empty box)
    - app/components/parlamentario-directory-row.tsx (L34 root rounded-lg)
    - app/app/agenda/page.tsx (L76 container; L191, L199 empty; L238 li)
  </read_first>
  <action>
    Aplicar los swaps enumerados en el bloque `<interfaces>` (BENTO-04, per D3):
    (1) Container: en cada `<main>` de buscar/parlamentarios/agenda cambiar `max-w-3xl`/`max-w-5xl` → `max-w-[1120px]`, preservando el resto de clases VERBATIM.
    (2) Radius primer nivel: buscar → añadir `className="rounded-[var(--radius-tile)]"` al `<Card>` en search-result-card.tsx L39, y swapear los 3 boxes empty/error (L93, L105, L139) de `rounded-lg` a `rounded-[var(--radius-tile)]`; parlamentarios → swap root de parlamentario-directory-row.tsx L34 y empty box page L128; agenda → swap `<li>` L238 y empty boxes L191/L199.
    NO editar app/components/ui/card.tsx (firewall D3 — filtraría a interiores). NO tocar skeletons, inputs de filtro, pills `rounded-full`, ni internals de acordeones/charts/tablas (byte-idénticos, invariante D3). Cero `#` hex. Sintaxis SIEMPRE `[var(--radius-tile)]`.
  </action>
  <verify>
    <automated>cd app; pnpm exec tsc --noEmit; if($LASTEXITCODE -ne 0){exit 1}; if (Select-String -Path components/ui/card.tsx -Pattern "rounded-\[var" -Quiet) { Write-Error "card.tsx fue editado — viola firewall D3"; exit 1 }; git diff --quiet -- components/ui/card.tsx; if($LASTEXITCODE -ne 0){Write-Error "card.tsx modificado"; exit 1}</automated>
  </verify>
  <done>tsc limpio; los 3 `<main>` con `max-w-[1120px]`; SearchResultCard/DirectoryRow/li-agenda/empty-boxes con `rounded-[var(--radius-tile)]`; card.tsx sin cambios (git diff limpio); skeletons/controles/pills intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Swap container en /sobre y /metodologia (prosa, solo container)</name>
  <files>app/app/sobre/page.tsx, app/app/metodologia/page.tsx</files>
  <read_first>
    - app/app/sobre/page.tsx (L20 container)
    - app/app/metodologia/page.tsx (L21 container)
  </read_first>
  <action>
    En el `<main>` de sobre (L20) y metodologia (L21) cambiar `max-w-3xl` → `max-w-[1120px]`, preservando `mx-auto px-4 md:px-8 py-8 md:py-16` VERBATIM. Son páginas prosa (`<section>` + `<Separator>`, sin `rounded-*`) → NO hay radius que swapear (invariante D3: interiores intactos). Cero copy nuevo, cero hex.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run sobre metodologia 2>$null; pnpm exec tsc --noEmit; exit $LASTEXITCODE</automated>
  </verify>
  <done>sobre y metodologia con `max-w-[1120px]`; sin cambios de radius ni copy; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 3: Extender tests de ruta con asserts de container/radius (BENTO-04)</name>
  <files>app/app/buscar/coverage.test.tsx, app/app/parlamentarios/page.test.tsx, app/app/agenda/page.test.tsx, app/components/search-result-card.test.tsx</files>
  <read_first>
    - app/app/buscar/coverage.test.tsx (usa renderToStaticMarkup)
    - app/app/parlamentarios/page.test.tsx (renderToStaticMarkup)
    - app/app/agenda/page.test.tsx (RTL render/screen)
    - app/components/search-result-card.test.tsx (RTL render, makeProps helper)
  </read_first>
  <action>
    Añadir asserts a NIVEL DE CLASE (jsdom no ve px), respetando la convención de cada archivo:
    - search-result-card.test.tsx: nuevo `it` que renderiza con makeProps y afirma que el HTML/`container.innerHTML` contiene `rounded-[var(--radius-tile)]` (la tarjeta de primer nivel lleva el radius bento) y NO afirma nada sobre card.tsx.
    - parlamentarios/page.test.tsx: assert de que el markup contiene `max-w-[1120px]` (no `max-w-5xl`) y que la fila de directorio lleva `rounded-[var(--radius-tile)]`.
    - buscar/coverage.test.tsx (o el test de ruta que renderice el `<main>`): assert `max-w-[1120px]` presente, `max-w-3xl` ausente en el `<main>`.
    - agenda/page.test.tsx: assert de que el `<li>` de resultado (o el container) lleva `rounded-[var(--radius-tile)]` / `max-w-[1120px]` según lo que ese test ya monte; si el test no monta el `<main>`, añadir assert de que la tarjeta `<li>` de resultado lleva el radius bento.
    Añadir en el archivo más natural (buscar o parlamentarios) un assert defensivo de firewall: el string `rounded-lg` sigue siendo emitido por el primitivo (interiores/skeleton intactos) — no exigir su ausencia global.
    Cero `#` hex en los tests.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run buscar parlamentario agenda search-result-card 2>$null; exit $LASTEXITCODE</automated>
  </verify>
  <done>Tests verdes; cada ruta con al menos un assert de `max-w-[1120px]` y/o `rounded-[var(--radius-tile)]` en su tarjeta de primer nivel; asserts a nivel de clase (no px).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno nuevo) | Fase 100% presentación: cero datos, cero RPC, cero red, cero input de usuario nuevo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering | card.tsx (firewall D3) | mitigate | verify de Task 1 falla si `rounded-[var` aparece en card.tsx o si git diff no está limpio; regresión a interiores queda bloqueada. |
| T-79-02 | Tampering | interiores byte-idénticos (skeletons/acordeones) | accept | swap acotado a call sites de primer nivel enumerados; sin edición de primitivos ni de internals. Bajo riesgo (cambio de className localizado). |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes (cero `shadcn add`, cero deps nuevas — UI-SPEC §Registry Safety); gate de legitimidad no aplica. |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` verde (base 870).
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- `git diff --stat -- app/components/ui/card.tsx` vacío (firewall D3).
- Ningún `#` hex añadido a los 11 archivos tocados.
</verification>

<success_criteria>
- /buscar, /parlamentarios, /agenda, /sobre, /metodologia con `<main>` a `max-w-[1120px]`.
- Tarjetas de primer nivel de /buscar (SearchResultCard + boxes), /parlamentarios (DirectoryRow + empty), /agenda (li + empty) con `rounded-[var(--radius-tile)]`.
- Interiores (skeletons, inputs, pills, acordeones) byte-idénticos.
- card.tsx sin editar. Tests de ruta extendidos y verdes.
</success_criteria>

<output>
Create `.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-01-SUMMARY.md` when done
</output>
