---
phase: 79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/parlamentario/[id]/page.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/app/contraparte/[id]/page.tsx
  - app/app/parlamentario/[id]/page.test.tsx
  - app/app/parlamentario/[id]/page-estructura.test.ts
autonomous: true
requirements: [BENTO-04]
must_haves:
  truths:
    - "Al abrir una ficha (/parlamentario/[id], /proyecto/[boletin], /contraparte/[id]) el ancho de contenido es 1120px, coherente con la home y las rutas de primer nivel"
    - "Los interiores de las fichas (acordeones, charts, tablas, DetalleColapsable, skeletons) quedan byte-idénticos"
    - "Al saltar a un ancla de sección en una ficha, el heading NO queda tapado por el header sticky de 80px (scroll-mt-6 local reconciliado)"
    - "El scrollspy del FichaRail sigue resaltando la sección correcta tras el ajuste de scroll-mt"
  artifacts:
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "container 1120px + scroll-mt local reconciliado (≥ header) en las <section> de carril"
      contains: "max-w-[1120px]"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "container 1120px + scroll-mt local reconciliado en las <section> de tramitación"
      contains: "max-w-[1120px]"
    - path: "app/app/contraparte/[id]/page.tsx"
      provides: "container 1120px (sin anclas — solo container)"
      contains: "max-w-[1120px]"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "globals.css scroll-margin-top: 5rem (L106, global desde Phase 76)"
      via: "quitar el scroll-mt-6 local para que aplique el offset global de 80px"
      pattern: "max-w-\\[1120px\\]"
---

<objective>
Propagar la coherencia bento ACOTADA a las 3 fichas (/parlamentario/[id], /proyecto/[boletin], /contraparte/[id]): (1) ensanchar el `<main>` a `max-w-[1120px]`, y (2) reconciliar el `scroll-mt-6` local de las `<section id=…>` con el offset global de 80px del header sticky (fix obligatorio UI-SPEC). Los interiores (acordeones, charts, tablas, DetalleColapsable, skeletons) quedan byte-idénticos — CERO swap de radio en fichas porque los únicos `rounded-lg` presentes son skeletons/aria-hidden (verificado), y las tarjetas visibles son interiores de `*Section`.

Purpose: Las fichas deben sentirse del mismo sitio que la home (BENTO-04), sin re-layoutear ningún panel interno (D3). El fix de scroll-mt resuelve el Pitfall #1 (header sticky ocluye anclas).
Output: 3 páginas de ficha con container swapeado + anclas reconciliadas; tests de ficha extendidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-UI-SPEC.md
@.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-CONTEXT.md

<interfaces>
<!-- Verificado por lectura directa 2026-07-15. El executor NO re-explora. -->

Contenedores actuales (verificado):
  app/app/parlamentario/[id]/page.tsx  L141 `<main className="max-w-5xl mx-auto px-4 md:px-8 py-8 md:py-16">`
  app/app/proyecto/[boletin]/page.tsx  L65  `<main className="max-w-5xl ...">`
  app/app/contraparte/[id]/page.tsx    L64  `<main className="max-w-3xl ...">`
→ En cada uno: `max-w-5xl`/`max-w-3xl` → `max-w-[1120px]`. Preservar VERBATIM `mx-auto px-4 md:px-8 py-8 md:py-16`.

Radius en fichas — NO HAY SWAP DE PRIMER NIVEL (verificado con grep exhaustivo):
  parlamentario/[id]: los carriles son `<section className="mt-12 scroll-mt-6">` PELADAS (sin rounded-*); los únicos rounded-lg son skeletons (L622-695). Las tarjetas visibles son interiores de `*Section`/DetalleColapsable → byte-idénticos. NADA que swapear de radio.
  proyecto/[boletin]: L613-614 (`mt-6 rounded-lg border p-6`) y L680-681 son `aria-hidden="true"` (fallbacks/skeleton, NO paneles de contenido de primer nivel) → NO se tocan; los `<section>` de tramitación no llevan rounded-* propio. NADA que swapear de radio.
  contraparte/[id]: único rounded-lg es skeleton L184 → queda. NADA que swapear.
  ⇒ Fichas = SOLO container + fix scroll-mt. Cero cambio de radio (correcto por construcción, respeta D3).

FIX scroll-mt (OBLIGATORIO — UI-SPEC §Sticky-Header Interaction, solo fichas):
  globals.css L106 tiene `scroll-margin-top: 5rem` GLOBAL (=80px, desde Phase 76). Las <section> de ficha tienen `scroll-mt-6` LOCAL (=24px) que GANA por especificidad Tailwind → el ancla queda 24px del top, tapada por el header de 80px.
  parlamentario/[id]: 8 `<section ... scroll-mt-6>` (L271,296,328,363,394,421,459) + 1 `<div id="cruces-detalle" ... scroll-mt-6>` (L369). Comentarios explicativos en L46,266,268 mencionan scroll-mt-6 (prosa, no clases — pueden quedar o actualizarse).
  proyecto/[boletin]: 9 `<section ... scroll-mt-6>` (L102,115,124,139,158,178,185,192,201).
  contraparte/[id]: SIN anclas scroll-mt-6 (grep confirma) → NO requiere fix.
  ACCIÓN preferida (UI-SPEC opción 1): QUITAR `scroll-mt-6` de todas las <section id=…>/div-ancla para que aplique el global `scroll-margin-top: 5rem`. Confirmar que el scrollspy del FichaRail sigue resaltando (no depende de scroll-mt; el rail es md:sticky md:top-6 — NO tocar md:top-6, es posición de columna, no offset de ancla). Alternativa (si una sección necesita más aire): subir a `scroll-mt-24` (96px). NUNCA dejar `scroll-mt-6` (24px, ocluido demostrable).

INTERIORES byte-idénticos (NO tocar): todo `*Section`, DetalleColapsable (rounded-lg detalle-colapsable.tsx L59), charts, timeline, votaciones, similares, tablas, rail chips, TODOS los skeletons.

Convención de tests de ficha (verificado): parlamentario/[id]/page.test.tsx (RTL) y page-estructura.test.ts existen. jsdom NO ve layout ni el offset real de scroll (no hay layout) → el assert de scroll-mt es a NIVEL DE CLASE (ausencia de `scroll-mt-6` en las <section>, o presencia de `scroll-mt-24`). El offset real se valida en Phase 81 (BrowserOS deploy real). Container assert también a nivel de clase (`max-w-[1120px]`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap container 1120px en las 3 fichas</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/app/proyecto/[boletin]/page.tsx, app/app/contraparte/[id]/page.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (L141 container)
    - app/app/proyecto/[boletin]/page.tsx (L65 container)
    - app/app/contraparte/[id]/page.tsx (L64 container)
  </read_first>
  <action>
    En el `<main>` de cada ficha cambiar `max-w-5xl`/`max-w-3xl` → `max-w-[1120px]`, preservando VERBATIM `mx-auto px-4 md:px-8 py-8 md:py-16` (BENTO-04, D3). NO swapear ningún radio: los únicos `rounded-lg` de estas fichas son skeletons/aria-hidden (verificado en `<interfaces>`) → byte-idénticos. NO tocar `*Section`, DetalleColapsable, charts, tablas, ni el `md:top-6` del FichaRail (es posición de columna, no offset de ancla). Cero `#` hex.
  </action>
  <verify>
    <automated>cd app; pnpm exec tsc --noEmit; exit $LASTEXITCODE</automated>
  </verify>
  <done>Las 3 fichas con `<main class="max-w-[1120px] mx-auto px-4 md:px-8 py-8 md:py-16">`; cero swap de radio; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciliar scroll-mt-6 local con el header sticky (fichas con anclas)</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/app/proyecto/[boletin]/page.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (8 <section scroll-mt-6> L271-459 + div-ancla L369)
    - app/app/proyecto/[boletin]/page.tsx (9 <section scroll-mt-6> L102-201)
  </read_first>
  <action>
    Aplicar el fix obligatorio (UI-SPEC §Sticky-Header Interaction, opción 1 preferida): QUITAR `scroll-mt-6` de TODAS las `<section id=…>` y del `<div id="cruces-detalle">` en parlamentario/[id] (L271,296,328,363,369,394,421,459) y de las 9 `<section id=…>` en proyecto/[boletin] (L102,115,124,139,158,178,185,192,201), dejando que aplique el global `scroll-margin-top: 5rem` (80px) de globals.css L106. Preservar el resto de clases de cada section (`mt-12`, `opacity-60` en L459, etc.) VERBATIM. Actualizar los comentarios de prosa que mencionan scroll-mt-6 (parlamentario L46,266,268) para reflejar que el offset ahora es global (no dejar comentarios que mientan). NO tocar `md:top-6` del FichaRail. contraparte NO tiene anclas → no se toca en este task. Cero hex.
    Si alguna sección necesitara más aire que 80px, la alternativa es `scroll-mt-24` (96px) — nunca dejar `scroll-mt-6`.
  </action>
  <verify>
    <automated>cd app; if (Select-String -Path "app/parlamentario/[id]/page.tsx","app/proyecto/[boletin]/page.tsx" -Pattern "scroll-mt-6" -SimpleMatch | Where-Object { $_.Line -notmatch "^\s*//|^\s*\*" }) { Write-Error "scroll-mt-6 (ocluido) sigue presente en una <section>"; exit 1 }; pnpm exec tsc --noEmit; exit $LASTEXITCODE</automated>
  </verify>
  <done>Ninguna `<section id=…>` de ficha lleva `scroll-mt-6` en className (removido → aplica global 80px, o subido a scroll-mt-24); comentarios de prosa reconciliados; md:top-6 del rail intacto; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 3: Extender tests de ficha con asserts de container + scroll-mt reconciliado</name>
  <files>app/app/parlamentario/[id]/page.test.tsx, app/app/parlamentario/[id]/page-estructura.test.ts</files>
  <read_first>
    - app/app/parlamentario/[id]/page.test.tsx (RTL; cómo mockea el fetch de ficha)
    - app/app/parlamentario/[id]/page-estructura.test.ts (asserts estructurales por string)
  </read_first>
  <action>
    Añadir asserts a NIVEL DE CLASE (jsdom no ve el offset real de scroll):
    - Container: afirmar que el markup de la ficha renderizada contiene `max-w-[1120px]` y NO `max-w-5xl`.
    - scroll-mt reconciliado: afirmar que las `<section id=…>` YA NO llevan `scroll-mt-6` (o, si se optó por subir, que llevan `scroll-mt-24`). page-estructura.test.ts (assert por string sobre el source/markup) es el lugar natural para el assert anti-`scroll-mt-6`.
    - Dejar constancia (comentario en el test) de que el offset REAL de ancla vs header se valida en Phase 81 (BrowserOS deploy real; jsdom no tiene layout).
    Cero `#` hex en los tests.
  </action>
  <verify>
    <automated>cd app; pnpm --filter ./app test -- --run "parlamentario/\[id\]" 2>$null; exit $LASTEXITCODE</automated>
  </verify>
  <done>Tests de ficha verdes con assert de `max-w-[1120px]` y assert de que `scroll-mt-6` fue reconciliado; comentario señalando el gate visual de Phase 81.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno nuevo) | Fase 100% presentación: cero datos, cero RPC, cero red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03 | Tampering | interiores de ficha (*Section/charts/tablas byte-idénticos) | accept | fichas = solo container + scroll-mt; cero swap de radio (verificado: únicos rounded-lg son skeletons); sin edición de internals. |
| T-79-04 | Information Disclosure | anclas ocluidas por header (usabilidad, no seguridad) | mitigate | fix obligatorio remueve scroll-mt-6; verify de Task 2 falla si persiste; validación visual real en Phase 81. |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes; gate de legitimidad no aplica. |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run` verde.
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- Ninguna `<section id=…>` de ficha con `scroll-mt-6` en className.
- Las 3 fichas con `max-w-[1120px]`; cero swap de radio; cero hex nuevo.
</verification>

<success_criteria>
- /parlamentario/[id], /proyecto/[boletin], /contraparte/[id] con `<main>` a `max-w-[1120px]`.
- scroll-mt-6 local de las fichas reconciliado con el offset global de 80px (removido o ≥scroll-mt-24).
- Interiores byte-idénticos (acordeones/charts/tablas/skeletons intactos).
- Tests de ficha extendidos y verdes.
</success_criteria>

<output>
Create `.planning/phases/79-bento-coherencia-propagaci-n-acotada-a-rutas-interiores/79-02-SUMMARY.md` when done
</output>
