---
phase: 114-link-int-links-internos-exhaustivos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/links-internos-manifiesto.mjs
  - scripts/verificar-links-internos.mjs
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json
autonomous: true
requirements: [LINK-02]

must_haves:
  truths:
    - "Existe un manifiesto declarativo que enumera cada link interno del inventario 113 instanciado con los sujetos de §1"
    - "Un operador puede correr un solo comando y obtener el estado HTTP de cada link interno contra el deploy real"
    - "La corrida deja una salida guardada (txt legible + json máquina) que otra persona puede re-generar"
    - "Los links bajo gate OFF se verifican como AUSENCIA en el DOM del emisor, nunca como destino"
  artifacts:
    - path: "scripts/links-internos-manifiesto.mjs"
      provides: "Universo declarativo de links internos derivado del inventario 113"
      exports: ["MANIFIESTO", "SUJETOS", "BASE_URL"]
    - path: "scripts/verificar-links-internos.mjs"
      provides: "Runner reproducible: status + ausencia, salida txt/json"
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json"
      provides: "Estado ANTES de cualquier fix"
  key_links:
    - from: "scripts/verificar-links-internos.mjs"
      to: "scripts/links-internos-manifiesto.mjs"
      via: "import estático del manifiesto"
      pattern: "from \"\\./links-internos-manifiesto\\.mjs\""
    - from: "scripts/verificar-links-internos.mjs"
      to: "https://observatorio-congreso.thevalis.workers.dev"
      via: "fetch secuencial con delay"
      pattern: "BASE_URL"
---

<objective>
Construir la infraestructura reproducible de verificación de links internos (manifiesto + runner) y
ejecutar la corrida PRE-FIX completa contra el deploy real, dejando la salida guardada como artefacto.

Purpose: LINK-02 exige verificación exhaustiva sobre el deploy real con corrida reproducible. El
universo NO se descubre por crawl — se deriva del inventario rector 113 (`estado: validado`).
Output: `scripts/links-internos-manifiesto.mjs`, `scripts/verificar-links-internos.mjs`,
`114-CORRIDA-PRE.txt`, `114-CORRIDA-PRE.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-VALIDATION.md
@scripts/validar-deeplinks.mjs

<interfaces>
<!-- Precedente de estilo: scripts/validar-deeplinks.mjs (CERO dependencias externas, parseArgs de
     node:util, UA identificatorio, sleep entre requests, tabla legible + exit code). -->

UA identificatorio ya usado en el repo (scripts/validar-deeplinks.mjs:30-31):
  "ObservatorioCongreso360/1.0 (+https://observatorio-congreso.thevalis.workers.dev; contacto: sanchez.rossi@gmail.com)"

Hallazgo de resolución de dependencias (verificado 2026-07-28):
  cheerio 1.2.0 está declarado SOLO en packages/agenda, packages/bio y packages/lobby.
  NO está en el package.json raíz ⇒ bajo pnpm (node_modules estricto) un script en scripts/
  probablemente NO puede `import("cheerio")`. El Task 2 resuelve esto con una probe explícita.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifiesto declarativo del universo de links internos</name>
  <files>scripts/links-internos-manifiesto.mjs</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` — leer §1
      (sujetos deterministas, líneas ~291-457), §2 (chrome C-01..C-04, ~458-523), §5 (gates y su
      estado, ~1914-1953) y CADA bloque `#### Tabla A — links internos` de §4 (las 15 rutas,
      ~985-1912) más los apéndices `4.1.b`, `4.2.b`, `4.3.b`, `4.9.b` (not-found).
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md` — sección
      `## Implementation Decisions` (alcance y exclusiones).
    - `scripts/validar-deeplinks.mjs` — estilo de script del repo.
  </read_first>
  <action>
    Crear `scripts/links-internos-manifiesto.mjs` como módulo ESM de DATOS puros (sin red, sin I/O),
    exportando `BASE_URL`, `SUJETOS` y `MANIFIESTO`.

    `BASE_URL` = "https://observatorio-congreso.thevalis.workers.dev" (deploy auditado por 113).

    `SUJETOS` instancia los sujetos deterministas de §1 del inventario, con los ids EXACTOS en
    formato PK string: diputado `D1165`, senador `S1338`, boletín bicameral `14309-04`, boletín
    solo-Senado `17870-05`. Añadir además ids inválidos deterministas para ejercitar las cuatro
    páginas `not-found`: `parlamentarioInexistente` = "D0000000", `boletinInexistente` = "00000-00",
    `seedInexistente` = "D0000000", `contraparteInexistente` = "c:sujeto-inexistente" (este último
    es el placeholder no-RUT que ya usa §5 del inventario; JAMÁS usar un valor con forma de RUT).

    `MANIFIESTO` = array de entradas, cada una con estas claves obligatorias:
      `id` — identificador estable "SECCION-FILA", p. ej. "4.1-A1", "C-02-4", "4.2.b-A1".
      `origen` — ruta (con sujeto instanciado) que EMITE el link, o "chrome" para C-01..C-04.
      `href` — URL interna instanciada, relativa (empieza con "/"), o null cuando el tipo es
               `ausencia` y lo que se busca es un patrón.
      `tipo` — uno de: "status" | "ancla" | "ausencia".
      `espera` — para "status": "no-404" (default) o 404 cuando el 404 es el comportamiento
                 declarado del inventario; para "ancla": el id sin "#"; para "ausencia": el patrón
                 literal que NO debe aparecer en el HTML del `origen`.
      `destino` — ruta que se solicita para verificar (para "ancla", la página donde debe existir el id).
      `gate` — lista cerrada del inventario §5.1: "—" | "NET" | "CRUCES" | "VSIM" | "MONEY" | "NOTIF".
      `nota` — cita corta del inventario que justifica la fila.

    Poblar el MANIFIESTO cubriendo, sin omitir ninguna:
    (a) Chrome C-01..C-04: `/metodologia`, `/sobre`, `/` (wordmark). El `mailto:` de C-01 y el link
        CC BY de C-01 NO son internos → excluidos con una fila-comentario en el módulo explicando
        por qué (van a la Phase 115).
    (b) Rutas estáticas del universo, cada una como entrada `status`: `/`, `/buscar`,
        `/parlamentarios`, `/agenda`, `/red`, `/sobre`, `/metodologia`, `/comparar`, `/cuenta`,
        `/notificaciones/baja`, `/notificaciones/confirmar?token=x`.
    (c) Rutas dinámicas instanciadas: `/parlamentario/D1165`, `/parlamentario/S1338`,
        `/proyecto/14309-04`, `/proyecto/17870-05`.
    (d) Variantes con query de las Tablas A (una por familia, con el sujeto real):
        `/comparar?a=D1165` (4.1-A1), `/red?seed=D1165` (4.1-A2, gate NET ON),
        `/parlamentario/D1165?vista=cronologica` (4.1-A12), paginaciones de votos/lobby/patrimonio
        de 4.1 (A10/A14/A17) usando el nombre de parámetro REAL que emite el código —
        derivarlo leyendo el emisor citado en la fila del inventario, no inventarlo —,
        `/agenda?semana=2026-W30`, `/agenda?q=trabajo&camara=diputados` (4.5-A2),
        `/buscar?q=pension&page=1` y `?page=2` (4.6-A2/A3),
        `/proyecto/14309-04?urgencias=<id>` solo si el inventario da un id derivable; si no lo da,
        registrar la entrada con `nota: "no instanciable sin dato de PROD"` y `tipo: "status"`
        contra `/proyecto/14309-04` a secas, declarando la limitación.
    (e) Rutas de not-found (4.1.b, 4.2.b, 4.3.b, 4.9.b): entrada `status` con `espera: 404` para la
        propia URL inválida, MÁS una entrada `status` para el único href que emiten (`/`).
    (f) Entradas `ancla` para CADA `#id` de las Tablas A que hoy se emite:
        de `/parlamentario/D1165` → votos, lobby, patrimonio, cruces;
        de `/proyecto/14309-04` → estado, timeline, votaciones, autores, lobby-tramitacion,
        lobby-menciones, cruces, idea-matriz, cuerpos-legales, similares, validacion-fuente.
        Para `/parlamentario/S1338` registrar las anclas que el inventario declara REDUCIDAS
        (sin `#cruces`, sin detalle de lobby) con `nota` citando la "Diferencia por sujeto".
    (g) Entradas `ausencia` para los gates OFF (§5): en `/parlamentario/D1165` y
        `/proyecto/14309-04` el patrón `href="/contraparte/` (MONEY) y el patrón `/cuenta?next=`
        (NOTIF); las anclas MONEY `#dinero`, `#financiamiento`, `#contratos`, `#aportes` como
        `ausencia` en sus emisores. Añadir la presencia declarada por §5 de
        `id="financiamiento-pendiente"` como entrada `ancla` en `/parlamentario/D1165`.
    (h) Exclusiones EXPLÍCITAS, como constante exportada `EXCLUIDOS` con `{ id, razon }`:
        emisores huérfanos E-003 (voto-ficha-row) y E-008 (actualidad-module) — no se montan en
        ninguna ruta (evidencia 113); `/admin/revisar-entidades` — EXCLUIDA por decisión LOCKED del
        CONTEXT de 113; `/contraparte/[id]` como destino de links entrantes — el inventario prueba
        cero inbound, así que su 404 no es defecto y se registra en (e), no como link roto.

    El módulo NO hace red ni escribe archivos. Cada entrada lleva su `nota` con la referencia al
    inventario; el que lea el manifiesto debe poder auditar el universo sin abrir el código de la app.
  </action>
  <verify>
    <automated>node -e "import('./scripts/links-internos-manifiesto.mjs').then(m=>{const s=new Set(m.MANIFIESTO.map(e=>e.id));if(s.size!==m.MANIFIESTO.length)throw new Error('ids duplicados');const t=new Set(m.MANIFIESTO.map(e=>e.tipo));for(const x of t)if(!['status','ancla','ausencia'].includes(x))throw new Error('tipo invalido '+x);const g=new Set(m.MANIFIESTO.map(e=>e.gate));for(const x of g)if(!['—','NET','CRUCES','VSIM','MONEY','NOTIF'].includes(x))throw new Error('gate invalido '+x);const c=(k)=>m.MANIFIESTO.filter(e=>e.tipo===k).length;if(c('status')<25)throw new Error('pocas entradas status: '+c('status'));if(c('ancla')<15)throw new Error('pocas anclas: '+c('ancla'));if(c('ausencia')<4)throw new Error('pocas ausencias: '+c('ausencia'));if(!Array.isArray(m.EXCLUIDOS)||m.EXCLUIDOS.length<3)throw new Error('EXCLUIDOS incompleto');console.log('OK',m.MANIFIESTO.length,'entradas | status',c('status'),'ancla',c('ancla'),'ausencia',c('ausencia'))})"</automated>
  </verify>
  <acceptance_criteria>
    - El comando de `<verify>` sale exit 0 e imprime `OK N entradas | status ≥25 ancla ≥15 ausencia ≥4`.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' scripts/links-internos-manifiesto.mjs` → **0** (cero RUT; el
      boletín `14309-04` no matchea el patrón porque tiene 5 dígitos, verificar que el grep dé 0).
    - Los 4 sujetos de §1 aparecen literalmente: `grep -c 'D1165' … `, `S1338`, `14309-04`,
      `17870-05` → cada uno ≥ 1.
    - `EXCLUIDOS` contiene al menos las entradas E-003, E-008 y `/admin/revisar-entidades`.
    - El módulo no importa `node:fs` ni `node:http*` (`grep -c "node:fs\|fetch(" ` → 0).
  </acceptance_criteria>
  <done>El universo de links internos está declarado en código, auditable contra el inventario 113, con exclusiones justificadas y cero PII.</done>
</task>

<task type="auto">
  <name>Task 2: Runner reproducible (status + ausencia) con salida guardada</name>
  <files>scripts/verificar-links-internos.mjs</files>
  <read_first>
    - `scripts/validar-deeplinks.mjs` — patrón del repo: `parseArgs`, UA, sleep, tabla, exit code.
    - `scripts/links-internos-manifiesto.mjs` — el módulo creado en el Task 1.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-VALIDATION.md` — §Test
      Infrastructure (comandos de corrida rápida y completa).
  </read_first>
  <action>
    Crear `scripts/verificar-links-internos.mjs`, ESM, CERO dependencias externas (usar el `fetch`
    global de Node ≥22 y `node:util`/`node:fs`).

    Antes de escribir el parseo de HTML, ejecutar la probe de resolución:
    `node -e "import('cheerio').then(()=>console.log('CHEERIO_OK')).catch(e=>console.log('CHEERIO_NO'))"`
    desde la raíz del repo. Si imprime `CHEERIO_OK`, usar cheerio para las aserciones de ancla y de
    ausencia (decisión del CONTEXT). Si imprime `CHEERIO_NO` (esperado: cheerio solo está declarado
    en packages/agenda|bio|lobby, no en la raíz), implementar la verificación por búsqueda literal
    sobre el HTML servido y DEJAR REGISTRADA la desviación en un comentario de cabecera del script
    citando la probe y su salida. No añadir cheerio al package.json raíz (paquete nuevo = fuera de
    alcance de esta fase).

    Comportamiento del runner:
    - Recorre `MANIFIESTO` de forma SECUENCIAL (nunca en paralelo), con `await sleep(400)` entre
      requests (rango 300-500ms del CONTEXT; el Worker es propio pero jamás ráfagas). Enviar el UA
      identificatorio del repo en cada request y `redirect: "manual"` para no enmascarar un 404
      detrás de una redirección.
    - Cachea el HTML por URL de destino en un `Map` dentro de la corrida, para que N anclas sobre la
      misma página cuesten UN solo request (y descontar ese hit del delay).
    - Aserción por tipo:
      · `status` → PASS si `espera === "no-404"` y el status NO es 404; PASS si `espera === 404` y el
        status ES 404; en cualquier otro caso FAIL con el status observado como causa.
      · `ancla` → solicita `destino`, y PASS si el HTML contiene el atributo id del ancla como
        atributo real (buscar `id="<ancla>"` y también la forma con comillas simples); si el status
        del destino no es 200, FAIL con causa `destino HTTP <n>`. Si el status es 200 pero el id no
        aparece, marcar el resultado como `MISSING-SSR` (NO como PASS ni como FAIL definitivo): es
        candidato al fallback BrowserOS del Plan 02.
      · `ausencia` → solicita `origen`, y PASS si el patrón NO aparece en el HTML; FAIL si aparece
        (el gate estaría filtrando superficie).
    - Flags: `--route <ruta>` filtra el manifiesto por `origen` o `destino` (corrida rápida de una
      ruta tras un fix); `--out <basename>` define el nombre base de los artefactos; `--json-only`
      suprime la tabla.
    - Salida: imprime una tabla legible por stdout (id, tipo, gate, href/destino, resultado, causa) y
      un resumen final con conteos por resultado (PASS / FAIL / MISSING-SSR). Escribe además dos
      archivos junto al `--out`: `<basename>.txt` (copia byte-fiel de lo impreso) y `<basename>.json`
      (array de resultados con `id, tipo, gate, origen, destino, href, espera, status, resultado,
      causa` + un objeto `meta` con `base_url`, `iso_timestamp`, `total`, conteos y la salida de la
      probe de cheerio).
    - Exit code: 0 si no hay FAIL (los `MISSING-SSR` no fallan la corrida — se resuelven en el Plan
      02); 1 si hay al menos un FAIL. Esto hace del script un gate, no un reporte pasivo.
    - Cabecera del script con el comando exacto de la corrida completa y el de la corrida por ruta.
  </action>
  <verify>
    <automated>node scripts/verificar-links-internos.mjs --route /metodologia --out "$TMPDIR/114-smoke" ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verificar-links-internos.mjs --route /metodologia` corre en < 30 s, imprime una
      tabla con al menos 1 fila y sale con exit 0 o 1 (nunca crash / stack trace).
    - Con `--out <base>` se crean exactamente `<base>.txt` y `<base>.json`; el `.json` parsea y su
      `meta.total` iguala el largo del array de resultados.
    - El script no importa ningún paquete fuera de `node:`* salvo cheerio bajo la probe:
      `grep -nE "^import .* from \"(?!node:|\./)" scripts/verificar-links-internos.mjs` sin match
      distinto de `./links-internos-manifiesto.mjs`.
    - El delay está presente y es secuencial: `grep -c "await sleep(" scripts/verificar-links-internos.mjs` ≥ 1
      y `grep -c "Promise.all" scripts/verificar-links-internos.mjs` → **0**.
    - El UA identificatorio aparece en el script (`grep -c "ObservatorioCongreso360" ` ≥ 1).
    - La cabecera cita la probe de cheerio y su salida observada.
  </acceptance_criteria>
  <done>Existe un comando único, reproducible y sin dependencias nuevas que verifica todo el universo del manifiesto y deja artefactos txt+json.</done>
</task>

<task type="auto">
  <name>Task 3: Corrida PRE-FIX completa contra el deploy real, guardada como artefacto</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt, .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json</files>
  <read_first>
    - `scripts/verificar-links-internos.mjs` — el runner del Task 2.
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §5 — los estados
      de gate OBSERVADOS (NET ON, CRUCES ON, VSIM ON, MONEY OFF, NOTIF OFF), que son la expectativa
      contra la que se leen los resultados.
  </read_first>
  <action>
    Ejecutar la corrida completa contra el deploy real y guardar los artefactos:
    `node scripts/verificar-links-internos.mjs --out .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE`

    Esta es la evidencia del estado ANTES de cualquier fix. NO corregir nada en esta tarea: si hay
    FAIL o MISSING-SSR, se registran y viajan al Plan 02 (triage de anclas) y al Plan 03 (fixes).

    Anexar al final de `114-CORRIDA-PRE.txt` un bloque `=== LECTURA DE LA CORRIDA ===` con:
    - el comando verbatim ejecutado y el timestamp de la corrida;
    - el exit code observado;
    - los conteos PASS / FAIL / MISSING-SSR;
    - por cada FAIL, una línea `id | href | status | causa | hipótesis` — hipótesis en una frase,
      SIN corregir nada todavía;
    - por cada MISSING-SSR, una línea `id | ancla | destino` marcada como
      `candidato a fallback BrowserOS (Plan 02)`;
    - la confirmación de que los estados de gate observados en esta corrida coinciden con §5 del
      inventario; si alguno divergiera (p. ej. un `href="/contraparte/` presente con MONEY OFF), se
      declara como HALLAZGO DE RÉGIMEN en mayúsculas — nunca se toca ningún flag para "arreglarlo".

    Si la corrida no puede completarse contra el deploy (red caída, Worker 5xx sostenido), NO
    fabricar resultados: guardar la corrida parcial con su causa declarada y detener el plan
    reportando el bloqueo.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json','utf8');const j=JSON.parse(r);if(!j.meta||!Array.isArray(j.resultados))throw new Error('forma json invalida');if(j.resultados.length<40)throw new Error('corrida incompleta: '+j.resultados.length);if(!require('fs').readFileSync('.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt','utf8').includes('LECTURA DE LA CORRIDA'))throw new Error('falta bloque de lectura');console.log('OK',j.resultados.length,'resultados')"</automated>
  </verify>
  <acceptance_criteria>
    - Existen `114-CORRIDA-PRE.txt` y `114-CORRIDA-PRE.json` en el dir de la fase.
    - El `.json` trae `meta.base_url === "https://observatorio-congreso.thevalis.workers.dev"`,
      `meta.iso_timestamp` y ≥ 40 resultados.
    - El `.txt` contiene el bloque `=== LECTURA DE LA CORRIDA ===` con comando verbatim, exit code y
      los tres conteos.
    - Cero fabricación: todo FAIL/MISSING-SSR aparece listado; ningún resultado sin status observado.
    - `git status --porcelain app/ packages/` → **vacío** (este plan NO toca código de producto).
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 114-CORRIDA-PRE.txt 114-CORRIDA-PRE.json` → **0** (cero RUT).
  </acceptance_criteria>
  <done>El estado PRE-FIX de todos los links internos está registrado con evidencia re-generable por comando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → deploy público | El script solo LEE HTML público del propio Worker; no envía credenciales ni cookies |
| resultados → artefacto commiteado | El HTML servido podría contener datos que no deben quedar en el repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Information disclosure | artefactos `114-CORRIDA-PRE.*` | mitigate | El JSON guarda status y causa, NUNCA el cuerpo HTML; gate de acceptance `grep RUT → 0` en ambos artefactos |
| T-114-02 | Denial of service | Worker propio | mitigate | Corrida secuencial con `sleep(400)` + caché de HTML por URL; `Promise.all` prohibido (gate en acceptance) |
| T-114-03 | Tampering | estado de gates | accept | El script solo lee; el plan prohíbe explícitamente tocar flags y declara cualquier divergencia como hallazgo |
| T-114-SC | Tampering | npm installs | mitigate | CERO paquete nuevo: el runner solo usa `node:`* y el manifiesto local; cheerio se usa únicamente si YA resuelve, jamás se instala |
</threat_model>

<verification>
- `node scripts/verificar-links-internos.mjs --out <tmp>` completa sin crash.
- Artefactos `114-CORRIDA-PRE.txt` / `.json` presentes y bien formados.
- `git status --porcelain app/ packages/` vacío (fase sin fixes todavía).
- Cero RUT y cero credenciales en los artefactos.
</verification>

<success_criteria>
- El universo de links internos del inventario 113 está declarado en un manifiesto auditable.
- Un solo comando reproduce la verificación completa y deja salida guardada (SC#4 del ROADMAP).
- El estado PRE-FIX de cada link interno (status) y de cada patrón gated (ausencia) queda registrado.
- Los candidatos a verificación de ancla no-SSR quedan identificados para el Plan 02.
</success_criteria>

<output>
Create `.planning/phases/114-link-int-links-internos-exhaustivos/114-01-SUMMARY.md` when done
</output>
