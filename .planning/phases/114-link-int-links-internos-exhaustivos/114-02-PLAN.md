---
phase: 114-link-int-links-internos-exhaustivos
plan: 02
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - scripts/verificar-links-internos.mjs
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md
autonomous: true
requirements: [LINK-02]

must_haves:
  truths:
    - "Cada ancla #id del inventario tiene un veredicto: existe en el DOM del destino, o no existe con causa"
    - "Las anclas ausentes del HTML SSR fueron verificadas contra el DOM real por BrowserOS antes de declararlas rotas"
    - "Existe una lista cerrada de hallazgos accionables, cada uno con su emisor y su intención (arreglar href vs arreglar destino)"
  artifacts:
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md"
      provides: "Veredicto por ancla con su método de verificación (SSR o BrowserOS)"
      contains: "método"
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md"
      provides: "Lista cerrada de defectos a corregir en el Plan 03"
      contains: "H-"
  key_links:
    - from: ".planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md"
      to: "114-CORRIDA-PRE.json"
      via: "cada hallazgo cita el id de manifiesto de su resultado"
      pattern: "114-CORRIDA-PRE"
---

<objective>
Cerrar el veredicto de cada ancla `#id` (SSR primero, BrowserOS solo para las ausentes del HTML
servido) y consolidar TODOS los defectos de links internos en una lista cerrada y accionable.

Purpose: SC#2 del ROADMAP exige que el ancla exista en el DOM de la página destino, no solo que la
página cargue — precedente v8.0 donde `section[id]`/scroll-margin solo se cazó inspeccionando el
deploy real. Un `MISSING-SSR` no es todavía un defecto: puede ser DOM cliente.
Output: `114-ANCLAS.md`, `114-HALLAZGOS.md`, refuerzo del runner si la verificación de ancla resultó
laxa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-01-SUMMARY.md
@scripts/bros-cli.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Veredicto SSR de cada ancla y endurecimiento de la aserción</name>
  <files>scripts/verificar-links-internos.mjs, .planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md</files>
  <read_first>
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json` — los
      resultados de tipo `ancla` y sus `MISSING-SSR`.
    - `scripts/verificar-links-internos.mjs` — la implementación actual de la aserción de ancla.
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` — las filas de
      Tabla A con `ancla #id? = sí` de §4.1 y §4.2, incluidas las notas "Diferencia por sujeto".
    - `app/components/ficha-rail.tsx` — el emisor E-042 que construye los chips `#{carril}`.
  </read_first>
  <action>
    Auditar la aserción de ancla del runner y endurecerla si es laxa: una coincidencia de la cadena
    `id="votos"` dentro de un atributo distinto (p. ej. `aria-controls`, `data-*`, o dentro de un
    string de JSON embebido en un `<script>`) NO prueba que exista el elemento con ese id. Ajustar la
    verificación para que exija el atributo `id` sobre un elemento —forma `<tag ... id="X"`— y para
    que ignore el contenido de bloques `<script>`. Si el runner ya lo hacía correctamente, dejarlo
    intacto y declararlo verificado en el SUMMARY (no cambiar por cambiar).

    Re-correr solo las entradas de tipo `ancla` y escribir `114-ANCLAS.md` con:
    - tabla `| id manifiesto | ancla | ruta destino | emisor del href | método | veredicto | evidencia |`
      donde `método` ∈ {`SSR`, `BrowserOS`} y `veredicto` ∈ {`existe`, `ausente`, `ausente-declarado`};
    - `ausente-declarado` es para las anclas que el inventario ya predice ausentes por sujeto o por
      gate (p. ej. `#cruces` en `/parlamentario/S1338` porque `conteos.cruces.tipo !== "dato"`, o las
      anclas MONEY) — esas NO son defectos y se citan con la línea del inventario que lo declara;
    - `evidencia` = el fragmento mínimo del HTML que contiene el atributo id, recortado a ≤ 120
      caracteres y SIN datos personales (jamás nombres, jamás RUT — si el fragmento los arrastra,
      recortar hasta el atributo y anotar `[recortado]`);
    - una sección `## Anclas pendientes de BrowserOS` que liste las que quedaron `ausente` por SSR y
      alimentan el Task 2.

    Si el conteo de anclas verificadas por SSR no cuadra con el conteo de entradas `ancla` del
    manifiesto, detenerse y declarar la discrepancia — jamás rellenar filas.
  </action>
  <verify>
    <automated>node scripts/verificar-links-internos.mjs --out "$TMPDIR/114-anclas" --json-only && node -e "const j=JSON.parse(require('fs').readFileSync(process.env.TMPDIR+'/114-anclas.json','utf8'));const a=j.resultados.filter(r=>r.tipo==='ancla');const doc=require('fs').readFileSync('.planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md','utf8');const filas=(doc.match(/^\|\s[^|]*\|/gm)||[]).length;if(a.length===0)throw new Error('cero anclas evaluadas');if(filas<a.length)throw new Error('114-ANCLAS.md tiene '+filas+' filas para '+a.length+' anclas');console.log('OK anclas',a.length,'filas',filas)"</automated>
  </verify>
  <acceptance_criteria>
    - `114-ANCLAS.md` tiene una fila por cada entrada `ancla` del manifiesto (conteo exacto, sin
      omisiones), cada una con `método` y `veredicto` de la lista cerrada.
    - Todo `ausente-declarado` cita la línea del inventario 113 que lo predice.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 114-ANCLAS.md` → **0**.
    - Si se endureció la aserción del runner: `node scripts/verificar-links-internos.mjs --route /metodologia`
      sigue saliendo sin crash, y el SUMMARY documenta qué cambió y por qué.
    - La sección `## Anclas pendientes de BrowserOS` existe (aunque quede vacía, con la frase
      "ninguna — todas las anclas resolvieron por SSR").
  </acceptance_criteria>
  <done>Cada ancla del universo tiene veredicto SSR y las dudosas están aisladas para el fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Fallback BrowserOS para anclas ausentes del HTML SSR</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md</files>
  <read_first>
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md` — sección
      `## Anclas pendientes de BrowserOS` (producida en el Task 1).
    - `scripts/bros-cli.mjs` — el wrapper de BrowserOS y sus argumentos reales.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md` — el gotcha LOCKED:
      screenshots en ráfaga tumban el MCP (sleep 8-10 s entre capturas) y un timeout de CDP obliga a
      reabrir la página.
  </read_first>
  <action>
    Si la sección `## Anclas pendientes de BrowserOS` quedó vacía, NO abrir BrowserOS: registrar en
    `114-ANCLAS.md` la frase `fallback BrowserOS no requerido — 0 anclas ausentes en SSR` y terminar
    la tarea. Esta rama es un resultado válido, no una omisión.

    Si hay anclas pendientes: para cada una, abrir la ruta destino en BrowserOS contra el deploy real
    (`https://observatorio-congreso.thevalis.workers.dev`) y evaluar en el DOM vivo si existe el
    elemento con ese id — preferir una evaluación de expresión que devuelva el `tagName` y las clases
    del elemento (o `null`) sobre una captura de pantalla, porque la evidencia textual es citable y
    no arrastra PII. Respetar el gotcha: esperar 8-10 s entre operaciones pesadas y reabrir la página
    ante un timeout de CDP en vez de reintentar en caliente.

    Actualizar la fila correspondiente de `114-ANCLAS.md` con `método = BrowserOS`, el veredicto
    resultante y la evidencia textual (el `tagName`/`id` observado, o `null`). Un ancla que existe en
    el DOM vivo pero no en el SSR NO es un defecto de link: es DOM cliente, y así se anota.

    Anotar además, por cada ancla que SÍ existe, si el destino tiene `scroll-margin` aplicado — es la
    lección v8.0 (`section[id]` no cubierto por la regla de scroll-margin, cazado solo en el deploy
    real). Un ancla que existe pero queda oculta bajo el header al saltar es un hallazgo de Plan 03,
    no un ancla rota; registrarlo como tal, con su clase o valor computado observado.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('.planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md','utf8');const pend=d.split('## Anclas pendientes de BrowserOS')[1]||'';const vacia=/ninguna|no requerido|0 anclas/i.test(pend);const usaBros=/BrowserOS/.test(d.split('## Anclas pendientes de BrowserOS')[0]||'');if(!vacia&&!usaBros)throw new Error('hay anclas pendientes pero ninguna fila con metodo BrowserOS');console.log(vacia?'OK sin pendientes':'OK con evidencia BrowserOS')"</automated>
  </verify>
  <acceptance_criteria>
    - O bien `114-ANCLAS.md` declara literalmente `fallback BrowserOS no requerido — 0 anclas ausentes en SSR`,
      o bien cada ancla pendiente tiene su fila con `método = BrowserOS` y evidencia textual observada.
    - Cero screenshots commiteados; la evidencia es textual (T-114-04).
    - Ningún ancla queda con veredicto vacío o `?`.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 114-ANCLAS.md` → **0**.
  </acceptance_criteria>
  <done>SC#2 del ROADMAP está resuelto: cada ancla tiene veredicto contra el DOM real, no solo contra el HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidar la lista cerrada de hallazgos accionables</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md</files>
  <read_first>
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt` — bloque
      `=== LECTURA DE LA CORRIDA ===`, todos los FAIL.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-ANCLAS.md` — veredictos.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md` — el criterio LOCKED
      de intención: "preferir corregir el emisor (href) salvo que el inventario muestre que el
      destino debía tener el id".
  </read_first>
  <action>
    Escribir `114-HALLAZGOS.md` como lista CERRADA (numerada `H-01`, `H-02`, …) que fusiona los FAIL
    de status, las anclas rotas y los `ausencia` violados. Por cada hallazgo:
    `id` · `síntoma observado` (con el id de manifiesto y el status/veredicto) · `emisor`
    (archivo:línea del inventario) · `destino` · `intención` (una de: `corregir el href del emisor` |
    `añadir el id al destino` | `no es defecto — declarado`) · `justificación` citando el inventario
    o la evidencia · `fix propuesto` en una frase, sin código.

    Reglas de clasificación LOCKED:
    - Un 404 de una ruta gated-OFF que el inventario ya declara 404 (p. ej. `/contraparte/[id]` con
      MONEY OFF) NO es hallazgo: va a una sección aparte `## No son defectos (declarados por 113)`.
    - Un patrón `ausencia` que SÍ aparece con el gate OFF es un HALLAZGO DE RÉGIMEN y se marca como
      severidad alta — jamás se "arregla" tocando un flag.
    - Un ancla que existe en DOM cliente pero no en SSR se declara en `## No son defectos` con su
      causa.
    - Un ancla que existe pero queda tapada por el header (scroll-margin, lección v8.0) es hallazgo
      con intención `corregir el href del emisor` sólo si el emisor apunta al id equivocado; si el id
      es correcto y el problema es de estilo, la intención es `añadir el id al destino` reformulada
      como corrección de estilo en el destino — indicarlo explícito.

    Cerrar el documento con `## Alcance del Plan 03` enumerando los ids `H-NN` que el Plan 03 debe
    corregir y los archivos de `app/` que tocará, y con la nota LOCKED: **el deploy de estos fixes
    viaja con la Phase 125; esta fase JAMÁS deploya y JAMÁS toca flags**.

    Si no hubo ningún hallazgo, escribir igual el documento con `0 hallazgos` y la evidencia de la
    corrida que lo respalda — un cero honesto es un resultado, no un archivo vacío.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('.planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md','utf8');if(!/## Alcance del Plan 03/.test(d))throw new Error('falta seccion Alcance del Plan 03');if(!/## No son defectos/.test(d))throw new Error('falta seccion No son defectos');if(!/Phase 125/.test(d))throw new Error('falta la nota de deploy diferido');const n=(d.match(/^\|?\s*\*?\*?H-\d{2}/gm)||[]).length;if(n===0&&!/0 hallazgos/.test(d))throw new Error('sin H-NN y sin declaracion de cero hallazgos');console.log('OK hallazgos',n)"</automated>
  </verify>
  <acceptance_criteria>
    - `114-HALLAZGOS.md` existe con las secciones `## No son defectos (declarados por 113)` y
      `## Alcance del Plan 03`, y la nota literal de deploy diferido a la Phase 125.
    - Cada FAIL de `114-CORRIDA-PRE.json` está o bien como `H-NN` o bien en `## No son defectos` con
      su causa — cero FAIL sin disposición (verificable comparando conteos contra el `.json`).
    - Cada `H-NN` declara una `intención` de la lista cerrada de tres valores.
    - `grep -cE '[0-9]{7,8}-[0-9kK]' 114-HALLAZGOS.md` → **0**.
    - `git status --porcelain app/ packages/` → **vacío** (este plan tampoco toca código de producto).
  </acceptance_criteria>
  <done>El Plan 03 recibe una lista cerrada, justificada y acotada de qué corregir y dónde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrowserOS → deploy público | Navegador local automatizado leyendo HTML público; sin sesión ni credenciales |
| DOM observado → artefacto commiteado | El DOM contiene nombres de parlamentarios y datos de fichas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-04 | Information disclosure | `114-ANCLAS.md` evidencia de DOM | mitigate | Evidencia recortada a ≤120 chars centrada en el atributo `id`; cero screenshots commiteados; gate `grep RUT → 0` |
| T-114-05 | Denial of service | MCP de BrowserOS | mitigate | Gotcha LOCKED aplicado: sleep 8-10 s entre operaciones pesadas, reabrir página ante timeout CDP en vez de reintentar en caliente |
| T-114-06 | Elevation of privilege | flags de gate | mitigate | Un `ausencia` violado se clasifica como hallazgo de régimen; el plan prohíbe explícitamente flipear cualquier flag como remedio |
| T-114-SC | Tampering | npm installs | mitigate | CERO paquete nuevo en este plan |
</threat_model>

<verification>
- `114-ANCLAS.md` cubre el 100% de las entradas `ancla` del manifiesto.
- `114-HALLAZGOS.md` da disposición al 100% de los FAIL de la corrida PRE.
- Cero RUT y cero screenshots en los artefactos.
- `git status --porcelain app/ packages/` vacío.
</verification>

<success_criteria>
- SC#2 del ROADMAP cerrado: cada ancla `#id` tiene veredicto contra el DOM del destino.
- Existe una lista cerrada y justificada de defectos, con intención por fix.
- Lo que no es defecto está declarado como tal citando la evidencia de 113.
</success_criteria>

<output>
Create `.planning/phases/114-link-int-links-internos-exhaustivos/114-02-SUMMARY.md` when done
</output>
