---
phase: 114-link-int-links-internos-exhaustivos
plan: 03
type: execute
wave: 3
depends_on: [114-01, 114-02]
files_modified:
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.txt
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.json
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md
autonomous: true
requirements: [LINK-02]

must_haves:
  truths:
    - "Todo link o ancla roto quedó corregido en el código de la app, con evidencia antes/después"
    - "La corrida post-fix se puede re-generar con el mismo comando que la pre-fix"
    - "La suite queda en el baseline exacto (2.963 tests / 283 files), tsc en 0 y los guards de régimen verdes"
    - "El deploy queda declaradamente diferido a la Phase 125 y ningún flag fue tocado"
  artifacts:
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.json"
      provides: "Estado DESPUÉS de los fixes"
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md"
      provides: "Cierre de fase: los 4 success criteria del ROADMAP con su evidencia"
      contains: "SC#"
  key_links:
    - from: ".planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md"
      to: "114-CORRIDA-PRE.json"
      via: "comparación pre/post por id de manifiesto"
      pattern: "114-CORRIDA-PRE"
    - from: ".planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md"
      to: ".planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md"
      via: "disposición de cada H-NN"
      pattern: "H-\\d{2}"
---

<objective>
Corregir en el código de la app cada link y cada ancla rota de `114-HALLAZGOS.md`, con evidencia
antes/después, y cerrar la fase con la corrida post-fix, la no-regresión y el veredicto por success
criterion.

Purpose: SC#3 del ROADMAP exige que todo roto quede corregido con evidencia. El deploy NO ocurre
aquí — viaja con la Phase 125 (decisión LOCKED del prompt rector v12.0), lo que condiciona cómo se
puede declarar SC#3.
Output: fixes en `app/`, `114-CORRIDA-POST.{txt,json}`, `114-VERIFICACION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-01-SUMMARY.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-02-SUMMARY.md

<interfaces>
Baseline de suite LOCKED (inventario 113 §0.5, re-verificado en su Plan 05):

| Workspace | Test files | Tests passed | Skipped |
|-----------|-----------:|-------------:|--------:|
| `app` | 107 | 1428 | 0 |
| `packages/*` (18) | 176 | 1535 | 11 |
| **Total** | **283** | **2963** | **11** |

Comando: `pnpm test` (root = `pnpm -r --filter "./packages/*" test && pnpm --filter ./app test`).
Typecheck: `pnpm typecheck` (`tsc -b`), exit 0.

Guards de régimen que deben quedar verdes (precedente 104-01): anti-insinuación, lockdown,
vsim-antiflip, notif-antiflip, money-antiflip, bento, bento-coherencia, name-match-rut, env-example.

Cobertura del universo: la reconciliación 77/77 (66 filas AN + 11 de chrome) ya se CERRÓ en Wave 1
(114-01 Task 3). Este plan la RE-CONFIRMA sobre la corrida post-fix; no la descubre ni admite gaps
abiertos a esta altura.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aplicar los fixes de link y de ancla con evidencia antes/después</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md (más los archivos de app/ que 114-HALLAZGOS.md §Alcance del Plan 03 enumere)</files>
  <read_first>
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-HALLAZGOS.md` — la lista cerrada
      `H-NN` y la sección `## Alcance del Plan 03` (ids + archivos a tocar).
    - Cada archivo de `app/` listado en ese alcance, ANTES de editarlo.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md` — criterio LOCKED de
      intención por fix.
    - Si algún fix toca copy visible: `app/lib/anti-insinuacion-guard.test.ts` — el linter y sus
      constantes de superficie.
  </read_first>
  <behavior>
    - Por cada `H-NN` con intención `corregir el href del emisor`: el emisor pasa a emitir el href
      correcto; el test existente de esa superficie (o uno nuevo, mínimo) asevera el href resultante.
    - Por cada `H-NN` con intención `añadir el id al destino`: el elemento destino gana el `id` (o el
      estilo de scroll) y un test asevera su presencia en el render.
    - Ningún fix cambia la semántica del dato mostrado, ni un conteo, ni una fecha, ni un gate.
    - Si `114-HALLAZGOS.md` declara 0 hallazgos, esta tarea NO edita código: se limita a registrar
      "cero fixes requeridos" con la evidencia y termina.
  </behavior>
  <action>
    Para cada `H-NN` del alcance, en orden, aplicar el fix mínimo que resuelve el síntoma sin ampliar
    superficie. Antes de cada edición, capturar la línea EXACTA del archivo en su estado actual
    (`archivo:línea` + contenido) como evidencia ANTES; después de la edición, capturar la línea
    resultante como evidencia DESPUÉS. Ambas van al bloque `## Fixes` de `114-VERIFICACION.md`, una
    subsección por `H-NN`, con el `git diff` acotado a ese archivo citado en un bloque de código.

    Reglas LOCKED de esta tarea:
    - **Wave-0 de copy**: si un fix introduce texto visible nuevo o una superficie/vocabulario que el
      linter anti-insinuación aún no cubre, EXTENDER primero el linter (constante de superficie +
      self-check que muerde) y recién después escribir el copy. Orden inverso está prohibido
      (precedente BLOCKER 91 / patrón 100-01, 101-02).
    - **JAMÁS tocar un flag** ni `.env` ni `.env.example` para hacer pasar una aserción. Un
      `ausencia` violado se corrige quitando el link del emisor bajo el gate, nunca encendiendo el gate.
    - **JAMÁS deployar.** El fix viaja con la Phase 125.
    - Ningún paquete npm nuevo.
    - Si un `H-NN` resulta no corregible sin un cambio que exceda el alcance de la fase (p. ej. exige
      dato de PROD ausente), NO forzarlo: reclasificarlo en `114-HALLAZGOS.md` con causa y
      registrarlo en `114-VERIFICACION.md` como diferido con destino explícito (Phase 117/122/125).

    Tras cada fix individual, correr la verificación acotada de esa ruta:
    `node scripts/verificar-links-internos.mjs --route <ruta afectada>` — no dejar acumular fixes sin
    señal (cadencia declarada en `114-VALIDATION.md`).
  </action>
  <verify>
    <automated>pnpm --filter ./app test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Cada `H-NN` del alcance tiene su subsección en `114-VERIFICACION.md` con evidencia ANTES
      (archivo:línea + contenido previo) y DESPUÉS (contenido nuevo) más el diff acotado.
    - `pnpm --filter ./app test` pasa; el conteo de tests de `app` es ≥ 1428 (los tests nuevos suman;
      ninguno desaparece).
    - Cero flags tocados: `git diff --stat .env .env.example` → salida **vacía**.
    - Cero paquete nuevo: `! git diff --name-only | grep -qE 'package\.json|pnpm-lock\.yaml'` sale exit 0.
    - Si hubo copy nuevo: el linter anti-insinuación se extendió en un commit ANTERIOR al del copy
      (`git log --oneline` lo muestra) y su self-check muerde.
    - Si `114-HALLAZGOS.md` declara 0 hallazgos: `git status --porcelain app/` → **vacío** y
      `114-VERIFICACION.md` lo declara literalmente.
  </acceptance_criteria>
  <done>Todo defecto accionable de link o ancla está corregido en código, con trazabilidad antes/después por hallazgo.</done>
</task>

<task type="auto">
  <name>Task 2: Corrida POST-FIX y no-regresión completa</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.txt, .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.json</files>
  <read_first>
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json` — la línea base
      a comparar.
    - `scripts/verificar-links-internos.mjs` — el comando exacto de corrida completa.
  </read_first>
  <action>
    Ejecutar la corrida completa post-fix con el MISMO comando de la pre-fix, cambiando solo el `--out`:
    `node scripts/verificar-links-internos.mjs --out .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST`

    Advertencia LOCKED sobre la lectura del resultado: **el deploy no incluye los fixes** (viajan con
    la Phase 125). Por lo tanto los hallazgos cuyo fix sólo se observa tras deployar seguirán
    apareciendo como FAIL en esta corrida contra el deploy real. Eso NO es una falla del plan: para
    cada uno, la evidencia del fix es (a) el diff citado en `114-VERIFICACION.md` y (b) el test de
    la suite que asevera el comportamiento corregido. Anotar cada caso en el bloque de lectura con
    la marca literal `FIX EN CÓDIGO — se re-verifica en 125` y su test de respaldo. Jamás declarar
    un fix como verificado en el deploy cuando no lo está.

    Anexar a `114-CORRIDA-POST.txt` un bloque `=== DELTA PRE→POST ===` que, comparando por `id` de
    manifiesto contra `114-CORRIDA-PRE.json`, tabule: ids que pasaron de FAIL a PASS, ids que siguen
    FAIL con la marca `FIX EN CÓDIGO — se re-verifica en 125`, ids que siguen FAIL sin fix con su
    causa, y cualquier id que haya REGRESIONADO de PASS a FAIL (regresión = bloqueante, se corrige
    antes de cerrar). Cerrar el bloque re-confirmando la cobertura 77/77 ya establecida en Wave 1.

    Después, correr la no-regresión completa y registrar los números observados:
    - `pnpm test` → comparar contra el baseline LOCKED (app 107/1428, packages 176/1535, total
      283/2963, 11 skipped). Cualquier desviación a la baja se investiga; los tests NUEVOS del Task 1
      suben el conteo de `app` y eso se declara con el delta exacto.
    - `pnpm typecheck` → exit 0.
    - Los 9 guards de régimen, corridos individualmente y con su conteo de tests.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -30 && pnpm typecheck && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Existen `114-CORRIDA-POST.txt` y `114-CORRIDA-POST.json`, generados con el mismo comando que la
      pre-fix salvo `--out`, y con `resultados.length` igual al de la corrida PRE.
    - `114-CORRIDA-POST.txt` contiene el bloque `=== DELTA PRE→POST ===` con las cuatro categorías,
      **cero** ids en la categoría de regresión PASS→FAIL, y la re-confirmación de cobertura 77/77.
    - `pnpm test` exit 0; `packages/*` exactamente 176 files / 1535 tests / 11 skipped (sin cambio);
      `app` ≥ 107 files / ≥ 1428 tests, con el delta declarado explícitamente.
    - `pnpm typecheck` exit 0 (`TYPECHECK_OK` impreso).
    - Los 9 guards de régimen verdes, cada uno con su conteo registrado en el SUMMARY.
    - Cero RUT en los artefactos:
      `! grep -hqE '[0-9]{7,8}-[0-9kK]' 114-CORRIDA-POST.txt 114-CORRIDA-POST.json` sale exit 0.
  </acceptance_criteria>
  <done>El estado POST-FIX está registrado y comparado contra el PRE, sin regresión de suite, tipos ni régimen.</done>
</task>

<task type="auto">
  <name>Task 3: Veredicto de cierre por success criterion</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — `### Phase 114`, los 4 Success Criteria verbatim.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-VALIDATION.md` — los 5 criterios
      de cierre de fase.
    - Los artefactos producidos: `114-CORRIDA-PRE.*`, `114-ANCLAS.md`, `114-HALLAZGOS.md`,
      `114-CORRIDA-POST.*`.
  </read_first>
  <action>
    Completar `114-VERIFICACION.md` con la sección `## Veredicto por success criterion`, una entrada
    por cada uno de los 4 SC del ROADMAP (`SC#1` links solicitados contra el deploy → no-404; `SC#2`
    cada ancla existe en el DOM destino; `SC#3` todo roto corregido con evidencia; `SC#4` corrida
    reproducible con salida guardada). Cada entrada lleva: veredicto (`PASS` / `PASS con limitación
    declarada` / `FAIL`), la evidencia concreta (archivo + conteo + comando), y la limitación si la hay.

    **Regla LOCKED para SC#3:** mientras existan fixes aplicados en código pero no observables contra
    el deploy (porque el deploy viaja con la Phase 125), SC#3 cierra como
    **`PASS con limitación declarada`**, nunca como `PASS` pelado. La limitación se redacta explícita:
    "los fixes están en código con evidencia antes/después y test de respaldo; su observación sobre el
    deploy real ocurre en la Phase 125". Solo si el conteo de fixes es CERO (0 hallazgos) SC#3 puede
    cerrar como `PASS` simple, y en ese caso se declara que no hubo nada que corregir.

    Añadir además:
    - `## Cobertura del universo` — re-confirmación de la reconciliación 77/77 ya CERRADA en Wave 1
      (114-01 Task 3): N entradas del manifiesto + N refs excluidas = 77 refs del inventario 113,
      con la lista de `EXCLUIDOS` y su razón individual. A esta altura no puede haber gaps: si
      apareciera uno, es una regresión del manifiesto y bloquea el cierre — no se declara "abierto".
    - `## Reproducción` — los comandos verbatim (corrida completa, por ruta, por tipo) y los nombres
      de los artefactos que producen.
    - `## Deuda y diferidos` — los `H-NN` diferidos con su fase destino, y la nota LOCKED: **el deploy
      de los fixes de esta fase viaja con la Phase 125; ningún flag fue tocado; `.env` y
      `.env.example` intactos**.
    - `## Régimen` — confirmación de que los estados de gate observados en las corridas coinciden con
      §5 del inventario 113, o el hallazgo de régimen si divergieron.

    Un `FAIL` honesto en un SC es un resultado válido y se reporta como tal con su causa; jamás se
    maquilla un veredicto para cerrar la fase.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const dir='.planning/phases/114-link-int-links-internos-exhaustivos/';const d=fs.readFileSync(dir+'114-VERIFICACION.md','utf8');for(const s of ['SC#1','SC#2','SC#3','SC#4','## Cobertura del universo','## Reproducción','## Deuda y diferidos','## Régimen','Phase 125'])if(d.indexOf(s)<0)throw new Error('falta: '+s);const i=d.indexOf('SC#3');const bloque=d.slice(i,i+600);const cero=/0 hallazgos|cero fixes/i.test(d);if(!cero&&bloque.indexOf('PASS con limitación declarada')<0)throw new Error('SC#3 debe cerrar como PASS con limitacion declarada mientras el deploy sea diferido');if(d.indexOf('77')<0)throw new Error('falta la reconciliacion 77/77');if(/[0-9]{7,8}-[0-9kK]/.test(d))throw new Error('patron RUT presente');console.log('OK verificacion completa | SC#3',cero?'PASS (0 hallazgos)':'PASS con limitacion declarada')"</automated>
  </verify>
  <acceptance_criteria>
    - `114-VERIFICACION.md` contiene las secciones `SC#1`..`SC#4`, `## Cobertura del universo`,
      `## Reproducción`, `## Deuda y diferidos` y `## Régimen`.
    - Cada SC tiene veredicto de la lista cerrada y evidencia citada (artefacto + conteo + comando).
    - **SC#3 cierra como `PASS con limitación declarada`** siempre que haya ≥1 fix aplicado, con la
      limitación redactada (deploy diferido a 125); solo con 0 hallazgos puede ser `PASS` simple.
    - La cobertura re-confirma 77/77 (manifiesto + excluidos) y justifica cada exclusión; cero gaps.
    - La nota de deploy diferido a la Phase 125 está presente, junto con
      `git diff --stat .env .env.example` → vacío citado como evidencia.
    - Cero RUT: `! grep -qE '[0-9]{7,8}-[0-9kK]' .planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md`
      sale exit 0.
  </acceptance_criteria>
  <done>La fase 114 cierra con veredicto trazable por success criterion, con SC#3 honesto respecto del deploy diferido y cero afirmación sin evidencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fix de UI → superficie pública | Un href corregido cambia lo que el sitio ofrece navegar |
| fix → régimen de gates | Un remedio perezoso podría "arreglar" una ausencia encendiendo un flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-07 | Elevation of privilege | flags MONEY / NOTIF / NET / CRUCES / VSIM | mitigate | Prohibición explícita en el Task 1 + gate `git diff --stat .env .env.example` vacío + los 3 anti-flip guards verdes en el Task 2 |
| T-114-08 | Information disclosure | copy nuevo introducido por un fix | mitigate | Wave-0 obligatorio: extender el linter anti-insinuación ANTES del copy; el guard corre verde en el Task 2 |
| T-114-09 | Tampering | superficie de datos | mitigate | Regla LOCKED: ningún fix cambia dato, conteo, fecha ni gate; `pnpm test` contra baseline exacto lo respalda |
| T-114-10 | Repudiation | evidencia de fix | mitigate | Cada `H-NN` exige ANTES/DESPUÉS con `archivo:línea` y diff citado; SC#3 no puede cerrar como PASS pelado con fixes no desplegados |
| T-114-SC | Tampering | npm installs | mitigate | Gate de acceptance: `! git diff --name-only \| grep -qE 'package\.json\|pnpm-lock\.yaml'` |
</threat_model>

<verification>
- `pnpm test` exit 0, `packages/*` idéntico al baseline, `app` ≥ baseline con delta declarado.
- `pnpm typecheck` exit 0.
- 9 guards de régimen verdes individualmente.
- `git diff --stat .env .env.example` vacío.
- Delta PRE→POST sin regresión PASS→FAIL; cobertura 77/77 re-confirmada.
- SC#3 cerrado como `PASS con limitación declarada` (o `PASS` simple solo con 0 hallazgos).
</verification>

<success_criteria>
- SC#3 del ROADMAP cerrado con honestidad respecto del deploy diferido: todo link/ancla roto
  corregido con evidencia antes/después y test de respaldo.
- SC#4 cerrado: corrida reproducible con salida guardada (pre y post).
- La fase cierra sin deployar y sin tocar ningún flag.
- `114-VERIFICACION.md` da veredicto trazable a los 4 success criteria.
</success_criteria>

<output>
Create `.planning/phases/114-link-int-links-internos-exhaustivos/114-03-SUMMARY.md` when done
</output>
