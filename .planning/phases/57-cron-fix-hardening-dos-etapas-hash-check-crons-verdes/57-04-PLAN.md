---
phase: 57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes
plan: 04
type: execute
wave: 3
depends_on:
  - 57-01
  - 57-02
  - 57-03
files_modified:
  - .github/workflows/agenda-weekly.yml
  - .github/workflows/lobby-camara-weekly.yml
  - .github/workflows/probidad-weekly.yml
  - docs/runbooks/cron-local-fallback.md
autonomous: false
requirements:
  - CRON-04

user_setup:
  - service: github-actions
    why: "Cargar los 5 secrets faltantes al repo Cuchecorp/gov-map"
    env_vars:
      - name: R2_ENDPOINT_URL
        source: ".env local (key R2_ENDPOINT_URL)"
      - name: R2_ACCESS_KEY_ID
        source: ".env local (key R2_ACCESS_KEY_ID)"
      - name: R2_SECRET_ACCESS_KEY
        source: ".env local (key R2_SECRET_ACCESS_KEY)"
      - name: R2_BUCKET
        source: ".env local (key R2_BUCKET)"
      - name: DEEPSEEK_API_KEY
        source: ".env local (key DEEPSEEK_API_KEY)"

must_haves:
  truths:
    - "gh secret list --repo Cuchecorp/gov-map muestra los 5 nombres: R2_ENDPOINT_URL R2_ACCESS_KEY_ID R2_SECRET_ACCESS_KEY R2_BUCKET DEEPSEEK_API_KEY"
    - "lobby-camara-weekly.yml NO tiene schedule activo — solo workflow_dispatch + comentario explicativo"
    - "agenda-weekly.yml tiene los 7 secrets en el bloque env: incluyendo DEEPSEEK_API_KEY y los 4 R2"
    - "probidad-weekly.yml usa grep consultados=[1-9] (no declaraciones=[1-9]) en su assert"
    - "Al menos 1 corrida workflow_dispatch de leyes-weekly o agenda-weekly termina con conclusion=success"
    - "docs/runbooks/cron-local-fallback.md existe con instrucciones para lobby-camara local + gh secret set cookbook"
  artifacts:
    - path: "docs/runbooks/cron-local-fallback.md"
      provides: "Runbook WAF lobby-camara + gh secret set cookbook + verificación post-corrida"
      contains: "lobby-camara"
    - path: ".github/workflows/lobby-camara-weekly.yml"
      provides: "Schedule deshabilitado, dispatch-only, comentario G7"
      contains: "workflow_dispatch"
    - path: ".github/workflows/agenda-weekly.yml"
      provides: "env: block con los 7 secrets completos"
      contains: "DEEPSEEK_API_KEY"
    - path: ".github/workflows/probidad-weekly.yml"
      provides: "Assert rediseñado: consultados=[1-9]"
      contains: "consultados="
  key_links:
    - from: "gh secret set"
      to: "Cuchecorp/gov-map repo secrets"
      via: "gh CLI autenticado (verificado en Phase 56)"
      pattern: "gh secret list --repo Cuchecorp/gov-map"
    - from: ".github/workflows/leyes-weekly.yml"
      to: "GH Actions runner"
      via: "workflow_dispatch (G4 fix en plan 02 ya desbloqueó este workflow)"
      pattern: "gh run watch"
---

<objective>
Ops closure: cargar los 5 secrets faltantes al repo de CI, actualizar los YAML de workflows
para que reflejen los secrets disponibles, deshabilitar el schedule de lobby-camara (G7 WAF),
corregir el assert de probidad en el YAML, crear el runbook de fallback local, y verificar al
menos una corrida E2E verde en GitHub Actions.

Purpose: El código de Wave 1 y 2 está completo pero inútil sin secrets en CI. Este plan cierra
el gap operacional: secrets → YAML correcto → corrida verde → runbook documentado. Al terminar,
el criterio "crons corren verdes L–V" de CRON-04 está verificado.

Output: 4 archivos modificados/creados, 5 secrets cargados, 1 corrida E2E `conclusion=success`,
runbook navegable. Checkpoint humano para confirmar la corrida verde.
</objective>

<execution_context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\phases\57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes\57-VALIDATION.md
</execution_context>

<context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\PROJECT.md
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\ROADMAP.md

<interfaces>
<!-- Mapa de secrets por workflow — de RESEARCH.md "Secrets y Workflows" -->

Secrets a cargar (5 nombres exactos del repo — los valores vienen de .env local):
  R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, DEEPSEEK_API_KEY

Gaps por workflow (de PATTERNS.md "agenda-weekly.yml + others"):
  agenda-weekly.yml: falta DEEPSEEK_API_KEY, R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET
  lobby-camara-weekly.yml: falta R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET (+ schedule OFF)
  probidad-weekly.yml: falta R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET + assert fix
  leyes-weekly.yml: sin gaps de secrets (solo tenía G4 — resuelto en plan 02)
  lobby-leylobby-weekly.yml: sin gaps (mapeo YAML ya correcto)

Referencia de env: block completo (de agenda-weekly.yml:53–70 — el analog canónico):
```yaml
env:
  SUPABASE_API_URL: ${{ secrets.SUPABASE_API_URL }}
  SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
  DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
  R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
  R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
  R2_BUCKET: ${{ secrets.R2_BUCKET }}
```

G7 on: block nuevo para lobby-camara-weekly.yml (de PATTERNS.md):
```yaml
# WAF de camara.cl bloquea IPs de GH Actions desde al menos 2026-06-30 (G7 audit Phase 56).
# curl devuelve <10KB (WAF intercept) → el paso de descarga falla antes del CLI.
# Schedule DESHABILITADO: usar dispatch manual solo cuando el operador confirme que el WAF
# permite GH Actions. Fallback local: docs/runbooks/cron-local-fallback.md
on:
  workflow_dispatch:
```

Probidad assert actual (lines 69–70 de probidad-weekly.yml — a reemplazar):
```bash
echo "$OUT" | grep -qE 'declaraciones=[1-9][0-9]*|confirmados=[1-9][0-9]*' \
  || { echo "sin declaraciones/confirmados"; exit 1; }
```
Nuevo assert:
```bash
echo "$OUT" | grep -qE 'consultados=[1-9][0-9]*' \
  || { echo "probidad: 0 parlamentarios consultados — posible error fatal"; exit 1; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar YAMLs de workflows + crear runbook</name>
  <files>
    .github/workflows/agenda-weekly.yml
    .github/workflows/lobby-camara-weekly.yml
    .github/workflows/probidad-weekly.yml
    docs/runbooks/cron-local-fallback.md
  </files>

  <read_first>
  - `.github/workflows/agenda-weekly.yml` completo — localizar el bloque `env:` actual (líneas ~53–70)
    y confirmar qué secrets ya están y cuáles faltan.
  - `.github/workflows/lobby-camara-weekly.yml` completo — ver el bloque `on:` actual y los steps
    para confirmar que el comentario G7 queda en contexto.
  - `.github/workflows/probidad-weekly.yml` completo — localizar las líneas 69–70 del assert y el
    bloque `env:` para añadir los R2 secrets.
  - PATTERNS.md sección ".github/workflows/lobby-camara-weekly.yml — disable schedule" y
    ".github/workflows/agenda-weekly.yml + others — add missing secrets".
  - RESEARCH.md "Runbook docs/runbooks/cron-local-fallback.md" para las 5 secciones del runbook.
  - `.github/workflows/lobby-camara-weekly.yml` lines 49–65 — los comandos curl exactos a copiar
    en el runbook como "pasos para corrida local".
  </read_first>

  <action>
  **agenda-weekly.yml:**
  Localizar el bloque `env:` en el job. Añadir las 5 entradas faltantes siguiendo el formato
  `KEY: ${{ secrets.KEY }}` (el analog exacto es el bloque existente de SUPABASE_API_URL).
  El bloque final debe tener 7 entradas (las 2 que ya existen + 5 nuevas).
  NO tocar ningún otro campo del YAML.

  **lobby-camara-weekly.yml:**
  Reemplazar el bloque `on:` completo con el bloque dispatch-only de PATTERNS.md (con el comentario
  explicativo de 4 líneas sobre G7). Dejar todos los steps intactos — solo el trigger cambia.
  También añadir las 4 entradas R2 al bloque `env:` del job (si existe; si el YAML no tiene bloque
  env todavía, añadirlo como bloque `env:` a nivel del job o step que corre el CLI).

  **probidad-weekly.yml:**
  1. Añadir las 4 entradas R2 al bloque `env:` (misma posición que en agenda-weekly).
  2. Reemplazar las líneas del assert (grep declaraciones=[1-9]) con el nuevo assert
     (grep consultados=[1-9]) de RESEARCH.md "Fix en dos partes — Parte 2".
  El mensaje de error del assert también cambia: "probidad: 0 parlamentarios consultados — posible error fatal".

  **docs/runbooks/cron-local-fallback.md:**
  Crear el directorio `docs/runbooks/` y el archivo. Estructura de 5 secciones (de RESEARCH.md):
  1. **Prerequisitos** — `.env` con R2_*, SUPABASE_*, DEEPSEEK_API_KEY; pnpm; Node 22; gh CLI.
  2. **Lobby Cámara (WAF-bloqueado en GH Actions)** — pasos: curl de descarga con UA + `--html-file`
     CLI. Copiar los comandos exactos de los steps de lobby-camara-weekly.yml:49–65.
  3. **gh secret set cookbook** — cómo cargar/actualizar los 5 secrets sin imprimir valores.
     Patrón seguro: `gh secret set R2_ENDPOINT_URL --repo Cuchecorp/gov-map < <(grep '^R2_ENDPOINT_URL=' .env | cut -d= -f2-)`.
     Listar los 5 nombres. NOTA CRÍTICA en el runbook: "NUNCA copiar/pegar valores en el terminal — usar redirección de archivo o pipe".
  4. **Re-enabling lobby-camara schedule** — instrucción: editar lobby-camara-weekly.yml `on:` block,
     descomentar el schedule, push a master, verificar en Actions tab.
  5. **Verificación post-corrida** — comandos psql para verificar counts en lobby_audiencia + source_snapshot;
     y `gh run list --workflow <wf> --repo Cuchecorp/gov-map --limit 3` para revisar historial.

  Restricciones:
  - Los valores de los secrets NUNCA aparecen en el runbook ni en ningún commit — solo los NOMBRES.
  - `lobby-camara-weekly.yml` queda con SOLO `workflow_dispatch:` en el bloque `on:`.
  - NO modificar `leyes-weekly.yml` ni `lobby-leylobby-weekly.yml` (no tienen gaps).
  </action>

  <verify>
    <automated>grep -n "DEEPSEEK_API_KEY" .github/workflows/agenda-weekly.yml && grep -n "workflow_dispatch" .github/workflows/lobby-camara-weekly.yml && grep -v "^#" .github/workflows/lobby-camara-weekly.yml | grep -c "schedule" | grep -q "^0$" && grep -n "consultados=" .github/workflows/probidad-weekly.yml && test -f docs/runbooks/cron-local-fallback.md</automated>
  </verify>
  <done>agenda-weekly.yml tiene 7 secrets en env; lobby-camara-weekly.yml no tiene schedule activo (solo workflow_dispatch); probidad-weekly.yml usa grep consultados=[1-9]; runbook existe.</done>
</task>

<task type="auto">
  <name>Task 2: Cargar secrets al repo CI y verificar presencia</name>
  <files></files>

  <read_first>
  - `.env` en la raíz del proyecto — localizar R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY,
    R2_BUCKET, DEEPSEEK_API_KEY. Verificar que todos existen (MEMORY.md: "R2 S3 OK al 2026-06-20").
  - RESEARCH.md "Secrets y Workflows" — tabla consolidada de secrets a cargar.
  - CONTEXT.md Decisions "Secrets: cargar R2_* etc. vía gh secret set leyendo de .env local. Los VALORES
    jamás se imprimen en logs/commits/docs."
  </read_first>

  <action>
  Para cada uno de los 5 secrets, ejecutar el comando de carga usando redirección o pipe desde `.env`
  sin que el valor aparezca en el terminal ni en los logs del proceso.

  Patrón seguro para cada secret (verificar primero que `gh` version soporta este syntax con
  `gh secret set --help`):
  ```
  gh secret set R2_ENDPOINT_URL --repo Cuchecorp/gov-map < <(grep '^R2_ENDPOINT_URL=' .env | cut -d= -f2-)
  ```

  Si la sintaxis de process substitution no funciona en PowerShell, alternativa:
  ```powershell
  $val = (Get-Content .env | Where-Object { $_ -match '^R2_ENDPOINT_URL=' }) -replace '^R2_ENDPOINT_URL=',''
  $val | gh secret set R2_ENDPOINT_URL --repo Cuchecorp/gov-map
  Remove-Variable val
  ```

  Cargar los 5 secrets en este orden:
  1. R2_ENDPOINT_URL
  2. R2_ACCESS_KEY_ID
  3. R2_SECRET_ACCESS_KEY
  4. R2_BUCKET
  5. DEEPSEEK_API_KEY

  Después de cargar, verificar presencia (sin valores):
  ```bash
  gh secret list --repo Cuchecorp/gov-map
  ```
  Los 5 nombres deben aparecer en el listado. Los valores NO se imprimen (GH solo lista nombres).

  Restricciones:
  - NUNCA usar `echo $VAL | gh secret set` — el valor quedaría en el historial de shell.
  - NUNCA imprimir el valor antes de pasarlo a gh.
  - Si algún secret falla, reintentar solo ese — no re-cargar todos.
  </action>

  <verify>
    <automated>gh secret list --repo Cuchecorp/gov-map | grep -E "R2_ENDPOINT_URL|R2_ACCESS_KEY_ID|R2_SECRET_ACCESS_KEY|R2_BUCKET|DEEPSEEK_API_KEY" | wc -l | grep -q "5"</automated>
  </verify>
  <done>gh secret list muestra los 5 nombres (R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, DEEPSEEK_API_KEY). Los valores nunca se imprimieron.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Disparar corrida E2E y verificar verde</name>

  <what-built>
  Plan 01 añadió R2Store.getObject + putImmutable{existed}. Plan 02 corrigió G4 (leyes-weekly
  desbloqueado). Plan 03 añadió Etapa 1 + hash-check + --from-r2 en los 5 conectores. Task 1
  actualizó los YAMLs. Task 2 cargó los 5 secrets. El agente ahora dispara una corrida acotada de
  leyes-weekly o agenda-weekly con workflow_dispatch para verificar que el stack E2E es verde.
  </what-built>

  <automated-first>
  El agente ejecuta estos pasos antes de pausar:

  1. Commit y push de todos los cambios de Wave 3 (YAMLs + runbook + cualquier pendiente):
     ```bash
     git add .github/workflows/agenda-weekly.yml .github/workflows/lobby-camara-weekly.yml \
             .github/workflows/probidad-weekly.yml docs/runbooks/cron-local-fallback.md
     git commit -m "ops(57): cargar secrets env, YAMLs secrets+G7, probidad assert, runbook WAF"
     git push
     ```

  2. Disparar corrida acotada de leyes-weekly (primer candidato — solo tiene 2 secrets, G4 ya fijo):
     ```bash
     gh workflow run leyes-weekly.yml --repo Cuchecorp/gov-map
     ```
     Si leyes-weekly falla por razón no relacionada con G4/secrets, intentar agenda-weekly:
     ```bash
     gh workflow run agenda-weekly.yml --repo Cuchecorp/gov-map
     ```

  3. Esperar resultado (max 10 minutos — los workflows son acotados):
     ```bash
     gh run list --repo Cuchecorp/gov-map --workflow leyes-weekly.yml --limit 3
     # o el workflow disparado
     gh run watch <RUN_ID> --repo Cuchecorp/gov-map
     ```

  4. Si la corrida termina: registrar `conclusion` (success/failure) y el run ID.

  5. Para los workflows que NO se dispararon en E2E (agenda, lobby-leylobby, probidad):
     verificar en dry-run local si es posible (sin escribir a Supabase):
     ```bash
     pnpm --filter @obs/tramitacion exec tsx src/run-tramitacion-prod-cli.ts --dry-run --limite 1
     pnpm --filter @obs/lobby exec tsx src/ingest-cli.ts --dry-run
     ```
  </automated-first>

  <how-to-verify>
  1. Revisar la corrida disparada en https://github.com/Cuchecorp/gov-map/actions
  2. Confirmar que el workflow elegido (leyes-weekly o agenda-weekly) terminó con estado "Success" (check verde).
  3. En los logs de la corrida, buscar cualquiera de estas señales positivas:
     - "upsertEventos" sin error de ON CONFLICT (leyes-weekly)
     - "Etapa 1: crudo en R2 →" (agenda-weekly con R2 ahora configurado)
     - Un "summary" al final sin "error" o "failure"
  4. Verificar en gh CLI:
     ```bash
     gh run list --repo Cuchecorp/gov-map --limit 5
     ```
     El run más reciente debe mostrar `completed / success`.
  </how-to-verify>

  <resume-signal>
  Escribe "verde: [workflow] run [RUN_ID]" si la corrida terminó en success.
  Escribe "fallo: [síntoma del log]" si terminó en failure — el agente diagnosticará y propondrá fix.
  Escribe "billing-bloqueado" si GH Actions rechaza la corrida por billing — el agente documentará
  el fallback local como entregable alternativo de CRON-04.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → gh secret set → GH repo | Las credenciales viajan de .env local a GH secrets sin pasar por terminal/historial/logs |
| GH Actions runner → fuentes gubernamentales | Los workflows de cron acceden a fuentes con rate-limit 2-3s y UA identificatorio ya configurados |
| GH Actions runner → R2 | Los secrets R2_* son los únicos que permiten escritura a R2; GH los lee solo en runtime, no los expone en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Information Disclosure | gh secret set — valores en terminal | mitigate | Usar redirección de archivo o pipe; NUNCA echo $val; los valores nunca aparecen en commits/docs/logs (CONTEXT.md D-locked) |
| T-57-07 | Denial of Service | probidad-weekly assert falso-positivo | mitigate | Assert rediseñado: consultados>0 (no declaraciones>0) — semana sin novedades no causa exit 1 |
| T-57-08 | Elevation of Privilege | lobby-camara schedule activo post-WAF | accept | Schedule deshabilitado en YAML; re-habilitar requiere editar YAML + push (no es un cambio accidental) |
| T-57-SC | Tampering | npm/pip installs | accept | No hay nuevas dependencias en este plan |
</threat_model>

<verification>
Verificaciones finales de toda la fase 57 (post checkpoint E2E):

```bash
# 1. Suite completa verde
pnpm -w test

# 2. Typecheck sin errores
pnpm -w typecheck

# 3. Los 5 secrets presentes (sin valores)
gh secret list --repo Cuchecorp/gov-map | grep -E "R2_ENDPOINT_URL|R2_ACCESS_KEY_ID|R2_SECRET_ACCESS_KEY|R2_BUCKET|DEEPSEEK_API_KEY"

# 4. lobby-camara-weekly.yml sin schedule activo
grep -v "^#" .github/workflows/lobby-camara-weekly.yml | grep -c "schedule"
# debe retornar 0

# 5. probidad assert usa consultados
grep "consultados=" .github/workflows/probidad-weekly.yml

# 6. agenda-weekly.yml tiene DEEPSEEK + R2 en env:
grep -E "DEEPSEEK_API_KEY|R2_ENDPOINT_URL" .github/workflows/agenda-weekly.yml

# 7. Runbook existe
test -f docs/runbooks/cron-local-fallback.md && echo "OK"

# 8. Corrida E2E — revisar el último run del workflow elegido
gh run list --repo Cuchecorp/gov-map --workflow leyes-weekly.yml --limit 1

# 9. [skip] log grep-able en código (para Phase 58)
grep -rn "\[skip\] sin novedades" packages/tramitacion/src/ packages/lobby/src/

# 10. getObject en r2-store
grep -n "getObject" packages/ingest/src/r2-store.ts
```
</verification>

<success_criteria>
- `gh secret list --repo Cuchecorp/gov-map` muestra los 5 secrets: R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, DEEPSEEK_API_KEY.
- `lobby-camara-weekly.yml` tiene solo `workflow_dispatch:` en el bloque `on:` (no hay `schedule:`).
- `agenda-weekly.yml` tiene 7 entradas en `env:` (incluyendo DEEPSEEK_API_KEY y los 4 R2).
- `probidad-weekly.yml` assert usa `consultados=[1-9]`.
- `docs/runbooks/cron-local-fallback.md` existe con las 5 secciones incluyendo gh secret set cookbook.
- Al menos 1 corrida workflow_dispatch de leyes-weekly o agenda-weekly con `conclusion=success`.
- Suite completa `pnpm -w test` verde.
- `pnpm -w typecheck` sin errores.
- Los valores de los secrets NUNCA aparecen en ningún commit, log, o archivo.
</success_criteria>

<output>
Crear `.planning/phases/57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes/57-04-SUMMARY.md` cuando termine.
</output>
