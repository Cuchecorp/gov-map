---
phase: 57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes
plan: 03
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - packages/tramitacion/src/ingest-cli.ts
  - packages/lobby/src/ingest-cli.ts
  - packages/lobby/src/ingest-run.ts
  - packages/agenda/src/run-agenda-prod-cli.ts
  - packages/probidad/src/run-probidad-todos.ts
  - packages/probidad/src/run-probidad-todos-cli.ts
autonomous: true
requirements:
  - CRON-02
  - CRON-03
  - CRON-04

must_haves:
  truths:
    - "tramitacion/ingest-cli con --from-r2 <path> no emite ningún fetch a senado.cl ni camara.cl"
    - "tramitacion/ingest-cli con crudo ya en R2 (412) emite log [skip] sin novedades — tramitacion <boletin>"
    - "lobby/ingest-cli persiste audiencias a R2 (Etapa 1) antes de parsear y retorna {existed}"
    - "agenda/run-agenda-prod-cli emite [WARN] R2 no configurado cuando R2_ACCESS_KEY_ID ausente y no es dry-run"
    - "probidad weekly con 0 declaraciones nuevas pero consultados>0 NO lanza exit 1"
    - "probidad emite log con rawBindings.length por parlamentario antes del matching"
  artifacts:
    - path: "packages/tramitacion/src/ingest-cli.ts"
      provides: "Etapa 1 R2, hash-check early-exit, modo --from-r2, WARN loud R2 no configurado"
      contains: "from-r2"
    - path: "packages/lobby/src/ingest-cli.ts"
      provides: "Etapa 1 R2 write (G10), modo --from-r2, R2Store import"
      contains: "R2Store"
    - path: "packages/probidad/src/run-probidad-todos.ts"
      provides: "rawBindings logging pre-matching"
      contains: "rawBindings"
    - path: "packages/probidad/src/run-probidad-todos-cli.ts"
      provides: "assertion redesign consultados>0"
      contains: "consultados"
  key_links:
    - from: "packages/tramitacion/src/ingest-cli.ts"
      to: "packages/ingest/src/r2-store.ts"
      via: "r2Store.putImmutableStatus (retorno existed, plan 01) + r2Store.getObject (plan 01)"
      pattern: "putImmutableStatus|getObject"
    - from: "packages/lobby/src/ingest-cli.ts"
      to: "packages/ingest/src/r2-store.ts"
      via: "R2Store import de @obs/ingest (no existía en G10)"
      pattern: "R2Store.*@obs/ingest"
    - from: "packages/probidad/src/run-probidad-todos-cli.ts"
      to: "packages/probidad/src/run-probidad-todos.ts"
      via: "RunProbidadTodosResult.consultados field"
      pattern: "consultados"
---

<objective>
Cablear los primitivos de Plan 01 en los 5 conectores recurrentes: (1) Etapa 1 R2 write en
tramitacion y lobby-leylobby donde faltaba (G5, G10), (2) hash-check early-exit con log `[skip]`
grep-able (CRON-03) en tramitacion y lobby-leylobby, (3) modo `--from-r2` en tramitacion y
lobby-leylobby para re-ingesta sin tocar fuentes (G23/CRON-02), (4) WARN loud cuando R2 no
configurado en agenda, (5) redesign de assertion probidad + logging rawBindings.

Purpose: Wave 1 produjo los primitivos; esta wave los conecta a los 5 CLIs recurrentes. Al
terminar esta wave, todos los conectores cumplen DOS ETAPAS + hash-check en el código — falta solo
cargar secrets y disparar la corrida E2E (Wave 3/Plan 04).

Output: 6 archivos modificados, tests de spy que verifican 0 fetches a fuentes gubernamentales en
modo `--from-r2`, y assertion probidad rediseñada. Suite completa verde.
</objective>

<execution_context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\phases\57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes\57-VALIDATION.md
</execution_context>

<context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\PROJECT.md
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\ROADMAP.md

<interfaces>
<!-- Primitivos producidos por Plan 01 (57-01-PLAN.md) — usar directamente -->

De packages/ingest/src/r2-store.ts (post plan 01):
```typescript
class R2Store {
  async putImmutable(source, resource, date, sha, ext, body): Promise<{ r2Path: string; existed: boolean }>
  async getObject(r2Path: string): Promise<Uint8Array>
}
```

De packages/ingest/src/index.ts:
```typescript
export { R2Store, sha256Hex } from "./r2-store";
```

<!-- Patrones de PATTERNS.md a reusar tal cual -->

Etapa 1 best-effort (de run-camara-lobby.ts:85–105):
```typescript
if (r2Store) {
  try {
    const bytes = new TextEncoder().encode(JSON.stringify(envelope));
    const sha = await sha256Hex(bytes);
    const { r2Path, existed } = await r2Store.putImmutable(source, resource, date, sha, ext, bytes);
    if (existed) {
      log(`[skip] sin novedades — ${source} ${resource}`);
      return; // o continue — skip Etapa 2
    }
    log(`${source}: crudo en R2 → ${r2Path}`);
  } catch (err) {
    log(`${source}: Etapa 1 R2 falló (no fatal): ${(err as Error).message}`);
  }
}
```

R2 env gate + WARN loud (de probidad/run-probidad-todos-cli.ts:66–77):
```typescript
const r2Store = (env.R2_ACCESS_KEY_ID && env.R2_ENDPOINT_URL)
  ? new R2Store({ accessKeyId: env.R2_ACCESS_KEY_ID, secretAccessKey: env.R2_SECRET_ACCESS_KEY ?? "",
                  endpoint: env.R2_ENDPOINT_URL, bucket: env.R2_BUCKET ?? "" })
  : null;
if (!r2Store && !dryRun) {
  log("[WARN] R2 no configurado — Etapa 1 omitida (sin crudo versionado)");
}
```

Flag --from-r2 en parseArgs (pattern de PATTERNS.md):
```typescript
case "--from-r2":
  opts.fromR2 = argv[++i];
  if (!opts.fromR2) throw new IngestCliArgsError("--from-r2 requiere un r2Path");
  break;
```

Probidad rawBindings log (insertar después de fetchSparql — PATTERNS.md):
```typescript
const rawBindings = (json as { results?: { bindings?: unknown[] } })?.results?.bindings ?? [];
log(`probidad-todos: ${p.id} (${frag}) → SPARQL devolvió ${rawBindings.length} bindings`);
```

Probidad assert nuevo en run-probidad-todos-cli.ts (después de runProbidadTodos):
```typescript
log(`[ok] probidad consultados=${res.consultados} declaraciones=${res.declaraciones} errores=${res.errores}`);
// El workflow YAML cambia el grep de declaraciones=[1-9] a consultados=[1-9]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tramitacion/ingest-cli — Etapa 1, hash-check, --from-r2</name>
  <files>packages/tramitacion/src/ingest-cli.ts</files>

  <read_first>
  - `packages/tramitacion/src/ingest-cli.ts` completo — localizar: comentario "R2/remoto diferidos"
    (línea 16), `parseArgs` (línea ~62), `IngestCliOptions` interface, `main()` body,
    el loop por boletín donde se hacen los fetch.
  - `packages/tramitacion/src/run-tramitacion-prod-cli.ts` — ver cómo se usa `loadEnv` y cómo se
    pasan opciones; el `ingest-cli.ts` de tramitacion puede ser diferente.
  - PATTERNS.md sección "tramitacion/src/ingest-cli.ts — add --from-r2, Etapa 1, hash-check" con
    los tres analogs (A flag, B etapa-1 best-effort, C R2 env gate).
  - RESEARCH.md "Pitfall 2: --from-r2 con crudo multi-boletín" y "Pitfall 1: putImmutable retorna
    void-like" (ya resuelto por plan 01 — putImmutable ahora retorna { existed }).
  - RESEARCH.md "Tramitacion: crudo por boletín vs. crudo por run" — usar crudo POR BOLETÍN:
    envelope `{ boletin, tramXml, votXml, detalles: string[] }` en JSON.
  </read_first>

  <behavior>
  - `--from-r2 <r2Path>`: cuando presente, llama r2Store.getObject(r2Path) → Uint8Array → JSON.parse envelope → reusar parsers existentes → writer. CERO fetches a senado.cl / camara.cl.
  - Etapa 1: tras cada fetch de XML por boletín, construir envelope JSON `{ boletin, tramXml, votXml, detalles }`, sha256Hex, r2Store.putImmutable. Si existed=true → log `[skip] sin novedades — tramitacion ${boletin}` → continue (skip Etapa 2 para ese boletín).
  - R2 env gate: si R2_ACCESS_KEY_ID ausente y no es dry-run → log `[WARN] R2 no configurado — Etapa 1 omitida (sin crudo versionado)`.
  - El modo `--from-r2` requiere r2Store configurado; si no está, lanzar error descriptivo.
  </behavior>

  <action>
  Editar `packages/tramitacion/src/ingest-cli.ts`:

  1. **Añadir import:** `import { R2Store, sha256Hex } from "@obs/ingest";` al top.

  2. **Extender `IngestCliOptions`:** añadir `fromR2?: string`.

  3. **En `parseArgs`:** añadir `case "--from-r2"` siguiendo el patrón de PATTERNS.md (Analog A).

  4. **En `main()`:**
     a. Construir `r2Store` con el env gate pattern (Analog C) — leer R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_ENDPOINT_URL, R2_BUCKET de env; WARN si ausentes y no dry-run.
     b. Guard temprano: si `opts.fromR2` presente, llamar `r2Store.getObject(opts.fromR2)` → parsear envelope JSON → ejecutar parsers → writer → return.
     c. En el loop por boletín, tras los fetch de XML, construir envelope `{ boletin, tramXml, votXml, detalles }`, calcular sha256Hex, llamar `r2Store.putImmutable(...)`. Si `existed=true`, log `[skip] sin novedades — tramitacion ${boletin}` y `continue`.
     d. Si Etapa 1 falla (try/catch), log no-fatal y continuar con Etapa 2.

  5. Borrar o actualizar el comentario "R2/remoto diferidos" en línea 16.

  Restricciones:
  - Los parsers existentes (`parseSenadoTramitacion`, etc.) se reusan SIN duplicar.
  - En modo `--from-r2`, 0 fetches a fuentes externas.
  - Rate-limit y UA no se modifican — solo se añade la capa R2 antes/después de los fetch existentes.
  </action>

  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run</automated>
  </verify>
  <done>Suite @obs/tramitacion verde; tramitacion/ingest-cli.ts contiene "from-r2", "R2Store", "[skip] sin novedades", "[WARN] R2 no configurado".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lobby/ingest-cli — Etapa 1 R2 (G10) + --from-r2; probidad rawBindings + assert redesign</name>
  <files>
    packages/lobby/src/ingest-cli.ts
    packages/lobby/src/ingest-run.ts
    packages/agenda/src/run-agenda-prod-cli.ts
    packages/probidad/src/run-probidad-todos.ts
    packages/probidad/src/run-probidad-todos-cli.ts
  </files>

  <read_first>
  - `packages/lobby/src/ingest-cli.ts` completo — confirmar ausencia de R2Store import (G10 confirmado),
    ver `parseArgs`, `RunIngestLobbyOpts` (si se pasa a ingest-run o inline en main).
  - `packages/lobby/src/ingest-run.ts` — ver `RunIngestLobbyOpts` type (líneas 41–64) y el loop de tareas
    donde se hace fetch de audiencias — ese es el punto de inserción de Etapa 1.
  - `packages/lobby/src/run-camara-lobby.ts` líneas 85–105 — el analog EXACTO de Etapa 1 best-effort
    para lobby. Copiar este patrón en ingest-cli.ts o ingest-run.ts.
  - `packages/agenda/src/run-agenda-prod-cli.ts` — ver si ya hay R2Store construction; si no, añadir
    solo el WARN loud gate (no hay Etapa 1 nueva para agenda en este plan — agenda ya tiene código parcial).
  - `packages/probidad/src/run-probidad-todos.ts` línea ~119–135 — localizar `fetchSparql` call e
    insertar el log rawBindings.
  - `packages/probidad/src/run-probidad-todos-cli.ts` — ver el summary log y el tipo
    `RunProbidadTodosResult`; determinar si `consultados` field existe o hay que añadirlo.
  - PATTERNS.md secciones: "lobby/ingest-cli.ts", "agenda/run-agenda-prod-cli.ts", "probidad/run-probidad-todos.ts", "probidad/run-probidad-todos-cli.ts".
  </read_first>

  <behavior>
  lobby/ingest-cli.ts:
  - Importa R2Store, sha256Hex de @obs/ingest.
  - R2 env gate (mismo patrón que tramitacion/probidad): WARN si ausente y no dry-run.
  - --from-r2 <path>: r2Store.getObject → body → parseLobbyAudiencias → writer, 0 fetches externos.
  - Etapa 1: en el loop de audiencias, tras fetch, putImmutable; if existed → [skip] sin novedades — leylobby <url/tarea>.

  agenda/run-agenda-prod-cli.ts:
  - Si no tiene ya el WARN gate: añadir chequeo de R2_ACCESS_KEY_ID; si ausente y !dryRun → log [WARN] R2 no configurado.

  probidad/run-probidad-todos.ts:
  - Tras fetchSparql → añadir log de rawBindings.length (pure addition, zero logic change).

  probidad/run-probidad-todos-cli.ts:
  - Añadir `consultados` al tipo de resultado si falta.
  - Emitir log `[ok] probidad consultados=N declaraciones=M errores=K`.
  - El workflow YAML probidad-weekly.yml cambia el grep de `declaraciones=[1-9]` a `consultados=[1-9]` — este archivo YAML se modifica en Plan 04; aquí solo se prepara el output del CLI.
  </behavior>

  <action>
  **lobby/src/ingest-cli.ts:**
  1. Añadir `import { R2Store, sha256Hex } from "@obs/ingest";`.
  2. Extender `IngestCliOptions` con `fromR2?: string`.
  3. En `parseArgs`, añadir `case "--from-r2"` (mismo patrón que tramitacion).
  4. En `main()`: construir r2Store con env gate + WARN. Guard `--from-r2` temprano (getObject → body → parsers → writer → return). En el loop de fetch de audiencias, aplicar Etapa 1 best-effort (patrón run-camara-lobby.ts:85–105): putImmutable → if existed → [skip] sin novedades — leylobby.
  5. Determinar si la Etapa 1 write va en `ingest-cli.ts::main()` (antes de llamar `runIngestLobby`) o en `ingest-run.ts::runIngestLobby`. Preferir inline en `main()` para no cambiar la interfaz `RunIngestLobbyOpts` — es más simple (per PATTERNS.md nota "alternatively do the write in ingest-cli.ts::main()").

  **agenda/src/run-agenda-prod-cli.ts:**
  Leer el archivo primero. Si ya construye R2Store pero no emite WARN cuando ausente: añadir el WARN gate. Si no construye R2Store en absoluto: añadir el bloque completo de env gate + WARN (solo WARN, no nueva lógica de Etapa 1 — agenda ya tiene código parcial de putImmutable en ingest-run.ts:218 que se activa cuando secrets lleguen en Wave 3).

  **probidad/src/run-probidad-todos.ts:**
  Localizar la llamada `fetchSparql`. Inmediatamente después, añadir:
  ```typescript
  const rawBindings = (json as { results?: { bindings?: unknown[] } })?.results?.bindings ?? [];
  log(`probidad-todos: ${p.id} (${frag}) → SPARQL devolvió ${rawBindings.length} bindings`);
  ```
  Solo esto. Cero cambios de lógica.

  **probidad/src/run-probidad-todos-cli.ts:**
  1. Si `RunProbidadTodosResult` no tiene campo `consultados: number`, añadirlo.
  2. Asegurar que el CLI incremente `consultados` en cada parlamentario procesado (si no existe el counter, añadirlo al loop en `run-probidad-todos.ts`).
  3. Después de `runProbidadTodos`, añadir la línea de log `[ok] probidad consultados=N declaraciones=M errores=K`.

  Restricciones:
  - NO modificar `.github/workflows/probidad-weekly.yml` aquí — eso va en Plan 04.
  - Los parsers de audiencias se reusan SIN duplicar.
  - Rate-limit 2-3s y UA no se tocan.
  </action>

  <verify>
    <automated>pnpm --filter @obs/lobby test --run && pnpm --filter @obs/probidad test --run && pnpm --filter @obs/agenda test --run</automated>
  </verify>
  <done>Suite lobby, probidad, agenda verdes; archivos contienen "from-r2" / "R2Store" / "rawBindings" / "consultados" según corresponde; [skip] log presente en lobby cli.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → fuentes gubernamentales | Modo --from-r2 debe garantizar CERO fetches a senado.cl, camara.cl, leylobby.gob.cl |
| CLI → R2 | Credenciales en env; nunca en logs ni en el path del objeto R2 |
| probidad assertion → GH Actions exit | Assertion rediseñada: consultados>0 es el gate de salud, no declaraciones>0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering | --from-r2 mode | mitigate | En modo --from-r2, el fetch de fuentes gubernamentales NO se ejecuta — el test de spy verifica 0 llamadas a hosts externos |
| T-57-04 | Information Disclosure | R2 credentials en WARN log | mitigate | El WARN log imprime solo "R2 no configurado" — no el valor de las keys (que son vacías/null cuando no configurado) |
| T-57-05 | Denial of Service | probidad-weekly assert con 0 declaraciones | mitigate | Assertion cambiada a consultados>0 — una semana sin novedades ya no causa exit 1 falso positivo |
| T-57-SC | Tampering | npm installs | accept | No se instalan paquetes nuevos — todo es de @obs/ingest ya presente en workspace |
</threat_model>

<verification>
Suite por paquete:
```bash
pnpm --filter @obs/tramitacion test --run
pnpm --filter @obs/lobby test --run
pnpm --filter @obs/probidad test --run
pnpm --filter @obs/agenda test --run
```

Greps de verificación de contratos:
```bash
# [skip] log presente en tramitacion y lobby:
grep -n "skip.*sin novedades" packages/tramitacion/src/ingest-cli.ts packages/lobby/src/ingest-cli.ts

# WARN R2 no configurado en los 3 CLIs:
grep -n "WARN.*R2 no configurado" packages/tramitacion/src/ingest-cli.ts packages/lobby/src/ingest-cli.ts packages/agenda/src/run-agenda-prod-cli.ts

# rawBindings log en probidad:
grep -n "rawBindings" packages/probidad/src/run-probidad-todos.ts

# consultados en probidad cli:
grep -n "consultados" packages/probidad/src/run-probidad-todos-cli.ts

# from-r2 en tramitacion y lobby:
grep -n "from-r2\|fromR2" packages/tramitacion/src/ingest-cli.ts packages/lobby/src/ingest-cli.ts
```

Suite completa + typecheck después de Wave 2:
```bash
pnpm -w test && pnpm -w typecheck
```
</verification>

<success_criteria>
- tramitacion/ingest-cli: Etapa 1 R2 + hash-check + --from-r2 + WARN R2 gate.
- lobby/ingest-cli: R2Store import + Etapa 1 R2 + --from-r2 + WARN (G10 cerrado).
- agenda/run-agenda-prod-cli: WARN loud cuando R2 ausente.
- probidad/run-probidad-todos: rawBindings log por parlamentario.
- probidad/run-probidad-todos-cli: consultados field + log [ok] consultados=N.
- Suite de todos los paquetes tocados verde.
- `pnpm -w typecheck` sin errores nuevos.
- Grep "[skip] sin novedades" encuentra matches en tramitacion y lobby.
</success_criteria>

<output>
Crear `.planning/phases/57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes/57-03-SUMMARY.md` cuando termine.
</output>
