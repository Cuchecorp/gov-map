---
phase: 57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/src/writer-supabase.ts
autonomous: true
requirements:
  - CRON-04

must_haves:
  truths:
    - "upsertEventos con un lote que contiene el mismo evento dos veces no lanza ON CONFLICT"
    - "El batch enviado a Supabase tiene exactamente 1 fila cuando el lote de entrada tiene 2 filas idénticas"
    - "El comportamiento de upsertVotos (ya correcto) no regresiona"
  artifacts:
    - path: "packages/tramitacion/src/writer-supabase.ts"
      provides: "G4 fix: dedupePorClave aplicada en upsertEventos (espejo de upsertVotos)"
      contains: "dedupePorClave"
    - path: "packages/tramitacion/src/writer-supabase.test.ts"
      provides: "Test reproductor G4 que confirma el fix"
      contains: "G4"
  key_links:
    - from: "packages/tramitacion/src/ingest-run.ts"
      to: "packages/tramitacion/src/writer-supabase.ts"
      via: "upsertEventos call en línea 263 (fusionarTimeline puede producir duplicados)"
      pattern: "upsertEventos"
---

<objective>
Corregir el bug CRITICAL G4 (leyes-weekly NO CORRE desde 2026-06-26): `upsertEventos` en
`writer-supabase.ts` envía el lote sin deduplicar, causando `ON CONFLICT DO UPDATE command cannot
affect row a second time` cuando `fusionarTimeline` produce eventos con la misma clave natural en
el mismo batch.

Purpose: Este fix desbloquea leyes-weekly inmediatamente — el workflow tiene sus 2 secrets en
orden; solo el bug G4 lo impide correr. Es independiente de Plan 01 (no toca r2-store) y puede
ejecutarse en paralelo en Wave 1.

Output: `dedupePorClave` aplicada en `upsertEventos` (la función ya existe en el mismo archivo —
es una inserción de 3 líneas); test reproductor que falla SIN el fix y pasa CON él.
</objective>

<execution_context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\phases\57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes\57-VALIDATION.md
</execution_context>

<context>
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\PROJECT.md
@C:\Users\Carlo\OneDrive - pjud.cl\Documentos\GitHub\Observatorio\.planning\ROADMAP.md

<interfaces>
<!-- Extraído de packages/tramitacion/src/writer-supabase.ts (leer antes de editar) -->

Función ya presente en el archivo (lines 43–47 per PATTERNS.md — NO reimplementar):
```typescript
function dedupePorClave<T>(arr: T[], key: (v: T) => string): T[] {
  const m = new Map<string, T>();
  for (const v of arr) m.set(key(v), v);
  return [...m.values()];
}
```

Analog correcto: upsertVotos (lines 85–88) — exactamente esto hay que copiar para upsertEventos:
```typescript
const deduped = dedupePorClave(
  filas,
  (v) => `${v.votacion_id} ${v.fuente_voter_id}`,
);
```

upsertEventos actual (línea 102) — el bug está en el for loop que usa `eventos` sin deduplicar:
```typescript
async upsertEventos(eventos: TramitacionEvento[]): Promise<void> {
  if (eventos.length === 0) return;
  for (const lote of chunk(eventos, CHUNK)) { // <-- BUG: chunk(eventos) sin deduplicar
    const { error } = await this.client
      .from("tramitacion_evento")
      .upsert(lote, {
        onConflict: "boletin,fecha,camara,tipo,descripcion",
        ignoreDuplicates: false,
      });
    if (error) throw new Error(`upsert tramitacion_evento falló: ${error.message}`);
  }
}
```

Clave natural en writer.ts:74: `[e.boletin, e.fecha, e.camara, e.tipo, e.descripcion].join(" ")`
(para el Map interno del dedupe usar "\x00" como separador — Pitfall 3 de RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G4 — Test reproductor de ON CONFLICT en upsertEventos</name>
  <files>packages/tramitacion/src/writer-supabase.test.ts</files>

  <read_first>
  - `packages/tramitacion/src/writer-supabase.ts` completo — para entender la interfaz de
    `SupabaseTramitacionWriter`, sus dependencias (constructor params), y el tipo `TramitacionEvento`.
  - PATTERNS.md sección "writer-supabase.ts — G4 dedupe fix" — template del test reproductor
    con `mockClient` que captura los arrays pasados a `upsert`.
  - Verificar si ya existe `packages/tramitacion/src/writer-supabase.test.ts`; si existe, extenderlo;
    si no, crearlo.
  </read_first>

  <behavior>
  Test G4: dado un lote de entrada con el mismo `TramitacionEvento` duplicado dos veces,
  `upsertEventos` llama a `.upsert()` con exactamente 1 fila (después del fix).
  ANTES del fix (escribir el test primero — TDD), el test debe FALLAR porque
  Postgres respondería con ON CONFLICT; el mock permite capturar el array antes de que
  "falle en producción" y asertarlo en length.
  </behavior>

  <action>
  Crear o extender `packages/tramitacion/src/writer-supabase.test.ts`:

  1. Construir un `mockClient` inline (sin imports de Supabase reales):
     ```
     mockClient = {
       from: () => ({
         upsert: (rows) => { capturedUpserts.push(rows); return Promise.resolve({ error: null }); }
       })
     }
     ```
  2. Instanciar `SupabaseTramitacionWriter` (o el nombre real de la clase — verificar en el archivo)
     pasando el mockClient como dependencia.
  3. Crear `ev: TramitacionEvento` con todos los campos requeridos por el tipo.
  4. Llamar `await writer.upsertEventos([ev, ev])` (mismo evento duplicado).
  5. Asertir: `expect(capturedUpserts[0]).toHaveLength(1)` — deduplicado a 1 fila.

  Este test FALLA antes del fix de Task 2 (el mock recibe 2 filas) y PASA después.
  Ejecutar la suite para confirmar que el test falla ANTES de Task 2:
  `pnpm --filter @obs/tramitacion test --run -- writer-supabase` debe mostrar 1 failing test.
  </action>

  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run -- writer-supabase 2>&1 | grep -E "FAIL|failed|1 test"</automated>
  </verify>
  <done>Test G4 creado y FALLA confirmado (rojo — esperado antes del fix).</done>
</task>

<task type="auto">
  <name>Task 2: G4 — Aplicar dedupePorClave en upsertEventos (fix de una línea)</name>
  <files>packages/tramitacion/src/writer-supabase.ts</files>

  <read_first>
  - `packages/tramitacion/src/writer-supabase.ts` — localizar `upsertEventos` (línea ~102) y
    `dedupePorClave` (línea ~43). Confirmar que `dedupePorClave` ya existe; NO reimplementar.
  - PATTERNS.md sección "Fix for upsertEventos" — el bloque exacto a copiar, incluyendo el
    comentario `// G4: de-duplicar por clave natural` y el separador `\x00`.
  - RESEARCH.md "Pitfall 3: dedupePorClave en writer-supabase.ts — separador de clave" para
    entender por qué usar `\x00` y no espacio.
  </read_first>

  <action>
  Editar `packages/tramitacion/src/writer-supabase.ts`:

  En `upsertEventos`, añadir la llamada a `dedupePorClave` ANTES del `for (const lote of chunk(...))`,
  y cambiar `chunk(eventos, CHUNK)` por `chunk(deduped, CHUNK)`:

  ```
  const deduped = dedupePorClave(
    eventos,
    (e) => [e.boletin, e.fecha, e.camara, e.tipo, e.descripcion].join("\x00"),
  );
  // cambiar chunk(eventos, ...) → chunk(deduped, ...)
  ```

  Añadir comentario: `// G4: de-duplicar por clave natural antes del upsert (espejo de upsertVotos:85-88).`

  Usar `\x00` como separador (NUL — no ambiguo con contenido de campo, diferente del separador
  espacio en `eventoKey` de writer.ts:74 — ver Pitfall 3 RESEARCH.md).

  NO modificar nada más en el archivo — el fix es exactamente este cambio.
  </action>

  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run -- writer-supabase</automated>
  </verify>
  <done>Test G4 pasa (verde); suite @obs/tramitacion completa verde; typecheck sin nuevos errores en tramitacion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tramitacion → Supabase | Datos de tramitación vienen de fuentes públicas (senado.cl, camara.cl); el upsert idempotente es la única escritura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-G4-01 | Tampering | upsertEventos batch | mitigate | dedupePorClave antes del upsert previene ON CONFLICT que corrompería la corrida y haría que leyes-weekly quede en estado unknown |
| T-57-SC | Tampering | npm installs | accept | No se instalan paquetes nuevos — fix puro en código existente |
</threat_model>

<verification>
Suite tramitacion completa + typecheck:
```bash
pnpm --filter @obs/tramitacion test --run
pnpm -w typecheck
```
Test G4 específico:
```bash
pnpm --filter @obs/tramitacion test --run -- -t "G4"
```
El test debe pasar (verde). Si el archivo test no tenía test de upsertVotos antes, agregar uno de regresión también es aceptable pero opcional.
</verification>

<success_criteria>
- `upsertEventos([ev, ev])` envía exactamente 1 fila a Supabase (verificado por test con mock).
- Test G4 reproductor pasa.
- Suite `@obs/tramitacion` verde completa.
- `pnpm -w typecheck` sin errores nuevos.
- `leyes-weekly` está desbloqueado para corrida en Wave 3 (el único impedimento era G4).
</success_criteria>

<output>
Crear `.planning/phases/57-cron-fix-hardening-dos-etapas-hash-check-crons-verdes/57-02-SUMMARY.md` cuando termine.
</output>
