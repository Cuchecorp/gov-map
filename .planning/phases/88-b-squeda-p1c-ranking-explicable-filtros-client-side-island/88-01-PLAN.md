---
phase: 88-b-squeda-p1c-ranking-explicable-filtros-client-side-island
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/estado-bucket.ts
  - app/lib/estado-bucket.test.ts
  - app/lib/types.ts
autonomous: true
requirements: [FILT-03]

must_haves:
  truths:
    - "Un texto libre de estado (proyecto.estado/etapa) se mapea a un bucket enum determinista"
    - "Texto no mapeado y NULL caen a `sin_dato` (nunca silenciosamente a un bucket sustantivo)"
    - "Una fecha ISO se convierte a año numérico; una fecha no parseable devuelve null (nunca un año fabricado)"
    - "El tipo de fila del slice serializado existe y es importable por page.tsx y el island"
  artifacts:
    - path: "app/lib/estado-bucket.ts"
      provides: "estadoBucket() table-driven + deriveAnio() puro"
      contains: "export type EstadoBucket"
    - path: "app/lib/estado-bucket.test.ts"
      provides: "cobertura de los 6 buckets + sin_dato + deriveAnio honesto"
    - path: "app/lib/types.ts"
      provides: "interface BuscarSliceRow con partido opcional (BIO-03 forward-compat)"
      contains: "BuscarSliceRow"
  key_links:
    - from: "app/lib/estado-bucket.ts"
      to: "app/lib/estado-bucket.test.ts"
      via: "import estadoBucket / deriveAnio"
      pattern: "estadoBucket|deriveAnio"
---

<objective>
Crear la capa pura y foundational de la fase 88: el normalizador de estado texto-libre→bucket enum (`estado-bucket.ts`), el helper `deriveAnio` (ISO→año honesto), y el tipo compartido `BuscarSliceRow` (la forma serializada que el server component pasará al island). Todo puro, server-side, sin I/O, sin JSX — 100% unit-testable.

Purpose: FILT-03 exige un normalizador de estado "definido, testeado y reusable" (ficha de proyecto y /agenda lo consumirán después) — no acoplado al island. Es el contrato que los planes 02 (island) y 03 (page wiring) consumen. Se planifica primero (interface-first) para que los downstream reciban el contrato, no lo exploren.

Output: `app/lib/estado-bucket.ts`, `app/lib/estado-bucket.test.ts`, tipo `BuscarSliceRow` en `app/lib/types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-CONTEXT.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-UI-SPEC.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-PATTERNS.md

<interfaces>
<!-- Contratos exactos extraídos del repo. El executor NO debe explorar para hallarlos. -->

De app/lib/types.ts (líneas 13-27) — la fila que el server component hidrata hoy:
```typescript
export interface ProyectoRow {
  boletin: string;
  boletin_num: string;
  titulo: string;
  iniciativa: string | null;      // "Mensaje" | "Moción" u otro texto libre
  camara_origen: string | null;
  autores: string[] | null;
  materia: string | null;
  estado: string | null;          // TEXTO LIBRE → normalizar a bucket
  etapa: string | null;           // TEXTO LIBRE → fallback si estado es null
  subetapa: string | null;
  origen: string;
  fecha_captura: string;          // ⚠ fecha de CAPTURA — NO es el año del proyecto
  enlace: string;
}
```

De app/components/etapa-badge.tsx (líneas 16-44) — el patrón table-driven a espejar (retorna bucket, NO className):
```typescript
function resolveVariant(value: string | null): EtapaVariant {
  const v = (value ?? "").toLowerCase();
  if (v.includes("promulg") || v.includes("ley")) { /* … */ }
  if (v.includes("rechaz")) { /* … */ }
  if (v.includes("archiv")) { /* … */ }
  if (v.includes("tramit")) { /* … */ }
  if (value && value.trim().length > 0) { /* fall-through non-empty */ }
  return { label: "Estado desconocido", className: "bg-slate-100 text-slate-500" }; // ← honest default
}
```
BUG LATENTE A EVITAR (PATTERNS §estado-bucket): EtapaBadge testea `ley` ANTES que `tramit`. Un estado compuesto como `"En tramitación (segundo trámite) — ley en …"` bucketea mal si el orden es descuidado. Ordenar la tabla explícitamente para que `rechazado`/`archivado`/`retirado`/`publicado_ley` ganen sobre el genérico `en_tramitacion` cuando el texto es compuesto.

Enum LOCKED (UI-SPEC §Estado normalizer):
```typescript
export type EstadoBucket =
  | "en_tramitacion" | "publicado_ley" | "archivado"
  | "rechazado" | "retirado" | "sin_dato";
// Labels display (deben pasar el linter anti-insinuación):
// En tramitación · Publicado / Ley · Archivado · Rechazado · Retirado · Sin dato
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Censo read-only de valores de estado en PROD + tabla de buckets</name>
  <read_first>
    - app/components/etapa-badge.tsx:15-44 (patrón table-driven + honest default a copiar)
    - app/lib/parlamentario-resumen-conteos.ts:167-190 (pureza + disciplina año-from-ISO con exclusión honesta ^\d{4}$)
    - .planning/phases/88-*/88-PATTERNS.md §estado-bucket (regla order-matters)
  </read_first>
  <behavior>
    - estadoBucket("Publicada como ley") → "publicado_ley"
    - estadoBucket("En tramitación (segundo trámite)") → "en_tramitacion"
    - estadoBucket("Archivado") → "archivado"
    - estadoBucket("Rechazado") → "rechazado"
    - estadoBucket("Retirado") → "retirado"
    - estadoBucket("En tramitación (…) — ley en …") → NO bucketea a publicado_ley por el token "ley" cuando el texto es compuesto de tramitación (order-matters: en_tramitacion gana solo si NINGÚN patrón terminal-de-vida aplica; documentar la regla de precedencia derivada del censo)
    - estadoBucket(null) → "sin_dato"
    - estadoBucket("") → "sin_dato"
    - estadoBucket("cualquier cosa no mapeada") → "sin_dato"
  </behavior>
  <action>
    Antes de escribir la tabla, censar los valores DISTINTOS reales de PROD (read-only, patrón `psql -tA` de los runbooks del repo): `SELECT DISTINCT estado FROM proyecto` y `SELECT DISTINCT etapa FROM proyecto` (SELECT-only, cero escritura). Documentar el censo como una lista de comentario en `estado-bucket.ts` (los valores literales observados → su bucket) para que la tabla tenga procedencia, no categorías inventadas. Si el entorno no da acceso read-only a PROD, degradar honestamente: derivar la tabla de los ejemplos del corpus en tests y anotar en el JSDoc que el censo quedó pendiente de confirmación LIVE (nunca fabricar valores).

    Crear `app/lib/estado-bucket.ts`. Exportar `type EstadoBucket` (los 6 valores LOCKED). Exportar función pura `estadoBucket(value: string | null): EstadoBucket` table-driven: minúsculas una vez, tabla ordenada `Array<{ patrones: string[]; bucket: EstadoBucket }>` con first-match-wins, fallback explícito `sin_dato` (JAMÁS folding silencioso a un bucket sustantivo, PATTERNS §Table-driven). ORDEN: los buckets terminales-de-vida (`publicado_ley`, `rechazado`, `archivado`, `retirado`) deben poder ganar sobre el genérico `en_tramitacion` cuando el texto compuesto lo justifique — encodear la precedencia explícitamente y comentarla, evitando el bug latente de EtapaBadge (`ley` antes que `tramit`). Exportar también un mapa `ETIQUETA_BUCKET: Record<EstadoBucket, string>` con los labels display LOCKED (En tramitación · Publicado / Ley · Archivado · Rechazado · Retirado · Sin dato) — factuales, sin términos del linter anti-insinuación.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run lib/estado-bucket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/estado-bucket.ts` exporta `EstadoBucket` (6 valores), `estadoBucket()`, `ETIQUETA_BUCKET`
    - La tabla lleva un comentario-censo con los valores DISTINCT reales (o nota honesta de censo pendiente)
    - Cero patrón folds unmapped/NULL a un bucket sustantivo (verificado en test)
    - `pnpm exec vitest run lib/estado-bucket.test.ts` verde con ≥8 casos incluyendo los 6 buckets + sin_dato + el caso compuesto order-matters
  </acceptance_criteria>
  <done>estadoBucket() determinista, table-driven, honest-default sin_dato, con censo documentado; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveAnio (ISO→año honesto) + tipo BuscarSliceRow</name>
  <read_first>
    - app/lib/parlamentario-resumen-conteos.ts:167-190 (año-from-ISO con exclusión ^\d{4}$)
    - app/lib/types.ts:13-27, 90-98 (estilo de la casa: ProyectoRow snake_case, MatchProyectoRow; JSDoc explica cada nullable)
    - .planning/phases/88-*/88-UI-SPEC.md §Year facet (LOCKED: nunca de fecha_captura, nunca del sufijo del boletín)
  </read_first>
  <behavior>
    - deriveAnio("2023-05-14T00:00:00Z") → 2023
    - deriveAnio("2019-01-01") → 2019
    - deriveAnio(null) → null
    - deriveAnio("") → null
    - deriveAnio("no-es-fecha") → null (nunca un año fabricado)
  </behavior>
  <action>
    En `app/lib/estado-bucket.ts` añadir `export function deriveAnio(fechaIso: string | null): number | null`: extrae el año de una ISO con la disciplina honesta de `parlamentario-resumen-conteos.ts` (regex `^\d{4}` sobre el año, EXCLUYE lo no parseable devolviendo `null` — nunca fabrica un año). JSDoc: prohíbe explícitamente derivar año de `fecha_captura` (fecha de captura) o del sufijo del boletín (código de materia) — UI-SPEC §Year facet LOCKED.

    En `app/lib/types.ts`, junto a `ProyectoRow`/`MatchProyectoRow`, añadir `export interface BuscarSliceRow`: la forma serializada camelCase que el server pasa al island: `{ boletin: string; titulo: string; anio: number | null; iniciativa: "Mensaje" | "Moción" | null; estadoBucket: EstadoBucket; camaraOrigen: string | null; fecha: string | null; partido?: string | null }`. `partido?` OPCIONAL para forward-compat BIO-03 (P2) — cuando ausente, el island simplemente no renderiza esa faceta (sin placeholder). Importar `EstadoBucket` desde `@/lib/estado-bucket`. JSDoc por cada nullable explicando POR QUÉ puede ser null (convención del archivo): `anio` null = sin evento Ingreso derivable; `iniciativa` null = texto de fuente no reconocido como Mensaje/Moción; `fecha` null = sin fecha derivable. NO fabricar valores.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run lib/estado-bucket.test.ts; pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `deriveAnio` devuelve null (no un año fabricado) para input no parseable, verificado en test
    - `BuscarSliceRow` existe en types.ts con `partido?` opcional y `estadoBucket: EstadoBucket`
    - JSDoc de `deriveAnio` prohíbe explícitamente fecha_captura y sufijo de boletín
    - `pnpm exec tsc --noEmit` sin errores
    - `estado-bucket.test.ts` cubre deriveAnio con ≥5 casos (ISO válida, sin-tz, null, vacío, basura)
  </acceptance_criteria>
  <done>deriveAnio honesto (null nunca fabricado) + BuscarSliceRow importable con partido opcional; tsc limpio; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → PROD (read-only) | El censo de estados corre un SELECT DISTINCT contra PROD; solo lectura, cero escritura, no cruza a runtime del sitio |
| ninguna otra | Este plan es lógica pura sin red, sin secrets, sin PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | estado-bucket.ts silencia unmapped a bucket sustantivo | mitigate | fallback explícito `sin_dato` + test que asserta que texto no mapeado NO cae en bucket sustantivo |
| T-88-02 | Information Disclosure | censo SELECT sobre PROD expone datos | accept | `proyecto.estado`/`etapa` son campos públicos no-PII (no en PII_TABLES del lockdown-guard); SELECT-only |
| T-88-03 | Tampering | año fabricado desde fecha_captura/sufijo boletín (dato falso presentado como año del proyecto) | mitigate | `deriveAnio` devuelve null para no-parseable; JSDoc prohíbe ambas fuentes falsas; test lo fija |
| T-88-SC | Tampering | npm/pip/cargo installs | mitigate | CERO instalaciones nuevas en este plan (solo archivos TS puros); sin superficie de supply-chain |
</threat_model>

<verification>
- `pnpm exec vitest run lib/estado-bucket.test.ts` verde
- `pnpm exec tsc --noEmit` limpio
- `estado-bucket.ts` no importa React ni Supabase (lib pura server-side)
- `BuscarSliceRow` con `partido?` opcional presente en types.ts
</verification>

<success_criteria>
- FILT-03 satisfecho: normalizador de estado definido, testeado, reusable, con censo documentado y honest-default sin_dato
- Contrato `BuscarSliceRow` disponible para los planes 02/03
- deriveAnio honesto (null nunca fabricado)
</success_criteria>

<output>
Create `.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-01-SUMMARY.md` when done
</output>
