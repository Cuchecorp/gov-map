---
phase: 03-tabla-maestra-parlamentario-identidad-determinista
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_parlamentario.sql
  - supabase/tests/0004_parlamentario.test.sql
autonomous: true
requirements: [ID-01]

must_haves:
  truths:
    - "Existen las tablas parlamentario y parlamentario_alias en el Supabase local tras la migración"
    - "parlamentario lleva provenance inline (origen, fecha_captura, enlace) y una columna estado con check (confirmado|probable|no_confirmado)"
    - "RLS deny-by-default: ambas tablas con RLS habilitada y sin policies para anon (rut nunca legible por anon)"
    - "Hay clave natural por cámara para upsert idempotente (parlid_senado / id_diputado_camara)"
  artifacts:
    - path: "supabase/migrations/0005_parlamentario.sql"
      provides: "DDL parlamentario + parlamentario_alias + RLS"
      contains: "create table parlamentario"
    - path: "supabase/tests/0004_parlamentario.test.sql"
      provides: "pgTAP de columnas, check de estado, RLS deny-by-default, unicidad natural"
      contains: "select plan("
  key_links:
    - from: "supabase/tests/0004_parlamentario.test.sql"
      to: "supabase/migrations/0005_parlamentario.sql"
      via: "verifica el DDL aplicado vía supabase test db"
      pattern: "has_table.*parlamentario"
---

<objective>
Crea la migración 0005 que materializa la tabla maestra `parlamentario` + `parlamentario_alias` en el Supabase LOCAL, con provenance inline, columna `estado`, claves naturales para upsert idempotente, y RLS deny-by-default; verificada por pgTAP espejando el patrón de Fase 1.

Purpose: ID-01 exige una maestra sembrada con identidades confirmadas por revisión humana (no auto-generadas). La columna `estado` modela la compuerta de promoción (seeder carga el lote, el operador lo promueve). RLS deny-by-default + `rut` interno cierra la exposición de datos personales (Ley 21.719, V4/V8).
Output: `supabase/migrations/0005_parlamentario.sql` + `supabase/tests/0004_parlamentario.test.sql` verdes con `supabase test db`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-CONTEXT.md
@supabase/migrations/0002_control_tables.sql
@supabase/tests/0001_control_plane.test.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0005 — parlamentario + parlamentario_alias + RLS deny-by-default</name>
  <files>supabase/migrations/0005_parlamentario.sql</files>
  <read_first>supabase/migrations/0002_control_tables.sql</read_first>
  <action>
    Crea `supabase/migrations/0005_parlamentario.sql` con la tabla `parlamentario` siguiendo el modelo exacto de 03-CONTEXT: `id text primary key` (interno estable, p.ej. P00001), `nombre_normalizado text not null`, `nombres text`, `apellido_paterno text`, `apellido_materno text`, `camara text not null check (camara in ('diputados','senado'))`, `periodo text not null`, `region text`, `distrito text` (NULLABLE — la Cámara no lo trae, Pitfall 4), `circunscripcion text` (nullable), `partido text`, `rut text` (NULLABLE, uso interno — los catálogos no lo traen; NO NOT NULL o la siembra revienta, Pitfall 4), `parlid_senado text` (nullable), `id_diputado_camara text` (nullable), `estado text not null default 'no_confirmado' check (estado in ('confirmado','probable','no_confirmado'))`, `email text`, y provenance inline `origen text not null`, `fecha_captura timestamptz not null default now()`, `enlace text not null`. Añade claves naturales para upsert idempotente: `unique (parlid_senado)` parcial `where parlid_senado is not null` y `unique (id_diputado_camara)` parcial `where id_diputado_camara is not null` (índices únicos parciales, NO columnas not null). Crea `parlamentario_alias` con `id bigint generated always as identity primary key`, `parlamentario_id text not null references parlamentario(id) on delete cascade`, `alias text not null`, `origen text`, y `unique (parlamentario_id, alias)`. Habilita RLS deny-by-default en AMBAS tablas con `alter table ... enable row level security;` SIN crear policies (espejo exacto del comentario y patrón de 0002 — anon nunca las lee; solo service role). Comenta el default `estado='no_confirmado'`: la promoción a `confirmado` es la compuerta de revisión humana (ID-01), no se auto-marca.
  </action>
  <verify>
    <automated>supabase db reset</automated>
  </verify>
  <done>`supabase db reset` aplica 0001→0005 sin error; tablas `parlamentario` y `parlamentario_alias` creadas con RLS habilitada y sin policies.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0004 — columnas, check de estado, RLS deny-by-default, unicidad natural</name>
  <files>supabase/tests/0004_parlamentario.test.sql</files>
  <read_first>supabase/tests/0001_control_plane.test.sql, supabase/migrations/0005_parlamentario.sql</read_first>
  <action>
    Crea `supabase/tests/0004_parlamentario.test.sql` espejando el patrón de `0001_control_plane.test.sql` (begin / select plan(N) / asserts / finish / rollback). Asserts: `has_table` para `parlamentario` y `parlamentario_alias`; `has_column` para las columnas críticas (`nombre_normalizado`, `camara`, `periodo`, `estado`, `rut`, `parlid_senado`, `id_diputado_camara`, `origen`, `fecha_captura`, `enlace`); `col_is_null('public','parlamentario','rut',...)` y `col_is_null` para `distrito` (nullable per Pitfall 4); verifica el check de `estado` insertando un valor inválido y esperando excepción (usa `throws_ok` con un insert de `estado='xxx'`) y un valor válido con `lives_ok`; verifica RLS habilitada en ambas tablas (`relrowsecurity = true`, como el assert de count en 0001) y `is_empty` de `pg_policy` para ambas (deny-by-default); verifica la unicidad natural (`throws_ok` al insertar dos filas con el mismo `parlid_senado`). Ajusta `plan(N)` al número real de asserts.
  </action>
  <verify>
    <automated>supabase test db</automated>
  </verify>
  <done>`supabase test db` corre 0001→0004 todos verdes; el pgTAP de parlamentario confirma columnas, nullables, check de estado, RLS deny-by-default y unicidad natural.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (frontend) → Postgres | El cliente anónimo nunca debe leer `rut` ni datos personales de la maestra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | tabla parlamentario (rut/email) | mitigate | RLS deny-by-default (enable sin policies) en ambas tablas; anon no lee nada. `rut` nullable interno. pgTAP verifica RLS + ausencia de policies. |
| T-03-05 | Tampering | columna estado | mitigate | `check (estado in ('confirmado','probable','no_confirmado'))` + default `no_confirmado`; nada se marca confirmado por DDL — la promoción es revisión humana (ID-01). pgTAP throws_ok valida el check. |
| T-03-06 | Tampering | idempotencia de siembra | mitigate | Índices únicos parciales por clave natural (parlid_senado / id_diputado_camara) permiten upsert idempotente sin duplicados; pgTAP lo verifica. |
</threat_model>

<verification>
- `supabase db reset` aplica las 5 migraciones sin error.
- `supabase test db` verde en 0001→0004.
- RLS habilitada y sin policies en `parlamentario` + `parlamentario_alias`.
</verification>

<success_criteria>
- [ ] `parlamentario` + `parlamentario_alias` existen con el modelo de 03-CONTEXT.
- [ ] `rut`, `distrito`, `circunscripcion` nullable (no revienta la siembra de Cámara).
- [ ] `estado` con check + default no_confirmado (compuerta de revisión humana).
- [ ] RLS deny-by-default verificada por pgTAP.
- [ ] Claves naturales únicas parciales para upsert idempotente.
</success_criteria>

<output>
Create `.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-02-SUMMARY.md` when done
</output>
