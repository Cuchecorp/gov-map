---
phase: 03-tabla-maestra-parlamentario-identidad-determinista
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/identity/package.json
  - packages/identity/tsconfig.json
  - packages/identity/vitest.config.ts
  - packages/core/src/parlamentario.ts
  - packages/core/src/nombre.ts
  - packages/core/src/index.ts
  - packages/identity/src/deterministic.ts
  - packages/identity/src/deterministic.test.ts
  - packages/core/src/nombre.test.ts
  - tsconfig.json
autonomous: true
requirements: [ID-02]

must_haves:
  truths:
    - "normalizarNombre produce el MISMO nombre_normalizado desde campos de catálogo y desde el formato de votación 'Apellido P., Nombre'"
    - "'Muñoz' y 'Munoz' colapsan al mismo token (ñ→n folding consistente)"
    - "matchDeterminista devuelve confirmado solo por RUT exacto o nombre único en (cámara, periodo); homónimo o sin candidato → no_confirmado"
    - "El matcher es fail-closed: ante cualquier ambigüedad nunca confirma"
  artifacts:
    - path: "packages/core/src/nombre.ts"
      provides: "normalizarNombre() función pura"
      exports: ["normalizarNombre"]
      min_lines: 25
    - path: "packages/identity/src/deterministic.ts"
      provides: "matchDeterminista() función pura fail-closed"
      exports: ["matchDeterminista"]
      min_lines: 25
    - path: "packages/core/src/parlamentario.ts"
      provides: "Tipos Parlamentario + zod schema del shape parseado"
      exports: ["Parlamentario", "ParlamentarioSeedSchema"]
  key_links:
    - from: "packages/identity/src/deterministic.ts"
      to: "packages/core/src/parlamentario.ts"
      via: "import type Parlamentario"
      pattern: "import.*Parlamentario.*@obs/core"
    - from: "packages/identity/src/deterministic.ts"
      to: "packages/core/src/nombre.ts"
      via: "compara contra nombre_normalizado producido por normalizarNombre"
      pattern: "nombre_normalizado"
---

<objective>
Crea el subsistema de identidad como lógica PURA y testeada: el tipo de dominio `Parlamentario` + zod (en `@obs/core`), la función `normalizarNombre` (NFD strip, ñ→n consistente, partículas, convergencia catálogo↔formato-votación), y el matcher determinista `matchDeterminista` (fail-closed, sin LLM). Es el corazón de la rebanada vertical: el componente que decide `estado` sin tocar red ni DB.

Purpose: ID-02 exige reconciliación determinista (RUT exacto / nombre único en cámara+periodo) SIN invocar LLM, con preferencia asimétrica por el falso negativo (riesgo existencial #1). Toda la novedad de la fase es esta lógica pura; la infraestructura de fetch ya existe en `@obs/ingest`.
Output: Paquete `@obs/identity` dado de alta en el workspace + `normalizarNombre` y `matchDeterminista` con tests golden verdes (homónimos, ñ, formato votación), sin red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-CONTEXT.md

<interfaces>
<!-- Patrón de scaffold de paquete del monorepo (de @obs/ingest, Fase 1). -->
<!-- pnpm-workspace.yaml ya incluye "packages/*" → un paquete nuevo bajo packages/ se autoregistra; SOLO hay que añadir su referencia a tsconfig.json raíz. -->

packages/ingest/package.json (forma a imitar para packages/identity/package.json):
  name "@obs/identity", private, type module, main/types "src/index.ts",
  scripts: { test: "vitest run", typecheck: "tsc -b" },
  dependencies: { "@obs/core": "workspace:*", "fast-xml-parser": "5", "zod": "..." },
  devDependencies: { vitest: "^3.0.0" }

Provenance (de @obs/core, ya existe — NO redefinir):
  export interface Provenance { source: string; fetchedAt: string; sourceUrl: string; snapshotRef?: string }
  export function makeProvenance(source: string, sourceUrl: string): Provenance

@obs/core/src/index.ts hoy hace: export * from "./provenance"; export * from "./domain";
  → este plan AÑADE: export * from "./parlamentario"; export * from "./nombre";

Diseño de referencia (de 03-RESEARCH.md, Code Examples) — NO copiar literal, es guía:
  - normalizarNombre: fold = s.normalize("NFD").replace(/\p{Diacritic}/gu,"").toLowerCase()
    .replace(/[.,]/g," ").replace(/\s+/g," ").trim(); PARTICULAS = {de,del,la,las,los,y,da,do}
    fuera de los tokens de blocking; nombre_normalizado = [...tokens].sort().join(" ").
  - matchDeterminista(mention, maestra): RUT exacto único → confirmado; nombre_normalizado
    único en (camara,periodo) → confirmado; else → no_confirmado con razon homonimo|sin-candidato.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @obs/identity + tipos Parlamentario en @obs/core</name>
  <files>packages/identity/package.json, packages/identity/tsconfig.json, packages/identity/vitest.config.ts, packages/core/src/parlamentario.ts, packages/core/src/index.ts, tsconfig.json</files>
  <read_first>packages/ingest/package.json, packages/ingest/tsconfig.json, packages/ingest/vitest.config.ts, packages/core/src/index.ts, packages/core/src/domain.ts, tsconfig.json</read_first>
  <action>
    Da de alta el paquete `@obs/identity` espejando el scaffold de `@obs/ingest`: `package.json` (name "@obs/identity", type module, main/types "src/index.ts", scripts test="vitest run" y typecheck="tsc -b", deps `@obs/core` workspace:* + `fast-xml-parser@5` + `zod`, devDep `vitest@^3.0.0`), `tsconfig.json` (referencias a `@obs/core`; incluir `lib: ["ES2022","DOM"]` igual que ingest porque el seeder de Plan 03 usa Web APIs `fetch`/`URL`/`TextDecoder`), y `vitest.config.ts` clon del de ingest. Añade `{ "path": "./packages/identity" }` a `references` en el `tsconfig.json` raíz (pnpm-workspace.yaml ya cubre `packages/*`, no tocar). Crea `packages/core/src/parlamentario.ts` con: el tipo `Parlamentario` (campos exactos del modelo en 03-CONTEXT: `id` string interno, `nombre_normalizado`, `nombres`, `apellido_paterno`, `apellido_materno`, `camara: "diputados"|"senado"`, `periodo` string, `region` nullable, `distrito` nullable, `circunscripcion` nullable, `partido` nullable, `rut` string|null, `parlid_senado` string|null, `id_diputado_camara` string|null, `estado: "confirmado"|"probable"|"no_confirmado"`, `email` nullable, provenance: `origen`/`fecha_captura`/`enlace`), el union `EstadoIdentidad`, y un zod `ParlamentarioSeedSchema` que valida el shape de UNA fila normalizada antes del upsert (rut/distrito/circunscripcion opcionales/nullable per Pitfall 4). Exporta todo desde `packages/core/src/index.ts` añadiendo `export * from "./parlamentario"`. NO incluyas RUT falso: `rut` por defecto null (los catálogos no lo traen). NO expongas `rut` en ningún tipo "público" — es campo interno.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity typecheck && pnpm --filter @obs/core typecheck && pnpm -w typecheck</automated>
  </verify>
  <done>El paquete `@obs/identity` typechecker verde; `@obs/core` exporta `Parlamentario` + `ParlamentarioSeedSchema`; `tsconfig.json` raíz referencia `packages/identity`; `pnpm -w typecheck` exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: normalizarNombre (RED→GREEN) — golden de ñ, partículas, formato votación</name>
  <files>packages/core/src/nombre.ts, packages/core/src/nombre.test.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/parlamentario.ts, .planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md</read_first>
  <behavior>
    - "Apellido P., Nombre" (formato votación Senado) y campos estructurados {apellidoPaterno, apellidoMaterno, nombres} del mismo humano producen el MISMO nombre_normalizado.
    - "Muñoz" y "Munoz" → mismo token (ñ→n folding consistente); "Núñez"/"Nunez" idem.
    - Acentos eliminados por NFD strip; casefold a minúsculas; puntos y comas → separadores.
    - Partículas (de, del, la, las, los, y, da, do) NO entran en los tokens de blocking.
    - Apellido compuesto ("De la Fuente") se maneja sin perder el apellido relevante.
    - nombre_normalizado es estable e independiente del orden de entrada (tokens ordenados canónicamente).
  </behavior>
  <action>
    Primero escribe `packages/core/src/nombre.test.ts` (vitest) con los casos golden del bloque behavior — DEBE fallar (RED), commit `test(03-01): RED normalizarNombre`. Luego implementa `packages/core/src/nombre.ts` exportando `normalizarNombre(input: { nombres?; apellidoPaterno?; apellidoMaterno?; libre? }): { nombre_normalizado: string; tokens: string[]; alias_capturados: string[] }` siguiendo el diseño de 03-RESEARCH (fold NFD+Unicode property escapes, PARTICULAS fuera de tokens, tokens ordenados → nombre_normalizado). Captura la inicial del materno en formato votación ("Apellido P., Nombre") como caso especial y registra variantes en `alias_capturados` (per Pitfall 3). Documenta en comentario la decisión A1 (ñ→n aceptable para la CLAVE de comparación; el display usa los campos originales, NUNCA el normalizado). Exporta desde `packages/core/src/index.ts` (`export * from "./nombre"`). Commit verde `feat(03-01): normalizarNombre`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/core test --run nombre</automated>
  </verify>
  <done>Todos los casos golden verdes: catálogo y formato-votación convergen, "Muñoz"/"Munoz" colapsan, partículas excluidas. 2 commits (RED, GREEN).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: matchDeterminista fail-closed (RED→GREEN) — RUT, nombre único, homónimo</name>
  <files>packages/identity/src/deterministic.ts, packages/identity/src/deterministic.test.ts, packages/identity/src/index.ts</files>
  <read_first>packages/core/src/parlamentario.ts, packages/core/src/nombre.ts, .planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md</read_first>
  <behavior>
    - RUT exacto único en la maestra → { estado: "confirmado", metodo: "rut", id }.
    - Sin RUT pero nombre_normalizado único dentro de (camara, periodo) → { estado: "confirmado", metodo: "nombre", id }.
    - nombre_normalizado con 2+ coincidencias en (camara, periodo) → { estado: "no_confirmado", razon: "homonimo" } (NUNCA confirma).
    - Sin candidato → { estado: "no_confirmado", razon: "sin-candidato" }.
    - Match por nombre solo dentro de la MISMA cámara y periodo (cross-cámara no auto-confirma).
    - RUT presente pero 2+ coincidencias (teórico) → no confirma por RUT, cae a la rama nombre / fail-closed.
  </behavior>
  <action>
    Primero escribe `packages/identity/src/deterministic.test.ts` con los casos del behavior usando una maestra de prueba (incluye un par de homónimos del mismo (camara,periodo) y un caso cross-cámara) — DEBE fallar (RED), commit `test(03-01): RED matchDeterminista`. Luego implementa `packages/identity/src/deterministic.ts` exportando `matchDeterminista(mention: { rut?; nombreNormalizado; camara; periodo }, maestra: Parlamentario[]): Resolution` con las 3 ramas del diseño de 03-RESEARCH (RUT exacto único → confirmado; nombre único en (camara,periodo) → confirmado; else no_confirmado con razon). Incluye un helper `normRut` (strip puntos/guión, casefold del dígito verificador) para la comparación de RUT. La rama RUT se implementa aunque los catálogos no traigan RUT (aplica al cruzar con InfoProbidad en Fase 4+) — documenta esto. Es el ÚNICO escritor de `estado`; fail-closed por construcción (solo confirma con `=== 1`). Crea `packages/identity/src/index.ts` exportando `matchDeterminista` y los tipos. Commit verde `feat(03-01): matchDeterminista fail-closed`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test --run deterministic</automated>
  </verify>
  <done>Todos los casos verdes incluyendo homónimo→no_confirmado y cross-cámara no auto-confirmado; ningún path confirma con length≠1. 2 commits (RED, GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mención foránea → matcher | Un registro de votación/proyecto (Fase 5+) cruza al subsistema de identidad pidiendo un `parlamentario_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (existencial #1) | matchDeterminista | mitigate | Fail-closed: confirma SOLO si RUT exacto único o nombre único en (cámara,periodo). length>1 o 0 → no_confirmado. Test golden de homónimo lo verifica. |
| T-03-02 | Spoofing | normalizarNombre | mitigate | El normalizado es clave de comparación, no de display; ñ→n consistente en ambos lados evita falsos "no match" pero no infla falsos positivos (el matcher exige unicidad). |
| T-03-03 | Information Disclosure | tipo Parlamentario | mitigate | `rut` modelado como campo interno nullable; ningún tipo "público" lo incluye; default null (no se fabrican RUTs). |
</threat_model>

<verification>
- `pnpm --filter @obs/core test --run` y `pnpm --filter @obs/identity test --run` verdes, sin red.
- `pnpm -w typecheck` exit 0 (paquete nuevo integrado en project references).
- Casos golden: convergencia catálogo↔votación, ñ→n, homónimo→no_confirmado presentes y verdes.
</verification>

<success_criteria>
- [ ] `@obs/identity` dado de alta y typechecker verde en el workspace.
- [ ] `normalizarNombre` y `matchDeterminista` son funciones puras (sin import de red/DB) con tests golden verdes.
- [ ] El matcher es fail-closed: ningún test confirma sin unicidad exacta.
- [ ] `rut` jamás fabricado; modelado como interno nullable.
</success_criteria>

<output>
Create `.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-01-SUMMARY.md` when done
</output>
