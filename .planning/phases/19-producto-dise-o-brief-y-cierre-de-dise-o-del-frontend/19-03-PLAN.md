---
phase: 19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/SCREENS.md
autonomous: true
requirements: []   # n/a (design closure)
user_setup: []

must_haves:
  truths:
    - "Each of the 5 key screens (landing, resultados de búsqueda, ficha de proyecto, ficha de parlamentario, contraparte) has an implementation-ready spec with nothing left 'abierto' or 'por decidir'"
    - "Each screen spec names its layout (container width), components, states (per the honest-states catalogue), and the anti-insinuación invariants it must satisfy"
    - "Every screen spec makes ProvenanceBadge per datum and the identity guard explicit"
    - "MONEY-gated surfaces (contraparte, #dinero/#financiamiento) document the OFF behaviour (node/page absent) and the future ON behaviour"
    - "The GlobalHeader and the NEW /parlamentario directory are specified to implementation-ready detail"
  artifacts:
    - path: ".planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/SCREENS.md"
      provides: "Per-screen executable specs for the 5 key screens + GlobalHeader + parlamentarios directory + sobre/metodología, derived from UI-SPEC §11"
      contains: "max-w-3xl"
      min_lines: 220
  key_links:
    - from: "SCREENS.md per-screen sections"
      to: "DESIGN-SYSTEM.md tokens/components/states"
      via: "each screen references DS tokens (cream/petrol), DS components, and the 3 honest states"
      pattern: "ProvenanceBadge|mt-12|honest|estados honestos"
---

<objective>
Produce SCREENS.md — implementation-ready per-screen specs for the 5 key screens (landing, resultados de búsqueda, ficha de proyecto, ficha de parlamentario, contraparte) plus the GlobalHeader, the NEW /parlamentario directory, and the light Sobre/Metodología surface. Each spec is derived from §11 of the approved UI-SPEC and consolidated into a self-contained, executable contract: layout, components, states, anti-insinuación invariants, traceability. Nothing is left "abierto" or "por decidir".

Purpose: an implementation phase can build each screen directly from this file + DESIGN-SYSTEM.md, no interpretation.
Output: SCREENS.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/BRIEF.md

This is a DESIGN/PRODUCT closure phase. Deliverables are Markdown design artifacts ONLY.
HARD CONSTRAINTS:
- Do NOT touch any file under app/. SCREENS.md is a spec; the throwaway landing HTML mockup is delivered separately in Plan 04.
- Do NOT invent features/data. Specs exploit only shipped data + the NEW spec-only components catalogued in DESIGN-SYSTEM.md.
- Anti-insinuación + traceability are per-screen invariants and MUST be stated explicitly in each screen spec.
- Build ON the shipped shell (e.g. parlamentario/[id] stacked-carriles pattern); do not redesign from scratch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: GlobalHeader + Landing + Resultados de búsqueda specs</name>
  <read_first>
    19-UI-SPEC.md §11.0 (GlobalHeader), §11.1 (Landing), §11.2 (/buscar), §6 (copywriting), §9 (per-surface state matrix), §10 (invariants);
    DESIGN-SYSTEM.md (tokens, component catalogue, honest states) from Plan 01;
    BRIEF.md landing/hero + per-surface value from Plan 02.
  </read_first>
  <action>
    Create SCREENS.md with a header stating it is the CLOSED per-screen contract set, derived from approved 19-UI-SPEC.md §11, companion to DESIGN-SYSTEM.md and BRIEF.md. Adopt a consistent per-screen template: Route · Layout (container width + padding) · Components used (from DS catalogue) · Structure (top-to-bottom) · States (the 3 honest states + loading + error from the matrix) · Anti-insinuación + traceability invariants that apply · Copy (exact strings). Then author:
    (1) GlobalHeader (layout.tsx) — persistent minimal sticky-optional header, cream bg + subtle bottom border; left wordmark "Observatorio del Congreso 360" → home; right desktop nav Buscar · Parlamentarios · Agenda · Sobre/Metodología with active item petrol underline; mobile compact menu; touch targets ≥44px; height ~56px; px-4 md:px-8; no auth, no required theme toggle.
    (2) Landing / — max-w-3xl mx-auto px-4 md:px-8 py-16 md:py-24 on cream; display hero headline with exactly ONE italic petrol accent phrase; SearchBox autofocus as hero with petrol focus ring + "Buscar proyectos" submit; the 4 LOCKED example pills (3 ideas + boletín 15234-07 in Mono) click-fills-and-submits; trust line under pills (muted, bullet-separated); inline "¿Cómo leer esto?" onboarding hint (no modal/tour); typographic/paper texture only, graph motif DEFERRED; no marketing/testimonials/fabricated counts. States: empty = "Aún no has buscado" + pills; static shell so no loading/error.
    (3) Resultados de búsqueda /buscar — max-w-5xl, two-column on lg (1fr / 320px): main results + MapaDeFuentes sidebar (stacks below on mobile); persistent search bar pre-filled (petrol focus); dismissible onboarding hint bar ("Entendido"); optional AiSummaryCallout banner "Analizar resultados · síntesis sobre estas fuentes · la fuente original queda íntegra" (labelled, never replaces sources); result header "N fuentes · ordenadas por relevancia" REAL counts only, NO per-result score/%; SourceTypeTabs (Todo / Proyectos / other real types, counts real or absent); SearchResultCard (boletín Mono, EtapaBadge, CamaraChip, title, ProvenanceBadge, no relevance bar); MapaDeFuentes composition-by-source, count/shape-coded, NEVER a people graph, NEVER a score. States: zero results = "Sin resultados para esta búsqueda" + pills; loading = shape-matched skeleton cards; error = "No pudimos completar la búsqueda…".
    No fenced code blocks of applied source — express layouts as Tailwind class strings inline and structure as prose/lists.
  </action>
  <acceptance_criteria>
    - SCREENS.md exists in the phase directory.
    - File contains a GlobalHeader spec naming the four nav entries and "≥44px" touch targets.
    - File contains a Landing spec with "max-w-3xl", "py-16 md:py-24", the 4 example pills (including "15234-07"), the trust line, and graph motif marked DEFERRED.
    - File contains a /buscar spec with "max-w-5xl", "MapaDeFuentes", "SourceTypeTabs", "ordenadas por relevancia", and an explicit "NO score"/"no relevance" statement.
    - Each of the three specs lists its honest states (empty / loading / error as applicable) and the anti-insinuación invariants that apply.
    - No fenced applied-source code blocks; no file under app/ modified.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/SCREENS.md';const s=fs.readFileSync(p,'utf8');for(const t of ['GlobalHeader','max-w-3xl','max-w-5xl','MapaDeFuentes','15234-07','Sin resultados para esta búsqueda','ordenadas por relevancia']){if(!s.includes(t)){console.error('MISSING:',t);process.exit(1);}}console.log('Task1 OK');"</automated>
  </verify>
  <done>SCREENS.md contains implementation-ready specs for GlobalHeader, Landing, and Resultados de búsqueda with layout, components, states, and invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Ficha de proyecto + Ficha de parlamentario + Contraparte + Directorio + Sobre/Metodología specs</name>
  <read_first>
    19-UI-SPEC.md §11.3 (/proyecto), §11.5 (/parlamentario/[id]), §11.6 (/parlamentario directory), §11.7 (/contraparte gated), §11.8 (/sobre + /metodologia), §10 (invariants), §9 (states);
    DESIGN-SYSTEM.md component catalogue + honest states + anti-insinuación invariants;
    BRIEF.md per-surface value sections.
  </read_first>
  <action>
    Append to SCREENS.md, using the same per-screen template:
    (4) Ficha de proyecto /proyecto/[boletin] — max-w-3xl single column (optional MapaDeFuentes/"proyectos similares" sidebar on lg); FichaHeader (title, boletín Mono, EtapaBadge, CamaraChip(s), ProvenanceBadge); stacked carriles (mt-12 siblings, each h2 + Suspense + shape-matched skeleton + honest empty): idea matriz (AiSummaryCallout with modelo/scope/fuentes + source intact), cuerpos legales (CuerposLegalesList), timeline de tramitación (TimelineView cross-chamber, chamber colour-coded by civic token), votaciones (VotacionCard/VotoRow outcome palette + identity guard on names), proyectos similares (ProyectosSimilares by embedding, NO score, framed "proyectos relacionados"). Every datum carries ProvenanceBadge. States: 404 if boletín invalid; per-section honest empty/skeleton/error.
    (5) Ficha de parlamentario /parlamentario/[id] — max-w-3xl single column, shipped stacked-carriles shell; ParlamentarioHeader (name, CamaraChip, period Mono — NO foto, NO partido per LEGAL-03); LOCKED carril order each mt-12 sibling with h2 + Suspense + skeleton + 3-state honest empty: #votos (VotosSection: list A favor/En contra/Abstención/Pareo/Ausente, asistencia, rebeldías as neutral data, by-tema view reusing embeddings, identity guard), #lobby (LobbySection: contraparte raw text + IdentityMarker never linked, ProvenanceBadge per row), #patrimonio (PatrimonioSection: version history, side-by-side comparison SOLO-datos no verdict/delta, "Presentada el {fecha}" prominent Mono + amber historical caveat, CC BY 4.0 in intro AND caption), #dinero (ContratosSection — GATED: moneyPublicEnabled wraps whole section incl. h2, OFF→absent; "mención de la fuente" attribution not CC BY 4.0), #financiamiento (FinanciamientoSection — GATED: grouped by election + amber prior-candidacy caveat; donor as own subject "Aporta:", donor RUT NEVER rendered; "asociado por nombre confirmado al candidato" never "por RUT"). Invariant: no carril ever composes with #votos; mt-12 gap never collapsed.
    (6) Contraparte /contraparte/[id] (gated) — page-level moneyPublicEnabled→notFound() as FIRST statement (OFF→whole route 404 serving not-found.tsx); when ON: header (empresa = persona jurídica) + TWO sibling mt-12 carriles contratos (ChileCompra) + aportes (SERVEL) each row ProvenanceBadge; hard anti-insinuación: zero vote data, zero causal language, neutral counting (no SUM/ranking), amounts verbatim, donor RUT never rendered; attribution per dataset (ChileCompra "mención de la fuente" / SERVEL "términos de uso por verificar"; never CC BY 4.0).
    (7) Directorio /parlamentario (NEW) — backed by real maestra ~186 rows (31 senadores + 155 diputados) via a public-read RPC mirroring parlamentario_publico (header-only fields); max-w-5xl list/grid of ParlamentarioDirectoryRow/Card; each entry name + chamber CamaraChip + period Mono, NO foto NO partido, links to /parlamentario/[id]; filters by chamber + name text search; ordering NEUTRAL (default alphabetical), any metric ordering is by neutral observable fact + MethodologyCaveat, NEVER a "ranking de los peores" (deferred). States per §9.
    (8) Sobre/Metodología /sobre + /metodologia (light spec) — data posture, anti-insinuación principles, source list + per-dataset attribution, "Beta abierta" honesty, legal/CC BY 4.0 notes; linked from header and MethodologyCaveat; informational, not a data carril.
    Close SCREENS.md with a one-line "all five key screens + header + directory + sobre are CLOSED; nothing left abierto" assertion.
  </action>
  <acceptance_criteria>
    - SCREENS.md contains a /proyecto spec listing the stacked carriles (idea matriz, cuerpos legales, timeline, votaciones, proyectos similares) each as mt-12 sibling, with "AiSummaryCallout" and "NO score" for proyectos similares.
    - SCREENS.md contains a /parlamentario/[id] spec with the LOCKED carril order naming "#votos", "#lobby", "#patrimonio", "#dinero", "#financiamiento", "NO foto", "NO partido", and the gate behaviour for #dinero/#financiamiento (OFF→absent).
    - SCREENS.md contains a /contraparte spec stating page-level "notFound()" when OFF and "donor RUT never rendered".
    - SCREENS.md contains a /parlamentario directory spec referencing "186", "alphabetical", and "MethodologyCaveat", with "ranking de los peores" marked as deferred/avoided.
    - SCREENS.md contains a /sobre or /metodologia light spec mentioning "Beta abierta" and per-dataset attribution.
    - File contains the closure assertion that the screens are CLOSED with nothing left abierto.
    - No file under app/ modified; no fenced applied-source code blocks.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/SCREENS.md';const s=fs.readFileSync(p,'utf8');for(const t of ['/proyecto/[boletin]','AiSummaryCallout','#votos','#financiamiento','NO partido','notFound()','MethodologyCaveat','Beta abierta','CC BY 4.0']){if(!s.includes(t)){console.error('MISSING:',t);process.exit(1);}}console.log('Task2 OK');"</automated>
  </verify>
  <done>SCREENS.md covers all 5 key screens plus directory and sobre/metodología, each implementation-ready with layout, components, states, gating, and anti-insinuación invariants; closure assertion present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase writes Markdown screen specs. No runtime, no auth, no input handling, no data egress.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | (none) | SCREENS.md | accept | No new attack surface — screen specs only. No code in app/, no secrets, no live data. |
</threat_model>

<verification>
- All 5 key screens + GlobalHeader + directory + sobre/metodología specified to implementation-ready detail.
- Each screen states layout, components, the 3 honest states, and applicable anti-insinuación invariants.
- MONEY gating documented for both OFF and future ON.
- Nothing left "abierto"; closure assertion present.
</verification>

<success_criteria>
Phase success criterion 4 (designs of the 5 key screens delivered as executable UI-SPEC per screen, nothing left abierto) is satisfied by this artifact.
</success_criteria>

<output>
Create `.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-03-SUMMARY.md` when done.
</output>
