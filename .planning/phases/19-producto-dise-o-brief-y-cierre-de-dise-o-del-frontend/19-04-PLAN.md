---
phase: 19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/mockup/landing.html
autonomous: true
requirements: []   # n/a (design closure)
user_setup: []

must_haves:
  truths:
    - "The landing mockup opens in a browser as a static file and visually realizes the cream paper background + display hero + single italic petrol accent phrase + search-as-hero + example pills + trust line"
    - "The mockup is throwaway: it lives in the phase directory (mockup/landing.html), NOT in app/, and is never built or deployed"
    - "The mockup uses the locked token values (cream bg, petrol accent, Geist) so it reads as the same brand as the shipped system"
    - "The mockup shows no fabricated stats, no graph motif, and no banned causal/score language"
  artifacts:
    - path: ".planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/mockup/landing.html"
      provides: "Throwaway static HTML+Tailwind visual anchor of the landing"
      contains: "40 33% 97%"
      min_lines: 60
  key_links:
    - from: "mockup/landing.html"
      to: "DESIGN-SYSTEM.md + UI-SPEC §11.1 landing contract"
      via: "realizes the locked landing tokens, pills, trust line, italic accent"
      pattern: "accent|cream|pill|Buscar proyectos"
---

<objective>
Produce mockup/landing.html — the single throwaway HTML/Tailwind mockup of the LANDING, the visual anchor for the design closure. It visually realizes the locked landing contract: cream paper background, large display hero with exactly ONE italic petrol accent phrase, semantic search as the protagonist (search box + "Buscar proyectos" submit), the 4 example pills (3 ideas + 1 boletín in mono), and the trust line — all in the locked brand tokens so it reads as the same product as the shipped system.

Purpose: a static visual reference that anchors the design; it is desechable and NOT part of the application.
Output: mockup/landing.html in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-CONTEXT.md

This is a DESIGN/PRODUCT closure phase.
HARD CONSTRAINTS:
- The mockup is THROWAWAY and lives ONLY in the phase directory at mockup/landing.html. It MUST NOT be created anywhere under app/. It is never imported, built, or deployed.
- Self-contained static file: Tailwind via CDN (or inline <style>), no build step, no JS framework, no external data. It must open directly in a browser by double-clicking.
- Use the LOCKED token values from DESIGN-SYSTEM.md / UI-SPEC §4: cream bg hsl(40 33% 97%), petrol accent hsl(183 38% 26%), near-black foreground, Geist (fall back to system sans is acceptable for a throwaway, but reference Geist where possible).
- NO fabricated stats/counts. NO graph motif (deferred). NO banned causal/affinity/score language anywhere in the copy.
- The hero contains exactly ONE italic petrol accent phrase (the "Sin alucinaciones." pattern), not more.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway landing mockup</name>
  <read_first>
    19-UI-SPEC.md §11.1 (landing contract), §4.1 (cream + petrol HSL), §Typography (display + italic accent + mono), §6 (CTA "Buscar proyectos", trust line, the 4 example pills);
    DESIGN-SYSTEM.md (final locked token values, spacing, pill/trust-line treatment) from Plan 01;
    19-CONTEXT.md <decisions> Área 1 + Área 2 (cream, italic accent, petrol chrome, search-as-hero).
  </read_first>
  <action>
    Create mockup/landing.html as a self-contained static HTML page (create the mockup/ subdirectory under the phase dir). Use Tailwind via CDN script plus an inline tailwind.config defining the locked CSS variables/colors (cream background hsl(40 33% 97%), petrol accent hsl(183 38% 26%), foreground near-black hsl(222 47% 11%)). Reference the Geist font (Google Fonts or system-sans fallback acceptable for a throwaway; use a monospace for the boletín pill). Realize the landing per §11.1, top to bottom:
    - A minimal header band: wordmark "Observatorio del Congreso 360" (left) + nav links Buscar · Parlamentarios · Agenda · Sobre (right), cream with subtle bottom border.
    - Centered hero column (max-w-3xl, generous vertical padding py-16 md:py-24): a large display headline (semibold, leading-tight) that is sober and confident with EXACTLY ONE italic phrase rendered in the petrol accent colour (a single italic clause in the spirit of "Sin alucinaciones." — write a neutral Spanish display sentence appropriate to the Observatorio with one italic petrol clause).
    - The search box as the protagonist: a wide input with placeholder text inviting a project idea or a boletín number, a petrol focus ring style, and a petrol "Buscar proyectos" submit button.
    - The 4 example pills below the box as clickable-looking chips: "protección de datos personales", "delitos económicos y medio ambiente", "40 horas / jornada laboral", and the boletín "15234-07" rendered in a monospace font.
    - The trust line under the pills, muted and bullet-separated: "Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad."
    - A subtle inline onboarding affordance "¿Cómo leer esto?" (link-styled, no modal).
    Background is typographic/paper texture only (a flat cream is acceptable; a subtle CSS texture is optional) — NO node/graph motif. NO stats, NO testimonials, NO fabricated counts. Keep all copy in neutral Chilean Spanish with no causal/score vocabulary. Add an HTML comment at the top of the file noting it is a THROWAWAY mockup for Phase 19, not part of app/, not for deployment.
  </action>
  <acceptance_criteria>
    - mockup/landing.html exists at .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/mockup/landing.html (NOT under app/).
    - File is a complete static HTML document (has <html>, <head>, <body>) that opens standalone with no build step.
    - File contains the cream background value "40 33% 97%" and the petrol accent value "183 38% 26%".
    - File contains the submit label "Buscar proyectos".
    - File contains all 4 example pills including the boletín "15234-07".
    - File contains the trust line "Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad."
    - File contains exactly ONE element styled italic as the hero accent (one <em>/italic clause in the headline; the rest of the headline is not italic).
    - File contains an HTML comment marking it THROWAWAY / not for app/ / not for deployment.
    - File contains no banned causal/affinity/score copy and no fabricated stats.
    - No file under app/ is created or modified.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/mockup/landing.html';const s=fs.readFileSync(p,'utf8');for(const t of ['<html','40 33% 97%','183 38% 26%','Buscar proyectos','15234-07','Fuente, fecha y enlace en cada dato','THROWAWAY']){if(!s.includes(t)){console.error('MISSING:',t);process.exit(1);}}const italic=(s.match(/<em[ >]/g)||[]).length + (s.match(/italic/g)||[]).length;if(italic<1){console.error('No italic accent found');process.exit(1);}console.log('Mockup OK');"</automated>
  </verify>
  <done>mockup/landing.html is a self-contained throwaway page realizing the cream bg + display hero + single italic petrol accent + search-as-hero + 4 pills + trust line, in the locked brand tokens, with no app/ footprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase writes a static, non-deployed throwaway HTML file. It is never built, imported, or served. No runtime, no auth, no input handling, no data egress.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | (none) | mockup/landing.html | accept | No new attack surface — throwaway mockup is not built/deployed and contains no secrets or live data. Tailwind CDN is the only external reference and is not part of any shipped bundle. |
</threat_model>

<verification>
- mockup/landing.html opens standalone and realizes the locked landing visual contract.
- Lives in the phase dir, never in app/; marked throwaway.
- Cream + petrol tokens present; one italic accent; pills + trust line present; no graph motif, no fabricated stats, no banned vocabulary.
</verification>

<success_criteria>
Phase success criterion 4 (a throwaway HTML/Tailwind mockup of the landing as the visual anchor) is satisfied by this artifact.
</success_criteria>

<output>
Create `.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-04-SUMMARY.md` when done.
</output>
