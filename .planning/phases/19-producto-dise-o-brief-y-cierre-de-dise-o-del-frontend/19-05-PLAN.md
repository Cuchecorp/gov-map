---
phase: 19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/CLOSURE.md
autonomous: true
requirements: []   # n/a (design closure)
user_setup: []

must_haves:
  truths:
    - "CLOSURE.md cross-checks every one of the 5 phase success criteria against the produced artifacts and records each as MET (or PARTIAL) with a SPECIFIC artifact + section-string citation as evidence — not a bare verdict word"
    - "CLOSURE.md verifies SC1's browseros study against the actual screenshot files on disk (refs/*.jpg); if any of the six is missing, SC1 is recorded PARTIAL, never MET"
    - "CLOSURE.md verifies the principios rectores are intact: trazabilidad (ProvenanceBadge fuente/fecha/enlace), anti-insinuación (carril mt-12, nunca dinero+voto, sin causal/score), MONEY gated, sin foto, sin partido — each citing its concrete enforcement location in an artifact"
    - "CLOSURE.md confirms no app/ source was modified and no feature/data was invented"
    - "The consolidated deliverable (BRIEF + DESIGN-SYSTEM + SCREENS + landing mockup) is marked CERRADO; implementation follows the brief without re-opening decisions"
  artifacts:
    - path: ".planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/CLOSURE.md"
      provides: "Consolidation + closure: success-criteria cross-check (with evidence citations), SC1 screenshot-evidence verification, principios-rectores audit (with enforcement citations), artifact index, CERRADO sign-off"
      contains: "CERRADO"
      min_lines: 80
  key_links:
    - from: "CLOSURE.md success-criteria table"
      to: "BRIEF.md, DESIGN-SYSTEM.md, SCREENS.md, mockup/landing.html, refs/*.jpg"
      via: "each criterion cites the artifact + section that satisfies it; SC1 cites the on-disk screenshots"
      pattern: "BRIEF\\.md|DESIGN-SYSTEM\\.md|SCREENS\\.md|landing\\.html|refs/.*\\.jpg"
---

<objective>
Produce CLOSURE.md — the consolidation and closure of the phase. It cross-checks all produced artifacts (BRIEF.md, DESIGN-SYSTEM.md, SCREENS.md, mockup/landing.html) against the 5 phase success criteria — each row citing a SPECIFIC artifact + section as evidence — verifies SC1's browseros study against the actual screenshot files on disk, audits that the principios rectores are intact across the design (each item citing its concrete enforcement location), confirms the hard constraints were honoured (no app/ source changes, no invented features/data, MONEY stays gated), indexes the deliverables, and marks the consolidated design CERRADO so that any later implementation phase follows the brief without re-opening decisions.

Purpose: the single closure gate that declares the design phase complete and the design contract frozen — and that cannot mark a criterion MET from a verdict word alone.
Output: CLOSURE.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/BRIEF.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/SCREENS.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/mockup/landing.html

This is the closure step. It reads and audits the prior artifacts; it does not redesign.
HARD CONSTRAINTS:
- Do NOT touch any file under app/. Do NOT modify the prior artifacts unless an audit finds a concrete gap (if a gap is found, fix the specific artifact, then re-audit).
- The closure must be honest: if any success criterion is only PARTIALLY met or an artifact (or its evidence) is missing a required element, record it explicitly as PARTIAL with the gap, and fix the source artifact rather than marking it MET falsely.
- A criterion may be marked MET ONLY when the row cites the concrete artifact + section string that proves it. A bare "MET" with no citation is forbidden and the automated verify will reject it.
- SC1 (browseros visual study) may be marked MET ONLY if all six screenshot files exist on disk in refs/; otherwise SC1 is PARTIAL with the missing file named.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit artifacts — existence + required-element checks + SC1 screenshot evidence</name>
  <read_first>
    ROADMAP.md Phase 19 "Success Criteria" (the 5 criteria) and the 7-deliverable list from the phase guidance;
    BRIEF.md, DESIGN-SYSTEM.md, SCREENS.md, mockup/landing.html (the produced artifacts);
    refs/ (the six browseros screenshots: tributalab-home.jpg, tributalab-resultados.jpg, legalatlas-home.jpg, legalatlas-ficha-articulo.jpg, ischilesafe-home.jpg, ischilesafe-rankings.jpg);
    19-UI-SPEC.md §0.1 (deliverables table), §10 (anti-insinuación invariants).
  </read_first>
  <action>
    Verify each prior artifact exists and contains its required elements (the acceptance criteria of Plans 01–04). Specifically confirm: DESIGN-SYSTEM.md has tokens (cream + petrol), typography, spacing, component catalogue, ES voice with banned-vocabulary list, honest-states catalogue, anti-insinuación invariants; BRIEF.md has value-per-surface for all 7 surfaces, IA/nav, landing/hero, reference verdicts, deferred ideas; SCREENS.md has implementation-ready specs for the 5 key screens + GlobalHeader + directory + sobre/metodología with the closure assertion; mockup/landing.html exists in mockup/ (NOT app/) and contains cream/petrol tokens, the 4 pills, the trust line, one italic accent, and the THROWAWAY marker.
    ALSO verify SC1 evidence: confirm the six browseros screenshots exist on disk under refs/ AND that BRIEF.md cites each by relative path. Record the result in an explicit "SC1 evidence" log line — list each of the six files with an EXISTS/MISSING status. If any screenshot is missing, SC1 is PARTIAL (Task 2 must record it so) and the missing file is named here.
    If any required element is missing, fix the SPECIFIC source artifact (append/correct the missing element) and note the fix in CLOSURE.md's audit log. Begin CLOSURE.md with a header and an "Artifact Index" table listing each deliverable file with a one-line description and an EXISTS/COMPLETE status.
  </action>
  <acceptance_criteria>
    - CLOSURE.md exists with an Artifact Index table referencing BRIEF.md, DESIGN-SYSTEM.md, SCREENS.md, and mockup/landing.html by filename.
    - Each indexed artifact is confirmed to exist on disk and its required elements verified (audit log section present).
    - CLOSURE.md contains an "SC1 evidence" log listing all six browseros screenshots (tributalab-home.jpg, tributalab-resultados.jpg, legalatlas-home.jpg, legalatlas-ficha-articulo.jpg, ischilesafe-home.jpg, ischilesafe-rankings.jpg) with an EXISTS/MISSING status each.
    - The six screenshot files actually exist on disk under refs/; if any is missing it is named in the SC1 evidence log and flagged for PARTIAL.
    - Any gap found is recorded AND fixed in the source artifact (not papered over).
    - No file under app/ is modified.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/';for(const f of ['BRIEF.md','DESIGN-SYSTEM.md','SCREENS.md','mockup/landing.html','CLOSURE.md']){if(!fs.existsSync(d+f)){console.error('MISSING artifact:',f);process.exit(1);}}const c=fs.readFileSync(d+'CLOSURE.md','utf8');for(const t of ['BRIEF.md','DESIGN-SYSTEM.md','SCREENS.md','landing.html']){if(!c.includes(t)){console.error('CLOSURE missing index entry:',t);process.exit(1);}}const shots=['tributalab-home.jpg','tributalab-resultados.jpg','legalatlas-home.jpg','legalatlas-ficha-articulo.jpg','ischilesafe-home.jpg','ischilesafe-rankings.jpg'];for(const f of shots){if(!c.includes(f)){console.error('CLOSURE SC1 evidence log missing screenshot entry:',f);process.exit(1);}if(!fs.existsSync(d+'refs/'+f)){console.error('SC1 EVIDENCE MISSING on disk:',f,'-> CLOSURE must record SC1 PARTIAL not MET');process.exit(1);}}console.log('Audit OK (artifacts + SC1 screenshots present)');"</automated>
  </verify>
  <done>All four prior artifacts exist and are complete; the six SC1 screenshots are confirmed on disk and logged; CLOSURE.md indexes them and logs any fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Success-criteria cross-check (with evidence citations), principios-rectores audit (with enforcement citations), CERRADO sign-off</name>
  <read_first>
    ROADMAP.md Phase 19 5 success criteria + LOCKED execution notes;
    CLOSURE.md artifact index + SC1 evidence log from Task 1;
    DESIGN-SYSTEM.md anti-insinuación invariants + honest states (note their section headings/anchors); SCREENS.md per-screen invariants (note their headings); BRIEF.md reference verdicts + deferred ideas (note their headings).
  </read_first>
  <action>
    Append to CLOSURE.md:
    (1) Success-criteria cross-check table — one row per Phase 19 success criterion (1: reference study with adopt/adapt/avoid verdict; 2: product brief value-per-surface + IA/nav + landing/hero + onboarding + squeeze real data; 3: closed design system tokens/voice/components/honest-states; 4: per-screen executable specs for the 5 key screens, nothing abierto; 5: principios rectores intact + consolidated deliverable revisado y cerrado). EACH row MUST cite, as evidence, a SPECIFIC artifact filename PLUS a section string (a literal "§N", a "## heading" name, or a quoted anchor that exists in that artifact) — e.g. "BRIEF.md → '## Reference verdicts applied' + refs/tributalab-home.jpg", "DESIGN-SYSTEM.md → '## Anti-insinuación invariants'". A bare status word with no artifact+section citation is NOT acceptable. Status is MET only when the cited section actually contains the proof; otherwise record PARTIAL with the gap and the resolution. SC1 specifically: status reflects the Task-1 SC1 evidence log — MET only if all six screenshots exist, PARTIAL (naming the missing file) otherwise.
    (2) Principios rectores audit — explicit checklist confirming: trazabilidad (ProvenanceBadge fuente/fecha/enlace present on every datum across specs), anti-insinuación (carril mt-12 sibling; a vote NEVER composed with money/lobby in one unit; no causal/affinity/score language; no relevance %/score), MONEY gated (OFF→node/page absent until LEGAL-01), no foto / no partido (LEGAL-03), no invented features/data (deferred items marked), attribution per dataset (CC BY 4.0 only InfoProbidad; ChileCompra "mención de la fuente"; SERVEL "términos de uso por verificar"). EACH item marked PASS MUST cite its concrete enforcement location — the artifact filename + the section/heading where that invariant is enforced (e.g. "DESIGN-SYSTEM.md → '## Anti-insinuación invariants' #1 carril mt-12"; "SCREENS.md → /parlamentario carril order"). A PASS without an enforcement-location citation is not acceptable.
    (3) Hard-constraints confirmation — state explicitly that no app/ source was modified (the only HTML is the throwaway mockup in the phase dir), MONEY was not turned on, and no DB/RLS/conector changes were made.
    (4) CERRADO sign-off — a final section declaring the consolidated deliverable (BRIEF + DESIGN-SYSTEM + SCREENS + landing mockup) CERRADO, with the statement that any later implementation phase follows this brief without re-opening decisions, plus a short "next: implementation phase" pointer. Use the literal word "CERRADO".
    Voice rule: CLOSURE.md's own prose must not use banned causal/score/affinity verdict vocabulary; if a forbidden term must be quoted (e.g. naming what is forbidden), fence it between `<!-- BANNED-VOCAB-START -->` and `<!-- BANNED-VOCAB-END -->`.
  </action>
  <acceptance_criteria>
    - CLOSURE.md contains a success-criteria cross-check table with a row for each of the 5 criteria. EACH row cites a specific artifact filename AND a section string (§N or a "## heading" / quoted anchor) as evidence, plus a status (MET or PARTIAL+resolution). No row may carry a bare status with no artifact+section citation.
    - SC1's row status is consistent with the Task-1 SC1 evidence log: MET only if all six screenshots exist on disk; PARTIAL (with the missing file named) otherwise.
    - CLOSURE.md contains a principios-rectores audit checklist covering trazabilidad, anti-insinuación (mt-12 + never compose vote with money/lobby + no score), MONEY gated, no foto/partido, no invented data, and attribution per dataset — each marked PASS AND citing its concrete enforcement location (artifact + section/heading).
    - CLOSURE.md states no app/ source modified and MONEY not turned on.
    - CLOSURE.md contains the literal word "CERRADO" in a final sign-off section.
    - NEGATIVE MATCH (machine-enforced): outside any `<!-- BANNED-VOCAB-START/END -->` fence, CLOSURE.md's own prose contains no banned causal/score/affinity verdict term.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/CLOSURE.md';const s=fs.readFileSync(p,'utf8');for(const t of ['CERRADO','trazabilidad','anti-insinuación','mt-12','MONEY','no foto','partido','mención de la fuente']){if(!s.includes(t)){console.error('MISSING:',t);process.exit(1);}}const crit=(s.match(/\b(MET|PARTIAL)\b/g)||[]).length;if(crit<5){console.error('Fewer than 5 criteria statuses found');process.exit(1);}const cites=(s.match(/(BRIEF|DESIGN-SYSTEM|SCREENS)\.md|landing\.html|§\s*\d/g)||[]).length;if(cites<5){console.error('Cross-check lacks >=5 artifact+section evidence citations (found '+cites+') -- a bare MET is not allowed');process.exit(1);}const stripped=s.replace(/<!--\s*BANNED-VOCAB-START\s*-->[\s\S]*?<!--\s*BANNED-VOCAB-END\s*-->/g,'').toLowerCase();const subs=['afinidad','a cambio de','% de coincidencia','puntaje','conflicto de interés','enriquecimiento'];for(const b of subs){if(stripped.includes(b)){console.error('BANNED vocab leaked outside fence:',b);process.exit(1);}}const tok=(' '+stripped+' ').replace(/[.,;:!?\n()]/g,' ');if(tok.includes(' porque ')){console.error('BANNED vocab leaked outside fence: porque');process.exit(1);}console.log('Closure OK (5 statuses + >=5 evidence citations + negative-match)');"</automated>
  </verify>
  <done>CLOSURE.md cross-checks all 5 success criteria each with an artifact+section evidence citation, records SC1 honestly against the on-disk screenshots, audits the principios rectores as PASS each with an enforcement-location citation, confirms hard constraints, passes the banned-vocab negative-match, and marks the consolidated design CERRADO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase writes a Markdown closure document. No runtime, no auth, no input handling, no data egress.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | (none) | CLOSURE.md | accept | No new attack surface — closure document only. No code in app/, no secrets, no live data. |
</threat_model>

<verification>
- All four prior artifacts exist and are complete; the six SC1 screenshots exist on disk and are logged.
- All 5 success criteria cross-checked against artifacts, each row citing a concrete artifact + section as evidence (a bare MET is rejected).
- SC1 recorded MET only if all six screenshots exist, PARTIAL otherwise.
- Principios rectores audited PASS, each citing its enforcement location; hard constraints confirmed; CLOSURE.md prose passes the banned-vocab negative-match; design marked CERRADO.
</verification>

<success_criteria>
Phase success criterion 5 (principios rectores intactos + consolidated deliverable revisado y cerrado) is satisfied by this artifact, which also confirms criteria 1–4 are met with concrete evidence citations — SC1 only when its six screenshots exist on disk.
</success_criteria>

<output>
Create `.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-05-SUMMARY.md` when done.
</output>
</content>
