---
phase: 19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md
autonomous: true
requirements: []   # n/a (design closure — no data REQ maps to this phase)
user_setup: []

must_haves:
  truths:
    - "A reader of DESIGN-SYSTEM.md can implement the cream bg + petrol accent + Geist typography + 8-pt spacing without re-opening any decision"
    - "Every token has a concrete HSL/value lifted verbatim from the approved UI-SPEC (no placeholders, nothing 'por decidir')"
    - "The three honest states (no consultado / consultado sin resultados / error) are textually and visually distinguished per surface and never read as 'limpio'"
    - "Banned vocabulary (causal / afinidad / score / ranking-as-verdict) is enumerated and absent from the doc's own prose"
    - "The anti-insinuación invariants (carril mt-12 sibling, nunca componer dinero+voto, sin score, MONEY gated, sin foto/partido) are present as a checkable invariant list"
  artifacts:
    - path: ".planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md"
      provides: "Closed design system: tokens, typography, spacing, color, component catalogue, editorial voice (ES), honest-states catalogue, anti-insinuación invariants"
      contains: "hsl(40 33% 97%)"
      min_lines: 200
  key_links:
    - from: "DESIGN-SYSTEM.md tokens section"
      to: "app/app/globals.css + app/app/styles/civic-tokens.css"
      via: "extend-not-break wiring note (cream --background, --accent-product, retuned --ring; civic tokens untouched)"
      pattern: "globals\\.css|civic-tokens\\.css|accent-product"
---

<objective>
Produce DESIGN-SYSTEM.md — the CLOSED design system for the Observatorio frontend, implementation-ready and consistent with what is already shipped. It consolidates §0–§10 of the approved UI-SPEC into a single self-contained reference: tokens (cream paper background + petrol/teal product accent + civic data tokens + Geist typography + 8-pt spacing), the component catalogue (shipped + NEW spec-only), the editorial voice guide in Spanish, the honest-states catalogue, and the anti-insinuación invariants.

Purpose: any implementation phase that follows must be able to build the visual layer without re-deciding anything. This is the foundation that the per-screen specs (Plan 03) and the landing mockup (Plan 04) build on.
Output: DESIGN-SYSTEM.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-UI-SPEC.md
@.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-CONTEXT.md
@app/app/globals.css
@app/app/styles/civic-tokens.css

This is a DESIGN/PRODUCT closure phase. Deliverables are Markdown design artifacts ONLY.
HARD CONSTRAINTS:
- Do NOT touch any file under app/ (the design system is documented here, NOT applied to source — application is a later implementation phase).
- Do NOT invent tokens, components, or values not present in the UI-SPEC. Where the UI-SPEC fixed a "Claude's discretion" value (accent HSL, spacing scale, example pills), use that fixed value verbatim.
- Build ON the shipped system (shadcn Slate + civic tokens + Geist); the cream bg and petrol accent EXTEND globals.css, they do not replace civic-tokens.css.
- Anti-insinuación and traceability are non-negotiable invariants and must be reproduced in the doc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens, typography, spacing, color (the visual foundation)</name>
  <read_first>
    19-UI-SPEC.md §0.3 (governing principles), §Design System table, §Spacing Scale, §Typography, §Color (§4.1–§4.4);
    app/app/globals.css (shipped Slate baseline + dark mode);
    app/app/styles/civic-tokens.css (--camara/--senado/--provenance-*/--identity-warn-*).
  </read_first>
  <action>
    Create DESIGN-SYSTEM.md with a header stating it is the CLOSED design system for the phase, derived from the approved 19-UI-SPEC.md, and that no listed decision may be re-opened by implementation. Then author these sections, lifting concrete values verbatim from the UI-SPEC:
    (1) Color — the 60/30/10 split with exact HSL for light and dark: cream --background hsl(40 33% 97%) / dark hsl(222 28% 7%); card/secondary/muted warm values; petrol --accent-product hsl(183 38% 26%) light / hsl(183 34% 46%) dark; retuned --destructive. Include the cream-rationale and petrol-rationale notes (contrast ratios). Reproduce §4.2 "accent reserved for" as an EXPLICIT enumerated list (search focus ring + submit, body text links, global focus ring, the single italic display accent, active nav/tab underline, the CENTRAL divider rule) AND the explicit "accent is NOT used for" list. Reproduce §4.3 civic/semantic tokens table (--camara, --senado, --provenance-*, --identity-warn-*) with the invariant "civic colours are data identity, never brand/UI". Note vote-outcome literal palette and "no destructive actions" (read-only product).
    (2) Typography — Geist Sans (body+display by weight) + Geist Mono (hard metadata). Reproduce the 4-size ramp + 1 display + 2 weights table with exact Tailwind classes. State the display-accent rule (exactly ONE italic petrol phrase in the hero), the Mono usage rule (identifiers/codes/dates/money verbatim, prose never Mono), and "heading hierarchy is sacred (h1→h2→h3 never skips)".
    (3) Spacing — the 8-pt scale table (xs 4px … 4xl 96px) with the LOCKED mt-12 carril boundary (2xl 48px) flagged as the anti-insinuación frontier that is never collapsed. Container width rule (max-w-3xl reading column; max-w-5xl two-column result surfaces). 44px touch-target exception.
    (4) Token wiring note — a SPEC-ONLY paragraph (no code applied) describing how a later implementation phase extends globals.css :root and .dark with the new cream/warm/petrol values and adds an accentProduct mapping in tailwind.config.ts, while leaving civic-tokens.css imports intact. Make explicit this is a wiring note for a future phase, not a change to be applied now.
    Use Markdown tables for token tables. No fenced code blocks containing applied CSS edits — values may be shown in tables/inline, but the doc must read as a spec, not a patch.
  </action>
  <acceptance_criteria>
    - DESIGN-SYSTEM.md exists in the phase directory.
    - File contains the literal string "hsl(40 33% 97%)" (cream background) and "hsl(183 38% 26%)" (petrol accent).
    - File contains a "## Color" (or equivalent) section with both light and dark HSL values for background, card/secondary/muted, accent-product, destructive.
    - File contains the explicit "accent reserved for" enumerated list AND the "accent is NOT used for" list.
    - File contains the civic-tokens table naming --camara, --senado, --provenance, --identity-warn with the "data identity, never brand" invariant.
    - File contains a typography ramp table referencing Geist Sans and Geist Mono and the exactly-one-italic-accent rule.
    - File contains the 8-pt spacing table with "mt-12" flagged as the carril boundary.
    - File contains a token-wiring note explicitly scoped to a future implementation phase (mentions globals.css and tailwind.config.ts, mentions civic-tokens.css stays intact).
    - No file under app/ is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md';const s=fs.readFileSync(p,'utf8');for(const t of ['hsl(40 33% 97%)','hsl(183 38% 26%)','Geist Mono','mt-12','--camara','globals.css','civic-tokens.css']){if(!s.includes(t)){console.error('MISSING token:',t);process.exit(1);}}console.log('Task1 OK');"</automated>
  </verify>
  <done>DESIGN-SYSTEM.md contains color, typography, spacing sections with all locked values verbatim and the future-phase wiring note.</done>
</task>

<task type="auto">
  <name>Task 2: Component catalogue, editorial voice (ES), honest states, anti-insinuación invariants</name>
  <read_first>
    19-UI-SPEC.md §7 (component catalogue 7.1 shipped + 7.2 NEW spec-only), §8 (editorial voice guide), §9 (honest states catalogue + per-surface matrix), §10 (anti-insinuación invariants), §6 (copywriting contract — empty/error/trust-line/pills);
    19-CONTEXT.md <code_context> (shipped components list) and <decisions> (voice, states).
  </read_first>
  <action>
    Append to DESIGN-SYSTEM.md these sections:
    (5) Component catalogue (closed) — reproduce §7.1 (shipped domain components: ProvenanceBadge, IdentityMarker, EtapaBadge, CamaraChip, FichaHeader/ParlamentarioHeader, TimelineView/Event, Votacion*/Voto* family, SearchBox, SearchResultCard, IdeaMatrizBlock/CuerposLegalesList/ProyectosSimilares, CitacionCard/WeekNav/SalaTableSection, the carril Sections) each with its anti-insinuación/traceability contract, and §7.2 (NEW spec-only: GlobalHeader, TrustLine, ExamplePills, OnboardingHints, SourceTypeTabs, MapaDeFuentes, AiSummaryCallout, MethodologyCaveat, ParlamentarioDirectoryCard/Row, HonestEmptyState, StaleNote/HistoricalCaveat) marked clearly as designed-now-built-later. State that this is the closed set: implementation adds nothing not listed.
    (6) Editorial voice guide (Spanish, CLOSED) — register (neutral, factual, sober-confident, Chilean Spanish). The "Always" list (facts with fuente/fecha/enlace; money verbatim in Mono; literal dated; "asociado al RUT"/"registrado en {fuente}" not possessive; AI synthesis labelled with modelo/scope/fuentes + source intact; honest degradation never collapsed). The "Never (banned vocabulary)" list as a checker-enforceable enumeration: causal/intention (porque, a cambio de, para favorecer, influyó, respondió a), affinity/score/ranking-as-verdict (afinidad, puntaje, score, % de coincidencia, el peor/mejor, ranking de los más), conflict conclusions (conflicto de interés, enriquecimiento, sospechoso), composing money/lobby+vote in one unit, marketing/fabricated counts. Include the trust line and tone exemplars. CRITICAL: the doc's OWN prose must not use any banned term except inside the explicit banned-vocabulary list (where they are quoted as forbidden).
    (7) Honest states catalogue — the 3-state definition table (no consultado / consultado sin resultados / error) with visual treatment + copy pattern, then the per-surface state matrix (landing, /buscar, /proyecto, /parlamentario, /parlamentario directory, /agenda, /contraparte) reproducing §9. State the skeleton rule (shape-matched, never spinner) and stale-data rule (>48h → amber ProvenanceBadge, shown never hidden).
    (8) Anti-insinuación invariants (HARD) — reproduce the 10 invariants of §10 as a numbered, checkable list: carril propio (mt-12 sibling sections, never nested); never composite (meeting/declaration/contract/contribution + vote never share article/Card/li/tr); no causal/affinity/score language; no relevance %/score; no "ranking de los peores"; MONEY gate OFF→node absent; identity guard (link only if confirmado); PII never rendered; AI labelled + source intact; attribution per dataset (CC BY 4.0 only InfoProbidad, ChileCompra "mención de la fuente", SERVEL "términos de uso por verificar").
  </action>
  <acceptance_criteria>
    - DESIGN-SYSTEM.md contains a component catalogue section listing ProvenanceBadge, IdentityMarker, GlobalHeader, ExamplePills, MapaDeFuentes, MethodologyCaveat, HonestEmptyState (at minimum) and distinguishes shipped vs NEW spec-only.
    - File contains an "## Editorial voice" (or equivalent ES voice) section with a banned-vocabulary list that includes "afinidad", "score", "conflicto de interés", "porque", "enriquecimiento".
    - File contains the trust line string "Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad."
    - File contains an honest-states section naming all three states ("no consultado", "consultado sin resultados", "error") AND a per-surface matrix referencing /buscar, /parlamentario, /agenda, /contraparte.
    - File contains a numbered anti-insinuación invariants list mentioning "mt-12", "nunca" composing vote with money/lobby, "sin score", MONEY gate, "identidad no verificada", and attribution per dataset.
    - Outside the explicit banned-vocabulary enumeration, the document's prose contains no banned causal/score term (the doc practices the voice it preaches).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/DESIGN-SYSTEM.md';const s=fs.readFileSync(p,'utf8');for(const t of ['ProvenanceBadge','MethodologyCaveat','HonestEmptyState','afinidad','conflicto de interés','no consultado','consultado sin resultados','Fuente, fecha y enlace en cada dato','identidad no verificada','mención de la fuente']){if(!s.includes(t)){console.error('MISSING:',t);process.exit(1);}}console.log('Task2 OK');"</automated>
  </verify>
  <done>DESIGN-SYSTEM.md is a complete closed design system: tokens + typography + spacing + color + component catalogue + ES voice guide + honest states + anti-insinuación invariants, all consistent with the UI-SPEC and shipped system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase writes Markdown design documentation. There is no runtime, no auth, no input handling, no data egress.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | (none) | DESIGN-SYSTEM.md | accept | No new attack surface — design artifact only. No code applied to app/, no secrets, no live data. |
</threat_model>

<verification>
- DESIGN-SYSTEM.md exists and is self-contained (a reader needs nothing but this file + the UI-SPEC to implement the visual layer).
- All locked token values appear verbatim (cream, petrol, Geist, 8-pt, mt-12).
- Banned vocabulary enumerated; the doc itself stays clean of it outside that list.
- No file under app/ touched.
</verification>

<success_criteria>
Phase success criterion 3 (closed design system: tokens, typography, spacing, ES voice, components, honest states, implementation-ready and consistent with shipped) is satisfied by this single artifact.
</success_criteria>

<output>
Create `.planning/phases/19-producto-dise-o-brief-y-cierre-de-dise-o-del-frontend/19-01-SUMMARY.md` when done.
</output>
