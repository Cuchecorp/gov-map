---
phase: 74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0054_leyes_rotacion_estado.sql
  - packages/tramitacion/src/run-tramitacion-prod-cli.ts
  - packages/tramitacion/src/rotacion-leyes.ts
  - packages/tramitacion/src/rotacion-leyes.test.ts
autonomous: true
requirements: [DEBT-04]

must_haves:
  truths:
    - "run-tramitacion-prod-cli lee el corpus COMPLETO (3.657) vía .order().range() paginado — no truncado al cap ~1000 de PostgREST"
    - "El cron leyes-weekly rota round-robin sobre el corpus vía un cursor de offset persistido → ningún proyecto queda indefinidamente sin refrescar"
    - "La agenda (citacion_punto + sesion_tabla_item) sigue teniendo prioridad; la ventana rotada cubre la COLA de proyectos sin actividad reciente"
    - "MONEY/SERVEL quedan EXCLUIDOS del cron (no se refrescan por esta vía mientras gated)"
    - "Un fallo de lectura (red/RLS) sigue LANZANDO (fail-loud), nunca se lee como 'DB vacía / nada que rotar'"
  artifacts:
    - path: "supabase/migrations/0054_leyes_rotacion_estado.sql"
      provides: "Tabla marcador leyes_rotacion_estado (singleton con ultimo_boletin/offset), RLS deny-by-default"
      contains: "create table leyes_rotacion_estado"
    - path: "packages/tramitacion/src/rotacion-leyes.ts"
      provides: "Selección round-robin pura (agenda-prioridad ∪ ventana-rotada del corpus paginado) — testeable sin red"
      min_lines: 40
    - path: "packages/tramitacion/src/rotacion-leyes.test.ts"
      provides: "Prueba de rotación determinista + paginación >1k con cliente Supabase fake"
      min_lines: 40
  key_links:
    - from: "packages/tramitacion/src/run-tramitacion-prod-cli.ts"
      to: "proyecto (corpus completo)"
      via: ".order('boletin').range(from, from+PAGE-1) en loop"
      pattern: "\\.range\\("
    - from: "packages/tramitacion/src/run-tramitacion-prod-cli.ts"
      to: "leyes_rotacion_estado"
      via: "leer offset antes de seleccionar + avanzar tras corrida"
      pattern: "leyes_rotacion_estado"
---

<objective>
Cerrar DEBT-04: `run-tramitacion-prod-cli.ts` (`boletinesARefrescar`, líneas 92-134) hoy (a) lee `proyecto`/`citacion_punto`/`sesion_tabla_item` SIN `.range()` → PostgREST recorta a ~1000 de 3.657 (bug 1k-cap oculto), y (b) hace `.slice(0, 80)` sobre el MISMO prefijo cada viernes → la cola de proyectos sin actividad de agenda NUNCA rota. Fix: (1) leer el corpus COMPLETO vía `.order('boletin').range()` paginado (espeja `fichas/writer-supabase.ts:124-143`), (2) añadir un cursor de rotación (marcador singleton) que avance el offset semana a semana → round-robin sobre la cola. Agenda mantiene prioridad; MONEY/SERVEL EXCLUIDOS; se preserva la semántica fail-loud (`run-tramitacion-prod-cli.ts:109-118`).

Purpose: Con 3.657 proyectos y 80/semana sobre un prefijo fijo truncado a 1k, ~2.657 proyectos jamás se refrescan y la frescura se diluye silenciosamente. La paginación + el cursor de rotación garantizan cobertura de TODO el corpus a lo largo de sucesivas corridas, minimizando minutos CI (lotes acotados, cadencia intacta).
Output: Migración 0054 (cursor de rotación), lógica de selección pura + paginada, wiring en el CLI, y tests offline con un fake Supabase que devuelve >1000 filas en páginas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-RESEARCH.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del codebase. Usar directo. -->

Función a arreglar HOY — packages/tramitacion/src/run-tramitacion-prod-cli.ts:92-134:
```typescript
async function boletinesARefrescar(url, serviceKey, limite, log): Promise<string[]> {
  const sb = createClient(url, serviceKey, { auth: { persistSession: false } });
  // ... push() dedup con BOLETIN_RE ...
  const leer = async (tabla, soloNoNull): Promise<BoletinRow[]> => {
    let q = sb.from(tabla).select("boletin");         // ⟵ SIN .range() → cap ~1000
    if (soloNoNull) q = q.not("boletin", "is", null);
    const { data, error } = await q;
    if (error) throw new Error(`boletinesARefrescar: ${tabla}: ${error.message}`);  // fail-loud (PRESERVAR)
    return (data as BoletinRow[] | null) ?? [];
  };
  for (const r of await leer("citacion_punto", true)) push(r.boletin);   // agenda PRIMERO
  for (const r of await leer("sesion_tabla_item", true)) push(r.boletin);
  const agendaCount = ordenados.length;
  for (const r of await leer("proyecto", false)) push(r.boletin);        // ya ingeridos DESPUÉS
  return ordenados.slice(0, limite);                                     // ⟵ prefijo fijo, no rota
}
```
`DEFAULT_LIMITE = 80`. `BOLETIN_RE = /^\d{3,6}-\d{1,3}$/`.

Patrón de paginación a ESPEJAR — packages/fichas/src/writer-supabase.ts:124-143:
```typescript
const PAGE = 1000;
const todos: string[] = [];
for (let from = 0; ; from += PAGE) {
  const { data, error } = await client.from(tabla).select("boletin")
    .order("boletin", { ascending: true })     // orden estable → páginas deterministas (WR-01)
    .range(from, from + PAGE - 1);
  if (error) throw new Error(`… falló: ${error.message}`);
  const filas = (data ?? []) as Array<{ boletin: string }>;
  todos.push(...filas.map((f) => f.boletin));
  if (filas.length < PAGE) break;
}
```

Marcador análogo (patrón cursor durable) — supabase/migrations/0021_lobby.sql:133-137 / 0024_servel.sql:163-167:
```sql
create table aportes_ingesta_estado (
  parlamentario_id text primary key references parlamentario(id) on delete cascade,
  ingestado_hasta  date,
  fecha_captura    timestamptz not null default now()
);
```
El cursor de rotación es un SINGLETON (una sola fila de estado del cron leyes). Próxima migración = 0054 (tras la 0053 del plan 74-01; NO reusar el número).

Nota MONEY/SERVEL: `boletinesARefrescar` ya lee SOLO `proyecto` + `citacion_punto` + `sesion_tabla_item` (tramitación de leyes). NO añadir lecturas de `contrato`/`aporte`/`servel`/`chilecompra`. La exclusión se mantiene por construcción; el test lo afirma (grep de las tablas leídas).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración 0054 + selección round-robin pura y paginada</name>
  <files>supabase/migrations/0054_leyes_rotacion_estado.sql, packages/tramitacion/src/rotacion-leyes.ts, packages/tramitacion/src/rotacion-leyes.test.ts</files>
  <read_first>
    - packages/fichas/src/writer-supabase.ts:124-143 (patrón .order().range() paginado)
    - packages/tramitacion/src/run-tramitacion-prod-cli.ts:84-134 (boletinesARefrescar, BOLETIN_RE, fail-loud)
    - supabase/migrations/0021_lobby.sql:133-140 (patrón marcador durable)
    - packages/tramitacion/vitest.config.ts (config del runner)
  </read_first>
  <behavior>
    - leerCorpusPaginado(clienteFake que devuelve 2500 filas en 3 páginas de 1000/1000/500) → devuelve las 2500 (prueba que el cap 1k está resuelto)
    - leerCorpusPaginado usa .order('boletin') → páginas deterministas (el fake verifica que se pidió order+range)
    - seleccionarRotado({agenda:[a,b], corpus:[10 boletines], offset:0, limite:5}) → agenda primero, luego los primeros de la ventana desde offset 0
    - seleccionarRotado con offset avanzado (p.ej. offset=8, corpus de 10) → envuelve (wrap-around) al inicio del corpus tras el final → round-robin cubre toda la cola en sucesivas corridas
    - avanzarOffset(offset, seleccionadosDeCola, tamañoCorpus) → nuevo offset = (offset + n) mod tamañoCorpus (nunca fuera de rango)
    - la ventana rotada EXCLUYE los boletines ya cubiertos por agenda (sin duplicar el presupuesto `limite`)
  </behavior>
  <action>
    Crear `supabase/migrations/0054_leyes_rotacion_estado.sql`: tabla `leyes_rotacion_estado` singleton — `id int primary key default 1 check (id = 1)`, `offset_rotacion int not null default 0`, `ultimo_boletin text`, `fecha_captura timestamptz not null default now()`. `enable row level security` SIN policy `to anon` (uso interno de cron, deny-by-default). Número 0054.
    Crear `packages/tramitacion/src/rotacion-leyes.ts`: (a) `leerCorpusPaginado(cliente, tabla): Promise<string[]>` espejando el patrón `.order('boletin').range(from, from+PAGE-1)` de fichas (PAGE=1000, loop hasta `filas.length < PAGE`, `error` → throw con solo `error.message`). (b) `seleccionarRotado({ agenda, corpus, offset, limite }): { seleccion: string[]; nuevoOffset: number }` — pura: dedup agenda-primero (respeta BOLETIN_RE, reusar el `push` idiom), luego toma `limite - agenda.length` de la cola (corpus menos agenda) empezando en `offset`, con WRAP-AROUND circular; calcula `nuevoOffset = (offset + tomadosDeCola) mod corpusCola.length`. (c) Preservar fail-loud: un error de lectura lanza (no devuelve []). Documentar que la ventana rotada garantiza que la cola se recorre completa a lo largo de las corridas.
    Crear `packages/tramitacion/src/rotacion-leyes.test.ts` (vitest) con un cliente Supabase FAKE que implemente `.from().select().order().range()` devolviendo páginas parametrizables (para simular >1000 filas), cubriendo cada línea de `<behavior>`. Sin código en `<action>` — identificadores: `leerCorpusPaginado`, `seleccionarRotado`, `avanzarOffset`, `offset_rotacion`, `leyes_rotacion_estado`, `.order`, `.range`, wrap-around.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test</automated>
  </verify>
  <acceptance_criteria>
    - Test con fake que devuelve 2500 filas en 3 páginas → `leerCorpusPaginado` devuelve length 2500 (cap 1k resuelto; aserción de count exacto)
    - El fake registra que se invocó `.order('boletin', ...)` antes de `.range()` (orden estable verificado)
    - Test de wrap-around: con offset cerca del final del corpus, la selección incluye boletines del INICIO (round-robin circular) — aserción sobre los ids concretos seleccionados
    - `avanzarOffset` nunca devuelve un valor ≥ tamaño de la cola (aserción `< corpusCola.length`)
    - Un error de lectura lanza (test que inyecta `error` y espera throw) — fail-loud preservado
    - grep de `rotacion-leyes.ts` NO muestra lecturas de `contrato`/`aporte`/`servel`/`chilecompra` (MONEY/SERVEL excluidos por construcción)
    - Sin lenguaje subjetivo en asserts
  </acceptance_criteria>
  <done>Migración 0054 escrita; selección round-robin pura + lectura paginada + tests verdes con fake >1k.</done>
</task>

<task type="auto">
  <name>Task 2: Wire de la rotación en run-tramitacion-prod-cli (leer offset, seleccionar, avanzar)</name>
  <files>packages/tramitacion/src/run-tramitacion-prod-cli.ts</files>
  <read_first>
    - packages/tramitacion/src/run-tramitacion-prod-cli.ts:47-220 (boletinesARefrescar, run(), gates dry-run, override --boletines)
    - packages/tramitacion/src/rotacion-leyes.ts (creado en Task 1)
    - packages/fichas/src/writer-supabase.ts:124-143 (referencia del patrón)
  </read_first>
  <action>
    Reemplazar el cuerpo de `boletinesARefrescar` para: (1) leer agenda (`citacion_punto` + `sesion_tabla_item`) como hoy; (2) leer el corpus `proyecto` vía `leerCorpusPaginado` (NO el `.select()` sin range de hoy); (3) leer el `offset_rotacion` actual de `leyes_rotacion_estado` (0 si no hay fila); (4) `seleccionarRotado({ agenda, corpus, offset, limite })` para obtener la ventana; (5) tras seleccionar, persistir `nuevoOffset` en `leyes_rotacion_estado` (upsert singleton `id=1`, `onConflict: "id"`, solo `error.message` en logs). Preservar: el override `--boletines` explícito (bypass total del gather + de la rotación), el gate dry-run/sin-credenciales, el log de conteos de candidatos, y la semántica fail-loud (un `error` de lectura sigue lanzando, NO devuelve []). NO añadir lecturas de tablas MONEY/SERVEL. Mantener `DEFAULT_LIMITE=80` y la cadencia del cron intacta (no cambiar el schedule del YAML). Actualizar el comentario de cabecera de la función para documentar la rotación + la paginación.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test</automated>
  </verify>
  <acceptance_criteria>
    - `boletinesARefrescar` invoca `leerCorpusPaginado` (grep muestra `.range(` en el path del corpus)
    - `boletinesARefrescar` lee `leyes_rotacion_estado` antes de seleccionar y upserta `offset_rotacion` después (grep muestra `leyes_rotacion_estado` 2×: read + upsert)
    - El override `--boletines a,b` sigue funcionando sin tocar el cursor (test o inspección: la rama explícita retorna antes del gather)
    - Un fallo de lectura sigue lanzando (fail-loud); dry-run/sin-credenciales no persiste el offset
    - `pnpm --filter @obs/tramitacion test` verde; `pnpm --filter @obs/tramitacion build` (o `tsc`) sin errores de tipo
    - NO se referencia `contrato`/`aporte`/`servel`/`chilecompra` en el archivo (MONEY/SERVEL excluidos)
  </acceptance_criteria>
  <done>Rotación cableada: corpus completo paginado + offset persistido + round-robin; override/gates/fail-loud preservados; MONEY/SERVEL excluidos; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → Supabase (read corpus + offset) | lectura de 3.657 proyectos + estado de rotación con service key |
| cron → fuentes (Cámara WS / Senado XML) | refresh de la ventana seleccionada, rate-limit 2-3s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05 | Information disclosure (cobertura falsa) | `.slice(80)` sobre un corpus truncado a 1k presentado como "corpus refrescado" | mitigate | `.order().range()` paginado lee los 3.657 reales + cursor de rotación cubre la cola → ningún proyecto queda indefinidamente stale |
| T-74-06 | Denial (silencioso) | lectura fallida (RLS/red) se lee como "DB vacía / nada que rotar" → no-op verde | mitigate | fail-loud preservado (`boletinesARefrescar` lanza ante `error`); dry-run no persiste offset |
| T-74-07 | Tampering (alcance) | ingestión accidental de MONEY/SERVEL vía el cron de leyes | mitigate | selección lee SOLO `proyecto`/`citacion_punto`/`sesion_tabla_item`; test/grep confirma ausencia de tablas MONEY/SERVEL |
| T-74-08 | Information disclosure | service key filtrada en logs del upsert del offset | mitigate | upsert reporta solo `error.message`, nunca la key ni la URL con password |
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test` verde (paginación >1k + rotación wrap-around + fail-loud).
- `grep -v '^--' supabase/migrations/0054_leyes_rotacion_estado.sql` muestra `create table leyes_rotacion_estado` + `enable row level security` y NO muestra `to anon`.
- `pnpm test` sin regresión al cierre de la wave.
</verification>

<success_criteria>
- El cron leyes-weekly rota round-robin sobre el corpus 3.657 → ningún proyecto queda indefinidamente sin refrescar (DEBT-04, SC#3 de la fase).
- El corpus se lee COMPLETO (cap 1k resuelto).
- Agenda mantiene prioridad; MONEY/SERVEL excluidos; fail-loud preservado; cadencia intacta.
</success_criteria>

<output>
Create `.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-02-SUMMARY.md` when done
</output>
