---
phase: 74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0053_leylobby_cursor_estado.sql
  - packages/lobby/src/ingest-cli.ts
  - packages/lobby/src/writer-supabase.ts
  - packages/lobby/src/writer.ts
  - packages/lobby/src/cursor-leylobby.ts
  - packages/lobby/src/cursor-leylobby.test.ts
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "El conector leylobby avanza un cursor (institución, año, página) entre corridas — no re-scrapea todo el histórico cada vez"
    - "El hash-check R2 (existed=true → [skip] sin novedades) sigue funcionando sin regresión"
    - "Una corrida que degrada (leylobby bloqueada 403/503) NO avanza el cursor como si hubiera datos"
    - "El cursor persiste DESPUÉS de una corrida exitosa (no antes), con orden estable y avance idempotente"
  artifacts:
    - path: "supabase/migrations/0053_leylobby_cursor_estado.sql"
      provides: "Tabla marcador leylobby_cursor_estado (institución PK + anio + pagina alcanzados), RLS deny-by-default"
      contains: "create table leylobby_cursor_estado"
    - path: "packages/lobby/src/cursor-leylobby.ts"
      provides: "Lógica pura de avance del cursor (página+1 / año-1) — pura, testeable sin red/DB"
      min_lines: 20
    - path: "packages/lobby/src/cursor-leylobby.test.ts"
      provides: "Prueba de avance/no-avance del cursor y degradación honesta"
      min_lines: 30
  key_links:
    - from: "packages/lobby/src/ingest-cli.ts"
      to: "leylobby_cursor_estado"
      via: "leer cursor antes de derivar (institución, año, página) + avanzar tras corrida exitosa"
      pattern: "leylobby_cursor_estado"
    - from: "packages/lobby/src/writer-supabase.ts"
      to: "leylobby_cursor_estado"
      via: "upsert del cursor (leerCursor / avanzarCursor)"
      pattern: "leylobby_cursor_estado"
---

<objective>
Cerrar DEBT-02: el conector leylobby usa un cursor INCREMENTAL durable que avanza (institución, año, página) entre corridas, en vez de fijar `año=actual, página=1` cada vez (`ingest-cli.ts:141-143`). Espeja el patrón de marcador de ingesta `lobby_ingesta_estado` (0021) / `aportes_ingesta_estado` (0024). El hash-check R2 existente (`ingest-run.ts:133-154`) se PRESERVA — el cursor decide QUÉ recurso pedir; el hash-check decide si hay novedades.

Purpose: Sin cursor, cada corrida re-pide la misma página 1 del año actual → nunca avanza al histórico y desperdicia requests contra un servidor volátil (leylobby: Laravel/Azure, 403/503). El cursor respeta al servidor (no re-scrape) y garantiza progreso determinista.
Output: Migración 0053 (tabla cursor), lógica pura de avance, wiring en el CLI/writer, y tests offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-RESEARCH.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del codebase. Usar directo, sin explorar. -->

Marcador análogo YA EXISTENTE (patrón a espejar) — supabase/migrations/0021_lobby.sql:133-140:
```sql
create table lobby_ingesta_estado (
  parlamentario_id text primary key references parlamentario(id) on delete cascade,
  ingestado_hasta  date,
  fecha_captura    timestamptz not null default now()
);
alter table lobby_ingesta_estado enable row level security;
create policy lobby_ingesta_estado_public_read on lobby_ingesta_estado for select to anon using (true);
grant select on lobby_ingesta_estado to anon;
```
NOTA: el cursor de leylobby es USO INTERNO de cron (no lo lee la ficha) → RLS deny-by-default SIN policy `to anon` (a diferencia de lobby_ingesta_estado que sí es public-read porque la ficha lo consulta). Próxima migración = 0053.

Writer marcador YA EXISTENTE (patrón a espejar) — packages/lobby/src/writer-supabase.ts:113-123:
```typescript
async marcarIngestado(parlamentarioIds: string[], hasta: string): Promise<void> {
  if (parlamentarioIds.length === 0) return;
  const ids = [...new Set(parlamentarioIds)];
  const filas = ids.map((id) => ({ parlamentario_id: id, ingestado_hasta: hasta }));
  for (const lote of chunk(filas, CHUNK)) {
    const { error } = await this.client.from("lobby_ingesta_estado")
      .upsert(lote, { onConflict: "parlamentario_id", ignoreDuplicates: false });
    if (error) throw new Error(`upsert lobby_ingesta_estado falló: ${error.message}`);
  }
}
```

CLI hoy (a modificar) — packages/lobby/src/ingest-cli.ts:140-144:
```typescript
const institucion = opts.institucion ?? DEFAULT_INSTITUCION;
const anio = opts.anio ?? new Date().getFullYear();       // FIJO — no avanza
const nPaginas = opts.paginas ?? 1;                       // corrida ACOTADA
const pages = Array.from({ length: nPaginas }, (_v, i) => i + 1);
const tareas: TareaInstitucion[] = [{ institucionCodigo: institucion, year: anio, pages }];
```
`LobbyCliOptions` ya acepta inyección (conector, writer, r2Store) para tests sin red/DB. `TareaInstitucion = { institucionCodigo: string; year: number; pages?: number[] }` (ingest-run.ts:26-31).

Hash-check R2 a PRESERVAR (no tocar) — packages/lobby/src/ingest-run.ts:138-149:
```typescript
const { r2Path, existed } = await opts.r2Store.putImmutable("leylobby", clave, date, sha, "html", bytes);
if (existed) { log(`[skip] sin novedades — leylobby ${clave}`); continue; }
```

Degradación honesta a PRESERVAR — ingest-run.ts:114-120: LeylobbyBloqueadaError (403/503) → `degradaciones.push(...)`, NO escribe. `RunIngestLobbyResult` incluye `degradaciones: DegradacionLobby[]` y `errores[]`. El cursor NO debe avanzar si la corrida no obtuvo datos (degradaciones/errores sin audiencias).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración 0053 + lógica pura de avance del cursor</name>
  <files>supabase/migrations/0053_leylobby_cursor_estado.sql, packages/lobby/src/cursor-leylobby.ts, packages/lobby/src/cursor-leylobby.test.ts</files>
  <read_first>
    - supabase/migrations/0021_lobby.sql:127-140 (patrón tabla marcador `lobby_ingesta_estado`)
    - supabase/migrations/0024_servel.sql:163-167 (patrón `aportes_ingesta_estado`)
    - packages/lobby/src/ingest-run.ts:26-31,55-70 (TareaInstitucion, opts de la corrida)
    - packages/lobby/vitest.config.ts (config del runner)
  </read_first>
  <behavior>
    - avanzarCursor({anio, pagina}, {huboDatos:true}) → misma institución, pagina+1 (avanza dentro del año)
    - avanzarCursor cuando se alcanzó el tope de páginas de un año → retrocede a anio-1, pagina=1 (recorre histórico hacia atrás)
    - avanzarCursor({...}, {huboDatos:false}) → devuelve el MISMO cursor (degradación/agotado NO avanza; evita saltar páginas o loop infinito)
    - deriveTarea(cursor) → TareaInstitucion determinista para la próxima corrida (institución/año/página)
    - un cursor sin fila previa (primera corrida) → arranca en (año actual, página 1) — el default de hoy, pero ahora persistible
  </behavior>
  <action>
    Crear `supabase/migrations/0053_leylobby_cursor_estado.sql`: tabla `leylobby_cursor_estado` con `institucion_codigo text primary key`, `anio int not null`, `pagina int not null`, `fecha_captura timestamptz not null default now()`. Espeja la forma de `lobby_ingesta_estado` (0021) PERO uso interno de cron → `enable row level security` SIN policy `to anon` (deny-by-default; el cursor no se expone a la ficha, a diferencia de `lobby_ingesta_estado` que sí es public-read). NO `grant select to anon`. Número 0053 (siguiente tras 0052).
    Crear `packages/lobby/src/cursor-leylobby.ts` con lógica PURA (sin red/DB): un tipo `CursorLeylobby = { institucionCodigo: string; anio: number; pagina: number }`, una función `avanzarCursor(cursor, { huboDatos, anioMin?, paginaMax? })` que avanza `pagina+1` mientras haya datos y no supere `paginaMax` (default un tope acotado p.ej. la ventana de la corrida), y al agotar el año retrocede a `anio-1, pagina=1` (histórico hacia atrás) hasta `anioMin` (default 2015 leylobby). Cuando `huboDatos===false` devuelve el cursor SIN modificar (Pitfall 4: degradación/agotamiento no avanza). Añadir `deriveTarea(cursor): TareaInstitucion` (institución/año/página). Documentar por qué NO avanza en degradación (referencia a la degradación honesta de `ingest-run`).
    Crear `packages/lobby/src/cursor-leylobby.test.ts` (vitest) cubriendo cada caso de `<behavior>`. Sin código en `<action>` — solo identificadores: `avanzarCursor`, `deriveTarea`, `CursorLeylobby`, `leylobby_cursor_estado`, `huboDatos`, `anioMin`, `paginaMax`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test</automated>
  </verify>
  <acceptance_criteria>
    - 0053 crea `leylobby_cursor_estado` con PK `institucion_codigo`, columnas `anio`/`pagina`/`fecha_captura`, RLS habilitada SIN policy `to anon` (grep del archivo muestra `enable row level security` y NO muestra `grant select on leylobby_cursor_estado to anon`)
    - `cursor-leylobby.test.ts` verde con ≥1 caso por línea de `<behavior>` (avanza pagina, retrocede año al agotar, NO avanza sin datos, deriveTarea determinista, primera corrida = año actual/pág 1)
    - `avanzarCursor(c, {huboDatos:false})` devuelve un cursor byte-idéntico al de entrada (aserción de igualdad en el test)
    - Sin lenguaje subjetivo en asserts (todos comparan valores concretos)
  </acceptance_criteria>
  <done>Migración 0053 escrita; lógica pura de cursor + tests verdes; el cursor no avanza en degradación.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire del cursor en ingest-cli + writer (leer antes, avanzar después)</name>
  <files>packages/lobby/src/ingest-cli.ts, packages/lobby/src/writer-supabase.ts, packages/lobby/src/writer.ts</files>
  <read_first>
    - packages/lobby/src/ingest-cli.ts:125-220 (main: derivación de tareas + corrida + reporte)
    - packages/lobby/src/writer-supabase.ts:113-124 (marcarIngestado — patrón de upsert a espejar)
    - packages/lobby/src/writer.ts (interfaz LobbyWriter + InMemoryLobbyWriter para tests)
    - packages/lobby/src/ingest-run.ts:72-85,94-104 (RunIngestLobbyResult: audiencias/degradaciones/errores)
    - packages/lobby/src/cursor-leylobby.ts (creado en Task 1)
  </read_first>
  <behavior>
    - main() sin --anio/--paginas explícitos → LEE el cursor de leylobby_cursor_estado, deriva la tarea, corre, y tras corrida exitosa (audiencias>0 o novedad) AVANZA + persiste el cursor
    - main() con --anio/--paginas explícitos → override (bootstrap dirigido), NO consulta el cursor (corrida operador puntual)
    - una corrida que degrada (RunIngestLobbyResult.degradaciones no vacío y audiencias===0) → NO avanza el cursor (persiste el mismo)
    - dry-run / sin credenciales → NO escribe el cursor (mismo gate que el writer Supabase)
    - writer.leerCursor(institucion) devuelve null sin fila → main arranca en (año actual, pág 1)
  </behavior>
  <action>
    Extender la interfaz `LobbyWriter` (`writer.ts`) con `leerCursor(institucionCodigo): Promise<CursorLeylobby | null>` y `avanzarCursor(cursor): Promise<void>`; implementarlos en `SupabaseLobbyWriter` (`writer-supabase.ts`) espejando el upsert de `marcarIngestado` sobre `leylobby_cursor_estado` (`onConflict: "institucion_codigo"`, `error.message` únicamente — nunca la service key, T-07-06). Implementar no-op / in-memory en `InMemoryLobbyWriter` para tests. En `ingest-cli.ts` main(): cuando NO se pasó `--anio`/`--paginas` y hay writer real (no dry-run), llamar `leerCursor(institucion)` → `deriveTarea` para construir `tareas`; tras `runIngestLobby`, si la corrida obtuvo datos (audiencias>0), llamar `avanzarCursor` con el resultado de `avanzarCursor(...)` de la lógica pura (`huboDatos = res.audiencias > 0`). Preservar el override explícito y el gate dry-run/sin-credenciales. NO tocar el hash-check R2 ni la degradación honesta de `ingest-run.ts`. Añadir/extender tests en un `.test.ts` del paquete usando `InMemoryLobbyWriter` + conector inyectado (sin red) que cubran `<behavior>`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test</automated>
  </verify>
  <acceptance_criteria>
    - `LobbyWriter` declara `leerCursor` + `avanzarCursor`; `SupabaseLobbyWriter` los implementa contra `leylobby_cursor_estado`; `InMemoryLobbyWriter` los stubbea
    - Test: corrida sin flags con cursor {2024,p1} y conector que devuelve audiencias → el writer in-memory registra un avance a {2024,p2} (o retroceso de año si agotó)
    - Test: corrida con `degradaciones` no vacío y audiencias===0 → NO se llamó `avanzarCursor`
    - Test: corrida con `--anio`/`--paginas` explícitos → `leerCursor` NO se invoca (override)
    - Test: dry-run → cursor NO se persiste
    - El hash-check R2 (`ingest-run.test.ts` [skip] sin novedades) sigue verde — no se tocó `ingest-run.ts`
    - Sin lenguaje subjetivo en asserts
  </acceptance_criteria>
  <done>Cursor cableado: leer antes / avanzar después; override y gates preservados; hash-check y degradación intactos; suite @obs/lobby verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/CLI → leylobby.gob.cl | fuente externa volátil (Laravel/Azure, 403/503); requests deben respetar rate-limit + no re-scrape |
| CLI → Supabase (cursor write) | escritura de estado de cursor con service key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Denial (al servidor) | re-scrape del histórico completo de leylobby en cada corrida | mitigate | cursor incremental (avanza página/año) + hash-check R2 preservado (`existed` → [skip]) → nunca re-pide lo ya obtenido |
| T-74-02 | Tampering (datos) | cursor avanza en una corrida degradada (403/503) saltando páginas o marcando cobertura falsa | mitigate | `avanzarCursor` con `huboDatos===false` no avanza; persistir DESPUÉS de corrida exitosa; degradación honesta de `ingest-run` intacta |
| T-74-03 | Information disclosure | service key filtrada en logs del writer del cursor | mitigate | upsert del cursor reporta solo `error.message` (patrón T-07-06 de `marcarIngestado`), nunca la key |
| T-74-04 | Elevation | cursor legible por `anon` (superficie innecesaria) | mitigate | `leylobby_cursor_estado` RLS deny-by-default SIN policy `to anon` (uso interno de cron, no ficha) |
</threat_model>

<verification>
- `pnpm --filter @obs/lobby test` verde (cursor + no-regresión hash-check).
- `grep -v '^--' supabase/migrations/0053_leylobby_cursor_estado.sql` muestra `create table leylobby_cursor_estado` + `enable row level security` y NO muestra `to anon`.
- `pnpm test` (suite completa) sin regresión al cierre de la wave.
</verification>

<success_criteria>
- El conector leylobby avanza un cursor durable (institución/año/página) entre corridas — no re-scrapea el histórico (DEBT-02, SC#1 de la fase).
- El hash-check R2 y la degradación honesta NO regresionan.
- El cursor no avanza en corridas degradadas.
</success_criteria>

<output>
Create `.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-01-SUMMARY.md` when done
</output>
