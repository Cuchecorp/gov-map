---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 03
type: execute
wave: 3
depends_on: ["129-02"]
files_modified:
  - app/lib/plural.ts
  - app/lib/plural.test.ts
  - app/components/panel-tile-comisiones.tsx
  - app/components/panel-tile-comisiones.test.tsx
  - app/components/panel-tile-urgencias.tsx
  - app/components/panel-tile-urgencias.test.tsx
  - app/components/panel-tile-votaciones.tsx
  - app/components/panel-tile-votaciones.test.tsx
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md
  - .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "Existe una crítica de diseño escrita contra los baselines v13, con hallazgos accionables y criterio de cierre verificable"
    - "La concordancia de plural del panel es correcta para n=1 en los 4 moldes con sustantivo contado"
    - "El project-ref y el host de Supabase quedan redactados en 07-01-SUMMARY.md"
  artifacts:
    - path: "app/lib/plural.ts"
      provides: "Helper general de pluralización es-CL"
      exports: ["plural"]
    - path: "app/lib/plural.test.ts"
      provides: "Tests de n=0/1/2 del helper"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md"
      provides: "Crítica Opus vs baselines + deuda de operador"
  key_links:
    - from: "app/components/panel-tile-comisiones.tsx"
      to: "app/lib/plural.ts"
      via: "import"
      pattern: "from \"@/lib/plural\""
---

<objective>
Criticar el deploy contra los baselines v13, matar el bug de concordancia de plural con un helper
general y testeado en los 4 moldes afectados, y redactar el hit de secretos preexistente.

Purpose: separar el trabajo de CÓDIGO (que debe estar cerrado antes del re-deploy final) del
trabajo de DEPLOY+MEDICIÓN (129-04). Así las capturas finales son necesariamente posteriores a
todos los cambios.
Output: `129-CRITICA.md`, `app/lib/plural.ts` + fixes, redacción B26. **Este plan NO deploya.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CONTEXT.md
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
@app/components/panel-tile-comisiones.tsx
@app/lib/idioms-panel.ts
</context>

<interfaces>
Contrato existente de los tiles (implementado en 128 — NO re-diseñar, NO re-implementar):

```ts
// app/components/panel-tile-comisiones.tsx
export interface PanelTileComisionesProps {
  filas: { cobertura_camara: string | null; conteo: number; evidencia: unknown }[];
  urgencias: Map<string, { grado: string; fecha: string }>;
  maxItems?: number; // default 4 — decisión O-7, ya arbitrada
}
```

El presupuesto O-7 = 4 ya está en comisiones/sala/movimiento/ingresos y el remanente `y N más →`
se calcula contra `puntos_total`/`tabla_total` del jsonb (0081), no contra `array.length`.
La VALIDACIÓN de eso vive en 129-04, no aquí.

Contrato NUEVO que este plan crea:

```ts
// app/lib/plural.ts
export function plural(n: number, singular: string, pluralForma: string): string;
```

Literales EXACTOS de los 4 sitios de riesgo (verificados en el código de hoy):
- `panel-tile-comisiones.tsx:37` → `${n} citaciones del Senado`
- `panel-tile-urgencias.tsx:160` → `${set.size} proyectos con ${grado}`
- `panel-tile-votaciones.tsx:64` → `${item.abstencion} abstenciones, ${item.pareo} pareos`
</interfaces>

<constraints>
- PROHIBIDO escribir bajo `supabase/migrations/`. PROHIBIDO tocar `.env`, la CSP ni rotar credenciales.
- PROHIBIDO deployar en este plan (el re-deploy incondicional es 129-04).
- Máximo 3 iteraciones de corrección de diseño en total para la fase; este plan declara cuántas
  consume y deja el resto disponible.
- Ningún criterio subjetivo: fragmento DOM, test, o comparación declarada contra el baseline nombrado.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Crítica de diseño contra los baselines v13</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md</files>
  <read_first>
    - `.planning/phases/129-.../assets/129-deploy-landing-desktop.png`
    - `.planning/phases/129-.../assets/129-deploy-panel-390.png`
    - `.planning/phases/129-.../assets/129-deploy-comparar.png`
    - `.planning/spikes/assets/v13-baseline-landing.png`
    - `.planning/spikes/assets/v13-baseline-panel-mid.png`
    - `.planning/spikes/assets/v13-baseline-panel.png`
    - `.planning/phases/128-panel-ui-el-panel-se-vuelve-accionable/128-VERIFICATION.md`
    - `129-DEPLOY-EVIDENCIA.md` (para saber por qué escalón se obtuvo la captura 390px)
  </read_first>
  <action>
    Leer con la tool Read las 3 capturas del deploy y los 3 baselines (son PNG; se ven).

    Escribir `129-CRITICA.md` con una tabla de hallazgos, una fila por hallazgo, con columnas:
    `id` (C-01, C-02…), `superficie` (landing desktop / panel 390 / comparar), `delta observado
    vs baseline` (descripción del delta, no juicio), `archivo del componente responsable`,
    `veredicto` (FIX / ACEPTAR / DIFERIR), `criterio de cierre` (literal a buscar en el DOM o
    comando de test).

    Si la captura 390px vino del escalón (b) (bundle local, no deploy real), decirlo en la
    cabecera de la tabla: los hallazgos de esa superficie se leen con esa salvedad.

    Prohibido registrar hallazgos irrecuperables ("falta pulido"): un hallazgo sin componente
    responsable y sin criterio de cierre verificable se marca ACEPTAR con razón explícita, no se
    cuela como FIX. Si no hay ningún FIX, declararlo con los deltas medidos que lo sustentan, no
    por omisión.

    Cerrar con §Presupuesto de iteraciones: cuántas de las 3 se van a gastar y en qué orden.

    **Arrastre obligatorio desde `129-02` (BN-3):** añadir a §Diferidos una fila `DIFERIR` con el
    hallazgo de resiliencia SSR de `/comparar` documentado en `129-H01-DEBUG.md`
    §DIFERIDO — mecanismo (`page.tsx:246` + lectores que lanzan), propuesta de fix, y la razón del
    diferimiento (enmendar el contrato LOCKED #34 requiere pronunciamiento del operador). Su
    criterio de cierre futuro es el test de fallo de upstream sobre el harness
    `app/app/comparar/page.test.tsx:218-231`.
  </action>
  <acceptance_criteria>
    - `129-CRITICA.md` existe, con tabla que cubre las 3 superficies y las 6 columnas nombradas.
    - Cada fila `FIX` nombra un archivo existente bajo `app/` (verificable con `test -f`).
    - Cada fila `FIX` tiene `criterio de cierre` que es un literal DOM o un comando — cero adjetivos
      evaluativos ("se ve mejor", "más limpio").
    - §Presupuesto de iteraciones declara un número <= 3 y el orden.
    - §Diferidos contiene la fila del hallazgo de resiliencia SSR de `/comparar` arrastrada desde
      `129-H01-DEBUG.md`, verificable con
      `grep -oF 'pronunciamiento del operador' 129-CRITICA.md | wc -l` >= 1.
    - Si la captura 390px fue del escalón (b), la salvedad está escrita en la cabecera de la tabla.
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien && test -f 129-CRITICA.md && test "$(grep -oF 'criterio de cierre' 129-CRITICA.md | wc -l)" -ge 1</automated>
  </verify>
  <done>Crítica escrita con hallazgos accionables, responsables y criterios verificables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helper de pluralización + fix D-06 en los 4 moldes</name>
  <files>app/lib/plural.ts, app/lib/plural.test.ts, app/components/panel-tile-comisiones.tsx, app/components/panel-tile-comisiones.test.tsx, app/components/panel-tile-urgencias.tsx, app/components/panel-tile-urgencias.test.tsx, app/components/panel-tile-votaciones.tsx, app/components/panel-tile-votaciones.test.tsx</files>
  <read_first>
    - `app/components/panel-tile-comisiones.tsx:33-44` (`ORDEN_COBERTURA`)
    - `app/components/panel-tile-comisiones.test.tsx:149` (aserción del molde de cobertura, `23 citaciones del Senado`)
    - `app/components/panel-tile-urgencias.tsx:155-163` (`${set.size} proyectos con ${grado}`)
    - `app/components/panel-tile-urgencias.test.tsx`
    - `app/components/panel-tile-votaciones.tsx:63-66` (`detalleVotacion`)
    - `app/components/panel-tile-votaciones.test.tsx`
    - `app/lib/idioms-panel.ts` (single-source de stems: entender qué NO es un stem)
  </read_first>
  <behavior>
    Helper:
    - `plural(1, "citación", "citaciones")` → `"citación"`
    - `plural(0, "citación", "citaciones")` → `"citaciones"` (el cero es plural en es-CL)
    - `plural(2, "citación", "citaciones")` → `"citaciones"`
    - `plural(1, "proyecto", "proyectos")` → `"proyecto"`

    Tiles (una aserción NUEVA con n=1 por tile, más la no-regresión existente):
    - comisiones, `conteo: 1` en Senado → `1 citación del Senado`; el resto del molde intacto
      (`· 0 de la Cámara en las fuentes consultadas`). Con `conteo: 23` sigue emitiendo
      `23 citaciones del Senado` (no-regresión, el test existente NO se toca).
    - urgencias, un grado con 1 proyecto → `1 proyecto con ` (literal exacto para grep); con 2 →
      `2 proyectos con `.
    - votaciones, `abstencion: 1` → `1 abstención`; `pareo: 1` → `1 pareo`; con 2 siguen los plurales.
  </behavior>
  <action>
    Registrar PRIMERO el conteo de tests base, con el comando base que **OMITE `lib/plural.test.ts`**
    (todavía no existe; incluirlo haría fallar la corrida):
    `pnpm --filter ./app exec vitest run components/panel-tile-comisiones.test.tsx components/panel-tile-urgencias.test.tsx components/panel-tile-votaciones.test.tsx`
    Anotar la línea `Tests  N passed` en `129-CRITICA.md` §Plural. El comando POSTERIOR (el de los
    criterios) sí incluye `lib/plural.test.ts`; el delta se mide entre esos dos números.

    Crear `app/lib/plural.ts` con `plural(n, singular, pluralForma)`: devuelve `singular` si y solo
    si `n === 1`; en cualquier otro caso `pluralForma`. Sin heurística morfológica — ambas formas
    explícitas, porque `citación → citaciones` lleva tilde y ninguna regla de sufijo la acierta.

    Escribir `app/lib/plural.test.ts` con los casos del `<behavior>` y verlo fallar antes de
    implementar.

    Aplicar el helper en los 3 archivos de tile y añadir en CADA `*.test.tsx` la aserción nueva con
    n=1. No inventar sitios adicionales: los 4 moldes de riesgo están enumerados en `<interfaces>`.

    NO añadir nada a `IDIOMS_APROBADOS`: los sustantivos contados no son stems de fecha/procedencia;
    tocar ese single-source rompería el guard sin necesidad.
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app exec vitest run lib/plural.test.ts components/panel-tile-comisiones.test.tsx components/panel-tile-urgencias.test.tsx components/panel-tile-votaciones.test.tsx`
      sale 0 y reporta un número de tests **mayor** que el conteo base anotado en §Plural (registrar
      ambos números). Rutas EXPLÍCITAS: un glob sale 0 sin correr nada.
    - `grep -oF 'citaciones del Senado' app/components/panel-tile-comisiones.tsx | wc -l` == 0.
    - `grep -oF 'from "@/lib/plural"' app/components/panel-tile-comisiones.tsx | wc -l` >= 1.
    - `grep -oF '1 citación del Senado' app/components/panel-tile-comisiones.test.tsx | wc -l` >= 1.
    - `grep -oF '1 proyecto con' app/components/panel-tile-urgencias.test.tsx | wc -l` >= 1.
    - `grep -oF '1 abstención' app/components/panel-tile-votaciones.test.tsx | wc -l` >= 1.
    - `pnpm --filter ./app test` sale 0 (suite completa, sin regresiones).
    - `pnpm guards` sale 0.
    - `git diff --name-only app/lib/idioms-panel.ts` no imprime nada.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app exec vitest run lib/plural.test.ts components/panel-tile-comisiones.test.tsx components/panel-tile-urgencias.test.tsx components/panel-tile-votaciones.test.tsx</automated>
  </verify>
  <done>Concordancia de plural correcta para n=1 en los 4 moldes, con aserción propia por tile y delta de tests probado.</done>
</task>

<task type="auto">
  <name>Task 3: Higiene B26 — redactar project-ref y host en 07-01-SUMMARY.md</name>
  <files>.planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md, .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md</files>
  <read_first>
    - `.planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md` (líneas 174-182)
    - `.planning/phases/129-.../.continue-here.md` §Deuda/notas para el operador
  </read_first>
  <action>
    En `07-01-SUMMARY.md` hay TRES cosas a redactar, no una (el handoff solo mencionaba la 176):
    - línea ~176: el project-ref `<PROJECT_REF_REDACTADO>` y el host del pooler
      `<POOLER_HOST_REDACTADO>`;
    - línea ~181: el host `<SUPABASE_HOST_REDACTADO>`.
    Sustituir por marcadores (`<PROJECT_REF_REDACTADO>`, `<SUPABASE_HOST_REDACTADO>`,
    `<POOLER_HOST_REDACTADO>`) conservando el sentido de cada frase. No borrar párrafos.
    NINGUNA credencial se toca: no hay password en el archivo, y `.env` no se abre.

    En `129-CRITICA.md` §Deuda de operador escribir:
    - la recomendación explícita de **rotar** la password de la DB, con el motivo (dos agentes
      ecoaron la URL con credencial en transcripts locales; los artefactos del repo quedaron
      redactados);
    - el **número de archivos tracked que aún contienen el project-ref** fuera del alcance de esta
      fase, medido con `grep -rlF '<PROJECT_REF_REDACTADO>' --exclude-dir=node_modules --exclude-dir=.git . | wc -l`
      (al momento de planificar: 96), declarando que el alcance de la Phase 129 es SOLO
      `07-01-SUMMARY.md` y que el resto es deuda de operador.
  </action>
  <acceptance_criteria>
    - 0 ocurrencias del project-ref, del host `*.supabase.co` y del host del pooler,
      medidas con `grep -roE "$PROJECT_REF|supabase\.co|aws-1-sa-east-1\.pooler"` sobre
      `07-01-SUMMARY.md` **y sobre `.planning/phases/129-*/`** (WR-09: el criterio original
      medía un solo archivo y este plan reintroducía los literales al lado).
      `PROJECT_REF` se exporta desde `.env`; NO se transcribe en artefactos versionados.
    - El archivo sigue teniendo el mismo número de líneas (`wc -l`) que antes de la edición
      (se redactó, no se borró contenido).
    - `129-CRITICA.md` §Deuda de operador contiene la palabra `rotar` y un número de archivos
      tracked restantes obtenido por el `grep -rlF … | wc -l` citado.
    - `git status --porcelain .env` vacío.
  </acceptance_criteria>
  <verify>
    <!-- WR-09 (129-REVIEW): el criterio medía UN SOLO archivo, y este plan
         transcribía los tres literales en claro seis veces — la remediación
         quedaba auto-anulada a un directorio de distancia. El alcance del
         criterio pasa a ser el archivo objetivo MÁS la carpeta de la fase. -->
    <!-- El ref NO se transcribe aquí (eso es lo que se está remediando): se toma
         de `.env`, que no se versiona. Ejecutar con PROJECT_REF exportado. -->
    <automated>test "$(grep -roE "${PROJECT_REF:?exporta PROJECT_REF desde .env}|aws-1-sa-east-1\.pooler" .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md .planning/phases/129-*/ | wc -l)" = "0"</automated>
  </verify>
  <done>Ref, host y pooler redactados en el archivo del alcance; deuda de rotación y alcance restante cuantificados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefactos `.planning` → repo | los SUMMARY quedan versionados; host/ref de infra no deben viajar ahí |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-08 | Information Disclosure | `07-01-SUMMARY.md` (líneas 176 y 181) | mitigate | redactar ref + host + pooler; criterio `grep -oE` == 0 sobre los tres patrones |
| T-129-09 | Information Disclosure | password de la DB | transfer | fuera del alcance del agente: recomendación de rotación registrada para el operador |
| T-129-10 | Information Disclosure | project-ref en ~96 archivos tracked más | accept | fuera del alcance de esta fase; cuantificado y declarado como deuda de operador |
| T-129-11 | Tampering | copy del panel | mitigate | fix de plural con tests n=0/1/2 y aserción por tile; `idioms-panel.ts` intacto |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | ningún paquete nuevo en este plan |
</threat_model>

<verification>
- `129-CRITICA.md` con tabla de hallazgos, §Presupuesto de iteraciones, §Plural y §Deuda de operador.
- `vitest run` de las 4 rutas explícitas verde con delta de tests > 0.
- Los 3 greps de aserción n=1 (comisiones/urgencias/votaciones) dan >= 1.
- `grep -oE '<PROJECT_REF_REDACTADO>|supabase\.co|<POOLER_HOST_REDACTADO>'` == 0 en 07-01-SUMMARY.md.
- `pnpm --filter ./app test` y `pnpm guards` salen 0.
- Ningún deploy ocurrió en este plan.
</verification>

<success_criteria>
Crítica escrita, plural arreglado de forma general y probado por tile, B26 redactado. El código
queda listo para el re-deploy incondicional de 129-04.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-03-SUMMARY.md` when done
</output>
