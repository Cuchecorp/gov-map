---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - app/lib/plural.ts
  - app/lib/plural.test.ts
  - app/components/panel-tile-comisiones.tsx
  - app/components/panel-tile-comisiones.test.tsx
  - app/components/panel-tile-urgencias.tsx
  - app/components/panel-tile-votaciones.tsx
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/
  - .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "Existe una crítica de diseño escrita contra los baselines v13, con hallazgos accionables y no subjetivos"
    - "La concordancia de plural del panel es correcta para n=1 en todos los moldes con sustantivo contado"
    - "La densidad a 390px está validada con presupuesto de 4 ítems por tile y 'y N más →' respaldado por el total del jsonb"
    - "El hit de secretos preexistente en 07-01-SUMMARY.md:176 queda redactado"
  artifacts:
    - path: "app/lib/plural.ts"
      provides: "Helper general de pluralización es-CL"
      exports: ["plural"]
    - path: "app/lib/plural.test.ts"
      provides: "Tests de n=0/1/2 del helper"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md"
      provides: "Crítica Opus vs baselines + iteraciones aplicadas"
  key_links:
    - from: "app/components/panel-tile-comisiones.tsx"
      to: "app/lib/plural.ts"
      via: "import"
      pattern: "from \"@/lib/plural\""
---

<objective>
Criticar el deploy real contra los baselines v13, aplicar fixes de diseño y copy (máx 3
iteraciones), matar el bug de concordancia de plural con un helper general y testeado, validar
la densidad a 390px, y redactar el hit de secretos preexistente.

Purpose: convertir la evidencia de 129-01 en un panel que "queda bien", con cada corrección
respaldada por test o por fragmento DOM del re-deploy.
Output: `129-CRITICA.md`, fixes commiteados, capturas post-fix, re-deploy si hubo cambios de código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CONTEXT.md
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
@.planning/phases/128-panel-ui-el-panel-se-vuelve-accionable/128-VERIFICATION.md
@app/components/panel-tile-comisiones.tsx
@app/lib/idioms-panel.ts
</context>

<interfaces>
Contrato existente de los tiles (ya implementado en 128, NO re-diseñar):

```ts
// app/components/panel-tile-comisiones.tsx
export interface PanelTileComisionesProps {
  filas: { cobertura_camara: string | null; conteo: number; evidencia: unknown }[];
  urgencias: Map<string, { grado: string; fecha: string }>;
  maxItems?: number; // default 4 — decisión O-7, ya arbitrada
}
```

Presupuesto O-7 = 4 ya está implementado en comisiones/sala/movimiento/ingresos, y el remanente
`y N más →` se calcula contra `puntos_total`/`tabla_total`/`total` del jsonb (0081), no contra
`array.length`. Este plan VALIDA ese comportamiento a 390px; no lo re-implementa.

Contrato NUEVO que este plan crea:

```ts
// app/lib/plural.ts
export function plural(n: number, singular: string, plural: string): string;
```
</interfaces>

<constraints>
- PROHIBIDO escribir, editar o re-aplicar cualquier archivo bajo `supabase/migrations/`.
- PROHIBIDO tocar `.env` o rotar credenciales (la rotación es recomendación al operador, no acción).
- Máximo 3 iteraciones de corrección. Al agotarlas, se presenta al operador lo que haya.
- Ningún criterio de aceptación puede ser subjetivo ("se ve mejor"): fragmento DOM, test, o
  comparación de píxeles declarada contra el baseline nombrado.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Crítica de diseño contra los baselines v13</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md</files>
  <read_first>
    - `.planning/phases/129-.../assets/129-deploy-landing-desktop.png`
    - `.planning/phases/129-.../assets/129-deploy-panel-390.png`
    - `.planning/spikes/assets/v13-baseline-landing.png`
    - `.planning/spikes/assets/v13-baseline-panel-mid.png`
    - `.planning/spikes/assets/v13-baseline-panel.png`
    - `.planning/phases/128-*/128-VERIFICATION.md`
  </read_first>
  <action>
    Leer las 3 capturas del deploy y los 3 baselines con la tool Read (son PNG; se ven).
    Escribir `129-CRITICA.md` con una tabla de hallazgos, uno por fila, cada uno con:
    `id` (C-01, C-02…), `superficie` (landing desktop / panel 390 / comparar), `qué difiere del
    baseline` (descripción del delta, no juicio), `archivo del componente responsable`,
    `veredicto` (FIX / ACEPTAR / DIFERIR) y `criterio de cierre verificable` (fragmento DOM o test).

    Prohibido registrar hallazgos irrecuperables ("falta pulido"). Un hallazgo sin componente
    responsable identificable y sin criterio de cierre verificable se marca ACEPTAR con razón
    explícita, no se cuela como FIX.

    Priorizar los hallazgos FIX: máximo los que quepan en 3 iteraciones. Declarar el orden.
    Si la crítica no encuentra ningún FIX, decirlo explícitamente con la evidencia que lo sustenta
    (los deltas medidos contra cada baseline), no por omisión.
  </action>
  <acceptance_criteria>
    - `129-CRITICA.md` existe y contiene una tabla con al menos las 3 superficies cubiertas
      (landing desktop, panel 390, comparar), cada fila con las 6 columnas nombradas.
    - Cada fila con veredicto `FIX` nombra un archivo existente bajo `app/components/` o `app/app/`
      (verificable con `test -f`).
    - Cada fila con veredicto `FIX` tiene un `criterio de cierre` que es un literal a buscar en el
      DOM o un comando de test — cero adjetivos evaluativos.
    - El documento declara el orden de las iteraciones y cuántas se van a gastar (≤3).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md && grep -oF 'criterio de cierre' .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md | wc -l</automated>
  </verify>
  <done>Crítica escrita, hallazgos con responsable y criterio verificable, orden de iteraciones declarado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helper de pluralización + fix D-06 en los moldes del panel</name>
  <files>app/lib/plural.ts, app/lib/plural.test.ts, app/components/panel-tile-comisiones.tsx, app/components/panel-tile-comisiones.test.tsx, app/components/panel-tile-urgencias.tsx, app/components/panel-tile-votaciones.tsx</files>
  <read_first>
    - `app/components/panel-tile-comisiones.tsx` (líneas 33-44, `ORDEN_COBERTURA`)
    - `app/components/panel-tile-comisiones.test.tsx` (línea ~149, la aserción del molde de cobertura)
    - `app/components/panel-tile-urgencias.tsx` (línea ~160, `${set.size} proyectos`)
    - `app/components/panel-tile-votaciones.tsx` (línea ~64, `abstenciones` / `pareos`)
    - `app/lib/idioms-panel.ts` (single-source de stems; entender qué NO es un stem)
  </read_first>
  <behavior>
    - `plural(1, "citación", "citaciones")` → `"citación"`
    - `plural(0, "citación", "citaciones")` → `"citaciones"` (el cero es plural en es-CL)
    - `plural(2, "citación", "citaciones")` → `"citaciones"`
    - `plural(1, "proyecto", "proyectos")` → `"proyecto"`
    - Tile comisiones con `conteo: 1` en Senado → el molde emite `1 citación del Senado`
      (no `1 citaciones`), y el resto del molde de cobertura queda intacto:
      `· 0 de la Cámara en las fuentes consultadas`.
    - Tile comisiones con `conteo: 23` → sigue emitiendo `23 citaciones del Senado` (no-regresión).
    - Tile urgencias con 1 proyecto → `1 proyecto`; con 2 → `2 proyectos`.
    - Tile votaciones con `abstencion: 1` → `1 abstención`; `pareo: 1` → `1 pareo`.
  </behavior>
  <action>
    Crear `app/lib/plural.ts` exportando `plural(n, singular, pluralForma)`: devuelve `singular`
    si y solo si `n === 1`, en cualquier otro caso `pluralForma`. Sin heurística morfológica —
    ambas formas explícitas, porque `citación → citaciones` lleva tilde que ninguna regla
    genérica de sufijo acierta.

    Escribir `app/lib/plural.test.ts` PRIMERO con los casos del bloque `<behavior>` y verlo fallar.

    Aplicar el helper en los 4 sitios de riesgo detectados (no inventar otros): el molde de
    `ORDEN_COBERTURA` del tile comisiones, `${set.size} proyectos` en urgencias, y `abstenciones`
    / `pareos` en votaciones. Ampliar `panel-tile-comisiones.test.tsx` con el caso `conteo: 1`
    manteniendo intacta la aserción existente de `23 citaciones del Senado`.

    NO añadir nada a `IDIOMS_APROBADOS`: los sustantivos contados no son stems de fecha/procedencia;
    tocar ese single-source rompería el guard sin necesidad. Si `pnpm --filter ./app guards` se
    queja de algún literal nuevo, ese es el momento de revisar — no antes.
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app exec vitest run lib/plural.test.ts components/panel-tile-comisiones.test.tsx components/panel-tile-urgencias.test.tsx components/panel-tile-votaciones.test.tsx`
      pasa, y su salida reporta un número de tests > 0 (rutas EXPLÍCITAS, jamás glob: un
      `vitest run lib/*plural*` sale 0 sin correr nada).
    - `grep -oF 'citaciones del Senado' app/components/panel-tile-comisiones.tsx | wc -l` == 0
      (el literal plural fijo ya no existe en el molde).
    - `grep -oF 'from "@/lib/plural"' app/components/panel-tile-comisiones.tsx | wc -l` >= 1.
    - `pnpm --filter ./app test` verde (suite completa, sin regresiones).
    - `pnpm guards` verde (11 app + 6 packages).
    - `git diff --name-only app/lib/idioms-panel.ts` vacío (no se tocó el single-source de stems).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app exec vitest run lib/plural.test.ts components/panel-tile-comisiones.test.tsx components/panel-tile-urgencias.test.tsx components/panel-tile-votaciones.test.tsx</automated>
  </verify>
  <done>Concordancia de plural correcta para n=1 en los 4 moldes, con tests, sin tocar idioms-panel.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Fixes de diseño, validación de densidad 390px, higiene B26 y re-deploy</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md, .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/, .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md</files>
  <read_first>
    - `129-CRITICA.md` (los hallazgos FIX y su orden)
    - Cada archivo nombrado como responsable en las filas FIX
    - `.planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md` (línea 176)
    - `app/components/panel-tile-sala.tsx` y `panel-tile-movimiento.tsx` (cómo se calcula el remanente)
    - `docs/deploy-cloudflare.md`
  </read_first>
  <action>
    **Fixes de diseño.** Aplicar los hallazgos FIX de `129-CRITICA.md` en el orden declarado,
    respetando el tope de 3 iteraciones (cada iteración = tanda de fixes + re-deploy + re-captura).
    Cada fix con su test o su aserción DOM. Commitear por iteración; NO usar `--amend`.

    **Densidad 390px.** Sobre la captura 390 del deploy vigente, verificar que ningún tile del
    panel muestra más de 4 ítems y que, donde el jsonb declara un total mayor, aparece el
    remanente `y N más →` con N == total − mostrados. Extraer del DOM (vía `evaluate_script` +
    `textContent`) los literales `y N más →` presentes y contrastar N contra el `total`
    correspondiente consultado en el HTML/RSC. Registrar la tabla tile → mostrados → total →
    N declarado en `129-CRITICA.md` §Densidad 390px. Si algún N no cuadra con el total del jsonb,
    es un FIX (remanente deshonesto), no una observación.

    **Higiene B26.** En `07-01-SUMMARY.md` línea ~176, redactar el host y el project-ref de
    Supabase (sustituir por marcadores tipo `<SUPABASE_HOST_REDACTADO>` /
    `<PROJECT_REF_REDACTADO>`) conservando el sentido de la frase. NO tocar `.env`, NO intentar
    rotar nada. Dejar anotada en `129-CRITICA.md` §Deuda de operador la recomendación explícita
    de rotar la password de la DB, con el motivo (dos agentes ecoaron la URL con credencial en
    transcripts locales).

    **Re-deploy.** Si hubo CUALQUIER cambio bajo `app/`, re-deployar siguiendo exactamente el
    procedimiento D-01 de `129-01-PLAN.md` Task 1 (robocopy → Docker `node:22-slim` → docker cp →
    wrangler global de AppData, `MSYS_NO_PATHCONV=1`), esperar la ventana de propagación y
    re-capturar las 3 superficies como `129-final-landing-desktop.png`, `129-final-panel-390.png`,
    `129-final-comparar.png` con los mismos gotchas de instrumento (path absoluto, sleep 8-10 s,
    verificar `test -s` tras cada shot). Registrar el nuevo `version-id` en `129-CRITICA.md`.
    Si NO hubo cambios bajo `app/`, declararlo explícitamente y reutilizar las capturas de 129-01
    copiándolas con el nombre `129-final-*` para que el checkpoint tenga un juego único.
  </action>
  <acceptance_criteria>
    - Toda fila `FIX` de `129-CRITICA.md` tiene columna `estado` en {`CERRADO`, `AGOTADAS ITERACIONES`}
      y, si `CERRADO`, la evidencia citada (comando de test o literal DOM encontrado).
    - `129-CRITICA.md` §Densidad 390px contiene la tabla tile/mostrados/total/N con al menos una
      fila por tile del panel, y ninguna fila con `N != total - mostrados`.
    - `grep -oF 'supabase.co' .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md | wc -l` == 0.
    - `129-CRITICA.md` §Deuda de operador contiene la recomendación de rotación de la password de
      la DB.
    - Existen `assets/129-final-landing-desktop.png`, `assets/129-final-panel-390.png`,
      `assets/129-final-comparar.png`, los tres con `test -s` (tamaño > 0).
    - Si hubo cambios bajo `app/`: `129-CRITICA.md` registra un `version-id` distinto al de
      `129-DEPLOY-EVIDENCIA.md`, y `curl -s -o /dev/null -w "%{http_code}"` sobre `/` da 200.
    - `git status --porcelain supabase/migrations` vacío. `.env` no aparece en `git status`.
    - `pnpm --filter ./app test` y `pnpm guards` verdes tras la última iteración.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -oF 'supabase.co' .planning/milestones/v1.0-phases/07-b-squeda-sem-ntica-fichas-estructuradas/07-01-SUMMARY.md | wc -l)" = "0" && cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets && test -s 129-final-landing-desktop.png && test -s 129-final-panel-390.png && test -s 129-final-comparar.png</automated>
  </verify>
  <done>Fixes aplicados o iteraciones agotadas con estado declarado, densidad 390px tabulada, B26 redactado, capturas finales en assets/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefactos .planning → repo público | los SUMMARY quedan versionados en git; host/ref de infra no deben viajar ahí |
| host → Cloudflare Worker | el re-deploy publica código a producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Information Disclosure | `.planning/milestones/v1.0-phases/07-*/07-01-SUMMARY.md:176` | mitigate | redactar host y project-ref; verificado por conteo `supabase.co` == 0 |
| T-129-05 | Information Disclosure | password de la DB (ecoada en transcripts locales) | transfer | fuera del alcance del agente: recomendación de rotación registrada para el operador |
| T-129-06 | Tampering | copy del panel | mitigate | fix de plural con test de n=0/1/2; `idioms-panel.ts` intacto (verificado por `git diff --name-only` vacío) |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | ningún paquete nuevo se instala en este plan |
</threat_model>

<verification>
- `129-CRITICA.md` con hallazgos, estado, densidad 390px y deuda de operador.
- `pnpm --filter ./app exec vitest run lib/plural.test.ts …` (rutas explícitas) verde con tests > 0.
- `pnpm --filter ./app test` y `pnpm guards` verdes.
- Conteo de `supabase.co` en 07-01-SUMMARY.md == 0.
- 3 capturas `129-final-*` con tamaño > 0.
- `supabase/migrations` sin cambios.
</verification>

<success_criteria>
El panel del deploy vigente pasó por crítica contra baseline, los FIX viables están cerrados con
evidencia, la concordancia de plural está arreglada de forma general y testeada, la densidad a
390px está tabulada con remanentes honestos, y el hit de secretos está redactado.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-02-SUMMARY.md` when done
</output>
