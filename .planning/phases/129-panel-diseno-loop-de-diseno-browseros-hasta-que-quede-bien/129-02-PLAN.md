---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - app/app/comparar/page.tsx
  - app/app/comparar/page.test.tsx
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-H01-DEBUG.md
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "La tasa de fallo de /comparar?a=D1178&b=D1099 está cuantificada sobre N>=20 requests y publicada tal cual"
    - "La causa raíz del 500 intermitente está identificada por lectura de código con archivo:línea"
    - "Un fallo transitorio de UNA RPC ya no tumba la página entera: degrada al eje afectado, declarado honestamente"
    - "Existe un test de regresión que simula el fallo del upstream y prueba que la página sigue renderizando"
  artifacts:
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-H01-DEBUG.md"
      provides: "Histograma de códigos, causa raíz con archivo:línea, fix y su justificación"
    - path: "app/app/comparar/page.test.tsx"
      provides: "Test de regresión con upstream fallando"
  key_links:
    - from: "app/app/comparar/page.tsx"
      to: "las 7 RPCs del path SSR"
      via: "Promise.all sin aislamiento de fallo"
      pattern: "Promise.all"
---

<objective>
Cerrar H-01 como lo que realmente es: un **500 SSR intermitente** en `/comparar?a=D1178&b=D1099`,
no un error boundary post-hidratación. Cuantificar la tasa, encontrar la causa raíz por lectura de
código, y aplicar un fix defensivo proporcional con test de regresión.

Purpose: el premortem observó un `500` real con `<html id="__next_error__">` (falla en Server
Component). Una medición posterior dio **8/8 → 200**. Es intermitente: exigir "reproducir el 500"
como criterio dejaría la fase colgada indefinidamente. El artefacto que cierra H-01 es el TEST,
no el curl.
Output: `129-H01-DEBUG.md` + fix + test de regresión.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CONTEXT.md
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
@app/app/comparar/page.tsx
@app/app/comparar/page.test.tsx
@app/app/error.tsx
</context>

<hechos_verificados>
- El `500` fue REAL (una observación, body `<html id="__next_error__">` ⇒ falla en Server
  Component), pero **NO es determinista**: una medición posterior dio 8/8 → 200, cero
  `__next_error__`, 109 KB de HTML. Sin params → 200.
- El path SSR de `/comparar` lee **7 RPCs** y cada lector **LANZA** ante cualquier error
  (`app/app/comparar/page.tsx:81-83`, `:92-97`, `:108-112`, `:124-128`, `:510-517`), y seis de
  ellas se disparan juntas en un `Promise.all` (`:246`). Un solo fallo transitorio del upstream
  tumba la página ENTERA a 500.
- **Regla LOCKED que NO se puede violar (#34, `page.tsx:74-75`)**: "error ≠ vacío". Un vacío
  honesto es `[]` SIN error. PROHIBIDO que el fix degrade un error a "sin relaciones" — eso
  fabricaría un hecho negativo. La degradación permitida es declarar el FALLO de ese eje
  ("no pudimos cargar este eje"), que no es lo mismo que afirmar ausencia.
- El boundary aplicable NO es propio de `/comparar`: es el root `app/app/error.tsx`, con copy de
  portada. Literales exactos para `grep -oF`: `No pudimos cargar la portada` y
  `Esto es una falla técnica`.
- `wrangler` desde el host requiere `XDG_CONFIG_HOME=C:/Users/Carlo/AppData/Roaming/xdg.config`
  (el OAuth vive ahí) y `MSYS_NO_PATHCONV=1`; usar `wrangler.cmd` de `%APPDATA%/npm`.
</hechos_verificados>

<constraints>
- PROHIBIDO escribir bajo `supabase/migrations/`. PROHIBIDO tocar `.env` o la CSP.
- PROHIBIDO escribir "no reproducible" a secas: la evidencia es una TASA observada.
- PROHIBIDO violar #34 degradando un error a vacío.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Cuantificar la flakiness y hallar la causa raíz por lectura de código</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-H01-DEBUG.md</files>
  <read_first>
    - `app/app/comparar/page.tsx` (completo: los 7 lectores y el `Promise.all` de :246)
    - `app/lib/supabase.ts` (`createServerSupabase` — ver si hay timeout/retry configurado)
    - `app/app/error.tsx`
    - `129-DEPLOY-EVIDENCIA.md` (version-id vigente)
  </read_first>
  <action>
    **(1) Control de flakiness cuantificado.** Correr un bucle seriado de **N >= 20** requests a
    `https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1178&b=D1099`, registrando el
    código HTTP de cada uno (`curl -s -o /dev/null -w "%{http_code}\n"`), y el MISMO control sobre
    `/` como referencia. Publicar en `129-H01-DEBUG.md` §Tasa observada el **histograma completo**
    (p.ej. `200: 20, 500: 0`) tal cual salió, con el version-id contra el que se midió y la marca
    de tiempo. Se prohíbe redondear el resultado a una conclusión: la tasa ES el hallazgo.

    **(2) Causa raíz por lectura de código, no por reproducción.** Recorrer el path SSR de
    `/comparar` y listar en §Causa raíz, con `archivo:línea`, cada fetch/RPC que ante un fallo
    transitorio del upstream produciría un 500: los que no tienen timeout, ni retry, ni manejo de
    fallo que aísle el eje. Como mínimo deben quedar nombrados los lectores de :81, :92, :108,
    :124 y :510, y el `Promise.all` de :246 como amplificador (basta que UNA de seis reviente).
    Concluir con el mecanismo: "fallo transitorio de una RPC → `throw` → sin boundary de ruta →
    boundary raíz → 500 `__next_error__`".

    **(3) `wrangler tail` como apoyo, NO como criterio.** Correr `wrangler tail` durante una
    ventana de recargas y, si captura un error, pegarlo en §Log capturado. Si NO se reproduce en
    la ventana, escribirlo así ("ventana de N minutos sin reproducción; la tasa observada fue X") —
    la ausencia de log NO bloquea la tarea.
  </action>
  <acceptance_criteria>
    - §Tasa observada contiene el histograma de **>= 20** requests a `/comparar?a=D1178&b=D1099`
      con conteo por código, más el control sobre `/`, el version-id y el timestamp.
    - `grep -oF 'no reproducible' 129-H01-DEBUG.md | wc -l` == 0 (la frase prohibida no aparece
      como conclusión suelta).
    - §Causa raíz nombra al menos 5 sitios con formato `app/app/comparar/page.tsx:<línea>` y
      explicita el rol del `Promise.all` de la línea 246.
    - §Causa raíz enuncia el mecanismo completo hasta el `500 __next_error__`.
    - §Log capturado existe, con el log o con la declaración de ventana sin reproducción.
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien && test -f 129-H01-DEBUG.md && test "$(grep -oF 'no reproducible' 129-H01-DEBUG.md | wc -l)" = "0" && test "$(grep -oE 'app/app/comparar/page\.tsx:[0-9]+' 129-H01-DEBUG.md | wc -l)" -ge 5</automated>
  </verify>
  <done>Tasa publicada tal cual, causa raíz con archivo:línea, mecanismo enunciado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix defensivo proporcional + test de regresión con upstream fallando</name>
  <files>app/app/comparar/page.tsx, app/app/comparar/page.test.tsx, .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-H01-DEBUG.md</files>
  <read_first>
    - `129-H01-DEBUG.md` §Causa raíz (de la Task 1)
    - `app/app/comparar/page.tsx:74-75` (la regla #34 "error ≠ vacío", LOCKED)
    - `app/app/comparar/page.test.tsx` (los mocks existentes de RPC y el caso "un error de RPC LANZA")
    - `app/components/relaciones-eje-comparar.tsx` (dónde se renderiza cada eje)
  </read_first>
  <behavior>
    - Si UNA de las RPCs de eje falla (la RPC devuelve `error`), la página **sigue renderizando 200**
      con los ejes sanos, y el eje afectado declara el FALLO ("no pudimos cargar este eje"),
      NUNCA "sin relaciones" ni una lista vacía (eso violaría #34).
    - Si la MISMA RPC falla en un reintento acotado, el fallo del eje queda declarado igual: sin
      loop infinito, sin espera larga (el retry es acotado y con tope).
    - Si la RPC del roster de selectores (`parlamentarios_publico_v2`, `page.tsx:81`) falla, el
      comportamiento sigue siendo el actual (esa sí es estructural para la ruta) — o se declara
      explícitamente en el debug por qué se decidió distinto.
    - Un eje que devuelve `[]` SIN error sigue mostrando el vacío honesto de siempre — el fix NO
      puede confundir vacío con fallo (control positivo obligatorio en el test).
    - `/comparar` sin params sigue devolviendo selectores vacíos, jamás 404 ni 500 (no-regresión
      del gotcha LOCKED `force-dynamic` + `searchParams`).
  </behavior>
  <action>
    Escribir PRIMERO en `app/app/comparar/page.test.tsx` los casos del bloque `<behavior>`, con el
    mock de Supabase devolviendo `{ data: null, error: {...} }` para UNA RPC de eje y datos sanos
    para el resto, y verlos fallar.

    Luego implementar en `app/app/comparar/page.tsx` el aislamiento de fallo por eje: envolver cada
    lector de eje de modo que su fallo se capture y se propague como un estado `fallo` de ESE eje
    (no como `[]`), con un retry acotado sobre el error transitorio, y renderizar ese estado como
    declaración de fallo del eje. La regla #34 se preserva porque el estado `fallo` es distinto del
    estado `vacío` y se declara distinto en la UI.

    Mantener intacto el caso de test existente que asegura que un error de RPC no se degrada a
    "sin relaciones" — si ese test asertaba que la página LANZA, actualizarlo conscientemente y
    dejar registrada la razón en `129-H01-DEBUG.md` §Decisión de diseño (#34 sigue respetado: el
    error se DECLARA, no se silencia).

    Registrar en `129-H01-DEBUG.md` §Fix: qué se cambió, por qué es proporcional, y el nombre exacto
    de los tests de regresión.
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app exec vitest run app/comparar/page.test.tsx` pasa y su salida reporta un
      número de tests **mayor** que el conteo registrado ANTES de tocar nada (registrar ambos
      números en §Fix). Ruta EXPLÍCITA: un glob sale 0 sin correr nada.
    - Existe al menos un test cuyo nombre menciona el fallo del upstream y que, con una RPC de eje
      en error, asserta que el render NO lanza y que el HTML contiene la declaración de fallo del eje.
    - Existe el **control positivo apareado**: un test con eje `[]` SIN error que asserta el vacío
      honesto de siempre (prueba que fallo y vacío no se confundieron).
    - `pnpm --filter ./app test` verde (suite completa).
    - `pnpm guards` sale 0.
    - `129-H01-DEBUG.md` §Fix nombra los tests y §Decisión de diseño justifica cualquier cambio al
      test previo de #34.
    - `git status --porcelain supabase/migrations` vacío.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app exec vitest run app/comparar/page.test.tsx</automated>
  </verify>
  <done>Un fallo transitorio de una RPC ya no tumba /comparar; el test de regresión lo prueba y #34 sigue respetado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker SSR → Supabase RPC | fallo transitorio del upstream cruza esta frontera y hoy se convierte en 500 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05 | Denial of Service | `/comparar` SSR | mitigate | aislamiento de fallo por eje + retry acotado; test de regresión con upstream en error |
| T-129-06 | Repudiation | copy de los ejes | mitigate | estado `fallo` distinto de `vacío` (#34): control positivo apareado en el test |
| T-129-07 | Information Disclosure | mensajes de error de RPC | mitigate | la declaración de fallo del eje no expone `error.message` crudo al usuario |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | ningún paquete nuevo en este plan |
</threat_model>

<verification>
- §Tasa observada con histograma de >= 20 requests; frase "no reproducible" ausente.
- §Causa raíz con >= 5 referencias `page.tsx:<línea>`.
- `vitest run app/comparar/page.test.tsx` verde con delta de tests > 0.
- Test de fallo de upstream + control positivo de vacío honesto, ambos presentes.
- `pnpm --filter ./app test` y `pnpm guards` salen 0.
</verification>

<success_criteria>
H-01 cerrado por TEST (no por curl): la tasa está publicada, la causa raíz nombrada con
archivo:línea, y un fallo transitorio de upstream ya no produce un 500.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-02-SUMMARY.md` when done
</output>
