---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "Existe un deploy NUEVO del HEAD, con version-id distinto del preexistente b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7"
    - "El estado PRE-deploy quedó medido y registrado, para no acreditar como logro del deploy lo que ya pasaba"
    - "B-02 cerrado sobre el DOM del deploy: cero '(sin materia)', con control positivo apareado"
    - "Existen capturas BrowserOS del deploy nuevo, cada una probada NO-vacía por contenido, no solo por tamaño"
  artifacts:
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md"
      provides: "Baseline pre-deploy, version-id nuevo, conteos B-02, escalera 390px usada"
      contains: "b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-landing-desktop.png"
      provides: "Captura del panel en desktop sobre el deploy nuevo"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-panel-390.png"
      provides: "Captura del panel a 390px por la escalera de fallback declarada"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-comparar.png"
      provides: "Captura de /comparar?a=D1178&b=D1099 con contenido real"
  key_links:
    - from: "HTML servido por el Worker"
      to: "tiles del panel 128"
      via: "grep -o sobre el HTML de 1 línea"
      pattern: "Comisiones citadas esta semana"
---

<objective>
Producir un deploy NUEVO del HEAD por el runbook que realmente funciona en esta máquina, medir el
estado PRE-deploy para no acreditar falsos logros, cerrar B-02 sobre el DOM servido, y capturar las
3 superficies con anti-falso-verde por contenido.

Purpose: el handoff afirmaba que no había deploy; es FALSO — `C:/Temp/obs-build/deploy.log` (30-jul
16:22) terminó en `Current Version ID: b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`. Sin un criterio de
version-id nuevo, esta fase entera puede salir verde sin deployar nada.
Output: `129-DEPLOY-EVIDENCIA.md` + 3 PNG en `assets/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CONTEXT.md
@.planning/milestones/v10.0-phases/104-vsim-p3b-flip-on-deploy-integrado/104-02-PLAN.md
@scripts/bros-cli.mjs
@docker-cf-build.sh
</context>

<hechos_verificados>
Medidos en vivo hoy (premortem). NO re-litigar, NO asumir lo contrario:

- **HEAD real = `982a4ad`** (no `46d4106`).
- **El sitio YA está deployado** con el panel 128. Version-id preexistente:
  `b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`. Los 3 conteos de B-02 YA pasan verde contra el deploy
  vivo, sin build ni deploy. Por eso el baseline pre-deploy es obligatorio.
- **El iframe same-origin a 390px está MUERTO**: la CSP ENFORCED (SEC-02, v9.0) manda
  `frame-ancestors 'none'` + `x-frame-options: DENY` ⇒ `f.contentDocument === null`. El precedente
  v8.0 es ANTERIOR a esa CSP. **PROHIBIDO tocar la CSP** (lockdown SEC-02, no es decisión de esta fase).
- **BrowserOS no tiene control de viewport**: `create_window` solo acepta `hidden`; `save_screenshot`
  solo `page/path/cwd/format/quality/fullPage`.
- **`evaluate_script` NO acepta `script`** — el parámetro es **`expression`** (required: `page`,
  `expression`). Pasar JSON con comillas por Git Bash a `bros-cli call` rompe con
  `Unterminated string in JSON` ⇒ escribir el JSON al scratchpad y usar `"$(cat args.json)"`.
  Los Page IDs incrementan por sesión: parsear siempre `Page ID: N`.
- **Credenciales**: `.env` NO tiene `CLOUDFLARE_API_TOKEN`; no existe `~/.wrangler` ni
  `%APPDATA%/.wrangler` ni `%LOCALAPPDATA%/.wrangler`. El OAuth real vive en
  `C:/Users/Carlo/AppData/Roaming/xdg.config/.wrangler/config/default.toml`. El deploy PROBADO es
  **dentro del contenedor**. `docker-cf-build.sh` SÍ está en el repo; `docker-deploy.sh` NO —
  hay que re-escribirlo en el mirror.
- **El mirror `C:/Temp/obs-build` está SUCIO**: ya contiene `.pnpm-store/`, `app/.open-next/worker.js`
  del 16:19 y `.env`. `robocopy /MIR` con `/XD .pnpm-store` **NO purga** el store viejo (los
  excluidos se ignoran, no se borran) ⇒ riesgo de deployar un bundle VIEJO.
- **`Por materia` es un cero ESTRUCTURAL**, no medido: el tile no se monta
  (`app/components/panel-actualidad.tsx:51`). El negativo con carne es `(sin materia)`.
</hechos_verificados>

<constraints>
- PROHIBIDO escribir, editar o re-aplicar cualquier archivo bajo `supabase/migrations/`.
- PROHIBIDO tocar `.env`, la CSP, `frame-ancestors` o `X-Frame-Options`.
- 500s en la ventana de propagación de 10-30 s tras el deploy NO son fallo: reintentar.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Medir el estado PRE-deploy (baseline honesto)</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md</files>
  <read_first>
    - `.planning/phases/129-.../129-CONTEXT.md`
    - `C:/Temp/obs-build/deploy.log` (últimas líneas: el version-id preexistente)
  </read_first>
  <action>
    ANTES de tocar nada, crear `129-DEPLOY-EVIDENCIA.md` con la sección `## Baseline PRE-deploy` y
    registrar, con comando y salida verbatim:
    1. `git log -1 --format=%H` (debe empezar por `982a4ad` o posterior).
    2. Las últimas 3 líneas de `C:/Temp/obs-build/deploy.log`, incluyendo
       `Current Version ID: b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`.
    3. `curl -s https://observatorio-congreso.thevalis.workers.dev/ > /tmp/p129-pre.html` y los
       conteos `grep -oF '(sin materia)' /tmp/p129-pre.html | wc -l` y
       `grep -oF 'Comisiones citadas esta semana' /tmp/p129-pre.html | wc -l`.
    4. `ls -l C:/Temp/obs-build/app/.open-next/worker.js` (mtime del bundle viejo).

    Escribir en el documento, textualmente, la frase: "El baseline pre-deploy YA pasaba los conteos
    de B-02; el cero de B-02 NO se acredita como logro del deploy nuevo." Esto evita que la fase se
    auto-felicite por un estado preexistente.
  </action>
  <acceptance_criteria>
    - `129-DEPLOY-EVIDENCIA.md` §Baseline PRE-deploy existe y contiene el literal
      `b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`.
    - Contiene los 2 conteos pre-deploy con el comando que los produjo.
    - Contiene el mtime del `worker.js` viejo del mirror.
    - Contiene textualmente la frase de no-acreditación citada en el `<action>`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -oF 'b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7' .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md | wc -l)" -ge 1</automated>
  </verify>
  <done>El estado previo está medido y registrado antes de cualquier build.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy NUEVO por el runbook 104-02 (deploy dentro del contenedor)</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md</files>
  <read_first>
    - `.planning/milestones/v10.0-phases/104-vsim-p3b-flip-on-deploy-integrado/104-02-PLAN.md` (§Task 1, el molde VERBATIM)
    - `.planning/milestones/v10.0-phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md` (§Runbook reproducible — origen de los gotchas)
    - `docker-cf-build.sh` (raíz del repo)
    - `app/AGENTS.md`
    - `129-DEPLOY-EVIDENCIA.md` (baseline de Task 1)
  </read_first>
  <action>
    Reproducir el runbook 104-02 §Task 1, que es el que produjo `b69f2ec2`. Pasos, en orden:

    1. **Purga explícita del mirror en PowerShell (NO git-bash)**: `Remove-Item -Recurse -Force`
       sobre `C:/Temp/obs-build/node_modules`, `C:/Temp/obs-build/.pnpm-store` y
       `C:/Temp/obs-build/app/.open-next`. Esto es obligatorio: `robocopy /MIR` con `/XD` NO purga
       los excluidos, solo los ignora — sin esta purga se puede deployar el bundle del 16:19.
    2. **Espejar** el repo con `robocopy /MIR` desde PowerShell, excluyendo
       `node_modules`, `.open-next`, `.next`, `.git`, `.turbo`, `dist`, `coverage`, `.planning`,
       `.pnpm-store`.
    3. **Re-escribir `C:/Temp/obs-build/docker-deploy.sh`** (NO está en el repo): `corepack enable`
       + `corepack prepare pnpm@11 --activate` + `pnpm config set dangerouslyAllowAllBuilds true` +
       `export XDG_CONFIG_HOME=/root/.config` + `export WRANGLER_HOME=/root/.config/.wrangler` +
       `cd /work/app && CI=true pnpm run deploy 2>&1 | tee /work/deploy.log`.
    4. Registrar un stamp de inicio del build: `date +%s` y `touch /tmp/129-build-start`.
    5. **Correr el contenedor** `node:22-slim` con `MSYS_NO_PATHCONV=1`, montando
       `-v "C:\Temp\obs-build:/work"` y el OAuth del host
       `-v "C:\Users\Carlo\AppData\Roaming\xdg.config\.wrangler:/root/.config/.wrangler"`,
       `-w /work`, ejecutando `bash -lc "chmod +x /work/docker-deploy.sh && /work/docker-deploy.sh"`.
       El build y el deploy ocurren AMBOS dentro del contenedor: `opennextjs-cloudflare` es
       devDependency y no está en el PATH del host.
    6. **ANTES de acreditar el deploy**, verificar que `C:/Temp/obs-build/app/.open-next/worker.js`
       tiene mtime POSTERIOR a `/tmp/129-build-start` (`test worker.js -nt /tmp/129-build-start`).
       Si no lo es, el build no corrió: abortar, no deployar.
    7. Extraer de `C:/Temp/obs-build/deploy.log` el nuevo `Current Version ID`.
    8. Confirmar el sitio: `curl -s -o /dev/null -w "%{http_code}"` sobre `/`; si 5xx, esperar 30 s
       y reintentar hasta 3 veces (ventana de propagación).

    Si en algún momento hace falta correr `wrangler` desde el HOST (p.ej. `deployments list`),
    prefijar SIEMPRE con `XDG_CONFIG_HOME=C:/Users/Carlo/AppData/Roaming/xdg.config MSYS_NO_PATHCONV=1`
    y usar `wrangler.cmd` de `%APPDATA%/npm` — el `wrangler` del PATH es un shim de miniconda.

    Registrar en `129-DEPLOY-EVIDENCIA.md` §Deploy nuevo: comandos exactos, nuevo version-id,
    mtime del `worker.js`, y el código HTTP final.
  </action>
  <acceptance_criteria>
    - El nuevo `Current Version ID` registrado es **distinto de `b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`**.
      Un version-id igual = deploy no ocurrido = tarea fallida.
    - `test C:/Temp/obs-build/app/.open-next/worker.js -nt /tmp/129-build-start` es verdadero (el
      bundle desplegado se construyó en esta corrida, no es el del 16:19).
    - `curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/`
      imprime `200`.
    - `129-DEPLOY-EVIDENCIA.md` §Deploy nuevo contiene: comandos, version-id nuevo, mtime del
      worker.js y el status HTTP.
    - `git status --porcelain supabase/migrations` no imprime nada.
    - `git status --porcelain .env` no imprime nada.
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://observatorio-congreso.thevalis.workers.dev/ | grep -x 200</automated>
  </verify>
  <done>Deploy nuevo con version-id distinto del preexistente y bundle construido en esta corrida.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar B-02 y capturar las 3 superficies (escalera 390px + anti-blanco)</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md, .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/</files>
  <read_first>
    - `scripts/bros-cli.mjs` (cabecera: atajos y gotchas)
    - `app/components/panel-actualidad.tsx` (línea ~51: el tile materia NO se monta — cero estructural)
    - `app/components/panel-actualidad.test.tsx` (§"filtra agrupacion_materia EXPLÍCITAMENTE")
    - `app/app/error.tsx` (literales del boundary raíz)
    - `129-DEPLOY-EVIDENCIA.md`
  </read_first>
  <action>
    **B-02.** Bajar el HTML del deploy NUEVO a `/tmp/p129.html` (`curl -s ... > archivo`), nunca
    pipear a grep. El HTML del Worker viene en UNA línea ⇒ `grep -c` topa en 1: usar SIEMPRE
    `grep -oF <literal> archivo | wc -l`. Nunca combinar `-i` con `-F` (da 0 siempre); nunca `.`
    como comodín (no matchea `ó` multibyte); nunca `grep -q` bajo `pipefail` (exit 141).
    Tres conteos:
    - `(sin materia)` → 0 (negativo CON carne: el literal existiría si el tile se montara).
    - `Por materia` → 0, declarado en la evidencia como **cero ESTRUCTURAL** (el tile no se monta,
      `panel-actualidad.tsx:51`), no como cero medido.
    - `Comisiones citadas esta semana` → >= 1 (control positivo apareado; un 0 aquí invalida los
      negativos por cero vacuo y bloquea la tarea). Un conteo `2` es normal: HTML + payload RSC.
    Registrar los 3 conteos con su comando en §B-02, y contrastarlos contra el baseline pre-deploy.

    **Capturas.** Tres PNG con path ABSOLUTO Windows con forward-slashes a
    `C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/`.
    Tras `open`, parsear `Page ID: N` y esperar 8-10 s (SSR + hidratación). Scrollear vía
    `evaluate_script` (parámetro **`expression`**, no `script`; JSON al scratchpad y
    `"$(cat args.json)"`) — el tool `scroll` puede no mover.

    1. `129-deploy-landing-desktop.png` — `/` en desktop.
    2. `129-deploy-panel-390.png` — **escalera de fallback, en este orden, parando en la primera
       que funcione, y DECLARANDO en la evidencia cuál se usó**:
       (a) `create_window` + `evaluate_script` con `window.resizeTo(406, 900)` (las ventanas creadas
           por script sí son redimensionables) y verificación previa por `evaluate_script` de que
           `window.innerWidth === 390` ANTES de capturar;
       (b) si (a) falla: servir el MISMO bundle buildeado en local (preview / `wrangler dev`) y
           capturar ahí, **declarando explícitamente en la evidencia y en el SUMMARY que la captura
           390px NO es del deploy real** y por qué (CSP `frame-ancestors 'none'` mató el iframe);
       (c) **PROHIBIDO**: envolver el panel en un `<div style="width:390px">` del top-level — las
           media queries de Tailwind evalúan el viewport, no el contenedor: la captura mentiría.
       El iframe same-origin está descartado de entrada (CSP). No tocar la CSP.
    3. `129-deploy-comparar.png` — `/comparar?a=D1178&b=D1099`.

    **Anti-falso-verde de capturas (obligatorio para las 3).** `bros-cli` sale 0 tras
    `CDP request timeout`, y un PNG en blanco pesa >0 ⇒ `test -s` NO prueba nada por sí solo.
    Para cada captura, además de `test -s`, extraer por `evaluate_script` + `textContent`
    (JAMÁS `innerText`) un literal de contenido REAL de lo que se capturó, y pegarlo en la evidencia:
    - landing y 390: el literal `Comisiones citadas esta semana`;
    - comparar: los **apellidos de los dos parlamentarios** (D1178 y D1099) — y verificar que el
      textContent NO contiene los literales del boundary raíz `app/app/error.tsx`:
      `No pudimos cargar la portada` ni `Esto es una falla técnica`.
    Cerrar cada página con `close`.
  </action>
  <acceptance_criteria>
    - §B-02 contiene los 3 conteos con comando; `(sin materia)` == 0, `Por materia` == 0 (rotulado
      como cero ESTRUCTURAL con la cita `panel-actualidad.tsx:51`), `Comisiones citadas esta semana` >= 1.
    - Los 3 PNG existen bajo `assets/` y cada uno pasa `test -s`.
    - Para cada uno de los 3 PNG, la evidencia incluye el fragmento `textContent` capturado con el
      literal exigido. Un `test -s` sin fragmento de contenido NO acredita la captura.
    - La evidencia declara cuál escalón (a) o (b) se usó para los 390px; si fue (b), incluye la
      frase explícita de que la captura NO es del deploy real y el motivo.
    - El textContent de `/comparar` contiene los apellidos de ambos parlamentarios y
      `grep -oF 'No pudimos cargar la portada'` sobre ese fragmento da 0.
    - `git diff --name-only` no incluye `middleware.ts`, `app/next.config.ts` ni `app/wrangler.jsonc`
      (la CSP no se tocó).
  </acceptance_criteria>
  <verify>
    <automated>curl -s https://observatorio-congreso.thevalis.workers.dev/ > /tmp/p129.html; test "$(grep -oF '(sin materia)' /tmp/p129.html | wc -l)" = "0" && test "$(grep -oF 'Por materia' /tmp/p129.html | wc -l)" = "0" && test "$(grep -oF 'Comisiones citadas esta semana' /tmp/p129.html | wc -l)" -ge 1 && cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets && test -s 129-deploy-landing-desktop.png && test -s 129-deploy-panel-390.png && test -s 129-deploy-comparar.png</automated>
  </verify>
  <done>B-02 cerrado con cero fuerte apareado y 3 capturas acreditadas por contenido, no solo por tamaño.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Cloudflare Worker | el OAuth montado en el contenedor publica a producción |
| Worker → navegador público | el HTML servido no debe contener PII ni secretos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | `C:/Temp/obs-build` (mirror sucio) | mitigate | purga explícita en PowerShell + criterio `worker.js -nt /tmp/129-build-start` antes de deployar |
| T-129-02 | Repudiation | evidencia del deploy | mitigate | version-id nuevo obligatoriamente distinto de `b69f2ec2…` + baseline pre-deploy registrado |
| T-129-03 | Information Disclosure | `.env` presente en el mirror | accept | el mirror es local y ya existía; ninguna tarea lo lee ni lo commitea; `git status .env` vacío |
| T-129-04 | Elevation of Privilege | CSP / `frame-ancestors` | mitigate | prohibición explícita de tocarla; escalera 390px sin iframe; verificado por `git diff --name-only` |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | el install del contenedor es del lockfile existente; cero paquetes nuevos |
</threat_model>

<verification>
- Baseline pre-deploy registrado con el version-id preexistente.
- Version-id nuevo != `b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`; `worker.js` posterior al stamp.
- B-02: `(sin materia)` == 0 con control positivo >= 1.
- 3 PNG con `test -s` **y** fragmento `textContent` de contenido real.
- `supabase/migrations`, `.env` y archivos de CSP sin cambios.
</verification>

<success_criteria>
`129-DEPLOY-EVIDENCIA.md` completo (baseline, deploy nuevo, B-02, capturas con escalera declarada)
y las 3 capturas acreditadas por contenido.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-01-SUMMARY.md` when done
</output>
