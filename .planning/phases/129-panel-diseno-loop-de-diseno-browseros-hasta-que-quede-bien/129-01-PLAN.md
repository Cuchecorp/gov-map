---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "El deploy actual de observatorio-congreso.thevalis.workers.dev sirve el HEAD del milestone (panel 128 + 0080-0083)"
    - "Existen capturas BrowserOS del deploy real: / desktop, panel a 390px, /comparar?a=D1178&b=D1099"
    - "B-02 cerrado: el DOM del deploy no contiene '(sin materia)' ni el tombstone del tile materia, probado con control positivo apareado"
    - "H-01 cerrado: /comparar no muestra error boundary post-hidratación, o hay causa raíz con evidencia"
  artifacts:
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-landing-desktop.png"
      provides: "Captura del panel en desktop sobre el deploy real"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-panel-390.png"
      provides: "Captura del panel a 390px (iframe same-origin)"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-deploy-comparar.png"
      provides: "Captura de /comparar?a=D1178&b=D1099"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md"
      provides: "version-id del deploy, conteos grep, fragmentos DOM textContent"
  key_links:
    - from: "HTML servido por el Worker"
      to: "tiles del panel 128"
      via: "grep -o sobre el HTML de 1 línea"
      pattern: "Comisiones citadas esta semana"
---

<objective>
Deployar el HEAD del milestone a Cloudflare y levantar la evidencia dura del deploy real:
capturas BrowserOS de las 3 superficies y el cierre de B-02 y H-01 sobre el DOM servido.

Purpose: sin deploy no hay nada que criticar ni que someter al operador. Este plan produce
el sustrato de evidencia; la crítica y los fixes viven en 129-02.
Output: 3 PNG en `assets/` + `129-DEPLOY-EVIDENCIA.md` con version-id, conteos y fragmentos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CONTEXT.md
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/.continue-here.md
@docs/deploy-cloudflare.md
@docker-cf-build.sh
@scripts/bros-cli.mjs
</context>

<constraints>
- PROHIBIDO escribir, editar, re-aplicar o crear cualquier archivo bajo `supabase/migrations/`.
  0080-0083 están en PROD y son INTOCABLES.
- PROHIBIDO tocar `.env` o intentar rotar credenciales.
- 500s durante la ventana de propagación de 10-30 s tras el deploy NO son fallo: reintentar.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Deployar el HEAD a Cloudflare (build OpenNext en Docker)</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md</files>
  <read_first>
    - `docs/deploy-cloudflare.md`
    - `docker-cf-build.sh` (raíz del repo)
    - `app/wrangler.jsonc`
    - `.planning/phases/129-.../129-CONTEXT.md` §D-01
  </read_first>
  <action>
    Verificar primero que el árbol está limpio y HEAD es el commit de la Pasada 1 (`git status
    --porcelain` y `git log -1 --oneline`).

    Espejar el repo a `C:/Temp/obs-build` con `robocopy` `/MIR` EXCLUYENDO los directorios que
    inflan y cuelgan el bind-mount: `.pnpm-store`, `.planning`, `.git`, `node_modules`,
    `app/.open-next`, `app/.next`. Tras el `/MIR`, verificar que `C:/Temp/obs-build/docker-cf-build.sh`
    existe; si `/MIR` lo borró o falta cualquier helper script del build, re-escribirlo (gotcha
    LOCKED: los helpers no siempre sobreviven al mirror).

    Build en contenedor Linux con `MSYS_NO_PATHCONV=1` OBLIGATORIO en todo `docker run`/`docker cp`
    (Git Bash mangleaba `/host/...` → `C:/Program Files/Git/host/...`):
    contenedor `node:22-slim`, bind-mount `C:/Temp/obs-build:/host`, ejecutando `bash /host/docker-cf-build.sh`.
    Luego `docker cp <contenedor>:/build/app/.open-next` a `C:/Temp/obs-build/app/`.

    Deployar con el wrangler GLOBAL de AppData (el `wrangler` del PATH está sombreado por un shim
    de Python/miniconda — usar la ruta explícita a `wrangler.cmd` bajo `%APPDATA%/npm`), corriendo
    desde `C:/Temp/obs-build/app/`. Capturar el `version-id` que imprime el deploy.

    Confirmar el deploy con `wrangler.cmd deployments list` y con un `curl -s -o /dev/null -w "%{http_code}"`
    a `https://observatorio-congreso.thevalis.workers.dev/`. Si devuelve 5xx, esperar 30 s y
    reintentar hasta 3 veces antes de declarar fallo (ventana de propagación).

    Crear `129-DEPLOY-EVIDENCIA.md` y registrar: commit desplegado (sha corto), `version-id`,
    salida de `deployments list` (línea del deploy), y el código HTTP final de `/`.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%H` coincide con el sha registrado en `129-DEPLOY-EVIDENCIA.md`.
    - `wrangler.cmd deployments list` incluye el `version-id` registrado como deploy activo.
    - `curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/`
      imprime `200`.
    - `129-DEPLOY-EVIDENCIA.md` existe y contiene las cuatro claves: `commit`, `version-id`,
      `deployments-list`, `http-status`.
    - `git status --porcelain supabase/migrations` no imprime nada (ninguna migración tocada).
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://observatorio-congreso.thevalis.workers.dev/ | grep -x 200</automated>
  </verify>
  <done>El Worker sirve 200 desde el commit HEAD y el version-id está registrado.</done>
</task>

<task type="auto">
  <name>Task 2: Cerrar B-02 y H-01 sobre el DOM del deploy real</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md</files>
  <read_first>
    - `129-DEPLOY-EVIDENCIA.md` (creado en Task 1)
    - `app/components/panel-actualidad.tsx` (comentario del tile materia muerto, O-3)
    - `app/components/panel-actualidad.test.tsx` §"filtra agrupacion_materia EXPLÍCITAMENTE"
    - `app/app/comparar/page.tsx` (gotcha LOCKED `force-dynamic` + searchParams)
    - `.planning/phases/129-.../129-CONTEXT.md` §D-04, §D-05
  </read_first>
  <action>
    **B-02 (control positivo apareado, cero fuerte).** Bajar el HTML servido a un archivo
    (`curl -s ... > /tmp/panel.html`), NO pipear a grep directo. El HTML del Worker viene en UNA
    línea ⇒ `grep -c` topa en 1: usar SIEMPRE `grep -o <patrón> archivo | wc -l`. Usar `grep -oF`
    con literales exactos y JAMÁS combinar `-i` con `-F` (da 0 siempre). No usar `.` como comodín
    (no matchea `ó` multibyte). No usar `grep -q` bajo `pipefail` (exit 141).

    Medir tres conteos sobre el mismo archivo:
    1. NEGATIVO: ocurrencias de `(sin materia)` → debe ser `0`.
    2. NEGATIVO: ocurrencias del heading tombstone `Por materia` → debe ser `0`.
    3. POSITIVO APAREADO: ocurrencias de `Comisiones citadas esta semana` → debe ser `>= 1`.
       Este es el control que prueba que el grep SÍ encuentra tiles presentes (cero fuerte, no
       cero vacuo). Un conteo `2` es normal: HTML + payload RSC, no duplicación.

    Registrar los tres conteos verbatim en `129-DEPLOY-EVIDENCIA.md` bajo `## B-02`.

    **H-01.** Abrir `https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1178&b=D1099`
    con `node scripts/bros-cli.mjs open <url>`, parsear el Page ID del output
    (`grep -oE "Page ID: [0-9]+"`), esperar 8-10 s (SSR + hidratación), y extraer el DOM
    post-hidratación con `evaluate_script` leyendo `document.body.textContent` — JAMÁS `innerText`.
    Buscar en ese textContent los literales del error boundary de la app (leer `app/app/error.tsx`
    para el copy exacto antes de buscar). Registrar en `## H-01` el fragmento textContent que
    prueba que la página renderizó contenido real (nombres de los dos parlamentarios) y la
    ausencia del literal del error boundary.

    Si el error boundary REAPARECE: prohibido escribir "no reproducible". Documentar causa raíz
    con evidencia — capturar el mensaje del boundary, revisar `wrangler tail` durante una recarga,
    y registrar el stack/log en `## H-01 — causa raíz`.
  </action>
  <acceptance_criteria>
    - `129-DEPLOY-EVIDENCIA.md` §B-02 contiene los 3 conteos, con el comando exacto que los produjo.
    - Conteo de `(sin materia)` == 0 Y conteo de `Por materia` == 0 Y conteo de
      `Comisiones citadas esta semana` >= 1 (el positivo apareado es obligatorio; un 0 en el
      positivo invalida los negativos y bloquea la tarea).
    - `129-DEPLOY-EVIDENCIA.md` §H-01 contiene un fragmento de `document.body.textContent` de
      `/comparar?a=D1178&b=D1099` con los apellidos de ambos parlamentarios, obtenido vía
      `evaluate_script` (no `innerText`), más la aserción de ausencia del literal de `app/app/error.tsx`.
    - Si hubo error boundary, existe la sección `## H-01 — causa raíz` con log o stack citado.
  </acceptance_criteria>
  <verify>
    <automated>curl -s https://observatorio-congreso.thevalis.workers.dev/ > /tmp/p129.html; test "$(grep -oF '(sin materia)' /tmp/p129.html | wc -l)" = "0" && test "$(grep -oF 'Comisiones citadas esta semana' /tmp/p129.html | wc -l)" -ge 1</automated>
  </verify>
  <done>B-02 cerrado con cero fuerte apareado y H-01 cerrado (o con causa raíz documentada), ambos en 129-DEPLOY-EVIDENCIA.md.</done>
</task>

<task type="auto">
  <name>Task 3: Capturas BrowserOS de las 3 superficies del deploy</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/</files>
  <read_first>
    - `scripts/bros-cli.mjs` (cabecera con gotchas y atajos `open`/`shot`/`close`)
    - `.planning/phases/129-.../129-CONTEXT.md` §D-02, §D-03
    - `129-DEPLOY-EVIDENCIA.md`
  </read_first>
  <action>
    Tres capturas, todas con path ABSOLUTO Windows con forward-slashes hacia
    `C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/`
    (`save_screenshot` resuelve contra el cwd de BrowserOS, no el tuyo).

    1. `129-deploy-landing-desktop.png` — `/` en desktop. Tras `open`, sleep 8-10 s antes del shot.
    2. `129-deploy-panel-390.png` — el panel a 390px vía **iframe same-origin** (precedente v8.0):
       abrir una página del propio origen, inyectar por `evaluate_script` un `<iframe>` de
       `width:390px;height:900px;border:0` apuntando a `/`, esperar 8-10 s y capturar.
    3. `129-deploy-comparar.png` — `/comparar?a=D1178&b=D1099`.

    Para llegar a la zona de tiles en la captura de `/`, scrollear vía `evaluate_script`
    (`window.scrollTo(...)`) — el tool `scroll` puede no mover.

    GOTCHA BLOQUEANTE: `bros-cli` sale 0 tras `CDP request timeout`. Un exit 0 NO prueba nada.
    Tras cada `shot`, verificar con `test -s <ruta>` que el PNG EXISTE y pesa >0; si no, reintentar
    una vez tras `sleep 3`. Cerrar cada página con `close` al terminar.

    Registrar en `129-DEPLOY-EVIDENCIA.md` §Capturas las 3 rutas absolutas con su tamaño en bytes
    (`ls -l`).
  </action>
  <acceptance_criteria>
    - Los 3 archivos existen bajo `assets/` y cada uno pasa `test -s` (tamaño > 0 bytes).
    - `file assets/129-deploy-landing-desktop.png` reporta `PNG image data` (no un archivo truncado).
    - `129-DEPLOY-EVIDENCIA.md` §Capturas lista las 3 rutas con tamaños en bytes distintos de 0.
    - La captura de 390px se obtuvo por iframe same-origin, y el hecho está declarado en la
      evidencia junto al snippet de `evaluate_script` usado.
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets && test -s 129-deploy-landing-desktop.png && test -s 129-deploy-panel-390.png && test -s 129-deploy-comparar.png</automated>
  </verify>
  <done>Las 3 capturas existen, pesan >0 y están registradas en la evidencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Cloudflare Worker | credenciales de wrangler (OAuth) publican código a producción |
| Worker → navegador público | el HTML servido no debe contener PII ni secretos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Information Disclosure | HTML servido por el Worker | mitigate | guards de lockdown/PII ya en `pnpm guards`; este plan no cambia queries ni RPCs |
| T-129-02 | Tampering | build en Docker | mitigate | build desde el mirror del repo en HEAD limpio; sha verificado antes del deploy |
| T-129-03 | Elevation of Privilege | supabase/migrations | accept | ninguna tarea escribe migraciones; verificado por `git status --porcelain supabase/migrations` vacío |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | este plan no instala paquetes nuevos; el install del contenedor es del lockfile existente |
</threat_model>

<verification>
- Worker sirve 200 desde el commit HEAD, con version-id registrado.
- B-02: cero `(sin materia)` y cero `Por materia`, con control positivo `>= 1`.
- H-01: `/comparar?a=D1178&b=D1099` con textContent real y sin error boundary (o causa raíz).
- 3 PNG existentes y con tamaño > 0.
- `git status --porcelain supabase/migrations` vacío.
</verification>

<success_criteria>
`129-DEPLOY-EVIDENCIA.md` completo (commit, version-id, B-02, H-01, capturas) y las 3 capturas
en `assets/`, todas del deploy real.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-01-SUMMARY.md` when done
</output>
