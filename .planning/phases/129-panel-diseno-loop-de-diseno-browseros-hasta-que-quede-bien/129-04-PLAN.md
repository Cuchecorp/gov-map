---
phase: 129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien
plan: 04
type: execute
wave: 4
depends_on: ["129-03"]
files_modified:
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
  - .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/
  - app/components/panel-tile-comisiones.test.tsx
  - app/components/panel-tile-sala.test.tsx
  - app/components/panel-tile-movimiento.test.tsx
  - app/components/panel-tile-ingresos.test.tsx
autonomous: true
requirements: [PANEL-09]

must_haves:
  truths:
    - "Los hallazgos FIX de la crítica están cerrados con evidencia, o las iteraciones se declararon agotadas"
    - "Hay un re-deploy INCONDICIONAL posterior a todos los cambios de código, con version-id nuevo"
    - "Las capturas finales son posteriores al re-deploy, probado por mtime contra un stamp"
    - "La densidad a 390px está validada: <=4 ítems por tile en el DOM, y el remanente 'y N más →' probado honesto por test"
  artifacts:
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-final-landing-desktop.png"
      provides: "Captura final desktop posterior al re-deploy"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-final-panel-390.png"
      provides: "Captura final a 390px posterior al re-deploy"
    - path: ".planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/129-final-comparar.png"
      provides: "Captura final de /comparar posterior al re-deploy"
  key_links:
    - from: "assets/129-final-*.png"
      to: "el re-deploy final"
      via: "mtime posterior al stamp /tmp/129-redeploy-stamp"
      pattern: "129-final-"
---

<objective>
Aplicar los FIX de la crítica, re-deployar INCONDICIONALMENTE (todo el código cambió en 129-02 y
129-03), capturar las 3 superficies finales con prueba de posterioridad, y validar la densidad a
390px por sus dos patas: DOM para el conteo visible, TEST para la honestidad del remanente.

Purpose: el checker mordió tres cosas — capturas "finales" que podían ser viejas, densidad medida
contra el deploy anterior, y una tabla de densidad que ningún agente puede construir desde el
navegador porque los totales del jsonb nunca llegan al DOM.
Output: capturas `129-final-*`, §Densidad 390px en `129-CRITICA.md`, version-id final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md
@.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md
@.planning/milestones/v10.0-phases/104-vsim-p3b-flip-on-deploy-integrado/104-02-PLAN.md
@scripts/bros-cli.mjs
</context>

<hechos_verificados>
- **Los totales del jsonb NUNCA llegan al DOM.** `panel-tile-comisiones.tsx`, `panel-tile-sala.tsx`
  y `panel-actualidad.tsx` NO son client components: `puntos_total` / `tabla_total` se consumen
  server-side y solo se emite el string ya calculado. El payload RSC lleva output renderizado, no
  props. Un agente NO puede sacar `total` del navegador — si lo intenta, fabrica la tabla.
  ⇒ La honestidad del remanente se prueba en TEST; el DOM solo aporta el conteo visible y el
  literal `y N más →` verbatim.
- Aserciones de remanente ya existentes que hay que CITAR (no re-escribir):
  `panel-tile-comisiones.test.tsx:161` (`puntos_total:31` con `maxItems:4` → `y 27 más →`),
  `panel-tile-sala.test.tsx:123` y `:159` (`tabla_total:25` → `y 24 más →`).
- **`panel-tile-urgencias` no tiene remanente por diseño**: en la tabla de densidad va como
  `N = n/a` con la razón citada, no como fila faltante.
- CSP ENFORCED mata el iframe 390px: se aplica la misma escalera (a)/(b)/(c) de 129-01.
  **PROHIBIDO tocar la CSP.**
- `evaluate_script` usa el parámetro **`expression`** (no `script`); JSON al scratchpad y
  `"$(cat args.json)"`; los Page IDs incrementan.
- Deploy PROBADO = dentro del contenedor, montando el OAuth de
  `C:/Users/Carlo/AppData/Roaming/xdg.config/.wrangler`. `robocopy /MIR` con `/XD` NO purga.
</hechos_verificados>

<constraints>
- PROHIBIDO escribir bajo `supabase/migrations/`. PROHIBIDO tocar `.env` o la CSP.
- El re-deploy es **INCONDICIONAL**: no existe la rama "si no hubo cambios, reutilizo capturas".
- Máximo 3 iteraciones de corrección de diseño en total para la fase (contando lo ya gastado).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar los FIX de la crítica (iteraciones acotadas)</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md</files>
  <read_first>
    - `129-CRITICA.md` §Hallazgos y §Presupuesto de iteraciones
    - Cada archivo nombrado como responsable en las filas `FIX`
  </read_first>
  <action>
    Aplicar los hallazgos `FIX` en el orden declarado en §Presupuesto de iteraciones, respetando el
    tope de 3 iteraciones para toda la fase. Cada fix con su test o su aserción DOM. Commitear por
    iteración; NO usar `--amend` (inseguro en waves).

    Actualizar en la tabla de `129-CRITICA.md` la columna `estado` de cada fila `FIX` a `CERRADO`
    (citando la evidencia: comando de test o literal encontrado) o a `AGOTADAS ITERACIONES` (con lo
    que faltó). Ningún `FIX` puede quedar sin columna `estado`.

    Si la crítica no tenía filas `FIX`, escribirlo explícitamente en §Iteraciones y pasar a Task 2
    — el re-deploy ocurre igual, porque 129-02 y 129-03 ya cambiaron código.
  </action>
  <acceptance_criteria>
    - Toda fila `FIX` de `129-CRITICA.md` tiene columna `estado` en {`CERRADO`, `AGOTADAS ITERACIONES`}.
    - Cada `CERRADO` cita evidencia concreta (comando de test corrido o literal DOM encontrado).
    - El número de iteraciones gastadas registrado en §Iteraciones es <= 3.
    - `pnpm --filter ./app test` sale 0 y `pnpm guards` sale 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app test</automated>
  </verify>
  <done>Los FIX están cerrados con evidencia o las iteraciones declaradas agotadas, sin filas sin estado.</done>
</task>

<task type="auto">
  <name>Task 2: Re-deploy INCONDICIONAL + capturas finales posteriores al deploy</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-DEPLOY-EVIDENCIA.md, .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets/</files>
  <read_first>
    - `.planning/milestones/v10.0-phases/104-vsim-p3b-flip-on-deploy-integrado/104-02-PLAN.md` §Task 1
    - `129-DEPLOY-EVIDENCIA.md` (version-id de 129-01, que el nuevo debe superar)
    - `scripts/bros-cli.mjs`
    - `app/app/error.tsx` (literales del boundary raíz)
  </read_first>
  <action>
    1. `touch /tmp/129-redeploy-stamp` ANTES de nada. Este stamp es lo que prueba que las capturas
       finales son posteriores al re-deploy.
    2. Re-deployar por el MISMO procedimiento de `129-01-PLAN.md` Task 2, sin atajos: purga
       explícita en PowerShell de `node_modules`, `.pnpm-store` y `app/.open-next` del mirror →
       `robocopy /MIR` → re-escribir `docker-deploy.sh` → contenedor `node:22-slim` con
       `MSYS_NO_PATHCONV=1` montando `/work` y el OAuth de
       `C:\Users\Carlo\AppData\Roaming\xdg.config\.wrangler` → build+deploy DENTRO del contenedor.
    3. Verificar, ANTES de acreditar: `worker.js` con mtime posterior a `/tmp/129-redeploy-stamp`.
    4. Extraer el nuevo `Current Version ID` y registrarlo en `129-DEPLOY-EVIDENCIA.md`
       §Re-deploy final.
    5. **Prueba de que el código nuevo VIAJÓ al bundle desplegado** (el literal en el HTML servido
       sería data-dependiente: el molde `1 citación del Senado` solo aparece si el conteo real es 1):
       `grep -oF 'citación del Senado' C:/Temp/obs-build/app/.open-next/worker.js | wc -l` >= 1.
       Adicionalmente, si el HTML servido resulta contener el literal singular, registrarlo también;
       si no, escribir explícitamente que el conteo vivo no es 1 y por eso el literal no aparece
       en el HTML — sin fingir que el criterio se cumplió por otra vía.
    6. Capturar las 3 superficies como `129-final-landing-desktop.png`, `129-final-panel-390.png`,
       `129-final-comparar.png`, con los mismos gotchas de instrumento: path ABSOLUTO, parsear
       `Page ID: N`, sleep 8-10 s, escalera (a)/(b)/(c) para los 390px con el escalón DECLARADO,
       reintento único tras `CDP request timeout`, y **verificación de contenido por
       `evaluate_script` + `textContent`** (landing/390: `Comisiones citadas esta semana`;
       comparar: apellidos de D1178 y D1099 y ausencia de `No pudimos cargar la portada`).
  </action>
  <acceptance_criteria>
    - El version-id de §Re-deploy final es distinto del de §Deploy nuevo (129-01) **y** distinto de
      `b69f2ec2-37c9-4212-b91c-a9ad97b4aeb7`.
    - `test C:/Temp/obs-build/app/.open-next/worker.js -nt /tmp/129-redeploy-stamp` es verdadero.
    - `grep -oF 'citación del Senado' C:/Temp/obs-build/app/.open-next/worker.js | wc -l` >= 1
      (el fix de plural está en el bundle desplegado).
    - Los 3 `129-final-*.png` existen, pasan `test -s`, y **cada uno pasa
      `test assets/129-final-X.png -nt /tmp/129-redeploy-stamp`** (posteriores al re-deploy).
    - Para cada captura final, la evidencia incluye el fragmento `textContent` con el literal
      exigido. `test -s` sin fragmento NO acredita.
    - `curl -s -o /dev/null -w "%{http_code}"` sobre `/` da `200`.
    - `git status --porcelain supabase/migrations` vacío; `.env` sin cambios; sin cambios en
      `middleware.ts` / `app/next.config.ts` / `app/wrangler.jsonc`.
  </acceptance_criteria>
  <verify>
    <automated>cd .planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/assets && test -s 129-final-landing-desktop.png && test -s 129-final-panel-390.png && test -s 129-final-comparar.png && test 129-final-landing-desktop.png -nt /tmp/129-redeploy-stamp && test 129-final-panel-390.png -nt /tmp/129-redeploy-stamp && test 129-final-comparar.png -nt /tmp/129-redeploy-stamp</automated>
  </verify>
  <done>Re-deploy incondicional acreditado y 3 capturas finales probadamente posteriores a él.</done>
</task>

<task type="auto">
  <name>Task 3: Densidad 390px — DOM para lo visible, TEST para la honestidad del remanente</name>
  <files>.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-CRITICA.md, app/components/panel-tile-comisiones.test.tsx, app/components/panel-tile-sala.test.tsx, app/components/panel-tile-movimiento.test.tsx, app/components/panel-tile-ingresos.test.tsx</files>
  <read_first>
    - `app/components/panel-tile-comisiones.test.tsx:161` (caso `puntos_total:31` → `y 27 más →`)
    - `app/components/panel-tile-sala.test.tsx:123` y `:159` (caso `tabla_total:25` → `y 24 más →`)
    - `app/components/panel-tile-movimiento.test.tsx:103` (caso `total:2` → sin remanente)
    - `app/components/panel-tile-ingresos.tsx` (`MAX_ITEMS_DEFAULT`, secciones ingresos/archivados)
    - `app/components/panel-tile-urgencias.tsx` (por qué no tiene remanente)
    - `129-DEPLOY-EVIDENCIA.md` §Re-deploy final (el version-id que debe citarse)
  </read_first>
  <action>
    Esta validación corre **DESPUÉS** del re-deploy de Task 2 y se mide contra el version-id final.

    **Pata 1 — DOM (solo lo que el navegador realmente sabe).** Sobre la superficie 390px del
    deploy final, por `evaluate_script` (parámetro `expression`), recorrer `document.querySelectorAll("section")`
    y emitir, por cada sección con `h2`, el par `{ h2: h2.textContent, items: section.querySelectorAll("ul > li").length }`
    más los literales `y N más →` presentes (`textContent` verbatim, JAMÁS `innerText`). Criterio:
    ninguna sección con `items > 4`. NO intentar leer `puntos_total`/`tabla_total` del navegador:
    no están ahí; inventarlos es fabricar evidencia.

    **Pata 2 — TEST (la honestidad del remanente).** Verificar por vitest, con rutas EXPLÍCITAS,
    que cada tile con remanente tiene un caso `total > maxItems` que asserta el literal exacto
    `y {total - mostrados} más →`. Citar los casos existentes (comisiones :161, sala :123/:159) y
    **añadir una fila nueva por cada tile que hoy NO tenga caso `total > maxItems`** (revisar
    movimiento e ingresos: movimiento solo tiene el caso `total:2` sin remanente). Los tests nuevos
    deben usar un `total` del jsonb estrictamente mayor que `maxItems` y asertar el N exacto.

    **Tabla §Densidad 390px** en `129-CRITICA.md`, una fila por tile, columnas:
    `tile` | `ítems visibles en el DOM (<=4)` | `literal 'y N más →' en el DOM (verbatim o ninguno)` |
    `caso de test que prueba el N honesto (archivo:línea)` | `version-id medido`.
    `panel-tile-urgencias` va con `N = n/a` y la razón citada (no tiene remanente por diseño).
  </action>
  <acceptance_criteria>
    - §Densidad 390px existe, con una fila por tile del panel y las 5 columnas nombradas, y cita el
      version-id de §Re-deploy final (el mismo, no el de 129-01).
    - Ninguna fila declara `ítems visibles > 4`; el dato viene del `querySelectorAll` citado, con el
      `expression` usado pegado en la evidencia.
    - Toda fila con literal `y N más →` en el DOM tiene su columna `caso de test` apuntando a un
      `archivo:línea` existente (verificable abriendo esa línea).
    - `pnpm --filter ./app exec vitest run components/panel-tile-comisiones.test.tsx components/panel-tile-sala.test.tsx components/panel-tile-movimiento.test.tsx components/panel-tile-ingresos.test.tsx`
      sale 0 y reporta más tests que antes de esta tarea (registrar ambos números).
    - `panel-tile-urgencias` aparece con `N = n/a` y la razón, no ausente.
    - Ninguna celda de la tabla contiene un `total` de jsonb obtenido del navegador.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./app exec vitest run components/panel-tile-comisiones.test.tsx components/panel-tile-sala.test.tsx components/panel-tile-movimiento.test.tsx components/panel-tile-ingresos.test.tsx</automated>
  </verify>
  <done>Densidad 390px validada contra el deploy final: <=4 por tile en el DOM y remanente honesto probado por test en cada tile que lo tiene.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Cloudflare Worker | el re-deploy publica el código de 129-02 y 129-03 a producción |
| navegador → evidencia | lo que el DOM NO expone no puede acreditarse desde el navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-12 | Repudiation | capturas "finales" | mitigate | stamp `/tmp/129-redeploy-stamp` + criterio `-nt` por captura; sin rama de reutilización |
| T-129-13 | Repudiation | tabla de densidad | mitigate | prohibición explícita de leer totales del jsonb desde el navegador; la honestidad va por test |
| T-129-14 | Tampering | bundle desplegado | mitigate | `grep` del literal del fix de plural dentro de `worker.js` + mtime posterior al stamp |
| T-129-15 | Elevation of Privilege | CSP | mitigate | escalera 390px sin iframe; verificado por ausencia de diffs en los archivos de headers |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | ningún paquete nuevo en este plan |
</threat_model>

<verification>
- Version-id final distinto del de 129-01 y de `b69f2ec2…`; `worker.js` posterior al stamp y
  conteniendo el literal del fix de plural.
- 3 capturas `129-final-*` con `test -s`, `-nt` el stamp, y fragmento `textContent`.
- §Densidad 390px con una fila por tile, `<=4` visibles, y caso de test por remanente.
- `vitest run` de los 4 tiles verde con delta > 0; `pnpm --filter ./app test` y `pnpm guards` 0.
</verification>

<success_criteria>
El deploy final contiene todo el código de la fase, las capturas lo prueban por mtime, y la
densidad a 390px está cerrada por sus dos patas sin fabricar ningún dato.
</success_criteria>

<output>
Create `.planning/phases/129-panel-diseno-loop-de-diseno-browseros-hasta-que-quede-bien/129-04-SUMMARY.md` when done
</output>
