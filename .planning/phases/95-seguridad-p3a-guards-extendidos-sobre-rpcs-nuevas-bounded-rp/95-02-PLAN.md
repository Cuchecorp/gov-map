---
phase: 95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SEC-01]
files_modified:
  - app/lib/lockdown-guard.test.ts
  - app/lib/anti-insinuacion-guard.test.ts

must_haves:
  truths:
    - "El allowlist de RPCs no tiene drift bidireccional: toda RPC servida ⊆ allowlist Y toda entrada del allowlist ⊆ funciones definidas en migraciones"
    - "Los 4 cross-links llamados vía crossLinkReader(variable) quedan cubiertos por Direction-A (hoy el rpcPattern estático los MISS)"
    - "La superficie deep-link (89, validacion-fuente) queda enumerada en el linter anti-insinuación"
    - "Cada guard extendido muerde sobre lo NUEVO (mutation self-check en memoria)"
  artifacts:
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "Assert Direction-B (allowlist ⊆ funciones definidas) + cobertura de los 4 cross-links de crossLinkReader + mutation self-checks"
      contains: "Direction-B"
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "Superficie deep-link (89) enumerada + mutation self-check"
      contains: "SUPERFICIES_DEEPLINK"
  key_links:
    - from: "app/lib/lockdown-guard.test.ts"
      to: "supabase/migrations/*.sql"
      via: "grep create (or replace )?function (public.)?<name> — public. OPCIONAL"
      pattern: "create\\s+(?:or\\s+replace\\s+)?function\\s+(?:public\\.)?"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/components/validacion-fuente.tsx"
      via: "SUPERFICIES_DEEPLINK en el loop de escaneo"
      pattern: "validacion-fuente"
---

<objective>
Extender (NO duplicar) los guards CI existentes sobre las RPCs y superficies NUEVAS de P1/P2, cerrando el drift del allowlist en ambas direcciones y probando que cada guard MUERDE sobre lo nuevo (no pasa por vacío). Bajo Camino A los guards son tripwires estáticos (denylist/allowlist) — su valor es cazar regresiones en review, y DEBEN morder o son un no-op verde.

Purpose: Cerrar SC#1 (guards extendidos sobre superficies nuevas), SC#3 (allowlist sin drift bidireccional) y SC#4 (mutation self-check sobre lo NUEVO). Direction-A (servida ⊆ allowlist) ya existe pero tiene UN blind-spot confirmado; Direction-B (allowlist ⊆ definidas) NO existe.

Output:
- `app/lib/lockdown-guard.test.ts` — assert Direction-B + cobertura de los 4 cross-links de `crossLinkReader` + mutation self-checks.
- `app/lib/anti-insinuacion-guard.test.ts` — superficie deep-link (89) enumerada + self-check.
- Suite app (1129+) + packages (1103) verde; `tsc --noEmit` limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-RESEARCH.md
@.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-PATTERNS.md

<interfaces>
<!-- Hechos VERIFICADOS del código este día (no confiar en memoria) — load-bearing para no shippear un guard rojo. -->

BLIND-SPOT CONFIRMADO (Direction-A, Pitfall 4 REALIZADO): `app/app/parlamentario/[id]/page.tsx:185` define
`function crossLinkReader(rpc: string) { … sb.rpc(rpc, { p_id: id }) … }` — llama `.rpc(VARIABLE)`, NO `.rpc("literal")`.
Los 4 nombres de cross-link aparecen SOLO como argumento de `crossLinkReader("...")` en las líneas 196-199:
  const getCopartidarios   = crossLinkReader("copartidarios_de_parlamentario");
  const getMismaZona       = crossLinkReader("de_la_misma_zona");
  const getCoComisionados  = crossLinkReader("co_comisionados_de_parlamentario");
  const getCoautores       = crossLinkReader("coautores_de_parlamentario");
El `rpcPattern = /\.rpc\(\s*['"`]([a-zA-Z_][\w]*)['"`]/g` del guard (lockdown-guard.test.ts:388) matchea `.rpc("lit")`,
NO matchea `crossLinkReader("lit")`. → HOY estos 4 NO son verificados por Direction-A (están en el allowlist,
pero el guard no los ENLAZA a su call site). Direction-B SÍ los respalda (todos definidos en 0061), así que no hay
regresión de seguridad hoy; el hueco es para FUTURAS adiciones vía este helper.

Los 4 cross-links YA están en PUBLIC_RPC_ALLOWLIST (lockdown-guard.test.ts:171-192, 26 entradas). Direction-A pasa
trivialmente para ellos (nunca aparecen como offender porque nunca aparecen como `.rpc("lit")`).

REGEX DIRECTION-B — CORRECCIÓN LOAD-BEARING: la pseudocode de RESEARCH usa `/create\s+(?:or\s+replace\s+)?function\s+public\.(\w+)/`.
ESO PRODUCE 3 FALSOS ORPHANS: estas RPCs allowlisted están definidas SIN el qualifier `public.`:
  - match_proyectos          → 0011:55  `create or replace function match_proyectos(`
  - parlamentario_publico    → 0020:28  `create or replace function parlamentario_publico(`
  - parlamentarios_publico   → 0026:30  `create or replace function parlamentarios_publico()`
El regex de Direction-B DEBE hacer `public.` OPCIONAL: `/create\s+(?:or\s+replace\s+)?function\s+(?:public\.)?(\w+)/gi`.
Sin este fix el guard shippea ROJO en la primera corrida (3 orphans falsos). Scope: repo-wide (TODAS las migraciones,
NO filtrado a >0044) porque match_proyectos/parlamentario_publico/parlamentarios_publico son pre-0044.

SUPERFICIE DEEP-LINK (89): `app/components/validacion-fuente.tsx` existe (TRACE-01/02/03). Copy factual (URLs
Senado/BCN/R2, fecha, hash) — grep confirma cero términos insinuantes. NO está en ningún surface array del linter hoy.

Helpers/constantes YA en scope (NO re-declarar):
  lockdown-guard.test.ts: stripSqlComments() (56-61), MIGRATIONS_DIR (46), PUBLIC_RPC_ALLOWLIST (165-192), APP_ROOT (43).
  anti-insinuacion-guard.test.ts: detectarInsinuaciones() pura (~417-432), stripTsComments(), APP_ROOT via import.meta.dirname (línea ~64 — usar ESTE, NO process.cwd(); Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lockdown-guard.test.ts — assert Direction-B (allowlist ⊆ definidas, public. opcional) + cobertura de los 4 cross-links de crossLinkReader + mutation self-checks</name>
  <files>app/lib/lockdown-guard.test.ts</files>
  <behavior>
    - Direction-B (real): `definedRpcNames(MIGRATIONS_DIR)` (detector puro) recolecta todo `create (or replace )?function (public.)?<name>` repo-wide; assert `PUBLIC_RPC_ALLOWLIST ⊆ definedRpcNames` → orphans == 0. Con el regex correcto (public. opcional), match_proyectos/parlamentario_publico/parlamentarios_publico NO son orphans.
    - Direction-B self-check (SC#4): allowlist sintético `{"funcion_fantasma_typo"}` + set de definidas vacío → el detector reporta exactamente 1 orphan == "funcion_fantasma_typo".
    - Direction-B sanity: `definedRpcNames(MIGRATIONS_DIR).size > 20` (el glob no está vacío — Pitfall 5).
    - Cross-links via crossLinkReader (cobertura del blind-spot): assert que los 4 literales pasados a `crossLinkReader("...")` en parlamentario/[id]/page.tsx están TODOS en PUBLIC_RPC_ALLOWLIST. Detector puro que extrae `crossLinkReader\(\s*['"`](\w+)['"`]\)` del archivo y verifica ⊆ allowlist.
    - Cross-links self-check (SC#4): fixture en memoria con `crossLinkReader("rpc_no_listada")` → el detector lo reporta como offender.
  </behavior>
  <read_first>
    - app/lib/lockdown-guard.test.ts (COMPLETO, 421 líneas) — Block A anonGrantOffenders() (219-227) como patrón detector-puro; su self-check `it("BLOQUEA todo grant…")` (299-346) como patrón mutation self-check; Block B rpcPattern (388) y PUBLIC_RPC_ALLOWLIST (165-192); stripSqlComments (56-61); sanity assert (356-358).
    - .planning/phases/95-.../95-PATTERNS.md §"lockdown-guard.test.ts (extend)" (líneas 162-216) — detector definedRpcNames, it() Direction-B, mutation self-check fantasma, dónde insertar (nuevo describe "(A2)"), reuso verbatim de stripSqlComments/MIGRATIONS_DIR/PUBLIC_RPC_ALLOWLIST.
    - .planning/phases/95-.../95-RESEARCH.md §"Drift bidireccional" (líneas 193-264) — la tabla de las 26 entradas → migración que las define (Direction-B esperado: 0 orphans); §"Code Examples" (líneas 423-434) Direction-B assert; §Pitfall 4 (crossLinkReader indirection); §Pitfall 5 (process.cwd/sanity).
    - app/app/parlamentario/[id]/page.tsx líneas 185-199 — crossLinkReader body (llama `.rpc(rpc)` VARIABLE) + los 4 literales en el call site.
  </read_first>
  <action>
    Editar `app/lib/lockdown-guard.test.ts`. NO tocar Block A ni Block B existentes (solo AÑADIR).

    (1) Detector puro `definedRpcNames(migrationsDir: string): Set<string>`: para cada `.sql` en migrationsDir, `stripSqlComments` + `readFileSync`, matchear `/create\s+(?:or\s+replace\s+)?function\s+(?:public\.)?(\w+)/gi` — `public.` OPCIONAL (CRÍTICO: sin esto match_proyectos/parlamentario_publico/parlamentarios_publico salen falsos orphans). Repo-wide (TODAS las migraciones, sin filtro >0044). Añadir a un Set.

    (2) Nuevo describe `"(A2) Guard — toda entrada del allowlist existe en migraciones (Direction-B, SC#3)"` con:
      - it Direction-B: `orphans = [...PUBLIC_RPC_ALLOWLIST].filter(n => !defined.has(n))`; `expect(orphans).toHaveLength(0)` con mensaje que lista los orphans.
      - it sanity: `expect(definedRpcNames(MIGRATIONS_DIR).size).toBeGreaterThan(20)`.
      - it mutation self-check: allowlist fantasma `new Set(["funcion_fantasma_typo"])` vs definidas vacías → 1 orphan == "funcion_fantasma_typo".

    (3) Cobertura de los 4 cross-links (blind-spot Direction-A). Detector puro `crossLinkRpcNames(tsSource: string): string[]` que extrae `/crossLinkReader\(\s*['"`](\w+)['"`]\)/g`. En un describe `"(A3) Guard — cross-links via crossLinkReader ⊆ allowlist (SC#1/SC#3)"`:
      - it real: leer `app/app/parlamentario/[id]/page.tsx` (path relativo a APP_ROOT — es `app/parlamentario/[id]/page.tsx` desde app/), stripTsComments NO necesario (nombres en literal), extraer con crossLinkRpcNames, assert todos ⊆ PUBLIC_RPC_ALLOWLIST (offenders == 0). Sanity: encontró ≥4 literales.
      - it self-check: `crossLinkRpcNames('const x = crossLinkReader("rpc_no_listada");')` devuelve ["rpc_no_listada"] y ese nombre ∉ allowlist → reportado offender.

    Documentar en un comentario que crossLinkReader llama `.rpc(rpc)` con VARIABLE → el rpcPattern de Block B lo MISS; este (A3) cierra el hueco enumerando el call site.

    Anclar rutas en APP_ROOT (process.cwd()=app/) igual que el resto del archivo; el path del page es `path.join(APP_ROOT, "app/parlamentario/[id]/page.tsx")`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app test lockdown-guard` sale 0 (verde) — incluyendo los nuevos describe (A2) y (A3).
    - El regex de definedRpcNames tiene `public.` OPCIONAL (`(?:public\.)?`). Verificable por grep del archivo: contiene `(?:public\\.)?` en la definición del detector.
    - Direction-B real: 0 orphans (las 26 entradas del allowlist se respaldan; match_proyectos/parlamentario_publico/parlamentarios_publico NO son orphans gracias al regex public.-opcional).
    - Direction-B self-check: detecta "funcion_fantasma_typo" como orphan (el guard MUERDE).
    - Cross-links (A3): los 4 literales de crossLinkReader ⊆ allowlist (0 offenders); self-check con "rpc_no_listada" reportado.
    - Sanity: `definedRpcNames().size > 20`.
    - Block A y Block B existentes intactos (no se borró ni modificó ningún it() previo).
  </acceptance_criteria>
  <verify>
    <automated>cd app && pnpm exec vitest run lib/lockdown-guard.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>lockdown-guard.test.ts extendido: (A2) Direction-B con regex public.-opcional (0 orphans reales + self-check fantasma muerde + sanity >20) y (A3) cobertura de los 4 cross-links de crossLinkReader (⊆ allowlist + self-check muerde). Block A/B intactos. Guard verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: anti-insinuacion-guard.test.ts — enumerar superficie deep-link (89) + mutation self-check</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <behavior>
    - `SUPERFICIES_DEEPLINK` (validacion-fuente.tsx + provenance-badge si existe) se escanea en el loop principal post-stripTsComments → 0 términos prohibidos (copy factual).
    - Mutation self-check DEEPLINK (SC#4): fixture en memoria con "influencia del parlamentario sobre la tramitación" → `detectarInsinuaciones(fixture)` incluye "influencia".
  </behavior>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (COMPLETO) — surface arrays existentes (SUPERFICIES_VOTO/MONEY/HOME/BUSQUEDA/PERSONAS/LOBBY/AGENDA), el loop de escaneo principal (spread de arrays), detectarInsinuaciones() pura, stripTsComments(), APP_ROOT via import.meta.dirname (NO process.cwd — Pitfall 5), NEGACIONES_LOCKED, y los self-check it() existentes (PERSONAS/LOBBY/AGENDA) como patrón.
    - .planning/phases/95-.../95-PATTERNS.md §"anti-insinuacion-guard.test.ts (extend)" (líneas 219-263) — hallazgo (validacion-fuente factual, no en array), SUPERFICIES_DEEPLINK pattern, cómo integrar al spread, mutation self-check pattern, NO se necesita NEGACIONES_LOCKED nueva.
    - app/components/validacion-fuente.tsx (grep de términos prohibidos para confirmar copy factual antes de enumerar).
    - .planning/phases/95-.../95-RESEARCH.md §Anti-insinuación 89-surface (Open Q2, líneas 468-471) — "extender solo si el linter no cubre alguna superficie de P1".
  </read_first>
  <action>
    Editar `app/lib/anti-insinuacion-guard.test.ts`. NO tocar los arrays ni self-checks existentes (solo AÑADIR).

    (1) Antes de enumerar, grep `app/components/validacion-fuente.tsx` por términos de TERMINOS_PROHIBIDOS para confirmar que el copy es factual (URLs/fecha/hash). Si por caso raro contuviera un término insinuante NO negado, reportarlo (esa sería una regresión real, no una falsa alarma).

    (2) Declarar `const SUPERFICIES_DEEPLINK: string[] = ["components/validacion-fuente.tsx"]` (añadir `"components/provenance-badge.tsx"` SOLO si el archivo existe; verificar con existsSync antes de incluirlo para no romper el sanity de archivos leídos). Comentario: superficie deep-link 89 (TRACE-01/02/03), copy actual factual → tripwire PREVENTIVO (mismo rationale que SUPERFICIES_HOME).

    (3) Añadir `...SUPERFICIES_DEEPLINK` al spread del loop principal de escaneo (junto a los otros SUPERFICIES_*). Path anclado en APP_ROOT (import.meta.dirname), igual que el resto.

    (4) Mutation self-check DEEPLINK: it() con fixture en memoria `"<p>Este dato fue obtenido gracias a la influencia del parlamentario sobre la tramitación.</p>"` → `expect(detectarInsinuaciones(fixture)).toContain("influencia")`. Cero contacto con disco en el self-check.

    NO añadir NEGACIONES_LOCKED (el copy de validacion-fuente no usa ningún término prohibido como negación).
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app test anti-insinuacion-guard` sale 0 (verde).
    - `SUPERFICIES_DEEPLINK` existe y contiene `components/validacion-fuente.tsx`.
    - `...SUPERFICIES_DEEPLINK` aparece en el spread del loop de escaneo (la superficie 89 se escanea de verdad — no un array huérfano).
    - El self-check DEEPLINK detecta "influencia" (el guard MUERDE sobre la superficie nueva).
    - Los arrays y self-checks existentes (VOTO/MONEY/HOME/BUSQUEDA/PERSONAS/LOBBY/AGENDA) intactos.
    - Anclado en import.meta.dirname (NO process.cwd) — sin regresión Pitfall 5.
  </acceptance_criteria>
  <verify>
    <automated>cd app && pnpm exec vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>anti-insinuacion-guard.test.ts extendido: SUPERFICIES_DEEPLINK (validacion-fuente 89) enumerada en el spread del loop + mutation self-check que muerde "influencia". Copy factual confirmado. Arrays/self-checks existentes intactos. Guard verde.</done>
</task>

<task type="auto">
  <name>Task 3: Suite completa verde + tsc limpio (gate del plan)</name>
  <files>app/lib/lockdown-guard.test.ts, app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - .planning/phases/95-.../95-VALIDATION.md §Sampling Rate — full suite `pnpm test` + tsc --noEmit; latencia <60s.
    - .planning/phases/95-.../95-RESEARCH.md §Test runner (líneas 87-91) — `pnpm test` = `pnpm -r --filter "./packages/*" test && pnpm --filter ./app test`; suite app 1129+ / packages 1103.
  </read_first>
  <action>
    Correr la suite completa y el typecheck para confirmar que las extensiones de guard NO regresionaron nada:
    1. `pnpm --filter ./app test` — toda la suite de app verde (1129+), incluidos los dos guards extendidos.
    2. `pnpm --filter ./app exec tsc --noEmit` — cero errores de tipo.
    3. (si el runner root está disponible) `pnpm test` — packages (1103) + app verdes.
    Si algún test previo estaba rojo por deuda pre-existente ajena a esta fase (p.ej. el drift documentado en STATE.md línea 124 sobre buscar.test.ts:193), NO lo arregles aquí — documenta en el SUMMARY que es deuda pre-existente fuera de alcance y que los DOS guards de esta fase están verdes.
  </action>
  <acceptance_criteria>
    - `pnpm --filter ./app test` reporta 0 fallos (o solo el/los fallo(s) de deuda pre-existente documentada en STATE.md, con los 2 guards de esta fase verdes).
    - `tsc --noEmit` reporta 0 errores.
    - Ningún test existente de lockdown/anti-insinuación fue eliminado (conteo de it() >= el de antes + los nuevos).
  </acceptance_criteria>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit && pnpm exec vitest run lib/lockdown-guard.test.ts lib/anti-insinuacion-guard.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Suite app verde (los 2 guards extendidos pasan), tsc --noEmit limpio; cualquier rojo residual es deuda pre-existente documentada, no introducida por esta fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (público) → review CI | Los guards son tripwires estáticos que corren en la suite vitest de app/ (ci.yml). Su valor es cazar regresiones de seguridad en review; deben MORDER o son un no-op verde. |
| app/ `.rpc()` call site → PUBLIC_RPC_ALLOWLIST | Toda RPC servida por el árbol público (service_role) debe estar enumerada; una RPC no-allowlisted podría alcanzar un endpoint caro/PII. |
| allowlist ↔ migraciones | Una entrada stale/typo en el allowlist enmascara una RPC real (drift Direction-B). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-06 | Tampering | Entrada stale/typo en PUBLIC_RPC_ALLOWLIST enmascara una RPC real (drift Direction-B, no cazado hoy) | mitigate | Assert Direction-B (allowlist ⊆ definidas repo-wide) con regex `public.`-opcional + mutation self-check fantasma que prueba que muerde |
| T-95-07 | Information Disclosure / Elevation | Un cross-link nuevo agregado vía `crossLinkReader(variable)` escaparía a Direction-A (rpcPattern estático solo matchea `.rpc("lit")`) → RPC no-allowlisted servida | mitigate | Detector (A3) que extrae los literales de `crossLinkReader("...")` del call site y los verifica ⊆ allowlist + self-check con nombre no-listado |
| T-95-08 | Repudiation / (legal) | Copy insinuante en la superficie deep-link (89) no escaneada = difamación de sujeto hostil; la correctitud ES la defensa legal | mitigate | Enumerar SUPERFICIES_DEEPLINK (validacion-fuente) en el loop del linter + mutation self-check que muerde "influencia" |
| T-95-09 | Denial of Service (falso verde) | Un guard nuevo anclado en process.cwd() escanea 0 archivos y pasa vacío (Pitfall 5, bug v8.1) | mitigate | Anclar en APP_ROOT/import.meta.dirname (idiom del archivo) + sanity assert (`definedRpcNames().size > 20`; ≥4 literales cross-link) que falla fuerte si escaneó nada |
| T-95-10 | Elevation of Privilege (regresión) | Regex Direction-B con `public.` obligatorio → 3 falsos orphans (match_proyectos/parlamentario_publico/parlamentarios_publico) → guard rojo → se relaja/borra el assert perdiendo la protección | mitigate | Regex con `(?:public\.)?` OPCIONAL desde el diseño (verificado: esas 3 RPCs se definen sin qualifier) → guard verde de primera, sin tentación de relajarlo |
| T-95-SC | Tampering | npm/pip/cargo installs | mitigate | Esta fase instala CERO paquetes (solo edita `.test.ts` existentes); no aplica audit de legitimidad |
</threat_model>

<verification>
- `pnpm --filter ./app test lockdown-guard` verde: Direction-B (0 orphans reales + self-check fantasma + sanity), (A3) cross-links (⊆ allowlist + self-check).
- `pnpm --filter ./app test anti-insinuacion-guard` verde: SUPERFICIES_DEEPLINK escaneada + self-check muerde.
- `pnpm --filter ./app test` (1129+) verde; `tsc --noEmit` limpio.
- Ningún guard/pgTAP/RPC de PROD tocado (plan puro app/lib, cero contacto con la DB viva).
</verification>

<success_criteria>
- SC#1 cerrado: guards re-corridos y extendidos sobre superficies nuevas (Direction-A ya cubre servida⊆allowlist; se añade cobertura del blind-spot crossLinkReader; anti-insinuación gana la superficie 89).
- SC#3 cerrado: drift bidireccional = 0 (Direction-A existente + Direction-B nueva con regex correcto).
- SC#4 cerrado: cada guard extendido gana su mutation self-check sobre lo NUEVO (Direction-B fantasma, cross-link no-listado, deep-link "influencia") — los guards MUERDEN, no pasan por vacío.
</success_criteria>

<output>
Crear `.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-02-SUMMARY.md` al terminar.
</output>
