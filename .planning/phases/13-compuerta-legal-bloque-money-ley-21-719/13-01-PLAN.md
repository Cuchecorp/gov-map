---
phase: 13-compuerta-legal-bloque-money-ley-21-719
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/money-gate.ts
  - app/lib/money-gate.test.ts
  - .env.example
  - supabase/tests/0023_money_gate.test.sql
autonomous: false
requirements: [LEGAL-01]
user_setup: []

must_haves:
  truths:
    - "El flag MONEY_PUBLIC_ENABLED nace OFF: sin la var, money-gate devuelve false (fail-closed)"
    - "Solo el literal 'true' enciende el flag; 'false', '1', '', undefined => false"
    - "El flag es server-only y nunca lleva prefijo NEXT_PUBLIC_ (no llega al bundle del browser)"
    - ".env.example documenta MONEY_PUBLIC_ENABLED=false con su contrato (encender depende de signoff: approved)"
    - "Existe un pgTAP que re-afirma el piso PII deny-by-default (RLS + cero policies + anon sin SELECT) como contrato que las tablas MONEY de 14-16 heredan"
  artifacts:
    - path: "app/lib/money-gate.ts"
      provides: "Flag server-side moneyPublicEnabled(env), default fail-closed false"
      contains: "server-only"
    - path: "app/lib/money-gate.test.ts"
      provides: "Vitest: default false + solo 'true' enciende"
    - path: "supabase/tests/0023_money_gate.test.sql"
      provides: "pgTAP re-afirmacion del piso PII deny-by-default (contrato gate MONEY)"
      contains: "relrowsecurity"
    - path: ".env.example"
      provides: "Contrato documentado del flag MONEY_PUBLIC_ENABLED"
      contains: "MONEY_PUBLIC_ENABLED"
  key_links:
    - from: "app/lib/money-gate.ts"
      to: "process.env.MONEY_PUBLIC_ENABLED"
      via: "lectura fail-closed === 'true'"
      pattern: "MONEY_PUBLIC_ENABLED.*===.*\"true\""
    - from: "supabase/tests/0023_money_gate.test.sql"
      to: "pg_class.relrowsecurity / pg_policy"
      via: "assercion deny-by-default"
      pattern: "relrowsecurity|pg_policy"
---

<objective>
Construir el **candado B (presentación)** y la **verificación del candado A (datos)** del doble candado de exposición MONEY (LEGAL-01, decisión D-25 de CONTEXT):

1. `app/lib/money-gate.ts` — flag server-only `moneyPublicEnabled(env)`, fail-closed (default `false`, solo `"true"` literal lo enciende), espejo de `app/lib/supabase.ts` (server-only, sin `NEXT_PUBLIC_`) y `packages/llm/src/config.ts` (env inyectado, testeable).
2. `app/lib/money-gate.test.ts` — Vitest que afirma default `false` + fail-closed, estilo de `packages/llm/src/config.test.ts`.
3. `.env.example` — documenta `MONEY_PUBLIC_ENABLED=false` y su contrato (encender depende de `signoff: approved`).
4. `supabase/tests/0023_money_gate.test.sql` — pgTAP que **re-afirma el piso PII deny-by-default** (RLS + cero policies + `anon` sin SELECT) sobre la tabla-exemplar existente, codificando el contrato que las tablas MONEY de Phases 14-16 heredarán. **Phase 13 NO introduce DDL MONEY** (eso es Phase 14, `packages/dinero`).

Purpose: dejar el gate de exposición apagado y verificado, listo para que Phases 14-16 construyan bajo él sin encender ninguna ruta pública hasta el sign-off humano (deuda F13).
Output: módulo del flag + sus tests, `.env.example` actualizado, pgTAP de contrato del piso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compuerta-legal-bloque-money-ley-21-719/13-CONTEXT.md
@.planning/phases/13-compuerta-legal-bloque-money-ley-21-719/13-RESEARCH.md
@.planning/phases/13-compuerta-legal-bloque-money-ley-21-719/13-PATTERNS.md

# Analogs a copiar (NO re-leer la base entera — extraer el patrón en una pasada):
@app/lib/supabase.ts
@packages/llm/src/config.ts
@packages/llm/src/config.test.ts
@supabase/tests/0018_piso_pii.test.sql
@.env.example

# AVISO AGENTS.md de app/: "This is NOT the Next.js you know". Antes de tocar
# codigo bajo app/, leer node_modules/next/dist/docs/ para confirmar convenciones
# del Next.js instalado. money-gate.ts es logica pura server-only — sin APIs de Next.

<interfaces>
<!-- Contrato del flag server-side. Espejo de app/lib/supabase.ts y config.ts. -->
<!-- El executor implementa exactamente esta firma; los consumidores (Phases 14-16) la usan. -->

```typescript
// app/lib/money-gate.ts
import "server-only";
// Default fail-closed: solo el literal "true" enciende. Cualquier otro valor
// (incluido undefined/""/"false"/"1") => false.
export function moneyPublicEnabled(
  env?: Record<string, string | undefined>,
): boolean;
```

Patron de env inyectado (de config.ts): la funcion recibe `env` explicito para
ser testeable sin tocar `process.env` global; por defecto usa `process.env`.
Patron server-only (de supabase.ts): `import "server-only"` en linea 1; la var
NO lleva prefijo `NEXT_PUBLIC_` (no debe viajar al bundle del navegador).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag server-only MONEY_PUBLIC_ENABLED (candado B) + Vitest</name>
  <files>app/lib/money-gate.ts, app/lib/money-gate.test.ts</files>
  <behavior>
    - moneyPublicEnabled({}) === false (default fail-closed; var ausente)
    - moneyPublicEnabled({ MONEY_PUBLIC_ENABLED: "false" }) === false
    - moneyPublicEnabled({ MONEY_PUBLIC_ENABLED: "1" }) === false
    - moneyPublicEnabled({ MONEY_PUBLIC_ENABLED: "TRUE" }) === false (case-sensitive, solo "true")
    - moneyPublicEnabled({ MONEY_PUBLIC_ENABLED: "true" }) === true (unico valor que enciende)
    - El modulo importa "server-only" en la primera linea
  </behavior>
  <action>
Crear `app/lib/money-gate.ts` como logica pura server-only que materializa el candado B del doble candado (D-25). Primera linea: `import "server-only";` (espejo de `app/lib/supabase.ts:1`) para garantizar que el flag NUNCA llega al bundle del navegador. Exportar `moneyPublicEnabled(env: Record<string, string | undefined> = process.env): boolean` que devuelve `env.MONEY_PUBLIC_ENABLED === "true"` y nada mas — fail-closed: cualquier otro valor (undefined, "", "false", "1", "TRUE") devuelve false (NO usar truthiness laxa tipo Boolean(env.X), que dejaria pasar "false"). Recibir `env` inyectado por defecto `process.env`, espejo de `loadRouterConfigFromEnv` en `packages/llm/src/config.ts:47`, para testeo sin runtime. NO lanzar ante ausencia: a diferencia de `supabase.ts` (que lanza si falta config), aqui la ausencia ES el default seguro (OFF), per D-26. La var NO lleva prefijo `NEXT_PUBLIC_`. Documentar en comentario de cabecera que encender requiere `signoff: approved` en el dossier (D-32) y enlazar a deuda F13.

Crear `app/lib/money-gate.test.ts` colocado junto al modulo (convencion del repo: `app/lib/buscar.test.ts`, `format.test.ts`), estilo de `packages/llm/src/config.test.ts` (describe/it, env inyectado explicito). Cubrir exactamente los casos del bloque <behavior>.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/money-gate.test.ts</automated>
  </verify>
  <done>money-gate.ts exporta moneyPublicEnabled fail-closed con import "server-only" en linea 1; el test colocado pasa con todos los casos del bloque behavior verdes; ningun uso de NEXT_PUBLIC_ ni de truthiness laxa.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar MONEY_PUBLIC_ENABLED en .env.example + pgTAP de contrato del piso</name>
  <files>.env.example, supabase/tests/0023_money_gate.test.sql</files>
  <action>
**(a) `.env.example`** — Anadir, despues de la seccion existente (tras la linea 39), un bloque documentado (espejo del estilo de comentario `# CR-01...` en `.env.example:21-32`):
- Cabecera de seccion: gate de exposicion MONEY (Ley 21.719, Phase 13).
- Comentario que indique: default OFF; encender SOLO tras sign-off legal humano (`signoff: approved` en `docs/legal/13-LEGAL-DOSSIER.md`); server-only, leido por `app/lib/money-gate.ts`; NUNCA prefijar `NEXT_PUBLIC_` (no debe llegar al bundle del navegador); no es un secreto sino un booleano de feature.
- Linea: `MONEY_PUBLIC_ENABLED=false`.
NO anadir el canal Postgres (`app.settings.money_public_enabled`): el consumidor declarado en CONTEXT es Next.js server-side (ficha), no un RPC SQL; Phases 14-16 lo anadiran si introducen un RPC que consulte el flag (Open Question 3 del research, diferida).

**(b) `supabase/tests/0023_money_gate.test.sql`** — Crear el pgTAP que **re-afirma el piso PII deny-by-default** como contrato que las tablas MONEY de 14-16 heredaran. Phase 13 NO crea tablas MONEY (D-domain: eso es Phase 14, `packages/dinero`), asi que este test verifica la **invariante sobre la tabla-exemplar existente** `pii_contraparte_declaracion` (creada en 0018), espejo VERBATIM de la estructura de `supabase/tests/0018_piso_pii.test.sql`. Copiar el comentario-cabecera textual de 0018 (lineas 1-5: que pgTAP es la UNICA prueba valida — build/typecheck dan falso positivo, Pitfall 4). Estructura `begin; select plan(N); ...asserts...; select * from finish(); rollback;`. Las tres aserciones que codifican el contrato del gate MONEY, sobre `pii_contraparte_declaracion`:
  1. `is(... pg_class where relname='pii_contraparte_declaracion' and relrowsecurity=true ..., 1, 'RLS enabled ...')` — RLS habilitada (candado A).
  2. `is_empty($$ select polname from pg_policy p join pg_class c on c.oid=p.polrelid where c.relname='pii_contraparte_declaracion' $$, 'ninguna policy (deny-by-default)')` — cero policies = deny efectivo a anon.
  3. `is((select count(*)::int from information_schema.role_table_grants where table_name='pii_contraparte_declaracion' and grantee='anon' and privilege_type='SELECT'), 0, 'anon SIN grant SELECT (tercero privado, Ley 21.719)')` — refuerzo de `0022_probidad.test.sql:59-62`: el privilegio tampoco existe (default privileges cerrados, leccion Phase 11).
Comentario de cabecera adicional: este test documenta el CONTRATO que toda tabla `money_*` de Phases 14-16 debe satisfacer al nacer (deny-by-default + revoke anon), per RESEARCH Pattern 1. Ajustar `plan(N)` al numero exacto de aserciones.
  </action>
  <verify>
    <automated>grep -v '^#' .env.example | grep -c 'MONEY_PUBLIC_ENABLED=false'</automated>
    <automated>grep -E "relrowsecurity|is_empty|role_table_grants" supabase/tests/0023_money_gate.test.sql</automated>
  </verify>
  <done>.env.example contiene MONEY_PUBLIC_ENABLED=false con su bloque de comentario de contrato; 0023_money_gate.test.sql existe con las 3 aserciones deny-by-default sobre pii_contraparte_declaracion + comentario-cabecera del Pitfall 4 y del contrato MONEY; plan(N) coincide con el numero de aserciones.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>El pgTAP `supabase/tests/0023_money_gate.test.sql` re-afirma el piso PII deny-by-default contra el schema. NO se ejecuto contra el remoto automaticamente: aplicar/correr pgTAP contra Supabase remoto es accion de operador (memoria de proyecto: DDL remoto funciona via `db push --db-url` pero es operator-confirmado; el BOM UTF-8 en `.env` rompe el CLI). Phase 13 NO anade migracion nueva, asi que NO hay `db push` que correr — solo el test pgTAP contra el schema ya aplicado.</what-built>
  <how-to-verify>
    1. Extraer la URL de DB esquivando el BOM UTF-8 (codepoint U+FEFF), reusando el mismo helper de extraccion que ya usaron Phases 9-12 (NO re-tipear un regex con el caracter literal; usar el script existente de Phases 9-12, que ya quita ese codepoint inicial). Resultado: el valor de `SUPABASE_DB_URL` sin el BOM inicial.
    2. Correr el pgTAP contra el remoto aplicado:
       `supabase test db --db-url "<URL_EXTRAIDA>"`
    3. Confirmar que `0023_money_gate.test.sql` pasa (todas las aserciones verdes) junto al resto de la suite pgTAP existente (0018/0021/0022 siguen verdes — el piso no se rompio).
  </how-to-verify>
  <resume-signal>Escribe "pgTAP verde" (con el conteo de tests que pasaron) o pega el error si alguna asercion falla.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to app server | El flag de gate NUNCA debe cruzar al cliente (decision de exposicion server-only). |
| anon role to Postgres tablas MONEY/PII | Roles no privilegiados no deben leer datos no publicos antes del sign-off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `app/lib/money-gate.ts` (flag) | mitigate | `import "server-only"` linea 1 + var SIN `NEXT_PUBLIC_`; Vitest afirma default false; el flag jamas se serializa al bundle. |
| T-13-02 | Information Disclosure | Default privileges de Supabase sobre tablas PII/MONEY | mitigate | pgTAP afirma `anon` sin grant SELECT (`role_table_grants`) — codifica el `revoke` que toda tabla money_* hereda (leccion Phase 11). |
| T-13-03 | Tampering / EoP | Verificacion RLS por CI verde sin DDL aplicado | mitigate | pgTAP corre contra schema APLICADO (checkpoint de operador, Task 3); build/typecheck explicitamente NO cuentan (Pitfall 4). |
| T-13-04 | Elevation of Privilege | Encender el flag sin `signoff: approved` | accept | Fuera de esta corrida (accion de operador, D-26/deferred). El test default-false + la inspeccion del YAML del dossier (13-02) son la barrera; documentado en `.env.example`. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Phase 13 NO instala paquetes (RESEARCH seccion Installation: "Ninguna"). No hay superficie de cadena de suministro. |
</threat_model>

<verification>
- `cd app && pnpm vitest run lib/money-gate.test.ts` -> verde (flag default false, fail-closed).
- `.env.example` contiene `MONEY_PUBLIC_ENABLED=false` con bloque de contrato (no `NEXT_PUBLIC_`).
- `supabase/tests/0023_money_gate.test.sql` existe con 3 aserciones deny-by-default + comentario Pitfall 4.
- [OPERADOR] `supabase test db --db-url <url>` -> `0023` y el resto de la suite pgTAP verdes contra el remoto aplicado.
- Grep negativo: `grep -rn "NEXT_PUBLIC_MONEY" app/ .env.example` -> 0 resultados.
</verification>

<success_criteria>
Mapeo a ROADMAP Phase 13 success criteria:
- **SC3** (sign-off prerrequisito duro y verificable; ninguna ruta MONEY se expone hasta aprobacion): el flag nace OFF fail-closed (Vitest verde) + el piso deny-by-default queda verificado por pgTAP; encender depende de `signoff: approved` documentado en `.env.example` y en el dossier (13-02). El doble candado (RLS + flag) esta construido y verificado, apagado.
- Soporta **SC2** (minimizacion): el pgTAP re-afirma que la PII de terceros (`pii_contraparte_declaracion`) sigue deny-by-default a `anon` — RUT/familiares/terceros internos, nunca expuestos.
</success_criteria>

<output>
Create `.planning/phases/13-compuerta-legal-bloque-money-ley-21-719/13-01-SUMMARY.md` when done
</output>
