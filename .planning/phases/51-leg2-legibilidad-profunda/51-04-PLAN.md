---
phase: 51-leg2-legibilidad-profunda
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
autonomous: true
requirements: [LEG2, SC6, SC9]
user_setup: []

must_haves:
  truths:
    - "La vista por defecto de lobby agrupa por contraparte, ordenada por frecuencia DESC"
    - "Cada grupo muestra contraparte + conteo + rango/lista de fechas, sin componer con votos"
    - "El toggle ?vista=cronologica preserva la vista paginada actual"
    - "El caveat de identidad aparece UNA vez al tope de la sección, no por fila"
    - "La contraparte sigue siendo texto crudo verbatim, NUNCA enlazada"
  artifacts:
    - path: "app/components/lobby-de-parlamentario.tsx"
      provides: "agruparPorContraparte + vista default agrupada + toggle ?vista + caveat 1×"
      contains: "agruparPorContraparte"
    - path: "app/components/lobby-de-parlamentario.test.tsx"
      provides: "RTL: agrupación por frecuencia, toggle, caveat 1×, sin IdentityMarker por fila"
      contains: "agruparPorContraparte"
  key_links:
    - from: "app/components/lobby-de-parlamentario.tsx"
      to: "?vista searchParam"
      via: "toggle server-driven entre agrupado y cronológico"
      pattern: "vista"
---

<objective>
SC6 (lobby agrupado por contraparte / B11): la sección de lobby pasa de una lista cronológica larga con un `IdentityMarker` por fila, a una vista agrupada por contraparte (orden por frecuencia DESC) como DEFAULT, con un caveat de identidad único por sección y un toggle server-driven al orden cronológico existente.

Purpose: "¿Con quién se reúne más?" es la pregunta ciudadana; agrupar por contraparte la responde sin ruido. El caveat 1×/sección reduce la repetición sin erosionar la advertencia de identidad.
Output: `lobby-de-parlamentario.tsx` extendido, tests RTL nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Piezas existentes a reusar (lobby-de-parlamentario.tsx). -->
LobbyView (:124) / LobbySection (:319) — el RPC lobby_de_parlamentario(p_id) trae TODO (sin limit, :341)
agruparAudiencias (:278) → audiencias normalizadas (left join → N filas/audiencia ya colapsadas)
ContraparteCruda (:99) con IdentityMarker por fila (:118) — verbatim (:103), NUNCA enlazada (RPC no emite contraparte_id)
paginación ?lobbyPage (:368-373), href builder (:95)
intro/caveat idiom (LobbyView:133-139) · conteo neutro idiom (:174-180)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper agruparPorContraparte + vista agrupada por defecto + caveat 1×</name>
  <files>app/components/lobby-de-parlamentario.tsx, app/components/lobby-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx:99-139 (ContraparteCruda/IdentityMarker/intro), :278-373 (agruparAudiencias/LobbySection/paginación), :341 (fetch RPC todo)
    - app/lib/format.ts (fechaCorta — NO hand-roll)
    - app/components/lobby-de-parlamentario.test.tsx (convención RTL + fixtures)
    - .planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md (sección lobby: contraparte verbatim, nunca enlazada; caveat 1×)
  </read_first>
  <behavior>
    - agruparPorContraparte(audiencias) → Grupo[] { contraparte, n, fechas[] } ordenado por n DESC. La contraparte se agrupa por su nombre crudo verbatim (contraparte_nombre); audiencias sin contraparte_nombre se manejan honesto (grupo aparte o excluidas de la agrupación por contraparte, nunca fabricando un nombre).
    - Vista por defecto (sin ?vista o ?vista != cronologica): renderiza los grupos: "{contraparte} — {N} reuniones: {fechas}" (h3 para contraparte, Mono para N/fechas). Cero composición con votos/dinero.
    - Caveat de identidad UNA vez al tope de la sección: "Las contrapartes se muestran tal como las registra la fuente; su identidad no está verificada." (reusa el idiom del intro; ámbar --identity-warn-*).
    - QUITAR el <IdentityMarker/> por fila de ContraparteCruda (el caveat de sección lo reemplaza). La contraparte sigue verbatim y NUNCA enlazada.
  </behavior>
  <action>
    Añadir el helper puro exportado `agruparPorContraparte(audiencias)` que agrupa por `contraparte_nombre` crudo, cuenta `n` y colecta `fechas`, ordenado por `n` DESC (el RPC ya trae todas las filas — bounded a cientos; si hiciera falta paginar el RPC en lotes, resolverlo server-side sin cambiar el contrato público). Hacer la vista agrupada el DEFAULT de `LobbySection`. Cada grupo: h3 con contraparte verbatim + "— {N} reuniones: {fechas}" (Mono para N/fechas), reusando el conteo neutro idiom (:174-180). Añadir el caveat de identidad UNA vez al tope de la sección (idiom del intro LobbyView:133-139, ámbar). Eliminar el `<IdentityMarker/>` por fila de `ContraparteCruda:118` (el caveat de sección lo cubre); mantener el nombre verbatim (:103) y NUNCA enlazarlo. Extender el test RTL: (a) fixture con {Enel×4, Codelco×2} → el primer grupo es Enel con "4 reuniones" y las fechas; orden por frecuencia; (b) el caveat de identidad aparece exactamente 1 vez en la sección (assert de conteo); (c) NO hay IdentityMarker por fila en la vista agrupada; (d) la contraparte no es un enlace (assert de que no hay <a> con el nombre de contraparte); (e) negative-match banned-vocab sobre el copy nuevo.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run lobby-de-parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - `agruparPorContraparte` exportado y testeado: {Enel×4, Codelco×2} → orden [Enel(4), Codelco(2)].
    - Test: la vista por defecto muestra grupos "{contraparte} — {N} reuniones"; Enel primero.
    - Test: el caveat de identidad aparece exactamente 1 vez (no por fila).
    - Test: la contraparte NO está dentro de un `<a>`/Link.
    - Test de banned-vocab verde sobre el copy nuevo.
    - `pnpm --dir app test -- --run lobby-de-parlamentario` verde.
  </acceptance_criteria>
  <done>Vista agrupada por contraparte (freq DESC) por defecto; caveat 1×/sección; IdentityMarker por fila removido; contraparte verbatim nunca enlazada.</done>
</task>

<task type="auto">
  <name>Task 2: Toggle ?vista=cronologica que preserva la vista paginada</name>
  <files>app/components/lobby-de-parlamentario.tsx, app/components/lobby-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx:95 (href builder de ?lobbyPage), :124-373 (LobbyView paginada + LobbySection)
    - app/components/patrimonio-de-parlamentario.tsx:779-782 (single() para string[])
    - app/AGENTS.md (Next 16: searchParams ya resuelto en el section, no re-await)
  </read_first>
  <action>
    Añadir un toggle server-driven de dos enlaces al tope de la sección: "Agrupar por contraparte" (default, activo) y "Ver en orden cronológico" → `?vista=cronologica`. Leer `vista` desde el `searchParams` ya resuelto (helper `single()`); si `vista === 'cronologica'` renderizar la `LobbyView` paginada existente (`?lobbyPage`) SIN cambios; en cualquier otro caso, la vista agrupada (Task 1). El enlace activo lleva el estado activo petróleo (UI-SPEC reserved-for). El toggle preserva el resto de params relevantes. Normalizar `vista` (solo 'cronologica' activa la vista cronológica; cualquier otro valor → default agrupado — fail-safe). Extender el test RTL: (a) sin `?vista` → vista agrupada; (b) `?vista=cronologica` → aparece la lista cronológica paginada (assert de una fila individual con fecha); (c) `?vista=basura` → cae a la vista agrupada (default); (d) el enlace activo tiene el marcador de estado activo.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run lobby-de-parlamentario && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Test: `?vista=cronologica` renderiza la LobbyView paginada existente; sin `?vista` renderiza la agrupada.
    - Test: `?vista=basura` cae al default agrupado (normalización fail-safe).
    - Los dos enlaces del toggle existen; el activo tiene el estado activo petróleo.
    - `pnpm --dir app test -- --run lobby-de-parlamentario` verde; `tsc -b` limpio.
  </acceptance_criteria>
  <done>Toggle server-driven agrupado↔cronológico; la vista cronológica paginada se preserva intacta detrás del toggle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?vista/?lobbyPage searchParams → Server Component | Entrada no confiable como flag |
| contraparte_nombre → DOM | Identidad no verificada; nunca debe implicar identidad confirmada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-11 | Spoofing (identidad falsa) | contraparte agrupada | mitigate | Contraparte verbatim, NUNCA enlazada (RPC no emite contraparte_id); caveat de identidad 1×/sección visible |
| T-51-12 | Tampering/Injection | ?vista | mitigate | Normalizado a un enum efectivo ('cronologica' o default); nunca SQL crudo |
| T-51-13 | Repudiation (dato falso) | audiencia sin contraparte | mitigate | Manejo honesto (grupo aparte o excluida), nunca fabrica un nombre |
| T-51-SC | Tampering | npm installs | accept | Zero new deps |
</threat_model>

<verification>
- `pnpm --dir app test -- --run lobby-de-parlamentario` verde.
- `pnpm --dir app exec tsc -b` limpio.
- Negative-match banned-vocab sobre el copy nuevo.
</verification>

<success_criteria>
SC6: vista agrupada por contraparte (freq DESC) por defecto + toggle cronológico + caveat 1×/sección; contraparte verbatim nunca enlazada. SC9 (parcial): suite + tsc verdes, anti-insinuación intacta.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-04-SUMMARY.md` when done
</output>
