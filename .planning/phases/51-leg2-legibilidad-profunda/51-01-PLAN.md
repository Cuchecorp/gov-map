---
phase: 51-leg2-legibilidad-profunda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0047_rebeldias_honestas.sql
  - supabase/tests/0047_rebeldias_honestas.test.sql
  - app/lib/lockdown-guard.test.ts
autonomous: false
requirements: [LEG2, SC5, SC9]
user_setup: []

must_haves:
  truths:
    - "El RPC rebeldias_de_parlamentario ya no cuenta ausencias como 'votó distinto'"
    - "Cada fila de rebeldías trae el título del proyecto (null honesto si no hay proyecto)"
    - "No hay filas duplicadas por votación (dedupe por votacion_id)"
    - "El RPC sigue siendo security definer con search_path='' y solo emite derivado público (nunca partido/rut)"
    - "anon puede ejecutar el RPC (sigue en PUBLIC_RPC_ALLOWLIST, sin nueva superficie)"
    - "lockdown-guard sigue verde: exime SOLO grant execute sobre RPCs allowlisted, bloquea cualquier otro grant a anon"
  artifacts:
    - path: "supabase/migrations/0047_rebeldias_honestas.sql"
      provides: "drop+recreate del RPC rebeldías sin ausencias + título + dedupe + doble revoke/grant"
      contains: "drop function if exists rebeldias_de_parlamentario"
    - path: "supabase/tests/0047_rebeldias_honestas.test.sql"
      provides: "pgTAP: firma, security definer, search_path, grant anon, exclusión ausencias, dedupe, título"
      contains: "rebeldias_de_parlamentario"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "guard refinado: grant execute on function sobre RPC allowlisted exento; otro grant a anon falla"
      contains: "PUBLIC_RPC_ALLOWLIST"
  key_links:
    - from: "supabase/migrations/0047_rebeldias_honestas.sql"
      to: "PUBLIC_RPC_ALLOWLIST"
      via: "rebeldias_de_parlamentario ya está en la lista (sin cambio de allowlist)"
      pattern: "rebeldias_de_parlamentario"
---

<objective>
Ajustar el RPC `rebeldias_de_parlamentario` (SC5/B5) para que sea honesto: excluir ausencias del cálculo y de la salida, hidratar el título del proyecto, deduplicar por votación. Reconciliar el `lockdown-guard` con el `grant execute ... to anon` que la migración debe re-emitir (Open Question 1, resuelta: eximir SOLO grant execute sobre RPCs allowlisted). Este es el ÚNICO artefacto de base de datos de la fase.

Purpose: Hoy una ausencia (`seleccion='ausente'`) cuenta como "votó distinto a su bancada" — una insinuación falsa que B5 debe cerrar. La asistencia ya es métrica propia; rebeldías debe medir SOLO disidencia real.
Output: migración 0047 (drop+recreate por 42P13), pgTAP acompañante, guard refinado, checkpoint de operador para el apply remoto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Contrato del RPC (contrato ÚNICO compartido con el consumidor 51-02). Pinear estas columnas exactas. -->
RPC rebeldias_de_parlamentario(p_id text) RETURNS TABLE:
  votacion_id text, boletin text, titulo text, etapa text, fecha timestamptz,
  seleccion_propia text, mayoria_bancada text
-- Firma vigente (0019): SIN titulo/etapa; returns table crece → drop obligatorio (42P13).
-- Ya está en PUBLIC_RPC_ALLOWLIST (lockdown-guard.test.ts:170). NO cambia la allowlist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir pgTAP 0047 (test-first, Wave 0) para el RPC honesto</name>
  <files>supabase/tests/0047_rebeldias_honestas.test.sql</files>
  <read_first>
    - supabase/tests/0041_cruces_rpc_fecha_captura.test.sql (idiom pgTAP del proyecto: proargnames vía array_to_string ordenado, NO pg_get_function_result; asserts anon-execute y no-PII)
    - supabase/migrations/0019_voto_asistencia_y_ficha.sql:73-104 (firma vigente + grants)
    - supabase/migrations/0041_cruces_rpc_fecha_captura.sql (molde drop+recreate + doble revoke)
  </read_first>
  <action>
    Crear el pgTAP que valida el RPC objetivo ANTES de escribir la migración. Asserts obligatorios: (1) la función existe con la firma de parámetros `(text)`; (2) el returns table contiene exactamente las columnas `votacion_id, boletin, titulo, etapa, fecha, seleccion_propia, mayoria_bancada` en ese orden (usar `array_to_string(proargnames, ',')` sobre `pg_proc`, idiom de 0041, NUNCA pg_get_function_result); (3) la función es `security definer`; (4) `proconfig` contiene `search_path=`; (5) `anon` tiene privilegio EXECUTE (`has_function_privilege('anon', ..., 'execute')` = true, espejo invertido del deny en 0040); (6) exclusión de ausencias: con un parlamentario cuyas únicas filas de disidencia son `seleccion='ausente'` (fixture tipo Alessandri, hoy 7 filas todas ausencias), el RPC devuelve 0 filas; (7) dedupe: no hay dos filas con el mismo `votacion_id`. Envolver los asserts de datos en un bloque que siembre filas mínimas de `voto`/`votacion`/`parlamentario`/`proyecto` en una transacción de test (rollback), NO depender de datos PROD. Cabecera del archivo: se corre por psql local/operador FUERA del glob vitest (convención del proyecto).
  </action>
  <verify>
    <automated>MISSING — pgTAP corre por psql, no por vitest. Verificación estructural: grep -q "array_to_string(proargnames" supabase/tests/0047_rebeldias_honestas.test.sql && grep -q "has_function_privilege('anon'" supabase/tests/0047_rebeldias_honestas.test.sql</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe y contiene `select plan(` con al menos 7 asserts.
    - Contiene un assert que verifica el orden exacto de columnas vía `array_to_string(proargnames`.
    - Contiene un assert `has_function_privilege('anon'` con expected true.
    - Contiene un assert de "0 filas cuando todas las disidencias son ausencias".
    - No usa `pg_get_function_result` (idiom prohibido por el proyecto).
  </acceptance_criteria>
  <done>pgTAP 0047 escrito con los 7 asserts; siembra datos en transacción de test con rollback; documenta que corre por psql/operador.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir migración 0047 (drop+recreate honesto + doble revoke/grant)</name>
  <files>supabase/migrations/0047_rebeldias_honestas.sql</files>
  <read_first>
    - supabase/migrations/0019_voto_asistencia_y_ficha.sql:70-104 (RPC vigente: WITH yo/mayoria, security definer set search_path='', grants)
    - supabase/migrations/0041_cruces_rpc_fecha_captura.sql (molde drop+recreate por 42P13 + doble revoke re-emitido por DEFAULT PRIVILEGES)
    - supabase/migrations/0028_votos_instructivos.sql:23-32 (cabecera-advertencia "build/typecheck NO prueban que Postgres ejecutó el DDL" + etapa/join votacion como fuente de `etapa`)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Code Examples 4: esbozo SQL [ASSUMED], validar semántica mode()/DISTINCT ON)
  </read_first>
  <action>
    Crear la migración numerada 0047 (la última en disco es 0046_drop_web_reader.sql). Empezar con `drop function if exists rebeldias_de_parlamentario(text);` (42P13: el returns table crece con `titulo`/`etapa`). Recrear con `language sql stable security definer set search_path = ''`. Cambios vs 0019: (a) la CTE `mayoria` computa la opción modal `mode() within group (order by v.seleccion)` añadiendo `and v.seleccion <> 'ausente'` (la mayoría se calcula sin ausencias); (b) el SELECT final usa `select distinct on (v.votacion_id)` para deduplicar por votación; (c) añadir `and v.seleccion <> 'ausente'` al WHERE final (una ausencia PROPIA no es "votó distinto"); (d) `left join public.proyecto pr on pr.boletin = vo.boletin` para hidratar `pr.titulo` (null honesto); (e) exponer `etapa` desde `vo.etapa` — verificar que `votacion.etapa` existe (misma fuente que `votos_de_parlamentario` en 0028); si `votacion` no tiene `etapa`, dejar `etapa` como null (honesto) y anotarlo. Returns table EXACTO: `votacion_id text, boletin text, titulo text, etapa text, fecha timestamptz, seleccion_propia text, mayoria_bancada text`. Cerrar con `revoke all on function rebeldias_de_parlamentario(text) from public;` y `grant execute on function rebeldias_de_parlamentario(text) to anon;` (el drop+recreate re-concede por DEFAULT PRIVILEGES a anon → el revoke/grant explícito es obligatorio). Cabecera del archivo: (1) advertencia LOUD estilo 0028 ("apply remoto por psql --db-url = checkpoint operador; build/typecheck NO prueban que Postgres ejecutó el DDL"); (2) razón del `grant ... to anon`: el RPC YA está en PUBLIC_RPC_ALLOWLIST y era público desde 0019 — esto preserva el status quo, no abre superficie nueva; el guard lo exime por estar allowlisted (ver Task 3). CERO policy nueva, CERO grant select sobre `parlamentario` (LEGAL-03).
  </action>
  <verify>
    <automated>grep -q "drop function if exists rebeldias_de_parlamentario(text)" supabase/migrations/0047_rebeldias_honestas.sql && grep -q "security definer set search_path = ''" supabase/migrations/0047_rebeldias_honestas.sql && grep -q "seleccion <> 'ausente'" supabase/migrations/0047_rebeldias_honestas.sql</automated>
  </verify>
  <acceptance_criteria>
    - Contiene `drop function if exists rebeldias_de_parlamentario(text);` antes del create.
    - El returns table declara las 7 columnas en el orden pineado (grep de `titulo text, etapa text`).
    - Contiene `distinct on (v.votacion_id)`.
    - Contiene al menos dos ocurrencias de `seleccion <> 'ausente'` (CTE mayoria + WHERE final).
    - Contiene `left join public.proyecto` para hidratar título.
    - Contiene `revoke all on function rebeldias_de_parlamentario(text) from public;` Y `grant execute on function rebeldias_de_parlamentario(text) to anon;`.
    - Sigue `security definer set search_path = ''`; NO contiene ningún `grant select` sobre tabla.
  </acceptance_criteria>
  <done>Migración 0047 escrita: drop+recreate, ausencias excluidas del cálculo y salida, título/etapa hidratados, dedupe por votación, doble revoke+grant, cabecera de advertencia de operador.</done>
</task>

<task type="auto">
  <name>Task 3: Refinar lockdown-guard para eximir grant execute sobre RPC allowlisted</name>
  <files>app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/lib/lockdown-guard.test.ts:150-235 (PUBLIC_RPC_ALLOWLIST + bloque (A) guard migraciones >0044 + stripSqlComments)
    - supabase/migrations/0047_rebeldias_honestas.sql (el grant que debe pasar el guard)
  </read_first>
  <action>
    Refinar SOLO el bloque (A) test "no existe ningún GRANT … TO … anon en migraciones > 0044". Resolución LOCKED (Open Question 1): un `grant execute on function public.<name>(<args>) to anon` es EXENTO únicamente cuando `<name>` ∈ `PUBLIC_RPC_ALLOWLIST`; CUALQUIER otro grant a anon (p.ej. `grant select on <tabla> to anon`, `grant all ...`, o `grant execute on function` de una función NO allowlisted) sigue fallando fuerte. Implementación: en vez de fallar ante el primer match del regex sobre el archivo entero, extraer cada sentencia `grant ... to ... anon` (sobre el SQL con comentarios ya stripeados por `stripSqlComments`), y para cada una decidir: si casa el patrón `grant\s+execute\s+on\s+function\s+(?:public\.)?(\w+)\s*\(` con el grupo capturado en `PUBLIC_RPC_ALLOWLIST` → exenta; si no → offender. NO eximir el archivo completo (una migración podría mezclar un grant legítimo con uno ilegítimo). Añadir un test NUEVO adyacente que documente la regla con dos casos sintéticos in-memory (sin escribir a disco): (a) `grant execute on function public.rebeldias_de_parlamentario(text) to anon` → PERMITIDO (allowlisted); (b) `grant select on public.parlamentario to anon` → BLOQUEADO. Comentar el porqué: bajo Camino A anon nunca es el rol de lectura del sitio (service_role lo es); el grant execute sobre un security-definer PII-safe allowlisted no expone filas, solo permite ejecutar; el guard sigue protegiendo contra re-exposición de TABLAS.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app test -- --run lockdown-guard` pasa 7/7 (o más, con el test nuevo) EN VERDE con 0047 presente en disco.
    - El bloque (A) extrae grants por sentencia y solo exime `grant execute on function` de nombres en `PUBLIC_RPC_ALLOWLIST`.
    - Existe un test nuevo que asserta: grant execute sobre RPC allowlisted PERMITIDO; grant select sobre tabla a anon BLOQUEADO.
    - El comentario del código explica la exención (Camino A + security-definer PII-safe allowlisted).
  </acceptance_criteria>
  <done>Guard refinado y verde con 0047 presente; regla documentada por test sintético; ninguna otra ruta de grant a anon queda desprotegida.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: CHECKPOINT OPERADOR — aplicar 0047 a PROD + pgTAP (el agente NO aplica)</name>
  <action>Operador aplica la migracion 0047 y corre el pgTAP por psql --db-url, luego estampa schema_migrations. Un agente NUNCA ejecuta el apply ni toca el ledger. Pasos exactos en how-to-verify.</action>
  <what-built>
    Migración 0047 (`supabase/migrations/0047_rebeldias_honestas.sql`) + pgTAP (`supabase/tests/0047_rebeldias_honestas.test.sql`). El código y los tests estáticos pasan, PERO el RPC vive en Postgres PROD y NO surte efecto hasta que un operador aplique el DDL. `tsc`/`pnpm test` NO prueban que Postgres ejecutó el drop+recreate (falso positivo conocido — ver cabecera 0028).
  </what-built>
  <how-to-verify>
    Operador (convención LOCKED: apply remoto por psql --db-url, NUNCA `supabase db push`, NUNCA por un agente):
    1. Aplicar la migración:
       `psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/0047_rebeldias_honestas.sql`
    2. Correr el pgTAP contra el schema aplicado (debe salir verde, sin fallas):
       `psql "$SUPABASE_DB_URL" -f supabase/tests/0047_rebeldias_honestas.test.sql`
    3. Verificación de datos real (parlamentario cuyas disidencias eran todas ausencias, p.ej. Alessandri): el conteo de rebeldías debe bajar a 0; otro parlamentario con disidencia real debe conservar filas con `titulo` poblado.
    4. Estampar la versión en el ledger de migraciones siguiendo el patrón de 0041/0046:
       `insert into supabase_migrations.schema_migrations (version) values ('0047') on conflict do nothing;`
       (si el proyecto estampa en otra tabla, seguir el patrón usado para 0046).
    5. Confirmar que el deploy Cloudflare (checkpoint separado, fuera de esta fase) es lo que hace visible el cambio en el sitio.
  </how-to-verify>
  <resume-signal>Escribe "aplicada" cuando 0047 esté en PROD y el pgTAP verde, o describe el error de apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| árbol público (service_role) → RPC | El sitio ejecuta con service_role (Camino A); la DB ya no bloquea RPCs → el guard estático es la protección |
| RPC security-definer → tabla parlamentario (PII) | El RPC lee `parlamentario.partido` internamente pero solo emite derivado público |
| migración → rol anon | Un grant mal puesto reabre superficie pública |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information Disclosure | rebeldias RPC lee partido | mitigate | `security definer set search_path=''`; returns table SOLO derivado público (votacion/boletin/titulo/etapa/fecha/seleccion/mayoria); pgTAP assert de no-PII; jamás partido/rut en el returns |
| T-51-02 | Elevation of Privilege | grant execute a anon en migración >0044 | mitigate | Doble revoke+grant explícito; guard refinado exime SOLO RPC allowlisted (rebeldias ya en la lista desde 0019); status quo, no nueva superficie |
| T-51-03 | Elevation of Privilege | guard demasiado laxo (exención de archivo) | mitigate | Exención por-sentencia, no por-archivo; test sintético prueba que `grant select on tabla to anon` sigue BLOQUEADO |
| T-51-04 | Repudiation (dato falso) | apply omitido → data vieja | mitigate | Checkpoint operador blocking; pgTAP contra PROD; el código consumidor (51-02) degrada honesto pre-apply |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | No hay instalación de paquetes en esta fase (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `pnpm --dir app test -- --run lockdown-guard` verde con 0047 en disco (7/7+).
- `pnpm --dir app exec tsc -b` limpio (la migración/pgTAP no afectan tsc; el guard sí compila).
- pgTAP 0047 verde contra PROD (checkpoint operador).
</verification>

<success_criteria>
SC5: ausencias excluidas de "votó distinto", título hidratado, dedupe por votación; RPC allowlisted sin nueva superficie; guard verde. SC9 (parcial): lockdown-guard sigue verde.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-01-SUMMARY.md` when done
</output>
