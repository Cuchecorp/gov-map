---
phase: 51-leg2-legibilidad-profunda
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/estado-actual-block.tsx
  - app/components/estado-actual-block.test.tsx
  - app/components/timeline-view.tsx
  - app/components/timeline-event.tsx
  - app/components/timeline-view.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
autonomous: true
requirements: [LEG2, SC2, SC7, SC9]
user_setup: []

must_haves:
  truths:
    - "La ficha de proyecto abre con un bloque '¿Dónde está hoy?' tras el header, antes de #idea-matriz"
    - "El bloque deriva etapa/estado + último hito + urgencia vigente + 'hace N días', omitiendo la línea no derivable"
    - "Los hitos estructurales del timeline siempre están visibles, sin paginación"
    - "Solo los pares de urgencia se colapsan en una línea por período, expandible server-driven"
    - "Hay UN ProvenanceBadge en el heading de la sección timeline; cada evento conserva su link 'Ver fuente oficial ↗'"
  artifacts:
    - path: "app/components/estado-actual-block.tsx"
      provides: "Server Component '¿Dónde está hoy?' con derivación honesta + omisión"
      contains: "Dónde está hoy"
    - path: "app/components/timeline-view.tsx"
      provides: "timeline 2 niveles: hitos visibles + colapso de pares de urgencia"
      contains: "esEventoUrgencia"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "wiring del EstadoActualBlock + 1 ProvenanceBadge en heading timeline"
      contains: "EstadoActualBlock"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "app/components/estado-actual-block.tsx"
      via: "primer elemento tras el header, antes de #idea-matriz"
      pattern: "EstadoActualBlock"
    - from: "app/components/timeline-view.tsx"
      to: "?urgencias searchParam"
      via: "expand server-driven de un período de urgencia colapsado"
      pattern: "urgencias"
---

<objective>
SC2 (timeline dos niveles + "¿dónde está hoy?") + SC7 (provenance por sección): la ficha de proyecto abre con un bloque de estado actual que responde "¿dónde está hoy?" derivando de datos existentes (omitiendo lo no derivable), y el timeline colapsa SOLO los pares repetitivos de urgencia dejando todo hito estructural visible, con un único ProvenanceBadge por sección.

Purpose: El timeline plano tiene 100+ badges idénticos y pares de urgencia repetidos que entierran la señal. El bloque de estado y el colapso conservador hacen la ficha legible en segundos sin perder trazabilidad ni fabricar dato.
Output: `estado-actual-block.tsx` (nuevo) + `timeline-view/event` extendidos + wiring en la page + tests (Wave 0 gaps cubiertos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Formas y piezas existentes. -->
TramitacionEventoRow (types.ts:29): { boletin, fecha, camara, tipo, descripcion, enlace, origen, fecha_captura }
  tipo:'urgencia' → descripcion = TIPO crudo ("Suma"|"Simple"|"Discusión inmediata")
  tipo:'tramite'  → descripcion incluye "hace presente la urgencia {tipo}" / "retira ... urgencia"
timeline-view.tsx TimelineView (:9-28, lista plana <ul> rail CSS, fecha ASC)
timeline-event.tsx TimelineEvent (:13-62): dot por cámara + CamaraChip + fechaCorta + tipo + descripcion + link "Ver fuente oficial ↗" (:41-51) + ProvenanceBadge por evento (:53-59)
proyecto/[boletin]/page.tsx TimelineSection (:149-167, fecha ASC); insertar EstadoActualBlock tras header, antes de #idea-matriz
Helpers (app/lib/format.ts): fechaCorta, fechaCortaSegura, relativeTimeEs ("hace N días"+fecha abs ≥7d), esStale/STALE 14d, ProvenanceBadge
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EstadoActualBlock "¿Dónde está hoy?" (nuevo) + wiring en la page</name>
  <files>app/components/estado-actual-block.tsx, app/components/estado-actual-block.test.tsx, app/app/proyecto/[boletin]/page.tsx</files>
  <read_first>
    - app/app/proyecto/[boletin]/page.tsx (header del proyecto, orden de secciones, dónde vive #idea-matriz y TimelineSection:149-167; cómo se leen los eventos y el registro de proyecto)
    - app/components/timeline-event.tsx:41-59 (idiom link+badge a reusar)
    - app/components/patrimonio-de-parlamentario.tsx:126-137 (idiom de omisión honesta: excluir el punto cuando no derivable)
    - app/lib/format.ts (relativeTimeEs, fechaCorta/fechaCortaSegura — NO hand-roll)
    - app/AGENTS.md (verificar API de Next 16 dudosa contra node_modules/next/dist/docs/)
  </read_first>
  <behavior>
    - derivarEstadoActual(proyecto, eventos) → { etapaLinea?, ultimoHito?, urgenciaVigente?, haceCuanto? } — cada campo OPCIONAL; si no derivable, ausente (se omite la línea).
    - Etapa line: "Etapa: {etapa} · {estado}" (campos directos de la tabla proyecto). Si faltan, omitir.
    - Último hito: "Último hito: {descripción} — {fecha}" (último tramitacion_evento por fecha; fecha Mono). Si no hay eventos, omitir.
    - Urgencia vigente: "Urgencia {tipo} vigente desde el {fecha} (hace {N} días)." — derivada del último "hace presente ... urgencia {tipo}" SIN un "retira ... urgencia" posterior. Si no derivable, omitir (NO hay campo urgencia_actual en la tabla proyecto).
    - "hace N días" vía relativeTimeEs + fecha absoluta. Nunca fabricar; nunca "—" como dato.
    - Es un Server Component (RSC), NUNCA "use client". Error real de DB/red → throw (#34); ausencia → omisión honesta.
  </behavior>
  <action>
    Crear `estado-actual-block.tsx` como Server Component con un helper puro exportado `derivarEstadoActual(proyecto, eventos)` que produce las (hasta) 4 líneas, OMITIENDO cada una cuando el dato no es derivable (espejo de la omisión de `seriePatrimonio`). Heading h2 "¿Dónde está hoy?" (pregunta factual neutra permitida). Urgencia vigente: recorrer los eventos de urgencia (usar el criterio del Task 2 `esEventoUrgencia`) y determinar el último "hace presente ... urgencia {tipo}" sin un "retira" posterior; si existe, la línea; si no, omitir. Usar `relativeTimeEs` para "hace N días" + fecha absoluta. Padding `p-6` (lg), superficie `--background` (UI-SPEC: el bloque NO usa petróleo). Cablearlo en `proyecto/[boletin]/page.tsx` como PRIMER elemento tras el header, ANTES de `#idea-matriz`. El bloque es un carril propio; no compone con otros dominios. Crear `estado-actual-block.test.tsx` (Wave 0): (a) proyecto con etapa/estado + eventos → las 3-4 líneas presentes; (b) proyecto sin urgencia derivable → la línea de urgencia se OMITE (assert de ausencia), las demás presentes; (c) sin eventos → "último hito" omitido; (d) resolver rutas de test por process.cwd()+path.join si hace source-scan (NUNCA import.meta.url — OneDrive rompe file://); (e) banned-vocab sobre el copy.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run estado-actual-block</automated>
  </verify>
  <acceptance_criteria>
    - `derivarEstadoActual` exportado y testeado; cada línea es opcional.
    - Test: proyecto sin urgencia vigente derivable → la línea de urgencia NO aparece; las demás sí (omisión honesta, no "—").
    - Test: sin eventos → "Último hito" omitido.
    - `EstadoActualBlock` aparece en `proyecto/[boletin]/page.tsx` antes de `#idea-matriz` (grep).
    - El componente no contiene "use client"; error → throw.
    - `pnpm --dir app test -- --run estado-actual-block` verde.
  </acceptance_criteria>
  <done>Bloque "¿Dónde está hoy?" derivado y honesto (omite lo no derivable), cableado como primer elemento tras el header; test Wave 0 creado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Timeline dos niveles (colapso de urgencias) + 1 ProvenanceBadge/heading (SC7)</name>
  <files>app/components/timeline-view.tsx, app/components/timeline-event.tsx, app/components/timeline-view.test.tsx, app/app/proyecto/[boletin]/page.tsx</files>
  <read_first>
    - app/components/timeline-view.tsx:9-28 (lista plana + rail CSS)
    - app/components/timeline-event.tsx:13-62 (link fuente :41-51 + ProvenanceBadge por evento :53-59)
    - app/app/proyecto/[boletin]/page.tsx:149-167 (TimelineSection: dónde va el heading + el badge de sección)
    - packages/tramitacion/src/parse-senado-tramitacion.ts:156-168 + packages/tramitacion/test/fixtures/senado-tramitacion.xml (strings reales de urgencia)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Pitfall 3: heurística CONSERVADORA — NO colapsar hitos estructurales)
  </read_first>
  <behavior>
    - esEventoUrgencia(e) → e.tipo === "urgencia" || (e.tipo === "tramite" && /urgencia/i.test(e.descripcion)). TODO evento fuera de este patrón = hito estructural, render normal (TimelineEvent tal cual), SIEMPRE visible, sin paginación.
    - paresDeUrgencia(eventos) → agrupa los eventos-urgencia contiguos del mismo tipo en períodos; cada período colapsa en "Urgencia {tipo} renovada {N} veces entre {mesX} y {mesY} — ver todas."
    - Expand server-driven: ?urgencias=<periodo> expande ESE período a sus eventos individuales; los demás siguen colapsados.
    - SC7: UN ProvenanceBadge en el heading de la sección timeline (en page.tsx); cada evento CONSERVA su "Ver fuente oficial ↗"; el ProvenanceBadge por evento (timeline-event.tsx:53-59) se RETIRA.
  </behavior>
  <action>
    Añadir los helpers puros exportados `esEventoUrgencia(e)` y `paresDeUrgencia(eventos)` (heurística LOCKED de Pitfall 3 — SOLO colapsar eventos-urgencia; cualquier informe/oficio/votación/cambio de trámite queda visible). En `TimelineView`, renderizar dos niveles: hitos estructurales como `TimelineEvent` normal; cada período de urgencia colapsado en una línea "Urgencia {tipo} renovada {N} veces entre {mesX} y {mesY} — ver todas." con expand server-driven `?urgencias=<periodo>` (href idiom `buildVerHref`). Retirar el `ProvenanceBadge` por evento (timeline-event.tsx:53-59) y mover UN `ProvenanceBadge` al heading de la sección timeline en `proyecto/[boletin]/page.tsx` (usar `esStale`/STALE 14d y el `fecha_captura` más reciente de los eventos); conservar el link "Ver fuente oficial ↗" por evento (timeline-event.tsx:41-51). Crear `timeline-view.test.tsx` con un fixture MIXTO (urgencia "hace presente Suma" + urgencia "Suma" misma fecha + un informe + una votación): assertar que (a) el informe y la votación SIEMPRE aparecen (NO se colapsan); (b) el par de urgencia se colapsa en la línea "renovada N veces"; (c) con `?urgencias=<periodo>` los eventos de urgencia de ese período aparecen; (d) hay exactamente UN ProvenanceBadge en el heading y CERO por evento, pero N links "Ver fuente oficial ↗"; (e) banned-vocab sobre el copy nuevo.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run timeline-view && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `esEventoUrgencia`/`paresDeUrgencia` exportados y testeados.
    - Test (fixture mixto): un informe y una votación NO se colapsan (assert de presencia); el par de urgencia SÍ se colapsa en "renovada N veces".
    - Test: `?urgencias=<periodo>` expande los eventos de ese período.
    - Test: exactamente 1 ProvenanceBadge en el heading, 0 por evento, N links "Ver fuente oficial ↗".
    - `pnpm --dir app test -- --run timeline-view` verde; `tsc -b` limpio.
  </acceptance_criteria>
  <done>Timeline dos niveles: hitos estructurales siempre visibles, pares de urgencia colapsados con expand server-driven; 1 ProvenanceBadge/sección con trazabilidad por link conservada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?urgencias searchParam → Server Component | Entrada no confiable como flag de expansión |
| tramitacion_evento → derivación de estado | Riesgo de fabricar un estado no derivable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-14 | Repudiation (dato falso) | EstadoActualBlock deriva urgencia/estado | mitigate | Omisión honesta: dato no derivable → línea ausente (nunca "—"/fabricado); error DB → throw (#34) |
| T-51-15 | Repudiation (señal perdida) | colapso de timeline demasiado agresivo | mitigate | Heurística conservadora: SOLO pares de urgencia colapsan; test con fixture mixto prueba que informe/votación siguen visibles |
| T-51-16 | Repudiation (trazabilidad perdida) | consolidar ProvenanceBadge a 1/heading | mitigate | Cada evento conserva "Ver fuente oficial ↗"; el badge se mueve, no se elimina la trazabilidad |
| T-51-17 | Tampering/Injection | ?urgencias | mitigate | Normalizado a un identificador de período conocido; nunca SQL crudo |
| T-51-SC | Tampering | npm installs | accept | Zero new deps |
</threat_model>

<verification>
- `pnpm --dir app test -- --run estado-actual-block` y `--run timeline-view` verdes.
- `pnpm --dir app exec tsc -b` limpio.
- Negative-match banned-vocab sobre el copy nuevo.
</verification>

<success_criteria>
SC2: bloque "¿Dónde está hoy?" derivado+honesto tras el header; timeline dos niveles con hitos visibles y pares de urgencia colapsados. SC7: 1 ProvenanceBadge/sección con trazabilidad por link conservada. SC9 (parcial): suite + tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-05-SUMMARY.md` when done
</output>
