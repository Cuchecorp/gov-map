---
phase: 51-leg2-legibilidad-profunda
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/parlamentario-header.tsx
  - app/lib/parlamentario-resumen-conteos.ts
  - app/components/parlamentario-resumen.tsx
  - app/components/parlamentario-header.test.tsx
  - app/components/parlamentario-resumen.test.tsx
autonomous: true
requirements: [LEG2, SC1, SC9]
user_setup: []

must_haves:
  truths:
    - "El header de la ficha muestra región/distrito/circunscripción + período (sin partido/foto)"
    - "El resumen above-fold muestra un chip 'Presente en N de M' derivado de los votos"
    - "La asistencia se deriva de las filas de votos ya traídas (una sola lectura, React.cache)"
    - "Ningún campo PII (partido/rut/email/foto) llega al DOM"
  artifacts:
    - path: "app/components/parlamentario-header.tsx"
      provides: "cargoPartes enriquecido con 'Período {periodo}' (Mono)"
      contains: "periodo"
    - path: "app/lib/parlamentario-resumen-conteos.ts"
      provides: "ConteoCarriles con asistencia derivada de las filas de votos"
      contains: "asistencia"
    - path: "app/components/parlamentario-resumen.tsx"
      provides: "chip 'Presente en N de M' en el resumen above-fold"
      contains: "Presente en"
  key_links:
    - from: "app/components/parlamentario-resumen.tsx"
      to: "contarCarriles (votos_de_parlamentario)"
      via: "asistencia derivada de las filas ya traídas"
      pattern: "asistencia"
---

<objective>
Enriquecimiento de cabecera/resumen (SC1 §2.1): añadir "Período {periodo}" al header de la ficha del parlamentario (todos campos públicos) y un chip "Presente en N de M" en el resumen above-fold, derivado de las filas de votos que `contarCarriles` ya trae. Nada de PII: sin partido, sin foto (LEGAL-03 intacto).

Purpose: El header hoy no dice el período y la asistencia real está enterrada en la sección de votos. Subirla al resumen la hace un dato de un vistazo, sin fabricar y sin exponer afiliación.
Output: `parlamentario-header.tsx`, `parlamentario-resumen-conteos.ts`, `parlamentario-resumen.tsx` extendidos + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Piezas existentes. -->
ParlamentarioPublicoRow (types.ts:109): { id, nombre, camara, region, distrito, circunscripcion, periodo, origen, fecha_captura, enlace } — periodo ya emitido (0020). NUNCA partido/rut/email.
parlamentario-header.tsx: cargoPartes (:31-37) = [distrito, circunscripcion, region].filter(...) → añadir período. NO foto, NO partido (:15-19 documenta la decisión LEGAL-03).
parlamentario-resumen-conteos.ts: contarCarriles = cache(...) (:100) fetchea votos_de_parlamentario (:118); ConteoCarriles (:46); construir asistencia desde esas filas (seleccion !== 'ausente' = presente).
parlamentario-resumen.tsx: ResumenView({chips}) pura (:61); construirChips (:101); ParlamentarioResumen server (:146) llama contarCarrilesSeguro.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header período (parlamentario-header.tsx)</name>
  <files>app/components/parlamentario-header.tsx, app/components/parlamentario-header.test.tsx</files>
  <read_first>
    - app/components/parlamentario-header.tsx (cargoPartes :31-37; decisión LEGAL-03 :15-19)
    - app/lib/types.ts:109-120 (ParlamentarioPublicoRow: periodo ya emitido)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Pitfall 7: solo region/distrito/circunscripcion/periodo; NUNCA partido)
  </read_first>
  <action>
    Añadir "Período {periodo}" a `cargoPartes` en `parlamentario-header.tsx` cuando `parlamentario.periodo` esté presente (omitir si null — honesto). El período va en Mono (UI-SPEC: dates/IDs en Geist Mono). Usar SOLO campos de `ParlamentarioPublicoRow` (region/distrito/circunscripcion/periodo); NO introducir partido/foto/rut/email (el RPC ni los emite; LEGAL-03). Extender/crear el test del header: (a) con `periodo` presente → aparece "Período {periodo}"; (b) con `periodo` null → no aparece la línea de período (ni "Período " suelto); (c) assert de que el DOM NO contiene ninguna referencia a partido/afiliación.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run parlamentario-header</automated>
  </verify>
  <acceptance_criteria>
    - Test: `periodo="2022-2026"` → el header muestra "Período 2022-2026".
    - Test: `periodo=null` → no aparece la etiqueta de período.
    - Test: assert de ausencia de "partido"/afiliación en el DOM.
    - `pnpm --dir app test -- --run parlamentario-header` verde.
  </acceptance_criteria>
  <done>Header enriquecido con período (Mono), sin PII; test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Asistencia derivada + chip "Presente en N de M" en el resumen</name>
  <files>app/lib/parlamentario-resumen-conteos.ts, app/components/parlamentario-resumen.tsx, app/components/parlamentario-resumen.test.tsx</files>
  <read_first>
    - app/lib/parlamentario-resumen-conteos.ts:40-130 (ConteoCarriles, contarCarriles=cache, fetch votos_de_parlamentario)
    - app/components/parlamentario-resumen.tsx:61-150 (ResumenView pura, construirChips, ParlamentarioResumen server)
    - app/components/votos-por-parlamentario.tsx (regla de asistencia: seleccion==='ausente' → ausente; presentes = total - ausentes)
    - app/components/parlamentario-resumen.test.tsx (convención RTL de la vista pura)
  </read_first>
  <action>
    Extender `ConteoCarriles` (parlamentario-resumen-conteos.ts) con un campo `asistencia: { presentes: number; total: number } | null`, derivado de las filas de `votos_de_parlamentario` que `contarCarriles` YA trae (una sola lectura, React.cache — NO añadir un segundo fetch): `total` = número de filas de voto; `presentes` = filas con `seleccion !== 'ausente'` (misma regla que VotosView). Si no hay filas de voto, `asistencia = null` (honesto, sin fabricar). En `conteosDesconocidos()` dejar `asistencia: null`. En `parlamentario-resumen.tsx`, renderizar un chip "Presente en {presentes} de {total}" (Mono para {presentes}/{total}) en el resumen above-fold cuando `asistencia` no es null; omitirlo cuando es null. El chip es un hecho neutro (conteo), NO petróleo, NO ranking/score. Extender el test de la vista pura: (a) `asistencia={presentes:40,total:52}` → aparece "Presente en 40 de 52" (Mono); (b) `asistencia=null` → el chip se omite; (c) banned-vocab sobre el copy.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run parlamentario-resumen && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `ConteoCarriles` incluye `asistencia: {presentes:number; total:number} | null`; `tsc -b` limpio.
    - La asistencia se deriva de las filas de votos ya traídas (grep confirma que no hay un segundo `.rpc("votos_de_parlamentario"` nuevo en el resumen).
    - Test: `asistencia={presentes:40,total:52}` → "Presente en 40 de 52".
    - Test: `asistencia=null` → chip omitido.
    - `pnpm --dir app test -- --run parlamentario-resumen` verde.
  </acceptance_criteria>
  <done>Chip de asistencia above-fold derivado de la lectura cacheada de votos; omisión honesta sin filas; sin doble fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParlamentarioPublicoRow → header/resumen | Solo campos públicos pueden llegar al DOM |
| votos rows → chip de asistencia | Derivación de conteo, nunca juicio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-21 | Information Disclosure | header enrichment | mitigate | Solo region/distrito/circunscripcion/periodo (RPC parlamentario_publico); NUNCA partido/rut/email/foto; test asserta ausencia de partido |
| T-51-22 | Repudiation (dato falso) | chip de asistencia sin datos | mitigate | asistencia=null cuando no hay filas → chip omitido, nunca "0 de 0" fabricado |
| T-51-23 | Repudiation (insinuación) | chip de asistencia como ranking | mitigate | Conteo neutro "Presente en N de M" (Mono), sin petróleo/score/ranking; banned-vocab negative-match |
| T-51-SC | Tampering | npm installs | accept | Zero new deps |
</threat_model>

<verification>
- `pnpm --dir app test -- --run parlamentario-header` y `--run parlamentario-resumen` verdes.
- `pnpm --dir app exec tsc -b` limpio.
- Negative-match banned-vocab sobre el copy nuevo.
</verification>

<success_criteria>
SC1 §2.1: header con período (sin PII) + chip "Presente en N de M" derivado y honesto en el resumen above-fold. SC9 (parcial): suite + tsc verdes, LEGAL-03 intacto.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-07-SUMMARY.md` when done
</output>
