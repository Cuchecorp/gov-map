---
phase: 51-leg2-legibilidad-profunda
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/layout.tsx
  - app/app/metodologia/page.tsx
  - app/app/layout.test.tsx
autonomous: true
requirements: [LEG2, SC8, SC9]
user_setup: []

must_haves:
  truths:
    - "Toda página del sitio muestra un footer global con atribución, licencia CC BY 4.0, links y contacto"
    - "El footer no contradice las atribuciones por-dataset (ChileCompra/SERVEL) que viven en sus secciones"
    - "Existe una página /metodologia mínima y honesta enlazada desde el footer"
    - "El link /sobre del footer resuelve a una página existente"
    - "El gate noindex PUBLIC_INDEXABLE del layout queda intacto"
  artifacts:
    - path: "app/app/layout.tsx"
      provides: "footer global tras {children} con CC BY scope-caveat + links + contacto"
      contains: "footer"
    - path: "app/app/metodologia/page.tsx"
      provides: "página mínima honesta (fuentes, estados, licencias por dataset, contacto)"
      contains: "Metodología"
  key_links:
    - from: "app/app/layout.tsx (footer)"
      to: "/metodologia"
      via: "link del footer a la página nueva"
      pattern: "/metodologia"
---

<objective>
SC8 (footer global): añadir un `<footer>` a `app/layout.tsx` con atribución de datos, licencia CC BY 4.0 (con scope-caveat para no contradecir atribuciones por-dataset), links a `/metodologia` y `/sobre`, y contacto; crear la página mínima honesta `/metodologia` que hoy no existe (el footer la enlaza → link roto si falta).

Purpose: La atribución CC BY 4.0 visible es un requisito legal del proyecto (CLAUDE.md). El footer global la cumple en toda página sin pisar las atribuciones no-CC-BY (ChileCompra "mención de la fuente", SERVEL "términos por verificar") que siguen por sección.
Output: `layout.tsx` con footer, `metodologia/page.tsx` nueva, test de layout/estructura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Piezas existentes. -->
app/app/layout.tsx: body = <GlobalHeader /> + {children} (:36-43); generateMetadata noindex gate PUBLIC_INDEXABLE (:21-29) — NO tocar.
app/app/sobre/page.tsx: molde de página estática honesta (export const metadata; <main className="max-w-3xl mx-auto px-4 md:px-8 py-8 md:py-16">; <h2> + <Separator>; "← Volver al inicio"; bloque CC BY <a> :92-103). /sobre ya declara que el diccionario de datos completo es milestone futuro.
DESIGN-SYSTEM §6: trust line LOCKED "Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página /metodologia mínima y honesta (twin de /sobre)</name>
  <files>app/app/metodologia/page.tsx</files>
  <read_first>
    - app/app/sobre/page.tsx (molde exacto: metadata, main container, h2+Separator, "← Volver al inicio", bloque CC BY; y su declaración de alcance sobre el diccionario de datos futuro)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Pitfall 6: página mínima honesta; NO prometer diccionario de datos completo)
  </read_first>
  <action>
    Crear `app/app/metodologia/page.tsx` como Server Component estático copiando el molde de `/sobre`: `export const metadata`, `<main className="max-w-3xl mx-auto px-4 md:px-8 py-8 md:py-16">`, secciones `<h2>` + `<Separator>`, "← Volver al inicio". Contenido HONESTO sobre su alcance ACTUAL: (a) qué fuentes se consultan (Cámara, Senado, BCN/LeyChile, LeyLobby, CPLT probidad, ChileCompra, SERVEL — solo las que efectivamente alimentan el sitio hoy); (b) qué significa cada uno de los 3 estados honestos (no consultado / consultado sin resultados / error); (c) licencias por dataset (compilación propia CC BY 4.0; ChileCompra "mención de la fuente"; SERVEL "términos de uso por verificar") — declarando que estas conviven, sin contradecirse; (d) contacto. NO prometer un diccionario de datos por sección completo (eso es milestone futuro; espejar la declaración honesta que /sobre ya hace). Pasar el negative-match de banned-vocab (§6). El link "← Volver al inicio" y el título deben resolver.
  </action>
  <verify>
    <automated>pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/metodologia/page.tsx` existe, exporta `metadata` y un default component.
    - Declara las fuentes, los 3 estados honestos y las licencias por dataset sin contradicción.
    - No promete un diccionario de datos completo (declaración de alcance honesta presente).
    - `tsc -b` limpio; el copy pasa banned-vocab.
  </acceptance_criteria>
  <done>Página /metodologia mínima y honesta creada; el link del footer ya no será roto.</done>
</task>

<task type="auto">
  <name>Task 2: Footer global en layout.tsx + test de estructura</name>
  <files>app/app/layout.tsx, app/app/layout.test.tsx</files>
  <read_first>
    - app/app/layout.tsx (body actual :36-43; gate noindex :21-29 — NO tocar)
    - app/app/sobre/page.tsx:92-103 (bloque/copy CC BY y el <a> a reusar)
    - app/app/parlamentario/[id]/page-estructura.test.ts (convención source-scan: process.cwd()+path.join, NUNCA import.meta.url — OneDrive)
  </read_first>
  <action>
    Añadir un `<footer>` global en `layout.tsx` DESPUÉS de `{children}` (body = `<GlobalHeader />` + `{children}` + `<footer>`). Contenido: (a) atribución "Datos de fuentes públicas del Congreso de Chile, con fuente, fecha y enlace en cada dato."; (b) licencia "Contenido bajo CC BY 4.0 — atribución a Observatorio del Congreso 360." con SCOPE-CAVEAT: esta línea global cubre la compilación propia y NO debe reafirmar/contradecir las atribuciones por-dataset (ChileCompra/SERVEL) que viven en sus secciones — NO restablecer esos datasets no-CC-BY en el footer global; (c) links "Metodología" → `/metodologia`, "Sobre el proyecto" → `/sobre`, "Contacto" → mailto o línea de contacto; (d) opcional: la trust line LOCKED §6. Links con `min-h-11` (touch-target), petróleo underline (reserved-for). NO tocar el gate `generateMetadata`/PUBLIC_INDEXABLE. Superficie `--muted`/`--card` (footer band), padding `md:px-8`. Crear `app/app/layout.test.tsx` (source-scan estructural, rutas por process.cwd()+path.join): assertar que (a) existe un `<footer>` tras `{children}`; (b) el footer contiene el link a `/metodologia` y a `/sobre`; (c) contiene la cadena "CC BY 4.0"; (d) el footer NO menciona "ChileCompra"/"SERVEL" (scope-caveat: esas atribuciones no se restablecen en el global); (e) el gate PUBLIC_INDEXABLE sigue presente en el archivo (no se removió).
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run layout && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` renderiza `<footer>` tras `{children}`; el test lo asserta.
    - El footer contiene links a `/metodologia` y `/sobre` y la cadena "CC BY 4.0".
    - El test asserta que el footer NO contiene "ChileCompra" ni "SERVEL" (scope-caveat).
    - El test asserta que `PUBLIC_INDEXABLE` sigue en el archivo (gate intacto).
    - `pnpm --dir app test -- --run layout` verde; `tsc -b` limpio.
  </acceptance_criteria>
  <done>Footer global en toda página con CC BY scope-caveat + links resolubles + contacto; gate noindex intacto; test de estructura verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| footer global → atribuciones por-dataset | Riesgo de contradecir licencias no-CC-BY |
| layout metadata gate | Riesgo de regresión del noindex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-18 | Repudiation (atribución falsa) | CC BY global vs ChileCompra/SERVEL | mitigate | Scope-caveat: el footer cubre solo la compilación; test asserta que no reafirma ChileCompra/SERVEL |
| T-51-19 | Information Disclosure | regresión del gate noindex | mitigate | No tocar generateMetadata; test asserta que PUBLIC_INDEXABLE sigue presente |
| T-51-20 | Repudiation (promesa falsa) | /metodologia sobre-promete | mitigate | Página mínima honesta; declara alcance actual, no promete diccionario completo |
| T-51-SC | Tampering | npm installs | accept | Zero new deps |
</threat_model>

<verification>
- `pnpm --dir app test -- --run layout` verde.
- `pnpm --dir app exec tsc -b` limpio.
- Negative-match banned-vocab sobre el copy del footer y /metodologia.
</verification>

<success_criteria>
SC8: footer global con CC BY 4.0 (scope-caveat), metodología, fuentes y contacto en toda página; /metodologia existe. SC9 (parcial): suite + tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-06-SUMMARY.md` when done
</output>
