---
phase: 51-leg2-legibilidad-profunda
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/patrimonio-de-parlamentario.tsx
  - app/components/patrimonio-de-parlamentario.test.tsx
autonomous: true
requirements: [LEG2, SC3, SC4, SC9]
user_setup: []

must_haves:
  truths:
    - "Cada versión de patrimonio se muestra como tarjeta-resumen (fecha + tipo + conteos por categoría)"
    - "El <dl> completo nunca se renderiza inline en la tarjeta; el detalle queda bajo ?ver=<versionId>"
    - "Ningún campo cuyo valor sea una URI absoluta (CPLT) se renderiza como valor, ni en tarjeta ni en detalle"
    - "El comparador es un <form method=get> nativo con dos selects de fecha + botón Comparar (cero JS)"
    - "Con <2 versiones el form se omite y queda el hecho neutro existente (cero contradicción)"
  artifacts:
    - path: "app/components/patrimonio-de-parlamentario.tsx"
      provides: "tarjeta-resumen (reusa seriePatrimonio) + filtro esUriCplt + form GET comparador"
      contains: "esUriCplt"
    - path: "app/components/patrimonio-de-parlamentario.test.tsx"
      provides: "RTL: tarjeta con conteos, filtro URI, form comparador, degrade <2 versiones"
      contains: "esUriCplt"
  key_links:
    - from: "app/components/patrimonio-de-parlamentario.tsx (tarjeta)"
      to: "seriePatrimonio"
      via: "conteos por categoría (misma fuente que el chart F46)"
      pattern: "seriePatrimonio"
    - from: "app/components/patrimonio-de-parlamentario.tsx (form)"
      to: "?comparar / ?a ?b"
      via: "form method=get nativo"
      pattern: "method=\"get\""
---

<objective>
SC3 (patrimonio tarjeta-resumen) + SC4 (comparador cableado): cada versión de declaración pasa de un `<dl>` volcado a una tarjeta-resumen con conteos por categoría (reusando `seriePatrimonio`, misma fuente de verdad que el chart F46); ningún valor-URI de CPLT se renderiza; el comparador deja de ser solo deep-link y se cablea con un `<form method="get">` nativo.

Purpose: El patrimonio hoy vuelca `<dl>` largos con valores basura (URIs de dereferencia CPLT). La tarjeta lo hace legible en segundos; el filtro URI cumple B3; el comparador cumple B4 sin JS.
Output: `patrimonio-de-parlamentario.tsx` extendido, tests RTL nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Piezas existentes a reusar (patrimonio-de-parlamentario.tsx). -->
seriePatrimonio(versiones) → SeriePunto[] { anio, tipo_declaracion, version_id, inmueble, mueble, actividad, pasivo, accion_derecho, valor }  (:126-159)
ORDEN_GRUPOS_BIENES (:232-239) — labels de categoría
buildVerHref(id, versionId) (:198-203) — href server-driven ?ver
PatrimonioSection (:770-857) — YA lee ?comparar y llama comparar_declaraciones; single() helper (:779-782)
DeclaracionComparacion (:565-586) — hecho neutro <2 columnas
AtribucionCcBy (:206-222) — NO duplicar
VersionRow (:371) / BienesDeVersion (:322) / paresDeContenido — donde vive el <dl> y los valores-URI
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filtro esUriCplt + tarjeta-resumen por versión (reusa seriePatrimonio)</name>
  <files>app/components/patrimonio-de-parlamentario.tsx, app/components/patrimonio-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/patrimonio-de-parlamentario.tsx:126-159 (seriePatrimonio), :232-239 (ORDEN_GRUPOS_BIENES), :322-420 (BienesDeVersion/VersionRow, paresDeContenido, campos), :198-222 (buildVerHref/AtribucionCcBy)
    - app/lib/format.ts (fechaCortaSegura — NO hand-roll)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Pitfall 4 + Assumption A1: validar prefijo URI contra datos reales)
    - app/components/patrimonio-de-parlamentario.test.tsx (convención RTL)
  </read_first>
  <behavior>
    - esUriCplt(valor: string) → true cuando el valor casa una URI absoluta genérica: /^https?:\/\//.test(valor.trim()). Regla LOCKED (Open Question 3): "las URIs nunca son valores" — filtrar por URI absoluta genérica, NO por host hardcodeado (a prueba de drift de host CPLT).
    - Tarjeta-resumen por versión: título "Declaración de {tipoDeclaracion}" (h3), "Presentada el {fechaCortaSegura}" (Mono, prominente), conteos por categoría desde el SeriePunto de esa versión ("{n} inmuebles · {n} vehículos · …") con labels de ORDEN_GRUPOS_BIENES, Mono para conteos.
    - Los pares de `paresDeContenido`/`campos` cuyo valor casa esUriCplt se EXCLUYEN de la tarjeta Y del detalle. La trazabilidad queda por ProvenanceBadge/fuente.
    - "Ver detalle" (?ver=<versionId>) sigue mostrando el detalle; el <dl> completo nunca aparece inline en la tarjeta.
  </behavior>
  <action>
    Añadir el helper puro exportado `esUriCplt(valor)` usando el regex de URI absoluta genérica `/^https?:\/\//` (por valor trimmeado). Aplicarlo en `paresDeContenido` (BienesDeVersion) y en el render de `campos` (VersionRow) para excluir el par completo cuando el valor es una URI — en tarjeta Y en detalle. Transformar `VersionRow` en una tarjeta-resumen: usar el `SeriePunto` de esa versión (invocar/derivar de `seriePatrimonio` sobre las versiones — misma fuente que el chart F46, LOCKED por CONTEXT) para los conteos por categoría con labels de `ORDEN_GRUPOS_BIENES`; título "Declaración de {tipo}" + "Presentada el {fecha}" (`fechaCortaSegura`, Mono). Conservar `HistoricalCaveat` ámbar en versiones antiguas y `AtribucionCcBy` (reusar, no duplicar). El `<dl>`/detalle completo solo se renderiza bajo `?ver=<versionId>` (patrón `buildVerHref` existente). Padding de tarjeta `p-6` (lg), superficie `--card`/`--muted` (UI-SPEC). Extender el test RTL: (a) versión con conteos → la tarjeta muestra "{n} inmuebles" etc.; (b) un `contenido`/`campo` con valor `http://datos.cplt.cl/...` (y también `https://otro.host/...`) NO aparece en el DOM ni en tarjeta ni en detalle; (c) el `<dl>` completo NO aparece salvo con `?ver`.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run patrimonio-de-parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - `esUriCplt` exportado y testeado: true para `http://datos.cplt.cl/x` y `https://cualquier.host/y`; false para "1.234" o "Casa habitación".
    - Test: una versión con SeriePunto {inmueble:12, valor:5} renderiza "12 inmuebles" y "5 valores" (labels ORDEN_GRUPOS_BIENES), Mono.
    - Test: un valor-URI no aparece en el DOM (assert de ausencia) ni en tarjeta ni en detalle.
    - Test: sin `?ver`, el detalle `<dl>` no está en el DOM; con `?ver=<versionId>` sí.
    - Negative-match banned-vocab: assert RTL de vocabulario prohibido sobre el copy nuevo de la tarjeta (misma lista/idiom que los asserts banned-vocab existentes del componente).
    - `pnpm --dir app test -- --run patrimonio-de-parlamentario` verde.
  </acceptance_criteria>
  <done>Tarjeta-resumen por versión con conteos de seriePatrimonio + fecha prominente; valores-URI excluidos de tarjeta y detalle; detalle bajo ?ver.</done>
</task>

<task type="auto">
  <name>Task 2: Comparador form GET nativo (SC4) con degrade <2 versiones</name>
  <files>app/components/patrimonio-de-parlamentario.tsx, app/components/patrimonio-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/patrimonio-de-parlamentario.tsx:770-857 (PatrimonioSection: lee ?comparar, split por coma, llama comparar_declaraciones; single())
    - app/components/patrimonio-de-parlamentario.tsx:565-586 (DeclaracionComparacion + hecho neutro <2 columnas)
    - .planning/phases/51-leg2-legibilidad-profunda/51-RESEARCH.md (Pattern 3: form GET nativo; leer ?a/?b y reconstruir/compat con ?comparar=A,B)
    - app/AGENTS.md (verificar API de Next 16 dudosa contra node_modules/next/dist/docs/)
  </read_first>
  <action>
    Añadir un `<form method="get">` nativo (cero JS, SSR) con dos `<select>` de las fechas de presentación de las versiones (`version.fecha_presentacion`, labels Mono) y un botón submit "Comparar" (`min-h-11`, petróleo). Label del form: "Elige dos fechas para comparar". Cablear la lectura server-side: aceptar `?a`/`?b` (los dos selects) y reconstruirlos a las dos fechas de comparación en `PatrimonioSection`, manteniendo compatibilidad con el deep-link `?comparar=A,B` existente (leer ambos: si vienen `a`+`b` úsalos; si viene `comparar` úsalo). El `<form>` debe incluir un `<input type="hidden">` sólo si es necesario para preservar el ancla/otros params — preferir la ruta `?a`/`?b` sin JS. Con <2 versiones disponibles: OMITIR el form por completo y conservar el hecho neutro existente `DeclaracionComparacion` "Se necesita más de una versión para comparar." (cero contradicción con el label). La tabla de comparación se mantiene sin cambios (solo-datos, CERO veredicto/delta; campo ausente = "No declarado en esta versión"). Extender el test RTL: (a) ≥2 versiones → el form con dos selects + botón "Comparar" aparece; (b) submit vía `?a=fechaA&b=fechaB` (o `?comparar=A,B`) → renderiza la tabla de comparación; (c) <2 versiones → el form NO aparece y sí el hecho neutro; (d) el form es `method="get"` y no hay handler onClick/onSubmit JS.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run patrimonio-de-parlamentario && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - El form es `<form method="get">` con exactamente dos `<select>` y un submit "Comparar"; sin onSubmit/onClick JS (assert de ausencia).
    - Test: con `?a`/`?b` (o `?comparar=A,B`) la tabla de comparación se renderiza.
    - Test: con <2 versiones el form se omite y aparece "Se necesita más de una versión para comparar."
    - El deep-link `?comparar=A,B` existente sigue funcionando (compat).
    - Negative-match banned-vocab: assert RTL de vocabulario prohibido sobre el copy nuevo del comparador ("Elige dos fechas para comparar", "Comparar").
    - `pnpm --dir app test -- --run patrimonio-de-parlamentario` verde; `tsc -b` limpio.
  </acceptance_criteria>
  <done>Comparador alcanzable por UI vía form GET nativo cero-JS; degrade honesto <2 versiones; compat con deep-link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?ver/?comparar/?a/?b searchParams → Server Component | Entrada no confiable como filtro/flag |
| jsonb contenido (valores CPLT) → DOM | Valores-URI son ruido interno, no datos ciudadanos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-08 | Information Disclosure | valores-URI CPLT en patrimonio | mitigate | esUriCplt excluye todo valor `^https?://` de tarjeta Y detalle (B3) |
| T-51-09 | Tampering/Injection | ?comparar/?a/?b | mitigate | supabase-js parametriza .rpc(); las fechas se pasan como argumentos, no SQL crudo; single()/split saneados |
| T-51-10 | Repudiation (dato falso) | comparación con <2 versiones | mitigate | Form omitido + hecho neutro existente; cero contradicción; campo ausente = "No declarado en esta versión" |
| T-51-SC | Tampering | npm installs | accept | Zero new deps (Recharts ya instalado, sin chart nuevo) |
</threat_model>

<verification>
- `pnpm --dir app test -- --run patrimonio-de-parlamentario` verde.
- `pnpm --dir app exec tsc -b` limpio.
- Negative-match banned-vocab sobre el copy nuevo (tarjeta/comparador).
</verification>

<success_criteria>
SC3: tarjeta-resumen por versión con conteos de seriePatrimonio; valores-URI nunca renderizados; detalle bajo ?ver. SC4: comparador cableado por form GET nativo, copy no contradictorio. SC9 (parcial): suite + tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-03-SUMMARY.md` when done
</output>
