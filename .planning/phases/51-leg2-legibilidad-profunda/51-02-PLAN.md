---
phase: 51-leg2-legibilidad-profunda
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/votos-por-parlamentario.tsx
  - app/components/voto-ficha-row.tsx
  - app/lib/types.ts
  - app/components/votos-por-parlamentario.test.tsx
autonomous: true
requirements: [LEG2, SC1, SC5, SC9]
user_setup: []

must_haves:
  truths:
    - "Cada arco de proyecto muestra UNA línea-resumen (conteos por sentido + rango de fechas) por defecto"
    - "Las líneas individuales de votación quedan bajo 'Ver detalle' server-driven (?votosVer=<boletin>)"
    - "El honest-state 'De qué trata: no disponible aún' ya NO se repite por fila (B24 eliminado)"
    - "Las filas de rebeldías muestran el título del proyecto cuando existe, con dedupe por votación"
    - "El copy nuevo pasa el negative-match de banned-vocab (conteos neutros, cero juicio)"
  artifacts:
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "línea-resumen por arco + toggle ?votosVer + consumidor rebeldías con título"
      contains: "votosVer"
    - path: "app/lib/types.ts"
      provides: "RebeldiaRow extendido con titulo/etapa"
      contains: "titulo"
    - path: "app/components/votos-por-parlamentario.test.tsx"
      provides: "RTL: resumenDeArco + detalle + banned-vocab + rebeldías con título"
      contains: "resumenDeArco"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "?votosVer searchParam"
      via: "buildVotosVerHref (patrón buildVerHref de patrimonio)"
      pattern: "votosVer"
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "RebeldiaRow.titulo"
      via: "consumidor de rebeldías (VotosView)"
      pattern: "r\\.titulo"
---

<objective>
SC1 (votos agregados por proyecto) + SC5 (consumidor de rebeldías) + B24 (dead code): colapsar cada arco de proyecto en UNA línea-resumen con el detalle a un clic server-driven; hidratar el título del proyecto en las filas de rebeldías; borrar el honest-state duplicado por fila.

Purpose: Las ~90 líneas idénticas por proyecto son la mayor fuente de sobrecarga de la ficha. Agregarlas sin perder ningún dato (el detalle queda bajo `?votosVer`) es el corazón de la fase.
Output: `votos-por-parlamentario.tsx` extendido, `RebeldiaRow` extendido, tests RTL nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-CONTEXT.md
@.planning/phases/51-leg2-legibilidad-profunda/51-UI-SPEC.md
@.planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md

<interfaces>
<!-- Contrato del RPC rebeldías (compartido con 51-01; pinear estas columnas exactas). -->
RebeldiaRow (types.ts:218) DEBE crecer a:
  votacion_id: string; boletin: string; titulo: string | null; etapa: string | null;
  fecha: string; seleccion_propia: Seleccion; mayoria_bancada: Seleccion;
-- Fuente: RPC 0047 (51-01). titulo/etapa pueden ser null (LEFT JOIN) → degradar honesto.
-- Helpers a REUSAR (app/lib/format.ts): fechaCorta, conteoVotacion, extractoIdea.
-- Patrón href server-driven: patrimonio-de-parlamentario.tsx:198 buildVerHref.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper resumenDeArco + línea-resumen por arco + toggle ?votosVer</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx (VotosView:285, agruparPorProyecto:180, ProyectoGrupo:206-278, VotosSection:642-668, normalizarPagina de votosPage)
    - app/components/patrimonio-de-parlamentario.tsx:198-203 (buildVerHref: patrón href server-driven a replicar) y :779-782 (helper single())
    - app/lib/format.ts (fechaCorta, conteoVotacion, extractoIdea — NO hand-roll)
    - app/components/votos-por-parlamentario.test.tsx (convención RTL + fixtures + asserts banned-vocab)
    - app/AGENTS.md (verificar cualquier API dudosa de Next 16 contra node_modules/next/dist/docs/ antes de escribir)
  </read_first>
  <behavior>
    - resumenDeArco(arco) → { n, si, no, ausente, abstencion, pareo, mesInicio, mesFin }: n = arco.etapas.length; conteos por e.seleccion; mesInicio/mesFin = min/max de e.fecha formateado "mmm AAAA".
    - Render por defecto: una sola línea-resumen por arco: "Votó en {n} ocasiones sobre este proyecto: {si} a favor · {no} en contra · {c} ausente, entre {mesInicio} y {mesFin}." — OMITIR cualquier sentido cuyo conteo sea 0 (nunca "· 0 ausente"). {n}/tallies/rango en font-mono.
    - Con ?votosVer=<boletin> presente: para ESE arco se expanden las etapas individuales (el ProyectoGrupo actual), y el afford cambia a "Ocultar detalle" (quita votosVer). Los demás arcos siguen colapsados.
    - buildVotosVerHref(id, boletin) construye `/parlamentario/${id}?votosVer=${boletin}#votos` (o quita votosVer si ya está abierto), preservando otros params relevantes.
    - Un arco con un solo sentido no fabrica sentidos ausentes; honest-state de idea null se dice 1× por sección (ya existe en ProyectoGrupo:225-232) — no tocarlo.
  </behavior>
  <action>
    Añadir el helper puro `resumenDeArco(arco)` (exportado para test) que agrega los conteos por `seleccion` y el rango de fechas desde `arco.etapas` (ya en memoria — `agruparPorProyecto` no pagina en origen, `VotosSection` pide `p_limit: 1000`). En `ProyectoGrupo` renderizar por defecto SOLO la línea-resumen (copy exacto del UI-SPEC §SC1) usando `conteoVotacion`/`fechaCorta`; mostrar las etapas individuales únicamente cuando `votosVer === arco.boletin`. Añadir `buildVotosVerHref` replicando `buildVerHref` de patrimonio (searchParam `votosVer`, ancla `#votos`). Leer `votosVer` en `VotosSection`/`VotosView` desde el `searchParams` ya resuelto (objeto plano — Next 16: NO re-await; usar el helper `single()` para `string[]`). Mono para n/tallies/rango; petróleo underline SOLO en "Ver detalle"/"Ocultar detalle" (reserved-for del UI-SPEC). NO colapsar el `mt-12` ni componer votos con otro dominio. Extender el test RTL: (a) fixture de arco multi-etapa → asserta la línea-resumen con conteos y rango, y que OMITE sentidos en 0; (b) con votosVer del arco → aparecen las etapas; (c) negative-match banned-vocab sobre el copy nuevo (sin "porque/afinidad/ranking/mejor/peor…").
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run votos-por-parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - `resumenDeArco` está exportado y testeado: para un arco con {2 a favor, 1 en contra} devuelve n=3, si=2, no=1 y el rango correcto.
    - La vista por defecto renderiza una única línea-resumen por arco (no las N etapas); el test asserta ausencia de las filas de etapa cuando votosVer no está.
    - Con `votosVer=<boletin>` el test asserta presencia de las etapas de ESE arco.
    - Un sentido con conteo 0 NO aparece en la línea-resumen (assert de texto negativo, p.ej. no "0 ausente").
    - El test de banned-vocab pasa sobre el copy nuevo.
    - `pnpm --dir app test -- --run votos-por-parlamentario` verde.
  </acceptance_criteria>
  <done>Línea-resumen por arco por defecto + detalle bajo ?votosVer; helper puro exportado y testeado; sin dato perdido; copy neutro.</done>
</task>

<task type="auto">
  <name>Task 2: RebeldiaRow con título + consumidor de rebeldías (SC5) + borrar dead code B24</name>
  <files>app/lib/types.ts, app/components/votos-por-parlamentario.tsx, app/components/voto-ficha-row.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/lib/types.ts:218-224 (RebeldiaRow vigente)
    - app/components/votos-por-parlamentario.tsx:496-537 (consumidor de rebeldías: hoy "Boletín N°{boletin}")
    - app/components/voto-ficha-row.tsx:76-106 (SustanciaYDesenlace + dead path :86-90 "De qué trata: no disponible aún")
    - .planning/phases/51-leg2-legibilidad-profunda/51-PATTERNS.md (sección voto-ficha-row: grep VotoFichaMencionRow/VotoFichaRow por consumidores antes de borrar — Assumption A6)
  </read_first>
  <action>
    (a) Extender `RebeldiaRow` (types.ts:218) a EXACTAMENTE: `votacion_id, boletin, titulo (string|null), etapa (string|null), fecha, seleccion_propia, mayoria_bancada` — mismo orden que el returns table de 0047 (51-01). (b) En el consumidor de rebeldías (`VotosView:496-537`), cambiar la fila de "Boletín N°{r.boletin}" a: si `r.titulo` presente → `<Link>{r.titulo}</Link>` con línea "{título} — {etapa} · {fecha}" (etapa omitida si null); si `r.titulo` null → fallback al boletín actual (degradación honesta pre-apply del RPC: mientras 0047 no esté aplicado, titulo llega undefined/null y cae al boletín, sin romper). Mantener el copy neutro existente (:531-534). El conteo "Votó distinto ... {N} veces" ya no incluye ausencias porque el RPC 0047 las excluye — no cambiar la lógica de conteo del componente (viene del RPC). (c) Borrar el dead path B24 en `voto-ficha-row.tsx:86-90`: eliminar la rama "De qué trata: no disponible aún" (el honest-state ya se dice 1× por sección en ProyectoGrupo). ANTES de borrar el archivo o el tipo, grep `VotoFichaMencionRow` y `VotoFichaRow` por consumidores en tests/fixtures; si `VotoFichaMencionRow` tiene consumidores, refactorizar el camino de menciones en lugar de borrar el archivo entero — borrar SOLO el path muerto. Actualizar el test RTL: fila de rebeldías con titulo → renderiza el título enlazado; con titulo null → renderiza el boletín; assert de que "no disponible aún" ya no aparece por fila.
  </action>
  <verify>
    <automated>pnpm --dir app test -- --run votos-por-parlamentario && pnpm --dir app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `RebeldiaRow` incluye `titulo: string | null` y `etapa: string | null`; `tsc -b` limpio.
    - Test: fila de rebeldías con `titulo` no-null renderiza el título como Link a `/proyecto/{boletin}`.
    - Test: fila de rebeldías con `titulo` null renderiza el fallback "Boletín N°{boletin}" (no crashea, no fabrica título).
    - grep en el árbol confirma que `voto-ficha-row.tsx` ya no contiene la cadena "no disponible aún".
    - Si `VotoFichaMencionRow` tenía consumidores, siguen compilando (no se borró de más).
    - `pnpm --dir app test -- --run votos-por-parlamentario` verde.
  </acceptance_criteria>
  <done>RebeldiaRow extendido al contrato de 0047; rebeldías muestran título con dedupe (del RPC) y fallback honesto; dead code B24 eliminado sin romper consumidores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams (?votosVer) → Server Component | Entrada no confiable usada como flag/filtro |
| RebeldiaRow → DOM | El RPC ya emite derivado público; el componente no debe añadir PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | Tampering/Injection | ?votosVer searchParam | mitigate | Se usa como flag de igualdad contra boletines conocidos; nunca interpolado en SQL crudo; normalizar vía single() (patrón del repo) |
| T-51-06 | Information Disclosure | consumidor de rebeldías | mitigate | Solo campos de RebeldiaRow (derivado público); NUNCA partido/rut; el título viene del RPC, no de una query nueva |
| T-51-07 | Repudiation (dato falso) | título null pre-apply | mitigate | Degradación honesta: titulo null → fallback boletín, nunca fabrica |
| T-51-SC | Tampering | npm installs | accept | Zero new deps |
</threat_model>

<verification>
- `pnpm --dir app test -- --run votos-por-parlamentario` verde.
- `pnpm --dir app exec tsc -b` limpio.
- grep confirma ausencia de "no disponible aún" en voto-ficha-row.tsx.
- Negative-match banned-vocab sobre el copy nuevo.
</verification>

<success_criteria>
SC1: arcos colapsados en línea-resumen con detalle bajo ?votosVer, cero dato perdido. SC5 (consumidor): título hidratado con fallback honesto. B24 cerrado. SC9 (parcial): suite + tsc verdes, anti-insinuación intacta.
</success_criteria>

<output>
Create `.planning/phases/51-leg2-legibilidad-profunda/51-02-SUMMARY.md` when done
</output>
