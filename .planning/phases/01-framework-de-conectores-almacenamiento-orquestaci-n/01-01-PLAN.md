---
phase: 01-framework-de-conectores-almacenamiento-orquestaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - deno.json
  - vitest.config.ts
  - app/
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/provenance.ts
  - packages/core/src/domain.ts
  - packages/core/src/index.ts
  - supabase/config.toml
  - supabase/migrations/0001_extensions.sql
  - supabase/migrations/0002_control_tables.sql
  - supabase/tests/0001_control_plane.test.sql
autonomous: true
requirements: [FND-02, FND-08]
user_setup:
  - service: cloudflare-r2
    why: "Almacenamiento de crudo inmutable (FND-02); aws4fetch escribe objetos pero no crea el bucket"
    env_vars:
      - name: R2_ENDPOINT_URL
        source: "Cloudflare Dashboard -> R2 -> Settings -> S3 API endpoint"
      - name: R2_ACCESS_KEY_ID
        source: "Cloudflare Dashboard -> R2 -> Manage API Tokens"
      - name: R2_SECRET_ACCESS_KEY
        source: "Cloudflare Dashboard -> R2 -> Manage API Tokens"
      - name: R2_BUCKET
        source: "Nombre del bucket creado en Cloudflare R2"
    dashboard_config:
      - task: "Crear bucket R2 para el crudo inmutable"
        location: "Cloudflare Dashboard -> R2 -> Create bucket"

must_haves:
  truths:
    - "Un desarrollador puede clonar el repo, correr pnpm install y supabase db reset sin errores"
    - "La suite de tests corre (aunque la mayoria sean rojos de Wave 0) via pnpm -w test y supabase db lint"
    - "Las extensiones vector, pg_cron, pg_net, pgmq quedan habilitadas por migracion (no por click)"
    - "Las tablas ingest_run, source_snapshot, drift_alert existen con provenance capturable en source_snapshot"
    - "El tipo Provenance es importable desde @obs/core por /packages/ingest y futuras fases"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Definicion de workspaces (app, packages/*)"
      contains: "packages/*"
    - path: "packages/core/src/provenance.ts"
      provides: "Tipo Provenance de primera clase (FND-08)"
      min_lines: 10
    - path: "packages/core/src/domain.ts"
      provides: "Tipos de control IngestRun, SourceSnapshot, DriftAlert"
      min_lines: 20
    - path: "supabase/migrations/0001_extensions.sql"
      provides: "Habilita vector, pg_cron, pg_net, pgmq"
      contains: "create extension"
    - path: "supabase/migrations/0002_control_tables.sql"
      provides: "DDL de ingest_run, source_snapshot, drift_alert con RLS deny-by-default"
      contains: "source_snapshot"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/provenance.ts"
      via: "re-export"
      pattern: "export.*provenance"
    - from: "supabase/migrations/0002_control_tables.sql"
      to: "Provenance (source_url, fetched_at)"
      via: "columnas de provenance en source_snapshot"
      pattern: "source_url|fetched_at"
---

<objective>
Establece el esqueleto del monorepo TypeScript/Deno y el plano de control en Postgres: workspaces pnpm, tooling de test (vitest + deno test), el paquete compartido `@obs/core` (tipo `Provenance` + tipos de control), y las migraciones SQL que habilitan extensiones y crean las tablas `ingest_run` / `source_snapshot` / `drift_alert`. Esta es la base que las Plans 02 (framework de ingesta) y 03 (orquestacion) consumen sin renegociar arquitectura.

Purpose: Sin scaffold no hay donde escribir codigo; sin el plano de control (R2 ref + provenance en Postgres) no hay donde registrar lo ingerido. Materializa los contratos (tipos de `@obs/core`, DDL de control) que las siguientes plans implementan contra ellos — evita el anti-patron scavenger-hunt.
Output: Monorepo instalable y reseteable, `@obs/core` publicado en el workspace, migraciones 0001/0002 que aplican limpio, y la infraestructura de test (Wave 0) lista con tests rojos por requisito.
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-CONTEXT.md
@.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-RESEARCH.md
@.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-VALIDATION.md

<environment_facts>
Verificado en este entorno (2026-06-17): deno 2.3.1, node 22.21.1, pnpm 11.3.0, supabase CLI 2.98.2, docker DISPONIBLE (`docker info` ok → `supabase start` viable para tests de integracion locales).

Nombres EXACTOS de variables en `.env` raiz (leidos, ya no asumidos): `R2_ENDPOINT_URL`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `TOKEN_VALUE_R2`, `SUPABASE_PROJECT_ID`, `SUPABASE_SECRET_KEY`, `SUPABASE_API_URL`, `GEMINI_API_KEY`, `MINIMAX_API_KEY`, `DEEPSEEK_API_KEY`. Resuelve A1/Open-Question-1 del RESEARCH. NO inventar otros nombres. El endpoint R2 incluye el account id en la URL (`R2_ENDPOINT_URL`), por lo que aws4fetch no necesita una variable de account id separada.
</environment_facts>

<interfaces>
<!-- Contratos que esta plan CREA y que Plans 02/03 consumen directamente. Sin exploracion de codebase. -->

Tipo Provenance (packages/core/src/provenance.ts) — campos minimos requeridos por FND-08:
- source: string            (id de la fuente, ej "dummy")
- fetchedAt: string         (ISO timestamp de captura)
- sourceUrl: string         (enlace original)
- snapshotRef?: string      (r2_path una vez escrito; opcional al construir)

Tipos de control (packages/core/src/domain.ts) — reflejan el DDL de 0002:
- IngestRun: { id, source, startedAt, finishedAt?, status: 'running'|'ok'|'error', stats, error? }
- SourceSnapshot: { id, ingestRunId, source, resource, cacheKey, r2Path, contentHash, fingerprint, sourceUrl, fetchedAt, dateBucket }
- DriftAlert: { id, source, resource, prevFingerprint?, newFingerprint, detectedAt, snapshotId?, acknowledged }

DDL de source_snapshot (0002) — la unique key materializa la cache diaria (FND-03) que Plan 02 consulta:
- unique (source, resource, date_bucket)
- columnas de provenance inline: source_url text not null, fetched_at timestamptz not null default now(), date_bucket date not null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold del monorepo pnpm + tooling de test (Wave 0)</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, .gitignore, .env.example, deno.json, vitest.config.ts, app/, packages/core/package.json, packages/core/tsconfig.json</files>
  <read_first>
    - .planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-RESEARCH.md (seccion "Recommended Project Structure" y "Installation"; usar pnpm workspaces, NO turbo)
    - .planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-VALIDATION.md (Test Infrastructure: vitest para /packages/*, deno test para Edge Functions; Wave 0 Requirements)
    - .env (raiz) — leer SOLO los nombres de claves para generar .env.example sin valores
  </read_first>
  <action>
    Crear el monorepo pnpm raiz. `pnpm-workspace.yaml` declara `packages: ["app", "packages/*"]`. `package.json` raiz privado con scripts `test` (vitest run en todos los packages via `pnpm -r --filter ./packages/* test`), `typecheck` (tsc -b) y `lint`; agregar `typescript@5` como devDependency raiz. `tsconfig.base.json` con `strict: true`, `moduleResolution: "bundler"`, `target: ES2022`, paths para `@obs/core`. `.gitignore` debe excluir `.env`, `node_modules`, `.pnpm`, `app/.next`, `supabase/.branches`, `supabase/.temp`. Generar `.env.example` con las MISMAS claves de `.env` (R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, TOKEN_VALUE_R2, SUPABASE_PROJECT_ID, SUPABASE_SECRET_KEY, SUPABASE_API_URL, GEMINI_API_KEY, MINIMAX_API_KEY, DEEPSEEK_API_KEY) pero con valores VACIOS (control V8 ASVS: nunca valores reales en git).

    Scaffold de Next.js 16 en `/app` via `pnpm create next-app@latest app` (App Router, TypeScript, sin features M1 — solo el scaffold por decision LOCKED de layout). Crear `packages/core/package.json` (name `@obs/core`, type module, main apunta a `src/index.ts`, devDeps vitest) y `packages/core/tsconfig.json` que extiende `tsconfig.base.json`.

    Crear `vitest.config.ts` raiz (entorno node, globs `packages/**/*.test.ts`). Crear `deno.json` raiz con `tasks.test` = `deno test` y un import map base; este `deno.json` es la config de test para el codigo Deno de /packages/ingest (Plan 02) y se ampliara en supabase/functions/deno.json (Plan 03). NO instalar turbo (diferido per RESEARCH §1). NO crear adaptadores LLM ni parsers de fuentes (deferred a Fases 2/5+).
  </action>
  <verify>
    <automated>pnpm install && pnpm -w typecheck && pnpm -w test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` resuelve el workspace sin errores y crea node_modules
    - `pnpm -w typecheck` pasa (tsc -b sobre packages/core)
    - `pnpm -w test --run` ejecuta vitest (0 tests o tests rojos de Wave 0 es aceptable; el runner DEBE arrancar)
    - `.env.example` existe con las 11 claves y CERO valores; `.gitignore` excluye `.env`
    - `git status` no muestra `.env` como trackeado
  </acceptance_criteria>
  <done>Monorepo instalable, typecheck verde, runner de test arranca, secrets fuera de git.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tipos compartidos @obs/core — Provenance + control plane</name>
  <files>packages/core/src/provenance.ts, packages/core/src/domain.ts, packages/core/src/index.ts, packages/core/src/provenance.test.ts</files>
  <read_first>
    - El bloque <interfaces> de esta plan (contratos exactos de Provenance y tipos de control)
    - .planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-RESEARCH.md (DDL de source_snapshot en "Code Examples"; tipo Provenance en "Recommended Project Structure")
  </read_first>
  <behavior>
    - Test 1: un objeto Provenance valido { source, fetchedAt, sourceUrl } construido por un helper `makeProvenance(source, sourceUrl)` produce fetchedAt en formato ISO 8601 parseable por Date
    - Test 2: makeProvenance NO incluye snapshotRef hasta que se asigna explicitamente (provenance capturada AL ingestar, snapshotRef se completa post-PUT)
    - Test 3: los tipos de control (IngestRun.status) solo admiten 'running'|'ok'|'error' (verificable via assertion de tipo en tiempo de compilacion + un valor invalido rechazado por un type-guard `isIngestStatus`)
  </behavior>
  <action>
    Definir en `provenance.ts` el tipo `Provenance` exacto del bloque <interfaces> y un helper puro `makeProvenance(source: string, sourceUrl: string): Provenance` que setea `fetchedAt = new Date().toISOString()` y deja `snapshotRef` indefinido (FND-08: procedencia capturada en el momento del fetch, no agregada despues). Definir en `domain.ts` los tipos `IngestRun`, `SourceSnapshot`, `DriftAlert` con los campos del bloque <interfaces>, un union `IngestStatus = 'running'|'ok'|'error'` y un type-guard `isIngestStatus`. `index.ts` re-exporta todo (`export * from "./provenance"`, `export * from "./domain"`). Escribir los tests del bloque <behavior> en `provenance.test.ts` ANTES de la implementacion (RED), luego implementar hasta verde (GREEN). Mantener `@obs/core` libre de dependencias de runtime (solo tipos + helpers puros); NO importar zod aqui (la validacion de forma vive en /packages/ingest, Plan 02).
  </action>
  <verify>
    <automated>pnpm --filter @obs/core test --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @obs/core test --run` pasa los 3 tests del bloque <behavior>
    - `Provenance`, `makeProvenance`, `IngestRun`, `SourceSnapshot`, `DriftAlert`, `IngestStatus`, `isIngestStatus` son importables desde `@obs/core`
    - `grep -n "export" packages/core/src/index.ts` muestra re-export de provenance y domain
  </acceptance_criteria>
  <done>@obs/core exporta el contrato de provenance y control plane, con tests verdes; consumible por Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Migraciones del plano de control — extensiones + tablas + RLS</name>
  <files>supabase/config.toml, supabase/migrations/0001_extensions.sql, supabase/migrations/0002_control_tables.sql, supabase/tests/0001_control_plane.test.sql</files>
  <read_first>
    - .planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-RESEARCH.md ("Code Examples": migracion de extensiones y DDL del plano de control; "Security Domain": RLS deny-by-default, tablas de control solo service role)
    - El bloque <interfaces> de esta plan (DDL de source_snapshot con unique (source,resource,date_bucket) y columnas de provenance)
  </read_first>
  <action>
    Correr `supabase init` (genera `config.toml`) si no existe. Crear `0001_extensions.sql` con `create extension if not exists` para `vector`, `pg_cron`, `pg_net`, `pgmq` (habilitar vector ya aunque se use en Fase 7 — barato; per RESEARCH). Crear `0002_control_tables.sql` con el DDL EXACTO del bloque <interfaces>/RESEARCH para `ingest_run`, `source_snapshot` (incluyendo `unique (source, resource, date_bucket)` que materializa la cache diaria FND-03, columnas de provenance `source_url`/`fetched_at`/`date_bucket` FND-08, y `r2_path`/`content_hash`/`fingerprint`) y `drift_alert`; agregar indices sobre `cache_key` y `content_hash`. Aplicar RLS deny-by-default: `alter table ... enable row level security` en las tres tablas SIN policies para `anon` (control V4/V8 ASVS: tablas de control solo accesibles via service role; el frontend nunca las lee). NO crear queues/cron aqui (eso es Plan 03, migracion 0003). Crear `supabase/tests/0001_control_plane.test.sql` con asserts pgTAP-style o `do $$ ... raise exception` que verifiquen: las 4 extensiones presentes, las 3 tablas existen, la unique constraint de source_snapshot existe, y RLS esta enabled en las 3 tablas.
  </action>
  <verify>
    <automated>supabase db reset && supabase db lint && supabase test db 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` aplica 0001 y 0002 sin error (docker disponible en este entorno → Postgres local arranca)
    - `supabase db lint` no reporta errores
    - El test SQL confirma: extensiones vector/pg_cron/pg_net/pgmq presentes, tablas ingest_run/source_snapshot/drift_alert existen, unique(source,resource,date_bucket) presente, RLS enabled en las 3
    - Verificacion de crudo-fuera-de-Postgres: `grep -iv '^--' supabase/migrations/0002_control_tables.sql | grep -ic 'bytea\|raw_body\|jsonb.*crudo'` retorna 0 (ninguna columna almacena el crudo; solo r2_path/hash) — FND-02
  </acceptance_criteria>
  <done>Migraciones 0001/0002 aplican limpio, RLS deny-by-default activo, plano de control reproducible desde git, crudo NUNCA en Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo git ↔ secrets | `.env` con claves R2/LLM/Supabase no debe entrar a git; `.env.example` sin valores |
| anon role ↔ tablas de control | el frontend (anon) nunca debe leer ingest_run/source_snapshot/drift_alert |
| Postgres ↔ crudo | el crudo (XML/JSON/HTML) nunca entra a Postgres; solo referencias r2_path/hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` en repo | mitigate | `.gitignore` excluye `.env`; `.env.example` con valores vacios; verify confirma `.env` no trackeado |
| T-01-02 | Elevation of Privilege | service_role key / tablas de control legibles por anon | mitigate | RLS deny-by-default (enable RLS sin policies anon) en las 3 tablas; solo service role accede |
| T-01-03 | Tampering | crudo inyectado en Postgres revienta 8 GB y mezcla inmutable con derivado | mitigate | DDL solo guarda r2_path/content_hash; verify greppea ausencia de columnas de crudo (FND-02) |
| T-01-SC | Tampering | npm/pgmq/pg_cron installs (supply chain) | mitigate | aws4fetch/robots-parser ya auditados [OK] en npm (RESEARCH Package Legitimacy); esta plan solo instala typescript/vitest/next (oficiales). Sin paquetes [ASSUMED]/[SUS] → sin checkpoint humano requerido aqui |
</threat_model>

<verification>
- `pnpm install && pnpm -w typecheck && pnpm -w test --run` corre sin fallo de infraestructura
- `supabase db reset && supabase db lint && supabase test db` verde
- `@obs/core` importable; `git status` confirma `.env` no trackeado
</verification>

<success_criteria>
- Monorepo pnpm instalable y reseteable por cualquier desarrollador
- `@obs/core` exporta Provenance + tipos de control con tests verdes
- Migraciones 0001/0002 aplican limpio con RLS deny-by-default; extensiones habilitadas por migracion (no por click)
- Secrets fuera de git; crudo fuera de Postgres (verificado)
</success_criteria>

<output>
Create `.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-01-SUMMARY.md` when done
</output>
