---
phase: 97-auth-p0-spike-auth-on-workers-de-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/pnpm-lock.yaml
  - app/middleware.ts
  - app/lib/supabase-user.ts
  - app/app/spike-auth/page.tsx
  - app/app/spike-auth/actions.ts
  - .env.example
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "El repo tiene su PRIMER middleware.ts (Edge-style deprecado), no proxy.ts, sin runtime config"
    - "Existe un cliente Supabase user de bajo privilegio (publishable key) separado del cliente service_role"
    - "Una ruta de prueba no enlazada /spike-auth ejecuta signInWithOtp + verifyOtp por server action"
    - "pnpm --filter app build (next) pasa con el middleware presente; suite app verde; tsc --noEmit 0"
    - "SUPABASE_PUBLISHABLE_KEY documentada en .env.example como placeholder vacío; env-example-guard verde"
    - "lockdown-guard Block B verde con supabase-user.ts presente (cero .rpc no-allowlisted, cero .from PII)"
  artifacts:
    - path: "app/middleware.ts"
      provides: "Edge-style middleware llamando updateSession con matcher que salta estáticos"
      contains: "export const config"
    - path: "app/lib/supabase-user.ts"
      provides: "createServerClient (@supabase/ssr) con publishable key + updateSession() cookie refresh"
      contains: "updateSession"
    - path: "app/app/spike-auth/page.tsx"
      provides: "Página server no enlazada que muestra estado de sesión vía getClaims"
    - path: "app/app/spike-auth/actions.ts"
      provides: "Server actions OTP send/verify"
      contains: "verifyOtp"
    - path: ".env.example"
      provides: "SUPABASE_PUBLISHABLE_KEY= placeholder vacío documentado"
      contains: "SUPABASE_PUBLISHABLE_KEY="
  key_links:
    - from: "app/middleware.ts"
      to: "app/lib/supabase-user.ts"
      via: "import { updateSession } from '@/lib/supabase-user'"
      pattern: "updateSession"
    - from: "app/lib/supabase-user.ts"
      to: "process.env.SUPABASE_PUBLISHABLE_KEY"
      via: "createServerClient con publishable key"
      pattern: "SUPABASE_PUBLISHABLE_KEY"
---

<objective>
Construir, EN LOCAL, la superficie mínima de código del spike auth-on-Workers: la primera `middleware.ts` del repo (convención deprecada Edge-style que OpenNext corre como Edge — JAMÁS `proxy.ts`), un cliente Supabase user de bajo privilegio (`@supabase/ssr` + publishable key) SEPARADO del cliente service_role, y una ruta de prueba NO enlazada `/spike-auth` que ejerce el flujo OTP (send/verify). Dejar la suite y el build de Next verdes.

Purpose: Aislar el riesgo estructural (¿añadir el primer middleware al bundle OpenNext rompe algo?) en código verificable antes de pagar el costo del deploy real (Plan 02). Todo el valor está en NO hand-rollear cookies/refresh — solo hay que acertar la FORMA del deploy (filename deprecado, sin `proxy.ts`, sin `runtime`).

Output: `middleware.ts`, `lib/supabase-user.ts`, `app/spike-auth/{page.tsx,actions.ts}`, `.env.example` con la nueva key placeholder; una sola dependencia npm nueva (`@supabase/ssr@0.12.3`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-RESEARCH.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-PATTERNS.md

<interfaces>
<!-- Contratos que el ejecutor necesita — extraídos por research/pattern-map. No explorar el codebase para reconstruirlos. -->

Cliente service_role EXISTENTE (analog, NO tocar) — app/lib/supabase.ts:
- `import "server-only"` en línea 1 (disciplina obligatoria en todo módulo server nuevo)
- `createServerSupabase()` lee `process.env.SUPABASE_URL` + `SUPABASE_SECRET_KEY`, hace `throw new Error(...)` fail-loud si faltan.
- El NUEVO supabase-user.ts NO copia `persistSession:false/autoRefreshToken:false`; usa el adaptador cookie getAll/setAll de @supabase/ssr.

Página gated no enlazada EXISTENTE (analog exacto) — app/app/admin/revisar-entidades/page.tsx:
- Skeleton: `export default async function Page()` → build client → render `<main className="max-w-3xl mx-auto px-4 md:px-8 py-8 md:py-16">`.
- Server action idiom `resolverEntidadAdmin`: valida input ANTES de la llamada; `const { data, error } = await sb...`; `if (error) throw new Error(...)`. NO interpolar PII (email/token) en mensajes de error.

Alias import: `@/*` = `app/*` (app/tsconfig.json). Importar `@/lib/supabase-user`.

Guard env-example (app/lib/env-example-guard.test.ts): `detectarValorNoPlaceholder` trata `value===""` como permitido; un valor ≥20 chars TRIPEA el guard. La nueva key va con valor VACÍO.

updateSession() y OTP: usar VERBATIM 97-RESEARCH.md §Pattern 2 (createServerClient + getAll/setAll + getClaims; NADA de código entre createServerClient y getClaims; devolver supabaseResponse sin mutar) y §Pattern 3 (signInWithOtp { shouldCreateUser:true } SIN emailRedirectTo; verifyOtp { type:"email" }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @supabase/ssr + crear cliente user de bajo privilegio</name>
  <files>app/package.json, app/pnpm-lock.yaml, app/lib/supabase-user.ts, .env.example</files>
  <read_first>
    - app/lib/supabase.ts (analog del client factory server-only + env-throw)
    - app/lib/supabase-admin.ts (segundo analog del env-throw)
    - .env.example (bloque `# --- Supabase ---`, líneas ~16-33, para espejar el idiom del comentario)
    - app/lib/env-example-guard.test.ts (contrato del guard: valor vacío permitido, ≥20 chars falla)
    - 97-RESEARCH.md §Pattern 2 (updateSession VERBATIM) + §Package Legitimacy Audit (@supabase/ssr [OK])
    - 97-PATTERNS.md sección `app/lib/supabase-user.ts` + `.env.example`
  </read_first>
  <action>
    Instalar la ÚNICA dependencia nueva: `pnpm --filter app add @supabase/ssr` (debe resolver a 0.12.3; slopcheck ya [OK] en research, sin postinstall — no requiere checkpoint humano). Crear `app/lib/supabase-user.ts` con `import "server-only"` en línea 1. Exportar `updateSession(request: NextRequest)` VERBATIM del §Pattern 2 de RESEARCH: `createServerClient(process.env.SUPABASE_URL!, process.env.SUPABASE_PUBLISHABLE_KEY!, { cookies: { getAll, setAll } })`, sin código entre `createServerClient` y `await supabase.auth.getClaims()`, devolviendo `supabaseResponse` sin mutar. Añadir un `createUserClient()` helper (opcional) para la página/actions que construya un server client @supabase/ssr con la MISMA publishable key y adaptador de cookies del contexto de la request/response; hacer `throw new Error(...)` fail-loud si falta `SUPABASE_URL` o `SUPABASE_PUBLISHABLE_KEY`. JAMÁS leer `SUPABASE_SECRET_KEY` ni `SUPABASE_ANON_KEY` en este módulo. Añadir a `.env.example` (NUEVA línea, no reusar la anon legacy muerta línea ~24) el bloque de comentario espejando el idiom + `SUPABASE_PUBLISHABLE_KEY=` con valor VACÍO. La key es server-side → SIN prefijo `NEXT_PUBLIC_`.
  </action>
  <verify>
    <automated>cd app && pnpm ls @supabase/ssr | grep -q "0.12" && pnpm test lib/env-example-guard.test.ts && pnpm exec node -e "const s=require('fs').readFileSync('lib/supabase-user.ts','utf8'); if(!s.startsWith('import \"server-only\"')) throw new Error('falta server-only en linea 1'); if(/SUPABASE_SECRET_KEY|SUPABASE_ANON_KEY/.test(s)) throw new Error('supabase-user NO debe leer secret/anon'); if(!/SUPABASE_PUBLISHABLE_KEY/.test(s)) throw new Error('falta publishable key'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `@supabase/ssr` resuelto a 0.12.x en app/package.json.
    - `app/lib/supabase-user.ts` empieza con `import "server-only"` (línea 1) y NO contiene la cadena `SUPABASE_SECRET_KEY` ni `SUPABASE_ANON_KEY`.
    - `updateSession` NO tiene código entre `createServerClient(...)` y `getClaims()`; devuelve `supabaseResponse`.
    - `.env.example` contiene una línea `SUPABASE_PUBLISHABLE_KEY=` con valor vacío; `env-example-guard.test.ts` pasa (0 offenders).
    - `pnpm --filter app test lib/env-example-guard.test.ts` verde.
  </acceptance_criteria>
  <done>@supabase/ssr instalado, supabase-user.ts server-only con publishable key + updateSession canónico, .env.example documenta la nueva key vacía, env-example-guard verde.</done>
</task>

<task type="auto">
  <name>Task 2: Primer middleware.ts (Edge deprecado) + ruta de prueba /spike-auth con OTP</name>
  <files>app/middleware.ts, app/app/spike-auth/page.tsx, app/app/spike-auth/actions.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (OBLIGATORIO por app/AGENTS.md — leer ANTES de escribir el middleware; la deprecación de middleware.ts es ESPERADA, NO un bug a migrar)
    - app/app/admin/revisar-entidades/page.tsx (analog exacto: página gated no enlazada + server action validate→call→throw)
    - app/lib/supabase-user.ts (creado en Task 1 — updateSession + createUserClient)
    - 97-RESEARCH.md §Pattern 1 (middleware.ts shape + matcher) + §Pattern 3 (OTP send/verify) + §Anti-Patterns
    - 97-PATTERNS.md secciones middleware.ts, spike-auth/page.tsx, Lockdown guard interaction
  </read_first>
  <action>
    Crear `app/middleware.ts` (raíz de app/) VERBATIM del §Pattern 1: `export async function middleware(request: NextRequest) { return await updateSession(request); }` importando `updateSession` de `@/lib/supabase-user`, más `export const config = { matcher: [...] }` que salta `_next/static`, `_next/image`, `favicon.ico` y assets de imagen. JAMÁS crear `proxy.ts`, JAMÁS correr `npx @next/codemod middleware-to-proxy`, JAMÁS añadir `export const config = { runtime: ... }` (proxy files tiran con runtime; el deprecado default es aceptable). Aceptar el warning de deprecación en build como esperado. Crear `app/app/spike-auth/page.tsx`: página server NO enlazada (no tocar nav ni home), copiando el skeleton `<main className="max-w-3xl ...">` del analog admin; muestra estado de sesión vía `getClaims()` del cliente user (sin PII sensible: mostrar solo si hay sesión / claims mínimos, NUNCA el email crudo ni el token). Crear `app/app/spike-auth/actions.ts` con dos server actions (`"use server"`): `enviarOtp(email)` → `supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: true } })` (SIN `emailRedirectTo`); `verificarOtp(email, token)` → `supabase.auth.verifyOtp({ email, token, type: "email" })`. El cliente supabase de las actions DEBE estar ligado a las cookies de la response (para que verifyOtp escriba Set-Cookie). Validar inputs ANTES de la llamada (email string no vacío, token string). En caso de error: `throw new Error(...)` SIN interpolar email ni token (contra el idiom del analog que sí echa el id). NUNCA loguear email/token/sesión (V7, 21.719). Cero `.rpc()` y cero `.from()` de tablas PII (OTP va por `supabase.auth.*`, que el lockdown-guard no policía).
  </action>
  <verify>
    <automated>cd app && [ ! -f proxy.ts ] && [ ! -f app/proxy.ts ] && pnpm exec node -e "const s=require('fs').readFileSync('middleware.ts','utf8'); if(/runtime/.test(s)) throw new Error('middleware NO debe declarar runtime'); if(!/export const config/.test(s)||!/matcher/.test(s)) throw new Error('falta config.matcher'); if(!/updateSession/.test(s)) throw new Error('middleware debe llamar updateSession'); const a=require('fs').readFileSync('app/spike-auth/actions.ts','utf8'); if(!/verifyOtp/.test(a)||!/signInWithOtp/.test(a)) throw new Error('faltan calls OTP'); if(/emailRedirectTo/.test(a)) throw new Error('OTP numerico: NO emailRedirectTo'); if(/console\.(log|error|info|warn)/.test(a)) throw new Error('NO loguear en actions OTP (PII)'); console.log('OK')" && pnpm test lib/lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/middleware.ts` existe; NO existe `app/proxy.ts` ni `proxy.ts`; el middleware NO declara `runtime`; tiene `export const config` con `matcher`; llama `updateSession`.
    - `app/app/spike-auth/actions.ts` llama `signInWithOtp` (con `shouldCreateUser:true`, SIN `emailRedirectTo`) y `verifyOtp({ type:"email" })`; cero `console.*`; cero `.rpc(`; cero `.from(`.
    - `lockdown-guard.test.ts` verde (Block B limpio con los nuevos archivos).
    - `/spike-auth` no aparece en navegación ni en home (verificado: no se tocó `app/app/page.tsx`).
  </acceptance_criteria>
  <done>middleware.ts Edge deprecado con matcher llama updateSession; /spike-auth no enlazada ejerce OTP send/verify server-side sin loguear PII; lockdown-guard verde; sin proxy.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Verde local — build de Next + suite completa + typecheck</name>
  <files>app/middleware.ts, app/lib/supabase-user.ts, app/app/spike-auth/page.tsx, app/app/spike-auth/actions.ts</files>
  <read_first>
    - 97-VALIDATION.md (Sampling Rate + comandos de suite)
    - 97-RESEARCH.md §Validation Architecture (Test Map: build no roto, guards verdes)
    - app/package.json (scripts build/test/typecheck)
  </read_first>
  <action>
    Correr `pnpm --filter app build` (Next only — NO el build OpenNext, eso es Plan 02 en Docker) y confirmar que compila con el middleware presente (el warning de deprecación de middleware.ts es ESPERADO y aceptable; NO migrar a proxy.ts para silenciarlo). Correr la suite completa de app + typecheck. Si algo falla, corregir en los archivos de Tasks 1-2 (nunca migrando a proxy.ts, nunca añadiendo runtime, nunca resucitando anon). Dejar todo verde. NO correr `pnpm audit` como bloqueante aquí si tarda; registrar su salida. Documentar en el SUMMARY: (a) que el warning de deprecación apareció como esperado, (b) cualquier ajuste, (c) que el build OpenNext real queda para Plan 02.
  </action>
  <verify>
    <automated>cd app && pnpm build && pnpm test && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter app build` termina con éxito (exit 0) con `middleware.ts` presente.
    - `pnpm --filter app test` verde (suite app ~1243 tests, incluidos env-example y lockdown guards).
    - `pnpm --filter app run typecheck` (tsc --noEmit) exit 0.
    - El warning de deprecación de middleware.ts está registrado en el SUMMARY como esperado (no se migró a proxy.ts).
  </acceptance_criteria>
  <done>next build + suite app + tsc --noEmit verdes con el middleware presente; warning de deprecación documentado como esperado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → middleware/SSR | request con cookies `sb-*` no confiables cruza aquí; el middleware refresca sesión |
| app server → GoTrue (Supabase) | server actions invocan signInWithOtp/verifyOtp; GoTrue emite tokens |
| módulo user client ↔ módulo service_role | dos clientes distintos; nunca confundir privilegios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Elevation of Privilege | app/lib/supabase-user.ts | mitigate | Módulo SEPARADO que lee SOLO `SUPABASE_PUBLISHABLE_KEY`; verify automatizado rechaza `SUPABASE_SECRET_KEY`/`SUPABASE_ANON_KEY` en el archivo. |
| T-97-02 | Spoofing / DoS | app/middleware.ts | mitigate | Filename deprecado `middleware.ts`, cero `proxy.ts`, cero `runtime` config (verify automatizado); evita el build roto que shippearía auth silenciosamente rota. |
| T-97-03 | Information Disclosure (PII 21.719) | app/app/spike-auth/actions.ts | mitigate | Cero `console.*`; email/token JAMÁS interpolados en errores ni logueados (verify automatizado); página no muestra email crudo. |
| T-97-04 | Elevation of Privilege | .env.example / publishable key | mitigate | Publishable key es de bajo privilegio RLS-gated; cero policies `to anon/authenticated` vivas (0044); no se crean grants ni migraciones en este plan. Placeholder vacío (guard). |
| T-97-05 | Tampering (resurrección anon) | .env.example | mitigate | NO se reusa `SUPABASE_ANON_KEY` (muerta 0044); línea NUEVA para publishable; verify rechaza anon en supabase-user.ts. |
| T-97-SC | Tampering | pnpm install `@supabase/ssr` | accept | slopcheck [OK] en RESEARCH (org Supabase oficial, repo presente, sin postinstall) → sin checkpoint humano; `[ASSUMED]` no aplica. |
</threat_model>

<verification>
- `pnpm --filter app build` exit 0 con middleware presente.
- Suite app + tsc --noEmit verdes; env-example-guard + lockdown-guard verdes.
- Cero `proxy.ts`; middleware sin `runtime`; supabase-user.ts server-only sin secret/anon.
- `app/app/page.tsx` (home) sin cambios (git diff vacío para ese archivo).
</verification>

<success_criteria>
Aporta a SC1 (middleware existe y no rompe el build de Next — el build OpenNext real es Plan 02) y a SC3 (Camino A intacto: publishable de bajo privilegio, cero toque a service_role/anon, cero migración/grant).
</success_criteria>

<output>
Create `.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-01-SUMMARY.md` when done.
</output>
