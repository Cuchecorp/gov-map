---
phase: 97-auth-p0-spike-auth-on-workers-de-risk
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - .planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md
autonomous: false
requirements: [AUTH-01]
user_setup:
  - service: supabase
    why: "Crear la publishable key de bajo privilegio + habilitar OTP por email numérico"
    env_vars:
      - name: SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard → Project Settings → API Keys → crear/leer publishable key (sb_publishable_…)"
    dashboard_config:
      - task: "Habilitar Email provider y plantilla OTP que renderice {{ .Token }} (NO {{ .ConfirmationURL }})"
        location: "Supabase Dashboard → Authentication → Email Templates / Providers"
  - service: cloudflare
    why: "Inyectar la publishable key como wrangler secret en el Worker (los secrets NO viven en wrangler.jsonc)"
    dashboard_config:
      - task: "wrangler secret put SUPABASE_PUBLISHABLE_KEY (OAuth global)"
        location: "terminal del operador con wrangler global autenticado"

must_haves:
  truths:
    - "El build OpenNext (Docker node:22-slim) tiene éxito con el nuevo middleware.ts presente"
    - "El OpenNext log se leyó: middleware.ts corre como Edge (warning de deprecación esperado), NO error 'Node.js middleware not supported'"
    - "El deploy wrangler publica el Worker y observatorio-congreso.thevalis.workers.dev sirve 200"
    - "SUPABASE_PUBLISHABLE_KEY está como wrangler secret en el Worker; provider Email + plantilla {{ .Token }} confirmados"
  artifacts:
    - path: ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md"
      provides: "Runbook reproducible del build Docker + deploy wrangler + secret + config Supabase, con el resultado del OpenNext log"
      min_lines: 30
  key_links:
    - from: "OpenNext build"
      to: "app/middleware.ts"
      via: "bundle del Worker incluye el middleware como Edge"
      pattern: "middleware"
    - from: "Worker (Cloudflare)"
      to: "SUPABASE_PUBLISHABLE_KEY"
      via: "wrangler secret"
      pattern: "SUPABASE_PUBLISHABLE_KEY"
---

<objective>
Desplegar el código del Plan 01 al deploy REAL (OpenNext → Cloudflare Workers) por el runbook probado (Docker `node:22-slim`, robocopy a `C:/Temp/obs-build`, wrangler global OAuth, pnpm 11 `dangerouslyAllowAllBuilds true`) y confirmar EMPÍRICAMENTE la pregunta #1 del spike: ¿el primer `middleware.ts` del repo sobrevive el build OpenNext (corre como Edge, NO dispara "Node.js middleware not supported")? Preparar el estado de runtime que la evidencia (Plan 03) necesita: la publishable key como wrangler secret y la config Auth de Supabase (provider Email + plantilla OTP `{{ .Token }}`).

Purpose: El middleware nuevo ES el riesgo del build OpenNext (Pitfall #1/#2). Este plan lo prueba en el pipeline real, no en `next build`. Si el build FALLA aquí → se documenta y Plan 03 ejecuta la rama de fallback honesto (SC4).

Output: `97-DEPLOY-RUNBOOK.md` con los comandos exactos, la lectura del OpenNext log (Edge vs error), la versión del deploy y la confirmación del secret + config Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-RESEARCH.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-PATTERNS.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-01-SUMMARY.md

<interfaces>
<!-- Runbook de deploy (REAL, no el stale docs/deploy-cloudflare.md). Fuente: 97-PATTERNS.md §Deploy runbook + MEMORY v6.0/61-02 + app/package.json scripts. -->
Scripts app/package.json:
- `cf-build` = `opennextjs-cloudflare build`
- `deploy` = `opennextjs-cloudflare build && opennextjs-cloudflare deploy`
- `preview` = `opennextjs-cloudflare build && opennextjs-cloudflare preview`

Runbook LOCKED (NO seguir docs/deploy-cloudflare.md — es del era anon-key, STALE):
- Build OpenNext en Docker `node:22-slim` (NUNCA alpine/Windows: symlink EPERM rompe el build en Windows).
- robocopy del repo a `C:/Temp/obs-build` (evita locks OneDrive/Windows), build ahí dentro.
- wrangler GLOBAL con OAuth (CI no tiene creds CF).
- pnpm 11 requiere `dangerouslyAllowAllBuilds true` en el contenedor.
- El Worker corre en https://observatorio-congreso.thevalis.workers.dev (ref Supabase bctyygbmqcvizyplktuw, sa-east-1).
- Secrets NO viven en wrangler.jsonc → `wrangler secret put SUPABASE_PUBLISHABLE_KEY`.
- Residuo benigno tras build: `C:/Temp/obs-build/node_modules` (lock Linux-container) — borrar tras reboot.

Señal de éxito/fallo del OpenNext log (Pitfall #1): éxito = warning de deprecación de middleware.ts + bundle emitido; FALLO = "Node.js middleware is not currently supported" / "switch to Edge Middleware".
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: OPERADOR — crear publishable key + habilitar OTP email + cargar wrangler secret</name>
  <read_first>
    - 97-RESEARCH.md §Environment Availability (SUPABASE_PUBLISHABLE_KEY = operador; provider/template) + §Pitfall 5/6 (OTP {{ .Token }}, rate limit 2/hr)
    - 97-PATTERNS.md §Deploy runbook (secret via wrangler)
  </read_first>
  <what-built>
    Plan 01 dejó el código listo (supabase-user.ts espera `SUPABASE_PUBLISHABLE_KEY`; actions.ts usa OTP numérico). Este paso es lo que Claude NO puede hacer: crear la key de bajo privilegio, tocar el dashboard de Supabase Auth, y cargar el secret en el Worker (OAuth de operador).
  </what-built>
  <how-to-verify>
    1. Supabase Dashboard → Project Settings → API Keys: crear/copiar la **publishable key** (`sb_publishable_…`) del proyecto `bctyygbmqcvizyplktuw`. NO es la anon legacy (muerta) ni la secret.
    2. Supabase Dashboard → Authentication → Providers: confirmar **Email** habilitado. Email Templates → plantilla de "Magic Link"/OTP: confirmar que renderiza `{{ .Token }}` (código numérico), NO `{{ .ConfirmationURL }}`. (Si envía link en vez de código, cambiar la plantilla.)
    3. Cargar el secret en el Worker: `wrangler secret put SUPABASE_PUBLISHABLE_KEY` (pegar el valor `sb_publishable_…`) con wrangler global autenticado (OAuth). Confirmar con `wrangler secret list` que aparece.
    4. Poner el valor también en el `.env` LOCAL del operador (NO en `.env.example`, que queda vacío) para permitir pruebas curl locales si hiciera falta.
    Emails de prueba = SOLO direcciones del operador. Recordar el rate limit 2/hora del SMTP interno.
  </how-to-verify>
  <resume-signal>Escribir "listo" con: (a) publishable key cargada como wrangler secret (confirmada en secret list), (b) provider Email ON + plantilla {{ .Token }} confirmada, o describir qué falta. Si el operador NO puede crear la key → registrar como bloqueo y la fase documenta fallback (Plan 03 SC4).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build OpenNext real (Docker) + deploy wrangler + leer el log del middleware</name>
  <files>.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md</files>
  <read_first>
    - 97-PATTERNS.md §Deploy runbook (Docker node:22-slim, robocopy C:/Temp/obs-build, wrangler global, dangerouslyAllowAllBuilds)
    - 97-RESEARCH.md §Pitfall 1 + §Pitfall 2 (señales de fallo del OpenNext log) + §Open Questions #1
    - app/package.json (scripts cf-build/deploy) + app/wrangler.jsonc + app/open-next.config.ts
    - 97-01-SUMMARY.md (confirmar que next build local ya pasó)
  </read_first>
  <action>
    Ejecutar el runbook de deploy REAL (NO el stale `docs/deploy-cloudflare.md`): robocopy del repo a `C:/Temp/obs-build`, build OpenNext dentro de Docker `node:22-slim` con pnpm 11 `dangerouslyAllowAllBuilds true`, luego `opennextjs-cloudflare deploy` con wrangler global OAuth. LEER el OpenNext build log con atención al middleware: éxito = warning de deprecación de `middleware.ts` + bundle emitido normal; FALLO = línea "Node.js middleware is not currently supported" o similar. Registrar TODO en `97-DEPLOY-RUNBOOK.md`: comandos exactos ejecutados, el fragmento relevante del log (Edge vs error), la versión/hash del deploy, y el resultado de `curl -I https://observatorio-congreso.thevalis.workers.dev/` (debe ser 200 — Camino A intacto). Si el build FALLA por el middleware → NO forzar (JAMÁS migrar a proxy.ts para "arreglarlo"); registrar el fallo textual en el runbook y marcarlo como disparador de la rama de fallback (Plan 03 Task 3). Borrar el residuo `C:/Temp/obs-build/node_modules` al final si el entorno lo permite.
  </action>
  <verify>
    <automated>curl -sI https://observatorio-congreso.thevalis.workers.dev/ | grep -E "HTTP/.* 200" && grep -Eiq "middleware|opennext|deploy|node:22-slim" ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md"</automated>
  </verify>
  <acceptance_criteria>
    - `97-DEPLOY-RUNBOOK.md` existe (≥30 líneas) con: comandos del build Docker + deploy, el fragmento del OpenNext log sobre el middleware (Edge OK vs error), versión del deploy.
    - `curl -I https://observatorio-congreso.thevalis.workers.dev/` devuelve 200 (home sirve — Camino A intacto).
    - El runbook declara explícitamente el veredicto del build: "middleware corre como Edge (deprecation warning)" O "build falló con <mensaje textual>".
    - Si falló: el runbook lo marca como disparador de fallback SC4; NO se creó `proxy.ts` ni se añadió `runtime`.
  </acceptance_criteria>
  <done>Build OpenNext real ejecutado por el runbook probado; log del middleware leído y registrado (Edge OK o error textual); deploy publicado y home 200; runbook completo. Si build roto → registrado como disparador de fallback sin migrar a proxy.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline build → Worker en prod | el bundle OpenNext (ahora con middleware) se publica al edge de Cloudflare |
| operador → wrangler/Supabase dashboard | acciones con credenciales que Claude no posee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06 | Denial of Service | OpenNext build con primer middleware | mitigate | Leer el log; si "Node.js middleware not supported" → NO shippear auth rota; registrar fallo y disparar fallback (SC4). Body del middleware mínimo aísla el fallo a "el middleware existe". |
| T-97-07 | Elevation of Privilege | wrangler secret SUPABASE_PUBLISHABLE_KEY | mitigate | Se carga la publishable (bajo privilegio), NUNCA la service key ni la anon. El secret vive en el Worker, no en wrangler.jsonc ni en git. |
| T-97-08 | Information Disclosure | Camino A / plano service_role | mitigate | `curl -I /` 200 confirma home intacta; el deploy no toca el cliente service_role existente; cero migración/grant. |
| T-97-09 | Information Disclosure (PII) | plantilla OTP + emails de prueba | accept | Emails = solo del operador; plantilla `{{ .Token }}` numérico evita link redirect; rate limit 2/hr conocido. |
</threat_model>

<verification>
- OpenNext build ejecutado en Docker (no Windows local, no CI puro sin creds).
- OpenNext log leído: middleware Edge OK (warning) o error textual — veredicto explícito en el runbook.
- `curl -I https://observatorio-congreso.thevalis.workers.dev/` → 200 (Camino A intacto).
- SUPABASE_PUBLISHABLE_KEY como wrangler secret; provider Email + plantilla {{ .Token }} confirmados.
</verification>

<success_criteria>
Cierra SC1 empíricamente sobre el deploy REAL (middleware corre en OpenNext sin romper el build — verificado, no asumido) y prepara SC2 (estado de runtime para la evidencia). Si el build falla, alimenta SC4 (fallback) en Plan 03.
</success_criteria>

<output>
Create `.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-02-SUMMARY.md` when done.
</output>
