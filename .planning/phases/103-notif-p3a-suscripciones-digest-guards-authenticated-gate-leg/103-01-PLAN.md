---
phase: 103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/lockdown-guard.test.ts
  - app/lib/notif-gate.ts
  - app/lib/notif-antiflip-guard.test.ts
  - .env.example
autonomous: true
requirements: [NOTIF-02, NOTIF-05]
must_haves:
  truths:
    - "The lockdown-guard bites on any `to authenticated` grant/policy targeting a table not in USER_OWNED_TABLES"
    - "The lockdown-guard bites on any `authenticated` grant to notificacion_envio (service_role-only queue)"
    - "The NOTIF flag reads NOTIF_PUBLIC_ENABLED with a single strict === \"true\""
    - "The anti-flip guard bites on any relaxation of the NOTIF flag and on .env.example=true"
  artifacts:
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "Block D (authenticated allowlist) + Block E (notificacion_envio zero-grant) + authenticatedGrantOffenders self-check"
      contains: "USER_OWNED_TABLES"
    - path: "app/lib/notif-gate.ts"
      provides: "notifPublicEnabled() chokepoint"
      exports: ["notifPublicEnabled"]
    - path: "app/lib/notif-antiflip-guard.test.ts"
      provides: "3-vector anti-flip guard + mutation self-check"
    - path: ".env.example"
      provides: "NOTIF_PUBLIC_ENABLED=false + RESEND_API_KEY= entries"
      contains: "NOTIF_PUBLIC_ENABLED=false"
  key_links:
    - from: "app/lib/lockdown-guard.test.ts"
      to: "supabase/migrations/*.sql"
      via: "static scan of migrations >0044 for authenticated grants"
      pattern: "authenticatedGrantOffenders"
    - from: "app/lib/notif-antiflip-guard.test.ts"
      to: "app/lib/notif-gate.ts"
      via: "RAW_ENV_ALLOWLIST chokepoint scan"
      pattern: "notif-gate"
---

<objective>
Land the CI guardrails that must exist BEFORE any user data reaches PROD. This is the FIRST commit of the phase (NOTIF-02, non-negotiable ordering): extend the lockdown-guard so the `authenticated` role — introduced for the first time in this phase — cannot silently over-grant. Also land the NOTIF feature flag chokepoint + anti-flip guard (NOTIF-05) and the `.env.example` entries the later plans consume.

Purpose: The existing lockdown-guard forbids only `to anon`/`to public`; `authenticated` is invisible to it (Pitfall 1 — a silent hole reachable for the first time this phase). Writing the guard RED-first means it will bite the migrations that Plan 02 writes, catching an over-grant before PROD.
Output: Extended `lockdown-guard.test.ts` (Block D/E + `authenticatedGrantOffenders` self-check), `notif-gate.ts`, `notif-antiflip-guard.test.ts`, and `.env.example` flag+secret entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend lockdown-guard to the authenticated role (Block D + Block E + self-check) — FIRST COMMIT</name>
  <files>app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/lib/lockdown-guard.test.ts (the file being modified — read the ENTIRE file: the `anonGrantOffenders` machinery ~line 222, the `PII_TABLES` const ~line 133, `PUBLIC_RPC_ALLOWLIST` ~line 165, `walkSourceFiles(APP_ROOT)` `.from()` scan ~lines 505-527, and the `parseDefinedRpcNames` mutation self-check ~lines 376-430)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md (§ lockdown-guard.test.ts — exact machinery to clone)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (§Pattern 2, §Pitfall 1)
  </read_first>
  <behavior>
    - authenticatedGrantOffenders("grant select on public.proyecto to authenticated;", USER_OWNED_TABLES) → returns 1 offender (proyecto not user-owned)
    - authenticatedGrantOffenders("grant insert on public.notificacion_envio to authenticated;", USER_OWNED_TABLES) → returns 1 offender (Block E: queue is service_role-only, not in USER_OWNED_TABLES)
    - authenticatedGrantOffenders("create policy x on suscripcion for select to authenticated using ((select auth.uid()) = user_id);", USER_OWNED_TABLES) → returns 0 offenders (suscripcion allowlisted)
    - authenticatedGrantOffenders on a comment-only line "-- grant select on x to authenticated" → 0 offenders (stripSqlComments removes it before scan)
    - Block D scan over all supabase/migrations/*.sql with version >0044 finds 0 offenders (the real migrations Plan 02 writes only grant authenticated on suscripcion/consentimiento)
    - Assertion: app/lib/supabase.ts (service_role chokepoint) never does .from('suscripcion'|'consentimiento'|'notificacion_envio')
    - Assertion: app/ tree never does .from('notificacion_envio')
  </behavior>
  <action>
    ADD (do NOT rewrite the file) Block D and Block E next to the existing `(A)`/`(A2)` blocks, mirroring the existing style. Add a top-level constant `const USER_OWNED_TABLES = new Set(["suscripcion", "consentimiento"]);` next to `PII_TABLES` — NOT notificacion_envio (it is the service_role-only queue). Clone `anonGrantOffenders` (~line 222) into `authenticatedGrantOffenders(strippedLowerSql: string, allowlist: Set<string>): string[]`: swap the `(anon|public)` regex for `authenticated`, extract the target table name per statement (the token after `on public.` for grants, after `on ` before `for`/`to` for `create policy`), and INVERT to a positive allowlist — a `grant … to authenticated` OR `create policy … to authenticated` whose target table is NOT in `allowlist` is an offender. Reuse the existing `stripSqlComments` + `split(";")` per-statement loop. Block D: scan every migration file with version >0044 (reuse the existing migration-version filter used by `anonGrantOffenders`) and assert `authenticatedGrantOffenders(...).length === 0` across all. Block E: assert no migration grants `authenticated` any privilege on `notificacion_envio` — this falls out of Block D automatically (notificacion_envio not in USER_OWNED_TABLES) but ALSO add an explicit named test asserting a `grant … on notificacion_envio to authenticated` fixture is flagged. Add the mutation self-check exactly per the `<behavior>` fixtures, cloning the in-memory `parseDefinedRpcNames` self-check idiom (~lines 403-430) — exercise the REAL `authenticatedGrantOffenders` function object so a green no-op is impossible. Reuse the existing `walkSourceFiles(APP_ROOT)` scan to add: (1) `app/lib/supabase.ts` never `.from()`s any user-owned or queue table; (2) `app/` never `.from('notificacion_envio')`. Grep-gate hygiene: strip comments before counting; never gate on an unfiltered `== 0`.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run lockdown-guard 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test -- --run lockdown-guard` exits 0 with the new Block D/E describe blocks green
    - The mutation self-check test proves `authenticatedGrantOffenders` returns 1 offender for the proyecto fixture and the notificacion_envio fixture, and 0 for the suscripcion fixture
    - `git grep -n "USER_OWNED_TABLES" app/lib/lockdown-guard.test.ts` prints the constant with exactly `suscripcion` and `consentimiento` (NOT notificacion_envio)
    - The existing anon/public blocks and RPC-allowlist blocks still pass (no regression)
  </acceptance_criteria>
  <done>Block D (authenticated allowlist) + Block E (notificacion_envio zero-grant) + `authenticatedGrantOffenders` self-check land as the first commit; the guard bites over-grants and passes on the real (yet-to-be-written) user tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NOTIF flag chokepoint + anti-flip guard + .env.example entries</name>
  <files>app/lib/notif-gate.ts, app/lib/notif-antiflip-guard.test.ts, .env.example</files>
  <read_first>
    - app/lib/vsim-gate.ts (byte-for-byte clone target; JSDoc doctrine lines 3-37)
    - app/lib/vsim-antiflip-guard.test.ts (byte-for-byte clone target — stripTsComments, walkSourceFiles+SKIP_DIRS, RAW_ENV_ALLOWLIST, detectarRelajacionGate V1a-V1d/V2a-V2b, detectarRawEnvEnRuta, the three Vector describe blocks scanning BOTH app/ and packages/, and the §4 mutation self-check)
    - .env.example (read the flags section — VSIM block; add NOTIF after it)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md (§notif-gate.ts, §notif-antiflip-guard.test.ts)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (§Pattern 8 — the DISTINCTION from VSIM/MONEY)
  </read_first>
  <behavior>
    - notifPublicEnabled({ NOTIF_PUBLIC_ENABLED: "true" }) === true
    - notifPublicEnabled({ NOTIF_PUBLIC_ENABLED: "false" }) === false
    - notifPublicEnabled({}) === false (absence = OFF, never throws)
    - notifPublicEnabled({ NOTIF_PUBLIC_ENABLED: "TRUE" }) === false (strict === "true")
    - anti-flip Vector 1: notif-gate.ts uses a single `=== "true"` — no `||`, `Boolean()`, `!== "false"`, `.trim()`, or `NODE_ENV` fallback
    - anti-flip Vector 2: .env.example line is exactly NOTIF_PUBLIC_ENABLED=false
    - anti-flip Vector 3: no raw NOTIF_PUBLIC_ENABLED reference anywhere in app/ or packages/ except notif-gate.ts (RAW_ENV_ALLOWLIST)
    - mutation self-check: a fixture flag file with `!== "false"` is detected as a relaxation
  </behavior>
  <action>
    Create `app/lib/notif-gate.ts` cloning `vsim-gate.ts` byte-for-byte, swapping VSIM→NOTIF: `import "server-only";` then `export function notifPublicEnabled(env: Record<string, string | undefined> = process.env): boolean { return env.NOTIF_PUBLIC_ENABLED === "true"; }`. Keep the full JSDoc doctrine (single `=== "true"`, no `NEXT_PUBLIC_` prefix, absence = OFF not throw, this is the ONLY reader of `NOTIF_PUBLIC_ENABLED`). Create `app/lib/notif-antiflip-guard.test.ts` cloning `vsim-antiflip-guard.test.ts` wholesale — reuse `stripTsComments`, `walkSourceFiles`+`SKIP_DIRS`, `RAW_ENV_ALLOWLIST` (with the relative path `lib/notif-gate.ts`), `detectarRelajacionGate` (V1a-V1d + V2a/V2b targeting `NOTIF_PUBLIC_ENABLED`), `detectarRawEnvEnRuta`, the three Vector describe blocks (scanning BOTH `app/` and `packages/`), and the §4 mutation self-check. Add to `.env.example` in the flags section (after the VSIM block): a comment block mirroring the VSIM comment then `NOTIF_PUBLIC_ENABLED=false` and `RESEND_API_KEY=` (empty; consumed by Plan 04's dry-run and Plan 05's deploy). NOTE the DISTINCTION from VSIM in the notif-gate.ts JSDoc: the operator PRE-AUTHORIZED the flip this run, but `.env.example` STAYS `=false` and the flip happens at deploy-time (Worker env var), NEVER committed — the anti-flip guard is unchanged in strictness.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run notif-antiflip-guard 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test -- --run notif-antiflip-guard` exits 0 (all 3 Vectors + self-check green)
    - `git grep -n "NOTIF_PUBLIC_ENABLED" app packages` shows the raw env token ONLY in app/lib/notif-gate.ts (plus test fixtures inside notif-antiflip-guard.test.ts)
    - `.env.example` contains exactly `NOTIF_PUBLIC_ENABLED=false` and a `RESEND_API_KEY=` line (never `=true`)
    - `cd app && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>notif-gate.ts is the single strict reader; the anti-flip guard bites relaxations and .env.example=true; .env.example carries the flag (false) + RESEND_API_KEY placeholder for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → PROD DB | new `authenticated` role grants cross here; the guard is the gate |
| committed env → deploy env | `.env.example` (committed) vs Worker env (deploy-time flip) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Elevation of Privilege | `to authenticated` over-grant to a non-user table | mitigate | Block D positive allowlist (USER_OWNED_TABLES) + self-check in lockdown-guard.test.ts |
| T-103-02 | Elevation of Privilege | `authenticated` grant on notificacion_envio queue | mitigate | Block E explicit zero-grant assertion (queue is service_role-only) |
| T-103-03 | Governance (feature flip) | agent commits NOTIF_PUBLIC_ENABLED=true | mitigate | anti-flip guard Vectors 1-3 + .env.example=false; flip is deploy-time only |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new external packages this plan (RESEARCH §Package Legitimacy Audit); no install task |
</threat_model>

<verification>
- `cd app && pnpm test -- --run lockdown-guard notif-antiflip-guard` green
- `cd app && pnpm exec tsc --noEmit` exits 0
- Guard is RED-capable: manually adding a fixture `grant select on public.proyecto to authenticated` to a test-only string is flagged by `authenticatedGrantOffenders`
</verification>

<success_criteria>
- Lockdown-guard extended to `authenticated` (Block D allowlist + Block E queue zero-grant + self-check) landed as first commit
- NOTIF flag chokepoint + 3-vector anti-flip guard green
- `.env.example` has `NOTIF_PUBLIC_ENABLED=false` + `RESEND_API_KEY=` (never true, never a real value)
- Full app suite + tsc green (no regression)
</success_criteria>

<output>
Create `.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-01-SUMMARY.md` when done
</output>
