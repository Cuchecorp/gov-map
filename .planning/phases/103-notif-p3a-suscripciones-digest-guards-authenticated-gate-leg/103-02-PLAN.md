---
phase: 103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - supabase/migrations/0069_suscripcion_rls.sql
  - supabase/migrations/0070_notificacion_envio.sql
  - supabase/migrations/0071_consentimiento.sql
  - supabase/tests/0069_suscripcion_rls.test.sql
  - supabase/tests/0070_notificacion_envio.test.sql
  - supabase/tests/0071_consentimiento.test.sql
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-04]
must_haves:
  truths:
    - "suscripcion is RLS deny-by-default; user A cannot see or delete user B's rows"
    - "consentimiento records fecha/version/metodo, owner-scoped RLS insert"
    - "notificacion_envio has ZERO authenticated grant (service_role-only queue with idempotent cursor)"
    - "anon has no select on any of the three tables (deny-by-default)"
  artifacts:
    - path: "supabase/migrations/0069_suscripcion_rls.sql"
      provides: "suscripcion table + RLS to authenticated + auth.uid()=user_id"
      contains: "to authenticated"
    - path: "supabase/migrations/0070_notificacion_envio.sql"
      provides: "queue/cursor table, service_role-only, no authenticated grant"
      contains: "notificacion_envio"
    - path: "supabase/migrations/0071_consentimiento.sql"
      provides: "consent record (fecha/version/metodo) 21.719"
      contains: "consentimiento"
    - path: "supabase/tests/0069_suscripcion_rls.test.sql"
      provides: "two-user isolation pgTAP"
      contains: "request.jwt.claims"
  key_links:
    - from: "supabase/migrations/0069_suscripcion_rls.sql"
      to: "auth.users"
      via: "user_id references auth.users(id) on delete cascade"
      pattern: "references auth.users"
    - from: "supabase/tests/0069_suscripcion_rls.test.sql"
      to: "suscripcion"
      via: "set local role authenticated + jwt claims sub"
      pattern: "set local role authenticated"
---

<objective>
Create the three user-data tables (migrations 0069/0070/0071) with RLS `to authenticated` + `auth.uid() = user_id` deny-by-default, plus their pgTAP tests proving isolation (user-A-no-ve-B), the notificacion_envio zero-grant invariant, and the consent-record shape. Tests run pre-apply against a local applied schema; PROD apply is Plan 05.

Purpose: This is the first user-owned data in the system (NOTIF-01). RLS is the DB boundary — app-layer checks are bypassable. The pgTAP two-user isolation test is the ONLY proof isolation works (vitest/tsc don't touch Postgres — Pitfall 6). The migrations are written AFTER Plan 01's guard so the guard bites any over-grant.
Output: 0069/0070/0071 migrations + matching pgTAP tests, validated locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 0069 (suscripcion) + 0071 (consentimiento) — RLS user-owned</name>
  <files>supabase/migrations/0069_suscripcion_rls.sql, supabase/migrations/0071_consentimiento.sql</files>
  <read_first>
    - supabase/migrations/0043_lockdown_web_reader.sql (banner header lines 1-41, `create policy … for select to <role> using` idiom, drop-if-exists idempotency, `--single-transaction` apply note with PGCLIENTENCODING=UTF8, schema_migrations insert note lines 274-276)
    - supabase/migrations/0068_coincidencia_votos_par.sql (most recent migration — header/apply convention, doble-revoke idiom)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md (section 0069 + section 0071 — exact policy shape)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (Pattern 1, Pattern 7)
  </read_first>
  <action>
    Create supabase/migrations/0069_suscripcion_rls.sql with the banner header cloned from 0043 (BIG comment, an "APLICACIÓN:" line reading `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f …`, closing schema_migrations insert note for version '0069'). Table suscripcion columns: id uuid primary key default gen_random_uuid(); user_id uuid not null references auth.users(id) on delete cascade; tipo text not null check (tipo in ('proyecto','parlamentario')); objetivo_id text not null; estado text not null default 'pendiente' check (estado in ('pendiente','confirmada','baja')); confirm_token_hash text; baja_token_hash text (sha256 hex, stored hashed — raw token lives only in the email link, see Plan 04); confirm_expira_at timestamptz; created_at timestamptz not null default now(); unique (user_id, tipo, objetivo_id). alter table suscripcion enable row level security. Three policies to authenticated using the (select auth.uid()) = user_id wrapper (NOT bare auth.uid() — init-plan optimization): suscripcion_select_own (for select), suscripcion_insert_own (for insert with check), suscripcion_delete_own (for delete using). NO grant to anon/public/web_reader. Deny-by-default comment mirroring 0043 lines 135-137. Create supabase/migrations/0071_consentimiento.sql (sibling shape): table consentimiento with id; user_id uuid not null references auth.users(id) on delete cascade; version_texto text not null; metodo text not null default 'doble_opt_in_email'; created_at timestamptz not null default now(). RLS enabled; policies consentimiento_insert_own (for insert to authenticated with check ((select auth.uid()) = user_id)) and consentimiento_select_own (for select to authenticated using ((select auth.uid()) = user_id)); service_role reads via RLS bypass. NO anon grant. Both tables are in Plan 01's USER_OWNED_TABLES allowlist.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && bash -c "grep -v '^--' supabase/migrations/0069_suscripcion_rls.sql | grep -c 'to authenticated'"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0069_suscripcion_rls.sql exists with exactly 3 `to authenticated` policies (select/insert/delete) using `(select auth.uid()) = user_id`
    - supabase/migrations/0071_consentimiento.sql exists with insert+select owner-scoped policies and version_texto/metodo/created_at columns
    - Neither migration contains `to anon`, `to public`, or `to web_reader` (grep of non-comment lines returns 0)
    - Both reference `auth.users(id) on delete cascade`
    - `cd app && pnpm test -- --run lockdown-guard` still green (guard accepts these allowlisted grants)
  </acceptance_criteria>
  <done>0069 + 0071 created: suscripcion (full CRUD by owner) and consentimiento (insert+read own), RLS deny-by-default, cascade delete, lockdown-guard passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0070 (notificacion_envio) — service_role-only queue + idempotent cursor</name>
  <files>supabase/migrations/0070_notificacion_envio.sql</files>
  <read_first>
    - supabase/migrations/0043_lockdown_web_reader.sql (grants idiom)
    - supabase/migrations/0053_leylobby_cursor_estado.sql (cursor precedent — per-key watermark advanced atomically)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md (section 0070)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (Pattern 3, Pattern 5)
  </read_first>
  <action>
    Create supabase/migrations/0070_notificacion_envio.sql (banner header cloned from 0043). Table notificacion_envio columns: id uuid primary key default gen_random_uuid(); user_id uuid not null references auth.users(id) on delete cascade; suscripcion_id uuid references suscripcion(id) on delete cascade; ultimo_evento_visto text (idempotent cursor watermark — max tramitacion_evento/actualidad_senal key included in the last digest); enviado_at timestamptz; estado text not null default 'pendiente' check (estado in ('pendiente','enviado','error')); created_at timestamptz not null default now(). alter table notificacion_envio enable row level security with NO policy for authenticated (service_role-only — the cron owns it; RLS bypass for service_role). Comment: "cola/log del digest; service_role-only (cron EGRESO); authenticated JAMÁS la toca (ni lectura). Cursor ultimo_evento_visto avanza atómicamente solo en envío exitoso." Do NOT grant authenticated any privilege (Block E of Plan 01's guard enforces this).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && bash -c "grep -v '^--' supabase/migrations/0070_notificacion_envio.sql | grep -ci 'to authenticated'"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0070_notificacion_envio.sql exists; grep for `to authenticated` on non-comment lines returns 0
    - Table has RLS enabled with NO authenticated policy; `ultimo_evento_visto` cursor column present
    - `cd app && pnpm test -- --run lockdown-guard` green (Block E confirms zero authenticated grant on notificacion_envio)
  </acceptance_criteria>
  <done>0070 created: notificacion_envio is a service_role-only queue with an idempotent cursor column and zero authenticated grant.</done>
</task>

<task type="auto">
  <name>Task 3: pgTAP tests 0069/0070/0071 + local pre-apply run</name>
  <files>supabase/tests/0069_suscripcion_rls.test.sql, supabase/tests/0070_notificacion_envio.test.sql, supabase/tests/0071_consentimiento.test.sql</files>
  <read_first>
    - supabase/tests/0065_actualidad_senal.test.sql (file structure: header block, `begin; select plan(N); … select * from finish(); rollback;`)
    - supabase/tests/0068_coincidencia_votos_par.test.sql (has_table_privilege idiom)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (Code Examples — the two-user `set local role authenticated` + `request.jwt.claims` idiom; Pitfall 2)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-PATTERNS.md (0069/0070/0071 test excerpts)
  </read_first>
  <action>
    Create supabase/tests/0069_suscripcion_rls.test.sql per RESEARCH Code Examples: header comment (verifies RLS user-owned A-no-ve-B against an APPLIED schema, run via `psql -tA -f`, mirrors 0065 style), then `begin; select plan(6);`. Structural asserts: has_table('public','suscripcion',...); RLS enabled via relrowsecurity. Seed two auth.users (a/b) as superuser. Then `set local role authenticated; set local request.jwt.claims = '{"sub":"…a","role":"authenticated"}';` insert A's suscripcion, results_eq A sees 1. Switch claims to B's sub, is_empty (B sees 0), and is_empty(delete … returning 1) (B cannot delete A's row). reset role; then is(has_table_privilege('anon','suscripcion','select'), false, 'anon SIN select'). select * from finish(); rollback;. Use the tree idiom — NOT basejump tests.authenticate_as (not installed). NEVER assert as postgres/service_role (bypasses RLS, green no-op, Pitfall 2). Create supabase/tests/0070_notificacion_envio.test.sql: has_table, RLS enabled, zero-grant asserts is(has_table_privilege('authenticated','notificacion_envio','select'), false, …) + insert + update. Create supabase/tests/0071_consentimiento.test.sql: has_table, RLS enabled, has_column for version_texto/metodo/created_at, two-user insert-own + B-cannot-read-A, anon zero-select. Apply the three migrations to a local scratch DB first, then run each pgTAP file and confirm all `ok`, zero `not ok`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && bash -c "test -f supabase/tests/0069_suscripcion_rls.test.sql && test -f supabase/tests/0070_notificacion_envio.test.sql && test -f supabase/tests/0071_consentimiento.test.sql && grep -c 'set local role authenticated' supabase/tests/0069_suscripcion_rls.test.sql"</automated>
  </verify>
  <acceptance_criteria>
    - All three pgTAP files exist; 0069 uses `set local role authenticated` + `request.jwt.claims` with two distinct `sub` values
    - Run against a locally applied schema: every line prints `ok`, zero `not ok`
    - 0070 asserts `authenticated` has no select/insert/update on notificacion_envio
    - 0069 + 0071 assert B cannot read/delete A's rows and anon has no select
    - No assertion runs as postgres/service_role (Pitfall 2 avoided)
  </acceptance_criteria>
  <done>Three pgTAP files prove two-user RLS isolation, queue zero-grant, and consent shape against a locally applied schema — ready for PROD apply in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated session → suscripcion/consentimiento | RLS auth.uid()=user_id is the only gate |
| service_role cron → notificacion_envio | queue is service_role-only; authenticated has zero grant |
| anon (public) → all three tables | deny-by-default, no policy, no grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04 | Information Disclosure | user B reads user A's subscriptions | mitigate | RLS `(select auth.uid()) = user_id` + two-user pgTAP isolation proof |
| T-103-05 | Tampering | user B deletes user A's row | mitigate | delete policy scoped to owner; pgTAP asserts B's delete returns empty |
| T-103-06 | Elevation of Privilege | authenticated touches notificacion_envio queue | mitigate | zero authenticated grant + Block E guard + pgTAP has_table_privilege false |
| T-103-07 | Information Disclosure | anon reads user tables | mitigate | no policy/grant for anon (deny-by-default); pgTAP anon-select false |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new external packages this plan; no install task |
</threat_model>

<verification>
- Apply 0069/0070/0071 to a local scratch DB (`PGCLIENTENCODING=UTF8 psql … --single-transaction -f`), then run each `supabase/tests/00XX_*.test.sql` via `psql -tA -f` — all ok, zero not ok
- `cd app && pnpm test -- --run lockdown-guard` green (guard bites nothing on the real migrations)
</verification>

<success_criteria>
- Three migrations created (RLS deny-by-default, cascade delete, allowlisted authenticated grants only)
- notificacion_envio has zero authenticated grant (queue service_role-only)
- pgTAP two-user isolation + zero-grant + consent shape all green locally
- Lockdown-guard from Plan 01 passes on the real migrations
</success_criteria>

<output>
Create `.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-02-SUMMARY.md` when done
</output>
