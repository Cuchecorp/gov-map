---
phase: 09-completitud-de-identidad-backfill-rut-invariante-de-writer-p
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/identity/src/backfill-rut.ts
  - packages/identity/src/backfill-rut.test.ts
  - packages/identity/src/writer-supabase.ts
  - packages/identity/src/index.ts
  - supabase/seeds/parlamentario-rut.seed.json
  - packages/adjudication/src/golden/golden-set.ts
  - packages/adjudication/src/golden/golden-set.test.ts
  - packages/identity/src/deterministic.test.ts
autonomous: false
requirements: [IDENT-10, IDENT-11]

must_haves:
  truths:
    - "El rut de la maestra queda completado (backfill) server-side, solo filas con DV módulo-11 válido"
    - "Ningún RUT se fabrica: un RUT que falla isRutValido se rechaza a un log de revisión, nunca se escribe"
    - "Cada RUT backfilleado lleva provenance (origen/fecha_captura/enlace)"
    - "El seed snapshot git se re-exporta: no queda rut: null que la DB sí tenga poblado"
    - "El golden set se extiende con DV inválido/homónimo/persona natural-vs-jurídica/colisión de RUT; el gate CI ≥0.95 sigue bloqueando"
  artifacts:
    - path: "packages/identity/src/backfill-rut.ts"
      provides: "Lógica de backfill: lee fuente RUT (Track A o curada), DV-valida con isRutValido, escribe solo válidos"
      contains: "isRutValido"
    - path: "supabase/seeds/parlamentario-rut.seed.json"
      provides: "Lista curada server-side de RUTs por id con provenance (Track B, fallback garantizado)"
    - path: "packages/adjudication/src/golden/golden-set.ts"
      provides: "Casos golden de DV/homónimo/persona-jurídica/colisión de RUT"
  key_links:
    - from: "packages/identity/src/backfill-rut.ts"
      to: "isRutValido (@obs/identity)"
      via: "DV módulo-11 antes de escribir"
      pattern: "isRutValido"
    - from: "packages/identity/src/backfill-rut.ts"
      to: "SupabaseMaestraWriter.updateRut"
      via: "update de rut por id en filas existentes"
      pattern: "updateRut"
---

<objective>
Completar (backfill) el `rut` interno de la maestra de parlamentarios (IDENT-10) y extender el golden set de reconciliación con casos de RUT (IDENT-11). HALLAZGO DECISIVO del research (verificado en vivo 2026-06-18): NINGÚN catálogo oficial del Congreso expone el RUT (Senado `senadores_vigentes.php` no tiene el elemento; Cámara `WSDiputado` lo tiene pero VACÍO). Por eso se planean DOS tracks: Track A = spike SERVEL/electoral acotado (intentar primero, frágil); Track B = lista curada server-side con provenance por fila (fallback GARANTIZADO). NUNCA se fabrica un RUT: cada uno se DV-valida con el `isRutValido` (módulo-11) ya existente en `@obs/identity` antes de escribir.

Purpose: El cruce determinista por RUT que dependen las fuentes de dinero/probidad (Phases 12/14/15) está dormido porque `rut` es NULL en las 186 filas. Esta fase lo enciende, internamente y nunca expuesto a `anon`.

Output: `backfill-rut.ts` (DV-valida + escribe solo válidos + provenance); `SupabaseMaestraWriter.updateRut`; `parlamentario-rut.seed.json` curado (Track B); seed snapshot re-exportado; golden set extendido con casos de RUT; gate CI ≥0.95 intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md

<interfaces>
<!-- Contratos reales del v1.0. Usar directamente — no explorar. -->

From packages/identity/src/deterministic.ts (REUSAR — no reimplementar):
```typescript
export function normRut(rut: string): string;       // strip puntos/espacios/guión, casefold DV
export function isRutValido(rut: string): boolean;   // DV módulo-11; false → tratar como "sin RUT"
```

From packages/identity/src/writer-supabase.ts (patrón a espejar para updateRut):
```typescript
export class SupabaseMaestraWriter implements MaestraWriter {
  async upsert(rows: Parlamentario[]): Promise<void>;             // upsert por PK id (onConflict: "id")
  async promoteToConfirmado(ids: string[]): Promise<{promovidos:number}>; // chunked .update().in("id", lote)
}
// PROMOTE_CHUNK = 100; chunk<T>(arr, size) helper existe en el archivo.
```

From packages/identity/src/backup.ts (re-export del seed snapshot git, ID-09):
```typescript
export { exportMaestra, serializeMaestra, SEED_PATH };
// SEED_PATH apunta al snapshot git autoritativo de la maestra.
```

From supabase/migrations/0005_parlamentario.sql (provenance OBLIGATORIA en la maestra):
```sql
rut text,                                  -- NULLABLE, USO INTERNO
origen text not null,
fecha_captura timestamptz not null default now(),
enlace text not null
```

Track B — forma de cada fila curada en supabase/seeds/parlamentario-rut.seed.json:
```json
{ "id": "S123", "rut": "12.345.678-9", "origen": "infoprobidad-declaracion", "fecha_captura": "2026-06-18T00:00:00Z", "enlace": "https://...fuente-oficial..." }
```
</interfaces>

<reuse_policy_track_a>
Si se persigue Track A (fetch remoto SERVEL), REUSAR la política de @obs/ingest tal cual lo hace el seeder (RESEARCH §Backfill plumbing):
assertAllowedUrl (SSRF) → robots.isAllowed → rateLimiter.wait(host) (2-3s) → fetcher.get.
NO usar BaseConnector.run (su caché diaria saltaría una re-corrida del mismo día). Agregar `servel.cl` al allowlist solo si Track A se persigue. Un RUT SERVEL matcheado por nombre es un CANDIDATO, no un hecho: solo cuenta como determinista contra el propio RUT de la fila maestra (Anti-Pattern: auto-confirmar por nombre).
</reuse_policy_track_a>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backfill-rut.ts (DV-valida + provenance + updateRut) — Track B curado como camino garantizado</name>
  <files>packages/identity/src/backfill-rut.ts, packages/identity/src/backfill-rut.test.ts, packages/identity/src/writer-supabase.ts, packages/identity/src/index.ts, supabase/seeds/parlamentario-rut.seed.json</files>
  <read_first>
    - packages/identity/src/deterministic.ts (isRutValido, normRut — REUSAR, no reimplementar el módulo-11)
    - packages/identity/src/writer-supabase.ts (espejar promoteToConfirmado para un método updateRut chunked por id)
    - packages/identity/src/seeder.ts (patrón de fetch reusando @obs/ingest, si Track A; y el estilo del paquete)
    - supabase/seeds/parlamentario.seed.json (186 filas, campo rut presente pero null para todas — leer 1-2 filas para la forma)
    - supabase/migrations/0005_parlamentario.sql (rut nullable + provenance NOT NULL)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§RUT Source Resolution: Track A/B, Hard rules, Pitfall 1)
  </read_first>
  <behavior>
    - Test: una fila curada con RUT de DV válido → se acepta y se devuelve normalizada (normRut) para escribir.
    - Test: una fila con RUT de DV INVÁLIDO → se rechaza a un log/lista de revisión y NO se incluye en las escrituras (nunca se fabrica/escribe).
    - Test: cada fila aceptada conserva provenance (origen, fecha_captura, enlace) — una fila sin provenance se rechaza (0005 exige NOT NULL).
    - Test: el backfill es idempotente — correr 2× con la misma lista produce las mismas escrituras por id (update por id es no-op la 2.ª vez).
    - Test (updateRut, con cliente Supabase mock/espía): actualiza solo `rut`+provenance de las filas cuyos `id` se pasan, en lotes (espeja PROMOTE_CHUNK).
  </behavior>
  <action>Crear `packages/identity/src/backfill-rut.ts` (per IDENT-10): una función pura `aceptarRutBackfill(filaCruda)` que (a) rechaza si falta provenance, (b) llama `isRutValido` (REUSAR de @obs/identity — RESEARCH "Don't Hand-Roll"), (c) si inválido devuelve al log de revisión sin escribir (NUNCA fabricar — Hard rules), (d) si válido devuelve la fila con `normRut` aplicado. Una función orquestadora `runBackfillRut(filas, writer)` que filtra válidas y llama `writer.updateRut(...)`, devolviendo `{ escritas, rechazadas }`. Agregar `updateRut(rows: {id, rut, origen, fecha_captura, enlace}[])` a `SupabaseMaestraWriter` espejando el patrón chunked `.update({...}).in("id", lote).select("id")` de `promoteToConfirmado`. Exportar `runBackfillRut`/`aceptarRutBackfill` desde el barrel `index.ts`. Crear `supabase/seeds/parlamentario-rut.seed.json` como Track B (RESEARCH dice que es el fallback GARANTIZADO y el default seguro): poblarlo con las filas curadas disponibles, cada una con `id` (matcheando un id de la maestra), `rut` y provenance por fila; si no se dispone de RUTs reales curados en este entorno, dejarlo como un arreglo vacío `[]` con un comentario de cabecera explicando que se puebla manualmente con RUTs DV-válidos + provenance (NUNCA placeholders fabricados). Track A (fetch SERVEL) es opcional y queda fuera de esta task salvo que el operador confirme disponibilidad (ver Task 2 checkpoint). NO usar v1/simplificación: el camino completo (DV-gate + provenance + updateRut + idempotencia) es el entregable.</action>
  <verify>
    <automated>pnpm --filter @obs/identity test && pnpm --filter @obs/identity typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `backfill-rut.ts` rechaza todo RUT con DV inválido (vía `isRutValido`) a un log de revisión; nunca lo escribe.
    - `SupabaseMaestraWriter.updateRut` actualiza solo `rut`+provenance por `id`, en lotes, idempotente.
    - `parlamentario-rut.seed.json` existe (poblado con filas DV-válidas + provenance, o `[]` documentado — nunca RUTs fabricados).
    - Tests verdes y cubren: DV válido/inválido, provenance faltante, idempotencia.
  </acceptance_criteria>
  <done>La lógica de backfill DV-valida cada RUT, exige provenance, escribe solo válidos por id de forma idempotente, y nunca fabrica un RUT; Track B (lista curada) es el camino garantizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender el golden set con casos de RUT (DV/homónimo/persona-jurídica/colisión) — gate ≥0.95 intacto</name>
  <files>packages/adjudication/src/golden/golden-set.ts, packages/adjudication/src/golden/golden-set.test.ts, packages/identity/src/deterministic.test.ts</files>
  <read_first>
    - packages/adjudication/src/golden/golden-set.ts (estructura CasoGolden, helper p(), GOLDEN_SET, GOLDEN_SET_GATE, IDS_CASOS_ADVERSARIOS — líneas ~36-80 y ~453-481)
    - packages/adjudication/src/golden/golden-set.test.ts (cómo corre el gate ≥0.95 y la meta-prueba "el gate puede fallar")
    - packages/identity/src/deterministic.ts (isRutValido — para los casos de DV en deterministic.test.ts)
    - packages/identity/src/deterministic.test.ts (casos existentes de isRutValido a extender)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (Pitfall 5: mantener el umbral significativo; al menos un caso de colisión DURO)
  </read_first>
  <behavior>
    - Test (deterministic.test.ts): `isRutValido` rechaza un DV inválido y acepta uno válido para al menos un caso de persona natural y uno de persona jurídica (RUT de empresa, cuerpo 7-8 dígitos válido).
    - Test (golden): se agregan a `GOLDEN_SET` casos de homónimo con colisión de RUT (homónimo cuyo RUT DV-valida pero pertenece a otra persona/empresa) etiquetados al `expected` correcto; el gate corre sobre `GOLDEN_SET_GATE` (adversarios excluidos) y la precisión sigue ≥0.95.
    - Test: la meta-prueba "el gate puede fallar" (sobre `GOLDEN_SET_ADVERSARIO`) sigue verde (la métrica sigue VIVA, no teatro).
  </behavior>
  <action>Extender `packages/adjudication/src/golden/golden-set.ts` (per IDENT-11) agregando a `GOLDEN_SET` casos de RUT propios de SERVEL/ChileCompra: (1) homónimo con colisión de RUT (dos personas que comparten nombre normalizado, una con RUT que DV-valida pero es de otra → debe ir a revisión/no_confirmado, no auto-aceptar); (2) persona natural vs jurídica (un RUT de empresa que no debe colapsar en atribución personal); (3) al menos un caso DURO de colisión (Pitfall 5: que el umbral siga significativo, no inflado). Etiquetar cada caso con su `expected` correcto usando el helper `p()` y la estructura `CasoGolden` existente. NO tocar el umbral ≥0.95 ni mover casos al set de adversarios. En `packages/identity/src/deterministic.test.ts`, agregar casos de `isRutValido` para DV inválido y persona-jurídica (RUT de empresa válido). El gate de CI debe seguir bloqueando bajo 0.95.</action>
  <verify>
    <automated>pnpm --filter @obs/adjudication test && pnpm --filter @obs/identity test</automated>
  </verify>
  <acceptance_criteria>
    - `GOLDEN_SET` incluye nuevos casos de DV/homónimo/persona-jurídica/colisión de RUT, etiquetados al outcome correcto.
    - El gate corre sobre `GOLDEN_SET_GATE` y la precisión sigue ≥0.95 (umbral SIN cambiar).
    - La meta-prueba "el gate puede fallar" sigue verde.
    - `isRutValido` tiene cobertura nueva de DV inválido + persona-jurídica en `deterministic.test.ts`.
  </acceptance_criteria>
  <done>El golden set cubre los casos de RUT de SERVEL/ChileCompra (DV, homónimo, persona natural/jurídica, colisión), el gate ≥0.95 sigue bloqueando, y la métrica sigue viva.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aplicar el backfill (Track A spike o Track B curado) + re-exportar el seed snapshot</name>
  <read_first>
    - packages/identity/src/backfill-rut.ts (lo que el operador correrá)
    - packages/identity/src/backup.ts (exportMaestra/serializeMaestra/SEED_PATH — re-export del snapshot git)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§Runtime State Inventory "Critical dual-write reminder"; Pitfall 3; Open Question 1; Environment Availability)
    - .planning/STATE.md (deuda v1.0: aplicar migraciones al local es blocker arrastrado; remote db push via pooler funciona)
  </read_first>
  <what-built>La lógica de backfill (DV-gate + updateRut + provenance) y la lista curada Track B. Esta task es el paso de OPERADOR que (a) decide Track A vs Track B, (b) aplica el backfill a la DB, y (c) re-exporta el seed snapshot git. Es checkpoint humano porque: aplicar escrituras a la DB y/o probar la alcanzabilidad live de SERVEL son no-deterministas y el environment puede no tener una DB aplicada (RESEARCH: build/typecheck NO prueban estado de la DB — falso positivo).</what-built>
  <how-to-verify>
    1. Decidir el track: intentar Track A (spike SERVEL acotado) SOLO si `servel.cl` es alcanzable y entrega RUTs limpios cruzables por `(nombre_normalizado, periodo)`+región; si no, usar Track B (la lista curada `parlamentario-rut.seed.json`). NUNCA fabricar un RUT.
    2. Poblar `supabase/seeds/parlamentario-rut.seed.json` con los RUTs obtenidos (DV-válidos + provenance por fila) si aún está vacío.
    3. Correr el backfill contra el Supabase LOCAL (URL 544xx + service key local del `.env`): `runBackfillRut` → `updateRut`. Confirmar el conteo `{ escritas, rechazadas }`; toda rechazada debe ser por DV inválido o provenance faltante (esperado), nunca un RUT fabricado escrito.
    4. Re-exportar el seed snapshot git (`exportMaestra`/`serializeMaestra` → `SEED_PATH`) para que git quede autoritativo (ID-09). DUAL-WRITE OBLIGATORIO (Pitfall 3): la DB y el seed deben quedar consistentes.
    5. Aserción de paridad: `grep -c '"rut": null' supabase/seeds/parlamentario.seed.json` (o el snapshot exportado) debe reflejar el conteo esperado de filas sin RUT (las no curadas), y el conteo de `rut` no-null del seed debe igualar el conteo en la DB.
  </how-to-verify>
  <resume-signal>Escribe "approved" con el conteo {escritas, rechazadas} y el track usado, o describe el blocker (SERVEL inalcanzable / DB local no aplicada).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente RUT externa (SERVEL) → maestra | Dato no confiable cruza hacia el `rut` interno; debe DV-validarse y matchearse solo determinísticamente |
| maestra.rut → anon | El RUT es PII interna; anon NUNCA debe leerlo (garantizado por RLS deny-by-default, verificado en plan 09-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | RUT fabricado/adivinado | mitigate | `isRutValido` (módulo-11) gate antes de escribir; un RUT inválido va al log de revisión, nunca a la DB (Task 1) |
| T-09-05 | Spoofing | RUT SERVEL matcheado por nombre | mitigate | Un RUT candidato por nombre NO es un hecho; solo determinista contra el RUT propio de la fila; golden set cubre colisión/homónimo (Task 2) |
| T-09-06 | Repudiation/Integrity | backfill parcial presentado como completo | mitigate | provenance por fila; dual-write DB+seed con aserción de paridad (Task 3, Pitfall 3) |
| T-09-07 | Information Disclosure | RUT legible por anon | accept (este plan) / mitigate (09-03) | RLS deny-by-default de `parlamentario.rut` ya existe (0005); 09-03 lo re-asegura por pgTAP |
</threat_model>

<verification>
- `pnpm --filter @obs/identity test && pnpm --filter @obs/adjudication test` verdes.
- Golden gate: la precisión sobre `GOLDEN_SET_GATE` es ≥0.95 (umbral sin cambiar) y la meta-prueba "el gate puede fallar" pasa.
- Nunca-fabricar: el test de Track 1 demuestra que un DV inválido NO se escribe.
- Dual-write (Task 3, operador): conteo de `rut` no-null del seed == conteo en la DB; ninguna fila con RUT fabricado.
</verification>

<success_criteria>
El `rut` de la maestra queda completado (backfill) server-side, de uso interno, nunca legible por `anon`, habilitando el cruce por RUT de dinero/probidad — con cada RUT DV-validado (módulo-11) y con provenance, NUNCA fabricado. El golden set se extiende con casos de homónimos/colisión de RUT/persona natural-vs-jurídica/DV inválido y el gate CI ≥0.95 sigue bloqueando. El seed snapshot git queda consistente con la DB.
</success_criteria>

<output>
Create `.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-02-SUMMARY.md` when done
</output>
