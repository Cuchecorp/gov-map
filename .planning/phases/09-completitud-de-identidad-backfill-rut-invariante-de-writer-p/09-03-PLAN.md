---
phase: 09-completitud-de-identidad-backfill-rut-invariante-de-writer-p
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - supabase/migrations/0018_piso_pii.sql
  - supabase/tests/0018_piso_pii.test.sql
  - packages/llm/src/data-routing.ts
  - packages/llm/src/data-routing.test.ts
autonomous: false
requirements: [LEGAL-03]

must_haves:
  truths:
    - "Existe una convención de migración deny-by-default reutilizable para toda PII nueva (espejo de 0005: RLS enabled, cero policies, sin GRANT a anon)"
    - "pgTAP prueba que la PII nueva (y parlamentario.rut) son anon-denied: RLS habilitada + cero policies"
    - "La compuerta data-routing del LLM cubre la PII nueva: ningún RUT/PII puede llegar al LLM (reusa assertNoRutInLlmInput/assertSensitivityAllowed, no duplica)"
  artifacts:
    - path: "supabase/migrations/0018_piso_pii.sql"
      provides: "Convención reutilizable RLS deny-by-default para PII nueva (espejo de 0005)"
      contains: "enable row level security"
    - path: "supabase/tests/0018_piso_pii.test.sql"
      provides: "pgTAP: RLS enabled + cero policies para la PII nueva y re-asegura parlamentario.rut"
      contains: "is_empty"
    - path: "packages/llm/src/data-routing.ts"
      provides: "Gate data-routing extendido a la nueva PII sensible"
  key_links:
    - from: "supabase/tests/0018_piso_pii.test.sql"
      to: "pg_policy / pg_class.relrowsecurity"
      via: "aserción RLS enabled + cero policies"
      pattern: "relrowsecurity"
    - from: "packages/llm/src/data-routing.ts"
      to: "SensitiveRoutingError / RutInLlmInputError"
      via: "reusa los asserts fail-closed existentes"
      pattern: "assertNoRutInLlmInput|assertSensitivityAllowed"
---

<objective>
Dejar el PISO de RLS/data-routing para TODA PII nueva de v2.0 (LEGAL-03), ANTES de que escriba el primer dataset de atribución. Dos entregables: (1) una convención de migración deny-by-default reutilizable (espejo exacto de `0005_parlamentario.sql`: RLS habilitada, CERO policies, sin GRANT a anon) + pgTAP que la prueba (espejo de `0004_parlamentario.test.sql`); (2) extender la compuerta `data-routing` del LLM (`assertNoRutInLlmInput`/`assertSensitivityAllowed`) para que ningún RUT/PII nuevo pueda llegar a un LLM — REUSANDO los asserts existentes, sin duplicar.

Purpose: Es el piso de seguridad/privacidad (Ley 21.719, minimización) que Phases 11/12/14/15 heredan copy-paste-safe. RUT y datos personales nuevos nacen ocultos a `anon` y nunca cruzan a un LLM que entrena con inputs.

Output: `0018_piso_pii.sql` (convención RLS deny-by-default); `0018_piso_pii.test.sql` (pgTAP, corre contra una migración APLICADA); `data-routing.ts` extendido a la nueva PII. La APLICACIÓN del DDL + corrida pgTAP es un checkpoint de operador (build/typecheck NO prueban que el DDL se aplicó — falso positivo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md

<interfaces>
<!-- Patrones reales del v1.0 a espejar. Usar directamente — no explorar. -->

From supabase/migrations/0005_parlamentario.sql (el patrón deny-by-default a convertir en convención):
```sql
-- RLS deny-by-default (Ley 21.719, V4/V8): enable SIN policies en ambas tablas.
-- anon nunca lee la maestra (ni rut ni email); solo service role.
alter table parlamentario enable row level security;
alter table parlamentario_alias enable row level security;
-- (intencionalmente NINGÚN `create policy ... to anon`; NINGÚN `grant select to anon`)
```

From supabase/tests/0004_parlamentario.test.sql (el patrón pgTAP a espejar):
```sql
-- RLS habilitada
select is(
  (select count(*)::int from pg_class
     where relname in ('parlamentario','parlamentario_alias') and relrowsecurity = true),
  2, 'RLS enabled');
-- cero policies → deny-by-default efectivo
select is_empty(
  $$ select polname from pg_policy p join pg_class c on c.oid = p.polrelid
     where c.relname in ('parlamentario','parlamentario_alias') $$,
  'ninguna policy definida (deny-by-default)');
```

From packages/llm/src/data-routing.ts (REUSAR, no duplicar):
```typescript
export class RutInLlmInputError extends Error {}
export function assertNoRutInLlmInput(text: string): void;   // throws si hay un RUT (regex amplio fail-closed)
export function assertSensitivityAllowed(
  task: { sensitivity: Sensitivity },
  provider: { id: string; trainsOnInputs: boolean },
): void;  // throws si dato personal va a un provider que entrena con inputs (SensitiveRoutingError)
```

From packages/llm/src/types.ts:
```typescript
export type Sensitivity = "public" | "personal";  // (verificar el set exacto al leer el archivo)
```
</interfaces>

<ddl_application_caveat>
RESEARCH Pitfall 4 + STATE.md: build/typecheck NO prueban que una migración se aplicó (CI verde confla "compila" con "schema cambió"). Por eso AUTORÍA del DDL (Task 1, autónoma) y APLICACIÓN + pgTAP (Task 3, checkpoint de operador) son tasks SEPARADAS. Matiz del environment (project memory 2026-06-18): el remote `supabase db push --db-url` vía el pooler sa-east-1 del `.env` SÍ aplicó migraciones 0012–0015; el Management API PAT es lo que falta, no el push por DB password. La aplicación LOCAL es el blocker arrastrado de v1.0 (0011 no aplicado al local). El plan autoriza el archivo y gatea la aplicación+pgTAP como prueba humana.

Nota de diseño (RESEARCH §RLS migration convention): la postura del proyecto (y 0005) es mantener la PII en una TABLA deny-by-default, NO una columna PII en una tabla pública; las filas públicas llevan solo el FK. Esta migración 0018 codifica la CONVENCIÓN (no necesariamente crea una tabla PII con datos hoy): puede materializarse como una tabla-plantilla/exemplar deny-by-default + las aserciones pgTAP que cualquier PII futura debe satisfacer, y re-aseverar que `parlamentario.rut` sigue oculto a anon tras el backfill de 09-02.
</ddl_application_caveat>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0018 — convención RLS deny-by-default para PII nueva (espejo de 0005)</name>
  <files>supabase/migrations/0018_piso_pii.sql</files>
  <read_first>
    - supabase/migrations/0005_parlamentario.sql (el patrón exacto a espejar: RLS enable SIN policies, sin GRANT a anon, provenance NOT NULL)
    - supabase/migrations/0017_higiene_seguridad.sql (la última migración numerada; estilo y numeración 0018)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§Code Examples "RLS deny-by-default migration convention"; nota: PII en tabla deny-by-default, no columna en tabla pública)
  </read_first>
  <action>Crear `supabase/migrations/0018_piso_pii.sql` (per LEGAL-03) que codifique la CONVENCIÓN deny-by-default reutilizable, espejo exacto de `0005`. Documentar en cabecera (español) que: toda tabla con PII nace con `enable row level security` y CERO policies + sin `grant select to anon` (anon nunca lee), provenance inline NOT NULL (origen/fecha_captura/enlace), y que las filas PÚBLICAS llevan solo el FK `parlamentario_id` (nunca el RUT). Materializar la convención como una tabla-exemplar deny-by-default que sirva de plantilla para Phases 11/12/14/15 (p.ej. una tabla de PII de contraparte/declaración mínima con su provenance y RLS habilitada sin policies), siguiendo literalmente la forma de 0005. NO crear policies. NO otorgar GRANT a anon. NO simplificar: la migración debe ser aplicable y dejar el patrón listo para copy-paste. (La APLICACIÓN del DDL NO se hace aquí — es Task 3.)</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0018_piso_pii.sql','utf8'); const body=s.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n'); if(!/enable row level security/i.test(body)) throw new Error('falta enable RLS'); if(/grant\s+select.*to\s+anon/i.test(body)) throw new Error('GRANT a anon prohibido'); if(/create\s+policy/i.test(body)) throw new Error('deny-by-default: sin policies'); console.log('0018 convención OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `0018_piso_pii.sql` existe, habilita RLS, NO crea policies, NO otorga GRANT a anon.
    - Lleva provenance NOT NULL espejando 0005.
    - El check automatizado (sin contar comentarios) confirma RLS enabled + cero policies + sin GRANT a anon.
  </acceptance_criteria>
  <done>La convención de migración deny-by-default para PII nueva existe como archivo numerado 0018, lista para que Phases 11/12/14/15 la copien; sin policies, sin GRANT a anon.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pgTAP 0018 + extensión del gate data-routing del LLM a la PII nueva</name>
  <files>supabase/tests/0018_piso_pii.test.sql, packages/llm/src/data-routing.ts, packages/llm/src/data-routing.test.ts</files>
  <read_first>
    - supabase/tests/0004_parlamentario.test.sql (patrón pgTAP "RLS enabled + cero policies"; líneas ~70-85)
    - packages/llm/src/data-routing.ts (assertNoRutInLlmInput + assertSensitivityAllowed — REUSAR, no duplicar)
    - packages/llm/src/data-routing.test.ts (casos existentes a extender)
    - packages/llm/src/types.ts (el tipo Sensitivity exacto)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§Code Examples "Extending the data-routing gate"; "no duplication")
  </read_first>
  <behavior>
    - pgTAP: la(s) tabla(s) PII de 0018 tienen `relrowsecurity = true` y CERO filas en `pg_policy` (deny-by-default).
    - pgTAP: re-aseverar que `parlamentario` sigue con RLS enabled + cero policies tras el backfill de 09-02 (parlamentario.rut sigue oculto a anon).
    - Test TS: un prompt que contiene un RUT lanza `RutInLlmInputError` (vía `assertNoRutInLlmInput`) — incluyendo el contexto de un documento PII nuevo (texto de declaración).
    - Test TS: dato `personal` enrutado a un provider con `trainsOnInputs: true` lanza `SensitiveRoutingError` (vía `assertSensitivityAllowed`); a un provider `trainsOnInputs: false` no lanza.
  </behavior>
  <action>Crear `supabase/tests/0018_piso_pii.test.sql` (per LEGAL-03) espejando `0004`: aserciones `is(... relrowsecurity = true ...)` y `is_empty(... pg_policy ...)` para la(s) tabla(s) PII de 0018, MÁS una re-aserción de que `parlamentario` (rut) sigue RLS-enabled + cero policies tras el backfill. En `packages/llm/src/data-routing.ts`, extender la compuerta a la PII nueva REUSANDO `assertNoRutInLlmInput`/`assertSensitivityAllowed` (RESEARCH "Don't Hand-Roll": no duplicar el regex ni el gate) — p.ej. agregar/ajustar el valor de `Sensitivity` o un helper delgado que aplique AMBOS asserts antes de cualquier `complete()` sobre un documento PII nuevo, sin reimplementar la lógica. Extender `data-routing.test.ts` con los casos de comportamiento de arriba. La APLICACIÓN del pgTAP contra una DB aplicada es Task 3 (no se corre aquí en CI sin DB).</action>
  <verify>
    <automated>pnpm --filter @obs/llm test && pnpm --filter @obs/llm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `0018_piso_pii.test.sql` existe y asevera RLS enabled + cero policies para la PII nueva y re-asevera `parlamentario`.
    - `data-routing.ts` cubre la PII nueva reusando los asserts existentes (sin duplicar el regex/gate).
    - Tests TS verdes: RUT en prompt → throw; personal→provider-que-entrena → throw; personal→provider-seguro → no throw.
  </acceptance_criteria>
  <done>El pgTAP de la PII nueva existe (corre contra una DB aplicada en Task 3), y el gate data-routing rechaza RUT/PII al LLM reusando los asserts fail-closed existentes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aplicar la migración 0018 + correr el pgTAP contra la DB aplicada</name>
  <read_first>
    - supabase/migrations/0018_piso_pii.sql (lo que el operador aplica)
    - supabase/tests/0018_piso_pii.test.sql (lo que el operador corre tras aplicar)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (Pitfall 4: build/typecheck NO prueban DDL aplicado; matiz del pooler remoto)
    - .planning/STATE.md (deuda v1.0: aplicar migraciones al local es blocker arrastrado; remote db push via pooler funciona)
  </read_first>
  <what-built>La migración 0018 (convención RLS deny-by-default) y su pgTAP. Esta task es el paso de OPERADOR que APLICA el DDL y corre el pgTAP contra la DB aplicada. Es checkpoint humano OBLIGATORIO porque build/typecheck NO prueban que Postgres ejecutó la migración (falso positivo, RESEARCH Pitfall 4): la única prueba válida de LEGAL-03 es el pgTAP corriendo contra un schema aplicado.</what-built>
  <how-to-verify>
    1. Aplicar 0018 a la DB objetivo. LOCAL: aplicar la migración al Supabase local (docker, blocker arrastrado de v1.0 — puede requerir resolver primero la aplicación de 0011). REMOTO (si se usa): `supabase db push --db-url "$SUPABASE_DB_URL" --include-all` vía el pooler sa-east-1 del `.env` (project memory: aplicó 0012–0015 OK; re-verificar que la DB password no rotó — deuda v1.0 #1).
    2. Correr el pgTAP: `supabase test db` (o el harness del proyecto) e incluir `0018_piso_pii.test.sql`. Confirmar que TODAS las aserciones pasan: RLS enabled + cero policies para la PII nueva, y `parlamentario` sigue anon-denied tras el backfill de 09-02.
    3. Confirmar con una consulta como rol `anon` que NO puede leer la(s) tabla(s) PII ni `parlamentario.rut` (deny-by-default efectivo).
  </how-to-verify>
  <resume-signal>Escribe "approved" con el resultado del pgTAP (plan/passed) y el target aplicado (local/remoto), o describe el blocker (DB local no aplicable / password rotada).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → tabla PII | Cliente no autenticado intenta leer PII (RUT, datos de declaración); debe ser denegado por RLS |
| extractor/conector → LLM | Texto que puede contener RUT/PII cruza hacia un provider de terceros; debe ser bloqueado si hay RUT o si entrena con inputs |
| archivo de migración → schema Postgres | El DDL existe como archivo pero puede no estar aplicado (falso positivo de CI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | tabla PII nueva sin RLS | mitigate | Convención deny-by-default (RLS enabled, cero policies, sin GRANT a anon) en 0018; pgTAP lo prueba (Task 1+2+3) |
| T-09-09 | Information Disclosure | RUT/PII egresando a un LLM | mitigate | `assertNoRutInLlmInput` (regex fail-closed) + `assertSensitivityAllowed` antes de todo `complete()`, extendidos a la PII nueva (Task 2) |
| T-09-10 | Repudiation | DDL "aplicado" como falso positivo de CI | mitigate | Autoría (Task 1) y aplicación+pgTAP (Task 3) separadas; pgTAP contra DB aplicada es la única prueba; checkpoint de operador (Pitfall 4) |
| T-09-11 | Tampering | rotación de DB password bloquea el push remoto | accept | Si la password rotó (deuda v1.0 #1), la aplicación revierte a paso de operador local; ya gateado como human-verify |
</threat_model>

<verification>
- `pnpm --filter @obs/llm test && pnpm --filter @obs/llm typecheck` verdes.
- Convención DDL (sin contar comentarios): `0018` habilita RLS, no crea policies, no otorga GRANT a anon (check automatizado de Task 1).
- pgTAP (Task 3, operador, contra DB aplicada): todas las aserciones pasan; anon no lee la PII nueva ni `parlamentario.rut`.
- Data-routing: un RUT en un prompt PII nuevo lanza; dato personal a un provider que entrena con inputs lanza.
</verification>

<success_criteria>
Toda columna/tabla PII nueva nace oculta a `anon` por RLS (deny-by-default, espejo de `parlamentario.rut`) y la compuerta `data-routing` del LLM se extiende a los nuevos datos sensibles: ningún RUT/PII puede llegar al LLM (reusa `assertNoRutInLlmInput`/`assertSensitivityAllowed`, sin duplicar). El pgTAP corre contra una migración APLICADA (no solo autorada) y pasa.
</success_criteria>

<output>
Create `.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-03-SUMMARY.md` when done
</output>
