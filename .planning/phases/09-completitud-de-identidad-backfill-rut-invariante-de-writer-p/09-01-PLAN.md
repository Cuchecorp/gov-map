---
phase: 09-completitud-de-identidad-backfill-rut-invariante-de-writer-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/identity/src/enlace-confirmado.ts
  - packages/identity/src/enlace-confirmado.test.ts
  - packages/identity/src/enlace-confirmado.test-d.ts
  - packages/identity/src/index.ts
  - packages/tramitacion/src/reconciliar-senado.ts
  - packages/tramitacion/src/writer.ts
  - packages/tramitacion/src/reconciliar-senado.test.ts
autonomous: true
requirements: [IDENT-12]

must_haves:
  truths:
    - "Solo un match determinista/confirmado (o promoción humana) puede producir un EnlaceConfirmado"
    - "Un *Writer no puede fijar parlamentario_id desde un string crudo: es un error de compilación"
    - "probable/revision/no_confirmado dejan parlamentario_id NULL + mención cruda (guarda LOCKED v1.0 preservada)"
  artifacts:
    - path: "packages/identity/src/enlace-confirmado.ts"
      provides: "Tipo branded EnlaceConfirmado + factory única confirmar()"
      contains: "unique symbol"
      exports: ["EnlaceConfirmado", "confirmar"]
    - path: "packages/identity/src/enlace-confirmado.test-d.ts"
      provides: "Prueba de compilación: FK crudo NO compila, FK branded SÍ compila"
      contains: "@ts-expect-error"
    - path: "packages/tramitacion/src/reconciliar-senado.ts"
      provides: "Choke point único que mintea EnlaceConfirmado solo en la rama determinista"
      contains: "confirmar("
  key_links:
    - from: "packages/tramitacion/src/reconciliar-senado.ts"
      to: "packages/identity/src/enlace-confirmado.ts"
      via: "import { confirmar } from @obs/identity"
      pattern: "confirmar\\("
    - from: "packages/tramitacion/src/writer.ts"
      to: "EnlaceConfirmado"
      via: "firma del input del writer tipa el FK como EnlaceConfirmado | null"
      pattern: "EnlaceConfirmado"
---

<objective>
Convertir la guarda de enlace-confirmado de v1.0 (TRAM-06, hoy una CONVENCIÓN: `reconciliar-senado.ts` mapea `res.tipo` a `string | null` a mano) en un INVARIANTE TIPADO a nivel de writer (IDENT-12). Un tipo branded `EnlaceConfirmado` (con un `unique symbol` privado) minteado por una ÚNICA factory `confirmar()` que solo la reconciliación invoca tras un resultado `determinista`/`confirmado`. Los `*Writer` tipan su FK de entrada como `EnlaceConfirmado | null`, haciendo que un FK string crudo sea un ERROR DE COMPILACIÓN.

Purpose: Es el piso estructural que toda fuente de atribución nueva (lobby/probidad/dinero, Phases 11/12/14/15) reusa. Hoy un writer nuevo podría pasar cualquier string al FK y publicar un match equivocado como hecho (riesgo existencial #1). Después de esta fase eso no compila.

Output: `@obs/identity` exporta `EnlaceConfirmado` + `confirmar`; `reconciliar-senado.ts` es el único call site del factory; el voto writer tipa su input con el branded type; una prueba de compilación demuestra la imposibilidad estructural.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md

<interfaces>
<!-- Contratos reales del v1.0. El executor usa estos directamente — no explorar el codebase. -->

From packages/adjudication/src/pipeline.ts (el choke point ya produce el discriminado):
```typescript
export type ResultadoPipeline =
  | { tipo: "determinista"; parlamentarioId: string }
  | { tipo: "no_confirmado"; razon: string }
  | { tipo: "probable"; parlamentarioId: string }
  | { tipo: "revision"; razones: string[] };
```

From packages/tramitacion/src/model.ts (la forma PERSISTIDA del voto — NO cambia):
```typescript
export interface Voto {
  votacion_id: string;
  fuente_voter_id: string;
  mencion_nombre: string;
  parlamentario_id: string | null;   // forma DB persistida (se mantiene string | null)
  seleccion: Seleccion;
  metodo: MetodoVinculo | null;
  estado_vinculo: EstadoVinculo | null;
}
// VotoSchema (zod) ya valida parlamentario_id: z.string().nullable()
```

From packages/tramitacion/src/writer.ts (interfaz a tipar):
```typescript
export interface TramitacionWriter {
  upsertVotos(votos: Voto[]): Promise<void>;   // hoy recibe Voto (FK string crudo)
}
```

From packages/identity/src/index.ts (dónde se exporta el nuevo símbolo — barrel existente):
```typescript
export { matchDeterminista, normRut, isRutValido } from "./deterministic";
```

Diseño recomendado del branded type (RESEARCH §Pattern 1, NO inline el símbolo público):
- `declare const ENLACE_CONFIRMADO: unique symbol;` — PRIVADO al módulo, NUNCA se exporta.
- `interface EnlaceConfirmado { readonly parlamentarioId: string; readonly metodo: "determinista" | "humano"; readonly [ENLACE_CONFIRMADO]: true }`
- `function confirmar(parlamentarioId: string, metodo?: "determinista" | "humano"): EnlaceConfirmado` — ÚNICA factory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear el tipo branded EnlaceConfirmado + factory confirmar() en @obs/identity</name>
  <files>packages/identity/src/enlace-confirmado.ts, packages/identity/src/enlace-confirmado.test.ts, packages/identity/src/enlace-confirmado.test-d.ts, packages/identity/src/index.ts</files>
  <read_first>
    - packages/identity/src/index.ts (barrel donde se agrega el export, sigue el estilo de los exports existentes)
    - packages/identity/src/deterministic.ts (estilo de doc-comments y exports de @obs/identity)
    - packages/adjudication/src/pipeline.ts (ResultadoPipeline — el discriminado que el factory consumirá aguas arriba)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§Pattern 1 y Pitfall 2: el unique symbol NO se exporta; prohibido `as EnlaceConfirmado`)
  </read_first>
  <behavior>
    - Test runtime: `confirmar("P00042")` devuelve un objeto con `parlamentarioId === "P00042"` y `metodo === "determinista"` por defecto.
    - Test runtime: `confirmar("P00042", "humano").metodo === "humano"`.
    - Test de compilación (.test-d.ts): asignar un string crudo a una variable `EnlaceConfirmado` es `// @ts-expect-error`.
    - Test de compilación: `confirmar("P00042")` SÍ es asignable a `EnlaceConfirmado` (línea sin error).
    - Test de compilación: el `unique symbol` NO está disponible para construir el tipo desde fuera (no exportado del barrel).
  </behavior>
  <action>Crear `packages/identity/src/enlace-confirmado.ts` con el diseño de RESEARCH §Pattern 1 (per IDENT-12): un `declare const ENLACE_CONFIRMADO: unique symbol` PRIVADO al módulo (NUNCA exportado — Pitfall 2 dice que exportarlo o permitir `as EnlaceConfirmado` rompe el invariante), la interfaz `EnlaceConfirmado` con `parlamentarioId`, `metodo: "determinista" | "humano"` y la propiedad branded, y la ÚNICA factory `confirmar(parlamentarioId, metodo = "determinista")`. Documentar en español que es la prueba estructural de un match determinista/confirmado y que `confirmar` es el único constructor legítimo (la reconciliación tras determinista, y `revisor-cli` tras promoción humana — Open Question 2 del research). Agregar el export al barrel `packages/identity/src/index.ts` exportando SOLO el tipo `EnlaceConfirmado` y la función `confirmar` (no el símbolo). Escribir `enlace-confirmado.test.ts` (vitest, runtime) y `enlace-confirmado.test-d.ts` (prueba de tipo con `@ts-expect-error`). NO usar v1/placeholder; el factory es definitivo.</action>
  <verify>
    <automated>pnpm --filter @obs/identity test && pnpm --filter @obs/identity typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `enlace-confirmado.ts` exporta `EnlaceConfirmado` (tipo) y `confirmar` (factory); el `unique symbol` NO se exporta.
    - `@obs/identity` re-exporta ambos desde `index.ts`.
    - `enlace-confirmado.test-d.ts` contiene al menos un `@ts-expect-error` que falla si un string crudo se vuelve asignable a `EnlaceConfirmado`.
    - `pnpm --filter @obs/identity test` y `typecheck` pasan.
  </acceptance_criteria>
  <done>El branded type y su factory existen, están exportados (sin el símbolo), y la prueba de compilación demuestra que un string crudo no es un EnlaceConfirmado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor del choke point + writer del voto para tipar el FK con EnlaceConfirmado</name>
  <files>packages/tramitacion/src/reconciliar-senado.ts, packages/tramitacion/src/writer.ts, packages/tramitacion/src/reconciliar-senado.test.ts</files>
  <read_first>
    - packages/tramitacion/src/reconciliar-senado.ts (el switch `res.tipo` en líneas ~139-156: la rama `determinista` es el ÚNICO sitio donde se debe llamar `confirmar()`)
    - packages/tramitacion/src/writer.ts (interfaz TramitacionWriter + InMemoryTramitacionWriter; `upsertVotos`)
    - packages/tramitacion/src/model.ts (Voto + VotoSchema — la forma PERSISTIDA mantiene `parlamentario_id: string | null`)
    - packages/tramitacion/src/reconciliar-senado.test.ts (casos existentes a preservar: determinista→confirmado, probable/revision/no_confirmado→null)
    - .planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-RESEARCH.md (§Pattern 2 y §Pattern 3, Anti-Patterns A4: input branded, storage plano)
  </read_first>
  <behavior>
    - Test: cuando `correrPipeline` devuelve `{ tipo: "determinista", parlamentarioId }`, la reconciliación llama `confirmar(parlamentarioId, "determinista")` y el voto resultante tiene `parlamentario_id === parlamentarioId`, `estado_vinculo === "confirmado"`.
    - Test: `probable` → `parlamentario_id === null`, `estado_vinculo === "probable"`, `metodo === "llm"` (guarda LOCKED preservada).
    - Test: `revision` y `no_confirmado` → `parlamentario_id === null`, `estado_vinculo === "no_confirmado"`.
    - Test: `confirmar(` aparece ÚNICAMENTE en `reconciliar-senado.ts` (grep en el paquete tramitacion no lo encuentra en writer.ts ni en parsers).
  </behavior>
  <action>Refactorizar `reconciliar-senado.ts` per IDENT-12 (RESEARCH §Pattern 2): importar `confirmar` y `EnlaceConfirmado` de `@obs/identity`; en el switch sobre `res.tipo`, la rama `determinista` mintea `const enlace = confirmar(res.parlamentarioId, "determinista")` y las ramas `probable`/`revision`/`no_confirmado`/default dejan `enlace = null`. Mantener EXACTAMENTE la semántica de la guarda LOCKED v1.0 (probable nunca vincula la ficha pública). El `Voto` persistido sigue con `parlamentario_id: string | null` (Anti-Pattern A4: storage plano); internamente el voto se construye con `parlamentario_id: enlace?.parlamentarioId ?? null`. En `writer.ts`, introducir un tipo de ENTRADA del writer (p.ej. `VotoParaEscribir` que tipa el FK como `enlace: EnlaceConfirmado | null` + el resto de campos crudos) y hacer que `upsertVotos` lo acepte, derivando internamente `parlamentario_id = row.enlace?.parlamentarioId ?? null` antes de persistir/almacenar; actualizar `InMemoryTramitacionWriter` en consecuencia. Un writer futuro (Phase 11+) que intente `enlace: "P00042"` (string) NO compilará. Extender `reconciliar-senado.test.ts` con la aserción de comportamiento de cada rama. NO introducir un segundo factory ni un cast `as EnlaceConfirmado` (Pitfall 2).</action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test && pnpm --filter @obs/tramitacion typecheck && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `reconciliar-senado.ts` es el único archivo del workspace que llama `confirmar(` (verificable por grep; ver verification).
    - El input del writer del voto tipa el FK como `EnlaceConfirmado | null`, nunca `string` crudo.
    - Los 3 comportamientos (determinista→confirmado, probable→null/probable, revision|no_confirmado→null/no_confirmado) se preservan y están testeados.
    - `pnpm -r typecheck` pasa (todos los consumidores recompilan limpio).
  </acceptance_criteria>
  <done>El FK del voto solo se puede fijar a través de un EnlaceConfirmado minteado por la reconciliación; un string crudo en esa posición es un error de compilación; la guarda LOCKED v1.0 se conserva intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciliación → writer | El resultado del pipeline (confiable solo en la rama determinista) cruza hacia la fila pública persistida |
| writer futuro (Phase 11+) → modelo público | Código nuevo intenta fijar un FK de atribución; debe ser rechazado salvo enlace confirmado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering/Spoofing | `*Writer.parlamentario_id` | mitigate | Branded `EnlaceConfirmado` minteado solo tras `determinista`/`confirmado`; FK string crudo = error de compilación (Task 1+2) |
| T-09-02 | Tampering | factory `confirmar` | mitigate | `unique symbol` privado (no exportado); `confirmar` es el ÚNICO constructor; prohibido `as EnlaceConfirmado` (grep gate en verification) |
| T-09-03 | Repudiation | rama probable/revision del LLM | accept | Auto-aceptar del LLM NUNCA vincula la ficha pública (deja NULL + mención cruda); comportamiento LOCKED de v1.0 preservado |
</threat_model>

<verification>
- `pnpm -r typecheck` verde (el branded type recompila todos los consumidores).
- Choke-point único — el factory solo se llama en la reconciliación:
  `grep -rn --include=*.ts "confirmar(" packages/tramitacion/src packages/identity/src | grep -v "\.test" | grep -v "enlace-confirmado.ts"` debe devolver SOLO líneas de `reconciliar-senado.ts`.
- Sin escape hatch por cast:
  `grep -rn --include=*.ts "as EnlaceConfirmado" packages | grep -v "\.test"` debe devolver 0 líneas.
- La prueba de compilación (`enlace-confirmado.test-d.ts`) contiene `@ts-expect-error` sobre un FK string crudo.
</verification>

<success_criteria>
Existe un invariante tipado: ningún `*Writer` puede fijar un FK `parlamentario_id` salvo match `determinista`/`confirmado`; en caso contrario fija NULL + mención cruda + marca de identidad no verificada (rechazado ESTRUCTURALMENTE, no por convención). El factory `confirmar` es el único mint site, invocado solo desde la reconciliación. Prueba de compilación verde.
</success_criteria>

<output>
Create `.planning/phases/09-completitud-de-identidad-backfill-rut-invariante-de-writer-p/09-01-SUMMARY.md` when done
</output>
