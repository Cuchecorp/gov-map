---
phase: 02-capa-de-providers-llm-embeddings
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/llm/src/json-schema.ts
  - packages/llm/src/json-schema.test.ts
  - packages/llm/src/providers/minimax.ts
  - packages/llm/src/providers/minimax.test.ts
  - packages/llm/src/data-routing.ts
  - packages/llm/src/data-routing.test.ts
  - packages/llm/src/smoke.test.ts
autonomous: true
requirements: [FND-06]

must_haves:
  truths:
    - "MiniMaxProvider obtiene salida estructurada via tool-calling forzado (tool_choice fija una funcion unica) y lee tool_calls[0].function.arguments como string JSON, sin usar response_format"
    - "El JSON schema de la funcion tool se deriva del schema zod (z.toJSONSchema), una sola fuente de verdad"
    - "El adapter MiniMax pasa por la MISMA compuerta zod externa (parseAndValidate) que DeepSeek — validacion identica, independiente del modo del proveedor"
    - "La politica dato->proveedor esta en codigo: detectar RUT en un input destinado a un LLM lo rechaza (RUT nunca al LLM); marcar dato personal exige tier trainsOnInputs=false (fail-closed compartido con el router)"
    - "Un smoke test live por proveedor existe pero esta gated por env (LLM_SMOKE=1) y se salta por defecto (CI no lo corre)"
  artifacts:
    - path: "packages/llm/src/providers/minimax.ts"
      provides: "MiniMaxProvider (tool-calling forzado) implementando LLMProvider"
      contains: "tool_choice"
    - path: "packages/llm/src/json-schema.ts"
      provides: "zodToToolSchema(schema) via z.toJSONSchema (zod v4)"
      contains: "toJSONSchema"
    - path: "packages/llm/src/data-routing.ts"
      provides: "Politica RUT-nunca-LLM + PII-nunca-tier-que-entrena, documentada en codigo"
      contains: "RUT"
    - path: "packages/llm/src/smoke.test.ts"
      provides: "Smoke test live gated por LLM_SMOKE, skip por defecto"
      contains: "LLM_SMOKE"
  key_links:
    - from: "packages/llm/src/providers/minimax.ts"
      to: "packages/llm/src/validate.ts"
      via: "parseAndValidate como compuerta unica externa (igual que DeepSeek)"
      pattern: "parseAndValidate"
    - from: "packages/llm/src/providers/minimax.ts"
      to: "packages/llm/src/json-schema.ts"
      via: "JSON schema de la tool derivado del schema zod"
      pattern: "zodToToolSchema|toJSONSchema"
    - from: "packages/llm/src/data-routing.ts"
      to: "packages/llm/src/router.ts"
      via: "PII solo a provider trainsOnInputs=false (fail-closed compartido)"
      pattern: "trainsOnInputs"
---

<objective>
Segunda rebanada vertical LLM: agregar el provider de tier critico/sensible (MiniMax) con su modo de salida estructurada propio (tool-calling forzado, NO response_format) que el adapter DeepSeek no cubre, y la politica de datos en codigo (RUT nunca al LLM; PII solo a tier que no entrena). Cierra el caso "MiniMax sin response_format universal" sin que el caller note diferencia: misma interfaz, misma compuerta zod. Agrega el smoke test live gated por env (skip por defecto).

Esta rebanada RELLENA los stubs creados en 02-01 (`json-schema.ts`, `data-routing.ts`, `providers/minimax.ts`) — el barrel `index.ts` ya re-exporta esos modulos, asi que este plan NO toca `index.ts` (cero colision con la rebanada 03, que corre en paralelo).

Purpose: Completa FND-06 — la capa enchufable real con dos modos de structured output unificados tras una sola compuerta, y materializa el criterio "agregar un modelo nuevo = un adapter + config, cero cambios aguas arriba" (MiniMax se suma sin tocar router/validate). La politica de datos hace operable el fail-closed con un gate de cumplimiento documentado en codigo.
Output: MiniMaxProvider testeado via mock-fetch; helper zod->JSON-schema; modulo data-routing testeado; smoke test live gated.
</objective>

<phase_goal>
**As a** desarrollador consumidor (Fase 4 adjudicacion critica), **I want to** enrutar una tarea critica/sensible a MiniMax y recibir el mismo objeto validado que recibiria de cualquier otro provider, **so that** cambiar el modelo de un tier (o agregar uno nuevo) es un adapter + config sin tocar el dominio, y ningun dato personal cruza a un tier que entrena.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-CONTEXT.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-RESEARCH.md

<interfaces>
<!-- Contratos creados en 02-01 (este plan los consume; NO redefinir). El barrel index.ts ya re-exporta minimax/json-schema/data-routing (stubs). Solo rellenar los stubs. -->
De packages/llm/src/types.ts: `LLMProvider { id; trainsOnInputs; complete<T>(req, schema) }`, `CompletionRequest { system?; user; criticality; sensitivity; maxRepairAttempts? }`, `Criticality`, `Sensitivity`.
De packages/llm/src/validate.ts: `parseAndValidate<T>(schema, raw, { reprompt, maxAttempts })` — COMPUERTA UNICA; `LLMValidationError`.
De packages/llm/src/router.ts: `SensitiveRoutingError` (reusar en data-routing; NO duplicar).
De packages/llm/src/config.ts: minimax baseURL `https://api.minimax.io/v1`, model `MiniMax-M3`, trainsOnInputs false.
De packages/llm/test/_helpers.ts: `makeMockFetch(routes)` -> `{ fn, calls }`.
Stubs a rellenar (creados en 02-01, ya exportados por index.ts): `json-schema.ts:zodToToolSchema`, `data-routing.ts:{RutInLlmInputError, assertNoRutInLlmInput, assertSensitivityAllowed}`, `providers/minimax.ts:MiniMaxProvider`.

zod v4 (4.4.3): `z.toJSONSchema(schema)` deriva el JSON schema (nativo v4). NO `zod-to-json-schema`.

MiniMax (VERIFICADO en vivo, 02-CONTEXT.md): model id `MiniMax-M3`; baseURL OpenAI-compat `https://api.minimax.io/v1`; auth Bearer MINIMAX_API_KEY; structured output = tool-calling forzado:
```
tools: [{ type:"function", function:{ name:"emit_result", description:"...", parameters: <jsonSchemaFromZod> } }]
tool_choice: { type:"function", function:{ name:"emit_result" } }   // FUERZA la funcion
// respuesta: res.choices[0].message.tool_calls[0].function.arguments  (string JSON)
```
NO usa response_format. El content puede traer tags `<think>`; para una sola llamada de extraccion basta leer tool_calls.

openai SDK v6: `new OpenAI({ apiKey, baseURL, fetch: fetchFn })`; `client.chat.completions.create({...})`. Acepta fetch custom -> mock sin red.

Patron error sin secretos (fetcher.ts / LLMValidationError): nunca incluir prompt ni API key en mensajes/logs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper zod->JSON-schema + MiniMaxProvider (tool-calling forzado)</name>
  <files>packages/llm/src/json-schema.ts, packages/llm/src/json-schema.test.ts, packages/llm/src/providers/minimax.ts, packages/llm/src/providers/minimax.test.ts</files>
  <read_first>packages/llm/src/types.ts, packages/llm/src/validate.ts, packages/llm/src/config.ts, packages/llm/test/_helpers.ts, packages/llm/src/providers/deepseek.ts, packages/llm/src/json-schema.ts, packages/llm/src/providers/minimax.ts</read_first>
  <behavior>
    zodToToolSchema:
    - Test: dado un `z.object({ decision: z.enum(["match","no_match"]), confidence: z.number() })`, devuelve un JSON schema con `type:"object"`, `properties.decision`, `properties.confidence`, y `required` incluyendo ambos.
    MiniMaxProvider (mock-fetch, sin red):
    - Test: respuesta con `choices[0].message.tool_calls[0].function.arguments` = JSON valido -> `complete()` devuelve el objeto validado. Assert: el request a `https://api.minimax.io/v1/chat/completions` lleva `tool_choice:{type:"function",function:{name:"emit_result"}}`, un `tools[0].function.parameters` = el JSON schema derivado del zod, model `MiniMax-M3`, y NO lleva `response_format`.
    - Test: respuesta SIN tool_calls (o arguments ausente) -> entra al repair loop (reprompt); si persiste, `LLMValidationError`. (cubre Assumption A1: el provider pudo no forzar la funcion).
    - Test (repair): 1a arguments invalido para el schema -> 2a valido -> devuelve data; fetch llamado 2 veces.
    - Test: `id==="minimax"`, `trainsOnInputs===false`.
  </behavior>
  <action>
    REEMPLAZAR los stubs de 02-01 con la implementacion real (mismos nombres exportados; `index.ts` NO se toca):
    `packages/llm/src/json-schema.ts`: `export function zodToToolSchema(schema: z.ZodType): Record<string, unknown> { return z.toJSONSchema(schema) as Record<string,unknown>; }` (zod v4 nativo; una sola fuente de verdad — el schema zod). Si `z.toJSONSchema` requiere opciones para target compatible con function-calling, fijarlas aqui (p.ej. sin `$ref` cuando sea posible).
    `packages/llm/src/providers/minimax.ts`: `class MiniMaxProvider implements LLMProvider` con `id="minimax"`, `trainsOnInputs=false`. Constructor `({ apiKey, baseURL?, model?, fetchFn })` -> `new OpenAI({ apiKey, baseURL: baseURL ?? "https://api.minimax.io/v1", fetch: fetchFn })`. `complete<T>(req, schema)`: derivar `params = zodToToolSchema(schema)`; messages `[{role:"system",content:req.system},{role:"user",content:req.user}]`; `create({ model: this.model ?? "MiniMax-M3", messages, tools:[{type:"function",function:{name:"emit_result",description:"Emit the structured result",parameters:params}}], tool_choice:{type:"function",function:{name:"emit_result"}} })`. Extraer `raw = res.choices[0]?.message?.tool_calls?.[0]?.function?.arguments` (string|undefined). Delegar a `parseAndValidate(schema, raw, { maxAttempts: req.maxRepairAttempts ?? 1, reprompt: async (errors) => { /* re-llamar create() agregando un message con los errores zod; devolver el nuevo tool_calls[0].function.arguments */ } })`. Validacion EXTERNA (compuerta unica) — NO safeParse propio. Nunca loguear prompt/key. NO usar response_format.
    Escribir `json-schema.test.ts` y `minimax.test.ts` cubriendo `<behavior>` con `makeMockFetch` (rutas a `https://api.minimax.io/v1/chat/completions`); inspeccionar `mock.calls[0].body` para asserts de tool_choice/tools/ausencia de response_format.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm test --run json-schema minimax && pnpm -w typecheck</automated>
  </verify>
  <done>zodToToolSchema deriva el JSON schema del zod; MiniMaxProvider fuerza la tool, lee tool_calls.arguments, valida via la compuerta unica, repara y maneja la ausencia de tool_calls; NO usa response_format; flags id/trainsOnInputs correctos; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Politica data-routing (RUT nunca al LLM; PII solo a tier que no entrena) + smoke test live gated</name>
  <files>packages/llm/src/data-routing.ts, packages/llm/src/data-routing.test.ts, packages/llm/src/smoke.test.ts</files>
  <read_first>packages/llm/src/types.ts, packages/llm/src/router.ts, packages/llm/src/config.ts, packages/llm/src/providers/deepseek.ts, packages/llm/src/providers/minimax.ts, packages/llm/src/data-routing.ts</read_first>
  <behavior>
    data-routing (logica pura, sin red):
    - Test: `assertNoRutInLlmInput(text)` con un texto que contiene un RUT chileno (formato `12.345.678-9` y `12345678-9`) -> lanza `RutInLlmInputError`. La deteccion es determinista (regex de RUT), documentando que el RUT se matchea aparte y NUNCA entra a un prompt.
    - Test: `assertNoRutInLlmInput(text)` sin RUT -> no lanza.
    - Test: `assertSensitivityAllowed({ sensitivity:"personal" }, provider con trainsOnInputs:true)` -> lanza (reusa/espeja la regla fail-closed del router: PII nunca a tier que entrena). Con trainsOnInputs:false -> OK.
    - Test: `assertSensitivityAllowed({ sensitivity:"public" }, cualquier provider)` -> OK (texto publico puede ir a Gemini free).
    smoke test:
    - Test: el bloque smoke se SALTA cuando `LLM_SMOKE !== "1"` (default). Verificable corriendo la suite normal sin la env -> 0 tests ejecutados del bloque smoke (describe.skip).
  </behavior>
  <action>
    REEMPLAZAR el stub de `data-routing.ts` de 02-01 con la implementacion real (mismos exports; `index.ts` NO se toca):
    `packages/llm/src/data-routing.ts`: documentar en un comentario de cabecera la POLITICA dato->proveedor (de 02-CONTEXT.md): (1) el RUT se matchea de forma determinista y NUNCA se envia a ningun LLM; (2) dato personal (nombres en contexto de identidad) solo a un tier que NO entrena con inputs (trainsOnInputs=false); (3) NUNCA dato personal por el free tier de Gemini (entrena) — Gemini solo texto publico para embeddings. Implementar y exportar: `class RutInLlmInputError extends Error`; `function assertNoRutInLlmInput(text: string): void` (regex de RUT chileno con y sin puntos, con DV 0-9/K; si matchea -> throw; el mensaje NO incluye el texto completo ni el RUT, solo "input contains a RUT; RUT must never be sent to an LLM"); `function assertSensitivityAllowed(task: { sensitivity: Sensitivity }, provider: Pick<LLMProvider,"id"|"trainsOnInputs">): void` que lanza `SensitiveRoutingError` (importada de router.ts; reusar, no duplicar) si `personal && trainsOnInputs`. Logica pura, Web APIs estandar, sin red.
    `packages/llm/src/smoke.test.ts`: el smoke test live gated por env segun el patron del RESEARCH: `const LIVE = process.env.LLM_SMOKE === "1"; (LIVE ? describe : describe.skip)("smoke live providers", () => { ... })`. Dentro, 1 test por proveedor (DeepSeek, MiniMax, Gemini) que usa las keys reales de env y el `fetch` global real, hace 1 llamada minima, y asserta el shape (objeto validado por zod / embedding de 768 dims). Por defecto NO corre (describe.skip). Documentar en comentario: correr con `LLM_SMOKE=1 pnpm --filter @obs/llm test --run smoke` una vez para confirmar A1 (MiniMax forced tool_choice) y A4 (Gemini batch shape). El smoke de Gemini importa el GeminiEmbeddingProvider (lo rellena la rebanada 03 sobre un stub que ya existe desde 02-01) — el import resuelve aunque 03 aun no haya corrido; el sub-test solo se ejecuta con LLM_SMOKE=1, asi que NO bloquea la suite por orden de waves.
    Escribir `data-routing.test.ts` cubriendo `<behavior>`.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm test --run && pnpm -w typecheck && npx --yes deno check packages/llm/src/providers/minimax.ts packages/llm/src/data-routing.ts packages/llm/src/json-schema.ts</automated>
  </verify>
  <done>data-routing rechaza RUT en input a LLM y PII a tier que entrena (reusando SensitiveRoutingError); smoke test live existe y se salta por defecto (sin LLM_SMOKE); suite `@obs/llm` completa verde; `deno check` de los nuevos modulos sin error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> @obs/llm (MiniMax) | Tarea critica/sensible cruza a un tier; debe ser tier que no entrena |
| input de texto -> prompt LLM | El RUT (identificador personal duro) jamas debe cruzar esta frontera |
| @obs/llm -> api.minimax.io (red) | Respuesta del modelo no confiable; salida adversaria validable solo por zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | data-routing (input con RUT) | mitigate | `assertNoRutInLlmInput` deteccion determinista (regex RUT); lanza antes de cualquier llamada LLM; mensaje de error sin el RUT. Test de formatos con/sin puntos |
| T-02-06 | Information Disclosure | data-routing (PII a tier que entrena) | mitigate | `assertSensitivityAllowed` reusa `SensitiveRoutingError`: `personal && trainsOnInputs` -> abort; Gemini free solo texto publico (documentado en codigo) |
| T-02-07 | Tampering | MiniMax tool_calls (ausentes/adversarios) | mitigate | Si no hay tool_calls/arguments -> repair loop -> `LLMValidationError`; nunca se acepta salida no estructurada (sella el hallazgo MiniMax) |
| T-02-08 | Information Disclosure | smoke test live (keys reales) | mitigate | Gated por `LLM_SMOKE=1`, skip por defecto -> CI no quema cuota ni expone keys; keys solo de env; sin keys en asserts/logs |
| T-02-SC | Tampering | npm installs (heredado de 02-01) | accept | Sin nuevos paquetes en este plan; los SDKs ya auditados (RESEARCH Package Legitimacy Audit, todos oficiales) |
</threat_model>

<verification>
- `pnpm --filter @obs/llm test --run` verde: json-schema, minimax (tool-calling forzado / sin response_format / repair / ausencia de tool_calls), data-routing (RUT / PII-fail-closed / public-OK), smoke skip por defecto.
- `pnpm -w typecheck` verde.
- `deno check` de minimax.ts/data-routing.ts/json-schema.ts sin error (consumo Edge).
- Sin `LLM_SMOKE`, la suite NO ejecuta los tests del bloque smoke (describe.skip).
</verification>

<success_criteria>
- MiniMaxProvider entrega salida estructurada via tool-calling forzado, validada por la MISMA compuerta zod externa (FND-06: sella "MiniMax sin response_format").
- Agregar MiniMax no toco router ni validate -> capa enchufable real (FND-06 criterio 2).
- Politica dato->proveedor en codigo y testeada: RUT nunca al LLM; PII nunca a tier que entrena (criterio 4 de la fase).
- Smoke test live por proveedor existe y se salta por defecto.
</success_criteria>

<output>
Create `.planning/phases/02-capa-de-providers-llm-embeddings/02-02-SUMMARY.md` when done
</output>
