---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 04
type: execute
wave: 3
depends_on: ["20-02"]
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Hay votos individuales (Cámara, por DIPID) en la nube enriqueciendo votacion/voto"
    - "Hay audiencias de lobby reales en la nube"
    - "Hay declaraciones de patrimonio reales en la nube"
    - "MONEY/SERVEL NO se corrieron (gated)"
  artifacts: []
  key_links:
    - from: "packages/votos/src/run-camara-votos.ts"
      to: "voto / votacion (cloud)"
      via: "runCamaraVotos con SUPABASE_URL + SUPABASE_LOCAL_SERVICE_KEY apuntados al cloud"
      pattern: "runCamaraVotos"
    - from: "packages/lobby/src/ingest-cli.ts"
      to: "lobby_audiencia (cloud)"
      via: "SupabaseLobbyWriter (SUPABASE_URL + SUPABASE_SERVICE_KEY)"
      pattern: "SupabaseLobbyWriter"
    - from: "packages/probidad/src/ingest-cli.ts"
      to: "declaracion (cloud)"
      via: "SupabaseProbidadWriter (SUPABASE_URL + SUPABASE_SERVICE_KEY)"
      pattern: "SupabaseProbidadWriter"
---

<objective>
Completar el frente "parlamentarios 360" poblando la nube con los tres datasets de atribución: votos individuales (Cámara, DIPID determinista), audiencias de lobby (leylobby) y declaraciones de patrimonio (InfoProbidad). Cada uno en lotes acotados, idempotente, rate-limit 2-3s. MONEY/SERVEL quedan EXCLUIDOS (gated).

Purpose: Las secciones de la ficha de parlamentario (votos/lobby/patrimonio) necesitan estos datos para no salir vacías. Es el cierre de la carga de datos del MVP.
Output: `voto`, `lobby_audiencia`, `declaracion` cloud con count(*) > 0; cero data MONEY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- TRES conectores con TRES contratos de env DISTINTOS. Confirmar cada uno antes de correr. -->

De packages/votos/src/run-camara-votos.ts (@obs/votos — NO tiene CLI ni script `ingest`):
- Export `runCamaraVotos({ boletines?, limite?, ... })`. EXIGE acotar: `boletines` explícitos O `limite` > 0 (si no, lanza).
- Writer por defecto: SupabaseTramitacionWriter si `SUPABASE_URL` + `SUPABASE_LOCAL_SERVICE_KEY` presentes; si no, in-memory (dry-run).
- Cruce DIPID→id_diputado_camara determinista (sin LLM); fail-closed (DIPID fuera de maestra → no_confirmado, nunca fabrica).
- Hay que invocarlo con un wrapper (tsx -e o script scratch) — no hay entrypoint CLI.

De packages/lobby/src/ingest-cli.ts:
- Flags: `--institucion CODE` (default AA001), `--anio YYYY` (default año actual), `--paginas N` (default 1), `--dry-run`.
- url = SUPABASE_DB_URL || SUPABASE_URL; serviceKey = SUPABASE_SERVICE_KEY || SUPABASE_LOCAL_SERVICE_KEY. (NO acepta --service-key por flag.)
- GOTCHA: el writer espera la URL REST (SUPABASE_URL), no la connection string. Exportar SUPABASE_URL=<rest> y SUPABASE_SERVICE_KEY=<secret> para esta corrida; NO dejar que tome SUPABASE_DB_URL como url REST.
- Drift BLOQUEANTE (cuarentena). Congreso NO está en leylobby (Open Q2): default AA001 ejercita el pipeline.

De packages/probidad/src/ingest-cli.ts:
- Flags: `--nombre FRAGMENTO` (repetible; default "bianchi chelech"), `--dry-run`.
- Mismo contrato de env que lobby (SUPABASE_URL + SUPABASE_SERVICE_KEY).
- Nunca fabrica; degrada a dry-run/fixture + human_verification si InfoProbidad no alcanzable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Votos individuales de Cámara (DIPID) al cloud</name>
  <read_first>
    - packages/votos/src/run-camara-votos.ts (confirmar firma de runCamaraVotos y los env vars que lee el writer por defecto: SUPABASE_URL + SUPABASE_LOCAL_SERVICE_KEY)
    - packages/votos/src/index.ts (confirmar el export)
    - .planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-02-SUMMARY.md (boletines validados Leg-58: 14309, 18296)
    - .env (SUPABASE_API_URL, SUPABASE_SECRET_KEY)
  </read_first>
  <action>
    @obs/votos no tiene CLI. Invocar `runCamaraVotos` con un wrapper one-off, acotado por boletines explícitos (los validados LIVE en Phase 8/10) o un límite. Exportar el destino cloud que el writer por defecto espera: `SUPABASE_URL=$SUPABASE_API_URL` y `SUPABASE_LOCAL_SERVICE_KEY=$SUPABASE_SECRET_KEY` (sí: el runner lee la var con nombre `SUPABASE_LOCAL_SERVICE_KEY` para el service key — confirmarlo en el código antes). Wrapper sugerido:
    `pnpm --filter @obs/votos exec tsx -e "import('./src/index.ts').then(m=>m.runCamaraVotos({boletines:['14309','18296'], log:console.log})).then(r=>{console.log('VOTOS', r.votaciones, r.votos, 'dbLoaded='+r.dbLoaded); process.exit(r.dbLoaded?0:1)})"`
    (ajustar la ruta de import al export real verificado). Idempotente por (votacion_id, fuente_voter_id). Confirmar `dbLoaded=true`. El cruce es determinista; un ratio de cruce < 100% es honesto (fail-closed), no un error.
  </action>
  <verify>
    <automated>El wrapper imprime `dbLoaded=true` con votos > 0 y exit code 0</automated>
  </verify>
  <acceptance_criteria>
    - `dbLoaded=true` y votos > 0 en la salida del wrapper
    - Si dbLoaded=false → faltó SUPABASE_URL/SUPABASE_LOCAL_SERVICE_KEY; corregir y reintentar
  </acceptance_criteria>
  <done>Votos individuales de Cámara enriquecidos en el cloud, determinista, idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Lobby (audiencias) y Patrimonio (declaraciones) al cloud</name>
  <read_first>
    - packages/lobby/src/ingest-cli.ts (flags --institucion/--anio/--paginas; env SUPABASE_URL + SUPABASE_SERVICE_KEY; GOTCHA url REST vs DB_URL)
    - packages/probidad/src/ingest-cli.ts (flags --nombre; mismo contrato env)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (orden FK; MONEY excluido)
    - .env (SUPABASE_API_URL, SUPABASE_SECRET_KEY)
  </read_first>
  <action>
    Correr lobby y patrimonio al cloud, ACOTADO. Para ambos exportar `SUPABASE_URL=$SUPABASE_API_URL` y `SUPABASE_SERVICE_KEY=$SUPABASE_SECRET_KEY` para la corrida. CUIDADO: lobby/probidad leen `SUPABASE_DB_URL || SUPABASE_URL` como url del writer REST — asegurar que SUPABASE_URL (REST) esté seteado y que NO tomen la connection string de DB como url REST (si hace falta, dejar SUPABASE_DB_URL fuera del entorno de ESTA corrida o confirmar que el writer usa el cliente supabase-js con url REST).
    Lobby (acotado, default AA001 ejercita el pipeline; el congreso no está en leylobby):
    `pnpm --filter @obs/lobby ingest -- --institucion AA001 --anio 2024 --paginas 1`
    Patrimonio (acotado por nombre; usar uno o dos apellidos conocidos del Senado/Cámara):
    `pnpm --filter @obs/probidad ingest -- --nombre "bianchi chelech"`
    Confirmar en cada log `LIVE` + `dbLoaded=true` (NO DRY-RUN). Drift BLOQUEANTE en lobby = cuarentena (honesto, no fabricar). Si un conector degrada a dry-run por env, corregir el env y reintentar. Correr en serie (rate-limit por host).
  </action>
  <verify>
    <automated>Ambos logs terminan en `LIVE ... dbLoaded=true` con audiencias > 0 y versiones (declaraciones) > 0; exit codes 0 (o 1 por degradaciones aisladas)</automated>
  </verify>
  <acceptance_criteria>
    - lobby: `ingest-lobby LIVE ... dbLoaded=true`, audiencias > 0
    - probidad: `ingest-probidad LIVE ... dbLoaded=true`, versiones (declaraciones) > 0
    - Ningún comando de @obs/dinero (ingest/ingest:servel) ejecutado
  </acceptance_criteria>
  <done>Lobby y patrimonio cargados al cloud; MONEY explícitamente NO corrido.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar con psql voto / lobby_audiencia / declaracion y confirmar cero MONEY</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (helper SUPABASE_DB_URL anti-BOM)
    - supabase/migrations (confirmar nombres reales de tablas MONEY para el conteo de exclusión: contrato/aporte/financiamiento)
  </read_first>
  <action>
    Con SUPABASE_DB_URL (helper anti-BOM), verificar los tres conteos de atribución y confirmar que las tablas MONEY están VACÍAS (gated):
    `psql "<SUPABASE_DB_URL>" -c "select (select count(*) from voto) as votos, (select count(*) from lobby_audiencia) as audiencias, (select count(*) from declaracion) as declaraciones;"`
    Confirmar nombres reales de tablas MONEY con un grep en supabase/migrations (p.ej. contrato/aporte) y verificar count = 0 en ellas:
    `psql "<SUPABASE_DB_URL>" -c "select count(*) from <tabla_money_contratos>;"` (esperado 0).
    No loguear la connection string.
  </action>
  <verify>
    <automated>psql: votos > 0, audiencias > 0, declaraciones > 0; las tablas MONEY devuelven count = 0</automated>
  </verify>
  <acceptance_criteria>
    - votos > 0, audiencias > 0, declaraciones > 0
    - Tablas MONEY (contratos/aportes) count = 0 (gated, sin data)
  </acceptance_criteria>
  <done>psql confirma los tres datasets de atribución poblados y MONEY vacío; carga de datos del MVP completa.</done>
</task>

</tasks>

<verification>
- voto / lobby_audiencia / declaracion (cloud) count > 0 verificados con psql
- Tablas MONEY confirmadas en 0 filas (gated)
- Ningún runner de @obs/dinero ejecutado
</verification>

<success_criteria>
El frente "parlamentarios 360" tiene votos/lobby/patrimonio reales en la nube; MONEY permanece gated y vacío.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-04-SUMMARY.md` when done
</output>
