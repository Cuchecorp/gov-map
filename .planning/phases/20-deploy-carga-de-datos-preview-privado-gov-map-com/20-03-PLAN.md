---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Los proyecto_ficha tienen embedding poblado (dim 768) en la nube"
    - "La búsqueda semántica /buscar puede devolver resultados reales"
  artifacts: []
  key_links:
    - from: "packages/fichas/src/pipeline-cli.ts"
      to: "proyecto_ficha.embedding (cloud)"
      via: "GeminiEmbeddingProvider 768 + SupabaseFichasWriter (SUPABASE_URL/SECRET_KEY ya cloud)"
      pattern: "GeminiEmbeddingProvider"
---

<objective>
Generar las fichas estructuradas y los embeddings (Gemini, dim 768) de los proyectos cargados, escribiéndolos al Supabase de la NUBE, para que `/buscar?q=...` devuelva proyectos reales por similitud semántica.

Purpose: La búsqueda semántica por idea matriz/cuerpos legales es uno de los dos frentes centrales del producto. Sin embeddings poblados, `/buscar` responde vacío.
Output: `proyecto_ficha` con columna embedding poblada en el cloud; búsqueda funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- pipeline-cli SÍ lee el cloud por defecto: SUPABASE_URL (o SUPABASE_API_URL) + SUPABASE_SECRET_KEY.
     A DIFERENCIA de seed/tramitacion, NO necesita override de destino — ya apunta al cloud si .env
     tiene SUPABASE_API_URL + SUPABASE_SECRET_KEY. Sin script pnpm dedicado → correr con tsx/deno. -->

De packages/fichas/src/pipeline-cli.ts:
- Flags: `--limite N` (default 50), `--boletines a,b,c`, `--reembed` (reprocesa TODO con bump de versión), `--dry-run`, `--service-key K` (o env SUPABASE_SECRET_KEY).
- url default = SUPABASE_URL || SUPABASE_API_URL; serviceKey default = SUPABASE_SECRET_KEY.
- Lee los proyecto_ficha PENDIENTES de la DB (writer.leerPendientes) y los procesa.
- Providers: DeepSeekProvider (DEEPSEEK_API_KEY) para extracción + GeminiEmbeddingProvider (GEMINI_API_KEY) dim 768.
- R2 gateado por presencia de credencial (degrada honesto si ausente).
- SIN service key (y sin --dry-run) → DRY-RUN (no carga DB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr el pipeline de fichas/embeddings contra el cloud (lotes acotados)</name>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts (confirmar que NO hay script pnpm: correr con tsx; confirmar que url/serviceKey salen del env cloud sin override)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (§code_context: "correr con deno" / pipeline-cli; Gemini dim 768)
    - .env (GEMINI_API_KEY, DEEPSEEK_API_KEY, SUPABASE_API_URL, SUPABASE_SECRET_KEY presentes)
  </read_first>
  <action>
    Correr el backfill de fichas+embeddings sobre los proyecto_ficha pendientes en el cloud. No hay script pnpm dedicado; invocar el entrypoint directo. Confirmar el runtime leyendo el archivo: usa imports `npm:`/node-style → preferir `pnpm --filter @obs/fichas exec tsx src/pipeline-cli.ts -- ...` o `tsx packages/fichas/src/pipeline-cli.ts ...`; si el paquete está pensado para Deno, usar `deno run --allow-net --allow-read --allow-env --allow-sys packages/fichas/src/pipeline-cli.ts ...` (CONTEXT pide `--allow-sys` para evitar falsos NotCapable; cargar el `.env` al entorno antes). Empezar acotado:
    `... pipeline-cli.ts --limite 50`
    (sin --reembed: procesa sólo pendientes; idempotente). pipeline-cli ya apunta al cloud vía SUPABASE_API_URL+SUPABASE_SECRET_KEY del .env — NO requiere override de destino (a diferencia de seed/tramitación). Asegurar que GEMINI_API_KEY y DEEPSEEK_API_KEY estén en el entorno. Si quedan muchos pendientes, repetir la corrida con `--limite` hasta drenar (idempotente). Confirmar `fichas LIVE: ... dbLoaded=true`. Degradaciones (texto fuente null → embed de título+materia) son honestas y esperadas; no abortan.
  </action>
  <verify>
    <automated>El log termina con `fichas LIVE: procesados=N embebidos=M ... dbLoaded=true` con M > 0 y exit code 0 (o 1 sólo por errores aislados con la mayoría embebida)</automated>
  </verify>
  <acceptance_criteria>
    - Log: `fichas LIVE` (no DRY-RUN), `dbLoaded=true`, `embebidos` > 0
    - Si aparece DRY-RUN → faltó SUPABASE_SECRET_KEY en el entorno; corregir y reintentar
  </acceptance_criteria>
  <done>Fichas + embeddings 768 cargados al cloud para los proyectos disponibles.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar embeddings poblados con psql</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (helper SUPABASE_DB_URL anti-BOM)
    - supabase/migrations (confirmar el nombre real de la columna/tabla de embedding: probablemente proyecto_ficha.embedding)
  </read_first>
  <action>
    Confirmar el nombre exacto de la tabla/columna de embedding leyendo el schema (grep en supabase/migrations por `embedding` y por `proyecto_ficha`), luego con `SUPABASE_DB_URL` (helper anti-BOM):
    `psql "<SUPABASE_DB_URL>" -c "select count(*) as fichas, count(embedding) as con_embedding from proyecto_ficha;"`
    Ajustar el nombre de tabla/columna si el schema difiere. No loguear la connection string.
  </action>
  <verify>
    <automated>psql: `con_embedding` > 0 (filas con vector poblado)</automated>
  </verify>
  <acceptance_criteria>
    - `con_embedding` > 0
    - Si fichas > 0 pero con_embedding = 0 → el embed degradó/falló; revisar GEMINI_API_KEY y reintentar el pipeline
  </acceptance_criteria>
  <done>psql confirma embeddings poblados; /buscar puede devolver resultados reales.</done>
</task>

</tasks>

<verification>
- proyecto_ficha.embedding (cloud) con filas pobladas verificado con psql
- Corrida LIVE (no dry-run), idempotente
</verification>

<success_criteria>
La búsqueda semántica tiene corpus vectorial real en la nube; /buscar es funcional.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-03-SUMMARY.md` when done
</output>
