---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Hay filas en proyecto_ficha estado='pendiente' (seedadas desde proyecto) ANTES de correr el pipeline"
    - "La tabla proyecto_embedding tiene vectores (dim 768) poblados en la nube"
    - "La búsqueda semántica /buscar puede devolver resultados reales"
  artifacts: []
  key_links:
    - from: "packages/fichas/src/pipeline-cli.ts"
      to: "proyecto_embedding (cloud, vector(768))"
      via: "GeminiEmbeddingProvider 768 + SupabaseFichasWriter.upsertEmbedding (SUPABASE_API_URL/SECRET_KEY ya cloud)"
      pattern: "GeminiEmbeddingProvider"
---

<objective>
Generar las fichas estructuradas y los embeddings (Gemini, dim 768) de los proyectos cargados, escribiéndolos al Supabase de la NUBE, para que `/buscar?q=...` devuelva proyectos reales por similitud semántica.

Purpose: La búsqueda semántica por idea matriz/cuerpos legales es uno de los dos frentes centrales del producto. Sin embeddings poblados, `/buscar` responde vacío.
Output: `proyecto_embedding` con vectores 768 poblados en el cloud; búsqueda funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- pipeline-cli SÍ lee el cloud por defecto: SUPABASE_URL (o SUPABASE_API_URL) + SUPABASE_SECRET_KEY.
     A DIFERENCIA de seed/tramitacion, NO necesita override de destino — ya apunta al cloud si .env
     tiene SUPABASE_API_URL + SUPABASE_SECRET_KEY. Sin script pnpm dedicado → correr con tsx/deno.

     SCHEMA CONFIRMADO (migración 0011_fichas_embeddings.sql + 0013):
       * proyecto_ficha: boletin (PK, FK proyecto), idea_matriz, cuerpos_legales, texto_r2_path,
         estado (default 'pendiente', check IN ('pendiente','procesando','embebido','error')),
         origen (NOT NULL), fecha_captura, error_msg. NO existe columna `embedding` aquí.
       * proyecto_embedding: boletin (PK, FK proyecto), embedding vector(768) NOT NULL,
         embedding_model, embedding_dims, embedding_version. AQUÍ vive el vector.
     pipeline-cli lee proyecto_ficha PENDIENTES (writer.leerPendientes filtra estado='pendiente');
     si NO hay filas pendientes, NO procesa nada. La nube está vacía → hay que SEEDEAR primero. -->

De packages/fichas/src/pipeline-cli.ts:
- Flags: `--limite N` (default 50), `--boletines a,b,c`, `--reembed` (reprocesa TODO con bump de versión), `--dry-run`, `--service-key K` (o env SUPABASE_SECRET_KEY).
- url default = SUPABASE_URL || SUPABASE_API_URL; serviceKey default = SUPABASE_SECRET_KEY.
- Lee los proyecto_ficha PENDIENTES de la DB (writer.leerPendientes) y los procesa.
- Providers: DeepSeekProvider (DEEPSEEK_API_KEY) para extracción + GeminiEmbeddingProvider (GEMINI_API_KEY) dim 768.
- R2 gateado por presencia de credencial (degrada honesto si ausente).
- SIN service key (y sin --dry-run) → DRY-RUN (no carga DB). GOTCHA: tsx/pnpm NO auto-cargan .env → si no precargas el entorno, GEMINI/DEEPSEEK/SECRET_KEY faltan y el pipeline degrada SILENCIOSO a DRY-RUN sin embeddings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seedear filas pendientes en proyecto_ficha desde proyecto (cloud)</name>
  <read_first>
    - supabase/migrations/0011_fichas_embeddings.sql (CONFIRMADO: proyecto_ficha tiene boletin/estado('pendiente')/origen NOT NULL; sin columna embedding)
    - supabase/migrations/0008_tramitacion.sql (CONFIRMADO: proyecto.origen es NOT NULL → el SELECT lo provee)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (helper SUPABASE_DB_URL anti-BOM; psql path)
  </read_first>
  <action>
    El pipeline de fichas SOLO procesa filas existentes en proyecto_ficha con estado='pendiente'; la nube está vacía, así que NADA se generaría. Sembrar las filas pendientes ANTES de correr el pipeline, derivándolas de la tabla `proyecto` ya cargada en 20-02. Extraer `SUPABASE_DB_URL` con el helper python anti-BOM de CONTEXT (UTF-8 sin BOM; NUNCA loguear la connection string) y ejecutar, con la service key (psql va por la connection string directa, que bypassa RLS):
    `psql "<SUPABASE_DB_URL>" -c "INSERT INTO proyecto_ficha (boletin, estado, origen) SELECT boletin, 'pendiente', origen FROM proyecto ON CONFLICT (boletin) DO NOTHING;"`
    (columnas confirmadas contra 0011/0008: proyecto_ficha.boletin/estado/origen; estado='pendiente' está en el CHECK; proyecto.origen es NOT NULL así que la fila satisface el NOT NULL de proyecto_ficha.origen). Es idempotente por `ON CONFLICT (boletin) DO NOTHING` → re-correr no duplica. Confirmar luego que quedaron pendientes con:
    `psql "<SUPABASE_DB_URL>" -c "select count(*) as pendientes from proyecto_ficha where estado='pendiente';"`
  </action>
  <verify>
    <automated>psql: `select count(*) from proyecto_ficha where estado='pendiente'` devuelve un valor > 0</automated>
  </verify>
  <acceptance_criteria>
    - `pendientes` > 0 (debe igualar aproximadamente count(*) de proyecto)
    - Si pendientes = 0 → proyecto está vacío (20-02 no cargó) o el INSERT falló por columna; corregir antes de seguir
  </acceptance_criteria>
  <done>proyecto_ficha tiene filas pendientes sembradas desde proyecto; el pipeline ya tiene qué procesar.</done>
</task>

<task type="auto">
  <name>Task 2: Correr el pipeline de fichas/embeddings contra el cloud (lotes acotados)</name>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts (confirmar que NO hay script pnpm: correr con tsx; confirmar que url/serviceKey salen del env cloud sin override)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (§code_context: "correr con deno" / pipeline-cli; Gemini dim 768)
    - .env (GEMINI_API_KEY, DEEPSEEK_API_KEY, SUPABASE_API_URL, SUPABASE_SECRET_KEY presentes)
  </read_first>
  <action>
    PRECARGAR EL ENTORNO desde .env ANTES de invocar (tsx/pnpm NO auto-cargan .env; sin esto el pipeline degrada SILENCIOSO a DRY-RUN y no produce embeddings). En bash: `set -a; source .env; set +a`. El host es Windows: un loader PowerShell equivalente es aceptable, p.ej. leer cada línea `KEY=VALUE` de .env (saltando comentarios) y hacer `$env:KEY=VALUE`; respetar la regla NO-BOM (leer el archivo como UTF-8 sin BOM, no reintroducir BOM). Verificar tras precargar que GEMINI_API_KEY, DEEPSEEK_API_KEY y SUPABASE_SECRET_KEY están presentes en el shell (sin imprimir sus valores).
    Luego correr el backfill de fichas+embeddings sobre los proyecto_ficha pendientes (sembrados en Task 1) en el cloud. No hay script pnpm dedicado; invocar el entrypoint directo. Confirmar el runtime leyendo el archivo: usa imports `npm:`/node-style → preferir `pnpm --filter @obs/fichas exec tsx src/pipeline-cli.ts -- ...` o `tsx packages/fichas/src/pipeline-cli.ts ...`; si el paquete está pensado para Deno, usar `deno run --allow-net --allow-read --allow-env --allow-sys packages/fichas/src/pipeline-cli.ts ...` (CONTEXT pide `--allow-sys` para evitar falsos NotCapable). Empezar acotado:
    `... pipeline-cli.ts --limite 50`
    (sin --reembed: procesa sólo pendientes; idempotente). pipeline-cli ya apunta al cloud vía SUPABASE_API_URL+SUPABASE_SECRET_KEY del .env precargado — NO requiere override de destino (a diferencia de seed/tramitación). Si quedan muchos pendientes, repetir la corrida con `--limite` hasta drenar (idempotente). Confirmar `fichas LIVE: ... dbLoaded=true`. Degradaciones (texto fuente null → embed de título+materia) son honestas y esperadas; no abortan.
  </action>
  <verify>
    <automated>El log termina con `fichas LIVE: procesados=N embebidos=M ... dbLoaded=true` con M > 0 y exit code 0 (o 1 sólo por errores aislados con la mayoría embebida)</automated>
  </verify>
  <acceptance_criteria>
    - Log: `fichas LIVE` (no DRY-RUN), `dbLoaded=true`, `embebidos` > 0
    - Si aparece DRY-RUN → faltó precargar .env (SUPABASE_SECRET_KEY/GEMINI_API_KEY ausentes en el shell); precargar y reintentar
  </acceptance_criteria>
  <done>Fichas + embeddings 768 cargados al cloud para los proyectos disponibles.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar embeddings poblados en proyecto_embedding con psql</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (helper SUPABASE_DB_URL anti-BOM)
    - supabase/migrations/0011_fichas_embeddings.sql (CONFIRMADO: el vector vive en proyecto_embedding (PK boletin, embedding vector(768)), NO en proyecto_ficha)
  </read_first>
  <action>
    El vector(768) vive en la tabla `proyecto_embedding` (PK boletin), NO en `proyecto_ficha` (que no tiene columna embedding). Con `SUPABASE_DB_URL` (helper anti-BOM), verificar el conteo de vectores poblados:
    `psql "<SUPABASE_DB_URL>" -c "select count(*) as embeddings from proyecto_embedding;"`
    Opcionalmente, contrastar con fichas embebidas: `psql "<SUPABASE_DB_URL>" -c "select count(*) from proyecto_ficha where estado='embebido';"`. No loguear la connection string.
  </action>
  <verify>
    <automated>psql: `select count(*) from proyecto_embedding` devuelve un valor > 0</automated>
  </verify>
  <acceptance_criteria>
    - `embeddings` (count de proyecto_embedding) > 0
    - Si proyecto_ficha tiene embebidos > 0 pero proyecto_embedding = 0 → el upsert del vector falló; revisar GEMINI_API_KEY y reintentar el pipeline
  </acceptance_criteria>
  <done>psql confirma vectores poblados en proyecto_embedding; /buscar puede devolver resultados reales.</done>
</task>

</tasks>

<verification>
- proyecto_ficha sembrado con filas pendientes desde proyecto (gate previo al pipeline)
- proyecto_embedding (cloud) con vectores poblados verificado con psql
- Corrida LIVE (no dry-run), idempotente, con .env precargado al entorno
</verification>

<success_criteria>
La búsqueda semántica tiene corpus vectorial real en la nube (proyecto_embedding poblado); /buscar es funcional.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-03-SUMMARY.md` when done
</output>
</content>
</invoke>
