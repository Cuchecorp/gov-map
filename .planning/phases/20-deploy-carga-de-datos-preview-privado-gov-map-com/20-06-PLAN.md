---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 06
type: execute
wave: 5
depends_on: ["20-05"]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "El landing del sitio desplegado carga"
    - "/buscar?q=... devuelve proyectos de ley reales"
    - "Una ficha /parlamentario/[id] muestra votos, lobby y patrimonio reales"
    - "NINGUNA sección MONEY visible (gated); sin foto ni partido"
    - "El sitio responde con noindex"
  artifacts: []
  key_links:
    - from: "sitio desplegado"
      to: "Supabase cloud poblado"
      via: "RPC public-read (anon)"
      pattern: "/buscar"
---

<objective>
Verificar end-to-end que el sitio desplegado sirve la data real y honra los principios rectores: búsqueda devuelve proyectos, una ficha muestra votos/lobby/patrimonio, MONEY gated (ausente del DOM), sin foto/partido, noindex presente. Cierra los 5 Success Criteria de la fase.

Purpose: Probar que la cadena completa (ingesta → nube → frontend desplegado) funciona y que las salvaguardas anti-insinuación / MONEY-gated están intactas en producción.
Output: Verificación documentada (rutas probadas, conteos visibles, noindex y MONEY-off confirmados).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- browseros: el cliente MCP puede tener la lista de tools VIEJA cacheada (server v0.0.119).
     Workaround en CONTEXT: driver HTTP directo en .planning/phases/19-.../refs/bros.py
     (JSON-RPC a 127.0.0.1:9200/mcp; tabs/navigate/screenshot/read). Para tools nativos:
     /mcp → browseros → Reconnect. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar HTTP + noindex + ausencia de MONEY por respuesta cruda</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-05-SUMMARY.md (la URL desplegada)
  </read_first>
  <action>
    Tomar la URL desplegada del SUMMARY de 20-05. Con curl, confirmar las salvaguardas a nivel de respuesta (sin depender del navegador):
    - Landing: `curl -sS -o /dev/null -w "%{http_code}" <URL>/` → 200.
    - noindex: `curl -sSI <URL>/` (header X-Robots-Tag) o `curl -sS <URL>/` y grep del `<meta name="robots"` con noindex.
    - /buscar responde: `curl -sS -o /dev/null -w "%{http_code}" "<URL>/buscar?q=ley"` → 200.
    - MONEY gated: traer el HTML de una ficha y confirmar que NO contiene marcadores de las secciones MONEY (p.ej. "Contratos del Estado", "Aportes de campaña", "SERVEL", "financiamiento") — `curl -sS "<URL>/parlamentario/<id>" | grep -i -E "contratos del estado|aportes de campaña|financiamiento|servel"` debe NO devolver nada (gate fail-closed = nodo ausente). Usar un id de parlamentario real (obtenerlo de psql o de un link del landing).
  </action>
  <verify>
    <automated>landing y /buscar devuelven 200; el HTML del landing lleva noindex; el grep de marcadores MONEY en una ficha NO devuelve coincidencias</automated>
  </verify>
  <acceptance_criteria>
    - `/` → 200; `/buscar?q=ley` → 200
    - noindex presente (header o meta)
    - cero coincidencias de marcadores MONEY en el HTML de la ficha
  </acceptance_criteria>
  <done>Salvaguardas confirmadas a nivel de respuesta: accesible, noindex, MONEY ausente del DOM.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificación visual end-to-end del sitio desplegado</name>
  <action>Checkpoint de verificación humana: la inspección visual del sitio en producción (landing, /buscar con resultados reales, una ficha con votos/lobby/patrimonio, ausencia de MONEY/foto/partido) la hace el operador con browseros/navegador siguiendo los pasos de abajo.</action>
  <what-built>Sitio desplegado sirviendo data real de la nube poblada. Verificación visual end-to-end.</what-built>
  <how-to-verify>
    Usando browseros (o el navegador) sobre la URL desplegada (de 20-05-SUMMARY):
    1. Abrir `<URL>/` → el landing carga (hero + búsqueda protagonista).
    2. `<URL>/buscar?q=<algún término del corpus, p.ej. una materia presente>` → aparecen PROYECTOS de ley reales con trazabilidad (boletín/fuente), no vacío.
    3. Abrir una ficha `<URL>/parlamentario/<id real>` → se ven las secciones de **votos**, **lobby** y **patrimonio** con datos reales (carriles separados mt-12), cada dato con su ProvenanceBadge.
    4. Confirmar que NO hay ninguna sección de MONEY (Contratos del Estado / Aportes de campaña), NO hay foto del parlamentario y NO se muestra partido.
    5. Confirmar lenguaje anti-insinuación: ninguna unidad de UI compone dinero/lobby con un voto; sin lenguaje causal.
    Tomar capturas (screenshot) de landing, /buscar con resultados, y una ficha con las 3 secciones, como evidencia.
  </how-to-verify>
  <resume-signal>Escribe "verificado" + nota de cualquier discrepancia (vacíos inesperados, MONEY visible, foto/partido), o describe el problema</resume-signal>
</task>

</tasks>

<verification>
- Landing, /buscar (con resultados reales) y una ficha /parlamentario/[id] (votos+lobby+patrimonio) verificados en el sitio desplegado
- MONEY ausente del DOM; sin foto/partido; noindex presente; anti-insinuación intacta
</verification>

<success_criteria>
El sitio en producción responde y muestra data real con todos los principios rectores intactos — cierra los 5 Success Criteria de la fase.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-06-SUMMARY.md` when done
</output>
