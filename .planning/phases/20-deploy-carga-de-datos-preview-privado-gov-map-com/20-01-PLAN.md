---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "La tabla parlamentario de la nube tiene filas reales (~186) tras la carga"
    - "La maestra se cargó al proyecto CLOUD (bctyygbmqcvizyplktuw), no al Supabase local"
  artifacts:
    - path: "supabase/seeds/parlamentario.seed.json"
      provides: "Snapshot autoritativo de la maestra (186 filas curadas, ID-09)"
      contains: "nombre_normalizado"
  key_links:
    - from: "packages/identity/src/seed-cli.ts"
      to: "parlamentario (cloud)"
      via: "SupabaseMaestraWriter con --service-key + SUPABASE_LOCAL_URL=<cloud>"
      pattern: "SupabaseMaestraWriter"
---

<objective>
Poblar la tabla `parlamentario` del Supabase de la NUBE con la maestra real (~186 filas: 31 senadores + 155 diputados). Es el primer eslabón del orden FK obligatorio: nada de tramitación/votos/lobby/patrimonio puede cruzar identidad sin esta maestra.

Purpose: Sin maestra confirmada, los conectores posteriores degradan a `no_confirmado` y las fichas salen vacías de vínculos. Es el gate de toda la carga.
Output: `parlamentario` cloud con count(*) > 0; snapshot git autoritativo presente/actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- seed-cli LEE por defecto el Supabase LOCAL (SUPABASE_LOCAL_URL / SUPABASE_LOCAL_SERVICE_KEY,
     default http://127.0.0.1:54421). NO lee SUPABASE_API_URL/SUPABASE_SECRET_KEY automáticamente.
     Para escribir a la NUBE hay que sobreescribir esos dos canales. GOTCHA clave de esta fase. -->

De packages/identity/src/seed-cli.ts:
- `main({ localUrl, serviceKey, promote, ... })` — `localUrl` default = env SUPABASE_LOCAL_URL || http://127.0.0.1:54421; `serviceKey` default = env SUPABASE_LOCAL_SERVICE_KEY.
- CLI flags reales: `--promote` (promueve a confirmado el lote vigente+match), `--preserve-estado`, `--r2`. NO existe flag de URL/host por CLI.
- El seeder corre LIVE contra catálogos Senado XML + Cámara XML (no carga desde snapshot; el snapshot es SALIDA, no entrada).
- Sin service key → OMITE la carga a DB pero SIEMPRE escribe el snapshot git.
- `--promote` confirma SOLO catálogo-vigentes + match determinista único; nunca blanket-confirm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargar la maestra a la NUBE apuntando seed-cli al proyecto cloud</name>
  <read_first>
    - packages/identity/src/seed-cli.ts (confirmar que localUrl/serviceKey son los ÚNICOS canales de destino; el seeder NO acepta --url)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (§code_context "Runners de ingesta" + credenciales)
    - .env (leer SUPABASE_API_URL y SUPABASE_SECRET_KEY; mantener UTF-8 sin BOM, NUNCA loguear el secret)
  </read_first>
  <action>
    Cargar la maestra LIVE escribiendo al proyecto CLOUD. seed-cli usa `SUPABASE_LOCAL_URL`+`SUPABASE_LOCAL_SERVICE_KEY` como destino, así que se sobreescriben en la invocación apuntándolos al cloud:
    `SUPABASE_LOCAL_URL=$SUPABASE_API_URL` y `SUPABASE_LOCAL_SERVICE_KEY=$SUPABASE_SECRET_KEY`.
    Invocación (desde la raíz del repo, una sola corrida, rate-limit 2-3s heredado de @obs/ingest):
    `pnpm --filter @obs/identity seed:live -- --promote`
    con esas dos env exportadas para esa corrida (en PowerShell: `$env:SUPABASE_LOCAL_URL=...; $env:SUPABASE_LOCAL_SERVICE_KEY=...` antes del comando). Usar `--promote` para que las filas del catálogo de vigentes + match determinista único queden `confirmado` (los conectores posteriores SOLO cruzan contra `confirmado`/determinista). Confirmar en el log de salida `dbLoaded=true` (NO "carga a DB OMITIDA"). Si el seeder reporta error de fetch 403/429 de un catálogo, reintentar la corrida (es idempotente por clave natural). Respetar la regla de dos etapas: el seeder ya descarga LIVE; el snapshot git queda como verdad versionada (ID-09).
  </action>
  <verify>
    <automated>El log de seed-cli termina con `dbLoaded=true` y `total=` ~186 (senadores ~31 + diputados ~155); exit code 0</automated>
  </verify>
  <acceptance_criteria>
    - Log final: `seed OK: total=186 senadores=31 diputados=155 dbLoaded=true promoted={...}` (conteos aproximados, dbLoaded DEBE ser true)
    - Si dbLoaded=false → la corrida apuntó al local; corregir las env de destino y reintentar (no continuar)
  </acceptance_criteria>
  <done>seed-cli completó con dbLoaded=true contra el proyecto cloud; sin errores de fetch sin resolver.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar con psql que parlamentario cloud quedó poblado</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (§code_context: extraer SUPABASE_DB_URL esquivando BOM; psql en /c/Users/Carlo/miniconda3/Library/bin/psql)
  </read_first>
  <action>
    Extraer `SUPABASE_DB_URL` del `.env` esquivando cualquier residuo de BOM con el helper de CONTEXT:
    `python -c "raw=open('.env','rb').read().decode('utf-8',errors='ignore'); [print(l.split('=',1)[1].strip()) for l in raw.splitlines() if l.strip().startswith('SUPABASE_DB_URL=')]"`
    Luego correr contra el cloud (pooler sa-east-1):
    `psql "<SUPABASE_DB_URL>" -c "select count(*) as n_parlamentario from parlamentario;"`
    y además un conteo por estado para confirmar que `--promote` confirmó:
    `psql "<SUPABASE_DB_URL>" -c "select estado, count(*) from parlamentario group by estado order by 2 desc;"`
    NO loguear la connection string completa (lleva password); referirla como `<SUPABASE_DB_URL>`.
  </action>
  <verify>
    <automated>psql `select count(*) from parlamentario` devuelve n > 0 (esperado ~186) y existe al menos una fila con estado='confirmado'</automated>
  </verify>
  <acceptance_criteria>
    - `n_parlamentario` >= 100 (esperado ~186)
    - El group-by por estado muestra filas en `confirmado` (no todo en no_confirmado)
  </acceptance_criteria>
  <done>psql confirma parlamentario cloud poblado con filas confirmadas; queda el gate de identidad listo para tramitación.</done>
</task>

</tasks>

<verification>
- parlamentario (cloud) count > 0 verificado con psql contra SUPABASE_DB_URL
- Hay filas en estado `confirmado` (el cruce determinista de fases posteriores depende de esto)
</verification>

<success_criteria>
La maestra real está en el Supabase de la nube y confirmada; el orden FK queda desbloqueado para tramitación.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-01-SUMMARY.md` when done
</output>
