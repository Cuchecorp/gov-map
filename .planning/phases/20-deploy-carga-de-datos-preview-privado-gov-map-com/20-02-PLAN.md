---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "La nube tiene proyectos de ley reales con sus eventos de tramitación"
    - "Hay votaciones registradas para esos proyectos"
    - "La carga se hizo en lotes acotados (por boletín/legislatura), idempotente, rate-limit 2-3s"
  artifacts: []
  key_links:
    - from: "packages/tramitacion/src/ingest-cli.ts"
      to: "proyecto / votacion / tramitacion_evento (cloud)"
      via: "SupabaseTramitacionWriter con --service-key + SUPABASE_LOCAL_URL=<cloud>"
      pattern: "SupabaseTramitacionWriter"
---

<objective>
Poblar `proyecto`, `votacion` y `tramitacion_evento` del Supabase de la NUBE con tramitación real de la legislatura vigente (Leg-58), en lotes acotados por boletín/legislatura. Segundo eslabón del orden FK: los proyectos son la base de la búsqueda (fichas) y de los votos.

Purpose: Sin proyectos no hay qué buscar ni qué votar; es el corpus central del frente "seguimiento de proyectos de ley".
Output: `proyecto`/`votacion`/`tramitacion_evento` cloud con count(*) > 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- ingest-cli LEE por defecto el Supabase LOCAL (SUPABASE_LOCAL_URL / SUPABASE_LOCAL_SERVICE_KEY,
     default http://127.0.0.1:54421). Para escribir a la NUBE: sobreescribir SUPABASE_LOCAL_URL=<cloud>
     y pasar --service-key <SUPABASE_SECRET_KEY> (acepta el service key por flag).
     Además: tsx/pnpm NO auto-cargan .env → hay que precargar el entorno antes de invocar. -->

De packages/tramitacion/src/ingest-cli.ts:
- Flags: `--anno N` (default 2026), `--limite N` (default 5), `--boletines a,b,c` (salta descubrimiento),
  `--dry-run`, `--service-key K` (o env SUPABASE_LOCAL_SERVICE_KEY).
- `localUrl` default = env SUPABASE_LOCAL_URL || http://127.0.0.1:54421 (no hay flag de URL).
- SIN service key (y sin --dry-run) → degrada a DRY-RUN con aviso (NO carga DB). El log distingue LIVE vs DRY-RUN.
- Carga la maestra del snapshot supabase/seeds/parlamentario.seed.json (sólo lectura, para el cruce).
- Idempotente por clave natural; provenance por fila.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ingesta de tramitación a la NUBE en lotes acotados</name>
  <read_first>
    - packages/tramitacion/src/ingest-cli.ts (confirmar flags exactos: --anno/--limite/--boletines/--service-key; confirmar que localUrl es el único canal de destino)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (§code_context Runners + orden FK)
    - .env (SUPABASE_API_URL, SUPABASE_SECRET_KEY — no loguear el secret)
  </read_first>
  <action>
    PRECARGAR EL ENTORNO desde .env ANTES de invocar (tsx/pnpm NO auto-cargan .env; sin esto el conector no ve SUPABASE_SECRET_KEY y degrada a DRY-RUN). En bash: `set -a; source .env; set +a`. El host es Windows: un loader PowerShell equivalente es aceptable (leer cada línea `KEY=VALUE` de .env saltando comentarios y hacer `$env:KEY=VALUE`); respetar la regla NO-BOM (UTF-8 sin BOM). Verificar tras precargar que SUPABASE_API_URL y SUPABASE_SECRET_KEY están en el shell (sin imprimir el secret).
    Correr la ingesta de tramitación escribiendo al CLOUD. Sobreescribir el destino: `SUPABASE_LOCAL_URL=$SUPABASE_API_URL` y pasar `--service-key $SUPABASE_SECRET_KEY`.
    Corrida en LOTES ACOTADOS para no exceder el tiempo del executor ni martillar el WAF (rate-limit 2-3s heredado). Estrategia: empezar por la legislatura vigente con un límite acotado, p.ej.
    `pnpm --filter @obs/tramitacion ingest -- --anno 2026 --limite 20 --service-key $SUPABASE_SECRET_KEY`
    (con `SUPABASE_LOCAL_URL` exportado al cloud). Si se conocen boletines concretos a demostrar a la ONG, alternativamente acotar con `--boletines 14309,18296,...` (incluir los validados en Phase 8/10). Si un lote tarda mucho o devuelve errores transitorios, repetir con `--limite` menor o boletines explícitos — la corrida es idempotente, los re-runs no duplican. Confirmar en el log `ingest LIVE: ... dbLoaded=true` (NO `DRY-RUN`). Errores por boletín individual no abortan la corrida (degradación honesta); registrarlos pero continuar si la mayoría cargó.
    Regla de dos etapas: el conector descarga LIVE y persiste; el re-run desde R2 (si existiera) es el camino de reingesta — anotarlo en el SUMMARY si el conector hace fuente→Supabase en una pasada.
  </action>
  <verify>
    <automated>El log termina con `ingest LIVE: proyectos=N ... dbLoaded=true` con N > 0 y exit code 0 (o 1 solo por errores de boletines aislados con la mayoría cargada)</automated>
  </verify>
  <acceptance_criteria>
    - Log: `ingest LIVE` (no DRY-RUN) y `dbLoaded=true`
    - `proyectos` > 0 y `votaciones` >= 0 en el resumen
    - Si aparece `DRY-RUN` → faltó precargar .env, el service-key o el destino apuntó al local; corregir y reintentar
  </acceptance_criteria>
  <done>Tramitación cargada al cloud en lotes acotados, LIVE, idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar con psql proyecto / votacion / tramitacion_evento</name>
  <read_first>
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (helper SUPABASE_DB_URL anti-BOM; psql path)
  </read_first>
  <action>
    Extraer `SUPABASE_DB_URL` con el helper python anti-BOM de CONTEXT y verificar los tres conteos contra el cloud:
    `psql "<SUPABASE_DB_URL>" -c "select (select count(*) from proyecto) as proyectos, (select count(*) from votacion) as votaciones, (select count(*) from tramitacion_evento) as eventos;"`
    No loguear la connection string completa.
  </action>
  <verify>
    <automated>psql devuelve proyectos > 0 (votaciones/eventos esperados > 0 también, pero el gate duro es proyectos > 0)</automated>
  </verify>
  <acceptance_criteria>
    - `proyectos` > 0 (gate duro)
    - `eventos` > 0 (la tramitación trae timeline)
    - votaciones >= 0 (puede ser 0 si los boletines del lote no tenían votaciones aún; los votos se enriquecen en 20-04)
  </acceptance_criteria>
  <done>psql confirma proyecto poblado en el cloud; corpus listo para fichas (20-03) y votos (20-04).</done>
</task>

</tasks>

<verification>
- proyecto (cloud) count > 0; tramitacion_evento > 0 verificados con psql
- Corrida LIVE (no dry-run), idempotente, rate-limit respetado
</verification>

<success_criteria>
El corpus de tramitación real está en la nube; desbloquea embeddings de búsqueda y votos.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-02-SUMMARY.md` when done
</output>
</content>
