---
phase: 20-deploy-carga-de-datos-preview-privado-gov-map-com
plan: 05
type: execute
wave: 4
depends_on: ["20-03", "20-04"]
files_modified: ["app/app/layout.tsx"]
autonomous: false
requirements: []
user_setup:
  - service: supabase
    why: "El frontend lee la nube con el anon key (RLS public-read); el service key bypassearía RLS"
    env_vars:
      - name: SUPABASE_ANON_KEY
        source: "Supabase dashboard → Project Settings → API → anon public"
  - service: cloudflare
    why: "Deploy del Worker vía wrangler (build OpenNext); custom domain gov-map.com"
    dashboard_config:
      - task: "wrangler login (o CLOUDFLARE_API_TOKEN) si la sesión expiró"
        location: "terminal / Cloudflare dashboard → My Profile → API Tokens"
      - task: "(opcional) adjuntar gov-map.com como custom domain del Worker"
        location: "Cloudflare → Workers & Pages → observatorio-congreso → Settings → Domains & Routes"

must_haves:
  truths:
    - "El sitio está desplegado y accesible por URL (*.workers.dev) sirviendo data real de la nube"
    - "Los secrets de runtime del Worker (SUPABASE_URL, SUPABASE_ANON_KEY, GEMINI_API_KEY) están seteados"
    - "El sitio responde con noindex (X-Robots-Tag/meta robots) hasta la pasada legal"
    - "MONEY_PUBLIC_ENABLED ausente/false; ninguna sección MONEY en el HTML"
  artifacts:
    - path: "app/app/layout.tsx"
      provides: "metadata.robots noindex (toggle flipeable)"
      contains: "robots"
  key_links:
    - from: "Cloudflare Worker observatorio-congreso"
      to: "Supabase cloud (anon, RLS public-read)"
      via: "wrangler secret put SUPABASE_URL / SUPABASE_ANON_KEY / GEMINI_API_KEY"
      pattern: "wrangler secret put"
---

<objective>
Cablear el frontend a la nube y dejarlo DESPLEGADO EN PRODUCCIÓN en Cloudflare Workers (`observatorio-congreso`, `*.workers.dev`), con `noindex` recomendado hasta la pasada legal y MONEY/NET apagados. Incluye el toggle noindex, los secrets de runtime del Worker, y el build+deploy vía wrangler directo (con ruta de contingencia para el EPERM de symlinks en Windows).

Purpose: Es el entregable visible: una URL real que mostrar a la ONG, sirviendo la data cargada en los planes anteriores, sin indexarse ni exponer MONEY.
Output: Worker desplegado y accesible; secrets seteados; noindex activo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md
@docs/deploy-cloudflare.md
@app/wrangler.jsonc
@app/app/layout.tsx
@CLAUDE.md

<interfaces>
<!-- Contrato de env DISTINTO al de ingesta (a propósito):
     el frontend usa SUPABASE_URL + SUPABASE_ANON_KEY (anon, RLS public-read),
     NUNCA el service/secret key del .env de ingesta. -->

De app/package.json:
- `pnpm --filter app deploy` = `opennextjs-cloudflare build && opennextjs-cloudflare deploy`.
- `pnpm --filter app cf-build` = solo el build OpenNext (para aislar el fallo de symlink).
- ⚠️ usar `pnpm run deploy` / `pnpm --filter app deploy` — NUNCA `pnpm deploy` (colisiona con el builtin de pnpm).

De app/wrangler.jsonc:
- Worker name: `observatorio-congreso`, main `.open-next/worker.js`, assets `.open-next/assets`.
- Los secrets NO van aquí; se setean con `wrangler secret put`.

De app/app/layout.tsx:
- `export const metadata: Metadata = { title, description }` — sin `robots` hoy. Aquí se agrega el toggle noindex.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Pre-flight de operador (anon key, Developer Mode/WSL, wrangler login)</name>
  <action>Checkpoint de operador: el anon key del dashboard, la activación de Modo Desarrollador de Windows (o WSL) para los symlinks de OpenNext, y la sesión de wrangler no se pueden hacer headless. El operador los provee siguiendo los pasos de abajo antes de que el executor continúe.</action>
  <what-built>Pre-flight de operador para wiring + deploy (lo que el executor no puede hacer headless).</what-built>
  <how-to-verify>
    1. Pegar el anon key en `.env` (UTF-8 SIN BOM): añadir
       `SUPABASE_URL=https://bctyygbmqcvizyplktuw.supabase.co`
       y `SUPABASE_ANON_KEY=<anon public del dashboard → Project Settings → API → anon public>`.
       (El anon key es ~208 chars JWT; NO es el service/secret key.)
    2. Confirmar el deploy local: o bien activar **Modo Desarrollador de Windows** (Configuración → Privacidad y seguridad → Para desarrolladores → ON) para que OpenNext pueda crear symlinks, o bien tener **WSL** disponible para correr el build ahí. Indicar cuál.
    3. Confirmar sesión de Cloudflare: `wrangler whoami` debe responder con la cuenta; si no, correr `wrangler login` (o exportar `CLOUDFLARE_API_TOKEN`).
  </how-to-verify>
  <resume-signal>Escribe "listo" e indica: Developer Mode ON / WSL / fallback-Actions, y confirma anon key + wrangler whoami OK</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Agregar el toggle noindex en el layout raíz</name>
  <read_first>
    - app/app/layout.tsx (el objeto metadata actual, sin robots)
    - node_modules/next/dist/docs (esta NO es la Next.js que conoces — confirmar la forma de metadata.robots en Next 16 antes de editar)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (noindex recomendado hasta la pasada legal, flipeable)
  </read_first>
  <action>
    Editar `app/app/layout.tsx` para agregar a `metadata` un campo `robots` que emita noindex/nofollow (Next 16 lo traduce a `<meta name="robots" content="noindex, nofollow">`). Dejarlo como toggle legible (un único punto de cambio para flipear a indexable tras la pasada legal). Preferir que el valor sea condicionable por env para no requerir un redeploy de código para flipear — p.ej. derivar de una variable como `PUBLIC_INDEXABLE` (ausente/false → noindex; sólo el literal "true" lo hace indexable, espejo del patrón fail-closed de `moneyPublicEnabled`). Si se condiciona por env, documentar el nombre exacto de la var en el SUMMARY y notar que también hay que setearla como secret/var del Worker para que surta efecto en runtime. Mantener title/description existentes. No tocar nada más del layout.
  </action>
  <verify>
    <automated>grep -n "robots" app/app/layout.tsx devuelve la línea agregada; `pnpm --filter app typecheck` pasa</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/layout.tsx` contiene `robots` en metadata con noindex por defecto
    - El toggle a indexable es un único punto de cambio (idealmente env-driven, fail-closed)
    - typecheck verde
  </acceptance_criteria>
  <done>noindex activo por defecto, flipeable; sin otros cambios de UI.</done>
</task>

<task type="auto">
  <name>Task 2: Setear los secrets de runtime del Worker</name>
  <read_first>
    - docs/deploy-cloudflare.md (Paso 3: los 3 secrets; el anon key NO es el service key)
    - .env (SUPABASE_API_URL → valor de SUPABASE_URL; SUPABASE_ANON_KEY que pegó el operador; GEMINI_API_KEY)
  </read_first>
  <action>
    Desde `app/`, con la sesión de wrangler activa (verificada en el checkpoint), setear los tres secrets de runtime del Worker `observatorio-congreso`:
    `wrangler secret put SUPABASE_URL` → `https://bctyygbmqcvizyplktuw.supabase.co`
    `wrangler secret put SUPABASE_ANON_KEY` → el anon key del `.env` (NUNCA el service/secret key)
    `wrangler secret put GEMINI_API_KEY` → el GEMINI_API_KEY del `.env` (embeddings de /buscar en runtime)
    Pasar el valor por stdin para no exponerlo en el historial de shell. Si se eligió toggle noindex env-driven en Task 1, setear también esa variable acorde (ausente/false = noindex). NO setear `MONEY_PUBLIC_ENABLED` (debe quedar ausente). Confirmar con `wrangler secret list` que los tres nombres aparecen.
  </action>
  <verify>
    <automated>`wrangler secret list` (en app/) lista SUPABASE_URL, SUPABASE_ANON_KEY, GEMINI_API_KEY; MONEY_PUBLIC_ENABLED NO aparece</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 secrets aparecen en `wrangler secret list`
    - MONEY_PUBLIC_ENABLED ausente de la lista
  </acceptance_criteria>
  <done>Secrets de runtime del Worker seteados; MONEY no encendido.</done>
</task>

<task type="auto">
  <name>Task 3: Build + deploy del Worker vía wrangler (con contingencia EPERM)</name>
  <read_first>
    - docs/deploy-cloudflare.md (Paso 4 opción B local + nota EPERM; opción A Actions como fallback)
    - app/package.json (scripts deploy / cf-build)
    - .planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-CONTEXT.md (EPERM symlinks Windows → Developer Mode / WSL; fallback deploy-cloudflare.yml)
  </read_first>
  <action>
    Construir y desplegar el Worker. Ruta según lo confirmado en el checkpoint:
    - Si Developer Mode ON: `pnpm --filter app deploy` (build OpenNext + wrangler deploy) desde la raíz. Si falla con `EPERM: symlink`, el Developer Mode no estaba realmente activo → cambiar a WSL.
    - Si WSL: correr `pnpm --filter app deploy` dentro de WSL (mismo repo montado), con wrangler autenticado en ese entorno.
    - Fallback ÚLTIMO (única excepción a "no Actions"): disparar el workflow `deploy-cloudflare.yml` (necesita los secrets `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID=10fb709d866bb5b06dd2a5d13c8dd472` en el repo).
    Al terminar, capturar la URL del Worker que wrangler imprime (`https://observatorio-congreso.<subdominio>.workers.dev`) y registrarla en el SUMMARY. NO adjuntar gov-map.com aquí (paso de operador diferido; ver checkpoint final).
  </action>
  <verify>
    <automated>`curl -sS -o /dev/null -w "%{http_code}" https://observatorio-congreso.<subdominio>.workers.dev/` devuelve 200; `curl -sSI .../ | grep -i x-robots-tag` o el HTML contiene meta robots noindex</automated>
  </verify>
  <acceptance_criteria>
    - La URL del Worker responde HTTP 200
    - La respuesta lleva noindex (header X-Robots-Tag o `<meta name="robots" content="noindex...">` en el HTML)
    - URL desplegada registrada en el SUMMARY
  </acceptance_criteria>
  <done>Sitio desplegado y accesible en *.workers.dev, sirviendo SSR, con noindex.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: (Opcional) Adjuntar gov-map.com como custom domain</name>
  <action>Checkpoint de operador: adjuntar el dominio propio en el dashboard de Cloudflare requiere acceso a la cuenta y propagación DNS — no es headless. Si el dominio aún no está en la cuenta, se SALTA y la URL *.workers.dev queda como entregable.</action>
  <what-built>Worker desplegado en *.workers.dev. Falta (opcional) el dominio gov-map.com.</what-built>
  <how-to-verify>
    Si gov-map.com ya está en la cuenta Cloudflare y se quiere el dominio propio ahora:
    Cloudflare → Workers & Pages → observatorio-congreso → Settings → Domains & Routes → Add custom domain → gov-map.com. Esperar la propagación DNS. Si el dominio aún NO está en la cuenta, SALTAR este paso: la URL *.workers.dev ya es la entregable para la ONG (entra por link directo, noindex activo).
  </how-to-verify>
  <resume-signal>Escribe "dominio adjuntado" + la URL final, o "sin dominio, usar *.workers.dev"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Supabase (anon) | El frontend público lee con anon key bajo RLS public-read; el service key NUNCA debe llegar al Worker |
| Public internet → sitio | Sitio accesible por URL; noindex evita indexación hasta la pasada legal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | wrangler secret SUPABASE_ANON_KEY | mitigate | Setear el ANON key, NUNCA el service/secret (docs/deploy-cloudflare.md Paso 3); `wrangler secret list` confirma nombres, no valores |
| T-20-02 | Information Disclosure | MONEY data en el HTML | mitigate | NO setear MONEY_PUBLIC_ENABLED; gate fail-closed envuelve la <section> entera (Phase 13/14) → nodo ausente del DOM; verificado en 20-06 |
| T-20-03 | Information Disclosure | Indexación prematura | mitigate | noindex por defecto (Task 1) hasta sign-off legal Ley 21.719; toggle fail-closed |
| T-20-04 | Elevation of Privilege | RLS bypass | accept | RLS public-read + tablas PII deny-by-default ya verificadas en Phases 5/9-12; el anon key respeta RLS por diseño |
</threat_model>

<verification>
- Worker accesible (HTTP 200) en *.workers.dev sirviendo data real
- noindex presente en la respuesta
- 3 secrets seteados; MONEY_PUBLIC_ENABLED ausente
</verification>

<success_criteria>
El Observatorio está en producción, accesible por URL, cableado a la nube poblada, con noindex y MONEY/NET apagados.
</success_criteria>

<output>
Create `.planning/phases/20-deploy-carga-de-datos-preview-privado-gov-map-com/20-05-SUMMARY.md` when done (incluir la URL desplegada)
</output>
