---
phase: 73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano
plan: 03
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [MONEY-04]

must_haves:
  truths:
    - "El linter anti-insinuación escanea las 4 superficies MONEY + /contraparte, además de las de voto."
    - "El linter bloquea vocabulario causal/insinuante de dinero ('empresa ligada a', 'financió', 'a cambio de', etc.) con tildes exactas."
    - "La leyenda MONEY está restada de NEGACIONES_LOCKED (no se auto-caza)."
    - "El mutation self-check MONEY muerde; 'Enlazado por RUT' (hecho) y 'empresa_ligada_por_rut' (identificador) NO disparan falsos positivos."
  artifacts:
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "Linter extendido: SUPERFICIES_MONEY + blocklist causal MONEY + leyenda MONEY en NEGACIONES_LOCKED + self-check MONEY"
      contains: "SUPERFICIES_MONEY"
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/lib/money-presentacion.ts"
      via: "import LEYENDA_ANTI_INSINUACION_MONEY (restada en NEGACIONES_LOCKED)"
      pattern: "LEYENDA_ANTI_INSINUACION_MONEY"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/components/*-parlamentario.tsx / *-contraparte.tsx + app/contraparte/[id]/page.tsx"
      via: "readFileSync + detectarInsinuaciones"
      pattern: "contratos-por-contraparte"
---

<objective>
Extender el linter anti-insinuación de Phase 68 (`app/lib/anti-insinuacion-guard.test.ts`) para cubrir las 4 superficies MONEY + `app/contraparte/[id]/page.tsx`, con la blocklist causal/insinuante de dinero (tildes exactas), restando la leyenda MONEY de `NEGACIONES_LOCKED` para que no se auto-cace, y un mutation self-check MONEY que prueba que el guard muerde. Reusar los helpers existentes (`stripTsComments`, `buildTermRegex`, `detectarInsinuaciones`) — extender constantes, no reescribir.

Purpose: MONEY-04 — el linter anti-vocabulario-insinuante cubre las superficies MONEY, bloqueando causalidad dinero→decisión y la insinuación "empresa ligada a", mientras permite el HECHO "ligada por RUT" / "Enlazado por RUT".
Output: `anti-insinuacion-guard.test.ts` extendido; verde hoy contra el árbol (las superficies MONEY, ya con leyenda del plan 02, no contienen términos causales).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-UI-SPEC.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-RESEARCH.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-01-SUMMARY.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-01-SUMMARY.md
@app/AGENTS.md

<interfaces>
<!-- Estructura del guard existente a EXTENDER (helpers reusados verbatim): -->
From app/lib/anti-insinuacion-guard.test.ts:
```typescript
function stripTsComments(content: string): string   // reusar
function buildTermRegex(term: string): RegExp        // reusar (WORD tolerante a acentos)
function detectarInsinuaciones(rawContent: string): string[]  // strip → resta NEGACIONES → match

const SUPERFICIES_VOTO: string[] = [ /* rutas de voto, incl. app/parlamentario/[id]/page.tsx */ ];
const TERMINOS_PROHIBIDOS: string[] = [ /* rebeldía, disciplina, …, "financió su voto", "a cambio de" */ ];
const NEGACIONES_LOCKED: string[] = [ "Un voto es un hecho observable. …No medimos disciplina ni motivo." ];

// El bucle del guard: for (const rel of SUPERFICIES_VOTO) { readFileSync; detectarInsinuaciones; }
//   una ruta ausente se SALTA (try/catch continue).
```

<!-- La leyenda MONEY del plan 01 (a restar en NEGACIONES_LOCKED): -->
From app/lib/money-presentacion.ts:
```typescript
export const LEYENDA_ANTI_INSINUACION_MONEY: string;
// contiene "influencia"/"intención"/"irregularidad" en contexto NEGADO → debe restarse.
```

<!-- Alcance MONEY a añadir (RESEARCH §Pattern 3, rutas relativas a app/): -->
// components/contratos-de-parlamentario.tsx, components/financiamiento-de-parlamentario.tsx,
// components/contratos-por-contraparte.tsx, components/aportes-por-contraparte.tsx,
// app/contraparte/[id]/page.tsx
// (app/parlamentario/[id]/page.tsx YA está en SUPERFICIES_VOTO.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el linter con SUPERFICIES_MONEY + blocklist causal + leyenda MONEY negada</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (COMPLETO: helpers `stripTsComments`/`buildTermRegex`/`detectarInsinuaciones`, arrays `SUPERFICIES_VOTO`/`TERMINOS_PROHIBIDOS`/`NEGACIONES_LOCKED`, el bucle del guard, los 3 describe)
    - app/lib/money-presentacion.ts (constante `LEYENDA_ANTI_INSINUACION_MONEY` a importar y restar)
    - .planning/phases/73-.../73-RESEARCH.md §Pattern 3 + §Pitfall 1/2/3 (leyenda restada; tildes exactas; "empresa ligada a" bloqueado vs "ligada por RUT"/"Enlazado por RUT" permitido)
    - .planning/phases/73-.../73-UI-SPEC.md §Linter (la lista dura de términos MONEY)
    - .planning/phases/68-.../68-01-SUMMARY.md (el molde del linter y su mutation self-check)
  </read_first>
  <action>
    Extender el MISMO archivo (no crear uno nuevo). Cambios:

    1. Importar `LEYENDA_ANTI_INSINUACION_MONEY` desde `@/lib/money-presentacion` (o ruta relativa equivalente) y añadirla a `NEGACIONES_LOCKED` (junto a la leyenda de voto), para que el detector la reste ANTES de matchear — la leyenda MONEY niega "influencia"/"intención"/"irregularidad" y sin restarla el guard se auto-cazaría (Pitfall 1).

    2. Definir `SUPERFICIES_MONEY: string[]` (rutas relativas a app/): `components/contratos-de-parlamentario.tsx`, `components/financiamiento-de-parlamentario.tsx`, `components/contratos-por-contraparte.tsx`, `components/aportes-por-contraparte.tsx`, `app/contraparte/[id]/page.tsx`. NO duplicar `app/parlamentario/[id]/page.tsx` (ya está en SUPERFICIES_VOTO). El bucle del guard debe escanear `[...SUPERFICIES_VOTO, ...SUPERFICIES_MONEY]` (concatenación; preferir mantener los arrays separados por legibilidad del corte voto/dinero, per RESEARCH §Pattern 3 decisión (b)). Conservar el try/catch que salta rutas ausentes.

    3. Añadir los términos MONEY a `TERMINOS_PROHIBIDOS` (con TILDES EXACTAS — Pitfall 2, `buildTermRegex` NO es accent-insensitive): `financió`, `financió su voto`, `a cambio del voto`, `compró`, `compró su voto`, `pagó por`, `soborno`, `coima`, `corrupción`, `favoreció`, `empresa ligada a`, `conflicto de interés`, `influencia`, `captura`, `lobby a cambio`, `contrato a dedo`, `direccionado`. ("a cambio de" ya está por el carril de voto.) CRÍTICO (Pitfall 3): el término bloqueado es la frase insinuante `empresa ligada a` (con la preposición `a`), NO `ligada por RUT` — el hecho "Enlazado por RUT al parlamentario." y el identificador snake_case `empresa_ligada_por_rut` NO deben disparar por el límite de palabra tolerante a acentos.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - El guard escanea las 4 superficies MONEY + `app/contraparte/[id]/page.tsx` (grep-verificable: `contratos-por-contraparte` y `contraparte/[id]/page.tsx` aparecen en el archivo).
    - `LEYENDA_ANTI_INSINUACION_MONEY` importada y presente en `NEGACIONES_LOCKED`.
    - La blocklist MONEY está en `TERMINOS_PROHIBIDOS` con tildes exactas (`financió`, `corrupción`, `favoreció`, `conflicto de interés`, `empresa ligada a`).
    - El guard corre VERDE hoy (0 offenders) contra el árbol con las superficies del plan 02 (la leyenda restada no se auto-caza; el copy real no contiene términos causales).
    - Reusa `stripTsComments`/`buildTermRegex`/`detectarInsinuaciones` (no los reescribe).
  </acceptance_criteria>
  <done>El linter cubre las superficies MONEY con la blocklist causal, sin auto-cazarse por la leyenda.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation self-check MONEY + tests de no-falso-positivo</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (los describe (2) Mutation self-check y (3) Sin falsos positivos — el molde a espejar para MONEY)
    - .planning/phases/73-.../73-RESEARCH.md §Pitfall 1/3 y §Validation "no-falso-positivo"
    - .planning/phases/73-.../73-UI-SPEC.md §Linter (mutation self-check MONEY exacto)
  </read_first>
  <action>
    Añadir, dentro del mismo archivo, casos MONEY espejando los bloques (2) y (3) existentes:

    - Mutation self-check MONEY (anti no-op): `detectarInsinuaciones("<p>esta empresa financió su voto a cambio de un contrato</p>")` debe contener `financió su voto` (y/o `a cambio de`). Un segundo fixture con `<p>empresa ligada a un caso de corrupción; conflicto de interés evidente</p>` debe cazar `empresa ligada a`, `corrupción`, `conflicto de interés`.
    - No-falso-positivo MONEY: (a) la frase factual real `"Enlazado por RUT al parlamentario."` NO dispara; (b) `"Asociado por nombre confirmado al candidato."` NO dispara; (c) el identificador snake_case `empresa_ligada_por_rut` (p.ej. en `sb.rpc("empresa_ligada_por_rut", {})`) NO dispara por el límite de palabra; (d) montar `LEYENDA_ANTI_INSINUACION_MONEY` en un fixture y esperar `[]` (la leyenda restada no es offender — Pitfall 1); (e) `"contratos a dedo"` como término renderizado SÍ dispara `contrato a dedo` (verificar el idiom del término elegido no rompe por número/plural — si el UI-SPEC lista `contrato a dedo`, probar el singular).

    Importar `LEYENDA_ANTI_INSINUACION_MONEY` para el caso (d) (no re-tipear el texto). No tocar los casos de voto existentes.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - El mutation self-check MONEY muerde: caza `financió su voto`/`a cambio de` y `empresa ligada a`/`corrupción`/`conflicto de interés` en fixtures EN MEMORIA.
    - No-falso-positivo: "Enlazado por RUT al parlamentario.", "Asociado por nombre confirmado al candidato.", `empresa_ligada_por_rut` y la leyenda MONEY completa NO disparan.
    - Los casos de voto existentes siguen verdes; el guard entero pasa.
    - `pnpm --filter ./app test` verde (sin regresión).
  </acceptance_criteria>
  <done>El linter MONEY muerde ante causalidad/insinuación y no genera falsos positivos sobre el copy factual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy MONEY → ciudadano | Un término causal/insinuante en el render sería difamación/editorialización |
| guard mismo → suite | Un guard no-op verde no protegería nada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Repudiation / reputación (difamación) | 4 superficies MONEY + /contraparte | mitigate | Linter blocklist causal ("financió"/"a cambio de"/"empresa ligada a"/…) con tildes exactas; escaneo estático de las superficies. |
| T-73-08 | Repudiation | financiamiento/aportes por nombre | mitigate | "empresa ligada a" bloqueado; "ligada por RUT"/"Enlazado por RUT" permitidos como hecho — no se conflaciona base RUT con by-name. |
| T-73-03 | Tampering (guard mismo) | anti-insinuacion-guard.test.ts | mitigate | Mutation self-check MONEY en memoria: prueba que el guard muerde; leyenda MONEY restada evita auto-caza (no-op falso-positivo). |
</threat_model>

<verification>
- `pnpm --filter ./app test anti-insinuacion-guard` verde (voto + MONEY, self-check + no-falso-positivo).
- `pnpm --filter ./app test` verde.
- El guard escanea las 4 superficies MONEY + /contraparte (grep-verificable).
</verification>

<success_criteria>
- El linter cubre las superficies MONEY con blocklist causal (tildes exactas).
- La leyenda MONEY restada; "Enlazado por RUT" no dispara; mutation self-check muerde.
- Sin regresión sobre el carril de voto.
</success_criteria>

<output>
Create `.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-03-SUMMARY.md` when done
</output>
