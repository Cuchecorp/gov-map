---
phase: 10-vote-voto-individual-por-parlamentario-en-la-ficha
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/votos/package.json
  - packages/votos/src/index.ts
  - packages/votos/src/run-camara-votos.ts
  - packages/votos/src/run-camara-votos.test.ts
  - packages/votos/src/run-camara-votos.live.test.ts
autonomous: false
requirements: [VOTE-02]
user_setup: []

must_haves:
  truths:
    - "El runner @obs/votos ingiere el voto individual por diputado de la Cámara y escribe a Supabase"
    - "El cruce DIPID→id_diputado_camara es determinista, sin LLM, minteando EnlaceConfirmado"
    - "Re-correr el runner con el mismo input NO duplica filas (idempotente por clave natural)"
    - "Cada voto lleva provenance por fila; un DIPID fuera de la maestra queda no_confirmado (fail-closed)"
    - "La corrida LIVE es acotada a la legislatura vigente y nunca fabrica votos"
  artifacts:
    - path: "packages/votos/src/run-camara-votos.ts"
      provides: "runner de producción Cámara-only que ensambla colaboradores LOCKED y escribe a Supabase"
      min_lines: 40
    - path: "packages/votos/src/run-camara-votos.test.ts"
      provides: "test offline (InMemoryWriter + fixtures): idempotencia + cruce DIPID + provenance"
      contains: "InMemoryTramitacionWriter"
    - path: "packages/votos/src/run-camara-votos.live.test.ts"
      provides: "test LIVE-gated (VOTOS_LIVE=1) corrida acotada"
      contains: "VOTOS_LIVE"
    - path: "packages/votos/src/index.ts"
      provides: "barrel que exporta el runner de producción"
      contains: "run-camara-votos"
  key_links:
    - from: "run-camara-votos.ts"
      to: "@obs/tramitacion (runIngest / reconciliarVotosCamara / SupabaseTramitacionWriter)"
      via: "import + ensamblaje LOCKED"
      pattern: "reconciliarVotosCamara|runIngest"
    - from: "run-camara-votos.ts"
      to: "EnlaceConfirmado"
      via: "reconciliarVotosCamara mintea confirmar()"
      pattern: "confirmar|EnlaceConfirmado"
---

<objective>
Promueve el spike de Phase 8 (`packages/votos/spike`) a un conector de producción `packages/votos/src` que ENRIQUECE el modelo `voto`/`votacion` existente (NO forka). Es un runner DELGADO que reusa verbatim los símbolos v1.0 de `@obs/tramitacion`/`@obs/ingest` — cero ingeniería nueva de fetch/parse/cruce — y cambia respecto del spike SOLO en dos cosas: (1) escribe a Supabase vía `SupabaseTramitacionWriter` (no in-memory) y (2) acota la corrida a la legislatura vigente.

Purpose: poblar `voto`/`votacion` con el voto individual por diputado cruzado determinísticamente por DIPID, con provenance por fila e idempotencia por clave natural, para que la ficha del parlamentario (Plan 03) lo lea.
Output: paquete `@obs/votos/src` de producción + tests offline y LIVE-gated; `spike/` queda obsoleto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-RESEARCH.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-01-SUMMARY.md

<read_first>
Lee ANTES de escribir — el runner ensambla estos símbolos verbatim (NO los reimplementes):

- `packages/votos/spike/spike.ts:18-97` — el ensamblaje LOCKED del `CamaraConnector` con `Fetcher`/`HostRateLimiter`/`RobotsGuard` y `allowlist={}` (el default cubre `opendata.camara.cl`). `buildCamaraConnector()` es el patrón a heredar en producción.
- `packages/votos/package.json` — hoy solo tiene `@obs/{core,ingest,tramitacion}`. Añade `@obs/identity` si el runner importa `confirmar`/`EnlaceConfirmado` directo (hoy lo trae transitivo vía `@obs/tramitacion`). Apunta `main`/`exports` a `src/index.ts`.
- `packages/tramitacion/src/ingest-run.ts:106-200` — `runIngest({ boletines, legislaturaId, limite, maestra, camara, senado, writer, log })`: orquesta descubrir→fetch→parse→reconciliar→upsert, tolera fuentes vacías, aísla errores por boletín (`errores[]`, no aborta), idempotente. El runner Cámara-only puede REUSAR `runIngest` (degrada sin provider Senado fail-closed) o llamar un subset Cámara-only — discreción del planner; preferir reusar `runIngest` (menos código, ya degrada).
- `packages/tramitacion/src/reconciliar-camara.ts:55-108` — cruce determinista DIPID→`id_diputado_camara`; en match mintea `confirmar(p.id,"determinista")` → `estado_vinculo='confirmado'`; en miss → `enlace:null`, `no_confirmado` (fail-closed). NO lo modifiques.
- `packages/tramitacion/src/writer.ts:28-66` + `writer-supabase.ts` — `SupabaseTramitacionWriter` upsert idempotente por `(votacion_id, fuente_voter_id)`; `InMemoryTramitacionWriter` para tests. El FK del voto es `EnlaceConfirmado | null` (IDENT-12): un string crudo NO compila.
- `packages/tramitacion/src/ingest-cli.ts` — `cargarMaestra(findWorkspaceRoot(...), log)` lee el seed autoritativo (155 DIPID) sin tocar DB. `parseArgs`/`main` muestran el ensamblaje de prod con `SupabaseTramitacionWriter`.
- `packages/votos/vitest.config.ts` — config existente (los tests del runner viven aquí).
- `.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-SUMMARY.md` — la muestra LIVE confirmada (boletines 14309/18296, Leg-58), 0 errores, delay 2-3s LOCKED reflejado.
- `.planning/phases/10-vote-.../10-01-SUMMARY.md` — el mapeo de códigos del WS a las 5 opciones (Plan 01); la corrida LIVE confirma los códigos no-nominales asumidos (Assumption A1/A2).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runner de producción @obs/votos/src + test offline</name>
  <files>packages/votos/package.json, packages/votos/src/index.ts, packages/votos/src/run-camara-votos.ts, packages/votos/src/run-camara-votos.test.ts</files>
  <behavior>
    - Correr el runner 2× con el mismo input (InMemoryWriter) deja `writer.votos.size` estable (idempotencia VOTE-02).
    - Un DIPID presente en la maestra → fila con `estado_vinculo='confirmado'`, `metodo='determinista'`, `parlamentario_id` poblado vía EnlaceConfirmado.
    - Un DIPID ausente de la maestra → `parlamentario_id=null`, `estado_vinculo='no_confirmado'` (fail-closed), conservando `mencion_nombre`.
    - Cada votación escrita lleva `origen`/`fecha_captura`/`enlace` (provenance por fila).
    - El runner acota por `boletines` explícitos o `limite` (corrida acotada, nunca todo a ciegas).
  </behavior>
  <action>Crea `packages/votos/src/run-camara-votos.ts`: una función `runCamaraVotos(opts)` que (a) construye el `CamaraConnector` con la política LOCKED de `@obs/ingest` heredando el patrón `buildCamaraConnector()` del spike (allowlist `{}`, `HostRateLimiter` 2-3s, `RobotsGuard`); (b) carga la maestra con `cargarMaestra(findWorkspaceRoot(...))` o la recibe inyectada para tests; (c) elige el writer: `SupabaseTramitacionWriter({url, serviceKey})` si hay `SUPABASE_URL`+`SUPABASE_LOCAL_SERVICE_KEY`, si no `InMemoryTramitacionWriter` (dry-run); (d) llama `runIngest({ boletines, legislaturaId: 58, limite, maestra, camara, senado, writer, log })` reusando el pipeline existente. NO reimplementes fetch/parse/cruce/upsert — solo ensambla. Acota la corrida: requiere `boletines` explícitos o un `limite` razonable (Leg-58 vigente). Exporta el runner y sus tipos. Crea `src/index.ts` (barrel) exportando `runCamaraVotos`. Actualiza `package.json`: `main`/`exports` → `./src/index.ts`, añade `@obs/identity` a dependencies si lo importas directo. Crea `run-camara-votos.test.ts` OFFLINE: inyecta un `CamaraConnector` fake/stub que devuelve fixtures XML (reusa el shape real capturado en Phase 8 / fixtures de `@obs/tramitacion`), un `InMemoryTramitacionWriter` y una maestra de prueba con ≥1 DIPID que cruza y ≥1 que no; assert sobre idempotencia (2 corridas, mismo `votos.size`), cruce confirmado/no_confirmado, y provenance presente. NO uses red en el test offline.</action>
  <verify>
    <automated>pnpm --filter @obs/votos test -- run-camara-votos.test</automated>
  </verify>
  <done>`run-camara-votos.ts` ensambla los colaboradores LOCKED y llama `runIngest`; el test offline pasa probando idempotencia, cruce DIPID determinista (confirmado/no_confirmado) y provenance; `package.json` exporta `src/index.ts`; el paquete typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Test LIVE-gated + obsoletar el spike</name>
  <files>packages/votos/src/run-camara-votos.live.test.ts, packages/votos/package.json</files>
  <action>Crea `run-camara-votos.live.test.ts` gateado por `VOTOS_LIVE=1` (skip si la flag no está, espejando el patrón `VOTE_SPIKE_LIVE` del spike). Corre `runCamaraVotos` con `boletines` explícitos de Leg-58 (los confirmados en Phase 8: `["14309","18296"]`) y un `limite` pequeño, con `InMemoryTramitacionWriter` (NO escribe a DB en el test — la escritura real a Supabase es paso de operador en Task 3). Assert: la corrida no lanza, hay ≥1 votación con ≥1 voto confirmado, los `errores[]` de `runIngest` se registran (no abortan), y NINGÚN voto carece de provenance. Respeta el delay 2-3s LOCKED (no lo override). Marca el archivo y el spike: deja un comentario en `spike/spike.ts` indicando que está OBSOLETO (reemplazado por `src/`) — opcionalmente elimina `spike/` si el runner cubre su rol, pero conserva el FINDINGS histórico referenciado en 08-SUMMARY. NO borres 08-SUMMARY.</action>
  <verify>
    <automated>pnpm --filter @obs/votos test -- run-camara-votos.live.test</automated>
  </verify>
  <done>`run-camara-votos.live.test.ts` existe, gateado por `VOTOS_LIVE=1`, skippea sin la flag (suite verde sin red); el spike queda marcado obsoleto. La suite offline completa de `@obs/votos` pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Corrida LIVE acotada + ingesta a Supabase (operador)</name>
  <action>Checkpoint de operador: la corrida LIVE contra el WAF gubernamental y la escritura real a Supabase no se hacen en CI (no determinista, rate-limited). El operador corre el runner acotado y verifica idempotencia/cruce/mapeo siguiendo los pasos abajo.</action>
  <what-built>Runner de producción `@obs/votos` que reusa el pipeline LIVE-validado de Phase 8. La corrida LIVE real contra `opendata.camara.cl` (WAF, rate-limited, no determinista) y la escritura a Supabase NO se hacen en CI — son paso de operador.</what-built>
  <how-to-verify>
    1. Corrida LIVE acotada con escritura al LOCAL: exporta `SUPABASE_URL` + `SUPABASE_LOCAL_SERVICE_KEY` y corre el runner con `boletines=["14309","18296"]` Leg-58 (vía el CLI/test del runner con `VOTOS_LIVE=1`). Espera 0 errores de red, delay 2-3s reflejado en la latencia.
    2. Verifica idempotencia EN VIVO: corre 2× y confirma que el conteo de filas `voto` no cambia entre corridas (upsert por clave natural).
    3. Verifica el cruce: filas con DIPID en la maestra → `estado_vinculo='confirmado'`, `parlamentario_id` poblado; las demás → `no_confirmado`, `parlamentario_id=null`. Ninguna fila sin `origen`/`enlace`.
    4. Confirma EN VIVO el mapeo de códigos no-nominales (Assumption A1/A2 de Plan 01): qué código del WS corresponde a Abstención/Pareo/Ausente y si vienen con DIPID por diputado. Si una opción NO trae DIPID individual, déjala como agregado honesto (no fabricar filas) y anótalo.
    5. (Opcional remoto) Si se expone esta fase: repetir contra el pooler sa-east-1 tras aplicar 0019 (Plan 01 Task 3).
  </how-to-verify>
  <resume-signal>Escribe "ingerido" + un resumen (boletines, n votos, n confirmados, errores, idempotencia OK, mapeo de códigos verificado), o describe el fallo / degradación.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/votos test` verde (offline; LIVE skippeado sin flag).
- El runner reusa `runIngest`/`reconciliarVotosCamara`/`SupabaseTramitacionWriter` (no reimplementa).
- Idempotencia verificada (offline + LIVE operador).
- Provenance por fila; fail-closed en DIPID fuera de la maestra.
</verification>

<success_criteria>
- `@obs/votos` de producción ingiere el voto individual por diputado y enriquece `voto`/`votacion` por DIPID determinista sin LLM, con provenance por fila (VOTE-02).
- Idempotente por clave natural; corrida acotada a Leg-58; nunca fabrica (VOTE-02).
- El FK se fija solo vía `EnlaceConfirmado` (IDENT-12).
</success_criteria>

<output>
Create `.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-02-SUMMARY.md` when done. Documenta la corrida LIVE (qué se ingirió, errores, idempotencia, mapeo de códigos confirmado) o la degradación honesta si el entorno no permitió la corrida completa.
</output>
