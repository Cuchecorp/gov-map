---
phase: 10-vote-voto-individual-por-parlamentario-en-la-ficha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/src/model.ts
  - packages/tramitacion/src/parse-camara-votacion.ts
  - packages/tramitacion/src/parse-camara-votacion.test.ts
  - app/lib/types.ts
  - supabase/migrations/0019_voto_asistencia_y_ficha.sql
  - supabase/tests/0019_voto_asistencia_y_ficha.test.sql
autonomous: false
requirements: [VOTE-03, VOTE-04, VOTE-05]
user_setup: []

must_haves:
  truths:
    - "voto.seleccion admite 'ausente' además de si/no/abstencion/pareo"
    - "parseCamaraVotoDetalle emite las 5 opciones por diputado (no solo sí/no nominal)"
    - "Existe un índice por voto(parlamentario_id) para la query de la ficha del parlamentario"
    - "anon NO puede leer parlamentario.partido pero SÍ puede invocar el RPC de rebeldías"
    - "El RPC de votos paginado y el RPC de rebeldías devuelven solo datos públicos derivados"
  artifacts:
    - path: "supabase/migrations/0019_voto_asistencia_y_ficha.sql"
      provides: "ausente al CHECK + índice parcial voto(parlamentario_id) + RPCs votos_de_parlamentario/rebeldias_de_parlamentario + grants anon"
      contains: "create or replace function rebeldias_de_parlamentario"
    - path: "supabase/tests/0019_voto_asistencia_y_ficha.test.sql"
      provides: "pgTAP: ausente CHECK pasa, índice existe, anon no lee partido, anon ejecuta los RPCs"
      contains: "select plan("
    - path: "packages/tramitacion/src/parse-camara-votacion.ts"
      provides: "parser extendido a 5 opciones (Seleccion completo)"
      contains: "abstencion"
    - path: "packages/tramitacion/src/model.ts"
      provides: "Seleccion + VotoSchema con 'ausente'"
      contains: "ausente"
    - path: "app/lib/types.ts"
      provides: "Seleccion del frontend con 'ausente'"
      contains: "ausente"
  key_links:
    - from: "reconciliarVotosCamara"
      to: "parseCamaraVotoDetalle"
      via: "CamaraVotoDetalle.opcion (ahora 5 opciones)"
      pattern: "opcion"
    - from: "rebeldias_de_parlamentario"
      to: "parlamentario.partido"
      via: "security definer interno"
      pattern: "security definer"
---

<objective>
Aterriza la base de datos y de parser que la ficha del parlamentario necesita, SIN tocar el conector ni el frontend de render todavía. Tres piezas acopladas que comparten el modelo `voto`/`votacion`:

1. Extender el tipo `Seleccion` y `parseCamaraVotoDetalle` para emitir las 5 opciones por diputado (si/no/abstencion/pareo/ausente), en vez de descartar todo lo no-nominal (cierra el GAP de VOTE-03).
2. Migración `0019` que: añade `ausente` al CHECK de `voto.seleccion`; crea el índice parcial `voto(parlamentario_id)`; crea el RPC `votos_de_parlamentario` (security invoker, lista paginada) y el RPC `rebeldias_de_parlamentario` (security definer, conteo+lista derivada que NUNCA expone `partido` a anon); otorga `execute` de ambos RPCs a anon.
3. pgTAP que prueba el CHECK extendido, la existencia del índice, que anon NO lee `parlamentario.partido`, y que anon SÍ puede ejecutar los RPCs.

Purpose: el conector (Plan 02) escribe `ausente`/`abstencion`/`pareo` como filas propias y la ficha (Plan 03) lee vía estos RPCs. Sin esta base, la asistencia no es mostrable honestamente y rebeldías filtraría afiliación política (LEGAL-03).
Output: parser + tipos extendidos + migración 0019 + pgTAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-RESEARCH.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-UI-SPEC.md

<read_first>
Lee ANTES de escribir — estos archivos definen los contratos exactos a extender:

- `packages/tramitacion/src/parse-camara-votacion.ts:197-263` — `parseCamaraVotoDetalle` + `opcionDeVoto`. HOY `opcionDeVoto` devuelve `null` para todo lo que no sea código 1 (sí) / 0 (no), y el caller hace `continue` (descarta). El tipo `CamaraVotoDetalle.opcion` es `"si" | "no"`.
- `packages/tramitacion/src/model.ts:14` (`export type Seleccion = "si" | "no" | "abstencion" | "pareo";`), `:123-131` (`VotoSchema` con `seleccion: z.enum(["si","no","abstencion","pareo"])`). Extender ambos con `"ausente"`.
- `packages/tramitacion/src/reconciliar-camara.ts:55-108` — consume `CamaraVotoDetalle.opcion` y lo pasa a `seleccion`. NO se modifica, pero su tipo de entrada se ensancha automáticamente al extender `opcion`; verifica que sigue compilando.
- `app/lib/types.ts:7` (`export type Seleccion = "si" | "no" | "abstencion" | "pareo";`). Extender con `"ausente"`.
- `supabase/migrations/0008_tramitacion.sql:55-66` — tabla `voto`: CHECK `seleccion in ('si','no','abstencion','pareo')`, índice solo `voto_votacion_id_idx`, RLS public-read + grant select a anon.
- `supabase/migrations/0009_voto_fuente_voter_id.sql` — clave natural `(votacion_id, fuente_voter_id)` (NO la toques; el CHECK de seleccion se altera sobre la tabla de 0008).
- `supabase/migrations/0011_fichas_embeddings.sql:55-90` — patrón EXACTO de RPC `language sql stable` + `grant execute on function ... to anon` sobre firma exacta (copia este patrón para `votos_de_parlamentario`).
- `supabase/migrations/0018_piso_pii.sql` — patrón deny-by-default de `parlamentario` (RLS enabled, cero policies). `rebeldias_de_parlamentario` DEBE ser `security definer set search_path = ''` para leer `partido` internamente sin abrir policy a anon.
- `supabase/tests/0018_piso_pii.test.sql` — patrón pgTAP EXACTO (`begin; select plan(N); ... select * from finish(); rollback;`, `has_column`, `throws_ok`, `is_empty` sobre `pg_policy`). Copia este esqueleto.
- `packages/core/src/parlamentario.ts:54` — `partido: string | null` (dato sensible, afiliación política, deny-by-default).
- `.planning/phases/08-vote-spike-validaci-n-en-vivo-de-opendata-camara-cl/08-SUMMARY.md` — códigos del WS confirmados LIVE: `1`=Afirmativo→si, `0`=En Contra→no, `4`=No Vota. Abstención/Pareo/dispensado/ausente NO fueron confirmados con código exacto (Assumption A1).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender Seleccion y parseCamaraVotoDetalle a las 5 opciones</name>
  <files>packages/tramitacion/src/model.ts, packages/tramitacion/src/parse-camara-votacion.ts, packages/tramitacion/src/parse-camara-votacion.test.ts, app/lib/types.ts</files>
  <behavior>
    - `Seleccion` (en `model.ts` y en `app/lib/types.ts`) incluye `"ausente"` → 5 valores: si/no/abstencion/pareo/ausente.
    - `VotoSchema.seleccion` (zod) acepta los 5 valores.
    - `parseCamaraVotoDetalle` sobre un fixture con `<Opcion Codigo="4">No Vota</Opcion>` emite una fila con `opcion` mapeada (NO la descarta).
    - `opcionDeVoto` mapea: código 1 / "a favor|afirmativ" → si; código 0 / "en contra|negativ" → no; "abstenci" → abstencion; "pareo" → pareo; "no vota"/"dispensad"/ausencia → ausente. Mapeo guiado por el catálogo real; los códigos no confirmados (Assumption A1) se resuelven por el texto de `#text` y se documentan en el SUMMARY para verificación LIVE.
    - NUNCA fabrica un sí/no: una opción no-nominal jamás se colapsa a si/no.
  </behavior>
  <action>Extiende `CamaraVotoDetalle.opcion` y `opcionDeVoto` en `parse-camara-votacion.ts` para devolver `Seleccion` completo (5 valores) en vez de `"si" | "no" | null`. El caller en `parseCamaraVotoDetalle` deja de hacer `continue` para opciones no-nominales: cada diputado del detalle produce una fila con su opción real. Mapea por código `@_Codigo` cuando sea conocido (1→si, 0→no, 4→ausente per Phase 8) y por el texto `#text` para los códigos no confirmados aún (regex sobre "abstenci"→abstencion, "pareo"→pareo, "no vota"/"dispensad"→ausente). Para `ausente`, deriva del roster de la votación según UI-SPEC §3.6/Assumption A2: el diputado aparece en `<Votos>` con código de no-asistencia — NUNCA derives ausente de la ausencia de fila. Si una opción no trae DIPID por diputado (solo total), NO inventes filas: documéntalo y déjalo como agregado (verificación LIVE en Plan 02). Extiende `Seleccion` en `model.ts:14` y `VotoSchema` en `model.ts:128` con `"ausente"`, e idéntico en `app/lib/types.ts:7`. Verifica que `reconciliar-camara.ts` sigue compilando (su `seleccion` ahora acepta los 5). Añade casos al test del parser cubriendo abstencion/pareo/ausente sobre fixtures (reusa/extiende los fixtures del shape real tempuri.org ya presentes en el test). NO fabriques: si un fixture LIVE no está disponible para una opción, marca el caso con un fixture sintético del shape documentado y anótalo.</action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test -- parse-camara-votacion</automated>
  </verify>
  <done>`Seleccion` tiene 5 valores en ambos archivos; `VotoSchema` acepta `ausente`; `parseCamaraVotoDetalle` emite filas para abstencion/pareo/ausente sobre el fixture; el test del parser pasa; `@obs/tramitacion` typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migración 0019 — ausente CHECK + índice parcial + RPCs votos/rebeldías + grants</name>
  <files>supabase/migrations/0019_voto_asistencia_y_ficha.sql, supabase/tests/0019_voto_asistencia_y_ficha.test.sql</files>
  <action>Crea `0019_voto_asistencia_y_ficha.sql` con, en orden: (a) `alter table voto drop constraint voto_seleccion_check;` seguido de `alter table voto add constraint voto_seleccion_check check (seleccion in ('si','no','abstencion','pareo','ausente'));` — verifica el nombre real del constraint generado por 0008 (`voto_seleccion_check` es el default de Postgres para `seleccion ... check`; si difiere, usa `select conname` o el nombre exacto). (b) `create index if not exists voto_parlamentario_id_idx on voto (parlamentario_id) where parlamentario_id is not null;` (parcial, per RESEARCH Pitfall 2 — las filas Senado no-confirmadas son null). (c) RPC `votos_de_parlamentario(p_id text, p_limit int default 20, p_offset int default 0)` `language sql stable` (security INVOKER, lee solo tablas público-read) que devuelve `(votacion_id, boletin, fecha, seleccion, etapa, camara, origen, fecha_captura, enlace)` join `voto`×`votacion` where `parlamentario_id = p_id and estado_vinculo='confirmado'` order by `vo.fecha desc nulls last` limit/offset — copia el patrón de `match_proyectos` (0011). Nota: el `enlace` de la fila es `votacion.enlace` y el `boletin` es `votacion.boletin` COMPLETO con sufijo (Pitfall 6 — la ficha enlaza a `/proyecto/[boletin]` con ese valor). (d) RPC `rebeldias_de_parlamentario(p_id text)` `language sql stable security definer set search_path = ''` que lee `public.parlamentario.partido` INTERNAMENTE, calcula la opción modal (`mode() within group (order by v.seleccion)`) de la bancada por votación SOLO sobre `estado_vinculo='confirmado'`, y devuelve SOLO `(votacion_id, boletin, fecha, seleccion_propia, mayoria_bancada)` donde el parlamentario difirió — usa el SQL de RESEARCH Pattern 5 verbatim como base. CERO score, cero etiqueta. (e) `grant execute on function votos_de_parlamentario(text, int, int) to anon;` y `grant execute on function rebeldias_de_parlamentario(text) to anon;` sobre las firmas EXACTAS. NO añadas ninguna policy ni grant select sobre `parlamentario.partido` — eso violaría LEGAL-03. Encabeza el archivo con un comentario explicando por qué rebeldías es `security definer` (toca partido) mientras `votos_de_parlamentario` es invoker.

Crea `supabase/tests/0019_voto_asistencia_y_ficha.test.sql` (pgTAP, esqueleto de 0018): `begin; select plan(N);` con asserts — el CHECK admite `ausente` (insert con `seleccion='ausente'` no lanza; usar `lives_ok`), el CHECK rechaza un valor inválido (`throws_ok` 23514), el índice `voto_parlamentario_id_idx` existe (`has_index`), ambos RPCs existeen (`has_function`), `parlamentario` sigue sin policies (`is_empty` sobre `pg_policy`, re-afirma deny de partido a anon), y los grants execute a anon existen (verifica `has_function_privilege('anon', ..., 'execute')`). `select * from finish(); rollback;`. NO ejecutas la migración aquí (eso es Task 3).</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0019_voto_asistencia_y_ficha.sql','utf8').split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n'); if(!/security definer/.test(s)) throw new Error('rebeldias debe ser security definer'); if(/create policy[\s\S]*parlamentario/.test(s)||/grant select[\s\S]*partido/.test(s)) throw new Error('PROHIBIDO: no abrir partido a anon'); if(!/grant execute on function votos_de_parlamentario/.test(s)) throw new Error('falta grant votos'); if(!/grant execute on function rebeldias_de_parlamentario/.test(s)) throw new Error('falta grant rebeldias'); console.log('0019 OK')"</automated>
  </verify>
  <done>`0019_...sql` existe con el CHECK extendido, índice parcial, ambos RPCs (rebeldías security definer), grants execute a anon, y CERO policy/grant select sobre partido. `0019_...test.sql` existe con el plan pgTAP. El gate de prohibición pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aplicar 0019 + correr pgTAP (operador)</name>
  <action>Checkpoint de operador: el DDL de 0019 y el pgTAP NO se aplican por CI (build/typecheck no prueban que Postgres ejecutó la migración). El operador aplica 0019 al schema y corre pgTAP siguiendo los pasos de verificación abajo.</action>
  <what-built>Migración 0019 (ausente CHECK + índice parcial + RPCs votos/rebeldías + grants) y su pgTAP. El DDL NO está aplicado por build/typecheck — solo el operador puede aplicarlo y correr pgTAP contra el schema aplicado (RESEARCH Pitfall 5: BOM en .env rompe el CLI; usar --db-url explícito).</what-built>
  <how-to-verify>
    1. Aplica 0019 al Supabase LOCAL: `supabase db push --db-url "$LOCAL_DB_URL"` (o `psql "$LOCAL_DB_URL" -f supabase/migrations/0019_voto_asistencia_y_ficha.sql`). El `.env` tiene BOM — pasa la db-url explícita, no dependas del parseo del CLI.
    2. Corre pgTAP: `supabase test db --db-url "$LOCAL_DB_URL"` (o el runner pgTAP del repo). Espera todos los asserts del plan en verde.
    3. Verifica a mano que anon NO lee partido: con la anon key, `select partido from parlamentario limit 1` devuelve 0 filas / denegado; `select * from rebeldias_de_parlamentario('P00001')` ejecuta (puede devolver 0 filas si no hay votos aún — eso es correcto).
    4. (Opcional, remoto sa-east-1) Si se expone públicamente esta fase: aplica 0019 al pooler remoto con el patrón de 0018 (db-url explícita).
  </how-to-verify>
  <resume-signal>Escribe "aplicada" + el conteo pgTAP (N/N PASS), o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @obs/tramitacion test` verde (parser 5 opciones).
- `pnpm --filter app typecheck` o build verde (Seleccion extendido).
- pgTAP 0019 verde contra schema aplicado (operador).
- anon no lee `parlamentario.partido`; anon ejecuta ambos RPCs.
- 0019 NO contiene ninguna policy ni grant select sobre partido.
</verification>

<success_criteria>
- `voto.seleccion` admite `ausente` (VOTE-03).
- `parseCamaraVotoDetalle` emite las 5 opciones por diputado sin fabricar (VOTE-03).
- Índice parcial `voto(parlamentario_id)` existe (VOTE-03 query de ficha).
- `votos_de_parlamentario` y `rebeldias_de_parlamentario` existen y son ejecutables por anon; rebeldías es security definer y NO expone partido (VOTE-04/VOTE-05, LEGAL-03).
</success_criteria>

<output>
Create `.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-01-SUMMARY.md` when done. Documenta el mapeo final de códigos del WS a opciones (cuáles fueron confirmados vs asumidos para verificación LIVE en Plan 02).
</output>
