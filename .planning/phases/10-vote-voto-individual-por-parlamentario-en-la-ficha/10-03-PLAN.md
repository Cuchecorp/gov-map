---
phase: 10-vote-voto-individual-por-parlamentario-en-la-ficha
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/app/parlamentario/[id]/page.tsx
  - app/app/parlamentario/[id]/not-found.tsx
  - app/components/parlamentario-header.tsx
  - app/components/voto-ficha-row.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/voto-row.tsx
  - app/lib/types.ts
autonomous: true
requirements: [VOTE-03, VOTE-04, VOTE-05]
user_setup: []

must_haves:
  truths:
    - "El ciudadano abre /parlamentario/[id] y ve la cabecera del parlamentario + su sección de votaciones"
    - "La lista muestra A favor/En contra/Abstención/Pareo/Ausente + boletín enlazado + ProvenanceBadge"
    - "El enlace al parlamentario aparece SOLO si estado_vinculo='confirmado'; si no, mención cruda + IdentityMarker"
    - "El ciudadano filtra votos por tema/materia (faceta cruda, sin score ni lenguaje de afinidad)"
    - "El ciudadano ve cuántas veces votó distinto a su bancada (conteo+lista, sin juicio)"
    - "Tres estados honestos: confirmado / presente-no-verificado / no-ingestado; un vacío nunca se lee como limpio"
  artifacts:
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "primera ficha del parlamentario; shell de secciones apilables + sección VOTE"
      min_lines: 60
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "lista paginada + asistencia + voto×tema + rebeldías + 3 estados"
      min_lines: 50
    - path: "app/components/parlamentario-header.tsx"
      provides: "cabecera reusable (CamaraChip + nombre + bancada chip neutro + ProvenanceBadge)"
      min_lines: 20
    - path: "app/components/votos-por-parlamentario.test.tsx"
      provides: "RTL: 3 estados + sin lenguaje de afinidad/score"
      contains: "identidad no verificada"
  key_links:
    - from: "votos-por-parlamentario.tsx"
      to: "rpc votos_de_parlamentario / rebeldias_de_parlamentario"
      via: "createServerSupabase().rpc"
      pattern: "votos_de_parlamentario|rebeldias_de_parlamentario"
    - from: "voto-ficha-row.tsx"
      to: "/parlamentario/[id] (guarda de identidad)"
      via: "link solo si estado_vinculo='confirmado'"
      pattern: "estado_vinculo"
    - from: "page.tsx"
      to: "parlamentario (maestra)"
      via: "lectura + notFound si no existe"
      pattern: "notFound"
---

<objective>
Construye la PRIMERA página `/parlamentario/[id]` — un Server Component de una sola columna con un shell de secciones apilables (INT en Phase 11/12 y MONEY en 14-16 agregan sus propias `<section>` después) y la sección VOTE completa: asistencia, lista de votos paginada agrupada por votación, faceta voto×tema, y rebeldías. Reusa VERBATIM el design system cívico de v1.0 (ProvenanceBadge, IdentityMarker, CamaraChip, patrón VotacionBar CSS puro) y aplica los tres estados honestos LOCKED y el gate de contenido anti-afinidad/anti-causal (UI-SPEC §9.1).

Purpose: el frente "análisis de parlamentarios 360" entrega su primera ficha; la ciudadanía ve cómo vota un parlamentario con trazabilidad a la fuente, sin que el producto editorialice.
Output: ruta `/parlamentario/[id]` + componentes nuevos + tests RTL del contrato de contenido.

Esta fase es MVP (vertical slice): tras esta ficha el ciudadano PUEDE hacer algo nuevo — consultar el voto individual de un parlamentario. La sección se construye lista→tema→rebeldías como sub-bloques, cada uno con su estado honesto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-UI-SPEC.md
@.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-01-SUMMARY.md

<read_first>
Lee ANTES de escribir. **NOTA CRÍTICA (app/AGENTS.md):** esta versión de Next.js tiene breaking changes — lee la guía relevante en `node_modules/next/dist/docs/` antes de escribir código de routing/Server Components; `params`/`searchParams` son Promises (ver el patrón existente).

- `app/app/proyecto/[boletin]/page.tsx` — patrón EXACTO de la ficha: `params: Promise<{...}>`, validación del path antes de tocar DB (`BOLETIN_RE.test`), `createServerSupabase()`, `.maybeSingle()` (distingue error real de 0 filas, #34), `notFound()`, secciones con `<Suspense>` + Skeleton, `React.cache` para deduplicar una consulta compartida entre sub-secciones. CÓPIALO de cerca.
- `app/app/proyecto/[boletin]/not-found.tsx` — patrón del not-found.
- `app/components/voto-row.tsx` — la guarda de identidad LOCKED (link SOLO si `estado_vinculo==='confirmado' && parlamentario_id!=null`, si no `<IdentityMarker/>`) + `SELECCION_STYLE`. Plan 01 ya extendió `Seleccion` con `ausente`; AÑADE la variante `ausente` a `SELECCION_STYLE` (`{ label: "Ausente", className: "bg-slate-100 text-slate-500" }`) per UI-SPEC §3.2.
- `app/components/provenance-badge.tsx` (props `capturedAt`, `sourceName`, `sourceUrl`), `app/components/identity-marker.tsx`, `app/components/camara-chip.tsx`, `app/components/votacion-bar.tsx` (patrón CSS puro `role="img"` + aria-label por segmento — reusar para asistencia/rebeldías; NO Recharts/visx). `app/components/ficha-header.tsx` (espejo para `parlamentario-header.tsx`).
- `app/lib/types.ts` — `Seleccion` (ya con `ausente` de Plan 01), `EstadoVinculo`, `sourceLabel()`. AÑADE interfaces para las filas de los RPCs (`VotoFichaRow` con `votacion_id/boletin/fecha/seleccion/estado_vinculo/parlamentario_id/mencion_nombre/origen/fecha_captura/enlace` per UI-SPEC §10; `RebeldiaRow`).
- `app/lib/supabase.ts` — `createServerSupabase()` (anon key server-only). Las lecturas usan `.rpc("votos_de_parlamentario",{p_id,p_limit,p_offset})` y `.rpc("rebeldias_de_parlamentario",{p_id})`.
- `supabase/migrations/0019_...sql` (Plan 01) — firmas EXACTAS de los RPCs y columnas devueltas. El `boletin` devuelto es el COMPLETO con sufijo → `/proyecto/${boletin}` (Pitfall 6).
- `app/lib/buscar.ts` (`BOLETIN_RE`) — patrón del validador único de path; define un `PARLAMENTARIO_ID_RE` análogo (p.ej. `/^P\d{5}$/` según los ids de la maestra "P00001") y valida `[id]` antes de la DB (V5 input validation).
- UI-SPEC §3 (shell, VotoFichaRow, asistencia, voto×tema, rebeldías), §3.6 (3 estados), §6.1 (copy de empties), §9 (copy contract + §9.1 reglas duras anti-afinidad/causal), §10 (data flow). Es el contrato de diseño VERBATIM.
- `app/components/voto-row.test.tsx`, `app/components/votacion-bar.test.tsx` — patrón de tests RTL existentes (reusar el setup).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shell /parlamentario/[id] + ParlamentarioHeader + VotoFichaRow + tipos</name>
  <files>app/app/parlamentario/[id]/page.tsx, app/app/parlamentario/[id]/not-found.tsx, app/components/parlamentario-header.tsx, app/components/voto-ficha-row.tsx, app/components/voto-row.tsx, app/lib/types.ts</files>
  <behavior>
    - Visitar `/parlamentario/P00001` (existe en maestra) renderiza `<h1>` con el nombre + sección "Votaciones" (`<h2>`).
    - Un `[id]` con formato inválido o ausente de la maestra → `notFound()` (copy UI-SPEC §6.1).
    - `VotoFichaRow` con `estado_vinculo='confirmado'` enlaza el boletín a `/proyecto/[boletin]`; con `no_confirmado`/`probable` NO enlaza al parlamentario y muestra `mencion_nombre` + `<IdentityMarker/>`.
    - El chip de opción soporta los 5 valores incluida `ausente`; nunca colapsa abstencion/ausente/pareo a "no votó".
  </behavior>
  <action>Crea `app/app/parlamentario/[id]/page.tsx` copiando la estructura de `proyecto/[boletin]/page.tsx`: `params: Promise<{id:string}>`, valida `id` contra un `PARLAMENTARIO_ID_RE` antes de DB, lee `parlamentario` por id con `.maybeSingle()` (error real → throw; sin fila → `notFound()`). Shell de una columna `max-w-3xl mx-auto px-4 md:px-8 py-8 md:py-16` con `<Suspense>` para el header y para la sección VOTE (`<section id="votos" className="mt-12">` con `<h2>Votaciones</h2>`). Deja un comentario indicando dónde Phase 11+ apila `<section id="lobby">` etc. (cada una su propio `<h2>`+Suspense+empty honesto; no anidar en #votos). Crea `not-found.tsx` (espejo de proyecto). Crea `app/components/parlamentario-header.tsx` (CamaraChip diputados→"Cámara"/senado→"Senado"; nombre `<h1>` text-3xl; chip de bancada/partido `Badge variant="secondary"` NEUTRO — nunca coloreado por partido; ProvenanceBadge obligatorio con `sourceLabel(origen)`+`fecha_captura`; foto opcional solo si hay URL con fuente, si no nada — sin silueta placeholder). Crea `app/components/voto-ficha-row.tsx` (hermano de VotoRow orientado a la ficha del parlamentario: chip de opción de `SELECCION_STYLE`, "Boletín N°XXXX-XX" mono enlazado a `/proyecto/${boletin}`, ProvenanceBadge por fila; la guarda de identidad gobierna el enlace AL parlamentario, no el enlace al proyecto que es ruta interna confiable). Añade la variante `ausente` a `SELECCION_STYLE` en `voto-row.tsx`. Añade a `app/lib/types.ts` las interfaces `VotoFichaRow` y `RebeldiaRow` (forma de los RPCs de 0019, UI-SPEC §10). Escribe `app/components/votos-por-parlamentario.test.tsx` (RTL, esqueleto inicial) que renderiza `VotoFichaRow` en los 3 estados con fixtures y verifica: confirmado→enlace; no_confirmado→texto "identidad no verificada" + sin enlace al parlamentario; chip "Ausente" presente. (El test crece en Task 2 con las secciones.)</action>
  <verify>
    <automated>pnpm --filter app test -- votos-por-parlamentario</automated>
  </verify>
  <done>La ruta `/parlamentario/[id]` existe con shell + header + sección VOTE; `VotoFichaRow` aplica la guarda de identidad y soporta los 5 chips; tipos de RPC en `types.ts`; `app` typecheck/build verde; el test RTL inicial de los 3 estados pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sección VOTE — asistencia + lista paginada + voto×tema + rebeldías + estados honestos</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <behavior>
    - Asistencia: barra CSS-pura (patrón VotacionBar) + desglose textual "A favor N · En contra N · Abstención N · Pareo N · Ausente N"; cada conteo repetido en texto (no color-solo, a11y). Si el denominador honesto no es computable, muestra solo el numerador crudo (sin % fabricado).
    - Lista: agrupada por votación `fecha DESC`, paginada server-driven `?votosPage=N` (20 votaciones/página), reusa `VotoFichaRow`.
    - Voto×tema: faceta por materia server-driven `?materia=slug`; muestra conteos crudos por tema; CERO score/afinidad.
    - Rebeldías: invoca `rebeldias_de_parlamentario`; muestra "Votó distinto a la mayoría de su bancada N veces" + lista + footnote del método; sin juicio.
    - Tres estados (§3.6): confirmado→fila completa; presente-no-verificado→mención cruda+IdentityMarker fuera de los agregados; no-ingestado→empty honesto que distingue "no consultado todavía" de "ingestado, 0 confirmados". Un vacío NUNCA se lee como "limpio".
  </behavior>
  <action>Crea `app/components/votos-por-parlamentario.tsx` como Server Component(s) que reciben `id` + `searchParams`. Lee la lista vía `createServerSupabase().rpc("votos_de_parlamentario",{p_id:id,p_limit:20,p_offset})` y rebeldías vía `.rpc("rebeldias_de_parlamentario",{p_id:id})`; distingue error real (throw → UI de error §6.3) de 0 filas (empty honesto §6.1). Sub-bloques en orden: asistencia (`<h3>`), lista por votación agrupada con `VotoFichaRow` + paginación anchor `?votosPage=N` `min-h-[44px]` + "Página N de M", voto×tema (`<h3>` + chips anchor `?materia=...` `border-b-2 border-primary` activo + lista filtrada), rebeldías (`<h3>` + conteo + lista + footnote "Se compara el voto del parlamentario con la opción mayoritaria de su bancada en esa misma votación."). Agregados (asistencia/rebeldías) SOLO sobre filas confirmadas; las menciones no verificadas viven en un área "menciones sin verificar" claramente etiquetada, nunca en los conteos. Barras = patrón VotacionBar CSS puro (NO Recharts/visx). Copy EXACTO del §9 (empties, rebeldías, error). **GATE DE CONTENIDO (§9.1, release gate):** PROHIBIDO en código y copy cualquier "afinidad", "alineado", "en línea con", "afín", "aliado", "rival", score/índice/ranking, "rebelde/díscolo/leal/disciplina" user-facing, o lenguaje causal ("por presión de", "a cambio de"). El heading neutro es "Votó distinto a su bancada"; "Rebeldías" solo como nombre interno. Identidad incierta = exactamente "identidad no verificada". Extiende `votos-por-parlamentario.test.tsx`: cubre los 3 estados (incl. empty "no ingestado" ≠ "0 confirmados" ≠ "limpio"), el desglose de asistencia con `ausente`, y un assert de gate de contenido (los strings prohibidos NO aparecen en el render de la sección).</action>
  <verify>
    <automated>pnpm --filter app test -- votos-por-parlamentario</automated>
  </verify>
  <done>La sección VOTE renderiza asistencia + lista paginada + voto×tema + rebeldías con los 3 estados honestos; los agregados solo cuentan confirmados; el test RTL pasa incluyendo el gate de contenido (sin afinidad/score/causal); `app` build verde.</done>
</task>

</tasks>

<verification>
- `pnpm --filter app test` verde (RTL: 3 estados, asistencia con ausente, gate de contenido).
- `pnpm --filter app build` / typecheck verde.
- Grep gate de contenido: ningún identificador/copy de afinidad/score/causal en la sección VOTE.
  `node -e "const fs=require('fs');const f=['app/components/votos-por-parlamentario.tsx','app/components/voto-ficha-row.tsx','app/components/parlamentario-header.tsx'].map(p=>fs.readFileSync(p,'utf8')).join('\n').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n');if(/afinidad|alinead|en l[ií]nea con|af[ií]n a|d[ií]scolo|loyalty|score de|ideolog/i.test(f))throw new Error('GATE: lenguaje prohibido §9.1');console.log('content gate OK')"`
- Render manual: `/parlamentario/[id]` muestra header + asistencia + lista + tema + rebeldías; enlace al parlamentario solo si confirmado.
</verification>

<success_criteria>
- La ficha muestra la lista de votos (5 opciones) + asistencia con la guarda de identidad (VOTE-03).
- Voto×tema facetea por materia sin score ni afinidad (VOTE-04).
- Rebeldías muestra conteo+lista bruto sin juicio; partido nunca llega a anon (vía RPC, VOTE-05).
- Tres estados honestos; un vacío nunca se lee como "limpio" (VOTE-03/05, §3.6).
- Gate de contenido §9.1 (anti-afinidad/causal) verificable como criterio de aceptación.
</success_criteria>

<output>
Create `.planning/phases/10-vote-voto-individual-por-parlamentario-en-la-ficha/10-03-SUMMARY.md` when done.
</output>
