---
phase: 108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - packages/llm/src/tiered.ts
  - packages/llm/src/tiered.test.ts
  - packages/llm/src/task-ladder.test.ts
  - packages/llm/src/tiered-scope-guard.test.ts
  - packages/llm/src/index.ts
autonomous: true
requirements: [TIER-02, TIER-03, TIER-04, TIER-05]
must_haves:
  truths:
    - "TieredProvider implements LLMProvider — es drop-in, tipa como un provider cualquiera"
    - "Sin task, TieredProvider de un solo tier devuelve EXACTAMENTE lo que devolvería el provider base llamado directo (passthrough byte-idéntico) con callCount==1 y escalated==false"
    - "Un fallo zod (LLMValidationError) en tier-0 dispara UN hop a tier-1; si tier-1 también falla, lanza EscalationExhaustedError (sin loop)"
    - "Un veredicto de juez ok:false dispara escalación; un veredicto ok:true NUNCA relaja una compuerta ni swallowea un fallo zod"
    - "assertNoRutInLlmInput muerde en la entrada del decorador antes de llamar a cualquier tier"
    - "Cada complete() emite exactamente un TelemetryEvent payload-free con providerId/task/latencyMs/escalated/validationOutcome"
    - "maxBudgetUsd agotado salta la escalación (previene loops)"
    - "El construction point de fichas (pipeline-cli.ts) NO contiene TieredProvider — verificado por guard estructural"
  artifacts:
    - path: "packages/llm/src/tiered.ts"
      provides: "TieredProvider (decorador LLMProvider) + TieredProviderOptions + EscalationExhaustedError"
      exports: ["TieredProvider", "TieredProviderOptions", "EscalationExhaustedError"]
      min_lines: 60
    - path: "packages/llm/src/tiered.test.ts"
      provides: "Suite MockProvider offline: passthrough, escalación zod, juez escalate-only, bounded, budget, RUT-guard"
      contains: "byte-identical"
    - path: "packages/llm/src/tiered-scope-guard.test.ts"
      provides: "Guard estructural: pipeline-cli.ts de fichas NO usa TieredProvider (TIER-05)"
  key_links:
    - from: "packages/llm/src/tiered.ts"
      to: "packages/llm/src/validate.ts (LLMValidationError)"
      via: "try/catch en complete para detectar el trigger de escalación"
      pattern: "LLMValidationError"
    - from: "packages/llm/src/tiered.ts"
      to: "packages/llm/src/data-routing.ts (assertNoRutInLlmInput)"
      via: "guard de entrada del decorador"
      pattern: "assertNoRutInLlmInput"
    - from: "packages/llm/src/index.ts"
      to: "./tiered"
      via: "export *"
      pattern: "export \\* from \"\\./tiered\""
    - from: "packages/llm/src/tiered.ts"
      to: "packages/llm/src/telemetry.ts (TelemetrySink)"
      via: "emisión de un evento por complete()"
      pattern: "TelemetrySink"
---

<objective>
Implementar `TieredProvider` — el decorador `implements LLMProvider` que cablea la cascada ACOTADA respond→validate→escalate: llama al tier primario, en fallo zod o veredicto de juez `ok:false` escala UN hop al tier siguiente, y si eso también falla marca terminal (`EscalationExhaustedError` → revisión humana). Toda su conducta se prueba con el `MockProvider` local (offline, sin keys). Incluye el guard estructural que fija TIER-05 (fichas intacto).

Purpose: Materializa el mecanismo de escalonamiento contra el que 109 hará el swap productivo. 108 solo prueba el mecanismo con mock.
Output: `tiered.ts` (net-new), `tiered.test.ts` + `task-ladder.test.ts` + `tiered-scope-guard.test.ts` (net-new), 1 línea aditiva al barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-CONTEXT.md
@.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-RESEARCH.md
@.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-VALIDATION.md

<interfaces>
<!-- Contratos que este plan CONSUME. Plan 01 (Wave 1) ya creó telemetry.ts/task-ladder.ts/test-mock.ts. -->

De Plan 108-01 (ya existen al correr Wave 2):
```typescript
// telemetry.ts
export interface TelemetryEvent { providerId: string; task?: string; latencyMs: number; costUsd: number | null; validationOutcome: ValidationOutcome | null; judgeVerdict?: Pick<Verdict,"ok"|"confidence">; escalated: boolean; ts: string; }
export type TelemetrySink = (event: TelemetryEvent) => void;
export const noopSink: TelemetrySink;
// task-ladder.ts (importa TieredProvider de ./tiered — este plan lo crea)
export interface TierSpec { provider: LLMProvider; costPerToken?: number; }
export interface LadderConfig { primary: TierSpec; escalation?: TierSpec; judge?: JudgeProvider; judgeContext?: string; maxBudgetUsd?: number; }
export function buildTieredProvider(config: LadderConfig, telemetrySink?: TelemetrySink): TieredProvider;
// test-mock.ts (helper de test)
export class MockProvider implements LLMProvider { callCount: number; constructor(responder, opts?); }
export class MockJudgeProvider implements JudgeProvider { callCount: number; constructor(verdict); }
```

Contratos base (packages/llm/src/):
```typescript
// types.ts — LLMProvider.complete<T>(req, schema): Promise<T>; CompletionRequest ahora tiene task?: string
// validate.ts — LLMValidationError (throw terminal del repair loop); ValidationOutcome (kind: clean|zod-repaired|structured-output-fail|zod-terminal); onValidationOutcome fires ANTES del throw (Pitfall 1)
// data-routing.ts:52 — assertNoRutInLlmInput(text: string): void
// data-routing.ts:63 — assertSensitivityAllowed(...)
// judge.ts — JudgeProvider.judge(req: JudgeRequest): Promise<Verdict>; JudgeRequest { answer; system?; sensitivity?; temperature?; context? }; Verdict { ok; reason?; confidence? }
```

Construction points READ-ONLY (108 NO los toca; guard verifica el de fichas):
- packages/cruces/src/clasificar-fichas-cli.ts:200 — new DeepSeekProvider (109 lo swappea, NO 108)
- packages/cruces/src/clasificar-lobby-cli.ts:190 — new MiniMaxProvider
- packages/fichas/src/pipeline-cli.ts:191 — new DeepSeekProvider (NUNCA se toca; prompt-cache vive aquí)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TieredProvider — decorador de cascada acotada respond→validate→escalate</name>
  <files>packages/llm/src/tiered.ts, packages/llm/src/index.ts</files>
  <read_first>
    - packages/llm/src/types.ts (LLMProvider, CompletionRequest.task)
    - packages/llm/src/validate.ts (LLMValidationError, ValidationOutcome, onValidationOutcome — Pitfall 1)
    - packages/llm/src/judge.ts (JudgeProvider, JudgeRequest, Verdict)
    - packages/llm/src/data-routing.ts (assertNoRutInLlmInput, assertSensitivityAllowed)
    - packages/llm/src/telemetry.ts (TelemetrySink, TelemetryEvent — de Plan 01)
    - packages/llm/src/test-mock.ts (MockProvider/MockJudgeProvider — de Plan 01)
  </read_first>
  <behavior>
    - Test "byte-identical passthrough" (TIER-02 crit #1): TieredProvider de 1 tier (MockProvider con valor conocido), request SIN task → devuelve el MISMO valor que el MockProvider directo; callCount==1; sin escalación; TelemetryEvent con task===undefined, escalated===false.
    - Test "drop-in": TieredProvider tipa como LLMProvider (asignación a variable LLMProvider compila; verificado por tsc).
    - Test "escalación zod": tier-0 responder lanza LLMValidationError; tier-1 devuelve válido → tier-0.callCount==1, tier-1.callCount==1, retorno = valor tier-1, telemetría escalated===true.
    - Test "bounded/terminal": tier-0 y tier-1 ambos lanzan → EscalationExhaustedError tras EXACTAMENTE 2 llamadas totales (1/tier), NO loop.
    - Test "juez escalate-only (reject)": tier-0 válido + MockJudgeProvider {ok:false} → escala a tier-1. MockJudgeProvider {ok:true} → NO escala.
    - Test "juez NO relaja compuerta": tier-0 lanza LLMValidationError + juez {ok:true} → el ok:true NO swallowea el fallo zod; sigue escalando (el juez corre como trigger, no como sustituto de zod).
    - Test "budget cap": maxBudgetUsd:0 + tier-0 falla → escalación SALTADA, EscalationExhaustedError con 1 llamada total.
    - Test "RUT-guard entrada": user con patrón RUT chileno → el decorador lanza ANTES de llamar a ningún tier (tier-0.callCount==0).
  </behavior>
  <action>
    Crear `packages/llm/src/tiered.ts` exportando `TieredProviderOptions { tiers: LLMProvider[]; judge?: JudgeProvider; judgeContext?: string; maxBudgetUsd?: number; telemetrySink?: TelemetrySink }`, `EscalationExhaustedError extends Error` (mensaje genérico, sin prompt — espeja `LLMValidationError`), y `TieredProvider implements LLMProvider`.

    Construcción: `id` derivado de los tiers (p.ej. `"tiered:" + tiers.map(t=>t.id).join("→")`); `trainsOnInputs = tiers.some(t => t.trainsOnInputs)` (Pitfall 2 — si CUALQUIER tier entrena, el decorador reporta true). `task` NO altera el `id`.

    `complete<T>(req, schema)`:
    1. `assertNoRutInLlmInput(req.user)` y, si presente, `assertNoRutInLlmInput(req.system)` en la ENTRADA — defensa en profundidad en la capa decorador (per TIER-03 guard, incluso si el tier envuelto también checa).
    2. Envolver `req.onValidationOutcome` para CAPTURAR el `ValidationOutcome` sin perder el callback original: `const wrapped = { ...req, onValidationOutcome: (o)=>{ captured=o; req.onValidationOutcome?.(o); } }` (Pitfall 1 — el callback dispara ANTES del throw; usar el try/catch para decidir escalación, el callback SOLO para telemetría).
    3. Llamar `tiers[0].complete(wrapped, schema)` en try/catch, midiendo latencia wall-clock (Date.now antes/después).
    4. Determinar trigger de escalación: `LLMValidationError` atrapado (fallo zod) O — si `judge` presente y tier-0 tuvo éxito — un `Verdict.ok === false`. Diseño del juez per Pitfall 4 (RECOMENDADO del research): el juez corre SIEMPRE tras el intento de tier-0, recibiendo `JSON.stringify(result)` como `answer` en éxito, o una descripción del fallo en zod-fail; `ok:false` es el trigger sin importar el camino. CRÍTICO ESCALATE-ONLY: `ok:true` NUNCA revierte un fallo zod (si zod falló, se escala aunque el juez apruebe) y NUNCA sustituye la compuerta zod.
    5. Si NO hay trigger → emitir UN TelemetryEvent (escalated:false, validationOutcome:captured, judgeVerdict si aplica) y retornar el resultado de tier-0.
    6. Si hay trigger → verificar presupuesto (`maxBudgetUsd`): si el costo estimado acumulado lo excede (o `maxBudgetUsd:0`), SALTAR la escalación → lanzar `EscalationExhaustedError` con telemetría escalated:true. El costo es aproximado para 108: derivar de `TierSpec.costPerToken` o un estimado por-llamada; si no hay dato, presupuesto deshabilitado salvo el caso explícito `0`.
    7. Escalación: si `tiers[1]` existe y presupuesto OK → `tiers[1].complete(req, schema)` UN hop. Éxito → emitir TelemetryEvent(escalated:true) y retornar.
    8. Si `tiers[1]` falla o no existe → `throw new EscalationExhaustedError(...)` (terminal; el caller marca revisión humana). NUNCA re-llamar tier-0 (sin loops; máx llamadas = tiers.length).

    Emitir SIEMPRE exactamente UN TelemetryEvent por invocación de `complete()` (usar `telemetrySink ?? noopSink`). El evento JAMÁS lleva `req.user`/`req.system`/el resultado/el `reason` del veredicto (per TIER-04).

    NO usar ni referenciar `selectProvider`/`loadRouterConfigFromEnv` de `router.ts` (dead code — anti-pattern del research). NO ruteo mid-sesión: la lista de tiers es fija al construir.

    Finalmente: agregar `export * from "./tiered";` al barrel `packages/llm/src/index.ts` (la única línea que Plan 01 dejó pendiente). Confirmar cero colisión de nombres (Pitfall 3).
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec tsc -b</automated>
  </verify>
  <done>
    `tiered.ts` exporta `TieredProvider`/`TieredProviderOptions`/`EscalationExhaustedError`; `implements LLMProvider` compila; barrel re-exporta `./tiered`; tsc -b exit 0; grep confirma cero referencia a `selectProvider` en `tiered.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suite de tests MockProvider (offline) + tests de task-ladder + guard estructural TIER-05</name>
  <files>packages/llm/src/tiered.test.ts, packages/llm/src/task-ladder.test.ts, packages/llm/src/tiered-scope-guard.test.ts</files>
  <read_first>
    - packages/llm/src/tiered.ts (recién creado — la superficie a probar)
    - packages/llm/src/test-mock.ts (MockProvider/MockJudgeProvider de Plan 01)
    - packages/llm/src/task-ladder.ts (buildTieredProvider de Plan 01)
    - packages/fichas/src/pipeline-cli.ts (el archivo que el guard estructural asevera intacto — línea 191)
  </read_first>
  <action>
    (1) Crear `packages/llm/src/tiered.test.ts` con TODOS los tests del bloque `<behavior>` de la Task 1, usando el `MockProvider`/`MockJudgeProvider` locales (Pitfall 5: NUNCA importar de `@obs/llm-bench` ni `@obs/adjudication`). El test "byte-identical" es LOAD-BEARING (criterio de aceptación #1): construir un `MockProvider` con responder que devuelve un objeto conocido, llamar el provider directo y el `TieredProvider` de 1 tier con request SIN `task`, y asertar igualdad profunda del retorno + `callCount===1` + telemetría `task===undefined && escalated===false`. Para "juez escalate-only" incluir AMBAS aristas: (a) juez `ok:false` escala; (b) juez `ok:true` sobre tier-0 exitoso NO escala y retorna el valor de tier-0 (prueba que el juez no puede aprobar-para-relajar). Para "bounded" asertar el conteo TOTAL de llamadas == tiers.length y el tipo `EscalationExhaustedError`. Para "RUT-guard" usar un patrón RUT chileno válido en `user` y asertar que tier-0.callCount===0.
    (2) Crear `packages/llm/src/task-ladder.test.ts`: `buildTieredProvider` con `LadderConfig { primary, escalation, maxBudgetUsd }` produce un `TieredProvider` cuyos tiers/judge/presupuesto reflejan la config; un `LadderConfig` sin `escalation` produce un provider de 1 tier que NO escala (lanza `EscalationExhaustedError` en fallo, sin tier-1). Verifica el sink pasado a `buildTieredProvider` recibe eventos.
    (3) Crear `packages/llm/src/tiered-scope-guard.test.ts`: guard estructural TIER-05 — leer el fuente de `packages/fichas/src/pipeline-cli.ts` (por ruta relativa desde el test, `import.meta.dirname` + join; gotcha conocido de MEMORY: usar `import.meta.dirname`, NO `new URL(import.meta.url)`) y asertar que NO contiene la cadena `TieredProvider` (el construction point de fichas queda SIN tocar ⇒ prompt-cache DeepSeek intacto — argumento estructural del research, Option 3). El guard MUERDE si un cambio futuro introduce `TieredProvider` en fichas.
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec vitest run src/tiered.test.ts src/task-ladder.test.ts src/tiered-scope-guard.test.ts</automated>
  </verify>
  <done>
    Los tres archivos de test verdes; el test "byte-identical" existe y es load-bearing; el guard estructural asevera ausencia de `TieredProvider` en `pipeline-cli.ts`; suite completa `@obs/llm` verde (102 previos + nuevos) y `tsc -b` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dominio → prompt LLM (decorador) | user/system cruzan al tier — RUT prohibido en la entrada del decorador |
| tier-0 → tier-1 (escalación) | el mismo request cruza a un segundo modelo — el guard RUT ya mordió en la entrada |
| evento → sink de telemetría | estructural/numérico solamente; sin texto de prompt/respuesta/reason |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03 | Information Disclosure | RUT en el camino de escalación | mitigate | `assertNoRutInLlmInput(user)` + `(system)` en la ENTRADA del decorador, antes de tier-0; test H lo asevera (tier-0.callCount==0). |
| T-108-04 | Denial of Service | loop de escalación infinito | mitigate | Cota dura: máx `tiers.length` llamadas por `complete()`; tras tier-1 fallar, `throw` inmediato sin re-llamar tier-0. Test "bounded" cuenta las llamadas. |
| T-108-05 | Tampering | juez "aprueba" salida mala | mitigate | El veredicto del juez SOLO gatea escalación (`ok:false`); `ok:true` no sustituye la compuerta zod ni revierte un fallo zod. Test D lo asevera en ambas aristas. |
| T-108-06 | Information Disclosure | PII en `TelemetryEvent` | mitigate | Evento payload-free (Plan 01 fija el shape); `reason` del Verdict excluido; solo `ok`/`confidence`. |
| T-108-07 | Tampering | fichas swappeado por error → prompt-cache roto | mitigate | Guard estructural asevera ausencia de `TieredProvider` en `pipeline-cli.ts` (TIER-05). |
| T-108-SC | Tampering | npm installs | accept | CERO paquete nuevo (research §Package Legitimacy: no install). |
</threat_model>

<verification>
- `pnpm --filter @obs/llm exec vitest run src/tiered.test.ts src/task-ladder.test.ts src/tiered-scope-guard.test.ts` verde.
- `pnpm --filter @obs/llm exec vitest run` — suite completa verde (baseline 102 + nuevos), 0 regresión.
- `pnpm --filter @obs/llm exec tsc -b` exit 0.
- Guard estructural: el fuente de `packages/fichas/src/pipeline-cli.ts` NO contiene `TieredProvider`.
</verification>

<success_criteria>
- `TieredProvider implements LLMProvider` drop-in; passthrough byte-idéntico sin `task`.
- Cascada acotada: 1 hop, terminal `EscalationExhaustedError`, sin loops, presupuesto respetado.
- Juez ESCALATE-ONLY: escala en `ok:false`, jamás relaja/aprueba.
- `assertNoRutInLlmInput` muerde en la entrada del decorador.
- Un TelemetryEvent payload-free por `complete()`.
- Guard estructural verde: fichas intacto (TIER-05).
</success_criteria>

<output>
Create `.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-02-SUMMARY.md` when done
</output>
