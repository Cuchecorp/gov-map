---
phase: 127-panel-mat-materializador-0080-puebla-los-sujetos
plan: 03
type: execute
wave: 3
depends_on: ["127-01", "127-02"]
files_modified: []
autonomous: true
requirements: [PANEL-01, PANEL-06]

must_haves:
  truths:
    - "0080 está aplicada a PROD por psql --single-transaction (jamás db push)"
    - "Tras correr materializar_senales(), toda señal positiva tiene evidencia poblada y toda supresión conserva '{}'"
    - "La query de paridad devuelve cero filas, apareada con un listado que exhibe conteos positivos"
    - "El conjunto de cobertura_camara vivas es exactamente el conjunto de grafía ciudadana esperado"
    - "Los ítems de agenda fuera de corpus se ven con en_corpus:false sobre un total > 0"
    - "pgTAP 0080 y pgTAP 0065 (regresión) salen verdes contra el schema aplicado"
  artifacts:
    - path: ".planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-03-SUMMARY.md"
      provides: "Salidas verbatim de las 7 verificaciones + los dos pgTAP"
  key_links:
    - from: "supabase/migrations/0080_actualidad_evidencia.sql"
      to: "PROD (public.actualidad_senal.evidencia)"
      via: "PGCLIENTENCODING=UTF8 psql --single-transaction -f"
      pattern: "single-transaction"
---

<objective>
Aplicar `0080` a PROD, correr el materializador una vez y **probar por `psql -tA`** (jamás REST) que
la evidencia quedó poblada, la supresión intacta, la paridad exacta, la grafía única y el guard 404
vivo. Cerrar con los dos pgTAP contra el schema aplicado y el checkpoint de operador.

Purpose: los criterios 1, 3, 4 y 5 del ROADMAP §Phase 127 solo son verificables contra el schema
APLICADO — build/typecheck no prueban que Postgres ejecutó el DDL (falso positivo de CI conocido).

Output: `127-03-SUMMARY.md` con las salidas verbatim. Cero archivos de código modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-CONTEXT.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-RESEARCH.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-01-SUMMARY.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-02-SUMMARY.md

<read_first>
- `127-RESEARCH.md` §Validation Architecture → "Verificación manual post-apply (checkpoint de
  operador, D-10)": las 7 queries están escritas ahí VERBATIM. Se ejecutan tal cual, sin re-inventar.
- `127-RESEARCH.md` §Common Pitfalls 6 (sin `PGCLIENTENCODING=UTF8` las tildes se corrompen y la
  grafía "única" nace rota en silencio) y 7 (`psql -tA` emite CRLF ⇒ `| tr -d '\r'` en TODO pipe).
- `supabase/migrations/0080_actualidad_evidencia.sql` y `supabase/tests/0080_actualidad_evidencia.test.sql`.
</read_first>

<regimen>
- Cargar el secreto por nombre: `set -a; source .env; set +a`. **JAMÁS ecoar `$SUPABASE_DB_URL`**
  (ni en logs, ni en mensajes, ni en el SUMMARY).
- `PGCLIENTENCODING=UTF8` en **toda** invocación de psql (el archivo contiene `Cámara`, `(sin cámara)`).
- `| tr -d '\r'` en **todo** pipe contra `psql -tA`.
- JAMÁS `supabase db push` (drift de `schema_migrations`). JAMÁS editar `0073`/`0075`/`0065`.
</regimen>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline del assert nuevo, aplicar 0080, materializar una vez</name>
  <files>(ninguno — operación contra PROD)</files>
  <read_first>
    - `127-RESEARCH.md` §Validation Architecture, pasos 0-2 del bloque bash
    - `supabase/tests/0065_actualidad_senal.test.sql` (editado por 127-02)
  </read_first>
  <action>
En este orden estricto, registrando cada salida verbatim para el SUMMARY:

1. **Baseline ANTES de aplicar** — correr el pgTAP de 0065 ya editado:
   `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0065_actualidad_senal.test.sql | tr -d '\r'`
   Con la migración vieja aún viva, el assert de grafía ciudadana **debe salir `not ok`** (la
   `cobertura_camara` todavía es `C.Diputados`). Ese rojo es la prueba de que el assert nuevo
   MUERDE — es el control positivo apareado del verde posterior. Si sale 17/17 verde ANTES de
   aplicar, el assert no muerde: **PARAR y reportar**, no seguir.
2. **Aplicar**:
   `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0080_actualidad_evidencia.sql`
   Esperado: dos `CREATE FUNCTION`. Cualquier `ERROR` revierte la transacción entera; corregir el
   SQL en `0080` (la migración aún NO está aplicada, así que se edita — no se crea una 0081) y
   reintentar.
3. **Materializar una vez**:
   `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select actualidad.materializar_senales();" | tr -d '\r'`
   Vigilar `23505 duplicate key`: significaría que la grafía se normalizó en el `select` pero no en
   el `group by` (Pitfall 1) ⇒ corregir `0080`, re-aplicar y re-materializar.

El cron `actualidad-materializar` NO se toca (D-12): invoca por nombre y el replace es transparente.
  </action>
  <verify>
    <automated>bash -c 'set -a; source .env; set +a; PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select p.proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname = '"'"'actualidad'"'"' order by 1;" | tr -d "\r"'</automated>
    <!-- Fix B-2 checker: JAMÁS $$...$$ dentro de comillas dobles de bash — $$ se expande al PID
         y la query llega corrupta (nspname=12345). Comilla simple SQL escapada con el idiom
         '"'"' de bash. Regla para TODA query de este plan que necesite literales SQL. -->
  </verify>
  <acceptance_criteria>
    - La query de verificación lista exactamente `grafia_camara` y `materializar_senales`.
    - `proconfig` del proc conserva el search_path vacío (D-09b):
      `psql -tA -c "select array_to_string(proconfig,',') from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='actualidad' and p.proname='materializar_senales';" | tr -d '\r'`
      → contiene `search_path=`.
    - El cron sigue registrado:
      `psql -tA -c "select count(*) from cron.job where jobname='actualidad-materializar';" | tr -d '\r'`
      → `1`.
    - La corrida del proc termina sin error (salida vacía de `select actualidad.materializar_senales();`).
    - El baseline del paso 1 quedó registrado con su `not ok` verbatim en el SUMMARY.
  </acceptance_criteria>
  <done>0080 aplicada, proc corrido una vez, cron intacto, search_path preservado.</done>
</task>

<task type="auto">
  <name>Task 2: Las 7 verificaciones del research + los dos pgTAP contra el schema aplicado</name>
  <files>(ninguno — lectura contra PROD)</files>
  <read_first>
    - `127-RESEARCH.md` §Validation Architecture, queries 3 a 7 del bloque bash (copiar VERBATIM)
  </read_first>
  <action>
Ejecutar, en orden, las queries 3-7 del research y los dos pgTAP, guardando la salida **verbatim**
de cada una para el SUMMARY (con `| tr -d '\r'` siempre). Ninguna se sustituye por REST (cap 1k).

- **Q3 — inventario**: `tipo_senal, cobertura_camara, conteo, (supresion_causa is not null) as suprimida,
  evidencia = '{}'::jsonb as vacia, jsonb_array_length(evidencia->'items') as n_items,
  octet_length(evidencia::text) as bytes` de `actualidad_senal where tipo_senal <> 'agrupacion_materia'`.
  Es el **control positivo apareado** de todos los ceros de abajo.
- **Q4 — paridad (D-06)**: la query de divergencia `conteo <> total or total <> jsonb_array_length(items)`
  sobre positivas. Debe devolver **cero filas**. Sola no prueba nada (cero vacuo): se lee junto a Q3.
- **Q5 — grafía (PANEL-06)**: `select distinct coalesce(cobertura_camara,'<null>') … order by 1`.
- **Q6 — guard 404**: por `tipo_senal like 'agenda%'`, `count(*) filter (where (i->>'en_corpus')::boolean is false)`
  vs `count(*)`, con `lateral jsonb_array_elements(s.evidencia->'items') i`. **Nota:** en
  `agenda_citacion` los boletines viven en `items[*].puntos[*]` y en `agenda_sala` en
  `items[*].tabla[*]` — extender la query con un `jsonb_array_elements` anidado para medir el guard
  donde realmente vive, y reportar ambos niveles.
- **Q7 — pgTAP**: primero `supabase/tests/0080_actualidad_evidencia.test.sql`, luego
  `supabase/tests/0065_actualidad_senal.test.sql` (regresión). Ambos con `-tA -f … | tr -d '\r'`.
- **Control de no-regresión de `app/`**: `cd app && pnpm test` — exit 0 con **≥ 1620 tests**
  (baseline post-126 medido 2026-07-30: 108 files / 1620 tests; W-3 checker: baseline FIJADO, no
  "igual que antes" sin número) + `git status --porcelain app/` vacío.
- **Guards de régimen** (W-4 checker — se EJECUTA aquí, no solo en <verification>):
  `cd app && pnpm guards` → patrón `11 passed (11)`; salida verbatim al SUMMARY.

Si algo falla, corregir en `0080`/el test, re-aplicar y **re-correr la secuencia completa** — jamás
reportar un verde parcial.
  </action>
  <verify>
    <automated>bash -c 'set -a; source .env; set +a; PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0080_actualidad_evidencia.test.sql | tr -d "\r" | tail -20'</automated>
  </verify>
  <acceptance_criteria>
    - **Q3**: al menos una fila con `suprimida=f`, `vacia=f` y `n_items > 0`; toda fila con
      `suprimida=t` muestra `vacia=t` y `n_items` vacío/NULL.
    - **Q4**: **0 filas**, apareada con la evidencia de Q3 de que existen positivas (cero fuerte, no vacuo).
    - **Q5**: el conjunto devuelto es exactamente un subconjunto de
      `{Cámara de Diputados, Senado, (sin cámara), 2022-2026 (piso de corpus), <null>}` — cero
      `C.Diputados`, cero `camara`, cero `senado` en minúscula. Las tildes se ven correctas
      (si aparece `CÃ¡mara`, faltó `PGCLIENTENCODING=UTF8`: re-aplicar).
    - **Q6**: `total > 0` en ambas señales de agenda y `fuera_corpus` reportado con su denominador
      (orden de magnitud esperado ~10/49 según el spike; **no** se asserta el número exacto — los
      datos vivos cambian a diario, Assumption A1).
    - **Q7**: `0080_actualidad_evidencia.test.sql` termina en `ok` para todos sus asserts y sin
      `# Looks like you planned N tests but ran M`. `0065_actualidad_senal.test.sql` sale **17/17
      verde** (contraste directo con el `not ok` del baseline de la Task 1).
    - `pnpm test` con el mismo resultado que antes de la fase (cero archivos de `app/` modificados:
      `git status --porcelain app/` vacío).
  </acceptance_criteria>
  <done>Las 7 verificaciones y los dos pgTAP están ejecutados con salida verbatim registrada; ningún cero queda sin control positivo apareado.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-ratificación con evidencia registrada (fix B-3 checker — régimen autónomo)</name>
  <files>(ninguno — asserts sobre las salidas ya obtenidas)</files>
  <action>
El régimen de la pasada ordena ejecución AUTÓNOMA (checkpoints de operador SOLO en Phases 128/129).
Las 5 lecturas que antes eran checkpoint humano se convierten en asserts ejecutables sobre las
salidas de Tasks 1-2, cada uno registrado verbatim en el SUMMARY:

1. Q3 contiene ≥1 fila `suprimida=f` con `n_items > 0` Y (tras el escenario del pgTAP) las filas
   `suprimida=t` muestran `vacia=t` — verificado por lectura programática de la salida capturada.
2. Q4 = 0 filas, leída JUNTO a Q3 (cero fuerte).
3. Q5 ⊆ conjunto de grafía ciudadana, tildes correctas (si aparece mojibake `CÃ`, FALLA).
4. Contraste pgTAP 0065: baseline pre-apply contiene `not ok` Y post-apply contiene 17/17 verde —
   AMBAS salidas verbatim en el SUMMARY; si falta cualquiera de las dos, la task FALLA.
5. pgTAP 0080 verde sin `planned N but ran M`, contrastando el N contra la lista numerada del
   127-02-SUMMARY.

Sección final del SUMMARY: "Ratificación autónoma (régimen pasada 1)" con las 5 lecturas y sus
salidas — es la evidencia que el operador auditará a posteriori; NO se bloquea esperando respuesta.
  </action>
  <verify><automated>bash -c "grep -c 'not ok' .planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-03-SUMMARY.md"</automated></verify>
  <acceptance_criteria>
    - El SUMMARY contiene la sección "Ratificación autónoma" con las 5 lecturas y salidas verbatim.
    - El baseline `not ok` pre-apply Y el 17/17 verde post-apply están AMBOS en el SUMMARY.
    - Ninguna de las 5 lecturas quedó "confirmada" sin su salida pegada (cero aprobados por afirmación).
  </acceptance_criteria>
  <done>Las 5 lecturas están asertadas con evidencia verbatim; la fase no queda bloqueada esperando operador (los checkpoints de esta pasada son 128/129).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador/agente → PROD (`SUPABASE_DB_URL`) | Credencial de escritura sobre la base viva |
| `actualidad_senal.evidencia` → RPC 0066 → anon | Todo lo aplicado queda públicamente legible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-09 | Information Disclosure | `SUPABASE_DB_URL` en logs/SUMMARY | mitigate | Usar por nombre vía `set -a; source .env; set +a`; JAMÁS ecoar; revisar el SUMMARY antes de commitear |
| T-127-10 | Tampering | Apply parcial que deje el proc a medias | mitigate | `--single-transaction` obligatorio: cualquier ERROR revierte todo |
| T-127-11 | Elevation of Privilege | Pérdida del `SET search_path` en el replace | mitigate | Acceptance criterion del Task 1 verifica `proconfig` en PROD post-apply |
| T-127-12 | Repudiation | Verde vacuo en las queries de "cero filas" | mitigate | Q4 y Q6 solo se leen apareadas con Q3 y con el denominador `total` |
| T-127-13 | Denial of Service | Materialización que aborta por `23505` y deja el panel sin filas | mitigate | El proc es transaccional (delete+insert en una función); un abort deja el estado anterior. Vigilancia explícita del `23505` en Task 1 |
</threat_model>

<verification>
- `git status --porcelain` no muestra cambios en `supabase/migrations/0065*`, `0073*`, `0075*` ni en `app/`.
- `supabase/migrations/` ganó exactamente un archivo respecto al inicio de la fase: `0080_actualidad_evidencia.sql`.
- El guard create-view de la Phase 126 no aplica (0080 no crea vistas) — confirmarlo corriendo el
  script `guards` por nombre explícito y registrando su salida.
</verification>

<success_criteria>
- Criterio 1 del ROADMAP: evidencia poblada en positivas / `'{}'` en supresión, probado por `psql -tA`.
- Criterio 2: paridad `total == n_items` sin cap en toda señal (Q4 con control apareado).
- Criterio 3: guard 404 con `en_corpus:false` y su denominador (Q6).
- Criterio 4: grafía única (Q5) + `consultado_al` presente en toda positiva (pgTAP 0080).
- Criterio 5: 0080 aplicada por `psql --single-transaction` + los dos pgTAP verdes contra el schema aplicado.
- Ratificación autónoma con evidencia verbatim en el SUMMARY (régimen pasada 1: checkpoints de
  operador solo en 128/129; el operador audita a posteriori).
</success_criteria>

<output>
Create `.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-03-SUMMARY.md` when done
</output>
