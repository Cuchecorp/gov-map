---
phase: 127-panel-mat-materializador-0080-puebla-los-sujetos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0080_actualidad_evidencia.sql
autonomous: true
requirements: [PANEL-01, PANEL-06]

must_haves:
  truths:
    - "Cada bloque INSERT positivo del proc emite una columna `evidencia` con total/items/consultado_al/fuente"
    - "Ningún INSERT de supresión lista `evidencia` (conserva el default '{}')"
    - "Todo ítem que nombra un boletín declara `en_corpus` calculado por left join contra public.proyecto"
    - "cobertura_camara sale con grafía ciudadana única y la misma expresión aparece en select y group by"
    - "El proc re-declarado conserva literalmente `security definer set search_path = ''`"
  artifacts:
    - path: "supabase/migrations/0080_actualidad_evidencia.sql"
      provides: "create or replace de actualidad.grafia_camara + actualidad.materializar_senales con evidencia"
      contains: "create or replace function actualidad.materializar_senales()"
      min_lines: 260
  key_links:
    - from: "supabase/migrations/0080_actualidad_evidencia.sql"
      to: "public.proyecto"
      via: "left join en los 4 bloques de evento (velocity/nuevos_ingresos/urgencias/archivados) + 2 sub-selects anidados (p2 puntos, p3 tabla) — los 2 bloques de agenda NO lo llevan a primer nivel (la citación/sesión no tiene boletín propio)"
      pattern: "left join public\\.proyecto"
    - from: "supabase/migrations/0080_actualidad_evidencia.sql"
      to: "actualidad.grafia_camara"
      via: "select + group by idénticos en velocity/agenda_citacion/agenda_sala"
      pattern: "group by actualidad\\.grafia_camara"
---

<objective>
Escribir la migración **aditiva** `0080_actualidad_evidencia.sql`: `create or replace` de
`actualidad.materializar_senales()` copiado LITERALMENTE de `0065:88-310` con seis cambios
quirúrgicos (una columna `evidencia` por INSERT positivo + `left join public.proyecto`) más una
función inmutable `actualidad.grafia_camara(text)` que unifica la grafía de cámara (PANEL-06).

Purpose: PANEL-01 — la DB debe tener los sujetos del hecho (boletín, título, fecha, enlace,
`en_corpus`) para que la UI de la Phase 128 los nombre. Arquitectura **Opción A adjudicada por
spike, NO se re-abre**: la RPC `0066` NO cambia de firma, cero allowlist nueva, cero DDL de tabla,
cero grant/revoke.

Output: `supabase/migrations/0080_actualidad_evidencia.sql`. **Este plan NO aplica nada a PROD**
(eso es el plan 127-03) y NO toca ningún archivo de `app/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-CONTEXT.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-RESEARCH.md

<read_first>
- `supabase/migrations/0065_actualidad_senal.sql` — **fuente literal del proc**. Leer COMPLETO una
  sola vez. Mapa de bloques: velocity 121-143 · nuevos_ingresos 145-186 · urgencias 188-216 ·
  agenda_citacion 218-251 · agenda_sala 253-274 · archivados 276-308 · envoltura 88-89 y 309-310 ·
  cron 312-342 (**NO se re-emite**).
- `supabase/migrations/0008_tramitacion.sql:19-90` — columnas reales de `proyecto` (PK `boletin`,
  `titulo` not null, `enlace` not null) y `tramitacion_evento` (SIN `titulo`; `enlace` nullable =
  documento del evento; `boletin` FK→proyecto).
- `supabase/migrations/0010_agenda.sql:18-91` — `citacion` (`horario`, `semana_iso`, `enlace` not
  null), `citacion_punto` (`posicion`, `materia`, `boletin` **nullable y SIN FK**), `sesion_sala`
  (`numero`, `hora_inicio`, `tipo` — NULL en la fila sintética), `sesion_tabla_item` (`quorum`,
  `parte_sesion`, `alias`, `materia`, `boletin` nullable **SIN `urgencia`**).
- `.planning/phases/127-.../127-RESEARCH.md` §Code Examples (bloques tipo A/B/C ya corregidos),
  §Common Pitfalls 1-6, §Riesgos de `create or replace`.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cabecera de 0080 + función actualidad.grafia_camara (PANEL-06)</name>
  <files>supabase/migrations/0080_actualidad_evidencia.sql</files>
  <read_first>
    - `supabase/migrations/0065_actualidad_senal.sql:1-45` (estilo de cabecera de este repo)
    - `127-RESEARCH.md` §Grafía única (inventario de valores crudos + forma recomendada)
  </read_first>
  <action>
Crear el archivo con (a) el bloque de comentarios de cabecera y (b) la función de grafía. NADA MÁS
en esta tarea.

**(a) Cabecera** — comentarios `--` que dejen escritas, como reglas del archivo:
  - Qué es: migración **ADITIVA** que hace `create or replace` del proc de `0065`; la última
    aplicada es `0079`; `0073`/`0075` están escritas y NO aplicadas y **jamás se editan**.
  - **Anti-B-01 (D-03):** CERO cap por recencia. `total` == número de ítems, siempre. Si algún día
    hiciera falta cappear, se hace **por grado** y con el `total` declarado — umbral documentado,
    NO implementado. Motivo: `order by fecha desc` + cap fue exactamente el defecto B-01 de v12.0.
  - **Guard 404 (D-05):** todo bloque que emita boletines usa `left join public.proyecto`; el ítem
    se emite SIEMPRE con `en_corpus`; JAMÁS inner join (divergiría del conteo).
  - **Unidad de la evidencia = unidad del conteo (D-02/D-06):** eventos en velocity/nuevos_ingresos/
    urgencias/archivados; **citaciones** en `agenda_citacion` (puntos anidados); **sesiones** en
    `agenda_sala` (ítems de tabla anidados, porque `0065:260-265` cuenta `sesion_sala`).
  - **Supresión conserva `'{}'` (D-09):** ningún INSERT de supresión lista `evidencia`.
  - **D-09b:** `create or replace` preserva owner y ACL pero **NO los `SET`** ⇒ este archivo
    restatea literal `security definer set search_path = ''`. Cero grants/revokes aquí.
  - **D-12:** el bloque `cron.schedule` de `0065:312-342` **NO se re-emite**; el job invoca por
    nombre y el replace es transparente.
  - Aplicación (D-10): `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f
    supabase/migrations/0080_actualidad_evidencia.sql`. JAMÁS `supabase db push`.

**(b) Función de grafía** (D-07, resuelve la discreción a favor de función sobre CASE ×3 — es
single-source real y hace infalsificable la igualdad select↔group by):

  `create or replace function actualidad.grafia_camara(p_camara text) returns text language sql immutable as $$ ... $$;`

  Semántica exacta:
  - `p_camara` null o `btrim()` vacío → `'(sin cámara)'` (D-08, preserva el comportamiento actual).
  - `lower(regexp_replace(p_camara,'\s+','','g'))` ∈ (`c.diputados`, `camara`, `cámara`,
    `diputados`, `camaradediputados`) → `'Cámara de Diputados'`.
  - misma normalización ∈ (`senado`) → `'Senado'`.
  - `else` → `regexp_replace(p_camara,'\s+',' ','g')` — **nunca descartar** un valor no previsto
    (el dominio de `tramitacion_evento.camara` es texto libre; el de `citacion.camara` /
    `sesion_sala.camara` está acotado por check a `camara`/`senado`).

  NO ponerle `set search_path` a esta función: bloquearía el inlining y solo referencia builtins de
  `pg_catalog` (siempre resolubles). Dejarlo escrito como comentario de una línea.

  Notas Fable (gate 2026-07-30) para la cabecera:
  - **M3:** documentar que `consultado_al = current_date` es correcto a las horas del cron
    (11/14/17/20 UTC = día chileno); una materialización MANUAL entre 00:00-04:00 UTC estamparía
    el día chileno siguiente — NO correr el proc a mano en esa franja.
  - **M5:** `grafia_camara` nace con EXECUTE-to-PUBLIC por default; riesgo ≈ 0 (el schema
    `actualidad` no tiene USAGE para anon/authenticated y la función es pura) — anotado, sin
    revoke (añadirlo sería el cambio de ACL que D-09 prohíbe).
  - **M4 (convención):** en COMENTARIOS referirse a la función como `grafia_camara` a secas
    (sin `actualidad.`) — el conteo de acceptance depende de que el literal calificado aparezca
    solo en código.
  </action>
  <verify>
    <automated>bash -c "grep -c 'anti-B-01\|B-01' supabase/migrations/0080_actualidad_evidencia.sql &amp;&amp; grep -n 'create or replace function actualidad.grafia_camara' supabase/migrations/0080_actualidad_evidencia.sql &amp;&amp; grep -n 'immutable' supabase/migrations/0080_actualidad_evidencia.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "create or replace function actualidad.grafia_camara(p_camara text)"` → 1 línea.
    - `grep -c "(sin cámara)"` sobre el archivo ≥ 1 (D-08 presente).
    - `grep -n "cron.schedule" supabase/migrations/0080_actualidad_evidencia.sql` → **0 líneas**
      fuera de comentarios; control positivo apareado: `grep -c "^--"` > 15 (la cabecera existe y
      el cero anterior no es por archivo vacío).
    - `grep -n "grant \|revoke " supabase/migrations/0080_actualidad_evidencia.sql | grep -v '^\s*--'` → 0 líneas.
  </acceptance_criteria>
  <done>El archivo existe con cabecera de reglas y la función de grafía; ningún objeto más.</done>
</task>

<task type="auto">
  <name>Task 2: create or replace del proc — bloques 1-3 (velocity, nuevos_ingresos, urgencias)</name>
  <files>supabase/migrations/0080_actualidad_evidencia.sql</files>
  <read_first>
    - `supabase/migrations/0065_actualidad_senal.sql:88-216` (envoltura + los 3 primeros bloques,
      con sus comentarios: se copian TAL CUAL)
    - `127-RESEARCH.md` §Code Examples bloques tipo A y B; §Common Pitfalls 1, 3, 4, 5
  </read_first>
  <action>
Abrir el `create or replace function actualidad.materializar_senales() returns void language plpgsql
security definer set search_path = '' as $$` **byte-a-byte igual a `0065:88-89`**, copiar el
`declare` (`0065:90-107`) y el `begin` con el delete acotado y las dos frescuras
(`0065:108-119`) sin cambio alguno, y luego copiar los bloques 1-3 con sus comentarios originales,
aplicando SOLO estos cambios:

**Regla transversal de los tres bloques:** la lista de columnas del INSERT positivo pasa de 8 a 9,
insertando `evidencia` inmediatamente después de `fecha_max`:
`(tipo_senal, ventana, conteo, cobertura_camara, fecha_max, evidencia, dataset, origen, fecha_captura)`.
El valor es un `jsonb_build_object` con exactamente estas claves y en este orden:
`'total', count(*)` · `'consultado_al', current_date` (D-04) · `'fuente', jsonb_build_object('dataset','tramitacion','origen','plataforma-tramitacion')`
(D-04b — la RPC 0066 no re-emite `dataset`/`origen`, es la única vía) · `'items', coalesce(jsonb_agg(...), '[]'::jsonb)`
(Pitfall 3: `jsonb_agg` sobre conjunto vacío devuelve **NULL**, no `'[]'`).
El `order by` va **dentro del agregado** (`jsonb_agg(... order by ...)`), nunca fuera. Los
`having count(*) > 0` y los `if not found` se copian intactos (Pitfall 4). Los INSERT de supresión
(`0065:138-142`, `171-176`, `180-185`, `205-209`, `212-215`) se copian **sin tocar** — no listan
`evidencia`.

**(1) velocity** (de `0065:127-135`): alias `te` a `public.tramitacion_evento` y
`left join public.proyecto p on p.boletin = te.boletin` (1:0..1 contra PK ⇒ no multiplica el
`count(*)`, Pitfall 5). `cobertura_camara` pasa de `coalesce(nullif(regexp_replace(camara,...)...))`
a `actualidad.grafia_camara(te.camara)` **y el `group by` usa esa MISMA expresión literal**
(Pitfall 1: dejar el `group by` viejo produce `23505` contra el unique key de `0065:69` y aborta la
corrida entera). Ítem: `boletin` `te.boletin`, `titulo` `p.titulo`, `fecha` `te.fecha::date`
(date-only, **sin `at time zone`**), `enlace` `p.enlace`, `enlace_evento` `te.enlace`,
`en_corpus` `(p.boletin is not null)`; `order by te.fecha desc`. Calificar con alias todas las
referencias del `where` (`te.fecha`).

**(2) nuevos_ingresos** (de `0065:157-169`): la subquery `pe(boletin, primer)` se copia intacta;
fuera de ella, `left join public.proyecto p on p.boletin = pe.boletin`. `cobertura_camara` sigue
siendo el literal `'2022-2026 (piso de corpus)'` — **NO pasa por `grafia_camara`** (no es una
cámara). Ítem: `boletin` `pe.boletin`, `titulo` `p.titulo`, `fecha` `pe.primer::date`, `enlace`
`p.enlace`, `en_corpus` `(p.boletin is not null)`; `order by pe.primer desc`. `having count(*) > 0`
intacto.

**(3) urgencias** (de `0065:195-203`): alias `te` + el mismo left join. `cobertura_camara` sigue
siendo `null` (anti-ranking, no tocar). Ítem: `boletin`, `titulo` `p.titulo`, `descripcion`
`te.descripcion` (**Fable blocker 3**: la clave se llama `descripcion`, NO `grado` — el valor es la
descripción completa del evento verbatim de fuente, que puede no ser un grado del dominio; una
clave que promete un grado y entrega una frase es editorialización-desde-el-dato; coherente con el
mismo campo en archivados; si 128 necesita el grado tipificado lo deriva ahí, jamás fabricado en
0080), `fecha` `te.fecha::date`, `enlace` `p.enlace`, `enlace_evento` `te.enlace`, `en_corpus`;
`order by te.fecha desc`. **Los ~95 eventos van completos: cero `limit`, cero cap.**
  </action>
  <verify>
    <automated>bash -c "grep -c \"coalesce(jsonb_agg\" supabase/migrations/0080_actualidad_evidencia.sql; grep -c 'left join public.proyecto' supabase/migrations/0080_actualidad_evidencia.sql; grep -vn '^\s*--' supabase/migrations/0080_actualidad_evidencia.sql | grep -c 'limit '"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "security definer set search_path = ''"` → exactamente 1 línea (D-09b).
    - Tras esta tarea: `grep -c "coalesce(jsonb_agg"` ≥ 3 y `grep -c "left join public.proyecto"` ≥ 3.
    - `grep -c "actualidad.grafia_camara(te.camara)"` == 2 (select + group by de velocity) — el
      control apareado de que la expresión NO quedó desalineada (Pitfall 1).
    - Líneas no comentadas con `limit ` → **0**; control positivo apareado: `grep -c "jsonb_build_object"` ≥ 6
      (el cero de `limit` no proviene de un archivo sin agregación).
    - `grep -n "at time zone" supabase/migrations/0080_actualidad_evidencia.sql` → 0 líneas.
  </acceptance_criteria>
  <done>Los bloques 1-3 emiten evidencia con items poblados; las 5 supresiones de estos bloques siguen sin listar `evidencia`.</done>
</task>

<task type="auto">
  <name>Task 3: Bloques 4-6 (agenda_citacion, agenda_sala, archivados) y cierre del proc</name>
  <files>supabase/migrations/0080_actualidad_evidencia.sql</files>
  <read_first>
    - `supabase/migrations/0065_actualidad_senal.sql:218-310` (bloques 4-6 con sus comentarios + `end; $$;`)
    - `supabase/migrations/0010_agenda.sql:18-91` (columnas reales; `citacion_punto.boletin` y
      `sesion_tabla_item.boletin` son **nullable y sin FK** — por eso el guard 404 es real aquí)
    - `127-RESEARCH.md` §Code Examples bloque tipo C; §Open Questions 1-3
  </read_first>
  <action>
Copiar los bloques 4-6 con sus comentarios originales aplicando la misma regla de la columna
`evidencia` (9 columnas, `'fuente'` = `{"dataset":"agenda","origen":"plataforma-agenda"}` en los
dos de agenda y `{"dataset":"tramitacion","origen":"plataforma-tramitacion"}` en archivados), y
cerrar con `end;` / `$$;` idénticos a `0065:309-310`. Los 3 INSERT de supresión de estos bloques
(`0065:240-244`, `247-250`, `268-273`, `297-301`, `304-307`) se copian intactos.

**(4) agenda_citacion** (de `0065:230-237`): alias `c`; `cobertura_camara` →
`actualidad.grafia_camara(c.camara)` en select **y** en `group by`; el `where c.fecha::date >=
current_date` se conserva literal (date-only = día chileno; **jamás** `at time zone`).
La unidad es la **citación**. Ítem: `fecha` `c.fecha::date`, `comision`, `horario`, `enlace`,
`semana_iso` (D-02 enmienda: existe y la Phase 128 la necesita para `/agenda?semana=`), y
`puntos` = **sub-select correlacionado** (Pitfall 5: un join plano contra `citacion_punto` haría
que `count(*)` contara puntos y divergiera del conteo):
`(select coalesce(jsonb_agg(jsonb_build_object('boletin', cp.boletin, 'titulo', p2.titulo, 'materia', cp.materia, 'posicion', cp.posicion, 'enlace', p2.enlace, 'en_corpus', (p2.boletin is not null)) order by cp.posicion), '[]'::jsonb) from public.citacion_punto cp left join public.proyecto p2 on p2.boletin = cp.boletin where cp.citacion_id = c.id and cp.boletin is not null)`.
(**Fable M1**: los ítems anidados llevan `'enlace', p2.enlace` — shape uniforme de ítem-con-boletín;
null cuando no está en corpus.) `citacion_punto` NO tiene `titulo` (viene de `p2.titulo`) — no fabricarlo.
Orden del agregado externo: `order by c.fecha` **ascendente**. Escribir un comentario de una línea
justificándolo: D-01 fija `desc` como orden de presentación para hechos pasados; en agenda **futura**
la presentación correcta es "lo más próximo primero", y no es un cap (van todas).

**(5) agenda_sala** (de `0065:258-265`): alias `s`; `cobertura_camara` →
`actualidad.grafia_camara(s.camara)` en select y `group by`. **La unidad es la SESIÓN**
(`0065:260-265` cuenta `sesion_sala`), tal como fija D-02b — los ítems de tabla van **anidados**,
no como ítems de primer nivel (si fueran de primer nivel se rompería la paridad D-06:
19 ítems vs 1-2 sesiones). Ítem: `fecha` `s.fecha::date`, `numero`, `hora_inicio`, `tipo`,
`enlace`, y `tabla` = sub-select correlacionado sobre `public.sesion_tabla_item sti left join
public.proyecto p3 on p3.boletin = sti.boletin where sti.sesion_id = s.id and sti.boletin is not
null`, con `jsonb_build_object('boletin', sti.boletin, 'titulo', p3.titulo, 'materia', sti.materia,
'posicion', sti.posicion, 'quorum', sti.quorum, 'parte_sesion', sti.parte_sesion, 'enlace',
p3.enlace, 'en_corpus', (p3.boletin is not null)) order by sti.posicion` (**Fable M1**: `enlace`
también en los anidados), con el `coalesce(..., '[]'::jsonb)` obligatorio.
**PROHIBIDO fabricar `urgencia`**: `sesion_tabla_item` no la tiene (D-02 enmendado) — se emite
`quorum`/`parte_sesion`, que es lo que la fuente trae. `numero`/`hora_inicio`/`tipo` van tal cual y
serán NULL en la fila sintética `camara:sesion:2026-W31` — no inventarlos.
Orden externo: `order by s.fecha` ascendente (misma justificación que el bloque 4).

**(6) archivados** (de `0065:285-295`): alias `te` + `left join public.proyecto p`; el `where`
completo (las 4 condiciones de `descripcion`) se copia literal; `cobertura_camara` sigue `null`.
Ítem: `boletin`, `titulo` `p.titulo`, `descripcion` `te.descripcion` (literal de la fuente),
`fecha` `te.fecha::date`, `enlace` `p.enlace`, `enlace_evento` `te.enlace`, `en_corpus`;
`order by te.fecha desc`. `having count(*) > 0` y el `if not found` intactos.

Al cerrar: verificar a ojo que el archivo NO contiene `cron.schedule`, ni `grant`, ni `revoke`, ni
`create table`, ni `create view`, ni referencias a `parlamentario`/`partido`/`rut`/`autores`.
  </action>
  <verify>
    <automated>bash -c "PGCLIENTENCODING=UTF8 psql \"$SUPABASE_DB_URL\" -tA -c \"select 1\" >/dev/null 2>&1 || echo 'sin DB — verificación sintáctica local'; grep -c 'coalesce(jsonb_agg' supabase/migrations/0080_actualidad_evidencia.sql"</automated>
  </verify>
  <acceptance_criteria>
    - Conteos INSENSIBLES a formato (W-1 checker: `grep -c` cuenta líneas y el SQL puede partir
      expresiones — normalizar SIEMPRE): usar el pipeline
      `tr -s '[:space:]' ' ' < supabase/migrations/0080_actualidad_evidencia.sql | grep -o "<patrón>" | wc -l`:
      · patrón `coalesce(jsonb_agg` == **8** (6 agregados de primer nivel + 2 sub-selects anidados)
      · patrón `left join public.proyecto` == **6** (Fable blocker 2: SOLO los 4 bloques de evento
        —velocity/nuevos_ingresos/urgencias/archivados— + `p2` puntos + `p3` tabla; los 2 bloques
        de agenda NO lo llevan a primer nivel; con == 8 el ejecutor metería joins espurios)
      · patrón `jsonb_build_object('total'` == **6** (una evidencia por bloque positivo)
      · patrón `actualidad.grafia_camara` == **7** (definición + 3 pares select/group by; comentarios
        la refieren sin calificar — convención M4)
    - Líneas no comentadas: `grep -v '^\s*--' … | grep -Ec "cron\.schedule|create table|create view|^\s*grant|^\s*revoke"` → **0**;
      control positivo apareado: el mismo pipeline con `-E "insert into public.actualidad_senal"` → ≥ 14.
    - `grep -Ein "\y(partido|rut|autores)\y" supabase/migrations/0080_actualidad_evidencia.sql` → 0 líneas
      (no-PII; el pgTAP de 0065 muerde partido/rut y el de 0080 añadirá `autores`).
    - `grep -c "at time zone"` → 0; control apareado: `grep -c "fecha::date"` ≥ 6.
  </acceptance_criteria>
  <done>`0080_actualidad_evidencia.sql` está completo: función de grafía + proc con los 6 bloques poblando evidencia y las 8 supresiones intactas. Cero DDL de tabla, cero ACL, cero cron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → `actualidad.materializar_senales()` (SECURITY DEFINER) | El proc corre como owner y escribe una tabla deny-by-default; el caller no aporta input |
| `actualidad_senal.evidencia` → RPC `0066` → frontend anon | Todo lo que entre al jsonb es públicamente visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Elevation of Privilege | `create or replace` del proc secdef | mitigate | Restatear literal `security definer set search_path = ''` (los `SET` NO se preservan) + todos los nombres calificados con schema (`public.*`, `actualidad.*`). Acceptance criterion del Task 2 |
| T-127-02 | Information Disclosure | jsonb `evidencia` expuesto por la RPC a anon | mitigate | El proc lee solo tablas no-PII; PROHIBIDO `proyecto.autores`, `parlamentario`, `partido`, `rut`. Grep de acceptance en Task 3; assert nuevo en el pgTAP (plan 127-02) |
| T-127-03 | Tampering | ACL/owner alterados por el replace | accept | `create or replace` preserva `proowner` y `proacl`; por eso 0080 **no declara** grants ni revokes (declararlos SÍ sería el cambio prohibido por D-09) |
| T-127-04 | Denial of Service | Crecimiento no acotado del payload de la RPC | accept | 39,7 KB medidos vs 314,8 KB ya en vuelo; `limit 200` + `statement_timeout='5s'` en `0066:52`. Umbral de cap documentado (D-03), no implementado |
| T-127-05 | Tampering | SQL injection | accept | Cero SQL dinámico: el proc es SQL estático sin parámetros |
</threat_model>

<verification>
- El archivo es la única adición a `supabase/migrations/` (`git status --porcelain supabase/migrations/` muestra solo `0080_actualidad_evidencia.sql`).
- `0073`/`0075` sin modificar: `git diff --name-only -- supabase/migrations/0073* supabase/migrations/0075*` → vacío.
- `0065_actualidad_senal.sql` sin modificar (la migración jamás se edita): `git diff --name-only -- supabase/migrations/0065_actualidad_senal.sql` → vacío.
- Cero archivos de `app/` tocados: `git status --porcelain app/` → vacío.
</verification>

<success_criteria>
- `supabase/migrations/0080_actualidad_evidencia.sql` existe con `actualidad.grafia_camara` + el proc completo.
- Los 6 INSERT positivos listan `evidencia`; los 8 de supresión NO.
- 6 `left join public.proyecto` (4 bloques de evento + 2 anidados), 8 `coalesce(jsonb_agg(...), '[]'::jsonb)`, 7 usos de `grafia_camara` (conteos normalizados con `tr -s '[:space:]'`).
- Clave `descripcion` (no `grado`) en urgencias; `enlace` presente en ítems anidados (M1).
- `security definer set search_path = ''` presente una vez y literal.
- Cero `limit`, cero `at time zone`, cero grant/revoke/cron/create table/create view, cero PII.
</success_criteria>

<output>
Create `.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-01-SUMMARY.md` when done
</output>
