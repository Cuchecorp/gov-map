---
phase: 127-panel-mat-materializador-0080-puebla-los-sujetos
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - supabase/tests/0080_actualidad_evidencia.test.sql
  - supabase/tests/0065_actualidad_senal.test.sql
autonomous: true
requirements: [PANEL-01, PANEL-06]

must_haves:
  truths:
    - "Existe un pgTAP que muerde si una señal positiva no tiene evidencia poblada"
    - "Existe un pgTAP que muerde si una fila de supresión deja de conservar '{}'"
    - "Existe un pgTAP de paridad conteo == total == jsonb_array_length(items)"
    - "Un boletín fantasma sembrado produce en_corpus:false con titulo/enlace null, con control positivo apareado"
    - "El assert de grafía de 0065 exige la grafía ciudadana en vez de 'sin espacios'"
  artifacts:
    - path: "supabase/tests/0080_actualidad_evidencia.test.sql"
      provides: "pgTAP de la evidencia, el guard 404, la paridad y la grafía"
      contains: "select plan("
      min_lines: 150
    - path: "supabase/tests/0065_actualidad_senal.test.sql"
      provides: "assert D2 actualizado a la grafía ciudadana (sigue plan(17))"
      contains: "Cámara de Diputados"
  key_links:
    - from: "supabase/tests/0080_actualidad_evidencia.test.sql"
      to: "actualidad.materializar_senales()"
      via: "siembra propia + select del proc + asserts, todo en una transacción con rollback"
      pattern: "select actualidad\\.materializar_senales\\(\\)"
---

<objective>
Escribir el pgTAP nuevo `supabase/tests/0080_actualidad_evidencia.test.sql` y **actualizar el assert
D2 de `supabase/tests/0065_actualidad_senal.test.sql`**, que rompe garantizadamente con la grafía
nueva.

Purpose: sin el test nuevo, "evidencia poblada" no es verificable (criterio 5 del ROADMAP); sin la
edición del test viejo, aplicar 0080 produce un rojo garantizado que no es un defecto real.

Output: dos archivos de test. **Este plan no aplica nada a PROD y no ejecuta los tests** (0080 aún
no está aplicada) — la corrida real es el plan 127-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-CONTEXT.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-RESEARCH.md
@.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-01-SUMMARY.md

<read_first>
- `supabase/tests/0065_actualidad_senal.test.sql` — **completo**. Es el patrón a espejar: `begin;`
  → `select plan(N);` → siembra owner (bypassa RLS) → `select actualidad.materializar_senales();`
  → asserts → `select * from finish(); rollback;`. El assert a editar está en **L104-114**.
- `supabase/migrations/0080_actualidad_evidencia.sql` (producido por 127-01) — las claves exactas
  del jsonb que hay que asertar.
- `supabase/migrations/0010_agenda.sql:18-91` — `citacion_punto.boletin` y `sesion_tabla_item.boletin`
  son **nullable y SIN foreign key** ⇒ el boletín fantasma se siembra ahí (en `tramitacion_evento`
  NO se puede: su `boletin` es FK→proyecto).
- `127-RESEARCH.md` §Validation Architecture (mapa Req→Test) y §Common Pitfalls 2, 3, 7.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar el assert D2 de 0065_actualidad_senal.test.sql (Pitfall 2)</name>
  <files>supabase/tests/0065_actualidad_senal.test.sql</files>
  <read_first>
    - `supabase/tests/0065_actualidad_senal.test.sql:100-114` (los dos asserts D2)
    - `127-RESEARCH.md` §Common Pitfalls, Pitfall 2
  </read_first>
  <action>
El assert de L110-114 afirma `bool_and(cobertura_camara !~ '\s')` para `velocity` con el mensaje
"la cobertura_camara de velocity no contiene espacios internos (normalizada)". La grafía ciudadana
`Cámara de Diputados` **tiene** espacios ⇒ ese assert pasa a FAIL en cuanto 0080 se aplica.

Reemplazarlo por un assert **más fuerte**, no más débil: que la `cobertura_camara` de `velocity`
que matchea `[Dd]iputados` sea exactamente `'Cámara de Diputados'`. Usar el mismo idiom
`select is((select bool_and(...) from actualidad_senal where tipo_senal = 'velocity' and
cobertura_camara ~ '[Dd]iputados'), true, '<mensaje>')` con el predicado
`cobertura_camara = 'Cámara de Diputados'`, y mensaje que diga que es la grafía ciudadana única
(PANEL-06 / 4-15) fijada por `actualidad.grafia_camara`.

**No tocar** el assert de L104-108 (`count(distinct cobertura_camara) = 1`): sigue verde y es la
protección D2 real (las dos grafías siguen colapsando a un bucket).
**No cambiar** `select plan(17)` — el número de asserts no varía.
**No tocar** `supabase/migrations/0065_actualidad_senal.sql`: la migración jamás se edita; el test sí.

Añadir un comentario de dos líneas sobre el assert editado explicando que el régimen viejo pedía
"sin espacios" (grafía cruda `C.Diputados`) y el nuevo pide la grafía ciudadana desde 0080.
  </action>
  <verify>
    <automated>bash -c "grep -c \"cobertura_camara !~\" supabase/tests/0065_actualidad_senal.test.sql; grep -c 'Cámara de Diputados' supabase/tests/0065_actualidad_senal.test.sql; grep -c 'select plan(17)' supabase/tests/0065_actualidad_senal.test.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cobertura_camara !~ '\\\\s'"` → **0**; control positivo apareado:
      `grep -c "cobertura_camara ~ '\[Dd\]iputados'"` ≥ 2 (los dos asserts D2 siguen existiendo, el
      cero anterior no es por haber borrado el bloque).
    - `grep -c "Cámara de Diputados" supabase/tests/0065_actualidad_senal.test.sql` ≥ 1.
    - `grep -c "select plan(17);"` == 1.
    - `git diff --name-only -- supabase/migrations/` → **vacío** (ninguna migración editada).
  </acceptance_criteria>
  <done>El test de 0065 exige la grafía ciudadana y sigue declarando 17 asserts.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0080 — evidencia, supresión, paridad, consultado_al y no-PII</name>
  <files>supabase/tests/0080_actualidad_evidencia.test.sql</files>
  <read_first>
    - `supabase/tests/0065_actualidad_senal.test.sql:30-56` (envoltura + siembra owner) y `:186-197` (cierre)
    - `supabase/migrations/0080_actualidad_evidencia.sql` (claves exactas del jsonb)
    - `127-RESEARCH.md` §Validation Architecture, §Common Pitfalls 3 (cero vacuo)
  </read_first>
  <action>
Crear el archivo espejando la envoltura de 0065: cabecera de comentarios explicando qué verifica y
cómo se corre (`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f <archivo> | tr -d '\r'` — no
existe runner de pgTAP en el repo, se corre a mano), `begin;`, `select plan(N);`, siembra, llamada
al proc, asserts, `select * from finish();`, `rollback;`.

**Siembra** (owner, bypassa RLS; todo se rollbackea):
  - `proyecto` `('99101-99', …, titulo 'Proyecto testigo evidencia', enlace 'http://x/99101')`.
  - `tramitacion_evento` para `99101-99`: un `urgencia` con `fecha = current_date - 1` y
    `descripcion 'Hace presente la urgencia'`, y un `tramite` con `camara = 'C. Diputados'` y
    `fecha = current_date - 2` (alimenta velocity con grafía a normalizar).
  - `citacion` futura: `id 'test:cit:1'`, `camara 'senado'`, `comision`, `fecha = current_date + 1`,
    `horario '10:00 a 12:00'`, `semana_iso`, `origen 'test'`, `enlace 'http://x/cit'`.
  - `citacion_punto` de esa citación: posición 0 con `boletin = '99101-99'` (en corpus) y
    posición 1 con `boletin = '88888-88'` — **boletín fantasma**, que NO existe en `proyecto`
    (la tabla no tiene FK sobre `boletin`, verificado en `0010:46-56`).
  - `sesion_sala` futura + `sesion_tabla_item` con un boletín en corpus y uno fantasma (mismo patrón).
  - Luego `select actualidad.materializar_senales();`.

**Asserts** (cada uno cuenta para el `plan(N)`; cerrar N con el número real escrito):
  1. `has_function('actualidad','grafia_camara', array['text'])`.
  2-5. `is(actualidad.grafia_camara('C. Diputados'),'Cámara de Diputados')` ·
     `is(actualidad.grafia_camara('camara'),'Cámara de Diputados')` ·
     `is(actualidad.grafia_camara('senado'),'Senado')` ·
     `is(actualidad.grafia_camara(null),'(sin cámara)')`.
  6. `prosecdef` de `actualidad.materializar_senales` sigue `true`.
  7. **D-09b**: `proconfig` de la función contiene `search_path=` — assert sobre
     `(select proconfig from pg_proc p join pg_namespace n … )` con `array_to_string(proconfig,',') like '%search_path=%'`.
  8. **no-PII ampliado**: el cuerpo (`pg_get_functiondef`, con los comentarios `--` stripeados como
     en `0065:78-83`) no matchea `\y(partido|rut|autores)\y`. El de 0065 solo muerde partido/rut;
     este añade `autores` (nombres de parlamentarios en `proyecto.autores`).
  9. **Control positivo apareado**: `count(*) de filas positivas` (`supresion_causa is null` y
     `tipo_senal <> 'agrupacion_materia'`) `>= 1` — sin esto los ceros de abajo son vacuos.
  10. Toda positiva tiene las 4 claves: violaciones
      `count(*) where not (evidencia ? 'total' and evidencia ? 'items' and evidencia ? 'consultado_al' and evidencia ? 'fuente')` == 0.
  11. `jsonb_typeof(evidencia->'items') = 'array'` en toda positiva (Pitfall 3: `jsonb_agg` vacío
      devuelve NULL; `'null'` o NULL aquí es FAIL).
  12. **Paridad D-06**: violaciones
      `count(*) where conteo <> (evidencia->>'total')::int or (evidencia->>'total')::int <> jsonb_array_length(evidencia->'items')` == 0.
      Es también el assert anti-cap (D-03): un cap rompería esta igualdad.
  13. **D-04**: `(evidencia->>'consultado_al')::date = current_date` en toda positiva.
  14. `evidencia->'fuente'->>'dataset'` no nulo en toda positiva (D-04b).
  15. Control positivo apareado de supresión: `count(*) where supresion_causa is not null` >= 1.
  16. **Supresión conserva `'{}'`**: `count(*) where supresion_causa is not null and evidencia <> '{}'::jsonb` == 0.
  17. **Guard 404 negativo**: existe al menos un ítem anidado con `en_corpus = false` y `titulo` null
      — vía `jsonb_array_elements` de `agenda_citacion` + `->'puntos'`, filtrando `boletin = '88888-88'`.
  18. **Guard 404 control positivo apareado**: existe al menos un punto con `en_corpus = true` y
      `titulo` NOT null (el `99101-99`). Sin este par, el assert 17 pasaría con evidencia vacía.
  19. **Grafía PANEL-06**: `count(*)` de filas temporales cuya `cobertura_camara` no está en
      `('Cámara de Diputados','Senado','(sin cámara)','2022-2026 (piso de corpus)')` y no es null == 0,
      apareado con el control 9.
  20. `agenda_sala`: sus ítems traen la clave `tabla` como array (la unidad es sesión, D-02b).

  **Prohibido asertar cifras de PROD** (95 urgencias, 10/49, 39,7 KB): son datos vivos que cambian a
  diario (Assumption A1 del research). Los asserts son estructurales; los conteos vivos se miden en
  el plan 127-03.
  </action>
  <verify>
    <automated>bash -c "grep -c '^select ' supabase/tests/0080_actualidad_evidencia.test.sql; grep -n 'select plan(' supabase/tests/0080_actualidad_evidencia.test.sql; grep -c 'rollback;' supabase/tests/0080_actualidad_evidencia.test.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "88888-88" supabase/tests/0080_actualidad_evidencia.test.sql` ≥ 2 (siembra + assert).
    - El archivo empieza con `begin;` y termina con `select * from finish();` + `rollback;`
      (`tail -3` lo muestra) — la siembra jamás persiste en PROD.
    - `grep -c "jsonb_array_length(evidencia->'items')"` ≥ 1 (paridad D-06 presente).
    - `grep -c "autores"` ≥ 1 (assert no-PII ampliado).
    - El número dentro de `select plan(N);` es **igual** al número de asserts escritos; el ejecutor
      lo cuenta y lo deja anotado en el SUMMARY. La prueba real ("planned N but ran M" ausente) se
      cobra en el plan 127-03.
    - `git status --porcelain supabase/migrations/` → vacío.
  </acceptance_criteria>
  <done>El pgTAP nuevo existe con siembra propia, boletín fantasma, controles positivos apareados y los asserts de evidencia/paridad/supresión/grafía.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test pgTAP → PROD | El test siembra y **borra** dentro de una transacción que se rollbackea; un `commit` accidental tocaría datos vivos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-06 | Tampering | Siembra del pgTAP contra PROD | mitigate | `begin;` … `rollback;` obligatorio (precedente `0065` test L30/L197); acceptance criterion verifica el cierre |
| T-127-07 | Repudiation | Verde vacuo (cero sin control positivo) | mitigate | Cada assert de "cero violaciones" va apareado con un control positivo (asserts 9, 15, 18) — gotcha "cero fuerte vs cero vacuo" de v12.0 |
| T-127-08 | Information Disclosure | PII colándose al jsonb en un futuro edit | mitigate | Assert no-PII ampliado a `autores` sobre `pg_get_functiondef` |
</threat_model>

<verification>
- Solo dos archivos tocados, ambos en `supabase/tests/`: `git status --porcelain supabase/` no
  muestra nada bajo `supabase/migrations/`.
- `supabase/tests/0065_actualidad_senal.test.sql` sigue con `plan(17)`.
- Cero archivos de `app/` tocados.
</verification>

<success_criteria>
- `supabase/tests/0080_actualidad_evidencia.test.sql` existe, transaccional y con boletín fantasma.
- El assert "sin espacios" de 0065 ya no existe y fue reemplazado por la grafía ciudadana exacta.
- Ningún assert depende de cifras vivas de PROD.
</success_criteria>

<output>
Create `.planning/phases/127-panel-mat-materializador-0080-puebla-los-sujetos/127-02-SUMMARY.md` when done
</output>
