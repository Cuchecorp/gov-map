---
phase: 124-supa-fix-migraciones-aditivas-a-prod
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - supabase/migrations/0074_default_acl_postgres_storage.sql
  - supabase/migrations/0075_revoke_net_roles_publicos.sql
  - supabase/tests/post-apply/0074_default_acl_postgres_storage.test.sql
  - supabase/tests/post-apply/0075_revoke_net_roles_publicos.test.sql
autonomous: true
requirements: [SUPA-02]

must_haves:
  truths:
    - "Ningun objeto futuro de `storage` nacera con privilegios para `anon`/`authenticated` — el bucket de OP-3 ya puede crearse sin nacer alcanzable"
    - "Ningun rol publico (`anon`, `authenticated`, ni `PUBLIC`) tiene `USAGE` sobre el esquema `net` ni `EXECUTE` sobre ninguna de sus 12 funciones"
    - "La cadena SSRF `lives_ok('… net.http_post …')` queda cortada aunque el mitigante de `proargnames` de pgtap desaparezca"
  artifacts:
    - path: "supabase/migrations/0074_default_acl_postgres_storage.sql"
      provides: "Revoke de los 3 default ACL (r, f, S) de postgres en storage, con pre-check fail-closed"
      contains: "in schema storage revoke all"
    - path: "supabase/migrations/0075_revoke_net_roles_publicos.sql"
      provides: "Revoke de USAGE sobre schema net y EXECUTE sobre sus 12 funciones, a anon, authenticated y PUBLIC"
      contains: "revoke all on schema net"
    - path: "supabase/tests/post-apply/0074_default_acl_postgres_storage.test.sql"
      provides: "pgTAP: 0 filas de pg_default_acl de postgres/storage con grantee publico"
    - path: "supabase/tests/post-apply/0075_revoke_net_roles_publicos.test.sql"
      provides: "pgTAP: has_schema_privilege y has_function_privilege en falso para las 12 funciones de net"
  key_links:
    - from: "0074"
      to: "Q-10 + Q-20/Q-21 de 123-SUPA-AUDIT.md"
      via: "pre-check reproduce la query que detecto OFF-6-04"
      pattern: "pg_default_acl"
    - from: "0075"
      to: "Q-22 / Q-22b / Q-24b de 123-SUPA-AUDIT.md"
      via: "pre-check reproduce las queries que detectaron OFF-6-03"
      pattern: "has_schema_privilege"
---

<objective>
Cerrar los pasos **2 y 3 del orden LOCKED**: **`OFF-6-04`** (default ACL de `postgres` sobre el
esquema `storage`, gemelo de `OFF-01`) y **`OFF-6-03`** (el esquema `net` con `USAGE` para `anon` y
`EXECUTE` sobre `net.http_get`/`http_post`/… — **12 funciones**, no 2).

Purpose: `OFF-6-04` debe cerrarse **antes de que exista cualquier bucket** (crear uno es `OP-3`,
acto de operador); hoy es inerte solo porque `storage.buckets` esta vacio (`Q-20`). `OFF-6-03` lo
subio de severidad el gate: *"Apliquenlo en 124 sin esperar al architect"* — encadenado con la
familia `lives_ok` de `pgtap` seria **SSRF real por la Data API**, bloqueado hoy solo por el
accidente de que `pgtap` no nombra sus argumentos, *"un mitigante fragil y no intencional"*.

Output: migraciones `0074` y `0075` aplicadas, con sus dos pgTAP verdes contra el schema aplicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-CONTEXT.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-01-PLAN.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-OFF-01-RESULTADO.md
@.claude/skills/supabase-ops/SKILL.md
</context>

<regimen>
**Hereda integro el `<regimen_de_la_fase>` del plan `124-01`** (apply por `psql --single-transaction`,
jamas `db push`, `PGCLIENTENCODING=UTF8`, sin BOM, `tr -d '\r'` en todo pipe de `psql -tA`,
`set -o pipefail`, nada destructivo, cero deploy/flags/PII, registro en `schema_migrations`,
anti-secreto por clases sin valor). No se repite aqui.

**Dos migraciones separadas, no una.** `0074` opera sobre defaults **del rol `postgres`** (que es la
identidad de la conexion ⇒ tiene ownership ⇒ **deberia** funcionar) y `0075` sobre grants de esquema.
Fundirlas en un solo archivo haria que el fallo de una abortara la otra bajo `--single-transaction`.
Se aplican **en orden** (`0074` y luego `0075`), cada una con su transaccion y su pgTAP.
</regimen>

<tasks>

<task type="auto">
  <name>Task 1: Migracion 0074 + pgTAP — default ACL de postgres en storage (OFF-6-04, paso 2 del orden LOCKED)</name>
  <read_first>
    - `123-SUPA-AUDIT.md` §Eje 3 **`Q-10`** verbatim (la fila `postgres | storage | r/f/S`) y §Eje 6 **`Q-20`**/**`Q-21`** (0 buckets, 0 policies — por que hoy es inerte y por que el orden es load-bearing)
    - `123-SUPA-AUDIT.md` fila `OFF-6-04` de la tabla de offenders — el fix propuesto textual y *"Escape identico a `OFF-01`"*
    - `supabase/migrations/0073_default_acl_supabase_admin_public.sql` — la estructura pre-check/fix/post-check a replicar
    - `.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-OFF-01-RESULTADO.md` — si `OFF-01` cayo en rama B, este plan debe saberlo (pero **no** cambia su propio comportamiento: `0074` es sobre el rol `postgres`, que si tiene ownership)
  </read_first>
  <files>supabase/migrations/0074_default_acl_postgres_storage.sql, supabase/tests/post-apply/0074_default_acl_postgres_storage.test.sql</files>
  <action>
**Migracion** (sin BOM), misma estructura de tres bloques que `0073`:

1. Cabecera: offender `OFF-6-04`, eje 6, query de origen `Q-10` (+ `Q-20`/`Q-21` para el contexto de
   orden), destino `124-aditivo`, **paso 2 del orden LOCKED — antes de crear cualquier bucket
   (`OP-3`)**.
2. **Pre-check fail-closed**: via `aclexplode(d.defaclacl)` sobre `pg_default_acl` con
   `defaclrole = postgres` y `defaclnamespace = storage`, contar los `defaclobjtype` distintos con
   grantee `anon` o `authenticated`. Si **no es 3**, `raise exception`.
3. **Fix**: `alter default privileges for role postgres in schema storage revoke all on tables from anon, authenticated;`
   + identicas para `on functions` y `on sequences`. **Solo revoke.** No se toca `storage.objects`,
   no se crea ningun bucket, no se escribe ninguna policy (eso es `OP-3`, acto de operador).
4. **Post-check** en la misma transaccion: el conteo debe ser **0**, si no `raise exception`.

**Escape identico a `OFF-01`**: si `psql` sale con `42501`/membresia → **`deuda-operador`**, se
reporta, **jamas se escala privilegio**. Si sale con otro error → **PARAR** y reportar. Anexar la
adjudicacion al SUMMARY de este plan (no hace falta archivo aparte: `0074` es sobre el propio rol de
la conexion y la rama B es improbable, pero **debe estar escrita antes de correr**).

**pgTAP post-apply** — `select plan(4)`:
- (A) grantee `anon` en el default ACL de `postgres`/`storage` → **0**.
- (B) grantee `authenticated` → **0**.
- (C) **denominador**: la fila de `pg_default_acl` sigue existiendo con `postgres`/`service_role` —
  el cero es por revoke, no por desaparicion.
- (D) **no-regresion del contexto**: `select count(*) from storage.buckets` sigue en **0** (esta
  migracion no crea buckets; si alguien creo uno, el hecho debe salir a la luz aqui).
Envolver en `begin; … rollback;`.

**Aplicar** y luego correr el pgTAP contra el schema aplicado; registrar `0074` en
`schema_migrations`.
  </action>
  <verify>
    <automated>set -o pipefail; PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0074_default_acl_postgres_storage.test.sql | tr -d '\r' | grep -c '^ok ' | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - El pgTAP contra el schema **aplicado** reporta **4 `ok`, 0 `not ok`**.
    - Re-correr **`Q-10` verbatim** (la del audit, sin modificar) y filtrar `rol_creador='postgres' and esquema='storage'`: ninguna de las 3 filas contiene `anon=` ni `authenticated=`.
    - La migracion no contiene (case-insensitive): `grant`, `drop`, `create policy`, `insert into storage`, `set role`.
    - `select count(*) from supabase_migrations.schema_migrations where version='0074'` → `1`.
    - Si hubo rama de fallo: el SUMMARY declara `OFF-6-04: DEUDA-OPERADOR` con `SQLSTATE` verbatim y pasos zero-credential-value; y el historial no contiene ningun intento de escalada.
  </acceptance_criteria>
  <done>`storage` ya no puede parir objetos abiertos a roles publicos; `OP-3` queda desbloqueado para el operador.</done>
</task>

<task type="auto">
  <name>Task 2: Migracion 0075 + pgTAP — revoke de `net` a roles publicos, las 12 funciones (OFF-6-03, paso 3 del orden LOCKED)</name>
  <read_first>
    - `123-SUPA-AUDIT.md` §Eje 6 **`Q-22`** (la fila `net` con `usage_anon = t` — la inesperada), **`Q-22b`** (`net` lleva ademas la entrada de **grantee vacio `=U` = `USAGE TO PUBLIC`** ⇒ revocar solo a `anon`/`authenticated` **NO basta**) y **`Q-24b`**
    - `123-SUPA-AUDIT.md` fila `OFF-6-03` — fix propuesto textual + *"`pg_net` es infraestructura de `pg_cron`; ningun rol publico lo necesita"*
    - `123-SUPA-REVIEWER-VEREDICTO.md` §corrección a OFF-6-01 — la cadena `lives_ok('select net.http_post(…)')` y por que el mitigante actual es fragil
    - `123-CONTEXT.md` — *"el verificador hallo que **son 12 funciones**, no 2: incluye `http_delete` y `worker_restart`"*
    - Los 5 jobs activos de `cron.job` (hueco nº2 del gate): la evidencia de que `pg_cron` corre como `postgres`/`service_role`, **no** como rol publico ⇒ el revoke no rompe la ingesta
  </read_first>
  <files>supabase/migrations/0075_revoke_net_roles_publicos.sql, supabase/tests/post-apply/0075_revoke_net_roles_publicos.test.sql</files>
  <action>
**Antes de escribir**: enumerar contra PROD (read-only) las funciones vivas de `net` con
`select p.proname, pg_get_function_identity_arguments(p.oid) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='net' order by 1,2;`
y contar. El audit dice **12**. Si el conteo vivo **no es 12**, **no adivinar**: escribir el numero
hallado en el pre-check y declararlo en el SUMMARY como discrepancia contra el audit.

**Migracion** (sin BOM):

1. Cabecera: offender `OFF-6-03`, eje 6, queries de origen `Q-22` + `Q-22b` + `Q-24b`, destino
   `124-aditivo`, **paso 3 del orden LOCKED**, y la nota de que el gate lo **subio de severidad**.
2. **Pre-check fail-closed**: asertar que hoy `has_schema_privilege('anon','net','USAGE')` es **true**
   (si ya estuviera cerrado, `raise exception`: el estado de partida no es el auditado) **y** que el
   conteo de funciones de `net` con `EXECUTE` para `anon` es **> 0**.
3. **Fix — tres frentes, los tres necesarios:**
   - `revoke all on schema net from anon, authenticated;`
   - **`revoke usage on schema net from public;`** — imprescindible por `Q-22b`: `net` tiene el
     grantee vacio `=U`, o sea `USAGE TO PUBLIC`. Revocar solo a los dos roles nombrados dejaria la
     puerta abierta para cualquier rol presente o futuro.
   - `revoke execute on all functions in schema net from anon, authenticated;` **y**
     `revoke execute on all functions in schema net from public;`
   **Ningun `grant`. Ningun `drop extension`. Ningun `alter extension … set schema`** (eso es
   `OFF-6-01`, destino `architect+checkpoint`, **fuera de esta fase**).
   No tocar el ownership ni los privilegios de `postgres`/`service_role`/`supabase_admin` sobre
   `net`: `pg_cron` y `pg_net` deben seguir funcionando.
4. **Post-check** en la misma transaccion: `has_schema_privilege('anon','net','USAGE')` = **false**,
   idem `authenticated`, y el conteo de funciones de `net` exec-`anon` = **0**. Si no, `raise exception`.

**pgTAP post-apply** — `select plan(6)`:
- (A) `has_schema_privilege('anon','net','USAGE')` es **false**.
- (B) idem `authenticated`.
- (C) **cero fuerte con denominador**: conteo de funciones de `net` con `EXECUTE` para `anon` = **0**,
  **y** el conteo total de funciones de `net` es el hallado (12 o el vivo) — un cero sobre cero
  objetos seria un cero **vacuo**.
- (D) idem para `authenticated` = 0.
- (E) **nombrar explicitamente las dos que el gate verifico**: `has_function_privilege('anon', 'net.http_get(…)', 'EXECUTE')`
  y `net.http_post(…)` en **false**, con la firma exacta obtenida de la enumeracion.
- (F) **no-regresion de la ingesta**: `service_role` (o `postgres`) **conserva** `EXECUTE` sobre
  `net.http_post`. Un revoke que rompiera `pg_cron` no es un fix.
Envolver en `begin; … rollback;`.

**Aplicar** `0075`, correr el pgTAP, registrar en `schema_migrations`.

**Chequeo de no-regresion operativa, obligatorio despues del apply**: verificar que los jobs de
`cron.job` siguen `active = true` y que el ultimo `cron.job_run_details` posterior al apply no reporta
fallo por privilegio. Si lo reportara → **PARAR** y reportar (seria el unico escenario en que este
revoke rompio algo real).
  </action>
  <verify>
    <automated>set -o pipefail; PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0075_revoke_net_roles_publicos.test.sql | tr -d '\r' | grep -c '^ok ' | grep -qx 6</automated>
  </verify>
  <acceptance_criteria>
    - El pgTAP contra el schema **aplicado** reporta **6 `ok`, 0 `not ok`**.
    - Re-correr **`Q-22` verbatim**: la fila `net` pasa a `f|f` (era `t|t`).
    - Re-correr **`Q-22b` verbatim**: el `nspacl` de `net` ya **no** contiene el grantee vacio `=U` ni `anon=U`.
    - La migracion contiene los **cuatro** revokes (schema a los 2 roles, schema a `public`, funciones a los 2 roles, funciones a `public`) y **ninguna** de: `grant`, `drop extension`, `alter extension`, `set role`.
    - `service_role` conserva `EXECUTE` sobre `net.http_post` (asercion F verde).
    - Los 5 jobs de `cron.job` siguen `active = true` tras el apply.
    - `select count(*) from supabase_migrations.schema_migrations where version='0075'` → `1`.
  </acceptance_criteria>
  <done>Ningun rol publico alcanza `net`; la cadena SSRF por la Data API queda cortada en su origen y la ingesta sigue viva.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente `anon` → PostgREST → `net.http_*` | SSRF potencial: el servidor emitiria requests arbitrarios a instancia del cliente |
| objeto futuro de `storage` → cliente `anon` | Un bucket creado despues heredaria el default ACL abierto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-05 | Elevation of Privilege / SSRF | `net.http_get`, `net.http_post`, `net.http_delete`, `net.worker_restart` (12 fn) | mitigate | `0075`: revoke de `USAGE` sobre el esquema (incl. `from public`) + `EXECUTE` sobre todas sus funciones |
| T-124-06 | Information Disclosure | default ACL de `postgres` sobre `storage` | mitigate | `0074`: revoke de los 3 tipos, **antes** de que `OP-3` cree bucket alguno |
| T-124-07 | Denial of Service | el propio revoke rompe `pg_cron`/ingesta | mitigate | Asercion (F) `service_role` conserva `EXECUTE`; chequeo post-apply de `cron.job.active` y `job_run_details` |
| T-124-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalacion de paquetes (regimen de la fase) |
</threat_model>

<verification>
1. `git diff --stat` = exactamente los 4 archivos de `files_modified`.
2. Ambos pgTAP corrieron **despues** de su apply respectivo; sus salidas van al SUMMARY verbatim.
3. `Q-10`, `Q-22` y `Q-22b` re-corridas **verbatim** (copiadas del audit, no reescritas) con la salida transcrita.
4. `grep -riE 'db push|db reset|drop extension|alter extension' supabase/migrations/007[45]*` → 0 lineas.
</verification>

<success_criteria>
- `OFF-6-04` y `OFF-6-03` cerrados en PROD, con pgTAP 4 y 6 `ok` respectivamente.
- `Q-22` da `net → f|f`; `Q-22b` sin `=U` en `net`.
- La ingesta (`pg_cron`/`pg_net`) sigue operativa.
- Los tres pasos del orden LOCKED estan consumados (o declarados como deuda, en el caso de `OFF-01`).
</success_criteria>

<output>
Create `.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-02-SUMMARY.md` when done.
</output>
