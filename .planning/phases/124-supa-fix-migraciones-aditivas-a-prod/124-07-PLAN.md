---
phase: 124-supa-fix-migraciones-aditivas-a-prod
plan: 07
type: execute
wave: 7
depends_on: ["124-06"]
files_modified:
  - .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md
  - .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md
autonomous: true
requirements: [SUPA-02]

must_haves:
  truths:
    - "La re-corrida del audit de 123 sobre la DB viva da 0 offenders en lo corregido, demostrado con LA MISMA query que los detecto"
    - "Cada offender de los 8 `124-aditivo` tiene un veredicto escrito: CERRADO, o DEUDA-OPERADOR con evidencia"
    - "Lo que 124 NO cierra (B-01, B-02, B-03, OFF-6-01, OFF-6-02, OP-1..OP-4) esta declarado con destino nombrado — cero cierre en silencio"
    - "El regimen se respeto: cero `db push`, cero deploy, cero flip de flag, cero destructivo, cero secreto impreso"
  artifacts:
    - path: ".planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md"
      provides: "Deliverable consolidado de SUPA-02: re-audit verbatim con salida real + tabla de veredicto por offender + identidad aritmetica"
      contains: "Re-corrida del audit"
    - path: ".planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md"
      provides: "Handoff nombrado de B-01/B-02/B-03 y del resto no cerrado, con su destino"
  key_links:
    - from: "124-SUPA-FIX.md"
      to: "123-SUPA-AUDIT.md"
      via: "las queries Q-NN se copian VERBATIM del audit, no se reescriben"
      pattern: "Q-1[0-9]|Q-2[0-9]"
---

<objective>
Cerrar la fase: **re-correr el audit de 123 sobre la DB viva** y demostrar **0 offenders en lo
corregido**, consolidar el deliverable de `SUPA-02`, y **declarar por escrito todo lo que 124 no
cierra**.

Purpose: es el criterio de exito **nº4** de la fase, y tiene una condicion que no es negociable —
*"demostrado con la misma consulta que los detecto, no con una nueva escrita para pasar"*. Una query
nueva escrita despues del fix es una query escrita **para** dar cero; no prueba nada.

Y es el plan que impide el modo de fallo mas barato de todos: **cerrar en silencio** lo que no se
hizo. `B-01`, `B-02`, `B-03`, los 2 offenders `architect+checkpoint` y los 4 actos de operador
salen de la fase **nombrados y con destino**, o la fase no esta cerrada.

Output: `124-SUPA-FIX.md` (deliverable de `SUPA-02`) + `124-HANDOFF-EXACTITUD.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-CONTEXT.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-01-SUMMARY.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-02-SUMMARY.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-03-SUMMARY.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-04-SUMMARY.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-05-SUMMARY.md
@.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-06-SUMMARY.md
@.claude/skills/supabase-ops/SKILL.md
</context>

<regimen>
Hereda el `<regimen_de_la_fase>` de `124-01`. Este plan es **read-only contra PROD**: cero DDL, cero
DML, **cero migracion nueva**. Solo `SELECT` y escritura de documentos.
</regimen>

<tasks>

<task type="auto">
  <name>Task 1: Re-correr VERBATIM las queries de 123 que detectaron cada offender corregido</name>
  <read_first>
    - `123-SUPA-AUDIT.md` — los bloques ```sql de **`Q-10`**, **`Q-12`**, **`Q-13bis`**, **`Q-15`**, **`Q-16`**, **`Q-17`**, **`Q-22`**, **`Q-22b`**, y los de control `Q-09b` y `Q-19`. **Se copian tal cual del archivo**: mismo texto, mismo filtro `deptype='e'`, mismo `order by`. Prohibido "mejorarlas"
    - `123-SUPA-AUDIT.md` §Método — la regla dura anti-"todo bien": *"'0 offenders' solo vale si la query que lo demuestra esta transcrita verbatim"*; y la distincion **cero fuerte vs cero vacuo** (denominador obligatorio)
    - `123-SUPA-AUDIT.md` §Método — la nota de CRLF: `psql -tA` emite `\r\n` en este host ⇒ `tr -d '\r'` en todo pipe; `sort -c` NO protege
    - Los 6 SUMMARY de los planes anteriores — el estado adjudicado de cada offender
  </read_first>
  <files>.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md</files>
  <action>
Ejecutar contra PROD, **read-only**, cada query **copiada verbatim** de `123-SUPA-AUDIT.md`, y
transcribir su **salida real** (no parafraseada) en `124-SUPA-FIX.md`. Mapeo obligatorio
query → offender:

| offender | query verbatim a re-correr | resultado esperado |
|---|---|---|
| `OFF-01` | `Q-10` | las 3 filas `supabase_admin`/`public` **sin** `anon=` ni `authenticated=` |
| `OFF-6-04` | `Q-10` | las 3 filas `postgres`/`storage` **sin** `anon=` ni `authenticated=` |
| `OFF-6-03` | `Q-22` + `Q-22b` | fila `net` → `f|f`; `nspacl` de `net` sin `=U` ni `anon=U` |
| `OFF-4-01` + `OFF-4-02` | `Q-12` + `Q-15` | `Q-15` → `(0 filas)`; `Q-12` → 0 de 42 con `exec_anon = t` |
| `OFF-5-01` | `Q-16` (+ `Q-17` de control) | `f_unaccent` con `search_path=`; `Q-17` sigue en `(0 filas)` |
| `OFF-4-03` + `OFF-4-04` | `Q-13bis` | las 18 con `tiene_timeout = t`; las 12 + 2 con `tiene_limit = t` (y la nota de que el regex falla en 4 casos ⇒ los rescatados se verifican leyendo `pg_get_functiondef`, igual que en 123) |
| control de no-regresion | `Q-09b`, `Q-19`, `Q-02`, `Q-05` | siguen en `(0 filas)` — el trabajo de 124 no abrio nada nuevo |

**Reglas de la transcripcion:**
- Cada cero lleva su **denominador explicito**. `Q-15 → (0 filas)` con el denominador **42** es un
  cero **fuerte**; sin denominador seria un cero indistinguible de vacuo.
- Cuando la heuristica de `Q-13bis` de un falso negativo (los 4 casos que el audit rescato a mano),
  **decirlo** y aportar la lectura de `pg_get_functiondef`, como hizo 123. No apoyarse en el regex.
- **Cero PII**: nombres de objeto, de columna y agregados. Jamas un valor.
- `tr -d '\r'` en toda canalizacion; nunca comparar listas de `psql -tA` con `comm` sin el.

Escribir `124-SUPA-FIX.md` con: frontmatter (`fase: 124`, `requisito: SUPA-02`, ancla temporal
ejecutada con `select now()::date, current_setting('TimeZone'), version();`), §Régimen aplicado,
§Migraciones aplicadas (`0073`–`0079` con su estado y su pgTAP), §Re-corrida del audit (las queries
verbatim con salida real), y §Veredicto por offender (Task 2).
  </action>
  <verify>
    <automated>set -o pipefail; test -f .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md && grep -c '```sql' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md | awk '$1>=8{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `124-SUPA-FIX.md` contiene **≥ 8 bloques ```sql**, cada uno **byte-comparable** con su original en `123-SUPA-AUDIT.md` (verificable: extraer ambos, `tr -d '\r'`, `diff` → sin diferencias de contenido).
    - Cada bloque va seguido de su **salida real** transcrita.
    - `Q-15` → `(0 filas)` con denominador **42** declarado; `Q-22` → fila `net` en `f|f`; `Q-10` sin `anon=` en las filas de `supabase_admin`/`public` (o el registro de la rama `DEUDA-OPERADOR`) y sin `anon=` en `postgres`/`storage`.
    - `Q-09b`, `Q-19`, `Q-02`, `Q-05` siguen en `(0 filas)` — no-regresion.
    - El documento **no** contiene el valor de `SUPABASE_DB_URL` ni ningun otro secreto (solo el **nombre** de la variable), y **cero** valores de PII.
  </acceptance_criteria>
  <done>El criterio de exito nº4 esta demostrado con las queries originales, no con queries escritas para pasar.</done>
</task>

<task type="auto">
  <name>Task 2: Veredicto por offender + identidad aritmetica de la fase</name>
  <read_first>
    - `123-SUPA-AUDIT.md` §Tabla de offenders (13 filas) y §Recuento por destino, **incluida la corrección aritmética RULE-1** (`124-aditivo` son **8**, no 7)
    - `123-SUPA-AUDIT.md` §Identidad aritmética de la consolidación — el control a espejar
    - Los 6 SUMMARY previos + `124-OFF-01-RESULTADO.md`
  </read_first>
  <files>.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md</files>
  <action>
Añadir a `124-SUPA-FIX.md` la **§Veredicto por offender**: una tabla con las **13** filas del audit
(no solo las 8 que 124 tocaba — las otras 5 llevan su estado heredado), columnas: `#` · objeto ·
destino declarado en 123 · **veredicto de 124** · evidencia (query + migracion + pgTAP).

Vocabulario cerrado de veredicto, **cuatro valores y ninguno mas**:
`CERRADO` · `DEUDA-OPERADOR` · `DIFERIDO` (con destino nombrado) · `CERRADO-EN-123` (los 3 de guard).

Reglas:
- `CERRADO` **solo** si hay (a) migracion aplicada con exit 0, (b) pgTAP verde contra el schema
  aplicado, **y** (c) re-corrida verbatim de su query en cero. Faltando una, no es `CERRADO`.
- `OFF-6-01` y `OFF-6-02` → `DIFERIDO`, destino `supabase-architect` + checkpoint de operador
  (`OP-4`). **Declarados, no aplicados** — es el alcance que el CONTEXT fijo.
- `OFF-01` → segun `124-OFF-01-RESULTADO.md`: `CERRADO` o `DEUDA-OPERADOR`, sin ambiguedad.

Cerrar con la **identidad aritmetica**, espejo del control de 123:
`CERRADO + DEUDA-OPERADOR + DIFERIDO + CERRADO-EN-123 == 13`. Si no cuadra, **escribir la
correccion**, no ajustar el numero en silencio (precedente RULE-1 de 123).

Añadir tambien §Régimen — evidencia de cumplimiento, con los controles corridos:
`grep -riE 'db push|db reset' supabase/migrations/007[3-9]*` → 0; ningun `drop` de objeto con datos;
ningun `*_PUBLIC_ENABLED` tocado (`git diff` sobre los archivos de flags → vacio); cero deploy;
suite `app/` **≥ 1590** verde; `tsc --noEmit` **0**; guards de regimen verdes.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm --filter ./app exec tsc --noEmit && pnpm --filter ./app test 2>&1 | tee /dev/stderr | grep -qE '1[5-9][0-9]{2} passed'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla tiene **13 filas** y cada veredicto es uno de los 4 valores del vocabulario cerrado.
    - Ningun `CERRADO` sin sus tres evidencias (migracion + pgTAP + query verbatim en cero) citadas en su fila.
    - La identidad `suma == 13` esta escrita y cuadra (o la correccion esta escrita).
    - Suite `app/` **≥ 1590** verde y `tsc --noEmit` exit **0**, con la salida transcrita.
    - `grep -riE 'db push|db reset' supabase/migrations/007[3-9]*` → **0 lineas**, transcrito.
    - `git diff` no muestra cambio en ningun archivo de flags `*_PUBLIC_ENABLED` ni en ningun artefacto de deploy.
  </acceptance_criteria>
  <done>Cada uno de los 13 offenders tiene un veredicto con evidencia, y la aritmetica cuadra.</done>
</task>

<task type="auto">
  <name>Task 3: Handoff nombrado de lo que 124 NO cierra (B-01, B-02, B-03 y el resto)</name>
  <read_first>
    - `123-SUPA-AUDIT.md` §Backlog de estructura heredado — las 3 filas `B-01`/`B-02`/`B-03` con su evidencia, su forma de fix aditiva y el precedente `0060` (firma v2 paralela)
    - `123-SUPA-AUDIT.md` §Checkpoints de operador — `OP-1`..`OP-4`
    - `123-SUPA-AUDIT.md` §Los 7 huecos del gate — los 4 marcados "al regimen" (pgmq/pg_cron, esquemas fuera de `public`, Edge Function `ingest-worker`, `graphql_public`)
    - `124-CONTEXT.md` §Deferred Ideas
    - `.planning/ROADMAP.md` §Phase 125 — para nombrar el destino con exactitud, no con un "mas adelante"
  </read_first>
  <files>.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md</files>
  <action>
**Decision explicita de esta fase, que el documento debe abrir declarando:** los tres items del
backlog de **exactitud** (`B-01`, `B-02`, `B-03`) **NO se toman en 124**. El presupuesto de la fase
se consumio integro en los **8 offenders de seguridad** con destino `124-aditivo` (7 migraciones
aplicadas a PROD, cada una con pre-checks, pgTAP contra schema aplicado y re-audit). No es que sean
dificiles: es que **no caben** sin degradar la calidad del trabajo de seguridad, y son de otra clase
(exactitud, no boundary). El audit lo separa con una regla LOCKED que esta fase respeta al pie.

Escribir `124-HANDOFF-EXACTITUD.md` con, para **cada** item pendiente, cinco campos obligatorios:
**que es** · **evidencia** (la del audit, citada) · **forma del fix aditiva** · **destino nombrado**
· **quien lo cierra**.

Items a cubrir, sin omitir ninguno:
- **`B-01`** — cap de 1.000 en votos con distorsion de composicion por `order by fecha desc`. Fix:
  **RPC de conteo dedicada**, aguja completa (secdef PII-safe, `search_path=''`,
  `statement_timeout`, `LIMIT`, doble-revoke, entrada en `PUBLIC_RPC_ALLOWLIST`). Recordar que el
  clamp de seguridad **ya se aplico** en `0078` y que **no es lo mismo**.
- **`B-02`** — denominador del tile *Por materia* (3.100/3.675, 84,4 % sin declarar cobertura). Fix:
  **firma v2 paralela**, precedente `0060` — no alterar la firma viva (evita `42P13` y la re-arma de
  default privileges).
- **`B-03`** — vista nueva en `public` sin `security_invoker`. Hoy es un **cero vacuo** (`Q-18`: 0
  vistas). Decirlo asi: no hay nada que arreglar todavia; lo que falta es la **asercion de guard**
  que exija `with (security_invoker = true)` a la primera vista que se cree.
- **`OFF-6-01`**, **`OFF-6-02`** — `supabase-architect` + checkpoint de operador (`OP-4`).
- **`OP-1`**..**`OP-4`** — actos de operador, con lo que cada uno desbloquea.
- Los **4 huecos "al regimen"** del gate.
- Cualquier **discrepancia** hallada durante 124 entre el audit y la DB viva (conteos que no
  coincidieron), que los SUMMARY previos hayan registrado.

**Destino nombrado, jamas "mas adelante":** cada item indica Phase 125, o `supabase-architect`, o
`checkpoint de operador`, o la proxima auditoria de regimen. Un item sin destino es un item cerrado
en silencio, y esta fase lo tiene prohibido.

Enlazar el archivo desde `124-SUPA-FIX.md` por nombre.
  </action>
  <verify>
    <automated>set -o pipefail; test -f .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md && grep -q 'B-01' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md && grep -q 'B-02' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md && grep -q 'B-03' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md && grep -q 'OP-4' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md && grep -q '124-HANDOFF-EXACTITUD' .planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md</automated>
  </verify>
  <acceptance_criteria>
    - El documento abre con la **decision explicita** de no tomar `B-01`/`B-02`/`B-03` en 124 y su razon (presupuesto, no dificultad).
    - Cubre `B-01`, `B-02`, `B-03`, `OFF-6-01`, `OFF-6-02`, `OP-1`..`OP-4` y los 4 huecos "al regimen".
    - **Cada** item tiene los 5 campos, y **ninguno** tiene destino vago: cada uno dice Phase 125, `supabase-architect`, `checkpoint de operador`, o proxima auditoria.
    - `124-SUPA-FIX.md` enlaza este archivo por nombre.
    - La separacion LOCKED esta escrita: `B-01` (exactitud) **no** es lo mismo que el clamp de `p_limit` de `0078` (seguridad).
  </acceptance_criteria>
  <done>Nada quedo cerrado en silencio: todo lo no hecho sale nombrado, con forma de fix y con dueño.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documento de auditoria → repositorio | El propio artefacto es superficie: transcribir un literal de secreto lo filtra (mitigacion T-123-22, ya activada una vez en 123) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-25 | Repudiation | declarar "0 offenders" con una query nueva escrita para pasar | mitigate | Las queries se copian **verbatim** de `123-SUPA-AUDIT.md`; acceptance criteria exige `diff` sin diferencias de contenido |
| T-124-26 | Repudiation | cerrar `B-01`/`B-02`/`B-03` en silencio | mitigate | `124-HANDOFF-EXACTITUD.md` con 5 campos y destino nombrado por item; verify grepea los tres IDs |
| T-124-27 | Information Disclosure | fuga de secreto o de PII en el deliverable | mitigate | Cero valores (solo nombres de objeto y agregados); clases sin valor para patrones; `SUPABASE_DB_URL` solo por nombre |
| T-124-28 | Tampering | cero vacuo presentado como cero fuerte | mitigate | Denominador explicito obligatorio en cada cero, regla heredada de 123 |
| T-124-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalacion de paquetes (regimen de la fase) |
</threat_model>

<verification>
1. `git diff --stat` = exactamente los 2 archivos de `files_modified`. **Cero cambio en `supabase/`**: `git diff --quiet -- supabase` → exit 0 (este plan no aplica nada).
2. Los bloques ```sql de `124-SUPA-FIX.md` son byte-comparables con sus originales de `123-SUPA-AUDIT.md`.
3. Suite `app/` ≥ 1590 verde + `tsc --noEmit` 0 + guards de regimen verdes.
4. `grep -riE 'db push|db reset' supabase/migrations/007[3-9]*` → 0 lineas.
</verification>

<success_criteria>
- **Criterio nº4 de la fase demostrado**: re-corrida verbatim → 0 offenders en lo corregido, con denominadores.
- Los 13 offenders con veredicto del vocabulario cerrado; identidad `suma == 13` cuadra.
- Todo lo no cerrado tiene destino nombrado en `124-HANDOFF-EXACTITUD.md`.
- Deliverable de `SUPA-02` completo en `124-SUPA-FIX.md`.
</success_criteria>

<output>
Create `.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-07-SUMMARY.md` when done.
</output>
