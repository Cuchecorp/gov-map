---
phase: 34-ingest-ingesta-lobby-probidad-programada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ingest/src/snapshot-store-supabase.ts
  - packages/ingest/src/snapshot-store-supabase.test.ts
  - packages/ingest/src/index.ts
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "SupabaseSnapshotStore implementa SnapshotStore (insertSnapshot) hablando con source_snapshot vía supabase-js"
    - "Un re-insert el mismo día (unique source,resource,date_bucket → 23505) devuelve el id de la fila existente, no falla"
    - "SupabaseSnapshotStore se exporta desde @obs/ingest y es consumible por los CLIs Node/tsx"
    - "pnpm --filter @obs/ingest test queda verde"
  artifacts:
    - path: "packages/ingest/src/snapshot-store-supabase.ts"
      provides: "SnapshotStore Node-side (supabase-js, manejo idempotente 23505)"
      exports: ["SupabaseSnapshotStore"]
      min_lines: 25
    - path: "packages/ingest/src/snapshot-store-supabase.test.ts"
      provides: "Unit test del store: insert OK, 23505 recupera fila, 23505 sin fila throw RETRYABLE, error real throw"
    - path: "packages/ingest/src/index.ts"
      provides: "Re-export de SupabaseSnapshotStore desde el barrel de @obs/ingest"
      contains: "SupabaseSnapshotStore"
  key_links:
    - from: "packages/ingest/src/snapshot-store-supabase.ts"
      to: "source_snapshot"
      via: "supabase-js .from('source_snapshot').insert(row).select('id').single()"
      pattern: "source_snapshot"
    - from: "packages/ingest/src/index.ts"
      to: "packages/ingest/src/snapshot-store-supabase.ts"
      via: "export"
      pattern: "SupabaseSnapshotStore"
---

<objective>
Cerrar el GAP DE API confirmado por RESEARCH: `SnapshotWriter` existe en `@obs/ingest` pero la ÚNICA implementación concreta de `SnapshotStore` vive inline en el Deno worker (`supabase/functions/ingest-worker/worker.ts` L197–228). Los CLIs de ingesta corren en Node/tsx, no Deno, así que no pueden reusarla. Este plan porta ese store a un helper Node-side reusable (`SupabaseSnapshotStore`), con el mismo manejo idempotente de la violación de unique 23505, y lo exporta desde `@obs/ingest`.

Purpose: Es la pieza fundacional de INGEST-04 — sin un `SnapshotStore` Node-side, ningún CLI puede escribir la fila de provenance run-level en `source_snapshot`. Plan 02 (probidad R2/SnapshotWriter) lo consume.
Output: `snapshot-store-supabase.ts` + test + re-export en el barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-RESEARCH.md
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-PATTERNS.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código real del repo. Usar directamente. -->

From packages/ingest/src/snapshot.ts (el contrato a implementar):
```typescript
export interface SnapshotStore {
  insertSnapshot(row: Record<string, unknown>): Promise<{ id: number }>;
}
```

From packages/ingest/src/index.ts L40–41 (dónde añadir el export, junto a SnapshotWriter):
```typescript
export { SnapshotWriter } from "./snapshot";
export type { SnapshotRef, SnapshotWrite, SnapshotStore } from "./snapshot";
```

Cliente supabase-js Node-side (espejo de writer-supabase.ts de probidad/lobby): `createClient(url, serviceKey)` de `@supabase/supabase-js`. La service key respeta RLS deny-by-default de source_snapshot (0002) por ser server-side.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SupabaseSnapshotStore Node-side (puerto del worker Deno)</name>
  <read_first>
    - supabase/functions/ingest-worker/worker.ts L197–228 (analog Deno — el ÚNICO SnapshotStore concreto; portar EXACTO el manejo de 23505)
    - packages/ingest/src/snapshot.ts L31–34 (interface SnapshotStore a implementar) y L36–60 (cómo SnapshotWriter llama insertSnapshot)
    - packages/probidad/src/writer-supabase.ts L17 (patrón createClient(url, serviceKey) con @supabase/supabase-js en Node)
  </read_first>
  <behavior>
    - Test 1: insertSnapshot con fila válida → store devuelve { id } leído de .select("id").single() (mock supabase devolviendo { data: { id: 7 } }).
    - Test 2: error.code === "23505" → store hace un SELECT por (source, resource, date_bucket) y devuelve el id de la fila existente (idempotencia caché-diaria).
    - Test 3: error.code === "23505" pero el SELECT de recuperación devuelve null → store lanza Error con mensaje RETRYABLE que nombra source/resource/date_bucket (NUNCA cae a undefined/TypeError).
    - Test 4: error real (code !== "23505") → store lanza Error("insert source_snapshot fallo: …") SIN exponer la service key en el mensaje.
  </behavior>
  <action>
    Crear `packages/ingest/src/snapshot-store-supabase.ts` exportando la clase `SupabaseSnapshotStore` que implementa `SnapshotStore` de `./snapshot`. Constructor toma `{ url: string; serviceKey: string }` y, opcionalmente, un cliente supabase-js inyectable para tests (firma `{ url; serviceKey; client? }` o un segundo parámetro opts con `createClient` inyectable — preferir un cliente `SupabaseClient`-like inyectable para no tocar red en el test). Internamente construye `createClient(url, serviceKey)` de `@supabase/supabase-js` si no se inyecta cliente (espejo de writer-supabase.ts). El método `insertSnapshot(row)` porta VERBATIM la lógica de worker.ts L197–228: `.from("source_snapshot").insert(row).select("id").single()`; si `error.code === "23505"` hacer el SELECT de recuperación por `.eq("source", …).eq("resource", …).eq("date_bucket", …).maybeSingle()` y devolver `{ id: existing.id }`, o lanzar el Error RETRYABLE nombrando los 3 campos si no aparece; cualquier otro `error` → `throw new Error("insert source_snapshot fallo: " + error.message)` (no incluir la service key). NUNCA loguear ni concatenar `serviceKey` en mensajes de error (T-34-02). No usar bloques de código en este action — la firma exacta vive en worker.ts L197–228, replicarla.
  </action>
  <verify>
    <automated>cd packages/ingest && node node_modules/tsx/dist/cli.mjs --version >/dev/null 2>&1; pnpm --filter @obs/ingest test -- snapshot-store-supabase</automated>
  </verify>
  <acceptance_criteria>
    - `SupabaseSnapshotStore` implementa `SnapshotStore` (tsc no se queja del contrato).
    - Los 4 casos del bloque behavior pasan con un cliente supabase mock (sin red ni DB).
    - El mensaje de error de 23505-sin-fila menciona source/resource/date_bucket y es distinguible de un error de insert genérico.
    - La service key nunca aparece en ningún mensaje de error (grep del archivo: el único uso de serviceKey es el constructor/createClient).
  </acceptance_criteria>
  <done>El store Node-side existe, compila contra la interface SnapshotStore, y su test (insert OK / 23505-recupera / 23505-RETRYABLE / error-real) pasa sin tocar red.</done>
</task>

<task type="auto">
  <name>Task 2: Exportar SupabaseSnapshotStore desde @obs/ingest</name>
  <read_first>
    - packages/ingest/src/index.ts L36–41 (barrel; añadir junto a R2Store/SnapshotWriter)
    - packages/ingest/src/snapshot-store-supabase.ts (creado en Task 1)
  </read_first>
  <action>
    En `packages/ingest/src/index.ts`, añadir tras la línea `export { SnapshotWriter } from "./snapshot";` (L40) un `export { SupabaseSnapshotStore } from "./snapshot-store-supabase";`. No reordenar ni tocar los demás exports. Esto lo hace consumible por `run-probidad-todos-cli.ts` (Plan 02) vía `import { SupabaseSnapshotStore } from "@obs/ingest"`.
  </action>
  <verify>
    <automated>cd packages/ingest && grep -v '^//' src/index.ts | grep -q "SupabaseSnapshotStore" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `import { SupabaseSnapshotStore } from "@obs/ingest"` resuelve (verificable porque Plan 02 lo importará; aquí basta el re-export presente).
    - `pnpm --filter @obs/ingest test` queda verde tras el cambio (sin regresión de snapshot.test.ts existente).
  </acceptance_criteria>
  <done>`SupabaseSnapshotStore` está re-exportado desde el barrel de `@obs/ingest`; los tests del paquete pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Node/tsx → Supabase Postgres | El store escribe a `source_snapshot` con la service key; cruza al plano de datos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | `insertSnapshot` error path | mitigate | El mensaje de error de insert NO concatena la service key ni la fila cruda con PII; solo `error.message` de PostgREST (espejo writer-supabase.ts L9). |
| T-34-02 | Denial of Service | 23505 sin fila recuperable | mitigate | Lanzar Error RETRYABLE explícito (no `undefined`/TypeError) para que el reintento del job reinserte limpio, no se cuelgue. |
| T-34-03 | Elevation of Privilege | RLS de source_snapshot | accept | `source_snapshot` es deny-by-default (0002 L55); solo la service role escribe — el store usa la service key server-side, comportamiento esperado, sin exposición a anon. |
| T-34-SC | Tampering | npm installs | mitigate | Cero dependencias nuevas — `@supabase/supabase-js` ya está en el paquete (writer-supabase). No hay paquetes `[ASSUMED]`/`[SUS]` que verificar. |
</threat_model>

<verification>
- `pnpm --filter @obs/ingest test` verde (incluye el nuevo test del store + snapshot.test.ts sin regresión).
- `grep -v '^//' packages/ingest/src/index.ts | grep SupabaseSnapshotStore` devuelve el export.
- Cero migración / DDL nuevo (la tabla source_snapshot 0002 ya existe).
</verification>

<success_criteria>
- `SupabaseSnapshotStore` existe, implementa `SnapshotStore`, maneja 23505 idempotente igual que el worker Deno, y se exporta desde `@obs/ingest`.
- Test unitario verde sin red ni DB.
- Sin DDL, sin secrets reales necesarios para que el test pase.
</success_criteria>

<output>
Create `.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-01-SUMMARY.md` when done
</output>
