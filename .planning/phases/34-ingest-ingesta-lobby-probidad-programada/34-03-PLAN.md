---
phase: 34-ingest-ingesta-lobby-probidad-programada
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/lobby-camara-weekly.yml
  - .github/workflows/lobby-leylobby-weekly.yml
  - .github/workflows/probidad-weekly.yml
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]
user_setup:
  - service: github-actions-secrets
    why: "Los 3 workflows corren LIVE solo con los secrets del repo destino (Cuchecorp/gov-map). El agente entrega los .yml validables por workflow_dispatch dry-run; encender LIVE es checkpoint de operador."
    env_vars:
      - name: SUPABASE_API_URL
        source: "Repo Settings -> Secrets and variables -> Actions (ya usado por agenda-weekly/leyes-weekly)"
      - name: SUPABASE_SECRET_KEY
        source: "Repo Settings -> Secrets and variables -> Actions"
      - name: R2_ENDPOINT_URL
        source: "Repo Settings -> Secrets and variables -> Actions (ya en agenda-weekly)"
      - name: R2_ACCESS_KEY_ID
        source: "Repo Settings -> Secrets and variables -> Actions"
      - name: R2_SECRET_ACCESS_KEY
        source: "Repo Settings -> Secrets and variables -> Actions"
      - name: R2_BUCKET
        source: "Repo Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "lobby-camara-weekly descarga el crudo de camara.cl via curl -A 'Bot-Ciudadano/1.0' a /tmp/lobby.html, falla si <10KB, corre el CLI con --html-file y asserta audiencias=N>0"
    - "lobby-leylobby-weekly mapea SUPABASE_URL/SUPABASE_SERVICE_KEY (env names divergentes del CLI) y asserta audiencias>0 OR degradaciones>0 (degradacion honesta = exit 0)"
    - "probidad-weekly corre run-probidad-todos-cli con los 4 R2_* + Supabase en env y asserta declaraciones>0 OR confirmados>0"
    - "Los 3 workflows tienen workflow_dispatch + cron en dia distinto a agenda(lun)/leyes(vie) + concurrency group + permissions contents:read"
    - "Inputs pasan por env: y se leen con $VAR — nunca ${{ }} interpolado en un run: con secrets"
  artifacts:
    - path: ".github/workflows/lobby-camara-weekly.yml"
      provides: "Workflow INGEST-01: curl anti-WAF + --html-file + assert audiencias"
      contains: "Bot-Ciudadano"
    - path: ".github/workflows/lobby-leylobby-weekly.yml"
      provides: "Workflow INGEST-02: run directo, env names mapeados, assert acepta degradacion"
      contains: "SUPABASE_URL"
    - path: ".github/workflows/probidad-weekly.yml"
      provides: "Workflow INGEST-03: run directo SPARQL + R2_* en env + assert declaraciones/confirmados"
      contains: "run-probidad-todos-cli"
  key_links:
    - from: ".github/workflows/lobby-camara-weekly.yml"
      to: "packages/lobby/src/run-camara-lobby-cli.ts"
      via: "pnpm --filter @obs/lobby exec tsx ... --html-file /tmp/lobby.html"
      pattern: "run-camara-lobby-cli"
    - from: ".github/workflows/lobby-leylobby-weekly.yml"
      to: "packages/lobby/src/ingest-cli.ts"
      via: "pnpm --filter @obs/lobby exec tsx src/ingest-cli.ts"
      pattern: "ingest-cli"
    - from: ".github/workflows/probidad-weekly.yml"
      to: "packages/probidad/src/run-probidad-todos-cli.ts"
      via: "pnpm --filter @obs/probidad exec tsx src/run-probidad-todos-cli.ts"
      pattern: "run-probidad-todos-cli"
---

<objective>
Crear los 3 workflows de GitHub Actions que programan los conectores ETL ya completos: `lobby-camara-weekly` (INGEST-01), `lobby-leylobby-weekly` (INGEST-02) y `probidad-weekly` (INGEST-03). Cada uno copia el esqueleto de `agenda-weekly.yml`/`leyes-weekly.yml` (checkout -> pnpm -> node 22 -> install -> run con secrets en env:), con las divergencias que RESEARCH/PATTERNS documentaron por fuente: curl anti-WAF para Camara, mapeo de env names divergentes para leylobby, y assert que acepta degradacion honesta.

Purpose: Los pipelines existen pero nunca fueron programados (INGEST-01..03). Estos .yml los ponen en cadencia semanal, validables por `workflow_dispatch` dry-run sin encender LIVE.
Output: 3 archivos `.github/workflows/*.yml`.

NOTA DE AUTONOMIA (LOCKED): el agente entrega los workflows con `workflow_dispatch` + cron y los valida por dry-run; encender LIVE (cargar/confirmar secrets de operador) es needs-human-checkpoint. Ninguna tarea de este plan enciende LIVE ni requiere secrets reales para pasar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-RESEARCH.md
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-PATTERNS.md
@.github/workflows/agenda-weekly.yml
@.github/workflows/leyes-weekly.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: lobby-camara-weekly.yml (INGEST-01, curl anti-WAF + --html-file)</name>
  <read_first>
    - .github/workflows/agenda-weekly.yml L27-71 (esqueleto + secrets->env + inputs por ENV)
    - .github/workflows/leyes-weekly.yml L28-33 (concurrency group + permissions)
    - 34-PATTERNS.md seccion "lobby-camara-weekly.yml" (bloque curl anti-WAF + assert audiencias)
    - packages/lobby/src/run-camara-lobby-cli.ts L52-61 (env names: SUPABASE_API_URL/SUPABASE_SECRET_KEY) y L79-88 (--html-file)
  </read_first>
  <action>
    Crear `.github/workflows/lobby-camara-weekly.yml`. `name: lobby-camara-weekly`. `on: schedule` con cron "0 11 * * 2" (martes 11:00 UTC, distinto de agenda lun y leyes vie) mas `workflow_dispatch` (sin inputs obligatorios). `permissions: contents: read`. `concurrency` group `lobby-camara-weekly`, `cancel-in-progress: false`. Job `lobby-camara` en `ubuntu-latest` con el esqueleto de agenda-weekly (checkout@v4 -> pnpm/action-setup@v4 -> setup-node@v4 node 22 cache pnpm -> `pnpm install --frozen-lockfile --ignore-scripts`). Primer step de trabajo "Descargar crudo Camara (curl anti-WAF)" con `run:` multilinea que ejecuta `curl -sS -A 'Bot-Ciudadano/1.0' -o /tmp/lobby.html` contra `https://www.camara.cl/transparencia/listadodeaudiencias.aspx`, luego `SIZE=$(stat -c%s /tmp/lobby.html)`, `echo` del tamano, y `if [ "$SIZE" -lt 10240 ]; then echo "WAF/respuesta < 10KB"; exit 1; fi`. Segundo step "Run lobby Camara" con `env:` mapeando SUPABASE_API_URL, SUPABASE_SECRET_KEY, R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET desde `${{ secrets.* }}`, y `run:` que captura `OUT=$(pnpm --filter @obs/lobby exec tsx src/run-camara-lobby-cli.ts --html-file /tmp/lobby.html)`, hace `echo "$OUT"`, y asserta `echo "$OUT" | grep -qE 'audiencias=[1-9][0-9]*' || { echo "audiencias=0"; exit 1; }`. Comentario de cabecera: WAF camara.cl bloquea Node fetch -> curl entrega el crudo; el CLI lo lee con --html-file. NO interpolar `${{ }}` dentro de ningun `run:`.
  </action>
  <verify>
    <automated>grep -q "^name:" .github/workflows/lobby-camara-weekly.yml && grep -q "^jobs:" .github/workflows/lobby-camara-weekly.yml && ! grep -qP "\t" .github/workflows/lobby-camara-weekly.yml && grep -q "Bot-Ciudadano" .github/workflows/lobby-camara-weekly.yml && grep -q "html-file /tmp/lobby.html" .github/workflows/lobby-camara-weekly.yml && grep -q "10240" .github/workflows/lobby-camara-weekly.yml && grep -q "workflow_dispatch" .github/workflows/lobby-camara-weekly.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - YAML valido (yaml.safe_load no lanza).
    - Contiene el curl con UA Bot-Ciudadano, el gate <10KB (10240), la invocacion --html-file /tmp/lobby.html y el assert audiencias.
    - Tiene workflow_dispatch + cron martes + concurrency + permissions contents:read.
    - Ningun `${{ }}` interpolado dentro de un `run:` (solo en `env:`).
  </acceptance_criteria>
  <done>El workflow lobby-camara-weekly existe, es YAML valido, descarga el crudo via curl anti-WAF con gate de tamano y corre el CLI con --html-file asertando audiencias>0.</done>
</task>

<task type="auto">
  <name>Task 2: lobby-leylobby-weekly.yml (INGEST-02, env names divergentes + assert acepta degradacion)</name>
  <read_first>
    - .github/workflows/leyes-weekly.yml L55-67 (run directo + inputs por ENV leidos con $VAR)
    - 34-PATTERNS.md seccion "lobby-leylobby-weekly.yml" (env names divergentes + assert no estricto)
    - packages/lobby/src/ingest-cli.ts L65-115 (flags --institucion/--anio/--paginas/--dry-run) y L110-126 (lee SUPABASE_URL/SUPABASE_DB_URL + SUPABASE_SERVICE_KEY/SUPABASE_LOCAL_SERVICE_KEY; sin key -> degrada a dry-run)
    - 34-RESEARCH.md Findings 1b (LeylobbyBloqueadaError -> degradacion honesta, exit 0)
  </read_first>
  <action>
    Crear `.github/workflows/lobby-leylobby-weekly.yml`. `name: lobby-leylobby-weekly`. `on: schedule` cron "0 11 * * 3" (miercoles 11:00 UTC) mas `workflow_dispatch` con inputs opcionales `institucion` (default "") y `anio` (default ""). `permissions: contents: read`. `concurrency` group `lobby-leylobby-weekly`, `cancel-in-progress: false`. Job `lobby-leylobby` en `ubuntu-latest` con el esqueleto estandar (checkout/pnpm/node22/install). Step "Run lobby LeyLobby" con `env:` que mapea los env names DIVERGENTES del CLI: `SUPABASE_URL: ${{ secrets.SUPABASE_API_URL }}` y `SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}` (CRITICO: ingest-cli.ts lee SUPABASE_URL/SUPABASE_SERVICE_KEY, NO SUPABASE_API_URL/SUPABASE_SECRET_KEY; sin este mapeo degrada a dry-run silencioso), mas `INSTITUCION: ${{ github.event.inputs.institucion }}` y `ANIO: ${{ github.event.inputs.anio }}`. `run:` multilinea que arma `ARGS` por ENV leyendo `$INSTITUCION`/`$ANIO` con `if [ -n "$VAR" ]; then ARGS="$ARGS --institucion $INSTITUCION"; fi` (espejo leyes-weekly L64-66; NUNCA interpolar `${{ }}` en el shell), captura `OUT=$(pnpm --filter @obs/lobby exec tsx src/ingest-cli.ts $ARGS)`, hace `echo "$OUT"`, y asserta NO estricto que acepta degradacion honesta: `echo "$OUT" | grep -qE 'audiencias=[1-9][0-9]*|degradaciones=[1-9][0-9]*' || { echo "ni audiencias ni degradaciones"; exit 1; }`. Comentario de cabecera: leylobby = solo instituciones del ejecutivo (Camara/Senado NO publican alli); sin WAF; LeylobbyBloqueadaError es degradacion honesta (exit 0), por eso el assert acepta degradaciones>0. Comentario inline explicando el mapeo de env names divergentes.
  </action>
  <verify>
    <automated>grep -q "^name:" .github/workflows/lobby-leylobby-weekly.yml && grep -q "^jobs:" .github/workflows/lobby-leylobby-weekly.yml && ! grep -qP "\t" .github/workflows/lobby-leylobby-weekly.yml && grep -q "SUPABASE_URL: \${{ secrets.SUPABASE_API_URL }}" .github/workflows/lobby-leylobby-weekly.yml && grep -q "SUPABASE_SERVICE_KEY: \${{ secrets.SUPABASE_SECRET_KEY }}" .github/workflows/lobby-leylobby-weekly.yml && grep -q "degradaciones=" .github/workflows/lobby-leylobby-weekly.yml && grep -q "ingest-cli.ts" .github/workflows/lobby-leylobby-weekly.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - YAML valido.
    - `env:` mapea SUPABASE_URL <- SUPABASE_API_URL y SUPABASE_SERVICE_KEY <- SUPABASE_SECRET_KEY (los env names que el CLI realmente lee).
    - El assert acepta `audiencias>0` OR `degradaciones>0` (no falla por degradacion honesta).
    - Inputs institucion/anio pasan por env: y se leen con $VAR; ningun `${{ }}` en el `run:`.
  </acceptance_criteria>
  <done>El workflow lobby-leylobby-weekly existe, mapea los env names divergentes para que el CLI vea los secrets, y su assert tolera la degradacion honesta de LeylobbyBloqueadaError.</done>
</task>

<task type="auto">
  <name>Task 3: probidad-weekly.yml (INGEST-03, SPARQL + R2_* + assert declaraciones/confirmados)</name>
  <read_first>
    - .github/workflows/leyes-weekly.yml L28-67 (concurrency + run directo + secrets->env)
    - 34-PATTERNS.md seccion "probidad-weekly.yml" (run directo + env R2 + assert)
    - packages/probidad/src/run-probidad-todos-cli.ts L47 (env names SUPABASE_API_URL/SUPABASE_SECRET_KEY) y L97-101 (output declaraciones=/confirmados=)
    - 34-RESEARCH.md Findings 1c (sin WAF; ~155-200 queries x 2-3s ~ 6-10 min; HostRateLimiter aplica el rate-limit, NO sleeps en CI)
  </read_first>
  <action>
    Crear `.github/workflows/probidad-weekly.yml`. `name: probidad-weekly`. `on: schedule` cron "0 11 * * 4" (jueves 11:00 UTC) mas `workflow_dispatch` con input opcional `limite` (default ""). `permissions: contents: read`. `concurrency` group `probidad-weekly`, `cancel-in-progress: false`. Job `probidad` en `ubuntu-latest` con el esqueleto estandar. Step "Run probidad weekly ingest" con `env:` mapeando SUPABASE_API_URL, SUPABASE_SECRET_KEY (los env names que este CLI lee, iguales a agenda/leyes) mas los 4 R2_* (R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET) desde `${{ secrets.* }}` — los R2 habilitan el wire de provenance del Plan 02; sin ellos el CLI corre igual y el bloque R2 queda best-effort/no-op. Tambien `LIMITE: ${{ github.event.inputs.limite }}`. `run:` multilinea que arma ARGS por ENV (`if [ -n "$LIMITE" ]; then ARGS="$ARGS --limit $LIMITE"; fi`), captura `OUT=$(pnpm --filter @obs/probidad exec tsx src/run-probidad-todos-cli.ts $ARGS)`, `echo "$OUT"`, y asserta `echo "$OUT" | grep -qE 'declaraciones=[1-9][0-9]*|confirmados=[1-9][0-9]*' || { echo "sin declaraciones/confirmados"; exit 1; }`. NO anadir `sleep` (el HostRateLimiter del conector ya aplica 2-3s). Comentario de cabecera: SPARQL datos.cplt.cl sin WAF; rate-limit por el conector; R2_* opcionales habilitan la fila source_snapshot por run. NO interpolar `${{ }}` en el `run:`.
  </action>
  <verify>
    <automated>grep -q "^name:" .github/workflows/probidad-weekly.yml && grep -q "^jobs:" .github/workflows/probidad-weekly.yml && ! grep -qP "\t" .github/workflows/probidad-weekly.yml && grep -q "run-probidad-todos-cli.ts" .github/workflows/probidad-weekly.yml && grep -q "declaraciones=" .github/workflows/probidad-weekly.yml && grep -q "R2_ENDPOINT_URL" .github/workflows/probidad-weekly.yml && grep -q "workflow_dispatch" .github/workflows/probidad-weekly.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - YAML valido.
    - Corre run-probidad-todos-cli con los 4 R2_* + Supabase en env: y asserta declaraciones>0 OR confirmados>0.
    - Cron jueves (distinto de lobby-camara mar, leylobby mie, agenda lun, leyes vie); concurrency + permissions presentes.
    - Sin `sleep` en el run; input limite por env: leido con $VAR; ningun `${{ }}` en el `run:`.
  </acceptance_criteria>
  <done>El workflow probidad-weekly existe, corre el CLI SPARQL con R2_* en env para habilitar el wire de provenance, y asserta declaraciones/confirmados>0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> fuente gubernamental | curl/fetch contra camara.cl / leylobby / datos.cplt.cl. |
| Step `run:` con secrets en env -> shell | inputs de workflow_dispatch cruzan al shell del step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Tampering | command injection en step con secrets | mitigate | Inputs (institucion/anio/limite) pasan por `env:` y se leen con `$VAR`; NUNCA `${{ github.event.inputs.* }}` interpolado en un `run:` que lleva SUPABASE_SECRET_KEY/R2_* (espejo agenda L63-66 / leyes L59-67). |
| T-34-09 | Information Disclosure | secrets en logs | mitigate | Solo se hace `echo "$OUT"` (output del CLI, que no imprime la service key — writer-supabase oculta credenciales); nunca `echo` de un secret. GitHub enmascara los secrets registrados de todos modos. |
| T-34-10 | Spoofing | UA / respeto a la fuente | accept | curl usa UA identificatorio `Bot-Ciudadano/1.0` (convencion del proyecto); el rate-limit lo aplica el conector. |
| T-34-11 | Denial of Service | runs solapados | mitigate | `concurrency` group con `cancel-in-progress: false` por workflow + dias de cron distintos evitan pisarse entre si y con agenda/leyes. |
| T-34-SC | Tampering | npm/pip installs | mitigate | `pnpm install --frozen-lockfile --ignore-scripts` (lockfile pineado, scripts desactivados); cero paquetes nuevos. Sin `[ASSUMED]`/`[SUS]`. |
</threat_model>

<verification>
- Los 3 .yml son YAML valido (yaml.safe_load).
- Cada uno tiene workflow_dispatch + cron en dia distinto + concurrency + permissions contents:read.
- lobby-camara: curl Bot-Ciudadano + gate 10240 + --html-file + assert audiencias.
- lobby-leylobby: SUPABASE_URL/SUPABASE_SERVICE_KEY mapeados + assert acepta degradaciones.
- probidad: R2_* en env + assert declaraciones/confirmados.
- Ningun `${{ }}` interpolado dentro de un `run:` (solo en `env:`).
- Validacion final = `workflow_dispatch` dry-run (checkpoint de operador, NO en este plan).
</verification>

<success_criteria>
- 3 workflows creados, YAML valido, copiando el esqueleto agenda/leyes con las divergencias por fuente.
- Cada uno asserta correctamente su senal de exito (audiencias / audiencias-o-degradaciones / declaraciones-o-confirmados).
- Seguridad: inputs por env:, sin interpolacion en shell con secrets; install con --frozen-lockfile --ignore-scripts.
- Ninguna tarea enciende LIVE ni requiere secrets reales para pasar.
</success_criteria>

<output>
Create `.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-03-SUMMARY.md` when done
</output>
