---
phase: 34-ingest-ingesta-lobby-probidad-programada
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - packages/probidad/src/run-probidad-todos.ts
  - packages/probidad/src/run-probidad-todos.test.ts
  - packages/probidad/src/run-probidad-todos-cli.ts
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "runProbidadTodos acepta r2Store + snapshotWriter opcionales y, si están, persiste UN crudo agregado por run a R2 (Etapa 1) best-effort"
    - "Tras el put R2, runProbidadTodos escribe UNA fila source_snapshot por run vía SnapshotWriter.write con las 8 columnas NOT NULL pobladas"
    - "Si R2 o el snapshot fallan, r2Path queda null y la corrida (carga a Supabase) procede (best-effort, NO fatal)"
    - "El CLI construye R2Store + SnapshotWriter(SupabaseSnapshotStore) desde env solo en LIVE y los pasa a runProbidadTodos"
    - "pnpm --filter @obs/probidad test queda verde"
  artifacts:
    - path: "packages/probidad/src/run-probidad-todos.ts"
      provides: "Bloque R2 Etapa-1 agregado por run + SnapshotWriter.write, espejo de run-camara-lobby.ts L85–105"
      contains: "putImmutable"
    - path: "packages/probidad/src/run-probidad-todos-cli.ts"
      provides: "Wire de R2Store + SupabaseSnapshotStore/SnapshotWriter desde env (espejo run-camara-lobby-cli.ts L93–101)"
      contains: "SupabaseSnapshotStore"
    - path: "packages/probidad/src/run-probidad-todos.test.ts"
      provides: "Test: r2Store que lanza → r2Path null, corrida sigue (best-effort); snapshotWriter recibe write con r2_path"
  key_links:
    - from: "packages/probidad/src/run-probidad-todos.ts"
      to: "SnapshotWriter.write → source_snapshot"
      via: "snapshotWriter.write({ source, resource, cacheKey, r2Path, contentHash, fingerprint, dateBucket, provenance })"
      pattern: "snapshotWriter\\.write"
    - from: "packages/probidad/src/run-probidad-todos-cli.ts"
      to: "SupabaseSnapshotStore (de @obs/ingest)"
      via: "new SnapshotWriter(new SupabaseSnapshotStore(...))"
      pattern: "SupabaseSnapshotStore"
---

<objective>
Cerrar INGEST-04 para probidad: añadir el paso Etapa-1 R2 crudo faltante a `run-probidad-todos.ts` (espejo EXACTO de `run-camara-lobby.ts` L85–105, best-effort try/catch, NO fatal) y, tras el put, escribir UNA fila de provenance run-level a `source_snapshot` vía `SnapshotWriter` + el `SupabaseSnapshotStore` del Plan 01. Wirear `R2Store` + `SnapshotWriter` desde env en el CLI (espejo del bloque R2 ya existente en `run-camara-lobby-cli.ts`).

Purpose: Hoy probidad NO escribe crudo a R2 ni provenance a source_snapshot (lobby sí tiene R2 pero descarta la key). Tras este plan, una corrida LIVE de probidad deja una fila source_snapshot con r2_path poblado — la verdad cruda versionada de la Etapa 1 LOCKED.
Output: `run-probidad-todos.ts` (+test) y `run-probidad-todos-cli.ts` modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-RESEARCH.md
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-PATTERNS.md
@.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos del código real. Usar directamente. -->

From packages/ingest/src/r2-store.ts (Etapa 1, content-addressed):
```typescript
sha256Hex(body: Uint8Array): Promise<string>
new R2Store({ accessKeyId, secretAccessKey, endpoint, bucket })
putImmutable(source, resource, date, sha, ext, body): Promise<string>  // key; 412 = idempotente OK
```

From packages/ingest/src/snapshot.ts (la fila a escribir; SnapshotWriter mapea a columnas):
```typescript
new SnapshotWriter(store: SnapshotStore)
writer.write(input: SnapshotWrite): Promise<SnapshotRef>
interface SnapshotWrite {
  source: string; resource: string; cacheKey: string;
  r2Path: string; contentHash: string; fingerprint: string;
  dateBucket: string; provenance: Provenance;  // { source, fetchedAt, sourceUrl }
  ingestRunId?: number;
}
```

From packages/core/src/provenance.ts:
```typescript
makeProvenance(source: string, sourceUrl: string): Provenance  // setea fetchedAt = now()
```

From @obs/ingest (Plan 01): `SupabaseSnapshotStore` (constructor `{ url, serviceKey }`).

Columnas NOT NULL de source_snapshot (0002) que la fila DEBE poblar: source, resource, cache_key, r2_path, content_hash, fingerprint, source_url, date_bucket. Mapeo SnapshotWrite→columna en 34-PATTERNS.md §snapshot-store-supabase (tabla de campos).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bloque R2 Etapa-1 + SnapshotWriter en run-probidad-todos.ts</name>
  <read_first>
    - packages/probidad/src/run-probidad-todos.ts (archivo a modificar; loop L90–115, RunProbidadTodosOpts L27–37, RunProbidadTodosResult L39–52)
    - packages/lobby/src/run-camara-lobby.ts L81–105 (analog EXACTO del bloque R2 best-effort: fechaCaptura→date, putImmutable, try/catch no-fatal)
    - packages/ingest/src/snapshot.ts L17–60 (SnapshotWrite + cómo write mapea a columnas)
    - packages/core/src/provenance.ts (makeProvenance)
    - 34-PATTERNS.md §"packages/probidad/src/run-probidad-todos.ts" (tabla de mapeo de campos NOT NULL)
  </read_first>
  <behavior>
    - Test 1: con r2Store mock que devuelve una key y snapshotWriter mock, runProbidadTodos llama putImmutable UNA vez (crudo agregado por run) y snapshotWriter.write UNA vez con r2Path = la key, source="infoprobidad", resource="declaraciones", y las 8 columnas NOT NULL no vacías.
    - Test 2: r2Store.putImmutable que LANZA → r2Path queda null, snapshotWriter.write NO se llama (o se omite), y la corrida termina normalmente con el writer de declaraciones ya invocado (best-effort, NO fatal). El resultado incluye r2Path: null.
    - Test 3: sin r2Store (opts sin r2Store) → comportamiento idéntico al actual (no R2, no snapshot), r2Path: null. Sin regresión del flujo existente.
  </behavior>
  <action>
    Extender `RunProbidadTodosOpts` con `r2Store?: R2Store` y `snapshotWriter?: SnapshotWriter` (tipos de `@obs/ingest`). Extender `RunProbidadTodosResult` con `r2Path: string | null` (espejo de RunCamaraLobbyResult.r2Path). En el cuerpo: acumular las responses SPARQL crudas del loop (cada `json` de `fetchSparql`) en un array `crudos` dentro del try del loop L100–115 (decisión LOCKED por RESEARCH Open Q1: UN crudo AGREGADO por run, no por query — un solo r2_path representativo). TRAS el loop (y tras `marcarIngestado`), si `opts.r2Store`, ejecutar el bloque best-effort espejo de run-camara-lobby.ts L85–105: serializar `JSON.stringify(crudos)` a bytes con TextEncoder, `sha = await sha256Hex(bytes)`, `r2Path = await opts.r2Store.putImmutable("infoprobidad", "declaraciones", date, sha, "json", bytes)` donde `date = (opts.ingestadoHasta ?? hoy).slice(0,10)`; luego, si `opts.snapshotWriter`, llamar `await opts.snapshotWriter.write({ source: "infoprobidad", resource: "declaraciones", cacheKey: "infoprobidad:declaraciones:" + date, r2Path, contentHash: sha, fingerprint: sha, dateBucket: date, provenance: makeProvenance("infoprobidad", "https://datos.cplt.cl/sparql") })`. (Decisión LOCKED por RESEARCH Pitfall 4 / Open Q2: `cacheKey = source:resource:date`, `fingerprint = sha` del crudo agregado — valores simples que satisfacen los NOT NULL sin consumidor de drift en esta fase.) TODO el bloque en un try/catch que ante error fija `r2Path = null` + log "probidad-todos: Etapa 1 R2/snapshot falló (no fatal): …" y NO re-lanza (la corrida ya escribió a Supabase). Importar `R2Store, SnapshotWriter, sha256Hex` de `@obs/ingest` y `makeProvenance` de `@obs/core`. Devolver `r2Path` en el result. NO inlinear bloques de código en este action — replicar L85–105 de run-camara-lobby.ts adaptando html→JSON.stringify(crudos) y "html"→"json".
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test -- run-probidad-todos</automated>
  </verify>
  <acceptance_criteria>
    - El bloque R2/snapshot corre DESPUÉS de la carga a Supabase y es best-effort (un throw deja r2Path null y no aborta).
    - `snapshotWriter.write` recibe las 8 columnas NOT NULL pobladas (source, resource, cacheKey, r2Path, contentHash, fingerprint, dateBucket, provenance.sourceUrl) — verificable por el mock del test.
    - Exactamente UNA fila por run (un solo putImmutable + un solo write), no N por query.
    - Sin r2Store, comportamiento idéntico al actual (sin regresión).
  </acceptance_criteria>
  <done>`run-probidad-todos.ts` persiste un crudo agregado por run a R2 y una fila source_snapshot vía SnapshotWriter, ambos best-effort; tests verdes incluyendo el caso de R2 que falla.</done>
</task>

<task type="auto">
  <name>Task 2: Wire R2Store + SupabaseSnapshotStore/SnapshotWriter en el CLI desde env</name>
  <read_first>
    - packages/probidad/src/run-probidad-todos-cli.ts (archivo a modificar; loadEnv L36–51, import L18, construcción writer L77–95, output L97–101)
    - packages/lobby/src/run-camara-lobby-cli.ts L52–127 (analog EXACTO: allowlist de env R2, R2Store condicional L93–101, spread condicional L119, output con r2Path L123–127)
  </read_first>
  <action>
    En `run-probidad-todos-cli.ts`: (1) extender el `for (const k of [...])` de `loadEnv` (L47) para incluir `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT_URL`, `R2_BUCKET` además de los 2 Supabase actuales (copiar la lista de run-camara-lobby-cli.ts L52–61). MANTENER el try/catch de loadEnv intacto (NO regresionar el patrón CI-safe de Phase 33). (2) Cambiar el import de `@obs/ingest` (L18) para añadir `R2Store, SnapshotWriter, SupabaseSnapshotStore` junto a `Fetcher, HostRateLimiter, RobotsGuard`. (3) Tras construir el `writer` Supabase, construir `r2Store` condicional (espejo run-camara-lobby-cli.ts L93–101: solo si `!dryRun && env.R2_ACCESS_KEY_ID && env.R2_SECRET_ACCESS_KEY && env.R2_ENDPOINT_URL`, `bucket: env.R2_BUCKET ?? "observatorio"`) y `snapshotWriter` condicional (`!dryRun && env.SUPABASE_API_URL && env.SUPABASE_SECRET_KEY` → `new SnapshotWriter(new SupabaseSnapshotStore({ url: env.SUPABASE_API_URL, serviceKey: env.SUPABASE_SECRET_KEY }))`, si no `undefined`). (4) Pasar ambos a `runProbidadTodos` con spread condicional `...(r2Store ? { r2Store } : {})` y `...(snapshotWriter ? { snapshotWriter } : {})` (espejo L119). (5) Añadir `r2Path=${res.r2Path ?? "none"}` a la línea de output (L97–101, espejo run-camara-lobby-cli.ts L126). NO inlinear código aquí — replicar L93–101/L119/L126 de run-camara-lobby-cli.ts.
  </action>
  <verify>
    <automated>cd packages/probidad && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20; pnpm --filter @obs/probidad exec tsx src/run-probidad-todos-cli.ts --dry-run --limit 1 2>&1 | grep -E 'DRY-RUN|r2Path='</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` limpio en los archivos del plan.
    - Corrida `--dry-run --limit 1` (sin secrets reales) imprime la línea con `r2Path=none` y NO toca R2/DB (r2Store y snapshotWriter quedan undefined en dry-run — doble candado `!dryRun`).
    - En LIVE (con secrets) r2Store y snapshotWriter se construyen; el dry-run nunca los construye.
  </acceptance_criteria>
  <done>El CLI de probidad ensambla R2Store + SnapshotWriter(SupabaseSnapshotStore) desde env solo en LIVE, los pasa a runProbidadTodos, e imprime r2Path; el dry-run corre in-memory sin tocar R2/DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Node/tsx → R2 (S3) | Escribe el crudo SPARQL agregado; aws4fetch firma SigV4 (nunca hand-roll). |
| CLI Node/tsx → Supabase | SnapshotWriter escribe source_snapshot con service key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Information Disclosure | crudo SPARQL en R2 | accept | El crudo de InfoProbidad es dato público (declaraciones CC BY 4.0); R2 es bucket privado, no expuesto. RUT de familiares queda deny-by-default en Supabase, no en el crudo público. |
| T-34-05 | Tampering | bloque R2 best-effort | mitigate | El try/catch deja r2Path null y procede — un fallo de R2 nunca corrompe ni bloquea la carga a Supabase (verdad derivada reconstruible desde R2 cuando R2 esté sano). |
| T-34-06 | Information Disclosure | dry-run vs LIVE | mitigate | r2Store y snapshotWriter SOLO se construyen con `!dryRun` — el dry-run de CI nunca toca R2 ni DB reales (requisito locked: `pnpm test` no necesita secrets). |
| T-34-07 | Spoofing | RUT al LLM | accept | Esta fase no invoca LLM (parser SPARQL literal); no hay superficie de fuga de RUT al modelo. |
| T-34-SC | Tampering | npm installs | mitigate | Cero dependencias nuevas — R2Store/SnapshotWriter/sha256Hex ya en @obs/ingest, makeProvenance en @obs/core. Sin paquetes `[ASSUMED]`/`[SUS]`. |
</threat_model>

<verification>
- `pnpm --filter @obs/probidad test` verde (nuevo caso R2-falla + sin regresión).
- `pnpm --filter @obs/probidad exec tsx src/run-probidad-todos-cli.ts --dry-run --limit 1` imprime `r2Path=none` y NO escribe a R2/DB.
- Cero migración / DDL.
- `pnpm test` (suite completa) verde — gate ACCEPTANCE 5 de la fase.
</verification>

<success_criteria>
- `run-probidad-todos.ts` escribe UN crudo agregado por run a R2 + UNA fila source_snapshot vía SnapshotWriter, ambos best-effort.
- El CLI ensambla R2Store + SnapshotWriter desde env solo en LIVE; dry-run sin secrets corre in-memory.
- Las 8 columnas NOT NULL de source_snapshot quedan pobladas.
- Tests verdes sin secrets reales.
</success_criteria>

<output>
Create `.planning/phases/34-ingest-ingesta-lobby-probidad-programada/34-02-SUMMARY.md` when done
</output>
