---
phase: 117-fecha-fix-etiquetas-de-fecha-corregidas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/format.ts
  - app/lib/format.test.ts
  - app/components/provenance-badge.tsx
  - app/components/provenance-badge.test.tsx
  - app/components/citacion-card.test.tsx
  - app/components/sala-table-section.test.tsx
  - app/components/cruces-de-parlamentario.test.tsx
  - app/components/search-result-card.test.tsx
  - app/components/timeline-view.test.tsx
autonomous: true
requirements: [FECHA-02]

must_haves:
  truths:
    - "El badge de procedencia dice 'según fuente al {fecha}' y jamás 'Actualizado'"
    - "El badge de un recálculo interno dice 'recalculado por el Observatorio al {fecha}', no 'según fuente'"
    - "fechaCorta rinde el mismo día en cualquier huso del runtime (contrato, no accidente del entorno)"
    - "Una fecha del hecho a las 00:14 UTC se rinde con el día chileno real, y una date-only-medianoche-UTC NO se corre al día anterior"
    - "Una fecha del año 2626 se declara no plausible (el llamante puede omitirla honestamente)"
    - "El linter anti-insinuación escanea todas las superficies que 117 toca, ANTES de que el copy nuevo aterrice"
  artifacts:
    - path: "app/lib/format.ts"
      provides: "fechaCorta con timeZone UTC + fechaHechoCorta/fechaHechoCortaSegura + fechaPlausible"
      contains: "fechaPlausible"
    - path: "app/components/provenance-badge.tsx"
      provides: "idiom LOCKED de captura en el chokepoint + props origenFecha/notaAgregacion"
      contains: "según fuente al"
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "SUPERFICIES_FECHA declarada antes del copy"
      contains: "SUPERFICIES_FECHA"
  key_links:
    - from: "app/components/provenance-badge.tsx"
      to: "app/lib/format.ts"
      via: "import de fechaCorta"
      pattern: "fechaCorta"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "TODAS_LAS_SUPERFICIES"
      via: "spread de SUPERFICIES_FECHA"
      pattern: "\\.\\.\\.SUPERFICIES_FECHA"
---

<objective>
Cerrar la CAPA transversal de los hallazgos de fecha: el linter primero, luego los formatters,
luego el chokepoint. Este plan entrega los contratos (helpers y props) que los planes 02, 03 y 04
consumen sin re-derivar nada.

Hallazgos cerrados aquí: **F-01** (copy del chokepoint), **F-10** (timeZone explícita en
`format.ts`), **F-11** (JSDoc de frescura). Contratos entregados para otros planes:
`fechaHechoCorta` / `fechaHechoCortaSegura` (F-05), `fechaPlausible` (F-04), props
`origenFecha` (F-02) y `notaAgregacion` (F-03) del badge.

Purpose: un solo cambio en el chokepoint arregla los 17 call-sites; un solo helper por semántica
evita que cada superficie invente su propia regla de zona horaria.
Output: `format.ts` con 3 helpers nuevos + timeZone UTC, `provenance-badge.tsx` con el idiom
LOCKED, y el linter extendido con las superficies de la fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md

<interfaces>
Contratos que ESTE plan crea y que 02/03/04 consumen. Escribirlos exactamente así.

En `app/lib/format.ts` (exports nuevos):

  export function fechaHechoCorta(d: Date): string
  export function fechaHechoCortaSegura(raw: string | null | undefined, fallback?: string): string
  export function fechaPlausible(d: Date, now?: Date): boolean

En `app/components/provenance-badge.tsx` (props nuevas, ambas opcionales):

  origenFecha?: "fuente" | "recalculo"     // default "fuente"
  notaAgregacion?: string                  // default undefined

Semántica del render de la fecha del badge (rama `capturedAt !== null`):
  origenFecha === "fuente"    → `según fuente al {fechaCorta(capturedAt)}`
  origenFecha === "recalculo" → `recalculado por el Observatorio al {fechaCorta(capturedAt)}`
  y si `notaAgregacion` está definida, se añade ` ({notaAgregacion})` inmediatamente después.
La rama `capturedAt === null` (`Sin fecha de actualización`) NO se toca.

REGLA LOCKED del vocabulario: la palabra "captura" (pelada) está en `TERMINOS_PROHIBIDOS`
del linter anti-insinuación. NINGÚN copy nuevo puede contenerla.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el linter anti-insinuación ANTES del copy (Wave-0)</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - `app/lib/anti-insinuacion-guard.test.ts` COMPLETO (patrón de arrays por carril,
      `TODAS_LAS_SUPERFICIES`, `TERMINOS_PROHIBIDOS`, `NEGACIONES_LOCKED`, tests (1)/(1b)/(1c)).
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-CONTEXT.md` §Reglas LOCKED
      ("extender el linter ANTES del copy").
    - `116-FECHAS-AUDIT.md` §3 F-01..F-14 (para saber qué archivos van a recibir copy nuevo).
  </read_first>
  <action>
    Añadir un array nuevo `SUPERFICIES_FECHA: string[]` con JSDoc que explique el carril
    (Phase 117 / FECHA-02: copy de fecha del hecho vs fecha de captura; el riesgo del carril es
    afirmar que el DATO cambió cuando lo que cambió es nuestro scraping). Rutas relativas a `app/`,
    exactamente estas y en este orden — las que YA viven en otro array se listan igual (el `Set` de
    `TODAS_LAS_SUPERFICIES` dedupea, precedente `SUPERFICIES_LINK_EXT`):

    `components/provenance-badge.tsx`, `components/cruces-de-parlamentario.tsx`,
    `components/cruces-de-proyecto.tsx`, `app/proyecto/[boletin]/page.tsx`,
    `components/timeline-event.tsx`, `components/capa1/tramitacion-stepper.tsx`,
    `components/estado-actual-block.tsx`, `components/votacion-card.tsx`,
    `components/votos-por-parlamentario.tsx`, `components/lobby-de-parlamentario.tsx`,
    `components/lobby-menciones-de-boletin.tsx`, `components/panel-actualidad.tsx`,
    `components/actualidad-module.tsx`, `components/search-result-card.tsx`,
    `components/contratos-de-parlamentario.tsx`, `components/financiamiento-de-parlamentario.tsx`,
    `components/contratos-por-contraparte.tsx`, `components/aportes-por-contraparte.tsx`,
    `lib/format.ts`, `lib/dia-calendario.ts`.

    Sumar `...SUPERFICIES_FECHA` al `Set` de `TODAS_LAS_SUPERFICIES` y añadir el array al bucle de
    carriles del test `(1c)`.

    Añadir un test nuevo `(1d) FECHA-02: los idioms de fecha de 117 están limpios` que monte, como
    fixture EN MEMORIA (no archivo del repo), los cuatro strings verbatim que la fase va a
    renderizar y exija `detectarInsinuaciones(fixture)` = `[]`:
      `según fuente al 14 may 2026`
      `recalculado por el Observatorio al 28 jul 2026`
      `la fuente cubre hasta el 31 dic 2025`
      `nuestra ingesta llega hasta el 31 dic 2025`
      `Hito del 7 jul 2026`
      `Votada el 16 nov 2023`
      `Reunión del 12 jun 2026`
      `Consolidado por el Observatorio`
      `primer trámite 2021`
      `Última consulta a las fuentes`
    Comentar en el JSDoc del test POR QUÉ existe: "captura" está en `TERMINOS_PROHIBIDOS`, así que
    este test es la prueba de que el idiom elegido para 117 no re-introduce el término prohibido.

    NO añadir términos nuevos a `TERMINOS_PROHIBIDOS` (117 no introduce vocabulario insinuante
    nuevo) y NO añadir entradas a `NEGACIONES_LOCKED` (ningún copy nuevo NIEGA un término
    prohibido). Si al escanear aparece un offender preexistente en una superficie recién sumada,
    NO relajar el copy: reportarlo en el SUMMARY como hallazgo y detener el plan.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SUPERFICIES_FECHA" app/lib/anti-insinuacion-guard.test.ts` devuelve ≥ 3 hits
      (declaración, spread en `TODAS_LAS_SUPERFICIES`, bucle del test 1c).
    - El test `(1d)` existe y pasa con los cuatro idioms verbatim.
    - `pnpm vitest run lib/anti-insinuacion-guard.test.ts` exit 0, cero offenders.
    - `TERMINOS_PROHIBIDOS` y `NEGACIONES_LOCKED` sin cambios (diff vacío en esos bloques).
  </acceptance_criteria>
  <done>El linter escanea las 20 superficies del carril FECHA y prueba que los idioms de 117 están limpios, con el copy nuevo aún sin escribir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capa de formatters — F-10, F-05 y F-04 como contrato de código</name>
  <files>app/lib/format.ts, app/lib/format.test.ts</files>
  <read_first>
    - `app/lib/format.ts` completo (`fechaCortaFormatter` :12, `fechaCorta` :21, `relativeTimeEs`
      :33, `esStale` :65, `fechaCortaSegura` :121).
    - `app/lib/dia-calendario.ts` COMPLETO — el contrato date-only-medianoche-UTC ya codificado;
      el helper nuevo debe respetarlo, no duplicarlo.
    - `116-FECHAS-AUDIT.md` §3 F-05 y F-10 (fix sugerido) y §6 límite 6.
    - `116-FECHAS-AUDIT.md` §1.3 (18 filas de auditoría date-only) para no romper ninguna.
  </read_first>
  <behavior>
    - `fechaCorta(new Date("2026-07-20T00:00:00Z"))` ⇒ `"20 jul 2026"` en CUALQUIER `TZ` del proceso
      (probar con `process.env.TZ` = `"Pacific/Kiritimati"` y `"America/Santiago"` reconstruyendo el
      formatter, o probando `fechaCorta` sobre husos extremos mediante `Intl` con timeZone fijada).
    - `fechaHechoCorta(new Date("2026-07-20T00:00:00.000Z"))` ⇒ `"20 jul 2026"` (date-only
      disfrazada: hora UTC exactamente 00:00:00.000 ⇒ NO se convierte de zona).
    - `fechaHechoCorta(new Date("2023-11-17T00:14:41Z"))` ⇒ `"16 nov 2023"` (hora real ⇒ día chileno).
    - `fechaHechoCorta(new Date("2026-07-20T15:00:00Z"))` ⇒ `"20 jul 2026"` (hora real, mismo día CL).
    - `fechaHechoCortaSegura(null)` ⇒ `"fecha no informada"`; `fechaHechoCortaSegura("basura")` ⇒
      fallback; `fechaHechoCortaSegura("2023-11-17T00:14:41+00:00")` ⇒ `"16 nov 2023"`;
      `fechaHechoCortaSegura("2026-03-31")` ⇒ `"31 mar 2026"` (date-only puro, sin corrimiento).
    - `fechaPlausible(new Date("2626-05-25T00:00:00Z"))` ⇒ `false`;
      `fechaPlausible(new Date("1989-12-31T00:00:00Z"))` ⇒ `false`;
      `fechaPlausible(new Date("2026-07-28T00:00:00Z"))` ⇒ `true`;
      `fechaPlausible(new Date("2030-01-01T00:00:00Z"), new Date("2026-07-28T00:00:00Z"))` ⇒ `true`
      (dentro de hoy + 5 años, para no romper /agenda ni urgencias futuras legítimas).
  </behavior>
  <action>
    (a) **F-10**: añadir `timeZone: "UTC"` a `fechaCortaFormatter` (`:12`). Comentar verbatim el
    porqué del audit: UTC —NO `America/Santiago`— porque preserva el comportamiento actual correcto
    y lo convierte en contrato del código en vez de accidente del entorno; convertir a Chile
    fabricaría el día anterior en las ~45.618 filas date-only-disfrazadas de `timestamptz`.

    (b) **F-05**: añadir `fechaHechoCorta(d: Date): string` — helper para fechas del HECHO que
    ramifica por presencia de hora: si `d.getUTCHours()`, `getUTCMinutes()`, `getUTCSeconds()` y
    `getUTCMilliseconds()` son todos 0 ⇒ date-only disfrazada ⇒ formatear con el formatter UTC
    (`fechaCorta`); si hay hora real ⇒ formatear con un `Intl.DateTimeFormat("es-CL", {day:"2-digit",
    month:"short", year:"numeric", timeZone:"America/Santiago"})` propio (constante de módulo,
    no construir por llamada). JSDoc que cite F-05 y el límite 6 del audit: esto MITIGA en el
    render; la corrección de fondo (dos semánticas en una columna) es de ingesta y queda declarada.

    (c) Añadir `fechaHechoCortaSegura(raw, fallback = "fecha no informada")` — espeja el guard
    anti-500 de `fechaCortaSegura` pero SIN el `slice(0,10)` destructivo: valida con
    `/^\d{4}-\d{2}-\d{2}(T.*)?$/` sobre el raw trim-eado, construye `new Date(raw)`, y delega en
    `fechaHechoCorta`. Un raw date-only puro (`"2026-03-31"`) sigue dando el día correcto porque
    `new Date("YYYY-MM-DD")` es medianoche UTC ⇒ cae en la rama date-only.

    (d) **F-04**: añadir `fechaPlausible(d: Date, now: Date = new Date()): boolean` — `true` sólo si
    `d` cae en `[1990-01-01T00:00:00Z, now + 5 años]`. JSDoc: rango del fix sugerido de F-04; es un
    PREDICADO, no un filtro — el llamante decide la omisión honesta. Prohibido usarlo como
    `where fecha <= current_date` global: /agenda muestra futuro legítimo (17 citaciones futuras
    verificadas en PROD).

    NO tocar `fechaCortaSegura`, `relativeTimeEs`, `esStale` ni `STALE_THRESHOLD_MS`.
    Escribir los tests de `<behavior>` en `app/lib/format.test.ts` ANTES de implementar.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/format.test.ts lib/dia-calendario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los 4 grupos de asserts de `<behavior>` pasan; en particular
      `fechaHechoCorta(new Date("2023-11-17T00:14:41Z")) === "16 nov 2023"`.
    - `grep -n 'timeZone: "UTC"' app/lib/format.ts` devuelve ≥ 1 hit.
    - `grep -c 'America/Santiago' app/lib/format.ts` = 1 (solo el formatter de hora real de
      `fechaHechoCorta`; JAMÁS en `fechaCortaFormatter`).
    - `pnpm vitest run lib/format.test.ts lib/dia-calendario.test.ts` exit 0.
    - Suite completa de app sigue verde tras el cambio de `fechaCorta` (`pnpm --filter app test`).
  </acceptance_criteria>
  <done>`format.ts` expone los tres helpers nuevos, `fechaCorta` fija UTC explícita, y ninguna date-only se corre de día.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chokepoint — F-01 (idiom LOCKED) + F-11 (JSDoc) + props para F-02/F-03</name>
  <files>app/components/provenance-badge.tsx, app/components/provenance-badge.test.tsx, app/components/citacion-card.test.tsx, app/components/sala-table-section.test.tsx, app/components/cruces-de-parlamentario.test.tsx, app/components/search-result-card.test.tsx, app/components/timeline-view.test.tsx</files>
  <read_first>
    - `app/components/provenance-badge.tsx` COMPLETO (JSDoc :14-22, props :36-58, `stale` :66,
      `displaySource` :67, render de la fecha :89-93, tooltip :122-135).
    - `app/components/provenance-badge.test.tsx` COMPLETO.
    - `116-FECHAS-AUDIT.md` §3 F-01, F-02, F-03, F-11 (fix sugerido de cada uno) y §1.1
      (texto VISIBLE exacto del badge, verificado con `grep -F`).
    - Contraejemplo limpio que el audit manda copiar: `app/components/estado-actual-block.tsx:429`
      (`según {sourceLabel(...)} al {fechaCorta(...)}.`).
    - `app/lib/format.ts` tras la Task 2 (los helpers ya existen).
  </read_first>
  <behavior>
    - Con `capturedAt = new Date("2026-05-14T10:00:00Z")`, `sourceName="Cámara"`: el texto visible
      contiene `según fuente al 14 may 2026` y NO contiene `Actualizado`.
    - Con `origenFecha="recalculo"`: el texto visible contiene
      `recalculado por el Observatorio al 14 may 2026` y NO `según fuente al`.
    - Con `notaAgregacion="evento más reciente"`: el texto contiene
      `según fuente al 14 may 2026 (evento más reciente)`.
    - Con `capturedAt = null`: sigue diciendo exactamente `Sin fecha de actualización` y
      `fuente desconocida` (rama intacta).
    - El tooltip sigue mostrando `capturedAt.toISOString()` y suma una línea
      `consultado {relativeTimeEs(capturedAt)}` (la señal de recencia no se pierde, sólo deja de
      ser el rótulo principal).
    - `esStale` a 14 días sigue pintando amber (sin cambio de comportamiento).
  </behavior>
  <action>
    (a) **F-01**: sustituir el contenido de `:90`. La rama `capturedAt !== null` renderiza, según
    `origenFecha`, el prefijo `según fuente al ` o `recalculado por el Observatorio al `, seguido de
    `fechaCorta(capturedAt)`, seguido de ` ({notaAgregacion})` si la prop está definida. Importar
    `fechaCorta` desde `@/lib/format` (junto a `relativeTimeEs` y `esStale`, que se conservan).
    Mantener intactas: la rama `null`, `displaySource`, el `·`, el enlace `fuente oficial ↗`, el
    guard `safeExternalHref`, la lógica `declarar`/`densidad` y la estructura del DOM (mismo
    `<span>` en la misma posición — WR-05 de 115: no cambiar la caja).

    (b) Mover `relativeTimeEs` al tooltip: dentro del `div` de `TooltipContent`, junto al
    `toISOString()`, añadir `<div>consultado {relativeTimeEs(capturedAt)}</div>` en la rama
    `capturedAt !== null`.

    (c) Declarar las props nuevas en `ProvenanceBadgeProps` con JSDoc: `origenFecha` cita F-02
    (`cruce_senal.fecha_captura` es el `now()` del FULL REBUILD diario, no una observación de la
    fuente — decirle "según fuente" sería igual de impreciso que "Actualizado"); `notaAgregacion`
    cita F-03 (el badge de sección declara la frescura de un MAX y debe calificar la agregación).

    (d) **F-11**: corregir el JSDoc `:17-21`. Reemplazar `"Actualizado hace X · {fuente} — fuente
    oficial ↗"` por el texto real nuevo, y `"más de 48h se marca en amber"` por
    `"más de 14 días (umbral por cadence de ingesta, `format.ts` STALE_THRESHOLD_MS) se marca en
    amber"`. Cero cambio de comportamiento.

    (e) Actualizar los 6 asserts existentes que buscan `/Actualizado/`:
    `provenance-badge.test.tsx:25` y `:211` (usar `/según fuente al/`),
    `citacion-card.test.tsx:152`, `sala-table-section.test.tsx:60`,
    `search-result-card.test.tsx:43`, `cruces-de-parlamentario.test.tsx:325,336`.
    `timeline-view.test.tsx:213` asserta `not.toMatch(/Actualizado/)` — cambiar a
    `not.toMatch(/según fuente al/)` para que siga probando lo mismo (SC7: cero badge por evento).
    En `cruces-de-parlamentario.test.tsx` dejar el assert como `/según fuente al/` por ahora: el
    Plan 03 lo mueve a `/recalculado por el Observatorio al/` cuando pase `origenFecha="recalculo"`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/provenance-badge.test.tsx components/citacion-card.test.tsx components/sala-table-section.test.tsx components/search-result-card.test.tsx components/cruces-de-parlamentario.test.tsx components/timeline-view.test.tsx lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Actualizado" app/components/provenance-badge.tsx` ⇒ 0 hits fuera de comentarios;
      `grep -v '^\s*\*' app/components/provenance-badge.tsx | grep -c "Actualizado"` = 0.
    - `grep -c "según fuente al" app/components/provenance-badge.tsx` ≥ 1 y
      `grep -c "recalculado por el Observatorio al" app/components/provenance-badge.tsx` ≥ 1.
    - `grep -n "Sin fecha de actualización" app/components/provenance-badge.tsx` sigue presente.
    - Todos los asserts de `<behavior>` pasan.
    - `pnpm --filter app test` exit 0 y `pnpm -r exec tsc -b` exit 0.
  </acceptance_criteria>
  <done>Los 17 call-sites del badge dicen el idiom LOCKED con un solo cambio; el JSDoc ya no miente sobre el umbral; las props para F-02/F-03 existen y están probadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB PROD → render SSR | `fecha_captura`/`fecha` llegan como strings de la RPC y se formatean sin sanitizar semánticamente |
| copy renderizado → ciudadano/prensa | una etiqueta de fecha errónea es una afirmación falsa publicada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Information disclosure (semántica) | `provenance-badge.tsx` | mitigate | idiom LOCKED "según fuente al…"; test de string exacta en `provenance-badge.test.tsx` |
| T-117-02 | Tampering (copy drift) | superficies de fecha | mitigate | `SUPERFICIES_FECHA` en el linter anti-insinuación + test `(1d)` de idioms limpios |
| T-117-03 | Repudiation (día erróneo) | `format.ts` | mitigate | `timeZone:"UTC"` explícita + `fechaHechoCorta` ramificado, con tests en husos extremos |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes; cero comandos de package manager |
</threat_model>

<verification>
- `pnpm --filter app test` verde (suite ~1428).
- `pnpm -r exec tsc -b` exit 0.
- Guards de régimen verdes: anti-insinuación, lockdown, anti-flip MONEY, bento, name-match-rut,
  env-example, integ-scope, provider-guard.
- `grep -rn "Actualizado" app/components/*.tsx` ⇒ solo comentarios.
- Flags NO tocados: `git diff --stat` no incluye `.env`, `.env.example` ni ningún gate.
</verification>

<success_criteria>
- F-01, F-10 y F-11 corregidos con evidencia antes/después.
- `fechaHechoCorta`, `fechaHechoCortaSegura`, `fechaPlausible`, `origenFecha` y `notaAgregacion`
  existen, están tipados y probados — los planes 02/03/04 los consumen sin re-derivar.
- Cero uso de `timeZone: "America/Santiago"` sobre date-only en todo el diff.
- Suite + typecheck + guards verdes.
</success_criteria>

<output>
Create `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-01-SUMMARY.md` when done.
Registrar, por hallazgo (F-01/F-10/F-11), el antes/después verbatim con archivo:línea y el commit
— el Plan 04 los consume para `117-DISPOSICION.md`.
</output>
