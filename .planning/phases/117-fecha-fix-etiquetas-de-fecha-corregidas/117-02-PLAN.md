---
phase: 117-fecha-fix-etiquetas-de-fecha-corregidas
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - app/components/timeline-view.tsx
  - app/components/timeline-view.test.tsx
  - app/components/timeline-event.tsx
  - app/components/capa1/tramitacion-stepper.tsx
  - app/components/estado-actual-block.tsx
  - app/components/estado-actual-block.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
autonomous: true
requirements: [FECHA-02]

must_haves:
  truths:
    - "La fecha del año 2626 del boletín 18232-25 ya no aparece en ninguna superficie de proyecto"
    - "El badge de la sección Tramitación dice 'según fuente al {fecha} (evento más reciente)'"
    - "Las seis fechas date-only del bloque '¿Dónde está hoy?' —incluidos los dos aria-label— se rinden con diaCalendarioCitacion, no con fechaCorta"
    - "El bloque de urgencia ya no duplica la misma fecha absoluta entre paréntesis"
    - "Un evento de tramitación de las 00:14 UTC se rinde con el día chileno real"
  artifacts:
    - path: "app/components/estado-actual-block.tsx"
      provides: "date-only por diaCalendarioCitacion + guard de plausibilidad + urgencia sin relativeTimeEs"
      contains: "badgeFechaCitacion"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "badge de sección con calificador de agregación"
      contains: "notaAgregacion"
    - path: "app/components/timeline-view.tsx"
      provides: "fechaValida con guard de plausibilidad + mesAnioFormatter con timeZone UTC"
      contains: "fechaPlausible"
  key_links:
    - from: "app/components/timeline-view.tsx"
      to: "app/lib/format.ts"
      via: "import de fechaPlausible"
      pattern: "fechaPlausible"
    - from: "app/components/estado-actual-block.tsx"
      to: "app/lib/dia-calendario.ts"
      via: "badgeFechaCitacion / dayLabelCitacion"
      pattern: "badgeFechaCitacion|dayLabelCitacion"
---

<objective>
Cerrar el carril `/proyecto/[boletin]`: **F-03** (badge de sección sobre un MAX), **F-04** (fecha
corrupta 2626), **F-05** (día UTC en `tramitacion_evento.fecha`), **F-07** (parte proyecto: rótulo
del hito), **F-09** (seis renders date-only con el formatter que el propio archivo prohíbe),
**F-13** (`relativeTimeEs` sobre una fecha del hecho) y el residual de **F-10** en
`timeline-view.tsx`.

Purpose: es la superficie donde conviven las tres semánticas (hecho, captura, date-only) y donde el
audit encontró la inconsistencia más dura — el mismo archivo prohíbe el patrón en su JSDoc y lo usa
seis líneas más abajo.
Output: carril proyecto con cada fecha rotulada por lo que es y sin ningún día fabricado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-01-SUMMARY.md

<interfaces>
Ya existen tras el Plan 01 (`app/lib/format.ts`):
  fechaHechoCorta(d: Date): string
  fechaHechoCortaSegura(raw: string | null | undefined, fallback?: string): string
  fechaPlausible(d: Date, now?: Date): boolean
`fechaCorta` ahora fija `timeZone: "UTC"`.

`ProvenanceBadge` (`app/components/provenance-badge.tsx`) acepta:
  origenFecha?: "fuente" | "recalculo"   // default "fuente" ⇒ `según fuente al {fecha}`
  notaAgregacion?: string                // ⇒ ` ({notaAgregacion})` tras la fecha

Ya existen desde antes (`app/lib/dia-calendario.ts`):
  diaCalendarioCitacion(f): string | null    // "YYYY-MM-DD", parte fecha UTC, sin tz
  badgeFechaCitacion(f): string | null       // "20-jul"
  dayLabelCitacion(f): string | null         // "Lunes 20 de julio"

ANCHORS: re-localizar SIEMPRE con grep antes de editar (las líneas del audit pueden haberse movido).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F-04 + F-05 + F-10 residual en el timeline y el stepper</name>
  <files>app/components/timeline-view.tsx, app/components/timeline-view.test.tsx, app/components/timeline-event.tsx, app/components/capa1/tramitacion-stepper.tsx</files>
  <read_first>
    - `app/components/timeline-view.tsx` (`mesAnioFormatter` :29, `fechaValida` exportada :42 —
      la reusa el stepper).
    - `app/components/timeline-event.tsx` (render de la fecha :81-82).
    - `app/components/capa1/tramitacion-stepper.tsx` (`fechaValida` importada :4, render :98-99 y
      urgencia :193-194).
    - `116-FECHAS-AUDIT.md` §3 **F-04**, **F-05**, **F-10** (fix sugerido de cada uno) y §6 límites
      6 y 7 (la deuda de datos NO se cierra aquí).
    - `app/lib/format.ts` tras el Plan 01.
  </read_first>
  <behavior>
    - `fechaValida("2626-05-25T00:00:00+00:00")` ⇒ `null` (fuera del rango plausible ⇒ omisión
      honesta, patrón que estos componentes ya usan para fecha inválida).
    - `fechaValida("1989-06-01T00:00:00Z")` ⇒ `null`.
    - `fechaValida("2026-07-07T00:00:00Z")` ⇒ `Date` válida.
    - `fechaValida(null)` / `fechaValida("basura")` ⇒ `null` (comportamiento previo intacto).
    - Renderizando `TimelineEvent` con `fecha` del 2626, el contenedor NO contiene `2626`.
    - `TimelineEvent` con `fecha = "2023-11-17T00:14:41Z"` renderiza `Hito del 16 nov 2023`.
    - `TramitacionStepper` con `evento.descripcion = "Ingreso de proyecto"` y fecha válida rinde
      `Ingreso de proyecto — {fecha}` (el guion largo U+2014 entre ambos, en ese orden).
  </behavior>
  <action>
    (a) **F-04**: en `timeline-view.tsx`, extender `fechaValida` para devolver `null` también cuando
    `!fechaPlausible(d)` (importar de `@/lib/format`). Actualizar su JSDoc: es el helper que el
    stepper reusa (T-55-11), así que el guard se propaga solo; citar F-04 y el boletín `18232-25`
    con `2626-05-25` como el caso real de PROD. Dejar comentado que las 2 filas corruptas se
    reportan a la fase de ingesta (límite 7 del audit) — 117 sólo filtra el render.

    (b) **F-04 en `timeline-event.tsx`**: `:82` recibe un `Date` ya construido por el llamante. Si el
    llamante no pasó por `fechaValida`, blindar aquí: renderizar la fecha sólo si
    `fechaPlausible(fecha)`; si no, omitir el `<span>` completo (nunca un placeholder inventado).

    (c) **F-05**: sustituir `fechaCorta(...)` por `fechaHechoCorta(...)` en `timeline-event.tsx:82`,
    `tramitacion-stepper.tsx:99` y `tramitacion-stepper.tsx:194` — las tres consumen
    `tramitacion_evento.fecha`, que es `timestamptz` con dos semánticas mezcladas. Comentar el
    porqué citando F-05 (drift real 27 filas; convertir a ciegas rompería 44.569).

    (d) **F-10 residual**: añadir `timeZone: "UTC"` al `mesAnioFormatter` de `timeline-view.tsx:29`
    con el comentario del fix sugerido (UTC, no `America/Santiago`).

    (e) **F-07 (lado proyecto)** — las dos superficies rinden HOY la fecha como un `<span
    className="font-mono">` pelado, y hay que cerrarlas de forma DISTINTA porque su adyacencia a la
    descripción es distinta (verificado en el árbol, no asumido del audit):

      · `capa1/tramitacion-stepper.tsx:98-99` — la fecha YA vive dentro del MISMO `<p>` que
        `{evento.descripcion}`, inmediatamente después. Falta sólo el separador del fix sugerido:
        anteponer un guion largo `—` (U+2014, con espacios alrededor) antes del `<span>` de la
        fecha, de modo que el render sea `{descripcion} — {fecha}`. CERO rótulo nuevo: la
        descripción ES el sustantivo del hecho.

      · `timeline-event.tsx:80-84` — aquí la fecha NO es adyacente: vive en el header junto al
        `CamaraChip`, y `{evento.descripcion}` se renderiza en un `<p>` aparte, más abajo. El
        separador no sirve. Anteponer el rótulo `Hito del ` a la fecha (dentro del mismo `<span>`,
        con el `font-mono` sólo sobre la fecha si se puede sin cambiar la caja).

    El idiom `Hito del …` ya está registrado en el fixture `(1d)` del linter (Plan 01, Task 1) —
    NO inventar un rótulo distinto. Si el ejecutor considera que otro rótulo es mejor, debe
    añadirlo PRIMERO al fixture del linter y sólo después al copy (regla LOCKED de 117-CONTEXT).

    NO tocar `esEventoUrgencia`, `esRetiroUrgencia` ni la heurística de colapso.
    Escribir los asserts de `<behavior>` en `timeline-view.test.tsx` antes de implementar.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/timeline-view.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fechaPlausible" app/components/timeline-view.tsx` ≥ 1.
    - `grep -v '^\s*[*/]' app/components/timeline-event.tsx | grep -c "fechaCorta("` = 0
      (sustituido por `fechaHechoCorta`).
    - `grep -v '^\s*[*/]' app/components/capa1/tramitacion-stepper.tsx | grep -c "fechaCorta("` = 0.
    - `grep -n 'timeZone: "UTC"' app/components/timeline-view.tsx` ≥ 1 hit.
    - `grep -c "Hito del" app/components/timeline-event.tsx` = 1.
    - `grep -c "—" app/components/capa1/tramitacion-stepper.tsx` ≥ 1 en la línea contigua al
      `<span>` de la fecha (F-07: `{descripcion} — {fecha}`).
    - El guard anti-insinuación sigue verde con los dos idioms nuevos ya en el fixture `(1d)`.
    - Todos los asserts de `<behavior>` pasan; `pnpm vitest run components/timeline-view.test.tsx`
      exit 0.
  </acceptance_criteria>
  <done>Ninguna fecha implausible llega al DOM del carril proyecto y las fechas del hecho se rinden con el día chileno real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F-09 + F-13 + F-04 + F-03 (matiz) en estado-actual-block</name>
  <files>app/components/estado-actual-block.tsx, app/components/estado-actual-block.test.tsx</files>
  <read_first>
    - `app/components/estado-actual-block.tsx` COMPLETO. Especial atención al JSDoc `:144-151`
      (que PROHÍBE el patrón que el propio archivo usa), `fechaValida` :88, los usos correctos de
      `diaCalendarioCitacion` en :189/:221/:237/:270, y los renders :397, :412-417, :428-429,
      :445, :460, :475, :479 (aria-label), :494 (aria-label), :497.
    - `116-FECHAS-AUDIT.md` §3 **F-09**, **F-13**, **F-04**, **F-03** y §1.2 fila 10 (`:429` es el
      CONTRAEJEMPLO LIMPIO — su idiom `según {sourceLabel(...)} al {fechaCorta(...)}.` NO se toca,
      sólo se le añade el calificador de agregación).
    - `app/lib/dia-calendario.ts` COMPLETO.
    - `app/components/estado-actual-block.test.tsx` COMPLETO.
  </read_first>
  <behavior>
    - Con `citacionVigente.fecha = "2026-07-20T00:00:00Z"`, el texto renderizado contiene
      `20-jul` (vía `badgeFechaCitacion`) y NO se corre a `19`.
    - Los dos `aria-label` de sesión de sala/citación contienen el mismo día que el texto visible
      (assert por `getByLabelText` o por `container.querySelector("[aria-label]")`).
    - Con `ultimoHito.fecha = "2626-05-25T00:00:00Z"`, el render NO contiene `2626` (la línea
      "Último hito" se omite honestamente o cae al hito plausible más reciente).
    - El bloque de urgencia vigente NO contiene la fecha absoluta duplicada: dado
      `urgenciaEstado.desde = "2026-07-22T00:00:00Z"`, `2026`/`22 jul` aparece exactamente UNA vez
      en ese bloque.
    - El copy `según … al …` de la línea de fuente de urgencia contiene el calificador
      `(evento más reciente)`.
  </behavior>
  <action>
    (a) **F-09**: en las SEIS ocurrencias (`:445`, `:460`, `:475`, `:479` aria-label, `:494`
    aria-label, `:497`) sustituir `fechaCorta(...)` por `badgeFechaCitacion(...)` — ambos delegan en
    `diaCalendarioCitacion`, ya importado. Donde el copy pida un día legible completo usar
    `dayLabelCitacion`. Manejar el `null` de retorno con la omisión honesta ya usada en el archivo
    (nunca `"Invalid Date"` ni una fecha inventada). Añadir un comentario que apunte al JSDoc
    `:144-151`: esta es la coherencia que el archivo ya declaraba y no cumplía (F-09).

    (b) **F-13**: eliminar el paréntesis `({relativeTimeEs(urgenciaEstado.desde)})` de `:417` y su
    `<span>` contenedor, dejando sólo la fecha absoluta de `:413`. Quitar `relativeTimeEs` del
    import si queda sin uso. Comentar el porqué verbatim del audit: `relativeTimeEs` existe para el
    reloj de scraping (su parámetro se llama `capturedAt`) y a ≥7 días duplicaba la misma fecha
    absoluta ya mostrada.

    (c) **F-04**: en `fechaValida` (`:88`) aplicar el mismo guard de plausibilidad
    (`fechaPlausible` de `@/lib/format`) que la Task 1 aplicó en `timeline-view`. Esto mata el
    `25 may 2626` del "Último hito" (`:397`) sin filtro global.

    (d) **F-05** en `:397` y `:413`: `tramitacion_evento.fecha` ⇒ usar `fechaHechoCorta`.

    (e) **F-03 (matiz)**: en `:429` NO tocar el idiom (es el contraejemplo limpio del audit); añadir
    únicamente el calificador de agregación tras la fecha: ` (evento más reciente)`, porque el
    valor viene de un `max(fecha_captura)` derivado en `:332-338`.

    NO tocar los predicados de vigencia (`:189`, `:221`), la clave de dedup (`:270`) ni
    `DIA_CALENDARIO_CHILE_HOY` (`:152`) — el audit los declara correctos y son el ejemplo canónico.

    **PROHIBICIÓN EXPLÍCITA (load-bearing):** `fechaCorta(urgenciaFuente.fechaCaptura)` en `:429`
    CONSERVA `fechaCorta` — es lo correcto y debe sobrevivir al diff. `fechaCorta` quedó con
    `timeZone:"UTC"` tras el Plan 01, que es exactamente el helper que una `fecha_captura` necesita
    en este render. `fecha_captura` JAMÁS se rutea por `fechaHechoCorta`: su rama de hora real
    formatea en `America/Santiago` y cambiaría el día visible en silencio — el defecto preciso que
    esta fase existe para impedir. `fechaHechoCorta` es sólo para fechas del HECHO
    (`tramitacion_evento.fecha`, `votacion.fecha`).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/estado-actual-block.test.tsx lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*[*/]' app/components/estado-actual-block.tsx | grep -c "fechaCorta("` = **1**,
      y esa ÚNICA ocurrencia es la línea de `urgenciaFuente.fechaCaptura` (`:429`). Todo lo demás
      migró a `badgeFechaCitacion` / `dayLabelCitacion` / `fechaHechoCorta`. Verificar el sujeto con
      `grep -n "fechaCorta(" app/components/estado-actual-block.tsx` — debe devolver una sola línea
      y contener `fechaCaptura`.
    - `grep -v '^\s*[*/]' app/components/estado-actual-block.tsx | grep -c "relativeTimeEs"` = 0.
    - `grep -c "evento más reciente" app/components/estado-actual-block.tsx` ≥ 1.
    - `grep -c "según" app/components/estado-actual-block.tsx` ≥ 1 (el idiom limpio sobrevive).
    - Todos los asserts de `<behavior>` pasan; el guard anti-insinuación sigue verde.
  </acceptance_criteria>
  <done>El bloque "¿Dónde está hoy?" es internamente coherente: date-only por `diaCalendarioCitacion`, hechos por `fechaHechoCorta`, captura con idiom + calificador, cero fecha implausible.</done>
</task>

<task type="auto">
  <name>Task 3: F-03 — badge de sección Tramitación con calificador de agregación</name>
  <files>app/app/proyecto/[boletin]/page.tsx</files>
  <read_first>
    - `app/app/proyecto/[boletin]/page.tsx` — el `reduce` que calcula el MAX (`:492-497`) y el
      `<ProvenanceBadge>` del heading (`:503-...`), más el comentario `:488` (SC7: UN badge por
      sección).
    - `116-FECHAS-AUDIT.md` §3 **F-03** y §1.2 fila 1 (la columna real es
      `tramitacion_evento.fecha_captura`, NO `source_snapshot`).
    - `app/components/provenance-badge.tsx` tras el Plan 01 (prop `notaAgregacion`).
  </read_first>
  <action>
    Pasar `notaAgregacion="evento más reciente"` al `<ProvenanceBadge>` del heading "Tramitación",
    de modo que el badge rinda `según fuente al {fecha} (evento más reciente)`. Corregir el
    comentario que documenta el origen del `capturedAt` para que cite `tramitacion_evento.fecha_captura`
    (§1.2 fila 1 del audit resolvió que la atribución a `source_snapshot` era imposible: esa tabla
    no tiene ni `fecha_captura` ni `proyecto_id`).

    Añadir junto al `reduce` un comentario que registre el defecto de agregación que el calificador
    hace visible (F-03): un solo evento re-scrapeado hoy hacía que la sección entera afirmara estar
    fresca — verificado en PROD sobre el boletín 14309-04, `max(fecha_captura) = 2026-07-09` sobre
    99 eventos cuyo hecho más reciente es del `2026-07-07`.

    NO cambiar el `reduce` ni el número de badges (SC7: UN badge por sección, no 100+).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts && pnpm -r exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notaAgregacion" "app/app/proyecto/[boletin]/page.tsx"` devuelve 1 hit con el valor
      `"evento más reciente"`.
    - `grep -c "source_snapshot" "app/app/proyecto/[boletin]/page.tsx"` = 0 en comentarios sobre el
      origen del `capturedAt`.
    - `grep -c "ProvenanceBadge" "app/app/proyecto/[boletin]/page.tsx"` sin aumentar respecto al
      estado previo (SC7 intacto).
    - `pnpm --filter app test` exit 0 y `pnpm -r exec tsc -b` exit 0.
  </acceptance_criteria>
  <done>La sección Tramitación declara que la frescura es la del evento más reciente, no la del set completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tramitacion_evento` PROD → render | columna con dos semánticas mezcladas + 2 filas corruptas del año 2626 |
| `citacion.fecha` / `sesion_sala.fecha` → aria-label | canal único de la fecha para lectores de pantalla |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04 | Repudiation (dato falso publicado) | `timeline-event.tsx`, `estado-actual-block.tsx` | mitigate | guard `fechaPlausible` en `fechaValida` + omisión honesta; test con la fila real 2626 |
| T-117-05 | Information disclosure (accesibilidad) | aria-labels de `estado-actual-block.tsx` | mitigate | los dos aria-label migran a `diaCalendarioCitacion`; assert de paridad texto/aria en el test |
| T-117-06 | Tampering (fix global destructivo) | carril proyecto | accept | prohibido `where fecha <= current_date` global y `timeZone:"America/Santiago"` global; el guard es por render y por rango |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes |
</threat_model>

<verification>
- `pnpm --filter app test` verde; `pnpm -r exec tsc -b` exit 0.
- Guards de régimen verdes (anti-insinuación incluido, con `SUPERFICIES_FECHA` activa).
- `grep -rn "2626" app/components/` ⇒ solo comentarios/tests que documentan el caso.
- Flags NO tocados.
</verification>

<success_criteria>
- F-03, F-04, F-05 (parte proyecto), F-07 (parte timeline/stepper), F-09, F-13 y el residual de
  F-10 corregidos con evidencia antes/después.
- Cero `fechaCorta(` crudo sobrevive en las tres superficies del carril proyecto.
- Suite + typecheck + guards verdes.
</success_criteria>

<output>
Create `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-02-SUMMARY.md` when done,
con el antes/después verbatim por hallazgo (archivo:línea + commit) para `117-DISPOSICION.md`.
</output>
