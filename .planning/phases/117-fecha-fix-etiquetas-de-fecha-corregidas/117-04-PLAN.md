---
phase: 117-fecha-fix-etiquetas-de-fecha-corregidas
plan: 04
type: execute
wave: 3
depends_on: ["117-01", "117-02", "117-03"]
files_modified:
  - app/components/actualidad-module.tsx
  - app/components/actualidad-module.test.tsx
  - app/components/panel-actualidad.tsx
  - app/components/panel-actualidad.test.tsx
  - .planning/phases/113-inventario-de-superficies/113-INVENTARIO.md
  - .planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md
autonomous: true
requirements: [FECHA-02]

must_haves:
  truths:
    - "El strip de transparencia dice que la fecha mide nuestra consulta a la fuente, no un cambio del dato"
    - "El panel de la home muestra '10 ago 2026', no '2026-08-10'"
    - "Los 14 hallazgos F-01..F-14 tienen fila de disposición: corregido (con evidencia) o declarado (con causa)"
    - "El umbral de frescura documentado en el inventario rector dice 14 días"
  artifacts:
    - path: ".planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md"
      provides: "tabla F-01..F-14 → corregido/declarado + evidencia antes/después + commit"
      contains: "F-14"
      min_lines: 60
    - path: "app/components/panel-actualidad.tsx"
      provides: "rótulo legible es-CL en el panel de la home"
      contains: "badgeFechaCitacion"
  key_links:
    - from: "117-DISPOSICION.md"
      to: "116-FECHAS-AUDIT.md §3"
      via: "una fila por hallazgo, cero omisiones"
      pattern: "F-0[1-9]|F-1[0-4]"
---

<objective>
Cerrar los hallazgos de la home (**F-06**, **F-14**, resto de **F-05**), propagar la corrección
documental de **F-11**, y producir el artefacto rector de la fase: `117-DISPOSICION.md` con la tabla
completa F-01..F-14 → corregido o declarado, **cero excepciones silenciosas**.

Purpose: la fase se juzga por la disposición completa, no por el número de líneas cambiadas. Un
hallazgo declarado con causa es un resultado válido; uno silenciado, no.
Output: home corregida + tabla de disposición + verificación global verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-CONTEXT.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-VALIDATION.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-01-SUMMARY.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-02-SUMMARY.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F-06 + F-05 en el emisor huérfano actualidad-module</name>
  <files>app/components/actualidad-module.tsx, app/components/actualidad-module.test.tsx</files>
  <read_first>
    - `app/components/actualidad-module.tsx` — encabezado `:437`, render por fuente `:450-451`,
      query `:479-482` (`max(fecha_captura)` de seis tablas NO-PII), y los renders de hecho
      `:200-203` y `:318`. Los formatters `FECHA_CHILE` `:46`, `DOW_CHILE` `:53` y `fmt` `:95` son
      CORRECTOS (timestamp real, tz explícita) y NO se tocan.
    - `116-FECHAS-AUDIT.md` §3 **F-06** y **F-05**; §6 límite 5 (E-008 es HUÉRFANO: superseded por
      E-055; su eliminación es alcance de otra fase — se declara, no se borra).
    - `117-CONTEXT.md` §Deferred Ideas ("Eliminación de componentes huérfanos E-003/E-008 → fuera de
      alcance (declarar)").
  </read_first>
  <behavior>
    - El encabezado del strip de frescura ya NO dice `Última actualización de datos`; dice
      `Última consulta a las fuentes`.
    - Por fuente, el texto contiene `{fuente} · según fuente al {fecha}` (idiom LOCKED) y NO
      contiene la palabra `captura` pelada.
    - Con `it.fecha = "2023-11-17T00:14:41Z"` en un ítem de votación, el texto contiene
      `16 nov 2023` (día chileno real vía `fechaHechoCorta`).
    - Con `it.fecha = "2026-07-22T00:00:00Z"`, el texto contiene `22 jul 2026` (date-only
      disfrazada, sin corrimiento).
  </behavior>
  <action>
    (a) **F-06**: sustituir el encabezado `:437` por `Última consulta a las fuentes` y el render por
    fuente `:450-451` para que la fecha lleve el idiom LOCKED (`según fuente al {fechaCorta(it.fecha)}`).
    Añadir bajo el strip una línea aclaratoria de una frase: la fecha mide cuándo consultamos la
    fuente, no cuándo la fuente publicó el dato. Prohibido usar la palabra `captura` pelada (está en
    `TERMINOS_PROHIBIDOS` del linter).

    (b) **F-05**: `:202`, `:203` (`votacion.fecha`) y `:318` (`tramitacion_evento.fecha` vía
    `it.desde`) ⇒ `fechaHechoCorta`.

    (c) Añadir en el JSDoc de cabecera del archivo una nota de estado: el componente está HUÉRFANO
    (superseded por `panel-actualidad.tsx`, E-055); 117 corrige su copy porque el veredicto vale si
    alguien lo re-monta, y DECLARA que su eliminación es alcance de otra fase (117-CONTEXT
    §Deferred). No borrar el componente.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/actualidad-module.test.tsx lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Última actualización de datos" app/components/actualidad-module.tsx` = 0.
    - `grep -c "Última consulta a las fuentes" app/components/actualidad-module.tsx` = 1.
    - `grep -v '^\s*[*/]' app/components/actualidad-module.tsx | grep -c "fechaCorta(it.fecha)"`
      corresponde sólo al strip de frescura (los renders de hecho usan `fechaHechoCorta`).
    - El guard anti-insinuación sigue verde (la palabra `captura` no aparece en copy renderizado).
    - Los asserts de `<behavior>` pasan.
  </acceptance_criteria>
  <done>El único strip que se presenta como de transparencia deja de afirmar que el dato cambió.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F-14 — el panel de la home rinde la fecha en es-CL</name>
  <files>app/components/panel-actualidad.tsx, app/components/panel-actualidad.test.tsx</files>
  <read_first>
    - `app/components/panel-actualidad.tsx` — `rotuloFecha` `:100-108` (ruteo por tipo, documentado
      en `:82-85` y `:95-98`), render en `:166` y `:187`, `fechaValida` `:88`.
    - `116-FECHAS-AUDIT.md` §3 **F-14** (fix sugerido: pasar la salida de `diaCalendarioCitacion`
      por `badgeFechaCitacion`) y §1.3 última fila (**matiz de PROD**: TODAS las señales tienen
      `fecha_max` a medianoche UTC, no sólo las `agenda_*` ⇒ la rama `fechaCorta` está formateando
      date-only de facto — riesgo latente ya cubierto por el `timeZone:"UTC"` del Plan 01).
    - `app/lib/dia-calendario.ts` COMPLETO.
  </read_first>
  <behavior>
    - Con una señal `agenda_*` y `fecha_max = "2026-08-10T00:00:00Z"`, el texto contiene `10-ago` y
      NO contiene `2026-08-10`.
    - Con una señal de otro tipo y `fecha_max = "2026-08-10T00:00:00Z"`, el día sigue siendo el 10
      (no se corre al 9).
    - Una señal con `fecha_max` NULL (`agrupacion_materia`) sigue omitiendo el rótulo — la ausencia
      honesta no se rellena.
  </behavior>
  <action>
    En `rotuloFecha`, sustituir el `return diaCalendarioCitacion(iso)` de la rama `TIPOS_AGENDA` por
    `return badgeFechaCitacion(iso)` (importar de `@/lib/dia-calendario`). El ruteo por tipo NO se
    toca: el audit lo declara correcto y documentado. Conservar el `null` como omisión honesta.

    Añadir al comentario `:95-98` el matiz que PROD aportó: todas las señales llegan a medianoche
    UTC, así que la rama `fechaCorta` formatea date-only de facto — y por eso el `timeZone:"UTC"`
    explícito del Plan 01 es lo que la mantiene correcta (no el huso del entorno).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/panel-actualidad.test.tsx lib/dia-calendario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "badgeFechaCitacion" app/components/panel-actualidad.tsx` ≥ 1.
    - `pnpm vitest run components/panel-actualidad.test.tsx` exit 0 con los 3 asserts de `<behavior>`.
    - Cero regresión de ruteo: los tests existentes de `rotuloFecha` por tipo siguen pasando.
  </acceptance_criteria>
  <done>La superficie de mayor tráfico deja de mostrar un ISO técnico a público general y prensa.</done>
</task>

<task type="auto">
  <name>Task 3: 117-DISPOSICION.md + F-11 documental + verificación global de la fase</name>
  <files>.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md, .planning/phases/113-inventario-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - `116-FECHAS-AUDIT.md` §3 COMPLETO (los 14 hallazgos) + §3.1 (índice) + §6 (los 8 límites).
    - Los tres SUMMARY de esta fase: `117-01-SUMMARY.md`, `117-02-SUMMARY.md`, `117-03-SUMMARY.md`,
      y el propio trabajo de las Tasks 1-2 de este plan.
    - `113-INVENTARIO.md` §3.1.1 (donde se propagó el error de las 48 h — **F-11**).
    - `117-CONTEXT.md` §Deferred Ideas (lo que se DECLARA, no se corrige).
  </read_first>
  <action>
    (a) **F-11 (propagación)**: localizar con grep `48h`/`48 h` en `113-INVENTARIO.md` §3.1.1 y
    corregirlo a **14 días**, citando el código real (`app/lib/format.ts` `STALE_THRESHOLD_MS`) y la
    razón (cadence de ingesta semanal). El comportamiento no cambia.

    (b) Crear `117-DISPOSICION.md` con front-matter (`phase: 117`, `requirement: FECHA-02`,
    `consumido_por: [125]`, `fecha: 2026-07-28`) y estas secciones:

    **§1 Tabla de disposición** — una fila por hallazgo, LAS 14, columnas:
    `id | severidad | disposición (corregido/declarado) | archivo:línea | ANTES (verbatim) | DESPUÉS (verbatim) | commit`.
    Los verbatim salen de los SUMMARY, no se re-inventan. Ningún `F-xx` puede faltar y ninguna celda
    puede quedar vacía.

    **§2 Declarados con causa** — para cada hallazgo o parte de hallazgo NO corregido en 117, la
    causa y a dónde va. Como mínimo, y sólo si el trabajo real lo confirma:
      - F-04 (parte datos): las 2 filas del boletín `18232-25` con `2626-05-25` NO se sanean en DB;
        117 filtra el render. Deuda de ingesta (audit §6 límite 7).
      - F-05 (parte fondo): la columna mezcla dos semánticas (45.618 date-only-disfrazadas vs 7.603
        con hora real); 117 mitiga en el render con `fechaHechoCorta`; la normalización en ingesta
        queda abierta (audit §6 límite 6).
      - F-06 / E-003 / E-008: los componentes huérfanos se corrigen de copy pero NO se eliminan
        (117-CONTEXT §Deferred).
      - F-08: corregido en código con MONEY OFF; su verificación contra dato real queda pendiente
        del flip (audit §6 límite 1: `contrato` y `aporte` con 0 filas en PROD).
      - F-11: umbral REAL de 14 días conservado; cambiarlo es decisión de producto, no de esta fase.

    **§3 Verificación de cierre** — pegar la salida (o el resumen exit-code) de:
      `pnpm test`
      `pnpm -r exec tsc -b`
      `grep -rn "Actualizado" app/components app/app --include=*.tsx | grep -v "\.test\." | grep -v "^\s*[*/]"` ⇒ vacío
      `grep -rniE "(^|[^a-záéíóúñ])captura([^a-záéíóúñ]|$)" app/components app/app --include=*.tsx | grep -v "\.test\."` ⇒ sólo comentarios
      `grep -rn "corte al" app/components --include=*.tsx | grep -v "\.test\."` ⇒ vacío
      `grep -rn "según fuente al" app/components --include=*.tsx` ⇒ ≥ 1 (el chokepoint)
      `git diff --name-only <base>..HEAD` ⇒ sin `.env`, `.env.example` ni gates

    **§4 Trazabilidad a los Success Criteria del ROADMAP** — SC1..SC4 de la Phase 117, cada uno con
    su evidencia.

    **§5 Límites de esta fase** — el deploy NO viaja en 117 (va con Phase 125); la verificación
    end-to-end sobre HTML renderizado la hace 125.

    (c) Auto-check antes de cerrar: contar las filas de §1 y exigir exactamente 14 ids distintos
    (`F-01`..`F-14`). Si falta uno, el plan NO está terminado.
  </action>
  <verify>
    <automated>cd app && pnpm test && pnpm -r exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE "F-(0[1-9]|1[0-4])" .planning/phases/117-*/117-DISPOSICION.md | sort -u | wc -l` = 14.
    - Cada fila de §1 tiene disposición `corregido` o `declarado` — cero celdas vacías
      (`grep -c "| *|" ` sobre la tabla = 0).
    - `grep -ci "48 *h" .planning/phases/113-*/113-INVENTARIO.md` = 0 en §3.1.1; aparece "14 días".
    - `pnpm test` exit 0 y `pnpm -r exec tsc -b` exit 0, con las salidas registradas en §3.
    - Todos los guards de régimen verdes (anti-insinuación, lockdown, anti-flip MONEY, bento,
      name-match-rut, env-example, integ-scope, provider-guard).
    - `git diff --name-only` de la fase completa sin `.env`, `.env.example` ni archivos de gate.
  </acceptance_criteria>
  <done>Los 14 hallazgos tienen disposición explícita con evidencia, y la fase cierra con suite, typecheck y guards verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefacto de cierre → Phase 125 | 125 re-verifica sobre el deploy: una disposición incompleta se propaga como falso verde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-10 | Repudiation (excepción silenciosa) | `117-DISPOSICION.md` | mitigate | auto-check de 14 ids distintos + prohibición de celdas vacías en el acceptance criteria |
| T-117-11 | Information disclosure (doc que miente al mantenedor) | `113-INVENTARIO.md` §3.1.1 | mitigate | propagación de F-11 a 14 días con cita al código real |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes |
</threat_model>

<verification>
- `pnpm test` (app ~1428 + packages) verde.
- `pnpm -r exec tsc -b` exit 0.
- Los 8 guards de régimen verdes.
- `117-DISPOSICION.md` con 14 ids distintos y cero celdas vacías.
- Deploy NO ejecutado (viaja con Phase 125); flags NO tocados.
</verification>

<success_criteria>
- F-06, F-14, F-11 (propagación) y el resto de F-05 corregidos.
- `117-DISPOSICION.md` completo: F-01..F-14 con disposición, evidencia antes/después y commit.
- SC1..SC4 del ROADMAP trazados con evidencia.
</success_criteria>

<output>
Create `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-04-SUMMARY.md` when done.
</output>
