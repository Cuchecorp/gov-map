---
phase: 104-cierre-p3b-verificaci-n-e2e-todo-funciona
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/legal/102-LEGAL-DOSSIER-VSIM.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "La suite completa (app + packages) corre verde antes del deploy"
    - "Todos los guards de régimen/anti-flip/lockdown corren verdes con el vocabulario nuevo v10.0"
    - "El dossier VSIM queda signoff: approved con la autorización verbatim del operador (2026-07-26)"
  artifacts:
    - path: "docs/legal/102-LEGAL-DOSSIER-VSIM.md"
      provides: "Dossier VSIM firmado (pending->approved) con autorización del operador registrada"
      contains: "signoff: approved"
  key_links:
    - from: "app/lib/vsim-antiflip-guard.test.ts"
      to: ".env.example"
      via: "assert VSIM_PUBLIC_ENABLED=false"
      pattern: "VSIM_PUBLIC_ENABLED"
    - from: "docs/legal/102-LEGAL-DOSSIER-VSIM.md"
      to: "app/lib/vsim-gate.ts flip"
      via: "signoff: approved habilita el flip deploy-time (Plan 02)"
      pattern: "signoff: approved"
---

<objective>
Pre-deploy gate del cierre v10.0: probar que TODO el árbol está verde (suite completa + todos los guards con el vocabulario nuevo) ANTES de tocar el deploy, y firmar el dossier legal VSIM con la autorización verbatim del operador para que el flip VSIM ON de Plan 02 sea legítimo (el agente DOCUMENTA la autorización; el operador AUTORIZÓ el 2026-07-26).

Purpose: El deploy de Plan 02 arrastra 101+102+103 y enciende VSIM. Un deploy sobre una suite roja o un flip sin firma sería regresión. Este plan es el candado que precede al deploy.
Output: Suite + guards verdes (evidencia en el SUMMARY), dossier VSIM firmado approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/legal/102-LEGAL-DOSSIER-VSIM.md
@docs/legal/103-LEGAL-DOSSIER-NOTIF.md

<interfaces>
<!-- El dossier NOTIF (103) es el molde de cómo registrar una autorización de operador -->
<!-- pre-autorizada en la corrida: front-matter signoff:approved + asesor + fecha_signoff -->
<!-- + observaciones con la cita verbatim. El agente NO inventa el sign-off; TRANSCRIBE la -->
<!-- autorización que el operador ya dio (mismo patrón que cerró Phase 103). -->

Front-matter actual de docs/legal/102-LEGAL-DOSSIER-VSIM.md (a editar):
```yaml
signoff: pending          # pending | approved | rejected
asesor: ""
fecha_signoff: ""
observaciones: ""
```

Guards y suites que deben correr verdes (rutas exactas):
- app/lib/anti-insinuacion-guard.test.ts  (linter anti-insinuación — SUPERFICIES_PANEL, VSIM, relaciones)
- app/lib/vsim-antiflip-guard.test.ts      (anti-flip VSIM V1/V2/V3, .env.example=false)
- app/lib/notif-antiflip-guard.test.ts     (anti-flip NOTIF)
- app/lib/money-antiflip-guard.test.ts     (anti-flip MONEY)
- app/lib/lockdown-guard.test.ts           (lockdown authenticated Block D/E)
- app/lib/bento-guards.test.ts             (candados de régimen home/panel cero-hex/tipografía)
- app/lib/bento-coherencia-guard.test.ts
- app/lib/name-match-rut-guard.test.ts
- app/lib/env-example-guard.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr la suite completa + todos los guards y capturar evidencia</name>
  <read_first>
    - app/AGENTS.md (Next.js de este repo NO es el que conoces)
    - .planning/STATE.md (líneas Decisions 100/101/102/103 — vocabulario nuevo que los guards muerden)
    - .claude/skills/spike-findings-98/SKILL.md (vocabulario LOCKED del panel: "captura" pelado prohibido, cifra voto 283.550)
  </read_first>
  <action>
    Correr, desde el directorio `app/`, la suite completa de vitest y capturar el conteo total verde: `cd app && pnpm exec vitest run`. Luego correr `pnpm -w test` (o el equivalente root que ejecuta también los packages @obs/*) para cubrir packages. Correr `tsc` (typecheck) del workspace app: `cd app && pnpm exec tsc -b --noEmit` (o el comando cf-typecheck si existe). Correr EXPLÍCITAMENTE, uno por uno, los 9 guards listados en <interfaces> para dejar constancia individual de que cada candado del régimen v10.0 muerde con el vocabulario nuevo. Registrar en el SUMMARY: conteo de tests app, conteo packages, y el resultado (ok/fail) de CADA guard nombrado. Si ALGÚN test o guard falla: NO continuar al deploy — reportar el fallo exacto (archivo::test, mensaje) como blocker; los fixes emergentes de suite van AQUÍ, no en el deploy. Este plan NO enciende ningún flag ni toca .env.example (el anti-flip debe seguir verde con .env.example=false).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run lib/vsim-antiflip-guard.test.ts lib/notif-antiflip-guard.test.ts lib/money-antiflip-guard.test.ts lib/lockdown-guard.test.ts lib/anti-insinuacion-guard.test.ts lib/bento-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm exec vitest run` termina EXIT 0 con conteo de tests > 1400 (base 103 = 1401) y CERO fallos.
    - Los 9 guards nombrados corren cada uno verde (registrado individualmente en el SUMMARY).
    - `.env.example` NO fue modificado (VSIM_PUBLIC_ENABLED=false, NOTIF_PUBLIC_ENABLED=false, MONEY_PUBLIC_ENABLED=false intactos) — verificado con `git diff --exit-code .env.example` retorna 0.
    - tsc del app termina EXIT 0.
  </acceptance_criteria>
  <done>Suite completa + los 9 guards + tsc verdes; conteos registrados en el SUMMARY; .env.example intacto. Si algo falla, el fallo exacto queda documentado como blocker y el deploy NO procede.</done>
</task>

<task type="auto">
  <name>Task 2: Firmar el dossier VSIM (signoff pending->approved) con la autorización verbatim del operador</name>
  <read_first>
    - docs/legal/102-LEGAL-DOSSIER-VSIM.md (front-matter + §9 gate + §10 checklist)
    - docs/legal/103-LEGAL-DOSSIER-NOTIF.md (molde de cómo se registró la autorización pre-dada del operador en Phase 103)
  </read_first>
  <action>
    Editar SOLO el front-matter YAML de `docs/legal/102-LEGAL-DOSSIER-VSIM.md`, transcribiendo la autorización que el operador YA dio en esta corrida (2026-07-26, respuesta VERBATIM a la AskUserQuestion de plan-phase: "Sí — firmar y flip ON"). Setear: `signoff: approved`; `asesor: "Operador (autorización directa 2026-07-26)"`; `fecha_signoff: "2026-07-26"`; `observaciones: "Autorización verbatim del operador el 2026-07-26 en respuesta a AskUserQuestion de la corrida de cierre v10.0: 'Sí — firmar y flip ON'. El agente DOCUMENTA la autorización; el operador AUTORIZÓ. Flip VSIM_PUBLIC_ENABLED=true habilitado deploy-time (Worker env var, Plan 104-02); .env.example permanece false (anti-flip guard)."` NO tocar el cuerpo del dossier (secciones 0-10 quedan byte-idénticas salvo el checklist §10 opcional). Marcar en §10 el bloque de checklist como revisado por el operador SOLO en prosa dentro de observaciones — NO reescribir las secciones técnicas. NO tocar `.env.example` ni `app/lib/vsim-gate.ts` (el flip es acto de deploy, Plan 02).
  </action>
  <verify>
    <automated>grep -c "^signoff: approved" "docs/legal/102-LEGAL-DOSSIER-VSIM.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^signoff:" docs/legal/102-LEGAL-DOSSIER-VSIM.md` devuelve `signoff: approved`.
    - `fecha_signoff: "2026-07-26"` y `asesor` no vacío presentes en el front-matter.
    - `observaciones` contiene la cita verbatim "Sí — firmar y flip ON".
    - `git diff docs/legal/102-LEGAL-DOSSIER-VSIM.md` NO muestra cambios fuera del front-matter YAML (cuerpo intacto).
    - `.env.example` sin cambios (`git diff --exit-code .env.example` retorna 0).
    - El anti-flip VSIM sigue verde: `cd app && pnpm exec vitest run lib/vsim-antiflip-guard.test.ts` EXIT 0 (firmar el dossier NO toca el flag, solo habilita el flip deploy-time).
  </acceptance_criteria>
  <done>Dossier VSIM firmado approved con la autorización del operador registrada verbatim; cuerpo intacto; .env.example intacto; anti-flip guard verde. Plan 02 puede flipear VSIM ON legítimamente.</done>
</task>

</tasks>

<verification>
- Suite completa (app > 1400 tests) + packages + tsc verdes, EXIT 0.
- Los 9 guards de régimen/anti-flip/lockdown corren individualmente verdes.
- docs/legal/102-LEGAL-DOSSIER-VSIM.md: signoff: approved con autorización verbatim.
- .env.example intacto (todos los flags OFF por commit; el flip es deploy-time).
</verification>

<success_criteria>
El árbol está verde y el dossier VSIM firmado — el deploy de Plan 02 procede sobre base sólida, con el flip VSIM legítimamente autorizado.
</success_criteria>

<output>
Create `.planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-01-SUMMARY.md` when done, registrando: conteo de suite app + packages, resultado individual de los 9 guards, y confirmación de la firma del dossier VSIM.
</output>
