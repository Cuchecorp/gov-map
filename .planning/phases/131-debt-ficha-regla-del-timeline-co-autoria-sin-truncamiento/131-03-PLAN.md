---
phase: 131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento
plan: 03
type: execute
wave: 2
depends_on: [131-01, 131-02]
files_modified:
  - supabase/migrations/00NN_coautoria_v2.sql
  - supabase/tests/00NN_coautoria_v2.test.sql
  - .planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md
autonomous: true
requirements: [DEBT-03, DEBT-04]

must_haves:
  truths:
    - "La migración v2 está APLICADA en PROD y Postgres lo probó: el pgTAP corre verde contra el schema aplicado, no contra el archivo."
    - "El conteo que `/comparar` mostraría para D1178 × D1099 es idéntico al recálculo SQL independiente sobre PROD (92)."
    - "La RPC vieja sigue viva y funcional tras la aplicación (devuelve sus 20 filas), y la ficha de parlamentario no cambió de comportamiento."
    - "El número de `Hito del` congelado por 131-01 sigue reproduciéndose contra PROD tras el merge."
    - "La evidencia de aplicación queda escrita con los comandos, salidas y fecha — auto-ratificación verificable, no declarativa."
  artifacts:
    - path: ".planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md"
      provides: "Evidencia de aplicación: número de migración final, salida pgTAP, recálculos, controles apareados"
      min_lines: 40
  key_links:
    - from: "supabase/migrations/00NN_coautoria_v2.sql"
      to: "PROD Postgres"
      via: "psql --db-url explícito (jamás supabase db push)"
      pattern: "coautores_de_parlamentario_v2"
    - from: "pg_proc de PROD"
      to: "el pgTAP"
      via: "psql -tA -f supabase/tests/00NN_coautoria_v2.test.sql"
      pattern: "ok [0-9]+"
---

<objective>
Aplicar la migración de la RPC v2 a PROD y PROBAR que Postgres la ejecutó, cerrando DEBT-04 con
evidencia; re-verificar contra PROD el número del timeline congelado por 131-01 tras el merge.

Purpose: `tsc`/build/vitest NO prueban que Postgres ejecutó el DDL (Pitfall 5 del research /
Pitfall 6 de `0060`). La ÚNICA prueba es el pgTAP contra el schema aplicado. Y el conteo que la UI
muestra sólo es honesto si coincide con un recálculo SQL independiente.

Output: migración aplicada, pgTAP verde, evidencia escrita, auto-ratificación con salidas reales
(precedente 127-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-CONTEXT.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-RESEARCH.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-01-SUMMARY.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-02-SUMMARY.md

<interfaces>
Régimen de aplicación (LOCKED en el proyecto):
```
set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8
psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 --single-transaction -f supabase/migrations/00NN_coautoria_v2.sql
psql "$SUPABASE_DB_URL" -tA -f supabase/tests/00NN_coautoria_v2.test.sql | tr -d '\r'
```
- `supabase db push` PROHIBIDO (deriva del ledger `schema_migrations`).
- Toda lectura de verificación por `psql -tA` + `tr -d '\r'` (`psql` emite CRLF en Windows;
  `sort -c` no protege). Conteo por REST PROHIBIDO (PostgREST capa a 1.000 filas).
- `pipefail` + `grep -q` ⇒ exit 141 (SIGPIPE) enmascarado como fallo: no encadenar `grep -q` tras
  un productor largo.
- Testigo de la fase: `D1178` (Héctor Ulloa Aguilera) × `D1099` (Jaime Araya Guerrero) = **92**
  boletines co-firmados; listas de 91 y 82 co-autores (ambas < 1.000 ⇒ COMPLETAS con la v2).
- Máximo real de coautores en PROD = 101 sobre 180 parlamentarios (dominio 100%).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver numeración, aplicar la migración a PROD y correr el pgTAP</name>
  <files>supabase/migrations/00NN_coautoria_v2.sql, supabase/tests/00NN_coautoria_v2.test.sql</files>
  <read_first>
    - `131-02-SUMMARY.md` — el número `00NN` elegido al escribir la migración.
  </read_first>
  <action>
1. **Resolver la colisión de numeración (D-08, Pitfall 7).** Estar en `master` con las ramas de
   131 (y de 130, si mergeó) ya integradas. Ejecutar `ls supabase/migrations | sort`. Si el número
   de la migración de co-autoría colisiona con otra (típicamente la de la Phase 130) o deja un
   hueco respecto del último aplicado, RENOMBRAR el par de archivos (`supabase/migrations/*.sql` y
   `supabase/tests/*.test.sql`) con `git mv` al siguiente número libre ANTES de aplicar, y ajustar
   toda referencia al número dentro de esos archivos y en el comentario del allowlist. Las
   migraciones se aplican SIEMPRE desde master, jamás desde un worktree.

2. **Control previo (para que el después signifique algo):** registrar el estado ANTES —
   `select count(*) from pg_proc p join pg_namespace n on n.oid=p.pronamespace
    where n.nspname='public' and p.proname like 'coautores_de_parlamentario%'`
   debe dar **1** antes de aplicar y **2** después. Un antes de 2 significa que ya estaba aplicada:
   detener y reportar en vez de re-aplicar a ciegas.

3. **Aplicar** con `psql -v ON_ERROR_STOP=1 --single-transaction` (jamás `supabase db push`).

4. **Correr el pgTAP** contra el schema APLICADO y capturar la salida íntegra. Todos los `ok`,
   cero `not ok`. Si algún assert falla, NO editar la migración ya aplicada: registrar y escalar
   (las migraciones aplicadas jamás se editan — precedente `0073`/`0075`).
  </action>
  <verify>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && psql "$SUPABASE_DB_URL" -tA -c "select count(*) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname like 'coautores_de_parlamentario%'" | tr -d '\r' | grep -qx 2 && echo "OK: viva + v2 conviven"</automated>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && psql "$SUPABASE_DB_URL" -tA -f supabase/tests/*_coautoria_v2.test.sql | tr -d '\r' > /tmp/pgtap-131.txt; grep -c '^not ok' /tmp/pgtap-131.txt | grep -qx 0 && test $(grep -c '^ok' /tmp/pgtap-131.txt) -ge 8 && echo "OK pgTAP verde con >=8 asserts"</automated>
    <automated>set -a && . ./.env && set +a && psql "$SUPABASE_DB_URL" -tA -c "select has_function_privilege('anon','public.coautores_de_parlamentario_v2(text)','execute')" | tr -d '\r' | grep -qx f && echo "OK: anon sin execute"</automated>
  </verify>
  <done>
La v2 existe en PROD junto a la viva (2 funciones), el pgTAP corre verde contra el schema aplicado
con al menos 8 asserts y cero `not ok`, y `anon` no puede ejecutarla.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificación del testigo por recálculo independiente + controles apareados</name>
  <files>.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md</files>
  <read_first>
    - `131-01-SUMMARY.md` — el número de `Hito del` congelado y el desglose de períodos.
  </read_first>
  <action>
Ejecutar contra PROD (todo por `psql -tA` + `tr -d '\r'`) y capturar salidas VERBATIM:

**A — El testigo, por recálculo INDEPENDIENTE de la RPC** (no llamar la RPC: contar directo sobre
`proyecto_autor` los boletines co-firmados por `D1178` y `D1099` con `estado_vinculo='confirmado'`).
Debe dar **92**. Este es el oráculo: el número que la UI muestra tiene que igualarlo.

**B — El mismo testigo, ahora POR la RPC v2**: `select n_proyectos from
public.coautores_de_parlamentario_v2('D1178') where id='D1099'` ⇒ **92**, y la dirección simétrica
`…_v2('D1099') where id='D1178'` ⇒ **92**. A == B es el criterio de D-06 (conteo mostrado ==
recálculo SQL de PROD).

**C — Control apareado del defecto (cero fuerte, no vacuo):** la RPC VIEJA sobre el mismo par
`select count(*) from public.coautores_de_parlamentario('D1178') where id='D1099'` ⇒ **0**
(D1099 cae fuera del top-20 alfabético) mientras la v2 da 1 fila. La misma consulta prueba, en un
solo par de comandos, que el defecto existía y que la v2 lo cierra.

**D — La vieja sigue funcional (SC#4):** `select count(*) from
public.coautores_de_parlamentario('D1178')` ⇒ exactamente **20**;
`select count(*) from public.coautores_de_parlamentario_v2('D1178')` ⇒ **91** (o el valor real
medido hoy — anotar el que salga; lo que se asserta es `> 20` y `< 1000`).

**E — Techo no rozado:** máximo de filas de la v2 sobre los 180 parlamentarios con autoría
confirmada < 1.000 (re-medir el máximo: el research midió 101 el 2026-07-30). Declarar
explícitamente que, con el techo derivado del máximo, esta aserción es tautológica hoy y su valor
es cazar deriva futura (honestidad estándar de `0079`).

**F — H-06 tras el merge:** re-ejecutar `supabase/queries/timeline-regla-de-seleccion.sql` sobre
`14309-04` y confirmar que reproduce el número congelado en
`app/components/__fixtures__/timeline-14309-04.esperado.json` (identidad `N − K = H`). Si difiere,
NO re-hornear el número en silencio: reportar (significaría que llegaron eventos nuevos al boletín
o que el orden no quedó total).

Escribir `131-EVIDENCIA-APLICACION.md` con: número final de la migración, fecha/hora, comando de
aplicación, salida íntegra del pgTAP, las seis mediciones A-F con su comando y su salida cruda, y
una sección "Qué NO prueba esta evidencia" (no toca el DOM del deploy — la paridad DOM del timeline
y el render real de `/comparar` se verifican en la Phase 138 sobre el deploy).
  </action>
  <verify>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && A=$(psql "$SUPABASE_DB_URL" -tA -c "select count(distinct a1.boletin) from public.proyecto_autor a1 join public.proyecto_autor a2 on a2.boletin=a1.boletin and a2.estado_vinculo='confirmado' and a2.parlamentario_id='D1099' where a1.parlamentario_id='D1178' and a1.estado_vinculo='confirmado'" | tr -d '\r'); B=$(psql "$SUPABASE_DB_URL" -tA -c "select n_proyectos from public.coautores_de_parlamentario_v2('D1178') where id='D1099'" | tr -d '\r'); echo "A=$A B=$B"; test "$A" = "$B" && test "$A" -gt 0 && echo "OK: conteo mostrado == recalculo"</automated>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && OLD=$(psql "$SUPABASE_DB_URL" -tA -c "select count(*) from public.coautores_de_parlamentario('D1178') where id='D1099'" | tr -d '\r'); NEW=$(psql "$SUPABASE_DB_URL" -tA -c "select count(*) from public.coautores_de_parlamentario_v2('D1178') where id='D1099'" | tr -d '\r'); echo "vieja=$OLD v2=$NEW"; test "$OLD" = "0" && test "$NEW" = "1" && echo "OK: control apareado (el defecto existia y la v2 lo cierra)"</automated>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && V=$(psql "$SUPABASE_DB_URL" -tA -c "select count(*) from public.coautores_de_parlamentario('D1178')" | tr -d '\r'); N=$(psql "$SUPABASE_DB_URL" -tA -c "select count(*) from public.coautores_de_parlamentario_v2('D1178')" | tr -d '\r'); echo "vieja=$V v2=$N"; test "$V" = "20" && test "$N" -gt 20 && test "$N" -lt 1000 && echo "OK: vieja funcional en 20, v2 completa bajo techo"</automated>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && psql "$SUPABASE_DB_URL" -tA -F'|' -v boletin=14309-04 -f supabase/queries/timeline-regla-de-seleccion.sql | tr -d '\r' | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const[a,b,p,h]=s.trim().split('|').map(Number);const e=require('./app/components/__fixtures__/timeline-14309-04.esperado.json');if(a!==e.eventos_totales||b!==e.eventos_absorbidos||h!==e.hitos_del)throw new Error('H-06 DERIVO: PROD='+s.trim()+' esperado='+JSON.stringify(e));console.log('OK H-06 reproduce el numero congelado')})"</automated>
    <automated>test $(grep -c . .planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md) -ge 40 && grep -q "Qué NO prueba" .planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md && echo OK</automated>
  </verify>
  <done>
El conteo del testigo por la RPC v2 iguala el recálculo SQL independiente (92); el control apareado
demuestra que la vieja daba 0 para el mismo par; la vieja sigue funcional en 20; el número de H-06
se reproduce contra PROD; la evidencia está escrita con salidas crudas y su sección de límites.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate de fase — suite completa, guards de régimen y auto-ratificación</name>
  <files>.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md</files>
  <read_first>
    - `package.json:13-14` — `test` y `guards` (17 guards por NOMBRE explícito).
  </read_first>
  <action>
Correr el gate completo de la fase (D-09) y anexar sus salidas a `131-EVIDENCIA-APLICACION.md`:
`pnpm test` (raíz: `packages/*` + `app/`), `pnpm guards`, `pnpm --filter ./app exec tsc --noEmit`.

Jamás invocar vitest por glob (`vitest run lib/*guard*.test.ts` sale 0 SIN CORRER NADA): usar los
scripts, que listan por nombre.

Auto-ratificación (precedente 127-03): escribir al final de la evidencia un bloque de ratificación
que enumere los cuatro success criteria del ROADMAP §Phase 131 y, para CADA uno, el comando y la
salida concreta que lo sostiene — cero afirmación sin salida pegada. Si algún criterio quedara
sostenido sólo por un unit test con mocks (caso del render de `/comparar`), decirlo explícitamente
y declarar su verificación DOM delegada a la Phase 138.

Actualizar `.planning/ROADMAP.md` §Phase 131 marcando los planes completados, y `REQUIREMENTS.md`
marcando `DEBT-03` y `DEBT-04` como cerrados con puntero a la evidencia.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm guards</automated>
    <automated>pnpm --filter ./app exec tsc --noEmit</automated>
    <automated>grep -c "Success Criteri\|SC#" .planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-EVIDENCIA-APLICACION.md | grep -qv '^0$' && echo "OK ratificacion presente"</automated>
  </verify>
  <done>
`pnpm test`, `pnpm guards` y `tsc --noEmit` verdes; la evidencia contiene la ratificación de los 4
criterios con salidas reales; ROADMAP y REQUIREMENTS actualizados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agente → PROD Postgres (DDL) | Única operación de escritura de la fase: `create function` + `revoke`. |
| Migración en disco → ledger `schema_migrations` | Un número mal resuelto produce deriva permanente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-11 | Tampering (deriva del ledger) | numeración de la migración | mitigate | `ls supabase/migrations \| sort` en master ANTES de aplicar; `git mv` si colisiona; aplicar sólo desde master, nunca desde worktree; `psql --db-url` explícito, jamás `supabase db push`. |
| T-131-12 | Tampering (aplicación parcial) | DDL en PROD | mitigate | `-v ON_ERROR_STOP=1 --single-transaction`: o entra todo o no entra nada. |
| T-131-13 | Repudiation (falso verde de aplicación) | "la migración está aplicada" | mitigate | La ÚNICA prueba es el pgTAP contra el schema aplicado + el control `count(pg_proc) 1→2`; `tsc`/build no prueban DDL. |
| T-131-14 | Elevation of Privilege | privilegios de la v2 en PROD | mitigate | Verificación post-apply de `has_function_privilege('anon',…,'execute') = false` (el doble-revoke se comprueba, no se asume). |
| T-131-15 | Denial of Service | recálculos contra PROD | accept | Lecturas puntuales sobre un par y sobre 180 filas, con `statement_timeout` de sesión; sin barridos masivos. |
| T-131-16 | Information Disclosure | evidencia escrita | mitigate | La evidencia contiene IDs públicos de parlamentarios y conteos; PROHIBIDO pegar `SUPABASE_DB_URL` o cualquier credencial en el `.md`. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes; control = diff vacío en `package.json`/`pnpm-lock.yaml`. |
</threat_model>

<verification>
1. `pg_proc` con `proname like 'coautores_de_parlamentario%'` = 2 en PROD.
2. pgTAP verde (>=8 `ok`, cero `not ok`) contra el schema aplicado.
3. Recálculo independiente == conteo por la v2 == 92 para `D1178 × D1099`.
4. Control apareado: la RPC vieja da 0 filas para ese par y sigue devolviendo 20 en total.
5. La regla del timeline reproduce contra PROD el número congelado por 131-01.
6. `pnpm test` + `pnpm guards` + `tsc --noEmit` verdes.
7. Evidencia escrita con salidas crudas, sección de límites y ratificación de los 4 criterios.
</verification>

<success_criteria>
- DEBT-04 cerrado con prueba de que Postgres ejecutó el DDL, no con la existencia del archivo.
- DEBT-03 sigue reproduciéndose tras el merge (no derivó al integrar).
- La RPC vieja intacta y funcional: cero regresión en `/parlamentario/[id]`.
- Auto-ratificación con evidencia verificable; verificación DOM declarada como delegada a 138.
</success_criteria>

<output>
Create `.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-03-SUMMARY.md` when done.
</output>
