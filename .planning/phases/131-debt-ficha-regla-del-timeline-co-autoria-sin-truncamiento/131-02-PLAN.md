---
phase: 131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00NN_coautoria_v2.sql
  - supabase/tests/00NN_coautoria_v2.test.sql
  - app/lib/lockdown-guard.test.ts
  - app/app/comparar/page.tsx
  - app/app/comparar/page.test.tsx
autonomous: true
requirements: [DEBT-04]

must_haves:
  truths:
    - "Existe `coautores_de_parlamentario_v2(text)` como firma PARALELA: la viva de 0064 queda byte-idéntica (jamás se altera un `returns table` — 42P13 re-arma default privileges)."
    - "La v2 lleva la aguja completa: secdef, `search_path=''`, `statement_timeout`, LIMIT explícito con techo derivado de medición, doble-revoke, entrada en PUBLIC_RPC_ALLOWLIST, pgTAP contra el schema aplicado."
    - "`/comparar` consume la v2 en el eje de co-autoría y SOLO en ese eje: el eje de militancia histórica sigue leyendo con cap 20 (su RPC 0067 no cambió)."
    - "El copy de indeterminación del eje de co-autoría cita el cap del canal de co-autoría, no el de militancia."
    - "El par testigo D1178 × D1099 deja de declararse indeterminado y pasa a declarar el hecho: comparten 92 proyectos co-firmados."
  artifacts:
    - path: "supabase/migrations/00NN_coautoria_v2.sql"
      provides: "RPC v2 paralela con limit 1000 y tabla de techo derivado"
      contains: "coautores_de_parlamentario_v2"
    - path: "supabase/tests/00NN_coautoria_v2.test.sql"
      provides: "pgTAP de la v2 contra el schema aplicado"
    - path: "app/app/comparar/page.tsx"
      provides: "CAP_RPC_COAUTORES separado + call-site de la v2 + copy con el cap correcto"
  key_links:
    - from: "app/app/comparar/page.tsx"
      to: "coautores_de_parlamentario_v2"
      via: "sb.rpc en getCoautores"
      pattern: "coautores_de_parlamentario_v2"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "PUBLIC_RPC_ALLOWLIST"
      via: "entrada nueva junto a la vieja (ambas conviven)"
      pattern: "coautores_de_parlamentario_v2"
    - from: "CAP_RPC_COAUTORES"
      to: "eje 3 (co-autoría) únicamente"
      via: "parámetro de listaCompleta/interseccionPar"
      pattern: "CAP_RPC_COAUTORES"
---

<objective>
Cerrar DEBT-04 (fila 3.3): la co-autoría de `/comparar` deja de truncar en silencio a 20 y emite
membresía de par COMPLETA, vía una RPC de firma v2 PARALELA (precedente `0060`).

Purpose: 153 de 180 parlamentarios con autoría confirmada (85%) superan el cap de 20 ⇒ el eje de
co-autoría declara *"no permiten determinar"* sobre pares que SÍ co-firman. El testigo `D1178`
(Héctor Ulloa Aguilera) × `D1099` (Jaime Araya Guerrero) co-firma **92 boletines** y hoy el sitio
declara indeterminación. El fix no corrige una mentira: corrige un SILENCIO (con `limit 20` el
defecto sólo puede producir `indeterminado`, nunca un `ausente` falso — `listaCompleta` es
fail-closed CR-01).

Output: migración v2 + pgTAP + allowlist + consumo en `/comparar` con constante de cap SEPARADA.
Este plan NO aplica la migración a PROD (eso es 131-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-CONTEXT.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-RESEARCH.md

@supabase/migrations/0064_bounded_rpc_statement_timeout.sql
@supabase/migrations/0079_limit_explicito_rpcs.sql
@supabase/migrations/0060_bio_partido_publico.sql
@app/app/comparar/page.tsx
@app/lib/lockdown-guard.test.ts

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del árbol. NO explorar el codebase. -->

DEFINICIÓN VIVA (`supabase/migrations/0064_bounded_rpc_statement_timeout.sql:259-291`) — el cuerpo
se copia VERBATIM a la v2 cambiando SÓLO el `limit`:
```sql
create or replace function public.coautores_de_parlamentario(p_id text)
returns table (id text, nombre text, camara text, n_proyectos int, total_n bigint)
language sql stable security definer
  set search_path = ''
  set statement_timeout = '5s'
as $$
  select p2.id,
         coalesce(nullif(trim(concat_ws(' ', p2.nombres, p2.apellido_paterno, p2.apellido_materno)), ''),
                  p2.nombre_normalizado) as nombre,
         p2.camara,
         count(distinct a2.boletin)::int as n_proyectos,
         count(*) over () as total_n
  from public.proyecto_autor a1
  join public.proyecto_autor a2
    on a2.boletin = a1.boletin
   and a2.estado_vinculo = 'confirmado'
   and a2.parlamentario_id is not null
   and a2.parlamentario_id <> p_id
  join public.parlamentario p2 on p2.id = a2.parlamentario_id
  where a1.parlamentario_id = p_id
    and a1.estado_vinculo = 'confirmado'
  group by p2.id, p2.nombres, p2.apellido_paterno, p2.apellido_materno,
           p2.nombre_normalizado, p2.camara
  order by nombre
  limit 20;                                  -- ← el cap del defecto (0064:286)
$$;
revoke all on function public.coautores_de_parlamentario(text) from public;
revoke all on function public.coautores_de_parlamentario(text) from anon, authenticated;
```

CONSUMIDORES (`app/app/comparar/page.tsx`):
```
L119-128  getCoautores()  → sb.rpc("coautores_de_parlamentario", { p_id: id })
L261      milPar   = interseccionPar(milA, b, milB, a)     ← EJE 2 militancia (0067, sigue limit 20)
L283      copy de indeterminación de militancia, cita ${CAP_RPC}
L401      coautPar = interseccionPar(coautA, b, coautB, a) ← EJE 3 co-autoría (el defecto)
L432      copy de indeterminación de co-autoría, cita ${CAP_RPC}
L578      const CAP_RPC = 20;               ← ⚠ COMPARTIDO por eje 2 y eje 3
L583      const CAP_RPC_COMISIONES = 50;
L586-590  listaCompleta(filas) → filas.length < CAP_RPC || total_n <= filas.length
L594-597  totalHonesto(filas)  → filas[0].total_n
L605-618  interseccionPar(listaA, idB, listaB, idA) → presente | ausente | indeterminado
                                                      (usa listaCompleta en L614)
```
`interseccionPar` y `listaCompleta` son COMPARTIDOS entre eje 2 y eje 3 ⇒ el cap debe entrar como
PARÁMETRO con default `CAP_RPC`, no como cambio de la constante global.

GUARD (`app/lib/lockdown-guard.test.ts`):
```
L195+     const PUBLIC_RPC_ALLOWLIST = new Set([...])  (orden alfabético; "coautores_de_parlamentario" en L203)
L621-634  (A2) toda entrada del allowlist DEBE existir como función definida en supabase/migrations/
L761+     todo `.rpc("x")` del árbol público DEBE estar en el allowlist
```

CALL-SITE QUE **NO** SE MIGRA (deferred por CONTEXT): `app/app/parlamentario/[id]/page.tsx:201`
(`crossLinkReader("coautores_de_parlamentario")`) — es el molde WR-01 legítimo (muestra 20 de M
declarando M honesto). La RPC vieja queda intacta y funcional (SC#4 del ROADMAP).

Medición de PROD que justifica el techo [VERIFIED psql, dominio 100% = 180 parlamentarios]:
máximo real de coautores = **101**; con >20 coautores = 153 (85,0%); con >1.000 = 0.
Criterio de `0079`: techo >= 4x el máximo medido, nunca menor a 1.000 ⇒ `4 × 101 = 404 < 1.000`
⇒ **techo = 1.000** (el piso), margen 9,9x.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración de la RPC v2 paralela + pgTAP</name>
  <files>supabase/migrations/00NN_coautoria_v2.sql, supabase/tests/00NN_coautoria_v2.test.sql</files>
  <read_first>
    - `supabase/migrations/0060_bio_partido_publico.sql:30-56` — PRECEDENTE de firma v2 paralela y
      el comentario que explica el `42P13`.
    - `supabase/migrations/0079_limit_explicito_rpcs.sql:1-60` — cabecera con la tabla
      `función | clase | máximo medido | techo | margen` + la declaración de lo que la aserción NO prueba.
    - `supabase/tests/0067_militancia_historica_compartida.test.sql` — molde de pgTAP del repo.
  </read_first>
  <action>
**Numeración (D-08):** ejecutar `ls supabase/migrations | sort | tail -3`. El último aplicado es
`0079`; `0080_actualidad_evidencia.sql` está reservada por la Phase 127. Tomar el SIGUIENTE número
libre observado en ese momento (si `0081` está libre → `0081`; si la Phase 130 ya lo reclamó →
`0082`). Nombre: `00NN_coautoria_v2.sql`. Registrar el número elegido en el SUMMARY; 131-03
re-verifica al aplicar y renombra si colisionó al mergear (las migraciones se aplican desde master,
jamás desde el worktree).

Crear `supabase/migrations/00NN_coautoria_v2.sql` con:

(a) Cabecera de comentario que declare: firma NUEVA **paralela** a `0064`; la viva NO se altera
    porque cambiar su `returns table` dispara `42P13` y re-arma los default privileges (superficie
    REST re-abierta); precedente `0060`. Declarar también que la vieja sigue en uso por
    `/parlamentario/[id]` (molde WR-01) y por eso NO se dropea.
(b) La tabla de techo derivado en el formato de `0079:29-42`:
    `función | clase | máximo medido | techo | margen`, con la consulta de medición usada, el
    dominio (180 parlamentarios = 100%, no muestra) y la fecha. Y la línea de honestidad que `0079`
    también lleva: con el techo derivado del máximo medido, `medido < techo` es **tautológico hoy**;
    el valor de la aserción es cazar DERIVA futura, no probar corrección presente.
(c) `drop function if exists public.coautores_de_parlamentario_v2(text);` seguido de
    `create or replace function public.coautores_de_parlamentario_v2(p_id text)` con la MISMA firma
    de columnas (`id text, nombre text, camara text, n_proyectos int, total_n bigint`),
    `language sql stable security definer`, `set search_path = ''`, `set statement_timeout = '5s'`,
    y el cuerpo copiado VERBATIM de `0064:263-287` con el único cambio `limit 20` → `limit 1000`.
    Cero drift semántico: el diff contra el cuerpo vivo debe ser exactamente esa línea.
(d) Doble-revoke: `revoke all on function public.coautores_de_parlamentario_v2(text) from public;`
    y `... from anon, authenticated;`.
(e) Cero `grant`. Cero `cron.schedule`. Cero DML (la función es `stable`, sólo lectura; la fase no
    escribe datos ni hace backfill).

Crear `supabase/tests/00NN_coautoria_v2.test.sql` (pgTAP, molde de `0067`), con al menos:
  1. `has_function('public','coautores_de_parlamentario_v2', ARRAY['text'])`.
  2. La v2 es `security definer` (`prosecdef`) y `provolatile='s'`.
  3. `proconfig` contiene `search_path=` y `statement_timeout=5s`.
  4. La firma de retorno (`proargnames`/`proallargtypes` vía `array_to_string`, idiom del repo)
     es IDÉNTICA a la de la viva — control apareado que prueba que la v2 no derivó de columnas.
  5. **La viva sigue intacta**: `has_function` de `coautores_de_parlamentario(text)` y su
     `prosrc` contiene `limit 20`; el `prosrc` de la v2 contiene `limit 1000`.
  6. Cero-grant efectivo: `has_function_privilege('anon', ..., 'execute')` es **false** para la v2,
     y también para `authenticated` y `public`.
  7. Control de contenido positivo: la v2 devuelve **> 20** filas para `D1178` (hoy la vieja
     devuelve exactamente 20) — el cero de la deuda deja de ser vacuo.
  8. La v2 no proyecta columnas PII (`rut`, `donante_id` ausentes del `prosrc`).
NO ejecutar la migración en este plan (131-03 la aplica y corre el pgTAP).
  </action>
  <verify>
    <automated>test -f supabase/migrations/*_coautoria_v2.sql && test -f supabase/tests/*_coautoria_v2.test.sql && echo OK</automated>
    <automated>grep -v '^--' supabase/migrations/*_coautoria_v2.sql | grep -c "limit 1000" | grep -qx 1 && grep -v '^--' supabase/migrations/*_coautoria_v2.sql | grep -c "^grant\|[^e]grant " | grep -qx 0 && echo "OK limit + cero grant"</automated>
    <automated>for t in "security definer" "set search_path = ''" "set statement_timeout" "revoke all"; do grep -v '^--' supabase/migrations/*_coautoria_v2.sql | grep -qF "$t" || { echo "FALTA: $t"; exit 1; }; done; echo "OK aguja completa"</automated>
    <automated>grep -v '^--' supabase/migrations/*_coautoria_v2.sql | grep -c "cron.schedule\|create view\|drop function if exists public.coautores_de_parlamentario(" | grep -qx 0 && echo "OK: sin cron, sin vista, sin tocar la viva"</automated>
    <automated>diff <(sed -n '/create or replace function public.coautores_de_parlamentario(p_id/,/^\$\$;/p' supabase/migrations/0064_bounded_rpc_statement_timeout.sql | sed 's/limit 20;/limit N;/; s/coautores_de_parlamentario(/FN(/') <(sed -n '/create or replace function public.coautores_de_parlamentario_v2(p_id/,/^\$\$;/p' supabase/migrations/*_coautoria_v2.sql | sed 's/limit 1000;/limit N;/; s/coautores_de_parlamentario_v2(/FN(/') && echo "OK: cuerpo verbatim, único diff = el limit"</automated>
  </verify>
  <done>
La migración existe con la aguja completa y el cuerpo verbatim de la viva salvo el `limit`; el
pgTAP existe con control positivo (>20 filas para D1178) y control de que la viva sigue en 20.
La viva no se dropea ni se altera.
  </done>
</task>

<task type="auto">
  <name>Task 2: Allowlist + consumo de la v2 en /comparar con CAP_RPC_COAUTORES separado</name>
  <files>app/lib/lockdown-guard.test.ts, app/app/comparar/page.tsx</files>
  <read_first>
    - `app/app/comparar/page.tsx:117-128, 255-290, 393-444, 556-618` — getCoautores, eje 2, eje 3,
      y el bloque de constantes/helpers compartidos.
    - `app/lib/lockdown-guard.test.ts:195-225` — el `PUBLIC_RPC_ALLOWLIST` (orden alfabético).
  </read_first>
  <action>
**⚠ TRAMPA #1 de la fase (Pitfall 1 del research, el bug más probable):** `CAP_RPC = 20` en
`page.tsx:578` es COMPARTIDO por el eje 2 (militancia histórica, RPC `0067`, que sigue con
`limit 20`) y el eje 3 (co-autoría). Subir `CAP_RPC` a 1000 haría que `listaCompleta` declarara
COMPLETAS listas de militancia truncadas a 20 → el eje 2 pasaría a afirmar **AUSENCIA FALSA con
atribución de fuente** = riesgo #1 del proyecto, introducido por el fix del riesgo #1.
`CAP_RPC` NO SE TOCA. Señal temprana: cualquier diff que toque `:578` o `:283`.

1. `app/lib/lockdown-guard.test.ts`: **añadir** `"coautores_de_parlamentario_v2"` al
   `PUBLIC_RPC_ALLOWLIST`, en su posición alfabética, inmediatamente tras
   `"coautores_de_parlamentario"` (que **se mantiene** — la vieja sigue viva y consumida por
   `/parlamentario/[id]`). Comentario breve citando la migración `00NN` (el assert A2 exige que la
   entrada exista como función definida en `supabase/migrations/`).

2. `app/app/comparar/page.tsx`:
   - Añadir junto a `CAP_RPC` una constante NUEVA y separada:
     `const CAP_RPC_COAUTORES = 1000;` con comentario que declare (i) es el techo de la RPC v2
     (`limit 1000` de `00NN`), (ii) máximo real medido 101 sobre el dominio completo ⇒ margen 9,9x,
     (iii) por qué NO se reutiliza `CAP_RPC` (rompería la disciplina del eje de militancia).
   - Parametrizar el cap SIN cambiar el comportamiento del eje 2:
     `function listaCompleta(filas: CrossLinkRow[], cap: number = CAP_RPC): boolean`
     (usar `cap` en la comparación `filas.length < cap`) y
     `function interseccionPar(listaA, idB, listaB, idA, cap: number = CAP_RPC): InterseccionPar`
     (pasando `cap` a ambas llamadas de `listaCompleta` en L614). Los defaults dejan el eje 2
     (L261) byte-equivalente en comportamiento.
   - `getCoautores` (L121): cambiar el nombre de la RPC a `"coautores_de_parlamentario_v2"` y
     actualizar el mensaje de error y el comentario de cabecera (L117-118: la referencia "0061"
     pasa a citar la migración v2 y el porqué del cambio de canal).
   - Eje 3 (L401): `interseccionPar(coautA, b, coautB, a, CAP_RPC_COAUTORES)`.
   - Copy de indeterminación del eje 3 (L432): sustituir `${CAP_RPC}` por `${CAP_RPC_COAUTORES}`.
     El copy del eje 2 (L283) NO se toca. Mantener el registro y el vocabulario actuales — el copy
     es hecho DECLARADO por fuente oficial, cero vocabulario de afinidad (D-07); el eje 3 ya usa
     "Comparten N proyectos co-firmados", que es el copy vivo y sólo cambia el valor de N.
   - Actualizar el comentario de bloque de L562-577 para que describa los TRES caps distintos
     (par-militancia 20, co-autoría 1000, comisiones 50) y por qué son distintos.
  </action>
  <verify>
    <automated>pnpm --filter ./app exec vitest run lib/lockdown-guard.test.ts lib/anti-insinuacion-guard.test.ts</automated>
    <automated>pnpm --filter ./app exec tsc --noEmit</automated>
    <automated>grep -n "const CAP_RPC = 20;" app/app/comparar/page.tsx | grep -qx "578:const CAP_RPC = 20;" && echo "OK: CAP_RPC intacto en su línea" || { echo "REVISAR: CAP_RPC movido o alterado"; grep -n "CAP_RPC = " app/app/comparar/page.tsx; }</automated>
    <automated>grep -c "coautores_de_parlamentario_v2" app/app/comparar/page.tsx | grep -qv '^0$' && grep -q "coautores_de_parlamentario\"" app/app/parlamentario/\[id\]/page.tsx && echo "OK: v2 en comparar, vieja intacta en ficha"</automated>
  </verify>
  <done>
`/comparar` invoca la v2 sólo en el eje de co-autoría; `CAP_RPC` sigue en 20 y el eje de militancia
mantiene su semántica; la v2 está en el allowlist junto a la vieja; guards y `tsc` verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testigo D1178 × D1099 en los tests de /comparar + control apareado del eje 2</name>
  <files>app/app/comparar/page.test.tsx</files>
  <read_first>
    - `app/app/comparar/page.test.tsx` — 586 líneas; los mocks de RPC viven en L77, 226, 305, 486.
      AÑADIR casos y ACTUALIZAR los mocks al nombre v2; jamás reescribir el archivo.
  </read_first>
  <behavior>
    - Test 1 (el testigo, hoy roto): mock de la v2 devolviendo las listas COMPLETAS de `D1178`
      (91 co-autores, incluyendo `D1099` con `n_proyectos: 92`) y de `D1099` (82 co-autores) ⇒ el
      render declara "Comparten 92" + "proyectos co-firmados", y NO contiene la frase de
      indeterminación del eje de co-autoría.
    - Test 2 (regresión del defecto): con listas truncadas a 20 que NO se contienen mutuamente y
      `total_n` > 20, el eje sigue declarando indeterminación citando **1000**, no 20 — prueba que
      el copy consume `CAP_RPC_COAUTORES`.
    - Test 3 (control apareado anti-Pitfall-1): con listas de MILITANCIA de exactamente 20 filas y
      `total_n` = 40 que no se contienen, el eje 2 sigue declarando **indeterminado** (NO
      "ausente"). Si alguien subiera `CAP_RPC` a 1000, este test se pone rojo — es el guard vivo
      contra la ausencia falsa.
    - Los mocks existentes que nombran `coautores_de_parlamentario` se actualizan al nombre v2 (el
      call-site cambió); ningún test debe seguir verde por mockear una RPC que ya no se invoca.
  </behavior>
  <action>
Actualizar en `app/app/comparar/page.test.tsx` los mocks de RPC (L77, 226, 305, 486) para que
respondan a `coautores_de_parlamentario_v2`. Verificar explícitamente que ningún mock quede
apuntando al nombre viejo por el eje 3 (un mock stale devolvería `[]` y el test pasaría por la
razón equivocada — falso verde).

Añadir un `describe` nuevo ("(3.3/DEBT-04) membresía de par completa — testigo D1178 × D1099") con
los tres tests del bloque `<behavior>`. Generar las listas largas programáticamente
(`Array.from({length: 91}, …)`) con `total_n` coherente con el largo, insertando la fila real del
par (`id: "D1099"`, `n_proyectos: 92`) en la lista de `D1178` y la simétrica en la de `D1099`.

Las aserciones de texto se hacen sobre el contenido renderizado con extracción numérica, jamás por
literal con dígitos pegados a markup (React intercala `<!-- -->` entre texto y `<span>`: un
`toContain("Comparten 92")` puede fallar aunque el DOM sea correcto). Aserción segura: buscar
"Comparten" y "92" como nodos/fragmentos separados, o normalizar quitando los comentarios de React
antes de comparar.
  </action>
  <verify>
    <automated>pnpm --filter ./app exec vitest run app/comparar/page.test.tsx</automated>
    <automated>grep -c '"coautores_de_parlamentario"' app/app/comparar/page.test.tsx | grep -qx 0 && echo "OK: cero mocks stale del nombre viejo"</automated>
    <automated>pnpm --filter ./app test</automated>
  </verify>
  <done>
El testigo `D1178 × D1099` renderiza "Comparten 92 proyectos co-firmados"; el eje 2 conserva su
indeterminación con cap 20 bajo control apareado; la suite completa de `app/` verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador → Worker (`/comparar?a=&b=`) | `a`/`b` son entrada no confiable; validados en el borde por `PARLAMENTARIO_ID_RE`. |
| Worker (`service_role`) → Postgres | Camino A: `service_role` bypassa RLS/ACL ⇒ el control REAL es `PUBLIC_RPC_ALLOWLIST` + doble-revoke, no la DB. |
| RPC secdef → tablas base | `security definer` con `search_path=''` ejecuta con privilegios del owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-05 | Tampering (SQL injection) | `coautores_de_parlamentario_v2(p_id text)` | mitigate | `language sql` con parámetro (jamás interpolación de string); todos los nombres schema-qualified; `PARLAMENTARIO_ID_RE` valida en el borde de `/comparar`. |
| T-131-06 | Elevation of Privilege | `security definer` de la v2 | mitigate | `set search_path = ''` + sólo tablas no-PII (`proyecto_autor`, `parlamentario`); doble-revoke `from public` y `from anon, authenticated`; pgTAP asserta `has_function_privilege('anon',…,'execute') = false`. |
| T-131-07 | Denial of Service | barrido de `proyecto_autor` × `proyecto_autor` | mitigate | `set statement_timeout = '5s'` + `limit 1000` (techo derivado de medición sobre dominio 100%, criterio `0079`). |
| T-131-08 | Information Disclosure (PII) | columnas proyectadas por la v2 | mitigate | Las MISMAS 5 columnas que la viva; pgTAP compara firmas; guard de chokepoint escanea `rut`/`donante_id`. |
| T-131-09 | Tampering (firma viva) | `coautores_de_parlamentario(text)` | mitigate | Firma v2 PARALELA: cero `alter`/`drop` sobre la viva (un cambio de `returns table` dispara `42P13` y re-arma default privileges = superficie REST re-abierta). Gate automatizado en Task 1. |
| T-131-10 | Repudiation / riesgo #1 del proyecto (ausencia falsa con atribución de fuente) | eje 2 militancia si `CAP_RPC` se contamina | mitigate | Constante `CAP_RPC_COAUTORES` separada + cap como parámetro con default; control apareado en `page.test.tsx` que se pone rojo si `CAP_RPC` sube. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes (`package.json`/`pnpm-lock.yaml` intactos). Slopcheck no corresponde; control = diff vacío en esos archivos. |
</threat_model>

<verification>
1. `pnpm --filter ./app test` — verde.
2. `pnpm guards` — verde (17 guards por NOMBRE, jamás glob).
3. `pnpm --filter ./app exec tsc --noEmit` — limpio.
4. Diff del cuerpo v2 vs `0064:263-287` normalizado = sólo la línea del `limit`.
5. `git diff --stat -- package.json pnpm-lock.yaml app/package.json` vacío.
6. `supabase/migrations/0064_*.sql` y `app/app/parlamentario/[id]/page.tsx` sin cambios en el diff.
</verification>

<success_criteria>
- La v2 existe en migraciones con la aguja completa; la viva intacta y aún consumida por la ficha.
- `/comparar` consume la v2 sólo en el eje 3; `CAP_RPC = 20` intacto para militancia.
- El testigo `D1178 × D1099` deja de declararse indeterminado en el render (probado por unit).
- Guards de régimen y suite `app/` verdes. La migración queda ESCRITA, no aplicada (131-03 aplica).
</success_criteria>

<output>
Create `.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-02-SUMMARY.md` when done.
Registrar en el SUMMARY el NÚMERO de migración elegido (`00NN`) para que 131-03 lo verifique al aplicar.
</output>
