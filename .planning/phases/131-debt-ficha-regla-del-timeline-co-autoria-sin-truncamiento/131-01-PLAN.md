---
phase: 131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/proyecto/[boletin]/page.tsx
  - supabase/queries/timeline-regla-de-seleccion.sql
  - app/components/__fixtures__/timeline-14309-04.json
  - app/components/__fixtures__/timeline-14309-04.esperado.json
  - app/components/timeline-view.test.tsx
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "La regla que decide qué evento de tramitación se vuelve una línea `Hito del` está escrita en un archivo SQL ejecutable con su criterio declarado (qué entra, qué se agrupa, por qué)."
    - "La regla explica la brecha del boletín testigo 14309-04 por conteo exacto y sin residuo: eventos_totales − eventos_absorbidos = hitos_del."
    - "El conteo de hitos es DETERMINISTA: la lectura de tramitacion_evento ordena por un orden total (fecha asc, id asc), no por fecha sola."
    - "Un test unitario ata el builder TS (construirItems) al número medido en PROD con la MISMA regla y el MISMO orden — si el builder deriva, el test se pone rojo."
  artifacts:
    - path: "supabase/queries/timeline-regla-de-seleccion.sql"
      provides: "Regla escrita + criterio declarado + query espejo ejecutable por psql"
      contains: "ORDEN TOTAL DECLARADO"
    - path: "app/components/__fixtures__/timeline-14309-04.json"
      provides: "Los 99 eventos reales del testigo, en orden (fecha asc, id asc)"
    - path: "app/components/__fixtures__/timeline-14309-04.esperado.json"
      provides: "SEDE ÚNICA del número congelado (eventos_totales/absorbidos/periodos/hitos_del)"
    - path: "app/components/timeline-view.test.tsx"
      provides: "Test de paridad builder↔regla + assert de orden total"
  key_links:
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "orden total (fecha, id)"
      via: "dos .order() encadenados en la lectura de tramitacion_evento"
      pattern: "\\.order\\(\"fecha\"[\\s\\S]{0,120}\\.order\\(\"id\""
    - from: "app/components/timeline-view.test.tsx"
      to: "app/components/__fixtures__/timeline-14309-04.esperado.json"
      via: "import del número congelado (jamás literal duplicado)"
      pattern: "timeline-14309-04\\.esperado"
---

<objective>
Cerrar DEBT-03 (H-06): la regla de selección del timeline deja de vivir implícita en TS y queda
gobernada por una query ESCRITA con criterio declarado, que explica por conteo exacto la brecha
entre los eventos del boletín y las líneas `Hito del` del render — y el número deja de depender del
orden físico que Postgres devuelva ese día.

Purpose: hoy el número de hitos de la ficha puede cambiar entre deploys/vacuums sin que cambie un
solo dato (defecto de determinismo medido en PROD: 14/12/12/16 eventos absorbidos según desempate).
Ese es el DEFECTO REAL que D-03 manda corregir por ser localizado (una línea de `.order`).

Output: `supabase/queries/timeline-regla-de-seleccion.sql` (regla + criterio), fixture de los 99
eventos reales, número congelado en UNA sede, orden total en la lectura, test de paridad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-CONTEXT.md
@.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-RESEARCH.md

@app/components/timeline-view.tsx
@app/app/proyecto/[boletin]/page.tsx

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del árbol. NO explorar el codebase. -->

De `app/components/timeline-view.tsx`:
- `export function construirItems(eventos: TramitacionEventoRow[]): TimelineItem[]` (L148-220) — LA REGLA.
- `export type TimelineItem = { kind: "evento"; evento: TramitacionEventoRow; key: string } | { kind: "periodo"; periodo: PeriodoUrgencia }`
- `export function paresDeUrgencia(eventos: TramitacionEventoRow[]): PeriodoUrgencia[]` (L226)
- Helpers internos (NO exportados, no tocar): `fechaValida` L64-69, `esEventoUrgencia` L76-81
  (`tipo==='urgencia'` OR (`tipo==='tramite'` AND `/urgencia/i` en `descripcion`)),
  `esRetiroUrgencia` L89-91 (`/retira/i`), `tipoUrgenciaKey` L96-103.
- El sort de `construirItems` (L157-164) es ESTABLE (ES2019) y sólo compara epoch de fecha ⇒
  **hereda el orden de entrada en los empates**. No corrige el no-determinismo: por eso el fix
  va en el `.order()` de la lectura.

De `app/app/proyecto/[boletin]/page.tsx` (`TramitacionSection`, L451-478):
```
L459-465:  sb.from("tramitacion_evento").select("*").eq("boletin", boletin)
             .order("fecha", { ascending: true })
L477:      const eventos = (data as TramitacionEventoRow[]) ?? [];
```
No existe RPC de eventos de tramitación: la página lee la tabla directa.

Columnas reales de `public.tramitacion_evento` [VERIFIED PROD]:
`id, boletin, fecha, camara, tipo, descripcion, enlace, origen, fecha_captura`.
`id` es la única columna garantizada única ⇒ es la clave de desempate.

Comandos del régimen:
```
set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8
psql "$SUPABASE_DB_URL" -tA -f <archivo.sql> | tr -d '\r'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la regla como SQL documental y MEDIR el número sobre el orden total</name>
  <files>supabase/queries/timeline-regla-de-seleccion.sql, app/components/__fixtures__/timeline-14309-04.json, app/components/__fixtures__/timeline-14309-04.esperado.json</files>
  <read_first>
    - `.planning/phases/131-.../131-RESEARCH.md` §"Code Examples" — la query gaps-and-islands YA
      validada contra PROD (da 14 absorbidos / 5 períodos con `order by fecha` sola).
    - `app/components/timeline-view.tsx:64-224` — los cuatro predicados que la query debe espejar.
  </read_first>
  <action>
Crear el directorio `supabase/queries/` (no existe) y dentro `timeline-regla-de-seleccion.sql`
con, en este orden:

(a) Cabecera de comentario con el CRITERIO DECLARADO (D-01), en español, cubriendo los cuatro puntos
    obligatorios: QUÉ ENTRA (todo evento de `tramitacion_evento` del boletín — NADA se excluye del
    render; la brecha es 100% agrupación, cero filtrado), QUÉ SE AGRUPA (runs CONTIGUOS, en el orden
    total declarado, de eventos de urgencia que NO son retiro y comparten `tipoUrgenciaKey`
    normalizada, con longitud >= 2, en una sola línea "Urgencia X: N eventos"), QUÉ SE EXCLUYE
    (nada), y POR QUÉ (la renovación repetitiva de urgencia enterraba la señal estructural; un
    RETIRO no es una renovación → corta el run).
(b) La línea `ORDEN TOTAL DECLARADO: (fecha asc, id asc)` con su justificación explícita: sin
    desempate el resultado NO es determinista — medido en PROD 14/12/12/16 eventos absorbidos según
    el criterio de desempate. Citar el espejo en `app/app/proyecto/[boletin]/page.tsx` (Task 2).
(c) Nota de fecha: `tramitacion_evento.fecha` es `timestamptz` **date-only disfrazado** (medianoche
    UTC = día chileno). PROHIBIDO `at time zone 'America/Santiago'` en esta query — correría el día.
    La regla opera sobre ORDEN, no sobre calendario: no necesita la parte día.
(d) Puntero a la SEDE ÚNICA del número: `app/components/__fixtures__/timeline-14309-04.esperado.json`.
(e) La query espejo, copiada de RESEARCH §Code Examples, con UN cambio: el `row_number() over` usa
    `order by fecha asc, id asc`. Debe emitir cuatro columnas:
    `eventos_totales, eventos_absorbidos, periodos, hitos_del`.
    Parametrizada por boletín vía `:'boletin'` (variable psql) — jamás interpolación de string.

Ejecutarla contra PROD para el testigo `14309-04` con el patrón del régimen
(`psql "$SUPABASE_DB_URL" -tA -F'|' -v boletin=14309-04 -f ... | tr -d '\r'`) y ANOTAR el resultado.
NO asumir 85 ni 87: el research midió `id desc` (12 absorbidos), NO `id asc`. El número que salga
de ESTA corrida es el que se congela.

Volcar además, en la MISMA sesión psql y con el MISMO `order by fecha asc, id asc`, los 99 eventos
reales a `app/components/__fixtures__/timeline-14309-04.json` como array JSON
(`select coalesce(json_agg(t order by fecha asc, id asc), '[]'::json) from ... t where boletin=...`),
con las 9 columnas reales. El array debe quedar EN ESE ORDEN en el archivo (es lo que la página
entregará a `construirItems`).

Escribir `app/components/__fixtures__/timeline-14309-04.esperado.json` — SEDE ÚNICA del número:
`{"boletin":"14309-04","orden":"fecha asc, id asc","medido_en":"<fecha ISO>",
  "eventos_totales":N,"eventos_absorbidos":K,"periodos":P,"hitos_del":H}`
con los valores REALES de la corrida. Verificar la identidad `N - K == H` antes de escribir.

PROHIBIDO escribir el número literal en cualquier otro archivo (Pitfall 2 del research: si `85`
o el número nuevo aparece literal en más de un archivo, es un falso verde esperando).
  </action>
  <verify>
    <automated>set -a && . ./.env && set +a && export PGCLIENTENCODING=UTF8 && psql "$SUPABASE_DB_URL" -tA -F'|' -v boletin=14309-04 -f supabase/queries/timeline-regla-de-seleccion.sql | tr -d '\r' | tee /dev/stderr | awk -F'|' 'NR==1{if($1-$2!=$4){print "FAIL: N-K != H"; exit 1} if($1!=99){print "FAIL: totales != 99"; exit 1} print "OK"}'</automated>
    <automated>node -e "const e=require('./app/components/__fixtures__/timeline-14309-04.esperado.json'),f=require('./app/components/__fixtures__/timeline-14309-04.json'); if(f.length!==e.eventos_totales) throw new Error('fixture length != eventos_totales'); if(e.eventos_totales-e.eventos_absorbidos!==e.hitos_del) throw new Error('N-K != H'); const ids=f.map(r=>r.id); for(let i=1;i<f.length;i++){const a=f[i-1],b=f[i]; if(a.fecha>b.fecha||(a.fecha===b.fecha&&a.id>b.id)) throw new Error('fixture no está en orden (fecha asc, id asc) en '+i);} console.log('OK',e.hitos_del)"</automated>
    <automated>grep -v '^--' supabase/queries/timeline-regla-de-seleccion.sql | grep -c "at time zone 'America/Santiago'" | grep -qx 0 && echo "OK sin tz"</automated>
  </verify>
  <done>
`supabase/queries/timeline-regla-de-seleccion.sql` existe, es ejecutable tal cual por psql contra
PROD, declara el criterio y el orden total; la identidad `eventos_totales − eventos_absorbidos =
hitos_del` cierra sin residuo sobre 14309-04; el fixture de 99 eventos está en orden
(fecha asc, id asc); el número vive en UNA sola sede (`*.esperado.json`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fijar el orden total en la lectura + assert de que el orden es total</name>
  <files>app/app/proyecto/[boletin]/page.tsx, app/components/timeline-view.test.tsx</files>
  <read_first>
    - `app/app/proyecto/[boletin]/page.tsx:451-478` — `TramitacionSection`, la única lectura de
      `tramitacion_evento` del árbol.
    - `app/components/timeline-view.test.tsx` — 603 líneas, 11 `describe`; AÑADIR, jamás reescribir.
  </read_first>
  <behavior>
    - Test 1 (source-scan, control positivo apareado): la lectura de `tramitacion_evento` en
      `page.tsx` encadena DOS `.order(` — el segundo por `"id"` ascendente. Con la fuente real →
      PASA. Con un fixture en memoria que contenga sólo `.order("fecha", { ascending: true })` →
      el MISMO detector debe FALLAR (cero vacuo: se ejercita el detector real sobre SQL/TS en
      memoria, no una copia del regex).
    - Test 2: el detector no se satisface con un `.order("id")` que viva en OTRA query del archivo
      — el match se acota al bloque que sigue a `.from("tramitacion_evento")`.
  </behavior>
  <action>
En `app/app/proyecto/[boletin]/page.tsx:459-465`, encadenar el desempate tras el `.order("fecha")`:
`.order("id", { ascending: true })`. Un solo cambio de una línea (D-03: fix localizado).
Añadir comentario breve citando el defecto: sin desempate, 99 eventos sobre 48 fechas distintas
dejan el orden dentro del empate en manos de Postgres (plan/heap/vacuum) y, como el colapso de
urgencias exige CONTIGÜIDAD, el número de `Hito del` cambia entre corridas sin que cambie un dato
(medido: 14/12/12/16 absorbidos). El sort de `construirItems` es estable ⇒ HEREDA este orden.
NO tocar nada más del archivo.

En `app/components/timeline-view.test.tsx`, añadir un `describe` nuevo
("(H-06/D-03) orden total determinista en la lectura de tramitacion_evento") con los dos tests del
bloque `<behavior>`. El detector se escribe como helper PURO
(`function ordenTotalDeclarado(src: string): boolean`) usado tanto contra el archivo real
(`readFileSync` de `app/app/proyecto/[boletin]/page.tsx`) como contra los fixtures en memoria del
control positivo — un solo detector, cero copia que pueda derivar.
  </action>
  <verify>
    <automated>pnpm --filter ./app exec vitest run components/timeline-view.test.tsx</automated>
    <automated>pnpm --filter ./app exec tsc --noEmit</automated>
  </verify>
  <done>
La lectura de `tramitacion_evento` declara un orden total `(fecha asc, id asc)`; el guard de orden
total muerde (control positivo apareado en rojo cuando falta el desempate); `tsc --noEmit` limpio.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test de paridad regla↔builder sobre los 99 eventos reales</name>
  <files>app/components/timeline-view.test.tsx</files>
  <read_first>
    - `app/components/timeline-view.tsx:148-220` — `construirItems`, el builder bajo prueba.
    - `app/components/__fixtures__/timeline-14309-04.esperado.json` — el número congelado (Task 1).
  </read_first>
  <behavior>
    - Test 1: `construirItems(fixture)` produce exactamente `esperado.periodos` ítems de
      `kind === "periodo"`.
    - Test 2: la suma de `periodo.eventos.length` sobre esos períodos es exactamente
      `esperado.eventos_absorbidos`.
    - Test 3: el número de ítems `kind === "evento"` es exactamente `esperado.hitos_del` — es decir,
      el número de líneas `Hito del` que el render emitirá (uno por `TimelineEvent`,
      `timeline-event.tsx:102` es la única emisión del literal en el árbol de producción).
    - Test 4 (cierre sin residuo): `hitos_del + eventos_absorbidos === eventos_totales ===
      fixture.length`.
    - Los cuatro leen los valores del JSON esperado por import — CERO literales numéricos horneados
      en el test (Pitfall 2).
  </behavior>
  <action>
Añadir en `app/components/timeline-view.test.tsx` un `describe` nuevo
("(H-06/D-02) paridad regla escrita ↔ construirItems sobre el testigo 14309-04") que importe el
fixture de 99 eventos y el JSON esperado, y ejerza los cuatro tests del bloque `<behavior>` contra
`construirItems`.

El fixture se pasa TAL CUAL (ya viene en el orden `(fecha asc, id asc)` que la página entrega tras
Task 2) — no re-ordenar en el test, o se estaría probando otra cosa que la producción.

Añadir en el mismo `describe` un comentario que declare qué NO prueba esta paridad: ata el builder
TS a la regla escrita medida contra PROD, pero NO toca el DOM del deploy real. La paridad DOM
(`grep -o 'Hito del' … | wc -l` sobre el HTML del Worker — jamás `grep -c`, el HTML es de una línea)
queda delegada a la Phase 138 con el número YA congelado en `*.esperado.json`.
  </action>
  <verify>
    <automated>pnpm --filter ./app exec vitest run components/timeline-view.test.tsx</automated>
    <automated>test $(grep -rn --include=*.ts --include=*.tsx --include=*.sql -o "$(node -e "process.stdout.write(String(require('./app/components/__fixtures__/timeline-14309-04.esperado.json').hitos_del))")" app/components/timeline-view.test.tsx supabase/queries/timeline-regla-de-seleccion.sql | wc -l) -eq 0 && echo "OK: el número no está horneado fuera de su sede"</automated>
  </verify>
  <done>
El test de paridad corre verde y liga `construirItems` al número medido en PROD; si el builder
cambia de heurística, se pone rojo. El número no aparece literal fuera de `*.esperado.json`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD Postgres → agente (psql read-only) | Sólo `select`; esta fase no escribe nada en Task 1-3. |
| tramitacion_evento → render público | Datos ya públicos; el riesgo no es fuga sino TERGIVERSACIÓN del conteo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering (integridad del hecho publicado) | `construirItems` + `.order()` de `page.tsx` | mitigate | Orden total `(fecha, id)` declarado en la regla escrita y en la lectura; test de paridad ata el builder al número medido. Sin esto el conteo publicado varía con el vacuum. |
| T-131-02 | Information Disclosure | `supabase/queries/timeline-regla-de-seleccion.sql` | mitigate | Archivo documental: cero credenciales embebidas; toma `$SUPABASE_DB_URL` del entorno; boletín por variable psql `:'boletin'`, jamás interpolación de string. |
| T-131-03 | Repudiation (número no auditable) | número de hitos | mitigate | SEDE ÚNICA (`*.esperado.json`) con `orden` y `medido_en`; gate que prohíbe el literal duplicado. |
| T-131-04 | Elevation of Privilege | — | accept | La fase NO crea función SQL ni vista (D-01 veta `create view`; Pattern 1 del research descarta la función por no tener consumidor). Cero superficie nueva ⇒ cero allowlist nuevo en este plan. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala ningún paquete (`package.json`/`pnpm-lock.yaml` intactos). Slopcheck no corresponde; el control es el diff vacío en esos dos archivos. |
</threat_model>

<verification>
1. `pnpm --filter ./app exec vitest run components/timeline-view.test.tsx` — verde.
2. `pnpm --filter ./app exec tsc --noEmit` — limpio.
3. `psql` de la regla contra PROD sobre `14309-04` cierra `N − K = H` sin residuo.
4. `git diff --stat -- package.json pnpm-lock.yaml app/package.json` vacío (cero dependencias nuevas).
5. `grep -v '^--' supabase/queries/timeline-regla-de-seleccion.sql | grep -c "America/Santiago"` = 0.
</verification>

<success_criteria>
- DEBT-03 satisfecho: regla escrita con criterio declarado que explica la brecha por conteo exacto.
- El defecto de determinismo (D-03) queda corregido con fix localizado + guard con control positivo.
- El número de `Hito del` está congelado en una sola sede, medido sobre el orden nuevo (NO 85 heredado).
- Paridad builder↔regla probada en unit; paridad DOM documentada como delegada a 138.
</success_criteria>

<output>
Create `.planning/phases/131-debt-ficha-regla-del-timeline-co-autoria-sin-truncamiento/131-01-SUMMARY.md` when done.
Registrar en el SUMMARY el número congelado (H) y el desglose de períodos, para que 138 lo consuma.
</output>
