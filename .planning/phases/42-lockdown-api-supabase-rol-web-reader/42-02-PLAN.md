---
phase: 42-lockdown-api-supabase-rol-web-reader
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0044_lockdown_revoke_anon.sql
  - supabase/tests/post-apply/0044_revoke_anon.test.sql
autonomous: false
requirements: [LOCKDOWN-02]
user_setup:
  - service: supabase-prod
    why: "Aplicar 0044 (revoke) es el PASO 3 ÚLTIMO del cutover y es checkpoint operador. Aplicar antes de que el server web_reader esté vivo = sitio caído."
    dashboard_config:
      - task: "Aplicar 0044 con psql --single-transaction SOLO DESPUÉS de 42-01 (0043) aplicada Y 42-03 deployada+verificada en Cloudflare"
        location: "psql \"$SUPABASE_DB_URL\" --single-transaction -f supabase/migrations/0044_lockdown_revoke_anon.sql"

must_haves:
  truths:
    - "Tras 0044, anon y authenticated NO tienen EXECUTE en ninguno de los 15 RPCs ni SELECT en ninguna tabla/vista (incl. PII) → API pública muerta"
    - "Las 26 policies _public_read (to anon) se eliminan; las 26 _wr (to web_reader) quedan intactas"
    - "ALTER DEFAULT PRIVILEGES FOR ROLE postgres revoca a anon/authenticated → objetos futuros postgres-owned no re-conceden anon"
    - "0044 aborta con raise exception si web_reader no existe (protege orden de cutover); web_reader y service_role quedan intactos"
  artifacts:
    - path: "supabase/migrations/0044_lockdown_revoke_anon.sql"
      provides: "Guard web_reader-exists + drop 26 policies anon + REVOKE ALL ON ALL + ALTER DEFAULT PRIVILEGES FOR ROLE postgres + rollback inline"
      contains: "alter default privileges"
    - path: "supabase/tests/post-apply/0044_revoke_anon.test.sql"
      provides: "pgTAP post-apply: anon/authed sin grants (incl vista+PII), 26 _public_read fuera, 26 _wr dentro, web_reader+service_role intactos"
      contains: "plan("
  key_links:
    - from: "0044_lockdown_revoke_anon.sql"
      to: "anon/authenticated grants"
      via: "revoke all on all + drop policy + alter default privileges"
      pattern: "revoke all on all"
    - from: "0044_lockdown_revoke_anon.sql"
      to: "web_reader existence guard"
      via: "raise exception si no existe"
      pattern: "raise exception"
---

<objective>
Escribir la migración `0044_lockdown_revoke_anon.sql` (LOCKDOWN-02) que mata el canal de API público de Supabase: dropea las 26 policies `*_public_read` (to anon), revoca ALL en todas las tablas/rutinas/secuencias de anon+authenticated, y aplica `ALTER DEFAULT PRIVILEGES FOR ROLE postgres … REVOKE` para neutralizar re-grants futuros. Guardada con `raise exception` si web_reader no existe (protege el orden). Más su pgTAP post-apply (en `supabase/tests/post-apply/`, fuera del glob normal). Es el PASO 3 ÚLTIMO del cutover.

Purpose: revocar anon antes de que el server web_reader esté vivo deja el sitio caído; por eso 0044 es el archivo independiente que se ESCRIBE ahora pero se APLICA último, tras 0043 aplicada y 03 deployada. El `ALTER DEFAULT PRIVILEGES … FOR ROLE postgres` cierra el gotcha recurrente del repo (re-grants silenciosos a anon).

Output: `supabase/migrations/0044_lockdown_revoke_anon.sql` + `supabase/tests/post-apply/0044_revoke_anon.test.sql`. La aplicación a PROD es checkpoint operador, ÚLTIMO en el orden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-RESEARCH.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/_FACTS-live-prod.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-VALIDATION.md
@CLAUDE.md

# Drafts de partida — AJUSTAR: hacer el ALTER DEFAULT PRIVILEGES explícito FOR ROLE postgres; firmas de RPC del test a las exactas; añadir asserts de vista+PII
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/0044_lockdown_revoke_anon.sql
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/0044_revoke_anon.test.sql

# Espejo de post-apply test (fuera del glob) y header de migración
@supabase/tests/post-apply/0042_cruces_grant_anon.test.sql
@supabase/migrations/0042_cruces_grant_anon.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 0044_lockdown_revoke_anon.sql (guard + drop 26 + revoke all + default privileges FOR ROLE postgres)</name>
  <read_first>
    - drafts/0044_lockdown_revoke_anon.sql (header LOUD + guard web_reader + drop de las 26 policies + revoke all on all + alter default privileges ya redactados; AJUSTAR: el draft duda del owner — hacer el bloque ALTER DEFAULT PRIVILEGES EXPLÍCITO con FOR ROLE postgres, per VALIDATION BLOCKER 2)
    - 42-RESEARCH.md §3 LOCKDOWN-02 (los 9 pasos del spec final, incl. `ALTER DEFAULT PRIVILEGES … FOR ROLE postgres REVOKE …` para tables/routines/sequences) y §4 (runbook/rollback)
    - 42-VALIDATION.md BLOCKER 2 (reversal de ALTER DEFAULT PRIVILEGES con FOR ROLE postgres; supabase_admin governa internals, backstop = CI guard)
    - _FACTS-live-prod.md §"26 RLS SELECT policies" (las 26 a dropear) y §"connection role = postgres" (current_role al aplicar = postgres → FOR ROLE postgres es el correcto)
  </read_first>
  <action>
    Crear supabase/migrations/0044_lockdown_revoke_anon.sql con header LOUD: "PASO 3 de 3 (ÚLTIMO); aplicar ANTES del deploy de 03 = SITIO CAÍDO; rollback inline = reverse-0044". En orden:
    (1) Guard: bloque `do $$ begin if not exists (select 1 from pg_roles where rolname='web_reader') then raise exception '0044 abortada: web_reader no existe. Aplicar 0043 + deployar 03 antes.'; end if; end; $$;` (copiar del draft).
    (2) `drop policy if exists <t>_public_read on public.<t>;` para las 26 tablas (lista exacta de _FACTS; copiar del draft — NO tocar las policies `_wr`).
    (3) `revoke all on all tables in schema public from anon, authenticated;` + idem `on all routines` + `on all sequences` (el catch-all es correcto y requerido per VALIDATION B1).
    (4) Bloque ALTER DEFAULT PRIVILEGES EXPLÍCITO con FOR ROLE postgres (CORRECCIÓN sobre el draft, que lo dejaba implícito/dubitativo): `alter default privileges for role postgres in schema public revoke all on tables from anon, authenticated;` + idem `on routines` + `on sequences`. Esto neutraliza re-grants futuros de objetos postgres-owned (el gotcha recurrente del repo).
    (5) NO revocar `usage on schema public` de anon (se mantiene vía PUBLIC; con cero grants de objeto anon ya recibe permission-denied; menor blast radius; simplifica rollback). Documentar la decisión en comentario.
    (6) Comentario de cabecera con el ROLLBACK (reverse-0044): recrear las 26 policies _public_read to anon + re-grant select/execute a anon + reverse del alter default privileges (re-grant). Terminar con comentario de la fila schema_migrations (versión 0044). EOL = LF.
  </action>
  <acceptance_criteria>
    - El archivo contiene el guard `raise exception` sobre la no-existencia de web_reader.
    - El archivo contiene exactamente 26 `drop policy if exists %_public_read on public.` (sin `_wr`).
    - El archivo contiene las 3 sentencias `revoke all on all tables/routines/sequences in schema public from anon, authenticated`.
    - El archivo contiene `alter default privileges for role postgres in schema public revoke all on tables from anon, authenticated` (con `for role postgres` EXPLÍCITO) y las variantes routines/sequences.
    - El archivo NO revoca `usage on schema public` de anon.
    - Header LOUD con "PASO 3" / "ÚLTIMO" / "SITIO CAÍDO" y bloque de rollback (reverse-0044) presente.
  </acceptance_criteria>
  <done>0044 escrita: guarded, dropea 26 anon policies, revoke all on all, default privileges FOR ROLE postgres explícito, rollback documentado.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir post-apply/0044_revoke_anon.test.sql (anon muerto + web_reader/service_role vivos)</name>
  <read_first>
    - drafts/0044_revoke_anon.test.sql (107 asserts ya redactados: 15 anon NOT EXEC + 15 authed NOT EXEC + 4 anon NOT SELECT + 26 _public_read fuera + 26 _wr dentro + 15 web_reader EXEC + 4 web_reader SELECT + 2 service_role; AJUSTAR: las firmas de RPC del draft están truncadas — usar las firmas exactas de RESEARCH §1; AÑADIR asserts negativos de vista (pg_all_foreign_keys) y PII (parlamentario) per VALIDATION B1/B5)
    - 42-RESEARCH.md §1 (firmas exactas de los 15 RPCs) y §3 LOCKDOWN-02 párrafo pgTAP (incl. assert sobre vista + tabla PII)
    - supabase/tests/post-apply/0042_cruces_grant_anon.test.sql (estilo post-apply fuera del glob, header explicando por qué)
  </read_first>
  <action>
    Crear supabase/tests/post-apply/0044_revoke_anon.test.sql con `begin; select plan(N); … finish; rollback;`. Header explicando: POST-APPLY ONLY, vive en post-apply/ fuera del glob normal (`supabase/tests/*.test.sql`) porque asserta el estado revocado — la suite regular lo rompería pre-lockdown; el operador lo corre a mano con `psql -tA -f` contra PROD ya aplicado. Asserts:
    SECCIÓN A: `not has_function_privilege('anon','public.<rpc>(<firma exacta>)','execute')` para los 15 RPCs (firmas de RESEARCH §1, p.ej. match_proyectos(vector, integer, double precision, text), buscar_citaciones(text, integer, text), subgrafo_red(text, integer, text[], timestamptz, timestamptz), votos_de_parlamentario(text, integer, integer), comparar_declaraciones(text, date[]) — NO las firmas truncadas del draft).
    SECCIÓN B: idem para 'authenticated'.
    SECCIÓN C: `not has_table_privilege('anon','public.<t>','select')` para proyecto, votacion, declaracion, lobby_audiencia, MÁS un assert de vista `not has_table_privilege('anon','public.pg_all_foreign_keys','select')` y uno de PII `not has_table_privilege('anon','public.parlamentario','select')`.
    SECCIÓN D: las 26 policies `<t>_public_read` ya no existen (is_empty sobre pg_policies).
    SECCIÓN E: las 26 policies `<t>_public_read_wr` siguen existiendo (isnt_empty).
    SECCIÓN F: web_reader SÍ tiene EXECUTE en los 15 RPCs (regresión: el revoke no lo tocó).
    SECCIÓN G: web_reader SÍ tiene SELECT en proyecto/votacion/declaracion/lobby_audiencia.
    SECCIÓN H: service_role sanity (EXECUTE + SELECT intactos).
    Ajustar `plan(N)` al conteo real (el draft dice 107 sin los 2 asserts nuevos de vista+PII → recalcular). EOL = LF.
  </action>
  <acceptance_criteria>
    - El archivo vive en `supabase/tests/post-apply/0044_revoke_anon.test.sql` (NO en supabase/tests/).
    - Contiene un assert `not has_table_privilege('anon', 'public.pg_all_foreign_keys', 'select')` (vista) y otro sobre `public.parlamentario` (PII).
    - Las firmas de RPC usan las exactas de RESEARCH §1 (p.ej. `match_proyectos(vector, integer, double precision, text)`), no las truncadas.
    - `plan(N)` coincide con el número de asserts ejecutados.
    - Tras aplicar 0044 a PROD (Task 3): `psql -tA -f supabase/tests/post-apply/0044_revoke_anon.test.sql` reporta todas las líneas `ok`, cero `not ok`.
  </acceptance_criteria>
  <done>Test post-apply escrito con firmas exactas, asserts de vista+PII, 26/26 policies, web_reader+service_role intactos; plan(N) consistente.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [CHECKPOINT OPERADOR] Aplicar 0044 a PROD — PASO 3 ÚLTIMO del cutover</name>
  <what-built>Migración 0044 (revoca anon/authenticated, dropea 26 policies anon, default privileges FOR ROLE postgres) y su pgTAP post-apply. El agente NO aplica (gate 4; orden load-bearing).</what-built>
  <how-to-verify>
    PRE-REQUISITOS OBLIGATORIOS (orden load-bearing — si alguno falla, NO aplicar):
    - 42-01 (0043) aplicada a PROD (web_reader existe con sus grants; pgTAP 0043 verde).
    - 42-03 deployado a Cloudflare Y smoke-test del sitio verde (server lee como web_reader, todas las superficies renderizan).
    Recién entonces:
    1. Cargar SUPABASE_DB_URL vía parse node de .env (strip BOM; nunca echo). PGCLIENTENCODING=UTF8.
    2. Aplicar: `psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0044_lockdown_revoke_anon.sql` (NUNCA `supabase db push`). El guard aborta si web_reader no existe.
    3. Insertar fila en schema_migrations para la versión 0044.
    4. Verificar (estructural): `psql -tA -f supabase/tests/post-apply/0044_revoke_anon.test.sql` → todas las líneas `ok`.
    5. PROBE LIVE OBLIGATORIO (behavioral — el pgTAP estructural NO basta, VALIDATION B5): con la anon key como `apikey` Y `Authorization: Bearer`, correr `curl` contra (a) `POST $SUPABASE_URL/rest/v1/rpc/parlamentario_publico`, (b) `GET $SUPABASE_URL/rest/v1/proyecto?select=*`, (c) la vista `GET $SUPABASE_URL/rest/v1/pg_all_foreign_keys?select=*`, (d) la tabla PII `GET $SUPABASE_URL/rest/v1/parlamentario?select=*` → esperar 401/`42501`/permission-denied en los CUATRO. Si alguno devuelve 200/filas, la API NO está muerta → ejecutar rollback. (Comandos exactos en docs/RUNBOOK-lockdown-cutover.md.)
    6. Confirmar que el sitio SIGUE arriba (lee como web_reader, no afectado por el revoke): smoke de votaciones/lobby/patrimonio/dinero/NET/cruces/búsqueda/agenda/parlamentarios/proyecto.
    ROLLBACK de emergencia (si el sitio cae): reverse-0044 — recrear las 26 policies _public_read to anon + re-grant select/execute a anon + reverse del alter default privileges. Script listo en el header de 0044 (y replicable desde 0043).
  </how-to-verify>
  <resume-signal>Escribe "0044 aplicada, anon muerto, sitio verde" cuando el pgTAP post-apply esté verde y el sitio siga vivo, o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- 0044 idempotente y guarded (raise exception si web_reader ausente; drop policy if exists; revoke es idempotente).
- pgTAP post-apply verde: anon/authenticated sin EXECUTE/SELECT (incl vista+PII), 26 _public_read fuera, 26 _wr dentro, web_reader+service_role intactos.
- Aplicado ÚLTIMO en el cutover, tras 0043 aplicada y 03 deployada+verificada.
</verification>

<success_criteria>
LOCKDOWN-02: migración que revoca TODO de anon/authenticated (execute en cada RPC + select en cada tabla + drop policies to anon + default privileges FOR ROLE postgres), aplicada ÚLTIMA tras el server web_reader vivo. pgTAP: anon/authenticated sin execute/select en todo el inventario; web_reader intacto.
</success_criteria>

<output>
Create `.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-02-SUMMARY.md` when done
</output>
