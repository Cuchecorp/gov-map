---
phase: 42-lockdown-api-supabase-rol-web-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0043_lockdown_web_reader.sql
  - supabase/tests/0043_web_reader.test.sql
autonomous: false
requirements: [LOCKDOWN-01]
user_setup:
  - service: supabase-prod
    why: "Aplicar DDL 0043 a PROD es checkpoint operador (gate 4 — el agente nunca aplica solo). Paso 1 del cutover ORDENADO."
    dashboard_config:
      - task: "Aplicar 0043 con psql --single-transaction + fila en schema_migrations (NUNCA supabase db push)"
        location: "psql \"$SUPABASE_DB_URL\" --single-transaction -f supabase/migrations/0043_lockdown_web_reader.sql"

must_haves:
  truths:
    - "El rol web_reader existe (NOLOGIN) y authenticator es miembro suyo (PostgREST puede SET ROLE web_reader)"
    - "web_reader tiene SELECT en exactamente las 26 tablas public-read + EXECUTE en exactamente los 15 RPCs curados — ni uno más"
    - "web_reader NO tiene EXECUTE sobre resolver_entidad, NO tiene SELECT sobre tablas PII (parlamentario) ni sobre las 2 vistas pgTAP"
    - "Aplicar 0043 no toca a anon: anon conserva todos sus grants (ventana segura: ambos roles leen idéntico)"
  artifacts:
    - path: "supabase/migrations/0043_lockdown_web_reader.sql"
      provides: "Migración idempotente: crea web_reader + grants enumerados + 26 policies _wr; NO revoca nada"
      contains: "create policy proyecto_public_read_wr"
    - path: "supabase/tests/0043_web_reader.test.sql"
      provides: "pgTAP con asserts positivos y NEGATIVOS (no resolver_entidad, no PII, no vista) + conteo 26 policies _wr"
      contains: "plan("
  key_links:
    - from: "0043_lockdown_web_reader.sql"
      to: "authenticator role"
      via: "grant web_reader to authenticator"
      pattern: "grant web_reader to authenticator"
    - from: "0043_web_reader.test.sql"
      to: "web_reader grants in catalog"
      via: "has_table_privilege / has_function_privilege / pg_policy asserts"
      pattern: "has_function_privilege\\('web_reader'"
---

<objective>
Escribir la migración `0043_lockdown_web_reader.sql` (LOCKDOWN-01) que crea el rol de mínimo privilegio `web_reader` y le concede un set ENUMERADO (estricto subconjunto de anon): SELECT en las 26 tablas public-read, EXECUTE en los 15 RPCs curados por firma exacta, USAGE en schema public, membresía en authenticator, y las 26 RLS policies paralelas `*_public_read_wr`. Más su test pgTAP con asserts positivos y NEGATIVOS. La migración es idempotente y NO revoca nada de anon (ventana segura). Es el PASO 1 del cutover.

Purpose: sin web_reader con sus grants exactos en PROD, el servidor no puede migrar de credencial y el revoke (0044) dejaría el sitio caído. El set debe ser un subconjunto estricto de anon para no violar gate 3 (PISO PII) ni abrir un RLS-bypass vía vistas.

Output: `supabase/migrations/0043_lockdown_web_reader.sql` + `supabase/tests/0043_web_reader.test.sql`. La aplicación a PROD es checkpoint operador separado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-RESEARCH.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/_FACTS-live-prod.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-CONTEXT.md
@CLAUDE.md

# Drafts de partida — CORREGIR a la forma ENUMERADA (los drafts usaban ON ALL, rechazado por los validadores Opus)
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/0043_lockdown_web_reader.sql
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/0043_web_reader.test.sql

# Migraciones espejo (estilo grant + policy public-read, security-definer RPCs, header + schema_migrations)
@supabase/migrations/0042_cruces_grant_anon.sql
@supabase/migrations/0040_cruces_rpc.sql
@supabase/migrations/0038_sector.sql

# Tests pgTAP espejo
@supabase/tests/0040_cruces_rpc.test.sql
@supabase/tests/post-apply/0042_cruces_grant_anon.test.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 0043_lockdown_web_reader.sql con grants ENUMERADOS (no ON ALL)</name>
  <read_first>
    - 42-RESEARCH.md §0 (por qué ON ALL rompe gate 3 + RLS-bypass) y §3 LOCKDOWN-01 (los 8 pasos del spec final)
    - _FACTS-live-prod.md §"26 RLS SELECT policies" (lista exacta de 26 tablas) y §"15 curated public RPCs" (firmas exactas) y §"15 RPC exact signatures" en RESEARCH §1
    - drafts/0043_lockdown_web_reader.sql (estructura del header + bloque do$$ del rol + las 26 policies _wr ya redactadas correctamente; SOLO hay que reemplazar el bloque "GRANT ... ON ALL" por grants enumerados y borrar el revoke de resolver_entidad)
    - supabase/migrations/0042_cruces_grant_anon.sql y 0038_sector.sql (header LOUD + convención de fila schema_migrations comentada)
  </read_first>
  <action>
    Crear supabase/migrations/0043_lockdown_web_reader.sql con header LOUD declarando "PASO 1 de 3 del cutover; NO revoca nada; seguro aplicar temprano; tras esto anon y web_reader leen idéntico (ventana segura)". Contenido en este orden, todo idempotente:
    (1) bloque `do $$ ... if not exists (select 1 from pg_roles where rolname='web_reader') then create role web_reader nologin; end if;` (copiar del draft, correcto).
    (2) `grant web_reader to authenticator;` guardado contra re-run con `if not exists (… pg_auth_members … donde member=authenticator y roleid=web_reader)` dentro de un do$$ (el draft lo hace sin guarda; añadir la guarda pg_auth_members por idempotencia limpia per RESEARCH §3.2).
    (3) `grant usage on schema public to web_reader;`.
    (4) Grants SELECT ENUMERADOS — una sentencia `grant select on public.<t> to web_reader;` por cada una de las 26 tablas de _FACTS §"26 RLS SELECT policies" (aporte, aportes_ingesta_estado, citacion, citacion_invitado, citacion_punto, contrato, contratos_ingesta_estado, declaracion, declaracion_accion_derecho, declaracion_actividad, declaracion_bien_inmueble, declaracion_bien_mueble, declaracion_pasivo, declaracion_valor, lobby_audiencia, lobby_ingesta_estado, probidad_ingesta_estado, proyecto, proyecto_embedding, proyecto_ficha, sector, sesion_sala, sesion_tabla_item, tramitacion_evento, votacion, voto). NO `grant select on all tables`.
    (5) Grants EXECUTE ENUMERADOS — una sentencia `grant execute on function public.<rpc>(<firma exacta>) to web_reader;` por cada uno de los 15 RPCs con las firmas EXACTAS de RESEARCH §1: agregado_por_contraparte(p_id text), aportes_de_parlamentario(p_id text), bienes_de_parlamentario(p_id text), buscar_citaciones(q text, limite integer, p_camara text), comparar_declaraciones(p_id text, fechas date[]), contratos_de_parlamentario(p_id text), cruces_de_parlamentario(p_id text), declaraciones_de_parlamentario(p_id text), lobby_de_parlamentario(p_id text), match_proyectos(query_embedding vector, match_count integer, match_threshold double precision, exclude_boletin text), parlamentario_publico(p_id text), parlamentarios_publico(), rebeldias_de_parlamentario(p_id text), subgrafo_red(p_id text, p_depth integer, p_tipos text[], p_desde timestamptz, p_hasta timestamptz), votos_de_parlamentario(p_id text, p_limit integer, p_offset integer). NO `grant execute on all routines`. NO incluir resolver_entidad. NO incluir ningún revoke de resolver_entidad (con la forma enumerada, resolver_entidad nunca se concede → no hay que revocarlo; eliminar el bloque del draft que lo revocaba).
    (6) Las 26 policies `create policy <t>_public_read_wr on <t> for select to web_reader using (true);` cada una precedida de `drop policy if exists <t>_public_read_wr on <t>;` (copiar tal cual del draft — ya están correctas y completas).
    (7) NO grants de secuencias, NO grants de escritura, NO grants pgvector (PUBLIC los cubre: match_proyectos hereda EXECUTE de los operadores pgvector vía PUBLIC), NO vistas.
    Terminar con un comentario de la fila a insertar en schema_migrations (versión 0043) siguiendo el patrón de 0042. EOL = LF.
  </action>
  <acceptance_criteria>
    - El archivo NO contiene la subcadena `on all tables` ni `on all routines` ni `on all sequences` (verificable con grep -i).
    - El archivo NO contiene `resolver_entidad` en ninguna forma.
    - El archivo contiene exactamente 26 sentencias `grant select on public.` (una por tabla) y exactamente 15 `grant execute on function public.` (una por RPC).
    - El archivo contiene `create policy proyecto_public_read_wr` y exactamente 26 sentencias `create policy %_public_read_wr`.
    - El archivo contiene `match_proyectos(query_embedding vector, match_count integer, match_threshold double precision, exclude_boletin text)` (firma de 4 args, no la firma truncada del draft de test).
    - Header LOUD presente con "PASO 1 de 3" y "NO revoca".
  </acceptance_criteria>
  <done>0043 escrita, enumerada, idempotente, sin ON ALL, sin resolver_entidad, con las 26 policies _wr y header de cutover.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir 0043_web_reader.test.sql con asserts positivos + NEGATIVOS + conteo</name>
  <read_first>
    - drafts/0043_web_reader.test.sql (14 asserts ya redactados; AJUSTAR: firma de match_proyectos a la de 4 args, añadir asserts negativos de PII y vista que faltan)
    - 42-RESEARCH.md §3 LOCKDOWN-01 párrafo pgTAP (lista de asserts requeridos, incl. negativos: no resolver_entidad EXECUTE, no parlamentario SELECT, no pg_all_foreign_keys SELECT, conteo=26)
    - supabase/tests/0040_cruces_rpc.test.sql (estilo plan(N)/ok/has_function_privilege/finish/rollback)
  </read_first>
  <action>
    Crear supabase/tests/0043_web_reader.test.sql con `begin; select plan(N); … select * from finish(); rollback;`. Asserts requeridos:
    POSITIVOS: has_role('web_reader'); web_reader es NOLOGIN; pg_has_role('authenticator','web_reader','member'); has_table_privilege('web_reader','public.proyecto','select') y para votacion y declaracion y proyecto_embedding; has_function_privilege('web_reader','public.parlamentario_publico(text)','execute'); has_function_privilege('web_reader','public.match_proyectos(vector, integer, double precision, text)','execute') (firma de 4 args — secdef=f, ejercita pgvector vía PUBLIC); has_function_privilege('web_reader','public.cruces_de_parlamentario(text)','execute'); existencia de policy proyecto_public_read_wr y voto_public_read_wr (vía pg_policy join pg_class); is(count(*),26) de policies like '%_public_read_wr'.
    NEGATIVOS (gate 3 — el set es subconjunto estricto de anon): not has_function_privilege('web_reader','public.resolver_entidad(text)','execute'); not has_table_privilege('web_reader','public.parlamentario','select') (tabla PII); not has_table_privilege('web_reader','public.pg_all_foreign_keys','select') (vista — RLS-bypass). 
    REGRESIÓN ANON: has_function_privilege('anon','public.parlamentario_publico(text)','execute') (0043 no revocó nada de anon).
    Ajustar el número de `plan(N)` al conteo real de asserts escritos. EOL = LF. Comentario de cabecera: post-aplicar correr con `psql -tA -f supabase/tests/0043_web_reader.test.sql` contra PROD aplicado.
  </action>
  <acceptance_criteria>
    - El archivo contiene `not has_function_privilege('web_reader', 'public.resolver_entidad(text)', 'execute')`.
    - El archivo contiene un assert negativo sobre `public.parlamentario` (PII) y otro sobre `public.pg_all_foreign_keys` (vista).
    - El archivo contiene `is(` con valor `26` para el conteo de policies `%_public_read_wr`.
    - El número en `plan(N)` coincide con el número de sentencias `select ok(`/`select is(`/`select has_*` ejecutadas (contables).
    - Tras aplicar 0043 a PROD (Task 3): `psql -tA -f supabase/tests/0043_web_reader.test.sql` reporta `ok` en todas las líneas y `# Looks like you ran N tests` sin fallos.
  </acceptance_criteria>
  <done>Test pgTAP escrito con cobertura positiva, negativa (PII+vista+resolver_entidad) y de conteo; plan(N) consistente.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [CHECKPOINT OPERADOR] Aplicar 0043 a PROD — PASO 1 del cutover</name>
  <what-built>Migración 0043 (crea web_reader + grants enumerados + 26 policies _wr, sin revocar nada) y su pgTAP. El agente NO aplica a PROD por gate 4.</what-built>
  <how-to-verify>
    1. Cargar SUPABASE_DB_URL vía parse node de .env (strip BOM; nunca echo). PGCLIENTENCODING=UTF8.
    2. Aplicar: `psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0043_lockdown_web_reader.sql` (NUNCA `supabase db push`).
    3. Insertar fila en schema_migrations para la versión 0043 (patrón del repo).
    4. Verificar: `psql -tA -f supabase/tests/0043_web_reader.test.sql` → todas las líneas `ok`, cero `not ok`.
    5. Sanity: el sitio sigue vivo (anon intacto; web_reader recién creado no se usa aún). Esta es la VENTANA SEGURA — ambos roles leen idéntico.
    Rollback si algo falla: `drop policy` de las 26 `_wr` + `drop role web_reader;` (nada más fue tocado).
  </how-to-verify>
  <resume-signal>Escribe "0043 aplicada" cuando el pgTAP esté verde en PROD, o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- 0043 es idempotente (re-aplicable sin error: guardas pg_roles/pg_auth_members + drop policy if exists).
- pgTAP verde contra PROD incluyendo los 3 asserts negativos (resolver_entidad / PII / vista).
- anon sigue con sus grants tras 0043 (regresión verde).
</verification>

<success_criteria>
LOCKDOWN-01: rol web_reader (NOLOGIN) + grant a authenticator + set ENUMERADO exacto (26 tablas SELECT + 15 RPCs EXECUTE + 26 policies _wr), idempotente, sin revocar anon. pgTAP prueba el set positivo y los límites negativos. Aplicación a PROD = checkpoint operador completado.
</success_criteria>

<output>
Create `.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-01-SUMMARY.md` when done
</output>
