---
phase: 42-lockdown-api-supabase-rol-web-reader
plan: 04
type: execute
wave: 2
depends_on: ["42-01", "42-02", "42-03"]
files_modified:
  - app/lib/lockdown-guard.test.ts
  - docs/RUNBOOK-lockdown-cutover.md
autonomous: true
requirements: [LOCKDOWN-04]

must_haves:
  truths:
    - "El CI falla si una migración nueva introduce un grant a anon (re-exposición) o una policy to anon"
    - "El CI falla si el cliente web_reader (createServerSupabase) gana un uso de .auth.* o un select de columna PII conocida"
    - "Existe un runbook de cutover ORDENADO (paso 0..3) con rollback (reverse-0044) que el operador puede seguir"
    - "El runbook incluye el probe live curl (RPC + tabla + vista + PII → permission denied) y el smoke de todas las superficies"
  artifacts:
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "Guard anti-regresión: escanea supabase/migrations (post-0042) por grant/policy a anon; escanea app/lib/supabase.ts por .auth. y selects PII"
      contains: "to anon"
    - path: "docs/RUNBOOK-lockdown-cutover.md"
      provides: "Runbook ordenado del cutover + rollback + probe live + checklist de superficies"
      contains: "reverse-0044"
  key_links:
    - from: "app/lib/lockdown-guard.test.ts"
      to: "supabase/migrations/*.sql"
      via: "lectura fs relativa a import.meta.url"
      pattern: "migrations"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "app/lib/supabase.ts"
      via: "lectura del chokepoint web_reader"
      pattern: "supabase\\.ts"
---

<objective>
Cerrar LOCKDOWN-04: un guard CI anti-regresión (test vitest que escanea las migraciones nuevas por `grant … to anon`/policies `to anon` y el chokepoint server por usos `.auth.*` o selects de columnas PII), más un runbook de cutover ORDENADO con rollback y el probe live. Depende de que 0043/0044/supabase.ts existan (Wave 2). No toca PROD: documenta y guarda.

Purpose: tras matar la API anon, el riesgo es la regresión silenciosa — una migración futura re-concede anon (el gotcha del repo) o el server gana un select PII. El guard lo atrapa en CI. El runbook codifica el orden load-bearing del cutover para el operador.

Output: `app/lib/lockdown-guard.test.ts` (verde) + `docs/RUNBOOK-lockdown-cutover.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-RESEARCH.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/_FACTS-live-prod.md
@CLAUDE.md
@app/AGENTS.md

# Lo que el guard inspecciona (creados en 42-01/02/03)
@app/lib/supabase.ts
@supabase/migrations/0044_lockdown_revoke_anon.sql

# Estilo de test vitest + cómo el repo resuelve fs en tests (no hay precedente — usar import.meta.url)
@app/lib/cruces-gate.test.ts
@app/vitest.config.ts

# Runbook de partida (cutover ordenado + rollback) ya sintetizado
# (RESEARCH §4 es la tabla autoritativa del runbook; §3 LOCKDOWN-04 es el probe + guard)

# Docs existentes (estilo + ubicación)
@docs/deploy-cloudflare.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir app/lib/lockdown-guard.test.ts (anti-regresión grant-to-anon + .auth + PII)</name>
  <read_first>
    - app/vitest.config.ts (vitest corre desde app/, include `lib/**/*.test.ts`; resolver migrations vía `new URL("../../supabase/migrations", import.meta.url)` porque el test vive en app/lib/)
    - app/lib/cruces-gate.test.ts (estilo describe/it; este test en cambio lee archivos del disco con node:fs)
    - app/lib/supabase.ts (el chokepoint a inspeccionar — debe NO tener `.auth.` salvo el `auth:{...}` de config, y NO seleccionar columnas PII)
    - _FACTS-live-prod.md §"PII tables" y §"App functional surface" (columnas/tablas PII conocidas: rut, partido, donante_id, declaraciones crudas; tablas PII: parlamentario, donante, etc.)
    - supabase/migrations/0042_cruces_grant_anon.sql (ejemplo del patrón histórico `grant … to anon` que NO debe reaparecer en migraciones > 0044)
  </read_first>
  <action>
    Crear app/lib/lockdown-guard.test.ts (vitest, sin red) que lee del disco con `readdirSync`/`readFileSync` de node:fs, resolviendo rutas vía `fileURLToPath(new URL("../../supabase/migrations", import.meta.url))` (robusto al cwd). Bloques:
    (A) Guard "ninguna migración nueva re-concede anon": listar supabase/migrations/*.sql con número > 0044 (parse del prefijo `NNNN_`); para cada uno, fallar si el contenido (minúsculas, comentarios `--` strippeados línea a línea para no contar prosa) matchea `grant\s+.*\s+to\s+.*\banon\b` o `create policy .* to anon` o `for select to anon`. El set actual (≤0044) se ignora — el guard es para el futuro. Si no hay migraciones >0044, el test pasa trivialmente (assert sobre lista vacía). ALCANCE HONESTO (VALIDATION B2): este guard es ESTÁTICO sobre archivos de migración del repo; NO detecta re-grants a anon que ocurran a nivel de CATÁLOGO por el default-ACL de `supabase_admin` (objetos creados por Supabase, no por una migración del proyecto). Ese hueco residual lo cubre SOLO la re-corrida periódica del pgTAP post-apply 0044 — documentarlo en comentario del bloque (A) y en el runbook (Task 2), sin prometer que el guard estático lo atrapa.
    (B) Guard "el chokepoint web_reader no usa .auth ni selecciona PII": leer app/lib/supabase.ts; fallar si contiene un uso de método `.auth.` (regex `\.auth\.` — distinguir del literal de config `auth:` que SÍ es válido; el guard busca `.auth.` con punto antes, no `auth:`); fallar si contiene un `.select(` que nombre una columna PII conocida (rut, donante_id, partido) o un `.from('parlamentario')`/`.from("parlamentario")` u otra tabla PII de _FACTS. Documentar en comentarios que esto es defensa-en-profundidad (RLS ya protege, pero el guard atrapa el intento).
    Usar `describe`/`it`. Mensajes de assert claros indicando el archivo y el patrón ofensor. EOL = LF.
  </action>
  <acceptance_criteria>
    - El test vive en `app/lib/lockdown-guard.test.ts` y matchea el include `lib/**/*.test.ts` del vitest.config.
    - Contiene un assert que escanea migraciones >0044 por `to anon` (grant o policy), ignorando líneas de comentario.
    - Contiene un assert que escanea app/lib/supabase.ts por `.auth.` (uso de método, no el literal de config `auth:`) y por selects/from de PII (parlamentario, rut, donante_id).
    - `cd app && npx vitest run lib/lockdown-guard.test.ts` → verde contra el estado actual del repo (0044 es la última migración; supabase.ts no usa .auth. ni PII).
    - `cd app && npx tsc -b` sin errores nuevos.
  </acceptance_criteria>
  <done>Guard CI verde que falla ante grant-to-anon futuro, .auth. en el chokepoint, o select PII.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir docs/RUNBOOK-lockdown-cutover.md (orden + rollback + probe live)</name>
  <read_first>
    - 42-RESEARCH.md §4 (tabla runbook autoritativa: paso 0 secret/verify → 1 aplicar 0043 → 2 deploy 03 + smoke → 3 aplicar 0044; columna "If it fails" con rollback por paso; failure modes) y §3 LOCKDOWN-04 (probe live curl: RPC + tabla + vista + PII → 401/42501; smoke de superficies; excluir admin)
    - _FACTS-live-prod.md §"Cutover order" y §"App functional surface" (las superficies a smoke-testear)
    - docs/deploy-cloudflare.md (estilo de runbook del repo + el build OpenNext Linux/Docker)
  </read_first>
  <action>
    Crear docs/RUNBOOK-lockdown-cutover.md con: (1) Resumen del cambio (anon → web_reader; API pública muerta; PII intacto). (2) PRE-CUTOVER paso 0: obtener SUPABASE_JWT_SECRET (Dashboard → Settings → API → JWT Secret; NO SUPABASE_SECRET_KEY), añadir a .env + Cloudflare, verificar offline que re-firma con firma que matchea la anon key; abortar si no matchea. (3) Tabla de cutover ORDENADO (de RESEARCH §4): Paso 1 aplicar 0043 (psql --single-transaction + schema_migrations; pgTAP 0043 verde; ventana segura ambos roles leen) → Paso 2 deploy 03 a Cloudflare (build OpenNext Linux/Docker; smoke-test del sitio; admin excluida) → Paso 3 aplicar 0044 ÚLTIMO (guard aborta si web_reader no existe; pgTAP post-apply verde). Marcar claramente "revocar antes del paso 2 = SITIO CAÍDO". (4) PROBE LIVE post-0044 (operador): comandos curl con la anon key como apikey y bearer contra (a) `POST /rest/v1/rpc/parlamentario_publico`, (b) `GET /rest/v1/proyecto?select=*`, (c) una vista, (d) una tabla PII → esperar 401/42501/permission-denied en TODOS. Luego cargar el sitio y confirmar render de: votaciones, lobby, patrimonio, dinero, NET, cruces, búsqueda, agenda, parlamentarios, proyecto. (5) ROLLBACK por paso: paso1 = drop role web_reader (tras drop de sus 26 _wr); paso2 = redeploy build anterior (anon sigue → sitio arriba); paso3 = reverse-0044 (recrear 26 policies _public_read to anon + re-grant select/execute + reverse alter default privileges) — el más alto leverage; mantener el script listo. (6) Nota de exclusión: la superficie admin (createAdminSupabase, mis-wired SUPABASE_SERVICE_KEY vs SUPABASE_SECRET_KEY, gated OFF) está FUERA del smoke para evitar falsa alarma. (7) Sección "Riesgo residual + mantenimiento": el guard CI estático NO cubre re-grants a anon a nivel catálogo vía el default-ACL de `supabase_admin` (VALIDATION B2) — el backstop es re-correr `supabase/tests/post-apply/0044_revoke_anon.test.sql` contra PROD periódicamente (p.ej. tras cada migración Supabase-managed o trimestralmente); documentar esta cadencia. EOL = LF.
  </action>
  <acceptance_criteria>
    - El archivo existe en `docs/RUNBOOK-lockdown-cutover.md`.
    - Contiene los 3 pasos del cutover en orden (0043 → deploy 03 → 0044) con la advertencia explícita "SITIO CAÍDO" si se invierte.
    - Contiene la sección PROBE LIVE con curl contra RPC + tabla + vista + tabla PII esperando permission-denied/401/42501.
    - Contiene la lista de superficies a smoke-testear (votaciones, lobby, patrimonio, dinero, NET, cruces, búsqueda, agenda, parlamentarios, proyecto) y la exclusión explícita del admin.
    - Contiene `reverse-0044` como rollback del paso 3 y rollback por paso 1 y 2.
    - Contiene una sección "Riesgo residual" que reconoce que el guard estático NO cubre el default-ACL de `supabase_admin` y fija la re-corrida periódica del pgTAP post-apply 0044 como backstop.
  </acceptance_criteria>
  <done>Runbook ordenado completo con probe live, checklist de superficies, rollback por paso y exclusión admin.</done>
</task>

</tasks>

<verification>
- `cd app && npx vitest run` y `npx tsc -b` verdes (guard incluido).
- El guard falla en una prueba mental: una migración 0045 con `grant select to anon` → rojo.
- El runbook cubre orden, probe live (incl vista+PII), todas las superficies y rollback por paso.
</verification>

<success_criteria>
LOCKDOWN-04: guard CI anti-regresión (falla ante nuevo grant-to-anon o select PII en el server) + runbook de cutover ordenado con rollback documentado + probe live especificado (anon key → permission denied en RPC/tabla/vista/PII; sitio renderiza todas las superficies).
</success_criteria>

<output>
Create `.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-04-SUMMARY.md` when done
</output>
