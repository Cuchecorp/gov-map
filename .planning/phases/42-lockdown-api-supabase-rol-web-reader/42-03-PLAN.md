---
phase: 42-lockdown-api-supabase-rol-web-reader
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/web-reader-jwt.ts
  - app/lib/web-reader-jwt.test.ts
  - app/lib/supabase.ts
  - .env.example
autonomous: false
requirements: [LOCKDOWN-03]
user_setup:
  - service: supabase-prod
    why: "El JWT secret del proyecto (HS256 legacy) NO está en .env hoy; el server lo necesita para firmar el token web_reader. Sin él, accessToken hace throw (fail-closed)."
    env_vars:
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard → Settings → API → JWT Settings → JWT Secret (NO es SUPABASE_SECRET_KEY/sb_secret_, esa es la service key)"
    dashboard_config:
      - task: "Añadir SUPABASE_JWT_SECRET a .env (local) y a Cloudflare Pages env (prod); verificar offline que re-firma un token con firma que matchea el esquema de la anon key viva"
        location: "Cloudflare Pages → Settings → Environment variables"
      - task: "Deployar LOCKDOWN-03 a Cloudflare (build OpenNext en Linux/Docker) — PASO 2 del cutover, ANTES de aplicar 0044"
        location: "docs/deploy-cloudflare.md"

must_haves:
  truths:
    - "createServerSupabase lee como web_reader: apikey=anon key (pasa Kong) + Authorization bearer = JWT firmado con role:web_reader"
    - "mintWebReaderToken hace throw si SUPABASE_JWT_SECRET falta — NUNCA retorna null/anon (fail-closed)"
    - "createServerSupabase mantiene import server-only y firma/exports sin cambios → callers intactos"
    - "El token decodifica a {iss:supabase, ref, role:web_reader}, exp en el futuro, firma HS256 verificable con el secret"
  artifacts:
    - path: "app/lib/web-reader-jwt.ts"
      provides: "mintWebReaderToken(): Promise<string> — HS256 via node:crypto, TTL ~5min, caché in-proc, fail-closed"
      contains: "SUPABASE_JWT_SECRET"
    - path: "app/lib/web-reader-jwt.test.ts"
      provides: "vitest sin red: role correcto, exp futuro, roundtrip firma, throw si falta secret"
      contains: "web_reader"
    - path: "app/lib/supabase.ts"
      provides: "createServerSupabase con opción accessToken llamando mintWebReaderToken"
      contains: "accessToken"
  key_links:
    - from: "app/lib/supabase.ts"
      to: "app/lib/web-reader-jwt.ts"
      via: "accessToken: async () => mintWebReaderToken()"
      pattern: "accessToken"
    - from: "app/lib/web-reader-jwt.ts"
      to: "process.env.SUPABASE_JWT_SECRET"
      via: "throw si ausente"
      pattern: "SUPABASE_JWT_SECRET"
---

<objective>
Cambiar el chokepoint de lectura `createServerSupabase` (app/lib/supabase.ts) para que el servidor lea PROD como `web_reader`: nuevo módulo `web-reader-jwt.ts` que minta un JWT HS256 `role:web_reader` con `node:crypto` (cero deps nuevas, fail-closed), inyectado vía la opción `accessToken` de supabase-js, manteniendo la anon key como apikey (Kong) y `import "server-only"`. Más sus tests vitest. Es el PASO 2 del cutover (se DEPLOYA a Cloudflare ANTES del revoke 0044).

Purpose: hoy server y público usan el mismo rol anon; sin migrar el server a web_reader, revocar anon (0044) tira el sitio. El minteo debe ser fail-closed (throw, nunca anon silencioso) para que un secret faltante falle en el deploy (paso 2) — antes del revoke — y no después.

Output: `app/lib/web-reader-jwt.ts`, `app/lib/web-reader-jwt.test.ts`, `app/lib/supabase.ts` modificado, nota de `SUPABASE_JWT_SECRET` en `.env.example`. Deploy a Cloudflare = checkpoint operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-RESEARCH.md
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/_FACTS-live-prod.md
@CLAUDE.md
@app/AGENTS.md

# El archivo a modificar + referencia de credencial alterna server-only
@app/lib/supabase.ts
@app/lib/supabase-admin.ts

# Drafts de partida — CORREGIR: el draft de mint es SÍNCRONO; accessToken exige Promise<string> (async)
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/web-reader-jwt.ts
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/web-reader-jwt.test.ts
@.planning/phases/42-lockdown-api-supabase-rol-web-reader/drafts/supabase.ts

# Estilo de test (vitest, env inyectado, sin red)
@app/lib/cruces-gate.test.ts

<interfaces>
<!-- supabase-js 2.108.2 (instalado): createClient acepta accessToken?: () => Promise<string|null>.
     Con accessToken seteado, apikey sigue siendo la anon key (createClient arg) y el bearer es el callback.
     client.auth.* se vuelve inutilizable — la app tiene CERO uso de .auth. → seguro.
     RESEARCH §2 manda: el callback DEBE throw si falta el secret (nunca return null). -->
createClient(url, anonKey, {
  accessToken: async () => mintWebReaderToken(),
  auth: { persistSession: false, autoRefreshToken: false },
})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear app/lib/web-reader-jwt.ts (HS256, async, fail-closed) + su test</name>
  <read_first>
    - drafts/web-reader-jwt.ts (estructura completa: b64url, hs256, mintToken, extractRef, caché _cache; CORREGIR: la firma exportada debe ser `async function mintWebReaderToken(): Promise<string>` para encajar en `accessToken`)
    - drafts/web-reader-jwt.test.ts (11 tests ya redactados; ajustar a la firma async — `await mintWebReaderToken()`)
    - 42-RESEARCH.md §2 (mecanismo accessToken, payload {iss,ref,role,iat,exp}, TTL ~5min, caché re-minta ~60s antes de exp, FAIL-CLOSED throw si falta secret) y §3 LOCKDOWN-03
    - _FACTS-live-prod.md §"JWT / credential reality" (ref=bctyygbmqcvizyplktuw; firmar con el legacy JWT secret; SUPABASE_SECRET_KEY NO sirve)
    - app/lib/cruces-gate.test.ts (estilo vitest: describe/it, env inyectado por process.env, restaurado)
  </read_first>
  <behavior>
    - Test: mintWebReaderToken() retorna JWT de 3 partes; header alg=HS256 typ=JWT.
    - Test: payload.role === 'web_reader'; payload.iss === 'supabase'; payload.ref derivado de SUPABASE_URL (bctyygbmqcvizyplktuw).
    - Test: payload.exp en el futuro; exp-iat ≈ 300s (±2).
    - Test: firma HS256 verificable con el mismo secret (roundtrip); NO verificable con otro secret.
    - Test: si SUPABASE_JWT_SECRET ausente → la promesa rechaza/throw con mensaje conteniendo "SUPABASE_JWT_SECRET" (fail-closed; nunca retorna null ni un token anon).
    - Test: dos llamadas consecutivas retornan el mismo token (caché in-proc).
  </behavior>
  <action>
    Crear app/lib/web-reader-jwt.ts portando el draft pero exportando `export async function mintWebReaderToken(): Promise<string>` (async para encajar en el callback `accessToken: () => Promise<string|null>`). Implementación: import { createHmac } from "node:crypto"; header/payload b64url sin padding; payload {iss:'supabase', ref, role:'web_reader', iat:now, exp:now+300}; firma HMAC-SHA256 base64url sobre `header.payload`. ref derivado de process.env.SUPABASE_URL (regex `https?://([^.]+).supabase.co`, fallback "local"). Caché de proceso `_cache:{token,exp}` que re-minta cuando faltan <60s para exp. Al inicio de la función: leer process.env.SUPABASE_JWT_SECRET y si falta hacer `throw new Error("...SUPABASE_JWT_SECRET...")` (fail-closed; mensaje que aclara que NO es SUPABASE_SECRET_KEY). NO añadir dependencias (sin jose). Marcar el módulo server-only si el patrón del repo lo usa para libs de credenciales (revisar supabase.ts: usa `import "server-only"`; añadirlo aquí también para que el secret nunca entre al bundle).
    Crear app/lib/web-reader-jwt.test.ts portando el draft, ajustando todos los llamados a `await mintWebReaderToken()` y el test de throw a `await expect(fresh()).rejects.toThrow("SUPABASE_JWT_SECRET")` (función async). Usar `vi.resetModules()` para el test de secret ausente (limpiar caché). EOL = LF.
  </action>
  <acceptance_criteria>
    - `app/lib/web-reader-jwt.ts` exporta `mintWebReaderToken` con tipo de retorno `Promise<string>` (async).
    - El módulo lee `process.env.SUPABASE_JWT_SECRET` y hace throw si falta; NO referencia `SUPABASE_SECRET_KEY` ni `SUPABASE_ANON_KEY` para firmar.
    - El archivo NO importa ninguna lib externa de JWT (solo `node:crypto`); grep no encuentra `jose` ni `jsonwebtoken`.
    - `cd app && npx vitest run lib/web-reader-jwt.test.ts` → todos los tests verdes (incluido role=web_reader, exp futuro, roundtrip, throw fail-closed).
    - `cd app && npx tsc -b` sin errores nuevos.
  </acceptance_criteria>
  <done>Módulo de minteo async, HS256, fail-closed, sin deps nuevas, con tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Modificar createServerSupabase para leer como web_reader (accessToken)</name>
  <read_first>
    - app/lib/supabase.ts (el archivo actual — mantener import "server-only", SUPABASE_URL + SUPABASE_ANON_KEY, la guarda de env vars, la firma export `createServerSupabase(): SupabaseClient`)
    - drafts/supabase.ts (referencia de cómo el draft inyecta accessToken)
    - 42-RESEARCH.md §3 LOCKDOWN-03 (apikey=anon key se queda; añadir accessToken; firma/exports sin cambios → callers untouched) y §5 (no tocar el admin client; está fuera de alcance)
  </read_first>
  <action>
    Editar app/lib/supabase.ts: mantener `import "server-only"`, la lectura de SUPABASE_URL y SUPABASE_ANON_KEY y su guarda de error (la anon key sigue siendo necesaria como apikey para pasar Kong). Importar `mintWebReaderToken` de "./web-reader-jwt". En el `createClient(url, anonKey, {...})`, añadir la opción `accessToken: async () => mintWebReaderToken()` y `auth: { persistSession: false, autoRefreshToken: false }`. NO cambiar el nombre ni la firma de `createServerSupabase` ni sus exports (los Server Components que la llaman quedan intactos). Actualizar el comentario de cabecera del archivo para reflejar que ahora lee como web_reader (no anon) y que la anon key es solo apikey. NO tocar app/lib/supabase-admin.ts. EOL = LF.
    Añadir a `.env.example` (crear si no existe, o editar) una línea `SUPABASE_JWT_SECRET=` con comentario: "JWT secret del proyecto (Dashboard → Settings → API → JWT Secret) para firmar el token web_reader; NO es SUPABASE_SECRET_KEY".
  </action>
  <acceptance_criteria>
    - `app/lib/supabase.ts` contiene `accessToken` y `import` de `./web-reader-jwt`; conserva `import "server-only"` en la línea 1.
    - La firma `export function createServerSupabase(): SupabaseClient` no cambió (grep la encuentra idéntica).
    - El archivo sigue usando `SUPABASE_ANON_KEY` como segundo argumento de createClient (apikey).
    - `.env.example` contiene `SUPABASE_JWT_SECRET`.
    - `cd app && npx tsc -b` y `cd app && npx vitest run` verdes (callers existentes no rompen).
  </acceptance_criteria>
  <done>El chokepoint de lectura usa accessToken→web_reader, server-only intacto, callers sin cambios, env documentada.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [CHECKPOINT OPERADOR] Añadir SUPABASE_JWT_SECRET + deployar a Cloudflare — PASO 2 del cutover</name>
  <what-built>Server-only mint HS256 fail-closed + createServerSupabase leyendo como web_reader. El agente NO deploya (creds Cloudflare no están en .env, gate 4).</what-built>
  <how-to-verify>
    PRE-REQUISITO: 42-01 (0043) ya aplicada a PROD (web_reader existe). Si no, ABORTAR — el orden es load-bearing.
    1. Obtener el JWT secret: Dashboard → Settings → API → JWT Settings → JWT Secret. NO usar SUPABASE_SECRET_KEY.
    2. Verificar offline: firmar un token de prueba con ese secret y confirmar que el esquema de firma matchea el de la anon key viva (prueba que el proyecto no es asimétrico-only). Si no matchea, ABORTAR.
    3. Añadir SUPABASE_JWT_SECRET a .env (local) y a Cloudflare Pages env (prod).
    4. Build OpenNext en Linux/Docker y deployar (docs/deploy-cloudflare.md). 
    5. Smoke-test del sitio LIVE (anon AÚN funciona, así que el sitio debe seguir arriba leyendo ya como web_reader): /parlamentarios, /agenda, búsqueda, una ficha de proyecto, votaciones, lobby, patrimonio, dinero, NET, cruces renderizan. EXCLUIR la superficie admin (mis-wired y gated OFF, fuera de alcance).
    Si el secret es incorrecto/falta en CF, el accessToken fail-closed hace throw → el sitio errorea AQUÍ (antes del revoke) → se captura temprano, anon sigue vivo, se corrige el secret.
    Rollback: redeploy del build anterior (anon sigue funcionando → sitio arriba).
  </how-to-verify>
  <resume-signal>Escribe "03 deployado y sitio verde" cuando el smoke-test pase, o describe el fallo.</resume-signal>
</task>

</tasks>

<verification>
- mintWebReaderToken es async, fail-closed, sin deps nuevas; tests verdes.
- createServerSupabase lee como web_reader manteniendo server-only y firma; callers intactos; tsc+vitest verdes.
- Deploy a Cloudflare con el secret + smoke-test de todas las superficies (admin excluida) = checkpoint operador.
</verification>

<success_criteria>
LOCKDOWN-03: createServerSupabase se autentica como web_reader (JWT role:web_reader firmado con el JWT secret, apikey=anon para Kong), server-only intacto, fail-closed; tests del cliente verdes; deployado a Cloudflare ANTES del revoke (gate de cutover).
</success_criteria>

<output>
Create `.planning/phases/42-lockdown-api-supabase-rol-web-reader/42-03-SUMMARY.md` when done
</output>
