---
phase: 50-fix-quick-wins-diagnostico-p1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/page.tsx
  - app/app/page.test.tsx
  - app/components/votacion-card.tsx
  - app/components/votacion-card.test.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
autonomous: true
requirements: [B1, B14, HS-rep]
must_haves:
  truths:
    - "El pill del hero del home apunta a 14309-04 (boletín existente en PROD) y navega a /buscar?q=14309-04"
    - "Una votación sin resultado muestra 'Desenlace no informado por la fuente.' (ya no omite silenciosamente la frase)"
    - "'De qué trata: no disponible aún' aparece a lo más UNA vez por sección de votos, no por cada arco de proyecto"
  artifacts:
    - path: "app/app/page.tsx"
      provides: "EXAMPLE_CHIPS con boletín real 14309-04"
      contains: "14309-04"
    - path: "app/components/votacion-card.tsx"
      provides: "línea honest-state para resultado null"
      contains: "Desenlace no informado por la fuente."
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "nota una-vez-por-sección para arcos sin idea_matriz; sin línea per-arco 'no disponible aún'"
  key_links:
    - from: "app/app/page.tsx"
      to: "/buscar?q=14309-04"
      via: "pill click → router.push"
      pattern: "14309-04"
    - from: "app/components/votos-por-parlamentario.tsx VotosView"
      to: "ProyectoGrupo"
      via: "hayArcoSinIdea computado en VotosView, nota de sección"
      pattern: "agruparPorProyecto"
---

<objective>
Cerrar tres fixes independientes de honestidad de superficie que no comparten archivos con ningún otro plan de wave 1: el pill roto del home (B1), la votación sin desenlace (B14, revierte decisión Phase 22) y el honest-state repetido en la sección de votos (nota una-vez-por-sección). Cada uno es cambio de copy/estructura de presentación; cero DB, cero RPC.

Purpose: Restaurar la primera impresión del home y la paridad de honest-states entre cards de Cámara/Senado, y quitar el ruido de repetir "no disponible aún" por cada arco.
Output: `page.tsx` con pill 14309-04; `votacion-card.tsx` con línea honest-state; `votos-por-parlamentario.tsx` con nota una-vez-por-sección; sus tests actualizados/migrados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-RESEARCH.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-PATTERNS.md
@app/AGENTS.md

<interfaces>
Sitios exactos (50-PATTERNS.md):
- B1: `app/app/page.tsx:23-28` array `EXAMPLE_CHIPS`, línea 27 `{ query: "15234-07", mono: true }`. Tests: `page.test.tsx:86` (`getByRole("button", { name: "15234-07" })`), `:111-112` (push `"/buscar?q=15234-07"`).
- B14: `app/components/votacion-card.tsx:65-82` bloque `{votacion.resultado && (…)}`. Barra/totales `:52-63` siempre visibles (intactos). `EtapaBadge` solo con resultado. Test que rompe: `votacion-card.test.tsx:51-66`, assertion `:62` `queryByText(/El proyecto fue/).not.toBeInTheDocument()`.
- honest-state: sitio LIVE `votos-por-parlamentario.tsx:225-229` (`ProyectoGrupo`, línea per-arco). Render `VotosView:422-428` (`agruparPorProyecto(votos).map`). Modelo de nota-de-sección YA existente justo debajo: `:430-437` (bloque "cobertura baja"). Test: `votos-por-parlamentario.test.tsx:210-219` (hoy testea `VotoFichaRow`, dead code).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B1 — pill del home 15234-07 → 14309-04 + test</name>
  <files>app/app/page.tsx, app/app/page.test.tsx</files>
  <read_first>
    - app/app/page.tsx (líneas 20-35: array EXAMPLE_CHIPS)
    - app/app/page.test.tsx (líneas 80-115: assertions del pill + navegación)
    - 50-PATTERNS.md §B1 (excerpt exacto)
    - 50-RESEARCH.md §B1 (14309-04 verificado en PROD; NO tocar search-result-card.test.tsx)
  </read_first>
  <action>
    En `app/app/page.tsx` cambiar el string del pill mono de `"15234-07"` a `"14309-04"` (única edición de la arquitectura; pills LOCKED UI-SPEC §6, es cambio de valor, NO construir validación dinámica contra DB). Actualizar `page.test.tsx`: línea 86 `getByRole("button", { name: "14309-04" })` y líneas 111-112 el push esperado a `"/buscar?q=14309-04"`. NO tocar `app/components/search-result-card.test.tsx` (usa 15234-07 como fixture arbitrario, no como el pill).
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/page.tsx` contiene `14309-04` y NO contiene `15234-07`.
    - `app/app/page.test.tsx` asierta el nombre de botón `"14309-04"` y el push `"/buscar?q=14309-04"`; NO contiene `15234-07`.
    - `git diff app/components/search-result-card.test.tsx` vacío.
    - `pnpm --filter app test -- app/page.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Pill del home apunta a boletín real; test verde; fixture de search-result-card intacto.</done>
</task>

<task type="auto">
  <name>Task 2: B14 — votación sin resultado → "Desenlace no informado por la fuente." + test</name>
  <files>app/components/votacion-card.tsx, app/components/votacion-card.test.tsx</files>
  <read_first>
    - app/components/votacion-card.tsx (líneas 45-85: barra/totales + bloque resultado)
    - app/components/votacion-card.test.tsx (líneas 45-70: caso resultado null)
    - 50-PATTERNS.md §B14 (excerpt + estilo honest-state)
    - 50-PATTERNS.md §Shared Patterns "honest-state sobrio" (className text-sm text-muted-foreground; vocabulario prohibido negative-match)
  </read_first>
  <action>
    En `app/components/votacion-card.tsx:65-82` reemplazar el `{votacion.resultado && (…)}` por un ternario: rama con resultado = el bloque actual (frase "El proyecto fue …" + EtapaBadge, intacto); rama null = una línea honest-state `<p className="text-sm text-muted-foreground">Desenlace no informado por la fuente.</p>`. Los totales/barra (`:52-63`) quedan intactos (ya se muestran siempre). `EtapaBadge` solo en la rama con resultado. Copy exacto es-CL, sin vocabulario prohibido (sin causalidad/juicio/score). Reescribir el test `votacion-card.test.tsx:51-66`: renombrar el `it` a "resultado null → muestra 'Desenlace no informado por la fuente.'" y cambiar la assertion `:62` por `getByText("Desenlace no informado por la fuente.")`; CONSERVAR las assertions de que la barra (`getByLabelText(/Resultado de votación/i)`) y los totales persisten.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/votacion-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/votacion-card.tsx` contiene la cadena exacta `Desenlace no informado por la fuente.`
    - Con `resultado` null el componente renderiza esa línea (test lo asierta con `getByText`).
    - Con `resultado` null la barra y los totales siguen presentes (assertions conservadas).
    - El test ya NO asierta `queryByText(/El proyecto fue/).not.toBeInTheDocument()` como comportamiento correcto para null.
    - `pnpm --filter app test -- components/votacion-card.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Votación sin desenlace muestra honest-state explícito; paridad Cámara/Senado; test reescrito verde.</done>
</task>

<task type="auto">
  <name>Task 3: Honest-state repetido — nota una-vez-por-sección en VotosView + migrar test</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx (líneas 200-240: ProyectoGrupo; 420-440: VotosView render + bloque cobertura baja)
    - app/components/votos-por-parlamentario.test.tsx (líneas 200-220: caso honest-state sobre VotoFichaRow)
    - 50-PATTERNS.md §"Honest-state repetido" (sitio LIVE vs dead code; nota-de-sección modelo :430-437)
    - 50-RESEARCH.md §"Honest-state repetido" (Open Question #1 → recomendación (b): NO tocar voto-ficha-row.tsx)
  </read_first>
  <action>
    En `app/components/votos-por-parlamentario.tsx`: (1) en `ProyectoGrupo` (`:225-229`), cuando `grupo.idea_matriz` es null → OMITIR la línea per-arco (no renderizar "De qué trata: no disponible aún"); cuando SÍ hay idea, seguir mostrando "De qué trata: {extractoIdea(grupo.idea_matriz)}". (2) En `VotosView`, computar `hayArcoSinIdea = agruparPorProyecto(votos).some((g) => !g.idea_matriz)` y renderizar UNA nota de sección (espejando el bloque de cobertura `:430-437`, `className="text-sm text-muted-foreground mt-4"`) con copy honesto es-CL sobrio (a discreción dentro de DESIGN-SYSTEM §6, sin vocabulario prohibido; el honest-state va UNA vez). NO tocar `app/components/voto-ficha-row.tsx` (dead code, research A2 confirmó 0 imports live; será B24/Phase 51). Migrar el test `votos-por-parlamentario.test.tsx:210-219`: en vez de asertar el honest-state sobre `VotoFichaRow`, asertar sobre `VotosView` que la nota de sección aparece UNA sola vez cuando ≥1 arco no tiene idea, y que la línea per-arco "no disponible aún" NO se repite.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/votos-por-parlamentario.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ProyectoGrupo` ya NO renderiza el literal "De qué trata: no disponible aún" (omite la línea cuando idea es null).
    - `VotosView` computa `hayArcoSinIdea` (o equivalente) y renderiza una nota de sección honesta una sola vez.
    - `app/components/voto-ficha-row.tsx` sin cambios (git diff vacío).
    - El test migrado verifica una-vez-por-sección (no per-arco) sobre `VotosView`.
    - Copy nuevo sin vocabulario prohibido (negative-match del test de la suite pasa).
    - `pnpm --filter app test -- components/votos-por-parlamentario.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Honest-state de votos aparece una vez por sección; dead code intacto; test migrado a VotosView verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato ausente (resultado/idea null) → render | ausencia de dato debe leerse como HECHO honesto, nunca como silencio ni como ruido repetido |
| copy es-CL nuevo → DOM | vocabulario prohibido fabricaría juicio/causalidad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02-INT | Information (integrity) | votacion-card / votos honest-states | mitigate | Copy sobrio "…no informado por la fuente"; honest-state UNA vez por sección; negative-match de vocabulario prohibido en tests. |
| T-50-02-CAMA | Tampering | Camino A (lockdown) | accept | Solo `app/**` presentación; cero RPC/`.from()`/DDL. Superficie lockdown-guard sin cambios. |
| T-50-02-SC | Tampering | npm installs | accept | Sin instalación de packages (50-RESEARCH §Audit N/A). |
</threat_model>

<verification>
- `pnpm --filter app test -- app/page.test.tsx components/votacion-card.test.tsx components/votos-por-parlamentario.test.tsx` verde.
- `pnpm --filter app exec tsc -b` limpio.
- `git diff app/components/voto-ficha-row.tsx app/components/search-result-card.test.tsx` vacío.
</verification>

<success_criteria>
Pill del home = 14309-04 (B1); votación sin resultado muestra "Desenlace no informado por la fuente." con barra/totales intactos (B14); "no disponible aún" aparece a lo más una vez por sección (honest-state); dead code y fixtures no relacionados intactos; tests verdes; cero vocabulario prohibido.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quick-wins-diagnostico-p1/50-02-SUMMARY.md` when done
</output>
