---
phase: 50-fix-quick-wins-diagnostico-p1
plan: 05
type: execute
wave: 2
depends_on: [50-01, 50-03]
files_modified:
  - app/app/agenda/page.tsx
  - app/components/patrimonio-de-parlamentario.tsx
  - app/components/patrimonio-de-parlamentario.test.tsx
autonomous: true
requirements: [B7, B12, B17]
must_haves:
  truths:
    - "/agenda deja de tragar errores de DB: un .error de Supabase se lanza (throw) y nunca se renderiza como 'No hay citaciones esta semana'"
    - "Las fechas de headers de agenda muestran solo la primera letra en mayúscula ('Jueves, 2 de julio'), no 'Jueves, 2 De Julio'"
    - "VersionRow (patrimonio) muestra 'fecha no informada' ante fecha_presentacion null/vacía/no-ISO en vez de 'Invalid Date'"
  artifacts:
    - path: "app/app/agenda/page.tsx"
      provides: "CitacionesSection + SalaTableServer chequean .error y throw (#34); capitalizarPrimera reemplaza className capitalize"
      contains: "throw new Error"
    - path: "app/components/patrimonio-de-parlamentario.tsx"
      provides: "fechaCortaSegura en :383 y :607 (guard de fecha)"
      contains: "fechaCortaSegura"
  key_links:
    - from: "app/app/agenda/page.tsx"
      to: "app/app/agenda/error.tsx"
      via: "throw en secciones server capturado por el boundary (creado en Plan 50-03)"
      pattern: "throw new Error"
    - from: "app/components/patrimonio-de-parlamentario.tsx"
      to: "fechaCortaSegura"
      via: "import de lib/format (helper del Plan 50-01)"
      pattern: "fechaCortaSegura\\(.*fecha_presentacion"
---

<objective>
Aplicar en componentes/páginas los fixes que dependen de wave 1: agenda deja de violar la doctrina #34 (B7, throw-on-error, capturado por el `agenda/error.tsx` del Plan 50-03), el locale se capitaliza correctamente vía `capitalizarPrimera` (B12, helper del Plan 50-01) y `VersionRow` guarda la fecha con `fechaCortaSegura` (B17, helper del Plan 50-01). Es la ÚNICA plan de wave 2.

Purpose: Cerrar la integridad informativa (un fallo de red nunca se lee como "sin datos") y la anti-fabricación de fecha, consumiendo los contratos ya fijados.
Output: `agenda/page.tsx` con throw en las 2 secciones + capitalizarPrimera en 2 sitios; `patrimonio-de-parlamentario.tsx` con fechaCortaSegura en `:383` y `:607`; test de patrimonio con caso fecha null.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-RESEARCH.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-PATTERNS.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-01-SUMMARY.md
@app/AGENTS.md

<interfaces>
Helpers disponibles (creados en Plan 50-01, `app/lib/format.ts`):
- `capitalizarPrimera(s: string): string` — primera letra en mayúscula, conserva el resto.
- `fechaCortaSegura(raw: string | null, fallback = "fecha no informada"): string` — valida ISO antes de `new Date`; fallback honesto.

Sitios exactos (50-PATTERNS.md):
- B7 sitio 1: `agenda/page.tsx:276-284` `CitacionesSection` — `const { data } = await sb.from("citacion").select(...)` NO desestructura `error`. Fix: `const { data, error } = ...` + `if (error) throw new Error(\`citacion falló para semana ${key}: ${error.message}\`)` antes del `?? []`.
- B7 sitio 2: `agenda/page.tsx:404-439` `SalaTableServer` — 3 queries (senadoRes, camaraRes en Promise.all `:404-419`, + probe forward-only `:429-435`). Chequear `.error` de las TRES; cada una throw. Un fallo del probe NO debe fabricar `fueraDeVentanaSenado`.
- Patrón #34 a copiar: `lobby-de-parlamentario.tsx:303-312` / `votos-por-parlamentario.tsx:651` (`if (error) throw new Error(...)`). En el MISMO archivo `ResultadosBusqueda:184-193` ya usa try/catch → UI error.
- B12: `agenda/page.tsx:242` `<span className="capitalize">{diaFmt.format(new Date(c.fecha))}</span>` y `:315` `<h3 className="text-base font-semibold capitalize">`. Los formatters `:215`/`:303` ya producen minúscula es-CL correcta ("jueves, 2 de julio"). Quitar `capitalize`, envolver output con `capitalizarPrimera`. Conservar la coma del locale (fix mínimo).
- B17: `patrimonio-de-parlamentario.tsx:383` `const fechaTexto = fechaCorta(new Date(version.fecha_presentacion))` (reusado en :391/:408) y `:607` `Presentada el {fechaCorta(new Date(c.fecha_presentacion))}`. Guard WR-03 modelo en `:130-137`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B7 — throw-on-error en CitacionesSection y SalaTableServer (#34)</name>
  <files>app/app/agenda/page.tsx</files>
  <read_first>
    - app/app/agenda/page.tsx (líneas 180-200: ResultadosBusqueda patrón try/catch; 276-284: CitacionesSection; 404-439: SalaTableServer + probe)
    - app/components/lobby-de-parlamentario.tsx (líneas 303-312: patrón #34 a copiar)
    - app/app/agenda/error.tsx (creado en Plan 50-03 — el boundary que captura el throw)
    - 50-PATTERNS.md §B7 (sitios exactos + 3 queries)
    - 50-RESEARCH.md §B7
  </read_first>
  <action>
    En `CitacionesSection` (`:276-284`) desestructurar `error` de la respuesta Supabase y, antes del `?? []`, `if (error) throw new Error(\`citacion falló para semana ${key}: ${error.message}\`)`. En `SalaTableServer` (`:404-439`) chequear `.error` de las TRES queries (`senadoRes`, `camaraRes`, y el probe forward-only de `:429-435`), cada una con su propio `throw new Error(\`<recurso> falló: ${error.message}\`)`; un fallo del probe NO debe degradarse a `fueraDeVentanaSenado` fabricado. Copiar el patrón #34 verbatim de `lobby-de-parlamentario.tsx:303-312`. NUNCA renderizar "No hay citaciones esta semana" ante un `.error`. El throw lo captura `agenda/error.tsx` (Plan 50-03).
  </action>
  <verify>
    <automated>cd app && pnpm --filter app exec tsc -b && pnpm --filter app test -- lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CitacionesSection` desestructura `error` y hace `throw new Error(...)` cuando `error` es truthy, antes de construir el array de citaciones.
    - `SalaTableServer` chequea `.error` de las 3 queries (senado, cámara, probe forward-only) y lanza en cada caso; el probe con error NO produce `fueraDeVentanaSenado`.
    - El literal "No hay citaciones" solo se renderiza cuando la query fue exitosa y devolvió cero filas (nunca ante `.error`).
    - `tsc -b` limpio y `lockdown-guard.test.ts` verde.
  </acceptance_criteria>
  <done>Agenda cumple #34: error de DB → throw (capturado por error.tsx), nunca "No hay citaciones" fabricado.</done>
</task>

<task type="auto">
  <name>Task 2: B12 — capitalizarPrimera reemplaza className capitalize en agenda</name>
  <files>app/app/agenda/page.tsx</files>
  <read_first>
    - app/app/agenda/page.tsx (líneas 210-245: ResultadosBusqueda formatter + span :242; 300-320: CitacionesSection h3 :315)
    - app/lib/format.ts (capitalizarPrimera — creado en Plan 50-01)
    - 50-PATTERNS.md §B12 (causa raíz + fix)
  </read_first>
  <action>
    Importar `capitalizarPrimera` de `@/lib/format`. En `agenda/page.tsx:242` quitar `className="capitalize"` del `<span>` y envolver `diaFmt.format(new Date(c.fecha))` con `capitalizarPrimera(...)`. En `:315` quitar `capitalize` del `className` del `<h3>` y envolver el output del formatter (rama que NO es "Sin fecha asignada") con `capitalizarPrimera(...)`. Conservar la coma que produce el locale es-CL (fix mínimo = solo capitalización; NO normalizar el formato). Resultado: "Jueves, 2 de julio".
  </action>
  <verify>
    <automated>cd app && pnpm --filter app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/agenda/page.tsx` ya NO contiene `className="capitalize"` (ni `capitalize` en los className de esos 2 sitios) y usa `capitalizarPrimera` sobre el output de los formatters.
    - El helper se importa desde `@/lib/format`.
    - `tsc -b` limpio.
  </acceptance_criteria>
  <done>Fechas de agenda con solo la primera letra en mayúscula; sin "De Julio".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: B17 — fechaCortaSegura en VersionRow + caso de test</name>
  <files>app/components/patrimonio-de-parlamentario.tsx, app/components/patrimonio-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/patrimonio-de-parlamentario.tsx (líneas 125-140: guard WR-03; 380-410: VersionRow fechaTexto; 600-610: vista ?ver= detalle)
    - app/components/patrimonio-de-parlamentario.test.tsx (fixtures existentes con fecha_presentacion)
    - app/lib/format.ts (fechaCortaSegura — creado en Plan 50-01)
    - 50-PATTERNS.md §B17
  </read_first>
  <behavior>
    - VersionRow con fecha_presentacion null → renderiza "fecha no informada" (nunca "Invalid Date")
    - VersionRow con fecha_presentacion ISO válida → renderiza la fecha corta como antes
  </behavior>
  <action>
    Importar `fechaCortaSegura` de `@/lib/format`. Reemplazar en `:383` `fechaCorta(new Date(version.fecha_presentacion))` por `fechaCortaSegura(version.fecha_presentacion)` (la rama `es_historica` de `:408` que reusa `fechaTexto` hereda el fallback — aceptable). Reemplazar en `:607` `fechaCorta(new Date(c.fecha_presentacion))` por `fechaCortaSegura(c.fecha_presentacion)`. Agregar en `patrimonio-de-parlamentario.test.tsx` un caso con `fecha_presentacion` null → asertar "fecha no informada" y ausencia de "Invalid Date". Conservar el caso con fecha válida.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/patrimonio-de-parlamentario.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `patrimonio-de-parlamentario.tsx` usa `fechaCortaSegura(version.fecha_presentacion)` en `:383` y `fechaCortaSegura(c.fecha_presentacion)` en `:607`; ya no hay `new Date(version.fecha_presentacion)` sin guard.
    - El test cubre fecha_presentacion null → "fecha no informada" y confirma que el DOM NO contiene "Invalid Date".
    - `pnpm --filter app test -- components/patrimonio-de-parlamentario.test.tsx` exits 0.
  </acceptance_criteria>
  <done>VersionRow guarda la fecha; null/no-ISO degrada a "fecha no informada"; test cubre el caso; sin "Invalid Date".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta Supabase (.error) → render (agenda) | un fallo de red renderizado como "No hay citaciones" fabrica la ausencia de un hecho (integridad informativa) — el bug de mayor severidad de la fase |
| fecha_presentacion nullable → new Date() | `new Date(null)` → Invalid Date renderizado (disponibilidad/legibilidad) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05-INT | Information (integrity) | agenda CitacionesSection/SalaTableServer | mitigate | `.error` → `throw new Error(...)` (#34) capturado por `agenda/error.tsx`; NUNCA degradar a "No hay citaciones". Cubre las 3 queries incl. el probe forward-only. |
| T-50-05-AV | Denial of Service | patrimonio VersionRow | mitigate | `fechaCortaSegura` valida ISO antes de `new Date`; degrada a "fecha no informada". Test cubre null. |
| T-50-05-CAMA | Tampering | Camino A (lockdown) | mitigate | No agrega RPC ni `.from()` PII; solo endurece el manejo de error de lecturas ya existentes. `lockdown-guard.test.ts` corre en el verify. |
| T-50-05-SC | Tampering | npm installs | accept | Sin instalación de packages. |
</threat_model>

<verification>
- `pnpm --filter app test` (suite completa; debe quedar ≥377 verde, cero regresión) + `pnpm --filter app exec tsc -b` limpio + `lockdown-guard.test.ts` verde.
- Grep: `agenda/page.tsx` sin `className="capitalize"`; con `throw new Error` en ambas secciones server.
- Grep: `patrimonio-de-parlamentario.tsx` sin `new Date(version.fecha_presentacion)`/`new Date(c.fecha_presentacion)` sin guard.
</verification>

<success_criteria>
Agenda cumple #34 (error → throw, nunca "No hay citaciones") (B7); fechas de agenda con capitalización correcta (B12); VersionRow guarda la fecha con fallback honesto (B17); suite completa ≥377 verde + tsc limpio + lockdown-guard verde; cero vocabulario prohibido.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quick-wins-diagnostico-p1/50-05-SUMMARY.md` when done
</output>
