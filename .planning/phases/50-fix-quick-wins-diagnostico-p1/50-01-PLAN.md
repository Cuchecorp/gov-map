---
phase: 50-fix-quick-wins-diagnostico-p1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/format.ts
  - app/lib/format.test.ts
  - app/components/provenance-badge.test.tsx
autonomous: true
requirements: [B6, B12, B17]
must_haves:
  truths:
    - "esStale devuelve false para un dato de 13 días y true para uno de 15 días (umbral por cadence, ya no 48 h)"
    - "El badge de ProvenanceBadge deja de estar en alarma ámbar permanente para datos con ingesta semanal"
    - "Existe un helper puro capitalizarPrimera que capitaliza solo la primera letra"
    - "Existe un helper puro fechaCortaSegura que degrada a copy honesto en fecha null/vacía/no-ISO en vez de producir 'Invalid Date'"
  artifacts:
    - path: "app/lib/format.ts"
      provides: "esStale con umbral 14 días (retro-compatible) + capitalizarPrimera + fechaCortaSegura"
      contains: "export function fechaCortaSegura"
    - path: "app/lib/format.test.ts"
      provides: "unit tests de esStale (≤14d false / >14d true), capitalizarPrimera, fechaCortaSegura"
    - path: "app/components/provenance-badge.test.tsx"
      provides: "fixture stale actualizado a >14 días"
  key_links:
    - from: "app/components/provenance-badge.tsx"
      to: "esStale"
      via: "import de lib/format (default 14d propaga sin tocar el call-site)"
      pattern: "esStale\\(capturedAt\\)"
---

<objective>
Crear/actualizar los tres helpers de lógica pura en `app/lib/format.ts` que el resto de la fase consume, y cerrar en verde sus tests unitarios y el test dependiente del umbral. Este plan es la capa de contratos (wave 1): B12 y B17 se APLICAN en componentes en el Plan 50-05 (wave 2) contra estos helpers; B6 propaga solo con cambiar el default (un único call-site).

Purpose: Fijar los contratos de helper antes de que los planes de aplicación los usen (interface-first) y absorber el blast-radius de tests del cambio de umbral (B6) en un commit atómico.
Output: `lib/format.ts` con `esStale` (umbral 14 días retro-compatible), `capitalizarPrimera`, `fechaCortaSegura`; tests unitarios nuevos + fixture de provenance-badge actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-RESEARCH.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-PATTERNS.md
@app/AGENTS.md

<interfaces>
Estado actual (verificado, 50-PATTERNS.md §B6):
- `app/lib/format.ts:6` — `const STALE_THRESHOLD_MS = 48 * 60 * 60 * 1000;`
- `app/lib/format.ts:52-58` — `export function esStale(capturedAt: Date, now: Date = new Date()): boolean { return now.getTime() - capturedAt.getTime() > STALE_THRESHOLD_MS; }`
- Único consumidor: `app/components/provenance-badge.tsx:33` — `const stale = capturedAt !== null && esStale(capturedAt);` (pasa solo `capturedAt`).
- Helpers puros hermanos a espejar en forma (exportados, docstring es-CL, sin efectos): `extractoIdea` (`:68`), `conteoVotacion` (`:83`), `fechaCorta`.
- Guard WR-03 a espejar para fechaCortaSegura: `app/components/patrimonio-de-parlamentario.tsx:130-137` (slice ISO + regex antes de `new Date`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B6 — esStale umbral 48h → 14 días (retro-compatible) + tests dependientes</name>
  <files>app/lib/format.ts, app/lib/format.test.ts, app/components/provenance-badge.test.tsx</files>
  <read_first>
    - app/lib/format.ts (líneas 1-60: constante y esStale + docstrings)
    - app/lib/format.test.ts (líneas 50-70: describe de esStale, casos 47h/49h)
    - app/components/provenance-badge.test.tsx (líneas 1-45: fixture stale 72h)
    - 50-PATTERNS.md §B6 (excerpt exacto + tests que rompen)
    - 50-RESEARCH.md §B6 (blast-radius: un solo call-site; default propaga)
  </read_first>
  <behavior>
    - esStale(hace 13 días) → false
    - esStale(hace 15 días) → true
    - esStale(hace 47h) → false (sigue verde)
    - Firma retro-compatible: el call-site provenance-badge.tsx:33 `esStale(capturedAt)` compila sin cambios
  </behavior>
  <action>
    Cambiar el umbral de `esStale` de 48 h fijas a 14 días por defecto, con firma retro-compatible: agregar un tercer parámetro opcional (p.ej. `staleAfterMs: number = 14 * 24 * 60 * 60 * 1000`) manteniendo `now: Date = new Date()` como segundo. NO tocar el único call-site (`provenance-badge.tsx:33`); el nuevo default propaga a los ~12 componentes que usan ProvenanceBadge. Renombrar/actualizar la constante y los docstrings de `:6` y `:53-55` (de "48 horas (UI-SPEC §4)" a "cadence de ingesta, ~14 días — ingesta semanal ⇒ 2× cadence, margen honesto"). Reescribir en `format.test.ts` el caso `">48h → true"` (usa 49h) a `≤14d → false` (13 días) y `>14d → true` (15 días); renombrar el `describe`/`it` acorde. En `provenance-badge.test.tsx` cambiar el fixture stale de 72h a `15 * 24 * 60 * 60 * 1000` (línea ~30). El caso fresco (<48h/3h) queda verde. NO tocar `provenance-badge.tsx` (fuente).
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- lib/format.test.ts components/provenance-badge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/format.ts` NO contiene `48 * 60 * 60 * 1000` como umbral de esStale; contiene `14 * 24 * 60 * 60 * 1000` (o equivalente 14-día).
    - `esStale` conserva la firma con primer parámetro `capturedAt: Date`; el nuevo parámetro de umbral es opcional (tiene default).
    - `app/components/provenance-badge.tsx` NO fue modificado (git diff vacío para ese archivo).
    - `pnpm --filter app test -- lib/format.test.ts components/provenance-badge.test.tsx` exits 0.
    - format.test.ts contiene un caso con 15 días esperando `true` y uno con 13 días esperando `false`.
  </acceptance_criteria>
  <done>esStale usa umbral 14 días retro-compatible; ambos tests dependientes verdes; provenance-badge.tsx intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B12/B17 helpers — capitalizarPrimera + fechaCortaSegura + tests unitarios</name>
  <files>app/lib/format.ts, app/lib/format.test.ts</files>
  <read_first>
    - app/lib/format.ts (helpers hermanos extractoIdea:68, conteoVotacion:83, fechaCorta — para espejar la forma)
    - app/components/patrimonio-de-parlamentario.tsx (líneas 130-137: guard WR-03 a espejar)
    - 50-PATTERNS.md §B12 y §B17 (firmas recomendadas)
    - 50-RESEARCH.md §B12, §B17
  </read_first>
  <behavior>
    - capitalizarPrimera("jueves, 2 de julio") → "Jueves, 2 de julio"
    - capitalizarPrimera("") → "" (sin crash)
    - fechaCortaSegura(null) → "fecha no informada"
    - fechaCortaSegura("") → "fecha no informada"
    - fechaCortaSegura("no-es-fecha") → "fecha no informada"
    - fechaCortaSegura("2024-03-15") → mismo output que fechaCorta(new Date("2024-03-15"))
  </behavior>
  <action>
    Agregar a `app/lib/format.ts` dos helpers puros exportados, con docstring es-CL espejando la forma de `extractoIdea`/`conteoVotacion`:
    (1) `capitalizarPrimera(s: string): string` que retorna `s.charAt(0).toUpperCase() + s.slice(1)` (solo la primera letra; conserva la coma del locale es-CL — NO normaliza el formato del resto). Docstring: por qué NO usar `capitalize` de Tailwind (CSS text-transform capitaliza cada palabra → "2 De Julio").
    (2) `fechaCortaSegura(raw: string | null, fallback = "fecha no informada"): string` que espeja el guard WR-03: hace `slice(0,10)` del string, valida con regex `^\d{4}-\d{2}-\d{2}$`, y si no parsea (o `new Date` da NaN) retorna `fallback`; si parsea, retorna `fechaCorta(d)`. Reutilizar `fechaCorta` existente para el caso válido (no duplicar el formateo). Este helper DEGRADA a copy honesto (a diferencia del guard del chart que EXCLUYE la fila). Agregar en `format.test.ts` los casos unitarios del bloque `<behavior>`.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- lib/format.test.ts && pnpm --filter app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/format.ts` contiene `export function capitalizarPrimera` y `export function fechaCortaSegura`.
    - `fechaCortaSegura` valida ISO (regex `\d{4}-\d{2}-\d{2}`) ANTES de `new Date` y NUNCA puede retornar la cadena "Invalid Date".
    - format.test.ts cubre los 6 comportamientos listados (null, "", no-ISO, ISO válida para fechaCortaSegura; string con coma y string vacío para capitalizarPrimera).
    - `pnpm --filter app test -- lib/format.test.ts` exits 0 y `tsc -b` limpio.
  </acceptance_criteria>
  <done>Ambos helpers exportados y probados; tsc limpio. Listos para consumo en Plan 50-05 (wave 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato de fuente → render (frescura) | `esStale` clasifica la edad del dato; un umbral engañoso comunica un hecho falso sobre la frescura |
| campo de fecha nullable → formateo | `fechaCortaSegura` recibe strings potencialmente null/no-ISO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01-INT | Information (integrity) | `esStale` umbral | mitigate | Umbral por cadence (14d) refleja la realidad de ingesta semanal; el ámbar deja de ser alarma permanente engañosa. Tests fijan ≤14d→false / >14d→true. |
| T-50-01-AV | Denial of Service | `fechaCortaSegura` | mitigate | Guard ISO (regex) antes de `new Date` evita "Invalid Date" renderizado; degrada a copy honesto. Test cubre null/""/no-ISO. |
| T-50-01-CAMA | Tampering | Camino A (lockdown) | accept | Plan puramente `lib/*`+tests; cero RPC, cero `.from()`, cero DDL. lockdown-guard.test.ts no cambia de superficie. |
| T-50-01-SC | Tampering | npm/pip installs | accept | La fase NO instala ni actualiza packages (50-RESEARCH.md §Package Legitimacy Audit = N/A). Sin superficie de registry. |
</threat_model>

<verification>
- `pnpm --filter app test -- lib/format.test.ts components/provenance-badge.test.tsx` verde.
- `pnpm --filter app exec tsc -b` limpio.
- `git diff app/components/provenance-badge.tsx` vacío (solo el test cambió, no la fuente).
</verification>

<success_criteria>
esStale usa umbral 14 días retro-compatible (B6); `capitalizarPrimera` y `fechaCortaSegura` existen, exportados y probados (B12/B17 capa helper); los 2 tests dependientes del umbral quedan verdes; tsc limpio; sin tocar fuente de provenance-badge ni de componentes de aplicación.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quick-wins-diagnostico-p1/50-01-SUMMARY.md` when done
</output>
