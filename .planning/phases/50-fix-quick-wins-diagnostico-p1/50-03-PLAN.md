---
phase: 50-fix-quick-wins-diagnostico-p1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/camara-chip.tsx
  - app/components/camara-chip.test.tsx
  - app/components/autores-list.tsx
  - app/components/autores-list.test.tsx
  - app/components/ficha-header.tsx
  - app/app/proyecto/[boletin]/error.tsx
  - app/app/parlamentarios/error.tsx
  - app/app/buscar/error.tsx
  - app/app/agenda/error.tsx
autonomous: true
requirements: [B8, B15, B9]
must_haves:
  truths:
    - "El timeline nunca muestra el chip literal 'Cámara origen desconocida': cuando la cámara no aplica el chip se omite (null)"
    - "Un proyecto de tipo Mensaje sin autores dice 'Iniciativa del Ejecutivo (Mensaje).' en vez de 'Autores no informados.'"
    - "Las rutas /proyecto/[boletin], /parlamentarios, /buscar y /agenda tienen error.tsx propio en español con botón Reintentar"
  artifacts:
    - path: "app/components/camara-chip.tsx"
      provides: "retorno null cuando classify() da 'desconocida'"
    - path: "app/components/autores-list.tsx"
      provides: "prop iniciativa; copy Mensaje vs 'Autores no informados.'"
      contains: "Iniciativa del Ejecutivo (Mensaje)."
    - path: "app/app/agenda/error.tsx"
      provides: "error boundary es-CL con unstable_retry (captura el throw de B7 en Plan 50-05)"
      contains: "unstable_retry"
  key_links:
    - from: "app/components/ficha-header.tsx"
      to: "AutoresList"
      via: "prop iniciativa={proyecto.iniciativa}"
      pattern: "iniciativa=\\{proyecto.iniciativa\\}"
    - from: "app/app/*/error.tsx"
      to: "unstable_retry"
      via: "firma de error boundary de este Next (NO reset)"
      pattern: "unstable_retry"
---

<objective>
Tres fixes de superficie con archivos exclusivos: omitir el chip-alarma "Cámara origen desconocida" (B8), distinguir Mensaje de Moción en el copy de autores (B15), y crear los 4 `error.tsx` faltantes espejando el patrón existente (B9). El `agenda/error.tsx` de este plan captura el `throw` que el Plan 50-05 (wave 2) añade a la agenda — por eso B9 va en wave 1, antes.

Purpose: Cerrar la doctrina de estados honestos en el 100% de las rutas (paridad de error boundaries) y quitar dos labels que fabrican/omiten información.
Output: `camara-chip.tsx` (omite null) + test nuevo; `autores-list.tsx` con prop `iniciativa` + `ficha-header.tsx` pasa la prop + test nuevo; 4 `error.tsx` nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-RESEARCH.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-PATTERNS.md
@app/AGENTS.md

<interfaces>
Sitios exactos (50-PATTERNS.md):
- B8: `app/components/camara-chip.tsx:31-45` — `STYLES.desconocida.label = "Cámara origen desconocida"`; `CamaraChip({ camara })` hace `classify(camara)` → `STYLES[kind]`. Call-sites (4, todos dentro de `flex flex-wrap gap-2`): `timeline-event.tsx:30`, `ficha-header.tsx:23`, `votacion-card.tsx:35`, `parlamentario-header.tsx:42`. NO tocar `camaraDotColor` (dot neutro OK).
- B15: `app/components/autores-list.tsx:11,14-20` — `AutoresList({ autores })`; rama `autores.length === 0` → "Autores no informados.". Campo distintivo = `ProyectoRow.iniciativa` (`lib/types.ts:17`), valores canónicos `"Mensaje"`/`"Moción"`. `origen` NO sirve. Pasar prop desde `ficha-header.tsx:55` (`proyecto.iniciativa` disponible, usado en `:35`). Caveat datos: ruta Cámara puebla `iniciativa: null` → esos siguen "Autores no informados." (honesto).
- B9: patrón a espejar VERBATIM `app/app/parlamentario/[id]/error.tsx` — `"use client"`, firma `{ error, unstable_retry }: { error: Error & { digest?: string }; unstable_retry: () => void }` (⚠️ NO `reset`), `useEffect(() => console.error(error), [error])`, h1 + párrafo es-CL + botón "Reintentar" → `unstable_retry()`. Las 4 rutas destino existen (verificado por Glob).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B8 — CamaraChip omite null en 'desconocida' + test nuevo</name>
  <files>app/components/camara-chip.tsx, app/components/camara-chip.test.tsx</files>
  <read_first>
    - app/components/camara-chip.tsx (completo: classify, STYLES, camaraDotColor)
    - app/components/votacion-card.test.tsx (patrón RTL colocalizado a espejar para el test nuevo)
    - 50-PATTERNS.md §B8 (excerpt + fix recomendado; camaraDotColor NO se toca)
    - 50-RESEARCH.md §B8 (4 call-sites seguros dentro de flex flex-wrap gap-2)
  </read_first>
  <action>
    En `app/components/camara-chip.tsx`, dentro de `CamaraChip`, después de `const kind = classify(camara)`, retornar `null` cuando `kind === "desconocida"` (antes del render del `<Badge>`). Es seguro en los 4 call-sites (todos dentro de `flex flex-wrap gap-2` con otros elementos; omitir no deja hueco). NO tocar `camaraDotColor` (el dot neutro es aceptable). Opcional: dejar de exportar el label "Cámara origen desconocida" o eliminar esa entrada de STYLES si queda muerta — a discreción, pero el DOM NUNCA debe contener ese literal. Crear `app/components/camara-chip.test.tsx` (RTL colocalizado, espejando `votacion-card.test.tsx`): casos senado→label "Senado" presente, diputados→label "Cámara" presente, desconocida/null→el componente no renderiza (queryBy… null); negative-match: cero "desconocida" en el DOM.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/camara-chip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CamaraChip` retorna `null` cuando `classify(camara)` es `"desconocida"`.
    - Ningún test de la suite renderiza el literal `Cámara origen desconocida` en el DOM.
    - `camaraDotColor` sin cambios de comportamiento (git diff no altera su lógica).
    - `app/components/camara-chip.test.tsx` existe y cubre senado/diputados/desconocida.
    - `pnpm --filter app test -- components/camara-chip.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Chip de cámara desconocida omitido; test nuevo verde; dot neutro intacto.</done>
</task>

<task type="auto">
  <name>Task 2: B15 — AutoresList distingue Mensaje + ficha-header pasa la prop + test nuevo</name>
  <files>app/components/autores-list.tsx, app/components/autores-list.test.tsx, app/components/ficha-header.tsx</files>
  <read_first>
    - app/components/autores-list.tsx (completo: rama autores.length === 0)
    - app/components/ficha-header.tsx (líneas 30-60: uso de proyecto.iniciativa :35 y AutoresList :55)
    - app/lib/types.ts (línea ~17: ProyectoRow.iniciativa)
    - 50-PATTERNS.md §B15 (fix + valores canónicos Mensaje/Moción)
    - 50-RESEARCH.md §B15 + Pitfall 4 (iniciativa null en ruta Cámara → sigue "Autores no informados.")
  </read_first>
  <action>
    Cambiar la firma de `AutoresList` a `AutoresList({ autores, iniciativa }: { autores: string[]; iniciativa: string | null })`. En la rama `autores.length === 0`: si `iniciativa === "Mensaje"` → renderizar "Iniciativa del Ejecutivo (Mensaje)."; si no (Moción sin autores, o `iniciativa` null) → mantener "Autores no informados." (honesto; NO fabricar). En `ficha-header.tsx:55` pasar `iniciativa={proyecto.iniciativa}` a `<AutoresList>` (`proyecto.iniciativa` ya disponible, usado en `:35`). Crear `app/components/autores-list.test.tsx` (RTL colocalizado): 3 casos — (a) Mensaje sin autores → "Iniciativa del Ejecutivo (Mensaje).", (b) Moción sin autores → "Autores no informados.", (c) con autores → lista los autores. Verificar que `ficha-header` sigue compilando con el prop nuevo (requerido).
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/autores-list.test.tsx && pnpm --filter app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/autores-list.tsx` acepta prop `iniciativa` y contiene la cadena exacta `Iniciativa del Ejecutivo (Mensaje).`
    - Mensaje sin autores → "Iniciativa del Ejecutivo (Mensaje)."; Moción/null sin autores → "Autores no informados." (test cubre los 3 casos).
    - `app/components/ficha-header.tsx` pasa `iniciativa={proyecto.iniciativa}` a AutoresList.
    - `pnpm --filter app test -- components/autores-list.test.tsx` exits 0 y `tsc -b` limpio (ningún otro call-site de AutoresList quedó sin la prop requerida).
  </acceptance_criteria>
  <done>Proyectos Mensaje muestran el copy correcto; el resto queda honesto; prop cableada desde ficha-header; test nuevo verde.</done>
</task>

<task type="auto">
  <name>Task 3: B9 — 4 error.tsx nuevos espejando parlamentario/[id]/error.tsx</name>
  <files>app/app/proyecto/[boletin]/error.tsx, app/app/parlamentarios/error.tsx, app/app/buscar/error.tsx, app/app/agenda/error.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/error.tsx (COMPLETO — patrón a espejar verbatim, firma unstable_retry)
    - app/AGENTS.md (⚠️ este Next usa unstable_retry, NO reset; consultar node_modules/next/dist/docs/ si hay duda)
    - 50-PATTERNS.md §B9 (excerpt completo + las 4 rutas confirmadas)
  </read_first>
  <action>
    Crear 4 archivos espejando VERBATIM `app/app/parlamentario/[id]/error.tsx`, ajustando SOLO el `<h1>` al contexto de cada ruta y el nombre del componente default export:
    - `app/app/proyecto/[boletin]/error.tsx` (h1 tipo "No pudimos cargar esta ficha")
    - `app/app/parlamentarios/error.tsx` (h1 tipo "No pudimos cargar el directorio de parlamentarios")
    - `app/app/buscar/error.tsx` (h1 tipo "No pudimos completar la búsqueda")
    - `app/app/agenda/error.tsx` (h1 tipo "No pudimos cargar la agenda")
    Cada uno: `"use client"` primera línea, `import { useEffect } from "react"`, firma `{ error, unstable_retry }: { error: Error & { digest?: string }; unstable_retry: () => void }` (NUNCA `reset`), `useEffect(() => console.error(error), [error])`, el mismo párrafo es-CL sobrio ("Esto es una falla técnica, no una ausencia de información: no asumas que no hay registros.") y el botón "Reintentar" → `unstable_retry()`. Cero inglés. El `agenda/error.tsx` capturará el throw que el Plan 50-05 añade a la agenda.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Existen los 4 archivos: `app/app/proyecto/[boletin]/error.tsx`, `app/app/parlamentarios/error.tsx`, `app/app/buscar/error.tsx`, `app/app/agenda/error.tsx`.
    - Cada uno inicia con `"use client"`, usa `unstable_retry` (NO contiene `reset`), y tiene botón "Reintentar".
    - Cero inglés en el copy (h1/párrafo/botón en es-CL).
    - `pnpm --filter app exec tsc -b` limpio.
  </acceptance_criteria>
  <done>Las 4 rutas tienen error boundary es-CL con unstable_retry; paridad con parlamentario/contraparte; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato ausente / no aplicable → chip | un chip-alarma fabricado ("desconocida") comunica un defecto donde el dato simplemente no aplica |
| fallo de ruta → error boundary | un error debe mostrar UI honesta ("falla técnica, no ausencia"), no una página en blanco ni un stack |
| Error object → DOM del boundary | el copy del boundary NO debe volcar el mensaje/stack del error al usuario (solo `console.error`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03-INT | Information (integrity) | camara-chip / autores-list | mitigate | Omitir chip cuando la cámara no aplica; distinguir Mensaje sin fabricar (null → honesto). Negative-match "desconocida"/vocabulario prohibido en tests. |
| T-50-03-ID | Information disclosure | error.tsx boundaries | mitigate | Espejar patrón existente: el `error` va solo a `console.error`; el DOM muestra copy fijo es-CL, nunca `error.message`/stack. |
| T-50-03-CAMA | Tampering | Camino A (lockdown) | accept | Solo `app/**` presentación + boundaries client; cero RPC/`.from()`/DDL. lockdown-guard sin cambio de superficie. |
| T-50-03-SC | Tampering | npm installs | accept | Sin instalación de packages (los error.tsx solo importan `react`). |
</threat_model>

<verification>
- `pnpm --filter app test -- components/camara-chip.test.tsx components/autores-list.test.tsx` verde.
- `pnpm --filter app exec tsc -b` limpio.
- Grep confirma cero `reset` y cero inglés en los 4 error.tsx; cero `Cámara origen desconocida` renderizable.
</verification>

<success_criteria>
Chip de cámara desconocida omitido (B8); proyectos Mensaje con copy correcto y el resto honesto (B15); 4 error.tsx es-CL con unstable_retry creados (B9); tests nuevos verdes; tsc limpio; boundaries no filtran error.message.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quick-wins-diagnostico-p1/50-03-SUMMARY.md` when done
</output>
