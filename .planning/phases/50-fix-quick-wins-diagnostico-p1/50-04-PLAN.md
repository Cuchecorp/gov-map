---
phase: 50-fix-quick-wins-diagnostico-p1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: [B10]
must_haves:
  truths:
    - "La ficha de un senador NUNCA dice 'la Cámara (camara.cl/transparencia)' en el copy de lobby: el frame refleja la cámara real del parlamentario"
    - "El copy de lobby se parametriza por cámara del parlamentario (senado vs diputados) sin tocar el enlace por fila (fuente real)"
  artifacts:
    - path: "app/components/lobby-de-parlamentario.tsx"
      provides: "LobbyView acepta camara y parametriza intro + empty-state; enlace por fila intacto"
    - path: "app/app/parlamentario/[id]/page.tsx"
      provides: "wrapper que lee parlamentario_publico.camara y la pasa a LobbySection (espejo de FinanciamientoSectionConPeriodo)"
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "parlamentario_publico"
      via: "RPC allowlisted (ya existente) → deriva camara → prop"
      pattern: "parlamentario_publico"
    - from: "LobbySection"
      to: "LobbyView"
      via: "camara propagada a LobbyViewData"
      pattern: "camara"
---

<objective>
Parametrizar por cámara el copy de la sección de lobby (B10): hoy el intro y el empty-state dicen "la Cámara (camara.cl/transparencia)" incluso en fichas de senador (donde lobby es solo-diputados → caen en estado noIngestado, pero el copy sigue mintiendo la fuente). Se threadea `parlamentario.camara` a `LobbyView` siguiendo el patrón EXACTO de `FinanciamientoSectionConPeriodo` (que ya lee `parlamentario_publico.periodo` y lo pasa como prop). El RPC `parlamentario_publico` YA está allowlisted → cero cambio en lockdown-guard.

Purpose: Trazabilidad honesta — el frame de lobby debe reflejar la fuente que corresponde a la cámara del parlamentario, sin fabricar un supuesto.
Output: `LobbyView`/`LobbyViewData` con `camara`; wrapper en page.tsx que deriva la cámara vía RPC; copy senado vs diputados; test con fixtures actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-RESEARCH.md
@.planning/phases/50-fix-quick-wins-diagnostico-p1/50-PATTERNS.md
@app/AGENTS.md

<interfaces>
Analog LOCKED para el threading (verificado en sesión, `app/app/parlamentario/[id]/page.tsx:311-345`):
`FinanciamientoSectionConPeriodo` lee `sb.rpc("parlamentario_publico", { p_id: id }).maybeSingle<ParlamentarioPublicoRow>()`, hace `if (error) throw new Error(...)` (#34), deriva `data?.periodo ?? null` y lo pasa EXPLÍCITO como prop a `FinanciamientoSection`. `ParlamentarioPublicoRow` incluye `camara` (usado en `parlamentario-header.tsx:42`). En este archivo cada wrapper hace su propio `.rpc("parlamentario_publico")` (no hay React.cache compartido) — ese es el patrón establecido.

Sitios del copy hardcodeado (50-PATTERNS.md §B10):
- `lobby-de-parlamentario.tsx:102-108` (intro) y `:129-132` (empty-state b) — literal "el registro oficial de la Cámara (camara.cl/transparencia)".
- `LobbySection` (`:288`) recibe `{ id, searchParams }`; `LobbyView` recibe `{ data: LobbyViewData }`.
- Call de LobbySection: `page.tsx:177` `<LobbySection id={id} searchParams={sp} />` dentro de `CarrilesSection`.
- Enlace por fila `:199` `sourceUrl={a.enlace}` — NO se toca (fuente real del dato).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threading de camara + copy parametrizado en LobbyView</name>
  <files>app/components/lobby-de-parlamentario.tsx, app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx (completo: LobbyViewData, LobbyView intro :102-108, empty-state :129-132, LobbySection :288, enlace :199)
    - app/app/parlamentario/[id]/page.tsx (líneas 135-180: CarrilesSection y call de LobbySection; 311-345: FinanciamientoSectionConPeriodo a espejar)
    - app/components/parlamentario-header.tsx (línea 42: uso de parlamentario.camara — valores)
    - 50-PATTERNS.md §B10 (opciones de threading; trazabilidad LOCKED del enlace)
    - 50-RESEARCH.md §B10 (lobby solo-diputados; senadores caen en noIngestado; copy debe reflejar la cámara real)
  </read_first>
  <action>
    (1) Añadir `camara: string | null` a `LobbyViewData` (o como prop directo de `LobbyView`, según sea más limpio). (2) Parametrizar el copy del intro (`:102-108`) y del empty-state (b) (`:129-132`): cuando la cámara es senado → referir "el Senado / el registro de la Ley del Lobby del Senado" (Ley 20.730 cubre ambas cámaras; el portal del Senado es la fuente que correspondería); cuando es diputados → mantener "la Cámara (camara.cl/transparencia)". Usar la clasificación de cámara ya presente (misma normalización que `camara-chip`/`parlamentario-header`; a discreción reutilizar el helper existente). NO tocar el enlace por fila (`:199` `sourceUrl={a.enlace}`) — es la fuente real y no se toca. (3) En `app/app/parlamentario/[id]/page.tsx`, agregar un wrapper `LobbySectionConCamara` espejando VERBATIM `FinanciamientoSectionConPeriodo`: lee `parlamentario_publico` (RPC ya allowlisted), `if (error) throw new Error(...)` (#34), deriva `data?.camara ?? null`, y lo pasa como prop a `LobbySection`; reemplazar el call `<LobbySection id={id} searchParams={sp} />` (`:177`) por el wrapper. `LobbySection` propaga `camara` a `LobbyViewData`.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app exec tsc -b && pnpm --filter app test -- lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LobbyViewData` (o `LobbyView`) acepta `camara`; el copy del intro y del empty-state ramifica por cámara.
    - Para cámara senado el copy NO contiene la subcadena `camara.cl/transparencia` ni "la Cámara" como fuente.
    - El wrapper en page.tsx usa `parlamentario_publico` (RPC ya allowlisted — NO agrega RPC nueva) y hace throw-on-error (#34).
    - El enlace por fila `sourceUrl={a.enlace}` sin cambios.
    - `pnpm --filter app test -- lockdown-guard.test.ts` verde (sin nueva RPC/`.from` PII) y `tsc -b` limpio.
  </acceptance_criteria>
  <done>Copy de lobby parametrizado por cámara; camara threaded vía RPC allowlisted; enlace intacto; lockdown verde.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar fixtures/asserts de lobby-de-parlamentario.test.tsx</name>
  <files>app/components/lobby-de-parlamentario.test.tsx</files>
  <read_first>
    - app/components/lobby-de-parlamentario.test.tsx (completo: makeData/fixtures + assertions del literal "Cámara (camara.cl/transparencia)")
    - 50-RESEARCH.md §B10 (Tests: si LobbyViewData gana campo camara, actualizar fixtures)
  </read_first>
  <action>
    Actualizar `makeData`/fixtures de `lobby-de-parlamentario.test.tsx` para incluir el nuevo campo `camara`. Agregar/ajustar assertions: (a) con cámara diputados → el copy contiene la referencia a la Cámara; (b) con cámara senado → el copy NO contiene `camara.cl/transparencia` y refiere el Senado. Conservar las assertions existentes que sigan válidas. Negative-match: cero vocabulario prohibido en el copy nuevo.
  </action>
  <verify>
    <automated>cd app && pnpm --filter app test -- components/lobby-de-parlamentario.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Los fixtures de `lobby-de-parlamentario.test.tsx` incluyen `camara`.
    - Un caso senado asierta ausencia de `camara.cl/transparencia`; un caso diputados asierta su presencia.
    - `pnpm --filter app test -- components/lobby-de-parlamentario.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Test de lobby cubre ambas cámaras; fixtures actualizados; verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cámara del parlamentario → copy de fuente | atribuir la fuente equivocada (Cámara para un senador) fabrica trazabilidad falsa |
| lectura de camara → RPC | debe usar el RPC público allowlisted, sin abrir superficie nueva ni tocar PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04-INT | Information (integrity) | LobbyView copy | mitigate | Copy ramifica por cámara real; el enlace por fila (fuente real) no se toca; test asierta que senador nunca dice "camara.cl/transparencia". |
| T-50-04-CAMA | Tampering | Camino A (lockdown) | mitigate | Se reusa `parlamentario_publico` (ya en el allowlist); CERO RPC nueva, cero `.from()` PII. `lockdown-guard.test.ts` corre en el verify y debe quedar verde. |
| T-50-04-ID | Information disclosure | wrapper RPC | mitigate | `parlamentario_publico` es security-definer PII-safe (no emite partido/rut/email); solo se lee `camara` (campo público). throw-on-error (#34), nunca degrada a vacío. |
| T-50-04-SC | Tampering | npm installs | accept | Sin instalación de packages. |
</threat_model>

<verification>
- `pnpm --filter app test -- components/lobby-de-parlamentario.test.tsx lockdown-guard.test.ts` verde.
- `pnpm --filter app exec tsc -b` limpio.
- Grep: cero `camara.cl/transparencia` en el path de copy para cámara senado.
</verification>

<success_criteria>
El copy de lobby se parametriza por cámara del parlamentario (B10); ficha de senador nunca atribuye a la Cámara; `camara` threaded vía RPC allowlisted (sin RPC nueva); enlace por fila intacto; lockdown-guard verde; tsc limpio; test cubre ambas cámaras.
</success_criteria>

<output>
Create `.planning/phases/50-fix-quick-wins-diagnostico-p1/50-04-SUMMARY.md` when done
</output>
