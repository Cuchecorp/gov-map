---
phase: 119-cron-fix-robustez-de-ingesta
plan: 06
type: execute
wave: 3
depends_on: ["119-04"]
files_modified:
  - packages/lobby/src/ingest-cli.ts
  - packages/lobby/src/ingest-run.ts
  - packages/lobby/src/ingest-run.test.ts
  - packages/lobby/src/ingest-cli.test.ts
  - packages/lobby/src/cursor-leylobby.ts
  - .github/workflows/fichas-backfill.yml
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "Tras un lote confirmado de leylobby, `lobby_ingesta_estado.ingestado_hasta` avanza"
    - "Una corrida que degrada (403/503, sin datos) NO avanza ningún cursor"
    - "El archivo explica por qué conviven dos cursores (o uno se deriva del otro)"
    - "`fichas-backfill` resuelve `SUPABASE_URL` sin depender de un secret que no existe"
  artifacts:
    - path: "packages/lobby/src/ingest-cli.ts"
      provides: "avance de lobby_ingesta_estado + decisión de los dos cursores documentada"
      contains: "lobby_ingesta_estado"
    - path: ".github/workflows/fichas-backfill.yml"
      provides: "remapeo SUPABASE_URL ← secrets.SUPABASE_API_URL"
      contains: "SUPABASE_API_URL"
  key_links:
    - from: "packages/lobby/src/ingest-cli.ts"
      to: "lobby_ingesta_estado.ingestado_hasta"
      via: "writer.marcarIngestado tras lote confirmado"
      pattern: "marcarIngestado"
---

<objective>
Cerrar G1 (P1: el cursor `lobby_ingesta_estado` lleva detenido en 2026-06-22 pese a corridas
`success` semanales — el CLI actualiza `leylobby_cursor_estado` pero no el `*_ingesta_estado` de
la misma fuente) y la parte YAML de G9 (verificar si `SUPABASE_URL` es un remapeo faltante de
`secrets.SUPABASE_API_URL`, no un secret ausente).

Purpose: G1 es la razón por la que `lobby-leylobby` reporta 36 días de staleness con umbral 7 —
la señal más ruidosa del panel de frescura, y no se sabe si miente o dice la verdad.
Output: el cursor avanza cuando hay datos y sólo cuando hay datos, con la convivencia de los dos
cursores explicada en el propio archivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-04-SUMMARY.md

Gaps de origen: `118-CRON-VERDICTS.md` §4 fila G1 (línea 1820) y fila G9 (línea 1828, SOLO la
parte YAML: `GEMINI_API_KEY` es deuda de operador con checkpoint YA emitido en
`118-OPERATOR-CHECKPOINT.md` — NO se re-pide, NO se carga ningún valor).

Dato de 118 (probe P8): `lobby_ingesta_estado.ingestado_hasta = 2026-06-22` vs
`leylobby_cursor_estado.fecha_captura = 2026-07-22 12:44:06`.

<interfaces>
Estado actual (`packages/lobby/src/ingest-run.ts:260-263`) — `marcarIngestado` SÍ se llama, pero
sólo con los parlamentarios cuyo FK quedó CONFIRMADO en la corrida:
```typescript
if (marcados.size > 0) {
  await opts.writer.marcarIngestado([...marcados], hasta);
}
```
Writer (`packages/lobby/src/writer-supabase.ts:114-124`):
```typescript
async marcarIngestado(parlamentarioIds: string[], hasta: string): Promise<void>;
// upsert a lobby_ingesta_estado onConflict 'parlamentario_id', columna ingestado_hasta
```
Cursor de recurso (`packages/lobby/src/cursor-leylobby.ts`) — regla LOCKED T-74-02:
```typescript
export function avanzarCursor(cursor: CursorLeylobby, opts: { huboDatos: boolean; ... }): CursorLeylobby;
// huboDatos === false ⇒ devuelve el MISMO cursor (degradación honesta NO avanza)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: diagnosticar POR QUÉ el cursor está detenido, antes de tocar nada</name>
  <read_first>
    - `packages/lobby/src/ingest-run.ts` :230-265 (dónde se puebla `marcados` — sólo `parlamentariosConfirmados` de `reconciliarSujeto` — y la guarda `if (marcados.size > 0)`)
    - `packages/lobby/src/ingest-cli.ts` :196-260 (modo cursor, `huboDatos = res.audiencias > 0`, avance de `leylobby_cursor_estado`)
    - `packages/lobby/src/reconciliar-sujeto.ts` :150-170 (qué cuenta como confirmado)
    - 118-CRON-VERDICTS.md §2 W-5 y §3.4 (cursores — el discriminante de stale) y §4 fila G1
  </read_first>
  <action>
    Determinar con evidencia cuál de estas tres es la causa real de que `ingestado_hasta` no avance
    (las tres son compatibles con lo que 118 observó; el fix correcto depende de cuál sea):
      (a) las corridas semanales traen audiencias pero CERO parlamentarios confirmados ⇒
          `marcados.size === 0` ⇒ `marcarIngestado` nunca corre;
      (b) las corridas degradan (403/503 de leylobby) ⇒ 0 audiencias ⇒ nada que marcar;
      (c) el cursor de recurso ya barrió el histórico y pide páginas sin datos nuevos.
    Evidencia admisible: logs de las últimas corridas de `lobby-leylobby-weekly` (`gh run view`),
    y psql read-only: `select count(*), max(ingestado_hasta) from lobby_ingesta_estado;`,
    `select * from leylobby_cursor_estado;`, y conteo de `lobby_audiencia` por `fecha_captura`
    reciente. NO inventar la causa: si la evidencia no distingue, decirlo y elegir el fix que sea
    correcto bajo las tres.
    Escribir el diagnóstico (3-8 líneas con su evidencia) en el SUMMARY — alimenta el documento de
    cierre del plan 07.
  </action>
  <verify>
    <automated>grep -q 'Diagnostico G1' .planning/phases/119-cron-fix-robustez-de-ingesta/119-06-SUMMARY.md && echo OK-DIAGNOSTICO</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY contiene un bloque encabezado literalmente `## Diagnostico G1` (sin tilde, para que el grep del verify muerda).
    - El diagnóstico nombra la causa (a/b/c) o declara explícitamente que la evidencia no distingue y cuál es el criterio que la distinguiría.
    - Cita al menos una salida psql (sin URL) y una referencia de corrida de GH Actions.
    - Ninguna afirmación sobre la causa sin evidencia detrás.
  </acceptance_criteria>
  <done>El fix del Task 2 se elige por diagnóstico, no por analogía.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hacer avanzar `lobby_ingesta_estado` sin fabricar frescura</name>
  <read_first>
    - Todo lo del Task 1 más `packages/probidad/src/run-probidad-todos.ts` :138 (`await opts.writer.marcarIngestado([...confirmados], hasta);` — la plantilla que G1 manda usar: el conector de probidad mantiene su cursor al día)
    - `packages/lobby/src/writer-supabase.ts` :114-124 y :126-152 (los DOS cursores: `lobby_ingesta_estado` por `parlamentario_id`, `leylobby_cursor_estado` por institución)
    - 118-CRON-VERDICTS.md §4 fila G1, pasos 1-4
  </read_first>
  <behavior>
    - Test 1: corrida con audiencias y ≥1 parlamentario confirmado ⇒ `marcarIngestado` invocado con `hasta` = la fecha máxima INGERIDA (no `new Date()` si el lote es histórico) y `max(ingestado_hasta)` avanza.
    - Test 2: corrida que degrada (403/503, `degradaciones.length > 0`, `audiencias === 0`) ⇒ NINGÚN cursor avanza (ni `lobby_ingesta_estado` ni `leylobby_cursor_estado`) — regla LOCKED T-74-02 preservada.
    - Test 3: corrida con audiencias pero CERO confirmados ⇒ el comportamiento es el que decida el Task 1 y está ASEVERADO explícitamente (si se decide marcar de todos modos un marcador de barrido, el test lo dice; si se decide no marcar, el test lo dice). Sin ambigüedad.
    - Test 4: doble corrida sobre el mismo lote es idempotente (upsert onConflict, `ingestado_hasta` no retrocede).
    - Test 5: `ingestado_hasta` NUNCA retrocede: un lote histórico posterior a uno reciente no baja la marca.
  </behavior>
  <action>
    1. Aplicar el fix derivado del Task 1, usando `packages/probidad/src/run-probidad-todos.ts:138`
       como plantilla del "cursor al día tras lote confirmado".
    2. Si el diagnóstico fue (a) [audiencias sin confirmados], la decisión de diseño es: ¿un
       barrido que no confirmó a nadie constituye "ingestado"? Resolverlo explícitamente y
       DOCUMENTAR la decisión en el archivo. Prohibido el atajo de marcar `ingestado_hasta = hoy`
       sin datos: eso es fabricar frescura y viola el criterio 2 del ROADMAP.
    3. `hasta` sale de los datos ingeridos, jamás del reloj cuando el lote es histórico.
    4. Añadir en `packages/lobby/src/ingest-cli.ts` (o en `cursor-leylobby.ts`, donde sea más
       legible) un bloque de comentario `POR QUÉ DOS CURSORES` que explique: `leylobby_cursor_estado`
       = qué recurso pedir la próxima vez (posición del barrido); `lobby_ingesta_estado` = hasta
       cuándo hay datos por parlamentario (marcador de cobertura, el que lee freshness). Si el
       executor concluye que uno debe derivarse del otro, implementarlo y decirlo; si concluye que
       conviven, decir por qué. Hoy NADA lo explica y ése es literalmente el paso 2 de G1.
    5. Tests de `<behavior>` ANTES del cambio.
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test</automated>
  </verify>
  <acceptance_criteria>
    - Los 5 tests de `<behavior>` existen y pasan.
    - `grep -c 'POR QUÉ DOS CURSORES' packages/lobby/src/*.ts` ≥ 1.
    - El Test 2 falla si se elimina la guarda de degradación (verificarlo mutando en memoria).
    - Corrida local acotada (una institución, una página, rate-limit intacto) seguida de
      `select max(ingestado_hasta) from lobby_ingesta_estado;` pegada en el SUMMARY, mostrando el
      valor antes y después. Si la fuente está bloqueada (403/503), la corrida debe mostrar la
      degradación honesta y el cursor SIN avanzar — ése también es un resultado válido y se
      documenta como tal.
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>El cursor de cobertura de leylobby avanza con datos y sólo con datos, y la convivencia de los dos cursores está explicada donde vive el código.</done>
</task>

<task type="auto">
  <name>Task 3: G9 parte YAML — `SUPABASE_URL` como remapeo, no como secret ausente</name>
  <read_first>
    - `.github/workflows/fichas-backfill.yml` :60-70 (bloque `env:` del step "Run fichas backfill"; hoy `SUPABASE_URL: ${{ secrets.SUPABASE_URL }}` junto a `SUPABASE_API_URL: ${{ secrets.SUPABASE_API_URL }}`)
    - `.github/workflows/lobby-leylobby-weekly.yml` :57-62 (PLANTILLA de remapeo: `SUPABASE_URL: ${{ secrets.SUPABASE_API_URL }}` con el comentario que explica la divergencia de nombres)
    - `packages/fichas/src/pipeline-cli.ts` (qué nombres de env lee realmente — verificar antes de tocar el YAML)
    - 118-CRON-VERDICTS.md §4 fila G9, paso 1, y §1.4 (los 7 secrets confirmados: `SUPABASE_API_URL` SÍ existe, `SUPABASE_URL` NO)
  </read_first>
  <action>
    1. Verificar en `pipeline-cli.ts` qué nombre de variable lee el CLI de fichas. Si lee
       `SUPABASE_URL` (o lo prefiere), cambiar la línea del YAML a
       `SUPABASE_URL: ${{ secrets.SUPABASE_API_URL }}`, copiando el comentario explicativo de
       `lobby-leylobby-weekly.yml:57-60` (divergencia de nombres entre CLIs y secrets del repo).
       Si el CLI ya acepta `SUPABASE_API_URL` como fallback, eliminar la línea `SUPABASE_URL`
       en vez de remapearla — menos superficie es mejor que más mapeo.
    2. NO tocar `GEMINI_API_KEY`: es acto de operador con checkpoint ya emitido en
       `118-OPERATOR-CHECKPOINT.md`. No se re-pide, no se carga, no se comenta la línea.
    3. Re-verificar (solo lectura, sin cargar nada) el estado de los secrets:
       `gh secret list --repo Cuchecorp/gov-map` → registrar NOMBRES y fechas en el SUMMARY,
       JAMÁS valores. Si `GEMINI_API_KEY` sigue ausente, dejarlo consignado como deuda abierta.
    4. El dispatch de prueba de `fichas-backfill` (paso 3 de G9) queda condicionado a que
       `GEMINI_API_KEY` exista: si no existe, NO se dispara y se declara así.
  </action>
  <verify>
    <automated>! grep -q 'secrets.SUPABASE_URL' .github/workflows/fichas-backfill.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'secrets.SUPABASE_URL' .github/workflows/fichas-backfill.yml` devuelve 0.
    - El YAML no referencia ningún secret que `gh secret list` no muestre, salvo `GEMINI_API_KEY`, que queda declarado como deuda de operador en el SUMMARY con su cita a `118-OPERATOR-CHECKPOINT.md`.
    - El SUMMARY registra la salida de `gh secret list` con NOMBRES y fechas solamente — cero valores.
    - `git diff .github/workflows/fichas-backfill.yml` toca 1-2 líneas del bloque `env:` y nada más (ni `schedule:`, ni permisos, ni el pin de las actions).
  </acceptance_criteria>
  <done>La parte de G9 que es un fix de YAML está cerrada; la parte que es acto de operador queda declarada, no re-pedida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leylobby (Laravel/Azure, volátil) → conector | HTML/JSON no confiable |
| GitHub Actions secrets → workflow env | expansión de secrets en el runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-17 | Repudiation | `ingestado_hasta` | mitigate | la marca sale de los datos ingeridos y NUNCA del reloj cuando el lote es histórico; los tests 1 y 5 lo aseveran |
| T-119-18 | Tampering | avance de cursor sin datos | mitigate | regla LOCKED T-74-02 preservada por el Test 2: degradación 403/503 ⇒ ningún cursor avanza |
| T-119-19 | Information disclosure | `gh secret list` en el SUMMARY | mitigate | se registran NOMBRES y fechas exclusivamente; el comando no expone valores y el agente no los solicita |
| T-119-20 | Elevation of privilege | remapeo de secrets en el YAML | accept | el remapeo no amplía el conjunto de secrets expuestos al step (ya recibía `SUPABASE_API_URL`); no se añaden permisos ni se cambia `permissions: contents: read` |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- `pnpm --filter @obs/lobby test` verde; `tsc -b` exit 0.
- `pnpm freshness --json` re-corrido tras la corrida local: registrar el estado de `lobby-leylobby`
  (stale true/false, días) ANTES y DESPUÉS, con la advertencia de que sólo pasa a `stale:false` si
  hubo datos reales — si sigue stale por falta de datos, eso es degradación honesta y se declara.
- Guard env-example verde (este plan no añade env vars nuevas).
</verification>

<success_criteria>
- G1 cerrado con diagnóstico, fix y test que impide el avance sin datos.
- G9-YAML cerrado; G9-GEMINI declarado como deuda de operador SIN re-pedir el checkpoint.
- Cero frescura fabricada: si `lobby-leylobby` sigue stale, es porque no hay datos, y así se dice.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-06-SUMMARY.md` when done
</output>
