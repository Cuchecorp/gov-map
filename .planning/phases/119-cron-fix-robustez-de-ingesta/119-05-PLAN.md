---
phase: 119-cron-fix-robustez-de-ingesta
plan: 05
type: execute
wave: 3
depends_on: ["119-04"]
files_modified:
  - packages/agenda/src/run-agenda-prod-cli.ts
  - packages/agenda/src/ingest-run.ts
  - packages/agenda/src/ingest-run.test.ts
  - packages/probidad/src/run-probidad-todos-cli.ts
  - packages/probidad/src/run-probidad-todos.ts
  - packages/probidad/src/run-probidad-todos.test.ts
  - packages/lobby/src/run-camara-lobby-cli.ts
  - packages/lobby/src/run-camara-lobby.test.ts
  - .github/workflows/lobby-camara-weekly.yml
  - docs/runbooks/cron-local-fallback.md
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "Re-ingestar agenda y probidad a Supabase se puede hacer DESDE R2, sin volver a la fuente (regla LOCKED etapa 2)"
    - "El fallback local de lobby-camara deja el HTML del curl en R2 y parsea desde R2 — re-procesable sin volver a chocar con el WAF"
    - "Parsear dos veces el mismo objeto R2 es idempotente (no duplica filas)"
  artifacts:
    - path: "packages/agenda/src/run-agenda-prod-cli.ts"
      provides: "flag --from-r2"
      contains: "--from-r2"
    - path: "packages/probidad/src/run-probidad-todos-cli.ts"
      provides: "flag --from-r2"
      contains: "--from-r2"
    - path: ".github/workflows/lobby-camara-weekly.yml"
      provides: "curl → putImmutable → parseo desde R2"
      contains: "R2"
  key_links:
    - from: "los CLIs"
      to: "R2Store.getObject"
      via: "--from-r2 <r2Path>"
      pattern: "from-r2|fromR2"
---

<objective>
Cerrar G7: hoy no hay ruta de Etapa 2 (`--from-r2`) en agenda, probidad ni lobby-camara ⇒
re-ingestar exige volver a la fuente, exactamente lo que la regla LOCKED de `CLAUDE.md` prohíbe
("Re-ingestar a Supabase se hace SIEMPRE desde R2 — no se vuelve a molestar al servidor").

Purpose: sin `--from-r2`, un cambio de schema o un error de carga obliga a re-crawlear un WAF
gubernamental. Con él, R2 es de verdad la verdad cruda versionada.
Output: los 3 caminos de replay existen, con la misma firma de flag que la plantilla dorada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-04-SUMMARY.md

Gap de origen: `118-CRON-VERDICTS.md` §4 fila G7 (línea 1824) + §2 W-2, W-6, W-9.
Regla LOCKED: `CLAUDE.md` §Conventions, "Ingesta y Cron", puntos 1 y 2.

<interfaces>
PLANTILLA DORADA de la firma del flag (`packages/lobby/src/ingest-cli.ts:111-116`):
```typescript
case "--from-r2": {
  const path = argv[++i];
  if (!path) throw new LobbyCliArgsError("--from-r2 requiere un r2Path");
  opts.fromR2 = path;
  break;
}
```
y el modo de construir el store desde env (`packages/tramitacion/src/ingest-cli.ts:200-213`).

Lectura desde R2 (`packages/ingest/src/r2-store.ts`):
```typescript
async getObject(r2Path: string): Promise<Uint8Array>;   // lanza Error con status y path (sin credenciales)
```

Helper de flags ya presente en los CLIs de agenda/probidad/lobby-camara:
```typescript
function flagValue(name: string): string | null;  // lee `--x <valor>` de process.argv
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agenda — `--from-r2 <r2Path>`</name>
  <read_first>
    - `packages/agenda/src/run-agenda-prod-cli.ts` :37-100 (`flagValue`, parsing de `--dry-run`/`--solo-senado`/`--desde`/`--hasta`) y :112-165 (construcción de `R2Store`, `writer`, `runIngest`)
    - `packages/agenda/src/ingest-run.ts` :140-172 y :275-305 (los dos puntos de Etapa 1: qué bytes se guardan y bajo qué key — el replay debe leer EXACTAMENTE eso)
    - `packages/tramitacion/src/ingest-cli.ts` :185-215 (plantilla dorada del flag y del store)
    - 118-CRON-VERDICTS.md §4 fila G7 paso 1
  </read_first>
  <behavior>
    - Test 1: con `--from-r2 camara/citaciones-semana/2026-W20/<sha>.html`, el conector de la fuente NO se invoca (cero fetch a camara.cl) y el parseo corre sobre los bytes de R2.
    - Test 2: el upsert resultante es idéntico al de la ingesta normal con los mismos bytes.
    - Test 3: correr el replay DOS veces sobre el mismo objeto produce el mismo estado (idempotencia: mismos conteos, sin filas duplicadas).
    - Test 4: `--from-r2` con un path inexistente falla LOUD (error con status y path, sin credenciales), no degrada a re-fetch de la fuente.
    - Test 5: `--from-r2` sin valor ⇒ error de flags antes de cualquier red/DB.
  </behavior>
  <action>
    1. Añadir `--from-r2 <r2Path>` con la firma de la plantilla dorada.
    2. En modo `--from-r2`: construir el `R2Store` desde `.env`, leer el objeto con `getObject`,
       y alimentar el parseo/upsert SALTANDO la Etapa 1 y el conector de la fuente. El tipo de
       recurso (citaciones-semana HTML vs tabla-sala PDF) se deriva del prefijo del `r2Path`
       (`camara/citaciones-semana/...` vs `camara/tabla-sala/...`); si el prefijo no matchea
       ninguno, fallar LOUD (no adivinar).
    3. PROHIBIDO tocar la fuente en este modo: el rate-limiter y el conector no se instancian.
       Aseverarlo con un stub de conector que lance si se invoca (Test 1).
    4. Tests de `<behavior>` ANTES de la implementación.
  </action>
  <verify>
    <automated>pnpm --filter @obs/agenda test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from-r2' packages/agenda/src/run-agenda-prod-cli.ts` ≥ 1.
    - Los 5 tests de `<behavior>` existen y pasan.
    - El Test 1 falla si el modo replay instancia el conector (verificarlo mutando en memoria).
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>Agenda se puede re-ingestar desde el crudo sin volver a pedirle nada a camara.cl/senado.cl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: probidad — `--from-r2 <r2Path>`</name>
  <read_first>
    - `packages/probidad/src/run-probidad-todos-cli.ts` :44-115 (`flagValue`, `--dry-run`, `--limit`)
    - `packages/probidad/src/run-probidad-todos.ts` :140-175 (qué se guarda en R2: el crudo AGREGADO por corrida, `infoprobidad/declaraciones/<hasta>/<sha>.json`, un JSON con el array `crudos`)
    - `packages/tramitacion/src/ingest-cli.ts` :185-215
    - 118-CRON-VERDICTS.md §4 fila G7 paso 1 y §2 W-6
  </read_first>
  <behavior>
    - Test 1: con `--from-r2 infoprobidad/declaraciones/<fecha>/<sha>.json`, el conector InfoProbidad NO se invoca y las declaraciones se derivan del JSON agregado.
    - Test 2: doble replay sobre el mismo objeto es idempotente (upsert por clave natural, mismos conteos).
    - Test 3: JSON malformado o con forma inesperada ⇒ error LOUD con el path (sin credenciales), CERO filas escritas — nunca una carga parcial silenciosa.
    - Test 4: `--from-r2` no avanza `lobby_ingesta_estado` ni ningún cursor a una fecha posterior a la que el propio crudo declara (un replay del pasado no puede fingir frescura de hoy).
  </behavior>
  <action>
    1. Añadir el flag con la firma de la plantilla dorada.
    2. Reconstruir la corrida desde el JSON agregado: parsear, validar la forma (zod si el paquete
       ya lo usa; si no, validación explícita con fallo loud) y correr el mismo pipeline de
       reconciliación + upsert que la ingesta normal.
    3. REGLA DE HONESTIDAD (Test 4): la fecha de `marcarIngestado`/`ingestado_hasta` en modo replay
       es la que trae el crudo (`hasta` embebido o derivado del path), NUNCA `new Date()`. Un
       replay no puede refrescar la señal de frescura sin datos nuevos.
    4. Tests de `<behavior>` ANTES de la implementación.
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from-r2' packages/probidad/src/run-probidad-todos-cli.ts` ≥ 1.
    - Los 4 tests de `<behavior>` existen y pasan.
    - El Test 4 es explícito sobre la fecha usada (asevera el valor, no sólo que "no crashea").
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>Probidad se re-ingesta desde R2 sin volver al CPLT y sin poder fingir frescura.</done>
</task>

<task type="auto">
  <name>Task 3: W-9 lobby-camara — el HTML del curl entra a R2 y el parseo sale de R2</name>
  <read_first>
    - `.github/workflows/lobby-camara-weekly.yml` :50-70 (el step de `curl` que deja `/tmp/lobby.html` con el guard de <10.240 bytes, y el step que corre `run-camara-lobby-cli.ts --html-file /tmp/lobby.html`)
    - `packages/lobby/src/run-camara-lobby-cli.ts` :30-110 (`flagValue`, `--dry-run`, `--html-file` y el stub que respeta `fetchListado(): Promise<string>`)
    - `packages/lobby/src/run-camara-lobby.ts` :84-110 (Etapa 1 YA correcta: `const { r2Path: newPath, existed }` con `[skip] sin novedades` — este conector es la referencia interna del paquete)
    - `docs/runbooks/cron-local-fallback.md` (el fallback local que debe quedar re-procesable)
    - 118-CRON-VERDICTS.md §4 fila G7 paso 2 y §4.1 (la ausencia de `schedule:` por el WAF NO es gap y NO se toca)
  </read_first>
  <action>
    1. En `run-camara-lobby-cli.ts`, añadir `--from-r2 <r2Path>` (misma firma dorada) que lea el
       HTML desde R2 con `getObject` y lo alimente al mismo stub de `fetchListado`.
    2. Hacer que `--html-file` PRIMERO haga `putImmutable("camara-lobby","listadodeaudiencias",<date>,<sha>,"html", bytes)`
       y luego parsee desde esos mismos bytes: así el HTML del curl queda content-addressed en R2
       y el fallback local pasa a ser re-procesable con `--from-r2`. El `existed:true` sigue
       produciendo `[skip] sin novedades` (comportamiento ya presente en `run-camara-lobby.ts`).
    3. En `lobby-camara-weekly.yml`, dejar el `curl` y su guard de 10.240 bytes INTACTOS (es el
       guard funcionando, §4.1) y añadir al log del step el `r2Path` resultante para que el
       operador pueda re-procesar. NO añadir `schedule:` (decisión declarada: WAF).
    4. Actualizar `docs/runbooks/cron-local-fallback.md` con el comando exacto de replay
       `--from-r2` (sin credenciales, sólo nombres de variables).
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test</automated>
  </verify>
  <acceptance_criteria>
    - Test de idempotencia: parsear DOS veces el mismo objeto R2 produce los mismos conteos y ninguna fila duplicada (clave natural).
    - Test: `--html-file` con `r2Store` presente invoca `putImmutable` una vez antes de parsear; con `r2Store` ausente, parsea igual y loguea el `[WARN]` de Etapa 1 omitida (degradación honesta, no fallo).
    - `grep -c 'schedule:' .github/workflows/lobby-camara-weekly.yml` = 0 (no se le añade cron).
    - `grep -c '10240' .github/workflows/lobby-camara-weekly.yml` ≥ 1 (el guard sigue ahí).
    - `docs/runbooks/cron-local-fallback.md` contiene el comando `--from-r2` y CERO valores de credenciales.
  </acceptance_criteria>
  <done>El fallback local de Cámara deja crudo versionado y se puede re-procesar sin volver a chocar con el WAF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R2 → conector (replay) | bytes leídos de R2 que cruzan a parseo y a la DB |
| argv del operador → CLI | el `r2Path` es input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-13 | Tampering | `--from-r2 <r2Path>` | mitigate | el `r2Path` se usa SOLO como key de `getObject` (SigV4 sobre el bucket propio); validar que empieza por uno de los prefijos conocidos del conector y rechazar `..`/rutas absolutas — sin construcción de URL arbitraria |
| T-119-14 | Spoofing | crudo alterado en R2 | mitigate | la key ES el sha256 del contenido; si se desea garantía dura, re-calcular el sha de los bytes leídos y compararlo con el segmento de la key, fallando loud ante mismatch |
| T-119-15 | Repudiation | frescura fingida por replay | mitigate | en modo replay la fecha de cursor/`ingestado_hasta` sale del crudo, nunca del reloj (Task 2, Test 4) |
| T-119-16 | Information disclosure | errores de R2 | mitigate | `getObject` ya lanza con status y path SIN credenciales (`r2-store.ts`); no añadir el endpoint ni las keys a ningún log nuevo |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- `pnpm --filter @obs/agenda test`, `@obs/probidad test`, `@obs/lobby test` verdes; `tsc -b` exit 0.
- Al menos UN replay real ejecutado localmente contra un objeto ya existente en R2 (sin tocar
  ninguna fuente), con la salida pegada en el SUMMARY. Si no hay objeto disponible para replay,
  decirlo así y dejar el criterio de cierre para el plan 07 — no se declara verificado sin corrida.
</verification>

<success_criteria>
- G7 cerrado: los 3 caminos de Etapa 2 existen con la firma dorada y test de idempotencia.
- Cero cambios en el guard del WAF, en la ausencia de `schedule:` de W-9 o en el rate-limit.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-05-SUMMARY.md` when done
</output>
