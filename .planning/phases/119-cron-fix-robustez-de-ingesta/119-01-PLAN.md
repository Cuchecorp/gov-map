---
phase: 119-cron-fix-robustez-de-ingesta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/query-runner.ts
  - packages/freshness/src/evaluate.ts
  - packages/freshness/src/evaluate.test.ts
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "`pnpm freshness --json` desde la raíz emite JSON sin fallar por binario ausente (G10)"
    - "`pnpm freshness` no imprime líneas HTTP 404 en stderr por workflows inexistentes (G2)"
    - "Una señal cuyo workflow de GH Actions falló o nunca corrió puede reportar stale:true por sí sola (G4)"
    - "`lobby-camara` y `fichas` miden una tabla propia del cron que nombran, no una que llena otro cron (G4)"
  artifacts:
    - path: "packages/freshness/src/catalog.ts"
      provides: "workflowYml nullable + entradas lobby-camara/fichas reapuntadas"
      contains: "workflowYml"
    - path: "packages/freshness/src/evaluate.ts"
      provides: "ghRun incorporado al cálculo de stale"
      contains: "ghRun"
    - path: "package.json"
      provides: "script freshness que resuelve tsx desde la raíz"
      contains: "freshness"
  key_links:
    - from: "packages/freshness/src/query-runner.ts"
      to: "gh run list"
      via: "ghRunSignal omitido cuando workflowYml es null"
      pattern: "workflowYml"
    - from: "packages/freshness/src/evaluate.ts"
      to: "FuenteResult.stale"
      via: "ghRun failure/sin-corridas ⇒ stale"
      pattern: "stale"
---

<objective>
Cerrar los gaps de INSTRUMENTO de frescura que 118 §4 detectó sobre `packages/freshness`:
G10 (la herramienta no arranca), G2 (dos 404 por workflows inexistentes) y G4 (verde prestado:
dos señales miden una tabla que llena OTRO cron y el dato `ghRun` se muestra pero se ignora).

Purpose: sin instrumento confiable, ninguna de las verificaciones de cierre del resto de la fase
(que se apoyan en `pnpm freshness`) es creíble.
Output: `pnpm freshness --json` corre desde la raíz, sin 404, con `lobby-camara`/`fichas`
apuntadas a tabla propia y con `ghRun` capaz de producir `stale:true` por sí solo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md

Gaps de origen (fuente de verdad, NO re-investigar):
`.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/118-CRON-VERDICTS.md` §4,
filas G2, G4 y G10 de la tabla "Detalle — ordenado por prioridad" (líneas 1818-1830), y §4.1
(estados esperados: MONEY/SERVEL gated NO son gaps — sus señales stale son honestas y NO se tocan).

<interfaces>
De `packages/freshness/src/catalog.ts`:
```typescript
export interface FuenteConfig {
  fuente: string;
  tabla: string;
  columna: string;
  umbralDias: number;
  overrideEnv: string;
  workflowYml: string;   // ← pasa a string | null en este plan
  agregado?: "MAX" | "MIN";
}
```

De `packages/freshness/src/query-runner.ts`:
```typescript
export interface QueryRow { fuente: string; ultimoUpsert: string | null; ghRun: string; r2Snapshot: string; }
function ghRunSignal(workflowYml: string): string;   // devuelve "success @ YYYY-MM-DD" | "n/d (sin corridas)" | "n/d"
export async function queryFreshness(dbUrl: string): Promise<QueryRow[]>;
```

De `packages/freshness/src/evaluate.ts`:
```typescript
export interface FuenteResult {
  fuente: string; tabla: string; ultimoUpsert: string | null;
  diasDesdeUpsert: number | null; umbralDias: number; stale: boolean;
  ghRun: string; r2Snapshot: string;
}
export function evaluate(rows: QueryRow[], catalog: FuenteConfig[], now: Date, envOverrides?: Record<string,string>): FuenteResult[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: G10 — `pnpm freshness` resuelve tsx desde la raíz</name>
  <read_first>
    - `package.json` (script `freshness` en línea 12: `tsx packages/freshness/src/cli.ts`)
    - `packages/freshness/src/cli.ts` líneas 294-300 (`const root = process.cwd(); const env = loadEnv(root);` — el `.env` se resuelve DESDE EL CWD; ése es el gotcha v8.1)
    - `packages/freshness/package.json` (donde vive hoy la dependencia `tsx`)
    - 118-CRON-VERDICTS.md §4, fila G10
  </read_first>
  <action>
    Elegir UNA de las dos vías del fix propuesto en G10 y dejar constancia de cuál en un comentario
    del `package.json` o del SUMMARY:
    (a) añadir `tsx` a `devDependencies` de la RAÍZ con la MISMA versión que ya resuelve
        `packages/freshness` (leerla del `pnpm-lock.yaml`/`packages/freshness/package.json`; NO
        introducir un paquete nuevo ni una versión nueva — es hoisting de una dependencia ya
        presente en el lockfile, ver `<threat_model>` T-119-SC); o
    (b) cambiar el script a `pnpm --filter @obs/freshness exec tsx src/cli.ts` PERO SOLO si el
        cwd efectivo sigue siendo la raíz del monorepo — si `pnpm --filter exec` mueve el cwd al
        directorio del paquete, `cli.ts:296` deja de encontrar `.env` y la vía (b) queda
        DESCARTADA (ése es exactamente el gotcha v8.1 citado por G10).
    Verificar empíricamente cuál de las dos preserva el cwd antes de commitear.
    NO tocar `cli.ts` para "arreglar" la resolución de `.env`: el contrato "el `.env` vive en la
    raíz y el cwd es la raíz" se preserva, no se relaja.
  </action>
  <verify>
    <automated>pnpm freshness --json 2>/dev/null | head -c 1 | grep -q '{' && echo OK-JSON</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm freshness --json` desde la raíz emite JSON por stdout (primer carácter `{` o `[`).
    - El exit code es 0 o 1 (1 = alguna fuente stale, legítimo), NUNCA 2 ni 127 (binario ausente).
    - `pnpm freshness --help` no lanza `command not found: tsx`.
    - `git diff pnpm-lock.yaml` no introduce ningún paquete que no estuviera ya en el lockfile
      (si se eligió la vía (a), sólo cambia la ubicación/declaración de `tsx`).
  </acceptance_criteria>
  <done>La herramienta de auditoría arranca desde la raíz y su fallo, si lo hay, es por datos stale y no por entorno.</done>
</task>

<task type="auto">
  <name>Task 2: G2 — workflowYml nullable, sin 404 fabricados</name>
  <read_first>
    - `packages/freshness/src/catalog.ts` líneas 24-42 (JSDoc + `interface FuenteConfig`) y las
      entradas `chilecompra` (:294-314) y `servel` (:316-338), cuyos comentarios YA declaran que
      esos YAML no existen
    - `packages/freshness/src/query-runner.ts` líneas 86-115 (`ghRunSignal`) y :144 (`const ghRun = ghRunSignal(cfg.workflowYml)`)
    - 118-CRON-VERDICTS.md §4, fila G2 (opción (a) RECOMENDADA; crear YAML vacíos = fabricar cobertura = PROHIBIDO)
  </read_first>
  <action>
    Opción (a) de G2, LOCKED por 119-CONTEXT.md:
    1. `FuenteConfig.workflowYml` pasa a `string | null`; actualizar el JSDoc de la línea 25 para
       decir que `null` significa "esta fuente no tiene workflow de GH Actions por decisión
       declarada" (no "aún no lo escribimos").
    2. Poner `workflowYml: null` en las entradas `chilecompra` y `servel`, CONSERVANDO
       byte-a-byte los comentarios que ya explican por qué (gating legal MONEY/SERVEL). Las
       señales siguen reportando `stale:true` — eso es §4.1, estado esperado, NO se toca.
    3. En `query-runner.ts`, cuando `cfg.workflowYml === null`, NO invocar `ghRunSignal` y usar
       la cadena literal `"n/d (sin workflow)"` como `ghRun`. Distinguirla explícitamente de
       `"n/d (sin corridas)"` (workflow existe pero nunca corrió) y de `"n/d"` (la llamada falló):
       las tres son degradaciones distintas y honestas.
    4. NO crear `chilecompra-weekly.yml` ni `servel-weekly.yml`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflowYml: null' packages/freshness/src/catalog.ts` devuelve exactamente 2.
    - `ls .github/workflows/chilecompra-weekly.yml .github/workflows/servel-weekly.yml` sigue fallando (los archivos NO existen).
    - `pnpm freshness 2>&1 >/dev/null | grep -c 'HTTP 404'` devuelve 0.
    - Un test nuevo en `evaluate.test.ts` asevera que `CATALOG.find(c => c.fuente === "servel")!.workflowYml === null` y que la fila sigue evaluando `stale:true` con `ingestado_hasta` null.
    - `tsc -b` exit 0 (el cambio de tipo `string | null` propaga limpio).
  </acceptance_criteria>
  <done>El catálogo ya no apunta a archivos inexistentes y el silencio del 404 se logró declarando la ausencia, no fabricando el archivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: G4 — matar el verde prestado (tabla propia + ghRun en el cálculo de stale)</name>
  <read_first>
    - `packages/freshness/src/catalog.ts` :261-268 (entrada `lobby-camara` → `lobby_audiencia`, que llena W-5) y :286-292 (entrada `fichas` → `proyecto`, que llena W-4)
    - `packages/freshness/src/evaluate.ts` líneas 28-60 (la regla fail-closed vigente: `diasDesdeUpsert === null` ⇒ stale)
    - `packages/freshness/src/evaluate.test.ts` líneas 20-120 (helper `row(...)` y el estilo de los casos)
    - 118-CRON-VERDICTS.md §4, fila G4 (dato delator: `ghRun: "failure @ 2026-07-07"` y `"n/d (sin corridas)"` presentes y NO usados)
  </read_first>
  <behavior>
    - Test 1: `lobby_contraparte` FRESCA (upsert de hoy) + `ghRun: "failure @ 2026-07-07"` ⇒ `stale: true` (el verde prestado ya no encubre la avería).
    - Test 2: tabla fresca + `ghRun: "success @ <hoy>"` ⇒ `stale: false` (no se introduce stale espurio).
    - Test 3: tabla fresca + `ghRun: "n/d (sin corridas)"` ⇒ `stale: true`.
    - Test 4: tabla fresca + `ghRun: "n/d (sin workflow)"` (G2, decisión declarada) ⇒ `stale: false` — la ausencia DECLARADA de workflow no es avería; MONEY/SERVEL no ganan un stale nuevo por esta vía.
    - Test 5: tabla fresca + `ghRun: "n/d"` (la llamada a `gh` falló) ⇒ `stale: false` con el motivo visible — no se puede afirmar avería del cron desde un fallo del instrumento (fail-closed sobre el DATO, no sobre el medidor).
  </behavior>
  <action>
    1. Reapuntar en `catalog.ts`:
       - `lobby-camara`: `tabla: "lobby_contraparte"` (tabla propia del conector de Cámara,
         escrita por `writer-supabase.ts` `upsertContrapartes`). Verificar por psql read-only que
         la columna temporal elegida existe (`fecha_captura`) ANTES de commitear — la lección A2
         de 118 §5 es que las columnas temporales NO son uniformes; si no existiera, elegir la
         que exista y documentarla en el comentario.
       - `fichas`: `tabla: "proyecto_ficha"` con su columna temporal real (misma verificación).
       Actualizar la tabla-resumen del JSDoc de cabecera (líneas 5-14) y la NOTA
       `lobby-camara vs lobby-leylobby` (:16-20), que hoy describe el comportamiento viejo.
    2. En `evaluate.ts`, incorporar `ghRun` al cálculo de `stale` con una función pura auxiliar
       exportada (p.ej. `ghRunEsAveria(ghRun: string): boolean`) que devuelva `true` SOLO para
       `conclusion` distinta de `success`/`skipped` y para `"n/d (sin corridas)"`; `false` para
       `"n/d (sin workflow)"` y `"n/d"`. `stale = staleTemporal || ghRunEsAveria(ghRun)`.
       El motivo debe quedar legible: añadir a `FuenteResult` un campo `motivoStale: string | null`
       (p.ej. `"dias>umbral"`, `"sin dato"`, `"gh-failure"`) para que la tabla del CLI y el `--json`
       digan POR QUÉ. Renderizarlo en la tabla ANSI de `cli.ts` sólo si cabe sin romper el layout;
       si no cabe, exponerlo únicamente en `--json` y declararlo en el SUMMARY.
    3. Escribir los 5 tests de `<behavior>` ANTES del cambio en `evaluate.ts` (RED → GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'lobby_contraparte' packages/freshness/src/catalog.ts` devuelve ≥1 línea y `grep -c 'tabla: "lobby_audiencia"' packages/freshness/src/catalog.ts` devuelve 0.
    - `grep -n 'proyecto_ficha' packages/freshness/src/catalog.ts` devuelve ≥1 línea.
    - Los 5 tests de `<behavior>` existen en `evaluate.test.ts` y pasan; el Test 1 falla si se revierte el cambio de `evaluate.ts` (verificarlo revirtiendo en memoria antes de commitear).
    - `pnpm freshness --json` incluye `motivoStale` en cada fuente.
    - Ninguna fuente pasa de `stale:true` a `stale:false` por este cambio (el fix sólo puede añadir stale honesto, nunca quitarlo): comparar el `--json` antes/después y pegar el diff en el SUMMARY.
  </acceptance_criteria>
  <done>Una avería de `lobby-camara-weekly` o de `fichas-backfill` ya no puede quedar tapada por la frescura de una tabla que llena otro cron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gh` CLI → freshness | salida JSON de un binario externo parseada por el instrumento |
| psql → freshness | SQL construida en `query-runner.ts` a partir del catálogo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | `queryFreshness` SQL | mitigate | `tabla`/`columna` siguen viniendo del enum cerrado del catálogo (nunca de input externo); el reapunte de G4 conserva esa propiedad — cero interpolación de datos de usuario |
| T-119-02 | Information disclosure | stderr de `freshness` | mitigate | los mensajes de error siguen omitiendo URL y credenciales (patrón vigente en `query-runner.ts:77-80`); no añadir el `dbUrl` a ningún log nuevo |
| T-119-03 | Denial of service | `ghRunSignal` | accept | `timeout: 5_000` ya vigente; omitir la llamada cuando `workflowYml` es null la reduce |
| T-119-SC | Tampering | dependencias npm | mitigate | este plan NO introduce ningún paquete nuevo: `tsx` ya está en el lockfile del monorepo (`packages/freshness/node_modules/.bin/tsx`); si el executor se ve tentado de instalar cualquier paquete que no esté ya en `pnpm-lock.yaml`, DETENERSE y escalar — no hay auditoría de legitimidad para paquetes nuevos en esta fase |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` verde.
- `tsc -b` exit 0.
- `pnpm freshness --json` corre desde la raíz, sin 404 en stderr.
- Guard env-example verde (este plan no añade env vars nuevas; si añadiera alguna, placeholder en `.env.example`).
</verification>

<success_criteria>
- G10, G2 y G4 cerrados en código con test que muerde.
- Ninguna señal de frescura pasa de stale a fresca por efecto de este plan.
- Cero YAML de workflow creados para callar un 404.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-01-SUMMARY.md` when done
</output>
