---
phase: 119-cron-fix-robustez-de-ingesta
plan: 04
type: execute
wave: 2
depends_on: ["119-03"]
files_modified:
  - packages/agenda/src/run-agenda-prod-cli.ts
  - packages/agenda/src/ingest-run.ts
  - packages/agenda/src/ingest-run.test.ts
  - packages/identity/src/seed-cli.ts
  - packages/identity/src/seed-cli.test.ts
  - packages/lobby/src/ingest-cli.ts
  - packages/lobby/src/ingest-run.ts
  - packages/lobby/src/ingest-run.test.ts
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "Tras una corrida de agenda, identity y lobby-leylobby, `source_snapshot` tiene filas de esas fuentes"
    - "Cada fila de `source_snapshot` lleva source, resource, r2_path, content_hash y fetched_at reales"
    - "Un fallo del registro de snapshot no aborta la ingesta (best-effort declarado)"
  artifacts:
    - path: "packages/agenda/src/run-agenda-prod-cli.ts"
      provides: "SnapshotWriter montado sobre SupabaseSnapshotStore"
      contains: "SnapshotWriter"
    - path: "packages/identity/src/seed-cli.ts"
      provides: "SnapshotWriter montado"
      contains: "SnapshotWriter"
    - path: "packages/lobby/src/ingest-cli.ts"
      provides: "SnapshotWriter montado"
      contains: "SnapshotWriter"
  key_links:
    - from: "los 3 CLIs"
      to: "tabla source_snapshot"
      via: "SnapshotWriter sobre SupabaseSnapshotStore"
      pattern: "SnapshotWriter"
---

<objective>
Cerrar G5: cuatro conectores escriben crudo a R2 sin dejar traza consultable en `source_snapshot`
(hoy sólo 2 fuentes registradas de 8 conectores: `leyes` 4.380 e `infoprobidad` 3, probe P10).
Sin esa fila, el crudo existe pero no es descubrible desde la DB — y `--from-r2` (plan 05) no
tiene de dónde derivar la ruta.

Purpose: la Etapa 1 sólo es útil si el crudo es localizable. `source_snapshot` es ese índice.
Output: agenda, identity y lobby-leylobby registran su provenance como ya lo hacen tramitación y
probidad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-03-SUMMARY.md

Gap de origen: `118-CRON-VERDICTS.md` §4 fila G5 (línea 1822) + §3.1 (probe P10, columnas reales
de `source_snapshot` verificadas: `source`, `resource`, `r2_path`, `content_hash`, `fetched_at`
— la columna temporal es `fetched_at`, NO `creado_en`: refutación A2 de §5).

<interfaces>
PLANTILLA DORADA — ensamblado del writer (`packages/tramitacion/src/run-tramitacion-prod-cli.ts:213-223`):
```typescript
const snapshotWriter =
  !efectivoDryRun && url && serviceKey
    ? new SnapshotWriter(
        new SupabaseSnapshotStore({ url, serviceKey, createClient: createSupabaseClient }),
      )
    : undefined;
```

PLANTILLA DORADA — la escritura best-effort (`packages/probidad/src/run-probidad-todos.ts:157-166`):
```typescript
if (opts.snapshotWriter) {
  await opts.snapshotWriter.write({
    source: "infoprobidad",
    resource: "declaraciones",
    cacheKey: `infoprobidad:declaraciones:${hasta}`,
    r2Path,
    contentHash: sha,
    fingerprint: sha,
    /* ... */
  });
}
```
Leer la firma completa de `SnapshotWriter.write` y de `SupabaseSnapshotStore` antes de copiar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agenda — SnapshotWriter en el CLI y en los dos puntos de Etapa 1</name>
  <read_first>
    - `packages/agenda/src/run-agenda-prod-cli.ts` :112-165 (construcción del writer Supabase, del `R2Store` y del `runIngest({...})`)
    - `packages/tramitacion/src/run-tramitacion-prod-cli.ts` :213-223 (plantilla dorada del ensamblado)
    - `packages/probidad/src/run-probidad-todos.ts` :150-170 (plantilla dorada de la escritura best-effort)
    - `packages/agenda/src/ingest-run.ts` (los dos bloques de `putImmutable` ya tocados por el plan 03)
    - 118-CRON-VERDICTS.md §4 fila G5
  </read_first>
  <action>
    1. En `run-agenda-prod-cli.ts`, ensamblar `snapshotWriter` copiando LITERALMENTE la forma de
       `run-tramitacion-prod-cli.ts:213-223` (gateado por `!dryRun && url && serviceKey`), y
       pasarlo a `runIngest({ ..., snapshotWriter })`.
    2. En `ingest-run.ts`, tras cada `putImmutable` con `existed:false`, escribir la fila con
       `source: "agenda"`, `resource: "citaciones-semana"` / `"tabla-sala"`, `r2Path`,
       `contentHash: sha`, `fetchedAt` = la fecha de la corrida, y `cacheKey` con la clave de
       semana ISO (`agenda:citaciones-semana:<clave>`) — la MISMA partición que ya usa la key de
       R2 (WR-01), para que el snapshot y el objeto compartan identidad.
    3. Best-effort y NO fatal: envolver en try/catch con log, igual que la plantilla. Un fallo de
       `source_snapshot` no puede tumbar la ingesta ni suprimir el upsert ya hecho.
    4. `source` debe coincidir con lo que `packages/freshness/src/query-runner.ts` consulta en
       `r2SnapshotSignal` (`where source = '<fuente del catálogo>'`) — verificar la correspondencia
       y, si no calza, declarar en el SUMMARY qué nombre se eligió y por qué (no renombrar el
       catálogo sin decirlo).
  </action>
  <verify>
    <automated>pnpm --filter @obs/agenda test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SnapshotWriter' packages/agenda/src/run-agenda-prod-cli.ts` ≥ 1.
    - Test nuevo: con un `snapshotWriter` stub, una corrida con `existed:false` invoca `write` una vez por recurso con `source`, `resource`, `r2Path` y `contentHash` no vacíos.
    - Test nuevo: si `snapshotWriter.write` lanza, la corrida termina OK y el resultado conserva los conteos (best-effort).
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>El crudo de agenda queda indexado en la DB, no sólo depositado en R2.</done>
</task>

<task type="auto">
  <name>Task 2: identity — SnapshotWriter en el seed CLI</name>
  <read_first>
    - `packages/identity/src/seed-cli.ts` :185-215 (`buildR2Target` con el `existed` ya propagado por el plan 03, y `main(opts)`)
    - `packages/tramitacion/src/run-tramitacion-prod-cli.ts` :213-223
    - `.github/workflows/backup-parlamentario.yml` :38-42 (SOLO mapea los 4 `R2_*`: en CI NO hay service key ⇒ el `snapshotWriter` quedará `undefined` allí)
    - 118-CRON-VERDICTS.md §4 fila G5 y §2 W-3
  </read_first>
  <action>
    1. Ensamblar `snapshotWriter` con la plantilla dorada, gateado por presencia de credenciales
       Supabase. HONESTIDAD REQUERIDA: en la corrida de GH Actions de `backup-parlamentario` NO
       hay service key (el propio YAML lo declara), así que allí el writer será `undefined` y NO
       se registrará snapshot — eso es correcto y debe quedar DICHO en un comentario del código y
       en el SUMMARY. No se fabrica una credencial ni se asume que el cron lo cubrirá.
    2. Escribir la fila con `source: "identity"`, `resource: "parlamentario-seed"`, `r2Path`,
       `contentHash: sha`, `cacheKey: identity:parlamentario-seed:<date>`. Best-effort, no fatal.
    3. La ruta que SÍ registra snapshot es la corrida LOCAL del operador (con `.env` completo);
       documentarlo.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SnapshotWriter' packages/identity/src/seed-cli.ts` ≥ 1.
    - Test: sin credenciales Supabase, `snapshotWriter` es `undefined` y la corrida no falla ni loguea un registro fantasma.
    - Test: con stub, `write` se invoca una vez con los 4 campos no vacíos.
    - El comentario que declara "en CI no hay service key ⇒ sin snapshot" está en el archivo.
  </acceptance_criteria>
  <done>Identity registra provenance donde puede y declara honestamente dónde no puede.</done>
</task>

<task type="auto">
  <name>Task 3: lobby-leylobby — SnapshotWriter en el CLI de ingesta</name>
  <read_first>
    - `packages/lobby/src/ingest-cli.ts` :160-230 (construcción de `r2Store`, del `writer` y llamada a `runIngestLobby({...})`)
    - `packages/lobby/src/ingest-run.ts` (punto donde se hace `putImmutable` del crudo por recurso; el conector YA tiene hash-check `existed` → `[skip] sin novedades` según la cabecera de `cursor-leylobby.ts`)
    - `packages/tramitacion/src/run-tramitacion-prod-cli.ts` :213-223
    - 118-CRON-VERDICTS.md §4 fila G5 y §2 W-5
  </read_first>
  <action>
    1. Ensamblar `snapshotWriter` con la plantilla dorada (gateado por `!dryRun && url && serviceKey`,
       usando las MISMAS variables ya resueltas en `ingest-cli.ts:132-147`, incluidos los fallbacks
       `SUPABASE_URL`/`SUPABASE_API_URL` que los workflows inyectan) y pasarlo a `runIngestLobby`.
    2. En `ingest-run.ts`, tras cada `putImmutable` con `existed:false`, escribir la fila con
       `source: "leylobby"`, `resource` = la clave del recurso ingerido (institución/año/página),
       `r2Path`, `contentHash: sha`. Best-effort, no fatal.
    3. NO tocar la lógica del cursor (`avanzarCursor`, `leerCursor`): es materia del plan 06 y
       tiene su propia regla LOCKED (no avanzar sin datos).
  </action>
  <verify>
    <automated>pnpm --filter @obs/lobby test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SnapshotWriter' packages/lobby/src/ingest-cli.ts` ≥ 1.
    - Test: con stub, una corrida con datos invoca `write` una vez por recurso nuevo y CERO veces por recurso con `existed:true`.
    - `git diff packages/lobby/src/cursor-leylobby.ts` vacío (el cursor no se toca en este plan).
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>Los 3 conectores restantes registran provenance; el objetivo de `source_snapshot` pasando de 2 a 5 fuentes queda alcanzable con la primera corrida real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector → Supabase (`source_snapshot`) | escritura con service_role desde CLIs de ingesta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-10 | Repudiation | filas de `source_snapshot` | mitigate | `content_hash` es el sha256 REAL de los bytes puestos en R2 (el mismo que compone la key) — la fila es verificable contra el objeto, no un dato declarativo |
| T-119-11 | Information disclosure | `r2_path` y logs | mitigate | ningún log ni fila incluye credenciales R2/Supabase; `r2Path` es una key content-addressed sin secretos |
| T-119-12 | Tampering | fila fabricada | mitigate | la escritura ocurre SOLO tras un `putImmutable` exitoso con `existed:false`; jamás se registra un snapshot cuyo objeto no se acaba de escribir |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- `pnpm --filter @obs/agenda test`, `@obs/identity test`, `@obs/lobby test` verdes; `tsc -b` exit 0.
- Corrida local acotada de al menos UNO de los tres conectores contra PROD (rate-limit intacto) y
  luego `select source, count(*) from source_snapshot group by 1;` por psql read-only, pegando la
  salida (sin URL) en el SUMMARY. Si la corrida no se puede hacer en la ventana, decirlo así: la
  verificación de "2 → 5 fuentes" queda para el plan de cierre, NUNCA se declara cumplida sin la fila.
</verification>

<success_criteria>
- G5 cerrado en código para los 3 CLIs con test que asevera la escritura y su carácter best-effort.
- Ninguna fila de `source_snapshot` se escribe sin un objeto R2 recién creado detrás.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-04-SUMMARY.md` when done
</output>
