---
phase: 119-cron-fix-robustez-de-ingesta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agenda/src/ingest-run.ts
  - packages/agenda/src/ingest-run.test.ts
  - packages/probidad/src/run-probidad-todos.ts
  - packages/probidad/src/run-probidad-todos.test.ts
  - packages/identity/src/seed-cli.ts
  - packages/identity/src/seed-cli.test.ts
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "Cuando el crudo ya existía en R2 (412), el conector loguea `[skip] sin novedades` y NO re-parsea"
    - "El hash-check antes de gastar trabajo queda respetado en agenda, probidad e identity (regla LOCKED 2 de CLAUDE.md)"
    - "En identity el skip salta la CARGA, no el commit del snapshot git"
  artifacts:
    - path: "packages/agenda/src/ingest-run.ts"
      provides: "existed consumido + guard de skip en los 2 puntos de putImmutable"
      contains: "existed"
    - path: "packages/probidad/src/run-probidad-todos.ts"
      provides: "existed consumido + guard de skip"
      contains: "existed"
    - path: "packages/identity/src/seed-cli.ts"
      provides: "existed propagado por buildR2Target + skip de la carga"
      contains: "existed"
  key_links:
    - from: "packages/ingest/src/r2-store.ts putImmutable"
      to: "los 3 llamadores"
      via: "destructuring de { r2Path, existed }"
      pattern: "existed"
---

<objective>
Cerrar G6: `putImmutable` devuelve `existed` (412 de `If-None-Match: *`) y TRES llamadores lo
descartan en el destructuring ⇒ no hay short-circuit "sin novedades" y se re-parsea siempre,
violando la regla LOCKED 2 de `CLAUDE.md` ("salir temprano cuando no hay novedades").

Purpose: el contrato de R2 ya es correcto (`r2-store.ts:71,79`); lo que falta es consumirlo.
Cada re-parseo evitado es trabajo y riesgo que no se gasta contra fuentes volátiles.
Output: los 3 conectores hacen skip honesto y visible cuando el crudo no cambió.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md

Gap de origen: `118-CRON-VERDICTS.md` §4 fila G6 (línea 1823).

<interfaces>
Contrato R2 (`packages/ingest/src/r2-store.ts:61-80`) — YA correcto, sólo falta consumirlo:
```typescript
async putImmutable(
  source: string, resource: string, date: string, sha: string, ext: string, body: Uint8Array,
): Promise<{ r2Path: string; existed: boolean }>;
// 412 Precondition Failed ⇒ el objeto ya existía ⇒ existed = true ⇒ éxito idempotente
```

PLANTILLA DORADA (`packages/tramitacion/src/ingest-run.ts:294`,`:330`) — copiar esta forma:
```typescript
let r2Path: string; let existed: boolean;
try {
  ({ r2Path, existed } = await opts.r2Store.putImmutable(/* ... */));
} catch (err) { /* Etapa-1-primero es LOCKED: si el crudo NO quedó en R2, NO se escribe el derivado */ }
if (existed) {
  log(`[skip] sin novedades — tramitacion ${boletinFull}`);
  continue;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agenda — `existed` en los dos puntos de Etapa 1</name>
  <read_first>
    - `packages/agenda/src/ingest-run.ts` :140-172 (Cámara citaciones-semana: `const { r2Path: key } = await opts.r2.putImmutable(...)` — `existed` DESCARTADO) y :275-305 (tabla de sala PDF: mismo descarte)
    - `packages/tramitacion/src/ingest-run.ts` :294-336 (plantilla dorada citada por G6)
    - `packages/agenda/src/ingest-run.test.ts` (estilo de los tests, stubs de conector y de R2)
    - 118-CRON-VERDICTS.md §4 fila G6 y §2 W-2
  </read_first>
  <behavior>
    - Test 1: con el store devolviendo `existed:true` para la semana `2026-W20`, el parser de citaciones (`parseCamaraCitaciones`) NO se invoca y el writer no recibe upserts para esa semana.
    - Test 2: con `existed:false`, el flujo actual se conserva byte-a-byte (parser invocado, upsert hecho) — el fix no puede cambiar la ruta feliz.
    - Test 3: el log contiene exactamente el prefijo `[skip] sin novedades` con la fuente y el recurso.
    - Test 4: con R2 deshabilitado (`r2Enabled:false`), no hay skip y todo se parsea (la degradación sin R2 sigue siendo procesar, no saltar).
    - Test 5 (tabla de sala PDF): `existed:true` ⇒ NO se invoca el proveedor LLM DeepSeek (el ahorro más caro del fix).
  </behavior>
  <action>
    En los dos bloques de `putImmutable` de `packages/agenda/src/ingest-run.ts`:
    1. Cambiar el destructuring a `const { r2Path: key, existed } = await opts.r2.putImmutable(...)`.
    2. Inmediatamente después del log de éxito de R2, añadir el guard calcado de
       `tramitacion/src/ingest-run.ts:330`:
       `if (existed) { log("[skip] sin novedades — camara citaciones-semana <clave>"); continue; }`
       En el bloque de la tabla de sala (que no está en un `for`), el equivalente es saltar el
       resto del paso 4 (extracción LLM + upsert), NO abortar la corrida completa.
    3. NO tocar el manejo de error de R2 existente (`semanasSinRespaldoR2`, best-effort): un fallo
       de R2 sigue dejando que la Etapa 2 proceda con los bytes en memoria; sólo el 412 hace skip.
    4. Tests de `<behavior>` ANTES del cambio.
  </action>
  <verify>
    <automated>pnpm --filter @obs/agenda test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'existed' packages/agenda/src/ingest-run.ts` ≥ 4 (dos destructurings + dos guards).
    - Los 5 tests de `<behavior>` existen y pasan; el Test 1 falla si se revierte el guard.
    - `grep -c '\[skip\] sin novedades' packages/agenda/src/ingest-run.ts` = 2.
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>Agenda deja de re-parsear (y de re-gastar DeepSeek) cuando el crudo no cambió.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: probidad — `existed` en el crudo agregado por corrida</name>
  <read_first>
    - `packages/probidad/src/run-probidad-todos.ts` :140-170 (`({ r2Path } = await opts.r2Store.putImmutable("infoprobidad","declaraciones",hasta,sha,"json",bytes))` — `existed` descartado; nótese que aquí la Etapa 1 corre DESPUÉS del upsert a Supabase)
    - `packages/tramitacion/src/ingest-run.ts` :294-336 (plantilla dorada)
    - `packages/probidad/src/run-probidad-todos.test.ts`
    - 118-CRON-VERDICTS.md §4 fila G6 y §2 W-6
  </read_first>
  <behavior>
    - Test 1: con `existed:true`, el resultado incluye una marca de "sin novedades" y el log contiene `[skip] sin novedades — infoprobidad declaraciones`.
    - Test 2: con `existed:false`, comportamiento actual intacto (`r2Path` seteado, `snapshotWriter.write` invocado).
    - Test 3: con `existed:true`, el `snapshotWriter.write` NO se invoca (no se registra una provenance nueva para un objeto que ya estaba registrado) — salvo que el executor descubra que el registro previo no existe, en cuyo caso documentar la decisión contraria y su razón.
  </behavior>
  <action>
    1. Destructurar `existed` junto a `r2Path`.
    2. Añadir el guard de skip con el log calcado de la plantilla dorada. ATENCIÓN al orden real
       de este conector: la carga a Supabase YA ocurrió arriba (`marcarIngestado`), así que el
       skip aquí NO puede "saltarse la carga" — lo que evita es re-registrar snapshot y deja
       constancia de que el crudo de esta corrida es idéntico al de la anterior. Documentarlo en
       un comentario: es una divergencia deliberada respecto de la plantilla, no un descuido.
    3. Si el executor concluye que el orden correcto es mover la Etapa 1 ANTES de la carga (para
       que el skip ahorre trabajo de verdad), NO hacerlo en este plan: registrarlo como hallazgo
       en el SUMMARY para el documento de cierre. Este plan no reordena etapas.
    4. Tests de `<behavior>` ANTES del cambio.
  </action>
  <verify>
    <automated>pnpm --filter @obs/probidad test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'existed' packages/probidad/src/run-probidad-todos.ts` ≥ 2.
    - `grep -c '\[skip\] sin novedades' packages/probidad/src/run-probidad-todos.ts` ≥ 1.
    - Los tests de `<behavior>` existen y pasan.
    - El comentario que declara la divergencia de orden respecto de la plantilla dorada está en el archivo.
  </acceptance_criteria>
  <done>Probidad consume `existed` y su divergencia de orden queda declarada, no oculta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: identity — `existed` sin romper el commit del snapshot git</name>
  <read_first>
    - `packages/identity/src/seed-cli.ts` :185-206 (`buildR2Target` → `const { r2Path } = await store.putImmutable("identity","parlamentario-seed",date,sha,"json",body)`; el target expone `put(content): Promise<string>` y pierde `existed`) y el punto donde se consume el target (~:201 según G6)
    - `.github/workflows/backup-parlamentario.yml` :38-42, :60 (el YAML declara: "SIN service key local en CI → la carga a DB se omite; el snapshot git es autoritativo" — refutación A4 de 118 §5)
    - `packages/identity/src/seed-cli.test.ts`
    - 118-CRON-VERDICTS.md §4 fila G6 (paso 4: "el skip debe saltar la carga, no el commit del snapshot git") y §2 W-3
  </read_first>
  <behavior>
    - Test 1: con `existed:true`, la carga a DB (writer Supabase) NO se invoca y el log dice `[skip] sin novedades — identity parlamentario-seed`.
    - Test 2: con `existed:true`, la ESCRITURA del seed al filesystem (que el bot commitea) SÍ ocurre — el snapshot git sigue siendo autoritativo y no puede quedarse sin actualizar por un skip de R2.
    - Test 3: con `existed:false`, comportamiento actual intacto.
    - Test 4: sin credenciales R2 (`buildR2Target()` devuelve null), no hay skip y todo procede (no-op explícito, no falso skip).
  </behavior>
  <action>
    1. Cambiar el contrato de `R2BackupTarget.put` para devolver `{ r2Path: string; existed: boolean }`
       en vez de `string`, propagando el `existed` de `putImmutable`. Actualizar todos los
       llamadores y los stubs de test.
    2. En el punto de consumo, añadir el guard de skip con el log calcado de la plantilla dorada,
       aplicado SOLO a la carga a Supabase. La escritura del seed a disco (`writer-fs.ts`) y el
       commit del bot quedan FUERA del skip — con cita al comentario del YAML en el código.
    3. Tests de `<behavior>` ANTES del cambio.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'existed' packages/identity/src/seed-cli.ts` ≥ 2.
    - Los 4 tests de `<behavior>` existen y pasan; el Test 2 falla si el skip se coloca antes de la escritura del seed.
    - `tsc -b` exit 0 (el cambio de firma de `R2BackupTarget.put` propaga limpio).
  </acceptance_criteria>
  <done>Identity hace skip de la carga sin poner en riesgo el artefacto autoritativo (el seed commiteado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente gubernamental → conector | HTML/XML/JSON no confiable que cruza a parseo |
| conector → R2 | escritura content-addressed inmutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-07 | Tampering | short-circuit por `existed` | mitigate | el skip se dispara SOLO por el 412 de R2 (hash del contenido), nunca por una heurística de fecha o de tamaño — un contenido distinto produce un sha distinto y NO hace skip |
| T-119-08 | Repudiation | pérdida de traza al saltar | mitigate | todo skip emite log `[skip] sin novedades — <fuente> <recurso>`; un skip silencioso es indistinguible de una corrida rota |
| T-119-09 | Denial of service | fuente gubernamental | mitigate | el skip REDUCE la carga sobre la fuente; el rate-limit 2-3s/host y el UA identificatorio NO se tocan en este plan |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- `pnpm --filter @obs/agenda test`, `pnpm --filter @obs/probidad test`, `pnpm --filter @obs/identity test` verdes.
- `tsc -b` exit 0.
- Corrida local acotada de UNO de los conectores (preferentemente agenda, `--dry-run` o con R2 real
  y una sola semana) mostrando el `[skip] sin novedades` en la segunda corrida consecutiva. Si toca
  la fuente, respetar el rate-limit 2-3s vigente en el conector (no bypassarlo para ir rápido).
</verification>

<success_criteria>
- G6 cerrado en los 3 conectores con test que asevera que el parser NO se invoca ante `existed:true`.
- Cero cambios en el rate-limit, el UA, robots.txt o el orden fuente→R2→Supabase.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-03-SUMMARY.md` when done
</output>
