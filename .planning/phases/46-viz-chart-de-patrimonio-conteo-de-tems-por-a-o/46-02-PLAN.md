---
phase: 46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified: []
autonomous: false
requirements: [VIZ-02]

must_haves:
  truths:
    - "El build OpenNext/Cloudflare en Docker Linux NO se rompe con Recharts en el bundle"
    - "El deploy wrangler sirve la ficha con el chart de patrimonio renderizado en el navegador"
    - "El worker NO devuelve 500 (Windows build prohibido)"
  artifacts: []
  key_links:
    - from: "isla cliente patrimonio-chart.tsx (Recharts)"
      to: "client JS bundle (no worker server bundle)"
      via: "\"use client\" → no entra al module graph del worker (precedente @xyflow/react en /red)"
      pattern: "use client"
---

<objective>
Validar que la isla Recharts (Plan 46-01) no rompe el build OpenNext/Cloudflare y desplegarla. El build DEBE correr en Docker Linux (el build Windows produce un worker que 500ea — MEMORY.md Camino A); el deploy wrangler es acción de operador (creds Cloudflare NO están en `.env`/CI). El precedente `@xyflow/react` (1.2 MB) ya embarca por esta misma ruta en `/red`, de-riskeando el bundle.

Purpose: cerrar VIZ-02 (el build no se rompe) — la única parte de la fase que no es autónoma.
Output: build Docker Linux verde + sitio desplegado con el chart de patrimonio visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-CONTEXT.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-RESEARCH.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operador — build OpenNext en Docker Linux + deploy wrangler + verificación visual</name>
  <what-built>
    Plan 46-01 agregó la isla Recharts `patrimonio-chart.tsx` (`"use client"`) + el shell server (caveat/degrade/footer) en la sección de patrimonio. La suite `app/` + `tsc -b` + lockdown guard están verdes. Falta confirmar que el bundle del worker no se rompe y desplegar — pasos que requieren Docker Linux + creds Cloudflare (operador).
  </what-built>
  <how-to-verify>
    1. Confirmar suite verde una vez más: `cd app && pnpm test && pnpm typecheck` (y `tsc -b` en la raíz).
    2. Build OpenNext en **Docker Linux** (NUNCA build Windows — produce worker 500ea, MEMORY.md): construir el worker con el flujo Camino A establecido (`pnpm cf-build` / `opennextjs-cloudflare build` dentro del contenedor Linux). Confirmar que el build termina sin error y que el tamaño del worker no excede el límite (Recharts es client-only → cae en el JS de cliente, no en el bundle del worker; el precedente @xyflow/react 1.2 MB ya embarca en esta cuenta Pro). Si el upload se rechaza por tamaño, aplicar `experimental.optimizePackageImports: ['recharts']` en `next.config.ts` (mitigación opcional, 46-RESEARCH Pitfall 2) y reconstruir.
    3. NO agregar `export const runtime = "edge"` en ninguna ruta (OpenNext no soporta Edge — 46-RESEARCH Anti-Patterns).
    4. Deploy con `wrangler` local (OAuth; CI no tiene creds CF).
    5. Verificación visual: abrir `/parlamentario/<id>` de un parlamentario con >=2 declaraciones (cobertura: 135 con >=2 años) → confirmar que el acordeón de patrimonio muestra el chart de conteo por año con el caveat de montos y el footer CC BY 4.0; abrir uno con <2 → confirmar el degrade "datos insuficientes para una tendencia" sin chart. Confirmar que el worker NO devuelve 500.
  </how-to-verify>
  <verify>
    <automated>cd app && pnpm test && pnpm typecheck</automated>
    <human-check>Build Docker Linux verde, worker dentro del límite de tamaño, deploy wrangler exitoso, chart visible en >=2 declaraciones y degrade en <2, worker sin 500.</human-check>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test && pnpm typecheck` verde antes del build.
    - El build OpenNext en Docker Linux termina sin error; el worker se sube dentro del límite de tamaño.
    - Ningún `runtime = "edge"` introducido.
    - El sitio desplegado renderiza el chart de patrimonio (>=2 declaraciones) y el degrade honesto (<2) sin que el worker devuelva 500.
  </acceptance_criteria>
  <resume-signal>Escribir "deploy verde" cuando el build Docker Linux + deploy estén confirmados y el chart se vea en producción, o describir el fallo (p.ej. tamaño de worker, 500) para replanificar.</resume-signal>
</task>

</tasks>

<verification>
- Suite `app/` + `tsc -b` verdes (re-confirmado por el operador antes del build).
- Build OpenNext en Docker Linux sin error; worker dentro del límite.
- Sitio desplegado: chart visible con >=2 declaraciones; degrade honesto con <2; worker sin 500.
</verification>

<success_criteria>
- VIZ-02 (parte de build/deploy): el build OpenNext/Cloudflare no se rompe con Recharts (validado en Docker Linux, no Windows); sitio desplegado con el chart visible.
</success_criteria>

<output>
Create `.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-02-SUMMARY.md` when done
</output>
