---
phase: 46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/pnpm-lock.yaml
  - app/components/patrimonio-de-parlamentario.tsx
  - app/components/patrimonio-chart.tsx
  - app/components/patrimonio-chart.test.tsx
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]

must_haves:
  truths:
    - "Con >=2 declaraciones, la sección de patrimonio muestra un chart del conteo de ítems por año dentro del acordeón de F45"
    - "El chart rotula el tipo de declaración y nunca fusiona versiones incomparables en una serie continua"
    - "No se grafica ningún monto; un caveat visible dice 'Montos no disponibles como cifra en la fuente'"
    - "Con <2 declaraciones se muestra 'datos insuficientes para una tendencia' y NO se monta el chart"
    - "El pie del chart muestra fuente+fecha+enlace (CC BY 4.0 CPLT) vía AtribucionCcBy"
    - "El copy del chart pasa el negative-match de vocabulario prohibido (sin verbo causal/veredicto)"
    - "El chart es una isla \"use client\"; el resto de la ficha sigue SSR"
    - "El guard de lockdown sigue verde: cero RPC nueva fuera del allowlist, cero .from('parlamentario')"
  artifacts:
    - path: "app/components/patrimonio-chart.tsx"
      provides: "Isla cliente Recharts (BarChart apilado); recibe SeriePunto[]"
      contains: "\"use client\""
    - path: "app/components/patrimonio-de-parlamentario.tsx"
      provides: "Transform puro seriePatrimonio() + tipo SeriePunto + shell server (caveat, degrade, footer)"
      contains: "export function seriePatrimonio"
    - path: "app/components/patrimonio-chart.test.tsx"
      provides: "Tests: transform (VIZ-01) + shell copy/caveat/degrade/footer/banned-vocab (VIZ-03) + source-scan use-client (VIZ-02)"
    - path: "app/package.json"
      provides: "Dependencia recharts@3.9.0"
      contains: "recharts"
  key_links:
    - from: "app/components/patrimonio-de-parlamentario.tsx (PatrimonioSection)"
      to: "seriePatrimonio(todas)"
      via: "transform sobre el set COMPLETO `todas`, NO la rebanada paginada `versiones`"
      pattern: "seriePatrimonio\\(\\s*todas"
    - from: "app/components/patrimonio-chart.tsx"
      to: "recharts + type SeriePunto"
      via: "import de recharts + import type-only; nunca @/lib/supabase"
      pattern: "from \"recharts\""
    - from: "app/components/patrimonio-de-parlamentario.tsx (shell)"
      to: "AtribucionCcBy + caveat montos"
      via: "footer server-rendered reusando el componente existente"
      pattern: "AtribucionCcBy"
---

<objective>
Agregar UN gráfico descriptivo dentro del acordeón de patrimonio de `/parlamentario/[id]` (creado en F45): serie temporal del CONTEO de ítems declarados por año (`declaracion.fecha_presentacion`), apilado por `tipo_bien`, rotulado por tipo de declaración. Solo conteos — NUNCA montos (son URIs CPLT → caveat honesto). Recharts como isla `"use client"`; el resto de la ficha sigue SSR. Cero RPC nueva, cero segunda query: el transform puro `seriePatrimonio()` deriva del `todas: DeclaracionVersionRow[]` que `PatrimonioSection` YA computa.

Purpose: cumplir VIZ-01/02/03 — el único chart con cobertura densa hoy (135 parlamentarios con >=2 años), descriptivo y nunca causal, sin romper el lockdown ni el SSR.
Output: recharts instalado; `seriePatrimonio()` + `SeriePunto` + shell server en `patrimonio-de-parlamentario.tsx`; isla `patrimonio-chart.tsx`; suite `patrimonio-chart.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-CONTEXT.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-RESEARCH.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-PATTERNS.md
@.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-VALIDATION.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código; NO explorar. -->

De app/components/patrimonio-de-parlamentario.tsx (server file que se MODIFICA):
- `PatrimonioSection` (Server Component): YA llama `sb.rpc("declaraciones_de_parlamentario", {p_id})` (:669) y `sb.rpc("bienes_de_parlamentario", {p_id})` (:683); construye `const todas = modelarVersiones(filas, id, bienesPorFuente)` (:696, SET COMPLETO, todos los años) y luego `const versiones = todas.slice(start, start + PAGE_SIZE)` (:716, REBANADA paginada). El chart usa `todas`, NUNCA `versiones`.
- `export function PatrimonioView({ data }: { data: PatrimonioViewData })` (:348) — vista PURA, RTL-testeada. Estado (c) "con versiones" en :391-414 (intro + `<p>` conteo neutro + `<ul>` de VersionRow). El shell del chart se monta aquí.
- `export interface PatrimonioViewData` (:69) — agregar el campo `serie: SeriePunto[]`. [Rule 3] TODO sitio que construya este tipo (la llamada en PatrimonioSection :738 + cualquier fixture en patrimonio-de-parlamentario.test.tsx) debe pasar el nuevo campo o el build rompe.
- `const ORDEN_GRUPOS_BIENES: ReadonlyArray<{ tipo: TipoBien; label: string }>` (:123-130) — orden + labels NOUN: "Bienes inmuebles", "Actividades e intereses", "Acciones y derechos", "Valores", "Bienes muebles", "Pasivos". Las leyendas del chart REUSAN estos labels.
- `function AtribucionCcBy()` (:96-113) — footer CC BY 4.0 ("Fuente: InfoProbidad — Consejo para la Transparencia. Datos bajo licencia CC BY 4.0."); ya string-matched por tests `/Datos bajo licencia CC BY 4\.0/`. REUSAR, no reconstruir.
- `const CC_BY_40_URL` (:66).

De app/lib/types.ts (importar, no redefinir):
- `type DeclaracionVersionRow` con `tipo: string` (tipo de declaración), `fecha_presentacion: string` (ISO), `bienes: BienRpcRow[]`.
- `type BienRpcRow` con `tipo_bien: TipoBien`.
- `type TipoBien = "inmueble" | "mueble" | "actividad" | "pasivo" | "accion_derecho" | "valor"`.

Analogo de la isla — app/components/red/red-graph.tsx:
- `:1-12` header `"use client"` + import de la viz-lib (swap `@xyflow/react` → `recharts`; Recharts NO trae CSS, omitir el import de style.css).
- `:48-76` props interface = array plano serializado (strings/numbers; nunca Date, nunca cliente Supabase).
- `:150-161` estado vacío/degrade retornado ANTES del canvas.

Analogo de tests — app/components/red/red-graph.test.tsx `:24-79` (`vi.mock("@xyflow/react", …)` + polyfill `ResizeObserver` en `beforeAll`); app/components/patrimonio-de-parlamentario.test.tsx `:24-30` (regexes `PROHIBIDO_VEREDICTO`/`PROHIBIDO_CONECTIVO`/`PATRON_RUT`), `:33-70` (factories `makeVersion`/`makeBien`); app/components/carril-accordion.test.tsx `:73-86` (source-scan `readFileSync` + asserts no `createServerSupabase`/`@/lib/supabase`).

Allowlist (no tocar) — app/lib/lockdown-guard.test.ts: `PUBLIC_RPC_ALLOWLIST` YA incluye `declaraciones_de_parlamentario` + `bienes_de_parlamentario`. Esta fase agrega CERO `sb.rpc`/`sb.from`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar recharts + tipo SeriePunto + transform puro seriePatrimonio() con tests</name>
  <read_first>
    - app/components/patrimonio-de-parlamentario.tsx (modificado: agregar tipo + transform; mirar :69 PatrimonioViewData, :123-130 ORDEN_GRUPOS_BIENES, :186-194 idiom de helper puro `agruparBienesPorTipo`, :696 `todas`)
    - app/lib/types.ts (tipos DeclaracionVersionRow / BienRpcRow / TipoBien)
    - app/components/patrimonio-de-parlamentario.test.tsx (:24-30 regexes, :33-70 factories makeVersion/makeBien)
    - app/package.json (:25 entrada `@xyflow/react` como precedente de pin exacto)
    - 46-RESEARCH.md (Package Legitimacy Audit: recharts [OK]/Approved; SeriePunto shape)
  </read_first>
  <behavior>
    - seriePatrimonio([v1]) con v1.bienes = 2 inmuebles + 1 pasivo → [{anio: Number(v1.fecha_presentacion.slice(0,4)), tipo_declaracion: v1.tipo, inmueble:2, pasivo:1, mueble:0, actividad:0, accion_derecho:0, valor:0}].
    - El año se deriva de `fecha_presentacion.slice(0,4)` como number — NUNCA `new Date()` (la salida cruza al cliente y debe ser JSON plano: strings+numbers).
    - Dos versiones del MISMO año pero distinto `tipo` (periódica vs rectificación) producen DOS puntos distintos con `tipo_declaracion` distinto — el transform NUNCA las fusiona en una serie comparable (VIZ-01 "no peras con manzanas").
    - seriePatrimonio([]) → [] y seriePatrimonio con 1 versión → array de length 1 (el degrade <2 lo decide el shell, no el transform).
  </behavior>
  <action>
    Instalar Recharts con pnpm dentro de `app/`: `cd app && pnpm add recharts` (pin exacto resultante `recharts@3.9.0`, vecino alfabético de `react`; NUNCA npm — MEMORY.md "pnpm no npm"). Confirmar que el lockfile `app/pnpm-lock.yaml` queda actualizado. La legitimidad de recharts ya está auditada (46-RESEARCH Package Legitimacy Audit: slopcheck [OK], 53.5M dl/sem, sin postinstall) → no requiere checkpoint humano.
    En `app/components/patrimonio-de-parlamentario.tsx` exportar el tipo `SeriePunto` (`{ anio: number; tipo_declaracion: string; inmueble: number; mueble: number; actividad: number; pasivo: number; accion_derecho: number; valor: number }`) y la función PURA `export function seriePatrimonio(versiones: DeclaracionVersionRow[]): SeriePunto[]`, colocada junto a los helpers puros existentes (espejo de `agruparBienesPorTipo` :186-194). Cuenta `v.bienes` por `tipo_bien` por versión (inicializando los 6 `TipoBien` en 0), deriva `anio` de `v.fecha_presentacion.slice(0,4)` como number, y carga `v.tipo` en `tipo_declaracion`. Mapea una versión = un SeriePunto (preserva el tipo de declaración distinto; nunca agrupa por año cruzando tipos). Cero `Date`, cero monto, cero lectura de `contenido`.
    Escribir las pruebas del transform en `app/components/patrimonio-chart.test.tsx` (nuevo) reusando los factories `makeVersion`/`makeBien` (patrimonio-de-parlamentario.test.tsx :33-70): conteos por `tipo_bien` por versión, año derivado del string ISO, `tipo_declaracion` mantenido distinto entre dos versiones del mismo año, y los casos `[]`/1-versión.
  </action>
  <verify>
    <automated>cd app && pnpm test patrimonio-chart && pnpm typecheck</automated>
    <automated>cd app; Select-String -Path package.json -Pattern '"recharts"'</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test patrimonio-chart` verde con los tests del transform (conteos, año-desde-ISO, tipos de declaración no fusionados, `[]`/1-versión).
    - `app/package.json` contiene `"recharts": "3.9.0"` en `dependencies` y `app/pnpm-lock.yaml` resuelve recharts.
    - `cd app && pnpm typecheck` limpio; `SeriePunto` y `seriePatrimonio` exportados desde `patrimonio-de-parlamentario.tsx`.
    - El transform NO usa `new Date`, NO lee `contenido`, NO referencia montos (grep negativo sobre la función: sin `new Date`, sin `monto`, sin `valor_plaza`).
    - El transform NO agrega un `sb.rpc`/`sb.from` (no es un componente server; es pura) — `cd app && pnpm test lockdown-guard` sigue verde.
  </acceptance_criteria>
  <done>recharts@3.9.0 instalado; `SeriePunto` + `seriePatrimonio()` exportados y probados (transform verde); typecheck limpio; guard verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isla cliente patrimonio-chart.tsx + shell server (caveat/degrade/footer) cableado con `todas`</name>
  <read_first>
    - app/components/patrimonio-de-parlamentario.tsx (modificado: shell + mount en PatrimonioView estado (c) :391-414; PatrimonioSection :736-738 pasa data; AtribucionCcBy :96-113; ORDEN_GRUPOS_BIENES :123-130; degrade neutral idiom en DeclaracionComparacion :461-472)
    - app/components/patrimonio-chart.tsx (NUEVO)
    - app/components/red/red-graph.tsx (:1-12 header use-client + import viz-lib; :48-76 props array plano; :150-161 estado vacío antes del canvas)
    - app/components/red/red-graph.test.tsx (:24-79 vi.mock + polyfill ResizeObserver)
    - app/components/patrimonio-de-parlamentario.test.tsx (:24-30 regexes banned-vocab)
    - app/components/carril-accordion.test.tsx (:73-86 source-scan no-leak)
    - app/lib/lockdown-guard.test.ts (allowlist; debe seguir verde)
    - .planning/phases/19-...-frontend/DESIGN-SYSTEM.md (§1 civic tokens — fills neutros; §6 vocabulario prohibido)
  </read_first>
  <behavior>
    - Con serie.length >= 2 el shell monta `<PatrimonioChart serie={serie} />` (la isla); con serie.length < 2 el shell renderiza "datos insuficientes para una tendencia" y NO monta la isla (degrade decidido server-side, grep-testable sin SVG).
    - El shell SIEMPRE renderiza el caveat "Montos no disponibles como cifra en la fuente" y el footer `<AtribucionCcBy />` (CC BY 4.0 CPLT).
    - El `container.textContent` del shell pasa `PROHIBIDO_VEREDICTO` y `PROHIBIDO_CONECTIVO` (sin "aumentó"/"variación"/"patrimonio total"/"conflicto") y no contiene un RUT (`PATRON_RUT`). El label de eje es "N.º de bienes declarados por año"; las leyendas reusan los labels NOUN de `ORDEN_GRUPOS_BIENES`.
    - La isla `patrimonio-chart.tsx` empieza con `"use client"`, importa `recharts` y SOLO el `type { SeriePunto }`; NUNCA importa `createServerSupabase` ni `@/lib/supabase` (source-scan).
  </behavior>
  <action>
    Crear `app/components/patrimonio-chart.tsx` como isla `"use client"` (espejo de red-graph.tsx :1-12): importar `{ BarChart, Bar, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer } from "recharts"` (sin import de CSS) y `import type { SeriePunto } from "./patrimonio-de-parlamentario"`. Exportar `export function PatrimonioChart({ serie }: { serie: SeriePunto[] })` que renderiza un `ResponsiveContainer` → `BarChart data={serie}` con `XAxis dataKey="anio"`, `YAxis allowDecimals={false}`, `Tooltip`, `Legend` y un `<Bar>` APILADO (mismo `stackId`) por cada `tipo_bien`, con `name` = el label NOUN de `ORDEN_GRUPOS_BIENES`. Tipo de chart = BarChart apilado discreto keyed por año/tipo de declaración — NUNCA línea/área conectada (insinuaría una "tendencia" de riqueza entre versiones incomparables; constraint de honestidad HARD). Fills = tokens cívicos neutros/petróleo (DESIGN-SYSTEM §1); PROHIBIDO reusar `--camara`/`--senado` (lectura de identidad política). `aria-label="N.º de bienes declarados por año"`. NO agregar `export const runtime = "edge"` (OpenNext no lo soporta).
    En `app/components/patrimonio-de-parlamentario.tsx` agregar un shell server (función local, p.ej. `PatrimonioChartShell({ serie }: { serie: SeriePunto[] })`) que: si `serie.length < 2` retorna el degrade "datos insuficientes para una tendencia" (framing de hecho neutro, espejo del idiom :461-472) y NO monta la isla; si `serie.length >= 2` monta `<PatrimonioChart serie={serie} />`. En ambos casos renderiza el caveat muted "Montos no disponibles como cifra en la fuente" y el footer `<AtribucionCcBy />`. Agregar `serie: SeriePunto[]` a `PatrimonioViewData` (:69), computar `seriePatrimonio(todas)` en `PatrimonioSection` (sobre el SET COMPLETO `todas` :696, NO la rebanada `versiones`) y pasarlo en el objeto `data` (:738). Montar `<PatrimonioChartShell serie={data.serie} />` dentro de `PatrimonioView` estado (c) (:391-414), tras el `<p>` de conteo neutro. [Rule 3] actualizar TODA construcción de `PatrimonioViewData` (la de :738 + cualquier fixture en el test) con el nuevo campo `serie`.
    Extender `app/components/patrimonio-chart.test.tsx`: mockear `recharts` con dobles ligeros + polyfill `ResizeObserver` en `beforeAll` (espejo red-graph.test.tsx :24-79); tests RTL del shell — presencia del caveat de montos, degrade con <2 (y la isla NO montada), footer CC BY 4.0, y negative-match de `PROHIBIDO_VEREDICTO`/`PROHIBIDO_CONECTIVO`/`PATRON_RUT` sobre `container.textContent` (reusar las regexes de patrimonio-de-parlamentario.test.tsx :24-30, importadas o duplicadas). Agregar el source-scan (carril-accordion.test.tsx :73-86): `readFileSync` de `patrimonio-chart.tsx` → `toMatch(/^"use client"/)`, `not.toMatch(/createServerSupabase/)`, `not.toMatch(/@\/lib\/supabase/)`.
  </action>
  <verify>
    <automated>cd app && pnpm test patrimonio-chart && pnpm test lockdown-guard && pnpm typecheck</automated>
    <automated>cd app && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test patrimonio-chart` verde: caveat presente, degrade <2 sin isla montada, footer CC BY 4.0, negative-match banned-vocab + sin RUT, source-scan `"use client"` + sin `@/lib/supabase`.
    - `app/components/patrimonio-chart.tsx` empieza con `"use client"`, importa `recharts`, NO importa `@/lib/supabase`/`createServerSupabase`; NO contiene `runtime = "edge"` ni línea/área conectada (sin `LineChart`/`AreaChart`/`<Line`/`<Area`); fills sin `--camara`/`--senado`.
    - `seriePatrimonio` se invoca sobre `todas` (no `versiones`) en `PatrimonioSection` — grep `seriePatrimonio\(\s*todas`.
    - `cd app && pnpm test lockdown-guard` verde (cero `sb.rpc`/`sb.from` nuevos; cero `.from('parlamentario')`).
    - Suite completa `cd app && pnpm test` verde + `pnpm typecheck` limpio (incluido el campo `serie` en todas las construcciones de `PatrimonioViewData`).
  </acceptance_criteria>
  <done>Isla `patrimonio-chart.tsx` (BarChart apilado, use-client) + shell server (caveat/degrade/footer) montados en la sección de patrimonio alimentados por `seriePatrimonio(todas)`; suite + typecheck + guard verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (param `[id]`) | Entrada del usuario; ya validada por `PARLAMENTARIO_ID_RE` (sin input nuevo en esta fase) |
| server (service_role) → isla cliente | El RSC pasa SOLO un array serializado de conteos a `patrimonio-chart.tsx`; nada de la conexión service_role cruza |
| npm install (recharts) | Cadena de suministro: dep nueva entra al build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | datos del chart | mitigate | El RPC es PII-safe por contrato; solo conteos (sin `contenido`, sin nombres). Tests banned-vocab + `PATRON_RUT` aseveran cero RUT en el DOM del shell. |
| T-46-02 | Elevation of Privilege | nueva `.rpc`/`.from` (service_role corre cualquier RPC) | mitigate | `lockdown-guard.test.ts` falla CI ante cualquier `.rpc` fuera del allowlist o `.from('parlamentario')`; esta fase agrega CERO. seriePatrimonio deriva de `todas` ya fetcheado. |
| T-46-03 | Information Disclosure | fuga de service_role al cliente vía la isla | mitigate | La isla recibe solo `SeriePunto[]` plano; source-scan test asevera que `patrimonio-chart.tsx` nunca importa `@/lib/supabase`/`createServerSupabase`. |
| T-46-SC | Tampering | `pnpm add recharts` | mitigate | Auditoría de legitimidad en 46-RESEARCH (slopcheck [OK], registry mature 53.5M dl/sem, source repo, sin postinstall) → Approved; pin exacto `3.9.0`. No requiere checkpoint humano ([OK], no [ASSUMED]/[SUS]). |
</threat_model>

<verification>
- `cd app && pnpm test` (suite completa) verde.
- `cd app && pnpm typecheck` limpio + `tsc -b` limpio.
- `cd app && pnpm test lockdown-guard` verde (cero RPC nueva, cero `.from('parlamentario')`).
- `cd app && pnpm test patrimonio-chart` verde (transform + shell copy/caveat/degrade/footer/banned-vocab + source-scan use-client).
- El build OpenNext/Cloudflare en Docker Linux se valida en el Plan 46-02 (checkpoint operador), NO aquí.
</verification>

<success_criteria>
- VIZ-01: chart = serie temporal del conteo de ítems por `fecha_presentacion`, rotulado por tipo de declaración, vía RPCs ya allowlisted; sin montos (caveat honesto); degrade "datos insuficientes para una tendencia" con <2 declaraciones.
- VIZ-02: recharts instalado; chart como isla `"use client"`; resto SSR; `pnpm test` + `tsc -b` verdes (build Cloudflare se confirma en 46-02).
- VIZ-03: descriptivo/neutro (negative-match verde), fuente+fecha+enlace CC BY 4.0 al pie; sin RPC nueva ni `.from('parlamentario')`; guard verde.
</success_criteria>

<output>
Create `.planning/phases/46-viz-chart-de-patrimonio-conteo-de-tems-por-a-o/46-01-SUMMARY.md` when done
</output>
