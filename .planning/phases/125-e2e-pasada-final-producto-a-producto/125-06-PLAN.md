---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 06
type: execute
wave: 3
depends_on: ["125-01", "125-02", "125-03", "125-04"]
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/captures/
autonomous: false
requirements: [E2E-01]

must_haves:
  truths:
    - "Las superficies que sólo existen tras hidratación (islands, acordeones, grafo, filtros) se recorren con BrowserOS sobre el deploy real"
    - "Cada superficie recorrida deja evidencia: contenido de página renderizado + captura"
    - "Lo que el recorrido por HTML servido no podía ver queda cubierto o declarado como no cubierto"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md"
      provides: "Pasada BrowserOS con evidencia por superficie y criterio de estratificación declarado"
      contains: "estratificación"
  key_links:
    - from: "scripts/bros-cli.mjs"
      to: "MCP http://127.0.0.1:9200/mcp"
      via: "new_hidden_page / get_page_content / save_screenshot"
      pattern: "bros-cli"
---

<objective>
Recorrer con **BrowserOS** sobre el deploy real el subconjunto estratificado de superficies que el
recorrido por HTML servido (Planes 02-04) **no puede** ver: contenido que sólo aparece tras hidratación
—islands cliente, acordeones, el grafo de `/red`, los filtros de `/buscar`, el header nav—, dejando
contenido renderizado + captura por superficie.

Purpose: SC2 del ROADMAP en su parte literal ("se recorre con BrowserOS").
Output: `125-E2E-D-BROWSEROS.md` + capturas en `captures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-A-FICHAS.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md
@scripts/bros-cli.mjs
</context>

<gotchas_de_browseros>
- MCP en `http://127.0.0.1:9200/mcp`. Usar **páginas ocultas** (`new_hidden_page`) para no interferir con
  el navegador del operador.
- El **Page ID INCREMENTA** por sesión: parsear `Page ID: N` de la salida de `open`, jamás asumir `1`.
- Tras `open`, esperar **4-5 s** antes de leer (SSR + hidratación).
- **Screenshots en ráfaga tumban el MCP** → `sleep` de **8-10 s** entre capturas.
- `save_screenshot` usa `{page:number, path:string}` (NO `pageId`/`filePath`); ruta ABSOLUTA Windows con
  forward-slashes.
- **CDP request timeout** → reintentar UNA vez tras `sleep 3`; si persiste, **reabrir la página**.
- Presupuesto: este plan NO re-recorre lo ya cubierto por HTML servido. Su denominador es el subconjunto
  hidratación-dependiente, declarado por adelantado en el Task 1.
</gotchas_de_browseros>

<tasks>

<task type="auto">
  <name>Task 1: Declarar el criterio de estratificación y abrir la sesión BrowserOS</name>
  <read_first>
    - `scripts/bros-cli.mjs` (cabecera: `open`, `content`, `shot`, `snapshot`, `links`, `close`; los gotchas probados 2026-07-07)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §0.4 Tabla D (las 19 filas: 15 rutas + 4 not-found) y §3.0 (catálogo de los 60 emisores)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §3.0.1 (emisores huérfanos)
    - Los tres artefactos de wave 2 (`125-E2E-A/B/C`) — para NO repetir lo ya cubierto
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md</files>
  <action>
Escribir §0 de `125-E2E-D-BROWSEROS.md` **antes** de abrir ninguna página: el criterio de estratificación,
por escrito y falsable. Reglas del criterio:

- **Toda ruta del inventario aparece al menos una vez** en el conjunto {Planes 02,03,04} ∪ {este plan}.
  La tabla de §0 debe mostrar, para las 19 filas de la Tabla D, en qué plan quedó cubierta.
- **Este plan cubre** las superficies hidratación-dependientes: `header-nav` (C-02, `"use client"`),
  los acordeones/`detalle-colapsable` de `/parlamentario/[id]`, el grafo de `/red`, el island de filtros
  de `/buscar` (`buscar-filtros.tsx`), y el island de facetas de `/parlamentarios`.
- **Los emisores huérfanos NO se buscan** (`E-029 ResumenView`, `E-003`, `E-008 actualidad-module.tsx`,
  empty-state de `E-053`): se listan como huérfanos con su evidencia de ausencia de call-site (L-3 de 122).
- **Los bloques gated OFF se verifican AUSENTES**, ya hecho en el Plan 04: aquí se referencia, no se repite.
- Lo que quede **fuera** del alcance de este plan se nombra explícitamente en §0 como no cubierto por
  BrowserOS y se dice qué plan lo cubrió por HTML servido. Prohibido escribir "se recorrió todo".

Después: verificar el MCP vivo (`node scripts/bros-cli.mjs tools`) y abrir la primera página oculta,
parseando el Page ID de la salida. Si el MCP no responde, es un gate que el subagente no puede cerrar:
**escalar al orquestador** (arrancar BrowserOS es acto de operador), no simular la pasada.
  </action>
  <verify>
    <automated>node scripts/bros-cli.mjs tools 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - §0 contiene una tabla con las **19** filas de la Tabla D y el plan que cubrió cada una
    - §0 nombra explícitamente lo NO cubierto por BrowserOS; la cadena "se recorrió todo" **no aparece**
    - Los 4 huérfanos listados como tales, sin fragmento DOM
    - `node scripts/bros-cli.mjs tools` responde con la lista de tools (o el gate está escalado por escrito)
  </acceptance_criteria>
  <done>Criterio de estratificación declarado y falsable; sesión BrowserOS operativa o gate escalado.</done>
</task>

<task type="auto">
  <name>Task 2: Pasada BrowserOS por las superficies hidratación-dependientes</name>
  <read_first>
    - §0 de `125-E2E-D-BROWSEROS.md` (el conjunto declarado en el Task 1)
    - `.planning/milestones/v10.0-phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-E2E-EVIDENCIA.md` (formato de evidencia precedente: superficie · fragmento · veredicto)
  </read_first>
  <files>
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/captures/
  </files>
  <action>
Para cada superficie del conjunto declarado, en secuencia y con una sola página a la vez:

1. `node scripts/bros-cli.mjs open <url>` → parsear `Page ID: N` con
   `grep -oE "Page ID: [0-9]+" | grep -oE "[0-9]+"`.
2. `sleep 5` (SSR + hidratación).
3. `node scripts/bros-cli.mjs content <N>` → guardar el contenido renderizado; extraer el fragmento
   relevante y citarlo verbatim en el artefacto.
4. `node scripts/bros-cli.mjs snapshot <N>` en las superficies interactivas (nav, filtros, acordeones)
   para registrar los elementos interactivos presentes.
5. `node scripts/bros-cli.mjs shot <N> "C:/…/125-…/captures/<slug>.png"`, con el patrón de reintento
   `cmd || (sleep 3; cmd)` ante CDP timeout, y **`sleep 8-10` entre capturas** — la ráfaga tumba el MCP.
6. `node scripts/bros-cli.mjs close <N>` antes de abrir la siguiente.

URLs concretas del recorrido (mínimo):
`/` · `/parlamentario/D1165` · `/parlamentario/S1338` · `/proyecto/14309-04` · `/buscar?q=pensiones` ·
`/parlamentarios` · `/red?seed=D1165` · `/red?seed=S1338` · `/comparar?a=D1170&b=D1165` · `/agenda`.

Registrar por superficie: URL · qué se verificó · fragmento renderizado verbatim · captura · veredicto
(`ok` / `hallazgo`). Un `hallazgo` se escribe con lo observado y lo esperado, y se escala; no se arregla
aquí (un fix exigiría re-deploy y esta fase ya desplegó).
  </action>
  <verify>
    <automated>ls .planning/phases/125-e2e-pasada-final-producto-a-producto/captures/*.png | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ≥ **10** capturas en `captures/`, una por URL del recorrido
    - Cada superficie del conjunto de §0 tiene fila con fragmento renderizado verbatim y veredicto
    - El grafo de `/red?seed=D1165` se ve renderizado (nodos presentes) y `/red?seed=S1338` muestra el vacío honesto
    - El island de filtros de `/buscar` aparece en el `snapshot` con sus elementos interactivos
    - Cero superficies con veredicto vacío
  </acceptance_criteria>
  <done>Las superficies hidratación-dependientes recorridas con BrowserOS y evidenciadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate humano — lectura fría de una captura y juicio de copy</name>
  <read_first>
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-VERIFICATION.md` → `human_verification`, ítems 1 (lectura fría del artefacto) y 2 (juicio de copy sobre `COBERTURA_MENCIONES_LOBBY`)
    - `125-E2E-D-BROWSEROS.md` §0 y las capturas de `captures/`
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md</files>
  <what-built>
    Pasada BrowserOS completa sobre el deploy real, con capturas de las 10 superficies principales y
    fragmento renderizado por superficie. Los 2 ítems post-deploy heredados de 122 ya quedaron cerrados
    por evidencia DOM en el Plan 02.
  </what-built>
  <how-to-verify>
    1. Abrir `captures/proyecto-14309-04.png` y leer el bloque de menciones de lobby.
       ¿La línea de cobertura describe el canal **sin insinuar ocultamiento ni causalidad**? ¿El parcial
       se lee como parcial y nunca como total? ¿El idiom "según fuente al …" se entiende?
    2. Abrir `captures/parlamentario-S1338.png` y confirmar a ojo que el carril de lobby **no muestra
       ningún número** ni afirma una ausencia en la fuente.
    3. Opcional (ítem 1 de 122): hojear `122-CRUCES-SQL.md` y decir si sus 82 filas se auditan sin abrir el código.
  </how-to-verify>
  <acceptance_criteria>
    - El operador responde con "aprobado" o con la lista concreta de objeciones de copy
    - **Si no responde**, el ítem se cierra como **handoff documentado** en `125-HANDOFF-HUMANO.md`
      (Plan 07) y la corrida CIERRA igual — patrón v7/v9/v10/v11. Jamás se marca como aprobado sin respuesta.
  </acceptance_criteria>
  <action>
Presentar al operador las dos capturas y las tres preguntas de <how-to-verify>, y esperar su respuesta.
Registrar la respuesta **verbatim** en la sección de gate de `125-E2E-D-BROWSEROS.md`. Si no hay respuesta,
escribir en esa misma sección la línea `SIN RESPUESTA DEL OPERADOR — handoff` y dejar el ítem para
`125-HANDOFF-HUMANO.md` (Plan 07). Prohibido inferir aprobación por silencio.
  </action>
  <verify>
    <automated>grep -cE "aprobado|SIN RESPUESTA DEL OPERADOR" .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md</automated>
  </verify>
  <done>La sección de gate registra la respuesta verbatim del operador, o su ausencia declarada como handoff.</done>
  <resume-signal>Escribir "aprobado" o describir las objeciones de copy</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → MCP local de BrowserOS | control del navegador del host |
| capturas → artefacto versionado | una captura podría contener datos sensibles en pantalla |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-16 | Information disclosure | capturas `.png` | mitigate | Sólo páginas **ocultas** del sitio público, sin sesión autenticada; cero navegación a `/admin`, cero capturas del navegador del operador |
| T-125-17 | Denial of service | MCP de BrowserOS | mitigate | Una página a la vez, `sleep` 8-10 s entre capturas, reintento único ante CDP timeout, `close` antes de abrir la siguiente |
| T-125-18 | Repudiation | veredicto humano | mitigate | Sin respuesta del operador ⇒ handoff documentado, nunca "aprobado" tácito |
</threat_model>

<verification>
Criterio de estratificación escrito antes del recorrido; ≥10 capturas; cada superficie con fragmento
renderizado y veredicto; gate humano respondido o convertido en handoff.
</verification>

<success_criteria>
SC2 del ROADMAP en su parte BrowserOS: las superficies que sólo existen tras hidratación quedan
recorridas con evidencia, y la cobertura del conjunto se declara por lista.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-06-SUMMARY.md` when done
</output>
