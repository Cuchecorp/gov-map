---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "El Worker de PROD sirve el código de HEAD: los fixes de 114 (links internos), 117 (fechas) y 122 (lobby capa-1 + cobertura) están en el DOM del deploy real"
    - "La versión desplegada queda registrada por id, no de memoria"
    - "El gate pre-deploy (suite + tsc + guards) pasó ANTES de construir el bundle"
    - "Ningún flag se tocó en el deploy: MONEY y NOTIF siguen OFF"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md"
      provides: "Secuencia ejecutada verbatim + id de versión desplegada + smoke post-deploy"
      contains: "VERSIÓN DESPLEGADA"
  key_links:
    - from: "HEAD de master"
      to: "Worker observatorio-congreso"
      via: "build OpenNext en Docker node:22-slim + opennextjs-cloudflare deploy"
      pattern: "VERSIÓN DESPLEGADA"
---

<objective>
Desplegar a Cloudflare el bundle agrupado con los fixes YA COMMITEADOS de las fases 114, 117 y 122,
y dejar registrada la versión desplegada. Sin este plan, los planes 02-07 medirían el sitio viejo.

Purpose: SC1 del ROADMAP. Es el único acto load-bearing de la fase: los 4 criterios restantes sólo
son verificables sobre este deploy.
Output: `125-DEPLOY-RUNBOOK.md` con la secuencia verbatim, el id de versión y el smoke post-deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/milestones/v10.0-phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md
@.planning/milestones/v10.0-phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md
</context>

<constantes>
- Base URL PROD: `https://observatorio-congreso.thevalis.workers.dev`
- Worker: `observatorio-congreso`
- Mirror de build: `C:/Temp/obs-build`
- OAuth de wrangler del host: `C:\Users\Carlo\AppData\Roaming\xdg.config\.wrangler`
- wrangler REAL (npm global, el del PATH está sombreado por un paquete Python):
  `C:\Users\Carlo\AppData\Roaming\npm\wrangler.cmd`
</constantes>

<tasks>

<task type="auto">
  <name>Task 1: Gate pre-deploy — suite, tsc, guards y verificación de que NADA nuevo entra al bundle</name>
  <read_first>
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` §3 (comandos de cierre verbatim: suite, tsc, greps de copy, lista de guards de régimen)
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-VERIFICATION.md` (spot-checks: `tsc --noEmit` exit 0, suite app 1577/107 archivos)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md</files>
  <action>
Correr el gate pre-deploy y escribir la sección §1 del runbook con la salida verbatim.

1. `set -o pipefail`. Desde la raíz: `pnpm test` (root = packages + app) y, por separado,
   `pnpm --filter ./app exec vitest run` para obtener el conteo exacto de `app/`.
2. `pnpm -r exec tsc -b` y `pnpm --filter ./app exec tsc --noEmit`.
3. Guards de régimen: `pnpm --filter ./app exec vitest run lib/*guard*.test.ts lib/*gate*.test.ts`.
   Nombrarlos uno por uno en el runbook (anti-insinuacion, money-antiflip, lockdown,
   bento-coherencia, name-match-rut, env-example, integ-scope, provider-guard, cruces-gate,
   vsim-gate, net-gate, busqueda-hibrida-gate, admin-gate, money-gate).
4. Higiene de alcance del bundle — verificar que el deploy agrupa SÓLO fixes ya commiteados:
   - `git status --porcelain` → los únicos no-commiteados admisibles son artefactos `.planning/`
     (`119-REVIEW.md`, `122/123/124-VERIFICATION.md`) y `pnpm-workspace.yaml`. **Cero archivos
     bajo `app/` o `packages/` modificados.** Si aparece uno, PARAR y escalar: nada nuevo entra al
     bundle (decisión LOCKED del CONTEXT).
   - `git diff --name-only HEAD -- app/ packages/ supabase/` → vacío.
   - Flags: `git log --oneline -30 --name-only | grep -iE "\.env|wrangler\.jsonc|gate" ` → registrar;
     ningún commit de este plan toca un flag.
5. Escribir `125-DEPLOY-RUNBOOK.md` §1 con: comando, exit code y conteo observado de cada check.
   Si el conteo de `app/` difiere del esperado (≥ 1590), NO ajustar el número a la baja: registrarlo
   con el commit que lo explica.

Cero DDL/DML. Cero `SUPABASE_DB_URL` ecoado.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit && pnpm exec vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app exec tsc --noEmit` → exit 0
    - Suite `app/` → exit 0, total de tests **≥ 1590** (o el número real registrado con su causa)
    - Los 14 guards de régimen nombrados y verdes
    - `git diff --name-only HEAD -- app/ packages/ supabase/` → **salida vacía**
    - `125-DEPLOY-RUNBOOK.md` §1 existe y cita exit code + conteo por check (cero "verde" sin número)
  </acceptance_criteria>
  <done>Gate pre-deploy pasado y documentado; confirmado que el bundle sólo lleva commits ya en HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Build OpenNext en Docker + deploy a Cloudflare + registro del id de versión</name>
  <read_first>
    - `.planning/milestones/v10.0-phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md` §1-§3 (robocopy, `docker-build.sh`, `docker-deploy.sh` verbatim, montaje del OAuth)
    - `.planning/milestones/v10.0-phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md` (gotchas re-confirmados)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md</files>
  <action>
Ejecutar la secuencia de deploy pagada en v10.0, sin inventar variantes.

1. **Espejo** (PowerShell, NO git-bash):
   `robocopy '<repo>' 'C:\Temp\obs-build' /MIR /XD node_modules .open-next .next .git .turbo dist coverage /XF *.log /NFL /NDL /NP /R:1 /W:1`
   Exit code < 8 = éxito. Si `C:/Temp/obs-build/node_modules` quedó locked, borrarlo primero.
2. **Purgar `.pnpm-store` del mirror** (gotcha v8.0: si viaja, el build revienta).
3. **Re-escribir los helper scripts TRAS el mirror** — `/MIR` los borra porque NO están en el repo:
   - `C:\Temp\obs-build\docker-build.sh`: `corepack enable && corepack prepare pnpm@11 --activate` ·
     `pnpm config set dangerouslyAllowAllBuilds true` · `cd /work && pnpm install --frozen-lockfile` ·
     `cd /work/app && pnpm run cf-build 2>&1 | tee /work/opennext-build.log`
   - `C:\Temp\obs-build\docker-deploy.sh`: idem cabecera + `export XDG_CONFIG_HOME=/root/.config` ·
     `export WRANGLER_HOME=/root/.config/.wrangler` ·
     `cd /work/app && CI=true pnpm run deploy 2>&1 | tee /work/deploy.log`
4. **Build**: `docker run --rm -v "C:\Temp\obs-build:/work" -w /work node:22-slim bash -lc "chmod +x /work/docker-build.sh && /work/docker-build.sh"`.
   Exigir `worker.js` emitido en `/work/app/.open-next/worker.js` y BUILD EXIT 0.
   NUNCA construir en Windows ni en alpine (symlink EPERM → worker roto).
5. **Deploy**: `docker run --rm -v "C:\Temp\obs-build:/work" -v "C:\Users\Carlo\AppData\Roaming\xdg.config\.wrangler:/root/.config/.wrangler" -w /work node:22-slim bash -lc "chmod +x /work/docker-deploy.sh && /work/docker-deploy.sh"`.
   Si el OAuth no está o expiró, PARAR y escalar como gate de autenticación al orquestador
   (no reintentar con credenciales inventadas, no crear tokens).
6. Capturar el **id de versión** de la salida del deploy y escribirlo en el runbook bajo el literal
   exacto `VERSIÓN DESPLEGADA: <uuid>`. Registrar también fecha/hora UTC.
7. **Cero flips**: no invocar `wrangler secret put` para ningún `*_PUBLIC_ENABLED`. Sólo se admite
   `wrangler secret list` (lectura) para dejar constancia del estado de los flags.

En git-bash usar `MSYS_NO_PATHCONV=1` para cualquier invocación con rutas.
  </action>
  <verify>
    <automated>grep -c "VERSIÓN DESPLEGADA" .planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `BUILD EXIT: 0` y existencia de `/work/app/.open-next/worker.js` registrados en el runbook
    - El runbook contiene la línea literal `VERSIÓN DESPLEGADA: ` seguida de un uuid real
    - Cero invocaciones de `wrangler secret put` en el log de la corrida
    - El runbook nombra los 3 gotchas re-confirmados o desviados (pnpm-store, helper scripts, wrangler sombreado)
  </acceptance_criteria>
  <done>El Worker sirve el bundle de HEAD y su versión está registrada por id.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke post-deploy — 200s, propagación, y prueba de que los 3 fixes llegaron</name>
  <read_first>
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §"Qué queda pendiente de observar sobre el deploy real (Phase 125)" (los 3 controles literales)
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` §1 F-01 (idiom `según fuente al `)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md</files>
  <action>
Esperar 30 s de propagación (los 500 intermitentes en esa ventana NO son fallo: si aparecen,
reintentar tras 15 s y registrarlo). Después, smoke mínimo por `curl` contra
`https://observatorio-congreso.thevalis.workers.dev`, secuencial, con 1 s entre requests
(es el Worker propio, pero con mesura):

1. Códigos HTTP de: `/`, `/parlamentarios`, `/agenda`, `/buscar`, `/metodologia`, `/sobre`,
   `/comparar`, `/red`, `/parlamentario/D1165`, `/proyecto/14309-04` → todos **200**.
2. **Prueba de que el bundle es el nuevo** (tres marcadores, uno por fase de origen):
   - 117: `/proyecto/14309-04` contiene el idiom `según fuente al ` (F-01) y **cero** ocurrencias
     del literal `Actualizado hace`.
   - 122: `/proyecto/14309-04` contiene el literal de cobertura de lobby con `3,8`
     (usar grep tolerante: buscar `3,8` en la vecindad del bloque de menciones, jamás el literal pelado
     de una frase completa, porque React intercala `<!-- -->` entre texto y dígitos).
   - 114: `/parlamentario/D1165` responde 200 y el header de la ficha resuelve (control de humo;
     la verificación exhaustiva de links es el Plan 05).
3. Registrar §3 del runbook con URL, código y el fragmento DOM citado verbatim (recortado).

Si algún marcador falta, el deploy NO trajo los fixes: PARAR y escalar antes de que las waves 2-4
midan un sitio viejo.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/proyecto/14309-04</automated>
  </verify>
  <acceptance_criteria>
    - 10/10 rutas del smoke devuelven **200**
    - `curl -s .../proyecto/14309-04 | grep -c "según fuente al "` → **≥ 1**
    - `curl -s .../proyecto/14309-04 | grep -c "Actualizado hace"` → **0**
    - `curl -s .../proyecto/14309-04 | grep -c "3,8"` → **≥ 1**
    - §3 del runbook cita cada fragmento verbatim (cero "se ve bien")
  </acceptance_criteria>
  <done>El deploy está vivo y demostrado nuevo por marcadores DOM de 114/117/122.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Cloudflare API | credencial OAuth de wrangler cruza aquí |
| repo → mirror `C:/Temp/obs-build` | `.env` y secretos podrían viajar al mirror y al contenedor |
| agente → flags del Worker | un `secret put` accidental flipearía una feature no autorizada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Information disclosure | log de deploy / runbook | mitigate | Prohibido pegar salida que contenga tokens; el runbook cita sólo el id de versión. `SUPABASE_DB_URL` jamás expandida |
| T-125-02 | Elevation of privilege | `wrangler secret put` | mitigate | Ningún `secret put` en esta fase; sólo `secret list`. El anti-flip guard corre en el gate del Task 1 |
| T-125-03 | Tampering | bundle desplegado | mitigate | Task 1 exige `git diff --name-only HEAD -- app/ packages/` vacío: sólo commits ya revisados entran |
| T-125-SC | Tampering | `pnpm install --frozen-lockfile` en el contenedor | mitigate | `--frozen-lockfile` obligatorio; cero paquetes nuevos en esta fase ⇒ no aplica el Package Legitimacy Gate |
</threat_model>

<verification>
- Gate pre-deploy documentado con números, no adjetivos.
- Versión desplegada registrada por id.
- Los 3 marcadores de fix presentes en el DOM del deploy.
- Cero flags tocados; cero migraciones; cero archivos de código modificados por este plan.
</verification>

<success_criteria>
SC1 del ROADMAP cumplido: los fixes de UI de links, fechas y cruces están en Cloudflare ANTES de la
pasada, con la versión registrada y probada por DOM.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-01-SUMMARY.md` when done
</output>
