---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 03
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "Las 8 rutas de navegación y catálogo del inventario dejan evidencia DOM sobre el deploy nuevo"
    - "El chrome compartido (footer, nav, wordmark, migajas) se verifica una vez y se declara compartido"
    - "Los conteos del panel de actualidad y de /comparar siguen cuadrando contra SQL"
    - "Las 4 sub-superficies not-found devuelven 404 con su copy"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md"
      provides: "Evidencia DOM de las rutas de navegación, catálogo y chrome"
      contains: "panel-actualidad"
  key_links:
    - from: "DOM del deploy"
      to: "SQL de PROD"
      via: "queries §3 y §4 de 122-CRUCES-SQL.md"
      pattern: "Q-6[0-9]"
---

<objective>
Recorrer sobre el deploy nuevo las rutas de navegación y catálogo del inventario 113 —`/`, `/agenda`,
`/buscar`, `/comparar`, `/parlamentarios`, `/red`, `/metodologia`, `/sobre`— más el chrome compartido
y las `not-found`, dejando evidencia DOM por superficie.

Purpose: SC2 y SC4 del ROADMAP sobre la mitad no-densa del inventario.
Output: `125-E2E-B-RUTAS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
</context>

<gotchas_de_lectura_del_DOM>
Idénticos al Plan 02: `<!-- -->` intercalado entre texto y dígitos · Suspense en `<div hidden id="S:1">` ·
`reunión` singular · **PROHIBIDO** `grep -o -E '…{0,9000}'` sobre HTML de una línea (colgó >120 s) ·
huérfanos `E-003` y `E-008 actualidad-module.tsx` **no se buscan en el DOM**, se registran como tales.
</gotchas_de_lectura_del_DOM>

<tasks>

<task type="auto">
  <name>Task 1: Chrome compartido + / (panel de actualidad) + /parlamentarios</name>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §2 (chrome: C-01 footer `app/app/layout.tsx`, C-02 `header-nav.tsx`, C-03 `global-header.tsx`, C-04 `breadcrumbs.tsx`), §4.4 (`/`) y §4.8 (`/parlamentarios`, E-012/019)
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §4 filas **4-14** (denominador del tile *Por materia*: `3100` de `3675`, 84,4 %) y **4-15** (dos grafías de cámara: `C.Diputados`/`Senado`/`(sin cámara)`/`senado`/`camara`)
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` F-06 (`Última consulta a las fuentes` + la línea aclaratoria) y F-14 (`fechaCivilCorta` en el tile: `datos al 10 ago 2026`)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md</files>
  <action>
1. **Chrome (C-01…C-04)**: verificar UNA VEZ sobre `/` y declarar que es compartido por las 15 rutas
   (con la prueba de que el mismo fragmento aparece también en `/sobre`, como muestra de segundo punto).
   Registrar el wordmark, los items de nav y el footer verbatim.
2. **`/` — panel de actualidad**:
   - Los tiles y sus conteos, re-calculados contra las queries de 122 §4 (`Q-57`, `Q-63`, `Q-64`, `Q-65`).
   - Fila **4-14** sigue `discrepancia-declarada`: el tile *Por materia* agrupa sobre el corpus
     **embebido** (`3100` de `3675`) sin declarar el denominador. Registrar el número vivo del DOM y el
     del SQL; **debe seguir declarada** (el fix era SQL de 124 y `124-HANDOFF-EXACTITUD.md` lo dejó en
     backlog de exactitud). Si apareciera un denominador nuevo en el DOM, es hallazgo.
   - Fila **4-15** sigue declarada: las dos grafías de cámara conviven. Registrar cuáles de los 6 chips
     se observan.
   - Fechas: `Última consulta a las fuentes` presente (F-06), la línea aclaratoria presente, `datos al
     <dd mmm aaaa>` con año (F-14). Cero `Última actualización de datos`.
3. **`/parlamentarios`**: conteo del directorio, facetas, y control de no-regresión del fix
   `partidoLegible` de v10.0 — **cero URI-como-partido visible**: `grep -c "datos.bcn.cl"` sobre el HTML;
   si aparece, verificar que la única ocurrencia es la clave de filtro serializada en el payload RSC del
   island (no-visible, RAW por diseño) y declararlo; cualquier otra es hallazgo.

Escribir §1 y §2 de `125-E2E-B-RUTAS.md`: una fila por superficie con emisor · fragmento DOM · número
SQL · veredicto.
  </action>
  <verify>
    <automated>curl -s https://observatorio-congreso.thevalis.workers.dev/ | grep -c "Última consulta a las fuentes"</automated>
  </verify>
  <acceptance_criteria>
    - Chrome: los 4 componentes C-01…C-04 con fragmento verbatim, verificados en `/` y confirmados en `/sobre`
    - `/`: `grep -c "Última consulta a las fuentes"` → **≥ 1**; `grep -c "Última actualización de datos"` → **0**
    - Cada tile del panel con su número del DOM y el de su query SQL, lado a lado
    - Filas 4-14 y 4-15 registradas como **siguen declaradas** (o escaladas como hallazgo si cambiaron)
    - `/parlamentarios`: toda ocurrencia de `datos.bcn.cl` justificada como payload RSC no-visible, o hallazgo
  </acceptance_criteria>
  <done>Chrome, home y directorio recorridos con evidencia DOM y conteos cuadrados contra SQL.</done>
</task>

<task type="auto">
  <name>Task 2: /agenda, /buscar, /comparar — las tres rutas con estado en searchParams</name>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §4.5 (`/agenda`, E-007/018/033), §4.6 (`/buscar`, E-028) y §4.7 (`/comparar`, VSIM)
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §3 (los 4 ejes de `/comparar`, los 3 estados, VSIM `Coinciden en N de M`, §3.5 el lead `(100%)` sobre `3655/3672`) y fila **3.3** (`discrepancia-declarada` por truncamiento fail-closed)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md</files>
  <action>
1. **`/agenda`**: la semana viene del reloj o de `searchParams`. Recorrer la vista por defecto y una
   semana explícita. Registrar: conteo de citaciones, la fecha civil renderizada y su idiom.
   **Gotcha LOCKED**: `citacion.fecha` es **date-only a medianoche UTC** ⇒ la parte de fecha UTC **ES**
   el día chileno. Jamás convertir tz. Verificar que el helper civil (`fechaCivilCorta`) es el que rinde
   esas fechas, contrastando con 117 F-09.
2. **`/buscar`**: correr una consulta determinista (p. ej. `?q=pensiones`) y registrar el conteo de
   resultados y las facetas. Control de F-12: la faceta de año rinde `Año del primer trámite` y las
   tarjetas `primer trámite <año>` — `grep -c "Año del primer trámite"` ≥ 1.
   **Canarios de 124 en esta ruta** (`/buscar` es la superficie que ejercita `match_proyectos`, tocada por
   `0077` `statement_timeout` y `0079` cotas de cardinalidad):
   - **`0077`**: la ruta responde **200** y no un error de timeout; registrar la latencia observada de la
     petición (`curl -w "%{time_total}"`). Un 5xx o un timeout visible es **hallazgo grave** atribuible a
     `0077`, no "lentitud".
   - **`0079`**: `124-SUPA-FIX.md` midió `match_proyectos(1001)` = **1001** bajo `service_role`, es decir la
     cota queda muy por encima de la demanda viva. ⇒ el conteo de resultados del DOM **no debe** aparecer
     recortado en un número redondo sospechoso (p. ej. exactamente el tope). Comparar el conteo del DOM
     contra su query SQL: si difieren, atribuir explícitamente a `0079` o descartarlo, jamás registrarlo como
     "conteo distinto" sin atribución.
3. **`/comparar?a=D1165&b=S1338`**: los 4 ejes factuales y sus 3 estados (compartido / no compartido /
   eje ausente); registrar el fragmento de cada uno.
   - Fila **3.3 sigue `discrepancia-declarada`**: el eje de co-autoría se declara truncado
     (`…están truncadas (más de 20 registros…)`) por fail-closed, aunque el SQL dé `0` boletines
     co-firmados. Registrar ambos números.
   - **VSIM**: con par mismo-cámara (`?a=D1170&b=D1165`) debe aparecer `Coinciden en N de M`
     —patrón tolerante a `<!-- -->`— más el caveat de base alta. Re-calcular N y M por SQL (§3.4/§3.5
     de 122) y comparar. Con par de cámaras distintas el eje puede ausentarse: registrar cuál de los dos
     comportamientos se observa, sin inventar.
Escribir §3 de `125-E2E-B-RUTAS.md`.
  </action>
  <verify>
    <automated>curl -s "https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1170&b=D1165" | grep -c "Coinciden en"</automated>
  </verify>
  <acceptance_criteria>
    - `/agenda`: conteo del DOM registrado + la fecha civil citada verbatim; declaración explícita de que no se convirtió tz
    - `/buscar?q=pensiones`: HTTP **200**, latencia registrada (canario `0077`), conteo del DOM comparado contra su SQL (canario `0079`) y `grep -c "Año del primer trámite"` → **≥ 1**
    - `/comparar`: los 4 ejes con su estado citado; fila 3.3 registrada como **sigue declarada**
    - VSIM: `Coinciden en` presente con par mismo-cámara y N/M **idénticos** al SQL re-ejecutado
  </acceptance_criteria>
  <done>Las tres rutas con estado en searchParams recorridas con evidencia DOM y números cuadrados.</done>
</task>

<task type="auto">
  <name>Task 3: /red, /metodologia, /sobre y las 4 not-found</name>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §4.9 (`/red`, gate NET, sujetos `D1165` con vecindario y `S1338` con grafo vacío honesto), §4.10 (`/metodologia`), §4.11 (`/sobre`), y las 4 filas `not-found` de la Tabla D (E-049, E-023, E-050, E-047)
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §2.5 (`/red` — E-011: `80 vecinos · 235 hechos documentados`, y el copy de grafo vacío honesto)
    - `.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-SUPA-FIX.md` (no-regresión medida bajo `service_role`: `subgrafo_red('D1009',2)` = 134 nodos / 7.394 aristas)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md</files>
  <action>
1. **`/red`** (gate NET ON): con semilla `D1165` registrar el seednote (`N vecinos · M hechos
   documentados`, patrón tolerante a `<!-- -->`) y re-calcular N/M por SQL. Con semilla `S1338`
   registrar el **vacío honesto** verbatim (`Aún no hay relaciones para mostrar para este
   parlamentario…`) — cero relleno.
   **124 no debió cambiar ningún número aquí**: las 5 migraciones aplicadas (`0074`, `0076`-`0079`) son
   ACL, `search_path`, `statement_timeout` y cotas muy por encima de la demanda viva. Si el DOM muestra
   un número distinto al SQL, es **hallazgo grave**: PARAR y escalar, no explicarlo.
2. **`/metodologia`** y **`/sobre`**: rutas estáticas (sin `rpc(`/`from(`). Registrar los links salientes
   y el copy institucional. Verificar cero `captura` pelado y cero causalidad (los guards ya lo cubren en
   código; aquí se comprueba sobre el DOM servido).
3. **Las 4 `not-found`**: `/parlamentario/NOEXISTE`, `/proyecto/00000-00`, `/contraparte/NOEXISTE`,
   `/red?seed=NOEXISTE` → **404** cada una, con su copy citado. Nota: `/contraparte/[id]` 404ea entera
   por gate MONEY (es lo único que el deploy sirve en esa ruta) — la verificación del gate en sí es del
   Plan 04; aquí sólo se registra el 404 y el copy de E-050.
Escribir §4 y §5 de `125-E2E-B-RUTAS.md`, más una **tabla de cobertura declarada** de este plan: qué
rutas del inventario cubrió, con qué sujeto, y cuáles quedaron para los Planes 02/04/06.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/proyecto/00000-00</automated>
  </verify>
  <acceptance_criteria>
    - `/red?seed=D1165`: seednote citado y N/M **idénticos** al SQL re-ejecutado (cualquier desvío ⇒ hallazgo escalado)
    - `/red?seed=S1338`: copy de vacío honesto citado verbatim; cero dígito de relleno
    - `/metodologia` y `/sobre`: 200 y links salientes listados
    - Las 4 `not-found` → HTTP **404** con su copy citado
    - Tabla de cobertura declarada presente, nombrando lo NO cubierto por este plan
  </acceptance_criteria>
  <done>Rutas restantes y not-found recorridas; cobertura de este plan declarada por lista, no por adjetivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → Worker propio | requests de lectura al deploy |
| agente → Supabase PROD | re-ejecución de queries de conteo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-07 | Denial of service | Worker propio | mitigate | Recorrido secuencial con 1 s entre requests; cero concurrencia; cero crawl |
| T-125-08 | Information disclosure | artefacto de evidencia | mitigate | Sólo conteos y copy institucional; cero PII; `SUPABASE_DB_URL` nunca ecoada |
| T-125-09 | Tampering | datos de PROD | mitigate | Régimen read-only: sólo `SELECT` |
</threat_model>

<verification>
Cada ruta recorrida tiene fragmento DOM; cada conteo tiene su número SQL al lado; la cobertura del plan
se declara por lista.
</verification>

<success_criteria>
Las rutas de navegación y catálogo del inventario quedan recorridas sobre el deploy real con evidencia
DOM y conteos cuadrados contra SQL.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-03-SUMMARY.md` when done
</output>
