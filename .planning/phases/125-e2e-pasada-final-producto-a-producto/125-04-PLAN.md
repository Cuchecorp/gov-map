---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 04
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "MONEY está OFF y ninguno de sus bloques aparece en el DOM de ninguna ruta"
    - "NOTIF está OFF: /cuenta y /notificaciones/* responden inertes y 'Seguir' no aparece en el DOM"
    - "El estado de los 5 gates queda registrado por evidencia (HTTP + DOM), no por suposición"
    - "Ningún flag fue tocado durante la fase"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md"
      provides: "Tabla de estado de los 5 gates con evidencia HTTP+DOM y prueba de ausencia de MONEY/NOTIF"
      contains: "MONEY"
  key_links:
    - from: "flags del Worker"
      to: "DOM del deploy"
      via: "gate en el server component"
      pattern: "MONEY|NOTIF"
---

<objective>
Verificar sobre el deploy nuevo que los flags **no autorizados** siguen OFF y **ausentes del DOM**, y
registrar el estado de los 5 gates (MONEY, NOTIF, NET, CRUCES, VSIM) con evidencia.

Purpose: SC5 del ROADMAP. Un gate OFF que filtra copy al DOM es una regresión de régimen, no un detalle.
Output: `125-E2E-C-GATES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
</context>

<regla_LOCKED>
**Ningún `*_PUBLIC_ENABLED` se toca en esta fase.** Se admite `wrangler secret list` (lectura) para
constatar qué claves existen; **jamás** `wrangler secret put`. Cualquier necesidad de flip es deuda de
operador (`MONEY`, provisión `NOTIF`) y está explícitamente **fuera de alcance** por el CONTEXT.
</regla_LOCKED>

<tasks>

<task type="auto">
  <name>Task 1: MONEY OFF y ausente — rutas 404, bloques no emitidos, cero copy filtrado</name>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §5 (fila MONEY), §4.3 (`/contraparte/[id]`: 404 entera, `page.tsx:50-52`, emisores E-014/016/060) y §0.4.1 LÍMITE 3 (los bloques gated llevan la cadena `no emitido en el deploy auditado`)
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` F-08 (copy MONEY corregido bajo gate OFF: `la fuente cubre hasta el …`; `corte al` eliminado)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md</files>
  <action>
1. **Ruta gated**: `/contraparte/S1338` y `/contraparte/cualquier-id` → HTTP **404** (gate MONEY,
   `page.tsx:50-52`). Registrar código y el copy de `not-found` (E-050).
2. **Bloques MONEY en la ficha de parlamentario**: sobre `/parlamentario/D1165` y `/parlamentario/S1338`,
   probar **ausencia** de los carriles de financiamiento y contratos. Greps de ausencia (cada uno debe dar
   **0**), tolerantes a `<!-- -->` — buscar palabras sueltas, no frases completas:
   `Financiamiento`, `ChileCompra`, `Aportes`, `Contratos del Estado`, `la fuente cubre hasta el`.
   Declarar el alcance exacto de cada grep en el artefacto.
3. **Cero copy MONEY filtrado en el resto del sitio**: repetir los mismos greps sobre `/`, `/comparar`
   y `/parlamentarios`.
4. Registrar en la tabla de gates: `MONEY` · estado **OFF** · evidencia = 404 de `/contraparte/*` + los
   greps de ausencia con su conteo `0`.
   Nota de honestidad a escribir en el artefacto: con MONEY OFF, el fix F-08 de 117 **no es observable
   contra dato real** — se declara así (ya venía declarado en 117 §2(d)), jamás se afirma verificado.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/contraparte/S1338</automated>
  </verify>
  <acceptance_criteria>
    - `/contraparte/<id>` → HTTP **404** en al menos 2 ids distintos
    - Los 5 greps de ausencia MONEY sobre D1165 y S1338 → **0** ocurrencias cada uno, con el comando citado
    - Los mismos greps sobre `/`, `/comparar`, `/parlamentarios` → **0**
    - El artefacto declara que F-08 **no** es verificable contra dato real con MONEY OFF
  </acceptance_criteria>
  <done>MONEY probado OFF y ausente del DOM por evidencia, no por suposición.</done>
</task>

<task type="auto">
  <name>Task 2: NOTIF OFF e inerte — /cuenta, /notificaciones/*, y "Seguir" ausente</name>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §4.12 (`/cuenta`, auth OTP), §4.13 (`/notificaciones/baja`) y §4.14 (`/notificaciones/confirmar`) — las tres marcadas `sin muestra: feature inerte con NOTIF OFF`
    - `.planning/milestones/v10.0-phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md` §"Verificación post-deploy #2" (`/cuenta` **200** con copy gated `Las suscripciones no están disponibles en este momento`; el 500 anterior fue el defecto que motivó el gate-primero `e7d588a`)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md</files>
  <action>
1. **`/cuenta`** → HTTP **200** (no 500) con el copy gated citado verbatim. Un 500 aquí es la regresión
   exacta que v10.0 pagó: si aparece, es **hallazgo** y se escala.
2. **`/notificaciones/baja`** y **`/notificaciones/confirmar`** sin token (NO se inventa ningún token,
   régimen LOCKED del inventario): registrar código y copy inerte.
3. **"Seguir" ausente del DOM**: `grep -c "Seguir"` sobre `/`, `/parlamentario/D1165` y `/proyecto/14309-04`
   → **0** en las tres. Si apareciera dentro de otra palabra, acotar el grep con límites de palabra y
   declarar el ajuste.
4. `wrangler secret list` (SOLO lectura, `wrangler.cmd` de AppData) para constatar qué claves existen en
   el Worker. **No** imprimir valores — `secret list` sólo devuelve nombres; si alguna salida contuviera
   un valor, no se pega.
5. Registrar en la tabla de gates: `NOTIF` · estado **OFF (ausente = OFF)** · evidencia = códigos HTTP +
   copy inerte + los 3 greps en `0`.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/cuenta</automated>
  </verify>
  <acceptance_criteria>
    - `/cuenta` → **200** con el copy gated citado verbatim (un 500 ⇒ hallazgo escalado)
    - `/notificaciones/baja` y `/notificaciones/confirmar` → código registrado + copy inerte citado
    - `grep -c "Seguir"` → **0** en las 3 rutas probadas
    - `wrangler secret list` ejecutado en modo lectura; cero `secret put` en el log de la corrida
    - Cero tokens inventados
  </acceptance_criteria>
  <done>NOTIF probado OFF e inerte, con el 500 histórico descartado por evidencia.</done>
</task>

<task type="auto">
  <name>Task 3: NET, CRUCES y VSIM — estado observado y guards de régimen sobre el deploy</name>
  <read_first>
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §1.2 (gates observados: greps de `id="cruces"` / `id="lobby"` / `id="relaciones"`, el literal VSIM, y el **HALLAZGO A**: `0` ocurrencias de VSIM con par de cámaras distintas **NO** significa VSIM OFF)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §5 y §5.1 (convención LOCKED de la columna `gate`)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md</files>
  <action>
1. **CRUCES ON**: `id="cruces"` presente en `/parlamentario/D1165` y en `/proyecto/14309-04`.
2. **NET ON**: `/red?seed=D1165` → 200 con grafo (no 404). `id="relaciones"` presente en la ficha.
3. **VSIM**: usar par **mismo-cámara** (`?a=D1170&b=D1165`) para no caer en el HALLAZGO A. Registrar el
   literal `Coinciden en` (tolerante a `<!-- -->`) y el caveat de base alta. Con par de cámaras distintas
   registrar la ausencia **explicándola por el hallazgo A**, jamás como "VSIM OFF".
4. **Guards de régimen sobre el DOM servido** (no sólo en código):
   - `grep -c "Actualizado hace"` → **0** en las 4 rutas de mayor densidad
   - `grep -riE "(^|[^a-záéíóúñ])captura([^a-záéíóúñ]|$)"` sobre el HTML servido de esas rutas → cero
     copy renderizado (identificadores de payload RSC como `fecha_captura` se declaran y no cuentan)
   - `grep -c "corte al"` → **0**
   - `grep -c "según fuente al "` → **≥ 1** por ruta con dato fechado
5. Escribir la **tabla final de los 5 gates**: gate · estado · mecanismo · evidencia (comando + salida).
   Cerrar el artefacto con la declaración: **cero `*_PUBLIC_ENABLED` tocados en la Phase 125**, respaldada
   por un diff **acotado a código y config** (el `.planning/` de esta fase contiene la palabra `gate`
   por su propio nombre de archivo — un grep sobre el diff completo se auto-falsaría):
   `git diff --name-only <base>..HEAD -- app/ packages/ supabase/ .github/ ':!.planning/' | grep -iE "\.env|wrangler|gate"` → **vacío**.
  </action>
  <verify>
    <automated>curl -s https://observatorio-congreso.thevalis.workers.dev/parlamentario/D1165 | grep -c 'id="cruces"'</automated>
  </verify>
  <acceptance_criteria>
    - Tabla con los **5** gates, cada uno con comando y salida citados
    - VSIM verificado con par mismo-cámara; toda ausencia con par cruzado explicada por el HALLAZGO A
    - `grep -c "Actualizado hace"` → **0** y `grep -c "corte al"` → **0** en las 4 rutas probadas
    - `grep -c "según fuente al "` → **≥ 1** en las rutas con dato fechado
    - Declaración final de cero flips respaldada por el diff **acotado** (`-- app/ packages/ supabase/ .github/ ':!.planning/'`) → salida vacía. El diff sin acotar NO es criterio válido: los artefactos de esta fase llevan `gate` y `wrangler` en su propio texto
  </acceptance_criteria>
  <done>Los 5 gates registrados con evidencia y los guards de régimen comprobados sobre el DOM del deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flags del Worker → superficie pública | un gate mal puesto expone una feature no autorizada |
| agente → `wrangler` | un `secret put` accidental sería un flip no autorizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-10 | Elevation of privilege | `*_PUBLIC_ENABLED` | mitigate | Regla LOCKED del plan: sólo `wrangler secret list`; `git diff` de `.env`/gate vacío como prueba de cierre |
| T-125-11 | Information disclosure | bloques MONEY | mitigate | Greps de ausencia con conteo `0` sobre 5 rutas; un hit es hallazgo escalado |
| T-125-12 | Spoofing | tokens de `/notificaciones/*` | mitigate | Prohibido inventar tokens; las rutas se prueban sin token, como declara el inventario |
</threat_model>

<verification>
Los 5 gates con evidencia; MONEY y NOTIF probados ausentes del DOM; cero flips.
</verification>

<success_criteria>
SC5 del ROADMAP: los flags no autorizados siguen OFF y ausentes del DOM, con guards de régimen verdes
sobre el deploy real.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-04-SUMMARY.md` when done
</output>
