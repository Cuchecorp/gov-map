---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 07
type: execute
wave: 4
depends_on: ["125-01", "125-02", "125-03", "125-04", "125-05", "125-06"]
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E.md
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "Un lector abre un solo artefacto y ve, ruta por ruta, qué se recorrió sobre el deploy real y con qué evidencia"
    - "La cobertura lograda se declara por lista (19 rutas × 60 emisores), jamás como 'se recorrió todo'"
    - "Las 8 filas discrepancia-declarada de 122 siguen declaradas, o su cambio queda registrado como hallazgo"
    - "Los ítems humanos sin respuesta quedan cerrados como handoff documentado, no como aprobados"
    - "Suite, tsc y guards de régimen verdes al cierre, con números"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E.md"
      provides: "Artefacto único de la pasada E2E: cobertura, veredictos, hallazgos y cierre"
      contains: "Cobertura declarada"
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md"
      provides: "Ítems de juicio humano no respondidos, con destino nombrado"
  key_links:
    - from: "125-E2E-A/B/C/D + 125-RE-VERIFICACION"
      to: "125-E2E.md"
      via: "consolidación con procedencia por fragmento"
      pattern: "Procedencia"
---

<objective>
Consolidar los cinco artefactos de la pasada en **un solo documento auditable** (`125-E2E.md`) con la
cobertura declarada por lista, el estado de las discrepancias heredadas, los hallazgos, y el gate de
cierre (suite + tsc + guards). Cerrar como handoff documentado lo que quedó en juicio humano.

Purpose: hacer la fase auditable por un lector que no siguió la corrida, y cerrar E2E-01.
Output: `125-E2E.md` + `125-HANDOFF-HUMANO.md`.

**Fuera de alcance (LOCKED):** el cierre del milestone (`audit-milestone` → `complete-milestone` →
cleanup → tag → push) lo hace el orquestador **después** de esta fase. Este plan no lo toca.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-A-FICHAS.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-B-RUTAS.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-C-GATES.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidar 125-E2E.md — cobertura declarada por lista + veredicto por superficie</name>
  <read_first>
    - Los 5 artefactos de la fase (`125-DEPLOY-RUNBOOK`, `125-E2E-A/B/C/D`, `125-RE-VERIFICACION`)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §0.4 Tabla D (19 filas) y §3.0 (60 emisores E-001…E-060) — el denominador de la cobertura
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md` §0.6 y §"Procedencia" (formato de consolidación con procedencia por fragmento, ya validado)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E.md</files>
  <action>
Escribir `125-E2E.md` con front-matter (`fase: 125`, `estado`, `version_desplegada`, contadores) y:

**§0 Método** — qué se recorrió, cómo (HTML servido vs BrowserOS), sobre qué versión desplegada
(citando el uuid del runbook), y con qué régimen (read-only, cero flips, rate-limit).

**§1 Cobertura declarada** — la pieza central. Dos tablas:
  - **Por ruta**: las **19** filas de la Tabla D. Columnas: ruta · sujeto usado · plan que la cubrió ·
    método (HTML servido / BrowserOS / ambos) · evidencia (ancla al fragmento). Cero celdas vacías.
    **La fila 15 es `/admin/revisar-entidades` = `n/a — EXCLUIDA`** por decisión LOCKED del CONTEXT de 113
    (§4.15, mitigación T-113-04): sus 4 celdas se rellenan con `n/a — EXCLUIDA` y **nadie navega a `/admin`**
    —el threat model del Plan 06 lo prohíbe—. "Cero celdas vacías" significa cero celdas en blanco, **no**
    cobertura inventada para esa fila. El denominador real de superficies recorridas es **18**.
  - **Por emisor**: los **60** emisores `E-001…E-060`. Columnas: emisor · veredicto de cobertura ·
    motivo escrito cuando el veredicto es `no`. Valores admisibles del veredicto: `verificado en DOM` ·
    `gated OFF — verificado AUSENTE` · `huérfano — sin call-site (no se busca)` · `no emite superficie visible`.
    **Ningún `no` sin motivo escrito** (regla heredada del barrido de 122).
  Cerrar §1 con el cociente explícito (N verificados / 60, M gated, K huérfanos) y la frase de límite:
  qué NO se cubrió y por qué. **La cadena "se recorrió todo" está prohibida en este documento.**

**§2 Veredictos por superficie** — consolidación de las filas de A/B/C/D: superficie · número del DOM ·
número del SQL · veredicto. Usar el mismo vocabulario de 3 valores de 122 (`cuadra` /
`discrepancia-corregida` / `discrepancia-declarada`) más `hallazgo` para lo nuevo.

**§3 Estado de las 8 `discrepancia-declarada` de 122** — una fila por cada una (2.1, 2.5, 2.6, 3.3, 3.b-9,
4-14, 4-15, 5.5): ¿sigue declarada tras el deploy? Recordatorio load-bearing: **2.1/2.5/2.6 deben seguir
declaradas** porque `124` aplicó `0078` con un clamp de **seguridad** que no tocó el cap de 1.000 del
call-site (`124-HANDOFF-EXACTITUD.md` §1, `B-01`). Si alguna se cerró sola, es **hallazgo**, y se escribe
como tal con lo observado y lo esperado.

**§4 Hallazgos de la pasada** — id · severidad · superficie · observado · esperado · disposición
(`corregido` / `declarado` / `escalado a fase futura`). Un hallazgo sin disposición es un ítem cerrado en silencio.

**§5 Procedencia** — qué fragmento aportó cada plan, con conteo de filas.
  </action>
  <verify>
    <automated>grep -c "Cobertura declarada" .planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - §1 tabla por ruta con **19** filas de datos y cero celdas en blanco; la fila `/admin/revisar-entidades` marcada `n/a — EXCLUIDA` en sus 4 columnas y **sin** evidencia de navegación a `/admin`
    - El denominador de superficies efectivamente recorridas se declara como **18**
    - §1 tabla por emisor con **60** filas; `grep -c "^| E-0"` → **60**; ningún veredicto `no` sin motivo
    - `grep -c "se recorrió todo" 125-E2E.md` → **0**
    - §3 tiene **8** filas, una por cada `discrepancia-declarada` de 122, con su estado post-deploy
    - §4: cada hallazgo tiene disposición escrita
    - §5 nombra los 6 planes con su aporte
  </acceptance_criteria>
  <done>La pasada queda auditable desde un solo documento, con cobertura por lista y no por adjetivo.</done>
</task>

<task type="auto">
  <name>Task 2: Handoff de los ítems de juicio humano no respondidos</name>
  <read_first>
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-VERIFICATION.md` → los **4** ítems de `human_verification` (2 son post-deploy y los cerró el Plan 02; 2 son juicio humano)
    - `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E-D-BROWSEROS.md` §gate humano (Task 3 del Plan 06): ¿respondió el operador?
    - `.planning/phases/124-supa-fix-migraciones-aditivas-a-prod/124-HANDOFF-EXACTITUD.md` (formato de handoff con los **cinco campos obligatorios**)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md</files>
  <action>
Escribir `125-HANDOFF-HUMANO.md` con la misma disciplina de `124-HANDOFF-EXACTITUD.md`: cada ítem lleva
sus **cinco campos** — qué es · evidencia · forma del cierre · destino nombrado · quién lo cierra.

Contenido mínimo:
1. **Los 2 ítems post-deploy de 122** → registrar como **CERRADOS** en el Plan 02, citando el fragmento
   DOM que los cierra. (No van al handoff salvo que fallaran.)
2. **Los 2 ítems de juicio humano de 122** (lectura fría del artefacto de 2.700 líneas; juicio de copy
   sobre `COBERTURA_MENCIONES_LOBBY`) + el gate del Plan 06:
   - Si el operador respondió → registrar su veredicto verbatim y cerrar.
   - Si **no** respondió → **handoff documentado** con destino nombrado. La corrida CIERRA igual
     (patrón v7/v9/v10/v11). **Prohibido** marcarlos como aprobados por silencio.
3. **Deuda de operador viva** que esta fase NO toca, sólo re-nombra para que no se pierda: `OP-1`/`OP-4`
   de `124-HANDOFF-EXACTITUD.md` (🔴 elevada), `B-01`/`B-02`/`B-03` (backlog de exactitud, síntoma
   verificable observado en esta pasada), CF secrets + `GEMINI` del checkpoint 118, identidad local,
   flip MONEY, provisión NOTIF, rotación B26.
4. **Hallazgos de esta pasada escalados a fase futura**, si los hubo, con su id de §4 de `125-E2E.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md && grep -c "destino" .planning/phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md</automated>
  </verify>
  <acceptance_criteria>
    - Cada ítem del handoff tiene los **5** campos (qué es · evidencia · forma del cierre · destino · quién)
    - Los 2 ítems post-deploy de 122 figuran como CERRADOS con su fragmento DOM
    - Ningún ítem de juicio humano aparece como "aprobado" sin respuesta verbatim del operador
    - La deuda de operador viva queda re-nombrada con su origen (`124-HANDOFF-EXACTITUD.md`, `118-OPERATOR-CHECKPOINT`)
  </acceptance_criteria>
  <done>Nada queda cerrado en silencio: lo no respondido tiene destino escrito.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de cierre — suite, tsc, guards y régimen de la fase</name>
  <read_first>
    - `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md` §1 (los mismos checks del gate pre-deploy: comparar contra ellos, no contra un número recordado)
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` §3 (lista nominal de los guards de régimen)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-E2E.md</files>
  <action>
Correr el gate de cierre y escribir §6 de `125-E2E.md` con la salida:

1. `set -o pipefail`; suite `app/` (`pnpm --filter ./app exec vitest run`) → exit 0 y conteo, comparado
   con el número del gate pre-deploy: **debe ser el mismo** (esta fase no toca código de producción).
2. `pnpm --filter ./app exec tsc --noEmit` → exit 0.
3. Guards de régimen nombrados uno por uno y verdes.
4. **Régimen de la fase**, probado por comando:
   - `git diff --name-only <base-de-la-fase>..HEAD -- app/ packages/ supabase/` → **vacío** (fase read-only
     sobre código; el único acto de escritura fue el deploy del código ya commiteado).
   - `ls supabase/migrations | tail -3` → última migración sigue siendo la de 124; **cero migraciones nuevas**.
   - Cero flips, con el diff **acotado a código y config** — `.planning/` se excluye a propósito porque los
     artefactos de esta fase contienen `gate`/`wrangler` en su propio nombre y texto, y un grep sin acotar se
     auto-falsaría:
     `git diff --name-only <base>..HEAD -- app/ packages/ supabase/ .github/ ':!.planning/' | grep -iE "\.env|wrangler|gate"` → **vacío**.
   - `grep -rnE "postgres://|postgresql://" .planning/phases/125-*/` → cero cadenas de conexión reales.
   - `grep -rnE "\\b(TBD|FIXME|XXX)\\b" .planning/phases/125-*/*.md` → cero marcadores de deuda **propios**.
     Un hit sólo es admisible si es una **cita verbatim** de otro artefacto (p. ej. una fila copiada de
     `124-HANDOFF-EXACTITUD.md`): en ese caso se lista el hit con su origen y se declara como cita, jamás se
     borra el criterio. Cero hits sin origen citado.
5. Cerrar §6 con la **trazabilidad a los 5 Success Criteria** del ROADMAP: una fila por SC con el artefacto
   y el fragmento que lo evidencia.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit && pnpm exec vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Suite `app/` exit 0 con conteo **idéntico** al del gate pre-deploy (cualquier desvío se explica con su commit)
    - `tsc --noEmit` exit 0
    - Los 14 guards nombrados y verdes
    - Los 5 comandos de régimen con salida **vacía / cero** según corresponde, citados verbatim
    - El check de flips usa el diff **acotado** (`-- app/ packages/ supabase/ .github/ ':!.planning/'`); un grep sobre el diff completo NO cuenta (se auto-falsa con los artefactos de la propia fase)
    - §6 traza los **5** SC del ROADMAP, uno por fila, cada uno con su evidencia
  </acceptance_criteria>
  <done>La fase cierra con números, no con adjetivos, y su régimen queda probado por comando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefactos de la fase → repositorio público | un fragmento pegado podría llevar credenciales o PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-19 | Information disclosure | `125-E2E.md` y fragmentos | mitigate | Grep de cierre por `postgres://`/`postgresql://` sobre el directorio de la fase → cero; `SUPABASE_DB_URL` nunca expandida |
| T-125-20 | Repudiation | veredicto de la fase | mitigate | Cobertura por lista con denominador (19 rutas, 60 emisores); prohibido "se recorrió todo" |
| T-125-21 | Tampering | régimen read-only | mitigate | `git diff` sobre `app/`, `packages/`, `supabase/` vacío + cero migraciones nuevas, ambos como criterio de aceptación |
</threat_model>

<verification>
- `125-E2E.md` con las dos tablas de cobertura completas (19 y 60 filas), §3 con las 8 filas heredadas,
  §4 con disposición por hallazgo y §6 con el gate de cierre.
- `125-HANDOFF-HUMANO.md` con los 5 campos por ítem.
- Régimen probado por comando: cero código, cero migraciones, cero flips, cero credenciales.
</verification>

<success_criteria>
E2E-01 cerrado: la pasada es auditable desde un solo documento, la cobertura está declarada por lista,
lo no cubierto y lo no respondido tienen destino escrito, y la suite/tsc/guards están verdes con números.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-07-SUMMARY.md` when done
</output>
