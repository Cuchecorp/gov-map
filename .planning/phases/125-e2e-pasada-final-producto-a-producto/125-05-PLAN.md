---
phase: 125-e2e-pasada-final-producto-a-producto
plan: 05
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.json
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.txt
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-ROBOTS-RUN.json
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-ROBOTS-RUN.txt
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.json
  - .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.txt
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "Los links internos del manifiesto siguen resolviendo sobre el deploy nuevo: cero 404, anclas presentes en el DOM destino"
    - "La muestra estratificada de links externos se re-prueba con rate-limit y robots-primero; ningún host recibe ráfagas"
    - "Toda diferencia contra la corrida de 114/115 queda clasificada: patrón malo (se arregla) vs fuente caída/WAF (se declara)"
  artifacts:
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md"
      provides: "Comparación post-deploy vs corrida de 114/115, con clasificación de cada diferencia"
      contains: "rate-limit"
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.json"
      provides: "Corrida completa del runner de links internos contra el deploy nuevo"
    - path: ".planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.json"
      provides: "Corrida de la muestra estratificada de links externos"
  key_links:
    - from: "scripts/verificar-links-internos.mjs"
      to: "deploy real"
      via: "manifiesto scripts/links-internos-manifiesto.mjs"
      pattern: "verificar-links-internos"
    - from: "scripts/probar-links-externos.mjs"
      to: "fuentes oficiales"
      via: "115-PATRONES.md §4 + 115-ROBOTS.txt"
      pattern: "probar-links-externos"
---

<objective>
Re-verificar sobre el deploy nuevo los links internos (exhaustivo, contra el Worker propio) y la muestra
estratificada de links externos (con rate-limit y robots-primero), comparando contra la corrida de las
fases 114 y 115.

Purpose: SC3 del ROADMAP.
Output: `125-RE-VERIFICACION.md` + las salidas de los dos runners.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-CONTEXT.md
@.planning/phases/125-e2e-pasada-final-producto-a-producto/125-DEPLOY-RUNBOOK.md
@scripts/verificar-links-internos.mjs
@scripts/probar-links-externos.mjs
</context>

<regla_LOCKED_de_mesura>
**JAMÁS ráfagas ni crawl exhaustivo contra fuentes gubernamentales** (decisión operador 2026-07-27).
Los dos runners ya implementan el régimen —secuencial, `DELAY_MS = 2500` entre requests **y entre
hosts**, User-Agent identificatorio, gate robots-primero fail-closed, GET con `--range 0-8191` (el
método de sólo-cabeceras está PROHIBIDO), curl-first ante el WAF de `camara.cl`—. **No se reescriben,
no se paraleliza, no se sube el ritmo, no se amplía el manifiesto.** Se corren tal como están.
"Patrón malo" se arregla; "fuente caída / WAF" se **declara**, jamás se evade.
</regla_LOCKED_de_mesura>

<tasks>

<task type="auto">
  <name>Task 1: Re-correr el runner de links internos contra el deploy nuevo y comparar con 114</name>
  <read_first>
    - `scripts/verificar-links-internos.mjs` — cabecera completa: comandos, `--route`/`--tipo`/`--json-only`, la desviación declarada de cheerio, la mesura (secuencial + `sleep(400)`, caché de HTML por URL, cota de 15 s, un reintento sólo ante fallo de red), y la regla de portabilidad (`os.tmpdir()`, `MSYS_NO_PATHCONV=1`)
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-POST.txt` y `114-ANCLAS.md` (la línea base a comparar) + `114-HALLAZGOS.md`
  </read_first>
  <files>
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.json,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.txt,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md
  </files>
  <action>
**PASO 0 — PRECONDICIÓN DE FRESCURA (BLOQUEANTE, antes de correr los runners).**
`depends_on` garantiza **orden**, no **frescura**: si el deploy falló a medias, si la ventana de
propagación de Cloudflare (10-30 s) no cerró, o si el Worker sirvió una versión anterior, la
re-verificación mediría **el sitio viejo** y la daría por verde.

1. Leer `VERSIÓN DESPLEGADA: <uuid>` de `125-DEPLOY-RUNBOOK.md` y registrarlo en la cabecera de
   `125-RE-VERIFICACION.md`. Si el runbook no lo declara → **PARAR**.
2. Marcador de frescura: `/proyecto/14309-04` debe contener `3,8` en la línea de cobertura lobby↔PL
   (literal que **solo existe tras el fix de 122**). Grep tolerante a `<!-- -->`.
3. Si no aparece → **PARAR y escalar al orquestador**. No correr los runners contra un deploy dudoso:
   sus salidas quedan archivadas como evidencia y una salida falsa contamina el cierre.

1. Corrida completa, sin filtros:
   `node scripts/verificar-links-internos.mjs --out .planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT`
   (en git-bash, prefijar `MSYS_NO_PATHCONV=1` si se usa `--route`/`--tipo`; sin filtros no hace falta).
   `set -o pipefail`.
2. **No modificar el runner ni el manifiesto.** Si el manifiesto quedó desincronizado del inventario, se
   **declara** como hallazgo en el artefacto; ampliarlo sería trabajo de otra fase.
3. Comparar entrada por entrada contra `114-CORRIDA-POST`:
   - **cero 404** en links internos;
   - cada ancla `#id` presente en el DOM de su página destino;
   - las aserciones de ausencia siguen dando ausencia.
4. Toda diferencia se clasifica en una de tres: (a) **mejora** (algo que 114 dejó rojo y el deploy cerró),
   (b) **regresión** (rojo nuevo ⇒ hallazgo, se escala), (c) **cambio de dato** (el destino existe pero el
   contenido cambió por ingesta ⇒ se declara con la fecha).
5. Escribir §1 de `125-RE-VERIFICACION.md`: comando, totales (`ok` / `404` / `ancla ausente`), y la tabla
   de diferencias con su clasificación. Cero "todo verde" sin los números.
  </action>
  <verify>
    <automated>node -e "const r=require('./.planning/phases/125-e2e-pasada-final-producto-a-producto/125-LINKS-INT.json');console.log(JSON.stringify(Object.keys(r).slice(0,5)))"</automated>
  </verify>
  <acceptance_criteria>
    - `125-LINKS-INT.json` y `.txt` existen y cubren el manifiesto completo (conteo de entradas ≥ el de `114-CORRIDA-POST`)
    - **0** links internos con status 404
    - **0** anclas declaradas ausentes del DOM destino
    - Toda diferencia vs 114 clasificada como mejora / regresión / cambio de dato, con su fila
    - `git diff --name-only -- scripts/` → **vacío** (el runner no se tocó)
  </acceptance_criteria>
  <done>Links internos re-verificados sobre el deploy nuevo y comparados con la línea base de 114.</done>
</task>

<task type="auto">
  <name>Task 2: Re-correr la muestra estratificada de links externos con robots-primero y rate-limit</name>
  <read_first>
    - `scripts/probar-links-externos.mjs` — cabecera completa: por qué curl y no el cliente HTTP de Node (WAF de `camara.cl`), GET con `--range 0-8191`, `DELAY_MS = 2500` secuencial y entre hosts, el gate de orden robots-primero (T-115-13) que **aborta** si `115-ROBOTS.txt` no existe o si un caso cae bajo un `Disallow`, y la política de reintento (uno, sólo ante fallo de red, tras 5 s; 403/429/5xx NO se reintenta)
    - `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md` §4 (la muestra: ≥1 caso por patrón×host) · `115-ROBOTS.txt` · `115-MUESTRA.txt` y `115-VEREDICTO.md` (línea base)
  </read_first>
  <files>
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-ROBOTS-RUN.json,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-ROBOTS-RUN.txt,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.json,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.txt,
    .planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md
  </files>
  <action>
1. **Primero robots, siempre**:
   `node scripts/probar-links-externos.mjs --robots --out .planning/phases/125-e2e-pasada-final-producto-a-producto/125-ROBOTS-RUN`
   Si un host cambió sus directivas y ahora prohíbe un path de la muestra, el runner **debe abortar**:
   eso NO se evade — se **declara** y ese caso se retira de la corrida citando la directiva.
2. **Después la muestra**:
   `node scripts/probar-links-externos.mjs --out .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT`
   Sin filtros, sin ampliar el manifiesto, sin tocar `DELAY_MS`, sin concurrencia.
3. Comparar contra `115-MUESTRA.txt` / `115-VEREDICTO.md` caso por caso. Clasificar cada diferencia:
   - **patrón malo** (la URL que el sitio construye está mal ⇒ es un defecto nuestro; se registra como
     hallazgo con el archivo/builder responsable, y su fix es trabajo de fase, no de este plan salvo que
     sea trivial y de copy);
   - **fuente caída / WAF / 403** (es de ellos ⇒ se **declara** con código y fecha, jamás se evade ni se
     reintenta en ráfaga).
4. **Instrumentar la prueba de mesura, no afirmarla**: capturar `date -u +%s` **inmediatamente antes** y
   **inmediatamente después** de la corrida de la muestra (o envolverla en `time`), y pegar ambos timestamps
   verbatim en el artefacto. Sin los dos sellos de tiempo el criterio de ≥2 s/request es indemostrable.
5. Escribir §2 de `125-RE-VERIFICACION.md`: comando, hosts tocados, nº de requests, delay efectivo, y la
   tabla caso×resultado con su clasificación. Incluir la **prueba de mesura** calculada a partir de los dos
   timestamps: `(fin - inicio) / nº requests` (un cociente < 2 s/request delataría una ráfaga).
  </action>
  <verify>
    <automated>test -f .planning/phases/125-e2e-pasada-final-producto-a-producto/125-MUESTRA-EXT.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `125-ROBOTS-RUN` generado **antes** que `125-MUESTRA-EXT` (comparar mtime de los archivos)
    - `125-MUESTRA-EXT.json` cubre ≥1 caso por patrón×host, igual que `115-PATRONES.md` §4
    - Los dos sellos `date -u +%s` (inicio y fin) pegados verbatim en el artefacto
    - `(fin - inicio) / nº de requests` → **≥ 2 s por request**, con la aritmética escrita (prueba de que no hubo ráfaga)
    - Cada diferencia vs 115 clasificada como *patrón malo* o *fuente caída/WAF*, con código HTTP y fecha
    - `git diff --name-only -- scripts/` → **vacío**
  </acceptance_criteria>
  <done>Muestra externa re-probada con robots-primero, rate-limit demostrado y diferencias clasificadas.</done>
</task>

<task type="auto">
  <name>Task 3: Fechas visibles — el barrido de etiquetado sobre el DOM del deploy</name>
  <read_first>
    - `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-DISPOSICION.md` §1 (las 14 filas F-01…F-14 con ANTES/DESPUÉS verbatim) y §3 (los greps de cierre: `Actualizado` fuera del tooltip, `captura` pelado, `corte al`, `según fuente al`)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §0.4 columna *fechas enumeradas por* (las 19 filas de la Tabla D)
  </read_first>
  <files>.planning/phases/125-e2e-pasada-final-producto-a-producto/125-RE-VERIFICACION.md</files>
  <action>
Los greps de 117 §3 corrían sobre el **código**. Aquí se corren sobre el **HTML servido** por el deploy,
que es lo que 117 no pudo verificar (LÍMITE análogo al A de 122).

Sobre una lista declarada de rutas —las 19 filas de la Tabla D que emiten fecha, instanciadas con los
sujetos deterministas— descargar el HTML una vez por ruta (secuencial, 1 s) y aplicar:
1. `grep -c "Actualizado hace"` → **0** en todas.
2. `grep -riE "(^|[^a-záéíóúñ])captura([^a-záéíóúñ]|$)"` → cero **copy renderizado**. Los identificadores
   del payload RSC (`fecha_captura` serializado) se declaran uno por uno y no cuentan como copy: registrar
   el criterio de separación usado.
3. `grep -c "corte al"` → **0**.
4. `grep -c "según fuente al "` → **≥ 1** en toda ruta con dato fechado; registrar el conteo por ruta.
5. `grep -c "recalculado por el Observatorio al "` → presente donde 117 F-02 lo puso (cruces de
   parlamentario y de proyecto).
6. Muestrear **3 fechas concretas** de distinta clase y verificar el helper correcto contra la disposición
   de 117: una **date-only disfrazada de timestamptz** (`fechaCivilCorta`, p. ej. una citación de `/agenda`:
   la parte UTC ES el día chileno, jamás convertir tz), una **fecha de hecho** (`fechaHechoCorta`, un hito
   del timeline de `/proyecto/14309-04`), y una **fecha de captura** (idiom `según fuente al `). Citar cada
   una verbatim y nombrar la fila F-NN que la gobierna.
Escribir §3 de `125-RE-VERIFICACION.md` con la lista de rutas barridas (cobertura declarada, no "todas")
y la tabla de los 5 greps + las 3 fechas muestreadas.
  </action>
  <verify>
    <automated>curl -s https://observatorio-congreso.thevalis.workers.dev/agenda | grep -c "Actualizado hace"</automated>
  </verify>
  <acceptance_criteria>
    - Lista explícita de rutas barridas (cobertura declarada); cero uso de la palabra "todas" sin lista
    - `Actualizado hace` → **0** y `corte al` → **0** en cada ruta barrida
    - `según fuente al ` → **≥ 1** en cada ruta con dato fechado, con el conteo por ruta
    - Toda ocurrencia de `captura` justificada como identificador de payload RSC, con el criterio escrito
    - Las 3 fechas muestreadas citadas verbatim, cada una con su fila F-NN de 117
  </acceptance_criteria>
  <done>El etiquetado de fechas queda verificado sobre el DOM del deploy, no sólo sobre el código.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → fuentes gubernamentales | requests salientes a camara.cl, senado.cl, BCN, leylobby |
| agente → Worker propio | recorrido del manifiesto de links internos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-13 | Denial of service | fuentes gubernamentales | mitigate | Runner de 115 sin modificar: secuencial, `DELAY_MS=2500` entre requests y entre hosts, UA identificatorio, robots-primero fail-closed. Prueba de mesura ≥2 s/request en el artefacto |
| T-125-14 | Repudiation | resultado de la muestra | mitigate | Salidas `.json`/`.txt` del runner como evidencia; cada diferencia clasificada con código HTTP y fecha |
| T-125-15 | Tampering | los runners | mitigate | `git diff --name-only -- scripts/` vacío como criterio de aceptación: los runners se corren, no se reescriben |
</threat_model>

<verification>
- Links internos: cero 404, anclas presentes, diferencias clasificadas.
- Links externos: robots-primero, ≥2 s/request demostrado, diferencias clasificadas en patrón-malo vs fuente-caída.
- Fechas: los 4 greps de régimen sobre el HTML servido, más 3 fechas muestreadas contra su fila de 117.
</verification>

<success_criteria>
SC3 y la mitad de fechas de SC4 del ROADMAP: links internos y muestra externa re-verificados post-deploy
con rate-limit respetado y demostrado; fechas correctamente etiquetadas en el DOM real.
</success_criteria>

<output>
Create `.planning/phases/125-e2e-pasada-final-producto-a-producto/125-05-SUMMARY.md` when done
</output>
