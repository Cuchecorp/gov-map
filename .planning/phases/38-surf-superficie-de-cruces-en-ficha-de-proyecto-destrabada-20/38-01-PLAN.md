---
phase: 38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0049_cruces_de_proyecto.sql
  - supabase/tests/0049_cruces_de_proyecto.test.sql
  - app/lib/lockdown-guard.test.ts
  - app/lib/types.ts
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "El RPC cruces_de_proyecto(boletin) existe como security definer, doble revoke, cero grant (escrito, no aplicado)"
    - "El pgTAP 0049 verifica contrato de columnas + deny anon + no-PII + join sector×voto'si'confirmado con fixture rollback"
    - "El árbol público puede invocar cruces_de_proyecto (allowlist) y JAMÁS emite partido/rut/email"
  artifacts:
    - path: "supabase/migrations/0049_cruces_de_proyecto.sql"
      provides: "RPC security definer con la ruta sector vía proyecto_ficha.sector_id"
      contains: "create or replace function public.cruces_de_proyecto"
    - path: "supabase/tests/0049_cruces_de_proyecto.test.sql"
      provides: "pgTAP post-apply, fixture sembrado en transacción rollback"
      contains: "select plan("
    - path: "app/lib/types.ts"
      provides: "Tipo PII-safe CruceProyectoRow para la sección frontend"
      contains: "CruceProyectoRow"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "cruces_de_proyecto en PUBLIC_RPC_ALLOWLIST"
      contains: "cruces_de_proyecto"
  key_links:
    - from: "supabase/migrations/0049_cruces_de_proyecto.sql"
      to: "public.proyecto_ficha.sector_id"
      via: "join sector del proyecto"
      pattern: "proyecto_ficha"
    - from: "supabase/migrations/0049_cruces_de_proyecto.sql"
      to: "public.cruce_senal"
      via: "filtro sector_id ∩ votantes a favor"
      pattern: "cruce_senal"
---

<objective>
Escribir la RPC `cruces_de_proyecto(p_boletin text)` (security definer, idiom post-Camino A espejo de 0048), su pgTAP post-apply con fixture rollback, el tipo PII-safe `CruceProyectoRow` en el frontend, y extender la allowlist del lockdown-guard. NADA se aplica a PROD en este plan: la migración se escribe y committea; aplicarla es checkpoint de operador (Plan 03).

Purpose: entregar el único canal público PII-safe que yuxtapone "parlamentarios que votaron a favor del boletín" ∩ "cruces de lobby en el sector del proyecto", sin fabricar la relación sector (ruta verificada `proyecto_ficha.sector_id`).
Output: `0049_cruces_de_proyecto.sql`, `0049_cruces_de_proyecto.test.sql`, tipo `CruceProyectoRow`, allowlist extendida. Suite 670 + tsc + lockdown-guard verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-CONTEXT.md
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase. Usar directamente. -->

RPC shape propuesto (RESEARCH §Recommended RPC shape — predicados [VERIFIED: psql PROD], columnas ajustables). Contrato pineado a 9 args posicionales (p_boletin + 8 columnas):

  create or replace function public.cruces_de_proyecto(p_boletin text)
  returns table (
    parlamentario_id   text,        -- p.id → link /parlamentario/[id]
    nombre_normalizado text,        -- proyección pública (NUNCA rut/partido/email)
    sector_id          text,
    sector_etiqueta    text,        -- public.sector.etiqueta (join s.codigo = cs.sector_id)
    tipo_senal         text,        -- 'lobby_sector'
    conteo             int,         -- reuniones (conteo neutro)
    evidencia          jsonb,       -- {conteo, items[]} PII-safe de cruce_senal
    fecha_captura      timestamptz  -- frescura del rebuild (WR-02/F41)
  )
  language sql stable security definer set search_path = '' ...

Ruta de datos [VERIFIED: psql PROD]:
  sec    = proyecto_ficha.sector_id WHERE boletin=p_boletin AND sector_id is not null
  afavor = distinct voto.parlamentario_id JOIN votacion ON votacion.id=voto.votacion_id
           WHERE votacion.boletin=p_boletin AND voto.seleccion='si'
           AND voto.estado_vinculo='confirmado' AND voto.parlamentario_id is not null
  select cruce_senal cs JOIN sec ON cs.sector_id=sec.sector_id
         JOIN afavor a ON a.parlamentario_id=cs.parlamentario_id
         JOIN public.sector s ON s.codigo=cs.sector_id
         JOIN public.parlamentario p ON p.id=cs.parlamentario_id
         order by cs.conteo desc, p.nombre_normalizado asc

Tipo base a extender (app/lib/types.ts:311-353):
  CruceSenalRpcRow { sector_id, sector_etiqueta, tipo_senal, conteo, evidencia: CruceEvidencia, fecha_captura }
  CruceEvidencia { conteo, items: CruceEvidenciaItem[] }
  CruceEvidenciaItem { tipo, fecha, contraparte_nombre_crudo, audiencia_id, enlace_fuente }

Allowlist (app/lib/lockdown-guard.test.ts:165) — Set alfabético:
  PUBLIC_RPC_ALLOWLIST contiene ya 'cruces_de_parlamentario', 'lobby_en_tramitacion', … (insertar 'cruces_de_proyecto' entre 'cruces_de_parlamentario' y 'declaraciones_de_parlamentario').
  cruce_senal YA está en PII_TABLES (línea 133) → el árbol público solo puede tocarlo vía RPC.
</interfaces>

@supabase/migrations/0048_lobby_en_tramitacion.sql
@supabase/tests/0048_lobby_en_tramitacion.test.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración 0049_cruces_de_proyecto.sql</name>
  <read_first>
    - supabase/migrations/0048_lobby_en_tramitacion.sql (idiom EXACTO: security definer, set search_path='', drop defensivo, DOBLE revoke líneas 131-132)
    - 38-RESEARCH.md §5 (idiom post-Camino A) + §Recommended RPC shape (predicados verificados)
  </read_first>
  <action>
    Crear `supabase/migrations/0049_cruces_de_proyecto.sql` espejando VERBATIM el idiom ACL de 0048. La RPC es NUEVA (incluir `drop function if exists public.cruces_de_proyecto(text);` como idiom defensivo antes del create). Firma `(p_boletin text)`, `returns table` con las 8 columnas del `<interfaces>` en ese orden posicional exacto, `language sql stable security definer set search_path = ''`, TODOS los nombres calificados con `public.`. Cuerpo = el CTE `sec` (proyecto_ficha.sector_id) + `afavor` (voto.seleccion='si' AND estado_vinculo='confirmado' AND parlamentario_id is not null, JOIN votacion por boletin) + select con los 4 joins (cruce_senal, sector, parlamentario) y `order by cs.conteo desc, p.nombre_normalizado asc`. Cabecera de comentarios que documente: (a) POR QUÉ security definer (LEGAL-03: lee public.parlamentario deny-by-default internamente, emite solo derivado público — JAMÁS partido/rut/email), (b) la ruta de sector = Alternativa B `proyecto_ficha.sector_id` (cero fabricación; boletín sin ficha/sector → 0 filas empty honesto), (c) el DOBLE revoke tras el create limpia los DEFAULT PRIVILEGES (from public; from anon, authenticated), CERO grant (Camino A, sitio lee con service_role), (d) APLICACIÓN = CHECKPOINT DE OPERADOR (52-06, `psql --db-url --single-transaction`, NUNCA `supabase db push`; `tsc`/`pnpm test` NO prueban que Postgres ejecutó el DDL). Terminar con las dos sentencias revoke exactas: `revoke all on function public.cruces_de_proyecto(text) from public;` y `revoke all on function public.cruces_de_proyecto(text) from anon, authenticated;`. CERO `grant … to anon`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0049_cruces_de_proyecto.sql','utf8').replace(/--.*$/gm,'').toLowerCase(); const g=/grant\s+[^;]*\bto\b\s*[^;]*\b(anon|public)\b/.test(s); const dv=(s.match(/revoke all on function public\.cruces_de_proyecto\(text\)/g)||[]).length; const sd=/security definer set search_path = ''/.test(s); process.exit(!g && dv===2 && sd ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo existe con `create or replace function public.cruces_de_proyecto(p_boletin text)`.
    - `security definer set search_path = ''` presente; cuerpo usa `proyecto_ficha`, `cruce_senal`, `sector`, `parlamentario`, `voto`, `votacion` todos calificados con `public.`.
    - Predicados exactos: `seleccion = 'si'`, `estado_vinculo = 'confirmado'`, `sector_id is not null`, `parlamentario_id is not null`.
    - EXACTAMENTE dos `revoke all on function public.cruces_de_proyecto(text)` (from public; from anon, authenticated); CERO `grant … to anon/public` (grep-gate exit 0, comentarios stripeados).
  </acceptance_criteria>
  <done>Migración escrita, idiom ACL byte-espejo de 0048, grep-gate verde. NO aplicada a PROD.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir el pgTAP 0049_cruces_de_proyecto.test.sql (fixture rollback)</name>
  <read_first>
    - supabase/tests/0048_lobby_en_tramitacion.test.sql (idiom pgTAP COMPLETO: proargnames/array_to_string, prosecdef, proconfig, has_function_privilege deny, fixture begin;…;rollback;, casos positivo+negativo)
    - 38-RESEARCH.md §5 pgTAP + §Data Reality #1/#2 (esquema cruce_senal, voto, votacion, proyecto_ficha)
  </read_first>
  <action>
    Crear `supabase/tests/0049_cruces_de_proyecto.test.sql` espejando 0048's test. `begin; select plan(10);`. Asserts de contrato: (1) `has_function('public','cruces_de_proyecto',array['text'])`; (2) `array_to_string(proargnames,',')` = `'p_boletin,parlamentario_id,nombre_normalizado,sector_id,sector_etiqueta,tipo_senal,conteo,evidencia,fecha_captura'`; (3) `prosecdef=true`; (4) `proconfig … like '%search_path=%'`; (5) `not has_function_privilege('anon','public.cruces_de_proyecto(text)','execute')`; (6) `array_to_string(proargnames,',') !~* '\y(partido|rut|email)\y'`. Fixture sembrado dentro de la transacción (NUNCA datos PROD — usar boletín `BTEST-38`, parlamentario `PTEST_SURF`, sector de prueba): insertar `parlamentario` (con partido no-null para probar que NO se filtra), `sector` (codigo `sec_test`, etiqueta `Sector de prueba`), `proyecto` (boletin BTEST-38), `proyecto_ficha` (boletin BTEST-38, sector_id `sec_test`), `votacion` (id, boletin BTEST-38), `voto` del PTEST_SURF con `seleccion='si'` + `estado_vinculo='confirmado'`, un SEGUNDO parlamentario `PTEST_NO` con voto `'no'`, y `cruce_senal` para ambos parlamentarios en `sec_test` con `tipo_senal='lobby_sector'` + `evidencia` jsonb mínima. Asserts de datos: (7) `count(*) from cruces_de_proyecto('BTEST-38') where parlamentario_id='PTEST_SURF'` = 1; (8) la fila emite `nombre_normalizado||'|'||sector_etiqueta` esperado; (9) CASO NEGATIVO: `count where parlamentario_id='PTEST_NO'` = 0 (votó 'no' → no está en afavor); (10) CASO NEGATIVO sector: un boletín SIN fila en proyecto_ficha (`BTEST-NOSEC`) → `count(*) from cruces_de_proyecto('BTEST-NOSEC')` = 0 (empty honesto, sin fabricar sector). `select * from finish(); rollback;`. Cabecera que declare: corre vía `psql -tA -f` post-apply (fuera del glob vitest), lo ejecuta el OPERADOR el día del apply (checkpoint 52-06).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/tests/0049_cruces_de_proyecto.test.sql','utf8'); process.exit(/select plan\(10\)/.test(s) && /rollback;/.test(s) && /p_boletin,parlamentario_id,nombre_normalizado,sector_id,sector_etiqueta,tipo_senal,conteo,evidencia,fecha_captura/.test(s) && /PTEST_NO/.test(s) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `select plan(10)` y `rollback;` presentes; ningún assert lee datos PROD (todos sobre BTEST-38/BTEST-NOSEC).
    - Assert 2 pinea las 9 columnas posicionales exactas; assert 6 verifica `!~* '\y(partido|rut|email)\y'`.
    - Assert 9 (voto 'no' excluido) y assert 10 (boletín sin sector → 0 filas) presentes como casos negativos.
    - Fixture inserta partido no-null en parlamentario y confirma que el returns table NO lo emite.
  </acceptance_criteria>
  <done>pgTAP escrito con 10 asserts (contrato + no-PII + deny anon + positivo + 2 negativos), fixture rollback autocontenido. Lo corre el operador post-apply.</done>
</task>

<task type="auto">
  <name>Task 3: Tipo CruceProyectoRow + extender allowlist del lockdown-guard</name>
  <read_first>
    - app/lib/types.ts:311-353 (CruceSenalRpcRow, CruceEvidencia, CruceEvidenciaItem — base a extender)
    - app/lib/lockdown-guard.test.ts:133,165 (PII_TABLES, PUBLIC_RPC_ALLOWLIST)
  </read_first>
  <action>
    En `app/lib/types.ts`: añadir `export interface CruceProyectoRow` PII-safe reutilizando `CruceEvidencia`. Campos: `parlamentario_id: string` (para el link `/parlamentario/[id]`), `nombre_normalizado: string`, `sector_id: string`, `sector_etiqueta: string`, `tipo_senal: string` (comentar: hoy solo `'lobby_sector'`, degradar honesto), `conteo: number` (conteo neutro), `evidencia: CruceEvidencia`, `fecha_captura: string` (ISO timestamptz → `ProvenanceBadge.capturedAt`). Docstring: proyección pública espejo de 0049, NUNCA rut/partido/email. En `app/lib/lockdown-guard.test.ts`: insertar `"cruces_de_proyecto",` en `PUBLIC_RPC_ALLOWLIST` (posición alfabética, entre `"cruces_de_parlamentario"` y `"declaraciones_de_parlamentario"`). NO tocar PII_TABLES (cruce_senal ya está). Verificar que el guard sigue verde (el árbol público aún no invoca la RPC — la invocación llega en Plan 02; añadir a la allowlist ahora la deja lista sin romper nada).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run lockdown-guard && pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `CruceProyectoRow` exportado en app/lib/types.ts con los 8 campos y `evidencia: CruceEvidencia`; `tsc -b` limpio.
    - `"cruces_de_proyecto"` presente en PUBLIC_RPC_ALLOWLIST en orden alfabético.
    - `pnpm exec vitest run lockdown-guard` verde (block A + block B sin regresión).
  </acceptance_criteria>
  <done>Tipo PII-safe disponible para Plan 02; allowlist extendida; guard + tsc verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → Next server (service_role) | El navegador nunca recibe el service_role; la RPC se invoca server-only |
| Next server → Postgres (RPC) | La RPC security definer lee tablas PII/deny-by-default internamente y emite solo derivado público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure | `cruces_de_proyecto` returns table | mitigate | El returns table proyecta solo `parlamentario_id/nombre_normalizado/sector_*/conteo/evidencia/fecha_captura`; JAMÁS partido/rut/email. pgTAP assert 6 `proargnames !~* '\y(partido\|rut\|email)\y'`; fixture con partido no-null confirma que no se emite |
| T-38-02 | Elevation of Privilege | ACL de la función nueva | mitigate | DOBLE revoke explícito (from public; from anon, authenticated) limpia los DEFAULT PRIVILEGES que re-conceden EXECUTE a anon sobre cada función nueva; CERO grant; pgTAP assert 5 (anon NO execute); guard CI block A |
| T-38-03 | Information Disclosure | acceso directo a `cruce_senal`/`parlamentario` | mitigate | Ambas en deny-by-default; `cruce_senal` en PII_TABLES → guard block B bloquea `.from('cruce_senal')` en el árbol público. La RPC allowlisted es el único canal |
| T-38-04 | Tampering | inyección en `p_boletin` | mitigate | supabase-js parametriza `p_boletin` (no interpolación); el consumidor (Plan 02) valida `BOLETIN_RE` en page.tsx:50 antes de tocar la DB |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas esta fase (UI-SPEC §Design System "New deps: NONE"); no hay tareas de install → sin superficie supply-chain |
</threat_model>

<verification>
- `cd app && pnpm test && pnpm exec tsc -b` → suite 670 baseline (nunca menos) + tsc limpio + lockdown-guard verde.
- Grep-gate de la migración: cero `grant … to anon/public`, exactamente 2 revoke, security definer.
- pgTAP `0049_*.test.sql` es post-apply (fuera del glob) — lo corre el operador en Plan 03; en este plan solo se verifica que existe con 10 asserts bien formados.
</verification>

<success_criteria>
- 0049_cruces_de_proyecto.sql escrita (NO aplicada), idiom ACL espejo de 0048, grep-gate verde.
- 0049_cruces_de_proyecto.test.sql con 10 asserts (contrato + no-PII + deny anon + 1 positivo + 2 negativos), fixture rollback.
- CruceProyectoRow exportado; cruces_de_proyecto en la allowlist; suite 670 + tsc + lockdown-guard verdes.
</success_criteria>

<output>
Create `.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-01-SUMMARY.md` when done
</output>
