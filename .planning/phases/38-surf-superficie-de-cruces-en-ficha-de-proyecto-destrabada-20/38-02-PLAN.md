---
phase: 38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - app/components/cruces-de-proyecto.tsx
  - app/components/cruces-de-proyecto.test.tsx
  - app/app/proyecto/[boletin]/page.tsx
  - app/app/proyecto/[boletin]/page-cruces.test.ts
autonomous: true
requirements: [SURF-02]

must_haves:
  truths:
    - "La ficha de /proyecto/[boletin] muestra parlamentarios que votaron a favor con cruces en el sector, cada nombre como LINK a /parlamentario/[id]"
    - "La sección degrada honesta pre-apply: RPC ausente (PGRST202) → null, wrapper mt-12 persiste, sin heading huérfano"
    - "Copy factual sin verbo causal (negative-match verde), caveat anti-causal exactamente 1×, conteo neutro, líneas voto/reunión separadas"
  artifacts:
    - path: "app/components/cruces-de-proyecto.tsx"
      provides: "CrucesView (pura) + CrucesSection (server, degrade PGRST202)"
      contains: "PGRST202"
    - path: "app/components/cruces-de-proyecto.test.tsx"
      provides: "RTL: capa-1/2, empty honesto, degrade, throw, banned-vocab"
      contains: "PGRST202"
    - path: "app/app/proyecto/[boletin]/page.tsx"
      provides: "<section id=cruces> + rail entry Cruces ◆ tras #lobby-tramitacion"
      contains: "id=\"cruces\""
  key_links:
    - from: "app/components/cruces-de-proyecto.tsx"
      to: "cruces_de_proyecto RPC"
      via: "sb.rpc + degrade PGRST202→null"
      pattern: "cruces_de_proyecto"
    - from: "app/app/proyecto/[boletin]/page.tsx"
      to: "CrucesSection + CrucesCapa1"
      via: "DetalleColapsable triggerVariant primary"
      pattern: "CrucesSection"
---

<objective>
Construir la superficie frontend de cruces en la ficha de proyecto: `CrucesView` (pura, testeable) + `CrucesSection` (Server Component con degrade honesto PGRST202→null), cablearla en `page.tsx` como carril hermano `mt-12` tras `#lobby-tramitacion` con marco petróleo capa-1 + `DetalleColapsable` primary "Explorar los N cruces" + entrada de rail "Cruces ◆", y cubrirla con RTL (capa-1/2, empty, degrade, throw, negative-match anti-causal).

Purpose: entregar el valor SURF-02 al ciudadano — ver qué parlamentarios que votaron a favor del proyecto se reunieron con el sector, con fuente/fecha/link y cero insinuación. Hereda la gramática visual de F55 verbatim (cero tokens/deps nuevos).
Output: `cruces-de-proyecto.tsx`, `cruces-de-proyecto.test.tsx`, `page.tsx` extendido, `page-cruces.test.ts`. Suite (baseline 670) + tsc verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-UI-SPEC.md
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-RESEARCH.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase. Usar directamente. -->

Tipo (creado en Plan 01, app/lib/types.ts):
  CruceProyectoRow { parlamentario_id, nombre_normalizado, sector_id, sector_etiqueta, tipo_senal, conteo, evidencia: CruceEvidencia, fecha_captura }
  CruceEvidenciaItem { tipo, fecha: string|null, contraparte_nombre_crudo, audiencia_id, enlace_fuente: string|null }

Degrade honesto — patrón EXACTO a copiar (app/components/lobby-en-tramitacion.tsx:255-275):
  const { data, error } = await sb.rpc("cruces_de_proyecto", { p_boletin: boletin });
  if (error?.code === "PGRST202") return null;   // función ausente → nodo AUSENTE (deploy antes de apply)
  if (error) throw new Error(...);                // error real de schema/red → #34 (NUNCA empty)
  const rows = (data as CruceProyectoRow[] | null) ?? [];  // 0 filas → empty honesto en la vista

Idiom evidencia + provenance (app/components/cruces-de-parlamentario.tsx:112-176):
  s.evidencia.items.map(item → li con contraparte cruda + ProvenanceBadge)
  <ProvenanceBadge capturedAt={new Date(s.fecha_captura)} sourceName={sourceLabel("lobby")} sourceUrl={item.enlace_fuente} />
  key = `${s.sector_id}-${item.audiencia_id}-${idx}` (índice load-bearing: N contrapartes/audiencia)

Componentes REUSE (imports):
  import { createServerSupabase } from "@/lib/supabase";
  import { ProvenanceBadge } from "@/components/provenance-badge";
  import { formatNombre, fechaCorta } from "@/lib/format";
  import { sourceLabel } from "@/lib/types";
  import { DetalleColapsable } from "@/components/detalle-colapsable";  // triggerVariant="primary" triggerLabel
  import { CrucesCapa1 } … (referencia visual; adaptar marco petróleo inline si el shape difiere)

Wiring page.tsx:
  Placement DOM: DESPUÉS de <section id="lobby-tramitacion"> (page.tsx:138), ANTES de <section id="idea-matriz"> (page.tsx:144).
  Rail (page.tsx:199 navEntries[]): insertar { id:"cruces", label:"Cruces", marker:"diamante" } tras la entrada "lobby-tramitacion".
  FichaRail YA soporta marker:"diamante" (ficha-rail.tsx:33,70) — NO extender el primitivo.
  crucesPublicEnabled() YA está ON en PROD desde 2026-07-02 — NO se toca ningún flag; la sección se envuelve en él igual que /parlamentario (page.tsx:362).

Banned-vocab negative-match (espejo cruces-de-parlamentario.test.tsx:13) — regex a ejercer sobre el render:
  /se reunió para|a cambio de|antes de votar|que resultó en|cercano a|vinculad[oa] a|aliad[oa] de|afinidad|conflicto de inter|influencia|influyente|score|ranking|índice de|sospechos|polémic|porque|presion|gestion/i
</interfaces>

@app/components/lobby-en-tramitacion.tsx
@app/components/cruces-de-parlamentario.tsx
@app/components/capa1/cruces-capa1.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrucesView (pura) + CrucesSection (server, degrade PGRST202)</name>
  <read_first>
    - app/components/lobby-en-tramitacion.tsx (patrón degrade 3-caminos, h2+caveat DENTRO del componente, vista pura + Section)
    - app/components/cruces-de-parlamentario.tsx (render evidencia.items[] + ProvenanceBadge + encabezado por señal degradable)
    - 38-UI-SPEC.md §Copywriting Contract (strings LOCKED) + §Departure (nombre = LINK)
  </read_first>
  <behavior>
    - CrucesView con rows>0: h2 "Cruces con el sector del proyecto" (petróleo) + caveat 1× + una fila por parlamentario: nombre via formatNombre como LINK a /parlamentario/[parlamentario_id], línea SEPARADA "Votó a favor de este proyecto", línea "{n} reuniones con gestores del sector {etiqueta}" (singular "1 reunión"; tipo_senal desconocido → "{n} registros en el sector {etiqueta}"), evidencia.items[] con ProvenanceBadge (capturedAt = fecha_captura, NO item.fecha).
    - CrucesView con rows=0: h2 + caveat + empty honesto "Aún no se registran parlamentarios con cruces en el sector de este proyecto en las fuentes consultadas." (NUNCA "limpio").
    - CrucesSection: PGRST202 → null; otro error → throw; data → CrucesView.
    - Caveat aparece EXACTAMENTE 1× por render; cero léxico causal (negative-match).
  </behavior>
  <action>
    Crear `app/components/cruces-de-proyecto.tsx` (sin `"use client"`). Exportar `CrucesView({ rows }: { rows: CruceProyectoRow[] })` PURA y `CrucesSection({ boletin }: { boletin: string })` async. El `<h2>` "Cruces con el sector del proyecto" (petróleo `text-accent-product`) y el caveat LOCKED "La coincidencia temporal no implica relación entre la reunión y el voto." viven DENTRO del componente (degrade path-1 no deja heading huérfano). Copy exacto de 38-UI-SPEC §Copywriting: capa-1 frame/intro factual, "{N} parlamentarios", "Votó a favor de este proyecto" (línea separada), "{n} reuniones con gestores del sector {sector}" (plural helper), meta "Reunión registrada el {fecha}" (Mono, omitida si item.fecha null), empty honesto. Nombre del parlamentario: `formatNombre(row.nombre_normalizado)` renderizado como `<a href={/parlamentario/${row.parlamentario_id}}>` con `underline underline-offset-4 text-accent-product min-h-11` (DEPARTURE LOCKED — el sujeto es parlamentario público, NO contraparte). Si la evidencia trae un `contraparte_nombre_crudo`, ese sigue TEXTO PLANO no-enlazado + IdentityMarker (regla 52-03 intacta para contrapartes). ProvenanceBadge por item: `capturedAt={new Date(row.fecha_captura)}` (nivel señal, WR-02), `sourceUrl={item.enlace_fuente}`, key con índice. CrucesSection = espejo del degrade de lobby-en-tramitacion.tsx:248-276 con `sb.rpc("cruces_de_proyecto", { p_boletin: boletin })`. NUNCA componer voto+reunión en un mismo `<li>/<article>` ni frase causal.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run cruces-de-proyecto && pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cruces-de-proyecto.tsx` exporta `CrucesView` y `CrucesSection`; sin `"use client"`; tsc limpio.
    - CrucesSection: `if (error?.code === "PGRST202") return null;` presente; error genérico → throw; 0 filas → empty honesto.
    - Nombre del parlamentario renderizado como `<a href="/parlamentario/...">` con formatNombre; contraparte (si existe) plana + IdentityMarker.
    - ProvenanceBadge usa `new Date(row.fecha_captura)`, NO item.fecha.
  </acceptance_criteria>
  <done>Componente construido: vista pura + Section con degrade honesto de 3 caminos, nombre como link, caveat 1× dentro del componente.</done>
</task>

<task type="auto">
  <name>Task 2: RTL de cruces-de-proyecto (capa-1/2, empty, degrade, throw, banned-vocab)</name>
  <read_first>
    - app/components/cruces-de-parlamentario.test.tsx (fixtures + negative-match banned-vocab líneas ~10-13)
    - app/components/lobby-en-tramitacion.tsx:248-276 (los 3 caminos a mockear con sb.rpc)
  </read_first>
  <action>
    Crear `app/components/cruces-de-proyecto.test.tsx`. Tests de `CrucesView` (pura, fixtures `CruceProyectoRow[]`): (1) rows>0 → renderiza h2, caveat 1× (assert que el texto del caveat aparece exactamente una vez), el nombre como `<a href="/parlamentario/{id}">`, la línea "Votó a favor de este proyecto" y "{n} reuniones con gestores del sector {etiqueta}"; (2) rows=0 → empty honesto, cero dígito fabricado, NO contiene "limpio"/"transparente"; (3) tipo_senal desconocido → "{n} registros en el sector …" (verbo de reunión NO fabricado); (4) NEGATIVE-MATCH: el `container.textContent` completo NO matchea el regex banned-vocab del `<interfaces>` (léxico causal/afinidad/ranking). Tests de `CrucesSection` mockeando `createServerSupabase().rpc`: (5) error `{code:"PGRST202"}` → el componente resuelve a `null` (nodo ausente); (6) error genérico `{code:"42P01",message:"…"}` → throw; (7) data con filas → contiene el nombre linkeado. Usar el patrón de mock de sb.rpc ya presente en el repo (espejo de las pruebas de contraparte/lobby).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run cruces-de-proyecto</automated>
  </verify>
  <acceptance_criteria>
    - Al menos 7 tests verdes cubriendo capa-1/2, empty honesto, tipo_senal degradado, negative-match, PGRST202→null, throw genérico.
    - El test de negative-match ejerce el regex banned-vocab sobre el render y pasa (cero léxico causal).
    - El test de caveat asserta exactamente 1 ocurrencia por render.
  </acceptance_criteria>
  <done>RTL cubre los 3 caminos del degrade + capa-1/2 + empty + anti-insinuación; verde.</done>
</task>

<task type="auto">
  <name>Task 3: Cablear <section id=cruces> + rail entry en page.tsx</name>
  <read_first>
    - app/app/proyecto/[boletin]/page.tsx:120-232 (secciones hermanas mt-12, navEntries, ProyectoRail)
    - app/app/parlamentario/[id]/page.tsx:362-382 (patrón CrucesCapa1 + DetalleColapsable primary "Explorar los N cruces")
    - app/components/ficha-rail.tsx:27-34,70-74 (RailEntry ya soporta marker:"diamante")
  </read_first>
  <action>
    En `app/app/proyecto/[boletin]/page.tsx`: importar `CrucesSection` de `@/components/cruces-de-proyecto` y `crucesPublicEnabled` de `@/lib/cruces-gate` (+ `DetalleColapsable`, `CrucesCapa1` si se usa el marco). Insertar la sección DESPUÉS de `<section id="lobby-tramitacion">` (línea 142) y ANTES de `<section id="idea-matriz">` (línea 144): `<section id="cruces" className="mt-12 scroll-mt-6">` envuelta en `crucesPublicEnabled(process.env) && (...)`, con `<Suspense fallback={<CrucesSkeleton/>}>` → `<CrucesSection boletin={boletin} />`. El h2/caveat viven DENTRO de CrucesSection (degrade honesto → null sin heading huérfano; el wrapper mt-12 persiste = frontier rule). Para el trigger primary "Explorar los N cruces": como el conteo N no es baratamente derivable en el server sin invocar la RPC (que puede degradar), montar CrucesSection directamente dentro del `<section>` (el detalle progresivo con DetalleColapsable primary vive DENTRO de CrucesSection sobre datos ya fetcheados, espejo del análogo — o, si se sigue el patrón /parlamentario con conteo server, documentar la elección en el SUMMARY). Añadir al array `navEntries` (page.tsx:199) la entrada `{ id: "cruces", label: "Cruces", marker: "diamante" }` inmediatamente tras `{ id: "lobby-tramitacion", ... }`. Reusar `CrucesSkeleton` existente (`@/components/ui/skeleton` o el skeleton de cruces ya en el repo) para el fallback. NO tocar ningún flag; NO filtrar petróleo a otras entradas de rail.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run page-cruces && pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `<section id="cruces" className="mt-12 scroll-mt-6">` presente entre `#lobby-tramitacion` y `#idea-matriz`, envuelta en `crucesPublicEnabled(process.env)`.
    - navEntries incluye `{ id:"cruces", label:"Cruces", marker:"diamante" }` tras la entrada de lobby.
    - `page-cruces.test.ts` (source-scan del page.tsx) verifica placement DOM (índice de `id="cruces"` entre lobby e idea-matriz) + rail entry + que CrucesSection se importa; verde.
    - tsc -b limpio.
  </acceptance_criteria>
  <done>Carril cruces cableado como hermano mt-12 gated, rail entry "Cruces ◆", skeleton de fallback; degrade honesto preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → navegador | El service_role (Camino A) NUNCA llega al cliente; la isla DetalleColapsable recibe el detalle como children, no importa Supabase |
| RPC ausente pre-apply → UI | El degrade honesto decide null/empty/throw en el server, no en el cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Information Disclosure | no-leak SSR (service_role al navegador) | mitigate | CrucesSection es Server Component; la isla cliente `DetalleColapsable` recibe el contenido como `children` y NUNCA importa `@/lib/supabase` (contrato F45 no-leak) |
| T-38-06 | Information Disclosure | degrade que enmascara error real de schema | mitigate | SOLO `error.code === "PGRST202"` → null; cualquier otro error → throw (#34). Un regex de mensaje tragaría "column … does not exist" (WR-01) — prohibido |
| T-38-07 | — (legal/ético) | insinuación causal / daño reputacional | mitigate | Caveat anti-causal exactamente 1×; banned-vocab negative-match en tests; conteo neutro sin ranking; líneas voto/reunión SEPARADAS, nunca en un mismo `<li>` ni frase causal |
| T-38-08 | Information Disclosure | render de PII (partido/rut) o contraparte enlazada | mitigate | La vista consume solo `CruceProyectoRow` (sin partido/rut); el parlamentario público se enlaza (DEPARTURE LOCKED), la contraparte de lobby (si aparece) queda plana + IdentityMarker (regla 52-03) |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas (UI-SPEC "New deps: NONE") — sin superficie supply-chain |
</threat_model>

<verification>
- `cd app && pnpm test && pnpm exec tsc -b` → suite baseline 670 (nunca menos) + tsc limpio + lockdown-guard + banned-vocab verdes.
- Negative-match anti-causal verde sobre el render de CrucesView.
- Degrade PGRST202→null verificado por RTL con mock de sb.rpc.
</verification>

<success_criteria>
- CrucesView + CrucesSection construidos; nombre del parlamentario como LINK a /parlamentario/[id]; degrade honesto de 3 caminos.
- Carril `#cruces` cableado tras `#lobby-tramitacion`, gated en crucesPublicEnabled (ON), rail entry "Cruces ◆".
- Copy factual sin verbo causal (negative-match verde), caveat 1×, conteo neutro; suite 670 + tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-02-SUMMARY.md` when done
</output>
