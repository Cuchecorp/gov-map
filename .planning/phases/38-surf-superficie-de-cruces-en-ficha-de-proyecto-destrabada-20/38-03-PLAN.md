---
phase: 38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - Temp/reshot2.mjs
autonomous: false
requirements: [SURF-02]

must_haves:
  truths:
    - "El gate completo (pnpm test 670 + typecheck + lockdown-guard + banned-vocab) pasa antes de desplegar"
    - "El código desplegado a PROD degrada honesto pre-apply: /proyecto/14309-04 responde 200 con la sección cruces ausente (RPC 0049 aún no aplicada)"
    - "El operador recibe el comando exacto de apply (psql --db-url + schema_migrations + pgTAP 0049) y el veredicto visual demo (14309-04 filas, 14782-13 empty honesto)"
  artifacts:
    - path: "Temp/reshot2.mjs"
      provides: "Helper de screenshot same-origin para evidencia demo"
      contains: "14309-04"
  key_links:
    - from: "docker-cf-build.sh + wrangler"
      to: "Cloudflare Pages (PROD)"
      via: "deploy patrón caliente"
      pattern: "wrangler"
    - from: "checkpoint operador"
      to: "supabase/migrations/0049_cruces_de_proyecto.sql"
      via: "psql --db-url --single-transaction + insert schema_migrations"
      pattern: "0049"
---

<objective>
Cerrar la fase: correr el gate completo, redesplegar el frontend a PROD (patrón caliente docker-cf-build.sh + wrangler), hacer smoke, capturar evidencia demo, y presentar el checkpoint de operador que entrega (a) el comando EXACTO de apply de la DDL 0049 + su pgTAP, y (b) el veredicto visual (demo 14309-04 con filas post-apply; 14782-13 como empty honesto documentado). El agente JAMÁS aplica la migración.

Purpose: separar limpiamente lo que el agente puede hacer (código en PROD, degradando honesto) de lo que es acción exclusiva de operador (aplicar el DDL a la DB), siguiendo el patrón 52-06.
Output: PROD redeployado con el carril degradando honesto; `Temp/reshot2.mjs`; checkpoint con comando de apply + veredicto visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Datos load-bearing para el smoke, la evidencia y el checkpoint. -->

Demo boletines [VERIFIED: psql PROD, 38-RESEARCH §3]:
  14309-04 (educación) → 47 parlamentarios / 144 reuniones POST-APPLY (emilia schneider 13, gonzalo winter 12, diego schalper 12). ← DEMO con filas.
  14782-13 (sala cuna) → 0 filas (SIN proyecto_ficha → sin sector) = empty honesto esperado, NO bug.
  Pre-apply (RPC 0049 ausente): AMBOS boletines → sección cruces AUSENTE del HTML (PGRST202 → null), la página responde 200.

Deploy (patrón caliente, 3 deploys hoy — STATE.md):
  docker-cf-build.sh (repo root) → build OpenNext en Linux/Docker (Windows worker roto) → docker cp → wrangler deploy desde host (OAuth local; CI sin creds CF).

Comando de apply para el checkpoint (patrón 52-06, NUNCA lo ejecuta el agente):
  psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0049_cruces_de_proyecto.sql
  psql "$SUPABASE_DB_URL" -c "insert into supabase_migrations.schema_migrations (version, name) values ('0049','cruces_de_proyecto') on conflict do nothing;"
  psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0049_cruces_de_proyecto.test.sql   # pgTAP: espera 10/10 ok
  # Windows: PGCLIENTENCODING=UTF8. NUNCA `supabase db push` (drift schema_migrations).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate completo + redeploy + smoke</name>
  <read_first>
    - docker-cf-build.sh (repo root, patrón de build/deploy)
    - STATE.md (deploy 2026-07-02 pattern + GOTCHA force-dynamic en rutas gated)
  </read_first>
  <action>
    Correr el gate completo desde `app/`: `pnpm test` (baseline 670, nunca menos) + `pnpm exec tsc -b` + confirmar lockdown-guard y banned-vocab verdes (parte de la suite). Si algo falla, DETENER y reportar (no desplegar sobre rojo). Con el gate verde, ejecutar `docker-cf-build.sh` (build OpenNext en Docker Linux) y desplegar con `wrangler deploy` desde el host (patrón caliente de hoy; OAuth local). Tras el deploy, smoke con curl: `GET /proyecto/14309-04` y `GET /proyecto/14782-13` deben responder 200, y (pre-apply) la sección cruces debe estar AUSENTE del HTML (grep de la respuesta: NO contiene "Cruces con el sector del proyecto" porque la RPC 0049 no existe aún → PGRST202 → null). Verificar también que /proyecto/[otra ficha] y las secciones vecinas (#lobby-tramitacion, #idea-matriz) siguen 200 sin regresión. Documentar la versión de deploy en el SUMMARY.
  </action>
  <verify>
    <automated>cd app && pnpm test && pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Gate verde ANTES del deploy: suite ≥670, tsc limpio, lockdown-guard + banned-vocab verdes.
    - Deploy ejecutado (docker-cf-build.sh + wrangler); versión anotada en el SUMMARY.
    - Smoke: /proyecto/14309-04 y /proyecto/14782-13 → 200; sección cruces ausente del HTML pre-apply (degrade honesto verificado en PROD); carriles vecinos sin regresión.
  </acceptance_criteria>
  <done>Gate verde, PROD redeployado, smoke confirma el degrade honesto pre-apply sin 500 ni regresión.</done>
</task>

<task type="auto">
  <name>Task 2: Evidencia de screenshot (helper same-origin)</name>
  <read_first>
    - 38-RESEARCH §3 (demo 14309-04 / 14782-13) + §Environment Availability
  </read_first>
  <action>
    Crear `Temp/reshot2.mjs` — helper de screenshot con técnica same-origin (evita el WAF/redirects), apuntando a las URLs de PROD `/proyecto/14309-04` y `/proyecto/14782-13`. Capturar el estado ACTUAL de PROD (pre-apply): documenta el degrade honesto (la sección cruces ausente / la ficha íntegra sin banda huérfana). Guardar las capturas en `Temp/` con nombres claros (p.ej. `cruces-14309-preapply.png`). Estas capturas son la evidencia técnica que acompaña el checkpoint: muestran que el deploy de código NO rompió la ficha pre-apply. Nota en el SUMMARY: el veredicto visual POST-APPLY (filas en 14309-04) lo verifica el operador tras aplicar la DDL.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); process.exit(fs.existsSync('Temp/reshot2.mjs') && /14309-04/.test(fs.readFileSync('Temp/reshot2.mjs','utf8')) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `Temp/reshot2.mjs` existe y referencia 14309-04 (+ opcionalmente 14782-13).
    - Al menos una captura pre-apply guardada en Temp/ documentando el degrade honesto.
  </acceptance_criteria>
  <done>Helper y evidencia pre-apply capturados; listos para el checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    El código de la superficie de cruces está EN VIVO en PROD, degradando honesto (la RPC `cruces_de_proyecto` aún no existe en la DB → la sección está ausente sin romper la ficha). La migración `0049_cruces_de_proyecto.sql` y su pgTAP están escritos y committeados, pero NO aplicados: aplicar el DDL a PROD es acción EXCLUSIVA del operador (patrón 52-06).
  </what-built>
  <how-to-verify>
    1. APLICAR LA DDL (comando exacto — el agente NUNCA lo ejecutó):
         PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -v ON_ERROR_STOP=1 -f supabase/migrations/0049_cruces_de_proyecto.sql
         psql "$SUPABASE_DB_URL" -c "insert into supabase_migrations.schema_migrations (version, name) values ('0049','cruces_de_proyecto') on conflict do nothing;"
       (NUNCA `supabase db push` — drift de schema_migrations.)
    2. CORRER EL pgTAP contra el schema aplicado (espera 10/10 ok):
         PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0049_cruces_de_proyecto.test.sql
    3. VEREDICTO VISUAL en PROD tras el apply:
         - https://<prod>/proyecto/14309-04 → la sección "Cruces con el sector del proyecto" muestra ~47 parlamentarios (emilia schneider, gonzalo winter, diego schalper …), cada nombre linkeado a /parlamentario/[id], líneas voto/reunión SEPARADAS, caveat 1×, ProvenanceBadge por evidencia.
         - https://<prod>/proyecto/14782-13 → empty honesto ("Aún no se registran parlamentarios con cruces…") — esperado (sin sector), NO es un bug.
    4. CONFIRMAR anti-insinuación: cero léxico causal, conteo neutro, cero ranking.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" tras aplicar la DDL + pgTAP 10/10 + veredicto visual OK, o describir los problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → PROD DB | El agente despliega código pero NUNCA aplica DDL; el apply cruza el boundary solo por acción de operador |
| deploy → Cloudflare Pages | Build reproducible en Docker Linux; creds CF locales (OAuth), nunca en CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | Tampering / Elevation of Privilege | apply de DDL a PROD | mitigate | El agente JAMÁS ejecuta `psql -f` de escritura ni `supabase db push`; el apply es checkpoint:human-verify blocking (patrón 52-06). La única prueba válida del DDL es el pgTAP post-apply contra el schema aplicado |
| T-38-10 | Denial of Service | deploy sobre suite roja | mitigate | Task 1 corre el gate completo ANTES del deploy y DETIENE si algo falla (no se despliega sobre rojo) |
| T-38-11 | Information Disclosure | falso positivo de CI (build ≠ Postgres ejecutó el DDL) | accept | tsc/vitest verdes NO prueban que la RPC existe en PROD (falso positivo conocido); el código degrada honesto (PGRST202) para pasar pre-apply; el pgTAP del operador es la prueba real |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas esta fase — sin superficie supply-chain |
</threat_model>

<verification>
- Gate completo verde antes del deploy (suite ≥670, tsc, lockdown-guard, banned-vocab).
- Smoke PROD: /proyecto/14309-04 y /proyecto/14782-13 → 200, sección cruces ausente pre-apply (degrade honesto), sin regresión de carriles vecinos.
- Checkpoint entrega el comando de apply exacto + pgTAP + veredicto visual; el agente no aplica nada.
</verification>

<success_criteria>
- PROD redeployado con el carril degradando honesto pre-apply (código en vivo, sin 500).
- Evidencia demo capturada (Temp/reshot2.mjs + capturas pre-apply).
- Checkpoint de operador presentado: comando de apply de 0049 + pgTAP 10/10 + veredicto visual (14309-04 filas, 14782-13 empty honesto).
</success_criteria>

<output>
Create `.planning/phases/38-surf-superficie-de-cruces-en-ficha-de-proyecto-destrabada-20/38-03-SUMMARY.md` when done
</output>
