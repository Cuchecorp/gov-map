---
phase: 115-link-ext-patrones-de-link-a-fuente-oficial
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md
autonomous: true
requirements: [LINK-03]

must_haves:
  truths:
    - "Existe al menos una respuesta live registrada por cada patrón y por cada host, con su cuerpo acotado"
    - "Los timestamps de la corrida prueban un delay ≥2s entre requests al mismo host; cero ráfagas"
    - "Cada patrón tiene UN veredicto de tres: OK / patrón-malo / fuente-caída-WAF"
    - "Los candidatos #1 (/buscar wspublico) y #2 (timeline 982 filas) tienen veredicto explícito y evidencia"
  artifacts:
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json"
      provides: "Registros crudos de la muestra con timestamps, http_code, url_effective y snippet"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt"
      provides: "Tabla legible de la corrida + cabecera con UA, delay y fecha"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md"
      provides: "Veredicto trinario cerrado por patrón + lista de acciones para el Plan 03"
      contains: "## 2. Veredicto por patrón"
  key_links:
    - from: "115-VEREDICTO.md"
      to: "115-MUESTRA.json"
      via: "cada veredicto cita el id de caso y su http_code"
      pattern: "115-MUESTRA"
    - from: "115-VEREDICTO.md"
      to: "115-PATRONES.md"
      via: "un veredicto por cada fila P-NN de §1"
      pattern: "P-[0-9]"
---

<objective>
Correr la muestra live estratificada con el runner del Plan 01 —respetando robots, UA y rate-limit—
y convertir sus respuestas en un veredicto trinario cerrado por patrón: OK, patrón-malo (defecto
nuestro, se arregla en el Plan 03) o fuente-caída/WAF (se declara con evidencia, jamás se evade).

Purpose: la distinción "defecto nuestro" vs "la fuente está caída" es el SC#4 del ROADMAP y es lo
único que impide que un 403 se use como excusa para no arreglar un patrón malo.
Output: `115-MUESTRA.txt`, `115-MUESTRA.json`, `115-VEREDICTO.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrida de la muestra live con evidencia de rate-limit</name>
  <files>
    .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt,
    .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json
  </files>
  <read_first>
    - `scripts/probar-links-externos.mjs` (cabecera + `CASOS` + flags)
    - `115-PATRONES.md` §4 (manifiesto con columna `robots`)
    - `115-ROBOTS.txt` (casos retirados por `Disallow`)
  </read_first>
  <action>
Correr UNA vez la muestra completa:
`node scripts/probar-links-externos.mjs --out .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA`

Antes de correr, verificar que el manifiesto del runner ya excluye los casos retirados por robots
en el Plan 01. Si alguno sobrevive, retirarlo del array `CASOS` citando la directiva.

Reglas de la corrida (no negociables, CLAUDE.md ingesta respetuosa):
- Una sola pasada. Si la corrida completa falla a mitad, se REANUDA con `--host` sobre los hosts
  pendientes; no se re-corre entero. Nunca dos pasadas sobre el mismo host el mismo día.
- Nada de aumentar la concurrencia ni de bajar `DELAY_MS` para "que termine antes".
- Un 403/429/5xx NO se reintenta ni se rodea (nada de cambiar UA, nada de proxies, nada de headers
  de navegador para esquivar el WAF). Se registra y punto: el SC#4 exige que la fuente caída se
  DECLARE, no que se venza.

Tras la corrida, añadir al final de `115-MUESTRA.txt` un bloque `=== EVIDENCIA DE RATE-LIMIT ===`
con, por host: número de requests, delta mínimo observado entre requests consecutivos al mismo host
(en ms) y el veredicto `>=2000ms OK` / `VIOLACIÓN`. El delta mínimo se calcula desde los timestamps
del `.json`, no se afirma.

Añadir también un bloque `=== COBERTURA ===`: `casos ejecutados / casos del manifiesto`,
`patrones cubiertos / patrones de §1`, `hosts cubiertos / hosts de §4`, y la lista nominal de
cualquier caso NO ejecutado con su causa (retirado por robots / host inalcanzable). Cero casos
"asumidos".

Redacción de la salida: el `snippet` se guarda con saltos colapsados y truncado; si algún snippet
de `datos.cplt.cl` contuviera datos personales de una declaración, se sustituye por
`[snippet redactado — dominio de declaraciones]` conservando `http_code` y `content_type`, y se
declara la redacción en el `.txt`. La prueba del patrón es el código y la URL efectiva, no el cuerpo.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-MUESTRA.json && test -f 115-MUESTRA.txt && grep -q '=== EVIDENCIA DE RATE-LIMIT ===' 115-MUESTRA.txt && grep -q '=== COBERTURA ===' 115-MUESTRA.txt && ! grep -q 'VIOLACIÓN' 115-MUESTRA.txt && ! grep -qE 'postgres(ql)?://' 115-MUESTRA.txt 115-MUESTRA.json && node -e "const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;if(!rows||rows.length<20)throw new Error('registros='+(rows&&rows.length));const hosts=new Set(rows.map(x=>x.host));if(hosts.size<7)throw new Error('hosts='+hosts.size);const byHost={};for(const x of rows){(byHost[x.host]=byHost[x.host]||[]).push(new Date(x.ts_inicio).getTime())}for(const[h,ts]of Object.entries(byHost)){ts.sort((a,b)=>a-b);for(let i=1;i<ts.length;i++){if(ts[i]-ts[i-1]<2000)throw new Error('rate-limit violado en '+h+': '+(ts[i]-ts[i-1])+'ms')}}console.log('MUESTRA_OK',rows.length,hosts.size)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;console.log(new Set(rows.map(x=>x.patron)).size)"` ≥ el número de filas `P-` de §1 menos las declaradas sin-probe (`partidoLegible` y los casos retirados por robots)
    - Cada registro tiene `http_code` no vacío: `node -e "const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;if(rows.some(x=>x.http_code===undefined||x.http_code===null))throw new Error('http_code faltante');console.log('OK')"`
    - `grep -c 'casos ejecutados' 115-MUESTRA.txt` = 1 y la fracción no tiene casos "asumidos"
    - Cero reintentos agresivos: `! grep -qi 'reintento 2\|retry 2' 115-MUESTRA.txt`
  </acceptance_criteria>
  <done>La muestra cubre ≥1 caso por patrón×host emitido, con http_code y snippet acotado; el bloque de rate-limit demuestra delta mínimo ≥2000ms por host; la cobertura es explícita y sin huecos silenciosos.</done>
</task>

<task type="auto">
  <name>Task 2: Veredicto trinario por patrón + resolución de los dos candidatos</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md</files>
  <read_first>
    - `115-MUESTRA.json` / `115-MUESTRA.txt` (respuestas y clasificación propuesta por el runner)
    - `115-PATRONES.md` §1 (universo) y §2 (exclusiones)
    - `113-INVENTARIO.md` §3.3.6 puntos 2, 3 y 4 (las 5 consecuencias para 115)
    - `app/components/buscar-filtros.tsx` alrededor de la línea 493 (`sourceUrl: row.enlace ?? null`)
    - `app/components/timeline-event.tsx` (el `<a href={evento.enlace}>` sin rewrite)
  </read_first>
  <action>
Crear `115-VEREDICTO.md` con:

**§1 Método del veredicto.** Las tres etiquetas y su criterio DURO:
- `OK` — el recurso responde 2xx Y la `url_effective` conserva el parámetro que instancia el patrón
  (boletín / prmID / id de audiencia), es decir se llega al recurso ESPECÍFICO. Un 2xx que aterriza
  en la portada del host NO es OK.
- `PATRON-MALO` — defecto NUESTRO: el patrón apunta a un recurso no-humano (XML crudo), a una página
  genérica, o construye la URL sin el parámetro. Se arregla en código (Plan 03).
- `FUENTE-CAIDA-WAF` — el servidor responde 403/429/5xx o no resuelve, con el patrón bien construido.
  Se DECLARA con comando + respuesta; jamás se evade ni se reintenta agresivamente.
Regla LOCKED: un `FUENTE-CAIDA-WAF` NO absuelve a un patrón que además está mal construido. Si el
patrón apunta a `/wspublico/` y encima da 403, el veredicto es `PATRON-MALO` con nota de WAF.

**§2 Veredicto por patrón.** Header exacto `## 2. Veredicto por patrón`. Tabla:
`id | patrón | host | caso | http_code | url_effective (acotada) | veredicto | evidencia (id de registro en 115-MUESTRA.json) | acción`.
UNA fila por cada `P-NN` de §1 de `115-PATRONES.md` — cero patrones sin veredicto. Los patrones que
no se prueban live (`partidoLegible`, casos retirados por robots) llevan veredicto
`OK-POR-CONSTRUCCION` con la razón y el test/gate que lo respalda; esa cuarta etiqueta se declara
explícitamente en §1 como "no-probado-por-diseño", nunca disfrazada de OK live.

**§3 Candidatos del inventario — resolución explícita.** Obligatorio, uno por sub-sección:
- **Candidato #1 — `/buscar`**: `buscar-filtros.tsx:493` pasa `row.enlace` crudo al badge sin
  `enlaceHumanoProyecto`, mientras `ficha-header.tsx`, `autor-row.tsx`, `votacion-card.tsx` y
  `proyectos-similares.tsx` sí lo aplican. Verificar contra PROD el denominador real
  (`proyecto.enlace` = 3.658 filas `tramitacion.senado.cl` + 1 `opendata.camara.cl`) y contra la
  muestra si ese host+path devuelve XML. Veredicto y, si es `PATRON-MALO`, la acción exacta para el
  Plan 03 (reusar `enlaceHumanoProyecto`, sin builder nuevo).
- **Candidato #2 — timeline B5**: `tramitacion_evento.enlace` con 982 filas en
  `tramitacion.senado.cl` renderizadas por `timeline-event.tsx:42` sin rewrite. Determinar por SQL
  cuántas de esas 982 tienen path `/wspublico/` (query verbatim, agrupando por
  `split_part(enlace,'/',4)` o `like '%/wspublico/%'` — conteo, no valores) y cruzar con la
  respuesta live del caso correspondiente. Veredicto + acción. Si el rewrite exige el boletín y el
  componente no lo recibe hoy, declararlo aquí como el trabajo que el Plan 03 debe hacer
  (threading del boletín), no como un impedimento.

**§4 Acciones para el Plan 03.** Lista numerada CERRADA: por cada `PATRON-MALO`, el archivo:línea,
el fix propuesto y el criterio de aceptación. Por cada `FUENTE-CAIDA-WAF`, si el sitio debe declarar
la limitación en la UI, decirlo aquí con el texto propuesto (que el Plan 03 pasará por el linter
anti-insinuación ANTES de escribirlo). Si la lista de fixes queda vacía, decirlo con esas palabras
y con la evidencia que lo sostiene — un "todo OK" sin evidencia no se acepta.

**§5 Deploy diferido.** Declarar que ningún fix de esta fase se observa contra el deploy real hasta
la Phase 125 (decisión LOCKED de v12.0), siguiendo el precedente de 114-03: PASS con limitación
declarada, jamás PASS pelado.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-VEREDICTO.md && grep -q '^## 2. Veredicto por patrón' 115-VEREDICTO.md && grep -q 'PATRON-MALO' 115-VEREDICTO.md && grep -q 'FUENTE-CAIDA-WAF' 115-VEREDICTO.md && grep -q 'OK-POR-CONSTRUCCION' 115-VEREDICTO.md && grep -q 'buscar-filtros.tsx' 115-VEREDICTO.md && grep -q 'timeline-event.tsx' 115-VEREDICTO.md && grep -q '982' 115-VEREDICTO.md && grep -q '115-MUESTRA' 115-VEREDICTO.md && grep -qi 'Phase 125' 115-VEREDICTO.md && ! grep -qE 'postgres(ql)?://' 115-VEREDICTO.md && echo VEREDICTO_OK</automated>
  </verify>
  <acceptance_criteria>
    - Cobertura 1:1 patrón→veredicto: `comm -23 <(grep -oE '^\| (P-[0-9]+[a-z]?)' 115-PATRONES.md | tr -d '| ' | sort -u) <(grep -oE 'P-[0-9]+[a-z]?' 115-VEREDICTO.md | sort -u)` → salida VACÍA
    - Cada veredicto usa exactamente una de las 4 etiquetas: `! grep -nE '\| *(OK\?|probable|parcial|más o menos)' 115-VEREDICTO.md`
    - §3 tiene las dos sub-secciones de candidatos: `grep -c -i 'Candidato #' 115-VEREDICTO.md` ≥ 2
    - §4 existe y es una lista numerada o la frase explícita de lista vacía con evidencia: `grep -qE '^(1\.|La lista de fixes queda vacía)' 115-VEREDICTO.md`
  </acceptance_criteria>
  <done>Todo patrón de §1 tiene exactamente un veredicto con su evidencia citada; los dos candidatos del inventario quedan resueltos con acción o con absolución fundada; §4 entrega la lista cerrada de trabajo del Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → servidores gubernamentales | única fase de v12.0 que hace requests salientes a terceros |
| respuestas de terceros → artefactos commiteados | el cuerpo remoto es entrada no confiable que se persiste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Denial of Service | hosts gubernamentales | mitigate | una sola pasada; reanudación por `--host`, nunca re-corrida completa; delta mínimo ≥2000ms verificado desde los timestamps del `.json` en el gate automatizado |
| T-115-07 | Information Disclosure | snippets de `datos.cplt.cl` (declaraciones de patrimonio) | mitigate | snippet redactable con marcador declarado; el veredicto se sostiene en `http_code` + `url_effective`, no en el cuerpo |
| T-115-08 | Repudiation | veredicto sin respaldo | mitigate | cada fila de §2 cita el id de registro de `115-MUESTRA.json`; sin cita no hay veredicto |
| T-115-09 | Spoofing | evadir el WAF cambiando identidad | accept→prohibido | prohibido explícitamente en el action: sin cambio de UA, sin proxies, sin headers de navegador; un WAF se declara |
| T-115-02 | Information Disclosure | `SUPABASE_DB_URL` en artefactos | mitigate | gate `! grep -qE 'postgres(ql)?://'` sobre los tres artefactos |
</threat_model>

<verification>
- Muestra: ≥1 registro por patrón×host emitido; delta mínimo por host ≥2000ms probado desde los timestamps.
- Cobertura explícita, con causa nominal para cada caso no ejecutado.
- Veredicto: cobertura 1:1 con §1 de `115-PATRONES.md`; 4 etiquetas cerradas; candidatos #1 y #2 resueltos.
- Cero credenciales en los artefactos.
</verification>

<success_criteria>
- `115-MUESTRA.json` con ≥20 registros sobre ≥7 hosts y cero violaciones de rate-limit.
- `115-VEREDICTO.md` con un veredicto por cada `P-NN` y §4 con lista cerrada de acciones.
- Los candidatos #1 y #2 nombrados con archivo:línea y con veredicto.
</success_criteria>

<output>
Crear `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-02-SUMMARY.md` al terminar.
</output>
