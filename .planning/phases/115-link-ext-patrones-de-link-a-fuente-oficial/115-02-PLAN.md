---
phase: 115-link-ext-patrones-de-link-a-fuente-oficial
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md
autonomous: true
requirements: [LINK-03]

must_haves:
  truths:
    - "Existe al menos una respuesta live registrada por cada patrón y por cada host del manifiesto vigente"
    - "Los timestamps de la corrida prueban un delay ≥2s entre requests al mismo host; cero ráfagas"
    - "Cada patrón tiene UN veredicto de la taxonomía cerrada, y la etiqueta sin-probe está acotada por gate"
    - "Los candidatos #1 (/buscar wspublico) y #2 (timeline 982 filas) tienen veredicto explícito y evidencia"
  artifacts:
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json"
      provides: "Registros crudos de la muestra con timestamps, http_code, url_effective y snippet"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt"
      provides: "Tabla legible + bloques de evidencia de rate-limit y de cobertura"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md"
      provides: "Veredicto cerrado por patrón + lista de acciones para el Plan 03"
      contains: "## 2. Veredicto por patrón"
  key_links:
    - from: "115-VEREDICTO.md"
      to: "115-MUESTRA.json"
      via: "cada veredicto cita el id P-NN-cNN y su http_code"
      pattern: "115-MUESTRA"
    - from: "115-VEREDICTO.md"
      to: "115-PATRONES.md"
      via: "un veredicto por cada fila P-NN de §1"
      pattern: "P-[0-9]"
---

<objective>
Correr la muestra live estratificada con el runner del Plan 01 —respetando robots, UA y rate-limit—
y convertir sus respuestas en un veredicto cerrado por patrón: OK, patrón-malo (defecto nuestro, se
arregla en el Plan 03) o fuente-caída/WAF (se declara con evidencia, jamás se evade).

Purpose: la distinción "defecto nuestro" vs "la fuente está caída" es el SC#4 del ROADMAP y es lo
único que impide que un 403 se use como excusa para no arreglar un patrón malo.
Output: `115-MUESTRA.txt`, `115-MUESTRA.json`, `115-VEREDICTO.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrida de la muestra live con evidencia de rate-limit</name>
  <files>
    .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.txt,
    .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA.json
  </files>
  <read_first>
    - `scripts/probar-links-externos.mjs` (cabecera, `CASOS`, flags, gate de orden robots-primero)
    - `115-PATRONES.md` §4 (manifiesto vigente, columna `robots`, líneas `CASOS_MANIFIESTO:` y
      `HOSTS_MANIFIESTO:` — son la fuente única de verdad de los conteos esperados)
    - `115-ROBOTS.txt` (bloque `RETIRADOS:` con los ids retirados por `Disallow`)
  </read_first>
  <action>
Correr UNA vez la muestra completa:
`node scripts/probar-links-externos.mjs --out .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-MUESTRA`

El gate de orden del runner ya garantiza que `115-ROBOTS.txt` existe. Antes de correr, verificar que
el array `CASOS` ya no contiene ningún id listado en el bloque `RETIRADOS:` del Plan 01; si alguno
sobrevive, retirarlo citando la directiva.

Reglas de la corrida (no negociables, CLAUDE.md ingesta respetuosa):
- Una sola pasada. Si falla a mitad, se REANUDA con `--host` sobre los hosts pendientes; no se
  re-corre entero. Nunca dos pasadas sobre el mismo host el mismo día.
- Nada de aumentar la concurrencia ni de bajar `DELAY_MS` para "que termine antes".
- Un 403/429/5xx NO se reintenta ni se rodea (nada de cambiar UA, proxies ni headers de navegador
  para esquivar el WAF). Se registra: el SC#4 exige que la fuente caída se DECLARE, no que se venza.

Tras la corrida, añadir al final de `115-MUESTRA.txt`:

**Bloque `=== EVIDENCIA DE RATE-LIMIT ===`** — por host: número de requests, delta mínimo observado
entre requests consecutivos al mismo host (ms) y el veredicto `>=2000ms OK` / `VIOLACIÓN`. El delta
mínimo se CALCULA desde los timestamps del `.json`, no se afirma.

**Bloque `=== COBERTURA ===`** — los conteos se comparan contra el MANIFIESTO VIGENTE, nunca contra
constantes (revisión checker W3): leer `CASOS_MANIFIESTO:` y `HOSTS_MANIFIESTO:` de §4 de
`115-PATRONES.md` y emitir, en este formato exacto y grepeable:
```
CASOS_EJECUTADOS: <n> / CASOS_MANIFIESTO: <n>
HOSTS_CUBIERTOS: <n> / HOSTS_MANIFIESTO: <n>
PATRONES_CUBIERTOS: <n> / PATRONES_PROBABLES: <n>
```
donde `PATRONES_PROBABLES` = patrones de §1 menos los declarados sin-probe (`partidoLegible` y los
retirados por robots). A continuación, la lista NOMINAL de cualquier caso del manifiesto no
ejecutado con su causa. Cero casos "asumidos".

Redacción de la salida: el `snippet` se guarda con saltos colapsados y truncado; si algún snippet de
`datos.cplt.cl` contuviera datos personales de una declaración, se sustituye por
`[snippet redactado — dominio de declaraciones]` conservando `http_code` y `content_type`, y se
declara la redacción en el `.txt`. La prueba del patrón es el código y la URL efectiva, no el cuerpo.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-MUESTRA.json && test -f 115-MUESTRA.txt && grep -q '=== EVIDENCIA DE RATE-LIMIT ===' 115-MUESTRA.txt && grep -q '=== COBERTURA ===' 115-MUESTRA.txt && ! grep -q 'VIOLACIÓN' 115-MUESTRA.txt && ! grep -qE 'postgres(ql)?://' 115-MUESTRA.txt 115-MUESTRA.json && node -e "const fs=require('fs');const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;const pat=fs.readFileSync('115-PATRONES.md','utf8');const nCasos=+/CASOS_MANIFIESTO: (\d+)/.exec(pat)[1];const nHosts=+/HOSTS_MANIFIESTO: (\d+)/.exec(pat)[1];if(rows.length!==nCasos)throw new Error('registros='+rows.length+' vs manifiesto='+nCasos);const hosts=new Set(rows.map(x=>x.host));if(hosts.size!==nHosts)throw new Error('hosts='+hosts.size+' vs manifiesto='+nHosts);const byHost={};for(const x of rows){(byHost[x.host]=byHost[x.host]||[]).push(new Date(x.ts_inicio).getTime())}for(const[h,ts]of Object.entries(byHost)){ts.sort((a,b)=>a-b);for(let i=1;i<ts.length;i++){if(ts[i]-ts[i-1]<2000)throw new Error('rate-limit violado en '+h+': '+(ts[i]-ts[i-1])+'ms')}}console.log('MUESTRA_OK',rows.length,hosts.size)"</automated>
  </verify>
  <acceptance_criteria>
    - Los conteos del bloque COBERTURA cuadran con el manifiesto vigente (el gate automatizado lo comprueba leyendo `CASOS_MANIFIESTO`/`HOSTS_MANIFIESTO`, no constantes)
    - Cada registro tiene `http_code` no vacío: `node -e "const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;if(rows.some(x=>x.http_code===undefined||x.http_code===null))throw new Error('http_code faltante');console.log('OK')"`
    - Todo id de registro está en el manifiesto: `node -e "const fs=require('fs');const r=require('./115-MUESTRA.json');const rows=Array.isArray(r)?r:r.registros;const pat=fs.readFileSync('115-PATRONES.md','utf8');const malos=rows.filter(x=>!pat.includes(x.id));if(malos.length)throw new Error('ids fuera del manifiesto: '+malos.map(x=>x.id).join(','));console.log('IDS_OK')"`
    - Ningún id retirado por robots fue ejecutado: cruzar el bloque `RETIRADOS:` de `115-ROBOTS.txt` contra los ids del `.json` → intersección vacía
    - Cero reintentos agresivos: `! grep -qi 'reintento 2\|retry 2' 115-MUESTRA.txt`
  </acceptance_criteria>
  <done>La muestra cubre exactamente el manifiesto vigente (≥1 caso por patrón×host emitido y no retirado), con http_code y snippet acotado; el bloque de rate-limit demuestra delta mínimo ≥2000ms por host; la cobertura es explícita y sin huecos silenciosos.</done>
</task>

<task type="auto">
  <name>Task 2: Veredicto por patrón + resolución de los dos candidatos</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VEREDICTO.md</files>
  <read_first>
    - `115-MUESTRA.json` / `115-MUESTRA.txt` (respuestas y clasificación propuesta por el runner)
    - `115-PATRONES.md` §1 (universo) y §2 (exclusiones)
    - `115-ROBOTS.txt` (bloque `RETIRADOS:` — insumo del gate de `OK-POR-CONSTRUCCION`)
    - `113-INVENTARIO.md` §3.3.6 puntos 2, 3 y 4 (las consecuencias para 115)
    - `app/components/buscar-filtros.tsx:478-497` — el `sourceUrl: row.enlace ?? null` de `:493` y el
      `row.boletin` que ya existe en `:482-483` (el threading del candidato #1 NO requiere trabajo extra)
    - `app/components/timeline-event.tsx` (el `<a href={evento.enlace}>` sin rewrite ni `safeExternalHref`)
      y su llamante real `app/components/timeline-view.tsx:243,252`
  </read_first>
  <action>
Crear `115-VEREDICTO.md` con:

**§1 Método del veredicto.** Taxonomía CERRADA de 4 etiquetas, con criterio duro:
- `OK` — el recurso responde 2xx **Y** la `url_effective` conserva el parámetro que instancia el
  patrón (boletín / prmID / id de audiencia): se llega al recurso ESPECÍFICO. Un 2xx que aterriza en
  la portada del host NO es OK.
- `PATRON-MALO` — defecto NUESTRO: el patrón apunta a un recurso no-humano (XML crudo), a una página
  genérica, o construye la URL sin el parámetro. Se arregla en código (Plan 03).
- `FUENTE-CAIDA-WAF` — 403/429/5xx o no resuelve, con el patrón bien construido. Se DECLARA con
  comando + respuesta; jamás se evade ni se reintenta agresivamente.
- `OK-POR-CONSTRUCCION` — **no-probado-por-diseño**, jamás disfrazado de OK live. Su uso está ACOTADO
  por gate (revisión checker W4): sólo se admite para (a) `partidoLegible`, que no emite href y por
  tanto no tiene recurso que pedir, y (b) los casos listados en el bloque `RETIRADOS:` de
  `115-ROBOTS.txt`. Cada línea con esta etiqueta DEBE citar, en la misma fila, o bien `partidoLegible`
  o bien la directiva `Disallow: …` verbatim que lo retiró. Sin esa cita, la etiqueta es inválida.

Regla LOCKED: un `FUENTE-CAIDA-WAF` NO absuelve a un patrón que además está mal construido. Si el
patrón apunta a `/wspublico/` y encima da 403, el veredicto es `PATRON-MALO` con nota de WAF.

**§2 Veredicto por patrón.** Header exacto `## 2. Veredicto por patrón`. Tabla:
`id | patrón | host | caso (P-NN-cNN) | http_code | url_effective (acotada) | veredicto | evidencia (id de registro en 115-MUESTRA.json) | acción`.
UNA fila por cada `P-NN` de §1 de `115-PATRONES.md` — cero patrones sin veredicto.

**§3 Candidatos del inventario — resolución explícita.** Una sub-sección por candidato:
- **Candidato #1 — `/buscar`**: `buscar-filtros.tsx:493` pasa `row.enlace` crudo al badge sin
  `enlaceHumanoProyecto`, mientras `ficha-header.tsx`, `autor-row.tsx`, `votacion-card.tsx` y
  `proyectos-similares.tsx` sí lo aplican. Registrar que `row.boletin` YA está disponible en el mismo
  `.map` (`:482-483`), luego el fix no requiere threading nuevo. Verificar contra PROD el denominador
  (`proyecto.enlace` = 3.658 `tramitacion.senado.cl` + 1 `opendata.camara.cl`) y contra la muestra si
  ese host+path devuelve XML. Veredicto + acción exacta para el Plan 03 (reusar
  `enlaceHumanoProyecto`, sin builder nuevo).
- **Candidato #2 — timeline B5**: `tramitacion_evento.enlace`, 982 filas en `tramitacion.senado.cl`
  renderizadas por `timeline-event.tsx:42` sin rewrite. Determinar por SQL cuántas de esas 982 tienen
  path `/wspublico/` (query verbatim, `like '%/wspublico/%'` — CONTEO, jamás valores) y cruzar con la
  respuesta live del caso correspondiente. Veredicto + acción. **Registrar la cadena de llamada real
  que el fix deberá tocar** (revisión checker BLOCKER 2): `app/app/proyecto/[boletin]/page.tsx` →
  `TimelineView` (`app/components/timeline-view.tsx`) → `TimelineEvent` en `:243` y `:252`.
  `TimelineView` es el intermediario obligado: sin él el boletín no llega al evento. Declararlo aquí
  como el trabajo que el Plan 03 debe hacer, no como un impedimento.

**§4 Acciones para el Plan 03.** Lista numerada CERRADA. Por cada `PATRON-MALO`: archivo:línea, fix
propuesto y criterio de aceptación — **incluyendo todos los archivos intermediarios** que el fix
obliga a tocar (p.ej. `timeline-view.tsx` para threadear el boletín). Por cada `FUENTE-CAIDA-WAF`
que exija declarar la limitación en la UI: el archivo de UI donde iría, y el TEXTO propuesto de la
leyenda (que el Plan 03 pasará por el linter anti-insinuación ANTES de escribirlo). Si la lista de
fixes queda vacía, decirlo con esas palabras y con la evidencia que lo sostiene — un "todo OK" sin
evidencia no se acepta.

**§5 Deploy diferido.** Ningún fix de esta fase se observa contra el deploy real hasta la Phase 125
(decisión LOCKED de v12.0), siguiendo el precedente de 114-03: PASS con limitación declarada, jamás
PASS pelado.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-VEREDICTO.md && grep -q '^## 2. Veredicto por patrón' 115-VEREDICTO.md && grep -q 'PATRON-MALO' 115-VEREDICTO.md && grep -q 'FUENTE-CAIDA-WAF' 115-VEREDICTO.md && grep -q 'OK-POR-CONSTRUCCION' 115-VEREDICTO.md && grep -q 'buscar-filtros.tsx' 115-VEREDICTO.md && grep -q 'timeline-event.tsx' 115-VEREDICTO.md && grep -q 'timeline-view.tsx' 115-VEREDICTO.md && grep -q '982' 115-VEREDICTO.md && grep -q '115-MUESTRA' 115-VEREDICTO.md && grep -qi 'Phase 125' 115-VEREDICTO.md && ! grep -qE 'postgres(ql)?://' 115-VEREDICTO.md && echo VEREDICTO_OK</automated>
  </verify>
  <acceptance_criteria>
    - Cobertura 1:1 patrón→veredicto: `comm -23 <(grep -oE '^\| P-[0-9]+' 115-PATRONES.md | tr -d '| ' | sort -u) <(grep -oE 'P-[0-9]+' 115-VEREDICTO.md | sort -u)` → salida VACÍA
    - **Gate anti-abuso de la etiqueta sin-probe (W4):** `grep -c 'OK-POR-CONSTRUCCION' 115-VEREDICTO.md` en §2 ≤ `1 + <número de ids del bloque RETIRADOS: de 115-ROBOTS.txt>`; y cada línea con esa etiqueta cita `partidoLegible` o una directiva `Disallow:` → `grep 'OK-POR-CONSTRUCCION' 115-VEREDICTO.md | grep -vcE 'partidoLegible|Disallow:'` → `0`
    - Cada veredicto usa exactamente una de las 4 etiquetas: `! grep -nE '\| *(OK\?|probable|parcial|más o menos)' 115-VEREDICTO.md`
    - §3 tiene las dos sub-secciones de candidatos: `grep -c -i 'Candidato #' 115-VEREDICTO.md` ≥ 2
    - §4 existe como lista numerada o con la frase explícita de lista vacía: `grep -qE '^(1\.|La lista de fixes queda vacía)' 115-VEREDICTO.md`
    - Si §4 pide fix del candidato #2, nombra el intermediario: `! grep -q 'Candidato #2' 115-VEREDICTO.md || grep -q 'timeline-view.tsx' 115-VEREDICTO.md`
  </acceptance_criteria>
  <done>Todo patrón de §1 tiene exactamente un veredicto con evidencia citada; `OK-POR-CONSTRUCCION` está acotada por gate y justificada línea a línea; los dos candidatos quedan resueltos con acción (incluyendo archivos intermediarios) o con absolución fundada; §4 entrega la lista cerrada de trabajo del Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → servidores gubernamentales | única fase de v12.0 que hace requests salientes a terceros |
| respuestas de terceros → artefactos commiteados | el cuerpo remoto es entrada no confiable que se persiste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Denial of Service | hosts gubernamentales | mitigate | una sola pasada; reanudación por `--host`, nunca re-corrida completa; delta mínimo ≥2000ms verificado desde los timestamps del `.json` en el gate automatizado |
| T-115-13 | Repudiation | tocar recursos retirados por robots | mitigate | intersección vacía entre los ids ejecutados y el bloque `RETIRADOS:` de `115-ROBOTS.txt`, verificada en acceptance |
| T-115-07 | Information Disclosure | snippets de `datos.cplt.cl` (declaraciones de patrimonio) | mitigate | snippet redactable con marcador declarado; el veredicto se sostiene en `http_code` + `url_effective`, no en el cuerpo |
| T-115-08 | Repudiation | veredicto sin respaldo | mitigate | cada fila de §2 cita el id `P-NN-cNN` de `115-MUESTRA.json`; la etiqueta sin-probe tiene gate de conteo y de cita |
| T-115-09 | Spoofing | evadir el WAF cambiando identidad | mitigate | prohibido explícitamente: sin cambio de UA, sin proxies, sin headers de navegador; un WAF se declara |
| T-115-02 | Information Disclosure | `SUPABASE_DB_URL` en artefactos | mitigate | gate `! grep -qE 'postgres(ql)?://'` sobre los tres artefactos |
</threat_model>

<verification>
- Muestra: registros == `CASOS_MANIFIESTO` y hosts == `HOSTS_MANIFIESTO` del manifiesto vigente; delta mínimo por host ≥2000ms probado desde los timestamps.
- Cobertura explícita, con causa nominal para cada caso no ejecutado; cero ids retirados ejecutados.
- Veredicto: cobertura 1:1 con §1; 4 etiquetas cerradas; `OK-POR-CONSTRUCCION` acotada por gate; candidatos #1 y #2 resueltos con su cadena de llamada real.
- Cero credenciales en los artefactos.
</verification>

<success_criteria>
- `115-MUESTRA.json` cuadra con `CASOS_MANIFIESTO`/`HOSTS_MANIFIESTO` y sin violaciones de rate-limit.
- `115-VEREDICTO.md` con un veredicto por cada `P-NN` y §4 con lista cerrada de acciones.
- `OK-POR-CONSTRUCCION` ≤ 1 + retirados, cada uno con su cita.
- Los candidatos #1 y #2 nombrados con archivo:línea, incluido `timeline-view.tsx`.
</success_criteria>

<output>
Crear `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-02-SUMMARY.md` al terminar.
</output>
