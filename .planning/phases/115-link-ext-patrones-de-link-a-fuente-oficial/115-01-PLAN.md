---
phase: 115-link-ext-patrones-de-link-a-fuente-oficial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md
  - scripts/probar-links-externos.mjs
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt
autonomous: true
requirements: [LINK-03]

must_haves:
  truths:
    - "Un lector ve, por cada patrón de URL externa que el sitio emite, su fuente, su plantilla verbatim, el dato que lo parametriza y un caso real de PROD"
    - "Las familias del inventario que NO se emiten al DOM quedan excluidas con razón declarada, no omitidas en silencio"
    - "El robots.txt de cada host queda registrado ANTES de que este plan pida ningún otro recurso de ese host"
    - "Existe un runner reproducible que respeta rate-limit ≥2s por host y deja timestamps que lo prueban"
  artifacts:
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md"
      provides: "Tabla cerrada patrón×host×plantilla×parámetro×caso real + tabla de exclusiones + manifiesto con ids P-NN-cNN"
      contains: "## 1. Universo de patrones"
    - path: "scripts/probar-links-externos.mjs"
      provides: "Runner curl-first, agrupado por host, delay ≥2s/host, UA identificatorio, salida .txt/.json con timestamps"
      contains: "MSYS_NO_PATHCONV"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt"
      provides: "robots.txt de cada host emitido, con comando, respuesta y veredicto"
  key_links:
    - from: "115-PATRONES.md"
      to: "113-INVENTARIO.md §3.2 / §3.3.3"
      via: "cada fila cita builder o tabla.columna del inventario"
      pattern: "3\\.3\\.3|3\\.2"
    - from: "scripts/probar-links-externos.mjs"
      to: "115-PATRONES.md §4"
      via: "el array CASOS transcribe el manifiesto con los mismos ids P-NN-cNN"
      pattern: "CASOS"
---

<objective>
Enumerar el universo COMPLETO de patrones de URL externa que el sitio emite al DOM —los 4 builders
de §3.2 más las familias URL-desde-columna de §3.3 que sí llegan al DOM— con plantilla verbatim,
parámetro y caso real elegido por SQL de PROD; y dejar montada la infraestructura de la muestra
live (runner rate-limited + robots.txt por host) SIN todavía correr la muestra.

Purpose: sin el universo cerrado y sin robots registrado, cualquier muestra live es a la vez
incompleta y una falta de respeto al servidor. Este plan cierra el denominador antes de tocar
un solo recurso gubernamental.
Output: `115-PATRONES.md`, `scripts/probar-links-externos.mjs`, `115-ROBOTS.txt`.

**Orden LOCKED de este plan (revisión checker, BLOCKER 1):** robots.txt ANTES que cualquier otro
recurso. El Task 2 valida el runner ejecutando ÚNICAMENTE su modo `--robots` (pedir `/robots.txt`
es exactamente el acto que el protocolo de exclusión sanciona como primero); el Task 3 completa el
registro y consolida el artefacto. Ningún task de este plan pide una URL de caso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VALIDATION.md

<interfaces>
<!-- Contratos que el ejecutor NO debe ir a buscar: están aquí. -->

**CORRECCIÓN al 115-CONTEXT.md (dato del checker):** los builders NO viven en `app/lib/format.ts`.
Tres de los cuatro están en `app/components/validacion-fuente.tsx` (`:60`, `:67`, `:87`); sólo
`partidoLegible` vive en `app/lib/format.ts:153-174`. Registrar esta corrección en §0.1 de
`115-PATRONES.md`.

`app/components/validacion-fuente.tsx`:
- `buildSenadoUrl(boletin: string): string` (`:60-62`) → plantilla
  `https://tramitacion.senado.cl/appsenado/templates/tramitacion/index.php?boletin_ini=${encodeURIComponent(boletin)}`
  (precondición: boletín COMPLETO con sufijo; sin sufijo el Senado devuelve lista, no ficha)
- `buildCamaraUrl(boletin: string, prmId: string): string` (`:67-69`) → plantilla
  `https://www.camara.cl/legislacion/ProyectosDeLey/tramitacion.aspx?prmID=${encodeURIComponent(prmId)}&prmBOLETIN=${encodeURIComponent(boletin)}`
  (precondición: sólo si `prm_id_camara != null`)
- `enlaceHumanoProyecto(enlace: string, boletin: string): string` (`:87-100`) → rewrite por host+path
  (`hostname === "tramitacion.senado.cl"` && `pathname.includes("/wspublico/")` → `buildSenadoUrl(boletin)`;
  cualquier otro caso → `enlace` VERBATIM)

`app/lib/format.ts:153-174` — `partidoLegible(...)`: NO construye link; desactiva URIs
`datos.bcn.cl/.../partido-politico/{slug}`. Invariante "CERO URI en el DOM".

`app/lib/utils.ts:15-23` — `safeExternalHref(url): string | null`: allowlist de esquema
`http:`/`https:`, fail-closed, no reescribe. Chokepoint de todo href externo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universo cerrado de patrones + exclusiones + casos reales por SQL</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §3.1.3 (safeExternalHref),
      §3.1.4 (tabla de 17 call-sites de `sourceUrl=`), §3.2 (los 4 builders verbatim),
      §3.3.3 (tabla de familias con host+n+emisor y su encabezado "Las 10 columnas con 0 filas…"),
      §3.3.5 (la tabla real, que trae 11 filas), §3.3.6 (5 consecuencias)
    - `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md` §Universo de patrones
    - `app/components/validacion-fuente.tsx:40-100` (builders) y `app/lib/utils.ts:15-23`
  </read_first>
  <action>
Crear `115-PATRONES.md` con front-matter `phase: 115`, `plan: 01`, `estado: borrador`, y estas secciones:

**§0 Método y cobertura.** Universo = §3.2 (4 builders) + familias de §3.3.3 que se EMITEN al DOM.
Declarar la regla de la fase verbatim: patrón por construcción + muestra estratificada ≥1 caso por
patrón×host; **JAMÁS crawl exhaustivo** (decisión operador 2026-07-27). Ancla temporal con
`select now()::date` por psql.

**§0.1 Correcciones de partida (hallazgos de este plan).** Tres, todas con su evidencia:
1. *Ubicación de los builders.* El 115-CONTEXT.md atribuye los builders a `app/lib/format.ts`. Lo
   real: `buildSenadoUrl` / `buildCamaraUrl` / `enlaceHumanoProyecto` viven en
   `app/components/validacion-fuente.tsx:60/67/87`; sólo `partidoLegible` está en
   `app/lib/format.ts:153-174`. Registrar la corrección con las citas.
2. *Completitud del grep de §3.1.4.* El inventario enumeró call-sites con `grep -rl "sourceUrl="`,
   que sólo caza el prop JSX. Re-correr el universo ampliado y registrar la salida verbatim:
   `grep -rn "sourceUrl[[:space:]]*[:=]" app/components app/app --include=*.tsx | grep -v "\.test\."`.
   Call-sites que §3.1.4 NO tenía y que este plan añade al universo: `buscar-filtros.tsx:493`
   (`sourceUrl: row.enlace ?? null` — sin `enlaceHumanoProyecto`, **candidato #1**),
   `proyectos-similares.tsx:109` (SÍ pasa por `enlaceHumanoProyecto`), `agenda-filtros.tsx:373`,
   `app/app/agenda/page.tsx:463,504`. El universo de esta fase es el AMPLIADO; la diferencia es de
   denominador del grep, no una regresión del inventario.
3. *Discrepancia 10-vs-11 en §3.3.5.* El encabezado de §3.3.3 dice "Las **10** columnas con 0 filas
   se listan en §3.3.5" pero la tabla de §3.3.5 trae **11** filas. Contarlas y declarar el número
   correcto con la lista nominal; esta fase usa **11**.

**§1 Universo de patrones.** Header exacto `## 1. Universo de patrones`. Tabla con una fila por
patrón×host, columnas: `id` (`P-01`…), `origen` (builder `archivo:línea` o `tabla.columna`),
`host`, `plantilla verbatim`, `parámetro que la instancia`, `emisor (E-NNN archivo:línea)`,
`gate`, `caso real`, `n en PROD`. Filas mínimas obligatorias:
- P-01 `buildSenadoUrl` → `tramitacion.senado.cl`, parámetro `boletin` (con sufijo)
- P-02 `buildCamaraUrl` → `www.camara.cl`, parámetros `prmID` + `prmBOLETIN`
- P-03 `enlaceHumanoProyecto` → registrar sus DOS ramas (rama-rewrite → destino P-01; rama-verbatim
  → el host de la columna de origen)
- P-04 `partidoLegible` → **no emite href**; invariante "CERO URI en el DOM"; se valida SÓLO por
  construcción, sin probe (no hay recurso que pedir). Declararlo explícitamente para que su ausencia
  de la muestra live no se lea como omisión.
- Una fila por cada par tabla.columna×host de §3.3.3 con emisor real: `arista.enlace`,
  `citacion.enlace` (×2 hosts), `comision.enlace`, `comision_membresia.enlace`,
  `cruce_senal.evidencia->>'enlace_fuente'`, `declaracion*.enlace` (agrupables en una fila
  `datos.cplt.cl` declarando las 7 tablas), `lobby_audiencia.enlace` (×2 hosts),
  `lobby_audiencia.enlace_detalle`, `parlamentario.enlace` (×2 hosts), `proyecto.enlace` (×2 hosts),
  `proyecto_autor.enlace`, `sesion_sala.enlace` (×2 hosts), `tramitacion_evento.enlace` (×3 hosts),
  `votacion.enlace` (×2 hosts).

**§2 Exclusiones con razón.** Tabla `origen | n | razón`. Obligatorias:
- `lobby_contraparte.enlace` (17.681 — no se emite al DOM; la contraparte se muestra como texto crudo)
- `source_snapshot.source_url` (4.383 — `sourceUrl={null}` en su único call-site)
- `parlamentario_militancia.enlace` (`datos.bcn.cl` 48 + `opendata.camara.cl` 315 — desactivadas por
  `partidoLegible`; cualquier URI BCN visible en el DOM sería regresión del invariante)
- **Las 11 columnas de §3.3.5 con 0 ocurrencias en PROD, de las cuales 4 son además dominio MONEY
  con gate OFF** (`aporte.enlace`, `contrato.enlace`, `contratista.enlace`, `donante.enlace`).
  Redactarlo exactamente así: **una sola línea de conteo, sin doble-contar** — las MONEY son un
  subconjunto de las 11, no un grupo adicional. Añadir la nota de §3.3.6 punto 5: aunque el gate
  MONEY se encendiera, esas columnas están vacías, luego no habría link.

**§3 Casos reales.** Por cada fila de §1 con `n > 0`, el bloque SQL VERBATIM que eligió el caso, con
`ORDER BY` determinista y `LIMIT 1`. Restricción de seguridad HEREDADA de 113 (T-113-05/06): las
queries de conteo/host devuelven sólo `split_part(<col>,'/',3)` y `count(*)`. Para el caso concreto
SÍ se necesita el valor de la URL —es el objeto de la fase— pero **sólo de columnas NO-PII**:
prohibido consultar `pii_contraparte_declaracion.*` y cualquier columna de los dominios
`infoprobidad/`, `servel/`, `money/`, `rut/`. Jamás ecoar ni escribir `SUPABASE_DB_URL`
(usar `psql "$SUPABASE_DB_URL" -tA -c ...`, nunca imprimir la variable).

**§4 Manifiesto de la muestra.** Lista AGRUPADA POR HOST (todos los casos de un host contiguos) con
columnas `id | host | url del caso | patrón | qué se espera del recurso | robots`.
**Esquema de ids LOCKED (revisión checker):** `P-NN-cNN` — el id del patrón, guion, `c` y el ordinal
del caso dentro de ese patrón, ambos con dos dígitos. Ejemplos: `P-01-c01`, `P-12-c02`. Este id es la
clave que consumen el array `CASOS` del runner (Task 2), el filtro `--id`, los registros de
`115-MUESTRA.json` (Plan 02) y las citas de evidencia de `115-VEREDICTO.md`. Un id que no matchee
`^P-[0-9]{2}-c[0-9]{2}$` es un defecto.
La columna `robots` nace con el valor `pendiente-Task-3` y la cierra el Task 3.
Cerrar §4 con dos líneas de conteo canónicas, en este formato exacto para que sean grepeables:
`CASOS_MANIFIESTO: <n>` y `HOSTS_MANIFIESTO: <n>` (los conteos VIGENTES tras el Task 3). Son la
fuente única de verdad de los gates de cobertura del Plan 02.

Reglas de escritura: cero celdas vacías (usar `—`); toda afirmación con su comando o su cita
`archivo:línea`; ningún valor de URL de dominio PII.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-PATRONES.md && grep -q '^## 1. Universo de patrones' 115-PATRONES.md && grep -q 'validacion-fuente.tsx:60' 115-PATRONES.md && grep -q 'buildCamaraUrl' 115-PATRONES.md && grep -q 'enlaceHumanoProyecto' 115-PATRONES.md && grep -q 'partidoLegible' 115-PATRONES.md && grep -q 'lobby_contraparte' 115-PATRONES.md && grep -q 'source_snapshot' 115-PATRONES.md && grep -q 'leylobby' 115-PATRONES.md && grep -q 'datos.cplt.cl' 115-PATRONES.md && grep -q 'web-back.senado.cl' 115-PATRONES.md && grep -q 'opendata.camara.cl' 115-PATRONES.md && grep -qE 'CASOS_MANIFIESTO: [0-9]+' 115-PATRONES.md && grep -qE 'HOSTS_MANIFIESTO: [0-9]+' 115-PATRONES.md && grep -qE 'P-[0-9]{2}-c[0-9]{2}' 115-PATRONES.md && ! grep -qE 'postgres(ql)?://' 115-PATRONES.md && echo PATRONES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| P-' 115-PATRONES.md` ≥ 20 (≥20 filas patrón×host en §1)
    - Todos los ids del manifiesto cumplen el esquema: `grep -oE 'P-[0-9]+-c[0-9]+' 115-PATRONES.md | grep -vcE '^P-[0-9]{2}-c[0-9]{2}$'` → `0`
    - Los 7 hosts emitidos aparecen: `for h in www.camara.cl opendata.camara.cl tramitacion.senado.cl www.senado.cl web-back.senado.cl www.leylobby.gob.cl datos.cplt.cl; do grep -q "$h" 115-PATRONES.md || echo "FALTA $h"; done` → sin salida
    - `! grep -nE '\|[[:space:]]*\|' 115-PATRONES.md` → cero celdas vacías (un match sólo se acepta dentro de bloque de código y declarado como tal, precedente 113-06)
    - `grep -q 'JAMÁS crawl exhaustivo' 115-PATRONES.md`
    - Sin doble-conteo MONEY: `grep -c 'de las cuales 4' 115-PATRONES.md` = 1 y `! grep -qE '11 columnas.*\+.*4 columnas MONEY' 115-PATRONES.md`
    - §0.1 nombra los tres hallazgos: `grep -q 'buscar-filtros.tsx:493' 115-PATRONES.md && grep -q 'validacion-fuente.tsx' 115-PATRONES.md && grep -qE '10.*11|11.*10' 115-PATRONES.md`
  </acceptance_criteria>
  <done>115-PATRONES.md tiene §0/§0.1/§1/§2/§3/§4; cada patrón emitido tiene fila con plantilla, parámetro y caso real por SQL; cada exclusión tiene razón sin doble-conteo; los ids siguen `P-NN-cNN`; cero credenciales y cero valores PII.</done>
</task>

<task type="auto">
  <name>Task 2: Runner curl-first rate-limited por host (validado SÓLO con --robots)</name>
  <files>scripts/probar-links-externos.mjs</files>
  <read_first>
    - `scripts/verificar-links-internos.mjs` — cabecera completa (secciones "Mesura", "Portabilidad
      (LOCKED)", "Endurecimiento post code-review"): molde de UA, delay, salida `.txt`/`.json`,
      `os.tmpdir()`, `MSYS_NO_PATHCONV=1` y tabla por stdout
    - `115-PATRONES.md` §4 (manifiesto, ids `P-NN-cNN`, líneas `CASOS_MANIFIESTO:` / `HOSTS_MANIFIESTO:`)
  </read_first>
  <action>
Crear `scripts/probar-links-externos.mjs`. DIFERENCIA CRÍTICA con el runner de 114: aquí NO se usa
`fetch` de Node. Gotcha pagado en v3.0: el WAF de `camara.cl` bloquea `fetch` de Node y deja pasar
`curl`. El runner invoca `curl` por `child_process.execFile` (nunca `exec` con string, nunca shell).

Contrato del runner:
- Manifiesto EN EL SCRIPT como array exportado `CASOS`, transcrito de §4 de `115-PATRONES.md`, con
  campos `{ id, patron, host, url, espera }`. Los `id` son los `P-NN-cNN` de §4 — la correspondencia
  documento↔array es el contrato: un caso en el .md sin fila en `CASOS` (o viceversa) es un defecto.
- Modo `--robots`: pide `https://<host>/robots.txt` para cada host DISTINTO del manifiesto, una vez,
  con el mismo delay entre hosts. Acepta `--host <h>` para pedir el robots de UN solo host.
- Modo por defecto: recorre `CASOS` **agrupados por host** (orden estable), SECUENCIAL. Concurrencia
  PROHIBIDA. `DELAY_MS = 2500` (banda 2-3s de CLAUDE.md) entre requests al mismo host; el delay se
  aplica también entre hosts para no acumular ráfagas.
- **Gate de orden (BLOCKER 1):** el modo por defecto se NIEGA a correr si no existe todavía el
  artefacto `115-ROBOTS.txt` — fail-loud con mensaje explícito ("robots.txt no consultado: correr
  `--robots` primero") y exit distinto de 0, sin emitir ningún request. El respeto al protocolo de
  exclusión no depende de la disciplina del operador: lo impone el runner.
- Flags de curl obligatorias: `--silent --show-error --location --max-time 20 --max-filesize 200000`
  y `-w` para el metadato (`%{http_code}`, `%{url_effective}`, `%{num_redirects}`, `%{content_type}`,
  `%{time_total}`). Método **GET** con `--range 0-8191`; **jamás `--head`** (servidores
  gubernamentales lo rechazan). `--user-agent` identificatorio con nombre del proyecto y URL de
  contacto, leído de la constante nombrada `USER_AGENT` del propio script para que sea auditable.
- Reintentos: máximo UNO por caso, sólo ante fallo de RED (código de salida de curl, no HTTP), tras
  5s. Un 403/429/5xx NO se reintenta: se registra y se clasifica.
- Por request: timestamps ISO-8601 con ms `ts_inicio` / `ts_fin`, `delta_ms_mismo_host`, host, id,
  url, `http_code`, `url_effective`, `num_redirects`, `content_type`, y `snippet` = primeros 300
  caracteres del cuerpo con saltos colapsados (para distinguir XML crudo de página humana y página
  de WAF de recurso real).
- Clasificación PROPUESTA (el veredicto de fase lo confirma el Plan 02): `OK`, `REDIR-GENERICA`,
  `XML-CRUDO`, `WAF-403`, `NO-DISPONIBLE`, `RED`.
- Salida: `--out <prefijo>` escribe `<prefijo>.txt` (tabla legible + cabecera con UA, `DELAY_MS` y
  fecha) y `<prefijo>.json` (registros crudos). Filtros componibles `--host <h>` y `--id <id>`.
- Portabilidad LOCKED: rutas temporales con `os.tmpdir()` de Node, JAMÁS una variable de entorno de
  temp (en Git Bash/Windows es `undefined`). Documentar en la cabecera que las invocaciones con
  `--id`/`--host` cuyos valores contengan `/` requieren el prefijo `MSYS_NO_PATHCONV=1`.
- Manifiesto vacío tras filtros → exit 2 (fail-loud, precedente W-01 de 114).
- Cabecera: documentar por qué curl y no fetch (WAF camara.cl), la banda de rate-limit y su origen
  (CLAUDE.md, ingesta respetuosa), el gate de orden robots-primero, y que este script NO hace crawl.

**Validación de este task (BLOCKER 1 — orden LOCKED):** el único acto de red permitido aquí es un
smoke del modo `--robots` sobre UN host. Pedir `/robots.txt` es precisamente el primer acto que el
protocolo de exclusión sanciona, luego no viola el orden. **Prohibido** en este task pedir la URL de
un caso; el manifiesto no se cierra hasta que el Task 3 llene la columna `robots`.
  </action>
  <verify>
    <automated>node --check scripts/probar-links-externos.mjs && grep -q "curl" scripts/probar-links-externos.mjs && grep -q "USER_AGENT" scripts/probar-links-externos.mjs && grep -q "os.tmpdir()" scripts/probar-links-externos.mjs && grep -q "MSYS_NO_PATHCONV" scripts/probar-links-externos.mjs && grep -q "115-ROBOTS.txt" scripts/probar-links-externos.mjs && ! grep -qE '\bfetch\(' scripts/probar-links-externos.mjs && ! grep -q '\-\-head' scripts/probar-links-externos.mjs && node -e "import('./scripts/probar-links-externos.mjs').then(m=>{const c=m.CASOS;if(!Array.isArray(c)||c.length<20)throw new Error('CASOS='+(c&&c.length));const mal=c.filter(x=>!/^P-[0-9]{2}-c[0-9]{2}$/.test(x.id));if(mal.length)throw new Error('ids fuera de esquema: '+mal.map(x=>x.id).join(','));const hosts=new Set(c.map(x=>x.host));if(hosts.size<7)throw new Error('hosts='+hosts.size);console.log('RUNNER_OK',c.length,hosts.size)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE 'DELAY_MS *= *[0-9]+' scripts/probar-links-externos.mjs` → valor ≥ 2000
    - `! grep -nE 'Promise\.all|Promise\.allSettled' scripts/probar-links-externos.mjs` → cero concurrencia
    - Gate de orden: con `115-ROBOTS.txt` ausente, `node scripts/probar-links-externos.mjs --out "$(node -p 'require("os").tmpdir()')/115-gate"; echo $?` → distinto de `0`, y no se escribió ningún `.json` con registros de caso
    - Smoke permitido (sólo robots, un host): `node scripts/probar-links-externos.mjs --robots --host www.senado.cl --out "$(node -p 'require("os").tmpdir()')/115-smoke"` → exit 0 y `.json` con exactamente 1 registro cuya `url` termina en `/robots.txt`
    - Filtro sin match: `node scripts/probar-links-externos.mjs --robots --host no.existe.invalid --out "$(node -p 'require("os").tmpdir()')/115-vacio"; echo $?` → `2`
    - Cero requests de caso en este task: ninguna url del `.json` del smoke deja de terminar en `/robots.txt`
  </acceptance_criteria>
  <done>El runner existe, pasa `node --check`, exporta `CASOS` con ≥20 ids `P-NN-cNN` sobre ≥7 hosts, usa curl (no fetch), delay ≥2000ms, recorrido secuencial, se niega a correr casos sin `115-ROBOTS.txt`, y fue validado sin pedir una sola URL de caso.</done>
</task>

<task type="auto">
  <name>Task 3: robots.txt de cada host, registrado ANTES de la muestra</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt</files>
  <read_first>
    - `scripts/probar-links-externos.mjs` (modo `--robots` y el gate de orden, escritos en el Task 2)
    - `115-PATRONES.md` §4 (hosts distintos, ids `P-NN-cNN`, líneas `CASOS_MANIFIESTO:` / `HOSTS_MANIFIESTO:`)
    - `CLAUDE.md` §Conventions → "Respeto al servidor: rate-limit 2-3s/host, User-Agent
      identificatorio, robots.txt, caché diaria. Nunca ráfagas."
  </read_first>
  <action>
Correr `node scripts/probar-links-externos.mjs --robots --out <ruta>` y consolidar en
`115-ROBOTS.txt`. Por cada host distinto del manifiesto (7 emitidos; `datos.bcn.cl` se incluye SÓLO
si alguna fila de §1 lo lista como emitido — hoy no lo es, y esa ausencia se declara en una nota):
- el comando verbatim ejecutado,
- el `http_code` recibido,
- el cuerpo del robots.txt tal cual, o la declaración explícita "404 — sin robots.txt publicado"
  (respuesta válida, no una evasión),
- una línea `VEREDICTO <host>: …`. Si alguna directiva `Disallow` cubre el path de alguno de nuestros
  casos, ese caso se RETIRA del manifiesto y se declara aquí **con el id `P-NN-cNN` y la directiva
  citada verbatim**. Un caso retirado por robots NO es "fuente caída": es respeto, y su patrón se
  valida sólo por construcción.
- cabecera del artefacto con el `USER_AGENT` usado y la fecha/hora de la corrida.

Si un host devuelve 403 al pedir robots.txt: registrarlo con evidencia (comando + código) y marcar
`WAF-en-robots`. La muestra de ese host se intenta igual, con el mismo UA y el mismo delay; su
resultado lo clasifica el Plan 02. Prohibido cambiar UA, usar proxies o headers de navegador para
esquivar el WAF.

Cerrar el bucle con `115-PATRONES.md` y el runner:
- llenar la columna `robots` de §4 (`permitido` / `retirado por Disallow: <directiva>`),
- retirar del array `CASOS` del runner los casos con `Disallow`, citando el id,
- actualizar `CASOS_MANIFIESTO:` y `HOSTS_MANIFIESTO:` a los conteos VIGENTES post-robots,
- añadir en `115-ROBOTS.txt` un bloque final `RETIRADOS:` con la lista de ids retirados (o la línea
  `RETIRADOS: ninguno`).
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-ROBOTS.txt && for h in www.camara.cl opendata.camara.cl tramitacion.senado.cl www.senado.cl web-back.senado.cl www.leylobby.gob.cl datos.cplt.cl; do grep -q "$h" 115-ROBOTS.txt || { echo "FALTA robots de $h"; exit 1; }; done && grep -qi 'VEREDICTO' 115-ROBOTS.txt && grep -q '^RETIRADOS:' 115-ROBOTS.txt && grep -q '| robots' 115-PATRONES.md && ! grep -q 'pendiente-Task-3' 115-PATRONES.md && echo ROBOTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i '^VEREDICTO' 115-ROBOTS.txt` ≥ 7 (un veredicto por host)
    - UA consistente: el literal de `grep -oE 'USER_AGENT *= *"[^"]+"' ../../../scripts/probar-links-externos.mjs` aparece en `115-ROBOTS.txt`
    - Ningún host sin respuesta: `! grep -qi 'pendiente\|TBD' 115-ROBOTS.txt`
    - Cada línea de retiro cita la directiva: `grep -i 'retirado' 115-ROBOTS.txt | grep -vc 'Disallow:'` → `0`
    - Manifiesto y runner cuadran post-retiro: `node -e "const m=require('fs').readFileSync('115-PATRONES.md','utf8');const n=+/CASOS_MANIFIESTO: (\d+)/.exec(m)[1];import('../../../scripts/probar-links-externos.mjs').then(x=>{if(x.CASOS.length!==n)throw new Error('CASOS='+x.CASOS.length+' vs manifiesto='+n);console.log('SYNC_OK',n)})"`
  </acceptance_criteria>
  <done>115-ROBOTS.txt registra los 7 hosts con comando, código, cuerpo y veredicto; los casos cubiertos por un Disallow quedan retirados del manifiesto y del runner con su id y directiva citados; `CASOS_MANIFIESTO`/`HOSTS_MANIFIESTO` reflejan los conteos vigentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → servidores gubernamentales | salida hacia terceros; el abuso es el riesgo, no la entrada |
| PROD Postgres → artefactos de fase | valores de columnas pueden contener PII o credenciales |
| artefactos de fase → repositorio público | todo lo que se escriba queda commiteado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Denial of Service | hosts gubernamentales | mitigate | recorrido secuencial + `DELAY_MS>=2500` por host + manifiesto estratificado; `Promise.all` prohibido y verificado por grep |
| T-115-13 | Repudiation | tocar recursos antes de consultar robots.txt | mitigate | gate de orden EN EL RUNNER: el modo por defecto falla si `115-ROBOTS.txt` no existe; el Task 2 sólo ejerce `--robots` |
| T-115-02 | Information Disclosure | `SUPABASE_DB_URL` | mitigate | `psql "$SUPABASE_DB_URL"` sin ecoar; gate `! grep -qE 'postgres(ql)?://'` sobre cada artefacto |
| T-115-03 | Information Disclosure | columnas PII (`pii_contraparte_declaracion`, dominios `infoprobidad/servel/money/rut`) | mitigate | prohibidas como origen de casos; §2 las excluye por escrito; gate de grep en el verify |
| T-115-04 | Repudiation | muestra live sin trazabilidad | mitigate | timestamps ISO con ms antes/después + delta por host en el `.json`; artefacto commiteado |
| T-115-05 | Tampering | ejecución de comandos | mitigate | `execFile` con array de argumentos (nunca `exec` con string interpolado ni shell); las URLs vienen del manifiesto, no de entrada libre |
| T-115-06 | Spoofing | identidad del cliente ante la fuente | mitigate | `USER_AGENT` identificatorio único, auditable como constante, registrado en robots y en la salida |
| T-115-SC | Tampering | instalación de paquetes | mitigate | CERO paquete nuevo (curl del sistema + stdlib de Node); si apareciera la necesidad, checkpoint humano bloqueante antes de instalar |
</threat_model>

<verification>
- `115-PATRONES.md` cubre §3.2 completo + toda familia de §3.3.3 con emisor real; exclusiones con razón y sin doble-conteo MONEY.
- Ids del manifiesto en esquema `P-NN-cNN`; `CASOS_MANIFIESTO`/`HOSTS_MANIFIESTO` vigentes.
- `115-ROBOTS.txt` con los 7 hosts, veredicto y bloque `RETIRADOS:`; ningún recurso de caso tocado antes.
- El runner no usa `fetch`, no concurre, delay ≥2s, y se niega a correr casos sin robots consultado.
- Cero credenciales y cero valores de columnas PII en los artefactos.
</verification>

<success_criteria>
- `grep -c '^| P-' 115-PATRONES.md` ≥ 20 y los 7 hosts emitidos presentes.
- `node --check scripts/probar-links-externos.mjs` exit 0; `CASOS.length >= 20` sobre ≥7 hosts, todos con id `^P-[0-9]{2}-c[0-9]{2}$`.
- `115-ROBOTS.txt` con ≥7 líneas `VEREDICTO` y un bloque `RETIRADOS:`.
- `CASOS.length` == `CASOS_MANIFIESTO` de §4.
- `! grep -rqE 'postgres(ql)?://' .planning/phases/115-*/115-PATRONES.md .planning/phases/115-*/115-ROBOTS.txt`
</success_criteria>

<output>
Crear `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-01-SUMMARY.md` al terminar.
</output>
