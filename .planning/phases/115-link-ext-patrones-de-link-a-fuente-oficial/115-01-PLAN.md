---
phase: 115-link-ext-patrones-de-link-a-fuente-oficial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md
  - scripts/probar-links-externos.mjs
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt
autonomous: true
requirements: [LINK-03]

must_haves:
  truths:
    - "Un lector ve, por cada patrón de URL externa que el sitio emite, su fuente, su plantilla verbatim, el dato que lo parametriza y un caso real de PROD"
    - "Las familias del inventario que NO se emiten al DOM quedan excluidas con razón declarada, no omitidas en silencio"
    - "El robots.txt de cada host de la muestra queda registrado ANTES de tocar ningún recurso del host"
    - "Existe un runner reproducible que respeta rate-limit ≥2s por host y deja timestamps que lo prueban"
  artifacts:
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md"
      provides: "Tabla cerrada patrón×host×plantilla×parámetro×caso real + tabla de exclusiones"
      contains: "## 1. Universo de patrones"
    - path: "scripts/probar-links-externos.mjs"
      provides: "Runner curl-first, agrupado por host, delay ≥2s/host, UA identificatorio, salida .txt/.json con timestamps"
      contains: "MSYS_NO_PATHCONV"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt"
      provides: "robots.txt de cada host emitido, con comando y respuesta"
  key_links:
    - from: "115-PATRONES.md"
      to: "113-INVENTARIO.md §3.2 / §3.3.3"
      via: "cada fila cita builder o tabla.columna del inventario"
      pattern: "3\\.3\\.3|3\\.2"
    - from: "scripts/probar-links-externos.mjs"
      to: "115-PATRONES.md"
      via: "el runner lee el catálogo de casos declarado en el manifiesto"
      pattern: "manifiesto|CASOS"
---

<objective>
Enumerar el universo COMPLETO de patrones de URL externa que el sitio emite al DOM —los 4 builders
de §3.2 más las familias URL-desde-columna de §3.3 que sí llegan al DOM— con plantilla verbatim,
parámetro y caso real elegido por SQL de PROD; y dejar montada la infraestructura de la muestra
live (robots.txt por host + runner rate-limited) SIN todavía correr la muestra.

Purpose: sin el universo cerrado y sin robots registrado, cualquier muestra live es a la vez
incompleta y una falta de respeto al servidor. Este plan cierra el denominador antes de tocar
un solo recurso gubernamental.
Output: `115-PATRONES.md`, `scripts/probar-links-externos.mjs`, `115-ROBOTS.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VALIDATION.md

<interfaces>
<!-- Contratos que el ejecutor NO debe ir a buscar: están aquí. -->

`app/components/validacion-fuente.tsx` — 3 de los 4 builders:
- `buildSenadoUrl(boletin: string): string` → plantilla
  `https://tramitacion.senado.cl/appsenado/templates/tramitacion/index.php?boletin_ini=${encodeURIComponent(boletin)}`
  (precondición: boletín COMPLETO con sufijo; sin sufijo el Senado devuelve lista, no ficha)
- `buildCamaraUrl(boletin: string, prmId: string): string` → plantilla
  `https://www.camara.cl/legislacion/ProyectosDeLey/tramitacion.aspx?prmID=${encodeURIComponent(prmId)}&prmBOLETIN=${encodeURIComponent(boletin)}`
  (precondición: sólo si `prm_id_camara != null`)
- `enlaceHumanoProyecto(enlace: string, boletin: string): string` → rewrite por host+path
  (`hostname === "tramitacion.senado.cl"` && `pathname.includes("/wspublico/")` → `buildSenadoUrl(boletin)`;
  cualquier otro caso → `enlace` VERBATIM)

`app/lib/format.ts` — `partidoLegible(...)`: NO construye link; desactiva URIs
`datos.bcn.cl/.../partido-politico/{slug}`. Invariante "CERO URI en el DOM".

`app/lib/utils.ts:15-23` — `safeExternalHref(url): string | null`: allowlist de esquema
`http:`/`https:`, fail-closed, no reescribe. Chokepoint de todo href externo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universo cerrado de patrones + exclusiones + casos reales por SQL</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §3.1.3 (safeExternalHref),
      §3.1.4 (tabla de 17 call-sites de `sourceUrl=`), §3.2 (los 4 builders verbatim),
      §3.3.3 (tabla de familias con host+n+emisor), §3.3.5 (columnas con 0 filas), §3.3.6 (5 consecuencias)
    - `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md` §Universo de patrones
    - `app/components/validacion-fuente.tsx` (builders) y `app/lib/utils.ts` (`safeExternalHref`)
  </read_first>
  <action>
Crear `115-PATRONES.md` con front-matter `phase: 115`, `plan: 01`, `estado: borrador`, y estas secciones:

**§0 Método y cobertura.** Declarar: universo = §3.2 (4 builders) + familias de §3.3.3 que se
EMITEN al DOM. Declarar la regla de la fase verbatim: patrón por construcción + muestra
estratificada ≥1 caso por patrón×host; JAMÁS crawl exhaustivo (decisión operador 2026-07-27).
Declarar el ancla temporal con `select now()::date` ejecutado por psql.

**§0.1 Corrección de completitud del grep de §3.1.4 (hallazgo de este plan).** El inventario
enumeró los call-sites con `grep -rl "sourceUrl="`, que sólo caza el prop JSX. Re-correr el
universo ampliado a la forma objeto-literal y registrar la salida verbatim:
`grep -rn "sourceUrl[[:space:]]*[:=]" app/components app/app --include=*.tsx | grep -v "\.test\."`.
Los call-sites que §3.1.4 NO tenía y que este plan añade al universo: `buscar-filtros.tsx:493`
(`sourceUrl: row.enlace ?? null` — sin `enlaceHumanoProyecto`, candidato #1),
`proyectos-similares.tsx:109` (SÍ pasa por `enlaceHumanoProyecto`),
`agenda-filtros.tsx:373`, `app/app/agenda/page.tsx:463,504`. Declarar que el universo de esta fase
es el AMPLIADO, y que la diferencia es de denominador del grep, no una regresión del inventario.

**§1 Universo de patrones.** Header exacto `## 1. Universo de patrones`. Tabla con una fila por
patrón×host, columnas: `id` (P-01…), `origen` (builder `archivo:línea` o `tabla.columna`),
`host`, `plantilla verbatim`, `parámetro que la instancia`, `emisor (E-NNN archivo:línea)`,
`gate`, `caso real`, `n en PROD`. Filas mínimas obligatorias:
- P-01 `buildSenadoUrl` → `tramitacion.senado.cl`, parámetro `boletin` (con sufijo)
- P-02 `buildCamaraUrl` → `www.camara.cl`, parámetros `prmID` + `prmBOLETIN`
- P-03 `enlaceHumanoProyecto` → rewrite: registrar sus DOS ramas (rama-rewrite → destino P-01;
  rama-verbatim → el host de la columna de origen)
- P-04 `partidoLegible` → **no emite href**; invariante "CERO URI en el DOM"; se valida SÓLO por
  construcción, sin probe (no hay recurso que pedir). Declararlo explícitamente para que su
  ausencia de la muestra live no se lea como omisión.
- Una fila por cada par tabla.columna×host de §3.3.3 con emisor real: `arista.enlace`,
  `citacion.enlace` (×2 hosts), `comision.enlace`, `comision_membresia.enlace`,
  `cruce_senal.evidencia->>'enlace_fuente'`, `declaracion*.enlace` (agrupables en una fila
  `datos.cplt.cl` declarando las 7 tablas), `lobby_audiencia.enlace` (×2 hosts),
  `lobby_audiencia.enlace_detalle`, `parlamentario.enlace` (×2 hosts), `proyecto.enlace` (×2 hosts),
  `proyecto_autor.enlace`, `sesion_sala.enlace` (×2 hosts), `tramitacion_evento.enlace` (×3 hosts),
  `votacion.enlace` (×2 hosts).

**§2 Exclusiones con razón.** Tabla `origen | n | razón`. Obligatorias:
`lobby_contraparte.enlace` (17.681 — no se emite al DOM, se muestra texto crudo),
`source_snapshot.source_url` (4.383 — `sourceUrl={null}` en el único call-site),
las 4 columnas bajo gate MONEY (`aporte.enlace`, `contrato.enlace`, `contratista.enlace`,
`donante.enlace` — 0 filas en PROD Y gate OFF: aunque el gate se encendiera no habría link),
`parlamentario_militancia.enlace` (`datos.bcn.cl` 48 + `opendata.camara.cl` 315 — desactivadas por
`partidoLegible`), y las 11 columnas de §3.3.5 con 0 filas.

**§3 Casos reales.** Por cada fila de §1 con `n > 0`, el bloque SQL VERBATIM que eligió el caso,
con `ORDER BY` determinista y `LIMIT 1`. Restricción de seguridad HEREDADA de 113 (T-113-05/06):
las queries de conteo/host devuelven sólo `split_part(<col>,'/',3)` y `count(*)`. Para el caso
concreto SÍ se necesita el valor de la URL —es el objeto de la fase— pero **sólo de columnas
NO-PII**: prohibido consultar `pii_contraparte_declaracion.*` y cualquier columna de los dominios
`infoprobidad/`, `servel/`, `money/`, `rut/`. Jamás ecoar ni escribir `SUPABASE_DB_URL`
(usar `psql "$SUPABASE_DB_URL" -tA -c ...`, nunca imprimir la variable).

**§4 Manifiesto de la muestra.** Lista ordenada AGRUPADA POR HOST (todos los casos de un host
contiguos) con `id | host | url del caso | patrón | qué se espera del recurso (ficha específica vs
página genérica)`. Este orden es el que consumirá el runner del Task 2.

Reglas de escritura: cero celdas vacías (usar `—`); toda afirmación con su comando o su cita
`archivo:línea`; ningún valor de URL de dominio PII.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-PATRONES.md && grep -q '^## 1. Universo de patrones' 115-PATRONES.md && grep -q 'buildSenadoUrl' 115-PATRONES.md && grep -q 'buildCamaraUrl' 115-PATRONES.md && grep -q 'enlaceHumanoProyecto' 115-PATRONES.md && grep -q 'partidoLegible' 115-PATRONES.md && grep -q 'lobby_contraparte' 115-PATRONES.md && grep -q 'source_snapshot' 115-PATRONES.md && grep -q 'leylobby' 115-PATRONES.md && grep -q 'datos.cplt.cl' 115-PATRONES.md && grep -q 'web-back.senado.cl' 115-PATRONES.md && grep -q 'opendata.camara.cl' 115-PATRONES.md && ! grep -qE 'postgres(ql)?://' 115-PATRONES.md && ! grep -qi 'pii_contraparte_declaracion.enlace *=' 115-PATRONES.md && echo PATRONES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| P-' 115-PATRONES.md` ≥ 20 (≥20 filas patrón×host en §1)
    - Los 7 hosts que SÍ se emiten aparecen todos: `for h in www.camara.cl opendata.camara.cl tramitacion.senado.cl www.senado.cl web-back.senado.cl www.leylobby.gob.cl datos.cplt.cl; do grep -q "$h" 115-PATRONES.md || echo "FALTA $h"; done` → sin salida
    - `! grep -nE '\|[[:space:]]*\|' 115-PATRONES.md` → cero celdas vacías (si hay match, debe estar dentro de un bloque de código y quedar declarado como tal, precedente 113-06)
    - `grep -q 'JAMÁS crawl exhaustivo' 115-PATRONES.md`
    - `grep -q 'sourceUrl\[\[:space:\]\]\*\[:=\]' 115-PATRONES.md || grep -q 'sourceUrl' 115-PATRONES.md` y §0.1 nombra `buscar-filtros.tsx:493`
  </acceptance_criteria>
  <done>115-PATRONES.md tiene §0/§0.1/§1/§2/§3/§4; cada patrón emitido tiene fila con plantilla, parámetro y caso real por SQL; cada exclusión tiene razón; cero credenciales y cero valores PII.</done>
</task>

<task type="auto">
  <name>Task 2: Runner curl-first rate-limited por host</name>
  <files>scripts/probar-links-externos.mjs</files>
  <read_first>
    - `scripts/verificar-links-internos.mjs` — cabecera completa (secciones "Mesura", "Portabilidad
      (LOCKED)", "Endurecimiento post code-review"): es el molde de UA, delay, salida `.txt`/`.json`,
      `os.tmpdir()`, `MSYS_NO_PATHCONV=1`, y de la tabla por stdout
    - `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-PATRONES.md` §4 (manifiesto)
  </read_first>
  <action>
Crear `scripts/probar-links-externos.mjs`. DIFERENCIA CRÍTICA con el runner de 114: aquí NO se usa
`fetch` de Node. Gotcha pagado en v3.0: el WAF de `camara.cl` bloquea `fetch` de Node y deja pasar
`curl`. El runner invoca `curl` por `child_process.execFile` (nunca `exec` con string, nunca shell)
y captura status + headers + los primeros bytes del cuerpo.

Contrato del runner:
- Manifiesto de casos EN EL PROPIO SCRIPT como array exportado `CASOS`, transcrito de §4 de
  `115-PATRONES.md`, con campos `{ id, patron, host, url, espera }`. La correspondencia con
  el documento es el contrato; un caso en el .md sin fila en `CASOS` es un defecto.
- Modo `--robots`: pide `https://<host>/robots.txt` para cada host DISTINTO del manifiesto, una vez.
- Modo por defecto: recorre `CASOS` **agrupados por host** (orden estable), SECUENCIAL. Concurrencia
  PROHIBIDA. Delay `DELAY_MS = 2500` (dentro de la banda 2-3s de CLAUDE.md) **entre requests al mismo
  host**; el delay se aplica también entre hosts para no acumular ráfagas.
- Flags de curl obligatorias: `--silent --show-error --location --max-time 20 --max-filesize 200000`
  y `-w` para el metadato (`%{http_code}`, `%{url_effective}`, `%{num_redirects}`, `%{content_type}`,
  `%{time_total}`). Método **GET** con `--range 0-8191` para acotar el cuerpo; **jamás `--head`**
  (servidores gubernamentales lo rechazan). `--user-agent` identificatorio con el nombre del proyecto
  y una URL de contacto — el mismo UA que ya usa la ingesta; leerlo de una constante nombrada
  `USER_AGENT` en el propio script para que sea auditable.
- Reintentos: como máximo UNO por caso, sólo ante fallo de RED (código de salida de curl, no HTTP),
  tras esperar 5s. Un 403/429/5xx NO se reintenta: se registra tal cual y se clasifica.
- Por cada request, registrar en la salida: timestamp ISO-8601 con milisegundos ANTES y DESPUÉS,
  `delta_ms_desde_request_previo_al_mismo_host`, host, id, url, http_code, url_effective,
  num_redirects, content_type, y `snippet` = primeros 300 caracteres del cuerpo con saltos de línea
  colapsados (para poder distinguir XML crudo de página humana y página de WAF de recurso real).
- Clasificación automática por caso: `OK` (2xx y `url_effective` conserva el parámetro que lo
  instancia), `REDIR-GENERICA` (2xx pero `url_effective` perdió el parámetro o `num_redirects>0`
  hacia una raíz), `XML-CRUDO` (`content_type` xml o el snippet abre con `<?xml`), `WAF-403`,
  `NO-DISPONIBLE` (4xx/5xx), `RED` (fallo de transporte). La clasificación es una PROPUESTA que el
  Plan 02 confirma a mano: el script no dicta veredicto de fase.
- Salida: `--out <prefijo>` escribe `<prefijo>.txt` (tabla legible + cabecera con UA, DELAY_MS,
  fecha de corrida) y `<prefijo>.json` (registros crudos). Filtros componibles `--host <h>` y
  `--id <id>` para la re-verificación acotada del Plan 03.
- Portabilidad LOCKED: para rutas temporales usar `os.tmpdir()` de Node, JAMÁS una variable de
  entorno de temp (en Git Bash/Windows es `undefined`). Documentar en la cabecera que las
  invocaciones con `--id`/`--host` cuyos valores contengan `/` requieren el prefijo
  `MSYS_NO_PATHCONV=1`.
- Si el manifiesto queda vacío tras aplicar filtros → exit 2 (fail-loud, precedente W-01 de 114).
- Cabecera del archivo: documentar por qué curl y no fetch (WAF camara.cl), la banda de rate-limit
  y su origen (CLAUDE.md, ingesta respetuosa), y que este script NO hace crawl: sólo recorre el
  manifiesto estratificado.

NO correr la muestra completa en este task (eso es el Plan 02). Sólo un smoke de UN caso para
probar que el runner funciona.
  </action>
  <verify>
    <automated>node --check scripts/probar-links-externos.mjs && grep -q "curl" scripts/probar-links-externos.mjs && grep -q "USER_AGENT" scripts/probar-links-externos.mjs && grep -q "os.tmpdir()" scripts/probar-links-externos.mjs && grep -q "MSYS_NO_PATHCONV" scripts/probar-links-externos.mjs && ! grep -qE '\bfetch\(' scripts/probar-links-externos.mjs && ! grep -q '\-\-head' scripts/probar-links-externos.mjs && node -e "import('./scripts/probar-links-externos.mjs').then(m=>{const c=m.CASOS;if(!Array.isArray(c)||c.length<20)throw new Error('CASOS='+(c&&c.length));const hosts=new Set(c.map(x=>x.host));if(hosts.size<7)throw new Error('hosts='+hosts.size);console.log('RUNNER_OK',c.length,hosts.size)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE 'DELAY_MS *= *[0-9]+' scripts/probar-links-externos.mjs` devuelve un valor ≥ 2000
    - `! grep -nE 'Promise\.all|Promise\.allSettled' scripts/probar-links-externos.mjs` → cero concurrencia
    - Smoke de un caso: `node scripts/probar-links-externos.mjs --id P-01-caso1 --out "$(node -p 'require("os").tmpdir()')/115-smoke"` → exit 0 y el `.json` existe con exactamente 1 registro
    - Con un filtro sin match: `node scripts/probar-links-externos.mjs --id NO-EXISTE --out "$(node -p 'require("os").tmpdir()')/115-vacio"; echo $?` → `2`
  </acceptance_criteria>
  <done>El runner existe, pasa `node --check`, exporta `CASOS` con ≥20 casos sobre ≥7 hosts, usa curl (no fetch), delay ≥2000ms, recorrido secuencial, y falla fuerte con manifiesto vacío.</done>
</task>

<task type="auto">
  <name>Task 3: robots.txt de cada host, registrado ANTES de la muestra</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-ROBOTS.txt</files>
  <read_first>
    - `scripts/probar-links-externos.mjs` (modo `--robots`, escrito en el Task 2)
    - `115-PATRONES.md` §4 (lista de hosts distintos)
    - `CLAUDE.md` §Conventions → "Respeto al servidor: rate-limit 2-3s/host, User-Agent
      identificatorio, robots.txt, caché diaria. Nunca ráfagas."
  </read_first>
  <action>
Correr `node scripts/probar-links-externos.mjs --robots --out <ruta del artefacto>` y consolidar en
`115-ROBOTS.txt`. Contenido obligatorio, por cada host distinto del manifiesto (7 hosts emitidos;
`datos.bcn.cl` se incluye SÓLO si alguna fila de §1 lo lista como emitido — hoy no lo es, y esa
ausencia se declara en una nota):
- el comando verbatim ejecutado,
- el `http_code` recibido,
- el cuerpo del robots.txt tal cual (o la declaración explícita "404 — sin robots.txt publicado",
  que es una respuesta válida y no una evasión),
- una línea de VEREDICTO por host: si alguna directiva `Disallow` cubre el path de alguno de
  nuestros casos, ese caso se RETIRA del manifiesto y se declara aquí y en §4 de `115-PATRONES.md`
  con la directiva citada. Un caso retirado por robots NO es "fuente caída": es respeto, y su
  patrón se valida sólo por construcción.
- la cabecera del artefacto lleva el `USER_AGENT` usado y la fecha/hora de la corrida.

Si un host devuelve 403 al pedir robots.txt, registrarlo como tal (evidencia: comando + código) y
marcar el host como `WAF-en-robots` — la muestra de ese host se intenta igual con el mismo UA y el
mismo delay, y su resultado se clasificará en el Plan 02.

Actualizar `115-PATRONES.md` §4 con la columna `robots` (`permitido` / `retirado por Disallow: …`)
para que el manifiesto y el robots sean un solo contrato.
  </action>
  <verify>
    <automated>cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-ROBOTS.txt && for h in www.camara.cl opendata.camara.cl tramitacion.senado.cl www.senado.cl web-back.senado.cl www.leylobby.gob.cl datos.cplt.cl; do grep -q "$h" 115-ROBOTS.txt || { echo "FALTA robots de $h"; exit 1; }; done && grep -qi 'VEREDICTO' 115-ROBOTS.txt && grep -q 'robots' 115-PATRONES.md && echo ROBOTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i '^VEREDICTO' 115-ROBOTS.txt` ≥ 7 (un veredicto por host)
    - `grep -qi 'user-agent' 115-ROBOTS.txt` y el UA registrado es idéntico a la constante `USER_AGENT` del runner: `grep -oE 'USER_AGENT *= *"[^"]+"' ../../../scripts/probar-links-externos.mjs`
    - Ningún host quedó sin respuesta registrada: `! grep -qi 'pendiente\|TBD' 115-ROBOTS.txt`
    - `115-PATRONES.md` §4 tiene columna robots: `grep -q '| robots' 115-PATRONES.md`
  </acceptance_criteria>
  <done>115-ROBOTS.txt registra los 7 hosts con comando, código, cuerpo y veredicto; los casos cubiertos por un Disallow quedan retirados del manifiesto con su directiva citada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → servidores gubernamentales | salida hacia terceros; el abuso es el riesgo, no la entrada |
| PROD Postgres → artefactos de fase | valores de columnas pueden contener PII o credenciales |
| artefactos de fase → repositorio público | todo lo que se escriba queda commiteado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Denial of Service | hosts gubernamentales | mitigate | recorrido secuencial + `DELAY_MS>=2500` por host + manifiesto estratificado (≤2 casos/host típicos); `Promise.all` prohibido y verificado por grep |
| T-115-02 | Information Disclosure | `SUPABASE_DB_URL` | mitigate | `psql "$SUPABASE_DB_URL"` sin ecoar; gate `! grep -qE 'postgres(ql)?://'` sobre cada artefacto |
| T-115-03 | Information Disclosure | columnas PII (`pii_contraparte_declaracion`, dominios `infoprobidad/servel/money/rut`) | mitigate | prohibidas como origen de casos; §2 las excluye por escrito; gate de grep en el verify |
| T-115-04 | Repudiation | muestra live sin trazabilidad | mitigate | timestamps ISO con ms antes/después + delta por host en el `.json`; artefacto commiteado |
| T-115-05 | Tampering | ejecución de comandos | mitigate | `execFile` con array de argumentos (nunca `exec` con string interpolado ni shell); las URLs vienen del manifiesto, no de entrada libre |
| T-115-06 | Spoofing | identidad del cliente ante la fuente | mitigate | `USER_AGENT` identificatorio único, auditable como constante, registrado en robots y en la salida |
| T-115-SC | Tampering | instalación de paquetes | mitigate | CERO paquete nuevo en esta fase (curl del sistema + stdlib de Node); si aparece la necesidad, checkpoint humano bloqueante antes de instalar |
</threat_model>

<verification>
- `115-PATRONES.md` cubre §3.2 completo + toda familia de §3.3.3 con emisor real; exclusiones con razón.
- `115-ROBOTS.txt` tiene los 7 hosts con veredicto; ningún host tocado sin robots consultado antes.
- El runner no usa `fetch`, no concurre, delay ≥2s, falla fuerte con manifiesto vacío.
- Cero credenciales y cero valores de columnas PII en los artefactos.
</verification>

<success_criteria>
- `grep -c '^| P-' 115-PATRONES.md` ≥ 20 y los 7 hosts emitidos presentes.
- `node --check scripts/probar-links-externos.mjs` exit 0 y `CASOS.length >= 20` sobre ≥7 hosts.
- `115-ROBOTS.txt` con ≥7 líneas `VEREDICTO`.
- `! grep -rqE 'postgres(ql)?://' .planning/phases/115-*/115-PATRONES.md .planning/phases/115-*/115-ROBOTS.txt`
</success_criteria>

<output>
Crear `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-01-SUMMARY.md` al terminar.
</output>
