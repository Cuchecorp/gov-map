---
phase: 115-link-ext-patrones-de-link-a-fuente-oficial
plan: 03
type: execute
wave: 3
depends_on: ["115-02"]
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
  - app/components/buscar-filtros.tsx
  - app/components/buscar-filtros.test.tsx
  - app/components/timeline-event.tsx
  - app/components/timeline-event.test.tsx
  - .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VERIFICACION.md
autonomous: true
requirements: [LINK-03]

must_haves:
  truths:
    - "El badge de /buscar enlaza a la ficha humana del Senado, no al XML crudo de wspublico"
    - "El enlace de cada evento del timeline llega al recurso humano o su limitación queda declarada"
    - "Todo patrón sin fix posible tiene su limitación declarada en la UI sin insinuar causalidad, con el linter verde"
    - "La suite queda en baseline, tsc en 0 y los guards de régimen verdes; el deploy queda declarado diferido a la Phase 125"
  artifacts:
    - path: "app/components/buscar-filtros.tsx"
      provides: "sourceUrl del badge de resultados pasado por enlaceHumanoProyecto"
      contains: "enlaceHumanoProyecto"
    - path: "app/components/timeline-event.tsx"
      provides: "href del evento resuelto al recurso humano y pasado por safeExternalHref"
      contains: "safeExternalHref"
    - path: ".planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VERIFICACION.md"
      provides: "Evidencia antes/después por hallazgo + veredicto de los 4 success criteria del ROADMAP"
      contains: "## Veredicto por success criterion"
  key_links:
    - from: "app/components/buscar-filtros.tsx"
      to: "app/components/validacion-fuente.tsx"
      via: "import de enlaceHumanoProyecto"
      pattern: "enlaceHumanoProyecto"
    - from: "115-VERIFICACION.md"
      to: "115-VEREDICTO.md §4"
      via: "una entrada por cada acción de la lista cerrada"
      pattern: "115-VEREDICTO"
---

<objective>
Cerrar la fase: extender el linter anti-insinuación ANTES de escribir copy nuevo, aplicar los fixes
de la lista cerrada de `115-VEREDICTO.md` §4 con tests que muerden, re-probar sólo los casos
afectados respetando el rate-limit, y dejar el veredicto de los 4 success criteria del ROADMAP con
la limitación del deploy diferido declarada honestamente.

Purpose: un patrón malo detectado y no arreglado es peor que no haberlo buscado. Y un fix con copy
nuevo que no pasó por el linter es una regresión del régimen anti-insinuación.
Output: fixes en código con tests, `115-VERIFICACION.md`, suite en baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-CONTEXT.md
@.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-02-SUMMARY.md

<interfaces>
`app/components/validacion-fuente.tsx`:
```
export function buildSenadoUrl(boletin: string): string
export function buildCamaraUrl(boletin: string, prmId: string): string
export function enlaceHumanoProyecto(enlace: string, boletin: string): string
```
`app/lib/utils.ts`: `export function safeExternalHref(url: string | null | undefined): string | null`

Call-site del candidato #1 (`app/components/buscar-filtros.tsx:493`, hoy):
`sourceUrl: row.enlace ?? null`  ← crudo, sin rewrite.

Call-sites que YA lo hacen bien (forma canónica a copiar):
`proyectos-similares.tsx:109` → `sourceUrl: enlaceHumanoProyecto(p.enlace ?? "", p.boletin) || null`
`ficha-header.tsx:70-73`, `autor-row.tsx:64`, `votacion-card.tsx:101-104` → misma forma.

Call-site del candidato #2 (`app/components/timeline-event.tsx`, hoy): `<a href={evento.enlace}>`
directo, sin `safeExternalHref` y sin rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — linter anti-insinuación extendido ANTES del copy</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - `app/lib/anti-insinuacion-guard.test.ts` COMPLETO: la estructura `SUPERFICIES_*`,
      `NEGACIONES_LOCKED`, el `stripTsComments`, el loader tolerante a archivo ausente
      (try/catch continue, añadido en 100-01) y el bloque de mutation self-check
    - `115-VEREDICTO.md` §4 (¿hay copy nuevo? ¿qué texto propone para las limitaciones declaradas?)
  </read_first>
  <action>
Orden LOCKED del proyecto (68-01, 100-01, 101-02, 103-03): el guard se extiende ANTES de escribir
el copy, nunca después.

Añadir un array `SUPERFICIES_LINK_EXT` con los archivos que este plan va a tocar o que emiten copy
sobre el origen del enlace: como mínimo `app/components/timeline-event.tsx` y
`app/components/buscar-filtros.tsx`, más cualquier archivo que §4 de `115-VEREDICTO.md` sume.
NO renombrar ni ampliar los arrays existentes (`SUPERFICIES_HOME`, `SUPERFICIES_PANEL`,
`SUPERFICIES_RELACIONES`, `SUPERFICIES_NOTIF`, MONEY/LOBBY) — Pitfall registrado en 100-01.
Reusar el loader tolerante a archivo ausente.

Vocabulario a cazar en estas superficies (el riesgo específico de una fase de links es insinuar que
la fuente oculta, miente o falla por voluntad): "oculta", "esconde", "no quiere", "se niega a",
"bloquea a propósito", "censura", además del vocabulario editorial/anti-ranking ya cubierto. Con
tildes exactas. Prohibido añadir términos genéricos que colisionen con identificadores del código
(lección del `top` pelado rechazado en 100-01): el linter corre sobre el texto RENDERIZADO tras
`stripTsComments`, no sobre identificadores.

Si §4 propone una leyenda de limitación (p.ej. para un patrón sin URL humana o para una fuente
caída), esa leyenda debe ser una CONSTANTE única y quedar restada del scan antes de los asserts
—como la leyenda MONEY (73-02) y la de similitud de voto (102-01)— para que su propio texto no se
auto-invalide.

Cerrar con un mutation self-check que pruebe que el detector MUERDE: inyectar EN MEMORIA un texto
prohibido sobre una superficie de `SUPERFICIES_LINK_EXT` y aseverar que produce offender; con el
árbol real, 0 offenders.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts 2>&1 | tail -20 && grep -q 'SUPERFICIES_LINK_EXT' lib/anti-insinuacion-guard.test.ts && grep -q 'timeline-event.tsx' lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts` → exit 0, 0 offenders sobre el árbol real
    - El self-check muerde: `grep -qi 'self-check\|mutation' app/lib/anti-insinuacion-guard.test.ts`
    - Arrays previos intactos: `git diff app/lib/anti-insinuacion-guard.test.ts | grep '^-' | grep -c 'SUPERFICIES_'` → `0`
  </acceptance_criteria>
  <done>El linter conoce las superficies de esta fase y muerde con el vocabulario nuevo, antes de que exista una sola línea de copy nuevo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixes de los patrones malos con tests que muerden</name>
  <files>
    app/components/buscar-filtros.tsx, app/components/buscar-filtros.test.tsx,
    app/components/timeline-event.tsx, app/components/timeline-event.test.tsx
  </files>
  <read_first>
    - `115-VEREDICTO.md` §3 (candidatos #1 y #2) y §4 (lista cerrada de acciones) — §4 es el alcance
      EXACTO de este task: ni un fix de más, ni uno de menos
    - `app/components/buscar-filtros.tsx` alrededor de `:493`
    - `app/components/proyectos-similares.tsx:100-115` — la forma canónica a copiar
    - `app/components/timeline-event.tsx` completo + su llamante en
      `app/app/proyecto/[boletin]/page.tsx` (de dónde sale el boletín) y el tipo del evento
    - `app/lib/utils.ts` (`safeExternalHref`)
  </read_first>
  <behavior>
    - Candidato #1: dado un resultado de búsqueda cuyo `enlace` es
      `https://tramitacion.senado.cl/wspublico/tramitacion.php?boletin=14309-04`, el `sourceUrl` del
      badge resulta la ficha humana `.../appsenado/templates/tramitacion/index.php?boletin_ini=14309-04`.
    - Candidato #1, rama verbatim: un `enlace` en `opendata.camara.cl` pasa SIN cambios (el rewrite
      es por host+path, jamás por substring).
    - Candidato #1, borde: `enlace` null/vacío → `sourceUrl` null y el `<a>` no se emite.
    - Candidato #2: un evento de timeline con `enlace` en `tramitacion.senado.cl/wspublico/...`
      resuelve al recurso humano; un evento con `enlace` en `www.senado.cl` u `opendata.camara.cl`
      pasa verbatim; `enlace` con esquema no-web (`javascript:`) → no se emite `<a>`.
    - Ningún test asevera copy nuevo sin que el linter del Task 1 esté verde.
  </behavior>
  <action>
Escribir primero los tests del bloque `<behavior>` y verlos FALLAR; luego el fix mínimo.

**Candidato #1 (`buscar-filtros.tsx:493`).** Sustituir `sourceUrl: row.enlace ?? null` por la forma
canónica de `proyectos-similares.tsx:109`: pasar por `enlaceHumanoProyecto(row.enlace ?? "",
row.boletin)` y colapsar el vacío a `null`. Importar desde `@/components/validacion-fuente`. NO
crear builder nuevo, NO tocar `enlaceHumanoProyecto`, NO cambiar la forma del badge. Si `row` no
expone el boletín en ese punto, threadearlo desde la fila — el boletín es la clave de la lista de
resultados y existe aguas arriba en `app/app/buscar/page.tsx`.

**Candidato #2 (`timeline-event.tsx`).** Dos cosas, ambas de la lista de §4:
(a) pasar el `href` por `safeExternalHref` — hoy este `<a>` es el único emisor externo del sitio que
se salta el chokepoint de §3.1.3 del inventario, y `safeExternalHref` es obligatorio en TODO href
externo; si devuelve `null`, el `<a>` no se emite (mismo contrato que el badge).
(b) aplicar el rewrite al recurso humano según el veredicto de §3: si `115-VEREDICTO.md` confirmó
que las 982 filas de `tramitacion.senado.cl` tienen path `/wspublico/`, pasar el enlace por
`enlaceHumanoProyecto(evento.enlace, boletin)` threadeando el boletín desde el llamante
(`app/app/proyecto/[boletin]/page.tsx` ya lo tiene como parámetro de ruta). Si el veredicto fue que
NO son `/wspublico/`, aplicar sólo (a) y registrar en `115-VERIFICACION.md` por qué (b) no procede
—con el conteo SQL como evidencia—, jamás por "no se pudo".

Prohibido: introducir un patrón "v1"/"por ahora"/"placeholder". Si un fix no cabe, se declara con
evidencia, no se degrada.

Deploy: NINGUNO. Los fixes viajan con la Phase 125 (decisión LOCKED de v12.0).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/buscar-filtros.test.tsx components/timeline-event.test.tsx 2>&1 | tail -20 && grep -q 'enlaceHumanoProyecto' components/buscar-filtros.tsx && grep -q 'safeExternalHref' components/timeline-event.tsx && ! grep -nE 'href=\{evento\.enlace\}' components/timeline-event.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Los tests muerden: revertir el fix de `buscar-filtros.tsx` debe tumbar al menos un test (correr la suite del archivo con el cambio revertido y restaurarlo después)
    - `cd app && pnpm vitest run components/buscar-filtros.test.tsx components/timeline-event.test.tsx` → exit 0
    - Cero builders nuevos: `git diff app/components/validacion-fuente.tsx app/lib/format.ts app/lib/utils.ts | wc -l` → `0`
    - Cero lenguaje de alcance reducido: `! git diff | grep -inE '^\+.*(por ahora|placeholder|versión simplificada)'`
    - Alcance = §4: cada archivo modificado aparece en la lista de acciones de `115-VEREDICTO.md`
  </acceptance_criteria>
  <done>Los patrones marcados `PATRON-MALO` en §2 quedan corregidos reusando `enlaceHumanoProyecto`/`safeExternalHref`, con tests que fallan si el fix se revierte, sin builders nuevos y sin deploy.</done>
</task>

<task type="auto">
  <name>Task 3: Re-probe acotado, no-regresión y cierre de fase</name>
  <files>.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-VERIFICACION.md</files>
  <read_first>
    - `115-VEREDICTO.md` (§2 tabla, §4 acciones, §5 deploy diferido)
    - `115-MUESTRA.txt` (bloques de rate-limit y cobertura)
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-VERIFICACION.md` — molde del
      documento de cierre: evidencia antes/después por hallazgo + veredicto por SC + limitación declarada
    - `.planning/ROADMAP.md` §Phase 115 (los 4 success criteria verbatim)
  </read_first>
  <action>
**(a) Re-probe acotado.** Sólo los casos afectados por los fixes, con el runner filtrado:
`MSYS_NO_PATHCONV=1 node scripts/probar-links-externos.mjs --id <id> --out "$(node -p 'require("os").tmpdir()')/115-repost"`
Mismo `DELAY_MS`, mismo UA, sin ráfagas. Copiar los registros al documento de cierre como bloque
`DESPUÉS`, junto al bloque `ANTES` tomado de `115-MUESTRA.json`. El re-probe prueba que la URL
RESULTANTE del patrón corregido responde; NO prueba el deploy — el sitio sigue sirviendo el bundle
viejo hasta la Phase 125.

**(b) No-regresión.** Correr y registrar la salida:
- `cd app && pnpm test` → comparar con la baseline **1431** de app; el delta esperado es sólo el de
  los tests nuevos de este plan (declarar el número exacto).
- `pnpm -r --filter "./packages/*" test` → **sin delta** (esta fase no toca packages).
- `pnpm typecheck` (raíz, `tsc -b`) → exit 0.
- Los 9 guards de régimen:
  `cd app && pnpm vitest run lib/anti-insinuacion-guard.test.ts lib/lockdown-guard.test.ts lib/vsim-antiflip-guard.test.ts lib/notif-antiflip-guard.test.ts lib/money-antiflip-guard.test.ts lib/bento-guards.test.ts lib/bento-coherencia-guard.test.ts lib/env-example-guard.test.ts lib/name-match-rut-guard.test.ts`
  → todos verdes; registrar los conteos.
- Ningún flag cambiado: `git diff .env.example | wc -l` → `0`.

**(c) `115-VERIFICACION.md`.** Estructura:
- §1 Qué se corrigió — tabla `hallazgo | archivo:línea | ANTES | DESPUÉS | test que lo respalda | commit`.
- §2 Qué se declaró — por cada `FUENTE-CAIDA-WAF` y cada `OK-POR-CONSTRUCCION`: la declaración con
  su evidencia (comando + respuesta) y dónde queda visible para el ciudadano si toca la UI.
- §3 Header exacto `## Veredicto por success criterion`: los 4 SC del ROADMAP §Phase 115 transcritos
  verbatim, cada uno con `PASS` / `PASS con limitación declarada` / `FAIL` + evidencia. Precedente
  LOCKED (114-03): lo que no se observó contra el deploy real cierra como **PASS con limitación
  declarada**, nunca PASS pelado; la observación ocurre en la Phase 125.
- §4 Reproducción — comandos exactos para re-correr muestra, veredicto y suite.
- §5 Deuda que viaja a 125 — lista nominal de lo que la Phase 125 debe re-verificar sobre el deploy
  (mínimo: los fixes de este plan), para que el E2E final no lo redescubra.
- §6 No-regresión — los conteos de (b) tal cual salieron.
  </action>
  <verify>
    <automated>cd app && pnpm test 2>&1 | tail -5 && cd .. && pnpm typecheck && cd .planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial && test -f 115-VERIFICACION.md && grep -q '^## Veredicto por success criterion' 115-VERIFICACION.md && grep -qi 'Phase 125' 115-VERIFICACION.md && grep -q 'ANTES' 115-VERIFICACION.md && grep -q 'DESPUÉS' 115-VERIFICACION.md && ! grep -qE 'postgres(ql)?://' 115-VERIFICACION.md && echo CIERRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && pnpm test` → exit 0; total = 1431 + tests nuevos de este plan, declarado en §6
    - `pnpm -r --filter "./packages/*" test` → exit 0 y sin delta de conteo
    - `pnpm typecheck` (raíz) → exit 0
    - Los 9 guards verdes en la invocación única de (b) → exit 0
    - `git diff .env.example | wc -l` → `0` (MONEY/NOTIF/VSIM/CLASIFICACION intactos)
    - §3 tiene 4 veredictos, uno por SC: `grep -cE 'PASS|FAIL' 115-VERIFICACION.md` ≥ 4
    - Cero acciones huérfanas: cada ítem de `115-VEREDICTO.md` §4 aparece en §1 o §2 de `115-VERIFICACION.md`
  </acceptance_criteria>
  <done>Re-probe acotado registrado antes/después; suite en baseline+delta declarado, tsc 0, 9 guards verdes, `.env.example` intacto; los 4 SC del ROADMAP con veredicto y la limitación del deploy diferido declarada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato de la fuente → `href` del DOM | una URL almacenada llega al navegador del ciudadano |
| copy nuevo → ciudadano | texto que puede insinuar intención de la fuente |
| agente → servidores gubernamentales | re-probe acotado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-10 | Tampering (XSS) | `timeline-event.tsx` `<a href={evento.enlace}>` | mitigate | pasar el href por `safeExternalHref` (allowlist `http:`/`https:`, fail-closed); test con `javascript:` que asevera que el `<a>` no se emite |
| T-115-11 | Information Disclosure | copy que insinúa intención de la fuente | mitigate | `SUPERFICIES_LINK_EXT` en el linter anti-insinuación, extendido ANTES del copy, con mutation self-check |
| T-115-12 | Elevation of Privilege | flips de régimen colaterales | mitigate | `git diff .env.example` = 0 líneas + los 4 anti-flip guards (money/notif/vsim) y lockdown verdes en el gate |
| T-115-01 | Denial of Service | hosts gubernamentales | mitigate | re-probe SÓLO por `--id` de los casos afectados, mismo delay y mismo UA; jamás re-corrida completa |
| T-115-SC | Tampering | instalación de paquetes | mitigate | CERO paquete nuevo; si un fix pareciera exigirlo, checkpoint humano bloqueante antes de instalar |
</threat_model>

<verification>
- Linter extendido y verde ANTES del copy; arrays previos intactos.
- Cada `PATRON-MALO` de §2 tiene fix con test que muerde, o declaración con evidencia.
- `safeExternalHref` cubre el último emisor externo que se lo saltaba.
- Suite baseline + delta declarado, tsc 0, 9 guards verdes, `.env.example` intacto.
- Los 4 SC del ROADMAP con veredicto honesto; deploy diferido a 125 declarado.
</verification>

<success_criteria>
- `cd app && pnpm test` exit 0 con total = 1431 + delta declarado; `pnpm typecheck` exit 0.
- `grep -q 'enlaceHumanoProyecto' app/components/buscar-filtros.tsx` y `grep -q 'safeExternalHref' app/components/timeline-event.tsx`.
- `115-VERIFICACION.md` con `## Veredicto por success criterion` y 4 veredictos con evidencia.
- Cero acciones huérfanas respecto de `115-VEREDICTO.md` §4.
</success_criteria>

<output>
Crear `.planning/phases/115-link-ext-patrones-de-link-a-fuente-oficial/115-03-SUMMARY.md` al terminar.
</output>
