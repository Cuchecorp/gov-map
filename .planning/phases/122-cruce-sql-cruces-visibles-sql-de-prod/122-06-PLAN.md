---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 06
type: execute
wave: 4
depends_on: [122-05]
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "Existe un artefacto único 122-CRUCES-SQL.md que un lector puede abrir y auditar sin leer código"
    - "Los 6 grupos del universo del §0.3 están cubiertos: cero grupo sin filas"
    - "Toda fila lleva query verbatim, nº SQL, nº deploy y veredicto"
    - "Los vacíos honestos y los límites declarados tienen sección propia"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md"
      provides: "Artefacto único de la fase 122, consolidado y coherente"
      contains: "## Vacíos honestos"
  key_links:
    - from: "122-CRUCES-SQL.md"
      to: "113-INVENTARIO.md"
      via: "cobertura del universo de cruces validada contra el inventario rector"
      pattern: "113-INVENTARIO"
---

<objective>
Consolidar los 4 fragmentos en el artefacto único de la fase, validar que la cobertura del universo de
cruces es total contra el inventario 113, y dejar el documento coherente con los fixes ya aplicados.

Purpose: la Phase 125 (E2E) consume ESTE archivo para re-verificar los cruces sobre el deploy final.
Un artefacto fragmentado o con veredictos desactualizados la haría fallar.
Output: `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md
@./CLAUDE.md

Régimen LOCKED: cero PII y cero cadenas de conexión en el artefacto; no se re-corre la auditoría
(este plan consolida, no descubre) salvo para cerrar un hueco de cobertura detectado aquí; no se
toca código; no se hace deploy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidar los fragmentos en 122-CRUCES-SQL.md</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md</files>
  <read_first>
    - Los 5 fragmentos listados en `<context>` (leer cada uno completo — son la materia prima)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §0 (formato de
      "Método y cobertura" y de la tabla de cobertura — el artefacto de 122 debe espejarlo)
  </read_first>
  <action>
Escribir `122-CRUCES-SQL.md` con front-matter (`fase: 122`, `estado: validado`, `fecha`,
`consumido_por: [125]`) y esta estructura:

1. `## 0. Método y cobertura` — del fragmento 00 (método, vocabulario de veredicto, plantilla,
   universo de 6 grupos, advertencia del emisor huérfano, límites del método).
2. `## 1. Sujetos deterministas y gates observados` — del fragmento 00 §1 y §2.
3. `## 2. Relaciones entre parlamentarios` — fragmento 01 §1 y §2.
4. `## 3. /comparar (4 ejes + VSIM)` — fragmento 01 §3 y §4.
5. `## 4. Cruces de ficha y de proyecto` — fragmento 02 §1 y §2.
6. `## 5. Panel de actualidad` — fragmento 02 §3.
7. `## 6. lobby↔PL` — fragmento 03 §1, §2 y §3 (cobertura declarada con su fecha).
8. `## Vacíos honestos` — fragmento 03 §4, INCLUYENDO la fila de `lobby_sector_aporte` con su
   "NO es bug".
9. `## Fixes aplicados` — la tabla del fragmento 04 (discrepancia → archivo:línea → test).
10. `## Límites declarados` — unión de los `## Límites de este fragmento` de 01/02/03 más los tres
    límites del método (§0.5), más los fixes NO aplicados con su motivo y su handoff (Phase 124 / 125).

Reglas de consolidación:
- **Coherencia de veredictos**: si el fragmento 04 degradó una fila de `discrepancia-corregida` a
  `discrepancia-declarada`, el artefacto final debe reflejar el estado FINAL. Cualquier contradicción
  entre fragmentos se resuelve a favor del fragmento 04 (es el más tardío) y se anota.
- **Queries verbatim**: se copian tal cual; nunca se reescriben "para que se lean mejor".
- Renumerar los bloques ```sql de forma global y correlativa, actualizando las referencias de cada fila.
- Los fragmentos NO se borran (quedan como evidencia de la corrida); el artefacto único los referencia.
  </action>
  <verify>
    <automated>test -f .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md &amp;&amp; grep -c "^## " .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo contiene los 10 headings `## ` listados arriba, en ese orden
    - `## Vacíos honestos` contiene la cadena `lobby_sector_aporte` y la afirmación de que NO es bug
    - Cada una de las secciones 2-6 contiene al menos una tabla con la columna `| veredicto |`
    - Cero `postgres://`, cero `rut`, cero email en el archivo
      (`grep -inE "postgres(ql)?://|[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}" ` no devuelve nada salvo el
      mailto institucional si aparece)
  </acceptance_criteria>
  <done>Artefacto único escrito, con los 6 grupos, los vacíos honestos, los fixes y los límites.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoría de cobertura contra el inventario 113 y cierre</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §3.0 (catálogo
      completo de emisores E-001…E-060) y §3.0.1 (huérfanos)
    - `122-CRUCES-SQL.md` recién escrito (secciones 2-6)
  </read_first>
  <action>
Añadir al final `## Cobertura × inventario 113` con una tabla:
`| emisor (E-NNN) | archivo | ¿emite un cruce/conteo? | ¿auditado en 122? | sección | motivo si NO |`

Método determinista: recorrer TODAS las filas del §3.0 del inventario 113 y clasificarlas en tres
cubetas, con el criterio escrito arriba de la tabla:
- **emite cruce/conteo** (un número derivado de más de una entidad, o un N/M, o una cobertura) →
  DEBE estar auditado en alguna sección 2-6. Si no lo está: es un HUECO.
- **no emite cruce** (link, fecha, chrome, primitiva) → fuera del alcance de 122, se marca así.
- **gated OFF o huérfano** (MONEY/NOTIF, E-003, E-008) → fuera del denominador observable, DECLARADO
  con su motivo, nunca omitido en silencio.

Si aparece un HUECO: cerrarlo AHORA con una fila nueva en la sección que corresponda (recalculando
con el mismo método: RPC por psql + primeros principios + `curl` del deploy). Si el hueco no es
recalculable, se declara en `## Límites declarados` con su evidencia. **Prohibido cerrar un hueco
declarando "no aplica" sin la query o el motivo estructural.**

Cerrar el artefacto con `## Veredicto de la fase`: nº total de cruces auditados, desglose por
veredicto (`cuadra` / `discrepancia-corregida` / `discrepancia-declarada`), y la afirmación explícita
de qué queda pendiente de observar sobre el deploy real (la Phase 125), dado que los fixes de UI no
se despliegan en esta fase.

Marcar `estado: validado` en el front-matter solo si la tabla de cobertura no deja huecos abiertos.
  </action>
  <verify>
    <automated>grep -q "## Cobertura × inventario 113" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md &amp;&amp; grep -q "## Veredicto de la fase" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md &amp;&amp; grep -c "E-0" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL.md</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de cobertura tiene una fila por cada emisor E-001…E-060 del inventario 113 (60 filas)
    - Cero filas con `¿auditado en 122?` = NO sin motivo escrito
    - `## Veredicto de la fase` da los 3 conteos por veredicto y suman el total de cruces auditados
    - El front-matter dice `estado: validado` solo si no hay huecos abiertos
    - Ningún archivo de `app/` o `supabase/` modificado por este plan
  </acceptance_criteria>
  <done>Cobertura probada emisor por emisor contra el inventario rector; artefacto cerrado y consumible por 125.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefacto .md → repo público | todo lo consolidado queda en git para siempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-17 | Information Disclosure | PII o credencial arrastrada al consolidar | mitigate | Acceptance corre un grep de `postgres://` y de patrón email sobre el artefacto final |
| T-122-18 | Repudiation | veredicto desactualizado tras los fixes | mitigate | Regla de coherencia: el fragmento 04 manda; contradicciones se anotan |
| T-122-19 | Information Disclosure | hueco de cobertura silenciado como "no aplica" | accept→mitigate | Tabla de cobertura obligatoria por emisor, con motivo escrito por cada NO |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes |
</threat_model>

<verification>
- `122-CRUCES-SQL.md` existe con las 10 secciones + cobertura + veredicto
- La tabla de cobertura cubre los 60 emisores del inventario 113
- `git diff --stat` lista únicamente el artefacto
</verification>

<success_criteria>
La Phase 125 puede tomar `122-CRUCES-SQL.md` y re-verificar cada cruce sobre el deploy final sin
volver a decidir qué auditar ni con qué query.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-06-SUMMARY.md` when done
</output>
