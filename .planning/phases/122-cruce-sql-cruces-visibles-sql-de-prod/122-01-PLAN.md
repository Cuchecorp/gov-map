---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "Un lector ve el método re-ejecutable (comando psql verbatim, comando de lectura del deploy) sin leer código"
    - "El universo de cruces visibles queda enumerado desde el inventario 113, no asumido"
    - "Los 3 sujetos deterministas (D1165, S1338, 14309-04) quedan re-verificados contra PROD de HOY, con los conteos observados"
    - "Los gates observados (CRUCES/VSIM/NET ON, MONEY/NOTIF OFF) quedan registrados con evidencia del deploy"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md"
      provides: "Método, universo de cruces, sujetos re-verificados, plantilla de tabla y vocabulario de veredicto"
      contains: "## 0. Método y cobertura"
  key_links:
    - from: "122-CRUCES-SQL-00-METODO.md"
      to: "113-INVENTARIO.md §1.1–1.3"
      via: "sujetos deterministas reutilizados"
      pattern: "D1165|S1338|14309-04"
---

<objective>
Fijar el método de la fase 122 y el universo cerrado de cruces visibles a recalcular, con los sujetos
deterministas re-verificados contra PROD de hoy.

Purpose: los 3 planes de auditoría de la Wave 1 (02/03/04) y el de fixes (05) tienen que hablar del
MISMO caso y del MISMO vocabulario de veredicto. Sin este fragmento, cada plan inventaría su método.
Output: `122-CRUCES-SQL-00-METODO.md` (fragmento; lo consolida el plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md
@./CLAUDE.md

Régimen LOCKED aplicable a TODA la fase (repetido en cada plan):
- Queries a PROD read-only: `set -a; source .env; set +a` y luego
  `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "…"`.
  **JAMÁS ecoar ni escribir el valor de `SUPABASE_DB_URL`** en archivo ni en salida.
- Conteos por `psql -tA`, nunca por REST (PostgREST capa a 1.000 filas).
- Cero PII: ni RUT, ni email, ni monto individual. Solo nombres de columna y agregados.
- Cero requests a fuentes gubernamentales (camara.cl / senado.cl / BCN / leylobby) en esta fase.
- Lectura del deploy = `curl -s https://observatorio-congreso.thevalis.workers.dev/<ruta>` (SSR, HTML
  server-rendered). NO es fuente gubernamental; no aplica rate-limit 2-3 s.
- NO se hace deploy en esta fase (viaja con la Phase 125). No se tocan flags `*_PUBLIC_ENABLED`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Método, régimen y universo cerrado de cruces</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §0 (líneas 22-210:
      formato de "Método y cobertura", tabla de cobertura, límites declarados) y §3.0 (catálogo de
      emisores E-001…E-060, para extraer los emisores de cruce)
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md`
  </read_first>
  <action>
Crear el fragmento con front-matter YAML (`fase: 122`, `fragmento: 00-metodo`, `estado: borrador`,
`fecha`, `ancla_temporal`) y estas secciones:

`## 0. Método y cobertura` — transcribir el régimen del bloque `<context>` de este plan (psql read-only,
prohibición de imprimir `SUPABASE_DB_URL`, `-tA` no REST, cero PII, cero fuentes gubernamentales,
lectura del deploy por `curl`). Declarar explícitamente el ancla temporal ejecutando
`select now()::date` y registrando el valor devuelto.

`## 0.1 Vocabulario de veredicto` — definir los 3 valores LOCKED que usarán TODOS los fragmentos:
`cuadra` (número SQL == número del deploy), `discrepancia-corregida` (difieren; fix aplicado en el
código del sitio en el plan 122-05, ambos números registrados), `discrepancia-declarada` (difieren o
no es recalculable; NO se corrige en esta fase, queda el porqué). Regla dura: "No encontré
discrepancias" SOLO vale si la query que lo demuestra está transcrita verbatim en el fragmento.

`## 0.2 Plantilla de fila` — el molde de tabla que los fragmentos 01/02/03 deben usar, con estas
columnas exactas y en este orden:
`| # | superficie (ruta) | emisor (archivo:línea) | origen (RPC o tabla.columna) | query verbatim | nº SQL | nº deploy | veredicto |`
La columna "query verbatim" apunta a un bloque ```sql numerado más abajo en el mismo fragmento
(las queries largas NO van dentro de la celda).

`## 0.3 Universo cerrado de cruces visibles` — enumerar los 6 grupos con su asignación de plan,
derivando cada emisor del catálogo §3.0 del inventario 113 (citar el id E-NNN):
  1. Relaciones entre parlamentarios — 5 bloques de ficha (E-022 `cross-links-parlamentario.tsx`,
     `app/app/parlamentario/[id]/page.tsx:197-206` readers: `copartidarios_de_parlamentario`,
     `de_la_misma_zona`, `co_comisionados_de_parlamentario`, `coautores_de_parlamentario`,
     `militancia_historica_compartida`) + los conteos del resumen (E-029
     `app/components/parlamentario-resumen.tsx`, cuyos conteos viven en la lib
     `app/lib/parlamentario-resumen-conteos.ts` — la lib NO tiene id E-NNN propio, es la
     dependencia de conteo de E-029 según el propio inventario) → **plan 122-02**
  2. `/comparar` — 4 ejes + VSIM (E-051 `app/app/comparar/page.tsx`; el literal VSIM
     "Coinciden en {n} de {m}" lo emite `app/components/similitud-votacion-comparar.tsx:120`,
     inventariado en **§4.7 (C3/C4)** de 113 y SIN id E-NNN propio en §3.0 — citar §4.7, jamás
     fabricar un sufijo de id) → **plan 122-02**
  3. Cruces de ficha y de proyecto (E-044 `cruces-de-proyecto.tsx:199`, E-053
     `cruces-de-parlamentario.tsx`; RPCs `cruces_de_proyecto` / `cruces_de_parlamentario`;
     migraciones 0047–0050) → **plan 122-03**
  4. Panel de actualidad — 6 señales (E-055 `app/components/panel-actualidad.tsx`, RPC
     `actualidad_senales_panel`, migraciones 0065/0066) → **plan 122-03**
  5. lobby↔PL (E-020 `lobby-menciones-de-boletin.tsx:212-213` `total_n`, E-002
     `lobby-de-parlamentario.tsx`; RPCs `lobby_menciones_de_boletin`, `lobby_de_parlamentario`)
     → **plan 122-04**
  6. `lobby_sector_aporte` — 0 filas HONESTAS (stub estructural de 0052) → **plan 122-04**

`## 0.4 Emisor huérfano — advertencia` — registrar VERBATIM el hallazgo §3.0.1 del inventario:
`app/components/actualidad-module.tsx` (E-008) NO se renderiza en ninguna ruta (superseded por
`panel-actualidad.tsx`, E-055). El plan 122-03 audita E-055, NO E-008.

`## 0.5 Límites del método` — declarar por adelantado los 3 límites conocidos: (a) los fixes de UI
NO se despliegan en esta fase ⇒ el "nº deploy" registrado es el del código PRE-fix (la re-verificación
post-deploy es la Phase 125); (b) los bloques gated OFF (MONEY, NOTIF) no emiten DOM ⇒ sus cruces no
son observables y quedan fuera del denominador, declarado, no omitido; (c) cualquier cruce no
recalculable por falta de datos en PROD se declara como límite con evidencia, jamás se fabrica.
  </action>
  <verify>
    <automated>test -f .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md &amp;&amp; grep -c "discrepancia-declarada" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe y contiene los headings literales `## 0. Método y cobertura`,
      `## 0.1 Vocabulario de veredicto`, `## 0.2 Plantilla de fila`,
      `## 0.3 Universo cerrado de cruces visibles`, `## 0.4 Emisor huérfano — advertencia`,
      `## 0.5 Límites del método`
    - §0.3 enumera los 6 grupos, y cada uno cita al menos un id de emisor `E-0NN` del inventario 113
    - Los 3 términos de veredicto (`cuadra`, `discrepancia-corregida`, `discrepancia-declarada`) aparecen definidos
    - `grep -c "SUPABASE_DB_URL" ` sobre el archivo devuelve solo ocurrencias del NOMBRE de la variable;
      cero cadenas `postgres://` o `postgresql://` en el archivo
  </acceptance_criteria>
  <done>Fragmento de método escrito, con universo cerrado de 6 grupos trazado al inventario 113.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verificar los 3 sujetos deterministas y los gates observados contra PROD de hoy</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §1.1 (líneas
      304-335, query del sujeto A `D1165`), §1.2 (336-369, sujeto B `S1338`), §1.3 (370-402,
      boletín A `14309-04`)
  </read_first>
  <action>
Re-ejecutar VERBATIM las 3 queries de selección de sujeto del inventario 113 §1.1/§1.2/§1.3 contra
PROD (read-only, `psql -tA`) y añadir al fragmento la sección `## 1. Sujetos deterministas (re-verificados)`:

- Una fila por sujeto: `sujeto | query (ref §1.x de 113) | valor 2026-07-27 (inventario) | valor de hoy | ¿sigue siendo el sujeto elegido?`
- **Si la query devuelve otro id que `D1165` / `S1338` / `14309-04`**: NO se cambia de sujeto. Se
  REGISTRA la deriva (id nuevo + conteos de ambos) y se declara que 122 y 125 siguen con los sujetos
  del inventario para mantener la comparabilidad, citando la decisión.
- Registrar los conteos observados hoy para cada sujeto (votos / lobby confirmado / patrimonio /
  cruces / comisiones / militancias para D1165 y S1338; votaciones / embedding / `cruces_de_proyecto`
  para 14309-04) — son la línea base contra la que los planes 02/03/04 comparan.

Añadir la sección `## 2. Gates observados en el deploy` leyendo el DOM real:
`curl -s https://observatorio-congreso.thevalis.workers.dev/parlamentario/D1165 | grep -o -E 'id="(relaciones|cruces|dinero|financiamiento|lobby)"' | sort -u`
y `curl -s "https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1165&b=S1338" | grep -c "Coinciden en"`
(la URL SIEMPRE entre comillas dobles: sin ellas el `&` manda el comando a background y el `?`
globbea ⇒ el comando registrado en el artefacto no sería reproducible).
Registrar el veredicto observado por gate (CRUCES / VSIM / NET ON; MONEY / NOTIF OFF y ausentes del
DOM) con el comando que lo demuestra. Si un gate observado difiere de lo que dice el CONTEXT, se
registra la observación (el DOM manda sobre la nota), NO se toca ningún flag.
  </action>
  <verify>
    <automated>grep -q "## 1. Sujetos deterministas (re-verificados)" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md &amp;&amp; grep -q "## 2. Gates observados en el deploy" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md &amp;&amp; grep -q "D1165" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md</automated>
  </verify>
  <acceptance_criteria>
    - §1 contiene los 3 sujetos `D1165`, `S1338`, `14309-04` con conteos numéricos observados HOY
      (no copiados del inventario — si coinciden, se dice "idéntico a 113")
    - §2 contiene, por cada gate (CRUCES, VSIM, NET, MONEY, NOTIF), el comando `curl` verbatim y el
      resultado observado
    - Cero valores PII y cero cadenas de conexión en el archivo
    - Ningún archivo fuera de `122-CRUCES-SQL-00-METODO.md` fue modificado (`git status --short` no
      lista código de `app/` ni `supabase/`)
  </acceptance_criteria>
  <done>Sujetos y gates re-verificados contra PROD y el deploy, con comandos verbatim registrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → PROD (psql) | credencial de DB con privilegio alto cruza aquí |
| artefacto .md → repo público | todo lo escrito queda en git para siempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Information Disclosure | `SUPABASE_DB_URL` en salida/artefacto | mitigate | Solo `psql "$SUPABASE_DB_URL"` con la variable sin expandir en el texto; acceptance criteria exige cero `postgres://` en el archivo |
| T-122-02 | Information Disclosure | PII en el artefacto (RUT/email/monto) | mitigate | Solo agregados y nombres de columna; ninguna query de este plan hace `select rut/email/monto` |
| T-122-03 | Tampering | escritura accidental a PROD | mitigate | Todas las queries son `select`; cero DDL/DML en este plan |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes |
</threat_model>

<verification>
- `122-CRUCES-SQL-00-METODO.md` existe con las secciones §0, §0.1–0.5, §1, §2
- `git diff --stat` muestra únicamente ese archivo
- Cero secretos: `grep -rE "postgres(ql)?://" .planning/phases/122-*/ ` no devuelve nada
</verification>

<success_criteria>
Los planes 02/03/04 pueden ejecutar su auditoría sin volver a decidir método, vocabulario, sujetos ni
formato de tabla.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-01-SUMMARY.md` when done
</output>
