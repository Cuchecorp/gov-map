---
phase: 62-red-grafo-de-relaciones-entendible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/red/red-graph.tsx
  - app/components/red/red-graph.test.tsx
autonomous: true
requirements: [RED-01, RED-02]
must_haves:
  truths:
    - "Con seed, el grafo renderiza SOLO seed + vecinos directos capados a 24 (alfabético); el DOM tiene ≤ 25 nodos, nunca ~136."
    - "El layout es radial ego-céntrico determinista: seed en (0,0), vecinos en anillo(s) por ángulo = orden alfabético — cero force-simulation."
    - "Si el seed tiene >24 vecinos directos, aparece un control honesto 'Ver N vecinos más' con N = total − 24, cada nombre un Link a /red?seed=<id>."
    - "La leyenda 'Cómo leer este grafo' declara explícito 'La posición en el anillo es orden alfabético, no cercanía'; el scan de banned-vocab sigue verde."
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "radial layout + neighbor cap + 'N vecinos más' + leyenda reescrita"
      contains: "radialPos"
    - path: "app/components/red/red-graph.test.tsx"
      provides: "radial-determinism test + node-count cap test + banned-vocab leyenda scan; grid tests eliminados"
      contains: "orden alfabético"
  key_links:
    - from: "app/components/red/red-graph.tsx"
      to: "seedNeighbors (memo, alfabético)"
      via: "radialPos(index, total) sobre rendered = seedNeighbors.slice(0, 24)"
      pattern: "radialPos"
    - from: "app/components/red/red-graph.tsx"
      to: "leyenda"
      via: "copy 'orden alfabético, no cercanía'"
      pattern: "orden alfab"
---

<objective>
Reemplazar el layout de rejilla por-cámara de `red-graph.tsx` por un layout radial ego-céntrico determinista, y capar los vecinos renderizados a 24 en orden alfabético con un control honesto "Ver N vecinos más". Reescribir la leyenda "Cómo leer este grafo" para describir el anillo radial (posición = orden alfabético, no cercanía). Reescribir los tests del layout (Wave 0) para asertar el nuevo contrato antes de escribir el código.

Purpose: Con seed "Jorge Alessandri Vergara" hoy se renderizan ~136 nodos apiñados en una franja horizontal ilegible. El ego-network real es seed + vecinos directos, y el layout debe ser legible sin implicar afinidad (F18 LOCKED: nunca force-simulation). Esto cierra RED-01 (ego-network real + cap honesto) y el núcleo de RED-02 (layout radial determinista + leyenda actualizada).

Output: `red-graph.tsx` con `radialPos()` en lugar de `posicion()`, lista de vecinos capada+ordenada, control "N vecinos más", leyenda reescrita; `red-graph.test.tsx` con tests de determinismo radial, conteo de nodos ≤ cap+1, y banned-vocab extendido a la leyenda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-CONTEXT.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-RESEARCH.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-PATTERNS.md

<interfaces>
<!-- Contratos que el ejecutor necesita — extraídos del código shipped. No explorar el codebase. -->

From app/components/red/red-graph.tsx (contratos que NO cambian):
```typescript
export interface SubgrafoNodo { id: string; nombre: string | null; camara: string | null; }
export interface SubgrafoArista {
  tipo: string; a: string; b: string; contexto: string | null;
  desde: string | null; hasta: string | null; dataset: string;
  origen: string; enlace: string; licencia: string | null;
}
export interface Subgrafo { nodos: SubgrafoNodo[]; aristas: SubgrafoArista[]; }
export interface RedGraphProps { subgrafo: Subgrafo | null; seedId?: string; }
```

From app/lib/format (ya usado en page.tsx L4 y nodo-parlamentario.tsx L5):
```typescript
export function formatNombre(nombre: string): string; // re-casea display-only
```

Función ACTUAL a reemplazar (red-graph.tsx L112-122):
```typescript
function posicion(laneIndex: number, camara: string | null): { x: number; y: number } { … } // rejilla — ELIMINAR
```

Geometría radial objetivo (RESEARCH.md Pattern 1 — trig pura, cero física):
- Seed en {x:0, y:0}, esSeed:true.
- radialPos(index, total): perRing=12, ring 0 R≈260 / ring 1 R≈460; theta = -Math.PI/2 + 2π·inRing/max(countInRing,1) (12 en punto, horario); Math.round en x,y.
- rendered = seedNeighbors.slice(0, CAP=24); overflow = seedNeighbors.slice(24).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescribir los tests del layout (Wave 0) — determinismo radial + cap + banned-vocab</name>
  <read_first>
    - app/components/red/red-graph.test.tsx (mock de @xyflow/react L37-104, ResizeObserver shim L25-33, helpers dos_nodos/arista L108-129; tests a ELIMINAR: "layout por carril" L393-427)
    - app/components/red/red-graph.tsx (contrato de props, seedId, aristasVisibles, rfNodes — para saber qué asertar)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-RESEARCH.md (§Pattern 1 radial, §Validation Architecture Wave 0 Gaps, §Pitfall 3 mock xyflow)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Radial Layout Contract, §Copywriting banned vocabulary, §Acceptance Hooks)
  </read_first>
  <action>
    Reescribir el bloque de tests de layout en `red-graph.test.tsx` reutilizando VERBATIM el mock `vi.mock("@xyflow/react")` existente (expone `data-x`/`data-y` por nodo y `data-fitview-nodes`) y el `ResizeObserver` shim. NO tocar los tests de nodo sobrio, arista=hecho, provenance, filtros ni estado-honesto (siguen pasando).
    (a) ELIMINAR el describe "layout por carril es determinista y separa cámaras" (L393-427) — la rejilla ya no existe.
    (b) NUEVO test "layout radial determinista y alfabético": con un subgrafo seed D0001 + 3 vecinos cuyos nombres desordenados en el array de entrada (p.ej. "Carlos", "Ana", "Bruno"), asertar que el nodo seed está en `data-x="0" data-y` cercano a 0, y que las posiciones de los vecinos son función pura del índice ALFABÉTICO (Ana antes que Bruno antes que Carlos): el vecino alfabético 0 cae ≈ 12 en punto (x≈0, y≈-260, es decir data-y negativo con |x| pequeño). Segundo aserto de determinismo: reordenar el array de nodos de entrada NO cambia el `data-x`/`data-y` de ningún nodo (mismo set → mismas posiciones).
    (c) NUEVO test "conteo de nodos ≤ cap+1 (RED-01)": construir un subgrafo con seed D0000 y 30 vecinos directos (30 aristas seed↔vecino). Asertar que `container.querySelectorAll('[data-testid^="rf-node-"]').length === 25` (24 vecinos + seed), y que existe un control con texto que matchea `/Ver\s+6\s+vecinos más/i` (30 − 24 = 6). Cada nombre en overflow debe ser un `<a>`/Link con `href` que matchee `/\/red\?seed=/`.
    (d) EXTENDER el scan de banned-vocab: sobre el `container.textContent` del render CON leyenda, asertar que NO matchea `/afinidad|cercan[íi]a|aliado|red de poder/i`, ADEMÁS de los `/partido/i` y `/puntaje|score|ranking/i` ya existentes. Asertar que SÍ aparece la copy literal "orden alfabético" y "no cercanía" en la leyenda.
    Estos tests DEBEN fallar (RED) contra el código actual — la Task 2 los pone en verde.
  </action>
  <verify>
    <automated>cd app && pnpm test -- red-graph 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `red-graph.test.tsx` ya NO contiene el string "layout por carril" ni "BANDA_SENADO".
    - Existe un test que asierta determinismo radial (posición = función del índice alfabético, invariante al orden del array de entrada).
    - Existe un test que asierta `[data-testid^="rf-node-"]` count === 25 con 30 vecinos, y un control "Ver 6 vecinos más" con Links a `/red?seed=`.
    - El scan de banned-vocab incluye `/afinidad|cercan[íi]a|aliado|red de poder/i` y asierta presencia de "orden alfabético" + "no cercanía".
    - `pnpm test -- red-graph` muestra estos tests nuevos en ROJO (el código aún no implementa radial/cap) — RED confirmado.
  </acceptance_criteria>
  <done>Los tests de layout reescritos existen y fallan contra el código actual (RED); el resto de la suite red-graph sigue verde salvo los tests de layout viejos que fueron reemplazados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar radial + cap + "N vecinos más" + leyenda reescrita en red-graph.tsx</name>
  <read_first>
    - app/components/red/red-graph.tsx (TODO el archivo: docblock anti-insinuación L34-46, posicion() L112-122 a reemplazar, aristasVisibles L156-168, nodosVisibles L197-208, rfNodes L214-230, egoIds/fitViewOptions L237-242, leyenda L263-286, canvas mount L343-359)
    - app/components/red/red-graph.test.tsx (los tests de la Task 1 que deben pasar)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-RESEARCH.md (§Code Examples "Building the capped, sorted neighbor list" + §Pattern 1)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Radial Layout Contract, §Copywriting Contract leyenda + "Neighbor cap / truncation")
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-PATTERNS.md (§red-graph.tsx pattern assignments)
  </read_first>
  <behavior>
    - Con seed y ≤24 vecinos: se renderizan seed + todos los vecinos, posición radial pura por índice alfabético.
    - Con seed y >24 vecinos: se renderizan seed + 24 (alfabéticos); control "Ver {total−24} vecinos más" lista los nombres restantes como Links a /red?seed=<id>.
    - Reordenar el array `nodos` de entrada no cambia ninguna posición (determinismo F18).
    - Leyenda declara "La posición en el anillo es orden alfabético, no cercanía".
    - Estado vacío (0 aristas) y filtros siguen byte-idénticos.
  </behavior>
  <action>
    En `red-graph.tsx`:
    (a) Importar `formatNombre` desde `@/lib/format` (junto a los imports existentes).
    (b) ELIMINAR la función `posicion(laneIndex, camara)` (L112-122) y AÑADIR las constantes `RING1_R = 260`, `RING2_R = 460`, `CAP = 24` y la función `radialPos(index, total)` con: `perRing = 12`; `ring = index < perRing ? 0 : 1`; `R = ring === 0 ? RING1_R : RING2_R`; `inRing = ring === 0 ? index : index - perRing`; `countInRing = ring === 0 ? Math.min(total, perRing) : total - perRing`; `theta = -Math.PI/2 + (2*Math.PI*inRing)/Math.max(countInRing,1)`; retorna `{ x: Math.round(R*Math.cos(theta)), y: Math.round(R*Math.sin(theta)) }`.
    (c) Tras `aristasVisibles` (L168) y ANTES de construir rfNodes, añadir un `useMemo` `seedNeighbors`: recorre `aristasVisibles`, junta el OTRO extremo de cada arista que toca `seedId` (si `a.a===seedId` añade `a.b`, si `a.b===seedId` añade `a.a`), filtra `nodos` por esos ids, y ordena por `formatNombre(n.nombre ?? n.id).localeCompare(otro, "es")`. Deps: `[aristasVisibles, nodos, seedId]`. Definir `const rendered = seedNeighbors.slice(0, CAP)` y `const overflow = seedNeighbors.slice(CAP)`.
    (d) Reemplazar el mapeo `rfNodes` (L214-230, incluyendo el `laneCounters` por carril que se ELIMINA): construir el nodo seed en `position:{x:0,y:0}` con `esSeed:true`, y mapear `rendered` con `position: radialPos(i, rendered.length)` y `esSeed:false`. El set de nodos renderizados es exactamente `[seed, ...rendered]`. IMPORTANTE fallback: cuando `seedId` es undefined o el seed no participa de ninguna arista visible (caso legacy sin seed), conservar el comportamiento previo NO-radial mínimo — usar el filtro `nodosVisibles` existente con `radialPos` sobre el índice de llegada, para no romper el render sin-seed; el foco de la fase es el path CON seed. Documentar esa rama con un comentario.
    (e) Las aristas: mantener `rfEdges` mapeando SOLO las aristas cuyos ambos extremos estén en el set renderizado (`[seed, ...rendered]`), para no dibujar líneas a nodos capados. Conservar el `id`, `type:"hecho"`, y todo el `data` byte-idéntico.
    (f) Reescribir el docblock anti-insinuación L34-46: cambiar SOLO la cláusula de layout de "rejilla determinista agrupada por cámara" a "layout radial ego-céntrico determinista: la posición en el anillo es orden alfabético, no cercanía — jamás una simulación física". Mantener las otras 4 invariantes byte-idénticas.
    (g) Reescribir el tercer `<li>` de la leyenda (L277-280) con la copy LOCKED de UI-SPEC: "La posición en el anillo es orden alfabético, no cercanía: el parlamentario elegido va al centro y sus vecinos se ordenan alfabéticamente alrededor. La distancia o el ángulo no indican afinidad ni relación entre las personas." Mantener los `<li>` de Nodo y Línea y la línea de fuente (Ley 20.730) byte-idénticos.
    (h) Añadir, DEBAJO del canvas (dentro del bloque `aristasVisibles.length > 0`), el control honesto de overflow cuando `overflow.length > 0`: un contenedor con la copy "Se muestran los primeros 24 vecinos en orden alfabético." + "Ver {overflow.length} vecinos más" y una lista donde cada `overflow` es `<Link href={`/red?seed=${n.id}`}>{formatNombre(n.nombre ?? n.id)}</Link>` (min-height 44px por fila vía clase existente). NO usar force-simulation, NO ordenar por peso.
    Mantener `fitView`, `minZoom={0.2}`, `proOptions={{ hideAttribution: true }}`, las clases `.net-lienzo mt-4 h-96 md:h-120` y el `ReactFlowProvider` byte-idénticos.
  </action>
  <verify>
    <automated>cd app && pnpm test -- red-graph && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `red-graph.tsx` ya NO contiene `function posicion(` ni `laneCounters`; contiene `function radialPos(` y `const CAP = 24`.
    - Con un seed de 30 vecinos, el set renderizado a rfNodes tiene exactamente 25 nodos y `overflow.length === 6` produce "Ver 6 vecinos más" con Links `/red?seed=`.
    - El seed va en `{x:0,y:0}`; el vecino alfabético 0 cae en x≈0, data-y negativo (12 en punto).
    - La leyenda contiene literal "orden alfabético" y "no cercanía"; el docblock ya no dice "rejilla … por cámara".
    - `pnpm test -- red-graph` VERDE (todos los tests de la Task 1 pasan) y `pnpm typecheck` sin errores.
  </acceptance_criteria>
  <done>El grafo con seed renderiza ego-network radial capado a 24 con "N vecinos más" honesto y leyenda reescrita; suite red-graph verde, typecheck verde; cero force-simulation, cero DDL, cero cambios a page.tsx/RPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → client render | `?seed=<id>` ya validado server-side con `PARLAMENTARIO_ID_RE` en page.tsx (no se toca); el overflow genera hrefs `/red?seed=<id>` con ids ya PII-safe del subgrafo. |
| Edge provenance `enlace` → DOM link | `safeExternalHref` filtra `javascript:` (en arista-hecho.tsx, no se toca). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure | overflow list hrefs | accept | Los ids provienen del subgrafo `subgrafo_red` (PII-safe: solo id/nombre/camara); el href `/red?seed=<id>` re-entra por el gate + `PARLAMENTARIO_ID_RE` de page.tsx. Sin superficie nueva. |
| T-62-02 | Tampering | banned-vocab / anti-insinuación en leyenda | mitigate | El scan de banned-vocab en el test (`/afinidad|cercan[íi]a|aliado|red de poder/i`) bloquea regresión de copy que implique afinidad. |
</threat_model>

<verification>
- `pnpm test -- red-graph` verde (radial-determinism, node-count cap ≤25, banned-vocab, más los tests de nodo/arista/provenance/filtros/estado-honesto preexistentes).
- `pnpm typecheck` verde.
- Grep: `red-graph.tsx` no contiene `posicion(` ni `laneCounters`; sí contiene `radialPos` y `CAP = 24` y `orden alfabético`.
</verification>

<success_criteria>
Con seed, el DOM del grafo tiene ≤ 25 nodos (24 vecinos alfabéticos + seed), nunca ~136; layout radial determinista sin force-simulation; "Ver N vecinos más" honesto cuando >24; leyenda declara "orden alfabético, no cercanía". Suite + typecheck verdes.
</success_criteria>

<output>
Create `.planning/phases/62-red-grafo-de-relaciones-entendible/62-01-SUMMARY.md` when done
</output>
