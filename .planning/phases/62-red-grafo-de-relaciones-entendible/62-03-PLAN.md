---
phase: 62-red-grafo-de-relaciones-entendible
plan: 03
type: execute
wave: 3
depends_on: [62-01, 62-02]
files_modified:
  - .planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/
autonomous: false
requirements: [RED-03]
user_setup: []
must_haves:
  truths:
    - "Existe captura 'antes' 390px con seed del /red live actual (el screenshot desktop del operador es el 'antes' desktop)."
    - "Tras deploy (runbook 61-02), existen capturas 'después' de /red con seed y sin seed, en desktop y 390px."
    - "La lectura fría BrowserOS da veredicto 'comprensible'; los hallazgos P0/P1 se corrigen y se re-capturan."
    - "La evidencia before/after queda archivada en red-evidence/ y la suite completa + build siguen verdes."
  artifacts:
    - path: ".planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/"
      provides: "capturas antes/después (seed+no-seed × desktop+390px) + veredicto cold-read"
    - path: ".planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/VEREDICTO.md"
      provides: "veredicto de lectura fría con hallazgos P0/P1 y su resolución"
      contains: "comprensible"
  key_links:
    - from: "deploy (61-02 runbook)"
      to: "https://observatorio-congreso.thevalis.workers.dev/red"
      via: "Docker node:22-slim + wrangler 4 global OAuth"
      pattern: "workers.dev/red"
    - from: "scripts/bros-cli.mjs"
      to: "red-evidence/*.png"
      via: "open → wait 5s → shot (retry once) → close"
      pattern: "bros-cli"
---

<objective>
Cerrar RED-03: correr el loop BrowserOS de la fase 61 (captura antes → deploy → re-captura después → lectura fría) sobre `/red`, en desktop y 390px, con seed y sin seed. Capturar el estado "antes" que falta (390px con seed), desplegar el fix de 62-01+62-02 con el runbook 61-02, re-capturar el "después", emitir un veredicto de comprensibilidad, corregir hallazgos P0/P1 con re-captura, y archivar toda la evidencia en `red-evidence/`.

Purpose: El criterio de cierre de la fase NO es la opinión del implementador sino la lectura fría (queja literal del operador: "todo se ve muy confuso"). Este plan es el gate humano de comprensión sobre el deploy real, donde la legibilidad de etiquetas sin zoom y la usabilidad a 390px solo se prueban de verdad.

Output: `red-evidence/` con capturas antes/después (seed+no-seed × desktop+390px) + `VEREDICTO.md` con el veredicto "comprensible" y la resolución de hallazgos; /red live actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-CONTEXT.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-01-SUMMARY.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-02-SUMMARY.md
@.planning/milestones/v6.0-phases/61-comp-comprensi-n-de-visualizaciones-loop-browseros/61-02-SUMMARY.md
@scripts/bros-cli.mjs

<interfaces>
<!-- Runbook y comandos extraídos de 61-02-SUMMARY y bros-cli.mjs — no re-descubrir. -->

Deploy (61-02 runbook, LOCKED):
- Build en Docker `node:22-slim` (NO alpine — workerd necesita glibc).
- Pre-copiar source a `C:/Temp/obs-build` (evitar bottleneck virtiofs OneDrive), excluir node_modules.
- Extraer `.open-next` del volumen Docker a `app/.open-next` (docker cp).
- Deploy con wrangler 4 GLOBAL OAuth desde `app/`:
  node "C:/Users/Carlo/AppData/Roaming/npm/node_modules/wrangler/bin/wrangler.js" deploy --config wrangler.jsonc
- CI (GitHub Actions) BLOQUEADO: falta CLOUDFLARE_API_TOKEN en Cuchecorp/gov-map → usar path local.
- URL: https://observatorio-congreso.thevalis.workers.dev
- Nota gate: /red requiere NET_PUBLIC_ENABLED=true en runtime; si el flag está OFF, /red devuelve 404 (no es bug de este plan — es el gate F17). Verificar que el flag esté ON antes de capturar; si está OFF, es un gate humano fuera de alcance (documentar y detener en checkpoint).

BrowserOS cold-read (scripts/bros-cli.mjs — páginas OCULTAS, path Windows forward-slash, esperar 5s, reintentar 1 vez):
  PID=$(node scripts/bros-cli.mjs open "<url>" | grep -oE "Page ID: [0-9]+" | grep -oE "[0-9]+")
  sleep 5
  node scripts/bros-cli.mjs shot "$PID" "C:/…/red-evidence/<nombre>.png" || (sleep 3; node scripts/bros-cli.mjs shot "$PID" "C:/…/<nombre>.png")
  node scripts/bros-cli.mjs close "$PID"
- Para 390px: usar el tool MCP de resize/viewport si bros-cli lo expone (`node scripts/bros-cli.mjs tools` para verificar); si no, documentar el método de captura móvil usado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar "antes" 390px + verificar suite/build, luego deploy con runbook 61-02</name>
  <files>.planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/</files>
  <read_first>
    - .planning/milestones/v6.0-phases/61-comp-comprensi-n-de-visualizaciones-loop-browseros/61-02-SUMMARY.md (runbook deploy completo, deviations)
    - scripts/bros-cli.mjs (header L1-28: gotchas, atajos open/shot/close)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-01-SUMMARY.md y 62-02-SUMMARY.md (qué cambió, para saber qué mirar en el cold-read)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Acceptance Hooks — qué hace "comprensible")
  </read_first>
  <action>
    (a) Crear el directorio `.planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/`. Guardar/copiar el screenshot del operador (desktop con seed) como `red-seed-desktop-antes.png` si está disponible; documentar su origen si es una referencia externa.
    (b) ANTES de deployar, capturar el "antes" 390px con seed desde el /red LIVE ACTUAL (todavía sin el fix): `red-seed-movil-antes.png`. También capturar el "antes" sin-seed desktop (`red-noseed-desktop-antes.png`) si el selector actual ya orienta — sirve de baseline. Usar el patrón bros-cli (páginas ocultas, esperar 5s, reintentar). Verificar primero que /red LIVE responde 200 (flag NET_PUBLIC_ENABLED ON); si devuelve 404, DETENER y escalar en el checkpoint (gate F17 humano fuera de alcance).
    (c) Verificar verde local ANTES de build: `cd app && pnpm test` (suite completa) y `pnpm build` (next build) — ambos deben pasar. Si algo está rojo, no deployar.
    (d) Deployar con el runbook 61-02 EXACTO: pre-copiar source a `C:/Temp/obs-build` excluyendo node_modules; build `.open-next` en Docker `node:22-slim`; docker cp a `app/.open-next`; `node "C:/Users/Carlo/AppData/Roaming/npm/node_modules/wrangler/bin/wrangler.js" deploy --config wrangler.jsonc` desde `app/`. Registrar el Version ID del deploy.
    (e) Verificar LIVE post-deploy: `/red?seed=<id-real>` devuelve 200 y su HTML contiene la leyenda nueva ("orden alfabético"). Guardar el id usado.
  </action>
  <verify>
    <automated>cd app && pnpm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Existe `red-evidence/` con al menos `red-seed-desktop-antes.png` (o su referencia documentada) y `red-seed-movil-antes.png`.
    - `pnpm test` (suite completa) y `pnpm build` verdes ANTES del deploy (registrado en el summary).
    - El deploy imprime un Version ID nuevo; `curl`/fetch a `https://observatorio-congreso.thevalis.workers.dev/red?seed=<id>` devuelve 200 y el HTML contiene "orden alfabético".
    - Si /red devolvía 404 (flag OFF), el plan se detiene documentando el gate F17 — NO se flipea ningún flag.
  </acceptance_criteria>
  <done>Evidencia "antes" capturada, suite+build verdes, deploy live con Version ID registrado y leyenda nueva confirmada en el HTML servido.</done>
</task>

<task type="auto">
  <name>Task 2: Re-capturar "después" + lectura fría + veredicto (corregir P0/P1 con re-captura)</name>
  <files>.planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/</files>
  <read_first>
    - scripts/bros-cli.mjs (atajos open/shot/content/close)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Acceptance Hooks 1-4: RED-01 conteo, RED-02 legibilidad+leyenda+móvil, anti-insinuación)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/ (las capturas "antes" de la Task 1)
  </read_first>
  <action>
    (a) Con el deploy live, capturar el "después" en 4 combinaciones: `red-seed-desktop-despues.png`, `red-seed-movil-despues.png`, `red-noseed-desktop-despues.png`, `red-noseed-movil-despues.png`. Usar el patrón bros-cli (páginas ocultas, esperar 5s, reintentar 1 vez). Para 390px usar el método de viewport verificado en la Task 1.
    (b) Lectura fría de cada captura contra los Acceptance Hooks de UI-SPEC: con seed → ¿se ve el ego-network (seed centrado + anillo de vecinos alfabéticos, ≤24 + "N vecinos más"), etiquetas legibles SIN zoom, leyenda declara "orden alfabético, no cercanía"? En 390px → ¿lista de vecinos usable con enlaces + fuente, no un anillo apiñado? Sin seed → ¿explicación + selector prominente, NUNCA el grafo completo? Anti-insinuación: sin petróleo en nodos, sin partido/score, procedencia en el DOM.
    (c) Escribir `red-evidence/VEREDICTO.md`: tabla antes/después por combinación, veredicto por superficie ("comprensible" / hallazgo), y lista de hallazgos clasificados P0 (rompe comprensión) / P1 (fricción notable) / P2 (menor, diferible).
    (d) Corregir los hallazgos P0/P1 que sean de código o CSS (p.ej. anillo aún apretado → subir radio/bajar cap; etiqueta ilegible → ajustar geometría; sin salir de `app/components/red/` + `globals.css`, respetando F18 y los floors de tipografía 14px/12px). Tras corregir: re-correr `pnpm test -- red-graph`, re-deployar con el runbook, y RE-CAPTURAR la superficie afectada (sufijo `-v2.png`). Repetir hasta que P0/P1 estén resueltos y el veredicto global sea "comprensible".
    (e) Actualizar `VEREDICTO.md` con el estado final y marcar los P2 diferidos (si los hay) como deuda documentada, no bloqueante.
  </action>
  <verify>
    <human-check>
      Revisar red-evidence/VEREDICTO.md y las capturas -despues: ¿el veredicto global es "comprensible" en las 4 combinaciones (seed+no-seed × desktop+390px)? ¿Los P0/P1 están resueltos con re-captura? ¿El anillo con seed muestra ego-network legible (no franja de 136)? ¿La lista móvil es usable a 390px?
    </human-check>
  </verify>
  <acceptance_criteria>
    - `red-evidence/` contiene las 4 capturas `-despues` (más las `-v2` de correcciones si hubo P0/P1).
    - `VEREDICTO.md` existe, contiene la palabra "comprensible" como veredicto global y una tabla antes/después.
    - Todo hallazgo P0/P1 tiene resolución + re-captura; los P2 (si hay) quedan documentados como diferibles.
    - `pnpm test -- red-graph` verde tras cualquier corrección.
  </acceptance_criteria>
  <done>Lectura fría BrowserOS emite veredicto "comprensible" en las 4 combinaciones; hallazgos P0/P1 corregidos y re-capturados; evidencia before/after archivada en red-evidence/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — aprobación humana del grafo comprensible en producción</name>
  <action>Pausar para verificación humana sobre el deploy live: presentar el veredicto de lectura fría y las capturas antes/después, y esperar la señal "aprobado" antes de cerrar la fase. Detalle en what-built / how-to-verify / resume-signal abajo.</action>
  <what-built>El grafo /red pasó de franja apiñada de ~136 nodos a ego-network radial (seed + ≤24 vecinos alfabéticos + "N vecinos más" honesto) en desktop, lista de vecinos con enlaces en 390px, cámara por borde institucional, leyenda "orden alfabético, no cercanía". Desplegado live y validado por lectura fría BrowserOS con evidencia antes/después.</what-built>
  <how-to-verify>
    1. Abrir https://observatorio-congreso.thevalis.workers.dev/red (sin seed): debe mostrar explicación + selector de parlamentario prominente, NUNCA un grafo completo.
    2. Elegir un parlamentario muy conectado (p.ej. "Jorge Alessandri Vergara") → /red?seed=<id>: el grafo debe mostrar el seed al centro + un anillo de vecinos alfabéticos (≤24), etiquetas legibles SIN zoom, y "Ver N vecinos más" si tiene más de 24. NO una franja horizontal apiñada.
    3. En móvil/390px (o DevTools responsive): con seed debe verse una LISTA de vecinos con enlaces + hecho + fuente, no un anillo encogido.
    4. Revisar la leyenda "Cómo leer este grafo": debe decir "La posición en el anillo es orden alfabético, no cercanía".
    5. Revisar `.planning/phases/62-red-grafo-de-relaciones-entendible/red-evidence/VEREDICTO.md` y las capturas antes/después.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si el grafo es comprensible en las 4 combinaciones, o describe los hallazgos que faltan corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy → producción | Deploy vía wrangler 4 OAuth local (61-02); NET_PUBLIC_ENABLED es gate F17 humano — este plan NO lo flipea. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05 | Elevation of Privilege | flag NET_PUBLIC_ENABLED | accept | El plan NUNCA flipea flags `*_PUBLIC_ENABLED` (gate legal F17); si /red está OFF, se detiene y escala. Solo lee el estado live. |
| T-62-SC | Tampering | npm/docker deploy build | accept | Sin instalación de paquetes nuevos (RESEARCH: "installs no external packages"); build reusa deps shipped; no aplica gate slopcheck. |
</threat_model>

<verification>
- red-evidence/ con capturas antes/después (seed+no-seed × desktop+390px) + VEREDICTO.md "comprensible".
- Deploy live con Version ID; /red?seed=<id> 200 con leyenda nueva.
- Suite completa + build verdes pre-deploy; red-graph verde tras cualquier corrección P0/P1.
</verification>

<success_criteria>
Lectura fría BrowserOS da veredicto "comprensible" en /red con seed y sin seed, en desktop y 390px; hallazgos P0/P1 corregidos con re-captura; evidencia before/after archivada; deploy live con runbook 61-02; cero flags flipeados.
</success_criteria>

<output>
Create `.planning/phases/62-red-grafo-de-relaciones-entendible/62-03-SUMMARY.md` when done
</output>
