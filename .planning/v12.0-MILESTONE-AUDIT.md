---
milestone: v12.0
name: Validación general producto-a-producto
phases: 113-125
audited: 2026-07-29
auditor: Claude Opus 5 (milestone auditor, stance FORCE, goal-backward)
status: tech_debt
requisitos_totales: 13
requisitos_cerrados_literalmente: 11
requisitos_cerrados_por_declaración: 2
gaps_de_cadena: 2
deuda_operador_viva: 9
deuda_técnica_viva: 5
---

# Auditoría de cierre · Milestone v12.0

> **NOTA DE PERSISTENCIA (agente que escribió este archivo, 2026-07-29).** El auditor de milestone es
> un subagente read-only por diseño y **no pudo escribir su propio informe**. Lo que sigue es el
> contenido que el orquestador transmitió **verbatim**. El **cuerpo de las secciones §1 a §11 llegó
> truncado**: el orquestador entregó el veredicto global completo (con su tabla de cinco
> re-mediciones) y, en lugar de las once secciones, el **índice estructural** de ellas. No se
> fabricaron secciones para rellenar el hueco — la instrucción explícita era *«prefiero un audit con
> una nota de "cuerpo truncado, ver transcripción" que un audit fabricado»*. **Cuerpo truncado: ver
> la transcripción de la conversación de cierre del milestone v12.0.** Los hallazgos accionables del
> audit **sí** quedaron persistidos, en sus destinos: las dos filas huérfanas en
> `phases/125-e2e-pasada-final-producto-a-producto/125-HANDOFF-HUMANO.md` §5, las dos correcciones de
> trazabilidad en `REQUIREMENTS.md`, y el riesgo de migraciones no aplicadas en §7 de este archivo y
> en `STATE.md`.

## Veredicto global

**El milestone logró lo que se propuso, y su mayor logro es incómodo: dejó por escrito que el sitio muestra un número falso.** v12.0 no era construir; era comprobar que lo mostrado es cierto y arreglar o declarar lo que no. Eso ocurrió, con un nivel de disciplina de evidencia que verifiqué de forma independiente y que no pude falsar por ninguna vía.

Re-medí yo mismo, contra PROD y contra el deploy vivo, las cinco afirmaciones más caras del milestone. **Las cinco se sostienen al dígito**, incluidas las tres que dejan al proyecto peor parado de lo que estaría si nadie hubiera mirado:

| afirmación re-medida por mí | fuente | mi medición hoy |
|---|---|---|
| `net` con `USAGE` para `anon` **y** para `PUBLIC` ⇒ cadena SSRF abierta | 123 `OFF-6-03` / 124 §6 | `has_schema_privilege('anon','net','USAGE')` = **t**; `nspacl` conserva `=U/supabase_admin` **y** `anon=U` ⇒ **OFFENDER VIVO** |
| `pgtap` sigue en `public`, expuesto a `anon` | 123 `OFF-6-01` | `extnamespace` = **public**; **1.201** funciones de `public` ejecutables por `anon` ⇒ **OFFENDER VIVO** |
| `0078` aplicada pero el call-site sigue pasando 1.000 | 124 §1 `B-01` | clamp `least(coalesce(p_limit,20),4000)` **presente** en `votos_de_parlamentario`; `app/lib/parlamentario-resumen-conteos.ts:281` sigue con `p_limit: 1000` ⇒ **el DOM vivo dice `Ver detalle (1000)`** (lo extraje hoy del deploy) |
| `0074` aplicada (default ACL de `storage`) | 124 | `pg_default_acl` en `storage` con `anon=` ⇒ **0** ✓ |
| cero secdef sin `search_path` | 123 eje 5 | **0** de los secdef de `public` ⇒ ✓ |

Y el deploy está vivo y sirve los fixes: `/`, `/proyecto/14309-04`, `/parlamentario/D1165`, `/buscar` → **200**; el idiom de 117 (`según fuente al `) presente en `/proyecto/14309-04`.

**Por qué `tech_debt` y no `passed`:** dos ítems de la cadena de handoff se rompieron (§3), la tabla de trazabilidad de `REQUIREMENTS.md` está pre-marcada como completa en una fecha **anterior** a la ejecución (§2.1), `STATE.md` quedó desincronizado, y dos requisitos están cerrados **por declaración honesta**, no por corrección. Nada de eso está oculto — todo lo contrario — pero son cosas que hay que arreglar antes de archivar.

**Por qué no `gaps_found`:** ninguno de los hallazgos incómodos se suavizó. Verifiqué específicamente los seis que el prompt sospechaba, y los seis están escritos con nombre, número y dueño (§6).

## Cuerpo del informe (§1–§11) — **TRUNCADO EN TRANSMISIÓN**

El auditor produjo, y el orquestador describió, la siguiente estructura. **El texto íntegro de estas
secciones no llegó a este archivo** y no se reconstruye aquí:

- **§1** — el hallazgo rector.
- **§2** — cobertura de requisitos, con **§2.1** (la tabla de trazabilidad firmada el día de la definición, no del cumplimiento).
- **§3** — integración cross-fase, con **§3.1 / §3.2 / §3.3 / §3.4** (aquí viven los **2 gaps de cadena**: las filas `3.3` y `4-15` de 122 saliendo del milestone sin dueño).
- **§4** — los 6 `human_needed` adjudicados.
- **§5** — coherencia del cierre.
- **§6** — honestidad, con los 6 hallazgos incómodos.
- **§7** — regresión (incluye el riesgo de las migraciones escritas y no aplicadas; ver abajo).
- **§8** — deuda viva: **§8.1** operador (9 ítems), **§8.2** técnica (5+2).
- **§9** — los 11 gotchas que deben sobrevivir.
- **§10** — lo que NO se logró (9 ítems).
- **§11** — acciones antes de archivar (6 filas).

## §7 (extracto persistido) · Riesgo latente de archivado: migraciones escritas y NO aplicadas

> `0073` (default ACL de `supabase_admin`) y `0075` (revoke de `net`) quedaron **escritas y NO
> aplicadas** por falta de ownership (`postgres` no es superusuario ni miembro de `supabase_admin`).
> **Jamás se editan.** Si algún día se pueden aplicar, va una migración **nueva** (`0080`). Sin esa
> disciplina, un futuro `supabase db push` o un ledger reconciliado a ciegas produciría deriva.

## Acciones de agente ejecutadas por esta persistencia (2026-07-29)

| acción | destino | estado |
|---|---|---|
| persistir el audit | `.planning/v12.0-MILESTONE-AUDIT.md` | hecho (cuerpo §1–§11 truncado, declarado arriba) |
| recuperar las 2 filas huérfanas de 122 (`3.3`, `4-15`) | `125-HANDOFF-HUMANO.md` §5, con sus 5 campos | hecho |
| corregir `CRUCE-01` y `SUPA-01` a `Complete (parcial — declarado)` + re-fechar | `.planning/REQUIREMENTS.md` | hecho |
| sincronizar posición y status | `.planning/STATE.md` | hecho |
| anotar el riesgo de `0073`/`0075` | este archivo §7 + `STATE.md` | hecho |
