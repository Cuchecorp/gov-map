---
phase: 100-panel-p1c-landing-panel
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - app/components/panel-actualidad.tsx
  - app/components/panel-actualidad.test.tsx
autonomous: true
requirements: [PANEL-02]
must_haves:
  truths:
    - "El componente RSC PanelActualidad lee la RPC actualidad_senales_panel({p_tipo:null}) vía createServerSupabase() y lanza (throw) en error real — nunca ?? [] que fabrique 'sin datos'"
    - "Las filas se agrupan por tipo_senal y cada grupo se renderiza como una BentoTile con framing factual, cobertura declarada, fuente + fecha_max"
    - "Una fila con supresion_causa != null renderiza la causa VERBATIM (nunca lista vacía, nunca '0' mudo, nunca 'sin movimiento')"
    - "agrupacion_materia con materia='(sin materia)' se renderiza tal cual sin romper ni fabricar un tema"
    - "Los tests RTL sobre las vistas puras (*View) aseveran: framing de conteo, fuente+fecha, render de supresion_causa verbatim, tolerancia de '(sin materia)'"
  artifacts:
    - path: "app/components/panel-actualidad.tsx"
      provides: "RSC PanelActualidad + vistas puras testeables (split async/View espejo de actualidad-module.tsx)"
      min_lines: 80
      exports: ["PanelActualidad"]
    - path: "app/components/panel-actualidad.test.tsx"
      provides: "RTL sobre las vistas puras con fixtures SenalRow[] (activa / suprimida / (sin materia))"
      contains: "supresion_causa"
  key_links:
    - from: "app/components/panel-actualidad.tsx"
      to: "actualidad_senales_panel"
      via: "sb.rpc('actualidad_senales_panel', { p_tipo: null })"
      pattern: "actualidad_senales_panel"
    - from: "app/components/panel-actualidad.tsx"
      to: "createServerSupabase"
      via: "import de @/lib/supabase (service_role, server-only, Camino A)"
      pattern: "createServerSupabase"
---

<objective>
Crear `components/panel-actualidad.tsx`: un Server Component (RSC puro, near-clone de `actualidad-module.tsx`) que lee las señales precomputadas vía la RPC bounded `actualidad_senales_panel`, agrupa por `tipo_senal`, y renderiza cada señal como BentoTile con framing factual, cobertura declarada, fuente+fecha y supresión honesta. Más su test RTL sobre las vistas puras con fixtures.

Purpose: PANEL-02 — cada tile lleva fuente+fecha + estado vacío honesto; cero agregación on-read (la RPC ya devuelve conteo/fecha_max/cobertura_camara). El riesgo NO es técnico sino de honestidad: supresión verbatim, anti-ranking, tolerancia de degradación de datos.
Output: `panel-actualidad.tsx` (component) + `panel-actualidad.test.tsx` (RTL con fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-panel-p1c-landing-panel/100-PATTERNS.md
@.planning/phases/100-panel-p1c-landing-panel/100-UI-SPEC.md
@.planning/phases/98-senales-p1a-spike-de-datos/98-SPIKE-FINDINGS.md
@.planning/phases/100-panel-p1c-landing-panel/100-01-SUMMARY.md

@app/components/actualidad-module.tsx
@app/components/bento/bento-tile.tsx

<interfaces>
<!-- Contrato de datos de la RPC (0066) + helpers. Extraído del código. NO explorar el árbol. -->

SenalRow (9 columnas de 0066_actualidad_rpc.sql:32-42):
```typescript
interface SenalRow {
  tipo_senal: string;          // 'velocity'|'nuevos_ingresos'|'urgencias'|'agenda_citacion'|'agenda_sala'|'archivados'|'agrupacion_materia'
  ventana: string | null;      // '7d' | '30d' | 'futuras' | null
  conteo: number;
  cobertura_camara: string | null;  // 'C.Diputados' | 'Senado' | '(sin cámara)' | '2022-2026 (piso de corpus)' | null
  materia: string | null;      // '(sin materia)' tolerado
  cluster_id: number | null;
  fecha_max: string | null;    // timestamptz ISO
  supresion_causa: string | null;   // NULL = activa; texto = suprimida CON causa
  evidencia: Record<string, unknown>;  // jsonb (deserializado a objeto por supabase-js)
}
```
Nota: la RPC NO devuelve dataset/origen; derivar el label de fuente de tipo_senal/cobertura_camara (o extender la RPC = fuera de alcance; preferir derivar).

Imports (verbatim de actualidad-module.tsx:1-12):
- `import Link from "next/link"`
- `import { createServerSupabase } from "@/lib/supabase"` (service_role, server-only)
- `import { fechaCorta } from "@/lib/format"` (format.ts:21)
- `import { safeExternalHref } from "@/lib/utils"`
- `import { sourceLabel } from "@/lib/types"` (types.ts:755 — origen → "Cámara"/"Senado")
- `import { BentoTile } from "@/components/bento/bento-tile"`

tz Chile (dia-calendario.ts): `diaCalendarioCitacion(iso)` para agenda_citacion/agenda_sala (fecha date-only-midnight-UTC = día chileno; NUNCA Intl tz America/Santiago sobre esos campos). velocity/urgencias/archivados/nuevos_ingresos usan fechaCorta (timestamp real).

BentoTile: variants default/accent; spans 2|4|6 ONLY; asChild (Slot) para full-card.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear panel-actualidad.tsx (RSC fetch + vistas puras)</name>
  <read_first>
    app/components/actualidad-module.tsx (COMPLETO — es el near-clone: split async server component + vistas puras *View; header reglas A-E L24-42; fetch+throw #34; empty-state L153-156; civic bar L166-176; source-link L205-221; fechaValida L115-119).
    app/app/contraparte/[id]/page.tsx:104-114 (patrón .rpc(name,params) + throw).
    100-PATTERNS.md §"app/components/panel-actualidad.tsx (NEW)" + §"tz-Chile agenda dates" + §Shared Patterns (VERBATIM: SenalRow, TileSenal, civic bar, source-link, supresion strings, cobertura literals).
    100-UI-SPEC.md §Panel Layout §Tile anatomy §Honesty Contract §Copywriting Contract (títulos, framing por tipo, denylist).
    98-SPIKE-FINDINGS §3 (anti-ranking cross-cámara) + §Sección 1 (framing por señal).
  </read_first>
  <behavior>
    - `PanelActualidad()` async: llama `sb.rpc("actualidad_senales_panel", { p_tipo: null })`; si `error` → `throw new Error(...)`; agrupa `data` por `tipo_senal` en un Map; renderiza una BentoTile por grupo dentro de un fragmento (para montar bajo Suspense en page.tsx).
    - Fila con `supresion_causa != null` → renderiza la causa VERBATIM como cuerpo del tile (text-sm text-muted-foreground), + "en las fuentes consultadas al {fecha}" si hay fecha_max; NUNCA lista vacía, NUNCA "0" mudo, NUNCA "sin movimiento".
    - Fila activa → conteo en font-mono + framing factual por tipo (LOCKED del UI-SPEC: velocity="trámites en 7 días", urgencias="urgencias fechadas en 30 días", agenda="citaciones próximas", nuevos_ingresos="proyectos ingresados en 7 días" + "2022-2026 (piso de corpus)", archivados="movimientos de archivo o retiro fechados (30 días)", agrupacion_materia="proyectos en «{materia}»"); cobertura_camara declarada; fecha_max en font-mono. NUNCA "top/los más/la cámara más activa"; NUNCA ordenar cobertura_camara por conteo cross-cámara (T-52-13).
    - agrupacion_materia con materia='(sin materia)' → label verbatim, sin romper ni fabricar tema.
    - Barra cívica 3px OMITIDA cuando cámara es null (regla A); `bg-[var(--camara)]`/`bg-[var(--senado)]` (v4 form, NUNCA bare `-[--camara]`).
    - Cada tile con footer "Fuente: {label} · datos al {fecha_max}" (label derivado de tipo_senal/cobertura_camara); dato sin fuente trazable no se muestra standalone (regla E).
  </behavior>
  <action>
    Escribir `app/components/panel-actualidad.tsx` como RSC puro (SIN "use client"), copiando el idioma exacto de actualidad-module.tsx: declarar `interface SenalRow` (las 9 columnas del interfaces block), `export async function PanelActualidad()` con el fetch+throw+group, y vistas puras EXPORTADAS y testeables (`TileSenal`/subcomponentes `*View`) que reciben las filas ya agrupadas por props (el split async/View es lo que hace el componente testeable con fixtures sin tocar la DB). Constantes `TITULO`/`FRAMING` por tipo_senal (copy LOCKED del UI-SPEC §Copywriting). Reusar `fechaCorta` para señales con timestamp real y `diaCalendarioCitacion` para agenda. Barra cívica, chips (px-[9px] py-0.5 accent-product-soft), source-link (text-accent-product underline min-h-11) verbatim de los patrones. Tipografía SOLO whitelisteada (text-lg/text-[15px]/text-[13px]/text-sm/text-[11px]); cero-hex (tokens/[var(--token)]); dos pesos (400/600). Padding tile p-6. NO recomputar ninguna agregación on-read. NO "use client". Si el copy introduce una leyenda que NIEGA un término prohibido, exportarla como const y registrarla en NEGACIONES_LOCKED del guard ANTES de proseguir (coordinar con el guard del Plan 01; Pitfall 2). NO montar aún en page.tsx (Plan 03).
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run anti-insinuacion-guard bento-guards && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `panel-actualidad.tsx` existe, exporta `PanelActualidad` + al menos una vista pura testeable.
    - anti-insinuacion-guard verde SOBRE la superficie panel (el copy nuevo pasa el linter).
    - bento-guards verde (cero-hex + tipografía + bare-var) sobre panel-actualidad.tsx.
    - tsc --noEmit sin errores.
    - grep confirma: `sb.rpc("actualidad_senales_panel"` presente; `throw new Error` en la rama de error; CERO `?? []` que oculte error; `bg-[var(--camara)]` (nunca `bg-[--camara]`).
  </acceptance_criteria>
  <done>
    El componente lee la RPC precomputada, agrupa por tipo_senal, renderiza framing factual + supresión honesta + fuente/fecha, pasa los guards y tsc.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RTL panel-actualidad.test.tsx con fixtures SenalRow[]</name>
  <read_first>
    app/components/actualidad-module.tsx (cómo las vistas puras se testean con fixtures — mismo idioma).
    Cualquier *.test.tsx de componente existente en app/components/ como molde de render RTL (buscar con glob **/*.test.tsx).
    100-RESEARCH.md §Validation Architecture (PANEL-02 → panel-actualidad test) + §Wave 0 Gaps (fixtures activa/suprimida/(sin materia)).
    100-UI-SPEC.md §Tile anatomy §Copywriting (strings verbatim a asertar).
  </read_first>
  <behavior>
    - Fixture ACTIVA (velocity, conteo>0, cobertura_camara='C.Diputados', fecha_max timestamp) → render muestra el conteo, el framing "trámites en 7 días", la cobertura y la fecha; NO muestra "top"/"los más".
    - Fixture SUPRIMIDA (agenda_sala, supresion_causa="sin sesiones agendadas en las fuentes consultadas", conteo=0) → render muestra la causa VERBATIM; NO renderiza lista vacía ni un "0" mudo aislado.
    - Fixture (sin materia) (agrupacion_materia, materia='(sin materia)') → render muestra "(sin materia)" verbatim, no crashea, no fabrica tema.
    - Cada fixture activa lleva fuente + fecha en el render (footer/línea).
  </behavior>
  <action>
    Escribir `app/components/panel-actualidad.test.tsx` con `@testing-library/react` (render de las vistas PURAS exportadas, NO del async server component — evita tocar la DB). Definir fixtures `SenalRow[]` para los 3 casos (activa / suprimida / (sin materia)) usando los strings de supresión VERBATIM de 0065 ("sin sesiones agendadas en las fuentes consultadas", "sin datos frescos de esta fuente", "sin urgencias fechadas en la ventana") y los literales de cobertura ('C.Diputados','Senado','(sin cámara)','2022-2026 (piso de corpus)'). Asertar los behaviors de arriba con getByText/queryByText. Al menos un test que asevere ausencia de vocabulario prohibido en el render de un fixture activo (queryByText de "top"/"los más" = null).
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run panel-actualidad</automated>
  </verify>
  <acceptance_criteria>
    - `panel-actualidad.test.tsx` existe con ≥3 fixtures (activa / suprimida / (sin materia)).
    - Test de supresión asevera que supresion_causa se renderiza verbatim.
    - Test de (sin materia) asevera que el label se muestra sin crash.
    - Test de fuente+fecha presente en fixture activa.
    - Suite panel-actualidad verde.
  </acceptance_criteria>
  <done>
    Los tests RTL prueban el contrato de honestidad del render (framing, supresión, fuente+fecha, tolerancia de degradación); suite verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → browser bundle | service_role NUNCA debe llegar al cliente (import "server-only") |
| árbol público → DB | el árbol público solo puede leer la RPC allowlisted / tablas no-PII |
| señal → lector | supresión/conteo/cobertura que podría leerse como insinuación o dato fabricado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04 | Information Disclosure | fuga de service_role al bundle cliente | mitigate | PanelActualidad es RSC puro (sin "use client"); createServerSupabase importa "server-only" en supabase.ts |
| T-100-05 | Information Disclosure | lectura de tabla PII desde el árbol público | mitigate | El panel solo llama la RPC no-PII actualidad_senales_panel (counts/labels/fechas); lockdown-guard Block B escanea .from() PII |
| T-100-06 | Elevation of Privilege | RPC fuera de allowlist | accept | actualidad_senales_panel ya en PUBLIC_RPC_ALLOWLIST; lockdown-guard Direction-A lo verifica (verde) |
| T-100-07 | Repudiation | dato fabricado por `?? []` que oculta error de lectura | mitigate | throw en error real (#34); [] SOLO en path legítimo de 0 filas; supresión es la única ausencia legítima (con causa) |
| T-100-08 | Repudiation | ranking cross-cámara / conteo mudo / label de tema fabricado | mitigate | framing factual LOCKED; cobertura declarada sin orden por conteo; supresion_causa verbatim; '(sin materia)' tolerado — asertado por RTL |
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run panel-actualidad anti-insinuacion-guard bento-guards` verde.
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- grep: `?? []` no oculta errores; `sb.rpc("actualidad_senales_panel"` presente; `bg-[var(--camara)]` (no bare-var).
</verification>

<success_criteria>
El componente panel lee la RPC precomputada (cero agregación on-read), renderiza fuente+fecha + supresión honesta + framing anti-ranking, tolera '(sin materia)', y sus vistas puras están cubiertas por RTL (PANEL-02). Guards y tsc verdes.
</success_criteria>

<output>
Create `.planning/phases/100-panel-p1c-landing-panel/100-02-SUMMARY.md` when done
</output>
