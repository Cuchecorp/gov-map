---
phase: 97-auth-p0-spike-auth-on-workers-de-risk
plan: 03
type: execute
wave: 3
depends_on: ["97-02"]
files_modified:
  - .planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-SPIKE-EVIDENCE.md
  - .planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md
autonomous: false
requirements: [AUTH-01]

must_haves:
  truths:
    - "Sobre el deploy real, verifyOtp emite Set-Cookie sb-*-auth-token (evidencia curl/BrowserOS)"
    - "Tras expiry del access token, una request con el jar recibe un NUEVO Set-Cookie (refresh sobrevive OpenNext)"
    - "Dos cookie jars distintos NUNCA reciben la sesión del otro; sin cf-cache-status HIT en respuestas con Set-Cookie"
    - "Home + rutas existentes siguen 200; CSP conserva frame-ancestors 'none' + object-src 'none'"
    - "Si el deploy NO sostiene la sesión: fallback honesto + re-plan NOTIF-103 server-side documentado (SC4)"
  artifacts:
    - path: ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-SPIKE-EVIDENCE.md"
      provides: "Comandos curl reproducibles (send/verify/refresh/dos-jars) + pasos BrowserOS + veredicto por criterio"
      min_lines: 40
    - path: ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md"
      provides: "Rama condicional: si el spike falla, fallback honesto server-side + re-plan de NOTIF-103"
      min_lines: 15
  key_links:
    - from: "97-SPIKE-EVIDENCE.md"
      to: "https://observatorio-congreso.thevalis.workers.dev/spike-auth"
      via: "curl -c/-b jar + BrowserOS sobre el deploy real"
      pattern: "spike-auth"
    - from: "middleware.ts (deployed)"
      to: "Set-Cookie sb-*-auth-token"
      via: "getClaims refresca y re-emite cookie"
      pattern: "sb-.*-auth-token"
---

<objective>
Probar EMPÍRICAMENTE, sobre el deploy real, la pregunta central del spike (SC2): ¿una sesión Supabase Auth (OTP) emite `Set-Cookie` y el REFRESH de sesión sobrevive el pipeline OpenNext → Workers? Capturar evidencia reproducible (curl con cookie jar + BrowserOS) de: emisión de cookie, refresh tras expiry, aislamiento entre dos jars (anti cache-leak, Pitfall #4), Camino A intacto y CSP intacta. Entregar `97-SPIKE-EVIDENCE.md` como artefacto de éxito PRIMARIO. Y — condicionalmente — si el spike falla (build roto en Plan 02 o cookie no sobrevive), entregar `97-FALLBACK-NOTIF-103.md` con el fallback honesto y el re-plan de NOTIF-103 server-side (SC4).

Purpose: Este es el propósito entero de la fase — de-riskear auth-on-Workers ANTES de construir NOTIF (Phase 103). El veredicto (funciona / no funciona con fallback) cierra la fase igual.

Output: `97-SPIKE-EVIDENCE.md` (siempre) + `97-FALLBACK-NOTIF-103.md` (siempre presente; con contenido de fallback SOLO si el spike falla, o una nota "no aplica — spike verde" si pasa).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-RESEARCH.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-02-SUMMARY.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md

<interfaces>
<!-- Comandos de evidencia. Fuente: 97-RESEARCH.md §Validation Architecture (Test Map) + §Pitfall 4/5/6 + 97-VALIDATION.md §Manual-Only. -->
Deploy: https://observatorio-congreso.thevalis.workers.dev
Ruta de prueba: /spike-auth (send/verify server actions del Plan 01)
Cookie objetivo: `sb-<ref>-auth-token` (chunked .0/.1), HttpOnly; Secure; SameSite=Lax; Path=/

Comandos base (adaptar a la forma real de las server actions del Plan 01):
- send:    curl -i -c jarA.txt -X POST el endpoint de la action  con { email: <operador> }
- verify:  curl -i -c jarA.txt -b jarA.txt -X POST el endpoint  con { email, token: codigo del email }  -> assert Set-Cookie sb-...-auth-token
- refresh: tras expiry del access token, curl -i -b jarA.txt -c jarA.txt https://.../spike-auth  -> assert NUEVO Set-Cookie
- dos-jars: curl con jarB.txt VACIO -> NUNCA debe recibir la cookie de jarA; grep -i cf-cache-status en respuestas con Set-Cookie (no debe ser HIT)
- Camino A: curl -sI https://.../  y rutas clave -> 200
- CSP: curl -sI https://... | grep -i content-security-policy -> contiene frame-ancestors 'none' y object-src 'none'

Rate limit SMTP interno = 2 emails/hora -> el operador pega el codigo; probar refresh reusando la sesion ya creada (no requiere nuevo OTP).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Evidencia OTP + Set-Cookie + refresh sobre el deploy real (operador pega el código)</name>
  <read_first>
    - 97-DEPLOY-RUNBOOK.md (versión desplegada + veredicto del build del Plan 02)
    - 97-RESEARCH.md §Validation Architecture (Test Map: cookie emit, refresh, dos-jars) + §Pitfall 4 (cache-leak) + §Pitfall 6 (rate limit)
    - 97-VALIDATION.md §Manual-Only Verifications
    - 97-01-SUMMARY.md (forma exacta de las server actions send/verify)
  </read_first>
  <what-built>
    Claude prepara los comandos curl exactos en `97-SPIKE-EVIDENCE.md` y automatiza todo lo que puede (curl send que dispara el email, curl de refresh, dos-jars, curl -I de home y CSP). Lo único que Claude NO puede hacer: leer el código OTP del email del operador. El operador dispara el send, lee el código de SU email, y lo pega para el verify.
  </what-built>
  <how-to-verify>
    1. Claude ejecuta el curl `send` con el email del operador (dispara OTP). El operador confirma recepción del código numérico (si llega un LINK en vez de código, la plantilla no quedó en {{ .Token }}; volver a Plan 02 Task 1).
    2. El operador pega el código de 6 dígitos; Claude ejecuta el curl `verify` con cookie jar A y captura la respuesta: DEBE traer Set-Cookie sb-<ref>-auth-token (HttpOnly; Secure; SameSite=Lax).
    3. Claude ejecuta el curl de REFRESH (request con jar A tras expiry del access token; usar un access token de vida corta o esperar la expiración; NO requiere nuevo OTP): DEBE emitir un NUEVO Set-Cookie con token refrescado. Este es el criterio de éxito central.
    4. Claude ejecuta el test DOS-JARS: jar B vacío hace request, NUNCA recibe la cookie de jar A; grep -i cf-cache-status en respuestas con Set-Cookie NO debe ser HIT.
    5. BrowserOS: navegar /spike-auth en el deploy real, confirmar en el DOM/console que la sesión se refleja y que NO hay "Refused to connect ... CSP" (si el cliente llamara a Supabase desde el navegador).
    Todo se registra en `97-SPIKE-EVIDENCE.md` con veredicto POR criterio. Emails = solo del operador; NUNCA loguear el código ni el email en el doc de evidencia (redactar).
  </how-to-verify>
  <resume-signal>Escribir el veredicto: "cookie+refresh OK" (con los fragmentos de Set-Cookie redactados en el doc) o "falló en <paso>" describiendo el síntoma (cookie no emitida / refresh no sobrevive / cache HIT / CSP bloqueó). Ese veredicto determina la Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verificar Camino A intacto + CSP intacta + suite verde; consolidar 97-SPIKE-EVIDENCE.md</name>
  <files>.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-SPIKE-EVIDENCE.md</files>
  <read_first>
    - 97-RESEARCH.md §Validation Architecture (Camino A intacto, CSP intact) + §Pitfall 3 (connect-src)
    - app/next.config.ts (securityHeaders CSP array — confirmar object-src/frame-ancestors)
    - 97-SPIKE-EVIDENCE.md (parcial, de Task 1)
  </read_first>
  <action>
    Confirmar Camino A intacto sobre el deploy real: `curl -sI https://observatorio-congreso.thevalis.workers.dev/` y rutas clave existentes (p.ej. /parlamentarios, /agenda) devuelven 200 — el middleware con su matcher no rompió rutas existentes ni el plano service_role. Confirmar CSP intacta: `curl -sI` del deploy, el header content-security-policy contiene `frame-ancestors 'none'` y `object-src 'none'`; si Plan 01 amplió `connect-src` al origen exacto de Supabase (solo si se usó cliente de navegador), documentar el diff MÍNIMO; jamás se removió frame-ancestors/object-src. Correr la suite completa local (`pnpm --filter app test`, `pnpm -r --filter './packages/**' test`, `pnpm --filter app run typecheck`) + `pnpm audit` (registrar 0 vulnerabilidades altas). Consolidar `97-SPIKE-EVIDENCE.md`: tabla de veredicto por los 4 success criteria del ROADMAP, con evidencia (comandos + salidas redactadas de PII) para cada uno.
  </action>
  <verify>
    <automated>curl -sI https://observatorio-congreso.thevalis.workers.dev/ | grep -Eq "HTTP/.* 200" && curl -sI https://observatorio-congreso.thevalis.workers.dev/ | grep -i "content-security-policy" | grep -q "frame-ancestors 'none'" && curl -sI https://observatorio-congreso.thevalis.workers.dev/ | grep -i "content-security-policy" | grep -q "object-src 'none'" && cd app && pnpm test && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `curl -I /` y rutas clave existentes devuelven 200 (Camino A intacto; matcher no rompió rutas).
    - Header CSP del deploy contiene `frame-ancestors 'none'` Y `object-src 'none'`; cualquier cambio a connect-src está documentado y es mínimo.
    - Suite app + packages + tsc --noEmit verdes; `pnpm audit` sin vulnerabilidades altas (registrado).
    - `97-SPIKE-EVIDENCE.md` (≥40 líneas) tiene tabla de veredicto por los 4 success criteria con evidencia redactada de PII.
  </acceptance_criteria>
  <done>Camino A + CSP intactas verificadas sobre el deploy real; suite completa + tsc verdes; 97-SPIKE-EVIDENCE.md consolidado con veredicto por criterio.</done>
</task>

<task type="auto">
  <name>Task 3: Rama condicional de fallback — 97-FALLBACK-NOTIF-103.md (honesto si el spike falla)</name>
  <files>.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md</files>
  <read_first>
    - 97-DEPLOY-RUNBOOK.md (veredicto del build) + 97-SPIKE-EVIDENCE.md (veredicto de cookie/refresh)
    - 97-RESEARCH.md §Alternatives Considered (server-side-only auth) + §Assumptions Log (A1/A2/A4) + §Summary (fallback)
    - .planning/ROADMAP.md §Phase 103 (NOTIF depende de 97; qué asume el server-side puro)
  </read_first>
  <action>
    Escribir `97-FALLBACK-NOTIF-103.md` SIEMPRE (satisface SC4 como entregable condicional). Determinar la rama según los veredictos de Plan 02 (build) y Task 1 (cookie/refresh):
    (A) Si el spike PASÓ (build Edge OK + cookie emitida + refresh sobrevive + sin cache-leak): escribir una nota corta "Fallback NO requerido — spike verde" con el resumen de por qué NOTIF-103 puede asumir middleware+cookies en Workers, y qué exactamente quedó probado (para que Phase 103 no re-verifique).
    (B) Si el spike FALLÓ (build rechazó el middleware, o la cookie/refresh no sobrevivió OpenNext, o hubo cache-leak): escribir el FALLBACK HONESTO — (1) qué falló textualmente y en qué criterio; (2) el diseño server-side-puro de auth para NOTIF-103 (Route Handlers validan, service_role escribe con `auth.uid()` derivado server-side, sin middleware/cookie-refresh en Workers) por §Alternatives Considered; (3) el re-plan concreto de las tareas de NOTIF-103 bajo ese modelo; (4) qué de este spike se conserva (supabase-user.ts, la ruta de prueba) y qué se retira o marca como no-viable. JAMÁS proponer resucitar anon ni migrar a proxy.ts como "arreglo". Registrar en el SUMMARY qué rama se tomó.
  </action>
  <verify>
    <automated>test -s ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md" && grep -Eiq "spike verde|fallback|NOTIF" ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md" && ! grep -iq "proxy.ts" ".planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-FALLBACK-NOTIF-103.md"</automated>
  </verify>
  <acceptance_criteria>
    - `97-FALLBACK-NOTIF-103.md` existe y no está vacío (≥15 líneas si rama B; nota corta si rama A).
    - El documento declara explícitamente la rama tomada (verde / fallback) coherente con los veredictos de Plan 02 y Task 1.
    - Si rama B: incluye diseño server-side-puro + re-plan NOTIF-103 + qué se conserva del spike.
    - El documento NUNCA propone resucitar anon ni migrar a proxy.ts.
  </acceptance_criteria>
  <done>97-FALLBACK-NOTIF-103.md escrito con la rama correcta (verde o fallback honesto), coherente con los veredictos, sin proponer anon ni proxy.ts. SC4 satisfecho.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador/curl → Worker (prod) | sesión con cookies cruza el edge; el CDN podría cachear Set-Cookie |
| operador → email OTP | el código numérico es PII-adyacente; jamás se loguea |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-10 | Information Disclosure / Spoofing | CDN cachea Set-Cookie entre usuarios | mitigate | Test dos-jars sobre el deploy real (jar B nunca recibe la sesión de A; sin cf-cache-status HIT en respuestas con Set-Cookie). force-dynamic + @supabase/ssr cache headers. |
| T-97-11 | Information Disclosure (PII 21.719) | 97-SPIKE-EVIDENCE.md | mitigate | Email/código OTP/token REDACTADOS en el doc; nunca logueados; emails = solo del operador. |
| T-97-12 | Tampering | CSP relajada de más | mitigate | Verify automatizado confirma frame-ancestors 'none' + object-src 'none' presentes; solo connect-src pudo ampliarse al origen exacto, documentado. |
| T-97-13 | Denial of Service | auth rota shippeada silenciosa | mitigate | Rama de fallback honesto (Task 3): si algo falla, se documenta y NOTIF-103 se re-planea server-side puro; JAMÁS anon ni proxy.ts. |
</threat_model>

<verification>
- Set-Cookie sb-*-auth-token emitido en verify sobre el deploy real (evidencia).
- Refresh emite NUEVO Set-Cookie tras expiry (criterio central SC2).
- Dos-jars aislados; sin cf-cache-status HIT en respuestas con Set-Cookie.
- Camino A 200 + CSP con frame-ancestors/object-src intactos.
- Suite completa + tsc verdes; pnpm audit sin altas.
- 97-FALLBACK-NOTIF-103.md presente con la rama correcta (SC4).
</verification>

<success_criteria>
Cierra SC2 (Set-Cookie + refresh sobreviven OpenNext, evidenciado sobre el deploy real), refuerza SC3 (Camino A + CSP intactos, sin cache-leak) y satisface SC4 (fallback honesto + re-plan NOTIF-103 como entregable condicional). Con esto la Phase 97 cierra tenga el veredicto que tenga.
</success_criteria>

<output>
Create `.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-03-SUMMARY.md` when done.
</output>
