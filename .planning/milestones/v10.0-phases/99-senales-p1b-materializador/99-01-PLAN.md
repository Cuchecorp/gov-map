---
phase: 99-senales-p1b-materializador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0065_actualidad_senal.sql
  - supabase/tests/0065_actualidad_senal.test.sql
autonomous: true
requirements: [SEN-02, SEN-03, SEN-04]
must_haves:
  truths:
    - "actualidad_senal existe como tabla precomputada deny-by-default (RLS enabled, cero policies, revoke all anon/authenticated)"
    - "El proc actualidad.materializar_senales() es security definer, full-rebuild acotado a los tipos temporales, y aplica los 3 defectos LOCKED en cada agregación"
    - "Las señales factuales existen: velocity, nuevos_ingresos (2022-2026), urgencias, agenda_citacion, agenda_sala, archivados — cada una anclada a tramitacion_evento.fecha"
    - "Cuando una fuente está stale o agenda_sala no tiene futuras, se emite una FILA con supresion_causa (nunca ausencia-como-hecho)"
    - "pg_cron registra el job actualidad-materializar intradía L-V"
    - "Ninguna fila fecha>current_date alimenta una señal; camara se normaliza colapsando las dos grafías"
  artifacts:
    - path: "supabase/migrations/0065_actualidad_senal.sql"
      provides: "tabla actualidad_senal + schema actualidad + proc materializar_senales + pg_cron job"
      contains: "create table actualidad_senal"
    - path: "supabase/tests/0065_actualidad_senal.test.sql"
      provides: "pgTAP: has_table, RLS, cero policies, security-definer, no-PII body, cron registrado, anon 42501, 3 defectos, supresión-como-fila"
      contains: "select plan("
  key_links:
    - from: "actualidad.materializar_senales()"
      to: "public.tramitacion_evento / public.citacion / public.sesion_sala"
      via: "insert ... select con fecha <= current_date + regexp_replace(camara)"
      pattern: "fecha <= current_date"
    - from: "cron.job"
      to: "actualidad.materializar_senales()"
      via: "cron.schedule intradía L-V"
      pattern: "actualidad-materializar"
---

<objective>
Crear la migración aditiva `0065_actualidad_senal.sql` — la tabla precomputada `actualidad_senal` deny-by-default, el proc full-rebuild transaccional `actualidad.materializar_senales()` (SQL puro, security definer, aplicando los 3 defectos de datos LOCKED del SPIKE), el job pg_cron intradía L-V, y el pgTAP `0065_actualidad_senal.test.sql` que lo valida contra el schema aplicado.

Purpose: Es la fundación de la fase — la superficie de datos que la RPC (99-02) lee y sobre la que el CLI k-means (99-03) escribe su capa `agrupacion_materia`. Cubre SEN-02 (tabla+proc+cron), SEN-03 (supresión-como-fila + sesgo declarado), SEN-04 (señales factuales).
Output: `supabase/migrations/0065_actualidad_senal.sql` + `supabase/tests/0065_actualidad_senal.test.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-senales-p1b-materializador/99-RESEARCH.md
@.planning/phases/99-senales-p1b-materializador/99-PATTERNS.md
@.planning/phases/98-senales-p1a-spike-de-datos/98-SPIKE-FINDINGS.md

<interfaces>
<!-- Analogs to mirror verbatim. Read these first; copy the security envelope + proc shape + cron DO-block. -->
Table + proc + cron molde: supabase/migrations/0039_cruce_senal.sql (L45-69 deny-by-default table, L79-122 full-rebuild proc, L124-154 pg_cron + version-guard + post-migration assertion).
pgTAP molde: supabase/tests/0039_cruce_senal.test.sql (has_table, RLS enabled, cero policies, prosecdef true, no-PII body regex `!~* '\y(partido|rut)\y'` con comments stripped, cron registrado, anon 42501).
Schema de columnas propuesto: 99-RESEARCH §Code Examples (tipo_senal CHECK con los 7 tipos, ventana, conteo, cobertura_camara, materia, cluster_id, fecha_max, supresion_causa, evidencia jsonb, provenance inline dataset/origen/fecha_captura/enlace, unique(tipo_senal, cobertura_camara, ventana, cluster_id)).
Source columns: proyecto.materia text (0008 L26); tramitacion_evento (tipo CHECK tramite/urgencia/informe/oficio/votacion, descripcion nullable, camara, fecha timestamptz — 0008 L68-82); citacion.fecha / sesion_sala.fecha = date-only-midnight-UTC = día chileno (app/lib/dia-calendario.ts — NO convertir tz).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el pgTAP (Wave 0 — test primero)</name>
  <read_first>
    supabase/tests/0039_cruce_senal.test.sql (molde completo, 119 líneas — copiar estructura begin/plan/seed/assert/finish/rollback).
    99-PATTERNS.md §"supabase/tests/0065_actualidad_senal.test.sql" (aserciones a copiar + las 3 específicas de Phase 99).
    98-SPIKE-FINDINGS.md §2 (los 3 defectos con sus queries de prueba).
  </read_first>
  <files>supabase/tests/0065_actualidad_senal.test.sql</files>
  <action>
    Crear el pgTAP espejo de 0039_cruce_senal.test.sql: `begin; select plan(N); … seed (owner bypassa RLS) … select actualidad.materializar_senales(); … asserts … select * from finish(); rollback;`.
    Aserciones estructurales (espejo 0039): has_table public.actualidad_senal; RLS enabled (relrowsecurity=true); cero policies (pg_policies count=0); proc actualidad.materializar_senales es security definer (prosecdef=true); no-PII body check sobre pg_get_functiondef con comments stripped (`regexp_replace(...,'--[^\n]*','','g') !~* '\y(partido|rut)\y'`); cron actualidad-materializar registrado (cron.job count=1); `set local role anon; throws_ok(select id from actualidad_senal, '42501'); reset role`.
    Aserciones específicas Phase 99 (los 3 defectos + supresión, per 98-SPIKE §2): sembrar un tramitacion_evento con fecha>current_date (typo 2626-05-25) y asertar que NO alimenta ninguna señal; sembrar ambas grafías camara (`C.Diputados`/`C. Diputados`) y asertar que colapsan a un solo bucket tras regexp_replace; sembrar fuente stale (max(fecha) sobre umbral) y asertar que aparece una fila con supresion_causa NOT NULL (nunca fila 0-como-hecho).
    Encabezado: nota que pgTAP corre `psql -tA -f` contra el schema APLICADO; build/typecheck son falsos positivos.
    NO inline el proc aquí — el proc lo crea Task 2; este test asume que la migración 0065 ya fue aplicada al scratch/prod DB antes de correr.
  </action>
  <verify>
    <automated>test -f "supabase/tests/0065_actualidad_senal.test.sql" &amp;&amp; grep -q "select plan(" supabase/tests/0065_actualidad_senal.test.sql &amp;&amp; grep -q "actualidad-materializar" supabase/tests/0065_actualidad_senal.test.sql &amp;&amp; grep -q "42501" supabase/tests/0065_actualidad_senal.test.sql</automated>
  </verify>
  <acceptance_criteria>
    El archivo existe con plan()/finish()/rollback, las 7 aserciones estructurales del molde 0039, y las 3 aserciones de defectos + la de supresión-como-fila. (Correrá verde contra el schema aplicado en 99-04.)
  </acceptance_criteria>
  <done>pgTAP escrito con aserciones estructurales + de defectos + supresión; espejo verificable de 0039.</done>
</task>

<task type="auto">
  <name>Task 2: Migración 0065 — tabla + schema + proc + pg_cron</name>
  <read_first>
    supabase/migrations/0039_cruce_senal.sql (completo — L45-69 tabla, L79-122 proc, L124-154 cron+guard+assertion).
    99-RESEARCH.md §Code Examples (DDL de actualidad_senal + esqueleto del proc con el insert de velocity + §Cómputo por señal L190-199 para cada insert).
    99-PATTERNS.md §Shared Patterns (Data Defects, Suppression-as-row, tz-Chile date-only, Anti-ranking).
    98-SPIKE-FINDINGS.md §1 (las 6 señales honestas + su guarda) y §2/§3/§4 (defectos, anti-ranking, regla del reloj).
  </read_first>
  <files>supabase/migrations/0065_actualidad_senal.sql</files>
  <action>
    Encabezado `-- 0065_actualidad_senal.sql` + warning de apply: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f …` — NUNCA `db push`.
    (A) Tabla `actualidad_senal` (espejo 0039 L45-69, esquema en 99-RESEARCH §Code Examples): columnas id identity PK, tipo_senal text con CHECK in ('velocity','nuevos_ingresos','urgencias','agenda_citacion','agenda_sala','archivados','agrupacion_materia'), ventana, conteo int default 0, cobertura_camara, materia, cluster_id, fecha_max timestamptz, supresion_causa (NULL=activa), evidencia jsonb default '{}', provenance inline NOT NULL (dataset/origen/fecha_captura default now()/enlace), unique(tipo_senal, cobertura_camara, ventana, cluster_id). Luego `enable row level security; revoke all on actualidad_senal from anon, authenticated;` + index en tipo_senal. CERO policies, CERO grant (deny-by-default; SEN-02).
    (B) `create schema if not exists actualidad;` + `create or replace function actualidad.materializar_senales() returns void language plpgsql security definer set search_path = '' as $$`. DELETE ACOTADO (CRÍTICO, Pitfall 5 — el CLI 99-03 posee 'agrupacion_materia'): `delete from public.actualidad_senal where tipo_senal in ('velocity','nuevos_ingresos','urgencias','agenda_citacion','agenda_sala','archivados');`. Luego un `insert … select` por señal, cada uno aplicando los 3 defectos LOCKED: (1) `where fecha <= current_date` en todo max/ventana; (2) agrupar por `coalesce(nullif(regexp_replace(camara,'\s+','','g'),''),'(sin cámara)')`; (3) camara NULL nunca repartido. Señales, per 98-SPIKE §1 + 99-RESEARCH §Cómputo por señal:
      - velocity: count(*)+count(distinct boletin) en ventana 7d por cámara normalizada; framing conteo, NUNCA "top" (anti-ranking T-52-13).
      - nuevos_ingresos: primer-evento por boletín `having min(fecha) >= '2022-01-01' and min(fecha) >= current_date - interval '7 days'`, EXCLUIR pre-2022, cobertura='2022-2026', JAMÁS fecha_captura.
      - urgencias: `where tipo='urgencia' and fecha <= current_date and fecha >= current_date - interval '30 days'`, fila con el hecho fechado, nunca "vigente".
      - agenda_citacion: `from public.citacion where fecha::date >= current_date` (date-only, SIN at time zone).
      - agenda_sala: si `count(*) from public.sesion_sala where fecha::date >= current_date` = 0 → insertar fila con supresion_causa='sin sesiones agendadas en las fuentes consultadas'. (Verificar el nombre exacto de la tabla de sesiones de sala contra el schema aplicado — Open Question A4/#1; si difiere, usar el nombre real y documentarlo en el SUMMARY.)
      - archivados: `where (descripcion ilike '%archiv%' or descripcion ilike '%retira%') and fecha <= current_date` EXCLUYENDO `ilike '%desarchiv%'` y `ilike '%retira y hace presente%'` (invierten sentido); framing "movimiento de archivo/retiro fechado", NO proyecto.estado.
      - supresión-frescura transversal: por fuente, si max(fecha_captura) supera el umbral stale → NO emitir señal positiva, emitir fila supresion_causa='sin datos frescos de esta fuente'. Umbral: hardcodear en el proc con comentario que cita el umbral de `pnpm freshness` (Open Question A5/#2 — el valor TS no es consultable desde SQL; documentar la constante). Setear fecha_max = max(tramitacion_evento.fecha saneada) por señal.
    (C) pg_cron (espejo 0039 L124-154 verbatim, cambiar jobname/expr): DO-block que verifica `pg_extension where extname='pg_cron'` (raise si null) + `cron.schedule('actualidad-materializar','7 11,14,17,20 * * 1-5', $cron$ select actualidad.materializar_senales(); $cron$)`; segundo DO-block assertion `if not exists (select 1 from cron.job where jobname='actualidad-materializar') then raise exception`.
    NO grant a anon/public/authenticated en ninguna parte (dispara lockdown-guard). NO referenciar tablas PII (partido/rut) — el pgTAP muerde el cuerpo.
  </action>
  <verify>
    <automated>grep -q "create table actualidad_senal" supabase/migrations/0065_actualidad_senal.sql &amp;&amp; grep -q "actualidad.materializar_senales" supabase/migrations/0065_actualidad_senal.sql &amp;&amp; grep -q "fecha <= current_date" supabase/migrations/0065_actualidad_senal.sql &amp;&amp; grep -q "regexp_replace(camara" supabase/migrations/0065_actualidad_senal.sql &amp;&amp; grep -q "actualidad-materializar" supabase/migrations/0065_actualidad_senal.sql &amp;&amp; grep -vi 'partido\|rut' supabase/migrations/0065_actualidad_senal.sql >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Tabla con RLS enabled + revoke all anon/authenticated + CHECK de 7 tipos + provenance inline + unique constraint.
    - Proc security definer, delete ACOTADO a los 6 tipos temporales (NO global), un insert por señal con los 3 defectos aplicados.
    - agenda_sala emite fila de supresión cuando no hay futuras; supresión-frescura emite fila con causa (nunca 0-como-hecho).
    - Ningún grant a anon/public/authenticated; ninguna referencia a partido/rut.
    - pg_cron job registrado + assertion post-migración.
  </acceptance_criteria>
  <done>0065 escrita: tabla deny-by-default + proc full-rebuild acotado con los 3 defectos + las 6 señales + supresión-como-fila + pg_cron intradía L-V.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → proc (security definer) | El proc corre como owner, bypassa RLS para escribir. Debe NO tocar tablas PII. |
| anon/authenticated → actualidad_senal | Lectura directa PROHIBIDA (deny-by-default); solo la RPC 99-02 lee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Information Disclosure | proc materializar_senales | mitigate | Cuerpo NO referencia partido/rut; actualidad_senal deriva de tramitacion/proyecto/citacion (no-PII). pgTAP asserta `!~* '\y(partido\|rut)\y'` sobre el cuerpo. |
| T-99-02 | Elevation of Privilege | migración 0065 | mitigate | CERO grant a anon/public/authenticated; tabla RLS enabled + revoke all. lockdown-guard muerde cualquier grant >0064. |
| T-99-03 | Tampering (de la verdad) | señales negativas | mitigate | Supresión = fila con supresion_causa (agenda_sala vacía, fuente stale), JAMÁS ausencia-como-hecho ni 0-como-hecho. pgTAP siembra fuente stale y verifica que dispara la fila. |
| T-52-13 | Tampering (de la verdad) | velocity + cortes por cámara | mitigate | PROHIBIDO ranking cross-cámara; cobertura_camara declarada por fila; framing "N trámites en 7 días", nunca "top/los más". |
| T-99-04 | Tampering | ventanas temporales | mitigate | `fecha <= current_date` en toda agregación (mata typo 2626); ancla a tramitacion_evento.fecha, nunca fecha_captura. pgTAP siembra fecha>current_date y verifica que no alimenta señal. |
</threat_model>

<verification>
- pgTAP `0065_actualidad_senal.test.sql` existe con aserciones estructurales + 3 defectos + supresión (correrá verde contra el schema aplicado en 99-04).
- 0065 escrita con tabla deny-by-default + proc acotado + pg_cron; grep confirma los invariantes.
- El apply live-DB + corrida del pgTAP contra el schema aplicado ocurren en 99-04 (checkpoint).
</verification>

<success_criteria>
- SEN-02: `actualidad_senal` es tabla precomputada refrescada por proc full-rebuild vía pg_cron intradía L-V.
- SEN-03: supresión-con-causa cuando fuente stale; sesgo por cámara declarado (cobertura_camara).
- SEN-04: señales factuales (velocity, nuevos_ingresos, urgencias, agenda_citacion, agenda_sala, archivados) ancladas a tramitacion_evento.fecha.
</success_criteria>

<output>
Create `.planning/phases/99-senales-p1b-materializador/99-01-SUMMARY.md` when done. Documentar: nombre real de la tabla de sesiones de sala verificado, umbral stale hardcodeado + su origen, cualquier deriva de nombre de columna vs el schema aplicado.
</output>
