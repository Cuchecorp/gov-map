---
phase: 99-senales-p1b-materializador
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - supabase/migrations/0066_actualidad_rpc.sql
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [SEN-02]
must_haves:
  truths:
    - "public.actualidad_senales_panel(p_tipo text) es la RPC bounded de lectura del panel: security definer, statement_timeout 5s, LIMIT explícito, doble-revoke, CERO grant"
    - "El filtro p_tipo es paramétrico (where p_tipo is null or tipo_senal = p_tipo) — sin SQL string-building"
    - "actualidad_senales_panel está en PUBLIC_RPC_ALLOWLIST o el guard Direction-B falla"
  artifacts:
    - path: "supabase/migrations/0066_actualidad_rpc.sql"
      provides: "RPC bounded actualidad_senales_panel + doble-revoke"
      contains: "actualidad_senales_panel"
    - path: "app/lib/lockdown-guard.test.ts"
      provides: "entrada actualidad_senales_panel en PUBLIC_RPC_ALLOWLIST"
      contains: "actualidad_senales_panel"
  key_links:
    - from: "public.actualidad_senales_panel"
      to: "public.actualidad_senal"
      via: "select ... limit 200 con statement_timeout 5s"
      pattern: "from public.actualidad_senal"
    - from: "PUBLIC_RPC_ALLOWLIST"
      to: "actualidad_senales_panel"
      via: "Set alfabético en lockdown-guard.test.ts"
      pattern: "actualidad_senales_panel"
---

<objective>
Crear la migración `0066_actualidad_rpc.sql` — la RPC bounded PII-safe de lectura del panel `public.actualidad_senales_panel(p_tipo)` (aguja completa espejo 0064) — y registrarla en `PUBLIC_RPC_ALLOWLIST` de `lockdown-guard.test.ts` para que el guard Direction-B no muerda.

Purpose: Es la superficie de lectura que la landing (Phase 100) consumirá con service_role (Camino A). Cierra la parte "RPC bounded PII-safe allowlisted" de SEN-02. Depende de que la tabla `actualidad_senal` exista (99-01).
Output: `supabase/migrations/0066_actualidad_rpc.sql` + entrada en `app/lib/lockdown-guard.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-senales-p1b-materializador/99-RESEARCH.md
@.planning/phases/99-senales-p1b-materializador/99-PATTERNS.md

<interfaces>
<!-- Analogs to mirror verbatim. -->
Bounded RPC molde: supabase/migrations/0064_bounded_rpc_statement_timeout.sql (L27-62 canonical single-RPC: drop function if exists then create or replace ... language sql stable security definer set search_path='' set statement_timeout='5s' ... limit then revoke all from public then revoke all from anon, authenticated).
Allowlist: app/lib/lockdown-guard.test.ts L165-192 (PUBLIC_RPC_ALLOWLIST Set alfabético; anonGrantOffenders regex L219-227 muerde grant a anon/public).
RPC signature (99-RESEARCH Bounded RPCs + Code Examples): actualidad_senales_panel(p_tipo text default null) returns table(tipo_senal text, ventana text, conteo int, cobertura_camara text, materia text, cluster_id int, fecha_max timestamptz, supresion_causa text, evidencia jsonb).
Columnas de actualidad_senal: creadas en 99-01 (0065). Alinear el returns table con el esquema real de 0065.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0066 — RPC bounded actualidad_senales_panel</name>
  <read_first>
    supabase/migrations/0064_bounded_rpc_statement_timeout.sql (L27-62 molde canónico + toda la disciplina doble-revoke).
    99-PATTERNS.md seccion 0066_actualidad_rpc.sql (la firma exacta + non-negotiables).
    supabase/migrations/0065_actualidad_senal.sql (de 99-01 — alinear returns table con las columnas reales).
  </read_first>
  <files>supabase/migrations/0066_actualidad_rpc.sql</files>
  <action>
    Encabezado con el nombre del archivo + warning de apply psql --single-transaction (NUNCA db push).
    Primero drop function if exists de public.actualidad_senales_panel(text) (idiom 42P13, ANTES del create-or-replace).
    Luego create or replace function public.actualidad_senales_panel(p_tipo text default null) returns table (tipo_senal text, ventana text, conteo int, cobertura_camara text, materia text, cluster_id int, fecha_max timestamptz, supresion_causa text, evidencia jsonb) language sql stable security definer set search_path='' set statement_timeout='5s'; cuerpo: select de las 9 columnas from public.actualidad_senal s where p_tipo is null or s.tipo_senal = p_tipo order by s.tipo_senal, s.cobertura_camara nulls last, s.cluster_id nulls last limit 200.
    Doble-revoke OBLIGATORIO (espejo 0064): revoke all on function public.actualidad_senales_panel(text) from public; y revoke all on function public.actualidad_senales_panel(text) from anon, authenticated;. CERO grant.
    Alinear el returns table con las columnas reales de 0065 (si 99-01 nombró alguna columna distinto, ajustar aquí). El filtro p_tipo es paramétrico — NO construir SQL por string (ASVS V5).
  </action>
  <verify>
    <automated>grep -q "drop function if exists public.actualidad_senales_panel" supabase/migrations/0066_actualidad_rpc.sql &amp;&amp; grep -q "security definer" supabase/migrations/0066_actualidad_rpc.sql &amp;&amp; grep -q "statement_timeout" supabase/migrations/0066_actualidad_rpc.sql &amp;&amp; grep -q "limit 200" supabase/migrations/0066_actualidad_rpc.sql</automated>
  </verify>
  <acceptance_criteria>
    - drop function if exists ANTES de create-or-replace.
    - language sql stable security definer + set search_path='' + set statement_timeout='5s' + limit 200.
    - Doble-revoke (from public Y from anon, authenticated); CERO grant.
    - returns table alineado con las columnas de 0065; filtro p_tipo paramétrico.
  </acceptance_criteria>
  <done>0066 escrita: RPC bounded aguja-completa espejo 0064, doble-revoke, cero grant.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir actualidad_senales_panel a PUBLIC_RPC_ALLOWLIST</name>
  <read_first>
    app/lib/lockdown-guard.test.ts (L160-227 — PUBLIC_RPC_ALLOWLIST Set alfabético L165-192, anonGrantOffenders regex L219-227).
    99-PATTERNS.md seccion lockdown-guard.test.ts (por qué es load-bearing bajo Camino A / service_role).
  </read_first>
  <files>app/lib/lockdown-guard.test.ts</files>
  <action>
    Insertar la cadena "actualidad_senales_panel" en el Set PUBLIC_RPC_ALLOWLIST (L165-192) EN ORDEN ALFABÉTICO — va PRIMERA, antes de "agregado_por_contraparte". Sin esta entrada el guard Direction-B falla porque bajo Camino A el árbol web lee con service_role, que puede ejecutar cualquier RPC; la DB ya no bloquea, así que el guard es la barrera. NO tocar nada más del archivo. Verificar que la suite de guard queda verde.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm test lockdown-guard 2>&amp;1 | grep -qi "pass\|✓" &amp;&amp; grep -q '"actualidad_senales_panel"' lib/lockdown-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "actualidad_senales_panel" presente en el Set PUBLIC_RPC_ALLOWLIST, primera alfabéticamente.
    - La suite lockdown-guard corre verde (Direction-B + anonGrantOffenders).
  </acceptance_criteria>
  <done>RPC registrada en el allowlist; guard verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role (Camino A) → RPC | El árbol web ejecuta la RPC; el allowlist (guard Direction-B) es la única barrera de superficie. |
| p_tipo (input) → cuerpo SQL | Input de usuario cruza a la query; debe ser paramétrico. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | Elevation of Privilege | RPC fuera del allowlist | mitigate | Alta en PUBLIC_RPC_ALLOWLIST; guard Direction-B falla si el árbol web llama una RPC no listada. |
| T-99-06 | Elevation of Privilege | grant en la migración | mitigate | Doble-revoke (public + anon/authenticated), CERO grant; anonGrantOffenders muerde grant a anon/public >0064. |
| T-99-07 | Denial of Service | agregación cara / query sin límite | mitigate | set statement_timeout='5s' + limit 200 explícito (0064). La tabla ya está precomputada → la RPC es trivialmente sub-5s. |
| T-99-08 | Tampering | SQL injection vía p_tipo | mitigate | Filtro paramétrico where p_tipo is null or tipo_senal=p_tipo; language sql (no string-building). ASVS V5. |
| T-99-09 | Information Disclosure | fuga PII vía RPC | accept | actualidad_senal es no-PII por construcción (solo conteos/labels/fechas); la RPC solo re-emite esas columnas. Verificado: no hace join a tablas PII. |
</threat_model>

<verification>
- 0066 escrita: RPC bounded doble-revoke, statement_timeout 5s, limit 200; grep confirma invariantes.
- "actualidad_senales_panel" en PUBLIC_RPC_ALLOWLIST; suite lockdown-guard verde.
- El apply live-DB de 0066 ocurre en 99-04 (checkpoint).
</verification>

<success_criteria>
- SEN-02: la landing lee filas listas vía RPC bounded PII-safe allowlisted (LIMIT + statement_timeout + allowlist).
</success_criteria>

<output>
Create `.planning/phases/99-senales-p1b-materializador/99-02-SUMMARY.md` when done. Documentar cualquier ajuste del returns table vs las columnas reales de 0065.
</output>
