---
phase: 99-senales-p1b-materializador
plan: 04
type: execute
wave: 3
depends_on: ["99-01", "99-02", "99-03"]
files_modified:
  - .github/workflows/actualidad-refresh.yml
autonomous: false
requirements: [SEN-02, SEN-05]
user_setup:
  - service: github-actions
    why: "El cron actualidad-refresh usa secrets ya presentes en el repo"
    env_vars:
      - name: SUPABASE_API_URL
        source: "GitHub repo secrets (ya cargado — usado por leyes-weekly)"
      - name: SUPABASE_SECRET_KEY
        source: "GitHub repo secrets (ya cargado — usado por leyes-weekly)"
must_haves:
  truths:
    - "actualidad-refresh.yml existe, clona leyes-weekly SIN el bloque R2 (solo SUPABASE_API_URL + SUPABASE_SECRET_KEY), cron intradía L-V, corre @obs/actualidad CLI"
    - "0065 y 0066 aplicadas al PROD live-DB por psql --single-transaction (NUNCA db push); es un write live-DB aditivo sin flip de flag de régimen"
    - "El pgTAP 0065_actualidad_senal.test.sql corre verde contra el schema APLICADO"
    - "La RPC actualidad_senales_panel retorna filas acotadas dentro del statement_timeout"
  artifacts:
    - path: ".github/workflows/actualidad-refresh.yml"
      provides: "cron intradía L-V del CLI de clustering, sin R2"
      contains: "actualidad-refresh"
  key_links:
    - from: ".github/workflows/actualidad-refresh.yml"
      to: "@obs/actualidad run-actualidad-prod-cli.ts"
      via: "pnpm --filter @obs/actualidad exec tsx"
      pattern: "@obs/actualidad"
---

<objective>
Crear el cron `.github/workflows/actualidad-refresh.yml` (clona leyes-weekly SIN R2 — no toca fuentes, sin rate-limit), y aplicar las migraciones `0065`/`0066` al PROD live-DB (psql --single-transaction) + correr el pgTAP contra el schema aplicado. Esta es la única acción de write live-DB de la fase; es ADITIVA (tabla + proc + RPC + cron) y NO flipea ningún flag de régimen.

Purpose: Cierra la parte "refresh L-V" de SEN-02 (scheduler TS) y activa la superficie en PROD. Depende de las migraciones (99-01, 99-02) y el CLI (99-03).
Output: `actualidad-refresh.yml` + migraciones aplicadas + pgTAP verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-senales-p1b-materializador/99-RESEARCH.md
@.planning/phases/99-senales-p1b-materializador/99-PATTERNS.md

<interfaces>
<!-- Analog: clone leyes-weekly minus R2. -->
YAML molde: .github/workflows/leyes-weekly.yml (completo, 76 líneas — checkout@v4.3.1 SHA-pinned L43, pnpm/action-setup L46, node22+cache L48-52, pnpm install --frozen-lockfile --ignore-scripts L56, env block L60-66, run step L75, inputs-by-ENV L67-70 anti-injection, permissions contents:read L31-32, concurrency L34-36, workflow_dispatch L20-29).
Deltas (99-PATTERNS.md seccion actualidad-refresh.yml): cron "0 11,14,17,20 * * 1-5"; BORRAR el bloque R2 (R2_ACCESS_KEY_ID/SECRET/ENDPOINT_URL/BUCKET), dejar SOLO SUPABASE_API_URL + SUPABASE_SECRET_KEY; run step pnpm --filter @obs/actualidad exec tsx src/run-actualidad-prod-cli.ts; concurrency group renombrado.
Apply discipline: PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f (NUNCA db push). Precedente 0055-0064.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear actualidad-refresh.yml (clon de leyes-weekly SIN R2)</name>
  <read_first>
    .github/workflows/leyes-weekly.yml (completo — clonar scaffold).
    99-PATTERNS.md seccion actualidad-refresh.yml (los 4 deltas exactos).
    99-RESEARCH.md Cron split (L209-214 — sin race, sin R2, sin rate-limit).
  </read_first>
  <files>.github/workflows/actualidad-refresh.yml</files>
  <action>
    Clonar el scaffold completo de leyes-weekly.yml (checkout SHA-pinned, pnpm/action-setup, node 22 + cache, pnpm install --frozen-lockfile --ignore-scripts, permissions contents:read, workflow_dispatch, inputs-by-ENV anti-injection). Cambios:
    1. name + concurrency group renombrados a actualidad-refresh.
    2. cron: "0 11,14,17,20 * * 1-5" (intradía L-V).
    3. BORRAR el bloque R2 (las 4 líneas R2_ACCESS_KEY_ID/R2_SECRET/R2_ENDPOINT_URL/R2_BUCKET) — Phase 99 NO toca fuentes → sin R2, sin rate-limit, sin robots.txt. Dejar SOLO SUPABASE_API_URL + SUPABASE_SECRET_KEY en el env block.
    4. run step: pnpm --filter @obs/actualidad exec tsx src/run-actualidad-prod-cli.ts (con $ARGS de los inputs-by-ENV si el molde los pasa).
    Mantener el patrón inputs-by-ENV para no inyectar shell en el step que carga el secret.
  </action>
  <verify>
    <automated>test -f ".github/workflows/actualidad-refresh.yml" &amp;&amp; grep -q "actualidad-refresh" .github/workflows/actualidad-refresh.yml &amp;&amp; grep -q "@obs/actualidad" .github/workflows/actualidad-refresh.yml &amp;&amp; grep -q "1-5" .github/workflows/actualidad-refresh.yml &amp;&amp; ! grep -qi "R2_" .github/workflows/actualidad-refresh.yml</automated>
  </verify>
  <acceptance_criteria>
    - YAML existe, cron intradía L-V, corre @obs/actualidad CLI.
    - CERO env R2_* (bloque borrado); solo SUPABASE_API_URL + SUPABASE_SECRET_KEY.
    - Scaffold seguro de leyes-weekly preservado (SHA-pinned checkout, ignore-scripts, inputs-by-ENV).
  </acceptance_criteria>
  <done>actualidad-refresh.yml creado: clon de leyes-weekly sin R2, intradía L-V.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OPERADOR — aplicar 0065/0066 a PROD (psql --single-transaction) + pgTAP verde + RPC bounded (write live-DB aditivo, sin flip de régimen)</name>
  <files>supabase/migrations/0065_actualidad_senal.sql, supabase/migrations/0066_actualidad_rpc.sql, supabase/tests/0065_actualidad_senal.test.sql</files>
  <what-built>
    Migraciones 0065 (tabla + proc + pg_cron) y 0066 (RPC bounded) listas para aplicar, pgTAP 0065_actualidad_senal.test.sql listo, RPC en el allowlist, CLI @obs/actualidad listo, cron YAML creado. TODO validado localmente (grep + suite). Falta el ÚNICO write live-DB de la fase.
  </what-built>
  <action>
    WRITE LIVE-DB ADITIVO (tabla + proc + RPC + cron nuevos) — NO flipea ningún flag de régimen (menor riesgo que MONEY/NET, pero sigue siendo un write a PROD). Ejecutar en orden:
    1. Aplicar 0065 (tabla + schema actualidad + proc + registra pg_cron): PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0065_actualidad_senal.sql (NUNCA supabase db push — drift schema_migrations).
    2. Aplicar 0066 (RPC bounded + doble-revoke): PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0066_actualidad_rpc.sql.
    3. Correr el pgTAP contra el schema APLICADO (N ok / 0 not ok): PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0065_actualidad_senal.test.sql.
    4. Verificar pg_cron: select jobname from cron.job where jobname='actualidad-materializar' → 1 fila.
    5. Correr el proc y verificar filas: select actualidad.materializar_senales(); luego select tipo_senal, count(*) from actualidad_senal group by 1 → filas de velocity/urgencias/agenda/archivados (+ supresión donde aplique).
    6. Probar la RPC bounded: select count(*) from actualidad_senales_panel(null) (< 5s, ≤ 200 filas) y select count(*) from actualidad_senales_panel('velocity').
    7. (Opcional) correr el CLI k-means LOCAL contra PROD para poblar agrupacion_materia, o dejar que el cron GH Actions lo haga.
  </action>
  <how-to-verify>
    Reportar: pgTAP N/N ok (0 not ok), cron actualidad-materializar registrado, conteos por tipo_senal tras la primera corrida del proc, y que la RPC actualidad_senales_panel responde bounded (< 5s, ≤ 200 filas). Confirmar que NINGÚN flag de régimen fue flipeado (write aditivo puro).
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint operador: el apply live-DB + pgTAP contra el schema aplicado NO son verificables desde CI (build/typecheck son falsos positivos para DDL). Verificación = pgTAP N/N ok reportado por el operador.</automated>
  </verify>
  <done>0065/0066 aplicadas a PROD; pgTAP N/N ok (0 not ok); cron registrado; RPC bounded responde; ningún flag de régimen flipeado.</done>
  <resume-signal>Escribir "aplicado" con el resultado del pgTAP (N ok / 0 not ok) + conteos por tipo_senal + confirmación RPC, o describir el fallo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → PROD live-DB | Write aditivo por psql; único punto donde el estado real cambia. |
| GH Actions cron → PROD | El CLI escribe con SUPABASE_SECRET_KEY (service_role) desde CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-14 | Tampering | apply por db push (drift) | mitigate | SOLO psql --single-transaction; el checkpoint lo enuncia explícito. Precedente 0055-0064. |
| T-99-15 | Elevation of Privilege | migración ensancha lockdown | mitigate | 0065/0066 son aditivas cero-grant (deny-by-default + doble-revoke); NO flipean flag de régimen; no ensanchan la superficie anon/authenticated. |
| T-99-16 | Information Disclosure | secrets en GH Actions | accept | Reusa secrets existentes (SUPABASE_API_URL + SUPABASE_SECRET_KEY), sin secret nuevo; sin R2. inputs-by-ENV evita inyección. |
| T-99-SC | Tampering | npm/pnpm installs | mitigate | Sin paquete net-new (k-means a mano; @supabase/supabase-js y tsx ya en lockfile). pnpm install --frozen-lockfile --ignore-scripts. No hay tabla de legitimidad porque no hay install nuevo (99-RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- actualidad-refresh.yml creado sin R2, intradía L-V; grep confirma.
- CHECKPOINT operador: 0065/0066 aplicadas por psql --single-transaction; pgTAP verde contra schema aplicado; cron registrado; RPC bounded responde; conteos por tipo_senal materializados.
- Ningún flag de régimen flipeado (write aditivo puro).
</verification>

<success_criteria>
- SEN-02: refresh L-V vivo (pg_cron para señales temporales + GH Actions para clustering); superficie de datos + RPC bounded aplicadas a PROD.
- SEN-05: cron del CLI de clustering activo.
</success_criteria>

<output>
Create `.planning/phases/99-senales-p1b-materializador/99-04-SUMMARY.md` when done. Documentar: resultado pgTAP (N/N ok), conteos por tipo_senal tras la primera corrida del proc, confirmación de que la RPC responde bounded, y que NINGÚN flag de régimen fue flipeado.
</output>
