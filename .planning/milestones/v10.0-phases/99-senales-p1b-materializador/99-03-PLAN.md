---
phase: 99-senales-p1b-materializador
plan: 03
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - packages/actualidad/package.json
  - packages/actualidad/tsconfig.json
  - packages/actualidad/vitest.config.ts
  - packages/actualidad/src/kmeans.ts
  - packages/actualidad/src/kmeans.test.ts
  - packages/actualidad/src/run-actualidad-prod-cli.ts
autonomous: true
requirements: [SEN-05]
must_haves:
  truths:
    - "@obs/actualidad existe como workspace con dep @supabase/supabase-js, tsconfig references (no paths), vitest.config.ts propio (evita CI-DARK)"
    - "kmeans.ts implementa Lloyd determinista con PRNG seed-fija (mulberry32) sobre vectores 768d, distancia coseno, k en [8,15]"
    - "El label del cluster = mode(materia) factual (frecuencia máxima, empate → alfabético), JAMÁS LLM"
    - "run-actualidad-prod-cli.ts lee proyecto_embedding paginado (.range, cap 1k), corre k-means, y hace full-rebuild SOLO de tipo_senal='agrupacion_materia' (delete acotado, disjunto del proc SQL)"
    - "kmeans.test.ts prueba: misma entrada → misma asignación (determinismo); label = mode(materia)"
  artifacts:
    - path: "packages/actualidad/src/kmeans.ts"
      provides: "Lloyd k-means determinista seed-fija + label mode(materia)"
      min_lines: 30
    - path: "packages/actualidad/src/kmeans.test.ts"
      provides: "test de determinismo + label factual"
      contains: "describe"
    - path: "packages/actualidad/src/run-actualidad-prod-cli.ts"
      provides: "CLI service_role writer de la capa agrupacion_materia"
      contains: "agrupacion_materia"
    - path: "packages/actualidad/package.json"
      provides: "workspace @obs/actualidad"
      contains: "@obs/actualidad"
  key_links:
    - from: "run-actualidad-prod-cli.ts"
      to: "public.proyecto_embedding"
      via: ".select(boletin, embedding).order().range() paginado"
      pattern: "proyecto_embedding"
    - from: "run-actualidad-prod-cli.ts"
      to: "public.actualidad_senal (tipo_senal='agrupacion_materia')"
      via: "delete acotado + insert una fila por cluster"
      pattern: "agrupacion_materia"
---

<objective>
Crear el workspace `@obs/actualidad` — `kmeans.ts` (Lloyd determinista seed-fija sobre embeddings 768d, label = mode(materia) factual, NUNCA LLM) + su test de determinismo, y `run-actualidad-prod-cli.ts` (service_role writer que hace full-rebuild SOLO de `tipo_senal='agrupacion_materia'`, disjunto del proc SQL).

Purpose: Es la capa SECUNDARIA de agrupación por tema (SEN-05). El label primario `materia` lo escribe el proc SQL (99-01); este CLI añade `cluster_id` sobre las mismas filas sin race (conjuntos de tipo_senal disjuntos). Corre en GH Actions intradía L-V (YAML en 99-04). Depende de que `actualidad_senal` exista (99-01).
Output: workspace `packages/actualidad/*` con kmeans + CLI + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-senales-p1b-materializador/99-RESEARCH.md
@.planning/phases/99-senales-p1b-materializador/99-PATTERNS.md

<interfaces>
<!-- Analogs + spec. kmeans.ts has NO codebase analog — use RESEARCH Clustering as spec. -->
CLI molde: packages/tramitacion/src/run-tramitacion-prod-cli.ts (L28-30 imports, L49-50 createClient(url, serviceKey), L55-58 flagValue argv helper, L64-75 loadEnv BOM-safe con precedencia process.env). Credenciales de SUPABASE_API_URL + SUPABASE_SECRET_KEY SOLO (nunca argv).
Package scaffold (gotchas Phase 43/90): tsconfig references NO paths; vitest.config.ts propio verbatim evita CI-DARK. Workspace glob packages/* en pnpm-workspace.yaml auto-incluye @obs/actualidad (sin edición manual del workspace).
Embeddings source: supabase/migrations/0011_fichas_embeddings.sql L36-45 — proyecto_embedding(boletin PK, embedding vector(768), embedding_model/dims/version), public-read. Join a proyecto por boletin. Cobertura ~84.6% (15.4% sin embedding → declarar cobertura del cluster). Cap PostgREST 1k → paginar .select().order().range().
Materia: proyecto.materia text (0008 L26), taxonomía oficial.
k-means spec: 99-RESEARCH Clustering (L201-207) + Pitfall 4 (L255-258). Lloyd seed-fija (mulberry32 PRNG, init k-means++ o primeros-k-por-boletín), distancia coseno (dot-product sobre vectores normalizados), k=10 [8,15], full-rebuild de su tipo. Label = mode(materia), empate → alfabético.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold @obs/actualidad + kmeans.ts + kmeans.test.ts</name>
  <read_first>
    packages/tramitacion/package.json + packages/tramitacion/tsconfig.json + packages/tramitacion/vitest.config.ts (molde de scaffold — references no paths, vitest propio).
    99-PATTERNS.md seccion No Analog Found (spec de kmeans.ts: Lloyd ~40 líneas, mulberry32, coseno, determinismo).
    99-RESEARCH.md Clustering (L201-207) + Pitfall 4 (L255-258).
  </read_first>
  <files>packages/actualidad/package.json, packages/actualidad/tsconfig.json, packages/actualidad/vitest.config.ts, packages/actualidad/src/kmeans.ts, packages/actualidad/src/kmeans.test.ts</files>
  <behavior>
    - Test 1 (determinismo): kmeans(vectores, k, seed) con la misma entrada y misma seed → asignaciones IDÉNTICAS en dos corridas.
    - Test 2 (label factual): dado un cluster con materias ['A','A','B'], el label = 'A' (mode). Empate ['A','B'] → 'A' (alfabético). NUNCA texto generado.
    - Test 3 (coseno): dos vectores idénticos caen en el mismo cluster; vectores ortogonales tienden a clusters distintos con k pequeño.
  </behavior>
  <action>
    Scaffold del workspace espejando packages/tramitacion: package.json name "@obs/actualidad" + dep @supabase/supabase-js (ya en el monorepo) + tsx; tsconfig.json con references (NO paths — gotcha Phase 43); vitest.config.ts propio VERBATIM del molde (evita CI-DARK, gotcha Phase 43/90).
    kmeans.ts: implementar Lloyd k-means a mano (~40 líneas, sin librería externa — evita dependencia + garantiza reproducibilidad byte-a-byte). PRNG determinista mulberry32 con seed FIJA constante (documentada). Init de centroides determinista (k-means++ con la seed o primeros-k por orden de boletín). Distancia coseno = dot-product sobre vectores normalizados (consistente con embeddings Gemini). Iterar asignación→recompute hasta convergencia o max-iter. Función de label: contar frecuencias de materia por cluster, tomar el máximo, empate → orden alfabético. Exportar kmeans(vectors, k, seed) y labelCluster(materias). k default 10 (rango [8,15] documentado).
    kmeans.test.ts: los 3 tests de behavior. Correr con vitest del propio paquete.
  </action>
  <verify>
    <automated>cd packages/actualidad &amp;&amp; pnpm test 2>&amp;1 | grep -qi "pass\|✓"</automated>
  </verify>
  <acceptance_criteria>
    - Workspace @obs/actualidad instalable (pnpm install lo recoge vía glob packages/*).
    - kmeans.ts: Lloyd determinista, mulberry32 seed-fija, coseno, label=mode(materia) empate-alfabético, sin dependencia externa de k-means.
    - kmeans.test.ts verde: determinismo (misma entrada→misma asignación) + label factual.
    - tsconfig references (no paths); vitest.config.ts propio (no CI-DARK).
  </acceptance_criteria>
  <done>@obs/actualidad scaffolded; k-means determinista + label factual con tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: run-actualidad-prod-cli.ts — writer de agrupacion_materia</name>
  <read_first>
    packages/tramitacion/src/run-tramitacion-prod-cli.ts (L28-75 — createClient, loadEnv BOM-safe, flagValue, precedencia process.env).
    packages/actualidad/src/kmeans.ts (de Task 1 — la API kmeans/labelCluster).
    99-PATTERNS.md seccion run-actualidad-prod-cli.ts (job del CLI + cap 1k + full-rebuild acotado).
    99-RESEARCH.md Clustering L201-207 (escritura: delete where tipo_senal='agrupacion_materia' + insert una fila por cluster).
  </read_first>
  <files>packages/actualidad/src/run-actualidad-prod-cli.ts</files>
  <action>
    Espejar el molde de run-tramitacion-prod-cli.ts: createClient(SUPABASE_API_URL, SUPABASE_SECRET_KEY) — service_role, credenciales SOLO de env (nunca argv), loadEnv BOM-safe con precedencia process.env (CI inyecta secrets ahí).
    Leer proyecto_embedding PAGINADO (.select('boletin, embedding').order('boletin').range() en lotes < 1000 — cap PostgREST, gotcha memoria repo). Opcionalmente filtrar a los boletines con movimiento reciente (los que velocity identificó) — o clusterizar todo el corpus con embedding y declarar cobertura ~84.6%. Join a proyecto por boletin para traer materia.
    Correr kmeans(vectors, k=10, SEED_FIJA) de kmeans.ts. Por cluster: labelCluster(materias del cluster) = label factual mode(materia).
    Full-rebuild ACOTADO (Pitfall 5, disjunto del proc SQL): delete from actualidad_senal where tipo_senal='agrupacion_materia' + insert UNA fila por cluster con tipo_senal='agrupacion_materia', cluster_id, conteo (boletines del cluster), materia (el label mode), evidencia jsonb (boletines miembro + enlace de fuente), provenance inline. Idempotente y reproducible.
    Declarar la cobertura del clustering (subset con embedding) en la evidencia o en un log. NO llamar LLM en ninguna parte.
  </action>
  <verify>
    <automated>cd packages/actualidad &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; grep -q "agrupacion_materia" src/run-actualidad-prod-cli.ts &amp;&amp; grep -q "range(" src/run-actualidad-prod-cli.ts &amp;&amp; grep -q "SUPABASE_SECRET_KEY" src/run-actualidad-prod-cli.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI usa service_role (SUPABASE_API_URL + SUPABASE_SECRET_KEY), loadEnv BOM-safe, credenciales no-argv.
    - Lee proyecto_embedding paginado (.range, cap 1k respetado).
    - Full-rebuild ACOTADO a tipo_senal='agrupacion_materia' (delete where tipo_senal=... NO global); una fila por cluster con label mode(materia).
    - CERO llamada LLM; tsc --noEmit limpio.
  </acceptance_criteria>
  <done>CLI escrito: lee embeddings paginado, k-means determinista, escribe la capa agrupacion_materia sin race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI (service_role) → actualidad_senal | El CLI escribe con service_role (bypassa RLS); debe acotar su delete a su propio tipo_senal. |
| proyecto_embedding (public-read) → CLI | Lectura de embeddings no-PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-10 | Tampering | race pg_cron vs CLI | mitigate | Delete ACOTADO a tipo_senal='agrupacion_materia' (disjunto del set temporal del proc); unique(tipo_senal, cobertura_camara, ventana, cluster_id) evita duplicados. |
| T-99-11 | Tampering (de la verdad) | label del cluster | mitigate | Label = mode(proyecto.materia) factual (SQL/TS), empate alfabético; JAMÁS LLM (riesgo existencial #2). El test verifica el label factual. |
| T-99-12 | Tampering | k-means no determinista | mitigate | PRNG mulberry32 seed-fija + input ordenado por boletín + empate alfabético; test de determinismo (misma entrada → misma asignación). |
| T-99-13 | Information Disclosure | secrets en argv/log | mitigate | Credenciales SOLO de env (SUPABASE_SECRET_KEY nunca en argv ni log); molde run-tramitacion. |
</threat_model>

<verification>
- kmeans.test.ts verde: determinismo + label factual.
- CLI tsc --noEmit limpio; escribe agrupacion_materia con delete acotado; lee embeddings paginado.
- La corrida LIVE del CLI (write PROD) ocurre vía GH Actions (99-04) o operador.
</verification>

<success_criteria>
- SEN-05: agrupación por materia con label primario `materia` + k-means determinista seed-fija sobre embeddings como capa secundaria; labels JAMÁS por LLM.
</success_criteria>

<output>
Create `.planning/phases/99-senales-p1b-materializador/99-03-SUMMARY.md` when done. Documentar: k elegido + su justificación (conteo real de PLs con movimiento si se hizo), la SEED_FIJA constante, cobertura declarada del clustering (subset con embedding).
</output>
