---
phase: 104-cierre-p3b-verificaci-n-e2e-todo-funciona
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - .planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "El build OpenNext corre en Docker Linux y emite worker.js sin error"
    - "El Worker observatorio-congreso queda desplegado con los cambios de 101+102+103"
    - "VSIM_PUBLIC_ENABLED=true está seteado como env var del Worker (deploy-time, NO committeado)"
    - "NOTIF y MONEY permanecen OFF (sin env var seteado en el Worker)"
    - "Camino A (rutas públicas) sigue 200 y la CSP intacta tras el deploy"
  artifacts:
    - path: ".planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md"
      provides: "Runbook reproducible del deploy + flip VSIM + verificación curl post-deploy"
      contains: "VSIM_PUBLIC_ENABLED"
  key_links:
    - from: "wrangler secret put VSIM_PUBLIC_ENABLED"
      to: "app/lib/vsim-gate.ts (=== 'true')"
      via: "env var del Worker enciende la 5ª sección de /comparar"
      pattern: "VSIM_PUBLIC_ENABLED"
    - from: "deploy OpenNext Docker"
      to: "https://observatorio-congreso.thevalis.workers.dev"
      via: "wrangler deploy (OAuth global)"
      pattern: "workers.dev"
---

<objective>
Deploy único de cierre v10.0: build OpenNext en Docker Linux + wrangler deploy que ARRASTRA los cambios de 101 (relaciones + /comparar), 102 (VSIM) y 103 (NOTIF código, flag OFF), seguido del flip VSIM_PUBLIC_ENABLED=true como env var del Worker (deploy-time, JAMÁS committeado). NOTIF y MONEY quedan OFF (sin env var). Verificación curl post-deploy de Camino A + CSP + VSIM visible.

Purpose: Sin este deploy, todo lo construido en las tres pasadas vive solo en el repo. El E2E de Plan 03 verifica el DEPLOY REAL — este plan lo produce. El flip VSIM es legítimo porque Plan 01 firmó el dossier.
Output: Worker desplegado (versión registrada), VSIM ON, 104-DEPLOY-RUNBOOK.md reproducible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md
@.planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md

<interfaces>
<!-- GOTCHAS de deploy LOCKED (memoria v10.0 + 103-DEPLOY-RUNBOOK §c): -->
<!-- 1. Build DENTRO de node:22-slim (NUNCA Windows/alpine — symlink EPERM). -->
<!-- 2. Espejar el repo a C:/Temp/obs-build (path sin locks OneDrive) con robocopy /MIR. -->
<!-- 3. Purgar .pnpm-store del mirror si robocopy lo arrastró (evita locks). -->
<!-- 4. Re-escribir docker-deploy.sh en el build dir tras cada /MIR (NO está en el repo). -->
<!-- 5. corepack prepare pnpm@11 + pnpm config set dangerouslyAllowAllBuilds true + install --frozen-lockfile. -->
<!-- 6. cd app && pnpm run cf-build → worker.js. -->
<!-- 7. Deploy dentro del MISMO contenedor montando el OAuth del host en /root/.config/.wrangler. -->
<!-- 8. wrangler real = npm global AppData/Roaming/npm/wrangler.cmd (sombreado por paquete Python) — invocar por ruta absoluta o dentro del contenedor. -->
<!-- 9. MSYS_NO_PATHCONV=1 solo si se corre desde git-bash. -->
<!-- 10. Propagación edge ~10-30s (500 intermitentes transitorios tras el deploy). -->

Worker URL: https://observatorio-congreso.thevalis.workers.dev
Supabase ref: bctyygbmqcvizyplktuw (sa-east-1) — migraciones 0067/0068/0069/0070/0071 YA aplicadas a PROD.

Flags (estado objetivo tras el deploy):
- VSIM_PUBLIC_ENABLED = true   (env var del Worker — encender la 5ª sección de /comparar)
- NOTIF_PUBLIC_ENABLED = (ausente) → OFF (provisión operador no existe; botón Seguir NO en DOM)
- MONEY_PUBLIC_ENABLED = (ausente) → OFF (gated legal v7.0)

Chokepoint del flag VSIM: app/lib/vsim-gate.ts lee `process.env.VSIM_PUBLIC_ENABLED === "true"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OpenNext en Docker Linux + deploy wrangler + flip VSIM ON</name>
  <read_first>
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md (§c deploy VERBATIM — el molde reproducible)
    - .planning/phases/97-auth-p0-spike-auth-on-workers-de-risk/97-DEPLOY-RUNBOOK.md (§Runbook reproducible — origen de los gotchas)
    - app/AGENTS.md
  </read_first>
  <action>
    Reproducir el deploy per 103-DEPLOY-RUNBOOK §c (que espeja 97). Pasos: (1) Espejar el repo a C:/Temp/obs-build con robocopy /MIR (PowerShell, NO git-bash) excluyendo node_modules/.open-next/.next/.git/.turbo/dist/coverage; PRIMERO `Remove-Item -Recurse -Force 'C:/Temp/obs-build/node_modules'` para limpiar residuo del build anterior. (2) Purgar `.pnpm-store` del mirror si robocopy lo arrastró. (3) Re-escribir `docker-deploy.sh` en el build dir (NO está en el repo): `export XDG_CONFIG_HOME=/root/.config; export WRANGLER_HOME=/root/.config/.wrangler; cd /work/app && CI=true pnpm run deploy`. (4) Build dentro de node:22-slim: `corepack prepare pnpm@11` + `pnpm config set dangerouslyAllowAllBuilds true` + `pnpm install --frozen-lockfile` + `cd app && pnpm run cf-build` (exit 0 → worker.js emitido). (5) Deploy dentro del MISMO contenedor montando el OAuth del host (`-v "C:\Users\Carlo\AppData\Roaming\xdg.config\.wrangler:/root/.config/.wrangler"`) corriendo docker-deploy.sh. (6) FLIP VSIM: `wrangler secret put VSIM_PUBLIC_ENABLED` valor `true` (wrangler global del host, OAuth) + `wrangler secret list` para confirmar que aparece — o setearlo como [vars] del Worker en el dashboard; NUNCA en wrangler.jsonc committeado ni en .env.example. NO setear NOTIF_PUBLIC_ENABLED ni MONEY_PUBLIC_ENABLED (quedan ausentes = OFF). Registrar TODO (comandos exactos, versión de deploy, salida de secret list redactada) en 104-DEPLOY-RUNBOOK.md.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://observatorio-congreso.thevalis.workers.dev/</automated>
  </verify>
  <acceptance_criteria>
    - El build en node:22-slim termina EXIT 0 y emite worker.js (`cd app && pnpm run cf-build` → "OpenNext build complete").
    - `wrangler deploy` reporta una versión nueva; la versión queda registrada en 104-DEPLOY-RUNBOOK.md.
    - `wrangler secret list` incluye `VSIM_PUBLIC_ENABLED` (registrado redactado en el runbook).
    - `wrangler secret list` NO incluye `NOTIF_PUBLIC_ENABLED` ni `MONEY_PUBLIC_ENABLED`.
    - `.env.example` sin cambios (`git diff --exit-code .env.example` retorna 0 — el flip es deploy-time).
  </acceptance_criteria>
  <done>Worker desplegado con 101+102+103; VSIM_PUBLIC_ENABLED=true como env var del Worker; NOTIF/MONEY sin env var (OFF); comandos y versión registrados en el runbook.</done>
</task>

<task type="auto">
  <name>Task 2: Verificación curl post-deploy — Camino A + CSP + VSIM visible + /spike-auth ausente</name>
  <read_first>
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md (§c paso 4 — verificación curl)
  </read_first>
  <action>
    Tras la propagación edge (~10-30s; los 500 iniciales son transitorios), verificar con curl sobre el deploy vivo: (1) Camino A — `/`, `/parlamentarios`, `/agenda`, `/buscar`, `/metodologia` devuelven 200. (2) `/spike-auth` devuelve 404 (borrado en Phase 103, ya no 500). (3) CSP intacta: los headers de `/` contienen `connect-src 'self'`, `object-src 'none'`, `frame-ancestors 'none'`. (4) VSIM visible: `curl -s "https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1170&b=D1165"` contiene el texto de la sección VSIM (la leyenda base-alta "La coincidencia alta es la norma" y/o "Coinciden en") — confirma que el flip ON llegó al DOM (par real verificado en el dossier: D1170/D1165, n=3655/m=3672). (5) NOTIF OFF: el HTML de una ficha de proyecto y una de parlamentario NO contiene el botón "Seguir" ni "/cuenta". Registrar cada resultado (código HTTP, presencia/ausencia del string) en 104-DEPLOY-RUNBOOK.md. Si Camino A NO es 200 o VSIM NO aparece: reportar como blocker (posible fix emergente + redeploy va en Plan 03, pero un Camino-A-roto se escala AQUÍ).
  </action>
  <verify>
    <automated>curl -s "https://observatorio-congreso.thevalis.workers.dev/comparar?a=D1170&b=D1165" | grep -c "La coincidencia alta es la norma"</automated>
  </verify>
  <acceptance_criteria>
    - `/`, `/parlamentarios`, `/agenda`, `/buscar`, `/metodologia` → cada uno HTTP 200.
    - `/spike-auth` → HTTP 404.
    - Headers de `/` contienen `connect-src 'self'`, `object-src 'none'`, `frame-ancestors 'none'`.
    - `curl -s ".../comparar?a=D1170&b=D1165" | grep -c "La coincidencia alta es la norma"` ≥ 1 (VSIM ON en el DOM).
    - `curl -s ".../comparar?a=D1170&b=D1165" | grep -c "Coinciden en"` ≥ 1.
    - El HTML de una ficha de parlamentario NO contiene "Seguir" como botón de suscripción ni enlace "/cuenta" (NOTIF OFF) — registrado.
  </acceptance_criteria>
  <done>Camino A 200, CSP intacta, /spike-auth 404, VSIM visible en /comparar con caveat base-alta, NOTIF ausente del DOM — todo registrado en 104-DEPLOY-RUNBOOK.md. Base lista para el E2E de Plan 03.</done>
</task>

</tasks>

<verification>
- Build OpenNext Docker EXIT 0 → worker.js.
- wrangler deploy versión nueva; VSIM_PUBLIC_ENABLED=true en secret list; NOTIF/MONEY ausentes.
- Camino A 200 × 5 rutas; /spike-auth 404; CSP intacta.
- /comparar con par real muestra VSIM + caveat base-alta en el DOM.
- .env.example intacto (flip deploy-time).
</verification>

<success_criteria>
El deploy vivo arrastra 101+102+103, VSIM está ON con caveat visible, NOTIF/MONEY OFF, Camino A y CSP intactos — el E2E de Plan 03 puede inventariar el deploy real.
</success_criteria>

<output>
Create `.planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md` (runbook reproducible: comandos exactos, versión de deploy, salida curl) y `104-02-SUMMARY.md` when done.
</output>
