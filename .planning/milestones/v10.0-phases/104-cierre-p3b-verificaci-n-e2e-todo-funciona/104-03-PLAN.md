---
phase: 104-cierre-p3b-verificaci-n-e2e-todo-funciona
plan: 03
type: execute
wave: 3
depends_on: ["104-02"]
files_modified:
  - .planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-E2E-EVIDENCIA.md
  - .planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-HUMAN-UAT.md
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "El panel de actualidad muestra señales vivas con fuente+fecha y cifra de voto honesta (283.550, jamás 'captura' pelado)"
    - "El bloque relaciones en la ficha muestra conteos que cuadran contra recálculo SQL (truncamiento >20 declarado, orden alfabético, jamás ranking)"
    - "El eje VSIM en /comparar muestra 'coinciden en N de M' que cuadra contra recálculo SQL de coincidencia_votos_par para >=2 pares reales, con caveat base-alta visible"
    - "Los flags OFF (NOTIF, MONEY) están AUSENTES del DOM — cero superficie fantasma"
    - "Los empty states son honestos y cero URI-como-partido"
    - "101-HUMAN-UAT.md queda cerrado con los resultados del E2E"
  artifacts:
    - path: ".planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-E2E-EVIDENCIA.md"
      provides: "Inventario E2E por superficie: URL, qué se verificó, SQL vs DOM, refs de screenshot"
      min_lines: 60
    - path: ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-HUMAN-UAT.md"
      provides: "UAT de Phase 101 cerrado con resultados del deploy real"
      contains: "result:"
  key_links:
    - from: "104-E2E-EVIDENCIA.md (conteo relaciones)"
      to: "psql recálculo (comisiones/co-autoría/militancia)"
      via: "SQL vs DOM cross-check"
      pattern: "psql"
    - from: "104-E2E-EVIDENCIA.md (VSIM N/M)"
      to: "coincidencia_votos_par recálculo SQL"
      via: "cuadra para >=2 pares reales"
      pattern: "coincidencia_votos_par"
---

<objective>
El inventario E2E que pidió el operador ("asegúrate que TODO funciona"): cada superficie nueva de v10.0 × dato real × BrowserOS sobre el DEPLOY de Plan 02, con cross-checks contra SQL directo. Verificar panel (señales vivas + cifra voto honesta), relaciones en ficha (conteos vs SQL, truncamiento, orden alfabético), /comparar (4 ejes + VSIM ON con caveat, similitud cuadra contra recálculo SQL), flags OFF ausentes del DOM, empty states honestos, cero URI-como-partido. Cerrar 101-HUMAN-UAT.md. Fixes emergentes + redeploy si el E2E rompe una superficie.

Purpose: Es el cierre del milestone v10.0. Sin este inventario exhaustivo no hay evidencia de que las tres pasadas funcionan sobre el deploy real.
Output: 104-E2E-EVIDENCIA.md (inventario por superficie), 101-HUMAN-UAT.md cerrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-HUMAN-UAT.md
@docs/legal/102-LEGAL-DOSSIER-VSIM.md
@app/app/comparar/page.tsx

<interfaces>
<!-- Herramientas: BrowserOS MCP (lectura fría del deploy real, getComputedStyle para cascada CSS, -->
<!-- iframe same-origin para móvil 390px); scripts/bros-cli.mjs; psql con .env (PGCLIENTENCODING=UTF8). -->

Worker URL: https://observatorio-congreso.thevalis.workers.dev
Supabase PROD ref: bctyygbmqcvizyplktuw. Usar $SUPABASE_DB_URL de .env con PGCLIENTENCODING=UTF8.

Vocabulario LOCKED del panel (skill spike-findings-98 — RESPETAR en la verificación):
- "captura" pelado PROHIBIDO → "según fuente al"; fecha_captura JAMÁS es fecha de un hecho.
- Cifra de voto = 283.550 confirmados / 186 parlamentarios / 4.852 votaciones (NO 548.642).
- Anti-ranking T-52-13: PROHIBIDO "top/los más/la cámara más activa"; framing "N trámites en 7 días".
- Ausencia ≠ hecho: fuente stale → "sin datos frescos de esta fuente", jamás "sin movimiento".

RPC VSIM (0068) — recálculo SQL de referencia (denominador SUSTANTIVA):
  select * from public.coincidencia_votos_par('D1170','D1165');
  -- devuelve (n_coinciden, m_compartidas, fecha_captura_max). Par de referencia del dossier:
  -- n_coinciden=3655, m_compartidas=3672. Caveat base-alta LOCKED:
  -- "La coincidencia alta es la norma, no una señal ...".

RPCs de relaciones en la ficha (conteos honestos, cap 20, total_n antes del cap):
  - militancia_historica_compartida(p_id)  (0067 net-new-only)
  - comisiones_de_parlamentario(p_id)      (0060, cap 50)
  - coautores_de_parlamentario(p_id)       (0061, cap 20, n_proyectos)
  Bloque en ficha = <section id="relaciones"> above-the-fold, grid 2×2, 5 bloques
  (partido, zona solo-Senado, comisiones, co-autoría, militancia histórica).

/comparar: 4 ejes factuales (militancia/comisiones/co-autoría/zona) + 5º eje VSIM gated.
  URL: /comparar?a=<id>&b=<id> (ids ordenados alfabéticamente, force-dynamic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E panel de actualidad + bloque relaciones en ficha (BrowserOS × SQL)</name>
  <read_first>
    - .claude/skills/spike-findings-98/SKILL.md (vocabulario LOCKED del panel)
    - .planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-DEPLOY-RUNBOOK.md (versión de deploy verificada)
    - app/app/comparar/page.tsx (conteos honestos: total_n antes del cap, orden alfabético)
  </read_first>
  <action>
    (A) PANEL: abrir con BrowserOS la home `/` del deploy vivo. Verificar: las señales SEN vivas presentes (actividad reciente, agenda próxima, urgencias, archivados/retirados — las que el materializador emite hoy); CADA tile lleva fuente + fecha; el framing es "N trámites en 7 días" (NUNCA "top/los más/la cámara más activa" — grep del HTML servido por el string prohibido = 0); si alguna señal está suprimida, muestra "sin datos frescos de esta fuente" (nunca "sin movimiento"); NINGÚN "captura" pelado en el DOM (solo "según fuente al"). Cross-check contra SQL: `psql "$SUPABASE_DB_URL" -tA -c "select tipo_senal, count(*) from actualidad_senal group by tipo_senal"` y confirmar que el panel refleja filas vivas (no vacío total). Si la cifra de voto aparece en algún tile, confirmar que es 283.550 (jamás 548.642).
    (B) RELACIONES EN FICHA: elegir 2 parlamentarios reales — uno con muchas relaciones (para probar truncamiento >20) y uno con pocas. Abrir con BrowserOS `/parlamentario/<id>`. Verificar: `<section id="relaciones">` visible above-the-fold (inmediatamente tras header/bio), grid 2×2 con los 5 bloques; conteos honestos con total_n; orden alfabético (NUNCA ranking); leyenda factual; cuando total_n > 20, se declara el truncamiento ("Ver el detalle en la ficha" / "posiblemente truncada"). Cross-check SQL: para cada bloque, `psql -tA -c "select * from public.comisiones_de_parlamentario('<id>')"` (y militancia/coautores) y confirmar que el conteo mostrado en el DOM cuadra con el total_n de la RPC. Verificar CERO URI-como-partido (ningún http:// como valor de partido). Usar getComputedStyle vía BrowserOS para confirmar que el grid NO tiene doble-espaciado mt-12 dentro (neutralización `[&>section]:mt-0`). Registrar cada verificación en 104-E2E-EVIDENCIA.md (URL, qué se verificó, SQL vs DOM, ref de screenshot BrowserOS).
    Si el E2E encuentra un defecto que rompe una superficie: fix inmediato + redeploy (reusar el runbook de Plan 02) dentro de esta fase — documentar el fix.
  </action>
  <verify>
    <automated>bash -c 'PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from actualidad_senal" | grep -qE "[1-9]" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Panel: home `/` renderiza señales vivas; grep del HTML servido por "top"/"los más"/"la cámara más activa" (como framing de señal) = 0; ningún "captura" pelado; si hay cifra de voto, es 283.550.
    - `psql ... "select count(*) from actualidad_senal"` > 0 y el panel refleja esas filas.
    - Ficha: `<section id="relaciones">` presente above-the-fold con grid 2×2; conteo del DOM de cada bloque == total_n de la RPC correspondiente (verificado por psql para 2 parlamentarios).
    - Para un parlamentario con >20 en algún eje: el DOM declara el truncamiento; el conteo mostrado es total_n (NO el .length cap-eado a 20).
    - Cero URI-como-partido en el DOM de las fichas verificadas.
    - Todo registrado en 104-E2E-EVIDENCIA.md con SQL vs DOM y refs de screenshot.
  </acceptance_criteria>
  <done>Panel con señales vivas y vocabulario honesto verificado; bloque relaciones en 2 fichas con conteos que cuadran contra SQL, truncamiento >20 declarado, orden alfabético, cero URI-como-partido — todo en 104-E2E-EVIDENCIA.md.</done>
</task>

<task type="auto">
  <name>Task 2: E2E /comparar (4 ejes + VSIM ON con caveat, similitud cuadra vs SQL) + flags OFF ausentes del DOM</name>
  <read_first>
    - app/app/comparar/page.tsx (los 5 ejes; el 5º VSIM gated por vsimPublicEnabled)
    - docs/legal/102-LEGAL-DOSSIER-VSIM.md (§1 métrica, §2 caveat VERBATIM, §4 cobertura 80/20, §8 par real D1170/D1165)
  </read_first>
  <action>
    (A) /comparar 4 EJES: abrir con BrowserOS `/comparar?a=<idA>&b=<idB>` con dos parlamentarios reales. Verificar los 4 ejes factuales (militancia histórica, comisiones, co-autoría, zona) con intersección honesta: incluir un par con >20 co-autores (sin falsa ausencia → "indeterminado" declarado), un par diputado/senador con comisión homónima (NO "comparten" — identidad compuesta camara+nombre), y dos diputados (zona "no aplica" honesto). Cada dato con fuente+fecha.
    (B) VSIM ON: en el mismo /comparar, verificar la 5ª sección (VSIM) presente con el flag ON. Debe mostrar "Coinciden en {N} de {M} votaciones compartidas ({X}%)" + el caveat base-alta VERBATIM ("La coincidencia alta es la norma, no una señal...") adyacente y no colapsable + la cobertura declarada (Cámara ~80% / Senado ~20%). La figura es NEUTRAL (sin barra/gauge/semáforo). CROSS-CHECK SQL para >=2 PARES REALES: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select * from public.coincidencia_votos_par('<idA>','<idB>')"` y confirmar que el N/M mostrado en el DOM == el recálculo SQL (par de referencia del dossier: D1170/D1165 → n=3655, m=3672; usar ese + un segundo par distinto). Verificar el estado M=0 con un par sin votaciones compartidas → "Sin votaciones compartidas suficientes..." (NUNCA "0%").
    (C) FLAGS OFF ausentes del DOM: (1) NOTIF — `curl -s` (o BrowserOS) del HTML de una ficha de proyecto Y una de parlamentario NO contiene el botón "Seguir" ni enlace "/cuenta"; `/cuenta` devuelve 404 o no renderiza el login. (2) MONEY — las superficies MONEY (contratos/aportes) están ausentes del DOM de la ficha. Grep del HTML servido: "Seguir" (como botón de suscripción) = 0, superficie MONEY = 0.
    Registrar cada verificación en 104-E2E-EVIDENCIA.md (URL, string DOM, recálculo SQL, ref de screenshot). Fix emergente + redeploy si algo rompe.
  </action>
  <verify>
    <automated>bash -c 'PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select n_coinciden, m_compartidas from public.coincidencia_votos_par('"'"'D1170'"'"','"'"'D1165'"'"')" | grep -q "3655" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `/comparar?a=&b=` con par real muestra los 4 ejes factuales; par >20 co-autores → "indeterminado" (no falsa ausencia); par cross-cámara con comisión homónima → NO "comparten"; dos diputados → zona "no aplica".
    - La 5ª sección VSIM está PRESENTE (flag ON) con "Coinciden en N de M" + caveat base-alta VERBATIM adyacente + cobertura 80/20.
    - Para >=2 pares reales, el N/M del DOM == recálculo SQL de `coincidencia_votos_par` (D1170/D1165 → n=3655/m=3672 confirmado por psql).
    - Estado M=0 muestra "Sin votaciones compartidas suficientes..." (grep del HTML por "0%" en esa sección = 0).
    - NOTIF OFF: HTML de ficha proyecto + parlamentario sin botón "Seguir" ni "/cuenta"; `/cuenta` no renderiza login.
    - MONEY OFF: superficies MONEY ausentes del DOM.
    - Todo registrado en 104-E2E-EVIDENCIA.md.
  </acceptance_criteria>
  <done>/comparar con 4 ejes honestos + VSIM ON con caveat base-alta visible y N/M que cuadra contra SQL para >=2 pares; flags NOTIF/MONEY ausentes del DOM — todo en 104-E2E-EVIDENCIA.md.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar 101-HUMAN-UAT + consolidar evidencia + re-correr guards contra el deploy</name>
  <read_first>
    - .planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-HUMAN-UAT.md (los 3 tests pending que este E2E cierra)
    - .planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-E2E-EVIDENCIA.md (evidencia de Tasks 1-2)
  </read_first>
  <action>
    (A) Cerrar 101-HUMAN-UAT.md: los 3 tests pending (grid relaciones 5-bloques above-the-fold con getComputedStyle; /comparar A/B con reales incluyendo par >20 co-autores y par cross-cámara; CTA "Comparar con otro parlamentario" desde la ficha pre-llena slot A) quedan cubiertos por el E2E de Tasks 1-2. Verificar el test #3 (CTA) con BrowserOS: navegar a `/parlamentario/<id>`, clickear el CTA "Comparar con otro parlamentario", confirmar que navega a `/comparar` con el slot A pre-llenado con ese parlamentario. Actualizar cada `result: [pending]` → `result: pass` (o `issue` con detalle si algo falló) + actualizar el bloque Summary (passed/pending) + `status: complete` en el front-matter.
    (B) Consolidar 104-E2E-EVIDENCIA.md: asegurar que el documento tiene una sección por superficie (panel, relaciones-ficha, /comparar-4-ejes, VSIM, flags-OFF) con URL, qué se verificó, SQL vs DOM, y refs de screenshot BrowserOS; una tabla resumen al final (superficie | verificado | resultado).
    (C) Re-correr los guards y el linter anti-insinuación una última vez contra el árbol post-deploy para confirmar que ningún fix emergente los rompió: `cd app && pnpm exec vitest run lib/anti-insinuacion-guard.test.ts lib/vsim-antiflip-guard.test.ts lib/notif-antiflip-guard.test.ts lib/lockdown-guard.test.ts lib/bento-guards.test.ts`. Si hubo fixes emergentes en Tasks 1-2, correr la suite completa `cd app && pnpm exec vitest run` de nuevo.
  </action>
  <verify>
    <automated>bash -c 'grep -c "result: pass" ".planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-HUMAN-UAT.md"'</automated>
  </verify>
  <acceptance_criteria>
    - 101-HUMAN-UAT.md: los 3 tests tienen `result: pass` (o `issue` documentado); Summary actualizado (pending=0); front-matter `status: complete`.
    - El CTA "Comparar con otro parlamentario" verificado con BrowserOS: navega a /comparar con slot A pre-llenado.
    - 104-E2E-EVIDENCIA.md tiene una sección por superficie + tabla resumen final (≥ 60 líneas).
    - `cd app && pnpm exec vitest run lib/anti-insinuacion-guard.test.ts lib/vsim-antiflip-guard.test.ts lib/notif-antiflip-guard.test.ts lib/lockdown-guard.test.ts lib/bento-guards.test.ts` → EXIT 0.
    - Si hubo fixes emergentes: suite completa `cd app && pnpm exec vitest run` verde de nuevo.
  </acceptance_criteria>
  <done>101-HUMAN-UAT cerrado (3/3 pass); 104-E2E-EVIDENCIA consolidado con sección por superficie + tabla resumen; guards + linter verdes contra el árbol post-deploy. Milestone v10.0 verificado E2E.</done>
</task>

</tasks>

<verification>
- Panel: señales vivas + fuente/fecha + vocabulario honesto (sin ranking, sin "captura" pelado, cifra 283.550).
- Relaciones ficha: conteos == total_n SQL; truncamiento >20 declarado; orden alfabético; cero URI-como-partido.
- /comparar: 4 ejes honestos + VSIM ON con caveat base-alta + N/M == recálculo SQL para >=2 pares.
- Flags OFF: NOTIF y MONEY ausentes del DOM.
- 101-HUMAN-UAT cerrado; 104-E2E-EVIDENCIA consolidado; guards + linter verdes.
</verification>

<success_criteria>
Cada superficie nueva de v10.0 verificada × dato real × BrowserOS sobre el deploy, con similitud y conteos que cuadran contra SQL, flags OFF ausentes del DOM, empty states honestos, guards verdes — el operador tiene el inventario "todo funciona" que pidió.
</success_criteria>

<output>
Create `.planning/phases/104-cierre-p3b-verificaci-n-e2e-todo-funciona/104-E2E-EVIDENCIA.md` (inventario por superficie) + actualizar `101-HUMAN-UAT.md` (cerrado) + `104-03-SUMMARY.md` when done.
</output>
