---
phase: 101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali
plan: 03
type: execute
wave: 3
depends_on: ["101-01", "101-02"]
files_modified:
  - supabase/migrations/0067_militancia_historica_compartida.sql
  - supabase/tests/0067_militancia_historica_compartida.test.sql
  - app/app/comparar/page.tsx
  - app/app/comparar/page.test.tsx
  - app/components/comparar-selector.tsx
  - app/components/relaciones-eje-comparar.tsx
autonomous: true
requirements: [REL-03, REL-04]
must_haves:
  truths:
    - "La RPC militancia_historica_compartida existe en PROD (secdef, alias-keyed, total_n, anon sin execute) y pgTAP la valida contra el schema aplicado"
    - "Existe /comparar?a=&b= force-dynamic que lee searchParams ANTES de notFound, orden canónico alfabético"
    - "Los 4 ejes factuales no-voto (militancia histórica, comisiones, co-autoría, zona) muestran A/B + intersección con fuente+fecha"
    - "Vacíos honestos declarados ('en las fuentes consultadas al [fecha]'); error de RPC LANZA, jamás degrada a 'sin relaciones'"
  artifacts:
    - path: "supabase/migrations/0067_militancia_historica_compartida.sql"
      provides: "RPC secdef alias-keyed net-new militancia histórica (mold 0061)"
      contains: "militancia_historica_compartida"
    - path: "supabase/tests/0067_militancia_historica_compartida.test.sql"
      provides: "pgTAP: has_function, prosecdef, anon sin execute, total_n"
      min_lines: 8
    - path: "app/app/comparar/page.tsx"
      provides: "Ruta /comparar force-dynamic con 4 ejes + intersección server-side"
      min_lines: 40
  key_links:
    - from: "app/app/comparar/page.tsx"
      to: "militancia_historica_compartida"
      via: ".rpc() server-side"
      pattern: "militancia_historica_compartida"
    - from: "app/app/comparar/page.tsx"
      to: "searchParams"
      via: "await searchParams antes de notFound (Phase 45)"
      pattern: "await searchParams"
---

<objective>
Cerrar REL-04 (relación nueva militancia histórica compartida) y REL-03 (/comparar 1-a-1 no-voto). Crear la migración aditiva `0067_militancia_historica_compartida.sql` (RPC security-definer keyed on `partido_alias`, mold 0061 VERBATIM, net-new-only), aplicarla a PROD por psql --single-transaction, validarla con pgTAP contra el schema aplicado, y construir la ruta `/comparar?a=&b=` (force-dynamic, orden canónico alfabético, 4 ejes factuales con intersección server-side, fuente+fecha por dato, vacíos honestos, error≠vacío).

Purpose: Militancia histórica compartida (696 net-new pares) NO es derivable de las RPC existentes (display partido ≠ partido_alias — Pitfall 1); necesita una RPC nueva que respete el mismo molde de seguridad. /comparar es el eje 1-a-1 que falta, reusando el directorio y las RPC existentes para las intersecciones.
Output: migración 0067 + pgTAP aplicados a PROD; ruta /comparar + selectores + componente de eje; el 5º bloque militancia histórica montado en la ficha si el audit lo sostiene.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-RESEARCH.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-PATTERNS.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-UI-SPEC.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-01-SUMMARY.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-02-SUMMARY.md

<interfaces>
<!-- Contratos verificados en codebase (RESEARCH/PATTERNS) -->

RPC nueva 0067 — molde VERBATIM de 0061 §E.1 copartidarios (RESEARCH § Pattern 1 / PATTERNS §0067):
  returns table (id text, nombre text, camara text, total_n bigint)
  language sql stable security definer set search_path = ''
  join m2 on m2.partido_alias = m1.partido_alias  -- KEY on ALIAS, no display (Pitfall 1)
  net-new-only: EXCLUIR pares que ya comparten el alias VIGENTE (el bloque "Del mismo partido")
  order by d.nombre, limit 20, count(*) over () as total_n
  double-revoke: from public + from anon, authenticated; ZERO grant
  emite SOLO id/nombre/camara/total_n — NUNCA rut/email/partido_alias

RPCs existentes en PROD para /comparar (0060/0061, ya allowlisted):
  parlamentarios_publico_v2()               → directorio con partido (selector)
  militancias_de_parlamentario(p_id)        → militancias display (NO usar para intersección alias)
  comisiones_de_parlamentario(p_id)         → returns nombre → intersectar por set de nombre (server)
  coautores_de_parlamentario(p_id)          → rows con id + n_proyectos → buscar b.id (count)

force-dynamic + searchParams (Phase 45 gotcha, VERBATIM ficha page.tsx:201-210):
  export const dynamic = "force-dynamic";
  const sp = await searchParams;                        // read FIRST — nunca notFound() antes
  const [a, b] = [sp.a, sp.b].filter(Boolean).sort();   // orden canónico alfabético
  // sin params → selectores vacíos (NO notFound)
  validar a/b contra PARLAMENTARIO_ID_RE (import from "@/lib/buscar") ANTES de cualquier .rpc()

Lectura RPC server-only (#34, VERBATIM parlamentarios/page.tsx:116-121):
  const sb = createServerSupabase();
  const { data, error } = await sb.rpc(rpc, { p_id: id });
  if (error) throw new Error(`${rpc} falló: ${error.message}`);  // error, jamás "sin resultados"
  return (data ?? []);                                            // [] = vacío honesto

Layout container (ambos análogos): <main className="max-w-[1120px] mx-auto px-4 md:px-8 py-8 md:py-16">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0067 militancia_historica_compartida (secdef alias-keyed, net-new) + apply PROD + pgTAP + 5º bloque en la ficha (REL-04)</name>
  <files>supabase/migrations/0067_militancia_historica_compartida.sql, supabase/tests/0067_militancia_historica_compartida.test.sql, app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - supabase/migrations/0061_cross_links_conteo_honesto_orden.sql § copartidarios_de_parlamentario (molde VERBATIM: header LOCKED, secdef, search_path='', double-revoke, total_n)
    - supabase/tests/0061_cross_links_conteo_honesto_orden.test.sql (molde pgTAP: has_function, prosecdef, has_function_privilege false, total_n regex)
    - .planning/phases/101-.../101-RESEARCH.md § Pattern 1 (0067 SQL completo) + Pitfall 1 (alias≠display) + Open Question 1 (net-new-only 696 vs shared-ever 1966)
    - .planning/phases/101-.../101-01-SUMMARY.md (decisión net-new/shared-ever + N real de la corrida)
    - .planning/phases/101-.../101-UI-SPEC.md § Copy table (5º bloque: heading "Compartieron militancia en un partido", coverage "En las militancias registradas al {fecha}: {N}…")
    - app/components/cross-links-parlamentario.tsx (CrossLinkBloque para reusar el 5º bloque con el mismo contrato) + app/app/parlamentario/[id]/page.tsx crossLinkReader
    - CLAUDE.md § Conventions (apply psql --single-transaction, PGCLIENTENCODING=UTF8, nunca db push; pgTAP = única prueba DDL válida)
  </read_first>
  <action>
    Crear `supabase/migrations/0067_militancia_historica_compartida.sql` copiando el molde 0061 §E.1 VERBATIM (RESEARCH § Pattern 1). Elementos LOCKED: `drop function if exists` antes de create; `security definer set search_path = ''`; nombres schema-qualified (`public.parlamentario_militancia`, `public.parlamentario`); `p_id text` parametrizado; `count(*) over () as total_n`; `order by d.nombre`; `limit 20`; double-revoke (`from public` + `from anon, authenticated`); ZERO grant. La clave del join es `m2.partido_alias = m1.partido_alias` (NUNCA el display `partido`). Emitir SOLO `id/nombre/camara/total_n` — jamás rut/email/partido_alias.
    NET-NEW-ONLY (per decisión del audit 101-01-SUMMARY, recomendación 696): EXCLUIR los pares que ya comparten el alias VIGENTE (los que salen en copartidarios_de_parlamentario). Implementar con un `not exists` / `except` contra el sub-conjunto es_actual sobre el mismo alias, o el filtro equivalente que el audit fijó. Si el audit eligió shared-ever, seguir esa decisión.

    Aplicar a PROD (LOCKED): `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0067_militancia_historica_compartida.sql`. NUNCA `supabase db push`. Precedente aditivo 0059-0066 (el agente PUEDE aplicar migraciones aditivas).

    Crear `supabase/tests/0067_militancia_historica_compartida.test.sql` (molde 0061, plan(4)): has_function('public','militancia_historica_compartida',ARRAY['text']); prosecdef=true; has_function_privilege('anon', ...,'execute')=false; pg_get_function_result ~ total_n. Correr contra el SCHEMA APLICADO: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0067_militancia_historica_compartida.test.sql` — debe dar 4 ok / 0 not ok. pgTAP es la ÚNICA prueba DDL válida (Pitfall 6); typecheck no prueba que Postgres corrió el DDL.

    5º bloque en la ficha (SOLO si el audit 101-01 sostiene sustancia — 696 net-new): reusar el patrón crossLinkReader + CrossLinkBloque para montar un `CrossLinkMilitanciaHistorica` que lea `militancia_historica_compartida` y se una a los otros 4 dentro de la `<section id=relaciones>` (grid, ahora 5 bloques). Heading "Compartieron militancia en un partido" (pasado, factual, NO "aliados/cercanos"). Coverage declarada del audit: "En las militancias registradas al {fecha}: {N} parlamentarios compartieron militancia en algún partido." NO añadir la RPC a NEGACIONES_LOCKED salvo que su leyenda niegue un término prohibido; el heading no lo hace. (La allowlist ya la puso Plan 02.)
    Si el audit marcó el dato como no-sustancial, NO montar el 5º bloque en la ficha — dejarlo solo como eje de /comparar (Task 2) y documentar la omisión.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0067_militancia_historica_compartida.test.sql 2>&1 | grep -c "^ok" ; cd app && pnpm test -- --run lockdown-guard 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0067_militancia_historica_compartida.sql` existe con secdef, search_path='', join por `partido_alias` (no display), total_n via count(*) over (), double-revoke, ZERO grant, limit 20, order by nombre.
    - La migración está APLICADA a PROD (verificable: `psql -tA -c "select prosecdef from pg_proc where proname='militancia_historica_compartida'"` devuelve `t`).
    - `supabase/tests/0067_*.test.sql` corre contra el schema aplicado con 4 ok / 0 not ok (has_function, prosecdef, anon sin execute, total_n).
    - La RPC emite SOLO id/nombre/camara/total_n (grep confirma que partido_alias/rut/email no están en el returns table).
    - Net-new-only implementado (excluye pares del alias vigente) per decisión del audit; o shared-ever si el audit lo eligió.
    - 5º bloque montado en la ficha SOLO si el audit lo sostiene, con heading exacto y coverage declarada del N; lockdown-guard verde (allowlist ya presente de Plan 02).
  </acceptance_criteria>
  <done>0067 aplicada a PROD y validada por pgTAP 4/4; la RPC es secdef alias-keyed net-new PII-safe; el 5º bloque militancia histórica está en la ficha (o documentado como omitido) con cobertura declarada.</done>
</task>

<task type="auto">
  <name>Task 2: Ruta /comparar force-dynamic + selectores + 4 ejes con intersección server-side (REL-03)</name>
  <files>app/app/comparar/page.tsx, app/app/comparar/page.test.tsx, app/components/comparar-selector.tsx, app/components/relaciones-eje-comparar.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx:201-210 (force-dynamic + await searchParams antes de notFound + PARLAMENTARIO_ID_RE — patrón VERBATIM a copiar)
    - app/app/parlamentarios/page.tsx (selector: DirectoryList lee parlamentarios_publico_v2 ~116-121, DirectoryFilter GET form ~67-106, error≠vacío) + app/components/parlamentarios-filtro.tsx
    - app/lib/buscar.ts (export PARLAMENTARIO_ID_RE)
    - .planning/phases/101-.../101-PATTERNS.md § "app/app/comparar/page.tsx" (force-dynamic, canonical URL, input validation V5, selector reuse, server-side intersection por eje, layout container) + § relaciones-eje-comparar.tsx
    - .planning/phases/101-.../101-UI-SPEC.md § "/comparar route" + Copy table (headings ejes, intersección "Comparten {N}: {lista}", per-datum provenance, empty states, error state) + § Color (petróleo SOLO en links/intersección-figura)
    - .planning/phases/101-.../101-01-SUMMARY.md (zona-gap: dos diputados = "no comparten zona" honesto)
  </read_first>
  <action>
    Crear `app/app/comparar/page.tsx` (Server Component). `export const dynamic = "force-dynamic"`. Leer `searchParams` (Promise) PRIMERO con `await`; orden canónico `const [a, b] = [sp.a, sp.b].filter(Boolean).sort()` (URL estable). Sin params → selectores vacíos (NO notFound). Validar a/b contra `PARLAMENTARIO_ID_RE` (import from "@/lib/buscar") ANTES de cualquier .rpc(); un id inválido → tratar como no-seleccionado (selector vacío), no 500. Layout `<main className="max-w-[1120px] mx-auto px-4 md:px-8 py-8 md:py-16">`. Heading "Comparar dos parlamentarios".

    Selectores (crear `app/components/comparar-selector.tsx`): dos selectores server-friendly reusando `parlamentarios_publico_v2` (lectura server-only vía createServerSupabase, error THROWS), búsqueda por nombre, modelados en DirectoryFilter (GET form, progressive enhancement). Labels "Parlamentario A" / "Parlamentario B".

    Cuando a Y b están presentes y válidos, computar los 4 ejes SERVER-SIDE (crear `app/components/relaciones-eje-comparar.tsx` para el render A/B por eje, o render inline — Discreción):
    - MILITANCIA (histórica): leer `militancia_historica_compartida(a)` y buscar b en el resultado (o leer para a y b y computar intersección por id). Heading "Militancia (histórica)".
    - COMISIONES: leer `comisiones_de_parlamentario(a)` + `(b)`, intersectar por SET de `nombre`. Heading "Comisiones". Intersección "Comparten {N} comisiones: {lista alfabética}".
    - CO-AUTORÍA: leer `coautores_de_parlamentario(a)`, buscar `b.id`, leer `n_proyectos` (count). Heading "Co-autoría de proyectos". "Proyectos en común con enlace": la RPC NO devuelve la lista de boletines (Claude's Discretion). DECISIÓN: mostrar count-only con provenance si no se crea RPC de boletines; NO fabricar el enlace. Documentar la decisión (count-only vs RPC boletines_compartidos nueva) en el SUMMARY. Preferir count-only para no expandir el alcance salvo que el UI-SPEC lo exija duro.
    - ZONA: comparar `circunscripcion`/`distrito` de `parlamentarios_publico_v2`. Dos diputados → NULL → "En las fuentes consultadas al {fecha}, no comparten zona." (ausencia declarada, NUNCA implícita). Heading "Zona electoral".
    Cada dato lleva fuente + fecha en `text-xs text-muted-foreground` ("Fuente: BCN · captura {fecha}"). Render lado-a-lado A/B (md:grid-cols-2), intersección full-width debajo. La figura de intersección en `text-accent-product` (petróleo NEUTRAL de conteo, jamás señal de afinidad). Orden alfabético en las listas; CERO ranking/score.
    Reusar `React.cache` por (rpc, id) para deduplicar cuando A y B leen la misma RPC.
    Empty state sin selección (UI-SPEC): heading "Elige dos parlamentarios para compararlos." + body "Selecciona un parlamentario en cada columna. La comparación muestra solo hechos con fuente y fecha; no ordena ni puntúa." Error de RPC LANZA (#34), jamás degrada a "sin relaciones".
    Añadir el link "Comparar con otro parlamentario" en la ficha (`/comparar?a=${id}`, pre-llena slot A) modelado en el <nav> gated de page.tsx:253-262 (navegación pura, no cruza la frontera anti-insinuación).

    Crear `app/app/comparar/page.test.tsx` (RTL): (1) force-dynamic exportado; (2) sin params → empty state (no notFound/500); (3) orden canónico (a/b ordenados alfabéticamente en la URL/lectura); (4) con a+b válidos → intersección factual renderiza por eje; (5) zona de dos diputados → "no comparten zona"; (6) RPC-error LANZA (mock error → throw), no "sin relaciones". Candados: cero-hex, tokens tipográficos.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run comparar anti-insinuacion-guard lockdown-guard 2>&1 | tail -20 && npx tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/comparar/page.tsx` existe con `export const dynamic = "force-dynamic"`, `await searchParams` ANTES de cualquier notFound, orden canónico alfabético (.sort()), validación PARLAMENTARIO_ID_RE antes de .rpc().
    - Los 4 ejes (militancia histórica, comisiones, co-autoría, zona) renderizan A/B + línea de intersección factual; cada dato con fuente+fecha.
    - Sin params → empty state con el copy exacto del UI-SPEC (NO 404/500); zona de dos diputados → "no comparten zona" declarado.
    - Error de RPC LANZA (test mock lo prueba), jamás "sin relaciones"; [] = vacío honesto.
    - Link "Comparar con otro parlamentario" en la ficha pre-llena `/comparar?a=${id}`.
    - `pnpm test -- --run comparar anti-insinuacion-guard lockdown-guard` verde + `tsc -b` limpio; SUPERFICIES_RELACIONES ahora MUERDE sobre comparar/page.tsx (ya no tolerado-ausente).
    - CERO ranking/score; petróleo SOLO en links/figura de intersección; orden alfabético en listas.
  </acceptance_criteria>
  <done>/comparar force-dynamic con 4 ejes factuales no-voto e intersección server-side existe; vacíos honestos y error≠vacío; el link desde la ficha pre-llena el slot A; guards + tsc verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searchParams (a/b)→RPC | input no confiable del cliente cruza a la query |
| RPC secdef→sitio público | la 0067 corre con privilegios de definer; ACL deny-by-default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-07 | Tampering (SQLi vía a/b) | /comparar params | mitigate | `.rpc()` parametrizado (p_id) + validación PARLAMENTARIO_ID_RE antes de la query; nunca string-interpolación |
| T-101-08 | DoS (RPC no acotada) | 0067 | mitigate | limit 20 + statement_timeout patrón 0064; secdef search_path='' |
| T-101-09 | Info disclosure (PII) | 0067 returns | mitigate | emite SOLO id/nombre/camara/total_n; NUNCA rut/email/partido_alias (interno) |
| T-101-10 | Elevation (RPC admin no allowlist) | árbol público | mitigate | militancia_historica_compartida ya en PUBLIC_RPC_ALLOWLIST (Plan 02); Direction-A verde |
| T-101-11 | Error handling (#34 vacío≠error) | lecturas /comparar | mitigate | error de RPC LANZA (UI de error honesta); [] = vacío honesto; test lo prueba |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — CERO paquetes nuevos (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- pgTAP 0067: 4 ok / 0 not ok contra schema aplicado; prosecdef=t en PROD.
- `pnpm test -- --run comparar relaciones-section anti-insinuacion-guard lockdown-guard` verde.
- `tsc -b` sin errores.
- SUPERFICIES_RELACIONES ahora muerde sobre comparar/page.tsx (archivo presente).
- Grep: 0067 no expone partido_alias/rut/email; /comparar valida PARLAMENTARIO_ID_RE + await searchParams antes de notFound.
</verification>

<success_criteria>
- REL-04: RPC militancia histórica compartida en PROD (secdef alias-keyed net-new, pgTAP 4/4), 5º bloque en la ficha si el audit lo sostiene, cobertura declarada.
- REL-03: /comparar 1-a-1 con 4 ejes factuales no-voto, fuente+fecha por dato, orden alfabético, vacíos honestos, error≠vacío, force-dynamic correcto.
- BrowserOS sobre el deploy real (block above-fold + /comparar A/B) = gate del orquestador tras deploy.
</success_criteria>

<output>
Create `.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-03-SUMMARY.md` when done. Registrar: net-new vs shared-ever elegido para 0067, si el 5º bloque entró a la ficha, la decisión co-autoría (count-only vs RPC boletines_compartidos), y el estado del gate BrowserOS pendiente (deploy).
</output>
