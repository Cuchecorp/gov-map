---
phase: 101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/lockdown-guard.test.ts
  - app/components/relaciones-section.tsx
  - app/components/relaciones-section.test.tsx
  - app/app/parlamentario/[id]/page.tsx
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "El linter anti-insinuación registra las superficies nuevas (relaciones + comparar) ANTES de que aterrice cualquier copy nuevo"
    - "El guard lockdown allowlista militancia_historica_compartida ANTES de que Plan 03 monte la RPC (Direction-B)"
    - "La ficha muestra los 4 cross-links en un bloque <section id=relaciones> above-the-fold, con heading y leyenda de grupo"
    - "Los CrossLinkBloque internos quedan byte-intactos (leyenda LOCKED, orden neutral, total_n honesto, mt-12 frontier)"
  artifacts:
    - path: "app/components/relaciones-section.tsx"
      provides: "Wrapper <section id=relaciones> con heading, leyenda de grupo, grid 2x2 de los 4 CrossLinkBloque"
      min_lines: 25
    - path: "app/components/relaciones-section.test.tsx"
      provides: "RTL: sección presente, 4 bloques, leyenda de grupo, mt-12 preservado"
      min_lines: 20
  key_links:
    - from: "app/app/parlamentario/[id]/page.tsx"
      to: "app/components/relaciones-section.tsx"
      via: "import + montaje above-the-fold"
      pattern: "RelacionesSection"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "SUPERFICIES_RELACIONES"
      via: "array + scan loop"
      pattern: "SUPERFICIES_RELACIONES"
---

<objective>
Wave 0 de la fase: extender los guards (anti-insinuación + lockdown) para cubrir TODAS las superficies nuevas de Phase 101 (relaciones-section, comparar, comparar-selector, relaciones-eje-comparar) y allowlistar la RPC `militancia_historica_compartida` — ANTES de que ningún copy o RPC nuevo aterrice. Luego, des-enterrar por COMPOSICIÓN el bloque de relaciones en la ficha (REL-02): envolver los 4 CrossLinkBloque ya montados en un `<section id="relaciones">` above-the-fold con heading + leyenda de grupo + grid 2×2, dejando los componentes internos byte-intactos.

Purpose: Wave 0 garantiza que el linter MUERDE en las superficies nuevas antes del copy (patrón Phase 100) y que Plan 03 no falle el guard Direction-B por RPC no allowlistada. La composición saca a la superficie relaciones que ya existen pero están enterradas al final de la columna.
Output: guards extendidos (2 archivos), `relaciones-section.tsx` + su test, y la ficha `page.tsx` re-compuesta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-PATTERNS.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-UI-SPEC.md
@.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-01-SUMMARY.md

<interfaces>
<!-- Contratos que el ejecutor necesita — extraídos del codebase por PATTERNS/RESEARCH -->

Ficha mount actual (app/app/parlamentario/[id]/page.tsx:283-294) — los 4 bloques a reubicar+envolver:
  <Suspense fallback={null}><CrossLinkCopartidarios id={id} /></Suspense>
  <Suspense fallback={null}><CrossLinkMismaZona id={id} /></Suspense>
  <Suspense fallback={null}><CrossLinkCoComisionados id={id} /></Suspense>
  <Suspense fallback={null}><CrossLinkCoautores id={id} /></Suspense>
Estos sub-componentes están DEFINIDOS en el mismo page.tsx (líneas ~320-382) y leen su RPC vía crossLinkReader; se quedan INTACTOS.

Leyenda de grupo (reusar VERBATIM, ya en NEGACIONES_LOCKED):
  LEYENDA_CROSS_LINK exportada desde app/components/cross-links-parlamentario.tsx (~línea 48).

Guard anti-insinuación (app/lib/anti-insinuacion-guard.test.ts):
  - Precedente Wave-0: SUPERFICIES_PANEL (~líneas 305-308) declara superficies antes de que existan;
    loader try/catch (~líneas 532-537) tolera archivos ausentes → guard VERDE hoy, MUERDE cuando el archivo aterriza.
  - Scan loop (~línea 529): array literal donde se hace spread [...SUPERFICIES_PANEL, ...].
  - NEGACIONES_LOCKED (~líneas 435-457): LEYENDA_CROSS_LINK ya registrada (~446).
  - Mutation self-check PANEL (~líneas 720-746): mold para el self-check RELACIONES.

Guard lockdown (app/lib/lockdown-guard.test.ts):
  - PUBLIC_RPC_ALLOWLIST Set (~líneas 165-193), mantenido ALFABÉTICO.
  - Direction-A (served ⊆ allowlist) + Direction-B (allowlist ⊆ defined-in-migrations).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — extender guards (SUPERFICIES_RELACIONES + PUBLIC_RPC_ALLOWLIST) ANTES de todo copy/RPC nuevo</name>
  <files>app/lib/anti-insinuacion-guard.test.ts, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (COMPLETO: patrón SUPERFICIES_PANEL, loader try/catch tolerante, scan loop, NEGACIONES_LOCKED, mutation self-check PANEL)
    - app/lib/lockdown-guard.test.ts (sección PUBLIC_RPC_ALLOWLIST Set alfabético + Direction-A/B)
    - .planning/phases/101-.../101-RESEARCH.md § "Code Examples" (SUPERFICIES_RELACIONES exacto + allowlist entry) + Pitfall 5/6
    - .planning/phases/101-.../101-PATTERNS.md § anti-insinuacion-guard + lockdown-guard
  </read_first>
  <action>
    En `app/lib/anti-insinuacion-guard.test.ts`: añadir un nuevo array `SUPERFICIES_RELACIONES` (espejo del comment style de SUPERFICIES_PANEL) con EXACTAMENTE estas 4 rutas relativas a `app/`:
      "components/relaciones-section.tsx"
      "components/relaciones-eje-comparar.tsx"
      "app/comparar/page.tsx"
      "components/comparar-selector.tsx"
    Añadir `...SUPERFICIES_RELACIONES` al array literal del scan loop, junto a `...SUPERFICIES_PANEL`. El loader try/catch YA tolera archivos ausentes (relaciones-eje-comparar / comparar-selector / comparar/page pueden no existir aún en este plan) → guard VERDE hoy, MUERDE cuando aterricen (Plan 03).
    NEGACIONES_LOCKED: NO añadir entrada por reusar LEYENDA_CROSS_LINK verbatim (ya está ~línea 446). El heading nuevo "Relaciones con otros parlamentarios" y el heading del 5º bloque "Compartieron militancia en un partido" NO niegan términos prohibidos → no requieren entrada. (Si Plan 03 introduce una leyenda nueva que NIEGA un término prohibido, la registrará ahí.)
    Añadir un mutation self-check `SUPERFICIES_RELACIONES` espejando el de PANEL (~720-746): probar que el guard MUERDE si se inyecta un término prohibido en una superficie relaciones.

    En `app/lib/lockdown-guard.test.ts`: añadir al Set `PUBLIC_RPC_ALLOWLIST` la entrada `"militancia_historica_compartida"` en su posición ALFABÉTICA (entre `match_proyectos` y `militancias_de_parlamentario`), con comment `// ← NEW (debe existir en supabase/migrations/0067_*.sql, montada en Plan 03)`. Esto satisface Direction-B por adelantado; Plan 03 crea la migración y el consumo.

    NO tocar copy de componentes en esta tarea (los archivos relaciones-section/comparar aún no existen o se crean en Task 2/Plan 03).
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run anti-insinuacion-guard lockdown-guard 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SUPERFICIES_RELACIONES" app/lib/anti-insinuacion-guard.test.ts` muestra el array declarado Y el spread en el scan loop.
    - Las 4 rutas exactas (relaciones-section.tsx, relaciones-eje-comparar.tsx, app/comparar/page.tsx, comparar-selector.tsx) están en el array.
    - `grep -n "militancia_historica_compartida" app/lib/lockdown-guard.test.ts` muestra la entrada en el Set allowlist en orden alfabético.
    - Existe un mutation self-check para SUPERFICIES_RELACIONES que prueba que el guard muerde.
    - `pnpm test -- --run anti-insinuacion-guard lockdown-guard` pasa VERDE (loader tolera archivos aún ausentes).
  </acceptance_criteria>
  <done>Guards extendidos: SUPERFICIES_RELACIONES cubre las 4 superficies nuevas + self-check muerde; militancia_historica_compartida allowlistada alfabéticamente; ambos guards verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Crear relaciones-section.tsx + montarlo above-the-fold en la ficha (REL-02), componentes internos intactos</name>
  <files>app/components/relaciones-section.tsx, app/components/relaciones-section.test.tsx, app/app/parlamentario/[id]/page.tsx</files>
  <read_first>
    - app/app/parlamentario/[id]/page.tsx (COMPLETO alrededor de: mount líneas 283-294, MilitanciasSection ~236-238, CarrilesSection ~271-273, sub-componentes CrossLink* ~320-382, JSDoc mt-12 frontier ~275-282)
    - app/components/cross-links-parlamentario.tsx (CrossLinkBloque presentacional + LEYENDA_CROSS_LINK export ~línea 48 + contrato: límite visual 8, total_n honesto, return null en N=0, mt-12 interno)
    - .planning/phases/101-.../101-UI-SPEC.md § "Interaction & Layout Contract" → Ficha relations block (posición above-the-fold, grid 2×2, mt-12 LOCK, copy table) y § Spacing Scale (mt-12 boundary)
    - .planning/phases/101-.../101-PATTERNS.md § relaciones-section.tsx (wrapper shape) + § "app/app/parlamentario/[id]/page.tsx" (LOCKED patterns, Pitfall A4 double-spacing)
    - .planning/phases/101-.../101-01-SUMMARY.md (N/M de zona-gap: el bloque MismaZona rinde 0 para diputados — esperado, no bug)
  </read_first>
  <action>
    Crear `app/components/relaciones-section.tsx` — un Server Component wrapper presentacional que recibe `id` y compone los 4 CrossLinkBloque. Estructura VERBATIM del UI-SPEC/PATTERNS:
      <section id="relaciones" className="mt-12">
        <h2 className="text-xl font-semibold">Relaciones con otros parlamentarios</h2>
        <p className="mt-2 text-sm text-muted-foreground">{LEYENDA_CROSS_LINK}</p>
        <div className="mt-4 grid grid-cols-1 md:grid-cols-2 gap-4"> ...4 bloques bajo Suspense fallback={null}... </div>
      </section>
    Los 4 sub-componentes CrossLink* (CrossLinkCopartidarios/MismaZona/CoComisionados/Coautores) están DEFINIDOS hoy en page.tsx. Para reusarlos desde el nuevo componente SIN duplicar lógica: la decisión de Discreción es (a) exportarlos de page.tsx e importarlos, o (b) pasar los 4 como children/props desde page.tsx. Preferir mantenerlos donde están y que `relaciones-section.tsx` reciba los 4 bloques ya renderizados como prop/children (composición pura) — así CrossLinkBloque y su reader quedan byte-intactos. Elegir la variante que NO mueva la definición de los readers (crossLinkReader/getCopartidarios etc. permanecen en page.tsx).
    Reusar `LEYENDA_CROSS_LINK` VERBATIM (import desde cross-links-parlamentario.tsx) como leyenda de grupo — NO reescribir el string.
    Candados de régimen: cero-hex, tokens tipográficos (text-xl/text-sm), heading `font-semibold` matcheando los carriles. Ningún hex, ningún text-[Npx].
    Pitfall A4 (mt-12 doble): cada CrossLinkBloque emite su propio <section mt-12> interno. Dentro del grid eso puede doble-espaciar. Neutralizar el mt-12 interno SOLO en el nivel de celda del grid vía un wrapper con `[&>section]:mt-0` SIN tocar CrossLinkBloque. La frontera anti-insinuación mt-12 se preserva ENTRE la sección relaciones y sus hermanas (la <section id=relaciones> misma lleva mt-12). Flag para ui-review.

    En `app/app/parlamentario/[id]/page.tsx`: MOVER los 4 <Suspense><CrossLink*/></Suspense> de su posición actual (283-294, final de columna) a INMEDIATAMENTE DESPUÉS de <MilitanciasSection> (~238) y ANTES de <CarrilesSection> (~271) — envueltos por `<RelacionesSection id={id}>`. Above-the-fold, cerca de la identidad, per UI-SPEC. Los readers (getCopartidarios etc.) y los sub-componentes CrossLink* NO se tocan. El bloque MismaZona rinde 0 para diputados (return null interno) — correcto, no fix.
    Añadir el ancla `#relaciones` a la navegación interna de la ficha (FichaRail/construirChips) SOLO si el patrón del rail soporta un ancla no-carril; si no lo soporta limpiamente, dejar el `id="relaciones"` deep-linkable (scroll-margin-top:5rem aplica auto por globals.css) y anotar la omisión del chip.

    Crear `app/components/relaciones-section.test.tsx` (RTL): (1) la sección con id="relaciones" está presente; (2) el heading "Relaciones con otros parlamentarios" renderiza; (3) la leyenda de grupo (LEYENDA_CROSS_LINK) renderiza 1×; (4) los 4 bloques (o slots) están presentes; (5) la sección conserva `mt-12` (frontera). Mockear los CrossLinkBloque hijos si es necesario para el shape test.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run relaciones-section anti-insinuacion-guard 2>&1 | tail -20 && npx tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/relaciones-section.tsx` existe con `<section id="relaciones" className="mt-12">`, heading exacto "Relaciones con otros parlamentarios", leyenda de grupo LEYENDA_CROSS_LINK, y grid `grid-cols-1 md:grid-cols-2 gap-4`.
    - Los 4 CrossLinkBloque quedan byte-intactos (grep confirma que cross-links-parlamentario.tsx no cambió; los readers en page.tsx no se movieron/reescribieron).
    - En page.tsx el bloque relaciones aparece DESPUÉS de MilitanciasSection y ANTES de CarrilesSection (above-the-fold), no al final.
    - `relaciones-section.test.tsx` verifica sección presente + heading + leyenda 1× + 4 bloques + mt-12.
    - `pnpm test -- --run relaciones-section anti-insinuacion-guard` verde + `tsc -b` sin errores.
    - CERO hex, CERO text-[Npx]; la frontera mt-12 entre secciones hermanas preservada.
  </acceptance_criteria>
  <done>El bloque relaciones vive above-the-fold en un <section id=relaciones> con heading+leyenda+grid 2×2; CrossLinkBloque intactos; test y guards verdes; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy nuevo→usuario | todo texto renderizado en la ficha cruza el linter anti-insinuación |
| RPC pública→sitio | allowlist Direction-A/B gobierna qué RPC puede invocar el árbol público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04 | Reputational (insinuación) | copy del bloque relaciones | mitigate | SUPERFICIES_RELACIONES registradas ANTES del copy (Wave 0) + mutation self-check muerde; leyenda LEYENDA_CROSS_LINK verbatim ya en NEGACIONES_LOCKED |
| T-101-05 | Elevation (RPC no allowlistada) | militancia_historica_compartida | mitigate | allowlistada alfabéticamente en Wave 0 (Direction-B) antes de que Plan 03 la monte |
| T-101-06 | Integrity (ranking encubierto) | grid/orden de bloques | mitigate | orden lo fija la RPC (alfabético); el grid es layout visual, no re-ordena por total_n (T-52-13 LOCKED) |
| T-101-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — CERO paquetes nuevos (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `pnpm test -- --run anti-insinuacion-guard lockdown-guard relaciones-section` verde.
- `tsc -b` sin errores.
- Grep confirma SUPERFICIES_RELACIONES (4 rutas) + militancia_historica_compartida en allowlist + RelacionesSection montado above-the-fold.
- CrossLinkBloque byte-intacto (git diff app/components/cross-links-parlamentario.tsx vacío).
</verification>

<success_criteria>
- Wave 0: guards extendidos y verdes ANTES del copy; self-check muerde; RPC allowlistada.
- REL-02: relaciones above-the-fold en `<section id=relaciones>` con heading, leyenda de grupo, grid 2×2; componentes internos intactos; frontera mt-12 preservada.
</success_criteria>

<output>
Create `.planning/phases/101-relaciones-p2a-audit-brecha-bloque-relaciones-comparar-coali/101-02-SUMMARY.md` when done. Registrar: la variante elegida para reusar los CrossLinkBloque (export vs children), si el chip #relaciones entró al rail, y cualquier riesgo de doble-espaciado mt-12 para ui-review.
</output>
