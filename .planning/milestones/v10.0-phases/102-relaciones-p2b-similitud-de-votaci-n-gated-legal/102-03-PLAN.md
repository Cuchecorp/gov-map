---
phase: 102-relaciones-p2b-similitud-de-votaci-n-gated-legal
plan: 03
type: execute
wave: 3
depends_on: ["102-01", "102-02"]
autonomous: true
requirements: [VSIM-01, VSIM-02]
files_modified:
  - app/components/similitud-votacion-comparar.tsx
  - app/app/comparar/page.tsx
  - app/app/comparar/page.test.tsx
  - docs/legal/102-LEGAL-DOSSIER-VSIM.md

must_haves:
  truths:
    - "Con flag ON (preview local), /comparar renderiza la 5ª sección mt-12 al final: heading + caveat VERBATIM adyacente + figura N/M/% NEUTRAL (sin petroleo/bold/gauge) + cobertura declarada + provenance"
    - "Con flag OFF (PROD default), la sección está AUSENTE del DOM (return null server-side, cero .rpc, cero nodo DOM)"
    - "M=0 → copy degradado honesto 'sin votaciones compartidas suficientes en las fuentes consultadas al {fecha}', jamás '0%'"
    - "El dossier 102-LEGAL-DOSSIER-VSIM.md existe con signoff: pending + anti-modelo DW-NOMINATE + caveat + evidencia preview ON local"
  artifacts:
    - path: "app/components/similitud-votacion-comparar.tsx"
      provides: "componente presentacional puro NEUTRAL + LEYENDA_SIMILITUD_VOTO (del Plan 01)"
      contains: "text-sm"
    - path: "app/app/comparar/page.tsx"
      provides: "5ª sección gated al final de CompararEjes (return null si flag OFF)"
      contains: "vsimPublicEnabled"
    - path: "docs/legal/102-LEGAL-DOSSIER-VSIM.md"
      provides: "dossier legal signoff: pending (gate del flip humano)"
      contains: "signoff: pending"
  key_links:
    - from: "app/app/comparar/page.tsx"
      to: "coincidencia_votos_par (RPC 0068)"
      via: "sb.rpc gated por vsimPublicEnabled(process.env)"
      pattern: "sb\\.rpc\\(\"coincidencia_votos_par\""
    - from: "app/app/comparar/page.tsx"
      to: "app/lib/vsim-gate.ts"
      via: "import vsimPublicEnabled (único lector del flag, anti-flip V3)"
      pattern: "vsimPublicEnabled"
    - from: "app/app/comparar/page.tsx"
      to: "app/components/similitud-votacion-comparar.tsx"
      via: "render de <SimilitudVotacionComparar> con valores resueltos"
      pattern: "SimilitudVotacionComparar"
---

<objective>
Llenar el cuerpo presentacional NEUTRAL del componente `similitud-votacion-comparar.tsx` (contrato de la constante ya fijado en Plan 01), montar la 5ª sección gated al FINAL de `CompararEjes` (return null si flag OFF, cero .rpc), extender el RTL con los casos ON/OFF/M=0, y escribir el dossier legal `signoff: pending`. Con flag ON en preview local se entrega la evidencia DOM para el cold-read del operador; PROD queda OFF.

Purpose: VSIM-01 (figura factual + caveat + cobertura, figura NEUTRAL anti-DW-NOMINATE) y VSIM-02 (gate server-side return null + dossier). El flip a true NO ocurre en esta fase.
Output: eje similitud renderizable con flag ON; ausente con flag OFF; RTL ON/OFF/M=0 verde; dossier pending.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-UI-SPEC.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md

<interfaces>
<!-- Contratos load-bearing. NO explorar; usar verbatim. -->

app/components/similitud-votacion-comparar.tsx (creada en Plan 01 con SOLO la constante):
  export const LEYENDA_SIMILITUD_VOTO = "La coincidencia alta es la norma, no una señal: ..." (ya presente)
  → añadir el componente presentacional puro (default export o named), props serializadas por el server.

app/lib/vsim-gate.ts (Plan 01):
  export function vsimPublicEnabled(env = process.env): boolean;  // === "true"

RPC (Plan 02, en PROD): coincidencia_votos_par(p_a text, p_b text)
  returns { n_coinciden: bigint, m_compartidas: bigint, fecha_captura_max: timestamptz }

app/app/comparar/page.tsx (Phase 101 — MODIFY):
  export const dynamic = "force-dynamic" (:41) — ya presente
  PARLAMENTARIO_ID_RE validacion antes del .rpc() (:185, V5)
  fechaConsultaHoy() tz America/Santiago (:52-59) para "consultado al"
  CompararEjes return actual (:459-464): return (<>{ejeMilitancia}{ejeComisiones}{ejeCoautoria}{ejeZona}</>);
  patron lector React.cache que THROWS ante error real de DB (:87-100, #34)
  InterseccionCompartida (:144-160) — ANTI-PATRON: font-semibold text-accent-product (NO replicar para VOTO)

app/app/comparar/page.test.tsx (MODIFY):
  harness :22-55 → rpcImpl = vi.fn(...), ROSTER_DEFAULT dos-diputado fixture, @/lib/supabase mockeado, renderEjes/renderToStaticMarkup.
  control del flag: vi.stubEnv("VSIM_PUBLIC_ENABLED", "true"/"false") (RESEARCH Q2).

docs/legal/13-LEGAL-DOSSIER.md:1-10 (mold front-matter):
  signoff: pending, alcance, asesor, nota: "Encender ... requiere signoff: approved."

Copy VERBATIM (102-UI-SPEC Copy table :117-127):
  heading: "Similitud de votación"
  figura: "Coinciden en {N} de {M} votaciones compartidas ({X}%)."  (X = round(N/M·100), % entero sin decimales)
  cobertura: "Cobertura del voto: Cámara ~80% confirmado por identificador; Senado ~20% por nombre (probable). El denominador refleja solo votaciones registradas en las fuentes al {fecha}."
  asimetria mixta (A y B de distinta cámara): append " Comparan una diputada/o y una senadora/or: la cobertura de voto es asimétrica entre ambas cámaras, por lo que el denominador de votaciones compartidas es más incompleto."
  degradado M=0: "Sin votaciones compartidas suficientes en las fuentes consultadas al {fecha}."  (NUNCA "0%")
  provenance: "Fuente: votaciones de Cámara y Senado · captura al {fecha_captura_max}."  (text-xs, mono para fecha; omitir si RPC sin filas)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente NEUTRAL similitud-votacion-comparar.tsx</name>
  <files>app/components/similitud-votacion-comparar.tsx</files>
  <read_first>
    - app/components/similitud-votacion-comparar.tsx (creada en Plan 01 con la constante LEYENDA_SIMILITUD_VOTO)
    - app/components/relaciones-eje-comparar.tsx (STRUCTURE del wrapper :63-83 — copiar la forma <section className="mt-12">, NO el color)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-UI-SPEC.md (Typography LOCK :64-66, Color LOCKS :86-90, Copy table :117-127)
  </read_first>
  <behavior>
    - Estado normal (M>0): renderiza heading "Similitud de votación" + caveat VERBATIM (LEYENDA_SIMILITUD_VOTO, adyacente/antes de la figura) + figura "Coinciden en N de M votaciones compartidas (X%)." en text-sm weight-400 --foreground (SIN petroleo/bold/gauge) + cobertura declarada + provenance
    - Estado M=0: renderiza el degradado honesto "Sin votaciones compartidas suficientes en las fuentes consultadas al {fecha}", jamás "0%", en text-muted-foreground, sin figura ni provenance
    - Asimetría mixta: la línea de cobertura incluye la nota de asimetría cuando A y B son de distinta cámara
    - Componente presentacional PURO: recibe todo serializado; nunca toca Supabase, nunca re-cuenta, nunca re-ordena
  </behavior>
  <action>
    Completar app/components/similitud-votacion-comparar.tsx conservando el export LEYENDA_SIMILITUD_VOTO (Plan 01). Añadir un componente presentacional puro que reciba props resueltas por el server: { n, m, pct, fechaConsulta, fechaCaptura, camaraMixta } (o forma equivalente). Estructura wrapper `<section className="mt-12">` con `<h2 className="text-xl font-semibold">Similitud de votación</h2>`, luego la LEYENDA (caveat) adyacente en text-sm, luego la figura en `text-sm` weight-400 `--foreground` (NUNCA font-semibold, NUNCA text-accent-product, NUNCA barra/gauge/meter — Color LOCKS UI-SPEC :86-90; NO reusar RelacionesEjeComparar ni InterseccionCompartida), luego la cobertura declarada (con la nota de asimetría si camaraMixta), luego provenance en `text-xs text-muted-foreground` (mono para la fecha). Si m===0: renderizar SOLO el heading + caveat + el copy degradado honesto (text-muted-foreground), sin figura ni provenance, JAMÁS "0%". Todo el copy VERBATIM del UI-SPEC Copy table. Cero-hex, cero rem ad-hoc, cero icono decorativo (source-scan guard DEBT-05).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc -b 2>&1 | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>process.exit(/similitud-votacion-comparar/.test(d)?1:0))"</automated>
  </verify>
  <acceptance_criteria>
    - El componente NO contiene `font-semibold text-accent-product` sobre la figura, ni barra/gauge/meter/progress
    - La figura está en text-sm weight-400; el heading es el único font-semibold
    - M=0 renderiza el copy degradado exacto, nunca "0%"
    - tsc -b limpio para el componente
    - LEYENDA_SIMILITUD_VOTO sigue exportada verbatim (no romper el import del linter del Plan 01)
  </acceptance_criteria>
  <done>Componente neutral presentacional puro; figura sin acento; degradado honesto M=0; constante intacta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Montar la 5ª sección gated en /comparar + RTL ON/OFF/M=0</name>
  <files>app/app/comparar/page.tsx, app/app/comparar/page.test.tsx</files>
  <read_first>
    - app/app/comparar/page.tsx (CompararEjes :215-466, return :459-464, PARLAMENTARIO_ID_RE :185, patron lector #34 :87-100, fechaConsultaHoy :52-59)
    - app/app/comparar/page.test.tsx (harness :22-55, renderEjes/renderToStaticMarkup)
    - app/lib/vsim-gate.ts (vsimPublicEnabled)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md (Gated block :249-268, RTL :278-289)
  </read_first>
  <behavior>
    - Flag OFF (ausente o "false"): renderEjes NO contiene "Similitud de votación" ni "La coincidencia alta es la norma"; NO se llama sb.rpc("coincidencia_votos_par") (ejeSimilitud queda null)
    - Flag ON ("true") + rpc devuelve {n,m,fecha}: la sección + caveat + figura neutral presentes; el % = round(n/m·100) computado en el server
    - Flag ON + rpc devuelve m=0: copy degradado, jamás "0%"
    - Un error real de la RPC se THROWS (patron #34), NUNCA se degrada a "sin votaciones compartidas"
    - La 5ª sección es el ÚLTIMO sibling del return de CompararEjes (después de ejeZona)
  </behavior>
  <action>
    En page.tsx, dentro de CompararEjes DESPUÉS de ejeZona: `import { vsimPublicEnabled } from "@/lib/vsim-gate"` (ÚNICO lector del flag — anti-flip V3) + import de SimilitudVotacionComparar. Añadir `let ejeSimilitud: React.ReactNode = null; if (vsimPublicEnabled(process.env)) { const { data, error } = await sb.rpc("coincidencia_votos_par", { p_a: a, p_b: b }); if (error) throw new Error(...); // resolver N/M, pct = M>0 ? Math.round(N/M*100) : null, fecha via fechaConsultaHoy(), camaraMixta desde el roster; pasar a <SimilitudVotacionComparar>. }`. Cambiar el return a `return (<>{ejeMilitancia}{ejeComisiones}{ejeCoautoria}{ejeZona}{ejeSimilitud}</>);`. Los ids ya vienen validados por PARLAMENTARIO_ID_RE y orden canonico [a,b].sort() antes del .rpc(). Flag OFF ⇒ ejeSimilitud null ⇒ cero DOM, cero rpc.
    En page.test.tsx, reusar el harness (rpcImpl, ROSTER_DEFAULT, @/lib/supabase mock). Añadir: (1) test flag OFF (vi.stubEnv "false"/ausente) → html NO contiene "Similitud de votación" ni "La coincidencia alta es la norma"; (2) test flag ON (vi.stubEnv "true") + rpcImpl devuelve {n_coinciden, m_compartidas, fecha_captura_max} → sección + caveat + figura presentes, figura sin font-semibold/text-accent-product; (3) test flag ON + m=0 → copy degradado, html NO contiene "0%". Limpiar vi.unstubAllEnvs en afterEach.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run comparar && pnpm test -- --run vsim-antiflip</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run comparar` verde con los 3 casos nuevos (OFF ausente, ON con datos, ON con M=0)
    - `pnpm test -- --run vsim-antiflip` sigue verde (V3: page.tsx importa vsimPublicEnabled, NO lee process.env.VSIM_PUBLIC_ENABLED crudo)
    - El return de CompararEjes tiene ejeSimilitud como último sibling
    - Flag OFF: 0 ocurrencias de "Similitud de votación" en el DOM y 0 llamadas a sb.rpc("coincidencia_votos_par")
  </acceptance_criteria>
  <done>5ª sección gated montada; return null con flag OFF; RTL ON/OFF/M=0 verde; anti-flip V3 intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Dossier legal 102-LEGAL-DOSSIER-VSIM.md (signoff: pending)</name>
  <files>docs/legal/102-LEGAL-DOSSIER-VSIM.md</files>
  <read_first>
    - docs/legal/13-LEGAL-DOSSIER.md (front-matter :1-10, secciones de superficie de riesgo, checklist)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md (base-rate empírica 19-100% avg 63%, ~32% cuasi-unánimes, cifras cobertura 80%/20%)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-UI-SPEC.md (caveat verbatim, anti-DW-NOMINATE LOCKS)
  </read_first>
  <action>
    Crear docs/legal/102-LEGAL-DOSSIER-VSIM.md con el molde de 13-LEGAL-DOSSIER: front-matter YAML `signoff: pending` + `alcance: VSIM (coincidencia de votación)` + `asesor:` + `nota: "Encender VSIM_PUBLIC_ENABLED requiere signoff: approved firmado por humano."`. Cuerpo: (1) la métrica exacta (N de M votaciones sustantivas compartidas, denominador estado_vinculo='confirmado' AND seleccion in si/no/abstencion); (2) el caveat base-alta VERBATIM + la evidencia empírica que lo sustenta (154 pares 19%-100%, promedio 63%, ~32% de votaciones cuasi-unánimes con lado perdedor ≤5% — citar del research); (3) el anti-modelo DW-NOMINATE explícito (prohibido score/ranking/eje/mapa; figura neutral sin petroleo/gauge; co_votacion∉/red); (4) la cobertura declarada Cámara ~80% / Senado ~20% (audit Phase 98); (5) sección de evidencia del estado ON en preview local (RTL/DOM del Task 2, sin deploy) — el flip a PROD queda gated humano. Checklist de sign-off al final. El agente NO firma ni pone approved.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/legal/102-LEGAL-DOSSIER-VSIM.md','utf8');process.exit(/signoff:\s*pending/.test(s)&&/DW-NOMINATE/.test(s)&&/coincidencia alta es la norma/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Front-matter con `signoff: pending` (nunca approved por el agente)
    - Contiene el caveat VERBATIM, el anti-modelo DW-NOMINATE, la base-rate empírica citada, la cobertura 80%/20%, y la sección de evidencia preview ON local
    - grep del verify pasa (signoff pending + DW-NOMINATE + caveat presentes)
  </acceptance_criteria>
  <done>Dossier legal signoff: pending completo; gate del flip humano documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag server-only → DOM público | vsimPublicEnabled decide si la superficie gated existe en el DOM |
| copy VSIM → lector | insinuación/difamación si el caveat no acompaña la figura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-08 | Information Disclosure | figura N/M/% | mitigate | figura neutral sin petroleo/bold/gauge (Color LOCKS); caveat adyacente pesa más; anti-modelo en dossier |
| T-102-09 | Tampering | page.tsx lector de flag | mitigate | único import de vsimPublicEnabled (anti-flip V3 lo caza si se lee env crudo); return null antes del .rpc |
| T-102-10 | Repudiation | copy sin caveat | mitigate | caveat VERBATIM no-colapsable adyacente + sign-off legal humano (dossier pending) |
| T-102-11 | Information Disclosure | fecha_captura como hecho | accept | fecha_captura_max es provenance de captura, NUNCA "fecha de ingreso" (regla LOCKED heredada) |
</threat_model>

<verification>
- `cd app && pnpm test -- --run comparar vsim-antiflip anti-insinuacion-guard` todo verde
- Flag OFF → DOM sin "Similitud de votación" ni caveat; cero .rpc
- Flag ON → sección + caveat + figura neutral; M=0 → degradado sin "0%"
- docs/legal/102-LEGAL-DOSSIER-VSIM.md con signoff: pending + DW-NOMINATE + caveat + evidencia preview ON
</verification>

<success_criteria>
- Componente neutral (figura sin acento); 5ª sección gated montada al final; return null con flag OFF
- RTL ON/OFF/M=0 verde; anti-flip V3 intacto (único lector del flag)
- Dossier legal signoff: pending; el flip a PROD queda como acto humano (fuera de esta fase)
</success_criteria>

<output>
Create `.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-03-SUMMARY.md` when done
</output>
