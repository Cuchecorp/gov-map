---
phase: 102-relaciones-p2b-similitud-de-votaci-n-gated-legal
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
autonomous: true
requirements: [VSIM-01]
files_modified:
  - supabase/tests/0068_coincidencia_votos_par.test.sql

must_haves:
  truths:
    - "La RPC coincidencia_votos_par existe en el schema PROD aplicado: secdef, search_path='', statement_timeout='5s', doble-revoke CERO grant"
    - "El returns es EXACTAMENTE (n_coinciden bigint, m_compartidas bigint, fecha_captura_max timestamptz) — nunca lista individual"
    - "El denominador cuenta solo votaciones sustantivas (estado_vinculo='confirmado' AND seleccion in si/no/abstencion): pareos/ausencias NO cuentan en m_compartidas"
    - "pgTAP corre contra el schema APLICADO y pasa 100%"
  artifacts:
    - path: "supabase/tests/0068_coincidencia_votos_par.test.sql"
      provides: "pgTAP de la RPC 0068 (firma, secdef, revokes, returns, denominador)"
      contains: "coincidencia_votos_par"
  key_links:
    - from: "supabase/migrations/0068_coincidencia_votos_par.sql"
      to: "schema PROD aplicado"
      via: "psql --single-transaction (precedente 0059-0067)"
      pattern: "coincidencia_votos_par"
---

<objective>
Aplicar la migración aditiva 0068 (escrita en Plan 01) al schema PROD por `psql --single-transaction` y probar el DDL con pgTAP contra el schema APLICADO. La RPC es aditiva pura (una función nueva + revokes) → dentro de forbidden-gate permitido; el agente PUEDE aplicar migraciones aditivas (precedente 0059-0067).

Purpose: VSIM-01 exige que la RPC bounded exista físicamente y que su contrato (secdef, timeout, doble-revoke, returns de 3 columnas, denominador sustantiva) esté probado — typecheck NO prueba que Postgres corrió el DDL (Pitfall 6, LOCKED).
Output: 0068 aplicada a PROD; pgTAP verde contra schema aplicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md

<interfaces>
<!-- Contrato del pgTAP a copiar de 0067; scope por ::regprocedure (WR-05). -->

supabase/tests/0067_militancia_historica_compartida.test.sql (63 lineas — mold):
  begin; select plan(N); ... select * from finish(); rollback;
  :20 has_function(schema, name, ARRAY[args], desc)
  :23-26 prosecdef = true (::regprocedure)
  :29 anon SIN execute (has_function_privilege ... false)
  :32 authenticated SIN execute
  :35-38 proconfig ~ 'search_path='
  :41-44 proconfig ~ 'statement_timeout=5s'
  :57-60 is(pg_get_function_result(...::regprocedure), 'TABLE(...)', desc)

Firma 0068: coincidencia_votos_par(text, text)
returns esperado: 'TABLE(n_coinciden bigint, m_compartidas bigint, fecha_captura_max timestamptz)'

Comando apply (RESEARCH Pitfall 6 / PATTERNS :172-175):
  PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0068_coincidencia_votos_par.sql
Comando pgTAP (contra schema aplicado):
  PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0068_coincidencia_votos_par.test.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el pgTAP 0068 (firma, secdef, revokes, returns, denominador sustantiva)</name>
  <files>supabase/tests/0068_coincidencia_votos_par.test.sql</files>
  <read_first>
    - supabase/tests/0067_militancia_historica_compartida.test.sql (63 lineas, mold completo)
    - supabase/migrations/0068_coincidencia_votos_par.sql (escrita en Plan 01 — verificar firma/returns)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md (bloque pgTAP :183-199, VSIM-01 denominator test :199)
  </read_first>
  <action>
    Crear supabase/tests/0068_coincidencia_votos_par.test.sql copiando el mold de 0067 (begin/plan/finish/rollback). Swappear a firma 2-arg `text, text` y scope por `::regprocedure` (WR-05, no proname a secas). Aserciones: (1) has_function('public','coincidencia_votos_par', ARRAY['text','text'], ...); (2) prosecdef = true; (3) anon SIN execute; (4) authenticated SIN execute; (5) proconfig ~ 'search_path='; (6) proconfig ~ 'statement_timeout=5s'; (7) is(pg_get_function_result('public.coincidencia_votos_par(text,text)'::regprocedure), 'TABLE(n_coinciden bigint, m_compartidas bigint, fecha_captura_max timestamptz)', ...). AÑADIR el test de denominador (VSIM-01, PATTERNS :199): fixture de un par con una fila pareo/ausente (seleccion NO en si/no/abstencion, o estado_vinculo!='confirmado') que NO debe contar en m_compartidas; probar que m_compartidas cuenta solo las sustantivas y n_coinciden ≤ m_compartidas. Ajustar plan(N) al número real de aserciones.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/tests/0068_coincidencia_votos_par.test.sql','utf8');process.exit(/coincidencia_votos_par/.test(s)&&/plan\(/.test(s)&&/rollback/.test(s)&&/statement_timeout/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe con begin/plan(N)/finish/rollback
    - Cubre: firma, prosecdef, revoke anon, revoke authenticated, search_path, statement_timeout, returns exacto (3 columnas), y el test de denominador sustantiva (pareo/ausente NO cuenta)
    - Scope por ::regprocedure, no proname a secas
  </acceptance_criteria>
  <done>pgTAP 0068 escrito, cubriendo contrato + denominador sustantiva.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar 0068 a PROD + correr pgTAP contra schema aplicado</name>
  <files>supabase/tests/0068_coincidencia_votos_par.test.sql</files>
  <read_first>
    - supabase/migrations/0068_coincidencia_votos_par.sql (verificar antes de aplicar)
    - supabase/tests/0068_coincidencia_votos_par.test.sql (del Task 1)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md (Pitfall 6: psql --single-transaction, NUNCA db push; agente PUEDE aplicar aditivas)
  </read_first>
  <action>
    Aplicar la migración por `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0068_coincidencia_votos_par.sql` (NUNCA `supabase db push` — deriva schema_migrations). 0068 es aditiva pura (función nueva + revokes, sin flip de régimen ni anon key) → dentro de forbidden-gate permitido, precedente 0059-0067. Luego correr el pgTAP contra el schema APLICADO por `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0068_coincidencia_votos_par.test.sql`. Si el pgTAP revela un fixture mal formado (drift de NOT NULL / FK, patrón 92-04), corregir el fixture del test — NO relajar el contrato. Registrar en el SUMMARY el conteo ok/not-ok y una verificación rápida `select coincidencia_votos_par('<par real>')` con cifras reales.
    NOTA: si `$SUPABASE_DB_URL` no está disponible en el entorno del agente, este task se convierte en checkpoint:human-action con el bloque ready-to-run en el SUMMARY (patrón 99-04); el agente NO fabrica el apply.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0068_coincidencia_votos_par.test.sql 2>&1 | grep -v '^#' | grep -c '^not ok' | node -e "process.stdin.on('data',d=>process.exit(parseInt(d.toString().trim())===0?0:1))"</automated>
  </verify>
  <acceptance_criteria>
    - `select pg_get_functiondef('public.coincidencia_votos_par(text,text)'::regprocedure)` devuelve la función (existe en PROD)
    - pgTAP corre contra el schema aplicado con 0 `not ok`
    - Verificación rápida: `select * from coincidencia_votos_par(p_a, p_b)` para un par real devuelve n_coinciden ≤ m_compartidas, ambos > 0, fecha_captura_max no null
  </acceptance_criteria>
  <done>0068 aplicada a PROD (o bloque ready-to-run si falta DB_URL); pgTAP verde 0 not-ok contra schema aplicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC secdef → cliente REST | La función corre como definer; los grants deciden quién la ejecuta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-05 | Elevation of Privilege | coincidencia_votos_par en PROD | mitigate | doble-revoke `from public` + `from anon, authenticated` verificado por pgTAP contra schema aplicado |
| T-102-06 | Denial of Service | query pairwise | mitigate | statement_timeout='5s' en la función (query prototipada 28ms); pgTAP verifica el proconfig |
| T-102-07 | Tampering | apply del DDL | mitigate | psql --single-transaction (NUNCA db push); rollback atómico ante error |
</threat_model>

<verification>
- `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0068_coincidencia_votos_par.test.sql` → 0 `not ok`
- La función existe en PROD (`::regprocedure` resuelve)
- Verificación de cifras reales para un par documentada en el SUMMARY
</verification>

<success_criteria>
- 0068 aplicada a PROD por psql --single-transaction (o handoff ready-to-run si falta DB_URL)
- pgTAP verde contra schema aplicado (contrato + denominador sustantiva)
- doble-revoke CERO grant y statement_timeout='5s' verificados
</success_criteria>

<output>
Create `.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-02-SUMMARY.md` when done
</output>
