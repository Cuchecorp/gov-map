---
phase: 102-relaciones-p2b-similitud-de-votaci-n-gated-legal
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [VSIM-02, VSIM-03]
files_modified:
  - app/lib/vsim-gate.ts
  - app/lib/vsim-gate.test.ts
  - app/lib/vsim-antiflip-guard.test.ts
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/lockdown-guard.test.ts
  - app/components/similitud-votacion-comparar.tsx
  - app/components/co-votacion-red-guard.test.ts
  - app/components/red/red-graph.tsx
  - app/components/red/arista-hecho.tsx
  - supabase/migrations/0068_coincidencia_votos_par.sql
  - .env.example

must_haves:
  truths:
    - "El gate vsimPublicEnabled() enciende SOLO con el literal 'true' (fail-closed); cualquier variante MUERDE el anti-flip guard"
    - ".env.example contiene VSIM_PUBLIC_ENABLED=false"
    - "El linter anti-insinuación MUERDE ante cada idiom VSIM nuevo inyectado en memoria, y NO se auto-caza sobre su propia leyenda LEYENDA_SIMILITUD_VOTO"
    - "Ningún archivo de la superficie /red contiene co_votacion/covotacion en código (test estático permanente, verde)"
    - "coincidencia_votos_par está en PUBLIC_RPC_ALLOWLIST y su migración 0068 existe escrita (Direction-B satisfecha)"
  artifacts:
    - path: "app/lib/vsim-gate.ts"
      provides: "chokepoint server-only del flag VSIM_PUBLIC_ENABLED (=== 'true')"
      contains: "VSIM_PUBLIC_ENABLED === \"true\""
    - path: "app/lib/vsim-antiflip-guard.test.ts"
      provides: "guard CI 3 vectores + mutation self-check espejo money-antiflip"
    - path: "app/components/co-votacion-red-guard.test.ts"
      provides: "test estático permanente co_votacion ∉ /red"
    - path: "supabase/migrations/0068_coincidencia_votos_par.sql"
      provides: "DDL de la RPC coincidencia_votos_par (escrita, NO aplicada en este plan)"
      contains: "create or replace function public.coincidencia_votos_par"
    - path: "app/components/similitud-votacion-comparar.tsx"
      provides: "exporta la constante LEYENDA_SIMILITUD_VOTO (contrato para el linter)"
      contains: "export const LEYENDA_SIMILITUD_VOTO"
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/components/similitud-votacion-comparar.tsx"
      via: "import { LEYENDA_SIMILITUD_VOTO } añadido a NEGACIONES_LOCKED"
      pattern: "LEYENDA_SIMILITUD_VOTO"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "supabase/migrations/0068_coincidencia_votos_par.sql"
      via: "allowlist Direction-B: la entrada exige la migración escrita"
      pattern: "coincidencia_votos_par"
---

<objective>
Wave 0 completo: montar TODOS los guards, el gate fail-closed, la extensión del linter, el test estático co_votacion∉/red (resolviendo las ramas muertas primero), la migración 0068 ESCRITA (no aplicada), la entrada de allowlist y la línea de `.env.example` — ANTES de cualquier copy o montaje UI. También se crea el archivo del componente `similitud-votacion-comparar.tsx` conteniendo SOLO la constante exportada `LEYENDA_SIMILITUD_VOTO` (contrato que el linter importa); el cuerpo presentacional completo lo llena Plan 03.

Purpose: VSIM-02 (gate + anti-flip) y VSIM-03 (linter + co_votacion∉/red) exigen que el enforcement exista ANTES del copy (precedente 100-01/101-02). La migración escrita destraba la allowlist Direction-B sin dejarla huérfana (lección 101-02).
Output: gate + 3 guards nuevos/extendidos verdes, ramas muertas /red eliminadas, 0068 escrita, allowlist + .env.example actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md
@.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-UI-SPEC.md

<interfaces>
<!-- Contratos que el executor necesita. NO explorar el codebase — usar estos verbatim. -->

app/lib/money-gate.ts:1,30-34 (mold del chokepoint, copiar swap money→vsim):
  linea 1: import "server-only";
  export function moneyPublicEnabled(env = process.env): boolean { return env.MONEY_PUBLIC_ENABLED === "true"; }

app/lib/money-antiflip-guard.test.ts (436 lineas — mold del guard; constantes a re-bind en :43-47):
  const MONEY_GATE = path.join(APP_ROOT, "lib", "money-gate.ts");  // → vsim-gate.ts
  const ENV_EXAMPLE = path.join(REPO_ROOT, ".env.example");        // unchanged
  const MONEY_GATE_REL = "lib/money-gate.ts";                      // → "lib/vsim-gate.ts"
  helpers verbatim: stripTsComments (:55-65), walkSourceFiles+SKIP_DIRS (:73-114),
    RAW_ENV_ALLOWLIST (:123-132), detectarRelajacionGate (:150-225, V1a/b/c/d+V2a/b),
    detectarRawEnvEnRuta (:232-236). V3 escanea tambien packages/.
  §4 mutation self-check :338-435 (una it() por relajacion).

app/lib/anti-insinuacion-guard.test.ts (911 lineas — extender):
  arrays SUPERFICIES_* (patron :305-344); TERMINOS_PROHIBIDOS (:355-463);
  NEGACIONES_LOCKED (:471-493, LEYENDA_CROSS_LINK en :482); bucle de scan (:565);
  self-check RELACIONES (:784-807), no-falso-positivo (:881-889).

app/lib/lockdown-guard.test.ts:
  PUBLIC_RPC_ALLOWLIST = new Set([...]) en :165 (entrada militancia_historica_compartida en :186);
  Direction-B :388-400 (allowlist ⊆ definidas; RPC_DEF_REGEX :372).

app/components/red/red-graph.tsx:79-82 (TIPO_LABEL) — borrar linea 81 `co_votacion: "Misma votación",`.
app/components/red/arista-hecho.tsx:32-33 — borrar el `case "co_votacion":` + su return (el default :34-35 cubre cualquier tipo desconocido). VERIFICADO: red-graph.test.tsx NO depende de estas ramas.

supabase/migrations/0067_militancia_historica_compartida.sql — mold RPC (secdef header :42-47, doble-revoke :86-87, comentario de orden de aplicacion :24-38).

0030_net.sql:27,145 — mencionan `co_votacion` en COMENTARIOS (explican la exclusion). El guard co_votacion∉/red DEBE strip-comments (reusar stripTsComments) para NO disparar sobre esos comentarios; NO borrar esos comentarios (son documentacion de la exclusion).

RPC 0068 body (102-PATTERNS lineas 138-164, VERBATIM, cronometrado 28ms):
  CTE a: select v.votacion_id, v.seleccion from public.voto v where v.parlamentario_id = p_a and v.estado_vinculo = 'confirmado' and v.seleccion in ('si','no','abstencion');
  CTE b: idem para p_b.
  select count(*) filter (where a.seleccion = b.seleccion) as n_coinciden, count(*) as m_compartidas,
    (select max(vt.fecha_captura) from public.votacion vt where vt.id in (select votacion_id from a intersect select votacion_id from b)) as fecha_captura_max
  from a join b using (votacion_id);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate fail-closed + anti-flip guard + .env.example</name>
  <files>app/lib/vsim-gate.ts, app/lib/vsim-gate.test.ts, app/lib/vsim-antiflip-guard.test.ts, .env.example</files>
  <read_first>
    - app/lib/money-gate.ts (mold entero, 34 lineas)
    - app/lib/money-gate.test.ts (mold, 25 lineas, 5 casos)
    - app/lib/money-antiflip-guard.test.ts (mold entero, 436 lineas; constantes :43-47, helpers :55-236, vectores + self-check :241-435)
    - .env.example (lineas 66-70: bloque de comentario + MONEY_PUBLIC_ENABLED=false)
  </read_first>
  <behavior>
    - vsimPublicEnabled({}) === false (var ausente, default fail-closed)
    - vsimPublicEnabled({VSIM_PUBLIC_ENABLED:"false"}) === false
    - vsimPublicEnabled({VSIM_PUBLIC_ENABLED:"1"}) === false (sin truthiness laxa)
    - vsimPublicEnabled({VSIM_PUBLIC_ENABLED:"TRUE"}) === false (case-sensitive)
    - vsimPublicEnabled({VSIM_PUBLIC_ENABLED:"true"}) === true (unico valor que enciende)
    - anti-flip V1: vsim-gate.ts mantiene `=== "true"`, sin Boolean()/!== "false"/||/NODE_ENV/.trim()/== laxo (single ignition CR-01)
    - anti-flip V2: `.env.example` matchea /^VSIM_PUBLIC_ENABLED\s*=\s*false\s*$/m; `=true` nunca
    - anti-flip V3: ningun archivo fuera de vsim-gate.ts (incl. packages/) nombra VSIM_PUBLIC_ENABLED
    - mutation self-check §4: el detector MUERDE en memoria por cada relajacion (Boolean, !== "false", || === "1", || NODE_ENV, .trim(), == laxo, .env=true, raw-env-in-route)
  </behavior>
  <action>
    Crear app/lib/vsim-gate.ts espejo byte-a-byte de money-gate.ts: `import "server-only";` en linea 1 + `export function vsimPublicEnabled(env: Record<string, string | undefined> = process.env): boolean { return env.VSIM_PUBLIC_ENABLED === "true"; }`. NO usar Boolean(), !== "false", ||, NODE_ENV, .trim() ni == laxo — el guard V1 exige EXACTAMENTE `=== "true"`. Sin prefijo NEXT_PUBLIC_.
    Crear app/lib/vsim-gate.test.ts espejo de money-gate.test.ts (5 casos), import desde "./vsim-gate".
    Crear app/lib/vsim-antiflip-guard.test.ts copiando money-antiflip-guard.test.ts entero, swap MONEY→VSIM y money→vsim: re-bind constantes MONEY_GATE→vsim-gate.ts, MONEY_GATE_REL→"lib/vsim-gate.ts", ENV_EXAMPLE sin cambio. Reusar VERBATIM stripTsComments, walkSourceFiles+SKIP_DIRS, RAW_ENV_ALLOWLIST (unica entrada = el chokepoint vsim-gate.ts), detectarRelajacionGate (todos los regex referencian VSIM_PUBLIC_ENABLED), detectarRawEnvEnRuta. Copiar los 3 vectores + §4 self-check swappeando el nombre del flag. Mantener los asserts de sanidad SKIP_DIRS/PACKAGES_ROOT `> 0` (previenen scan vacio silencioso).
    Añadir a .env.example, junto a MONEY_PUBLIC_ENABLED (linea 70), un bloque de comentario espejo de :66-69 + `VSIM_PUBLIC_ENABLED=false`.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run vsim-gate vsim-antiflip</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/vsim-gate.ts contiene exactamente `env.VSIM_PUBLIC_ENABLED === "true"` y `import "server-only"` en linea 1
    - `pnpm test -- --run vsim-gate` pasa los 5 casos
    - `pnpm test -- --run vsim-antiflip` pasa los 3 vectores + self-check (§4 muerde por cada relajacion)
    - .env.example tiene una linea que matchea `/^VSIM_PUBLIC_ENABLED\s*=\s*false\s*$/m`
  </acceptance_criteria>
  <done>Gate fail-closed + guard anti-flip verdes; .env.example con VSIM_PUBLIC_ENABLED=false.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constante de leyenda + extension del linter anti-insinuacion</name>
  <files>app/components/similitud-votacion-comparar.tsx, app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts (arrays SUPERFICIES_* :305-344, TERMINOS_PROHIBIDOS :355-463, NEGACIONES_LOCKED :471-493, bucle :565, self-check RELACIONES :784-807, no-falso-positivo :881-889)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-UI-SPEC.md (Copy table: caveat verbatim linea 118)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-RESEARCH.md (Pitfall 4 DEDUPE, Pitfall 3 negacion)
  </read_first>
  <behavior>
    - El linter MUERDE en memoria por cada idiom genuinamente nuevo inyectado: "votan juntos", "votan igual", "votan parecido", "aliados", "aliada", "tasa de coincidencia", "señal"
    - El linter NO se auto-caza: montada LEYENDA_SIMILITUD_VOTO verbatim (que contiene "afinidad" y "señal") → detectarInsinuaciones devuelve []
    - No hay doble-registro: idioms ya cubiertos (afín/afinidad/aliado/nivel de acuerdo/bloque de/vota como/votan como) NO se re-añaden
    - SUPERFICIES_VSIM entra al bucle de scan real
  </behavior>
  <action>
    Crear app/components/similitud-votacion-comparar.tsx conteniendo por ahora SOLO `export const LEYENDA_SIMILITUD_VOTO = "..."` con el caveat VERBATIM del UI-SPEC Copy table (linea 118): "La coincidencia alta es la norma, no una señal: la mayoría de las votaciones se aprueban por amplia mayoría o unanimidad. Coincidir en muchas no indica afinidad, coordinación ni bancada; discrepar en pocas no indica lo contrario." (El cuerpo presentacional del componente lo llena Plan 03; este plan solo fija el contrato de la constante para que el linter la importe.)
    Extender app/lib/anti-insinuacion-guard.test.ts: (1) añadir array `SUPERFICIES_VSIM = ["components/similitud-votacion-comparar.tsx"]` (NO duplicar app/comparar/page.tsx — ya esta en SUPERFICIES_RELACIONES); (2) hacer spread de ...SUPERFICIES_VSIM en el bucle de scan (:565); (3) DEDUPE-ar y añadir a TERMINOS_PROHIBIDOS SOLO los genuinamente nuevos: "votan juntos", "votan igual", "votan parecido", "aliados", "aliada", "tasa de coincidencia", "señal" (NO re-añadir afín/afinidad/aliado/nivel de acuerdo/bloque de/vota como/votan como — ya cubiertos, Pitfall 4); (4) `import { LEYENDA_SIMILITUD_VOTO } from "@/components/similitud-votacion-comparar"` y añadirla a NEGACIONES_LOCKED ANTES de que SUPERFICIES_VSIM entre al scan (Pitfall 3, espejo LEYENDA_CROSS_LINK :482); (5) añadir un `it(...)` de mutacion EN MEMORIA por cada idiom nuevo (espejo self-check RELACIONES) + un test no-falso-positivo que la LEYENDA montada verbatim → [].
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run anti-insinuacion-guard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run anti-insinuacion-guard` verde
    - Cada idiom nuevo tiene un it() de mutacion que prueba que el guard MUERDE (falla si el idiom se remueve de TERMINOS_PROHIBIDOS)
    - Un test prueba que LEYENDA_SIMILITUD_VOTO montada verbatim NO dispara falso positivo
    - app/components/similitud-votacion-comparar.tsx exporta LEYENDA_SIMILITUD_VOTO con el caveat VERBATIM del UI-SPEC
  </acceptance_criteria>
  <done>Linter extendido con idioms VSIM DEDUPE-ados + leyenda restada; muerde por cada idiom; no falso-positivo sobre su leyenda.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: co_votacion∉/red (borrar ramas muertas + test estatico) + 0068 escrita + allowlist</name>
  <files>app/components/red/red-graph.tsx, app/components/red/arista-hecho.tsx, app/components/co-votacion-red-guard.test.ts, supabase/migrations/0068_coincidencia_votos_par.sql, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/components/red/red-graph.tsx (TIPO_LABEL :79-82)
    - app/components/red/arista-hecho.tsx (switch etiquetaHecho :29-36)
    - app/lib/money-antiflip-guard.test.ts (helpers stripTsComments + walkSourceFiles a reusar para el nuevo guard)
    - app/lib/lockdown-guard.test.ts (PUBLIC_RPC_ALLOWLIST :165, entrada 101 :186, Direction-B :388-400, RPC_DEF_REGEX :372)
    - supabase/migrations/0067_militancia_historica_compartida.sql (mold secdef header :42-47, doble-revoke :86-87, comentario de orden :24-38)
    - .planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-PATTERNS.md (bloque RPC 0068 verbatim :138-164, Dead Branch Resolution)
  </read_first>
  <behavior>
    - co-votacion-red-guard MUERDE si se inyecta `co_votacion` en un fixture de codigo de superficie /red
    - Tras borrar las 2 ramas muertas, el scan de la superficie /red REAL (codigo, no comentarios) devuelve 0 ocurrencias de co_votacion/covotacion
    - red-graph.test.tsx sigue verde tras borrar las ramas
    - lockdown-guard Direction-B (allowlist ⊆ definidas) queda verde con la entrada coincidencia_votos_par (porque 0068 la define)
  </behavior>
  <action>
    Borrar la linea `co_votacion: "Misma votación",` de red-graph.tsx (TIPO_LABEL queda solo con co_lobby_contraparte). Borrar el `case "co_votacion":` + su return de arista-hecho.tsx (el default ya maneja tipo desconocido honestamente). VERIFICAR que red-graph.test.tsx siga verde (no depende de esas ramas).
    Crear app/components/co-votacion-red-guard.test.ts: escanea la superficie /red (app/app/red/**, app/components/red/**, supabase/migrations/0030_net.sql) buscando `co_votacion`/`covotacion`; reusar stripTsComments + walkSourceFiles del mold para IGNORAR comentarios (0030_net.sql:27,145 mencionan co_votacion en comentarios que documentan la exclusion — NO deben disparar; son documentacion legitima). Para el .sql aplicar un strip-comments SQL (lineas que empiezan con `--`). Añadir mutation self-check EN MEMORIA que prueba que el guard MUERDE si co_votacion se inyecta en un fixture de codigo.
    Escribir supabase/migrations/0068_coincidencia_votos_par.sql (NO aplicar — apply es Plan 02): copiar el skeleton secdef de 0067 (drop-before-create `drop function if exists public.coincidencia_votos_par(text, text);`, `language sql stable security definer`, `set search_path = ''`, `set statement_timeout = '5s'`, doble-revoke `revoke all on function public.coincidencia_votos_par(text, text) from public;` + `from anon, authenticated;`) con la firma `coincidencia_votos_par(p_a text, p_b text)` y `returns table (n_coinciden bigint, m_compartidas bigint, fecha_captura_max timestamptz)`. Cuerpo = la query verbatim de 102-PATTERNS lineas 138-164 (ver interfaces). EXACTAMENTE 3 columnas agregadas, NUNCA lista individual. Cabecera con comentario de orden ("Ultima migracion = 0067, esta es la 0068; aplicar por psql --single-transaction, NUNCA db push").
    Añadir `"coincidencia_votos_par",` a PUBLIC_RPC_ALLOWLIST en lockdown-guard.test.ts (alfabetico cerca de :168) — Direction-B queda satisfecha porque 0068 la define.
  </action>
  <verify>
    <automated>cd app && pnpm test -- --run co-votacion-red red-graph lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run co-votacion-red` verde (0 ocurrencias en codigo /red; self-check muerde)
    - `pnpm test -- --run red-graph` sigue verde tras borrar las ramas
    - `pnpm test -- --run lockdown-guard` verde con la entrada coincidencia_votos_par (Direction-B ⊆ definidas satisfecha por 0068 escrita)
    - supabase/migrations/0068_coincidencia_votos_par.sql define `create or replace function public.coincidencia_votos_par(p_a text, p_b text)` con returns de 3 columnas agregadas + doble-revoke, sin lista individual
  </acceptance_criteria>
  <done>Ramas muertas borradas; test estatico co_votacion∉/red verde permanente; 0068 escrita; allowlist actualizada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código → gate | El literal del flag decide exposición de una superficie legalmente gated |
| /red superficie → grafo público | co_votacion como label expondría insinuación espacial (DW-NOMINATE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | vsim-gate.ts | mitigate | anti-flip guard V1/V2/V3 + mutation self-check; único ignition `=== "true"` |
| T-102-02 | Information Disclosure | app/components/red/* | mitigate | co-votacion-red-guard estático permanente + borrado de ramas muertas |
| T-102-03 | Information Disclosure | copy VSIM | mitigate | linter idioms + leyenda VERBATIM restada de NEGACIONES_LOCKED (el sign-off legal en Plan 02/03 GARANTIZA) |
| T-102-04 | Elevation of Privilege | RPC 0068 (definida) | mitigate | doble-revoke CERO grant escrito en 0068 + allowlist Direction-B |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | Phase 102 NO instala paquetes (RESEARCH Package Legitimacy Audit: no aplica) |
</threat_model>

<verification>
- `cd app && pnpm test -- --run vsim-gate vsim-antiflip anti-insinuacion-guard co-votacion-red red-graph lockdown-guard` todo verde
- `.env.example` matchea `/^VSIM_PUBLIC_ENABLED\s*=\s*false\s*$/m`
- 0068 escrita con firma + returns + doble-revoke correctos; NO aplicada a PROD en este plan
- `grep -c co_votacion` sobre red-graph.tsx + arista-hecho.tsx (excluyendo comentarios) = 0
</verification>

<success_criteria>
- Los 6 specs verdes; gate fail-closed; guards anti-flip/anti-insinuacion/co_votacion∉red muerden por mutacion
- Ramas muertas co_votacion eliminadas de /red; red-graph.test.tsx intacto
- 0068 escrita (no aplicada); coincidencia_votos_par en PUBLIC_RPC_ALLOWLIST (Direction-B satisfecha)
- .env.example con VSIM_PUBLIC_ENABLED=false
</success_criteria>

<output>
Create `.planning/phases/102-relaciones-p2b-similitud-de-votaci-n-gated-legal/102-01-SUMMARY.md` when done
</output>
