---
phase: 103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg
plan: 05
type: execute
wave: 4
depends_on: ["103-01", "103-02", "103-03", "103-04"]
files_modified:
  - docs/legal/103-LEGAL-DOSSIER-NOTIF.md
  - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md
autonomous: false
requirements: [NOTIF-01, NOTIF-03, NOTIF-05]
must_haves:
  truths:
    - "Migrations 0069/0070/0071 are applied to PROD and their pgTAP is green against the applied PROD schema"
    - "The legal dossier 21.719 records the operator's pre-authorization as signoff: approved"
    - "NOTIF flag flips ON at deploy-time via Worker env var; .env.example stays false"
    - "The operator checkpoint covers publishable key + OTP template + SC2 curl evidence + Resend domain/API key"
    - "The DEPLOY-RUNBOOK has an executable Flag-OFF closure (NOTIF-05 fallback) for when provisioning fails"
    - "Seguir button present in DOM when flag ON (BrowserOS), absent when OFF"
  artifacts:
    - path: "docs/legal/103-LEGAL-DOSSIER-NOTIF.md"
      provides: "DPA Resend, licitud, ARCO-P, retención, signoff approved"
      contains: "signoff"
    - path: ".planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md"
      provides: "apply + deploy + operator checkpoint + Flag-OFF closure steps"
      contains: "Flag-OFF closure"
  key_links:
    - from: "supabase/migrations/0069_suscripcion_rls.sql"
      to: "PROD Postgres"
      via: "psql --single-transaction -f (applied)"
      pattern: "schema_migrations"
    - from: "deploy Worker env"
      to: "app/lib/notif-gate.ts"
      via: "NOTIF_PUBLIC_ENABLED=true at deploy (not committed)"
      pattern: "NOTIF_PUBLIC_ENABLED"
---

<objective>
Close the phase: apply migrations 0069/0070/0071 to PROD (BLOCKING — after guard + pgTAP green locally), verify post-apply pgTAP against the applied PROD schema, write the legal dossier 21.719 with the operator's pre-authorized signoff, deploy (build OpenNext Docker + wrangler), flip NOTIF_PUBLIC_ENABLED ON at deploy-time, and surface the single operator checkpoint (publishable key + OTP template + SC2 curl evidence + Resend domain/API key). If provisioning is not completable, execute the NOTIF-05 Flag-OFF closure so the feature is parked cleanly.

Purpose: The DDL is additive and deny-by-default (RLS user-owned) — PROD application is AUTHORIZED for this run (operator pre-authorized the legal checkpoint + flip). pgTAP against the applied PROD schema is the only proof the RLS actually runs (Pitfall 6). The flip happens at deploy (Worker env), never in committed `.env.example` — the anti-flip guard stays green. The operator checkpoint bundles every credential the agent cannot provide. NOTIF-05 requires an explicit, executable fallback: if the Resend domain/key or publishable key is unavailable, the flag stays OFF and the phase closes with a documented handoff.
Output: applied migrations, PROD pgTAP evidence, legal dossier (signoff approved), deploy, operator checkpoint, Flag-OFF closure fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md
@.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-CONTEXT.md
@docs/legal/102-LEGAL-DOSSIER-VSIM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legal dossier 21.719 + DEPLOY-RUNBOOK (incl. Flag-OFF closure)</name>
  <files>docs/legal/103-LEGAL-DOSSIER-NOTIF.md, .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md</files>
  <read_first>
    - docs/legal/102-LEGAL-DOSSIER-VSIM.md (clone structure — scope, disposition, signoff block)
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-CONTEXT.md (Área 4 — operator pre-authorized 2026-07-26, verbatim quote "autorizo desde ya el checkpoint legal (soy abogado)")
    - .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-RESEARCH.md (Security Domain — Dossier scope: DPA/subencargado Resend, base de licitud consentimiento, ARCO-P, retención; Assumptions A2/A6 → NOTIF-05 fallback trigger)
  </read_first>
  <action>
    Create docs/legal/103-LEGAL-DOSSIER-NOTIF.md cloning the 102-LEGAL-DOSSIER-VSIM.md structure. Scope: (1) DPA / subencargado — Resend as data processor (email addresses transferred for delivery); (2) Base de licitud — consent (double opt-in), recorded in consentimiento (fecha/versión/método); (3) Derechos ARCO-P — unsubscribe (one-click, login-less) + preference center in /cuenta + deletion via on delete cascade / account removal; (4) Retención — define retention of notificacion_envio logs and unsubscribed rows; (5) Signoff — record the operator's pre-authorization VERBATIM (2026-07-26, operador abogado, invocación de la corrida) as `signoff: approved`. The agent DOCUMENTS; the operator AUTHORIZED — state this explicitly.
    Create the 103-DEPLOY-RUNBOOK.md with these ordered sections:
      (a) Apply 0069→0070→0071 to PROD via `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f` (each once, per-migration; schema_migrations insert notes).
      (b) Run the three pgTAP files against the applied PROD schema via `psql -tA -f`.
      (c) Deploy (build OpenNext Docker node:22-slim, purge .pnpm-store from the mirror, MSYS_NO_PATHCONV=1, wrangler global OAuth — mirror 97-DEPLOY-RUNBOOK).
      (d) Flip NOTIF_PUBLIC_ENABLED=true as a Worker env var at deploy (NOT committed, NOT .env.example). Verify /spike-auth is absent from the build.
      (e) Operator checkpoint block (Task 3): publishable key + OTP template + SC2 curl evidence + Resend domain/API key.
      (f) **Flag-OFF closure (NOTIF-05 fallback) — FIRST-CLASS, executable.** Document the exact fallback path when the operator checkpoint CANNOT be completed (missing publishable key, unverified Resend sending domain, or absent RESEND_API_KEY — see RESEARCH Assumptions A2/A6):
          • **Flag stays OFF:** do NOT set the Worker env var NOTIF_PUBLIC_ENABLED (deny-by-default holds); the Seguir button remains absent from the DOM; /cuenta login and subscription surfaces are gated OFF. Schema (0069/0070/0071) and code remain shipped and inert — no user data is captured because nothing is exposed.
          • **Feature parked, not reverted:** migrations stay applied (additive, deny-by-default, safe at rest); the digest cron stays dispatch-only + dry-run (RESEND_API_KEY absent ⇒ no send); no emails are ever collected or sent.
          • **What gets documented:** in this runbook, record which specific credential was missing, the date, and that the flag was deliberately left OFF per NOTIF-05. State that email capture is NOT exposed in the deploy.
          • **Deuda entry:** append a deuda-de-operador entry to the 103-05-SUMMARY.md handoff (and note it belongs in STATE.md / MEMORY at phase close) listing the exact missing item(s) — publishable key, Resend verified domain, RESEND_API_KEY — and the one command to flip the flag ON once provisioned (set Worker env NOTIF_PUBLIC_ENABLED=true + redeploy). No `.env.example` change is ever required to flip.
          • **Legal consistency:** the dossier signoff stays `approved` (the operator's pre-authorization is unconditional), but the runbook notes the flip was not exercised this run; ARCO-P surfaces are dormant until the flip.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && bash -c "test -f docs/legal/103-LEGAL-DOSSIER-NOTIF.md && grep -ci 'signoff' docs/legal/103-LEGAL-DOSSIER-NOTIF.md && test -f .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md && grep -ci 'Flag-OFF closure' .planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-DEPLOY-RUNBOOK.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/legal/103-LEGAL-DOSSIER-NOTIF.md exists covering DPA/licitud/ARCO-P/retención with `signoff: approved` and the operator's verbatim pre-authorization
    - 103-DEPLOY-RUNBOOK.md has ordered apply → pgTAP → deploy → flip → operator-checkpoint steps
    - 103-DEPLOY-RUNBOOK.md has a first-class "Flag-OFF closure" section (NOTIF-05 fallback): flag stays OFF, feature parked (migrations kept, cron dry-run), what gets documented, the deuda entry with the flip command, and legal consistency
    - The dossier states the agent documents and the operator authorized (agent never provides the sign-off itself)
  </acceptance_criteria>
  <done>Legal dossier 21.719 signed-off (operator pre-authorization recorded) + deploy runbook with the exact apply/deploy/flip/checkpoint sequence AND an executable Flag-OFF closure (NOTIF-05 fallback).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migrations to PROD + post-apply pgTAP</name>
  <what-built>
    Migrations 0069 (suscripcion RLS), 0070 (notificacion_envio queue), 0071 (consentimiento) — additive, deny-by-default, RLS user-owned. pgTAP tests proving two-user isolation, queue zero-grant, consent shape. Guard (Plan 01) + local pre-apply pgTAP (Plan 02) already green. PROD application is AUTHORIZED for this run (operator pre-authorized; RLS user-owned tables are additive and deny-by-default).
  </what-built>
  <how-to-verify>
    Apply to PROD in order, each once, per the 103-DEPLOY-RUNBOOK:
    1. `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0069_suscripcion_rls.sql`
    2. `… -f supabase/migrations/0070_notificacion_envio.sql`
    3. `… -f supabase/migrations/0071_consentimiento.sql`
    4. Insert schema_migrations rows (0069/0070/0071) per the migration notes.
    5. Run each pgTAP against the applied PROD schema: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0069_suscripcion_rls.test.sql` (repeat for 0070/0071) — every line `ok`, zero `not ok`.
    6. Confirm `cd app && pnpm test -- --run lockdown-guard` still green against the now-applied schema.
    Expected: all pgTAP green; user-A-no-ve-B holds on PROD; authenticated has zero grant on notificacion_envio; anon has no select.
  </how-to-verify>
  <resume-signal>Type "aplicado" with the pgTAP ok-counts (0069/0070/0071), or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Operator provisioning + deploy + flip ON + SC2 evidence (OR Flag-OFF closure)</name>
  <what-built>
    All code is built and PROD schema applied. What remains requires operator credentials the agent cannot provide: Supabase publishable key + OTP template, Resend domain + API key, the deploy, and the deploy-time flag flip. The flip is AUTHORIZED this run (CONTEXT Área 4). If a credential is unavailable, execute the Flag-OFF closure (NOTIF-05 fallback) from the DEPLOY-RUNBOOK §(f).
  </what-built>
  <how-to-verify>
    Per 103-DEPLOY-RUNBOOK + 97-DEPLOY-RUNBOOK §"Estado de runtime pendiente" + 97-SPIKE-EVIDENCE §Reproducción SC2:
    1. Supabase: create publishable key `sb_publishable_…` (project bctyygbmqcvizyplktuw); Auth → Email provider ON + OTP template renders `{{ .Token }}` (NOT `{{ .ConfirmationURL }}`); `wrangler secret put SUPABASE_PUBLISHABLE_KEY` + confirm with `wrangler secret list`; value into local `.env` (NOT `.env.example`).
    2. Resend: verify a sending domain for the `from` address; create API key; `wrangler secret put RESEND_API_KEY` (deploy) + GH secret `RESEND_API_KEY` (cron); value into local `.env`.
    3. Deploy: build OpenNext (Docker node:22-slim, purge .pnpm-store, MSYS_NO_PATHCONV=1), wrangler deploy (global OAuth). Confirm /spike-auth is gone; Camino A + CSP intact.
    4. Flip: set `NOTIF_PUBLIC_ENABLED=true` as a Worker env var at deploy (NOT committed, NOT .env.example — anti-flip guard stays green).
    5. SC2 evidence: run the 97-SPIKE-EVIDENCE curl block (PII-redacted) confirming Set-Cookie + session refresh through the OpenNext pipeline on the live deploy.
    6. BrowserOS DOM check: flag ON ⇒ Seguir button PRESENT on both fichas; (optionally) confirm a manual green `workflow_dispatch` of digest-daily (dry-run acceptable if domain pending).
    IF any credential is unavailable (publishable key, Resend verified domain, or RESEND_API_KEY): execute DEPLOY-RUNBOOK §(f) Flag-OFF closure — leave NOTIF_PUBLIC_ENABLED unset (flag OFF), park the feature (migrations kept, cron dry-run), record the missing item + date + deuda entry, and confirm email capture is NOT exposed.
  </how-to-verify>
  <resume-signal>Type "listo" with: publishable key loaded + OTP template confirmed + Resend domain/key status + deploy version + SC2 evidence + BrowserOS Seguir-present; OR "flag-off" naming the missing credential (feature parked per DEPLOY-RUNBOOK §(f) Flag-OFF closure, deuda recorded).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration files → PROD Postgres | applying DDL; RLS is the boundary that must survive apply |
| committed repo → deploy env | flag flip lives at deploy, never committed |
| operator credentials → runtime | keys the agent cannot and must not hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-17 | Governance | flag flipped without legal signoff | mitigate | dossier signoff: approved recorded BEFORE flip; flip is deploy-time + operator checkpoint |
| T-103-18 | Tampering | committed .env.example=true | mitigate | flip is Worker env var only; anti-flip guard (Plan 01) keeps .env.example=false |
| T-103-19 | Information Disclosure | RLS fails silently on PROD apply | mitigate | post-apply pgTAP against applied PROD schema (two-user isolation, Pitfall 6) |
| T-103-20 | Spoofing | OTP template renders ConfirmationURL not Token | mitigate | operator checkpoint verifies `{{ .Token }}` template + SC2 evidence |
| T-103-24 | Information Disclosure | email capture exposed before provisioning is complete | mitigate | Flag-OFF closure (NOTIF-05): flag stays OFF until publishable key + Resend domain/key ready; feature parked, no capture |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new external packages; deploy reuses existing toolchain |
</threat_model>

<verification>
- All three migrations applied to PROD; schema_migrations updated
- Post-apply pgTAP (0069/0070/0071) green against the applied PROD schema
- Legal dossier signoff: approved recorded
- NOTIF_PUBLIC_ENABLED flipped ON at deploy (Worker env); .env.example still false; anti-flip guard green
- OR Flag-OFF closure executed (flag OFF, feature parked, deuda recorded) if provisioning incomplete
- SC2 evidence captured; BrowserOS: Seguir present when ON
- Full suite + all guards green
</verification>

<success_criteria>
- Migrations applied to PROD with post-apply pgTAP green (RLS isolation holds on PROD)
- Legal dossier 21.719 signed-off (operator pre-authorization documented)
- Deploy live with NOTIF flag ON (deploy-time flip; .env.example untouched)
- Operator checkpoint resolved (publishable key + OTP + Resend + SC2 evidence) OR feature parked behind flag OFF via the executable Flag-OFF closure (NOTIF-05 fallback), deuda recorded
</success_criteria>

<output>
Create `.planning/phases/103-notif-p3a-suscripciones-digest-guards-authenticated-gate-leg/103-05-SUMMARY.md` when done
</output>
