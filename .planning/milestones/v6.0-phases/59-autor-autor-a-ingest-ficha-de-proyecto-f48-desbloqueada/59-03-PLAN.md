---
phase: 59
plan: 03
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - app/app/proyecto/[boletin]/page.tsx
  - app/components/autor-row.tsx
  - app/components/autor-row.test.tsx
autonomous: true
requirements:
  - AUTOR-02

must_haves:
  truths:
    - "La ficha de proyecto muestra sección #autores colapsada con conteo visible si hay autores"
    - "Autores con parlamentario_id confirmado muestran link a /parlamentario/[id]"
    - "Autores sin parlamentario_id muestran IdentityMarker (guarda de identidad)"
    - "Sección ausente del DOM si 0 filas de autoría (moción sin datos)"
    - "Boletines tipo Mensaje con 0 filas muestran línea 'Iniciativa del Ejecutivo' en vez de sección"
  artifacts:
    - path: app/components/autor-row.tsx
      provides: componente AutorRow con identity guard (link o IdentityMarker)
      exports: [AutorRow]
    - path: app/components/autor-row.test.tsx
      provides: RTL tests 4 estados de AutorRow/AutoresSection
      contains: "confirmado\|IdentityMarker\|Ejecutivo\|null"
  key_links:
    - from: app/app/proyecto/[boletin]/page.tsx
      to: proyecto_autor (Supabase)
      via: createServerSupabase().from('proyecto_autor').select().eq('boletin', boletin)
      pattern: "proyecto_autor"
    - from: AutoresSection
      to: AutorRow
      via: autores.map(a => <AutorRow autor={a} />)
      pattern: "AutorRow"
---

<objective>
Montar la sección de autoría (F48) en la ficha de proyecto: componente AutorRow con guarda de identidad, AutoresSection server component con 3 estados honestos (autores, Mensaje del Ejecutivo, ausente), y entrada en el rail de navegación.

Purpose: AUTOR-02 — la ficha de proyecto responde "¿Quién presentó este proyecto?" con trazabilidad a la fuente y guarda de identidad (link solo si confirmado).
Output: AutorRow + autores-section inline en page.tsx + RTL tests 4 estados + pnpm build verde.
</objective>

<execution_context>
@C:\Users\Carlo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Carlo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-CONTEXT.md
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-RESEARCH.md
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-01-SUMMARY.md

<interfaces>
<!-- Interfaces clave del codebase. No explorar — usar estas directamente. -->

From app/app/proyecto/[boletin]/page.tsx (patrón DetalleColapsable existente):
```typescript
// Uso en page.tsx (tramitacion — línea ~414):
<DetalleColapsable n={eventos.length} defaultOpen={urgenciaExpandida != null}>
  <TimelineView ... />
</DetalleColapsable>
// Para autores: defaultOpen={false} (colapsado por defecto per CONTEXT.md)
// El rail se construye pasando entries con { id, label, count } al componente de navegación existente
```

From app/components/voto-row.tsx (patrón identity guard a espejear):
```typescript
// VotoRow: si parlamentario_id confirmado → Link href="/parlamentario/[id]"
// si no → IdentityMarker (renders nombre crudo sin link)
// AutorRow debe espejear exactamente este patrón
```

From app/components/autores-list.tsx (componente existente — plain text, SIN identity guard):
```typescript
// AutoresList actual solo renderiza strings planos.
// NO modificar AutoresList (puede tener otros callers).
// Crear nuevo AutorRow que acepta ProyectoAutorRow con parlamentario_id.
```

From packages/tramitacion/src/model.ts (tipo a usar en el componente):
```typescript
export interface ProyectoAutor {
  id?: number;
  boletin: string;
  autor_crudo: string;
  autor_crudo_norm: string;
  parlamentario_id: string | null;   // null → IdentityMarker
  metodo: MetodoVinculo | null;
  estado_vinculo: EstadoVinculo | null;
  origen: string;
  fecha_captura: string;
  enlace: string;
}
```

ProvenanceBadge ya existe en el codebase — importar desde su ubicación actual.
IdentityMarker ya existe en el codebase — importar desde su ubicación actual.
createServerSupabase ya existe — importar desde el helper de app/.
BOLETIN_RE ya existe para validar el path param — usar el import existente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AutorRow con identity guard + RTL tests 4 estados</name>
  <files>
    app/components/autor-row.tsx,
    app/components/autor-row.test.tsx
  </files>
  <behavior>
    - Test estado "confirmado": AutorRow con parlamentario_id "P001" → renderiza `<a href="/parlamentario/P001">` con nombre del autor
    - Test estado "no_confirmado": AutorRow con parlamentario_id null → renderiza IdentityMarker (sin `<a>`) con nombre crudo
    - Test AutoresSection (mock de datos): 0 filas + iniciativa "Mensaje" → renderiza texto "Ejecutivo" o "Mensaje presidencial"
    - Test AutoresSection (mock): 0 filas + iniciativa "Moción" → retorna null (sección ausente del DOM)
  </behavior>
  <action>
    **AutorRow** — Crear `app/components/autor-row.tsx` como componente React (puede ser async si necesita Suspense, pero preferir sync si los datos ya llegan por prop). Props: `{ autor: ProyectoAutorRow }` donde `ProyectoAutorRow` es el tipo Supabase inferido de `proyecto_autor` (o importar `ProyectoAutor` desde `@obs/tramitacion`).

    Estructura de render (espejo de VotoRow):
    - Si `autor.parlamentario_id` es not null: renderizar `<Link href={/parlamentario/${autor.parlamentario_id}}>` con `autor.autor_crudo` como texto.
    - Si `autor.parlamentario_id` es null: renderizar `<IdentityMarker nombre={autor.autor_crudo} />` (sin enlace).
    - En ambos casos, mostrar `ProvenanceBadge` con `{ origen: autor.origen, fecha: autor.fecha_captura, enlace: autor.enlace }` al lado o debajo (seguir el patrón visual de otros rows en la ficha).
    - Vocabulario copy: el componente NO debe usar lenguaje causal ("autor de...", "responsable de..."); usar "Presentó este proyecto" o simplemente el nombre vinculado. Anti-insinuación: sin composición con votos/lobby/dinero en este componente.

    **autor-row.test.tsx** — Tests RTL. Usar `@testing-library/react` + vitest. Mockear IdentityMarker si es un componente server (o usar su implementación real si es compatible con jsdom). Cubrir los 4 casos del `<behavior>` block. Para los tests de AutoresSection (estados Mensaje y ausente), puede ser un test del componente inline o un subcomponente si el executor prefiere separarlo — lo que permite las RTL más sencillas dado el patrón de la ficha.

    NO importar `next/navigation` en el test file (GOTCHA: rompe jsdom). Usar `jest.mock`/`vi.mock` para `next/link` si es necesario (seguir el patrón de los otros tests de componentes en app/).
  </action>
  <verify>
    <automated>pnpm --filter app test --run -- -t "AutorRow\|autores"</automated>
  </verify>
  <done>RTL tests 4 estados pasan: link si confirmado, IdentityMarker si null, texto Ejecutivo si Mensaje+0rows, null si Moción+0rows. AutorRow compila sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: AutoresSection + rail entry en page.tsx + build verde</name>
  <files>
    app/app/proyecto/[boletin]/page.tsx
  </files>
  <action>
    **AutoresSection** — Añadir función/componente async `AutoresSection({ boletin }: { boletin: string })` dentro de `app/app/proyecto/[boletin]/page.tsx` (inline como las otras secciones del archivo, o en un archivo hermano si el executor detecta que page.tsx ya es muy largo — en ese caso crear `app/app/proyecto/[boletin]/autores-section.tsx` y @-importar). Implementación según RESEARCH.md Pattern 7:

    1. Fetch en paralelo con `leerProyecto`:
       ```typescript
       const [autoresResult] = await Promise.all([
         createServerSupabase()
           .from("proyecto_autor")
           .select("*")
           .eq("boletin", boletin)
           .order("id", { ascending: true }),
       ]);
       ```
       (Si `leerProyecto` ya se llama en el componente padre y pasa `iniciativa` como prop, usar esa prop en vez de un segundo fetch.)

    2. Lógica de tres estados:
       - 0 filas + `proyecto.iniciativa === "Mensaje"` → `<p className="text-sm text-muted-foreground">Iniciativa del Ejecutivo (Mensaje presidencial).</p>`
       - 0 filas + cualquier otro iniciativa → `return null` (sección ausente del DOM)
       - N filas → `<DetalleColapsable n={autores.length} defaultOpen={false}>{autores.map(a => <AutorRow key={a.id} autor={a} />)}</DetalleColapsable>`

    3. Wrapper de sección (espejo de otras secciones de la ficha):
       ```tsx
       <section id="autores" className="mt-12">
         <h2 className="...">¿Quién presentó este proyecto?</h2>
         <AutoresSection boletin={boletin} />
       </section>
       ```
       Insertar la sección entre las secciones existentes (después de tramitación, antes de o después de votaciones — orden por discreción del executor, coherente con el rail). Título orientado a pregunta ciudadana per CONTEXT.md specifics y COMP-03 pattern.

    **Rail entry** — Localizar el array/objeto del rail de navegación en page.tsx (el que construye las entradas de anchor links con id + label + count). Añadir entrada `{ id: "autores", label: "Autores", count: autores.length }` SOLO si `autores.length > 0`. El rail no debe mostrar la entrada si no hay datos (honestidad = ausencia de sección en rail también). Seguir el patrón exacto de las entradas existentes.

    **pnpm build** — Verificar que el build de la app Next.js pasa sin errores de TypeScript ni de rendering. Ejecutar `pnpm --filter app build`. Si el build falla por Server/Client Component mismatch (AutorRow usa Link de next/link que es client-compatible en RSC), ajustar si es necesario. NOT intentar build OpenNext (Docker Linux, fuera de alcance de este plan — Phase 61 hace el deploy).
  </action>
  <verify>
    <automated>pnpm --filter app build 2>&1 | tail -5</automated>
  </verify>
  <done>pnpm build de app pasa sin errores. page.tsx contiene `<section id="autores">` y `"proyecto_autor"`. AutoresSection renderiza los 3 estados. Rail tiene entrada "autores" condicional al count. Suite RTL + typecheck verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param [boletin] → Supabase query | BOLETIN_RE valida el param antes de .eq(); .eq() parametriza la query (no interpolación) |
| proyecto_autor → cliente | RLS public-read; no hay PII en proyecto_autor (parlamentario_id es UUID referencia, no PII) |
| AutorRow → Link href | parlamentario_id es un ID interno, no user-controlled input; no XSS risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Tampering | AutorRow link URL | mitigate | Link href construido como `/parlamentario/${parlamentario_id}` donde parlamentario_id viene de la DB (no del request); BOLETIN_RE ya valida el boletin param upstream |
| T-59-05 | Information Disclosure | AutoresSection sin datos | mitigate | 0 filas → sección ausente (no "sin datos disponibles" que insinúa hay datos pero no se muestran); honest-state es parte del threat model |
| T-59-SC | Tampering | npm installs | accept | Sin nuevas dependencias |
</threat_model>

<verification>
- `pnpm --filter app test --run` — RTL tests de AutorRow 4 estados pasan
- `pnpm --filter app build` — build Next.js pasa sin errores
- `pnpm -w typecheck` — sin errores TypeScript
- `grep -c "proyecto_autor" app/app/proyecto/\[boletin\]/page.tsx` >= 1
- `grep -c "id=\"autores\"" app/app/proyecto/\[boletin\]/page.tsx` >= 1
- `grep -v "^//" app/components/autor-row.tsx | grep -c "parlamentario_id"` >= 1
</verification>

<success_criteria>
- AutorRow: link a /parlamentario/[id] si confirmado, IdentityMarker si null — RTL pasa.
- AutoresSection: 3 estados correctos (autores, Mensaje del Ejecutivo, ausente del DOM).
- page.tsx monta la sección con id="autores" y rail entry condicional.
- pnpm build verde. Anti-insinuación: sección no compone con votos/lobby/dinero.
- pnpm -w test verde (suite completa 720+).
</success_criteria>

<output>
Crear `.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-03-SUMMARY.md` cuando termine. Anotar: estados RTL passing, build result, y nota sobre deploy pendiente (Phase 61 / Docker Linux + wrangler local per MEMORY.md).
</output>
