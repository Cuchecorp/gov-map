---
phase: 59
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/src/parse-senado-tramitacion.ts
  - packages/tramitacion/src/parse-senado-tramitacion.test.ts
  - packages/tramitacion/src/model.ts
  - packages/tramitacion/src/reconciliar-autor.ts
  - packages/tramitacion/src/reconciliar-autor.test.ts
  - packages/tramitacion/src/writer.ts
  - packages/tramitacion/src/writer-supabase.ts
  - packages/tramitacion/src/writer-supabase.test.ts
  - packages/tramitacion/src/ingest-run.ts
  - packages/tramitacion/src/__fixtures__/mocion-16588-autores.xml
  - supabase/migrations/0051_proyecto_autor.sql
  - supabase/tests/0051_proyecto_autor.test.sql
autonomous: true
requirements:
  - AUTOR-01

must_haves:
  truths:
    - "El parser extrae nombres de autores de XML Senado con estructura <autor><PARLAMENTARIO>Name</PARLAMENTARIO></autor>"
    - "Un nombre que existe en la maestra como único match → parlamentario_id poblado (confirmado)"
    - "Un nombre sin match en la maestra → autor_crudo guardado, parlamentario_id null (no_confirmado)"
    - "La tabla proyecto_autor existe en la DB con RLS public-read y clave natural (boletin, autor_crudo_norm)"
    - "upsertAutores se llama desde ingest-run después de upsertProyecto (orden FK correcto)"
  artifacts:
    - path: supabase/migrations/0051_proyecto_autor.sql
      provides: DDL tabla proyecto_autor con RLS
      contains: "create table proyecto_autor"
    - path: supabase/tests/0051_proyecto_autor.test.sql
      provides: pgTAP tests de la tabla
      contains: "has_table"
    - path: packages/tramitacion/src/reconciliar-autor.ts
      provides: reconciliarAutores + AutorParaEscribir
      exports: [reconciliarAutores]
    - path: packages/tramitacion/src/__fixtures__/mocion-16588-autores.xml
      provides: fixture XML real con 5 autores
      contains: "<PARLAMENTARIO>"
  key_links:
    - from: packages/tramitacion/src/ingest-run.ts
      to: packages/tramitacion/src/writer.ts
      via: upsertAutores call after upsertProyecto
      pattern: "upsertAutores"
    - from: packages/tramitacion/src/reconciliar-autor.ts
      to: packages/identity/src/index.ts
      via: matchDeterminista + confirmar
      pattern: "matchDeterminista"
---

<objective>
Fix the parser bug that silently drops all moción authors, define the ProyectoAutor model and migration, implement deterministic reconciliation, extend the writer interface and Supabase implementation, and hook the author pipeline into ingest-run.

Purpose: AUTOR-01 — after this plan, the full ingest pipeline for authors is code-complete and tested locally. Plan 02 applies the migration to PROD and runs the live backfill.
Output: parser fix + fixture + model + migration (0051) + pgTAP + reconciliar-autor + writer extension + ingest-run hook + unit tests
</objective>

<execution_context>
@C:\Users\Carlo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Carlo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-CONTEXT.md
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-RESEARCH.md

<interfaces>
<!-- Interfaces clave extraídas del codebase para el executor. Sin necesidad de exploración. -->

From packages/tramitacion/src/model.ts:
```typescript
export type Iniciativa = "Mensaje" | "Moción";
export type MetodoVinculo = "determinista" | "llm" | "humano";
export type EstadoVinculo = "confirmado" | "probable" | "no_confirmado";

export interface Proyecto {
  boletin: string;
  boletin_num: string;
  titulo: string;
  iniciativa: Iniciativa | null;
  camara_origen: string | null;
  autores: string[];      // populated by parser fix
  materia: string | null;
  estado: string | null;
  etapa: string | null;
  subetapa: string | null;
  origen: string;
  fecha_captura: string;
  enlace: string;
}
```

From packages/tramitacion/src/writer.ts:
```typescript
export interface VotoParaEscribir {
  votacion_id: string;
  fuente_voter_id: string;
  mencion_nombre: string;
  enlace: EnlaceConfirmado | null;
  seleccion: Voto["seleccion"];
  metodo: Voto["metodo"];
  estado_vinculo: Voto["estado_vinculo"];
}

export interface TramitacionWriter {
  upsertProyecto(proyecto: Proyecto): Promise<void>;
  upsertVotacion(votaciones: Votacion[]): Promise<void>;
  upsertVotos(votos: VotoParaEscribir[]): Promise<void>;
  upsertEventos(eventos: TramitacionEvento[]): Promise<void>;
  // Plan 01 ADDS: upsertAutores(autores: AutorParaEscribir[]): Promise<void>;
}
```

From packages/identity/src/index.ts (verified exports):
```typescript
export { matchDeterminista } from "./deterministic";
export { confirmar } from "./confirmar";
export { normalizarNombre } from "@obs/core";
// matchDeterminista(mention: Mention, maestra: MaestraRow[]): Resolution
// confirmar(id: string): EnlaceConfirmado
// normalizarNombre({ paterno, materno?, nombres }): { nombre_normalizado: string }
```

From packages/tramitacion/src/parse-senado-tramitacion.ts — el bug:
```typescript
// BUG: txt() busca "#text" — falla para <autor><PARLAMENTARIO>Name</PARLAMENTARIO></autor>
// fast-xml-parser 5.9.2 parsea ese nodo como { PARLAMENTARIO: "Name" }, NOT { "#text": "Name" }
function txt(v: unknown): string | null { /* looks for #text only */ }
// FIX: añadir txtAutor() que lee rec["PARLAMENTARIO"] primero (ver RESEARCH.md Pattern 1)
```

From supabase/migrations/0008_tramitacion.sql — patrón RLS a espejear:
```sql
alter table tramitacion_evento enable row level security;
create policy tramitacion_evento_public_read on tramitacion_evento for select to anon using (true);
grant select on tramitacion_evento to anon;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parser fix + fixture + ProyectoAutor model + migración 0051</name>
  <files>
    packages/tramitacion/src/__fixtures__/mocion-16588-autores.xml,
    packages/tramitacion/src/parse-senado-tramitacion.ts,
    packages/tramitacion/src/parse-senado-tramitacion.test.ts,
    packages/tramitacion/src/model.ts,
    supabase/migrations/0051_proyecto_autor.sql,
    supabase/tests/0051_proyecto_autor.test.sql
  </files>
  <behavior>
    - Test: parsear fixture mocion-16588-autores.xml con `parseSenadorTramitacion()` retorna `autores` con 5 strings no vacíos
    - Test: parsear fixture con boletín tipo Mensaje retorna `autores: []` (correcto, no bug)
    - Test: parsear fixture moción retorna `iniciativa === "Moción"`
  </behavior>
  <action>
    **Fixture XML** — Crear `packages/tramitacion/src/__fixtures__/mocion-16588-autores.xml` con contenido mínimo real del Senado para boletín 16588 que incluya nodo `<autores>` con 5 `<autor><PARLAMENTARIO>Nombre Apellido</PARLAMENTARIO></autor>` y `<iniciativa>Moción</iniciativa>` en `<descripcion>`. Usar los 5 nombres reales verificados en RESEARCH.md (vía live fetch 2026-07-08). El XML debe ser válido y parseable por fast-xml-parser.

    **Parser fix** — En `parse-senado-tramitacion.ts`, añadir la función helper `txtAutor(a: unknown): string | null` que lee `(a as Record<string,unknown>)["PARLAMENTARIO"]` primero (nested tag form), con fallback a `#text` para defensividad. Reemplazar el `map(txt)` existente sobre `autoresRaw?.autor` con `map(txtAutor)`. La función `txt()` existente NO se modifica (sigue usándose para otros nodos). Ver RESEARCH.md Pattern 1 para la implementación exacta.

    **Model.ts** — Añadir al final del archivo el tipo `ProyectoAutor` y `AutorParaEscribir` (branded-input mirror de `VotoParaEscribir`):
    - `ProyectoAutor`: { id?: number; boletin: string; autor_crudo: string; autor_crudo_norm: string; parlamentario_id: string | null; metodo: MetodoVinculo | null; estado_vinculo: EstadoVinculo | null; origen: string; fecha_captura: string; enlace: string }
    - `AutorParaEscribir`: { boletin: string; autor_crudo: string; autor_crudo_norm: string; enlace_confirmado: EnlaceConfirmado | null; metodo: MetodoVinculo | null; estado_vinculo: EstadoVinculo | null; origen: string; fecha_captura: string; enlace_provenance: string }
    - `ProyectoAutorSchema` (zod) espejando los campos.
    - Función de aplanado `aplanarAutor(row: AutorParaEscribir): Omit<ProyectoAutor, 'id'>` que materializa `parlamentario_id = row.enlace_confirmado?.parlamentarioId ?? null`.

    **Migration 0051** — Crear `supabase/migrations/0051_proyecto_autor.sql` con el DDL exacto de RESEARCH.md Pattern 5:
    - `create table proyecto_autor (id bigint generated always as identity primary key, boletin text not null references proyecto(boletin), autor_crudo text not null, autor_crudo_norm text not null, parlamentario_id text references parlamentario(id), metodo text check (metodo in ('determinista','humano')), estado_vinculo text check (estado_vinculo in ('confirmado','no_confirmado')), origen text not null, fecha_captura timestamptz not null default now(), enlace text not null, unique (boletin, autor_crudo_norm));`
    - Índices: `proyecto_autor_boletin_idx` en `(boletin)` y `proyecto_autor_parlamentario_idx` en `(parlamentario_id) where parlamentario_id is not null`.
    - RLS: `enable row level security` + policy `proyecto_autor_public_read for select to anon using (true)` + `grant select on proyecto_autor to anon`.
    - NO incluir sentencias pgTAP en el migration file (van en el archivo de test separado).

    **pgTAP test** — Crear `supabase/tests/0051_proyecto_autor.test.sql` con:
    - `select has_table('public', 'proyecto_autor', 'tabla proyecto_autor existe');`
    - `select col_is_nullable('public', 'proyecto_autor', 'parlamentario_id', 'parlamentario_id nullable');`
    - `select col_not_null('public', 'proyecto_autor', 'autor_crudo', 'autor_crudo not null');`
    - `select col_not_null('public', 'proyecto_autor', 'boletin', 'boletin not null');`
    - `select has_index('public', 'proyecto_autor', 'proyecto_autor_boletin_idx', 'indice boletin existe');`
    - `select policies_are('public', 'proyecto_autor', ARRAY['proyecto_autor_public_read'], 'RLS policy public_read');`
    Envuelto en `SELECT plan(6); ... SELECT finish();`
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run -- -t "autores\|autor\|PARLAMENTARIO"</automated>
  </verify>
  <done>Tests de parser pasan: fixture moción retorna 5 autores no vacíos; fixture Mensaje retorna []. Archivos 0051_proyecto_autor.sql y 0051_proyecto_autor.test.sql existen con contenido correcto. ProyectoAutor y AutorParaEscribir exportados desde model.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconciliar-autor + extensión writer + hook ingest-run</name>
  <files>
    packages/tramitacion/src/reconciliar-autor.ts,
    packages/tramitacion/src/reconciliar-autor.test.ts,
    packages/tramitacion/src/writer.ts,
    packages/tramitacion/src/writer-supabase.ts,
    packages/tramitacion/src/writer-supabase.test.ts,
    packages/tramitacion/src/ingest-run.ts,
    packages/tramitacion/src/index.ts
  </files>
  <behavior>
    - Test reconciliarAutores: nombre único en maestra (diputados o senado 2022-2026) → estado_vinculo "confirmado", parlamentario_id populated via confirmar()
    - Test reconciliarAutores: nombre desconocido → estado_vinculo "no_confirmado", enlace_confirmado null
    - Test reconciliarAutores: lista vacía (Mensaje) → retorna []
    - Test upsertAutores en InMemoryTramitacionWriter: segunda llamada con mismo input → Map.size no crece
    - Test ingest-run: mock writer verifica que upsertAutores se llama después de upsertProyecto
  </behavior>
  <action>
    **reconciliar-autor.ts** — Crear `packages/tramitacion/src/reconciliar-autor.ts` implementando `reconciliarAutores(autores: string[], maestra: Parlamentario[], boletin: string, provenance: {origen: string; fecha_captura: string; enlace: string}): AutorParaEscribir[]`. Lógica exacta de RESEARCH.md Pattern 3:
    - Para cada nombre: `autor_crudo_norm = nombre.toLowerCase().replace(/\s+/g,' ').trim()` (NOT normalizarNombre — esta es la clave de dedup, no el campo para matching).
    - Para matching: usar `normalizarNombre({paterno: nombre, nombres: ''})` para obtener `nombre_normalizado`, luego probar `matchDeterminista({nombreNormalizado, camara: "diputados", periodo: "2022-2026"}, maestra)` y si no es confirmado probar `camara: "senado"`. Si alguno retorna `estado: "confirmado"` → `enlace_confirmado: confirmar(id)`, `metodo: "determinista"`, `estado_vinculo: "confirmado"`. Si ninguno → `enlace_confirmado: null`, `metodo: null`, `estado_vinculo: "no_confirmado"`.
    - Importar `matchDeterminista`, `confirmar`, `normalizarNombre` desde `@obs/identity`.
    - No usar LLM, no usar RUT. Fail-closed total.

    **reconciliar-autor.test.ts** — Tests con maestra sintética pequeña (2-3 filas) que cubran: match confirmado, no_confirmado, lista vacía. Usar la misma maestra sintética que `reconciliar-senado.test.ts` como referencia de estructura.

    **writer.ts** — Añadir `AutorParaEscribir` al import y agregar al interface `TramitacionWriter`: `upsertAutores(autores: AutorParaEscribir[]): Promise<void>;`. En `InMemoryTramitacionWriter` añadir: `readonly autores = new Map<string, Omit<ProyectoAutor,'id'>>();` y el método que llama `aplanarAutor(row)` y hace upsert por `${boletin}\x00${autor_crudo_norm}`.

    **writer-supabase.ts** — Añadir `upsertAutores` a `SupabaseTramitacionWriter` siguiendo el patrón `upsertVotos` (RESEARCH.md Pattern 4): dedup por `${boletin}\x00${autor_crudo_norm}`, chunk de N (usar la misma constante CHUNK del archivo), `.upsert(lote, {onConflict: "boletin,autor_crudo_norm", ignoreDuplicates: false})`. Los objetos a insertar se mapean con `aplanarAutor()`.

    **writer-supabase.test.ts** — Añadir un test que ejercita `upsertAutores` con el fake/mock existente o con el writer en memoria para verificar idempotencia. Seguir el patrón de tests existentes en ese archivo.

    **ingest-run.ts** — Localizar el bloque que llama `writer.upsertProyecto(proyecto)`. DESPUÉS de ese call (y después de que `upsertVotos` se llame si existe ese patrón), añadir:
    ```
    const autores = reconciliarAutores(proyecto.autores, opts.maestra, boletinKey, provCols);
    await opts.writer.upsertAutores(autores);
    ```
    Donde `opts.maestra` ya existe si se pasa por opciones (si no existe, añadir `maestra: Parlamentario[]` a las opciones de ingest-run y actualizarlo en `ingest-cli.ts` donde se construye). Mantener el logging existente: añadir `nAutores` al resumen de fin de corrida.

    **index.ts** — Exportar `reconciliarAutores` y `AutorParaEscribir` desde el barrel del paquete si no están ya.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run && pnpm -w typecheck</automated>
  </verify>
  <done>Toda la suite del paquete tramitacion pasa. typecheck verde. reconciliarAutores exportado. upsertAutores en TramitacionWriter interface y en ambas implementaciones (InMemory y Supabase). ingest-run.ts llama upsertAutores después de upsertProyecto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R2 envelope → parser | tramXml viene de crudo inmutable en R2; el parser lee strings, no ejecuta |
| parser output → writer | AutorParaEscribir con EnlaceConfirmado branded — no acepta string crudo |
| writer → Supabase | service_role (bypass RLS por diseño); escritura solo desde server/Edge |
| anon → proyecto_autor | RLS public-read policy + grant pair — solo SELECT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Elevation of Privilege | proyecto_autor RLS | mitigate | Policy `for select to anon using (true)` + `grant select on proyecto_autor to anon`; espejo exacto de 0008; pgTAP verifica policy y grant antes de merge |
| T-59-02 | Tampering | reconciliarAutores FK | mitigate | EnlaceConfirmado branded type — asignación de string crudo a parlamentario_id es error de compilación; aplanarAutor() único punto de materialización |
| T-59-SC | Tampering | npm/pip installs | accept | No hay nuevas dependencias en este plan — audit N/A |
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test --run` — suite completa del paquete verde
- `pnpm -w typecheck` — sin errores TypeScript
- `grep -c "upsertAutores" packages/tramitacion/src/ingest-run.ts` devuelve >= 1
- `grep -c "AutorParaEscribir" packages/tramitacion/src/writer.ts` devuelve >= 1
- `grep -c "create table proyecto_autor" supabase/migrations/0051_proyecto_autor.sql` devuelve 1
</verification>

<success_criteria>
- Parser fix: parsear XML de moción con `<autor><PARLAMENTARIO>Name</PARLAMENTARIO></autor>` retorna autores no vacíos.
- reconciliarAutores: match determinista → confirmado; sin match → no_confirmado + null id; lista vacía → [].
- TramitacionWriter interface y ambas implementaciones tienen `upsertAutores`.
- ingest-run.ts tiene `reconciliarAutores` + `upsertAutores` en el pipeline, con FK después de `upsertProyecto`.
- 0051_proyecto_autor.sql y 0051_proyecto_autor.test.sql existen y son correctos.
- Suite y typecheck verde.
</success_criteria>

<output>
Crear `.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-01-SUMMARY.md` cuando termine.
</output>
