---
phase: 59
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - supabase/migrations/0051_proyecto_autor.sql
autonomous: false
requirements:
  - AUTOR-01

must_haves:
  truths:
    - "La tabla proyecto_autor existe en PROD con RLS public-read verificada por pgTAP"
    - "El corpus de boletines (~74-136) tiene filas en proyecto_autor tras la corrida from-r2"
    - "Una segunda corrida produce 0 upserts nuevos (idempotencia verificada en logs)"
    - "La corrida no realiza fetch gubernamental (usa crudo de R2)"
  artifacts:
    - path: supabase/migrations/0051_proyecto_autor.sql
      provides: aplicado a PROD (fila en schema_migrations)
      contains: "create table proyecto_autor"
  key_links:
    - from: run-tramitacion-prod-cli (--from-r2)
      to: proyecto_autor en PROD
      via: ingest-run.upsertAutores
      pattern: "SELECT count"
---

<objective>
Aplicar la migración 0051 a PROD con psql --single-transaction + pgTAP verify, luego ejecutar la corrida de backfill desde R2 para poblar proyecto_autor, y verificar idempotencia.

Purpose: AUTOR-01 — poblar autores en PROD. El código de Plan 01 es completo; este plan materializa los datos reales.
Output: tabla proyecto_autor en PROD con datos reales; corrida idempotente verificada; conteos en SUMMARY.
</objective>

<execution_context>
@C:\Users\Carlo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Carlo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-CONTEXT.md
@.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar migración 0051 a PROD + pgTAP verify</name>
  <files>supabase/migrations/0051_proyecto_autor.sql</files>
  <action>
    Aplicar la migración a PROD siguiendo el patrón LOCKED (NUNCA db push):

    1. Verificar que la migración no está ya aplicada:
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT EXISTS(SELECT 1 FROM schema_migrations WHERE version='0051');"
       ```
       Si retorna `t`, la migración ya fue aplicada — saltar al paso 4 (pgTAP verify).

    2. Verificar encoding antes de aplicar (GOTCHA de MEMORY.md: PGCLIENTENCODING UTF8):
       ```
       PGCLIENTENCODING=UTF8 psql $SUPABASE_DB_URL --single-transaction -f supabase/migrations/0051_proyecto_autor.sql
       ```
       Si el comando falla, reportar el error completo y detener — NO reintentar sin diagnóstico.

    3. Registrar en schema_migrations:
       ```
       psql $SUPABASE_DB_URL -c "INSERT INTO schema_migrations(version) VALUES('0051') ON CONFLICT DO NOTHING;"
       ```

    4. Ejecutar pgTAP contra PROD:
       ```
       PGCLIENTENCODING=UTF8 psql $SUPABASE_DB_URL -tA -f supabase/tests/0051_proyecto_autor.test.sql
       ```
       Verificar que la salida contiene `ok 1..6` (6 tests, todos ok). Si algún test falla → blocker honesto, NO continuar con Task 2.

    5. Verificar RLS desde anon (smoke test):
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT count(*) FROM proyecto_autor;"
       ```
       Debe retornar 0 (tabla vacía antes de backfill). Si retorna error de permisos → blocker.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql $SUPABASE_DB_URL -tA -f supabase/tests/0051_proyecto_autor.test.sql | grep -c "^ok"</automated>
  </verify>
  <done>pgTAP retorna 6 líneas "ok". tabla proyecto_autor existe en PROD. SELECT count(*) FROM proyecto_autor retorna 0 sin error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migración 0051 aplicada a PROD. tabla proyecto_autor existe con RLS public-read verificada por pgTAP (6/6 tests ok).</what-built>
  <how-to-verify>
    1. Confirmar que la salida de Task 1 muestra pgTAP 6/6 ok sin errores.
    2. Opcionalmente verificar en el dashboard de Supabase que la tabla `proyecto_autor` aparece en el schema público.
    3. Si hay algún test fallido en pgTAP, reportar el mensaje exacto antes de continuar.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para continuar con el backfill, o describe el error para hacer rollback.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Corrida backfill from-r2 + verificación idempotencia</name>
  <files></files>
  <action>
    Ejecutar el backfill de autores desde el crudo en R2 sin tocar las fuentes gubernamentales.

    1. Primera corrida (backfill):
       ```
       node packages/tramitacion/src/run-tramitacion-prod-cli.ts --from-r2
       ```
       Observar el log de fin de corrida: debe mostrar el conteo de autores upsertados (`nAutores`), boletines procesados, y cualquier error de fetch (esperado: 0 fetches gubernamentales).

    2. Verificar que hay filas en PROD:
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT count(*) FROM proyecto_autor;"
       ```
       Debe ser > 0.

    3. Verificar breakdown por estado_vinculo:
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT estado_vinculo, count(*) FROM proyecto_autor GROUP BY estado_vinculo ORDER BY estado_vinculo;"
       ```
       Anotar los conteos en el SUMMARY (confirmados vs no_confirmados).

    4. Segunda corrida (idempotencia):
       ```
       node packages/tramitacion/src/run-tramitacion-prod-cli.ts --from-r2
       ```
       El log de fin de corrida debe mostrar `nAutores: 0` (o el mismo conteo total pero 0 nuevos upserts). Si los logs no muestran diferencia, verificar con:
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT count(*) FROM proyecto_autor;"
       ```
       El conteo debe ser IDÉNTICO al del paso 2.

    5. Verificar % confirmados y mociones vs mensajes:
       ```
       psql $SUPABASE_DB_URL -tA -c "SELECT p.iniciativa, count(pa.id) FROM proyecto p LEFT JOIN proyecto_autor pa ON pa.boletin = p.boletin GROUP BY p.iniciativa ORDER BY p.iniciativa;"
       ```
       Anotar en el SUMMARY: boletines tipo Mensaje sin filas en proyecto_autor (correcto), mociones con autores.

    Anotar en el SUMMARY:
    - Total autores ingestados
    - % confirmados (parlamentario_id not null)
    - % no_confirmados
    - Conteo de boletines tipo Moción con al menos 1 autor
    - Boletines tipo Mensaje (0 autores, correcto)
    - Segunda corrida: 0 nuevos upserts (sí/no)
  </action>
  <verify>
    <automated>psql $SUPABASE_DB_URL -tA -c "SELECT count(*) FROM proyecto_autor;" | grep -v "^0$"</automated>
  </verify>
  <done>proyecto_autor tiene filas en PROD. Segunda corrida no añade filas. Log de corrida contiene conteo de autores. Breakdown confirmados/no_confirmados anotado en SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → psql → PROD | psql --single-transaction; rollback automático si cualquier statement falla |
| R2 → ingest-run | crudo inmutable; no hay fetch gubernamental en --from-r2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Elevation of Privilege | proyecto_autor RLS en PROD | mitigate | pgTAP 6/6 ok antes de continuar al backfill; checkpoint blocking |
| T-59-03 | Information Disclosure | corrida rebota a fuentes externas | mitigate | --from-r2 usa solo R2; putImmutable {existed=true} short-circuits Etapa 1; verificar en logs que fetch count = 0 |
| T-59-SC | Tampering | npm installs | accept | Sin nuevas dependencias |
</threat_model>

<verification>
- pgTAP PROD: 6/6 ok
- `SELECT count(*) FROM proyecto_autor` > 0
- Segunda corrida: nAutores = 0 en log
- Ningún error de fetch gubernamental en log de corrida from-r2
</verification>

<success_criteria>
- tabla proyecto_autor existe en PROD con pgTAP 6/6 passing.
- Backfill from-r2 poblò filas: total > 0, breakdown confirmados/no_confirmados documentado.
- Segunda corrida: 0 nuevos upserts (idempotencia verificada).
- Ningún fetch gubernamental durante el backfill (crudo R2 es suficiente para el corpus).
</success_criteria>

<output>
Crear `.planning/phases/59-autor-autor-a-ingest-ficha-de-proyecto-f48-desbloqueada/59-02-SUMMARY.md` cuando termine. Incluir conteos: autores totales, % confirmados, mociones con autores, Mensajes sin autores (correcto).
</output>
