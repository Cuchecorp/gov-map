---
phase: "56"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md
autonomous: true
requirements:
  - CRON-01

must_haves:
  truths:
    - "9/9 workflows tienen veredicto cerrado (VERDE | CORRE-CON-GAPS | NO-CORRE | NO-APLICA-CRON)"
    - "Gap-list tiene ≥20 entradas con archivo:línea y severidad"
    - "Estado de billing GH documentado con evidencia de corridas reales"
    - "Sección 'Cómo re-verificar' con comandos reproducibles para los 9 workflows"
    - "Ningún valor de secret impreso en el documento"
    - "Frescura observada por fuente registrada como baseline para Phase 58"
    - "Los dos casos de assumptions A1 (probidad 0 resultados) y A2 (backup startup_failure) resueltos o documentados como irresolubles"
  artifacts:
    - path: ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      provides: "Documento de auditoría completo con tabla 9 filas, secciones por workflow, gap-list G1-G23+"
      contains: "Veredicto:"
  key_links:
    - from: "56-CRON-AUDIT.md#gap-list"
      to: "Phase 57 CRON-FIX"
      via: "Gap IDs G1-G23 son el backlog directo de hardening"
      pattern: "G[0-9]+.*severidad.*(CRITICAL|HIGH|MEDIUM|LOW)"
    - from: "56-CRON-AUDIT.md#frescura"
      to: "Phase 58 CRON-FRESH"
      via: "Baseline de freshness por fuente"
      pattern: "max\\(.*_at\\).*\\d{4}-\\d{2}-\\d{2}"
---

<objective>
Producir `56-CRON-AUDIT.md`: el diagnóstico completo del estado de la ingesta programada para los 9 workflows de GitHub Actions. El documento es la fuente de verdad que alimenta Phase 57 (hardening) y Phase 58 (freshness dashboard).

Purpose: Saber exactamente qué corre, qué no corre y por qué, con precisión archivo:línea suficiente para que Phase 57 no necesite re-investigar nada.

Output: `56-CRON-AUDIT.md` con tabla resumen 9 filas, sección por workflow (cadena fuente→R2→Supabase + cumplimiento DOS ETAPAS/hash-check), gap-list numerada G1-G23+ con severidad + fix propuesto, sección "Cómo re-verificar" por workflow, y baseline de frescura por fuente.

Restricciones absolutas (de CONTEXT.md, D-locked):
- PROHIBIDO disparar ingestas reales, tocar fuentes gubernamentales, workflow_dispatch de crons de datos, escribir a R2/Supabase.
- Solo probes read-only: gh run list, gh secret list, R2 list (local .env), psql SELECT.
- Jamás imprimir valores de secrets.
</objective>

<execution_context>
@C:/Users/Carlo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CONTEXT.md
@.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-RESEARCH.md
@.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-VALIDATION.md

<interfaces>
<!-- Convenciones LOCKED del proyecto (CLAUDE.md "Ingesta y Cron") que el audit mide -->
<!-- Ingesta en DOS ETAPAS siempre separadas:                                         -->
<!--   Etapa 1 — fuente → R2 crudo (PUT If-None-Match, content-addressed)             -->
<!--   Etapa 2 — R2 crudo → Supabase (nunca volver a la fuente)                      -->
<!-- Hash-check ANTES de descargar (sha256/ETag/If-None-Match)                        -->
<!-- Rate-limit 2-3s/host, UA identificatorio, robots.txt                             -->
<!-- Backfill masivo = LOCAL; cron = novedades L-V acotadas                          -->

<!-- Veredictos cerrados (vocabulario exacto para el documento):                       -->
<!-- VERDE            — corre sin gaps materiales                                     -->
<!-- CORRE-CON-GAPS   — corre pero viola alguna convención LOCKED                     -->
<!-- NO-CORRE         — falla con causa raíz identificada                             -->
<!-- NO-APLICA-CRON   — trigger manual por diseño; auditar igual                      -->

<!-- Estructura del documento (D-locked en CONTEXT.md):                               -->
<!-- (a) Tabla resumen 9 filas                                                        -->
<!-- (b) Sección por workflow: cadena fuente→R2→Supabase + DOS ETAPAS/hash-check     -->
<!-- (c) Gap-list numerada G1, G2, ... con severidad + archivo:línea + fix propuesto -->
<!-- (d) Sección "Cómo re-verificar" con comandos reproducibles por workflow         -->
<!-- (e) Baseline de frescura por fuente (para Phase 58)                             -->

<!-- RESEARCH.md ya contiene los findings principales. El plan no re-investiga:      -->
<!-- re-ejecuta los probes en vivo para refresh + resuelve A1/A2 antes de sintetizar -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ejecutar probes read-only en vivo y resolver A1/A2</name>
  <files>.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md (no existe aún — se crea en Task 2; este task produce SOLO notas en contexto)</files>

  <read_first>
    - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-RESEARCH.md — base de los findings; los probes de este task refrescan/confirman los datos ya en el research.
    - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CONTEXT.md — restricciones (prohibiciones, vocabulario de veredictos).
    - CLAUDE.md sección "Ingesta y Cron" — convenciones LOCKED que se auditan.
  </read_first>

  <action>
Ejecutar los siguientes probes en orden. Registrar los resultados en contexto (no en archivo aún — Task 2 los sintetiza en el documento). Nunca imprimir valores de secrets.

**Probe P1 — Run history refresh (gh, por workflow):**
```
gh run list --repo Cuchecorp/gov-map --limit 40 --json workflowName,conclusion,createdAt,status,event
```
Para cada workflow con historial relevante o incierto, obtener también el log de fallo más reciente:
```
gh run list --repo Cuchecorp/gov-map --workflow agenda-weekly.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow leyes-weekly.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow lobby-camara-weekly.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow lobby-leylobby-weekly.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow probidad-weekly.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow backup-parlamentario.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow fichas-backfill.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow backfill.yml --limit 5
gh run list --repo Cuchecorp/gov-map --workflow deploy-cloudflare.yml --limit 5
```
Comparar con los veredictos de RESEARCH.md. Si un veredicto cambió (p.ej., leyes-weekly ahora corre verde), actualizar el veredicto antes de sintetizar.

**Probe P2 — Secrets inventory refresh:**
```
gh secret list --repo Cuchecorp/gov-map
```
Confirmar que solo `SUPABASE_API_URL` y `SUPABASE_SECRET_KEY` están presentes (o documentar cualquier cambio).
IMPORTANTE: solo registrar nombres de secrets y timestamps, NUNCA valores.

**Probe P3 — Fallo backup-parlamentario 2026-07-08 (resolve A2):**
Obtener el run ID del startup_failure del 2026-07-08 desde la salida de P1, luego:
```
gh run view <RUN_ID> --repo Cuchecorp/gov-map --log-failed 2>&1 | head -50
```
Si el log está disponible, registrar la causa raíz exacta (¿error de YAML syntax? ¿fallo de step?).
Si `startup_failure` no tiene log, registrar "log no disponible, causa: startup_failure (YAML validation error ante push trigger)" como hallazgo.

**Probe P4 — Probidad root cause (resolve A1):**
Leer la tabla `probidad_ingesta_estado` en Supabase PROD para la última corrida (read-only):
```
PGCLIENTENCODING=UTF8 psql "$(powershell -Command "(Get-Content .env | Where-Object { $_ -match '^SUPABASE_DB_URL' }) -replace '^SUPABASE_DB_URL=','' -replace '^\xEF\xBB\xBF','' -replace '\r$',''")" \
  -c "SELECT parlamentario_id, estado, error_msg, updated_at FROM probidad_ingesta_estado ORDER BY updated_at DESC LIMIT 20;"
```
Si el error_msg da contexto sobre el fallo (SPARQL 503, 0 rows, identity mismatch), registrar. Si la tabla está vacía, confirmar que ningún parlamentario fue procesado en la última corrida.

También verificar `source_snapshot` para probidad:
```
PGCLIENTENCODING=UTF8 psql "<DB_URL>" \
  -c "SELECT source, date_bucket, record_count, created_at FROM source_snapshot WHERE source ILIKE '%probidad%' ORDER BY created_at DESC LIMIT 5;"
```

**Probe P5 — Freshness baseline por fuente (para Phase 58):**
Ejecutar todas estas queries read-only (separadas para evitar errores en tablas que puedan no existir):
```sql
SELECT 'citacion' AS tabla, max(created_at) AS ultima FROM citacion;
SELECT 'lobby_audiencia' AS tabla, max(created_at) AS ultima FROM lobby_audiencia;
SELECT 'declaracion' AS tabla, max(updated_at) AS ultima FROM declaracion;
SELECT 'proyecto' AS tabla, max(updated_at) AS ultima FROM proyecto;
SELECT 'source_snapshot' AS tabla, max(date_bucket) AS ultima FROM source_snapshot;
SELECT source, max(date_bucket) AS ultima FROM source_snapshot GROUP BY source ORDER BY ultima DESC;
SELECT 'lobby_ingesta_estado' AS tabla, max(ultima_ingesta) AS ultima FROM lobby_ingesta_estado;
SELECT 'probidad_ingesta_estado' AS tabla, max(ultima_ingesta) AS ultima FROM probidad_ingesta_estado;
```
Registrar resultados con fecha exacta por tabla.

**Probe P6 — R2 freshness (local .env, read-only, sin escribir):**
En PowerShell, cargar las credenciales R2 del .env local y listar los objetos más recientes por prefijo:
```powershell
$env_content = Get-Content .env
$R2_ENDPOINT = ($env_content | Where-Object { $_ -match "^R2_ENDPOINT_URL" }) -replace "^R2_ENDPOINT_URL=",""
$R2_KEY = ($env_content | Where-Object { $_ -match "^R2_ACCESS_KEY_ID" }) -replace "^R2_ACCESS_KEY_ID=",""
$R2_SECRET = ($env_content | Where-Object { $_ -match "^R2_SECRET_ACCESS_KEY" }) -replace "^R2_SECRET_ACCESS_KEY=",""
$R2_BUCKET = ($env_content | Where-Object { $_ -match "^R2_BUCKET" }) -replace "^R2_BUCKET=",""
# Listar via aws CLI (leer R2_BUCKET y R2_ENDPOINT):
aws s3 ls "s3://$R2_BUCKET/" --endpoint-url "$R2_ENDPOINT" --recursive --human-readable 2>&1 | Sort-Object | Select-Object -Last 30
```
Si `aws` CLI no está disponible, usar el cliente S3 de Deno/Node (R2Store class) en un script read-only de una línea. Si R2 no es accesible, documentar "R2 local probe failed — endpoint unreachable" y continuar.
IMPORTANTE: solo mostrar fechas y nombres de objetos, nunca claves de acceso ni secrets.

**Probe P7 — Billing GH (evidencia indirecta):**
La evidencia de que billing NO está bloqueado es el historial de corridas exitosas. Registrar el número de corridas exitosas programadas en los últimos 30 días (de P1). No intentar el endpoint de billing del dashboard (requiere autenticación web).
```
gh api repos/Cuchecorp/gov-map/actions/runs --paginate --jq '.workflow_runs[] | select(.event=="schedule") | {conclusion, created_at}' 2>&1 | head -40
```

Registrar todos los hallazgos de P1-P7 como notas estructuradas en el contexto del agente antes de pasar a Task 2.
  </action>

  <verify>
    <automated>
      # Verificar que los probes se ejecutaron correctamente:
      # (Al final de Task 1, el agente debe haber en contexto:)
      # - Lista de veredictos actualizados por workflow (9 entradas)
      # - Lista de secrets presentes (nombres solo)
      # - Resultado de probidad_ingesta_estado (resolve A1)
      # - Resultado del log del startup_failure de backup-parlamentario (resolve A2)
      # - Timestamps de frescura por tabla Supabase
      # - Estado de R2 (fechas de últimos objetos o "probe failed")
      # No hay archivo que grep verificar en este task — la verificación
      # es sobre los datos en contexto que alimentan Task 2.
      echo "Task 1 completo si el agente tiene en contexto los resultados de P1-P7"
    </automated>
  </verify>

  <acceptance_criteria>
    - Los 9 veredictos están confirmados o actualizados frente a la realidad live (no solo copiados del RESEARCH.md sin re-verificar).
    - A1 (probidad root cause) está resuelto: o se obtuvo error_msg de `probidad_ingesta_estado`, o se documentó como "tabla vacía — 0 parlamentarios procesados" o "irresolvable sin log".
    - A2 (backup startup_failure 2026-07-08) está resuelto: o se obtuvo causa exacta del log, o se documentó como "log no disponible: startup_failure generado por push event, no por schedule; siguiente corrida scheduled verde".
    - Baseline de frescura (max timestamps) disponible para las 8 tablas objetivo.
    - Ningún valor de secret fue impreso en ningún output.
  </acceptance_criteria>

  <done>El agente tiene en contexto todos los datos frescos de los 9 workflows, frescura Supabase, estado R2, y los dos assumptions A1/A2 resueltos o documentados. Listo para Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Sintetizar y escribir 56-CRON-AUDIT.md</name>
  <files>.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md</files>

  <read_first>
    - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-RESEARCH.md — fuente de todos los hallazgos (cadena fuente→R2→Supabase, file:line, gap-list G1-G23, rate-limit/robots compliance).
    - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CONTEXT.md — estructura LOCKED del documento y criterios de cumplimiento.
    - .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-VALIDATION.md — comandos grep de validación que el documento debe satisfacer.
    - Resultados de los probes P1-P7 del Task 1 (en contexto del agente).
    - CLAUDE.md sección "Ingesta y Cron" — convenciones LOCKED (texto exacto para citar en el audit).
  </read_first>

  <action>
Crear el archivo `56-CRON-AUDIT.md` en el directorio de la fase con la siguiente estructura EXACTA (las secciones son obligatorias en este orden):

---

**Sección 1: Encabezado + metadata**
```
# Auditoría E2E de Ingesta — Phase 56 CRON-AUDIT
Fecha: 2026-07-08
Repo remoto: Cuchecorp/gov-map
Auditor: Claude (Sonnet 4.6) + probes live gh CLI / psql / R2
Fase siguiente: Phase 57 (CRON-FIX) — la gap-list es su backlog directo.
```

**Sección 2: Tabla resumen 9 filas**

Cabecera obligatoria:
`| # | Workflow | Schedule | Trigger | Secrets requeridos | Secrets presentes | Última corrida real | Veredicto |`

Rellenar con los datos de RESEARCH.md actualizados con los resultados de P1/P2 de Task 1.
- Usar vocabulario EXACTO para Veredicto: `VERDE`, `CORRE-CON-GAPS`, `NO-CORRE`, `NO-APLICA-CRON`.
- "Secrets presentes" = N/M (ej: 2/7) — nunca listar valores, solo nombre y presencia.
- "Última corrida real" = fecha ISO de la última corrida (success o failure), fuente gh run list.

**Sección 3: Secciones por workflow (9 secciones)**

Para cada workflow, usar este esquema EXACTO:

```markdown
### W-N: <nombre-workflow>

**YAML:** `.github/workflows/<nombre>.yml`
**Schedule:** `<cron expr>` (<descripción legible>) | Manual only
**Veredicto:** <VERDE|CORRE-CON-GAPS|NO-CORRE|NO-APLICA-CRON>
**Causa raíz del veredicto:** <una oración precisa>

#### Cadena de ingesta
| Etapa | Implementada | Estado | Archivo:Línea |
|-------|-------------|--------|---------------|
| Etapa-1 fuente→R2 | Sí/No/Parcial | Activa/No-op (secrets ausentes)/Ausente | `<archivo>:<línea>` o — |
| Etapa-2 desde R2 | Sí/No | Activa/No-implementada | `<archivo>:<línea>` o — |
| Hash-check pre-descarga | Sí/No/Parcial | <descripción> | `<archivo>:<línea>` o — |

#### DOS ETAPAS compliance
- **Etapa-1 (fuente→R2):** [cumple/no cumple/parcial] — [evidencia archivo:línea]
- **Etapa-2 (R2→Supabase, re-ingest sin tocar fuente):** [cumple/no cumple] — [evidencia]
- **Hash-check:** [cumple/no cumple/parcial] — [evidencia]
- **Rate-limit 2-3s:** [cumple/no cumple] — [evidencia archivo:línea]
- **UA identificatorio:** [cumple/no cumple] — [evidencia]
- **robots.txt:** [cumple/no cumple] — [evidencia]

#### Gaps de este workflow
(lista de los gaps G-N que aplican a este workflow, con referencia a la sección de Gap-List)

#### Cómo re-verificar
```bash
# Corrida más reciente:
gh run list --repo Cuchecorp/gov-map --workflow <nombre>.yml --limit 5
# Fallo más reciente (si aplica):
gh run view <LAST_RUN_ID> --repo Cuchecorp/gov-map --log-failed | head -30
# Probe de código (sin ejecutar ingesta):
grep -n "putImmutable\|R2Store\|cache.hasToday" packages/<pkg>/src/run-*-cli.ts
```
```

**IMPORTANTE:** Los comandos en "Cómo re-verificar" son literales, ejecutables por el operador sin modificación (o con un solo `<placeholder>` documentado). Deben incluir el repo flag `--repo Cuchecorp/gov-map`.

**Sección 4: Gap-list consolidada**

Cabecera obligatoria:
`| # | Gap | Severidad | Workflow | Archivo:Línea | Fix propuesto (Phase 57) |`

Incorporar los 23 gaps de RESEARCH.md (G1-G23). Si Task 1 descubrió gaps adicionales, numerarlos continuando desde G24.
- Severidad: `CRITICAL` | `HIGH` | `MEDIUM` | `LOW`
- Archivo:Línea: ruta relativa al repo + número de línea exacto (del RESEARCH.md)
- Fix propuesto: descripción concreta de una oración (qué hacer en Phase 57)
- Ordenar por Severidad (CRITICAL primero, luego HIGH, MEDIUM, LOW)

**Sección 5: DOS ETAPAS — Mapa de cumplimiento sistémico**

Tabla global de cumplimiento de los 5 conectores activos:
`| Connector | Etapa-1 | Etapa-2 desde R2 | Hash-check | Rate-limit | UA | robots |`

Nota sobre el hallazgo sistémico: "Ningún conector implementa la ruta R2→Supabase de re-ingesta (Etapa-2). Re-ingestar a Supabase requeriría re-tocar la fuente gubernamental, violando la convención LOCKED. Gap sistémico: G23."

**Sección 6: Observabilidad — Estado de tablas**

Tabla de las 5 tablas de observabilidad:
`| Tabla | Definida en | Escrita por | Estado actual | Última entrada (probe) |`

Rellenar con los resultados de P4 y P5 de Task 1.

**Sección 7: Frescura baseline por fuente (Phase 58 seed)**

```markdown
## Frescura baseline (2026-07-08)

| Fuente / Tabla | Última fecha observada | Probe |
|----------------|----------------------|-------|
| citacion | <fecha de P5> | `SELECT max(created_at) FROM citacion` |
| lobby_audiencia | <fecha de P5> | `SELECT max(created_at) FROM lobby_audiencia` |
| declaracion | <fecha de P5> | `SELECT max(updated_at) FROM declaracion` |
| proyecto | <fecha de P5> | `SELECT max(updated_at) FROM proyecto` |
| source_snapshot (último) | <fecha de P5> | `SELECT max(date_bucket) FROM source_snapshot` |
| lobby_ingesta_estado | <fecha de P5> | `SELECT max(ultima_ingesta) FROM lobby_ingesta_estado` |
| probidad_ingesta_estado | <fecha de P5> | `SELECT max(ultima_ingesta) FROM probidad_ingesta_estado` |
| R2 (último objeto) | <fecha de P6 o "probe failed"> | `aws s3 ls s3://<bucket>/ --endpoint-url <endpoint> --recursive --human-readable` |
```

**Sección 8: Estado de billing GH**

```markdown
## Estado de billing GitHub Actions

Evidencia: <N> corridas programadas (`event: schedule`) ejecutadas en los últimos 30 días.
Conclusión: Billing NO bloqueado al 2026-07-08. (El gotcha 2026-06-23 estaba resuelto.)
Evidencia: gh run list output — ver corridas exitosas en agenda-weekly (2026-07-06), backup-parlamentario (2026-07-06), lobby-leylobby (2026-07-08).
```

**Sección 9: Assumptions resueltas**

```markdown
## Assumptions resueltas

| ID | Claim original | Resultado | Evidencia |
|----|---------------|-----------|-----------|
| A1 | probidad 2026-07-02: 0 resultados SPARQL vs 0 identity matches | <hallazgo de P4> | `probidad_ingesta_estado` / log |
| A2 | backup-parlamentario 2026-07-08: startup_failure = YAML push-trigger | <hallazgo de P3> | `gh run view <ID> --log-failed` |
| A3 | SUPABASE_URL en fichas-backfill = secret distinto de SUPABASE_API_URL | Confirmar con P2 | `gh secret list` — SUPABASE_URL ausente |
```

**Sección 10: Cómo reproducir esta auditoría**

```markdown
## Cómo reproducir esta auditoría

```bash
# 1. Run history (todos los workflows):
gh run list --repo Cuchecorp/gov-map --limit 40

# 2. Secrets inventory (nombres only, sin valores):
gh secret list --repo Cuchecorp/gov-map

# 3. Código — R2 write presence:
grep -rn "putImmutable\|R2Store" packages/ --include="*.ts"

# 4. Código — R2 read-back absence (Etapa-2 sistémica):
grep -rn "GetObject\|getObject\|readFromR2\|from_r2" packages/ --include="*.ts"

# 5. Supabase frescura (PGCLIENTENCODING=UTF8 requerido en Windows):
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -c "SELECT max(created_at) FROM citacion;"

# 6. Grep de validación del documento:
grep -cE "Veredicto: (VERDE|CORRE-CON-GAPS|NO-CORRE|NO-APLICA-CRON)" 56-CRON-AUDIT.md
# → debe ser 9
grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+" 56-CRON-AUDIT.md
# → debe ser ≥ 20
grep -iE "(sk-|AKIA|secret.*=|Bearer )" 56-CRON-AUDIT.md
# → debe ser 0 (sin valores de secrets)
```
```

---

**Restricciones de escritura:**
- No imprimir en ningún punto del documento valores de secrets (API keys, passwords, tokens). Solo nombres y presencia (Sí/No).
- Los comandos `Cómo re-verificar` deben usar `--repo Cuchecorp/gov-map` explícito.
- Todos los gap file:line deben ser rutas relativas al repo (no absolutas Windows).
- El veredicto en la tabla resumen y en la sección del workflow deben coincidir exactamente.
  </action>

  <verify>
    <automated>
      # Validación 1: 9 veredictos con vocabulario cerrado
      # (Ejecutar desde el directorio de la fase o con ruta completa)
      grep -cE "Veredicto: (VERDE|CORRE-CON-GAPS|NO-CORRE|NO-APLICA-CRON)" ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      # → debe ser exactamente 9

      # Validación 2: gap-list con archivo:línea (≥20 entradas)
      grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+" ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      # → debe ser ≥ 20

      # Validación 3: cero valores de secrets impresos
      grep -iE "(sk-|AKIA|secret.*=|Bearer )" ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      # → debe retornar 0 matches (exit code 1 = OK)

      # Validación 4: sección "Cómo re-verificar" presente (≥9 apariciones, una por workflow)
      grep -c "Cómo re-verificar" ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      # → debe ser ≥ 9

      # Validación 5: sección de frescura presente
      grep -c "Frescura baseline" ".planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md"
      # → debe ser ≥ 1

      # Validación 6: typecheck no roto (fase no toca código)
      # cd al root del repo y:
      pnpm -w typecheck
      # → debe pasar sin errores
    </automated>
  </verify>

  <acceptance_criteria>
    - `56-CRON-AUDIT.md` existe en el directorio de la fase.
    - `grep -cE "Veredicto: (VERDE|CORRE-CON-GAPS|NO-CORRE|NO-APLICA-CRON)" 56-CRON-AUDIT.md` retorna exactamente 9.
    - `grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+" 56-CRON-AUDIT.md` retorna ≥ 20.
    - `grep -iE "(sk-|AKIA|secret.*=|Bearer )" 56-CRON-AUDIT.md` retorna 0 matches.
    - Sección "Cómo re-verificar" presente para los 9 workflows (grep-c ≥ 9).
    - Sección "Frescura baseline" presente con datos de P5 de Task 1.
    - Secciones "Assumptions resueltas" y "Estado de billing GitHub Actions" presentes.
    - `pnpm -w typecheck` pasa (sin cambios de código).
    - Los dos veredictos CORRE-CON-GAPS (agenda-weekly, lobby-leylobby-weekly, backup-parlamentario) y los tres NO-CORRE (leyes-weekly, lobby-camara-weekly, probidad-weekly) y los tres NO-APLICA-CRON (fichas-backfill, backfill, deploy-cloudflare) están presentes con causa raíz.
  </acceptance_criteria>

  <done>
    `56-CRON-AUDIT.md` completo y commiteable. Los 9 workflows tienen veredicto cerrado, la gap-list tiene ≥20 entradas con archivo:línea, el documento no contiene valores de secrets, y Phase 57 puede empezar directamente desde el gap-list sin re-investigar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → gh CLI | Comandos read-only contra repo remoto; solo lee nombres de secrets, nunca valores |
| executor → psql (Supabase PROD) | SELECT read-only; PGCLIENTENCODING=UTF8 requerido; BOM-strip del SUPABASE_DB_URL |
| executor → R2 (local .env) | Listado de objetos read-only; credenciales R2 solo en .env local, nunca en el documento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | 56-CRON-AUDIT.md | mitigate | Instrucción explícita en Task 2 action: nunca imprimir valores de secrets; grep de validación `grep -iE "(sk-\|AKIA\|secret.*=\|Bearer )"` en verify devuelve 0 matches |
| T-56-02 | Tampering | gh run / workflow_dispatch | mitigate | CONTEXT.md D-locked: PROHIBIDO disparar ingestas; solo gh run list / gh secret list (read); no `gh workflow run` en ningún comando del plan |
| T-56-03 | Information Disclosure | psql Supabase PROD | accept | Solo SELECTs read-only sobre tablas de observabilidad (max timestamps, counts); no PII de parlamentarios, no credenciales; risk: bajo |
</threat_model>

<verification>
Verificación de la fase completa:

```bash
# 1. El documento existe
ls .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md

# 2. 9 veredictos cerrados
grep -cE "Veredicto: (VERDE|CORRE-CON-GAPS|NO-CORRE|NO-APLICA-CRON)" \
  .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md
# → 9

# 3. ≥20 gaps con archivo:línea
grep -cE "G[0-9]+.*\.(ts|yml|sql):[0-9]+" \
  .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md
# → ≥20

# 4. Cero valores de secrets
grep -icE "(sk-|AKIA|secret.*=|Bearer )" \
  .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md
# → 0

# 5. Cómo re-verificar presente para los 9 workflows
grep -c "Cómo re-verificar" \
  .planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-CRON-AUDIT.md
# → ≥9

# 6. Suite no rota (fase doc-only, zero código cambiado)
pnpm -w typecheck
```
</verification>

<success_criteria>
- `56-CRON-AUDIT.md` creado y commiteable en el directorio de la fase.
- 9/9 workflows con veredicto en vocabulario cerrado.
- Gap-list G1-G23 (mínimo) con severidad, archivo:línea y fix propuesto ordenados por severidad.
- Billing GH documentado con evidencia de corridas reales (no asumido).
- Los tres assumptions A1, A2, A3 resueltos o documentados como irresolubles con justificación.
- Frescura baseline por fuente (8 tablas) registrada con fecha ISO.
- Sección "Cómo re-verificar" con comandos literales ejecutables para los 9 workflows.
- Cero valores de secrets en el documento (validado por grep).
- `pnpm -w typecheck` verde (la fase no toca código).
</success_criteria>

<output>
Crear `.planning/phases/56-cron-audit-auditor-a-e2e-de-los-9-workflows-de-ingesta/56-01-SUMMARY.md` al completar.
</output>
