---
phase: 76-bento-base-primitivas-bento-chrome-global
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/global-header.tsx
  - app/components/global-header.test.ts
  - app/app/layout.tsx
  - app/app/layout.test.tsx
autonomous: true
requirements: [BENTO-01]

must_haves:
  truths:
    - "GlobalHeader es sticky (top-0, z sobre contenido) con contenedor max-w-[1120px]"
    - "La nav sigue intacta: 5 ítems, Red gated por netPublicEnabled (fail-closed, sin cambio)"
    - "El footer no tiene bg-muted/40, tiene border-top, contenedor max-w-[1120px]"
    - "El contenido CC BY LOCKED del footer es byte-idéntico (copy no deriva)"
    - "Ninguna página cambia de layout interno: {children} NO se envuelve en un <main> global (cada page trae el suyo)"
    - "/red NO recibe cambio de contenedor en 76 (su main max-w-3xl propio queda intacto); verificación visual propia es 79/81"
  artifacts:
    - path: "app/components/global-header.tsx"
      provides: "header sticky + contenedor 1120px (nav/gate intactos)"
      contains: "sticky top-0"
    - path: "app/app/layout.tsx"
      provides: "footer border-top sin fondo, contenedor 1120px, CC BY byte-idéntico"
      contains: "max-w-[1120px]"
    - path: "app/components/global-header.test.ts"
      provides: "asserts sticky/top-0/1120px (extendido)"
    - path: "app/app/layout.test.tsx"
      provides: "asserts footer sin bg-muted/40, 1120px, strings LOCKED, no-<main>-global (extendido)"
  key_links:
    - from: "app/components/global-header.tsx"
      to: "lib/net-gate.ts (netPublicEnabled)"
      via: "showRed server-side read (INTACTO)"
      pattern: "netPublicEnabled\\(process\\.env\\)"
    - from: "app/app/layout.tsx"
      to: "children de cada página"
      via: "sin <main> global (cada page trae el suyo)"
      pattern: "\\{children\\}"
---

<objective>
Aplicar el chrome global del mockup: `GlobalHeader` sticky con contenedor 1120px y footer border-top sin fondo con contenedor 1120px — cambios de clase quirúrgicos que NO tocan copy LOCKED, gate nav ni layout interno de ninguna página.

Purpose: el único diff visual de la Fase 76 (ancho de contenedor header/footer + sticky). Nada más cambia; las primitivas del plan 01 quedan inertes.
Output: 2 archivos modificados (2 líneas de clase en header, clases del footer) + 2 tests source-scan extendidos (SC3/SC4/SC5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-RESEARCH.md
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md
@.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-UI-SPEC.md

<interfaces>
<!-- Líneas EXACTAS a cambiar (VERIFIED contra el código actual). -->

app/components/global-header.tsx línea 30 (INTACTA — preservar el gate):
```typescript
const showRed = netPublicEnabled(process.env);
```
app/components/global-header.tsx línea 33 (<header> — AÑADIR sticky top-0 z-40):
```typescript
<header className="border-b border-border bg-background">
```
app/components/global-header.tsx línea 34 (contenedor — max-w-5xl → max-w-[1120px], padding px-6 py-3):
```typescript
<div className="mx-auto flex min-h-14 max-w-5xl flex-wrap items-center justify-between gap-x-4 gap-y-2 px-4 py-2 md:px-8">
```
app/components/global-header.tsx línea 43 (INTACTA — nav gated):
```typescript
<HeaderNav showRed={showRed} />
```

app/app/layout.tsx líneas 49-50 (footer — quitar bg-muted/40, max-w-3xl → max-w-[1120px]):
```typescript
<footer className="mt-16 border-t bg-muted/40">
  <div className="max-w-3xl mx-auto px-4 md:px-8 py-8 text-sm text-muted-foreground">
```
app/app/layout.tsx línea 41 ({children} — NO envolver en <main>; cada page trae el suyo):
```typescript
{children}
```
Todo desde `<p className="leading-relaxed">` (línea 51) hasta `</footer>` (línea 93): byte-idéntico (copy LOCKED).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GlobalHeader sticky + contenedor 1120px (2 líneas) + extender test</name>
  <files>app/components/global-header.tsx, app/components/global-header.test.ts</files>
  <read_first>
    - app/components/global-header.tsx (archivo completo, 47 líneas — solo 2 clases cambian)
    - app/components/global-header.test.ts (source-scan a extender; NO reescribir Tests 1-4)
    - app/components/header-nav.test.tsx (gate showRed ya cubierto — INTACTO, referencia)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md (§global-header.tsx)
  </read_first>
  <action>
    En `app/components/global-header.tsx` cambiar EXACTAMENTE 2 strings de clase, nada más:
    1. Línea 33 `<header>`: `"border-b border-border bg-background"` → `"sticky top-0 z-40 border-b border-border bg-background"` (sticky sobre contenido; RESEARCH §GlobalHeader).
    2. Línea 34 contenedor `<div>`: `max-w-5xl` → `max-w-[1120px]`; padding `px-4 py-2 md:px-8` → `px-6 py-3` (mapea al mockup `padding:12px 24px`); mantener `min-h-14` y el resto de clases (`mx-auto flex flex-wrap items-center justify-between gap-x-4 gap-y-2`).

    NO tocar nada más: `netPublicEnabled(process.env)` (línea 30), `<HeaderNav showRed={showRed} />` (línea 43), BrandIcon, el Link home, ni el texto `gov-map`. El split Server Component + island cliente (HeaderNav usa usePathname) se preserva verbatim (invariante 5, gate nav — invariante 3 fail-closed).

    Extender `app/components/global-header.test.ts` AÑADIENDO asserts al SRC ya cargado (los Tests 1-4 existentes quedan verdes sin cambio): `expect(SRC).toMatch(/sticky/)`, `expect(SRC).toMatch(/top-0/)`, `expect(SRC).toContain("max-w-[1120px]")`. Usar el idiom existente del archivo (process.cwd() + stripComments — ya presente).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run global-header header-nav</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test -- --run global-header header-nav` verde (Tests 1-4 + nuevos sticky/1120px + gate nav)
    - global-header.tsx contiene `sticky top-0 z-40` y `max-w-[1120px]`
    - global-header.tsx NO contiene ya `max-w-5xl`
    - Línea `netPublicEnabled(process.env)` y `<HeaderNav showRed={showRed} />` intactas (grep)
    - `pnpm --filter ./app exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>Header sticky con contenedor 1120px; nav/gate intactos; test source-scan extendido verde.</done>
</task>

<task type="auto">
  <name>Task 2: Footer border-top sin fondo + contenedor 1120px (copy LOCKED) + extender test</name>
  <files>app/app/layout.tsx, app/app/layout.test.tsx</files>
  <read_first>
    - app/app/layout.tsx (archivo completo — solo el <footer>/<div> interno cambia; copy LOCKED)
    - app/app/layout.test.tsx (source-scan Tests 1-5 a extender; NO reescribir)
    - .planning/phases/76-bento-base-primitivas-bento-chrome-global/76-PATTERNS.md (§layout.tsx, Pitfall 2/4)
  </read_first>
  <action>
    En `app/app/layout.tsx` cambiar SOLO 2 strings de clase del footer:
    1. Línea 49 `<footer className="mt-16 border-t bg-muted/40">` → `<footer className="mt-16 border-t">` (quitar `bg-muted/40`; mantener `mt-16` — fijar esta discreción per RESEARCH Discretion note; mantener `border-t`).
    2. Línea 50 `<div className="max-w-3xl mx-auto px-4 md:px-8 py-8 text-sm text-muted-foreground">` → cambiar `max-w-3xl` → `max-w-[1120px]` (resto de clases intactas).

    TODO desde `<p className="leading-relaxed">` (línea 51) hasta `</footer>` (línea 93) es byte-idéntico — copy LOCKED (Pitfall 2: no alterar ni una palabra del bloque CC BY, licencia, trust line, nav links). NO envolver `{children}` (línea 41) en un `<main>` (Pitfall 4: cada página ya trae su propio `<main>`; un `<main>` global duplicaría el landmark y rompería a11y). El contenedor global de `main` per-page es Phase 79, NO aquí. `/red` NO recibe cambio de contenedor en 76 (su `<main max-w-3xl>` propio queda intacto) — documentar en el SUMMARY que el ajuste consciente de `/red` es 79/81 (invariante 4).

    Extender `app/app/layout.test.tsx` AÑADIENDO asserts al `LAYOUT_SRC` ya cargado (Tests 1-5 existentes verdes sin cambio):
    - `expect(LAYOUT_SRC).not.toMatch(/bg-muted\/40/)` (fondo quitado, SC4)
    - `expect(LAYOUT_SRC).toContain("max-w-[1120px]")` (contenedor, SC4)
    - `expect(LAYOUT_SRC).not.toMatch(/<main/)` (no-<main>-global, SC5)
    - blindar byte-identidad de las strings LOCKED completas: assert del párrafo de licencia ("atribución a Observatorio del Congreso 360. Esta licencia cubre la compilación propia") y de la trust line ("Fuente, fecha y enlace en cada dato · Sin afirmar intención ni causalidad."). Usar `toContain` con los substrings LOCKED completos.
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- --run layout</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test -- --run layout` verde (Tests 1-5 + nuevos SC4/SC5)
    - layout.tsx footer NO contiene `bg-muted/40` ni `max-w-3xl`; contiene `border-t` y `max-w-[1120px]`
    - layout.tsx NO contiene `<main` (grep)
    - Strings LOCKED (CC BY 4.0, licencia, trust line, links /metodologia + /sobre) byte-idénticas (git diff no muestra cambio de copy)
    - `pnpm --filter ./app exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>Footer border-top sin fondo, contenedor 1120px, copy LOCKED byte-idéntico, sin <main> global; test extendido verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client (nav Red gated) | El gate `netPublicEnabled(process.env)` se lee server-side y baja como boolean no-sensible. Esta fase lo PRESERVA verbatim, no lo cruza. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Elevation of Privilege | gate nav Red (`netPublicEnabled`) | mitigate | `header-nav.test.tsx` (gate ON/OFF) intacto + el plan preserva la línea `showRed = netPublicEnabled(process.env)` sin tocar; fail-closed conservado. |
| T-76-04 | Tampering | copy CC BY LOCKED del footer | mitigate | `layout.test.tsx` asevera strings LOCKED completas (licencia + trust line); git diff no debe mostrar cambio de copy. |
| T-76-05 | Information Disclosure | gate PUBLIC_INDEXABLE (noindex) | accept | `generateMetadata`/`PUBLIC_INDEXABLE` no se tocan; Test 5 existente lo verifica. Sin nuevo flujo de datos. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | CERO instalaciones esta fase. Sin superficie de slopsquatting. |

Nota: fase 100% presentación — sin fetch cliente, sin lectura de env cruda (el chokepoint `netPublicEnabled` se preserva), sin nuevos flujos de datos.
</threat_model>

<verification>
- `pnpm --filter ./app test -- --run global-header header-nav layout` verde (SC3, SC4, SC5).
- `pnpm --filter ./app exec tsc --noEmit` limpio.
- Suite app completa verde al merge de wave (`pnpm --filter ./app test -- --run`, target 820+).
- git diff confirma: solo clases de header/footer cambian; copy LOCKED intacto; gate nav intacto; sin `<main>` global.
</verification>

<success_criteria>
- `GlobalHeader` sticky top-0 z-40 + contenedor `max-w-[1120px]`; nav 5 ítems, Red gated intacta.
- Footer sin `bg-muted/40`, border-top, contenedor `max-w-[1120px]`, CC BY LOCKED byte-idéntico.
- `{children}` sin `<main>` global; ninguna página cambia layout interno.
- `/red` sin cambio de contenedor en 76 (documentado; verificación propia 79/81).
</success_criteria>

<output>
Create `.planning/phases/76-bento-base-primitivas-bento-chrome-global/76-02-SUMMARY.md` when done.
</output>
