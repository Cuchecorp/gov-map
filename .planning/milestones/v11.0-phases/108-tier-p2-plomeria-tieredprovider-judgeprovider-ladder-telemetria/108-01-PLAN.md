---
phase: 108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/llm/src/types.ts
  - packages/llm/src/telemetry.ts
  - packages/llm/src/task-ladder.ts
  - packages/llm/src/test-mock.ts
  - packages/llm/src/telemetry.test.ts
  - packages/llm/src/index.ts
autonomous: true
requirements: [TIER-02, TIER-04, TIER-05]
must_haves:
  truths:
    - "CompletionRequest acepta un campo opcional task; su ausencia deja el tipo byte-idéntico al de hoy"
    - "Existe un contrato TelemetrySink + TelemetryEvent que NO declara ningún campo de texto (user/system/answer/prompt/reason)"
    - "noopSink no escribe nada; jsonlSink escribe una línea JSON por evento a un target inyectado"
    - "task-ladder.ts expone LadderConfig/TaskLadder y buildTieredProvider como fábrica declarativa"
    - "Existe un MockProvider local en @obs/llm (sin importar de @obs/llm-bench) para los tests de la escalera"
    - "El barrel re-exporta telemetry y task-ladder sin colisión de nombres"
  artifacts:
    - path: "packages/llm/src/telemetry.ts"
      provides: "TelemetrySink, TelemetryEvent, noopSink, jsonlSink"
      exports: ["TelemetrySink", "TelemetryEvent", "noopSink", "jsonlSink"]
    - path: "packages/llm/src/task-ladder.ts"
      provides: "LadderConfig, TaskLadder, TierSpec, buildTieredProvider"
      exports: ["LadderConfig", "TaskLadder", "buildTieredProvider"]
    - path: "packages/llm/src/test-mock.ts"
      provides: "MockProvider local (LLMProvider) + MockJudgeProvider (JudgeProvider) para tests offline"
      exports: ["MockProvider", "MockJudgeProvider"]
    - path: "packages/llm/src/types.ts"
      provides: "CompletionRequest.task aditivo"
      contains: "task?: string"
  key_links:
    - from: "packages/llm/src/index.ts"
      to: "./telemetry, ./task-ladder"
      via: "export *"
      pattern: "export \\* from \"\\./(telemetry|task-ladder)\""
    - from: "packages/llm/src/task-ladder.ts"
      to: "packages/llm/src/tiered.ts"
      via: "import type TieredProvider / TieredProviderOptions (implementado en Plan 02)"
      pattern: "from \"\\./tiered\""
---

<objective>
Crear los CONTRATOS y andamiaje de la capa escalonada de `@obs/llm`: el campo aditivo `CompletionRequest.task`, el contrato de telemetría payload-free (`telemetry.ts`), la config declarativa tarea→escalera (`task-ladder.ts`), un `MockProvider` local para tests offline, y el cableado del barrel. NO implementa la lógica de cascada (eso es Plan 02) — define las superficies contra las que Plan 02 compila.

Purpose: Interface-first. Plan 02 (cascada `TieredProvider`) recibe estos contratos ya definidos en el plan, sin explorar el codebase.
Output: `telemetry.ts`, `task-ladder.ts`, `test-mock.ts`, `telemetry.test.ts` (net-new); edición aditiva a `types.ts` e `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-CONTEXT.md
@.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-RESEARCH.md

<interfaces>
<!-- Contratos existentes que este plan CONSUME. Extraídos del codebase (research §"Exact Interfaces"). -->
<!-- Usar estos directamente — NO explorar el codebase. -->

Desde packages/llm/src/types.ts:26-69 (LLMProvider + CompletionRequest actual):
```typescript
export type Criticality = "critical" | "bulk";
export type Sensitivity = "public" | "personal";
export interface CompletionRequest {
  system?: string;
  user: string;
  criticality: Criticality;
  sensitivity: Sensitivity;
  maxRepairAttempts?: number;
  temperature?: number;
  onValidationOutcome?: (o: ValidationOutcome) => void;   // ValidationOutcome viene de ./validate
}
export interface LLMProvider {
  readonly id: string;
  readonly trainsOnInputs: boolean;
  complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T>;
}
```

Desde packages/llm/src/validate.ts:35-58:
```typescript
export type ValidationOutcome =
  | { kind: "clean" }
  | { kind: "zod-repaired"; attempts: number }
  | { kind: "structured-output-fail" }
  | { kind: "zod-terminal"; issues: ZodIssue[] };
export class LLMValidationError extends Error { readonly issues: ZodIssue[]; }
```

Desde packages/llm/src/judge.ts:30-70:
```typescript
export interface JudgeRequest { answer: string; system?: string; sensitivity?: Sensitivity; temperature?: number; context?: string; }
export const VerdictSchema = z.object({ ok: z.boolean(), reason: z.string().optional(), confidence: z.number().min(0).max(1).optional() });
export type Verdict = z.infer<typeof VerdictSchema>;
export interface JudgeProvider { readonly id: string; readonly trainsOnInputs: boolean; judge(req: JudgeRequest): Promise<Verdict>; }
```

Barrel actual (packages/llm/src/index.ts): re-exporta types, config, validate, router, json-schema, data-routing, judge, y los 5 providers. SIN nombres `TelemetrySink`/`TelemetryEvent`/`noopSink`/`jsonlSink`/`LadderConfig`/`TaskLadder`/`buildTieredProvider`/`TierSpec` (grep confirmó cero colisión).

MockProvider de referencia (packages/llm-bench/src/mock-provider.ts): NO importable aquí — @obs/llm-bench depende de @obs/llm (dependencia circular prohibida). Clonar un mock mínimo local (decisión LOCKED del CONTEXT: "si el import cruza límites de paquete, clonar un mock mínimo dentro de @obs/llm").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo aditivo CompletionRequest.task + contrato de telemetría payload-free</name>
  <files>packages/llm/src/types.ts, packages/llm/src/telemetry.ts</files>
  <read_first>
    - packages/llm/src/types.ts (CompletionRequest completo — punto de la edición aditiva)
    - packages/llm/src/validate.ts (ValidationOutcome — lo referencia TelemetryEvent)
    - packages/llm/src/judge.ts (Verdict — TelemetryEvent hace Pick<Verdict,"ok"|"confidence">)
  </read_first>
  <action>
    (1) En `packages/llm/src/types.ts`, agregar a `CompletionRequest` DESPUÉS de `temperature?` un campo `task?: string` con JSDoc que declare: ADITIVO y OPCIONAL, la ausencia reproduce el comportamiento actual byte-por-byte; lo consume SOLO `TieredProvider` para seleccionar la escalera; los adapters concretos (DeepSeek/MiniMax/Granite) lo ignoran por completo (per TIER-02, D "task aditivo"). Es el ÚNICO cambio a un archivo fuente existente en esta fase — no tocar ningún otro campo ni la interfaz `LLMProvider`.
    (2) Crear `packages/llm/src/telemetry.ts` exportando: `TelemetryEvent` (interface con SOLO campos estructurales/numéricos: `providerId: string`, `task?: string`, `latencyMs: number`, `costUsd: number | null`, `validationOutcome: ValidationOutcome | null` importado de `./validate`, `judgeVerdict?: Pick<Verdict,"ok"|"confidence">` importado de `./judge`, `escalated: boolean`, `ts: string`); `TelemetrySink = (event: TelemetryEvent) => void`; `noopSink: TelemetrySink` (cuerpo vacío); `jsonlSink(target: { write: (line: string) => void }): TelemetrySink` (factory que serializa cada evento con `JSON.stringify` + `"\n"` y lo pasa a `target.write`). PROHIBIDO DURO: `TelemetryEvent` NO declara `user`/`system`/`answer`/`prompt`/`reason` — el `reason` del Verdict es texto de opinión del juez que puede filtrar contenido de tarea; solo `ok`/`confidence` (per TIER-04, threat "PII in telemetry logs"). Importar `ValidationOutcome`/`Verdict` de sus módulos fuente; NO re-exportarlos desde `telemetry.ts` (Pitfall 3, colisión de barrel).
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec tsc -b</automated>
  </verify>
  <done>
    `types.ts` tiene `task?: string` en `CompletionRequest`; `telemetry.ts` exporta los 4 símbolos; tsc -b exit 0; grep confirma que `TelemetryEvent` no menciona user/system/answer/prompt/reason.
  </done>
</task>

<task type="auto">
  <name>Task 2: Config declarativa task-ladder + MockProvider local + telemetry.test.ts + barrel</name>
  <files>packages/llm/src/task-ladder.ts, packages/llm/src/test-mock.ts, packages/llm/src/telemetry.test.ts, packages/llm/src/index.ts</files>
  <read_first>
    - packages/llm/src/index.ts (barrel — orden de re-exports, cero colisión)
    - packages/llm-bench/src/mock-provider.ts (patrón de referencia del MockProvider a clonar; NO importar de ahí)
    - packages/llm/src/telemetry.ts (recién creado — TelemetrySink/TelemetryEvent que consumen ladder y tests)
  </read_first>
  <action>
    (1) Crear `packages/llm/src/task-ladder.ts` exportando: `TierSpec { provider: LLMProvider; costPerToken?: number }`; `LadderConfig { primary: TierSpec; escalation?: TierSpec; judge?: JudgeProvider; judgeContext?: string; maxBudgetUsd?: number }`; `type TaskLadder = Record<string, LadderConfig>`; y `buildTieredProvider(config: LadderConfig, telemetrySink?: TelemetrySink): TieredProvider`. `buildTieredProvider` traduce `LadderConfig` a `TieredProviderOptions` (tiers = [primary.provider, ...(escalation ? [escalation.provider] : [])], judge, judgeContext, maxBudgetUsd, telemetrySink) y devuelve `new TieredProvider(...)`. Importar `TieredProvider`/`TieredProviderOptions` de `./tiered` con `import type` para la firma y `import { TieredProvider }` para la construcción (per D "task-ladder desacopla config de cascada"). `./tiered` lo escribe Plan 02 — la referencia es forward y compila cuando Plan 02 exista (esta plan corre en Wave 1, así que `./tiered` NO existe todavía: dejar el import y aceptar que `tsc -b` de ESTE plan reporta el módulo faltante — la verificación de tsc de task-ladder es responsabilidad del merge de Wave 2). Para que Wave 1 cierre verde por sí solo, ver la nota de verificación abajo.
    (2) Crear `packages/llm/src/test-mock.ts` (helper de test compartido, no un `.test.ts`): `MockProvider implements LLMProvider` clonado mínimo del de llm-bench — `id`/`trainsOnInputs` configurables por constructor (default `"mock"`/`false`), `callCount` público, `complete<T>` que invoca un `responder(req, schema)` inyectado, emite opcionalmente latencia/tokens sintéticos a un sink, y valida con `schema.parse`; el responder puede lanzar `LLMValidationError` para simular zod-fail. Además `MockJudgeProvider implements JudgeProvider` con veredicto fijo inyectado (`{ ok, reason?, confidence? }`) y `callCount` público. NO importar nada de `@obs/llm-bench` ni `@obs/adjudication` (Pitfall 5 + dependencia circular).
    (3) Crear `packages/llm/src/telemetry.test.ts`: probar `noopSink` (no lanza, no escribe) y `jsonlSink` (target de captura recibe una línea JSON parseable por evento; el JSON round-trip preserva `providerId`/`task`/`latencyMs`/`escalated`). Test de disciplina payload-free: construir un `TelemetryEvent` de ejemplo y asertar por `Object.keys` que NO contiene `user`/`system`/`answer`/`prompt`/`reason` (per TIER-04).
    (4) Editar `packages/llm/src/index.ts`: agregar al final `export * from "./telemetry";` y `export * from "./task-ladder";` (el `export * from "./tiered";` lo agrega Plan 02 — este plan NO lo agrega para no romper el barrel con un módulo inexistente en Wave 1). Confirmar cero colisión de nombres.
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec vitest run src/telemetry.test.ts</automated>
  </verify>
  <done>
    `task-ladder.ts`, `test-mock.ts`, `telemetry.test.ts` creados; barrel re-exporta telemetry + task-ladder; `telemetry.test.ts` verde; `MockProvider`/`MockJudgeProvider` locales sin import de otros paquetes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dominio → prompt LLM | user/system/context cruzan a un provider — RUT prohibido (guard vive en Plan 02, decorador) |
| evento → log de telemetría | el `TelemetryEvent` cruza a un sink/archivo — NUNCA texto de prompt/respuesta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Information Disclosure | `TelemetryEvent` shape | mitigate | La interfaz NO declara campos de texto (user/system/answer/prompt/reason); solo estructural/numérico + `ok`/`confidence`. Test por `Object.keys`. |
| T-108-02 | Information Disclosure | `CompletionRequest.task` leído por adapters | mitigate | `task` lo consume SOLO `TieredProvider`; JSDoc lo declara ignorado por adapters concretos; ningún adapter lo lee (verificable por grep en Plan 02). |
| T-108-SC | Tampering | npm installs | accept | Fase code-only, CERO paquete nuevo (research §Package Legitimacy: no install). |
</threat_model>

<verification>
- `pnpm --filter @obs/llm exec vitest run src/telemetry.test.ts` verde.
- Baseline `@obs/llm` (102 pass / 3 skip) no regresiona en la suite completa.
- `task-ladder.ts` compila cuando `./tiered` exista (merge de Wave 2).
</verification>

<success_criteria>
- `CompletionRequest.task` aditivo presente; tipo byte-idéntico sin `task`.
- `telemetry.ts` con contrato payload-free y `noopSink`/`jsonlSink`.
- `task-ladder.ts` con `LadderConfig`/`TaskLadder`/`buildTieredProvider`.
- `MockProvider`/`MockJudgeProvider` locales, sin cruce de paquete.
- Barrel re-exporta telemetry + task-ladder sin colisión.
</success_criteria>

<output>
Create `.planning/phases/108-tier-p2-plomeria-tieredprovider-judgeprovider-ladder-telemetria/108-01-SUMMARY.md` when done
</output>
