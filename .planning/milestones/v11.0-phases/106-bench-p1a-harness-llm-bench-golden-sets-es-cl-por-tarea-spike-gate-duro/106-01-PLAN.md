---
phase: 106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/llm-bench/package.json
  - packages/llm-bench/tsconfig.json
  - packages/llm-bench/vitest.config.ts
  - packages/llm-bench/src/index.ts
  - packages/llm-bench/src/metrics.ts
  - packages/llm-bench/src/metrics.test.ts
  - packages/llm-bench/src/pricing.ts
  - packages/llm-bench/src/instrument.ts
  - packages/llm-bench/src/instrument.test.ts
  - packages/llm-bench/src/report.ts
  - tsconfig.json
autonomous: true
requirements: [BENCH-01]
must_haves:
  truths:
    - "El paquete @obs/llm-bench existe FUERA de @obs/llm, depende de @obs/llm y nunca al revés"
    - "percentile (nearest-rank sin interpolación), costo/1k desde usage, y la separación de fallos zod/structured-output son funciones puras testeadas"
    - "structured_output_fail_rate y zod_fail_rate.{repaired,terminal} son campos SEPARADOS de primera clase en el tipo Reporte"
    - "instrumentedFetch captura latencia + usage.prompt_tokens/completion_tokens de la respuesta interceptada del provider real"
    - "El tipo Reporte puede expresar 'nada aprueba paridad' (ningún campo fuerza una aprobación) y versiona modelo+endpoint+tarifaFecha"
    - "El barrel src/index.ts se AUTORIZA una sola vez aquí (owner único), pre-declarando los re-exports de guards y los cuatro scorers que llenan las olas 2 — Wave 2 nunca lo edita"
  artifacts:
    - path: "packages/llm-bench/package.json"
      provides: "@obs/llm-bench workspace package, dep @obs/llm + dev vitest/zod"
      contains: "@obs/llm-bench"
    - path: "packages/llm-bench/src/index.ts"
      provides: "barrel único y completo: núcleo (metrics/pricing/instrument/report) + guards + los cuatro scorers como re-exports adelantados; Wave 2 solo llena los módulos"
      contains: "guards"
    - path: "packages/llm-bench/src/metrics.ts"
      provides: "percentile nearest-rank, costo, clasificación de outcome zod/structured-output"
      min_lines: 40
    - path: "packages/llm-bench/src/instrument.ts"
      provides: "instrumentedFetch fetch-wrapper que muestrea latencia + usage"
      min_lines: 20
    - path: "packages/llm-bench/src/report.ts"
      provides: "MetricasModelo + Reporte con métricas separadas de primera clase"
      contains: "structured_output_fail_rate"
    - path: "packages/llm-bench/src/pricing.ts"
      provides: "tabla de tarifas versionada con fecha (MEDIUM-confidence, re-verify 107)"
      contains: "fecha"
  key_links:
    - from: "packages/llm-bench/package.json"
      to: "@obs/llm"
      via: "workspace dependency"
      pattern: "@obs/llm"
    - from: "tsconfig.json"
      to: "packages/llm-bench"
      via: "references entry (NOT paths)"
      pattern: "packages/llm-bench"
---

<objective>
Scaffold the `@obs/llm-bench` package OUTSIDE the runtime lib and build its measurement core:
the pure metric functions (percentile, cost, zod/structured-output failure classification), the
versioned pricing table, the instrumented fetch-wrapper that captures latency + token usage from
the real provider path, and the `Reporte` type that keeps every metric as a SEPARATE first-class
field. This is the foundation Waves 2–3 import.

This plan is also the SINGLE owner of the package barrel `src/index.ts`: it authors the complete
barrel up front (core + guards + the four per-task scorers as forward re-exports) so that the two
parallel Wave-2 plans (106-02, 106-03) fill in only their own modules and NEVER touch the barrel —
keeping Wave 2 free of shared-file writes.

Purpose: Without an instrumented path the harness cannot see cost/latency/zod-fail (the runtime
`complete<T>()` returns only the validated `T`). This plan owns the instrument; it never touches
`@obs/llm` runtime.
Output: A new workspace package that compiles under `tsc -b` (via `references`, NOT `paths`) and
whose metric/instrument unit tests are green under `pnpm --filter @obs/llm-bench test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-RESEARCH.md
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts the harness drives + hooks it instruments. Use these directly — no exploration needed. -->

From packages/llm/src/types.ts — the runtime contract (DO NOT modify @obs/llm):
```typescript
export type Criticality = "critical" | "bulk";
export type Sensitivity = "public" | "personal";
export interface CompletionRequest { system?: string; user: string; criticality: Criticality; sensitivity: Sensitivity; maxRepairAttempts?: number; temperature?: number; }
export interface LLMProvider { readonly id: string; readonly trainsOnInputs: boolean; complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T>; }
```

From packages/llm/src/providers/deepseek.ts:39 and minimax.ts:43 — the instrumentation hook:
```typescript
// Every adapter accepts opts.fetchFn?: typeof fetch (injected into new OpenAI({ fetch: opts.fetchFn }))
export interface DeepSeekProviderOptions { apiKey: string; baseURL?: string; model?: string; fetchFn?: typeof fetch; }
// DeepSeek default baseURL "https://api.deepseek.com", default model "deepseek-v4-flash"
// MiniMax  default baseURL "https://api.minimax.io/v1", default model "MiniMax-M3"
```

From the openai@5 chat completion response body (what instrumentedFetch reads):
`body.usage = { prompt_tokens: number, completion_tokens: number, total_tokens: number }`

From packages/llm/src/validate.ts — the zod-fail outcome semantics to mirror in the classifier:
- structured-output fail = no usable payload on attempt 0 (no forced tool_call / json_object JSON.parse rejects → undefined)
- zod fail (repaired) = parsed but safeParse failed, then a reprompt round-trip produced a valid object (non-terminal)
- zod fail (terminal) = attempts exhausted → LLMValidationError thrown
- clean = parsed + validated on attempt 0
</interfaces>

Mirror EXACTLY for package scaffolding (read these before writing config):
@packages/cruces/package.json
@packages/cruces/tsconfig.json
@packages/cruces/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @obs/llm-bench workspace package + author the complete barrel</name>
  <files>packages/llm-bench/package.json, packages/llm-bench/tsconfig.json, packages/llm-bench/vitest.config.ts, packages/llm-bench/src/index.ts, packages/llm-bench/src/guards/freeze.ts, packages/llm-bench/src/guards/no-rut.ts, packages/llm-bench/src/tasks/routing/scorer.ts, packages/llm-bench/src/tasks/clasificacion/scorer.ts, packages/llm-bench/src/tasks/juez/scorer.ts, packages/llm-bench/src/tasks/extraccion/scorer.ts, tsconfig.json</files>
  <read_first>
    - packages/cruces/package.json (mirror name/type/exports/scripts shape)
    - packages/cruces/tsconfig.json (mirror extends/composite/references — references NOT paths, Phase 43 gotcha)
    - packages/cruces/vitest.config.ts (mirror verbatim)
    - packages/llm/package.json (confirm @obs/llm export barrel and version)
    - pnpm-workspace.yaml (confirm packages/* glob already covers the new package)
    - tsconfig.json (root — the references array to extend)
    - CLAUDE.md (conventions: pnpm workspaces, tsc -b references NOT paths, vitest per package)
  </read_first>
  <action>
    Create `packages/llm-bench/package.json` named `@obs/llm-bench`, private, `type: module`,
    `main`/`types` = `src/index.ts`, `exports` `.` → `./src/index.ts`, scripts `test: "vitest run"`
    and `typecheck: "tsc -b"`. Dependency `@obs/llm: "workspace:*"` and `zod: "^4.4.3"`; devDeps
    `@types/node: "^20.19.43"` and `vitest: "^3.0.0"`. DO NOT add any other dependency — the phase
    installs ZERO external packages (RESEARCH § Package Legitimacy Audit).
    Create `packages/llm-bench/tsconfig.json` extending `../../tsconfig.base.json` with `composite: true`,
    `rootDir: src`, `outDir: dist`, `noEmit: false`, `lib: ["ES2022","DOM"]`, `types: ["node"]`, a
    `references` entry `{ "path": "../llm" }` (NOT `paths`), `include: ["src/**/*.ts","src/**/*.json"]`,
    `exclude: ["src/**/*.test.ts","test","dist"]`.
    Create `packages/llm-bench/vitest.config.ts` verbatim from cruces (node env, include `src/**/*.test.ts`,
    passWithNoTests true).
    Author the COMPLETE, FINAL barrel `packages/llm-bench/src/index.ts` ONCE, here — this plan is the
    single owner of the barrel; Wave-2 plans (106-02/106-03) are forbidden to edit it. The barrel names
    every export the package will expose via forward re-exports: the four core modules built in this plan
    (`export * from "./metrics"; export * from "./pricing"; export * from "./instrument"; export * from "./report";`),
    the shared guards created by 106-02 (`export * from "./guards/freeze"; export * from "./guards/no-rut";`),
    and the four per-task scorers created by 106-02/106-03
    (`export * from "./tasks/routing/scorer"; export * from "./tasks/clasificacion/scorer"; export * from "./tasks/juez/scorer"; export * from "./tasks/extraccion/scorer";`).
    Because those guard/scorer modules do not exist yet in Wave 1, create minimal placeholder module files
    at each of those six paths containing exactly `export {};` (guards/freeze.ts, guards/no-rut.ts, and the
    four tasks/<t>/scorer.ts) so the forward re-exports resolve and `tsc -b` is exit 0 now. Wave-2 plans
    OVERWRITE only these placeholder module bodies with their real implementations — they never touch the
    barrel. Add a header comment in the barrel: "// OWNER: 106-01. Complete barrel. Wave-2 plans fill the
    referenced modules; they must NOT edit this file (WARNING 1 fix)."
    Append `{ "path": "./packages/llm-bench" }` to the root `tsconfig.json` `references` array.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/llm-bench/package.json` declares `@obs/llm-bench` depending on `@obs/llm` (workspace:*); grep confirms no external package beyond zod/vitest/@types/node.
    - `packages/llm-bench/tsconfig.json` uses a `references` entry to `../llm`, never a `paths` map.
    - Root `tsconfig.json` `references` array contains `{ "path": "./packages/llm-bench" }`.
    - `src/index.ts` forward re-exports all four core modules, both guards, and all four task scorers; six placeholder module files (`export {};`) exist so `tsc -b` is exit 0.
    - `@obs/llm` package.json is UNCHANGED (never depends on @obs/llm-bench).
  </acceptance_criteria>
  <done>New package resolves in the workspace, the complete barrel compiles (via placeholders), and `tsc -b` finds it via references with exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Metric core — percentile, cost, and the two SEPARATE failure classifications</name>
  <files>packages/llm-bench/src/metrics.ts, packages/llm-bench/src/metrics.test.ts</files>
  <read_first>
    - packages/llm/src/validate.ts (the repair-loop outcomes: structured-output fail vs zod repaired vs zod terminal vs clean — the classifier must mirror these EXACTLY)
    - 106-RESEARCH.md § Metric Methodology (nearest-rank formula: idx = ceil(p/100*n)-1 clamped; cost = prompt/1e6*rate_in + completion/1e6*rate_out; the outcome table)
    - 106-RESEARCH.md § Code Examples (percentile reference implementation)
  </read_first>
  <behavior>
    - percentile([], 50) → NaN; percentile of 40 sorted samples at p50 → index 19, at p95 → index 37 (nearest-rank, NO interpolation, clamped to [0,n-1]).
    - costoUsd({prompt_tokens, completion_tokens}, {in, out}) → prompt/1e6*in + completion/1e6*out; when usage is undefined → null (never silently 0).
    - clasificarOutcome maps a per-call record to exactly one of: "clean" | "structured-output-fail" | "zod-repaired" | "zod-terminal" (mirrors validate.ts semantics).
    - agregarFallos over a list of outcomes yields { structured_output_fail_rate, zod_fail_rate: { repaired, terminal } } as SEPARATE rates — folding any into the other is a test failure.
  </behavior>
  <action>
    Create `packages/llm-bench/src/metrics.ts` exporting: `percentile(samples: number[], p: number): number`
    (nearest-rank, dependency-free, per RESEARCH Code Examples — clamp idx to `[0, n-1]`, empty → NaN);
    `costoUsd(usage: { prompt_tokens?: number; completion_tokens?: number } | undefined, rate: { in: number; out: number }): number | null`
    (null when usage or either token count is absent — never zero-fill); a `CallOutcome` union type
    `"clean" | "structured-output-fail" | "zod-repaired" | "zod-terminal"`; `clasificarOutcome(rec)` that
    maps a per-call record (fields: `payloadUsableAttempt0: boolean`, `repaired: boolean`, `terminal: boolean`)
    to a `CallOutcome` mirroring `validate.ts` semantics; and `agregarFallos(outcomes: CallOutcome[])` returning
    `{ structured_output_fail_rate: number; zod_fail_rate: { repaired: number; terminal: number } }` with each
    rate = count/total, KEPT SEPARATE (Pitfall B — never fold zod into structured-output or into quality).
    Create `packages/llm-bench/src/metrics.test.ts` covering the four behaviors above, INCLUDING an
    assertion that a model that structures-fails 30% but is "clean on what it managed" reports a high
    structured_output_fail_rate (does NOT look good) — the anti-Pitfall-B meta-check.
    (The barrel already re-exports `./metrics` from 106-01 — do NOT edit `src/index.ts`.)
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test metrics 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `percentile` uses nearest-rank with NO interpolation (test asserts p50/p95 land on discrete sorted indices for n=40).
    - `costoUsd` returns `null` (not 0) when `usage` is undefined or a token count is missing.
    - `agregarFallos` returns `structured_output_fail_rate` and `zod_fail_rate.{repaired,terminal}` as three distinct numbers; a test proves they are not conflated.
    - `clasificarOutcome` distinguishes structured-output-fail (attempt-0 no payload) from zod-terminal (attempts exhausted).
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test metrics` green; the separation of the two failure metrics is enforced by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pricing table (versioned+dated), instrumented fetch-wrapper, and the Reporte type</name>
  <files>packages/llm-bench/src/pricing.ts, packages/llm-bench/src/instrument.ts, packages/llm-bench/src/instrument.test.ts, packages/llm-bench/src/report.ts</files>
  <read_first>
    - packages/llm/src/providers/deepseek.ts (fetchFn injection point + default baseURL/model for the endpoint provenance)
    - packages/llm/src/providers/minimax.ts (fetchFn injection point + default baseURL/model)
    - packages/llm/src/types.ts (EmbeddingResult model/dims/version — the provenance discipline pricing mirrors)
    - 106-RESEARCH.md § Cost/1k (versioned rate table shape with fecha; candidate rates are 107 — leave DeepSeek/MiniMax [ASSUMED] with the date)
    - 106-RESEARCH.md § Report type (MetricasModelo + Reporte with metrics SEPARATE; "nada aprueba paridad" expressible)
    - 106-RESEARCH.md § Instrumented call (res.clone().json() reads usage; Assumption A5 — fallback noted)
  </read_first>
  <behavior>
    - instrumentedFetch wraps a base fetch: times the call with performance.now(), clones the response, reads body.usage.{prompt_tokens,completion_tokens}, emits a CallMetric to the sink, returns the ORIGINAL response untouched.
    - When the intercepted body has no usage (host omits it), the emitted CallMetric carries undefined token counts (so cost later resolves to null, never 0).
    - PRICING.fecha is a dated string; tarifas keys include the two incumbents; the Reporte embeds pricingFecha so every cost number is traceable.
    - A Reporte with every model showing its numbers and no model flagged "approved" is a valid, constructible value (nada-aprueba-paridad expressible).
  </behavior>
  <action>
    Create `packages/llm-bench/src/pricing.ts` exporting `PRICING` = `{ fecha: "2026-07-26", tarifas: { "deepseek-v4-flash": { in, out }, "MiniMax-M3": { in, out } } } as const` with per-1M
    in/out rates marked `[ASSUMED] MEDIUM — re-verify in 107` in a comment; do NOT add candidate (Granite/Phi)
    rates here (107 scope). Provide `tarifaDe(modelo: string)` returning the rate or `undefined`.
    Create `packages/llm-bench/src/instrument.ts` exporting `CallMetric` (`{ latencyMs: number; promptTokens?: number; completionTokens?: number }`)
    and `instrumentedFetch(base: typeof fetch, sink: (m: CallMetric) => void): typeof fetch` per RESEARCH Code
    Examples: `t0 = performance.now()`, `await base(...)`, `res.clone().json().catch(()=>undefined)`, emit
    `{ latencyMs, promptTokens: body?.usage?.prompt_tokens, completionTokens: body?.usage?.completion_tokens }`,
    return the original `res`. Include repair round-trips in the wall clock (do NOT strip — Pitfall 7).
    Create `packages/llm-bench/src/report.ts` exporting `TaskId` (`"routing" | "clasificacion" | "juez" | "extraccion"`),
    a per-task `QualityScore` union placeholder type (task plans refine their own shape), `MetricasModelo`
    (`{ modelo; endpoint; tarifaFecha; calidad_por_tarea: Partial<Record<TaskId, unknown>>; latencia_p50_ms; latencia_p95_ms; n_muestras; costo_por_1k: number | null; zod_fail_rate: { repaired: number; terminal: number }; structured_output_fail_rate: number }`)
    and `Reporte` (`{ fecha: string; pricingFecha: string; modelos: MetricasModelo[] }`). Add a `p95Indicativo`
    flag/comment noting small-N uncertainty. NO field may force an approval (nada-aprueba-paridad).
    Create `packages/llm-bench/src/instrument.test.ts` with a fake `base` fetch returning a canned body
    (with and without `usage`) asserting the sink receives correct latency+token values and that a missing
    usage yields undefined token counts.
    (The barrel already re-exports `./pricing`, `./instrument`, and `./report` from 106-01 — do NOT edit `src/index.ts`.)
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test instrument 2>&1 | tail -15 && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `PRICING.fecha` is a dated string and `Reporte.pricingFecha` carries it (grep confirms the field).
    - `instrumentedFetch` returns the original response object (not the clone) and emits latency + usage-derived token counts to the sink.
    - A canned response WITHOUT `usage` produces a CallMetric with undefined token counts (test asserts).
    - `report.ts` declares `structured_output_fail_rate` and `zod_fail_rate: { repaired; terminal }` as separate fields on `MetricasModelo`.
    - `tsc -b` for the package is exit 0.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test instrument` green, `tsc -b` clean; pricing dated, metrics separated in the Reporte type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → harness | `casos.json` (later plans) and any fixture is untrusted-JSON crossing into the harness |
| harness → provider network | the instrumented fetch wraps the real openai@5 call (LIVE plan only); no PII may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | pricing.ts rates drift silently | mitigate | `PRICING.fecha` dated + `[ASSUMED]` comment; re-verified in 107 (LOCKED) |
| T-106-02 | Information Disclosure | instrumentedFetch logging prompt/answer payloads | mitigate | sink receives only latency + token counts + endpoint; never the request/response body text (mirror LLMValidationError issues-only discipline) |
| T-106-03 | Elevation of Scope | new secret added prematurely (Workers AI token) | accept/flag | 106 adds NO secret; if a task needs one it has absorbed 107 scope → STOP and flag |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages (RESEARCH § Package Legitimacy Audit); package.json restricted to zod/vitest/@types/node — no install task, no [ASSUMED]/[SUS] package |
</threat_model>

<verification>
- `pnpm --filter @obs/llm-bench test` green (metrics + instrument unit tests, no network).
- `pnpm --filter @obs/llm-bench exec tsc -b` exit 0 (package compiles via references).
- Root suite not regressed: `pnpm test` still green (new package adds green tests only) — checked at wave merge.
- `@obs/llm` unchanged (git diff shows no runtime-lib edit).
- `src/index.ts` is the complete barrel authored here; Wave-2 plans do not modify it (WARNING 1 fix).
</verification>

<success_criteria>
- `@obs/llm-bench` exists outside `@obs/llm`, depends on it (never the reverse), and is in the root `references` graph.
- The two failure metrics are modeled as separate first-class fields; percentile/cost are pure & tested.
- instrumentedFetch measures the real provider path (latency + usage) without logging payloads.
- The `Reporte` type versions model+endpoint+tarifaFecha and can express "nada aprueba paridad".
- The complete barrel is authored once here (single owner); Wave 2 has zero shared-file writes to `src/index.ts`.
- ZERO new external package installed.
</success_criteria>

<output>
Create `.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-01-SUMMARY.md` when done
</output>
</content>
</invoke>
