---
phase: 106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro
plan: 04
type: execute
wave: 3
depends_on: ["106-01", "106-02", "106-03"]
files_modified:
  - packages/llm-bench/src/harness.ts
  - packages/llm-bench/src/harness.test.ts
  - packages/llm-bench/src/mock-provider.ts
  - packages/llm-bench/src/baseline.live.test.ts
  - packages/llm-bench/README.md
  - packages/llm-bench/src/index.ts
autonomous: true
requirements: [BENCH-01, BENCH-03]
must_haves:
  truths:
    - "El operador puede correr el harness que evalúa un provider sobre las 4 golden sets POR TAREA y produce un Reporte con calidad + latencia p50/p95 + costo/1k + zod/structured-output-fail SEPARADOS"
    - "En CI el harness corre con MockProvider (sin red, determinista) y produce un Reporte válido; los 4 golden sets gatean como piso de regresión mock"
    - "El bloque LIVE instancia DeepSeek/MiniMax reales (keys existentes), registra endpoint+tarifaFecha por modelo, y NUNCA corre en CI (env-gated, espejo cruces/fichas)"
    - "El Reporte puede expresar 'nada aprueba paridad' — ningún campo fuerza aprobación; cada modelo muestra sus números etiquetados con su endpoint"
    - "El harness es host-agnóstico (baseURL-swap): 107 enchufa Granite/Phi sin cambiar el harness"
  artifacts:
    - path: "packages/llm-bench/src/harness.ts"
      provides: "correrTarea/correrHarness que drive un LLMProvider por las 4 tareas → Reporte con métricas separadas"
      min_lines: 60
    - path: "packages/llm-bench/src/mock-provider.ts"
      provides: "MockProvider determinista para CI (sin red), keyed por caso/tarea"
      min_lines: 20
    - path: "packages/llm-bench/src/harness.test.ts"
      provides: "harness corre las 4 tareas con mock y produce Reporte + los 4 gates como piso mock"
      min_lines: 30
    - path: "packages/llm-bench/src/baseline.live.test.ts"
      provides: "baseline LIVE DeepSeek/MiniMax env-gated, NUNCA en CI, con endpoint provenance"
      min_lines: 25
    - path: "packages/llm-bench/README.md"
      provides: "cómo correr el harness (CI mock) y el baseline (LIVE) — runbook operador"
      min_lines: 20
  key_links:
    - from: "packages/llm-bench/src/harness.ts"
      to: "los 4 scorers (routing/clasificacion/juez/extraccion)"
      via: "correrHarness itera las tareas y agrega calidad_por_tarea"
      pattern: "routing|clasificacion|juez|extraccion"
    - from: "packages/llm-bench/src/baseline.live.test.ts"
      to: "DeepSeekProvider/MiniMaxProvider reales"
      via: "instrumentedFetch + env-gate LLM_BENCH_LIVE"
      pattern: "LLM_BENCH_LIVE|DeepSeekProvider|MiniMaxProvider"
---

<objective>
Wire the instrument (Plan 01) and the four scorers (Plans 02–03) into a runnable harness that drives
ANY `LLMProvider` across the four golden tasks and emits a single `Reporte` keeping every metric
SEPARATE. In CI it runs with a deterministic `MockProvider` (no network) and the four golden sets act as
a mock regression floor. A LIVE, env-gated baseline block instruments the REAL DeepSeek/MiniMax (existing
keys) via `instrumentedFetch`, records endpoint+tarifaFecha per model, and never runs in CI — the BENCH-03
baseline artifact 107 measures candidates against.

Purpose: This is the operator-runnable deliverable of BENCH-01/BENCH-03 — the report, not a verdict
(verdict-por-tarea is 107/BENCH-05). It closes the harness so 107 only plugs Granite/Phi by baseURL.
Output: `harness.ts` + `mock-provider.ts` + CI harness test + LIVE baseline test + operator README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-RESEARCH.md
@.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Everything the harness composes. All from Plans 01–03; drive via the runtime contract. -->

From packages/llm-bench/src/report.ts + metrics.ts + instrument.ts + pricing.ts (Plan 01):
```typescript
export type TaskId = "routing" | "clasificacion" | "juez" | "extraccion";
export interface MetricasModelo { modelo; endpoint; tarifaFecha; calidad_por_tarea; latencia_p50_ms; latencia_p95_ms; n_muestras; costo_por_1k; zod_fail_rate: { repaired; terminal }; structured_output_fail_rate; }
export interface Reporte { fecha; pricingFecha; modelos: MetricasModelo[]; }
export function percentile(samples: number[], p: number): number;
export function costoUsd(usage, rate): number | null;
export function agregarFallos(outcomes): { structured_output_fail_rate; zod_fail_rate: { repaired; terminal } };
export type CallMetric = { latencyMs: number; promptTokens?: number; completionTokens?: number };
export function instrumentedFetch(base: typeof fetch, sink: (m: CallMetric) => void): typeof fetch;
export const PRICING: { fecha: string; tarifas: Record<string, { in: number; out: number }> };
export function tarifaDe(modelo: string): { in: number; out: number } | undefined;
```

From the four scorers (Plans 02–03):
```typescript
// routing:      GOLDEN_SET, GOLDEN_SET_GATE, evaluarRouting(set, ejecutar), gatePasa
// clasificacion:GOLDEN_SET, GOLDEN_SET_GATE, evaluarClasificacion(set, ejecutar), gatePasa
// extraccion:   GOLDEN_SET, GOLDEN_SET_GATE, evaluarExtraccion(set, ejecutar), gatePasa
// juez:         GOLDEN_SET_SCORING, evaluarJuez(set, juzgar)  (report-only in 106)
```

From packages/llm/src/providers (LIVE baseline — real adapters, fetchFn injectable):
```typescript
new DeepSeekProvider({ apiKey: process.env.DEEPSEEK_API_KEY!, fetchFn: instrumentedFetch(fetch, sink) }); // endpoint https://api.deepseek.com
new MiniMaxProvider({  apiKey: process.env.MINIMAX_API_KEY!,  fetchFn: instrumentedFetch(fetch, sink) }); // endpoint https://api.minimax.io/v1
```
</interfaces>

CI-mock / LIVE-gated split precedent to mirror EXACTLY:
@packages/cruces/src/golden/golden-set.test.ts   <!-- LIVE = process.env.X === "1"; (LIVE ? describe : describe.skip); it.skipIf(!KEY) -->
@packages/fichas/src/mock-provider.ts   <!-- MockProvider shape: implements LLMProvider, keyed, validates against schema, trainsOnInputs=false -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MockProvider + harness driver → Reporte (CI, no network)</name>
  <files>packages/llm-bench/src/mock-provider.ts, packages/llm-bench/src/harness.ts, packages/llm-bench/src/harness.test.ts, packages/llm-bench/src/index.ts</files>
  <read_first>
    - packages/fichas/src/mock-provider.ts (MockProvider implements LLMProvider; keyed by prompt content; validates against the passed schema; id + trainsOnInputs=false)
    - packages/cruces/src/golden/golden-set.test.ts (how ejecutar wires a provider to a scorer per case)
    - packages/llm-bench/src/report.ts + metrics.ts (Plan 01: MetricasModelo/Reporte + percentile/agregarFallos/costoUsd)
    - the four scorers (Plans 02–03) exports listed in <interfaces>
    - 106-RESEARCH.md § Harness Architecture (drive LLMProvider, host-agnostic; report not auto-merge-verdict) + § Report type (metrics SEPARATE; nada-aprueba-paridad expressible)
  </read_first>
  <behavior>
    - MockProvider implements LLMProvider deterministically (no network), returning a canned answer per case validated against the passed zod schema; id "bench-mock", trainsOnInputs=false.
    - correrHarness(provider, opts) runs the four golden GATE sets, collects per-call CallMetrics (latency + usage) via a sink, computes latencia_p50/p95 (percentile), costo_por_1k (costoUsd + tarifaDe, null if usage absent), agregarFallos for the two separate fail-rates, and assembles one MetricasModelo → Reporte with the model's endpoint + PRICING.fecha.
    - A Reporte where no model is flagged approved is constructible (nada-aprueba-paridad) — a test builds one.
    - Swapping the provider (mock → any other LLMProvider) requires NO harness change (host-agnostic) — a test drives two different mock providers unchanged.
  </behavior>
  <action>
    Create `packages/llm-bench/src/mock-provider.ts` — a deterministic `MockProvider implements LLMProvider`
    (mirror fichas mock-provider: keyed responses or a fixed answer, `complete<T>` validates against the
    passed schema, `id="bench-mock"`, `trainsOnInputs=false`, a `callCount`). It also records synthetic
    CallMetrics so CI can exercise the metric aggregation path without network.
    Create `packages/llm-bench/src/harness.ts` exporting `correrTarea(tarea, provider, sink)` (runs one
    golden GATE set through its scorer, returning that task's quality shape) and `correrHarness(provider,
    { modelo, endpoint }): Promise<MetricasModelo>` that: runs the four tasks, drains the CallMetric sink into
    latencia_p50_ms/latencia_p95_ms (percentile) + n_muestras, computes costo_por_1k (costoUsd × tarifaDe,
    null if usage missing), agregarFallos → structured_output_fail_rate + zod_fail_rate.{repaired,terminal}
    kept SEPARATE, and stamps endpoint + PRICING.fecha. Provide `construirReporte(modelos: MetricasModelo[]): Reporte`.
    Do NOT bake any auto-approval — the report only reports.
    Create `packages/llm-bench/src/harness.test.ts` (CI, no network): correrHarness with MockProvider produces
    a Reporte with all four tasks in calidad_por_tarea and the metrics as separate fields; the four golden
    GATE sets pass with a perfect mock (mock regression floor); a nada-aprueba-paridad Reporte is constructible;
    and the same harness runs unchanged with a second mock provider (host-agnostic). Extend the barrel.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test harness 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `MockProvider` implements `LLMProvider`, validates against the passed schema, never touches network (grep: no fetch/http in mock-provider.ts).
    - `correrHarness` produces a `MetricasModelo` with `structured_output_fail_rate` and `zod_fail_rate.{repaired,terminal}` as separate fields and `costo_por_1k` null when usage is absent.
    - The four golden GATE sets pass under a perfect mock (regression floor); a test asserts each `gatePasa` true.
    - A `Reporte` with no approved model is constructible (nada-aprueba-paridad test green).
    - The harness runs unchanged with two different providers (host-agnostic test green).
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test harness` green; harness produces a separated-metric Reporte over the four tasks with a mock.</done>
</task>

<task type="auto">
  <name>Task 2: LIVE baseline block (real DeepSeek/MiniMax, env-gated, NEVER in CI) + operator README</name>
  <files>packages/llm-bench/src/baseline.live.test.ts, packages/llm-bench/README.md</files>
  <read_first>
    - packages/cruces/src/golden/golden-set.test.ts (LIVE = process.env.X==="1"; (LIVE?describe:describe.skip); it.skipIf(!KEY) — mirror EXACTLY, rename env to LLM_BENCH_LIVE)
    - packages/fichas/src/golden/golden-set.test.ts (FICHAS_GOLDEN_LIVE precedent)
    - packages/llm-bench/src/{harness,instrument,report,pricing}.ts (Plan 01 + Task 1)
    - packages/llm/src/providers/{deepseek,minimax}.ts (real adapters, fetchFn injection for instrumentation; default endpoints for provenance)
    - 106-RESEARCH.md § Baseline Readiness (DeepSeek/MiniMax real; NO new secret; endpoint provenance) + § Validation Architecture (LIVE command)
    - 106-CONTEXT.md (LOCKED: en 106 NO se necesita key nueva; Workers AI/OpenRouter token es 107 — si se necesita, se detiene y se marca)
  </read_first>
  <action>
    Create `packages/llm-bench/src/baseline.live.test.ts` mirroring the cruces/fichas LIVE block EXACTLY:
    `const LIVE = process.env.LLM_BENCH_LIVE === "1"; (LIVE ? describe : describe.skip)(...)` with
    `it.skipIf(!process.env.DEEPSEEK_API_KEY || !process.env.MINIMAX_API_KEY)`. Inside: build a metric sink,
    instantiate `new DeepSeekProvider({ apiKey, fetchFn: instrumentedFetch(fetch, sink) })` (endpoint
    "https://api.deepseek.com") and `new MiniMaxProvider({ apiKey, fetchFn: instrumentedFetch(fetch, sink) })`
    (endpoint "https://api.minimax.io/v1"), run `correrHarness` for each, `console.log` the resulting Reporte
    (JSON + a legible table), and assert each `MetricasModelo` carries `endpoint` + `tarifaFecha`. This block
    is SKIPPED by default → never burns quota in CI. Do NOT assert candidate parity (that is 107). Do NOT add
    any Workers AI / Cloudflare / OpenRouter provider or secret here — if the task appears to need one, STOP
    and flag it as 107 scope-creep in the SUMMARY (per 106-CONTEXT LOCKED).
    Create `packages/llm-bench/README.md` — an operator runbook: (1) CI usage (`pnpm --filter @obs/llm-bench test`
    = mock + guards, no network); (2) LIVE baseline (`LLM_BENCH_LIVE=1 pnpm --filter @obs/llm-bench test baseline`
    with DEEPSEEK_API_KEY/MINIMAX_API_KEY in `.env`); (3) how to read the Reporte (metrics are SEPARATE;
    p95 is small-N indicative; cost carries tarifaFecha; a "nada aprueba paridad" outcome is valid); (4) the
    107 boundary (candidates/adapters/verdict/new-secret are NOT here).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm-bench test 2>&1 | tail -15 && pnpm --filter @obs/llm-bench exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `baseline.live.test.ts` is SKIPPED when `LLM_BENCH_LIVE !== "1"` (default CI run does NOT hit the network — grep confirms the `(LIVE ? describe : describe.skip)` gate + `it.skipIf` on keys).
    - The LIVE block instantiates the REAL DeepSeek/MiniMax adapters with `instrumentedFetch` and records `endpoint` + `tarifaFecha` per model.
    - NO Workers AI / Cloudflare / OpenRouter provider or new secret is referenced anywhere (grep: no CLOUDFLARE_ACCOUNT_ID, no new env var beyond LLM_BENCH_LIVE + existing keys).
    - `README.md` documents CI-mock vs LIVE-baseline usage and the 107 boundary.
    - Full package suite green + `tsc -b` exit 0.
  </acceptance_criteria>
  <done>`pnpm --filter @obs/llm-bench test` green (LIVE skipped); baseline block is real-provider, env-gated, never-in-CI, with endpoint provenance; README is an operator runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → provider network | LIVE baseline crosses to DeepSeek/MiniMax; golden is RUT-free by construction (Plans 02–03) |
| CI runner | must NEVER hit the network — LIVE block env-gated + skipped by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-11 | Denial of Service | LIVE baseline burns provider quota in CI | mitigate | `(LIVE ? describe : describe.skip)` + `it.skipIf(!KEY)` — skipped by default, mirror cruces/fichas |
| T-106-12 | Repudiation | Ollama/local latency reported as a served-host number | mitigate | baseline only measures DeepSeek/MiniMax real endpoints; every MetricasModelo tagged with its endpoint; candidate host measurement is 107 |
| T-106-13 | Elevation of Scope | a new secret (Workers AI/OpenRouter) added to run a candidate | accept/flag | 106 adds NO secret; if a task needs one it has absorbed 107 scope → STOP + flag in SUMMARY (LOCKED) |
| T-106-14 | Information Disclosure | harness logs prompt/answer payloads or API keys | mitigate | sink records only latency+tokens+endpoint; Reporte holds metrics not payloads; adapters never log key/prompt |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new external package |
</threat_model>

<verification>
- `pnpm --filter @obs/llm-bench test` green with LIVE SKIPPED (no network in CI).
- Root suite intact: `pnpm test` green (app 1428 + packages ~1310 + 9 guards) + `tsc -b` exit 0 at phase gate.
- Harness produces a separated-metric Reporte over the four tasks with a mock; the four golden gates pass as the mock floor.
- LIVE baseline block instantiates real DeepSeek/MiniMax with endpoint provenance and is never in CI.
- Operator runs the LIVE baseline once (records the report artifact) — documented in README; the CI side is fully green regardless.
</verification>

<success_criteria>
- Operator can run `packages/llm-bench` and get a per-task, per-model Reporte with quality + latency p50/p95 + cost/1k + zod/structured-output-fail as SEPARATE first-class fields (BENCH-01).
- CI runs mock/no-network green; the four frozen golden sets are the regression floor.
- The baseline runs against the EXACT prod endpoints (DeepSeek/MiniMax real, endpoint+tarifaFecha stamped) — never a different host (BENCH-03).
- The Reporte can express "nada aprueba paridad"; the harness is host-agnostic (107 plugs candidates by baseURL).
- NO new secret; no 107 scope (adapters/candidate-endpoint/verdict) introduced.
</success_criteria>

<output>
Create `.planning/phases/106-bench-p1a-harness-llm-bench-golden-sets-es-cl-por-tarea-spike-gate-duro/106-04-SUMMARY.md` when done
</output>
