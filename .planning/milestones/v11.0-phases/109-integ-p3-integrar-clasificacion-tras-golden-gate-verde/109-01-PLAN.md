---
phase: 109-integ-p3-integrar-clasificacion-tras-golden-gate-verde
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/llm/src/provider-guard.test.ts
  - packages/llm/src/integ-scope-guard.test.ts
autonomous: true
requirements: [INTEG-02]
must_haves:
  truths:
    - "Todo provider LLM en packages/llm/src/providers/ está probado por CI de tener assertNoRutInLlmInput + assertSensitivityAllowed; uno nuevo sin los guards ROMPE CI"
    - "clasificar-lobby-cli.ts (MiniMax/adjudicación) NO puede contener TieredProvider — el guard MUERDE si aparece"
    - "pipeline-cli.ts (extracción strict-schema) sigue sin TieredProvider — TIER-05 verde tras la fase"
  artifacts:
    - path: "packages/llm/src/provider-guard.test.ts"
      provides: "Source-scan que enumera todos los providers y asevera zod+PII wrapper"
      contains: "assertNoRutInLlmInput"
    - path: "packages/llm/src/integ-scope-guard.test.ts"
      provides: "Guard estático que muerde si la escalera se cablea en adjudicación/extracción"
      contains: "clasificar-lobby-cli"
  key_links:
    - from: "packages/llm/src/provider-guard.test.ts"
      to: "packages/llm/src/providers/*.ts"
      via: "readdirSync + readFileSync source-scan"
      pattern: "readdirSync|readFileSync"
    - from: "packages/llm/src/integ-scope-guard.test.ts"
      to: "packages/cruces/src/clasificar-lobby-cli.ts"
      via: "readFileSync + not.toContain(TieredProvider)"
      pattern: "not.toContain"
---

<objective>
PRIMER COMMIT de la fase (patrón lockdown-guard-first v10.0): entregar los DOS guards que muerden ANTES de cualquier swap del CLI. Esta wave NO toca ningún construction point; solo instala la red de seguridad estática.

Purpose: Garantizar que (1) ningún provider LLM pueda existir sin el wrapper zod+PII (RUT jamás cruza a un LLM), y (2) la escalera TieredProvider NUNCA pueda cablearse en la adjudicación de identidad (MiniMax) ni en la extracción strict-schema (DeepSeek prompt-cache). Los guards existen y MUERDEN antes de que la Wave 2 haga el swap, de modo que el swap nace ya vigilado.

Output: `provider-guard.test.ts` (enumera providers, asevera guards) + `integ-scope-guard.test.ts` (source-scan anti-adjudicación/anti-extracción). Ambos offline, sin red, sin keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-CONTEXT.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-RESEARCH.md

<read_first>
- packages/llm/src/tiered-scope-guard.test.ts — molde EXACTO del guard estático a clonar (readFileSync + import.meta.dirname + not.toContain). LEER COMPLETO: es 34 líneas.
- packages/llm/src/providers/granite.ts:36-37,99-102 — confirma que un provider "correcto" llama assertNoRutInLlmInput(req.user) + assertNoRutInLlmInput(req.system) + assertSensitivityAllowed(...). El guard busca estas cadenas.
- app/lib/lockdown-guard.test.ts — precedente del readdirSync recursivo + strip de comentarios + mutation self-check + fail-loud. Referencia de estilo para el enumerador de providers.
</read_first>

<interfaces>
Providers existentes en packages/llm/src/providers/ (confirmado vía ls):
  deepseek.ts, minimax.ts, granite.ts, phi-judge.ts, gemini-embeddings.ts
  (los .test.ts NO son providers — el enumerador debe filtrar *.test.ts).

Cadenas que un provider LLM correcto contiene (verificado en granite.ts):
  - "assertNoRutInLlmInput"   (llamado sobre req.user y req.system)
  - "assertSensitivityAllowed"

EXCLUSIÓN documentada: gemini-embeddings.ts NO maneja datos de usuario con el mismo
patrón LLM (es EmbeddingProvider, no LLMProvider) — el guard debe excluirlo
explícitamente de la aserción zod+PII (RESEARCH §3 "Scope del guard").

Gotcha LOCKED (TIER-05): usar `import.meta.dirname`, NUNCA `new URL(import.meta.url)` — falla en vitest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: provider-guard — enumera providers y asevera wrapper zod+PII</name>
  <files>packages/llm/src/provider-guard.test.ts</files>
  <behavior>
    - Test 1 (positivo): para cada archivo de packages/llm/src/providers/*.ts que NO sea *.test.ts NI gemini-embeddings.ts, el source contiene "assertNoRutInLlmInput" Y "assertSensitivityAllowed". Hoy pasa (deepseek/minimax/granite/phi-judge ya los tienen por construcción 107).
    - Test 2 (mutation self-check en memoria): un source sintético de provider SIN "assertNoRutInLlmInput" es detectado como offender por el mismo predicado — prueba que el guard MUERDE (no que siempre pasa). No escribir archivos al disco; ejercitar el predicado sobre un string en memoria.
    - Test 3 (enumeración no-vacía): la lista de providers escaneados tiene length ≥ 4 (fail-loud si el glob se rompe y devuelve 0 → falso verde).
  </behavior>
  <action>
    Crear el test vitest en packages/llm/src/provider-guard.test.ts. Usar `readdirSync(join(import.meta.dirname, "providers"))` para enumerar; filtrar con `.filter(f => f.endsWith(".ts") && !f.endsWith(".test.ts") && f !== "gemini-embeddings.ts")`. Extraer el predicado offender a una función pura `esProviderSinGuard(source: string): boolean` que retorna true si falta cualquiera de las dos cadenas — esa función es la que ejercita el mutation self-check (Test 2). Aserción principal: para cada provider, `readFileSync(...,"utf-8")` NO es offender. Documentar en comentario de cabecera la EXCLUSIÓN de gemini-embeddings (EmbeddingProvider, no LLMProvider) y por qué el guard es PROSPECTIVO (hoy 0 violaciones; su valor es romper CI ante un provider futuro sin guards). Usar import.meta.dirname (gotcha TIER-05). NO usar new URL(import.meta.url).
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec vitest run src/provider-guard.test.ts</automated>
  </verify>
  <done>Los 3 tests pasan; la lista enumerada tiene ≥4 providers; el mutation self-check demuestra que un provider sin assertNoRutInLlmInput es detectado como offender.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: integ-scope-guard — la escalera NO toca adjudicación/extracción</name>
  <files>packages/llm/src/integ-scope-guard.test.ts</files>
  <behavior>
    - Test 1: packages/cruces/src/clasificar-lobby-cli.ts NO contiene "TieredProvider" (la ruta MiniMax/adjudicación queda intacta). Hoy pasa.
    - Test 2: packages/fichas/src/pipeline-cli.ts NO contiene "TieredProvider" (extracción strict-schema; espeja TIER-05 pero re-asevera desde 109 para que la fase falle si el swap toca fichas por error). Hoy pasa.
    - Test 3 (mutation self-check en memoria): un source sintético que SÍ contiene "TieredProvider" es detectado como offender por el predicado compartido — prueba que el guard MUERDE.
  </behavior>
  <action>
    Crear packages/llm/src/integ-scope-guard.test.ts clonando el idiom de tiered-scope-guard.test.ts (readFileSync + import.meta.dirname + navegar "..","..","..","packages",<pkg>,"src",<file>). Extraer el predicado a `contieneTiered(source: string): boolean => source.includes("TieredProvider")` y ejercitarlo en el mutation self-check. Aserciones: clasificar-lobby-cli.ts y pipeline-cli.ts NO son offenders. Comentario de cabecera: cita RESEARCH §Landmines 1-2 (clasificar-lobby-cli=MiniMax adjudicación; pipeline-cli=extracción vetada es-CL). NO ampliar tiered-scope-guard.test.ts existente — archivo NUEVO paralelo (evita colisión de ownership con 108).
  </action>
  <verify>
    <automated>pnpm --filter @obs/llm exec vitest run src/integ-scope-guard.test.ts src/tiered-scope-guard.test.ts</automated>
  </verify>
  <done>Los 3 tests nuevos pasan; TIER-05 existente sigue verde; el mutation self-check demuestra que un source con TieredProvider es offender.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI de pipeline → provider LLM | Texto de ficha (idea matriz, cuerpo) cruza al LLM; RUT NUNCA debe cruzar |
| Escalera candidata → rutas sensibles | La escalera NUNCA debe cablearse en adjudicación (MiniMax) ni extracción (DeepSeek prompt-cache) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Information Disclosure | provider LLM nuevo sin wrapper zod+PII | mitigate | provider-guard.test.ts enumera providers y falla CI si alguno carece de assertNoRutInLlmInput (Task 1) |
| T-109-02 | Tampering | escalera cableada en clasificar-lobby-cli.ts (adjudicación MiniMax) | mitigate | integ-scope-guard.test.ts asevera not.toContain(TieredProvider) (Task 2, Test 1) |
| T-109-03 | Tampering | escalera cableada en pipeline-cli.ts (extracción vetada es-CL) | mitigate | integ-scope-guard.test.ts re-asevera + TIER-05 existente sigue verde (Task 2, Test 2) |
| T-109-SC | Tampering | npm installs | accept | Cero paquetes nuevos esta fase (RESEARCH §Package Legitimacy Audit N/A); solo tests vitest sobre código propio |
</threat_model>

<verification>
- `pnpm --filter @obs/llm exec vitest run src/provider-guard.test.ts src/integ-scope-guard.test.ts src/tiered-scope-guard.test.ts` → todo verde
- `pnpm --filter @obs/llm exec tsc -b` → exit 0
- Suite completa @obs/llm sin regresión (baseline 144 pass / 3 skip post-108; +6 tests nuevos)
</verification>

<success_criteria>
- provider-guard existe, enumera ≥4 providers, muerde ante uno sin wrapper (mutation self-check)
- integ-scope-guard existe, muerde ante TieredProvider en lobby-cli/pipeline-cli (mutation self-check)
- Cero cambio a construction points (NO se toca clasificar-fichas-cli.ts en esta wave)
- tsc -b exit 0, suite @obs/llm verde
</success_criteria>

<output>
Create `.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-01-SUMMARY.md` when done
</output>
