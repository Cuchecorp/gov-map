---
phase: 109-integ-p3-integrar-clasificacion-tras-golden-gate-verde
plan: "03"
type: execute
wave: 3
depends_on: ["109-02"]
files_modified:
  - packages/cruces/src/shadow-eval.test.ts
  - packages/cruces/src/drift-canary.test.ts
  - .env.example
autonomous: true
requirements: [INTEG-01, INTEG-03]
must_haves:
  truths:
    - "El shadow-eval corre Granite en sombra vs DeepSeek sobre el golden set, compara sector_codigo salida-a-salida y reporta acuerdo/desacuerdo, SIN alterar la salida productiva"
    - "El shadow-eval y el drift canary hacen skip LIMPIO cuando faltan WORKERS_AI_API_TOKEN o CLOUDFLARE_ACCOUNT_ID (nunca corren en CI)"
    - "El drift canary detecta si el modelo servido por Workers AI difiere del pinneado (@cf/ibm-granite/granite-4.0-h-micro) e invalida el veredicto full-40"
  artifacts:
    - path: "packages/cruces/src/shadow-eval.test.ts"
      provides: "Shadow-eval Granite vs DeepSeek, LIVE-gated CLASIFICACION_SHADOW_LIVE"
      contains: "CLASIFICACION_SHADOW_LIVE"
    - path: "packages/cruces/src/drift-canary.test.ts"
      provides: "Probe del endpoint, compara modelo servido vs pinneado, LIVE-gated"
      contains: "granite-4.0-h-micro"
  key_links:
    - from: "packages/cruces/src/shadow-eval.test.ts"
      to: "GraniteProvider + DeepSeekProvider (@obs/llm)"
      via: "clasificarFicha con ambos providers, comparación de sector_codigo"
      pattern: "clasificarFicha"
    - from: "packages/cruces/src/drift-canary.test.ts"
      to: "Workers AI endpoint (model served)"
      via: "probe HTTP + comparación contra pinneado"
      pattern: "granite-4.0-h-micro"
---

<objective>
La observabilidad de la promoción: shadow-eval (candidato en sombra vs incumbente, LIVE-gated) + prueba explícita del rollback por config + canario de drift del endpoint. Es la evidencia que el operador necesitará ANTES de flipear CLASIFICACION_ESCALERA=1.

Purpose: Cumplir el contrato "shadow-evaluation ON antes de promover" (INTEG-01) sin promover: correr Granite en paralelo a DeepSeek sobre la distribución del golden y reportar acuerdo/desacuerdo, sin tocar la salida productiva. Y el canario de drift (INTEG-03): detectar si Workers AI cambió el modelo servido para no confiar en los números del veredicto full-40. Todo LIVE-gated con skip limpio (nunca en CI).

Output: `shadow-eval.test.ts` (LIVE-gated) + `drift-canary.test.ts` (LIVE-gated) + placeholders en `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-CONTEXT.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-RESEARCH.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-02-SUMMARY.md

<read_first>
- packages/cruces/src/golden/golden-set.test.ts:121-147 — el patrón LIVE-gated EXACTO a clonar: `const LIVE = process.env.CRUCES_GOLDEN_LIVE === "1"`; `(LIVE ? describe : describe.skip)(...)`; `it.skipIf(!process.env.KEY)(...)`. Skip limpio sin keys. GOLDEN_SET_GATE + clasificarFicha ya se usan aquí.
- packages/cruces/src/golden/golden-set.ts — GOLDEN_SET_GATE (10 casos), evaluarGolden, inputDeCaso/inputDeFila. Reutilizar la muestra congelada como distribución de shadow.
- packages/cruces/src/clasificar.ts:54 — clasificarFicha(input, provider): Promise<{sector_codigo}>. La función que shadow-eval corre con ambos providers.
- packages/llm/src/providers/granite.ts:46-56 — DEFAULT_BASE_URL con {ACCOUNT_ID}; interpolar CLOUDFLARE_ACCOUNT_ID (Pitfall 1). El body de la completion de Workers AI trae el campo `model` (RESEARCH §7).
- .planning/phases/107-*/107-VEREDICTO-LIVE-FULL-2026-07-27.md — provenance pinneado: modelo @cf/ibm-granite/granite-4.0-h-micro, endpoint .../accounts/{ACCOUNT_ID}/ai/v1, fecha 2026-07-27.
</read_first>

<interfaces>
Patrón LIVE-gate (verbatim del golden-set.test.ts):
  const LIVE = process.env.CLASIFICACION_SHADOW_LIVE === "1";   // shadow
  const LIVE = process.env.CLASIFICACION_DRIFT_CHECK === "1";   // drift
  (LIVE ? describe : describe.skip)("...", () => {
    it.skipIf(!process.env.WORKERS_AI_API_TOKEN || !process.env.CLOUDFLARE_ACCOUNT_ID)("...", async () => {...});
  });

Rate-limit LOCKED: 2-3s entre llamadas al mismo host si se golpea PROD. El shadow corre
sobre el golden congelado (10 casos) — secuencial con delay 2-3s, NO paralelo contra el mismo host.

Modelo pinneado (constante en el test): "@cf/ibm-granite/granite-4.0-h-micro".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadow-eval Granite vs DeepSeek (LIVE-gated) + rollback assert</name>
  <files>packages/cruces/src/shadow-eval.test.ts</files>
  <action>
    Crear packages/cruces/src/shadow-eval.test.ts clonando el LIVE-gate de golden-set.test.ts:121. Gate: CLASIFICACION_SHADOW_LIVE === "1"; it.skipIf(!WORKERS_AI_API_TOKEN || !CLOUDFLARE_ACCOUNT_ID). En el test LIVE: construir DeepSeekProvider (incumbente) y GraniteProvider (baseURL interpolado con CLOUDFLARE_ACCOUNT_ID); iterar GOLDEN_SET_GATE (fichas), para cada caso correr clasificarFicha(input, deepseek) y clasificarFicha(input, granite) SECUENCIAL con await delay(2500) entre llamadas al mismo host (rate-limit LOCKED); comparar sector_codigo salida-a-salida; acumular acuerdos/desacuerdos; console.log del resumen (acuerdo N/M, lista de desacuerdos con caso.id). NO persistir en DB, NO alterar el provider productivo — es observación pura. Sin assert de gate automático sobre el acuerdo (informativo; el veredicto full-40 ya aprobó Δ0.0000) salvo un piso mínimo defensivo documentado. AÑADIR un describe OFFLINE (siempre corre, sin gate) que asevera INTEG-03 con MockProvider: usando resolverProvider (de 109-02) con env {CLASIFICACION_ESCALERA:"1"} → TieredProvider, con env {} → DeepSeekProvider (re-confirma el rollback por config desde este plan). Comentario de cabecera: cita RESEARCH §6 + "shadow-eval ON antes de promover; el agente NO promueve".
  </action>
  <verify>
    <automated>pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts</automated>
  </verify>
  <done>El describe OFFLINE (rollback assert) pasa siempre. El describe LIVE hace skip limpio sin CLASIFICACION_SHADOW_LIVE=1 (0 red en CI). tsc -b exit 0. Con keys+flag, el shadow corre secuencial con delay 2-3s y reporta acuerdo/desacuerdo.</done>
</task>

<task type="auto">
  <name>Task 2: drift canary del endpoint (LIVE-gated) + .env.example</name>
  <files>packages/cruces/src/drift-canary.test.ts, .env.example</files>
  <action>
    Crear packages/cruces/src/drift-canary.test.ts con LIVE-gate CLASIFICACION_DRIFT_CHECK === "1"; it.skipIf(!WORKERS_AI_API_TOKEN || !CLOUDFLARE_ACCOUNT_ID). Constante MODELO_PINNEADO = "@cf/ibm-granite/granite-4.0-h-micro". En el test LIVE: hacer una probe mínima (1 caso del GOLDEN_SET_GATE) vía GraniteProvider, capturar el campo `model` que Workers AI devuelve en el body de la completion (RESEARCH §7). Si el modelo servido !== MODELO_PINNEADO: fallar con mensaje "[DRIFT DETECTADO] modelo servido: X ≠ pinneado: Y — veredicto full-40 INVALIDADO. Re-correr benchmark antes de promover." Si coincide: pasar + console.log de confirmación de provenance (modelo + endpoint + fecha pinneada). Si GraniteProvider no expone el campo model del body directamente, extender la probe con un fetch directo al endpoint (mismo baseURL interpolado + header Authorization Bearer WORKERS_AI_API_TOKEN + body mínimo) y leer response.model — documentar cuál vía se usó. En .env.example: agregar `CLASIFICACION_SHADOW_LIVE=` y `CLASIFICACION_DRIFT_CHECK=` con comentario "gate LIVE de observación (shadow-eval / canario de drift); vacío = skip; nunca en CI". NO tocar otros valores.
  </action>
  <verify>
    <automated>pnpm --filter @obs/cruces exec vitest run src/drift-canary.test.ts</automated>
  </verify>
  <done>El drift canary hace skip limpio sin CLASIFICACION_DRIFT_CHECK=1. tsc -b exit 0. .env.example tiene CLASIFICACION_SHADOW_LIVE y CLASIFICACION_DRIFT_CHECK vacíos documentados. Con keys+flag, la probe captura el modelo servido y lo compara contra el pinneado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shadow-eval → Workers AI PROD | Llamadas LIVE al endpoint; rate-limit 2-3s obligatorio; nunca en CI |
| Veredicto full-40 → endpoint servido | El modelo servido puede cambiar sin aviso, invalidando la paridad medida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-08 | Repudiation | promover sin evidencia de sombra | mitigate | shadow-eval LIVE-gated reporta acuerdo/desacuerdo antes de que el operador flipee (Task 1) |
| T-109-09 | Spoofing | Workers AI sirve otro modelo, números viejos ya no valen | mitigate | drift-canary compara modelo servido vs @cf/ibm-granite/granite-4.0-h-micro e invalida el veredicto (Task 2) |
| T-109-10 | Denial of Service | ráfaga al WAF/endpoint gubernamental o Workers AI | mitigate | delay 2-3s secuencial entre llamadas al mismo host (regla LOCKED, Task 1) |
| T-109-11 | Information Disclosure | shadow-eval LIVE corre en CI y filtra keys/latencia | mitigate | LIVE-gate + skipIf sin keys; CI no setea CLASIFICACION_SHADOW_LIVE ni CLASIFICACION_DRIFT_CHECK → skip limpio |
| T-109-SC | Tampering | npm installs | accept | Cero paquetes nuevos; probe vía GraniteProvider o global fetch (patrón existente 103-04) |
</threat_model>

<verification>
- `pnpm --filter @obs/cruces exec vitest run src/shadow-eval.test.ts src/drift-canary.test.ts` → OFFLINE verde, LIVE skip limpio sin flags
- `pnpm --filter @obs/cruces exec tsc -b` → exit 0
- .env.example tiene CLASIFICACION_SHADOW_LIVE + CLASIFICACION_DRIFT_CHECK vacíos
- El describe OFFLINE de shadow-eval re-confirma el rollback por config (INTEG-03)
- CI (ci.yml de 109-02) sigue verde: los LIVE-gated se saltean sin flags
</verification>

<success_criteria>
- Shadow-eval corre Granite en sombra vs DeepSeek sobre el golden, reporta acuerdo/desacuerdo, sin alterar salida productiva → INTEG-01 shadow-eval ON
- Rollback por config re-confirmado con MockProvider (env var off = incumbente) → INTEG-03
- Drift canary detecta cambio de modelo servido e invalida el veredicto → INTEG-03 canario de drift
- Todo LIVE-gated con skip limpio; nunca corre en CI; rate-limit 2-3s respetado
- El agente NO promueve la escalera (deja default incumbente; la promoción es acto de operador tras shadow verde)
</success_criteria>

<output>
Create `.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-03-SUMMARY.md` when done
</output>
