---
phase: 109-integ-p3-integrar-clasificacion-tras-golden-gate-verde
plan: "02"
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - packages/cruces/src/clasificar-fichas-cli.ts
  - packages/cruces/src/clasificar-fichas-cli.test.ts
  - .github/workflows/ci.yml
  - .env.example
autonomous: true
requirements: [INTEG-01, INTEG-03]
must_haves:
  truths:
    - "Sin la env var CLASIFICACION_ESCALERA (o !=1), el CLI construye DeepSeekProvider directo — comportamiento byte-idéntico al actual (default incumbente)"
    - "Con CLASIFICACION_ESCALERA=1, el CLI construye la escalera Granite→DeepSeek vía buildTieredProvider (TieredProvider, drop-in LLMProvider)"
    - "El golden de clasificación corre en CI como gate PERMANENTE que MUERDE (ci.yml ampliado con step offline @obs/cruces + @obs/llm)"
  artifacts:
    - path: "packages/cruces/src/clasificar-fichas-cli.ts"
      provides: "Construction point con env-gate: default DeepSeek, escalera si CLASIFICACION_ESCALERA=1"
      contains: "CLASIFICACION_ESCALERA"
    - path: "packages/cruces/src/clasificar-fichas-cli.test.ts"
      provides: "Test offline del wiring: default incumbente byte-idéntico + escalera con env var"
      contains: "CLASIFICACION_ESCALERA"
    - path: ".github/workflows/ci.yml"
      provides: "Steps CI para @obs/cruces y @obs/llm (golden + guards muerden en CI)"
      contains: "@obs/cruces"
  key_links:
    - from: "packages/cruces/src/clasificar-fichas-cli.ts"
      to: "buildTieredProvider (@obs/llm)"
      via: "import + construcción condicional por env var"
      pattern: "buildTieredProvider"
    - from: ".github/workflows/ci.yml"
      to: "packages/cruces + packages/llm vitest"
      via: "step pnpm --filter exec vitest run"
      pattern: "@obs/cruces|@obs/llm"
---

<objective>
El swap del construction point (protegido ya por los guards de Wave 1) + convertir el golden de clasificación en una regresión CI PERMANENTE.

Purpose: Integrar TieredProvider como drop-in en el único punto de construcción del CLI de clasificación de fichas, con DEFAULT = incumbente (DeepSeek): sin la env var el comportamiento es byte-idéntico al de hoy. Encender la escalera es un config-flip reversible del operador, NO un acto del agente. Y hacer que el golden set muerda en CI (hoy ci.yml solo corre `app/`).

Output: `clasificar-fichas-cli.ts` con env-gate; test offline del wiring (default + escalera vía MockProvider); ci.yml ampliado; `.env.example` con placeholders documentados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-CONTEXT.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-RESEARCH.md
@.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-01-SUMMARY.md

<read_first>
- packages/cruces/src/clasificar-fichas-cli.ts:187-234 — el construction point EXACTO (línea 199-200: `opts.provider ?? new DeepSeekProvider(...)`). El swap va aquí; el resto del cuerpo NO cambia. FichasCliOptions.provider?: LLMProvider ya existe (línea 57), y clasificarFicha(inputDeFila(fila), provider) lo consume (línea 234).
- packages/llm/src/task-ladder.ts:96-116 — buildTieredProvider(config: LadderConfig): TieredProvider. La fábrica canónica. LadderConfig = { primary: TierSpec, escalation?: TierSpec, judge?, maxBudgetUsd? }.
- packages/llm/src/providers/granite.ts:46-56,90 — DEFAULT_BASE_URL con template {ACCOUNT_ID}; DEFAULT_MAX_TOKENS=2048. baseURL debe interpolarse con CLOUDFLARE_ACCOUNT_ID (Pitfall 1).
- packages/llm/src/test-mock.ts — MockProvider para el test offline del wiring (sin red).
- packages/cruces/src/golden/golden-set.test.ts:1-119 — el golden gate offline (MockClasificadorProvider, evaluarGolden, gatePasa). Ya pasa sin red — es lo que CI debe correr.
- .github/workflows/ci.yml:44-52 — hoy solo `pnpm --filter ./app test` + tsc. Ampliar aquí.
- .env.example:7,167,170 — DEEPSEEK_API_KEY / WORKERS_AI_API_TOKEN / CLOUDFLARE_ACCOUNT_ID ya presentes. Agregar CLASIFICACION_ESCALERA.
</read_first>

<interfaces>
Construcción propuesta en clasificar-fichas-cli.ts:200 (lógica, NO código literal):
  Si opts.provider está presente → usarlo (tests lo inyectan; NO tocar esta rama).
  Si CLASIFICACION_ESCALERA !== "1" → new DeepSeekProvider({ apiKey: process.env.DEEPSEEK_API_KEY ?? "" }) (default incumbente, byte-idéntico).
  Si CLASIFICACION_ESCALERA === "1":
    validar que WORKERS_AI_API_TOKEN y CLOUDFLARE_ACCOUNT_ID no estén vacíos (Pitfall 2); si vacíos → caer a DeepSeek con log de advertencia (nunca construir Granite con key vacía).
    construir buildTieredProvider({
      primary: { provider: new GraniteProvider({ apiKey: WORKERS_AI_API_TOKEN, baseURL: `https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/ai/v1` }), costPerToken: 0.00000000125 },
      escalation: { provider: new DeepSeekProvider({ apiKey: DEEPSEEK_API_KEY }), costPerToken: 0.00000014 },
    })  // sin judge, sin maxBudgetUsd (RESEARCH §2)
  Log del provider activo (verificabilidad rollback): "cruces-fichas: provider=tiered:granite→deepseek" vs "cruces-fichas: provider=deepseek".

Imports a agregar: buildTieredProvider, GraniteProvider de @obs/llm (DeepSeekProvider ya importado).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: swap del construction point con env-gate default-incumbente</name>
  <files>packages/cruces/src/clasificar-fichas-cli.ts, packages/cruces/src/clasificar-fichas-cli.test.ts</files>
  <behavior>
    - Test 1 (default incumbente, LOAD-BEARING): con opts.provider undefined y CLASIFICACION_ESCALERA ausente, el provider construido es instancia de DeepSeekProvider (NO TieredProvider). Byte-idéntico al comportamiento actual. Este test es la prueba de INTEG-03 (rollback por config).
    - Test 2 (escalera): con CLASIFICACION_ESCALERA="1" + WORKERS_AI_API_TOKEN y CLOUDFLARE_ACCOUNT_ID seteados (valores dummy en el test), el provider construido es instancia de TieredProvider.
    - Test 3 (fail-safe keys ausentes): con CLASIFICACION_ESCALERA="1" pero WORKERS_AI_API_TOKEN vacío, cae a DeepSeekProvider directo (nunca construye Granite con key vacía) — Pitfall 2.
    - Test 4 (inyección respetada): con opts.provider = MockProvider, se usa ese (ninguna env var lo sobrescribe).
  </behavior>
  <action>
    Modificar clasificar-fichas-cli.ts:198-200. Reemplazar la construcción por la lógica del bloque <interfaces>: rama opts.provider intacta; env-gate CLASIFICACION_ESCALERA; validación de keys no-vacías antes de GraniteProvider; log del provider activo. Agregar imports buildTieredProvider + GraniteProvider de @obs/llm. Para hacer el construction point testeable sin arrancar el CLI completo, extraer la resolución del provider a una función pura exportada `resolverProvider(opts: Pick<FichasCliOptions,"provider">, env: NodeJS.ProcessEnv, log): LLMProvider` y llamarla desde main(). El test crea la función con env dummy — NO depende de process.env global. NO tocar nada más del cuerpo de main() (dry-run, writer, client, loop). costPerToken son informativos (telemetría), no afectan lógica.
  </action>
  <verify>
    <automated>pnpm --filter @obs/cruces exec vitest run src/clasificar-fichas-cli.test.ts</automated>
  </verify>
  <done>Los 4 tests pasan. Default sin env var = DeepSeekProvider (instanceof). Escalera con env var = TieredProvider (instanceof). Keys vacías = fallback DeepSeek. tsc -b exit 0. El guard integ-scope de Wave 1 sigue verde (clasificar-fichas-cli.ts SÍ puede tener TieredProvider — es el target legítimo; lobby-cli/pipeline-cli NO).</done>
</task>

<task type="auto">
  <name>Task 2: golden de clasificación como gate CI permanente + .env.example</name>
  <files>.github/workflows/ci.yml, .env.example</files>
  <action>
    Ampliar .github/workflows/ci.yml agregando DOS steps offline tras el step de tsc (líneas 51-52), sin quitar los existentes: (a) `- name: Test @obs/llm (TieredProvider + provider-guard + scope-guards)` con `run: pnpm --filter @obs/llm exec vitest run`; (b) `- name: Test @obs/cruces (golden clasificación + wiring)` con `run: pnpm --filter @obs/cruces exec vitest run`. Ambos corren OFFLINE (sin secrets: el golden usa MockClasificadorProvider, los LIVE-gated se saltean por CRUCES_GOLDEN_LIVE ausente). NO agregar secrets de env al job. Agregar comentario que explique que el golden muerde aquí (gate de regresión permanente INTEG-01). En .env.example: agregar `CLASIFICACION_ESCALERA=` con comentario "solo setear a 1 tras shadow-eval verde sostenido (config-flip de operador; default vacío = incumbente DeepSeek)". NO tocar los valores DEEPSEEK/WORKERS_AI/CLOUDFLARE ya presentes.
  </action>
  <verify>
    <automated>grep -q "@obs/cruces" .github/workflows/ci.yml && grep -q "@obs/llm exec vitest" .github/workflows/ci.yml && grep -q "CLASIFICACION_ESCALERA" .env.example && echo OK</automated>
  </verify>
  <done>ci.yml tiene los dos steps offline nuevos (@obs/llm + @obs/cruces) sin secrets; .env.example tiene CLASIFICACION_ESCALERA vacío documentado. Localmente `pnpm --filter @obs/cruces exec vitest run` y `pnpm --filter @obs/llm exec vitest run` verdes (simulan lo que CI correrá).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Construction point → escalera | El swap introduce TieredProvider; el default debe ser incumbente para no cambiar producción |
| CI → packages | El golden/guards no muerden hasta que ci.yml los corra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04 | Repudiation | promover la escalera sin evidencia | mitigate | Default = incumbente; CLASIFICACION_ESCALERA vacío en .env.example + comentario "solo tras shadow-eval verde"; el agente NO setea la var (Task 2) |
| T-109-05 | Denial of Service | GraniteProvider con key vacía → 401 en primeros items | mitigate | Validación de WORKERS_AI_API_TOKEN/CLOUDFLARE_ACCOUNT_ID no-vacíos antes de construir Granite; fallback DeepSeek (Task 1, Test 3, Pitfall 2) |
| T-109-06 | Tampering | regresión del golden no detectada en CI | mitigate | ci.yml ampliado corre golden + guards offline como gate permanente (Task 2) |
| T-109-07 | Information Disclosure | ACCOUNT_ID hardcodeado en baseURL | mitigate | Interpolación de CLOUDFLARE_ACCOUNT_ID desde env, nunca literal (Task 1, Pitfall 1) |
| T-109-SC | Tampering | npm installs | accept | Cero paquetes nuevos; ci.yml usa pnpm install --frozen-lockfile --ignore-scripts existente |
</threat_model>

<verification>
- `pnpm --filter @obs/cruces exec vitest run` → golden + wiring verdes
- `pnpm --filter @obs/llm exec vitest run` → guards Wave 1 + TieredProvider verdes
- `pnpm --filter @obs/cruces exec tsc -b` → exit 0
- ci.yml contiene los steps @obs/llm y @obs/cruces; .env.example tiene CLASIFICACION_ESCALERA vacío
- integ-scope-guard sigue verde (lobby-cli/pipeline-cli sin TieredProvider)
</verification>

<success_criteria>
- Sin CLASIFICACION_ESCALERA: provider = DeepSeekProvider (byte-idéntico, test load-bearing) → INTEG-03 rollback por config probado
- Con CLASIFICACION_ESCALERA=1 + keys: provider = TieredProvider (Granite→DeepSeek) → INTEG-01 escalera integrada
- Golden de clasificación corre en CI como gate permanente que muerde → INTEG-01 regresión permanente
- Agente NO setea la env var (default incumbente); config-flip diferido a operador
</success_criteria>

<output>
Create `.planning/phases/109-integ-p3-integrar-clasificacion-tras-golden-gate-verde/109-02-SUMMARY.md` when done
</output>
