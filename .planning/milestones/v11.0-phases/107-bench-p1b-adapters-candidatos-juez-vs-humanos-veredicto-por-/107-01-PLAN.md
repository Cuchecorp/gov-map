---
phase: 107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/llm/src/providers/granite.ts
  - packages/llm/src/providers/granite.test.ts
  - packages/llm/src/judge.ts
  - packages/llm/src/providers/phi-judge.ts
  - packages/llm/src/providers/phi-judge.test.ts
  - packages/llm/src/index.ts
  - packages/llm-bench/src/pricing.ts
  - .env.example
autonomous: true
requirements: [TIER-01]

must_haves:
  truths:
    - "GraniteProvider implements LLMProvider cloning the MiniMaxProvider shape (openai@5 + baseURL, forced single-function tool_choice, external parseAndValidate) with zero new SDK"
    - "GraniteProvider matches its tool_call BY function.name (never position): a positional-only mismatch (wrong first call) still finds emit_result; an all-wrong-name response yields undefined -> repair -> LLMValidationError"
    - "GraniteProvider sets max_tokens EXPLICITLY (Workers AI default 256 would truncate) and is host-agnostic by baseURL (Workers AI primary / OpenRouter fallback)"
    - "Both new adapters run the IDENTICAL fail-closed guards: assertNoRutInLlmInput on user AND system, assertSensitivityAllowed — a RUT in either input throws before any network call"
    - "PhiJudge implements a SEPARATE JudgeProvider interface with the LOCKED JudgeRequest{answer, system?, sensitivity?, temperature?, context?} shape, returns a zod-validated Verdict{ok, reason?, confidence?}, is deterministic (temp forced 0), and matches its tool_call by name (Phi hallucinates function names)"
    - "All tests run against a fake fetch (makeMockFetch) with NO network and NO key — CI green without any candidate secret"
  artifacts:
    - path: "packages/llm/src/providers/granite.ts"
      provides: "GraniteProvider implements LLMProvider (MiniMax clone, explicit max_tokens, host-agnostic baseURL)"
      contains: "class GraniteProvider"
    - path: "packages/llm/src/judge.ts"
      provides: "JudgeProvider interface + Verdict zod schema/type + JudgeRequest (LOCKED shape: answer, system?, sensitivity?, temperature?, context?)"
      contains: "JudgeProvider"
    - path: "packages/llm/src/providers/phi-judge.ts"
      provides: "PhiJudge implements JudgeProvider (deterministic, match-by-name, same guards)"
      contains: "class PhiJudge"
    - path: "packages/llm/src/providers/granite.test.ts"
      provides: "Mock-fetch tests: match-by-name, RUT-guard-bites, max_tokens explicit, repair"
      contains: "RutInLlmInputError"
    - path: "packages/llm/src/providers/phi-judge.test.ts"
      provides: "Mock-fetch tests: Verdict validated, deterministic, match-by-name, RUT-guard-bites (answer AND system)"
      contains: "Verdict"
  key_links:
    - from: "packages/llm/src/providers/granite.ts"
      to: "packages/llm/src/data-routing.ts"
      via: "assertNoRutInLlmInput + assertSensitivityAllowed"
      pattern: "assertNoRutInLlmInput"
    - from: "packages/llm/src/index.ts"
      to: "packages/llm/src/providers/granite.ts"
      via: "barrel re-export"
      pattern: "providers/granite"
    - from: "packages/llm-bench/src/pricing.ts"
      to: "granite + phi model ids"
      via: "PRICING.tarifas entries for candidate models"
      pattern: "granite"
---

<objective>
Build the candidate adapters that the 107 veredicto will score: `GraniteProvider` (responder on the existing `LLMProvider` interface) and `PhiJudge` (on a NEW, separate `JudgeProvider` interface). Both clone the `MiniMaxProvider` tool-calling shape EXACTLY — openai@5 + baseURL (zero new SDK), forced single-function `tool_choice`, tool_call matched BY NAME, external `parseAndValidate` repair loop, and the IDENTICAL fail-closed guards. Add candidate pricing rows and the three empty `.env.example` placeholders.

Purpose: TIER-01 — the adapters must exist and be provably correct BEFORE any measurement. Everything here is CI-testable with a fake fetch: no network, no candidate key. The LIVE numbers come later (Plan 03, deferred).
Output: `granite.ts`, `judge.ts` (with the LOCKED `JudgeRequest` shape), `phi-judge.ts` + their mock-fetch tests, barrel re-exports, candidate PRICING rows, and 3 empty `.env.example` placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-CONTEXT.md

<read_first>
- packages/llm/src/providers/minimax.ts — THE TEMPLATE. Clone its structure exactly: constructor (apiKey/baseURL/model/fetchFn), the `callModel` closure, `tool_choice: { type:"function", function:{ name: TOOL_NAME } }`, the `calls.find(c => c.type==="function" && c.function.name === TOOL_NAME)` match-by-name, and the `parseAndValidate(schema, first, { maxAttempts, onOutcome: req.onValidationOutcome, reprompt })` tail. Granite adds ONE thing MiniMax lacks: an explicit `max_tokens`.
- packages/llm/src/providers/minimax.test.ts — the mock-fetch test patterns to mirror: `toolResponse(args)`, `reorderedToolResponse(args, wrongName)` (proves match-by-name), the RUT-guard bite via `RutInLlmInputError`, and the `makeMockFetch` import from `../../test/_helpers`.
- packages/llm/src/providers/deepseek.ts — confirms the `readonly id` / `readonly trainsOnInputs` / `DEFAULT_BASE_URL` / `DEFAULT_MODEL` module-constant idiom (lines 22-54).
- packages/llm/src/types.ts — `CompletionRequest` (has `onValidationOutcome`, `temperature`, `sensitivity`), `LLMProvider`, `Sensitivity`. The new `JudgeProvider` lives in a NEW file `judge.ts`, NOT in types.ts.
- packages/llm/src/validate.ts — `parseAndValidate`, `clampRepairAttempts`, `LLMValidationError`, `ValidationOutcome`. Do not reimplement.
- packages/llm/src/data-routing.ts — `assertNoRutInLlmInput`, `assertSensitivityAllowed`, `RutInLlmInputError`. Call VERBATIM; do not re-derive the RUT regex.
- packages/llm/src/json-schema.ts — `zodToToolSchema`. Reuse for the tool `parameters`.
- packages/llm/src/index.ts — the barrel (owner note: 02-01). Add two re-exports: `providers/granite` and `providers/phi-judge`, plus `./judge`.
- packages/llm-bench/src/pricing.ts — add candidate rows to `PRICING.tarifas`. The comment at line 24 says candidates are "107 scope — NOT listed here"; this plan IS 107, so add them now.
- .env.example — current tail has `RESEND_API_KEY=` (empty placeholder). Add the three new empty placeholders in the same style.
- app/lib/env-example-guard.test.ts — the guard that MUST stay green. Confirm: an EMPTY value (`KEY=`) is allowlisted (line 57 `if (value === "") continue;`). The three additions MUST be empty (`KEY=`), never a value.
</read_first>

<interfaces>
<!-- MiniMaxProvider constructor + complete() shape the executor clones for Granite -->
From packages/llm/src/providers/minimax.ts:
```
export interface MiniMaxProviderOptions { apiKey: string; baseURL?: string; model?: string; fetchFn?: typeof fetch; }
class MiniMaxProvider implements LLMProvider {
  readonly id = "minimax";
  readonly trainsOnInputs = false;
  async complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T> { ... }
}
```
From packages/llm/src/types.ts:
```
export interface LLMProvider { readonly id: string; readonly trainsOnInputs: boolean; complete<T>(req: CompletionRequest, schema: ZodType<T>): Promise<T>; }
export interface CompletionRequest { system?: string; user: string; criticality: Criticality; sensitivity: Sensitivity; maxRepairAttempts?: number; temperature?: number; onValidationOutcome?: (o: ValidationOutcome) => void; }
export type Sensitivity = /* ... existing union, e.g. "public" | ... */;
```
From packages/llm/src/data-routing.ts:
```
export function assertNoRutInLlmInput(text: string): void;  // throws RutInLlmInputError
export function assertSensitivityAllowed(task: { sensitivity: Sensitivity }, provider: { id: string; trainsOnInputs: boolean }): void;
```

CONTRACT the executor MUST define VERBATIM in judge.ts (Wave 2 / 107-02 compiles against this — do NOT rename fields in Wave 2):
```
// packages/llm/src/judge.ts — the EXACT JudgeRequest shape Plan 02/03 consume.
export interface JudgeRequest {
  answer: string;                 // the answer text to judge — guarded by assertNoRutInLlmInput
  system?: string;                // optional system/rubric text — ALSO guarded by assertNoRutInLlmInput (the guard-on-system criterion has this to guard)
  sensitivity?: Sensitivity;      // defaults to "public" (the juez golden is NO-PII by construction)
  temperature?: number;           // defaults to 0 (determinism)
  context?: string;               // optional extra context passed to the judge prompt
}
export const VerdictSchema = z.object({ ok: z.boolean(), reason: z.string().optional(), confidence: z.number().min(0).max(1).optional() });
export type Verdict = z.infer<typeof VerdictSchema>;
export interface JudgeProvider { readonly id: string; readonly trainsOnInputs: boolean; judge(req: JudgeRequest): Promise<Verdict>; }
```
Field names are LOCKED here: `answer` (not pregunta/text/input) and `system` MUST exist, because 107-02's bridge calls `judge.judge({ answer: caso.answer, sensitivity: "public" })` and the guard-on-system-text criterion needs a `system` field to guard. This is inside Claude's Discretion per 107-CONTEXT (no conflict) — it is fixed ONCE here so Wave 2 compiles against a stable contract; do not reinvent it in 107-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraniteProvider — clone MiniMax with explicit max_tokens + candidate PRICING rows</name>
  <files>packages/llm/src/providers/granite.ts, packages/llm/src/providers/granite.test.ts, packages/llm-bench/src/pricing.ts</files>
  <behavior>
    - A well-formed emit_result tool_call → the validated object (clean).
    - reorderedToolResponse (wrong-name call FIRST, emit_result SECOND) → still finds emit_result by name and returns the validated object (proves match-by-name, not position).
    - An all-wrong-name response (no emit_result at all) → arguments undefined → repair loop → after attempts exhausted, throws LLMValidationError (never accepts unstructured output).
    - assertNoRutInLlmInput bites: a RUT in req.user throws RutInLlmInputError BEFORE any fetch (fetch spy never called); a RUT in req.system also throws.
    - The outgoing request body carries an explicit numeric max_tokens (assert via the captured mock-fetch body); NOT absent/256.
    - trainsOnInputs is a fixed readonly boolean (conservative per host DPA posture until Workers AI DPA confirmed) — documented, NOT env-configurable.
  </behavior>
  <action>Create GraniteProvider by cloning MiniMaxProvider verbatim, changing only: (1) `readonly id = "granite"`; (2) DEFAULT_BASE_URL = the Workers AI OpenAI-compat base — document that it is templated with CLOUDFLARE_ACCOUNT_ID (`https://api.cloudflare.com/client/v4/accounts/{ACCOUNT_ID}/ai/v1`) and that OpenRouter (`https://openrouter.ai/api/v1`) is the fallback by baseURL swap; DEFAULT_MODEL = `@cf/ibm-granite/granite-4.0-h-micro`; (3) add an explicit `max_tokens` to the `chat.completions.create` call (a module constant, e.g. DEFAULT_MAX_TOKENS with a high value) because Workers AI defaults to 256 and would truncate structured output — cite the Pitfall in a comment; (4) keep the `calls.find(c => c.type === "function" && c.function.name === TOOL_NAME)` match-by-name VERBATIM; (5) keep both guards (assertNoRutInLlmInput on req.user AND req.system, assertSensitivityAllowed) BEFORE building the request; (6) keep the external parseAndValidate tail with `onOutcome: req.onValidationOutcome` (so the harness measures the real outcome). Add `readonly trainsOnInputs` as a fixed boolean with a comment that it reflects the host DPA posture and is conservative until Workers AI DPA is confirmed (per 107-CONTEXT specifics). Add a constructor option `model`/`baseURL` so the LIVE run (Plan 03) can point it at Workers AI via env. In pricing.ts, add two rows to PRICING.tarifas keyed by the candidate model ids used in the LIVE run (Granite id `@cf/ibm-granite/granite-4.0-h-micro` and the Phi judge id `microsoft/phi-4-mini-instruct`) with [ASSUMED] MEDIUM comments dated 2026-07-26; remove/adjust the "candidates NOT listed here" comment since 107 owns them now. Write granite.test.ts mirroring minimax.test.ts (import makeMockFetch from `../../test/_helpers`), covering every behavior above, INCLUDING an assertion on the captured request body that max_tokens is present and numeric.</action>
  <verify>
    <automated>cd packages/llm && pnpm vitest run src/providers/granite.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GraniteProvider matches tool_call by function.name; the reordered-call test passes; the all-wrong-name test throws LLMValidationError.
    - The RUT-guard-bites test PASSES for both req.user and req.system (RutInLlmInputError thrown, fetch never invoked).
    - A test asserts the outgoing body has an explicit numeric max_tokens.
    - `pnpm vitest run src/providers/granite.test.ts` green; no network (only makeMockFetch).
    - PRICING has candidate rows; grep `granite` in pricing.ts returns a tarifa entry.
  </acceptance_criteria>
  <done>GraniteProvider is a faithful MiniMax clone with explicit max_tokens and identical guards; all mock tests green; candidate pricing rows added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JudgeProvider/Verdict/JudgeRequest interface + PhiJudge adapter + barrel + .env.example placeholders</name>
  <files>packages/llm/src/judge.ts, packages/llm/src/providers/phi-judge.ts, packages/llm/src/providers/phi-judge.test.ts, packages/llm/src/index.ts, .env.example</files>
  <behavior>
    - PhiJudge.judge(req) returns a zod-validated Verdict{ok:boolean, reason?:string, confidence?:number} — a malformed tool_call payload goes through repair and, if it never validates, throws LLMValidationError (never returns an unstructured verdict).
    - Determinism: the outgoing request forces temperature 0 (assert via captured mock-fetch body).
    - Match-by-name: a response whose FIRST tool_call has a hallucinated function name and the correct verdict function SECOND still resolves the verdict by name; an all-wrong-name response → repair → LLMValidationError.
    - RUT-guard bites: a RUT inside req.answer throws RutInLlmInputError before any fetch; a RUT inside req.system ALSO throws (both inputs guarded).
    - PhiJudge exposes readonly id="phi-judge" and trainsOnInputs (conservative fixed boolean, documented as a legal gate not a convenience flag).
  </behavior>
  <action>Create `packages/llm/src/judge.ts` defining the SEPARATE `JudgeProvider` interface (NOT LLMProvider — the judge is not a responder), using the LOCKED shapes from the `<interfaces>` block VERBATIM: `VerdictSchema` zod `z.object({ ok: z.boolean(), reason: z.string().optional(), confidence: z.number().min(0).max(1).optional() })` + inferred `Verdict` type; the `JudgeRequest` interface EXACTLY as pinned (`answer: string; system?: string; sensitivity?: Sensitivity; temperature?: number; context?: string`) — field names LOCKED, do not rename; and `interface JudgeProvider { readonly id: string; readonly trainsOnInputs: boolean; judge(req: JudgeRequest): Promise<Verdict>; }`. Document (JSDoc) that the judge is conceptually ESCALATE-ONLY — 108 wires that composition; 107 only MEASURES capability. Create `packages/llm/src/providers/phi-judge.ts`: `class PhiJudge implements JudgeProvider`, cloning the MiniMax tool-calling internals (openai@5 + baseURL + fetchFn, forced single-function tool_choice, match tool_call BY NAME, external parseAndValidate against the Verdict schema via zodToToolSchema). Force temperature 0 (determinism) by always passing `temperature: req.temperature ?? 0`. Run BOTH guards fail-closed: assertNoRutInLlmInput on `req.answer` AND on `req.system` (when present), assertSensitivityAllowed with `req.sensitivity ?? "public"` and a conservative provider posture. DEFAULT_MODEL = `microsoft/phi-4-mini-instruct`, DEFAULT_BASE_URL = OpenRouter (`https://openrouter.ai/api/v1`); constructor accepts model/baseURL/apiKey/fetchFn for the LIVE run. Add a comment that Phi is documented to hallucinate function names → match-by-name is load-bearing. Update `packages/llm/src/index.ts` barrel with three re-exports: `export * from "./judge";`, `export * from "./providers/granite";`, `export * from "./providers/phi-judge";`. Append three EMPTY placeholders to `.env.example` (same style as `RESEND_API_KEY=`), each with a one-line `#` comment: `WORKERS_AI_API_TOKEN=`, `CLOUDFLARE_ACCOUNT_ID=`, `OPENROUTER_API_KEY=` — NO values, ever. Write phi-judge.test.ts mirroring minimax.test.ts covering every behavior above, INCLUDING the RUT-guard bite on BOTH req.answer and req.system.</action>
  <verify>
    <automated>cd packages/llm && pnpm vitest run src/providers/phi-judge.test.ts && cd ../.. && pnpm --filter @obs/observatorio exec vitest run lib/env-example-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `judge.ts` exports `JudgeProvider`, `Verdict`/`VerdictSchema`, and `JudgeRequest` with the LOCKED field names (`answer`, `system?`, `sensitivity?`, `temperature?`, `context?`).
    - PhiJudge.judge returns a zod-validated Verdict; the deterministic test asserts temperature 0 in the captured body; the match-by-name reordered test resolves the verdict; the all-wrong-name test throws LLMValidationError.
    - The RUT-guard-bites test PASSES for req.answer AND req.system (RutInLlmInputError, fetch never invoked).
    - `.env.example` contains exactly the three new EMPTY placeholders; env-example-guard.test.ts stays green (0 offenders).
    - Barrel re-exports judge, granite, phi-judge; `@obs/llm` type-checks.
  </acceptance_criteria>
  <done>JudgeProvider/Verdict/JudgeRequest interface defined with LOCKED field names; PhiJudge deterministic with match-by-name and identical guards (answer + system); barrel updated; three empty .env.example placeholders added with the guard still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → LLM prompt | Untrusted text (may contain a RUT / personal data) crosses into a network request to a third-party model host |
| candidate host response → zod gate | The candidate model's tool_call output is untrusted until it passes parseAndValidate |
| .env.example (public repo) | A committed secret value here is Information Disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Information Disclosure | assertNoRutInLlmInput on Granite/Phi inputs | mitigate | Call the existing RUT guard on user AND system (Granite) / answer AND system (Phi) BEFORE any fetch; test that BITES for both inputs of both adapters (RutInLlmInputError, fetch never called) |
| T-107-02 | Tampering | candidate tool_call output | mitigate | External parseAndValidate + repair; match tool_call by function.name (Phi hallucinates names) — never trust position or `response_format: json_schema` |
| T-107-03 | Information Disclosure | .env.example placeholders | mitigate | Three additions are EMPTY (`KEY=`); env-example-guard.test.ts stays green; agent never loads a secret value |
| T-107-04 | Repudiation | trainsOnInputs legal gate | accept | Set conservative fixed boolean documented as a DPA gate (not env-configurable); Workers AI DPA confirmation is a provision datum (Plan 03 handoff) |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages (openai@5 + zod already present); no install task, no legitimacy checkpoint needed |
</threat_model>

<verification>
- `cd packages/llm && pnpm vitest run` (all provider + judge tests green, no network).
- `cd packages/llm && pnpm exec tsc -b` exit 0 (references, not paths).
- `pnpm --filter @obs/observatorio exec vitest run lib/env-example-guard.test.ts` green.
- Grep confirms no `response_format: json_schema` in granite.ts / phi-judge.ts; both use forced tool_choice.
- Grep confirms judge.ts exports `JudgeRequest` with an `answer` and a `system` field.
</verification>

<success_criteria>
GraniteProvider and PhiJudge exist, clone the MiniMax tool-calling shape, run identical fail-closed guards (RUT-guard bites in tests for both inputs), match tool_call by name, and set max_tokens (Granite) / temperature 0 (Phi) explicitly. JudgeProvider/Verdict/JudgeRequest interface is defined with LOCKED field names and separate from LLMProvider. Three empty .env.example placeholders added with the guard still green. Zero new SDK. All CI-side tests green with a fake fetch.
</success_criteria>

<output>
Create `.planning/phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-01-SUMMARY.md` when done
</output>
</content>
</invoke>
