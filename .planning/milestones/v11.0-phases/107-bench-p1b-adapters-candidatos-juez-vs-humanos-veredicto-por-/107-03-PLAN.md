---
phase: 107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-
plan: 03
type: execute
wave: 3
depends_on: ["107-02"]
files_modified:
  - packages/llm-bench/src/candidatos.live.test.ts
  - packages/llm-bench/README.md
autonomous: false
user_setup:
  - service: cloudflare-workers-ai
    why: "Candidato PRIMARIO Granite (@cf/ibm-granite/granite-4.0-h-micro) servido en Workers AI OpenAI-compat"
    env_vars:
      - name: WORKERS_AI_API_TOKEN
        source: "Cloudflare Dashboard -> My Profile -> API Tokens -> Create Token (permiso Workers AI)"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard -> cuenta (baseURL https://api.cloudflare.com/client/v4/accounts/{ACCOUNT_ID}/ai/v1)"
  - service: openrouter
    why: "Phi-4-mini juez (microsoft/phi-4-mini-instruct) + fallback baseURL-swap de Granite"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "openrouter.ai -> Keys -> Create Key (sk-or-...)"
  - service: deepseek-incumbent
    why: "Incumbente DeepSeek re-corrido en el MISMO bloque LIVE para medir candidato e incumbente contra los mismos sets congelados en una sola corrida (WARNING-1: baseline pinneado, no artefacto suelto)"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "YA presente en .env (sk-...); requerida para que el incumbente esté disponible cuando corre el candidato"
requirements: [BENCH-04, BENCH-05]

must_haves:
  truths:
    - "A single env-gated LIVE test runs the real Granite + Phi candidates AND re-runs the DeepSeek incumbent through the 106 harness (same frozen sets, same run) and the PhiJudge-vs-human measurement, producing real candidate + incumbent MetricasModelo + MetricasJuez"
    - "The LIVE test computes the real per-task VEREDICTO (candidate vs the SAME-RUN incumbent baseline) and prints the definitive {task -> approved-model | incumbent-stays | pending-evidence}"
    - "The test is SKIPPED by default (no LLM_BENCH_LIVE=1 / no keys) — CI never runs it, never touches network, never burns quota; the it.skipIf gates on ALL keys needed for BOTH candidate and incumbent"
    - "The exact keys and run command are documented for the operator; the agent never loads a secret value"
    - "A 'nada aprueba / pending-evidence' outcome is a VALID, non-failing result — the test asserts the veredicto was COMPUTED, not that anything approved"
  artifacts:
    - path: "packages/llm-bench/src/candidatos.live.test.ts"
      provides: "Env-gated LIVE candidate run + same-run DeepSeek incumbent + real veredicto (mirrors baseline.live.test.ts split)"
      contains: "LLM_BENCH_LIVE"
    - path: "packages/llm-bench/README.md"
      provides: "Operator run command + exact keys (incl. DEEPSEEK_API_KEY for the incumbent) + interpretation of pending-evidence"
      contains: "WORKERS_AI_API_TOKEN"
  key_links:
    - from: "packages/llm-bench/src/candidatos.live.test.ts"
      to: "packages/llm/src/providers/granite.ts + phi-judge.ts"
      via: "instantiates real adapters with instrumentedFetch + env keys"
      pattern: "GraniteProvider"
    - from: "packages/llm-bench/src/candidatos.live.test.ts"
      to: "packages/llm-bench/src/veredicto.ts"
      via: "computes the real per-task verdict against the same-run incumbent"
      pattern: "computarVeredicto"
---

<objective>
The DEFERRED live run. With Plan 01 (adapters) and Plan 02 (veredicto machine + es-CL negacion sub-metric + juez bridge) green in CI, this plan wires the SINGLE env-gated LIVE test that: (1) runs the real Granite responder + Phi judge AND re-runs the DeepSeek incumbent against the frozen golden sets through the 106 harness + the Plan 02 juez-vs-human bridge — all in ONE run so candidate and incumbent are measured against the same frozen sets, (2) computes the DEFINITIVE per-task VEREDICTO against the same-run incumbent baseline, and (3) prints it as the phase artifact.

It is DEFERRED because the CANDIDATE credentials are ABSENT from `.env` (verified: no OpenRouter `sk-or-…`, no `WORKERS_AI_API_TOKEN`, no `CLOUDFLARE_ACCOUNT_ID`). The incumbent's `DEEPSEEK_API_KEY` IS present. The operator was asked once; the candidate placeholders exist (Plan 01) but carry no values. **This plan is `autonomous:false`: it is a checkpoint that the operator provisions and runs.** Until then the phase CLOSES honestly on a documented handoff — the veredicto is "pending-evidence" (a VALID milestone outcome, per 107-CONTEXT LOCKED).

Purpose: BENCH-04 + BENCH-05 live evidence. The verdict only counts against the SERVED endpoint (Ollama-local numbers don't transfer — Pitfall 9). The incumbent is PINNED by re-running DeepSeek in the SAME block (WARNING-1) so candidate vs incumbent is apples-to-apples, not a stale committed artifact.
Output: `candidatos.live.test.ts` (skipped in CI), `README.md` operator run instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-CONTEXT.md

<read_first>
- packages/llm-bench/src/baseline.live.test.ts — the EXACT template for this test's LIVE/CI split: `const LIVE = process.env.LLM_BENCH_LIVE === "1"; (LIVE ? describe : describe.skip)(...) + it.skipIf(!KEY)`, the `instrumentedFetch(fetch, m => muestras.push(m))` wiring, `correrHarness(provider, id, { drenarMetricas, limitePorTarea })`, the `tablaLegible` printer, and the endpoint/tarifaFecha provenance asserts. It ALREADY runs the DeepSeek incumbent — reuse the SAME DeepSeek provider construction + harness call here so the incumbent is produced in-run. Clone this structure for the candidates.
- packages/llm/src/providers/deepseek.ts — the incumbent DeepSeekProvider (DEFAULT_MODEL, DEFAULT_BASE_URL, apiKey `DEEPSEEK_API_KEY`). Instantiate it here for the same-run incumbent.
- packages/llm-bench/src/harness.ts — `correrHarness`, `construirReporte`, `IdentidadModelo`. Both the candidate and incumbent runs call these unchanged (host-agnostic driver).
- packages/llm-bench/src/veredicto.ts (Plan 02) — `computarVeredicto` / `computarVeredictoDeReporte`, the `Veredicto` output type, `EPSILON_POR_TAREA`, and the es-CL hard veto that reads the extracción scorer's FIRST-CLASS `negacion.accuracy`. The LIVE test feeds candidate + same-run incumbent MetricasModelo in and prints the result.
- packages/llm-bench/src/tasks/juez/juez-vs-humano.ts (Plan 02) — `medirJuezVsHumano(judge)`. The LIVE test calls it with the real PhiJudge.
- packages/llm/src/providers/granite.ts + phi-judge.ts (Plan 01) — the real adapters. Instantiate with env keys + instrumentedFetch.
- packages/llm-bench/src/instrument.ts — `instrumentedFetch`, `CallMetric` (privacy: latency + token counts only, never payload/key).
- .env.example — the three CANDIDATE placeholders (WORKERS_AI_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, OPENROUTER_API_KEY) added by Plan 01. DEEPSEEK_API_KEY already exists in `.env` (native, no placeholder needed). The operator puts REAL candidate values in `.env` (LOCAL), NEVER in `.env.example`.
</read_first>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Operator provisions the three candidate keys and runs the definitive LIVE verdict</name>
  <what-built>The env-gated LIVE candidate test + same-run DeepSeek incumbent + veredicto printer are written and green-when-skipped; the CI suite never runs them. The ONLY missing thing to produce the definitive verdict is the three CANDIDATE credentials, which the agent cannot create (DeepSeek's incumbent key already exists in .env).</what-built>
  <how-to-verify>
  To PROVISION and RUN the definitive verdict (operator-only):

  1. Add THREE candidate values to the LOCAL `.env` (NEVER to `.env.example`, which stays empty). DEEPSEEK_API_KEY is ALREADY present (the incumbent is re-run in the same block):
     - `WORKERS_AI_API_TOKEN=` &lt;Cloudflare API token with Workers AI permission&gt;
     - `CLOUDFLARE_ACCOUNT_ID=` &lt;Cloudflare account id&gt;
     - `OPENROUTER_API_KEY=` &lt;OpenRouter key `sk-or-...`&gt;

  2. Run the LIVE candidate benchmark + incumbent + veredicto (from repo root):
     `LLM_BENCH_LIVE=1 pnpm --filter @obs/llm-bench exec vitest run src/candidatos.live.test.ts`
     (a smoke cap runs by default via `LLM_BENCH_LIMIT`; set `LLM_BENCH_LIMIT=0` for the full frozen golden sets — slower, decenas de llamadas reales; the incumbent DeepSeek run uses the SAME cap so the comparison stays apples-to-apples).

  3. Read the printed artifact: the per-model `MetricasModelo` for BOTH candidate and incumbent, the PhiJudge-vs-human `MetricasJuez` (precision_ok / recall_rechazo + bias hooks), and the definitive per-task `Veredicto` {task -> approved-model | incumbent-stays | pending-evidence}.

  4. Interpretation (LOCKED): a "nada aprueba paridad" or "pending-evidence" verdict is a VALID outcome — do NOT force an integration or fabricate numbers. Any es-CL negation/fidelity deficit (candidate `negacion.accuracy` below the incumbent's) is a HARD VETO regardless of aggregate metrics; English benchmarks are irrelevant.

  NOTE: numbers only count against the SERVED endpoint (Workers AI / OpenRouter). Register the host's DPA / no-train posture for the `trainsOnInputs` gate as a provision datum. The agent never loads a secret value nor runs the LIVE test.
  </how-to-verify>
  <resume-signal>Type "corrido" with the printed veredicto attached (or the endpoint/quota blocker), or "diferido" to close the phase on the documented handoff (pending-evidence).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Env-gated LIVE candidate test + same-run DeepSeek incumbent + veredicto printer + operator README</name>
  <files>packages/llm-bench/src/candidatos.live.test.ts, packages/llm-bench/README.md</files>
  <action>Create `packages/llm-bench/src/candidatos.live.test.ts` cloning the LIVE/CI split of `baseline.live.test.ts` EXACTLY: `const LIVE = process.env.LLM_BENCH_LIVE === "1"; (LIVE ? describe : describe.skip)(...)`. Inside, ONE `it.skipIf(...)` whose predicate requires ALL keys needed for BOTH candidate and incumbent so the incumbent is ALWAYS available when the candidate run fires (WARNING-1): skip unless `process.env.DEEPSEEK_API_KEY` (incumbent) AND (`process.env.WORKERS_AI_API_TOKEN` OR `process.env.OPENROUTER_API_KEY`) (candidate). The test: (a) instantiates the real `GraniteProvider` — model `@cf/ibm-granite/granite-4.0-h-micro`, baseURL the Workers AI OpenAI-compat endpoint built from `CLOUDFLARE_ACCOUNT_ID` (`https://api.cloudflare.com/client/v4/accounts/${id}/ai/v1`) with apiKey `WORKERS_AI_API_TOKEN`, OR the OpenRouter fallback (baseURL `https://openrouter.ai/api/v1`, model `ibm-granite/granite-4.0-h-micro`, apiKey `OPENROUTER_API_KEY`) when Workers AI is absent — pick by which key is present; inject `instrumentedFetch`; (b) runs `correrHarness` for the Granite candidate producing a candidate MetricasModelo; (c) RE-RUNS THE INCUMBENT IN THE SAME BLOCK: instantiate the real `DeepSeekProvider` (apiKey `DEEPSEEK_API_KEY`, its DEFAULT model/baseURL, instrumentedFetch) and call `correrHarness` for it with the SAME `limitePorTarea` cap so candidate and incumbent are measured against the identical frozen sets in one run — this is the pinned incumbent MetricasModelo (do NOT read a stale committed artifact; the same-run DeepSeek IS the baseline); (d) instantiates the real `PhiJudge` (OpenRouter, `microsoft/phi-4-mini-instruct`, `OPENROUTER_API_KEY`, instrumentedFetch) and calls `medirJuezVsHumano(phiJudge)` to produce the real judge-vs-human MetricasJuez; (e) calls `computarVeredicto(candidato, incumbente)` and prints the JSON + a legible table (clone `tablaLegible`) plus the printed `Veredicto` and the `MetricasJuez`. ASSERT ONLY provenance + that the verdict was COMPUTED (every TaskId present in the Veredicto with a valid estado) — NEVER assert that anything approved (a "nada aprueba"/"pending-evidence" run must PASS). Privacy: only `instrumentedFetch` metrics, never prompt/key in logs. Set a generous timeout (600_000) like the baseline. Create `packages/llm-bench/README.md` (or append a section if it exists) documenting: the three exact CANDIDATE env keys and where the operator gets them, that `DEEPSEEK_API_KEY` (already in .env) is ALSO required so the incumbent re-runs in the same block, the exact run command from the checkpoint, that `.env.example` placeholders stay EMPTY, the smoke-cap vs full-run flag (applies to both candidate and incumbent), and the LOCKED interpretation (pending-evidence is valid; es-CL negacion.accuracy hard veto; Ollama-local numbers don't transfer).</action>
  <verify>
    <automated>cd packages/llm-bench && pnpm vitest run src/candidatos.live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Without `LLM_BENCH_LIVE=1` the test SKIPS (describe.skip) — CI never runs it, no network.
    - The `it.skipIf` predicate gates on `DEEPSEEK_API_KEY` (incumbent) AND a candidate key (WORKERS_AI_API_TOKEN or OPENROUTER_API_KEY) — the incumbent is always available when the candidate run fires (no unspecified/throwing incumbent path).
    - The test instantiates the real GraniteProvider + PhiJudge + DeepSeekProvider (same-run incumbent) with instrumentedFetch and env keys; it never hardcodes a key; the incumbent uses the SAME limitePorTarea cap as the candidate.
    - It calls `computarVeredicto(candidato, incumbente)` with the SAME-RUN incumbent and prints a definitive per-task Veredicto; it asserts the verdict was COMPUTED (all TaskIds present), NOT that anything approved.
    - README documents the three candidate keys + the DEEPSEEK_API_KEY incumbent requirement, the run command, empty-placeholder discipline, and the pending-evidence-is-valid + es-CL-veto interpretation.
    - `cd packages/llm-bench && pnpm exec tsc -b` exit 0.
  </acceptance_criteria>
  <done>The single deferred LIVE run exists, is skipped in CI, re-runs the DeepSeek incumbent in the same block (pinned baseline), produces the definitive per-task veredicto when the operator provisions candidate keys and runs it, and the operator README carries the exact command + keys. The phase can close honestly on pending-evidence without it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator secrets → .env (LOCAL only) | Three new candidate keys must land in `.env`, NEVER in the committed `.env.example`; DEEPSEEK_API_KEY already present |
| candidate host endpoint → verdict | The verdict only counts against the SERVED endpoint (Pitfall 9: Ollama-local doesn't transfer) |
| LIVE test → CI | The LIVE test must never run in CI (no network, no quota burn) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-08 | Information Disclosure | candidate keys | mitigate | Keys read from env only; `.env.example` placeholders stay empty; instrumentedFetch logs latency+tokens only, never prompt/key |
| T-107-09 | Denial of Service | quota burn in CI | mitigate | LLM_BENCH_LIVE gate + it.skipIf(all keys: DEEPSEEK + candidate) → CI never touches network; smoke cap by default on both candidate and incumbent runs |
| T-107-10 | Spoofing | non-served / stale-baseline numbers | mitigate | Verdict computed only against the served endpoint AND against a SAME-RUN incumbent (not a stale committed artifact); README states Ollama-local numbers don't transfer; endpoint provenance asserted |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new packages; no install task; no legitimacy checkpoint needed |
</threat_model>

<verification>
- CI: `cd packages/llm-bench && pnpm vitest run` — candidatos.live.test.ts is SKIPPED (describe.skip), suite green, no network.
- `cd packages/llm-bench && pnpm exec tsc -b` exit 0.
- Operator (LIVE, out of CI): `LLM_BENCH_LIVE=1 pnpm --filter @obs/llm-bench exec vitest run src/candidatos.live.test.ts` prints the definitive per-task Veredicto against the same-run DeepSeek incumbent.
- Grep: `DEEPSEEK_API_KEY` appears in the `it.skipIf` predicate of candidatos.live.test.ts (incumbent pinned).
- `.env.example` unchanged by this plan (placeholders from Plan 01 stay empty); env-example-guard.test.ts green.
</verification>

<success_criteria>
The LIVE candidate run + same-run DeepSeek incumbent + PhiJudge-vs-human measurement + definitive per-task VEREDICTO exist as a SINGLE env-gated test, skipped in CI, runnable by the operator with the three documented candidate keys (plus the already-present DEEPSEEK_API_KEY for the pinned incumbent) and the exact command. A "nada aprueba / pending-evidence" outcome is a valid, non-failing result. The agent never loads a secret; the phase closes honestly on the documented handoff if unprovisioned.
</success_criteria>

<output>
Create `.planning/phases/107-bench-p1b-adapters-candidatos-juez-vs-humanos-veredicto-por-/107-03-SUMMARY.md` when done
</output>
</content>
