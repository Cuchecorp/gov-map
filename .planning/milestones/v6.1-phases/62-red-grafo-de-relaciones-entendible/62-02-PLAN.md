---
phase: 62-red-grafo-de-relaciones-entendible
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - app/components/red/red-graph.tsx
  - app/components/red/red-graph.test.tsx
  - app/components/red/nodo-parlamentario.tsx
  - app/app/globals.css
autonomous: true
requirements: [RED-02]
must_haves:
  truths:
    - "En <768px, /red con seed muestra una lista de vecinos con enlaces (nombre + cámara + hecho compartido + procedencia), NO un anillo encogido ni un canvas."
    - "El nodo distingue cámara vs senado por borde institucional (no por posición, no por color de partido, nunca petróleo)."
    - "El anillo radial sigue siendo el render ≥768px; la nota band-aid 'se lee mejor en pantalla ancha' desaparece, reemplazada por la lista real."
  artifacts:
    - path: "app/components/red/red-graph.tsx"
      provides: "vecinos-list <768px (hidden md:block canvas + md:hidden list)"
      contains: "md:hidden"
    - path: "app/components/red/nodo-parlamentario.tsx"
      provides: "clase de borde por cámara (net-nodo--camara / net-nodo--senado)"
      contains: "net-nodo--"
    - path: "app/app/globals.css"
      provides: ".net-nodo--camara/--senado border + .net-vecinos list styles"
      contains: "net-nodo--camara"
  key_links:
    - from: "app/app/globals.css .net-nodo--camara"
      to: "civic-tokens.css --camara-muted"
      via: "border-left-color: var(--camara-muted) (token ya-wrapped, sin doble hsl)"
      pattern: "var\\(--camara"
    - from: "app/components/red/red-graph.tsx vecinos-list"
      to: "/red?seed=<id>"
      via: "Link por vecino con hecho compartido + procedencia (etiquetaHecho/ventanaTexto)"
      pattern: "/red\\?seed="
---

<objective>
Completar RED-02 en las dos dimensiones que faltan tras el core radial: (1) el fallback móvil <768px como una LISTA de vecinos con enlaces (no un anillo encogido ilegible), reemplazando la nota band-aid actual; (2) la distinción de cámara vs senado por borde institucional del nodo (no por posición, F18). Añadir el CSS `.net-*` correspondiente consumiendo los tokens cívicos correctos.

Purpose: A 390px un anillo de 24 nodos es ilegible; la forma honesta y usable es una lista de vecinos con su hecho + fuente. Y la cámara debe ser distinguible sin usar la posición radial (que ya es orden alfabético) ni color de marca política — se usa el borde institucional de `civic-tokens.css`. Cierra la parte "usable en móvil 390px" y "cámara distinguible por borde" de RED-02.

Output: `red-graph.tsx` con canvas `hidden md:block` + lista `md:hidden`; `nodo-parlamentario.tsx` con clase de borde por cámara; `globals.css` con `.net-nodo--camara/--senado` y `.net-vecinos*`; tests de la lista móvil + del borde de cámara.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-CONTEXT.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-PATTERNS.md
@.planning/phases/62-red-grafo-de-relaciones-entendible/62-01-SUMMARY.md

<interfaces>
<!-- Extraído del código shipped — no explorar. -->

From app/components/red/arista-hecho.tsx (reutilizar para las filas de la lista móvil):
```typescript
export function etiquetaHecho(tipo: string, contexto: string | null): string;
export function ventanaTexto(desde: string | null, hasta: string | null): string | null;
// safeExternalHref(enlace) filtra javascript: — usar para el enlace de fuente en cada fila.
// El bloque de procedencia (dl.net-prov: Fuente/Periodo/Registro/Licencia) es el patrón a espejar en cada fila.
```

From app/components/red/nodo-parlamentario.tsx (clase condicional ya existente L54-58):
```tsx
<div className={esSeed ? "net-nodo net-nodo--seed" : "net-nodo"} role="group" aria-label={...}>
```

Tokens cívicos ya-wrapped en app/app/styles/civic-tokens.css (CONSUMIR DIRECTO, sin hsl() alrededor):
  --camara: hsl(213 94% 38%);  --camara-muted: hsl(213 60% 94%);
  --senado: hsl(355 65% 38%);  --senado-muted: hsl(355 40% 94%);

CSS shipped a espejar en app/app/globals.css:
  .net-nodo--seed { border-width: 2px; border-color: hsl(var(--foreground)); }  // marca sobria
  .net-filtros__tipo { min-height: 2.75rem; }  // 44px touch target
  @media (max-width: 47.99rem) { … }  // breakpoint md = 768px
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Borde por cámara en el nodo + CSS institucional (sin petróleo, sin partido)</name>
  <read_first>
    - app/components/red/nodo-parlamentario.tsx (className condicional L54-58, proyección nombre+cámara, NodoParlamentarioData L22-33)
    - app/app/globals.css (.net-nodo L210-220, .net-nodo--seed L226-229, .net-nodo__nombre/camara L231-240)
    - app/app/styles/civic-tokens.css (L8-19 tokens ya-wrapped)
    - app/components/red/red-graph.test.tsx (test de nodo sobrio L131-157 para extender el aserto de borde)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Color "Cámara distinction" + "Anti-insinuación color rule")
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-RESEARCH.md (§Pitfall 2 tokens cívicos ya-wrapped)
  </read_first>
  <action>
    (a) En `nodo-parlamentario.tsx`: extender el `className` del div raíz para APPEND una clase por cámara según `data.camara`: `net-nodo--camara` cuando `camara === "diputados"`, `net-nodo--senado` cuando `camara === "senado"`, nada cuando es null. Componer las clases de forma que se conserve `net-nodo` y `net-nodo--seed` (cuando `esSeed`). NO proyectar partido/foto/RUT/insignia — solo el borde. El `aria-label` no cambia.
    (b) En `globals.css`, junto a `.net-nodo--seed`, añadir `.net-nodo--camara` y `.net-nodo--senado`: un borde-izquierdo de 3px consumiendo el token cívico DIRECTO (ya viene wrapped): `border-left-width: 3px; border-left-color: var(--camara-muted)` y `var(--senado-muted)` respectivamente. NUNCA `hsl(var(--camara-muted))` (doble-wrap = CSS inválido). NUNCA petróleo (`--accent-product`) en un nodo. El cuerpo del nodo se mantiene crema/card. El `.net-nodo--seed` (2px `--foreground`) tiene prioridad visual y coexiste con el borde de cámara.
    (c) En `red-graph.test.tsx`, extender el describe de nodo sobrio: asertar que un nodo de `camara:"diputados"` tiene la clase `net-nodo--camara` y uno de `camara:"senado"` tiene `net-nodo--senado`, y que NINGÚN nodo tiene color de partido ni `accent-product` inline. Mantener los asertos de "sin foto/partido/score" byte-idénticos.
  </action>
  <verify>
    <automated>cd app && pnpm test -- red-graph 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `nodo-parlamentario.tsx`: un nodo `diputados` renderiza className con `net-nodo--camara`; `senado` con `net-nodo--senado`; `null` con ninguna de las dos.
    - `globals.css` contiene `.net-nodo--camara` con `border-left-color: var(--camara-muted)` (SIN `hsl(` alrededor) y `.net-nodo--senado` con `var(--senado-muted)`.
    - Grep: `globals.css` no introduce `hsl(var(--camara` ni `accent-product` en ninguna regla `.net-nodo`.
    - `pnpm test -- red-graph` verde incluyendo el nuevo aserto de borde por cámara.
  </acceptance_criteria>
  <done>El nodo distingue cámara por borde institucional consumiendo el token cívico correcto (sin doble-wrap, sin petróleo, sin partido); tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fallback móvil como lista de vecinos con enlaces + procedencia (reemplaza la nota band-aid)</name>
  <read_first>
    - app/components/red/red-graph.tsx (post-62-01: seedNeighbors/rendered/overflow, canvas mount, la nota móvil band-aid a eliminar; leyenda; estado vacío)
    - app/components/red/arista-hecho.tsx (etiquetaHecho L63-73, ventanaTexto L76-86, bloque net-prov L100-134, safeExternalHref L17/L98)
    - app/components/red/red-graph.test.tsx (tests móvil F-04 a REEMPLAZAR L472-509: nota band-aid; test "el lienzo usa clases token h-96 md:h-120" L474-484 debe seguir pasando sobre el canvas ≥md)
    - app/app/globals.css (@media max-width:47.99rem L197-207, min-height 2.75rem idiom)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-UI-SPEC.md (§Radial Layout Contract "Móvil 390px", §Copywriting "Móvil vecinos list heading", §Page States)
    - .planning/phases/62-red-grafo-de-relaciones-entendible/62-PATTERNS.md (§Provenance-in-DOM + safe href pattern)
  </read_first>
  <behavior>
    - <768px con seed y ≥1 vecino: se renderiza un heading "Vecinos de {nombre}" + lista; cada fila es un Link a /red?seed=<id> con nombre + cámara + hecho compartido (etiquetaHecho) + ventana + enlace a la fuente (safeExternalHref). NO se renderiza el canvas xyflow en móvil.
    - ≥768px: el canvas radial (Task 62-01) se conserva; la lista está oculta.
    - La nota "El grafo se lee mejor en pantalla ancha…" y sus tests desaparecen.
    - Estado vacío (0 aristas) sigue byte-idéntico; la lista no aparece sin aristas.
  </behavior>
  <action>
    En `red-graph.tsx`, dentro del bloque `aristasVisibles.length > 0`:
    (a) ELIMINAR la nota band-aid `<p … md:hidden>El grafo se lee mejor en pantalla ancha…</p>` (L337-340).
    (b) Envolver el `<div className="net-lienzo mt-4 h-96 md:h-120">…ReactFlow…</div>` en un contenedor `hidden md:block` para que el canvas radial SOLO exista ≥768px. Mantener las clases `net-lienzo mt-4 h-96 md:h-120` y todo el `ReactFlow` byte-idéntico (el test L474-484 sigue asertando esas clases — el canvas sigue en el DOM en el árbol, solo oculto por CSS).
    (c) Añadir un sibling `md:hidden` con la lista de vecinos: heading `Vecinos de {seedNombre ?? "este parlamentario"}` (usar `formatNombre` sobre el nombre del nodo seed si está disponible en `nodos`); luego, para cada vecino de `rendered`, una fila `<Link href={`/red?seed=${vecino.id}`}>` con: nombre (`formatNombre`), cámara (label), y — tomando la(s) arista(s) seed↔vecino de `aristasVisibles` — el hecho vía `etiquetaHecho(arista.tipo, arista.contexto)` + `ventanaTexto(arista.desde, arista.hasta)` + un `<a href={safeExternalHref(arista.enlace)} target="_blank" rel="noopener noreferrer">Ver fuente oficial ↗</a>` cuando el href es seguro. Cada fila con `min-height: 2.75rem` (44px). Si un par comparte >1 hecho, listar cada hecho (o el patrón agregado "N hechos en común" — discreción, pero la procedencia de cada hecho debe estar en el DOM, no solo hover). Tras la lista, incluir el mismo control de overflow "Ver N vecinos más" (ya existe en 62-01) de modo que funcione en ambos breakpoints.
    (d) En `globals.css`, añadir un bloque `.net-vecinos` (lista) + `.net-vecinos__fila` (Link, `min-height: 2.75rem`, borde inferior sutil `hsl(var(--border))`) + `.net-vecinos__hecho`/`__prov` reutilizando el tono muted y `font-mono` para las fechas, siguiendo el idiom de `.net-prov`. La procedencia (fuente + ventana + enlace) va SIEMPRE en el DOM de cada fila (trazabilidad rector). Petróleo solo en el enlace de fuente (`--accent-product`), nunca en la fila entera.
    (e) En `red-graph.test.tsx`, ELIMINAR los tests de la nota band-aid ("muestra UNA nota honesta 'mejor en pantalla ancha'" L486-494 y "la nota NO aparece en el estado vacío" L496-501) y AÑADIR: (i) test "lista de vecinos móvil": con seed + 2 vecinos, existe un contenedor `md:hidden` con heading `/Vecinos de/i` y filas que son Links a `/red?seed=` con el hecho (`/Ambos recibieron audiencia/i`) y un enlace `Ver fuente oficial`; (ii) test "el canvas queda en un wrapper hidden md:block"; (iii) mantener el test "el lienzo usa clases token h-96 md:h-120" y "los filtros … siguen intactos" verdes.
  </action>
  <verify>
    <automated>cd app && pnpm test -- red-graph && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `red-graph.tsx` ya NO contiene el string "se lee mejor en pantalla ancha"; contiene un contenedor `md:hidden` con heading `Vecinos de` y filas Link a `/red?seed=`.
    - Cada fila de la lista móvil contiene el hecho (`etiquetaHecho`), la ventana temporal y — cuando el enlace es seguro — un `<a target="_blank">Ver fuente oficial`.
    - El canvas radial vive en un wrapper `hidden md:block`; el test de clases `h-96 md:h-120` sigue verde.
    - `globals.css` contiene reglas `.net-vecinos` con filas de `min-height: 2.75rem` y enlace de fuente en `--accent-product`; sin petróleo en la fila completa.
    - `red-graph.test.tsx` ya no referencia "mejor en pantalla ancha"; los tests de lista móvil pasan.
    - `pnpm test -- red-graph` y `pnpm typecheck` verdes.
  </acceptance_criteria>
  <done>En móvil <768px /red con seed muestra una lista honesta de vecinos con enlaces + hecho + procedencia; el anillo radial se conserva ≥768px; la nota band-aid desapareció; suite + typecheck verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge `enlace` → fila móvil link | `safeExternalHref` filtra esquemas peligrosos (`javascript:`) también en las filas de la lista móvil. |
| Vecino id → href /red?seed= | ids PII-safe del subgrafo; re-entran por el gate + `PARLAMENTARIO_ID_RE` de page.tsx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03 | Tampering/XSS | enlace de fuente en fila móvil | mitigate | Usar `safeExternalHref(enlace)` en cada fila; sin href cuando el esquema es peligroso (espejo del comportamiento de arista-hecho.tsx, ya testeado). |
| T-62-04 | Information Disclosure | borde de cámara en el nodo | accept | El borde usa el token institucional cívico (identidad de cámara, NO partido); no expone dato vedado — la cámara ya se muestra como label textual. |
</threat_model>

<verification>
- `pnpm test -- red-graph` verde (borde de cámara, lista móvil, canvas hidden md:block, filtros intactos, más los tests de 62-01).
- `pnpm typecheck` verde.
- Grep: `red-graph.tsx` sin "mejor en pantalla ancha"; `globals.css` sin `hsl(var(--camara`; con `.net-nodo--camara`, `.net-nodo--senado`, `.net-vecinos`.
</verification>

<success_criteria>
En 390px /red con seed es usable: lista de vecinos con enlaces + hecho + fuente, sin anillo encogido; cámara distinguible por borde institucional (no posición, no partido, no petróleo); anillo radial intacto en desktop. Suite + typecheck verdes.
</success_criteria>

<output>
Create `.planning/phases/62-red-grafo-de-relaciones-entendible/62-02-SUMMARY.md` when done
</output>
