---
phase: 63-busq-b-squeda-de-proyectos-completa
plan: 04
type: execute
wave: 3
depends_on: [63-03]
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/evaluate.ts
  - packages/freshness/src/evaluate.test.ts
  - app/lib/coverage.ts
  - app/app/buscar/page.tsx
  - app/app/buscar/coverage.test.tsx
autonomous: false
requirements: [BUSQ-03]
nyquist_compliant: true

must_haves:
  truths:
    - "/buscar declara 'Busca sobre N proyectos de ley (alcance X)' con N REAL desde la DB (count(proyecto_embedding)), nunca hardcodeado"
    - "El operador ve cobertura N/M por señal (proyecto/ficha/idea/embedding) vía pnpm freshness sin bucear"
    - "El contador de /buscar es server-only (import 'server-only'), la key nunca al cliente, no lee tablas PII"
    - "El banner de /buscar está desplegado en producción vía runbook 61-02"
  artifacts:
    - path: "app/lib/coverage.ts"
      provides: "contarCoberturaBusqueda() server-only cacheado"
      contains: "server-only"
    - path: "app/app/buscar/page.tsx"
      provides: "banner de cobertura bajo el SearchBox"
      contains: "proyectos de ley"
    - path: "packages/freshness/src/catalog.ts"
      provides: "señal de cobertura de búsqueda N/M"
  key_links:
    - from: "app/app/buscar/page.tsx"
      to: "app/lib/coverage.ts"
      via: "await contarCoberturaBusqueda()"
      pattern: "contarCoberturaBusqueda"
    - from: "app/lib/coverage.ts"
      to: "proyecto_embedding"
      via: "count exact head"
      pattern: "count.*exact"
---

<objective>
Cerrar BUSQ-03 (honestidad de cobertura): `/buscar` declara sobre cuántos proyectos busca con N REAL desde la DB, y el operador ve la cobertura N/M por señal vía `pnpm freshness`. Desplegar el banner con el runbook 61-02.

Purpose: El usuario nunca debe creer que buscó sobre todo el corpus si no fue así. Tras el backfill (P03), N refleja el corpus ampliado real. Sin declaración, la búsqueda miente por omisión.
Output: `app/lib/coverage.ts`, banner en `app/app/buscar/page.tsx`, señal de cobertura en freshness, deploy.

**Dependencia:** Este plan va DESPUÉS de P03 para que el N declarado refleje el corpus ampliado (no un número intermedio).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md
@scripts/verify-cobertura.sql

<interfaces>
From app/lib/buscar.ts (línea 1): `import "server-only"` (data layer server-only).
From app/lib/supabase.ts (líneas 34-53): `createServerSupabase()` (service_role, bypassa RLS — usar SOLO tablas public-read).
From app/app/buscar/page.tsx (líneas 29-58): Server Component `BuscarPage`; `<SearchBox>` en líneas 41-45 (punto de inserción del banner).
From packages/freshness/src/catalog.ts (líneas 36-85): array `CATALOG`.
From packages/freshness/src/evaluate.ts (líneas 29-70): `evaluate` (pura, sin I/O).
From packages/freshness/src/query-runner.ts (líneas 39-98): `psql(dbUrl, sql)` con `PGCLIENTENCODING=UTF8`, degrada a "" en fallo.

CONSTRAINT app/AGENTS.md: Next.js aquí tiene breaking changes vs training data — leer `node_modules/next/dist/docs/` antes de escribir App Router / semántica de `revalidate`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coverage.ts server-only + test del banner</name>
  <files>app/lib/coverage.ts, app/app/buscar/coverage.test.tsx</files>
  <read_first>
    - app/lib/buscar.ts (líneas 1-22, 192-209: import "server-only" + count idiom)
    - app/lib/supabase.ts (líneas 34-53: createServerSupabase)
    - app/app/buscar/page.tsx (líneas 29-58: Server Component, punto de inserción)
    - app/AGENTS.md (constraint Next.js) + node_modules/next/dist/docs/ (semántica revalidate)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (bloque coverage.ts)
  </read_first>
  <behavior>
    - Test 1: el banner renderiza "Busca sobre {N} proyectos de ley" con N mockeado desde el count.
    - Test 2: N viene del count (mock), NO de una constante hardcodeada.
  </behavior>
  <action>Crear `app/lib/coverage.ts`: `import "server-only"`; `contarCoberturaBusqueda(): Promise<number>` usa `createServerSupabase()` → `.from("proyecto_embedding").select("*", { count: "exact", head: true })` → retorna `count ?? 0`. Cachear con la semántica de `revalidate` de Next.js (~1h) — confirmar la API vigente en `node_modules/next/dist/docs/` antes de escribir (app/AGENTS.md). NUNCA leer una tabla PII (proyecto_embedding es public-read; service_role bypassa RLS pero esta tabla es segura). El "alcance X" del copy se deriva de `63-ALCANCE-HISTORICO.md` (constante documentada, p.ej. "legislatura 2022–2026").

  Crear `app/app/buscar/coverage.test.tsx` (RTL) que mockea `contarCoberturaBusqueda` y verifica que el banner muestra el N mockeado y que no hay número hardcodeado. Tests en RED.</action>
  <verify>
    <automated>pnpm --filter ./app exec vitest run app/buscar/coverage.test.tsx</automated>
  </verify>
  <done>coverage.ts server-only implementado; test del banner en verde tras Task 2 wiring (o RED aquí si el banner aún no está montado).</done>
  <acceptance_criteria>`grep -n "server-only" app/lib/coverage.ts` presente; `grep -n "count.*exact" app/lib/coverage.ts` presente.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Banner de cobertura en /buscar + señal en freshness</name>
  <files>app/app/buscar/page.tsx, packages/freshness/src/catalog.ts, packages/freshness/src/evaluate.ts, packages/freshness/src/evaluate.test.ts</files>
  <read_first>
    - app/app/buscar/page.tsx (líneas 29-58: BuscarPage, SearchBox líneas 41-45)
    - app/lib/coverage.ts (de Task 1)
    - packages/freshness/src/catalog.ts (líneas 36-85), evaluate.ts (líneas 29-70), query-runner.ts (líneas 39-98)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-PATTERNS.md (bloque freshness)
    - scripts/verify-cobertura.sql (P01: fuente única de los conteos)
  </read_first>
  <action>En `app/app/buscar/page.tsx` (Server Component `BuscarPage`): `const n = await contarCoberturaBusqueda()` y agregar un `<p>` bajo `<SearchBox>` con "Busca sobre {n} proyectos de ley (alcance {ALCANCE})". `ALCANCE` = string documentado desde `63-ALCANCE-HISTORICO.md`. NO hardcodear n.

  En freshness: agregar la señal de cobertura de búsqueda N/M (proyecto / ficha / idea / embedding). Elegir lo más barato per RESEARCH: una sección/fila de cobertura (no un `FuenteConfig` de staleness completo). Mantener `evaluate` puro (sin I/O); el I/O (psql read-only con PGCLIENTENCODING=UTF8) vive en query-runner reusando `scripts/verify-cobertura.sql` como fuente única. Ampliar `evaluate.test.ts` para la nueva señal (dado counts, evalúa N/M correctamente). Degradar a "" en fallo de psql (patrón query-runner).</action>
  <verify>
    <automated>pnpm --filter @obs/freshness exec vitest run && pnpm --filter ./app exec vitest run app/buscar/coverage.test.tsx</automated>
  </verify>
  <done>Banner montado leyendo N real; freshness reporta cobertura N/M; tests verdes.</done>
  <acceptance_criteria>`grep -n "contarCoberturaBusqueda" app/app/buscar/page.tsx` presente; `grep -ni "cobertura" packages/freshness/src/catalog.ts` presente; suites freshness + app coverage verdes.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy del banner via runbook 61-02</name>
  <what-built>Banner de cobertura en /buscar (N real desde DB) + señal de cobertura en pnpm freshness. Backfill de P03 ya poblado, así que N refleja el corpus ampliado.</what-built>
  <how-to-verify>
    1. Ejecutar el runbook de deploy 61-02 (build OpenNext en docker Linux + wrangler local; ver 62-03/61-02). El backfill de datos ya es visible sin redeploy; SOLO el banner UI necesita deploy.
    2. Visitar la URL de producción de /buscar y confirmar que muestra "Busca sobre N proyectos de ley (alcance ...)" con N coincidente con `count(proyecto_embedding)` (verify-cobertura.sql).
    3. Correr `pnpm freshness` y confirmar la sección de cobertura N/M por señal.
    4. Confirmar que la key NO aparece en el bundle cliente (server-only respetado).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si el banner muestra el N real en producción y freshness reporta cobertura, o describe el problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador → /buscar (SSR) | conteo calculado server-side; key nunca al cliente |
| /buscar → Supabase (service_role) | count sobre tabla public-read; bypassa RLS pero tabla segura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-12 | Information Disclosure | key Supabase/Gemini filtrada al cliente | mitigate | `import "server-only"` en coverage.ts; keys de process.env sin prefijo público (patrón buscar.ts) |
| T-63-13 | Information Disclosure | count sobre tabla PII vía service_role | mitigate | Solo `count(proyecto_embedding)` (public-read); NUNCA una tabla con PII (V4) |
| T-63-14 | Spoofing/Integrity | N hardcodeado que miente | mitigate | N desde `count(*)` con revalidate; test verifica no-hardcode (Task 1) |
</threat_model>

<verification>
- `/buscar` en PROD muestra N == count(proyecto_embedding) (verify-cobertura.sql).
- `pnpm freshness` reporta cobertura N/M por señal.
- coverage.ts es server-only; no lee tablas PII; N no hardcodeado.
- Suites app + freshness verdes.
</verification>

<success_criteria>
- Criterio 4: /buscar declara la cobertura; operador ve N/M por señal sin bucear.
- Banner desplegado en producción vía runbook 61-02.
</success_criteria>

<output>
Create `.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-04-SUMMARY.md` when done
</output>
