---
phase: 63-busq-b-squeda-de-proyectos-completa
plan: 03
type: execute
wave: 2
depends_on: [63-01, 63-02]
files_modified:
  - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md
  - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-COBERTURA-REPORTE.md
autonomous: false
requirements: [BUSQ-01, BUSQ-02]
nyquist_compliant: true

must_haves:
  truths:
    - "El alcance histórico está DECIDIDO tras enumeración live y DOCUMENTADO con su porqué (volumen por año)"
    - "Los boletines históricos entran vía run-tramitacion-prod-cli (R2 Etapa-1 primero), idempotente/reanudable"
    - "El seed cierra el gap: count(proyecto)==count(proyecto_ficha) tras el seed"
    - "pipeline-cli procesa los pendientes; los imposibles quedan con estado='error'+causa (techo honesto)"
    - "Los 8 embeddings v1 stale se re-embeben; el reporte final iguala counts o explica la diferencia por boletín"
    - "El cron leyes-weekly sigue acotado (--limite 80) tras ampliar el corpus, sin re-backfill"
  artifacts:
    - path: ".planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md"
      provides: "decisión de alcance + volumen por año + porqué"
    - path: ".planning/phases/63-busq-b-squeda-de-proyectos-completa/63-COBERTURA-REPORTE.md"
      provides: "reporte final de cobertura + techo honesto por boletín/causa"
  key_links:
    - from: "run-tramitacion-prod-cli --boletines"
      to: "R2 (Etapa 1) + proyecto"
      via: "ingesta dos-etapas"
      pattern: "boletines"
    - from: "seed-fichas-cli + pipeline-cli"
      to: "proyecto_ficha + proyecto_embedding"
      via: "seed → pendientes → extracción → embed"
      pattern: "pendiente"
---

<objective>
Ejecutar el backfill LOCAL end-to-end (BUSQ-01 + BUSQ-02) usando el código de P01/P02: decidir el alcance histórico tras enumeración live, ingerir tramitación (R2 primero), sembrar fichas, correr el pipeline reanudable, re-embeber los stale, y verificar la igualdad de counts o el techo honesto por causa. Verificar que el cron leyes-weekly sigue acotado (criterio 5).

Purpose: Este es el trabajo de datos que hace la búsqueda COMPLETA. Todo LOCAL, rate-limit 2-3s, reanudable. Sin este plan, el código de P01/P02 no cambia la cobertura real.
Output: `63-ALCANCE-HISTORICO.md` (decisión + porqué), `63-COBERTURA-REPORTE.md` (counts finales + techo honesto), y la DB de PROD poblada.

**NOTA CRÍTICA:** El backfill corre LOCAL en la máquina del operador con `.env`. Las corridas largas son reanudables (`proyecto_ficha.estado`); las tareas incluyen checkpoints humanos de progreso porque son corridas contra fuentes vivas + PROD que pueden tardar horas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-01-PLAN.md
@.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-02-PLAN.md
@scripts/verify-cobertura.sql

<interfaces>
CLIs disponibles (de P01/P02 + existentes):
- `pnpm --filter @obs/tramitacion exec tsx src/run-enumerar-historico-cli.ts --desde 2022 --hasta 2026` → lista de boletines (P02)
- `pnpm --filter @obs/tramitacion exec tsx src/run-tramitacion-prod-cli.ts --boletines <lista>` → proyecto+autores+R2 (existente; es el MISMO entrypoint que corre el cron)
- `pnpm --filter @obs/fichas exec tsx src/seed-fichas-cli.ts` → seed pendientes (P01)
- `pnpm --filter @obs/fichas exec tsx src/pipeline-cli.ts --limite N` → backfill fichas+embeddings reanudable
- `pnpm --filter @obs/fichas exec tsx src/pipeline-cli.ts --reembed` → re-embebe los v1 stale
- `psql "$SUPABASE_DB_URL" -f scripts/verify-cobertura.sql` (PGCLIENTENCODING=UTF8)

Techo honesto conocido (NO reintentar al LLM): 8 RUT-bloqueados (permanente), 1 PDF escaneado, 3 schema-fail (revisables 1 vez). Cron entrypoint verificado = `run-tramitacion-prod-cli.ts` (leyes-weekly.yml).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerar live y DECIDIR el alcance histórico</name>
  <files>.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md</files>
  <read_first>
    - packages/tramitacion/src/run-enumerar-historico-cli.ts (de P02)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md (Open Question 1: volumen por año; A4: alcance manejable)
  </read_first>
  <action>Correr `run-enumerar-historico-cli.ts` año por año sobre el rango candidato (default 2022→2026, ampliable hacia atrás si es barato). Contar boletines por año. Decidir el alcance FINAL en función del volumen real vs el costo de backfill (1 PDF+DeepSeek+Gemini por boletín a 2-3s ≈ minutos-horas). Documentar en `63-ALCANCE-HISTORICO.md`: volumen por año, alcance elegido, y el PORQUÉ (criterio 2). Si un año devuelve miles, acotar y documentar la razón; si es barato, extender. Comparar el conteo de un año conocido contra la expectativa (A1) para confirmar que mociones+mensajes cubren todo.</action>
  <verify>
    <automated>test -f .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md</automated>
  </verify>
  <done>Alcance decidido y documentado con volumen por año y porqué; lista de boletines históricos generada.</done>
  <acceptance_criteria>`grep -ni "porqué\|porque\|volumen\|alcance" 63-ALCANCE-HISTORICO.md` presente con conteos concretos por año.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ingerir tramitación histórica (R2 primero) + seed fichas</name>
  <files>.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-COBERTURA-REPORTE.md</files>
  <read_first>
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-ALCANCE-HISTORICO.md (lista de boletines de Task 1)
    - packages/tramitacion/src/run-tramitacion-prod-cli.ts (Etapa 1 R2 + upsertProyecto; idempotente)
    - CLAUDE.md (Conventions LOCKED: dos etapas, R2 antes de parsear, rate-limit 2-3s, backfill LOCAL)
  </read_first>
  <action>Correr `run-tramitacion-prod-cli.ts --boletines <lista histórica>` en LOTES acotados (para reanudabilidad y no exceder ventanas) — la Etapa-1 R2 content-addressed es automática (If-None-Match; existed=true = skip idempotente). Los boletines ya presentes se saltan. Tras ingerir tramitación, correr `seed-fichas-cli.ts` (P01) para crear filas `proyecto_ficha` estado='pendiente' de TODO proyecto sin ficha (cierra el gap 82 + los nuevos históricos). Verificar con `psql -f scripts/verify-cobertura.sql` que `count(proyecto)==count(proyecto_ficha)` tras el seed. Registrar conteos intermedios en `63-COBERTURA-REPORTE.md`.</action>
  <verify>
    <automated>psql "$SUPABASE_DB_URL" -tAc "select (select count(*) from proyecto) = (select count(*) from proyecto_ficha)"</automated>
  </verify>
  <done>Tramitación histórica ingerida vía R2; count(proyecto)==count(proyecto_ficha) tras el seed.</done>
  <acceptance_criteria>El query de igualdad retorna `t`; el reporte registra los conteos proyecto/ficha antes y después del seed.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint de progreso — backfill de tramitación + seed</name>
  <what-built>Ingesta histórica (R2 primero) + seed de fichas pendientes ejecutados en lotes LOCAL.</what-built>
  <how-to-verify>
    1. Revisar `63-COBERTURA-REPORTE.md`: counts proyecto/ficha coinciden (gap cerrado).
    2. Confirmar en logs que la Etapa-1 R2 se ejecutó (snapshots o existed=true), rate-limit respetado (sin ráfagas), sin 403 sostenido del WAF.
    3. Si una fuente bloqueó (WAF), confirmar que hubo backoff y techo honesto — NO martillar.
  </how-to-verify>
  <resume-signal>Escribe "continuar" para lanzar el pipeline de fichas+embeddings, o describe el problema.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Pipeline fichas+embeddings + reembed + reporte de techo honesto</name>
  <files>.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-COBERTURA-REPORTE.md</files>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts (flags --limite/--reembed; reanudable por estado='pendiente')
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md (Pitfalls 3/4/5: PDF escaneado, RUT-bloqueado, v1 stale)
    - scripts/verify-cobertura.sql
  </read_first>
  <action>Correr `pipeline-cli.ts --limite N` en lotes reanudables (re-correr procesa solo los 'pendiente' restantes) hasta agotar la cola. Luego `pipeline-cli.ts --reembed` para re-embeber los 8 `v1` stale (title-only). Los 3 schema-fail se reintentan UNA vez; si vuelven a fallar, techo honesto. NUNCA reintentar los 8 RUT-bloqueados enviándolos al LLM (`assertNoRutInLlmInput` LOCKED — techo permanente). Al terminar, correr `verify-cobertura.sql` y escribir el reporte final en `63-COBERTURA-REPORTE.md`: `count(proyecto)` vs `count(proyecto_ficha)` vs `count(proyecto_embedding)` vs `count(idea)`, y la LISTA por boletín de los que no cerraron con su causa (RUT / PDF escaneado / schema-fail / fuente caída). La meta es igualdad de counts O diferencia 100% explicada por causa (criterio 1 + 3). Verificar `select embedding_version, count(*)` no muestra `v1` residual.</action>
  <verify>
    <automated>psql "$SUPABASE_DB_URL" -tAc "select embedding_version, count(*) from proyecto_embedding group by 1"</automated>
  </verify>
  <done>Fichas+embeddings al máximo alcanzable; v1 stale re-embebido; reporte final con techo honesto por boletín/causa.</done>
  <acceptance_criteria>`grep -ni "causa\|RUT\|escaneado\|techo" 63-COBERTURA-REPORTE.md` lista los no-cerrados; el query de versiones no muestra `v1` residual (solo versiones reembed).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Verificar que el cron leyes-weekly sigue acotado (criterio 5)</name>
  <files>.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-COBERTURA-REPORTE.md</files>
  <read_first>
    - .github/workflows/leyes-weekly.yml (entrypoint = run-tramitacion-prod-cli.ts; DEFAULT_LIMITE 80; agenda primero)
    - packages/tramitacion/src/run-tramitacion-prod-cli.ts (boletinesARefrescar + DEFAULT_LIMITE)
    - .planning/phases/63-busq-b-squeda-de-proyectos-completa/63-RESEARCH.md (Pitfall 6, A5)
  </read_first>
  <action>Confirmar (leyendo el código, no re-corriendo el cron) que `run-tramitacion-prod-cli.ts` sigue recortando a `DEFAULT_LIMITE=80` y que el orden (agenda/novedades primero) garantiza que lo reciente se refresca aunque el corpus haya crecido. Verificar que el entrypoint del YAML (`run-tramitacion-prod-cli.ts`) es el correcto (MEMORY gotcha "dos entrypoints CLI") — hay AHORA un segundo entrypoint nuevo (`run-enumerar-historico-cli.ts` de P02) que NO debe estar en ningún YAML. Documentar en `63-COBERTURA-REPORTE.md`: el cron NO re-backfilla el histórico (novedades acotadas), el histórico fue one-shot LOCAL. Opcional: un `workflow_dispatch` manual acotado (--limite pequeño) como sanity, NO un run completo.</action>
  <verify>
    <automated>grep -c "run-enumerar-historico-cli" .github/workflows/*.yml; grep -c "run-tramitacion-prod-cli" .github/workflows/leyes-weekly.yml</automated>
  </verify>
  <done>Confirmado: cron usa run-tramitacion-prod-cli con --limite 80, el enumerador histórico NO está en ningún YAML, criterio 5 documentado.</done>
  <acceptance_criteria>El primer grep (enumerar en YAML) da 0; el segundo (tramitacion en leyes-weekly) da >=1; reporte documenta que el cron no re-backfilla.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill LOCAL → fuentes gov (Cámara/Senado) | fetch saliente masivo; rate-limit obligatorio |
| Texto fuente → DeepSeek/Gemini | contenido no confiable; guard RUT antes del LLM |
| CLI → PROD (service key) | escritura idempotente con service key desde .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Information Disclosure | RUT/PII enviado al LLM | mitigate | `assertNoRutInLlmInput` LOCKED (fail-closed); 8 RUT-bloqueados = techo permanente, NUNCA reintentar al LLM |
| T-63-09 | Denial of Service | ráfagas al servidor gob durante backfill masivo | mitigate | rate-limit 2-3s/host + UA + robots (@obs/ingest); lotes acotados; backoff ante 429/5xx; jamás martillar |
| T-63-10 | Tampering | SSRF via link_mensaje_mocion (PDF) | mitigate | `assertAllowedUrl` deny-by-default en texto-fuente (ya cableado) |
| T-63-11 | Information Disclosure | fabricar idea matriz donde la fuente no la da | accept→mitigate | degradación honesta (idea null / estado='error'+causa); NUNCA fabricar (LOCKED) |
</threat_model>

<verification>
- `verify-cobertura.sql`: count(proyecto)==count(proyecto_ficha)==count(proyecto_embedding) o diferencia explicada por causa en `63-COBERTURA-REPORTE.md`.
- `select embedding_version` sin `v1` residual.
- Cron: enumerador histórico ausente de YAMLs; leyes-weekly usa run-tramitacion-prod-cli con --limite 80.
- Alcance histórico documentado con porqué.
</verification>

<success_criteria>
- Criterio 1: counts iguales o diferencia 100% explicada por causa/boletín.
- Criterio 2: alcance histórico documentado, ingerido vía R2, buscable.
- Criterio 3: ideas al máximo; imposibles con estado honesto + causa.
- Criterio 5: cron acotado, sin re-backfill.
</success_criteria>

<output>
Create `.planning/phases/63-busq-b-squeda-de-proyectos-completa/63-03-SUMMARY.md` when done
</output>
