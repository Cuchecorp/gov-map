---
phase: 72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql
  - supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql
autonomous: true
requirements: [MONEY-03]

must_haves:
  truths:
    - "cruce_senal admite el token tipo_senal='lobby_sector_aporte' (el CHECK fue ampliado; un insert directo con ese token NO viola 23514)"
    - "cruces.materializar_cruces() conserva la rama lobby_sector byte-idéntica a 0039:91-120: tras el FULL REBUILD la señal lobby_sector sigue produciendo >=5 parlamentarios distintos con datos sembrados"
    - "La nueva rama lobby_sector_aporte se une por RUT de la EMPRESA contratista (contrato.rut_proveedor → contratista.rut_proveedor → sector de esa empresa), NO por parlamentario_id: es un cruce de ENTIDAD-COMPARTIDA correcto-por-construcción, no una yuxtaposición persona-nivel"
    - "La arista empresa-RUT→sector NO existe en el schema hoy (ni contratista ni contrato ni entidad_tercero tienen sector_id): la rama la modela como una relación honesta-vacía y por tanto rinde 0 filas correcto-por-construcción, con un comentario SQL explícito que marca <company-rut → sector> como la sustancia diferida de MONEY-03"
    - "La rama NUNCA une dinero y lobby por parlamentario_id (esa yuxtaposición persona-nivel es la 'máquina de sospechas' diferida — RECHAZADA)"
    - "La evidencia jsonb de lobby_sector_aporte es PII-safe: no contiene el token rut ni donante_id (solo codigo_orden/monto_verbatim/enlace/fecha de la fila de hecho)"
    - "El cuerpo de cruces.materializar_cruces() NO contiene el token literal rut ni partido bajo el aserto \\y(partido|rut)\\y de 0039 (rut_proveedor NO trip el guard por el _ tras rut; ningún rut crudo se referencia)"
    - "Ninguna palabra causal ('financió su voto','a cambio de','por eso votó') aparece en el token, el SQL de la rama, ni en la evidencia"
  artifacts:
    - path: "supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql"
      provides: "Migración aditiva: ALTER del CHECK a ('lobby_sector','lobby_sector_aporte') + CREATE OR REPLACE de cruces.materializar_cruces() con la rama lobby existente verbatim + la nueva rama aporte (join empresa-RUT→sector contra una arista AUSENTE = 0 filas honestas)"
      contains: "lobby_sector_aporte"
    - path: "supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql"
      provides: "pgTAP espejo de 0039: seed (contrato confirmado + contratista con RUT + caso no_confirmado) → materializar → aserciones CHECK/lobby-preservado/empty-honest/PII-safe/cuerpo-no-PII"
      contains: "lobby_sector_aporte"
  key_links:
    - from: "supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql"
      to: "public.cruce_senal (CHECK constraint cruce_senal_tipo_senal_check)"
      via: "alter table drop/add constraint"
      pattern: "check \\(tipo_senal in \\('lobby_sector', ?'lobby_sector_aporte'\\)\\)"
    - from: "cruces.materializar_cruces() rama aporte"
      to: "public.contrato.rut_proveedor ⨝ public.contratista.rut_proveedor ⨝ <arista company-rut→sector AUSENTE>"
      via: "join por RUT de la empresa contratista (entidad-compartida), NUNCA por parlamentario_id"
      pattern: "'lobby_sector_aporte'"
---

<objective>
Escribir la migración aditiva `0052` que amplía el materializador de cruces con la señal `lobby_sector_aporte` como STUB ESTRUCTURAL correcto-por-construcción — un cruce dinero×sector keyed por el RUT de la EMPRESA contratista (entidad compartida), que hoy rinde 0 filas porque la arista `<company-rut → sector>` no existe todavía en el schema — más su suite pgTAP. Todo offline y autónomo: se escribe y se valida la migración contra un fixture; la APLICACIÓN al remoto PROD es el Plan 02 (operador, deferido).

Purpose: MONEY-03 — los cruces dinero × sector aparecen en `cruce_senal` como CONTEO FACTUAL con evidencia jsonb (enlaces de fuente), nunca un score, nunca "financió su voto", y NUNCA una yuxtaposición persona-nivel (contratos del parlamentario × sus propios sectores de lobby = la "máquina de sospechas" diferida, RECHAZADA). La señal nace VACÍA honesta hasta que exista una arista real de entidad-compartida empresa→sector.

Output:
- `supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql`
- `supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-RESEARCH.md
@.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-VALIDATION.md

<design_decisions>
DISEÑO CORREGIDO (operador 2026-07-14 — construir EXACTAMENTE esto; el join persona-nivel del plan previo está RECHAZADO):

- El puente empresa→dinero A NIVEL DE COMPAÑÍA NO es construible hoy: no hay arista empresa→sector (`lobby_contraparte` no tiene RUT; ninguna tabla de dinero tiene `sector_id`; `contratista`/`contrato`/`entidad_tercero` NO tienen columna de sector — VERIFICADO leyendo 0023/0025/0034/0038). El operador eligió un STUB ESTRUCTURAL, correcto-por-construcción, VACÍO hasta que exista una arista real de entidad-compartida.

- La migración 0052 es ADITIVA: ampliar `check (tipo_senal in ('lobby_sector'))` → `('lobby_sector','lobby_sector_aporte')`, y añadir una NUEVA rama del insert para `lobby_sector_aporte`.

- La rama nueva DEBE keyearse en un join GENUINO de entidad-compartida `company-RUT → sector`: el RUT de la empresa contratista (`contrato.rut_proveedor` → `contratista.rut_proveedor`) → el sector DE ESA EMPRESA. Como no existe un mapeo empresa→sector en el schema hoy, la rama se une contra esa arista (ausente/vacía) y por tanto rinde 0 filas — correcto-por-construcción, NO un fake ni una yuxtaposición persona-nivel.

- PROHIBIDO puentear dinero y lobby por `parlamentario_id` (esa yuxtaposición persona-nivel es la "máquina de sospechas" diferida — RECHAZADA). El plan previo (72-01) unía `contrato` del parlamentario × sectores de las lobby-contrapartes DEL MISMO parlamentario por `parlamentario_id` común: eso es exactamente lo RECHAZADO. NO reproducirlo.

- Concretamente: la rama une `contrato`/`contratista` (la EMPRESA por RUT-exacto; `parlamentario_id` confirmado de Phase 70 para restringir el universo a contratos ligados) a una fuente empresa-sector keyed por el RUT de la empresa (p.ej. un `sector` de la entidad-empresa/tercero). Como esa columna/tabla NO existe todavía, el join produce nada. Añadir un comentario SQL explícito que marque la arista faltante `<company-rut → sector>` como la sustancia diferida de MONEY-03.

- Preservar el insert `lobby_sector` existente VERBATIM (el FULL REBUILD borra todas las filas primero — dropearlo borraría la señal de lobby). El cron + el RPC `cruces_de_parlamentario` (0040) heredan automáticamente — NO tocarlos.

- SIN token `rut`/`partido` en el CUERPO de la función bajo el aserto pgTAP de 0039 (`\y(partido|rut)\y`). VERIFICADO: `rut_proveedor` NO trip el guard porque `_` es carácter de palabra → no hay boundary tras `rut` (`\yrut\y` no matchea `rut_proveedor`). Referenciar el RUT de la empresa SOLO vía `rut_proveedor` como llave de join a la tabla de entidad; NUNCA un `rut` crudo/literal ni `entidad_tercero.rut` que trip el guard. Verificar contra el pgTAP existente.

- Evidencia jsonb PII-safe: solo enlaces de fuente + campos públicos de la fila de hecho (codigo_orden/monto_verbatim/enlace/fecha); NUNCA rut/donante_id.

- Sin wording causal en ninguna parte ("financió su voto"/"a cambio de"/"por eso votó" PROHIBIDOS).

- Gate MONEY OFF. La aplicación de 0052 a PROD = operador (db push --db-url), deferida (Plan 02, autonomous:false) — necesita RUT-01 + backfill ChileCompra de todos modos.

- SERVEL (aportes por nombre, sin RUT) queda FUERA de esta señal: la señal es empresa-RUT→sector; SERVEL no aporta RUT (fail-closed "RUT presente").

RINDE 0 FILAS HOY por DOS razones honestas independientes: (a) la arista empresa→sector no existe (razón estructural, correcto-por-construcción), y (b) RUT-01 a 0% + backfill ChileCompra pendiente (razón de datos). Eso es CORRECTO por requisito, no un bug.
</design_decisions>

<interfaces>
<!-- Contratos extraídos del schema aplicado (0023/0025/0038/0039). El executor usa estos directamente, sin explorar. -->

Constraint a ampliar (0039:45-61) — CHECK inline SIN nombre → Postgres lo nombra `cruce_senal_tipo_senal_check`:
- cruce_senal(id, parlamentario_id text not null → parlamentario(id), sector_id text not null → sector(codigo),
  tipo_senal text not null check (tipo_senal in ('lobby_sector')),  -- AMPLIAR a ('lobby_sector','lobby_sector_aporte')
  conteo int, evidencia jsonb, dataset text, origen text, fecha_captura timestamptz, enlace text,
  unique (parlamentario_id, sector_id, tipo_senal))

Función a re-emitir (0039:81-122) — el cuerpo actual: `delete from public.cruce_senal;` (ÚNICO delete, FULL REBUILD) seguido de UN insert `select ... 'lobby_sector' ...` desde `lobby_audiencia a join lobby_contraparte c on c.identificador = a.identificador where a.estado_vinculo='confirmado' and a.parlamentario_id is not null and c.sector_id is not null group by a.parlamentario_id, c.sector_id`. Esa rama lobby (0039:91-120) se CONSERVA VERBATIM; la nueva rama va a continuación, DENTRO de la misma función. La función es `security definer set search_path = ''` → todos los nombres calificados con schema (public./cruces.).

Columnas de `contrato` (0023 + 0025) — la EMPRESA y su hecho:
- contrato(fuente_id, fecha_corte, parlamentario_id text → parlamentario, mencion_proveedor,
  estado_vinculo check ('confirmado'|'no_confirmado'|'cuarentena'),
  codigo_orden, proveedor_nombre, tipo_persona, organismo, nombre_orden,
  monto text VERBATIM (hoy null — NUNCA sumar/castear), fecha_oc date, origen text, fecha_captura, enlace text, licencia,
  rut_proveedor text)   -- rut_proveedor (0025:51) = RUT de la EMPRESA. NO trip \yrut\y (el _ tras rut).

Sub-maestra de empresa (0023:103-114) — keyed por RUT, DENY-BY-DEFAULT:
- contratista(rut_proveedor text primary key, nombre, tipo_persona, codigo_empresa, origen, fecha_captura, enlace, licencia)
  -- NO tiene sector_id. NO existe columna de sector en contratista/contrato/entidad_tercero.

Catálogo de sector (0038:35-70): `sector(codigo text pk, etiqueta text)` public-read.
sector_id existe SOLO en proyecto_ficha/lobby_contraparte/donante (0038:68-70) — NUNCA en dinero.
⇒ La arista `<company-rut → sector>` NO EXISTE. La rama la modela como relación honesta-vacía.

pgTAP no-PII del cuerpo (0039 test:77-82) a CONSERVAR VERBATIM en 0052:
`select ok( (select regexp_replace(pg_get_functiondef(p.oid),'--[^\n]*','','g') from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='cruces' and p.proname='materializar_cruces') !~* '\y(partido|rut)\y', 'el cuerpo ... NO contiene partido ni rut (no-PII, LEGAL-03)');`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración 0052 (ampliar el CHECK + re-emitir el materializador con la rama lobby_sector_aporte como stub empresa-RUT→sector correcto-por-construcción)</name>
  <files>supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql</files>
  <read_first>
    - supabase/migrations/0039_cruce_senal.sql (el materializador completo; la rama lobby 91-120 a conservar VERBATIM; el CHECK 0039:50; el bloque cron/guard a NO re-emitir; el header patrón operador)
    - supabase/migrations/0023_dinero.sql (columnas de `contrato` y de `contratista`; `estado_vinculo` dominio; `monto` text VERBATIM; contratista keyed por rut_proveedor, SIN sector_id)
    - supabase/migrations/0025_agregacion.sql (líneas 48-56: `contrato.rut_proveedor` añadida aquí — el RUT de la EMPRESA, la llave de entidad-compartida; NO trip el guard \yrut\y)
    - supabase/migrations/0038_sector.sql (líneas 64-70: sector_id SOLO en proyecto_ficha/lobby_contraparte/donante — CONFIRMA que ninguna tabla de dinero tiene sector)
    - supabase/migrations/0034_entidad_tercero.sql (entidad_tercero — CONFIRMA que tampoco tiene sector_id; es la tabla-empresa donde viviría la arista faltante)
    - supabase/migrations/0049_cruces_de_proyecto.sql (patrón de join confirmado-solo + orden NEUTRO anti-insinuación)
  </read_first>
  <action>
    Crear `supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql` con encabezado en el estilo de 0039/0049: qué hace, que es ADITIVA + FULL REBUILD, que la última migración en disco es 0051, que la APLICACIÓN es checkpoint de operador (`psql --db-url --single-transaction`, NUNCA `supabase db push`, `PGCLIENTENCODING=UTF8`, esquivar BOM del `.env`) y vive en el Plan 02. El encabezado DEBE declarar el diseño CORREGIDO (operador 2026-07-14): la señal `lobby_sector_aporte` es un STUB ESTRUCTURAL correcto-por-construcción — un cruce dinero×sector keyed por el RUT de la EMPRESA contratista (entidad compartida), NO por `parlamentario_id`; que la arista `<company-rut → sector>` NO EXISTE hoy en el schema (ni contratista/contrato/entidad_tercero tienen sector_id) y por eso la rama rinde 0 filas honestas; que puentear dinero×lobby por `parlamentario_id` (yuxtaposición persona-nivel) es la "máquina de sospechas" diferida, PROHIBIDA aquí.

    Bloque 1 — ampliar el CHECK (Pattern 1 de RESEARCH). Antes del `alter`, un comentario que documente el Pitfall A1: el nombre `cruce_senal_tipo_senal_check` es por convención Postgres para el CHECK inline sin nombre de 0039; si un forward-fix lo renombró, el operador lo verifica con `select conname from pg_constraint where conrelid='public.cruce_senal'::regclass and contype='c'` (Plan 02) antes del drop. Emitir `alter table public.cruce_senal drop constraint cruce_senal_tipo_senal_check;` seguido de `alter table public.cruce_senal add constraint cruce_senal_tipo_senal_check check (tipo_senal in ('lobby_sector', 'lobby_sector_aporte'));`.

    Bloque 2 — `create or replace function cruces.materializar_cruces()` re-emitiendo el cuerpo COMPLETO: el mismo `delete from public.cruce_senal;` (ÚNICO delete, NO añadir un segundo), la rama `lobby_sector` existente COPIADA VERBATIM de 0039:91-120 (byte-idéntica — mismos identificadores, mismo jsonb con contraparte_nombre_crudo/audiencia_id/enlace_fuente, mismo `group by a.parlamentario_id, c.sector_id`, dataset 'lobby'; si se altera u omite, el próximo rebuild borra la señal lobby), y a continuación la NUEVA rama `insert into public.cruce_senal (...) select ...` para `lobby_sector_aporte`.

    La rama aporte — cruce de ENTIDAD-COMPARTIDA (empresa-RUT→sector), NO persona-nivel: modelar la arista faltante como una CTE explícita `empresa_sector` que representa el mapeo `<rut de la empresa> → <sector de la empresa>` — la relación que HOY no existe. Como no hay columna de sector en ninguna tabla de dinero/entidad, esta CTE es honesta-vacía: implementarla como un `select` tipado que no produce filas hoy (p.ej. `select ct2.rut_proveedor as rut_empresa, null::text as sector_id from public.contratista ct2 where false` — tipos correctos, 0 filas correcto-por-construcción), con un comentario SQL en MAYÚSCULAS marcando: "ARISTA FALTANTE <company-rut → sector>: la sustancia diferida de MONEY-03. Cuando exista un sector clasificado por empresa (columna sector_id en contratista/entidad_tercero + su clasificador), reemplazar el cuerpo de esta CTE por el mapeo real; hasta entonces la señal es vacía honesta por construcción." El insert luego hace: `select ct.parlamentario_id, es.sector_id, 'lobby_sector_aporte', count(*), jsonb_build_object('conteo', count(*), 'items', jsonb_agg(...)), 'chilecompra', min(ct.origen), now(), min(ct.enlace) from public.contrato ct join public.contratista cta on cta.rut_proveedor = ct.rut_proveedor join empresa_sector es on es.rut_empresa = cta.rut_proveedor where ct.estado_vinculo = 'confirmado' and ct.parlamentario_id is not null and es.sector_id is not null group by ct.parlamentario_id, es.sector_id`. El join a `empresa_sector` (vacía) garantiza 0 filas HOY, keyed por el RUT de la EMPRESA (`rut_proveedor`), NUNCA por `parlamentario_id` común entre dinero y lobby. `parlamentario_id` aquí SOLO restringe el universo a contratos ya ligados (Phase 70 confirmado), NO es el puente sector↔dinero — el puente es el RUT de la empresa. Comentar explícitamente esa distinción para que no se confunda con el join persona-nivel RECHAZADO.

    Restricciones del cuerpo: `sector_id` del catálogo NULL → la fila NO entra (`es.sector_id is not null`, 0038 D-05, sin catch-all). Filtros fail-closed: `ct.estado_vinculo = 'confirmado'` y `ct.parlamentario_id is not null` (espejo del confirmado-solo de 0049/contratos_de_parlamentario). NO referenciar ningún `rut` crudo/literal ni `entidad_tercero.rut`: la única mención permitida es `rut_proveedor` como llave de join (VERIFICADO: no trip `\yrut\y`). Verificar mentalmente contra el aserto no-PII de 0039 que el cuerpo (excluyendo comentarios) NO contiene el token `\yrut\y` ni `\ypartido\y`.

    Evidencia jsonb PII-safe: cada item lleva `'tipo':'contrato'`, `'fecha': ct.fecha_oc`, `'monto_verbatim': ct.monto` (text VERBATIM, JAMÁS sumado/casteado), `'codigo_orden': ct.codigo_orden`, `'enlace_fuente': ct.enlace`, ordenados `order by ct.fecha_oc desc nulls last`. NUNCA incluir `rut`, `rut_proveedor`, `donante_id`, `parlamentario_id` interno ni `proveedor_nombre` como llave PII. NO usar lenguaje causal en comentarios ni literales ("financió su voto"/"a cambio de"/"por eso votó" PROHIBIDOS); describir la señal como "conteo de contratos de empresas del sector X ligadas al parlamentario (con enlaces de fuente)".

    NO tocar el bloque `cron.schedule` de 0039 (el cron `cruces-materializar` ya existe; `CREATE OR REPLACE` lo hereda) — la migración 0052 NO re-programa el cron ni re-crea la tabla; solo `alter constraint` + `create or replace function`. NO añadir policies ni grants sobre `cruce_senal` (deny-by-default heredado, V4 ASVS). NO tocar el RPC 0040 (genérico por tipo_senal, hereda el token automáticamente).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql','utf8'); const body=t.split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n'); const a=[['CHECK ampliado',/check\s*\(\s*tipo_senal\s+in\s*\(\s*'lobby_sector'\s*,\s*'lobby_sector_aporte'\s*\)/i.test(body)],['create or replace function',/create\s+or\s+replace\s+function\s+cruces\.materializar_cruces/i.test(body)],['un solo delete',(body.match(/delete\s+from\s+public\.cruce_senal/gi)||[]).length===1],['rama lobby_sector preservada',/'lobby_sector'/.test(body)],['rama aporte',/'lobby_sector_aporte'/.test(body)],['join por contrato+contratista',/public\.contrato\b/i.test(body)&&/public\.contratista\b/i.test(body)],['CTE empresa_sector (arista faltante)',/empresa_sector/i.test(body)],['join por rut_proveedor (entidad-compartida)',/rut_proveedor/i.test(body)],['NO join persona-nivel dinero-lobby',!/lobby_contraparte[\s\S]{0,120}'lobby_sector_aporte'|'lobby_sector_aporte'[\s\S]{0,400}lobby_audiencia/i.test(body)],['sin rut crudo en cuerpo (\\\\yrut\\\\y)',!/\brut\b/i.test(body)],['sin partido en cuerpo',!/\bpartido\b/i.test(body)],['confirmado fail-closed',/estado_vinculo\s*=\s*'confirmado'/i.test(body)],['comentario arista faltante',/company-rut|arista faltante|<company-rut/i.test(t)],['sin causal',!/financi[oó]\s+su\s+voto|a\s+cambio\s+de|por\s+eso\s+vot[oó]/i.test(t)]]; let ok=true; for(const[n,p]of a){console.log((p?'PASS':'FAIL')+': '+n); if(!p)ok=false;} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql` existe
    - El CHECK se amplía a `check (tipo_senal in ('lobby_sector', 'lobby_sector_aporte'))` vía drop+add del constraint `cruce_senal_tipo_senal_check`
    - Hay exactamente UN `delete from public.cruce_senal` (no se añade un segundo delete)
    - La rama `lobby_sector` existente aparece byte-idéntica a 0039:91-120 (mismos identificadores, mismo jsonb con contraparte_nombre_crudo/audiencia_id/enlace_fuente, mismo group by, dataset 'lobby')
    - La rama `lobby_sector_aporte` se une por el RUT de la EMPRESA: `contrato.rut_proveedor` → `contratista.rut_proveedor` → CTE `empresa_sector` (la arista `<company-rut → sector>` AUSENTE, modelada honesta-vacía con `where false`), agrupando por `parlamentario_id, sector_id` con el sector proveniente de la empresa (NO de las lobby-contrapartes del parlamentario)
    - Existe un comentario SQL explícito que marca la arista faltante `<company-rut → sector>` como la sustancia diferida de MONEY-03
    - La rama NO une dinero y lobby por `parlamentario_id` común (NO reproduce el join persona-nivel del plan previo: `lobby_audiencia`/`lobby_contraparte` NO aparecen dentro de la rama aporte)
    - `parlamentario_id` en la rama aporte solo restringe el universo (contratos confirmados), no es el puente sector↔dinero
    - La evidencia jsonb lleva `monto_verbatim`/`codigo_orden`/`enlace_fuente`/`fecha`; NO lleva `rut`/`donante_id`
    - El cuerpo (excluyendo comentarios) NO contiene el token literal `\yrut\y` ni `\ypartido\y` (rut_proveedor no cuenta como `\yrut\y`)
    - No hay lenguaje causal ("financió su voto"/"a cambio de"/"por eso votó") en el archivo
    - El bloque `cron.schedule` de 0039 NO se re-emite; no se añaden policies/grants sobre `cruce_senal`; el RPC 0040 no se toca
  </acceptance_criteria>
  <done>La migración aditiva existe, amplía el CHECK, preserva la rama lobby verbatim, añade la rama aporte como stub empresa-RUT→sector correcto-por-construcción (join contra la arista ausente vía CTE empresa_sector, 0 filas hoy, comentario que marca la arista faltante), NUNCA por parlamentario_id, evidencia PII-safe, cuerpo sin rut/partido, sin causalidad; el verify automatizado pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir el pgTAP 0052 (CHECK ampliado + lobby preservado + empty-honest correcto-por-construcción + PII-safe + cuerpo no-PII)</name>
  <files>supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql</files>
  <read_first>
    - supabase/tests/0039_cruce_senal.test.sql (el patrón COMPLETO a espejar: begin/plan/seed owner/materializar/assert/rollback; el seed de 5 parlamentarios CR1..CR5 + audiencias + contrapartes con sector; el aserto no-PII del cuerpo 77-82; el aserto lobby_sector >=5 en 84-88; el aserto anon 42501)
    - supabase/migrations/0052_cruce_senal_lobby_sector_aporte.sql (la migración recién escrita en Task 1 — el shape exacto de la rama aporte, la CTE empresa_sector vacía, y de la evidencia)
    - supabase/migrations/0023_dinero.sql (columnas NOT NULL de `contrato` que el seed debe poblar: fuente_id, fecha_corte, mencion_proveedor, origen, enlace; PK (fuente_id, fecha_corte); columnas NOT NULL de `contratista`: rut_proveedor pk, origen, fecha_captura default, enlace, licencia default)
    - supabase/tests/0049_cruces_de_proyecto.test.sql (patrón de sembrar un caso confirmado + un caso no_confirmado para el aserto de fail-closed)
  </read_first>
  <action>
    Crear `supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql` espejando la estructura de `0039_cruce_senal.test.sql`: `begin;` → `select plan(N);` (N = número real de aserciones `select`; contar exactamente, evitar el off-by-one que 0039 documenta en su comentario) → seed como owner (bypassa RLS) → `select cruces.materializar_cruces();` → aserciones → `select * from finish(); rollback;`.

    Seed (owner): reusar el patrón de siembra de lobby de 0039 VERBATIM para CONSERVAR el aserto lobby_sector >=5 (sembrar 5 parlamentarios CR1..CR5 con audiencia confirmada + contraparte con sector 'salud'/'mineria_energia'/'banca_finanzas'/'transporte'/'educacion', idéntico a 0039). Añadir el seed de dinero de la EMPRESA para probar la rama aporte y su vacío honesto: (a) un `contratista` con `rut_proveedor` poblado (p.ej. '76.000.000-0') + un `contrato` `estado_vinculo='confirmado'`, `parlamentario_id='CR1'`, `rut_proveedor='76.000.000-0'`, poblando todas las columnas NOT NULL (fuente_id, fecha_corte, mencion_proveedor, origen, enlace) + `codigo_orden`, `fecha_oc`, `monto` (string VERBATIM p.ej. '$1.000.000' o null), `tipo_persona='juridica'`. (b) un `contrato` `estado_vinculo='no_confirmado'` y/o `parlamentario_id=null` (caso fail-closed).

    Documentar EN EL SEED que la CTE `empresa_sector` de la migración es honesta-vacía HOY (no hay columna de sector en la empresa), de modo que la rama aporte rinde 0 filas correcto-por-construcción AUNQUE el contrato confirmado esté sembrado — ese es el comportamiento ESPERADO, no un fallo. El pgTAP NO debe intentar forzar filas de aporte poblando una columna de sector que no existe.

    Aserciones (mínimo):
    (1) `lives_ok(insert ... tipo_senal 'lobby_sector_aporte' ...)` — el CHECK admite el token nuevo (insert directo NO viola 23514). Sembrar valores válidos (parlamentario_id existente CR1, sector_id 'salud' del catálogo, evidencia jsonb, provenance). Documentar que este insert directo PRUEBA el CHECK; NO prueba la rama del materializador.
    (2) `is(count lobby_sector_aporte materializado, 0)` tras `materializar_cruces()` (excluyendo la fila insertada a mano en (1) si aún vive; si (1) va DESPUÉS de materializar, contar solo lo producido por el proc con un predicado que distinga — p.ej. materializar ANTES de (1), aserir 0, luego (1)): EMPTY-HONEST correcto-por-construcción — la rama existe y compila pero la arista empresa→sector está vacía → 0 filas materializadas hoy. Es el aserto RECTOR del stub estructural.
    (3) `cmp_ok(count distinct parlamentario_id where tipo_senal='lobby_sector', '>=', 5)` — la señal lobby existente NO se perdió tras el rebuild (aserto reusado de 0039).
    (4) el aserto no-PII del CUERPO de la función VERBATIM de 0039 (regexp_replace comentarios + `!~* '\y(partido|rut)\y'`) — el cuerpo sigue sin partido/rut tras la extensión (con `rut_proveedor` como única mención, que NO trip el guard).
    (5) `ok(bool_and evidencia::text !~* '\y(rut|donante_id)\y' for lobby_sector_aporte)` — evidencia PII-safe (si hay filas; con 0 filas materializadas el `bool_and` sobre conjunto vacío es NULL→usar `coalesce(bool_and(...), true)` para que el aserto sea verde y honesto sobre el shape esperado).
    (6) `has_table('public','cruce_senal',...)` + el aserto anon 42501 (`set local role anon; throws_ok select ... 42501`) reusados de 0039 para confirmar que el candado deny-by-default sigue intacto tras la extensión.

    ORDENAR las aserciones para que el conteo `plan(N)` sea exacto. Encabezado del archivo: documentar que corre contra un schema APLICADO vía `psql -tA -f` (build/typecheck NO prueban el DDL — Pitfall 5), que requiere 0023/0025/0038/0039 aplicadas, que la rama aporte es un STUB ESTRUCTURAL vacío-por-construcción (arista empresa→sector ausente) — el aserto (2) lo codifica —, y que la APLICACIÓN a PROD + la corrida real es Plan 02 (operador). Ajustar `plan(N)` al conteo exacto.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql','utf8'); const a=[['begin/rollback',/begin;/.test(t)&&/rollback;/.test(t)],['plan()',/select\s+plan\(\s*\d+\s*\)/i.test(t)],['finish()',/select\s+\*\s+from\s+finish\(\)/i.test(t)],['materializar',/cruces\.materializar_cruces\(\)/i.test(t)],['lives_ok CHECK token',/lives_ok/i.test(t)&&/lobby_sector_aporte/.test(t)],['seed contratista rut',/insert\s+into\s+contratista/i.test(t)],['seed contrato',/insert\s+into\s+contrato/i.test(t)],['empty-honest 0 filas',/is\(\s*[\s\S]{0,160}lobby_sector_aporte[\s\S]{0,120},\s*0\b/i.test(t)||/,\s*0,\s*'[^']*empty|vac[ií]o|correcto-por-constr/i.test(t)],['caso no_confirmado',/no_confirmado/i.test(t)],['lobby preservado >=5',/count\(distinct[^)]*lobby_sector/i.test(t)&&/'>=',\s*5/i.test(t)],['no-PII cuerpo',/partido\|rut/i.test(t)],['PII-safe evidencia',/rut\|donante_id/i.test(t)],['anon 42501',/42501/.test(t)]]; let ok=true; for(const[n,p]of a){console.log((p?'PASS':'FAIL')+': '+n); if(!p)ok=false;} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `supabase/tests/0052_cruce_senal_lobby_sector_aporte.test.sql` existe con estructura begin/plan/seed/materializar/assert/finish/rollback
    - `plan(N)` coincide con el número real de aserciones `select` del archivo (sin off-by-one)
    - Existe un `lives_ok` que inserta `tipo_senal='lobby_sector_aporte'` sin violar el CHECK (23514)
    - El seed puebla `contratista` (con `rut_proveedor`) y `contrato` con un caso confirmado (parlamentario_id + estado_vinculo='confirmado' + rut_proveedor) y un caso no_confirmado
    - Hay un aserto RECTOR de empty-honest: tras `materializar_cruces()`, el conteo de filas `lobby_sector_aporte` MATERIALIZADAS es 0 (correcto-por-construcción; la arista empresa→sector está vacía) — con seed de contrato confirmado presente
    - Hay el aserto lobby_sector >=5 (señal existente preservada), el aserto no-PII del cuerpo VERBATIM de 0039 (`!~* '\y(partido|rut)\y'`), el aserto PII-safe de evidencia (`!~* '\y(rut|donante_id)\y'`, verde sobre conjunto vacío), y el aserto anon 42501 (deny-by-default intacto)
    - El seed y las aserciones no introducen lenguaje causal
  </acceptance_criteria>
  <done>El pgTAP 0052 existe, espeja 0039, cubre CHECK/lobby-preservado/empty-honest-correcto-por-construcción/PII-safe/cuerpo-no-PII/anon-42501 con seed de contratista+contrato confirmado + no_confirmado; el verify automatizado pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChileCompra (contratos por RUT de empresa, deny-by-default) → materializador | El hecho de contrato cruza hacia una señal derivada por SECTOR DE LA EMPRESA; el puente empresa→sector es entrada no confiable — hoy inexistente, por eso se modela vacío |
| cruce_senal (tabla PII, RLS deny-by-default) → RPC 0040 / superficies | La señal materializada no debe presentarse hasta el flip legal (Phase 73); MONEY gate OFF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Repudiation/Reputational | señal `lobby_sector_aporte` + su evidencia | mitigate | Conteo factual por diseño (`count(*)` de contratos por sector de la EMPRESA), NUNCA score; sin lenguaje causal en token/SQL/evidencia; el verify de Tasks 1/2 prohíbe "financió su voto"/"a cambio de"/"por eso votó" |
| T-72-02 | Repudiation (yuxtaposición persona-nivel = "máquina de sospechas") | rama aporte del insert | mitigate | El puente sector↔dinero es el RUT de la EMPRESA (`contrato.rut_proveedor`→`contratista`→arista empresa→sector), NUNCA `parlamentario_id` común entre dinero y lobby; el verify de Task 1 falla si `lobby_audiencia`/`lobby_contraparte` aparecen dentro de la rama aporte; comentario SQL marca la arista faltante como la sustancia diferida |
| T-72-03 | Information Disclosure (PII, Ley 21.719) | evidencia jsonb + cuerpo del proc | mitigate | Evidencia solo codigo_orden/monto_verbatim/enlace/fecha; NUNCA rut/donante_id; el RUT de la empresa se referencia SOLO como `rut_proveedor` (no trip `\yrut\y`); pgTAP `!~* '\y(rut|donante_id)\y'` (evidencia) + `!~* '\y(partido|rut)\y'` (cuerpo) |
| T-72-04 | Tampering (atribución falsa) | rama aporte del insert | mitigate | `estado_vinculo='confirmado'` + `parlamentario_id is not null` (fail-closed, IDENT-12; el FK nació por RUT-exacto Phase 70); la arista empresa→sector ausente → 0 filas (vacío honesto correcto-por-construcción), nunca falso; SERVEL-por-nombre EXCLUIDO |
| T-72-05 | Tampering (pérdida de señal) | FULL REBUILD borra cruce_senal | mitigate | La rama `lobby_sector` se conserva byte-idéntica (0039:91-120); pgTAP `count distinct lobby_sector >= 5` verifica que el rebuild la repone; un solo `delete` (verify lo cuenta) |
| T-72-06 | Elevation (exposición prematura) | presentación de la señal | accept/mitigate | Doble candado heredado: RLS deny-by-default sobre cruce_senal + RPC 0040 sin grant a anon + `moneyPublicEnabled`/`crucesPublicEnabled` OFF; esta migración NO añade policies/grants; el flip es acto humano Phase 73 con guard CI; pgTAP anon 42501 lo verifica |
| T-72-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Fase SIN instalaciones de paquetes (SQL + pgTAP dentro del repo); RESEARCH Package Legitimacy Audit = "no aplica" |
</threat_model>

<verification>
- Task 1 verify: la migración amplía el CHECK, un solo delete, rama lobby preservada + rama aporte (join empresa-RUT→sector vía CTE empresa_sector ausente), NO join persona-nivel, cuerpo sin rut/partido, comentario de arista faltante, sin causalidad.
- Task 2 verify: el pgTAP espeja 0039 con el aserto RECTOR de empty-honest (0 filas materializadas correcto-por-construcción) + lobby preservado + PII-safe + cuerpo no-PII + anon 42501.
- `pnpm test` (root) sigue verde: el DDL nuevo no toca TS; no debe regresionar la suite.
- La corrida real del pgTAP contra un schema APLICADO + la aplicación a PROD son Plan 02 (operador) — este plan es offline-válido (se escribe y se valida estructuralmente).
</verification>

<success_criteria>
1. `cruce_senal` incluye la señal `lobby_sector_aporte` como CONTEO factual con evidencia jsonb (enlaces de fuente), NUNCA un score (SC#1).
2. Migración aditiva (CHECK ampliado + rama nueva) + FULL REBUILD transaccional idempotente; la rama lobby_sector se conserva byte-idéntica (SC#2).
3. La rama es un cruce de ENTIDAD-COMPARTIDA keyed por el RUT de la empresa contratista (dinero por RUT-exacto, parlamentario_id confirmado solo restringe el universo); la arista empresa→sector NO existe hoy → vacío honesto correcto-por-construcción (0 filas), NUNCA una yuxtaposición persona-nivel, unión sin rut crudo en el cuerpo (SC#3).
4. Ninguna afirmación causal aparece en la señal, su token ni su evidencia (SC#4).
</success_criteria>

<output>
Create `.planning/phases/72-dinero-p5d-extender-materializador-cruce-senal-con-lobby-sector-aporte/72-01-SUMMARY.md` when done
</output>
