---
phase: 74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly
plan: 03
type: execute
wave: 2
depends_on: [74-02]
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/query-runner.ts
  - packages/freshness/src/evaluate.ts
  - packages/freshness/src/evaluate.test.ts
  - .planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md
autonomous: false
requirements: [DEBT-03]

must_haves:
  truths:
    - "La referencia secrets.CLOUDFLARE_API_TOKEN existe en deploy-cloudflare.yml y el job deploy la consume"
    - "Ningún cron de ingesta (leyes/agenda/lobby-camara/lobby-leylobby/probidad) referencia CLOUDFLARE_API_TOKEN (no lo necesitan)"
    - "Existe una nota de operador que documenta cargar el VALOR del secret en Cuchecorp/gov-map GH settings (acto humano)"
    - "pnpm freshness refleja la rotación de leyes vía una señal de EDAD-MÍNIMA (proyecto más viejo sin refrescar) sin regresionar las señales MAX de los conectores v6.0 (leyes/lobby/probidad)"
  artifacts:
    - path: ".planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md"
      provides: "Nota de operador: cargar CLOUDFLARE_API_TOKEN en GH settings; verificación de la referencia YAML"
      min_lines: 20
    - path: "packages/freshness/src/catalog.ts"
      provides: "Señal de edad-mínima de leyes (MIN fecha_captura) añadida sin tocar las entradas MAX existentes"
      contains: "leyes-min-edad"
  key_links:
    - from: "packages/freshness/src/query-runner.ts"
      to: "proyecto (MIN fecha_captura)"
      via: "SELECT MIN(fecha_captura) para la señal de edad-mínima"
      pattern: "MIN\\("
    - from: "packages/freshness/src/evaluate.ts"
      to: "señal edad-mínima leyes"
      via: "evaluación de la señal MIN (el proyecto más viejo revela la dilución)"
      pattern: "leyes-min-edad|edadMinima"
---

<objective>
Cerrar DEBT-03 y SC#4 de la fase.

**DEBT-03 (RECTOR — el framing del requisito es impreciso):** `CLOUDFLARE_API_TOKEN` aparece SOLO en `deploy-cloudflare.yml:59` (deploy del frontend). Los crons de ingesta (leyes/agenda/lobby/probidad) escriben a Supabase+R2 y NO tocan Cloudflare → corren verdes SIN el token. La deuda real: el VALOR del secret está AUSENTE del repo Cuchecorp/gov-map (acto de operador cargarlo). El trabajo del agente: (a) VERIFICAR que la referencia `secrets.CLOUDFLARE_API_TOKEN` existe y el job `deploy` la consume; (b) CONFIRMAR que los crons de ingesta NO la referencian (y no la necesitan); (c) ESCRIBIR una nota de operador para cargar el valor. NO cablear el token en los crons de ingesta (sería incorrecto).

**SC#4 (freshness refleja la rotación):** `catalog.ts` mide `leyes` con `MAX(fecha_captura)` → un solo refresh la pone verde ocultando la dilución (3.000 proyectos pueden llevar meses sin tocar). Añadir una señal de EDAD-MÍNIMA para leyes (revela el proyecto más viejo sin refrescar → la rotación del plan 74-02 se hace visible) SIN regresionar las señales MAX existentes de los conectores v6.0.

Purpose: El deploy CI reproducible requiere el secret cargado (operador); la frescura honesta requiere una señal que muestre la cola, no solo el último upsert.
Output: Verificación de la referencia YAML + nota de operador + señal de edad-mínima en freshness con tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-RESEARCH.md

<interfaces>
<!-- Contratos que el executor necesita — extraídos del codebase. Usar directo. -->

Referencia CF (ÚNICA en el repo, ya correcta) — .github/workflows/deploy-cloudflare.yml:56-61:
```yaml
- name: Build + Deploy (OpenNext → Cloudflare Workers)
  working-directory: app
  env:
    CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  run: pnpm run deploy
```
Crons de ingesta (NINGUNO referencia CF): agenda-weekly.yml, leyes-weekly.yml, lobby-camara-weekly.yml, lobby-leylobby-weekly.yml, probidad-weekly.yml, fichas-backfill.yml. Verificado: grep `CLOUDFLARE_API_TOKEN` en `.github/workflows/` devuelve SOLO `deploy-cloudflare.yml`.

Catálogo de freshness (a extender, NO regresionar) — packages/freshness/src/catalog.ts:28-35,211-219:
```typescript
export interface FuenteConfig {
  fuente: string; tabla: string; columna: string;
  umbralDias: number; overrideEnv: string; workflowYml: string;
}
// entrada EXISTENTE (MAX, NO tocar):
{ fuente: "leyes", tabla: "proyecto", columna: "fecha_captura", umbralDias: 7,
  overrideEnv: "FRESHNESS_UMBRAL_LEYES", workflowYml: "leyes-weekly.yml" }
```

Query-runner (señal MAX hoy) — packages/freshness/src/query-runner.ts:130-146:
```typescript
export async function queryFreshness(dbUrl: string): Promise<QueryRow[]> {
  return CATALOG.map((cfg) => {
    const sql = `SELECT MAX(${cfg.columna}) FROM ${cfg.tabla};`;   // ⟵ solo MAX
    const raw = psql(dbUrl, sql);
    // ...
  });
}
```
`psql(dbUrl, sql)` es read-only, PGCLIENTENCODING=UTF8, NUNCA imprime dbUrl/password (WR-06). `evaluate()` (evaluate.ts:29-76) marca `stale = diasDesdeUpsert === null || diasDesdeUpsert > umbralDias` (fail-closed).

DECISIÓN DE DISEÑO (research A3, VALIDATION): la señal de edad-mínima es ADITIVA. Opciones válidas: (a) nueva entrada de CATALOG `leyes-min-edad` que use `MIN(fecha_captura)` en vez de MAX — requiere que `queryFreshness` sepa qué agregado usar por entrada (añadir un campo opcional `agregado?: "MAX" | "MIN"` a `FuenteConfig`, default MAX → no regresiona las demás); o (b) un bloque de señal separado (espejo de COBERTURA_VOTO_SENALES). PREFERIR (a): mínimo cambio, reusa el pipeline `evaluate` existente. El proyecto MÁS VIEJO (MIN) con umbral generoso (p.ej. 45-60d) revela si la cola quedó sin rotar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar la referencia CF + escribir la nota de operador (DEBT-03)</name>
  <files>.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md</files>
  <read_first>
    - .github/workflows/deploy-cloudflare.yml (referencia CF + comentario de secrets)
    - .github/workflows/leyes-weekly.yml (env de un cron de ingesta — NO tiene CF)
    - .planning/STATE.md (memoria: billing GH activo v6.0, secrets re-cargados en transfer Cuchecorp/gov-map)
  </read_first>
  <action>
    Verificar estáticamente (Grep) que: (1) `deploy-cloudflare.yml` referencia `secrets.CLOUDFLARE_API_TOKEN` y el step `Build + Deploy` la consume vía `env:`; (2) NINGÚN cron de ingesta (`agenda-weekly.yml`, `leyes-weekly.yml`, `lobby-camara-weekly.yml`, `lobby-leylobby-weekly.yml`, `probidad-weekly.yml`, `fichas-backfill.yml`) referencia `CLOUDFLARE_API_TOKEN`. Escribir `74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md` documentando: el hallazgo rector (CF token = concern de DEPLOY, no de ingesta); la evidencia del grep (referencia presente en 1 archivo, ausente en los 6 crons de ingesta); el paso de OPERADOR (Settings → Secrets and variables → Actions en Cuchecorp/gov-map → añadir `CLOUDFLARE_API_TOKEN` con permiso "Edit Cloudflare Workers" + `CLOUDFLARE_ACCOUNT_ID`); y la aclaración explícita de que NO se debe añadir el token a los crons de ingesta (warning sign: un diff que lo añada = mala interpretación). NO hardcodear ningún valor de secret. NO editar los YAMLs (la referencia ya es correcta).
  </action>
  <verify>
    <automated>bash -c "grep -rl 'CLOUDFLARE_API_TOKEN' '.github/workflows/' | grep -v deploy-cloudflare.yml | wc -l | grep -qx 0 && echo OK-solo-deploy"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl CLOUDFLARE_API_TOKEN .github/workflows/` devuelve EXACTAMENTE `deploy-cloudflare.yml` (0 crons de ingesta lo referencian)
    - `deploy-cloudflare.yml` tiene `CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}` bajo el `env:` del step de deploy (referencia consumida)
    - La nota de operador existe (≥20 líneas), documenta el paso GH settings + la aclaración anti-mal-interpretación, y NO contiene ningún valor de token literal
    - Ningún YAML de cron de ingesta fue modificado (git diff de `.github/workflows/*weekly*.yml` + `fichas-backfill.yml` vacío)
  </acceptance_criteria>
  <done>Referencia CF verificada correcta + ausente de los crons de ingesta; nota de operador escrita; sin cambios de YAML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Señal de edad-mínima de leyes en freshness (SC#4) sin regresionar MAX</name>
  <files>packages/freshness/src/catalog.ts, packages/freshness/src/query-runner.ts, packages/freshness/src/evaluate.ts, packages/freshness/src/evaluate.test.ts</files>
  <read_first>
    - packages/freshness/src/catalog.ts:28-35,211-259 (FuenteConfig + entradas leyes/lobby/probidad EXISTENTES)
    - packages/freshness/src/query-runner.ts:130-146 (queryFreshness — señal MAX)
    - packages/freshness/src/evaluate.ts:29-76 (evaluate — stale fail-closed)
    - packages/freshness/src/evaluate.test.ts (patrón de test existente)
  </read_first>
  <behavior>
    - CATALOG incluye una entrada nueva `leyes-min-edad` que usa MIN(fecha_captura) de `proyecto`, umbral generoso (edad del proyecto MÁS viejo)
    - Las entradas MAX existentes (leyes/agenda/lobby-camara/lobby-leylobby/probidad/fichas/chilecompra) quedan IDÉNTICAS (mismo agregado MAX, mismos umbrales)
    - evaluate() sobre la señal MIN: si el proyecto más viejo supera el umbral → stale=true (la cola no rotó → visible); si toda la cola está fresca → stale=false
    - queryFreshness elige MIN vs MAX según el nuevo campo `agregado` de FuenteConfig (default MAX → las demás no cambian)
    - degradación honesta preservada: MIN nulo/ilegible → stale (fail-closed, misma regla de evaluate)
  </behavior>
  <action>
    Añadir un campo opcional `agregado?: "MAX" | "MIN"` a `FuenteConfig` (default MAX). En `queryFreshness`, construir la SQL con `SELECT ${cfg.agregado ?? "MAX"}(${cfg.columna}) FROM ${cfg.tabla};` — cambio quirúrgico que NO altera el comportamiento de las entradas existentes (todas omiten `agregado` → MAX). Añadir a `CATALOG` una entrada `leyes-min-edad` (tabla `proyecto`, columna `fecha_captura`, `agregado: "MIN"`, `umbralDias` generoso p.ej. 45, `overrideEnv: "FRESHNESS_UMBRAL_LEYES_MIN_EDAD"`, `workflowYml: "leyes-weekly.yml"`). NO modificar la entrada `leyes` (MAX) ni ninguna otra. `evaluate()` ya trata `stale` genéricamente → verificar que la señal MIN se evalúa sin cambios de lógica (o extensión mínima documentada). Extender `evaluate.test.ts`: (1) un caso donde el MIN viejo → stale (dilución visible); (2) un caso donde toda la cola fresca → no stale; (3) aserción de NO-REGRESIÓN: las señales MAX de leyes/lobby/probidad conservan agregado MAX y sus umbrales. Documentar en el comentario de la nueva entrada por qué MIN revela la rotación del plan 74-02 (la cola más vieja) y MAX no.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `FuenteConfig` tiene `agregado?: "MAX" | "MIN"`; `queryFreshness` usa `cfg.agregado ?? "MAX"` (grep muestra `MIN(` alcanzable solo para la entrada nueva)
    - CATALOG contiene `leyes-min-edad` con `agregado: "MIN"`; la entrada `leyes` original permanece con MAX y umbral 7 (aserción en test: ambas entradas presentes, agregados distintos)
    - Test: MIN por encima del umbral → `stale=true`; toda la cola fresca → `stale=false`
    - Test de no-regresión: leyes/agenda/lobby-camara/lobby-leylobby/probidad/fichas conservan agregado MAX (ninguna cambió de agregado ni umbral)
    - MIN nulo/ilegible → stale (fail-closed, degradación honesta preservada)
    - `pnpm --filter @obs/freshness test` verde; sin lenguaje subjetivo en asserts
  </acceptance_criteria>
  <done>Señal de edad-mínima de leyes añadida (MIN) sin regresionar las señales MAX v6.0; tests verdes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Operador carga CLOUDFLARE_API_TOKEN en Cuchecorp/gov-map + deploy CI verde</name>
  <action>Acto de operador (el agente no tiene acceso a GH settings ni al valor del token): cargar el secret CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID en Cuchecorp/gov-map GH settings y disparar deploy-cloudflare.yml para confirmar deploy verde. Ver la nota 74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md y los detalles abajo.</action>
  <what-built>
    El agente verificó que la referencia `secrets.CLOUDFLARE_API_TOKEN` en `deploy-cloudflare.yml` es correcta y que ningún cron de ingesta la referencia (no la necesitan). Escribió la nota de operador `74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md`. Añadió la señal de edad-mínima de leyes a `pnpm freshness`.
  </what-built>
  <how-to-verify>
    Cargar el VALOR del secret (acto de operador — el agente no tiene acceso a GH settings ni al valor del token):
    1. En GitHub → repo Cuchecorp/gov-map → Settings → Secrets and variables → Actions.
    2. Añadir/verificar el secret `CLOUDFLARE_API_TOKEN` (token con permiso "Edit Cloudflare Workers") y `CLOUDFLARE_ACCOUNT_ID`.
    3. Confirmar que GH billing está activo (memoria v6.0: activo; verificar si volvió a bloquearse).
    4. Disparar `deploy-cloudflare.yml` (workflow_dispatch) → confirmar que corre VERDE (deploy reproducible en CI, sin fallback wrangler local).
    5. (Opcional, sobre semanas) observar que la señal de edad-mínima de `pnpm freshness` tiende a la baja a medida que la rotación del cron leyes-weekly (plan 74-02) cubre el corpus.
  </how-to-verify>
  <resume-signal>Escribir "cargado" (con el resultado del deploy) o describir el fallo (billing/permiso).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo git → GH Actions secrets | el VALOR del token vive en GH settings, NUNCA en git |
| freshness (read-only) → Supabase | lectura agregada MIN/MAX de fecha_captura con service key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-09 | Information disclosure | secret CF hardcodeado en git al "cablearlo" | mitigate | solo referencia `${{ secrets.* }}` (ya presente); la nota de operador NO contiene ningún valor; el valor lo carga el operador en GH settings |
| T-74-10 | Tampering (alcance) | añadir CF token a los crons de ingesta (mala interpretación del requisito) | mitigate | la nota documenta el warning sign explícitamente; verify-gate afirma que solo `deploy-cloudflare.yml` lo referencia; git diff de crons vacío |
| T-74-11 | Information disclosure (cobertura falsa) | freshness verde por MAX ocultando 3.000 proyectos stale | mitigate | señal de edad-mínima (MIN) revela el proyecto más viejo sin refrescar → la dilución/rotación se hace visible; degradación fail-closed preservada |
</threat_model>

<verification>
- `grep -rl CLOUDFLARE_API_TOKEN .github/workflows/` → solo `deploy-cloudflare.yml`.
- `pnpm --filter @obs/freshness test` verde (señal MIN + no-regresión MAX).
- `pnpm test` sin regresión al cierre de la wave.
- Nota de operador presente sin ningún valor de secret literal.
</verification>

<success_criteria>
- La referencia `CLOUDFLARE_API_TOKEN` está verificada correcta + consumida por el deploy; los crons de ingesta no la referencian; existe la nota de operador (DEBT-03, SC#2 de la fase — porción del agente).
- `pnpm freshness` refleja la rotación vía la señal de edad-mínima de leyes sin regresionar las señales MAX v6.0 (SC#4 de la fase).
- Cargar el valor del secret + el deploy CI verde = checkpoint de operador.
</success_criteria>

<output>
Create `.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-03-SUMMARY.md` when done
</output>
