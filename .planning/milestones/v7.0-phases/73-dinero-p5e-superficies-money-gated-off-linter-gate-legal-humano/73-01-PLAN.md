---
phase: 73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/money-presentacion.ts
  - app/lib/money-antiflip-guard.test.ts
autonomous: true
requirements: [MONEY-04, MONEY-05]

must_haves:
  truths:
    - "La leyenda anti-insinuación MONEY existe como constante única, importable por las 4 superficies y por el linter."
    - "Un commit de agente NO puede relajar el gate `=== \"true\"`, meter `MONEY_PUBLIC_ENABLED=true` en el repo, ni leer la env cruda en una ruta — el guard lo caza y muerde (mutation self-check)."
    - "El flag MONEY_PUBLIC_ENABLED permanece OFF/default; .env.example sigue `=false`."
  artifacts:
    - path: "app/lib/money-presentacion.ts"
      provides: "LEYENDA_ANTI_INSINUACION_MONEY (constante single-source, texto LOCKED verbatim)"
      exports: ["LEYENDA_ANTI_INSINUACION_MONEY"]
      contains: "Un contrato o un aporte registrado es un hecho público observable"
    - path: "app/lib/money-antiflip-guard.test.ts"
      provides: "Guard anti-flip: gate fail-closed + .env.example=false + no-raw-env-en-ruta + mutation self-check"
      min_lines: 80
  key_links:
    - from: "app/lib/money-antiflip-guard.test.ts"
      to: "app/lib/money-gate.ts"
      via: "readFileSync + regex `=== \"true\"`"
      pattern: "MONEY_PUBLIC_ENABLED\\\\s\\*===\\\\s\\*"
    - from: "app/lib/money-antiflip-guard.test.ts"
      to: "app/ (walk recursivo)"
      via: "walkSourceFiles reproducido, FAIL si MONEY_PUBLIC_ENABLED fuera de money-gate.ts"
      pattern: "walkSourceFiles"
---

<objective>
Sentar la base (Wave 0) de la fase: (1) la constante única `LEYENDA_ANTI_INSINUACION_MONEY` (texto LOCKED verbatim del UI-SPEC), fuente de la leyenda que las 4 superficies renderizan y que el linter resta como negación; y (2) el guard anti-flip `money-antiflip-guard.test.ts`, que congela las tres invariantes del gate deny-by-default (fail-closed `=== "true"`, `.env.example=false`, ninguna ruta lee la env cruda) con mutation self-check para no ser un no-op verde.

Purpose: MONEY-04 (single-source de la leyenda) + MONEY-05 (el agente construye hasta el gate; un guard impide que un commit de agente flipee el flag/default). Estas dos piezas son consumidas por los planes 02 (leyenda en superficies), 03 (linter la resta) y 04 (dossier).
Output: `app/lib/money-presentacion.ts` (nuevo) + `app/lib/money-antiflip-guard.test.ts` (nuevo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-UI-SPEC.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-RESEARCH.md
@app/AGENTS.md

<interfaces>
<!-- El gate único (NO tocar; el guard lo lee, no lo modifica). -->
From app/lib/money-gate.ts:
```typescript
import "server-only";
export function moneyPublicEnabled(
  env: Record<string, string | undefined> = process.env,
): boolean {
  return env.MONEY_PUBLIC_ENABLED === "true";
}
```

<!-- Molde verbatim del guard anti-flip: packages/dinero/src/servel-frozen-guard.test.ts §(5). -->
<!-- Constantes de ruta en ESE archivo (a replicar/adaptar al proyecto app): -->
From packages/dinero/src/servel-frozen-guard.test.ts:
```
const REPO_ROOT = path.resolve(DINERO_SRC, "..", "..", "..");
const ENV_EXAMPLE = path.join(REPO_ROOT, ".env.example");
const MONEY_GATE  = path.join(REPO_ROOT, "app", "lib", "money-gate.ts");
// (5) money-gate.ts enciende SOLO con "true":
//   expect(/MONEY_PUBLIC_ENABLED\s*===\s*["']true["']/.test(gate)).toBe(true);
//   expect(/Boolean\s*\(\s*[^)]*MONEY_PUBLIC_ENABLED/.test(gate)).toBe(false);
// .env.example trae =false:
//   expect(/^MONEY_PUBLIC_ENABLED\s*=\s*false\s*$/m.test(env)).toBe(true);
//   expect(/^MONEY_PUBLIC_ENABLED\s*=\s*true\s*$/m.test(env)).toBe(false);
```

<!-- El walk recursivo a REPRODUCIR (NO exportado; módulo-local en lockdown-guard). -->
<!-- En app/lib/lockdown-guard.test.ts vive verbatim: -->
From app/lib/lockdown-guard.test.ts:
```typescript
const APP_ROOT = process.cwd(); // vitest corre desde app/
const REPO_ROOT = path.resolve(APP_ROOT, "..");
const SKIP_DIRS = new Set([ /* .next, .open-next, node_modules, ... */ ]);
function walkSourceFiles(dir: string): string[] { /* recursivo, salta SKIP_DIRS */ }
```

<!-- La leyenda análoga single-source de Phase 68 (patrón a espejar): -->
From app/lib/voto-presentacion.ts:
```typescript
export const LEYENDA_ANTI_INSINUACION =
  "Un voto es un hecho observable. Ausente o pareo no equivalen a votar en contra. No medimos disciplina ni motivo.";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constante única LEYENDA_ANTI_INSINUACION_MONEY</name>
  <files>app/lib/money-presentacion.ts</files>
  <read_first>
    - .planning/phases/73-.../73-UI-SPEC.md §Leyenda anti-insinuación MONEY / §Copywriting Contract (texto LOCKED verbatim)
    - app/lib/voto-presentacion.ts (patrón single-source `LEYENDA_ANTI_INSINUACION`, líneas 58-59, a espejar)
  </read_first>
  <action>
    Crear `app/lib/money-presentacion.ts` con un único `export const LEYENDA_ANTI_INSINUACION_MONEY` = el texto LOCKED VERBATIM del UI-SPEC §Leyenda: "Un contrato o un aporte registrado es un hecho público observable. Un vínculo por RUT es una coincidencia exacta de identificador, no una afirmación de irregularidad. No medimos influencia ni intención, ni afirmamos que un aporte compre una decisión." Copiar carácter por carácter, con tildes exactas; ninguna reformulación. Espejar la estructura de `voto-presentacion.ts` (un módulo `app/lib/` sin efectos secundarios, sin `import "server-only"` — la leyenda es texto renderizable en cliente). No añadir un componente aquí; solo la constante (el plan 02 la importa y renderiza). Documentar en un comentario que es single-source: importada por las 4 superficies MONEY Y restada en `NEGACIONES_LOCKED` del linter (plan 03).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/lib/money-presentacion.ts','utf8'); if(!/export const LEYENDA_ANTI_INSINUACION_MONEY/.test(s)) throw new Error('falta export'); if(!s.includes('Un contrato o un aporte registrado es un hecho público observable')) throw new Error('texto no verbatim'); if(!s.includes('no afirmamos que un aporte compre una decisión')&&!s.includes('ni afirmamos que un aporte compre una decisión')) throw new Error('cola de la leyenda no verbatim'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/money-presentacion.ts` existe y exporta `LEYENDA_ANTI_INSINUACION_MONEY`.
    - El valor es el texto LOCKED verbatim del UI-SPEC §Leyenda (con tildes exactas: "público", "afirmación", "influencia", "intención", "decisión").
    - Es una constante `string`, no una función ni un componente; sin `import "server-only"`.
    - `pnpm typecheck` (tsc -b root) limpio.
  </acceptance_criteria>
  <done>La leyenda MONEY existe como fuente única, verbatim, importable.</done>
</task>

<task type="auto">
  <name>Task 2: Guard anti-flip money-antiflip-guard.test.ts (3 vectores + mutation self-check)</name>
  <files>app/lib/money-antiflip-guard.test.ts</files>
  <read_first>
    - packages/dinero/src/servel-frozen-guard.test.ts §(5) (aserciones núcleo: `=== "true"` + no-Boolean-laxo + `.env.example=false` — molde verbatim)
    - app/lib/lockdown-guard.test.ts (walkSourceFiles + SKIP_DIRS + APP_ROOT/REPO_ROOT a reproducir; patrón guard-como-test)
    - app/lib/money-gate.ts (el chokepoint único que el guard congela — NO modificar)
    - .planning/phases/73-.../73-RESEARCH.md §Pitfall 4 (el vector "raw env en ruta") y §Pattern 2
    - .planning/phases/73-.../73-UI-SPEC.md §Guard CI anti-flip (los 3 vectores exigidos)
  </read_first>
  <action>
    Crear `app/lib/money-antiflip-guard.test.ts` como test de vitest en el proyecto `app` (corre en `pnpm --filter ./app test`). Espejar `servel-frozen-guard.test.ts §5` y `lockdown-guard.test.ts`. Definir `APP_ROOT = process.cwd()` (app/), `REPO_ROOT = path.resolve(APP_ROOT, "..")`, `MONEY_GATE = path.join(APP_ROOT, "lib", "money-gate.ts")`, `ENV_EXAMPLE = path.join(REPO_ROOT, ".env.example")`. Reproducir `walkSourceFiles` + `SKIP_DIRS` verbatim del lockdown-guard (NO importarlos — son módulo-local; el molde de reproducción es el mismo que usó anti-insinuacion-guard con stripTsComments).

    Tres bloques `describe` de aserción sobre archivos reales (vector 1, 2, 3) + un cuarto de mutation self-check:
    (Vector 1 — fail-closed) leer `money-gate.ts`: `expect(/MONEY_PUBLIC_ENABLED\s*===\s*["']true["']/.test(gate)).toBe(true)` y `expect(/Boolean\s*\(\s*[^)]*MONEY_PUBLIC_ENABLED/.test(gate)).toBe(false)`.
    (Vector 2 — nada `=true` en repo committeado) leer `.env.example`: `expect(/^MONEY_PUBLIC_ENABLED\s*=\s*false\s*$/m.test(env)).toBe(true)` y `expect(/^MONEY_PUBLIC_ENABLED\s*=\s*true\s*$/m.test(env)).toBe(false)`.
    (Vector 3 — no raw env en ruta) `walkSourceFiles(APP_ROOT)`, y para cada `.ts/.tsx` cuyo path relativo NO sea `lib/money-gate.ts`, FALLAR si su contenido contiene `MONEY_PUBLIC_ENABLED` (regex `/MONEY_PUBLIC_ENABLED/`). El único archivo que puede nombrar el flag es `app/lib/money-gate.ts` (el chokepoint). Los `*.test.ts`/`*.test.tsx` que legítimamente prueban el gate (p.ej. money-gate.test.ts, money-antiflip-guard.test.ts, los RTL de superficie que inyectan `{ MONEY_PUBLIC_ENABLED: "true" }`) deben permitirse por una allowlist EXPLÍCITA nominada (paths exactos), no por un patrón amplio — documentar cada excepción con su razón. Ensamblar `offenders` y `expect(offenders, "<mensaje accionable>").toHaveLength(0)`.

    Extraer la lógica de detección a un helper puro `detectarRelajacionGate({ gateSrc, envSrc })` y `detectarRawEnvEnRuta(fileSrc, relPath)` para poder ejercerlos en el mutation self-check EN MEMORIA (no tocar archivos reales del repo):
    - self-check A: `detectarRelajacionGate` sobre un gate mutado a `Boolean(env.MONEY_PUBLIC_ENABLED)` o a `!== "false"` → debe reportar violación.
    - self-check B: `detectarRelajacionGate` sobre un `.env.example` mutado a `MONEY_PUBLIC_ENABLED=true` → debe reportar violación.
    - self-check C: `detectarRawEnvEnRuta` sobre un fixture `if (process.env.MONEY_PUBLIC_ENABLED === "true")` con relPath `app/contraparte/[id]/page.tsx` → debe reportar violación; y sobre el mismo con relPath `lib/money-gate.ts` → NO reporta (es el chokepoint permitido).

    PROHIBIDO: este guard NO edita `money-gate.ts` ni `.env.example`; NO pone `=true`; NO relaja el `===`. Solo LEE y afirma. Si el guard, al correr HOY, encontrara una violación real, es un fallo legítimo a reportar, NO a "arreglar" flipeando nada.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run lib/money-antiflip-guard.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/money-antiflip-guard.test.ts` corre VERDE hoy contra el árbol actual (el gate ya es fail-closed, `.env.example=false`, y los 3 consumidores importan `moneyPublicEnabled()` — ninguna ruta lee raw env).
    - El guard tiene los 3 vectores: relajación del `=== "true"` (incl. no-`Boolean` laxo), `=true` en `.env.example`, y `MONEY_PUBLIC_ENABLED` fuera de `money-gate.ts` (walk de app/ con allowlist explícita de tests).
    - El mutation self-check muerde en los 3 casos (A gate relajado, B `.env.example=true`, C raw-env en ruta) — probado EN MEMORIA, no es no-op.
    - El guard NO importa el módulo server-only `money-gate.ts` (lo lee con readFileSync); reproduce `walkSourceFiles`/`SKIP_DIRS` (no los importa).
    - `.env.example` sigue `=false` y `money-gate.ts` sigue `=== "true"` tras el plan (git diff no los toca).
    - `pnpm --filter ./app test` verde (sin regresión).
  </acceptance_criteria>
  <done>El gate deny-by-default queda congelado por un guard con 3 vectores + mutation self-check; el agente no puede flipearlo sin que la suite falle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit del agente → repo | Un cambio del agente podría relajar el gate o meter `=true` |
| ruta MONEY → env | Una ruta podría leer `MONEY_PUBLIC_ENABLED` cruda, bypasseando el chokepoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering / Elevation | money-gate.ts / .env.example | mitigate | Guard anti-flip vectores 1+2: `=== "true"` fail-closed + `.env.example=false`; mutation self-check A/B. |
| T-73-02 | Tampering | rutas app/ | mitigate | Guard vector 3: walk app/, FAIL si `MONEY_PUBLIC_ENABLED` fuera de money-gate.ts; self-check C. |
| T-73-03 | Tampering (guard mismo) | money-antiflip-guard.test.ts | mitigate | Mutation self-check en memoria: el guard prueba que muerde, no es no-op verde. |
| T-73-SC | Tampering | npm/pip/cargo installs | mitigate | Ninguna instalación en esta fase (RESEARCH §Standard Stack: cero paquetes nuevos); no aplica install checkpoint. |
</threat_model>

<verification>
- `pnpm --filter ./app test money-antiflip-guard` verde (3 vectores + self-check).
- `pnpm typecheck` limpio.
- git diff NO toca `money-gate.ts` ni `.env.example` (siguen fail-closed / `=false`).
</verification>

<success_criteria>
- `LEYENDA_ANTI_INSINUACION_MONEY` existe como constante única verbatim.
- `money-antiflip-guard.test.ts` verde con 3 vectores + mutation self-check.
- El flag permanece OFF; `.env.example=false`; el gate sigue `=== "true"`.
</success_criteria>

<output>
Create `.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-01-SUMMARY.md` when done
</output>
