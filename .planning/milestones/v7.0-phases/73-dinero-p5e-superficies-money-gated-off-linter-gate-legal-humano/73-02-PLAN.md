---
phase: 73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - app/components/contratos-de-parlamentario.tsx
  - app/components/financiamiento-de-parlamentario.tsx
  - app/components/contratos-por-contraparte.tsx
  - app/components/aportes-por-contraparte.tsx
  - app/components/contratos-de-parlamentario.test.tsx
  - app/components/financiamiento-de-parlamentario.test.tsx
  - app/components/contratos-por-contraparte.test.tsx
  - app/components/aportes-por-contraparte.test.tsx
autonomous: true
requirements: [MONEY-04]

must_haves:
  truths:
    - "Cada una de las 4 superficies MONEY renderiza la leyenda anti-insinuación MONEY 1× al tope de cada estado (encima del Intro)."
    - "La leyenda es la constante única importada, nunca inline duplicada."
    - "El copy RUT-vs-nombre se conserva y NO se conflaciona: contratos dice 'Enlazado por RUT', aportes dice 'Asociado por nombre confirmado'."
    - "La procedencia inline y el monto VERBATIM ya presentes se preservan; frescura por dato (corte/elección) intacta."
  artifacts:
    - path: "app/components/contratos-de-parlamentario.tsx"
      provides: "ContratosView con leyenda MONEY al tope de cada rama de estado"
      contains: "LEYENDA_ANTI_INSINUACION_MONEY"
    - path: "app/components/financiamiento-de-parlamentario.tsx"
      provides: "FinanciamientoView con leyenda MONEY (base nombre)"
      contains: "LEYENDA_ANTI_INSINUACION_MONEY"
    - path: "app/components/contratos-por-contraparte.tsx"
      provides: "ContratosPorContraparteView con leyenda MONEY"
      contains: "LEYENDA_ANTI_INSINUACION_MONEY"
    - path: "app/components/aportes-por-contraparte.tsx"
      provides: "AportesPorContraparteView con leyenda MONEY"
      contains: "LEYENDA_ANTI_INSINUACION_MONEY"
  key_links:
    - from: "app/components/*-parlamentario.tsx / *-contraparte.tsx"
      to: "app/lib/money-presentacion.ts"
      via: "import { LEYENDA_ANTI_INSINUACION_MONEY }"
      pattern: "LEYENDA_ANTI_INSINUACION_MONEY"
---

<objective>
Montar la leyenda anti-insinuación MONEY (constante única del plan 01) como primer hijo de cada rama de estado de las cuatro superficies MONEY, encima del `<Intro />`, con el tratamiento visual LOCKED del UI-SPEC (`border-l-[3px] border-[--primary] pl-2.5 mb-4`, `text-sm text-muted-foreground`). Extender los 4 `*.test.tsx` (RTL) para afirmar que la leyenda aparece en cada estado y que el copy RUT-vs-nombre se conserva sin conflación.

Purpose: MONEY-04 — "toda superficie de dinero lleva procedencia inline + leyenda anti-insinuación; el vínculo se afirma solo con base RUT-exacta". Las superficies ya llevan procedencia inline (`ProvenanceBadge`) y monto verbatim; el DELTA es la leyenda 1× por superficie.
Output: 4 componentes montan la leyenda; 4 test files la afirman por estado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-UI-SPEC.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-RESEARCH.md
@.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-01-SUMMARY.md
@app/AGENTS.md

<interfaces>
<!-- La constante creada en el plan 01: -->
From app/lib/money-presentacion.ts:
```typescript
export const LEYENDA_ANTI_INSINUACION_MONEY: string; // texto LOCKED verbatim
```

<!-- Estructura de cada View pura (multi-rama; la leyenda va PRIMERO en cada return): -->
From app/components/contratos-de-parlamentario.tsx:
```
export function ContratosView({ data }): 3 ramas de return
  - estado === "no_consultado"          → <><Intro/>…</>
  - estado === "consultado_sin_contratos" → <><Intro/>…</>
  - (enlazado)                          → <div><Intro/>…conteo neutro + lista</div>
  Intro() ya contiene: "Enlazado por RUT al parlamentario."   (línea ~144)
```
From app/components/financiamiento-de-parlamentario.tsx:
```
export function FinanciamientoView({ data }): ramas con <Intro/>
  Intro() ya contiene: "Asociado por nombre confirmado al candidato." (línea ~187)  // NUNCA "por RUT"
```
From app/components/contratos-por-contraparte.tsx:
```
export function ContratosPorContraparteView({...}): ramas con <Intro/>
```
From app/components/aportes-por-contraparte.tsx:
```
export function AportesPorContraparteView({...}): ramas con <Intro/>
```

<!-- Patrón visual LOCKED del rail (UI-SPEC §Leyenda): -->
```tsx
<p className="text-sm text-muted-foreground border-l-[3px] border-[--primary] pl-2.5 mb-4">
  {LEYENDA_ANTI_INSINUACION_MONEY}
</p>
```

<!-- RTL: los tests montan la VIEW PURA con fixtures (NO pasan por el gate). -->
From app/components/contratos-de-parlamentario.test.tsx:
```typescript
import { render, screen } from "@testing-library/react";
import { ContratosView, type ContratosViewData } from "./contratos-de-parlamentario";
function makeViewData(overrides): ContratosViewData { … }
// describe("ContratosView — redacción asociada al RUT …") ya existe → añadir aserción de leyenda por estado.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Montar la leyenda MONEY en las 2 superficies de ficha (contratos + financiamiento)</name>
  <files>app/components/contratos-de-parlamentario.tsx, app/components/financiamiento-de-parlamentario.tsx</files>
  <read_first>
    - app/lib/money-presentacion.ts (constante `LEYENDA_ANTI_INSINUACION_MONEY`)
    - app/components/contratos-de-parlamentario.tsx (las 3 ramas de return de `ContratosView`, cada una con `<Intro/>`; el `Intro()` con "Enlazado por RUT al parlamentario.")
    - app/components/financiamiento-de-parlamentario.tsx (ramas de `FinanciamientoView`; `Intro()` con "Asociado por nombre confirmado al candidato." — base NOMBRE, NUNCA "por RUT")
    - .planning/phases/73-.../73-UI-SPEC.md §Leyenda (colocación: primer hijo, encima del Intro; tratamiento visual `border-l-[3px] border-[--primary] pl-2.5 mb-4`)
    - .planning/phases/73-.../73-RESEARCH.md Open Question 1 (la leyenda va DENTRO de la View, primer hijo de CADA rama de estado — incluido el empty-state)
  </read_first>
  <action>
    Importar `LEYENDA_ANTI_INSINUACION_MONEY` desde `@/lib/money-presentacion` en ambos componentes. Definir un pequeño componente local `LeyendaMoney()` (o un fragmento reutilizable dentro del archivo) que renderice `<p className="text-sm text-muted-foreground border-l-[3px] border-[--primary] pl-2.5 mb-4">{LEYENDA_ANTI_INSINUACION_MONEY}</p>`, y colocarlo como PRIMER hijo de cada rama de return de la View, ANTES del `<Intro />`, en TODAS las ramas (no_consultado / consultado_sin_contratos / enlazado para contratos; las ramas equivalentes de financiamiento). La leyenda va 1× por render (no por fila ni por grupo). NO tocar el gate `moneyPublicEnabled()` de la Section (la leyenda vive en la View pura, que la RTL testea con fixtures). NO alterar el copy existente de `Intro()`: contratos conserva VERBATIM "Enlazado por RUT al parlamentario." y financiamiento conserva "Asociado por nombre confirmado al candidato." — las dos bases NUNCA se conflacionan (financiamiento jamás dice "por RUT"). Conservar `ProvenanceBadge`, monto VERBATIM (`c.monto ?? "No publicado"`, `font-mono`), fecha de corte y `Elección:` intactos. Respetar Next 16 (Server Components; leer `node_modules/next/dist/docs/` si se toca algo del boundary — aquí solo se añade markup estático a Views puras).
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/contratos-de-parlamentario.test.tsx components/financiamiento-de-parlamentario.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Ambos componentes importan `LEYENDA_ANTI_INSINUACION_MONEY` (nunca texto inline duplicado).
    - La leyenda aparece 1× encima del `<Intro/>` en CADA rama de estado de `ContratosView` y `FinanciamientoView`.
    - Contratos conserva "Enlazado por RUT al parlamentario."; financiamiento conserva "Asociado por nombre confirmado al candidato." y NO contiene "por RUT".
    - `ProvenanceBadge`, monto verbatim, fecha de corte y `Elección:` intactos.
    - `pnpm typecheck` limpio.
  </acceptance_criteria>
  <done>Las 2 superficies de ficha llevan la leyenda MONEY en todos sus estados sin conflar RUT-vs-nombre.</done>
</task>

<task type="auto">
  <name>Task 2: Montar la leyenda en las 2 superficies de /contraparte + RTL por estado en las 4</name>
  <files>app/components/contratos-por-contraparte.tsx, app/components/aportes-por-contraparte.tsx, app/components/contratos-de-parlamentario.test.tsx, app/components/financiamiento-de-parlamentario.test.tsx, app/components/contratos-por-contraparte.test.tsx, app/components/aportes-por-contraparte.test.tsx</files>
  <read_first>
    - app/lib/money-presentacion.ts (constante)
    - app/components/contratos-por-contraparte.tsx + app/components/aportes-por-contraparte.tsx (ramas de sus Views con `<Intro/>`)
    - Los 4 `*.test.tsx` de superficie (patrón `render(<XView data={makeViewData()} />)`; ya afirman gate y copy — extender con aserción de leyenda por estado)
    - .planning/phases/73-.../73-UI-SPEC.md §Leyenda (contraparte: 1× al tope de cada carril `#contratos` / `#aportes`)
  </read_first>
  <action>
    Repetir el montaje de la leyenda (mismo componente/fragmento `LeyendaMoney`, misma clase LOCKED, importando la constante) como primer hijo de cada rama de return de `ContratosPorContraparteView` y `AportesPorContraparteView`. Aportes-por-contraparte es base NOMBRE → jamás etiquetar "por RUT".

    Extender los CUATRO `*.test.tsx`: por cada estado que ya montan (no_consultado/consultado/enlazado o el equivalente por componente), añadir una aserción RTL de que `screen.getByText(...)` (o `container.textContent`) contiene el texto LOCKED de `LEYENDA_ANTI_INSINUACION_MONEY` (importar la constante en el test y afirmar contra ella, no un string re-tipeado — evita drift). Verificar además: contratos-de-parlamentario y contratos-por-contraparte afirman "Enlazado por RUT" (base RUT); financiamiento-de-parlamentario y aportes-por-contraparte afirman "Asociado por nombre confirmado"/base nombre y que NO aparece "por RUT". Añadir una aserción de que la leyenda aparece EXACTAMENTE 1× por render (p.ej. `getAllByText(leyenda).length === 1`), para cazar duplicación por fila. Conservar las aserciones existentes de gate/monto/provenance/corte/elección.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/contratos-por-contraparte.test.tsx components/aportes-por-contraparte.test.tsx components/contratos-de-parlamentario.test.tsx components/financiamiento-de-parlamentario.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Las 2 superficies de `/contraparte` montan la leyenda 1× por rama de estado.
    - Los 4 `*.test.tsx` importan `LEYENDA_ANTI_INSINUACION_MONEY` y afirman su presencia en CADA estado + exactamente 1× por render.
    - Los tests afirman la distinción RUT-vs-nombre por superficie (contratos → "Enlazado por RUT"; aportes/financiamiento → nombre confirmado, sin "por RUT").
    - Las aserciones previas (gate OFF ausente / monto verbatim / provenance / corte / elección) siguen verdes.
    - `pnpm --filter ./app test` verde.
  </acceptance_criteria>
  <done>Las 4 superficies llevan la leyenda MONEY, afirmada por RTL en cada estado, sin conflar RUT-vs-nombre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato MONEY → copy renderizado | El copy podría insinuar causalidad o conflar RUT-exacto con by-name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Repudiation / reputación | 4 superficies MONEY | mitigate | Leyenda anti-insinuación 1× por superficie (encima del Intro), afirmada por RTL en cada estado. |
| T-73-05 | Repudiation (difamación) | financiamiento/aportes (base nombre) | mitigate | Conservar "Asociado por nombre confirmado"; RTL afirma que NO aparece "por RUT" — nunca se conflaciona con RUT-exacto. |
| T-73-06 | Information Disclosure | RUT de terceros | accept | No se renderiza RUT de terceros (RPC no lo proyecta, `[VERIFIED]` en RESEARCH); este plan solo añade markup estático, no toca datos. |
</threat_model>

<verification>
- `pnpm --filter ./app test` verde (los 4 RTL + guards del plan 01).
- `pnpm typecheck` limpio.
- La leyenda aparece 1× por superficie por estado; RUT-vs-nombre no conflado.
</verification>

<success_criteria>
- 4 superficies renderizan `LEYENDA_ANTI_INSINUACION_MONEY` (importada, single-source) 1× por estado.
- RTL afirma leyenda + distinción RUT-vs-nombre en cada superficie.
- Procedencia inline + monto verbatim + frescura por dato preservados.
</success_criteria>

<output>
Create `.planning/phases/73-dinero-p5e-superficies-money-gated-off-linter-gate-legal-humano/73-02-SUMMARY.md` when done
</output>
