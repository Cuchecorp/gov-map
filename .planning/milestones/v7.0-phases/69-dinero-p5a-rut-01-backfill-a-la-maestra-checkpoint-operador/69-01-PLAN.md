---
phase: 69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/name-match-rut-guard.test.ts
autonomous: true
requirements: [RUT-01]

must_haves:
  truths:
    - "Un name-match NUNCA escribe el rut de la maestra: un RUT derivado por nombre viaja a revisionesRut/enqueueRevision (cola humana), jamás a runBackfillRut/runHarvestRut/updateRut"
    - "El único canal que alimenta el writer (cosechas) se dispara SOLO cuando la maestra ya tenía un rut que coincide (corroboración)"
    - "El guard MUERDE: un mutation self-check prueba que una violación sintética (name-only → cosechas) hace fallar el guard"
    - "El rut nunca se proyecta en una ruta/RPC pública (ya cubierto por lockdown-guard guard B; no se añade rut a ninguna superficie)"
  artifacts:
    - path: "app/lib/name-match-rut-guard.test.ts"
      provides: "Guard-guardián estático + de comportamiento del corte name-match≠write-rut, con mutation self-check"
      min_lines: 120
  key_links:
    - from: "app/lib/name-match-rut-guard.test.ts"
      to: "packages/dinero/src/reconciliar-contrato.ts"
      via: "lectura estática del árbol de fuentes (walkSourceFiles + stripTsComments)"
      pattern: "reconciliar-contrato"
    - from: "app/lib/name-match-rut-guard.test.ts"
      to: "packages/dinero/src/harvest-rut.ts"
      via: "aserción: ningún import de runBackfillRut/runHarvestRut/updateRut recibe revisionesRut"
      pattern: "revisionesRut"
---

<objective>
Congelar contra refactors futuros el corte estructural "name-match NUNCA escribe el `rut` de la maestra" (LOCKED, riesgo #1 = atribución financiera falsa por difamación). El corte YA es estructural en `reconciliar-contrato.ts` (canal `cosechas`/corroboración vs `revisionesRut`/cola humana), pero HOY no existe un test-guardián dedicado que muerda si un refactor rompe el corte.

Purpose: Un name-match que escriba un RUT ajeno = vínculo financiero falso, verificable como difamación. El guard es la defensa-en-profundidad que enfuerza el corte más allá de la convención de código.
Output: `app/lib/name-match-rut-guard.test.ts` — guard estático + de comportamiento, con mutation self-check, espejo de `lockdown-guard.test.ts` / `anti-insinuacion-guard.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-RESEARCH.md

<interfaces>
<!-- El corte a congelar vive en reconciliar-contrato.ts:369-407. Contratos que el guard consume. -->

De packages/dinero/src/reconciliar-contrato.ts (el corte CR-01, líneas 369-407):
- `cosechas: CandidatoCosechaRut[]` — CANAL CORROBORACIÓN. Se hace `cosechas.push(...)` SOLO dentro del bloque `if (rutMaestra != null && rutMaestra === rutNorm)`. Es el ÚNICO input de runHarvestRut/runBackfillRut/updateRut.
- `revisionesRut: CandidatoRevisionRut[]` — CANAL COLA HUMANA. Se hace `revisionesRut.push(...)` + `await encolarRevisionRut(writer, mencion, candidato)` en la rama `else if (nombreGlobalUnico)`. NUNCA fluye a un writer de escritura de RUT.
- Rama `else` (homónimo global): enlace se mantiene, NO se propone RUT.

De packages/dinero/src/harvest-rut.ts:
- `runHarvestRut(cosechas: CandidatoCosechaRut[], writer)` — recibe SOLO CandidatoCosechaRut, delega en runBackfillRut. Es el consumidor de `cosechas`, jamás de `revisionesRut`.

De packages/identity/src/backfill-rut.ts:
- `runBackfillRut(filas, writer)` / `RutBackfillWriter.updateRut(rows)` — el path de escritura a la maestra.

Molde a espejar (verbatim donde aplica) de app/lib/lockdown-guard.test.ts + app/lib/anti-insinuacion-guard.test.ts:
- `stripTsComments(content)` — quita `/* */` y `// …`, con skip de `://` (WR-05: no truncar URLs). REUSAR verbatim.
- `walkSourceFiles(dir)` + `SKIP_DIRS` — camina .ts/.tsx no-test, salta node_modules/.next/dist/etc.
- Patrón mutation self-check (anti-insinuacion Test 2): un detector PURO y testeable que se ejercita contra un fixture EN MEMORIA (string), probando que el guard SÍ falla ante una violación inyectada.
- Resolución de paths: `APP_ROOT = process.cwd()` (app/); `REPO_ROOT = path.resolve(APP_ROOT, "..")`; las fuentes de dinero viven en `path.join(REPO_ROOT, "packages", "dinero", "src")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard-guardián name-match≠write-rut (comportamiento + estático) con mutation self-check</name>
  <files>app/lib/name-match-rut-guard.test.ts</files>
  <read_first>
    - packages/dinero/src/reconciliar-contrato.ts (líneas 240-410: el corte CR-01 — `cosechas.push` dentro de `if (rutMaestra === rutNorm)`; `revisionesRut.push` + `encolarRevisionRut` en `else if (nombreGlobalUnico)`; homónimo global no propone RUT)
    - packages/dinero/src/harvest-rut.ts (runHarvestRut recibe SOLO CandidatoCosechaRut)
    - app/lib/lockdown-guard.test.ts (molde: stripTsComments con skip `://`, walkSourceFiles, SKIP_DIRS, APP_ROOT/REPO_ROOT, mensajes de offender)
    - app/lib/anti-insinuacion-guard.test.ts (molde del mutation self-check: detector puro ejercitado contra fixture en memoria, Test 2/3)
  </read_first>
  <behavior>
    - Test estático 1 (corroboración es el único input del writer): escanear packages/dinero/src/reconciliar-contrato.ts (post-stripTsComments). Aserta que TODA aparición de `cosechas.push` está sintácticamente dentro del bloque guardado por la comparación `rutMaestra === rutNorm` (corroboración de RUT ya presente), y que `revisionesRut` NUNCA aparece como argumento pasado a `runBackfillRut`/`runHarvestRut`/`updateRut` en el árbol de packages/dinero/src ni packages/identity/src.
    - Test estático 2 (canal humano no alimenta writer): aserta que `revisionesRut.push` co-ocurre con `encolarRevisionRut`/`enqueueRevision` y NUNCA con `updateRut`/`runBackfillRut`/`runHarvestRut` en la misma sentencia/edge.
    - Test de comportamiento 3 (fail-closed real): construir en memoria un caso persona-natural name-only (proveedor con nombre único en la maestra, sin `rut` presente que coincida) y correr `reconciliarContrato`; aserta `cosechas.length === 0` y `revisionesRut.length === 1` (el RUT derivado por nombre va a cola humana, jamás a cosecha).
    - Test de comportamiento 4 (corroboración escribe): un caso donde la maestra YA tiene el `rut` que coincide → `cosechas.length === 1`, y ese candidato SÍ es lo único que llegaría al writer.
    - Mutation self-check 5 (el guard muerde, estático): un detector PURO recibe un fixture EN MEMORIA que simula un refactor roto (`cosechas.push({ rutHarvested: rutCandidato })` FUERA del bloque `rutMaestra === rutNorm`, o `runBackfillRut(revisionesRut, ...)`) y DEBE reportar offender. Prueba que el guard no es un no-op verde.
    - Sin falsos positivos 6: un comentario/JSDoc que mencione `revisionesRut` junto a `updateRut` NO cuenta (stripTsComments lo remueve); el `://` de una URL no rompe el strip.
  </behavior>
  <action>
    Crear `app/lib/name-match-rut-guard.test.ts` como test de vitest, espejo de `lockdown-guard.test.ts`. REUSAR verbatim `stripTsComments` (con el skip de `://`, WR-05) y `walkSourceFiles`/`SKIP_DIRS`. Resolver las fuentes de dinero/identity vía `REPO_ROOT = path.resolve(process.cwd(), "..")` + `path.join(REPO_ROOT, "packages", "dinero", "src")` y `.../packages/identity/src`.

    Implementar un detector PURO y testeable (espejo de `detectarInsinuaciones` en anti-insinuacion-guard) — p.ej. `detectarViolacionesCorteRut(rawContent): string[]` — que sobre contenido post-strip: (a) verifique que ningún argumento pasado a `runBackfillRut`/`runHarvestRut`/`updateRut` sea `revisionesRut`; (b) verifique que cada `cosechas.push` esté dentro del bloque `if (rutMaestra === rutNorm)` (heurística estructural robusta: buscar el `cosechas.push` y confirmar que el `if (rutMaestra ... rutNorm)` que lo domina lo precede sin cierre de bloque intermedio; o el enfoque de RESEARCH: aserta que NO existe edge `revisionesRut → (runBackfillRut|runHarvestRut|updateRut)`). El detector es la pieza que el mutation self-check (behavior 5) ejercita contra un fixture en memoria.

    Para los tests de comportamiento (3, 4): importar `reconciliarContrato` de `@obs/dinero` y construir fixtures mínimos de `Contrato[]` + `Parlamentario[]` (maestra) — un caso name-only sin rut coincidente (→ revisión) y un caso con rut coincidente (→ cosecha). REUSAR el patrón de fixtures de `reconciliar-contrato` tests existentes; NO fabricar RUTs inválidos (usar un RUT DV-válido para no gatillar cuarentena en el paso 1).

    Mensajes de offender accionables (espejo lockdown): nombrar el archivo + la sentencia + "el canal `revisionesRut` es cola humana; NUNCA debe alimentar runBackfillRut/updateRut — un name-match no escribe el RUT de la maestra (CR-01, riesgo #1)".

    NO tocar `reconciliar-contrato.ts` ni `harvest-rut.ts` (el guard los PROTEGE, no los modifica). NO agregar `rut` a ninguna proyección/RPC público (guard B de lockdown ya lo cubre; NO duplicar ni relajar).
  </action>
  <verify>
    <automated>pnpm --filter ./app test -- name-match-rut-guard</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/name-match-rut-guard.test.ts` existe (≥120 líneas), corre en la suite de app.
    - Test de comportamiento: un contrato persona-natural name-only (sin rut coincidente) produce `cosechas.length === 0` y `revisionesRut.length === 1`; un contrato con rut coincidente produce `cosechas.length === 1`.
    - Guard estático: ninguna sentencia en packages/dinero/src ni packages/identity/src pasa `revisionesRut` a `runBackfillRut`/`runHarvestRut`/`updateRut`.
    - Mutation self-check: el detector puro reporta ≥1 offender ante un fixture en memoria que empuja un RUT name-only a `cosechas` o pasa `revisionesRut` a un writer.
    - Sin falsos positivos: un comentario que mencione los términos NO gatilla; `://` en URL no rompe el strip.
    - `pnpm --filter ./app test` sigue verde (no regresiona lockdown/anti-insinuacion).
  </acceptance_criteria>
  <done>El guard bloquea estáticamente y por comportamiento que un name-match escriba el RUT de la maestra, con mutation self-check que prueba que muerde. Suite app verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuente de RUT (nombre) → writer de la maestra | Un RUT derivado por nombre NO es propiedad establecida; cruzar este límite sin corroboración humana es atribución falsa |
| código de reconciliación → refactor futuro | Un refactor podría romper el corte estructural sin que nadie lo note (regresión silenciosa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering / Spoofing | reconciliar-contrato.ts (corte name-match≠write-rut) | mitigate | Este guard: aserta estáticamente que `revisionesRut` nunca alimenta `runBackfillRut`/`updateRut` + test de comportamiento fail-closed (name-only → 0 cosechas) |
| T-69-02 | Tampering | el guard mismo (no-op verde) | mitigate | Mutation self-check: el detector puro reporta offender ante un fixture en memoria con el corte roto |
| T-69-03 | Information Disclosure | proyección de `rut` en superficie pública | accept | Ya cubierto por lockdown-guard guard B (escanea app/ por `rut`); este plan NO añade `rut` a ninguna proyección/RPC |
</threat_model>

<verification>
- `pnpm --filter ./app test -- name-match-rut-guard` verde (comportamiento + estático + self-check).
- `pnpm --filter ./app test` verde completo (lockdown/anti-insinuacion no regresionan).
- `reconciliar-contrato.ts` y `harvest-rut.ts` sin modificar (git diff limpio salvo el nuevo test).
</verification>

<success_criteria>
- Un name-match NUNCA puede escribir el `rut` de la maestra sin que el guard falle (estático + comportamiento).
- El guard muerde ante una violación sintética (mutation self-check).
- Cero `rut` nuevo en superficies públicas (no se toca la allowlist).
</success_criteria>

<output>
Create `.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-01-SUMMARY.md` when done
</output>
