---
phase: 69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador
plan: 03
type: execute
wave: 2
depends_on: ["69-01", "69-02"]
files_modified:
  - .planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-BACKFILL-RUT-RUNBOOK.md
autonomous: false
requirements: [RUT-01]

must_haves:
  truths:
    - "Existe un runbook operador-LOCAL que documenta cómo poblar el seed Track B, correr runBackfillRut contra la maestra REMOTA vía db-url (DV-gate + provenance NOT NULL), y reportar cobertura N/M"
    - "El runbook declara que la escritura remota es checkpoint de operador (bloqueante duro): el agente NO ejecuta la escritura remota"
    - "El runbook exige DV-gate módulo-11 (isRutValido) + provenance NOT NULL; Track B (seed) es el default, Track A (SERVEL) solo corrobora, no sobre-escribe a ciegas"
    - "El operador reporta la cobertura N/M vía pnpm freshness (la señal RUT del Plan 02); el estado HOY es ≈ 0/M hasta poblar el seed"
  artifacts:
    - path: ".planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-BACKFILL-RUT-RUNBOOK.md"
      provides: "Runbook operador-LOCAL para el write remoto del RUT + reporte de cobertura, espejo de 66/67-BACKFILL-RUNBOOK.md"
      min_lines: 80
  key_links:
    - from: "69-BACKFILL-RUT-RUNBOOK.md"
      to: "packages/identity (runBackfillRut / SupabaseMaestraWriter.updateRut)"
      via: "instrucciones del operador para el write remoto vía db-url"
      pattern: "runBackfillRut"
    - from: "69-BACKFILL-RUT-RUNBOOK.md"
      to: "pnpm freshness (COBERTURA_RUT del Plan 02)"
      via: "reporte de cobertura N/M tras el backfill"
      pattern: "freshness"
---

<objective>
Entregar el handoff al operador: un runbook operador-LOCAL para la ESCRITURA REMOTA del RUT a la maestra (checkpoint bloqueante duro de TODO P5), espejo de `66-BACKFILL-RUNBOOK.md` / `67-BACKFILL-SENADO-RUNBOOK.md`. El agente construyó y validó el mecanismo offline (existe + testeado), el guard (Plan 01) y la medición (Plan 02); el operador puebla el seed con RUTs reales DV-válidos y corre la escritura remota. El agente NUNCA ejecuta la escritura remota ni fabrica RUTs.

Purpose: RUT-01 es el dato bloqueante duro de P5 (Phases 70/71/72). Sin RUT presente, el cruce de dinero rinde `null` o, peor, FALSO por name-match. La escritura es genuinamente operador-only (db-url sin cred en .env por diseño; RUTs reales son PII que el agente no posee).
Output: `69-BACKFILL-RUT-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-RESEARCH.md
@.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md

<interfaces>
<!-- El mecanismo YA EXISTE. El runbook lo INVOCA, no lo construye. -->

Mecanismo existente (packages/identity, packages/dinero):
- `runBackfillRut(filas: FilaRutCruda[], writer: RutBackfillWriter): ResultadoBackfill` — DV-gate (isRutValido, módulo-11) + provenance NOT NULL + fail-closed; devuelve `{ escritas, rechazadas }`. Idempotente por `id`.
- `aceptarRutBackfill` — rechaza DV inválido a `rechazadas["dv-invalido"]`, provenance faltante a `rechazadas["provenance-faltante"]`; NUNCA fabrica.
- `SupabaseMaestraWriter` (implements `RutBackfillWriter`) — `updateRut` por PK `id`; apunta al LOCAL por diseño; el write REMOTO es checkpoint operador (docstring writer-supabase.ts líneas 19-21).
- `SpyRutWriter` (in-memory, tests) — lo que el agente usa offline; NO es el path remoto.
- `runHarvestRut` — canal de CORROBORACIÓN (Track A / SERVEL): solo re-escribe un RUT ya presente; name-only → cola humana (guard Plan 01).

Fuentes de RUT (dos tracks, LOCKED):
- Track B (default de escritura): `supabase/seeds/parlamentario-rut.seed.json` — HOY `filas: []` (0 filas). El operador lo puebla con RUTs reales DV-válidos + provenance (origen, fecha_captura, enlace).
- Track A (corroboración): SERVEL / ChileCompra persona-natural — corrobora un RUT presente, no sobre-escribe a ciegas.

Estado / credenciales (RESEARCH Runtime State, A3):
- El write remoto usa `db-url` (MEMORY: write DB remoto solo vía `db push --db-url`; service key API ≠ PAT). El `.env` HOY NO tiene DB password apto para push remoto — por diseño. El runbook documenta QUÉ credencial usa el operador, NUNCA valores.
- Medición: `pnpm freshness` → tabla COBERTURA_RUT (Plan 02), ambas maestras (parlamentario + entidad_tercero).
- Maestras destino: `parlamentario.rut` (0005, RLS deny-by-default) y `entidad_tercero.rut` (0034, RLS + revoke). RUT nunca a anon/LLM/ruta pública.

Molde a espejar: `66-BACKFILL-RUNBOOK.md` (frontmatter, PRE-CHECKS offline, corrida LIVE gated, reporte de cobertura, criterios de cierre, rollback, seguridad de credenciales solo-nombres).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 69-BACKFILL-RUT-RUNBOOK.md (espejo 66/67)</name>
  <files>.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-BACKFILL-RUT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md (molde: frontmatter, quién corre esto, pre-checks offline, corrida gated, reporte de cobertura, criterios de cierre, rollback, seguridad de credenciales)
    - packages/dinero/src/harvest-rut.ts (checkpoint de operador: el writer real remoto es deuda de operador; el agente usa SpyRutWriter)
    - packages/identity/src/backfill-rut.ts (runBackfillRut / aceptarRutBackfill: DV-gate + provenance NOT NULL + fail-closed; ResultadoBackfill escritas/rechazadas)
    - packages/identity/src/backfill-entidad-cli.ts + packages/identity/src/seed-cli.ts (patrones de CLI operador LOCAL — identificar/confirmar el entrypoint real del write remoto)
    - supabase/seeds/parlamentario-rut.seed.json (Track B, filas: [] — el operador lo puebla)
    - packages/freshness/src/cli.ts (pnpm freshness → cómo el operador lee la cobertura RUT del Plan 02)
  </read_first>
  <action>
    Crear el runbook espejando `66-BACKFILL-RUNBOOK.md`. Frontmatter: `phase`, `plan: 03`, `tipo: runbook-operador-LOCAL`, `requirements: [RUT-01]`, `locked` (write remoto = checkpoint operador; DV-gate módulo-11 obligatorio; provenance NOT NULL; Track B default / Track A corrobora; name-match NUNCA escribe rut — guard Plan 01; RUT nunca a anon/LLM/ruta pública), `entry_point` (el CLI de backfill de RUT que corre runBackfillRut con SupabaseMaestraWriter — confirmar el nombre real del entrypoint en packages/identity, p.ej. `seed-cli.ts`/`backfill-entidad-cli.ts`; si no existe un CLI operador con writer remoto, documentarlo como gap explícito y describir el invocador que el operador debe usar/crear con db-url).

    Encabezado "QUIÉN CORRE ESTO": el operador, LOCAL; NO el agente, NO CI. Declarar que el agente que produjo este runbook NO ejecutó la escritura remota, NO tocó db-url, NO fabricó RUTs, y validó todo offline con SpyRutWriter (RESEARCH Pitfall 3: CI verde ≠ escritura remota probada).

    Secciones (espejo 66):
    1. PRE-CHECKS OBLIGATORIOS (offline, antes de cualquier write): (a) mecanismo verde — `pnpm --filter @obs/identity test` (backfill-rut: DV-gate + provenance + idempotencia) y `pnpm --filter @obs/dinero test` (harvest-rut + reconciliar-contrato); (b) guard name-match≠write-rut verde — `pnpm --filter ./app test -- name-match-rut-guard` (Plan 01); (c) las maestras destino tienen RLS deny-by-default (0005/0018/0034) — verificar contra la DB REMOTA que `rut` no es legible por anon; (d) `.env` / credencial de write remoto presente (solo NOMBRES; el db-url no está en .env por diseño → el operador provee la credencial).
    2. POBLAR EL SEED (Track B): cómo el operador llena `parlamentario-rut.seed.json` con RUTs reales DV-válidos + provenance (origen, fecha_captura, enlace). ADVERTENCIA LOCKED: NUNCA fabricar RUTs ni placeholders; el DV-gate rechaza inválidos a `rechazadas["dv-invalido"]` (fail-closed). Track A (SERVEL) solo corrobora un RUT presente, no sobre-escribe.
    3. CORRIDA LIVE (write remoto, gated): el comando real del entrypoint con `SupabaseMaestraWriter` apuntando al REMOTO vía db-url. Verificar el destino en el log ANTES de escribir (espejo Pitfall 5 de 66: si el log no dice REMOTO/PROD, DETENER). Idempotente por `id`. Correr para AMBAS maestras si aplica (parlamentario + entidad_tercero).
    4. REPORTE DE COBERTURA (RUT-01): tras el write, correr `pnpm freshness` y leer la tabla COBERTURA_RUT (Plan 02) — N/M de RUT DV-válido de ambas maestras. Interpretación honesta: "sin dato de RUT" ≠ "sin vínculos"; el techo se declara, no se infla. Reportar `escritas` / `rechazadas[dv-invalido]` / `rechazadas[provenance-faltante]` del ResultadoBackfill.
    5. CRITERIOS DE CIERRE: mecanismo verde + guard verde + seed poblado + write remoto corrido + cobertura N/M declarada vía freshness. Señal de reanudación/cierre para el checkpoint: el operador reporta "backfill hecho" con N/M concreto, o describe el problema.
    6. ROLLBACK / SEGURIDAD: idempotencia (re-correr = no-op por `id`); el RUT nunca a logs/anon/LLM; credenciales SOLO por nombre (NUNCA valores).

    NO ejecutar la escritura remota. NO poblar el seed con RUTs. NO tocar el mecanismo (existe + testeado). NO agregar `rut` a ninguna superficie pública.
  </action>
  <verify>
    <human-check>El operador confirma que el runbook es ejecutable: pre-checks offline pasan, el entrypoint del write remoto está identificado (o el gap documentado), el reporte de cobertura vía pnpm freshness es claro, y las advertencias LOCKED (no fabricar RUT, write = checkpoint operador) están presentes.</human-check>
  </verify>
  <acceptance_criteria>
    - `69-BACKFILL-RUT-RUNBOOK.md` existe (≥80 líneas) con frontmatter (phase/plan/tipo/requirements/locked/entry_point).
    - Declara la escritura remota como checkpoint operador (bloqueante duro); el agente NO la ejecuta.
    - Documenta: poblar Track B (RUTs reales DV-válidos + provenance, NUNCA fabricados), DV-gate módulo-11 + provenance NOT NULL, Track A corrobora (no sobre-escribe), verificación de destino REMOTO antes de escribir.
    - Referencia `pnpm freshness` (COBERTURA_RUT del Plan 02) como el reporte de cobertura N/M de ambas maestras.
    - Credenciales solo por NOMBRE (nunca valores); RUT nunca a logs/anon/LLM/ruta pública.
    - Pre-checks offline incluyen el guard del Plan 01 y los tests de mecanismo existentes.
  </acceptance_criteria>
  <done>El operador tiene un runbook ejecutable para el write remoto del RUT + el reporte de cobertura; el checkpoint queda documentado como su acto exclusivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint operador — escritura remota del RUT + reporte de cobertura</name>
  <files>(ninguno: checkpoint de operador; el write remoto no lo ejecuta el agente)</files>
  <action>
    CHECKPOINT DE OPERADOR — el agente NO ejecuta la escritura remota, NO toca db-url, NO fabrica ni puebla RUTs. Presentar el runbook (Task 1) al operador y PAUSAR hasta su veredicto. El write remoto a la maestra es el acto exclusivo del operador (bloqueante duro de TODO P5; sin credencial db-url en .env por diseño; RUTs reales son PII que el agente no posee). Ver `<how-to-verify>` para los pre-checks offline que SÍ corre el agente/operador y los pasos del write remoto que solo corre el operador.
  </action>
  <what-built>El agente entregó el mecanismo validado offline (existe + testeado con SpyRutWriter), el guard name-match≠write-rut (Plan 01), la medición de cobertura N/M de ambas maestras (Plan 02), y el runbook operador (Task 1). La ESCRITURA REMOTA a la maestra es el acto exclusivo del operador.</what-built>
  <how-to-verify>
    1. Correr los pre-checks offline del runbook: `pnpm --filter @obs/identity test` + `pnpm --filter @obs/dinero test` + `pnpm --filter ./app test -- name-match-rut-guard` — todos verde.
    2. Correr `pnpm freshness` y confirmar que la tabla COBERTURA_RUT aparece (ambas maestras), reportando el techo honesto HOY (≈ 0/M con el seed vacío).
    3. (Cuando el operador decida ejecutar el backfill) Poblar `parlamentario-rut.seed.json` con RUTs reales DV-válidos + provenance, correr el write remoto vía db-url por el runbook, y re-leer `pnpm freshness` para reportar la nueva cobertura N/M.
    4. Confirmar que el RUT NO es legible por anon en la DB REMOTA (RLS deny-by-default) y que ninguna ruta pública/RPC lo proyecta (lockdown-guard verde).
  </how-to-verify>
  <verify>
    <human-check>El operador confirma que los pre-checks offline pasan (mecanismo + guard verdes), que `pnpm freshness` declara la cobertura RUT de ambas maestras, y ejecuta (o difiere explícitamente) el write remoto — el agente no lo ejecutó.</human-check>
  </verify>
  <resume-signal>El operador escribe "backfill hecho" con la cobertura N/M concreta, o "mecanismo entregado, write remoto diferido" (el checkpoint queda como deuda de operador, sin bloquear el cierre del mecanismo), o describe el problema.</resume-signal>
  <done>El write remoto queda ejecutado por el operador (con cobertura N/M reportada) o explícitamente diferido como deuda de operador; el agente no tocó el remoto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente (CI) → maestra REMOTA (db-url) | El agente NUNCA escribe al remoto; el write es acto exclusivo del operador (sin cred en .env por diseño) |
| RUTs reales (PII) → seed / maestra | Solo el operador provee RUTs reales DV-válidos; el agente jamás fabrica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-08 | Elevation of Privilege / Tampering | escritura remota accidental desde CI | mitigate | Write = checkpoint operador (autonomous: false); sin credencial db-url en .env por diseño; el agente usa SpyRutWriter offline |
| T-69-09 | Tampering | RUT fabricado / DV falso escrito | mitigate | DV-gate módulo-11 (isRutValido) + provenance NOT NULL en runBackfillRut rechaza fail-closed; el runbook prohíbe fabricar RUTs |
| T-69-10 | Information Disclosure | db-url/credencial en el runbook | mitigate | El runbook referencia SOLO nombres de credenciales/env, nunca valores (espejo 66 §Seguridad de credenciales) |
| T-69-11 | Spoofing (falso "backfill hecho") | reporte de cobertura | accept | El operador reporta N/M concreto vía pnpm freshness (Plan 02); el estado HOY ≈ 0/M es el veredicto honesto hasta poblar el seed |
</threat_model>

<verification>
- `69-BACKFILL-RUT-RUNBOOK.md` existe y es ejecutable por el operador.
- Pre-checks offline verdes: mecanismo (identity/dinero) + guard (Plan 01).
- `pnpm freshness` muestra la cobertura RUT de ambas maestras (Plan 02).
- El write remoto queda como checkpoint operador; el agente no lo ejecutó.
</verification>

<success_criteria>
- El operador tiene un runbook ejecutable para el write remoto del RUT (DV-gate + provenance + Track B default) + el reporte de cobertura N/M.
- La escritura remota está declarada como checkpoint bloqueante duro (acto exclusivo del operador).
- Ningún RUT fabricado, ninguna credencial expuesta, ningún RUT en superficie pública.
</success_criteria>

<output>
Create `.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-03-SUMMARY.md` when done
</output>
