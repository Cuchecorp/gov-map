---
phase: 69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/query-runner.ts
  - packages/freshness/src/cli.ts
  - packages/freshness/src/evaluate.test.ts
autonomous: true
requirements: [RUT-01]

must_haves:
  truths:
    - "pnpm freshness imprime la cobertura N/M de RUT DV-válido para AMBAS maestras: parlamentario cruzable + entidad_tercero jurídica"
    - "La cobertura degrada honestamente: un count que no se pudo leer → n/d (null), NUNCA 0; M=0 → pct null (no divide por cero)"
    - "'sin dato de RUT' se reporta como techo honesto (N/M), no como 0 vínculos; el estado real HOY (seed vacío) es cobertura ≈ 0/M declarada correctamente"
    - "La señal RUT es un array SEPARADO — no rompe el denominador único del corpus (COBERTURA_SENALES) ni la señal de voto (COBERTURA_VOTO_SENALES)"
  artifacts:
    - path: "packages/freshness/src/catalog.ts"
      provides: "COBERTURA_RUT_SENALES (array separado, SQL 100% estática, dos denominadores/dos numeradores)"
      contains: "COBERTURA_RUT_SENALES"
    - path: "packages/freshness/src/query-runner.ts"
      provides: "queryCoberturaRut (read-only counts vía el mismo psql sin exponer dbUrl)"
      contains: "queryCoberturaRut"
    - path: "packages/freshness/src/cli.ts"
      provides: "renderCoberturaRut + append a la salida de pnpm freshness"
      contains: "renderCoberturaRut"
    - path: "packages/freshness/src/evaluate.test.ts"
      provides: "caso de cobertura RUT (degradación null/M=0) sobre evaluateCobertura"
      contains: "COBERTURA_RUT"
  key_links:
    - from: "packages/freshness/src/cli.ts"
      to: "packages/freshness/src/catalog.ts"
      via: "import COBERTURA_RUT_SENALES + evaluateCobertura + queryCoberturaRut"
      pattern: "COBERTURA_RUT_SENALES"
    - from: "packages/freshness/src/query-runner.ts"
      to: "parlamentario.rut / entidad_tercero.rut"
      via: "SELECT count(*) read-only (SQL estática, sin interpolación)"
      pattern: "rut IS NOT NULL"
---

<objective>
Medir y DECLARAR la cobertura N/M de RUT DV-válido como techo honesto (RUT-01), espejo EXACTO del patrón `COBERTURA_VOTO_SENALES` de Phase 68. Mide AMBAS maestras (RESEARCH A1 / Open Q1): `parlamentario` (cruzable) y `entidad_tercero` (jurídica) — porque el cruce de dinero de Phases 70/71 consulta ambas. "Sin dato de RUT" ≠ "sin vínculos": el operador debe ver el techo real (HOY ≈ 0/M por el seed vacío) sin fingir 0% ni 100%.

Purpose: El criterio de éxito de RUT-01 NO es "cobertura alta" sino "medición honesta declarada". El operador puebla el dato (checkpoint, Plan 03); esta señal reporta el techo verídico HOY y tras cada backfill.
Output: `COBERTURA_RUT_SENALES` + `queryCoberturaRut` + `renderCoberturaRut` en `@obs/freshness`, más un caso en `evaluate.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-RESEARCH.md

<interfaces>
<!-- El patrón a espejar (Phase 68 VOTO-05) YA existe en @obs/freshness. Reusar evaluateCobertura tal cual. -->

De packages/freshness/src/catalog.ts:
- `interface CoberturaSenalConfig { senal: string; etiqueta: string; sql: string; esDenominador: boolean }`
- `COBERTURA_VOTO_SENALES: CoberturaSenalConfig[]` — array SEPARADO (denominador propio = `count(DISTINCT votacion.id)`, no `proyecto`). Cada array marca su propio `esDenominador`. Este es el molde EXACTO a espejar.

De packages/freshness/src/query-runner.ts:
- `psql(dbUrl, sql)` — execFileSync sin shell, PGCLIENTENCODING=UTF8, timeout 15s; NUNCA imprime dbUrl/password (solo err.code). REUSAR.
- `interface CoberturaCount { senal: string; count: number | null }`
- `queryCoberturaVoto(dbUrl): CoberturaCount[]` — corre las SQL de COBERTURA_VOTO_SENALES; count null si psql falló / no numérico. Molde EXACTO.

De packages/freshness/src/evaluate.ts:
- `evaluateCobertura(counts, senales): CoberturaResult[]` — REUSAR TAL CUAL. M = count de la señal `esDenominador`; degrada honesto (count null → pct null; M=0 → pct null). NO tocar.
- `interface CoberturaResult { senal; etiqueta; n; m; pct; esDenominador }`

De packages/freshness/src/cli.ts:
- `renderCoberturaVoto(results): string` — tabla ANSI espejo; en `main()` se hace `queryCoberturaVoto` + `evaluateCobertura` + append a stdout/stderr y al JSON `{ frescura, cobertura, coberturaVoto }`. Molde EXACTO.

Columnas de las maestras (verificadas):
- `parlamentario`: `rut` (text NULLABLE, RLS deny-by-default 0005/0018), `estado` (check confirmado/probable/no_confirmado), `camara`, `periodo`.
- `entidad_tercero`: `rut` (text NULLABLE, RLS + revoke 0034), `tipo_entidad` (check natural/juridica), `estado`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COBERTURA_RUT_SENALES (catalog) + queryCoberturaRut (query-runner) + caso evaluate.test</name>
  <files>packages/freshness/src/catalog.ts, packages/freshness/src/query-runner.ts, packages/freshness/src/evaluate.test.ts</files>
  <read_first>
    - packages/freshness/src/catalog.ts (COBERTURA_VOTO_SENALES líneas 107-137: array separado, SQL estática, denominador propio — el molde exacto)
    - packages/freshness/src/query-runner.ts (queryCoberturaVoto líneas 169-176 + psql líneas 55-81: REUSAR psql, count null si falla)
    - packages/freshness/src/evaluate.ts (evaluateCobertura: M del esDenominador, degrada null/M=0 — REUSAR, no tocar)
    - packages/freshness/src/evaluate.test.ts (patrón de test de cobertura existente para añadir el caso RUT)
    - supabase/migrations/0005_parlamentario.sql + 0034_entidad_tercero.sql (columnas rut/estado/tipo_entidad)
  </read_first>
  <behavior>
    - `COBERTURA_RUT_SENALES` es un array SEPARADO con DOS bloques medidos, cada uno con su denominador y numerador. Bloque parlamentario: denominador = `count(*) FROM parlamentario WHERE estado = 'confirmado'` (universo cruzable), numerador = mismos + `rut IS NOT NULL AND rut <> ''`. Bloque entidad_tercero: denominador = `count(*) FROM entidad_tercero WHERE tipo_entidad = 'juridica'` (personas jurídicas = las cruzables por RUT exacto), numerador = mismos + `rut IS NOT NULL AND rut <> ''`.
    - NOTA de arquitectura de evaluateCobertura: `evaluateCobertura` toma UN solo `esDenominador` por array. Para medir DOS maestras con denominadores distintos, usar DOS arrays separados (`COBERTURA_RUT_PARLAMENTARIO_SENALES` y `COBERTURA_RUT_ENTIDAD_SENALES`), cada uno con su propio `esDenominador: true`, o un único array evaluado dos veces. Elegir el enfoque que NO rompa el contrato de `evaluateCobertura` (un denominador por evaluación).
    - `queryCoberturaRut(dbUrl)` corre las SQL vía el mismo `psql` read-only (NUNCA imprime dbUrl/password); count null si psql falló o el valor no es numérico. Espejo exacto de `queryCoberturaVoto`.
    - Test en evaluate.test.ts: con counts sintéticos (denominador leído, numerador null) → pct null (no 0); con M=0 → pct null; con N=0, M>0 → pct 0 (cobertura real cero es válida, distinta de n/d).
    - SQL 100% estática, sin interpolación de input (T-69-04, tampering) — igual que las SQL de VOTO.
  </behavior>
  <action>
    En `catalog.ts`: agregar el/los array(s) `COBERTURA_RUT_*_SENALES` espejando `COBERTURA_VOTO_SENALES` (mismo shape `CoberturaSenalConfig`, SQL estática). Documentar en JSDoc que es señal SEPARADA (no rompe el denominador del corpus ni el de voto), que mide AMBAS maestras (parlamentario cruzable + entidad_tercero jurídica) por RESEARCH A1, y que el techo honesto HOY es ≈ 0/M (seed `filas: []`, `rut` vacío). Las etiquetas legibles: "parlamentarios cruzables (universo)" / "con RUT DV-válido", "entidades jurídicas (universo)" / "con RUT DV-válido".

    En `query-runner.ts`: agregar `queryCoberturaRut(dbUrl): CoberturaCount[]` espejando `queryCoberturaVoto` — importar el/los nuevo(s) array(s), correr `psql` por señal, mapear count null si falla. REUSAR `psql` existente (no crear otro runner).

    En `evaluate.test.ts`: añadir un caso que ejercite `evaluateCobertura` con las señales RUT — probar degradación honesta (numerador null → pct null; M=0 → pct null; N=0/M>0 → pct 0). NO modificar `evaluate.ts`.

    NO agregar `rut` a ninguna proyección de app/ ni RPC — esto son counts agregados read-only, jamás filas con RUT. NO tocar `COBERTURA_SENALES` ni `COBERTURA_VOTO_SENALES`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test -- evaluate</automated>
  </verify>
  <acceptance_criteria>
    - `COBERTURA_RUT_*_SENALES` existe en catalog.ts con SQL estática que cuenta parlamentario (estado confirmado) y entidad_tercero (tipo_entidad juridica), numerador filtrado por `rut IS NOT NULL AND rut <> ''`.
    - `queryCoberturaRut` existe, reusa `psql`, devuelve count null cuando psql falla (nunca 0 falso).
    - Caso en evaluate.test.ts verde: numerador null → pct null; M=0 → pct null; N=0/M>0 → pct 0.
    - `pnpm --filter @obs/freshness test` verde; `pnpm --filter @obs/freshness typecheck` verde.
    - Ninguna SQL interpola input (100% estática).
  </acceptance_criteria>
  <done>La medición N/M de RUT DV-válido de ambas maestras existe como señal separada, degrada honesto, y no toca las señales de corpus/voto.</done>
</task>

<task type="auto">
  <name>Task 2: renderCoberturaRut + wire a pnpm freshness (cli.ts)</name>
  <files>packages/freshness/src/cli.ts</files>
  <read_first>
    - packages/freshness/src/cli.ts (renderCoberturaVoto líneas 163-196 + main() líneas 231-293: cómo se hace query + evaluate + append a stdout/stderr y al JSON)
    - packages/freshness/src/query-runner.ts (queryCoberturaRut de Task 1)
    - packages/freshness/src/catalog.ts (COBERTURA_RUT_*_SENALES de Task 1)
  </read_first>
  <action>
    En `cli.ts`: importar `queryCoberturaRut` + el/los `COBERTURA_RUT_*_SENALES`. Agregar `renderCoberturaRut(results): string` espejando `renderCoberturaVoto` (mismo layout de tabla ANSI: Señal | N | M (total) | N/M; `n/d` cuando pct null). Encabezado: "Cobertura de RUT DV-válido (RUT-01):" con subtítulo que declare "techo honesto — 'sin dato de RUT' ≠ 'sin vínculos'; el RUT es interno (minimización), nunca público".

    En `main()`: tras el bloque de coberturaVoto, correr `queryCoberturaRut` + `evaluateCobertura` (una vez por cada denominador/maestra) y APPEND la(s) tabla(s) a stdout (y a stderr en jsonMode), y añadir `coberturaRut` al objeto JSON `{ frescura, cobertura, coberturaVoto, coberturaRut }`. NO reemplazar ninguna tabla existente; solo append.

    Si la maestra se mide con dos arrays (dos denominadores), renderizar ambas sub-tablas bajo el mismo encabezado RUT-01. NUNCA imprimir el RUT crudo (son counts agregados). El CLI ya no falla por cobertura (solo staleness marca exit 1); mantener ese comportamiento.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness build && pnpm --filter @obs/freshness typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `renderCoberturaRut` existe y produce tabla ANSI con `n/d` para pct null.
    - `pnpm freshness` imprime la tabla de cobertura de RUT (ambas maestras) además de las tablas de corpus y voto; `--json` incluye `coberturaRut`.
    - `pnpm --filter @obs/freshness typecheck` verde; `pnpm --filter @obs/freshness test` verde.
    - El RUT crudo nunca se imprime (solo counts).
    - Las tablas de corpus (BUSQ-03) y voto (VOTO-05) siguen apareciendo sin cambios.
  </acceptance_criteria>
  <done>`pnpm freshness` declara la cobertura N/M de RUT DV-válido de ambas maestras como techo honesto, en append a las señales existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (columna PII `rut`) → salida del CLI del operador | El CLI cuenta filas con RUT pero NUNCA proyecta el RUT crudo (solo counts agregados) |
| SQL de cobertura ← configuración estática | La SQL no toma input del operador (evita injection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Tampering | queryCoberturaRut (SQL de cobertura) | mitigate | SQL 100% estática en catalog.ts, sin interpolación de input (espejo VOTO T-68-03) |
| T-69-05 | Information Disclosure | dbUrl/password en logs | mitigate | Reusa `psql` existente que nunca imprime dbUrl/password (solo err.code) |
| T-69-06 | Information Disclosure | RUT crudo en la salida del CLI | mitigate | El CLI imprime SOLO counts agregados (N/M); nunca `SELECT rut` — el RUT permanece interno (minimización 21.719) |
| T-69-07 | Spoofing (falso "100%") | render de cobertura | mitigate | evaluateCobertura degrada honesto: count null → n/d, M=0 → null, N=0 → 0% (nunca finge 0 ni 100) |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` verde (incluye el caso RUT de degradación honesta).
- `pnpm --filter @obs/freshness build && typecheck` verde.
- `pnpm freshness` (con SUPABASE_DB_URL) imprime la tabla de cobertura RUT de ambas maestras además de corpus + voto; el estado HOY reporta ≈ 0/M correctamente (seed vacío).
- Cero `SELECT rut` (solo counts).
</verification>

<success_criteria>
- La cobertura N/M de RUT DV-válido de AMBAS maestras (parlamentario + entidad_tercero) está medida y declarada como techo honesto en `pnpm freshness`.
- Degrada honesto (null/n/d), nunca finge 0% ni 100%.
- No rompe las señales de corpus ni de voto; no expone el RUT crudo.
</success_criteria>

<output>
Create `.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-02-SUMMARY.md` when done
</output>
