---
phase: 68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/ausencias-contexto.tsx
  - app/components/ausencias-contexto.test.tsx
  - app/lib/types.ts
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [VOTO-02, VOTO-04, VOTO-05]
must_haves:
  truths:
    - "El ciudadano ve su historial de votos individuales por sesión/proyecto (arco por proyecto, enlace a votación + proyecto) — NUNCA como 'alineamiento' ni 'rebeldía'"
    - "El carril de votaciones NO renderiza 'Votó distinto a su bancada' ni la comparación con la mediana de la cámara"
    - "La leyenda anti-insinuación verbatim aparece 1× al tope del detalle del carril, antes de cualquier dato; provenance inline por voto/arco"
    - "pareo y ausente en slate NEUTRO, jamás fundidos con el rojo de 'en contra'"
    - "La cobertura N/M + techo por causa se declara en la UI; probable/no_confirmado nunca se presenta como voto atribuido"
    - "rebeldias_de_parlamentario y tasa_ausencia_comparada quedan fuera de PUBLIC_RPC_ALLOWLIST (endurecimiento); las RPCs quedan inertes en la DB"
  artifacts:
    - path: "app/components/votos-por-parlamentario.tsx"
      provides: "Carril de votaciones podado + leyenda anti-insinuación + cobertura N/M + techo por causa"
      contains: "hecho observable"
    - path: "app/components/ausencias-contexto.tsx"
      provides: "ELIMINADO (comparativo mediana de cámara — diferido)"
  key_links:
    - from: "app/components/votos-por-parlamentario.tsx"
      to: "leyenda anti-insinuación verbatim"
      via: "primer hijo del detalle del carril"
      pattern: "hecho observable"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "PUBLIC_RPC_ALLOWLIST"
      via: "rebeldias_de_parlamentario y tasa_ausencia_comparada removidos"
      pattern: "PUBLIC_RPC_ALLOWLIST"
---

<objective>
Podar del render ciudadano los dos bloques prohibidos ("Votó distinto a su bancada" / RPC `rebeldias_de_parlamentario` y el comparativo con la mediana de la cámara / `AusenciasContexto` + RPC `tasa_ausencia_comparada`), eliminar `ausencias-contexto.tsx` entero, montar la leyenda anti-insinuación verbatim + provenance inline, declarar la cobertura N/M + techo por causa en la UI, y endurecer `PUBLIC_RPC_ALLOWLIST` quitando las dos RPCs podadas (quedan inertes en la DB).

Purpose: Cerrar el 360 del voto en la ficha de forma DESCRIPTIVA (VOTO-02), con leyenda + provenance (VOTO-04) y cobertura honesta en la UI (VOTO-05). La poda cierra el riesgo legal 17-LEGAL-DOSSIER (rebeldía/mediana = ítem diferido VOTOX v2).
Output: `votos-por-parlamentario.tsx` podado + leyenda + cobertura; `ausencias-contexto.*` borrado; `lib/types.ts` sin tipos huérfanos; allowlist endurecido; linter (Plan 01) verde tras la poda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-RESEARCH.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-UI-SPEC.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-CONTEXT.md

<interfaces>
<!-- Puntos EXACTOS de la poda. Verificados contra el snapshot 2026-07-14 (coinciden con RESEARCH). -->

De app/components/votos-por-parlamentario.tsx:
- import línea 7: `import { AusenciasContexto } from "@/components/ausencias-contexto";` → ELIMINAR
- import tipos líneas 19-20: `RebeldiaRow`, `AusenciaContextoRow` → ELIMINAR (dentro del `import type … from "@/lib/types"`)
- campo `rebeldias: RebeldiaRow[]` en `VotosViewData` línea 122 → ELIMINAR
- campo `ausenciaContexto?: AusenciaContextoRow | null` línea 160 (+ docblock 155) → ELIMINAR
- render `<AusenciasContexto data={data.ausenciaContexto ?? null} />` línea 709 → ELIMINAR (sub-bloque 3 "¿Falta más o menos que la mediana…?")
- bloque completo "Votó distinto a su bancada" líneas 829-886 (`<h3>Votó distinto a su bancada</h3>` + lista) → ELIMINAR (bloque 6)
- param/return `rebeldias` en `derivarVotosViewData` líneas 921, 987 → ELIMINAR
- fetch `sb.rpc("rebeldias_de_parlamentario", …)` líneas 1073-1083 → ELIMINAR
- fetch `sb.rpc("tasa_ausencia_comparada", …)` + spread líneas 1085-1104 → ELIMINAR; dejar `<VotosView id={id} data={data} />` limpio
- comentario cabecero líneas 26-37 menciona "votó distinto"/"rebeldías" → actualizar prosa
- `COBERTURA_BAJA_UMBRAL = 5` línea 269; nota de cobertura N proyectos líneas 783-789 (CONSERVAR + hacer incondicional cuando no es exhaustiva)
- nota "A favor / En contra se refiere a aprobar o rechazar…" línea 749 (CONSERVAR)
- `¿Cuándo votó?` <h3> línea 634 (la leyenda va al tope del detalle, ANTES de este bloque)

De app/lib/lockdown-guard.test.ts:
- `PUBLIC_RPC_ALLOWLIST` línea 165: contiene `rebeldias_de_parlamentario` (180) y `tasa_ausencia_comparada` (182) → ELIMINAR ambas líneas del Set (conservar `votos_de_parlamentario` 183)

<!-- La leyenda verbatim es NET-NEW (grep confirmó: NO existe hoy en las superficies de voto). -->
Leyenda anti-insinuación (verbatim LOCKED, UI-SPEC §Leyenda / Copywriting):
"Un voto es un hecho observable. Ausente o pareo no equivalen a votar en contra. No medimos disciplina ni motivo."

Copy techo por causa (verbatim, UI-SPEC §Cobertura, condicional):
"Algunas votaciones no se pueden atribuir individualmente porque la fuente publica el registro sin desglose nominal; se declara lo disponible."

Colores de sentido (LOCKED, fuente única app/lib/voto-presentacion.ts + voto-row.tsx):
pareo = bg-slate-400 / badge bg-slate-100 text-slate-600 · ausente = bg-slate-300 / badge bg-slate-100 text-slate-500 (SLATE, jamás rojo)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Poda de los dos bloques prohibidos + eliminar ausencias-contexto + limpiar tipos + endurecer allowlist</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/ausencias-contexto.tsx, app/components/ausencias-contexto.test.tsx, app/lib/types.ts, app/lib/lockdown-guard.test.ts, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx (los 9 puntos de poda del bloque interfaces arriba — leer las líneas 7, 19-20, 122, 155-160, 705-709, 829-886, 915-921, 987, 1073-1104)
    - app/components/ausencias-contexto.tsx (a borrar — verificar que NADIE más lo importa: RESEARCH confirma solo votos-por-parlamentario + su test)
    - app/lib/types.ts (RebeldiaRow, AusenciaContextoRow — borrar; verificar que ningún otro consumidor los use)
    - app/lib/lockdown-guard.test.ts (PUBLIC_RPC_ALLOWLIST líneas 165-184)
    - app/components/votos-por-parlamentario.test.tsx (fixtures que setean `rebeldias`/`ausenciaContexto` — quitarlas)
    - .planning/phases/68-.../68-RESEARCH.md (§Code Examples — tabla de puntos exactos + pitfall 4: poda coherente de los 4 puntos por bloque)
  </read_first>
  <behavior>
    - Test 1 (RTL): `VotosView` renderizado con datos NO contiene `<h3>Votó distinto a su bancada</h3>` (queryByText → null).
    - Test 2 (RTL): `VotosView` NO monta el comparativo de mediana de cámara (no hay texto "mediana de su cámara" ni el sub-bloque de `AusenciasContexto`).
    - Test 3 (build): `tsc -b` limpio tras quitar los tipos `RebeldiaRow`/`AusenciaContextoRow` y los campos de `VotosViewData` (ningún fetch `.rpc()` huérfano, ningún campo de tipo sin poblar).
    - Test 4 (guard): `lockdown-guard.test.ts` sigue verde con `rebeldias_de_parlamentario`/`tasa_ausencia_comparada` fuera del allowlist (el árbol público ya no las invoca tras la poda).
    - Edge: `ausencias-contexto.tsx` + su test dejan de existir; ningún import roto.
  </behavior>
  <action>
    Podar los 4 puntos coherentes por bloque en `votos-por-parlamentario.tsx` (RESEARCH pitfall 4): (rebeldía) import tipo, campo `VotosViewData.rebeldias`, param/return en `derivarVotosViewData`, fetch `rebeldias_de_parlamentario`, y el bloque JSX 829-886; (ausencias/mediana) import `AusenciasContexto`, campo `ausenciaContexto`, render `<AusenciasContexto/>`, fetch `tasa_ausencia_comparada`. Actualizar el docblock cabecero (26-37) para reflejar el nuevo orden (sin "votó distinto"/"rebeldías"). Borrar `app/components/ausencias-contexto.tsx` y `app/components/ausencias-contexto.test.tsx` (nadie más los importa). Borrar `RebeldiaRow` y `AusenciaContextoRow` de `app/lib/types.ts` (verificar que no queden consumidores). En `app/lib/lockdown-guard.test.ts` quitar `"rebeldias_de_parlamentario"` y `"tasa_ausencia_comparada"` de `PUBLIC_RPC_ALLOWLIST` (conservar `votos_de_parlamentario`) — endurecimiento: si un futuro re-monta la RPC, el guard lo caza (RESEARCH A2). En `votos-por-parlamentario.test.tsx`, quitar `rebeldias`/`ausenciaContexto` de todas las fixtures y añadir los asserts de ausencia (Test 1/2). NO borrar las RPCs de la DB (DDL destructivo gated — quedan inertes). NO usar bloques de código fuera de estos archivos.
  </action>
  <verify>
    <automated>cd app && pnpm test votos-por-parlamentario; pnpm test lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "AusenciasContexto\|rebeldias_de_parlamentario\|tasa_ausencia_comparada\|Votó distinto a su bancada\|RebeldiaRow\|AusenciaContextoRow" app/components/votos-por-parlamentario.tsx` → 0 matches (poda completa, comentarios incluidos).
    - `app/components/ausencias-contexto.tsx` y `.test.tsx` NO existen.
    - `PUBLIC_RPC_ALLOWLIST` ya no contiene `rebeldias_de_parlamentario` ni `tasa_ausencia_comparada`; sí conserva `votos_de_parlamentario`.
    - `pnpm --filter ./app test votos-por-parlamentario` y `test lockdown-guard` verdes.
    - `pnpm typecheck` (root) limpio (ningún `.rpc()` huérfano ni tipo sin usar).
    - El linter del Plan 01 (`anti-insinuacion-guard`) queda VERDE tras la poda (ningún término renderizado sobrevive).
  </acceptance_criteria>
  <done>Los dos bloques prohibidos están fuera del render, `ausencias-contexto` borrado, tipos huérfanos eliminados, allowlist endurecido, RPCs inertes en DB; suite y linter verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leyenda anti-insinuación verbatim + provenance inline + cobertura N/M + techo por causa en la UI</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx</files>
  <read_first>
    - app/components/votos-por-parlamentario.tsx (tope del detalle del carril, ANTES de `¿Cuándo votó?` línea 634; nota de cobertura 783-789; ProvenanceBadge ya presente por voto/arco ~538)
    - .planning/phases/68-.../68-UI-SPEC.md (§Leyenda — copy verbatim + tratamiento visual `text-sm text-muted-foreground border-l-[3px] border-[--accent-product] pl-2.5`; §Cobertura — techo por causa condicional; §Color regla neutral-slate)
    - .planning/phases/68-.../68-CONTEXT.md (leyenda verbatim LOCKED; pareo/ausente slate; probable/no_confirmado nunca atribuido)
    - app/lib/voto-presentacion.ts (labels + tokens de sentido — no duplicar literales de color)
  </read_first>
  <behavior>
    - Test 1 (RTL): la leyenda verbatim "Un voto es un hecho observable. Ausente o pareo no equivalen a votar en contra. No medimos disciplina ni motivo." aparece EXACTAMENTE 1× al tope del detalle del carril (getByText, no repetida por arco).
    - Test 2 (RTL): la nota "A favor / En contra se refiere a aprobar o rechazar el proyecto…" sigue presente (conservada).
    - Test 3 (RTL): cuando aplica el techo por causa, la línea verbatim de UI-SPEC aparece 1×; cuando no hay causa conocida, NO aparece (condicional, sin fabricar el techo).
    - Test 4 (RTL): la nota de cobertura N proyectos aparece cuando la cobertura no es exhaustiva.
    - Test 5 (RTL/token): pareo y ausente usan clases slate (`bg-slate-*` / `text-slate-*`) y NUNCA `bg-red-*`/`text-red-*` — verificable por el token de `voto-presentacion.ts` (no duplicar literales).
    - Test 6: provenance inline (`ProvenanceBadge`) presente por voto/arco (fuente/fecha/enlace) — conservado.
  </behavior>
  <action>
    Añadir la leyenda anti-insinuación verbatim (bloque 0) como PRIMER hijo del detalle del carril, encima de `¿Cuándo votó?`, con el tratamiento visual de UI-SPEC (`text-sm text-muted-foreground`, borde-izquierdo petróleo `border-l-[3px] border-[--accent-product] pl-2.5`). Copy EXACTO, sin variaciones. Añadir la línea de techo por causa (copy verbatim de UI-SPEC) como nota CONDICIONAL — solo cuando hay una causa conocida (RUT-bloqueado / PDF escaneado / sin desglose nominal); si no hay causa, omitir la línea (RESEARCH Open Question 3: condicional, no fabricar). Hacer la nota de cobertura N proyectos incondicional cuando la cobertura no es exhaustiva (conservar el copy). Conservar la nota "A favor / En contra…" (749), el `ProvenanceBadge` por voto/arco, y los tokens de sentido de `voto-presentacion.ts` (pareo/ausente en slate — NO tocar los literales de color, viven en la fuente única). Verificar que la RPC `votos_de_parlamentario` solo devuelve `confirmado` (ya es así); NO presentar `probable/no_confirmado` como voto atribuido (degrada a nombre crudo + IdentityMarker, ya presente en voto-row). Añadir los asserts RTL (Test 1-6) en `votos-por-parlamentario.test.tsx`. NO usar bloques de código fuera de estos archivos.
  </action>
  <verify>
    <automated>cd app && pnpm test votos-por-parlamentario</automated>
  </verify>
  <acceptance_criteria>
    - La leyenda verbatim aparece EXACTAMENTE 1× (RTL getByText del string completo); su ubicación es el tope del detalle, antes de `¿Cuándo votó?`.
    - El techo por causa es condicional (test cubre presente-cuando-aplica y ausente-cuando-no-hay-causa).
    - pareo/ausente en slate, nunca rojo (assert de clase/token; el checker hard-falla si se funden con "en contra").
    - `ProvenanceBadge` (fuente/fecha/enlace) presente por voto/arco; enlace a proyecto (`/proyecto/{boletin}`) y a votación conservados.
    - `probable/no_confirmado` NUNCA se presenta como voto atribuido a la persona.
    - `pnpm --filter ./app test votos-por-parlamentario` verde; `pnpm typecheck` limpio; el linter del Plan 01 verde (la leyenda no introduce términos prohibidos).
  </acceptance_criteria>
  <done>La leyenda anti-insinuación verbatim precede a los datos, provenance inline por voto, pareo/ausente en slate neutro, y la cobertura N/M + techo por causa se declaran honestamente en la UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (RPC) → render ciudadano | Un voto/ausencia presentado como insinuación de deslealtad es difamación/editorialización |
| árbol público → tablas PII | La poda no debe re-exponer PII (guard lockdown B sigue vigente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-06 | Repudiation/Info-disclosure | Bloques "Votó distinto a su bancada" / "mediana de cámara" | mitigate | PODA del render + linter (Plan 01) caza el vocabulario; RPCs inertes en DB |
| T-68-07 | Info disclosure | Exponer una RPC insinuante vía Data API | mitigate | Quitar `rebeldias_de_parlamentario`/`tasa_ausencia_comparada` de `PUBLIC_RPC_ALLOWLIST` — el guard caza cualquier re-invocación pública |
| T-68-08 | Repudiation | Fundir pareo/ausente con "en contra" (insinuación de postura) | mitigate | Slate neutro LOCKED en `voto-presentacion.ts`; assert de token en RTL; hard-fail del checker |
| T-68-09 | Info disclosure | Fuga de PII al podar/mover render | mitigate | La poda solo suprime render de RPCs ya PII-safe; guard lockdown (B) sigue escaneando `.from('<pii>')` |
| T-68-SC | Tampering | npm installs | accept | No se instala ningún paquete (RESEARCH §Package Legitimacy: no aplica) |
</threat_model>

<verification>
- `pnpm --filter ./app test votos-por-parlamentario` + `test lockdown-guard` + `test anti-insinuacion-guard` verdes.
- `pnpm typecheck` (root) limpio.
- `git grep` limpio de los términos podados en `votos-por-parlamentario.tsx`; `ausencias-contexto.*` inexistente.
- `PUBLIC_RPC_ALLOWLIST` sin las dos RPCs podadas.
- Suite completa `pnpm test` verde (baseline 751 no regresiona).
</verification>

<success_criteria>
El carril de votaciones es puramente descriptivo: historial por sesión/proyecto con enlace a votación + proyecto (VOTO-02), leyenda anti-insinuación verbatim + provenance inline con pareo/ausente en slate (VOTO-04), cobertura N/M + techo por causa en la UI (VOTO-05). Los bloques rebeldía/mediana están fuera del render y del allowlist; las RPCs quedan inertes en la DB.
</success_criteria>

<output>
Create `.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-03-SUMMARY.md` when done
</output>
