---
phase: 68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/query-runner.ts
  - packages/freshness/src/cli.ts
  - packages/freshness/src/evaluate.test.ts
autonomous: true
requirements: [VOTO-05]
must_haves:
  truths:
    - "El operador ve, en `pnpm freshness`, la cobertura del voto individual N/M por cámara (Cámara determinista confirmado, Senado por nombre) sin bucear SQL"
    - "La cobertura de voto NO rompe la semántica de denominador único del corpus de búsqueda (evaluateCobertura del array actual queda intacto)"
    - "Un count que no se pudo leer (psql falló) degrada a `n/d`/null honesto, NO a 0"
  artifacts:
    - path: "packages/freshness/src/catalog.ts"
      provides: "Array COBERTURA_VOTO_SENALES separado (denominador = sesiones de sala conocidas)"
      contains: "COBERTURA_VOTO_SENALES"
    - path: "packages/freshness/src/cli.ts"
      provides: "renderCoberturaVoto que imprime la tabla N/M del voto individual"
      contains: "renderCoberturaVoto"
  key_links:
    - from: "packages/freshness/src/cli.ts"
      to: "COBERTURA_VOTO_SENALES"
      via: "el main llama a queryCoberturaVoto + evaluateCobertura + renderCoberturaVoto"
      pattern: "COBERTURA_VOTO_SENALES|renderCoberturaVoto"
    - from: "packages/freshness/src/query-runner.ts"
      to: "psql read-only"
      via: "queryCoberturaVoto reusa el runner psql sin shell, PGCLIENTENCODING=UTF8"
      pattern: "queryCoberturaVoto|COBERTURA_VOTO_SENALES"
---

<objective>
Declarar la cobertura del voto individual en `pnpm freshness` como una señal N/M SEPARADA (`COBERTURA_VOTO_SENALES` + `renderCoberturaVoto`), con denominador propio (sesiones de sala conocidas) y dos cámaras (Cámara determinista confirmado, Senado por nombre). NO tocar la semántica de denominador único del corpus de búsqueda (`COBERTURA_SENALES`/`evaluateCobertura`).

Purpose: Cumplir la mitad-freshness de VOTO-05 (cobertura declarada honestamente). El operador ve N/M del voto individual sin SQL manual, con degradación honesta a `n/d` cuando la DB no responde.
Output: `COBERTURA_VOTO_SENALES` (catalog), `queryCoberturaVoto` (runner), `renderCoberturaVoto` + wiring en el main (cli), y casos de test en `evaluate.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-RESEARCH.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-UI-SPEC.md

<interfaces>
<!-- Patrón existente a espejar. Extraído del código real (líneas verificadas 2026-07-14). -->

De packages/freshness/src/catalog.ts:
- `interface CoberturaSenalConfig { senal: string; etiqueta: string; sql: string; esDenominador: boolean; }` (línea 47)
- `const COBERTURA_SENALES: CoberturaSenalConfig[]` (línea 58) — denominador ÚNICO `proyecto` (esDenominador:true una sola vez). NO tocar su semántica.

De packages/freshness/src/query-runner.ts:
- `function psql(dbUrl, sql): string` — read-only, sin shell (execFileSync), PGCLIENTENCODING=UTF8, NUNCA imprime dbUrl/password
- `function queryCobertura(dbUrl: string): CoberturaCount[]` (línea 148) — mapea COBERTURA_SENALES por psql; degrada a null (NO 0) cuando el count no se pudo leer

De packages/freshness/src/evaluate.ts:
- `function evaluateCobertura(counts, senales)` (línea 98) — divide cada señal por el denominador del array; ASUME UN solo denominador

De packages/freshness/src/cli.ts:
- `function renderCobertura(results): string` (línea 117) — tabla texto "Señal | N | M (total) | N/M"; degrada a "n/d"
- main (línea 207+): `evaluateCobertura(coberturaCounts, COBERTURA_SENALES)` → `renderCobertura(...)` → stdout
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir COBERTURA_VOTO_SENALES (array separado) + su runner</name>
  <files>packages/freshness/src/catalog.ts, packages/freshness/src/query-runner.ts</files>
  <read_first>
    - packages/freshness/src/catalog.ts (COBERTURA_SENALES líneas 58-83 + interface 47-56 — el molde declarativo)
    - packages/freshness/src/query-runner.ts (psql + queryCobertura líneas 148-155 — reusar el runner, NO re-escribir psql)
    - .planning/phases/68-.../68-UI-SPEC.md (§Cobertura — numerador = sesiones con voto individual confirmado; denominador = sesiones de sala conocidas; Cámara determinista vs Senado por nombre = dos números)
    - .planning/phases/68-.../68-RESEARCH.md (§Pattern 2 + Open Question 1 — recomendación: array separado; NO extender evaluateCobertura para no romper el denominador único del corpus)
  </read_first>
  <behavior>
    - `COBERTURA_VOTO_SENALES` existe como array separado de `CoberturaSenalConfig[]` con: un denominador propio (sesiones de sala conocidas en el período) marcado `esDenominador:true`, un numerador "Cámara — voto individual confirmado", y un numerador "Senado — voto por nombre (probable/no_confirmado)". SQL 100% estático (sin interpolación de input).
    - `queryCoberturaVoto(dbUrl)` mapea `COBERTURA_VOTO_SENALES` por el mismo `psql` read-only; un count no legible → null (NO 0).
    - Los SQL usan las tablas de voto reales (`voto`/`votacion`) con el filtro de `estado_vinculo='confirmado'` para el numerador Cámara y el filtro por nombre para Senado. Si el executor no puede confirmar el shape exacto de la tabla, usar la misma fuente que `scripts/verify-cobertura.sql` para consistencia; declarar el SQL como estático.
  </behavior>
  <action>
    En `catalog.ts`, añadir `export const COBERTURA_VOTO_SENALES: CoberturaSenalConfig[]` DESPUÉS de `COBERTURA_SENALES` (NO modificar `COBERTURA_SENALES` — su denominador único `proyecto` queda intacto per RESEARCH pitfall 3). El nuevo array: denominador = sesiones de sala conocidas (`esDenominador:true`, p.ej. `SELECT count(distinct votacion_id) FROM votacion;` o el conteo de sesiones que refleje el universo conocido); numerador Cámara = votaciones/sesiones con voto individual `confirmado` ingerido; numerador Senado = voto por nombre `probable/no_confirmado`. Etiquetas legibles ("sesiones de sala conocidas", "Cámara — voto confirmado", "Senado — voto por nombre"). SQL estático, sin interpolación. En `query-runner.ts`, añadir `export function queryCoberturaVoto(dbUrl: string): CoberturaCount[]` espejando `queryCobertura` (mapear `COBERTURA_VOTO_SENALES` por `psql`, degradar a null). Reusar `psql` tal cual (no re-implementar; nunca imprimir dbUrl). Importar `COBERTURA_VOTO_SENALES` en query-runner. NO usar bloques de código fuera de estos archivos.
  </action>
  <verify>
    <automated>cd packages/freshness && pnpm build 2>&1 | Select-String -NotMatch error; pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `COBERTURA_VOTO_SENALES` existe y es un array SEPARADO (grep-verificable); `COBERTURA_SENALES` sin cambios en su denominador único.
    - `queryCoberturaVoto` existe y reusa `psql` (no re-implementa el runner).
    - Todos los SQL son estáticos (sin `${...}` de input de usuario) — verificable por lectura.
    - `pnpm --filter @obs/freshness test` verde; `pnpm typecheck` limpio.
  </acceptance_criteria>
  <done>La señal de cobertura del voto individual existe como array + runner separados, con denominador propio y dos cámaras, sin tocar la semántica del corpus de búsqueda.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderCoberturaVoto + wiring en el main + casos de test</name>
  <files>packages/freshness/src/cli.ts, packages/freshness/src/evaluate.test.ts</files>
  <read_first>
    - packages/freshness/src/cli.ts (renderCobertura líneas 117-145 + main wiring 207-212 — el molde de render + el punto de llamada)
    - packages/freshness/src/evaluate.test.ts (evaluateCobertura BUSQ-03 líneas 114-166 — el molde de los casos N/M + degrade null)
    - packages/freshness/src/evaluate.ts (evaluateCobertura 98-116 — se reusa tal cual con el array de voto; funciona porque el array tiene su propio esDenominador)
  </read_first>
  <behavior>
    - `renderCoberturaVoto(results)` imprime una tabla N/M etiquetada "Cobertura del voto individual (VOTO-05)" con filas Cámara/Senado y el M (sesiones conocidas); degrada `n/d` como `renderCobertura`.
    - El main de `pnpm freshness` llama `queryCoberturaVoto` → `evaluateCobertura(..., COBERTURA_VOTO_SENALES)` → `renderCoberturaVoto` → stdout, ADEMÁS de la cobertura del corpus existente (no la reemplaza).
    - Test: `evaluateCobertura(counts, COBERTURA_VOTO_SENALES)` calcula N/M contra el denominador de sesiones (NO contra `proyecto`); un count null degrada a pct null; un denominador 0 no divide por cero.
  </behavior>
  <action>
    En `cli.ts`, añadir `renderCoberturaVoto(results: CoberturaResult[]): string` espejando `renderCobertura` (mismo layout de columnas, título "Cobertura del voto individual (VOTO-05):"). En el `main`, tras el bloque de cobertura del corpus, añadir: `const coberturaVotoCounts = queryCoberturaVoto(dbUrl);` → `evaluateCobertura(coberturaVotoCounts, COBERTURA_VOTO_SENALES)` → `renderCoberturaVoto(...)` → imprimir a stdout (append, no reemplazar la tabla del corpus). Importar `COBERTURA_VOTO_SENALES` y `queryCoberturaVoto`. En `evaluate.test.ts`, añadir un `describe("evaluateCobertura del voto (VOTO-05)")` con: caso feliz (Cámara N y Senado K contra M sesiones → pcts correctos), caso degrade (numerador null → pct null), caso denominador ausente/0 (no cae, pct null/0 honesto). NO usar bloques de código fuera de estos archivos.
  </action>
  <verify>
    <automated>cd packages/freshness && pnpm --filter @obs/freshness test evaluate</automated>
  </verify>
  <acceptance_criteria>
    - `renderCoberturaVoto` existe y el main lo llama (grep `renderCoberturaVoto` en cli.ts da ≥2 matches: definición + llamada).
    - La cobertura del corpus de búsqueda SIGUE imprimiéndose (no fue reemplazada) — verificable en el main.
    - `pnpm --filter @obs/freshness test evaluate` verde con los nuevos casos (feliz + degrade + denominador 0).
    - `pnpm typecheck` (root) limpio.
    - Corrida offline: `pnpm --filter @obs/freshness test` verde; la tabla del voto no requiere DB para el test (lógica pura).
  </acceptance_criteria>
  <done>`pnpm freshness` declara la cobertura del voto individual N/M por cámara además del corpus; los casos N/M están cubiertos por tests de lógica pura; la degradación honesta a n/d está probada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → SQL de cobertura | El SQL corre read-only contra la DB con `psql`; input estático |
| freshness stdout → operador | Cifras N/M; una completitud fingida sería deshonesta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | Tampering | SQL de la señal de cobertura de voto | mitigate | SQL 100% estático en `COBERTURA_VOTO_SENALES` (sin interpolación de input); `psql` vía execFileSync sin shell |
| T-68-04 | Info disclosure | dbUrl/password en logs de freshness | mitigate | Reusar `query-runner.ts` tal cual — nunca imprime dbUrl (WR-06) |
| T-68-05 | Repudiation | Fingir completitud del voto | mitigate | Degradación honesta a `n/d`/null (NO 0) cuando el count no se lee; N/M declarado, nunca "completo" |
| T-68-SC | Tampering | npm installs | accept | No se instala ningún paquete (RESEARCH §Package Legitimacy: no aplica) |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` verde (incluye los casos de cobertura de voto).
- `pnpm typecheck` (root) limpio.
- `git grep COBERTURA_VOTO_SENALES packages/freshness/src` devuelve matches en catalog + query-runner + cli.
- (operador, opcional) `pnpm freshness` con DB muestra la tabla "Cobertura del voto individual" además del corpus.
</verification>

<success_criteria>
La cobertura del voto individual está declarada en `pnpm freshness` como señal N/M separada por cámara, sin romper la semántica del corpus de búsqueda, con degradación honesta a n/d. Cumple la mitad-freshness de VOTO-05.
</success_criteria>

<output>
Create `.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-02-SUMMARY.md` when done
</output>
