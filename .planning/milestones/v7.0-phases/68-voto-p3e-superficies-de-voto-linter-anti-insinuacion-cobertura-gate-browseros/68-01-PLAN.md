---
phase: 68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [VOTO-04]
must_haves:
  truths:
    - "Un guard ejecutable falla si aparece un término de insinuación (rebeldía/disciplina/alineamiento/vota como/mediana de su cámara/similar a) en el texto renderizado de cualquier superficie de voto"
    - "El guard NO da falsos positivos por los ~15 usos legítimos de esos términos en comentarios/prosa (incl. page.tsx que LISTA los términos prohibidos)"
    - "El guard demuestra, con un self-check de mutación, que SÍ fallaría ante un término inyectado"
  artifacts:
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "Linter anti-insinuación como test de vitest (espejo de lockdown-guard.test.ts)"
      contains: "stripTsComments"
      min_lines: 80
  key_links:
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "app/components/votos-por-parlamentario.tsx"
      via: "walkSourceFiles escanea las superficies de voto"
      pattern: "walkSourceFiles|readFileSync"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "stripTsComments"
      via: "strippea comentarios antes de aplicar los regex de términos prohibidos"
      pattern: "stripTsComments"
---

<objective>
Crear el linter anti-vocabulario-insinuante como un test de vitest (`app/lib/anti-insinuacion-guard.test.ts`), espejo exacto de `app/lib/lockdown-guard.test.ts`. Escanea las superficies de voto y falla si aparece un término de insinuación en el texto renderizado (JSX/labels), tras strippear comentarios para no cazar los usos legítimos en prosa. Incluye un self-check de mutación que prueba que el guard fallaría ante un término inyectado.

Purpose: Formalizar la regla anti-insinuación (rector del proyecto, VOTO-04) como un artefacto ejecutable único, en vez de depender de comentarios por-componente + code-review. Es el gate que hace verificable la poda del Plan 02.
Output: `app/lib/anti-insinuacion-guard.test.ts` (net-new), verde y con self-check de mutación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-RESEARCH.md
@.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-UI-SPEC.md

<interfaces>
<!-- El molde a espejar. Extraído de app/lib/lockdown-guard.test.ts. -->
<!-- El executor DEBE reusar estas piezas — no re-inventar walker ni strip. -->

De app/lib/lockdown-guard.test.ts:
- `const APP_ROOT = process.cwd();` — vitest corre desde app/ (línea 43)
- `function stripTsComments(content: string): string` (línea 75) — quita `/* */` y `// …`,
  SALTA `://` de URLs para no crear falsos negativos. REUSAR esta técnica verbatim.
- `function walkSourceFiles(dir: string): string[]` (línea 104) — recorre .ts/.tsx,
  salta node_modules/.next y archivos *.test.*. REUSAR.
- Patrón de aserción: acumular `offenders: string[]`, `expect(offenders, "<mensaje accionable>").toHaveLength(0)`.

<!-- Regla clave (RESEARCH pitfall 2): sin stripTsComments hay ~15 falsos positivos,
     p.ej. ausencias-contexto.tsx "disciplina anti-fabricación" y page.tsx un comentario
     que LISTA los términos prohibidos. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear el guard anti-insinuación como test de vitest</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - app/lib/lockdown-guard.test.ts (EL MOLDE — copiar la técnica de `stripTsComments` líneas 75-102 y `walkSourceFiles` líneas 104-128; NO importar, reproducir para mantener el guard autocontenido como el original)
    - app/components/votos-por-parlamentario.tsx (superficie principal a escanear)
    - app/components/voto-row.tsx, app/components/votos-chart.tsx, app/components/voto-detalle.tsx (resto de superficies de voto)
    - app/lib/voto-presentacion.ts (fuente de verdad de labels de sentido)
    - app/app/parlamentario/[id]/page.tsx (sección VOTE — comentario ~167 que LISTA los términos prohibidos: DEBE strippearse)
    - .planning/phases/68-.../68-UI-SPEC.md (§Linter — lista dura de términos prohibidos, verbatim)
  </read_first>
  <behavior>
    - Test 1: para cada archivo de la lista de superficies de voto (`votos-por-parlamentario`, `votos-chart`, `voto-detalle`, `voto-row`/`voto-ficha-row`, `voto-presentacion`, y la sección VOTE de `page.tsx`), tras `stripTsComments`, NINGÚN término prohibido aparece → offenders === 0.
    - Test 2 (self-check de mutación): inyectar en un string de prueba (fixture en memoria, NO en un archivo del repo) un término prohibido (p.ej. "rebeldía") y verificar que el matcher del guard lo detecta → prueba que el guard NO es un no-op.
    - Test 3: un término prohibido dentro de un comentario `//` o `/* */` NO cuenta como offender (verifica que stripTsComments está activo).
    - Edge: `://` en una URL no debe romper el strip (heredado del molde).
  </behavior>
  <action>
    Crear `app/lib/anti-insinuacion-guard.test.ts` espejando `lockdown-guard.test.ts`. Reproducir `stripTsComments` y `walkSourceFiles` (misma técnica, mismo skip de node_modules/.next/*.test.*). Definir `SUPERFICIES_VOTO` como la lista explícita de rutas a escanear: `app/components/votos-por-parlamentario.tsx`, `app/components/votos-chart.tsx`, `app/components/voto-detalle.tsx`, `app/components/voto-row.tsx` (y `voto-ficha-row.tsx` si existe), `app/lib/voto-presentacion.ts`, y `app/app/parlamentario/[id]/page.tsx` (per VOTO-04, el linter cubre la sección VOTE de la ficha). Definir `TERMINOS_PROHIBIDOS` con la lista dura verbatim de UI-SPEC §Linter: rebeldía, rebelde, disciplina, indisciplina, alineamiento, alineado, afinidad, "cercanía política", lealtad, traición, díscolo, score, puntaje, índice, ranking, "nivel de acuerdo", "vota como", "similar a", "mediana de su cámara", y verbos causales dinero/lobby→voto ("financió su voto", "a cambio de"). Los términos con tilde (rebeldía, índice) se buscan con la tilde. Regla de matching (RESEARCH Open Question 2): cazar el término en el CONTENIDO STRIPPEADO (texto renderizado), NO como identificador; el identificador de RPC `rebeldias_de_parlamentario` en un `.rpc("…")` NO debe cazarse por sí mismo — usar un regex que exija límite de palabra en español o comillas de string JSX, de modo que `rebeldias_de_parlamentario` (snake_case, sin tilde) NO dispare pero `rebeldía`/`rebeldías` en prosa SÍ. Para cada archivo: `stripTsComments(readFileSync(...))`, luego por cada término aplicar el regex y acumular offenders con ruta + término. Aserción final `expect(offenders, "<mensaje accionable: qué archivo, qué término, y que la superficie de voto es descriptiva no de juicio>").toHaveLength(0)`. Añadir el `describe`/`it` de mutación (Test 2) probando el matcher contra un string inyectado en memoria. NO crear un CLI `node`/`tsx` ni un step de CI — es un `*.test.ts` que la suite recoge (RESEARCH anti-pattern 1). NO usar bloques de código fuera de este archivo.
  </action>
  <verify>
    <automated>cd app && pnpm test anti-insinuacion-guard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test anti-insinuacion-guard` verde.
    - El archivo contiene `stripTsComments` (grep-verificable) y escanea al menos `votos-por-parlamentario.tsx`.
    - El test de mutación existe y PASA (prueba que el guard fallaría ante un término inyectado) — sin él el guard sería un no-op indetectable.
    - CERO falsos positivos: el guard corre verde HOY contra el árbol actual (que ya contiene comentarios con los términos prohibidos) — demuestra que stripTsComments funciona. Nota: como el Plan 02 aún no podó el bloque rebeldía, el guard debe escanear SOLO texto renderizado post-strip; si el bloque "Votó distinto a su bancada" (JSX `<h3>Votó distinto a su bancada</h3>`) todavía existe, el guard PUEDE fallar por el término "distinto" NO listado (no es un término prohibido) — pero "rebeldía" en JSX text SÍ debe cazarse. Si el guard falla HOY por un término renderizado real (no comentario), documentarlo: el Plan 02 (poda) lo resuelve. En ese caso, marcar el test como el gate que el Plan 02 debe dejar verde (no relajar el guard).
    - `pnpm typecheck` (root) limpio.
  </acceptance_criteria>
  <done>El linter anti-insinuación existe como test de vitest, reusa stripTsComments, tiene self-check de mutación, y define el conjunto de términos prohibidos verbatim de UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → render ciudadano | Texto que llega al ciudadano; una insinuación aquí es difamación/editorialización |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Repudiation/Info-disclosure | Copy renderizado de superficies de voto | mitigate | El guard `anti-insinuacion-guard.test.ts` caza términos de juicio en el texto renderizado; corre en `pnpm test` + GSD verify-work |
| T-68-02 | Tampering | El guard mismo (podría degradar a no-op) | mitigate | Self-check de mutación: el test prueba que el guard FALLA ante un término inyectado |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | No se instala ningún paquete (RESEARCH §Package Legitimacy: no aplica) |
</threat_model>

<verification>
- `pnpm --filter ./app test anti-insinuacion-guard` verde con self-check de mutación.
- `pnpm typecheck` (root) limpio.
- `git grep stripTsComments app/lib/anti-insinuacion-guard.test.ts` devuelve match (reusa la técnica del molde).
</verification>

<success_criteria>
El linter anti-insinuación es un artefacto ejecutable único (test de vitest), reusa stripTsComments (sin falsos positivos por comentarios), tiene self-check de mutación, y establece el conjunto de términos prohibidos verbatim de UI-SPEC. Es el gate que hace verificable la poda del Plan 02.
</success_criteria>

<output>
Create `.planning/phases/68-voto-p3e-superficies-de-voto-linter-anti-insinuacion-cobertura-gate-browseros/68-01-SUMMARY.md` when done
</output>
