---
phase: 71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dinero/src/ingest-run-servel.ts
  - packages/dinero/src/ingest-run-servel.test.ts
autonomous: true
requirements: [MONEY-02, DEBT-01]
must_haves:
  truths:
    - "La ingesta SERVEL escribe PRIMERO los BYTES del .xlsx content-addressed a R2 (Etapa 1) y LUEGO parsea/upsert (Etapa 2), re-ejecutables por separado"
    - "Un fallo del put R2 (no-412) GATEA el upsert de ESA elección — nunca hay derivado sin crudo reconstruible"
    - "--from-r2 lee los bytes del .xlsx que el operador colocó en R2 y corre parse→reconcilia→upsert SIN tocar el blob (0 fetch a la fuente)"
    - "El cruce candidato→parlamentario es por NOMBRE determinista: solo `determinista` mintea el FK, todo lo demás → parlamentario_id NULL (nunca falso por nombre)"
    - "ServelBloqueadaError y la cuarentena de drift degradan ESA elección sin abortar la corrida (fail-soft por elección)"
    - "eleccion + fecha_corte sobreviven verbatim el replay (fecha de corte/elección visible por dato)"
  artifacts:
    - path: "packages/dinero/src/ingest-run-servel.ts"
      provides: "runIngestServel con r2Store/snapshotWriter/fromR2 + modo LOCAL (tarea con r2Path)"
      contains: "putImmutable"
    - path: "packages/dinero/src/ingest-run-servel.test.ts"
      provides: "Tests fake-R2 (put-antes-upsert, from-r2 0-fetch, put-falla-gatea, 412-idempotente, fail-soft por elección, eleccion/fecha_corte verbatim) + fixture .xlsx exceljs in-test"
      contains: "FakeR2Store"
  key_links:
    - from: "packages/dinero/src/ingest-run-servel.ts"
      to: "opts.r2Store.putImmutable"
      via: "Etapa 1 R2 (bytes .xlsx) antes de parse/upsert"
      pattern: "putImmutable\\(\\s*\"servel\""
    - from: "packages/dinero/src/ingest-run-servel.ts"
      to: "opts.r2Store.getObject"
      via: "modo LOCAL --from-r2 lee los bytes .xlsx"
      pattern: "getObject"
---

<objective>
Añadir la Etapa 1 R2 (crudo BYTES del `.xlsx` fuente/operador→R2 content-addressed) a `runIngestServel`, que hoy fetchea el blob de Azure y sube el crudo a Supabase Storage DESPUÉS de parsear (no es R2-primero). Añadir un modo LOCAL `--from-r2` donde la Etapa 1 la hizo el operador (colocar el `.xlsx` en R2) y el pipeline lee los bytes de R2 sin tocar la fuente. Espejar exactamente el molde de dos-etapas que Phase 70 (`ingest-run.ts`) aplicó a ChileCompra, adaptado a que el crudo SERVEL son los BYTES binarios del `.xlsx`, no un envelope JSON.

Purpose: Funde DEBT-01 (dos-etapas R2 + `--from-r2`) para SERVEL y cumple la porción de MONEY-02 que dice "el operador deja el .xlsx en R2 y el pipeline re-corre sin volver a tocar la fuente". El wire es PURO plumbing sobre primitivos ya probados; el único código de dominio nuevo es leer bytes `.xlsx` de R2 en el modo LOCAL. NO se toca `reconciliar-aporte.ts`, `model-servel.ts`, ni `0024_servel.sql`.
Output: `runIngestServel` con `r2Store`/`snapshotWriter`/`fromR2` + tarea con `r2Path`; tests fake-R2 con fixture `.xlsx` exceljs in-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-RESEARCH.md
@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-VALIDATION.md
@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-01-PLAN.md

<interfaces>
<!-- Contratos extraídos del código. Usar directamente — sin exploración. -->

From packages/dinero/src/ingest-run-servel.ts (el archivo a MODIFICAR):
```typescript
export interface TareaEleccion { eleccion: string; url: string; anio?: string | null; declaredRowCount?: number | null; }
export type SubirCrudoFn = (eleccion: string, fechaCorte: string, bytes: Uint8Array) => Promise<string>;
export interface RunIngestServelOpts {
  conector: ServelConnector;
  writer: ServelWriter;
  maestra: Parlamentario[];
  tareas: TareaEleccion[];
  reconciliar?: ReconciliarAporteOpts;
  subirCrudo?: SubirCrudoFn;   // Supabase Storage — se DEGRADA a secundario best-effort (ver decisión)
  fechaCorte?: string;
  log?: (msg: string) => void;
  // ← AÑADIR: r2Store?, snapshotWriter?, fromR2?
}
// El loop por-tarea (líneas 107-230): 0.guarda eleccion/url · 1.descargar (ServelBloqueadaError→continue) ·
// 2.parseAportes (THROW→cuarentena run · continue) · 3.reconciliarCompletitud (!ok→cuarentena · continue) ·
// 4.subirCrudo→reconciliarAporte→upsertDonantes→upsertAportes. El bloque Etapa 1 R2 va en el paso 4
// (bytes ya en mano tras descargar/leer), ANTES de subirCrudo y ANTES de upsert.
```

From packages/dinero/src/connector-servel.ts:
```typescript
async descargar(url: string): Promise<{ bytes: Uint8Array; anclas: {...}; byteLength: number }>;
export class ServelBloqueadaError extends Error { status: number; }
```

From @obs/ingest (r2-store.ts):
```typescript
export async function sha256Hex(body: Uint8Array): Promise<string>;
export class R2Store {
  putImmutable(source, resource, dateBucket, sha, ext, bytes): Promise<{ r2Path: string; existed: boolean }>;
  getObject(r2Path: string): Promise<Uint8Array>;
}
export type SnapshotWriter; // .write({source, resource, cacheKey, r2Path, contentHash, fingerprint, dateBucket, provenance})
```

From packages/dinero/src/parse-servel.ts:
```typescript
export const HEADER_ROW = 4;                       // los headers viven en la FILA 4
export const EXPECTED_HEADERS: readonly string[];  // los 11 literales UPPERCASE VERBATIM (leer el archivo para copiarlos exactos)
export async function parseAportes(bytes: Uint8Array, ctx: { anio; fechaCorte; enlace }): Promise<...>;
```
</interfaces>
</context>

<decisions>
## Resolución de las open questions del research (LOCKED en este plan)

1. **`subirCrudo` (Supabase Storage) NO es la Etapa 1.** Grep de aceptación confirma que ningún consumidor LEE el crudo de Supabase Storage (`storage-supabase.ts`/`writer-supabase-servel.ts` solo ESCRIBEN; no hay reader). Decisión: R2 es la ÚNICA verdad cruda gateante (Etapa-1-primero, put fallido no-412 GATEA); `subirCrudo` se DEGRADA a secundario best-effort que corre DESPUÉS del put R2 exitoso y NUNCA gatea (su fallo no aborta ni impide el upsert). No se retira (evita romper el writer Supabase existente), pero deja de ser la Etapa 1.
2. **Umbral de frescura SERVEL = 365d [ASSUMED]**, override por `FRESHNESS_UMBRAL_SERVEL` (se implementa en Plan 71-02). SERVEL es LOCAL bianual/cuatrienal → umbral generoso; la frescura POR DATO (`eleccion`+`fecha_corte` en la fila) es la señal rectora, la staleness operativa es secundaria.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests fake-R2 + fixture .xlsx exceljs in-test en ingest-run-servel.test.ts</name>
  <files>packages/dinero/src/ingest-run-servel.test.ts</files>
  <read_first>
    - packages/dinero/src/ingest-run-servel.ts (el runner completo — el bloque Etapa 1 R2 se inserta en el paso 4, tras `descargar`/lectura-R2 y ANTES de subirCrudo/upsert; el modo LOCAL entra antes del paso 1)
    - packages/dinero/src/ingest-run.ts (Phase 70 — el MOLDE Etapa-1-R2-primero + `--from-r2`: sha256Hex→putImmutable→catch gatea (errores.push + continue)→existed 412 skip→snapshotWriter best-effort→conector fake que LANZA en replay)
    - packages/dinero/src/ingest-run.test.ts (Phase 70 — patrón FakeR2Store putImmutable/getObject + writer con contador monotónico compartido para el ORDEN de captura Test A)
    - packages/dinero/src/parse-servel.ts (HEADER_ROW=4, EXPECTED_HEADERS — copiar los 11 literales EXACTOS para el fixture; un header renombrado dispara el gate de drift → útil para el test de cuarentena)
    - packages/dinero/src/reconciliar-aporte.ts (la firma de reconciliarAporte y ReconciliarAporteOpts — para verificar que el cruce por NOMBRE deja no_confirmado→parlamentario_id NULL sin tocarlo)
    - packages/dinero/src/writer-servel.ts (InMemoryServelWriter / ServelWriter — upsertDonantes/upsertAportes/marcarIngestado; usar como writer de test o subclasear para trackear orden)
    - packages/dinero/src/connector-servel.ts (ServelConnector.descargar + ServelBloqueadaError — para el conector fake que sirve bytes en el camino normal y que LANZA en replay)
  </read_first>
  <behavior>
    - Fixture: `fakeXlsxAportes()` construye un `.xlsx` in-test con exceljs — headers VERBATIM en la FILA 4 (los 11 EXPECTED_HEADERS), una fila de aporte válida en fila 5; devuelve `Uint8Array`. Un helper `fakeXlsxDrift()` renombra un header (para el test de cuarentena por drift). Sin binario en git.
    - Test A (put-antes-upsert): con `r2Store` (FakeR2Store) + un `OrderTrackingWriter` que comparte un contador monotónico con el store, la primera `putImmutable("servel", ...)` (Etapa 1) captura un orden ESTRICTAMENTE menor que la primera `upsertAportes` (Etapa 2) — por orden de captura, no por presencia. DEBT-01.
    - Test B (--from-r2 / LOCAL 0-fetch): con `fromR2` (o una tarea con `r2Path`) apuntando a un r2Path, `runIngestServel` lee los bytes del `.xlsx` de `getObject`, parsea→reconcilia→upsert, y el `conector` inyectado LANZA en `descargar` si se invoca → prueba 0 fetch al blob. Guard: `fromR2` sin `r2Store` → lanza error de args.
    - Test C (put falla gatea upsert): FakeR2Store cuyo `putImmutable` LANZA (no-412) → la elección se registra en `errores` y NO se llama `upsertAportes` para ESA elección. Etapa-1-primero LOCKED.
    - Test D (412 idempotente): FakeR2Store cuyo `putImmutable` devuelve `existed=true` → skip de la Etapa 2 (0 upsert) en el camino normal; en modo LOCAL puro con `--from-r2` explícito, `getObject` lee siempre.
    - Test E (fail-soft por elección): dos tareas en una corrida — elección A con `fakeXlsxDrift()` (cuarentena run-level → 0 filas A) + elección B con `fakeXlsxAportes()` (ok). El resultado tiene A en `cuarentenados`/`degradaciones` y B con aportes>0. Análogo: elección A que lanza `ServelBloqueadaError` (fake conector) degrada A sin abortar B.
    - Test F (eleccion + fecha_corte verbatim tras replay): tras el replay `--from-r2`, la fila `aporte` capturada por el writer conserva `eleccion` (string compuesto verbatim) y `fecha_corte` byte-idénticos a los del parse (no recomputados por el wire).
  </behavior>
  <action>Escribir/extender `ingest-run-servel.test.ts` con: (1) `fakeXlsxAportes()`/`fakeXlsxDrift()` que construyen el `.xlsx` in-test con `exceljs` (headers en la FILA 4 leyendo `EXPECTED_HEADERS` de `parse-servel.ts` — copiar los 11 literales exactos; el drift renombra uno). (2) `FakeR2Store` con `putImmutable(source,resource,dateBucket,sha,ext,bytes)` y `getObject(r2Path)`; para el modo LOCAL, `getObject` devuelve los bytes del fixture. (3) `OrderTrackingWriter` (subclase/wrapper de `InMemoryServelWriter`) que comparte un contador monotónico con el store para el ORDEN de captura (Test A). El conector fake del camino normal sirve `descargar`→`{bytes: fakeXlsx, anclas, byteLength}`; para el modo LOCAL `descargar` LANZA (Test B). Test C usa un store cuyo `putImmutable` rechaza; Test D uno que devuelve `existed:true`; Test E combina drift/bloqueada con una elección ok; Test F asevera `eleccion`/`fecha_corte` verbatim en las filas capturadas. Estos tests FALLAN ahora (RED) porque `RunIngestServelOpts` aún no tiene `r2Store`/`fromR2` ni la tarea acepta `r2Path`. Commit `test(71-01)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test ingest-run-servel 2>&1 | grep -qi "fail\|FAIL\|✗\|r2Store\|fromR2" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `ingest-run-servel.test.ts` contiene `fakeXlsxAportes()`/`fakeXlsxDrift()` (exceljs, headers fila 4) y `FakeR2Store` con `putImmutable`+`getObject`.
    - Test A: orden de captura (put `"servel"` < upsertAportes) vía contador monotónico compartido, no por presencia.
    - Test B: modo LOCAL/`--from-r2` con conector fake que LANZA en `descargar` (0 fetch); guard `fromR2` sin `r2Store` lanza.
    - Test C: `putImmutable` que lanza → elección en `errores`, `upsertAportes` NO llamado para esa elección.
    - Test D: `existed:true` → skip Etapa 2 (camino normal).
    - Test E: elección A cuarentenada por drift (o ServelBloqueadaError) + elección B ok en la misma corrida (fail-soft por elección).
    - Test F: `eleccion` + `fecha_corte` byte-idénticos tras replay.
    - Los tests fallan ahora (RED) — la suite reporta fallo referenciando `r2Store`/`fromR2`/`r2Path` inexistentes.
  </acceptance_criteria>
  <done>El archivo de test define los 6 casos + el fixture exceljs offline; corre y falla (RED) porque el runner aún no acepta `r2Store`/`snapshotWriter`/`fromR2`/`r2Path`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Etapa 1 R2 (bytes .xlsx) + modo LOCAL --from-r2 en runIngestServel</name>
  <files>packages/dinero/src/ingest-run-servel.ts</files>
  <read_first>
    - packages/dinero/src/ingest-run-servel.ts (RunIngestServelOpts líneas 55-69; el loop paso 1-4 líneas 107-230 — la Etapa 1 R2 se inserta en el paso 4 tras tener `bytes`, ANTES de subirCrudo/upsert; el modo LOCAL entra al tope del loop, saltando descargar)
    - packages/dinero/src/ingest-run.ts (Phase 70 — molde VERBATIM del bloque Etapa 1 R2: sha256Hex→putImmutable→catch (errores.push redactado + continue)→existed 412 skip→snapshotWriter best-effort; y el bloque `--from-r2`: getObject→construir conector fake→re-entrar al flujo reusando el writer resuelto)
    - packages/dinero/src/parse-servel.ts (parseAportes — se llama igual sobre los bytes leídos de R2)
    - @obs/ingest export: `import { sha256Hex, type R2Store, type SnapshotWriter } from "@obs/ingest"` (ver ingest-run.ts de Phase 70)
  </read_first>
  <action>Añadir a `RunIngestServelOpts`: `r2Store?: R2Store`, `snapshotWriter?: SnapshotWriter`, `fromR2?: string`. Añadir a `TareaEleccion` un `r2Path?: string` OPCIONAL (modo LOCAL: la Etapa 1 la hizo el operador; la tarea trae `r2Path` en vez de `url`). Importar `sha256Hex`, `type R2Store`, `type SnapshotWriter` de `@obs/ingest`.

(1) Guard de args: al inicio de `runIngestServel`, si `opts.fromR2` está seteado y falta `opts.r2Store`, lanzar `Error("runIngestServel: --from-r2 requiere r2Store")`.

(2) Modo LOCAL / obtención de bytes: en el paso 1 del loop, si la tarea trae `r2Path` (o `opts.fromR2` aplica a esa tarea), LEER los bytes con `opts.r2Store.getObject(r2Path)` en vez de `opts.conector.descargar(url)` — NUNCA tocar el blob. La guarda de frontera del paso 0 debe aceptar `r2Path` como alternativa válida a `url` (una tarea LOCAL con `eleccion`+`r2Path` es válida aunque `url` esté vacío). En modo LOCAL, las anclas de completitud (Content-MD5/length del HEAD) no vienen del fetch → reconciliarCompletitud usa lo que el crudo permita (md5/byte-length locales); mantener la cuarentena run-level intacta. NO alterar la semántica fail-closed del cruce.

(3) Camino NORMAL (fetched): tras `descargar` (paso 1) y ANTES de subirCrudo/parse-upsert, si `opts.r2Store` está configurado: `const sha = await sha256Hex(bytes)`; `const today = fechaCorte`; `putImmutable("servel", tarea.eleccion, today, sha, "xlsx", bytes)`. Si LANZA (no-412) → `errores.push({fuente: ORIGEN_SERVEL, clave: \`${clave}#r2-etapa1\`, mensaje: (err as Error).message})` + `continue` (Etapa-1-primero LOCKED: sin crudo en R2, NO upsert de ESA elección). Si `existed` (412) → `log("[skip] sin novedades — servel " + clave)` + `continue`. Tras el put exitoso, `snapshotWriter?.write({source:"servel", resource: tarea.eleccion, ...})` best-effort (try/catch no-fatal).

(4) `subirCrudo` DEGRADADO: dejar la llamada a `opts.subirCrudo` DESPUÉS del put R2 exitoso, envuelta en try/catch que solo loguea (NUNCA gatea ni aborta) — R2 es la verdad cruda gateante; Supabase Storage queda como secundario best-effort (decisión LOCKED de este plan).

Mantener `ServelBloqueadaError`→degradación honesta `continue`, el gate de drift de `parseAportes`→cuarentena run `continue`, y `reconciliarCompletitud`→cuarentena `continue` EXACTAMENTE como están (fail-soft por elección). NO tocar `reconciliar-aporte.ts`, `model-servel.ts`, `parse-servel.ts`, ni `0024_servel.sql`. Commit `feat(71-01)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test ingest-run-servel && pnpm --filter @obs/dinero typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `RunIngestServelOpts` tiene `r2Store?`, `snapshotWriter?`, `fromR2?`; `TareaEleccion` tiene `r2Path?`.
    - Camino normal: `putImmutable("servel", ...)` ocurre ANTES de `upsertAportes` (Test A verde).
    - `putImmutable` que lanza → `errores.push` + `continue`, sin upsert de esa elección (Test C verde).
    - `existed:true` → skip Etapa 2 en el camino normal (Test D verde).
    - Modo LOCAL/`--from-r2`: lee bytes con `getObject`, parsea/upsert, `descargar` NUNCA se toca (Test B verde, 0 fetch); guard lanza si `fromR2` sin `r2Store`.
    - Fail-soft por elección intacto: A cuarentenada/bloqueada + B ok (Test E verde).
    - `eleccion` + `fecha_corte` verbatim tras replay (Test F verde).
    - `subirCrudo` corre DESPUÉS del put R2 y su fallo NO gatea (envuelto en try/catch no-fatal).
    - `git diff --exit-code -- packages/dinero/src/reconciliar-aporte.ts packages/dinero/src/model-servel.ts packages/dinero/src/parse-servel.ts supabase/migrations/0024_servel.sql` → exit 0 (VACÍO).
    - `pnpm --filter @obs/dinero test` verde (suite completa sin regresión); `tsc -b` verde.
  </acceptance_criteria>
  <done>`runIngestServel` ejecuta Etapa 1 R2 (bytes .xlsx) put-gatea-upsert en el camino normal y lee de R2 en el modo LOCAL/`--from-r2` sin fetch; los 6 tests verdes; reconciliador/modelo/parser/migración con git diff vacío.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador/conector→R2 | crudo BYTES del .xlsx content-addressed vía SigV4 (aws4fetch); en modo LOCAL el operador PUSO el objeto |
| R2→runner (Etapa 2) | lectura de bytes crudos reconstruibles (getObject) |
| runner→Supabase | upsert VERSIONADO service_role (gated deny-by-default 0024) |
| parse-servel (fila 4) | frontera de validación de contrato del .xlsx (drift→cuarentena) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering/Repudiation | Aporte mal atribuido a un parlamentario por nombre (defamación) | mitigate | `reconciliar-aporte.ts` INTACTO (git diff vacío): cruce por NOMBRE determinista, solo `determinista` mintea FK, todo lo demás → `parlamentario_id NULL` (IDENT-12); CHECK `aporte_parlamentario_solo_confirmado` (schema) |
| T-71-02 | Tampering/Integridad | Derivado sin crudo reconstruible (put R2 falla, upsert procede) | mitigate | Etapa-1-primero LOCKED: `putImmutable` que lanza (no-412) → `errores.push` + `continue`, sin upsert de esa elección (Test C); espejo ingest-run.ts Phase 70 |
| T-71-03 | Availability | Un `.xlsx` malo de UNA elección aborta la corrida multi-elección | mitigate | `ServelBloqueadaError` + cuarentena de drift + completitud hacen `continue` (fail-soft por elección); Test E prueba A cuarentenada + B ok |
| T-71-04 | Tampering | Fetch accidental al blob en modo LOCAL (molesta la fuente) | mitigate | En modo LOCAL se lee de R2 (`getObject`); el conector fake LANZA en `descargar` en el test → 0 fetch (Test B) |
| T-71-05 | Info Disclosure | Exposición prematura de aportes (Ley 21.719) | accept (fuera de scope de este plan) | Este plan no toca `MONEY_PUBLIC_ENABLED` ni superficies públicas; el gate + anti-flip + RLS `donante` deny-by-default se validan en Plan 71-02 |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones de registro (RESEARCH §Package Legitimacy Audit: no aplica). `exceljs@4.4.0` y `aws4fetch` ya viven en el monorepo → sin superficie nueva; sin checkpoint de legitimidad |
</threat_model>

<verification>
- `pnpm --filter @obs/dinero test` — suite dinero verde (todos los previos + los 6 nuevos del wire SERVEL).
- `pnpm --filter @obs/dinero typecheck` (`tsc -b`) — verde.
- `git diff --exit-code -- packages/dinero/src/reconciliar-aporte.ts packages/dinero/src/model-servel.ts packages/dinero/src/parse-servel.ts supabase/migrations/0024_servel.sql` — exit 0 (VACÍO).
- Grep de aceptación: `putImmutable("servel"` + `getObject` en ingest-run-servel.ts; conector fake que LANZA en `descargar` bajo modo LOCAL.
</verification>

<success_criteria>
- SERVEL ingesta escribe bytes .xlsx→R2 ANTES de parse/upsert; put fallido gatea el upsert de esa elección; modo LOCAL `--from-r2` lee de R2 sin tocar el blob (0 fetch).
- Cruce candidato→parlamentario por NOMBRE determinista fail-closed (solo `determinista` mintea FK); reconciliador/modelo/parser/migración con git diff VACÍO.
- `ServelBloqueadaError` + drift degradan ESA elección sin abortar la corrida.
- `eleccion` + `fecha_corte` verbatim tras replay.
- Suite dinero + typecheck verdes.
</success_criteria>

<output>
Create `.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-01-SUMMARY.md` when done
</output>
</content>
</invoke>
