---
phase: 71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dinero/src/run-servel-local-cli.ts
  - packages/dinero/src/run-servel-local-cli.test.ts
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/evaluate.test.ts
  - packages/dinero/src/servel-frozen-guard.test.ts
autonomous: true
requirements: [MONEY-02, DEBT-01]
must_haves:
  truths:
    - "El CLI de operador construye un R2Store real de .env R2_* y threadea r2Store/fromR2/r2Path a runIngestServel (modo LOCAL)"
    - "pnpm freshness cubre SERVEL con una señal de staleness (LOCAL, sin cron → workflowYml n/d honesto; stale si null o > umbral)"
    - "El corte congelado (reconciliar-aporte / model-servel / parse-servel / 0024) tiene un guard que MUERDE si el diff no está vacío"
    - "El gate MONEY_PUBLIC_ENABLED sigue OFF por defecto y su anti-flip guard sigue verde"
  artifacts:
    - path: "packages/dinero/src/run-servel-local-cli.ts"
      provides: "CLI operador LOCAL: construye R2Store de .env, acepta --from-r2/--r2-path/--eleccion, threadea a runIngestServel"
      contains: "R2Store"
    - path: "packages/freshness/src/catalog.ts"
      provides: "Entrada CATALOG servel (staleness sobre aportes_ingesta_estado.ingestado_hasta, umbral 365d, workflowYml n/d)"
      contains: "servel"
    - path: "packages/dinero/src/servel-frozen-guard.test.ts"
      provides: "Guard que aserta git diff VACÍO en reconciliar-aporte/model-servel/parse-servel/0024 + MONEY gate OFF"
      contains: "reconciliar-aporte"
  key_links:
    - from: "packages/freshness/src/catalog.ts"
      to: "aportes_ingesta_estado.ingestado_hasta"
      via: "señal de staleness declarativa"
      pattern: "servel"
    - from: "packages/dinero/src/run-servel-local-cli.ts"
      to: "runIngestServel"
      via: "threading de r2Store/fromR2"
      pattern: "runIngestServel"
---

<objective>
Entregar el CLI de operador LOCAL de SERVEL (construye el `R2Store` real de `.env`, acepta el modo `--from-r2`/`--r2-path` para leer el `.xlsx` que el operador colocó, y threadea a `runIngestServel` del Plan 01), añadir la señal de staleness de SERVEL al catálogo de `pnpm freshness` (LOCAL sin cron → `workflowYml` "n/d" honesto), y anclar el corte congelado de la fase con un guard que MUERDE si `reconciliar-aporte.ts`/`model-servel.ts`/`parse-servel.ts`/`0024_servel.sql` cambian, verificando que `MONEY_PUBLIC_ENABLED` sigue OFF.

Purpose: Cierra la porción operativa de MONEY-02 ("el operador deja el .xlsx en R2 y re-corre") + SC#4 ("SERVEL aparece en pnpm freshness con staleness") y protege las invariantes LOCKED de la fase (cruce por NOMBRE intacto, no añadir RUT, MONEY gate OFF). Sin file overlap con Plan 71-01 → corre en paralelo (Wave 1).
Output: `run-servel-local-cli.ts` + test; entrada CATALOG `servel` + test; guard frozen-servel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-RESEARCH.md
@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-02-PLAN.md

<interfaces>
<!-- Contratos extraídos del código. -->

From packages/freshness/src/catalog.ts (patrón CATALOG existente — el entry chilecompra a espejar):
```typescript
export interface FuenteConfig { fuente: string; tabla: string; columna: string; umbralDias: number; overrideEnv: string; workflowYml: string; }
export const CATALOG: FuenteConfig[] = [ /* ...leyes/agenda/lobby/probidad/fichas/chilecompra */ ];
// chilecompra: { fuente:"chilecompra", tabla:"contratos_ingesta_estado", columna:"ingestado_hasta",
//               umbralDias:30, overrideEnv:"FRESHNESS_UMBRAL_CHILECOMPRA", workflowYml:"chilecompra-weekly.yml" }
// evaluate.ts: stale si la columna es null (desconocido = stale fail-closed) o antigüedad > umbralDias;
//              override por env overrideEnv; workflowYml inexistente → señal GH Actions "n/d" (honesto)
```

From packages/dinero/src/ingest-run-servel.ts (tras Plan 71-01):
```typescript
export interface TareaEleccion { eleccion: string; url: string; anio?: string | null; r2Path?: string; declaredRowCount?: number | null; }
export interface RunIngestServelOpts { conector; writer; maestra; tareas; reconciliar?; subirCrudo?; fechaCorte?; r2Store?; snapshotWriter?; fromR2?; log?; }
export async function runIngestServel(opts: RunIngestServelOpts): Promise<RunIngestServelResult>;
```

From @obs/ingest (r2-store.ts):
```typescript
export class R2Store { constructor(cfg: { accessKeyId; secretAccessKey; endpoint; bucket }, opts?); putImmutable(...); getObject(...); }
```

From packages/dinero/src/ingest-cli-servel.ts (el CLI acotado existente — NO se toca; se crea uno LOCAL separado):
```typescript
export function parseArgs(argv: string[]): ServelCliOptions;   // molde de parseo de flags (--eleccion/--url/--anio/--dry-run)
export class ServelCliArgsError extends Error {}
```

From app/lib/money-gate.ts:
```typescript
export function moneyPublicEnabled(env: NodeJS.ProcessEnv): boolean;  // fail-closed: solo literal "true"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI operador LOCAL run-servel-local-cli + threading a runIngestServel</name>
  <files>packages/dinero/src/run-servel-local-cli.ts, packages/dinero/src/run-servel-local-cli.test.ts</files>
  <read_first>
    - packages/dinero/src/ingest-cli-servel.ts (molde del parseo de flags + construcción de writer/conector/maestra de env; NO se toca — se crea uno LOCAL separado, precedente Phase 70 run-dinero-masivo-cli separado del ingest-cli)
    - packages/votos/src/run-votos-masivo-cli.ts:78-97 (molde de construcción de `R2Store` de `.env R2_*` + guard `--from-r2 requiere R2` + log destino LOCAL/REMOTO)
    - packages/dinero/src/run-dinero-masivo-cli.ts (Phase 70 — el CLI masivo hermano de ChileCompra: construcción R2Store + threading fromR2 + dry-run)
    - packages/dinero/src/ingest-run-servel.ts (tras Plan 71-01 — la firma RunIngestServelOpts con r2Store/fromR2 y TareaEleccion con r2Path)
    - packages/ingest/src/r2-store.ts (R2Store constructor cfg: accessKeyId/secretAccessKey/endpoint/bucket)
  </read_first>
  <action>Crear `run-servel-local-cli.ts` (espejo de `run-dinero-masivo-cli.ts` de Phase 70, adaptado a SERVEL LOCAL): construye un `R2Store` real de `.env R2_*` (`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_ENDPOINT_URL`/`R2_BUCKET`); acepta `--eleccion <slug>`, `--r2-path <r2Path>` (o `--from-r2 <r2Path>`) para el modo LOCAL (la Etapa 1 la hizo el operador colocando el `.xlsx` en R2), `--anio <YYYY>`, `--dry-run`; construye la `TareaEleccion` con `eleccion`+`r2Path` (sin `url` — modo LOCAL no fetchea); threadea `r2Store`/`fromR2`/la tarea a `runIngestServel`; loguea destino LOCAL (lee de R2) y si carga DB o dry-run. Guard: `--from-r2`/`--r2-path` sin R2 configurado → lanza `ServelCliArgsError`. En modo LOCAL NUNCA construye ni invoca el fetch del blob (la fuente no se toca). NO corre ninguna corrida LIVE que consuma la fuente en esta corrida (el backfill real es operador-LOCAL, Plan 71-03). Añadir `run-servel-local-cli.test.ts`: (a) construcción de R2Store cuando `.env R2_*` presente; (b) threading de `--r2-path`/`--from-r2` a `runIngestServel` (verificar que la `TareaEleccion` lleva `r2Path` y NO `url`); (c) log destino LOCAL; (d) guard: `--from-r2` sin R2 lanza; (e) modo LOCAL: el conector fetch NUNCA se construye/invoca (inyectar un conector fake que LANZA en `descargar` y asertar 0 llamadas). Commit `feat(71-02)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test run-servel-local-cli && pnpm --filter @obs/dinero typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `run-servel-local-cli.ts` construye `R2Store` de `.env R2_*` y lo threadea (construcción + threading verificados por test, no solo grep del flag).
    - La `TareaEleccion` en modo LOCAL lleva `eleccion`+`r2Path`, `url` vacío; `--from-r2`/`--r2-path` reenviado a `runIngestServel`.
    - Guard lanza si `--from-r2`/`--r2-path` sin R2.
    - Log distingue destino LOCAL (lee de R2) y carga DB vs dry-run.
    - Modo LOCAL: el conector de fetch NUNCA se toca (conector fake que lanza en `descargar` → 0 llamadas).
    - `pnpm --filter @obs/dinero test` verde; `tsc -b` verde.
  </acceptance_criteria>
  <done>El CLI de operador arma el R2Store real, construye la tarea LOCAL con `r2Path`, threadea `--from-r2`, jamás fetchea el blob; suite dinero verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Señal freshness SERVEL en CATALOG + guard frozen-servel</name>
  <files>packages/freshness/src/catalog.ts, packages/freshness/src/evaluate.test.ts, packages/dinero/src/servel-frozen-guard.test.ts</files>
  <read_first>
    - packages/freshness/src/catalog.ts (el array CATALOG + FuenteConfig; el entry `chilecompra`:275-281 a ESPEJAR — misma forma, tabla `aportes_ingesta_estado`, columna `ingestado_hasta`)
    - packages/freshness/src/evaluate.ts (la lógica de staleness: null→stale fail-closed, antigüedad>umbral→stale, override por overrideEnv, workflowYml inexistente→"n/d")
    - packages/freshness/src/evaluate.test.ts (molde de los casos de test por-fuente — añadir el caso `servel`)
    - supabase/migrations/0024_servel.sql:158-170 (la tabla `aportes_ingesta_estado` con `ingestado_hasta` — el marcador de barrido por-parlamentario)
    - packages/dinero/src/reconciler-frozen-guard.test.ts (Phase 70 — el molde del guard git-diff-vacío + mutation self-check; espejar para SERVEL)
    - app/lib/money-gate.ts + app/lib/money-gate.test.ts (moneyPublicEnabled fail-closed; verificar OFF por defecto)
  </read_first>
  <action>(1) Añadir al array `CATALOG` de `catalog.ts` el entry `servel`: `{ fuente:"servel", tabla:"aportes_ingesta_estado", columna:"ingestado_hasta", umbralDias:365, overrideEnv:"FRESHNESS_UMBRAL_SERVEL", workflowYml:"servel-weekly.yml" }`. Comentario honesto (espejo del bloque chilecompra): SERVEL es LOCAL por elección, sin cron → `servel-weekly.yml` NO existe ni debe crearse → la señal GH Actions figura "n/d" (honesto, no error); umbral 365d generoso porque los ciclos electorales son bianuales/cuatrienales, override por `FRESHNESS_UMBRAL_SERVEL`; sin barrido corrido, `ingestado_hasta` null → stale honesto (desconocido = stale, fail-closed), reflejando cobertura ≈ 0. Añadir a `evaluate.test.ts` los casos `servel`: null→stale; antigüedad < 365d→fresh; > 365d→stale; override por env cambia el umbral; `workflowYml` inexistente→señal GH "n/d".

(2) Crear `servel-frozen-guard.test.ts` (espejo de `reconciler-frozen-guard.test.ts` de Phase 70): asevera `git diff --exit-code` VACÍO sobre `packages/dinero/src/reconciliar-aporte.ts`, `packages/dinero/src/model-servel.ts`, `packages/dinero/src/parse-servel.ts`, `supabase/migrations/0024_servel.sql` (las 4 firmas LOCKED de la fase — el cruce por NOMBRE, el schema sin RUT, el parser fila-4, la migración) con un mutation self-check que prueba que el guard MUERDE si el diff no está vacío. Verificar `moneyPublicEnabled` OFF: reusar/afirmar el test de `money-gate.test.ts` (default `false`, solo literal `"true"` enciende; `.env.example` no lo pone en true). Este plan NO toca `money-gate.ts` ni superficies públicas. Commit `test(71-02)`.</action>
  <verify>
    <automated>pnpm --filter @obs/freshness test && pnpm --filter @obs/dinero test servel-frozen-guard && pnpm --filter @obs/dinero typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `catalog.ts` tiene el entry `servel` (tabla `aportes_ingesta_estado`, columna `ingestado_hasta`, umbral 365d, override `FRESHNESS_UMBRAL_SERVEL`, workflowYml "servel-weekly.yml" inexistente → "n/d").
    - `evaluate.test.ts` cubre `servel`: null→stale, <365d→fresh, >365d→stale, override cambia umbral, GH "n/d".
    - `servel-frozen-guard.test.ts` asevera git diff VACÍO en reconciliar-aporte/model-servel/parse-servel/0024 con mutation self-check que muerde.
    - `moneyPublicEnabled` OFF por defecto verificado; `git diff --exit-code -- app/lib/money-gate.ts` → exit 0.
    - `pnpm --filter @obs/freshness test` verde; `pnpm --filter @obs/dinero test` verde; `tsc -b` verde.
  </acceptance_criteria>
  <done>La señal `servel` figura en `pnpm freshness` con staleness honesta (n/d GH Actions); el guard congela las 4 firmas LOCKED y muerde ante cambios; MONEY gate re-verificado OFF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→R2 | construye R2Store de .env; el operador colocó el crudo (modo LOCAL) |
| CLI→consola/logs | superficie de salida (SERVEL es GET anónimo — sin ticket secreto, a diferencia de ChileCompra) |
| freshness→aportes_ingesta_estado | lectura del marcador de barrido (distingue "0 aportes" de "no barrido") |
| gate→superficie pública | MONEY_PUBLIC_ENABLED fail-closed (candado B, Phase 73) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-06 | Info Disclosure | Dato viejo presentado como actual (stale silencioso) | mitigate | Señal `servel` en CATALOG (stale si null o >365d, fail-closed) + frescura POR DATO (`eleccion`+`fecha_corte`) rectora; GH "n/d" honesto por ser LOCAL sin cron |
| T-71-07 | Tampering | Alguien afloja el cruce por NOMBRE o mete un RUT en SERVEL (schema/reconciler drift) | mitigate | `servel-frozen-guard.test.ts` asevera git diff VACÍO en reconciliar-aporte/model-servel/parse-servel/0024 con mutation self-check |
| T-71-08 | Elevation of Privilege | Flip accidental del gate MONEY | mitigate | `moneyPublicEnabled` fail-closed (solo literal "true"); re-verificado OFF; anti-flip guard verde; el flip es acto humano Phase 73 |
| T-71-09 | Tampering | Fetch accidental al blob desde el CLI (molesta la fuente) | mitigate | El CLI LOCAL construye la tarea con `r2Path` (sin `url`); el conector fetch nunca se toca (test con conector fake que lanza) |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalaciones de registro; sin superficie nueva (`exceljs`/`aws4fetch` ya en el monorepo) |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` — el caso `servel` verde.
- `pnpm --filter @obs/dinero test` — CLI LOCAL + guard frozen-servel verdes.
- `pnpm --filter @obs/dinero typecheck` (`tsc -b`) — verde.
- `git diff --exit-code -- packages/dinero/src/reconciliar-aporte.ts packages/dinero/src/model-servel.ts packages/dinero/src/parse-servel.ts supabase/migrations/0024_servel.sql app/lib/money-gate.ts` — exit 0.
- Grep de aceptación: `servel` en el CATALOG; `R2Store` + `runIngestServel` en run-servel-local-cli.ts.
</verification>

<success_criteria>
- El CLI operador LOCAL arma el R2Store real y threadea el modo `--from-r2`/`r2Path` sin fetchear la fuente.
- `pnpm freshness` cubre SERVEL con staleness (n/d GH Actions honesto por ser LOCAL sin cron).
- El corte congelado (reconciliar-aporte/model-servel/parse-servel/0024) tiene guard que muerde.
- `MONEY_PUBLIC_ENABLED` OFF re-verificado.
- Suites freshness + dinero + typecheck verdes.
</success_criteria>

<output>
Create `.planning/phases/71-dinero-p5c-servel-local-xlsx-artesanal-funde-debt-01/71-02-SUMMARY.md` when done
</output>
</content>
