---
phase: 70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/evaluate.test.ts
  - packages/dinero/src/reconciler-frozen-guard.test.ts
autonomous: true
requirements: [MONEY-01, DEBT-01]
must_haves:
  truths:
    - "pnpm freshness cubre ChileCompra con una señal de staleness (stale si null o > umbral)"
    - "El corte congelado (reconciliar-contrato / model / 0023) tiene un guard que MUERDE si el diff no está vacío"
    - "El gate MONEY_PUBLIC_ENABLED sigue OFF por defecto y su anti-flip guard sigue verde"
  artifacts:
    - path: "packages/freshness/src/catalog.ts"
      provides: "Entrada CATALOG ChileCompra (staleness sobre contratos_ingesta_estado.ingestado_hasta)"
      contains: "chilecompra"
    - path: "packages/dinero/src/reconciler-frozen-guard.test.ts"
      provides: "Guard que aserta git diff VACÍO en reconciliar-contrato/model/0023"
      contains: "reconciliar-contrato"
  key_links:
    - from: "packages/freshness/src/catalog.ts"
      to: "contratos_ingesta_estado.ingestado_hasta"
      via: "señal de staleness declarativa"
      pattern: "chilecompra"
---

<objective>
Añadir la señal de staleness de ChileCompra al catálogo de `pnpm freshness` (espejo del patrón CATALOG declarativo de leyes/probidad/lobby-leylobby), y anclar el corte congelado de esta fase con un guard que MUERDE si `reconciliar-contrato.ts` / `model.ts` / `0023_dinero.sql` cambian. Verificar que el gate `MONEY_PUBLIC_ENABLED` sigue OFF y su anti-flip guard verde.

Purpose: Cierra la porción de MONEY-01 SC#4 ("freshness cubre ChileCompra con staleness") y protege las invariantes LOCKED de la fase (no tocar el reconciliador/modelo/migración; MONEY gate OFF). Sin file overlap con Plan 70-01 → corre en paralelo (Wave 1).
Output: Entrada CATALOG ChileCompra + test; guard frozen-reconciler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-RESEARCH.md
@.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-VALIDATION.md
@.planning/phases/69-dinero-p5a-rut-01-backfill-a-la-maestra-checkpoint-operador/69-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos del código. -->

From packages/freshness/src/catalog.ts (patrón CATALOG existente — leyes/agenda/lobby/probidad/fichas):
```typescript
export interface FuenteConfig {
  fuente: string;      // p.ej. "chilecompra"
  tabla: string;       // p.ej. "contratos_ingesta_estado"
  columna: string;     // p.ej. "ingestado_hasta"
  umbralDias: number;  // p.ej. 30 (fuente OCDS se actualiza mensual, día 20)
  overrideEnv: string; // p.ej. "FRESHNESS_UMBRAL_CHILECOMPRA"
  workflowYml: string; // "chilecompra-weekly.yml" (aún no existe → señal GH Actions ausente hasta el flip)
}
export const CATALOG: FuenteConfig[]; // array de entradas; añadir la de ChileCompra
```
Precedente clave: `lobby-leylobby` mide `lobby_ingesta_estado.ingestado_hasta` (distingue "consultado sin resultados" de "no consultado") — mismo patrón que ChileCompra debe seguir con `contratos_ingesta_estado.ingestado_hasta`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Señal CATALOG ChileCompra + test</name>
  <files>packages/freshness/src/catalog.ts, packages/freshness/src/evaluate.test.ts</files>
  <read_first>
    - packages/freshness/src/catalog.ts:210-259 (el array CATALOG — copiar la forma de la entrada `lobby-leylobby`, que mide `lobby_ingesta_estado.ingestado_hasta`)
    - packages/freshness/src/evaluate.test.ts (el molde de test de evaluación por-fuente: stale si null / stale si > umbral / fresh si ≤ umbral)
    - packages/freshness/src/evaluate.ts (evaluateCobertura/evaluate — el evaluador que consume CATALOG; NO se modifica, la entrada declarativa basta)
    - supabase/migrations/0023_dinero.sql (confirmar que `contratos_ingesta_estado` tiene la columna `ingestado_hasta` — usar columna EXISTENTE, NO añadir migración; Pitfall 6)
  </read_first>
  <behavior>
    - La señal ChileCompra evalúa `stale` cuando `contratos_ingesta_estado.ingestado_hasta` es null (nunca barrido) o su MAX supera `umbralDias` (30) desde hoy.
    - La señal evalúa `fresh` cuando el MAX está dentro del umbral.
    - La entrada respeta el override `FRESHNESS_UMBRAL_CHILECOMPRA`.
    - `workflowYml: "chilecompra-weekly.yml"` aún no existe → la señal de GH Actions figura ausente hasta el flip (comportamiento honesto, no error).
  </behavior>
  <action>Añadir una entrada al `CATALOG` de `catalog.ts` con `fuente:"chilecompra"`, `tabla:"contratos_ingesta_estado"`, `columna:"ingestado_hasta"`, `umbralDias:30`, `overrideEnv:"FRESHNESS_UMBRAL_CHILECOMPRA"`, `workflowYml:"chilecompra-weekly.yml"`. Elegir `contratos_ingesta_estado.ingestado_hasta` (NO `contrato.fecha_captura`) porque distingue "consultado sin contratos" de "no consultado" — espejo de `lobby-leylobby` (Open Question 1 del RESEARCH, recomendación resuelta). Usar SOLO columnas existentes (0023 ya define la tabla) — NO añadir migración. Extender `evaluate.test.ts` con los 3 casos de la señal ChileCompra (stale-null, stale->umbral, fresh<=umbral) reusando el evaluador tal cual. Commit `feat(70-02)`.</action>
  <verify>
    <automated>pnpm --filter @obs/freshness test && pnpm --filter @obs/freshness typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `CATALOG` contiene una entrada `chilecompra` sobre `contratos_ingesta_estado.ingestado_hasta`, umbral 30, override `FRESHNESS_UMBRAL_CHILECOMPRA`.
    - `evaluate.test.ts` cubre stale-null / stale>umbral / fresh<=umbral para ChileCompra.
    - NO se añadió ninguna migración (columna existente de 0023).
    - `pnpm --filter @obs/freshness test` verde; `tsc -b` verde.
    - `git diff --exit-code -- supabase/migrations/` → exit 0 (ninguna migración nueva).
  </acceptance_criteria>
  <done>La señal ChileCompra figura en el catálogo de `pnpm freshness` con staleness declarativa sobre `contratos_ingesta_estado.ingestado_hasta`; test verde; cero migraciones.</done>
</task>

<task type="auto">
  <name>Task 2: Guard frozen-reconciler + verificación gate MONEY OFF</name>
  <files>packages/dinero/src/reconciler-frozen-guard.test.ts</files>
  <read_first>
    - packages/dinero/src/name-match-rut-guard.behavior.test.ts (Phase 69 — molde de un guard-como-test en packages/dinero que ejercita el corte; misma suite)
    - 69-01-SUMMARY.md (el corte CR-01 congelado — este guard es su complemento a nivel de archivo-intacto)
    - app/lib/money-gate.test.ts (el test existente del gate `moneyPublicEnabled` OFF salvo literal "true" — NO se modifica; este plan solo lo re-corre para confirmar verde)
    - app/lib/money-gate.ts (chokepoint del gate — NO tocar)
  </read_first>
  <action>Crear `reconciler-frozen-guard.test.ts` en `packages/dinero/src/`: un test que ejecuta `git diff --name-only HEAD -- packages/dinero/src/reconciliar-contrato.ts packages/dinero/src/model.ts supabase/migrations/0023_dinero.sql` (vía `child_process.execSync`) y aserta que la salida es VACÍA respecto al baseline de la fase — es decir, que esos tres archivos NO fueron modificados por el trabajo de esta fase. Como referencia estable, el test compara el contenido actual contra un hash/copia embebida NO es viable en runtime puro; en su lugar el test aserta que el archivo contiene las firmas LOCKED conocidas (p.ej. `reconciliar-contrato.ts` mantiene la rama jurídica RUT-only `resolverEntidadProveedor` y NO introduce `correrPipeline` en la rama jurídica; `model.ts` mantiene `monto` como `text`/string VERBATIM y NO `numeric`; el header de `0023` intacto). Este guard MUERDE si un refactor rompe cualquiera de esas firmas. NO modificar `reconciliar-contrato.ts`/`model.ts`/`0023`. Verificar que `pnpm --filter ./app test money-gate` sigue verde (gate OFF salvo literal "true") + `pnpm --filter ./app test name-match-rut-guard` (anti-flip de Phase 69) sin regresión. Commit `test(70-02)`.</action>
  <verify>
    <automated>pnpm --filter @obs/dinero test reconciler-frozen-guard && pnpm --filter ./app test money-gate && pnpm --filter ./app test name-match-rut-guard</automated>
  </verify>
  <acceptance_criteria>
    - `reconciler-frozen-guard.test.ts` aserta las firmas LOCKED de `reconciliar-contrato.ts` (jurídica RUT-only, sin `correlationPipeline`/`correrPipeline` en la rama jurídica), `model.ts` (`monto` string/text VERBATIM, no numeric), y `0023` (header intacto).
    - El guard MUERDE ante una firma rota (mutation self-check: inyectar mentalmente/comentar la aserción confirma que dispara — describir en el SUMMARY qué firma cada aserción protege).
    - `pnpm --filter ./app test money-gate` verde (gate OFF salvo literal "true").
    - `pnpm --filter ./app test name-match-rut-guard` verde (anti-flip Phase 69 sin regresión).
    - `git diff --exit-code -- packages/dinero/src/reconciliar-contrato.ts packages/dinero/src/model.ts supabase/migrations/0023_dinero.sql app/lib/money-gate.ts` → exit 0.
  </acceptance_criteria>
  <done>Existe un guard que congela las firmas LOCKED del reconciliador/modelo/migración y muerde ante refactors; el gate MONEY sigue OFF y su anti-flip verde; cero archivos LOCKED tocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| freshness CLI→Supabase | read-only, agregados (no imprime dbUrl, no SELECT de PII) |
| guard→archivos LOCKED | aserción estática que congela el corte de la fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06 | Tampering | Refactor "de paso" rompe el corte RUT-only jurídico o el monto VERBATIM | mitigate | `reconciler-frozen-guard.test.ts` aserta las firmas LOCKED y muerde; espejo del guard-como-test de Phase 69 |
| T-70-07 | Elevation of Privilege | Flip accidental del gate MONEY a "true" | mitigate | Este plan re-corre `money-gate` (OFF salvo literal "true") + el anti-flip de Phase 69; no toca `money-gate.ts` ni `MONEY_PUBLIC_ENABLED` |
| T-70-08 | Information Disclosure | Señal freshness expone datos de contrato/RUT | mitigate | La señal mide `contratos_ingesta_estado.ingestado_hasta` (agregado de barrido, no PII); patrón CATALOG no imprime dbUrl ni SELECT de rut |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` + `pnpm --filter @obs/dinero test reconciler-frozen-guard` + `pnpm --filter ./app test money-gate name-match-rut-guard` — todos verdes.
- `git diff --exit-code -- packages/dinero/src/reconciliar-contrato.ts packages/dinero/src/model.ts supabase/migrations/0023_dinero.sql app/lib/money-gate.ts` — exit 0.
- Grep: `chilecompra` en catalog.ts; `contratos_ingesta_estado` en la entrada.
</verification>

<success_criteria>
- `pnpm freshness` cubre ChileCompra con staleness sobre `contratos_ingesta_estado.ingestado_hasta`.
- El corte LOCKED (reconciliador/modelo/migración) está congelado por un guard que muerde.
- El gate MONEY sigue OFF; anti-flip verde.
- Cero migraciones nuevas; cero archivos LOCKED tocados.
</success_criteria>

<output>
Create `.planning/phases/70-dinero-p5b-wire-dos-etapas-chilecompra-por-rut-funde-debt-01-spike-cuota/70-02-SUMMARY.md` when done
</output>
