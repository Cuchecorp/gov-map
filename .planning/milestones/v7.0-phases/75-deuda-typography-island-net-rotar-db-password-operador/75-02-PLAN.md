---
phase: 75-deuda-typography-island-net-rotar-db-password-operador
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md
autonomous: false
requirements: [DEBT-06]
user_setup:
  - service: supabase
    why: "Rotar el DB password (B26, credencial expuesta) — acto de operador en el dashboard; el agente no tiene acceso y rotar en vivo rompe conexiones activas"
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard → Settings → Database (re-cargar el connection string con el nuevo password en .env local + GH secrets Cuchecorp/gov-map si aplica)"
    dashboard_config:
      - task: "Rotar el password de Postgres y confirmar que el password viejo queda inválido"
        location: "Supabase Dashboard → Settings → Database → Reset database password"
must_haves:
  truths:
    - "Existe `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` que documenta el flujo de rotación del DB password para el operador, modelado sobre `74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md`"
    - "La nota contiene CERO valores de credencial (ningún `postgresql://…:password@…`, ningún password): solo NOMBRES de variables + pasos de dashboard"
    - "La nota deja claro que la rotación invalida ÚNICAMENTE `SUPABASE_DB_URL` (DDL/bulk CLIs + psql runbooks locales); los crons de CI y el sitio usan `SUPABASE_SECRET_KEY` + REST y NO se ven afectados"
    - "La nota instruye al operador a revisar los Actions secrets de Cuchecorp/gov-map por cualquier `*_DB_URL` antes de asumir que no hay ninguno (Q1)"
    - "El plan termina en un checkpoint de operador; el agente NO rota la credencial"
  artifacts:
    - path: ".planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md"
      provides: "Runbook operador para rotar el DB password de Supabase (B26), zero-credential-values, con checkpoint"
      contains: "SUPABASE_DB_URL"
  key_links:
    - from: "75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md"
      to: "Supabase dashboard rotation + operator .env / GH secrets reload"
      via: "documented flow only (agent does not execute)"
      pattern: "SUPABASE_DB_URL"
---

<objective>
Write the operator runbook `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` that documents how the OPERATOR rotates the exposed Supabase DB password (B26) in the dashboard and re-loads the new credential, modeled on `74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md`, ending in an operator checkpoint. The agent DOCUMENTS the action and does NOT rotate the credential (DEBT-06).

Purpose: The DB password lives ONLY in `SUPABASE_DB_URL` and is consumed ONLY by local DDL/bulk CLIs + psql runbooks — VERIFIED zero GitHub workflows reference it (CI crons + the deployed site authenticate with the independent service_role key `SUPABASE_SECRET_KEY` over REST). Rotation is therefore an operator-only act (agent has no dashboard access; live rotation breaks active connections). This closes B26 by handing the operator a precise, zero-credential-values runbook.

Output: `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` (zero secret values); an operator checkpoint gating phase completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-RESEARCH.md
@.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-VALIDATION.md
@.planning/phases/74-deuda-cursor-leylobby-cloudflare-api-token-ci-round-robin-cron-leyes-weekly/74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md

<interfaces>
<!-- Facts the note must state, VERIFIED against the codebase — no exploration needed. -->

Credential isolation (VERIFIED):
  - The DB password lives ONLY inside SUPABASE_DB_URL:
      SUPABASE_DB_URL=postgresql://postgres:<password>@<host>:5432/postgres   (.env.example:26–29)
  - grep over .github/workflows/ → 0 files reference SUPABASE_DB_URL (VERIFIED this session).
    Workflows present: agenda-weekly, backfill, backup-parlamentario, deploy-cloudflare,
    fichas-backfill, leyes-weekly, lobby-camara-weekly, lobby-leylobby-weekly, probidad-weekly.
    None reference SUPABASE_DB_URL.
  - CI crons + the deployed site authenticate with SUPABASE_SECRET_KEY (service_role) +
    SUPABASE_API_URL over REST — INDEPENDENT credentials, NOT affected by a DB-password rotation.
  - Independent credentials that must NOT be touched by this rotation (naming them wrongly as
    "reload after rotation" is the Pitfall-2 warning sign): SUPABASE_SECRET_KEY, SUPABASE_API_URL,
    SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET.

Consumers of SUPABASE_DB_URL (what breaks on rotation, until operator re-loads):
  - local DDL/bulk CLIs + psql runbooks, e.g. the established idiom in
    docs/RUNBOOK-lockdown-cutover.md:96–110:
      DB_URL=$(node -e "require('dotenv').config(); console.log(process.env.SUPABASE_DB_URL)")
      PGCLIENTENCODING=UTF8 psql "$DB_URL" -c "select 1;"

Open question the note must forward to the operator (Q1):
  - The deployed repo Cuchecorp/gov-map is NOT inspectable from this workspace. Its Actions
    secrets MIGHT hold a *_DB_URL used by a DDL cron (e.g. backfill). The note instructs the
    operator to grep/inspect Cuchecorp/gov-map Actions secrets for any *_DB_URL and refresh it
    if present; safe default = assume none, verify at rotation time.

Template to mirror (74-DEBT-03 structure): Hallazgo rector · Evidencia (static/read-only) ·
Pasos de OPERADOR (numbered) · Aclaración anti-mal-interpretación (warning signs) · Estado
(checkbox list ending in the operator checkpoint). And: "Esta nota no contiene ningún valor de
secret" (verbatim guarantee, 74-DEBT-03:56–57).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md (zero credential values)</name>
  <files>.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md</files>
  <read_first>
    - .planning/phases/74-.../74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md (the full structure to mirror: Hallazgo rector → Evidencia read-only → Pasos de OPERADOR → Aclaración anti-mal-interpretación → Estado with checkpoint; and the "no contiene ningún valor de secret" guarantee at lines 56–57)
    - 75-RESEARCH.md §Security Domain (V6) + §Runtime State Inventory + §Common Pitfalls 2 (credential isolation, the "reload only SUPABASE_DB_URL, NOT the service_role/API secrets" rule) + §Open Questions Q1 (Cuchecorp/gov-map *_DB_URL check)
    - The `<interfaces>` block above (verified facts the note must state)
  </read_first>
  <action>
    Write `.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` mirroring the section structure of `74-DEBT-03-CF-TOKEN-OPERATOR-NOTE.md`. Sections: (1) Hallazgo rector — the DB password (B26) lives ONLY in `SUPABASE_DB_URL`; rotation is an operator dashboard act (the agent has no dashboard access and live rotation breaks active connections); (2) Evidencia (verificación estática, read-only) — state that grep over `.github/workflows/` returns 0 references to `SUPABASE_DB_URL`, list the workflows that DO run (they use `SUPABASE_SECRET_KEY` + `SUPABASE_API_URL` over REST), and cite `.env.example:26–29` as the sole location of the password (by NAME, never the value); (3) Qué rompe la rotación / qué NO — rotation invalidates ONLY `SUPABASE_DB_URL` consumers (local DDL/bulk CLIs + psql runbooks like `docs/RUNBOOK-lockdown-cutover.md`); CI crons + the deployed site keep running because they use `SUPABASE_SECRET_KEY`; (4) Pasos de OPERADOR (numbered) — rotate in Supabase Dashboard → Settings → Database → Reset database password; re-load the new `SUPABASE_DB_URL` value into local `.env`; grep/inspect Cuchecorp/gov-map Actions secrets for any `*_DB_URL` and refresh if present (per Q1, safe default = none); confirm the OLD `SUPABASE_DB_URL` now FAILS `psql` auth (rotation actually invalidated it); run the smoke `PGCLIENTENCODING=UTF8 psql "$DB_URL" -c "select 1;"` with the NEW credential; (5) Aclaración anti-mal-interpretación (warning signs) — do NOT re-load `SUPABASE_SECRET_KEY`/`SUPABASE_API_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_JWT_SECRET` after this rotation (they are independent credentials, unaffected — a runbook step touching them is WRONG per Pitfall 2); `.env` stays gitignored (never commit the new password); (6) Estado — checkbox list ending in the operator checkpoint. Include the verbatim guarantee "Esta nota no contiene ningún valor de secret". CRITICAL: the note MUST contain ZERO credential values — no `postgresql://…:<password>@…` with a real password, no host, no actual secret string; use variable NAMES and `<password>`/`<host>` placeholders only.
  </action>
  <verify>
    <automated>bash -c 'F=".planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md"; test -f "$F" && ! grep -nE "postgresql://[^<]*:[^<@]+@|password[[:space:]]*=[[:space:]]*[^<[:space:]]" "$F" && grep -q "SUPABASE_DB_URL" "$F" && grep -q "SUPABASE_SECRET_KEY" "$F" && echo GUARD_OK'</automated>
  </verify>
  <acceptance_criteria>
    - File `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` exists with the 6-section structure mirroring `74-DEBT-03`
    - ZERO credential values: no `postgresql://<user>:<realpassword>@<realhost>` string, no literal password, no real host — verifiable by the guard grep returning `GUARD_OK` (the regex finds no populated `postgresql://…:secret@` and no `password=<value>`); only NAMES + `<password>`/`<host>` placeholders appear
    - Note states rotation invalidates ONLY `SUPABASE_DB_URL` (local DDL/bulk CLIs + psql); CI crons + site use `SUPABASE_SECRET_KEY` + REST and are unaffected
    - Note instructs checking Cuchecorp/gov-map Actions secrets for any `*_DB_URL` (Q1) before assuming none
    - Note's warning section forbids re-loading `SUPABASE_SECRET_KEY`/`SUPABASE_API_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_JWT_SECRET` as part of this rotation (Pitfall 2)
    - Note contains the verbatim "no contiene ningún valor de secret" guarantee and ends in a `Estado` checklist with an unchecked operator checkpoint
  </acceptance_criteria>
  <done>The operator note exists, mirrors 74-DEBT-03, carries zero credential values (guard grep = `GUARD_OK`), correctly scopes the blast radius to `SUPABASE_DB_URL`, forwards Q1, and ends in an operator checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator checkpoint — rotate the DB password + confirm /red non-regression</name>
  <action>Acto de operador (el agente NO rota: no tiene acceso al dashboard de Supabase y rotar en vivo rompe conexiones activas). Siguiendo `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md`, rotar el DB password (B26) en Supabase Dashboard → Settings → Database, re-cargar el nuevo `SUPABASE_DB_URL` en `.env` local (+ GH secret `*_DB_URL` en Cuchecorp/gov-map si existe), confirmar que el password viejo queda inválido y el nuevo funciona, y confirmar la no-regresión visual de `/red` (swap tipográfico de Plan 01) en el deploy real. Detalles abajo.</action>
  <what-built>
    El agente redactó `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` — un runbook zero-credential-values para rotar el DB password expuesto de Supabase (B26). El agente NO rotó (ni puede): sin acceso al dashboard, y rotar en vivo rompe conexiones activas. La rotación es acto exclusivo de operador.

    Aquí también se confirma la no-regresión visual `/red` (ui-review + deploy real) del swap tipográfico de Plan 01: jsdom no mide layout, así que la preservación de píxeles del swap `.net-*` → `--text-*` debe confirmarse en un deploy real.
  </what-built>
  <how-to-verify>
    A) Rotación del DB password (DEBT-06) — seguir `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md`:
       1. Supabase Dashboard → Settings → Database → Reset database password → rotar.
       2. Re-cargar el nuevo `SUPABASE_DB_URL` en `.env` local (y cualquier GH secret `*_DB_URL` en Cuchecorp/gov-map si existe — grep de los Actions secrets del repo primero).
       3. Confirmar que el `SUPABASE_DB_URL` VIEJO ahora FALLA: `PGCLIENTENCODING=UTF8 psql "<url-vieja>" -c "select 1;"` → error de auth.
       4. Confirmar que la credencial NUEVA funciona: `DB_URL=$(node -e "require('dotenv').config(); console.log(process.env.SUPABASE_DB_URL)"); PGCLIENTENCODING=UTF8 psql "$DB_URL" -c "select 1;"` → `1`.
       5. NO re-cargar `SUPABASE_SECRET_KEY`/`SUPABASE_API_URL` — son independientes y no se ven afectadas; confirmar que los crons de CI + el sitio desplegado siguen verdes (usan el service_role key por REST).

    B) No-regresión visual `/red` (DEBT-05, Plan 01) — en un deploy real:
       1. Abrir el `/red?seed=<parlamentario-id>` desplegado.
       2. Confirmar que el layout radial B (seed → columna + conectores SVG fan-out) es visualmente idéntico a antes: los conectores siguen anclados a la tarjeta seed y a cada fila; los tamaños de texto nombre/banda lucen iguales (14px nombre de fila / 16px nombre seed / 12px banda).
       3. Opcional: `getComputedStyle(nameEl).fontSize` sobre los elementos seed/fila/banda = px esperado (16/14/12).
  </how-to-verify>
  <resume-signal>Escribir "rotado" (con la confirmación url-vieja-falla + url-nueva-funciona y la nota de no-regresión `/red`) o describir qué falló.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator note (git-committed) → secret value | The runbook is committed to the repo; a leaked credential value would cross into git history. |
| Supabase server ↔ `SUPABASE_DB_URL` copies | The authoritative password lives on the Supabase server + local `.env`/GH secrets, never in git; rotation must invalidate the old value everywhere it is trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Information Disclosure | Secret value leaked into the committed operator note | mitigate | Zero-credential-values rule; guard grep in Task 1 verify blocks any populated `postgresql://…:secret@` / `password=<value>`; verbatim "no contiene ningún valor de secret". |
| T-75-04 | Elevation / Spoofing | Old DB password still valid after "rotation" | mitigate | Operator checkpoint step 3 requires confirming the OLD `SUPABASE_DB_URL` FAILS `psql` auth post-rotation. |
| T-75-05 | Information Disclosure | New password accidentally committed via `.env` | mitigate | Note reminds `.env` is gitignored; operator re-loads into `.env` + GH settings only, never git. |
</threat_model>

<verification>
- `75-DB-PASSWORD-ROTATION-OPERATOR-NOTE.md` exists, mirrors 74-DEBT-03, guard grep = `GUARD_OK` (zero credential values)
- Note correctly scopes rotation blast radius to `SUPABASE_DB_URL` (0 CI consumers stated + verified), forwards Q1 (Cuchecorp/gov-map `*_DB_URL` check)
- Operator checkpoint present and blocking; agent did NOT rotate
- `/red` visual non-regression (Plan 01) folded into the operator checkpoint (jsdom-blind)
</verification>

<success_criteria>
- The DB password rotation is documented for the operator with a zero-credential-values runbook + a blocking operator checkpoint; the agent does NOT rotate (DEBT-06 success criterion 2)
- The runbook correctly states rotation affects ONLY `SUPABASE_DB_URL` (local DDL/CLIs), keeping CI crons + the site running on the independent service_role key
</success_criteria>

<output>
Create `.planning/phases/75-deuda-typography-island-net-rotar-db-password-operador/75-02-SUMMARY.md` when done.
</output>
