---
phase: 66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - .planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md
autonomous: false
requirements: [VOTO-01, DEBT-01]

must_haves:
  truths:
    - "El voto individual de Cámara se puebla A ESCALA por --boletines-file respetando rate-limit 2-3s"
    - "El backfill escribe a la Supabase REMOTA (no LOCAL) verificando el destino antes de escribir"
    - "El backfill es reanudable: interrumpir y re-correr con el mismo archivo no duplica (upsert por clave natural)"
    - "Tras el backfill, la cobertura confirmado/no_confirmado queda medida y '0 DIPID-maestra no_confirmado' se verifica"
  artifacts:
    - path: ".planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md"
      provides: "Runbook operador-LOCAL: pre-checks, comando LIVE, particionado reanudable, reporte de cobertura"
      contains: "VOTOS_LIVE=1"
  key_links:
    - from: "66-BACKFILL-RUNBOOK.md"
      to: "run-votos-masivo-cli.ts --boletines-file"
      via: "comando operador LOCAL con rate-limit 2-3s"
      pattern: "boletines-file"
---

<objective>
Documentar el backfill a escala del voto individual de Cámara como un HANDOFF de operador-LOCAL: pre-checks, el comando LIVE gated (`VOTOS_LIVE=1`, rate-limit 2-3s), el particionado reanudable del archivo de boletines, la escritura a la Supabase REMOTA, y el reporte de cobertura post-backfill. Este plan NO ejecuta contra el WAF gubernamental ni escribe a PROD — la corrida LIVE es un acto deliberado del operador (LOCKED: backfill masivo = LOCAL; CLAUDE.md).

Purpose: SC#3 (voto poblado a escala, backfill masivo LOCAL reanudable) y la medición de SC#4 (cobertura tras escalar) requieren tocar `opendata.camara.cl` a escala y escribir a la Supabase remota. Ambos son operador-LOCAL por regla LOCKED. El agente entrega el runbook ejecutable; el operador lo corre.

Output: `66-BACKFILL-RUNBOOK.md` — un runbook que el operador sigue paso a paso, apoyado enteramente en el wire del Plan 01 (`--from-r2`, cobertura) y en `--boletines-file` (ya existente).

## Por qué autonomous:false
- La corrida LIVE golpea el WS gubernamental a escala con rate-limit 2-3s — LOCKED como operador-LOCAL, NO CI (nunca ráfagas desde el agente).
- La escritura es a la Supabase REMOTA (PROD) — mantenerla fuera de CI evita escritura accidental a PROD (T-66-05).
- El agente NO invoca `VOTOS_LIVE=1`; solo produce el runbook y verifica sus pre-checks contra el estado del repo/DB destino donde sea offline-seguro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-RESEARCH.md

<interfaces>
Entry-point de operador (packages/votos/src/run-votos-masivo-cli.ts, tras Plan 01):
  tsx packages/votos/src/run-votos-masivo-cli.ts [--dry-run] [--limit N] [--boletines-file <ruta>] [--from-r2 <path>]
  - Sin dry-run: writer = SupabaseTramitacionWriter({ url: env.SUPABASE_API_URL, serviceKey: env.SUPABASE_SECRET_KEY }) → REMOTO.
  - --boletines-file: un boletín por línea (vía robusta; el descubrimiento por sesión da 0).
  - Tras la corrida real: imprime el reporte de cobertura (reportarCobertura del Plan 01).
  - Idempotente por (votacion_id, fuente_voter_id): re-correr = no-op de upsert.

Pre-check DB destino (RESEARCH Pitfall 4): `\d voto` debe incluir 'ausente' en el CHECK (migración 0019 aplicada).
Golden gate (P65): `pnpm --filter @obs/votos test golden-dipid` DEBE estar verde antes del backfill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir el runbook operador-LOCAL del backfill (pre-checks, comando LIVE, reanudable, cobertura)</name>
  <files>.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md</files>
  <read_first>
    - packages/votos/src/run-votos-masivo-cli.ts (flags, construcción del writer remoto desde .env, --boletines-file)
    - 66-RESEARCH.md "Pattern 3" (backfill LOCAL por --boletines-file, reanudable por particionado) + "Common Pitfalls" 1/4/5 (descubrimiento=0, CHECK 0019, destino LOCAL vs REMOTO)
    - 66-RESEARCH.md "Code Examples" (SQL group by estado_vinculo + query DIPID-maestra no_confirmado)
    - CLAUDE.md "Ingesta y Cron — LOCKED" (backfill masivo LOCAL, rate-limit 2-3s, dos etapas)
  </read_first>
  <action>Crear `66-BACKFILL-RUNBOOK.md` con secciones: (1) PRE-CHECKS OBLIGATORIOS — golden gate verde (`pnpm --filter @obs/votos test golden-dipid`), suite offline verde (Plan 01), `\d voto` incluye 'ausente' en la DB REMOTA destino (0019 aplicada), `.env` tiene R2_* + SUPABASE_API_URL + SUPABASE_SECRET_KEY. (2) DERIVAR EL ARCHIVO DE BOLETINES — `SELECT boletin FROM proyecto` (paginando `.order().range()` si >1k) → un boletín por línea → `boletines.txt`; assert `wc -l > 0` (Pitfall 1). (3) PARTICIONAR PARA REANUDABLE — dividir `boletines.txt` en lotes (p.ej. `split -l 200`); reanudar = correr el lote pendiente (idempotencia por clave natural hace no-op los ya cargados). (4) CORRIDA LIVE — el comando exacto `VOTOS_LIVE=1 pnpm --filter @obs/votos exec tsx src/run-votos-masivo-cli.ts --boletines-file boletines-lote-NN.txt` (rate-limit 2-3s LOCKED, serial, NUNCA ráfagas); verificar en el log que el destino dice el endpoint REMOTO (Pitfall 5) antes de dejarlo correr. (5) REPLAY ETAPA 2 — si un lote falló en Etapa 2 (parse/DB) tras persistir el crudo, re-ejecutar con `--from-r2 <r2Path>` SIN tocar la fuente. (6) REPORTE DE COBERTURA — tras cada lote, el CLI imprime `reportarCobertura`; consolidar N confirmado / M total y VERIFICAR el invariante `dipidsMaestraNoConfirmados === 0` (SC#4; si >0 → BUG que rompe P65, DETENER). (7) CRITERIOS DE CIERRE — todos los lotes corridos, cobertura declarada, invariante en 0, crudo en R2 (primeros snapshots de votos). Incluir el bloque SQL de cobertura verbatim del RESEARCH. NO incluir credenciales; referenciar nombres de env vars.</action>
  <verify>
    <automated>test -f ".planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md" &amp;&amp; grep -q "VOTOS_LIVE=1" ".planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md" &amp;&amp; grep -q "boletines-file" ".planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-BACKFILL-RUNBOOK.md"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe y contiene las 7 secciones (grep de encabezados: PRE-CHECKS, boletines, particionar/reanudable, LIVE, from-r2, cobertura, cierre).
    - El comando LIVE incluye `VOTOS_LIVE=1` y `--boletines-file` y menciona el rate-limit 2-3s.
    - Contiene la verificación del invariante `dipidsMaestraNoConfirmados === 0` como criterio de cierre.
    - Contiene el pre-check `\d voto` incluye 'ausente' (0019 en la DB REMOTA).
    - NO contiene ninguna credencial literal (solo nombres de env vars); grep de patrones tipo `sb_secret_`/`R2_ACCESS_KEY_ID=` con valor → 0 matches.
  </acceptance_criteria>
  <done>Runbook completo, ejecutable por el operador, apoyado en el wire del Plan 01 y en `--boletines-file`; sin credenciales; invariante de cobertura como gate de cierre.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT operador-LOCAL): Correr el backfill LIVE a escala + write REMOTO + reporte de cobertura</name>
  <what-built>El wire de dos-etapas para votos (Plan 01) + el runbook de backfill (Task 1). El backfill LIVE a escala y la escritura a la Supabase REMOTA son operador-LOCAL por regla LOCKED (backfill masivo = LOCAL; golpea el WAF gubernamental con rate-limit 2-3s; escribe a PROD).</what-built>
  <how-to-verify>
    1. Correr los PRE-CHECKS del runbook: `pnpm --filter @obs/votos test golden-dipid` verde; suite offline verde; `\d voto` incluye 'ausente' en la DB REMOTA; `.env` con R2_* + SUPABASE_API_URL + SUPABASE_SECRET_KEY.
    2. Derivar `boletines.txt` de `SELECT boletin FROM proyecto` y particionar en lotes.
    3. Correr LIVE lote por lote: `VOTOS_LIVE=1 pnpm --filter @obs/votos exec tsx src/run-votos-masivo-cli.ts --boletines-file boletines-lote-NN.txt` — confirmar en el log que el destino es el endpoint REMOTO antes de dejarlo correr; respetar el rate-limit 2-3s.
    4. Tras los lotes, revisar el reporte de cobertura: N confirmado / M total, y CONFIRMAR `dipidsMaestraNoConfirmados === 0`.
    5. Confirmar que hay crudo de votos en R2 (primeros snapshots).
  </how-to-verify>
  <files>(operador-LOCAL) boletines.txt derivado de proyecto; R2 (crudo de votos); Supabase REMOTA (voto/votacion)</files>
  <action>OPERADOR-LOCAL, NO el agente: seguir 66-BACKFILL-RUNBOOK.md end-to-end. El agente NO ejecuta `VOTOS_LIVE=1` ni escribe a PROD; este checkpoint pausa para que el operador corra el backfill LIVE con rate-limit 2-3s y reporte la cobertura.</action>
  <verify>Reporte de cobertura N confirmado / M total impreso por el CLI + `dipidsMaestraNoConfirmados === 0`; crudo de votos presente en R2.</verify>
  <done>Todos los lotes corridos; cobertura declarada; invariante DIPID-maestra en 0; primeros snapshots de votos en R2.</done>
  <resume-signal>Escribe "backfill hecho" con la cobertura N/M y el conteo del invariante (esperado 0), o describe el problema (p.ej. un DIPID-maestra quedó no_confirmado → BUG que detiene el cierre).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → opendata.camara.cl | Fetch a escala del WS gubernamental (rate-limit 2-3s LOCKED) |
| CLI de operador → Supabase REMOTA | Escritura masiva a PROD con service key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02b | DoS | WS gubernamental (backfill a escala) | mitigate | Backfill LOCAL operador (NO CI/GitHub Actions); fetch serial + rate-limit 2-3s LOCKED; particionar en lotes; `--from-r2` para re-ingestar sin re-tocar la fuente. |
| T-66-05b | Elevation/Tampering | Escritura accidental a PROD desde CI | mitigate | El backfill LIVE + write REMOTO es operador-LOCAL (autonomous:false); el agente NO corre `VOTOS_LIVE=1`. Verificar el destino REMOTO en el log antes de escribir (Pitfall 5). |
| T-66-01b | Tampering/Repudiation | Atribución errónea al escalar a periodos históricos | mitigate | DIPID determinista fail-closed; el invariante de cierre "0 DIPID-maestra no_confirmado" DETIENE el cierre si se viola; no_confirmado de periodos históricos es correcto (fail-closed), no un fallo. |
| T-66-06 | Info Disclosure | Credenciales en el runbook o en logs | mitigate | El runbook referencia nombres de env vars, nunca valores; el CLI loguea URL de destino + counts, no service key ni filas PII. |
</threat_model>

<verification>
- `66-BACKFILL-RUNBOOK.md` existe con las 7 secciones y el comando LIVE gated.
- Pre-checks del runbook son verificables offline por el operador antes de la corrida LIVE.
- El invariante de cierre `dipidsMaestraNoConfirmados === 0` está declarado como gate.
- (Operador, fuera de CI) Tras la corrida: cobertura N/M declarada, crudo de votos en R2, invariante en 0.
</verification>

<success_criteria>
- Backfill a escala documentado como operador-LOCAL reanudable por `--boletines-file`, rate-limit 2-3s (SC#3).
- Escritura a la Supabase REMOTA con verificación de destino antes de escribir (Pitfall 5).
- Cobertura confirmado/no_confirmado medida tras el backfill + invariante "0 DIPID-maestra no_confirmado" como gate (SC#4).
- La corrida LIVE es un acto deliberado del operador — el agente NO golpea el WAF ni escribe PROD.
</success_criteria>

<output>
Create `.planning/phases/66-voto-p3c-wire-dos-etapas-c-mara-backfill-a-escala-funde-debt/66-02-SUMMARY.md` when done
</output>
