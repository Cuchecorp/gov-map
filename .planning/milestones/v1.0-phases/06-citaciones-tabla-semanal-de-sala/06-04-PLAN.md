---
phase: 06-citaciones-tabla-semanal-de-sala
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-03]
files_modified:
  - app/lib/agenda-types.ts
  - app/lib/week-utils.ts
  - app/lib/week-utils.test.ts
  - app/components/ui/button.tsx
  - app/components/week-nav.tsx
  - app/components/citacion-card.tsx
  - app/components/citacion-card.test.tsx
  - app/components/sala-table-section.tsx
  - app/components/sala-table-section.test.tsx
  - app/app/agenda/page.tsx
  - app/package.json
autonomous: false
requirements: [TRAM-07, TRAM-08]

must_haves:
  truths:
    - "Un ciudadano abre /agenda, navega por semana ISO (Link server nav) y ve las citaciones de ambas cámaras agrupadas por día"
    - "Cada CitacionCard muestra comisión/horario/sala/materia/invitados/chip de cámara/ProvenanceBadge y enlaza el boletín a /proyecto/[boletin]"
    - "Los invitados se muestran como texto crudo (nombre + calidad) SIN IdentityMarker y sin link a parlamentario"
    - "SalaTableSection renderiza la tabla del Senado (available) y la degradación honesta de Cámara (PDF link, sin filas inventadas)"
    - "Una corrida LIVE acotada deja citaciones reales de ambas cámaras + tabla del Senado en Supabase local con provenance, idempotente"
  artifacts:
    - path: "app/app/agenda/page.tsx"
      provides: "Ruta /agenda Server Component, semana navegable"
      min_lines: 50
    - path: "app/components/citacion-card.tsx"
      provides: "Tarjeta de citación (invitados sin IdentityMarker)"
      min_lines: 40
    - path: "app/components/sala-table-section.tsx"
      provides: "Tabla del Senado available + degradación Cámara"
      min_lines: 40
    - path: "app/lib/week-utils.ts"
      provides: "parseISOWeek + getWeekBounds (frontend)"
      min_lines: 20
  key_links:
    - from: "app/components/citacion-card.tsx"
      to: "/proyecto/[boletin] (Fase 5)"
      via: "Link interno cuando hay boletín"
      pattern: "/proyecto/"
    - from: "app/app/agenda/page.tsx"
      to: "citacion (Supabase anon)"
      via: "query WHERE semana_iso"
      pattern: "semana_iso|from\\(\"citacion\"\\)"
    - from: "app/components/citacion-card.tsx"
      to: "ProvenanceBadge (Fase 5)"
      via: "import + render"
      pattern: "ProvenanceBadge"
---

<objective>
Cierra la rebanada ciudadana: construye `/agenda` (Next.js 16, Server Components) que lee las tablas de agenda de Supabase (anon, RLS public-read de 06-01) y renderiza la semana navegable con las citaciones de ambas cámaras (CitacionCard: comisión/horario/sala/materia/invitados/chip/ProvenanceBadge + enlace de boletín a la ficha de Fase 5) y la tabla de sala (Senado estructurada / Cámara degradada al PDF). Reusa el design system de Fase 5 verbatim. Termina con una corrida LIVE acotada que deja datos reales en Supabase local — el slice queda vivo end-to-end.

Purpose: Es la cara ciudadana de TRAM-07/08 y el cierre del MVP de la fase: "un ciudadano abre /agenda, navega la semana y ve TODAS las citaciones de ambas cámaras + la tabla de sala, cada dato con frescura+fuente, alimentado por una ingesta real".
Output: `/agenda` + 3 componentes nuevos + helpers de semana + corrida LIVE acotada verificada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-UI-SPEC.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-01-SUMMARY.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-03-SUMMARY.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-04-SUMMARY.md
@app/app/proyecto/[boletin]/page.tsx
@app/lib/types.ts
@app/lib/supabase.ts
@app/components/provenance-badge.tsx

<interfaces>
<!-- Patrones de Fase 5 ya en el codebase. Reusar, NO re-explorar. -->

IMPORTANTE (app/AGENTS.md): este Next.js tiene breaking changes vs tu conocimiento.
LEER node_modules/next/dist/docs/ del área relevante (searchParams, Server Components,
dynamic routes) ANTES de escribir código de ruta. `params`/`searchParams` son Promise (await).

createServerSupabase() (app/lib/supabase.ts): cliente server-only, anon key SIN NEXT_PUBLIC_.
  Lee SUPABASE_URL / SUPABASE_ANON_KEY del entorno.

ProvenanceBadge (app/components/provenance-badge.tsx):
  props { capturedAt: Date|null, sourceName: string, sourceUrl: string|null }. Reusar tal cual.

CamaraChip (app/components/camara-chip.tsx): reusar (chip "Cámara"/"Senado").
EtapaBadge (app/components/etapa-badge.tsx): reusar para la columna Etapa de la tabla del Senado.
sourceLabel(origen) (app/lib/types.ts): "Cámara"/"Senado"/"BCN"/"fuente desconocida".

Tablas Supabase (de migración 0010, snake_case, RLS public-read anon):
  citacion(id, camara, comision, fecha, horario, sala, materia, estado, semana_iso, origen, fecha_captura, enlace)
  citacion_invitado(citacion_id, nombre, calidad)
  citacion_punto(citacion_id, boletin, id_proyecto, materia, tipo_tramite)
  sesion_sala(id, camara, fecha, numero, hora_inicio, tipo, origen, fecha_captura, enlace)
  sesion_tabla_item(sesion_id, posicion, parte_sesion, materia, boletin, id_proyecto, alias, quorum)

UI-SPEC §0: añadir shadcn Button (`pnpm dlx shadcn@latest add button`) — único componente nuevo.
  En Windows no-interactivo, copiar el código del registro oficial (patrón 05-04), no `init`.
UI-SPEC §3-6: WeekNav (Link prev/next, server nav, sin "use client"), CitacionCard, SalaTableSection
  (available=tabla / degraded=border-border bg-muted/40, PDF link, NUNCA "próximamente").
UI-SPEC §4: invitados = texto crudo nombre + (calidad) — NO IdentityMarker, NO link a parlamentario.
UI-SPEC §12.2: parseISOWeek(semana)→{year,week}, getWeekBounds(year,week)→{start,end} en app/lib/week-utils.ts.
URL: /agenda?semana=2026-W25 ; ausente/malformado → semana ISO actual (sin redirect).

Frontend desacoplado del paquete backend (patrón 05-04): app define tipos locales en
  app/lib/agenda-types.ts (filas snake_case), NO importa @obs/agenda.

Degradación Cámara (UI-SPEC §6.2): PDF oficial verDoc.aspx?prmTipo=TABLASEMANAL + copy honesto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers de semana + tipos locales + shadcn Button</name>
  <files>app/lib/agenda-types.ts, app/lib/week-utils.ts, app/lib/week-utils.test.ts, app/components/ui/button.tsx, app/package.json</files>
  <read_first>app/lib/types.ts, app/lib/format.test.ts, app/components/ui/badge.tsx</read_first>
  <action>Crear `app/lib/week-utils.ts` con `parseISOWeek(semana: string): { year: number; week: number }` (parsea `YYYY-Www`, tolerante a malformado → fallback a la semana ISO actual) y `getWeekBounds(year, week): { start: Date; end: Date }` (lunes–domingo de la semana ISO), más `currentISOWeek()` y `formatWeekLabel(year, week)` ("Semana 25 · 16–22 jun 2026", es-CL). Tests de bordes de año (espejo del helper de 06-02 pero en el frontend). Crear `app/lib/agenda-types.ts` con las filas snake_case (`CitacionRow`, `CitacionInvitadoRow`, `CitacionPuntoRow`, `SesionSalaRow`, `SesionTablaItemRow`) — el frontend NO importa `@obs/agenda`. Añadir el componente shadcn `Button` copiando el código del registro oficial (Default/Slate, igual que las primitivas de 05-04); registrar sus deps si faltan (`@radix-ui/react-slot` ya está).</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm vitest run week-utils && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>week-utils tests verdes (bordes ISO), tipos locales definidos, Button disponible, tsc exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeekNav + CitacionCard + SalaTableSection</name>
  <files>app/components/week-nav.tsx, app/components/citacion-card.tsx, app/components/citacion-card.test.tsx, app/components/sala-table-section.tsx, app/components/sala-table-section.test.tsx</files>
  <read_first>app/components/provenance-badge.tsx, app/components/camara-chip.tsx, app/components/etapa-badge.tsx, app/components/voto-row.tsx, .planning/phases/06-citaciones-tabla-semanal-de-sala/06-UI-SPEC.md</read_first>
  <behavior>
    - citacion-card.test.tsx: con `boletin` presente renderiza un `<a href="/proyecto/{boletin}">`; con invitados renderiza nombre + (calidad) como texto plano y NO renderiza ningún enlace `/parlamentario/` ni IdentityMarker; renderiza el ProvenanceBadge.
    - sala-table-section.test.tsx: `mode="available"` con items renderiza filas de la tabla (boletín → link `/proyecto/`); `mode="degraded"` renderiza el copy "Tabla de sala no disponible" + el link al PDF de Cámara y NO usa estilo destructive ni dice "próximamente".
  </behavior>
  <action>Implementar los 3 componentes per UI-SPEC §3-6 (Server Components, sin "use client"). `WeekNav`: `<nav>` con dos `<Button asChild><Link href="/agenda?semana={prev/next}">` (server nav, aria-labels "Semana anterior/siguiente") + label central `formatWeekLabel`. `CitacionCard`: `Card` con CamaraChip + fecha/horario (mono) + comisión + sala (header), y materia (line-clamp-3, `<details>` para ver más sin JS) + bloque invitados (nombre + `(calidad)` texto plano, SIN IdentityMarker, SIN link a parlamentario — son gestores de interés/terceros, per D de invitados) + enlace de boletín interno `/proyecto/[boletin]` (cuando hay punto con boletín) + `ProvenanceBadge` al final (sourceName via sourceLabel, sourceUrl = enlace de la fila). `SalaTableSection`: prop `mode: "available" | "degraded"`; available → `<table>` con caption sr-only, columnas N°/Boletín/Materia/Etapa, boletín→link, + ProvenanceBadge; degraded → contenedor `border-border bg-muted/40` con el copy honesto y el link al PDF oficial de Cámara (`verDoc.aspx?prmTipo=TABLASEMANAL`), SIN estilo destructive, SIN "próximamente". Tests RTL de los comportamientos del bloque <behavior>.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm vitest run citacion-card sala-table-section</automated>
  </verify>
  <done>3 componentes implementados; tests verdes; invitados sin IdentityMarker/link; degradación honesta sin destructive ni "próximamente".</done>
</task>

<task type="auto">
  <name>Task 3: Ruta /agenda (Server Component, semana navegable)</name>
  <files>app/app/agenda/page.tsx, app/package.json</files>
  <read_first>app/app/proyecto/[boletin]/page.tsx, app/lib/week-utils.ts, app/components/week-nav.tsx, app/components/citacion-card.tsx, app/components/sala-table-section.tsx</read_first>
  <action>LEER primero node_modules/next/dist/docs/ sobre searchParams en Server Components (este Next.js tiene breaking changes; `searchParams` es Promise → await). Crear `app/app/agenda/page.tsx` (Server Component): leer `searchParams.semana` (await), `parseISOWeek` con fallback a la semana ISO actual si ausente/malformado (sin redirect). Layout `max-w-3xl mx-auto px-4 md:px-8` per UI-SPEC §2. Render: `<h1>Agenda legislativa</h1>`, `<WeekNav>`, sección citaciones (query `citacion` `WHERE semana_iso = key` o por rango de `getWeekBounds`, con embeds `citacion_invitado` y `citacion_punto`, order `fecha ASC, camara ASC, comision ASC`, agrupadas por día → `CitacionCard`; empty state UI-SPEC §7.1), sección tabla de sala (query `sesion_sala` + `sesion_tabla_item` de la semana → si hay filas del Senado: `SalaTableSection mode="available"`; SIEMPRE incluir la degradación de Cámara `mode="degraded"` con el PDF link). Cada sección en su `<Suspense>` con skeleton (UI-SPEC §7.2). Validar/normalizar el `semana` param (formato `\d{4}-W\d{2}`) antes de usarlo. Las lecturas usan `createServerSupabase()` (anon).</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio/app" && pnpm exec tsc --noEmit && pnpm build 2>&1 | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{process.exit(/\/agenda/.test(s)&&!/Failed to compile|Error:/.test(s)?0:1)})"</automated>
  </verify>
  <done>`/agenda` compila en el build (ruta presente), tsc exit 0, lee las tablas de agenda como anon, navega por semana, degradación de Cámara siempre visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Corrida LIVE acotada + verificación visual de /agenda</name>
  <action>Correr la ingesta LIVE acotada (de 06-03) con los conectores REALES + `SupabaseAgendaWriter` contra el Supabase local (varias semanas ISO recientes + algunas históricas de Cámara + la ventana forward-only del Senado + la tabla `weekly_table` del Senado), luego servir `/agenda` localmente para la verificación humana descrita abajo.</action>
  <what-built>Corrida LIVE acotada de la agenda: `runIngest` (de 06-03) con los conectores REALES + `SupabaseAgendaWriter` contra el Supabase local, enumerando varias semanas ISO recientes + algunas históricas de Cámara (p.ej. 2026-W25, 2026-W24, 2024-W20 como muestra representativa) y la ventana forward-only del Senado + la tabla `weekly_table` del Senado. Luego `/agenda` servida localmente leyendo esos datos reales.</what-built>
  <how-to-verify>
    1. Asegurar Supabase local arriba (`supabase status`) y `supabase db reset` (aplica 0001→0010).
    2. Correr la ingesta LIVE acotada: `pnpm --filter @obs/agenda ingest -- --desde=2026-W24 --hasta=2026-W25` (+ una semana histórica como muestra). Con header-set anti-Cloudflare. Si Cámara da 403 PERSISTENTE desde este egreso → usar browseros como fallback para capturar, y confirmar que el Senado se pobló igual (degradación sin abortar).
    3. Confirmar en la DB local: `citacion` > 0 de AMBAS cámaras, `citacion_invitado`/`citacion_punto` poblados, `sesion_sala`/`sesion_tabla_item` > 0 (Senado); 0 filas de tabla para Cámara; provenance (origen/fecha_captura/enlace) en cada fila; 0 errores 403/429/500 desde el egreso real (o degradación documentada de Cámara).
    4. Re-correr el mismo comando → conteos idénticos (idempotente).
    5. `cd app && pnpm start` (con SUPABASE_URL/ANON_KEY del local) y abrir `http://localhost:3000/agenda?semana=2026-W25`: ver citaciones de ambas cámaras agrupadas por día con chips + ProvenanceBadge + enlaces de boletín a `/proyecto/[boletin]`; la tabla del Senado como dato y la de Cámara como "no disponible + PDF oficial"; navegar prev/next de semana funciona; 0 enlaces `/parlamentario/` desde invitados.
  </how-to-verify>
  <verify>
    <human-check>Operador confirma en la DB local + en `/agenda` servida: citaciones reales de ambas cámaras + tabla del Senado con provenance, idempotente, sin fabricar la tabla de Cámara, 0 enlaces /parlamentario/ desde invitados.</human-check>
  </verify>
  <done>Corrida LIVE acotada con datos reales de ambas cámaras + tabla del Senado en Supabase local (provenance, idempotente, degradación honesta de Cámara), y `/agenda` servida muestra el slice vivo.</done>
  <resume-signal>Escribe "approved" si la agenda muestra citaciones reales de ambas cámaras + tabla del Senado con provenance (idempotente, sin fabricar la tabla de Cámara), o describe los problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador→/agenda | `semana` searchParam es input no confiable |
| anon→Postgres | lectura pública de las tablas de agenda |
| corrida LIVE→fuente gov | conectores reales cruzan a hosts gubernamentales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | injection vía `semana` searchParam | mitigate | validar el formato `\d{4}-W\d{2}` antes de usarlo; fallback a semana actual; supabase-js `.eq()`/rango parametriza |
| T-06-02 | Tampering | identidad de invitados en la UI | mitigate | invitados como texto crudo (nombre+calidad), SIN IdentityMarker, SIN link a `/parlamentario/`; test RTL afirma 0 enlaces de parlamentario |
| T-06-11 | Information Disclosure | anon key en el bundle del cliente | mitigate | `createServerSupabase()` server-only, anon key sin `NEXT_PUBLIC_`; lecturas en Server Components (patrón 05-04, T-05-10) |
| T-06-09 | Tampering | fabricar tabla de sala de Cámara | mitigate | `SalaTableSection mode="degraded"` muestra PDF link honesto; NUNCA filas sintéticas ni "próximamente"; test RTL lo afirma |
| T-06-07 | DoS | WAF de Cámara en la corrida LIVE | mitigate | rate-limit 2-3s (reuso @obs/ingest) + header-set CF + alcance acotado; degradación de Cámara sin abortar Senado |
</threat_model>

<verification>
- `app` build verde con `/agenda` presente; `tsc --noEmit` exit 0.
- `pnpm vitest run` (app): week-utils + citacion-card + sala-table-section verdes.
- Corrida LIVE acotada: citaciones reales de ambas cámaras + tabla del Senado en Supabase local con provenance, idempotente, 0 errores (o degradación de Cámara documentada).
- `/agenda` servida muestra el slice vivo; 0 enlaces `/parlamentario/` desde invitados.
</verification>

<success_criteria>
- `/agenda` navegable por semana ISO (server nav).
- CitacionCard con comisión/horario/sala/materia/invitados/chip/ProvenanceBadge + boletín link; invitados sin IdentityMarker.
- SalaTableSection: Senado available + Cámara degradada (PDF) honesta.
- Corrida LIVE acotada con datos reales de ambas cámaras (énfasis del usuario: TODAS las citaciones), idempotente.
</success_criteria>

<output>
Create `.planning/phases/06-citaciones-tabla-semanal-de-sala/06-04-SUMMARY.md` when done
</output>
