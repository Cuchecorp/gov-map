---
phase: 06-citaciones-tabla-semanal-de-sala
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agenda/package.json
  - packages/agenda/tsconfig.json
  - packages/agenda/vitest.config.ts
  - packages/agenda/src/index.ts
  - packages/agenda/src/model.ts
  - packages/agenda/src/model.test.ts
  - packages/agenda/src/slice.e2e.test.ts
  - packages/agenda/test/fixtures/camara-citaciones-semana.html
  - packages/agenda/test/fixtures/senado-commissions-citations.json
  - packages/agenda/test/fixtures/senado-weekly-table.json
  - supabase/migrations/0010_agenda.sql
  - supabase/tests/0008_agenda.test.sql
  - tsconfig.json
  - tsconfig.base.json
autonomous: false
requirements: [TRAM-07, TRAM-08]

must_haves:
  truths:
    - "El paquete @obs/agenda existe en el workspace y typecheck pasa exit 0"
    - "El modelo común Citacion/CitacionInvitado/CitacionPunto/SesionSala/SesionTablaItem está tipado y validado con zod (boletín = llave de cruce con Fase 5)"
    - "La migración 0010 crea las tablas de agenda con RLS public-read EXPLÍCITO para anon (policy + grant)"
    - "anon SÍ lee las tablas de agenda y NO lee parlamentario.rut (pgTAP)"
    - "Existen fixtures reales de las 3 fuentes (HTML Cámara, JSON commissions_citations, JSON weekly_table)"
    - "slice.e2e.test.ts está en RED por símbolos ausentes (diana ciudadana de la fase)"
  artifacts:
    - path: "packages/agenda/src/model.ts"
      provides: "Modelo común agenda + zod schemas"
      min_lines: 60
    - path: "supabase/migrations/0010_agenda.sql"
      provides: "4 tablas de agenda + RLS public-read anon"
      contains: "create policy"
    - path: "supabase/tests/0008_agenda.test.sql"
      provides: "pgTAP RLS public-read + guarda rut"
      contains: "set local role anon"
    - path: "packages/agenda/src/slice.e2e.test.ts"
      provides: "Diana E2E RED de la fase"
  key_links:
    - from: "supabase/tests/0008_agenda.test.sql"
      to: "parlamentario.rut"
      via: "guarda anon is_empty"
      pattern: "is_empty.*rut"
    - from: "supabase/migrations/0010_agenda.sql"
      to: "anon"
      via: "grant select + policy"
      pattern: "grant select.*anon"
---

<objective>
Funda el slice de Agenda: da de alta `@obs/agenda` en el workspace (espejando `@obs/tramitacion`), materializa el modelo común de citaciones + tabla de sala (tipos + zod, con boletín como llave de cruce hacia la ficha de Fase 5), crea la migración `0010` con las tablas de agenda y RLS public-read EXPLÍCITO para `anon` (probado por pgTAP), persiste los fixtures reales de las 3 fuentes ya capturadas, y deja `slice.e2e.test.ts` en RED como diana ciudadana de la fase.

Purpose: Es el walking-skeleton de la fase. Sin RLS public-read explícito la `/agenda` quedaría en blanco (Pitfall 5). El modelo común es el contrato que las olas 2-4 consumen.
Output: Paquete `@obs/agenda` (scaffold + modelo), migración 0010 + pgTAP 0008, 3 fixtures reales, slice E2E RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-CONTEXT.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-RESEARCH.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-01-SUMMARY.md

<interfaces>
<!-- Patrón a espejar (de Fase 5, verificado en codebase). NO re-explorar. -->

packages/tramitacion/package.json (espejar; cambiar name → @obs/agenda):
- type:module, main/types → src/index.ts, exports "." → ./src/index.ts
- scripts: test (vitest run), typecheck (tsc -b), ingest (tsx src/ingest-cli.ts)
- deps: @obs/core, @obs/ingest (workspace:*), @supabase/supabase-js ^2.108.2, zod ^4.4.3
  - cheerio ^1.2.0 (NUEVO — ver nota de paquete abajo)
  - NO @obs/adjudication ni @obs/identity (los invitados NO se reconcilian contra la maestra)
  - NO fast-xml-parser (las 3 fuentes son HTML+JSON, no XML)
- devDeps: @types/node ^20.19.43, tsx ^4.22.4, vitest ^3.0.0

@obs/core exporta `Provenance` (origen/fecha_captura/enlace) — reusar inline en las entidades (TRAM-09).

tsconfig.json raíz: añadir project reference { "path": "./packages/agenda" }.
tsconfig.base.json: añadir path mapping "@obs/agenda" → ["packages/agenda/src/index.ts"].

Migración previa más alta: 0009_voto_fuente_voter_id.sql → la nueva es 0010.
Test pgTAP previo más alto: 0007_tramitacion.test.sql → el nuevo es 0008.

Patrón RLS public-read de 0008_tramitacion.sql (verbatim a replicar por tabla):
  alter table <t> enable row level security;
  create policy <t>_public_read on <t> for select to anon using (true);
  grant select on <t> to anon;
(La policy SIN el grant no expone filas — ambos son necesarios.)

Patrón pgTAP de 0007_tramitacion.test.sql:
  - plan(N) con N = nº exacto de asserts (un off-by-N hace FAIL aunque pasen)
  - sembrar como owner → `set local role anon` → isnt_empty en cada tabla de agenda
  - is_empty($$select rut from parlamentario$$) como rol anon (guarda T-06-01)
  - `supabase db reset` aplica 0001→0010 antes de `supabase test db`
</interfaces>
</context>

<package_note>
**Paquete nuevo de runtime: `cheerio@1.2.0`.** NO es un paquete sin auditar: ya está en el workspace (usado por @obs/tramitacion en Fases 3/5) y figura en la Package Legitimacy Audit del RESEARCH como "Reusado (auditado Fase 5) — no re-instalar". Añadirlo como dependencia de `@obs/agenda` reusa la versión ya presente en el lockfile; NO se instala nada nuevo del registro. Por eso NO requiere checkpoint de legitimidad de paquete (el gate aplica a `[ASSUMED]`/`[SUS]`, y cheerio es `[VERIFIED: codebase]`).
</package_note>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @obs/agenda + alta en el workspace</name>
  <files>packages/agenda/package.json, packages/agenda/tsconfig.json, packages/agenda/vitest.config.ts, packages/agenda/src/index.ts, tsconfig.json, tsconfig.base.json</files>
  <read_first>packages/tramitacion/package.json, packages/tramitacion/tsconfig.json, packages/tramitacion/vitest.config.ts, tsconfig.json, tsconfig.base.json</read_first>
  <action>Crear el paquete `@obs/agenda` espejando `@obs/tramitacion`. `package.json`: name `@obs/agenda`, type:module, main/types → `src/index.ts`, exports "." → `./src/index.ts`; scripts `test`/`typecheck`/`ingest`; deps `@obs/core`+`@obs/ingest` (workspace:*), `@supabase/supabase-js`, `cheerio@^1.2.0`, `zod`; NO incluir `@obs/adjudication`/`@obs/identity`/`fast-xml-parser`. `tsconfig.json` composite con project references a `@obs/core` y `@obs/ingest` (lib incluyendo DOM para cheerio). `vitest.config.ts` igual al de tramitacion (sin RTL — esto es paquete de backend). Barrel `src/index.ts` que por ahora re-exporta solo `./model` (creado en Task 2). Registrar el paquete: añadir `{ "path": "./packages/agenda" }` a `references` en `tsconfig.json` raíz y el path mapping `"@obs/agenda": ["packages/agenda/src/index.ts"]` en `tsconfig.base.json`. Correr `pnpm install` para enlazar el workspace.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm -w typecheck</automated>
  </verify>
  <done>`@obs/agenda` existe en el workspace, project reference + path mapping registrados, `pnpm -w typecheck` exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelo común agenda + zod + migración 0010 + pgTAP 0008</name>
  <files>packages/agenda/src/model.ts, packages/agenda/src/model.test.ts, packages/agenda/src/index.ts, supabase/migrations/0010_agenda.sql, supabase/tests/0008_agenda.test.sql</files>
  <read_first>packages/tramitacion/src/model.ts, supabase/migrations/0008_tramitacion.sql, supabase/tests/0007_tramitacion.test.sql</read_first>
  <behavior>
    - model.test.ts (RED→GREEN): el `CitacionSchema` de zod valida un objeto de citación de Cámara (camara="camara", comision, fecha, horario, sala, materia, estado nullable, semana_iso, invitados[], puntos[]) y rechaza uno sin `comision`.
    - El `SesionTablaItemSchema` valida un item de tabla del Senado (posicion int, parte_sesion, materia, boletin nullable, id_proyecto nullable) y rechaza `posicion` no-entero.
    - Provenance inline (origen/fecha_captura/enlace) es requerido en `Citacion`, `SesionSala` (TRAM-09).
  </behavior>
  <action>Crear el modelo común en `model.ts` (per Claude's Discretion sobre el esquema fino, respetando los shapes del RESEARCH):
- `Citacion`: `id` sintético (clave natural estable), `camara` ("camara"|"senado"), `comision`, `fecha` (Date/ISO), `horario` (texto crudo de la fuente), `sala`, `materia`, `estado` (nullable — "Suspendida"/"Sin efecto"/null), `semana_iso` (`YYYY-Www`), `invitados: CitacionInvitado[]`, `puntos: CitacionPunto[]`, provenance inline.
- `CitacionInvitado`: `nombre`, `calidad` (nullable — rol/calidad cruda; estos son gestores de interés/terceros, NO parlamentarios → sin parlamentario_id, sin reconciliación).
- `CitacionPunto`: `boletin` (nullable, formato `NNNNN-NN` → cruce con `proyecto.boletin`), `id_proyecto` (nullable), `materia` (nullable), `tipo_tramite` (nullable).
- `SesionSala`: `id` (= ID_SESION del Senado), `camara`, `fecha`, `numero` (nullable), `hora_inicio` (nullable), `tipo` (nullable), `items: SesionTablaItem[]`, provenance inline.
- `SesionTablaItem`: `posicion` (int), `parte_sesion` ("ORDEN DEL DÍA"|"TIEMPO DE VOTACIONES"|texto), `materia`, `boletin` (nullable), `id_proyecto` (nullable), `alias` (nullable), `quorum` (nullable).
Exportar tipos + zod schemas desde el barrel `index.ts`.
Crear `0010_agenda.sql` con DDL de 4 tablas: `citacion` (PK id, columnas del modelo + `semana_iso` text indexado + provenance), `citacion_invitado` (citacion_id FK on delete cascade, nombre, calidad nullable), `citacion_punto` (citacion_id FK, boletin nullable indexado, id_proyecto, materia, tipo_tramite), `sesion_sala` (PK id, camara, fecha, provenance) con `sesion_tabla_item` (sesion_id FK, posicion, parte_sesion, materia, boletin nullable, id_proyecto, alias, quorum). Claves naturales TOTALES para upsert idempotente (`citacion.id` PK; `unique(citacion_id, nombre)` en invitado; `unique(citacion_id, posicion/boletin)` en punto; `sesion_sala.id` PK; `unique(sesion_id, posicion)` en item). Índices de la `/agenda`: `citacion(semana_iso)`, `citacion(fecha)`, `citacion_punto(boletin)`, `sesion_tabla_item(boletin)`. **RLS public-read EXPLÍCITO** en las 5 tablas: `enable row level security` + `create policy ..._public_read for select to anon using(true)` + `grant select ... to anon`. `parlamentario` intacta.
Crear `0008_agenda.test.sql` (pgTAP): existencia de las 5 tablas + columnas provenance, checks de `camara`, FKs, RLS habilitada; prueba EFECTIVA de RLS sembrando una citación + sesión como owner, `set local role anon`, `isnt_empty` en las 5 tablas de agenda y `is_empty($$select rut from parlamentario$$)` (guarda T-06-01). `plan(N)` con N = nº exacto de asserts.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test model && supabase db reset && supabase test db</automated>
  </verify>
  <done>`model.test.ts` verde; `supabase test db` PASS con la guarda anon (lee agenda, no lee parlamentario.rut); migración 0010 aplica limpia desde 0001.</done>
</task>

<task type="auto">
  <name>Task 3: Fixtures reales de las 3 fuentes + slice E2E (RED)</name>
  <files>packages/agenda/test/fixtures/camara-citaciones-semana.html, packages/agenda/test/fixtures/senado-commissions-citations.json, packages/agenda/test/fixtures/senado-weekly-table.json, packages/agenda/src/slice.e2e.test.ts</files>
  <read_first>packages/tramitacion/src/slice.e2e.test.ts, .planning/phases/06-citaciones-tabla-semanal-de-sala/06-RESEARCH.md</read_first>
  <action>Capturar/persistir los fixtures reales de las 3 fuentes confirmadas LIVE (ya validadas en el RESEARCH). Para Cámara: `GET https://www.camara.cl/legislacion/comisiones/citaciones_semana.aspx?prmSemana=2026-25` con el header-set de navegador anti-Cloudflare (Pattern 2 del RESEARCH: `User-Agent` Chrome + sufijo `Bot-Ciudadano/1.0`, `Accept`, `Accept-Language`, `Sec-Ch-Ua*`, `Sec-Fetch-*`, `Upgrade-Insecure-Requests`); guardar el HTML descomprimido (~235KB) como `camara-citaciones-semana.html`. Para Senado: `GET https://web-back.senado.cl/api/commissions_citations?limit=100` → `senado-commissions-citations.json`; `GET https://web-back.senado.cl/api/weekly_table?limit=100` → `senado-weekly-table.json`. Respetar delay 2-3s + UA identificatorio entre requests. Si Cámara da 403 persistente desde este egreso, usar browseros (navegador real) como fallback documentado para capturar el HTML — NO inventar el fixture. Crear `slice.e2e.test.ts` que importe `parseCamaraCitaciones`, `parseSenadoCitaciones`, `parseSenadoTabla` (NO existen aún → RED por símbolos ausentes, no por `it.todo`): cargar los 3 fixtures desde disco, afirmar que el parser de Cámara devuelve ≥1 `Citacion` con `comision`/`horario`/`materia`, que el de Senado devuelve `Citacion[]` con al menos un `CitacionPunto.boletin`, y que el de tabla del Senado devuelve un `SesionSala` con `items[]`. Esto es el contrato observable ciudadano de la fase.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && node -e "const fs=require('fs');const d='packages/agenda/test/fixtures';['camara-citaciones-semana.html','senado-commissions-citations.json','senado-weekly-table.json'].forEach(f=>{const s=fs.statSync(d+'/'+f).size;if(s<2000)throw new Error(f+' demasiado pequeño ('+s+'B) — posible 403/vacío');});console.log('fixtures OK')" && pnpm --filter @obs/agenda test slice.e2e 2>&1 | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{if(/parseCamaraCitaciones|is not (defined|a function)|failed to (load|resolve) import/.test(s)){console.log('RED esperado (símbolos ausentes)');process.exit(0)}process.exit(/FAIL|✓/.test(s)?0:1)})"</automated>
  </verify>
  <done>Los 3 fixtures existen con tamaño plausible (HTML >100KB, JSONs >2KB, no páginas de error CF); `slice.e2e.test.ts` falla por símbolos ausentes (parsers no implementados) — diana RED de la fase establecida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector→fuente gov | URL construida (semana ISO) cruza a `www.camara.cl`/`web-back.senado.cl` (input potencialmente manipulable) |
| anon→Postgres | el navegador público lee las tablas de agenda vía RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | RLS de las tablas de agenda | mitigate | policy `for select to anon using(true)` + `grant select` SOLO en las 5 tablas de agenda; `parlamentario` deny-by-default; pgTAP prueba anon lee agenda y NO lee `parlamentario.rut` |
| T-06-02 | Tampering | identidad de invitados | mitigate | `CitacionInvitado` SIN `parlamentario_id` ni reconciliación; nombre+calidad como texto crudo de la fuente (terceros/gestores de interés, no parlamentarios) |
| T-06-03 | DoS | fixture HTML gigante / parser | accept | fixtures acotados (~235KB Cámara); zod por fila en olas siguientes; cheerio no ejecuta scripts |
| T-06-SC | Tampering | npm install (cheerio) | mitigate | `cheerio@1.2.0` ya en el workspace (auditado Fase 5, `[VERIFIED: codebase]`); reuso del lockfile, sin instalación nueva del registro → no requiere checkpoint de legitimidad |
</threat_model>

<verification>
- `pnpm -w typecheck` exit 0 (paquete dado de alta).
- `pnpm --filter @obs/agenda test` verde para `model`; `slice.e2e` en RED por símbolos ausentes.
- `supabase db reset && supabase test db` PASS (pgTAP 0008 con guarda anon).
- 3 fixtures reales presentes con tamaño plausible.
</verification>

<success_criteria>
- `@obs/agenda` existe, typecheck verde.
- Modelo común + zod exportados desde el barrel.
- Migración 0010 con RLS public-read anon en las 5 tablas; pgTAP 0008 verde (anon lee agenda, no lee rut).
- Fixtures reales de las 3 fuentes persistidos.
- `slice.e2e.test.ts` RED (diana de la fase).
</success_criteria>

<output>
Create `.planning/phases/06-citaciones-tabla-semanal-de-sala/06-01-SUMMARY.md` when done
</output>
