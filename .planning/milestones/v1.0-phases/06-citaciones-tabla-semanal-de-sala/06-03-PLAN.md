---
phase: 06-citaciones-tabla-semanal-de-sala
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - packages/agenda/src/headers-camara.ts
  - packages/agenda/src/connector-camara.ts
  - packages/agenda/src/connector-camara.test.ts
  - packages/agenda/src/connector-senado.ts
  - packages/agenda/src/connector-senado.test.ts
  - packages/agenda/src/writer.ts
  - packages/agenda/src/writer.test.ts
  - packages/agenda/src/writer-supabase.ts
  - packages/agenda/src/ingest-run.ts
  - packages/agenda/src/ingest-run.test.ts
  - packages/agenda/src/ingest-cli.ts
  - packages/agenda/src/ingest-cli.test.ts
  - packages/agenda/src/index.ts
  - packages/agenda/package.json
autonomous: true
requirements: [TRAM-07, TRAM-08]

must_haves:
  truths:
    - "El conector de Cámara reusa @obs/ingest en el orden LOCKED (assertAllowedUrl→robots→rateLimiter.wait→fetcher.get) y envía el header-set anti-Cloudflare"
    - "El conector del Senado fetchea web-back.senado.cl/api/{commissions_citations,weekly_table} reusando @obs/ingest"
    - "El writer es idempotente por clave natural (2× no duplica) — in-memory y Supabase"
    - "runIngest enumera semanas ISO de Cámara, ingesta Senado forward-only, y la tabla de Cámara degrada al PDF sin fabricar filas"
    - "Si Cámara da 403 persistente, runIngest degrada esa fuente sin abortar la del Senado"
    - "assertAllowedUrl permite web-back.senado.cl (subdominio de senado.cl) — verificado por test"
  artifacts:
    - path: "packages/agenda/src/connector-camara.ts"
      provides: "Conector Cámara (cheerio + header-set CF + enumeración ISO)"
      min_lines: 40
    - path: "packages/agenda/src/connector-senado.ts"
      provides: "Conector Senado (web-back API + fallback _next/data)"
      min_lines: 40
    - path: "packages/agenda/src/writer-supabase.ts"
      provides: "upsert idempotente onConflict por clave natural"
      min_lines: 30
    - path: "packages/agenda/src/ingest-run.ts"
      provides: "Orquestación tolerante a fuentes vacías + degradación Cámara"
      min_lines: 40
  key_links:
    - from: "packages/agenda/src/connector-camara.ts"
      to: "@obs/ingest assertAllowedUrl/rateLimiter"
      via: "orden LOCKED de fetch"
      pattern: "rateLimiter\\.wait|assertAllowedUrl"
    - from: "packages/agenda/src/connector-camara.ts"
      to: "header-set anti-Cloudflare"
      via: "BROWSER_HEADERS_CAMARA"
      pattern: "Sec-Fetch|Sec-Ch-Ua"
    - from: "packages/agenda/src/writer-supabase.ts"
      to: "Postgres upsert"
      via: "onConflict clave natural"
      pattern: "onConflict"
---

<objective>
Cablea los conectores (Cámara con header-set anti-Cloudflare + enumeración de semanas ISO; Senado con la API backend `web-back.senado.cl`) reusando `@obs/ingest` en el orden LOCKED (NO `BaseConnector.run`), el writer idempotente (in-memory + Supabase), y `runIngest` + CLI que orquestan la corrida tolerante a fuentes vacías: enumera semanas de Cámara, ingesta la ventana forward-only del Senado, expone la tabla del Senado estructurada y degrada honestamente la tabla de Cámara al PDF. Si Cámara da 403 persistente, degrada esa fuente sin abortar el Senado.

Purpose: Es la maquinaria de ingesta de TRAM-07/08 — el mecanismo que permite la "cobertura completa" de citaciones (énfasis del usuario) y la degradación honesta de la tabla de Cámara. El backfill histórico masivo es job posterior con el mismo conector.
Output: 2 conectores + writer (×2) + runIngest + CLI, todos con tests (mock verifica el orden de @obs/ingest + el header-set CF).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-RESEARCH.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-01-SUMMARY.md
@.planning/phases/06-citaciones-tabla-semanal-de-sala/06-02-SUMMARY.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-05-SUMMARY.md

<interfaces>
<!-- @obs/ingest y el patrón de conector de Fase 5. NO re-explorar. -->

@obs/ingest exporta (verificado en codebase):
  Fetcher (get({url, host?, headers?}) → body), RobotsGuard (isAllowed(url)),
  HostRateLimiter (wait(host)), assertAllowedUrl(rawUrl, opts?) → URL,
  HostNotAllowedError.

ORDEN LOCKED del conector (de Fase 5, NO BaseConnector.run — su caché diaria saltaría re-corridas LIVE):
  assertAllowedUrl(url) → robots.isAllowed(url) → rateLimiter.wait(new URL(url).host) → fetcher.get({url, headers})

ALLOWLIST — YA cubre los hosts de esta fase (verificado en packages/ingest/src/allowlist.ts):
  DEFAULT_ALLOWED_SUFFIXES incluye "camara.cl" y "senado.cl".
  → `www.camara.cl` (citaciones + PDF) y `web-back.senado.cl` (APIs) son subdominios permitidos.
  El test allowlist.test.ts ya afirma `https://web-back.senado.cl/x` permitido.
  ⇒ NO hace falta MODIFICAR la allowlist. Este plan AÑADE un test en connector-senado.test.ts
    que afirma `assertAllowedUrl("https://web-back.senado.cl/api/commissions_citations")` NO lanza,
    documentando explícitamente la cobertura (cierra el ítem del key_facts sin tocar @obs/ingest).

BROWSER_HEADERS_CAMARA (verbatim del RESEARCH, Pattern 2 — UA simple da 403 Cloudflare):
  User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko)
               Chrome/124.0.0.0 Safari/537.36 Bot-Ciudadano/1.0 (consulta ciudadana Chile; contacto@dominio.cl)"
  Accept: "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8"
  Accept-Language: "es-CL,es;q=0.9,en;q=0.8"
  Sec-Ch-Ua: '"Chromium";v="124", "Google Chrome";v="124"'
  Sec-Ch-Ua-Mobile: "?0", Sec-Ch-Ua-Platform: '"Windows"'
  Sec-Fetch-Dest: "document", Sec-Fetch-Mode: "navigate", Sec-Fetch-Site: "none", Sec-Fetch-User: "?1"
  Upgrade-Insecure-Requests: "1"

URLs (validadas LIVE en RESEARCH):
  Cámara citaciones: https://www.camara.cl/legislacion/comisiones/citaciones_semana.aspx?prmSemana={año}-{semana}
  Cámara tabla PDF (degradación): https://www.camara.cl/verDoc.aspx?prmId=0&prmTipo=TABLASEMANAL
  Senado citaciones (preferida): https://web-back.senado.cl/api/commissions_citations?limit=100
  Senado tabla: https://web-back.senado.cl/api/weekly_table?limit=100
  Senado fallback: _next/data/{buildId}/...json (autodetectar buildId de __NEXT_DATA__, cachear ≤1 día)

Senado citaciones API: ventana FORWARD-ONLY (offset/fecha/year ignorados → sin histórico). NO fabricar histórico.

Writer (patrón Fase 5 writer-supabase.ts): SupabaseWriter.upsert(filas, { onConflict: '<clave natural>' });
  la service key NUNCA se interpola en mensajes de error (T-06-06).
  Tablas/claves naturales de 0010: citacion (PK id) / citacion_invitado unique(citacion_id,nombre) /
  citacion_punto unique(citacion_id,...) / sesion_sala (PK id) / sesion_tabla_item unique(sesion_id,posicion).

Parsers (de 06-02): parseCamaraCitaciones(html, semanaIso), parseSenadoCitaciones(json), parseSenadoTabla(json).
Helper ISO (de 06-02): enumerarSemanas, prmSemanaParam, semanaIsoKey.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conectores (Cámara header-set CF + Senado API backend) + writer idempotente</name>
  <files>packages/agenda/src/headers-camara.ts, packages/agenda/src/connector-camara.ts, packages/agenda/src/connector-camara.test.ts, packages/agenda/src/connector-senado.ts, packages/agenda/src/connector-senado.test.ts, packages/agenda/src/writer.ts, packages/agenda/src/writer.test.ts, packages/agenda/src/writer-supabase.ts, packages/agenda/src/index.ts</files>
  <read_first>packages/tramitacion/src/connector-camara.ts, packages/tramitacion/src/connector-senado.ts, packages/tramitacion/src/writer.ts, packages/tramitacion/src/writer-supabase.ts, packages/ingest/src/allowlist.ts</read_first>
  <action>Crear `headers-camara.ts` exportando `BROWSER_HEADERS_CAMARA` (el header-set verbatim del RESEARCH/interfaces). Crear `CitacionesCamaraConnector` que reusa `@obs/ingest` en el ORDEN LOCKED: por cada semana, construye `citaciones_semana.aspx?prmSemana={prmSemanaParam}`, `assertAllowedUrl → robots.isAllowed → rateLimiter.wait(host) → fetcher.get({url, headers: BROWSER_HEADERS_CAMARA})`, devuelve el HTML crudo (el parser vive en 06-02; el conector solo fetchea). Método `fetchSemana(year, week)` y `fetchPdfTabla()` (devuelve la URL del PDF + content_type, para la degradación; NO parsea el PDF). NO `BaseConnector.run`. Crear `SenadoActividadConnector` con `fetchCitaciones()` (GET `web-back.senado.cl/api/commissions_citations?limit=100`) y `fetchTablaSala()` (GET `.../weekly_table?limit=100`), mismo orden LOCKED (sin header-set CF — `web-back` no está tras Cloudflare); incluir un fallback documentado a `_next/data` con autodetección de buildId que NO se ejecuta por defecto. Crear el writer: `AgendaWriter` (interfaz inyectable) + `InMemoryAgendaWriter` (Map por clave natural → 2× no duplica) + `SupabaseAgendaWriter` que espeja `SupabaseTramitacionWriter` (upsert onConflict por clave natural en las 5 tablas; service key nunca interpolada en errores). Tests con mocks: (a) Cámara llama `rateLimiter.wait` ANTES de `fetcher.get` y pasa `BROWSER_HEADERS_CAMARA`; (b) Senado idem (sin esos headers); (c) `assertAllowedUrl("https://web-back.senado.cl/api/commissions_citations")` NO lanza (cobertura de allowlist); (d) writer idempotente 2×.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test connector-camara connector-senado writer</automated>
  </verify>
  <done>Conectores reusan @obs/ingest en orden LOCKED (test del orden con mocks), Cámara envía el header-set CF, allowlist cubre web-back.senado.cl (test), writer idempotente 2×; tests verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Orquestación runIngest (tolerante + degradación) + CLI</name>
  <files>packages/agenda/src/ingest-run.ts, packages/agenda/src/ingest-run.test.ts, packages/agenda/src/ingest-cli.ts, packages/agenda/src/ingest-cli.test.ts, packages/agenda/src/index.ts, packages/agenda/package.json</files>
  <read_first>packages/tramitacion/src/ingest-run.ts, packages/tramitacion/src/ingest-cli.ts</read_first>
  <action>Crear `runIngest({ conectorCamara, conectorSenado, writer, semanas, soloSenado? })`: (1) Cámara — por cada semana de `semanas` (de `enumerarSemanas`), fetch+parse+upsert citaciones; si una semana da 403 (Cloudflare endurecido) → reintento con backoff; si el 403 PERSISTE, marcar la fuente Cámara como "temporalmente no disponible" para esa corrida y CONTINUAR (no abortar). (2) Senado citaciones — fetch (ventana forward-only) + parse + upsert; NO fabricar histórico. (3) Senado tabla de sala — fetch `weekly_table` + parse + upsert `sesion_sala`/`sesion_tabla_item`. (4) Cámara tabla de sala — degradación honesta: NO ingestar filas; registrar/exponer la URL del PDF oficial (`verDoc.aspx?prmTipo=TABLASEMANAL`) como marcador "no disponible como dato estructurado" (no se persiste una fila inventada). Devolver un reporte por fuente (filas escritas, errores por semana, degradaciones). Tolerar fuentes vacías/ausentes sin abortar la corrida completa. Crear `ingest-cli.ts` (tsx) espejando el de Fase 5: flags `--desde=YYYY-Www`, `--hasta=YYYY-Www`, `--solo-senado`, `--dry-run`, validados ANTES de tocar red/DB; sin `SUPABASE_SERVICE_ROLE_KEY` → dry-run con aviso. Script `ingest` (tsx) ya en package.json. Tests: (a) Cámara 403 persistente → la corrida termina con Senado poblado y Cámara marcada degradada (no throw); (b) la tabla de Cámara NUNCA produce filas en `sesion_sala`/`sesion_tabla_item`; (c) flags inválidos rechazados antes de red/DB.</action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/agenda test ingest-run ingest-cli</automated>
  </verify>
  <done>runIngest enumera semanas, degrada Cámara ante 403 sin abortar Senado, expone la tabla de Cámara como PDF sin fabricar filas; CLI valida flags antes de red/DB; tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conector→fuente gov | URL construida (semana ISO/buildId) cruza a hosts gubernamentales |
| writer→Postgres | service role escribe (bypassa RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | DoS | WAF de Cámara (ráfagas → 403/ban) | mitigate | `HostRateLimiter` 2-3s serial por host (reuso @obs/ingest) + header-set anti-CF + backoff; alcance acotado; el conector NO reimplementa la política |
| T-06-08 | Spoofing/Tampering | SSRF vía URL construida (semana/buildId) | mitigate | `assertAllowedUrl` deny-by-default antes de cada fetch; `web-back.senado.cl`/`www.camara.cl` cubiertos por sufijos `senado.cl`/`camara.cl` (test lo afirma) |
| T-06-06 | Information Disclosure | service key en mensajes de error | mitigate | `SupabaseAgendaWriter` espeja el patrón Fase 5: la key nunca se interpola en errores (solo `error.message` de PostgREST); test lo prueba |
| T-06-09 | Tampering | fabricar tabla de sala de Cámara | mitigate | runIngest NUNCA escribe filas de tabla de Cámara; degradación honesta (PDF link). Test afirma 0 filas Cámara en sesion_tabla_item |
| T-06-SC | Tampering | npm installs | accept | sin paquetes nuevos (cheerio ya añadido en 06-01; @obs/*/@supabase/zod reusados) |
</threat_model>

<verification>
- `pnpm --filter @obs/agenda test` verde (connector-camara/senado, writer, ingest-run, ingest-cli).
- Test del orden LOCKED (rateLimiter.wait antes de fetcher.get) + header-set CF en Cámara.
- Test de cobertura de allowlist para web-back.senado.cl.
- Test de degradación: Cámara 403 → Senado poblado, Cámara marcada; 0 filas de tabla de Cámara.
- `pnpm -w typecheck` exit 0.
</verification>

<success_criteria>
- 2 conectores reusan @obs/ingest en orden LOCKED; Cámara con header-set CF.
- Writer idempotente (in-memory + Supabase) por clave natural.
- runIngest tolerante: enumera Cámara, ingesta Senado forward-only + tabla del Senado, degrada Cámara (403 y tabla→PDF) sin abortar.
- CLI con flags validados antes de red/DB.
</success_criteria>

<output>
Create `.planning/phases/06-citaciones-tabla-semanal-de-sala/06-03-SUMMARY.md` when done
</output>
