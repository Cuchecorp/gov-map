---
phase: 02-capa-de-providers-llm-embeddings
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/llm/src/providers/gemini-embeddings.ts
  - packages/llm/src/providers/gemini-embeddings.test.ts
autonomous: true
requirements: [FND-07]

must_haves:
  truths:
    - "Dado un texto publico, embed() devuelve un EmbeddingResult versionado: { vector, model, dims, version } — ningun vector anonimo"
    - "Los vectores son de 768 dims (truncado MRL via outputDimensionality) y estan L2-normalizados (norma ~= 1.0), porque Gemini NO normaliza a dims != 3072"
    - "embedding_model, embedding_dims y embedding_version son constantes explicitas; cambiar el modelo obliga a una version distinta (re-embed), nunca se mezclan versiones"
    - "El provider corre sin red en tests (mock-fetch / fetch inyectable) y sin API key"
  artifacts:
    - path: "packages/llm/src/providers/gemini-embeddings.ts"
      provides: "GeminiEmbeddingProvider (gemini-embedding-001, 768-dim, L2-normalizado, versionado) implementando EmbeddingProvider"
      contains: "outputDimensionality"
  key_links:
    - from: "packages/llm/src/providers/gemini-embeddings.ts"
      to: "packages/llm/src/types.ts"
      via: "implements EmbeddingProvider; devuelve EmbeddingResult (contrato de 02-01)"
      pattern: "EmbeddingResult"
    - from: "packages/llm/src/providers/gemini-embeddings.ts"
      to: "vector normalizado"
      via: "l2normalize manual a 768 dims"
      pattern: "l2normalize|Math.sqrt"
---

<objective>
Tercera rebanada vertical: dado un texto publico, devolver un embedding de 768-dim versionado a traves de `EmbeddingProvider` (Gemini `gemini-embedding-001`). Cada vector lleva su `{model, dims, version}` — no existe vector anonimo — y esta L2-normalizado manualmente (Gemini no normaliza a dims != 3072), condicion necesaria para que la distancia cosine de pgvector funcione en Fase 7.

Esta rebanada RELLENA el stub `providers/gemini-embeddings.ts` creado en 02-01 (el barrel `index.ts` ya lo re-exporta), asi que NO toca `index.ts` (cero colision con la rebanada 02, que corre en paralelo).

Purpose: Materializa FND-07 (interfaz EmbeddingProvider que fija y versiona modelo/dimensiones por vector). La forma del resultado versionado se fija aqui; la columna/indice pgvector se crea en Fase 7. Habilita re-embedding incremental sin corromper el indice (bump de version => re-embed; nunca mezclar).
Output: GeminiEmbeddingProvider testeado via fetch inyectable, sin red ni key.
</objective>

<phase_goal>
**As a** desarrollador consumidor (Fase 7 fichas + busqueda semantica), **I want to** pasar textos publicos y recibir embeddings de 768-dim que siempre cargan su modelo/dims/version, **so that** puedo indexarlos en pgvector con cosine y re-embeddizar de forma incremental cuando cambie el modelo sin corromper el indice ni mezclar versiones.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-CONTEXT.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-RESEARCH.md

<interfaces>
<!-- Contratos creados en 02-01 (este plan los implementa; NO redefinir). El stub providers/gemini-embeddings.ts ya existe y esta re-exportado por index.ts: solo rellenarlo. -->
De packages/llm/src/types.ts:
```typescript
interface EmbeddingResult { vector: number[]; model: string; dims: number; version: string }
interface EmbeddingProvider { readonly id: string; readonly trainsOnInputs: boolean; embed(texts: string[]): Promise<EmbeddingResult[]> }
```
Stub a rellenar (creado en 02-01, ya exportado por index.ts): `providers/gemini-embeddings.ts` con `EMBEDDING_MODEL`, `EMBEDDING_DIMS`, `EMBEDDING_VERSION`, `l2normalize`, `GeminiEmbeddingProvider` (todos como stub `not implemented`). Mantener los mismos nombres exportados.
De packages/llm/test/_helpers.ts: `makeMockFetch(routes)` -> `{ fn, calls }` (respuestas como string JSON).

Gemini embeddings (RESEARCH Pattern 6, CITADO ai.google.dev/gemini-api/docs/embeddings):
- SDK: `@google/genai` (2.8.0). `new GoogleGenAI({ apiKey, ... })`; el SDK es fetch-based y acepta un fetch custom para tests sin red (inyectar via la opcion del constructor / httpOptions; confirmar la firma exacta del SDK 2.8.0 al implementar — Assumption A4).
- `ai.models.embedContent({ model: "gemini-embedding-001", contents: texts, config: { outputDimensionality: 768 } })` -> `resp.embeddings[].values` (number[]).
- CRITICO: a dims != 3072 Gemini NO normaliza -> L2-normalizar manualmente cada vector.
- Free tier ENTRENA con inputs -> `trainsOnInputs = true`; solo texto PUBLICO (la politica data-routing de 02-02 lo gobierna).

Constantes: `EMBEDDING_MODEL="gemini-embedding-001"`, `EMBEDDING_DIMS=768`, `EMBEDDING_VERSION="v1"` (bump => re-embed; nunca mezclar versiones en un indice).

Patron error sin secretos (fetcher.ts): nunca incluir la key en mensajes/logs.
Solo Web APIs estandar (Math.sqrt, map); sin `node:`/`Buffer` (corre en Deno).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GeminiEmbeddingProvider — 768-dim, L2-normalizado, versionado</name>
  <files>packages/llm/src/providers/gemini-embeddings.ts, packages/llm/src/providers/gemini-embeddings.test.ts</files>
  <read_first>packages/llm/src/types.ts, packages/llm/test/_helpers.ts, packages/llm/src/providers/deepseek.ts, packages/llm/src/providers/gemini-embeddings.ts</read_first>
  <behavior>
    GeminiEmbeddingProvider (fetch inyectable / mock-fetch, sin red ni key):
    - Test: `embed(["texto publico"])` devuelve un array de EmbeddingResult; cada item tiene `vector` (length 768), `model==="gemini-embedding-001"`, `dims===768`, `version==="v1"`. (FND-07: ningun vector anonimo.)
    - Test: el vector devuelto esta L2-normalizado -> `Math.sqrt(sum(x^2)) ~= 1.0` (tolerancia 1e-6). El mock devuelve un vector SIN normalizar (p.ej. valores que suman norma != 1) para probar que la normalizacion la hace el provider, no el mock.
    - Test: batch de N textos -> N resultados, cada uno con su vector versionado.
    - Test: la request al endpoint Gemini lleva `outputDimensionality: 768` (inspeccionar el body capturado o el config pasado al SDK).
    - Test: `id==="gemini"`, `trainsOnInputs===true` (free tier entrena).
    - Test (helper puro): `l2normalize([3,4])` -> `[0.6, 0.8]`; `l2normalize([0,0])` -> `[0,0]` (guard de norma 0).
  </behavior>
  <action>
    REEMPLAZAR el stub de `providers/gemini-embeddings.ts` de 02-01 con la implementacion real (mismos nombres exportados; `index.ts` NO se toca):
    - Constantes exportadas: `EMBEDDING_MODEL = "gemini-embedding-001"`, `EMBEDDING_DIMS = 768`, `EMBEDDING_VERSION = "v1"`.
    - `function l2normalize(v: number[]): number[]` segun Pattern 6: `const norm = Math.sqrt(v.reduce((s,x)=>s+x*x,0)); return norm === 0 ? v : v.map(x=>x/norm);`. Exportarla (testeable aparte).
    - `class GeminiEmbeddingProvider implements EmbeddingProvider` con `id="gemini"`, `trainsOnInputs=true`. Constructor `({ apiKey, fetchFn }: { apiKey: string; fetchFn?: typeof fetch })` -> `new GoogleGenAI({ apiKey })` inyectando el `fetchFn` por la via que el SDK 2.8.0 exponga (httpOptions/fetch). CONFIRMAR la firma exacta del SDK al implementar (Assumption A4): leer la API de `@google/genai` 2.8.0 para `embedContent` (metodo, forma de `contents` batch, ruta de `resp.embeddings[].values`) y para inyeccion de fetch; ajustar si difiere de lo asumido — sin cambiar el contrato `embed()`.
    - `async embed(texts: string[]): Promise<EmbeddingResult[]>`: llamar `embedContent({ model: EMBEDDING_MODEL, contents: texts, config: { outputDimensionality: EMBEDDING_DIMS } })`; mapear cada `embeddings[i].values` a `{ vector: l2normalize(values), model: EMBEDDING_MODEL, dims: EMBEDDING_DIMS, version: EMBEDDING_VERSION }`. NINGUN vector sin esos 4 campos. Nunca loguear la key.
    - `gemini-embeddings.test.ts`: cubrir `<behavior>` con fetch inyectable / `makeMockFetch` apuntando al endpoint de embeddings de Gemini (la ruta exacta sale de la inspeccion del SDK; el mock devuelve `{ embeddings: [{ values: [...] }] }` en el shape que el SDK espera). Probar normalizacion con un vector deliberadamente no-normalizado.
    Solo Web APIs estandar; sin `node:`/`Buffer` (consumo Deno).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm test --run gemini && pnpm -w typecheck && npx --yes deno check packages/llm/src/providers/gemini-embeddings.ts</automated>
  </verify>
  <done>embed() devuelve EmbeddingResult versionado (model/dims/version) por texto; vectores de 768 dims L2-normalizados (norma ~=1.0); request lleva outputDimensionality:768; l2normalize guard de norma 0 verde; id/trainsOnInputs correctos; tests sin red ni key; typecheck y `deno check` verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> EmbeddingProvider | Solo debe pasar texto PUBLICO (Gemini free entrena con inputs); la politica data-routing de 02-02 lo gobierna |
| @obs/llm -> Gemini API (red) | Cruce a un tercero; key de servicio; respuesta (vector) que debe normalizarse para ser usable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Gemini free tier (entrena con inputs) | mitigate | `trainsOnInputs=true` declarado en el provider -> el gate fail-closed (router + data-routing de 02-02) impide que PII llegue aqui; documentado: Gemini solo texto publico |
| T-02-10 | Tampering | vector sin normalizar / anonimo | mitigate | L2-normalizacion manual obligatoria a 768 (test de norma ~=1.0); todo vector carga model/dims/version (test) -> sin vector anonimo (FND-07) |
| T-02-11 | Information Disclosure | API key Gemini en logs | mitigate | Key solo de env; nunca en mensajes de error ni asserts (patron fetcher.ts); tests sin key |
| T-02-SC | Tampering | npm install @google/genai | accept | Auditado en RESEARCH Package Legitimacy Audit: SDK oficial de Google (github.com/googleapis/js-genai), repo publico verificado; no `[ASSUMED]`/`[SUS]`/`[SLOP]`. Instalado en 02-01 |
</threat_model>

<verification>
- `pnpm --filter @obs/llm test --run gemini` verde: versionado (model/dims/version), 768 dims + L2 (norma ~=1.0), batch, outputDimensionality:768 en el request, flags id/trainsOnInputs, l2normalize guard.
- `pnpm -w typecheck` verde.
- `deno check packages/llm/src/providers/gemini-embeddings.ts` sin error (consumo Edge).
- Tests corren sin red ni API key.
</verification>

<success_criteria>
- GeminiEmbeddingProvider implementa EmbeddingProvider; cada embedding versionado {vector,model,dims,version} (FND-07: ningun vector anonimo).
- Vectores de 768 dims L2-normalizados manualmente (cosine-ready para Fase 7).
- Constante de version explicita; cambiar modelo => version distinta (re-embed), nunca mezclar.
- Sin red ni key en tests.
</success_criteria>

<output>
Create `.planning/phases/02-capa-de-providers-llm-embeddings/02-03-SUMMARY.md` when done
</output>
