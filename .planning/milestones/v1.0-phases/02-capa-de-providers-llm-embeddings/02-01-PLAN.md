---
phase: 02-capa-de-providers-llm-embeddings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/llm/package.json
  - packages/llm/tsconfig.json
  - packages/llm/vitest.config.ts
  - packages/llm/test/_helpers.ts
  - packages/llm/src/index.ts
  - packages/llm/src/types.ts
  - packages/llm/src/config.ts
  - packages/llm/src/validate.ts
  - packages/llm/src/validate.test.ts
  - packages/llm/src/router.ts
  - packages/llm/src/router.test.ts
  - packages/llm/src/providers/deepseek.ts
  - packages/llm/src/providers/deepseek.test.ts
  - packages/llm/src/e2e.test.ts
  - packages/llm/src/json-schema.ts
  - packages/llm/src/data-routing.ts
  - packages/llm/src/providers/minimax.ts
  - packages/llm/src/providers/gemini-embeddings.ts
  - tsconfig.json
  - deno.json
autonomous: true
requirements: [FND-06, FND-07]

must_haves:
  truths:
    - "Un caller invoca la capa con (prompt, schema zod, criticality, sensitivity) y recibe un objeto validado del provider correcto, sin red, usando mock-fetch"
    - "El router selecciona el provider por criticality (bulk -> deepseek) y aborta (fail-closed) si una tarea sensitivity=personal va a un provider trainsOnInputs=true"
    - "La validacion zod es la compuerta unica: salida invalida dispara un repair loop (reprompt con los issues) y, agotados los reintentos, lanza LLMValidationError sin filtrar prompt ni keys"
    - "El paquete @obs/llm es consumible desde Node (vitest) y declarado en el import-map de deno.json para Edge Functions"
  artifacts:
    - path: "packages/llm/src/types.ts"
      provides: "Contratos LLMProvider, EmbeddingProvider, EmbeddingResult, CompletionRequest, Criticality, Sensitivity"
      contains: "interface LLMProvider"
    - path: "packages/llm/src/validate.ts"
      provides: "parseAndValidate(schema, raw, ctx) + repair loop + LLMValidationError"
      contains: "safeParse"
    - path: "packages/llm/src/providers/deepseek.ts"
      provides: "DeepSeekProvider (response_format json_object) implementando LLMProvider"
      contains: "json_object"
    - path: "packages/llm/src/router.ts"
      provides: "selectProvider fail-closed + SensitiveRoutingError"
      contains: "SensitiveRoutingError"
    - path: "packages/llm/src/index.ts"
      provides: "Barrel completo de @obs/llm (re-exporta todos los modulos de la fase, incl. stubs de 02/03)"
      contains: "export"
    - path: "packages/llm/test/_helpers.ts"
      provides: "makeMockFetch para respuestas chat-completions sin red"
      contains: "makeMockFetch"
  key_links:
    - from: "packages/llm/src/providers/deepseek.ts"
      to: "packages/llm/src/validate.ts"
      via: "parseAndValidate como compuerta unica externa al adapter"
      pattern: "parseAndValidate"
    - from: "packages/llm/src/router.ts"
      to: "packages/llm/src/types.ts"
      via: "LLMProvider.trainsOnInputs como gate del fail-closed"
      pattern: "trainsOnInputs"
    - from: "deno.json"
      to: "packages/llm/src/"
      via: "import-map @obs/llm/"
      pattern: "@obs/llm/"
---

<objective>
Entregar la rebanada vertical LLM end-to-end de la capa de providers: un caller pasa (prompt, schema zod, criticality, sensitivity) y recibe un objeto validado del provider correcto — o un error fail-closed si la politica lo prohibe — sin tocar la red (mock-fetch). Esta es la primera rebanada usable: scaffold del paquete `@obs/llm`, contratos (`types.ts`), compuerta zod unica con repair loop, el primer adapter real (DeepSeek `json_object`), el router fail-closed, y un test e2e que ejercita toda la pila.

Purpose: Materializa FND-06 (interfaz enchufable + salida estructurada validada + seleccion por config) con el provider de volumen, y deja los contratos que las rebanadas 02 (MiniMax) y 03 (Gemini embeddings) implementan sin renegociar arquitectura. Declara `EmbeddingResult` aqui (contrato) para que la rebanada 03 lo implemente (FND-07). Crea los STUBS de los modulos de 02/03 y el barrel `index.ts` completo, para que 02 y 03 (Wave 2, paralelas) rellenen stubs sin tocar `index.ts` (cero colision de archivo compartido).
Output: Paquete `@obs/llm` instalable y testeado; happy path LLM verde end-to-end con DeepSeek; router fail-closed verde; import-map Deno listo; stubs + barrel para las rebanadas 02/03.
</objective>

<phase_goal>
**As a** desarrollador consumidor (Fase 4 adjudicacion / Fase 7 extraccion), **I want to** invocar una sola interfaz `LLMProvider` con un prompt y un schema zod y recibir un objeto ya validado del provider correcto segun (criticidad, sensibilidad), **so that** el dominio nunca conoce ni acopla a un modelo concreto y cambiar de modelo no toca codigo aguas arriba.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-CONTEXT.md
@.planning/phases/02-capa-de-providers-llm-embeddings/02-RESEARCH.md
@.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-01-SUMMARY.md

<interfaces>
<!-- Patrones extraidos del repo (Fase 1). El executor los usa directo: NO explorar el codebase. -->

zod instalado en el repo: **4.4.3** (lockfile verificado). Usar la API v4:
- `schema.safeParse(value)` -> `{ success: true, data } | { success: false, error }`
- `error.issues` -> `{ path: (string|number)[], message: string }[]`
- `z.toJSONSchema(schema)` para derivar el JSON schema (zod v4 nativo) — usado en la rebanada 02 (MiniMax tool-calling). NO usar `zod-to-json-schema` (es para zod v3).

Helper de mock-fetch a ESPEJAR desde packages/ingest/test/_helpers.ts (mismo contrato, body como string JSON en vez de bytes crudos):
```typescript
export interface MockResponseSpec { status: number; body?: string; headers?: Record<string,string>; }
export interface CapturedRequest { url: string; method: string; headers: Record<string,string>; body?: BodyInit | null; }
export interface MockFetch { fn: typeof fetch; calls: CapturedRequest[]; }
export function makeMockFetch(routes: Record<string, MockResponseSpec>): MockFetch;
// URLs no mapeadas -> 404. Captura cada request en `calls` para asserts de orden/repair.
```

Patron de paquete @obs/* (de packages/ingest):
- package.json: `"name":"@obs/llm","type":"module","main":"src/index.ts","exports":{".":"./src/index.ts"}`, script `"test":"vitest run"`, `"typecheck":"tsc -b"`.
- tsconfig.json: `extends "../../tsconfig.base.json"`, `compilerOptions:{rootDir:"src",outDir:"dist",composite:true,noEmit:false,lib:["ES2022","DOM"]}`, `references:[{"path":"../core"}]` (solo si se importa @obs/core; aqui NO es necesario), `include:["src/**/*.ts"]`, `exclude:["src/**/*.test.ts","test","dist"]`.
- vitest.config.ts: `{ test: { environment:"node", include:["src/**/*.test.ts"], passWithNoTests:true } }`.

Patron de error sin secretos (de fetcher.ts): los mensajes de error NUNCA incluyen headers de auth, API keys ni el prompt completo.

openai SDK v6 (fetch-based): `new OpenAI({ apiKey, baseURL, fetch: fetchFn })` acepta un `fetch` custom -> mock sin red. `client.chat.completions.create({ model, messages, response_format })`. DeepSeek baseURL `https://api.deepseek.com`, modelo `deepseek-v4-flash` (NO `deepseek-chat`/`deepseek-reasoner`, se retiran 2026-07-24).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @obs/llm + infraestructura de test (Wave 0) + contratos + stubs/barrel</name>
  <files>packages/llm/package.json, packages/llm/tsconfig.json, packages/llm/vitest.config.ts, packages/llm/test/_helpers.ts, packages/llm/src/index.ts, packages/llm/src/types.ts, packages/llm/src/config.ts, packages/llm/src/json-schema.ts, packages/llm/src/data-routing.ts, packages/llm/src/providers/minimax.ts, packages/llm/src/providers/gemini-embeddings.ts, tsconfig.json, deno.json</files>
  <read_first>packages/ingest/package.json, packages/ingest/tsconfig.json, packages/ingest/test/_helpers.ts, packages/core/vitest.config.ts, tsconfig.json, deno.json, tsconfig.base.json</read_first>
  <action>
    Crear el paquete `@obs/llm` espejando el patron de `@obs/ingest`:
    - `packages/llm/package.json`: name `@obs/llm`, type module, main/types/exports a `src/index.ts`, scripts test (`vitest run`) y typecheck (`tsc -b`). dependencies: `openai` (^6.44.0), `@google/genai` (^2.8.0), `zod` (^4.4.3). devDependencies: `vitest` (^3.0.0). NO depender de `@obs/core` (esta capa no usa sus tipos).
    - `packages/llm/tsconfig.json`: extends `../../tsconfig.base.json`, compilerOptions rootDir src / outDir dist / composite true / noEmit false / lib `["ES2022","DOM"]`. SIN `references` (no importa @obs/core). include `src/**/*.ts`, exclude `src/**/*.test.ts`, `test`, `dist`.
    - `packages/llm/vitest.config.ts`: igual a packages/core/vitest.config.ts (environment node, include `src/**/*.test.ts`, passWithNoTests true).
    - `packages/llm/test/_helpers.ts`: copiar el `makeMockFetch` de `packages/ingest/test/_helpers.ts` adaptando `MockResponseSpec.body` a `string` (JSON), manteniendo `fn`, `calls`, normalizeHeaders, y la captura de cada request. Devolver `new Response(body ?? "", { status, headers })`.
    - `packages/llm/src/types.ts`: definir y exportar los CONTRATOS (sin implementacion): `type Criticality = "critical" | "bulk"`; `type Sensitivity = "public" | "personal"`; `interface CompletionRequest { system?: string; user: string; criticality: Criticality; sensitivity: Sensitivity; maxRepairAttempts?: number }`; `interface LLMProvider { readonly id: string; readonly trainsOnInputs: boolean; complete<T>(req: CompletionRequest, schema: import("zod").ZodType<T>): Promise<T> }`; `interface EmbeddingResult { vector: number[]; model: string; dims: number; version: string }`; `interface EmbeddingProvider { readonly id: string; readonly trainsOnInputs: boolean; embed(texts: string[]): Promise<EmbeddingResult[]> }`. EmbeddingProvider/EmbeddingResult quedan aqui como contrato que implementa la rebanada 03 (FND-07).
    - `packages/llm/src/config.ts`: `interface ProviderConfig { model: string; baseURL: string; trainsOnInputs: boolean }`; `interface RouterConfig { byCriticality: Record<Criticality, string>; providers: Record<string, ProviderConfig> }`; una funcion `loadRouterConfigFromEnv(env: Record<string,string|undefined>): RouterConfig` que mapea `critical->"minimax"`, `bulk->"deepseek"` y arma `providers` con baseURL/model/trainsOnInputs (deepseek: baseURL `https://api.deepseek.com`, model `deepseek-v4-flash`, trainsOnInputs false; minimax: baseURL `https://api.minimax.io/v1`, model `MiniMax-M3`, trainsOnInputs false). El config es la pieza swappable (FND-06 criterio 2): cambiar de modelo = cambiar config, cero codigo aguas arriba. Leer keys de env, NUNCA hardcodear ni loguear.
    - CREAR STUBS (para que las rebanadas 02/03 los rellenen sin tocar `index.ts`; los stubs deben TYPECHECKEAR y EXPORTAR los nombres publicos esperados, lanzando `not implemented` en runtime): `packages/llm/src/json-schema.ts` (`export function zodToToolSchema(schema: import("zod").ZodType): Record<string,unknown> { throw new Error("not implemented (02-02)"); }`); `packages/llm/src/data-routing.ts` (`export class RutInLlmInputError extends Error {}`; `export function assertNoRutInLlmInput(_text: string): void { throw new Error("not implemented (02-02)"); }`; `export function assertSensitivityAllowed(_task: { sensitivity: import("./types").Sensitivity }, _provider: { id: string; trainsOnInputs: boolean }): void { throw new Error("not implemented (02-02)"); }`); `packages/llm/src/providers/minimax.ts` (`export class MiniMaxProvider { /* stub: implementa LLMProvider en 02-02 */ }` con la firma minima para typecheck — declarar campos `id`/`trainsOnInputs` y `complete` que lanza `not implemented`); `packages/llm/src/providers/gemini-embeddings.ts` (`export const EMBEDDING_MODEL = "gemini-embedding-001"`; `export const EMBEDDING_DIMS = 768`; `export const EMBEDDING_VERSION = "v1"`; `export function l2normalize(v: number[]): number[] { throw new Error("not implemented (02-03)"); }`; `export class GeminiEmbeddingProvider { /* stub: implementa EmbeddingProvider en 02-03 */ }` con firma minima). Marcar cada stub con un comentario `// STUB — rellenado por 02-0X; no agregar tests aqui`.
    - `packages/llm/src/index.ts`: BARREL COMPLETO de la fase — re-exportar TODOS los modulos: `export * from "./types"; export * from "./config"; export * from "./validate"; export * from "./router"; export * from "./json-schema"; export * from "./data-routing"; export * from "./providers/deepseek"; export * from "./providers/minimax"; export * from "./providers/gemini-embeddings";`. (validate/router/deepseek se crean en Tasks 2-3 de este plan; minimax/json-schema/data-routing/gemini son stubs que 02/03 rellenan.) `index.ts` queda 100% propiedad de este plan; 02 y 03 NO lo tocan.
    - Editar `tsconfig.json` raiz: agregar `{ "path": "./packages/llm" }` a `references`.
    - Editar `deno.json` raiz: agregar al import-map `"@obs/llm/": "./packages/llm/src/"`.
    Instalar deps: `pnpm --filter @obs/llm add openai@6 @google/genai@2 zod@4` y `pnpm --filter @obs/llm add -D vitest` (o agregar a package.json y `pnpm install`).
    Solo Web APIs estandar (fetch, TextEncoder); imports sin extension nativa; sin `node:`/`Buffer` (debe correr en Deno).
    NOTA: en este punto `index.ts` re-exporta validate/router/deepseek que aun no existen — crearlos como archivos vacios minimos NO; el orden correcto es: crear stubs + types + config en esta tarea, y validate/router (Task 2) + deepseek (Task 3) ANTES de que `tsc -b` cubra el barrel completo. Para que la verificacion de Task 1 pase, crear tambien archivos minimos placeholder de `validate.ts`/`router.ts`/`providers/deepseek.ts` con los exports esperados lanzando `not implemented` (Tasks 2-3 los reemplazan con la implementacion real). Asi `index.ts` typechecker desde Task 1.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm install && pnpm -w typecheck && pnpm --filter @obs/llm test --run</automated>
  </verify>
  <done>`pnpm install` resuelve; `pnpm -w typecheck` verde (tsc -b incluye packages/llm con el barrel completo + stubs); `pnpm --filter @obs/llm test --run` arranca verde (passWithNoTests); `deno.json` tiene `@obs/llm/`; `types.ts` exporta LLMProvider, EmbeddingProvider, EmbeddingResult, CompletionRequest, Criticality, Sensitivity; stubs de json-schema/data-routing/minimax/gemini-embeddings existen y typecheckean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compuerta zod unica (parseAndValidate + repair loop) y router fail-closed</name>
  <files>packages/llm/src/validate.ts, packages/llm/src/validate.test.ts, packages/llm/src/router.ts, packages/llm/src/router.test.ts</files>
  <read_first>packages/llm/src/types.ts, packages/llm/src/config.ts, packages/ingest/src/fetcher.ts</read_first>
  <behavior>
    parseAndValidate:
    - Test: JSON valido que pasa el schema -> devuelve `result.data` tipado (sin reprompt).
    - Test: 1a respuesta invalida (campo faltante) -> llama `ctx.reprompt(issuesMsg)` una vez; 2a respuesta valida -> devuelve data. Assert: reprompt llamado 1 vez y el mensaje contiene `path: message` de los issues (p.ej. "confidence: ...").
    - Test: respuesta sigue invalida tras `maxAttempts` -> lanza `LLMValidationError`; el `message`/propiedades NO contienen el prompt ni ninguna API key (solo los issues zod).
    - Test: raw `undefined` o JSON no parseable -> tratado como invalido (no throw de JSON.parse fuera de control), entra al repair loop.
    router selectProvider:
    - Test: `criticality:"bulk"` -> provider id de `config.byCriticality.bulk` (deepseek) desde el registry.
    - Test: `criticality` sin provider en registry -> `RouterError`.
    - Test (FAIL-CLOSED): `sensitivity:"personal"` y el provider candidato `trainsOnInputs===true` -> lanza `SensitiveRoutingError`, nunca devuelve un provider ni degrada.
    - Test: `sensitivity:"personal"` y provider `trainsOnInputs===false` -> devuelve el provider OK.
  </behavior>
  <action>
    REEMPLAZAR los placeholders de Task 1 con la implementacion real (mismos nombres exportados; `index.ts` NO se toca):
    `packages/llm/src/validate.ts`: implementar `parseAndValidate<T>(schema: z.ZodType<T>, raw: string | undefined, ctx: { reprompt: (errors: string) => Promise<string | undefined>; maxAttempts: number }): Promise<T>` exactamente como el Pattern 4 del RESEARCH: loop `for attempt 0..maxAttempts`, `JSON.parse` envuelto en try/catch (-> undefined si falla), `schema.safeParse(parsed)`, si success devolver data; si es el ultimo intento `throw new LLMValidationError(result.error.issues)`; si no, construir `errorMsg = issues.map(i => `${i.path.join(".")}: ${i.message}`).join("; ")` y `current = await ctx.reprompt(errorMsg)`. Definir y exportar `class LLMValidationError extends Error` que recibe `issues` y construye un message GENERICO ("LLM output failed schema validation") + guarda `issues` como propiedad; NUNCA incluir prompt ni keys (espejar la regla de fetcher.ts). Esta es la compuerta UNICA: ningun adapter valida por su cuenta.
    `packages/llm/src/router.ts`: implementar `selectProvider(task: { criticality: Criticality; sensitivity: Sensitivity }, registry: Record<string, LLMProvider>, config: RouterConfig): LLMProvider` segun Pattern 5: `providerId = config.byCriticality[task.criticality]`; `provider = registry[providerId]`; si no existe -> `throw new RouterError(...)`; GATE fail-closed: `if (task.sensitivity === "personal" && provider.trainsOnInputs) throw new SensitiveRoutingError(...)`; devolver provider. Definir y exportar `class RouterError extends Error` y `class SensitiveRoutingError extends Error`. NingUn branch catch/`||` que sustituya un provider sensible por otro.
    Escribir `validate.test.ts` y `router.test.ts` cubriendo el bloque `<behavior>` (RED primero, luego GREEN).
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm test --run validate router && pnpm -w typecheck</automated>
  </verify>
  <done>Tests de validate (happy/repair/throw-sin-secretos/json-invalido) y router (seleccion por criticality/RouterError/fail-closed SensitiveRoutingError/personal-permitido) verdes; LLMValidationError no expone prompt ni keys; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapter DeepSeek (json_object) + test e2e de la rebanada LLM</name>
  <files>packages/llm/src/providers/deepseek.ts, packages/llm/src/providers/deepseek.test.ts, packages/llm/src/e2e.test.ts</files>
  <read_first>packages/llm/src/types.ts, packages/llm/src/validate.ts, packages/llm/src/router.ts, packages/llm/src/config.ts, packages/llm/test/_helpers.ts</read_first>
  <behavior>
    DeepSeekProvider (mock-fetch, sin red):
    - Test: respuesta con `choices[0].message.content` = JSON valido para el schema -> `complete()` devuelve el objeto validado; assert que el request POST a `https://api.deepseek.com/chat/completions` lleva `response_format: {type:"json_object"}` y `model:"deepseek-v4-flash"`, y que el `system` (prefijo estable) va PRIMERO en messages (prompt-cache).
    - Test (repair via provider): 1a respuesta content invalido -> 2a respuesta valida -> devuelve data; assert fetch llamado 2 veces (el reprompt re-llama al provider).
    - Test: `trainsOnInputs === false` e `id === "deepseek"`.
    e2e (rebanada completa, mock-fetch):
    - Test: dado registry={deepseek}, config bulk->deepseek, un caller hace `selectProvider({criticality:"bulk",sensitivity:"public"}).complete(req, schema)` y recibe el objeto validado — ejercita router -> provider -> zod gate end-to-end sin red.
    - Test (fail-closed e2e): `sensitivity:"personal"` con un registry donde el provider de esa criticality tiene `trainsOnInputs:true` -> el flujo lanza `SensitiveRoutingError` antes de cualquier fetch (assert: 0 calls).
  </behavior>
  <action>
    REEMPLAZAR el placeholder de `providers/deepseek.ts` de Task 1 con la implementacion real (mismo export; `index.ts` NO se toca):
    `packages/llm/src/providers/deepseek.ts`: `class DeepSeekProvider implements LLMProvider` con `id="deepseek"`, `trainsOnInputs=false`. Constructor `({ apiKey, baseURL?, model?, fetchFn }: { apiKey: string; baseURL?: string; model?: string; fetchFn?: typeof fetch })` -> `new OpenAI({ apiKey, baseURL: baseURL ?? "https://api.deepseek.com", fetch: fetchFn })`. `complete<T>(req, schema)`: construir messages `[{role:"system",content:req.system ?? <instruccion JSON estable>},{role:"user",content:req.user}]` (system PRIMERO = prefijo estable para prompt-cache; debe incluir la palabra "json" por requisito de DeepSeek json_mode), llamar `chat.completions.create({ model: this.model, messages, response_format:{type:"json_object"} })`, leer `res.choices[0].message.content` (string JSON). Delegar a `parseAndValidate(schema, content, { maxAttempts: req.maxRepairAttempts ?? 1, reprompt: async (errors) => { /* re-llamar create() agregando un message user con los errores zod y devolver el nuevo content */ } })`. La validacion es EXTERNA (compuerta unica) — el adapter NO hace su propio safeParse. NUNCA loguear el prompt ni la key.
    `deepseek.test.ts`: cubrir el bloque `<behavior>` con `makeMockFetch` (rutas a `https://api.deepseek.com/chat/completions`). Inspeccionar `mock.calls[0].body` (string) para asserts de response_format/model/orden de messages.
    `e2e.test.ts`: el test de rebanada vertical descrito en `<behavior>`: construir registry + RouterConfig + un schema zod pequeno (p.ej. `z.object({ decision: z.enum(["match","no_match"]), confidence: z.number() })`), correr router->complete con mock-fetch, y el caso fail-closed que NO debe emitir ningun fetch.
  </action>
  <verify>
    <automated>cd "C:/Users/Carlo/OneDrive - pjud.cl/Documentos/GitHub/Observatorio" && pnpm --filter @obs/llm test --run && pnpm -w typecheck && npx --yes deno check packages/llm/src/providers/deepseek.ts packages/llm/src/validate.ts packages/llm/src/router.ts</automated>
  </verify>
  <done>DeepSeekProvider valida json_object via la compuerta zod externa; e2e de la rebanada (router->provider->zod) verde con mock-fetch; fail-closed e2e no emite fetch (0 calls); suite `@obs/llm` verde; `deno check` de los modulos core de la rebanada sin error (consumo Edge).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (dominio) -> @obs/llm | El dominio entrega un prompt + schema; la capa decide provider y valida la salida |
| @obs/llm -> API del proveedor (red) | Cruce a un tercero (DeepSeek); auth con API key; respuesta no confiable (adversaria) |
| (criticidad, sensibilidad) -> seleccion de tier | Frontera de cumplimiento: dato personal no puede ir a un tier que entrena |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | router selectProvider | mitigate | Fail-closed: `personal && trainsOnInputs` -> `SensitiveRoutingError`; test que verifica el throw y que no se emite fetch (e2e.test.ts) |
| T-02-02 | Tampering | salida del LLM (message.content) | mitigate | Compuerta zod unica `parseAndValidate`; salida invalida -> repair o `LLMValidationError`, nunca pasa cruda. Schema con enums/rangos rechaza valores adversarios |
| T-02-03 | Information Disclosure | LLMValidationError / logs | mitigate | El error lleva solo los `issues` zod; NUNCA prompt ni API key (espeja regla fetcher.ts). Test que asserta ausencia de secretos en el message |
| T-02-04 | Information Disclosure | config / API keys | mitigate | Keys solo de env (`loadRouterConfigFromEnv`); nunca hardcodeadas ni en mensajes de error; `.env` gitignored (Fase 1) |
| T-02-SC | Tampering | npm installs (openai, @google/genai, zod) | mitigate | RESEARCH Package Legitimacy Audit: 3 SDKs oficiales de primera mano (OpenAI/Google) + zod (ya en repo Fase 1), repos publicos verificados, sin `[ASSUMED]`/`[SUS]`/`[SLOP]` -> no requiere checkpoint humano bloqueante |
</threat_model>

<verification>
- `pnpm install && pnpm -w typecheck` verde (tsc -b incluye packages/llm con barrel completo + stubs).
- `pnpm --filter @obs/llm test --run` verde: validate (happy/repair/throw/json-invalido), router (seleccion/RouterError/fail-closed/personal-permitido), deepseek (json_object/repair/flags), e2e (rebanada router->provider->zod + fail-closed sin fetch).
- `deno check` de validate.ts/router.ts/deepseek.ts sin error (consumo Edge / Deno).
- `deno.json` declara `@obs/llm/` en el import-map.
</verification>

<success_criteria>
- @obs/llm scaffolded, instalable, typecheck verde, consumible Node + declarado para Deno.
- Happy path LLM end-to-end verde con DeepSeek via mock-fetch (FND-06 criterio 1 parcial: provider de volumen + zod gate).
- Router fail-closed verde: tarea personal a tier que entrena -> SensitiveRoutingError (FND-06 + politica de datos).
- Compuerta zod unica con repair loop; LLMValidationError sin secretos.
- Contratos LLMProvider/EmbeddingProvider/EmbeddingResult exportados + stubs/barrel listos (habilita rebanadas 02 y 03 sin colision de index.ts).
</success_criteria>

<output>
Create `.planning/phases/02-capa-de-providers-llm-embeddings/02-01-SUMMARY.md` when done
</output>
