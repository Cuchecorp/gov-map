---
phase: 04-adjudicacion-identidad-compuerta-humana-golden-set
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0006_revision_identidad.sql
  - supabase/tests/0005_revision_identidad.test.sql
autonomous: true
requirements: [ID-05, ID-06, ID-08]

must_haves:
  truths:
    - "Existe una tabla `vinculo_identidad` (mención foránea → parlamentario_id) con estado confirmado/probable/no_confirmado y default no_confirmado"
    - "Existe una cola `revision_identidad` (registro foráneo + candidatos + salida del modelo + estado pendiente/resuelto + revisor_id + timestamps)"
    - "Existe `identidad_audit` append-only: un UPDATE o DELETE sobre ella SIEMPRE falla, incluso ejecutado por el service role"
    - "Las tres tablas tienen RLS deny-by-default (anon nunca lee identidad/cola/auditoría)"
  artifacts:
    - path: "supabase/migrations/0006_revision_identidad.sql"
      provides: "DDL de vinculo_identidad + revision_identidad + identidad_audit append-only"
      contains: "identidad_audit"
    - path: "supabase/tests/0005_revision_identidad.test.sql"
      provides: "pgTAP: columnas, checks, RLS, inmutabilidad del audit"
      contains: "throws_ok"
  key_links:
    - from: "identidad_audit"
      to: "trigger identidad_audit_immutable"
      via: "before update or delete → RAISE EXCEPTION"
      pattern: "before update or delete"
    - from: "identidad_audit"
      to: "REVOKE update, delete"
      via: "defensa en profundidad contra service role"
      pattern: "revoke update"
---

<objective>
Materializa el estado durable del subsistema de identidad asistida en la migración 0006: la tabla final `vinculo_identidad` (estado por mención→id, ID-06), la cola de trabajo `revision_identidad` (ID-05) y el audit log inmutable `identidad_audit` (ID-08). La inmutabilidad del audit se enforcea a nivel DB con AMBOS mecanismos — trigger `RAISE EXCEPTION` (única defensa que aplica al service role, que bypassa RLS) Y `REVOKE update/delete` (defensa en profundidad) — y se verifica con pgTAP que un UPDATE/DELETE como service role realmente falla.

Purpose: El audit log es la garantía de procedencia no-repudiable de cada decisión de identidad. Confiar en que la aplicación "nunca actualiza" es insuficiente (Pitfall 4): el writer usa service role. La inmutabilidad debe estar en la DB.
Output: Migración 0006 aplicada limpia (0001→0006) + pgTAP verde incluyendo el throws_ok de inmutabilidad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-CONTEXT.md
@.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-RESEARCH.md

# Patrón EXACTO de migración + pgTAP a espejar (RLS deny-by-default, provenance, checks)
@supabase/migrations/0005_parlamentario.sql
@supabase/tests/0004_parlamentario.test.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 0006 — vinculo_identidad + revision_identidad + identidad_audit append-only</name>
  <files>supabase/migrations/0006_revision_identidad.sql</files>
  <action>
Crea `supabase/migrations/0006_revision_identidad.sql` espejando el estilo de 0005 (cabecera de principios, checks inline, RLS deny-by-default al final). Tres tablas:

1. `vinculo_identidad` — el producto final mención→id (ID-06):
   - `id bigint generated always as identity primary key`
   - `mencion_nombre text not null` (el nombre como apareció en la fuente foránea; display)
   - `mencion_normalizada text not null` (clave normalizada)
   - `camara text not null check (camara in ('diputados','senado'))`
   - `periodo text not null`
   - `parlamentario_id text references parlamentario(id)` (nullable: no_confirmado puede no tener id)
   - `estado text not null default 'no_confirmado' check (estado in ('confirmado','probable','no_confirmado'))` — default no_confirmado: nada se auto-confirma por DDL (espejo 0005)
   - `metodo text not null check (metodo in ('determinista','llm','humano'))`
   - provenance inline: `origen text not null`, `fecha_captura timestamptz not null default now()`, `enlace text not null`
   - índice único PARCIAL sobre `(camara, periodo, mencion_normalizada)` donde `parlamentario_id is not null` para idempotencia del vínculo confirmado/probable.

2. `revision_identidad` — la cola humana (ID-05):
   - `id bigint generated always as identity primary key`
   - `vinculo_id bigint references vinculo_identidad(id)` (nullable hasta resolver)
   - `mencion_nombre text not null`, `mencion_normalizada text not null`, `camara text not null`, `periodo text not null`, `region text`
   - `candidatos jsonb not null default '[]'::jsonb` (lista de candidatos del blocking; SIN rut)
   - `salida_modelo jsonb` (la Adjudicacion validada: decision/chosen_id/confidence/evidence/conflicts)
   - `modelo_version text` (p.ej. 'MiniMax-M3')
   - `estado text not null default 'pendiente' check (estado in ('pendiente','confirmado','rechazado','corregido'))`
   - `revisor_id text` (null hasta que un humano resuelve)
   - `motivo text` (null; razón de rechazo/corrección)
   - `created_at timestamptz not null default now()`, `resolved_at timestamptz`

3. `identidad_audit` — append-only (ID-08), siguiendo Pattern 4 del RESEARCH:
   - `id bigint generated always as identity primary key`
   - `vinculo_id bigint references vinculo_identidad(id)`
   - `metodo text not null check (metodo in ('determinista','llm','humano'))`
   - `decision text not null` (match|no_match|uncertain|confirmado|rechazado|corregido)
   - `confidence numeric` (null para determinista/humano)
   - `modelo_version text` (null si no-LLM)
   - `revisor_id text` (null salvo metodo='humano')
   - `evidence jsonb not null default '[]'::jsonb`, `conflicts jsonb not null default '[]'::jsonb`
   - `created_at timestamptz not null default now()`

Inmutabilidad de `identidad_audit` (AMBOS mecanismos):
   - Función `identidad_audit_immutable()` plpgsql que hace `raise exception 'identidad_audit es append-only: % no permitido', tg_op;`
   - Trigger `before update or delete on identidad_audit for each row execute function identidad_audit_immutable()` — aplica a TODOS los roles incluido service role.
   - `revoke update, delete, truncate on identidad_audit from public;` (defensa en profundidad).

RLS deny-by-default en las TRES tablas: `alter table <t> enable row level security;` SIN policies (espejo exacto de 0005 — anon nunca lee). Verifica `supabase db reset` aplica 0001→0006 limpio y `supabase db lint` sin errores.
  </action>
  <verify>
    <automated>supabase db reset && supabase db lint</automated>
  </verify>
  <done>0001→0006 aplica limpio; lint sin errores; las tres tablas existen con sus checks/defaults; trigger + REVOKE de inmutabilidad presentes; RLS habilitada en las tres.</done>
</task>

<task type="auto">
  <name>Task 2: pgTAP 0005 — columnas, checks, RLS deny-by-default, inmutabilidad del audit</name>
  <files>supabase/tests/0005_revision_identidad.test.sql</files>
  <action>
Crea `supabase/tests/0005_revision_identidad.test.sql` espejando `0004_parlamentario.test.sql` (begin / select plan(N) / asserts / select * from finish() / rollback). Cubre:

- `has_table` para `vinculo_identidad`, `revision_identidad`, `identidad_audit`.
- `has_column` de las columnas críticas de cada tabla (estado, metodo, parlamentario_id, candidatos, salida_modelo, revisor_id, confidence, modelo_version, created_at, evidence, conflicts).
- `col_default_is('public','vinculo_identidad','estado','no_confirmado', ...)` y `col_default_is('public','revision_identidad','estado','pendiente', ...)`.
- `throws_ok` con SQLSTATE '23514' para un `estado` inválido en vinculo_identidad y para un `metodo` inválido en identidad_audit (check constraints).
- `lives_ok`: un INSERT válido en `identidad_audit` (append es legal).
- INMUTABILIDAD (el corazón de ID-08, Pitfall 4): inserta una fila de audit en un `lives_ok`, luego
  - `throws_ok($$ update identidad_audit set decision='x' where id = ... $$, 'identidad_audit es append-only ...', ...)` verificando que el UPDATE LANZA (mensaje del trigger o cualquier excepción — usa el patrón throws_ok que captura el error del trigger).
  - `throws_ok($$ delete from identidad_audit where id = ... $$, ...)` verificando que el DELETE LANZA.
  - Nota: pgTAP corre como el rol de test (superuser-equivalente del entorno local), que es el peor caso para inmutabilidad — si el trigger bloquea aquí, bloquea al service role. El trigger, no el REVOKE, es lo que hace fallar este UPDATE/DELETE; el test prueba la barrera efectiva.
- RLS: `is(relrowsecurity, true, ...)` (vía pg_class) y `is_empty($$ select * from pg_policy where polrelid = '<tabla>'::regclass $$, ...)` para las TRES tablas (deny-by-default, espejo de los asserts de RLS en 0004).
- Verifica también que existe el REVOKE: consulta `has_function_privilege`/`pg_class` no aplica directo; en su lugar añade un assert documentando que `update`/`delete` no están en los grants de `public` sobre `identidad_audit` (vía `is_empty` de una query a `information_schema.role_table_grants` filtrando privilege_type in ('UPDATE','DELETE') and grantee='PUBLIC' and table_name='identidad_audit').

Ajusta `plan(N)` al número real de asserts (recuerda el off-by-one de 0005: el plan debe igualar los asserts ejecutados).
  </action>
  <verify>
    <automated>supabase test db</automated>
  </verify>
  <done>`supabase test db` pasa incluyendo: los dos throws_ok de UPDATE y DELETE sobre identidad_audit (inmutabilidad), RLS habilitada + sin policies en las tres tablas, y el REVOKE verificado. plan(N) coincide con los asserts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service role → identidad_audit | El writer usa service role (bypassa RLS); solo el trigger lo frena |
| anon → las 3 tablas | anon no debe leer identidad/cola/auditoría |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering/Repudiation | identidad_audit (editar/borrar una decisión) | mitigate | Trigger `RAISE EXCEPTION` en UPDATE/DELETE (aplica al service role) + `REVOKE update,delete,truncate`; pgTAP `throws_ok` verifica que UPDATE y DELETE fallan. |
| T-04-06 | Information Disclosure | anon lee cola/audit/vínculo (rut en candidatos? no) | mitigate | RLS deny-by-default (enable sin policies) en las 3 tablas; pgTAP verifica `relrowsecurity` + `is_empty(pg_policy)`. `candidatos` jsonb sin rut (minimización aguas arriba). |
| T-04-07 | Tampering | estado/metodo fuera del dominio permitido | mitigate | Checks `(confirmado|probable|no_confirmado)` y `(determinista|llm|humano)`; default no_confirmado; pgTAP `throws_ok`. |
| T-04-SC | Tampering | npm/pnpm installs | accept | Sin paquetes nuevos (solo DDL); ver Package Legitimacy Audit del RESEARCH. |
</threat_model>

<verification>
- `supabase db reset` aplica 0001→0006 limpio.
- `supabase db lint` sin errores.
- `supabase test db` verde, con los throws_ok de inmutabilidad (UPDATE+DELETE) como asserts explícitos.
</verification>

<success_criteria>
- `vinculo_identidad` con estado default no_confirmado + check (ID-06).
- `revision_identidad` cola con candidatos/salida_modelo/revisor_id/timestamps (ID-05).
- `identidad_audit` append-only enforced por trigger + REVOKE (ID-08), probado contra UPDATE y DELETE.
- RLS deny-by-default en las tres tablas.
</success_criteria>

<output>
Create `.planning/phases/04-adjudicacion-identidad-compuerta-humana-golden-set/04-02-SUMMARY.md` when done
</output>
