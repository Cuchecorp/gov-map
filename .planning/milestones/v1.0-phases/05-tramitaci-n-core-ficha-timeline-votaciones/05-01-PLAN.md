---
phase: 05-tramitaci-n-core-ficha-timeline-votaciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tramitacion/package.json
  - packages/tramitacion/tsconfig.json
  - packages/tramitacion/vitest.config.ts
  - packages/tramitacion/src/index.ts
  - packages/tramitacion/src/model.ts
  - packages/tramitacion/src/model.test.ts
  - packages/tramitacion/test/fixtures/camara-votacion-boletin.xml
  - packages/tramitacion/test/fixtures/camara-votacion-detalle.xml
  - packages/tramitacion/test/fixtures/camara-sesiones-58.xml
  - packages/tramitacion/test/fixtures/senado-tramitacion.xml
  - packages/tramitacion/test/fixtures/senado-votacion.xml
  - packages/tramitacion/src/slice.e2e.test.ts
  - supabase/migrations/0008_tramitacion.sql
  - supabase/tests/0007_tramitacion.test.sql
  - tsconfig.json
  - tsconfig.base.json
autonomous: false
requirements: [TRAM-03, TRAM-09]
must_haves:
  truths:
    - "El paquete @obs/tramitacion existe en el workspace y typecheck pasa"
    - "Existen las tablas proyecto/votacion/voto/tramitacion_evento con RLS public-read explícito para anon"
    - "anon puede SELECT proyecto/votacion/voto/tramitacion_evento pero NO parlamentario.rut"
    - "Existe un test E2E del slice (RED) que describe el objetivo ciudadano end-to-end"
  artifacts:
    - path: "packages/tramitacion/src/model.ts"
      provides: "Tipos del modelo común (Proyecto/Votacion/Voto/TramitacionEvento) + zod schemas + Provenance inline"
      contains: "export interface Proyecto"
    - path: "supabase/migrations/0008_tramitacion.sql"
      provides: "DDL proyecto/votacion/voto/tramitacion_evento + RLS public-read para anon"
      contains: "create policy"
    - path: "supabase/tests/0007_tramitacion.test.sql"
      provides: "pgTAP: existencia de tablas + RLS anon read público + guarda rut no-anon"
      contains: "select plan("
    - path: "packages/tramitacion/test/fixtures/senado-votacion.xml"
      provides: "Fixture XML real de votación nominal del Senado (boletín con votos)"
      min_lines: 10
  key_links:
    - from: "packages/tramitacion/tsconfig.json"
      to: "@obs/ingest, @obs/core, @obs/identity, @obs/adjudication"
      via: "project references + path mapping"
      pattern: "@obs/(ingest|core|identity|adjudication)"
    - from: "supabase/migrations/0008_tramitacion.sql"
      to: "parlamentario"
      via: "voto.parlamentario_id FK"
      pattern: "references parlamentario"
---

<objective>
Establecer la base de la rebanada vertical de Tramitación: dar de alta el paquete `@obs/tramitacion` en el workspace, crear la migración `0008` con las 4 tablas del modelo común (`proyecto`/`votacion`/`voto`/`tramitacion_evento`) y sus **policies RLS de lectura pública explícitas para `anon`** (el deny-by-default heredado bloquea al frontend), definir los tipos + zod schemas del modelo común, capturar las **fixtures XML reales** que las olas siguientes parsean, y escribir el **test E2E del slice en RED** que describe el objetivo ciudadano completo (Walking-skeleton-first de MVP).

Purpose: Sin scaffold, migración y RLS pública, ninguna otra tarea de la fase puede correr; sin el test E2E rojo no hay contrato observable del valor ciudadano. Esta es la fundación que las olas 2-4 hacen verde.
Output: Paquete `@obs/tramitacion` typecheck-verde, migración `0008` + pgTAP de RLS verde, tipos del modelo común, 5 fixtures reales, y un test E2E (RED) que falla con módulos ausentes — la diana de la fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-CONTEXT.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-RESEARCH.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-VALIDATION.md

<interfaces>
<!-- Contratos existentes que este plan reusa. NO explorar el codebase: usar estos. -->

From packages/core/src/parlamentario.ts:
```typescript
export type Camara = "diputados" | "senado";
export type EstadoIdentidad = "confirmado" | "probable" | "no_confirmado";
export interface Parlamentario {
  id: string;                       // p.ej. "P00001"
  nombre_normalizado: string;
  nombres: string; apellido_paterno: string; apellido_materno: string;
  camara: Camara; periodo: string; region: string | null;
  distrito: string | null; circunscripcion: string | null; partido: string | null;
  rut: string | null;               // INTERNO, jamás público
  parlid_senado: string | null; id_diputado_camara: string | null;
  estado: EstadoIdentidad; email: string | null;
  origen: string; fecha_captura: string; enlace: string;   // provenance inline
}
```

From packages/core/src/provenance.ts:
```typescript
export interface Provenance { /* source/sourceUrl/capturedAt-shape */ }
export function makeProvenance(source: string, sourceUrl: string): Provenance;
```

From packages/ingest/src/index.ts (barrel) — disponible para olas siguientes:
```typescript
export { Fetcher, RetryableError, FetchError } from "./fetcher";
export { HostRateLimiter } from "./rate-limiter";
export { RobotsGuard, IDENTIFIED_UA } from "./robots";
export { assertAllowedUrl, HostNotAllowedError, DEFAULT_ALLOWED_SUFFIXES } from "./allowlist";
```

Migración existente — patrón RLS y pgTAP a espejar:
- supabase/migrations/0005_parlamentario.sql: `alter table X enable row level security;` SIN policies = deny-by-default (anon lee 0). Migraciones previas llegan hasta 0007.
- supabase/tests/0004_parlamentario.test.sql: `begin; select plan(N); select has_table(...); select throws_ok(...); select * from finish(); rollback;`. Los tests pgTAP previos llegan hasta supabase/tests/0006_*.
- Supabase local: URL `http://127.0.0.1:54421`; service key vía `SUPABASE_LOCAL_SERVICE_KEY`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @obs/tramitacion + alta en workspace</name>
  <files>packages/tramitacion/package.json, packages/tramitacion/tsconfig.json, packages/tramitacion/vitest.config.ts, packages/tramitacion/src/index.ts, tsconfig.json, tsconfig.base.json</files>
  <action>
Crear el paquete `@obs/tramitacion` espejando el alta de `@obs/identity` (ver 03-03-SUMMARY): `package.json` con `name: "@obs/tramitacion"`, `type: module`, deps workspace `@obs/core`, `@obs/ingest`, `@obs/identity`, `@obs/adjudication` (todas `workspace:*`) + `fast-xml-parser@5`, `zod`, `@supabase/supabase-js@^2` (para el writer de la ola 4), y devDep `vitest`. Script `test: vitest`.
`tsconfig.json` del paquete: extiende `../../tsconfig.base.json`, `composite: true`, `lib: ["ES2022","DOM"]` (consume Web APIs como en @obs/ingest), y project references a `../core`, `../ingest`, `../identity`, `../adjudication`.
`vitest.config.ts`: igual al de `packages/identity/vitest.config.ts`.
`src/index.ts`: barrel inicial (re-export de `./model`; las olas siguientes añaden parsers/timeline/writer).
Añadir el project reference `{ "path": "packages/tramitacion" }` en el `tsconfig.json` raíz y el path mapping `"@obs/tramitacion": ["packages/tramitacion/src/index.ts"]` en `tsconfig.base.json`. Correr `pnpm install` para linkear el workspace.
NO crear `BaseConnector`-derivados aquí (la ingesta acotada reusa colaboradores sueltos en la ola 4 — anti-pattern documentado: la caché diaria saltaría re-corridas).
  </action>
  <verify>
    <automated>pnpm install; pnpm -w typecheck</automated>
  </verify>
  <done>`@obs/tramitacion` existe, está linkeado en el workspace, y `pnpm -w typecheck` sale con exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelo común (tipos + zod) + migración 0008 + pgTAP de RLS público</name>
  <files>packages/tramitacion/src/model.ts, packages/tramitacion/src/model.test.ts, supabase/migrations/0008_tramitacion.sql, supabase/tests/0007_tramitacion.test.sql</files>
  <behavior>
    - model.ts: `Proyecto`, `Votacion`, `Voto`, `TramitacionEvento` con sus zod schemas; cada uno lleva provenance inline (`origen`/`fecha_captura`/`enlace`) per TRAM-09.
    - Test model: un objeto válido pasa el schema; un `seleccion` fuera de {si,no,abstencion,pareo} lo rechaza; un `voto` con `parlamentario_id=null` es válido (Senado no confirmado).
    - pgTAP: las 4 tablas existen; `anon` (rol del frontend) puede SELECT en las 4; `anon` NO lee `parlamentario.rut`; `voto.parlamentario_id` es nullable y FK a `parlamentario(id)`.
  </behavior>
  <action>
**model.ts** — definir el modelo común normalizado (TRAM-03), llave de cruce = número de boletín:
- `Proyecto`: `boletin` (PK, string completo p.ej. "18296-05"), `boletin_num` (base sin sufijo "18296" — Pitfall 1: el Senado se consulta con el base), `titulo`, `iniciativa` ("Mensaje"|"Moción"|null), `camara_origen` (string|null), `autores` (string[]), `materia` (string|null), `estado` (string|null), `etapa` (string|null), `subetapa` (string|null), + provenance inline (`origen`/`fecha_captura`/`enlace`).
- `Votacion`: `id` (string, p.ej. `camara:89178` / `senado:<sesion>:<fecha>`), `boletin` (FK), `fecha` (ISO string), `etapa` (string|null), `tipo` (string|null), `quorum` (string|null), `resultado` (string|null), `total_si`/`total_no`/`total_abstencion`/`total_pareo` (int), `camara` ("diputados"|"senado"), + provenance inline.
- `Voto`: `votacion_id` (FK), `mencion_nombre` (string crudo de la fuente), `parlamentario_id` (string|null — solo poblado si confirmado/determinista), `seleccion` ("si"|"no"|"abstencion"|"pareo"), `metodo` ("determinista"|"llm"|"humano"|null), `estado_vinculo` ("confirmado"|"probable"|"no_confirmado"|null).
- `TramitacionEvento`: `boletin` (FK), `fecha` (ISO string), `camara` (string), `tipo` ("tramite"|"urgencia"|"informe"|"oficio"|"votacion"), `descripcion` (string), `enlace` (string|null), + provenance inline.
- Definir zod `ProyectoSchema`/`VotacionSchema`/`VotoSchema`/`TramitacionEventoSchema` con los enums anteriores y exportarlos. Reusar `Provenance` de `@obs/core` para la forma de procedencia.

**0008_tramitacion.sql** — DDL + RLS:
- `proyecto`: `boletin text primary key`, `boletin_num text not null`, columnas anteriores; provenance inline (`origen text`, `fecha_captura timestamptz`, `enlace text`).
- `votacion`: `id text primary key`, `boletin text not null references proyecto(boletin)`, `camara text not null check (camara in ('diputados','senado'))`, totales `int not null default 0`, provenance inline.
- `voto`: `id bigint generated always as identity primary key`, `votacion_id text not null references votacion(id)`, `mencion_nombre text not null`, `parlamentario_id text references parlamentario(id)` (nullable), `seleccion text not null check (seleccion in ('si','no','abstencion','pareo'))`, `metodo text`, `estado_vinculo text`.
- `tramitacion_evento`: `id bigint generated always as identity primary key`, `boletin text not null references proyecto(boletin)`, `fecha timestamptz`, `camara text`, `tipo text not null check (tipo in ('tramite','urgencia','informe','oficio','votacion'))`, `descripcion text`, `enlace text`, provenance inline.
- Índices para la ficha: `votacion(boletin)`, `voto(votacion_id)`, `tramitacion_evento(boletin, fecha)`.
- Claves naturales para upsert idempotente (ola 4): `unique (votacion_id, mencion_nombre)` en `voto`; `unique (boletin, fecha, camara, tipo, descripcion)` en `tramitacion_evento`.
- **RLS público-read EXPLÍCITO (Pitfall 5):** por cada tabla `alter table X enable row level security;` + `create policy X_public_read on X for select to anon using (true);`. NO policies de insert/update/delete (el writer usa service key que bypassa RLS). NO tocar `parlamentario` (sigue deny-by-default: rut/email nunca al anon).

**0007_tramitacion.test.sql** — pgTAP (espejar 0004_parlamentario.test.sql):
- `begin; select plan(N);`
- `has_table` para las 4 tablas; `has_column` para `voto.parlamentario_id`, `proyecto.boletin`, provenance; `col_is_null('public','voto','parlamentario_id', ...)`.
- RLS público: insertar una fila semilla mínima de `proyecto`/`votacion`/`voto`/`tramitacion_evento` como `postgres`; luego `set local role anon;` y `lives_ok($$ select 1 from proyecto $$, ...)` + análogos para las 4 + assert de que devuelven la fila (`isnt_empty`).
- Guarda de identidad: con rol `anon`, verificar que `parlamentario` NO es legible (`is_empty($$ select rut from parlamentario $$, 'anon NO lee parlamentario.rut')` — patrón deny-by-default que 0004/0006 ya emplean).
- `select * from finish(); rollback;`
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run model; supabase test db</automated>
  </verify>
  <done>model.test verde; `supabase test db` corre `0007_tramitacion.test.sql` verde (tablas existen, anon lee las 4, anon NO lee parlamentario.rut).</done>
</task>

<task type="auto">
  <name>Task 3: Fixtures XML reales + test E2E del slice (RED)</name>
  <files>packages/tramitacion/test/fixtures/camara-votacion-boletin.xml, packages/tramitacion/test/fixtures/camara-votacion-detalle.xml, packages/tramitacion/test/fixtures/camara-sesiones-58.xml, packages/tramitacion/test/fixtures/senado-tramitacion.xml, packages/tramitacion/test/fixtures/senado-votacion.xml, packages/tramitacion/src/slice.e2e.test.ts</files>
  <action>
**Capturar fixtures reales** (mismo método de Fase 3: `curl` con `User-Agent: Bot-Ciudadano/1.0 (consulta ciudadana Chile; contacto@dominio.cl)`, delay 2-3s entre llamadas, guardar la respuesta cruda sin editar). Capturar un boletín que CRUCE ambas cámaras (14309, con votos en ambas):
- `camara-votacion-boletin.xml` ← `https://opendata.camara.cl/wscamaradiputados.asmx/getVotaciones_Boletin?prmBoletin=14309` (namespace tempuri.org, `<Boletin>` estructurado, `<ID>`/`Codigo`).
- `camara-votacion-detalle.xml` ← `https://opendata.camara.cl/camaradiputados/WServices/WSLegislativo.asmx/retornarVotacionDetalle?prmVotacionID=89178` (155 `<Voto><Diputado><Id>...<OpcionVoto>`, namespace v1).
- `camara-sesiones-58.xml` ← `https://opendata.camara.cl/camaradiputados/WServices/WSSala.asmx/retornarSesionesXLegislatura?prmLegislaturaID=58`.
- `senado-tramitacion.xml` ← `https://tramitacion.senado.cl/wspublico/tramitacion.php?boletin=18296` (descripcion + tramites + urgencias + informes + oficios).
- `senado-votacion.xml` ← `https://tramitacion.senado.cl/wspublico/votaciones.php?boletin=14309` (voto-a-voto por `<PARLAMENTARIO>` + SELECCION).
Si algún endpoint devuelve 403/429 o vacío, re-intentar con el delay; documentar en el SUMMARY el boletín efectivo usado. Boletines del Senado con votos validados de respaldo: 10634, 11608, 15096, 16374.

**slice.e2e.test.ts (RED)** — describe el objetivo ciudadano end-to-end como contrato fallido a propósito (importa símbolos que aún no existen → falla en RED). NO usar `it.todo`; usar imports reales que fallarán:
- Importar `parseCamaraVotacion`, `parseSenadoTramitacion`, `parseSenadoVotacion`, `fusionarTimeline`, `reconciliarVotosSenado` desde `../src/index` (aún no exportados → RED).
- Test 1 (TRAM-01/06): `parseCamaraVotacion(fixtureCamara)` produce ≥1 `Votacion` con `boletin` "14309-04" y totales SI/NO/Abst mapeados.
- Test 2 (TRAM-02/04): `parseSenadoTramitacion(fixtureSenado)` produce un `Proyecto` con `estado`/`etapa` derivados de `descripcion` + ≥1 `TramitacionEvento`.
- Test 3 (TRAM-05): `fusionarTimeline([...])` ordena eventos de ambas cámaras por fecha ascendente.
- Test 4 (TRAM-06b/guarda): `reconciliarVotosSenado(...)` vincula `parlamentario_id` solo cuando el resultado es determinista/confirmado.
Marcar el archivo con un comentario `// SLICE E2E — RED hasta ola 4; las olas 2-3 lo vuelven verde.`. NO añadir los exports todavía (deben fallar). Verificar que el test FALLA por símbolos ausentes (no por error de fixture).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['camara-votacion-boletin','camara-votacion-detalle','camara-sesiones-58','senado-tramitacion','senado-votacion'].forEach(f=>{const p='packages/tramitacion/test/fixtures/'+f+'.xml';if(!fs.existsSync(p)||fs.statSync(p).size<200)throw new Error('fixture faltante/vacio: '+p)});console.log('fixtures OK')"</automated>
  </verify>
  <done>Los 5 fixtures existen y no están vacíos (>200 bytes); `slice.e2e.test.ts` existe y FALLA en RED por símbolos ausentes (no por fixture roto).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (frontend) → Postgres | El rol `anon` del frontend lee datos públicos; jamás debe leer `parlamentario.rut`/`email`. |
| fuente gov (XML) → parser | Entrada no confiable: XML de Cámara/Senado capturado como fixture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | RLS de proyecto/votacion/voto/tramitacion_evento vs parlamentario | mitigate | Policies public-read SOLO en las 4 tablas de datos públicos; `parlamentario` queda deny-by-default; pgTAP verifica que `anon` NO lee `parlamentario.rut`. |
| T-05-02 | Tampering | voto.parlamentario_id (afirmación de identidad) | mitigate | `parlamentario_id` nullable + FK; default null hasta vínculo confirmado (la lógica de poblado vive en olas 2-3). |
| T-05-03 | DoS | XML malformado / XXE / billion laughs | mitigate | fast-xml-parser sin expansión de entidades externas; respuestas < 300 KB; zod valida shape (olas siguientes). |
| T-05-SC | Tampering | npm installs (@obs/tramitacion deps) | mitigate | Sin paquetes de runtime nuevos: `fast-xml-parser`/`zod`/`@supabase/supabase-js` ya auditados (Fase 1-4). Sin checkpoint de paquete requerido. |
</threat_model>

<verification>
- `pnpm -w typecheck` exit 0.
- `pnpm --filter @obs/tramitacion test --run model` verde.
- `supabase test db` con `0007_tramitacion.test.sql` verde (tablas + RLS anon + guarda rut).
- 5 fixtures reales presentes y no vacíos.
- `slice.e2e.test.ts` falla en RED por símbolos ausentes.
</verification>

<success_criteria>
- Paquete `@obs/tramitacion` dado de alta y typecheck-verde.
- Migración `0008` aplicada con RLS público-read explícito en las 4 tablas; `parlamentario` intacto.
- Modelo común tipado + zod.
- Fixtures XML reales capturados para un boletín cross-cámara.
- Test E2E del slice en RED como diana de la fase.
</success_criteria>

<output>
Create `.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-01-SUMMARY.md` when done
</output>
