---
phase: 05-tramitaci-n-core-ficha-timeline-votaciones
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/tramitacion/src/parse-camara-votacion.ts
  - packages/tramitacion/src/parse-camara-votacion.test.ts
  - packages/tramitacion/src/parse-camara-sesion.ts
  - packages/tramitacion/src/parse-camara-sesion.test.ts
  - packages/tramitacion/src/parse-senado-tramitacion.ts
  - packages/tramitacion/src/parse-senado-tramitacion.test.ts
  - packages/tramitacion/src/parse-senado-votacion.ts
  - packages/tramitacion/src/parse-senado-votacion.test.ts
  - packages/tramitacion/src/fecha.ts
  - packages/tramitacion/src/fecha.test.ts
  - packages/tramitacion/src/timeline.ts
  - packages/tramitacion/src/timeline.test.ts
  - packages/tramitacion/src/index.ts
autonomous: true
requirements: [TRAM-01, TRAM-02, TRAM-03, TRAM-04, TRAM-05, TRAM-06, TRAM-09]
must_haves:
  truths:
    - "Un parser produce Votacion[] con totales SI/NO/Abst/Pareo + resultado desde el XML real de la Cámara"
    - "Un parser produce un Proyecto (estado/etapa/iniciativa/autores) + TramitacionEvento[] desde el XML real del Senado"
    - "Un parser produce Votacion + voto-a-voto crudo desde votaciones.php del Senado"
    - "El timeline fusiona eventos de ambas cámaras ordenados cronológicamente por fecha"
    - "Las fechas dd/mm/yyyy se parsean correctamente (no via new Date directo)"
  artifacts:
    - path: "packages/tramitacion/src/parse-camara-votacion.ts"
      provides: "getVotaciones_Boletin XML → Votacion[]; retornarVotacionDetalle → voto-a-voto por Diputado/Id"
      contains: "export function parseCamaraVotacion"
    - path: "packages/tramitacion/src/parse-senado-tramitacion.ts"
      provides: "tramitacion.php → Proyecto + TramitacionEvento[] (tramite/urgencia/informe/oficio)"
      contains: "export function parseSenadoTramitacion"
    - path: "packages/tramitacion/src/parse-senado-votacion.ts"
      provides: "votaciones.php → Votacion + votos por nombre (mencion cruda + seleccion)"
      contains: "export function parseSenadoVotacion"
    - path: "packages/tramitacion/src/timeline.ts"
      provides: "fusionarTimeline(eventos) ordenado cronológicamente cross-cámara"
      contains: "export function fusionarTimeline"
  key_links:
    - from: "packages/tramitacion/src/parse-*.ts"
      to: "@obs/tramitacion model (Proyecto/Votacion/TramitacionEvento) + zod"
      via: "validación de cada fila con los schemas de model.ts"
      pattern: "(Proyecto|Votacion|TramitacionEvento)Schema"
    - from: "packages/tramitacion/src/parse-*.ts"
      to: "provenance inline (origen/fecha_captura/enlace)"
      via: "makeProvenance al construir cada modelo"
      pattern: "makeProvenance|fecha_captura"
---

<objective>
Construir los 4 parsers XML (Cámara opendata + Senado wspublico) que mapean las respuestas reales al modelo común, el helper de parseo de fechas `dd/mm/yyyy`, y la fusión cronológica del timeline cross-cámara. Cada fila lleva provenance inline (TRAM-09) y valida contra los zod schemas del modelo. Esta ola hace verde los Tests 1-3 del slice E2E (la parte de datos; la reconciliación de votos es la ola 3).

Purpose: Es el corazón de la normalización (TRAM-01/02/03/04/05/06): convierte dos fuentes heterogéneas (dos namespaces de Cámara + XML del Senado) en `Proyecto`/`Votacion`/`Voto`/`TramitacionEvento` cruzables por boletín. Sin esto no hay datos que mostrar.
Output: Parsers testeados sobre fixtures reales, fusión de timeline ordenada, exports en el barrel para que la ola 3 (reconciliación) y la ola 4 (writer/ingesta) los consuman.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-RESEARCH.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-CONTEXT.md
@.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-01-SUMMARY.md

<interfaces>
<!-- Modelo común creado por 05-01. Usar estos tipos; no redefinir. -->
From packages/tramitacion/src/model.ts:
```typescript
export interface Proyecto { boletin; boletin_num; titulo; iniciativa; camara_origen; autores: string[]; materia; estado; etapa; subetapa; origen; fecha_captura; enlace }
export interface Votacion { id; boletin; fecha; etapa; tipo; quorum; resultado; total_si; total_no; total_abstencion; total_pareo; camara; origen; fecha_captura; enlace }
export interface Voto { votacion_id; mencion_nombre; parlamentario_id: string|null; seleccion: 'si'|'no'|'abstencion'|'pareo'; metodo; estado_vinculo }
export interface TramitacionEvento { boletin; fecha; camara; tipo: 'tramite'|'urgencia'|'informe'|'oficio'|'votacion'; descripcion; enlace; origen; fecha_captura; enlace }
export { ProyectoSchema, VotacionSchema, VotoSchema, TramitacionEventoSchema };
```

Patrón de parseo XML establecido (packages/identity/src/parse-senado.ts / parse-camara.ts, 03-03):
```typescript
import { XMLParser } from "fast-xml-parser";
const parser = new XMLParser({ ignoreAttributes: false });
// Listas opcionales: forzar array → [].concat(doc.x?.y ?? [])  (fast-xml-parser colapsa nodo único a objeto)
// Validar cada fila con el zod schema antes de devolver.
```

XML shapes reales (de 05-RESEARCH §3-6 — todos VERIFIED live):
- Cámara getVotaciones_Boletin (ns tempuri.org): `<Votaciones><Votacion><ID>88813</ID><Fecha>2026-05-11T19:21:07</Fecha><Tipo Codigo="6">Única</Tipo><Resultado Codigo="2">Rechazado</Resultado><Quorum Codigo="1">..</Quorum><Boletin>14309-04</Boletin>...`  (boletín ESTRUCTURADO, sin regex).
- Cámara retornarVotacionDetalle (ns v1): `<Votacion><Id>..</Id><TotalSi>94</TotalSi>...<Votos><Voto><Diputado><Id>803</Id>..</Diputado><OpcionVoto Valor="0">En Contra</OpcionVoto></Voto> ...155...</Votos>` (Valor 0=En Contra, 1=Afirmativo).
- Senado tramitacion.php: `<proyectos><proyecto><descripcion><boletin>18296-05</boletin><titulo/><fecha_ingreso>03/06/2026</fecha_ingreso><iniciativa>Mensaje</iniciativa><camara_origen>C.Diputados</camara_origen><etapa/><subetapa/><estado>En tramitación </estado><link_mensaje_mocion/></descripcion><autores/><tramitacion><tramite><FECHA/><DESCRIPCIONTRAMITE/><ETAPDESCRIPCION/><CAMARATRAMITE/></tramite></tramitacion><urgencias/><informes><informe><FECHAINFORME/><TRAMITE/><LINK_INFORME/></informe></informes><oficios><oficio><FECHA/><TRAMITE/><CAMARA/><LINK_OFICIO/></oficio></oficios></proyecto></proyectos>`
- Senado votaciones.php: `<votaciones><votacion><SESION>47/372</SESION><FECHA>27/08/2024</FECHA><SI>30</SI><NO>1</NO><ABSTENCION>4</ABSTENCION><PAREO>0</PAREO><QUORUM/><TIPOVOTACION/><ETAPA/><DETALLE_VOTACION><VOTO><PARLAMENTARIO>Coloma C., Juan Antonio</PARLAMENTARIO><SELECCION>Si</SELECCION></VOTO>...</DETALLE_VOTACION></votacion></votaciones>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parser de fechas dd/mm/yyyy + parsers de Cámara (votación boletín + detalle + sesiones)</name>
  <files>packages/tramitacion/src/fecha.ts, packages/tramitacion/src/fecha.test.ts, packages/tramitacion/src/parse-camara-votacion.ts, packages/tramitacion/src/parse-camara-votacion.test.ts, packages/tramitacion/src/parse-camara-sesion.ts, packages/tramitacion/src/parse-camara-sesion.test.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - fecha.ts: `parseFechaCL("03/06/2026")` → Date(2026-06-03); rechaza/`null` ante `"Invalid"`; pasa ISO `2026-05-11T19:21:07` directo a Date.
    - parseCamaraVotacion(fixtureBoletin): ≥1 Votacion con `boletin="14309-04"`, `camara="diputados"`, `resultado` mapeado, totales (de detalle o de boletín según disponibilidad).
    - parseCamaraVotacion con detalle (fixtureDetalle): produce votos-a-voto crudos con `diputadoId` (Diputado/Id) y `opcion` (En Contra→no, Afirmativo→si).
    - parseCamaraSesion(fixtureSesiones58): ≥1 sesión con Id/Numero/FechaInicio.
  </behavior>
  <action>
**fecha.ts** — `parseFechaCL(s: string): Date | null`: si `s` matchea `^\d{2}/\d{2}/\d{4}$` parsear como `dd/mm/yyyy` (construir `new Date(yyyy, mm-1, dd)`); si matchea ISO `yyyy-mm-ddThh:mm:ss` pasarlo directo a `new Date`; cualquier otro → `null`. NUNCA `new Date("03/06/2026")` directo (Pitfall 3). Exportar también un helper `toIso(d: Date): string`.

**parse-camara-votacion.ts** — `parseCamaraVotacion(xml: string, opts?: { detalleXml?: string; enlace?: string }): Votacion[]`:
- Parsear con `XMLParser({ ignoreAttributes:false })`. Namespace tempuri.org (`getVotaciones_Boletin`): leer `<Votaciones><Votacion>`; forzar array con `[].concat()`.
- Mapear: `id = "camara:" + ID`, `boletin = <Boletin>` (estructurado, NO regex), `camara="diputados"`, `fecha = parseFechaCL(<Fecha>)`, `tipo`/`quorum`/`resultado` del texto del nodo. `boletin` puede traer sufijo "14309-04"; conservarlo completo.
- Si se pasa `detalleXml` (namespace v1, `retornarVotacionDetalle`): leer `<TotalSi>/<TotalNo>/<TotalAbstencion>/<TotalDispensado>` para totales; mapear `<Votos><Voto>` a `{ diputadoId: <Diputado><Id>, opcion: OpcionVoto.Valor==="1"?"si":"no", nombreCrudo: nombre+apellidos }`. Exponer estos votos-a-voto en el retorno (p.ej. una propiedad `votos` adjunta a la Votación o una función hermana `parseCamaraVotoDetalle(detalleXml)` que la ola 3 consume). Pitfall 4: dos namespaces distintos = dos shapes; no asumir uno solo.
- Provenance inline con `makeProvenance("camara", enlace ?? url-fuente)`.
- Validar cada `Votacion` con `VotacionSchema` antes de devolver.

**parse-camara-sesion.ts** — `parseCamaraSesion(xml: string)`: leer `<SesionesSalaColeccion><Sesion>` (ns v1), devolver `{ id, numero, fechaInicio, fechaTermino, tipo, estado }[]`. Sirve a la ola 4 para descubrir sesiones de Leg 58.

Exportar `parseCamaraVotacion`, `parseCamaraVotoDetalle` (si se separa), `parseCamaraSesion`, `parseFechaCL`, `toIso` desde `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run "fecha|parse-camara"</automated>
  </verify>
  <done>fecha, parse-camara-votacion y parse-camara-sesion verdes sobre fixtures reales; totales y voto-a-voto por Diputado/Id mapeados; Test 1 del slice E2E (parseCamaraVotacion) verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parsers del Senado (tramitación + votaciones nominales)</name>
  <files>packages/tramitacion/src/parse-senado-tramitacion.ts, packages/tramitacion/src/parse-senado-tramitacion.test.ts, packages/tramitacion/src/parse-senado-votacion.ts, packages/tramitacion/src/parse-senado-votacion.test.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - parseSenadoTramitacion(fixture): un Proyecto con boletin completo + boletin_num base, titulo, iniciativa, camara_origen, autores[], estado/etapa/subetapa; y TramitacionEvento[] con tramites+urgencias+informes(LINK_INFORME)+oficios(LINK_OFICIO), cada uno con fecha parseada y enlace cuando aplica.
    - parseSenadoVotacion(fixture): Votacion[] con totales SI/NO/ABSTENCION/PAREO + quorum/tipo/etapa; cada votación con sus votos crudos {mencionNombre (trim), seleccion mapeada a si|no|abstencion|pareo}.
    - Votaciones vacías (proyecto joven): devuelve [] sin lanzar (Pitfall 2).
  </behavior>
  <action>
**parse-senado-tramitacion.ts** — `parseSenadoTramitacion(xml: string, enlace?: string): { proyecto: Proyecto; eventos: TramitacionEvento[] }`:
- `<proyectos><proyecto>`; del `<descripcion>` derivar el `Proyecto`: `boletin` (completo, p.ej. "18296-05"), `boletin_num` (base "18296" — quitar sufijo `-\d+` para futuras consultas, Pitfall 1), `titulo`, `iniciativa` ("Mensaje"|"Moción"), `camara_origen`, `estado` (trim — viene con espacio final), `etapa`, `subetapa`, `materia` (null si vacío). `autores`: `[].concat(autores?.autor ?? [])` → string[].
- `eventos`: fusionar dentro del Senado las 4 secciones a `TramitacionEvento`:
  - `tramitacion/tramite` → `{ tipo:'tramite', fecha: parseFechaCL(FECHA), camara: CAMARATRAMITE, descripcion: DESCRIPCIONTRAMITE, enlace: null }`
  - `urgencias/urgencia` → `{ tipo:'urgencia', ... }`
  - `informes/informe` → `{ tipo:'informe', fecha: parseFechaCL(FECHAINFORME), descripcion: TRAMITE, enlace: LINK_INFORME }`
  - `oficios/oficio` → `{ tipo:'oficio', fecha: parseFechaCL(FECHA), camara: CAMARA, descripcion: TRAMITE, enlace: LINK_OFICIO }`
  - Cada sección: forzar array con `[].concat()`. `boletin` del evento = boletín completo del proyecto.
- Provenance inline `makeProvenance("senado", enlace ?? url)` en proyecto y eventos.
- Validar `proyecto` con `ProyectoSchema` y cada evento con `TramitacionEventoSchema`.
- Encoding (A2): leer el XML como texto UTF-8; si aparecen acentos corruptos, documentarlo (no bloquea — fixture validado UTF-8).

**parse-senado-votacion.ts** — `parseSenadoVotacion(xml: string, boletin: string, enlace?: string): { votacion: Votacion; votos: { mencionNombre: string; seleccion: 'si'|'no'|'abstencion'|'pareo' }[] }[]`:
- `<votaciones><votacion>`; si vacío → `[]` (Pitfall 2: proyecto en primer trámite Cámara no tiene votos Senado).
- Por votación: `id = "senado:" + boletin + ":" + FECHA`, `camara="senado"`, totales `SI/NO/ABSTENCION/PAREO` (int), `quorum`/`tipo`(TIPOVOTACION)/`etapa`(ETAPA), `fecha = parseFechaCL(FECHA)`.
- `votos`: `<DETALLE_VOTACION><VOTO>` → `{ mencionNombre: PARLAMENTARIO.trim() (Pitfall 3: whitespace final), seleccion: map(SELECCION) }` donde map: "Si"→si, "No"→no, "Abstencion"→abstencion, "Pareo"→pareo (case-insensitive). NO usar el `<TEMA>` para la llave de boletín (trae puntos de millar); usar el `boletin` que recibe la función.
- Provenance inline. Validar la Votacion con `VotacionSchema`.
- NO reconciliar identidad aquí (eso es la ola 3): solo devolver `mencionNombre` crudo + `seleccion`.

Exportar ambos parsers desde `index.ts`. Tras esta tarea, Tests 2 del slice E2E (parseSenadoTramitacion) verde.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run parse-senado</automated>
  </verify>
  <done>parse-senado-tramitacion y parse-senado-votacion verdes sobre fixtures reales; proyecto con estado/etapa, eventos con enlaces, votos crudos con seleccion mapeada; votaciones vacías → [] sin lanzar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fusión cronológica del timeline cross-cámara</name>
  <files>packages/tramitacion/src/timeline.ts, packages/tramitacion/src/timeline.test.ts, packages/tramitacion/src/index.ts</files>
  <behavior>
    - fusionarTimeline(eventos): ordena por fecha ASC todos los TramitacionEvento (Senado tramite/urgencia/informe/oficio + eventos de votación de ambas cámaras).
    - Empate de fecha: orden estable, Cámara antes que Senado (UI-SPEC §3.2).
    - Eventos con fecha null: van al final (o se filtran de forma documentada), sin romper el orden.
  </behavior>
  <action>
**timeline.ts** — `fusionarTimeline(eventos: TramitacionEvento[]): TramitacionEvento[]`:
- Recibir la lista ya normalizada (los parsers de la Task 2 producen `TramitacionEvento[]`; la ola 4 además añade eventos `tipo:'votacion'` derivados de cada Votacion con `descripcion = resultado + totales`).
- Ordenar por `fecha` ascendente (parseadas a Date ANTES de comparar; usar `parseFechaCL`/ISO ya resuelto en los modelos). Empate exacto de fecha: Cámara (`camara` que contenga "Diputados") antes que Senado — orden estable.
- Eventos con `fecha` null/inválida: agruparlos al final en orden de inserción (documentar la decisión; no descartarlos silenciosamente).
- Función pura, sin red ni DB. Exportar desde `index.ts`.
- Incluir un helper `eventoDesdeVotacion(v: Votacion): TramitacionEvento` que la ola 4 usa para inyectar las votaciones en el timeline (tipo:'votacion', descripcion: `${resultado} (Sí ${total_si} · No ${total_no} · Abst ${total_abstencion} · Pareo ${total_pareo})`, enlace null o de la fuente).

Tras esta tarea, Test 3 del slice E2E (fusionarTimeline) verde. El Test 4 (reconciliarVotosSenado) permanece RED hasta la ola 3.
  </action>
  <verify>
    <automated>pnpm --filter @obs/tramitacion test --run timeline; pnpm -w typecheck</automated>
  </verify>
  <done>fusionarTimeline verde: eventos de ambas cámaras ordenados cronológicamente, empate estable Cámara-antes-Senado, fechas null al final; typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XML de fuente gov → parser | Entrada no confiable; el XML puede venir malformado, con entidades, o encoding mixto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | DoS / Disclosure | fast-xml-parser sobre XML de Cámara/Senado | mitigate | fast-xml-parser sin expansión de entidades externas (default seguro); fixtures < 300 KB; zod valida shape de cada fila (Proyecto/Votacion/TramitacionEvento) antes de devolver. Patrón T-03-09. |
| T-05-04 | Tampering | fecha mal parseada altera el orden del timeline | mitigate | `parseFechaCL` explícito para dd/mm/yyyy; NUNCA `new Date(string-local)`; test de borde con "03/06/2026". |
| T-05-05 | Tampering | boletín tomado de texto libre (`<TEMA>`/`<Descripcion>`) | mitigate | Usar `<Boletin>` estructurado (Cámara) y el param de consulta (Senado); NO regex sobre texto con puntos de millar. |
| T-05-SC | Tampering | npm installs | mitigate | Sin paquetes nuevos: fast-xml-parser/zod ya en uso. N/A. |
</threat_model>

<verification>
- `pnpm --filter @obs/tramitacion test --run` verde para fecha/parse-camara/parse-senado/timeline.
- `pnpm -w typecheck` exit 0.
- Tests 1-3 del `slice.e2e.test.ts` verdes (Test 4 sigue RED → ola 3).
</verification>

<success_criteria>
- Los 4 parsers mapean el XML real al modelo común con provenance inline.
- Fechas dd/mm/yyyy parseadas correctamente.
- Timeline fusionado cronológicamente cross-cámara con empate estable.
- Votaciones del Senado vacías manejadas como caso normal.
</success_criteria>

<output>
Create `.planning/phases/05-tramitaci-n-core-ficha-timeline-votaciones/05-02-SUMMARY.md` when done
</output>
