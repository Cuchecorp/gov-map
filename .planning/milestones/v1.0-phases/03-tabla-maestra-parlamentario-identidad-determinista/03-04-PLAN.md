---
phase: 03-tabla-maestra-parlamentario-identidad-determinista
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/identity/src/writer-supabase.ts
  - packages/identity/src/writer-fs.ts
  - packages/identity/src/seed-cli.ts
  - packages/identity/package.json
  - supabase/seeds/parlamentario.seed.json
  - .github/workflows/backup-parlamentario.yml
  - docs/operador-fase3.md
autonomous: false
requirements: [ID-01, ID-09]

must_haves:
  truths:
    - "Una corrida LIVE del seeder trae los catálogos reales de Cámara (≈155) y Senado (vigentes) sin errores 403/429"
    - "La maestra real queda cargada en el Supabase LOCAL vía el writer real"
    - "El snapshot autoritativo supabase/seeds/parlamentario.seed.json existe en git con la maestra real"
    - "El operador revisó el lote sembrado y promovió (o no) las filas a confirmado — la promoción es revisión humana"
    - "Hay una cadencia de respaldo (GitHub Actions) y los pasos de operador diferidos (push remoto, R2) están documentados"
  artifacts:
    - path: "packages/identity/src/seed-cli.ts"
      provides: "CLI que ejecuta la corrida live: fetch→parse→match→upsert local + exportMaestra a git"
      exports: ["main"]
    - path: "supabase/seeds/parlamentario.seed.json"
      provides: "Snapshot autoritativo de la maestra real (ID-09)"
    - path: ".github/workflows/backup-parlamentario.yml"
      provides: "Cadencia de respaldo periódico del snapshot"
    - path: "docs/operador-fase3.md"
      provides: "Pasos de operador: promoción a confirmado, push remoto diferido, R2 diferido"
  key_links:
    - from: "packages/identity/src/seed-cli.ts"
      to: "packages/identity/src/seeder.ts"
      via: "invoca runSeeder + upsertMaestra + exportMaestra con writers reales"
      pattern: "runSeeder"
    - from: "packages/identity/src/writer-supabase.ts"
      to: "parlamentario (Postgres local)"
      via: "upsert por clave natural contra Supabase local"
      pattern: "parlamentario"
---

<objective>
Realiza/habilita la corrida LIVE: cablea el writer real contra el Supabase LOCAL y el writer de filesystem, ejecuta el seeder en VIVO contra ambos catálogos gubernamentales, produce el dataset maestro REAL, lo carga al Postgres local y escribe el snapshot autoritativo `supabase/seeds/parlamentario.seed.json` en git (ID-09 cumplido HOY). Cierra con la compuerta de revisión humana (operador promueve el lote a `confirmado`) y deja documentada la cadencia de respaldo + los pasos de operador diferidos (push remoto, R2).

Purpose: ID-01 exige una maestra sembrada con datos reales y confirmada por revisión humana (no auto-generada); ID-09 exige respaldo fuera de Supabase. El "live now" de esta fase = correr el fetch real → maestra real → snapshot git + carga local. La orquestación CAN ejecutar fetches gov reales (rate-limited, UA identificado).
Output: maestra real en Supabase local + `supabase/seeds/parlamentario.seed.json` en git + workflow de respaldo + doc de operador + lote revisado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-CONTEXT.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-VALIDATION.md

<interfaces>
<!-- Contratos producidos por Plan 03 (usar directamente). -->
From @obs/identity:
  runSeeder(deps): Promise<Parlamentario[]>      // fetch→parse→normaliza→match
  upsertMaestra(rows, writer): Promise<...>      // idempotente por clave natural; writer inyectable
  exportMaestra(maestra, opts): ...              // JSON estable → supabase/seeds/parlamentario.seed.json (+ R2 gateado)
  parseSenado / parseCamara, normalizarNombre, matchDeterminista

<!-- El writer inyectable que Plan 03 dejó como interfaz; aquí van las impls reales. -->
<!-- Supabase LOCAL ya operativo (docker, Fases 1-2). Migración 0005 aplicada por Plan 02. -->
<!-- Credenciales (CONTEXT): .env LEE catálogos OK; Supabase REMOTO no aplicable (service key, no PAT) → solo LOCAL; R2 da 401 → diferido. -->

Endpoints LIVE:
  Senado:  https://tramitacion.senado.cl/wspublico/senadores_vigentes.php
  Cámara:  https://opendata.camara.cl/camaradiputados/WServices/WSDiputado.asmx/retornarDiputadosPeriodoActual
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Writers reales (Supabase local + fs) + CLI de siembra; corrida LIVE</name>
  <files>packages/identity/src/writer-supabase.ts, packages/identity/src/writer-fs.ts, packages/identity/src/seed-cli.ts, packages/identity/package.json, supabase/seeds/parlamentario.seed.json</files>
  <read_first>packages/identity/src/seeder.ts, packages/identity/src/backup.ts, packages/ingest/src/index.ts</read_first>
  <action>
    Implementa el `writer-supabase.ts` (impl real del writer inyectable de Plan 03): upsert por clave natural (`parlid_senado` / `id_diputado_camara`) contra el Supabase LOCAL vía `@supabase/supabase-js` usando la SERVICE key del `.env` y la URL local (puertos 544xx remapeados, Fase 1) — `on conflict` por la clave natural para idempotencia. Implementa `writer-fs.ts`: escribe el contenido de `exportMaestra` a `supabase/seeds/parlamentario.seed.json`. Crea `seed-cli.ts` (`main`) que: instancia `Fetcher`+`HostRateLimiter`+`RobotsGuard` reales, corre `runSeeder` en VIVO contra ambos endpoints, hace `upsertMaestra` con el writer Supabase local, y `exportMaestra` con el writer fs. Añade un script `"seed:live"` en `packages/identity/package.json`. EJECUTA la corrida live una vez (la orquestación puede hacer fetches gov reales con rate-limit 2-3s + UA identificado). Verifica: ≈155 diputados + senadores vigentes cargados, 0 errores 403/429, y `supabase/seeds/parlamentario.seed.json` generado con la maestra real. Si un endpoint diera 403/429, NO insistas en ráfaga: reporta y deja el snapshot del que sí respondió, documentando el reintento como paso de operador. El `estado` cargado NO es `confirmado` (queda para el checkpoint siguiente).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/seeds/parlamentario.seed.json','utf8');const d=JSON.parse(s);const dip=d.filter(p=>p.camara==='diputados').length;if(dip<150)throw new Error('esperaba ~155 diputados, hay '+dip);console.log('OK diputados:',dip,'total:',d.length)"</automated>
  </verify>
  <done>Corrida live ejecutada sin 403/429; `supabase/seeds/parlamentario.seed.json` contiene ≈155 diputados + senadores vigentes con provenance; maestra cargada en Supabase local; ningún estado=confirmado automático.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Revisión humana del lote sembrado — promoción a confirmado (ID-01)</name>
  <what-built>
    El seeder corrió en vivo y produjo la maestra real: `supabase/seeds/parlamentario.seed.json` (snapshot autoritativo en git) + la carga en el Supabase local. Cada fila tiene `estado` intermedio (no_confirmado/probable), `nombre_normalizado`, provenance (origen, fecha_captura, enlace) y la clave natural de su cámara. Nada se marcó `confirmado` automáticamente — los catálogos oficiales son autoritativos para los vigentes, pero la promoción es una decisión humana (riesgo existencial #1).
  </what-built>
  <how-to-verify>
    1. Abre `supabase/seeds/parlamentario.seed.json` y revisa los conteos: deben ser ≈155 diputados (camara="diputados") + el set de senadores vigentes (camara="senado").
    2. Hojea una muestra: nombres, apellidos, partido (militancia vigente en diputados), region/circunscripcion, parlid_senado / id_diputado_camara presentes; `rut` null (los catálogos no lo traen — esperado); `distrito` null en diputados (esperado).
    3. Confirma que ninguna fila trae `estado: "confirmado"` aún.
    4. Si aceptas el lote como autoritativo: promueve las filas vigentes a `confirmado` en el Supabase local (un UPDATE acotado a la clave natural del lote; la orquestación lo aplica tras tu visto bueno) y re-exporta el snapshot.
    5. Si detectas anomalías (conteo raro, partido vacío masivo, homónimos colapsados), descríbelas para corregir antes de promover.
  </how-to-verify>
  <resume-signal>Escribe "aprobado: promover a confirmado" para sellar el lote, o describe las anomalías a corregir.</resume-signal>
  <files>supabase/seeds/parlamentario.seed.json</files>
  <action>Checkpoint de revisión humana (ver what-built / how-to-verify / resume-signal). Tras el visto bueno del operador, aplicar un UPDATE acotado a la clave natural del lote en el Supabase local para promover las filas vigentes a estado=confirmado y re-exportar el snapshot. Si hay anomalías, corregirlas antes de promover. NO promover nada sin aprobación humana explícita (riesgo existencial #1).</action>
  <verify><human-check>El operador confirmó los conteos (≈155 diputados + senadores vigentes), validó que ninguna fila estaba en confirmado automático, y dio el visto bueno para la promoción.</human-check></verify>
  <done>El operador revisó el lote y selló la promoción a confirmado (o reportó anomalías a corregir); nada se confirmó sin aprobación humana.</done>
</task>

<task type="auto">
  <name>Task 3: Cadencia de respaldo (GitHub Actions) + doc de pasos de operador diferidos</name>
  <files>.github/workflows/backup-parlamentario.yml, docs/operador-fase3.md</files>
  <read_first>packages/identity/src/seed-cli.ts, packages/identity/src/backup.ts, .planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-CONTEXT.md</read_first>
  <action>
    Crea `.github/workflows/backup-parlamentario.yml`: un workflow programado (cron, p.ej. semanal) que corre el seeder/export en CI (Deno/Node con rate-limit) y commitea/actualiza `supabase/seeds/parlamentario.seed.json` como respaldo periódico fuera de Supabase (ID-09 cadencia). Gatea el push a R2 con un `if` sobre la presencia de credencial R2 válida (hoy 401 → omitido). Crea `docs/operador-fase3.md` documentando los pasos de operador diferidos por credencial (per CONTEXT): (a) **push remoto**: cuando haya DB password/PAT `sbp_`, `supabase link` + `supabase db push` aplica 0005 + carga el seed al remoto; (b) **R2**: cuando la credencial S3 deje de dar 401, correr el backup a R2 vía `R2Store` (cierra también el checkpoint R2 de Fase 1); (c) **promoción a confirmado**: el flujo de revisión humana del lote; (d) re-correr la siembra es idempotente (clave natural), no duplica. Deja claro que el snapshot git es el respaldo autoritativo HOY y no depende del free tier.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/backup-parlamentario.yml','utf8');if(!/schedule|cron/.test(y))throw new Error('falta cadencia cron');const d=require('fs').readFileSync('docs/operador-fase3.md','utf8');if(!/R2/.test(d)||!/push/.test(d))throw new Error('faltan pasos diferidos');console.log('OK')"</automated>
  </verify>
  <done>Workflow de respaldo con cadencia cron y R2 gateado; `docs/operador-fase3.md` documenta push remoto, R2 y promoción como pasos de operador diferidos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/seed-cli → fuentes gubernamentales | La corrida live hace GET a Cámara/Senado bajo WAF |
| operador → estado=confirmado | La promoción a hecho público pasa por decisión humana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (existencial #1) | promoción a confirmado | mitigate | El seeder nunca auto-marca confirmado; la promoción es un checkpoint humano bloqueante (Task 2). Solo confirmado sería público (Fase 4/6). |
| T-03-12 | DoS (WAF / ráfaga) | corrida live | mitigate | HostRateLimiter 2-3s + UA identificado; ante 403/429 NO se insiste en ráfaga (se reporta como paso de operador). |
| T-03-13 | Information Disclosure | seed.json en git | accept | El snapshot contiene datos de catálogo público (nombres/partido/region); `rut` null (no se exporta dato personal fabricado); email es público de los catálogos oficiales. Aceptado: dato ya público. |
| T-03-14 | Repudiation | respaldo periódico | mitigate | GitHub Actions versiona cada snapshot en git con historial; provenance por fila (origen, fecha_captura, enlace). |
</threat_model>

<verification>
- Corrida live: ≈155 diputados + senadores vigentes, 0 errores 403/429.
- `supabase/seeds/parlamentario.seed.json` en git con la maestra real + provenance.
- Lote revisado por operador; promoción a confirmado sellada tras visto bueno.
- Workflow de respaldo + doc de operador presentes.
</verification>

<success_criteria>
- [ ] La maestra real está sembrada desde Cámara y Senado en el Supabase local.
- [ ] El snapshot autoritativo en git cumple ID-09 HOY (sin depender de R2/remoto).
- [ ] La promoción a `confirmado` pasó por revisión humana (no auto-generada).
- [ ] Cadencia de respaldo + pasos de operador diferidos documentados.
</success_criteria>

<output>
Create `.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-04-SUMMARY.md` when done
</output>
