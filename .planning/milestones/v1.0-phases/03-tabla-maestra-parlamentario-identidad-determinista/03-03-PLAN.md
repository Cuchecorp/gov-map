---
phase: 03-tabla-maestra-parlamentario-identidad-determinista
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - packages/identity/test/fixtures/senado-real.xml
  - packages/identity/test/fixtures/camara-real.xml
  - packages/identity/src/parse-senado.ts
  - packages/identity/src/parse-camara.ts
  - packages/identity/src/parse-senado.test.ts
  - packages/identity/src/parse-camara.test.ts
  - packages/identity/src/seeder.ts
  - packages/identity/src/seeder.test.ts
  - packages/identity/src/backup.ts
  - packages/identity/src/backup.test.ts
  - packages/identity/src/index.ts
autonomous: true
requirements: [ID-01, ID-02, ID-09]

must_haves:
  truths:
    - "Parsear el XML real del Senado produce N senadores y el de la Cámara 155 diputados con sus campos esperados"
    - "El partido del diputado sale de la <Militancia> vigente (FechaTermino nil), no del nodo período"
    - "Correr el seeder/upsert dos veces produce el mismo estado (idempotente, sin duplicados)"
    - "exportMaestra produce un JSON estable y completo; el round-trip export→import preserva la maestra"
    - "El seeder corre normalizarNombre y matchDeterminista; nada se marca confirmado automáticamente (estado intermedio)"
  artifacts:
    - path: "packages/identity/src/parse-senado.ts"
      provides: "XML Senado → modelo Parlamentario (vía fast-xml-parser + zod)"
      exports: ["parseSenado"]
    - path: "packages/identity/src/parse-camara.ts"
      provides: "XML Cámara → modelo Parlamentario con militancia vigente"
      exports: ["parseCamara"]
    - path: "packages/identity/src/seeder.ts"
      provides: "Orquesta fetch→parse→normaliza→match→upsert idempotente reusando @obs/ingest"
      exports: ["runSeeder", "upsertMaestra"]
    - path: "packages/identity/src/backup.ts"
      provides: "exportMaestra → JSON git (autoritativo) + R2 gateado por credencial"
      exports: ["exportMaestra"]
    - path: "packages/identity/test/fixtures/senado-real.xml"
      provides: "Muestra REAL del XML del Senado capturada live"
    - path: "packages/identity/test/fixtures/camara-real.xml"
      provides: "Muestra REAL del XML de la Cámara capturada live"
  key_links:
    - from: "packages/identity/src/seeder.ts"
      to: "@obs/ingest"
      via: "reusa Fetcher/RobotsGuard/HostRateLimiter/assertAllowedUrl"
      pattern: "from \"@obs/ingest\""
    - from: "packages/identity/src/seeder.ts"
      to: "packages/identity/src/deterministic.ts"
      via: "corre matchDeterminista para asignar estado"
      pattern: "matchDeterminista"
    - from: "packages/identity/src/backup.ts"
      to: "supabase/seeds/parlamentario.seed.json"
      via: "escribe el snapshot autoritativo en git"
      pattern: "parlamentario.seed.json"
---

<objective>
Completa la rebanada vertical end-to-end: parsea el XML REAL de ambos catálogos (con fixtures capturados live) hacia el modelo `Parlamentario`, orquesta el seeder idempotente (fetch reusando `@obs/ingest` → parse → `normalizarNombre` → `matchDeterminista` → upsert por clave natural), y exporta la maestra a un snapshot JSON versionado en git (ID-09 autoritativo HOY) con R2 gateado por credencial.

Purpose: ID-01 (maestra sembrada desde Cámara+Senado), ID-02 (el seeder corre el matcher determinista) e ID-09 (respaldo fuera de Supabase). Tras este plan, el sistema tiene la maestra poblable con datos reales, consultable, con cada persona en un estado de identidad, respaldada en git.
Output: parsers + seeder + backup con tests verdes sobre fixtures reales (sin red), listos para la corrida LIVE de Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md
@.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-CONTEXT.md
@.planning/phases/01-framework-de-conectores-almacenamiento-orquestaci-n/01-02-SUMMARY.md

<interfaces>
<!-- API REAL exportada por @obs/ingest (Fase 1) — usar directamente, NO explorar. -->
From packages/ingest/src/index.ts:
  export class Fetcher { get(spec: { url: string; host?: string }): Promise<Uint8Array> }
    // constructor opts: { fetchFn?: typeof fetch; ua?: string; allowlist?: AllowlistOptions }
  export class HostRateLimiter { wait(host: string): Promise<void> }   // serial 2-3s por host
  export class RobotsGuard { isAllowed(url: string): Promise<boolean> }
  export function assertAllowedUrl(rawUrl: string, opts?: AllowlistOptions): URL  // SSRF deny-by-default
  export const IDENTIFIED_UA: string
  export class R2Store { putImmutable(...): Promise<...> }  // gateado por credencial (R2 da 401 hoy)
  export function sha256Hex(...): Promise<string>

From @obs/core (este fase añadió en Plan 01):
  export function makeProvenance(source: string, sourceUrl: string): Provenance
  export interface Parlamentario { ... }   // Plan 01
  export const ParlamentarioSeedSchema    // Plan 01, zod
  export function normalizarNombre(...)    // Plan 01

From @obs/identity (Plan 01):
  export function matchDeterminista(mention, maestra): Resolution

<!-- IMPORTANTE: opendata.camara.cl es subdominio de camara.cl → YA está en el allowlist
     (DEFAULT_ALLOWED_SUFFIXES incluye "camara.cl"). NO hay que tocar el allowlist. -->

Endpoints (LIVE-confirmados 2026-06-18):
  Senado:  https://tramitacion.senado.cl/wspublico/senadores_vigentes.php   (XML 12.9KB)
  Cámara:  https://opendata.camara.cl/camaradiputados/WServices/WSDiputado.asmx/retornarDiputadosPeriodoActual   (XML 179KB, 155 dip)

Forma del XML (de 03-RESEARCH Code Examples):
  Senado:  doc.senadores.senador[]  → PARLID, PARLAPELLIDOPATERNO, PARLAPELLIDOMATERNO,
           PARLNOMBRE, REGION, CIRCUNSCRIPCION, PARTIDO, FONO, EMAIL, CURRICULUM
  Cámara:  doc.DiputadosPeriodoColeccion.DiputadoPeriodo[].Diputado → Id, Nombre, Nombre2,
           ApellidoPaterno, ApellidoMaterno, RUT(vacío), Sexo, Militancias.Militancia[]→Partido{Id,Nombre,Alias}
           vigencia = FechaTermino nil; partido = militancia que cubre la fecha de corte (2026-03-11)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixtures reales + parse-senado/parse-camara con militancia vigente</name>
  <files>packages/identity/test/fixtures/senado-real.xml, packages/identity/test/fixtures/camara-real.xml, packages/identity/src/parse-senado.ts, packages/identity/src/parse-camara.ts, packages/identity/src/parse-senado.test.ts, packages/identity/src/parse-camara.test.ts</files>
  <read_first>packages/core/src/parlamentario.ts, packages/core/src/nombre.ts, .planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md</read_first>
  <behavior>
    - parseSenado(xml) sobre el fixture real → array de Parlamentario con camara="senado"; cada uno con parlid_senado (PARLID), apellidos, nombres, region, circunscripcion, partido, email; nombre_normalizado vía normalizarNombre.
    - parseCamara(xml) sobre el fixture real → exactamente 155 Parlamentario con camara="diputados"; cada uno con id_diputado_camara (Id), partido = militancia vigente, rut null, distrito null.
    - partidoVigente elige la <Militancia> cuyo rango cubre la fecha de corte (FechaTermino nil = vigente), NO el nodo período (Pitfall 5).
    - El shape parseado pasa ParlamentarioSeedSchema (zod) antes de devolver.
  </behavior>
  <action>
    Captura una muestra REAL de cada XML como fixture: descarga `senadores_vigentes.php` y `retornarDiputadosPeriodoActual` con el UA `IDENTIFIED_UA` y respetando rate-limit (usa el Fetcher/HostRateLimiter de `@obs/ingest`, o un curl manual con `User-Agent: Bot-Ciudadano/1.0` y delay 2-3s), y guárdalas en `packages/identity/test/fixtures/{senado-real.xml,camara-real.xml}`. Escribe primero los tests (`parse-senado.test.ts`, `parse-camara.test.ts`) leyendo los fixtures y aseverando el behavior — RED, commit `test(03-03): RED parsers`. Implementa `parse-senado.ts` (`parseSenado`) y `parse-camara.ts` (`parseCamara`) con `XMLParser` de fast-xml-parser@5 (`ignoreAttributes:false, parseTagValue:false`), forzando arrays con `[].concat(...)`, mapeando al modelo `Parlamentario`, corriendo `normalizarNombre`, y validando con `ParlamentarioSeedSchema`. Para la Cámara implementa `partidoVigente(militancias, corte)` que filtra por `FechaTermino` nil/rango (corte = 2026-03-11) per Pattern 2/Pitfall 5. `rut` y `distrito` quedan null para diputados (Pitfall 4 — NO fabricar). Fija el valor de `periodo`: "2026-2030" para diputados; para senadores una etiqueta consistente (p.ej. "senado-vigente-2026") — documenta la convención (Open Question 2 / A2). Commit verde `feat(03-03): parsers Senado/Cámara`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test --run parse</automated>
  </verify>
  <done>Fixtures reales presentes; parseCamara devuelve 155 diputados con partido vigente; parseSenado devuelve los senadores con parlid; shape valida zod; tests verdes sin red. 2 commits (RED, GREEN).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seeder idempotente (fetch reusa @obs/ingest → parse → match → upsert)</name>
  <files>packages/identity/src/seeder.ts, packages/identity/src/seeder.test.ts, packages/identity/src/index.ts</files>
  <read_first>packages/ingest/src/index.ts, packages/ingest/src/fetcher.ts, packages/identity/src/deterministic.ts, packages/identity/src/parse-senado.ts, packages/identity/src/parse-camara.ts</read_first>
  <behavior>
    - runSeeder con fetch inyectable (mock que devuelve los fixtures) produce la maestra combinada (senadores + 155 diputados) con provenance (origen, fecha_captura=now, enlace=endpoint) por fila.
    - upsertMaestra es idempotente: correr 2× con el mismo input deja el mismo estado (sin duplicados) — la clave natural es parlid_senado / id_diputado_camara.
    - El seeder corre matchDeterminista sobre cada fila contra la propia maestra; el estado inicial es 'no_confirmado'/'probable' (NO 'confirmado' automático — la promoción es revisión humana, Plan 04 checkpoint).
    - El seeder usa assertAllowedUrl + RobotsGuard + HostRateLimiter antes del fetch (reuso de política, no reimplementa rate-limit).
    - Un DB-writer inyectable (interfaz upsert) permite testear idempotencia sin Postgres real (fake in-memory).
  </behavior>
  <action>
    Escribe primero `seeder.test.ts`: usa un `fetchFn` mock que devuelve los fixtures (sin red), un `Fetcher` con `extraHosts`/allowlist real, y un fake in-memory del writer; asevera el behavior (combinación, provenance, idempotencia 2×, estado no auto-confirmado) — RED, commit `test(03-03): RED seeder`. Implementa `seeder.ts` exportando `runSeeder(deps)` y `upsertMaestra(rows, writer)`. `runSeeder` instancia/recibe `Fetcher`+`HostRateLimiter`+`RobotsGuard`, para cada endpoint hace `assertAllowedUrl` → `robots.isAllowed` → `rl.wait(host)` → `fetcher.get` → `parseSenado`/`parseCamara` → genera `id` interno estable (p.ej. P + correlativo o derivado de la clave natural) → corre `matchDeterminista` → arma la fila con `makeProvenance("senado"|"camara", url)` mapeada a (origen, fecha_captura, enlace). NO uses `BaseConnector.run` (su caché diaria saltaría re-siembras del mismo día — anti-pattern de 03-RESEARCH). `upsertMaestra` hace upsert por clave natural vía el writer inyectable (idempotente; el writer real contra Supabase local lo cablea Plan 04). El estado inicial NO es 'confirmado'. Exporta desde `index.ts`. Commit verde `feat(03-03): seeder idempotente`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test --run seeder</automated>
  </verify>
  <done>runSeeder combina ambos catálogos con provenance; upsertMaestra idempotente (2× = mismo estado); estado inicial no auto-confirmado; reusa @obs/ingest (no reimplementa rate-limit). 2 commits (RED, GREEN).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: exportMaestra — snapshot JSON git (ID-09) + R2 gateado, round-trip</name>
  <files>packages/identity/src/backup.ts, packages/identity/src/backup.test.ts, packages/identity/src/index.ts</files>
  <read_first>packages/ingest/src/r2-store.ts, packages/identity/src/seeder.ts, .planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-RESEARCH.md</read_first>
  <behavior>
    - exportMaestra(maestra) produce un JSON estable y determinista (orden de filas y de claves fijo) — dos exports de la misma maestra son byte-idénticos.
    - El round-trip export→parse→import preserva la maestra (mismo conjunto de filas y campos).
    - La escritura a R2 está gateada: si no hay credencial válida (R2 da 401 hoy) el export a git ocurre igual y el R2 se omite/loguea sin romper (ID-09 se cumple HOY solo con git).
    - El destino del snapshot autoritativo es `supabase/seeds/parlamentario.seed.json`.
  </behavior>
  <action>
    Escribe primero `backup.test.ts`: asevera determinismo (dos exports byte-idénticos), round-trip, y que con writer R2 ausente/erroring el export git no falla — RED, commit `test(03-03): RED backup`. Implementa `backup.ts` exportando `exportMaestra(maestra, opts)`: serializa a JSON con orden estable (ordena filas por `id`, claves ordenadas) y devuelve/escribe el contenido destinado a `supabase/seeds/parlamentario.seed.json` (la escritura de archivo real la dispara Plan 04 con un writer inyectable; el test usa un writer fake). Si se pasa un `R2Store` con credencial válida, intenta `putImmutable`; envuélvelo en try/catch o un flag `r2Enabled` que por defecto es false (credencial 401 — diferido per CONTEXT) de modo que la ausencia de R2 NO rompe el export a git. Documenta que el JSON en git ES el respaldo fuera de Supabase que cumple ID-09 HOY, y que R2 + push remoto son pasos de operador diferidos. Exporta desde `index.ts`. Commit verde `feat(03-03): exportMaestra backup`.
  </action>
  <verify>
    <automated>pnpm --filter @obs/identity test --run backup</automated>
  </verify>
  <done>exportMaestra determinista + round-trip verde; R2 gateado (ausencia de credencial no rompe el export git); destino `supabase/seeds/parlamentario.seed.json` fijado. 2 commits (RED, GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeder → fuentes gubernamentales | El seeder hace GET a Cámara/Senado (WAF + rate-limit) |
| seeder → Postgres | Escribe la maestra; único origen de datos de identidad |
| export → git/R2 | El activo más caro de reconstruir sale del free tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering/EoP (SSRF) | seeder fetch | mitigate | `assertAllowedUrl` (allowlist gubernamental + bloqueo de IPs internas) antes de cada fetch; opendata.camara.cl ya cubierto por el sufijo camara.cl. |
| T-03-08 | DoS (auto-inflingido / WAF) | seeder | mitigate | Reuso de `HostRateLimiter` (2-3s serial por host) + `RobotsGuard` + `IDENTIFIED_UA`; el seeder NO reimplementa la política. |
| T-03-09 | DoS (XML malformado / entidades) | parsers | mitigate | `fast-xml-parser` sin expansión de entidades externas; `ParlamentarioSeedSchema` (zod) como guard de shape; catálogos < 200KB. |
| T-03-10 | Information Disclosure | provenance/snapshot | mitigate | El snapshot git contiene el modelo normalizado; `rut` viene null de catálogos (no se exporta dato personal fabricado); R2 con crudo gateado. |
| T-03-SC | Tampering | install fast-xml-parser@5 | mitigate | Auditado en 03-RESEARCH (Package Legitimacy: `[OK]`, sin postinstall, dist-tag latest=5.9.2). |
</threat_model>

<verification>
- `pnpm --filter @obs/identity test --run` verde (parse, seeder, backup), sin red — todo sobre fixtures reales + mocks.
- `pnpm -w typecheck` exit 0.
- parseCamara = 155 diputados; idempotencia 2× = mismo estado; export determinista + round-trip.
</verification>

<success_criteria>
- [ ] Fixtures reales de Senado y Cámara capturados y commiteados.
- [ ] Parsers mapean al modelo Parlamentario con militancia vigente (Cámara) y parlid (Senado).
- [ ] Seeder reusa `@obs/ingest` (rate-limit/robots/SSRF), es idempotente, NO auto-confirma.
- [ ] exportMaestra produce snapshot JSON estable (ID-09) con R2 gateado.
</success_criteria>

<output>
Create `.planning/phases/03-tabla-maestra-parlamentario-identidad-determinista/03-03-SUMMARY.md` when done
</output>
