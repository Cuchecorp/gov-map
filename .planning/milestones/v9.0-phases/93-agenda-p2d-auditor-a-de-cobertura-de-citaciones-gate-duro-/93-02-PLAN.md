---
phase: 93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md
autonomous: true
requirements: [CIT-01]
must_haves:
  truths:
    - "Queda declarado con evidencia DOM de BrowserOS sobre PROD qué renderizan /agenda (semana vigente y semana pasada con datos) y la ficha del proyecto (estado/tramitación) vs los counts de DB"
    - "El gap #1 (citacionVigente forward-only oculta citaciones PASADAS en la ficha) queda CONFIRMADO o refutado con DOM del deploy real"
    - "El gap sesion_tabla_item-no-se-lee-en-la-ficha queda confirmado o refutado con DOM"
    - "Cada gap enlaza un boletín/proyecto/semana CONCRETO (no genérico) para que 94 lo pueda reproducir"
  artifacts:
    - path: ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md"
      provides: "Gaps de wiring frontend con evidencia DOM BrowserOS (URL PROD + selector/texto capturado + count DB esperado vs mostrado)"
      contains: "citacionVigente"
  key_links:
    - from: "ficha de proyecto (estado-actual-block.tsx citacionVigente)"
      to: "citacion PASADA en DB del mismo boletín"
      via: "filtro forward-only fecha >= hoy oculta las pasadas"
      pattern: "citacionVigente|forward-only"
---

<objective>
Auditar el WIRING frontend de citaciones con BrowserOS sobre PROD: capturar evidencia DOM de qué renderizan /agenda y la ficha del proyecto vs los counts de DB, y declarar los gaps concretos (con boletín/semana reproducibles). NO se arregla nada aquí (los fixes son 94); se DECLARA la brecha con evidencia.

Purpose: El reporte del operador (STATE 2026-07-22) dice que citaciones sala/comisiones y tablas de sesión están mal wired en el frontend. CIT-01 exige medir el wiring, no solo el scraping. La evidencia DOM alimenta directamente los fixes de UI de 94.
Output: `93-WIRING-EVIDENCIA.md` con los gaps confirmados/refutados por DOM real (Plan 03 lo consume para la DECLARACIÓN de cobertura).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-CONTEXT.md
@.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md

@app/app/agenda/page.tsx
@app/components/estado-actual-block.tsx

<interfaces>
<!-- Evidencia estática del research; el gate de la fase es CONFIRMAR con DOM en PROD. -->

Gap #1 (candidato principal del reporte del operador) — app/components/estado-actual-block.tsx:
  export function citacionVigente(citaciones, hoy) {
    const futuras = citaciones... .filter(x.d.toISOString().slice(0,10) >= hoyChile) // SOLO fecha >= hoy
    return futuras[0] ...
  }
  → La línea "Citado en {comision} el {fecha}" desaparece en cuanto la fecha pasa.
     Un proyecto citado la semana pasada NO lo declara su ficha aunque esté en DB.

Gap #2 — sesion_tabla_item NO se lee en estado-actual-block.tsx:
  → un proyecto en la tabla de sala de la semana NO lo declara su ficha (solo /agenda lo muestra).

/agenda (app/app/agenda/page.tsx):
  CitacionesSection (línea ~277): .eq("semana_iso", key) ambas cámaras, agrupa por día — muestra pasadas y futuras de la semana navegada. Gap presunto = DATOS (solo 2 semanas Cámara ingeridas).
  SalaTableServer (línea ~453): rango [lunes, lunes+1); Senado mode=available; Cámara degrada al PDF si 0 filas.

Herramientas: BrowserOS MCP + scripts/bros-cli.mjs (usado en 68/89/91/92). Deploy PROD vigente: fa4d4369 (STATE); el gate lee el DOM del deploy REAL (lección: cascada solo cazable en deploy real).

Plan de captura (research § Evidencia BrowserOS):
  1. /agenda semana vigente (2026-W30): ¿render de citaciones Cámara+Senado? (Cámara W30 = 0 filas → esperado "No hay"; Senado sí).
  2. /agenda semana pasada con datos (ej. 2026-W26): ¿muestra las Cámara/Senado ingeridas?
  3. Ficha de un proyecto con citación PASADA en DB: ¿aparece "Citado en..."? (esperado: NO, por forward-only) → gap.
  4. Ficha de un proyecto en sesion_tabla_item: ¿lo declara? (esperado: NO) → gap.

Nota A3 (research): quick 260722-eia tocó deep-links; el deploy PROD podría haber cambiado algo. BrowserOS DIRIME — reportar lo OBSERVADO, no lo asumido.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elegir los sujetos concretos de prueba (boletines/semanas) por psql</name>
  <files>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md</files>
  <read_first>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md (§ Wiring Frontend), app/app/agenda/page.tsx, app/components/estado-actual-block.tsx:116-137,271,312</read_first>
  <action>
    Crear `93-WIRING-EVIDENCIA.md` con encabezado y sección "0. Sujetos de prueba".
    Con psql (PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c), elegir sujetos REPRODUCIBLES:
    (i) un semana_iso PASADA con citaciones en DB (ej. la más poblada: `select semana_iso, count(*) from citacion where fecha < now() group by semana_iso order by 2 desc limit 3`);
    (ii) un boletín cuya ÚNICA/última citación sea PASADA (fecha < hoy) — para probar el gap forward-only: `select cp.boletin, max(c.fecha)::date from citacion_punto cp join citacion c on c.id=cp.citacion_id where cp.boletin is not null group by 1 having max(c.fecha) < now() order by 2 desc limit 5`;
    (iii) un boletín presente en sesion_tabla_item: `select sti.boletin from sesion_tabla_item sti where sti.boletin is not null limit 5`.
    Anotar en el reporte cada sujeto elegido con su valor exacto + la URL PROD que se visitará (`/agenda?semana=YYYY-Www` y `/proyecto/<boletin>` — confirmar el patrón de ruta real leyendo app/app estructura si difiere). Estos son los inputs deterministas del Task 2.
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select cp.boletin from citacion_punto cp join citacion c on c.id=cp.citacion_id where cp.boletin is not null group by cp.boletin having max(c.fecha) < now() limit 1" && grep -q "Sujetos de prueba" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md"</automated>
  </verify>
  <done>La sección "0. Sujetos de prueba" lista: una semana pasada poblada, un boletín con citación solo-pasada, un boletín en sesion_tabla_item — cada uno con valor exacto y la URL PROD a visitar.</done>
</task>

<task type="auto">
  <name>Task 2: Capturar DOM en PROD con BrowserOS y declarar los gaps</name>
  <files>.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md</files>
  <read_first>scripts/bros-cli.mjs (uso), .planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-RESEARCH.md (§ Evidencia BrowserOS a capturar)</read_first>
  <action>
    Con BrowserOS MCP (take_snapshot antes de leer; re-snapshot tras cada navegación) sobre el deploy PROD real, ejecutar las 4 capturas del bloque <interfaces> usando los sujetos del Task 1. Rate-limit implícito de navegación; NO tocar UI ni escribir nada en el sitio (solo lectura DOM).
    Por cada captura registrar en "1. Capturas /agenda" y "2. Capturas ficha de proyecto": URL visitada, texto/selector DOM capturado (verbatim recortado), count DB esperado (del sujeto) vs lo mostrado, y VEREDICTO (wiring OK / gap de DATOS / gap de WIRING).
    En "3. Gaps declarados" consolidar cada gap con: nombre, evidencia DOM (URL + snippet), count DB que debería aparecer, causa raíz citando el código (ej. `citacionVigente` filtro forward-only estado-actual-block.tsx:122-128), y clasificación DATOS vs WIRING. Confirmar o REFUTAR explícitamente el gap #1 forward-only y el gap #2 sesion_tabla_item con lo OBSERVADO (nota A3: el deploy pudo cambiar — reportar lo real, no lo asumido).
    NO proponer fix (eso es 94). Solo declarar.
  </action>
  <verify>
    <automated>grep -q "Gaps declarados" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md" && grep -qi "citacionVigente\|forward-only" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md" && grep -qi "sesion_tabla_item" ".planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-WIRING-EVIDENCIA.md"</automated>
  </verify>
  <done>Las 4 capturas están registradas con URL PROD + snippet DOM + esperado-vs-mostrado; el gap forward-only de la ficha y el gap sesion_tabla_item están confirmados o refutados con DOM real; cada gap enlaza un sujeto concreto reproducible y su causa raíz en el código. Cero propuestas de fix.</done>
</task>

</tasks>

<verification>
- `93-WIRING-EVIDENCIA.md` existe con secciones 0-3.
- Cada gap tiene evidencia DOM del deploy PROD real (no análisis de código solo).
- Cada gap enlaza un boletín/semana concreto reproducible por 94.
- Ningún fix propuesto (los fixes son 94, regla LOCKED "esta fase NO toca UI").
</verification>

<success_criteria>
- 4 capturas DOM sobre PROD registradas con esperado-vs-mostrado.
- Gap forward-only de la ficha confirmado/refutado con DOM.
- Gap sesion_tabla_item confirmado/refutado con DOM.
- Cada gap reproducible (sujeto concreto) y con causa raíz en el código.
</success_criteria>

<output>
Crear `.planning/phases/93-agenda-p2d-auditor-a-de-cobertura-de-citaciones-gate-duro-/93-02-SUMMARY.md` al terminar.
</output>
