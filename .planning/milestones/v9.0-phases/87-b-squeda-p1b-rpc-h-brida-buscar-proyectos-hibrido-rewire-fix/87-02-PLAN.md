---
phase: 87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/busqueda-hibrida-gate.ts
  - app/lib/busqueda-hibrida-gate.test.ts
  - app/lib/boletin-detector.ts
  - app/lib/boletin-detector.test.ts
  - app/lib/buscar.ts
  - app/lib/buscar.test.ts
  - app/lib/lockdown-guard.test.ts
autonomous: true
requirements: [RETR-01, RETR-05]

must_haves:
  truths:
    - "Con BUSQUEDA_HIBRIDA_ENABLED ausente/false/1/TRUE, buscarProyectos llama match_proyectos (camino actual); solo el literal 'true' enciende la RPC híbrida"
    - "Con el flag ON, buscarProyectos llama buscar_proyectos_hibrido con {q, query_embedding, match_count}"
    - "Una query de boletín en formato punteado (14.309-04) redirige a /proyecto/14309-04 antes de embeber"
    - "buscar_proyectos_hibrido está en PUBLIC_RPC_ALLOWLIST y el guard CI de lockdown pasa"
    - "match_proyectos con excludeBoletin (proyectos similares) queda intacto"
  artifacts:
    - path: "app/lib/busqueda-hibrida-gate.ts"
      provides: "flag server-only fail-closed busquedaHibridaEnabled(env)"
      contains: "BUSQUEDA_HIBRIDA_ENABLED"
    - path: "app/lib/boletin-detector.ts"
      provides: "detectarBoletin puro (3 formatos incl. punteado)"
      contains: "export function detectarBoletin"
    - path: "app/lib/buscar.ts"
      provides: "rewire por flag + redirect extendido a detectarBoletin"
      contains: "busquedaHibridaEnabled"
  key_links:
    - from: "app/lib/buscar.ts"
      to: "buscar_proyectos_hibrido"
      via: "sb.rpc cuando el flag está ON"
      pattern: "rpc\\(\"buscar_proyectos_hibrido\""
    - from: "app/lib/buscar.ts"
      to: "app/lib/boletin-detector.ts"
      via: "detectarBoletin en el redirect (RETR-01, formato punteado)"
      pattern: "detectarBoletin"
    - from: "app/lib/lockdown-guard.test.ts"
      to: "PUBLIC_RPC_ALLOWLIST"
      via: "entrada buscar_proyectos_hibrido"
      pattern: "buscar_proyectos_hibrido"
---

<objective>
Recablear la capa de datos server-only de `/buscar` para que, detrás del flag `BUSQUEDA_HIBRIDA_ENABLED` (fail-closed, default OFF), enrute a la nueva RPC `buscar_proyectos_hibrido` en vez de `match_proyectos`; extender el atajo de boletín para cubrir el formato punteado (`14.309-04`) vía `detectarBoletin`; y registrar la RPC nueva en el `PUBLIC_RPC_ALLOWLIST` del guard CI. Todo con tests OFFLINE (embedder mockeado, `env` inyectado) — sin tocar DB ni red.

Purpose: El flag ON es lo que hace visible al ciudadano el fix del bug estrella; el redirect extendido garantiza que el formato punteado no caiga al camino FTS/semántico (RETR-01). El allowlist entry es obligatorio: sin él, el guard B (`.rpc()` fuera del allowlist) FALLA al recablear buscar.ts.
Output: flag + detector + rewire + allowlist, con suite `app/` verde. El default queda OFF hasta que el gate de dominancia (Plan 03) lo flipee.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-RESEARCH.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-PATTERNS.md
@app/AGENTS.md

<interfaces>
<!-- Contratos verificados file:line — el executor NO explora. -->

Flag server-only (espejo EXACTO app/lib/cruces-gate.ts:37-41):
  import "server-only";
  export function busquedaHibridaEnabled(env = process.env): boolean {
    return env.BUSQUEDA_HIBRIDA_ENABLED === "true";   // solo el literal "true" enciende
  }
  Var SIN prefijo NEXT_PUBLIC_. Test = 5 casos de cruces-gate.test.ts:6-24: ausente→false, "false"→false, "1"→false, "TRUE"→false, "true"→true.

Detector de boletín (fuente pura, espejo packages/fichas/src/spike/boletin.ts:14-26):
  export function detectarBoletin(q: string): { base: string; sufijo: string | null } | null
  Maneja "14309-04"→{base:"14309",sufijo:"04"}, "14309"→{base,sufijo:null}, "14.309-04"→strip puntos→14309-04, distingue decimal (3.14→null).

buscar.ts existente (app/lib/buscar.ts):
  - línea 28: `export const BOLETIN_RE = /^\d{3,6}(-\d{1,2})?$/;` (NO matchea punteado — Pitfall 5).
  - línea 185-187 (aprox): guard `if (BOLETIN_RE.test(q)) redirect('/proyecto/'+q)`.
  - buscarProyectos(qRaw, opts): trimea + cap MAX_QUERY_CHARS (300), embedder inyectable (opts.embedder), createServerSupabase(), sb.rpc("match_proyectos", {...}), throw-on-error, returns MatchProyectoRow[].
  - "proyectos similares" usa match_proyectos con excludeBoletin — INTACTO.

Rewire (insertar tras el embed, espejo 87-RESEARCH Code Examples):
  const sb = createServerSupabase();
  if (busquedaHibridaEnabled()) {
    const { data, error } = await sb.rpc("buscar_proyectos_hibrido", {
      q, query_embedding: emb.vector, match_count: opts.matchCount ?? 20,
    });
    if (error) throw new Error(`buscar_proyectos_hibrido RPC falló: ${error.message}`);
    return (data as MatchProyectoRow[] | null) ?? [];
  }
  // OFF (default): camino match_proyectos SIN CAMBIOS.

Redirect extendido (RETR-01, espejo 87-PATTERNS.md:164-170):
  const bol = detectarBoletin(q);
  if (bol !== null) {
    const full = bol.sufijo !== null ? `${bol.base}-${bol.sufijo}` : bol.base;
    redirect(`/proyecto/${full}`);
  }

Allowlist (app/lib/lockdown-guard.test.ts:165-182 — Set de NOMBRES, no firmas):
  agregar "buscar_proyectos_hibrido" en orden alfabético (entre buscar_citaciones y comparar_declaraciones).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /buscar | `qRaw` es input no confiable; se trimea + capea ≤300; el vector va parametrizado por `.rpc()`, `q` jamás se interpola |
| bundle cliente → módulo server-only | `import "server-only"` impide que el flag y la Gemini key entren al cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-05 | Information Disclosure | flag/embedder en cliente | mitigate | `import "server-only"` en gate y buscar.ts; var sin `NEXT_PUBLIC_`; test offline con `env` inyectado |
| T-87-06 | Tampering | `q` hacia la RPC | mitigate | `.rpc()` parametriza `q` y `query_embedding`; cap MAX_QUERY_CHARS 300 conservado; nunca interpolación |
| T-87-07 | Elevation of Privilege | RPC fuera del allowlist | mitigate | entrada `buscar_proyectos_hibrido` en `PUBLIC_RPC_ALLOWLIST`; guard CI de lockdown pasa; guard A (grant-a-anon) ya cubre 0055 (verificado sin grant en Plan 01) |
| T-87-08 | Denial of Service | query espúrea | mitigate | boletín/punteado redirige ANTES de embeber (no gasta embedding en atajos); `match_count` capeado en la RPC (Plan 01) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag busqueda-hibrida-gate + boletin-detector (módulos puros server-only + tests)</name>
  <files>app/lib/busqueda-hibrida-gate.ts, app/lib/busqueda-hibrida-gate.test.ts, app/lib/boletin-detector.ts, app/lib/boletin-detector.test.ts</files>
  <read_first>
    - app/lib/cruces-gate.ts (espejo del flag; import "server-only", env inyectado)
    - app/lib/cruces-gate.test.ts:6-24 (5 casos exactos del flag)
    - packages/fichas/src/spike/boletin.ts (detectarBoletin puro a portar)
    - packages/fichas/src/spike/boletin.test.ts si existe (casos ya probados a espejar)
  </read_first>
  <behavior>
    - gate: env ausente → false; "false" → false; "1" → false; "TRUE" → false; "true" → true.
    - detector: "14309-04" → {base:"14309",sufijo:"04"}; "14309" → {base:"14309",sufijo:null}; "14.309-04" → {base:"14309",sufijo:"04"}; "14.309" → {base:"14309",sufijo:null}; "3.14" → null; "medio ambiente" → null; "  14309-04  " (con espacios) → {base:"14309",sufijo:"04"}.
  </behavior>
  <action>
    Crear `app/lib/busqueda-hibrida-gate.ts` con `import "server-only"` + `busquedaHibridaEnabled(env = process.env)` que retorna `env.BUSQUEDA_HIBRIDA_ENABLED === "true"` (literal, fail-closed). Var SIN prefijo `NEXT_PUBLIC_`.
    Crear `app/lib/boletin-detector.ts` portando `detectarBoletin` verbatim de `spike/boletin.ts:14-26` (fuente única #36 para el árbol `app/`; el spike conserva su copia). Función pura, sin imports.
    Escribir los tests espejo: `busqueda-hibrida-gate.test.ts` (5 casos con `env` inyectado, sin tocar process.env real) y `boletin-detector.test.ts` (los casos del bloque behavior).
    NOTA sobre `import "server-only"` en tests: `cruces-gate.test.ts` ya resuelve el patrón — replicar su import/mocking exacto para que vitest no rompa con el módulo server-only.
  </action>
  <verify>
    <automated>cd app && pnpm test busqueda-hibrida-gate boletin-detector</automated>
  </verify>
  <acceptance_criteria>
    - `busqueda-hibrida-gate.test.ts` verde: 5 casos, solo "true" enciende.
    - `boletin-detector.test.ts` verde: 3 formatos + decimal(null) + texto libre(null) + trim.
    - `busqueda-hibrida-gate.ts` tiene `import "server-only"` en línea 1 y la var sin `NEXT_PUBLIC_`.
  </acceptance_criteria>
  <done>El flag fail-closed y el detector puro existen con tests offline verdes, espejando cruces-gate y spike/boletin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire buscar.ts (flag → RPC híbrida, redirect extendido) + allowlist entry</name>
  <files>app/lib/buscar.ts, app/lib/buscar.test.ts, app/lib/lockdown-guard.test.ts</files>
  <read_first>
    - app/lib/buscar.ts (íntegro — buscarProyectos, BOLETIN_RE línea 28, redirect ~185-187, camino match_proyectos, embedder inyectable, throw-on-error)
    - app/lib/buscar.test.ts si existe (patrón de mock del embedder + supabase)
    - app/lib/lockdown-guard.test.ts:165-182 (PUBLIC_RPC_ALLOWLIST Set) + :236 (guard grant-a-anon) + :378 (extractor de .rpc('nombre'))
    - app/lib/busqueda-hibrida-gate.ts + app/lib/boletin-detector.ts (creados en Task 1)
  </read_first>
  <behavior>
    - flag OFF (default): buscarProyectos con embedder mockeado llama `sb.rpc("match_proyectos", ...)`, NO `buscar_proyectos_hibrido`.
    - flag ON: buscarProyectos llama `sb.rpc("buscar_proyectos_hibrido", {q, query_embedding, match_count})` y retorna sus filas.
    - flag ON + RPC error: buscarProyectos hace throw (no retorna "sin resultados").
    - redirect: una query `14.309-04` (punteado) dispara `redirect('/proyecto/14309-04')` ANTES de embeber; `medio ambiente` NO redirige.
    - "proyectos similares" (match_proyectos con excludeBoletin) NO cambia de comportamiento.
  </behavior>
  <action>
    En `buscar.ts`: (1) importar `busquedaHibridaEnabled` de `./busqueda-hibrida-gate` y `detectarBoletin` de `./boletin-detector`; (2) reemplazar el guard `if (BOLETIN_RE.test(q)) redirect(...)` por el redirect extendido con `detectarBoletin(q)` (construye `full = base` o `base-sufijo` y redirige a `/proyecto/${full}`) — esto cubre el formato punteado (RETR-01, Pitfall 5); (3) tras el embed, insertar la rama `if (busquedaHibridaEnabled()) { const { data, error } = await sb.rpc("buscar_proyectos_hibrido", { q, query_embedding: emb.vector, match_count: opts.matchCount ?? 20 }); if (error) throw ...; return (data ?? []); }` ANTES del camino `match_proyectos` actual; (4) dejar el camino `match_proyectos` sin cambios como default OFF. CONSERVAR `BOLETIN_RE` exportado (lo importan /buscar y /proyecto/[boletin] — #36) aunque el redirect ya use detectarBoletin; NO borrarlo salvo que ningún consumidor lo use (verificar con grep antes de tocar). NO tocar el camino de "proyectos similares" / excludeBoletin.
    En `lockdown-guard.test.ts`: agregar `"buscar_proyectos_hibrido"` al `PUBLIC_RPC_ALLOWLIST` en orden alfabético (entre `buscar_citaciones` y `comparar_declaraciones`). Solo el NOMBRE, no la firma.
    Escribir/extender `buscar.test.ts` con los 5 comportamientos del bloque behavior (embedder mockeado, `busquedaHibridaEnabled` controlado por `env` inyectado o mock del módulo, supabase `.rpc` espiado, `redirect` de next/navigation mockeado para aserción sin lanzar). Reusar el patrón de mock ya presente en el árbol `app/lib/*.test.ts`.
  </action>
  <verify>
    <automated>cd app && pnpm test buscar lockdown-guard</automated>
  </verify>
  <acceptance_criteria>
    - `buscar.test.ts` verde: OFF→match_proyectos, ON→buscar_proyectos_hibrido, ON+error→throw, punteado→redirect, texto libre→no redirect.
    - `buscar.ts` contiene `busquedaHibridaEnabled` y `detectarBoletin`; la rama ON llama `sb.rpc("buscar_proyectos_hibrido", ...)`.
    - `PUBLIC_RPC_ALLOWLIST` contiene `"buscar_proyectos_hibrido"`; el guard de lockdown pasa entero (guard A grant-a-anon + guard B allowlist).
    - El camino "proyectos similares" (excludeBoletin) no cambió (grep confirma match_proyectos aún presente para similares).
  </acceptance_criteria>
  <done>buscar.ts enruta por flag a la RPC híbrida, el redirect cubre los 3 formatos de boletín, la RPC está en el allowlist, y el guard CI pasa — todo con tests offline. Default OFF (fail-closed) hasta el gate de Plan 03.</done>
</task>

</tasks>

<verification>
- Suite `app/` verde incluyendo flag, detector, rewire y lockdown-guard.
- `tsc --noEmit` sobre `app/` sin errores nuevos.
- Guard CI de lockdown pasa: `buscar_proyectos_hibrido` en el allowlist, sin grant-a-anon en 0055.
</verification>

<success_criteria>
- `busqueda-hibrida-gate.ts` (fail-closed) + `boletin-detector.ts` (3 formatos) + `buscar.ts` recableado + allowlist entry, con suite `app/` verde.
- RETR-01 (redirect punteado) y RETR-05 (flag + allowlist, sin regresión de similares) implementados a nivel app; el default OFF espera el gate de dominancia (Plan 03).
</success_criteria>

<output>
Create `.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-02-SUMMARY.md` when done
</output>
