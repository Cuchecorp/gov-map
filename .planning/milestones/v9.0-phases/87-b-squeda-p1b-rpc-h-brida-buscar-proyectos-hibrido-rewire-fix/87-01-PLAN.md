---
phase: 87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0055_busqueda_hibrida.sql
  - supabase/tests/post-apply/0055_busqueda_hibrida.test.sql
autonomous: true
requirements: [RETR-01, RETR-02, RETR-05]

must_haves:
  truths:
    - "unaccent existe en PROD y la config text-search public.es_unaccent aplica el mismo pipeline en índice y consulta"
    - "buscar_proyectos_hibrido(text,vector,int) existe, devuelve SOLO (boletin,rank), es SECURITY DEFINER con search_path=''"
    - "Un boletín exacto o su boletin_num prefijo sale con rank 0 (tope absoluto, RETR-01)"
    - "PUBLIC no tiene EXECUTE sobre la RPC (anon muerto, doble-revoke, CERO grant)"
    - "match_proyectos (0011) queda intacta"
  artifacts:
    - path: "supabase/migrations/0055_busqueda_hibrida.sql"
      provides: "extension unaccent + f_unaccent IMMUTABLE + config es_unaccent + GIN título + RPC buscar_proyectos_hibrido + ACL doble-revoke"
      contains: "create or replace function public.buscar_proyectos_hibrido"
    - path: "supabase/tests/post-apply/0055_busqueda_hibrida.test.sql"
      provides: "pgTAP post-apply: firma, PUBLIC sin EXECUTE, unaccent presente, es_unaccent existe, short-circuit boletín rank 0"
      contains: "select plan("
  key_links:
    - from: "supabase/migrations/0055_busqueda_hibrida.sql"
      to: "public.proyecto_ficha"
      via: "LEFT JOIN (preserva proyectos sin ficha, buscables por título)"
      pattern: "left join public.proyecto_ficha"
    - from: "supabase/migrations/0055_busqueda_hibrida.sql"
      to: "websearch_to_tsquery"
      via: "config custom schema-qualified public.es_unaccent"
      pattern: "websearch_to_tsquery\\('public.es_unaccent'"
---

<objective>
Materializar en PROD la decisión LOCKED del SPIKE 86 (RRF domina 43.8/68.8/53.6): la migración aditiva `0055` que instala `unaccent`, define la config text-search `public.es_unaccent` (copy=spanish + mapping unaccent), crea el índice GIN de expresión sobre el tsvector del título, y crea la RPC `buscar_proyectos_hibrido` (short-circuit de boletín FUERA del RRF + FTS ponderado A/B/C con LEFT JOIN a ficha + kNN semántico + fusión RRF por rank). Incluye el pgTAP post-apply, la aplicación a PROD por `psql` UTF8 `--single-transaction`, y el reconciliado del ledger `schema_migrations`.

Purpose: Sin esta migración el ciudadano que escribe un boletín o un fragmento literal del título recibe "sin resultados" (el bug estrella). Esta es la aguja de todo camino público nuevo (migración >0044 + RPC definer PII-safe + allowlist), pero el allowlist entry vive en el rewire (Plan 02) y el flip de default en el gate (Plan 03).
Output: `0055_busqueda_hibrida.sql` aplicada a PROD + pgTAP verde + ledger reconciliado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-RESEARCH.md
@.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-PATTERNS.md

<interfaces>
<!-- Schema real verificado file:line en 87-RESEARCH.md — el executor NO necesita explorar. -->

Tabla `proyecto` (0008:19-34): PK `boletin text`, `boletin_num text not null` (base sin sufijo — clave RETR-01), `titulo text not null`, `materia text`, `iniciativa`, `estado`, `etapa`, `autores text[]`.
Tabla `proyecto_ficha` (0011:20-34): PK `boletin` (1:1 con proyecto), `idea_matriz text` (nullable), `cuerpos_legales jsonb` (elementos `{norma, articulos[]}`), `estado`. NO todos los proyectos tienen ficha.
Tabla `proyecto_embedding` (0011): `boletin`, `embedding vector(768)`, índice HNSW cosine. 3100/3659 cubiertos.

Firma RPC objetivo:
  public.buscar_proyectos_hibrido(q text, query_embedding vector(768), match_count int default 20)
  returns table (boletin text, rank int)   -- PII-safe: SOLO (boletin, rank)

Idiom lockdown post-0044 (espejo EXACTO de 0049:55-115):
  language sql stable security definer set search_path = ''
  drop function if exists ... (firma) defensivo antes del create (gotcha 42P13)
  revoke all on function ... from public;
  revoke all on function ... from anon, authenticated;
  CERO grant (service_role ejecuta por bypass).

Config accent-insensitive (una vez, en la migración; STACK.md pattern F.48):
  create extension if not exists unaccent;
  create or replace function public.f_unaccent(text) returns text
    language sql immutable parallel safe strict
    as $$ select public.unaccent('public.unaccent', $1) $$;   -- 2-arg = IMMUTABLE
  create text search configuration public.es_unaccent ( copy = spanish );
  alter text search configuration public.es_unaccent
    alter mapping for hword, hword_part, word with public.unaccent, spanish_stem;

Cuerpo RPC (transcripción del SQL medido en el spike, ver 87-RESEARCH.md Pattern 1 líneas 141-207):
  - boletin_hit: where q ~ '^\d{3,6}(-\d{1,2})?$' and (p.boletin = q or p.boletin_num = split_part(q,'-',1)) → rank 0.
  - full_text: setweight A=titulo / B=idea_matriz / C=string_agg de cuerpos_legales->>'norma', LEFT JOIN proyecto_ficha, @@ websearch_to_tsquery('public.es_unaccent', q), limit least(match_count,50)*2.
  - semantic: row_number over (order by embedding <=> query_embedding), limit least(match_count,50)*2.
  - fused: full outer join full_text/semantic on boletin, RRF 1.0/(50+rank_ix) por rama, w_fts=w_sem=1.
  - salida: boletin_hit primero; si hubo hit, solo ese; si no, fused; order by rank; limit least(match_count,50).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → RPC | El sitio ejecuta la RPC por bypass RLS; el retorno DEBE ser PII-safe por construcción |
| anon → RPC | anon está muerto post-0044; la RPC NO debe re-exponerlo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Elevation of Privilege | RPC ACL | mitigate | doble-revoke `from public` + `from anon, authenticated`, CERO grant; pgTAP post-apply verifica PUBLIC sin EXECUTE (aclexplode grantee=0) |
| T-87-02 | Information Disclosure | retorno RPC | mitigate | returns table (boletin text, rank int) — SOLO 2 columnas, sin PII; la RPC solo lee proyecto/proyecto_ficha/proyecto_embedding (tablas públicas) |
| T-87-03 | Tampering | injection vía `q` | mitigate | `q` entra parametrizado; `websearch_to_tsquery` no interpola; nunca string-building; `search_path=''` + todo schema-qualified |
| T-87-04 | Denial of Service | query cara | mitigate | caps de fila desde el día 1: `least(match_count,50)`, `…*2` por rama; timeout como escape-hatch documentado |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | NO se instala ningún paquete externo (100% SQL + extensiones contrib PG15 ya en Supabase; ver 87-RESEARCH Package Legitimacy Audit "No aplica") |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración 0055 (extension + config + GIN + RPC + ACL) y su pgTAP post-apply</name>
  <files>supabase/migrations/0055_busqueda_hibrida.sql, supabase/tests/post-apply/0055_busqueda_hibrida.test.sql</files>
  <read_first>
    - supabase/migrations/0049_cruces_de_proyecto.sql (idiom RPC definer + search_path='' + doble-revoke + CERO grant, líneas 55-115)
    - supabase/migrations/0032_agenda_search.sql (FTS spanish + websearch_to_tsquery + GIN)
    - supabase/migrations/0011_fichas_embeddings.sql (match_proyectos INTACTA; forma de proyecto_ficha/proyecto_embedding)
    - supabase/migrations/0008_tramitacion.sql:19-34 (proyecto.boletin_num, titulo)
    - supabase/tests/post-apply/0045_revoke_public_rpc_gap.test.sql (pgTAP aclexplode PUBLIC sin EXECUTE)
    - packages/fichas/src/spike/strategies.ts:45-119 (SQL RRF ya medido — fuente conceptual del cuerpo)
    - 87-RESEARCH.md Pattern 1/2/3 + Anti-Patterns; 87-PATTERNS.md secciones 0055
  </read_first>
  <action>
    Crear `0055_busqueda_hibrida.sql` con 5 secciones (per 87-RESEARCH línea 124-134): SECCIÓN 0 comentario-cabecera con orden de apply (patrón 0049); SECCIÓN 1 `create extension if not exists unaccent`; SECCIÓN 2 `f_unaccent` IMMUTABLE 2-arg + config `public.es_unaccent` (copy=spanish, alter mapping for hword/hword_part/word with public.unaccent, spanish_stem); SECCIÓN 3 índice `idx_proyecto_titulo_fts` GIN de expresión sobre `to_tsvector('public.es_unaccent', coalesce(titulo,''))`; SECCIÓN 4 `drop function if exists` defensivo + `create or replace function public.buscar_proyectos_hibrido(q text, query_embedding vector(768), match_count int default 20) returns table (boletin text, rank int) language sql stable security definer set search_path = ''` con el cuerpo transcrito del interfaces block (boletin_hit rank 0, full_text A/B/C con LEFT JOIN proyecto_ficha, semantic kNN, fused RRF rrf_k=50 w=1/1, salida short-circuit-o-fused, caps de fila); SECCIÓN 5 doble-revoke con firma EXACTA `(text, vector, int)`, CERO grant.
    Reglas duras: TODO schema-qualified (`public.proyecto`, `public.proyecto_ficha`, `public.proyecto_embedding`, `public.es_unaccent`) porque `search_path=''`. LEFT JOIN NUNCA INNER (Pitfall 2 — 559 sin ficha buscables por título A). `websearch_to_tsquery('public.es_unaccent', q)` SIEMPRE (jamás `to_tsquery` crudo). `language sql` (NO plpgsql; caps de fila bastan — Pattern 3). NO tocar `match_proyectos`. NO `grant … to anon/public/service_role`. NO columna generada, NO trigger, NO vista materializada (Anti-Patterns).
    Crear `0055_busqueda_hibrida.test.sql` (pgTAP post-apply, vive fuera del glob regular): `begin; select plan(5); … select * from finish(); rollback;` con 5 asserts: (a) `has_function` firma `buscar_proyectos_hibrido(text,vector,int)`; (b) PUBLIC sin EXECUTE vía `aclexplode` con `grantee=0` y `privilege_type='EXECUTE'` = 0 (NO `has_function_privilege('public',…)`); (c) `select 1 from pg_extension where extname='unaccent'`; (d) config `es_unaccent` existe (`pg_ts_config`); (e) short-circuit boletín devuelve el boletín exacto con rank 0 (elegir un boletín REAL existente en PROD para el assert — el executor puede leerlo con un SELECT durante la aplicación en Task 2 y fijarlo). La firma en el `regprocedure` cast DEBE ser exacta `(text,vector,int)` (Pitfall 4).
    NO colocar BOM al inicio del archivo (gotcha 72-APPLY-RUNBOOK).
  </action>
  <verify>
    <automated>grep -q "language sql stable security definer set search_path = ''" "supabase/migrations/0055_busqueda_hibrida.sql" && grep -q "left join public.proyecto_ficha" "supabase/migrations/0055_busqueda_hibrida.sql" && grep -q "websearch_to_tsquery('public.es_unaccent'" "supabase/migrations/0055_busqueda_hibrida.sql" && ! grep -Ei "grant .* to (anon|public|service_role)" "supabase/migrations/0055_busqueda_hibrida.sql" && grep -c "select plan(" "supabase/tests/post-apply/0055_busqueda_hibrida.test.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `0055_busqueda_hibrida.sql` contiene las 5 secciones; la RPC es `language sql stable security definer set search_path = ''`; usa `left join public.proyecto_ficha` (nunca inner); `websearch_to_tsquery('public.es_unaccent', ...)` presente; `match_proyectos` NO aparece modificada.
    - CERO líneas `grant … to anon|public|service_role`; SÍ dos líneas `revoke all … from public` y `… from anon, authenticated` con firma `(text, vector, int)`.
    - `0055_busqueda_hibrida.test.sql` tiene `select plan(5)` con los 5 asserts (has_function, aclexplode PUBLIC=0, unaccent en pg_extension, es_unaccent en pg_ts_config, short-circuit boletín rank 0) y `rollback;` al final.
  </acceptance_criteria>
  <done>La migración y su pgTAP existen, transcriben el SQL medido del spike con el idiom de lockdown, y pasan el grep-gate de seguridad (sin grant, con doble-revoke, LEFT JOIN, config qualified).</done>
</task>

<task type="auto">
  <name>Task 2: Checklist de seguridad + aplicar 0055 a PROD por psql UTF8 + ledger + pgTAP post-apply verde</name>
  <files>supabase/migrations/0055_busqueda_hibrida.sql, supabase/tests/post-apply/0055_busqueda_hibrida.test.sql</files>
  <read_first>
    - 87-CONTEXT.md líneas 44-47 (aplicación a PROD autorizada, migración aditiva NO es checkpoint operador; checklist de seguridad ANTES de aplicar)
    - 87-PATTERNS.md "Ledger de aplicación (psql UTF8, NUNCA db push)"
    - supabase/migrations/0053_* / 0054_* (patrón de reconciliación de schema_migrations más reciente)
    - CLAUDE.md Conventions §Ingesta y Cron (migración por PGCLIENTENCODING=UTF8 psql --single-transaction -f)
  </read_first>
  <action>
    PASO 1 — Checklist de seguridad ANTES de aplicar (gate previo, per CONTEXT línea 46). Verificar sobre el `.sql` final: (1) CERO `grant … to anon/public`; (2) definer con `set search_path=''` y todos los nombres schema-qualified; (3) caps presentes (`least(match_count,50)`, `…*2` por rama); (4) retorno PII-safe `(boletin, rank)`; (5) `drop function if exists` con firma exacta antes del create. Si algún ítem falla → NO aplicar, corregir en Task 1 primero.
    PASO 2 — Aplicar: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0055_busqueda_hibrida.sql` (NUNCA `supabase db push`). `$SUPABASE_DB_URL` de `.env`. La migración es idempotente/aditiva (`create extension if not exists`, `create or replace function`, `create index if not exists`) → segura de re-aplicar salvo el `create text search configuration` (que no es `if not exists`; si ya existe lanza error benigno — envolver la SECCIÓN 2 con guard `do $$ … exception when duplicate_object then null; end $$;` si el executor prefiere idempotencia total, o dejar que --single-transaction aborte limpio la primera vez que no existía).
    PASO 3 — Ledger: `insert into schema_migrations (version) values ('0055_busqueda_hibrida') on conflict do nothing;` (reconciliar como 0053/0054; verificar la forma exacta de la tabla contra la última migración aplicada).
    PASO 4 — pgTAP post-apply contra el schema APLICADO: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0055_busqueda_hibrida.test.sql`. Debe reportar los 5 `ok` y 0 `not ok`. Si el assert (e) del boletín falla por elegir un boletín inexistente, fijar en el test un boletín REAL leído con `select boletin from proyecto limit 1`.
    PASO 5 — Smoke manual de la RPC: `select boletin, rank from public.buscar_proyectos_hibrido('medio ambiente', (select embedding from proyecto_embedding limit 1), 5);` devuelve filas sin error (confirma que la config qualified y el LEFT JOIN no rompen con search_path='').
    NO tocar el WAF ni las fuentes; esta fase solo escribe a Supabase (DDL aditiva).
  </action>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from pg_proc where proname='buscar_proyectos_hibrido';" | grep -qx "1" && PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "select count(*) from pg_extension where extname='unaccent';" | grep -qx "1"</automated>
  </verify>
  <acceptance_criteria>
    - `buscar_proyectos_hibrido` existe en `pg_proc` en PROD (count = 1) y `unaccent` en `pg_extension` (count = 1).
    - El pgTAP post-apply reporta 5/5 `ok`, 0 `not ok`.
    - `schema_migrations` contiene la versión `0055_busqueda_hibrida`.
    - El smoke `select … from buscar_proyectos_hibrido(...)` retorna sin error (config qualified OK bajo search_path='').
    - `match_proyectos` sigue existiendo intacta (count en pg_proc = 1, sin cambio de firma).
  </acceptance_criteria>
  <done>0055 está aplicada a PROD de forma aditiva, el ledger reconciliado, el pgTAP post-apply verde (5/5), la RPC ejecutable, y match_proyectos intacta. La aplicación NO requirió checkpoint de operador (aditiva, autorizada por CONTEXT línea 47).</done>
</task>

</tasks>

<verification>
- La migración pasa el checklist de seguridad (cero grant anon, definer+search_path='', caps, retorno PII-safe) ANTES de aplicar.
- 0055 aplicada a PROD; pgTAP post-apply 5/5 verde contra la DB viva.
- Smoke de la RPC devuelve filas; short-circuit de boletín devuelve rank 0 para un boletín real.
- match_proyectos intacta.
</verification>

<success_criteria>
- `supabase/migrations/0055_busqueda_hibrida.sql` existe, transcribe el SQL RRF medido con el idiom lockdown, y está aplicada a PROD.
- `supabase/tests/post-apply/0055_busqueda_hibrida.test.sql` existe y pasa 5/5 contra PROD.
- Ledger `schema_migrations` reconciliado con `0055_busqueda_hibrida`.
- RETR-01 (short-circuit boletín rank 0), RETR-02 (FTS unaccent título A), RETR-05 (pesos A/B/C + RRF + anon denegado) quedan implementados en SQL — el rewire (Plan 02) y el gate (Plan 03) los cablean y validan end-to-end.
</success_criteria>

<output>
Create `.planning/phases/87-b-squeda-p1b-rpc-h-brida-buscar-proyectos-hibrido-rewire-fix/87-01-SUMMARY.md` when done
</output>
