---
phase: 95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SEC-01]
files_modified:
  - supabase/migrations/0064_bounded_rpc_statement_timeout.sql
  - supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql

must_haves:
  truths:
    - "Las 10 RPCs nuevas de v9.0 (0060/0061/0063) quedan acotadas contra DoS con statement_timeout"
    - "Aplicar 0064 a PROD no aborta (returns tables byte-idénticas → sin 42P13)"
    - "El pgTAP 0064 contra el schema APLICADO reporta N ok, 0 not ok"
    - "Cada RPC re-emitida conserva CERO grant a public/anon (doble-revoke)"
  artifacts:
    - path: "supabase/migrations/0064_bounded_rpc_statement_timeout.sql"
      provides: "Re-emisión de las 10 RPCs con set statement_timeout = '5s' + doble-revoke"
      contains: "set statement_timeout = '5s'"
    - path: "supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql"
      provides: "Prueba pgTAP de bounded-ness contra schema aplicado (has_function + prosecdef + no-anon + PII-safe + statement_timeout)"
      contains: "statement_timeout=%"
  key_links:
    - from: "supabase/migrations/0064_bounded_rpc_statement_timeout.sql"
      to: "pg_proc.proconfig"
      via: "función-atributo set statement_timeout='5s'"
      pattern: "set statement_timeout = '5s'"
    - from: "supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql"
      to: "pg_proc.proconfig (schema APLICADO)"
      via: "cross join lateral unnest(proconfig) … cfg like 'statement_timeout=%'"
      pattern: "statement_timeout=%"
---

<objective>
Acotar contra DoS las 10 RPCs nuevas de v9.0 (0060/0061/0063) que hoy tienen LIMIT pero CERO `statement_timeout`. Bajo Camino A (service_role bypassa RLS), cada RPC ES el boundary de seguridad; un input patológico a una RPC sin timeout es un DoS barato contra la DB del plan Pro. La única mitigación de esta fase es el atributo de función `set statement_timeout = '5s'` (el rate-limit a nivel Worker está diferido, fuera de milestone).

Purpose: Cerrar SC#2 (toda RPC nueva acotada: LIMIT ya presente + `statement_timeout` + cap de `match_count`). El único gap real confirmado es el `statement_timeout` — las 9 RPCs de 0060/0061 y `lobby_menciones_de_boletin` (0063) NO lo tienen; el híbrido (0057) es el ÚNICO que sí.

Output:
- `supabase/migrations/0064_bounded_rpc_statement_timeout.sql` — re-emite las 10 RPCs con el patrón 0057, returns tables BYTE-IDÉNTICAS a 0061/0063, doble-revoke.
- `supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql` — pgTAP post-apply que prueba bounded-ness contra el schema APLICADO.
- 0064 APLICADA a PROD por `psql --single-transaction` + pgTAP verde (N ok, 0 not ok).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-RESEARCH.md
@.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-PATTERNS.md

<interfaces>
<!-- Firmas EXACTAS (final def) que 0064 debe re-emitir BYTE-IDÉNTICAS. Cualquier deriva en la returns table dispara 42P13 y aborta el --single-transaction. Extraídas de las migraciones fuente. -->

De 0060 (bio, language sql, sin total_n):
- parlamentario_publico_v2(p_id text) → (id text, nombre text, camara text, region text, distrito text, circunscripcion text, periodo text, origen text, fecha_captura timestamptz, enlace text, partido text, partido_fecha_captura timestamptz, partido_origen text)
- parlamentarios_publico_v2() → (id text, nombre text, camara text, region text, distrito text, circunscripcion text, periodo text, partido text, partido_fecha_captura timestamptz, partido_origen text)
- militancias_de_parlamentario(p_id text) → (partido text, desde date, hasta date, es_actual boolean, origen text, fecha_captura timestamptz, enlace text)
- comisiones_de_parlamentario(p_id text) → (nombre text, camara text, tipo text, cargo text, origen text, fecha_captura timestamptz, enlace text)

De 0061 (cross-links, FINAL def con total_n bigint — NO usar la de 0060):
- copartidarios_de_parlamentario(p_id text) → (id text, nombre text, camara text, total_n bigint)
- de_la_misma_zona(p_id text) → (id text, nombre text, camara text, total_n bigint)
- co_comisionados_de_parlamentario(p_id text) → (id text, nombre text, camara text, comision_nombre text, total_n bigint)
- coautores_de_parlamentario(p_id text) → (id text, nombre text, camara text, n_proyectos int, total_n bigint)

De 0063 (lobby, FINAL def, 13 cols con contraparte_rol/representado NULL por compat):
- lobby_menciones_de_boletin(p_boletin text) → (identificador text, fecha timestamptz, materia text, parlamentario_id text, parlamentario_nombre text, contraparte_nombre text, contraparte_rol text, representado text, enlace_detalle text, origen text, fecha_captura timestamptz, enlace text, total_n bigint)

DECISIÓN DE DISCRECIÓN (Claude, per CONTEXT §Claude's Discretion + RESEARCH A4):
- match_proyectos NO se incluye en 0064. Razón: (1) está definida SIN el qualifier `public.` (0011:55 `create or replace function match_proyectos(...)`), es `security invoker` (secdef=f, NO definer) y fue revocada de public en 0045 — re-emitirla con timeout exige un re-emit byte-idéntico de una función security-INVOKER (más riesgo 42P13 por marginal ganancia); (2) es pre-v9.0 (fuera del set "RPC nueva" de la fase); (3) su LIMIT/threshold ya la acotan. El híbrido (0057) ya la reemplaza tras flag. Documentar esta exclusión en el header de 0064.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 0064_bounded_rpc_statement_timeout.sql (10 RPCs, returns byte-idénticas + doble-revoke)</name>
  <files>supabase/migrations/0064_bounded_rpc_statement_timeout.sql</files>
  <read_first>
    - supabase/migrations/0057_busqueda_hibrida_statement_timeout.sql — TEMPLATE EXACTO del idiom: header (líneas 1-16), drop→create-or-replace con `set search_path = ''` + `set statement_timeout = '5s'` como DOS opciones de función en líneas separadas (27-28), doble-revoke (120-124). OJO: 0057 es `language plpgsql`; las 10 RPCs objetivo son `language sql` → mantener `language sql stable security definer`.
    - supabase/migrations/0060_bio_partido_publico.sql — cuerpos VERBATIM de parlamentario_publico_v2 (50-80), militancias_de_parlamentario (90-104), comisiones_de_parlamentario (114-129), parlamentarios_publico_v2 (143-168). Copiar el body EXACTO (el `as $$ … $$` completo), solo añadiendo la línea `set statement_timeout = '5s'` al header de la función.
    - supabase/migrations/0061_cross_links_conteo_honesto_orden.sql — cuerpos VERBATIM de los 4 cross-links (copartidarios 46-74, de_la_misma_zona 82-104, co_comisionados 112-141, coautores 149-176). ESTA es la FINAL def (con `total_n bigint`), NO la de 0060 — reproducir la returns table de 0061 byte-idéntica.
    - supabase/migrations/0063_lobby_menciones_una_fila_por_audiencia.sql — cuerpo VERBATIM de lobby_menciones_de_boletin (52-156); returns table de 13 cols (55-69). Copiar el body completo incluyendo el CTE req/pat + agregación de contrapartes.
    - .planning/phases/95-.../95-PATTERNS.md §"0064 migration" (líneas 22-77) — header pattern, drop+create-or-replace pattern, ACL doble-revoke, key distinctions de 0057.
  </read_first>
  <action>
    Crear `supabase/migrations/0064_bounded_rpc_statement_timeout.sql`. Header comment idiom del proyecto (espejo 0060/0061/0063): número/fase/qué cambia (añade statement_timeout='5s' a las 10 RPCs de 0060/0061/0063), orden de apply (DESPUÉS de 0063), comando `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0064_bounded_rpc_statement_timeout.sql`, "NUNCA supabase db push", "ADITIVA (create or replace + revokes; sin DROP de tabla, sin backfill) → autoridad del agente (precedente 0055-0063)", "la única prueba válida del DDL es el pgTAP contra schema APLICADO (Pitfall 6)". Documentar en el header por qué `match_proyectos` se EXCLUYE (ver <interfaces>).

    Por CADA una de las 10 RPCs, en este orden (bio primero, cross-links, lobby):
    1. `drop function if exists public.<name>(<args-exactos>);`
    2. `create or replace function public.<name>(<args-exactos>) returns table ( <returns table BYTE-IDÉNTICA de la migración fuente> ) language sql stable security definer set search_path = '' set statement_timeout = '5s' as $$ <BODY VERBATIM de la migración fuente> $$;` — las opciones `set search_path = ''` y `set statement_timeout = '5s'` van como DOS líneas de atributo separadas (espejo 0057:27-28). NO usar `set local` dentro del body (no aterriza en proconfig → el pgTAP fallaría).
    3. Doble-revoke VERBATIM: `revoke all on function public.<name>(<args>) from public;` y `revoke all on function public.<name>(<args>) from anon, authenticated;`. CERO grant.

    Firmas/args exactos por RPC (todas `(text)` salvo `parlamentarios_publico_v2()` sin args): usar las de <interfaces>. Para los 4 cross-links usar la returns table de 0061 (con `total_n bigint`), NO la de 0060.

    Regla de estabilidad de firma (0060/0061 headers): como 0064 NO cambia ninguna returns table, es un cambio body-only; el `drop function if exists` previo es defensivo pero el body/returns deben ser byte-idénticos a la fuente para no disparar 42P13 en medio del `--single-transaction`.
  </action>
  <acceptance_criteria>
    - El archivo `supabase/migrations/0064_bounded_rpc_statement_timeout.sql` existe.
    - `grep -c "set statement_timeout = '5s'"` sobre el archivo (excluyendo comentarios: `grep -v '^--' | grep -c "set statement_timeout = '5s'"`) == 10 (una por RPC).
    - `grep -v '^--' | grep -c "create or replace function public\."` == 10.
    - `grep -v '^--' | grep -c "revoke all on function public\."` == 20 (doble-revoke × 10).
    - CERO `grant ` (fuera de comentarios) en el archivo: `grep -v '^--' | grep -ci '\bgrant\b'` == 0.
    - Las returns tables de los 4 cross-links contienen `total_n bigint` (no la firma de 0060). `grep -c "total_n bigint"` >= 4.
    - NINGUNA aparición de `set local` en el archivo.
    - `match_proyectos` NO aparece en ninguna sentencia `create` (solo puede mencionarse en el header como exclusión documentada).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0064_bounded_rpc_statement_timeout.sql','utf8').split('\n').filter(l=>!l.trimStart().startsWith('--')).join('\n'); const c=(re)=>(s.match(re)||[]).length; const to=c(/set statement_timeout = '5s'/g), cr=c(/create or replace function public\./g), rv=c(/revoke all on function public\./g), gr=c(/\bgrant\b/gi), tn=c(/total_n bigint/g), sl=c(/set local/g); if(to!==10||cr!==10||rv!==20||gr!==0||tn<4||sl!==0){console.error('FAIL',{to,cr,rv,gr,tn,sl});process.exit(1)} console.log('OK 10 timeouts / 10 defs / 20 revokes / 0 grants / total_n>=4 / 0 set-local')"</automated>
  </verify>
  <done>0064 escrita con las 10 RPCs re-emitidas: cada una con `set statement_timeout = '5s'`, returns table byte-idéntica a su fuente (0061 para cross-links), doble-revoke, CERO grant, sin `set local`. `match_proyectos` excluida y documentada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escribir el pgTAP post-apply 0064 (has_function + prosecdef + no-anon + PII-safe + statement_timeout por RPC)</name>
  <files>supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql</files>
  <behavior>
    - Test A (por RPC): `has_function('public', '<name>', <array de arg types>, msg)` — la función existe tras 0064.
    - Test B (por RPC): PUBLIC sin EXECUTE — `aclexplode(proacl) where grantee=0 and privilege_type='EXECUTE'` cuenta 0 (idiom 0057:21-29, NO `has_function_privilege`).
    - Test C (por RPC): `statement_timeout` seteado — `exists(… unnest(proconfig) cfg where cfg like 'statement_timeout=%')` (idiom 0057:32-41 VERBATIM) — ESTE es el core SC#2.
    - Test D (por RPC): PII-safe — `pg_get_function_result(<regprocedure>) !~* '\y(rut|email|partido_alias)\y'`. `partido` (sin `_alias`) NO se bloquea (dato público del cargo electo, decisión operador 2026-07-21).
    - `plan(N)` con N = número total de asserts (4 por RPC × 10 = 40).
  </behavior>
  <read_first>
    - supabase/tests/post-apply/0057_busqueda_hibrida_statement_timeout.test.sql — TEMPLATE EXACTO: begin/plan/finish/rollback (9-10, 63-64), has_function con `array['text','vector','integer']` (13-18), no-anon vía aclexplode+grantee=0 (21-29), statement_timeout via unnest(proconfig) cfg like 'statement_timeout=%' (32-41). Copiar estos idioms verbatim, adaptando nombre/args por RPC.
    - .planning/phases/95-.../95-PATTERNS.md §"0064 pgTAP post-apply" (líneas 81-159) — header pattern, has_function assert, no-anon aclexplode assert, statement_timeout assert VERBATIM, PII-safe assert, formato del cast regprocedure (parlamentarios_publico_v2() sin args = `'public.parlamentarios_publico_v2()'::regprocedure`; usar `text`/`integer` no `int`).
    - supabase/tests/post-apply/0055 y 0056 test (idiom PII-safe `pg_get_function_result` si existe) — para el patrón exacto del assert D.
  </read_first>
  <action>
    Crear `supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql`. Header (POST-APPLY ONLY, comando `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql`, "Debe reportar 40 ok, 0 not ok", "corre DESPUÉS de aplicar 0064 a PROD").

    `begin; select plan(40);` luego 4 asserts (A/B/C/D) por cada una de las 10 RPCs. `select * from finish(); rollback;`.

    Casts regprocedure exactos: todas `<name>(text)` salvo `parlamentarios_publico_v2()`. Usar `'public.<name>(text)'::regprocedure` (o `()` para la sin-args). `has_function` con `array['text']` (o `'{}'::text[]` para la sin-args).

    Assert C (statement_timeout) VERBATIM de 0057:32-41 por RPC — es la prueba central de SC#2.
    Assert D (PII-safe): la regex debe bloquear `rut|email|partido_alias` pero NO `partido` suelto. Verificar que `\y…\y` (word boundary) sobre `partido_alias` no matchea `partido` (matchea porque `_` no es boundary… OJO): usar `\y(rut|email|partido_alias)\y` — `partido_alias` como token completo no matchea la col `partido` (que es token distinto). Confirmar leyendo el idiom de 0055 si existe.
  </action>
  <acceptance_criteria>
    - El archivo `supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql` existe.
    - `select plan(N)` con N == número de asserts (40 si 4×10). Verificable: contar `has_function` == 10, asserts `statement_timeout=%` == 10, asserts `aclexplode` grantee=0 == 10, asserts `pg_get_function_result` PII == 10.
    - Contiene `cfg like 'statement_timeout=%'` al menos 10 veces (statement_timeout=% aparece 10×).
    - Contiene `finish()` y `rollback;` (no commit).
    - Los 10 nombres de RPC de <interfaces> aparecen cada uno en al menos un `has_function`.
    - La regex PII bloquea `partido_alias` pero el string `partido text` como columna NO dispara falso positivo (assert D usa `partido_alias` como alternativa, no `partido`).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql','utf8'); const c=(re)=>(s.match(re)||[]).length; const hf=c(/has_function\(/g), st=c(/statement_timeout=%/g), ac=c(/aclexplode/g), pii=c(/pg_get_function_result/g); const names=['parlamentario_publico_v2','parlamentarios_publico_v2','militancias_de_parlamentario','comisiones_de_parlamentario','copartidarios_de_parlamentario','de_la_misma_zona','co_comisionados_de_parlamentario','coautores_de_parlamentario','lobby_menciones_de_boletin']; const missing=names.filter(n=>!s.includes(n)); const fin=/finish\(\)/.test(s)&&/rollback;/.test(s); if(hf<10||st<10||ac<10||pii<10||missing.length||!fin){console.error('FAIL',{hf,st,ac,pii,missing,fin});process.exit(1)} console.log('OK 10 has_function / 10 timeout asserts / 10 aclexplode / 10 PII / finish+rollback / all names present')"</automated>
  </verify>
  <done>pgTAP 0064 escrito: 40 asserts (A/B/C/D × 10 RPCs), plan(40), statement_timeout assert VERBATIM de 0057 por RPC, no-anon vía aclexplode, PII-safe que NO bloquea `partido`, cerrado con finish()+rollback.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Aplicar 0064 a PROD por psql --single-transaction + correr pgTAP contra schema aplicado</name>
  <files>supabase/migrations/0064_bounded_rpc_statement_timeout.sql, supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql</files>
  <read_first>
    - .planning/phases/95-.../95-PATTERNS.md §"Shared Patterns" (líneas 267-303) — comando de apply LOCKED, runner pgTAP LOCKED, BOM en .env → --db-url explícito.
    - .planning/STATE.md líneas 126-133 — precedente de apply de 0059/0060/0062 a PROD por el agente (pgTAP 28/28, 30/30, 13/13); esto NO es checkpoint de operador (DDL aditivo de RPCs públicas ya-servidas).
    - 95-RESEARCH.md §"pgTAP: cómo se corre aquí + precedente de apply DDL a PROD" (líneas 301-307).
  </read_first>
  <action>
    Precedente LOCKED (STATE.md 0059-0063): DDL aditivo de RPCs públicas ya-servidas = autoridad del agente, NO checkpoint de operador. Aplicar 0064 a PROD.

    1. (Opcional, si scratch DB local es barato) validar primero contra un pg efímero local con `begin/rollback`; pero el apply a PROD está EN ALCANCE por precedente.
    2. Apply LOCKED (NUNCA `supabase db push`; BOM en .env → pasar `$SUPABASE_DB_URL` explícito): `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0064_bounded_rpc_statement_timeout.sql`. Debe terminar sin error (si algún returns table derivó → 42P13 aborta y el `--single-transaction` hace rollback: corregir la returns table en Task 1 y reintentar).
    3. Correr el pgTAP post-apply contra el schema APLICADO: `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql`. EXIGIR salida "N ok, 0 not ok" (N=40) — cero `not ok`.

    Si el psql client / SUPABASE_DB_URL no están disponibles en el entorno de ejecución, marcar este task como checkpoint bloqueante para el operador con el comando exacto y detenerse (no declarar SC#2 cerrado sin el pgTAP verde contra PROD aplicado).
  </action>
  <acceptance_criteria>
    - El comando de apply termina sin error (sin 42P13, sin abort del `--single-transaction`).
    - El pgTAP post-apply imprime la línea de resumen con "0 not ok" (todos ok; N == 40 asserts).
    - NUNCA se usó `supabase db push` ni `supabase test db` (solo `psql -f` para apply y `psql -tA -f` para pgTAP).
    - build/typecheck NO se aceptan como prueba del apply (Pitfall 6): la ÚNICA prueba válida es el pgTAP contra el schema aplicado.
  </acceptance_criteria>
  <verify>
    <automated>PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql 2>&1 | grep -q 'not ok' && { echo 'FAIL: hay not ok'; exit 1; } || echo 'OK: 0 not ok contra schema aplicado'</automated>
  </verify>
  <done>0064 aplicada a PROD por `psql --single-transaction` sin 42P13; pgTAP post-apply reporta 40 ok, 0 not ok contra el schema APLICADO. SC#2 (bounded RPCs) probado en vivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → RPC (service_role) | El sitio ejecuta con service_role que BYPASSA RLS/ACL — el cuerpo de la RPC es el ÚNICO punto de enforcement. Input `p_id`/`p_boletin` es no confiable (repo público, sujetos hostiles). |
| migración → pg_proc (PROD) | El DDL aplicado por el agente cambia el catálogo vivo; una returns-table derivada corrompe el apply (42P13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Denial of Service | Las 10 RPCs 0060/0061/0063 sin `statement_timeout` | mitigate | `set statement_timeout = '5s'` (atributo de función, aterriza en proconfig) por RPC en 0064; LIMIT ya presente (20/50/single-row); probado por pgTAP Test C |
| T-95-02 | Elevation of Privilege | Re-emitir un grant a anon/public al re-crear la RPC re-abriría la superficie REST no autenticada | mitigate | Doble-revoke VERBATIM (`from public;` + `from anon, authenticated;`) por RPC; CERO grant; el lockdown-guard Block A (Plan 02) caza cualquier grant >0044 |
| T-95-03 | Tampering | Deriva de returns-table en 0064 vs 0061/0063 → 42P13 mid-transaction, PROD en estado a medias | mitigate | Returns tables byte-idénticas a 0061 (con total_n) / 0063 (13 cols); `--single-transaction` hace rollback ante 42P13; pgTAP Test A (has_function con arg types) confirma la firma |
| T-95-04 | Information Disclosure | Una RPC re-emitida podría proyectar PII (rut/email/partido_alias) por error de copy | mitigate | Body VERBATIM de la fuente (ya PII-safe); pgTAP Test D `pg_get_function_result !~* rut/email/partido_alias`; `partido` NO es PII (decisión operador 2026-07-21) |
| T-95-05 | Tampering | `set local statement_timeout` dentro del body en vez del atributo → no aterriza en proconfig, DoS cap ausente pese a "verse" bien | mitigate | Usar el atributo de función `set statement_timeout = '5s'` (espejo 0057); pgTAP Test C valida `proconfig like 'statement_timeout=%'` (falla si se usó `set local`) |
| T-95-SC | Tampering | npm/pip/cargo installs | mitigate | Esta fase instala CERO paquetes (RESEARCH §Standard Stack: "installs zero external packages"); no aplica audit de legitimidad |
</threat_model>

<verification>
- `supabase/migrations/0064_bounded_rpc_statement_timeout.sql` existe con 10 RPCs re-emitidas (10 statement_timeout, 20 revokes, 0 grants).
- `supabase/tests/post-apply/0064_bounded_rpc_statement_timeout.test.sql` existe con 40 asserts (4×10).
- 0064 APLICADA a PROD sin 42P13; pgTAP post-apply reporta 40 ok, 0 not ok contra schema aplicado.
- `tsc --noEmit` no aplica a SQL; la suite de app no cambia en este plan (Plan 02 toca los guards). No se regresiona ningún pgTAP existente (0055-0057/0060/0062 intactos; 0064 es net-new).
</verification>

<success_criteria>
- SC#2 (bounded RPCs) cerrado: las 10 RPCs nuevas de v9.0 tienen `statement_timeout` + LIMIT probado por pgTAP contra el schema APLICADO (40 ok, 0 not ok).
- Cero regresión: returns tables byte-idénticas → sin 42P13; doble-revoke → CERO grant preservado.
- Apply por idiom LOCKED (`psql --single-transaction`), nunca `db push`; pgTAP por `psql -tA -f`, nunca `supabase test db`.
</success_criteria>

<output>
Crear `.planning/phases/95-seguridad-p3a-guards-extendidos-sobre-rpcs-nuevas-bounded-rp/95-01-SUMMARY.md` al terminar.
</output>
