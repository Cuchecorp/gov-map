---
phase: 92-personas-p2c-lobby-legible-audiencia-pl-fail-closed
plan: 04
type: execute
wave: 4
depends_on: ["92-01", "92-02", "92-03"]
files_modified:
  - .planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-04-APPLY-RUNBOOK.md
  - .planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-BROWSEROS-GATE.md
autonomous: true
requirements: [LOB-01, LOB-02, LOB-03]
user_setup: []
must_haves:
  truths:
    - "La migración 0062 queda aplicada a PROD por psql --single-transaction (nunca db push) y el pgTAP pasa contra el schema APLICADO"
    - "El deploy Cloudflare está LIVE y arrastra además los fixes UI de 91 que quedaron fuera del bundle e0c969af"
    - "El gate BrowserOS confirma empíricamente sobre el deploy real: materia completa visible, chips de mención operando, sección en ficha proyecto con leyenda"
    - "La cobertura de menciones en PROD está MEDIDA y DECLARADA (cuántas audiencias tienen mención válida de boletín)"
  artifacts:
    - path: ".planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-04-APPLY-RUNBOOK.md"
      provides: "Runbook operador de apply 0062 + pgTAP + medición de cobertura"
      min_lines: 20
    - path: ".planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-BROWSEROS-GATE.md"
      provides: "Evidencia empírica del gate BrowserOS sobre el deploy real"
      min_lines: 15
  key_links:
    - from: "supabase/migrations/0062_lobby_menciones_de_boletin.sql"
      to: "PROD (SUPABASE_DB_URL)"
      via: "psql --single-transaction (por el AGENTE — precedente 0059/0060/0061, DDL aditivo deny-by-default)"
      pattern: "single-transaction"
---

<objective>
Cierre de la fase: aplicar 0062 a PROD (por el AGENTE vía psql --single-transaction, precedente 0059-0061), correr el pgTAP
contra el schema aplicado, MEDIR y DECLARAR la cobertura de menciones válidas en PROD,
desplegar a Cloudflare (arrastrando los fixes UI de 91 aún sin bundle) y correr el gate
BrowserOS empírico sobre el deploy real.

Purpose: la fase entrega dato LIVE. El apply DDL a PROD y el crawl/deploy son actos con
efecto en producción — 0062 es aditiva deny-by-default, DENTRO de la autoridad del agente (precedente 0059-0061); el deploy también lo ejecuta el agente (precedente 91-03/quick-260722). El gate BrowserOS es el veredicto
empírico LOCKED del ciclo por producto (v9.0).

Output: runbook de apply + pgTAP + cobertura declarada; evidencia del gate BrowserOS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-CONTEXT.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-01-SUMMARY.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-02-SUMMARY.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-03-SUMMARY.md

<interfaces>
<!-- Patrón de apply LOCKED (precedente 0059/0060/0061). -->
Comando de apply (VERBATIM, UNA vez):
```
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f \
  supabase/migrations/0062_lobby_menciones_de_boletin.sql
```
pgTAP contra schema APLICADO:
```
PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f \
  supabase/tests/0062_lobby_menciones_de_boletin.test.sql
```
<!-- Deploy: build OpenNext en Docker Linux (Windows rompe worker); wrangler local (OAuth,
     CI sin creds CF). Precedente 89-03 deploy e0c969af / 91-03. -->
<!-- Dato PROD: 5.106 audiencias confirmadas; materia verbatim poblada. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar 0062 a PROD + pgTAP + medir cobertura de menciones (operador)</name>
  <files>.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-04-APPLY-RUNBOOK.md</files>
  <action>El agente escribe `92-04-APPLY-RUNBOOK.md` con: el comando exacto de apply
    (`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f
    supabase/migrations/0062_lobby_menciones_de_boletin.sql`, NUNCA `supabase db push`), el comando
    pgTAP contra el schema aplicado, y una query de cobertura que cuenta, sobre las ~5.106
    audiencias confirmadas de PROD, cuántas tienen al menos una mención VÁLIDA de boletín (patrón +
    existencia en proyecto) — el dato de cobertura honesto a declarar en el SUMMARY. El apply DDL a
    PROD tiene efecto en producción; 0062 es aditiva deny-by-default → el AGENTE la aplica (precedente 0059-0061), UNA vez, y corre pgTAP contra el schema aplicado.</action>
  <what-built>Runbook con comando de apply psql --single-transaction, comando pgTAP contra schema
    aplicado, y query de cobertura de menciones válidas sobre PROD.</what-built>
  <how-to-verify>
    1. `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0062_lobby_menciones_de_boletin.sql` (UNA vez).
    2. `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -f supabase/tests/0062_lobby_menciones_de_boletin.test.sql` → todos `ok`, 0 `not ok`.
    3. Correr la query de cobertura del runbook → anotar N audiencias con mención válida / total confirmado.
  </how-to-verify>
  <verify>
    <automated>MISSING — apply DDL a PROD es acto de operador (blocking-human); la verificación es el pgTAP contra schema aplicado (paso 2) + la query de cobertura (paso 3), reportados por el operador.</automated>
  </verify>
  <done>0062 aplicada UNA vez por psql --single-transaction; pgTAP verde (0 not ok) contra schema aplicado; cobertura de menciones válidas medida y anotada.</done>
  <resume-signal>"aplicado" (con pgTAP verde + N de cobertura) o describir el fallo.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy Cloudflare (arrastra fixes UI 91) + gate BrowserOS empírico</name>
  <files>.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-BROWSEROS-GATE.md</files>
  <action>El agente prepara el build OpenNext (Docker Linux) y documenta el gate en
    `92-BROWSEROS-GATE.md`. El deploy a Cloudflare (wrangler local, OAuth) arrastra además los fixes
    UI de 91 que quedaron fuera del bundle e0c969af. Tras el deploy, cold-read BrowserOS sobre el
    deploy REAL verificando los tres hechos de la fase. El deploy a producción es acto de operador
    (creds CF no están en CI) → deploy LOCAL por el agente vía wrangler OAuth (precedente 91-03).</action>
  <what-built>Build OpenNext (Docker Linux) listo; documento de gate BrowserOS; el deploy wrangler
    arrastra los fixes UI de 91 aún sin bundle.</what-built>
  <how-to-verify>
    1. Build OpenNext en Docker Linux + deploy wrangler local → anotar la versión desplegada.
    2. BrowserOS sobre una ficha de parlamentario con lobby: la materia completa de las audiencias
       es visible y legible en AMBAS vistas (agrupada y cronológica), sin recorte; los chips
       "Menciona boletín N" aparecen bajo la materia cuando corresponde y navegan a /proyecto/N.
    3. BrowserOS sobre una ficha de proyecto mencionado: la sección "Audiencias de lobby que
       mencionan este boletín" aparece SEPARADA de "Lobby del período" (0048), con su leyenda
       anti-causal, parlamentario enlazado y conteo honesto.
    4. Registrar evidencia (versión + hallazgos) en `92-BROWSEROS-GATE.md`.
  </how-to-verify>
  <verify>
    <automated>MISSING — deploy a producción es acto de operador (blocking-human); la verificación es el cold-read BrowserOS sobre el deploy real (pasos 2-3), reportado por el operador.</automated>
  </verify>
  <done>Deploy Cloudflare LIVE (versión anotada, incluye fixes UI 91); BrowserOS confirma materia completa visible + chips operando + sección con leyenda; evidencia en 92-BROWSEROS-GATE.md.</done>
  <resume-signal>Escribir "comprensible" (con la versión desplegada) o listar los puntos fallidos.</resume-signal>
</task>

</tasks>

<verification>
- 0062 aplicada por psql --single-transaction (nunca db push); pgTAP verde contra schema aplicado.
- Cobertura de menciones válidas en PROD medida y declarada (dato honesto).
- Deploy Cloudflare LIVE incluyendo los fixes UI de 91.
- Gate BrowserOS: materia completa visible, chips operando, sección con leyenda — veredicto empírico.
</verification>

<success_criteria>
- 0062 en PROD + pgTAP verde; cobertura declarada.
- Deploy live; gate BrowserOS aprobado sobre el deploy real.
- LOB-01/LOB-02/LOB-03 verificados empíricamente en producción.
</success_criteria>

<output>
Create `.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-04-SUMMARY.md` when done.
</output>


<!-- PLAN-CHECKER FIX (MINOR 8): la query SQL de cobertura de menciones va VERBATIM en el ledger/SUMMARY para reproducibilidad. -->
