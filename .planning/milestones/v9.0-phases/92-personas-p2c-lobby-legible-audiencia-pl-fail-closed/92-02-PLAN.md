---
phase: 92-personas-p2c-lobby-legible-audiencia-pl-fail-closed
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - app/components/mencion-boletin-chip.tsx
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
autonomous: true
requirements: [LOB-01, LOB-02, LOB-03]
must_haves:
  truths:
    - "La materia COMPLETA de cada audiencia es visible y legible en AMBAS vistas (cronológica y agrupada) de la ficha del parlamentario — sin line-clamp/truncate, whitespace-pre-line, leading-relaxed"
    - "La vista agrupada muestra la materia por reunión (hoy no lo hace)"
    - "Cada audiencia cuya materia menciona boletín(es) válido(s) muestra chip-link 'Menciona boletín N' → /proyecto/N, en ambas vistas; fail-closed doble (patrón + existencia)"
    - "El chip navega a la ficha del proyecto (navegación bidireccional audiencia→PL); LobbyView sigue PURO"
  artifacts:
    - path: "app/components/lobby-de-parlamentario.tsx"
      provides: "Materia legible en ambas vistas + chips de mención server-computados"
      contains: "whitespace-pre-line"
    - path: "app/components/mencion-boletin-chip.tsx"
      provides: "Chip-link 'Menciona boletín N' reutilizable"
      min_lines: 20
  key_links:
    - from: "app/components/lobby-de-parlamentario.tsx"
      to: "app/lib/boletin-en-materia.ts"
      via: "extraerBoletines sobre a.materia en el server component"
      pattern: "extraerBoletines"
    - from: "app/components/lobby-de-parlamentario.tsx"
      to: "public.proyecto"
      via: "batched .in('boletin', …) para validar existencia server-side"
      pattern: "\\.from\\(\"proyecto\"\\)"
    - from: "app/components/mencion-boletin-chip.tsx"
      to: "/proyecto/{N}"
      via: "next/link href"
      pattern: "/proyecto/"
---

<objective>
Hacer LEGIBLE la materia (falla presentacional) en AMBAS vistas de la sección de lobby
de la ficha del parlamentario, y montar el chip-link "Menciona boletín N" (fail-closed
doble) calculado server-side sobre las filas que ya devuelve `lobby_de_parlamentario`.

Purpose: LOB-01 (materia completa visible/legible), LOB-02 (chip solo por mención
explícita de boletín válido) y LOB-03 (navegación audiencia→PL) en la superficie del
parlamentario. La materia YA está entera en DB — el dato NO se re-ingiere; solo cambia
el tratamiento tipográfico. El chip llega ya resuelto en los datos serializados a la
vista pura (Block-B: `proyecto` no es PII, se lee server-side; ningún island toca .rpc/.from).

Output: `lobby-de-parlamentario.tsx` con materia legible + chips; chip reutilizable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-CONTEXT.md
@.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-UI-SPEC.md

<interfaces>
<!-- Del Plan 01 (dependencia dura). -->
From app/lib/boletin-en-materia.ts:
```typescript
export function extraerBoletines(materia: string | null): string[]; // canónicos, dedupe, asc
```

<!-- Componente EXISTENTE a modificar. LobbyView es PURO (props); LobbySection es el
     Server Component que lee el RPC + createServerSupabase. -->
From app/components/lobby-de-parlamentario.tsx:
```typescript
export interface LobbyViewData { id; audiencias: LobbyAudienciaRow[]; grupos: GrupoContraparte[]; totalAudiencias; page; totalPages; vista; noIngestado; camara? }
export interface GrupoContraparte { contraparte: string; n: number; fechas: string[] }
export function agruparPorContraparte(audiencias): GrupoContraparte[]; // HOY no lleva materia
export function LobbyView({ data }): JSX;                 // PURO
export async function LobbySection({ id, searchParams, camara }): JSX; // lee lobby_de_parlamentario
// VistaCronologica muestra {a.materia} en un <span> inline (líneas ~445-450) → cambiar a bloque whitespace-pre-line
// VistaAgrupada muestra solo contraparte + conteo + fechas (NO materia) → añadir materia por reunión
```

From app/lib/types.ts:
```typescript
export interface LobbyAudienciaRow { identificador; fecha; fecha_raw; materia: string | null; contrapartes; origen; fecha_captura; enlace }
```

<!-- Chip de referencia (estructura Badge outline). El chip NUEVO es un LINK (navega). -->
<!-- PartidoChip / chips cívicos: Badge outline; NO anidar TooltipTrigger dentro de un Link (WR-04). -->
</interfaces>

@app/components/lobby-de-parlamentario.tsx
@app/lib/boletin-en-materia.ts
@app/components/lobby-en-tramitacion.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chip-link "Menciona boletín N" (reutilizable, fail-closed, accesible)</name>
  <files>app/components/mencion-boletin-chip.tsx</files>
  <action>Crear `MencionBoletinChip` — un `<Link href="/proyecto/{N}">` que envuelve un
    `<Badge variant="outline">` (asChild) con fondo neutro/petróleo-soft, borde/texto vía la
    utilidad registrada `accent-product`/`text-accent-product` (NUNCA hsl() a mano; guard
    cero-hex), `min-h-11` táctil, `focus-visible:outline-2 focus-visible:outline-offset-2
    focus-visible:outline-accent-product`. Contenido: texto `Menciona boletín ` (Sans) + `{N}`
    en `font-mono`. `aria-label` LOCKED verbatim del UI-SPEC: `Esta materia menciona el boletín
    {N}; abre el proyecto.`. NO anidar TooltipTrigger dentro del Link (WR-04, HTML hostil).
    Props: `boletin: string`. También exportar un helper de layout `MencionBoletinChips`
    (o dejar el flex-wrap al consumidor) que rinde `flex flex-wrap gap-2 mt-1` sobre un array
    de boletines. El color petróleo es IDÉNTICO para todo chip — jamás color-por-tema/boletín;
    codifica "enlace", nunca relevancia/riesgo (UI-SPEC §Color PROHIBIDO).</action>
  <verify>
    <automated>pnpm --filter ./app exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>MencionBoletinChip existe, es un Link a /proyecto/N con Badge outline petróleo, min-h-11, focus-visible, aria-label LOCKED, N en font-mono; sin TooltipTrigger anidado.</done>
</task>

<task type="auto">
  <name>Task 2: Materia legible en ambas vistas + wire de chips server-side + tests</name>
  <files>app/components/lobby-de-parlamentario.tsx, app/components/lobby-de-parlamentario.test.tsx</files>
  <action>Tres cambios coordinados, materia verbatim, dato NO re-transformado:
    (1) MATERIA LEGIBLE — VistaCronologica: el fragmento `Asunto: {a.materia}` pasa de `<span>`
    inline a un bloque (`<div>`/`<p>`) con `whitespace-pre-line leading-relaxed text-sm`; el
    contenedor flex padre ya tiene `min-w-0` (línea ~425). PROHIBIDO añadir line-clamp/truncate/
    max-h-overflow. Materia SELECCIONABLE, sin "ver más".
    (2) MATERIA LEGIBLE — VistaAgrupada: HOY el grupo solo muestra contraparte + conteo + fechas.
    Extender `GrupoContraparte` + `agruparPorContraparte` para listar POR REUNIÓN dentro del
    grupo: `{fecha mono}` + `{materia completa}` (whitespace-pre-line leading-relaxed) + sus chips
    de mención. El conteo neutro `{N} reuniones` se mantiene. La materia y sus chips viven en la
    MISMA fila de la reunión (restricción de carril LOCKED — la mención proviene de ESA materia,
    es metadata de la propia audiencia, no composición de dominios).
    (3) WIRE DE CHIPS server-side (fail-closed doble, Block-B): en `LobbySection`, tras cargar las
    audiencias, para cada `a.materia` llamar `extraerBoletines(a.materia)`; juntar todos los
    boletines candidatos, hacer UNA query batched `sb.from("proyecto").select("boletin").in("boletin", candidatos)`
    (proyecto NO es PII, ya se lee server; paginar/cap si el set fuese grande, pero es acotado),
    construir un Set de boletines EXISTENTES, y adjuntar a cada `LobbyAudienciaRow`/reunión de
    grupo su lista de `boletines_mencionados` VALIDADOS (patrón + existencia). Los que matchean el
    patrón pero NO existen se descartan (fail-closed #2 — jamás chip muerto). `LobbyView` /
    `VistaCronologica` / `VistaAgrupada` reciben los boletines ya resueltos y montan
    `MencionBoletinChip` bajo la materia en `flex flex-wrap gap-2 mt-1` (orden asc, dedupe). El
    error real de la batched query se LANZA (#34), nunca degrada a "sin chips".
    Tests RTL (fixtures, LobbyView puro): materia multilínea completa visible en ambas vistas
    (assert whitespace-pre-line, sin clamp); chip presente cuando el boletín existe; SIN chip
    cuando el patrón matchea pero el boletín no está en el Set validado; SIN chip cuando la
    materia no menciona boletín; múltiples chips deduplicados por boletín distinto.</action>
  <verify>
    <automated>pnpm --filter ./app test -- lobby-de-parlamentario</automated>
  </verify>
  <done>Materia completa legible en cronológica y agrupada (whitespace-pre-line, sin clamp); chips "Menciona boletín N" server-computados fail-closed doble en ambas vistas → /proyecto/N; LobbyView puro (chips llegan en datos); tests RTL verdes; suite app verde.</done>
</task>

</tasks>

<verification>
- La materia se muestra ENTERA y legible en ambas vistas (núcleo de LOB-01); dato verbatim, sin re-ingesta.
- El chip aparece SOLO con boletín que matchea el patrón Y existe en `proyecto` (fail-closed doble, LOB-02).
- La validación de existencia es SERVER-SIDE (Block-B respetado); LobbyView permanece puro (sin .rpc/.from en islands).
- Navegación audiencia→PL operativa vía el chip (LOB-03).
</verification>

<success_criteria>
- LOB-01: materia completa visible/legible en ambas vistas.
- LOB-02: chips solo por mención explícita de boletín válido (nunca keywords).
- LOB-03: navegación bidireccional audiencia→PL desde la ficha del parlamentario.
- Suite app verde; tsc 0.
</success_criteria>

<output>
Create `.planning/phases/92-personas-p2c-lobby-legible-audiencia-pl-fail-closed/92-02-SUMMARY.md` when done.
</output>


<!-- PLAN-CHECKER FIX (MAJOR 4): el reshape de agruparPorContraparte debe declarar GrupoContraparte.reuniones: {fecha, materia, boletines}[] explicito y conservar con tests de regresion la semantica actual (orden frecuencia-DESC, dedupe por audiencia). (MAJOR 5): declarar el bound concreto del .in() de existencia (audiencias por pagina x max boletines por materia) y asertar < 1000 (cap PostgREST); si el calculo puede exceder, paginar .range(). (MINOR 9): confirmar que el copy del chip no contiene terminos prohibidos (cero entrada NEGACIONES por el chip). -->
