---
phase: 89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0058_proyecto_prm_id_camara.sql
  - packages/tramitacion/src/parse-camara-legislativo.ts
  - packages/tramitacion/src/parse-camara-legislativo.test.ts
  - packages/tramitacion/src/connector-camara.ts
  - packages/tramitacion/src/run-backfill-prmid-cli.ts
autonomous: false
requirements: [TRACE-01]
must_haves:
  truths:
    - "La columna proyecto.prm_id_camara existe en PROD (aditiva, nullable) y viaja por los reads existentes sin cambiar la query"
    - "parseCamaraLegislativo devuelve pares {boletin, prmId} en vez de descartar el <Id>"
    - "El CLI de backfill LOCAL persiste el crudo XML a R2 PRIMERO (etapa 1) y solo entonces hace UPDATE por boletin (etapa 2)"
    - "Tras correr el backfill, un subconjunto de filas de proyecto tiene prm_id_camara poblado (N/M declarado); el resto queda NULL (fail-honest)"
  artifacts:
    - path: "supabase/migrations/0058_proyecto_prm_id_camara.sql"
      provides: "Columna aditiva prm_id_camara SIN grant nuevo"
      contains: "add column prm_id_camara"
    - path: "packages/tramitacion/src/parse-camara-legislativo.ts"
      provides: "Parser que emite {boletin, prmId}"
      contains: "prmId"
    - path: "packages/tramitacion/src/run-backfill-prmid-cli.ts"
      provides: "CLI LOCAL dos-etapas reanudable (enumerar Cámara -> R2 -> UPDATE)"
      exports: []
  key_links:
    - from: "packages/tramitacion/src/connector-camara.ts"
      to: "packages/ingest R2Store.putImmutable"
      via: "etapa 1: persistir crudo XML antes del parse"
      pattern: "putImmutable"
    - from: "packages/tramitacion/src/run-backfill-prmid-cli.ts"
      to: "proyecto.prm_id_camara"
      via: "sb.from('proyecto').update(...).eq('boletin', ...)"
      pattern: "update.*prm_id_camara"
---

<objective>
Plumbing de ingesta para el deep-link de Cámara (TRACE-01): persistir el `prmID` interno de la Cámara en una columna aditiva de `proyecto`, extrayéndolo del `<Id>` de WSLegislativo (que el parser YA lee pero descarta), y poblar las filas existentes con un backfill LOCAL dos-etapas (crudo a R2 primero, luego UPDATE). El `prmID` NO está en ningún dato ingerido ni en el crudo R2 (censo: R2 solo tiene `leyes`/`infoprobidad`, sin WSLegislativo) — requiere una pasada NUEVA a la fuente Cámara, respetuosa (rate-limit 2-3s, UA identificatorio, curl/fetcher-first por WAF).

Purpose: Sin `prm_id_camara` persistido no existe el deep-link `tramitacion.aspx?prmID=&prmBOLETIN=` (Plan 02 lo consume). El Senado no necesita plumbing (boletín en mano); BCN se acota honestamente (sin `idNorma` en datos).
Output: Migración 0058 aplicada a PROD + ledger reconciliado; parser devolviendo `{boletin, prmId}`; CLI de backfill LOCAL; filas de `proyecto` con `prm_id_camara` poblado (N/M declarado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-RESEARCH.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-PATTERNS.md

# Análogos exactos/role-match (leer para copiar idioms — NO explorar el árbol a ciegas)
@packages/tramitacion/src/parse-camara-legislativo.ts
@packages/tramitacion/src/connector-camara.ts

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del research/pattern-map. NO re-explorar. -->

parse-camara-legislativo.ts (shape actual, `:34-96`):
- `txt(v): string | null` — string no-vacío o null, maneja `#text`.
- `asArray(v): unknown[]` — normaliza a array.
- `ProyectoLeySchema = z.object({ NumeroBoletin: z.string().regex(BOLETIN_RE), Nombre: z.string().optional() })`
- `parseCamaraLegislativo(xml): string[]` — HOY devuelve boletines; el `<Id>` se lee (`:10-11`) pero se descarta.
- Idiom LOCKED: zod-validate-before-return; elemento inválido → `continue` (DESCARTA, NUNCA lanza).
- CAMBIO: añadir `Id: z.string().optional()` al schema; return `[{ boletin, prmId: Id ?? null }]`.

connector-camara.ts:
- `private async fetch(url): Promise<string>` (`:69-75`) — assertAllowedUrl → robots.isAllowed → rateLimiter.wait(host) [2-3s] → fetcher.get. NUNCA hand-roll.
- `enumerarProyectosXAnno(anno)` (`:140-170`) — ops `["retornarMocionesXAnno","retornarMensajesXAnno"]`, best-effort por op con log, si AMBAS fallan → LANZA (WR-04). Valida año 1990..2100 + encodeURIComponent.
- `BASE_LEG` constante para WSLegislativo.

@obs/ingest R2Store (r2-store.ts:56-81):
- `putImmutable(fuente, recurso, fecha, sha, ext, body): Promise<void>` — If-None-Match:*, 412=OK idempotente. SIN presign.

run-enumerar-historico-cli.ts (esqueleto LOCAL a copiar):
- `flagValue(name)` (`:29-32`), `loadEnv()` BOM-safe con precedencia process.env (`:39-51`).
- Validación rango `--desde/--hasta` 1990..2100 (`:64-76`).
- Ensamblado `@obs/ingest` verbatim (`:80-84`): `new Fetcher()`, `new HostRateLimiter()`, `new RobotsGuard({ allowlist: {} })`.
- `--dry-run` gate (`:86-93`). isMain regex del PROPIO archivo (`:129-134`). exit 1 si hubo errores.

run-tramitacion-prod-cli.ts (cliente remoto service_role + R2 env):
- `createClient(url, serviceKey, { auth: { persistSession: false } })` (`:110`).
- loadEnv promueve: SUPABASE_API_URL, SUPABASE_SECRET_KEY, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_ENDPOINT_URL, R2_BUCKET.
- Solo propaga `error.message`, NUNCA la service key (`:102, :120`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración 0058 (columna aditiva) + parser emite {boletin, prmId}</name>
  <files>supabase/migrations/0058_proyecto_prm_id_camara.sql, packages/tramitacion/src/parse-camara-legislativo.ts, packages/tramitacion/src/parse-camara-legislativo.test.ts</files>
  <behavior>
    - Test (parse): un XML WSLegislativo con `<Id>17140</Id><NumeroBoletin>16572-06</NumeroBoletin>` → `[{ boletin: "16572-06", prmId: "17140" }]`.
    - Test (parse): un `<ProyectoLey>` sin `<Id>` → `{ boletin, prmId: null }` (no lanza, no descarta la fila por falta de Id).
    - Test (parse): un `<ProyectoLey>` con `NumeroBoletin` inválido (no matchea BOLETIN_RE) → DESCARTADO (`continue`), NUNCA lanza (idiom LOCKED preservado).
  </behavior>
  <action>Crear `supabase/migrations/0058_proyecto_prm_id_camara.sql` con `alter table proyecto add column prm_id_camara text;` (nullable) + `comment on column`. SIN NINGÚN grant/policy/create policy — el read público lo hereda de 0008 (`grant select ... to anon`); un grant nuevo haría MORDER al lockdown-guard block A (migraciones >0044 no pueden tener `grant … to anon`/`to public`). Luego MODIFICAR `parse-camara-legislativo.ts`: añadir `Id: z.string().optional()` a `ProyectoLeySchema`, y cambiar el return de `string[]` a `{ boletin: string; prmId: string | null }[]` usando `txt(p.Id)` (mismo idiom que los demás campos). Extender el `.test.tsx` existente (que ya tiene shape XML `:11-29`) con los 3 casos de behavior. Preservar el idiom zod-validate-before-return: elemento inválido → `continue`, jamás lanza. Actualizar cualquier caller interno del parser que asumiera `string[]` (grep dentro de packages/tramitacion). Esta migración NO se aplica todavía — se aplica a PROD en Task 2 tras reconciliar el ledger.</action>
  <verify>
    <automated>pnpm --filter ./packages/tramitacion test -- --run parse-camara-legislativo</automated>
  </verify>
  <done>0058 existe sin grants; `parseCamaraLegislativo` devuelve pares `{boletin, prmId}`; los 3 tests nuevos pasan; el idiom fail-soft (continue) sigue vivo.</done>
</task>

<task type="auto">
  <name>Task 2: Conector enumerarProyectosConIdXAnno (dos-etapas) + CLI backfill LOCAL</name>
  <files>packages/tramitacion/src/connector-camara.ts, packages/tramitacion/src/run-backfill-prmid-cli.ts</files>
  <action>MODIFICAR `connector-camara.ts`: añadir método `enumerarProyectosConIdXAnno(anno)` que espeja `enumerarProyectosXAnno` (`:140-170`) — mismas ops `["retornarMocionesXAnno","retornarMensajesXAnno"]`, misma validación de año 1990..2100 + encodeURIComponent, mismo best-effort-por-op-con-log, mismo LANZA-si-AMBAS-fallan (WR-04) — PERO (a) captura el XML crudo de cada op y lo expone (o recibe un callback/`R2Store`) para persistirlo a R2, y (b) devuelve pares `{boletin, prmId}[]` usando el parser modificado. Reusar `this.fetch` (SIEMPRE — assertAllowedUrl+robots+rateLimiter 2-3s+fetcher); NUNCA hand-roll fetch a camara.cl. Crear `run-backfill-prmid-cli.ts` (NUEVO, LOCAL one-shot) componiendo `run-enumerar-historico-cli.ts` (esqueleto: flagValue, loadEnv BOM-safe, `--desde/--hasta` 1990..2100, colaboradores @obs/ingest verbatim, `--dry-run`, isMain con regex del PROPIO archivo, exit 1 si hubo errores) + `run-tramitacion-prod-cli.ts` (createClient service_role con persistSession:false + env R2). Flujo dos-etapas por año: ETAPA 1 `R2Store.putImmutable("camara-legislativo", String(anno), fechaBucket, sha, "xml", body)` (crudo PRIMERO, idempotente 412=OK); ETAPA 2 por par `sb.from("proyecto").update({ prm_id_camara: prmId }).eq("boletin", boletin)` — SOLO filas que existen (update, no upsert; no crea proyectos), y SOLO cuando `prmId != null`. Reanudable: el hash-check de R2 salta años ya persistidos; el UPDATE es idempotente (mismo valor). Fail-loud: error de R2/DB LANZA; propagar solo `error.message`, JAMÁS la service key. Reportar por año: enumerados, con-prmId, updated, errores. Añadir el CLI a los scripts del package si aplica (patrón de los otros run-*-cli).</action>
  <verify>
    <automated>pnpm --filter ./packages/tramitacion test -- --run && pnpm --filter ./packages/tramitacion exec tsc --noEmit</automated>
  </verify>
  <done>El conector tiene el método dos-etapas reusando la política de fetch LOCKED; el CLI compila, valida rango, ensambla @obs/ingest, tiene `--dry-run` e isMain propio, y hace ETAPA1(R2)→ETAPA2(UPDATE) fail-loud sin filtrar secretos. Suite del package verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (checkpoint): Aplicar 0058 a PROD + correr el backfill LOCAL</name>
  <what-built>Migración 0058 (aditiva, sin grants) lista para aplicar por psql; conector + CLI de backfill LOCAL dos-etapas (`run-backfill-prmid-cli.ts`) que enumera Cámara WSLegislativo (rate-limit 2-3s, UA identificatorio), persiste crudo a R2 y hace UPDATE de `prm_id_camara` por boletín.</what-built>
  <how-to-verify>
    Este checkpoint cubre DOS actos que el agente NO ejecuta solo contra PROD/fuente:

    1) APLICAR 0058 a PROD (write remoto):
       - Reconciliar el ledger ANTES (drift conocido, MEMORY 85-01 D3) — NUNCA `supabase db push`.
       - `PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0058_proyecto_prm_id_camara.sql` (esquivar BOM).
       - Verificar: `psql "$SUPABASE_DB_URL" -c "\d proyecto"` muestra `prm_id_camara text`; y `select count(*) from pg_policies where tablename='proyecto'` no aumentó (cero grant nuevo).

    2) CORRER el backfill LOCAL (pasada a fuente Cámara + write PROD):
       - Validar 1-2 pares live ANTES de la corrida masiva (A1 del research): p.ej. boletín `16572-06` debe resolver a prmID `17140`.
       - `--dry-run` primero; luego la corrida real con `--desde/--hasta` (rate-limit 2-3s LOCKED, ~3.659 boletines ≈ 2-3h). Correr con Monitor/background y reportar avance N/M.
       - Si el tiempo excede la ventana: dejar backfill PARCIAL documentado (N/M por año) — es reanudable (hash-check R2 salta lo hecho; UPDATE idempotente). NO es un bug: proyectos sin prmID quedan NULL (fail-honest, deep-link Cámara ausente para ellos).
       - Reportar cobertura final: `select count(*) filter (where prm_id_camara is not null) as con_prmid, count(*) as total from proyecto;`
  </how-to-verify>
  <resume-signal>Escribe "aplicado" con: (a) `\d proyecto` mostrando prm_id_camara + pg_policies sin aumento, (b) cobertura N/M del backfill (con_prmid/total). Si el backfill quedó parcial, indicar hasta qué año y que el resto es corrida LOCAL reanudable. Si algo falló, describir el fallo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI backfill → fuente camara.cl (WSLegislativo) | Salida a un host gubernamental con WAF; input controlado (año numérico validado) |
| CLI backfill → R2 (crudo) | Escritura de crudo inmutable content-addressed |
| CLI backfill → Supabase PROD (service_role) | Write remoto que bypassa RLS |
| Migración 0058 → schema público | Columna nueva servida por reads públicos existentes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/SSRF | connector fetch a WSLegislativo | mitigate | Reusar `this.fetch` (assertAllowedUrl allowlist + robots + rateLimiter 2-3s); año validado 1990..2100 + encodeURIComponent; NUNCA hand-roll fetch |
| T-89-02 | DoS (self, WAF Cámara) | backfill masivo | mitigate | rate-limit 2-3s serial LOCKED, UA identificatorio, backfill LOCAL (no CI), reanudable vía hash-check R2 |
| T-89-03 | Information Disclosure | logs del CLI | mitigate | Propagar solo `error.message`, JAMÁS la service key ni R2 secret (patrón run-tramitacion-prod-cli:102,120) |
| T-89-04 | Information Disclosure | migración 0058 | accept | `prm_id_camara` es ID público de Cámara (no PII); columna hereda read anon existente; SIN grant nuevo (lockdown-guard block A lo verifica) |
| T-89-05 | Tampering | UPDATE por boletín | mitigate | `.update().eq('boletin', ...)` solo toca filas existentes (no upsert, no crea proyectos); dos-etapas: crudo a R2 antes del write |
| T-89-SC | Tampering | npm installs | mitigate | N/A — cero instalaciones (RESEARCH §Package Legitimacy Audit); todo workspace/dep existente |
</threat_model>

<verification>
- `pnpm --filter ./packages/tramitacion test -- --run` verde (parser + conector).
- `pnpm --filter ./packages/tramitacion exec tsc --noEmit` sin errores.
- lockdown-guard NO muerde sobre 0058: `pnpm --filter ./app test -- --run lockdown-guard` verde (0058 sin `grant … to anon`).
- Post-checkpoint: `\d proyecto` muestra `prm_id_camara`; cobertura N/M reportada.
</verification>

<success_criteria>
- Columna `proyecto.prm_id_camara` existe en PROD (aditiva, nullable, sin grant nuevo).
- `parseCamaraLegislativo` emite `{boletin, prmId}` preservando el idiom fail-soft.
- CLI backfill LOCAL dos-etapas (R2 primero, UPDATE después) reanudable y fail-loud.
- Backfill corrido: N/M de filas con `prm_id_camara` poblado, resto NULL (fail-honest).
</success_criteria>

<output>
Create `.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-01-SUMMARY.md` when done
</output>
