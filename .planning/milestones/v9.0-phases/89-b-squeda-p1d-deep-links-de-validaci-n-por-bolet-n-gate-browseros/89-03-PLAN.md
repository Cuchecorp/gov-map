---
phase: 89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros
plan: 03
type: execute
wave: 2
depends_on: [89-01, 89-02]
files_modified:
  - .planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-BROWSEROS-GATE.md
autonomous: false
requirements: [TRACE-02]
must_haves:
  truths:
    - "El sitio está desplegado en Cloudflare con búsqueda híbrida (default ON), filtros island (88) y deep-links (89) LIVE"
    - "Los deep-links Senado/Cámara abren la página oficial del boletín correcto en deploy real (HTTP 200 + content-match)"
    - "El gate BrowserOS emite un veredicto empírico ('valida el dato') sobre la pasada 1 completa (87+88+89), con evidencia commiteada — o handoff honesto si el MCP está caído"
  artifacts:
    - path: ".planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-BROWSEROS-GATE.md"
      provides: "Registro del gate: URL deploy, resultados curl muestra, veredicto BrowserOS, evidencia visual o handoff"
  key_links:
    - from: "deploy Cloudflare"
      to: "https://observatorio-congreso.thevalis.workers.dev"
      via: "Docker node:22-slim build + wrangler deploy OAuth local"
      pattern: "workers.dev"
    - from: "scripts/validar-deeplinks.mjs"
      to: "deep-links vivos"
      via: "curl 200 + content-match sobre muestra >=10 con prmID poblado"
      pattern: "validar-deeplinks"
---

<objective>
Cierre de la PASADA 1 (fases 87+88+89) en deploy real: desplegar el sitio a Cloudflare con el runbook probado (Docker node:22-slim, robocopy a C:/Temp, wrangler OAuth local, pnpm 11 dangerouslyAllowAllBuilds), y correr el gate empírico sobre el deploy vivo — (1) /buscar con query literal + boletín punteado encuentra; (2) filtros/orden operables; (3) ficha → deep-links Senado/Cámara abren la página oficial del boletín correcto (HTTP 200 + content-match, curl-first Cámara); (4) evidencia visual BrowserOS (desktop + móvil 390px vía iframe same-origin). El veredicto BrowserOS es "valida el dato" (TRACE-02).

Purpose: El gate BrowserOS de 89 es criterio de éxito de la pasada 1 completa, no solo de los deep-links. Búsqueda híbrida (default ON, no requiere setearse en Cloudflare), filtros (88) y deep-links (89) quedan LIVE con este deploy.
Output: `89-BROWSEROS-GATE.md` con URL de deploy, resultados curl de la muestra, veredicto BrowserOS + screenshots (o handoff honesto documentado si el MCP está caído — patrón v7, JAMÁS fingir capturas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-RESEARCH.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-02-SUMMARY.md
@.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-01-SUMMARY.md
@scripts/bros-cli.mjs

<interfaces>
<!-- Runbook + gate extraídos de RESEARCH. NO re-derivar. -->

Deploy runbook VIGENTE (RESEARCH §Deploy, de 85-01-SUMMARY.md — el path del CONTEXT `61-02-SUMMARY.md` NO existe, usar 85-01):
1. Build: Docker node:22-slim, mount C:/Temp/obs-build:/host + C:/Temp/obs-build/app/.open-next:/open-next-out, `cp -r /build/app/.open-next/. /open-next-out/` inline al final. pnpm 11 dangerouslyAllowAllBuilds en Docker. Copia previa vía robocopy a C:/Temp/obs-build (Windows OneDrive → build path limpio).
2. Deploy: `node C:/Users/Carlo/AppData/Roaming/npm/node_modules/wrangler/bin/wrangler.js deploy --config wrangler.jsonc` desde C:/Temp/obs-build/app/ (wrangler global, OAuth local — CI no tiene creds CF).
3. Worker: https://observatorio-congreso.thevalis.workers.dev

Flags en Cloudflare:
- BUSQUEDA_HIBRIDA_ENABLED: default ON (`!== "false"`) → NO setearse en Cloudflare, viaja horneado ON.
- CRUCES_PUBLIC_ENABLED: ON en PROD desde 2026-07-02 → NO se toca.
- No hay flag nuevo para deep-links (89): la sección se monta siempre; fail-honest por-dato.

Verificación HTTP post-deploy (patrón 85-01): `GET /` → 200 + copy SSR; headers de seguridad presentes; `GET /proyecto/{boletin}` → 200 con sección de validación + deep-links.

Gate empírico (4 puntos, RESEARCH §Gate BrowserOS):
1. /buscar con query literal + boletín punteado (`14.309-04`) → encuentra el proyecto.
2. Filtros/orden operables (island 88).
3. Ficha → deep-links Senado/Cámara abren la página oficial del boletín correcto (HTTP 200 + content-match; curl-first Cámara por WAF) → `node scripts/validar-deeplinks.mjs` sobre muestra ≥10 con prmID poblado (incl. golden 14309-04 + fixture 16572-06).
4. Evidencia visual BrowserOS: desktop + móvil 390px (iframe same-origin, patrón v8.0).

BrowserOS wrapper (scripts/bros-cli.mjs, MCP en http://127.0.0.1:9200/mcp):
- `open <url>` (new_hidden_page, imprime `Page ID: N` — parsear N, INCREMENTA por sesión).
- Esperar 4-5s tras open (SSR+hidratación). `shot <page> <abs-path>`, `content <page>`, `snapshot`, `close`.
- save_screenshot puede fallar 1× "CDP request timeout" → reintentar tras sleep 3. save_screenshot en RÁFAGA tumba el MCP → sleep 8-10s ENTRE shots.
- Si MCP caído: pedir al operador levantarlo; si no responde → documentar handoff con la evidencia curl (content-match) y cerrar (patrón v7). JAMÁS fingir capturas.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (checkpoint): Deploy Cloudflare + gate BrowserOS sobre deploy real</name>
  <what-built>Pasada 1 (87 retrieval + 88 filtros/ranking + 89 deep-links) lista para deploy. El build/deploy usa credenciales que el agente no tiene en CI (wrangler OAuth local, Docker local) y el gate BrowserOS depende del MCP en la máquina del operador (127.0.0.1:9200).</what-built>
  <how-to-verify>
    DEPLOY (runbook 85-01, idéntico a 81-01/85-01):
    1. robocopy el repo a C:/Temp/obs-build (build path limpio fuera de OneDrive).
    2. Build en Docker node:22-slim con el mount de .open-next y `cp -r` inline (pnpm 11 dangerouslyAllowAllBuilds).
    3. `wrangler deploy --config wrangler.jsonc` desde C:/Temp/obs-build/app/ (OAuth local).
    4. NO setear BUSQUEDA_HIBRIDA_ENABLED (default ON); NO tocar CRUCES_PUBLIC_ENABLED.
    5. Verificación HTTP post-deploy: `GET /` → 200 + copy SSR + headers seguridad; `GET /proyecto/16572-06` (o un boletín con prmID poblado) → 200 con la sección "Valida este dato en la fuente" + deep-links.

    GATE (4 puntos) sobre el deploy vivo:
    1. /buscar `14.309-04` (punteado) → encuentra como resultado; /buscar por fragmento literal del título → encuentra.
    2. Filtros/orden island operables (reordena sin re-buscar).
    3. `node scripts/validar-deeplinks.mjs` con la muestra ≥10 (prmID del backfill Plan 01) → todos HTTP 200 + content-match (curl-first Cámara).
    4. BrowserOS: `node scripts/bros-cli.mjs open <ficha-url>` → parsear Page ID → esperar 4-5s → `shot` desktop + `shot` móvil 390px (iframe same-origin), sleep 8-10s ENTRE shots. Guardar screenshots en el phase dir.

    Si BrowserOS MCP está caído: pedir al operador levantarlo; si no responde → documentar handoff con la evidencia curl (content-match) y cerrar (patrón v7). JAMÁS fingir capturas.
  </how-to-verify>
  <resume-signal>Escribe "desplegado" con: URL de deploy viva, resultado de `validar-deeplinks.mjs` (muestra N/N HTTP 200 + match), y veredicto del gate BrowserOS ("valida el dato" + rutas de los screenshots) O el handoff honesto si el MCP no respondió. Si algún punto del gate falla, describir el fallo (no cerrar como éxito).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar el gate + evidencia (89-BROWSEROS-GATE.md) y commitear</name>
  <files>.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-BROWSEROS-GATE.md</files>
  <action>Con los resultados del checkpoint, escribir `89-BROWSEROS-GATE.md`: (a) URL del deploy + hash/versión del worker; (b) verificación HTTP post-deploy (GET / y GET /proyecto/{boletin} → 200); (c) tabla de resultados de `validar-deeplinks.mjs` (por boletín: Senado/Cámara HTTP + content-match sí/no) sobre la muestra ≥10 con golden 14309-04 + fixture 16572-06; (d) veredicto BrowserOS sobre los 4 puntos del gate ("valida el dato" / puntos fallidos), con rutas a los screenshots desktop + móvil 390px commiteados en el phase dir — O el handoff honesto si el MCP estuvo caído (patrón v7, con la evidencia curl como respaldo reproducible); (e) declaración explícita de que el gate valida la PASADA 1 completa (87+88+89) en deploy real, no solo los deep-links; (f) cualquier deuda documentada (p.ej. respaldo R2 sin descarga → 95/96; prmID solo poblado por backfill LOCAL, proyectos nuevos quedan NULL hasta re-correrlo; BCN sin idNorma acotado a "cuando exista"). Commitear el archivo + los screenshots (si los hay) con `docs(89): browseros gate + deep-links validation evidence`.</action>
  <verify>
    <automated>test -f .planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-BROWSEROS-GATE.md && grep -qi "valida" .planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-BROWSEROS-GATE.md</automated>
  </verify>
  <done>89-BROWSEROS-GATE.md existe con URL de deploy, tabla curl de la muestra, veredicto BrowserOS (o handoff honesto), declaración de cierre de pasada 1, y deudas documentadas; commiteado con screenshots si los hubo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build local → Cloudflare Worker | Deploy de artefacto a producción con OAuth local |
| Gate → deep-links vivos (senado.cl/camara.cl) | Validación curl a hosts gubernamentales con WAF |
| BrowserOS MCP → deploy vivo | Captura de evidencia sobre el sitio real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-11 | Elevation/Tampering | deploy Cloudflare | mitigate | wrangler OAuth local (no creds en CI); no setear flags nuevos; CRUCES_PUBLIC_ENABLED intacto; verificación HTTP post-deploy antes de cerrar |
| T-89-12 | DoS (self, WAF Cámara) | validar-deeplinks sobre muestra | mitigate | curl-first, rate-limit sleep 3, UA identificatorio, muestra acotada ≥10 (no crawl) |
| T-89-13 | Repudiation (evidencia falsa) | gate BrowserOS | mitigate | Handoff honesto si MCP caído; JAMÁS fingir capturas (patrón v7); curl content-match como respaldo reproducible commiteado |
| T-89-14 | Information Disclosure | ficha en deploy real | mitigate | Verificar que el respaldo R2 en la ficha vive muestra fecha+hash y NUNCA r2_path como href (heredado Plan 02); headers de seguridad presentes post-deploy |
| T-89-SC | Tampering | npm installs | mitigate | N/A — cero instalaciones (deploy reusa build existente; RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Deploy vivo: `GET /` → 200 + headers seguridad; `GET /proyecto/{boletin}` → 200 con sección de validación.
- `node scripts/validar-deeplinks.mjs` sobre la muestra → todos HTTP 200 + content-match.
- `89-BROWSEROS-GATE.md` existe con veredicto + evidencia (o handoff honesto), commiteado.
</verification>

<success_criteria>
- Sitio desplegado a Cloudflare con híbrida (ON) + filtros (88) + deep-links (89) LIVE.
- Los 4 puntos del gate pasan sobre el deploy real (o los fallos quedan documentados honestamente, no cerrados como éxito).
- `89-BROWSEROS-GATE.md` cierra la pasada 1 (87+88+89) con evidencia reproducible commiteada.
</success_criteria>

<output>
Create `.planning/phases/89-b-squeda-p1d-deep-links-de-validaci-n-por-bolet-n-gate-browseros/89-03-SUMMARY.md` when done
</output>
