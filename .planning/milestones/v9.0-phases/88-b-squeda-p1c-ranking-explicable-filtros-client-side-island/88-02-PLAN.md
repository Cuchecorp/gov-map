---
phase: 88-b-squeda-p1c-ranking-explicable-filtros-client-side-island
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - app/components/buscar-filtros.tsx
  - app/components/buscar-filtros.test.tsx
autonomous: true
requirements: [FILT-01, FILT-02, RANK-01]

must_haves:
  truths:
    - "El island filtra/reordena el slice YA recibido EN MEMORIA sin re-consultar ni tocar Supabase"
    - "Cada chip de faceta muestra un count 'de estos N resultados' con leyenda explícita, nunca como conteo global"
    - "Las facetas con count 0 quedan deshabilitadas (aria-disabled) — nunca removidas"
    - "El estado sin_dato y el año Sin dato se renderizan como bucket visible (nunca oculto)"
    - "El toggle de orden ofrece Relevancia (default) · Más recientes · Mensajes primero, cada uno determinista y explicado; el orden primario es siempre el rank del retrieval"
    - "La faceta partido NO se renderiza (llega con BIO-03/P2); el island acepta partido opcional sin refactor"
  artifacts:
    - path: "app/components/buscar-filtros.tsx"
      provides: "island client de filtros/orden sobre el slice serializado"
      contains: "\"use client\""
    - path: "app/components/buscar-filtros.test.tsx"
      provides: "tests jsdom de estructura/estado/facetas/aria-pressed/empty-after-filter"
  key_links:
    - from: "app/components/buscar-filtros.tsx"
      to: "app/lib/estado-bucket.ts"
      via: "import BuscarSliceRow (tipo) + ETIQUETA_BUCKET"
      pattern: "BuscarSliceRow|ETIQUETA_BUCKET"
    - from: "app/components/buscar-filtros.tsx"
      to: "NO Supabase"
      via: "cero import de @/lib/supabase (contrato FichaRail)"
      pattern: "use client"
---

<objective>
Construir el island client `buscar-filtros.tsx`: recibe el slice serializado (`BuscarSliceRow[]`, del plan 01) ya obtenido por el server, y filtra/reordena EN MEMORIA con React state — cero red, cero Supabase (contrato FichaRail LOCKED). Facetas año/iniciativa/estado/cámara con counts honestos "de estos N", facetas vacías deshabilitadas, sin_dato visible, y un toggle de orden explicable (RANK-01). La faceta partido NO se renderiza (BIO-03/P2), pero el tipo la acepta opcional.

Purpose: FILT-01 (filtros que reordenan/filtran sin re-buscar), FILT-02 (counts honestos + facetas vacías deshabilitadas + sin_dato explícito), RANK-01 (orden explicable mensaje>moción + recencia, nunca ML ni score). El island es net-new pero espeja `red-graph.tsx` (filtros en memoria sobre slice recibido) y `ficha-rail.tsx` (contrato zero-network). Introduce `aria-pressed` al repo (0 usos hoy) espejando el tratamiento `aria-current` de FichaRail.

Output: `app/components/buscar-filtros.tsx`, `app/components/buscar-filtros.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-CONTEXT.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-UI-SPEC.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-PATTERNS.md
@app/lib/estado-bucket.ts

<interfaces>
<!-- Contrato consumido (del plan 01, ya en el repo cuando este plan corre). -->

De app/lib/estado-bucket.ts (plan 88-01):
```typescript
export type EstadoBucket =
  | "en_tramitacion" | "publicado_ley" | "archivado"
  | "rechazado" | "retirado" | "sin_dato";
export const ETIQUETA_BUCKET: Record<EstadoBucket, string>;
// (los labels display LOCKED: En tramitación · Publicado / Ley · Archivado · Rechazado · Retirado · Sin dato)
```

De app/lib/types.ts (plan 88-01):
```typescript
export interface BuscarSliceRow {
  boletin: string;
  titulo: string;
  anio: number | null;                          // null = sin evento Ingreso derivable → bucket "Sin dato"
  iniciativa: "Mensaje" | "Moción" | null;
  estadoBucket: EstadoBucket;
  camaraOrigen: string | null;
  fecha: string | null;
  partido?: string | null;                      // OPCIONAL — BIO-03/P2; ausente → faceta no renderiza
}
```

Componentes reusables (NO reconstruir):
- `EtapaBadge` (@/components/etapa-badge) — chip de estado; sin_dato usa la variante slate neutra
- `CamaraChip` (@/components/camara-chip) — chip de cámara con tokens cívicos; retorna null para desconocido
- `Badge variant="outline"` (@/components/ui/badge) — label neutro Mensaje/Moción + año

Patrón zero-network island (red-graph.tsx:1-3, 233-239):
```typescript
"use client";
import { useCallback, useMemo, useState } from "react";
const [tiposOcultos, setTiposOcultos] = useState<Set<string>>(new Set()); // ← estado de faceta-toggle
```
Base de chip-button (search-box.tsx:140-155): real `<button type="button">`, `min-h-11`, `rounded-full border border-border bg-muted px-4`, `focus-visible:*` ring.
Tratamiento activo petróleo (ficha-rail.tsx:60-67): `bg-accent-product-soft border-accent-product font-semibold text-accent-product` + `focus-visible:outline-accent-product` — expresarlo como `aria-pressed={engaged ? "true" : "false"}`.
Disabled faceta count-0 (globals.css .net-b-pager__btn:disabled): `opacity: 0.4; cursor: default` + `disabled` + `aria-disabled="true"`.
Contenedor de grupo de filtros: clases `.net-filtros` / `.net-filtros__tipos` / `.net-filtros__legend` / `.net-filtros__tipo` (globals.css 165-231, incluye stack móvil 390px `@media (max-width: 47.99rem)` con `min-height: 2.75rem`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Island buscar-filtros.tsx — facetas + counts honestos + orden explicable</name>
  <read_first>
    - app/components/red/red-graph.tsx (contrato zero-network, useState/useMemo sobre slice recibido, counts)
    - app/components/ficha-rail.tsx:24-89 (contrato serialized-slice LOCKED + tratamiento activo aria-current petróleo:60-67)
    - app/components/search-box.tsx:140-155 (base de chip-button + focus-visible ring)
    - app/components/autores-list.tsx:36-52 (island mínimo, texto inline honesto)
    - app/components/etapa-badge.tsx, app/components/camara-chip.tsx (chips reusables)
    - app/app/globals.css:165-231, 286-311, 521-524 (.net-filtros, .net-chip fills-nothing rule, disabled)
    - .planning/phases/88-*/88-UI-SPEC.md §Interaction Contract (LOCKED: facets, chips+counts, order toggle, accessibility)
  </read_first>
  <behavior>
    - Con slice de 10 filas (7 en_tramitacion, 3 archivado), el chip "En tramitación" muestra count 7 y "Archivado" muestra 3; el conteo es del slice recibido, no global
    - Existe una leyenda visible con el texto exacto "Conteos sobre estos N resultados, no sobre todo el corpus."
    - Un chip de faceta cuyo count sobre el slice es 0 renderiza con atributo `disabled` y `aria-disabled="true"` — no se remueve del DOM
    - Al activar la faceta estado "Archivado" (aria-pressed pasa a "true"), la lista filtrada contiene solo las filas archivado; al desactivarla vuelve a 10
    - Filas con estadoBucket "sin_dato" y con anio null rinden un chip visible "Sin dato" (nunca ocultas)
    - El toggle de orden tiene 3 botones: "Relevancia (por defecto)" (aria-pressed true por defecto), "Más recientes", "Mensajes primero"; el orden por defecto preserva el orden de entrada del slice (rank del retrieval)
    - "Más recientes" ordena por anio desc, empujando filas con anio null al final (nunca fabricadas a una fecha)
    - "Mensajes primero" agrupa Mensaje antes que Moción, preservando el orden relativo (rank) dentro de cada grupo
    - Con filtros que dejan 0 resultados, se muestra "Ningún resultado con estos filtros" + "Ajusta o quita filtros para ver más proyectos."
    - Si ninguna fila tiene `partido`, NO existe grupo de faceta de partido en el DOM (sin placeholder deshabilitado)
  </behavior>
  <action>
    Crear `app/components/buscar-filtros.tsx` con `"use client"`. Props: `{ slice: BuscarSliceRow[] }` (importar el tipo de `@/lib/types`). El island NUNCA importa `@/lib/supabase` ni re-consulta (contrato FichaRail — verificado por el guard PII que escanea `.from`/`.rpc` en todo `app/`).

    Estado (useState): un Set de valores activos por cada dimensión de faceta (año, iniciativa, estado, cámara) y un `orderMode: "relevancia" | "recientes" | "mensajes"` (default "relevancia"). Derivar la lista visible con `useMemo(slice, facetasActivas, orderMode)`.

    Facetas (FILT-01): renderizar grupos año · iniciativa (Mensaje/Moción) · estado (buckets) · cámara de origen. Usar `ETIQUETA_BUCKET` para los labels de estado; `EtapaBadge`/`CamaraChip`/`Badge variant="outline"` para los chips de valor (reusar, no reconstruir). Faceta partido: renderizar SOLO si alguna fila del slice trae `partido` no vacío; ausente → no renderizar (sin placeholder). Año null → bucket "Sin dato"; estadoBucket "sin_dato" → chip visible (nunca oculto).

    Chips + counts (FILT-02): cada chip = `<button type="button">` con `min-h-11`, base de search-box.tsx, mostrando label + count computado EN MEMORIA sobre el slice (de estos N). Leyenda visible exacta: "Conteos sobre estos N resultados, no sobre todo el corpus." (copy LOCKED UI-SPEC). Facetas con count 0 → `disabled` + `aria-disabled="true"` + opacidad reducida (nunca removidas). El chip activo lleva `aria-pressed="true"` + tratamiento petróleo (espejo ficha-rail.tsx:60-67); `focus-visible:outline-accent-product` en todos.

    Orden explicable (RANK-01): toggle segmentado de 3 botones deterministas y explicados: "Relevancia (por defecto)" (default; preserva el orden de entrada = rank del retrieval), "Más recientes" (anio desc, null al final, nunca fabricado), "Mensajes primero" (grupo Mensaje antes que Moción, rank interno preservado). El botón activo lleva `aria-pressed="true"` + petróleo. Leyenda de reglas visible exacta: "Orden por relevancia de la búsqueda; en empates se priorizan los mensajes del Ejecutivo y lo más reciente." NUNCA superficie score/cosine/rank/número, NUNCA término del linter (ranking/puntaje/índice/afinidad).

    Empty-after-filter: cuando la lista visible queda vacía tras filtrar, mostrar heading "Ningún resultado con estos filtros" + body "Ajusta o quita filtros para ver más proyectos." (copy LOCKED). El island NUNCA falla por red (no consulta) — no maneja error de red.

    Accesibilidad (LOCKED): todo chip/toggle es `<button type="button">` real, tab-alcanzable, Enter/Space; `aria-pressed` refleja el estado; `min-h-11` (44px) en todos; `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-product`. Tokens/utilidades solamente (cero-hex), Tailwind 4 `bg-[var(--t)]` sin re-wrap en `hsl()` (gotcha v8.1). Font-size solo vía `text-sm`/`text-xs`/`text-base`.

    NOTA: este plan NO monta el island en page.tsx (eso es plan 03) — entrega el componente y sus tests. El linter anti-insinuación se extiende sobre el archivo en el plan 03.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run components/buscar-filtros.test.tsx; pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `buscar-filtros.tsx` tiene `"use client"` y NO importa `@/lib/supabase` (grep 0 hits)
    - Leyenda de counts contiene el string exacto "Conteos sobre estos N resultados, no sobre todo el corpus."
    - Leyenda de reglas contiene el string exacto "Orden por relevancia de la búsqueda; en empates se priorizan los mensajes del Ejecutivo y lo más reciente."
    - Facetas count-0 llevan `disabled` + `aria-disabled="true"` (no removidas)
    - Chips/toggles activos llevan `aria-pressed="true"`; todos son `<button type="button">` con `min-h-11`
    - `pnpm exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>Island filtra/reordena en memoria, counts honestos, facetas vacías deshabilitadas, sin_dato visible, orden explicable, partido opcional no-renderizado; tsc limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests jsdom de buscar-filtros (estructura/estado/facetas/orden/aria)</name>
  <read_first>
    - app/components/red/red-graph.test.tsx (patrón jsdom: estructura, transiciones de estado de filtro, sin getComputedStyle)
    - app/components/buscar-filtros.tsx (el componente de la Task 1)
    - MEMORY gotcha: jsdom rompe `new URL(import.meta.url)` → usar `import.meta.dirname` si se lee fixture de disco
  </read_first>
  <behavior>
    - Render con fixture de ≥10 filas mixtas (buckets variados, un sin_dato, un anio null, mensajes y mociones)
    - Assert: cada chip de faceta muestra su count correcto sobre el fixture
    - Assert: la leyenda de counts y la leyenda de reglas están presentes con el texto exacto
    - Assert: activar/desactivar una faceta cambia la lista filtrada (transición de aria-pressed y del conteo de tarjetas)
    - Assert: una faceta con count 0 aparece con disabled + aria-disabled="true"
    - Assert: el bucket sin_dato y el año Sin dato están visibles
    - Assert: "Más recientes" reordena por año desc con null al final; "Mensajes primero" agrupa Mensaje antes que Moción
    - Assert: filtros que vacían la lista muestran "Ningún resultado con estos filtros"
    - Assert: fixture sin ningún partido → no hay grupo de faceta de partido en el DOM
  </behavior>
  <action>
    Crear `app/components/buscar-filtros.test.tsx` (jsdom, patrón `red-graph.test.tsx`). Fixture EN MEMORIA de `BuscarSliceRow[]` (no leer de disco si es evitable; si se lee, `import.meta.dirname`). Cubrir los asserts del bloque behavior con `@testing-library/react` / fireEvent (el patrón que use red-graph.test.tsx). Sin getComputedStyle ni gates visuales (van a Phase 89). Los asserts son de estructura DOM, estado de filtro/orden, atributos aria, y copy exacto.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run components/buscar-filtros.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ≥9 casos que cubren counts, ambas leyendas exactas, transición de faceta, disabled count-0, sin_dato/Sin dato visibles, los 3 modos de orden, empty-after-filter, y ausencia del grupo partido
    - `pnpm exec vitest run components/buscar-filtros.test.tsx` verde
    - Cero uso de getComputedStyle
  </acceptance_criteria>
  <done>Suite jsdom verde cubriendo facetas, counts honestos, orden explicable, aria-pressed, disabled, sin_dato y forward-compat partido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → island (client) | El island recibe el slice serializado como props; NUNCA cruza a Supabase ni a la red (contrato FichaRail) → superficie de ataque mínima |
| texto de fuente → render | Título/materia de fuente se renderiza en chips/tarjetas → potencial XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | Information Disclosure | island que consulta Supabase filtra datos/keys al cliente | mitigate | Contrato FichaRail: cero import de `@/lib/supabase`; el guard PII (lockdown-guard) escanea `.from`/`.rpc` en todo app/; test asserta ausencia del import |
| T-88-05 | Tampering (XSS) | texto de fuente (título/materia) inyecta HTML en el render | mitigate | Escape por defecto de React (JSX interpolation); PROHIBIDO `dangerouslySetInnerHTML` en el island |
| T-88-06 | Denial of Service | slice gigante degrada el filtrado en cliente | accept | El slice está capado aguas-arriba (≤20-50 resultados por página, page.tsx MAX_PAGE); el island no amplía el cap |
| T-88-07 | Repudiation | count presentado como global engaña sobre cobertura | mitigate | Leyenda LOCKED "Conteos sobre estos N resultados, no sobre todo el corpus." + test que la fija |
| T-88-SC | Tampering | npm/pip/cargo installs | mitigate | CERO instalaciones nuevas (todas las primitivas shadcn/Radix ya presentes; sin registry nuevo — UI-SPEC §Registry Safety) |
</threat_model>

<verification>
- `pnpm exec vitest run components/buscar-filtros.test.tsx` verde
- `pnpm exec tsc --noEmit` limpio
- `buscar-filtros.tsx` no importa `@/lib/supabase` (contrato FichaRail)
- Sin `dangerouslySetInnerHTML`
- Cero hex, Tailwind 4 `[var(--t)]` sin re-wrap hsl(), font-size vía tokens/utilidades
</verification>

<success_criteria>
- FILT-01: filtros año/iniciativa/estado/cámara reordenan/filtran sin re-buscar
- FILT-02: counts honestos "de estos N", facetas vacías deshabilitadas, sin_dato explícito
- RANK-01: orden explicable mensaje>moción + recencia por reglas declaradas, nunca ML/score
- Faceta partido no renderizada, tipo la acepta opcional (forward-compat BIO-03)
</success_criteria>

<output>
Create `.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-02-SUMMARY.md` when done
</output>
