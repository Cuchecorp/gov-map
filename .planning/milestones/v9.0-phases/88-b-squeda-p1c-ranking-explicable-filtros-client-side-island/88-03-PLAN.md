---
phase: 88-b-squeda-p1c-ranking-explicable-filtros-client-side-island
plan: 03
type: execute
wave: 3
depends_on: ["88-01", "88-02"]
files_modified:
  - app/app/buscar/page.tsx
  - app/components/search-result-card.tsx
  - app/lib/anti-insinuacion-guard.test.ts
autonomous: true
requirements: [FILT-01, FILT-02, RANK-01, FILT-03]

must_haves:
  truths:
    - "El server component /buscar enriquece cada fila del slice con estadoBucket + año derivado del primer tramitacion_evento + iniciativa, preservando el rank del retrieval"
    - "El año se deriva del min(fecha) de tramitacion_evento por boletín; sin evento → año null (nunca de fecha_captura ni del sufijo del boletín)"
    - "Un fallo del read de tramitacion_evento se maneja como error honesto (nunca ?? [] que enmascare como 'sin dato')"
    - "El island BuscarFiltros recibe el slice enriquecido y se monta en /buscar"
    - "Cada SearchResultCard muestra un chip neutro Mensaje/Moción + año; jamás score/cosine/rank"
    - "El linter anti-insinuación escanea buscar-filtros.tsx y buscar/page.tsx y muerde sobre términos como ranking/score/afinidad"
  artifacts:
    - path: "app/app/buscar/page.tsx"
      provides: "enriquecimiento del slice + montaje del island"
      contains: "tramitacion_evento"
    - path: "app/components/search-result-card.tsx"
      provides: "chip Mensaje/Moción + año, sin score"
    - path: "app/lib/anti-insinuacion-guard.test.ts"
      provides: "SUPERFICIES_BUSQUEDA en el bucle de escaneo"
      contains: "SUPERFICIES_BUSQUEDA"
  key_links:
    - from: "app/app/buscar/page.tsx"
      to: "app/lib/estado-bucket.ts"
      via: "import estadoBucket + deriveAnio"
      pattern: "estadoBucket|deriveAnio"
    - from: "app/app/buscar/page.tsx"
      to: "tramitacion_evento"
      via: ".from('tramitacion_evento') read no-PII"
      pattern: "from\\(\"tramitacion_evento\"\\)"
    - from: "app/app/buscar/page.tsx"
      to: "app/components/buscar-filtros.tsx"
      via: "<BuscarFiltros slice={…} />"
      pattern: "BuscarFiltros"
    - from: "app/lib/anti-insinuacion-guard.test.ts"
      to: "buscar-filtros.tsx + buscar/page.tsx"
      via: "SUPERFICIES_BUSQUEDA en el loop de escaneo"
      pattern: "SUPERFICIES_BUSQUEDA"
---

<objective>
Cablear todo: enriquecer el slice server-side en `/buscar/page.tsx` (año desde el primer `tramitacion_evento`, estadoBucket, iniciativa), montar el island `BuscarFiltros` del plan 02, extender `SearchResultCard` con el chip neutro Mensaje/Moción + año, y extender el linter anti-insinuación para que MUERDA sobre los archivos nuevos (`SUPERFICIES_BUSQUEDA`).

Purpose: cierra FILT-01/FILT-02/RANK-01 en la superficie real (el island necesita el slice enriquecido) y FILT-03 (el normalizador se consume aquí). Directiva dura del orquestador: el año se deriva de `min(tramitacion_evento.fecha)` por boletín (patrón actualidad-module), JAMÁS de fecha_captura ni del sufijo del boletín; sin evento Ingreso → año null. El linter anti-insinuación banea `ranking`/`score` — los archivos nuevos DEBEN entrar en la lista SUPERFICIES para que el guard muerda.

Output: `app/app/buscar/page.tsx` (MODIFY), `app/components/search-result-card.tsx` (MODIFY), `app/lib/anti-insinuacion-guard.test.ts` (MODIFY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-CONTEXT.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-UI-SPEC.md
@.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-PATTERNS.md
@app/app/buscar/page.tsx
@app/components/search-result-card.tsx
@app/lib/estado-bucket.ts
@app/components/buscar-filtros.tsx

<interfaces>
<!-- Contratos consumidos, ya en el repo cuando este plan corre. -->

De app/lib/estado-bucket.ts (88-01): `estadoBucket(value: string | null): EstadoBucket`, `deriveAnio(fechaIso: string | null): number | null`, `ETIQUETA_BUCKET`.
De app/lib/types.ts (88-01): `BuscarSliceRow` (camelCase; `partido?` opcional).
De app/components/buscar-filtros.tsx (88-02): `export function BuscarFiltros({ slice }: { slice: BuscarSliceRow[] }): JSX.Element` — island client.

Hidratación actual a preservar (page.tsx:126-150) — el punto de enriquecimiento:
```typescript
const sb = createServerSupabase();
const boletines = pageSlice.map((v) => v.boletin);
const { data: proyectos, error: proyectosError } = await sb
  .from("proyecto").select("*").in("boletin", boletines);
if (proyectosError) { console.error(...); return <banner honesto/>; }   // ← disciplina de error a espejar
const porBoletin = new Map<string, ProyectoRow>(((proyectos as ProyectoRow[]) ?? []).map((p) => [p.boletin, p]));
const ordenados = pageSlice.map((v) => porBoletin.get(v.boletin)).filter((p): p is ProyectoRow => p !== undefined);
// ⚠ PRESERVAR este orden rank-preserving — es el sort primario (RANK-01)
```

Read del año — copiar el analog (actualidad-module.tsx:376-389):
```typescript
const { data, error } = await sb
  .from("tramitacion_evento").select("*")
  .in("boletin", boletinesCandidatos)
  .order("fecha", { ascending: true });
if (error) { throw new Error(`… no se pudo leer tramitacion_evento: ${error.message}`); }
const eventos = (data as TramitacionEventoRow[] | null) ?? [];   // solo tras haber lanzado en error
```
Tomar el evento MÁS TEMPRANO (min fecha) por boletín como proxy de fecha de ingreso → `deriveAnio()`. `.in("boletin", …)` sobre ≤20-50 boletines no toca el cap PostgREST 1k → sin `.range()`.

SearchResultCard actual (search-result-card.tsx) — props y header:
```typescript
export interface SearchResultCardProps {
  boletin: string; titulo: string; materia: string | null;
  estado: string | null; camaraOrigen: string | null; provenance: ProvenanceBadgeProps;
}
// header: <EtapaBadge/> <CamaraChip/> <span font-mono>Boletín N°{boletin}</span>
// JSDoc §5 LOCKED: NUNCA cercanía coseno, porcentaje ni rank
```

Linter — bucle de escaneo (anti-insinuacion-guard.test.ts:298):
```typescript
for (const rel of [...SUPERFICIES_VOTO, ...SUPERFICIES_MONEY, ...SUPERFICIES_HOME]) { … }
```
Términos prohibidos incluyen: `ranking`, `score`, `puntaje`, `índice`, `afinidad` (líneas 152+). Archivos ausentes se saltan sin fallar (patrón try/catch:301-306).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el linter anti-insinuación con SUPERFICIES_BUSQUEDA</name>
  <read_first>
    - app/lib/anti-insinuacion-guard.test.ts:93-141 (arrays SUPERFICIES_*), :298 (bucle de escaneo), :152+ (términos: ranking/score/afinidad)
    - .planning/phases/88-*/88-PATTERNS.md §Guards That Will Bite (directiva SUPERFICIES_BUSQUEDA)
  </read_first>
  <action>
    En `app/lib/anti-insinuacion-guard.test.ts`, añadir un array `SUPERFICIES_BUSQUEDA: string[] = ["components/buscar-filtros.tsx", "app/buscar/page.tsx"]` (rutas relativas a app/, mismo formato que los otros arrays), con un JSDoc que explique por qué (la fase de "ranking explicable" tienta usar `ranking`/`score`/`índice`/`afinidad` — todos baneados). Incluirlo en el bucle de escaneo de la sección (1): cambiar `[...SUPERFICIES_VOTO, ...SUPERFICIES_MONEY, ...SUPERFICIES_HOME]` a incluir `...SUPERFICIES_BUSQUEDA`. La tolerancia a archivo ausente (try/catch existente) ya cubre el caso de que este plan corra antes de que existan (no ocurrirá: dependen de 02, pero mantener la robustez). Hacer esto PRIMERO para que muerda sobre el copy que escriben las tasks siguientes.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `SUPERFICIES_BUSQUEDA` existe con exactamente `["components/buscar-filtros.tsx", "app/buscar/page.tsx"]`
    - El bucle de la sección (1) itera también sobre `...SUPERFICIES_BUSQUEDA`
    - `pnpm exec vitest run lib/anti-insinuacion-guard.test.ts` verde (incluyendo el mutation self-check que prueba que el guard muerde)
  </acceptance_criteria>
  <done>El linter escanea los dos archivos nuevos de búsqueda; sin offenders (el copy es factual); mutation self-check verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enriquecer el slice en page.tsx (año desde tramitacion_evento) + montar el island</name>
  <read_first>
    - app/app/buscar/page.tsx:83-204 (función Resultados: hidratación, orden rank-preserving, disciplina de error honesto)
    - app/components/actualidad-module.tsx:376-389 (read tramitacion_evento con .in/.order/throw-on-error)
    - app/lib/estado-bucket.ts (estadoBucket, deriveAnio)
    - app/components/buscar-filtros.tsx (island BuscarFiltros)
    - .planning/phases/88-*/88-UI-SPEC.md §Year facet (LOCKED: min fecha tramitacion_evento; nunca fecha_captura ni sufijo boletín)
    - .planning/phases/88-*/88-PATTERNS.md §page.tsx (honest read-error discipline; nunca ?? [])
  </read_first>
  <behavior>
    - Con boletines hidratados, se lee tramitacion_evento para esos boletines y se toma el min(fecha) por boletín → deriveAnio
    - Un boletín sin ningún tramitacion_evento → anio null (fila cae en bucket "Sin dato" del año, nunca de fecha_captura)
    - Un error del read de tramitacion_evento produce el mismo banner honesto que la hidratación (nunca ?? [] silencioso)
    - El slice enriquecido preserva el orden rank del retrieval (sort primario RANK-01)
    - <BuscarFiltros slice={…} /> recibe filas con estadoBucket, anio, iniciativa, camaraOrigen, fecha, boletin, titulo
  </behavior>
  <action>
    En `app/app/buscar/page.tsx`, tras la hidratación existente (que se PRESERVA con su orden rank-preserving y su disciplina de error), añadir un read de `tramitacion_evento` copiando el analog de `actualidad-module.tsx`: `sb.from("tramitacion_evento").select("*").in("boletin", boletines).order("fecha", { ascending: true })`. Manejar el error con la MISMA disciplina honesta que la hidratación (`console.error` + banner destructivo; NUNCA `?? []` que enmascare el fallo como "sin dato"). Construir un `Map<string, string>` boletín→min(fecha) tomando el primer evento por boletín (ya vienen ordenados asc por fecha).

    Construir el `BuscarSliceRow[]` a partir de `ordenados` (que ya preserva el rank): por cada fila `p`, `{ boletin: p.boletin, titulo: p.titulo, anio: deriveAnio(minFechaPorBoletin.get(p.boletin) ?? null), iniciativa: normalizarIniciativa(p.iniciativa), estadoBucket: estadoBucket(p.estado ?? p.etapa), camaraOrigen: p.camara_origen, fecha: minFechaPorBoletin.get(p.boletin) ?? null }`. `normalizarIniciativa`: mapear el texto libre de `p.iniciativa` a `"Mensaje" | "Moción" | null` (honest null si no reconoce ninguno de los dos — nunca fabricar). Montar `<BuscarFiltros slice={sliceEnriquecido} />` en el render, por encima o junto a la lista de `SearchResultCard` según el layout del UI-SPEC (§Filter panel layout: desktop arriba/al lado, móvil apilado). Mantener el `SearchResultCard` list existente como el render de resultados; el island opera sobre el mismo slice.

    NOTA sobre orden con el island: el orden primario sigue siendo el rank del retrieval (page.tsx ya lo preserva); el island reordena OPT-IN en cliente (RANK-01). No mover el sort primario al cliente.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run; pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contiene `.from("tramitacion_evento")` con `.in("boletin", …)` y `.order("fecha", { ascending: true })`
    - El error del read produce banner honesto (grep: no aparece `tramitacion_evento` seguido de `?? []` para tragar el fallo)
    - El año se deriva vía `deriveAnio(min fecha)`; grep confirma que NO se usa `fecha_captura` para el año del slice
    - `<BuscarFiltros` montado en page.tsx con el slice enriquecido
    - `pnpm exec tsc --noEmit` limpio; suite completa verde
  </acceptance_criteria>
  <done>Slice enriquecido con año honesto (min tramitacion_evento, null sin evento), island montado, orden rank preservado, error honesto; tsc limpio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chip Mensaje/Moción + año en SearchResultCard (sin score)</name>
  <read_first>
    - app/components/search-result-card.tsx (props actuales, header, JSDoc §5 no-score LOCKED)
    - app/components/ui/badge.tsx (Badge variant="outline")
    - .planning/phases/88-*/88-UI-SPEC.md §Order toggle + ranking (per-result explanatory labels: chip Mensaje/Moción + año, NO score)
  </read_first>
  <behavior>
    - Card con iniciativa "Mensaje" muestra un Badge outline "Mensaje"; con "Moción" muestra "Moción"; con null no muestra chip de iniciativa (honest)
    - Card con anio 2023 muestra un Badge outline "2023"; con anio null muestra "Sin dato" o no muestra chip de año (según la regla honesta del UI-SPEC — elegir el mismo tratamiento sin_dato visible)
    - NUNCA se renderiza score/cosine/rank/número
  </behavior>
  <action>
    Extender `SearchResultCardProps` con `iniciativa?: "Mensaje" | "Moción" | null` y `anio?: number | null` (opcionales para no romper otros call-sites). En el header (junto a EtapaBadge/CamaraChip/boletín) añadir chips neutros `Badge variant="outline"` (`border-transparent bg-muted text-muted-foreground`, patrón EtapaBadge wrapper): uno para iniciativa (Mensaje/Moción; omitir si null — honest), uno para el año (mostrar el año, o el bucket "Sin dato" visible si null, coherente con la regla sin_dato del UI-SPEC). Preservar la HARD RULE §5: NUNCA superficie score/cosine/rank/número. Actualizar el call-site en page.tsx para pasar `iniciativa` y `anio` desde el slice enriquecido de la Task 2.
  </action>
  <verify>
    <automated>cd app; pnpm exec vitest run components/search-result-card; pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SearchResultCardProps` tiene `iniciativa?` y `anio?` opcionales
    - El header renderiza chips neutros outline para iniciativa (omitido si null) y año (Sin dato visible si null)
    - Grep: cero aparición de score/cosine/similarity/rank en el render de la card
    - Call-site en page.tsx pasa iniciativa y anio; `pnpm exec tsc --noEmit` limpio
  </acceptance_criteria>
  <done>SearchResultCard con chips explicativos neutros Mensaje/Moción + año, sin score; call-site actualizado; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → Supabase (service_role) | page.tsx lee `proyecto` + `tramitacion_evento` (reads públicos no-PII) bajo service_role; ninguna tabla PII, ningún cambio de allowlist |
| texto de fuente → render | título/materia/estado se renderiza en chips/tarjetas → potencial XSS |
| copy de la fase → linter | el copy nuevo de ranking/filtros debe pasar el linter anti-insinuación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-08 | Information Disclosure | read de tabla PII o RPC no-allowlisted en page.tsx | mitigate | Solo `.from("proyecto")` y `.from("tramitacion_evento")` — NINGUNA en PII_TABLES (parlamentario/donante/cruce_senal…); cero `.rpc()` nuevo; lockdown-guard escanea app/ y confirma |
| T-88-09 | Tampering (XSS) | título/materia/estado de fuente inyecta HTML | mitigate | Escape por defecto de React; PROHIBIDO `dangerouslySetInnerHTML` |
| T-88-10 | Repudiation | año fabricado desde fecha_captura/sufijo boletín se muestra como año del proyecto | mitigate | año solo vía `deriveAnio(min tramitacion_evento)`; null si no hay evento; grep-gate confirma que fecha_captura no alimenta el año del slice |
| T-88-11 | Elevation of Privilege | copy insinuante (ranking/score/afinidad) escapa al linter porque los archivos no están en scope | mitigate | `SUPERFICIES_BUSQUEDA` añadido al bucle del linter (Task 1) — el guard muerde sobre buscar-filtros.tsx + buscar/page.tsx; mutation self-check prueba que muerde |
| T-88-12 | Denial of Service | read tramitacion_evento sin cota | accept | `.in("boletin", …)` sobre ≤20-50 boletines de la página (page.tsx MAX_PAGE cap upstream); no toca el cap PostgREST 1k |
| T-88-SC | Tampering | npm/pip/cargo installs | mitigate | CERO instalaciones nuevas (primitivas shadcn ya presentes; sin registry nuevo) |
</threat_model>

<verification>
- `pnpm exec vitest run` (suite completa) verde, incluyendo anti-insinuacion-guard con SUPERFICIES_BUSQUEDA
- `pnpm exec tsc --noEmit` limpio
- Grep: page.tsx no deriva año de `fecha_captura`; no traga error de tramitacion_evento con `?? []`
- Grep: SearchResultCard no superficie score/cosine/rank
- Sin `dangerouslySetInnerHTML`; sin `.rpc()` nuevo; sin tabla PII
</verification>

<success_criteria>
- FILT-03: el normalizador se consume en page.tsx (estadoBucket sobre estado/etapa)
- FILT-01/FILT-02: island montado sobre el slice enriquecido → filtros y counts operan en la superficie real
- RANK-01: orden primario = rank del retrieval preservado server-side; chips explicativos Mensaje/Moción + año en cada card, sin score
- Directiva de año honesto respetada (min tramitacion_evento, nunca fecha_captura ni sufijo boletín); sin evento → "Sin dato"
- Linter anti-insinuación muerde sobre los archivos nuevos
</success_criteria>

<output>
Create `.planning/phases/88-b-squeda-p1c-ranking-explicable-filtros-client-side-island/88-03-SUMMARY.md` when done
</output>
