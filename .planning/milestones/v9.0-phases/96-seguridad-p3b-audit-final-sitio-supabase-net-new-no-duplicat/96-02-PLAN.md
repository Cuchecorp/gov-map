---
phase: 96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md
autonomous: true
requirements: [SEC-03]
tags: [security, database, supabase, rls, allowlist, pgvector, audit]

must_haves:
  truths:
    - "La DB VIVA (no las migraciones) queda auditada: 0 funciones de app ejecutables por anon, 0 tablas de app con grant a anon/authenticated, 0 tablas con RLS deshabilitada, 0 policies to anon — todo con el filtro pg_depend deptype='e' que excluye el ruido pgTAP"
    - "El allowlist repo (26) se re-deriva contra pg_proc VIVO: las 2 secdef fuera de allowlist quedan documentadas como inertes-por-diseño (68-03), cero drift no-explicado"
    - "pgvector vivo queda medido (0.8.0) y el gap ≥0.8.2 (CVE-2026-3172) queda declarado como handoff de operador — el agente NO fuerza el update"
    - "Los golden gates de identidad (adjudicación + cruces + gate DIPID votos) se re-corren verdes con counts citados — la correctitud ES la defensa legal"
  artifacts:
    - path: ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md"
      provides: "Reporte de auditoría de la DB viva (N/M + queries verbatim + allowlist drift + pgvector gap + golden gate)"
      contains: "pg_depend"
  key_links:
    - from: ".env SUPABASE_DB_URL"
      to: "Postgres PROD"
      via: "psql read-only (set -a source, jamás imprimir)"
      pattern: "PGCLIENTENCODING=UTF8 psql"
    - from: "96-AUDIT-DB-VIVA.md"
      to: "app/lib/lockdown-guard.test.ts PUBLIC_RPC_ALLOWLIST"
      via: "re-derivación secdef vivos vs 26 allowlist"
      pattern: "prosecdef"
---

<objective>
Auditar la SUPABASE DB VIVA (SEC-03, porción DB): grants/RLS reales sobre PROD (no migraciones), re-derivación del RPC allowlist contra `pg_proc` vivo, versión de pgvector, y re-verificación de los golden gates de identidad. Todo read-only, cero DDL, jamás imprimir la connection string.

Purpose: Los guards de Phase 95 revisan MIGRACIONES; drift entre repo y PROD (una RPC viva no en migraciones, un grant residual a anon) solo se caza contra la DB viva. Bajo Camino A el cliente público es `service_role` (bypassa RLS) → el allowlist repo es la última línea; debe estar en sync con la superficie ejecutable real. La correctitud del dato (golden gates) es la defensa legal.
Output: `96-AUDIT-DB-VIVA.md` (N/M declarado, queries verbatim con filtro pg_depend, allowlist drift documentado, pgvector gap → handoff, golden gate counts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-RESEARCH.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-PATTERNS.md

<interfaces>
<!-- Idioms LOCKED verificados esta sesión. El executor los re-corre para confirmar cero deriva. -->

psql read-only (JAMÁS imprimir la URL/password):
```bash
DB_URL=$(node -e "const fs=require('fs');console.log(fs.readFileSync('.env','utf8').match(/^SUPABASE_DB_URL=(.+)$/m)[1].trim())")
PGCLIENTENCODING=UTF8 psql "$DB_URL" -tA -c "<query>"
```

Filtro pg_depend OBLIGATORIO en toda query de funciones/grants (excluye las 1200+ funciones y 28 grants de pgTAP):
```sql
and not exists (select 1 from pg_depend d where d.objid = p.oid and d.deptype = 'e')
```

Query allowlist drift (verificada → 25 secdef vivos de app):
```sql
select p.proname from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public' and p.prosecdef = true
and not exists (select 1 from pg_depend d where d.objid = p.oid and d.deptype = 'e')
order by 1;
```

Query pgvector:
```sql
select extversion from pg_extension where extname = 'vector';                       -- → 0.8.0
select default_version from pg_available_extensions where name = 'vector';          -- → 0.8.0 (no ≥0.8.2)
```

PUBLIC_RPC_ALLOWLIST vive en `app/lib/lockdown-guard.test.ts:165-192` (26 entradas). Las 2 secdef vivas FUERA del allowlist esperadas: `rebeldias_de_parlamentario`, `tasa_ausencia_comparada` (carril de voto PODADO, 68-03 — inertes-por-diseño, NO agregar al allowlist, NO dropear).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grants/RLS/allowlist sobre la DB VIVA (SEC-03)</name>
  <files>.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md</files>
  <read_first>
    - 96-RESEARCH.md §"DB viva — todo VERDE en la superficie de app" (los 4 counts esperados: 0/0/0/0) y §"Allowlist drift" (25 secdef vivos, 23 en allowlist, 2 invoker legítimas, 2 inertes-por-diseño)
    - 96-PATTERNS.md §"psql read-only idiom" + §"pg_depend filter" (el patrón MÁS importante del audit) + §"96-AUDIT-*.md" (estructura del reporte)
    - app/lib/lockdown-guard.test.ts:165-192 (las 26 entradas de PUBLIC_RPC_ALLOWLIST para el cruce)
  </read_first>
  <action>
    Cargar `SUPABASE_DB_URL` con el idiom `node -e ... readFileSync('.env')` (JAMÁS `echo`/`log` la URL). Correr read-only, cero DDL, las queries de research VERBATIM con el filtro `pg_depend deptype='e'` inline:
    (a) funciones public EXECUTABLE por anon/authenticated → esperado 0; (b) tablas de app con grant a anon/authenticated → esperado 0; (c) tablas public con RLS deshabilitada → esperado 0; (d) policies `to anon` en public → esperado 0. Si algún count ≠ 0 tras el filtro: es un HALLAZGO real → documentarlo con la fila ofensora (nombre de objeto, no datos) y marcarlo BLOCKING en el reporte. No fabricar el "0"; reportar lo que la query devuelva.
    Re-derivar el allowlist: correr la query secdef-vivos (25 filas esperadas), cruzar contra las 26 entradas de `PUBLIC_RPC_ALLOWLIST`. Documentar: 23 en allowlist Y secdef vivas, 2 en allowlist pero INVOKER (`buscar_citaciones`, `match_proyectos` — PII-safe reads, legítimas), 2 secdef vivas NO en allowlist (`rebeldias_de_parlamentario`, `tasa_ausencia_comparada` — inertes-por-diseño 68-03, confirmar count anon-executable=0). Cualquier RPC secdef viva no explicada por estas categorías = drift real → BLOCKING.
    Escribir `96-AUDIT-DB-VIVA.md` (estructura analog 93-AUDITORIA): "Principio rector" (psql directo, nunca REST, cero valor impreso), tabla N/M por ítem con el filtro citado, queries verbatim copiables, sección allowlist drift con las 4 categorías. Correr Splinter/lints de Supabase disponibles vía psql (queries de pg_catalog para security advisors) y anexar findings con severidad+triage si los hay.
  </action>
  <verify>
    <automated>test -f ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md" && grep -c "pg_depend" ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md"</automated>
  </verify>
  <acceptance_criteria>
    - `96-AUDIT-DB-VIVA.md` existe con tabla N/M de los 4 checks de app, cada uno con su count real y el filtro `pg_depend deptype='e'` citado inline
    - La sección allowlist drift documenta las 4 categorías con las 2 secdef inertes-por-diseño nombradas y su count anon-executable=0
    - Toda query mostrada es copiable (verbatim) e incluye el filtro pg_depend en las de funciones/grants
    - El reporte NO contiene la connection string ni ningún valor de secreto (grep por `postgresql://` con password real = 0; el template `<password>` está OK)
    - Si algún check devolvió ≠0, está marcado BLOCKING con el objeto ofensor (sin datos)
  </acceptance_criteria>
  <done>DB viva auditada, 0 offenders de app confirmados con el filtro pgTAP, allowlist sin drift no-explicado, Splinter anexado.</done>
</task>

<task type="auto">
  <name>Task 2: pgvector version → handoff + golden gates de identidad (SEC-03)</name>
  <files>.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-DB-VIVA.md</files>
  <read_first>
    - 96-RESEARCH.md §"pgvector — GAP real, resuelto por handoff" (0.8.0 vivo, default_version topa en 0.8.0, alter extension no alcanza ≥0.8.2) y §"Open Questions #1" (CVE-2026-3172 exposición no verificada — clasificar con la matización de 0 funciones anon-executable)
    - 96-RESEARCH.md §"Validation Architecture → golden gate" (packages/adjudication/src/golden/golden-set.test.ts, packages/cruces/src/golden/golden-set.test.ts, gate DIPID votos)
  </read_first>
  <action>
    Correr read-only `select extversion from pg_extension where extname='vector'` y `select default_version from pg_available_extensions where name='vector'`. Si vivo <0.8.2 Y default_version <0.8.2 (esperado 0.8.0/0.8.0): documentar en `96-AUDIT-DB-VIVA.md` que el update ≥0.8.2 NO es alcanzable por `alter extension` en la plataforma gestionada → es un platform Postgres upgrade = handoff de operador (el Plan 03 lo consolida en 96-OPERATOR-HANDOFF.md). NO correr `alter extension vector update` (falla silenciosamente en 0.8.0 y sería DDL en una fase read-only). Si al re-correr HOY la plataforma YA publica ≥0.8.2 (research pidió no asumir), documentarlo y dejar el update como acción de operador igualmente (DDL sobre PROD gestionado). Consultar el advisory oficial de CVE-2026-3172 (WebFetch a la fuente de pgvector/GitHub) para clasificar exposición real: si el vector requiere ejecutar SQL arbitrario que anon NO puede (0 funciones anon-executable confirmado en Task 1), matizar que la exposición práctica es baja y el handoff es "cuando Supabase publique ≥0.8.2".
    Re-correr los golden gates de identidad: `pnpm -r --filter "./packages/*" test` (incluye adjudication golden-set + cruces golden + gate DIPID votos). Citar counts (N tests verdes por golden set) en el reporte. Un golden gate rojo = HALLAZGO BLOCKING (regresión de correctitud = riesgo legal).
    Anexar a `96-AUDIT-DB-VIVA.md`: sección pgvector (versión + gap + exposición matizada + ruta de handoff) y sección golden gate (counts verdes).
  </action>
  <verify>
    <automated>pnpm -r --filter "./packages/*" test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r --filter "./packages/*" test` pasa (golden sets adjudication + cruces + gate DIPID verdes) con counts citables
    - `96-AUDIT-DB-VIVA.md` tiene la sección pgvector con extversion real, default_version, y la declaración de handoff (NO se corrió `alter extension`)
    - La exposición de CVE-2026-3172 queda clasificada con la matización de 0 funciones anon-executable (o marcada como pendiente de advisory si WebFetch falla)
    - La sección golden gate cita los counts de tests verdes
  </acceptance_criteria>
  <done>pgvector medido y su gap declarado como handoff de operador (sin forzar DDL); golden gates verdes con counts — la correctitud queda re-verificada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente público → DB (service_role) | Camino A: el sitio usa service_role que BYPASSA RLS; el allowlist repo es la única barrera contra RPCs no previstas |
| anon → DB | anon debe estar muerto (0 funciones/grants) post-0044/0045; verificar contra lo VIVO, no migraciones |
| extensión pgvector → runtime DB | CVE en la extensión de vectores; el update es platform-level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-05 | Elevation of Privilege | RPC secdef viva no-allowlisted bajo service_role | mitigate | re-derivación allowlist vs pg_proc VIVO; 2 secdef fuera = inertes-por-diseño (68-03) con anon-executable=0; cero drift no-explicado |
| T-96-06 | Information Disclosure | grant residual a anon / RLS deshabilitada en PROD | mitigate | queries live con filtro pg_depend; 0 offenders de app confirmados contra lo vivo (no migraciones) |
| T-96-07 | DoS / (por clasificar) | pgvector 0.8.0 (CVE-2026-3172) | accept + transfer | plataforma no ofrece ≥0.8.2 → handoff operador (platform upgrade); exposición práctica baja (0 funciones anon-executable); agente NO fuerza DDL |
| T-96-08 | Tampering (correctitud=difamación) | golden gate de identidad | mitigate | re-correr adjudicación + cruces + gate DIPID votos; rojo = BLOCKING |
| T-96-09 | Information Disclosure | connection string en logs/reporte | mitigate | idiom `node -e readFileSync('.env')`; JAMÁS echo/log de DB_URL; reporte sin valores |
</threat_model>

<verification>
- Los 4 checks de app (funciones anon / grants tabla / RLS / policies) = 0 con filtro pg_depend, o BLOCKING documentado
- allowlist drift: 25 secdef vivos cruzados vs 26 repo, 2 inertes-por-diseño documentadas
- pgvector: extversion real citada, gap ≥0.8.2 → handoff (sin DDL)
- `pnpm -r --filter "./packages/*" test` verde con counts
- `96-AUDIT-DB-VIVA.md` sin connection string ni valores
</verification>

<success_criteria>
SEC-03 (porción DB): Splinter + grants/RLS sobre la DB VIVA (0 offenders de app), re-derivación del allowlist (cero drift no-explicado), pgvector medido con gap declarado como handoff, golden gate de identidad re-verificado verde. La porción `pnpm audit` de SEC-03 la cubre el Plan 01; el handoff consolidado (pgvector + B26) lo cierra el Plan 03.
</success_criteria>

<output>
Create `.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-02-SUMMARY.md` when done
</output>
