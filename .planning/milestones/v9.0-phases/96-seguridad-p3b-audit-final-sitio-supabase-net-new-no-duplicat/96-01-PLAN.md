---
phase: 96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/env-example-guard.test.ts
  - .gitleaks.toml
  - app/package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-REPO.md
autonomous: true
requirements: [SEC-02, SEC-03]
tags: [security, secrets, dependencies, audit]

must_haves:
  truths:
    - "gitleaks corre sobre TODO el historial git; los findings quedan triados (FP con razón) y CERO valor de secreto impreso"
    - ".env.example queda asertado como solo-placeholders por un guard vitest que MUERDE ante un valor con forma de secreto real"
    - "pnpm audit --prod termina sin advisories tras el bump de Next ≥16.2.11 + overrides brace-expansion/protobufjs/sharp"
    - "La suite de app y el tsc siguen verdes tras el bump de Next (el bundle real lo valida el Plan 03)"
  artifacts:
    - path: "app/lib/env-example-guard.test.ts"
      provides: "Guard placeholder-only de .env.example (detector puro + mutation self-check)"
      contains: "detectarValorNoPlaceholder"
    - path: ".gitleaks.toml"
      provides: "Allowlist de los 4 FP para scan futuro limpio"
      contains: "[allowlist]"
    - path: "pnpm-workspace.yaml"
      provides: "Overrides transitivos de seguridad"
      contains: "brace-expansion"
    - path: ".planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-REPO.md"
      provides: "Reporte de auditoría del repo público (gitleaks triage + audit fix)"
      contains: "gitleaks"
  key_links:
    - from: "app/lib/env-example-guard.test.ts"
      to: ".env.example"
      via: "readFileSync desde REPO_ROOT"
      pattern: "ENV_EXAMPLE"
    - from: "pnpm-workspace.yaml"
      to: "pnpm-lock.yaml"
      via: "pnpm install re-lock"
      pattern: "overrides"
---

<objective>
Auditar y endurecer el REPO PÚBLICO (SEC-02) + higiene de dependencias (SEC-03 porción `pnpm audit`): scan de secretos sobre todo el historial git con triage de falsos positivos (cero valor impreso), guard vitest de `.env.example` solo-placeholders, y fix de `pnpm audit` (bump Next ≥16.2.11 + overrides transitivos) dejando la suite verde.

Purpose: El repo es PÚBLICO con sujetos hostiles (Pitfall 12). Un secreto en el historial, un valor real filtrado en `.env.example`, o una dependencia con CVE de SSRF/DoS son vectores directos de Information Disclosure / DoS. Esta fase VERIFICA sobre lo vivo y cierra el único ítem sucio confirmado por research (`pnpm audit` = 14 advisories).
Output: `96-AUDIT-REPO.md` (reporte con N/M + triage), `app/lib/env-example-guard.test.ts` (guard nuevo en CI), `.gitleaks.toml` (allowlist FP), `pnpm-workspace.yaml` + `app/package.json` + `pnpm-lock.yaml` (audit limpio).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-RESEARCH.md
@.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-PATTERNS.md

<interfaces>
<!-- Analog del guard: patrón verificado en repo. El executor lo copia estructuralmente. -->

Analog `app/lib/money-antiflip-guard.test.ts` (anclas de ruta):
```typescript
import { readdirSync, readFileSync, statSync } from "node:fs";
import path from "node:path";
import { describe, expect, it } from "vitest";

const APP_ROOT = process.cwd();                      // app/ — vitest de app/ corre desde app/
const REPO_ROOT = path.resolve(APP_ROOT, "..");      // raíz del monorepo
const ENV_EXAMPLE = path.join(REPO_ROOT, ".env.example");
```

`.env.example` — valores NO-vacíos que son LEGÍTIMOS (config, no secretos) y el detector NO debe morder:
  BACKFILL_ITERATIONS=1
  MONEY_PUBLIC_ENABLED=false
  ADMIN_REVISION_ENABLED=false
  PUBLIC_INDEXABLE=false
  SERVEL_CRUDO_BUCKET=crudo-servel
  SUPABASE_DB_URL comentario-template: postgresql://postgres:<password>@<host>:5432/postgres (angle-brackets = placeholder)

Formatos de secreto real que el detector SÍ debe morder: `sb_secret_*`, JWT `eyJ*`, hex ≥32 chars, connection string `postgresql://user:<algo-no-placeholder>@host`, R2 key con `.r2.cloudflarestorage.com`.

pnpm 11 overrides — viven en `pnpm-workspace.yaml` bajo `overrides:` (NO en package.json). Bloque actual:
```yaml
overrides:
  postcss: "^8.5.10"
  esbuild: "^0.28.1"
  uuid: "^11.1.1"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gitleaks full-history scan + triage + .gitleaks.toml (SEC-02)</name>
  <files>.gitleaks.toml, .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-REPO.md</files>
  <read_first>
    - 96-RESEARCH.md §"gitleaks — 4 findings, TODOS falsos positivos" (tabla de los 4 FP: rule, file, commit, veredicto)
    - 96-PATTERNS.md §".gitleaks.toml" (shape gitleaks 8.x + paths allowlisted) y §"96-AUDIT-*.md" (estructura del reporte, analog 93-AUDITORIA)
  </read_first>
  <action>
    Correr `gitleaks git --redact --report-format json --report-path gitleaks-report.json` sobre TODO el historial (repo root). REGLA LOCKED: `--redact` SIEMPRE; JAMÁS imprimir ni copiar el valor de un secreto — solo file, commit, rule-id.
    Triar cada finding contra la tabla de research (research verificó 4 FP: 3 constantes de test `S3CR3T-TICKET-*` en packages/dinero + 1 fixture HTML `camara-citaciones-semana.html`). Si aparece un finding NUEVO no en la tabla de research, NO descartarlo: documentarlo como candidato de rotación (sin valor) y marcarlo para el handoff del Plan 03 — B26 sigue siendo el único secreto real ya-expuesto conocido.
    Crear `.gitleaks.toml` en repo root con `[allowlist]` que liste los 4 paths FP (per 96-PATTERNS.md). Re-correr `gitleaks git --redact --config .gitleaks.toml` → debe dar 0 findings.
    Iniciar `96-AUDIT-REPO.md` (estructura analog 93-AUDITORIA: "Principio rector" arriba, N/M declarado, sección gitleaks con tabla de triage redactada). Borrar `gitleaks-report.json` al terminar (no commitear el JSON crudo — puede contener contexto de línea; el reporte redactado es la salida).
  </action>
  <verify>
    <automated>gitleaks git --redact --config .gitleaks.toml ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `gitleaks git --redact --config .gitleaks.toml` sale con 0 findings (exit 0)
    - `.gitleaks.toml` existe en repo root con `[allowlist]` y los 4 paths FP
    - `96-AUDIT-REPO.md` tiene la sección gitleaks con los findings triados y CERO valor de secreto (grep del reporte por `sb_secret_`/`eyJ`/`S3CR3T` real = 0; los identificadores de rule/nombre-de-constante como texto están OK)
    - `gitleaks-report.json` NO queda en el árbol de trabajo
  </acceptance_criteria>
  <done>Historial escaneado, 4 FP triados con razón, `.gitleaks.toml` deja el scan futuro limpio, reporte sin valores.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard vitest .env.example solo-placeholders (SEC-02)</name>
  <files>app/lib/env-example-guard.test.ts</files>
  <read_first>
    - app/lib/money-antiflip-guard.test.ts (analog EXACTO: anclas APP_ROOT/REPO_ROOT, detector puro exportado, mutation self-check §4)
    - .env.example (los 5 valores config legítimos que NO deben morder — ver <interfaces>)
    - 96-PATTERNS.md §"app/lib/env-example-guard.test.ts" (detector puro + allowlist de booleanos/slugs + self-check)
  </read_first>
  <behavior>
    - Test: `.env.example` real → `detectarValorNoPlaceholder(src)` retorna [] (0 offenders — la línea de base viva pasa)
    - Test (MUERDE): `SUPABASE_SECRET_KEY=sb_secret_realvalueXXXXXXX` → offenders > 0
    - Test (MUERDE): `SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5c...` (JWT) → offenders > 0
    - Test (MUERDE): hex ≥32 chars como valor → offenders > 0
    - Test (MUERDE): connection string con password real (no `<...>`) → offenders > 0
    - Test (NO MUERDE): `MONEY_PUBLIC_ENABLED=false`, `BACKFILL_ITERATIONS=1`, `SERVEL_CRUDO_BUCKET=crudo-servel`, `PUBLIC_INDEXABLE=false` → []
    - Test (NO MUERDE): template placeholder `SUPABASE_DB_URL=postgresql://postgres:<password>@<host>:5432/postgres` (angle-brackets) → []
    - Sanity: el archivo `.env.example` leído es no-vacío (no es un escaneo vacío)
  </behavior>
  <action>
    Crear `app/lib/env-example-guard.test.ts` espejando `money-antiflip-guard.test.ts`. Exportar `detectarValorNoPlaceholder(envSrc: string): string[]` PURO: por cada línea `KEY=value` (ignorar `#` y vacías), morder si `value` matchea un formato de secreto real (sb_secret_, `eyJ` JWT, hex ≥32, connection string con password que NO sea `<placeholder>`, R2 key). Allowlistar valores config: booleanos (`false`/`true`), enteros, slugs conocidos (`crudo-servel`), y templates con angle-brackets (`<password>`/`<host>` marca placeholder).
    NO incluir `walkSourceFiles` (este guard solo lee `.env.example`, no barre el árbol). NO `stripTsComments` (`.env` no tiene comentarios TS). Dos secciones: (1) aserción contra archivo real = 0 offenders; (2) mutation self-check en memoria (arriba). El guard SOLO LEE Y AFIRMA — nunca edita `.env.example`.
  </action>
  <verify>
    <automated>cd app && pnpm test env-example-guard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./app test env-example-guard` pasa (todos los `it` verdes)
    - El detector muerde en los ≥4 casos de secreto real y NO muerde en los ≥6 casos config/template
    - El detector está EXPORTADO (`export function detectarValorNoPlaceholder`)
    - 0 offenders contra el `.env.example` real actual
  </acceptance_criteria>
  <done>Guard nuevo en CI (entra gratis en la suite de app), muerde ante secretos reales, no colisiona con `MONEY_PUBLIC_ENABLED=false` (ya asertado por money-antiflip).</done>
</task>

<task type="auto">
  <name>Task 3: pnpm audit fix — bump Next ≥16.2.11 + overrides + re-lock (SEC-03)</name>
  <files>app/package.json, pnpm-workspace.yaml, pnpm-lock.yaml, .planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-AUDIT-REPO.md</files>
  <read_first>
    - pnpm-workspace.yaml (bloque `overrides:` actual — postcss/esbuild/uuid; APPEND, no reemplazar)
    - 96-RESEARCH.md §"pnpm audit — SUCIO hoy (14 advisories)" (tabla: 8 Next + brace-expansion/protobufjs/sharp) y §Installation
    - 96-PATTERNS.md §"pnpm-workspace.yaml + app/package.json" (gotcha allowBuilds placeholder + secuencia de verificación 260715-bvd)
  </read_first>
  <action>
    ANTES de fijar pins: correr `npm view next version`, `npm view brace-expansion version`, `npm view protobufjs version`, `npm view sharp version` para confirmar las últimas versiones parcheadas disponibles (research fijó Next ≥16.2.11, brace-expansion ≥2.1.2, protobufjs ≥7.6.5, sharp ≥0.35.0 — confirmar compat de Next con React 19.2).
    Desde `app/`: `pnpm add next@^16.2.11` (o el pin exacto confirmado). Añadir al bloque `overrides:` de `pnpm-workspace.yaml` (APPEND): `brace-expansion`, `protobufjs`, `sharp` con los pins confirmados. Re-lock con `pnpm install`. GOTCHA (260715-bvd): si pnpm 11 inyecta un placeholder `allowBuilds:` en `pnpm-workspace.yaml`, removerlo antes de commitear (el repo usa `onlyBuiltDependencies`, no `allowBuilds`; protobufjs/sharp ya están listados ahí).
    Correr `pnpm audit --prod` → 0 advisories. Correr `cd app && pnpm test` (suite app completa) + `tsc --noEmit` para confirmar que el bump de Next NO rompe. Si el bump rompe la suite/tsc: documentar el fallo exacto en `96-AUDIT-REPO.md`, evaluar pin exacto (16.2.11 en vez de ^) y re-verificar — NO dejar la suite roja. El bundle real (OpenNext/Docker) lo valida el Plan 03.
    Registrar en `96-AUDIT-REPO.md`: tabla antes/después (14 advisories → 0), versiones pineadas, y nota de que el deploy del Plan 03 valida el build OpenNext.
  </action>
  <verify>
    <automated>pnpm audit --prod ; echo "audit_exit=$?" && cd app && pnpm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm audit --prod` reporta 0 advisories (ni high ni moderate)
    - `pnpm-lock.yaml` no contiene ninguna versión de `next` en el rango vulnerable (grep `next@16\.(0|1|2\.([0-9]|10)\b)` = vacío; ≥16.2.11)
    - `pnpm-workspace.yaml` tiene `brace-expansion`, `protobufjs`, `sharp` en `overrides:` sin `allowBuilds:` placeholder espurio
    - `cd app && pnpm test` pasa y `npx tsc --noEmit` sale con 0 errores
    - `96-AUDIT-REPO.md` tiene la tabla antes/después del audit con versiones pineadas
  </acceptance_criteria>
  <done>Audit limpio, Next parcheado, transitivos por override, suite+tsc verdes; el build real queda para el deploy del Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo público → mundo | Todo el historial git es visible; un secreto commiteado alguna vez es leído por cualquiera |
| `.env.example` → repo | Plantilla commiteada; un valor real filtrado ahí es disclosure |
| dep tree → runtime | Dependencias transitivas con CVE (SSRF/DoS) ejecutan en el Worker de prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information Disclosure | historial git (repo público) | mitigate | gitleaks full-history `--redact`; 4 FP triados; `.gitleaks.toml` deja scan futuro limpio; secretos reales → tabla de rotación (handoff Plan 03), NUNCA se imprime valor |
| T-96-02 | Information Disclosure | `.env.example` | mitigate | guard vitest `detectarValorNoPlaceholder` muerde ante formato de secreto real; corre en CI de app |
| T-96-03 | DoS / SSRF | Next.js 16.2.9 (< parcheado) | mitigate | bump a ≥16.2.11 cierra 8 advisories (middleware bypass, SSRF Server Actions, DoS, cache confusion) |
| T-96-04 | DoS | brace-expansion/protobufjs/sharp transitivos | mitigate | overrides en pnpm-workspace.yaml a versiones parcheadas |
| T-96-SC | Tampering | pnpm install (bump Next + overrides) | mitigate | solo bumps de paquetes YA en el lockfile (next/brace-expansion/protobufjs/sharp — años de historia, millones de descargas); slopcheck N/A per 96-RESEARCH §Package Legitimacy Audit; NO hay descubrimiento de paquetes nuevos |
</threat_model>

<verification>
- `gitleaks git --redact --config .gitleaks.toml` → 0 findings
- `pnpm --filter ./app test env-example-guard` → verde
- `pnpm audit --prod` → 0 advisories
- `cd app && pnpm test` + `npx tsc --noEmit` → verdes
- `96-AUDIT-REPO.md` con gitleaks triage + audit antes/después, cero valores de secreto
</verification>

<success_criteria>
SEC-02 (repo público): scan de secretos historial completo triado sin valores + `.env.example` guard placeholder-only en CI. SEC-03 (porción audit): `pnpm audit --prod` limpio con suite+tsc verdes. Errores genéricos y CSP quedan para el Plan 03 (verificación empírica en deploy).
</success_criteria>

<output>
Create `.planning/phases/96-seguridad-p3b-audit-final-sitio-supabase-net-new-no-duplicat/96-01-SUMMARY.md` when done
</output>
