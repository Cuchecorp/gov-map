---
phase: 86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga
plan: 03
type: execute
wave: 3
depends_on: ["86-01", "86-02"]
files_modified:
  - packages/fichas/src/spike/retrieval-cli.ts
  - packages/fichas/src/spike/retrieval-golden.live.test.ts
  - packages/fichas/vitest.live.config.ts
  - packages/fichas/vitest.config.ts
  - packages/fichas/src/spike/embed-cache.json
  - .planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md
autonomous: false
requirements: [RETR-03, RETR-04]

must_haves:
  truths:
    - "El CLI corre las 3 estrategias sobre el golden set y emite una tabla estrategia × categoría"
    - "El golden set corre como test de regresión permanente env-gated (SKIP honesto sin credenciales)"
    - "La suite normal de fichas EXCLUYE el .live.test.ts (no se colecta en CI)"
    - "La decisión queda registrada: algoritmo, pesos, rrf_k, límite de candidatos, plan de flag"
    - "El cache query→vector queda committeado para reproducibilidad"
  artifacts:
    - path: "packages/fichas/src/spike/retrieval-cli.ts"
      provides: "CLI tsx: parseArgs → 3 estrategias → reporte markdown"
      contains: "parseArgs"
    - path: "packages/fichas/src/spike/retrieval-golden.live.test.ts"
      provides: "gate de regresión env-gated (describe.skip sin env)"
      contains: "describe.skip"
    - path: "packages/fichas/vitest.live.config.ts"
      provides: "config live (include src/**/*.live.test.ts)"
      contains: "live.test.ts"
    - path: "packages/fichas/vitest.config.ts"
      provides: "suite normal con exclude de *.live.test.ts (MODIFICADO)"
      contains: "live.test.ts"
    - path: ".planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md"
      provides: "tabla estrategia × categoría + decisión registrada"
      contains: "estrategia"
  key_links:
    - from: "packages/fichas/src/spike/retrieval-cli.ts"
      to: "packages/fichas/src/spike/strategies.ts"
      via: "import runFtsOnly/runSemanticOnly/runRrf + evaluarRetrieval"
      pattern: "from \"\\./strategies"
    - from: "packages/fichas/src/spike/retrieval-golden.live.test.ts"
      to: "GOLDEN_SET + evaluarRetrieval"
      via: "import del golden set + scorer"
      pattern: "GOLDEN_SET"
    - from: "packages/fichas/vitest.config.ts"
      to: "*.live.test.ts"
      via: "exclude glob"
      pattern: "live\\.test\\.ts"
---

<objective>
Cerrar el spike: CLI que corre las 3 estrategias sobre el golden set y emite la tabla `estrategia × categoría` (hit@1/hit@5/MRR), test de regresión permanente env-gated (SKIP honesto sin credenciales) con su `vitest.live.config.ts`, la MODIFICACIÓN de la suite normal para excluir el live test, la corrida LIVE contra PROD (medición reproducible + cobertura de embeddings), y el registro de la DECISIÓN (algoritmo, pesos, `rrf_k`, límite de candidatos, plan de flag). El cache de vectores queda committeado.

Purpose: Es el entregable de la fase — la decisión de retrieval elegida por evidencia (RETR-03) y el gate de regresión permanente (RETR-04). Sin dominación demostrada sobre el golden set, no hay rewire en fase 87.
Output: CLI + live test + 2 configs vitest + cache committeado + `86-SCORING.md` con la tabla y la decisión. Read-only estricto contra PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-CONTEXT.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-PATTERNS.md
@.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-RESEARCH.md

<interfaces>
<!-- Analogs verificados: -->
<!--   CLI:        packages/fichas/src/pipeline-cli.ts (parseArgs + *ArgsError + entry-point guard regex sobre process.argv[1]) -->
<!--   live test:  packages/votos/src/spike-votacion-detalle.live.test.ts (LIVE gate + loadEnv BOM-safe + findWorkspaceRoot) -->
<!--   live config: packages/votos/vitest.live.config.ts (include src/**/*.live.test.ts, testTimeout 120_000) -->
<!--   run wrapper: scripts/run-with-env.mjs -->

De 86-01/86-02 (ya existentes):
  import { GOLDEN_SET, type CasoRetrieval } from "./golden-set";
  import { evaluarRetrieval, type MetricasRetrieval } from "./score";
  import { runFtsOnly, runSemanticOnly, runRrf } from "./strategies";
  import { getCachedEmbeddings } from "./embed-cache";
  import { embedQuery } from "./embed-query";
  import { runSql, probeUnaccent } from "./psql";

vitest.live.config.ts (copiar de votos):
  include: ["src/**/*.live.test.ts"]; exclude: ["**/node_modules/**"]; passWithNoTests: true; testTimeout: 120_000;

vitest.config.ts (MODIFICAR — hoy NO excluye live; línea 6 es solo include):
  include: ["src/**/*.test.ts"], exclude: ["**/node_modules/**", "**/*.live.test.ts"], passWithNoTests: true

live gate + loadEnv (copiar de votos):
  import { findWorkspaceRoot } from "@obs/tramitacion";
  const LIVE = !!process.env.SUPABASE_DB_URL && !!process.env.GEMINI_API_KEY;
  (LIVE ? describe : describe.skip)("retrieval golden — hybrid dominates baseline", () => { ... });

Comando de corrida (BOM-safe .env):
  node scripts/run-with-env.mjs pnpm --filter @obs/fichas exec tsx src/spike/retrieval-cli.ts --report 86-SCORING.md
Comando live gate:
  SUPABASE_DB_URL=… GEMINI_API_KEY=… pnpm --filter @obs/fichas exec vitest run --config vitest.live.config.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI de scoring + live regression test + configs vitest</name>
  <files>packages/fichas/src/spike/retrieval-cli.ts, packages/fichas/src/spike/retrieval-golden.live.test.ts, packages/fichas/vitest.live.config.ts, packages/fichas/vitest.config.ts</files>
  <read_first>
    - packages/fichas/src/pipeline-cli.ts (parseArgs + SpikeCliArgsError + entry-point guard regex sobre process.argv[1]; líneas ~60-118 y ~244-269)
    - packages/votos/src/spike-votacion-detalle.live.test.ts (LIVE gate, loadEnv BOM-safe, findWorkspaceRoot; líneas ~22-56)
    - packages/votos/vitest.live.config.ts (copiar near-verbatim)
    - packages/fichas/vitest.config.ts (MODIFICAR: hoy include-only, agregar exclude de *.live.test.ts)
    - packages/fichas/src/spike/{golden-set,score,strategies,embed-cache,embed-query,psql}.ts (de 86-01/02)
  </read_first>
  <action>
    Crear `retrieval-cli.ts` espejando `pipeline-cli.ts`: `export class SpikeCliArgsError`, `export function parseArgs(argv)` con flags `--report <path>`, `--rrf-k <n>` (default 50), `--limit <n>` (default 50, el grid 20/50/100 se corre variando este flag), `--w-fts`/`--w-sem` (default 1), fail-fast en valor faltante ANTES de tocar red/DB. `main()`: probe unaccent, cargar `GOLDEN_SET`, embeber queries vía `getCachedEmbeddings(GOLDEN_SET.map(c=>c.query), embedQuery, cachePath)`, correr `evaluarRetrieval` una vez por estrategia (FTS-solo, semántico-solo, RRF), y escribir una tabla markdown `estrategia × categoría` (hit@1/hit@5/MRR por categoría + agregado + `detalle[]`) al `--report`. Incluir la cobertura de embeddings (count proyecto vs proyecto_embedding via runSql — evidencia pro-híbrido). Entry-point guard con regex `retrieval-cli\.(ts|js)$` sobre `process.argv[1]`; try/catch parse → `exit(2)`; run → `exit(0/1)`. READ-ONLY: sin writer, sin upsert.
    Crear `retrieval-golden.live.test.ts` espejando el live test de votos: `const LIVE = !!process.env.SUPABASE_DB_URL && !!process.env.GEMINI_API_KEY;` `(LIVE ? describe : describe.skip)(...)`; `loadEnv` BOM-safe + `findWorkspaceRoot` de `@obs/tramitacion`; el test corre las 3 estrategias sobre `GOLDEN_SET` con el cache committeado y ASSERTA que la estrategia ganadora ≥ baseline (`match_proyectos` piso 0.59) en literal/boletín Y ≥ baseline en NL/similares (no regresión). `testTimeout` a nivel config.
    Crear `packages/fichas/vitest.live.config.ts` copiando de votos (include `src/**/*.live.test.ts`, testTimeout 120_000).
    MODIFICAR `packages/fichas/vitest.config.ts`: agregar `exclude: ["**/node_modules/**", "**/*.live.test.ts"]` para que la suite normal NO colecte el live test.
  </action>
  <verify>
    <automated>pnpm --filter @obs/fichas exec vitest run && pnpm --filter @obs/fichas exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - Suite normal de fichas verde y NO colecta `retrieval-golden.live.test.ts` (el describe.skip no aparece porque el exclude lo saca; verificar con `pnpm --filter @obs/fichas exec vitest run --reporter=verbose` que no lista el archivo live)
    - `grep -n "live.test.ts" packages/fichas/vitest.config.ts` ≥1 (exclude agregado)
    - `grep -n "include.*live.test.ts" packages/fichas/vitest.live.config.ts` ≥1
    - `grep -n "describe.skip\|LIVE =" packages/fichas/src/spike/retrieval-golden.live.test.ts` ≥1 (env-gate)
    - `grep -n "parseArgs\|SpikeCliArgsError" packages/fichas/src/spike/retrieval-cli.ts` ≥1
    - `grep -c "writer\|upsert\|\.insert\|\.update" packages/fichas/src/spike/retrieval-cli.ts` == 0 (read-only, sin escritura)
    - `tsc -b` limpio
  </acceptance_criteria>
  <done>CLI emite tabla estrategia × categoría; live test env-gated (skip sin env); vitest.live.config.ts creado; vitest.config.ts excluye el live test; suite normal verde; tsc limpio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Corrida LIVE del spike contra PROD + cache committeado</name>
  <what-built>
    El CLI `retrieval-cli.ts` corre las 3 estrategias (FTS-solo, semántico-solo, RRF) sobre el golden set congelado contra PROD (READ-ONLY: SELECT + `match_proyectos` RPC, cero DDL/DML), embebe las queries con Gemini `RETRIEVAL_QUERY` y las cachea a `embed-cache.json` committeable, y emite la tabla `estrategia × categoría` (hit@1/hit@5/MRR) más la cobertura de embeddings. Esta corrida necesita `SUPABASE_DB_URL` + `GEMINI_API_KEY` reales del `.env` — de ahí el checkpoint (CI no tiene DB, skipea honesto).
  </what-built>
  <how-to-verify>
    1. Desde la raíz del repo, correr:
       `node scripts/run-with-env.mjs pnpm --filter @obs/fichas exec tsx src/spike/retrieval-cli.ts --report .planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md`
    2. Si el probe `select unaccent('Ñuñoa')` falla (extensión ausente), el CLI degrada a FTS sin unaccent y lo documenta en el reporte — eso es evidencia esperada, no un error.
    3. Correr el grid: repetir con `--limit 20`, `--limit 100`, y `--rrf-k 30`/`--rrf-k 70` para tener los vecinos; anotar cuál combinación domina.
    4. Verificar que `packages/fichas/src/spike/embed-cache.json` quedó escrito con vectores (solo floats, cero claves/URLs).
    5. Correr el gate live: `node scripts/run-with-env.mjs pnpm --filter @obs/fichas exec vitest run --config vitest.live.config.ts` → debe pasar (ganador ≥ baseline en literal/boletín Y no regresión en NL/similares). Si NO domina, es un hallazgo válido: registrar y NO forzar el assert.
    6. Confirmar que NINGÚN log imprimió la Gemini key ni el `SUPABASE_DB_URL`.
  </how-to-verify>
  <resume-signal>
    Escribe "medido" con: (a) la estrategia ganadora + rrf_k + límite elegidos, (b) la cobertura de embeddings LIVE (proyecto vs proyecto_embedding), (c) si unaccent estaba habilitado, (d) confirmación de que el cache quedó committeado; o describe el bloqueo (p.ej. rol sin permiso de lectura, unaccent ausente sin fallback claro).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Registrar la decisión + congelar el golden set</name>
  <files>.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md, packages/fichas/src/spike/golden-set.ts, packages/fichas/src/spike/embed-cache.json</files>
  <read_first>
    - .planning/phases/86-.../86-SCORING.md (generado por la corrida live en Task 2)
    - .planning/phases/86-.../86-CONTEXT.md (sección "Registro de decisión + regresión permanente": qué debe registrarse)
    - packages/fichas/src/spike/golden-set.ts (para el congelamiento final tras confirmar expected LIVE)
  </read_first>
  <action>
    Completar `86-SCORING.md` con la sección DECISIÓN: (1) algoritmo elegido (RRF FTS∪semántico con short-circuit de boletín, o el que la evidencia dicte), (2) pesos A/B/C, (3) `rrf_k` elegido, (4) límite de candidatos por rama (20/50/100), (5) cobertura de embeddings LIVE medida (no asumida), (6) plan de flag: `match_proyectos` se CONSERVA; la híbrida entra tras flag en fase 87 hasta dominar el golden set; sin dominación no hay rewire (gate de 87 explícito). Registrar el criterio de victoria cumplido: arregla literal/boletín Y no regresiona NL/similares.
    Si la corrida live obligó a ajustar algún `expected` boletín del golden set (porque el boletín canónico difería del real en PROD), aplicar el ajuste AHORA y anotar la razón en el `nota` del caso + en `86-SCORING.md` (disciplina "cambio con decisión registrada, no edición silenciosa"). Tras esto, el golden set queda CONGELADO — dejar el comentario de cabecera explícito.
    Confirmar que `embed-cache.json` está committeado (reproducibilidad + no re-pega a Gemini en CI).
  </action>
  <verify>
    <automated>grep -c "rrf_k\|pesos\|flag\|match_proyectos\|CONGELADO" ".planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-SCORING.md"</automated>
  </verify>
  <acceptance_criteria>
    - `86-SCORING.md` contiene la tabla estrategia × categoría (hit@1/hit@5/MRR) Y la sección DECISIÓN con: algoritmo, pesos A/B/C, rrf_k, límite de candidatos, cobertura embeddings LIVE, plan de flag
    - `grep -n "gate.*87\|dominaci\|no hay rewire" 86-SCORING.md` ≥1 (gate de 87 explícito)
    - `grep -n "CONGELADO" packages/fichas/src/spike/golden-set.ts` ≥1 y cualquier ajuste de expected tiene su `nota` justificando
    - `packages/fichas/src/spike/embed-cache.json` existe y está trackeado por git (`git status --porcelain` no lo lista como untracked tras commit)
    - `pnpm --filter @obs/fichas exec vitest run` sigue verde (golden set congelado no rompe los units)
  </acceptance_criteria>
  <done>Decisión registrada en 86-SCORING.md (algoritmo/pesos/rrf_k/límite/flag/gate-87); golden set congelado con cualquier ajuste justificado; cache committeado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/live test → PROD DB (owner URL) | Read-only estricto; el gate live corre contra PROD |
| reporte/fixture/cache commiteados → repo público | No deben contener PII ni secretos |
| CI runner → live test | Debe SKIPEAR sin credenciales (no fallar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-09 | Tampering | corrida live contra PROD | mitigate | El harness (86-02) es SELECT-only con guarda; la corrida no ejecuta DDL/DML. Checkpoint humano confirma cero escritura. |
| T-86-10 | Info Disclosure | 86-SCORING.md + embed-cache.json commiteados | mitigate | Reporte = boletines + queries públicas + métricas; cache = floats. Cero PII, cero claves, cero SUPABASE_DB_URL. Revisión en Task 3. |
| T-86-11 | Denial of Service | live test en CI | accept | El test SKIPEA sin env (describe.skip) y el exclude lo saca de la suite normal → CI nunca lo corre sin credenciales; riesgo nulo. |
| T-86-12 | Info Disclosure | logs del CLI | mitigate | Gemini key por header nunca logueada; SUPABASE_DB_URL nunca impreso (heredado de 86-02). Checkpoint verifica. |
</threat_model>

<verification>
- `pnpm --filter @obs/fichas exec vitest run` verde y NO colecta el live test
- `pnpm test` (root) verde (sin regresión en app 991 / packages 1103)
- Live gate: `… vitest run --config vitest.live.config.ts` pasa CON env, skipea SIN env
- `86-SCORING.md` con tabla + decisión; `embed-cache.json` committeado; golden set congelado
- Cero DDL/DML contra PROD; cero secreto/PII en artefactos commiteados
</verification>

<success_criteria>
- CLI emite tabla estrategia × categoría reproducible (RETR-03)
- Golden set como test de regresión permanente env-gated, SKIP honesto sin env, excluido de la suite normal (RETR-04)
- Decisión registrada: algoritmo, pesos, rrf_k, límite de candidatos, plan de flag, gate de 87 (RETR-03/04)
- Cache de vectores committeado; golden set congelado
</success_criteria>

<output>
Create `.planning/phases/86-b-squeda-p1a-spike-retrieval-h-brido-golden-set-congelado-ga/86-03-SUMMARY.md` when done
</output>
