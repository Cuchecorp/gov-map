---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 03
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "El nº de cruces del boletín 14309-04 mostrado por el deploy cuadra con cruces_de_proyecto en PROD"
    - "El nº de cruces de la ficha de D1165 cuadra con cruces_de_parlamentario, y S1338 muestra cero como cero"
    - "Cada señal del panel de actualidad tiene su query verbatim y su conteo comparado con el DOM"
    - "Ninguna señal con cero filas se presenta rellena ni se oculta"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md"
      provides: "Grupos 3 y 4: cruces de ficha/proyecto + panel de actualidad"
      contains: "| veredicto |"
  key_links:
    - from: "122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md"
      to: "RPC actualidad_senales_panel (0066) sobre actualidad_senal (0065)"
      via: "una query por señal comparada con el DOM de /"
      pattern: "actualidad_senales_panel"
---

<objective>
Recalcular por SQL contra PROD los grupos 3 y 4: los cruces de ficha y de proyecto (0047–0050) y las
señales del panel de actualidad (0065/0066), y comparar cada número con el deploy real.

Purpose: son los cruces materializados por cron — donde el número mostrado puede quedar rezagado
respecto de la base sin que nada falle visiblemente.
Output: `122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md` (fragmento; lo consolida el plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md
@./CLAUDE.md

Régimen LOCKED: psql read-only con `set -a; source .env; set +a` +
`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "…"`; JAMÁS imprimir el valor de
`SUPABASE_DB_URL`; conteos por `psql -tA` nunca por REST; cero PII; cero requests a fuentes
gubernamentales; lectura del deploy por `curl -s` al Worker; NO se toca código en este plan (los
fixes son 122-05); NO se hace deploy; no se tocan flags `*_PUBLIC_ENABLED`.

Doble lectura obligatoria por cruce: (a) la MISMA RPC invocada por psql, (b) la query de primeros
principios contra las tablas base. Divergencia entre (a) y (b) ES un hallazgo aunque el DOM coincida
con (a).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cruces de proyecto y de parlamentario (0047-0050)</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md</files>
  <read_first>
    - `app/components/cruces-de-proyecto.tsx` (línea 199 `const n = rows.length`; línea 282 la
      llamada `sb.rpc("cruces_de_proyecto", …)`; cómo se rinde el conteo y el `capturedAt`)
    - `app/components/cruces-de-parlamentario.tsx` (el conteo rendido y `fechaCorta(item.fecha)`)
    - `app/lib/parlamentario-resumen-conteos.ts` líneas 359-392 (`agruparSectores`,
      `crucesTotal = crucesFilas.length`, `derivarEstado({ total, ingestado: true })`)
    - Las migraciones del carril: localizarlas con
      `grep -rln "cruces_de_parlamentario\|cruces_de_proyecto\|cruce_senal" supabase/migrations`
      (NO por rango numérico: las definiciones están repartidas). Las conocidas al planificar:
      `0039_cruce_senal.sql` (tabla), `0040_cruces_rpc.sql` + `0041_cruces_rpc_fecha_captura.sql`
      (`cruces_de_parlamentario`), `0049_cruces_de_proyecto.sql` (`cruces_de_proyecto`),
      `0052_cruce_senal_lobby_sector_aporte.sql` (tipo de señal `lobby_sector_aporte`). Leer la
      definición VIGENTE de cada RPC (la última migración que la redefine manda)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §1.3 (boletín
      `14309-04`, 47 cruces esperados) y las filas E-044 / E-053 de §3.0
  </read_first>
  <action>
Crear el fragmento (front-matter `fase: 122`, `fragmento: 02-cruces-actualidad`) con:

`## 1. Cruces de proyecto` — sujeto **14309-04**. Filas de la plantilla §0.2 con:
(a) `select count(*) from cruces_de_proyecto('14309-04')`; (b) la query de primeros principios contra
`cruce_senal` (o la tabla base que la función agrega) reproduciendo su filtro tal como lo define la
migración; (c) el nº mostrado por el deploy:
`curl -s https://observatorio-congreso.thevalis.workers.dev/proyecto/14309-04` extrayendo el conteo
del bloque de cruces (registrar el `grep`/`sed` exacto usado).
Verificar si el RPC es bounded (LIMIT) y si el componente muestra `rows.length` en vez de un total
honesto — `cruces-de-proyecto.tsx:199` usa `rows.length`: si el conteo real supera el LIMIT del RPC,
el número mostrado ES el cap ⇒ `discrepancia-corregida` (si el fix cabe en código en 122-05) o
`discrepancia-declarada` (si exige emitir un `total_n` nuevo, o sea SQL). Registrar ambos números.
Añadir un segundo sujeto de contraste: el boletín con el MAYOR conteo de `cruces_de_proyecto` en PROD
hoy (query de selección con `order by … desc, boletin asc limit 1`) — es el que revienta el cap si lo hay.

`## 2. Cruces de parlamentario` — sujetos **D1165** (11 cruces según 113 §1.1) y **S1338** (0 cruces,
vacío honesto). Las mismas 3 lecturas por sujeto. Para S1338 registrar el fragmento de DOM que
demuestra que el cero se presenta COMO cero (estado `vacio` → "sin registros"), no como ausencia de la
sección ni como "—" (`no_ingerido`): la distinción 3-estado de `derivarEstado` es load-bearing.
Verificar la agregación por sector (`agruparSectores`): la suma de `nReuniones` por sector debe
igualar `sum(conteo) where tipo_senal='lobby_sector'` en SQL; `nVotos` debe ser 0 hoy (no existe
`tipo_senal` de voto) — un `nVotos > 0` en el DOM es discrepancia.
  </action>
  <verify>
    <automated>grep -q "## 2. Cruces de parlamentario" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md &amp;&amp; grep -c "cruces_de_proyecto" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md</automated>
  </verify>
  <acceptance_criteria>
    - §1 tiene ≥2 sujetos (14309-04 + el de mayor conteo) con nº SQL, nº deploy y veredicto
    - §2 tiene D1165 y S1338 con nº SQL, nº deploy y veredicto; el caso S1338 incluye el fragmento de
      DOM literal que prueba el cero-como-cero
    - Queda escrito si `cruces_de_proyecto` / `cruces_de_parlamentario` son bounded y si el componente
      muestra `.length` o un total honesto
    - Cada fila referencia un bloque ```sql numerado del mismo archivo
    - Ningún archivo de `app/` o `supabase/` modificado; cero `postgres://`; cero PII
  </acceptance_criteria>
  <done>Cruces de ficha y de proyecto recalculados con ambos números y veredicto por fila.</done>
</task>

<task type="auto">
  <name>Task 2: Panel de actualidad — una query por señal contra el DOM de /</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md</files>
  <read_first>
    - `app/components/panel-actualidad.tsx` (contrato de la fila en `:30-45`: `conteo`, `fecha_max`,
      `cobertura_camara`, `ventana`, `cluster_id`; `:274-295` la llamada a `actualidad_senales_panel`
      y el orden anti-ranking; `fuenteLabel` y `claseCamara`)
    - `supabase/migrations/0065_actualidad_senal.sql` (tabla materializada, proc de rebuild ACOTADO,
      los tipos temporales y los 3 defectos LOCKED: filtro `fecha <= current_date`, `regexp_replace`
      de las dos grafías de cámara, cámara NULL → `(sin cámara)`; supresión-como-fila)
    - `supabase/migrations/0066_actualidad_rpc.sql` (RPC bounded `actualidad_senales_panel(p_tipo)`,
      LIMIT 200, orden neutro)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` fila E-055 de §3.0
      (emisor vigente) y §3.0.1 (E-008 `actualidad-module.tsx` es HUÉRFANO — NO auditarlo)
  </read_first>
  <action>
Añadir al fragmento:

`## 3. Panel de actualidad — señales` — enumerar los `tipo_senal` REALES con
`select tipo_senal, count(*), max(fecha_max) from actualidad_senal group by 1 order by 1;`
(no asumir la lista de 6: el universo sale de la base; si el conteo observado difiere de 6, se
registra la diferencia). Por cada tipo, una fila de la plantilla §0.2 con:
(a) `select * from actualidad_senales_panel('<tipo>')` — conteo y cobertura emitidos por la RPC;
(b) la query de primeros principios que el proc de 0065 materializa para ese tipo (transcribirla
desde la migración, no reinventarla), corrida en vivo — el punto es detectar **rezago del
materializado** respecto de las tablas fuente (`votacion`, `tramitacion_evento`, `citacion`,
`sesion_tabla_item`, `proyecto_embedding`);
(c) el nº que muestra el deploy: `curl -s https://observatorio-congreso.thevalis.workers.dev/`
extrayendo conteo y chip de cobertura por tile (registrar el comando exacto).

Reglas de veredicto específicas de este grupo:
- Si (a) == (c) pero (b) difiere de (a) ⇒ el sitio es fiel al materializado y el materializado está
  rezagado: veredicto `discrepancia-declarada`, con la magnitud del rezago y la fecha del último
  rebuild. **NO se dispara el cron ni se reescribe `actualidad_senal` en esta fase.**
- Una señal con cero filas debe aparecer como **supresión-como-fila** (fila con causa + conteo 0),
  jamás como ausencia del tile. Verificarlo en el DOM y transcribir el literal.
- Verificar que el chip `cobertura_camara` declara PROVENIENCIA y no ranking (anti-insinuación
  T-52-13): si el DOM ordena por conteo o dice "la más activa", es hallazgo de copy para 122-05.

Cerrar con `## 4. Límites de este fragmento`: señales no recalculables y por qué (patrón "vacío
honesto", nunca un número fabricado).
  </action>
  <verify>
    <automated>grep -q "## 3. Panel de actualidad" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md &amp;&amp; grep -q "## 4. Límites de este fragmento" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md</automated>
  </verify>
  <acceptance_criteria>
    - §3 tiene una fila por `tipo_senal` observado en `actualidad_senal` (mínimo 6 si hay 6), cada una
      con nº RPC, nº de primeros principios y nº del DOM
    - Toda señal con conteo 0 tiene transcrito el literal del DOM que la presenta como cero declarado
    - §4 existe (aunque diga "sin límites")
    - Ningún archivo de `app/` o `supabase/` modificado; cero `postgres://`; cero PII
  </acceptance_criteria>
  <done>Panel de actualidad recalculado señal por señal, con rezago del materializado declarado si existe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → PROD (psql) | credencial de DB cruza aquí |
| tabla materializada → DOM público | dato ya PII-safe por contrato de la RPC bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-07 | Information Disclosure | credencial en el fragmento | mitigate | `SUPABASE_DB_URL` sin expandir; acceptance exige cero `postgres://` |
| T-122-08 | Tampering | rebuild accidental de `actualidad_senal` | mitigate | Prohibido invocar el proc de rebuild; solo `select` |
| T-122-09 | Information Disclosure | PII vía `cruce_senal` / lobby | mitigate | Solo agregados y `sector_etiqueta`; prohibido `select rut/nombre de tercero` |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes |
</threat_model>

<verification>
- El fragmento tiene §1–§4 con filas de veredicto para ambos grupos
- `git diff --stat` lista únicamente el fragmento
</verification>

<success_criteria>
Ningún conteo de cruces ni de señal del panel queda sin su par (SQL, deploy) y su veredicto.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-03-SUMMARY.md` when done
</output>
