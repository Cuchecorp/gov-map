---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 02
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "Cada uno de los 5 bloques de relaciones de la ficha tiene su nº SQL y su nº en el DOM del deploy, con veredicto"
    - "Los 4 chips de conteo del resumen de la ficha (votos, lobby, patrimonio, cruces) están recalculados por SQL"
    - "Los 4 ejes de /comparar tienen su nº SQL y su nº del deploy"
    - "El literal VSIM 'Coinciden en N de M' cuadra contra coincidencia_votos_par en al menos 3 pares"
    - "Todo denominador de lobby excluye estado_vinculo <> 'confirmado' o la desviación queda registrada"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md"
      provides: "Grupos 1 y 2 del universo: relaciones de ficha + /comparar incl. VSIM"
      contains: "| veredicto |"
  key_links:
    - from: "122-CRUCES-SQL-01-RELACIONES-COMPARAR.md"
      to: "RPC coincidencia_votos_par (0068)"
      via: "query verbatim comparada con el literal del DOM"
      pattern: "coincidencia_votos_par"
---

<objective>
Recalcular por SQL contra PROD los grupos 1 y 2: las relaciones entre parlamentarios (5 bloques de
ficha + los conteos del resumen) y `/comparar` (4 ejes 3-estados + VSIM), y comparar cada número con
el que emite el deploy real.

Purpose: son los cruces con más aritmética propia (total_n vs .length, caps de RPC, denominador de
asistencia, N/M de VSIM) — donde una desincronización es más probable y más dañina.
Output: `122-CRUCES-SQL-01-RELACIONES-COMPARAR.md` (fragmento; lo consolida el plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md
@./CLAUDE.md

Régimen LOCKED: psql read-only con `set -a; source .env; set +a` +
`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "…"`; JAMÁS imprimir el valor de
`SUPABASE_DB_URL`; conteos por `psql -tA` nunca por REST; cero PII; cero requests a fuentes
gubernamentales; lectura del deploy por `curl -s` al Worker; NO se toca código en este plan (los
fixes son el plan 122-05); NO se hace deploy; no se tocan flags `*_PUBLIC_ENABLED`.

Doble lectura obligatoria por cruce: (a) invocar la MISMA RPC por psql
(`select * from <rpc>('<sujeto>')`), (b) la query "de primeros principios" que la RPC pretende
implementar, escrita contra las tablas base. Si (a) y (b) difieren, ESO ya es un hallazgo y se
registra aunque el DOM coincida con (a).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relaciones de ficha — 5 bloques + conteos del resumen (sujetos D1165 y S1338)</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md</files>
  <read_first>
    - `app/app/parlamentario/[id]/page.tsx` (líneas 180-210 `crossLinkReader` y los 5 readers;
      370-400 `RelacionesConDatos` y el contrato de vacío honesto; `totalReal`)
    - `app/components/cross-links-parlamentario.tsx` (cómo se renderiza N y `verTodosHref`)
    - `app/lib/parlamentario-resumen-conteos.ts` (completo, 496 líneas — punto único de conteos:
      `contarCarriles`, `derivarEstado`, el cap `p_limit: 1000` documentado como WR-03, el dedupe de
      lobby por `identificador`)
    - `supabase/migrations/0061_cross_links_conteo_honesto_orden.sql` y
      `supabase/migrations/0067_militancia_historica_compartida.sql` (definición de `total_n` y del
      net-new-only 696)
  </read_first>
  <action>
Crear el fragmento (front-matter `fase: 122`, `fragmento: 01-relaciones-comparar`) con dos secciones.

`## 1. Relaciones entre parlamentarios (5 bloques de ficha)` — una fila de la plantilla §0.2 por
cada uno de los 5 ejes, para el sujeto **D1165** (rico) y para **S1338** (vacío honesto):
`copartidarios_de_parlamentario`, `de_la_misma_zona`, `co_comisionados_de_parlamentario`,
`coautores_de_parlamentario`, `militancia_historica_compartida`.
Por eje registrar: (a) `select count(*), max(total_n) from <rpc>('D1165')` — nº de filas devueltas y
`total_n`; (b) la query de primeros principios contra tablas base (p.ej. copartidarios =
militancia vigente compartida; misma zona = columna de zona/circunscripción del Senado —
recordar el hallazgo 101-01: diputados dan 0, NO fabricar distrito; co-comisionados =
`comision_membresia`; co-autoría = `proyecto_autor` con `estado_vinculo='confirmado'`; militancia
histórica = el cruce net-new-only de 0067, 696 LOCKED); (c) el número que muestra el deploy:
`curl -s https://observatorio-congreso.thevalis.workers.dev/parlamentario/D1165` y extraer el conteo
renderizado en `section#relaciones` (registrar el comando `grep`/`sed` exacto usado).
Verificar explícitamente que el número mostrado es el `total_n` honesto y NO el `.length` cap-eado
(regla LOCKED de `/comparar` y de la ficha); si un bloque muestra `.length` con `total_n` mayor →
`discrepancia-corregida` (fix en 122-05) con ambos números.
Para **S1338** verificar el contrato de vacío honesto: los 5 ejes en `total_n = 0` ⇒ el deploy debe
emitir la ausencia DECLARADA (`RelacionesSection vacio`), no un silencio ni un 0 oculto. Registrar el
fragmento de DOM que lo demuestra.

`## 2. Conteos del resumen de la ficha (chips above-the-fold)` — una fila por carril
(`votos`, `lobby`, `patrimonio`, `cruces`) para D1165 y S1338, más el chip de asistencia
"Presente en N de M". Contrastar contra SQL:
- votos: `select count(*) from voto where parlamentario_id='D1165'` vs `votos_de_parlamentario` con
  `p_limit 1000` — **registrar explícitamente si el sujeto supera 1000** (D1165 tiene 3.752 votos
  según 113 §1.1 ⇒ el chip muestra el cap como si fuera el total: es el WR-03 documentado en
  `parlamentario-resumen-conteos.ts:271-278`). Es una discrepancia REAL entre el nº mostrado y el nº
  de la base: registrarla con ambos números y clasificarla (`discrepancia-corregida` si el fix cabe
  en 122-05 sin RPC nueva, `discrepancia-declarada` si exige un RPC de conteo dedicado).
- lobby: denominador honesto = `count(distinct identificador)` sobre audiencias con
  `estado_vinculo='confirmado'`. Comparar contra el dedupe por `identificador` del código.
- patrimonio y cruces: conteo directo de las tablas base vs el `.length` del RPC.
- asistencia: `presentes` = votos con `seleccion <> 'ausente'`; verificar que M es el mismo total del
  chip de votos (si el total está cap-eado, la asistencia hereda el cap → registrarlo).
  </action>
  <verify>
    <automated>grep -c "| cuadra \|| discrepancia-corregida \|| discrepancia-declarada " .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md</automated>
  </verify>
  <acceptance_criteria>
    - §1 tiene ≥10 filas de veredicto (5 ejes × 2 sujetos), cada una con nº SQL y nº deploy numéricos
      (o el literal `ausente del DOM` con el grep que lo prueba)
    - §2 tiene ≥9 filas (4 carriles × 2 sujetos + asistencia), cada una con nº SQL y nº deploy
    - El WR-03 del cap `p_limit: 1000` para D1165 aparece con AMBOS números (3.752 real vs lo mostrado)
    - Cada fila referencia un bloque ```sql numerado presente en el mismo archivo (cero query "citada
      de memoria")
    - Cero `postgres://` y cero valores PII en el archivo; ningún archivo de `app/` o `supabase/` modificado
  </acceptance_criteria>
  <done>Grupos de relaciones y resumen recalculados, con veredicto por fila y queries verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: /comparar — 4 ejes 3-estados + VSIM sobre 3 pares</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md</files>
  <read_first>
    - `app/app/comparar/page.tsx` (líneas 80-135 los 4 readers de eje; 300-400 el eje de comisiones y
      su cap `CAP_RPC_COMISIONES` sin `total_n`; 490-520 el reader VSIM `coincidencia_votos_par`;
      560-650 `estaCompleta` / `totalHonesto` / las columnas de militancia y co-autoría)
    - `app/components/similitud-votacion-comparar.tsx` (línea 120: literal
      `Coinciden en {n} de {m} votaciones compartidas ({pct}%)`; la leyenda anti-DW-NOMINATE LOCKED)
    - `supabase/migrations/0068_coincidencia_votos_par.sql` (definición de `n_coinciden` /
      `m_compartidas` y el filtro de votación sustantiva `seleccion in (si,no,abstencion)` sobre
      `estado_vinculo='confirmado'` — el denominador VSIM-01)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §4.7 (`/comparar`)
  </read_first>
  <action>
Añadir al mismo fragmento:

`## 3. /comparar — los 4 ejes` — usar el par determinista **A = D1165, B = S1338** y además un par
diputado-diputado con datos en los 4 ejes (elegirlo por SQL con `order by ... , id asc` y registrar
la query de selección). Por cada eje (`militancia_historica_compartida`, `comisiones_de_parlamentario`,
`coautores_de_parlamentario`, y el eje de partido vigente vía `parlamentarios_publico_v2`):
nº SQL (RPC + primeros principios) vs nº del deploy leído con
`curl -s "https://observatorio-congreso.thevalis.workers.dev/comparar?a=<A>&b=<B>"`.
Verificar los 3 estados: compartido / no compartido / eje ausente — un eje sin datos debe declarar la
ausencia, nunca mostrar un 0 que parezca "no comparten". Verificar la disciplina de completitud:
las columnas muestran `totalHonesto` (`total_n`), y el eje de comisiones —que NO emite `total_n`—
solo puede afirmar completitud con `length < CAP_RPC_COMISIONES` (registrar si el par elegido roza
el cap; si lo roza y el deploy afirma completitud → `discrepancia-corregida`).

`## 4. VSIM — "Coinciden en N de M"` — sobre **3 pares** (los 3 del precedente 104-03 si el SUMMARY
de esa fase los nombra; si no, elegir 3 pares por SQL con desempate estable por `(id_a, id_b)` y
registrar la query de selección). Por par:
`select n_coinciden, m_compartidas, fecha_captura_max from coincidencia_votos_par('<A>','<B>')` y la
query de primeros principios (votaciones donde AMBOS emitieron selección sustantiva ∈ {si,no,abstencion}
con `estado_vinculo='confirmado'`; `pareo` y `ausente` EXCLUIDOS del denominador — VSIM-01) contra el
literal del DOM. Registrar N, M y el porcentaje mostrado; verificar que el redondeo del `pct` es el
firmado en el dossier (precedente 104-03: `(100%)` sobre 3655/3672 es dossier-compliant, NO una
discrepancia). Verificar además que la leyenda anti-DW-NOMINATE sigue presente en el DOM (`grep` del
verbatim) — si el número cuadra pero la leyenda falta, es `discrepancia-declarada` de copy.

Cerrar el fragmento con `## 5. Límites de este fragmento`: todo eje/par que no se pudo recalcular y
por qué (patrón "vacío honesto", nunca un número fabricado).
  </action>
  <verify>
    <automated>grep -q "## 4. VSIM" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md &amp;&amp; grep -c "coincidencia_votos_par" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md</automated>
  </verify>
  <acceptance_criteria>
    - §3 tiene ≥8 filas (4 ejes × 2 pares) con nº SQL y nº deploy
    - §4 tiene exactamente 3 pares, cada uno con `n_coinciden`, `m_compartidas`, el literal del DOM
      transcrito y el veredicto
    - El denominador VSIM aparece explícito: `pareo` y `ausente` excluidos, `estado_vinculo='confirmado'`
    - §5 existe (aunque diga "sin límites: los N cruces del alcance fueron recalculados")
    - Ningún archivo de `app/` o `supabase/` modificado; cero `postgres://`; cero PII
  </acceptance_criteria>
  <done>/comparar y VSIM recalculados sobre pares deterministas, con ambos números por fila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → PROD (psql) | credencial de DB cruza aquí |
| RPC pública → DOM del deploy | dato ya PII-safe por contrato secdef |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04 | Information Disclosure | queries de votos/lobby podrían tocar columnas PII | mitigate | Solo `count(*)`/agregados y columnas ya expuestas por RPC pública; prohibido `select rut, email` |
| T-122-05 | Information Disclosure | credencial en el fragmento | mitigate | `SUPABASE_DB_URL` sin expandir; acceptance exige cero `postgres://` |
| T-122-06 | Tampering | escritura a PROD | mitigate | Solo `select`; cero DDL/DML |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes |
</threat_model>

<verification>
- El fragmento tiene §1–§5 y ≥30 filas de veredicto en total
- `git diff --stat` lista únicamente el fragmento
</verification>

<success_criteria>
Ningún número de relaciones ni de `/comparar` queda sin su par (SQL, deploy) y su veredicto.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-02-SUMMARY.md` when done
</output>
