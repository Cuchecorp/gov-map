---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 04
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "El total_n de menciones de lobby de un boletín cuadra con lobby_menciones_de_boletin en PROD"
    - "La cobertura lobby↔PL declarada (~3,8%) está recalculada contra PROD con su cifra observada y su fecha"
    - "Todo denominador de lobby excluye estado_vinculo <> 'confirmado'"
    - "lobby_sector_aporte con 0 filas queda registrado explícitamente como vacío HONESTO, no como bug"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md"
      provides: "Grupos 5 y 6: lobby↔PL con cobertura declarada + sección de vacíos honestos"
      contains: "lobby_sector_aporte"
  key_links:
    - from: "122-CRUCES-SQL-03-LOBBY.md"
      to: "RPC lobby_menciones_de_boletin (0062/0063)"
      via: "total_n comparado con el conteo del DOM"
      pattern: "lobby_menciones_de_boletin"
---

<objective>
Recalcular por SQL contra PROD los grupos 5 y 6: el cruce lobby↔proyecto de ley (con su cobertura
parcial declarada) y el vacío honesto de `lobby_sector_aporte`.

Purpose: es el cruce con la cobertura más parcial del sitio (~3,8% declarado en la Phase 92) y el
único con un stub estructural de 0 filas — los dos casos donde una lectura descuidada fabrica o
alarma. Ambos deben quedar con su cifra observada HOY y su fecha.
Output: `122-CRUCES-SQL-03-LOBBY.md` (fragmento; lo consolida el plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md
@./CLAUDE.md

Régimen LOCKED: psql read-only con `set -a; source .env; set +a` +
`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" -tA -c "…"`; JAMÁS imprimir el valor de
`SUPABASE_DB_URL`; conteos por `psql -tA` nunca por REST; cero PII (el lobby trae contrapartes —
NUNCA `rut`, `contraparte_id` ni datos de terceros en el artefacto: solo agregados); cero requests a
fuentes gubernamentales; lectura del deploy por `curl -s`; NO se toca código (fixes = 122-05); NO se
hace deploy; no se tocan flags.
</context>

<tasks>

<task type="auto">
  <name>Task 1: lobby↔PL — conteos por superficie y cobertura declarada recalculada</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md</files>
  <read_first>
    - `app/components/lobby-menciones-de-boletin.tsx` (`:60-80` el contrato con `total_n =
      count(*) over ()` y el LIMIT 50; `:198-215` la elección entre `mostradas` y `total`; `:248` la
      llamada RPC)
    - `app/components/lobby-de-parlamentario.tsx` (el conteo de audiencias y su paginación)
    - `app/lib/parlamentario-resumen-conteos.ts` `:308-332` (dedupe de lobby por `identificador`,
      `lobby_ingesta_estado` para distinguir `vacio` de `no_ingerido`)
    - `supabase/migrations/0062_lobby_menciones_de_boletin.sql` y
      `supabase/migrations/0063_lobby_menciones_una_fila_por_audiencia.sql` (el filtro fail-closed
      doble: regex SQL espejo del TS + join por existencia en `proyecto`; SOLO `confirmado` +
      `parlamentario_id`; `total_n` honesto; LIMIT 50)
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` filas E-002, E-020
      y E-041 de §3.0
  </read_first>
  <action>
Crear el fragmento (front-matter `fase: 122`, `fragmento: 03-lobby`) con:

`## 1. Menciones de lobby por boletín` — sujetos: el boletín **14309-04** (usado por 92-04 en el pgTAP
real) y el boletín con MAYOR `total_n` de menciones en PROD hoy (query de selección con desempate
estable por `boletin asc`). Por sujeto: (a) `select count(*), max(total_n) from
lobby_menciones_de_boletin('<boletin>')`; (b) la query de primeros principios sobre `lobby_audiencia`
reproduciendo el filtro fail-closed de 0062/0063 (`estado_vinculo='confirmado'`,
`parlamentario_id is not null`, regex de boletín, join de existencia con `proyecto`); (c) el nº del
DOM: `curl -s https://observatorio-congreso.thevalis.workers.dev/proyecto/<boletin>` extrayendo el
conteo de la sección de menciones. Verificar que cuando `mostradas < total_n` el sitio muestra el
`total_n` (no las 50 filas) — si muestra `mostradas`, es `discrepancia-corregida`.

`## 2. Lobby en la ficha del parlamentario` — sujetos **D1165** (112 audiencias confirmadas según 113
§1.1) y **S1338** (0 — vacío honesto; `lobby_audiencia.parlamentario_id` confirmado es hoy exclusivo
de diputados). Comparar el chip/encabezado del DOM contra
`select count(distinct identificador) from lobby_audiencia where parlamentario_id='D1165' and
estado_vinculo='confirmado'`. **Denominador honesto**: correr también el conteo SIN el filtro
`estado_vinculo='confirmado'` y registrar AMBOS; si el número mostrado corresponde al conteo sin
filtrar, es `discrepancia-corregida`. Para S1338 registrar qué estado 3-valores emite el sitio
(`vacio` vs `no_ingerido`) y si corresponde al marcador `lobby_ingesta_estado` observado.

`## 3. Cobertura declarada lobby↔PL` — recalcular la cifra `~3,8%` de la Phase 92 con la query
verbatim de aquel runbook: numerador = audiencias confirmadas con al menos un boletín mencionado que
existe en `proyecto`; denominador = audiencias confirmadas totales; registrar además el nº de
boletines distintos alcanzados. Escribir: cifra 92-04 (195/5106 ≈ 3,8%), cifra observada hoy, fecha
de observación, y el veredicto. Si la cifra cambió, la fila queda `discrepancia-corregida` y el plan
122-05 actualiza el copy con la cifra observada **y su fecha** usando el idiom aprobado
("según fuente al …"; "captura" pelado PROHIBIDO). Localizar dónde vive hoy ese literal de cobertura
en el código (`grep -rn "cobertura" app --include=*.tsx | grep -v "\.test\."`) y registrar
archivo:línea — si NO existe un literal de cobertura en la superficie, ESO es el hallazgo (cobertura
parcial no declarada) y se registra como `discrepancia-corregida` con la ubicación propuesta.
  </action>
  <verify>
    <automated>grep -q "## 3. Cobertura declarada" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md &amp;&amp; grep -c "estado_vinculo" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md</automated>
  </verify>
  <acceptance_criteria>
    - §1 tiene ≥2 boletines con nº RPC, nº de primeros principios, nº del DOM y veredicto
    - §2 tiene D1165 y S1338, y para D1165 registra el conteo CON y SIN `estado_vinculo='confirmado'`
    - §3 contiene la cifra 92-04, la cifra observada hoy con su fecha, y archivo:línea del literal de
      cobertura en el código (o la constatación de que no existe)
    - Cero `contraparte_id`, cero `rut`, cero nombres de terceros individuales en el archivo
    - Ningún archivo de `app/` o `supabase/` modificado; cero `postgres://`
  </acceptance_criteria>
  <done>lobby↔PL recalculado por superficie, con denominador honesto y cobertura observada fechada.</done>
</task>

<task type="auto">
  <name>Task 2: Sección de vacíos honestos — lobby_sector_aporte y el resto de ceros del alcance</name>
  <files>.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md</files>
  <read_first>
    - `supabase/migrations/0052_*.sql` (localizar con `ls supabase/migrations | grep 0052`) — la rama
      `lobby_sector_aporte` como stub estructural: CTE `empresa_sector` `where false` ⇒ arista
      company-rut→sector ausente ⇒ 0 filas por construcción; la rama `lobby_sector` byte-idéntica a 0039
    - `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CONTEXT.md` §specifics
    - `app/lib/parlamentario-resumen-conteos.ts` `:148-164` (`agruparSectores`: `nVotos` = 0 por
      ausencia de `tipo_senal` de voto — otro cero honesto por construcción)
  </read_first>
  <action>
Añadir al fragmento:

`## 4. Vacíos honestos` — una fila por cero observado en el alcance de la fase, con estas columnas:
`| qué | query verbatim | nº observado | causa estructural | ¿es bug? | dónde se declara en la UI |`.
Entradas obligatorias:
- **`lobby_sector_aporte`**: `select count(*) from cruce_senal where tipo_senal='lobby_sector_aporte';`
  → registrar el conteo observado. Causa: stub estructural de 0052 — la arista
  `rut de la empresa → sector` no existe (CTE `where false`), y además RUT-01 sigue en 0% con el
  backfill de ChileCompra pendiente. **¿Es bug? NO.** Dejar la frase explícita en el artefacto para
  que un auditor futuro no lo lea como defecto: *"0 filas es el resultado CORRECTO por construcción;
  rellenarlo exigiría un dato que la fuente no entrega hoy."* La superficie que lo consumiría está
  bajo MONEY (OFF) ⇒ no emitida en el deploy auditado: registrarlo, no ocultarlo.
- **`nVotos` de los sectores de cruce** = 0 (no existe `tipo_senal` que empiece con `voto`), con la
  query que lo demuestra sobre `select distinct tipo_senal from cruce_senal;`.
- **S1338 sin lobby y sin cruces** (conteo 0 real, no wiring roto) — enlazar a §2 y a
  `122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md §2`.
- Cualquier otro cero que las tareas anteriores hayan encontrado.

Regla LOCKED a transcribir al inicio de §4: **cero filas se presenta como cero; jamás se rellena,
jamás se oculta la superficie**. Toda fila de §4 debe tener su query — un vacío afirmado sin query no
se acepta.

Cerrar con `## 5. Límites de este fragmento` (lo no recalculable y por qué).
  </action>
  <verify>
    <automated>grep -q "## 4. Vacíos honestos" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md &amp;&amp; grep -q "lobby_sector_aporte" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md &amp;&amp; grep -q "## 5. Límites de este fragmento" .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md</automated>
  </verify>
  <acceptance_criteria>
    - §4 tiene ≥3 filas, cada una con una query verbatim y la columna "¿es bug?" resuelta
    - La fila de `lobby_sector_aporte` dice explícitamente que NO es bug y explica la causa estructural
      (arista company-rut→sector ausente en 0052)
    - §4 abre con la regla LOCKED de cero-como-cero transcrita
    - Ningún archivo de `app/` o `supabase/` modificado; cero `postgres://`; cero PII
  </acceptance_criteria>
  <done>Vacíos honestos registrados con query y causa, con `lobby_sector_aporte` blindado contra lectura futura como bug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente → PROD (psql) | credencial de DB cruza aquí |
| `lobby_audiencia` → artefacto | tabla con datos de TERCEROS (contrapartes), la más sensible del alcance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-10 | Information Disclosure | PII de terceros vía lobby | mitigate | Solo `count(*)`/`count(distinct identificador)`; prohibido seleccionar nombre de contraparte, `contraparte_id`, `rut` o representado |
| T-122-11 | Information Disclosure | credencial en el fragmento | mitigate | `SUPABASE_DB_URL` sin expandir; cero `postgres://` |
| T-122-12 | Tampering | escritura a PROD | mitigate | Solo `select`; cero DDL/DML |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes |
</threat_model>

<verification>
- El fragmento tiene §1–§5, con §3 fechada y §4 con la fila de `lobby_sector_aporte`
- `git diff --stat` lista únicamente el fragmento
</verification>

<success_criteria>
La cobertura parcial de lobby↔PL queda con su cifra observada y su fecha, y los ceros del alcance
quedan probados como honestos con su query.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-04-SUMMARY.md` when done
</output>
