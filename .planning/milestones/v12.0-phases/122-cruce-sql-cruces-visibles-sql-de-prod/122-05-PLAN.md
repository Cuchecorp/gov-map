---
phase: 122-cruce-sql-cruces-visibles-sql-de-prod
plan: 05
type: execute
wave: 3
depends_on: [122-02, 122-03, 122-04]
files_modified:
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md
  # Los 3 fragmentos de auditoría: la Task 2 actualiza en ellos el veredicto de las filas
  # degradadas a `discrepancia-declarada` (sin colisión: este plan está solo en su ola).
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md
  - .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md
  - app/lib/anti-insinuacion-guard.test.ts
  - app/lib/parlamentario-resumen-conteos.ts
  - app/components/cruces-de-proyecto.tsx
  - app/components/lobby-menciones-de-boletin.tsx
  - app/app/comparar/page.tsx
  - app/components/panel-actualidad.tsx
  # Tests colindantes de los 5 archivos de arriba (mismo directorio, sufijo .test.ts/.test.tsx).
  # Solo si la Task 3 determina que un fix exige SQL:
  - supabase/migrations/0073_*.sql
autonomous: true
requirements: [CRUCE-01]

must_haves:
  truths:
    - "Cada discrepancia marcada `discrepancia-corregida` en los 3 fragmentos tiene un cambio de código que la cierra"
    - "Ningún denominador mostrado incluye lo no confirmado tras el fix"
    - "Toda cobertura parcial queda declarada en la superficie con su cifra y su fecha, con el idiom aprobado"
    - "La suite y los guards de régimen quedan verdes; ningún flag `*_PUBLIC_ENABLED` fue tocado"
    - "Ninguna migración se aplicó a PROD en esta fase"
  artifacts:
    - path: ".planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md"
      provides: "Bitácora de fixes: discrepancia → cambio → antes/después → test de respaldo"
      contains: "| discrepancia |"
  key_links:
    - from: "122-CRUCES-SQL-04-FIXES.md"
      to: "los 3 fragmentos de auditoría"
      via: "una entrada por cada fila con veredicto discrepancia-corregida"
      pattern: "discrepancia-corregida"
---

<objective>
Cerrar en el código del sitio cada discrepancia que los planes 02/03/04 marcaron como
`discrepancia-corregida`, y dejar declarada toda cobertura parcial. Sin deploy.

Purpose: la fase no vale por el diagnóstico sino por el arreglo. Este plan es el único que toca
código; el resto son artefactos.
Output: código corregido + tests de respaldo + `122-CRUCES-SQL-04-FIXES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-00-METODO.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-01-RELACIONES-COMPARAR.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-02-CRUCES-ACTUALIDAD.md
@.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-03-LOBBY.md
@./CLAUDE.md

Régimen LOCKED de este plan:
- **NO se hace deploy** (viaja con la Phase 125). **No se tocan flags `*_PUBLIC_ENABLED`.**
- **Ninguna migración se aplica a PROD en esta fase.** Si un fix requiere SQL, se ESCRIBE como
  migración aditiva numerada (`0073_*.sql`, la siguiente después de 0072) con pgTAP, y su apply se
  entrega a la **Phase 124 (SUPA-FIX)**. Nada destructivo (drop / cambio de tipo / backfill): eso se
  delega a `supabase-architect` y queda BLOQUEADO en checkpoint de operador.
- **Wave-0 del copy:** si un fix introduce vocabulario nuevo o toca una superficie nueva, se EXTIENDE
  el linter anti-insinuación ANTES de escribir el copy (patrón v10.0/v11.0).
- `fecha_captura` JAMÁS se presenta como el hecho; "captura" pelado PROHIBIDO; idiom aprobado
  "según fuente al …".
- Suite verde al cerrar: app (~1428 tests) + packages + `tsc` 0 + guards de régimen
  (anti-insinuación, lockdown 22, anti-flip vsim/notif/money, bento, name-match-rut, env-example,
  integ-scope, provider-guard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 — extender el linter anti-insinuación antes de tocar copy</name>
  <files>app/lib/anti-insinuacion-guard.test.ts</files>
  <read_first>
    - `app/lib/anti-insinuacion-guard.test.ts` completo (estructura de `SUPERFICIES_*`,
      `NEGACIONES_LOCKED`, el orden de resta de leyendas —lección BLOCKER 91: las negaciones se restan
      ANTES del scan—, y el mutation self-check que prueba que el detector muerde)
    - Las tres secciones de veredicto de los fragmentos 01/02/03: listar cada fix de COPY propuesto
      (cobertura lobby↔PL, declaración de truncamiento, cero-como-cero, chip de cobertura del panel)
  </read_first>
  <action>
Inventariar los fixes de COPY que exigen los fragmentos. Para cada uno decidir:
- ¿Toca una superficie ya cubierta por un `SUPERFICIES_*` existente? → no se añade superficie.
- ¿Introduce vocabulario nuevo (p. ej. una frase de cobertura parcial, un "no se registran
  audiencias", un literal de truncamiento)? → añadir los términos prohibidos vecinos al nuevo
  vocabulario ANTES de escribir el copy, y añadir la superficie si es nueva.
Reglas duras al extender:
- NO renombrar constantes existentes (Pitfall 1 de 100-01); se añade una constante nueva.
- Los loaders deben tolerar archivo ausente (`try/catch continue`, hallazgo 100-01).
- Si el copy nuevo NIEGA un término prohibido (p. ej. "no indica coordinación"), restarlo vía
  `NEGACIONES_LOCKED` ANTES del scan, nunca suprimir el detector.
- Añadir un mutation self-check que demuestre que el detector MUERDE con el término nuevo (falla si
  se inyecta la frase prohibida en memoria) y que NO da falso positivo sobre el árbol actual.
Si tras el inventario NINGÚN fix introduce vocabulario ni superficie nueva, dejar el guard intacto y
registrar en el SUMMARY la frase: "Wave-0 no requerida: los fixes de copy reutilizan vocabulario ya
cubierto" con la lista de fixes revisados. No se toca el archivo por tocarlo.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm vitest run lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run lib/anti-insinuacion-guard.test.ts` sale con exit 0 y 0 offenders
    - Si se añadieron términos: existe un self-check que falla al inyectar la frase prohibida
      (demostrado en el SUMMARY con el output del run mutado)
    - Ninguna constante `SUPERFICIES_*` preexistente fue renombrada (`git diff` no muestra renames)
    - Cero cambios en archivos de copy en esta tarea (el copy es la Task 2)
  </acceptance_criteria>
  <done>El linter cubre el vocabulario de los fixes ANTES de que el copy exista, o se justificó por escrito que no hacía falta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixes de conteo, denominador y declaración de cobertura</name>
  <files>app/lib/parlamentario-resumen-conteos.ts, app/components/cruces-de-proyecto.tsx, app/components/lobby-menciones-de-boletin.tsx, app/app/comparar/page.tsx, app/components/panel-actualidad.tsx, y los tests colindantes</files>
  <read_first>
    - Las filas con veredicto `discrepancia-corregida` de los 3 fragmentos (son la lista de trabajo
      COMPLETA de esta tarea; ninguna otra)
    - `app/lib/parlamentario-resumen-conteos.ts` (comentario WR-03 en `:271-278`: el cap `p_limit:
      1000` es espejo BYTE-A-BYTE del de `VotosSection` — cambiarlo en un solo lado DESINCRONIZA chip
      y sección; cualquier fix debe tocar AMBOS o ninguno)
    - `app/components/cruces-de-proyecto.tsx:199`, `app/components/lobby-menciones-de-boletin.tsx:208-213`
    - `app/app/comparar/page.tsx:580-600` (`CAP_RPC_COMISIONES`, `estaCompleta`, `totalHonesto`)
  </read_first>
  <behavior>
    - Un conteo cuyo total real supera el cap del RPC ya NO se presenta como si fuera el total exacto:
      o muestra el total honesto, o declara el truncamiento (variante "N+" / "al menos N"), y el chip
      y la sección muestran lo MISMO.
    - Un denominador de lobby cuenta solo `estado_vinculo='confirmado'`.
    - Una cobertura parcial se declara en la superficie con su cifra y su fecha, con el idiom
      "según fuente al …" (nunca "captura" pelado, nunca `fecha_captura` presentada como el hecho).
    - Un conteo de cero se sigue presentando como cero declarado (nunca ausencia, nunca "—" cuando el
      carril SÍ fue ingestado).
  </behavior>
  <action>
**TECHO DE ALCANCE (válvula de presupuesto de contexto).** Antes de tocar nada, contar las filas
`discrepancia-corregida` de los 3 fragmentos y listar los archivos que tocarían. Si el total supera
**8 filas o 6 archivos de `app/`**, NO se estiran los fixes: se priorizan por daño al lector
(1º denominador deshonesto — un número que sobre-declara; 2º conteo cap-eado presentado como total;
3º cobertura parcial sin declarar; 4º copy) y las filas restantes se degradan a
`discrepancia-declarada` en su fragmento de origen, **con ambos números registrados** y un handoff
explícito nombrando su destino (Phase 124 si exige SQL, Phase 125 si solo exige verificación
post-deploy, o una fila nueva en `.planning/deferred-items.md` si es deuda de datos). El criterio 3
de la fase admite "corregida **o** declarada": lo prohibido no es diferir, es **no registrar**.

Aplicar UN fix por cada fila `discrepancia-corregida` seleccionada, en este orden y con esta disciplina:

1. **Sincronía obligatoria**: todo fix de conteo que toque un valor mostrado en dos lugares (chip del
   resumen y encabezado de la sección) se aplica en AMBOS en el mismo commit. Si el fix honesto exige
   un RPC de conteo dedicado (SQL), NO se improvisa: se degrada la fila a
   `discrepancia-declarada` en el fragmento correspondiente, se escribe la migración en la Task 3, y
   se anota el handoff a la Phase 124.
2. **Denominadores**: añadir/asegurar el filtro `estado_vinculo='confirmado'` donde el fragmento lo
   marcó, y un test que falle si el filtro se retira.
3. **Cobertura declarada**: escribir el literal de cobertura de lobby↔PL con la cifra observada HOY
   y su fecha, en la ubicación que el fragmento 03 §3 propuso. Formato del idiom: cifra + "según
   fuente al <fecha>". Prohibido presentar el parcial como total.
4. **Vacíos**: no tocar ningún cero honesto. Si un fix "arreglara" un cero rellenándolo, se rechaza.
5. Cada fix lleva su test (RTL o unitario) que falla ANTES del cambio y pasa después; registrar el
   nombre del test en la bitácora.

Escribir `122-CRUCES-SQL-04-FIXES.md` con una tabla:
`| # | fragmento y fila origen | discrepancia (nº deploy → nº SQL) | archivo:línea | qué cambió | test de respaldo | estado |`
y una sección `## Fixes NO aplicados` con las filas degradadas a `discrepancia-declarada` y su motivo
(SQL requerido / destructivo / dato inexistente). Actualizar el veredicto de esas filas en el
fragmento de origen para que el artefacto final sea coherente.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm vitest run &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` en `app/` sale exit 0 con conteo de tests ≥ 1428
    - `pnpm exec tsc --noEmit` sale exit 0
    - Cada fila `discrepancia-corregida` de los 3 fragmentos aparece en la bitácora: o en la tabla de
      fixes (con archivo:línea y nombre de test), o en `## Fixes NO aplicados` con ambos números y su
      handoff nombrado. Cero filas huérfanas: `nº filas en la tabla + nº en "Fixes NO aplicados" ==
      nº total de filas `discrepancia-corregida` de los fragmentos` (dejar la aritmética escrita)
    - `git diff` no toca ningún archivo `.env*`, ni ningún `*_PUBLIC_ENABLED`, ni ningún archivo bajo
      `.github/workflows/`
    - Cero literales `captura` pelados nuevos: `git diff -U0 | grep -E "^\+" | grep -v '^\+\+\+' | grep -c "captura" ` — toda ocurrencia nueva va acompañada de "según fuente al"
  </acceptance_criteria>
  <done>Toda discrepancia corregible está cerrada en código con test, y las no corregibles están degradadas y justificadas.</done>
</task>

<task type="auto">
  <name>Task 3: Migración escrita-no-aplicada (solo si un fix exige SQL) + guards de régimen</name>
  <files>supabase/migrations, .planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-CRUCES-SQL-04-FIXES.md</files>
  <read_first>
    - `supabase/migrations/0064_bounded_rpc_statement_timeout.sql` (molde de RPC bounded: secdef,
      `search_path=''`, `statement_timeout`, LIMIT, drop-before-create, doble-revoke, CERO grant)
    - `supabase/migrations/0072_notificacion_envio_idempotencia.sql` (última numerada — la nueva es 0073)
    - `app/lib/` — el archivo que define `PUBLIC_RPC_ALLOWLIST` (localizar con
      `grep -rn "PUBLIC_RPC_ALLOWLIST" app --include=*.ts | grep -v "\.test\."`)
    - `.claude/skills/supabase-ops/SKILL.md` si existe
  </read_first>
  <action>
Si —y solo si— la Task 2 degradó alguna fila por requerir SQL (típicamente un `total_n` honesto para
`cruces_de_proyecto`, o un RPC de conteo de votos que reemplace el cap `p_limit: 1000`):

1. Escribir `supabase/migrations/0073_<nombre>.sql` **aditiva pura** (crear función nueva o añadir una
   columna de salida en una función NUEVA de nombre distinto; JAMÁS drop de una función viva, jamás
   cambio de tipo, jamás backfill). Molde de aguja completa: SECURITY DEFINER, `search_path=''`,
   `statement_timeout`, LIMIT explícito, doble-revoke CERO grant (`revoke all ... from anon,
   authenticated`), orden neutro (anti-ranking).
2. Escribir su pgTAP en la ruta que usa el repo para las migraciones previas (espejar el archivo
   pgTAP de 0068 o 0062).
3. Si el RPC es público: añadirlo a `PUBLIC_RPC_ALLOWLIST` en el mismo commit (guard Direction-B exige
   allowlist ⊆ definidas).
4. **NO aplicar a PROD.** Registrar en `122-CRUCES-SQL-04-FIXES.md` la sección
   `## Handoff a la Phase 124 (SUPA-FIX)` con: archivo de migración, comando de apply exacto
   (`PGCLIENTENCODING=UTF8 psql "$SUPABASE_DB_URL" --single-transaction -f supabase/migrations/0073_*.sql`,
   NUNCA `supabase db push`), pre-checks fail-closed, y el pgTAP a correr contra el schema aplicado.
   El consumo de la superficie queda inerte/degradado honesto hasta que 124 aplique.

Si NINGÚN fix exigió SQL: no crear archivo alguno y escribir en la bitácora la frase
"Sin migración: todos los fixes fueron de código" con la lista de filas revisadas.

Cerrar el plan corriendo los guards de régimen uno por uno y registrando el resultado de cada uno.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm vitest run lib/anti-insinuacion-guard.test.ts lib/lockdown-guard.test.ts lib/vsim-antiflip-guard.test.ts lib/notif-antiflip-guard.test.ts lib/money-antiflip-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los 5 guards nombrados salen exit 0 (si alguna ruta de archivo difiere, localizarla con
      `ls app/lib/*guard*.test.ts` y correr TODOS los que aparezcan)
    - Si existe `supabase/migrations/0073_*.sql`: contiene `security definer`, `search_path`,
      `revoke all`, y NO contiene `drop function` sobre una función preexistente ni `alter table ... type`
    - `git log`/`git diff` no muestran ningún apply a PROD ni uso de `supabase db push`
    - La bitácora contiene la sección de handoff a 124, o la frase "Sin migración: todos los fixes
      fueron de código"
  </acceptance_criteria>
  <done>El SQL necesario (si lo hubo) está escrito, testeado y entregado a 124 sin aplicarse; guards verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código del sitio → DOM público | todo cambio de conteo/copy es visible a cualquiera |
| migración escrita → PROD | boundary NO cruzado en esta fase (apply = Phase 124) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-13 | Tampering | flip accidental de un flag al editar código de gate | mitigate | Guards anti-flip vsim/notif/money corridos en la Task 3; acceptance prohíbe diffs en `.env*` |
| T-122-14 | Elevation of Privilege | RPC nueva sin doble-revoke o sin allowlist | mitigate | Molde de aguja completa 0064 + entrada en `PUBLIC_RPC_ALLOWLIST` en el mismo commit |
| T-122-15 | Information Disclosure | un fix de conteo que exponga PII vía una columna nueva | mitigate | Las RPCs nuevas devuelven solo agregados; pgTAP asegura ausencia de rut/email |
| T-122-16 | Repudiation | fix aplicado sin registrar el número original | mitigate | La bitácora exige `nº deploy → nº SQL` por fila |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | Este plan NO instala paquetes nuevos (CERO dependencia nueva) |
</threat_model>

<verification>
- Suite app verde (≥1428) + `tsc --noEmit` exit 0 + los guards de régimen verdes
- Cero deploy, cero apply a PROD, cero flag tocado
- `122-CRUCES-SQL-04-FIXES.md` cubre TODAS las filas `discrepancia-corregida` de los 3 fragmentos
</verification>

<success_criteria>
Ninguna discrepancia queda ni sin arreglar ni sin declarar, y el árbol queda verde y desplegable por
la Phase 125.
</success_criteria>

<output>
Create `.planning/phases/122-cruce-sql-cruces-visibles-sql-de-prod/122-05-SUMMARY.md` when done
</output>
