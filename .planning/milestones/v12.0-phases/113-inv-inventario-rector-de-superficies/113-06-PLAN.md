---
phase: 113-inv-inventario-rector-de-superficies
plan: 06
type: execute
wave: 3
depends_on: ["113-02"]
files_modified:
  - .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "ProvenanceBadge está declarado como chokepoint DUAL: fecha (`capturedAt`) Y link externo (`sourceUrl`), con sus 16 call-sites trazados a columna de origen"
    - "Los 4 builders de URL externa están citados con plantilla verbatim y `safeExternalHref` declarado como chokepoint de LINK-03"
    - "Las 4 clases de fuente del ROADMAP (camara, senado, BCN, leylobby) están resueltas con host+conteo o declaración de cero respaldada por query"
  artifacts:
    - path: ".planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md"
      provides: "Secciones 3.1 (chokepoints), 3.2 (builders) y 3.3 (URL-desde-columna)"
      contains: "### 3.3"
  key_links:
    - from: "ProvenanceBadge sourceUrl"
      to: "columna/RPC de origen"
      via: "tabla de call-sites en §3.1"
      pattern: "sourceUrl"
---

<objective>
Cerrar la capa de links externos del inventario: **§3.1 chokepoints** (`ProvenanceBadge` DUAL + `safeExternalHref`), **§3.2 los 4 builders** con plantilla verbatim y **§3.3 familias de URL almacenadas en columnas**, descubiertas por catálogo.

Hallazgo del plan-checker incorporado: `ProvenanceBadge` no es solo el chokepoint de `fecha_captura`; también renderiza `<a href={safeExternalHref(sourceUrl)}>` (`provenance-badge.tsx:25,37,62`) y `sourceUrl=` aparece en **16 archivos** — es el mayor emisor de links externos del sitio. Y leylobby vive en columnas `enlace`/`enlace_detalle` de audiencias, que NO matchean el patrón de nombres previo: el descubrimiento va por `information_schema.columns`.

Purpose: esta sección ES el insumo de 115 (patrones de link externo). Si queda incompleta, 115 valida 4 plantillas e ignora la mayoría de los links reales.
Output: §3.1, §3.2 y §3.3 de `113-INVENTARIO.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-inv-inventario-rector-de-superficies/113-RESEARCH.md
@.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §3.1 chokepoints (ProvenanceBadge DUAL) + §3.2 los 4 builders</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/components/provenance-badge.tsx (props `capturedAt` Y `sourceUrl`; líneas 25, 37, 62 — el `<a href={safeExternalHref(sourceUrl)}>`)
    - app/lib/utils.ts (safeExternalHref)
    - app/components/validacion-fuente.tsx (buildSenadoUrl, buildCamaraUrl, enlaceHumanoProyecto, esR2PathPermitido)
    - app/lib/format.ts (partidoLegible)
    - Los 16 archivos que resulten de `grep -rl "sourceUrl=" app/components app/app`
  </read_first>
  <action>
    Escribir `### 3.1 Chokepoints` declarando `ProvenanceBadge` como chokepoint **DUAL**:
    - **Fecha**: prop `capturedAt` (usa `relativeTimeEs` + `esStale`). REGLA: toda fecha que llega vía `capturedAt` se MARCA como `fecha_captura` sin más análisis; toda otra fecha exige rastreo a su columna.
    - **Link externo**: prop `sourceUrl`, renderizada como `<a href={safeExternalHref(sourceUrl)} target="_blank" rel="noopener noreferrer">` con el texto "fuente oficial ↗" (`provenance-badge.tsx:25,37,62`); si `safeExternalHref` devuelve `null` el link NO se emite (degradación a "sin enlace"). REGLA: el badge es el MAYOR emisor de links externos del sitio — 115 debe tratarlo como superficie de primera clase, no como decoración.

    Añadir la **tabla de call-sites de `sourceUrl`** (obligatoria), una fila por cada archivo devuelto por `grep -rl "sourceUrl=" app/components app/app`: `| # | archivo:línea | expresión del prop (verbatim) | columna/RPC de origen | fuente (camara/senado/BCN/leylobby/otro) | gate |`. La "expresión del prop" se copia tal cual del código (p. ej. una lectura de campo o un ternario); la "columna/RPC de origen" se rastrea hasta el vocabulario cerrado de §3. Si un call-site pasa una expresión cuyo origen no puede rastrearse estáticamente, registrar `origen: indeterminado — rastrear en 115` en vez de adivinar.

    Declarar también `safeExternalHref` (`lib/utils.ts`) como guard único de href externo (devuelve `null` salvo `http:`/`https:`), chokepoint de LINK-03.

    Escribir `### 3.2 Constructores de URL externa` con los 4 builders y su plantilla VERBATIM: `buildSenadoUrl` → `https://tramitacion.senado.cl/appsenado/templates/tramitacion/index.php?boletin_ini={boletin}` (boletín COMPLETO con sufijo); `buildCamaraUrl` → `https://www.camara.cl/legislacion/ProyectosDeLey/tramitacion.aspx?prmID={prmId}&prmBOLETIN={boletin}` (solo si `prm_id_camara != null`); `enlaceHumanoProyecto` → rewrite por host+path (si `proyecto.enlace` es host `tramitacion.senado.cl` con path `/wspublico/` → `buildSenadoUrl(boletin)`, si no VERBATIM) — registrar SIEMPRE el link post-rewrite; `partidoLegible` (`lib/format.ts`) → NO construye link, desactiva URIs `datos.bcn.cl/.../partido-politico/{slug}` (invariante "CERO URI en el DOM"), display-only.
  </action>
  <verify>
    <automated>grep -q 'sourceUrl' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'buildSenadoUrl' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'buildCamaraUrl' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'enlaceHumanoProyecto' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'partidoLegible' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -q 'safeExternalHref' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</automated>
  </verify>
  <acceptance_criteria>
    - §3.1 declara las DOS caras del badge (`capturedAt` fecha, `sourceUrl` link externo) con cita `provenance-badge.tsx:25,37,62`
    - La tabla de call-sites de `sourceUrl` cubre TODOS los archivos de `grep -rl "sourceUrl=" app/components app/app` (hoy 16): el conteo de filas de la tabla ≥ el valor de `grep -rl "sourceUrl=" app/components app/app | wc -l`
    - Toda fila de esa tabla tiene expresión del prop verbatim y columna/RPC de origen (o la marca explícita `indeterminado — rastrear en 115`)
    - Los 4 builders aparecen con plantilla verbatim; `safeExternalHref` declarado chokepoint de LINK-03
    - Cero celdas vacías: `grep -nE '\|[[:space:]]*\|'` sin match
  </acceptance_criteria>
  <done>§3.1 y §3.2 completas; el badge queda trazado como emisor de links externos con sus 16 call-sites y origen por columna.</done>
</task>

<task type="auto">
  <name>Task 2: §3.3 familias de URL almacenadas en columnas (descubrimiento por catálogo)</name>
  <files>.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md</files>
  <read_first>
    - app/components/lobby-de-parlamentario.tsx (líneas ~537 y ~601: hrefs desde `enlace` / `enlace_detalle` de audiencias — NO matchean el patrón `url_fuente|enlace_fuente|link_*`)
    - app/components/validacion-fuente.tsx (`esR2PathPermitido`: allowlist `tramitacion/`)
    - 113-RESEARCH.md §Constructores de URL externa (hosts solo-en-fixtures) + Pitfall 2 + Open Question 2
    - 113-INVENTARIO.md §3.1 (tabla de call-sites de `sourceUrl`)
  </read_first>
  <action>
    Cerrar el hallazgo rector. **No usar una lista de columnas adivinada**: descubrir el universo real de columnas de URL con una query al catálogo, porque el patrón `url_fuente|enlace_fuente|link_*` NO captura `enlace`/`enlace_detalle` de las audiencias de lobby (que es justamente donde vive leylobby):

    `select table_name, column_name from information_schema.columns where table_schema='public' and data_type like '%char%' and column_name ~ 'url|enlace|link' order by table_name, column_name`

    Registrar esa query verbatim con su resultado inline como el paso de descubrimiento. Luego, por cada columna descubierta, una query de familias por host: `select split_part(<col>,'/',3) as host, count(*) from <tabla> where <col> is not null group by 1 order by 2 desc, 1 asc`, con resultado inline.

    Escribir `### 3.3 Familias de URL almacenadas en columnas` con la tabla `| tabla.columna | host | n | fuente (camara/senado/BCN/leylobby/otro) | emisor que la renderiza (→ E-NNN o §3.1) |`.

    **Criterio duro de cierre:** las 4 clases de fuente que nombra el ROADMAP —`camara`, `senado`, `BCN`, `leylobby`— deben quedar RESUELTAS: o aparecen con host + conteo, o llevan la declaración explícita `cero ocurrencias en PROD al {fecha}` respaldada por la query que lo demuestra. Ninguna de las 4 puede quedar sin mención. Si leylobby no aparece bajo el patrón de nombres, buscarlo por valor (`where <col> like '%leylobby%'`) sobre las columnas descubiertas y registrar el resultado.

    Restricciones de seguridad: nunca imprimir la URL de conexión; las queries devuelven solo host y conteo, jamás la URL completa fila a fila; no listar keys de r2_path de dominios PII (`infoprobidad/`, `servel/`, `money/`, `rut/`) — solo `tramitacion/` está allowlistado por `esR2PathPermitido`.
  </action>
  <verify>
    <automated>grep -q 'information_schema.columns' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; grep -qi 'leylobby' .planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md &amp;&amp; bash .planning/phases/113-inv-inventario-rector-de-superficies/check-inventario.sh</automated>
  </verify>
  <acceptance_criteria>
    - §3.3 contiene la query de descubrimiento a `information_schema.columns` verbatim con su resultado inline
    - Cada columna descubierta tiene su query de familias por host con resultado inline
    - Las 4 clases `camara`, `senado`, `BCN`, `leylobby` aparecen todas: con host+conteo o con `cero ocurrencias en PROD al {fecha}` + query de respaldo
    - Las columnas `enlace` y `enlace_detalle` de audiencias de lobby están cubiertas explícitamente (son las que el patrón de nombres previo omitía)
    - Cero URLs completas fila a fila; cero keys de r2_path PII; cero credenciales (`grep -E 'postgres(ql)?://'` sin match)
    - `grep -nE '\|[[:space:]]*\|'` sin match
  </acceptance_criteria>
  <done>§3.3 cierra el agujero de 115: universo de columnas descubierto por catálogo y las 4 clases de fuente resueltas con evidencia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD Postgres → documento en git | los hosts y conteos de §3.3 salen de datos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Information Disclosure | queries de §3.3 | mitigate | Solo `split_part(...,'/',3)` (host) + `count(*)`; jamás `select <col>` completo; invocación sin ecoar `SUPABASE_DB_URL` |
| T-113-06 | Information Disclosure | r2_path de dominios PII | mitigate | Solo el prefijo allowlistado `tramitacion/` per `esR2PathPermitido`; no listar keys de `infoprobidad/`, `servel/`, `money/`, `rut/` |
| T-113-10 | Information Disclosure | tabla de call-sites `sourceUrl` | mitigate | Se registra la EXPRESIÓN del prop y la columna de origen, nunca valores de URL de filas reales |
| T-113-03 | Tampering | PROD Postgres | mitigate | Solo `SELECT` |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | La fase no instala paquetes |
</threat_model>

<verification>
- `bash check-inventario.sh` (STRICT=0) exit 0 y el check de builders pasa (los 4 citados)
- Tabla de call-sites de `sourceUrl` con filas ≥ `grep -rl "sourceUrl=" app/components app/app | wc -l`
- Las 4 clases de fuente resueltas en §3.3
- `grep -nE '\|[[:space:]]*\|' 113-INVENTARIO.md` sin match
</verification>

<success_criteria>
`ProvenanceBadge` declarado chokepoint DUAL con sus 16 call-sites de `sourceUrl` trazados a columna, los 4 builders con plantilla verbatim, `safeExternalHref` como chokepoint de LINK-03, y las familias de URL-desde-columna descubiertas por catálogo con las 4 clases de fuente resueltas.
</success_criteria>

<output>
Create `.planning/phases/113-inv-inventario-rector-de-superficies/113-06-SUMMARY.md` when done
</output>
