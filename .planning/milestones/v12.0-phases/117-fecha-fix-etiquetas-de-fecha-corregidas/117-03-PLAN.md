---
phase: 117-fecha-fix-etiquetas-de-fecha-corregidas
plan: 03
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - app/components/cruces-de-parlamentario.tsx
  - app/components/cruces-de-parlamentario.test.tsx
  - app/components/cruces-de-proyecto.tsx
  - app/components/cruces-de-proyecto.test.tsx
  - app/components/votos-por-parlamentario.tsx
  - app/components/votos-por-parlamentario.test.tsx
  - app/components/votacion-card.tsx
  - app/components/votacion-card.test.tsx
  - app/components/lobby-de-parlamentario.tsx
  - app/components/lobby-de-parlamentario.test.tsx
  - app/components/lobby-menciones-de-boletin.tsx
  - app/components/lobby-menciones-de-boletin.test.tsx
  - app/components/contratos-de-parlamentario.tsx
  - app/components/contratos-de-parlamentario.test.tsx
  - app/components/financiamiento-de-parlamentario.tsx
  - app/components/financiamiento-de-parlamentario.test.tsx
  - app/components/contratos-por-contraparte.tsx
  - app/components/contratos-por-contraparte.test.tsx
  - app/components/aportes-por-contraparte.tsx
  - app/components/aportes-por-contraparte.test.tsx
autonomous: true
requirements: [FECHA-02]

must_haves:
  truths:
    - "El badge de los bloques de cruces dice que el dato fue recalculado por el Observatorio, no capturado de la fuente ese día"
    - "Ninguna fecha del hecho aparece sin rótulo junto a un badge de procedencia"
    - "'corte al' ya no cubre dos columnas de naturaleza distinta: la fuente y nuestra ingesta se nombran por separado"
  artifacts:
    - path: "app/components/cruces-de-parlamentario.tsx"
      provides: "badge con origenFecha=recalculo"
      contains: "origenFecha"
    - path: "app/components/financiamiento-de-parlamentario.tsx"
      provides: "rótulos separados fuente vs ingesta"
      contains: "nuestra ingesta llega hasta el"
  key_links:
    - from: "app/components/cruces-de-parlamentario.tsx"
      to: "app/components/provenance-badge.tsx"
      via: "prop origenFecha"
      pattern: "origenFecha=\\{?\"recalculo\""
    - from: "app/components/votacion-card.tsx"
      to: "app/lib/format.ts"
      via: "fechaHechoCorta"
      pattern: "fechaHechoCorta"
---

<objective>
Cerrar el carril parlamentario / lobby / cruces / dinero / búsqueda: **F-02** (el reloj del
pipeline presentado como el de la fuente), **F-07** (fechas del hecho sin rótulo), **F-08**
(`fecha_corte` e `ingestado_hasta` bajo el mismo rótulo) y el resto de **F-05**
(`votacion.fecha` / `lobby_audiencia.fecha`).

**F-12 NO viaja en este plan.** El chip de año de `/buscar` es de otro carril y su premisa cambió:
el audit lo dio por INERTE, pero el grep vivo demuestra que `buscar-filtros.tsx:490` sí pasa
`anio={row.anio}`. Se cierra en el **Plan 04, Task 3**, con la rama correcta (rotular, no borrar).

Purpose: es donde el hecho y la captura conviven en la MISMA fila —la fecha sola en `font-mono` a
centímetros de un badge de procedencia— que es exactamente el defecto de desambiguación que la fase
existe para cerrar.
Output: cada fecha visible de estas superficies lleva un sustantivo que dice de qué es.

**Gate MONEY:** las superficies de dinero siguen OFF. El copy se corrige en el código; NINGÚN flag
se toca (`git diff` no puede incluir `.env`, `.env.example` ni el gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-CONTEXT.md
@.planning/phases/116-fecha-audit-sem-ntica-de-cada-fecha-visible/116-FECHAS-AUDIT.md
@.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-01-SUMMARY.md

<interfaces>
Tras el Plan 01 (`app/lib/format.ts`):
  fechaHechoCorta(d: Date): string
  fechaHechoCortaSegura(raw: string | null | undefined, fallback?: string): string
  fechaPlausible(d: Date, now?: Date): boolean
`fechaCorta` fija `timeZone: "UTC"`.

`ProvenanceBadge` acepta `origenFecha?: "fuente" | "recalculo"` y `notaAgregacion?: string`.
`origenFecha="recalculo"` ⇒ el badge rinde `recalculado por el Observatorio al {fecha}`.

Contraejemplos limpios que el audit manda copiar (rótulos que YA existen en el árbol):
  `Reunión registrada el {fecha}`  — cruces-de-parlamentario.tsx:178
  `Presentada el {fecha}`          — patrimonio-de-parlamentario.tsx:418
  `Fecha del aporte:`              — financiamiento-de-parlamentario.tsx:213

ANCHORS: re-localizar SIEMPRE con grep antes de editar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F-02 — el badge de cruces nombra el rebuild como rebuild</name>
  <files>app/components/cruces-de-parlamentario.tsx, app/components/cruces-de-parlamentario.test.tsx, app/components/cruces-de-proyecto.tsx, app/components/cruces-de-proyecto.test.tsx</files>
  <read_first>
    - `app/components/cruces-de-parlamentario.tsx` :160-205 (el comentario `:183-192` YA declara que
      `fecha_captura` es el `now()` del FULL REBUILD diario — hoy esa distinción sólo vive en el
      comentario, invisible para el ciudadano).
    - `app/components/cruces-de-proyecto.tsx` :160-185.
    - `116-FECHAS-AUDIT.md` §3 **F-02** (fix sugerido: copy propio, distinto del genérico) y §1.3
      fila de `cruce_senal.evidencia->items[].fecha` (drift 0 — la fecha de la reunión está bien).
    - `app/components/provenance-badge.tsx` tras el Plan 01.
  </read_first>
  <behavior>
    - `cruces-de-parlamentario`: con `s.fecha_captura = "2026-07-28T03:23:00Z"`, el texto renderizado
      contiene `recalculado por el Observatorio al 28 jul 2026` y NO contiene `según fuente al`.
    - `cruces-de-proyecto`: idem.
    - La fecha de la reunión (`Reunión registrada el …`) sigue presente y sin cambios de día.
  </behavior>
  <action>
    En ambos componentes, pasar `origenFecha="recalculo"` al `<ProvenanceBadge>` de la evidencia
    (`cruces-de-parlamentario.tsx:194`, `cruces-de-proyecto.tsx:176`). Actualizar el comentario
    contiguo: la distinción que hoy vive sólo en el código pasa a ser VISIBLE, citando la evidencia
    de PROD del audit (las 11 señales de D1165 con `min(fecha_captura) = max(fecha_captura) =
    2026-07-28 03:23:00.035505+00`, idénticas al microsegundo, sobre una reunión del `2025-04-10`).

    NO tocar `Reunión registrada el {…}` (es un rótulo limpio que el audit cita como contraejemplo)
    ni la elección de `s.fecha_captura` sobre `item.fecha` (WR-02: eso mata el stale-amber falso).

    Actualizar los asserts de `cruces-de-parlamentario.test.tsx:325,336` de `/según fuente al/` a
    `/recalculado por el Observatorio al/` y añadir un assert negativo de que `según fuente al` NO
    aparece en ese bloque. Añadir el assert equivalente en `cruces-de-proyecto.test.tsx`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/cruces-de-parlamentario.test.tsx components/cruces-de-proyecto.test.tsx lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'origenFecha="recalculo"' app/components/cruces-de-parlamentario.tsx` = 1 e idem en
      `cruces-de-proyecto.tsx`.
    - Los asserts de `<behavior>` pasan, incluido el negativo sobre `según fuente al`.
    - El guard anti-insinuación sigue verde con ambas superficies en `SUPERFICIES_FECHA`.
  </acceptance_criteria>
  <done>El ciudadano lee que el cruce se recalculó ese día, no que la fuente publicó algo ese día.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F-07 + F-05 — rótulo del hecho en votos y lobby</name>
  <files>app/components/votos-por-parlamentario.tsx, app/components/votos-por-parlamentario.test.tsx, app/components/votacion-card.tsx, app/components/votacion-card.test.tsx, app/components/lobby-de-parlamentario.tsx, app/components/lobby-de-parlamentario.test.tsx, app/components/lobby-menciones-de-boletin.tsx, app/components/lobby-menciones-de-boletin.test.tsx</files>
  <read_first>
    - `app/components/votos-por-parlamentario.tsx` :520-550 (la fecha sola en `font-mono` :528 y el
      badge :544 en la MISMA fila) + el guard anti-fecha-basura :190-240.
    - `app/components/votacion-card.tsx` :36-42 y :95.
    - `app/components/lobby-de-parlamentario.tsx` :150-155, :438-445, :475-490, :532-540.
    - `app/components/lobby-menciones-de-boletin.tsx` :98-135.
    - `116-FECHAS-AUDIT.md` §3 **F-07** (fix sugerido: los NOUN-labels exactos por emisor) y
      **F-05**; §1.3 fila de `lobby_audiencia.fecha` (drift REFUTADO por PROD: 0/17.762 — el defecto
      vivo es de copy, no de zona).
  </read_first>
  <behavior>
    - `votos-por-parlamentario`: con `e.fecha = "2023-11-17T00:14:41+00:00"` el texto contiene
      `Votada el 16 nov 2023` (rótulo + día chileno real).
    - `votacion-card`: con `fecha = "2026-07-22T00:00:00Z"` el texto contiene `Votada el 22 jul 2026`
      (date-only disfrazada ⇒ NO se corre a 21).
    - `lobby-de-parlamentario` (agrupada y cronológica) con `a.fecha` presente: el texto contiene
      `Reunión del …`.
    - `lobby-de-parlamentario` con `a.fecha = null` y `a.fecha_raw = null`: el texto contiene
      exactamente `Fecha no publicada` y **NO** contiene `Reunión del Fecha no publicada`
      (assert negativo OBLIGATORIO, en ambas vistas).
    - `lobby-de-parlamentario` con `a.fecha = null` y `a.fecha_raw = "junio 2026"`: el texto
      contiene `junio 2026` SIN el prefijo `Reunión del`.
    - `lobby-menciones-de-boletin`: el texto contiene `Reunión del …`.
    - En cada superficie, un assert de que el rótulo del hecho y el idiom de captura
      (`según fuente al`) coexisten y son distinguibles: ambos strings presentes en el mismo render.
  </behavior>
  <action>
    (a) **F-07 + F-05 en votos**: `votos-por-parlamentario.tsx:528` — anteponer el rótulo
    `Votada el ` a la fecha y sustituir `fechaCortaSegura(e.fecha)` por
    `fechaHechoCortaSegura(e.fecha)` (la columna es `votacion.fecha`, `timestamptz`). Mantener el
    `font-mono` sólo sobre la fecha, no sobre el rótulo. NO tocar el `mesAnioFormatter` de `:287`
    (el audit lo declara seguro: `timeZone:"UTC"` explícita + base `Date.UTC`) ni el guard
    anti-fecha-basura de `:234`.

    (b) `votacion-card.tsx:39` — rótulo `Votada el ` + `fechaHechoCorta(fecha)`.

    (c) **F-07 en lobby** — **el rótulo es CONDICIONAL, jamás un prefijo ciego.** Ambas vistas
    computan `fechaTexto` en `:152-154` (agrupada) y `:477-479` (cronológica) como
    `a.fecha ? fechaCorta(new Date(a.fecha)) : a.fecha_raw ?? "Fecha no publicada"`: la MISMA
    variable transporta o una fecha formateada o un fallback textual. Anteponer `Reunión del ` en
    el render (`:441`, `:487`) produciría `Reunión del Fecha no publicada` — peor que el defecto
    que F-07 cierra.

    Fix: rotular SOLO la rama con fecha real, componiendo el rótulo DENTRO del ternario donde
    `fechaTexto` se calcula (`Reunión del ${fechaCorta(new Date(a.fecha))}`), o llevando un flag
    hermano (`tieneFecha`) al render y envolviendo el prefijo con él. El fallback — el `a.fecha_raw`
    crudo de la fuente, o `Fecha no publicada` — viaja SIN prefijo, verbatim.

    Las fechas siguen con `fechaCorta` (PROD refutó el drift: `lobby_audiencia.fecha` está 100 % a
    las 04:00 UTC = 00:00 Chile) — dejar comentado ese hallazgo del audit para que nadie "arregle"
    lo que no está roto.

    (d) `lobby-menciones-de-boletin.tsx:129` — anteponer `Reunión del `.

    Cero cambio de estructura de DOM más allá del texto. Escribir los asserts de `<behavior>` antes
    de implementar.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/votos-por-parlamentario.test.tsx components/votacion-card.test.tsx components/lobby-de-parlamentario.test.tsx components/lobby-menciones-de-boletin.test.tsx lib/anti-insinuacion-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Votada el" app/components/votos-por-parlamentario.tsx` ≥ 1 y en `votacion-card.tsx` ≥ 1.
    - `grep -c "Reunión del" app/components/lobby-de-parlamentario.tsx` = 2 y en
      `lobby-menciones-de-boletin.tsx` ≥ 1.
    - `grep -c "Fecha no publicada" app/components/lobby-de-parlamentario.tsx` sin cambios respecto
      al estado previo, y los asserts negativos `Reunión del Fecha no publicada` pasan en ambas
      vistas.
    - `grep -n "Reunión del" app/components/lobby-de-parlamentario.tsx` apunta a las líneas del
      TERNARIO (cálculo de `fechaTexto`), no al JSX del render — prueba de que el rótulo es
      condicional.
    - `grep -v '^\s*[*/]' app/components/votacion-card.tsx | grep -c "fechaCorta("` = 0.
    - Todos los asserts de `<behavior>` pasan, incluido el de coexistencia rótulo-del-hecho +
      `según fuente al`.
  </acceptance_criteria>
  <done>Donde conviven dos fechas de naturaleza distinta, ambas están nombradas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: F-08 — `fecha_corte` vs `ingestado_hasta` (MONEY, gated)</name>
  <files>app/components/contratos-de-parlamentario.tsx, app/components/contratos-de-parlamentario.test.tsx, app/components/financiamiento-de-parlamentario.tsx, app/components/financiamiento-de-parlamentario.test.tsx, app/components/contratos-por-contraparte.tsx, app/components/contratos-por-contraparte.test.tsx, app/components/aportes-por-contraparte.tsx, app/components/aportes-por-contraparte.test.tsx</files>
  <read_first>
    - Los cuatro call-sites de "corte al": `contratos-de-parlamentario.tsx:184` y `:233`,
      `financiamiento-de-parlamentario.tsx:223` y `:363`, `contratos-por-contraparte.tsx:167`,
      `aportes-por-contraparte.tsx:188`.
    - `116-FECHAS-AUDIT.md` §3 **F-08** (fix sugerido con los rótulos exactos); §6
      límite 1 (`contrato` y `aporte` con 0 filas en PROD ⇒ el veredicto se respalda por código y
      schema, no por dato observado; F-08 debe cerrarse ANTES de cualquier flip del gate).
    - `app/lib/money-presentacion.ts` (`LEYENDA_ANTI_INSINUACION_MONEY` — el guard la resta;
      no tocarla).
  </read_first>
  <behavior>
    - `contratos-de-parlamentario`: el texto contiene `la fuente cubre hasta el {fecha}` para
      `fecha_corte` y `nuestra ingesta llega hasta el {fecha}` para `ingestado_hasta`; NO contiene
      `corte al`.
    - `financiamiento-de-parlamentario`: idem para sus dos call-sites.
    - `contratos-por-contraparte` y `aportes-por-contraparte`: `Consolidado` ya no va pelado —
      el texto contiene `Consolidado por el Observatorio`.
  </behavior>
  <action>
    (a) **F-08**: separar los rótulos por categoría, verbatim del fix sugerido del audit.
    Para `fecha_corte` (borde de lo que cubre la FUENTE): `la fuente cubre hasta el {fecha}`.
    Para `ingestado_hasta` (borde de lo que ingerimos NOSOTROS):
    `nuestra ingesta llega hasta el {fecha}`. Reescribir las cuatro frases completas conservando su
    sujeto actual, p. ej.:
      `Consultado por RUT; la fuente cubre hasta el {fechaCorteTexto}.`
      `Consultado por nombre del candidato; la fuente cubre hasta el {fechaCorteTexto}.`
      `Consolidado por el Observatorio; la fuente cubre hasta el {fechaCorteTexto}.`
      `Consultamos ChileCompra por el RUT de este parlamentario (nuestra ingesta llega hasta el {fechaTexto}) y no se registran contratos asociados a ese RUT a esa fecha.`
      `Consultamos SERVEL por este candidato (nuestra ingesta llega hasta el {fechaTexto}) y no se registran aportes asociados a ese candidato a esa fecha.`
    Nombrar el sujeto de "Consolidado" en las dos superficies de contraparte
    (`Consolidado por el Observatorio`). Las fechas siguen con `fechaCorta` (`fecha_corte`,
    `fecha_oc`, `fecha_aporte`, `ingestado_hasta` son `date` puros — §1.3 los declara `cumple`).

    **Los flags NO se tocan.** El gate MONEY sigue OFF; sólo cambia el copy en el código.

    Actualizar los tests de las cuatro superficies MONEY para reflejar el copy nuevo; añadir en
    cada una un assert negativo de `corte al`.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run components/contratos-de-parlamentario.test.tsx components/financiamiento-de-parlamentario.test.tsx components/contratos-por-contraparte.test.tsx components/aportes-por-contraparte.test.tsx lib/anti-insinuacion-guard.test.ts lib/money-antiflip-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "corte al" app/components/*.tsx | grep -v "\.test\." | grep -vc '^\s*[*/]'` = 0.
    - `grep -rlc "la fuente cubre hasta el" app/components/` cubre las 4 superficies MONEY.
    - `grep -rn "nuestra ingesta llega hasta el" app/components/` devuelve ≥ 2 hits
      (`contratos-de-parlamentario`, `financiamiento-de-parlamentario`).
    - `grep -c "Consolidado por el Observatorio" app/components/contratos-por-contraparte.tsx` = 1
      e idem en `aportes-por-contraparte.tsx`.
    - `git diff --name-only` NO incluye `.env`, `.env.example` ni ningún archivo de gate.
    - `pnpm --filter app test` exit 0 y `pnpm -r exec tsc -b` exit 0.
  </acceptance_criteria>
  <done>El día que MONEY se encienda, ninguna de las tres fechas de esas fichas será ambigua.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline de cruces → render | `cruce_senal.fecha_captura` es el `now()` del rebuild, no una observación de la fuente |
| superficies MONEY gated → futuro flip | copy que hoy no se ve pero se hará público el día del sign-off legal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-07 | Spoofing (atribución falsa a la fuente) | `cruces-de-*.tsx` | mitigate | `origenFecha="recalculo"` + assert negativo de `según fuente al` |
| T-117-08 | Repudiation (deuda de copy que se publica al flip) | superficies MONEY | mitigate | F-08 se cierra ANTES del flip; guard anti-flip MONEY se ejecuta en la verificación |
| T-117-09 | Elevation of privilege (flip accidental) | gate MONEY | mitigate | acceptance criteria exige `git diff` sin `.env`/gate; `money-antiflip-guard.test.ts` en el verify |
| T-117-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes |
</threat_model>

<verification>
- `pnpm --filter app test` verde; `pnpm -r exec tsc -b` exit 0.
- Guards de régimen verdes, con énfasis en anti-insinuación y anti-flip MONEY.
- `git diff --name-only` sin `.env`, `.env.example` ni archivos de gate.
</verification>

<success_criteria>
- F-02, F-05 (parte votos/lobby), F-07 y F-08 corregidos con evidencia antes/después.
- F-12 explicitamente NO viaja aqui: se cierra en el Plan 04, Task 3.
- Cero `corte al` sobreviviente en copy visible.
- MONEY sigue OFF; suite + typecheck + guards verdes.
</success_criteria>

<output>
Create `.planning/phases/117-fecha-fix-etiquetas-de-fecha-corregidas/117-03-SUMMARY.md` when done,
con el antes/después verbatim por hallazgo (archivo:línea + commit) para `117-DISPOSICION.md`.
</output>
