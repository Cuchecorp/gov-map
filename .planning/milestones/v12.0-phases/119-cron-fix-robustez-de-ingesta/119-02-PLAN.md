---
phase: 119-cron-fix-robustez-de-ingesta
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - packages/freshness/src/catalog.ts
  - packages/freshness/src/query-runner.ts
  - packages/freshness/src/evaluate.ts
  - packages/freshness/src/evaluate.test.ts
  - packages/freshness/src/cli.ts
autonomous: true
requirements: [CRON-02]

must_haves:
  truths:
    - "Una avería de `actualidad-refresh` dispara señal de frescura propia (hoy no dispara ninguna)"
    - "Un job de `pg_cron` que deja de correr produce señal stale con umbral derivado de su schedule"
    - "Los huecos deliberados de cobertura (W-3 backup-parlamentario, W-7 digest-daily) están DECLARADOS en `catalog.ts`, no olvidados"
  artifacts:
    - path: "packages/freshness/src/catalog.ts"
      provides: "entrada actualidad-refresh + catálogo de jobs pg_cron + comentario de huecos declarados"
      contains: "actualidad_senal"
    - path: "packages/freshness/src/query-runner.ts"
      provides: "lectura de cron.job_run_details por jobid"
      contains: "job_run_details"
  key_links:
    - from: "packages/freshness/src/query-runner.ts"
      to: "cron.job_run_details"
      via: "max(start_time) por jobid vía psql read-only"
      pattern: "job_run_details"
---

<objective>
Cerrar G3: el catálogo de frescura tiene 9 entradas y no cubre 8 de las 20 unidades de cron
inventariadas por 118 ⇒ una avería silenciosa de esas 8 hoy no dispara ninguna señal.

Purpose: la frescura es el único instrumento que el operador mira; un hueco no declarado en el
instrumento es indistinguible de un cron sano.
Output: entrada `actualidad-refresh`, una comprobación de `pg_cron` distinta de las de tabla, y
los huecos que se deciden dejar fuera declarados EN EL CÓDIGO con su razón.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-01-SUMMARY.md

Gap de origen: `118-CRON-VERDICTS.md` §4 fila G3 (línea 1826) + §3.3 (tabla `pg_cron` vivo ×
`job_run_details`, probe P6, ancla 2026-07-28) + §2 secciones PG-1 a PG-5.

Los 5 jobs de `pg_cron` vivos y sus schedules (§3.3, delta CERO contra migraciones, A3 CONFIRMADA):

| jobid | jobname | schedule | migración |
|---|---|---|---|
| 1 | process-ingest-jobs | `30 seconds` | `0003_orchestration.sql:214` |
| 2 | cleanup-net-http | (ver §3.3) | `0003_orchestration.sql:229` |
| 3 | net-materializar-aristas | (ver §3.3) | `0030_net.sql:162` |
| 4 | cruces-materializar | (ver §3.3) | `0039_cruce_senal.sql:138` |
| 5 | actualidad-materializar | `7 11,14,17,20 * * 1-5` | `0065_actualidad_senal.sql:326` |

Confirmar los schedules exactos leyendo §3.3 y/o `select jobid, jobname, schedule from cron.job order by jobid;`
por psql read-only — NO copiarlos de memoria.
</context>

<tasks>

<task type="auto">
  <name>Task 1: entrada `actualidad-refresh` en el catálogo</name>
  <read_first>
    - `packages/freshness/src/catalog.ts` (array `CATALOG`, forma de una entrada; el JSDoc de cabecera con la tabla fuente→tabla/columna)
    - `supabase/migrations/0065_actualidad_senal.sql` (tabla `actualidad_senal`; columna temporal = `fecha_captura`, REFUTACIÓN A2 de 118 §5 — NO es `creado_en`)
    - `.github/workflows/actualidad-refresh.yml` (cadencia intradía L-V `0 11,14,17,20 * * 1-5`)
    - 118-CRON-VERDICTS.md §2 W-1 y §4 fila G3, paso 1
  </read_first>
  <action>
    Añadir al array `CATALOG` una entrada:
      `fuente: "actualidad-refresh"`, `tabla: "actualidad_senal"`, `columna: "fecha_captura"`,
      `umbralDias: 2` (justificación en comentario: cadencia intradía L-V ⇒ >2 días es fin de
      semana largo + al menos una ventana perdida), `overrideEnv: "FRESHNESS_UMBRAL_ACTUALIDAD_REFRESH"`,
      `workflowYml: "actualidad-refresh.yml"`.
    Verificar por psql read-only que `actualidad_senal.fecha_captura` existe ANTES de commitear
    (`select column_name from information_schema.columns where table_name='actualidad_senal';`) —
    la lección A2 de 118 es exactamente que asumir la columna temporal quema.
    Actualizar la tabla-resumen del JSDoc de cabecera con la fila nueva.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'actualidad_senal' packages/freshness/src/catalog.ts` ≥ 1.
    - Test nuevo en `evaluate.test.ts`: con `ultimoUpsert` de hace 3 días ⇒ `stale:true`; de hace 1 día ⇒ `stale:false`.
    - `pnpm freshness --json` incluye la fuente `actualidad-refresh` con `ultimoUpsert` NO nulo (la tabla tiene datos en PROD; si saliera nulo, investigar la columna antes de dar por bueno).
  </acceptance_criteria>
  <done>W-1 deja de ser un punto ciego del instrumento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: señal de `pg_cron` por `max(start_time)` con umbral derivado del schedule</name>
  <read_first>
    - `packages/freshness/src/query-runner.ts` (helper `psql(dbUrl, sql)`, `queryFreshness`, patrón de degradación a `"n/d"`)
    - `packages/freshness/src/evaluate.ts` (regla fail-closed: desconocido ⇒ stale)
    - `packages/freshness/src/cli.ts` líneas 60-125 (`renderTable`) y 350-360 (`anyStale`)
    - 118-CRON-VERDICTS.md §3.3 (probe real de `cron.job_run_details`, incluida la nota de método de la línea 1338) y §4 fila G3 paso 2
  </read_first>
  <behavior>
    - Test 1: job con schedule `30 seconds` y `max(start_time)` de hace 1 hora ⇒ stale (umbral derivado, no el de 7 días de las tablas).
    - Test 2: job con schedule `7 11,14,17,20 * * 1-5` y `max(start_time)` de hace 4 horas un día hábil ⇒ NO stale.
    - Test 3: job con el mismo schedule y `max(start_time)` de hace 3 días ⇒ stale.
    - Test 4: `max(start_time)` nulo o ilegible ⇒ stale (desconocido = stale, MISMA regla fail-closed de `evaluate.ts`).
    - Test 5: la derivación del umbral es una función PURA testeable (`umbralDesdeSchedule(schedule: string): number` en horas), sin red ni DB.
  </behavior>
  <action>
    1. Añadir a `catalog.ts` un array SEPARADO `PGCRON_JOBS` (NO mezclar con `CATALOG`: los jobs de
       pg_cron no tienen tabla/columna ni workflow, su señal es distinta — mismo criterio con que
       `COBERTURA_VOTO_SENALES` vive separado de `CATALOG`). Cada entrada: `jobname`, `jobid`,
       `schedule` esperado (copiado de la migración citada) y `overrideEnv`.
    2. En `query-runner.ts`, añadir `queryPgCron(dbUrl)` que corra por el mismo helper `psql`
       read-only: `select jobid, max(start_time) from cron.job_run_details group by jobid;` y
       `select jobid, jobname, schedule, active from cron.job;`. Degradar a `null` (nunca 0, nunca
       fecha inventada) cuando la lectura falle, con el mismo mensaje PII-safe existente.
    3. Función pura `umbralDesdeSchedule` en `evaluate.ts` (o módulo hermano): tolerancia = N×
       intervalo nominal del schedule, con N declarado en constante y justificado en comentario;
       para `30 seconds` la unidad es horas, para crones diarios/intradía días. Se aserta que el
       schedule VIVO coincide con el esperado del catálogo — un drift de schedule es señal por sí
       mismo (`active=false` también ⇒ señal).
    4. Renderizar la tabla nueva en `cli.ts` como bloque APPEND (espejo exacto de
       `renderCoberturaVoto`), sin tocar la tabla de fuentes. Incorporarla al exit code igual que
       `anyStale` y al `--json` bajo su propia clave.
    5. Tests de `<behavior>` ANTES de la implementación.
  </action>
  <verify>
    <automated>pnpm --filter @obs/freshness test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'job_run_details' packages/freshness/src/query-runner.ts` ≥ 1.
    - Los 5 tests de `<behavior>` existen y pasan.
    - `pnpm freshness --json | grep -c pgcron` ≥ 1 y la salida lista los 5 jobs por `jobname` con su `stale` y su `maxStartTime`.
    - Ninguna cifra de la tabla nueva es un default silencioso: un job sin filas en `job_run_details` sale `n/d` + `stale:true`, jamás `0`.
    - `tsc -b` exit 0.
  </acceptance_criteria>
  <done>Los 5 jobs de pg_cron dejan de ser invisibles para el instrumento de frescura.</done>
</task>

<task type="auto">
  <name>Task 3: declarar en `catalog.ts` los huecos que se dejan fuera</name>
  <read_first>
    - `packages/freshness/src/catalog.ts` (JSDoc de cabecera, líneas 1-26)
    - 118-CRON-VERDICTS.md §2 W-3 (`backup-parlamentario`: NO escribe Supabase, su autoridad es el commit del bot — refutación A4) y §2 W-7 (`digest-daily`: NOTIF parked, schedule comentado, `notificacion_envio` 0 filas)
    - 118-CRON-VERDICTS.md §4 fila G3 paso 3
  </read_first>
  <action>
    Añadir al JSDoc de cabecera de `catalog.ts` un bloque `HUECOS DECLARADOS DE COBERTURA` que
    liste, con su razón y su cita archivo:línea, las unidades que el catálogo NO cubre A PROPÓSITO:
      - W-3 `backup-parlamentario`: no escribe Supabase (`backup-parlamentario.yml:38-42`, `:60`);
        su señal autoritativa es el commit del bot sobre `supabase/seeds/parlamentario.seed.json`
        — una señal de frescura sobre una tabla sería verde prestado, exactamente el defecto que
        G4 corrige. Si en el futuro se quiere cubrir, la vía es la fecha del commit, no una tabla.
      - W-7 `digest-daily`: NOTIF parked (`digest-daily.yml:17`, `:24-25`), schedule comentado;
        cubrirlo hoy produciría un stale permanente que no es avería sino gating.
    Redactar en tiempo presente y sin promesas ("queda fuera porque X", no "se añadirá cuando Y").
    El objetivo es que el hueco sea una DECISIÓN legible, no un olvido.
  </action>
  <verify>
    <automated>grep -v '^ \*$' packages/freshness/src/catalog.ts | grep -c 'HUECOS DECLARADOS DE COBERTURA'</automated>
  </verify>
  <acceptance_criteria>
    - El bloque existe en `catalog.ts` y nombra explícitamente `backup-parlamentario` y `digest-daily` con su cita archivo:línea.
    - No se añade ninguna entrada de `CATALOG` para esas dos unidades (el hueco es decisión, no relleno).
    - `pnpm --filter @obs/freshness test` verde.
  </acceptance_criteria>
  <done>Cada unidad no cubierta lo está por decisión escrita en el código, no por omisión.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| psql → freshness | nuevas consultas al schema `cron` de la DB de PROD (solo lectura) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04 | Elevation of privilege | `queryPgCron` | mitigate | consultas EXCLUSIVAMENTE `select` sobre `cron.job` y `cron.job_run_details`; ningún `cron.schedule`/`cron.unschedule` — el instrumento observa, nunca altera el scheduler |
| T-119-05 | Information disclosure | salida de `job_run_details` | mitigate | proyectar SOLO `jobid`, `jobname`, `schedule`, `active` y `max(start_time)`; NUNCA la columna `command` ni `return_message` (pueden contener URLs/keys de las llamadas `pg_net`) |
| T-119-06 | Spoofing | schedule declarado vs vivo | mitigate | el catálogo asevera el schedule esperado contra el vivo; un mismatch es señal, no se adopta el valor vivo en silencio |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- `pnpm --filter @obs/freshness test` verde; `tsc -b` exit 0.
- `pnpm freshness --json` incluye `actualidad-refresh` y el bloque de pg_cron.
- Las cifras de pg_cron cuadran con una lectura manual de `select jobid, max(start_time) from cron.job_run_details group by jobid;` (pegar ambas en el SUMMARY, sin URL).
</verification>

<success_criteria>
- G3 cerrado: cobertura ampliada donde procede y hueco declarado donde no.
- Cero cobertura fabricada: ninguna unidad aparece "verde" por una señal que no la mide.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-02-SUMMARY.md` when done
</output>
