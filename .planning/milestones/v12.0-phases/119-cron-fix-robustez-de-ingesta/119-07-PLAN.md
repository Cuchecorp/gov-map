---
phase: 119-cron-fix-robustez-de-ingesta
plan: 07
type: execute
wave: 4
depends_on: ["119-01", "119-02", "119-03", "119-04", "119-05", "119-06"]
files_modified:
  - .planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md
autonomous: false
requirements: [CRON-02]

must_haves:
  truths:
    - "Cada uno de los 11 gaps de 118 §4 tiene estado explícito: cerrado, diferido como deuda de operador, o en observación con criterio de cierre"
    - "Ninguna corrida de cron puede fabricar filas: la degradación honesta está aseverada por test o por observación"
    - "`pnpm freshness` refleja el estado real por fuente tras los fixes"
    - "La suite completa, tsc y los guards de régimen están verdes"
  artifacts:
    - path: ".planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md"
      provides: "registro de cierre por gap con evidencia"
      contains: "G1"
  key_links:
    - from: "119-GAP-CLOSURES.md"
      to: "118-CRON-VERDICTS.md §4"
      via: "un renglón por Gn con su evidencia"
      pattern: "G[0-9]+"
---

<objective>
Cerrar la fase: consolidar el estado de los 11 gaps de `118-CRON-VERDICTS.md` §4 en un registro
propio, re-verificar las dos deudas de operador (G8, G9-gemini) SIN re-pedirlas, re-observar G11
sin cerrarlo por inferencia, y correr la batería de régimen completa.

Purpose: 118 produjo el veredicto; 119 debe poder decir, gap por gap, qué quedó cerrado y con qué
evidencia — y qué NO, con su razón y sus pasos.
Output: `119-GAP-CLOSURES.md` + árbol verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-CONTEXT.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-01-SUMMARY.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-02-SUMMARY.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-03-SUMMARY.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-04-SUMMARY.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-05-SUMMARY.md
@.planning/phases/119-cron-fix-robustez-de-ingesta/119-06-SUMMARY.md

Fuente de verdad de los gaps: `118-CRON-VERDICTS.md` §4 (índice compacto + detalle) y §4.1.
Deuda de operador ya emitida: `118-OPERATOR-CHECKPOINT.md` (G8 Cloudflare, G9 `GEMINI_API_KEY`).
El documento de 118 NO se reescribe: 119 produce su propio registro.
</context>

<tasks>

<task type="auto">
  <name>Task 1: re-verificaciones que no cierran nada por inferencia (G8, G9-gemini, G11)</name>
  <read_first>
    - `118-OPERATOR-CHECKPOINT.md` completo (los pasos exactos y el permiso mínimo ya escritos — NO se re-redactan ni se re-piden)
    - `118-CRON-VERDICTS.md` §4 filas G8, G9 y G11, y §2 PG-5 (historial de 8 filas desde `2026-07-24 17:07`, control anti-poda: PG-2 conserva 1.344 filas del mismo período)
    - `supabase/migrations/0065_actualidad_senal.sql` :326 (`schedule` de `actualidad-materializar`: `7 11,14,17,20 * * 1-5`)
  </read_first>
  <action>
    1. **G8 (Cloudflare)**: correr `gh secret list --repo Cuchecorp/gov-map` y registrar NOMBRES y
       fechas. Si `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` aparecieron, re-disparar
       `deploy-cloudflare` y registrar el resultado. Si NO aparecieron: dejar la deuda abierta con
       puntero a `118-OPERATOR-CHECKPOINT.md`. PROHIBIDO re-emitir el checkpoint (ya se pidió una
       vez) y PROHIBIDO cargar valores.
    2. **G9-gemini**: mismo tratamiento; el fix YAML ya lo hizo el plan 06.
    3. **G11 (PG-5)**: volver a leer `cron.job_run_details` para `jobid=5` por psql read-only:
       `select jobid, status, start_time from cron.job_run_details where jobid = 5 order by start_time desc limit 60;`
       Contar los días hábiles cubiertos desde el 2026-07-28 y buscar huecos. REGLA: NO se cierra
       por inferencia. Sólo hay tres desenlaces admisibles:
         - evidencia de ≥2 semanas de cadencia sin huecos en días hábiles ⇒ se cierra como "arranque";
         - aparece otro día hábil vacío ⇒ escala a P1 con la hipótesis `job startup timeout`
           (el único fallo observado en PG-1 fue de ese tipo) y queda como gap abierto para la
           fase siguiente;
         - la ventana de esta fase no alcanza las 2 semanas ⇒ queda ABIERTO con el criterio de
           cierre escrito y la fecha a partir de la cual se puede evaluar.
       Registrar el conteo crudo de filas por día — sin interpretarlo de más.
  </action>
  <verify>
    <automated>grep -q 'Re-verificacion G8/G9/G11' .planning/phases/119-cron-fix-robustez-de-ingesta/119-07-SUMMARY.md && echo OK-REVERIFICACION</automated>
  </verify>
  <acceptance_criteria>
    - El SUMMARY de este plan contiene un bloque encabezado literalmente `## Re-verificacion G8/G9/G11` (sin tildes, para que el grep del verify muerda) con los hallazgos de los tres.
    - La salida de `gh secret list` queda en el registro con nombres y fechas, cero valores.
    - El estado de G8 y G9-gemini es "deuda de operador abierta" o "cerrada con evidencia de corrida" — nunca "asumida cerrada".
    - G11 tiene uno de los tres desenlaces admisibles, con el conteo de filas por día que lo respalda.
    - Cero re-emisión de checkpoint de operador.
  </acceptance_criteria>
  <done>Las tres cosas que el agente no puede cerrar por sí mismo quedan re-verificadas y honestamente etiquetadas.</done>
</task>

<task type="auto">
  <name>Task 2: batería de régimen + freshness real</name>
  <read_first>
    - `.planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh` (el script de 118; se corre con `STRICT=1` y debe seguir verde — el documento de 118 NO se reescribe)
    - Los 6 SUMMARYs de esta fase (qué se tocó y qué verificación quedó pendiente en cada uno)
    - `.github/workflows/ci.yml` (los guards de régimen que corren en CI: lockdown, anti-insinuación, env-example, name-match-rut, bento, etc.)
  </read_first>
  <action>
    Correr y registrar, con la salida pegada (sin URL ni credenciales):
    1. `pnpm test` (raíz: packages + app). Base declarada de la fase: 1560 tests; debe CRECER por
       los tests nuevos. Si algún test pre-existente se rompió, arreglarlo o declararlo como deuda
       con su causa — nunca silenciarlo.
    2. `pnpm typecheck` (`tsc -b`) exit 0.
    3. Cada guard de régimen individualmente (los de `ci.yml`), en particular `env-example`
       (por si algún plan añadió una variable) y `lockdown-guard`.
    4. `STRICT=1 bash .planning/phases/118-cron-audit-veredicto-por-cron-con-evidencia/check-crons.sh`
       verde.
    5. **Verificación delegada por el plan 04 (G5)**: `select source, count(*) from source_snapshot group by 1;`
       por psql read-only, con la salida pegada (sin URL). Esperado: pasar de 2 fuentes (`leyes`,
       `infoprobidad`) a 5, sumando `agenda`, `identity` y `leylobby`. Si alguna de las tres NO
       aparece porque su conector no se corrió en la ventana, decirlo así y nombrar cuál falta —
       la cifra "2 → 5" NO se declara alcanzada sin las filas.
    6. **Verificación delegada por el plan 05 (G7)**: constancia de al menos un replay `--from-r2`
       EJECUTADO contra un objeto real de R2, con su salida. Si no se pudo ejecutar (no hay objeto
       disponible, credenciales R2 ausentes), registrar "PASS con limitación declarada" NOMBRANDO
       la limitación y el comando exacto que la cerraría (precedente 114-03) — jamás un PASS pelado.
    7. `pnpm freshness` completo: pegar la tabla resultante. Por cada fuente `stale:true`, decir en
       una línea si es (i) avería real, (ii) gating declarado (MONEY/SERVEL, §4.1), o (iii) falta
       de datos honesta. Una tabla con stale sin explicación NO cierra la fase.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` verde con conteo total ≥ 1560 y el delta explicado por los tests nuevos de la fase.
    - `pnpm typecheck` exit 0.
    - Todos los guards de `ci.yml` verdes, con `env-example` explícitamente nombrado.
    - `STRICT=1 check-crons.sh` verde; `git diff` sobre `118-CRON-VERDICTS.md` VACÍO.
    - La salida de `select source, count(*) from source_snapshot group by 1;` está pegada, y el conteo de fuentes distintas está declarado explícitamente (5, o menos con las faltantes nombradas).
    - El replay `--from-r2` figura EJECUTADO con salida, o como `PASS con limitación declarada` con la limitación nombrada y el comando de cierre.
    - Cada fila stale de `pnpm freshness` tiene su causa clasificada en (i)/(ii)/(iii).
  </acceptance_criteria>
  <done>El árbol está verde y la tabla de frescura es legible sin adivinar.</done>
</task>

<task type="auto">
  <name>Task 3: escribir `119-GAP-CLOSURES.md`</name>
  <read_first>
    - `118-CRON-VERDICTS.md` §4 (índice compacto: los 11 ids `Gn` con su prioridad y unidad — los ids son ESTABLES y se citan tal cual) y §4.1
    - Los 6 SUMMARYs de esta fase
    - `118-CRON-VERDICTS.md` §7 (Verificación de cierre) como referencia de formato y nivel de evidencia
  </read_first>
  <action>
    Escribir `.planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md` con:
    1. **Front-matter** con la fecha ancla de las observaciones y la nota de que las cifras son
       vivas (no heredadas), espejando la convención de 118.
    2. **Tabla maestra**: un renglón por `Gn` (los 11, ninguno omitido) con columnas
       `Gn | prioridad original | estado (cerrado / diferido-operador / abierto-en-observación) |
       plan que lo cerró | evidencia (archivo:línea, test, comando + salida) | nota`.
    2b. **Anclaje obligatorio del renglón G1**: el criterio de cierre que 118 §4 fijó para G1 es
       literal — «`pnpm freshness` debe pasar `lobby-leylobby` a `stale:false`». Si tras el fix del
       plan 06 la señal SIGUE `stale:true`, el renglón G1 NO puede decir "cerrado" a secas: debe
       decir qué se cerró (el mecanismo: el cursor avanza cuando hay datos) y POR QUÉ el criterio
       original no se cumple (p.ej. la fuente sigue degradando 403/503 ⇒ no hay datos que ingerir),
       citando la evidencia. Es una divergencia honesta anclada, no un criterio reinterpretado a
       conveniencia. Si el criterio SÍ se cumple, pegar la fila de `pnpm freshness` que lo prueba.
    3. **Sección por gap NO cerrado** con: razón, pasos exactos para cerrarlo, quién debe hacerlo
       (agente vs operador) y criterio de cierre verificable.
    4. **Sección "Lo que NO se tocó y por qué"**: los estados esperados de §4.1 (MONEY/SERVEL
       gated, W-9 sin schedule por WAF, digest/roster gated, backfills LOCAL, ci/deploy no-ingesta)
       re-declarados para que la fase siguiente no los tome como backlog.
    5. **Sección "Degradación honesta — evidencia"**: por cada conector tocado, el test o la
       corrida que demuestra que sin datos NO escribe filas (criterio 2 del ROADMAP §119).
    6. **Sección "Dos etapas y rate-limit intactos"**: por cada conector tocado, la constatación
       de que fuente→R2→Supabase, el hash-check y el rate-limit 2-3s siguen en su lugar
       (criterio 3 del ROADMAP §119), con `git diff --stat` de los archivos de política.
    REGLAS: cero valores de secrets; cero afirmación sin evidencia; si un criterio quedó a medias,
    se dice "PASS con limitación declarada" y se nombra la limitación (precedente 114-03).
  </action>
  <verify>
    <automated>test -f .planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md && grep -c '^| G' .planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md && grep -q 'G11' .planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md && echo OK-REGISTRO</automated>
  </verify>
  <acceptance_criteria>
    - La tabla maestra tiene exactamente 11 renglones `G1`..`G11`, ninguno con estado vacío.
    - El renglón G1 cita el criterio original de 118 (`lobby-leylobby` a `stale:false`) y declara si se cumple; si no se cumple, la razón está escrita y respaldada por evidencia.
    - Cada renglón "cerrado" cita al menos una evidencia verificable (comando + salida, o archivo:línea + test).
    - Cada renglón "diferido"/"abierto" tiene pasos y criterio de cierre.
    - `grep -ci 'api_key\|secret_key\|token=' 119-GAP-CLOSURES.md` no revela ningún VALOR (nombres de variables sí, valores jamás).
    - Los 4 success criteria del ROADMAP §119 aparecen nombrados con su veredicto (PASS / PASS con limitación declarada / FAIL).
  </acceptance_criteria>
  <done>La fase deja un registro que se puede auditar sin volver a leer el código.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: lectura fria del registro de cierre por el operador</name>
  <files>.planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md</files>
  <action>Presentar el registro de cierre al operador para lectura fria; no se avanza sin su senal.</action>
  <what-built>
    Los 11 gaps de la auditoría 118 procesados: 5 P1 y 3 P2 cerrados en código con tests, 2 P2
    re-verificados como deuda de operador (G8 Cloudflare, G9 `GEMINI_API_KEY`) y 1 P2 en
    observación (G11). Registro consolidado en `119-GAP-CLOSURES.md`.
  </what-built>
  <how-to-verify>
    1. Abrir `.planning/phases/119-cron-fix-robustez-de-ingesta/119-GAP-CLOSURES.md` y revisar la
       tabla maestra: ¿algún gap declarado "cerrado" sin evidencia que puedas comprobar tú mismo?
    2. Correr `pnpm freshness` y contrastar la tabla con la clasificación (i)/(ii)/(iii) del
       registro: ¿alguna fuente stale queda sin explicación?
    3. Revisar la sección "Degradación honesta": ¿te convence que ningún cron puede escribir filas
       inventadas?
    4. Revisar la sección de deuda de operador: los pasos de G8/G9 ya estaban en
       `118-OPERATOR-CHECKPOINT.md`; aquí sólo se re-verificó su estado. Si quieres cargarlos, es
       tu acto, no del agente.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o indica qué gap necesita más evidencia</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gh secret list` → documento de cierre | metadatos de secrets que se transcriben a un archivo versionado |
| psql PROD → documento de cierre | cifras vivas transcritas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-21 | Information disclosure | `119-GAP-CLOSURES.md` | mitigate | sólo NOMBRES y fechas de secrets; ninguna URL de DB, ningún token, ningún RUT — el guard de PII de `ci.yml` sigue aplicando al repo |
| T-119-22 | Repudiation | cifras del registro | mitigate | cada cifra lleva el comando que la produjo y la fecha ancla; nada se copia de 118 sin re-medir cuando la fase lo tocó |
| T-119-23 | Tampering | documento de 118 | mitigate | `118-CRON-VERDICTS.md` es inmutable en esta fase: `git diff` sobre él debe salir vacío (acceptance criteria del Task 2) |
| T-119-SC | Tampering | dependencias npm | mitigate | plan sin instalaciones; cualquier paquete nuevo ⇒ detenerse y escalar |
</threat_model>

<verification>
- Los 4 success criteria del ROADMAP §119 evaluados uno por uno en `119-GAP-CLOSURES.md`.
- `pnpm test` + `pnpm typecheck` + guards + `STRICT=1 check-crons.sh` verdes.
- `git diff .planning/phases/118-*/118-CRON-VERDICTS.md` vacío.
</verification>

<success_criteria>
- Los 11 gaps con estado explícito y evidencia.
- Ningún cierre por inferencia (G11 en particular).
- Ninguna deuda de operador re-pedida.
</success_criteria>

<output>
Create `.planning/phases/119-cron-fix-robustez-de-ingesta/119-07-SUMMARY.md` when done
</output>
