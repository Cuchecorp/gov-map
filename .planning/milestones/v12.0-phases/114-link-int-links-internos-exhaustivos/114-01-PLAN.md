---
phase: 114-link-int-links-internos-exhaustivos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/links-internos-manifiesto.mjs
  - scripts/verificar-links-internos.mjs
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt
  - .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json
autonomous: true
requirements: [LINK-02]

must_haves:
  truths:
    - "Existe un manifiesto declarativo que enumera cada link interno del inventario 113 instanciado con los sujetos de §1"
    - "Las 77 referencias del inventario (66 filas AN + 11 filas de chrome) están cubiertas una-a-una, sin gap silencioso"
    - "Un operador puede correr un solo comando y obtener el estado HTTP de cada link interno contra el deploy real"
    - "La corrida deja una salida guardada (txt legible + json máquina) que otra persona puede re-generar"
    - "Los links bajo gate OFF se verifican como AUSENCIA en el DOM del emisor, nunca como destino"
  artifacts:
    - path: "scripts/links-internos-manifiesto.mjs"
      provides: "Universo declarativo de links internos derivado del inventario 113, con trazabilidad ref-por-ref"
      exports: ["MANIFIESTO", "SUJETOS", "BASE_URL", "EXCLUIDOS", "REFS_INVENTARIO"]
    - path: "scripts/verificar-links-internos.mjs"
      provides: "Runner reproducible: status + ausencia, salida txt/json, filtrable por ruta y por tipo"
    - path: ".planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json"
      provides: "Estado ANTES de cualquier fix"
  key_links:
    - from: "scripts/verificar-links-internos.mjs"
      to: "scripts/links-internos-manifiesto.mjs"
      via: "import estático del manifiesto"
      pattern: "from \"\\./links-internos-manifiesto\\.mjs\""
    - from: "scripts/verificar-links-internos.mjs"
      to: "https://observatorio-congreso.thevalis.workers.dev"
      via: "fetch secuencial con delay"
      pattern: "BASE_URL"
---

<objective>
Construir la infraestructura reproducible de verificación de links internos (manifiesto + runner) y
ejecutar la corrida PRE-FIX completa contra el deploy real, dejando la salida guardada como artefacto.

Purpose: LINK-02 exige verificación exhaustiva sobre el deploy real con corrida reproducible. El
universo NO se descubre por crawl — se deriva del inventario rector 113 (`estado: validado`), y la
exhaustividad se prueba por IGUALDAD contra las referencias del inventario, nunca por un piso.
Output: `scripts/links-internos-manifiesto.mjs`, `scripts/verificar-links-internos.mjs`,
`114-CORRIDA-PRE.txt`, `114-CORRIDA-PRE.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md
@.planning/phases/114-link-int-links-internos-exhaustivos/114-VALIDATION.md
@scripts/validar-deeplinks.mjs

<interfaces>
<!-- Precedente de estilo: scripts/validar-deeplinks.mjs (CERO dependencias externas, parseArgs de
     node:util, UA identificatorio, sleep entre requests, tabla legible + exit code). -->

UA identificatorio ya usado en el repo (scripts/validar-deeplinks.mjs:30-31):
  "ObservatorioCongreso360/1.0 (+https://observatorio-congreso.thevalis.workers.dev; contacto: sanchez.rossi@gmail.com)"

Hallazgo de resolución de dependencias (verificado 2026-07-28):
  cheerio 1.2.0 está declarado SOLO en packages/agenda, packages/bio y packages/lobby.
  NO está en el package.json raíz ⇒ bajo pnpm (node_modules estricto) un script en scripts/
  probablemente NO puede `import("cheerio")`. El Task 2 resuelve esto con una probe explícita.

Directorio temporal portable (Windows/Git Bash NO define $TMPDIR):
  usar SIEMPRE `os.tmpdir()` desde Node, jamás `$TMPDIR` ni `process.env.TMPDIR` — son `undefined`
  en este entorno y harían escribir en la raíz del repo o crashear el comando.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifiesto declarativo del universo, con cobertura probada ref-por-ref</name>
  <files>scripts/links-internos-manifiesto.mjs</files>
  <read_first>
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` — leer §1
      (sujetos deterministas, líneas ~291-457), §2 (chrome C-01..C-04, ~458-523), §5 (gates y su
      estado, ~1914-1953) y CADA bloque `#### Tabla A — links internos` de §4 (las 15 rutas,
      ~985-1912) más los apéndices `4.1.b`, `4.2.b`, `4.3.b`, `4.9.b` (not-found).
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-CONTEXT.md` — sección
      `## Implementation Decisions` (alcance y exclusiones).
    - `app/components/capa1/tramitacion-stepper.tsx` — el emisor E-045, para resolver las anclas de
      la fila `4.2-A5`.
    - `scripts/validar-deeplinks.mjs` — estilo de script del repo.
  </read_first>
  <action>
    Crear `scripts/links-internos-manifiesto.mjs` como módulo ESM de DATOS puros (sin red, sin I/O),
    exportando `BASE_URL`, `SUJETOS`, `REFS_INVENTARIO`, `MANIFIESTO` y `EXCLUIDOS`.

    `BASE_URL` = "https://observatorio-congreso.thevalis.workers.dev" (deploy auditado por 113).

    `SUJETOS` instancia los sujetos deterministas de §1 del inventario, con los ids EXACTOS en
    formato PK string: diputado `D1165`, senador `S1338`, boletín bicameral `14309-04`, boletín
    solo-Senado `17870-05`. Añadir además ids inválidos deterministas para ejercitar las cuatro
    páginas `not-found`: `parlamentarioInexistente` = "D0000000", `boletinInexistente` = "00000-00",
    `seedInexistente` = "D0000000", `contraparteInexistente` = "c:sujeto-inexistente" (este último
    es el placeholder no-RUT que ya usa §5 del inventario; JAMÁS usar un valor con forma de RUT).

    `REFS_INVENTARIO` = array congelado con las **77 referencias** que el inventario 113 define y que
    este manifiesto debe cubrir en su totalidad. Conteo verificado el 2026-07-28 sobre
    `113-INVENTARIO.md`:
      · **66 filas `AN` de Tabla A** — `4.1-A1..A20`, `4.1.b-A1`, `4.2-A1..A11`, `4.2.b-A1`,
        `4.3-A1..A3`, `4.3.b-A1`, `4.4-A1..A4`, `4.5-A1..A8`, `4.6-A1..A4`, `4.7-A1`, `4.8-A1`,
        `4.9-A1..A2`, `4.9.b-A1`, `4.10-A1`, `4.11-A1..A4`, `4.12-A1`, `4.13-A1`, `4.14-A1`.
      · **11 filas de chrome** — `C-01-1..C-01-4`, `C-02-1..C-02-5`, `C-03-1`, `C-04-1`.
    Dejar en un comentario del módulo los dos comandos de derivación re-ejecutables, que deben seguir
    dando 66 y 11 respectivamente: el `awk` que cuenta las filas de forma `| A<n> |` desde la línea
    985 del inventario, y el equivalente acotado a las líneas 458-523 contando las filas `| <n> |`
    del chrome. Si el conteo derivado difiere de 77, DETENERSE y declarar la deriva — el inventario
    es el denominador, no una cifra recordada.

    `MANIFIESTO` = array de entradas, cada una con estas claves obligatorias:
      `id` — identificador estable de la entrada, p. ej. "4.1-A1", "C-02-4", "4.2.b-A1".
      `inventarioRef` — **array** de refs de `REFS_INVENTARIO` que esta entrada cubre. Es array, no
               escalar, justamente para permitir **colapsar** N filas del inventario que resuelven al
               mismo destino verificable en UNA entrada (p. ej. paginaciones que difieren solo en el
               signo de la página). Colapsar es válido si queda declarado ref-por-ref; omitir no.
      `origen` — ruta (con sujeto instanciado) que EMITE el link, o "chrome" para C-01..C-04.
      `href` — URL interna instanciada, relativa (empieza con "/"), o null cuando el tipo es
               `ausencia` y lo que se busca es un patrón.
      `tipo` — uno de: "status" | "ancla" | "ausencia".
      `espera` — para "status": "no-404" (default) o 404 cuando el 404 es el comportamiento
                 declarado del inventario; para "ancla": el id sin "#"; para "ausencia": el patrón
                 literal que NO debe aparecer en el HTML del `origen`.
      `destino` — ruta que se solicita para verificar (para "ancla", la página donde debe existir el id).
      `gate` — lista cerrada del inventario §5.1: "—" | "NET" | "CRUCES" | "VSIM" | "MONEY" | "NOTIF".
      `nota` — cita corta del inventario que justifica la fila.

    Poblar el MANIFIESTO recorriendo `REFS_INVENTARIO` en orden y resolviendo cada ref, cubriendo:
    (a) Chrome: `/metodologia` (C-01-2), `/sobre` (C-01-3), los 5 ítems de nav (C-02-1..5) y `/`
        (C-03-1). Los tres refs de chrome que NO son links internos van a `EXCLUIDOS`, ver abajo.
    (b) Rutas estáticas del universo, cada una como entrada `status`: `/`, `/buscar`,
        `/parlamentarios`, `/agenda`, `/red`, `/sobre`, `/metodologia`, `/comparar`, `/cuenta`,
        `/notificaciones/baja`, `/notificaciones/confirmar?token=x`.
    (c) Rutas dinámicas instanciadas: `/parlamentario/D1165`, `/parlamentario/S1338`,
        `/proyecto/14309-04`, `/proyecto/17870-05`.
    (d) Variantes con query de las Tablas A, con el sujeto real: `/comparar?a=D1165` (4.1-A1),
        `/red?seed=D1165` (4.1-A2, gate NET ON), `/parlamentario/D1165?vista=cronologica` (4.1-A12),
        las paginaciones de votos/lobby/patrimonio de 4.1 (A10/A14/A17) usando el nombre de parámetro
        REAL que emite el código — derivarlo leyendo el emisor citado en la fila del inventario, no
        inventarlo —, `/agenda?semana=2026-W30`, `/agenda?q=trabajo&camara=diputados` (4.5-A2),
        `/buscar?q=pension&page=1` y `?page=2` (4.6-A2/A3).
    (e) Rutas de not-found (4.1.b, 4.2.b, 4.3.b, 4.9.b): entrada `status` con `espera: 404` para la
        propia URL inválida, MÁS una entrada `status` para el único href que emiten (`/`).
    (f) Entradas `ancla` para CADA `#id` de las Tablas A que hoy se emite:
        de `/parlamentario/D1165` → votos, lobby, patrimonio, cruces;
        de `/proyecto/14309-04` → estado, timeline, votaciones, autores, lobby-tramitacion,
        lobby-menciones, cruces, idea-matriz, cuerpos-legales, similares, validacion-fuente.
        Para `/parlamentario/S1338` registrar las anclas que el inventario declara REDUCIDAS
        (sin `#cruces`, sin detalle de lobby) con `nota` citando la "Diferencia por sujeto".
    (g) Entradas `ausencia` para los gates OFF (§5): en `/parlamentario/D1165` y
        `/proyecto/14309-04` el patrón `href="/contraparte/` (MONEY) y el patrón `/cuenta?next=`
        (NOTIF); las anclas MONEY `#dinero`, `#financiamiento`, `#contratos`, `#aportes` como
        `ausencia` en sus emisores. Añadir la presencia declarada por §5 de
        `id="financiamiento-pendiente"` como entrada `ancla` en `/parlamentario/D1165`.

    **Regla única para filas que exigen un id de PROD no fijado en §1** (afecta al menos a `4.1-A7`
    `buildVotosVerHref`, `4.1-A16` `buildVerHref(version_id)` y `4.2-A4` `buildUrgenciasHref`): un
    href que es la **misma ruta con un query-param añadido** NO requiere inventar un id. Se verifica
    la **ruta base** (que ya está en el manifiesto) y la entrada se registra igual, con su
    `inventarioRef`, `tipo: "status"`, `destino` = la ruta base, y
    `nota: "query-param sobre ruta ya verificada — id de PROD no fijado en §1; limitación declarada"`.
    Así la fila **cuenta para la cobertura** en vez de desaparecer. Prohibido inventar un `version_id`
    o un id de urgencia.

    **Anclas del stepper (`4.2-A5`, E-045 `app/components/capa1/tramitacion-stepper.tsx:133`):** el
    inventario las marca `ancla #id? = sí` con destino "misma ruta". Leer el emisor y (a) incluir cada
    ancla que emita como entrada `ancla` propia, o (b) si sus ids **colisionan** con anclas ya
    cubiertas por `4.2-A1` (p. ej. `#timeline`, `#estado`), declarar la colisión en la `nota` citando
    `archivo:línea` y colapsarlas vía `inventarioRef`. Lo prohibido es que `4.2-A5` no aparezca en
    ningún `inventarioRef`.

    `EXCLUIDOS` = array de `{ id, inventarioRef, razon }`. Cada ref excluida lleva su razón
    **individual** (una por ref, sin agrupar). Debe cubrir al menos:
      · `C-01-1` — link externo CC BY 4.0, no es navegación interna (va a la Phase 115).
      · `C-01-4` — `mailto:`, no es navegación interna.
      · `C-04-1` — placeholder dinámico: `Breadcrumbs` no emite href propio, renderiza los `items`
        literales de la página llamante; sus hrefs reales ya viven en las filas de la ruta llamante.
      · Emisores huérfanos E-003 (voto-ficha-row) y E-008 (actualidad-module) — no se montan en
        ninguna ruta (evidencia 113); citar la ref de Tabla A que quede cubierta por esa razón.
      · `/admin/revisar-entidades` — EXCLUIDA por decisión LOCKED del CONTEXT de 113.
      · Las filas de `4.3` (`/contraparte/[id]`) que el gate MONEY deja sin emitir, si se decide no
        instanciarlas como `status` — cada una con su razón citando §5.

    **Compuerta de cobertura (invariante del módulo):** la unión de todos los `inventarioRef` de
    `MANIFIESTO` más los de `EXCLUIDOS` debe ser **exactamente igual** al conjunto `REFS_INVENTARIO`
    — ni una ref de menos (gap), ni una ref inventada (deriva).

    El módulo NO hace red ni escribe archivos. Cada entrada lleva su `nota` con la referencia al
    inventario; el que lea el manifiesto debe poder auditar el universo sin abrir el código de la app.
  </action>
  <verify>
    <automated>node -e "import('./scripts/links-internos-manifiesto.mjs').then(m=>{const R=new Set(m.REFS_INVENTARIO);if(R.size!==77)throw new Error('REFS_INVENTARIO='+R.size+' esperado 77');const ids=m.MANIFIESTO.map(e=>e.id);if(new Set(ids).size!==ids.length)throw new Error('ids duplicados');for(const e of m.MANIFIESTO){if(['status','ancla','ausencia'].indexOf(e.tipo)<0)throw new Error('tipo invalido '+e.tipo);if(['—','NET','CRUCES','VSIM','MONEY','NOTIF'].indexOf(e.gate)<0)throw new Error('gate invalido '+e.gate);if(!Array.isArray(e.inventarioRef)||!e.inventarioRef.length)throw new Error('sin inventarioRef: '+e.id);}const cub=new Set();for(const e of m.MANIFIESTO)for(const r of e.inventarioRef)cub.add(r);for(const x of m.EXCLUIDOS){if(!Array.isArray(x.inventarioRef)||!x.inventarioRef.length)throw new Error('EXCLUIDOS sin inventarioRef: '+x.id);if(!x.razon)throw new Error('EXCLUIDOS sin razon: '+x.id);for(const r of x.inventarioRef)cub.add(r);}const falt=[...R].filter(r=>!cub.has(r));const sobra=[...cub].filter(r=>!R.has(r));if(falt.length)throw new Error('REFS SIN COBERTURA ('+falt.length+'): '+falt.join(','));if(sobra.length)throw new Error('REFS INVENTADAS: '+sobra.join(','));const c=k=>m.MANIFIESTO.filter(e=>e.tipo===k).length;if(!c('status')||!c('ancla')||!c('ausencia'))throw new Error('falta algun tipo');console.log('OK cobertura 77/77 | entradas',m.MANIFIESTO.length,'| status',c('status'),'ancla',c('ancla'),'ausencia',c('ausencia'),'| excluidos',m.EXCLUIDOS.length);}).catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - El comando de `<verify>` sale exit 0 e imprime `OK cobertura 77/77 | …`. **Igualdad exacta**, no
      un piso: cualquier ref del inventario sin cubrir hace fallar el comando nombrando las faltantes.
    - `REFS_INVENTARIO.length === 77`, y los dos `awk` de derivación citados en el módulo siguen dando
      **66** filas `AN` y **11** filas de chrome al re-ejecutarse contra `113-INVENTARIO.md`.
    - Toda entrada de `MANIFIESTO` y de `EXCLUIDOS` tiene `inventarioRef` no vacío; toda entrada de
      `EXCLUIDOS` tiene además `razon` propia (una por ref, sin agrupar).
    - `4.1-A7`, `4.1-A16` y `4.2-A4` aparecen en algún `inventarioRef`, con la nota de limitación de
      query-param; `4.2-A5` (stepper) aparece cubierto o con colisión declarada citando `archivo:línea`.
    - Cero RUT: `! grep -qE '[0-9]{7,8}-[0-9kK]' scripts/links-internos-manifiesto.mjs` sale exit 0.
    - Los 4 sujetos de §1 aparecen literalmente: `grep -c 'D1165'`, `S1338`, `14309-04`, `17870-05`
      → cada uno ≥ 1.
    - El módulo no hace red ni I/O: `! grep -qE 'node:fs|fetch\(' scripts/links-internos-manifiesto.mjs`
      sale exit 0.
  </acceptance_criteria>
  <done>El universo de links internos está declarado en código y su exhaustividad PROBADA por igualdad contra las 77 refs del inventario 113, con exclusiones justificadas ref-por-ref y cero PII.</done>
</task>

<task type="auto">
  <name>Task 2: Runner reproducible (status + ausencia) con salida guardada</name>
  <files>scripts/verificar-links-internos.mjs</files>
  <read_first>
    - `scripts/validar-deeplinks.mjs` — patrón del repo: `parseArgs`, UA, sleep, tabla, exit code.
    - `scripts/links-internos-manifiesto.mjs` — el módulo creado en el Task 1.
    - `.planning/phases/114-link-int-links-internos-exhaustivos/114-VALIDATION.md` — §Test
      Infrastructure (comandos de corrida rápida y completa).
  </read_first>
  <action>
    Crear `scripts/verificar-links-internos.mjs`, ESM, CERO dependencias externas (usar el `fetch`
    global de Node ≥22 y `node:util` / `node:fs` / `node:os`).

    Antes de escribir el parseo de HTML, ejecutar la probe de resolución:
    `node -e "import('cheerio').then(()=>console.log('CHEERIO_OK')).catch(()=>console.log('CHEERIO_NO'))"`
    desde la raíz del repo. Si imprime `CHEERIO_OK`, usar cheerio para las aserciones de ancla y de
    ausencia (decisión del CONTEXT). Si imprime `CHEERIO_NO` (esperado: cheerio solo está declarado
    en packages/agenda|bio|lobby, no en la raíz), implementar la verificación por búsqueda sobre el
    HTML servido y DEJAR REGISTRADA la desviación en un comentario de cabecera del script citando la
    probe y su salida. No añadir cheerio al package.json raíz (paquete nuevo = fuera de alcance).

    Comportamiento del runner:
    - Recorre `MANIFIESTO` de forma SECUENCIAL (nunca en paralelo), con `await sleep(400)` entre
      requests (rango 300-500ms del CONTEXT; el Worker es propio pero jamás ráfagas). Enviar el UA
      identificatorio del repo en cada request y `redirect: "manual"` para no enmascarar un 404
      detrás de una redirección.
    - Cachea el HTML por URL de destino en un `Map` dentro de la corrida, para que N anclas sobre la
      misma página cuesten UN solo request (y descontar ese hit del delay).
    - Aserción por tipo:
      · `status` → PASS si `espera === "no-404"` y el status NO es 404; PASS si `espera === 404` y el
        status ES 404; en cualquier otro caso FAIL con el status observado como causa.
      · `ancla` → solicita `destino`, y PASS si el id aparece como **atributo `id` de un elemento**
        del HTML; si el status del destino no es 200, FAIL con causa `destino HTTP <n>`. Si el status
        es 200 pero el id no aparece, marcar `MISSING-SSR` (NO PASS ni FAIL definitivo): es candidato
        al fallback BrowserOS del Plan 02.
      · `ausencia` → solicita `origen`, y PASS si el patrón NO aparece en el HTML; FAIL si aparece
        (el gate estaría filtrando superficie).
    - Flags del contrato del runner (los cuatro son parte del contrato consumido por planes
      posteriores, no opcionales de implementar):
      · `--route <ruta>` — filtra el manifiesto por `origen` o `destino` (corrida rápida tras un fix).
      · `--tipo <status|ancla|ausencia>` — filtra por tipo de entrada; **requerido** por el Plan 02,
        que re-corre solo las anclas. Valor fuera de la lista cerrada ⇒ error de uso con exit 2.
      · `--out <basename>` — nombre base de los artefactos.
      · `--json-only` — suprime la tabla por stdout (el archivo `.txt` se escribe igual).
      `--route` y `--tipo` son componibles (se aplican en AND).
    - Salida: imprime una tabla legible por stdout (id, tipo, gate, href/destino, resultado, causa) y
      un resumen final con conteos por resultado (PASS / FAIL / MISSING-SSR). Escribe además dos
      archivos junto al `--out`: `<basename>.txt` (copia byte-fiel de lo impreso) y `<basename>.json`
      con la forma `{ meta, resultados }`, donde `resultados` es el array de
      `{ id, inventarioRef, tipo, gate, origen, destino, href, espera, status, resultado, causa }` y
      `meta` trae `base_url`, `iso_timestamp`, `total`, los conteos, los filtros aplicados y la
      salida de la probe de cheerio.
    - Exit code: 0 si no hay FAIL (los `MISSING-SSR` no fallan la corrida — se resuelven en el Plan
      02); 1 si hay al menos un FAIL; 2 ante error de uso. Esto hace del script un gate, no un
      reporte pasivo.
    - Cabecera del script con el comando exacto de la corrida completa, el de la corrida por ruta y
      el de la corrida por tipo.

    **Portabilidad LOCKED:** el script y todos sus comandos de ejemplo deben funcionar en Windows /
    Git Bash, donde `$TMPDIR` NO está definido. Para rutas temporales usar siempre `os.tmpdir()`
    desde Node — jamás `$TMPDIR` ni `process.env.TMPDIR`, que son `undefined` en este entorno y
    harían crashear el comando o escribir en la raíz del repo.
  </action>
  <verify>
    <automated>node -e "const os=require('node:os'),p=require('node:path'),fs=require('node:fs'),cp=require('node:child_process');const base=p.join(os.tmpdir(),'114-smoke');const r=cp.spawnSync(process.execPath,['scripts/verificar-links-internos.mjs','--route','/metodologia','--out',base],{encoding:'utf8'});if(r.status===2)throw new Error('error de uso --route: '+r.stderr);if(!fs.existsSync(base+'.json'))throw new Error('no escribio '+base+'.json');const j=JSON.parse(fs.readFileSync(base+'.json','utf8'));if(!j.meta||!Array.isArray(j.resultados))throw new Error('forma json invalida');if(j.meta.total!==j.resultados.length)throw new Error('meta.total no cuadra');const t=cp.spawnSync(process.execPath,['scripts/verificar-links-internos.mjs','--tipo','ancla','--json-only','--out',base+'-t'],{encoding:'utf8'});if(t.status===2)throw new Error('--tipo no soportado: '+t.stderr);const jt=JSON.parse(fs.readFileSync(base+'-t.json','utf8'));if(!jt.resultados.length)throw new Error('--tipo ancla devolvio 0');if(jt.resultados.some(x=>x.tipo!=='ancla'))throw new Error('--tipo no filtra');console.log('OK smoke exit='+r.status+' | route='+j.resultados.length+' | tipo-ancla='+jt.resultados.length)"</automated>
  </verify>
  <acceptance_criteria>
    - El smoke de `<verify>` sale exit 0; ni `--route` ni `--tipo` devuelven exit 2 (error de uso).
    - `--tipo ancla` devuelve un `.json` no vacío cuyas entradas son TODAS de tipo `ancla`.
    - Con `--out <base>` se crean exactamente `<base>.txt` y `<base>.json`; `meta.total` iguala el
      largo de `resultados`; cada resultado arrastra su `inventarioRef`.
    - Cero `$TMPDIR` en el script:
      `! grep -qE '\$TMPDIR|process\.env\.TMPDIR' scripts/verificar-links-internos.mjs` sale exit 0.
    - El script no importa ningún paquete fuera de `node:`* salvo cheerio bajo la probe, y salvo el
      import local `./links-internos-manifiesto.mjs`.
    - El delay está presente y es secuencial: `grep -c "await sleep(" scripts/verificar-links-internos.mjs` ≥ 1
      y `! grep -q "Promise.all" scripts/verificar-links-internos.mjs` sale exit 0.
    - El UA identificatorio aparece en el script (`grep -c "ObservatorioCongreso360"` ≥ 1).
    - La cabecera cita la probe de cheerio, su salida observada y los tres comandos de corrida.
  </acceptance_criteria>
  <done>Existe un comando único, reproducible, portable a Windows y sin dependencias nuevas, que verifica el universo del manifiesto, filtra por ruta y por tipo, y deja artefactos txt+json.</done>
</task>

<task type="auto">
  <name>Task 3: Corrida PRE-FIX completa contra el deploy real, con cobertura reconciliada</name>
  <files>.planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.txt, .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE.json</files>
  <read_first>
    - `scripts/verificar-links-internos.mjs` — el runner del Task 2.
    - `scripts/links-internos-manifiesto.mjs` — para conocer el total de entradas esperado.
    - `.planning/phases/113-inv-inventario-rector-de-superficies/113-INVENTARIO.md` §5 — los estados
      de gate OBSERVADOS (NET ON, CRUCES ON, VSIM ON, MONEY OFF, NOTIF OFF), que son la expectativa
      contra la que se leen los resultados.
  </read_first>
  <action>
    Ejecutar la corrida completa contra el deploy real y guardar los artefactos:
    `node scripts/verificar-links-internos.mjs --out .planning/phases/114-link-int-links-internos-exhaustivos/114-CORRIDA-PRE`

    Esta es la evidencia del estado ANTES de cualquier fix. NO corregir nada en esta tarea: si hay
    FAIL o MISSING-SSR, se registran y viajan al Plan 02 (triage de anclas) y al Plan 03 (fixes).

    Anexar al final de `114-CORRIDA-PRE.txt` un bloque `=== LECTURA DE LA CORRIDA ===` con:
    - el comando verbatim ejecutado y el timestamp de la corrida;
    - el exit code observado;
    - los conteos PASS / FAIL / MISSING-SSR;
    - **la reconciliación de cobertura, que se cierra AQUÍ, en Wave 1** (no se difiere a Wave 3):
      total de entradas corridas, y la confirmación explícita de que la unión de sus `inventarioRef`
      más los de `EXCLUIDOS` cubre las **77** refs del inventario. Si faltara alguna ref, el plan se
      detiene y se corrige el manifiesto — no se admite un "gap ABIERTO" que viaje a otra wave.
    - por cada FAIL, una línea `id | href | status | causa | hipótesis` — hipótesis en una frase,
      SIN corregir nada todavía;
    - por cada MISSING-SSR, una línea `id | ancla | destino` marcada como
      `candidato a fallback BrowserOS (Plan 02)`;
    - la confirmación de que los estados de gate observados en esta corrida coinciden con §5 del
      inventario; si alguno divergiera (p. ej. un `href="/contraparte/` presente con MONEY OFF), se
      declara como HALLAZGO DE RÉGIMEN en mayúsculas — nunca se toca ningún flag para "arreglarlo".

    Si la corrida no puede completarse contra el deploy (red caída, Worker 5xx sostenido), NO
    fabricar resultados: guardar la corrida parcial con su causa declarada y detener el plan
    reportando el bloqueo.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const d='.planning/phases/114-link-int-links-internos-exhaustivos/';const j=JSON.parse(fs.readFileSync(d+'114-CORRIDA-PRE.json','utf8'));if(!j.meta||!Array.isArray(j.resultados))throw new Error('forma json invalida');import('./scripts/links-internos-manifiesto.mjs').then(m=>{if(j.resultados.length!==m.MANIFIESTO.length)throw new Error('corrida incompleta: '+j.resultados.length+' de '+m.MANIFIESTO.length);const cub=new Set();for(const r of j.resultados)for(const x of(r.inventarioRef||[]))cub.add(x);for(const x of m.EXCLUIDOS)for(const r of x.inventarioRef)cub.add(r);const falt=m.REFS_INVENTARIO.filter(r=>!cub.has(r));if(falt.length)throw new Error('corrida no cubre refs ('+falt.length+'): '+falt.join(','));const t=fs.readFileSync(d+'114-CORRIDA-PRE.txt','utf8');if(t.indexOf('LECTURA DE LA CORRIDA')<0)throw new Error('falta bloque de lectura');if(/[0-9]{7,8}-[0-9kK]/.test(t))throw new Error('patron RUT en el txt');console.log('OK corrida',j.resultados.length,'resultados | cobertura 77/77');}).catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Existen `114-CORRIDA-PRE.txt` y `114-CORRIDA-PRE.json` en el dir de la fase.
    - El `.json` trae `meta.base_url === "https://observatorio-congreso.thevalis.workers.dev"`,
      `meta.iso_timestamp`, y un `resultados` cuyo largo es **igual** a `MANIFIESTO.length` (corrida
      completa, no muestra).
    - La unión de `inventarioRef` de los resultados más los `EXCLUIDOS` cubre las **77** refs — lo
      asevera el comando de `<verify>`, que nombra las faltantes si las hay.
    - El `.txt` contiene `=== LECTURA DE LA CORRIDA ===` con comando verbatim, exit code, los tres
      conteos y la reconciliación de cobertura cerrada en esta wave.
    - Cero fabricación: todo FAIL/MISSING-SSR aparece listado; ningún resultado sin status observado.
    - `git status --porcelain app/ packages/` → **vacío** (este plan NO toca código de producto).
    - Cero RUT en los artefactos:
      `! grep -hqE '[0-9]{7,8}-[0-9kK]' 114-CORRIDA-PRE.txt 114-CORRIDA-PRE.json` sale exit 0.
  </acceptance_criteria>
  <done>El estado PRE-FIX de todos los links internos está registrado, con la cobertura de las 77 refs reconciliada en Wave 1 y evidencia re-generable por comando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → deploy público | El script solo LEE HTML público del propio Worker; no envía credenciales ni cookies |
| resultados → artefacto commiteado | El HTML servido podría contener datos que no deben quedar en el repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Information disclosure | artefactos `114-CORRIDA-PRE.*` | mitigate | El JSON guarda status y causa, NUNCA el cuerpo HTML; gate `! grep -hqE` RUT sobre ambos artefactos |
| T-114-02 | Denial of service | Worker propio | mitigate | Corrida secuencial con `sleep(400)` + caché de HTML por URL; `Promise.all` prohibido (gate en acceptance) |
| T-114-03 | Tampering | estado de gates | accept | El script solo lee; el plan prohíbe explícitamente tocar flags y declara cualquier divergencia como hallazgo |
| T-114-11 | Repudiation | exhaustividad del universo | mitigate | `REFS_INVENTARIO` congelado (77) + compuerta de igualdad en Wave 1; un gap nombra las refs faltantes y falla el comando |
| T-114-SC | Tampering | npm installs | mitigate | CERO paquete nuevo: el runner solo usa `node:`* y el manifiesto local; cheerio se usa únicamente si YA resuelve, jamás se instala |
</threat_model>

<verification>
- Cobertura 77/77 probada por IGUALDAD (no por piso) sobre `MANIFIESTO ∪ EXCLUIDOS`.
- Runner smoke con `--route` y `--tipo` verde, usando `os.tmpdir()` (portable a Windows/Git Bash).
- Artefactos `114-CORRIDA-PRE.txt` / `.json` presentes, completos y bien formados.
- `git status --porcelain app/ packages/` vacío (fase sin fixes todavía).
- Cero RUT y cero credenciales en los artefactos.
</verification>

<success_criteria>
- El universo de links internos del inventario 113 está declarado y su exhaustividad PROBADA ref-por-ref.
- Un solo comando reproduce la verificación completa y deja salida guardada (SC#4 del ROADMAP).
- El estado PRE-FIX de cada link interno (status) y de cada patrón gated (ausencia) queda registrado.
- Los candidatos a verificación de ancla no-SSR quedan identificados para el Plan 02.
</success_criteria>

<output>
Create `.planning/phases/114-link-int-links-internos-exhaustivos/114-01-SUMMARY.md` when done
</output>
