{
  "name": "observatorio",
  "version": "0.0.0",
  "private": true,
  "description": "Observatorio del Congreso 360 — monorepo (app + packages compartidos)",
  "//tsx-raiz": "G10 (119-01): `tsx` está en devDependencies de la RAÍZ para que el script `freshness` resuelva el binario desde aquí. Vía (a) del plan; la vía (b) (`pnpm --filter @obs/freshness exec tsx src/cli.ts`) quedó DESCARTADA empíricamente: `pnpm --filter exec` mueve el cwd a packages/freshness y cli.ts:296 (`loadEnv(process.cwd())`) dejaría de encontrar el .env de la raíz (gotcha v8.1). Versión ^4.22.4 = la misma que ya resuelve packages/freshness y que ya está en pnpm-lock.yaml — NO es un paquete nuevo (T-119-SC).",
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "test": "pnpm -r --filter \"./packages/*\" test && pnpm --filter ./app test",
    "typecheck": "tsc -b",
    "lint": "echo \"(lint placeholder — configurar en fase posterior)\"",
    "//guards": "126 (review WR-06) + 133-05 (D-133-K3): entrypoint reproducible de los 20 guards de régimen = 11 (app) + 3 (@obs/dinero) + 3 (@obs/llm) + 3 (@obs/news, Phase 133-a). El script `guards` de app/ cubre SOLO los 11 de app/ y tiene guard-of-the-guards contra el filesystem (create-view-guard.test.ts §4); los 9 de packages/ se listan aquí por NOMBRE (D-13: jamás glob — `vitest run src/*guard*.test.ts` sale 0 sin correr nada).",
    "guards": "pnpm --filter ./app guards && pnpm --filter @obs/dinero exec vitest run src/name-match-rut-guard.behavior.test.ts src/reconciler-frozen-guard.test.ts src/servel-frozen-guard.test.ts && pnpm --filter @obs/llm exec vitest run src/integ-scope-guard.test.ts src/provider-guard.test.ts src/tiered-scope-guard.test.ts && pnpm --filter @obs/news exec vitest run src/eval/taxonomia-guard.test.ts src/eval/taxonomia-superficie-guard.test.ts src/eval/congelado.test.ts",
    "freshness": "tsx packages/freshness/src/cli.ts"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.8.3",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  }
}
